---
phase: 165-gem-move-elo-calibration-harness-restore-fen-analysis-deep-l
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/analysisUrl.ts
  - frontend/src/lib/analysisUrl.test.ts
  - frontend/src/pages/Analysis.tsx
autonomous: false
requirements: [SEED-094]

must_haves:
  truths:
    - "Opening `/analysis?fen=<url-encoded FEN>` loads that arbitrary mid-game position as a free-play root; the board shows it and the user can make moves from it (SEED-094 / D-06)."
    - "`?line=` still works unchanged; when both `?fen=` and `?line=` are present, precedence is game_id > fen > line (fen wins over line)."
    - "`buildAnalysisFenUrl` encodeURIComponent-encodes the FEN's spaces and `/`; `parseAnalysisFenParam` decodes + validates via chess.js, round-trips build→parse, and returns null for null/empty/garbage."
  artifacts:
    - "frontend/src/lib/analysisUrl.ts (new exports buildAnalysisFenUrl, parseAnalysisFenParam)"
    - "frontend/src/lib/analysisUrl.test.ts (extended with ?fen= build/parse/round-trip cases)"
    - "frontend/src/pages/Analysis.tsx (reads ?fen=, seeds free-play root, enforces precedence)"
  key_links:
    - "parseAnalysisFenParam validates the decoded FEN via chess.js — an invalid FEN degrades to null, so a hand-typed bad URL cannot crash the board."
    - "The new fen-seeding effect calls the EXISTING `loadMainLine([], rootFenSeed)` (empty sans → free-play root at that FEN); the line-effect guard gains `&& rootFenSeed === null` so fen wins deterministically."
---

<objective>
Restore an additive `?fen=<fen>` analysis deep-link so the harness TSV's arbitrary mid-game positions are clickable into `/analysis`. This is the interactive study half of the SEED-094 resource — additive alongside the existing start-anchored `?line=`, NOT a revert.

Purpose: SEED-094 / D-06. `?fen=` was previously removed in favor of `?line=` (which can seed a navigable line back to move 1); brilliant-dataset positions are arbitrary mid-game FENs with no move-list-to-start, so they need direct-snapshot loading.
Output: two new pure helpers in `analysisUrl.ts` with unit tests, and `?fen=` seeding wired into `Analysis.tsx` with deterministic precedence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-gem-move-elo-calibration-harness-restore-fen-analysis-deep-l/165-CONTEXT.md
@.planning/phases/165-gem-move-elo-calibration-harness-restore-fen-analysis-deep-l/165-RESEARCH.md

@frontend/src/lib/analysisUrl.ts
@frontend/src/lib/analysisUrl.test.ts
@frontend/src/pages/Analysis.tsx
</context>

<artifacts_this_phase_produces>
New exported symbols in `frontend/src/lib/analysisUrl.ts`:
- `buildAnalysisFenUrl(fen: string): string` — returns `/analysis?fen=<encodeURIComponent(fen)>`.
- `parseAnalysisFenParam(fenParam: string | null): string | null` — decode + chess.js-validate; null on invalid/empty.
- (private) `FEN_PARAM = 'fen'` constant.

Both new exports are consumed by `analysisUrl.test.ts` (both) and `Analysis.tsx` (parseAnalysisFenParam) so knip sees them as used.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add buildAnalysisFenUrl / parseAnalysisFenParam + unit tests</name>
  <files>frontend/src/lib/analysisUrl.ts, frontend/src/lib/analysisUrl.test.ts</files>
  <read_first>
    - frontend/src/lib/analysisUrl.ts (existing buildAnalysisLineUrl/parseAnalysisLineParam/buildGameAnalysisUrl shape; Chess is already imported; ANALYSIS_PATH + *_PARAM constants; the header doc comment stating ?fen= was replaced by ?line=)
    - frontend/src/lib/analysisUrl.test.ts (existing describe/it structure to mirror — round-trip, encoding-safety, null/empty, illegal-input cases)
  </read_first>
  <behavior>
    - buildAnalysisFenUrl(fen) → `/analysis?fen=` + encodeURIComponent(fen); a mid-game FEN's space encodes to `%20` and `/` to `%2F`.
    - parseAnalysisFenParam(null) → null; parseAnalysisFenParam('') → null.
    - parseAnalysisFenParam(garbage) → null (chess.js rejects → caught → null).
    - Round-trip: for a mid-game full FEN (with counters), parseAnalysisFenParam(new URLSearchParams(buildAnalysisFenUrl(fen).split('?')[1]).get('fen')) === fen.
  </behavior>
  <action>
    In `frontend/src/lib/analysisUrl.ts`: add a `const FEN_PARAM = 'fen';` alongside the existing param constants. Export `buildAnalysisFenUrl(fen: string): string` returning `` `${ANALYSIS_PATH}?${FEN_PARAM}=${encodeURIComponent(fen)}` `` (encodeURIComponent handles the FEN's spaces and `/`). Export `parseAnalysisFenParam(fenParam: string | null): string | null`: return null when falsy; otherwise `decodeURIComponent(fenParam)`, then validate by constructing `new Chess(fen)` inside a try/catch — return the fen on success, null on throw (defensive, mirroring parseAnalysisLineParam's tolerance). Update the module header doc comment: note that `?fen=` is now ADDITIVELY restored (per SEED-094 / D-06) for arbitrary mid-game snapshot loading, complementing `?line=` for start-anchored lines — do not describe it as a replacement or revert.

    In `frontend/src/lib/analysisUrl.test.ts`: add two `describe` blocks (`buildAnalysisFenUrl`, `parseAnalysisFenParam`) mirroring the existing line-param tests, covering the four behaviors above plus: buildAnalysisFenUrl output starts with `/analysis?fen=`; the encoded value contains no raw space or `/`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/analysisUrl && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/lib/analysisUrl` passes including the new `buildAnalysisFenUrl` / `parseAnalysisFenParam` describe blocks.
    - `cd frontend && npx tsc -b` exits 0 (new exports type-check; shared-type change surfaced).
    - The round-trip test asserts a mid-game full FEN survives build→parse unchanged, and garbage/null/empty each return null.
  </acceptance_criteria>
  <done>Two pure, unit-tested helpers exist; the module doc comment reflects the additive restoration.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ?fen= seeding + precedence into Analysis.tsx</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (line ~430 lineParam read; ~435-448 game_id/ply + isGameMode; ~283-291 fenToRootPly; ~656-673 the game-mode and free-play loadMainLine seeding effects; ~52 the analysisUrl import)
    - frontend/src/lib/analysisUrl.ts (parseAnalysisFenParam signature from Task 1)
    - frontend/src/hooks/useAnalysisBoard.ts (loadMainLine(sans, newRootFen) — empty sans seeds a free-play root at newRootFen; getPosition returns rootFen when currentNodeId===null)
  </read_first>
  <action>
    In `frontend/src/pages/Analysis.tsx`: extend the existing `@/lib/analysisUrl` import (line ~52) to also import `parseAnalysisFenParam`. Add `const fenParam = searchParams.get('fen');` near the existing `lineParam` read, and `const rootFenSeed = useMemo(() => parseAnalysisFenParam(fenParam), [fenParam]);`.

    Add a new seeding effect mirroring the free-play `?line=` effect (~668-673): guard `if (isGameMode || rootFenSeed === null || hasLoadedMainLine.current) return;`, set `hasLoadedMainLine.current = true`, call the EXISTING `loadMainLine([], rootFenSeed)` (empty sans → free-play root at the FEN — no new hook method needed), deps `[rootFenSeed, isGameMode]` with the same `eslint-disable-next-line react-hooks/exhaustive-deps` the sibling effects use.

    Enforce precedence game_id > fen > line: add `&& rootFenSeed === null` to the existing `?line=` seeding effect's guard so `?fen=` wins when both params are present (they share the `hasLoadedMainLine` ref, so without this guard effect-ordering decides the winner — RESEARCH Landmine 8). Do NOT touch the game-mode effect (game_id already wins via `isGameMode`). `fenToRootPly` already derives correct move numbering from an arbitrary FEN, so the variation tree / engine labels are correct at a mid-game root — no change needed there.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint && npm test -- --run && npx tsc -b` all exit 0 (lint incl. knip clean, existing Analysis tests still pass, types build).
    - The `?line=` seeding effect guard includes `rootFenSeed === null` (grep Analysis.tsx), making precedence deterministic.
    - `loadMainLine([], rootFenSeed)` is called from the new fen-seeding effect (no new hook method added to useAnalysisBoard).
  </acceptance_criteria>
  <done>`?fen=` seeds a free-play root additively; `?line=` unchanged; precedence game_id > fen > line is explicit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify ?fen= deep-link in the browser</name>
  <action>Human-only verification: exercise the additive `?fen=` deep-link in a real browser against the running dev server, per the how-to-verify steps below. No code changes — this gate confirms the mid-game FEN seeds a free-play root, `?line=` is unaffected, and a garbage FEN degrades gracefully.</action>
  <what-built>Additive `?fen=<fen>` analysis deep-link: opening the URL with an arbitrary mid-game FEN loads that position as a free-play root on `/analysis`, navigable and playable, with `?line=` unaffected.</what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev`.
    2. Take any full mid-game FEN (e.g. a row's `fen` from a harness TSV, or `3r2k1/p5pp/1p1Np1q1/2pRP3/5P2/P3n1PP/1PP3Q1/3R3K w - - 1 28`).
    3. Visit `http://localhost:5173/analysis?fen=<url-encoded FEN>` (URL-encode spaces as %20, `/` as %2F — or use the exact `analysis_url` column value from the TSV).
    4. Confirm: the board renders THAT position (not the start), move numbering reads mid-game, and you can make a legal move from it (forks a child off the root).
    5. Confirm an existing `?line=e2e4,e7e5,g1f3` URL still loads a start-anchored line navigable back to move 1.
    6. Confirm a garbage `?fen=not-a-fen` URL does not crash the page (falls back to the standard start / free play).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query (`?fen=`) → board seeding | Untrusted, user/link-controlled string decoded and used to seed the analysis board. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-03 | Denial of Service | parseAnalysisFenParam | medium | mitigate | chess.js-validate the decoded FEN inside try/catch; invalid → null → page falls back to free-play start (no crash). Unit-tested with garbage input. |
| T-165-04 | Tampering | ?fen= / ?line= double-seed | low | mitigate | Deterministic precedence via `rootFenSeed === null` guard on the line effect (shared hasLoadedMainLine ref) — effect ordering no longer decides the winner. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` all green.
- New `analysisUrl.test.ts` cases (build/parse/round-trip/garbage) pass.
- Human-verify: `?fen=` loads a mid-game root in the browser; `?line=` still works; garbage FEN does not crash.
</verification>

<success_criteria>
- `?fen=<encoded fen>` deep-links an arbitrary position into `/analysis` as a free-play root (makes the harness TSV links clickable).
- Additive: `?line=` behavior unchanged; precedence game_id > fen > line.
- knip/lint/types clean.
</success_criteria>

<output>
Create `.planning/phases/165-gem-move-elo-calibration-harness-restore-fen-analysis-deep-l/165-02-SUMMARY.md` when done.
</output>
