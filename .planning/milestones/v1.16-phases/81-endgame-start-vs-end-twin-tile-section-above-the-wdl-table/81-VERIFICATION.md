---
phase: 81-endgame-start-vs-end-twin-tile-section-above-the-wdl-table
verified: 2026-05-09T21:28:24Z
status: passed
score: 22/22 must-haves verified
overrides_applied: 0
---

# Phase 81: Endgame Start vs End — twin-tile section above the WDL table — Verification Report

**Phase Goal (from ROADMAP.md):** Add a new "Endgame Start vs End" section to the Endgame Overall Performance area of the Endgames page, positioned above the existing "Games with vs without Endgame" WDL table. The section renders two tiles via the existing MiniBulletChart: "where you start" (avg eval at endgame entry, sig-tested against 0) + "what you do with it" (absolute endgame score, sig-tested against 50%). Both tiles: p<0.05, three-state color. Phase 81 is purely additive.

**Verified:** 2026-05-09T21:28:24Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (D-01 .. D-22)

| #     | Truth (decision id)                                                                                                | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01  | Section sits above WDL table inside `showPerfSection`                                                              | VERIFIED    | `frontend/src/pages/Endgames.tsx:411` `<EndgameStartVsEndSection data={perfData} />` is between `</Accordion>` (line 410) and `<EndgamePerformanceSection>` (line 413). Page-level test `Endgames.startVsEnd.test.tsx::it('renders EndgameStartVsEndSection before EndgamePerformanceSection (D-01)')` asserts `compareDocumentPosition` order — GREEN.                |
| D-02  | Section heading style matches Endgame Overall Performance                                                          | VERIFIED (with note) | Parent `<h2>Endgame Overall Performance</h2>` at `Endgames.tsx:343` covers the section. Tile titles use `<h3>` ("Where you start" / "What you do with it") matching the inner-tile pattern from ExplorerTab. The literal string "Endgame Start vs End" does not appear as a separate sub-heading — see Note 1 below; UAT explicitly approved the rendered hierarchy.   |
| D-03  | No standalone lead paragraph; no section-level info popover                                                        | VERIFIED    | `EndgameStartVsEndSection.tsx` renders only the 2-col grid with two tiles inside `<section>` — no lead `<p>` or info-popover trigger at the section root.                                                                                                                                                                                                              |
| D-04  | Two tiles side-by-side on lg+, stacked on mobile                                                                   | VERIFIED    | `EndgameStartVsEndSection.tsx:95` `<div className="grid grid-cols-1 lg:grid-cols-2 gap-4">`.                                                                                                                                                                                                                                                                          |
| D-05  | n ≥ 10 gate applied consistently to both tiles                                                                     | VERIFIED    | `endgame_service.py:1696` `entry_eval_p_value: float | None = p_eval_raw if eval_n >= 10 else None`; `:1712` `endgame_score_p_value` gated on `endgame_wdl.total >= 10`. Frontend `EndgameStartVsEndSection.tsx:59` `deriveLevel` returns `'low'` when `n < MIN_GAMES_FOR_RELIABLE_STATS`.                                                                          |
| D-06  | n<10 placeholder per tile; section hidden when zero endgame games                                                  | VERIFIED    | `EndgameStartVsEndSection.tsx:142-144` and `:188-190` render `<p>Not enough data yet</p>` when `showTile1Chart` / `showTile2Chart` is false. Section hidden via `showPerfSection` predicate at `Endgames.tsx:341`. Test `does NOT render the start-vs-end section when no endgame games (showPerfSection false)` asserts hide-when-empty — GREEN.                       |
| D-07  | Tile 1: one-sample Wald-z test of mean against 0; mate scores excluded                                             | VERIFIED    | `endgame_service.py:1690` calls `compute_eval_confidence_bucket(eval_sum, eval_sumsq, eval_n)` (Wald-z helper). Mate exclusion at `:1677` `if row.eval_mate is not None: continue  # mate-excluded per D-07`. Tested in `TestEntryEvalAggregation::test_mate_exclusion`.                                                                                                |
| D-08  | Tile 2: Wilson score test against 50%                                                                              | VERIFIED    | `endgame_service.py:1709` `_conf_s, p_score_raw, _se = compute_confidence_bucket(endgame_wdl.wins, endgame_wdl.draws, endgame_wdl.losses, endgame_wdl.total)` — the project's Wilson helper. Tested in `TestEntryEvalAggregation::test_endgame_score_p_value_gated_below_n_ten`.                                                                                       |
| D-09  | Three-state color: sig+ → green, sig− → red, otherwise neutral                                                     | VERIFIED    | `EndgameStartVsEndSection.tsx:70-71`: `evalShowZoneFontColor = isConfident(evalLevel) && evalIsInColoredZone; evalColor = evalShowZoneFontColor ? evalZoneHex : undefined`. Same gating for Tile 2 at `:83-84`. Component tests cover all 6 color-zone permutations (sig+/sig−/non-sig × pos/neg/neutral-band).                                                         |
| D-10  | Significance threshold p < 0.05                                                                                    | VERIFIED    | `EndgameStartVsEndSection.tsx:48-49`: `CONFIDENCE_HIGH_MAX_P = 0.01; CONFIDENCE_MEDIUM_MAX_P = 0.05`. `deriveLevel` returns `'low'` when `p >= 0.05`, and `isConfident('low') === false`, so the value is unstyled at p ≥ 0.05.                                                                                                                                         |
| D-11  | EndgamePerformanceResponse gains 6 new fields                                                                      | VERIFIED    | Backend `app/schemas/endgames.py:124-140`: `entry_eval_mean_pawns`, `entry_eval_n`, `entry_eval_p_value`, `endgame_score_p_value`, `entry_eval_ci_low_pawns`, `entry_eval_ci_high_pawns`. Frontend `frontend/src/types/endgames.ts:63-68` mirrors the same six. API contract test `test_endgames_router.py::test_overview_includes_start_vs_end_fields` asserts presence + types. |
| D-12  | Aggregation reuses existing endgame query path; no new SQL pipeline                                                | VERIFIED    | `endgame_service.py:1645-1726` — single new aggregation block. No new SQL added. (D-22 amendment switches the source from `query_endgame_entry_rows` to `query_endgame_bucket_rows` — both already existed.)                                                                                                                                                          |
| D-13  | Two new accordion paragraphs (Endgame entry eval + Endgame score) in the existing concepts accordion               | VERIFIED    | `Endgames.tsx:383-400` has both `<p><strong>Endgame entry eval:</strong>...</p>` and `<p><strong>Endgame score:</strong>...</p>` inside the `endgame-concepts-trigger` `<AccordionContent>`. The "Opponent Strength filter" literal text appears at line 395. Tests assert text presence — GREEN.                                                                       |
| D-14  | New paragraphs placed AFTER Conv/Parity/Recovery and BEFORE rating-changes caveat                                  | VERIFIED    | `Endgames.tsx`: Recovery `<p>` ends line 382; Endgame entry eval `<p>` starts line 383-391; Endgame score `<p>` line 392-400; rating-changes caveat `<p>` line 401-407. Page-level test `places the 2 new accordion paragraphs AFTER Recovery and BEFORE the rating-changes caveat (D-14)` asserts the index order — GREEN.                                            |
| D-15  | Pawn-axis domain ±2.0 pawns, neutral band ±0.75                                                                    | VERIFIED    | `frontend/src/lib/endgameEntryEvalZones.ts:20-26`: `ENDGAME_ENTRY_EVAL_NEUTRAL_MIN_PAWNS = -0.75`, `ENDGAME_ENTRY_EVAL_NEUTRAL_MAX_PAWNS = 0.75`, `ENDGAME_ENTRY_EVAL_DOMAIN_PAWNS = 2.0`. Used by `EndgameStartVsEndSection.tsx:131-134`. Zone-color tests assert boundaries.                                                                                          |
| D-16  | Tile 2 reuses existing Openings score-bullet constants                                                             | VERIFIED    | `EndgameStartVsEndSection.tsx:32-39` imports `SCORE_BULLET_CENTER`, `SCORE_BULLET_NEUTRAL_MAX`, `SCORE_BULLET_NEUTRAL_MIN`, `clampScoreCi`, `scoreBulletDomain`, `scoreZoneColor` from `@/lib/scoreBulletConfig`. Used in `MiniBulletChart` props at `:177-180`.                                                                                                          |
| D-17  | Mobile stacking: entry-eval first (chronological setup → execution)                                                | VERIFIED    | `EndgameStartVsEndSection.tsx`: Tile 1 (`tile-entry-eval`) renders before Tile 2 (`tile-endgame-score`) in DOM order (lines 96-145 vs 147-191). Component test asserts DOM order. With `grid-cols-1` at sub-lg, natural flow stacks entry-eval first.                                                                                                                  |
| D-18  | No clock-diff pairing introduced into this section                                                                 | VERIFIED    | `EndgameStartVsEndSection.tsx` makes no reference to clock-diff / time-pressure / clock-pressure. Search `grep clock` finds zero matches.                                                                                                                                                                                                                              |
| D-19  | No per-TC stratification of entry eval (TC-pooled response)                                                        | VERIFIED    | Backend `EndgamePerformanceResponse` carries scalar `entry_eval_*` fields (no per-TC mapping). Frontend consumes single scalars from `data.entry_eval_*`.                                                                                                                                                                                                              |
| D-20  | No distribution / histogram view of per-game evals                                                                 | VERIFIED    | `EndgameStartVsEndSection.tsx` renders only `MiniBulletChart` (single mean + CI whisker). No histogram, no per-game distribution.                                                                                                                                                                                                                                      |
| D-21  | WDL table + Score Gap column + score-over-time chart unchanged                                                     | VERIFIED    | `Endgames.tsx:412-414` `<EndgamePerformanceSection data={perfData} scoreGap={scoreGapData} />` rendered unmodified. The only edit to `EndgamePerformanceSection.tsx` was the additive `data-testid="endgame-performance-section"` wrapper attribute (line 108) — no behavioral change. Page-level test asserts `perf-wdl-table`/`perf-wdl-cards`, `score-gap-difference`, and `endgame-score-timeline-chart` testids still present — GREEN. |
| D-22  | Entry-eval aggregation consumes bucket_rows (UAT amendment)                                                        | VERIFIED    | `endgame_service.py:1763` (`get_endgame_performance`) and `:2086` (`get_endgame_overview`) both call `query_endgame_bucket_rows(...)` and pass to `_get_endgame_performance_from_rows`. The function signature param is `bucket_rows` (line 1648), no per-game dedupe loop (one row per game by SQL construction). Docstring at `:1652-1664` codifies the amendment. |

**Score:** 22/22 truths verified

### Required Artifacts

| Artifact                                                          | Expected                                              | Status      | Details                                                                                                                                                              |
| ----------------------------------------------------------------- | ----------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/schemas/endgames.py`                                         | EndgamePerformanceResponse with 6 new fields          | VERIFIED    | Lines 124-140: all 6 fields present with safe Pydantic defaults.                                                                                                     |
| `app/services/endgame_service.py`                                 | Aggregation in `_get_endgame_performance_from_rows`   | VERIFIED    | Lines 1645-1726. Wald-z + Wilson + sign-flip + mate exclusion + n≥10 gating. Both call sites converted to bucket_rows source.                                        |
| `frontend/src/types/endgames.ts`                                  | EndgamePerformanceResponse interface mirror           | VERIFIED    | Lines 63-68 mirror backend.                                                                                                                                          |
| `frontend/src/lib/endgameEntryEvalZones.ts`                       | Zone constants (±2.0 domain, ±0.75 band) + helper     | VERIFIED    | All four constants and `endgameEntryEvalZoneColor` exported. 7 unit tests cover boundaries.                                                                          |
| `frontend/src/components/charts/EndgameStartVsEndSection.tsx`     | Twin-tile section component                           | VERIFIED    | 195 LOC, single component. Composes MiniBulletChart + BulletConfidencePopover + ScoreConfidencePopover. 14 component tests GREEN.                                    |
| `frontend/src/pages/Endgames.tsx`                                 | Section wired between Accordion and WDL section       | VERIFIED    | Import line 22; render line 411. Two new accordion `<p>` blocks at lines 383-400 in correct order.                                                                   |
| `frontend/src/components/charts/EndgamePerformanceSection.tsx`    | Additive `data-testid="endgame-performance-section"`  | VERIFIED    | Line 108. Pure addition; no behavioral change.                                                                                                                       |
| `tests/test_endgame_service.py`                                   | TestEntryEvalAggregation class                        | VERIFIED    | Class at line 3800; tests for empty, n<10 gate, n=10 boundary, sign-flip, mate exclusion, NULL exclusion, endgame-score gate.                                       |
| `tests/test_endgames_router.py`                                   | API contract test for 6 new keys                      | VERIFIED    | Asserts presence + types of all 6 new keys via /api/endgames/overview.                                                                                                |
| `frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx` | 14 component tests covering layout/color/placeholder | VERIFIED    | All GREEN; 14 tests run from this file.                                                                                                                              |
| `frontend/src/lib/__tests__/endgameEntryEvalZones.test.ts`        | Zone-color helper boundary tests                      | VERIFIED    | 7 boundary tests cover constants + zone-color logic.                                                                                                                 |
| `frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx`       | Page-level integration tests                          | VERIFIED    | 6 tests covering DOM order (D-01), hide-when-empty (D-06), accordion paragraphs (D-13, D-14), D-21 negative scope, "Opponent Strength filter" plain text. All GREEN. |

### Key Link Verification

| From                                                            | To                                                                | Via                                                            | Status | Details                                                                                       |
| --------------------------------------------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| `endgame_service.py::_get_endgame_performance_from_rows`        | `eval_confidence.compute_eval_confidence_bucket`                  | import line 66 + call line 1690                                | WIRED  | Wald-z helper called with `(eval_sum, eval_sumsq, eval_n)`.                                  |
| `endgame_service.py::_get_endgame_performance_from_rows`        | `score_confidence.compute_confidence_bucket`                      | import line 68 + call line 1709                                | WIRED  | Wilson helper called with `(wins, draws, losses, total)`.                                     |
| `endgame_service.py::get_endgame_performance`                   | `query_endgame_bucket_rows`                                       | call line 1763 + result passed to `_get_endgame_performance_from_rows` line 1775 | WIRED  | D-22 amendment: bucket_rows is the entry-eval source.                                         |
| `endgame_service.py::get_endgame_overview`                      | `query_endgame_bucket_rows`                                       | call line 2086 + result passed to `_get_endgame_performance_from_rows` line 2098 | WIRED  | Both top-level orchestrators converted.                                                       |
| `EndgameStartVsEndSection.tsx`                                  | `MiniBulletChart`                                                 | import line 21 + render lines 129, 175                          | WIRED  | Both tiles render the chart with appropriate prop sets.                                       |
| `EndgameStartVsEndSection.tsx`                                  | `BulletConfidencePopover`                                         | import line 22 + render line 115 (Tile 1)                       | WIRED  | Tile 1 popover wired with `level`, `pValue`, `gameCount`, `evalMeanPawns`, `color="white"`.    |
| `EndgameStartVsEndSection.tsx`                                  | `ScoreConfidencePopover`                                          | import line 23 + render line 166 (Tile 2)                       | WIRED  | Tile 2 popover wired with `level`, `pValue`, `score`, `gameCount`.                             |
| `EndgameStartVsEndSection.tsx`                                  | `endgameEntryEvalZones` constants + helper                        | import lines 25-31 + use at 68, 131-134                         | WIRED  | All four constants + `endgameEntryEvalZoneColor` consumed.                                    |
| `EndgameStartVsEndSection.tsx`                                  | `scoreBulletConfig`                                               | import lines 32-39 + use at 81, 177-180                         | WIRED  | `SCORE_BULLET_CENTER`, neutral bounds, `scoreBulletDomain`, `clampScoreCi`, `scoreZoneColor`. |
| `Endgames.tsx`                                                  | `EndgameStartVsEndSection`                                        | import line 22 + render line 411                                | WIRED  | Inside `showPerfSection`, between `</Accordion>` and `<EndgamePerformanceSection>`.           |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable     | Source                                                                                                  | Produces Real Data | Status   |
| --------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------- | ------------------ | -------- |
| `EndgameStartVsEndSection.tsx`    | `data` (prop)     | `Endgames.tsx::perfData` from `useEndgameOverview()` query → `/api/endgames/overview` → `EndgamePerformanceResponse` populated by `_get_endgame_performance_from_rows` over `query_endgame_bucket_rows` (real DB) | Yes              | FLOWING  |
| `Endgames.tsx::perfData`          | `perfData`        | `useEndgameOverview` hook returns real backend response when game data exists; section is gated by `showPerfSection = !!(perfData && perfData.endgame_wdl.total > 0)` | Yes              | FLOWING  |
| `EndgamePerformanceResponse` (backend) | new fields populated | Real Postgres query via `query_endgame_bucket_rows` (one row per game with eval_cp / eval_mate / user_color) feeds the aggregation | Yes              | FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                                    | Command                                                                                                                                                  | Result          | Status |
| --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------ |
| Backend service + router tests for phase 81                                 | `uv run pytest tests/test_endgame_service.py tests/test_endgames_router.py -x -q`                                                                        | 227 passed      | PASS   |
| Frontend phase-81 component / zone / page tests                              | `npm test -- --run src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx src/lib/__tests__/endgameEntryEvalZones.test.ts src/pages/__tests__/Endgames.startVsEnd.test.tsx` | 26/26 passed   | PASS   |
| D-22 invariant `entry_eval_n + mate_excluded == endgame_wdl.total`           | Manual UAT against user 28 reported 2918 + 42 = 2960 = WDL total                                                                                          | Holds            | PASS (per UAT) |

### Requirements Coverage (D-01 .. D-22 from 81-CONTEXT.md)

All 22 decision IDs are accounted for in the Observable Truths table above. No REQUIREMENTS.md exists for this project; the canonical decisions are codified in `.planning/phases/81-.../81-CONTEXT.md`.

| Requirement | Source Plan(s)         | Description (short)                                                | Status     | Evidence                                                                          |
| ----------- | ---------------------- | ------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------- |
| D-01        | 81-03, 81-04           | Section above WDL table, additive                                  | SATISFIED  | `Endgames.tsx:411`                                                                |
| D-02        | 81-03                  | Section heading style                                              | SATISFIED (note) | `Endgames.tsx:343` `<h2>Endgame Overall Performance</h2>` covers (Note 1).      |
| D-03        | 81-03                  | No standalone lead paragraph / section info popover                | SATISFIED  | `EndgameStartVsEndSection.tsx`                                                    |
| D-04        | 81-03                  | Side-by-side desktop / stacked mobile                              | SATISFIED  | `EndgameStartVsEndSection.tsx:95`                                                 |
| D-05        | 81-01, 81-03           | n ≥ 10 gate                                                        | SATISFIED  | Backend gate + frontend `deriveLevel`                                             |
| D-06        | 81-03                  | Sparse placeholders                                                | SATISFIED  | `EndgameStartVsEndSection.tsx:142`, `:188`; `showPerfSection` gates section      |
| D-07        | 81-01                  | Wald-z entry-eval test                                             | SATISFIED  | `endgame_service.py:1690`                                                         |
| D-08        | 81-01                  | Wilson score test                                                  | SATISFIED  | `endgame_service.py:1709`                                                         |
| D-09        | 81-03                  | Three-state color                                                  | SATISFIED  | `EndgameStartVsEndSection.tsx:70-71`, `:83-84`                                    |
| D-10        | 81-01, 81-03           | p < 0.05 threshold                                                 | SATISFIED  | `CONFIDENCE_MEDIUM_MAX_P = 0.05`                                                  |
| D-11        | 81-01, 81-02           | Schema + 6 new fields                                              | SATISFIED  | Backend + frontend interfaces match                                               |
| D-12        | 81-01                  | Reuse existing query path                                          | SATISFIED  | No new SQL pipeline                                                               |
| D-13        | 81-04                  | Two new accordion paragraphs                                       | SATISFIED  | `Endgames.tsx:383-400`                                                            |
| D-14        | 81-04                  | Paragraph order                                                    | SATISFIED  | After Recovery, before rating-changes caveat                                      |
| D-15        | 81-02                  | ±2.0 domain, ±0.75 band                                            | SATISFIED  | `endgameEntryEvalZones.ts:20-26`                                                  |
| D-16        | 81-03                  | Reuse score-bullet constants                                       | SATISFIED  | `EndgameStartVsEndSection.tsx:32-39`                                              |
| D-17        | 81-03                  | Mobile order: entry-eval first                                     | SATISFIED  | DOM order Tile 1 → Tile 2                                                         |
| D-18        | 81-04                  | No clock-diff pairing                                              | SATISFIED  | No clock references                                                               |
| D-19        | 81-03, 81-04           | No per-TC stratification                                           | SATISFIED  | Scalar fields                                                                     |
| D-20        | 81-04                  | No histogram                                                       | SATISFIED  | Single mean + CI whisker only                                                     |
| D-21        | 81-04                  | Existing WDL/score-gap/timeline unchanged                          | SATISFIED  | Only additive testid added; behavior unchanged                                    |
| D-22        | 81-CONTEXT (UAT amend) | bucket_rows source                                                 | SATISFIED  | `endgame_service.py:1763, 2086`                                                   |

### Anti-Patterns Found

| File                                                            | Line  | Pattern                                | Severity | Impact                                                                                                                                                          |
| --------------------------------------------------------------- | ----- | -------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `EndgameStartVsEndSection.tsx`                                  | 142, 188 | Fallback `<p>Not enough data yet</p>` | Info     | Intentional sparse-state placeholder per D-06; not a stub.                                                                                                      |
| `endgame_service.py`                                            | 1677, 1679 | `# ty: ignore[unresolved-attribute]` for `Row` access | Info | Existing project-wide pattern for SQLAlchemy `Row` attribute access; documented in CLAUDE.md ty-compliance section.                                              |

No TODO / FIXME / placeholder comments introduced by this phase. No hardcoded empty data. No `console.log`-only handlers.

### Notes

**Note 1 — D-02 sub-heading "Endgame Start vs End":** The plan-frontmatter must-have for D-02 expected `<h3>Endgame Start vs End</h3>` rendered inside the section. The implemented `EndgameStartVsEndSection` instead relies on the existing parent `<h2>Endgame Overall Performance</h2>` (line 343 of `Endgames.tsx`) and uses two `<h3>` titles for the inner tile labels ("Where you start", "What you do with it"). UAT (Plan 05) explicitly approved this rendered hierarchy as PASS for "Section placement (D-01)". The locked-CONTEXT decision D-02 says the section heading style "matches the existing `Endgame Overall Performance` heading style on the page" — the implementation uses that heading directly, treating the new content as a sub-block under it. This is a soft divergence from the plan's literal-text wording but consistent with the design intent and explicitly accepted by the user during UAT. Not a blocker.

### Gaps Summary

No gaps. All 22 decision IDs (D-01..D-22) are observably true in the codebase. Manual UAT was approved with two amendments folded back (D-22 entry-eval source switch and UI polish for label rename + mobile chart stacking). All automated tests (backend 227/227 phase-81-relevant, frontend 26/26 phase-81-relevant) pass.

---

_Verified: 2026-05-09T21:28:24Z_
_Verifier: Claude (gsd-verifier)_
