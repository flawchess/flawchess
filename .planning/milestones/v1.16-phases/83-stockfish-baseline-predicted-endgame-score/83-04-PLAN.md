---
phase: 83
plan: 04
type: execute
wave: 3
depends_on: [83-02]
files_modified:
  - .claude/skills/benchmarks/SKILL.md
  - reports/benchmarks-2026-05-11.md
  - app/services/endgame_zones.py
  - frontend/src/generated/endgameZones.ts
autonomous: false
requirements: [D-14, D-15, D-16]
must_haves:
  truths:
    - "A new section in .claude/skills/benchmarks/SKILL.md documents the canonical entry_expected_score CTE (selected_users + endgame_game_ids + rows + per_user + per_user_excl_sparse) including bic.status='completed' filter and (2400, classical) sparse-cell exclusion"
    - "A new reports/benchmarks-YYYY-MM-DD.md section records the 5x4 cell table, TC and ELO marginals, pooled overall, recommendations, and collapse verdict"
    - "ENTRY_EXPECTED_SCORE_ZONES is registered in app/services/endgame_zones.py with direction='higher_is_better' and bounds chosen per editorial-judgement principle (D-15, memory feedback_zone_band_judgement.md)"
    - "frontend/src/generated/endgameZones.ts is regenerated and exports entryExpectedScoreZoneColor + ENTRY_EXPECTED_SCORE_NEUTRAL_MIN + ENTRY_EXPECTED_SCORE_NEUTRAL_MAX"
    - "'entry_expected_score' is added to the MetricId Literal in endgame_zones.py"
    - "CI drift check passes: git diff --exit-code on frontend/src/generated/endgameZones.ts after running scripts/gen_endgame_zones_ts.py"
  artifacts:
    - path: ".claude/skills/benchmarks/SKILL.md"
      provides: "New section X documenting the entry_expected_score CTE + per-cell metric definition + per-user aggregation"
      contains: "entry_expected_score"
    - path: "reports/benchmarks-2026-05-11.md"
      provides: "New section X with 5x4 cell grid, marginals, pooled overall, recommendations, collapse verdict, heatmap"
      contains: "Stockfish-baseline expected score at endgame entry"
    - path: "app/services/endgame_zones.py"
      provides: "ENTRY_EXPECTED_SCORE_ZONES ZoneSpec entry + entry_expected_score in MetricId Literal"
      contains: "entry_expected_score"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "Regenerated zone helper exports for entry_expected_score"
      contains: "entryExpectedScoreZoneColor"
  key_links:
    - from: "app/services/endgame_zones.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "scripts/gen_endgame_zones_ts.py codegen pipeline"
      pattern: "scripts/gen_endgame_zones_ts.py"
    - from: ".claude/skills/benchmarks/SKILL.md"
      to: "reports/benchmarks-2026-05-11.md"
      via: "SKILL.md operator runbook references reports for the locked band"
      pattern: "reports/benchmarks-"
---

<objective>
Run a formal `/benchmarks` calibration for the new `entry_expected_score` metric, lock the resulting cohort band into `app/services/endgame_zones.py`, and regenerate the frontend zone helper file so Plan 3 can import `entryExpectedScoreZoneColor`. This is the same calibration pipeline used for Phase 82's `entry_eval_pawns` band (`reports/benchmarks-2026-05-10.md` Section 3).

Purpose: D-14 locks the band source as formal calibration over a v1 shortcut so the new bullet stays consistent with Phase 82's bands and tile/LLM agree from day one. D-15 invokes the editorial-judgement principle (memory `feedback_zone_band_judgement.md`): if the pooled IQR is too wide for the metric to ever land in green/red, tighten inside IQR so meaningful patterns surface. D-16 requires the frontend codegen step so CI drift detection works.

Output: SKILL.md extended with the new metric's section, a new `reports/benchmarks-2026-05-11.md` report, `ENTRY_EXPECTED_SCORE_ZONES` in the Python registry, regenerated `endgameZones.ts`.

This plan is `autonomous: false` because the band tightening (D-15) is a judgement call requiring human approval, and final wording for the SKILL.md narrative can iterate. Plan 3 depends on this plan completing first (the regenerated TS file is its zone-helper source).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/benchmarks/SKILL.md
</execution_context>

<context>
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-02-SUMMARY.md

@.claude/skills/benchmarks/SKILL.md
@reports/benchmarks-2026-05-10.md
@app/services/endgame_zones.py
@scripts/gen_endgame_zones_ts.py
@frontend/src/generated/endgameZones.ts
@app/services/eval_utils.py

<interfaces>
Per-game expected_score expression for the benchmark CTE (PATTERNS.md SKILL.md section):

```sql
CASE
  WHEN entry_eval_mate IS NOT NULL AND (entry_eval_mate * color_sign) > 0 THEN 1.0
  WHEN entry_eval_mate IS NOT NULL AND (entry_eval_mate * color_sign) < 0 THEN 0.0
  WHEN entry_eval_cp IS NOT NULL AND abs(entry_eval_cp) < 2000
       THEN 1.0 / (1.0 + exp(-0.00368208 * (entry_eval_cp * color_sign)))
  ELSE NULL
END
```

where `color_sign = CASE WHEN g.user_color = 'white' THEN 1 ELSE -1 END` (same shape used in SKILL.md Section 2 for Conv/Recov).

ZoneSpec shape to register (bounds filled by Task 2 based on Task 1 outputs):

```python
"entry_expected_score": ZoneSpec(
    # Phase 83 D-14: pooled benchmark calibration from reports/benchmarks-YYYY-MM-DD.md section X.
    # Collapse verdict: TC d_max=<>, ELO d_max=<> -> single global band.
    # Editorial: <if tightened, cite IQR vs band rationale per memory feedback_zone_band_judgement.md>.
    typical_lower=<TBD>,
    typical_upper=<TBD>,
    direction="higher_is_better",
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend benchmarks/SKILL.md with the entry_expected_score section and produce the report</name>
  <files>.claude/skills/benchmarks/SKILL.md, reports/benchmarks-2026-05-11.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (full file; especially Section 0 "Endgame score" at lines 310-419 — the analog to mirror)
    - reports/benchmarks-2026-05-10.md (Section 3 entry_eval_pawns) — analog format
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (sections ".claude/skills/benchmarks/SKILL.md" and "reports/benchmarks-YYYY-MM-DD.md (NEW)")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-14, D-15)
    - Memory: feedback_wilson_chess_score.md (no methodology editorial in SKILL narrative)
    - Memory: project_benchmark_outliers_unfiltered.md (must filter bic.status='completed' AND exclude (2400, classical) sparse cell)
  </read_first>
  <action>
    Step 1: Verify the benchmark DB is up: `bin/benchmark_db.sh start`. Confirm read-only role connectivity via MCP `flawchess-benchmark-db`.

    Step 2: Add a new section to `.claude/skills/benchmarks/SKILL.md` modelled exactly on Section 0 ("Endgame score" at lines 310-419). Section title: "Section X. Stockfish-baseline expected score at endgame entry". Reproduce the canonical CTE pattern:
      - `selected_users` CTE joining `benchmark_selected_users` -> `benchmark_ingest_checkpoints` (with `bic.status='completed'`) -> `users` -> on `lichess_username`
      - `endgame_game_ids` CTE selecting `game_id` from `game_positions` where `endgame_class IS NOT NULL` GROUP BY `game_id` HAVING count(*) >= 6
      - `rows` CTE joining `games` -> `endgame_game_ids` -> the entry-eval span row, applying the equal-footing rating filter `abs(opp_rating - user_rating) <= 100`, computing `color_sign` from `user_color`, and emitting the per-game `expected_score` via the CASE expression in the Interfaces block
      - `per_user` CTE: per (user_id, rating_bucket, tc_bucket) aggregate AVG(expected_score) WHERE expected_score IS NOT NULL plus a per-user game count
      - `per_user_excl_sparse` CTE: exclude the (rating_bucket=2400, tc_bucket='classical') sparse cell per the universal SKILL convention (memory `project_benchmark_outliers_unfiltered.md`)
      - Final SELECT pivot producing the 5x4 cell table (p25 / p50 / p75 (n)), TC marginal, ELO marginal, pooled overall (n_users, mean, p05/p25/p50/p75/p95), and a per-cell heatmap of per-user p50 across (ELO x TC)

    Use the same per-cell formatting as Section 0 (`p25 / p50 / p75 (n)` with `*` footnote for sparse cells). Keep section narrative descriptive (no Wilson methodology editorial per memory `feedback_wilson_chess_score.md`).

    Step 3: Execute the canonical CTE against `flawchess-benchmark-db` (MCP read-only query). Capture outputs.

    Step 4: Create `reports/benchmarks-2026-05-11.md` (date stamp = today, per CONTEXT.md). Mirror `reports/benchmarks-2026-05-10.md` Section 3 layout exactly:
      1. Section header: "## X. Stockfish-baseline expected score at endgame entry"
      2. One-line definition
      3. "### Currently set in code" — blank for now (will be filled in by Task 2)
      4. "### Cell table — per-user expected_score p25 / p50 / p75 (n)" — 5x4 grid (ratings 1200/1600/2000/2400 x TCs bullet/blitz/rapid/classical, with sparse cell footnoted)
      5. "### TC marginal (excl. sparse)" — 4 rows
      6. "### ELO marginal (excl. sparse)" — 5 rows
      7. "### Pooled overall" — single row with `n_users / mean / p05 / p25 / p50 / p75 / p95`
      8. "### Recommendations" — bullet list including (a) sanity check on equal-footing filter (the pooled p50 should be slightly above 0.5 since equal-footing users score slightly above 50%; if it lands at ~0.48-0.52, the filter is working), (b) proposed cohort neutral band from pooled [p25, p75], (c) editorial tightening recommendation per D-15 if pooled IQR is too wide for the metric to land in green/red zones at realistic user populations (memory `feedback_zone_band_judgement.md`)
      9. "### Collapse verdict" — TC d_max + ELO d_max with the canonical < 0.2 collapse / 0.2-0.5 review / >= 0.5 keep-separate thresholds; if any d_max >= 0.5, recommend keeping per-ELO bands (defer per CONTEXT.md "Deferred Ideas" but document)

    Step 5: Present the report draft + the recommended `(typical_lower, typical_upper)` pair to the user for approval (Task 2 is gated on this).
  </action>
  <verify>
    <automated>test -f reports/benchmarks-2026-05-11.md && grep -c "entry_expected_score\|Stockfish-baseline expected score" reports/benchmarks-2026-05-11.md && grep -c "Section X\|## .* Stockfish-baseline expected score at endgame entry" .claude/skills/benchmarks/SKILL.md && grep -c "bic.status" .claude/skills/benchmarks/SKILL.md && grep -c "2400.*classical\|sparse" .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/benchmarks/SKILL.md` gains a new section modelled on Section 0, with the canonical CTE (selected_users + endgame_game_ids + rows + per_user + per_user_excl_sparse)
    - `bic.status = 'completed'` filter present in the new SKILL section
    - Sparse-cell exclusion (rating_bucket=2400, tc_bucket='classical') documented and applied
    - `reports/benchmarks-2026-05-11.md` exists with header, 5x4 cell table, TC marginal, ELO marginal, pooled overall, recommendations, collapse verdict
    - Recommendations section explicitly cites either pooled [p25, p75] or an editorial-tightened band with IQR-vs-band rationale (per D-15)
    - Equal-footing sanity check is recorded in the recommendations section (pooled p50 reasonableness given equal-footing filter)
    - No Wilson methodology editorial in SKILL narrative (memory `feedback_wilson_chess_score.md`)
  </acceptance_criteria>
  <done>
    SKILL.md + report ready for human approval of the recommended `(typical_lower, typical_upper)` band.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human approval of recommended band</name>
  <what-built>Task 1 produced a SKILL.md section and a benchmarks report with a recommended band for ENTRY_EXPECTED_SCORE_ZONES. The recommendation may be the raw pooled [p25, p75] OR an editorially tightened band per D-15 (memory feedback_zone_band_judgement.md).</what-built>
  <how-to-verify>
    1. Read reports/benchmarks-2026-05-11.md sections "Recommendations" and "Collapse verdict"
    2. Verify the pooled p50 is in a reasonable range given the equal-footing filter (slightly above 0.5)
    3. Verify the collapse verdict supports a single global band (TC d_max < 0.5 AND ELO d_max < 0.5) OR explicitly defers per-ELO stratification per CONTEXT.md "Deferred Ideas"
    4. Decide on the final (typical_lower, typical_upper) pair: either accept the pooled [p25, p75] or accept an editorially tightened version. If tightened, confirm the rationale cites IQR-vs-band per memory feedback_zone_band_judgement.md
    5. Respond with the approved pair like "typical_lower=0.45, typical_upper=0.55" (or whatever the agreed values are)
  </how-to-verify>
  <resume-signal>Reply with the approved (typical_lower, typical_upper) pair, or "use pooled IQR" / "tighten to <values>"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Register ENTRY_EXPECTED_SCORE_ZONES + regenerate endgameZones.ts</name>
  <files>app/services/endgame_zones.py, frontend/src/generated/endgameZones.ts, reports/benchmarks-2026-05-11.md</files>
  <read_first>
    - app/services/endgame_zones.py (full file; especially the MetricId Literal at lines 30-53 and the "entry_eval_pawns" entry at lines 141-150)
    - scripts/gen_endgame_zones_ts.py (full file: understand what gets generated)
    - frontend/src/lib/endgameEntryEvalZones.ts (lines 40-44: the existing helper shape the generator emits)
    - reports/benchmarks-2026-05-11.md (the report Task 1 produced; for the comment citation)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "app/services/endgame_zones.py")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-14, D-15, D-16)
  </read_first>
  <behavior>
    - "entry_expected_score" appears in the MetricId Literal in endgame_zones.py with a comment referencing Phase 83 D-17
    - ZONE_REGISTRY has a new "entry_expected_score" entry with `direction="higher_is_better"`, `typical_lower` and `typical_upper` set to the values approved in Task 2, and a multi-line comment citing reports/benchmarks-2026-05-11.md section X, TC d_max, ELO d_max, and any editorial tightening rationale per D-15
    - Running `uv run python scripts/gen_endgame_zones_ts.py` updates frontend/src/generated/endgameZones.ts; the file now exports `entryExpectedScoreZoneColor`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MIN`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MAX`
    - Running the generator a second time produces no diff (idempotency; CI drift gate passes)
    - reports/benchmarks-2026-05-11.md "Currently set in code" section is filled in with the approved values
  </behavior>
  <action>
    Step 1: Add `"entry_expected_score",` to the MetricId Literal in `app/services/endgame_zones.py` (lines 30-53) with comment `# Phase 83 D-17: new endgame_start_vs_end Tile 1 row 2 — achievable score`.

    Step 2: Add a new entry to ZONE_REGISTRY (alongside the existing "entry_eval_pawns" at lines 141-150):
      - Key: "entry_expected_score"
      - typical_lower / typical_upper: values approved in Task 2
      - direction: "higher_is_better"
      - Multi-line comment block citing reports/benchmarks-2026-05-11.md section X, the collapse verdict (TC d_max=<>, ELO d_max=<>), and any editorial tightening rationale (memory feedback_zone_band_judgement.md). Unit annotation: "expected score on the 0-1 W+0.5D scale, centered at 0.5".

    Step 3: Run `uv run python scripts/gen_endgame_zones_ts.py`. Verify the regenerated `frontend/src/generated/endgameZones.ts` now exports `entryExpectedScoreZoneColor`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MIN`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MAX`. If the generator's emission shape for these names doesn't match the existing `endgameEntryEvalZoneColor` family (frontend/src/lib/endgameEntryEvalZones.ts:40-44), edit the generator script to emit them in the consistent shape, then re-run.

    Step 4: Run `uv run python scripts/gen_endgame_zones_ts.py` again. The TS file must NOT change (idempotency check; CI drift gate). Verify with `git diff --exit-code frontend/src/generated/endgameZones.ts`.

    Step 5: Fill in the "Currently set in code" section of reports/benchmarks-2026-05-11.md with the locked values and a code reference to `app/services/endgame_zones.py`.

    Step 6: Type check + lint: `uv run ty check app/services/endgame_zones.py` and `cd frontend && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts && uv run ty check app/services/endgame_zones.py && grep -c "entry_expected_score" app/services/endgame_zones.py && grep -c "entryExpectedScoreZoneColor\|ENTRY_EXPECTED_SCORE_NEUTRAL_MIN\|ENTRY_EXPECTED_SCORE_NEUTRAL_MAX" frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"entry_expected_score\"" app/services/endgame_zones.py` returns >= 2 (one in MetricId Literal, one in ZONE_REGISTRY)
    - ZoneSpec entry has `direction="higher_is_better"`
    - The new ZoneSpec entry's comment block cites `reports/benchmarks-2026-05-11.md` AND includes TC d_max + ELO d_max numbers
    - `grep -c "entryExpectedScoreZoneColor" frontend/src/generated/endgameZones.ts` returns >= 1
    - `grep -c "ENTRY_EXPECTED_SCORE_NEUTRAL_MIN" frontend/src/generated/endgameZones.ts` returns >= 1
    - `grep -c "ENTRY_EXPECTED_SCORE_NEUTRAL_MAX" frontend/src/generated/endgameZones.ts` returns >= 1
    - Running the generator a second time produces no diff (`git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0 after the second run)
    - ty-clean on endgame_zones.py
    - reports/benchmarks-2026-05-11.md "Currently set in code" section is no longer blank
  </acceptance_criteria>
  <done>
    Zone band is locked into the Python registry; TS codegen is current and idempotent; Plan 3 can import the new helpers.
  </done>
</task>

</tasks>

<verification>
- `uv run ty check app/services/endgame_zones.py` zero errors
- `cd frontend && npx tsc --noEmit` exits 0
- `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0 (drift gate)
- `grep -c "\"entry_expected_score\"" app/services/endgame_zones.py` returns >= 2
- `test -f reports/benchmarks-2026-05-11.md` succeeds
</verification>

<success_criteria>
- The new cohort band is sourced from formal calibration over the benchmark DB (D-14), not v1 sigmoid math or hand-picked thresholds
- The editorial-judgement principle is applied where the IQR alone would suppress meaningful zone signal (D-15, memory feedback_zone_band_judgement.md)
- The frontend zone helper is regenerated, not hand-edited (D-16); CI drift detection works
- Plan 3 can `import { entryExpectedScoreZoneColor, ENTRY_EXPECTED_SCORE_NEUTRAL_MIN, ENTRY_EXPECTED_SCORE_NEUTRAL_MAX } from '@/generated/endgameZones'`
- Plan 5 can `from app.services.endgame_zones import assign_zone` and call `assign_zone("entry_expected_score", value)` without error
</success_criteria>

<output>
After completion, create `.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-04-SUMMARY.md` documenting: the locked `(typical_lower, typical_upper)` pair, the collapse verdict numbers, any editorial tightening rationale, and the canonical SKILL.md CTE shape used.
</output>
