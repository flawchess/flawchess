---
phase: 83
plan: 03
type: execute
wave: 4
depends_on: [83-02, 83-04]
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/popovers/AchievableScorePopover.tsx
  - frontend/src/components/charts/EndgameStartVsEndSection.tsx
  - frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx
autonomous: true
requirements: [D-08, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "User sees a new 'Achievable score' bullet inside the 'Where you start' tile (D-09 label, D-11 visuals)"
    - "User sees a MiniWDLBar at the top of the 'What you do with it' tile (D-08, D-13)"
    - "Both tiles render as a 2-row stack: top row (eval / WDL) then bottom row (achievable / endgame score) per D-12"
    - "The existing 'Games with vs without Endgame' table below the section is UNCHANGED (D-08 redundancy accepted)"
    - "Popover on the Achievable score bullet states 'what a 2300+ rated player would score' and 'Compare against your achieved Endgame score', and does NOT contain the word 'underperformance' (D-10)"
    - "Tile-color rule (zone != neutral) AND p < 0.05 applies to the new bullet; borderline-but-significant cases inside neutral band stay neutral (D-11)"
    - "Mobile DOM order matches desktop: eval / WDL first, score-axis bullets second within each tile (D-12)"
  artifacts:
    - path: "frontend/src/components/popovers/AchievableScorePopover.tsx"
      provides: "Thin wrapper around PopoverPrimitive with D-10 verbatim body copy"
      contains: "2300+"
    - path: "frontend/src/components/charts/EndgameStartVsEndSection.tsx"
      provides: "2x2 grid restructure: new achievable-score bullet in tile-1, lifted MiniWDLBar in tile-2"
      contains: "AchievableScorePopover"
    - path: "frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx"
      provides: "At least 6 new RTL cases for achievable-score rendering, color-gate, MiniWDLBar placement, popover testid"
      contains: "achievable-score-value"
  key_links:
    - from: "frontend/src/components/charts/EndgameStartVsEndSection.tsx"
      to: "frontend/src/components/stats/MiniWDLBar.tsx"
      via: "import { MiniWDLBar } from '@/components/stats/MiniWDLBar'"
      pattern: "from '@/components/stats/MiniWDLBar'"
    - from: "frontend/src/components/charts/EndgameStartVsEndSection.tsx"
      to: "frontend/src/components/popovers/AchievableScorePopover.tsx"
      via: "renders AchievableScorePopover next to the achievable-score value"
      pattern: "AchievableScorePopover"
    - from: "frontend/src/components/charts/EndgameStartVsEndSection.tsx"
      to: "frontend/src/generated/endgameZones.ts"
      via: "import entryExpectedScoreZoneColor, ENTRY_EXPECTED_SCORE_NEUTRAL_MIN, ENTRY_EXPECTED_SCORE_NEUTRAL_MAX added by Plan 4"
      pattern: "entryExpectedScoreZoneColor"
---

<objective>
Restructure the existing `EndgameStartVsEndSection` twin-tile section from a single-row-per-tile layout into a 2x2 grid:

| Tile | Top row | Bottom row |
|---|---|---|
| "Where you start" | Endgame entry eval (centipawns, existing) | "Achievable score" (W+0.5D axis, NEW) |
| "What you do with it" | Win / Draw / Loss (MiniWDLBar, NEW: lifted from EndgamePerformanceSection's table row) | Endgame score (W+0.5D axis, existing) |

The bottom row of both tiles shares the same W+0.5D axis so the achievable-vs-achieved gap is directly readable across the two tiles. Mobile DOM order matches desktop. The existing "Games with vs without Endgame" table below the section stays unchanged (D-08 redundancy accepted).

Purpose: Closes the units mismatch that motivated the seed. Today the LLM has to translate centipawns to a score in prose; the UI now shows both in the same units side by side. Phase 81/82 conventions carry forward (sample-size gate >= 10, (zone != neutral) AND p < 0.05 tile color rule).

Output: New `AchievableScorePopover.tsx` wrapper, modified `EndgameStartVsEndSection.tsx`, extended types in `endgames.ts`, extended component tests.

Sequencing note: Plan 3 depends on Plan 4 because it imports `entryExpectedScoreZoneColor`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MIN`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MAX` from the regenerated `frontend/src/generated/endgameZones.ts` (RESEARCH Pitfall 5: band-lands-first ordering). Wave assignment encodes this via `depends_on: [83-02, 83-04]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-02-SUMMARY.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-04-SUMMARY.md

@frontend/src/components/charts/EndgameStartVsEndSection.tsx
@frontend/src/components/stats/MiniWDLBar.tsx
@frontend/src/components/insights/ScoreConfidencePopover.tsx
@frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx
@frontend/src/lib/scoreBulletConfig.ts
@frontend/src/lib/theme.ts
@frontend/src/types/endgames.ts

<interfaces>
Component imports the executor needs to wire together:

From frontend/src/components/stats/MiniWDLBar.tsx (existing, line 14):
- `export function MiniWDLBar({ win_pct, draw_pct, loss_pct, heightClass? }): JSX.Element`

From frontend/src/generated/endgameZones.ts (NEW exports added by Plan 4):
- `export const ENTRY_EXPECTED_SCORE_NEUTRAL_MIN: number`
- `export const ENTRY_EXPECTED_SCORE_NEUTRAL_MAX: number`
- `export function entryExpectedScoreZoneColor(value: number): string`

From frontend/src/lib/scoreBulletConfig.ts (existing, reuse for the new bullet):
- `SCORE_BULLET_CENTER` (= 0.5)
- `scoreBulletDomain()` (returns [0, 1])

D-10 popover body copy (verbatim, do NOT paraphrase, do NOT use em-dashes):

> This is what a 2300+ rated player would score from your endgame-entry positions, via the Lichess winning-chances sigmoid. The Lichess curve is fitted on 2300+ rapid games, so scoring below this baseline from positive evals is normal at lower ratings and is not a flaw. Compare against your achieved Endgame score in the other tile.

(Final wording polish allowed at PR review per CONTEXT.md "Claude's Discretion".)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EndgamePerformance TS type with the 5 new fields</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts (full file: locate the existing EndgamePerformance interface and its entry_eval_* block)
    - app/schemas/endgames.py (Plan 2 output: Python source of truth for the wire format)
  </read_first>
  <behavior>
    - The TS EndgamePerformance interface gains 5 new fields: entry_expected_score, entry_expected_score_n, entry_expected_score_p_value, entry_expected_score_ci_low, entry_expected_score_ci_high
    - Types match the Python wire format: number, number, number | null, number | null, number | null
    - Existing call sites that don't destructure these fields keep compiling
  </behavior>
  <action>
    Open `frontend/src/types/endgames.ts`, find the existing `entry_eval_mean_pawns` field on the response interface, and append the 5 new fields mirroring the Phase 81 entry-eval shape. Match the surrounding casing and optional-marker conventions already in the file (do not introduce optional ? unless the existing entry_eval_* fields use it).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E '(entry_expected_score|endgames\.ts)' | head -20; cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "entry_expected_score(_n|_p_value|_ci_low|_ci_high)?:" frontend/src/types/endgames.ts | wc -l` returns 5
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    TS interface mirrors Plan 2's Python schema; project typechecks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AchievableScorePopover thin wrapper</name>
  <files>frontend/src/components/popovers/AchievableScorePopover.tsx</files>
  <read_first>
    - frontend/src/components/insights/ScoreConfidencePopover.tsx (full file: pattern to mirror, PopoverPrimitive.Root + hover handlers + Portal + Content)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "frontend/src/components/popovers/AchievableScorePopover.tsx")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-10 verbatim copy)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md (Open Question 1 resolution: wrapper preferred over bodyCopy prop)
  </read_first>
  <behavior>
    - Component exports AchievableScorePopover taking at minimum a testId?: string prop
    - Renders a HelpCircle trigger (same icon as ScoreConfidencePopover)
    - Trigger has role="button", tabIndex={0}, aria-label="What is Achievable score?", and data-testid defaulting to "popover-trigger-achievable-score"
    - On hover (100ms delay) or click/keyboard, the popover opens
    - Popover body contains the D-10 verbatim string, including "2300+", "Lichess winning-chances sigmoid", "fitted on 2300+ rapid games", "normal at lower ratings", "Compare against your achieved Endgame score"
    - Body does NOT contain the word "underperformance" (D-10 forbidden word)
    - Body does NOT contain " — " (em-dash space sequence) per CLAUDE.md style
  </behavior>
  <action>
    Create new directory `frontend/src/components/popovers/` if it does not exist. Create `AchievableScorePopover.tsx` as a thin wrapper. Lift the hover handling and PopoverPrimitive.Root / Trigger / Portal / Content scaffolding from `ScoreConfidencePopover.tsx` and replace the `WdlConfidenceTooltip` body with a plain p element containing the D-10 verbatim copy (Interfaces block above). Default testId to "popover-trigger-achievable-score". Use HelpCircle from lucide-react (same import as the analog). Match the analog's className conventions for the trigger span. Do NOT add a bodyCopy prop to the existing ScoreConfidencePopover (rejected by RESEARCH). The wrapper is single-purpose: the body copy is hard-coded inside it.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && grep -c '2300+' src/components/popovers/AchievableScorePopover.tsx && (grep -i 'underperformance' src/components/popovers/AchievableScorePopover.tsx; test $? -ne 0); cd ..</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/popovers/AchievableScorePopover.tsx`
    - Component exports AchievableScorePopover as a named export
    - `grep -c '2300+' frontend/src/components/popovers/AchievableScorePopover.tsx` returns >= 1
    - `grep -ci 'underperformance' frontend/src/components/popovers/AchievableScorePopover.tsx` returns 0
    - `grep -ci 'fall short\|below your potential' frontend/src/components/popovers/AchievableScorePopover.tsx` returns 0
    - Trigger has aria-label and a data-testid defaulting to "popover-trigger-achievable-score"
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    New popover wrapper compiles, passes copy/style gates, and is ready for use by EndgameStartVsEndSection.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restructure EndgameStartVsEndSection into 2x2 grid + extend tests</name>
  <files>frontend/src/components/charts/EndgameStartVsEndSection.tsx, frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameStartVsEndSection.tsx (full file: especially lines 60-201, the derived-level/zone block + both tiles)
    - frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx (full file: extend existing cases)
    - frontend/src/components/stats/MiniWDLBar.tsx (the lifted import; its data-testid="mini-wdl-bar" already exists)
    - frontend/src/lib/scoreBulletConfig.ts (SCORE_BULLET_CENTER, scoreBulletDomain, scoreZoneColor: reuse axis config)
    - frontend/src/generated/endgameZones.ts (Plan 4 output: new entryExpectedScoreZoneColor, ENTRY_EXPECTED_SCORE_NEUTRAL_MIN/MAX exports)
    - frontend/src/lib/theme.ts (ZONE_NEUTRAL, MIN_GAMES_FOR_RELIABLE_STATS)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "frontend/src/components/charts/EndgameStartVsEndSection.tsx (MODIFY: 2x2 restructure)")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-08..D-13)
  </read_first>
  <behavior>
    - Tile-1 ("Where you start") becomes a flex flex-col gap-4 2-row stack: existing entry-eval block on top, new achievable-score block on bottom
    - Tile-2 ("What you do with it") becomes a flex flex-col gap-4 2-row stack: new MiniWDLBar on top fed from data.endgame_wdl.{win_pct,draw_pct,loss_pct}; existing endgame-score block on bottom
    - The outer lg:grid-cols-2 grid is UNCHANGED (D-12: only tile interior changes)
    - New derived values added: achievableLevel = deriveLevel(data.entry_expected_score_p_value, data.entry_expected_score_n), achievableZoneHex = entryExpectedScoreZoneColor(data.entry_expected_score), achievableIsInColoredZone = achievableZoneHex !== ZONE_NEUTRAL, achievableShowZoneFontColor = isConfident(achievableLevel) AND achievableIsInColoredZone, achievableColor = achievableShowZoneFontColor ? achievableZoneHex : undefined, showAchievableChart = data.entry_expected_score_n >= MIN_GAMES_FOR_RELIABLE_STATS
    - When showAchievableChart is true: render an achievable-score row identical in shape to the existing endgame-score row (label "Achievable score:", value formatted as percent, AchievableScorePopover next to value, MiniBulletChart with value=data.entry_expected_score, center=SCORE_BULLET_CENTER, neutralMin=ENTRY_EXPECTED_SCORE_NEUTRAL_MIN, neutralMax=ENTRY_EXPECTED_SCORE_NEUTRAL_MAX, domain=scoreBulletDomain(), ciLow/ciHigh clamped from data.entry_expected_score_ci_low/_high, barColor="neutral", descriptive ariaLabel)
    - When showAchievableChart is false: render "Not enough data yet" placeholder (mirroring existing pattern)
    - Tile color rule applies per D-11: (zone != neutral) AND p < 0.05; borderline cases inside neutral band stay neutral even if significant
    - New testids: data-testid="achievable-score-value" on the percent text; data-testid="popover-trigger-achievable-score" on the popover trigger (default from Task 2); data-testid="endgame-achievable-score" on the achievable-score row wrapper
    - The existing "Games with vs without Endgame" table below this section is NOT modified (D-08)
  </behavior>
  <action>
    Modify `frontend/src/components/charts/EndgameStartVsEndSection.tsx` per PATTERNS.md section "frontend/src/components/charts/EndgameStartVsEndSection.tsx (MODIFY: 2x2 restructure)":

    1. Imports: add `import { MiniWDLBar } from '@/components/stats/MiniWDLBar'` (PATTERNS.md correction: NOT from EndgamePerformanceSection); add `import { AchievableScorePopover } from '@/components/popovers/AchievableScorePopover'`; add `entryExpectedScoreZoneColor`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MIN`, `ENTRY_EXPECTED_SCORE_NEUTRAL_MAX` to the existing `@/generated/endgameZones` import block.

    2. Derived values: after the existing evalLevel / scoreLevel derivations (lines 69-94), add the parallel `achievable*` triad (per Behavior bullet above). Use the existing deriveLevel, isConfident, ZONE_NEUTRAL, MIN_GAMES_FOR_RELIABLE_STATS helpers; do NOT duplicate.

    3. Tile-1 JSX: wrap the existing tile-1 body in a flex flex-col gap-4 container and append a sibling block for the achievable-score row, structured identically to the existing endgame-score row at lines 158-200 (label + value with conditional color style + popover trigger + MiniBulletChart). Use ENTRY_EXPECTED_SCORE_NEUTRAL_MIN/MAX for the bullet neutral band; reuse SCORE_BULLET_CENTER and scoreBulletDomain() for the W+0.5D axis. Add data-testid="endgame-achievable-score" on the achievable-score row wrapper.

    4. Tile-2 JSX: wrap the existing tile-2 body in a flex flex-col gap-4 container and PREPEND a top row containing the MiniWDLBar fed from data.endgame_wdl.{win_pct,draw_pct,loss_pct}, with a label row matching the tile-1 label conventions (e.g. text-muted-foreground "Win / Draw / Loss:"). Verify data.endgame_wdl exists on the response type; if it lives under a different key, use the existing key as currently consumed by the "Games with vs without Endgame" table below.

    5. Mobile order: make sure the DOM order matches desktop (top row then bottom row inside each tile). flex-col does this naturally; do not add flex-col-reverse or any breakpoint-specific reorder (D-12).

    6. Tests: extend `__tests__/EndgameStartVsEndSection.test.tsx` with at least these RTL cases (use within(screen.getByTestId('tile-entry-eval')) and within(screen.getByTestId('tile-endgame-score')) to scope queries):
       - renders achievable-score bullet when entry_expected_score_n >= 10
       - renders "Not enough data yet" inside tile-1 row 2 when entry_expected_score_n < 10 (independent of tile-1 row 1 entry-eval gate)
       - paints achievable-score value color when zone != neutral AND p < 0.05
       - does not paint achievable-score value color when sig but inside neutral band (Phase 82 D-12 gate carried forward)
       - renders MiniWDLBar in tile-2 top row: assert within(tile2).getByTestId('mini-wdl-bar') is present
       - achievable-score popover trigger has data-testid="popover-trigger-achievable-score"
       - popover body does not contain the word "underperformance": render section, programmatically open popover (hover or click), assert text content does not match /underperformance/i

    Keep the existing vi.mock('@/components/charts/MiniBulletChart', ...) spy pattern (or extend it if the new bullet's props need to be asserted). Do not introduce new data-testids on the underlying MiniWDLBar; it already exposes mini-wdl-bar (lines 15-16 of MiniWDLBar.tsx).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx --reporter=verbose && npx tsc --noEmit && npm run lint; cd ..</automated>
  </verify>
  <acceptance_criteria>
    - All new RTL cases pass; all existing cases STILL pass (no regression)
    - `grep -c "AchievableScorePopover" frontend/src/components/charts/EndgameStartVsEndSection.tsx` returns >= 1
    - `grep -c "from '@/components/stats/MiniWDLBar'" frontend/src/components/charts/EndgameStartVsEndSection.tsx` returns 1
    - `grep -c "entryExpectedScoreZoneColor" frontend/src/components/charts/EndgameStartVsEndSection.tsx` returns >= 1
    - `grep -c "data-testid=\"achievable-score-value\"" frontend/src/components/charts/EndgameStartVsEndSection.tsx` returns 1
    - `grep -c "data-testid=\"endgame-achievable-score\"" frontend/src/components/charts/EndgameStartVsEndSection.tsx` returns 1
    - `grep -c "flex-col-reverse" frontend/src/components/charts/EndgameStartVsEndSection.tsx` returns 0 (mobile order rule)
    - The existing "Games with vs without Endgame" table in the codebase is unchanged: `git diff frontend/src/components/charts/EndgamePerformanceSection.tsx | wc -l` returns 0 unless an unrelated import change was needed (verify by inspection)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `npm run lint` clean (no new warnings)
  </acceptance_criteria>
  <done>
    The 2x2 grid renders end to end, both new visuals (achievable-score bullet + MiniWDLBar top row) display when data is present, popover wording passes copy gates, all tests green.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx` passes
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run lint` clean (no new warnings)
- `cd frontend && npm run knip` reports no new unused exports (the new ENTRY_EXPECTED_SCORE_* exports must be imported by EndgameStartVsEndSection.tsx)
- Popover body copy verified for D-10 forbidden words via grep
</verification>

<success_criteria>
- The new "Achievable score" bullet appears in the "Where you start" tile with the W+0.5D axis and Plan 4's cohort band
- The MiniWDLBar appears at the top of the "What you do with it" tile, mirroring the endgame WDL from the existing table row
- The popover wording communicates the 2300+ baseline framing without the word "underperformance"
- The outer twin-tile grid structure is preserved (only tile interiors change)
</success_criteria>

<output>
After completion, create `.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-03-SUMMARY.md` documenting: new component path, types extended, RTL cases added, and any wording adjustments to the popover body (Claude's Discretion per CONTEXT.md).
</output>
