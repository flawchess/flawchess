---
phase: 80.1
plan: 03
type: execute
wave: 2
depends_on: ["80.1-01"]
files_modified:
  - app/services/opening_insights_service.py
  - tests/services/test_opening_insights_service.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Opening Insights findings: classification (weakness/strength), Wilson confidence, ranking key, and displayed bar all reflect resulting-position WDL (per D-03)"
    - "Surfacing gate (n_games >= OPENING_INSIGHTS_MIN_GAMES_PER_CANDIDATE = 20) stays on move-played count in the SQL HAVING clause of query_opening_transitions (per D-04 — no SQL change)"
    - "OpeningInsightFinding.n_games becomes resulting-position N per D-05 (always >= move-played N by construction; Pydantic Field(ge=20) validator stays satisfied)"
    - "_classify_row, _compute_score, compute_confidence_bucket, wilson_bounds receive a SimpleNamespace adapter carrying resulting-position W/D/L/N — helper code unchanged per D-07"
    - "MG-entry eval pillar untouched — query_opening_phase_entry_metrics_batch already keys on resulting_full_hash"
  artifacts:
    - path: "app/services/opening_insights_service.py"
      provides: "compute_insights with SimpleNamespace adapter feeding resulting-position WDL into classification/ranking"
      contains: "query_resulting_position_wdl"
    - path: "tests/services/test_opening_insights_service.py"
      provides: "Updated _run_compute mock + new transposition fixture tests"
      contains: "mock_pos_wdl"
  key_links:
    - from: "compute_insights"
      to: "query_resulting_position_wdl"
      via: "single batch call before the per-color loop, mirroring the existing query_opening_phase_entry_metrics_batch call"
      pattern: "await query_resulting_position_wdl"
    - from: "_classify_row + _compute_score + compute_confidence_bucket + wilson_bounds"
      to: "SimpleNamespace adapter carrying pos_w/pos_d/pos_l/pos_n"
      via: "adapter object replacing direct row access in the inner loop"
      pattern: "SimpleNamespace"
---

<objective>
Wire `compute_insights` to consume the new `query_resulting_position_wdl` and feed resulting-position W/D/L into `_classify_row`, `_compute_score`, `compute_confidence_bucket`, `wilson_bounds`, and `OpeningInsightFinding` field population. The surfacing gate (HAVING clause inside `query_opening_transitions`) stays on move-played count per D-04 — no SQL change.

Purpose: Closes the same 57%→61% mismatch on the Opening Insights findings. Classification, ranking, and the displayed bar all switch to resulting-position WDL per D-03/D-05.

Output: Service-layer field swap using a `SimpleNamespace` adapter (per RESEARCH.md Open Question #3) so the four downstream helpers stay untouched. Test mock helper extended to patch the new query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-CONTEXT.md
@.planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-RESEARCH.md
@.planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-PATTERNS.md
@.planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-01-SUMMARY.md
@app/services/opening_insights_service.py
@app/repositories/openings_repository.py
@app/services/score_confidence.py
@app/services/opening_insights_constants.py
@app/schemas/opening_insights.py

<interfaces>

From app/repositories/openings_repository.py (added in Plan 01):
```
async def query_resulting_position_wdl(
    session, user_id, hash_list,
    time_control, platform, rated, opponent_type,
    recency_cutoff, color, opponent_gap_min=None, opponent_gap_max=None,
) -> dict[int, tuple[int, int, int]]
# Returns {resulting_full_hash: (wins, draws, losses)}; missing hashes omitted.
# Caller in compute_insights passes color=None to mirror query_opening_transitions.
```

From app/services/opening_insights_service.py (existing helpers — UNCHANGED per D-07):
```
def _classify_row(row) -> tuple[str, str] | None:
    # Reads row.w, row.d, row.n. Returns (classification, severity) or None.

def _compute_score(row) -> float:
    # Reads row.w, row.d, row.n. Returns (w + 0.5*d) / n.
```

From app/services/score_confidence.py (UNCHANGED post quick task 260507-aw5):
```
def compute_confidence_bucket(wins, draws, losses, n) -> tuple[Literal[...], float, float]
def wilson_bounds(score: float, n: int) -> tuple[float, float]
```

From app/services/opening_insights_constants.py (UNCHANGED per D-07):
```
OPENING_INSIGHTS_MIN_GAMES_PER_CANDIDATE: int = 20  # surfacing gate, stays on move-played
OPENING_INSIGHTS_SCORE_PIVOT: float = 0.5
OPENING_INSIGHTS_MAJOR_EFFECT: float = 0.10
OPENING_INSIGHTS_MINOR_EFFECT: float = 0.05
```

From app/schemas/opening_insights.py (UNCHANGED per D-07):
```
class OpeningInsightFinding(BaseModel):
    n_games: int = Field(ge=OPENING_INSIGHTS_MIN_GAMES_PER_CANDIDATE)
    wins: int; draws: int; losses: int
    score: float
    confidence: Literal["low", "medium", "high"]
    p_value: float | None
    ci_low: float; ci_high: float
    # plus eval_* fields (already transposition-inclusive — no change)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire query_resulting_position_wdl into compute_insights via SimpleNamespace adapter</name>
  <files>app/services/opening_insights_service.py</files>
  <read_first>
    - app/services/opening_insights_service.py (read fully — 666 lines; pay attention to compute_insights inner loop ~498-565, _classify_row ~72-103, _compute_score, _wilson_bounds usage, _rank_section, _robustness)
    - app/repositories/openings_repository.py (verify query_resulting_position_wdl signature added by Plan 01)
    - app/services/score_confidence.py (compute_confidence_bucket + wilson_bounds — call signatures)
    - .planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-PATTERNS.md (lines 235-345 — Pattern 4 SimpleNamespace adapter + import block)
    - .planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-CONTEXT.md (D-03, D-04, D-05, D-07 — particularly the no-touch list and the n_games validator note)
    - .planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-RESEARCH.md (Pitfall 2 — _classify_row must receive substituted values; Pitfall 3 — n_games validator)
  </read_first>
  <behavior>
    - Before the per-color findings loop, compute_insights calls query_resulting_position_wdl ONCE with all distinct resulting_full_hash values from the query_opening_transitions result set.
    - In the inner loop, for each row (move-played source), construct pos_row = SimpleNamespace(w=pos_w, d=pos_d, l=pos_l, n=pos_n) from the dict lookup, then pass pos_row (not row) to _classify_row, _compute_score, compute_confidence_bucket, wilson_bounds, and into the OpeningInsightFinding constructor for n_games/wins/draws/losses/score/confidence/p_value/ci_low/ci_high.
    - row.entry_san_sequence, row.move_san, row.entry_hash, row.resulting_full_hash, row.opening_id, etc. (non-WDL fields) continue to come from row.
    - The HAVING clause in query_opening_transitions stays unchanged — surfacing gate continues to operate on move-played n per D-04.
    - Defensive fallback: if pos_wdl is missing a resulting_full_hash, log a warning and fall back to row.{w,d,l,n} (move-played values). In the typical path with correct filter parity, this fallback never triggers.
    - Call to query_resulting_position_wdl uses color=None per the call-site contract (mirrors query_opening_transitions filter behavior — the per-row color is already determined by the entry_hash partition white_hash vs black_hash).
  </behavior>
  <action>
    Modify app/services/opening_insights_service.py:

    STEP 1 — Add imports.

    Add `from types import SimpleNamespace` if not already present. Add `query_resulting_position_wdl` to the existing import block from `app.repositories.openings_repository`. Final shape:

    ```
    from types import SimpleNamespace  # Phase 80.1: adapter for resulting-position W/D/L

    from app.repositories.openings_repository import (
        query_opening_transitions,
        query_openings_by_hashes,
        query_resulting_position_wdl,  # NEW: Phase 80.1 D-03
    )
    ```

    STEP 2 — Insert the resulting-position WDL batch call.

    Locate the section AFTER query_opening_transitions returns and AFTER rows_by_color is built, and BEFORE the per-color findings loop begins. Read 30 lines around the existing query_opening_phase_entry_metrics_batch call to copy the variable names exactly (likely `cutoff` for the recency parameter; verify by reading).

    Insert this block before the per-color findings loop:

    ```
    # Phase 80.1 D-03/D-05: aggregate W/D/L per resulting_full_hash across
    # ALL move orders (transposition-inclusive). Surfacing gate already ran
    # in query_opening_transitions HAVING n_games >= OPENING_INSIGHTS_MIN_GAMES_PER_CANDIDATE
    # on move-played n (per D-04). The values below feed _classify_row /
    # _compute_score / compute_confidence_bucket / wilson_bounds /
    # OpeningInsightFinding via a SimpleNamespace adapter so those helpers
    # don't change.
    all_resulting_hashes: list[int] = list({
        int(row.resulting_full_hash)
        for color_rows in rows_by_color.values()
        for row in color_rows
    })
    pos_wdl: dict[int, tuple[int, int, int]] = {}
    if all_resulting_hashes:
        pos_wdl = await query_resulting_position_wdl(
            session=session,
            user_id=user_id,
            hash_list=all_resulting_hashes,
            time_control=request.time_control,
            platform=request.platform,
            rated=request.rated,
            opponent_type=request.opponent_type,
            recency_cutoff=cutoff,
            color=None,  # mirror query_opening_transitions filter behavior
            opponent_gap_min=request.opponent_gap_min,
            opponent_gap_max=request.opponent_gap_max,
        )
    ```

    NO asyncio.gather per CLAUDE.md — sequential await with the eval-metrics call already in place.

    STEP 3 — Replace direct row.w/d/l/n access in the inner loop with the SimpleNamespace adapter.

    Locate the inner loop body that currently calls `_classify_row(row)`, `_compute_score(row)`, `compute_confidence_bucket(row.w, row.d, row.l, row.n)`, `wilson_bounds(score, row.n)`, and constructs `OpeningInsightFinding(..., n_games=row.n, wins=row.w, draws=row.d, losses=row.l, score=score, ...)`.

    Insert this lookup at the very top of the loop body:

    ```
    pos = pos_wdl.get(int(row.resulting_full_hash))
    if pos is None:
        # Filter mismatch defensive fallback (Pitfall 4 from RESEARCH.md).
        logger.warning(
            "query_resulting_position_wdl missing resulting_full_hash=%s; "
            "falling back to move-played WDL",
            row.resulting_full_hash,
        )
        pos_row = row
    else:
        pos_w, pos_d, pos_l = pos
        pos_n = pos_w + pos_d + pos_l
        pos_row = SimpleNamespace(w=pos_w, d=pos_d, l=pos_l, n=pos_n)
    ```

    Then replace every subsequent occurrence of `row.w` / `row.d` / `row.l` / `row.n` (and the bare `row` argument when passed to _classify_row / _compute_score) with `pos_row.w` / `pos_row.d` / `pos_row.l` / `pos_row.n` / `pos_row`. Concretely:

    - `_classify_row(row)` becomes `_classify_row(pos_row)`
    - `_compute_score(row)` becomes `_compute_score(pos_row)`
    - `compute_confidence_bucket(row.w, row.d, row.l, row.n)` becomes `compute_confidence_bucket(pos_row.w, pos_row.d, pos_row.l, pos_row.n)`
    - `wilson_bounds(score, row.n)` becomes `wilson_bounds(score, pos_row.n)`
    - In OpeningInsightFinding(...): `n_games=row.n` → `n_games=pos_row.n`, `wins=row.w` → `wins=pos_row.w`, `draws=row.d` → `draws=pos_row.d`, `losses=row.l` → `losses=pos_row.l`.

    Keep ALL non-WDL fields (move_san, entry_san_sequence, entry_full_hash, opening_*, eval_*) reading from `row` exactly as before.

    STEP 4 — Verify untouched components.

    - _classify_row, _compute_score, compute_confidence_bucket, wilson_bounds, _rank_section, _robustness: NOT modified.
    - HAVING clause in query_opening_transitions: NOT modified — gate stays on move-played count per D-04.
    - OpeningInsightFinding schema: NOT modified — no new fields per D-07.
    - The Field(ge=OPENING_INSIGHTS_MIN_GAMES_PER_CANDIDATE) validator stays satisfied because pos_n >= move-played n by construction.

    STEP 5 — No frontend changes. Field names unchanged; only semantics flip per CONTEXT.md.
  </action>
  <verify>
    <automated>uv run ruff check app/services/opening_insights_service.py &amp;&amp; uv run ty check app/services/opening_insights_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from types import SimpleNamespace" app/services/opening_insights_service.py` returns `>= 1`.
    - `grep -c "query_resulting_position_wdl" app/services/opening_insights_service.py` returns `>= 2` (one in import block, one in await call).
    - `grep -c "await query_resulting_position_wdl" app/services/opening_insights_service.py` returns exactly `1`.
    - `grep -c "SimpleNamespace(w=" app/services/opening_insights_service.py` returns `>= 1`.
    - `grep -c "_classify_row(row)" app/services/opening_insights_service.py` returns `0` in the production code path (only `_classify_row(pos_row)` remains in compute_insights — but the helper definition `def _classify_row(row)` is allowed since the parameter is just a name).
    - `grep -c "_classify_row(pos_row)" app/services/opening_insights_service.py` returns `>= 1`.
    - `grep -c "compute_confidence_bucket(row.w" app/services/opening_insights_service.py` returns `0`.
    - `grep -c "compute_confidence_bucket(pos_row.w" app/services/opening_insights_service.py` returns `>= 1`.
    - `grep -c "asyncio.gather" app/services/opening_insights_service.py` returns `0`.
    - `grep -c "n_games=pos_row.n" app/services/opening_insights_service.py` returns `>= 1`.
    - `grep -c "n_games=row.n" app/services/opening_insights_service.py` returns `0`.
    - `uv run ty check app/services/opening_insights_service.py` exits 0.
    - `uv run ruff check app/services/opening_insights_service.py` exits 0.
    - The HAVING clause in query_opening_transitions (in app/repositories/openings_repository.py) is byte-identical to its pre-Plan-03 state — `git diff --stat app/repositories/openings_repository.py` shows changes ONLY from Plan 01 additions, not from compute_insights work.
    - app/schemas/opening_insights.py is byte-identical to pre-phase state — `git diff app/schemas/opening_insights.py` shows zero lines changed.
    - app/services/opening_insights_constants.py is byte-identical to pre-phase state.
  </acceptance_criteria>
  <done>
    SimpleNamespace adapter wired; classification + ranking + Wilson + display bar all reflect resulting-position WDL; D-04 surfacing gate untouched; D-07 no-touch list respected; ty + ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend _run_compute mock + add transposition tests in tests/services/test_opening_insights_service.py</name>
  <files>tests/services/test_opening_insights_service.py</files>
  <read_first>
    - tests/services/test_opening_insights_service.py (read fully — 1164 lines; pay attention to _run_compute helper around lines 91-127, _make_row factory, and tests asserting specific finding.score/wins/draws/losses values)
    - app/services/opening_insights_service.py (after Task 1 — confirm new structure)
    - .planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-PATTERNS.md (lines 421-507 — _run_compute extension pattern)
    - .planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-CONTEXT.md (D-08: significant test churn expected)
  </read_first>
  <behavior>
    - _run_compute helper accepts an optional `pos_wdl: dict[int, tuple[int, int, int]] | None = None` parameter. Default behavior preserves existing tests: when pos_wdl is None, build it from the input rows so resulting-position WDL == move-played WDL (no transpositions in fixture). This means existing tests that compute expected score from row.w/d/l/n still produce the same expected score under the new aggregation.
    - The mock context manager additionally patches `app.services.opening_insights_service.query_resulting_position_wdl` returning the pos_wdl dict.
    - New test test_finding_uses_resulting_position_wdl_when_diverges_from_move_played: input row has w=5/d=3/l=2/n=10 (move-played, score 0.65) but pos_wdl maps that resulting_full_hash to (12, 6, 2) (resulting-position W/D/L, n=20, score=0.75). Assert: surfacing gate passes because move-played n=10... wait, MIN=20. Use w=15/d=3/l=2/n=20 (move-played, score 0.80) but pos_wdl maps to (24, 6, 10) (resulting-position n=40, score=0.675). Assert finding.n_games == 40, finding.score ~ 0.675, finding.wins == 24.
    - New test test_classification_flips_when_pos_wdl_diverges: input row would classify as STRENGTH on move-played WDL (e.g., score=0.62 with n=20), but pos_wdl gives score=0.48 (below pivot+minor). Assert: finding does NOT appear for that candidate (or appears with classification flipped to neutral/dropped — depends on classify thresholds; recompute precise expected behavior from constants).
    - All other existing tests continue to pass because the default pos_wdl mirrors move-played (no fixture rewrites needed for the bulk of them, per the helper-default approach).
  </behavior>
  <action>
    Modify tests/services/test_opening_insights_service.py:

    STEP 1 — Extend _run_compute helper.

    Locate the _run_compute helper around lines 91-127. Add an optional pos_wdl parameter and a fourth patch in the with-block, defaulting pos_wdl to derive from the input rows so the existing test bodies continue producing the same expected numbers without rewrite.

    Replace the helper body with:

    ```
    async def _run_compute(
        rows,
        openings_by_hash=None,
        color="white",
        pos_wdl=None,
    ):
        if openings_by_hash is None:
            openings_by_hash = {100: _make_opening()}
        if pos_wdl is None:
            # Default: resulting-position WDL == move-played WDL (no transpositions
            # in fixture). Phase 80.1: existing tests that compute expected score
            # from row.w/d/l/n produce the same expected score under the new
            # aggregation because pos == row by default.
            pos_wdl = {
                int(row.resulting_full_hash): (row.w, row.d, row.l)
                for row in rows
            }

        with (
            patch(
                "app.services.opening_insights_service.query_opening_transitions",
                new_callable=AsyncMock,
            ) as mock_transitions,
            patch(
                "app.services.opening_insights_service.query_openings_by_hashes",
                new_callable=AsyncMock,
            ) as mock_attribution,
            patch(
                "app.services.opening_insights_service.query_opening_phase_entry_metrics_batch",
                new_callable=AsyncMock,
            ) as mock_eval,
            patch(
                "app.services.opening_insights_service.query_resulting_position_wdl",
                new_callable=AsyncMock,
            ) as mock_pos_wdl,
        ):
            mock_transitions.return_value = rows
            mock_attribution.return_value = openings_by_hash
            mock_eval.return_value = {}
            mock_pos_wdl.return_value = pos_wdl

            return await compute_insights(
                session=AsyncMock(),
                user_id=1,
                request=_default_request(color=color),
            )
    ```

    Verify the existing _make_row factory always sets `resulting_full_hash` on the SimpleNamespace it returns; if not, update _make_row to accept (and default) a resulting_full_hash kwarg so the dict-building works. Read _make_row carefully before editing.

    STEP 2 — Add new transposition-divergence tests.

    Add at least two new tests near the bottom of the file (or grouped into a new class TestComputeInsightsTranspositionWdl):

    a. test_finding_uses_resulting_position_wdl_when_diverges_from_move_played

    Build a row with move-played WDL that crosses the surfacing gate (n=20+) and would classify as some strength/weakness. Pass an explicit pos_wdl mapping the row.resulting_full_hash to a different (W, D, L) tuple where W+D+L > n_move_played (transposition-inclusive). Run compute. Assert that the produced finding's n_games, wins, draws, losses, score, ci_low, ci_high all match the pos_wdl values — NOT the move-played values.

    Concrete numbers (chosen so both move-played and pos cross the surfacing + effect-size gates):
    - row: w=15, d=3, l=2, n=20 (move-played score = 16.5/20 = 0.825 — clear strength)
    - pos_wdl[row.resulting_full_hash] = (24, 6, 10) (resulting-position n=40, score = 27/40 = 0.675 — still above pivot+minor=0.55)
    - Expected: finding.n_games == 40, finding.wins == 24, finding.draws == 6, finding.losses == 10
    - Expected: finding.score ~ 0.675 (within pytest.approx)
    - Expected: classification stays "strength" but the score numerically reflects pos_wdl.

    b. test_finding_classification_uses_resulting_position_score

    Build a row where move-played classifies as strength (score ~ 0.625, n=20) but pos_wdl yields a score below pivot+minor=0.55 (e.g., (10, 10, 20) → 15/40 = 0.375 — clear weakness). Run compute. Assert: classification of the produced finding reflects pos_wdl (weakness), not move-played (strength). The finding still surfaces (move-played n >= 20 gate passed) but its classification has flipped because _classify_row is fed pos_row, not row.

    Concrete:
    - row: w=12, d=1, l=7, n=20 (move-played score = 12.5/20 = 0.625 — strength via PIVOT 0.5 + MINOR 0.05)
    - pos_wdl: (10, 10, 20), n=40, score = 15/40 = 0.375 — weakness via PIVOT - MAJOR 0.10 = 0.40
    - Expected: finding for this candidate appears in `weaknesses_white` (or analogous), NOT in `strengths_white`.

    c. test_finding_n_games_validator_satisfied_when_pos_n_gt_move_played_n

    Build row with move-played n=20 (just at the gate). pos_wdl yields larger pos_n (e.g., 25). Confirm Pydantic validator on n_games (Field(ge=20)) does NOT raise — finding.n_games == 25.

    STEP 3 — Audit existing tests for cascade churn (D-08).

    Run the full module: `uv run pytest tests/services/test_opening_insights_service.py -x -v`. The default pos_wdl mirroring move-played WDL means most tests should continue passing. For any test that specifically asserts a `finding.score` or `finding.wins/draws/losses` value AND constructs its rows with non-default pos_wdl semantics, recompute the expected values from the seed data (D-08 cascade). The factory default of pos==row should keep this list short.

    Use unique hash values per new test (e.g., DIVERGE_HASH = 0xDD01, FLIP_HASH = 0xDD02) to avoid collision with other tests.

    No changes to _make_row's positional args — keep backward compatibility. Add resulting_full_hash kwarg if not present, with a sensible default (e.g., 200).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_opening_insights_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mock_pos_wdl" tests/services/test_opening_insights_service.py` returns `>= 1`.
    - `grep -c "query_resulting_position_wdl" tests/services/test_opening_insights_service.py` returns `>= 1`.
    - `grep -c "test_finding_uses_resulting_position_wdl_when_diverges_from_move_played" tests/services/test_opening_insights_service.py` returns `1`.
    - `grep -c "test_finding_classification_uses_resulting_position_score" tests/services/test_opening_insights_service.py` returns `1`.
    - `grep -c "test_finding_n_games_validator_satisfied_when_pos_n_gt_move_played_n" tests/services/test_opening_insights_service.py` returns `1`.
    - The default-pos-wdl branch in _run_compute exists: `grep -c "if pos_wdl is None:" tests/services/test_opening_insights_service.py` returns `>= 1`.
    - `uv run pytest tests/services/test_opening_insights_service.py -x` exits 0 with all tests passing.
    - `uv run pytest tests/services/test_opening_insights_arrow_consistency.py -x` exits 0 (Phase 76 D-06 structural assertion remains green; no second WDL/Wilson implementation introduced).
  </acceptance_criteria>
  <done>
    _run_compute mock helper extended with backward-compatible pos_wdl default; three new tests verify resulting-position WDL drives field population, classification flips, and n_games validator stays satisfied; full module test run is green; arrow-consistency structural test still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | OpeningInsightsRequest validated by Pydantic v2; no new request fields |
| service → repo | compute_insights passes user_id to apply_game_filters via the new query_resulting_position_wdl call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80.1-07 | Information Disclosure | OpeningInsightFinding payload | accept | No new fields per D-07; only semantics of n_games/wins/draws/losses/score/ci/p_value flip. user_id isolation preserved by query_resulting_position_wdl reusing apply_game_filters. |
| T-80.1-08 | Tampering | filter parity between query_opening_transitions and query_resulting_position_wdl | mitigate | Acceptance criteria enforce identical filter argument names in both call sites. The pos_n less than move-played n edge case (Pitfall 3 from RESEARCH.md) would surface as a Pydantic validator error — defensive fallback path logs the anomaly. test_finding_n_games_validator_satisfied_when_pos_n_gt_move_played_n is the regression test. |
| T-80.1-09 | Repudiation | warning log on missing pos_wdl entry | mitigate | logger.warning with the missing resulting_full_hash + user_id context allows post-hoc debugging if filter mismatch is introduced. |
</threat_model>

<verification>
- All tests in tests/services/test_opening_insights_service.py pass: `uv run pytest tests/services/test_opening_insights_service.py -x`
- Arrow-consistency structural test stays green: `uv run pytest tests/services/test_opening_insights_arrow_consistency.py -x`
- ty + ruff clean on modified files: `uv run ty check app/services/opening_insights_service.py tests/services/test_opening_insights_service.py && uv run ruff check app/services/opening_insights_service.py tests/services/test_opening_insights_service.py`
- Spot-check via Bash: `git diff --stat app/schemas/opening_insights.py app/services/opening_insights_constants.py app/services/score_confidence.py` shows zero lines changed in those three files.
</verification>

<success_criteria>
- compute_insights consumes query_resulting_position_wdl with filter parity to query_opening_transitions.
- _classify_row, _compute_score, compute_confidence_bucket, wilson_bounds receive resulting-position W/D/L/N via SimpleNamespace adapter.
- OpeningInsightFinding.{n_games, wins, draws, losses, score, confidence, p_value, ci_low, ci_high} reflect resulting-position aggregation.
- Surfacing gate (HAVING n >= 20) stays on move-played count — query_opening_transitions SQL unchanged.
- Three new transposition-divergence tests pass; full module test run is green.
- D-07 no-touch list verified by git diff (schema, constants, score_confidence untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/80.1-include-transpositions-in-move-explorer-and-opening-insights-stats/80.1-03-SUMMARY.md` recording: (1) which lines in compute_insights were touched, (2) confirmation that _classify_row / _compute_score / compute_confidence_bucket / wilson_bounds bodies are byte-identical, (3) any cascade test churn that hit existing assertions despite the default pos_wdl mirror, (4) actual full-module test runtime, (5) the new test class location.
</output>
