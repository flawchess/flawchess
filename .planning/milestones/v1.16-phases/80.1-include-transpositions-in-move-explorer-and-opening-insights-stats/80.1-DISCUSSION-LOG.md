# Phase 80.1 Discussion Log

**Date:** 2026-05-07
**Phase:** 80.1 — Include transpositions in Move Explorer and Opening Insights stats

## Context Loaded

- `.planning/ROADMAP.md` Phase 80.1 entry (lines 57-70) — authoritative scope and implementation sketch.
- `.planning/STATE.md` — project state (v1.16 Stockfish Eval Analyses milestone, Phase 80 merged, eval pillar already on `resulting_full_hash`).
- `.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-CONTEXT.md` — sibling phase context confirming eval pillar is transposition-inclusive.

## Codebase Scout

- `app/repositories/openings_repository.py` — `query_next_moves` (372), `query_transposition_counts` (457), `query_opening_transitions` (511).
- `app/schemas/opening_insights.py:40-97` — `OpeningInsightFinding` (Wilson + MG-entry eval).
- `app/repositories/stats_repository.py:452` — `query_opening_phase_entry_metrics_batch` (already keyed on `resulting_full_hash`; eval pillar correct).
- `frontend/src/components/move-explorer/MoveExplorer.tsx:340-393` — Moves row + `TranspositionInfo` popover (existing wording correct; no change needed).
- `app/services/score_confidence.py` — Wilson score-test (just migrated 2026-05-07 in 260507-aw5).

## Gray Areas Presented

1. **`n_position_reached` field on `OpeningInsightFinding`** — add a parallel field for honest "Score across X games; you played [move] in Y" disclosure on cards, or skip and keep `n_games` as the single denominator?
2. **Score-pivot recalibration** — Phase 75 thresholds (`SCORE_PIVOT ± MAJOR/MINOR_EFFECT`) calibrated on move-played distributions; resulting-position scores may cluster tighter. Sanity-check in execute / recalibrate in this phase / defer to follow-up?
3. **Tooltip / copy updates** — Move Explorer Score column `InfoPopover` and `OpeningFindingCard` score-badge tooltip wording — clarify "across all games reaching this position, via any move order" or leave as-is?
4. **Move Explorer Moves row sort + muting denominator** — keep `game_count` desc and let mute denominator follow the field switch (resulting-position N), or anchor either back to move-played count?

## User Selection

**"Keep these things as they are."** — single answer covering all four areas. Out-of-scope decoration; the phase becomes the pure mechanical switch from move-played WDL to resulting-position WDL on both surfaces.

## Decisions Captured (mapped to CONTEXT.md)

- D-01 / D-02 — Move Explorer Moves rows: keep `game_count` (move-played) at line 366 of MoveExplorer; recompute `wins/draws/losses/win_pct/draw_pct/loss_pct/score/confidence/p_value` from a batch query keyed on `result_hash` mirroring `query_next_moves` filters.
- D-03 / D-04 — Opening Insights findings: classification, Wilson confidence, ranking, displayed bar all switch to resulting-position WDL. Surfacing gate (n_games >= 10) stays on move-played count.
- D-05 — `OpeningInsightFinding.n_games` becomes resulting-position N. **No** `n_position_reached` companion field.
- D-06 — SQL shape (single CTE vs second-pass on `resulting_full_hash`) left to planner.
- D-07 — Locked "keep as-is" no-touch list: schema fields, tooltip copy, Moves row sort, mute threshold constant, score-pivot constants.
- D-08 — Test churn expected and called out for executor; regenerate expected values from fixtures.
- D-09 — Sanity check (read-only prod-DB queries) during execute; flag and stop if finding volumes shift dramatically — do NOT auto-recalibrate.

## Deferred Ideas

- `n_position_reached` field — UX-polish follow-up if user feedback warrants.
- Tooltip copy refresh — separate copy pass.
- Score-pivot recalibration — own follow-up phase if D-09 sanity check shows material shift.
- Move Explorer sort by resulting-position score — UX tweak, not bug fix.
- Mute threshold anchored to move-played count — follow-up if mute-denominator behavior feels wrong post-switch.
