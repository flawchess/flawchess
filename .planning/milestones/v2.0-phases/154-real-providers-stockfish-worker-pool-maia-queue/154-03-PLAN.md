---
phase: 154-real-providers-stockfish-worker-pool-maia-queue
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/engine/workerPool.ts
  - frontend/src/lib/engine/__tests__/workerPool.test.ts
autonomous: true
gap_closure: true
requirements: [POOL-01, POOL-02, POOL-04]

must_haves:
  truths:
    - "stopAll() settles every in-flight (dispatched, slot.current, state 'thinking') grade() promise with an empty Map, not just queued pending requests (POOL-04, D-03) — closes VERIFICATION Truth #9 / CR-01"
    - "terminate() settles every in-flight grade() promise with an empty Map before worker.terminate() kills the worker (POOL-04, D-03) — closes VERIFICATION Truth #9 / CR-02"
    - "grade() called with an already-aborted AbortSignal resolves immediately with an empty Map, never enqueuing or dispatching a search (WR-01)"
    - "grade() with an empty candidateUcis array resolves an empty Map without dispatching an unrestricted searchmoves go (WR-05)"
    - "An asynchronous worker script-load failure (worker.onerror) and a synchronous construction failure both capture to Sentry (source tag) and settle affected promises instead of hanging (WR-03, WR-04)"
  artifacts:
    - frontend/src/lib/engine/workerPool.ts
    - frontend/src/lib/engine/__tests__/workerPool.test.ts
  key_links:
    - "workerPool.grade stays structurally assignable to EngineProviders.grade in frontend/src/lib/engine/types.ts — the fixes touch bodies only, not the 2-arg (+optional signal) signature"
    - "Sentry capture reuses @sentry/react (already the frontend convention) under a 'stockfish-worker-pool' source tag, mirroring maiaQueue.ts's existing capture pattern"
---

<objective>
Close the two reproduced lifecycle promise-hang defects in `workerPool.ts` (CR-01 stopAll,
CR-02 terminate) plus the abort/input/observability warnings that share the same "a promise
silently hangs forever with zero Sentry visibility" failure theme (WR-01, WR-03, WR-04, WR-05).
Strengthen the tests that currently mask the hang (`void second;`, IN-04), and correct the
module header/docstrings that claim priority-scheduling behavior the module cannot exhibit
under the frozen 2-arg contract (WR-02, documented as a Phase 155 deferral).

Purpose: Phase 155 wires this pool into `mctsSearch.dispatchExpansion`, which `await`s
`grade()` on every expansion and calls `stopAll()`/`terminate()` on the D-03 "engine wins,
pause the eval bar" gate, on unmount, and on navigation. A permanently-unresolved `grade()`
promise deadlocks the entire engine search — the exact code paths this plan makes reliably
settling.
Output: modified `workerPool.ts` + strengthened `workerPool.test.ts`. No signature changes,
no new exports, no wiring into any hook (still Phase 155). `types.ts`/`mctsSearch.ts` stay
UNTOUCHED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-CONTEXT.md
@.planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-VERIFICATION.md
@.planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-REVIEW.md
@frontend/src/lib/engine/workerPool.ts
@frontend/src/lib/engine/__tests__/workerPool.test.ts
@frontend/src/lib/engine/maiaQueue.ts
</context>

<artifacts_this_phase_produces>
No new exported symbols. Modified function bodies in `workerPool.ts`: `stopAll`, `terminate`,
`grade`, `ensureSpawned`, `createSlot` (adds `worker.onerror`), plus a new module-level
`import * as Sentry from '@sentry/react'` and corrected header/docstring prose. New/updated
test cases in `workerPool.test.ts`: an awaited-in-flight `stopAll()` assertion (replacing
`void second;`), a `terminate()`-with-in-flight-request test, a pre-aborted-signal test, an
empty-`candidateUcis` test, a `worker.onerror` Sentry+settle test, and a construction-failure
Sentry-capture assertion; refreshed file header. Consumed (already exist, frozen):
`EngineProviders`/`MoveGrade` from `frontend/src/lib/engine/types.ts`, `parseInfoLine` from
`frontend/src/hooks/uciParser.ts`, `@sentry/react`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settle in-flight requests on stopAll()/terminate() (CR-01, CR-02, IN-04)</name>
  <files>frontend/src/lib/engine/workerPool.ts, frontend/src/lib/engine/__tests__/workerPool.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/workerPool.ts (stopAll lines 361-375, terminate lines 377-388, the bestmove/stopPending discard branch in handleLine lines 253-264, the abort path lines 329-355)
    - frontend/src/lib/engine/__tests__/workerPool.test.ts (the stopAll test lines 331-344 with `void second;`, the terminate test lines 346-356, the abort test lines 366-381, driveInit helper lines 58-62)
    - frontend/src/lib/engine/maiaQueue.ts (terminate lines 221-233 — the CORRECT precedent that folds currentBatch into the unresolved set; mirror this handling for slot.current)
    - .planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-VERIFICATION.md (gap #1 reason + missing items)
  </read_first>
  <behavior>
    - After grade() dispatches to a slot (slot reaches state 'thinking', slot.current set),
      calling stopAll() resolves that in-flight grade() promise with an empty Map (new Map())
      within the test's microtask window — it does NOT hang.
    - The same in-flight setup followed by terminate() resolves the in-flight grade() promise
      with an empty Map and calls worker.terminate() on every slot.
    - The existing bestmove/stopPending discard branch still tolerates slot.current === null
      (stopAll already nulled it) without throwing and without double-resolving.
    - Queued (still-pending, undispatched) requests continue to resolve empty on stopAll()/
      terminate() exactly as before (no regression to the currently-passing pending-drain path).
  </behavior>
  <action>
    In `stopAll()`, inside the loop over slots for each slot whose state is 'thinking': after
    posting 'stop' / setting stopPending=true / state='stopping', ALSO settle the dispatched
    request by calling `slot.current?.resolve(new Map())` and then `slot.current = null`, so the
    request is settled before its eventual bestmove is discarded by the stopPending/FLAWCHESS-7V
    guard in handleLine (which will now see slot.current === null and skip the resolve it never
    did anyway). Keep the trailing `while (pending.length > 0)` pending-drain unchanged. In
    `terminate()`, inside the loop over slots after posting 'stop' and calling
    `slot.worker.terminate()`, ALSO call `slot.current?.resolve(new Map())` then
    `slot.current = null` for every slot (mirroring maiaQueue.terminate()'s existing correct
    folding of currentBatch into the settled set) — because worker.terminate() kills the worker
    so no bestmove will ever arrive to trigger cleanup. Keep the pending-drain and
    `slots.length = 0` / `spawned = false` reset unchanged. In `workerPool.test.ts`: in the
    existing stopAll test, DELETE the `void second;` line and instead drive
    `createdWorkers[0]` so `second` is the DISPATCHED request (it dequeues before `first` per the
    'd7d5' < 'e7e5' tie-break comment already in the test), then assert
    `await expect(second).resolves.toEqual(new Map())`. Add a new test that dispatches a request
    in-flight and asserts `terminate()` both resolves that in-flight promise to `new Map()` and
    sets `terminated === true` on every worker. Author the strengthened assertions FIRST (they
    fail RED against current source), then apply the source fix to GREEN.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/engine/__tests__/workerPool.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The stopAll test no longer contains `void second;` (`grep -n "void second" frontend/src/lib/engine/__tests__/workerPool.test.ts` returns nothing) and instead awaits the in-flight promise resolving to `new Map()`.
    - A terminate-with-in-flight-request test exists and asserts the in-flight grade() promise resolves to `new Map()`.
    - `stopAll()` and `terminate()` in workerPool.ts each call `slot.current?.resolve(new Map())` and null `slot.current` for the relevant slots.
    - `cd frontend && npm test -- --run src/lib/engine/__tests__/workerPool.test.ts` passes with all pre-existing tests still green (no regression).
    - `cd frontend && npx tsc -b` reports zero errors.
  </acceptance_criteria>
  <done>stopAll() and terminate() reliably settle in-flight grade() promises; the test that formerly masked CR-01 with `void second;` now proves the fix, and a new terminate-in-flight test covers CR-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-fast grade() input guards — pre-aborted signal (WR-01) and empty candidateUcis (WR-05)</name>
  <files>frontend/src/lib/engine/workerPool.ts, frontend/src/lib/engine/__tests__/workerPool.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/workerPool.ts (grade lines 307-359 — cache lookup, ensureSpawned, enqueue, the signal.addEventListener('abort', ...) listener that only fires on the abort transition)
    - frontend/src/lib/engine/__tests__/workerPool.test.ts (the unstarted-abort test lines 366-381, the grade dispatch tests lines 206-305 for the MockWorker driving pattern)
    - .planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-REVIEW.md (WR-01 lines 97-105, WR-05 lines 133-140)
  </read_first>
  <behavior>
    - grade(fen, candidateUcis, signal) where signal.aborted is ALREADY true resolves to an
      empty Map immediately, constructs no Worker, enqueues nothing, and posts no 'go' message.
    - grade(fen, []) (empty candidateUcis) resolves to an empty Map immediately, constructs no
      Worker (on a cold pool), and never posts `setoption name MultiPV value 0` or a
      `go ... searchmoves ` message with an empty searchmoves list.
    - A NON-aborted signal still follows the existing abort-listener path unchanged (the
      unstarted-abort test at lines 366-381 stays green).
  </behavior>
  <action>
    At the very top of `grade()` — BEFORE the cache lookup and BEFORE `ensureSpawned()` — add two
    fail-fast early returns: (1) `if (candidateUcis.length === 0) return Promise.resolve(new Map())`
    (WR-05 — an empty searchmoves list makes Stockfish search all moves and burn full movetime on
    the public EngineProviders.grade surface), and (2) `if (signal?.aborted) return
    Promise.resolve(new Map())` (WR-01 — a listener added via addEventListener never fires for an
    already-aborted signal, so the search would otherwise run to completion). Do NOT change the
    existing in-Promise `signal.addEventListener('abort', ...)` block that handles the
    abort-during-flight transition — the new guard only covers the already-aborted-at-call-time
    case. In `workerPool.test.ts` add: a pre-aborted-signal test (construct an AbortController,
    call `controller.abort()` BEFORE `grade(TEST_FEN, ['e7e5'], controller.signal)`, assert the
    promise resolves to `new Map()` and `createdWorkers.length === 0`), and an empty-candidates
    test (`await pool.grade(TEST_FEN, [])` resolves `new Map()` and posts no `go ` message /
    constructs no worker). Tests RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/engine/__tests__/workerPool.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A pre-aborted-signal test asserts grade() resolves `new Map()` with zero Worker constructions.
    - An empty-`candidateUcis` test asserts grade() resolves `new Map()` and emits no `go ` message.
    - grade() in workerPool.ts contains an early `candidateUcis.length === 0` guard and a
      `signal?.aborted` guard, both before `ensureSpawned()`.
    - The existing unstarted-abort test (lines 366-381) still passes.
    - `cd frontend && npm test -- --run src/lib/engine/__tests__/workerPool.test.ts` and `npx tsc -b` both pass.
  </acceptance_criteria>
  <done>grade() fails fast and empty on an already-aborted signal or an empty candidate list, never spawning a worker or emitting a malformed unrestricted search.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Worker-failure observability — onerror + construction-catch Sentry, empty-pool drain, WR-02 docstring honesty (WR-03, WR-04)</name>
  <files>frontend/src/lib/engine/workerPool.ts, frontend/src/lib/engine/__tests__/workerPool.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/workerPool.ts (createSlot lines 276-289, ensureSpawned lines 291-305 with the empty `catch { continue; }`, module header lines 1-17, priority-queue header lines 102-108, grade lines 307-359)
    - frontend/src/lib/engine/maiaQueue.ts (ensureSpawned lines 190-206 — the CORRECT precedent: Sentry.captureException with a `source` tag on construction failure; and the handleMessage error branch lines 172-187 — the Sentry tag convention this file should mirror)
    - frontend/src/lib/engine/__tests__/maiaQueue.test.ts (the `vi.mock('@sentry/react', ...)` pattern line 21, the construction-failure test lines 282-297, the worker-error test lines 268-280)
    - .planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-REVIEW.md (WR-03 lines 113-117, WR-04 lines 119-131, WR-02 lines 107-111)
  </read_first>
  <behavior>
    - When a slot's worker fires an asynchronous `error` event (script 404 / CSP / syntax
      error — never a synchronous throw from `new Worker`), it is captured to Sentry with a
      `source: 'stockfish-worker-pool'` tag; the slot is marked dead (removed from active
      dispatch); and if no live slot remains, every still-pending request resolves to an empty
      Map instead of hanging forever.
    - A synchronous `new Worker` construction throw is captured to Sentry with the same
      `source` tag (no longer an empty `catch { continue; }`), and the pool still degrades to
      whatever slots succeeded (the existing graceful-degradation floor test stays green).
    - If EVERY slot construction throws (slots.length === 0 after the spawn loop), a subsequent
      grade() resolves its request empty rather than enqueuing into a queue nothing will service.
    - The module header and priority-queue docstring no longer assert that dispatch runs "toward
      the currently-highest-scoring root line first" as present behavior; they state that the
      priority channel is populated in Phase 155 (WR-02 honesty — see deferred_findings).
  </behavior>
  <action>
    Add `import * as Sentry from '@sentry/react'` at the top of workerPool.ts (the module
    currently imports no Sentry). In `createSlot()`, set `worker.onerror` to a handler that: calls
    `Sentry.captureException` with `new Error(...)` (a fixed, variable-free message per CLAUDE.md
    Sentry grouping rules) tagged `{ source: 'stockfish-worker-pool' }`; marks the slot dead by
    setting `slot.isReady = false` and, if the slot has an in-flight request, resolving it
    `slot.current?.resolve(new Map())` then `slot.current = null`; and, when no slot in `slots`
    remains `isReady`, drains `pending` (resolve each `new Map()`). In `ensureSpawned()`, replace
    the empty `catch { continue; }` with a catch that calls `Sentry.captureException` (message
    variable-free, tagged `source: 'stockfish-worker-pool'`) then `continue`. After the spawn
    loop in `ensureSpawned()`, if `slots.length === 0`, do NOT leave the caller enqueued forever —
    have `grade()` detect the no-live-slot condition after `ensureSpawned()` and resolve its
    request empty (either check `slots.length === 0` in grade() right after ensureSpawned and
    resolve `new Map()`, or drain pending inside ensureSpawned). Finally, correct the misleading
    prose: in the file header (lines 4-5) and the priority-queue section header (lines 102-108),
    replace claims that the pool "dispatches work toward the currently-highest-scoring root line
    first" with an accurate statement that request `priority`/`depth` are populated by the Phase
    155 MCTS orchestrator and are currently always 0 (the ordering machinery is correct and
    unit-tested but unreachable through the frozen 2-arg grade() contract until then — see
    VERIFICATION Truth #4 / WR-02). In `workerPool.test.ts`: add the `vi.mock('@sentry/react',
    () => ({ captureException: vi.fn() }))` module mock (mirror maiaQueue.test.ts line 21) and
    import `* as Sentry`; add a test firing `worker.onerror` on a slot with an in-flight request
    that asserts the promise resolves `new Map()` AND `Sentry.captureException` was called with a
    `source: 'stockfish-worker-pool'` tag; and extend the existing graceful-degradation test
    (lines 389-405) to also assert `Sentry.captureException` was called for the simulated
    construction failure. Do NOT weaken the existing floor assertion (3 live slots after 1 of 4
    fails). Tests RED first, then GREEN.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/engine/__tests__/workerPool.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@sentry/react" frontend/src/lib/engine/workerPool.ts` shows the Sentry import; no `catch {` in workerPool.ts is empty (each catch calls `Sentry.captureException`).
    - `grep -n "onerror" frontend/src/lib/engine/workerPool.ts` shows a `worker.onerror` handler in createSlot.
    - A `worker.onerror` test asserts the in-flight promise resolves `new Map()` and `Sentry.captureException` was called with `tags` containing `source: 'stockfish-worker-pool'`.
    - The graceful-degradation test additionally asserts `Sentry.captureException` fired on the construction failure; the 3-live-slots floor assertion is unchanged and green.
    - The workerPool.ts header/priority-queue prose no longer claims present-tense "toward the currently-highest-scoring root line first" dispatch; it references the Phase 155 priority source (`grep -n "Phase 155" frontend/src/lib/engine/workerPool.ts` returns a match near the priority-queue docstring).
    - No variable is interpolated into any Sentry error message string in workerPool.ts (grouping-safe per CLAUDE.md).
    - `cd frontend && npm test -- --run src/lib/engine/__tests__/workerPool.test.ts && npm run lint && npx tsc -b` all green.
  </acceptance_criteria>
  <done>Both worker-failure paths (async onerror, sync construction throw) are Sentry-visible and settle affected promises; an all-slots-failed pool no longer silently hangs; the header no longer overstates priority behavior.</done>
</task>

</tasks>

<deferred_findings>
Findings triaged this pass but deliberately NOT changed, with rationale:

- **WR-02 (priority channel unreachable, VERIFICATION Truth #4):** DEFERRED to Phase 155.
  The frozen 2-arg `EngineProviders.grade(fen, candidateUcis)` contract has no channel for a
  caller to supply a `priority`/`depth`, and there is NO caller in Phase 154 (the module is
  imported by nothing but its own tests until Phase 155 wires it). Adding an unused optional
  `hints` argument now would be speculative API surface built against no real caller. The
  ordering machinery (`enqueue`/`dequeueHighestPriority`) is correct and unit-tested in
  isolation. Phase 155 owns the MCTS orchestrator that computes per-root-line practical scores;
  it will supply real priorities via an optional 4th `grade()` argument (a `{ priority?, depth? }`
  hints bag, still structurally assignable to the 2-arg contract) or a pool-level
  `setPriorityHint(fen, priority)` map — decided when the real priority source exists. Task 3 of
  this plan removes the misleading present-tense docstring claim so the source no longer asserts
  behavior it cannot currently exhibit. Tracked forward as a Phase 155 requirement.

- **IN-01 (cacheGrades overwrites instead of merging):** DEFERRED. Never returns wrong data
  (cache hits require full candidate coverage) — only re-buys already-graded moves. A throughput
  optimization, not a correctness or hang defect; out of scope for this gap-closure pass.

- **IN-02 (cached containers shared by reference):** DEFERRED. Latent only — the sole current/
  Phase-155 consumer (`mctsSearch`) is read-only. No live corruption path today.

- **IN-03 (computePoolSize undefined-cores fallback takes the mobile branch):** DEFERRED. Only
  "coincidentally correct" today because `MOBILE_POOL_SIZE === DESKTOP_POOL_MIN === 2`, but it
  produces the correct value and is not a live bug. Touching the verified-green sizing logic/test
  is not justified for a hang-defect closure pass; note it for a future tuning change.

- **IN-05 (maiaQueue caches `{}` on missing ELO):** DEFERRED and belongs to maiaQueue.ts (Plan
  04's file). Cannot fire today (the worker echoes requested ELOs verbatim). Defensive-only.
</deferred_findings>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stockfish Worker → main thread | UCI text + async `error` events cross back; both are untrusted input to the main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-03-01 | Denial of Service (self-inflicted) | unsettled grade() promises on stopAll/terminate/abort/worker-failure | high | mitigate | Tasks 1-3 settle every in-flight/pending request with an empty Map on every lifecycle and failure path, closing the permanent-deadlock class that would freeze Phase 155's engine search |
| T-154-03-02 | Denial of Service | empty-searchmoves unrestricted Stockfish search (WR-05) | low | mitigate | Task 2 early-returns an empty Map for empty candidateUcis before any `go` |
| T-154-03-03 | Information disclosure (observability gap) | silent worker-load failure with no Sentry event (WR-03/WR-04) | medium | mitigate | Task 3 captures async onerror + sync construction throws to Sentry under a `stockfish-worker-pool` source tag |

Block threshold (ASVS L1): high. T-154-03-01 is high-severity and is mitigated by this plan's core tasks — no unmitigated high/critical threat remains.
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` fully green.
- VERIFICATION Truth #9 re-verifiable: a scratch test driving a MockWorker to 'thinking' then
  calling stopAll() (separately terminate()) sees the in-flight grade() promise resolve to
  `new Map()` within the microtask window (was: timeout).
- `grep -n "void second" frontend/src/lib/engine/__tests__/workerPool.test.ts` returns nothing.
- WR-01/WR-05 guards present at the top of grade(); pre-aborted-signal and empty-candidates
  tests green.
- WR-03/WR-04: `worker.onerror` handler + non-empty construction catch both Sentry-capturing;
  onerror + construction-failure tests assert the `stockfish-worker-pool` source tag.
</verification>

<success_criteria>
- stopAll()/terminate() settle in-flight grade() promises (CR-01/CR-02 closed) — POOL-04/D-03.
- Already-aborted signal and empty candidate list fail fast empty (WR-01/WR-05) — POOL-04.
- Async worker-load failure and sync construction failure are Sentry-visible and non-hanging
  (WR-03/WR-04) — POOL-01 pool robustness.
- Misleading priority-dispatch docstring corrected; WR-02 tracked forward to Phase 155 — POOL-02.
- All pre-existing workerPool tests remain green (no regression).
</success_criteria>

<output>
Create `.planning/phases/154-real-providers-stockfish-worker-pool-maia-queue/154-03-SUMMARY.md` when done
</output>
