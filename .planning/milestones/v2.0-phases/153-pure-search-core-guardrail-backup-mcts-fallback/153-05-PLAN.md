---
phase: 153-pure-search-core-guardrail-backup-mcts-fallback
plan: 05
type: execute
wave: 4
depends_on: ["153-01", "153-02", "153-03", "153-04"]
files_modified:
  - frontend/src/lib/engine/fallbackExpectimax.ts
  - frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts
  - frontend/knip.json
autonomous: true
requirements: [ENGINE-06]

must_haves:
  truths:
    - "fallbackExpectimax is a depth-limited expectimax that implements the IDENTICAL SearchRunner interface and is swapped in for a test run with no interface change (ENGINE-06, SC5)."
    - "The fallback reuses backup.ts, leafScore.ts, and truncateAndRenormalize — it does NOT reimplement its own leaf-conversion or backup logic, so its practicalScore semantics match the MCTS core (D-06)."
    - "The full engine public surface passes knip (imported by tests) and tsc -b; the whole phase suite is green (ENGINE-06/07 phase gate)."
  artifacts:
    - frontend/src/lib/engine/fallbackExpectimax.ts
    - frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts
  key_links:
    - "The SC5 swap-in test imports BOTH mctsSearch and fallbackExpectimax and runs each under the same SearchRunner-typed call site."
    - "If knip flags engine public exports as unused, a documented knip.json ignore entry is the sanctioned remediation."
---

<objective>
Build `fallbackExpectimax.ts` — the depth-limited expectimax guardrail behind the identical `SearchRunner` interface — prove the SC5 swap-in, and run the phase integration gate (full suite + lint + tsc -b + knip).

Purpose: ENGINE-06/SC5 require the fallback to be EXERCISED, not just claimed — a real second implementation of the frozen contract that reuses the same `backup.ts`/`leafScore.ts` so its score semantics cannot silently drift. This plan also closes the phase's knip caveat (engine exports consumed only by tests) explicitly.

Output: `fallbackExpectimax` (a `SearchRunner`), the swap-in test, any documented knip.json accommodation, and a green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-CONTEXT.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md
@frontend/src/lib/engine/guardrail.ts
@frontend/src/lib/engine/backup.ts
@frontend/src/lib/engine/select.ts
@frontend/src/lib/engine/leafScore.ts
@frontend/src/lib/engine/mctsSearch.ts
@frontend/knip.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: fallbackExpectimax.ts — depth-limited expectimax reusing shared primitives (ENGINE-06, SC5)</name>
  <files>frontend/src/lib/engine/fallbackExpectimax.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Pattern 1/2 reuse lines 171-248; Open Question 2 lines 472-475 — the fallback MUST reuse leafScore/truncateAndRenormalize, not just backup; System diagram fallback box lines 136-141)
    - frontend/src/lib/engine/mctsSearch.ts (sibling runner — same SearchRunner contract, same rootMover threading, same terminal/ELO/snapshot rules; the fallback is structurally simpler: uniform depth-first descent to maxPlies, no PUCT budget allocation)
    - frontend/src/lib/engine/backup.ts, leafScore.ts, select.ts (the shared primitives to import)
    - frontend/src/lib/engine/guardrail.ts (the SearchRunner type)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/fallbackExpectimax.ts` exporting `fallbackExpectimax` typed as `SearchRunner` (from `./guardrail`). Implement a depth-limited expectimax that walks the tree uniformly to `budget.maxPlies` (no PUCT/visit budget — the guardrail alternative to MCTS), computing `rootMover` ONCE and threading it into `leafExpectedScore` for every leaf (identical root-relative frame as the MCTS core). It MUST import and reuse `backupExpectation`/`backupRootMax` from `./backup`, `leafExpectedScore` from `./leafScore`, and `truncateAndRenormalize` from `./select` — do NOT reimplement leaf conversion, truncation, or backup (RESEARCH Open Question 2: reusing only backup.ts is insufficient; leaf-frame and truncation must also be shared or practicalScore semantics silently diverge from D-06). Apply the same per-node color-keyed ELO (`budget.elo[side]`, side from the node's own fen), the same terminal short-circuit (chess.js `isGameOver`, root-relative fixed value), the same `nodesEvaluated += 1` per expansion (D-09), the same `onSnapshot`-after-every-backup emission with no wall-clock (D-10), and honor `signal`. Return the same `EngineSnapshot` shape with `rankedLines` sorted by practicalScore descending, canonical-UCI tie-break. Header JSDoc cites ENGINE-06 / SC5 and states this is the drop-in guardrail behind the identical contract.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | grep -E "engine/fallbackExpectimax" || echo "no type errors in fallbackExpectimax.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `fallbackExpectimax.ts` exports `fallbackExpectimax`, assignable to `SearchRunner` (verified by `npx tsc -b` clean).
    - The file imports `backupExpectation`/`backupRootMax` from `./backup`, `leafExpectedScore` from `./leafScore`, and `truncateAndRenormalize` from `./select` (reuse, not reimplementation).
    - No second copy of the sigmoid/truncation/backup math is declared in the file.
    - ELO is keyed on each node's own fen side (`budget.elo[side]`); `rootMover` computed once; no `Date.now`/`performance.now`.
  </acceptance_criteria>
  <done>The fallback runner exists behind the identical SearchRunner contract, reusing the shared primitives; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap-in test + phase integration gate (SC5, knip, lint, tsc, full suite)</name>
  <files>frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts, frontend/knip.json</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md ("Guardrail Swap-in" lines 432-441 — the literal swap-in assertion)
    - frontend/src/lib/engine/mctsSearch.ts, fallbackExpectimax.ts (both runners to swap between)
    - frontend/knip.json (the current ignore/entry config — only add an engine entry if knip actually flags the surface)
    - frontend/src/lib/engine/__tests__/mctsSearch.test.ts (reuse the fabricated-provider construction style)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts`. Build (or reuse the shape of) the fabricated `EngineProviders` + fixed FEN + `budget` from the MCTS tests. The SC5 swap-in test declares a single `SearchRunner`-typed variable, assigns `mctsSearch` to it and runs it, then assigns `fallbackExpectimax` to the SAME variable and runs it with the SAME `(rootFen, budget, providers, onSnapshot, signal)` arguments — asserting both compile against the identical type with NO call-site change and both return a non-empty `rankedLines` array with UCI `rootMove`s and `practicalScore` in [0,1]. Add a determinism check that the fallback, like the MCTS core, produces bit-identical output across two repeated runs. Header JSDoc cites ENGINE-06 / SC5.
    Then run the phase integration gate and resolve every output: `npm run lint`, `npx tsc -b`, `npx vitest run src/lib/engine`, `npm test -- --run`, and `npm run knip`. KNIP CAVEAT: the engine public surface (mctsSearch, fallbackExpectimax, SearchRunner, exported types/constants) is consumed only by test files in this phase (Phase 155 consumes it later). Knip's vitest plugin normally treats test files as entry points, so this should pass. IF `npm run knip` flags any `src/lib/engine/` export as unused, the sanctioned remediation is to add the specific engine file(s) to `knip.json`'s `ignore` array with a comment noting "Phase 153: engine public surface consumed by tests now, by Phase 155 hook later" — do NOT delete or downgrade the export, and do NOT add a blanket `src/lib/engine/**` ignore (keep it to the flagged files). Document whatever knip resolution was applied (or "knip passed unchanged") in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/fallbackExpectimax.test.ts && npm run lint && npx tsc -b && npm run knip && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/fallbackExpectimax.test.ts` passes; the swap-in test runs BOTH runners through one `SearchRunner`-typed variable with identical arguments.
    - The fallback determinism check passes (bit-identical across two runs).
    - `npm run lint` passes with zero errors.
    - `npx tsc -b` reports zero errors across the whole frontend.
    - `npm run knip` passes (unchanged, or with a documented per-file `ignore` entry limited to the flagged engine file(s) — no blanket glob, no deleted export).
    - `npm test -- --run` (full frontend suite) is green.
  </acceptance_criteria>
  <done>SC5 swap-in proven; the full phase gate (lint + tsc + knip + full suite) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → fallback runner | Fabricated providers + fixed FENs; no external input this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-06 | Tampering | score-semantics drift between runners | low | mitigate | The fallback reuses backup.ts/leafScore.ts/truncateAndRenormalize rather than reimplementing them; the swap-in test asserts identical interface + comparable output. Correctness invariant. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase — zero new dependencies (RESEARCH Package Legitimacy Audit). knip.json is edited only to accommodate an intra-repo export visibility rule, not to add a dependency. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/fallbackExpectimax.test.ts` green.
- `cd frontend && npm run lint && npx tsc -b && npm run knip && npm test -- --run` all green.
</verification>

<success_criteria>
- The depth-limited expectimax fallback implements the identical `SearchRunner` and is exercised via a swap-in test (ENGINE-06, SC5).
- The full phase gate (lint, type-check, knip, full test suite) is green.
</success_criteria>

## Artifacts this plan produces

- File `frontend/src/lib/engine/fallbackExpectimax.ts` — exports: `fallbackExpectimax` (a `SearchRunner`).
- File `frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts` — test file (no exports).
- Possible edit to `frontend/knip.json` — a documented per-file `ignore` entry only if knip flags the engine surface.

<output>
Create `.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-05-SUMMARY.md` when done.
</output>
