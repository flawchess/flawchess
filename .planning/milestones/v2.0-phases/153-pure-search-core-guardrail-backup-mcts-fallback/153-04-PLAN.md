---
phase: 153-pure-search-core-guardrail-backup-mcts-fallback
plan: 04
type: execute
wave: 3
depends_on: ["153-01", "153-02", "153-03"]
files_modified:
  - frontend/src/lib/engine/mctsSearch.ts
  - frontend/src/lib/engine/__tests__/mctsSearch.test.ts
autonomous: true
requirements: [ENGINE-01, ENGINE-02, ENGINE-04, ENGINE-05, ENGINE-07]

must_haves:
  truths:
    - "Given a fixed FEN + budget + fabricated providers, mctsSearch returns a ranked RankedLine[] scored by practicalScore (ENGINE-01)."
    - "Every policy() call uses the ELO keyed on the NODE's own side-to-move color, proven for both root colors — never depth/ply parity (ENGINE-04, D-07)."
    - "Expansion truncates the Maia top-k at ~90% mass, grades via the fabricated Stockfish, and converts leaves via the root-relative lichess sigmoid to the depth cutoff (ENGINE-02, ENGINE-05)."
    - "Output — including the FULL onSnapshot emission sequence — is bit-identical across repeated runs at concurrency=1 AND concurrency=2 (ENGINE-07, D-03, D-10)."
  artifacts:
    - frontend/src/lib/engine/mctsSearch.ts
    - frontend/src/lib/engine/__tests__/mctsSearch.test.ts
  key_links:
    - "mctsSearch threads a single rootMover constant into leafScore for every leaf (root-relative frame)."
    - "Concurrent results are buffered and applied in canonical node-id order, never raw arrival order (Pattern 5 / ENGINE-07)."
    - "onSnapshot fires after EVERY completed backup; no Date.now anywhere (D-10)."
---

<objective>
Build `mctsSearch.ts` — the MCTS orchestrator implementing `SearchRunner` — and its full test suite proving ENGINE-01 (ranked output), ENGINE-04 (per-node color-keyed ELO oracle), ENGINE-02/05 (truncation + grade + root-relative leaf sigmoid to the depth cutoff), and ENGINE-07 (bit-identical output + snapshot sequence at concurrency 1 and 2).

Purpose: This is the loop that composes backup/select/leafScore into the frozen `SearchRunner` contract. It is where the two remaining high-risk pitfalls live — asymmetric ELO keyed on side-to-move (not ply parity) and arrival-order nondeterminism under concurrency — so both get dedicated oracle/determinism tests, not incidental coverage.

Output: `mctsSearch` (a `SearchRunner`), and `mctsSearch.test.ts` covering the functional, ELO-oracle, and determinism requirements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-CONTEXT.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md
@frontend/src/lib/engine/types.ts
@frontend/src/lib/engine/guardrail.ts
@frontend/src/lib/engine/backup.ts
@frontend/src/lib/engine/select.ts
@frontend/src/lib/engine/leafScore.ts
@frontend/src/lib/sanToSquares.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: mctsSearch.ts — select→terminal→expand→backup→snapshot loop (ENGINE-01/02/04/05/07)</name>
  <files>frontend/src/lib/engine/mctsSearch.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (System Architecture Diagram lines 109-148; D-03 concurrency lines 15; D-09 node counting lines 27; D-10 snapshot lines 28; Pattern 5 lines 292-295; Pitfall 6 terminal lines 347-351; anti-patterns lines 297-302)
    - frontend/src/lib/engine/types.ts, guardrail.ts (the SearchRunner signature + budget/snapshot shapes)
    - frontend/src/lib/engine/backup.ts, select.ts, leafScore.ts (the primitives to compose — import, do not reimplement)
    - frontend/src/lib/sanToSquares.ts (uciToSquares — reuse for UCI parsing; do NOT write a second parser)
    - frontend/src/lib/liveFlaw.ts (sideToMoveFromFen — for computing rootMover once and each node's own side)
    - frontend/src/hooks/useStockfishGradingEngine.ts (batched pv[0]-keyed grade orchestration shape — structural precedent)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/mctsSearch.ts` exporting `mctsSearch` typed as `SearchRunner` (from `./guardrail`). Compute `rootMover = sideToMoveFromFen(rootFen)` ONCE and thread it as a constant into every `leafExpectedScore(grade, rootMover)` call — never recompute per node (root-relative frame; anti-pattern lines 299). Derive each node's `side` for policy() strictly from that node's OWN fen side field (`fen.split(' ')[1]`), never from depth/ply parity (Pitfall 3/anti-pattern lines 301), and select the ELO as `budget.elo[side]` (D-07). Loop until `nodesEvaluated >= budget.maxNodes`: (1) SELECT root→leaf via the Plan-03 PUCT functions (root uses Q + floor-boosted exploration prior from `rootExplorationPriors`; non-root drops Q), skipping in-flight/pending nodes via a virtual-visit/pending marker (Claude's discretion on mechanics, D-03); (2) TERMINAL CHECK via chess.js `isGameOver()` on the node's fen BEFORE expanding — if terminal, assign a fixed root-relative value (checkmate: 1.0 if the checkmated side is NOT rootMover else 0.0; stalemate/insufficient-material/threefold/draw: 0.5) and skip provider calls entirely (Pitfall 6); (3) EXPAND: call `providers.policy(fen, budget.elo[side], side)`, `truncateAndRenormalize` it (union with `budget.extraRootMoves` at the ROOT only, D-04, before truncation/grading — all graded in ONE batched `providers.grade(fen, ucis)` call), and give each unexpanded child a `leafExpectedScore` estimate from its grade; increment `nodesEvaluated += 1` per expansion (D-09), set `budgetExhausted` when `nodesEvaluated >= budget.maxNodes`, and stop descending past `budget.maxPlies` (hard depth ceiling, ENGINE-05); (4) BACKUP root-ward using `backupExpectation` (non-root, over the FULL truncated set, expanded+unexpanded mixed) and `backupRootMax` (root); (5) call `onSnapshot(snapshot)` after EVERY completed backup with the current `rankedLines` (sorted by practicalScore descending, canonical-UCI tie-break), `nodesEvaluated`, `budgetExhausted` — NO `Date.now()` / wall-clock anywhere (D-10). For `budget.concurrency > 1`, issue up to `concurrency` expansions concurrently but BUFFER their resolved results and APPLY them to the tree in a canonical node-id order assigned at dispatch time (never raw promise-arrival order, Pattern 5) so output is order-independent. Honor `signal` (AbortSignal) by stopping the loop when aborted. Return the final `EngineSnapshot`. Use `?? fallback` narrowing for all indexed/Map access (`noUncheckedIndexedAccess`). Header JSDoc cites ENGINE-01/02/04/05/07 and D-03/D-04/D-07/D-09/D-10.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | grep -E "engine/mctsSearch" || echo "no type errors in mctsSearch.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `mctsSearch.ts` exports `mctsSearch` and it is assignable to the `SearchRunner` type (verified by `npx tsc -b` clean).
    - `rootMover` is computed once (single `sideToMoveFromFen(rootFen)` call) and passed into `leafExpectedScore`; no per-node recomputation of the leaf frame.
    - Each `providers.policy` call's `side`/`elo` derive from the node's own fen side field and `budget.elo[side]` — no `depth % 2` / ply-parity expression appears in the file.
    - A terminal check via chess.js precedes every expansion; terminal nodes never call `policy()`/`grade()`.
    - `nodesEvaluated` increments once per expansion (D-09); `onSnapshot` is called after each backup; no `Date.now` / `performance.now` appears in the file (D-10).
    - `extraRootMoves` is unioned only at the root; deeper nodes use Maia top-k only (D-04).
  </acceptance_criteria>
  <done>The orchestrator implements SearchRunner, composes the Plan 01-03 primitives, and type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: mctsSearch.test.ts — ranked output, ELO oracle (both colors), truncation+leaf end-to-end, terminal (ENGINE-01/02/04/05)</name>
  <files>frontend/src/lib/engine/__tests__/mctsSearch.test.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md ("ELO Oracle Fixture" lines 391-407; "Root-Relative Frame" context; Truncation/Pattern 4)
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md (fabricated-provider construction guidance, lines 208-212)
    - frontend/src/lib/engine/mctsSearch.ts (the runner under test, from Task 1)
    - frontend/src/lib/engine/types.ts (EngineProviders/SearchBudget shapes to fabricate)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/__tests__/mctsSearch.test.ts`. Build a small deterministic fabricated `EngineProviders`: a `policy` returning fixed UCI-keyed distributions per fen (concentrated so truncation keeps a known small set) and a `grade` returning fixed white-POV `MoveGrade`s per (fen, uci). ENGINE-01: assert `mctsSearch` returns a non-empty `rankedLines` array sorted by `practicalScore` descending with UCI `rootMove` values. ENGINE-04 ELO ORACLE: wrap `policy` in a recorder capturing every `(fen, elo, side)`; run a search from a WHITE-to-move root and again from a BLACK-to-move root (e.g. the position after 1.e4), with `budget.elo = { w: 1500, b: 1800 }`; assert for EVERY recorded call `side === 'w' ⇒ elo === 1500` and `side === 'b' ⇒ elo === 1800`, for BOTH root-color runs (proving keying on node side-to-move, not root color or ply parity). ENGINE-02/05 end-to-end: assert only the ~90%-mass truncated candidates are graded (the recorder's `grade` candidate lists never include the dropped tail move) and that a leaf's `practicalScore` matches `leafExpectedScore` of its fabricated grade under the root frame; include a fixture that reaches `budget.maxPlies` and stops descending. TERMINAL: a fixture one ply from checkmate yields a confident practicalScore (≈1.0 when the root player delivers mate, ≈0.0 when mated) with no `policy()` call recorded for the terminal node. Header JSDoc cites ENGINE-01/02/04/05.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts` passes.
    - The ELO-oracle test records provider calls and asserts `w→1500`/`b→1800` for BOTH a white-root and a black-root run.
    - A test proves the dropped-tail (sub-90%-mass) move is never passed to `grade()`.
    - A terminal-position fixture asserts a confident ≈1.0/≈0.0 practicalScore and zero provider calls for the terminal node.
    - A depth-cutoff fixture confirms descent stops at `budget.maxPlies`.
  </acceptance_criteria>
  <done>Ranked output, both-color ELO oracle, truncation+leaf end-to-end, and terminal handling all pass.</done>
</task>

<task type="auto">
  <name>Task 3: mctsSearch.test.ts — determinism suite, concurrency 1 and 2 (ENGINE-07)</name>
  <files>frontend/src/lib/engine/__tests__/mctsSearch.test.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Determinism Mechanics / ENGINE-07 lines 52; Pattern 5 lines 292-295; Pitfall 4 lines 335-339; D-10 full-sequence assertion lines 28/88)
    - frontend/src/lib/engine/__tests__/mctsSearch.test.ts (the fabricated providers from Task 2 to reuse)
  </read_first>
  <action>
    Extend `frontend/src/lib/engine/__tests__/mctsSearch.test.ts` with an ENGINE-07 determinism describe block. Run the SAME fabricated providers + fixed FEN + fixed `budget` TWICE and assert the final `EngineSnapshot` is deeply equal (`toEqual`) across runs. Additionally capture the FULL sequence of `onSnapshot` emissions (push each into an array) and assert the two runs produce deeply-equal snapshot SEQUENCES, not just equal final output (D-10 — the strengthened guarantee). Add a `concurrency = 2` fixture whose fabricated provider promises resolve in a deliberately jittered order (e.g. alternating microtask delays) and assert its output + snapshot sequence are bit-identical to the `concurrency = 1` run for the same inputs — proving the buffer-then-apply-in-canonical-order mechanism (Pattern 5) holds. Header/describe JSDoc cites ENGINE-07 / D-03 / D-10.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts` passes with the determinism block included.
    - A test asserts two repeated runs produce `toEqual` final snapshots AND `toEqual` full onSnapshot SEQUENCES.
    - A `concurrency = 2` test with jittered-resolution fabricated promises produces output + snapshot sequence bit-identical to the `concurrency = 1` run.
  </acceptance_criteria>
  <done>Bit-identical output and snapshot sequence proven at concurrency 1 and 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mctsSearch | Fabricated providers + fixed FENs; no external/network input this phase. |
| `rootFen: string` param | A FEN string; in this phase always well-formed test input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-04a | Tampering | ELO keying (Pitfall 3 parity bug) | low | mitigate | ELO derived from each node's own fen side-to-move, never ply parity; the both-color ELO-oracle test enforces it. Correctness invariant. |
| T-153-04b | Tampering | determinism under concurrency (Pitfall 4) | low | mitigate | Canonical-order buffered apply + UCI tie-break; the concurrency=2 determinism test enforces it. Correctness invariant. |
| T-153-05 | Denial of Service | unbounded search | low | accept | Bounded by `budget.maxNodes` + `budget.maxPlies` hard ceilings (D-09); no user-tunable unbounded path this phase. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase — zero new dependencies (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts` green (functional + ELO oracle + determinism).
- `cd frontend && npx tsc -b` zero errors for `mctsSearch.ts`.
</verification>

<success_criteria>
- Ranked practicalScore output (ENGINE-01), both-color ELO oracle (ENGINE-04), truncation+leaf-sigmoid+depth-cutoff (ENGINE-02/05), and bit-identical output+snapshot sequence at concurrency 1 and 2 (ENGINE-07) all pass.
</success_criteria>

## Artifacts this plan produces

- File `frontend/src/lib/engine/mctsSearch.ts` — exports: `mctsSearch` (a `SearchRunner`).
- File `frontend/src/lib/engine/__tests__/mctsSearch.test.ts` — test file (no exports).

<output>
Create `.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-04-SUMMARY.md` when done.
</output>
