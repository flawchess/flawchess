---
phase: 155-react-hook-anytime-ui-free-analysis
plan: 03
type: execute
wave: 2
depends_on: [155-01]
files_modified:
  - frontend/src/components/analysis/FlawChessEngineLines.tsx
  - frontend/src/components/analysis/EngineLines.tsx
  - frontend/src/components/analysis/__tests__/FlawChessEngineLines.test.tsx
autonomous: true
requirements: [DISPLAY-02, DISPLAY-03]
must_haves:
  truths:
    - "FlawChessEngineLines renders the top 3 ranked practical lines (D-08), each with a two-number score-pair badge: objective (blue, STOCKFISH_ACCENT) and practical (bronze/gold, FLAWCHESS_ENGINE_HEADLINE_ACCENT), both on the pawn scale, both white-POV (D-06, DISPLAY-03)."
    - "Each line renders its modalPath as clickable SAN chips, first MAX_PLIES=5 with an expand chevron for the rest, mirroring EngineLines (D-07, DISPLAY-02)."
    - "Move chips are clickable and call onMoveClick(uciMoves) to graft the line prefix as a sideline, with the same hover MiniBoard preview as EngineLines (D-10)."
    - "First paint shows a fixed-height skeleton sized for 3 rows so card height is stable as lines arrive (D-09)."
  artifacts:
    - frontend/src/components/analysis/FlawChessEngineLines.tsx
    - frontend/src/components/analysis/__tests__/FlawChessEngineLines.test.tsx
  key_links:
    - "practicalScore (0-1 root-STM) → expectedScoreToWhitePovCp(rootMover) → formatScore() for the practical number; objectiveEvalCp is already white-POV → formatScore() directly."
    - "modalPath (UCI[]) → replayPvLine(baseFen, slice(0, MAX_PLIES)) → SAN chips (reused from EngineLines)."
---

<objective>
Build `FlawChessEngineLines.tsx` — the display card body that renders the hook's ranked practical
lines. It is a structural sibling of `EngineLines.tsx`: same row skeleton, chip/badge/hover-preview
pattern, expand chevron — with three deltas: top 3 lines (D-08), a two-number objective/practical
score-pair badge (D-06, DISPLAY-03), and modal-path chips walked from `RankedLine.modalPath`
(D-07, DISPLAY-02).

Purpose: This is the visible surface of DISPLAY-02 and DISPLAY-03. It is body-only (like
EngineLines) — the Card/CardHeader/Switch wrapper and placement are Plan 04's job.

Output: `FlawChessEngineLines.tsx` + its filled test file. May extend `EngineLines.tsx` to share
`replayPvLine`/`formatScore` and a rows-parametrized skeleton (see action).
</objective>

## Artifacts This Phase Produces (this plan)

- `FlawChessEngineLines` component (accepts `rankedLines: RankedLine[]`, `baseFen`/`rootMover`, `startPly`, `flipped`, `onMoveClick`, `isSearching`)
- Local `MAX_LINES = 3` constant (distinct from EngineLines' `MAX_LINES = 2`)
- new data-testids: `analysis-flawchess-card`, `analysis-flawchess-info`, `flawchess-line-{n}-move-{m}`, `flawchess-line-{n}-expand`
- Filled `FlawChessEngineLines.test.tsx` (DISPLAY-02 chips, DISPLAY-03 score pair)
- (possibly) exported `replayPvLine`/`formatScore` + a `rows`-parametrized `EngineLinesSkeleton` from EngineLines.tsx

## Out of Scope (do NOT build)

- The Card/CardHeader/Switch wrapper, card placement (desktop column + mobile tab), and hook mounting — all Plan 04.
- Eval-bar precedence (Plan 04).
- Board arrows / arrow colors (Phase 156). The badge must NEVER render "best move" unqualified (ARROW-04 principle starts here, D-06) — frame as objective vs "practical for you".
- Game-review comparison (Phase 157).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-RESEARCH.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-PATTERNS.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-UI-SPEC.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-VALIDATION.md
@frontend/src/lib/engine/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FlawChessEngineLines (score-pair badge + modal-path SAN chips + graft)</name>
  <files>frontend/src/components/analysis/FlawChessEngineLines.tsx, frontend/src/components/analysis/EngineLines.tsx</files>
  <read_first>
    - frontend/src/components/analysis/EngineLines.tsx (full 406 lines — MAX_LINES line 38, MAX_PLIES line 40, replayPvLine lines 58-82 (LOCAL, not exported), EngineLinesSkeleton line 122 + LINES_MIN_HEIGHT lines 105-115, formatScore lines 161-175 (LOCAL), PvLineRow lines 220-354 including the badge span + move-chip map + hover Tooltip/MiniBoard + ChevronDown expand, EngineLines export lines 362-406, the noUncheckedIndexedAccess narrowing at line 388)
    - frontend/src/lib/engine/types.ts (RankedLine = { rootMove, practicalScore(0-1 root-STM), objectiveEvalCp(white-POV cp|null), modalPath(UCI[]), visits })
    - frontend/src/lib/liveFlaw.ts (expectedScoreToWhitePovCp from Plan 01; sideToMoveFromFen; MoverColor)
    - frontend/src/lib/theme.ts (STOCKFISH_ACCENT, FLAWCHESS_ENGINE_ACCENT, FLAWCHESS_ENGINE_HEADLINE_ACCENT)
    - 155-UI-SPEC.md Component Inventory §1 (authoritative visual/behavioral contract) + Copywriting Contract (aria-label wording, never "best move")
    - 155-PATTERNS.md FlawChessEngineLines section (row layout, two-segment badge, data-testid names)
  </read_first>
  <behavior>
    - Given a rankedLines array of 4, renders exactly 3 rows (MAX_LINES=3).
    - Each row's badge shows two numbers: objective via formatScore(line.objectiveEvalCp, null); practical via formatScore(expectedScoreToWhitePovCp(line.practicalScore, rootMover), null). Objective span colored STOCKFISH_ACCENT, practical span colored FLAWCHESS_ENGINE_HEADLINE_ACCENT.
    - A line with objectiveEvalCp=null renders the objective side as the "…" placeholder (formatScore's null branch), not a crash.
    - modalPath of length 8 renders the first 5 SAN chips + an expand chevron; expanding reveals the rest.
    - Clicking chip m calls onMoveClick with the UCI prefix slice(0, m+1).
    - Before the first snapshot (empty rankedLines while searching), renders the fixed-height skeleton.
    - No rendered string contains "best move" unqualified.
  </behavior>
  <action>
    Create `FlawChessEngineLines.tsx` as a structural clone of `EngineLines.tsx` (body-only, no Card wrapper),
    props accepting `rankedLines: RankedLine[]` (instead of PvLine[]) plus `baseFen`/`rootMover`
    (derive rootMover via sideToMoveFromFen(baseFen) if not passed), `startPly`, `flipped`, `isSearching`,
    `onMoveClick`. Local `MAX_LINES = 3` (D-08) and `MAX_PLIES = 5` (D-07) constants — do NOT mutate
    EngineLines' shared constants.
    Score-pair badge (D-06, DISPLAY-03): replace the single-number badge with a two-segment span —
    objective number (formatScore(objectiveEvalCp, null), STOCKFISH_ACCENT) + a thin separator + practical
    number (formatScore(expectedScoreToWhitePovCp(practicalScore, rootMover), null),
    FLAWCHESS_ENGINE_HEADLINE_ACCENT). Both white-POV, both pawn scale. aria-label per the UI-SPEC Copywriting
    Contract: "Line {n}: objectively {X}, practically {Y} for you" — never the bare phrase "best move"
    (ARROW-04 principle, D-06).
    Modal path (D-07, DISPLAY-02): render line.modalPath as clickable SAN chips via replayPvLine, first
    MAX_PLIES + ChevronDown expand for the rest, with the identical hover Tooltip+MiniBoard preview and the
    identical onMoveClick(moves.slice(0, moveIndex+1)) graft semantics (D-10). data-testids:
    `analysis-flawchess-card` (root), `flawchess-line-{n}-move-{m}` (chips), `flawchess-line-{n}-expand`
    (chevron) per the UI-SPEC naming table.
    Skeleton (D-09): reuse EngineLinesSkeleton sized for 3 rows. Prefer extending EngineLinesSkeleton with a
    small `rows`/min-height prop over duplicating it. For replayPvLine and formatScore, prefer exporting them
    from EngineLines.tsx and importing (avoids a Knip-flagged unused duplicate) over copy-paste; if exporting
    is awkward, a verbatim duplicate is acceptable per the UI-SPEC allowance — but do not diverge the logic.
    Any EngineLines.tsx edits are additive (add `export`, add an optional prop) — do not change existing
    EngineLines behavior. Use text-xs on the dense engine-surface (approved CLAUDE.md exception, same as
    EngineLines); narrow every array index (noUncheckedIndexedAccess). data-testid + semantic
    button/aria-label on every interactive chip/chevron.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Renders top 3 lines; each badge shows objective (blue) + practical (bronze/gold), both white-POV pawn scale.
    - modalPath renders 5 SAN chips + expand; chips graft via onMoveClick; hover MiniBoard preview present.
    - Fixed-height skeleton on first paint; no "best move" unqualified string anywhere.
    - EngineLines.tsx changes are additive only; type-checks clean; knip clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill in FlawChessEngineLines test (chips + score pair) — DISPLAY-02/03</name>
  <files>frontend/src/components/analysis/__tests__/FlawChessEngineLines.test.tsx</files>
  <read_first>
    - frontend/src/components/analysis/__tests__/EngineLines.test.tsx (chip-rendering + score-badge assertion style, MiniBoard/Tooltip mocking if any)
    - The Wave 0 scaffold from Plan 01 at this same path (replace the DISPLAY-02 / DISPLAY-03 `it.todo` placeholders)
    - frontend/src/lib/engine/types.ts (RankedLine shape for building fixtures)
  </read_first>
  <behavior>
    - Render with a 4-line rankedLines fixture: asserts exactly 3 rows render (MAX_LINES=3).
    - Asserts a line's modalPath renders SAN chips, first 5 visible + an expand control present for a >5-ply path (DISPLAY-02).
    - Asserts the score-pair badge shows both the objective and practical numbers for a known fixture (e.g. objectiveEvalCp=+300 → "+3.0"; practicalScore=0.9, rootMover white → "+6.0" region via expectedScoreToWhitePovCp) (DISPLAY-03).
    - Asserts clicking a chip fires onMoveClick with the expected UCI prefix.
  </behavior>
  <action>
    Convert the DISPLAY-02 and DISPLAY-03 `it.todo` scaffolds into real @testing-library/react render tests
    against a RankedLine fixture. Assert row count (3), SAN-chip + expand rendering (DISPLAY-02), the two
    score numbers with correct color-coded spans / aria-label (DISPLAY-03), and onMoveClick graft on chip
    click (D-10). Build fixtures from a real starting FEN so replayPvLine produces valid SAN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analysis/__tests__/FlawChessEngineLines.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file green; asserts 3-row cap, SAN chips + expand (DISPLAY-02), objective+practical numbers (DISPLAY-03), and onMoveClick graft.
    - No watch-mode flags; under 60s feedback budget.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine string → DOM | UCI/SAN strings from the search core are rendered into the card. Not a new boundary — identical to EngineLines.tsx, hardened project-wide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-04 | Tampering / Information Disclosure | Unescaped engine-string XSS (malicious/corrupted UCI/SAN rendered raw) | low | mitigate | Render all SAN/UCI strings as React children (auto-escaped), never dangerouslySetInnerHTML — inherited by construction from EngineLines.tsx (T-137-03 mitigation). replayPvLine's per-move try/catch falls back to raw UCI, never throws. |

No high-severity threats. Pure client-side rendering over already-hardened engine strings.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/analysis/__tests__/FlawChessEngineLines.test.tsx` green.
- `cd frontend && npm run lint && npx tsc -b && npm run knip` clean.
- Per-wave: `cd frontend && npm test` full suite green before merging Wave 2.
</verification>

<success_criteria>
- Card body renders top 3 practical lines with the objective/practical score pair and modal-path SAN chips + graft.
- DISPLAY-02 and DISPLAY-03 tests green; no "best move" unqualified copy.
</success_criteria>

<output>
Create `.planning/phases/155-react-hook-anytime-ui-free-analysis/155-03-SUMMARY.md` when done.
</output>
