---
phase: 155-react-hook-anytime-ui-free-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/lib/liveFlaw.ts
  - frontend/src/components/ui/switch.tsx
  - frontend/src/lib/__tests__/expectedScoreToWhitePovCp.test.ts
  - frontend/src/hooks/__tests__/useFlawChessEngine.test.ts
  - frontend/src/components/analysis/__tests__/FlawChessEngineLines.test.tsx
autonomous: true
requirements: [DISPLAY-03]
must_haves:
  truths:
    - "expectedScoreToWhitePovCp(es, rootMover) converts a 0-1 root-STM expected score to a white-POV centipawn value, and returns ±MATE_CP_EQUIVALENT at the es<=0 / es>=1 mate boundaries instead of ±Infinity/NaN (D-06)."
    - "theme.ts exposes FLAWCHESS_ENGINE_ACCENT (brand brown) and FLAWCHESS_ENGINE_HEADLINE_ACCENT (bronze/gold) alongside STOCKFISH_ACCENT and MAIA_ACCENT (D-05)."
    - "A reusable Switch UI primitive exists at components/ui/switch.tsx whose checked-track fill color is caller-supplied per card accent (D-03)."
    - "Wave 0 test scaffolds exist for the hook and the card so later waves flesh them into passing tests; the pure-function test is fully green now."
  artifacts:
    - frontend/src/lib/theme.ts
    - frontend/src/lib/liveFlaw.ts
    - frontend/src/components/ui/switch.tsx
    - frontend/src/lib/__tests__/expectedScoreToWhitePovCp.test.ts
    - frontend/src/hooks/__tests__/useFlawChessEngine.test.ts
    - frontend/src/components/analysis/__tests__/FlawChessEngineLines.test.tsx
  key_links:
    - "expectedScoreToWhitePovCp imports LICHESS_K and MATE_CP_EQUIVALENT from @/generated/flawThresholds (single source of truth) — never redefines them."
---

<objective>
Build the shared foundation every later wave consumes: the two new `theme.ts` source-accent
tokens (D-05), the inverse-sigmoid `expectedScoreToWhitePovCp` pure function that powers the
score-pair badge (D-06, DISPLAY-03), a reusable `Switch` UI primitive for the 3-toggle refactor
(D-03), and the Wave 0 test scaffolds required by 155-VALIDATION.md.

Purpose: These are the leaf dependencies of the phase's dependency graph. The hook (Plan 02),
the card (Plan 03), and the Analysis integration (Plan 04) all import from here, so this must
land first (Wave 1).

Output: extended `theme.ts` + `liveFlaw.ts`, new `switch.tsx`, one fully-green pure-function
test, and two test scaffolds (`it.todo` placeholders) that Plans 02/03 convert to passing tests.
</objective>

## Artifacts This Phase Produces (this plan)

- `FLAWCHESS_ENGINE_ACCENT` + `FLAWCHESS_ENGINE_HEADLINE_ACCENT` (theme.ts)
- `expectedScoreToWhitePovCp(es, rootMover)` (liveFlaw.ts)
- `Switch` (components/ui/switch.tsx)
- Test files: `expectedScoreToWhitePovCp.test.ts`, `useFlawChessEngine.test.ts` (scaffold), `FlawChessEngineLines.test.tsx` (scaffold)

## Out of Scope (do NOT build)

- Board arrows or arrow-color tokens (Phase 156 — the FlawChess arrow color is deliberately NOT decided here; do not add an arrow token to theme.ts).
- Game-review overlay (Phase 157).
- Any consumer wiring of these tokens/functions — that is Plans 03/04.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-RESEARCH.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-PATTERNS.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-UI-SPEC.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add theme tokens + inverse-sigmoid helper (with mate-boundary guards)</name>
  <files>frontend/src/lib/theme.ts, frontend/src/lib/liveFlaw.ts, frontend/src/lib/__tests__/expectedScoreToWhitePovCp.test.ts</files>
  <read_first>
    - frontend/src/lib/theme.ts (STOCKFISH_ACCENT line ~70, MAIA_ACCENT line ~74 — match export style/comment convention)
    - frontend/src/lib/liveFlaw.ts (full file, 63 lines — MoverColor line 24, sideToMoveFromFen line 27, evalToExpectedScore lines 36-51, the forward function being inverted)
    - frontend/src/generated/flawThresholds.ts (LICHESS_K line 20, MATE_CP_EQUIVALENT line 19)
    - 155-RESEARCH.md Code Examples §1 (the exact derivation + verified round-trip values) and Common Pitfalls §2 (mate-boundary blow-up)
  </read_first>
  <behavior>
    - expectedScoreToWhitePovCp(0.9, 'white') ≈ +596.6 (white ahead).
    - expectedScoreToWhitePovCp(0.9, 'black') ≈ -596.6 (white-POV negative when black is winning).
    - expectedScoreToWhitePovCp(0, 'white') === -MATE_CP_EQUIVALENT (-1000); (0, 'black') === +1000.
    - expectedScoreToWhitePovCp(1, 'white') === +1000; (1, 'black') === -1000.
    - Round-trip: for a mid-range cp, evalToExpectedScore then expectedScoreToWhitePovCp returns approximately the original cp (within a small epsilon) for both colors.
    - No NaN/Infinity for any es in [0, 1].
  </behavior>
  <action>
    Add `FLAWCHESS_ENGINE_ACCENT = 'oklch(0.55 0.09 55)'` (brand brown) and
    `FLAWCHESS_ENGINE_HEADLINE_ACCENT = 'oklch(0.75 0.14 85)'` (bronze/gold) to theme.ts, verbatim from
    155-UI-SPEC.md's New theme.ts tokens block, alongside STOCKFISH_ACCENT/MAIA_ACCENT with the same
    comment convention (D-05). These are source-accent + headline tokens only — do NOT add any board-arrow
    color (that is a Phase 156 decision).
    Add `expectedScoreToWhitePovCp(es: number, rootMover: MoverColor): number` to liveFlaw.ts, alongside
    evalToExpectedScore, as the algebraic inverse: sign = rootMover==='white'?1:-1; special-case es<=0 →
    -MATE_CP_EQUIVALENT*sign and es>=1 → MATE_CP_EQUIVALENT*sign (mirroring the forward function's
    mate-before-sigmoid convention — a naive log-odds inversion produces ±Infinity/NaN at these exact
    boundaries, which RankedLine.practicalScore genuinely hits on a forced-mate subtree, per Pitfall 2);
    otherwise return Math.log(es/(1-es)) / (LICHESS_K * sign). Import LICHESS_K and MATE_CP_EQUIVALENT from
    @/generated/flawThresholds — never redefine them (they mirror the backend curve). This is display-only
    math (D-06); it is never fed back into the search core.
    Write the fully-green table test at the named path asserting the behaviors above for both rootMover
    colors and both mate boundaries.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/expectedScoreToWhitePovCp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both theme tokens exported with the D-05 comments; no arrow token added.
    - expectedScoreToWhitePovCp returns finite values across [0,1] and the exact ±MATE_CP_EQUIVALENT boundary values; test file green.
    - Constants imported from @/generated/flawThresholds, not redefined.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Hand-roll the Switch UI primitive (accent-fill via prop)</name>
  <files>frontend/src/components/ui/switch.tsx</files>
  <read_first>
    - frontend/src/components/ui/checkbox.tsx (entire 31-line file — the exact hand-rolled Radix-wrapper + cn() + data-slot + data-checked: styling convention to mirror)
    - 155-UI-SPEC.md Registry Safety + Component Inventory §3 (switch checked-track fill uses each card's own accent; do NOT hardcode a single accent in the primitive)
    - 155-RESEARCH.md Standard Stack (radix-ui@1.4.3 already exposes `Switch`; confirmed present in node_modules)
  </read_first>
  <action>
    Create `frontend/src/components/ui/switch.tsx` as a hand-rolled wrapper around `import { Switch as SwitchPrimitive } from 'radix-ui'`, mirroring checkbox.tsx's structure exactly: `SwitchPrimitive.Root` + `SwitchPrimitive.Thumb`, `data-slot` attributes, `cn(...)` className merge, unstyled `{...props}` passthrough, `React.ComponentProps<typeof SwitchPrimitive.Root>` typing. Use Radix's `data-state="checked"/"unchecked"` (or `data-checked:`) attribute selectors for the on/off track+thumb transition — no custom CSS transitions. The checked-state track fill must be caller-controllable (accept the accent via `style`/`className` from the calling card so Stockfish blue / Maia violet / FlawChess brown each tint their own switch per D-03); do NOT bake a single accent into the primitive. Add `data-testid` passthrough so callers can set `btn-analysis-*-toggle`. Do NOT run `npx shadcn add switch` — hand-roll to match every other ui/ primitive and avoid re-theming (research + UI-SPEC recommendation). No new npm dependency is installed.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - switch.tsx exists, imports Switch from the already-installed radix-ui, type-checks clean.
    - Checked-track fill is caller-supplied (not a hardcoded single accent); animation uses Radix data-state selectors.
    - No new package added to package.json.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffolds for the hook and the card</name>
  <files>frontend/src/hooks/__tests__/useFlawChessEngine.test.ts, frontend/src/components/analysis/__tests__/FlawChessEngineLines.test.tsx</files>
  <read_first>
    - 155-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact `-t "throttle"` / `-t "abort"` filter names and the DISPLAY-02/03 chip assertions the impl waves must satisfy)
    - frontend/src/hooks/__tests__/useStockfishEngine.test.ts (driveInit + vi.useFakeTimers pattern to mirror in Plan 02)
    - frontend/src/components/analysis/__tests__/EngineLines.test.tsx (chip-rendering assertion style to mirror in Plan 03)
  </read_first>
  <action>
    Create the two Wave 0 test files as compiling scaffolds so the suite stays green between waves:
    `useFlawChessEngine.test.ts` with a `describe('useFlawChessEngine', ...)` containing `it.todo(...)`
    placeholders named to match the VALIDATION filters — one whose name contains "throttle" (first snapshot
    commits near-instantly; later snapshots throttle at ~150ms, never a debounce that delays first paint,
    DISPLAY-01) and one whose name contains "abort" (navigating to a new FEN aborts the previous run AND
    calls pool.stopAll(), DISPLAY-01 / Pitfall 1 regression guard). `FlawChessEngineLines.test.tsx` with
    `describe('FlawChessEngineLines', ...)` and `it.todo(...)` placeholders for: renders modalPath as SAN
    chips with first MAX_PLIES=5 + expand chevron (DISPLAY-02); renders the objective/practical score pair
    per line (DISPLAY-03). Use `it.todo` (not skipped real assertions) so the files compile and pass now;
    Plans 02/03 will replace each todo with a real passing test. Do NOT import the not-yet-existing hook or
    component at scaffold stage (todos need no imports).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useFlawChessEngine.test.ts src/components/analysis/__tests__/FlawChessEngineLines.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both scaffold files compile and the suite reports them as passing/pending (todo), not failing.
    - Todo names include the literal substrings "throttle" and "abort" so Plan 02's `-t` filters resolve.
    - All four 155-VALIDATION.md Wave 0 test seams are represented (the fourth, Analysis.test.tsx, already exists and is extended in Plan 04 — not created here).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side UI-wiring foundation over an already-security-reviewed engine core (Phases 136-154). No network, auth, session, or DB surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-01 | Tampering / Information Disclosure | Any engine/UCI/SAN string rendered by future consumers of these tokens | low | accept | No rendering happens in this plan; downstream cards render engine strings as React children (auto-escaped, never dangerouslySetInnerHTML) — inherited from EngineLines.tsx by construction (see 155-RESEARCH.md Security Domain). |
| T-155-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed — the Switch primitive reuses the already-present radix-ui dependency; no `npx shadcn add` run. No legitimacy checkpoint required (155-RESEARCH.md Package Legitimacy Audit: "Not applicable this phase"). |

No high-severity threats. This is a pure client-side chrome/utility phase introducing no new attack surface (155-RESEARCH.md Security Domain, confirmed).
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/expectedScoreToWhitePovCp.test.ts` green.
- `cd frontend && npx vitest run src/hooks/__tests__/useFlawChessEngine.test.ts src/components/analysis/__tests__/FlawChessEngineLines.test.tsx` green (todos pending).
- `cd frontend && npm run lint && npx tsc -b` clean (lint/test do NOT type-check — tsc is mandatory per CLAUDE.md).
- `cd frontend && npm run knip` reports no new dead exports (theme tokens / expectedScoreToWhitePovCp / Switch will be consumed by Plans 02-04; if knip flags them before those land, note it — they are consumed later this phase, not truly dead).
</verification>

<success_criteria>
- Two new theme tokens + inverse-sigmoid helper + Switch primitive exist and type-check.
- Pure-function test green; hook/card scaffolds green (todo).
- No new npm dependency; no arrow token added (Phase 156 boundary respected).
</success_criteria>

<output>
Create `.planning/phases/155-react-hook-anytime-ui-free-analysis/155-01-SUMMARY.md` when done.
</output>
