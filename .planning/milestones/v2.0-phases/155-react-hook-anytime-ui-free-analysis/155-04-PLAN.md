---
phase: 155-react-hook-anytime-ui-free-analysis
plan: 04
type: execute
wave: 3
depends_on: [155-01, 155-02, 155-03]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/pages/__tests__/Analysis.test.tsx
autonomous: true
requirements: [DISPLAY-01, DISPLAY-04]
must_haves:
  truths:
    - "The FlawChess Engine card is visible directly above the Maia card in the left column on desktop AND inside the mobile 'Human' tab, on-by-default (D-01, D-02, DISPLAY-04)."
    - "All three engine cards (Stockfish, Maia, FlawChess) have an independent on/off Switch in their header, each tinted with its own accent, all defaulting ON (D-03)."
    - "The left eval bar shows FC (brown, cap 'FC') when the FlawChess Engine is enabled and falls back to Maia (violet, 'Maia') when only Maia is on; FC takes precedence (D-04)."
    - "While the FlawChess Engine runs, the right SF eval bar is fed the top line's objectiveEvalCp (no separate standalone useStockfishEngine search on that position), honoring POOL-04 mutual exclusion (D-04)."
  artifacts:
    - frontend/src/pages/Analysis.tsx
    - frontend/src/pages/__tests__/Analysis.test.tsx
  key_links:
    - "useFlawChessEngine mounted with { fen: position, enabled: flawChessEnabled, elo: selectedElo }; topLine = rankedLines[0] feeds both the left FC bar fraction and the right SF bar's objectiveEvalCp."
    - "useStockfishEngine.enabled becomes stockfishToggleOn && !flawChessEnabled (the eval-bar handoff / POOL-04 mutual exclusion); the Maia switch gates useMaiaEngine + useStockfishGradingEngine but NOT the engine's internal maiaQueue."
---

<objective>
Integrate everything into `/analysis`: mount `useFlawChessEngine`, place the `FlawChessEngineLines`
card above the Maia card in both the desktop left column and the mobile "Human" tab (D-01,
DISPLAY-04), refactor the single `engineEnabled` icon-button into three independent header
`Switch` toggles (D-03), and wire the eval-bar precedence + data-source handoff (D-04). This is the
plan that makes the anytime engine actually visible and live on the page (DISPLAY-01 + DISPLAY-04).

Purpose: All prior waves produced isolated pieces; this plan composes them on the real page and
resolves the two research decisions that touch a Phase-154-complete criterion (POOL-04
mutual-exclusion via the Stockfish-switch gating, and the SF-bar objective-eval handoff).

Output: modified `Analysis.tsx` + extended `Analysis.test.tsx` with FC-precedence cases.
</objective>

## Artifacts This Phase Produces (this plan)

- `flawChessEnabled` + `maiaEnabled` toggle state (in addition to the existing `engineEnabled`), all default ON
- The FlawChess Engine `Card` + `CardHeader` + `Switch` + `CardBody` wrapper around `FlawChessEngineLines`, in both desktop and mobile layouts
- Eval-bar left-slot precedence (FC > Maia) + right-slot SF source swap to `objectiveEvalCp`
- 'FC' bar cap (extends the existing 'Maia' | 'SF' cap union)
- new data-testid: `analysis-flawchess-eval-bar` (left slot when FC active); `btn-analysis-flawchess-toggle` / `btn-analysis-maia-toggle` (upgraded switches)
- Extended `Analysis.test.tsx` FC-precedence describe block (DISPLAY-04)

## Out of Scope (do NOT build)

- Board arrows / arrow colors / arrow toggles (Phase 156).
- Game-review overlay + "what you played vs practically best" comparison (Phase 157). Do not touch `?game_id&ply` review-mode plumbing beyond the existing `position`/`enabled` props.
- Any change to EngineLines.tsx, the hook, or the card internals (owned by Plans 02/03).
- A device-adaptive default (on desktop/off mobile) — that is the SC4 fallback, built only if the mobile-memory UAT fails; keep on-by-default everywhere for now (D-02).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-RESEARCH.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-PATTERNS.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-UI-SPEC.md
@.planning/phases/155-react-hook-anytime-ui-free-analysis/155-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-toggle Switch refactor + engine-gating (D-03, POOL-04 handoff)</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (engineEnabled state line 299; useStockfishEngine mount lines 355-357; useStockfishGradingEngine lines 585-588; useMaiaEngine line 434; the CardHeader toggle-button pattern lines 1508-1529 with the Cpu icon-Button + aria-pressed + btn-analysis-engine-toggle; useMaiaEloDefault/selectedElo lines 403-409)
    - frontend/src/components/ui/switch.tsx (Plan 01 — the new primitive; checked-track accent is caller-supplied)
    - frontend/src/lib/theme.ts (STOCKFISH_ACCENT, MAIA_ACCENT, FLAWCHESS_ENGINE_ACCENT)
    - 155-UI-SPEC.md Component Inventory §3 (toggle binding table — authoritative) + Copywriting Contract (aria-labels)
    - 155-RESEARCH.md Open Question 1 + Assumption A2 (the Stockfish-switch gating decision) and Pitfall 5 (Analysis.tsx is already ~1564 lines — extract a small toggle-row/eval-bar helper rather than inlining, to stay under the CLAUDE.md bloat limits)
  </read_first>
  <action>
    Add `const [maiaEnabled, setMaiaEnabled] = useState(true)` and `const [flawChessEnabled, setFlawChessEnabled]
    = useState(true)` alongside the existing `engineEnabled` (all default ON per D-02/D-03).
    Replace the Stockfish card header's icon-`Button` toggle with the new `Switch` (keep
    `data-testid="btn-analysis-engine-toggle"`, aria-label "Toggle Stockfish engine", checked-track tinted
    STOCKFISH_ACCENT). Retrofit the Maia card header analogously (`btn-analysis-maia-toggle`, "Toggle Maia",
    MAIA_ACCENT) and add the FlawChess card header switch (`btn-analysis-flawchess-toggle`, "Toggle FlawChess
    Engine", FLAWCHESS_ENGINE_ACCENT, caption "FlawChess Engine" styled FLAWCHESS_ENGINE_ACCENT). Per Pitfall
    5, factor the near-identical switch+caption into a tiny local helper/component shared across all three
    headers rather than tripling inline JSX — keep each header body under the bloat limits.
    Toggle binding (UI-SPEC §3, resolved): the Maia switch gates BOTH `useMaiaEngine` (chart worker) AND
    `useStockfishGradingEngine` (colors the Moves-by-Rating chart) — change their `enabled`/`fen` inputs from
    `engineEnabled` to `maiaEnabled`. The engine's internal maiaQueue (Plan 02) is a separate instance and
    MUST keep running regardless of the Maia switch — do not gate the FlawChess hook on maiaEnabled.
    Stockfish-switch gating (D-04 handoff / POOL-04, Open Question 1 / A2): the Stockfish switch's own state
    is `engineEnabled`, but `useStockfishEngine`'s effective `enabled`/`fen` input becomes
    `engineEnabled && !flawChessEnabled` — so the standalone Stockfish search is suppressed while the
    FlawChess Engine runs (mutual exclusion; the SF bar is instead fed the engine's objective root eval in
    Task 2). This preserves the switch's independent ON/OFF UI state while honoring the already-complete
    POOL-04 criterion (never two Stockfish searches on the same position). Leave a code comment citing D-04 /
    Open Question 1 at the gating site. Mount `useFlawChessEngine({ fen: position, enabled: flawChessEnabled,
    elo: selectedElo })` and derive `topLine = rankedLines[0]` (narrow before use). Every switch is an
    interactive element: data-testid + aria-label required.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Three independent header Switches (Stockfish/Maia/FlawChess), each accent-tinted, all default ON, each with data-testid + aria-label.
    - Maia switch gates useMaiaEngine + useStockfishGradingEngine only; FlawChess hook independent of it.
    - useStockfishEngine effective enabled = engineEnabled && !flawChessEnabled (commented, cites D-04/OQ1).
    - useFlawChessEngine mounted with position/flawChessEnabled/selectedElo; topLine narrowed. Type-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Card placement (desktop + mobile) + eval-bar precedence & SF-source handoff (D-01/D-04)</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (left EvalBar hard-wired to Maia ~lines 1067-1075 with maiaWhiteFraction/MAIA_ACCENT/analysis-maia-eval-bar; right EvalBar ~lines 1107-1113 with gameOverlay.evalCp/STOCKFISH_ACCENT; evalBarCap helper ~lines 1129-1156 with the 'Maia'|'SF' union; analysis-human-column desktop ~line 1424-1436 with MaiaHumanPanel; mobile 'Human' tab content ~lines 1264-1281; the CardBody engine-off/loading pattern ~lines 1534-1551; playUciLine from useAnalysisBoard ~line 347)
    - frontend/src/components/analysis/FlawChessEngineLines.tsx (Plan 03 — props: rankedLines, baseFen, startPly, flipped, onMoveClick, isSearching)
    - frontend/src/components/analysis/EngineLines.tsx (EngineLinesSkeleton for the loading state)
    - 155-PATTERNS.md Analysis.tsx section (exact eval-bar precedence + right-slot swap snippets) + Common Pitfall 4 (bar caps already use text-xs — match that precedent for the new 'FC' cap, do not introduce a new size)
    - 155-UI-SPEC.md Layout Contract + Component Inventory §1/§2
  </read_first>
  <action>
    Card placement (D-01, DISPLAY-04, mobile-parity): wrap `FlawChessEngineLines` in a `Card` +
    `CardHeader` (the FlawChess switch from Task 1) + `CardBody`, stacked directly ABOVE `MaiaHumanPanel` in
    (a) the desktop `analysis-human-column` and (b) the mobile "Human" tab content — BOTH, per CLAUDE.md
    mobile-parity and D-01. Do not add a 5th mobile tab. Root testid `analysis-flawchess-card`, header
    `analysis-flawchess-info`. CardBody follows the existing engine-card body pattern: skeleton while
    searching-before-first-snapshot, `"FlawChess Engine off"` row when `!flawChessEnabled` (exact clone of
    the "Engine off" div), else `<FlawChessEngineLines rankedLines={...} isSearching={...} baseFen={position}
    startPly={currentPly} flipped={boardFlipped} onMoveClick={playUciLine} />`. On worker-init failure show
    the CLAUDE.md isError copy: "FlawChess Engine unavailable. Something went wrong. Please try again in a
    moment." Pass the SAME `playUciLine` callback both cards already use (D-10).
    Left eval-bar precedence (D-04): make the left slot conditional — when `flawChessEnabled`, render with
    `accentColor={FLAWCHESS_ENGINE_ACCENT}`, `testId="analysis-flawchess-eval-bar"`, and `whiteFraction`
    derived from `topLine.practicalScore` root-STM→white-POV (rootMover==='white' ? practicalScore : 1 -
    practicalScore; 0.5 when no topLine); else the existing Maia bar unchanged (MAIA_ACCENT,
    analysis-maia-eval-bar). Left bar cap: `flawChessEnabled ? 'FC' (FLAWCHESS_ENGINE_ACCENT) : 'Maia'
    (MAIA_ACCENT)` — extend the cap union to include 'FC'; match the existing caps' text-xs size (Pitfall 4,
    pre-existing precedent — do not introduce text-sm here).
    Right eval-bar SF-source handoff (D-04): while `flawChessEnabled`, feed the right SF bar
    `evalCp={topLine?.objectiveEvalCp ?? null}`, `evalMate={null}`, `depth={0}` (objectiveEvalCp is already
    white-POV and never carries a mate — ±1000 reads as near-mate on the sigmoid scale); else the existing
    `gameOverlay.evalCp`/`evalMate`/`evalDepth`. Cap stays 'SF' STOCKFISH_ACCENT (no visual change, only the
    data source). Per Pitfall 5, extract the precedence derivations (fcWhiteFraction, the left/right source
    selection) into a small memo/derived block rather than inlining conditionals in the JSX, to stay under
    the bloat limits. Narrow all array-index access.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - FlawChess card appears above the Maia card in BOTH desktop column and mobile Human tab, on-by-default, with off/loading/error states.
    - Left bar shows FC (brown, cap 'FC', analysis-flawchess-eval-bar) when FC enabled, else Maia; right bar fed objectiveEvalCp while FC runs.
    - Bar caps stay text-xs (matched precedent); precedence logic extracted, not inlined; type-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend Analysis.test.tsx with FC-precedence cases + phase type-check gate (DISPLAY-04)</name>
  <files>frontend/src/pages/__tests__/Analysis.test.tsx</files>
  <read_first>
    - frontend/src/pages/__tests__/Analysis.test.tsx (existing `describe('Maia eval bar perspective (151.1 UAT regression)')` ~line 211 asserting on analysis-maia-eval-bar/analysis-eval-bar test IDs; the useStockfishGradingEngine mock ~line 44 — add analogous mocks for useFlawChessEngine / workerPool / maiaQueue as needed for jsdom)
    - 155-VALIDATION.md DISPLAY-04 row (the `-t "FlawChess"` filter + the left-slot precedence / right-slot swap behavior to assert)
  </read_first>
  <behavior>
    - With the FlawChess Engine enabled (default), the left bar renders `analysis-flawchess-eval-bar` (FC precedence) and NOT `analysis-maia-eval-bar`.
    - With the FlawChess Engine toggled off but Maia on, the left bar falls back to `analysis-maia-eval-bar`.
    - The FlawChess card (`analysis-flawchess-card`) renders above the Maia panel by default.
  </behavior>
  <action>
    Add a new `describe('FlawChess Engine eval-bar precedence (Phase 155)')` block to the EXISTING
    Analysis.test.tsx (do not create a new file). Mock the FlawChess hook / providers as needed (jsdom has no
    Worker — mirror the existing grading-engine mock). Assert: FC-enabled → left slot is
    analysis-flawchess-eval-bar; FC-off/Maia-on → left slot falls back to analysis-maia-eval-bar; the FC card
    is present by default. Keep at least one test name containing "FlawChess" so the VALIDATION `-t`
    filter resolves. Then run the full phase gate.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx -t "FlawChess" && npm test -- --run && npm run lint && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - New FC-precedence describe block green; asserts left-slot precedence + fallback + card presence (DISPLAY-04).
    - Full frontend suite green; `npm run lint` clean; `npx tsc -b` zero errors (type-check is mandatory — lint/test do not type-check per CLAUDE.md).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Page-level composition. All rendered engine strings are auto-escaped React children; no new network/auth/DB/session surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-05 | Denial of Service (self-inflicted, resource) | Concurrent Stockfish instances (standalone useStockfishEngine + the 2-4 pool) on the same position | medium | mitigate | Gate useStockfishEngine.enabled on `engineEnabled && !flawChessEnabled` so the standalone search is suppressed while the FlawChess pool runs — honors POOL-04 mutual exclusion (A2). The residual on-by-default mobile-memory ceiling is the SC4 real-device UAT gate (D-02), not code-fixable here. |
| T-155-06 | Tampering / Information Disclosure | Engine/UCI/SAN strings rendered on the page | low | mitigate | React-children auto-escaping inherited from EngineLines/FlawChessEngineLines; no dangerouslySetInnerHTML. |

No high-severity threats. Pure client-side UI composition; the one resource concern (T-155-05) is mitigated in code plus the scheduled SC4 UAT.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx -t "FlawChess"` green.
- Phase gate (per 155-VALIDATION.md): `cd frontend && npm test -- --run && npm run lint && npx tsc -b && npm run knip` all clean.
- **HUMAN-UAT (SC4, deferred from Phase 154 — the on-by-default gate):** On a real iOS device, open `/analysis`, navigate through ~20 positions with all 3 engines ON; confirm no crash/OOM and lines keep refining. If it fails, the fallback is the device-adaptive default (on desktop / off mobile) — not built unless needed (D-02).
- **HUMAN-UAT (decision confirmation):** confirm the two research decisions that touch the Phase-154-complete POOL-04 criterion read correctly on-device — (a) the standalone Stockfish search is suppressed while FC runs and the SF bar shows the engine's objective root eval (D-04 handoff); (b) both colors sharing `selectedElo` for `budget.elo` is acceptable for free analysis (true asymmetry deferred to Phase 157). Surface for sign-off in `/gsd-verify-work`.
- **HUMAN-UAT (perceptual):** live-refine reads smoothly (no jank/flicker) as lines reorder at the 150ms cadence on desktop + mobile.
</verification>

<success_criteria>
- The FlawChess Engine surfaces on `/analysis` (DISPLAY-04): card above Maia in desktop + mobile, on by default, lines appearing immediately and refining live (DISPLAY-01).
- Three independent header switches; left eval bar FC>Maia precedence; right SF bar fed the engine's objective root eval while FC runs.
- Full suite + lint + tsc + knip green; SC4 + decision UAT items surfaced for sign-off.
</success_criteria>

<output>
Create `.planning/phases/155-react-hook-anytime-ui-free-analysis/155-04-SUMMARY.md` when done.
</output>
