---
phase: 158-flawchess-engine-displayed-eval-provenance-reconciliation-se
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useStockfishGradingEngine.ts
  - frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts
  - frontend/src/hooks/useStockfishEngine.ts
autonomous: true
requirements: [SEED-087]
must_haves:
  truths:
    - "The shared grading run reaches a depth where its eval for a shared candidate (e.g. O-O) agrees with the free run's eval for the same move within a few centipawns (noise level), on both a middlegame and an endgame position (SEED-087 SC2)."
    - "The grading run's depth/movetime budget is set from a real measurement, not a guessed number, and named as constants (no bare thresholds — project rule)."
  artifacts:
    - "frontend/src/hooks/useStockfishGradingEngine.ts with revised named budget constants (raised movetime cap and lifted/removed depth-14 cap per the measurement)."
    - "frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts updated to assert the new go-command shape."
  key_links:
    - "The go-command construction (useStockfishGradingEngine.ts:232-236) uses the new constants; the hook's test command-string assertions (:119-136) match the new shape."
---

<objective>
Establish the analysis-grade budget for the shared Stockfish grading run (`useStockfishGradingEngine`) via a real headless measurement on live positions, then apply the resulting named constants. This is SEED-087's "Open Measurement" gate: CONTEXT.md LOCKED that the depth-14 / 2500 ms cap is the proven eval-skew source and must be demonstrably lifted, with the replacement budget chosen from a measured latency/depth trade rather than guessed.

Purpose: The grading run becomes the single shared display-eval fallback (wired in plan 158-03). It only produces trustworthy evals if it searches deep enough to agree with the authoritative free run. Guessing a new depth risks either a stale-search regression at a harder-to-notice depth or an ELO-slider responsiveness regression (RESEARCH Pitfall 3).

Output: Revised named budget constants in `useStockfishGradingEngine.ts`, matching updated test assertions, and the measured depth-per-config data recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-CONTEXT.md
@.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-RESEARCH.md
@.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure depth-vs-movetime for the grading run and free run on real positions</name>
  <files>(throwaway dev harness — do NOT commit; scratchpad or a gitignored temp dir)</files>
  <read_first>
    - .planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-RESEARCH.md (the "Open Measurement: Depth/Latency Trade" section — the exact sweep to run and the three things to measure)
    - frontend/src/hooks/useStockfishGradingEngine.ts (lines 34-49 constants; 210-238 go-command construction — the exact command shape to reproduce headlessly)
    - frontend/src/hooks/useStockfishEngine.ts (lines 27, 36, 218 — the free run's `go movetime 1500 nodes 2000000` movetime-only convention, no depth clause, and MULTIPV=2)
    - Project memory `project_headless_stockfish_wasm_verification` (in MEMORY.md): copy vendored `frontend/public/engine/stockfish-18-lite-single.js` to a non-ESM dir as `.cjs`; it auto-starts a UCI CLI on stdin/stdout; illegal `searchmoves` entries are silently dropped; the `multipv` index is an eval rank — key results by `pv[0]`.
  </read_first>
  <action>
    Build a throwaway Node harness (copy `frontend/public/engine/stockfish-18-lite-single.js` to a `.cjs` file in the scratchpad dir so plain `node` execution loads it — it auto-starts a UCI CLI on stdin/stdout). Drive it with two real FENs captured from the seed's evidence or any middlegame + endgame position (a branching middlegame and a low-piece endgame — depth-per-second varies a lot by branching factor).

    Run three sweeps, keying every result by `pv[0]` (never the `multipv` index — it reorders by eval as depth climbs):
    1. Free run: `MultiPV` = 2, 3, 4 at the existing `movetime 1500` (no depth clause, mirroring `useStockfishEngine`), recording the last `info depth N` before `bestmove` per width.
    2. Grading run: candidate-union sizes 4, 6, 8 (realistic `shownSans` up to CANDIDATE_HARD_CAP=5 ∪ FC displayed moves MAX_LINES=2 ≈ 5-7), sweeping movetime 1500/2500/4000/6000 ms, recording depth reached per candidate count. Also compare removing the `depth` clause entirely (pure `movetime` cap, mirroring the free run) versus keeping a high `depth 20` safety ceiling.
    3. Agreement check: for a candidate shared with the free run (e.g. O-O), compare the grading-run eval at the chosen final budget against the free-run eval for that same move — target is the seed's "O-O +1.4 ≈ +1.3" agreement (disagreement reduced to a few cp of noise, not provably zero).

    Record the raw depth-per-config table and the CHOSEN final budget (movetime value; depth-clause removed or a high ceiling) in this plan's SUMMARY. Do NOT commit the harness `.cjs`/script (throwaway, per Phase 151.1 precedent). Choose the smallest movetime that (a) makes the grading eval agree with the free run within noise at union size ~7 AND (b) stays within the ELO-slider responsiveness budget (the existing RAPID_STEP_DEBOUNCE_MS=150 ms debounce coalesces drags, so a multi-second settled search is acceptable as long as it is debounced — the concern is search THRASH, addressed in plan 158-03, not per-search wall-clock).
  </action>
  <verify>
    <automated>MISSING — measurement task produces data, not code. Verification is Task 2's applied constants + updated test. The harness output (depth-per-config table + chosen budget + agreement-check cp delta) must be recorded in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a depth-per-config table for the grading run at candidate sizes 4/6/8 across the swept movetimes.
    - The SUMMARY states the chosen final budget as named values (movetime ms; and either "depth clause removed" or a specific high ceiling), with a one-line rationale tying it to the measured agreement-check cp delta.
    - The agreement-check cp delta between the grading run and the free run for a shared candidate at the chosen budget is recorded and is within a few centipawns (noise level).
    - No harness script is committed to the repo (verify `git status` shows no new file under `frontend/public/` or elsewhere).
  </acceptance_criteria>
  <done>Measured depth-per-config data and a chosen named grading budget (agreeing with the free run within noise) are recorded in the SUMMARY; no throwaway harness is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the measured grading budget constants and update the hook's test assertions</name>
  <files>frontend/src/hooks/useStockfishGradingEngine.ts, frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts, frontend/src/hooks/useStockfishEngine.ts</files>
  <read_first>
    - frontend/src/hooks/useStockfishGradingEngine.ts (lines 34-49 constant block; 232-236 go-command construction)
    - frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts (lines 8-9 header note; 119-136 the `setoption MultiPV value 3 + go depth 14 searchmoves <ucis> movetime 2500` assertions that bake in the old constants; 207-213 the `info depth 14` simulated lines)
    - frontend/src/hooks/useStockfishEngine.ts (lines 27, 36, 218 — free-run MOVETIME_MS=1500, MULTIPV=2, and its `go movetime … nodes …` no-depth-clause convention to mirror if dropping the grading depth cap)
    - This plan's Task 1 SUMMARY entry (the chosen budget)
  </read_first>
  <action>
    Apply the Task 1 chosen budget to `useStockfishGradingEngine.ts`. Update `GRADING_MOVETIME_SAFETY_CAP_MS` (currently 2500) to the measured value. For the depth cap: if the measurement chose a pure movetime cap, remove `GRADING_TARGET_DEPTH` (currently 14) and drop the `depth ${GRADING_TARGET_DEPTH}` clause from the go-command at line 234-236 so it mirrors `useStockfishEngine`'s `go movetime … ` convention; if the measurement chose a high safety ceiling instead, rename/retarget the constant to the chosen ceiling (e.g. a `GRADING_MAX_DEPTH` ceiling) with an updated docstring citing the measurement. Keep every constant named with a docstring — no bare numbers in the go-command.

    Update the docstring on the changed constant(s) to reference SEED-087 / Phase 158 and state that the value came from the headless measurement (replacing the old "conservative start per RESEARCH Open Question 1" note on GRADING_TARGET_DEPTH).

    Update `useStockfishGradingEngine.test.ts`: the header note (lines 8-9) and the command-string assertions (lines 133-134 `expect(goCmd).toContain('depth 14')` / `toContain('movetime 2500')`) and the simulated `info depth 14 …` lines (207-213) to match the new command shape and budget. If the depth clause is removed, assert the go-command does NOT contain a `depth ` clause and DOES contain the new `movetime` value; keep the `searchmoves e7e5 c7c5 e7e6` and `setoption name MultiPV value 3` assertions intact (candidate-count-driven MultiPV is unchanged by this task).

    Free-run MULTIPV (useStockfishEngine.ts:36): per RESEARCH Open Question 2, only change MULTIPV from 2 if Task 1's free-run sweep showed raising it meaningfully improves union coverage without an unacceptable depth cost. The default expectation is to LEAVE MULTIPV=2 unchanged (the SF card shows top-2; the grading run covers the rest of the union). If left unchanged, make no edit to useStockfishEngine.ts and note the decision in the SUMMARY. If changed, update its docstring to cite the measurement.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useStockfishGradingEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GRADING_MOVETIME_SAFETY_CAP_MS" frontend/src/hooks/useStockfishGradingEngine.ts` shows the measured value, not 2500.
    - The go-command no longer contains the literal `depth 14` (either no depth clause, or a measured higher ceiling): `grep -c "depth 14" frontend/src/hooks/useStockfishGradingEngine.ts` returns 0.
    - No bare numeric literal appears in the go-command postMessage string (all budget values are named constants).
    - `cd frontend && npm test -- --run src/hooks/__tests__/useStockfishGradingEngine.test.ts` passes.
    - `cd frontend && npx tsc -b` reports zero errors (shared-type safety, per CLAUDE.md).
    - If MULTIPV was left at 2, `git diff --stat frontend/src/hooks/useStockfishEngine.ts` shows no change and the SUMMARY records the "leave MULTIPV=2" decision.
  </acceptance_criteria>
  <done>The grading run's budget constants reflect the measurement, the depth-14 cap is demonstrably lifted, the hook test asserts the new command shape and passes, and `tsc -b` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan tunes client-side engine search constants only. No new user input, network call, endpoint, storage, or auth surface. All data is client-computed engine output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-01 | Denial of Service | Grading Web Worker (raised movetime budget) | low | accept | Budget is chosen against the ELO-slider responsiveness constraint (Task 1); the existing RAPID_STEP_DEBOUNCE_MS=150 ms debounce coalesces rapid inputs; search thrash is addressed structurally in plan 158-03. A single settled multi-second grading search is the established, accepted behavior for this in-browser worker. |

No package installs in this plan (T-{phase}-SC not applicable — verified no new npm dependency).
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/hooks/__tests__/useStockfishGradingEngine.test.ts` green.
- `cd frontend && npx tsc -b` clean.
- SUMMARY records the measurement table, the chosen named budget, the agreement-check cp delta, and the MULTIPV decision.
</verification>

<success_criteria>
The shared grading run's budget is measured (not guessed), the depth-14 cap is lifted, and the resulting eval agrees with the free run within a few centipawns for a shared candidate on both a middlegame and an endgame position (SEED-087 SC2 budget component).
</success_criteria>

## Artifacts this phase produces (plan 158-01)

New symbols / changes introduced by this plan (excluded from drift verification downstream):
- Revised named constant `GRADING_MOVETIME_SAFETY_CAP_MS` (new measured value) in `useStockfishGradingEngine.ts`.
- Either removal of `GRADING_TARGET_DEPTH` (pure movetime cap) or a renamed depth-ceiling constant (e.g. `GRADING_MAX_DEPTH`) in `useStockfishGradingEngine.ts` — the executor names it per the measurement.
- Updated command-string assertions in `useStockfishGradingEngine.test.ts`.

<output>
Create `.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-01-SUMMARY.md` when done. Record: the depth-per-config measurement table, the chosen named budget with rationale, the agreement-check cp delta, and the free-run MULTIPV decision.
</output>
