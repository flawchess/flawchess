---
phase: 158-flawchess-engine-displayed-eval-provenance-reconciliation-se
plan: 03
type: execute
wave: 2
depends_on: ["158-02"]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/components/analysis/FlawChessEngineLines.tsx
  - frontend/src/pages/__tests__/Analysis.test.tsx
autonomous: true
requirements: [SEED-087]
must_haves:
  truths:
    - "A move displayed on two or more of the SF card, FC card, and Maia chart renders the SAME eval on all of them, resolved through the single evalLookup (SEED-087 SC1)."
    - "The shared grading run is gated on `maiaEnabled || flawChessEnabled` and its candidate set is the deduplicated `shownSans ∪ displayed FC moves` union (SEED-087 SC2)."
    - "The Maia move-quality buckets (classifyMoveQuality) classify from the reconciled evals they display, so a move's number and its severity color agree at bucket boundaries — this covers the Maia chart line/SAN-label colors, the quality-bar segments, and positionVerdict (SEED-087 SC3, CONTEXT.md user note 2026-07-07)."
    - "The agreement verdict's FC-pick eval and SF-best eval both come from the lookup, so 'FC pick grades higher than the objective best' (the Qc7 +2.8 vs O-O +1.3 case) is impossible by construction (SEED-087 SC4)."
    - "Practical scores (brown badges / practicalScore) and the MCTS search core are untouched — RankedLine is not mutated or extended; only new parallel display objects carry the reconciled objectiveEvalCp (SEED-087 SC5)."
  artifacts:
    - "Analysis.tsx `evalLookup` memo (buildEvalLookup over engine.pvLines + grading.gradeMap + position)."
    - "Analysis.tsx `reconciledRankedLines` memo (parallel RankedLine-shaped objects, only objectiveEvalCp swapped)."
    - "Analysis.tsx reconciled `qualityBySan` (classifyMoveQuality fed the reconciled grade map)."
    - "FlawChessEngineLines.tsx exported MAX_LINES constant."
  key_links:
    - "reconciledRankedLines is threaded to FlawChessEngineLines rankedLines, flawChessVerdict's flawChessLine, and FlawChessAgreementVerdict's flawChessRankedLines; stockfishLine stays engine.pvLines[0]."
    - "The candidate union is a stable sorted deduplicated array so the grading hook's candidatesKey debounce absorbs no-op FC snapshot ticks (RESEARCH Pitfall 4)."
    - "gradingEnabled pairs fen and enabled on the same OR'd condition so the worker is never alive-but-positionless (RESEARCH Pitfall 5)."
---

<objective>
Wire the plan-158-02 lookup into `Analysis.tsx` so every displayed Stockfish eval on the three `/analysis` surfaces resolves through one UCI-keyed source with free-run-first precedence. This is where SEED-087's four observable success criteria (SC1-SC4) become true, and where SC5 (scope fence) is honored by building parallel display objects instead of touching the frozen `RankedLine` type or the MCTS search core.

Purpose: `Analysis.tsx` is the only place all three engine hooks' outputs are simultaneously in scope (RESEARCH Architectural Responsibility Map), so reconciliation lives here (or in the pure lib it calls), keeping every downstream component's signature and existing unit tests unchanged.

Output: the `evalLookup` / `reconciledRankedLines` memos, a reconciled `qualityBySan`, the promoted grading run (union candidate set + OR gating), reconciled values threaded to the FC card and the agreement verdict, and new `Analysis.test.tsx` assertions for gating + reconciled wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-CONTEXT.md
@.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-RESEARCH.md
@.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-PATTERNS.md
@frontend/src/lib/engineEvalLookup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the grading run — OR gating + FC∪Maia candidate union (export MAX_LINES)</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/components/analysis/FlawChessEngineLines.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lines 660-740 — the bestSan/engineTopLines/shownSans/grading/qualityBySan memo chain; specifically the grading call at 718-722 pairing fen/enabled on `maiaEnabled`, and shownSans at 707-710)
    - frontend/src/hooks/useStockfishGradingEngine.ts (lines 56-70 options interface; 178 the `candidatesKey` stable-primitive-dependency memo; 286-384 the `enabled`-keyed Worker lifecycle effect — why OR gating must not tear down the worker when only one consumer toggles)
    - frontend/src/components/analysis/FlawChessEngineLines.tsx (line 45 `const MAX_LINES = 2` — currently local/unexported; line 252 `rankedLines.slice(0, MAX_LINES)`)
    - 158-RESEARCH.md (Pitfall 4 — stable sorted deduped union absorbs FC snapshot ticks; Pitfall 5 — pair fen/enabled on the OR'd condition; Open Question 3 — export MAX_LINES as the single source of the FC displayed-count)
  </read_first>
  <action>
    In `FlawChessEngineLines.tsx`, export the `MAX_LINES` constant (currently `const MAX_LINES = 2` at line 45) so `Analysis.tsx` sizes the FC-displayed slice from the single source of truth (RESEARCH Open Question 3). Keep the existing local use unchanged.

    In `Analysis.tsx`:
    - Import `MAX_LINES` from `@/components/analysis/FlawChessEngineLines` (name it clearly at the call site if a rename avoids collision, e.g. `FC_MAX_LINES`).
    - Add a `gradingEnabled` boolean: `const gradingEnabled = maiaEnabled || flawChessEnabled;` (CONTEXT.md LOCKED gating — the grading run must run when EITHER display consumer is active).
    - Build the FC displayed-moves SAN list from the top `MAX_LINES` ranked lines: map `flawChessEngine.rankedLines.slice(0, MAX_LINES)` through `bestSanFromPv(position, line.rootMove)` (the same SAN conversion already used by `bestSan`/`engineTopLines`), filtering nulls.
    - Build a stable union memo `unionSans`: the deduplicated, sorted array of `[...shownSans, ...flawChessDisplayedSans]` (mirror the hook's own `candidatesKey` sort-join pattern so a re-throttle of the SAME top-2 FC moves produces the same array and does NOT re-trigger the grading search — RESEARCH Pitfall 4). Gate the FC contribution on `flawChessEnabled` and the Maia contribution on `maiaEnabled` so the union reflects only active consumers (CONTEXT.md: "the union reflects which consumers are active").
    - Rewrite the `grading` call (718-722) to pair fen and enabled on the SAME `gradingEnabled` condition and pass the union: `fen: gradingEnabled ? position : null`, `candidateSans: unionSans`, `enabled: gradingEnabled` (RESEARCH Pitfall 5 — never let fen/enabled desync). Update the existing comment (lines 712-717) to state the new OR gating and union set, replacing the "gated on the Maia switch only" note.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const MAX_LINES" frontend/src/components/analysis/FlawChessEngineLines.tsx` shows MAX_LINES is exported.
    - `grep -n "maiaEnabled || flawChessEnabled" frontend/src/pages/Analysis.tsx` shows the OR'd gating expression.
    - The grading call passes the same `gradingEnabled` value to both `fen` (conditionally) and `enabled` (no separate `maiaEnabled`-only condition remains on the grading `fen`/`enabled` pair).
    - The union candidate array is sorted+deduplicated (a `.sort()` and a dedup via `Set` or equivalent appear in the union construction).
    - `cd frontend && npx tsc -b` reports zero errors.
    - Existing Analysis.test.tsx passes (Task 3 adds the new gating assertions).
  </acceptance_criteria>
  <done>The grading run runs when either Maia or FlawChess is on, over the deduplicated sorted FC∪Maia SAN union, with fen/enabled paired on the OR'd condition and MAX_LINES exported.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve every displayed eval through the lookup — evalLookup, reconciledRankedLines, reconciled qualityBySan, engineTopLines</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lines 683-701 engineTopLines memo; 724-740 qualityBySan memo; the grading/shownSans memos from Task 1)
    - frontend/src/lib/engineEvalLookup.ts (the plan-158-02 module: `buildEvalLookup(pvLines, gradeMapBySan, baseFen)`, `getByUci(lookup, uci)`, `getBySan(lookup, baseFen, san)`)
    - frontend/src/lib/moveQuality.ts (classifyMoveQuality signature `(gradeMap, mover, bestSan)` and the MoveGrade / MoveQualityEval shapes; lines 33-37)
    - frontend/src/lib/engine/types.ts (the frozen RankedLine type — Phase 153 freeze; objectiveEvalCp is `number | null`, no mate field — RESEARCH Pitfall 6: do NOT extend or mutate it)
    - frontend/src/components/analysis/FlawChessEngineLines.tsx (line 124 `formatScore(line.objectiveEvalCp, null)` — the FC card shows cp only, never mate, so reconciled objectiveEvalCp must remain cp-only)
    - 158-RESEARCH.md (Pattern 2 — parallel RankedLine objects; Pattern 3 / Progressive Refinement — unresolved entries render `formatScore(null,null) → '…'`, never a pool fallback)
  </read_first>
  <action>
    Import `buildEvalLookup`, `getByUci`, `getBySan` from `@/lib/engineEvalLookup`.

    Add an `evalLookup` memo AFTER the `grading` call: `buildEvalLookup(engine.pvLines, grading.gradeMap, position)`, deps `[engine.pvLines, grading.gradeMap, position]`. Free-run entries win by construction (module precedence), so this is the single source every display site resolves through. Progressive refinement is emergent: once the free run reports a UCI its value cannot regress to the grading value (RESEARCH Pattern 3) — no extra state machine.

    Add a `reconciledRankedLines` memo: map `flawChessEngine.rankedLines.slice(0, MAX_LINES)` to NEW objects spreading each line and swapping only `objectiveEvalCp` for `getByUci(evalLookup, line.rootMove)?.evalCp ?? null`. Never mutate the original objects (they are live MCTS-core snapshots; RankedLine is frozen). Because objectiveEvalCp is cp-only, a resolved mate grade (evalCp null / evalMate set) yields `null` → the card renders the existing `'…'` placeholder, matching the pre-existing FC-source cp-only limitation — add a one-line comment noting this so it is not mistaken for a bug.

    Rewrite `qualityBySan` (724-740) so classification AND the merged evals come from the reconciled lookup, not `grading.gradeMap` directly (CONTEXT.md LOCKED SC3 — number and severity color must agree at bucket boundaries, covering the Maia chart line/SAN-label colors, the quality-bar segments, and positionVerdict): build a reconciled SAN-keyed grade map by resolving each `shownSan` (or each key of `grading.gradeMap`) via `getBySan(evalLookup, position, san)`; pass THAT reconciled map to `classifyMoveQuality(reconciledGradeMap, sideToMoveFromFen(position), bestSan)`; and take each merged entry's `evalCp`/`evalMate` from the SAME reconciled map (so displayed number = classification input). An unresolved SAN maps to `{ evalCp: null, evalMate: null }` (never the pool grade). Update the memo deps to `[evalLookup, position, bestSan]` (or the reconciled map's inputs) accordingly.

    Reconcile the `engineTopLines` FC-fallback branch (693-699), which currently reads raw `line.objectiveEvalCp`. Since it must be sourced from reconciled values but `evalLookup`/`reconciledRankedLines` are declared later in the memo chain, RELOCATE the `engineTopLines` memo to AFTER `reconciledRankedLines`, and change its FC branch to map over `reconciledRankedLines` (its objectiveEvalCp is already reconciled) instead of `flawChessEngine.rankedLines.slice(0,2)`. The Stockfish branch (685-692, reading `engine.pvLines`) is already free-run-sourced and stays unchanged. Verify nothing between the old and new location depends on `engineTopLines` (only the JSX consumes it).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx src/lib/__tests__/moveQuality.test.ts && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "buildEvalLookup(engine.pvLines, grading.gradeMap, position)" frontend/src/pages/Analysis.tsx` shows the evalLookup memo.
    - `reconciledRankedLines` spreads each source line and overrides only `objectiveEvalCp` (the original RankedLine objects are not mutated; no field is ADDED to RankedLine — `grep -c "reconciledEvalCp" frontend/src/lib/engine/types.ts` returns 0).
    - `qualityBySan` no longer passes `grading.gradeMap` directly to `classifyMoveQuality`; it passes a reconciled map derived via `getBySan` (grep confirms `getBySan` is used in the qualityBySan construction).
    - The engineTopLines FC branch maps over `reconciledRankedLines`, not `flawChessEngine.rankedLines`.
    - `moveQuality.test.ts` still passes with ZERO edits (signature unchanged — a design-correctness signal per RESEARCH: if it needed edits, reconciliation leaked out of Analysis.tsx).
    - `cd frontend && npx tsc -b` reports zero errors.
  </acceptance_criteria>
  <done>Every Maia-surface and FC-card eval resolves through evalLookup; qualityBySan classifies from reconciled evals; engineTopLines' FC branch is reconciled; RankedLine is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Thread reconciled values into the FC card + agreement verdict, and add gating/wiring test coverage</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/pages/__tests__/Analysis.test.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lines 1510-1525 — the FlawChessEngineLines `rankedLines={flawChessEngine.rankedLines}` prop, the FlawChessAgreementVerdict `flawChessLine`/`stockfishLine`/`flawChessRankedLines` props; also the mobile render sites — search for every `flawChessEngine.rankedLines` passed to a display/verdict consumer, and the desktop/mobile duplication per CLAUDE.md "apply changes to mobile too")
    - frontend/src/components/analysis/FlawChessAgreementVerdict.tsx (lines 97-129, 206-209 — the two popover bodies and matchedLine `.find(...)` that read objectiveEvalCp; confirms passing reconciled flawChessRankedLines fixes all verdict read sites without touching this file)
    - frontend/src/lib/flawChessVerdict.ts (line 94 — flawChessMove.evalCp = flawChessLine.objectiveEvalCp; signature accepts RankedLine | null, unchanged)
    - frontend/src/pages/__tests__/Analysis.test.tsx (lines 42-99 — the hook mocks, especially the static useStockfishGradingEngine mock at 49-55 and flawChessState at 81-89; the existing wiring/gating assertions style below line 163)
    - 158-RESEARCH.md (Phase Requirements → Test Map — SC2 gating across 4 toggle combos, SC4 verdict comparability at the Analysis.tsx wiring level)
  </read_first>
  <action>
    In `Analysis.tsx`, replace `flawChessEngine.rankedLines` with `reconciledRankedLines` at every DISPLAY/VERDICT consumer (NOT the practical-eval-bar `topLine` at ~1237, which reads `practicalScore` and is scope-fenced): the `FlawChessEngineLines` `rankedLines` prop (1511), the `FlawChessAgreementVerdict` `flawChessLine` (1523, `reconciledRankedLines[0] ?? null`) and `flawChessRankedLines` (1525) props — and the matching MOBILE render sites if separate. Keep `stockfishLine={engine.pvLines[0] ?? null}` (1524) unchanged — the SF side is already free-run-sourced (existing D-01 convention), and both sides now resolve through the same evalLookup, making the verdict's FC-pick vs SF-best comparison mutually comparable by construction (SC4). Do NOT touch the blue SF arrows (1102) or engineArrows — those key by rootMove for square highlighting, not eval display.

    In `Analysis.test.tsx`:
    - Upgrade the `useStockfishGradingEngine` mock (49-55) to a `vi.fn()` that captures its call arguments so the test can assert `enabled` and `candidateSans` across the four `(maiaEnabled, flawChessEnabled)` toggle combinations. Assert the grading hook receives `enabled: true` whenever EITHER switch is on and `enabled: false` only when BOTH are off (SC2 gating).
    - Add an assertion that when both a free-run pvLine and a grading grade exist for the same move, the value rendered on the FC card / fed to qualityBySan matches the free-run value (SC1 precedence at the wiring level). Use the existing mutable `engineState` / `flawChessState` / grading-mock stubs to set up a shared move present in both sources.
    - Add a regression assertion for SC4: with `flawChessState.rankedLines[0]` set to a move whose reconciled objectiveEvalCp resolves from the lookup, the verdict's FC-pick eval cannot exceed the SF-best eval when both are lookup-sourced (assert the two inputs passed to the verdict are drawn from reconciledRankedLines[0] and engine.pvLines[0], both resolving through evalLookup).
    - Keep flawChessVerdict.test.ts, FlawChessAgreementVerdict.test.tsx, moveQuality.test.ts, MaiaMoveQualityBar.test.tsx UNEDITED (their signatures are unchanged — editing them signals a leak).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "flawChessEngine.rankedLines" frontend/src/pages/Analysis.tsx` shows NO remaining occurrence feeding a display/verdict consumer (FlawChessEngineLines rankedLines, FlawChessAgreementVerdict flawChessLine/flawChessRankedLines) — those now pass `reconciledRankedLines`; remaining occurrences are only bestSan/engineArrows/topLine(practicalScore) which are out of the display-eval scope.
    - `stockfishLine={engine.pvLines[0] ?? null}` is unchanged.
    - Analysis.test.tsx asserts grading `enabled` is true when maiaEnabled XOR/OR flawChessEnabled and false only when both off (4 combinations).
    - Analysis.test.tsx has a new SC1 precedence assertion and a new SC4 verdict-comparability assertion.
    - `git diff --stat frontend/src/lib/flawChessVerdict.test.ts frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx frontend/src/lib/__tests__/moveQuality.test.ts frontend/src/components/analysis/__tests__/MaiaMoveQualityBar.test.tsx` shows NO changes (unedited).
    - `cd frontend && npm run lint && npm test -- --run && npx tsc -b` all pass.
    - `cd frontend && npm run knip` reports no dead exports (buildEvalLookup/getByUci/getBySan/MAX_LINES are all now consumed).
  </acceptance_criteria>
  <done>The FC card and agreement verdict consume reconciled evals; gating + precedence + verdict-comparability are covered by new Analysis.test.tsx assertions; the untouched-test invariant holds; full frontend suite + lint + tsc are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-only reconciliation of already-client-computed evals. No new user input, network call, endpoint, storage, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-03 | Tampering | engine-derived eval/move strings rendered in FC card + verdict popovers | low | accept | Every render site is a React child (auto-escaped); all data is client-computed engine output, not user/server-supplied — identical to the T-137-03/T-155-04 mitigated convention already documented in FlawChessEngineLines.tsx/EngineLines.tsx. |

No package installs in this plan (T-{phase}-SC not applicable — verified no new npm dependency).
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` all green (full suite, per VALIDATION.md wave-merge command).
- Manual UAT (phase gate, manual-only per VALIDATION.md): on `/analysis`, a move present on all three cards (exd5/Bc5 class) shows one identical number everywhere, including Maia chart line colors matching the displayed evals; the Qc7-class verdict impossibility can no longer occur; evals settle monotonically (`… → value`) without flapping between grading and free-run values.
</verification>

<success_criteria>
Every displayed Stockfish eval on the SF card, FC card, Maia chart/quality bar, and agreement verdict resolves through the single evalLookup with free-run-first precedence; quality buckets classify from reconciled evals; the verdict's FC-pick cannot exceed the SF-best; practical scores and the MCTS core are untouched (SEED-087 SC1-SC5).
</success_criteria>

## Artifacts this phase produces (plan 158-03)

New symbols introduced by this plan (excluded from drift verification downstream):
- Exported `MAX_LINES` constant in `frontend/src/components/analysis/FlawChessEngineLines.tsx`.
- `Analysis.tsx` local memos: `evalLookup`, `reconciledRankedLines`, `unionSans` (and the FC-displayed-SAN list), plus a reworked `qualityBySan` and relocated `engineTopLines`.
- New `Analysis.test.tsx` assertions for OR gating, SC1 precedence, and SC4 verdict comparability.

Consumes (from plan 158-02, not new drift): `buildEvalLookup`, `getByUci`, `getBySan` from `@/lib/engineEvalLookup`.

<output>
Create `.planning/phases/158-flawchess-engine-displayed-eval-provenance-reconciliation-se/158-03-SUMMARY.md` when done.
</output>
