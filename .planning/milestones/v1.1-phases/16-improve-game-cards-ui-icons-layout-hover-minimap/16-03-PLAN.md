---
phase: 16-improve-game-cards-ui-icons-layout-hover-minimap
plan: 03
type: execute
wave: 1
depends_on: ["16-01", "16-02"]
files_modified:
  - alembic/versions/
autonomous: true
requirements: [GCUI-01]
gap_closure: true

must_haves:
  truths:
    - "Alembic migration file exists that adds result_fen column to games table"
    - "Running alembic upgrade head succeeds without error"
    - "Full test suite passes after migration"
  artifacts:
    - path: "alembic/versions/*_add_result_fen_to_games.py"
      provides: "Migration adding result_fen nullable VARCHAR(100) column to games table"
      contains: "op.add_column"
  key_links:
    - from: "alembic/versions/*_add_result_fen_to_games.py"
      to: "app/models/game.py"
      via: "autogenerated from SQLAlchemy model diff"
      pattern: "result_fen.*String.*100"
---

<objective>
Generate the missing Alembic migration for the result_fen column on the games table.

Purpose: The VERIFICATION report found that result_fen was added to the SQLAlchemy model (app/models/game.py) and the import pipeline stores it, but no Alembic migration was generated. Without the migration, any environment running `alembic upgrade head` will lack the column, causing `OperationalError: column games.result_fen does not exist` at runtime.

Output: One Alembic migration file in alembic/versions/
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-improve-game-cards-ui-icons-layout-hover-minimap/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Alembic migration for result_fen column and verify</name>
  <files>alembic/versions/</files>
  <read_first>
    - app/models/game.py (verify result_fen column definition: Mapped[str | None] = mapped_column(String(100), nullable=True))
    - alembic/versions/ (list existing migrations to confirm no result_fen migration exists yet)
    - alembic/env.py (confirm autogenerate configuration)
  </read_first>
  <action>
1. Run: `uv run alembic revision --autogenerate -m "add_result_fen_to_games"`
2. Read the generated migration file and verify it contains:
   - `op.add_column('games', sa.Column('result_fen', sa.String(length=100), nullable=True))` in upgrade()
   - `op.drop_column('games', 'result_fen')` in downgrade()
3. Run: `uv run alembic upgrade head` to apply the migration
4. Run: `uv run pytest -x` to confirm the full test suite still passes

If autogenerate produces an empty migration (no changes detected), it means the DB is already in sync with the model from development. In that case, still commit the migration file so that fresh environments get the column via `alembic upgrade head`.
  </action>
  <verify>
    <automated>uv run alembic heads && uv run pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - A new file exists in alembic/versions/ with "add_result_fen_to_games" in its filename
    - The migration file contains the string "result_fen"
    - The migration file contains "sa.String(length=100)"
    - The migration file contains "nullable=True"
    - `uv run alembic heads` shows the new migration as head
    - `uv run pytest -x` exits 0
  </acceptance_criteria>
  <done>Alembic migration for result_fen column exists, is applied, and all tests pass</done>
</task>

</tasks>

<verification>
- `ls alembic/versions/*result_fen*` returns exactly one file
- `grep -l "result_fen" alembic/versions/*.py` returns the migration file
- `uv run alembic current` shows the migration has been applied
- `uv run pytest` exits 0
</verification>

<success_criteria>
- Alembic migration file for result_fen exists and is committed
- Migration applies cleanly with `alembic upgrade head`
- Full test suite passes
- GCUI-01 requirement fully satisfied (model + migration + import pipeline)
</success_criteria>

<output>
After completion, create `.planning/phases/16-improve-game-cards-ui-icons-layout-hover-minimap/16-03-SUMMARY.md`
</output>
