---
phase: 99-percentile-badges-for-conversion-parity-and-recovery
plan: 04
type: execute
wave: 2
depends_on: ["99-02"]
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameMetricsByTcCard.tsx
autonomous: true
requirements: []   # standalone — coverage driven by SC-1, SC-4
must_haves:
  truths:
    - "PerTcBucketStats (TS) carries optional rate_percentile / rate_percentile_n_games / rate_percentile_value, separate from percentile (D-01)"
    - "Each per-TC card renders a title-line, right-aligned rate PercentileChip on Conversion/Parity/Recovery when rate_percentile != null AND anchorRating present; gap chips stay on the ΔES bullet (SC-1/D-02)"
    - "Below floor (rate_percentile == null) or no anchor → no rate chip renders (SC-1)"
    - "The rate chip reuses the 4-bullet disclosure contract verbatim with the first two bullets TC-scoped; only bullet 1's metric noun changes to the raw-rate noun (Conversion/Parity/Recovery Rate) via metricLabel (SC-4/D-07/D-08)"
    - "The existing ΔES-gap chip is unchanged and coexists with the new rate chip (D-01)"
    - "One shared MetricBlock renderer covers desktop + mobile — no duplicated markup"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "rate_percentile field trio on PerTcBucketStats interface"
      contains: "rate_percentile"
    - path: "frontend/src/components/charts/EndgameMetricsByTcCard.tsx"
      provides: "title-line rate chip in MetricBlock"
      contains: "rate-percentile-chip"
  key_links:
    - from: "frontend/src/components/charts/EndgameMetricsByTcCard.tsx"
      to: "frontend/src/components/charts/PercentileChip.tsx"
      via: "second PercentileChip instance with flavor + metricLabel + tc + anchorRating"
      pattern: "PercentileChip"
    - from: "EndgameMetricsByTcCard.tsx MetricBlock h4"
      to: "block.rate_percentile"
      via: "null-and-anchor gate on the title-line chip"
      pattern: "block.rate_percentile != null"
---

<objective>
Wave 2B frontend wiring. Add the raw-rate percentile field to the TypeScript PerTcBucketStats type and render a second `PercentileChip` on each metric block's title line (right-aligned), reusing the existing `PercentileChip` primitive with the bucket-appropriate flavor and a rate-specific `metricLabel`. The existing ΔES-gap chip stays exactly as-is (D-01: both coexist).

This depends only on the Wave-1 contracts (the field names are locked in 99-02/99-03 — but the TS type is independent of the backend Python schema, so this plan can run in parallel with Plan 03; the only shared knowledge is the agreed field names `rate_percentile` / `rate_percentile_n_games` / `rate_percentile_value`). Zero file overlap with Plan 03.

Differentiation between the two chips is tooltip-only (D-03): same flavor, different `metricLabel` — no inline qualifier label, no new visual primitive. The metric noun in tooltip bullet 1 is the sole distinguisher (D-08).

Purpose: surface the rate percentile on the per-TC cards (desktop + mobile via the single shared MetricBlock).
Output: TS type trio + title-line chip in MetricBlock + the `w-full` h4 fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-RESEARCH.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PerTcBucketStats (TS) with the rate field trio</name>
  <read_first>
    - frontend/src/types/endgames.ts lines 422-439 (PerTcBucketStats interface — the existing percentile / percentile_n_games / percentile_value fields + their comment style)
    - 99-RESEARCH.md "Pattern 5" frontend TypeScript snippet
    - 99-PATTERNS.md "frontend/src/types/endgames.ts" section
  </read_first>
  <action>
    Append three optional fields to the PerTcBucketStats interface immediately after percentile_value: rate_percentile?: number | null; rate_percentile_n_games?: number | null; rate_percentile_value?: number | null;. Add a one-line comment marking them as the raw-rate percentile, SEPARATE from the ΔES-gap percentile (D-01). Keep them optional (?) to match the backend Pydantic None defaults and to avoid breaking existing fixtures. Respect noUncheckedIndexedAccess (these are direct object fields, not index access — no narrowing needed at the type site, but consumers in Task 2 must narrow via the != null gate).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "rate_percentile" frontend/src/types/endgames.ts returns >= 3.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - The three fields are optional (`?:`) and typed `number | null`.
  </acceptance_criteria>
  <done>PerTcBucketStats (TS) has the optional rate field trio, distinct from percentile*, and tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Title-line rate chip in MetricBlock (desktop + mobile)</name>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsByTcCard.tsx FULL FILE — focus: line 150 (h4 class lacking w-full — Pitfall 6), lines 86-100 (MetricBlockProps — anchorRating already a prop), lines 216-234 (existing ΔES-gap chip wiring — the null-gate + prop-threading pattern to mirror), lines 403-439 (single MetricBlock call per bucket confirming shared desktop+mobile renderer)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx lines 182-203 (ClockGapHeaderRow — the title-line right-aligned ml-auto chip placement precedent to mirror)
    - frontend/src/components/charts/PercentileChip.tsx lines 88-96 + 215-307 (PercentileChipFlavor union — DO NOT extend; the existing conversion/parity/recovery flavors are reused; metricLabel weaves into popover bullet 1)
    - frontend/src/lib/theme.ts (confirm no new color constant is needed — PercentileChip owns its color band)
    - frontend/src/components/charts/EndgameMetricsByTcCard.test.tsx (the Wave-0 test — render/suppress/tooltip assertions must turn green)
    - 99-RESEARCH.md "Pattern 6" + Pitfall 6 + Pitfall 8 (do NOT add a new flavor)
    - 99-PATTERNS.md "EndgameMetricsByTcCard.tsx" section (the 3 exact changes)
  </read_first>
  <action>
    Make three changes to MetricBlock in EndgameMetricsByTcCard.tsx (per 99-PATTERNS.md):
      1. Add w-full to the h4 className at line 150 (the h4 is inline-flex; without w-full the ml-auto chip will not push right — Pitfall 6).
      2. Inside the h4, after the closing InfoPopover, add a right-aligned (`ml-auto inline-flex` span) rate chip gated on BOTH block.rate_percentile != null AND anchorRating != null (mirror the existing gap-chip gate exactly; null = below floor → silent suppression, SC-1; anchor required so the tooltip can disclose the cohort honestly).
      3. Render PercentileChip with: percentile={block.rate_percentile}, flavor derived from bucket (conversion→'conversion', parity→'parity', recovery→'recovery' — REUSE existing flavors, do NOT add a new one, Pitfall 8), tc={tc}, anchorRating={anchorRating}, metricLabel derived from bucket ('Conversion Rate' / 'Parity Rate' / 'Recovery Rate' — this is the SOLE differentiator from the gap chip per D-03/D-08), testId={`${testId}-rate-percentile-chip`}, nGames={block.rate_percentile_n_games}, value={block.rate_percentile_value}.
    Do NOT touch the existing ΔES-gap chip wiring (lines 216-234) — it stays exactly as-is, both chips coexist (D-01). Do NOT add an inline "rate"/"gap" qualifier label (D-03 — deferred). Because MetricBlock is the single shared renderer (lines 403-439 show one call per bucket, no mobile-specific branch), this one change covers desktop AND mobile — DO NOT duplicate markup; assert the single-renderer fact in the SUMMARY. The testId on the chip satisfies the CLAUDE.md data-testid-on-interactive-elements rule; PercentileChip already carries its own aria-label.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run EndgameMetricsByTcCard --reporter=dot && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - The Wave-0 frontend tests pass: `cd frontend && npx vitest run EndgameMetricsByTcCard` exits 0 (render-when-present, suppress-on-null-percentile, suppress-on-null-anchor, TC-scoped tooltip + rate noun, coexistence with gap chip).
    - grep -c "rate-percentile-chip" frontend/src/components/charts/EndgameMetricsByTcCard.tsx returns >= 1.
    - The h4 at line ~150 contains "w-full".
    - grep -c "Conversion Rate\|Parity Rate\|Recovery Rate" EndgameMetricsByTcCard.tsx returns >= 3 (the metricLabel mapping).
    - PercentileChipFlavor is NOT modified: `git diff --stat frontend/src/components/charts/PercentileChip.tsx` shows no change to that file.
    - There is exactly ONE MetricBlock component definition and the rate chip lives inside it (no duplicated mobile markup): grep -c "function MetricBlock\|const MetricBlock" returns 1.
    - `cd frontend && npm run lint && npm run knip && npx tsc --noEmit` all exit 0 (no dead exports, no text-xs violation outside the allowed tooltip body, noUncheckedIndexedAccess satisfied).
  </acceptance_criteria>
  <done>The shared MetricBlock renders a title-line, right-aligned rate chip gated on percentile+anchor, reusing PercentileChip with the rate metricLabel; the gap chip is untouched; Wave-0 frontend tests green; lint/knip/tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | The rate_percentile payload field is rendered client-side; it is per-user data already scoped by the authenticated API call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-XSS | Tampering | rate chip render | accept | Values are numbers/enums (percentile, tc, metricLabel from a closed set); React escapes by default; no dangerouslySetInnerHTML. Low risk. |
| T-99-IDOR | Information Disclosure | client render of percentile | mitigate | Data is scoped server-side by fetch_for_user (Plan 03); the client renders only what the user's authenticated request returned. |
| T-99-SC | Tampering | npm installs | mitigate | No new npm packages. No install task → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npx vitest run EndgameMetricsByTcCard` green (render + suppression + tooltip + coexistence).
- `cd frontend && npm run lint && npm run knip && npx tsc --noEmit` all clean.
- Single shared MetricBlock confirmed (no duplicated mobile markup).
</verification>

<success_criteria>
SC-1 (chip suppresses below floor / no-anchor) and SC-4 (TC-scoped 4-bullet tooltip naming the raw-rate noun) are satisfied on the rendered title-line chip; D-01 (both chips coexist) and D-03 (tooltip-only differentiation) honored. Desktop + mobile parity via the single MetricBlock renderer.
</success_criteria>

<output>
Create `.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-04-SUMMARY.md` — confirm the single-renderer (desktop+mobile) fact, the metricLabel mapping, and that PercentileChip + the gap chip were not modified.
</output>
