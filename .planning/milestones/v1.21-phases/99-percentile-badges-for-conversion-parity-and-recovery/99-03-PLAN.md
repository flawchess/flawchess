---
phase: 99-percentile-badges-for-conversion-parity-and-recovery
plan: 03
type: execute
wave: 2
depends_on: ["99-02"]
files_modified:
  - app/services/user_benchmark_percentiles_service.py
  - scripts/gen_global_percentile_cdf.py
  - app/schemas/endgames.py
  - app/services/endgame_service.py
autonomous: true
requirements: []   # standalone — coverage driven by SC-2
must_haves:
  truths:
    - "STAGE_B_METRIC_FAMILIES includes the 3 rate families; the per-user dispatch routes them to the new builders (SC-2)"
    - "IN_SCOPE_METRICS + the regen dispatch route the 3 rate families to the same builders (drift-proof, SC-2)"
    - "PerTcBucketStats carries rate_percentile / rate_percentile_n_games / rate_percentile_value, SEPARATE from percentile (D-01)"
    - "endgame_service looks up conversion_rate/parity_rate/recovery_rate rows per TC and threads them onto the per-TC blocks (SC-2)"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "rate_percentile field trio on PerTcBucketStats"
      contains: "rate_percentile"
    - path: "app/services/endgame_service.py"
      provides: "rate-row lookup + _build_per_tc_bucket_stats rate param"
      contains: "conversion_rate"
    - path: "app/services/user_benchmark_percentiles_service.py"
      provides: "Stage B dispatch arms for the 3 rate families"
      contains: "per_user_cte_conv_rate_tc"
    - path: "scripts/gen_global_percentile_cdf.py"
      provides: "regen dispatch arms + IN_SCOPE_METRICS entries for the 3 rate families"
      contains: "conversion_rate"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/repositories/user_benchmark_percentiles_repository.py"
      via: "_effective_rows.get('conversion_rate', {}).get(tc_bucket) — same fetch_for_user [metric][tc] path"
      pattern: "_effective_rows.get\\(\"conversion_rate\""
    - from: "app/services/user_benchmark_percentiles_service.py"
      to: "app/services/canonical_slice_sql.py"
      via: "import + dispatch of the 3 rate builders"
      pattern: "per_user_cte_(conv|parity|recovery)_rate_tc"
---

<objective>
Wave 2A backend wiring. With the Wave-1 contracts in place, this plan connects the rate builders to BOTH compute paths (per-user Stage B + cohort-CDF regen dispatch) and threads the resulting percentile onto the API payload as a SEPARATE field from the existing ΔES-gap percentile.

This is the drift-proof closing move (SC-2): the same builders from Plan 02 are dispatched identically in `user_benchmark_percentiles_service._per_user_cte_for_family_and_tc` (per-user) and `gen_global_percentile_cdf._per_user_cte_for_metric_and_tc` (cohort). The payload field (`rate_percentile`) is named distinctly so the existing gap `percentile` is untouched (D-01: both coexist).

Runs in parallel with Plan 04 (frontend) — zero file overlap.

Purpose: make the rate percentile computable per-user and surface it on the per-TC block payload.
Output: dispatch extensions in 2 files, schema field trio, endgame_service lookup + threading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-RESEARCH.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-PATTERNS.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage B + regen dispatch arms for the 3 rate families</name>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py lines 139-147 (STAGE_B_METRIC_FAMILIES) + lines 155-191 (_per_user_cte_for_family_and_tc dispatch + raise) + the top-of-file import block from canonical_slice_sql
    - scripts/gen_global_percentile_cdf.py lines 154-163 (IN_SCOPE_METRICS) + lines 258-326 (_per_user_cte_for_metric_and_tc dispatch) + ~line 586 (_METRIC_SECTION_LABELS) + lines 159-164 of backfill_user_percentiles.py for the derived _ALL_METRICS note
    - app/services/canonical_slice_sql.py (the 3 new builders from Plan 02 — confirm exact names from 99-02-SUMMARY.md)
    - 99-RESEARCH.md "Pattern 4" + "Pattern 7" + Pitfalls 5/7
    - 99-PATTERNS.md "user_benchmark_percentiles_service.py" + "gen_global_percentile_cdf.py" + "backfill_user_percentiles.py" sections
  </read_first>
  <action>
    In app/services/user_benchmark_percentiles_service.py: import per_user_cte_conv_rate_tc, per_user_cte_parity_rate_tc, per_user_cte_recovery_rate_tc from canonical_slice_sql. Append "conversion_rate", "parity_rate", "recovery_rate" to STAGE_B_METRIC_FAMILIES (this auto-propagates to backfill _ALL_METRICS per Pitfall 7 — no backfill edit needed). Add 3 dispatch arms to _per_user_cte_for_family_and_tc before the raise ValueError, each routing to the matching builder with source="single_user", snapshot_date=None.
    In scripts/gen_global_percentile_cdf.py: append the same 3 family names to IN_SCOPE_METRICS, add 3 dispatch arms to _per_user_cte_for_metric_and_tc before its raise, each routing to the matching builder with source="benchmark", snapshot_date=snapshot_date. Add 3 entries to _METRIC_SECTION_LABELS (~line 586) so the regen report names the new families. (The actual regen RUN happens in Plan 05 — this plan only wires the dispatch so the script is correct.)
    Both dispatch tables route to the SAME builders → CDF construction and per-user lookup share one code path (SC-2 drift-proof). Do NOT add a parallel rate-computation path anywhere.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x -q && uv run ty check app/ scripts/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "conversion_rate\|parity_rate\|recovery_rate" on STAGE_B_METRIC_FAMILIES region of user_benchmark_percentiles_service.py shows 3 additions.
    - app/services/user_benchmark_percentiles_service.py contains "per_user_cte_conv_rate_tc" (import + dispatch arm).
    - scripts/gen_global_percentile_cdf.py IN_SCOPE_METRICS contains the 3 family names and _per_user_cte_for_metric_and_tc has 3 new arms (grep -c "per_user_cte_conv_rate_tc\|per_user_cte_parity_rate_tc\|per_user_cte_recovery_rate_tc" >= 3).
    - `uv run python -c "from scripts.gen_global_percentile_cdf import IN_SCOPE_METRICS; assert {'conversion_rate','parity_rate','recovery_rate'} <= set(IN_SCOPE_METRICS)"` exits 0.
    - `uv run ty check app/ scripts/ tests/` exits 0 (all CdfMetricId dispatch arms cover the widened Literal — Pitfall 5).
  </acceptance_criteria>
  <done>Both dispatch tables route the 3 rate families to the shared Plan-02 builders; ty check clean; the regen script imports/dispatches correctly (run deferred to Plan 05).</done>
</task>

<task type="auto">
  <name>Task 2: PerTcBucketStats rate field trio + endgame_service threading</name>
  <read_first>
    - app/schemas/endgames.py lines 822-856 (PerTcBucketStats — the existing percentile/percentile_n_games/percentile_value trio + docstring to extend)
    - app/services/endgame_service.py lines 2478-2550 (_build_per_tc_bucket_stats signature + return) + lines 2655-2689 (the 3-row lookup block in _compute_per_tc_metric_cards) + line 2562-2574 (terminal-span semantics)
    - app/repositories/user_benchmark_percentiles_repository.py lines 158-203 (fetch_for_user return shape — PercentileRow with .percentile/.n_games/.value; nested [metric][tc])
    - 99-RESEARCH.md "Pattern 5" + "endgame_service.py wire-through" code example
    - 99-PATTERNS.md "endgames.py" + "endgame_service.py" sections (exact field names + signature adaptation)
    - tests/services/test_endgame_service.py (the Wave-0 rate_percentile field test — must turn green)
  </read_first>
  <action>
    In app/schemas/endgames.py: append three optional fields to PerTcBucketStats immediately after percentile_value — rate_percentile: float | None = None, rate_percentile_n_games: int | None = None, rate_percentile_value: float | None = None. They MUST be named distinctly from percentile* (D-01: the new raw-rate percentile coexists with the existing ΔES-gap percentile; do NOT reuse or overload `percentile`). Default None preserves all existing fixtures. Extend the class docstring to document the 3 new fields following the existing percentile_n_games/percentile_value documentation style; note they carry the RAW-RATE percentile (separate signal from the skill-adjusted ΔES-gap percentile).
    In app/services/endgame_service.py: in the _compute_per_tc_metric_cards lookup block (after the existing conv_row/parity_row/recov_row .get lookups, ~line 2659), add conv_rate_row = _effective_rows.get("conversion_rate", {}).get(tc_bucket), and parity_rate_row / recov_rate_row for the other two families — same nested [metric][tc] path as the gap rows (no new repository method; fetch_for_user already serves these once Plan 05 backfills them). Add rate_percentile_row: PercentileRow | None = None as a 5th parameter to _build_per_tc_bucket_stats and wire the 3 new return fields (rate_percentile = rate_percentile_row.percentile if not None else None, etc.). Update the 3 call sites to pass conv_rate_row / parity_rate_row / recov_rate_row respectively. Keep _build_per_tc_bucket_stats within the CLAUDE.md nesting/LOC limits — it is already factored; the addition is one param + 3 return lines.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service.py -x -q && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "rate_percentile" app/schemas/endgames.py returns >= 3 (the field trio).
    - The Wave-0 schema field test passes: `uv run pytest tests/services/test_endgame_service.py -x -q -k "rate_percentile or PerTcBucketStats"` exits 0 — proving rate_percentile is a distinct, defaulted field that does not collide with percentile (D-01).
    - app/services/endgame_service.py contains _effective_rows.get("conversion_rate", and the matching parity_rate / recovery_rate lookups (grep -c returns 3).
    - _build_per_tc_bucket_stats signature includes rate_percentile_row and the return constructs rate_percentile / rate_percentile_n_games / rate_percentile_value.
    - `uv run pytest tests/services/test_endgame_service.py -x -q` exits 0 (no existing fixture broke — backward compat via None defaults).
    - `uv run ty check app/ tests/` exits 0.
  </acceptance_criteria>
  <done>PerTcBucketStats carries the distinct rate field trio, endgame_service looks up + threads the 3 rate rows via the existing fetch_for_user path, the Wave-0 schema test is green, and no existing endgame_service test regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | GET /api/endgames/metrics/cards returns per-user percentile data scoped to the authenticated user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-IDOR | Information Disclosure | rate-percentile lookup in endgame_service | mitigate | Rate rows are read through the unchanged fetch_for_user(user_id=current_user.id) [metric][tc] path; no new user-id parameter is introduced. Same V4 access-control mitigation as the existing gap percentiles. |
| T-99-SQLi | Tampering | dispatch arms passing tc to builders | mitigate | tc remains a TimeControlBucket Literal end-to-end; the dispatch only selects a builder by a closed-set family name. No user string flows into SQL. |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. No install task → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_endgame_service.py -x -q` green.
- `uv run python -c "from scripts.gen_global_percentile_cdf import IN_SCOPE_METRICS; assert {'conversion_rate','parity_rate','recovery_rate'} <= set(IN_SCOPE_METRICS)"` exits 0.
- `uv run ruff format app/ scripts/ tests/ && uv run ruff check app/ scripts/ tests/ && uv run ty check app/ scripts/ tests/` clean.
</verification>

<success_criteria>
SC-2 fully wired on the backend: one builder per family feeds both compute paths; the rate percentile threads onto the per-TC block as a separate field. Ready for Plan 05 to regen + backfill and Plan 04 to render.
</success_criteria>

<output>
Create `.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-03-SUMMARY.md` — record the final PerTcBucketStats field names, the dispatch arm wiring, and confirm the regen script run is deferred to Plan 05.
</output>
