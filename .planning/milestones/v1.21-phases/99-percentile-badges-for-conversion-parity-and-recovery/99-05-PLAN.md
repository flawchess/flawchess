---
phase: 99-percentile-badges-for-conversion-parity-and-recovery
plan: 05
type: execute
wave: 3
depends_on: ["99-03"]
files_modified:
  - app/services/global_percentile_cdf.py
  - reports/percentile-cdf-regen/
# autonomous: false — benchmark-DB regen + dev backfill verification + prod backfill gated on human sign-off (D-11)
autonomous: false
requirements: []  # standalone — coverage driven by SC-3, SC-5
user_setup: []
must_haves:
  truths:
    - "Cohort CDFs are regenerated into global_percentile_cdf.py for conversion_rate/parity_rate/recovery_rate across all 4 TCs (12 cells), non-empty, under the per-(metric, ELO anchor, TC) sliding-window protocol (SC-3/D-10)"
    - "The regen report is archived (SC-3/D-10)"
    - "The COHORT_PERCENTILE_CDF generated block stays CI-drift-checked (regenerated, not hand-edited)"
    - "Backfill populates the 12 new metrics on dev DB; prod backfill runs only after explicit human sign-off (SC-5/D-11)"
  artifacts:
    - path: "app/services/global_percentile_cdf.py"
      provides: "regenerated COHORT_PERCENTILE_CDF with the 12 new (metric, TC) cells"
      contains: "conversion_rate"
  key_links:
    - from: "scripts/gen_global_percentile_cdf.py"
      to: "app/services/global_percentile_cdf.py"
      via: "BEGIN/END GENERATED REGISTRY sentinel overwrite against benchmark DB"
      pattern: "GENERATED REGISTRY"
    - from: "scripts/backfill_user_percentiles.py"
      to: "user_benchmark_percentiles (dev)"
      via: "_ALL_METRICS (auto-derived) upsert for the 3 new rate families"
      pattern: "conversion_rate"
---

<objective>
Wave 3 materialization. With dispatch wired (Plan 03), regenerate the cohort CDFs into `global_percentile_cdf.py` against the benchmark DB (SC-3), archive the regen report, then backfill the 12 new metric rows on the dev DB (SC-5). The prod backfill is explicitly gated behind human sign-off per D-11 — this plan stops at dev and surfaces a checkpoint for prod.

This plan is NOT autonomous: the regen requires the benchmark DB (Docker port 5433), the dev backfill writes DB rows we verify by query, and the prod backfill (via `bin/prod_db_tunnel.sh`) must not run without explicit user approval (D-11). No dev DB reset is required or used (per the no-dev-DB-reset rule) — backfill runs against the existing dev DB.

It modifies `global_percentile_cdf.py` (the generated registry block), which Plan 02 also touched (CdfMetricId) — hence this is sequenced after the backend waves, not parallel.

Purpose: populate the cohort CDFs and per-user rows so the Plan-04 chips actually render with data.
Output: regenerated registry, archived regen report, dev backfill rows, prod checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-RESEARCH.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate cohort CDFs into global_percentile_cdf.py + archive the regen report</name>
  <read_first>
    - scripts/gen_global_percentile_cdf.py FULL FILE — the BEGIN/END GENERATED REGISTRY sentinel overwrite logic, the benchmark-only guard, the regen-report archive path, the IN_SCOPE_METRICS loop (Plan 03 added the 3 rate families + dispatch arms)
    - app/services/global_percentile_cdf.py lines 101-110 (CdfMetricId — already widened in Plan 02) + the BEGIN/END GENERATED REGISTRY sentinels (do NOT hand-edit the block; the script overwrites it)
    - reports/percentile-cdf-regen/ (existing archived regen reports — match the naming + location convention) — locate via: ls reports/percentile-cdf-regen/ 2>/dev/null || ls reports/ | grep -i cdf
    - CLAUDE.md "benchmark-db" + gen_endgame_zones_ts.py CI-drift-discipline note (generated files are CI-drift-checked; regenerate, never hand-edit)
    - 99-RESEARCH.md "Don't Hand-Roll" (CDF generation row) + "Validation Architecture" SC-3 row + Pitfall 1 (user_id JOIN → empty tables if omitted)
    - skill: benchmarks (reports/benchmark/benchmarks-latest.md is the source of truth for per-TC rate distributions — sanity-check the regen output against §3.2.1)
  </read_first>
  <action>
    Start the benchmark DB: `bin/benchmark_db.sh start` (Docker port 5433; runs migrations + re-grants RO privileges). Run the regen script targeting benchmark per its existing invocation (confirm the exact flag from the script's argparse — e.g. `uv run python scripts/gen_global_percentile_cdf.py` with the benchmark target). The script overwrites the COHORT_PERCENTILE_CDF block between the GENERATED REGISTRY sentinels in global_percentile_cdf.py with the new 12 (metric, TC) cells alongside the existing metrics, and writes a regen report to the archive location. Verify the new cells are NON-EMPTY (Pitfall 1: empty CdfTables mean the per_user_values builder dropped user_id — if any of the 12 cells is empty, STOP and flag back to Plan 02's builder rather than shipping empty CDFs). Sanity-check the generated rate distributions against reports/benchmark/benchmarks-latest.md §3.2.1 (conversion/parity/recovery per-TC rates) — the CDF medians should be in the same ballpark as the benchmark report. Archive the regen report under the established reports/ location. Do NOT hand-edit the generated registry block.
  </action>
  <verify>
    <automated>uv run python -c "from app.services.global_percentile_cdf import COHORT_PERCENTILE_CDF as C; import sys; missing=[m for m in ('conversion_rate','parity_rate','recovery_rate') if not any(k[0]==m for k in C)]; sys.exit(1 if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - After regen, app/services/global_percentile_cdf.py COHORT_PERCENTILE_CDF contains entries for conversion_rate, parity_rate, recovery_rate (the keying matches the existing per-(metric, anchor, TC) structure — confirm via the verify command above exiting 0).
    - For each of the 3 families × 4 TCs, at least one anchor cell has a non-empty CdfTable (no empty tables → Pitfall 1 not triggered). Document the per-cell row counts in the SUMMARY.
    - A regen report file exists in the archive location with a 2026-05-3x timestamp.
    - `uv run ruff format app/services/global_percentile_cdf.py && uv run ty check app/services/global_percentile_cdf.py` clean (the generated block must be format-stable so CI drift-check passes).
    - The generated block was produced by the script, not hand-edited (the regen-report archive is the evidence).
  </acceptance_criteria>
  <done>The 12 new cohort CDF cells are regenerated, non-empty, format-stable, and the regen report is archived; benchmark output sanity-matches benchmarks-latest.md §3.2.1.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill the 12 new metrics on the dev DB</name>
  <read_first>
    - scripts/backfill_user_percentiles.py FULL FILE — _ALL_METRICS derivation (auto-picks up the 3 families from STAGE_B_METRIC_FAMILIES, Plan 03), _assert_target_safe port-check, the --target/--metric argparse, the _PercentileSummary cell dict
    - 99-RESEARCH.md "Validation Architecture" SC-5 row + Pitfall 7 (_ALL_METRICS auto-derivation) + "Environment Availability" (dev DB on 5432)
    - CLAUDE.md "Database Access (MCP)" (flawchess-db read-only query tool for verifying row counts) + the no-dev-DB-reset rule (backfill against existing dev DB; do NOT reset)
  </read_first>
  <action>
    Ensure the dev DB is up (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`) and migrated to head (Plan 02's migration). Run the backfill against dev for the 3 new families per the script's existing invocation (e.g. `uv run python scripts/backfill_user_percentiles.py --target dev --metric conversion_rate`, then parity_rate, recovery_rate — or whatever the script's batch form is; confirm from its argparse). The _assert_target_safe port-check (T-99-ENV mitigation) guards against accidental cross-env writes. After the run, verify rows were written for the 12 new metric values using the flawchess-db MCP query tool (count rows in user_benchmark_percentiles grouped by metric for the new ENUM values). Expect rows for the bullet/blitz/rapid users above the ≥30 floor and naturally fewer/zero for thin classical users (per the D-05 validation in RESEARCH — this is correct suppression, not a bug). Do NOT reset the dev DB.
    Then STOP for the prod gate (Task 3).
  </action>
  <verify>
    <automated>uv run python -c "import scripts.backfill_user_percentiles as b; ms=set(b._ALL_METRICS); import sys; sys.exit(0 if {'conversion_rate','parity_rate','recovery_rate'} <= ms else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The backfill run completes without error against dev; its summary table reports cells for conversion_rate / parity_rate / recovery_rate.
    - A flawchess-db MCP query `SELECT metric, count(*) FROM user_benchmark_percentiles WHERE metric::text LIKE '%_rate_%' GROUP BY metric` returns rows for the new TC-suffixed metric values (non-zero for bullet/blitz/rapid; classical may be sparse/zero — acceptable per D-05).
    - `_ALL_METRICS` includes the 3 new families (verify command exits 0).
    - No dev DB reset was performed (backfill ran against the existing populated dev DB).
  </acceptance_criteria>
  <done>Dev DB has rate-percentile rows for the new metrics (above-floor users), row counts verified by query, classical sparsity confirmed as expected suppression; no dev reset.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Prod backfill sign-off (D-11)</name>
  <action>Pause for human sign-off before any prod write (D-11). Present dev verification, then on approval run the prod backfill via bin/prod_db_tunnel.sh as detailed below; do not write to prod without explicit approval.</action>
  <what-built>
    Cohort CDFs regenerated (Task 1) and dev DB backfilled (Task 2) for the 12 new rate-percentile metrics. The full backend + frontend chain is in place; chips render on dev with real data.
  </what-built>
  <how-to-verify>
    1. Confirm dev verification looks right: load the dashboard endgame metrics cards locally (`bin/run_local.sh`), open the per-TC Conversion/Parity/Recovery cards, and confirm the new title-line rate chips render (and that classical may suppress where data is thin). Confirm the existing ΔES-gap chips are unchanged (both chips visible).
    2. When satisfied, the PROD backfill (D-11) runs against prod via the tunnel: `bin/prod_db_tunnel.sh` to open the tunnel (forwards localhost:15432), then run `scripts/backfill_user_percentiles.py --target prod` for the 3 new families, then `bin/prod_db_tunnel.sh stop`. The _assert_target_safe port-check guards the target. NOTE: the regenerated CDF artifact (global_percentile_cdf.py) ships with the code deploy — prod reads the same static CDF; only the per-user rows need backfilling on prod.
    3. Do NOT run the prod backfill until you explicitly approve it here.
  </how-to-verify>
  <resume-signal>Type "approved — prod backfill done" (after you have run the prod backfill via tunnel), or "approved — defer prod" to ship the code and run the prod backfill at deploy time, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → DB (dev/benchmark/prod) | The regen reads the benchmark DB; the backfill writes user_benchmark_percentiles on dev and (after sign-off) prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-ENV | Tampering | backfill cross-env write | mitigate | scripts/backfill_user_percentiles.py `_assert_target_safe` port-check covers the new metrics automatically (they ride the existing _ALL_METRICS path); prod target requires the explicit tunnel + the blocking human checkpoint. |
| T-99-PROD | Tampering | prod backfill without sign-off | mitigate | Prod backfill is behind a blocking checkpoint:human-action (D-11); never auto-runs. autonomous:false on this plan. |
| T-99-DRIFT | Tampering | hand-edited generated CDF block | mitigate | The COHORT_PERCENTILE_CDF block is regenerated by the script (CI drift-check enforces this); the archived regen report is the provenance evidence. |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. No install task → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run python -c "from app.services.global_percentile_cdf import COHORT_PERCENTILE_CDF as C; ..."` confirms the 3 new families exist in the registry.
- flawchess-db MCP query confirms dev rows for the new metrics.
- Regen report archived; generated block format-stable (CI drift-check passes).
- Prod backfill executed (or explicitly deferred) only after human sign-off.
</verification>

<success_criteria>
SC-3 (cohort CDFs regenerated into global_percentile_cdf.py under the per-(metric, anchor, TC) protocol, report archived) and SC-5 (dev backfill done; prod gated on sign-off) satisfied. The full chain renders chips on dev with real cohort data.
</success_criteria>

<output>
Create `.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-05-SUMMARY.md` — record per-cell CDF row counts, dev backfill row counts per metric, the archived regen report path, and the prod-backfill disposition (done / deferred to deploy).
</output>
