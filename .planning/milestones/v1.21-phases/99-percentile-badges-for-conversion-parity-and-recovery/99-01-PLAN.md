---
phase: 99-percentile-badges-for-conversion-parity-and-recovery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/services/test_canonical_slice_sql.py
  - tests/models/test_user_benchmark_percentile.py
  - tests/services/test_endgame_service.py
  - frontend/src/components/charts/EndgameMetricsByTcCard.test.tsx
autonomous: true
requirements: []   # standalone — no REQ-IDs; coverage driven by SC-1..SC-5
must_haves:
  truths:
    - "A failing/MISSING-marked test exists for each rate builder's floor HAVING clause (SC-1)"
    - "A failing/MISSING-marked test exists for CDF↔per-user source parity per rate builder (SC-2)"
    - "A test asserts the 12 new ENUM members exist in the SAEnum descriptor (SC-2)"
    - "A test asserts rate_percentile is a separate field on PerTcBucketStats, distinct from percentile (SC-2/D-01)"
    - "A frontend test asserts the title-line rate chip renders/suppresses + TC-scoped bullet 1 (SC-1/SC-4)"
  artifacts:
    - path: "tests/models/test_user_benchmark_percentile.py"
      provides: "ENUM-member presence assertions for the 12 new rate metrics"
      contains: "conversion_rate_bullet"
    - path: "frontend/src/components/charts/EndgameMetricsByTcCard.test.tsx"
      provides: "Rate-chip render/suppression + TC-scoped tooltip assertions"
      contains: "rate_percentile"
  key_links:
    - from: "tests/services/test_canonical_slice_sql.py"
      to: "app/services/canonical_slice_sql.py"
      via: "import of per_user_cte_conv_rate_tc / parity_rate / recovery_rate (will resolve once Wave 1 lands)"
      pattern: "per_user_cte_(conv|parity|recovery)_rate_tc"
---

<objective>
Wave 0 test scaffolding. Create or extend the four test surfaces named in 99-VALIDATION.md so that every Phase 99 Success Criterion has an automated home BEFORE implementation lands. Two backend test files are extended in place; one backend test file and one frontend test file are created new.

This plan establishes the Nyquist contract: each implementation task in Waves 1-2 points its `<verify><automated>` at a test that already exists here. Tests that depend on Wave-1 symbols (the rate builders, the widened ENUM) are written to import those symbols; they will fail to import until Wave 1 lands, which is the intended RED state. Tests that exercise only existing surfaces (the `rate_percentile` field default, the frontend suppression gate) can pass immediately against `None`/absent data.

Purpose: prevent shallow execution — no implementation task ships without a test asserting its behavior.
Output: 4 test files (2 extended, 2 new) covering SC-1, SC-2, SC-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-RESEARCH.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-PATTERNS.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend rate-builder + ENUM + schema test scaffolds</name>
  <read_first>
    - tests/services/test_canonical_slice_sql.py (existing — extend; note the substring-matching assertion style and the existing import block from app.services.canonical_slice_sql)
    - tests/services/test_endgame_service.py (existing — extend; reuse the existing PerTcBucketStats fixture construction style)
    - app/services/canonical_slice_sql.py lines 119-138 (constant block) and 726-836 (per_user_cte_score_gap_bucket_tc — the analog whose floor HAVING and per_user_values projection the rate builders mirror)
    - app/models/user_benchmark_percentile.py lines 54-65 (benchmark_metric_enum SAEnum — assertion target for ENUM membership)
    - app/schemas/endgames.py lines 822-856 (PerTcBucketStats — the rate_percentile field pair to assert)
    - 99-PATTERNS.md "Pattern Assignments" (rate builder + per_user_values projection + SAEnum sections)
  </read_first>
  <action>
    Extend tests/services/test_canonical_slice_sql.py with three new tests, one per rate builder (conv/parity/recovery). Import per_user_cte_conv_rate_tc, per_user_cte_parity_rate_tc, per_user_cte_recovery_rate_tc and MINIMUM_RATE_BUCKET_SPANS from app.services.canonical_slice_sql (these imports will fail until Plan 02 lands — that is the intended RED). For each builder, follow the file's substring-matching style:
      - Floor assertion (SC-1): assert the rendered SQL contains a HAVING clause filtering the per-bucket span count against MINIMUM_RATE_BUCKET_SPANS (substring like ">= 30" or the constant value interpolated), naming the test test_per_user_cte_conv_rate_tc_floor (and parity/recovery variants).
      - Source-parity assertion (SC-2): name the test test_per_user_cte_conv_rate_tc_source_parity (and parity/recovery variants). Render the builder twice with source="benchmark" and source="single_user" and assert the two SQL strings are byte-identical (the cohort difference lives only in selected_users_cte, never in the pooled body — `_ = source` idiom). This is the drift-proof guarantee.
      - metric_value formula assertion: assert per_user_values projects (user_id, metric_value, n_games) and the metric_value expression matches the family formula — wins/spans for conversion, (wins + 0.5*draws)/spans for parity, (wins+draws)/spans for recovery. Substring-match the division expression.
    Create a NEW file tests/models/test_user_benchmark_percentile.py. Import benchmark_metric_enum from app.models.user_benchmark_percentile and assert all 12 new string members are present in its enums tuple: conversion_rate_bullet/blitz/rapid/classical, parity_rate_*, recovery_rate_* (SC-2). Use a parametrized test over the 12 names. This test will fail until Plan 02 widens the SAEnum.
    Extend tests/services/test_endgame_service.py: add a test asserting PerTcBucketStats accepts and round-trips the rate_percentile / rate_percentile_n_games / rate_percentile_value fields, and that they default None and are DISTINCT from percentile/percentile_n_games/percentile_value (D-01: both coexist). Build a PerTcBucketStats with both field families populated to different values and assert they do not collide. This test can pass immediately once Plan 03 adds the fields; until then it fails on the unknown-field validation error (intended RED).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py tests/models/test_user_benchmark_percentile.py tests/services/test_endgame_service.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_canonical_slice_sql.py --collect-only -q` lists test_per_user_cte_conv_rate_tc_source_parity, test_per_user_cte_parity_rate_tc_source_parity, test_per_user_cte_recovery_rate_tc_source_parity, and the three *_floor tests.
    - File tests/models/test_user_benchmark_percentile.py exists and references the literal string "conversion_rate_bullet".
    - The collect step succeeds (tests are syntactically valid); individual rate-builder tests are EXPECTED to error on import (ImportError for the not-yet-existing builders) — that is the intended Wave 0 RED state and is documented in the SUMMARY.
    - grep -c "source_parity" tests/services/test_canonical_slice_sql.py returns >= 3.
  </acceptance_criteria>
  <done>The three backend test surfaces collect; the new ENUM test file exists; rate-builder tests import the Wave-1 symbols (RED until Plan 02), the schema field test references rate_percentile.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend rate-chip test scaffold</name>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsByTcCard.tsx (the component under test — note MetricBlock, the existing ΔES-gap chip wiring, the anchorRating prop, and the testId scheme)
    - frontend/src/components/charts/EndgameTimePressureCard.test.tsx if present (analog test for the multi-chip title-line pattern) — locate via: ls frontend/src/components/charts/*.test.tsx
    - frontend/src/components/charts/PercentileChip.tsx (chip props: percentile, flavor, tc, anchorRating, metricLabel, testId, nGames, value; and PercentileChipPopoverBody bullet-1 copy)
    - frontend/src/types/endgames.ts lines 422-439 (PerTcBucketStats — note the rate_percentile fields are added in Plan 04)
    - 99-PATTERNS.md "EndgameMetricsByTcCard.tsx" section (chip placement, metricLabel mapping, tooltip differentiation)
  </read_first>
  <action>
    Create frontend/src/components/charts/EndgameMetricsByTcCard.test.tsx (vitest + @testing-library/react). Cast test fixtures to the PerTcBucketStats / props shape so the file compiles before Plan 04 adds the rate_percentile field (use a typed builder + `as` cast on the extra fields, matching how other chart tests stub partial data). Assert four behaviors:
      - RENDER (SC-1): when a block has rate_percentile != null AND anchorRating is provided, a PercentileChip with testId matching the `*-rate-percentile-chip` suffix is present for that bucket.
      - SUPPRESS-on-null-percentile (SC-1): when rate_percentile == null, NO rate chip renders for that bucket (query the rate-chip testId returns null).
      - SUPPRESS-on-null-anchor (SC-1): when anchorRating is undefined but rate_percentile is set, NO rate chip renders.
      - TC-SCOPED TOOLTIP + metric noun (SC-4 / D-08): the rendered rate chip carries metricLabel "Conversion Rate" / "Parity Rate" / "Recovery Rate" per bucket, and the popover body names the time control (tc) and the rate noun. Assert via the chip's aria-label / metricLabel prop surface (or open the popover and assert bullet-1 text contains the TC label and the rate noun).
      - COEXISTENCE (D-01): assert the existing ΔES-gap chip and the new rate chip both render for the same block when both percentile sources are present (two distinct chip testIds).
    Do NOT assert exact percentile color or copy beyond the TC label + rate noun (per popover-copy-minimalism memory; the test pins behavior, not prose wording).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run EndgameMetricsByTcCard --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/components/charts/EndgameMetricsByTcCard.test.tsx exists and contains the substring "rate_percentile" and "rate-percentile-chip".
    - `cd frontend && npx tsc --noEmit` does not report a NEW error originating from the test file's fixture typing (casts compile).
    - The test file defines at least 4 distinct `it(`/`test(` cases covering render, suppress-on-null-percentile, suppress-on-null-anchor, and TC-scoped tooltip.
    - Running the suite is EXPECTED to fail the render/coexistence assertions until Plan 04 wires the chip — document the RED state in the SUMMARY. The suppression-on-null cases may already pass.
  </acceptance_criteria>
  <done>The frontend test file exists, compiles (tsc clean for its own typing), and pins the four chip behaviors; render assertions are RED until Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 touches only test files; no runtime trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-00 | Tampering | test files only | accept | No production code, no input surface; tests assert the very mitigations the impl plans implement. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/models/test_user_benchmark_percentile.py tests/services/test_endgame_service.py --collect-only -q` succeeds (collection, not pass).
- `cd frontend && npx tsc --noEmit` clean for the new test file's own typing.
- All four Wave 0 files from 99-VALIDATION.md exist or are extended.
</verification>

<success_criteria>
SC-1, SC-2, SC-4 each have a named automated test in the suite. Rate-builder and ENUM tests are RED (import/membership failures) until Wave 1; the schema-field and frontend-suppression tests pin behavior immediately. No 3 consecutive downstream tasks lack an automated verify.
</success_criteria>

<output>
Create `.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-01-SUMMARY.md` when done — record which tests are intentionally RED (awaiting Wave 1/2) vs already green.
</output>
