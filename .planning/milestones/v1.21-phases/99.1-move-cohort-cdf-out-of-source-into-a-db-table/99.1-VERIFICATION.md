---
phase: 99.1-move-cohort-cdf-out-of-source-into-a-db-table
verified: 2026-05-31T12:00:00Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---

# Phase 99.1: Move Cohort CDF Out of Source into a DB Table — Verification Report

**Phase Goal:** Refactor app/services/global_percentile_cdf.py by moving the generated COHORT_PERCENTILE_CDF registry into a benchmark_cohort_cdf DB table. The generator emits a compact seed file; an idempotent seed script loads it, wired into bin/run_local.sh. Module shrinks to ~250 lines (keeps CdfMetricId, CdfTable, interpolation math). Output must be byte-for-byte equivalent to pre-refactor.
**Verified:** 2026-05-31T12:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | global_percentile_cdf.py no longer contains the generated registry; ~215 lines; BEGIN/END sentinels gone | VERIFIED | `wc -l` = 215; grep for COHORT_PERCENTILE_CDF, BEGIN/END GENERATED returns 0 hits in live code; `collections.abc.Mapping` import removed |
| 2 | interpolate_cohort_percentile stays SYNC and takes a prefetched CdfTable (no session, no async) — D-04 | VERIFIED | `inspect.signature` confirms params=['value','table']; `iscoroutinefunction` = False; module docstring preserved purity invariant |
| 3 | compute_stage_a and compute_stage_b issue one batched load_cohort_cells query per import (D-03) and resolve from in-memory dict | VERIFIED | Both stages call `load_cohort_cells(session, rounded_anchors, ...)` exactly once before their respective inner loops; no asyncio.gather; service source confirmed via code inspection |
| 4 | Chip / percentile output is byte-for-byte equivalent to pre-refactor (SC#6) | VERIFIED | SUMMARY-04 records diff of /tmp/cdf_baseline_pre.json vs /tmp/cdf_baseline_post.json = identical (33 entries, user 14, 4 anchor ratings); live spot-check: DB breakpoints[0]=-0.2498 matches TSV, p50 interpolation = 50.0 |
| 5 | benchmark_cohort_cdf model + single-head Alembic migration exist | VERIFIED | app/models/benchmark_cohort_cdf.py (80 lines); migration d0a962b51c68 with composite PK (metric, anchor_elo, tc, percentile) confirmed; migration is schema-only (no bulk_insert/INSERT data) |
| 6 | app/data/cohort_cdf.tsv committed; generator emits TSV (no longer rewrites .py); drift test exists | VERIFIED | TSV: 122,662 lines (header + 122,661 rows); 7-column header confirmed; generator has _write_seed_tsv, OUTPUT_SEED_PATH, no _write_module/_emit_registry_block; drift test passes (3 tests) |
| 7 | scripts/seed_cohort_cdf.py idempotent (ON CONFLICT DO UPDATE) | VERIFIED | ON CONFLICT in _INSERT_SQL; no f-string SQL interpolation; parameterized via text() |
| 8 | bin/run_local.sh has count-gated seed block | VERIFIED | Lines 46-51: COHORT_CDF_COUNT query + -eq 0 gate + seed call, mirroring openings block; `bash -n` passes |
| 9 | dev DB is seeded (122,661 rows) | VERIFIED | `SELECT COUNT(*) FROM benchmark_cohort_cdf` = 122,661 |
| 10 | app/repositories/benchmark_cohort_cdf_repository.py load_cohort_cells = batched prefetch (one query) | VERIFIED | Single SELECT with .in_() on anchors and tcs; ORDER BY metric, anchor_elo, tc, percentile; no asyncio.gather; early-return {} on empty inputs |
| 11 | Drift test compares regenerated TSV content, not .py source bytes | VERIFIED | test_write_seed_tsv_round_trip reconstructs cells from TSV, calls _write_seed_tsv to tmp file, asserts byte equality; does NOT import COHORT_PERCENTILE_CDF |
| 12 | All backend gates pass | VERIFIED | ty: 0 errors; ruff check: clean; ruff format: clean (231 files); Phase 99.1-relevant tests: 70 passed |
| 13 | The new model is discoverable by Alembic (reaches Base.metadata) | VERIFIED | alembic/env.py line 19: explicit import with # noqa: F401; PK check confirms ['metric','anchor_elo','tc','percentile'] |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/models/benchmark_cohort_cdf.py` | BenchmarkCohortCdf model, composite PK | VERIFIED | 80 lines; 4-column composite PK via primary_key=True; reuses benchmark_metric_enum + time_control_bucket_enum (create_type=False); Double for value, SmallInteger for anchor_elo/percentile, nullable audit columns |
| `alembic/versions/20260531_063037_d0a962b51c68_add_benchmark_cohort_cdf.py` | Schema-only create_table migration | VERIFIED | op.create_table('benchmark_cohort_cdf'...) with PrimaryKeyConstraint; no bulk_insert/INSERT; downgrade drops only benchmark_cohort_cdf |
| `app/data/cohort_cdf.tsv` | Long-row seed file with 7-column header | VERIFIED | 122,661 data rows; header = metric/anchor_elo/tc/percentile/value/n_users/snapshot_month |
| `scripts/gen_global_percentile_cdf.py` | _write_seed_tsv emitter; no source-rewrite stack | VERIFIED | _write_seed_tsv defined; OUTPUT_SEED_PATH = app/data/cohort_cdf.tsv; retired symbols absent (count=0) |
| `scripts/seed_cohort_cdf.py` | Idempotent INSERT ... ON CONFLICT DO UPDATE | VERIFIED | ON CONFLICT on 4-column PK; parameterized text(); csv.DictReader streaming |
| `app/repositories/benchmark_cohort_cdf_repository.py` | async load_cohort_cells batched prefetch | VERIFIED | One SELECT via .in_() on anchors and tcs; ORDER BY percentile is load-bearing; _group_rows_into_cells helper; empty-input guard |
| `tests/scripts/test_cohort_cdf_seed_artifact.py` | TSV drift test (round-trip formulation) | VERIFIED | 3 tests pass: header, row count, byte-identical round-trip |
| `tests/repositories/test_benchmark_cohort_cdf_repository.py` | Repository loader tests (5 tests) | VERIFIED | 5 tests pass: empty anchors, empty tcs, cell round-trip, grouping keys, incomplete cell skipped |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| alembic/env.py | app.models.benchmark_cohort_cdf.BenchmarkCohortCdf | explicit import line 19 with # noqa: F401 | WIRED | Confirmed at line 19 of alembic/env.py |
| scripts/gen_global_percentile_cdf.py | app/data/cohort_cdf.tsv | _write_seed_tsv(cells, OUTPUT_SEED_PATH) at line 808 | WIRED | regenerate() calls _write_seed_tsv; OUTPUT_SEED_PATH ends with app/data/cohort_cdf.tsv |
| tests/scripts/test_cohort_cdf_seed_artifact.py | app/data/cohort_cdf.tsv | round-trip TSV content equality assertion | WIRED | _load_cells_from_tsv reads committed TSV; _write_seed_tsv emits to tmp; bytes compared |
| app/services/user_benchmark_percentiles_service.py | app.repositories.benchmark_cohort_cdf_repository.load_cohort_cells | one prefetch call before inner loop in compute_stage_a and compute_stage_b | WIRED | load_cohort_cells called twice in service (once per stage), confirmed at lines 422 and 510 |
| app/services/user_benchmark_percentiles_service.py | app.services.global_percentile_cdf.interpolate_cohort_percentile | sync call with cohort.get((metric, rounded_anchor, tc)) prefetched table | WIRED | interpolate_cohort_percentile(value, table_a) and (value, table_b) in loops at lines 435, 522 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| app/repositories/benchmark_cohort_cdf_repository.py | rows via BenchmarkCohortCdf SELECT | benchmark_cohort_cdf table (122,661 rows in dev DB) | Yes | FLOWING — live query confirmed; spot-check: ('score_gap', 850, 'blitz') returns CdfTable with breakpoints[0]=-0.2498 |
| app/services/user_benchmark_percentiles_service.py | cohort_a/cohort_b dict | load_cohort_cells batched prefetch | Yes | FLOWING — cohort dict populated before loops; interpolate_cohort_percentile receives real CdfTable |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| load_cohort_cells returns real CdfTable from DB | Python: load_cohort_cells(session, [850], ['blitz']) | 4 cells returned, ('score_gap',850,'blitz') breakpoints[0]=-0.2498 | PASS |
| interpolate_cohort_percentile is sync and correct | Python: interpolate_cohort_percentile(p50_val, table) where p50_val from DB | 50.0 (exact p50) | PASS |
| DB row count | SELECT COUNT(*) FROM benchmark_cohort_cdf | 122,661 | PASS |
| TSV header and row count | Python TSV check | header 7-col correct, 122,661 data rows | PASS |
| Generator emitter shape | Python import check | GEN OK (has _write_seed_tsv, no _write_module) | PASS |
| Module shape | Python inspect | MODULE OK lines=215, params=['value','table'], sync=True | PASS |
| Composite PK | Python Base.metadata check | PK OK ['metric', 'anchor_elo', 'tc', 'percentile'] | PASS |

### Probe Execution

Phase has no conventional probe scripts. Step 7b/7c: SKIPPED (no probe-*.sh declared; behavioral spot-checks above cover the same ground).

### Requirements Coverage

No ROADMAP requirement IDs — this is a standalone internal refactor. Coverage via phase Success Criteria (SC#1–SC#6) and CONTEXT decisions D-01..D-04, all verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| app/services/user_benchmark_percentiles_service.py | 145 | `# COHORT_PERCENTILE_CDF outer key` comment | INFO | Historical comment only; not live code. No action needed. |

No TBD/FIXME/XXX/HACK/PLACEHOLDER markers found in phase-modified files. No stub returns. No hardcoded empty data flowing to render paths.

**Pre-existing test failure (unrelated to phase):** `tests/test_integration_routers.py::TestEndgameEloTimelineRouter::test_endgame_overview_elo_timeline_respects_filters` fails in the current branch. This test is in a file not modified by Phase 99.1 (last modified in Phase 92, commit e061a1a4). The test depends on `seeded_user` fixture and DB state; none of the Phase 99.1-modified files touch endgame ELO timeline logic. This is a pre-existing flaky/data-state-dependent test, not a Phase 99.1 regression. The SUMMARY-04 reported "2199 passed / 16 skipped" — the count difference vs current run is explained by the branch predating Phase 99's 476+ new tests that landed on main.

### Human Verification Required

None. All must-haves are verifiable programmatically:

- Parity (SC#6) was proved by the executor via diff of pre/post JSON snapshots (33 entries, user 14, identical), and confirmed by spot-check in this verification (TSV breakpoints round-trip, p50 interpolation = 50.0).
- The only human-gated item in PLAN-04 (Task 5 checkpoint) was already approved by the user before the phase was submitted for verification.

### Gaps Summary

None. All 13 truths verified. The phase goal is fully achieved.

---

_Verified: 2026-05-31T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
