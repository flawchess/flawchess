---
phase: 99.1-move-cohort-cdf-out-of-source-into-a-db-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/benchmark_cohort_cdf.py
  - app/models/__init__.py
  - alembic/env.py
  - alembic/versions/
autonomous: true
requirements: []   # standalone internal refactor — no ROADMAP requirement IDs; coverage via phase Success Criteria + CONTEXT D-01
user_setup: []

must_haves:
  truths:
    - "A benchmark_cohort_cdf table exists in the dev DB after alembic upgrade head"
    - "The new model is discoverable by Alembic autogenerate (reaches Base.metadata)"
    - "The migration is schema-only — it contains no data INSERT statements"
    - "The migration round-trips (downgrade -1 then upgrade head succeeds)"
  artifacts:
    - path: "app/models/benchmark_cohort_cdf.py"
      provides: "BenchmarkCohortCdf SQLAlchemy model, composite PK (metric, anchor_elo, tc, percentile)"
      contains: "class BenchmarkCohortCdf"
    - path: "alembic/versions/"
      provides: "schema-only create_table migration for benchmark_cohort_cdf"
      contains: "op.create_table('benchmark_cohort_cdf'"
  key_links:
    - from: "alembic/env.py"
      to: "app.models.benchmark_cohort_cdf.BenchmarkCohortCdf"
      via: "explicit import with # noqa: F401 so the model reaches Base.metadata for autogenerate"
      pattern: "from app.models.benchmark_cohort_cdf import"
---

<objective>
Create the `benchmark_cohort_cdf` SQLAlchemy model (CONTEXT D-01: LONG layout, one
row per breakpoint, composite PK `(metric, anchor_elo, tc, percentile)`) and a
schema-only Alembic migration that creates the table. Register the model so Alembic
autogenerate and `Base.metadata` see it.

This is the foundation. Nothing else in the phase can seed or query the table until
it exists. SCHEMA ONLY — no data INSERTs in the migration (seeding is Plan 03's job).

Purpose: Relocate the 130k-line in-source CDF registry into a SQL-analyzable table.
Output: New model file + schema-only migration; dev DB gains an empty
`benchmark_cohort_cdf` table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-CONTEXT.md
@.planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the BenchmarkCohortCdf model</name>
  <files>app/models/benchmark_cohort_cdf.py, app/models/__init__.py</files>
  <read_first>
    - app/models/benchmark_cohort_cdf.py (target — does not exist yet)
    - app/models/user_rating_anchors.py (composite-PK + shared TC enum analog; lines 56-115)
    - app/models/opening.py (terse model with SmallInteger/column-type discipline)
    - app/models/game.py lines 33-41 (the shared time_control_bucket_enum, create_type=False)
    - app/models/user_benchmark_percentile.py lines 45-95 (the benchmark_metric_enum / SAEnum reuse precedent + CdfMetricId mirror note)
    - app/services/global_percentile_cdf.py lines 101-115 (the 11 CdfMetricId values — single source of truth)
    - app/models/__init__.py (registration target)
    - .planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md §"app/models/benchmark_cohort_cdf.py"
  </read_first>
  <action>
    Create `app/models/benchmark_cohort_cdf.py` with class `BenchmarkCohortCdf(Base)`,
    `__tablename__ = "benchmark_cohort_cdf"`. Columns per CONTEXT D-01 with project
    "appropriate column types" discipline:
      - `metric`: REUSE the existing `benchmark_metric` Postgres ENUM (import the
        `benchmark_metric_enum` SAEnum from `app/models/user_benchmark_percentile.py`,
        which is `create_type=False`). RESOLUTION of the PATTERNS open item: reuse the
        ENUM, not plain String, for consistency with `user_benchmark_percentiles.metric`
        and because the 11 CdfMetricId family names are exactly the ENUM's bare family
        values (the migration 52c928794fe7 added `conversion_rate`/`parity_rate`/
        `recovery_rate`). The `tc`-suffix is NOT used here — TC lives in its own column.
        Type the column `Mapped[CdfMetricId]`, import `CdfMetricId` from
        `app.services.global_percentile_cdf`. primary_key=True.
      - `anchor_elo`: `SmallInteger`, primary_key=True (800..2600 grid, fits i16).
      - `tc`: REUSE `time_control_bucket_enum` from `app.models.game` (create_type=False,
        exactly as user_rating_anchors does — do NOT declare a second enum). Type
        `Mapped[TimeControlBucket]` (import the Literal from
        `app.models.user_rating_anchors` or `app.services.global_percentile_cdf`).
        primary_key=True.
      - `percentile`: `SmallInteger`, primary_key=True (1..99 per BREAKPOINT_PERCENTILES).
      - `value`: `Double` (double precision; 0-1 score-gap float, matches
        `CdfTable.breakpoints` element type). nullable=False.
      - `n_users`: `Integer`, nullable=True (audit column — Discretion resolution: NULLable).
      - `snapshot_month`: `String(length=7)` (e.g. "2026-05"), nullable=True (plain audit
        column, NOT idempotency-relevant per notes #1 — Discretion resolution: NULLable).
    The composite PK on `(metric, anchor_elo, tc, percentile)` is enforced via
    `primary_key=True` on those four columns (no separate __table_args__ PK needed). Add a
    module docstring summarizing the LONG layout, the cell-reconstruction note (99 rows per
    (metric, anchor_elo, tc) ORDER BY percentile rebuild one CdfTable), and the audit-column
    purpose. Use `from __future__ import annotations`. Export via `__all__`.
    Then add `from app.models.benchmark_cohort_cdf import BenchmarkCohortCdf` to
    `app/models/__init__.py` and append `"BenchmarkCohortCdf"` to its `__all__`.
  </action>
  <verify>
    <automated>uv run python -c "from app.models.benchmark_cohort_cdf import BenchmarkCohortCdf; from app.models.base import Base; t=Base.metadata.tables['benchmark_cohort_cdf']; pk=[c.name for c in t.primary_key.columns]; assert pk==['metric','anchor_elo','tc','percentile'], pk; print('PK OK', pk)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import app.models.benchmark_cohort_cdf"` exits 0
    - The verify command prints `PK OK ['metric', 'anchor_elo', 'tc', 'percentile']`
    - `grep -c 'class BenchmarkCohortCdf' app/models/benchmark_cohort_cdf.py` returns 1
    - `grep -c 'SAEnum\|Enum(' app/models/benchmark_cohort_cdf.py` does NOT introduce a new
      enum with `create_type=True` (the file reuses existing enums; no new CREATE TYPE)
    - `uv run ty check app/ tests/` exits 0
  </acceptance_criteria>
  <done>Model exists, reaches Base.metadata with the correct composite PK, reuses both
  existing Postgres ENUMs (no new type), ty passes.</done>
</task>

<task type="auto">
  <name>Task 2: Register model in alembic env + generate schema-only migration</name>
  <files>alembic/env.py, alembic/versions/</files>
  <read_first>
    - alembic/env.py lines 10-34 (the `# noqa: F401` model-import block + target_metadata)
    - alembic/versions/20260530_220134_52c928794fe7_add_rate_family_names_to_benchmark_metric.py (current head — revision header block + how it references the existing benchmark_metric ENUM without recreating it)
    - app/models/user_rating_anchors.py lines 100-106 (how the shared timecontrolbucket enum is referenced, create_type=False)
    - .planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md §"alembic/versions/<rev>_add_benchmark_cohort_cdf.py"
  </read_first>
  <action>
    First add `from app.models.benchmark_cohort_cdf import BenchmarkCohortCdf  # noqa: F401`
    to the model-import block in `alembic/env.py` (mirroring the existing `Opening` /
    `LlmLog` imports). This is load-bearing: without it autogenerate may not see the table
    on a clean import (the existing benchmark models reach metadata transitively via
    `User`, but rely on that explicitly here to be safe).
    Then re-check the current head: `uv run alembic heads` (expected `52c928794fe7` at
    mapping time — use whatever it actually reports as `down_revision`). Run
    `uv run alembic revision --autogenerate -m "add benchmark_cohort_cdf"`. Hand-verify the
    generated `upgrade()` contains a single `op.create_table('benchmark_cohort_cdf', ...)`
    with columns metric (the existing benchmark_metric ENUM, referenced with
    create_type=False so it does NOT emit a `CREATE TYPE`), anchor_elo SmallInteger, tc
    (timecontrolbucket ENUM, create_type=False), percentile SmallInteger, value
    Double/double precision, n_users Integer nullable, snapshot_month String(7) nullable,
    and `sa.PrimaryKeyConstraint('metric','anchor_elo','tc','percentile')`. SCHEMA ONLY:
    remove any autogenerated noise unrelated to this table and assert there are ZERO
    `op.bulk_insert` / `op.execute("INSERT` data statements. Ensure `downgrade()` drops only
    `benchmark_cohort_cdf` (and does NOT drop the shared enums, since create_type=False
    means this migration doesn't own them).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml -p flawchess-dev up -d >/dev/null 2>&1; uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run python -c "import re,glob; latest=max(glob.glob('alembic/versions/*benchmark_cohort_cdf*.py')); s=open(latest).read(); assert 'op.create_table' in s and \"benchmark_cohort_cdf\" in s, 'create_table missing'; assert 'bulk_insert' not in s and 'INSERT' not in s.upper().replace('PRIMARYKEYCONSTRAINT',''), 'data statements present'; print('MIGRATION OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0 and creates table `benchmark_cohort_cdf`
    - `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips with exit 0
    - The generated migration file matches `alembic/versions/*benchmark_cohort_cdf*.py` and
      contains `op.create_table('benchmark_cohort_cdf'` and `PrimaryKeyConstraint('metric', 'anchor_elo', 'tc', 'percentile')`
    - The migration contains NO `op.bulk_insert` and NO `INSERT` data statement
    - `uv run alembic check` (or a fresh `--autogenerate` dry run) shows NO pending model
      drift for `benchmark_cohort_cdf` (model and migration agree)
  </acceptance_criteria>
  <done>Migration applied to dev DB, round-trips cleanly, is schema-only, model registered
  for autogenerate, table empty (seeded in Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → dev/prod DB schema | DDL executed by a trusted operator/entrypoint; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.1-01 | Denial of Service / data loss | Alembic migration drop on downgrade | mitigate | downgrade drops ONLY `benchmark_cohort_cdf`; create_type=False means it never drops the shared `benchmark_metric`/`timecontrolbucket` ENUMs that other tables depend on. Round-trip test (downgrade -1 / upgrade head) gates the task. |
| T-99.1-02 | Tampering | Migration carrying data INSERTs | mitigate | Schema-only invariant asserted in verify (no `bulk_insert`/`INSERT`). Data lives in the seed path (Plan 03), keeping the migration auditable and small. |
| T-99.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; no audit checkpoint required. |
</threat_model>

<verification>
- `uv run alembic upgrade head` then connect to dev DB and confirm `\d benchmark_cohort_cdf`
  shows the 7 columns and the 4-column composite PK.
- `uv run ty check app/ tests/` exits 0.
- `uv run ruff check app/ && uv run ruff format --check app/` clean for the new model file.
</verification>

<success_criteria>
- `benchmark_cohort_cdf` table created via schema-only migration; LONG layout per D-01.
- Model reaches `Base.metadata`; autogenerate detects no further drift.
- Migration round-trips; no data INSERTs.
</success_criteria>

<output>
Create `.planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-01-SUMMARY.md` when done.
</output>
