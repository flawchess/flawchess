---
phase: 114.1-move-count-to-ply-count
verified: 2026-06-10T22:45:00Z
status: passed
score: 6/6 must-haves verified
human_verification_outcome: "UAT-1 passed 2026-06-10 — game card 'N Moves' labels render byte-identically post-migration (see 114.1-UAT.md)."
overrides_applied: 0
human_verification:
  - test: "Open any game card (GameCard, LibraryGameCard, or FlawCard) in the browser and confirm the 'N Moves' label shows the same value as before the migration — e.g., a 40-move game had move_count=40, now has ply_count=80, and Math.ceil(80/2)=40 should still display '40 Moves'."
    expected: "The displayed full-move count is unchanged. The null-guard hides the label for the 127 position-less games in dev (ply_count=NULL)."
    why_human: "Exact byte-identical display requires a live browser against real imported game data; automated checks verify the math but not the rendered DOM against a real API response."
resolved_during_execution:
  - finding: "Stale chapter5.py render() template + docstrings described user_moves as a game_positions scan (lines 10, 132, 133, 230, 469). Future chapter5.compute() runs would have regenerated prose contradicting the ply_count basis."
    resolution: "Fixed in commits e437262c (render callout) and f579ee19 (docstrings + MATERIALIZED comments). render() now matches the corrected benchmarks-latest.md; chapter5 diff gate green; ruff clean."
---

# Phase 114.1: move-count-to-ply-count Verification Report

**Phase Goal:** Replace `games.move_count` (full-move count) with exact half-move count `games.ply_count` across the entire stack, so an exact per-game user-move denominator is available without a 190M-row `game_positions` scan. Display labels ("N Moves") must remain unchanged; benchmark §5 user_moves_per_game must read `ply_count` directly.

**Verified:** 2026-06-10T22:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `games.move_count` removed from model, schema, import path; `games.ply_count` exists as nullable Integer | VERIFIED | `app/models/game.py:120` has `ply_count: Mapped[int | None]`; dev DB schema: `ply_count INTEGER YES`, `move_count` absent; zero functional `move_count` refs in `app/` outside one explanatory comment |
| 2 | Migration adds `ply_count`, backfills via `max(ply) GROUP BY game_id`, drops `move_count`, with working `downgrade()` | VERIFIED | `alembic/versions/20260610_200128_07994baf3b15_replace_move_count_with_ply_count.py` confirmed; `down_revision = "d9e3f1a4b5c6"`; `alembic current` shows `07994baf3b15 (head)`; DB has 185,554/185,681 games with `ply_count` backfilled (127 NULL for position-less games) |
| 3 | `process_game_pgn` returns `ply_count = len(nodes)` (not `(len(nodes)+1)//2`); Stage 5 bulk UPDATE writes `ply_count` | VERIFIED | `zobrist.py:271` `ply_count = len(nodes)`; `import_service.py:765` `.values(ply_count=bindparam("b_pc"))`; test `test_process_game_pgn_ply_count` passes (3 half-moves for "1. e4 e5 2. Nf3 *"); `TestFlushBatchStage5::test_ply_count_lands_for_all_games` passes |
| 4 | Frontend: `plysToFullMoves` helper exists; all three cards derive label from `ply_count` via `Math.ceil`; null guard preserved; TS types renamed; zero `move_count` in `frontend/src` | VERIFIED | `frontend/src/lib/chess.ts` exports `plysToFullMoves`; imported + used in `GameCard.tsx`, `FlawCard.tsx`, `LibraryGameCard.tsx`; `null !== null` guard preserved on all three; `types/api.ts:111` and `types/library.ts:65,208` all have `ply_count: number | null`; `grep -rn "move_count" frontend/src/` = 0 results |
| 5 | `chapter5.py` `user_moves_per_game` reads `games.ply_count` via `FLOOR/CEIL` join, zero `game_positions` scan in that CTE | VERIFIED | Lines 243-251: CTE uses `JOIN games g ON g.id = bg.game_id` with `FLOOR(g.ply_count / 2.0)` / `CEIL(g.ply_count / 2.0)`; `grep "FROM game_positions\|JOIN game_positions" chapter5.py` = 0 results (6 remaining references are all in comments/docstrings) |
| 6 | Test fixtures use `ply_count` with half-move values (2x old `move_count`); green suite plausible | VERIFIED | Backend test fixtures confirmed: `test_zobrist.py` asserts `result["ply_count"] == 3`; `test_import_service.py` uses `ply_count` throughout; `tests/services/test_import_service.py` returns `ply_count: 4` and `ply_count: 1`; frontend fixtures: `useLibraryGame.test.tsx: ply_count: 80`, `GameCard.test.tsx: ply_count: 80`, `FlawCard.test.tsx: ply_count: 84/60/null`; zero `move_count` in all test files |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `alembic/versions/20260610_200128_07994baf3b15_replace_move_count_with_ply_count.py` | Single migration: add nullable + backfill + drop + downgrade | VERIFIED | File exists; `down_revision = "d9e3f1a4b5c6"`; `upgrade()` has 3 steps; `downgrade()` reverses correctly |
| `app/models/game.py` | `ply_count: Mapped[int | None]`; `move_count` removed | VERIFIED | Line 120 confirmed; dev DB schema confirmed |
| `app/services/zobrist.py` | `ply_count = len(nodes)` return | VERIFIED | Line 271-276 confirmed |
| `app/schemas/openings.py` | `GameRecord.ply_count: int | None` | VERIFIED | Line 114 confirmed |
| `app/schemas/library.py` | Two `ply_count: int | None` fields | VERIFIED | Lines 86 and 145 confirmed |
| `frontend/src/lib/chess.ts` | `plysToFullMoves(plyCount)` helper with `Math.ceil` | VERIFIED | File exists; function confirmed |
| `frontend/src/types/api.ts` | `ply_count: number | null` | VERIFIED | Line 111 confirmed |
| `scripts/benchmarks/chapter5.py` | `user_moves_per_game` CTE reads `games.ply_count` | VERIFIED | Lines 243-251 confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `import_service.py` | `models/game.py games.ply_count` | `Stage 5 .values(ply_count=bindparam("b_pc"))` | WIRED | Line 765 confirmed; test passes |
| `import_service.py` | `zobrist.py GameProcessingResult` | `processing_result["ply_count"]` accumulation | WIRED | Line 860 confirmed |
| `library_service.py` | `models/game.py games.ply_count` | `ply_count=game.ply_count` | WIRED | Line 379 confirmed |
| `GameCard.tsx` | `frontend/src/lib/chess.ts plysToFullMoves` | import + call on `game.ply_count` | WIRED | Line 10 import + line 161 call confirmed |
| `FlawCard.tsx` | `frontend/src/lib/chess.ts plysToFullMoves` | import + call on `flaw.ply_count` | WIRED | Line 30 import + line 230 call confirmed |
| `LibraryGameCard.tsx` | `frontend/src/lib/chess.ts plysToFullMoves` | import + call on `game.ply_count` | WIRED | Line 22 import + line 352 call confirmed |
| `chapter5.py` | `games.ply_count` | `FLOOR/CEIL(g.ply_count/2.0)` JOIN | WIRED | Lines 243-251 confirmed |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `GameCard.tsx` `plysToFullMoves(game.ply_count)` | `game.ply_count` | `openings_service.py:243` reads `g.ply_count` from `games` table (DB-backed) | Yes — DB query confirmed | FLOWING |
| `chapter5.py` `user_moves_per_game` | `g.ply_count` | `JOIN games g` with `games.ply_count` column (verified in DB, backfilled) | Yes — backfilled from `max(ply)`, not static | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `process_game_pgn` returns `ply_count = len(nodes) = 3` for 3-half-move PGN | `uv run pytest tests/test_zobrist.py::test_process_game_pgn_ply_count -v` | 1 passed | PASS |
| Stage 5 issues bulk UPDATE for `ply_count` | `uv run pytest tests/test_import_service.py::TestFlushBatchStage5::test_ply_count_lands_for_all_games -v` | 1 passed | PASS |
| Real DB Stage 5 round-trip | `uv run pytest tests/test_import_service.py::TestFlushBatchStage5RealDb -v` | 2 passed | PASS |
| Dev DB schema has `ply_count` nullable, no `move_count` | Docker `psql` query on `information_schema.columns` | `ply_count INTEGER YES` only | PASS |
| Alembic current = `07994baf3b15 (head)` | `uv run alembic current` | `07994baf3b15 (head)` | PASS |
| `plysToFullMoves` imported in all three card components | `grep -l "plysToFullMoves" GameCard.tsx FlawCard.tsx LibraryGameCard.tsx \| wc -l` | 6 (3 import + 3 call lines) | PASS |

### Requirements Coverage

No requirement IDs declared in PLAN frontmatter (`requirements: []`). No 114.1-tagged entries in `.planning/REQUIREMENTS.md`. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/benchmarks/chapter5.py` | 10, 133, 469 | Stale documentation — module docstring (line 10) and `_per_user_cte` docstring (line 133) say "count of game_positions rows"; `render()` template (line 469) produces report text saying "ply-parity counting from game_positions" | Info | No functional impact; computation is correct; but future `chapter5.compute()` runs will produce reports with the stale D-01 description unless `render()` is updated. The saved `benchmarks-latest.md` was hand-edited correctly, but the template is still stale. |

### Human Verification Required

#### 1. Game Card Display Byte-Identical Check

**Test:** Import or view existing games in the browser. Open an opening position or library game and inspect the "N Moves" chip on GameCard, LibraryGameCard, or FlawCard.

**Expected:** A 40-move game (old `move_count=40`, now `ply_count=80`) displays "40 Moves". A 41-move game (`ply_count=82`) displays "41 Moves". Position-less games show no move count chip.

**Why human:** The math is verified (`Math.ceil(ply_count/2)`), but byte-identical display against live API data requires a browser check.

#### 2. chapter5.py render() Template Stale Text Decision

**Test:** Review `scripts/benchmarks/chapter5.py` line 469. The `render()` function produces the D-01 callout paragraph in benchmark reports. It currently says: `"> render layer (\"pp\" unit). user_moves uses ply-parity counting from game_positions"` — this text will appear in any future `chapter5.compute()` regeneration.

**Expected:** Either (a) update `render()` line 469 to say "user_moves derived from `games.ply_count` (Phase 114.1): FLOOR(ply_count/2) for white, CEIL(ply_count/2) for black" and re-run `chapter5.compute()` to regenerate `benchmarks-latest.md`, or (b) accept the current saved `benchmarks-latest.md` as correct (it was hand-edited to the right text) and leave `render()` as-is until the next full benchmark regeneration.

**Why human:** This is a documentation quality judgment — the stale `render()` template text does not affect correctness but will mislead readers of future regenerated reports. A human should decide whether this warrants a fix before closing the phase.

### Gaps Summary

No functional gaps found. All 6 observable truths are verified against the live codebase and dev database. The two human verification items are:

1. A browser spot-check for display byte-identity (routine end-of-phase UAT).
2. A decision on whether to fix the stale `render()` template text in `chapter5.py` before the next benchmark regeneration.

Neither item blocks the phase goal — the schema migration, import path, API schemas, frontend types, and benchmark computation are all correctly implemented.

---

_Verified: 2026-06-10T22:45:00Z_
_Verifier: Claude (gsd-verifier)_
