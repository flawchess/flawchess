---
phase: 113-opponent-flaw-materialization
plan: 03
type: execute
wave: 3
depends_on: ["113-01", "113-02"]
files_modified:
  - tests/test_flaws_materialization.py
autonomous: false
requirements: [FLAWX-04]
tags: [flaws, backfill, benchmark, data-migration]

must_haves:
  truths:
    - "Dev users 28 & 44 game_flaws are repopulated with BOTH sides via an idempotent wipe+recompute, with no code change to backfill_flaws.py beyond the Plan-01 kernel (D-09)"
    - "After dev backfill, the gated self-only Library reads still match their pre-phase player-only counts (no-regression carried end-to-end), while ungated reads show both sides"
    - "The benchmark cohort backfill is a HUMAN-UAT long run that does NOT gate phase completion (D-09); prod game_flaws stays empty (no prod backfill)"
  artifacts:
    - path: "tests/test_flaws_materialization.py"
      provides: "post-backfill verification assertions (ungated ~2x, gated == baseline) if not already covered by Plan 01/02 fixtures"
  key_links:
    - from: "scripts/backfill_flaws.py"
      to: "app/services/flaws_service.py classify_game_flaws"
      via: "D-10 single classify path — kernel change propagates, no script edit"
      pattern: "classify_game_flaws"
    - from: "benchmark game_flaws (both sides)"
      to: "phase 114 benchmark delta-zone computation"
      via: "HUMAN-UAT backfill hand-off (D-08)"
      pattern: "backfill_flaws.py --db benchmark"
---

<objective>
Run the idempotent wipe+recompute backfill so existing analyzed games carry BOTH sides' flaws (FLAWX-04). Per D-09, `scripts/backfill_flaws.py` needs NO code change beyond the Plan-01 kernel — it already uses `delete_flaws_for_game` + `bulk_insert_game_flaws` (ON CONFLICT DO NOTHING) batched at `BACKFILL_GAMES_PER_BATCH = 100`, and the kernel change propagates via the D-10 single classify path.

Scope (D-08): dev users 28 & 44 (normal execution/UAT step) AND the benchmark cohort (HUMAN-UAT long run — phase-114 hand-off). Prod `game_flaws` stays EMPTY (no prod backfill). Do NOT use `bin/reset_db.sh` (project rule) — the backfill's per-game `delete_flaws_for_game` IS the intended idempotent recompute path against the existing dev DB.

Purpose: Materialize the both-sides data so the no-regression invariant is proven against real dev data, and so the benchmark DB is populated before phase 114 reads it.

Output: Repopulated dev `game_flaws` (both sides), an end-to-end no-regression verification, and a flagged HUMAN-UAT benchmark backfill step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-opponent-flaw-materialization/113-CONTEXT.md
@.planning/phases/113-opponent-flaw-materialization/113-RESEARCH.md
@.planning/phases/113-opponent-flaw-materialization/113-VALIDATION.md
@.planning/phases/113-opponent-flaw-materialization/113-01-SUMMARY.md
@.planning/phases/113-opponent-flaw-materialization/113-02-SUMMARY.md
@scripts/backfill_flaws.py
</context>

<artifacts_produced>
No new code symbols. This plan EXECUTES the existing `scripts/backfill_flaws.py` (unchanged per D-09) and verifies the result. Any new assertions land in the existing `tests/test_flaws_materialization.py`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-backfill baseline, run dev backfill (users 28 & 44), verify both-sides + no-regression</name>
  <files>tests/test_flaws_materialization.py</files>
  <read_first>
    - scripts/backfill_flaws.py (confirm `--db dev --user-id <N>`, `--dry-run`, `BACKFILL_GAMES_PER_BATCH = 100`, the `delete_flaws_for_game` + `bulk_insert_game_flaws` recompute loop — verify NO code change is needed per D-09)
    - 113-RESEARCH.md §Runtime State Inventory (dev users 28 & 44 = full wipe+recompute), §Pitfall 5 (no-regression baseline), §D-07/D-08/D-09
    - 113-02-SUMMARY.md (the gated readers + the no-regression baseline approach to reuse)
  </read_first>
  <action>
First, capture the pre-backfill player-only `game_flaws` row count for dev users 28 & 44 (a read-only `SELECT count(*)` via the dev DB / MCP query — record the baseline number in the SUMMARY). Optionally run `uv run python scripts/backfill_flaws.py --db dev --user-id 28 --dry-run` to confirm the plan. Then run `uv run python scripts/backfill_flaws.py --db dev --user-id 28` and `--user-id 44` (normal autonomous execution; the dev run is NOT the HUMAN-UAT step). After backfill, verify the ungated `game_flaws` count for those users is roughly double the captured baseline (both sides now present), and that the GATED self-only Library reads (reuse the Plan-02 gated query paths) still equal the pre-phase player-only baseline — the no-regression invariant carried end-to-end against real data. Confirm NO edit to `backfill_flaws.py` was required (D-09); if the script needed any change, flag it as a deviation. Do NOT run `bin/reset_db.sh`. If `tests/test_flaws_materialization.py` does not already cover the post-backfill ungated-doubles / gated-equals-baseline assertions from Plan 01/02 fixtures, add them here.
  </action>
  <verify>
    <automated>uv run pytest tests/test_flaws_materialization.py -x</automated>
  </verify>
  <done>Dev users 28 & 44 repopulated with both sides via idempotent wipe+recompute (no script edit); ungated count ~2x the captured baseline; gated self-only reads unchanged vs baseline; backfill is idempotent (re-running yields the same counts); tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Benchmark-cohort backfill (HUMAN-UAT, phase-114 hand-off — does NOT gate phase completion)</name>
  <files>scripts/backfill_flaws.py (executed, not edited)</files>
  <what-built>The kernel + gated readers + idempotent backfill are complete and verified on dev. The benchmark-cohort backfill is the phase-114 hand-off: a longer run that populates the benchmark `game_flaws` with both sides. Per CONTEXT D-09 this is a HUMAN-UAT step and MUST NOT gate phase completion on the unattended long run.</what-built>
  <action>
This is a HUMAN-UAT checkpoint, not an autonomous code task. Claude prepares and (optionally) kicks off the benchmark backfill, then pauses for human verification. Do NOT edit `backfill_flaws.py`. Do NOT run any prod backfill. Steps are in `<how-to-verify>`.
  </action>
  <how-to-verify>
    1. Ensure the benchmark DB is running: `bin/benchmark_db.sh start`.
    2. Run the benchmark backfill (long run): `uv run python scripts/backfill_flaws.py --db benchmark` (optionally `--dry-run` first to estimate scope).
    3. Spot-check a sample of benchmark games show opponent-ply rows now exist (e.g. query `game_flaws` for a known game_id and confirm rows at both even and odd plies for the relevant user_color), and that the row volume per game roughly doubled (~2-10 vs ~1-5, RESEARCH D-08).
    4. Confirm prod `game_flaws` was NOT backfilled (prod stays empty — FLAWX-04).
  </how-to-verify>
  <verify>
    <human-check>Benchmark `game_flaws` shows opponent-ply rows for sampled games (row volume roughly doubled); prod confirmed untouched. This HUMAN-UAT step does NOT gate phase completion (D-09).</human-check>
  </verify>
  <done>Benchmark backfill has run (or is explicitly deferred to before phase 114); sampled benchmark games show both-sides rows; prod stays empty. Phase completion is NOT blocked on the unattended long run finishing.</done>
  <resume-signal>Type "approved" once the benchmark backfill has run (or is intentionally deferred to before phase 114), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill recompute -> dev/benchmark game_flaws | Wipe+recompute over already-imported, already-evaluated games. No external/untrusted input. Prod is explicitly out of scope (stays empty). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-06 | Denial of Service | backfill memory pressure (prod OOM history) | accept | Backfill runs against dev/benchmark only, never prod (FLAWX-04 — prod stays empty). `BACKFILL_GAMES_PER_BATCH = 100` is the existing OOM-safe batching; row volume only roughly doubles (~2-10/game, RESEARCH D-08 "still tiny, OOM-safe"). No prod exposure, so the documented prod-OOM risk does not apply this phase. |
| T-113-07 | Tampering | data integrity of recompute | mitigate | `delete_flaws_for_game` + `bulk_insert_game_flaws` (ON CONFLICT DO NOTHING) is already idempotent and user+game-scoped; re-running yields identical counts (verified in Task 1). PK (user_id, game_id, ply) prevents player/opponent collision. |
| T-113-08 | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |

No `high`-severity threats.
</threat_model>

<verification>
- `uv run pytest tests/test_flaws_materialization.py -x` green after dev backfill.
- Wave merge: `uv run pytest -n auto -x` green (full suite).
- Idempotency: re-running the dev backfill yields identical row counts.
- HUMAN-UAT: benchmark spot-check shows opponent-ply rows (does NOT gate phase completion).
</verification>

<success_criteria>
- FLAWX-04: dev users 28 & 44 repopulated with both sides, idempotent + batched, no `backfill_flaws.py` code change (D-09); prod stays empty.
- End-to-end no-regression: gated self-only reads on real dev data equal the pre-phase baseline; ungated reads show both sides.
- Benchmark cohort backfill flagged as HUMAN-UAT (phase-114 hand-off), not a phase-completion gate.
</success_criteria>

<output>
Create `.planning/phases/113-opponent-flaw-materialization/113-03-SUMMARY.md` when done. Record: the captured pre-backfill baseline counts for users 28 & 44, the post-backfill counts (ungated ~2x, gated == baseline), idempotency confirmation, and the benchmark-backfill HUMAN-UAT status (run / deferred to before phase 114).
</output>
