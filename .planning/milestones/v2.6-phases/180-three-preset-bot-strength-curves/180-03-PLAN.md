---
phase: 180-three-preset-bot-strength-curves
plan: 03
type: execute
wave: 2
depends_on:
  - 180-01
files_modified:
  - scripts/calibration-harness.mjs
  - scripts/lib/calibration-near-free-metrics.check.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "the harness windows/picks/brackets anchors by INTERNAL_RATING via internalRatingFor (from the bot-cell schedule module), NOT by nominal bot_elo via anchorRatingFor (fixes the 2026-07-12 clamp bug)"
    - "DEFAULT_ANCHOR_TOKENS is exactly the 10 measured labels (maia700/1100/1500/1900/2300 + sf0/3/5/8/10) so both anchor families fire in every measured cell (D-07, both families)"
    - "each cell runs a locate pass (~8 games vs 2 widely-spaced anchors) then a measure pass vs the 3-4 bracketing anchors (D-07 two-pass); the D-15 static-window + dynamic-cutoff path is superseded (kept in-file, not wired into this cell loop)"
    - "a cell beyond the ladder edge emits a beyond_ladder=true row (warn-and-proceed), never throws (Pitfall 4)"
    - "--resume replays a raw per-game ledger (mirroring calibration-anchor-ladder.mjs) so variable-length two-pass cells resume without corrupting downstream gameIndex/seed assignment (Pitfall 5)"
    - "near-free metric columns (draw rate, game length, ACPL, blunder rate, SF-agreement, Maia-agreement) populate per cell; a fabricated-provider .check.mjs proves the onPly extension"
  artifacts:
    - scripts/calibration-harness.mjs
    - scripts/lib/calibration-near-free-metrics.check.mjs
  key_links:
    - "calibration-harness.mjs imports internalRatingFor/pickLocateAnchors/locateEstimate/selectMeasureBracket/bracketBeyondLadder from scripts/lib/calibration-bot-cell-schedule.mjs (Plan 01)"
    - "the emitted TSV is the input contract for calibration_anchor_fit.py's load_bot_cells (Plan 02) — both anchor families + near-free columns must be present"
---

<objective>
Integrate the internal-scale two-pass machinery into `scripts/calibration-harness.mjs`: swap the nominal-scale anchor accessor for the schedule module's `internalRatingFor`, restrict the default anchors to the 10 measured labels (enabling both families), wire the locate-then-measure two-pass into the cell loop (superseding the D-15 static-window path per RESEARCH open-Q1), flag beyond-ladder cells, adapt `--resume` to raw-ledger-replay, and surface the near-free metrics. This is the load-bearing harness fix (the bug that clamped every cell on 2026-07-12).

Purpose: Make the harness measure bot cells on the correct (internal) axis against both anchor families, so the emitted TSV feeds a real (non-clamped) fit.
Output: Modified harness + a new near-free-metrics check.

**Planner decision on RESEARCH open-Q1 (D-15 supersede):** The two-pass locate/bracket module is the SOLE cell-level anchor-selection mechanism for this run. `partitionAnchorsByWindow`/`orderAnchorsForDynamicCutoff`/`ANCHOR_ELO_WINDOW` are NOT deleted (future non-two-pass uses may want them) but are NOT wired into this phase's cell loop — surface this explicitly in a comment at the retirement site rather than silently dropping D-15 code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-three-preset-bot-strength-curves/180-CONTEXT.md
@.planning/phases/180-three-preset-bot-strength-curves/180-RESEARCH.md
@.planning/phases/180-three-preset-bot-strength-curves/180-PATTERNS.md
@scripts/lib/calibration-bot-cell-schedule.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Internal-scale integration — 10-anchor default, both families, internalRatingFor, supersede D-15</name>
  <files>scripts/calibration-harness.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (targeted: imports ~65-86; DEFAULT_ANCHOR_TOKENS + DEFAULT_BOT_ELOS/BLENDS ~152-172; summaryRowForCellGroup ~667-676; partitionAnchorsByWindow ~910-918; orderAnchorsForDynamicCutoff ~939-947; ANCHOR_ELO_WINDOW ~891)
    - scripts/lib/calibration-bot-cell-schedule.mjs (Plan 01 — the internalRatingFor export to import)
    - scripts/lib/calibration-internal-scale.mjs (the 10 measured labels)
    - 180-PATTERNS.md "calibration-harness.mjs — internalRatingFor swap" + "DEFAULT_ANCHOR_TOKENS restriction"; 180-RESEARCH.md Pattern 1/2, Open Question 1
  </read_first>
  <action>
    Import `{ internalRatingFor }` (and the two-pass functions used in Task 2) from `./lib/calibration-bot-cell-schedule.mjs`. Replace `DEFAULT_ANCHOR_TOKENS` (currently the Maia-every-200-ELO ladder + sf0/3/5) with exactly the 10 measured labels: maia700, maia1100, maia1500, maia1900, maia2300, sf0, sf3, sf5, sf8, sf10 — this is what enables both families (per RESEARCH Pattern 2, no new dispatch logic needed; parseAnchorSpec already handles the sf-N / maia-ELO token forms). Update the adjacent DEFAULT_BOT_ELOS/DEFAULT_BOT_BLENDS default comment; set DEFAULT_BOT_BLENDS to the three locked presets 0, 0.05, 0.5 (D-01/SEED-102), noting the operator passes per-preset --elo grids at runtime (D-04, see Plan 04).

    In summaryRowForCellGroup (~671) replace the anchorRatingFor lookup with internalRatingFor and add a bug-fix comment: the advisory combined estimate must be combined on the INTERNAL axis; per Pitfall 3 this advisory print may remain a single combined number — the real G_preset comes from the Python fit, not here.

    At partitionAnchorsByWindow / orderAnchorsForDynamicCutoff / ANCHOR_ELO_WINDOW: do NOT delete them; add a comment marking them superseded for the two-pass path (RESEARCH open-Q1 decision) and ensure the new cell loop (Task 2) does not call them. Leave their internals as-is.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs -e "import('./scripts/calibration-harness.mjs').then(()=>console.log('harness loads')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/calibration-harness.mjs` imports `internalRatingFor` from `./lib/calibration-bot-cell-schedule.mjs`.
    - `DEFAULT_ANCHOR_TOKENS` is exactly the 10 measured labels (grep confirms it contains `sf8` and `sf10`, and no `maia600`/`maia1300`/`maia2500`).
    - `summaryRowForCellGroup` calls `internalRatingFor`, not `anchorRatingFor`.
    - `partitionAnchorsByWindow` and `orderAnchorsForDynamicCutoff` still exist but carry a superseded-not-deleted comment.
    - The module still imports and evaluates without error.
  </acceptance_criteria>
  <done>Default anchors are the 10 measured labels, both families enabled, advisory estimate on the internal axis, D-15 explicitly retired-not-deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Two-pass locate to measure cell loop + beyond_ladder rows + raw-ledger-replay resume</name>
  <files>scripts/calibration-harness.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (targeted: main ~1086-1270; playCell ~1048-1085; cellKey ~752; loadPriorSweep ~825-864 incl. the games-per-cell guard ~845; buildSkipRow + SKIP_REASON_* ~903-950; mainTsvColumns/mainTsvRowLine ~560-620)
    - scripts/calibration-anchor-ladder.mjs (targeted: header comment + resume/ledger section — grep resume/ledger; openLedgerWriter ~207, playPairGames ~233, probePass ~313, measurePass ~463; the raw-per-game-ledger-replay resume model to mirror, per Pitfall 5)
    - scripts/lib/calibration-bot-cell-schedule.mjs (pickLocateAnchors/locateEstimate/selectMeasureBracket/bracketBeyondLadder/LOCATE_PASS_GAMES/DEFAULT_BRACKET_SIZE — Plan 01)
    - 180-RESEARCH.md Pattern 3, Pitfall 4, Pitfall 5, Open Question 2; 180-PATTERNS.md "--resume two-pass integrity" section
  </read_first>
  <action>
    Replace the cell loop's static-window anchor selection with the two-pass schedule. For each (bot_elo, bot_blend) cell in grid order: (1) LOCATE — play LOCATE_PASS_GAMES games against pickLocateAnchors(anchorSpecs) (weakest + strongest by internal rating), then locateEstimate(...) the rough internal rating; (2) BRACKET — selectMeasureBracket(anchorSpecs, estimate) picks the 3-4 nearest anchors with the cross-family floor; (3) MEASURE — play the remaining games-per-cell against each bracket anchor, REUSING the locate games as the first N vs any bracket anchor that was also a locate anchor (never replay — mirror the anchor-ladder info-efficiency). Emit one aggregated TSV row per (cell, anchor) via the existing mainTsvColumns/mainTsvRowLine.

    Add a beyond_ladder column to mainTsvColumns/mainTsvRowLine and set it from bracketBeyondLadder(estimate, bracket) on that cell's rows. Do NOT throw for beyond-ladder cells — they are real, imprecise measurements (Pitfall 4). Keep the existing buildSkipRow/SKIP_REASON row-not-silently-absent convention for any genuinely un-played anchor.

    Adapt --resume: the fixed games-per-cell assumption in loadPriorSweep (the row.games check that throws) breaks because two-pass cells consume variable game counts (locate + bracket-dependent measure). Follow calibration-anchor-ladder.mjs's raw-per-game-ledger pattern: write a durable raw per-game ledger (one row per completed game, both passes) and reconstruct progress on resume by replaying that ledger (fast-forward the seeded gameIndex through already-logged games so a resumed run is byte-identical), instead of validating a single global games-per-cell count. Keep functions small — extract locatePass/measureCellPass helpers rather than growing main() past the CLAUDE.md nesting/LOC limits.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs -e "import('./scripts/calibration-harness.mjs').then(m=>{const c=m.mainTsvColumns();if(!c.includes('beyond_ladder'))process.exit(1);console.log('beyond_ladder column present')})"</automated>
  </verify>
  <acceptance_criteria>
    - `mainTsvColumns()` includes a `beyond_ladder` column.
    - The cell loop calls `pickLocateAnchors`, `locateEstimate`, and `selectMeasureBracket` (grep confirms the three call sites) and does NOT call `partitionAnchorsByWindow`/`orderAnchorsForDynamicCutoff` in the two-pass path.
    - `--resume` no longer hard-throws on a variable per-cell game count: the resume path reads a raw per-game ledger (grep confirms a ledger writer/replay analogous to calibration-anchor-ladder.mjs) rather than the single `args.gamesPerCell` equality guard.
    - The harness still imports and evaluates without error.
  </acceptance_criteria>
  <done>Two-pass locate to measure wired as the sole cell-level selection, beyond_ladder surfaced, resume replays a raw ledger.</done>
</task>

<task type="auto">
  <name>Task 3: Near-free metric columns + fabricated-provider .check.mjs</name>
  <files>scripts/calibration-harness.mjs, scripts/lib/calibration-near-free-metrics.check.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (targeted: mainTsvColumns/mainTsvRowLine ~560-620; playCell/onPly hook ~1048-1085; the adjudication eval + bestmove byproduct path in the game loop)
    - scripts/lib/calibration-game-loop.mjs (playTwoMoverGame + the onPly hook signature; where post-move eval / adjudication bestmove is already available)
    - scripts/lib/calibration-providers.mjs (makeNodeProviders / adjudicationFallbackStats — the eval provider reused for ACPL/blunder-rate/SF-agreement at zero extra engine calls)
    - scripts/lib/calibration-anchors.mjs (maiaArgmaxMove — reuse for Maia-agreement, one cheap policy forward-pass per bot ply)
    - frontend/src/lib/liveFlaw.ts (BLUNDER_DROP/MISTAKE_DROP/INACCURACY_DROP + evalToExpectedScore — canonical severity thresholds, do not hand-roll)
    - scripts/lib/calibration-anchor-schedule.check.mjs (the fabricated-provider .check.mjs structure to mirror)
    - 180-CONTEXT.md "Also log near-free" list; 180-RESEARCH.md Summary near-free paragraph + Don't-Hand-Roll table
  </read_first>
  <action>
    Extend the per-ply capture (onPly hook) and TSV to surface the near-free metrics per cell (SEED-102): draw rate and game length (already computed — just aggregate and add columns); ACPL and blunder rate (reuse the existing per-ply adjudication eval — zero extra engine calls — classify eval drops with liveFlaw's BLUNDER_DROP/MISTAKE_DROP thresholds via evalToExpectedScore, do not hand-roll); SF-agreement rate (bot move == adjudication bestmove, reuse the existing bestmove byproduct); Maia-agreement rate (bot move == maiaArgmaxMove for the bot ply — one cheap policy forward-pass per bot ply). Add the corresponding columns to mainTsvColumns/mainTsvRowLine. Keep the onPly hook shallow; extract an accumulator helper if it grows.

    Create scripts/lib/calibration-near-free-metrics.check.mjs (engine-free) mirroring the calibration-anchor-schedule.check.mjs structure: feed a fabricated deterministic eval/policy provider (a canned eval sequence + canned argmax move, no real Maia/Stockfish) through the metric accumulator and assert each metric (draw rate, mean game length, ACPL, blunder rate, SF-agreement, Maia-agreement) computes to the expected value from the fixture. One PASS block per metric, process.exit(0) at the end.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-near-free-metrics.check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-near-free-metrics.check.mjs` prints a PASS line per metric and exits 0.
    - `mainTsvColumns()` includes columns for draw rate, game length, ACPL, blunder rate, SF-agreement, and Maia-agreement.
    - The metric code reuses liveFlaw severity thresholds and maiaArgmaxMove (grep confirms imports) rather than hand-rolled thresholds/argmax.
    - The check uses only fabricated providers (no real engine, no filesystem).
  </acceptance_criteria>
  <done>Six near-free metric columns populate from the onPly extension and are proven by an engine-free fabricated-provider check.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- **Modified** `scripts/calibration-harness.mjs`: imports `internalRatingFor`/`pickLocateAnchors`/`locateEstimate`/`selectMeasureBracket`/`bracketBeyondLadder` from the schedule module; `DEFAULT_ANCHOR_TOKENS` restricted to the 10 measured labels; `DEFAULT_BOT_BLENDS = [0, 0.05, 0.5]`; two-pass locate/measure cell loop (new `locatePass`/`measureCellPass` helpers); `beyond_ladder` + six near-free metric columns added to `mainTsvColumns`/`mainTsvRowLine`; raw-per-game-ledger `--resume`; D-15 functions retired-not-deleted with a superseded comment.
- **New file** `scripts/lib/calibration-near-free-metrics.check.mjs` (engine-free metric-accumulator test).
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local engine subprocesses -> harness | The harness spawns local Stockfish/ONNX-Maia subprocesses and reads their UCI/policy output; no network, no untrusted external input. |
| prior ledger TSV -> resume | `--resume` reads a developer-produced raw ledger from local disk. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-04 | Tampering | internalRatingFor on a malformed anchor token from --anchors | low | mitigate | Fail-loud throw (Plan 01) so a bad token cannot silently degrade to the nominal-scale bug (Pitfall 1). |
| T-180-05 | Tampering | --resume replaying a corrupted/mismatched ledger | low | mitigate | Ledger replay validates the seeded gameIndex/opening prefix on fast-forward (mirrors calibration-anchor-ladder.mjs); a mismatch surfaces rather than silently corrupting the rest of the sweep. |

No high-severity threats: local CLI over local engine subprocesses, no network exposure, no secrets, no persisted user data.
</threat_model>

<verification>
Harness imports and evaluates; `mainTsvColumns()` exposes `beyond_ladder` + the six near-free columns; the two-pass call sites are present and the D-15 functions are not called in the cell loop; the near-free `.check.mjs` exits 0.
</verification>

<success_criteria>
The harness selects anchors on the internal scale via the two-pass module against both families, flags beyond-ladder cells, resumes via a raw ledger, and surfaces the near-free metrics — all provable engine-free (module load + near-free check). Every source assertion in the tasks holds.
</success_criteria>

<output>
Create `.planning/phases/180-three-preset-bot-strength-curves/180-03-SUMMARY.md` when done.
</output>
