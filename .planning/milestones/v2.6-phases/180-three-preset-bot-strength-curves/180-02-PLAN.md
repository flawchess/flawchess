---
phase: 180-three-preset-bot-strength-curves
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/calibration_anchor_fit.py
  - tests/scripts/test_calibration_anchor_fit.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "fit_bot_cell_rating is a single-parameter MLE: iterates only the bot cell's strength while holding the 10 anchors fixed at their INTERNAL_RATING (D-06)"
    - "each bot cell is fit TWICE — once on vs-Maia rows, once on vs-SF rows — against the SAME fixed INTERNAL_RATING dict, never one combined fit (D-06, Pitfall 3)"
    - "G_preset = rating_vs_maia - rating_vs_sf is a direct output per cell, plus a per-preset combined G_preset scalar (open-Q3: report both) (D-06)"
    - "the bot-curves JSON mirrors reports/data/anchor-ladder-internal-scale.json shape with added rating_vs_maia/rating_vs_sf/g_preset/beyond_ladder fields and the INTERNAL-SCALE-NOT-human-ELO caveat header"
    - "pytest recovers a known synthetic ground-truth strength within tolerance and asserts input-validation throws (D-02a/D-06)"
  artifacts:
    - scripts/calibration_anchor_fit.py
    - tests/scripts/test_calibration_anchor_fit.py
  key_links:
    - "fit_bot_cell_rating reuses RATING_SCALE, DEFAULT_FIT_TOL, DEFAULT_FIT_MAX_ITER and the _clamp_win_counts continuity-correction discipline already in the file"
    - "the bot-curves JSON is the interface SEED-104 consumes (out of scope here) — it must carry per-preset per-bot_elo ratings + CIs + G_preset"
---

<objective>
Extend `scripts/calibration_anchor_fit.py` with a single-parameter pinned-anchor MLE (`fit_bot_cell_rating`), a bot-cell TSV loader, a two-fits-per-cell driver that emits `G_preset` directly, and a bot-curves JSON writer mirroring the anchor-ladder JSON. Add pytest coverage. This is the fit path (D-06) and is fully independent of the JS harness (it reads the harness's aggregated WDL TSV schema), so it runs in Wave 1 in parallel with Plan 01.

Purpose: Turn the harness's per-(cell,anchor) WDL counts into per-preset strength curves with CIs and the load-bearing cross-family gap `G_preset`, using the same proven Bradley-Terry math as Phase 173 (CIs for free, consistent method).
Output: Extended `calibration_anchor_fit.py` + extended pytest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-three-preset-bot-strength-curves/180-CONTEXT.md
@.planning/phases/180-three-preset-bot-strength-curves/180-RESEARCH.md
@.planning/phases/180-three-preset-bot-strength-curves/180-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fit_bot_cell_rating single-parameter MLE + two-fits-per-cell G_preset + bot-curves JSON</name>
  <files>scripts/calibration_anchor_fit.py</files>
  <behavior>
    - fit_bot_cell_rating recovers a strength that reproduces the true rating within tolerance when fed WDL counts generated at a known internal rating against the 10 fixed anchors.
    - A cell fit vs a mostly-Maia bracket and vs a mostly-SF bracket yields two ratings whose difference IS G_preset (not averaged away).
    - Empty games or a referenced anchor label missing from fixed_ratings raises (fail-loud), never returns NaN.
  </behavior>
  <read_first>
    - scripts/calibration_anchor_fit.py (FULL — extend in place; direct targets: fit_bradley_terry lines 156-194, _clamp_win_counts lines 124-153, build_win_counts lines 101-121, _write_outputs lines 345-378, main/argparse lines 398-461; reuse RATING_SCALE=400, DEFAULT_FIT_TOL, DEFAULT_FIT_MAX_ITER, SCORE_CLAMP_EPSILON_DIVISOR)
    - reports/data/anchor-ladder-internal-scale.json (the JSON envelope shape to mirror: _caveat header, internal_rating, confidence_intervals)
    - scripts/lib/calibration-internal-scale.mjs (the 10 fixed INTERNAL_RATING values the Python fit must hold constant — hardcode-import the same numbers, or read them; keep them in sync)
    - 180-RESEARCH.md Code Examples (fit_bot_cell_rating shape + G_preset), Pitfall 3, Open Question 3; 180-PATTERNS.md "calibration_anchor_fit.py" section
  </read_first>
  <action>
    Add `fit_bot_cell_rating(win_counts_vs_fixed, games_vs_fixed, fixed_ratings, tol=DEFAULT_FIT_TOL, max_iter=DEFAULT_FIT_MAX_ITER) -> float`: single free-parameter Zermelo/MM iteration (specialize fit_bradley_terry from N free anchors to 1 free bot strength against N FIXED opponents held at `fixed_ratings`). Apply the same continuity-correction clamp as `_clamp_win_counts` before iterating (reuse it directly if its signature generalizes to the 1-vs-N count dict, else mirror its epsilon formula `1/(SCORE_CLAMP_EPSILON_DIVISOR*games)`). Validate inputs up front and raise ValueError on: empty `games_vs_fixed`, or any anchor label in the counts absent from `fixed_ratings` (STRIDE Tampering mitigation — never fit garbage). Docstring warns to seed `strength=1.0` neutral, never from folklore ratings.

    Add `load_bot_cells(path)` reading the harness's aggregated per-(cell,anchor) TSV: parse each row's bot_elo, bot_blend, anchor_label, and WDL/games tally into per-cell win/games dicts split by anchor family (maia* vs sf*). Fail-loud on a missing expected column.

    Add a driver (e.g. `fit_all_bot_cells(cells, fixed_ratings)`): for each (bot_elo, bot_blend) cell, call `fit_bot_cell_rating` TWICE — once on the vs-Maia rows, once on the vs-SF rows, both against the FULL 10-anchor `fixed_ratings` — and compute `g_preset = rating_vs_maia - rating_vs_sf` per cell (Pitfall 3: never merge families before fitting). Carry the `beyond_ladder` flag through from the TSV row if present. Add a parametric-bootstrap CI (`bootstrap_bot_cell_ci`) that resamples the WDL counts per anchor (A3: aggregate counts are sufficient since anchors are fixed). Per open-Q3, also compute a per-preset combined `G_preset` scalar (inverse-CI-weighted mean of the preset's per-cell g_preset values, same weighting convention as combineAnchorEstimates).

    Add `_write_bot_curves(out_json, cells_fit)` mirroring `_write_outputs`'s JSON envelope: `_caveat` = the INTERNAL-SCALE-NOT-human-ELO header verbatim, plus per-cell entries carrying `bot_elo`, `bot_blend`, `rating_vs_maia`, `rating_vs_sf`, `g_preset`, CIs, and `beyond_ladder`, plus a `per_preset` block with the combined `G_preset` scalar per blend.

    Wire a NEW CLI path in `main`: add `--bot-input <tsv>` and `--out-bot-curves <json>` flags; when `--bot-input` is provided, run the bot-cell fit path (load_bot_cells -> fit_all_bot_cells -> _write_bot_curves) instead of the existing anchor-ladder joint fit. Leave the existing `--input`/`--out-js`/`--out-json` anchor-ladder path untouched. Reuse the file's fail-loud argparse discipline.
  </action>
  <verify>
    <automated>uv run python -c "import ast,sys; src=open('scripts/calibration_anchor_fit.py').read(); ast.parse(src); assert 'def fit_bot_cell_rating' in src and 'def _write_bot_curves' in src and '--bot-input' in src; print('structure OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/calibration_anchor_fit.py` defines `fit_bot_cell_rating`, `load_bot_cells`, a two-fits-per-cell driver, `bootstrap_bot_cell_ci`, and `_write_bot_curves`; parses clean (`ast.parse`).
    - The bot-cell path computes `g_preset` from two separate fits and never one combined/averaged rating (grep confirms two `fit_bot_cell_rating` call sites in the driver).
    - `main` accepts `--bot-input`/`--out-bot-curves`; the existing anchor-ladder `--input` path is unchanged.
    - `uv run ty check scripts/calibration_anchor_fit.py` (or the project's Python type gate over this file) reports zero new errors; stdlib-only (no numpy/scipy).
  </acceptance_criteria>
  <done>Fit extension parses, exposes the two-fit G_preset path and the bot-curves writer, keeps the anchor-ladder path intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pytest coverage for fit_bot_cell_rating + G_preset + input validation</name>
  <files>tests/scripts/test_calibration_anchor_fit.py</files>
  <behavior>
    - test_fit_bot_cell_rating_synthetic_ground_truth: fabricate fixed_ratings + WDL counts generated at a KNOWN true strength, assert the fit recovers it within a small tolerance.
    - test_g_preset_sign: on a fabricated asymmetric fixture (bot beats SF harder than Maia), assert g_preset has the expected sign/direction.
    - test_fit_bot_cell_rating_rejects_bad_input: empty games and a missing anchor label each raise.
  </behavior>
  <read_first>
    - tests/scripts/test_calibration_anchor_fit.py (FULL — the existing precedent; mirror its fixture-generation + tolerance-assertion style for fit_bradley_terry, per RESEARCH "173-03 RED-first pattern")
    - scripts/calibration_anchor_fit.py (the functions under test — from Task 1)
  </read_first>
  <action>
    Add the three tests above using synthetic fixtures (no real engines, no TSV files on disk — build the count dicts in-memory). For the ground-truth test, generate expected win counts from the Elo/BT expected-score formula at a chosen true rating against the 10 fixed anchors, then assert `fit_bot_cell_rating` returns that rating within tolerance (e.g. abs error < a few Elo points). For validation, use `pytest.raises(ValueError)`. Name tests so `-k fit_bot_cell_rating` and `-k g_preset` select them.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/test_calibration_anchor_fit.py -k "fit_bot_cell_rating or g_preset" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/scripts/test_calibration_anchor_fit.py -k "fit_bot_cell_rating or g_preset" -x` exits 0.
    - Reverting the input-validation guard in `fit_bot_cell_rating` makes `test_fit_bot_cell_rating_rejects_bad_input` fail (proves the test actually exercises the guard, not symbol-presence).
    - Tests use only in-memory fabricated counts (no engine, no on-disk TSV).
  </acceptance_criteria>
  <done>Three new pytest cases pass; the ground-truth recovery test and the validation test both meaningfully constrain the implementation.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- **Modified** `scripts/calibration_anchor_fit.py`: new `fit_bot_cell_rating(win_counts_vs_fixed, games_vs_fixed, fixed_ratings, tol, max_iter) -> float`, `load_bot_cells(path)`, `fit_all_bot_cells(cells, fixed_ratings)`, `bootstrap_bot_cell_ci(...)`, `_write_bot_curves(out_json, cells_fit)`; new CLI flags `--bot-input`/`--out-bot-curves`.
- **Output artifact (written by the operator sweep in Plan 04, shape defined here):** `reports/data/bot-curves-internal-scale.json` — per-cell `rating_vs_maia`/`rating_vs_sf`/`g_preset`/CIs/`beyond_ladder` + per-preset combined `G_preset`.
- **Modified** `tests/scripts/test_calibration_anchor_fit.py`: `test_fit_bot_cell_rating_synthetic_ground_truth`, `test_g_preset_sign`, `test_fit_bot_cell_rating_rejects_bad_input`.
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| TSV file -> fitter | The harness-emitted aggregated WDL TSV is read from local disk (developer-produced, not untrusted) and fitted into a published downstream artifact. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-02 | Tampering | fit_bot_cell_rating / load_bot_cells on a corrupted or mis-keyed TSV | medium | mitigate | Fail-loud input validation (empty games, missing anchor label / column) raises rather than producing NaN/garbage consumed downstream by SEED-104. |
| T-180-03 | Information Disclosure | bot-curves JSON mis-read downstream as human ELO | low | mitigate | Carry the INTERNAL-SCALE-NOT-human-ELO caveat verbatim in the JSON `_caveat` header (same pattern as anchor-ladder JSON). |

No high-severity threats: local stdlib CLI, no network, no secrets, no untrusted input.
</threat_model>

<verification>
`uv run pytest tests/scripts/test_calibration_anchor_fit.py -k "fit_bot_cell_rating or g_preset" -x` exits 0; `ast.parse` clean; two distinct `fit_bot_cell_rating` call sites in the driver; anchor-ladder path unchanged.
</verification>

<success_criteria>
The fitter produces two separate per-family ratings per cell and a direct `G_preset`, emits a caveated bot-curves JSON mirroring the anchor-ladder shape, and recovers a synthetic ground truth in pytest. Stdlib-only, zero new type errors.
</success_criteria>

<output>
Create `.planning/phases/180-three-preset-bot-strength-curves/180-02-SUMMARY.md` when done.
</output>
