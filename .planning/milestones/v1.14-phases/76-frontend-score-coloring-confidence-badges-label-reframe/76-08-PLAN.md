---
phase: 76
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
autonomous: false
requirements:
  - INSIGHT-UI-04
tags:
  - verify
  - requirements-amendment
  - changelog

must_haves:
  truths:
    - "INSIGHT-UI-04 is marked DESCOPED in REQUIREMENTS.md per D-04"
    - "Full backend + frontend test suite is green"
    - "ty check passes with zero errors"
    - "knip and tsc --noEmit are green on the frontend"
    - "Manual mobile QA at 375px viewport confirms no horizontal scroll on Move Explorer with the new Conf column"
    - "Manual tap-target QA on the four section-title InfoPopovers confirms ≥44px touch area"
    - "CHANGELOG.md gains an [Unreleased] entry referencing Phase 76 + descope of INSIGHT-UI-04"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "INSIGHT-UI-04 amendment record"
      contains: "DESCOPED"
    - path: "CHANGELOG.md"
      provides: "Phase 76 user-facing changelog entry"
      contains: "Phase 76"
  key_links: []
---

<objective>
Final-pass verification + REQUIREMENTS amendment + changelog entry. This plan blocks the merge:

1. Mark INSIGHT-UI-04 as DESCOPED in REQUIREMENTS.md (D-04 — severity word never appears as user-facing text; confidence badge + sort calibration carries the SEED-008 intent).
2. Run the full test/type/lint suite end-to-end (backend + frontend) and confirm green.
3. Perform the three manual mobile QA checks from VALIDATION.md.
4. Append a Phase 76 entry to `CHANGELOG.md` under `## [Unreleased]` per CLAUDE.md changelog policy.

Purpose: Ensure no requirements drift, no manual gap left unverified, and the Unreleased changelog reflects what shipped before merge.
Output: Updated REQUIREMENTS.md + CHANGELOG.md + a manual-QA approval checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-CONTEXT.md
@.planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-VALIDATION.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend + frontend suite end-to-end</name>
  <files></files>
  <read_first>
    - .planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-VALIDATION.md (Test Infrastructure → "Full suite command")
  </read_first>
  <action>
Run the full suite verbatim from VALIDATION.md:

```bash
uv run pytest && \
uv run ty check app/ tests/ && \
cd frontend && npm run lint && npm run knip && npx tsc --noEmit && npx vitest run
```

If any command exits non-zero, do NOT amend REQUIREMENTS.md or CHANGELOG.md. Triage the failure: read the failing test/output, identify which Phase 76 plan introduced the regression, fix at the root cause (or, if the failure is unrelated to Phase 76, document it in `STATE.md` Blockers/Concerns and proceed). Do not silence tests.

Run BOTH commands separately and capture exit codes:

```bash
uv run pytest -x  # exit 0 expected
uv run ty check app/ tests/  # exit 0 expected
```

Then in the frontend directory:

```bash
cd frontend
npm run lint  # exit 0 expected
npm run knip  # exit 0 expected — must catch any dead exports from D-15/D-20 cleanup
npx tsc --noEmit  # exit 0 expected — must be clean across the whole frontend after Wave 3
npx vitest run  # exit 0 expected
```
  </action>
  <verify>
    <automated>uv run pytest -x && uv run ty check app/ tests/ && cd frontend && npm run lint && npm run knip && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -x` exits 0
    - `uv run ty check app/ tests/` exits 0
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm run knip` exits 0
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx vitest run` exits 0
  </acceptance_criteria>
  <done>All gates green. The phase is technically mergeable.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS.md to mark INSIGHT-UI-04 as DESCOPED</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 24-65 — INSIGHT-UI section + phase mapping table)
    - .planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-CONTEXT.md (D-04 — descope rationale)
  </read_first>
  <action>
**Step A — mark INSIGHT-UI-04 as DESCOPED**. Locate the INSIGHT-UI-04 line (currently line 29 per the grep result in planning context). Update the bullet to reflect the descope:

Before:
```markdown
- [ ] **INSIGHT-UI-04**: Soften Opening Insights section titles and severity copy per SEED-008 — e.g. "Worth a closer look (White)" / "Played confidently (White)" instead of "White Opening Weaknesses / Strengths". Severity word ("Major" / "Minor") may stay inside the card but reads as a tone cue, not a verdict. Exact copy resolved during Phase 76 `/gsd-discuss-phase`; tone calibration follows `feedback_llm_prompt_design.md`.
```

After:
```markdown
- [x] **INSIGHT-UI-04** ~~Soften Opening Insights section titles and severity copy per SEED-008~~ — **DESCOPED 2026-04-28** per Phase 76 D-04. Rationale: severity word ("major" / "minor") never appears as user-facing text today (only drives border color). Once Phase 76 ships the confidence badge + `(confidence DESC, |score-0.5| DESC)` sort, the existing "Weakness/Strength" framing is justified — confidence + sort carry the SEED-008 calibration. Section titles stay unchanged.
```

The `[x]` checkbox marks it as resolved (not implemented, but accounted for). Strikethrough formatting on the original headline preserves audit trail.

**Step B — update the phase mapping table footer note** (currently around line 68 per grep). Append a Phase 76 amendment line:

```markdown
*Last updated: 2026-04-28 — milestone v1.14 opened. 14/14 requirements mapped (INSIGHT-SCORE-01..07 → Phase 75; INSIGHT-UI-01..07 → Phase 76). Phase 75 amendments: INSIGHT-SCORE-04 pivots from Wilson to trinomial Wald with half-width-based buckets (D-05); INSIGHT-SCORE-06 extends API contract to include `p_value` alongside `confidence` (D-09). Phase 76 amendment: INSIGHT-UI-04 descoped per D-04 — confidence badge + sort calibration covers SEED-008 intent.*
```

(Replace the existing trailing italic line. Read the file first to confirm exact wording — do not duplicate the line.)

**Step C — confirm no other INSIGHT-UI requirement IDs claim INSIGHT-UI-04 work**. Grep the phase plans:

```bash
grep -l "INSIGHT-UI-04" .planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-*-PLAN.md
```

INSIGHT-UI-04 should ONLY appear in this plan (76-08) under `requirements:`. The other plans must not claim it.
  </action>
  <verify>
    <automated>grep -q "DESCOPED 2026-04-28" .planning/REQUIREMENTS.md && grep -q "Phase 76 amendment: INSIGHT-UI-04 descoped" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "INSIGHT-UI-04.*DESCOPED" .planning/REQUIREMENTS.md` returns 0
    - `grep -q "D-04" .planning/REQUIREMENTS.md` returns 0
    - `grep -q "Phase 76 amendment: INSIGHT-UI-04 descoped" .planning/REQUIREMENTS.md` returns 0
    - The Phase mapping table still shows `INSIGHT-UI-04 | 76` (the requirement is still mapped to Phase 76 — descope is recorded as resolution, not unmap)
  </acceptance_criteria>
  <done>REQUIREMENTS.md records the descope per D-04 with audit trail.</done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 76 changelog entry under [Unreleased]</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — find `## [Unreleased]` section)
    - CLAUDE.md (Changelog & Releases section — per-phase rules: terse, user-facing, grouped Added/Changed/Fixed)
  </read_first>
  <action>
Append bullets to `## [Unreleased]` in `CHANGELOG.md`. Per CLAUDE.md guidance: terse, user-facing tone (what changed, not how). Group into `### Added` / `### Changed` / `### Fixed` subsections.

The change set for Phase 76 in user-facing terms:

```markdown
### Added
- Move Explorer: new "Conf" column showing low / med / high confidence per move so you can tell how well-sampled each next-move statistic is (Phase 76).
- Opening Insights cards: each card now shows a "Confidence: low / medium / high" line with a hover tooltip explaining how to read it (Phase 76).
- Opening Insights section titles: a small `?` icon next to each section title opens a popover explaining score, the 5%-from-pivot effect-size gate, and what confidence means (Phase 76).

### Changed
- Move Explorer arrows and row tints now reflect chess score (W + ½D)/N rather than separate win/loss rates. Color encoding stays effect-size only — arrows show how far from a 50% break-even your performance sits (Phase 76).
- Opening Insights card prose reframed from "You lose / win X%" to "You score X% as <colour> after <move>" — same form for both weakness and strength sections (Phase 76).
- Opening Insights cards within each section now sort by confidence first, then by distance from 50% — high-confidence findings rise to the top (Phase 76).
- Low-confidence (or low-game-count) Move Explorer rows and Opening Insights cards are visually muted at 50% opacity to flag treat-as-a-hint findings without hiding them (Phase 76).

### Fixed
- Opening Insights cards no longer break when the backend stops returning the (now removed) `loss_rate` / `win_rate` fields — cards read `score` directly per the Phase 75 contract (Phase 76).
```

If `## [Unreleased]` already has subsections (Added / Changed / Fixed), append to those. If it doesn't exist yet, create the section above the most recent versioned section.

Do NOT mention internal helper modules (`score_confidence.py`), test files, type interfaces, or migration notes — those are implementation details, not user-facing changes.
  </action>
  <verify>
    <automated>grep -A 30 "^## \[Unreleased\]" CHANGELOG.md | grep -q "Phase 76"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## \[Unreleased\]" CHANGELOG.md` returns 0
    - `grep -A 50 "^## \[Unreleased\]" CHANGELOG.md | grep -q 'Conf.*column'` returns 0
    - `grep -A 50 "^## \[Unreleased\]" CHANGELOG.md | grep -q "score X%"` returns 0
    - `grep -A 50 "^## \[Unreleased\]" CHANGELOG.md | grep -q 'Phase 76'` returns 0
    - `grep -c "Phase 76" CHANGELOG.md` returns >= 5 (multiple bullets reference Phase 76)
  </acceptance_criteria>
  <done>CHANGELOG.md has a Phase 76 entry under [Unreleased] grouped Added / Changed / Fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT): Manual mobile QA at 375px viewport</name>
  <files></files>
  <what-built>
    - Move Explorer with new "Conf" column (between Games and WDL)
    - Opening Insights cards with "Confidence: …" line and level-specific tooltip
    - Opening Insights section titles with four `?` icon InfoPopover triggers
    - Visual mute (opacity 0.5) on low-confidence and low-game-count rows/cards
  </what-built>
  <action>
This is a human-verify checkpoint — Claude does NOT execute code here. Present the `<how-to-verify>` instructions to the user, wait for the resume signal ("approved" or issue description), then proceed. If the user reports issues, do NOT continue to Plan 08 SUMMARY — instead, route the issue back to the relevant Wave 3 plan (05/06/07) for a fix and re-run Task 1 (full suite) before re-presenting this checkpoint.
  </action>
  <how-to-verify>
    1. Start the dev environment: `bin/run_local.sh` (backend + frontend).
    2. Open Chrome DevTools, set device emulation to iPhone SE (375 × 667).
    3. **Move Explorer Conf column**:
       - Navigate to Openings → Moves tab.
       - Verify all four columns (Move, Games, Conf, WDL bar) render WITHOUT horizontal scroll on the page.
       - Verify the Conf cell labels (`low` / `med` / `high`) are legible at `text-xs` font size.
       - Verify a low-confidence row (or one with `game_count < 10`) renders at ~50% opacity.
    4. **Opening Insights tap-friendly InfoPopover**:
       - Navigate to Openings → Insights tab.
       - Tap (single touch) the `?` icon next to "White Opening Weaknesses". Popover should open. Tap outside to dismiss. Tap the icon again — opens.
       - Repeat for the three other section icons (`Black Weaknesses`, `White Strengths`, `Black Strengths`). All four should open the SAME copy.
       - Verify each `?` icon's effective tap area is at least 44 × 44 px (CLAUDE.md frontend rule).
    5. **Card-level confidence indicator + tooltip**:
       - On the Insights tab, hover (or long-press on touch) the "Confidence: medium" line on a card.
       - Tooltip should appear with text "enough games to trust the direction".
       - Repeat for a `low` card ("small sample, treat as a hint") and a `high` card ("sample is large enough to trust the magnitude").
    6. **Visual mute merge with deep-link pulse**:
       - Bookmark a low-confidence position (n_games < 10), navigate to it via the bookmark deep link.
       - Verify the row pulse animation fires correctly OVER the muted (50% opacity) tint without one wiping the other.
    7. **Score-prose rounding edge case**:
       - Find or seed a finding with `score = 0.499` (weakness section, would round to 50%). Display should fall back to `49.9%` (one decimal place) rather than `50%`.
       - Sanity check: red-tinted cards should never display ≥50%; green-tinted cards should never display ≤50%.

    Expected outcome: all 7 checks pass. Approve this checkpoint to unblock merge.
  </how-to-verify>
  <verify>Manual — see how-to-verify steps. No automated command; this is a checkpoint:human-verify task.</verify>
  <done>User has approved (typed "approved") after running all 7 mobile QA checks. If issues found, they have been routed to the relevant plan and re-verified.</done>
  <resume-signal>Type "approved" once all 7 manual checks pass; describe issues if any fail.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries. This plan is documentation amendment + verification. The manual QA covers presentation behaviors that automated tests (jsdom) can't faithfully simulate (real touch event handling, real font metrics, real CSS compositing).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-08-01 | I (Information disclosure) | REQUIREMENTS.md amendment | accept | Public planning doc; no secrets. |
| T-76-08-02 | T (Tampering) | CHANGELOG.md | accept | Public release notes; no integrity-critical content. |

No high-severity threats. `block_on=high` gate satisfied.
</threat_model>

<verification>
1. Full backend + frontend suite green (Task 1).
2. REQUIREMENTS.md records the INSIGHT-UI-04 descope (Task 2).
3. CHANGELOG.md has Phase 76 entry under [Unreleased] (Task 3).
4. Manual mobile QA approved by user (Task 4 checkpoint).
</verification>

<success_criteria>
- All quality gates green.
- INSIGHT-UI-04 descope recorded with audit trail.
- CHANGELOG.md updated.
- Manual mobile QA passes for the seven concrete checks.
</success_criteria>

<output>
After completion, create `.planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-08-SUMMARY.md` per the SUMMARY template. Update STATE.md to mark Phase 76 as ready for verification.
</output>
