---
phase: 76
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/src/types/insights.ts
  - frontend/src/types/api.ts
autonomous: true
requirements:
  - INSIGHT-UI-05
  - INSIGHT-UI-03
tags:
  - frontend
  - types

must_haves:
  truths:
    - "OpeningInsightFinding TS type carries confidence and p_value, no longer carries loss_rate or win_rate"
    - "NextMoveEntry TS type carries score, confidence, p_value mirroring the Plan 02 backend extension"
  artifacts:
    - path: "frontend/src/types/insights.ts"
      provides: "OpeningInsightFinding interface aligned with Phase 75 backend"
      contains: "confidence: 'low' | 'medium' | 'high'"
    - path: "frontend/src/types/api.ts"
      provides: "NextMoveEntry interface aligned with Plan 02 backend"
      contains: "score: number"
  key_links:
    - from: "frontend/src/types/insights.ts"
      to: "app/schemas/opening_insights.py"
      via: "Hand-mirrored type definition (no codegen)"
      pattern: "OpeningInsightFinding"
    - from: "frontend/src/types/api.ts"
      to: "app/schemas/openings.py"
      via: "Hand-mirrored NextMoveEntry"
      pattern: "NextMoveEntry"
---

<objective>
Catch the frontend type definitions up to the backend contract: remove `loss_rate` / `win_rate` from `OpeningInsightFinding` (Phase 75 backend already dropped them), add `confidence` and `p_value`. Add `score`, `confidence`, `p_value` to `NextMoveEntry` to mirror Plan 02's schema extension.

Purpose: Wave 3 component plans (05/06/07) read these fields. Without this catch-up, `OpeningFindingCard.tsx` continues reading removed fields (currently broken since Phase 75 shipped) and `MoveExplorer.tsx` cannot display the Conf column.
Output: Two updated TS interface files. Type-check passes for the type files themselves; the broader project may still have errors from consumer call sites until Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-CONTEXT.md
@.planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-PATTERNS.md
@frontend/src/types/insights.ts
@frontend/src/types/api.ts
@app/schemas/opening_insights.py
@app/schemas/openings.py

<interfaces>
<!-- Backend source-of-truth — app/schemas/opening_insights.py (Phase 75 shipped) -->
class OpeningInsightFinding(BaseModel):
    color: Literal["white", "black"]
    classification: Literal["weakness", "strength"]
    severity: Literal["minor", "major"]
    opening_name: str
    opening_eco: str
    display_name: str
    entry_fen: str
    entry_san_sequence: list[str]
    entry_full_hash: str
    candidate_move_san: str
    resulting_full_hash: str
    n_games: int
    wins: int
    draws: int
    losses: int
    score: float
    confidence: Literal["low", "medium", "high"]
    p_value: float

<!-- Backend source-of-truth — app/schemas/openings.py NextMoveEntry (after Plan 02) -->
class NextMoveEntry(BaseModel):
    move_san: str
    game_count: int
    wins: int
    draws: int
    losses: int
    win_pct: float
    draw_pct: float
    loss_pct: float
    result_hash: str
    result_fen: str
    transposition_count: int
    score: float
    confidence: Literal["low", "medium", "high"]
    p_value: float
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpeningInsightFinding TS interface in types/insights.ts</name>
  <files>frontend/src/types/insights.ts</files>
  <read_first>
    - frontend/src/types/insights.ts (lines 69-88 — current `OpeningInsightFinding` interface)
    - app/schemas/opening_insights.py (lines 39-81 — Python source-of-truth)
    - .planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-PATTERNS.md (`frontend/src/types/insights.ts` section)
  </read_first>
  <action>
Locate the `OpeningInsightFinding` interface (currently at lines 69-88 per PATTERNS.md). Make two edits:

1. **Remove fields** `loss_rate: number;` and `win_rate: number;` (currently lines 85-86 per PATTERNS.md). They no longer exist in the Python schema.
2. **Add fields** in their place:
   ```typescript
   confidence: 'low' | 'medium' | 'high';
   p_value: number;
   ```

Final interface shape MUST match this exactly (mirroring the Python schema):

```typescript
export interface OpeningInsightFinding {
  color: 'white' | 'black';
  classification: 'weakness' | 'strength';
  severity: 'minor' | 'major';
  opening_name: string;
  opening_eco: string;
  display_name: string;
  entry_fen: string;
  entry_san_sequence: string[];
  entry_full_hash: string;
  candidate_move_san: string;
  resulting_full_hash: string;
  n_games: number;
  wins: number;
  draws: number;
  losses: number;
  score: number;
  confidence: 'low' | 'medium' | 'high';
  p_value: number;
}
```

Preserve any leading JSDoc / comment block on the interface. Preserve any sibling interfaces in the file (read full file before editing).

Note: this change will cause `tsc --noEmit` to flag `OpeningFindingCard.tsx:25-27` (still reads `finding.loss_rate` / `finding.win_rate`). That's expected — Plan 06 fixes it in Wave 3.
  </action>
  <verify>
    <automated>grep -q "confidence: 'low' | 'medium' | 'high';" frontend/src/types/insights.ts && grep -q "p_value: number;" frontend/src/types/insights.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "loss_rate" frontend/src/types/insights.ts` returns NON-ZERO (field removed)
    - `grep -q "win_rate" frontend/src/types/insights.ts` returns NON-ZERO (field removed)
    - `grep -q "confidence: 'low' | 'medium' | 'high';" frontend/src/types/insights.ts` returns 0
    - `grep -q "p_value: number;" frontend/src/types/insights.ts` returns 0
    - `grep -q "score: number;" frontend/src/types/insights.ts` returns 0 (kept from Phase 75)
  </acceptance_criteria>
  <done>OpeningInsightFinding TS interface mirrors the Python schema; loss_rate/win_rate gone; confidence/p_value added.</done>
</task>

<task type="auto">
  <name>Task 2: Extend NextMoveEntry TS interface in types/api.ts</name>
  <files>frontend/src/types/api.ts</files>
  <read_first>
    - frontend/src/types/api.ts (lines 93-105 — current `NextMoveEntry` interface)
    - app/schemas/openings.py (lines 185-208 after Plan 02 — Python source-of-truth)
    - .planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-PATTERNS.md (`frontend/src/types/api.ts` section)
  </read_first>
  <action>
Locate the `NextMoveEntry` interface (currently at lines 93-105 per PATTERNS.md). Append three fields AFTER `transposition_count: number;`:

```typescript
score: number;
confidence: 'low' | 'medium' | 'high';
p_value: number;
```

Final interface shape MUST be:

```typescript
export interface NextMoveEntry {
  move_san: string;
  game_count: number;
  wins: number;
  draws: number;
  losses: number;
  win_pct: number;
  draw_pct: number;
  loss_pct: number;
  result_hash: string;
  result_fen: string;
  transposition_count: number;
  score: number;
  confidence: 'low' | 'medium' | 'high';
  p_value: number;
}
```

Preserve any sibling interfaces in the file (read full file before editing). Do NOT remove or modify any existing fields — only append.

Note: `MoveExplorer.tsx:228` currently calls `getArrowColor(entry.win_pct, entry.loss_pct, entry.game_count, false)`. Plan 03 already changed `getArrowColor`'s signature so this call is broken. Plan 05 fixes the call site in Wave 3. Until Plan 05 ships, `npx tsc --noEmit` will fail.
  </action>
  <verify>
    <automated>grep -q "score: number;" frontend/src/types/api.ts && grep -q "confidence: 'low' | 'medium' | 'high';" frontend/src/types/api.ts && grep -q "p_value: number;" frontend/src/types/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 20 'export interface NextMoveEntry' frontend/src/types/api.ts | grep -q "score: number;"` returns 0
    - `grep -A 20 'export interface NextMoveEntry' frontend/src/types/api.ts | grep -q "confidence: 'low' | 'medium' | 'high';"` returns 0
    - `grep -A 20 'export interface NextMoveEntry' frontend/src/types/api.ts | grep -q "p_value: number;"` returns 0
    - `grep -A 20 'export interface NextMoveEntry' frontend/src/types/api.ts | grep -q "transposition_count: number;"` returns 0 (existing field preserved)
  </acceptance_criteria>
  <done>NextMoveEntry TS interface mirrors the Plan 02 backend schema with score/confidence/p_value appended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries. Pure type definitions — no runtime behavior, no auth surface, no I/O.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-01 | T (Tampering) | TS type definitions | accept | Compile-time only; runtime validation enforced by Pydantic at backend boundary (Plan 02). |

No high-severity threats. `block_on=high` gate satisfied.
</threat_model>

<verification>
1. Both interfaces match the Python source-of-truth.
2. `cd frontend && npx tsc --noEmit` — EXPECTED TO FAIL at `OpeningFindingCard.tsx:25-27` (loss_rate/win_rate reads) and `MoveExplorer.tsx:228` (getArrowColor call site). Both fixed by Wave 4 plans (05 and 06).
</verification>

<success_criteria>
- `OpeningInsightFinding` TS interface has `confidence` and `p_value`, lacks `loss_rate` and `win_rate`.
- `NextMoveEntry` TS interface gains `score`, `confidence`, `p_value`.
- Both interfaces structurally mirror the Python source-of-truth.
</success_criteria>

<output>
After completion, create `.planning/phases/76-frontend-score-coloring-confidence-badges-label-reframe/76-04-SUMMARY.md` per the SUMMARY template.
</output>
