---
phase: 75
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - INSIGHT-SCORE-04
  - INSIGHT-SCORE-06
  - INSIGHT-SCORE-07
tags:
  - planning
  - requirements
user_setup: []

must_haves:
  truths:
    - "INSIGHT-SCORE-04 in REQUIREMENTS.md says 'trinomial Wald 95% confidence interval' (not 'Wilson 95% confidence interval')."
    - "INSIGHT-SCORE-04 specifies the half-width buckets: <= 0.10 → high, <= 0.20 → medium, else low."
    - "INSIGHT-SCORE-06 specifies the API contract addition includes both confidence: Literal['low','medium','high'] AND p_value: float."
    - "The traceability table at the bottom is unchanged (INSIGHT-SCORE-04 → 75, INSIGHT-SCORE-06 → 75)."
    - "The 'Last updated' line is bumped to 2026-04-28 with a Phase 75 amendment note."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Amended INSIGHT-SCORE-04 (trinomial Wald, half-width buckets) and INSIGHT-SCORE-06 (confidence + p_value)"
      contains: "trinomial Wald 95% confidence interval"
  key_links:
    - from: ".planning/REQUIREMENTS.md::INSIGHT-SCORE-04"
      to: "app/services/opening_insights_service.py::_compute_confidence"
      via: "REQUIREMENTS prose mirrors D-05 / D-06 implementation in Plan 03"
      pattern: "trinomial Wald 95% confidence interval"
    - from: ".planning/REQUIREMENTS.md::INSIGHT-SCORE-06"
      to: "app/schemas/opening_insights.py::OpeningInsightFinding"
      via: "REQUIREMENTS prose names the two new fields delivered by Plan 02"
      pattern: "confidence.*p_value"
---

<objective>
Apply the two REQUIREMENTS.md amendments locked in 75-CONTEXT.md (D-05 amends
INSIGHT-SCORE-04; D-09 amends INSIGHT-SCORE-06) so the milestone REQUIREMENTS
document accurately reflects what Phase 75 ships.

Purpose: REQUIREMENTS.md is the single source of truth for the v1.14
milestone scope. Phase 75's `/gsd-discuss-phase` revealed that the
default-direction Wilson approach in INSIGHT-SCORE-04 over-states uncertainty
when draws are common, so the implementation pivots to the trinomial Wald
formula (the standard for chess match statistics: BayesElo, Ordo). It also
adds `p_value` alongside `confidence` per D-09. These amendments must land at
Phase 75 commit time so the doc and the code stay in sync.

Output: One file modified — `.planning/REQUIREMENTS.md`. Pure documentation
update; no code, no tests. Independent of all other Phase 75 plans (no file
overlap), so it can run in Wave 1 alongside Plans 01 and 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/75-backend-score-metric-confidence-annotation/75-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend INSIGHT-SCORE-04 (Wilson → trinomial Wald + half-width buckets) and INSIGHT-SCORE-06 (add p_value)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — focus on lines 19-21 for SCORE-04 and SCORE-06)
    - .planning/phases/75-backend-score-metric-confidence-annotation/75-CONTEXT.md (D-05 and D-09 — exact prose to use)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Two surgical edits, plus a "Last updated" footer bump.

    **Edit 1 — INSIGHT-SCORE-04 (line 19).** The current bullet reads:

    > - [ ] **INSIGHT-SCORE-04**: Compute Wilson 95% confidence interval on score per (entry_position, candidate_move) pair. Bucket each surviving finding to `confidence: "low" | "medium" | "high"` based on Wilson half-width (or equivalent p-value bucket). Exact bucket boundaries resolved during Phase 75 `/gsd-discuss-phase`; default direction: `high` = LB clears the effect-size threshold; `medium` = LB clears 0.50 but not the threshold; `low` = LB doesn't clear 0.50 but observed score does.

    Replace with (per D-05 amendment text in CONTEXT.md):

    > - [ ] **INSIGHT-SCORE-04**: Compute a **trinomial Wald 95% confidence interval** on score per (entry_position, candidate_move) pair. The formula uses per-game variance `(W + 0.25·D)/N − score²` (the actual variance under the trinomial result distribution X ∈ {0, 0.5, 1}) rather than the binomial Wilson approximation, which over-states uncertainty when draws are common. Standard formula for chess match statistics (BayesElo, Ordo). Bucket each surviving finding to `confidence: "low" | "medium" | "high"` based on the half-width: `≤ 0.10 → high`, `≤ 0.20 → medium`, else `low`. Strict `≤` boundaries. Pure Python `math` only — no scipy dependency. Bucket boundaries live in `opening_insights_constants.py` so they can be retuned against real data without touching service code.

    **Edit 2 — INSIGHT-SCORE-06 (line 21).** The current bullet reads:

    > - [ ] **INSIGHT-SCORE-06**: Extend the `OpeningInsightFinding` API contract with a `confidence: "low" | "medium" | "high"` field. The existing `severity: "major" | "minor"` field stays, so the frontend can render both severity (effect size) and confidence (annotation) per finding without overloading either.

    Replace with (per D-09 amendment text in CONTEXT.md):

    > - [ ] **INSIGHT-SCORE-06**: Extend the `OpeningInsightFinding` API contract with two new fields: `confidence: "low" | "medium" | "high"` (the half-width bucket from INSIGHT-SCORE-04) and `p_value: float` (two-sided test of observed score vs the 0.50 pivot, computed via the same Z-statistic that drives the half-width). The existing `severity: "major" | "minor"` field stays, so the frontend can render severity (effect size), confidence (precision), and a tooltip-grade p_value per finding without overloading any one cue. Phase 75 also drops `loss_rate` and `win_rate` cleanly — only consumer is the v1.14 frontend (Phase 76) in this same milestone.

    **Edit 3 — Footer line.** The current footer (line 68) reads:

    > *Last updated: 2026-04-28 — milestone v1.14 opened. 14/14 requirements mapped (INSIGHT-SCORE-01..07 → Phase 75; INSIGHT-UI-01..07 → Phase 76).*

    Replace with:

    > *Last updated: 2026-04-28 — milestone v1.14 opened. 14/14 requirements mapped (INSIGHT-SCORE-01..07 → Phase 75; INSIGHT-UI-01..07 → Phase 76). Phase 75 amendments: INSIGHT-SCORE-04 pivots from Wilson to trinomial Wald with half-width-based buckets (D-05); INSIGHT-SCORE-06 extends API contract to include `p_value` alongside `confidence` (D-09).*

    Do NOT touch:
    - INSIGHT-SCORE-01, -02, -03, -05, -07 (locked, no amendments).
    - INSIGHT-UI-01..07 (Phase 76 scope).
    - The "Future Requirements (deferred)" and "Out of Scope" sections.
    - The traceability table at the bottom (mapping unchanged).
    - The milestone-goal paragraph at the top.
  </action>
  <verify>
    <automated>
    grep -q "trinomial Wald 95% confidence interval" .planning/REQUIREMENTS.md && \
    grep -q "≤ 0.10 → high" .planning/REQUIREMENTS.md && \
    grep -q "≤ 0.20 → medium" .planning/REQUIREMENTS.md && \
    grep -q "p_value: float" .planning/REQUIREMENTS.md && \
    grep -q "two-sided test of observed score" .planning/REQUIREMENTS.md && \
    grep -q "Phase 75 amendments" .planning/REQUIREMENTS.md && \
    ! grep -E "Wilson 95% confidence interval" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - INSIGHT-SCORE-04 says "trinomial Wald 95% confidence interval" with the variance formula `(W + 0.25·D)/N − score²` and half-width buckets `≤ 0.10 → high`, `≤ 0.20 → medium`, else `low`.
    - The phrase "Wilson 95% confidence interval" no longer appears anywhere in REQUIREMENTS.md.
    - INSIGHT-SCORE-06 names both `confidence: "low" | "medium" | "high"` and `p_value: float` as fields, and notes that `loss_rate`/`win_rate` are dropped.
    - The footer's "Last updated" line carries the amendment note.
    - The traceability table at the bottom is unchanged (INSIGHT-SCORE-04 → 75, INSIGHT-SCORE-06 → 75).
    - INSIGHT-SCORE-01, -02, -03, -05, -07 unchanged (diff against current file shows zero edits to those bullets).
    - INSIGHT-UI-01..07 unchanged.
    - "Future Requirements (deferred)", "Out of Scope" unchanged.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md amended per D-05 / D-09. Document and Phase 75 code now
    in sync.
  </done>
</task>

</tasks>

<threat_model>
Pure documentation update. No code, no behavior, no attack surface. No
threat-model entries needed.
</threat_model>

<verification>
After Task 1 completes:
- The grep automated checks above all return non-empty / zero exit.
- A diff against the pre-Plan-04 REQUIREMENTS.md shows changes only on lines
  19, 21, and 68 (the two amended bullets and the footer).
</verification>

<success_criteria>
1. INSIGHT-SCORE-04 prose reflects D-05 (trinomial Wald + half-width buckets, no Wilson).
2. INSIGHT-SCORE-06 prose reflects D-09 (confidence + p_value).
3. Footer notes the Phase 75 amendments with D-05/D-09 anchors.
4. No collateral edits to other requirements or sections.
</success_criteria>

<output>
After completion, create `.planning/phases/75-backend-score-metric-confidence-annotation/75-04-SUMMARY.md`
documenting:
- Two amended bullets (INSIGHT-SCORE-04, INSIGHT-SCORE-06) with verbatim before/after text
- Footer amendment line
- Cross-reference to D-05 and D-09 in 75-CONTEXT.md
- Note that Phase 76 picks up the amended SCORE-06 contract for frontend consumption
</output>
