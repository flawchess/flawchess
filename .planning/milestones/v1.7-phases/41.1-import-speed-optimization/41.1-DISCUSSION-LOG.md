# Phase 41.1: Import Speed Optimization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-03
**Phase:** 41.1-import-speed-optimization
**Areas discussed:** Optimization scope, PGN processing, Batch size & memory

---

## Optimization Scope

### Pain Point

| Option | Description | Selected |
|--------|-------------|----------|
| Initial import too slow | First-time import of large game libraries (1000+ games) takes too long | |
| Re-sync too slow | Incremental syncs after initial import take longer than expected | |
| Both equally | Both initial imports and re-syncs feel sluggish | |

**User's choice:** Free text — "Import is not super slow, but could definitely be faster. Not sure how much slower things get when multiple users import at the same time. Resync is fast."
**Notes:** Queried prod import_jobs table — baseline is ~20 games/sec. 10k games = ~7 minutes.

### Target

| Option | Description | Selected |
|--------|-------------|----------|
| No specific target | Just make it noticeably faster — squeeze out obvious inefficiencies | |
| I have numbers | Rough measurements or expectations to share | |

**User's choice:** Free text — "Look at the import_jobs table in prod and see for yourself"
**Notes:** Prod data analysis showed ~16-24 games/sec across platforms. Processing dominates over network I/O.

### Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Processing wins only | Consolidate PGN parsing, remove SELECT, batch UPDATEs. ~2x speedup. | |
| Processing + concurrent fetch | Also fetch 2-3 chess.com archives in parallel. | |
| Include profiling first | Add timing instrumentation first, then optimize. | |

**User's choice:** Free text — initially unsure whether CPU or API calls are the bottleneck. After seeing breakdown (processing ~400s vs fetching ~30s for a 432s import), chose "Processing wins only."
**Notes:** User raised a good question about concurrent archive fetching. Analysis showed it would only save ~10% vs ~50%+ from processing optimization.

---

## PGN Processing

### Consolidation approach

| Option | Description | Selected |
|--------|-------------|----------|
| Single unified function | Replace hashes_for_game with new function yielding all data per ply | ✓ |
| Merge #1 and #2 only | Extend hashes_for_game, eliminate #3 via length calc | |
| You decide | Claude picks best approach | |

**User's choice:** Single unified function
**Notes:** None

### SELECT elimination

| Option | Description | Selected |
|--------|-------------|----------|
| Track PGN by platform_game_id | Dict mapping platform_game_id → PGN, lightweight SELECT for ID mapping | ✓ |
| Return PGN from bulk_insert | Modify bulk_insert_games to RETURNING pgn | |
| You decide | Claude picks | |

**User's choice:** Track PGN by platform_game_id
**Notes:** None

---

## Batch Size & Memory

### Batch size increase

| Option | Description | Selected |
|--------|-------------|----------|
| Increase to 25-30 | Position chunking prevents huge INSERTs. Fewer commits. | ✓ |
| Keep at 10 | Don't touch it — OOM risk on 3.7 GB server | |
| Make it configurable | Add env var, default 10, tune per deployment | |

**User's choice:** Increase to 25-30
**Notes:** Position INSERT chunking (1,700 rows) was added after the OOM incident. With chunking, larger game batches are safe.

### Bulk UPDATE for move_count/result_fen

| Option | Description | Selected |
|--------|-------------|----------|
| Bulk UPDATE with CASE | Single UPDATE with CASE expressions per batch | ✓ |
| Compute before INSERT | Set move_count/result_fen on Game row before bulk_insert_games | |
| You decide | Claude picks | |

**User's choice:** Bulk UPDATE with CASE
**Notes:** None

---

## Claude's Discretion

- Exact batch size within 25-30 range
- Internal API design of unified PGN processing function
- Whether to keep hashes_for_game as thin wrapper or replace entirely
- Test strategy for correctness verification

## Deferred Ideas

- Concurrent archive fetching — ~10% gain but rate limit risk
- Profiling instrumentation — useful for future fine-tuning
- ProcessPoolExecutor for CPU-bound work — complex, uncertain benefit
