---
phase: 92-custom-date-range-filter
plan: 05
type: execute
wave: 5
depends_on: [92-04]
files_modified:
  - frontend/src/components/filters/FilterPanel.tsx
  - frontend/src/components/filters/CustomRangePopover.tsx
  - frontend/src/components/filters/CustomRangeDrawer.tsx
autonomous: true
requirements: [P92-R4]
must_haves:
  truths:
    - "A 9th SelectItem labeled 'Custom range…' with data-testid='filter-recency-custom' is rendered inside the Recency Select"
    - "On desktop (md+), clicking the 9th item opens a Radix Popover anchored via PopoverAnchor to the Select trigger; the Popover contains a range-mode Calendar"
    - "On mobile (<md), clicking the 9th item opens a nested Vaul Drawer (DrawerNested) layered over the FilterPanel drawer; the Drawer contains a single-month range-mode Calendar plus an Apply CTA"
    - "Picking both bounds on desktop auto-closes the Popover (D-05); the Apply CTA on mobile commits the range and closes the Drawer (D-07); backdrop dismiss on mobile cancels without committing (D-08)"
    - "Picking any preset (non-custom) clears filters.customRange to null and reverts the trigger label to the preset name"
    - "Trigger label renders the resolved range as 'MMM d, yyyy – MMM d, yyyy' when both bounds set, or 'From MMM d, yyyy' / 'Until MMM d, yyyy' for single-bound cases"
    - "Required data-testids present: filter-recency-custom, custom-range-popover, drawer-custom-range, custom-range-calendar, calendar-day-${YYYY-MM-DD}, btn-apply-custom-range"
    - "All text inside new UI is text-sm or larger (CLAUDE.md text-sm floor)"
    - "Both desktop and mobile renderers receive identical functionality (CLAUDE.md mobile parity rule)"
  artifacts:
    - path: frontend/src/components/filters/CustomRangePopover.tsx
      provides: "Desktop popover-anchored range Calendar"
      contains: "PopoverAnchor"
      min_lines: 30
    - path: frontend/src/components/filters/CustomRangeDrawer.tsx
      provides: "Mobile nested-drawer range Calendar with Apply CTA"
      contains: "DrawerNested"
      min_lines: 30
  key_links:
    - from: frontend/src/components/filters/FilterPanel.tsx
      to: frontend/src/components/filters/CustomRangePopover.tsx
      via: "Desktop branch passes value/onChange/anchorRef/open/onOpenChange props"
      pattern: "CustomRangePopover"
    - from: frontend/src/components/filters/FilterPanel.tsx
      to: frontend/src/components/filters/CustomRangeDrawer.tsx
      via: "Mobile branch passes value/onChange/open/onOpenChange props"
      pattern: "CustomRangeDrawer"
---

<objective>
Wire the Calendar primitive into the FilterPanel: add the 9th "Custom range…" SelectItem, render a Radix Popover anchored to the Select trigger on desktop, render a nested Vaul Drawer on mobile, render a resolved-range label on the Select trigger, and clear `customRange` when the user picks any non-custom preset. Both desktop and mobile branches use the same Calendar in range mode but differ in the open/dismiss UX per D-05/D-06/D-07/D-08.

Implements D-03 (two-step Select → Popover anchored), D-04 (trigger label rendering), D-05 (desktop auto-close), D-06 (mobile nested Vaul), D-07 (mobile Apply CTA), D-08 (mobile backdrop = cancel), D-17 (single-bound allowed). Addresses synthetic requirement P92-R4 (UI half — infrastructure was Plan 04).

Purpose: User-facing feature is live. The 95% preset case remains visually unchanged (the existing 8 items render exactly as before); the new 9th item adds the custom-range workflow.
Output: All three UI files, fully working desktop and mobile flows, every interactive element has a data-testid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/92-custom-date-range-filter/92-CONTEXT.md
@.planning/phases/92-custom-date-range-filter/92-RESEARCH.md
@.planning/phases/92-custom-date-range-filter/92-PATTERNS.md
@.planning/phases/92-custom-date-range-filter/92-04-SUMMARY.md
@CLAUDE.md
@frontend/src/components/filters/FilterPanel.tsx
@frontend/src/components/ui/popover.tsx
@frontend/src/components/ui/drawer.tsx
@frontend/src/components/ui/calendar.tsx
@frontend/src/components/filters/OpponentStrengthFilter.tsx

<interfaces>
<!-- Existing Recency Select block (frontend/src/components/filters/FilterPanel.tsx:173-195): -->
<!--   {show('recency') && ( -->
<!--     <div> -->
<!--       <p className="mb-1 text-xs text-muted-foreground">Recency</p> -->
<!--       <Select value={filters.recency ?? 'all'} onValueChange={...}> -->
<!--         <SelectTrigger data-testid="filter-recency" ...> ... </SelectTrigger> -->
<!--         <SelectContent> 8 SelectItems </SelectContent> -->
<!--       </Select> -->
<!--     </div> -->
<!--   )} -->
<!-- Target: Wrap Select in Popover with PopoverAnchor asChild; add 9th SelectItem value="custom"; -->
<!--   defer popover open via queueMicrotask; render custom trigger label when filters.recency === 'custom'. -->

<!-- CustomRangePopover props (per 92-PATTERNS.md): -->
<!--   value: { from?: Date; to?: Date } | null -->
<!--   onChange: (range: { from?: Date; to?: Date } | null) => void -->
<!--   open: boolean -->
<!--   onOpenChange: (open: boolean) => void -->
<!--   triggerRef: React.RefObject<HTMLElement> (or omitted if PopoverAnchor asChild handles anchoring) -->

<!-- CustomRangeDrawer props: -->
<!--   value: { from?: Date; to?: Date } | null -->
<!--   onChange: (range: { from?: Date; to?: Date } | null) => void -->
<!--   open: boolean -->
<!--   onOpenChange: (open: boolean) => void -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CustomRangePopover (desktop) and CustomRangeDrawer (mobile) components with all data-testids and text-sm floor</name>
  <files>frontend/src/components/filters/CustomRangePopover.tsx, frontend/src/components/filters/CustomRangeDrawer.tsx</files>
  <read_first>
    - frontend/src/components/ui/popover.tsx (full file — note PopoverAnchor export at lines 40-44; Popover content className at line 31)
    - frontend/src/components/ui/drawer.tsx (full file — note DrawerNested added in Plan 04; DrawerContent styling)
    - frontend/src/components/ui/calendar.tsx (new from Plan 04 — verify Calendar accepts mode="range", selected, onSelect)
    - frontend/src/components/filters/OpponentStrengthFilter.tsx (full file — analog for filter sub-component prop shape)
    - frontend/src/components/filters/FilterPanel.tsx (lines 316-327 for the Reset button pattern → mirror for Apply CTA)
    - frontend/src/lib/recency.ts (to import the wire-shape helpers if needed for the label formatter)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§CustomRangePopover.tsx, §CustomRangeDrawer.tsx)
  </read_first>
  <behavior>
    - Desktop: rendering `<CustomRangePopover open={true} value={null} onChange={fn} />` mounts a `<PopoverContent data-testid="custom-range-popover">` containing a `<Calendar mode="range" data-testid="custom-range-calendar">`. Picking both bounds calls `onChange({ from, to })` then triggers `onOpenChange(false)` (auto-close per D-05).
    - Mobile: rendering `<CustomRangeDrawer open={true} value={null} onChange={fn} />` mounts a `<DrawerNested><DrawerContent data-testid="drawer-custom-range">` containing a single-month Calendar and a `<Button data-testid="btn-apply-custom-range">Apply</Button>`. The Apply button is disabled when range has no `from` bound. Clicking Apply calls `onChange({ from, to })` then `onOpenChange(false)`. Backdrop dismiss (vaul's default `onOpenChange(false)` via outside click) does NOT call `onChange` (D-08).
    - Trigger label helper (exported alongside the components or co-located): given `{ from, to }`, returns the formatted label string. Both set → `'MMM d, yyyy – MMM d, yyyy'`. From only → `'From MMM d, yyyy'`. To only → `'Until MMM d, yyyy'` (symmetric — Open Question 2 in RESEARCH.md resolves to symmetric phrasing).
    - All visible text is `text-sm` or larger.
    - `calendar-day-${YYYY-MM-DD}` data-testid is present on each day button (patch via DayPicker `formatters.formatDay` slot or wrap the day component).
  </behavior>
  <action>
    **Step 1 — frontend/src/components/filters/CustomRangePopover.tsx:** Component accepting `{ value, onChange, open, onOpenChange }` props. Renders a `<PopoverContent data-testid="custom-range-popover" className="...">` (use the existing popover.tsx default className for `text-sm` inheritance — see popover.tsx:31). Inside, render `<Calendar mode="range" selected={value ?? undefined} onSelect={(range) => { onChange(range); if (range?.from && range?.to) onOpenChange(false); }} numberOfMonths={2} data-testid="custom-range-calendar" />`. Two-month layout chosen per Discretion call — the Popover's default `w-72` (288px) is too narrow for two months, so apply a wider className (`w-auto` or `w-[36rem]`) on the PopoverContent for desktop. Verify the popover content doesn't overflow the viewport edge — Radix Popover handles collision detection by default.

    Pass day-cell `data-testid` via the Calendar's `formatters.formatDay` prop or `components.Day` slot — react-day-picker v10 supports `components.Day` for full day-cell override. Wrap the default day render with `<button data-testid={`calendar-day-${format(day.date, 'yyyy-MM-dd')}`}>{children}</button>`. Import `format` from `date-fns`.

    **Step 2 — frontend/src/components/filters/CustomRangeDrawer.tsx:** Component accepting `{ value, onChange, open, onOpenChange }` props. Outer wrapper `<DrawerNested open={open} onOpenChange={onOpenChange}>`. Inside: `<DrawerContent data-testid="drawer-custom-range">` with `<Calendar mode="range" selected={localRange ?? undefined} onSelect={setLocalRange} numberOfMonths={1} data-testid="custom-range-calendar" />` (single month per D-06). Maintain a `localRange` `useState` inside the component — DO NOT commit to parent on every Calendar click (this is the desktop/mobile difference; on mobile, only Apply commits). Below the Calendar, render `<Button variant="default" size="lg" className="w-full min-h-11 sm:min-h-0" data-testid="btn-apply-custom-range" disabled={!localRange?.from} onClick={() => { onChange(localRange); onOpenChange(false); }}>Apply</Button>` — mirror the Reset button pattern from FilterPanel.tsx:316-327 but variant="default" (primary CTA per CLAUDE.md). For backdrop dismiss = cancel (D-08): vaul's default `onOpenChange(false)` from outside-click does NOT touch `onChange`, so simply NOT calling `onChange` in any close path other than Apply is the correct implementation.

    **Step 3 — Trigger label helper:** Either export from each component file or create a small helper function (e.g. `formatCustomRangeLabel(range): string` co-located with CustomRangePopover.tsx). Given `{ from, to }`:
    - both set: `format(from, 'MMM d, yyyy') + ' – ' + format(to, 'MMM d, yyyy')` (en-dash, with date-fns format)
    - from only: `'From ' + format(from, 'MMM d, yyyy')`
    - to only: `'Until ' + format(to, 'MMM d, yyyy')`
    - neither set (shouldn't happen if recency === 'custom' but defensive): `'Custom range…'`

    **Step 4 — text-sm floor + theme tokens:** Verify nothing in either component uses `text-xs` on visible text (the existing FilterPanel "Recency" header at line 175 is grandfathered; do NOT propagate). Verify all colors come from theme tokens (no raw hex).

    **Step 5 — Day cell data-testid implementation:** react-day-picker v10's `components.Day` override pattern (verified via Context7 / shadcn registry). Provide a `Day` component that renders the default day button with the `data-testid` attached. The exact API is `components={{ Day: CustomDay }}` where `CustomDay` receives `day: { date: Date }` and other props — wrap with `data-testid={`calendar-day-${format(day.date, 'yyyy-MM-dd')}`}`. If the v10 Day API requires re-implementing more of the button behavior (selection styling, etc.), prefer the lighter `formatters.formatDay` override and instead attach the data-testid via a parent `<Calendar>` className that selects child buttons (less ideal) — first-line attempt is `components={{ Day }}`.

    Implements D-04 (trigger label format), D-05/D-06/D-07/D-08 (UX), D-17 (single-bound).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/filters/CustomRangePopover.tsx` succeeds
    - `test -f frontend/src/components/filters/CustomRangeDrawer.tsx` succeeds
    - `grep -n "data-testid=\"custom-range-popover\"" frontend/src/components/filters/CustomRangePopover.tsx` returns 1 match
    - `grep -n "data-testid=\"custom-range-calendar\"" frontend/src/components/filters/CustomRangePopover.tsx frontend/src/components/filters/CustomRangeDrawer.tsx` returns ≥ 2 matches (one in each file)
    - `grep -n "data-testid=\"drawer-custom-range\"" frontend/src/components/filters/CustomRangeDrawer.tsx` returns 1 match
    - `grep -n "data-testid=\"btn-apply-custom-range\"" frontend/src/components/filters/CustomRangeDrawer.tsx` returns 1 match
    - `grep -n "calendar-day-\${" frontend/src/components/filters/` returns ≥ 1 match (day-cell testid template literal in at least one of the two files, ideally factored into a shared Day wrapper)
    - `grep -n "PopoverContent" frontend/src/components/filters/CustomRangePopover.tsx` returns ≥ 1 match
    - `grep -n "DrawerNested" frontend/src/components/filters/CustomRangeDrawer.tsx` returns ≥ 1 match
    - `grep -n "text-xs" frontend/src/components/filters/CustomRangePopover.tsx frontend/src/components/filters/CustomRangeDrawer.tsx` returns 0 matches (text-sm floor enforced)
    - `grep -nE "#[0-9a-fA-F]{6}\b" frontend/src/components/filters/CustomRangePopover.tsx frontend/src/components/filters/CustomRangeDrawer.tsx` returns 0 matches (theme tokens only)
    - `cd frontend && npx tsc --noEmit` passes
    - `cd frontend && npm run lint` passes
    - `cd frontend && npm test -- --run` passes
  </acceptance_criteria>
  <done>Both Custom Range UI components exist, render correctly, have full data-testid coverage, and respect the text-sm floor and theme constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire FilterPanel.tsx — 9th SelectItem, two-step Select→Popover anchor (desktop), nested Drawer (mobile), trigger label resolver, clear customRange on preset pick</name>
  <files>frontend/src/components/filters/FilterPanel.tsx</files>
  <read_first>
    - frontend/src/components/filters/FilterPanel.tsx (full file — Recency Select at lines 173-195, areFiltersEqual at 70-97, OpponentStrengthFilter usage at 255-261 for the filter-sub-component invocation pattern)
    - frontend/src/components/filters/CustomRangePopover.tsx (just created — verify prop interface matches)
    - frontend/src/components/filters/CustomRangeDrawer.tsx (just created — verify prop interface)
    - frontend/src/hooks/useBreakpoint.ts OR whatever utility the FilterPanel uses to detect mobile (search FilterPanel for `useBreakpoint`, `useMediaQuery`, or look at where the FilterPanel is invoked by its parent — Openings.tsx renders FilterPanel inside a Drawer on mobile via lines 1063+)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§FilterPanel.tsx)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Pattern 1 lines 286-318 for the Popover/PopoverAnchor + Select recipe; §Pitfall 6 for queueMicrotask)
  </read_first>
  <behavior>
    - Selecting a preset (e.g. clicking "Past month"): `update({ recency: 'month', customRange: null })` — customRange cleared, trigger label shows "Past month".
    - Selecting "All time": `update({ recency: null, customRange: null })` — same trigger as today.
    - Selecting "Custom range…": triggers the Popover (desktop) or Drawer (mobile) open. The Select itself does NOT update `filters.recency` to 'custom' yet — `filters.recency` only becomes 'custom' after the range is committed (so an opened-but-cancelled popover doesn't change state).
    - Range commit (desktop auto-close on full pick, mobile Apply): `update({ recency: 'custom', customRange: { from, to } })`.
    - Range commit with single bound: same as above with `from` OR `to` set (D-17 supports this).
    - Trigger label when `filters.recency === 'custom'`: render the formatted range via the helper from Task 1.
    - Trigger label when `filters.recency` is a preset: render the preset's existing label (unchanged from current behavior).
    - Mobile vs desktop branching: verify which utility/breakpoint FilterPanel uses today. If the FilterPanel is itself dual-rendered by its parent (one instance in the desktop sidebar, another inside a mobile Drawer), use a context cue (e.g. `inMobileDrawer` prop) or just detect the breakpoint via a hook. Match the existing pattern in the file.
  </behavior>
  <action>
    **Step 1 — Imports:** Add `Popover, PopoverAnchor, PopoverContent` from `@/components/ui/popover`. Add `CustomRangePopover` from `./CustomRangePopover` and `CustomRangeDrawer` from `./CustomRangeDrawer`. Add the trigger-label helper from CustomRangePopover (or wherever it landed in Task 1). Verify `RecencyPreset` import (already present from Plan 03).

    **Step 2 — Add 9th SelectItem:** Inside the existing `<SelectContent>` block at lines 184-194, add at the END: `<SelectItem value="custom" data-testid="filter-recency-custom">Custom range…</SelectItem>` — typographic ellipsis character per CONTEXT.md D-03 verbatim wording.

    **Step 3 — Track popoverOpen/drawerOpen state:** Add `const [customOpen, setCustomOpen] = useState(false);` near other local state. Branch on the existing breakpoint detection (e.g. `const isMobile = useBreakpoint('md')` — verify the existing pattern in the file).

    **Step 4 — Update onValueChange:** Replace the existing handler with logic that:
    - If `v === 'custom'`: defer `setCustomOpen(true)` via `queueMicrotask(() => setCustomOpen(true))` per RESEARCH.md §Pitfall 6 (avoids race with the Select close animation). DO NOT call `update({ recency: 'custom', ... })` yet — leave `filters.recency` as-is until the range commits.
    - Otherwise (any preset including 'all'): `update({ recency: v === 'all' ? null : (v as RecencyPreset), customRange: null })` — clear customRange when picking a preset.

    **Step 5 — Wrap the Select in Popover (desktop branch):**

    When NOT mobile, wrap the existing Select in a Popover/PopoverAnchor structure per RESEARCH.md §Pattern 1 lines 286-318:
    - `<Popover open={customOpen && !isMobile} onOpenChange={(o) => setCustomOpen(o)}>`
    - `  <PopoverAnchor asChild>` ... existing Select ... `</PopoverAnchor>`
    - `  <PopoverContent>` ... but: render `<CustomRangePopover value={filters.customRange} onChange={(range) => { if (range) update({ recency: 'custom', customRange: range }); setCustomOpen(false); }} open={customOpen} onOpenChange={setCustomOpen} />` inside PopoverContent.

    Alternative cleaner shape: put the Popover wrapper INSIDE the CustomRangePopover component itself and only render `<CustomRangePopover ... />` here, gated on `!isMobile && customOpen`. The trade-off: anchoring needs the Select trigger element, so `PopoverAnchor asChild` must wrap the Select. Recommended: wrap the Select with PopoverAnchor here in FilterPanel; render `<CustomRangePopover>` as a child of `<Popover>` and let CustomRangePopover provide only the `<PopoverContent>` body. (Plan 04 SUMMARY may dictate which shape; pick the cleanest.)

    **Step 6 — Mobile branch:** When mobile, render `<CustomRangeDrawer value={filters.customRange} onChange={(range) => { if (range) update({ recency: 'custom', customRange: range }); setCustomOpen(false); }} open={customOpen && isMobile} onOpenChange={setCustomOpen} />` after the Select.

    **Step 7 — Trigger label:** Replace `<SelectValue />` inside `<SelectTrigger>` with a conditional render: `{filters.recency === 'custom' && filters.customRange ? formatCustomRangeLabel(filters.customRange) : <SelectValue />}`. The `<SelectValue />` handles the 8 preset labels via the existing `<SelectItem>` text.

    **Step 8 — Confirm the existing `<p className="mb-1 text-xs text-muted-foreground">Recency</p>` at line 175 is NOT touched (grandfathered per CONTEXT.md). All new UI text in this plan must be text-sm or larger.

    **Step 9 — `filters.recency === 'custom'` initial render:** When mounted with `filters.recency === 'custom'`, the Select's controlled `value` prop currently reads `filters.recency ?? 'all'`. Update to `value={filters.recency === 'custom' ? 'custom' : (filters.recency ?? 'all')}` so the Select correctly highlights "Custom range…" in the dropdown when re-opened.

    Implements D-03 (full two-step), D-04 (label), D-05 (auto-close — handled by CustomRangePopover), D-06/D-07/D-08 (mobile — handled by CustomRangeDrawer).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run &amp;&amp; npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data-testid=\"filter-recency-custom\"" frontend/src/components/filters/FilterPanel.tsx` returns 1 match
    - `grep -n "queueMicrotask" frontend/src/components/filters/FilterPanel.tsx` returns 1 match (Pitfall 6 mitigation)
    - `grep -n "PopoverAnchor" frontend/src/components/filters/FilterPanel.tsx` returns ≥ 1 match
    - `grep -n "CustomRangePopover\|CustomRangeDrawer" frontend/src/components/filters/FilterPanel.tsx` returns ≥ 2 matches
    - `grep -n "customRange: null" frontend/src/components/filters/FilterPanel.tsx` returns ≥ 1 match (preset-pick path clears it)
    - `grep -n "filters.recency === 'custom'" frontend/src/components/filters/FilterPanel.tsx` returns ≥ 1 match (label conditional)
    - `cd frontend && npx tsc --noEmit` passes
    - `cd frontend && npm run lint` passes
    - `cd frontend && npm test -- --run` passes
    - `cd frontend && npm run knip` passes
    - Existing test for filter-recency testid still passes (the existing `data-testid="filter-recency"` on SelectTrigger is preserved)
  </acceptance_criteria>
  <done>FilterPanel renders 9 SelectItems; clicking "Custom range…" opens the desktop Popover or mobile Drawer; the trigger label renders the resolved range when custom; preset picks clear customRange.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → app UI | New interactive elements (Calendar day buttons, Apply CTA) accept user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05-01 | Tampering | Range selection | accept | react-day-picker mode="range" naturally enforces `to >= from` by click order; mobile Apply disabled when !from. Backend 422 (Plan 02) is the authoritative gate. |
| T-92-05-02 | Spoofing | data-testid attributes | accept | Test IDs are non-secret automation hooks; existing project convention. |
| T-92-05-03 | UX | Backdrop dismiss = cancel (D-08) | mitigate | Drawer onChange not called from backdrop close path; localRange state in CustomRangeDrawer guarantees uncommitted picks are discarded. |
</threat_model>

<verification>
- tsc/lint/vitest/knip all green
- 9 SelectItems in the Recency Select (verify in the rendered DOM via existing component test if available, OR grep the file for 9 SelectItem occurrences in the recency block)
- All data-testids from must_haves present
- text-sm floor enforced (no new text-xs in the touched files)
</verification>

<success_criteria>
- "Custom range…" item visible at the bottom of the Recency Select dropdown
- Desktop: clicking it opens an anchored Popover with a 2-month range Calendar; full range pick auto-closes
- Mobile: clicking it opens a nested Drawer with a 1-month range Calendar + Apply CTA; backdrop cancels
- Trigger label renders the resolved range for custom; preset labels for presets
- Preset selection clears customRange
</success_criteria>

<output>
Create `.planning/phases/92-custom-date-range-filter/92-05-SUMMARY.md` when done
</output>
