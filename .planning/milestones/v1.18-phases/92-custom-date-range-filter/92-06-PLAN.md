---
phase: 92-custom-date-range-filter
plan: 06
type: execute
wave: 6
depends_on: [92-05]
files_modified:
  - tests/test_integration_routers.py
  - tests/routers/test_insights_openings.py
  - CHANGELOG.md
  - .planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md
autonomous: false
requirements: [P92-R2, P92-R4]
must_haves:
  truths:
    - "Backend integration tests cover from_date / to_date boundary semantics (from_date inclusive, to_date exclusive of next day)"
    - "Both-omitted = no date filter is integration-tested"
    - "from_date > to_date returns HTTP 422 on at least one router endpoint AND at least one Pydantic-body endpoint (covers both Query-param and request-body code paths)"
    - "CHANGELOG.md [Unreleased] documents the user-facing change and the removed wire shape"
    - "92-HUMAN-UAT.md exists with the explicit UAT script for the 8 presets + Custom range desktop + Custom range mobile + back-to-preset flow"
  artifacts:
    - path: .planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md
      provides: "Human UAT script for visual verification"
      contains: "Custom range"
    - path: CHANGELOG.md
      provides: "[Unreleased] entries documenting Phase 92"
      contains: "Custom date range"
  key_links:
    - from: tests/test_integration_routers.py
      to: "app/routers/{stats,endgames,insights}.py"
      via: "HTTP-level assertions on 422 and boundary semantics"
      pattern: "from_date|to_date"
---

<objective>
Close the phase with the verification surface: backend integration tests that exercise the new boundary semantics (a game played on the exact `from_date` matches; a game on the exact `to_date` matches; a game one day after `to_date` does not), a 422 assertion on the cross-field invariant, the CHANGELOG entry under `## [Unreleased]`, and a human UAT script for the visual flow that automation cannot cover.

Implements the verification commitments from ROADMAP §Verification and the must_haves about boundary tests; addresses synthetic requirements P92-R2 (boundary semantics) and P92-R4 (UI visual verification).

Purpose: Close the phase with a defensible verification trail. Per the project's "no dev DB reset in plans" rule (Adrian memory), the human UAT is designed to run against the existing dev DB without resetting.
Output: New/extended integration tests, CHANGELOG entry, UAT script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/92-custom-date-range-filter/92-CONTEXT.md
@.planning/phases/92-custom-date-range-filter/92-RESEARCH.md
@.planning/phases/92-custom-date-range-filter/92-05-SUMMARY.md
@CLAUDE.md
@CHANGELOG.md
@tests/test_integration_routers.py
@tests/routers/test_insights_openings.py

<interfaces>
<!-- Boundary semantics under test (per app/repositories/query_utils.py target — Plan 02): -->
<!--   from_date is INCLUSIVE: played_at >= from_date (where from_date is a DATE, Postgres casts to UTC midnight) -->
<!--   to_date is INCLUSIVE of the chosen day: played_at < to_date + 1 day -->
<!-- Test cases: -->
<!--   - Game at 2026-03-01 00:00 UTC, filter from_date=2026-03-01: matches -->
<!--   - Game at 2026-04-01 23:59 UTC, filter to_date=2026-04-01: matches (still < 2026-04-02 00:00) -->
<!--   - Game at 2026-04-02 00:00 UTC, filter to_date=2026-04-01: does NOT match -->
<!--   - Game at 2026-02-28 23:59 UTC, filter from_date=2026-03-01: does NOT match (in UTC; FE TZ fuzz acknowledged in §Pitfall 2) -->
<!--   - 422 returned when from_date > to_date -->
<!--   - Insights router returns blocking message "Clear Custom date range filter" when either date param is set -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Boundary semantics integration tests + 422 assertions + insights gate assertion</name>
  <files>tests/test_integration_routers.py, tests/routers/test_insights_openings.py</files>
  <read_first>
    - tests/test_integration_routers.py (full file — note existing fixture patterns for game seeding and HTTP assertions)
    - tests/routers/test_insights_openings.py (full file — note existing pattern for asserting the LLM gating response)
    - app/repositories/query_utils.py (verify the predicate landed correctly from Plan 02)
    - app/routers/insights.py (verify the gate message "Clear Custom date range filter" landed)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Pitfall 2 boundary semantics, §Pitfall 3 gate update)
  </read_first>
  <behavior>
    - Test 1 `test_openings_request_from_date_inclusive`: Seed a game with `played_at = datetime(2026, 3, 1, 0, 0, tzinfo=UTC)`. POST `/openings/positions` with `from_date: '2026-03-01'`. Game appears in the result.
    - Test 2 `test_openings_request_to_date_end_of_day`: Seed a game with `played_at = datetime(2026, 4, 1, 23, 59, tzinfo=UTC)`. POST with `to_date: '2026-04-01'`. Game appears in the result.
    - Test 3 `test_openings_request_excludes_after_to_date`: Seed a game with `played_at = datetime(2026, 4, 2, 0, 0, tzinfo=UTC)`. POST with `to_date: '2026-04-01'`. Game does NOT appear.
    - Test 4 `test_openings_request_no_date_filter`: Seed a game with arbitrary `played_at`. POST with both `from_date` and `to_date` omitted. Game appears (no date filter applied).
    - Test 5 `test_openings_request_422_on_from_after_to`: POST `/openings/positions` with `from_date: '2026-04-01', to_date: '2026-03-01'`. Response code is 422.
    - Test 6 `test_stats_get_422_on_from_after_to`: GET `/stats/global?from_date=2026-04-01&to_date=2026-03-01`. Response code is 422. (Different code path — Query-param + inline HTTPException.)
    - Test 7 (in test_insights_openings.py) `test_insights_blocked_when_from_date_set`: POST to the insights endpoint with `from_date: '2026-03-01'`. Response includes the blocking message containing the substring `"Clear Custom date range filter"`. Mirror the existing gating-test pattern in the file.
  </behavior>
  <action>
    Extend `tests/test_integration_routers.py` with the 6 backend integration tests above. Use existing game-seeding fixtures (search the file for the existing `seed_game` or equivalent helper; do NOT introduce a new factory unless one doesn't exist). For the 422 tests, assert on `response.status_code == 422` AND on a substring match in the response detail (e.g. `"from_date must be <= to_date"` per Plan 02 schema validator messages).

    Extend `tests/routers/test_insights_openings.py` with test 7 — the gating assertion. Copy the existing pattern from the file's other `_validate_full_history_filters` tests; only the trigger condition changes (was `recency != "all_time"`, now `from_date is not None or to_date is not None`).

    Run `uv run pytest tests/test_integration_routers.py tests/routers/test_insights_openings.py -x` to verify all 7 new tests pass.

    Implements the verification commitment from the ROADMAP. Addresses P92-R2 (boundary semantics) and the silent-regression risk closure for the insights gate (RESEARCH.md §Pitfall 3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_integration_routers.py tests/routers/test_insights_openings.py -x &amp;&amp; uv run ruff check tests/ &amp;&amp; uv run ty check tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_openings_request_from_date_inclusive\|def test_openings_request_to_date_end_of_day\|def test_openings_request_excludes_after_to_date\|def test_openings_request_no_date_filter\|def test_openings_request_422_on_from_after_to\|def test_stats_get_422_on_from_after_to" tests/test_integration_routers.py` returns 6 (one match per added test)
    - `grep -c "def test_insights_blocked_when_from_date_set" tests/routers/test_insights_openings.py` returns 1
    - `uv run pytest tests/test_integration_routers.py tests/routers/test_insights_openings.py -x` passes
    - `uv run ruff check tests/` clean
    - `uv run ty check tests/` zero errors
  </acceptance_criteria>
  <done>Backend boundary semantics + 422 + insights-gate updates fully integration-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Write 92-HUMAN-UAT.md script + CHANGELOG.md [Unreleased] entries</name>
  <files>.planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md, CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — confirm `## [Unreleased]` section exists with the standard `### Added` / `### Changed` / `### Removed` / `### Tests` subsections per CLAUDE.md §Changelog & Releases)
    - .planning/ROADMAP.md (Phase 92 §Verification — copy the bullets into the UAT script verbatim)
    - .planning/phases/92-custom-date-range-filter/92-CONTEXT.md (for D-01..D-19 reference and the user-facing behavior summary)
  </read_first>
  <action>
    **Step 1 — Write `.planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md`:** Per the project's "no dev DB reset" memory, design the UAT script to work against the existing dev DB. Sections:
    - **Setup**: Confirm dev DB is running (`docker compose -f docker-compose.dev.yml -p flawchess-dev ps`) and backend + frontend dev servers are up (`bin/run_local.sh`). Confirm at least one imported user with ≥ 50 games spanning multiple months exists.
    - **Scenario 1 — Preset parity (the 95% case)**: For each of the 8 existing presets ('All time', 'Past week', 'Past month', '3 months', '6 months', '1 year', '3 years', '5 years'), open Openings → pick the preset → record the WDL total. Compare against the same WDL numbers from a pre-deploy screenshot or from a parallel git-checkout of the prior commit. They should match for "All time"; for date-bounded presets they may differ ONLY at the UTC/local-day boundary (≤24h of games at the boundary may shift in/out — acceptable per D-16 and RESEARCH.md §Pitfall 2).
    - **Scenario 2 — Desktop Custom range**: Open Openings at desktop width. Click Recency Select → pick "Custom range…". Verify the dropdown closes and a Popover opens anchored to the Select trigger (no layout jump). Pick a known tournament window (e.g. dates of a known tournament the user played) — both bounds. Verify the popover auto-closes on the second pick, the trigger label shows the resolved range as "MMM d, yyyy – MMM d, yyyy", and the WDL count matches the expected tournament subset.
    - **Scenario 3 — Mobile Custom range**: Resize to mobile width (or use Chrome DevTools mobile emulation). Open the filter Drawer. Click Recency → "Custom range…". Verify a nested Drawer slides up from the bottom (NOT overlapping the FilterPanel Drawer fully — both should be visible). Single-month Calendar visible. Pick a range. Click Apply. Verify the nested Drawer closes, the FilterPanel Drawer remains open, the trigger label shows the resolved range. Test backdrop dismiss: open Custom range, pick `from` only, click outside the nested Drawer — verify the range is NOT committed (filters.recency remains the previous value).
    - **Scenario 4 — Switch back to preset**: With a custom range active, open Recency → pick "Past month". Verify the trigger label changes to "Past month", the WDL updates to the past month, and `customRange` is cleared (subsequent custom-range opens start fresh).
    - **Scenario 5 — Insights gating regression check**: With Recency set to "All time", verify the Endgame Insights and Opening Insights endpoints return a report. Then set a custom range (or any non-all-time preset) and verify the insights endpoint returns the gating message "Clear Custom date range filter" (per Plan 02 gate update). Confirms RESEARCH.md §Pitfall 3 fix.
    - **Scenario 6 — Reload behavior**: With any filter active, reload the page. Per `useFilterStore` being in-memory only (RESEARCH.md §Frontend State Architecture), filters reset to DEFAULT_FILTERS — this is unchanged from prior behavior. Verify no error states or stale-cache issues.
    - **Sign-off line**: At the bottom, list each scenario with a checkbox. The user types "approved" or describes issues for each.

    **Step 2 — CHANGELOG.md [Unreleased] entries:** Append under the existing `## [Unreleased]` section. Use these subsections (create them if absent):
    - `### Added`: `- Custom date range filter — pick arbitrary windows via a 9th "Custom range…" item in the Recency dropdown (Phase 92). Desktop opens a popover with a two-month range Calendar; mobile uses a nested bottom sheet with a single-month Calendar and Apply button.`
    - `### Changed`: `- API filter shape: the closed Recency string union (week / month / ... / all) on the wire is replaced by two optional ISO date params from_date / to_date. Frontend computes preset → dates in the user's local timezone. Internal LLM windows preserve existing semantics via fixed date offsets (Phase 92).`
    - `### Removed`: `- Bookmark time-series request no longer accepts a recency field (was unused at the UI call boundary — pre-work in Phase 92 D-19).`
    - `### Tests`: `- Added direct unit tests for app.repositories.query_utils.apply_game_filters covering from_date/to_date predicate semantics; extended integration tests for the +1-day inclusive upper bound, the 422 cross-field validator, and the insights LLM gate behavior (Phase 92).`

    Per CLAUDE.md §Changelog & Releases, this is the per-phase update at merge-to-main time. The milestone-level promotion to a versioned section happens at milestone close (separate ritual).
  </action>
  <verify>
    <automated>test -f .planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md &amp;&amp; grep -c "^## \[Unreleased\]" CHANGELOG.md | grep -q '^1$' &amp;&amp; grep -c "Custom date range" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md` exists
    - It contains the 6 scenarios listed above (`grep -c "^### Scenario " .planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md` returns 6)
    - It contains a checklist at the bottom with one checkbox per scenario
    - `CHANGELOG.md` has the `[Unreleased]` section with at least one bullet in each of `### Added`, `### Changed`, `### Removed`, `### Tests` subsections referencing Phase 92
    - `grep -c "Phase 92" CHANGELOG.md` returns ≥ 4 (one per new bullet)
    - `grep -c "Custom date range" CHANGELOG.md` returns ≥ 1
  </acceptance_criteria>
  <done>UAT script written; CHANGELOG updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the human UAT script</name>
  <what-built>The full Phase 92 stack: backend wire-format flip, frontend type/hook migration, shadcn Calendar primitive, two-step Select → Popover (desktop) / nested Drawer (mobile) Custom Range UI, boundary integration tests, CHANGELOG entries.</what-built>
  <how-to-verify>
    Open `.planning/phases/92-custom-date-range-filter/92-HUMAN-UAT.md` and run through all 6 scenarios. For each scenario, either mark the checkbox as passed or capture the issue. Pay particular attention to:
    1. Scenario 1 preset parity — WDL counts should match the pre-deploy baseline for "All time" and within ≤24h boundary fuzz for date-bounded presets
    2. Scenario 2 desktop popover anchoring — no layout jump when transitioning from Select dropdown to Popover
    3. Scenario 3 mobile nested Drawer — backdrop dismiss does NOT commit a half-set range (D-08)
    4. Scenario 5 insights gating — the blocking message contains "Clear Custom date range filter" (silent-regression check for RESEARCH.md §Pitfall 3)

    When complete, type "approved" to mark the phase done, or describe any failed scenarios.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → production code paths | Integration tests exercise the full router→service→repository→SQL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06-01 | Information Disclosure / Silent regression | insights LLM gate | mitigate | Integration test `test_insights_blocked_when_from_date_set` asserts the gate behavior; closes RESEARCH.md §Pitfall 3 risk. |
| T-92-06-02 | DoS | boundary-day games | accept | UTC fuzz documented in apply_game_filters and acknowledged in UAT Scenario 1; per D-16 / §Pitfall 2 accepted trade-off. |
</threat_model>

<verification>
- All 7 new tests pass
- UAT script complete and run
- CHANGELOG updated
- Pre-PR checklist (per CLAUDE.md): `uv run ruff format/check`, `uv run ty check`, `uv run pytest -x`, `cd frontend && npm run lint && npm test -- --run` all green at this point
</verification>

<success_criteria>
Phase 92 ships. The custom-range filter works end-to-end; the wire format change is integration-tested; UAT documents the visual verification; CHANGELOG records the change.
</success_criteria>

<output>
Create `.planning/phases/92-custom-date-range-filter/92-06-SUMMARY.md` when done
</output>
