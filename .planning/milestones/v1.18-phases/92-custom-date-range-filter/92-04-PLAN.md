---
phase: 92-custom-date-range-filter
plan: 04
type: execute
wave: 4
depends_on: [92-03]
files_modified:
  - frontend/package.json
  - frontend/components.json
  - frontend/src/components/ui/calendar.tsx
  - frontend/src/components/ui/drawer.tsx
autonomous: false
requirements: [P92-R4]
# Synthetic requirement IDs — see Plan 01 frontmatter for full scheme.
# This plan has a blocking checkpoint for package legitimacy verification (react-day-picker).
must_haves:
  truths:
    - "frontend/src/components/ui/calendar.tsx exists and was generated by the shadcn registry"
    - "react-day-picker is installed in frontend/package.json at the version verified via npm view"
    - "frontend/src/components/ui/drawer.tsx re-exports DrawerNested wrapping Drawer.NestedRoot from vaul"
    - "Calendar exports include the React component; tsc compiles cleanly"
  artifacts:
    - path: frontend/src/components/ui/calendar.tsx
      provides: "shadcn Calendar primitive with mode='range' support"
      contains: "Calendar"
      min_lines: 30
    - path: frontend/src/components/ui/drawer.tsx
      provides: "Drawer wrapper extended with DrawerNested export"
      contains: "DrawerNested"
  key_links:
    - from: frontend/src/components/ui/drawer.tsx
      to: vaul
      via: "DrawerNested wraps Drawer.NestedRoot for nested mobile sheets"
      pattern: "DrawerPrimitive\\.NestedRoot|NestedRoot"
---

<objective>
Install the shadcn Calendar primitive and extend the existing Drawer wrapper with a `DrawerNested` export for the mobile nested-drawer pattern that Plan 05 will use. Includes a blocking legitimacy checkpoint for `react-day-picker` per the package legitimacy gate protocol (`[ASSUMED]` disposition per RESEARCH.md §Package Legitimacy Audit).

Implements D-01 (Calendar install) and the Plan-02 caveat from RESEARCH.md §Pattern 2 line 349 (`DrawerNested` wrapper not currently exported); addresses synthetic requirement P92-R4 (infrastructure half — UI wiring is Plan 05).

Purpose: Stand up the UI primitives that Plan 05 needs (Calendar component + nested-drawer support). Splitting this from Plan 05 keeps the npx interactive install + checkpoint isolated; Plan 05 then runs fully autonomous against existing primitives.
Output: Two new/modified UI primitive files, react-day-picker dep present at a known-legitimate version.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/92-custom-date-range-filter/92-CONTEXT.md
@.planning/phases/92-custom-date-range-filter/92-RESEARCH.md
@.planning/phases/92-custom-date-range-filter/92-PATTERNS.md
@.planning/phases/92-custom-date-range-filter/92-03-SUMMARY.md
@CLAUDE.md
@frontend/components.json
@frontend/src/components/ui/drawer.tsx
@frontend/src/components/ui/popover.tsx

<interfaces>
<!-- Existing Drawer wrapper (frontend/src/components/ui/drawer.tsx:1-10): -->
<!--   import { Drawer as DrawerPrimitive } from "vaul" -->
<!--   function Drawer({ ...props }: ComponentProps<typeof DrawerPrimitive.Root>) { ... } -->
<!-- Target addition (per 92-PATTERNS.md §CustomRangeDrawer.tsx): -->
<!--   function DrawerNested({ ...props }: ComponentProps<typeof DrawerPrimitive.NestedRoot>) { -->
<!--     return <DrawerPrimitive.NestedRoot data-slot="drawer-nested" {...props} /> -->
<!--   } -->
<!-- And add DrawerNested to the export block. -->

<!-- Calendar (frontend/src/components/ui/calendar.tsx): new file, written by shadcn registry add. -->
<!-- Registry source (verified): https://ui.shadcn.com/r/styles/new-york-v4/calendar.json -->
<!-- Auto-installs react-day-picker@latest as a transitive dep. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy checkpoint for react-day-picker</name>
  <what-built>About to run `npx shadcn@latest add calendar` which installs `react-day-picker` (disposition `[ASSUMED]` per RESEARCH.md §Package Legitimacy Audit). The package was discovered via the shadcn registry JSON (authoritative source) but slopcheck did not run.</what-built>
  <how-to-verify>
    Verify the package on https://www.npmjs.com/package/react-day-picker matches:
    1. Author: github.com/gpbl (Giampaolo Bellavite — long-standing maintainer)
    2. Initial publish: 2017 or earlier (~8+ years)
    3. Weekly downloads: ≥ 1M (RESEARCH.md cites ~4M)
    4. Source repo: github.com/gpbl/react-day-picker exists and is active
    5. Latest version: matches what `cd frontend && npm view react-day-picker version` returns (expected 10.x per RESEARCH.md verification on 2026-05-21)

    If all 5 match, type "approved" to proceed with the install.
    If anything looks off (typosquat, abandoned, unexpected maintainer), type "rejected" and we will reassess (likely fall back to a native date input — D-02 would be revisited).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run shadcn add calendar + verify install + extend Drawer wrapper with DrawerNested</name>
  <files>frontend/package.json, frontend/src/components/ui/calendar.tsx, frontend/src/components/ui/drawer.tsx</files>
  <read_first>
    - frontend/components.json (verify the shadcn config exists)
    - frontend/src/components/ui/drawer.tsx (full file — note lines 1-10 for the existing Drawer wrapper shape and the export block at lines 92-98)
    - frontend/src/components/ui/popover.tsx (for cn() + data-slot pattern reference)
    - frontend/src/lib/theme.ts (for theme tokens — verify whether the post-install calendar.tsx uses theme tokens or raw hex)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§calendar.tsx and §CustomRangeDrawer.tsx sections)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Standard Stack lines 130-143)
  </read_first>
  <action>
    **Step 1 — Verify version (per RESEARCH.md line 140):** Run `cd frontend && npm view react-day-picker version` and confirm the major version is 10 (per 2026-05-21 verification). Record the resolved version.

    **Step 2 — Install Calendar:** Run `cd frontend && npx shadcn@latest add calendar`. This writes `frontend/src/components/ui/calendar.tsx`, adds `react-day-picker` to `frontend/package.json`, and may also touch `components.json` or `tsconfig.json` paths. Accept the install.

    **Step 3 — Audit calendar.tsx output:** Read the newly written `frontend/src/components/ui/calendar.tsx`. Verify:
    - It uses `cn(...)` from `@/lib/utils` and `data-slot="..."` conventions consistent with `popover.tsx` and `drawer.tsx`.
    - It uses theme tokens (`bg-primary`, `bg-accent`, etc.) for `range_start`/`range_middle`/`range_end` classNames — NOT raw hex. If it uses raw hex, replace with theme tokens per CLAUDE.md §Frontend (Theme constants).
    - Default font-size is `text-sm` or larger (CLAUDE.md text-sm floor). If any class is `text-xs` on visible day labels or month/year navigation, override via className overrides at the export wrapper to `text-sm`. The shadcn-emitted Calendar already passes a `className` prop through — adding a wrapper at this layer is fine.
    - The DayPicker render does NOT include a `data-testid` on day buttons. Plan 05 will add this via a custom `formatters.formatDay` or by patching the day-button render here. For this plan, just confirm the absence and note the patch target. If the registry emits a `Day` formatter slot, wrap it to add `data-testid={`calendar-day-${format(day, 'yyyy-MM-dd')}`}`.

    **Step 4 — Extend Drawer wrapper (frontend/src/components/ui/drawer.tsx):** Add a `DrawerNested` function between `Drawer` and `DrawerPortal` (around line 10), mirroring the existing wrapper shape:

    Add the function that wraps `DrawerPrimitive.NestedRoot` with `data-slot="drawer-nested"`, accepting all props via `React.ComponentProps<typeof DrawerPrimitive.NestedRoot>`. Add `DrawerNested` to the existing export block at lines 92-98 in alphabetical order (or matching the file's existing ordering). Per 92-PATTERNS.md §CustomRangeDrawer.tsx, this is Option 1 (the recommended approach over importing `NestedRoot` directly from `vaul` in Plan 05's CustomRangeDrawer.tsx).

    **Step 5 — Lint/format:** Run `cd frontend && npm run lint && npx tsc --noEmit` to verify everything compiles.

    Implements D-01 (Calendar install). Sets up the wrapper infrastructure for D-06 (mobile nested drawer) without yet using it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/ui/calendar.tsx` exists (`test -f frontend/src/components/ui/calendar.tsx` succeeds)
    - `grep -c "react-day-picker" frontend/package.json` returns ≥ 1
    - `grep -n "DrawerNested" frontend/src/components/ui/drawer.tsx` returns ≥ 2 matches (function declaration + export)
    - `grep -n "data-slot=\"drawer-nested\"" frontend/src/components/ui/drawer.tsx` returns 1 match
    - `grep -n "DrawerPrimitive\.NestedRoot" frontend/src/components/ui/drawer.tsx` returns 1 match
    - `cd frontend && npx tsc --noEmit` passes with zero errors
    - `cd frontend && npm run lint` passes
    - `cd frontend && npm test -- --run` passes (no regressions)
    - Reading `frontend/src/components/ui/calendar.tsx` shows no raw hex color codes (regex `#[0-9a-fA-F]{6}\b` returns 0 matches) — theme tokens only
    - The Calendar export accepts `mode="range"` prop (verify by reading the type or component signature — should pass through to DayPicker)
  </acceptance_criteria>
  <done>Calendar primitive installed and audited; DrawerNested wrapper exported; tsc + lint + tests clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain | npm install pulls react-day-picker into the bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04-SC | Tampering | react-day-picker | mitigate | Blocking human checkpoint verifies maintainer, age, downloads, source repo before install (per Package Legitimacy Gate protocol). `[ASSUMED]` disposition cannot auto-advance. |
| T-92-04-01 | Information Disclosure | calendar.tsx generated code | accept | Registry source is shadcn (authoritative); generated code uses theme tokens, no inline secrets. |
</threat_model>

<verification>
- Calendar primitive exists at the expected path
- react-day-picker resolved to a verified version (npm view check passed during Task 2 Step 1)
- DrawerNested wrapper exported from drawer.tsx
- tsc/lint/tests all green
</verification>

<success_criteria>
Plan 05 can compose `<Calendar mode="range">` inside `<PopoverContent>` (desktop) and inside `<DrawerNested><DrawerContent>` (mobile) without further primitive work.
</success_criteria>

<output>
Create `.planning/phases/92-custom-date-range-filter/92-04-SUMMARY.md` when done
</output>
