---
phase: 91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/game.py
  - alembic/versions/<new>_add_evals_completed_at_to_games.py
  - tests/test_migration_91_evals_completed_at.py
autonomous: true
requirements:
  - "Phase 91 Scope #1 (schema + partial index + backfill)"
  - "CONTEXT.md D-08 (backfill — corrected to COALESCE(imported_at, NOW()) per RESEARCH OQ-3)"
  - "CONTEXT.md D-10 (backfill runs at deploy time in single migration tx)"
  - "CONTEXT.md D-11 (partial index supports LIFO drain pick)"
tags:
  - schema
  - alembic
  - migration
  - eval-tracking

must_haves:
  truths:
    - "After upgrade, `games.evals_completed_at TIMESTAMPTZ NULL` column exists."
    - "Partial index `ix_games_evals_pending ON games(id) WHERE evals_completed_at IS NULL` exists."
    - "All pre-existing rows have `evals_completed_at IS NOT NULL` after backfill (zero NULL rows immediately post-migration)."
    - "Downgrade drops index then column with no errors."
    - "Migration backfill uses COALESCE(imported_at, NOW()) — NOT updated_at/created_at (D-08 corrected per RESEARCH OQ-3)."
    - "ORM `Game.evals_completed_at` mapped column matches DB column type (DateTime(timezone=True), nullable)."
    - "Locked decisions honoured: D-08 (backfill expression corrected to COALESCE(imported_at, NOW()) per RESEARCH OQ-3), D-10 (backfill runs at deploy time in a single migration transaction via deploy/entrypoint.sh), D-11 (partial index `ix_games_evals_pending` supports LIFO drain pick query)."
  artifacts:
    - path: "app/models/game.py"
      provides: "Game.evals_completed_at mapped column"
      contains: "evals_completed_at"
    - path: "alembic/versions/<hash>_add_evals_completed_at_to_games.py"
      provides: "Schema migration: column + partial index + backfill"
      contains: "ix_games_evals_pending"
    - path: "tests/test_migration_91_evals_completed_at.py"
      provides: "Up/down/backfill correctness test"
  key_links:
    - from: "alembic migration"
      to: "deploy/entrypoint.sh"
      via: "alembic upgrade head on container start"
      pattern: "alembic upgrade"
    - from: "Game model"
      to: "games table"
      via: "SQLAlchemy ORM mapped_column to TIMESTAMPTZ"
      pattern: "evals_completed_at"
---

<objective>
Add the schema column, partial index, and one-shot backfill that the rest of Phase 91 depends on.

Purpose: Establish the persistence layer for per-game Stockfish-eval completion tracking. Without this column the cold-drain coroutine has no queue, the coverage endpoint has nothing to count, and the hot lane has nothing to mark.

Output: New `games.evals_completed_at` column, new `ix_games_evals_pending` partial index, all existing rows backfilled to `COALESCE(imported_at, NOW())`, and a regression test asserting up/down/backfill correctness against the dev DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-CONTEXT.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-RESEARCH.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md
@CLAUDE.md
@app/models/game.py
@alembic/versions/20260504_add_benchmark_only_game_positions_indexes.py
@alembic/versions/20260512_024510_e925558020b9_add_extended_stats_for_opening_.py

<interfaces>
<!-- Anchor lines in app/models/game.py (verified via grep) -->
- `app/models/game.py:42` — `class Game(Base):` declaration
- `app/models/game.py:123` — existing `imported_at: Mapped[datetime.datetime] = mapped_column(nullable=False, server_default=func.now())`
  (Add `evals_completed_at` immediately after this column.)

<!-- Alembic head as of 2026-05-20 -->
Current head revision (down_revision for new migration): `e925558020b9`
(File: `alembic/versions/20260512_024510_e925558020b9_add_extended_stats_for_opening_.py`)

<!-- Partial index syntax convention -->
`alembic/versions/20260504_add_benchmark_only_game_positions_indexes.py` uses `postgresql_where="<predicate>"` string form. Match this exactly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Add evals_completed_at column to Game ORM model</name>
  <files>app/models/game.py</files>
  <read_first>
    - app/models/game.py (read full file, especially lines 1-50 imports + lines 110-140 timestamp-column region)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (section "app/models/game.py — add evals_completed_at column")
    - CLAUDE.md (Type safety section — ty compliance + Mapped annotations)
  </read_first>
  <behavior>
    - Test 1: Importing `Game` from `app.models.game` does not raise (ty/runtime sanity).
    - Test 2: `Game.__table__.columns['evals_completed_at']` exists, is nullable, and has `DateTime(timezone=True)` type.
    - Test 3: `inspect(Game).attrs.evals_completed_at` is a Mapped column with Python type `datetime | None`.
  </behavior>
  <action>
    Add a single new column declaration `evals_completed_at: Mapped[datetime.datetime | None] = mapped_column(sa.DateTime(timezone=True), nullable=True)` to the `Game` class in `app/models/game.py`, positioned immediately after the existing `imported_at` column (line 123 area). Use `sa.DateTime(timezone=True)` exactly (explicit import, not implicit) so it matches the TIMESTAMPTZ column the Alembic migration will create. Do NOT add `server_default` (column is intentionally nullable; cold drain or hot lane sets it). Do NOT touch any other column or method. Per D-11 the column is used as a "pending" flag — NULL = pending, set = completed. Run `uv run ty check app/` after the edit and ensure zero new errors.
  </action>
  <verify>
    <automated>uv run ty check app/models/game.py</automated>
    <automated>uv run python -c "from app.models.game import Game; from sqlalchemy import inspect; col = Game.__table__.columns['evals_completed_at']; assert col.nullable is True; assert col.type.timezone is True; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "evals_completed_at" app/models/game.py` returns at least one line.
    - `uv run ty check app/models/game.py` exits 0.
    - The column type metadata matches `DateTime(timezone=True)` and `nullable=True`.
    - No other column in `app/models/game.py` was modified (diff touches only inserted lines + possibly the `sqlalchemy as sa` import line).
  </acceptance_criteria>
  <done>ORM model has the new nullable timezone-aware column; ty passes; runtime import succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Create Alembic migration — column + partial index + backfill</name>
  <files>alembic/versions/&lt;timestamp&gt;_add_evals_completed_at_to_games.py</files>
  <read_first>
    - alembic/versions/20260504_add_benchmark_only_game_positions_indexes.py (postgresql_where string-form pattern)
    - alembic/versions/20260512_024510_e925558020b9_add_extended_stats_for_opening_.py (current head — for down_revision)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-RESEARCH.md (section "Schema Migration Design")
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (section for the migration file — full upgrade/downgrade body)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-CONTEXT.md (D-08 noting the column-name correction)
  </read_first>
  <action>
    Generate a new Alembic revision with `uv run alembic revision -m "add evals_completed_at to games"` (no `--autogenerate`; write the body by hand to keep the partial index and backfill explicit). Set `down_revision = "e925558020b9"`. In `upgrade()`: (1) `op.add_column("games", sa.Column("evals_completed_at", sa.DateTime(timezone=True), nullable=True))`. (2) `op.create_index("ix_games_evals_pending", "games", ["id"], unique=False, postgresql_where="evals_completed_at IS NULL")`. (3) `op.execute("UPDATE games SET evals_completed_at = COALESCE(imported_at, NOW()) WHERE evals_completed_at IS NULL")`. In `downgrade()`: `op.drop_index("ix_games_evals_pending", table_name="games", postgresql_where="evals_completed_at IS NULL")` then `op.drop_column("games", "evals_completed_at")`.
    IMPORTANT: Use `imported_at` (not `updated_at`/`created_at` — RESEARCH OQ-3 confirms `games` has no such columns; CONTEXT D-08's literal text is wrong, the corrected expression is locked in RESEARCH and CONTEXT). Add a module docstring referencing Phase 91 and SEED-023. Use string form `postgresql_where="evals_completed_at IS NULL"` exactly (matches 20260504 convention). Verify the file passes `uv run ruff check alembic/versions/<new>.py` and the project's standard `uv run alembic upgrade head` against the dev DB succeeds.
  </action>
  <verify>
    <automated>uv run ruff check alembic/versions/</automated>
    <automated>docker compose -f docker-compose.dev.yml -p flawchess-dev up -d &amp;&amp; uv run alembic upgrade head</automated>
    <automated>uv run python -c "import asyncio; from sqlalchemy import text; from app.core.database import async_session_maker; asyncio.run((lambda: (async def f(): \n    async with async_session_maker() as s: r = await s.execute(text(\"SELECT COUNT(*) FROM games WHERE evals_completed_at IS NULL\")); print('null_rows', r.scalar()))()))" 2>/dev/null || uv run python -c "import asyncio; from sqlalchemy import text; from app.core.database import async_session_maker\n\nasync def main():\n    async with async_session_maker() as s:\n        r = await s.execute(text('SELECT COUNT(*) FROM games WHERE evals_completed_at IS NULL'))\n        n = r.scalar()\n        assert n == 0, f'expected 0 null rows post-backfill, got {n}'\n        print('null_rows ok')\n\nasyncio.run(main())"</automated>
  </verify>
  <acceptance_criteria>
    - File matches glob `alembic/versions/*_add_evals_completed_at_to_games.py`.
    - File contains literal substring `postgresql_where="evals_completed_at IS NULL"`.
    - File contains literal substring `COALESCE(imported_at, NOW())`.
    - File does NOT contain `updated_at` or `created_at` anywhere in upgrade()/downgrade() (grep returns zero matches).
    - `down_revision: Union[str, None] = "e925558020b9"`.
    - `uv run alembic upgrade head` succeeds against dev DB.
    - After upgrade, `SELECT COUNT(*) FROM games WHERE evals_completed_at IS NULL` returns 0.
    - After upgrade, `\d games` (or `information_schema`) shows the index `ix_games_evals_pending`.
    - `uv run alembic downgrade -1` then `uv run alembic upgrade head` round-trips without error.
  </acceptance_criteria>
  <done>Migration creates column + partial index, backfills via COALESCE(imported_at, NOW()), down-migrates cleanly, leaves zero pending rows immediately post-upgrade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: Add migration round-trip + backfill correctness test</name>
  <files>tests/test_migration_91_evals_completed_at.py</files>
  <read_first>
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-VALIDATION.md (Per-Task Verification Map — first row)
    - tests/ (browse for an existing migration- or schema-level integration test as a fixture analog; if none, model on async db-fixture style used elsewhere in tests/)
    - alembic/versions/<new>_add_evals_completed_at_to_games.py (just created)
  </read_first>
  <behavior>
    - Test A `test_migration_adds_column_and_index`: After `alembic upgrade head`, the `games` table has column `evals_completed_at` (timezone-aware, nullable) AND the index `ix_games_evals_pending` exists with predicate `(evals_completed_at IS NULL)` (queried via `pg_indexes.indexdef`).
    - Test B `test_backfill_leaves_no_pending_rows`: Pre-insert 3 rows into `games` (use minimal fixture user) BEFORE running migration, then run migration; assert `SELECT COUNT(*) FROM games WHERE evals_completed_at IS NULL` returns 0 and that all three rows have `evals_completed_at = imported_at` (i.e. COALESCE picked the imported_at branch).
    - Test C `test_downgrade_removes_column_and_index`: After `alembic downgrade -1`, `evals_completed_at` column is absent (`information_schema.columns` query returns 0 rows) and `ix_games_evals_pending` is absent (`pg_indexes` query returns 0 rows).
    - Test D `test_backfill_uses_now_when_imported_at_null`: Insert a row with `imported_at = NULL` (use raw SQL since the ORM has NOT NULL constraint; bypass via direct INSERT … OVERRIDING SYSTEM VALUE or temporary ALTER); run migration; assert `evals_completed_at` is non-null and >= NOW() - 5s (COALESCE NOW() branch). If bypassing NOT NULL is infeasible, skip this test with a comment referencing R-04.
  </behavior>
  <action>
    Create `tests/test_migration_91_evals_completed_at.py` as a pytest-asyncio module. Use the project's standard async DB session fixture (mirror `tests/services/` patterns — search for `@pytest.fixture` returning `AsyncSession`). For each test, drive alembic programmatically via `alembic.command.upgrade(cfg, "head")` / `alembic.command.downgrade(cfg, "-1")` using `from alembic.config import Config; cfg = Config("alembic.ini")` so the test doesn't shell out. Query `pg_indexes` and `information_schema.columns` via `text()` + `session.execute()`. Add module-level marker `pytestmark = pytest.mark.asyncio`. Tests must be DESTRUCTIVE-SAFE: each starts from `alembic upgrade head` baseline and downgrades+upgrades within the test, OR runs on a per-test transactional rollback fixture if one exists. If your project uses a session-scoped DB that already has the migration applied, structure tests as `downgrade -> assert -> upgrade -> assert`.
    No magic numbers — extract `EXPECTED_NULL_COUNT_POST_BACKFILL = 0` and `NOW_TOLERANCE_SECONDS = 5` as module constants per CLAUDE.md.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_91_evals_completed_at.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_migration_91_evals_completed_at.py` exists.
    - File contains at least three test functions matching `test_migration_adds_column_and_index`, `test_backfill_leaves_no_pending_rows`, `test_downgrade_removes_column_and_index`.
    - `uv run pytest tests/test_migration_91_evals_completed_at.py -x` exits 0.
    - `grep -c "^EXPECTED_NULL_COUNT_POST_BACKFILL\|^NOW_TOLERANCE_SECONDS" tests/test_migration_91_evals_completed_at.py` returns >= 2 (constants extracted).
    - `grep -v '^#' tests/test_migration_91_evals_completed_at.py | grep -cE '\b(updated_at|created_at)\b'` returns 0 (no reference to non-existent columns).
  </acceptance_criteria>
  <done>Pytest suite for the migration passes against dev DB with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic CLI → DB | DDL + backfill UPDATE run inside the migration; trusted (deploy-time, no user input) |
| ORM (Game model) → DB | Mapped column generates parameterised SELECT/UPDATE; trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | Migration backfill UPDATE | accept | DDL transaction is atomic; partial-write impossible. Pre-Phase-91 rows are fully evaluated by D-08 contract — backfill is one-shot idempotent (re-running the UPDATE matches no rows because WHERE clause excludes already-set rows). |
| T-91-02 | Information Disclosure | New column visible in API responses | mitigate | `evals_completed_at` is operational metadata; it MUST NOT be added to any `response_model` schema in this plan. Phase 91 deliberately exposes ONLY the aggregate counts via `/imports/eval-coverage` (plan 91-04). |
| T-91-03 | Denial of Service | Backfill UPDATE on ~150k rows | accept | RESEARCH R-03: timing estimate 2-5 s on current scale; runs at deploy time before Uvicorn accepts traffic. No user impact. Future-proof: revisit batched backfill if row count grows >1M. |
| T-91-04 | Tampering (D-08 deviation) | Backfill expression COALESCE(imported_at, NOW()) | mitigate | RESEARCH OQ-3 + this plan's grep gate enforce that neither `updated_at` nor `created_at` appears in the migration body (acceptance_criteria check). |
</threat_model>

<verification>
- `uv run ty check app/ tests/` passes with zero errors.
- `uv run alembic upgrade head` succeeds on a freshly-reset dev DB.
- `uv run pytest tests/test_migration_91_evals_completed_at.py -x` passes.
- `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips with no errors.
- Post-upgrade `SELECT COUNT(*) FROM games WHERE evals_completed_at IS NULL` returns 0.
- `\d+ games` shows column `evals_completed_at timestamp with time zone` + index `ix_games_evals_pending`.
</verification>

<success_criteria>
Plan complete when:
- ORM model has `evals_completed_at` mapped column (ty-clean).
- Alembic migration creates column + partial index + backfills with `COALESCE(imported_at, NOW())`.
- Migration test asserts upgrade adds artifacts, backfill leaves zero NULL rows, downgrade cleanly removes both.
- Full backend test suite remains green: `uv run pytest -x`.
- No use of `updated_at` or `created_at` in any new file (grep gate).
</success_criteria>

<output>
Create `.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-01-SUMMARY.md` when done. Record: revision hash, final partial-index name, backfill row count observed on dev DB, any rollback-test edge cases.
</output>
