---
phase: 88.3-endgame-stats-viz-refinements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
  - frontend/src/components/charts/EndgameOverallConnectorArrows.tsx
  - frontend/src/components/charts/EndgameOverallCard.tsx
  - frontend/src/components/charts/EndgameOverallEntryCard.tsx
  - frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "Endgame Overall Performance renders as one responsive bordered card (no arrow-flow, no empty black flanking boxes)"
    - "Desktop shows two columns separated by a vertical divider; mobile stacks them with horizontal dividers"
    - "Column 1 = Games without Endgame / Games with Endgame; Column 2 = Eval at Endgame Entry / Endgame Score Differences"
    - "All metric/chart content is unchanged (pure layout reorganization)"
    - "knip reports 0 issues after EndgameOverallConnectorArrows.tsx is deleted and its imports removed"
  artifacts:
    - path: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      provides: "2-column responsive card layout, no ConnectorArrows, no useRef"
      contains: "charcoal-texture"
  key_links:
    - from: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      to: "EndgameOverallCard / EndgameOverallEntryCard / EndgameOverallScoreGapRow"
      via: "two flex-1 columns with border-t / w-px dividers"
      pattern: "flex-1"
---

<objective>
SC-3: Replace the arrow-flow layout and empty black flanking boxes in "Endgame Overall Performance" with one responsive bordered card. Desktop: two equal-height columns separated by a vertical divider. Mobile: stacked columns with horizontal dividers. Column 1: "Games without Endgame" / "Games with Endgame". Column 2: "Eval at Endgame Entry" / "Endgame Score Differences". Both columns carry exactly 2 sections → equal height by construction. Pure layout reorganization — no metric or chart content change. Delete the now-orphaned `EndgameOverallConnectorArrows.tsx` and keep knip clean.

Purpose: The arrow metaphor + empty flanking boxes add cognitive load and read as broken content (decisions note Thread 6). The 2+2 arrangement was chosen specifically to balance column heights.
Output: Rewritten `EndgameOverallPerformanceSection.tsx`, deleted `EndgameOverallConnectorArrows.tsx`, stripped inner `charcoal-texture` wrappers on the two sub-cards, updated layout tests, knip clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/notes/endgame-stats-viz-decisions.md
@.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-RESEARCH.md
@.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-PATTERNS.md

<interfaces>
Survives unchanged (data-derivation logic, props, sub-component bodies):
- `EndgameOverallCard` (EndgameOverallCard.tsx) — WDL bar + score bullet; takes a `tileTestId` prop
- `EntryCard` (EndgameOverallEntryCard.tsx) — two bullet rows; renders `data-testid="tile-at-endgame-entry"`
- `ScoreGapRow` (EndgameOverallScoreGapRow.tsx) — reusable score-gap bullet row
- `EndgameOverallShared.ts` constants / `deriveLevel()`
All gap-zone color / prop calculations in EndgameOverallPerformanceSection stay as-is —
only the JSX layout container changes.

Divider class precedent (EndgameTimePressureCard.tsx line ~483):
  <div className="border-t border-border/40" aria-hidden="true" />
Vertical desktop divider: <div className="hidden lg:block w-px bg-border/40 mx-6" aria-hidden="true" />
Outer card shell: className="charcoal-texture rounded-md p-4"

testids that MUST survive (move to replacement bare divs when inner wrappers are stripped):
  endgame-overall-performance-section, tile-games-without-endgame,
  tile-games-with-endgame, tile-at-endgame-entry, endgame-score-differences

knip baseline: 0 issues. knip.json uses ignoreExportsUsedInFile: true.
`ConnectorArrows` is exported only from EndgameOverallConnectorArrows.tsx and imported
only at EndgameOverallPerformanceSection.tsx line ~42. `EndgameMetricsSection.tsx`
references it only in comments (no import). `useRef` in
EndgameOverallPerformanceSection.tsx is used solely for `gridRef` → ConnectorArrows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the layout to a 2-column responsive card and delete ConnectorArrows</name>
  <files>frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/charts/EndgameOverallConnectorArrows.tsx, frontend/src/components/charts/EndgameOverallCard.tsx, frontend/src/components/charts/EndgameOverallEntryCard.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (file under change — current 3-col `lg:grid-cols-3` grid, `useRef`/`gridRef`, `relative`, `<ConnectorArrows .../>`, the prop-derivation logic that must stay)
    - frontend/src/components/charts/EndgameOverallConnectorArrows.tsx (file to delete — confirm it exports only `ConnectorArrows`)
    - frontend/src/components/charts/EndgameOverallCard.tsx (the inner `charcoal-texture rounded-md p-4` wrapper to strip, ~line 84, `tileTestId` prop placement)
    - frontend/src/components/charts/EndgameOverallEntryCard.tsx (its outer `charcoal-texture` wrapper + `tile-at-endgame-entry` testid placement)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (analog: `border-t border-border/40` divider class ~line 483 and `charcoal-texture rounded-md p-4` outer card shell)
  </read_first>
  <action>
    Delete the file `frontend/src/components/charts/EndgameOverallConnectorArrows.tsx` entirely. In `EndgameOverallPerformanceSection.tsx`: remove `import { ConnectorArrows } from './EndgameOverallConnectorArrows';`; remove the `useRef` import (no other use in the file); remove `const gridRef = useRef<HTMLDivElement>(null);`; remove the `ref={gridRef}` prop and the `relative` class (it only anchored the absolute ConnectorArrows overlay) and the entire `grid grid-cols-1 lg:grid-cols-3 ...` grid class from the container; remove the `<ConnectorArrows ... />` JSX block. Keep ALL prop-derivation / gap-zone-color logic exactly as-is. Replace the grid container with: an outer `<div className="charcoal-texture rounded-md p-4 mt-2">` inside the existing `<section data-testid="endgame-overall-performance-section">`, containing `<div className="flex flex-col lg:flex-row">` with — Column 1 `<div className="flex-1 flex flex-col gap-4">` holding the "Games without Endgame" `EndgameCard`, a `<div className="border-t border-border/40" aria-hidden="true" />` divider, then the "Games with Endgame" `EndgameCard`; a desktop-only vertical divider `<div className="hidden lg:block w-px bg-border/40 mx-6" aria-hidden="true" />`; a mobile-only horizontal divider `<div className="block lg:hidden border-t border-border/40 my-4" aria-hidden="true" />`; Column 2 `<div className="flex-1 flex flex-col gap-4">` holding the `EntryCard`, a `border-t` divider, then the existing `<div data-testid="endgame-score-differences">` block (its `<h3>` + the two `ScoreGapRow`s) unchanged. Strip the inner `charcoal-texture rounded-md p-4` wrapper from `EndgameOverallCard.tsx` and `EndgameOverallEntryCard.tsx`, replacing each with a bare `<div data-testid={...}>` (or equivalent) that PRESERVES the existing testid (`tileTestId` for the cards, `tile-at-endgame-entry` for the entry card) so the texture is not double-nested under the new outer card. Do not change any metric, bullet, WDL bar, or chart content. Keep the descriptive `<p className="text-sm text-muted-foreground">` line. Honor CLAUDE.md: theme/border tokens only (`border-border/40`, `bg-border/40` — no hard-coded colors), `aria-hidden="true"` on the decorative dividers, `text-sm` floor for any visible label, every surviving interactive element keeps its `data-testid`, semantic `<section>` retained.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameOverallPerformanceSection && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/charts/EndgameOverallConnectorArrows.tsx` no longer exists (`test ! -e ...` succeeds)
    - `grep -rn "ConnectorArrows\|EndgameOverallConnectorArrows" frontend/src --include=*.tsx --include=*.ts` returns only comment references in `EndgameMetricsSection.tsx` (no import / JSX usage anywhere)
    - `grep -n "useRef\|gridRef\|grid-cols-3" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` returns nothing
    - `grep -n "charcoal-texture\|flex-1\|border-t border-border/40\|w-px bg-border/40" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` shows the new card shell, two `flex-1` columns, and both divider variants
    - `grep -c "charcoal-texture" frontend/src/components/charts/EndgameOverallCard.tsx frontend/src/components/charts/EndgameOverallEntryCard.tsx` shows 0 in each (inner wrappers stripped — no double texture)
    - `cd frontend && npm run knip` reports 0 issues (88.3-H)
    - `cd frontend && npm run lint` clean and `cd frontend && npm run build` succeeds (no orphaned `useRef`/import; ty/tsc passes)
  </acceptance_criteria>
  <done>Section renders as one responsive 2-column card with theme dividers; ConnectorArrows deleted and fully de-referenced; inner texture wrappers stripped; knip 0; no metric/chart content changed.</done>
</task>

<task type="auto">
  <name>Task 2: Update layout tests for the 2-column card and surviving testids</name>
  <files>frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx (file under change — existing testid assertions, the `beforeAll` matchMedia/ResizeObserver mocks to keep)
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (the rewritten layout from Task 1, to assert against)
  </read_first>
  <action>
    Update the test so it asserts the new structure: (a) all four testids still resolve — `endgame-overall-performance-section`, `tile-games-without-endgame`, `tile-games-with-endgame`, `tile-at-endgame-entry`, `endgame-score-differences` (Pitfall 5); (b) the old grid is gone — `section.querySelector('.lg\\:grid-cols-3')` is null; (c) the outer `charcoal-texture` card exists within the section; (d) ConnectorArrows is gone — no absolute-positioned aria-hidden SVG overlay (e.g. assert `document.querySelector('[class*="absolute inset-0"]')` is null, or the equivalent the old overlay used). Keep the existing `beforeAll` `matchMedia` and `ResizeObserver` mocks (EntryCard/EndgameCard may touch matchMedia via popovers). Remove or rewrite any assertion that depended on the deleted ConnectorArrows / 3-column grid. Do not assert on metric values (content is unchanged and already covered elsewhere).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameOverallPerformanceSection</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run EndgameOverallPerformanceSection` passes (88.3-F, 88.3-G)
    - Test asserts all 5 surviving testids resolve and that `.lg:grid-cols-3` and the ConnectorArrows overlay are absent
    - No test references `ConnectorArrows` or `gridRef`
    - `cd frontend && npm run lint` clean
  </acceptance_criteria>
  <done>Layout regression tests assert the 2-column card structure, surviving testids, and the absence of grid/ConnectorArrows (88.3-F, 88.3-G).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan is a pure CSS/JSX layout reorganization plus deletion of a dead client-side overlay component. No new inputs, endpoints, parameters, or data flow; no metric/data change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.3-03 | — | client-side layout reorg / dead-code deletion | accept | No security-relevant surface — client-side visualization of already-authorized, already-rendered stats data; no new inputs, endpoints, or trust boundaries. Deleting `EndgameOverallConnectorArrows.tsx` removes code, adding no surface. No package installs. |

No high-severity threats. Block-on threshold (high) not reached.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameOverallPerformanceSection` green
- `cd frontend && npm run knip` reports 0 issues (ConnectorArrows fully removed)
- `cd frontend && npm run lint` clean and `cd frontend && npm run build` succeeds
- Manual (UAT, recorded in 88.3-VALIDATION.md): at desktop and 375px — single bordered card, two balanced columns desktop / stacked mobile with the correct dividers, no arrow metaphor, no empty flanking boxes, no double-texture
</verification>

<success_criteria>
SC-3 satisfied: one responsive 2-column card replaces the arrow-flow + empty boxes; columns balanced by the 2+2 arrangement; metric/chart content unchanged; ConnectorArrows deleted; knip clean; layout tests updated.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-03-SUMMARY.md` when done.
</output>
