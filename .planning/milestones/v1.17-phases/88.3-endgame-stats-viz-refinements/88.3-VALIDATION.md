---
phase: 88.3
slug: endgame-stats-viz-refinements
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-18
---

# Phase 88.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (frontend) |
| **Config file** | `frontend/vitest.config.ts` |
| **Quick run command** | `cd frontend && npm test` |
| **Full suite command** | `cd frontend && npm test && npm run lint && npm run knip` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npm test`
- **After every plan wave:** Run `cd frontend && npm test && npm run lint && npm run knip`
- **Before `/gsd:verify-work`:** Full suite must be green (tests + lint + knip + `npm run build`)
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 88.3-01-01 | 01 | 1 | SC-1 | — / — | N/A (frontend viz, no security surface) | unit | `cd frontend && npm test -- inactivityGaps` | ❌ W0 | ⬜ pending |
| 88.3-02-01 | 02 | 1 | SC-2 | — / — | N/A | unit | `cd frontend && npm test -- EndgameEloTimeline` | ✅ | ⬜ pending |
| 88.3-03-01 | 03 | 1 | SC-3 | — / — | N/A | unit + knip | `cd frontend && npm test && npm run knip` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Plan/Wave/Task IDs above are indicative; the planner finalizes them in PLAN.md and the executor reconciles this map.*

---

## Wave 0 Requirements

- [ ] `frontend/src/<...>/inactivityGaps.test.ts` — unit tests for the shared `computeInactivityGaps(sortedDates, thresholdDays)` helper (new file; see RESEARCH.md)

*All other phase behaviors are covered by existing frontend test infrastructure + knip/lint/build gates.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Break-glyph + "≈Ny inactive" label is legible and uncrowded at 375px (no overlap with axis ticks, respects text-sm floor / tooltip exception) | SC-1 | Visual rendering / responsive layout cannot be asserted reliably in unit tests | Open `/endgames/stats` at 375px width; confirm zigzag/double-slash glyph + compact label render at each inactivity gap on both "Endgame Score Gap over Time" and "Endgame ELO Timeline"; no horizontal scroll; ticks not overlapped |
| ELO Timeline first-render shows only the single most-active series; other series toggle on via existing legend | SC-2 | First-render default + interactive toggle behavior is best confirmed visually | Load `/endgames/stats` with ≥2 (platform×TC) series; confirm exactly one (most-active) series visible initially; toggle others via legend; confirm single-series account is unchanged |
| Endgame Overall Performance renders as one responsive 2-column card (desktop vertical divider / mobile stacked horizontal dividers), equal column heights, no arrow-flow / empty black boxes | SC-3 | Equal-height-by-construction and responsive divider layout are visual properties | Open `/endgames/stats` desktop + 375px; confirm single bordered card, 2 balanced columns desktop / stacked mobile, no arrow metaphor, no empty flanking boxes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
