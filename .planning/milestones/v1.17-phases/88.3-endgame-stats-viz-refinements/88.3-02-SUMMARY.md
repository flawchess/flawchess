---
phase: 88.3-endgame-stats-viz-refinements
plan: "02"
subsystem: frontend-charts
tags: [elo-timeline, default-visibility, recharts, regression-tests]
dependency_graph:
  requires: [inactivity-gap-annotations-SC1]
  provides: [elo-timeline-single-series-default-SC2]
  affects: [EndgameEloTimelineSection]
tech_stack:
  added: []
  patterns: [MAX_DEFAULT_VISIBLE constant, computeDefaultHidden ranking]
key_files:
  created: []
  modified:
    - frontend/src/components/charts/EndgameEloTimelineSection.tsx
    - frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx
decisions:
  - MAX_DEFAULT_VISIBLE lowered from 3 to 1; computeDefaultHidden logic and MIN_ACTIVE_WEEKS_RATIO unchanged
  - Existing default-hidden describe block tests updated to reflect cap=1 behavior (previously verified cap=3)
metrics:
  duration_minutes: 10
  completed_date: "2026-05-18"
  tasks_completed: 1
  files_changed: 2
---

# Phase 88.3 Plan 02: ELO Timeline Single-Series Default (SC-2) — Summary

`MAX_DEFAULT_VISIBLE` lowered from 3 to 1 in `EndgameEloTimelineSection.tsx`: on first render the ELO Timeline shows only the single most-active (platform x TC) series, keeping the chart readable when users have up to 8 overlaid combos.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Lower MAX_DEFAULT_VISIBLE to 1 and add regression tests | 532b98a6 | EndgameEloTimelineSection.tsx, EndgameEloTimelineSection.test.tsx |

## What Was Built

**`EndgameEloTimelineSection.tsx`:** Single constant change — `MAX_DEFAULT_VISIBLE = 3` changed to `MAX_DEFAULT_VISIBLE = 1`. The comment in the `if (combos.length <= MAX_DEFAULT_VISIBLE)` guard block updated to reflect threshold 1. No other code changes: `computeDefaultHidden()`, `MIN_ACTIVE_WEEKS_RATIO = 0.33`, the legend toggle (`hiddenKeys: Set<string>`), the data-change state reset, and the 88.3-01 gap-annotation block all remain exactly as written by plan 01.

**`EndgameEloTimelineSection.test.tsx`:** Two-pronged test update:

1. New `describe('MAX_DEFAULT_VISIBLE = 1', ...)` block with two regression cases:
   - Case (a): 3-combo payload where only the most-active combo (highest summed `per_week_total_games`) is visible on first render; the other two are in `hiddenKeys`/`aria-pressed=false` and togglable via legend.
   - Case (b): 1-combo payload where nothing is hidden (single-series auto-behaviour unchanged — `ranked.slice(0, 1)` = [that combo], `hidden = {}`).

2. Existing "default-hidden filter" and "3-line + signed-band layout" describe blocks updated to reflect cap=1 behavior (the 22 previously passing tests were all affected by the constant change and needed their expected counts/states adjusted).

## Test Results

- `EndgameEloTimelineSection.test.tsx`: 22 tests, all passing
  - Pre-existing rename, gradient, layout, and default-hidden tests: updated and passing
  - New `MAX_DEFAULT_VISIBLE = 1` describe: 2 new tests, passing
  - Carried-over `gap annotations` describe (88.3-01): 2 tests, passing
- Lint: clean
- Build: passes

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Existing tests expected cap=3 behavior and would fail with cap=1**

- **Found during:** Task 1 (pre-emptive analysis before running tests)
- **Issue:** The "3-line + signed-band layout" describe block expected 6 lines (2×3) and 2 areas for the 2-combo `buildResponse()` fixture. With `MAX_DEFAULT_VISIBLE = 1`, only 1 combo is visible on first render (lichess_rapid, which has more total games), so the counts are 3 lines and 1 area. The "default-hidden filter" describe block similarly expected 3 visible in a 5-combo test, and the aria-pressed/opacity assertions in the layout tests needed inversion.
- **Fix:** Updated all affected tests to reflect cap=1 behavior. Added assertions for the newly-hidden combo (chess_com_blitz) in the layout tests.
- **Files modified:** `EndgameEloTimelineSection.test.tsx`
- **Commit:** 532b98a6

**2. [Rule 3 - Blocking] Worktree node_modules absent — tests could not find vitest**

- **Found during:** Task 1 first test run
- **Issue:** No `node_modules` in the worktree's `frontend/` directory; vitest not found.
- **Fix:** Created symlink `frontend/node_modules -> /home/aimfeld/Projects/Python/flawchess/frontend/node_modules` (same worktree pattern as 88.3-01).
- **Files modified:** none (filesystem-level fix only)

## Known Stubs

None. The constant change is immediately effective — no wiring or data-source changes needed.

## Threat Flags

None. Client-side constant change only; no new trust boundaries, network endpoints, auth paths, or schema changes.

## Self-Check: PASSED

- `frontend/src/components/charts/EndgameEloTimelineSection.tsx` — FOUND, `MAX_DEFAULT_VISIBLE = 1` present, `= 3` absent
- `frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx` — FOUND, 22 tests passing
- `.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-02-SUMMARY.md` — FOUND
- Commit 532b98a6 — FOUND
