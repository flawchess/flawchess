---
status: partial
phase: 88.3-endgame-stats-viz-refinements
source: [88.3-VERIFICATION.md]
started: 2026-05-18T19:52:28Z
updated: 2026-05-18T23:05:00Z
notes: "Re-issued after SC-4 gap closure (88.3-04). Prior GAP-1 (Palmtree glyph + larger font) and GAP-2 (6-chart rollout via shared helper) are RESOLVED — verifier score 9/9 -> 13/13, all automated must-haves pass. Remaining items are visual-only confirmations against real user data."
---

## Current Test

[awaiting human testing]

## Tests

### 1. Palmtree glyph + compact label legibility at 375px (both endgame charts)
expected: At a 375px viewport, on `/endgames/stats` for a user with a >56-day gap between game weeks, both "Endgame Score Gap over Time" and "Endgame ELO Timeline" render a dashed break ReferenceLine with a Palmtree glyph (BREAK_LABEL_GLYPH_SIZE=22px) plus a compact label (e.g. "1.1y" or "3mo"). Label and glyph stay legible inside the chart canvas, do not overlap axis ticks, and trigger no horizontal scroll.
result: [pending]

### 2. ELO Timeline first-render series selection (real multi-combo data)
expected: For a user with 2+ active (platform × TC) combos, the ELO Timeline shows only the single most-active series on first render; all other combos are dimmed/line-through in the legend and become visible when clicked. No tabs. A single-combo user sees unchanged behaviour (all visible).
result: [pending]

### 3. Endgame Overall Performance 2-column card visual
expected: One responsive bordered card. Desktop (≥1024px): two equal-height columns separated by a vertical divider — Col 1 "Games without Endgame" above "Games with Endgame", Col 2 "Eval at Endgame Entry" above "Endgame Score Differences". Mobile (375px): columns stack with horizontal dividers. No arrow-flow overlay, no empty black flanking boxes, no double texture. Metric/chart content unchanged.
result: [pending]

### 4. Palmtree annotation on the 4 newly wired charts
expected: For a user with >56-day inactive stretches, a Palmtree glyph + compact label appears at the gap on all four newly wired charts: Bookmarked Openings Score over Time (`/openings` bookmarked view), Chess.com Rating + Lichess Rating (`/stats`), and Average Clock Gap over Time (`/endgames/stats`). No crash or horizontal overflow; charts with no gap show no annotation.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

None — GAP-1 (Palmtree glyph + enlarged break-label font) and GAP-2 (extract shared helper + roll out to all 6 ordinal-axis timeline charts) were closed by plan 88.3-04. Verifier re-verification: score 9/9 → 13/13, all SC-4 automated must-haves pass, no regressions to SC-1/2/3. Remaining items are visual-only confirmations, not gaps.
