---
phase: 88.3-endgame-stats-viz-refinements
plan: 02
type: execute
wave: 2
depends_on: ["88.3-01"]
files_modified:
  - frontend/src/components/charts/EndgameEloTimelineSection.tsx
  - frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "On first render the ELO Timeline shows only the single most-active (platform × TC) series"
    - "All other series remain user-toggleable via the existing legend (no tabs added)"
    - "A 1-combo payload shows nothing hidden (single-series auto-behaviour unchanged)"
  artifacts:
    - path: "frontend/src/components/charts/EndgameEloTimelineSection.tsx"
      provides: "MAX_DEFAULT_VISIBLE lowered to 1; computeDefaultHidden logic unchanged"
      contains: "MAX_DEFAULT_VISIBLE = 1"
  key_links:
    - from: "frontend/src/components/charts/EndgameEloTimelineSection.tsx"
      to: "computeDefaultHidden / hiddenKeys state"
      via: "MAX_DEFAULT_VISIBLE constant"
      pattern: "MAX_DEFAULT_VISIBLE\\s*=\\s*1"
---

<objective>
SC-2: On first render, the "Endgame ELO Timeline" chart shows only the single most-active (platform × TC) series. All other series stay user-toggleable through the existing legend. No tabs are introduced. The auto-behaviour when only one series exists is unchanged.

Purpose: 2 platforms × 4 TCs = up to 8 overlaid series make the first render unreadable (decisions note Thread 3). Defaulting to the most-active series + the existing toggle is the agreed minimal fix.
Output: One constant change (`MAX_DEFAULT_VISIBLE` 3 → 1) plus added regression tests for the multi-combo and single-combo cases.

Wave note: This plan modifies `EndgameEloTimelineSection.tsx`, which plan 88.3-01 also modifies (gap annotations). To avoid a same-wave file conflict, this plan runs in Wave 2 and depends on 88.3-01. The two changes are otherwise logically independent — apply the constant change cleanly on top of 01's gap-annotation edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/notes/endgame-stats-viz-decisions.md
@.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-RESEARCH.md
@.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-PATTERNS.md
@.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-01-SUMMARY.md

<interfaces>
Existing behaviour to preserve (do not refactor):
- `MAX_DEFAULT_VISIBLE` (currently 3) caps how many combos are visible on first render.
- `MIN_ACTIVE_WEEKS_RATIO = 0.33` — unchanged.
- `computeDefaultHidden()` ranks combos by total games among active-weeks-qualified
  combos, keeps the top MAX_DEFAULT_VISIBLE visible, hides the rest. Logic unchanged;
  only the cap changes. With 1 combo, `ranked.slice(0, 1)` = [that combo],
  `hidden = {}` — correct, the comment-only `if (combos.length <= MAX_DEFAULT_VISIBLE)`
  block falls through and is harmless at threshold 1.
- The legend/toggle (`hiddenKeys: Set<string>` + `setHiddenKeys`) and the state reset
  on data change are unchanged.
- 88.3-01 added gap-annotation `ReferenceLine`s + a `computeInactivityGaps` import to
  this file; leave those edits intact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lower MAX_DEFAULT_VISIBLE to 1 and add regression tests</name>
  <files>frontend/src/components/charts/EndgameEloTimelineSection.tsx, frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameEloTimelineSection.tsx (file under change — `MAX_DEFAULT_VISIBLE` near line 110, `computeDefaultHidden()` lines ~113-146, the comment-only single-series block; note 88.3-01's gap-annotation edits are already present)
    - frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx (analog: self — existing describe nesting and data-builder helpers to extend; 88.3-01 added a `gap annotations` describe — leave it)
  </read_first>
  <action>
    Change the single constant `const MAX_DEFAULT_VISIBLE = 3;` to `const MAX_DEFAULT_VISIBLE = 1;` in `EndgameEloTimelineSection.tsx`. Make NO other code change in the component — `computeDefaultHidden()`, `MIN_ACTIVE_WEEKS_RATIO`, the legend toggle, the data-change reset, and the 88.3-01 gap-annotation block all stay exactly as they are. If the now-misleading comment in the `if (combos.length <= MAX_DEFAULT_VISIBLE)` block still says something like "the cap can't bite", update only that comment text to reflect threshold 1 (no logic change). Then add a `describe('MAX_DEFAULT_VISIBLE = 1', ...)` block to the test file with two cases: (a) a payload with 3 combos where the most-active (highest summed `per_week_total_games`) is one specific combo — assert the two less-active combo keys are in `hiddenKeys` / rendered hidden and the most-active combo is visible; (b) a payload with exactly 1 combo — assert nothing is hidden (no legend entry rendered in the hidden/muted state). Reuse the file's existing data-builder helpers and test infra; do not introduce a new mock framework.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameEloTimelineSection</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAX_DEFAULT_VISIBLE" frontend/src/components/charts/EndgameEloTimelineSection.tsx` shows `MAX_DEFAULT_VISIBLE = 1` and NO remaining `= 3`
    - `cd frontend && npm test -- --run EndgameEloTimelineSection` passes including the new `MAX_DEFAULT_VISIBLE = 1` describe block AND the pre-existing `gap annotations` block from 88.3-01 (88.3-D and 88.3-E)
    - No change to `MIN_ACTIVE_WEEKS_RATIO`, `computeDefaultHidden()` body, or the 88.3-01 gap-annotation code (verifiable by diff: only the constant value and an optional comment line change)
    - `cd frontend && npm run lint` clean
  </acceptance_criteria>
  <done>ELO Timeline defaults to the single most-active series; other series stay toggleable; 1-combo case unchanged; regression tests cover both (88.3-D, 88.3-E); 88.3-01 gap annotations untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan changes one client-side default-visibility constant. No new inputs, endpoints, parameters, or data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.3-02 | — | client-side legend default | accept | No security-relevant surface — client-side visualization of already-authorized, already-rendered stats data; no new inputs, endpoints, or trust boundaries. No package installs. |

No high-severity threats. Block-on threshold (high) not reached.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameEloTimelineSection` green (including new multi-combo and 1-combo cases and the carried-over 88.3-01 gap-annotation case)
- `cd frontend && npm run lint` and `cd frontend && npm run knip` clean
- `cd frontend && npm run build` succeeds
- Manual (UAT, recorded in 88.3-VALIDATION.md): with ≥2 series, exactly one (most-active) series visible on load; other series reveal via the existing legend; single-series account unchanged
</verification>

<success_criteria>
SC-2 satisfied: first render shows only the single most-active series; remaining series toggleable via existing legend; no tabs; single-series behaviour unchanged; covered by regression tests.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88.3-endgame-stats-viz-refinements/88.3-02-SUMMARY.md` when done.
</output>
