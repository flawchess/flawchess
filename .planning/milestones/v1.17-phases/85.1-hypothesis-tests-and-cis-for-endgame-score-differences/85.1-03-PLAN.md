---
phase: 85.1-hypothesis-tests-and-cis-for-endgame-score-differences
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
  - frontend/src/components/popovers/AchievableScorePopover.tsx
  - frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx
  - frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx
  - frontend/src/components/popovers/__tests__/AchievableScorePopover.test.tsx
autonomous: true
requirements: [SEC1-10, SEC1-11]
must_haves:
  truths:
    - "EndgamePerformanceResponse and ScoreGapMaterialResponse TS types carry the 4+3 new optional fields"
    - "Both ScoreGapRow instances render 95% CI whiskers when CI fields are non-null on the response"
    - "achievableGapValue is sourced from data.achievable_score_gap (server-computed), not from withScore - achievableScore"
    - "withScore local variable is removed (dead after migration)"
    - "AchievableScorePopover.pValue accepts number | null and the `?? 1` fallback at the call site is removed (WR-04)"
    - "AchievableScorePopover imports ConfidenceLevel from @/lib/scoreConfidence (IN-02 — drops local re-declaration)"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "extended EndgamePerformanceResponse + ScoreGapMaterialResponse interfaces"
    - path: "frontend/src/components/charts/EndgameOverallScoreGapRow.tsx"
      provides: "ciLow?/ciHigh? props on ScoreGapRow threaded into MiniBulletChart"
    - path: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      provides: "server-sourced achievableGapValue; CI fields threaded into both ScoreGapRow call sites; withScore removed"
    - path: "frontend/src/components/popovers/AchievableScorePopover.tsx"
      provides: "pValue: number | null; ConfidenceLevel imported from scoreConfidence"
  key_links:
    - from: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      to: "frontend/src/types/endgames.ts::EndgamePerformanceResponse"
      via: "data.achievable_score_gap / data.achievable_score_gap_ci_low / data.achievable_score_gap_ci_high"
      pattern: "data\\.achievable_score_gap"
    - from: "frontend/src/components/charts/EndgameOverallScoreGapRow.tsx"
      to: "frontend/src/components/charts/MiniBulletChart.tsx"
      via: "ciLow / ciHigh prop pass-through"
      pattern: "ciLow=\\{ciLow\\}"
---

<objective>
Frontend wiring for the new backend fields delivered by Plan 02. Three changes:

1. **TypeScript types** — Extend `EndgamePerformanceResponse` (+4 fields) and `ScoreGapMaterialResponse` (+3 fields) in `frontend/src/types/endgames.ts`. Match the backend schema field names and `number | null` for gated fields, plain `number` for `achievable_score_gap`.

2. **CI whisker wiring** — Add `ciLow?: number` / `ciHigh?: number` props to `ScoreGapRow` and thread them into `MiniBulletChart` (which already supports the props per RESEARCH §Frontend Anchors). Wire both `ScoreGapRow` instances in `EndgameOverallPerformanceSection.tsx` with `data.achievable_score_gap_ci_* ?? undefined` / `scoreGap.score_difference_ci_* ?? undefined` coercion.

3. **Migrate achievableGapValue derivation + clean adjacent debt:**
   - Replace `achievableGapValue = withScore - achievableScore` (line 81) with `achievableGapValue = data.achievable_score_gap` (server-computed).
   - Remove the `withScore` local variable (lines 71-75) — verified dead after migration (only used on line 81).
   - WR-04: Make `AchievableScorePopover.pValue` accept `number | null`, drop the `?? 1` fallback at the call site (location: search the section file for `AchievableScorePopover` usage). The fallback was masking the type mismatch — proper nullable typing removes it.
   - IN-02: `AchievableScorePopover.tsx` line 6 declares a local `type ConfidenceLevel = 'low' | 'medium' | 'high'`. The canonical type lives in `frontend/src/lib/scoreConfidence.ts` (line 20). Import from there and delete the local re-declaration.

Out of scope for this plan (deferred to Plan 04):
- D-04 decision (gap font coloring — zone-only vs triple gate). Plan 04 records the decision + implements color logic changes if reopened.
- InfoPopover copy updates (sigmoid-bias caveat) — Plan 04.
- Final lint/test/build sweep — Plan 04.

Purpose: Type contract + visual CI whiskers shipped; frontend derivation retired.

Output: Both ScoreGapRow rows render CI whiskers when n>=2 on the relevant cohort. No frontend math on `achievableGapValue` — the server is the source of truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-RESEARCH.md
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-PATTERNS.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-REVIEW.md
@frontend/src/types/endgames.ts
@frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/components/charts/MiniBulletChart.tsx
@frontend/src/components/popovers/AchievableScorePopover.tsx
@frontend/src/lib/scoreConfidence.ts
@CLAUDE.md

<interfaces>
<!-- Existing TS contracts the executor needs. -->

From frontend/src/types/endgames.ts (current):
```typescript
export interface EndgamePerformanceResponse {
  // ... existing fields incl. entry_expected_score, entry_expected_score_ci_low/high ...
}
export interface ScoreGapMaterialResponse {
  endgame_score: number;
  non_endgame_score: number;
  score_difference: number;
  material_rows: MaterialRow[];
  timeline: ScoreGapTimelinePoint[];
  timeline_window: number;
}
```

From frontend/src/components/charts/MiniBulletChart.tsx (already supports CI):
- ciLow?: number — whisker lower bound (domain units)
- ciHigh?: number — whisker upper bound (domain units)
- Whiskers render when both are defined; suppressed end caps when bound extends past axis domain.

From frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (current):
```typescript
interface ScoreGapRowProps {
  label: string;
  value: number;
  formatted: string;
  resultColor: string | undefined;
  valueTestId: string;
  ariaLabel: string;
  tooltip?: ReactNode;
  valueClassName?: string;
}
```

From frontend/src/lib/scoreConfidence.ts (line 20):
- export type ConfidenceLevel = 'low' | 'medium' | 'high';

From frontend/src/components/popovers/AchievableScorePopover.tsx (current, line 6 + 43):
- Local `type ConfidenceLevel = 'low' | 'medium' | 'high'` — DELETE.
- `pValue: number` — CHANGE to `number | null`.

`withScore` location in EndgameOverallPerformanceSection.tsx (verified):
- Declared lines 71-75, used only at line 81. DELETE both after migration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TS types + ScoreGapRow CI prop threading</name>
  <files>frontend/src/types/endgames.ts, frontend/src/components/charts/EndgameOverallScoreGapRow.tsx, frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx</files>
  <behavior>
    - EndgamePerformanceResponse carries achievable_score_gap (number), achievable_score_gap_p_value / _ci_low / _ci_high (number | null).
    - ScoreGapMaterialResponse carries score_difference_p_value / _ci_low / _ci_high (number | null).
    - ScoreGapRow accepts optional ciLow / ciHigh props (both `number | undefined`) and forwards them to MiniBulletChart.
    - When ciLow + ciHigh both passed, MiniBulletChart renders whiskers. When either omitted, no whiskers.
  </behavior>
  <action>
    **`frontend/src/types/endgames.ts`:** Append to `EndgamePerformanceResponse` (after `entry_expected_score_ci_high`):
    ```typescript
    // Phase 85.1 (SEC1-10): server-side achievable score gap + paired z-test.
    achievable_score_gap: number;
    achievable_score_gap_p_value: number | null;
    achievable_score_gap_ci_low: number | null;
    achievable_score_gap_ci_high: number | null;
    ```
    Append to `ScoreGapMaterialResponse` (after `timeline_window`):
    ```typescript
    // Phase 85.1 (SEC1-08, SEC1-09): independent two-sample z-test on endgame_score − non_endgame_score.
    score_difference_p_value: number | null;
    score_difference_ci_low: number | null;
    score_difference_ci_high: number | null;
    ```

    **`frontend/src/components/charts/EndgameOverallScoreGapRow.tsx`:** Extend `ScoreGapRowProps` with `ciLow?: number;` and `ciHigh?: number;` (after `valueClassName?: string`). Add JSDoc per the MiniBulletChart pattern: `/** 95% CI lower bound in domain units (signed). Renders whisker when both ciLow + ciHigh are defined. */`. Destructure the new props in the component signature. Pass through to `<MiniBulletChart>`:
    ```tsx
    <MiniBulletChart
      value={value}
      center={0}
      neutralMin={SCORE_GAP_NEUTRAL_MIN}
      neutralMax={SCORE_GAP_NEUTRAL_MAX}
      domain={SCORE_GAP_DOMAIN}
      barColor="neutral"
      ariaLabel={ariaLabel}
      ciLow={ciLow}
      ciHigh={ciHigh}
    />
    ```

    **Test file `EndgameOverallScoreGapRow.test.tsx`:** Create a new test file (or extend existing if present — `find frontend/src/components/charts/__tests__ -name "EndgameOverallScoreGapRow*"`). Mock `MiniBulletChart` to surface `data-ci-low` and `data-ci-high` attributes from the props (mirror the WR-02 pattern referenced in Phase 85 REVIEW). Tests:
    1. Renders without ciLow/ciHigh -> mock has no `data-ci-low` / `data-ci-high` attrs (or attrs == undefined).
    2. Renders with ciLow=-0.05, ciHigh=0.07 -> mock surfaces both attrs.
    3. Renders with only ciLow -> ciHigh attr absent (defensive — guard against half-defined CI).

    Per CLAUDE.md every interactive element needs `data-testid`; `ScoreGapRow` is wholly presentational so the existing `valueTestId` on the value span is sufficient. No new ARIA / semantic-HTML obligations.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm test -- --run -t "ScoreGapRow"</automated>
  </verify>
  <done>
    Both TS interfaces carry the new fields. `ScoreGapRow` accepts and forwards CI props. New test file passes. `npm run typecheck` clean. `npm run lint` clean on the touched files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate achievableGapValue + thread CI props + WR-04 + IN-02</name>
  <files>frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/popovers/AchievableScorePopover.tsx, frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx, frontend/src/components/popovers/__tests__/AchievableScorePopover.test.tsx</files>
  <behavior>
    - `EndgameOverallPerformanceSection` reads `achievableGapValue` from `data.achievable_score_gap`. `withScore` local variable is removed.
    - Both `ScoreGapRow` instances receive `ciLow` / `ciHigh` from the appropriate schema fields with `?? undefined` coercion.
    - `AchievableScorePopover.pValue` accepts `number | null`. The component renders the existing `(p = X.XXX)` line as `(p = X.XXX)` when pValue is a float, and an alternate "(p unavailable)" or omits the line entirely when pValue is null — choose the cleaner of the two and document in code comment.
    - `AchievableScorePopover.tsx` imports `ConfidenceLevel` from `@/lib/scoreConfidence` and the local re-declaration on line 6 is removed.
    - The `?? 1` fallback at the AchievableScorePopover call site in `EndgameOverallPerformanceSection` (or `EndgameOverallEntryCard` — search for `AchievableScorePopover` usage) is removed.
  </behavior>
  <action>
    **`frontend/src/components/charts/EndgameOverallPerformanceSection.tsx`:**

    1. Replace the `withScore` block at lines 71-75 — remove it entirely. The variable is only used on line 81 (verified).
    2. Replace line 81: `const achievableGapValue = withScore - achievableScore;` → `const achievableGapValue = data.achievable_score_gap;` (sourced from Plan 02 schema field).
    3. Remove `const achievableScore = data.entry_expected_score;` at line 80 IF it has no other usage in the file (verify with `grep -n "achievableScore" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx`). If used elsewhere, keep it.
    4. Add a code comment on the new line explaining the source: `// achievable_score_gap is computed server-side per SEC1-10 (paired z-test on per-game actual − expected pairs).`
    5. Extend the first `ScoreGapRow` (Achievable Score Gap, lines 144-165) with:
       ```tsx
       ciLow={data.achievable_score_gap_ci_low ?? undefined}
       ciHigh={data.achievable_score_gap_ci_high ?? undefined}
       ```
    6. Extend the second `ScoreGapRow` (Endgame Score Gap, lines 166-186) with:
       ```tsx
       ciLow={scoreGap.score_difference_ci_low ?? undefined}
       ciHigh={scoreGap.score_difference_ci_high ?? undefined}
       ```

    **WR-04 / IN-02 fixes in `frontend/src/components/popovers/AchievableScorePopover.tsx`:**

    1. Remove `type ConfidenceLevel = 'low' | 'medium' | 'high';` at line 6.
    2. Add `import type { ConfidenceLevel } from '@/lib/scoreConfidence';` to the import block.
    3. Change `pValue: number;` (line 43) to `pValue: number | null;`. Update the JSDoc to note the null case ("null when n is below the reliability gate").
    4. Update the rendered line at 130-133:
       ```tsx
       <p>
         <strong>{headline(level, score)}</strong> {CONFIDENCE_LABEL[level]} confidence
         {pValue !== null ? ` (p = ${pValue.toFixed(3)})` : ''}.
       </p>
       ```
       The terminating period stays outside the conditional so prose remains grammatical when pValue is null. Add inline comment: `// pValue is null when bucket-row sample size is below the reliability gate (PVALUE_RELIABILITY_MIN_N = 10).`

    **Locate and clean the `?? 1` fallback (WR-04):**

    Search for the `AchievableScorePopover` call site: `grep -rn "AchievableScorePopover" frontend/src/components/charts/`. It is invoked from `EndgameOverallEntryCard.tsx` (based on the codebase tour). The existing call passes something like `pValue={data.entry_expected_score_p_value ?? 1}` — strip the `?? 1` so it becomes `pValue={data.entry_expected_score_p_value}`. The popover now handles null gracefully.

    **Test updates:**

    1. `EndgameOverallPerformanceSection.test.tsx`: Update the existing mock of `MiniBulletChart` (if any) or the existing `ScoreGapRow` rendering tests to assert `data-ci-low` / `data-ci-high` on the rendered MiniBulletChart for both rows. Update the test fixture for `data` to include the new fields (`achievable_score_gap`, `achievable_score_gap_p_value`, `achievable_score_gap_ci_low/high`) and for `scoreGap` to include `score_difference_p_value / ci_low / ci_high`. Add explicit assertion that `achievableGapValue` rendered in the row equals the value computed from `data.achievable_score_gap` (use a fixture where this differs from `withScore - entry_expected_score` to prove the migration is real).

    2. `AchievableScorePopover.test.tsx`: Add a test "renders without (p = …) when pValue is null" asserting the popover content does not include the substring `p = `. Update one existing test fixture to pass `pValue={null}` to confirm no crash. Keep all existing tests for the non-null path passing.

    Per CLAUDE.md min `text-sm` — no font-size changes in this plan. Per CLAUDE.md `data-testid` on every interactive element — `AchievableScorePopover` trigger already has `data-testid` (line 95); no new interactive surface introduced.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint && npm test -- --run -t "EndgameOverallPerformanceSection|AchievableScorePopover|ScoreGapRow"</automated>
  </verify>
  <done>
    `achievableGapValue` reads from `data.achievable_score_gap`. `withScore` removed. Both `ScoreGapRow` rows receive CI props. `AchievableScorePopover.pValue` is `number | null`. Local `ConfidenceLevel` type in `AchievableScorePopover` deleted. `?? 1` fallback at the call site removed. All updated tests pass. `npm run typecheck`, `npm run lint`, `npm run knip` on touched files clean. Full frontend test suite (`npm test -- --run`) still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → client | Schema fields are `number \| null`; client must coerce to `number \| undefined` for downstream optional props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85.1-07 | Tampering (type coercion) | ScoreGapRow CI prop boundary | mitigate | `?? undefined` at both call sites converts `null` to `undefined`; MiniBulletChart suppresses whiskers when either is undefined |
| T-85.1-08 | Information Disclosure | AchievableScorePopover with null pValue | mitigate | Conditional rendering omits the "(p = X.XXX)" line entirely when null; no NaN or "null" string leaks into rendered prose |
</threat_model>

<verification>
- `npm run typecheck` zero errors.
- `npm run lint` zero errors / warnings on touched files.
- `npm run knip` clean (no new dead exports; `withScore` removal does not create a knip warning since it was a local).
- `npm test -- --run` passes (full frontend suite).
- Manual visual check (deferred to Plan 04 final verification): CI whiskers render on both ScoreGapRow rows in dev when the active user has >= 2 endgame + >= 2 non-endgame games and >= 2 bucket-row evals.
</verification>

<success_criteria>
SEC1-11 satisfied: both `ScoreGapRow` rows render CI whiskers. SEC1-10 wire-to-wire complete: `achievableGapValue` is server-sourced. WR-04 + IN-02 (Phase 85 REVIEW carry-forwards) resolved. Plan 04 can focus on the D-04 decision + InfoPopover copy + final verification.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-03-SUMMARY.md`
</output>
