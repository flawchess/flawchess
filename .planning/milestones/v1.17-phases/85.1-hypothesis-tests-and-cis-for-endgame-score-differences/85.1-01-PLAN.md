---
phase: 85.1-hypothesis-tests-and-cis-for-endgame-score-differences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/score_confidence.py
  - tests/services/test_score_confidence.py
autonomous: true
requirements: [SEC1-08, SEC1-09, SEC1-10]
must_haves:
  truths:
    - "compute_score_difference_test returns (p_value, ci_low, ci_high) and gates both independently by n"
    - "compute_paired_difference_test returns (mean_d, p_value, ci_low, ci_high) and gates p/ci independently"
    - "Both helpers handle SE=0 degenerate inputs without raising (variance-0 trap)"
    - "Both helpers handle n=0 and n=1 boundaries explicitly"
    - "All new helpers are covered by unit tests at every documented boundary"
  artifacts:
    - path: "app/services/score_confidence.py"
      provides: "compute_score_difference_test, compute_paired_difference_test"
    - path: "tests/services/test_score_confidence.py"
      provides: "TestComputeScoreDifferenceTest + TestComputePairedDifferenceTest classes"
  key_links:
    - from: "app/services/score_confidence.py"
      to: "app/services/opening_insights_constants.py"
      via: "import CI_Z_95, CONFIDENCE_MIN_N (already in scope at lines 54-63)"
      pattern: "OPENING_INSIGHTS_CI_Z_95 as CI_Z_95"
    - from: "tests/services/test_score_confidence.py"
      to: "app/services/score_confidence.py"
      via: "import compute_score_difference_test, compute_paired_difference_test"
      pattern: "from app.services.score_confidence import"
---

<objective>
Add two pure-math helpers to `app/services/score_confidence.py` for Phase 85.1 hypothesis tests:

1. `compute_score_difference_test` — independent two-sample z-test on chess-score difference between two WDL cohorts. Used by Plan 02 inside `_compute_score_gap_material` to populate `score_difference_p_value` / `ci_low` / `ci_high` on `ScoreGapMaterialResponse`.
2. `compute_paired_difference_test` — paired one-sample z-test on pre-computed differences. Used by Plan 02 inside `_get_endgame_performance_from_rows` on the per-game `d_i = actual_i − expected_i` sequence to populate `achievable_score_gap` + p_value + CI on `EndgamePerformanceResponse`.

Both helpers are pure stats functions (no DB, no schema). They encapsulate n-gating internally and return `None` for fields that fail their gate so the caller doesn't re-implement gating logic.

Purpose: Single source of truth for the two new z-tests per SEC1-08/09/10. Plan 02 wires them into the service layer; this plan keeps the math isolated and unit-tested.

Output: 2 new exported functions + corresponding test classes. No schema changes, no service wiring (deferred to Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-RESEARCH.md
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-PATTERNS.md
@app/services/score_confidence.py
@app/services/eval_confidence.py
@app/services/opening_insights_constants.py
@tests/services/test_score_confidence.py
@tests/services/test_eval_confidence.py
@CLAUDE.md

<interfaces>
<!-- Already in scope at score_confidence.py lines 54-63 — no new imports needed for constants. -->

From app/services/opening_insights_constants.py:
- OPENING_INSIGHTS_CI_Z_95: float = 1.96  (imported as `CI_Z_95`)
- OPENING_INSIGHTS_CONFIDENCE_MIN_N: int = 10  (imported as `CONFIDENCE_MIN_N`)

From app/services/score_confidence.py (existing analog at line 219):
- empirical_variance = max(0.0, (w + 0.25 * d) / n - score * score)
- se = math.sqrt(empirical_variance / n)

From app/services/eval_confidence.py (SE=0 trap analog at lines 116-119):
- if se == 0.0: p_value = 0.0 if mean != baseline else 1.0
- else: z = (mean - baseline) / se; p_value = math.erfc(abs(z) / math.sqrt(2.0))

New helper signatures (this plan ships):
```python
def compute_score_difference_test(
    eg_w: int, eg_d: int, eg_l: int, eg_n: int,
    ne_w: int, ne_d: int, ne_l: int, ne_n: int,
) -> tuple[float | None, float | None, float | None]:
    """Return (p_value, ci_low, ci_high) for the independent two-sample z-test
    on (W+0.5D)/N difference. p_value None when min(eg_n, ne_n) < CONFIDENCE_MIN_N.
    ci_low/ci_high None when min(eg_n, ne_n) < 2."""

def compute_paired_difference_test(
    diffs: Sequence[float],
) -> tuple[float, float | None, float | None, float | None]:
    """Return (mean_d, p_value, ci_low, ci_high) for the paired one-sample z-test
    on per-game differences. mean_d always returned (0.0 when empty).
    p_value None when len(diffs) < CONFIDENCE_MIN_N. ci_* None when len(diffs) < 2."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compute_score_difference_test helper + unit tests</name>
  <files>app/services/score_confidence.py, tests/services/test_score_confidence.py</files>
  <behavior>
    Test cases for `compute_score_difference_test(eg_w, eg_d, eg_l, eg_n, ne_w, ne_d, ne_l, ne_n)`:
    - n=0 on either side returns (None, None, None) without raising
    - eg_n=9 (below CONFIDENCE_MIN_N) returns (None, ci_low, ci_high) — p gated, CI present at n>=2
    - ne_n=1 returns (p_value, None, None) — p gated by min(n) rule, CI gated at n<2
    - Both n>=10 with identical W/D/L proportions: z=0, p_value=1.0, CI brackets 0
    - All-wins (10,0,0) vs all-losses (0,0,10): SE_diff=0, scores differ -> p_value=0.0, ci_low==ci_high==1.0
    - All-draws (0,10,0) both sides: SE_diff=0 in each variance term but score_eg=score_ne=0.5 -> p_value=1.0, CI half-width=0
    - CI half-width matches CI_Z_95 * sqrt(var_eg/eg_n + var_ne/ne_n) within 1e-9 for a known input
    - p_value lies in [0, 1] for any non-degenerate input
  </behavior>
  <action>
    Add `compute_score_difference_test` to `app/services/score_confidence.py` between the existing public exports (place it after `compute_score_confidence_from_mean` so related helpers cluster). Reuse the existing trinomial variance formula at line 219 inline (do NOT call `compute_confidence_bucket` — different return shape, different gating policy). Follow the SE=0 trap pattern from `eval_confidence.py:116-119` verbatim, replacing `(mean - baseline)` with `(score_eg - score_ne)`. Compute SE_diff = sqrt(var_eg/eg_n + var_ne/ne_n) where each `var_i = max(0.0, (w_i + 0.25*d_i)/n_i - score_i**2)` and `score_i = (w_i + 0.5*d_i)/n_i`. Gate p_value to None when `min(eg_n, ne_n) < CONFIDENCE_MIN_N`; gate ci_low/ci_high to None when `min(eg_n, ne_n) < 2`. Both gates are independent. Return signature exactly `tuple[float | None, float | None, float | None]`. Add a Field-style docstring documenting the gates and the variance-0 short-circuit. Add `from typing import Sequence` if not already imported (it isn't — only `Literal` is at line 55, so widen the existing typing import).

    Add a new `TestComputeScoreDifferenceTest` class to `tests/services/test_score_confidence.py` mirroring the structure of `TestComputeScoreConfidenceFromMean` (lines 196-254). One method per behavior bullet above. Use `pytest.approx(..., rel=1e-9)` for CI half-width assertions and `pytest.approx(..., abs=1e-6)` for p-value assertions on degenerate boundaries. Per CLAUDE.md no magic numbers: extract any reused fixture counts to local constants in the test class (e.g. `N_AT_GATE = 10`). Do NOT import `CONFIDENCE_MIN_N` constant into tests — assert against literal `10` once in a comment and use it bare in test inputs (per existing test file convention at lines 35-39).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_score_confidence.py -x -k "ScoreDifferenceTest" -v</automated>
  </verify>
  <done>
    `compute_score_difference_test` exported from `app/services/score_confidence.py`. All TestComputeScoreDifferenceTest methods pass. `uv run ruff check app/services/score_confidence.py tests/services/test_score_confidence.py` clean. `uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compute_paired_difference_test helper + unit tests</name>
  <files>app/services/score_confidence.py, tests/services/test_score_confidence.py</files>
  <behavior>
    Test cases for `compute_paired_difference_test(diffs: Sequence[float])`:
    - diffs=[] -> (0.0, None, None, None) — empty sequence does not raise
    - diffs=[0.1] (n=1) -> (0.1, None, None, None) — Bessel undefined at n=1; mean still returned
    - diffs=[0.0]*15 -> (0.0, 1.0, 0.0, 0.0) — all-zeros, SE=0, mean==H0 -> p=1.0, CI collapses to point
    - diffs=[0.25]*15 -> (0.25, 0.0, 0.25, 0.25) — all-equal nonzero, SE=0, mean!=H0 -> p=0.0, CI collapses
    - diffs=[+0.1]*9 (n=9 below gate, but n>=2) -> mean=0.1, p=None, ci_low/ci_high are floats (CI gate is n<2)
    - diffs of n=20 mixed pairs: mean_d == sum/n, ci_high - ci_low == 2 * CI_Z_95 * sqrt(var_d / n) where var_d is Bessel-corrected
    - p_value in [0, 1] for any non-degenerate input
    - Bessel correction: var_d uses (sumsq - n*mean²) / (n-1) not / n
  </behavior>
  <action>
    Add `compute_paired_difference_test` to `app/services/score_confidence.py` directly after `compute_score_difference_test` from Task 1. Take `diffs: Sequence[float]` (NOT `list[float]` per CLAUDE.md — Sequence is covariant). Guard `len(diffs) == 0` -> return `(0.0, None, None, None)` early. Compute `mean_d = sum(diffs) / n`. Guard `n == 1` -> return `(mean_d, None, None, None)`. For n >= 2, compute Bessel-corrected sample variance `var_d = max(0.0, (sumsq - n * mean_d * mean_d) / (n - 1))` (the `max(0.0, ...)` clamp is for floating-point rounding, exactly as in `eval_confidence.py:113`). Compute `se = math.sqrt(var_d / n)`. Apply SE=0 trap: if `se == 0.0`, set `p_value = 0.0 if mean_d != 0.0 else 1.0` (H0 is mean_d == 0); else `z = mean_d / se; p_value = math.erfc(abs(z) / math.sqrt(2.0))`. Compute `ci_half = CI_Z_95 * se`, `ci_low = mean_d - ci_half`, `ci_high = mean_d + ci_half`. Gate p_value to None when `n < CONFIDENCE_MIN_N`. CI bounds are not gated separately past the n=1 guard (any n >= 2 gets CI). Docstring documents the gates, the H0 (mean_d == 0), and the SE=0 short-circuit.

    Add `TestComputePairedDifferenceTest` class to `tests/services/test_score_confidence.py` directly after `TestComputeScoreDifferenceTest`. One method per behavior bullet. For the n=20 mixed case, hand-compute the expected mean and SE on a known dataset (e.g. `diffs = [0.1, -0.1, 0.2, -0.2] * 5`) so the assertions are deterministic. Use `pytest.approx` with `rel=1e-9` for arithmetic, `abs=1e-6` for p-value comparisons. Comment the n=1 case: "Bessel correction divides by n-1 which is zero at n=1; the helper must short-circuit rather than divide-by-zero."
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_score_confidence.py -x -k "PairedDifferenceTest" -v</automated>
  </verify>
  <done>
    `compute_paired_difference_test` exported from `app/services/score_confidence.py`. All TestComputePairedDifferenceTest methods pass. Full test file passes: `uv run pytest tests/services/test_score_confidence.py -x`. `uv run ruff check app/services/score_confidence.py tests/services/test_score_confidence.py` clean. `uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure-math helpers; no untrusted input crosses any boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85.1-01 | Tampering (numeric) | `compute_score_difference_test` variance term | mitigate | `max(0.0, ...)` clamp on empirical variance (RESEARCH §Math T1) prevents negative variance from FP rounding; SE=0 short-circuit prevents NaN propagation in z = diff/0 |
| T-85.1-02 | Denial of Service | `compute_paired_difference_test` Bessel correction at n=1 | mitigate | Explicit n<=1 guard returns (mean_d, None, None, None) instead of dividing by zero |
| T-85.1-03 | Information Disclosure | helper outputs | accept | Helpers produce only aggregate stats over caller-supplied inputs; no PII, no DB access |
</threat_model>

<verification>
- Both helpers exported from `app/services/score_confidence.py`.
- All test methods in `TestComputeScoreDifferenceTest` and `TestComputePairedDifferenceTest` pass.
- Full `tests/services/test_score_confidence.py` still passes (no regression in existing 196-254 lines).
- `uv run ruff check app/services/score_confidence.py tests/services/test_score_confidence.py` clean.
- `uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py` clean (zero errors).
- No magic numbers in helper bodies — all thresholds reference `CONFIDENCE_MIN_N` / `CI_Z_95` already imported at the top of the file.
- `Sequence[float]` not `list[float]` on `compute_paired_difference_test` (CLAUDE.md type-safety rule).
</verification>

<success_criteria>
SEC1-08 / SEC1-09 / SEC1-10 math is implemented as pure helpers with full boundary-test coverage. Plan 02 can call them as a black box without re-implementing variance/SE/gating logic.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-01-SUMMARY.md`
</output>
