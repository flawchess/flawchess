# Phase 85.1 — Plan Index

**Phase:** Hypothesis tests + CIs for Endgame Score Differences
**Inserted:** 2026-05-14
**Depends on:** Phase 85 (shipped, PR #96)
**Requirements:** SEC1-08, SEC1-09, SEC1-10, SEC1-11

## Plans

| Plan | Wave | Depends on | Requirements | Objective |
|------|------|------------|--------------|-----------|
| [85.1-01-PLAN.md](./85.1-01-PLAN.md) | 1 | — | SEC1-08, SEC1-09, SEC1-10 | Backend math helpers: `compute_score_difference_test` + `compute_paired_difference_test` + boundary unit tests in `score_confidence.py` |
| [85.1-02-PLAN.md](./85.1-02-PLAN.md) | 2 | 01 | SEC1-08, SEC1-09, SEC1-10 | Wire helpers into `_compute_score_gap_material` + `_get_endgame_performance_from_rows` (merged paired-diff loop). Extend `ScoreGapMaterialResponse` + `EndgamePerformanceResponse`. Extract `PVALUE_RELIABILITY_MIN_N` (IN-01). Add service-layer test classes mirroring `TestEntryEvalAggregation`. |
| [85.1-03-PLAN.md](./85.1-03-PLAN.md) | 3 | 02 | SEC1-10, SEC1-11 | TS type extensions. `ScoreGapRow` ciLow/ciHigh prop threading. Migrate `achievableGapValue` to server-computed `data.achievable_score_gap`; delete dead `withScore`. WR-04 (pValue nullable) + IN-02 (ConfidenceLevel canonical import) cleanups. |
| [85.1-04-PLAN.md](./85.1-04-PLAN.md) | 4 | 03 | SEC1-11 | D-04 decision checkpoint (kept zone-only by default per RESEARCH; reopen optional). Append sigmoid-bias caveat to Achievable Score Gap InfoPopover. Full backend + frontend verification sweep. CHANGELOG entry. |

## Wave Structure

```
Wave 1: [85.1-01]                # Plan 01 in parallel? No — sole plan in this wave.
Wave 2: [85.1-02]                # Sequential (depends on 01 helper APIs)
Wave 3: [85.1-03]                # Sequential (needs schema fields from 02)
Wave 4: [85.1-04]                # Sequential (decision + final sweep)
```

All four plans run sequentially — each consumes the artifacts of the prior wave. No parallelism possible inside this phase (single chain: math -> wire -> types/UI -> decision/polish).

## Carry-forward from Phase 85 REVIEW (resolved in Phase 85.1)

| Finding | Resolved in | Notes |
|---------|-------------|-------|
| IN-01 (magic `10` in endgame_service.py at 4 sites) | Plan 02 | Extract `PVALUE_RELIABILITY_MIN_N`; Phase 85.1 adds 2+ more occurrences — exactly the trigger condition |
| IN-02 (local `ConfidenceLevel` re-declaration in `AchievableScorePopover.tsx:6`) | Plan 03 | Import from `@/lib/scoreConfidence` |
| WR-04 (`AchievableScorePopover.pValue: number` non-nullable) | Plan 03 | Change to `number \| null`; drop `?? 1` fallback at call site |
| WR-01 (deriveLevel duplication) | Already resolved in shipped Phase 85 code (deriveLevel exported from `EndgameOverallShared.ts`; no local re-declaration remains in `EndgameOverallPerformanceSection.tsx`) | No-op in Phase 85.1 |

## Out of Scope (not planned)

- FUT-05: per-rating-bucket sigmoid bias quantification on benchmark data. Caveat is surfaced in InfoPopover copy only (Plan 04 Task 3).
- Reopening Phase 85 D-01..D-09 decisions other than D-04.
- Refactors outside the directly-touched files.

## Decision

D-04 (Phase 85.1 revisit): zone-only vs triple-gate font coloring on Endgame Score Differences. Decision recorded in Plan 04 Task 1 -> `85.1-DECISIONS.md`. RESEARCH recommendation: keep zone-only (CI whiskers from Plan 03 already carry the uncertainty signal).
