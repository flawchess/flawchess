---
phase: 85.1-hypothesis-tests-and-cis-for-endgame-score-differences
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - tests/test_endgame_service.py
autonomous: true
requirements: [SEC1-08, SEC1-09, SEC1-10]
must_haves:
  truths:
    - "ScoreGapMaterialResponse exposes score_difference_p_value / ci_low / ci_high computed by compute_score_difference_test"
    - "EndgamePerformanceResponse exposes achievable_score_gap + p_value + ci_low + ci_high computed by compute_paired_difference_test"
    - "achievable_score_gap is server-computed from per-game (actual_i, expected_i) pairs"
    - "PVALUE_RELIABILITY_MIN_N constant replaces the 4 existing bare-10 occurrences plus the 2 new ones (IN-01)"
    - "All n-gate / CI-gate boundaries verified by tests mirroring the existing TestEntryEvalAggregation pattern"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "ScoreGapMaterialResponse + 3 new fields; EndgamePerformanceResponse + 4 new fields"
    - path: "app/services/endgame_service.py"
      provides: "PVALUE_RELIABILITY_MIN_N constant; wired test helpers; paired-diff accumulator merged into ex_sum/ex_n loop"
    - path: "tests/test_endgame_service.py"
      provides: "TestAchievableScoreGap class + TestScoreGapMaterial new methods covering p-gate, CI-gate, mean correctness, SE=0 trap"
  key_links:
    - from: "app/services/endgame_service.py::_compute_score_gap_material"
      to: "compute_score_difference_test"
      via: "import from app.services.score_confidence"
      pattern: "score_diff_p, score_diff_ci_low, score_diff_ci_high = compute_score_difference_test\\(.*endgame_wdl.*non_endgame_wdl"
    - from: "app/services/endgame_service.py::_get_endgame_performance_from_rows"
      to: "compute_paired_difference_test"
      via: "diffs accumulated in extended ex_sum/ex_n loop, passed to helper"
      pattern: "mean_d, achievable_p, achievable_ci_low, achievable_ci_high = compute_paired_difference_test"
---

<objective>
Wire the two helpers from Plan 01 into `app/services/endgame_service.py`, extend the two Pydantic schemas, and add `PVALUE_RELIABILITY_MIN_N` to replace bare `10` magic numbers (IN-01 carry-forward from Phase 85 REVIEW).

Two extension sites:
1. `_compute_score_gap_material` (endgame_service.py:723) — call `compute_score_difference_test(endgame_wdl.{wins,draws,losses,total}, non_endgame_wdl.{wins,draws,losses,total})` and pass results into `ScoreGapMaterialResponse(...)`.
2. `_get_endgame_performance_from_rows` (endgame_service.py:1687) — extend the existing `ex_sum`/`ex_n` loop (lines 1778-1796) to also accumulate per-game `d_i = actual_score_i − expected_score_i`. Call `compute_paired_difference_test(diffs)` and pass results into `EndgamePerformanceResponse(...)`.

The `d_n == ex_n` invariant is enforced by construction (single loop, identical filter logic). The paired-diff accumulator must apply the exact same `eval_mate`/`eval_cp`/`EVAL_CLIP_MAX_CP` filtering as the expected-score branch; both `actual_score_i` and `expected_score_i` are derived inside the SAME `if`/`elif`/`else` branches.

IN-01 cleanup: extract `PVALUE_RELIABILITY_MIN_N: int = 10` near the existing module-level constants block (around `EVAL_CLIP_MAX_CP`) and replace bare `10` at endgame_service.py:1744, 1760, 1770, 1803 plus the new occurrences this plan adds. Phase 85.1 adds 2+ new occurrences — exactly the trigger condition called out in REVIEW IN-01.

Purpose: Server-side authoritative test outputs flow to the schemas. Frontend derivation of `achievable_score_gap` (Plan 03) goes away once this lands.

Output: Backend tests green. Both response schemas carry the new fields with safe `= None` / `= 0.0` defaults so existing call sites in older tests still construct without explicit args.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-RESEARCH.md
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-PATTERNS.md
@app/services/endgame_service.py
@app/schemas/endgames.py
@tests/test_endgame_service.py
@app/services/score_confidence.py
@CLAUDE.md

<interfaces>
<!-- Plan 01 shipped these helpers — import and call directly. -->

From app/services/score_confidence.py (added in Plan 01):
- compute_score_difference_test(eg_w, eg_d, eg_l, eg_n, ne_w, ne_d, ne_l, ne_n) -> (p_value, ci_low, ci_high)
- compute_paired_difference_test(diffs: Sequence[float]) -> (mean_d, p_value, ci_low, ci_high)

From app/schemas/endgames.py:
- EndgameWDLSummary (lines 108-121): exposes .wins, .draws, .losses, .total
- ScoreGapMaterialResponse (line 299): constructed in _compute_score_gap_material; existing fields at lines 885-892
- EndgamePerformanceResponse (line 124): constructed in _get_endgame_performance_from_rows; existing return at lines 1816-1832

From app/services/endgame_service.py (already imported):
- eval_cp_to_expected_score, eval_mate_to_expected_score (line 68)
- derive_user_result (line 71) — returns "win" | "draw" | "loss"
- EVAL_CLIP_MAX_CP (line 181 ish; verify exact location during execution)

bucket_rows shape (per RESEARCH §Cursor):
- row.eval_mate: int | None
- row.eval_cp: int | None
- row.user_color: "white" | "black"
- row.result: "1-0" | "0-1" | "1/2-1/2"
All accesses need `# ty: ignore[unresolved-attribute]` (SQLAlchemy Row forward-ref pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schemas + extract PVALUE_RELIABILITY_MIN_N</name>
  <files>app/schemas/endgames.py, app/services/endgame_service.py</files>
  <behavior>
    - `EndgamePerformanceResponse` carries `achievable_score_gap: float = 0.0`, `achievable_score_gap_p_value: float | None = None`, `achievable_score_gap_ci_low: float | None = None`, `achievable_score_gap_ci_high: float | None = None`.
    - `ScoreGapMaterialResponse` carries `score_difference_p_value: float | None = None`, `score_difference_ci_low: float | None = None`, `score_difference_ci_high: float | None = None`.
    - Existing tests that construct either response without the new fields continue to pass (safe-default rule).
    - Module-level `PVALUE_RELIABILITY_MIN_N: int = 10` exported from `endgame_service.py` and used at every previous bare-`10` site (lines 1744, 1760, 1770, 1803 per RESEARCH).
    - No new bare `10` introduced.
  </behavior>
  <action>
    In `app/schemas/endgames.py`, append the 4 new fields to `EndgamePerformanceResponse` AFTER `entry_expected_score_ci_high` (preserve the existing Phase 81 / Phase 83 grouping comments). Use the exact docstring text from RESEARCH §Schema Additions. Append the 3 new fields to `ScoreGapMaterialResponse` AFTER `timeline_window` with the SEC1-08/09 comment header. All four `*_p_value` / `*_ci_*` fields use `float | None = None` defaults; `achievable_score_gap` uses `float = 0.0` (non-nullable for the always-computed mean per RESEARCH gating table).

    In `app/services/endgame_service.py`, add `PVALUE_RELIABILITY_MIN_N: int = 10` to the module-level constants block (place it adjacent to `EVAL_CLIP_MAX_CP` — search the file for that constant to locate). Add a one-line comment per CLAUDE.md "No magic numbers" referencing both SEC1-08 (p-gate) and IN-01 (REVIEW carry-forward). Replace every bare `>= 10` / `< 10` n-gate in `endgame_service.py` with `>= PVALUE_RELIABILITY_MIN_N` / `< PVALUE_RELIABILITY_MIN_N`. The known sites from RESEARCH are lines 1744, 1760, 1770, 1803 — `grep -n " 10$\\| 10 \\|>= 10\\|< 10\\|< 10:" app/services/endgame_service.py | grep -i "n " ` to confirm there isn't a fifth site, but do NOT touch unrelated `10`s (e.g. timeline window sizes — only the p-value reliability gate).

    No service-layer wiring changes yet — that's Task 2. This task only adds fields + constant; the new schema fields receive their default values until Task 2 wires real values.

    Run `uv run pytest tests/test_endgame_service.py -x` after this task to verify nothing breaks from the schema additions alone (existing tests should still pass since all new fields have defaults).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -x -k "TestScoreGapMaterial or TestEntryEvalAggregation or TestEntryExpectedScore" -v && uv run ruff check app/schemas/endgames.py app/services/endgame_service.py && uv run ty check app/services/endgame_service.py app/schemas/endgames.py</automated>
  </verify>
  <done>
    Both schemas carry the new fields with documented defaults. `PVALUE_RELIABILITY_MIN_N` extracted and used at every prior bare-10 site. Existing endgame service tests still pass without modification. ty + ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire compute_score_difference_test into _compute_score_gap_material + extend bucket_rows loop with paired-diff accumulator</name>
  <files>app/services/endgame_service.py, tests/test_endgame_service.py</files>
  <behavior>
    `_compute_score_gap_material` behavior:
    - When both endgame_wdl.total >= 10 and non_endgame_wdl.total >= 10: score_difference_p_value is float in [0,1].
    - When either total < 10: score_difference_p_value is None.
    - When either total < 2: score_difference_ci_low / ci_high are None.
    - All-wins endgame vs all-losses non-endgame (both n>=10): SE_diff=0, scores differ -> p_value=0.0.
    - Identical WDL distributions both sides (n>=10): p_value=1.0, CI brackets 0.

    `_get_endgame_performance_from_rows` paired-diff behavior:
    - achievable_score_gap is the mean over all surviving bucket_rows of (actual_score_i - expected_score_i) where actual is {"win":1.0,"draw":0.5,"loss":0.0}[derive_user_result(row.result, row.user_color)] and expected is via the existing eval_*_to_expected_score branches.
    - achievable_score_gap == 0.0 when bucket_rows is empty or every row is excluded (both eval fields None or |eval_cp| >= EVAL_CLIP_MAX_CP).
    - achievable_score_gap_p_value None when surviving n < PVALUE_RELIABILITY_MIN_N; float in [0,1] otherwise.
    - achievable_score_gap_ci_low / ci_high None when surviving n < 2; floats with ci_low <= mean_d <= ci_high otherwise.
    - The number of diffs accumulated equals ex_n (the existing expected-score counter). Add an assertion in the helper call site OR a comment documenting the invariant.
  </behavior>
  <action>
    **Site 1 — `_compute_score_gap_material` (line 723):**
    Add an import for `compute_score_difference_test` to the existing `from app.services.score_confidence import ...` block (search the file for `score_confidence` to locate). After the existing `score_difference = endgame_score - non_endgame_score` computation (RESEARCH §Pattern site 1, line ~764), call:
    ```python
    score_diff_p, score_diff_ci_low, score_diff_ci_high = compute_score_difference_test(
        endgame_wdl.wins, endgame_wdl.draws, endgame_wdl.losses, endgame_wdl.total,
        non_endgame_wdl.wins, non_endgame_wdl.draws, non_endgame_wdl.losses, non_endgame_wdl.total,
    )
    ```
    Extend the `ScoreGapMaterialResponse(...)` constructor at lines 885-892 with `score_difference_p_value=score_diff_p, score_difference_ci_low=score_diff_ci_low, score_difference_ci_high=score_diff_ci_high`.

    **Site 2 — `_get_endgame_performance_from_rows` ex_sum/ex_n loop (lines 1778-1796):**
    Add an import for `compute_paired_difference_test`. **Merge the paired-diff accumulator into the existing ex_sum/ex_n loop** — do NOT add a third pass over bucket_rows. Inside the existing `if row.eval_mate is not None:` and `elif row.eval_cp is not None:` branches (after the `ex_sum +=` / `ex_n += 1` lines but BEFORE the implicit branch-end), compute:
    ```python
    outcome = derive_user_result(row.result, row.user_color)  # ty: ignore[unresolved-attribute]
    actual_score_i = {"win": 1.0, "draw": 0.5, "loss": 0.0}[outcome]
    diffs.append(actual_score_i - expected_score_i)
    ```
    where `expected_score_i` is the value just added to `ex_sum` (capture it in a local variable inside each branch so the same value is reused for both the sum and the diff — this is the cleanest seam and avoids re-computing the sigmoid). Initialize `diffs: list[float] = []` immediately before the loop. After the loop:
    ```python
    mean_d, achievable_p, achievable_ci_low, achievable_ci_high = compute_paired_difference_test(diffs)
    # d_n == ex_n by construction (same filter applied to both accumulators).
    ```
    Add the comment exactly as quoted above. If you prefer an assertion, use `assert len(diffs) == ex_n, "d_n != ex_n — paired-diff filter diverged from ex_n filter"` — but the comment is sufficient per RESEARCH §Pitfall 6.

    Extend the `EndgamePerformanceResponse(...)` constructor at lines 1816-1832 with:
    ```python
    achievable_score_gap=mean_d,
    achievable_score_gap_p_value=achievable_p,
    achievable_score_gap_ci_low=achievable_ci_low,
    achievable_score_gap_ci_high=achievable_ci_high,
    ```

    Apply `# ty: ignore[unresolved-attribute]` to every new `row.*` attribute access inside the loop (RESEARCH §Pattern "ty: ignore for SQLAlchemy row attributes"). Be precise about the rule name.

    **Test additions in `tests/test_endgame_service.py`:**

    1. New test method on `TestScoreGapMaterial` covering `score_difference_p_value` gating (mirror `test_endgame_score_p_value_gated_below_n_ten` from PATTERNS §test_endgame_service.py): three cases — below gate either side, at gate both sides, SE_diff=0 short-circuit.

    2. New test method on `TestScoreGapMaterial` covering `score_difference_ci_*` gating: n=1 either side -> None; n>=2 both sides -> floats with ci_low <= score_difference <= ci_high.

    3. New `TestAchievableScoreGap(TestEntryEvalAggregation)` class — subclass per the PATTERNS §"Subclassing pattern" template so `_bucket` and `_wdl_rows` fixtures are reused. Methods: (a) empty bucket_rows -> achievable_score_gap=0.0, all None; (b) n=9 surviving -> mean is float, p_value is None, CI bounds are floats; (c) n>=10 with mixed wins/draws/losses and known eval values -> mean matches hand-computed expectation; (d) n=1 -> CI bounds are None; (e) all-equal (n>=10 with d_i=0 for all) -> p_value=1.0, CI collapses to mean_d.

    For the hand-computed expectation in (c), construct bucket_rows with known eval_cp values that produce known expected_scores via the existing sigmoid, and known game results that produce known actual_scores. Computing one or two of these by hand is sufficient — the rest are covered by the helper's own tests from Plan 01.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -x -k "TestScoreGapMaterial or TestAchievableScoreGap or TestEntryEvalAggregation or TestEntryExpectedScore" -v</automated>
  </verify>
  <done>
    Both helpers wired into the service layer. ScoreGapMaterialResponse and EndgamePerformanceResponse carry computed values (not defaults) in all non-degenerate paths. All new test methods pass. `uv run pytest tests/` (full suite) passes — no regression. `uv run ty check app/ tests/` zero errors. `uv run ruff check app/ tests/` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | All inputs are aggregated user-owned game data already validated at import time. No new external endpoint, no new auth surface, no new user-controlled parameters cross any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85.1-04 | Tampering (numeric) | bucket_rows paired-diff loop | mitigate | Same filter logic as existing ex_sum/ex_n branch; d_n == ex_n invariant documented in comment; SE=0 trap handled inside compute_paired_difference_test (Plan 01) |
| T-85.1-05 | Information Disclosure | new schema fields | accept | Same data domain as existing entry_eval_p_value / entry_expected_score fields already in responses; no new sensitive surface |
| T-85.1-06 | Denial of Service | paired-diff loop | accept | Loop is the same O(len(bucket_rows)) as existing ex_sum loop — no asymptotic change; merging into existing loop avoids a second pass |
</threat_model>

<verification>
- `uv run pytest tests/` passes (full backend suite, ~1400 tests).
- `uv run ty check app/ tests/` zero errors.
- `uv run ruff check app/ tests/` clean.
- New schema fields populated with non-default values for any user with >= 10 endgame + >= 10 non-endgame games.
- `PVALUE_RELIABILITY_MIN_N` is the single source for the n-gate; no bare `10` remains as a reliability threshold in endgame_service.py (verify via `grep -n " 10\\b" app/services/endgame_service.py | grep -v "PVALUE_RELIABILITY_MIN_N\\|MIN_GAMES_FOR_TIMELINE\\|EVAL_CLIP_MAX_CP\\|batch_size"` showing only timeline/non-related occurrences).
- All `row.*` accesses in the extended loop have `# ty: ignore[unresolved-attribute]`.
</verification>

<success_criteria>
SEC1-08, SEC1-09, SEC1-10 are wire-ready on the backend. A request to `/api/endgames/overview` returns the new fields with documented gating behavior. Plan 03 can rely on `data.achievable_score_gap` as the canonical value and replace the frontend derivation.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-02-SUMMARY.md`
</output>
