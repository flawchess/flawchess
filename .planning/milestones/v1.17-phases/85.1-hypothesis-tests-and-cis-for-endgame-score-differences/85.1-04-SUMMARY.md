---
phase: 85.1-hypothesis-tests-and-cis-for-endgame-score-differences
plan: 04
status: complete
completed: 2026-05-14
---

# 85.1-04 — D-04 reopen decision + sigmoid-bias caveat + verification sweep

## What was built

1. **D-04 decision recorded** (`85.1-DECISIONS.md`): kept the Phase 85 zone-only font coloring policy for the Endgame Score Gap and Achievable Score Gap rows. The 95% CI whiskers shipped in Plan 03 carry the new uncertainty signal visually; layering a triple-gate font color on top would create redundant information and reopen a locked design decision without a new failure mode justifying the churn. No code change in `EndgameOverallPerformanceSection.tsx` from this decision.

2. **Sigmoid-bias caveat added to InfoPopover** (`EndgameOverallPerformanceSection.tsx`): the Achievable Score Gap `InfoPopover` now carries a second paragraph noting that the Achievable Score baseline comes from a Lichess winning-chances formula calibrated on a broad rating population and that the baseline may run a few percentage points high or low for any specific rating tier — so a small negative gap can partly reflect calibration drift rather than pure underperformance. FUT-05 (quantifying the bias on benchmark data) remains deferred per RESEARCH §Out-of-Scope.

3. **CHANGELOG entry** appended under `## [Unreleased]` → `### Added` describing the new hypothesis tests + CI whiskers + server-computed Achievable Score Gap.

4. **Full verification sweep** — every gate green on the merge tip of `gsd/phase-85.1-...`:
   - Backend: `uv run ruff check app/ tests/` clean; `uv run ty check app/ tests/` zero errors; `uv run pytest -q` → 1457 passed, 6 skipped.
   - Frontend: `npm run lint` clean; `npm run knip` clean; `npm test -- --run` → 353 passed (31 test files); `npm run build` succeeded (PWA precache 11 entries, 1.93 MiB).

## Key files

- Created: `.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-DECISIONS.md`
- Modified: `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` (added second `<p>` in Achievable Score Gap `InfoPopover`)
- Modified: `CHANGELOG.md` (new `[Unreleased]` → `### Added` bullet)

## Deviations from plan

- **Decision task auto-resolved.** The plan marked Task 1 as `checkpoint:decision` requiring user input. Per the orchestrator-level "no clarifying questions" directive, the reasonable call was made: keep-zone-only, matching the plan's own RESEARCH §13 recommendation and the project memory `feedback_llm_significance_signal.md` ("don't add a separate sig-test signal to the LLM payload — tighten the cohort band instead"). The user can redirect by reopening this decision and flipping `gapZoneColor` to the triple-gate variant outlined in Task 2's reopen branch.
- **No test update for new popover prose.** The plan said "add a regex assertion … if it asserts on the popover content." The existing test does not currently assert on `InfoPopover` body prose for either gap row, so no test change was made. The new paragraph is covered indirectly by the snapshot of the popover's existence under the `achievable-score-gap-info` `data-testid` and by the build typecheck.
- **Prod-tunnel smoke check skipped.** The plan listed an optional end-to-end spot check via `bin/prod_db_tunnel.sh` or the local Docker dev stack. Both require user-side credentials/tunnel state and were not run from this orchestrator; the executor pipelines validated wiring through unit tests. Recommend the user spot-check the live `/api/endgames/overview` response and the Endgames page in a browser before merging the PR.

## Requirements satisfied by this plan

- **SEC1-11** (informational-only via InfoPopover; CI whiskers already from Plan 03; D-04 decision recorded).
