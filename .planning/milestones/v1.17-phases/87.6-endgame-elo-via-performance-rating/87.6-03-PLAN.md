---
phase: 87.6-endgame-elo-via-performance-rating
plan: 03
type: execute
wave: 2
depends_on:
  - 87.6-01
files_modified:
  - app/services/insights_service.py
  - app/services/insights_llm.py
  - app/prompts/endgame_insights.md
  - tests/services/test_insights_service_series.py
  - tests/services/test_insights_llm.py
  - CHANGELOG.md
  - .planning/notes/endgame-elo-rebuild-on-score-gap.md
autonomous: true
requirements:
  - EGELO-PR-07  # LLM payload gains endgame_elo + non_endgame_elo per timeline point (SC#7 payload half)
  - EGELO-PR-07b # _PROMPT_VERSION bumps endgame_v33 → endgame_v34 + prompt prose updated (SC#7 prompt half)
  - EGELO-PR-08  # Phase 87.5 in-code K-comment block + design-note todo reference superseded (SC#8)
  - EGELO-PR-09c # Pytest coverage for LLM payload extension + endgame_v34 prompt version (SC#9 LLM half)

must_haves:
  truths:
    - "`_series_for_endgame_elo_combo` in `app/services/insights_service.py` emits per-point tuples carrying `pt.endgame_elo` AND `pt.non_endgame_elo` (in addition to existing `actual_elo` / `value` / `n`)"
    - "`TimePoint` schema in `app/schemas/insights.py` carries optional `endgame_elo: int | None = None` AND `non_endgame_elo: int | None = None` fields populated for endgame_elo_timeline series"
    - "`_PROMPT_VERSION` in `app/services/insights_llm.py` equals `endgame_v34` (was `endgame_v33`)"
    - "The `_PROMPT_VERSION` inline-comment changelog prepends a new v34 paragraph; the v33 entry stays preserved verbatim below it (changelog never trimmed)"
    - "The prompt file `app/prompts/endgame_insights.md` glossary for `endgame_elo` describes the FIDE Performance Rating formula in one sentence (no K, no additive `actual_elo + K · eg_score_gap`); a sibling `non_endgame_elo` glossary entry exists"
    - "The `endgame_elo_timeline` subsection in the prompt body documents a THIRD `[summary non_endgame_elo | platform=..., time_control=...]` block alongside the existing two (`endgame_elo` + `endgame_elo_gap`)"
    - "The 'lifts up / holds back actual rating' headline metaphor is preserved (Phase 87.5 restored it; Phase 87.6 keeps it)"
    - "The prompt prose pattern for the endgame_elo_timeline subsection includes the new sentence skeleton 'Your Endgame ELO sits at X, vs Y in non-endgame games — your endgame play is [lifting / holding back] your rating by ~Δ ELO'"
    - "MetricId / SubsectionId Literals UNCHANGED (`endgame_elo_gap`, `endgame_elo_timeline` from Phase 87.5 stay as-is)"
    - "CHANGELOG.md `[Unreleased]` `### Changed` section gains one bullet describing the Phase 87.6 PR-direct rebuild"
    - "`.planning/notes/endgame-elo-rebuild-on-score-gap.md` carries a Phase 87.6 supersession header pointing forward to `.planning/notes/endgame-elo-pr-direct-rebuild.md`"
    - "Pytest tests for `_series_for_endgame_elo_combo` assert all 3 ELO values pass through to `TimePoint` instances; pytest tests for `_PROMPT_VERSION` assert the bump and the prompt prose containing the new sentence pattern"
    - "`uv run ty check app/ tests/` exits 0; `uv run ruff check .` exits 0; `uv run pytest tests/ -x` exits 0"
  artifacts:
    - path: "app/services/insights_service.py"
      provides: "`_series_for_endgame_elo_combo` extended to thread `endgame_elo` + `non_endgame_elo` through to `TimePoint`s; `_weekly_points_to_time_points_with_elo` carries the new fields per window"
      contains: "non_endgame_elo"
    - path: "app/services/insights_llm.py"
      provides: "`_PROMPT_VERSION = 'endgame_v34'` with new v34 paragraph prepended to inline-comment changelog; `_render_endgame_elo_summary_block` or sibling renders a third `[summary non_endgame_elo | ...]` block per combo"
      contains: "endgame_v34"
    - path: "app/prompts/endgame_insights.md"
      provides: "Phase 87.6 prompt rewrite — glossary entry for `endgame_elo` rewritten under PR framing; new sibling glossary entry for `non_endgame_elo`; subsection-level documentation of the THREE summary blocks per combo; UI-label table extended with `non_endgame_elo` row; pairing/narration rule for the new sentence pattern"
      contains: "non_endgame_elo"
    - path: "app/schemas/insights.py"
      provides: "`TimePoint` gains optional `endgame_elo: int | None = None` AND `non_endgame_elo: int | None = None` fields"
      contains: "non_endgame_elo:"
  key_links:
    - from: "app/services/insights_service.py::_series_for_endgame_elo_combo"
      to: "TimePoint.endgame_elo / TimePoint.non_endgame_elo"
      via: "per-week tuple extension threading endgame_elo + non_endgame_elo into _weekly_points_to_time_points_with_elo"
      pattern: "p\\.non_endgame_elo"
    - from: "app/services/insights_llm.py::_render_endgame_elo_summary_block"
      to: "_PROMPT_VERSION"
      via: "module-level constant bumped to endgame_v34; renderer emits non_endgame_elo summary block when the metric pairs with the existing endgame_elo summary"
      pattern: "endgame_v34"
    - from: "app/prompts/endgame_insights.md"
      to: ".planning/notes/endgame-elo-pr-direct-rebuild.md"
      via: "glossary cross-reference for the FIDE Performance Rating derivation (planner-facing comment, optional in user-facing prose)"
      pattern: "Performance Rating"
---

<objective>
LLM-stack rewire for Phase 87.6: extend the per-timeline-point payload in `_series_for_endgame_elo_combo` (and the sibling `_weekly_points_to_time_points_with_elo`) with `endgame_elo` + `non_endgame_elo` fields threaded through to `TimePoint` instances; bump `_PROMPT_VERSION` from `endgame_v33` to `endgame_v34` with a new leading paragraph in the inline-comment changelog (preserving every prior entry verbatim per PATTERNS pattern 8); rewrite the `endgame_elo` glossary entry in `app/prompts/endgame_insights.md` under the FIDE Performance Rating framing, add a sibling `non_endgame_elo` glossary entry, extend the UI-label table, document the THREE summary blocks per combo in the subsection-level prose, and add the new sentence skeleton "Your Endgame ELO sits at X, vs Y in non-endgame games — your endgame play is [lifting / holding back] your rating by ~Δ ELO" to the pairing rule; render the third `[summary non_endgame_elo | ...]` block in `insights_llm.py` alongside the existing two (`endgame_elo` + `endgame_elo_gap`); append a CHANGELOG.md `[Unreleased]` bullet describing the rebuild; add a Phase 87.6 supersession header at the top of `.planning/notes/endgame-elo-rebuild-on-score-gap.md` pointing forward to the new design note; back the changes with pytest assertions on the extended payload + the new prompt version + the new sentence pattern.

Purpose: Surface the new `non_endgame_elo` field to the LLM so it can narrate the dual-PR framing the chart visualizes. CONTEXT §LLM payload (option b) is explicit: `eg_score_gap` stays as the primary signal (the §3.1.6 zone bands stay valid in score-gap units — no zone re-calibration here); PR-ELO fields are narrational additions. The "lifts up / holds back actual rating" metaphor (Phase 87.5 restored, Phase 87.4 had traded away) stays as the load-bearing narration pattern. The prompt prose gains "Your Endgame ELO sits at X, vs Y in non-endgame games" style sentences so the LLM has a canonical phrasing for the comparison the chart makes visible.

Output: Extended LLM payload threading both PRs per timeline point; `endgame_v34` prompt version bump with full changelog preservation; glossary + subsection-level prompt prose rewritten under PR framing; third `[summary non_endgame_elo]` block rendered per combo; CHANGELOG `[Unreleased]` bullet appended; Phase 87.5 design note marked superseded; pytest coverage; ty + ruff + pytest green.

Mirrors PATTERNS.md pattern 7 (LLM payload field additions) + pattern 8 (`_PROMPT_VERSION` bump + glossary update) verbatim. Does NOT touch MetricId / SubsectionId literals (CONTEXT §LLM payload + PATTERNS pattern 7 anti-pattern: "Don't introduce a new MetricId / SubsectionId literal for the new fields"). Does NOT introduce "Performance Rating" jargon into user-facing prompt prose (CONTEXT §Naming policy: user surfaces stay on plain ELO terminology; the formal distinction lives only in the chart's info popover body, which Plan 02 owns).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-CONTEXT.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-RESEARCH.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-PATTERNS.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-01-PLAN.md
@.planning/notes/endgame-elo-pr-direct-rebuild.md
@.planning/notes/endgame-elo-rebuild-on-score-gap.md
@app/prompts/endgame_insights.md
@CHANGELOG.md

<interfaces>
<!-- Key code surfaces this plan modifies. Extracted from RESEARCH.md + direct file reads so the executor does not need to re-explore the codebase. -->

From `app/services/insights_service.py`:
- Lines 1263-1287: `_series_for_endgame_elo_combo(combo, window) -> list[TimePoint] | None`. Currently emits per-week tuples `(date, value, n, actual_elo)` where `value = float(pt.endgame_elo - pt.actual_elo)`. EXTEND the tuple shape to `(date, value, n, actual_elo, endgame_elo, non_endgame_elo)` — append both new fields. The threading point is the `weekly: list[tuple[str, float, int, int]] = [...]` comprehension at lines 1283-1286 — grow to a 6-tuple `tuple[str, float, int, int, int, int]`. Pass to `_weekly_points_to_time_points_with_elo` (line 1287) — that function's signature must also grow.
- Lines 1290-1334: `_weekly_points_to_time_points_with_elo(weekly, window) -> list[TimePoint]`. Currently consumes the 4-tuple; EXTEND to consume the 6-tuple AND emit `endgame_elo` / `non_endgame_elo` on each `TimePoint`:
  * `last_3mo` branch (lines 1306-1310): each `TimePoint(bucket_start=d, value=v, n=n, actual_elo=elo)` becomes `TimePoint(..., endgame_elo=e_elo, non_endgame_elo=ne_elo)` where `e_elo` / `ne_elo` are the 5th / 6th tuple slots.
  * `all_time` branch (lines 1311-1334): weighted-mean aggregation over weeks per month. The new fields are weighted-meaned the same way `actual_elo` is at lines 1323-1324: `e_elo_weighted_sum = sum(e * n for _, n, _, e, _ in weeks)`; `mean_e_elo = round(e_elo_weighted_sum / total_n)`. Same shape for `non_endgame_elo`. (Adjust the `for d, v, n, elo, e_elo, ne_elo in sorted(weekly, ...)` unpacking too.) Apply equivalently to the `statistics.mean` fallback when `total_n == 0`.

From `app/schemas/insights.py`:
- Lines 291-309: `TimePoint` BaseModel. Currently carries `actual_elo: int | None = None` (populated only for endgame_elo_timeline series; None for all other timelines). ADD `endgame_elo: int | None = None` and `non_endgame_elo: int | None = None` as siblings with the same gating contract (populated only for endgame_elo_timeline; None elsewhere). Update the class docstring at lines 291-304 to mention the two new fields.
- The `actual_elo` defaulting-to-None pattern was added by Phase 87.5; mirror it exactly for the two new fields.

From `app/services/insights_llm.py`:
- Line 68: `_PROMPT_VERSION = "endgame_v33"` with the full per-version changelog crammed into the inline comment. Phase 87.6 BUMPS to `endgame_v34`. Prepend a new leading paragraph to the inline comment per PATTERNS pattern 8. The existing v33 entry continues unmodified below it (PATTERNS anti-pattern: "Do NOT trim the historical inline-comment changelog. The pattern is intentional — it's the audit trail for cache invalidation across deploys").
- New v34 paragraph (PATTERNS pattern 8 template):
  ```
  v34 (260517 Phase 87.6 PR-direct rebuild): replaced the additive `K · eg_score_gap` mapping with FIDE Performance Rating computed per side. `endgame_elo = R_opp_avg_E + 400·log10(s_E*/(1−s_E*))` with Laplace-smoothed `s_E* = (n·score + 1) / (n + 2)` over the trailing 100-game window; same formula symmetric on the non-endgame side as `non_endgame_elo`. K=450 deleted, not retuned — the `400` is theory-fixed by Elo's logistic-skill assumption. New per-point fields `endgame_elo` (re-derived; same name, new math) and `non_endgame_elo` (new) flow into the existing `endgame_elo_timeline` subsection's payload. The subsection now emits THREE `[summary]` blocks per (platform, time_control) combo: `endgame_elo`, `non_endgame_elo`, `endgame_elo_gap` (gap stays the zoned metric; PR-ELO blocks have no zone). New prompt sentence skeleton: "Your Endgame ELO sits at X, vs Y in non-endgame games — your endgame play is [lifting / holding back] your rating by ~Δ ELO." The "lifts up / holds back actual rating" headline metaphor (v33 restored) stays. MetricId `endgame_elo_gap` and SubsectionId `endgame_elo_timeline` UNCHANGED from v33. Cache-busts v33 reports so newly generated narration uses the new sentence pattern. See `.planning/notes/endgame-elo-pr-direct-rebuild.md`.
  ```
- Lines 1166-1184: `_endgame_elo_per_bucket(points)` helper — UNCHANGED (it consumes `pt.actual_elo + pt.value` to back-derive Endgame ELO; that math still works under PR because `value = endgame_elo - actual_elo` still holds at the TimePoint level — the new PR-derived `endgame_elo` field on the TimePoint IS the value Phase 87.5 also wrote, just computed via PR instead of K·gap). No code change here.
- Lines 1187-1256 (approx): `_render_endgame_elo_summary_block`. Currently renders the `[summary endgame_elo | dim]` block from the endgame_elo_gap series. EXTEND with a sibling renderer (either an additional method `_render_non_endgame_elo_summary_block` or in-line additional emission) that emits a parallel `[summary non_endgame_elo | dim]` block. Threading: each `TimePoint` now carries `non_endgame_elo: int | None`; the renderer reads it from the same series points that drive the existing endgame_elo summary. The new block sits between the existing `endgame_elo` and `endgame_elo_gap` blocks per the subsection ordering defined in the prompt prose update.
- Lines 1430-1437: subsection-iteration block that wires the endgame_elo_timeline render path. EXTEND to call the new non_endgame_elo renderer at the right ordering (between endgame_elo and endgame_elo_gap). The exact integration depends on the existing structure — the executor reads the surrounding code (lines 1620-1650 area where `subsection_id == "endgame_elo_timeline"` dispatches the renderer) and threads in the new block emission.
- Lines 1620-1650 (approx): the subsection dispatcher `if subsection_id == "endgame_elo_timeline" and metric == "endgame_elo_gap"`. EXTEND to also call the non_endgame_elo block renderer after the endgame_elo block. (The endgame_elo_gap branch at line 1637-1639 is the existing call site for `_render_endgame_elo_summary_block`; emit the non_endgame_elo block immediately after the endgame_elo block.)

From `app/prompts/endgame_insights.md`:
- Lines 80-92 (UI-label table): ADD a row for `non_endgame_elo` next to the existing `endgame_elo` row at line 88. Suggested entry:
  ```
  | `non_endgame_elo`             | "Non-Endgame ELO"                   | "Non-Endgame ELO of 1500" |
  ```
- Lines 132-138 (the `[series ...]` block documentation for `endgame_elo_timeline`): UPDATE line 138 to mention the third number per row. Currently says: "each row carries two numbers: `gap=<int>` (endgame_elo − actual_elo, the zoned value) and `elo=<int>` (the user's actual rating at that bucket)". Rewrite to: "each row carries three numbers per side: `gap=<int>` (endgame_elo − actual_elo, the zoned value), `elo=<int>` (the user's actual rating at that bucket), and `non_eg_elo=<int>` (the Non-Endgame ELO at that bucket, the PR computed on non-endgame games in the same trailing window). The midpoint of `endgame_elo` and `non_eg_elo` sits at `elo` by construction (Performance Rating midpoint property); a wide spread between the two PRs is a salient endgame-vs-non-endgame gap regardless of where `elo` is heading."
- Lines 140-144 (the THREE-summary-blocks rule for endgame_elo_timeline): currently documents TWO blocks (`endgame_elo` + `endgame_elo_gap`). Rewrite to document THREE:
  ```
  The `endgame_elo_timeline` subsection emits THREE summary blocks per `(platform, time_control)` combo, in this order:
  1. `[summary endgame_elo | platform=..., time_control=...]` — the **absolute** Endgame ELO (FIDE Performance Rating on this combo's endgame games over the trailing 100-game window). No `zone` / `quality` fields — Endgame ELO has no calibrated band; it is the chart's headline value. Quote this `mean=<int> Elo` as the primary narration value for endgame ELO claims.
  2. `[summary non_endgame_elo | platform=..., time_control=...]` — the **absolute** Non-Endgame ELO (FIDE Performance Rating on this combo's non-endgame games over the same trailing window). Same shape as endgame_elo; no `zone` / `quality` fields. Quote this when narrating the comparison "your endgame play is [lifting / holding back] your rating".
  3. `[summary endgame_elo_gap | platform=..., time_control=...]` — the gap (`endgame_elo − actual_elo`, signed Elo). Carries the `zone` (typical -100 to +100), the `[near edge]` marker, and the `within-noise` flags. Use the gap's **zone and flags** to decide how confidently to frame the Endgame ELO reading; do not lead with the gap number itself.
  ```
- Line 144 (pairing rule): rewrite the trailing pairing rule from "lead with 'Endgame ELO at X Elo'..." to add the new sentence skeleton:
  ```
  Pairing rule: when narrating a combo, lead with "Endgame ELO at X Elo" (from the endgame_elo summary). Then cite Non-Endgame ELO and frame the comparison using one of these forms:
    - "Your Endgame ELO sits at X, vs Y in non-endgame games — your endgame play is lifting your rating by ~Δ ELO." (when Endgame ELO > Non-Endgame ELO)
    - "Your Endgame ELO sits at X, vs Y in non-endgame games — your endgame play is holding back your rating by ~Δ ELO." (when Endgame ELO < Non-Endgame ELO)
    - "Your Endgame ELO at X is right alongside your Non-Endgame ELO at Y — your endgame is neither lifting nor holding back your rating." (when Δ is within the typical band around 0)
  Then qualify the gap interpretation using the `endgame_elo_gap` summary's zone and flags ("…sits +60 Elo above actual rating, well within the typical band"). Do NOT cite the gap as the headline; do NOT cite Non-Endgame ELO without first citing Endgame ELO.
  ```
- Lines 428-432 (the `endgame_elo` glossary entry): REWRITE under PR framing. Suggested text:
  ```
  - **endgame_elo** (UI label: "Endgame ELO"): your FIDE Performance Rating computed on your endgame games over the trailing 100-game window for this platform and time control. Formula: `endgame_elo = R_opp_avg + 400 · log10(s* / (1 − s*))` where `R_opp_avg` is the trailing-window mean opponent rating and `s*` is the Laplace-smoothed per-game score `(n · score + 1) / (n + 2)`. Conceptually: "what your rating would be if you played all your games at the level of your endgame games". Positive Endgame Score Gap (your endgame games scoring better than your non-endgame games) lifts your Endgame ELO above your actual rating; negative gaps push it below. **This is the chart's headline endgame value and the primary number to cite when narrating endgame ELO.**
    - Scale: absolute **Elo points** (e.g. `1734`). Quote as "Endgame ELO of 1734" or "Endgame ELO at 1734".
    - Fanned out per `(platform, time_control)` combo via the `dimension` field.
    - No `zone` / `quality` fields — endgame_elo is an absolute rating, not a zoned metric. The accompanying `[summary endgame_elo_gap]` block carries the zone interpretation.
    - Narrate within a (platform, time_control) combo, not across (see cross-platform note in Player profile section).
  ```
- ADD a sibling `non_endgame_elo` glossary entry immediately after the `endgame_elo` entry (before the `endgame_elo_gap` entry at line 434). Suggested text:
  ```
  - **non_endgame_elo** (UI label: "Non-Endgame ELO"): your FIDE Performance Rating computed on your non-endgame games over the same trailing 100-game window. Same formula as Endgame ELO, just over the non-endgame subset of games. Conceptually: "what your rating would be if you played all your games at the level of your non-endgame games". The midpoint of Endgame ELO and Non-Endgame ELO sits at your actual rating by construction (Performance Rating midpoint property), so a wide spread between the two PRs is a salient endgame-vs-non-endgame gap regardless of where the actual rating is heading.
    - Scale: absolute **Elo points** (e.g. `1671`). Quote as "Non-Endgame ELO of 1671" or "Non-Endgame ELO at 1671".
    - Fanned out per `(platform, time_control)` combo via the `dimension` field. Always paired with an `[summary endgame_elo]` block above it.
    - No `zone` / `quality` fields — Non-Endgame ELO is an absolute rating, not a zoned metric.
    - Use the comparison Endgame ELO vs Non-Endgame ELO to frame the "lifts up / holds back" narration. Do NOT cite Non-Endgame ELO without first citing Endgame ELO.
  ```
- Line 434 (`endgame_elo_gap` glossary entry): unchanged — the gap math `endgame_elo − actual_elo` still holds under PR (research §2: midpoint property anchors actual_elo between the two PRs; the gap is now the PR_E - actual_elo distance, which carries the same zoned interpretation as before).
- Line 193 (cross-section story "Endgame lagging rating growth"): unchanged — the framing still works under PR (the gap math is the same).

From `tests/services/test_insights_service_series.py`:
- The file currently has fixture-based tests for `_series_for_endgame_elo_combo` and `_weekly_points_to_time_points_with_elo` (per research §4a: lines 269, 309). EXTEND the fixture builder to produce points with both `endgame_elo` and `non_endgame_elo`; assert that emitted `TimePoint` instances carry both fields populated; assert per-side independence (changing only `non_endgame_elo` in the input fixture doesn't shift the emitted `endgame_elo` and vice versa).
- ADD test class `class TestSeriesForEndgameEloCombo`:
  * `test_emits_both_pr_fields`: construct a 3-point combo with distinct `endgame_elo` and `non_endgame_elo` values per point; assert each emitted `TimePoint.endgame_elo == input.endgame_elo` and `TimePoint.non_endgame_elo == input.non_endgame_elo` (exact pass-through under `last_3mo` window).
  * `test_all_time_weighted_mean_threads_both_pr_fields`: construct a 4-point combo spanning 2 months (2 weekly buckets per month) with distinct PR values; under `all_time` window, assert the monthly aggregated TimePoints' `endgame_elo` matches the weighted mean of the input weekly `endgame_elo` values (rounded int); same for `non_endgame_elo`.
- ADD test class `class TestEndgameEloLLMPayloadShape`:
  * `test_subsection_payload_carries_non_endgame_elo`: invoke `_series_for_endgame_elo_combo` on a fixture combo and assert every returned `TimePoint` carries `non_endgame_elo is not None`.
  * `test_other_timeline_series_have_none_non_endgame_elo`: invoke any other timeline series helper (e.g. `_series_for_score_timeline_combo`) and assert the returned `TimePoint`s have `non_endgame_elo is None` (gating contract: only endgame_elo_timeline populates).

From `tests/services/test_insights_llm.py`:
- Search for the existing `_PROMPT_VERSION` assertion in the test file. ALL `_PROMPT_VERSION == "endgame_v33"` assertions become `"endgame_v34"`.
- ADD test `test_prompt_version_is_endgame_v34`: `assert _PROMPT_VERSION == "endgame_v34"`. (May already exist as `test_prompt_version_is_endgame_v33`; rename + update.)
- ADD test `test_prompt_changelog_preserves_v33_entry`: assert that the `_PROMPT_VERSION` source line (read via `inspect.getsource` or `Path("app/services/insights_llm.py").read_text()`) contains the substring `"v33 (260517 Phase 87.5 Endgame ELO rebuild"` — verifying the prior changelog entry was NOT trimmed when v34 was prepended.
- ADD test `test_prompt_prose_includes_non_endgame_elo_sentence_pattern`: read `app/prompts/endgame_insights.md` and assert it contains the substring `"Your Endgame ELO sits at X, vs Y in non-endgame games"` (the canonical sentence skeleton; verbatim presence is required so future drift surfaces as a test failure).
- ADD test `test_prompt_glossary_has_non_endgame_elo_entry`: assert the prompt file contains `"- **non_endgame_elo** (UI label: \"Non-Endgame ELO\")"` (verifies the sibling glossary entry exists).
- ADD test `test_endgame_elo_timeline_emits_three_summary_blocks`: build a mock `SubsectionFinding` + `TimePoint` series with populated `endgame_elo` / `non_endgame_elo` / `actual_elo`; invoke the subsection renderer and assert the rendered string contains all three substrings `"[summary endgame_elo | platform="`, `"[summary non_endgame_elo | platform="`, `"[summary endgame_elo_gap | platform="`.

From `CHANGELOG.md`:
- The current `[Unreleased]` `### Changed` section (lines 17-25 area per the snapshot) carries the Phase 87.5 bullet from the just-shipped Endgame ELO rebuild. APPEND a Phase 87.6 bullet at the TOP of `### Changed` (newest first per the file's existing convention):
  ```
  - **Phase 87.6: Endgame ELO Timeline rebuilt on FIDE Performance Rating per side.** The timeline now uses the textbook Performance Rating formula `endgame_elo = R_opp_avg + 400·log10(s*/(1−s*))` with Laplace-smoothed scores, applied independently to endgame and non-endgame games. A new Non-Endgame ELO line is added per combo (fine stroke); the prior dashed Endgame ELO line becomes a fine non-dashed line. A signed band between the two PRs is filled green when Endgame ELO ≥ Non-Endgame ELO and red when below — the endgame-vs-non-endgame gap is now visible at a glance. The Actual ELO bold line is bracketed by the two PRs by construction (Performance Rating midpoint property). K=450 deleted, not retuned — the `400` is theory-fixed by Elo's logistic-skill assumption. LLM Insights gains a per-point Non-Endgame ELO field and a new "your endgame play is lifting / holding back your rating" sentence pattern; prompt version bumps `endgame_v33` → `endgame_v34`. See `.planning/notes/endgame-elo-pr-direct-rebuild.md`.
  ```

From `.planning/notes/endgame-elo-rebuild-on-score-gap.md` (Phase 87.5 design note):
- ADD a one-paragraph supersession header at the TOP of the file (above the existing front matter / first heading):
  ```
  > **Phase 87.6 supersedes this design note (2026-05-17).** The additive `K · eg_score_gap` mapping documented below was eyeball-calibrated against §3.1.6 percentiles and shipped in Phase 87.5; Phase 87.6 replaces it with the FIDE Performance Rating formula, which has no free parameter. See `.planning/notes/endgame-elo-pr-direct-rebuild.md` for the PR derivation and the midpoint-property argument that anchors Actual ELO between the two PRs. K is deleted, not retuned; the §3.1.6 score-gap zone bands remain valid (they are still the primary LLM signal). This note is preserved as the audit trail for the Phase 87.5 → 87.6 transition.
  ```
- The note's existing body stays unchanged — it carries the Phase 87.5 rationale, which is valuable history even after supersession.

Note on the `.planning/todos/pending/calibrate-endgame-elo-k.md` cleanup: research §5c and CONTEXT §decisions say this file should be deleted. The file ALREADY LIVES at `.planning/todos/done/calibrate-endgame-elo-k.md` (it was moved by Phase 87.5's completion). No-op for this plan — confirm via `ls .planning/todos/pending/ | grep -i calibrate` (must return empty) and skip the deletion step. Note this in the SUMMARY.
</interfaces>

<chart_data>
<!-- Reference numbers for prompt prose + tests. -->

Tooltip phrasing example (from CONTEXT §specifics):
```
Sunday 2026-05-10
Endgame ELO     1734  (52 games)
Non-Endgame ELO 1671
Actual ELO      1700
```

The numerical example `Endgame ELO 1734 / Actual 1700 / Non-Endgame ELO 1671` is consistent with the midpoint property (midpoint = (1734 + 1671) / 2 = 1702.5; actual_elo = 1700; drift 2.5 ELO inside the 5-ELO tolerance). Use these numbers in test fixtures + prompt prose examples where a concrete number is needed.

Sentence skeletons for the prompt pairing rule (from CONTEXT §LLM payload + the design note):
- Lifting: "Your Endgame ELO sits at 1734, vs 1671 in non-endgame games — your endgame play is lifting your rating by ~30 ELO."
- Holding back: "Your Endgame ELO sits at 1671, vs 1734 in non-endgame games — your endgame play is holding back your rating by ~30 ELO."
- Neutral: "Your Endgame ELO at 1710 is right alongside your Non-Endgame ELO at 1690 — your endgame is neither lifting nor holding back your rating."
</chart_data>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests — extended LLM payload + v34 prompt + non_endgame_elo glossary</name>
  <files>
    tests/services/test_insights_service_series.py,
    tests/services/test_insights_llm.py
  </files>
  <behavior>
    Land the new tests BEFORE the helper / schema / prompt changes so we have a RED → GREEN cycle to drive the rest of the plan.

    **`tests/services/test_insights_service_series.py`** — extend with two new test classes per <interfaces>:
    - `class TestSeriesForEndgameEloComboPRFields`:
      * `test_emits_endgame_and_non_endgame_elo_per_timepoint`: construct an `EndgameEloTimelineCombo` fixture with 3 points carrying distinct `endgame_elo` / `non_endgame_elo` / `actual_elo` (e.g. point 1: 1700/1650/1675; point 2: 1700/1750/1725; point 3: 1750/1700/1725). Call `_series_for_endgame_elo_combo(combo, window="last_3mo")` and assert: result is not None; len(result) == 3; for each emitted `TimePoint`, `tp.endgame_elo == input.endgame_elo` AND `tp.non_endgame_elo == input.non_endgame_elo` AND `tp.actual_elo == input.actual_elo`.
      * `test_all_time_window_aggregates_both_pr_fields`: construct a 4-point combo spanning 2 months (Jan 2026 weeks + Feb 2026 weeks) with distinct PR values per week and `per_week_endgame_games` weights. Call with `window="all_time"`; assert that the 2 emitted monthly TimePoints carry weighted-mean `endgame_elo` and `non_endgame_elo` matching `round(sum(elo_i * n_i) / sum(n_i))` per side.
      * `test_other_timeline_series_have_none_for_pr_fields`: build a `score_gap_timeline` series helper invocation (use whichever exists — e.g. `_series_for_score_timeline_combo` or similar; if the codebase doesn't expose one, mock a `TimePoint(bucket_start='2026-04-06', value=0.05, n=50)` construction and assert `tp.endgame_elo is None` AND `tp.non_endgame_elo is None`). The gating contract is: only `endgame_elo_timeline` populates the PR fields; everywhere else defaults to None.

    **`tests/services/test_insights_llm.py`** — extend with 4 new tests:
    - Update any existing `test_prompt_version_is_endgame_v33` test (or whatever it's named): rename to `test_prompt_version_is_endgame_v34` and update the asserted version string. If multiple sites in the file assert `"endgame_v33"`, update all of them in lockstep.
    - `test_prompt_changelog_preserves_v33_entry`: read the `_PROMPT_VERSION` line source (via `Path("app/services/insights_llm.py").read_text()`) and assert the substring `"v33 (260517 Phase 87.5 Endgame ELO rebuild"` is present (verifies the prior changelog entry was NOT trimmed when v34 was prepended).
    - `test_prompt_prose_includes_non_endgame_elo_sentence_pattern`: read `app/prompts/endgame_insights.md` (via `Path(...).read_text()`) and assert the substring `"Your Endgame ELO sits at X, vs Y in non-endgame games"` is present.
    - `test_prompt_glossary_has_non_endgame_elo_entry`: assert the prompt file contains the substring `'- **non_endgame_elo** (UI label: "Non-Endgame ELO")'` (verbatim glossary header — uses double-quotes inside the label string per the existing pattern at line 88 / 441 of the prompt file).
    - `test_endgame_elo_timeline_emits_three_summary_blocks`: build a minimal mock setup invoking the subsection renderer for `endgame_elo_timeline` with a populated series (`TimePoint`s carrying `endgame_elo` / `non_endgame_elo` / `actual_elo`). Assert the rendered output string contains all three substrings: `"[summary endgame_elo | platform="`, `"[summary non_endgame_elo | platform="`, `"[summary endgame_elo_gap | platform="`. The exact mock shape will depend on the existing test patterns in the file — read the surrounding test methods (e.g. existing tests that build mock `SubsectionFinding` + `TimePoint` series for `_render_endgame_elo_summary_block`) and mirror them.

    Critical CLAUDE.md gates:
    - Tests should be RED until Task 2 lands the helper + schema + prompt changes.
    - `uv run pytest tests/services/test_insights_service_series.py::TestSeriesForEndgameEloComboPRFields tests/services/test_insights_llm.py::test_prompt_version_is_endgame_v34 -x` confirms RED state.
  </behavior>
  <action>
    1. Edit `tests/services/test_insights_service_series.py`:
       a. Add `class TestSeriesForEndgameEloComboPRFields` with 3 test methods per <behavior>.
       b. Update any existing fixture builders (`_make_endgame_elo_combo` or similar) that don't currently set `non_endgame_elo` — but rather than rewriting all of them, add a new local fixture builder in the new test class to keep the diff minimal.
    2. Edit `tests/services/test_insights_llm.py`:
       a. Rename / update any `test_prompt_version_is_endgame_v33` (or equivalent) to assert `"endgame_v34"`. Grep `endgame_v33` in this file to find all sites.
       b. ADD the 4 new tests per <behavior>.
    3. Run `uv run pytest tests/services/test_insights_service_series.py tests/services/test_insights_llm.py -x 2>&1 | tail -30` — confirm RED (new tests fail because the helper, prompt, and version haven't been updated yet).
    4. Commit: `test(87.6-03): RED tests for extended LLM payload + v34 prompt + non_endgame_elo glossary`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_insights_service_series.py::TestSeriesForEndgameEloComboPRFields tests/services/test_insights_llm.py -k "endgame_v34 or non_endgame_elo or three_summary_blocks or v33_entry" 2>&1 | grep -E "FAILED|ERROR" | wc -l</automated>
  </verify>
  <done>
    - `tests/services/test_insights_service_series.py::TestSeriesForEndgameEloComboPRFields` class added with 3 test methods (RED — `TimePoint` doesn't yet carry the PR fields).
    - `tests/services/test_insights_llm.py` updated: prompt-version assertion bumped to v34 (RED — code still says v33); 4 new tests added (RED — prompt and renderer not yet updated).
    - Commit `test(87.6-03): RED tests for extended LLM payload + v34 prompt + non_endgame_elo glossary` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic LLM rewire — TimePoint schema + payload threading + prompt version + glossary + CHANGELOG + design-note supersession</name>
  <files>
    app/schemas/insights.py,
    app/services/insights_service.py,
    app/services/insights_llm.py,
    app/prompts/endgame_insights.md,
    CHANGELOG.md,
    .planning/notes/endgame-elo-rebuild-on-score-gap.md
  </files>
  <behavior>
    Single atomic pass spanning schema + service + prompt + docs. The Phase 87.5 Plan 03 lesson (and the Phase 87.4 Plan 03 lesson before it): when bumping `_PROMPT_VERSION`, every test assertion + every test fixture + the glossary + the subsection-level prose must change in lockstep, or the new version's first prod request emits a malformed prompt that the LLM tries to interpret with stale glossary.

    Order within the pass (top-down by dependency):

    **`app/schemas/insights.py`** (do FIRST — service depends on the extended TimePoint):
    - `TimePoint` (lines 291-309): ADD two optional fields after `actual_elo`:
      ```python
      endgame_elo: int | None = None
      non_endgame_elo: int | None = None
      ```
      Update the class docstring (lines 291-304) to mention the two new fields, mirroring the `actual_elo` gating language: "`endgame_elo` and `non_endgame_elo` are populated ONLY for `endgame_elo_timeline` series; None for all other timelines (Phase 87.6). Carrying both PRs alongside the gap lets the prompt render the new Non-Endgame ELO comparison sentence pattern."

    **`app/services/insights_service.py`** (do SECOND — payload threading):
    - `_series_for_endgame_elo_combo` (lines 1263-1287):
      a. Update the `weekly` tuple type annotation at line 1283: `weekly: list[tuple[str, float, int, int, int, int]] = [...]` — 6-tuple.
      b. Update the comprehension at lines 1283-1286 to read the new fields:
         ```python
         weekly: list[tuple[str, float, int, int, int, int]] = [
             (p.date, float(p.endgame_elo - p.actual_elo), p.per_week_endgame_games, p.actual_elo, p.endgame_elo, p.non_endgame_elo)
             for p in combo.points
         ]
         ```
      c. Update the docstring (lines 1267-1280): replace the Phase 87.5 (D-06) note with "Phase 87.6: per-point Non-Endgame ELO field carried through alongside Endgame ELO and Actual ELO, so the prompt can render the 'lifts up / holds back' comparison sentence skeleton."
    - `_weekly_points_to_time_points_with_elo` (lines 1290-1334):
      a. Update the function signature at lines 1290-1293: `weekly: list[tuple[str, float, int, int, int, int]]` — 6-tuple.
      b. Update the `last_3mo` branch at lines 1306-1310:
         ```python
         if window == "last_3mo":
             return [
                 TimePoint(
                     bucket_start=d,
                     value=v,
                     n=n,
                     actual_elo=elo,
                     endgame_elo=e_elo,
                     non_endgame_elo=ne_elo,
                 )
                 for d, v, n, elo, e_elo, ne_elo in sorted(weekly, key=lambda t: t[0])
             ]
         ```
      c. Update the `all_time` branch at lines 1311-1334:
         - Update the `buckets` dict value-type annotation: `buckets: dict[str, list[tuple[float, int, int, int, int]]] = defaultdict(list)` — 5-tuple per week (was 3).
         - Update the bucket-append at line 1313-1315: `for date_iso, value, n, elo, e_elo, ne_elo in weekly: buckets[ym].append((value, n, elo, e_elo, ne_elo))`.
         - Update the per-month aggregation loop at lines 1317-1334:
           ```python
           for ym in sorted(buckets.keys()):
               weeks = buckets[ym]
               total_n = sum(n for _, n, _, _, _ in weeks)
               if total_n > 0:
                   weighted_sum = sum(v * n for v, n, _, _, _ in weeks)
                   mean_value = weighted_sum / total_n
                   elo_weighted_sum = sum(elo * n for _, n, elo, _, _ in weeks)
                   mean_elo = round(elo_weighted_sum / total_n)
                   e_elo_weighted_sum = sum(e * n for _, n, _, e, _ in weeks)
                   mean_e_elo = round(e_elo_weighted_sum / total_n)
                   ne_elo_weighted_sum = sum(ne * n for _, n, _, _, ne in weeks)
                   mean_ne_elo = round(ne_elo_weighted_sum / total_n)
               else:
                   mean_value = statistics.mean(v for v, _, _, _, _ in weeks)
                   mean_elo = round(statistics.mean(elo for _, _, elo, _, _ in weeks))
                   mean_e_elo = round(statistics.mean(e for _, _, _, e, _ in weeks))
                   mean_ne_elo = round(statistics.mean(ne for _, _, _, _, ne in weeks))
               points.append(
                   TimePoint(
                       bucket_start=f"{ym}-01",
                       value=mean_value,
                       n=total_n,
                       actual_elo=mean_elo,
                       endgame_elo=mean_e_elo,
                       non_endgame_elo=mean_ne_elo,
                   )
               )
           ```
         - Update the docstring (lines 1294-1303) to mention the new fields.

    **`app/services/insights_llm.py`** (do THIRD — prompt version + renderer):
    - Line 68: BUMP `_PROMPT_VERSION = "endgame_v33"` to `_PROMPT_VERSION = "endgame_v34"`. PREPEND the new v34 paragraph (from <interfaces>) to the inline comment, keeping every prior entry (v14-v33) verbatim per PATTERNS pattern 8.
    - ADD a new renderer `_render_non_endgame_elo_summary_block` next to `_render_endgame_elo_summary_block` (~line 1187). Body mirrors the existing endgame_elo renderer EXCEPT the per-bucket value source is `pt.non_endgame_elo` (read directly from each `TimePoint`, NOT derived via `actual_elo + value`). Suggested signature:
      ```python
      def _render_non_endgame_elo_summary_block(
          *,
          dim_key: str,
          all_time_finding: SubsectionFinding | None,
          last_3mo_finding: SubsectionFinding | None,
          all_time_series: list[TimePoint] | None,
          last_3mo_series: list[TimePoint] | None,
          stale_markers: dict[int, str],
      ) -> list[str]:
          """Emit `[summary non_endgame_elo | dim]` derived from the endgame_elo_timeline series.

          Phase 87.6: parallel to _render_endgame_elo_summary_block. Reads
          `pt.non_endgame_elo` directly (populated only on endgame_elo_timeline
          TimePoints). No zone / quality fields — Non-Endgame ELO is an absolute
          rating, not a zoned metric.
          """
          # ... implementation analogous to the endgame_elo renderer, but reading
          # pt.non_endgame_elo instead of (pt.actual_elo + pt.value). ...
      ```
      The per-bucket extraction helper (analog of `_endgame_elo_per_bucket` at lines 1166-1184) is:
      ```python
      def _non_endgame_elo_per_bucket(points: list[TimePoint] | None) -> list[tuple[float, int]]:
          out: list[tuple[float, int]] = []
          if not points:
              return out
          for pt in points:
              if pt.non_endgame_elo is None:
                  continue
              out.append((pt.non_endgame_elo, pt.n))
          return out
      ```
    - In the subsection dispatcher around lines 1620-1650 (`if subsection_id == "endgame_elo_timeline" and metric == "endgame_elo_gap"`): immediately after the existing call to `_render_endgame_elo_summary_block(...)` (around line 1639), call `_render_non_endgame_elo_summary_block(...)` with the same args. This places the new block between the existing `endgame_elo` and `endgame_elo_gap` blocks per the subsection ordering documented in the prompt.

    **`app/prompts/endgame_insights.md`** (do FOURTH — prompt prose):
    - UI-label table (lines 80-92): ADD a row for `non_endgame_elo` next to the existing `endgame_elo` row at line 88:
      ```
      | `non_endgame_elo`             | "Non-Endgame ELO"                   | "Non-Endgame ELO of 1500" |
      ```
    - Lines 132-138 (`[series ...]` doc): rewrite line 138 to describe the three numbers per row (`gap=`, `elo=`, `non_eg_elo=`) per <interfaces>.
    - Lines 140-144 (subsection-level prose): rewrite from TWO summary blocks to THREE per <interfaces>. Include the new pairing rule with the three sentence skeletons.
    - Lines 428-432 (`endgame_elo` glossary entry): rewrite under PR framing per <interfaces>.
    - Insert a new `non_endgame_elo` glossary entry immediately after `endgame_elo` (before `endgame_elo_gap` at line 434) per <interfaces>.
    - Line 434 (`endgame_elo_gap`): unchanged.
    - Line 193 (cross-section story "Endgame lagging rating growth"): UNCHANGED — the framing still works under PR. (Sanity-check: the gap math `endgame_elo - actual_elo` is the same number whether endgame_elo comes from PR or from K·gap; midpoint property anchors actual_elo between the two PRs, so the "regressing gap while rising elo" framing still applies.)

    **`CHANGELOG.md`**:
    - APPEND the Phase 87.6 bullet at the TOP of `### Changed` (above the Phase 87.5 bullet per the file's newest-first convention). Bullet text per <interfaces>.

    **`.planning/notes/endgame-elo-rebuild-on-score-gap.md`**:
    - PREPEND the supersession header per <interfaces>.

    Critical CLAUDE.md gates:
    - `uv run ty check app/ tests/` exits 0 — the new optional fields on `TimePoint` narrow correctly; the 6-tuple type signatures match throughout the threading.
    - `uv run ruff check .` exits 0.
    - `uv run pytest tests/ -x` exits 0 — Task 1's RED tests now GREEN; baseline tests unchanged.
    - No `# ty: ignore` workarounds.
    - No magic numbers (the new sentence skeleton uses placeholder `X, Y, Δ` not literals).
    - The `_PROMPT_VERSION` inline-comment changelog is NEVER trimmed (PATTERNS pattern 8 anti-pattern).
    - The new Performance Rating glossary entry's "Performance Rating" jargon is glossary-only; it does NOT leak into user-facing prompt prose (CONTEXT §Naming policy). Verify by grepping the prompt body (outside the glossary section): the substring `"Performance Rating"` should appear ONLY inside the `## Metric glossary` section AND in one place inside the `[series ...]` doc at line 138 (the planner's parenthetical "Performance Rating midpoint property" — acceptable as a brief technical reference).
    - CHANGELOG.md bullet is terse (≤ 8 lines, user-facing language, no implementation jargon beyond what's necessary to motivate the change).
  </behavior>
  <action>
    1. Edit `app/schemas/insights.py`: add `endgame_elo: int | None = None` and `non_endgame_elo: int | None = None` to `TimePoint`. Update class docstring.
    2. Edit `app/services/insights_service.py`:
       a. Update `_series_for_endgame_elo_combo` tuple shape + docstring.
       b. Update `_weekly_points_to_time_points_with_elo` signature + both window branches (last_3mo emits the new fields directly; all_time computes weighted means for both PR fields).
    3. Edit `app/services/insights_llm.py`:
       a. Bump `_PROMPT_VERSION` to `endgame_v34`; prepend the new v34 paragraph to the inline comment.
       b. Add `_non_endgame_elo_per_bucket` helper near line 1184.
       c. Add `_render_non_endgame_elo_summary_block` renderer near line 1257 (after the existing endgame_elo block renderer).
       d. In the subsection dispatcher around line 1639, call the new renderer immediately after the existing `_render_endgame_elo_summary_block` call.
    4. Edit `app/prompts/endgame_insights.md`:
       a. UI-label table: add `non_endgame_elo` row.
       b. Lines 132-138: rewrite the `[series ...]` row description for three numbers.
       c. Lines 140-144: rewrite from 2 blocks to 3 blocks; rewrite the pairing rule with the three sentence skeletons.
       d. Lines 428-432: rewrite `endgame_elo` glossary entry under PR framing.
       e. Insert `non_endgame_elo` glossary entry (before line 434 `endgame_elo_gap`).
    5. Edit `CHANGELOG.md`: prepend the Phase 87.6 bullet to `### Changed` per <interfaces>.
    6. Edit `.planning/notes/endgame-elo-rebuild-on-score-gap.md`: prepend the supersession header per <interfaces>.
    7. Confirm no-op cleanup: `ls .planning/todos/pending/ | grep -i calibrate-endgame-elo-k` returns empty (file already in `done/` per Phase 87.5 closure). Note this in the SUMMARY; no file delete needed.
    8. Run quality gates iteratively:
       - `uv run ty check app/ tests/` — clean.
       - `uv run ruff check .` — clean.
       - `uv run ruff format app/services/insights_service.py app/services/insights_llm.py app/schemas/insights.py` — apply if drift.
       - `uv run pytest tests/services/test_insights_service_series.py tests/services/test_insights_llm.py -x` — Task 1 RED tests now GREEN.
       - `uv run pytest tests/ -x` — full suite green; no regressions.
    9. Verify the regression grep: `grep -nE "endgame_v33" app/services/insights_llm.py` — exactly ONE match (inside the historical changelog comment block, preserved verbatim); no matches in code. `grep -nE "_PROMPT_VERSION\s*=\s*[\"']endgame_v34[\"']" app/services/insights_llm.py` — exactly one match.
    10. Commit: `feat(87.6-03): LLM payload + endgame_v34 prompt + non_endgame_elo glossary + CHANGELOG + supersession header`.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run ruff check . && uv run pytest tests/services/test_insights_service_series.py tests/services/test_insights_llm.py -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `app/schemas/insights.py::TimePoint` carries `endgame_elo: int | None = None` and `non_endgame_elo: int | None = None`; class docstring updated.
    - `app/services/insights_service.py::_series_for_endgame_elo_combo` emits 6-tuple `(date, value, n, actual_elo, endgame_elo, non_endgame_elo)`; `_weekly_points_to_time_points_with_elo` consumes the extended shape and emits both PR fields on each `TimePoint` for `last_3mo` and `all_time` windows.
    - `app/services/insights_llm.py::_PROMPT_VERSION == "endgame_v34"`; the inline-comment changelog has a new leading v34 paragraph with the v33 paragraph + every prior entry preserved verbatim below it. A new `_render_non_endgame_elo_summary_block` (+ helper `_non_endgame_elo_per_bucket`) renders the third summary block; the subsection dispatcher calls it between the endgame_elo and endgame_elo_gap renderers.
    - `app/prompts/endgame_insights.md` updated: UI-label table gains `non_endgame_elo` row; `[series ...]` doc mentions the three numbers per row; subsection-level prose documents the THREE summary blocks; pairing rule carries the three sentence skeletons; glossary has rewritten `endgame_elo` entry + new sibling `non_endgame_elo` entry; "lifts up / holds back" headline metaphor preserved; no "Performance Rating" jargon outside the glossary section + a single brief parenthetical in the series doc.
    - `CHANGELOG.md` `[Unreleased]` `### Changed` prepended with the Phase 87.6 bullet.
    - `.planning/notes/endgame-elo-rebuild-on-score-gap.md` prepended with the Phase 87.6 supersession header.
    - All Task 1 RED tests GREEN; full pytest + ty + ruff suite clean.
    - Commit `feat(87.6-03): LLM payload + endgame_v34 prompt + non_endgame_elo glossary + CHANGELOG + supersession header` exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service -> LLM prompt | `_render_endgame_elo_summary_block` + new `_render_non_endgame_elo_summary_block` emit aggregate Elo values into the LLM prompt body. The values originate from `TimePoint` instances that the user's own game data drove; no external input crosses this boundary. |
| prompt cache key | `_PROMPT_VERSION` is part of the report cache key. Bumping to `endgame_v34` invalidates all v33-cached reports; this is intentional (the new sentence pattern requires fresh generation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.6-L1 | Tampering | `_render_non_endgame_elo_summary_block` | accept | Pure formatting helper consuming `TimePoint.non_endgame_elo` (int from Pydantic-validated schema). No user-controlled string reaches the renderer; the rendered block is bounded prose. |
| T-87.6-L2 | Information disclosure | `non_endgame_elo` in LLM prompt body | accept | Same per-user data scope as `endgame_elo` (Phase 87.5); the new field is derived from the same per-user game subset and does not widen access. The LLM call already operates on per-user data under the existing privacy contract. |
| T-87.6-L3 | Repudiation | `_PROMPT_VERSION` drift | mitigate | `test_prompt_version_is_endgame_v34` asserts the exact value; any future change is surfaced as a visible test failure. `test_prompt_changelog_preserves_v33_entry` asserts the historical entry stays present — the changelog cannot be silently trimmed. |
| T-87.6-L4 | Tampering | Prompt prose drift | mitigate | `test_prompt_prose_includes_non_endgame_elo_sentence_pattern` + `test_prompt_glossary_has_non_endgame_elo_entry` assert the canonical sentence skeleton and the glossary entry are present verbatim — future edits that accidentally remove either fail the test. |
| T-87.6-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced. Package legitimacy gate does not apply to this plan. |
</threat_model>

<verification>
After both tasks land:

1. `uv run ty check app/ tests/` — clean.
2. `uv run ruff check .` — clean.
3. `uv run pytest tests/ -x` — green; the 7+ new tests from Task 1 are GREEN; no regressions vs the Phase 87.5 baseline. The pytest suite count delta is roughly +3 (in `test_insights_service_series.py`) + +4 (in `test_insights_llm.py`) - 1 (existing `test_prompt_version_is_endgame_v33` renamed to v34, net same count).
4. Grep regression queries:
   - `grep -nE "_PROMPT_VERSION\s*=\s*["']endgame_v34["']" app/services/insights_llm.py` — exactly one match.
   - `grep -n "endgame_v33" app/services/insights_llm.py` — exactly one match (inside the preserved historical changelog comment).
   - `grep -n "endgame_v32" app/services/insights_llm.py` — at least one match (Phase 87.4 entry, preserved).
   - `grep -n "non_endgame_elo" app/services/insights_llm.py` — at least 2 matches (renderer name + helper name + dispatcher call).
   - `grep -n "non_endgame_elo" app/prompts/endgame_insights.md` — at least 4 matches (UI-label table row + series-doc + subsection prose + glossary entry).
   - `grep -n "Non-Endgame ELO" app/prompts/endgame_insights.md` — at least 5 matches (glossary header + UI-label table + 3 sentence skeletons).
   - `grep -n "Your Endgame ELO sits at X, vs Y" app/prompts/endgame_insights.md` — at least one match.
   - `grep -n "Phase 87.6" CHANGELOG.md` — at least one match (the new bullet).
   - `grep -n "Phase 87.6 supersedes" .planning/notes/endgame-elo-rebuild-on-score-gap.md` — exactly one match.
5. Sanity verification: `cat .planning/todos/pending/ | grep -i calibrate-endgame-elo-k` returns empty (no-op; file already in `done/`).

The Phase 87.6 milestone is complete after this plan ships. Plans 01 + 02 + 03 together deliver: backend PR helper + extended repo + extended schemas; frontend dual-line chart + signed band + shared gradient helper; LLM payload + endgame_v34 prompt + CHANGELOG + supersession marker.
</verification>

<success_criteria>
Maps to ROADMAP Phase 87.6 Success Criteria (the LLM half — SC#7, #8 fully; SC#9 LLM half):

- **SC#7**: LLM payload gains `endgame_elo` + `non_endgame_elo` + (existing) `actual_elo` per timeline point. `eg_score_gap` (the §3.1.6 score-gap series) stays as the primary signal — zone bands unchanged; the new PR-ELO fields are narrational additions. `_PROMPT_VERSION` bumps `endgame_v33` -> `endgame_v34` per CONTEXT D-05. The `endgame_elo_timeline` `SubsectionId` stays in `overall` (unchanged from Phase 87.5). The prose template gains the "Your Endgame ELO sits at X, vs Y in non-endgame games" sentence skeletons. Verified by Task 1's 4 prompt-content tests + Task 1's payload-shape tests + the `_render_non_endgame_elo_summary_block` integration.
- **SC#8**: `.planning/todos/pending/calibrate-endgame-elo-k.md` is absent (already moved to `done/` by Phase 87.5 closure; no-op for this plan, documented in SUMMARY). The Phase 87.5 in-code K-comment block at `endgame_service.py:1283-1304` is deleted by Plan 01; this plan deletes the design-note's forward-looking calibration reference by prepending a Phase 87.6 supersession header to `.planning/notes/endgame-elo-rebuild-on-score-gap.md`.
- **SC#9 (LLM half)**: Pytest covers (a) the LLM payload extension carrying both PR fields through to `TimePoint` (Task 1 `TestSeriesForEndgameEloComboPRFields`), (b) the `_PROMPT_VERSION` bump (`test_prompt_version_is_endgame_v34`), (c) the historical changelog preservation (`test_prompt_changelog_preserves_v33_entry`), (d) the new sentence pattern present in the prompt body (`test_prompt_prose_includes_non_endgame_elo_sentence_pattern`), (e) the new glossary entry present (`test_prompt_glossary_has_non_endgame_elo_entry`), (f) the THREE summary blocks emitted per combo (`test_endgame_elo_timeline_emits_three_summary_blocks`). The backend math tests live in Plan 01; the frontend chart + gradient tests live in Plan 02; this plan covers the LLM-stack slice.
</success_criteria>

<output>
After both tasks land, create `.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-03-SUMMARY.md` using the standard summary template. Include:
- The TimePoint schema extension (`endgame_elo` + `non_endgame_elo` as optional int fields).
- The threading changes through `_series_for_endgame_elo_combo` + `_weekly_points_to_time_points_with_elo` (6-tuple shape, weighted-mean aggregation for `all_time`).
- The `_PROMPT_VERSION` bump to `endgame_v34` and the new v34 paragraph text (note that every prior entry was preserved verbatim).
- The new renderer `_render_non_endgame_elo_summary_block` and its helper `_non_endgame_elo_per_bucket`.
- The prompt prose changes (UI-label table row, `[series ...]` doc rewrite, subsection prose rewrite from 2 to 3 blocks, glossary entry rewrite + new sibling, pairing rule with three sentence skeletons).
- The CHANGELOG bullet text.
- The supersession header prepended to `.planning/notes/endgame-elo-rebuild-on-score-gap.md`.
- Confirmation that `.planning/todos/pending/calibrate-endgame-elo-k.md` is absent (already in `done/` per Phase 87.5; no-op).
- Verification commands + grep regression results.
- Phase 87.6 is COMPLETE after this plan ships — note any UAT items the user should sanity-check (e.g. visual render of the 3-line chart on a real fixture user; LLM-generated narration for a user with significant Endgame ELO − Non-Endgame ELO spread, to confirm the new sentence pattern lands well).
</output>
