---
phase: 87.6-endgame-elo-via-performance-rating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/endgame_repository.py
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - tests/test_endgame_service.py
  - tests/services/test_endgame_elo_k_mapping.py
autonomous: true
requirements:
  - EGELO-PR-01  # PR formula `R_opp + 400·log10(s*/(1-s*))` with Laplace smoothing (SC#1)
  - EGELO-PR-02  # K constant + eyeball comment block deleted (SC#2)
  - EGELO-PR-03  # query_endgame_performance_rows extended with white_rating, black_rating (SC#3)
  - EGELO-PR-04  # ScoreGapTimelinePoint gains per-side opp_rating_avg; EndgameEloTimelinePoint gains non_endgame_elo (SC#4)
  - EGELO-PR-09a # Pytest coverage: PR helper + midpoint property + K-absence regression (SC#9 backend half)

must_haves:
  truths:
    - "`_performance_rating(score=0.5, n=100, opp_rating_avg=R)` returns int(round(R)) for any R in [800, 2800]"
    - "`_performance_rating(score=0.7, n=100, opp_rating_avg=1500)` and `_performance_rating(score=0.3, n=100, opp_rating_avg=1500)` are symmetric around 1500 within 1 ELO"
    - "`_performance_rating(score=1.0, n=100, opp_rating_avg=1500)` returns 1500 + 802 (±1) — the Laplace upper bound"
    - "`_performance_rating(score=0.0, n=100, opp_rating_avg=1500)` returns 1500 − 802 (±1) — the Laplace lower bound"
    - "On synthetic windowed series with |score_gap| ≤ 0.20 and equal endgame/non-endgame opp pools, `abs(midpoint(PR_E, PR_N) − actual_elo) <= 5`"
    - "Changing only endgame games in a synthetic window leaves `non_endgame_elo` unchanged (per-side independence)"
    - "EndgameEloTimelinePoint JSON carries `endgame_elo: int` AND `non_endgame_elo: int`"
    - "ScoreGapTimelinePoint JSON carries `endgame_opp_rating_avg: float` AND `non_endgame_opp_rating_avg: float`"
    - "Grep of `app/services/endgame_service.py` for `^K\\s*[:=]\\s*450` returns zero matches"
    - "Grep of `app/services/endgame_service.py` for `_endgame_elo_from_score_gap` returns zero matches"
    - "Grep of `app/services/endgame_service.py` for the eyeball-calibration percentile block (e.g. `\\bp05\\b.*-0\\.227`) returns zero matches"
    - "`uv run ty check app/ tests/` exits 0; `uv run ruff check .` exits 0; `uv run pytest tests/ -x` exits 0"
  artifacts:
    - path: "app/services/endgame_service.py"
      provides: "`_performance_rating` helper + rewritten `_compute_endgame_elo_weekly_series` consuming per-side opp_rating_avg + n_E/n_N from extended ScoreGapTimelinePoint; K + eyeball block + `_endgame_elo_from_score_gap` deleted"
      contains: "def _performance_rating\\("
    - path: "app/repositories/endgame_repository.py"
      provides: "query_endgame_performance_rows extended SELECT (white_rating, black_rating) on both endgame_stmt and non_endgame_stmt with NULL guards"
      contains: "Game\\.white_rating"
    - path: "app/schemas/endgames.py"
      provides: "ScoreGapTimelinePoint.endgame_opp_rating_avg + non_endgame_opp_rating_avg (float = 0.0); EndgameEloTimelinePoint.non_endgame_elo: int"
      contains: "endgame_opp_rating_avg"
    - path: "tests/test_endgame_service.py"
      provides: "TestPerformanceRating class + rewritten TestEndgameEloTimeline asserting PR math + per-week midpoint + K-absence regression"
      min_lines: 100
  key_links:
    - from: "app/services/endgame_service.py::_compute_endgame_elo_weekly_series"
      to: "_performance_rating"
      via: "per-week call inside the per-point loop, once for endgame_elo and once for non_endgame_elo"
      pattern: "_performance_rating\\("
    - from: "app/services/endgame_service.py::_compute_score_gap_timeline"
      to: "ScoreGapTimelinePoint(endgame_opp_rating_avg=..., non_endgame_opp_rating_avg=...)"
      via: "per-ISO-week emission inside the events loop"
      pattern: "endgame_opp_rating_avg="
    - from: "app/repositories/endgame_repository.py::query_endgame_performance_rows"
      to: "Game.white_rating / Game.black_rating in the SELECT"
      via: "added to the shared game_cols.select() block before WHERE filters"
      pattern: "Game\\.white_rating,\\s*Game\\.black_rating"
---

<objective>
Land the entire backend rewire for Phase 87.6 in a single atomic plan: delete K + the eyeball-calibration comment block + `_endgame_elo_from_score_gap`; introduce `_performance_rating` as the new pure helper; extend the SQL repo query with opponent ratings; extend `_compute_score_gap_timeline`'s trailing-window accumulator with per-side opponent-rating running means; extend the two schemas (`ScoreGapTimelinePoint` + `EndgameEloTimelinePoint`) with the new fields; rewire `_compute_endgame_elo_weekly_series` to compute `endgame_elo` AND `non_endgame_elo` as Laplace-smoothed FIDE Performance Ratings per side; back the whole stack with new pytest unit tests + a rewritten `TestEndgameEloTimeline` class + a K-absence regression test.

Purpose: Replace Phase 87.5's free-constant additive `endgame_elo = actual_elo + K · eg_score_gap` (K=450 eyeball-calibrated against §3.1.6 percentiles) with the FIDE Performance Rating formula applied independently to endgame and non-endgame subsets — yielding two PRs with NO free parameter (the `400` is theory-fixed by Elo's logistic-skill assumption). By the midpoint property (research §2: drift ≤ 2.3 ELO on |gap| ≤ 0.20, exact when score = 0.5), Actual ELO ends up bracketed by Endgame ELO and Non-Endgame ELO, preserving the "lifts up / holds back" semantics by construction without K.

Output: `_performance_rating` helper; extended `query_endgame_performance_rows`; extended `_compute_score_gap_timeline` (per-side opp-rating running sums); extended `ScoreGapTimelinePoint` (`endgame_opp_rating_avg`, `non_endgame_opp_rating_avg`); extended `EndgameEloTimelinePoint` (`non_endgame_elo`); rewritten `_compute_endgame_elo_weekly_series` consuming the new fields and computing both PRs per week; full pytest coverage (PR boundary cases + midpoint property + per-side independence + K-absence regression); ty + ruff + pytest green.

Mirrors Phase 87.5 Plan 01's atomic-edit pattern (`87.5-01-PLAN.md` Task 3): the schema rename, helper rewrite, producer extension, and test rewrite all land in one commit pass so the working tree never holds a half-renamed state that ty rejects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-CONTEXT.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-RESEARCH.md
@.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-PATTERNS.md
@.planning/notes/endgame-elo-pr-direct-rebuild.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-01-PLAN.md

<interfaces>
<!-- Key code surfaces this plan modifies. Extracted from RESEARCH.md + direct file reads so the executor does not need to re-explore the codebase. -->

From `app/services/endgame_service.py` (current Phase 87.5 post-merge state):
- Lines 1279-1281: `ENDGAME_ELO_TIMELINE_WINDOW = 100` — KEEP unchanged (window size; theory-fixed for PR's "trailing 100 games").
- Lines 1283-1304: K eyeball-calibration comment block referencing §3.1.6 percentiles — DELETE WHOLESALE. Replace with a short docstring on the new `_performance_rating` helper pointing at `.planning/notes/endgame-elo-pr-direct-rebuild.md`.
- Line 1305: `K: float = 450.0` — DELETE.
- Lines 1309-1311: `_ENDGAME_ELO_COMBO_ORDER` — KEEP unchanged.
- Lines 1314-1335: `_endgame_elo_from_score_gap(actual_elo, eg_score_gap, k) -> int` — DELETE. Replaced by new `_performance_rating(score, n, opp_rating_avg) -> int` at the same line position (per PATTERNS pattern 1 "keeps grep continuity").
- Lines 1338-1422: `_compute_endgame_elo_weekly_series(score_gap_timeline, actual_elo_dates, actual_elo_ratings, cutoff_str=None) -> list[EndgameEloTimelinePoint]` — REWRITE body. Signature stays identical except for the additional fields it now READS from each `ScoreGapTimelinePoint`. Logic changes: instead of `_endgame_elo_from_score_gap(actual_elo, eg_score_gap, K)`, compute two PRs per point:
  - `endgame_elo = _performance_rating(pt.endgame_score, pt.endgame_game_count, pt.endgame_opp_rating_avg)`
  - `non_endgame_elo = _performance_rating(pt.non_endgame_score, pt.non_endgame_game_count, pt.non_endgame_opp_rating_avg)`
  Emit both onto `EndgameEloTimelinePoint`. The `actual_elo_dates / actual_elo_ratings` asof-join stays exactly as is (Actual ELO line is unchanged).
- Lines 833-950: `_compute_score_gap_timeline(endgame_rows, non_endgame_rows, window, cutoff_str=None)` — EXTEND. Two new trailing-window accumulators (`endgame_opp_window: list[float]`, `non_endgame_opp_window: list[float]`) parallel to the existing score windows (lines 887-906). Per-side mean of the opp-rating window is written into `data_by_week[...]` next to `endgame_score` / `non_endgame_score` (lines 940-941). MIN_GAMES_FOR_TIMELINE drop filter at lines 944-950 stays unchanged — same gate covers the new fields.
- Lines 862-867: `_score_for(row)` helper — EXTEND or sibling helper. The 3-tuple `(played_at, side, score)` event payload at line 873 grows to a 4-tuple `(played_at, side, score, opp_rating: float)`. Derive opp_rating from the row: now that the repo SELECT returns 7 columns `(played_at, result, user_color, platform, time_control_bucket, white_rating, black_rating)`, the opponent rating is `row[5] if row[2] == 'black' else row[6]` (white_rating for a black-playing user, black_rating for white). Skip rows where the relevant rating is None (Pattern 3: NULL-rating rows can't contribute to PR).
- Lines 2380-2421: `get_endgame_elo_timeline` orchestrator per-combo loop — NO CHANGE NEEDED. It already calls `_compute_score_gap_timeline(endgame_by_combo[key], non_endgame_by_combo[key], ...)` per combo and feeds the result to `_compute_endgame_elo_weekly_series`. The per-combo partitioning is correct; the executor only needs to verify the comment at line 2383-2388 stays accurate after the extension (it mentions "indices 0/1/2"; the producer now also reads index 2 = user_color and indices 5/6 = white_rating/black_rating, so the comment needs a one-line update).

From `app/repositories/endgame_repository.py`:
- Lines 458-541: `query_endgame_performance_rows`. EXTEND the shared `game_cols = select(...)` block at lines 494-500 with two extra columns: `Game.white_rating, Game.black_rating`. Both endgame_stmt and non_endgame_stmt inherit them through `game_cols`; no separate edits to the two `where(...)` branches are needed for the SELECT extension. Add NULL guards `Game.white_rating.isnot(None), Game.black_rating.isnot(None)` to the shared `.where(...)` clause at lines 500-503 (matching the precedent in `query_endgame_elo_timeline_rows` lines 854-855). Update the return type annotation at line 468 to reflect the 7-tuple. Update the docstring "Returns: ... where each row is (played_at, result, user_color, platform, time_control_bucket)." at lines 480-486 — bump to a 7-tuple and note "Phase 87.6: white_rating + black_rating appended at indices 5-6 for the PR-ELO opponent-rating term; existing Time Pressure callers continue to ignore columns past index 4."

From `app/schemas/endgames.py`:
- Lines 308-350 `ScoreGapTimelinePoint`: ADD two fields after `non_endgame_score` (line 350):
  ```python
  # Phase 87.6: per-side trailing-window mean opponent rating. Drives the
  # Performance Rating math in _compute_endgame_elo_weekly_series. Default
  # 0.0 for back-compat with older fixtures that don't construct these —
  # a real timeline emission ALWAYS writes the rolling mean. Tests that
  # exercise the PR math must supply non-zero values.
  endgame_opp_rating_avg: float = 0.0
  non_endgame_opp_rating_avg: float = 0.0
  ```
  Defaults to `0.0` matches the existing back-compat pattern set by `per_week_endgame_games: int = 0` at line 345 (research §4d).
- Lines 538-577 `EndgameEloTimelinePoint`: ADD one field `non_endgame_elo: int` after `endgame_elo: int` (line 573). Update the class docstring (lines 539-570) to describe the PR math instead of the additive K formula. New docstring text for `endgame_elo` (block at lines 546-555):
  ```
  endgame_elo: FIDE Performance Rating computed on this combo's endgame games over
      the trailing 100-game window (Phase 87.6 D-01 — supersedes the Phase 87.5
      additive `actual_elo + K · eg_score_gap` mapping). Formula:
          endgame_elo = R_opp_avg_E + 400 · log10(s_E* / (1 − s_E*))
      with Laplace-smoothed s_E* = (n_E · score_E + 1) / (n_E + 2). The `400`
      is fixed by Elo's logistic-skill assumption — not a calibration knob.
      Bounds PR delta near ±802 ELO at n=100 (research §1b); no clamping.
      See `.planning/notes/endgame-elo-pr-direct-rebuild.md`.
  ```
  Add a parallel `non_endgame_elo` docstring block. NO default value on `non_endgame_elo` (it's emitted on every timeline point); int field type matches existing Phase 87.5 contract.

From `tests/test_endgame_service.py` (around line 3312-3496, the `TestEndgameEloTimeline` class):
- Imports near top of file: REMOVE `K` if imported; ADD `_performance_rating`.
- `TestEndgameEloTimeline` class docstring (line 3362): rewrite from "Each point's endgame_elo = round(actual_elo + K * eg_score_gap)" to "Each point's endgame_elo / non_endgame_elo is the per-side FIDE Performance Rating; midpoint(PR_E, PR_N) ≈ actual_elo within ±5 ELO on |gap| ≤ 0.20 (research §2a)."
- `_sg_pt` fixture builder (lines 3333-3353): add two kwargs `endgame_opp_rating_avg: float = 1500.0`, `non_endgame_opp_rating_avg: float = 1500.0` (research §5a recommendation — defensible defaults so the 11 existing call sites without K still produce sensible PR values).
- `test_single_point_at_zero_gap_preserves_actual_elo` (line 3368): rewrite assertion. Under PR math, with `actual_elo = 1500` and equal opp pools `R_opp = 1500`, at score = 0.5 PR equals R_opp = 1500 exactly, so the midpoint property gives `midpoint(PR_E, PR_N) = 1500 = actual_elo`. Assert that.
- `test_positive_gap_lifts_endgame_elo` (line 3379): rewrite. Construct `_sg_pt(endgame_score=0.6, non_endgame_score=0.5, endgame_opp_rating_avg=1500.0, non_endgame_opp_rating_avg=1500.0, endgame_game_count=100, non_endgame_game_count=100)`. Assert `endgame_elo > non_endgame_elo` (sign-only) and `endgame_elo == _performance_rating(0.6, 100, 1500.0)` (exact formula).
- `test_negative_gap_holds_back_endgame_elo` (line 3390): rewrite same shape, score=0.4 for endgame, score=0.5 for non-endgame.
- `test_multi_point_series_each_point_matches_formula` (line 3400): rewrite the assertion at line 3421 (`pt.endgame_elo - pt.actual_elo == round(K * sgp.score_difference)`) into a midpoint-property assertion: `abs((pt.endgame_elo + pt.non_endgame_elo) / 2 - pt.actual_elo) <= 5` for every point in the series. Constrain fixture inputs to |score gap| ≤ 0.20 with equal opp pools so the property holds tightly (research §2c).
- `test_cutoff_filters_pre_cutoff_points` (line 3423): NO CHANGE needed — doesn't reference K or PR math.
- `test_asof_uses_largest_date_le_point_date` (line 3438): line 3454 currently asserts `endgame_elo == 1520`. Rewrite under PR math: with `endgame_opp_rating_avg = 1500.0` and `endgame_score = 0.5`, PR equals 1500 exactly. Update the asserted value.
- `test_per_week_endgame_games_carries_through` (line 3456): NO CHANGE.
- `test_per_combo_isolation` (line 3477): lines 3493-3494 assert `round(K * 0.20)`. Rewrite to assert per-combo PR isolation: combo A and combo B produce independent `endgame_elo` / `non_endgame_elo` values that match each combo's own per-side score and opp pool.

From `tests/services/test_endgame_elo_k_mapping.py` (current Phase 87.5 test file):
- DELETE WHOLESALE via `git rm`. The file's whole reason to exist is the K calibration verification; with K removed the file's coverage is invalid. The new `TestPerformanceRating` class in `tests/test_endgame_service.py` (Task 1 below) replaces its coverage at the helper level; the rewritten `TestEndgameEloTimeline` covers the per-week integration.

From `tests/services/test_insights_llm.py:370`:
- One incidental K-string match in a prompt-version comment (research §5c). Update from `"endgame_elo_from_score_gap"` → `"_performance_rating"` to keep the prompt-version test green. This is a single string-comparison edit; no math.
</interfaces>

<chart_data>
<!-- Reference numbers from RESEARCH.md §1b / §2a for use in test assertions and docstring numeric examples. -->

PR bounds at n=100 (research §1b — verified to 2 decimals via Python):
- score = 0.0 → PR delta = −801.73 ELO
- score = 1.0 → PR delta = +801.73 ELO

Midpoint drift |midpoint(PR_E, PR_N) − actual_elo| in the realistic regime (research §2a):
- s_all = 0.50, |gap| = 0.20 → 0.00 ELO (exact at score = 0.5)
- s_all = 0.55, |gap| = 0.10 → 0.00 ELO
- s_all = 0.60, |gap| = 0.25 → 2.26 ELO
- Test tolerance: 5 ELO (comfortably above all observed drifts in the |gap| ≤ 0.20 / s_all ∈ [0.4, 0.6] regime).

Test fixtures MUST keep `s_all ∈ [0.4, 0.6]` and `|score gap| ≤ 0.20` to satisfy the 5-ELO tolerance. Beyond that the logit nonlinearity bites (research §2a: s_all = 0.947 produces a 71-ELO drift — outside the §3.1.6 score-gap data range; users never trigger this regime).
</chart_data>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests — PR helper unit tests + rewritten TestEndgameEloTimeline + K-absence regression</name>
  <files>
    tests/test_endgame_service.py,
    tests/services/test_endgame_elo_k_mapping.py,
    tests/services/test_insights_llm.py
  </files>
  <behavior>
    Land the new tests BEFORE the helper / schema / producer changes so we have a RED → GREEN cycle to drive the rest of the plan. The Phase 87.5 Plan 01 Task 1 pattern (Wave 0 RED tests + import-error assertion) is reused verbatim — except this time we extend an existing test file (`tests/test_endgame_service.py`) rather than create a new sibling, and we DELETE the Phase 87.5 K-mapping test file wholesale.

    **`tests/services/test_endgame_elo_k_mapping.py` — DELETE wholesale.** Use `git rm tests/services/test_endgame_elo_k_mapping.py`. The file imports `K` and `_endgame_elo_from_score_gap` and asserts ELO numbers at §3.1.6 percentiles; with K removed the whole file is obsolete (research §5a). Replaced by the new `TestPerformanceRating` class below.

    **`tests/test_endgame_service.py` — ADD `TestPerformanceRating` class** (sibling of `TestEndgameEloTimeline` at line 3356; modelled on the Phase 87.5 `TestEndgameEloFromScoreGap` shape that lived in the now-deleted k_mapping file):
    - `class TestPerformanceRating`:
      * `test_score_half_equals_opp_avg`: parametrized over `R_opp ∈ {800, 1200, 1500, 2000, 2800}`. At `score=0.5, n=100, opp_rating_avg=R`, assert `_performance_rating(0.5, 100, R) == int(round(R))`. (Math: at s = 0.5 the Laplace-smoothed `s* = (50 + 1) / (102) = 0.5`, log10(0.5/0.5) = 0, so PR = R exactly.)
      * `test_symmetric_scores_around_half`: at `score=0.7, n=100, opp_rating_avg=1500.0` vs `score=0.3, n=100, opp_rating_avg=1500.0`, assert (a) `pr_07 > 1500 + 100` (real positive magnitude — rules out the degenerate bug where both PRs collapse to R_opp), (b) `pr_03 < 1500 - 100` (real negative magnitude on the mirror), and (c) `abs((pr_07 - 1500) + (pr_03 - 1500)) <= 1` (mirror symmetry around R_opp). Strengthened per WARN-02 — additive symmetry alone would pass a buggy `_performance_rating` that always returned R_opp.
      * `test_laplace_upper_bound_at_score_one`: assert `abs(_performance_rating(1.0, 100, 1500.0) - (1500 + 802)) <= 1`. (Reference: research §1b — exact value +801.73.)
      * `test_laplace_lower_bound_at_score_zero`: assert `abs(_performance_rating(0.0, 100, 1500.0) - (1500 - 802)) <= 1`.
      * `test_rounds_to_int`: `isinstance(_performance_rating(0.55, 80, 1500.0), int)`.
      * `test_small_window_n_10`: `n=10, score=0.5, R=1500` ⇒ PR = 1500 (smoothing makes s* = 0.5 regardless of n at score = 0.5).
      * `test_small_window_n_10_extreme`: `n=10, score=1.0, R=1500` ⇒ PR delta is bounded (Laplace at n=10: s* = 11/12 = 0.9167, log10(0.9167 / 0.0833) = ~1.04, so PR ≈ 1500 + 416). Assert `1500 + 400 < pr < 1500 + 450`. This documents that small-n smoothing yields wider PR deltas than large-n (counterintuitive but correct: more pseudo-counts shift s* further toward 0.5 only relatively, not absolutely).
    - Imports near top of file: `from app.services.endgame_service import _performance_rating`. (Add this alongside the existing endgame_service imports.)

    **`tests/test_endgame_service.py` — REWRITE `TestEndgameEloTimeline`** (currently lines 3356-3496 per RESEARCH.md §5a):
    - Update class docstring at line 3362 from "Each point's endgame_elo = round(actual_elo + K * eg_score_gap)" to "Each point's endgame_elo and non_endgame_elo are per-side FIDE Performance Ratings (Phase 87.6); midpoint(PR_E, PR_N) ≈ actual_elo within ±5 ELO on |gap| ≤ 0.20 with equal opp pools (research §2)."
    - Update `_sg_pt` fixture builder at lines 3333-3353: add two kwargs at end, `endgame_opp_rating_avg: float = 1500.0` and `non_endgame_opp_rating_avg: float = 1500.0`. Update the inner `ScoreGapTimelinePoint(...)` constructor call to pass these through. (Backward compat: all 11 existing call sites omit the new kwargs, which fall back to 1500.0 — a defensible "user at pool level" default that keeps midpoint property tests passing.)
    - Rewrite `test_single_point_at_zero_gap_preserves_actual_elo` (line 3368): construct `_sg_pt(endgame_score=0.5, non_endgame_score=0.5, endgame_game_count=20, non_endgame_game_count=20)` with `actual_elo_dates=[d], actual_elo_ratings=[1500]`. Assert `pt.endgame_elo == 1500` AND `pt.non_endgame_elo == 1500` AND `pt.actual_elo == 1500`. (Mathematical guarantee: PR equals R_opp at score = 0.5; midpoint = R_opp = 1500.)
    - Rewrite `test_positive_gap_lifts_endgame_elo` (line 3379): construct `_sg_pt(endgame_score=0.6, non_endgame_score=0.5, endgame_game_count=100, non_endgame_game_count=100)`. Assert `pt.endgame_elo > pt.non_endgame_elo` AND `pt.endgame_elo == _performance_rating(0.6, 100, 1500.0)` AND `pt.non_endgame_elo == _performance_rating(0.5, 100, 1500.0) == 1500` AND `pt.non_endgame_elo == 1500`. Drop the magic `K * 0.10 = 45 ELO` assertion.
    - Rewrite `test_negative_gap_holds_back_endgame_elo` (line 3390): mirror with `endgame_score=0.4, non_endgame_score=0.5`. Assert `pt.endgame_elo < pt.non_endgame_elo` AND `pt.endgame_elo == _performance_rating(0.4, 100, 1500.0)`.
    - Rewrite `test_multi_point_series_each_point_matches_formula` (line 3400): construct a 5-point synthetic series with `endgame_score ∈ {0.45, 0.50, 0.55, 0.60, 0.55}`, `non_endgame_score ∈ {0.50, 0.50, 0.50, 0.50, 0.50}`, equal `actual_elo_dates / actual_elo_ratings = [1500]*5`, equal opp pools = 1500.0. Replace the line 3421 K-formula assertion with a midpoint property loop: for every emitted point, `abs((pt.endgame_elo + pt.non_endgame_elo) / 2 - pt.actual_elo) <= 5`. ALSO assert per-point exactness: `pt.endgame_elo == _performance_rating(score_E, 100, 1500.0)`.
    - Rewrite `test_asof_uses_largest_date_le_point_date` (line 3438): line 3454 currently asserts `endgame_elo == 1520`. New fixture: `_sg_pt(endgame_score=0.5, non_endgame_score=0.5, endgame_opp_rating_avg=1500.0, non_endgame_opp_rating_avg=1500.0)`. At score=0.5 PR equals R_opp = 1500. Assert `pt.endgame_elo == 1500`. The asof-join logic (largest date ≤ point date for actual_elo) is what this test verifies — the magic 1520 value was an additive `actual_elo + K * gap` artifact; the new assertion verifies asof-join still works under PR math (actual_elo correctly read from the asof arrays even though endgame_elo/non_endgame_elo no longer depend on it).
    - Rewrite `test_per_combo_isolation` (line 3477): lines 3493-3494 assert `round(K * 0.20)`. Replace with PR-based assertions — combo A with `endgame_score=0.6, non_endgame_score=0.5` and combo B with `endgame_score=0.4, non_endgame_score=0.5`, both at `opp_rating_avg=1500.0, n=100`. Assert: combo A's `endgame_elo == _performance_rating(0.6, 100, 1500.0)` (> 1500), combo B's `endgame_elo == _performance_rating(0.4, 100, 1500.0)` (< 1500). No cross-combo contamination — combo B's score doesn't appear in combo A's emitted points and vice versa.
    - ADD new test `test_per_side_independence` (new method in same class): synthesize two `_sg_pt` series that differ ONLY in endgame_score (non_endgame_score, non_endgame_game_count, non_endgame_opp_rating_avg held fixed at 0.5 / 100 / 1500.0). Assert `series_A[i].non_endgame_elo == series_B[i].non_endgame_elo` for every i. This locks the per-side computation invariant from CONTEXT §Tests.

    **`tests/test_endgame_service.py` — ADD `test_K_constant_deleted` regression test** (sibling to TestPerformanceRating; or in any existing module-level test class — placement is planner's choice but it MUST live in this file). Pure source-code grep:
    ```python
    def test_K_constant_deleted_from_endgame_service():
        """Regression: Phase 87.5 K=450 must be gone after Phase 87.6 ships."""
        from pathlib import Path
        src = Path("app/services/endgame_service.py").read_text()
        # Strip comments before searching so a historical mention in a comment
        # cannot mask a real definition.
        non_comment_lines = [ln for ln in src.splitlines() if not ln.lstrip().startswith("#")]
        non_comment = "\n".join(non_comment_lines)
        assert "K: float = 450" not in non_comment, "Phase 87.6: K must be deleted, not retuned"
        assert "K = 450" not in non_comment, "Phase 87.6: K must be deleted, not retuned"
        assert "_endgame_elo_from_score_gap" not in non_comment, "Phase 87.6: helper must be deleted"
    ```
    Comment-stripping uses `lstrip().startswith("#")` to avoid the "grep -c counts comments" pitfall flagged in the planner's grep-gate hygiene rule.

    **`tests/services/test_insights_llm.py:370`** — one-line edit: update the string match `"endgame_elo_from_score_gap"` to `"_performance_rating"` (research §5c) so the prompt-version test stays green when Plan 03 bumps the prompt comment. This is a safe pre-emptive edit — Plan 03 will land the matching prompt-comment update.

    RED state: all new `TestPerformanceRating` tests fail with `ImportError: cannot import name '_performance_rating'`. The rewritten `TestEndgameEloTimeline` tests fail because `non_endgame_elo` is not yet a field on `EndgameEloTimelinePoint`. The `test_K_constant_deleted_from_endgame_service` regression test fails because K is still present. Task 2 turns all of them GREEN.
  </behavior>
  <action>
    1. `git rm tests/services/test_endgame_elo_k_mapping.py` (research §5a — entire file deleted; coverage moves to new TestPerformanceRating class).
    2. Edit `tests/test_endgame_service.py`:
       a. Update imports near top of file: ADD `_performance_rating` to the `from app.services.endgame_service import ...` line. REMOVE `K` if currently imported (RESEARCH.md notes the import sites; grep `K\b` in the imports section to verify).
       b. Update `_sg_pt` fixture builder (lines 3333-3353): add `endgame_opp_rating_avg: float = 1500.0` and `non_endgame_opp_rating_avg: float = 1500.0` kwargs; pass through to the `ScoreGapTimelinePoint(...)` constructor.
       c. Update `TestEndgameEloTimeline` class docstring at line 3362 per the behavior spec.
       d. Rewrite the 5 K-dependent test methods listed in <behavior> per the spec (one-by-one, preserving non-K test methods unchanged: `test_cutoff_filters_pre_cutoff_points`, `test_per_week_endgame_games_carries_through`, etc.).
       e. ADD `TestPerformanceRating` class with 7 test methods per <behavior>.
       f. ADD `test_per_side_independence` method to `TestEndgameEloTimeline`.
       g. ADD module-level `test_K_constant_deleted_from_endgame_service` (or place inside an existing regression-test class — planner's choice; the function above is the canonical body).
    3. Edit `tests/services/test_insights_llm.py:370`: rename string-comparison `"endgame_elo_from_score_gap"` → `"_performance_rating"`.
    4. Run `uv run pytest tests/test_endgame_service.py::TestPerformanceRating tests/test_endgame_service.py::TestEndgameEloTimeline -x 2>&1 | tail -30` — confirm RED (ImportError on `_performance_rating` AND/OR ValidationError on `EndgameEloTimelinePoint(non_endgame_elo=...)`). This is the planned RED state.
    5. Run `uv run pytest tests/test_endgame_service.py::test_K_constant_deleted_from_endgame_service -x` — confirm RED (K still present in the source file).
    6. Commit: `test(87.6-01): RED tests for PR helper + rewritten TestEndgameEloTimeline + K-absence regression`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py::TestPerformanceRating tests/test_endgame_service.py::TestEndgameEloTimeline -x 2>&1 | grep -E "FAILED|ERROR|ImportError|ValidationError" | wc -l</automated>
  </verify>
  <done>
    - `tests/services/test_endgame_elo_k_mapping.py` removed (git rm).
    - `tests/test_endgame_service.py::TestPerformanceRating` class added with 7 test methods (currently failing on ImportError of `_performance_rating`).
    - `tests/test_endgame_service.py::TestEndgameEloTimeline` rewritten: 5 K-dependent methods rewritten under PR math, `_sg_pt` extended with new kwargs, `test_per_side_independence` added.
    - `tests/test_endgame_service.py::test_K_constant_deleted_from_endgame_service` regression test added (currently failing because K is present).
    - `tests/services/test_insights_llm.py:370` string updated.
    - Commit `test(87.6-01): RED tests for PR helper + rewritten TestEndgameEloTimeline + K-absence regression` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic backend rewire — extend repo query + producer accumulator + schemas + helper rewrite + weekly-series rewire</name>
  <files>
    app/repositories/endgame_repository.py,
    app/schemas/endgames.py,
    app/services/endgame_service.py
  </files>
  <behavior>
    Single atomic edit pass spanning repository + schema + service. The Phase 87.5 Plan 01 Task 3 lesson: partial edits leave `ty` broken or behavior silently drifting. Land the full backend rewire in one commit so the working tree never has a half-extended state. Order within the pass (bottom-up by dependency):

    **`app/schemas/endgames.py`** (do FIRST within the edit pass — Pydantic models are imported by the service layer):
    - `ScoreGapTimelinePoint` (lines 308-350): add two fields at the bottom of the class body after `non_endgame_score: float` (line 350):
      ```python
      # Phase 87.6: per-side trailing-window mean opponent rating. Drives the
      # Performance Rating math in _compute_endgame_elo_weekly_series. Default
      # 0.0 for back-compat with older fixtures and tests that don't construct
      # these — a real timeline emission ALWAYS writes the rolling mean.
      endgame_opp_rating_avg: float = 0.0
      non_endgame_opp_rating_avg: float = 0.0
      ```
      Defaults to `0.0` match the precedent of `per_week_endgame_games: int = 0` at line 345 (research §4d).
    - `EndgameEloTimelinePoint` (lines 538-577): rewrite the `endgame_elo` docstring block at lines 546-555 per the <interfaces> section's "endgame_elo: FIDE Performance Rating ..." text. ADD a parallel `non_endgame_elo: int` field below `endgame_elo: int` (line 573) with a sibling docstring block explaining the PR computed on non-endgame games. NO default value on `non_endgame_elo` — emitted on every timeline point. Update the class-level docstring at lines 538-545 from "Phase 87.5 D-06 rebuild on Endgame Score Gap" to "Phase 87.6 rebuild on FIDE Performance Rating per side" with a one-line pointer to `.planning/notes/endgame-elo-pr-direct-rebuild.md`.

    **`app/repositories/endgame_repository.py`** (do SECOND — service depends on the extended return shape):
    - `query_endgame_performance_rows` (lines 458-541):
      a. Extend the shared `game_cols = select(...)` block at lines 494-500: add `Game.white_rating` and `Game.black_rating` after `Game.time_control_bucket`. Both endgame_stmt and non_endgame_stmt inherit through the shared base — no separate edits needed.
      b. Add NULL guards `Game.white_rating.isnot(None), Game.black_rating.isnot(None)` to the shared `.where(...)` clause at lines 500-503. This matches the precedent in `query_endgame_elo_timeline_rows` lines 854-855 and follows research §3 / PATTERNS pattern 3: NULL-rating rows cannot contribute to PR — silently drop, same as the elo_timeline function does.
      c. Update return type annotation at line 468: the rows now carry 7 columns `(played_at, result, user_color, platform, time_control_bucket, white_rating, black_rating)`. The current annotation is `tuple[list[Row[Any]], list[Row[Any]]]` — that stays the same shape since `Row[Any]` doesn't encode the tuple width. No type-level breakage; the docstring carries the column contract.
      d. Update the docstring "Returns:" line (480-486): rewrite as "Returns: (endgame_rows, non_endgame_rows) where each row is (played_at, result, user_color, platform, time_control_bucket, white_rating, black_rating). Phase 87.6: white_rating + black_rating appended at indices 5-6 for the PR-ELO opponent-rating term. Existing callers (Time Pressure sections) ignore columns past index 4 today, so the new columns sit safely past them."

    **`app/services/endgame_service.py`** — main rewire (do THIRD — depends on the extended schema + repo shape):
    - DELETE the K eyeball-calibration comment block (lines 1283-1304) entirely.
    - DELETE `K: float = 450.0` (line 1305).
    - DELETE `_endgame_elo_from_score_gap` (lines 1314-1335).
    - INSERT new `_performance_rating` helper at the same line position (~line 1283 onward) the deleted helper occupied (per PATTERNS pattern 1: "keeps grep continuity"). Body:
      ```python
      # Phase 87.6 (D-01): FIDE Performance Rating with Laplace smoothing.
      # The `400` is fixed by Elo's logistic-skill assumption — not a knob.
      # See .planning/notes/endgame-elo-pr-direct-rebuild.md for derivation.
      _ELO_LOGISTIC_SCALE = 400.0


      def _performance_rating(score: float, n: int, opp_rating_avg: float) -> int:
          """FIDE Performance Rating with Laplace smoothing (Phase 87.6 D-01).

          PR = R_opp_avg + 400 · log10(s* / (1 - s*))
          s* = (n · score + 1) / (n + 2)   # Laplace-smoothed; bounds PR delta near ±802 ELO at n=100

          The Laplace prior of (1, 1) shifts the raw score toward 0.5 to handle
          all-win or all-loss streaks without infinite PR. At score = 0.5 the
          smoothed score is exactly 0.5 regardless of n, so PR equals R_opp_avg
          exactly — preserving the midpoint property that anchors the dual-line
          chart's center on Actual ELO.

          Pure function. No clamping — Laplace smoothing alone bounds PR delta
          at ±802 ELO at n=100, comfortably inside realistic territory.
          Phase 87.6 supersedes the Phase 87.5 additive `K · eg_score_gap`
          mapping wholesale; K is deleted, not retuned.
          See `.planning/notes/endgame-elo-pr-direct-rebuild.md`.
          """
          smoothed = (n * score + 1) / (n + 2)
          return int(round(opp_rating_avg + _ELO_LOGISTIC_SCALE * math.log10(smoothed / (1 - smoothed))))
      ```
      Explicit `-> int` return type (CLAUDE.md ty rule). Pure function, no I/O.
    - REWRITE `_compute_endgame_elo_weekly_series` body (lines 1338-1422). Signature unchanged. Per-point loop changes:
      ```python
      for pt in score_gap_timeline:
          # ... (existing cutoff_str + monday + asof-join logic unchanged)
          actual_elo_at_date = actual_elo_ratings[idx - 1]
          endgame_elo = _performance_rating(
              pt.endgame_score, pt.endgame_game_count, pt.endgame_opp_rating_avg
          )
          non_endgame_elo = _performance_rating(
              pt.non_endgame_score, pt.non_endgame_game_count, pt.non_endgame_opp_rating_avg
          )
          out.append(
              EndgameEloTimelinePoint(
                  date=monday.isoformat(),
                  endgame_elo=endgame_elo,
                  non_endgame_elo=non_endgame_elo,
                  actual_elo=int(actual_elo_at_date),
                  endgame_games_in_window=pt.endgame_game_count,
                  per_week_endgame_games=pt.per_week_endgame_games,
              )
          )
      ```
      Drop the comment block at lines 1403-1408 that explains the `score_difference` pre-rounding rationale (it referenced the K mapping's rounding boundary at half-integer K·gap; not relevant under PR math). Replace with a one-line comment: "Phase 87.6: per-side PR. The asof-join carries Actual ELO; PR_E and PR_N derive from per-side score + opp_rating_avg in the trailing window — no dependence on actual_elo."
      Update the function docstring (lines 1344-1375) to describe PR per side instead of additive K. Keep the description of `per_week_endgame_games` semantics intact.
    - EXTEND `_compute_score_gap_timeline` (lines 833-950):
      a. Extend `_score_for(row)` helper (lines 862-867) OR keep the helper score-only and derive opp_rating inline. Recommended: add a sibling helper `_opp_rating_for(row)` (or compute inline in the events loop). Logic: `opp_rating = row[5] if row[2] == "black" else row[6]` (white_rating for a black-playing user, black_rating for a white-playing user). Return `float(opp_rating)` since the schema field is `float`. Skip rows where the relevant rating is None (the repo query now filters those out via the new NULL guards, but defensive None-handling in the service layer is cheap and matches the Phase 87.5 pattern).
      b. Grow the event tuple at line 873: `events: list[tuple[Any, Literal["endgame", "non_endgame"], float, float]]` — adds `opp_rating: float` as the 4th slot. Lines 874-878 + 879-883 emit the extended tuples.
      c. Add two trailing-window accumulators next to the score windows at lines 887-888:
         ```python
         endgame_opp_window: list[float] = []
         non_endgame_opp_window: list[float] = []
         ```
      d. Inside the events loop at lines 900-906, append to the opp window in lockstep with the score window. Slice-truncate identically (PATTERNS pattern 2: "Trailing-window slice-truncation idiom"):
         ```python
         if side == "endgame":
             endgame_window.append(score)
             endgame_window = endgame_window[-window:]
             endgame_opp_window.append(opp_rating)
             endgame_opp_window = endgame_opp_window[-window:]
         else:
             non_endgame_window.append(score)
             non_endgame_window = non_endgame_window[-window:]
             non_endgame_opp_window.append(opp_rating)
             non_endgame_opp_window = non_endgame_opp_window[-window:]
         ```
      e. At the per-week emission (lines 933-942), compute per-side opp-rating means and write into `data_by_week[(iso_year, iso_week)]`:
         ```python
         "endgame_opp_rating_avg": round(statistics.mean(endgame_opp_window), 1),
         "non_endgame_opp_rating_avg": round(statistics.mean(non_endgame_opp_window), 1),
         ```
         `round(..., 1)` matches PATTERNS pattern 2 guidance (one decimal — the downstream PR helper only uses them as a float anchor).
      f. The `ScoreGapTimelinePoint(**data_by_week[key])` construction at line 945 is unchanged in shape — Pydantic v2 picks up the new fields by keyword.
      g. The MIN_GAMES_FOR_TIMELINE drop filter at lines 944-950 is unchanged — same gate covers the new fields.
    - Update the comment block at lines 2383-2388 in `get_endgame_elo_timeline`: currently says `_compute_score_gap_timeline ... only reads indices 0/1/2 (played_at, result, user_color)`. Update to: "Phase 87.6: _compute_score_gap_timeline now also reads indices 5/6 (white_rating, black_rating) to thread per-side opponent-rating running means through to the PR helper. The extended 7-tuple from query_endgame_performance_rows passes through unchanged."

    **Critical CLAUDE.md gates** (apply iteratively as the edit progresses):
    - `uv run ty check app/ tests/` exits 0 at the end of this task — no `# ty: ignore` workarounds. The new `non_endgame_elo` field on `EndgameEloTimelinePoint` is `int` (matches `endgame_elo`); the new `endgame_opp_rating_avg` / `non_endgame_opp_rating_avg` fields on `ScoreGapTimelinePoint` are `float = 0.0`. All field types narrow correctly.
    - `uv run ruff check .` exits 0.
    - `uv run pytest tests/ -x` exits 0 with no regressions vs the Phase 87.5 baseline.
    - The new `_performance_rating` carries `-> int` return type annotation.
    - No `asyncio.gather` on `AsyncSession` introduced.
    - No magic numbers: `_ELO_LOGISTIC_SCALE = 400.0` is the theory-fixed constant; the test percentile values (±802) live only in test assertions and the helper docstring.
    - Function-size: `_compute_endgame_elo_weekly_series` body ≤ 100 logic LOC after rewrite. `_compute_score_gap_timeline` body grows by ~10 LOC (still ≤ 120 logic LOC including docstring); nesting depth unchanged at ≤ 3.
    - `import math` at the top of `endgame_service.py` — verify it's already present (search for `^import math` or `from math import`). If absent, add it.
  </behavior>
  <action>
    1. Edit `app/schemas/endgames.py`:
       a. Add `endgame_opp_rating_avg: float = 0.0` and `non_endgame_opp_rating_avg: float = 0.0` to `ScoreGapTimelinePoint` after line 350 (after `non_endgame_score`).
       b. Add `non_endgame_elo: int` to `EndgameEloTimelinePoint` after the `endgame_elo: int` field (line 573).
       c. Rewrite the docstring blocks for `endgame_elo` and the new `non_endgame_elo` per the behavior spec (PR formula description, pointer to design note, Phase 87.6 supersession of Phase 87.5).
       d. Update class-level docstring at lines 538-545: change "Phase 87.5 D-06 rebuild on Endgame Score Gap" to "Phase 87.6 rebuild on FIDE Performance Rating per side".
    2. Edit `app/repositories/endgame_repository.py::query_endgame_performance_rows`:
       a. Add `Game.white_rating, Game.black_rating` to the `game_cols = select(...)` at lines 494-500 (after `Game.time_control_bucket`).
       b. Add NULL guards `Game.white_rating.isnot(None), Game.black_rating.isnot(None)` to the shared `.where(...)` at lines 500-503.
       c. Update docstring at lines 480-486 to document the new 7-tuple and the Phase 87.6 callers-ignore-past-index-4 contract.
    3. Edit `app/services/endgame_service.py`:
       a. DELETE lines 1283-1304 (K eyeball-calibration comment block) and line 1305 (`K: float = 450.0`).
       b. DELETE lines 1314-1335 (`_endgame_elo_from_score_gap` helper).
       c. INSERT new `_performance_rating` helper at the same line position (~1283 area) per the behavior spec. Include `_ELO_LOGISTIC_SCALE = 400.0` constant directly above the helper.
       d. REWRITE `_compute_endgame_elo_weekly_series` body per the spec: replace the single K-based call with two `_performance_rating` calls per point, emit both `endgame_elo` and `non_endgame_elo` on the constructed `EndgameEloTimelinePoint`. Update docstring.
       e. EXTEND `_compute_score_gap_timeline`: add `_opp_rating_for(row)` helper (or inline), grow event tuple to 4 slots, add two `_opp_window` accumulators, append in lockstep with slice-truncation, emit per-side means at the weekly write site.
       f. Update the comment block in `get_endgame_elo_timeline` at lines 2383-2388 per the behavior spec.
       g. Verify `import math` is present at the top of the file; add if missing.
       h. Run `grep -nE "(_endgame_elo_from_score_gap|^K\s*[:=]\s*450|^K: float = 450)" app/services/endgame_service.py | grep -v '^[[:space:]]*#'` — must return ZERO non-comment matches.
    4. Run quality gates iteratively as you go:
       - `uv run ty check app/ tests/` — must be clean before commit.
       - `uv run ruff check .` — must be clean.
       - `uv run ruff format app/services/endgame_service.py app/schemas/endgames.py app/repositories/endgame_repository.py` — apply if formatting drifts.
       - `uv run pytest tests/test_endgame_service.py::TestPerformanceRating tests/test_endgame_service.py::TestEndgameEloTimeline -x` — Task 1's RED tests must now pass GREEN.
       - `uv run pytest tests/test_endgame_service.py::test_K_constant_deleted_from_endgame_service -x` — GREEN.
       - `uv run pytest tests/ -x` — full suite, no regressions vs the Phase 87.5 baseline. Expect ~7 new tests passing in TestPerformanceRating + ~6 rewritten passing in TestEndgameEloTimeline + the K-absence regression.
    5. Verify the regression grep: `grep -nE "(PIVOT|ALPHA|CALIBRATION_VERSION|_endgame_elo_from_score_gap|^K: float = 450|^K = 450)" app/services/endgame_service.py | grep -v "^[[:space:]]*#"` — must return zero non-comment matches (the K=450 + helper are gone; PIVOT/ALPHA/CALIBRATION_VERSION were already gone post-Phase 87.5).
    6. Commit: `refactor(87.6-01): backend rewire — PR helper + per-side opp_rating accumulator + schema + K deletion`. Single commit per the atomic-pass rule.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run ruff check . && uv run pytest tests/ -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `app/schemas/endgames.py`: `ScoreGapTimelinePoint` gains `endgame_opp_rating_avg` and `non_endgame_opp_rating_avg` (float = 0.0); `EndgameEloTimelinePoint` gains `non_endgame_elo: int`; both docstrings updated to Phase 87.6 PR framing.
    - `app/repositories/endgame_repository.py`: `query_endgame_performance_rows` SELECT extended with `Game.white_rating, Game.black_rating`; NULL guards added; docstring updated to 7-tuple shape.
    - `app/services/endgame_service.py`: K constant + eyeball comment block + `_endgame_elo_from_score_gap` deleted; `_performance_rating` + `_ELO_LOGISTIC_SCALE` added at same line position; `_compute_score_gap_timeline` extended with per-side opp-rating windows; `_compute_endgame_elo_weekly_series` rewritten to emit both PRs per point; orchestrator comment updated.
    - Grep `^K\s*[:=]\s*450` in `app/services/endgame_service.py` (non-comment): 0 matches.
    - Grep `_endgame_elo_from_score_gap` in `app/services/endgame_service.py` (non-comment): 0 matches.
    - All Task 1 RED tests pass GREEN; full pytest suite green.
    - ty + ruff clean.
    - Commit `refactor(87.6-01): backend rewire — PR helper + per-side opp_rating accumulator + schema + K deletion` exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → schema | `_compute_endgame_elo_weekly_series` produces `EndgameEloTimelinePoint` instances (now with `non_endgame_elo`) consumed by the overview API response. Pydantic validates at the boundary. |
| service → DB (read) | `_compute_score_gap_timeline` consumes pre-fetched row sequences from `query_endgame_performance_rows`. The new `white_rating`/`black_rating` columns come from the existing `Game` table — no new SQL paths, no new user-controlled input. |
| DB row → service helper | The opp_rating derivation (`row[5] if row[2] == 'black' else row[6]`) trusts that `user_color` is one of `{"white", "black"}`. This invariant is enforced upstream by the import pipeline (constrained Literal in the schema); a defensive `else: continue` on unexpected values is cheap insurance but not strictly required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.6-01 | Tampering | `_performance_rating` | accept | Pure function on float/int inputs from internal services. No user-controlled value reaches `score`, `n`, or `opp_rating_avg`. Bounded outputs (Laplace ±802 ELO at n=100) prevent overflow of the downstream `int` field. |
| T-87.6-02 | Information disclosure | `EndgameOverviewResponse.endgame_elo_timeline` JSON | accept | Same per-user data scope as Phase 87.5; the additional `non_endgame_elo` field is derived from the same per-user game subset and does not widen access. The opp_rating_avg fields are aggregates (means over the 100-game window) — they reveal no individual opponent rating. |
| T-87.6-03 | Denial of service | extended `_compute_score_gap_timeline` accumulator | accept | O(N) per combo where N is row count per combo. The two new windows + per-week means add ~2× the constant-factor work of the existing scoring windows; well within the existing endpoint's perf envelope. No new query, no new round-trip. |
| T-87.6-04 | Repudiation | `_performance_rating` math drift | mitigate | `TestPerformanceRating` asserts exact values at boundary cases (s=0.5, s=1.0, s=0.0); `TestEndgameEloTimeline::test_per_side_independence` locks the per-side computation invariant; `test_K_constant_deleted_from_endgame_service` grep-asserts the deletion. Any future change is surfaced as a visible test failure. |
| T-87.6-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced; the existing `math` module from the Python stdlib provides `log10`. Package legitimacy gate does not apply to this plan. |
</threat_model>

<verification>
After both tasks land:

1. `uv run ty check app/ tests/` — clean.
2. `uv run ruff check .` — clean.
3. `uv run pytest tests/ -x` — green; test count ≥ Phase 87.5 baseline (rough delta: +7 TestPerformanceRating tests, +1 TestEndgameEloTimeline test (per_side_independence), +1 K-absence regression, −entire `test_endgame_elo_k_mapping.py` file (~10 tests), net ≈ +0 / minor delta).
4. Grep regression queries:
   - `grep -nE "(^K\s*[:=]\s*450|^K = 450)" app/services/endgame_service.py | grep -v '^[[:space:]]*#'` — zero non-comment matches.
   - `grep -nE "_endgame_elo_from_score_gap" app/services/endgame_service.py | grep -v '^[[:space:]]*#'` — zero non-comment matches.
   - `grep -n "_performance_rating" app/services/endgame_service.py` — at least one definition match.
   - `grep -n "endgame_opp_rating_avg" app/schemas/endgames.py` — at least one field match.
   - `grep -n "non_endgame_elo" app/schemas/endgames.py` — at least one field match.
   - `grep -n "Game.white_rating" app/repositories/endgame_repository.py` — at least one SELECT match.
5. Manual sanity: `uv run python -c "from app.services.endgame_service import _performance_rating; print(_performance_rating(0.5, 100, 1500.0), _performance_rating(1.0, 100, 1500.0), _performance_rating(0.0, 100, 1500.0))"` — outputs `1500 2302 698` (1500 ± 802; matches research §1b).

Plan 02 (frontend) and Plan 03 (LLM payload + prompt) can start once this plan's commits land on the working branch. Both Wave 2 plans depend on the extended `EndgameEloTimelinePoint.non_endgame_elo` field from this plan.
</verification>

<success_criteria>
Maps to ROADMAP Phase 87.6 Success Criteria (the backend half — SC#1, #2, #3, #4 fully; SC#9 partially):

- **SC#1**: `endgame_elo = R_opp_avg_E + 400·log10(s_E*/(1-s_E*))` with Laplace smoothing implemented in `_performance_rating` and called per-side per-week in `_compute_endgame_elo_weekly_series`. Asserted by `TestPerformanceRating::test_score_half_equals_opp_avg` + `test_laplace_upper_bound_at_score_one` + `test_laplace_lower_bound_at_score_zero` + the rewritten `TestEndgameEloTimeline` midpoint property assertions.
- **SC#2**: `K: float = 450.0` constant + eyeball-calibration comment block (lines 1283-1304) + `_endgame_elo_from_score_gap` helper all deleted from `app/services/endgame_service.py`. Asserted by `test_K_constant_deleted_from_endgame_service`. The repo-wide grep for `K · eg_score_gap` returns zero hits outside CHANGELOG (Plan 03's responsibility for CHANGELOG itself).
- **SC#3**: `query_endgame_performance_rows` SELECT extended with `Game.white_rating` and `Game.black_rating` on both endgame_stmt and non_endgame_stmt branches via the shared `game_cols` base. NULL guards added. Existing Time Pressure callers (per the function docstring) continue to ignore columns past index 4 — no breakage. Verified by the pytest run (Time Pressure tests still green).
- **SC#4**: `ScoreGapTimelinePoint` gains `endgame_opp_rating_avg: float = 0.0` and `non_endgame_opp_rating_avg: float = 0.0`. Tests for the producer's asof-join behaviour at week boundaries are covered by the rewritten `TestEndgameEloTimeline::test_asof_uses_largest_date_le_point_date` (which exercises the per-week emission against fixture inputs).
- **SC#9 (backend half)**: Pytest coverage for the PR helper (boundary cases s=0.5, s=1.0, s=0.0, symmetry, small-n) lives in `TestPerformanceRating`; midpoint property within ±5 ELO is asserted in `TestEndgameEloTimeline::test_multi_point_series_each_point_matches_formula`; per-side independence is asserted in `test_per_side_independence`; K-absence regression in `test_K_constant_deleted_from_endgame_service`. The Vitest gradient-stops tests + chart-render tests are Plan 02's responsibility; the LLM-payload tests are Plan 03's responsibility.
</success_criteria>

<output>
After both tasks land, create `.planning/milestones/v1.17-phases/87.6-endgame-elo-via-performance-rating/87.6-01-SUMMARY.md` using the standard summary template. Include:
- The list of deleted symbols (K constant + eyeball comment block + `_endgame_elo_from_score_gap`).
- The list of new symbols (`_performance_rating`, `_ELO_LOGISTIC_SCALE`, the two opp-rating fields on `ScoreGapTimelinePoint`, `non_endgame_elo` on `EndgameEloTimelinePoint`, the new white_rating/black_rating columns in the repo query).
- The single-atomic-pass rationale and any auto-fixes the executor made vs the plan (e.g., adjusting the line numbers if the post-87.5 file drifted from RESEARCH.md's stated lines).
- Verification commands and their pass counts.
- Note any Plan 02 / Plan 03 follow-ups required (frontend `EndgameEloTimelinePoint` TS mirror must add `non_endgame_elo`; LLM payload assembly must read `endgame_elo` AND `non_endgame_elo` from the new schema fields; `_PROMPT_VERSION` bump deferred to Plan 03).
</output>
