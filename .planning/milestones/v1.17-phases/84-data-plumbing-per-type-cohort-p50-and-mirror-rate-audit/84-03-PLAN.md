---
phase: 84
plan: 03
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md
autonomous: true
requirements: [DATA-01, DATA-02]
tags: [audit, documentation, mirror-bucket, summary, phase-84]

must_haves:
  truths:
    - "84-01-SUMMARY.md carries an inline audit section (per D-10) documenting Section 2 already-wired status with file:line citations"
    - "Audit section enumerates the 4 new ConversionRecoveryStats fields and the mirror identities derived in Plan 02"
    - "Audit section documents the threshold-constant duality (backend _MIN_OPPONENT_SAMPLE vs frontend MIN_OPPONENT_BASELINE_GAMES)"
    - "Audit section cross-references Phase 87 as the downstream consumer of both deliverables (per-class p50 + per-type opponent fields)"
  artifacts:
    - path: ".planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md"
      provides: "Inline audit section appended to Plan 01's SUMMARY (per D-10, no separate audit file)"
      contains: "## Audit"
  key_links:
    - from: ".planning/milestones/v1.17-phases/84-.../84-01-SUMMARY.md"
      to: "app/services/endgame_service.py Section 2 wiring (Phase 60)"
      via: "file:line citations in audit section"
      pattern: "endgame_service.py:"
    - from: ".planning/milestones/v1.17-phases/84-.../84-01-SUMMARY.md"
      to: ".planning/milestones/v1.17-phases/84-.../84-02-SUMMARY.md (Plan 02 deliverable)"
      via: "cross-reference in audit section to Section 3 schema extension"
      pattern: "84-02-SUMMARY"
---

<objective>
Append an inline audit section to `84-01-SUMMARY.md` (per D-10, no separate `.planning/notes/` file) that documents:
1. Section 2 mirror-bucket plumbing is already wired (Phase 60), with concrete file:line citations.
2. The 4 new `ConversionRecoveryStats` fields shipped in Plan 02, with mirror-identity formulas inline.
3. Threshold-constant reuse and the backend/frontend duality (`_MIN_OPPONENT_SAMPLE` vs `MIN_OPPONENT_BASELINE_GAMES`).
4. Cross-references: Phase 60 (precedent), Phase 87 (downstream consumer of both DATA-01 and DATA-02).

No code changes. Pure documentation, executed AFTER Plans 01 and 02 land so the audit cites the actually-committed state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md

@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-02-SUMMARY.md

@app/services/endgame_service.py
@app/schemas/endgames.py
@frontend/src/components/charts/EndgameScoreGapSection.tsx

<interfaces>
<!-- Key file:line references the audit must cite, validated against current repo state. -->

Phase 60 Section 2 wiring (already-shipped, audit confirms):
- `app/services/endgame_service.py:824-855` (nominal) — `_compute_score_gap_material` mirror-bucket block: `swap` dict, per-bucket loop, `opponent_score = 1.0 - bucket_score[swap_bucket]` when `swap_games >= _MIN_OPPONENT_SAMPLE`, else None.
- `app/services/endgame_service.py:233` (nominal) — `_MIN_OPPONENT_SAMPLE = 10` constant.
- `app/schemas/endgames.py:215-237` (nominal) — `MaterialRow` with `opponent_score: float | None` and `opponent_games: int`.
- `app/schemas/endgames.py:475-491` (nominal) — `EndgameOverviewResponse` composes `ScoreGapMaterialResponse.material_rows`.
- `frontend/src/components/charts/EndgameScoreGapSection.tsx:111-145` (nominal) — `MIRROR_BUCKET` map + `opponentRate()` helper (frontend consumer that double-uses both the wire `opponent_score` AND a client-side re-derivation from WDL).

Phase 84 Plan 02 new wiring (post Plan 02):
- `app/schemas/endgames.py::ConversionRecoveryStats` — extended with 4 new fields at end of model: `opponent_conversion_pct: float | None`, `opponent_conversion_games: int`, `opponent_recovery_pct: float | None`, `opponent_recovery_games: int`.
- `app/services/endgame_service.py::_aggregate_endgame_stats` — per-class mirror-identity computation block immediately before the `ConversionRecoveryStats(...)` construction (~line 357 nominal).

Mirror identities (D-07, scoped to one EndgameClass X):
- `opp_conv_pct[X] = (user_recovery_games − user_recovery_wins − user_recovery_draws) / user_recovery_games` (gated on `user_recovery_games >= _MIN_OPPONENT_SAMPLE`)
- `opp_recov_pct[X] = (user_conversion_losses + user_conversion_draws) / user_conversion_games` (gated on `user_conversion_games >= _MIN_OPPONENT_SAMPLE`)

Threshold duality (RESEARCH.md "Project Constraints" final bullet):
- Backend: `_MIN_OPPONENT_SAMPLE = 10` in `app/services/endgame_service.py` (governs `None`-vs-value gating on the wire).
- Frontend: `MIN_OPPONENT_BASELINE_GAMES` constant referenced in `EndgameScoreGapSection.tsx` (governs display gating). Conceptually the same threshold, lives in two places. Phase 84 doesn't change either; Phase 87 will apply the frontend constant when consuming the backend-supplied `opponent_*_games`.

Downstream consumer: Phase 87 Section 3 per-type cards consume `PER_CLASS_GAUGE_ZONES[<class>].p50` (DATA-01) and `EndgameCategoryStats.conversion.opponent_*` (DATA-02). Out of scope here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Plans 01 and 02 SUMMARYs exist and cited file:line refs are current</name>
  <files></files>
  <read_first>
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md (must exist, Plan 01 writes it)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-02-SUMMARY.md (must exist, Plan 02 writes it)
    - app/services/endgame_service.py (confirm `_MIN_OPPONENT_SAMPLE = 10` and the Section 2 mirror block still present)
    - app/schemas/endgames.py (confirm `MaterialRow.opponent_score` intact; confirm the 4 new Plan 02 fields on `ConversionRecoveryStats`)
    - frontend/src/components/charts/EndgameScoreGapSection.tsx (confirm `opponentRate()` / `MIRROR_BUCKET` consumer)
  </read_first>
  <action>
    1. Confirm both SUMMARY files exist. If either is missing, STOP. Plan 01 and Plan 02 must complete before this plan runs (wave 2 dependency).
    2. Grep for the cited anchors and record actual line numbers for use in Task 2:
       - `grep -n "_MIN_OPPONENT_SAMPLE = 10" app/services/endgame_service.py`
       - `grep -n "def _compute_score_gap_material\|swap_bucket\|opponent_score = 1.0" app/services/endgame_service.py`
       - `grep -n "class MaterialRow\|opponent_score:" app/schemas/endgames.py`
       - `grep -n "class ConversionRecoveryStats\|opponent_conversion_pct\|opponent_recovery_pct" app/schemas/endgames.py`
       - `grep -n "MIRROR_BUCKET\|opponentRate" frontend/src/components/charts/EndgameScoreGapSection.tsx`
    3. If any nominal range cited in the planning docs is materially off (>10 lines drift), use the corrected line numbers in Task 2. Do NOT modify any source files in this task.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md &amp;&amp; test -f .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-02-SUMMARY.md &amp;&amp; grep -q "_MIN_OPPONENT_SAMPLE = 10" app/services/endgame_service.py &amp;&amp; grep -q "opponent_conversion_pct" app/schemas/endgames.py &amp;&amp; grep -q "opponent_score: float \| None" app/schemas/endgames.py &amp;&amp; grep -qE "MIRROR_BUCKET|opponentRate" frontend/src/components/charts/EndgameScoreGapSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both `84-01-SUMMARY.md` and `84-02-SUMMARY.md` exist.
    - `_MIN_OPPONENT_SAMPLE = 10` present in `app/services/endgame_service.py`.
    - `opponent_conversion_pct` present in `app/schemas/endgames.py` (Plan 02 landed).
    - `MaterialRow.opponent_score: float | None` present (Phase 60 wiring intact).
    - `EndgameScoreGapSection.tsx` contains `MIRROR_BUCKET` or `opponentRate` (Section 2 consumer present).
    - Actual line numbers recorded for use in Task 2.
  </acceptance_criteria>
  <done>
    Plans 01 + 02 SUMMARYs verified to exist; all cited file:line refs verified against current repo state; corrected line numbers recorded for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append inline audit section to 84-01-SUMMARY.md per D-10</name>
  <files>.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md</files>
  <read_first>
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md (existing content from Plan 01; the audit is appended, not replacing)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md (D-06, D-07, D-08, D-09, D-10)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md (final section "84-01-SUMMARY.md (audit doc, D-10)" content pattern)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md ("Project Constraints" final bullet, threshold duality)
  </read_first>
  <behavior>
    Append a new top-level section `## Audit (DATA-02, per D-10)` to `84-01-SUMMARY.md`. The section has 4 subsections matching the `<objective>` items.

    Subsection content:

    1. `### Section 2 mirror-bucket plumbing — already wired (Phase 60)` — Confirms no backend work was needed for Section 2 in Phase 84. Cites verified file:line refs for:
       - `_MIN_OPPONENT_SAMPLE = 10` declaration in `app/services/endgame_service.py`
       - Section 2 mirror block (`swap` dict, `opponent_score = 1.0 - bucket_score[swap_bucket]`) in `_compute_score_gap_material`
       - `MaterialRow.opponent_score` + `opponent_games` field declarations in `app/schemas/endgames.py`
       - `EndgameScoreGapSection.tsx` `opponentRate()` / `MIRROR_BUCKET` consumer

    2. `### Section 3 schema extension — shipped in Plan 02` — Enumerates the 4 new `ConversionRecoveryStats` fields. Includes the two mirror identity formulas verbatim as fenced code blocks:
       ```
       opp_conv_pct[X]  = (user_recovery_games − user_recovery_wins − user_recovery_draws) / user_recovery_games
       opp_recov_pct[X] = (user_conversion_losses + user_conversion_draws) / user_conversion_games
       ```
       Notes the asymmetry (Pitfall 3 from RESEARCH.md): conversion is win rate, recovery is save rate, so the mirror formulas are not symmetric. Cites `84-02-SUMMARY.md` as the implementation deliverable.

    3. `### Threshold reuse and backend/frontend duality` — Documents:
       - Backend `_MIN_OPPONENT_SAMPLE = 10` (at the file:line verified in Task 1).
       - Frontend `MIN_OPPONENT_BASELINE_GAMES` constant in `EndgameScoreGapSection.tsx` (conceptually the same threshold, separate location).
       - Gate-on-MIRROR-sample semantics (Pitfall 4 from RESEARCH.md): `opponent_conversion_*` gates on `user_recovery_games`; `opponent_recovery_*` gates on `user_conversion_games`. NOT gated on the field's own bucket.
       - `_games` always `int` (mirror sample size, possibly 0). `_pct` is `float | None`.

    4. `### Cross-references — downstream consumers` — Lists:
       - Phase 60 (Section 2 mirror precedent, already shipped)
       - Phase 87 (Section 3 per-type peer-bullet consumer; reads both `PER_CLASS_GAUGE_ZONES[<class>].p50` from DATA-01 and `EndgameCategoryStats.conversion.opponent_*` from DATA-02)
       - Sig-test pattern (D-09): Wald-z vs 0 on `myRate − oppRate`, gated on `MIN_OPPONENT_BASELINE_GAMES` per type. Not implemented in Phase 84, only data shape is locked here.
       - D-08 note: the 4 new schema fields are derivable client-side from existing WDL fields. User locked the schema extension for consistency with the `MaterialRow` pattern, avoiding two drift-prone code paths.

    Style: terse, file:line refs where applicable, no editorialising. Avoid emojis. Use em-dashes sparingly (CLAUDE.md style guide).
  </behavior>
  <action>
    1. Open `84-01-SUMMARY.md` and read its current content.
    2. Append a `## Audit (DATA-02, per D-10)` heading at the bottom.
    3. Write the 4 subsections with content described in `<behavior>`, using the actual file:line refs verified in Task 1 (not the nominal 824-855 etc. from the planning docs if those have drifted).
    4. Use markdown bullet lists for file:line citations. Inline backticks for identifiers, file paths, and line numbers.
    5. Quote the two mirror identity formulas verbatim as fenced code blocks (not inline) so they render unambiguously.
    6. Keep the audit concise: target 50-120 lines of markdown for the appended section. Do NOT duplicate Plan 02's full SUMMARY content; link to `84-02-SUMMARY.md` instead.
    7. Save the file.
  </action>
  <verify>
    <automated>SUMMARY=.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md &amp;&amp; grep -q "^## Audit" "$SUMMARY" &amp;&amp; grep -q "Section 2 mirror-bucket plumbing" "$SUMMARY" &amp;&amp; grep -q "Section 3 schema extension" "$SUMMARY" &amp;&amp; grep -q "Threshold reuse" "$SUMMARY" &amp;&amp; grep -q "Cross-references" "$SUMMARY" &amp;&amp; grep -q "_MIN_OPPONENT_SAMPLE" "$SUMMARY" &amp;&amp; grep -q "MIN_OPPONENT_BASELINE_GAMES" "$SUMMARY" &amp;&amp; grep -q "opp_conv_pct" "$SUMMARY" &amp;&amp; grep -q "opp_recov_pct" "$SUMMARY" &amp;&amp; grep -q "Phase 87" "$SUMMARY" &amp;&amp; grep -q "Phase 60" "$SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - `## Audit` heading present in `84-01-SUMMARY.md`.
    - All 4 subsection titles present: "Section 2 mirror-bucket plumbing", "Section 3 schema extension", "Threshold reuse", "Cross-references".
    - Both threshold constants named: `_MIN_OPPONENT_SAMPLE` and `MIN_OPPONENT_BASELINE_GAMES`.
    - Both mirror-identity variable names present: `opp_conv_pct` and `opp_recov_pct`.
    - Cross-references to Phase 60 and Phase 87 present.
    - File:line refs use actual current line numbers (verified against repo state in Task 1).
    - Appended section is between 50 and 200 markdown lines (concise, but covers all 4 subsections).
  </acceptance_criteria>
  <done>
    Inline audit section appended to `84-01-SUMMARY.md`; cites Phase 60 Section 2 wiring with concrete file:line refs; documents the 4 Plan 02 schema fields with mirror identities; documents threshold duality; cross-references Phase 87 as downstream consumer.
  </done>
</task>

</tasks>

<verification>
- `84-01-SUMMARY.md` contains a `## Audit (DATA-02, per D-10)` section with 4 subsections.
- Audit cites `_MIN_OPPONENT_SAMPLE`, `MIN_OPPONENT_BASELINE_GAMES`, the two mirror-identity formulas, Phase 60, and Phase 87.
- No code changes; no source files modified.
- No separate audit file created (D-10 mandates inline placement).
</verification>

<success_criteria>
- D-10 satisfied: audit is inline in `84-01-SUMMARY.md`, not a separate `.planning/notes/` file.
- All four required topics covered (Section 2 already-wired status, Section 3 new fields + identities, threshold reuse + duality, Phase 87 cross-reference).
- File:line refs match the actual repo state at the time Plan 03 runs (not stale planning-doc nominal ranges).
- Plan 03 is documentation-only; no test or lint gates apply beyond markdown grep assertions.
</success_criteria>

<output>
After completion, append a brief note to `84-03-SUMMARY.md` (per the standard summary template) stating: "Audit deliverable landed inline in `84-01-SUMMARY.md` per D-10. No code changes. See `84-01-SUMMARY.md#audit-data-02-per-d-10` for the audit content." Phase 84 is then complete and ready for `/gsd-verify-work`.
</output>
