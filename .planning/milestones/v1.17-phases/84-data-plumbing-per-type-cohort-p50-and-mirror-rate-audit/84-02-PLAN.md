---
phase: 84
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/test_endgame_service.py
autonomous: true
requirements: [DATA-02]
tags: [endgame, schema, mirror-bucket, opponent-baseline, pydantic]

must_haves:
  truths:
    - "ConversionRecoveryStats exposes 4 new opponent fields: opponent_conversion_pct (float | None), opponent_conversion_games (int), opponent_recovery_pct (float | None), opponent_recovery_games (int)"
    - "Per-class opponent rates computed via same-game-symmetry mirror identity (scoped to one EndgameClass)"
    - "opponent_*_pct returns None when MIRROR sample (not own sample) < _MIN_OPPONENT_SAMPLE (10)"
    - "opponent_*_games is always int (never None), equals the mirror sample size"
    - "All four mirror tests pass: 60/40 symmetric identity, below-threshold (9), at-threshold boundary (10), zero-sample safety"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "ConversionRecoveryStats extended with 4 opponent fields at end of model (Pitfall 1 — no default-ordering issues)"
      contains: "opponent_conversion_pct"
    - path: "app/services/endgame_service.py"
      provides: "_aggregate_endgame_stats wires mirror-identity opponent rates into ConversionRecoveryStats(...) call at line ~357"
      contains: "opp_conv_pct"
    - path: "tests/test_endgame_service.py"
      provides: "≥4 new tests in TestAggregateEndgameStats covering mirror identity, threshold semantics, boundary, zero-sample"
      contains: "test_per_type_opponent_"
  key_links:
    - from: "app/services/endgame_service.py::_aggregate_endgame_stats"
      to: "app/schemas/endgames.py::ConversionRecoveryStats"
      via: "ConversionRecoveryStats(...) kwargs at line ~357 with 4 new opponent_* keyword args"
      pattern: "opponent_conversion_pct=opp_conv_pct"
    - from: "app/services/endgame_service.py (Section 2 mirror block, lines 824-855)"
      to: "app/services/endgame_service.py (Section 3 per-class mirror block, new)"
      via: "same _MIN_OPPONENT_SAMPLE threshold, same gate-on-mirror-sample semantics"
      pattern: "_MIN_OPPONENT_SAMPLE"
---

<objective>
Extend `ConversionRecoveryStats` in `app/schemas/endgames.py` with 4 new fields (`opponent_conversion_pct: float | None`, `opponent_conversion_games: int`, `opponent_recovery_pct: float | None`, `opponent_recovery_games: int`), wire them in `_aggregate_endgame_stats` (`app/services/endgame_service.py`) using the same-game-symmetry mirror identity scoped per EndgameClass (D-07), and add per-class opponent unit tests to `TestAggregateEndgameStats` in `tests/test_endgame_service.py`.

Purpose: DATA-02 (Section 3 extension) — surface the per-class opponent baseline on the wire so Phase 87 can render per-type peer bullets without re-deriving the math on the frontend. Mirrors the Section 2 `MaterialRow.opponent_score` / `opponent_games` pattern shipped in Phase 60.

Output: Extended Pydantic schema, service wiring, ≥4 new unit tests — all behind the same `_MIN_OPPONENT_SAMPLE = 10` threshold Section 2 uses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-VALIDATION.md

@app/schemas/endgames.py
@app/services/endgame_service.py
@tests/test_endgame_service.py

<interfaces>
<!-- Key types and contracts. Extracted from codebase per PATTERNS.md + RESEARCH.md. -->

From app/schemas/endgames.py:19-42 (current ConversionRecoveryStats):
```python
class ConversionRecoveryStats(BaseModel):
    conversion_pct: float          # 0-100 scale
    conversion_games: int
    conversion_wins: int
    conversion_draws: int
    conversion_losses: int

    recovery_pct: float            # 0-100 scale; save rate = (wins + draws) / games
    recovery_games: int
    recovery_saves: int
    recovery_wins: int
    recovery_draws: int
```

From app/schemas/endgames.py:215-237 (MaterialRow pattern to mirror — Phase 60):
```python
class MaterialRow(BaseModel):
    bucket: MaterialBucket
    ...
    # opponent_score: mirror-bucket score (1 - user_score); None when sample < _MIN_OPPONENT_SAMPLE.
    opponent_score: float | None
    # opponent_games: opponent's sample size (== swap-bucket game count).
    opponent_games: int
```

From app/services/endgame_service.py:
- Line 233: `_MIN_OPPONENT_SAMPLE = 10` (constant to reuse — no new constant)
- Lines 322-388: `_aggregate_endgame_stats` accumulator body
- Lines 345-355: existing per-class `conversion_pct` and `recovery_pct` computation (uses `round(x, 1)` — 1dp percent)
- Line 357: `conversion_stats = ConversionRecoveryStats(...)` — single construction site (Pitfall 5)
- Lines 824-855: Section 2 mirror-bucket template (`_compute_score_gap_material`'s mirror block — pattern to follow)

Mirror identities (D-07, scoped to one EndgameClass X):
- `opp_conv_pct[X] = (user_recovery_games − user_recovery_wins − user_recovery_draws) / user_recovery_games`
  (= user_recovery_losses / user_recovery_games; opp's "conversion" = pure win rate where opp was the side with advantage = where user was recovering)
- `opp_recov_pct[X] = (user_conversion_losses + user_conversion_draws) / user_conversion_games`
  (opp's "recovery" = save rate where opp was the side at disadvantage = where user was converting; matches user's recovery_pct definition as save rate)
- Gates (Pitfall 4):
  - `opponent_conversion_*` gates on `user_recovery_games >= _MIN_OPPONENT_SAMPLE`
  - `opponent_recovery_*`   gates on `user_conversion_games >= _MIN_OPPONENT_SAMPLE`
- `_pct` fields use `round(..., 1)` → 1dp percent (matches existing `conversion_pct` / `recovery_pct` style)
- `_games` fields always `int` (never None); equal mirror sample size (`user_recovery_games` for opp_conv_games, `user_conversion_games` for opp_recov_games); use `0` when bucket is empty.

From tests/test_endgame_service.py:
- Lines 181-188: `TestAggregateEndgameStats` row-tuple shape: `(game_id, endgame_class_int, result, user_color, eval_cp, eval_mate)` with `endgame_class_int` mapping 1=rook, 2=minor_piece, 3=pawn, 4=queen, 5=mixed, 6=pawnless (confirm exact mapping by reading the file).
- Lines 199-205, 216-219, 231-234: existing test patterns use bare tuples (NOT the `_FakeRow` helper from `TestScoreGapMaterialOpponentBaseline`).
- Lines 1381-1474: `TestScoreGapMaterialOpponentBaseline` — the Section 2 mirror-identity test pattern, including `test_opponent_baseline_symmetric_60_40` (line 1405) and `test_opponent_baseline_below_threshold_9_games` (line 1446).
- eval_cp = 150 → conversion bucket (user up ≥100cp); eval_cp = −150 → recovery bucket (user down ≤−100cp); confirm by reading existing test rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for per-type opponent fields (TDD red)</name>
  <files>tests/test_endgame_service.py</files>
  <read_first>
    - tests/test_endgame_service.py — full sections at lines 181-360 (`TestAggregateEndgameStats` row shape and existing test patterns) and lines 1381-1474 (`TestScoreGapMaterialOpponentBaseline` Section 2 template: `test_opponent_baseline_symmetric_60_40`, `test_opponent_baseline_below_threshold_9_games`, `test_opponent_baseline_at_threshold_10_games`)
    - app/services/endgame_service.py:181-280 — `_aggregate_endgame_stats` signature, row-tuple shape, endgame_class_int → EndgameClass mapping
    - app/services/endgame_service.py:824-855 — Section 2 mirror block (the formulas these tests verify per-class)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md — Example 5 (mirror identity unit tests), Pitfall 3 (mirror formula asymmetry), Pitfall 4 (threshold gate)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-VALIDATION.md — DATA-02 test rows
  </read_first>
  <behavior>
    Append 4 new test methods to `TestAggregateEndgameStats` in `tests/test_endgame_service.py` (the existing class around lines 181-360, not `TestScoreGapMaterialOpponentBaseline`). Tests use bare tuples (not `_FakeRow`) — match the existing `TestAggregateEndgameStats` style.

    - `test_per_type_opponent_conversion_pct_mirror_identity`: For class=rook (endgame_class_int=1), construct 60 conv-wins + 40 conv-losses (eval_cp=150) and 40 recov-wins + 60 recov-losses (eval_cp=−150), all white. Assert: `rook.conversion.opponent_conversion_pct == pytest.approx(60.0, abs=0.1)`, `opponent_conversion_games == 100`, `opponent_recovery_pct == pytest.approx(40.0, abs=0.1)`, `opponent_recovery_games == 100`.

    - `test_per_type_opponent_pct_none_below_threshold`: User has 100 conv games (60W/0D/40L) but only 9 recov games on rook. Assert: `rook.conversion.opponent_conversion_pct is None` (mirror sample 9 < 10), `opponent_conversion_games == 9`, `opponent_recovery_pct is not None` (mirror sample 100 ≥ 10), `opponent_recovery_games == 100`.

    - `test_per_type_opponent_pct_at_threshold_10`: User has 100 conv games and exactly 10 recov games. Assert: `rook.conversion.opponent_conversion_pct is not None` (boundary case; threshold is `>=`, not `>`).

    - `test_per_type_opponent_zero_sample`: A class with conversion_games == 0 AND recovery_games == 0 (e.g. construct only rook rows; queen class has 0 games). Assert: no DivByZero raised, `queen.conversion.opponent_conversion_pct is None`, `opponent_conversion_games == 0`, `opponent_recovery_pct is None`, `opponent_recovery_games == 0`.

    Use `pytest.approx(..., abs=0.1)` for 1dp percentage tolerance. Type-annotate methods with `-> None`.

    These tests MUST FAIL at this point (the schema and service wiring don't yet exist). That's the TDD red signal.
  </behavior>
  <action>
    1. Read existing `TestAggregateEndgameStats` to confirm the row-tuple shape and `endgame_class_int` mapping (lines 181-360).
    2. Append 4 new test methods to `TestAggregateEndgameStats`. Use exact identifiers from `<behavior>`. Construct row tuples directly — NO new helper, NO `_FakeRow` dataclass.
    3. Pick the per-class row via `next(c for c in result if c.endgame_class == "rook")` (or "queen" for the zero-sample test).
    4. Add comment block above the new tests citing the mirror identities from D-07 and the threshold gate from Pitfall 4. Reference `_MIN_OPPONENT_SAMPLE = 10` by name (not by literal).
    5. Run `uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -k per_type_opponent -x` and CONFIRM all 4 tests fail with `AttributeError` (no `opponent_conversion_pct`) or similar — this is the expected TDD red.
    6. Do NOT touch any other tests in the file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -k per_type_opponent --tb=no -q 2&gt;&amp;1 | grep -E "(failed|error)" &gt; /dev/null &amp;&amp; uv run ruff check tests/test_endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_per_type_opponent_" tests/test_endgame_service.py` returns 4 (exactly the four tests named above).
    - `uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -k per_type_opponent --tb=no -q` shows 4 FAILED tests (TDD red baseline — schema fields not yet added).
    - `uv run ruff check tests/test_endgame_service.py` clean.
    - No other tests modified: `git diff --stat tests/test_endgame_service.py` shows only insertions (no deletions in pre-existing test methods).
  </acceptance_criteria>
  <done>
    4 new failing tests appended to `TestAggregateEndgameStats`; ruff clean; TDD red state established.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ConversionRecoveryStats schema + wire mirror identity in _aggregate_endgame_stats (TDD green)</name>
  <files>app/schemas/endgames.py, app/services/endgame_service.py</files>
  <read_first>
    - app/schemas/endgames.py — full file; focus on lines 1-50 (imports + ConversionRecoveryStats at 19-42), 215-237 (MaterialRow pattern), 475-491 (EndgameOverviewResponse composition)
    - app/services/endgame_service.py — focus on lines 220-280 (constants + `_MIN_OPPONENT_SAMPLE` at 233), lines 322-388 (`_aggregate_endgame_stats` body, including conversion_pct/recovery_pct compute at 345-355 and `ConversionRecoveryStats(...)` construction at 357), lines 824-855 (Section 2 mirror block pattern)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md (D-06, D-07, D-08)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md (Example 3 schema; Example 4 wiring; Pitfalls 1, 3, 4, 5)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md (sections for app/schemas/endgames.py and app/services/endgame_service.py)
  </read_first>
  <behavior>
    Schema (`app/schemas/endgames.py::ConversionRecoveryStats`):
    - After edit, the model carries 14 fields total: existing 10 + 4 new (`opponent_conversion_pct: float | None`, `opponent_conversion_games: int`, `opponent_recovery_pct: float | None`, `opponent_recovery_games: int`). New fields appended at the END of the model body (Pitfall 1 — none of the existing fields have defaults, so any field ordering works, but append-at-end is safest and matches the MaterialRow pattern).
    - One-line inline comment above the new block: `# Phase 84 (DATA-02): per-type opponent baselines via same-game symmetry, mirroring MaterialRow.opponent_score / opponent_games (Phase 60).`
    - Per-field one-line comments explaining each field (matching `MaterialRow` style at lines 235-237).
    - Class docstring updated to mention Phase 84 and the same-game-symmetry derivation.

    Service (`app/services/endgame_service.py::_aggregate_endgame_stats`):
    - Inside the existing per-class loop (just before `conversion_stats = ConversionRecoveryStats(...)` at line ~357), compute:
      ```python
      opp_conv_losses_equiv = recovery_games - recovery_wins - recovery_draws
      if recovery_games >= _MIN_OPPONENT_SAMPLE:
          opp_conv_pct: float | None = round(opp_conv_losses_equiv / recovery_games * 100, 1)
      else:
          opp_conv_pct = None

      opp_recov_saves_equiv = conversion_losses + conversion_draws
      if conversion_games >= _MIN_OPPONENT_SAMPLE:
          opp_recov_pct: float | None = round(opp_recov_saves_equiv / conversion_games * 100, 1)
      else:
          opp_recov_pct = None
      ```
    - `ConversionRecoveryStats(...)` call extended with 4 new kwargs: `opponent_conversion_pct=opp_conv_pct`, `opponent_conversion_games=recovery_games` (mirror sample, always int — even when 0), `opponent_recovery_pct=opp_recov_pct`, `opponent_recovery_games=conversion_games`.
    - Comment block above the new computation cites D-07 mirror identity, Pitfall 3 (asymmetric conv-vs-recov definition), Pitfall 4 (threshold on MIRROR sample), and reuses `_MIN_OPPONENT_SAMPLE` (no new constant — magic-number rule).

    Result: All 4 tests from Task 1 PASS. ty zero errors. Function stays under 200 logic LOC.
  </behavior>
  <action>
    Edit `app/schemas/endgames.py`:
    1. Append 4 new fields at the end of `ConversionRecoveryStats` (after `recovery_draws`): `opponent_conversion_pct: float | None`, `opponent_conversion_games: int`, `opponent_recovery_pct: float | None`, `opponent_recovery_games: int`. Use exact identifiers — Task 1 tests reference these names.
    2. Add a leading inline comment per the `<behavior>` block citing Phase 84 (DATA-02) and the MaterialRow analog.
    3. Update the class docstring to mention the same-game-symmetry derivation and reference Phase 60.

    Edit `app/services/endgame_service.py`:
    4. Inside `_aggregate_endgame_stats`, locate the per-class loop body where `conversion_pct` and `recovery_pct` are computed (lines 345-355) and where `conversion_stats = ConversionRecoveryStats(...)` is constructed (line 357).
    5. Insert the mirror-identity computation block BEFORE the `ConversionRecoveryStats(...)` call. Use the exact formulas from `<behavior>` and Example 4 in 84-RESEARCH.md. Reuse the existing `_MIN_OPPONENT_SAMPLE` constant from line 233 — do NOT introduce a new constant.
    6. Pass the 4 new kwargs into the `ConversionRecoveryStats(...)` call: `opponent_conversion_pct=opp_conv_pct`, `opponent_conversion_games=recovery_games`, `opponent_recovery_pct=opp_recov_pct`, `opponent_recovery_games=conversion_games`.
    7. Add a comment block (4-6 lines) above the mirror block citing D-07 mirror identity, Pitfall 3 (conv = win rate, recov = save rate — asymmetric), Pitfall 4 (gate on MIRROR sample), and the Section 2 analog at lines 824-855.
    8. Keep `_pct` percentages on 0-100 scale via `round(..., 1)` matching the existing `conversion_pct` / `recovery_pct` style at lines 345-355. Do NOT switch to 0.0-1.0.
    9. `opponent_*_games` is always `int` (never None) and equals the mirror sample size — zero when the bucket is empty (handles the zero-sample test case naturally).

    Re-run the Task 1 tests; expect all 4 to pass (TDD green).

    Function size: `_aggregate_endgame_stats` should stay under 200 logic LOC after the addition (the mirror block is ~12 lines per CLAUDE.md "function size limits"). Do NOT extract a context dataclass or helper (CLAUDE.md "don't invent context dataclasses for fewer than 3 threaded fields").
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -k per_type_opponent -x &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run ruff check app/schemas/endgames.py app/services/endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opponent_conversion_pct: float | None" app/schemas/endgames.py` returns one match inside `ConversionRecoveryStats`.
    - `grep -c "opponent_" app/schemas/endgames.py` shows 4 new field declarations in ConversionRecoveryStats (in addition to any pre-existing matches elsewhere in the file like MaterialRow).
    - `grep -n "opp_conv_pct" app/services/endgame_service.py` returns at least 2 matches (computation + ConversionRecoveryStats kwarg).
    - `grep -n "opp_recov_pct" app/services/endgame_service.py` returns at least 2 matches.
    - `grep -n "_MIN_OPPONENT_SAMPLE" app/services/endgame_service.py` returns 3 matches: line 233 (declaration), Section 2 block ~838, and the new Section 3 block.
    - `grep -c "PER_CLASS_OPPONENT_SAMPLE_MIN\|MIN_OPPONENT_PER_CLASS" app/services/endgame_service.py` returns 0 (no new constant introduced — reuse only).
    - `uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -k per_type_opponent -x` exits 0 with 4 tests passing (TDD green).
    - `uv run ty check app/ tests/` exits 0 (zero errors required per CLAUDE.md).
    - `uv run ruff check app/schemas/endgames.py app/services/endgame_service.py` clean.
    - `grep -rn "ConversionRecoveryStats(" app/ | grep -v "schemas/endgames.py"` returns exactly one match (at `endgame_service.py:357`) — confirms Pitfall 5 assumption (only one construction site).
  </acceptance_criteria>
  <done>
    Schema extended with 4 new fields at end of `ConversionRecoveryStats`; service wires mirror identity via existing `_MIN_OPPONENT_SAMPLE`; all 4 Task 1 tests pass; ty + ruff clean; no new constants; function still under size limits.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full backend regression + verify wire schema surface</name>
  <files></files>
  <read_first>
    - app/schemas/endgames.py (post-edit ConversionRecoveryStats)
    - app/services/endgame_service.py (post-edit _aggregate_endgame_stats)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-VALIDATION.md (Phase gate row)
  </read_first>
  <action>
    1. Run full backend pytest suite: `uv run pytest -x`. Expect all tests green (no regression to existing tests, plus 4 new tests passing).
    2. Run type checker: `uv run ty check app/ tests/`. Expect zero errors.
    3. Run lint: `uv run ruff check .`. Expect clean.
    4. Run format check: `uv run ruff format --check app/schemas/endgames.py app/services/endgame_service.py tests/test_endgame_service.py`. If drift, run `uv run ruff format` on the same files and re-commit.
    5. Verify the new fields surface on the response shape (no DB needed): start a Python REPL and import the model, instantiate with explicit kwargs, dump to dict, confirm all 4 keys present:
       ```
       uv run python -c "from app.schemas.endgames import ConversionRecoveryStats; m = ConversionRecoveryStats(conversion_pct=70.0, conversion_games=100, conversion_wins=70, conversion_draws=0, conversion_losses=30, recovery_pct=30.0, recovery_games=100, recovery_saves=30, recovery_wins=30, recovery_draws=0, opponent_conversion_pct=30.0, opponent_conversion_games=100, opponent_recovery_pct=30.0, opponent_recovery_games=100); print(sorted(m.model_dump().keys()))"
       ```
       Expect output containing all 4 `opponent_*` keys.
    6. If any gate fails, STOP and fix before declaring done.
  </action>
  <verify>
    <automated>uv run pytest -x &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run ruff check . &amp;&amp; uv run python -c "from app.schemas.endgames import ConversionRecoveryStats; m = ConversionRecoveryStats(conversion_pct=70.0, conversion_games=100, conversion_wins=70, conversion_draws=0, conversion_losses=30, recovery_pct=30.0, recovery_games=100, recovery_saves=30, recovery_wins=30, recovery_draws=0, opponent_conversion_pct=30.0, opponent_conversion_games=100, opponent_recovery_pct=30.0, opponent_recovery_games=100); d = m.model_dump(); assert 'opponent_conversion_pct' in d and 'opponent_conversion_games' in d and 'opponent_recovery_pct' in d and 'opponent_recovery_games' in d; print('schema-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -x` exits 0 (full backend suite green; no regression).
    - `uv run ty check app/ tests/` exits 0 (zero errors).
    - `uv run ruff check .` exits 0.
    - The Python -c verification command prints `schema-ok` and exits 0.
    - `uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -k per_type_opponent -x` shows 4 passed.
  </acceptance_criteria>
  <done>
    Full backend regression green, ty zero errors, ruff clean, new schema fields verified on response model surface, all 4 new tests passing.
  </done>
</task>

</tasks>

<verification>
- All 4 new tests in `TestAggregateEndgameStats` (per-type opponent: mirror identity, below-threshold, at-threshold-10, zero-sample) pass.
- Full backend suite green: `uv run pytest -x`.
- ty zero errors: `uv run ty check app/ tests/`.
- ruff clean: `uv run ruff check .`.
- `ConversionRecoveryStats.model_dump()` includes all 4 new opponent_* keys.
- Only one `ConversionRecoveryStats(...)` construction site exists in `app/` outside the schema (at `endgame_service.py:357`); Pitfall 5 assumption holds.
</verification>

<success_criteria>
- DATA-02 success criteria: per-class opponent rates exposed under `/api/endgames/overview` → `stats.categories[*].conversion.opponent_*`.
- Mirror identities verified at the symmetric 60/40 case and the below-threshold edge case.
- `_MIN_OPPONENT_SAMPLE` reused (no new constant); function size within CLAUDE.md limits; no context dataclass invented.
- Schema field nullability matches Phase 60 `MaterialRow` convention: `_pct` is `float | None`, `_games` is always `int`.
- No DB / Alembic / migration changes (this is a pure additive schema + service refactor).
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-02-SUMMARY.md` per the standard summary template. Include sections:
- What changed (schema + service + tests; file:line refs)
- D-06..D-09 references where each decision landed
- Mirror-identity formulas (final form, with the Pitfall 3 conv-vs-recov asymmetry noted)
- Threshold reuse confirmation (`_MIN_OPPONENT_SAMPLE = 10`, gated on mirror sample)
- Test summary (4 new tests, what each verifies)
- Cross-reference to Plan 01 (codegen) and Plan 03 (audit doc)
</output>
