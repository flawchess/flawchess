---
phase: 84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_zones.py
  - scripts/gen_endgame_zones_ts.py
  - frontend/src/generated/endgameZones.ts
  - tests/services/test_endgame_zones.py
autonomous: true
requirements: [DATA-01]
tags: [endgame, codegen, zones, p50, benchmarks]

must_haves:
  truths:
    - "Each EndgameClass in PER_CLASS_GAUGE_ZONES exposes an explicit p50 centre tick alongside the existing IQR (conversion, recovery)"
    - "Regenerated frontend/src/generated/endgameZones.ts contains a `p50: { conversion: <num>, recovery: <num> }` key on each of the 6 class entries"
    - "CI drift guard (gen_endgame_zones_ts.py + git diff --exit-code) passes after the change"
    - "p50 values for all 6 classes match D-03 benchmark values (within 2dp tolerance)"
  artifacts:
    - path: "app/services/endgame_zones.py"
      provides: "PerClassBands dataclass with p50 field + PER_CLASS_GAUGE_ZONES table populated for all 6 classes"
      contains: "p50: tuple[float, float]"
    - path: "scripts/gen_endgame_zones_ts.py"
      provides: "_format_per_class_gauge_zones() emits p50 nested object per class"
      contains: "p50: {{ conversion:"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "Regenerated TS mirror including p50 keys"
      contains: "p50: { conversion:"
    - path: "tests/services/test_endgame_zones.py"
      provides: "TestPerClassP50 unit tests asserting field presence, type, per-class value match, and IQR sanity"
      contains: "class TestPerClassP50"
  key_links:
    - from: "app/services/endgame_zones.py::PerClassBands"
      to: "scripts/gen_endgame_zones_ts.py::_format_per_class_gauge_zones"
      via: "module import + iteration over PER_CLASS_GAUGE_ZONES.items()"
      pattern: "from app.services.endgame_zones import .*PER_CLASS_GAUGE_ZONES"
    - from: "scripts/gen_endgame_zones_ts.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "codegen write; CI git diff --exit-code blocks drift"
      pattern: "p50: \\{ conversion:"
---

<objective>
Extend `PerClassBands` in `app/services/endgame_zones.py` with an explicit `p50: tuple[float, float]` centre tick (ordered `(conversion_p50, recovery_p50)`), populate the 6 entries in `PER_CLASS_GAUGE_ZONES` with the locked D-03 benchmark values, extend `_format_per_class_gauge_zones()` in `scripts/gen_endgame_zones_ts.py` to emit a `p50: { conversion: <num>, recovery: <num> }` nested object on each class line, regenerate `frontend/src/generated/endgameZones.ts`, and append a `TestPerClassP50` class to `tests/services/test_endgame_zones.py` covering presence, type, per-class value match, and IQR sanity.

Purpose: DATA-01 — provides Section 3 (Phase 87) the explicit per-class cohort p50 centre tick for the cohort bullet, replacing the inaccurate IQR-midpoint inference. Python authoritative + TS codegen pattern (Phase 63 D-01) preserved.

Output: Extended dataclass, updated emitter, regenerated TS mirror, new unit-test class — all committed together so CI drift guard passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-VALIDATION.md

@app/services/endgame_zones.py
@scripts/gen_endgame_zones_ts.py
@frontend/src/generated/endgameZones.ts
@tests/services/test_endgame_zones.py

<interfaces>
<!-- Key types and contracts. Extracted from codebase per PATTERNS.md. -->

From app/services/endgame_zones.py:320-339 (current shape):
- `EndgameClass = Literal["rook", "minor_piece", "pawn", "queen", "mixed", "pawnless"]` (imported from app.schemas.endgames)
- `@dataclass(frozen=True) class PerClassBands` with fields `conversion: tuple[float, float]`, `recovery: tuple[float, float]`. Extend with `p50: tuple[float, float]` ordered `(conversion_p50, recovery_p50)`.
- `PER_CLASS_GAUGE_ZONES: Mapping[EndgameClass, PerClassBands]` — 6 entries (rook, minor_piece, pawn, queen, mixed, pawnless).

From scripts/gen_endgame_zones_ts.py:67-78 (current shape):
- `_format_per_class_gauge_zones() -> str` — iterates `PER_CLASS_GAUGE_ZONES.items()`, emits one line per class.
- Current emission per line: `f"  {cls}: {{ conversion: [{c_lo}, {c_hi}], recovery: [{r_lo}, {r_hi}] }},"` — single line, raw float repr (trailing-zero stripping is the existing convention).

D-03 LOCKED p50 values (from reports/benchmarks-2026-05-12.md §6, rounded to 2dp):

| class       | p50.conversion | p50.recovery |
|-------------|---------------:|-------------:|
| rook        | 0.71           | 0.30         |
| minor_piece | 0.69           | 0.33         |
| pawn        | 0.74           | 0.28         |
| queen       | 0.77           | 0.23         |
| mixed       | 0.69           | 0.31         |
| pawnless    | 0.79           | 0.20         |

Comment text (D-04, verbatim): "pooled per-class typical centre, source: §6 mean (≈ per-user p50)"

CI drift guard (.github/workflows/ci.yml:47-50):
```
- name: Zone drift check
  run: |
    uv run python scripts/gen_endgame_zones_ts.py
    git diff --exit-code frontend/src/generated/endgameZones.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PerClassBands + populate p50 table, then extend codegen emitter</name>
  <files>app/services/endgame_zones.py, scripts/gen_endgame_zones_ts.py</files>
  <read_first>
    - app/services/endgame_zones.py (full file; focus on lines 310-339 for PerClassBands + PER_CLASS_GAUGE_ZONES, and lines 390-420 for downstream consumers per_class_zone_spec / assign_per_class_zone — confirm they only read .conversion / .recovery and are safe)
    - scripts/gen_endgame_zones_ts.py (full file; focus on lines 67-78 _format_per_class_gauge_zones and lines 136-140 _render docstring referencing the emitted shape)
    - frontend/src/generated/endgameZones.ts (full file, to understand current emission style — trailing-zero stripping is expected; do NOT change IQR formatting)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md (D-01..D-05)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md (Pitfall 2 line-wrapping; Pitfall 6 float formatting; Example 1 / Example 2)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md (Pattern A; sections for endgame_zones.py and gen_endgame_zones_ts.py)
  </read_first>
  <behavior>
    - After edit, `PerClassBands` is a frozen dataclass with 3 fields in order: `conversion: tuple[float, float]`, `recovery: tuple[float, float]`, `p50: tuple[float, float]`.
    - All 6 entries of `PER_CLASS_GAUGE_ZONES` carry the locked p50 kwargs from D-03 (rook=(0.71, 0.30), minor_piece=(0.69, 0.33), pawn=(0.74, 0.28), queen=(0.77, 0.23), mixed=(0.69, 0.31), pawnless=(0.79, 0.20)).
    - A comment near the table reads exactly "pooled per-class typical centre, source: §6 mean (≈ per-user p50)" (D-04).
    - `_format_per_class_gauge_zones()` emits each class on ONE line (Pitfall 2) with the new key appended: e.g. `  rook: { conversion: [0.65, 0.75], recovery: [0.26, 0.36], p50: { conversion: 0.71, recovery: 0.3 } },`
    - IQR formatting unchanged (raw f-string repr, trailing-zero stripping retained). p50 values emitted via raw f-string (same convention) — `0.30` will render as `0.3`, that is acceptable per Pitfall 6.
    - Docstring on `_format_per_class_gauge_zones` updated to mention the new `p50` key.
    - Module-level comment above `export const PER_CLASS_GAUGE_ZONES` in `_render()` (gen_endgame_zones_ts.py lines 136-140) updated to reference the new `p50` key alongside conversion/recovery.
    - `Mapping[EndgameClass, PerClassBands]` annotation on `PER_CLASS_GAUGE_ZONES` is unchanged (only the value type expanded).
    - No `_MIN_OPPONENT_SAMPLE`, no service-layer changes, no schema changes (those are Plan 02).
    - Downstream consumers `per_class_zone_spec` / `assign_per_class_zone` continue to compile (they only read `.conversion` / `.recovery`).
  </behavior>
  <action>
    Edit `app/services/endgame_zones.py`:
    1. In `PerClassBands` (around lines 320-326), append `p50: tuple[float, float]` after `recovery`. Update the class docstring to mention "centre p50 tick" and the rationale (per D-04 comment text).
    2. In `PER_CLASS_GAUGE_ZONES` (lines 328-339), add a `p50=(c, r)` kwarg to every PerClassBands(...) call using the D-03 values verbatim from the table in `<interfaces>`. Keep call-args on one line per class entry (matches existing style).
    3. Above the table, ensure the comment block includes the D-04 line: `# p50 values: pooled per-class typical centre, source: §6 mean (≈ per-user p50)` referencing `reports/benchmarks-2026-05-12.md`.

    Edit `scripts/gen_endgame_zones_ts.py`:
    4. In `_format_per_class_gauge_zones()` (lines 67-78), unpack `p50_c, p50_r = bands.p50` after the existing `r_lo, r_hi = bands.recovery`. Extend the appended line to add `, p50: {{ conversion: {p50_c}, recovery: {p50_r} }}` BEFORE the closing `}},` so each class stays on one line. Concretely: `lines.append(f"  {cls}: {{ conversion: [{c_lo}, {c_hi}], recovery: [{r_lo}, {r_hi}], p50: {{ conversion: {p50_c}, recovery: {p50_r} }} }},")`.
    5. Update the function docstring to read: "Each class entry has { conversion: [lower, upper], recovery: [lower, upper], p50: { conversion: <num>, recovery: <num> } }." (replace existing two-key description).
    6. In `_render()` (lines 136-140), update the `// AUTO-GENERATED` comment/heading text above `export const PER_CLASS_GAUGE_ZONES` to also reference `p50` if it currently lists the keys.

    Do NOT modify `frontend/src/generated/endgameZones.ts` directly (it is regenerated in Task 2). Do NOT change `_MIN_OPPONENT_SAMPLE`, `ConversionRecoveryStats`, or any service-layer file (those are Plan 02 scope per D-11).

    Type-safety: every change must pass `uv run ty check app/ tests/` with zero errors. Comment any non-obvious lines per CLAUDE.md ("comment bug fixes / non-obvious code").
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ &amp;&amp; uv run ruff check app/services/endgame_zones.py scripts/gen_endgame_zones_ts.py &amp;&amp; uv run python -c "from app.services.endgame_zones import PER_CLASS_GAUGE_ZONES; assert len(PER_CLASS_GAUGE_ZONES) == 6, f'expected 6 classes, got {len(PER_CLASS_GAUGE_ZONES)}'; assert all(isinstance(b.p50, tuple) and len(b.p50) == 2 for b in PER_CLASS_GAUGE_ZONES.values()), 'p50 must be a 2-tuple on every class'; assert PER_CLASS_GAUGE_ZONES['rook'].p50 == (0.71, 0.30); assert PER_CLASS_GAUGE_ZONES['pawnless'].p50 == (0.79, 0.20); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "p50: tuple\[float, float\]" app/services/endgame_zones.py` returns exactly one match in the `PerClassBands` dataclass.
    - `grep -c "p50=" app/services/endgame_zones.py` returns 6 (one per class in PER_CLASS_GAUGE_ZONES).
    - `grep -n "pooled per-class typical centre" app/services/endgame_zones.py` returns at least one match (D-04 comment).
    - `grep -n "p50: {{ conversion:" scripts/gen_endgame_zones_ts.py` returns one match in `_format_per_class_gauge_zones`.
    - `uv run python -c "from app.services.endgame_zones import PER_CLASS_GAUGE_ZONES; assert PER_CLASS_GAUGE_ZONES['rook'].p50 == (0.71, 0.30); assert PER_CLASS_GAUGE_ZONES['pawnless'].p50 == (0.79, 0.20); print('ok')"` prints `ok` and exits 0.
    - `uv run ty check app/ tests/` exits 0.
  </acceptance_criteria>
  <done>
    `PerClassBands` carries `p50: tuple[float, float]`; all 6 PER_CLASS_GAUGE_ZONES entries populated with D-03 values; emitter extended; ty + ruff clean; no service / schema changes touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate endgameZones.ts via codegen and validate drift guard locally</name>
  <files>frontend/src/generated/endgameZones.ts</files>
  <read_first>
    - frontend/src/generated/endgameZones.ts (current emitted shape, lines 65-72 PER_CLASS_GAUGE_ZONES block)
    - scripts/gen_endgame_zones_ts.py (the freshly-edited emitter)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md (Pitfall 2: byte-exact diff guard; Pitfall 6: float formatting; CI invocation block)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md (Pattern A; expected post-regeneration shape excerpt)
  </read_first>
  <action>
    1. Run `uv run python scripts/gen_endgame_zones_ts.py`. This writes `frontend/src/generated/endgameZones.ts`.
    2. Inspect the regenerated file. Confirm the PER_CLASS_GAUGE_ZONES block now contains a `p50: { conversion: <num>, recovery: <num> }` key on every class line (rook, minor_piece, pawn, queen, mixed, pawnless), and the IQR formatting on existing keys (`conversion: [...]`, `recovery: [...]`) is byte-identical to the pre-edit state for those keys (Pitfall 2 — no whitespace drift).
    3. Stage the regenerated file alongside the Task 1 changes so they commit together (CI drift guard requires Python source + TS output in one commit).
    4. Run the CI drift guard locally: `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts`. Expect exit 0 (no diff after a second run — the first run wrote the new content; the second run confirms idempotence).
    5. Do NOT hand-edit `endgameZones.ts`. If formatting drift appears (e.g. trailing-zero behaviour inconsistency between classes), fix the emitter in Task 1 and re-run.
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py &amp;&amp; git diff --exit-code frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "p50: { conversion:" frontend/src/generated/endgameZones.ts` returns 6 (one per class in PER_CLASS_GAUGE_ZONES).
    - `grep -E "^\s+(rook|minor_piece|pawn|queen|mixed|pawnless): \{ conversion: \[.*\], recovery: \[.*\], p50: \{ conversion: [0-9.]+, recovery: [0-9.]+ \} \}," frontend/src/generated/endgameZones.ts | wc -l` returns 6.
    - `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0 (idempotence — running codegen twice in a row produces no diff).
    - File still carries the `// AUTO-GENERATED — do not edit by hand.` header.
  </acceptance_criteria>
  <done>
    `frontend/src/generated/endgameZones.ts` regenerated, contains p50 keys on all 6 classes, drift guard green locally, file staged for commit alongside Task 1 changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add TestPerClassP50 unit-test class</name>
  <files>tests/services/test_endgame_zones.py</files>
  <read_first>
    - tests/services/test_endgame_zones.py (full file; focus on the existing `TestRegistrySanity` class at lines 191-240 as template; import block at lines 6-13)
    - app/services/endgame_zones.py (freshly extended PerClassBands; PER_CLASS_GAUGE_ZONES values)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md (Example 6 — TestPerClassP50 template; Phase Requirements → Test Map for DATA-01 rows)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md (section for tests/services/test_endgame_zones.py)
    - .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-VALIDATION.md (Wave 0 + DATA-01 test rows)
  </read_first>
  <behavior>
    Test cases (append a new `TestPerClassP50` class to the file):
    - Test 1 (`test_all_six_classes_have_p50`): For every class in `("rook", "minor_piece", "pawn", "queen", "mixed", "pawnless")`, `PER_CLASS_GAUGE_ZONES[cls].p50` is a 2-tuple of floats, each value in `[0.0, 1.0]`.
    - Test 2 (`test_per_class_p50_matches_benchmark`): Per-class `pytest.approx` assertions against D-03 values (rook=(0.71, 0.30), minor_piece=(0.69, 0.33), pawn=(0.74, 0.28), queen=(0.77, 0.23), mixed=(0.69, 0.31), pawnless=(0.79, 0.20)) with `abs=0.005`. May be one parametrized test or a single method asserting all 6.
    - Test 3 (`test_p50_inside_iqr_for_most_classes`): Sanity — for rook, minor_piece, pawn, queen, mixed, the p50 falls inside `[c_lo, c_hi]` / `[r_lo, r_hi]`. Skip pawnless per D-05 (the band-shift recommendation is deferred; pawnless conv p50=0.79 sits at the high edge of [0.70, 0.80]).
    All tests use `def test_xxx(self) -> None:` annotation style matching `TestRegistrySanity` (lines 195, 225).
  </behavior>
  <action>
    1. Append `from app.services.endgame_zones import PER_CLASS_GAUGE_ZONES` to the existing import block at lines 6-13 (keep imports tidy and alphabetically grouped if the existing file is).
    2. Append a new class `TestPerClassP50` at the END of the file, after the last existing class. Docstring: `"""Phase 84 (DATA-01): per-class p50 centre tick on PerClassBands."""`.
    3. Implement the three test methods described in `<behavior>` using the exact assertion style from Example 6 in 84-RESEARCH.md. For Test 2, hardcode the expected values from the D-03 table — DO NOT recompute from any source.
    4. Use `pytest.approx(value, abs=0.005)` tolerance for value comparisons (matches 2dp rounding granularity). Import `pytest` at the top if not already imported.
    5. Type-annotate every test method with `-> None`. Pass `uv run ty check app/ tests/`.
    6. Do NOT modify any existing tests in the file (TestAssignZone, TestRegistrySanity, etc. are out of scope).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_zones.py::TestPerClassP50 -x &amp;&amp; uv run ty check tests/services/test_endgame_zones.py &amp;&amp; uv run ruff check tests/services/test_endgame_zones.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class TestPerClassP50" tests/services/test_endgame_zones.py` returns one match.
    - `grep -c "def test_" tests/services/test_endgame_zones.py` increases by at least 3 vs pre-edit baseline.
    - `uv run pytest tests/services/test_endgame_zones.py::TestPerClassP50 -x` exits 0 with at least 3 tests passing.
    - `uv run pytest tests/services/test_endgame_zones.py::TestPerClassP50::test_per_class_p50_matches_benchmark -x` exits 0 (verifies the D-03 values landed correctly).
    - `uv run ty check tests/services/test_endgame_zones.py` exits 0.
    - Full file still parses: `uv run pytest tests/services/test_endgame_zones.py -x` exits 0 (no regression to existing tests).
  </acceptance_criteria>
  <done>
    `TestPerClassP50` class with ≥3 test methods (presence, per-class match, IQR sanity) appended to `tests/services/test_endgame_zones.py`; all green; ty clean; pre-existing tests in the file still pass.
  </done>
</task>

</tasks>

<verification>
- `uv run ty check app/ tests/` exits 0 (zero errors, CI gate).
- `uv run ruff check .` clean on the four modified files.
- `uv run pytest tests/services/test_endgame_zones.py -x` green.
- CI drift guard: `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0.
- `frontend/src/generated/endgameZones.ts` regenerated and committed alongside the Python source changes (single-commit-per-CI-pass convention).
</verification>

<success_criteria>
- DATA-01 success criteria (v1.17-ROADMAP.md): per-class p50 reachable from the frontend for Section 3 bullets.
- `PER_CLASS_GAUGE_ZONES[<class>].p50` populated for all 6 classes (rook, minor_piece, pawn, queen, mixed, pawnless) matching D-03 verbatim (2dp).
- Regenerated `endgameZones.ts` exposes `p50: { conversion: <num>, recovery: <num> }` on each class entry.
- CI drift guard green; ty zero errors; ruff clean; new `TestPerClassP50` (≥3 tests) green.
- No service-layer or schema changes (those are Plan 02 scope per D-11).
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md` per the standard summary template. Include sections:
- What changed (PerClassBands extension, codegen extension, regenerated TS, new test class)
- D-01..D-05 references where each decision landed (file:line cites)
- Drift-guard status (idempotent re-run passes)
- Cross-reference to Plan 02 (schema/service wiring) and Plan 03 (audit doc)
- This SUMMARY also serves as the inline audit deliverable per D-10 (Plan 03 will append the audit content; Plan 01 SUMMARY records the codegen deliverable only).
</output>
