---
phase: 84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/test_endgame_service.py
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "ConversionRecoveryStats on /api/endgames/overview and /api/endgames/stats carries four opponent-baseline fields per endgame class."
    - "opponent_conversion_pct is computed via the mirror identity (recovery_games - recovery_wins - recovery_draws) / recovery_games and rounded to one decimal on the 0-100 percent scale."
    - "opponent_recovery_pct is computed via the mirror identity (conversion_losses + conversion_draws) / conversion_games and rounded to one decimal on the 0-100 percent scale."
    - "opponent_conversion_pct is None when recovery_games < 10; opponent_recovery_pct is None when conversion_games < 10."
    - "opponent_conversion_games equals recovery_games (always int, possibly 0); opponent_recovery_games equals conversion_games (always int, possibly 0)."
    - "An audit paragraph in SUMMARY.md cites the file:line evidence that Section 2 peer rates are already exposed on MaterialRow and documents how Phase 86 derives Opp Skill from the existing MaterialRow[conversion].opponent_score and MaterialRow[recovery].opponent_score."
    - "All existing endgame_service tests pass; new mirror-identity tests pass; ty and ruff are green."
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "ConversionRecoveryStats with 4 new opponent_* fields appended after recovery_draws"
      contains: "opponent_conversion_pct"
    - path: "app/services/endgame_service.py"
      provides: "_aggregate_endgame_stats wiring that populates the 4 new fields using mirror identities + _MIN_OPPONENT_SAMPLE gating"
      contains: "opponent_conversion_pct"
    - path: "tests/test_endgame_service.py"
      provides: "Unit tests for symmetric mirror, below-threshold None, at-threshold computed, zero-sample safety, schema shape"
      contains: "test_per_type_opponent_baseline"
  key_links:
    - from: "app/services/endgame_service.py:_aggregate_endgame_stats"
      to: "app/schemas/endgames.py:ConversionRecoveryStats"
      via: "ConversionRecoveryStats(...) constructor kwargs"
      pattern: "opponent_conversion_pct=opponent_conversion_pct"
    - from: "app/services/endgame_service.py mirror block"
      to: "app/services/endgame_service.py:233 _MIN_OPPONENT_SAMPLE"
      via: "threshold gate on mirror bucket size"
      pattern: "_MIN_OPPONENT_SAMPLE"
---

<objective>
Ship DATA-02 in a single plan: extend `ConversionRecoveryStats` with four opponent-baseline fields populated via same-game mirror identities scoped per `EndgameClass`, and record the Section 2 audit (already-wired confirmation) inline in the phase SUMMARY.md.

Purpose: Phase 87 will render per-type Conv/Recov peer bullets directly from the response payload without re-deriving from raw WDL fields client-side. Keeping a single backend identity (mirroring the Phase 60 Section 2 pattern) avoids FE math drift between Section 2 and Section 3.

Output:
- `app/schemas/endgames.py` — `ConversionRecoveryStats` extended with four required fields after `recovery_draws` (per D-03).
- `app/services/endgame_service.py` — mirror identities wired into `_aggregate_endgame_stats()` between the existing `recovery_pct = ...` line (around line 355) and the `ConversionRecoveryStats(...)` constructor call (line 357), gated on `_MIN_OPPONENT_SAMPLE` against the mirror bucket size (per D-04, D-05, D-06).
- `tests/test_endgame_service.py` — five new unit tests inside `TestAggregateEndgameStats` (bare-tuple convention, per the discretion call in CONTEXT.md and RESEARCH.md Open Question 1) covering symmetric mirror, below-threshold gating, at-threshold gating, zero-sample safety, and schema shape.
- `.planning/milestones/v1.17-phases/84-.../84-01-SUMMARY.md` — audit paragraph (per D-02) with file:line citations and the `Opp Skill` derivation note for Phase 86.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-CONTEXT.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-RESEARCH.md
@.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-PATTERNS.md
@.planning/notes/v1.17-single-bullet-doctrine.md
@CLAUDE.md
@app/schemas/endgames.py
@app/services/endgame_service.py

<interfaces>
<!-- Key types and contracts the executor needs. Use directly — no codebase exploration required. -->

From `app/schemas/endgames.py:19-42` (current `ConversionRecoveryStats` shape — extend at end, after `recovery_draws`):

```python
class ConversionRecoveryStats(BaseModel):
    """Inline conversion/recovery stats for one endgame category ..."""
    conversion_pct: float
    conversion_games: int
    conversion_wins: int
    conversion_draws: int
    conversion_losses: int

    recovery_pct: float
    recovery_games: int
    recovery_saves: int
    recovery_wins: int
    recovery_draws: int
```

From `app/services/endgame_service.py:233` (threshold constant — reuse, do NOT introduce a parallel `PER_CLASS_OPPONENT_SAMPLE_MIN`):

```python
_MIN_OPPONENT_SAMPLE = 10
```

From `app/services/endgame_service.py:341-368` (current wiring site — insertion point is between line 355 and line 357):

```python
conversion_games = conv_data["games"]
conversion_wins = conv_data["wins"]
conversion_draws = conv_data["draws"]
conversion_losses = conversion_games - conversion_wins - conversion_draws
conversion_pct = (
    round(conversion_wins / conversion_games * 100, 1) if conversion_games > 0 else 0.0
)

recovery_games = recov_data["games"]
recovery_wins = recov_data["wins"]
recovery_draws = recov_data["draws"]
recovery_saves = recovery_wins + recovery_draws
recovery_pct = (
    round(recovery_saves / recovery_games * 100, 1) if recovery_games > 0 else 0.0
)
# <-- INSERT mirror-identity block HERE -->
conversion_stats = ConversionRecoveryStats(
    conversion_pct=conversion_pct,
    conversion_games=conversion_games,
    conversion_wins=conversion_wins,
    conversion_draws=conversion_draws,
    conversion_losses=conversion_losses,
    recovery_pct=recovery_pct,
    recovery_games=recovery_games,
    recovery_saves=recovery_saves,
    recovery_wins=recovery_wins,
    recovery_draws=recovery_draws,
    # <-- ADD 4 new kwargs HERE -->
)
```

From `app/services/endgame_service.py:824-855` (Phase 60 Section 2 template — pattern reference only, do not copy verbatim because Section 3 is per-class not per-bucket):

```python
# Phase 60 mirror-bucket opponent baseline (Section 2 pattern).
# `swap_bucket` maps conv<->recov; opponent_score = 1.0 - bucket_score[swap_bucket]
# is gated on _MIN_OPPONENT_SAMPLE applied to swap_games. opponent_games is plain int.
```

From `tests/test_endgame_service.py:184-205` (bare-tuple row convention used by `TestAggregateEndgameStats`):

Row tuple shape: `(game_id, class_int, result, color, eval_cp, eval_mate)` where `class_int` follows `_INT_TO_CLASS` (rook = 1). Eval >= +100 cp → conversion bucket; eval <= -100 cp → recovery bucket. `result` is `"1-0" | "0-1" | "1/2-1/2"`; `color` is `"white" | "black"`. Win/loss is computed from `(result, color)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ConversionRecoveryStats schema with 4 opponent fields</name>
  <files>app/schemas/endgames.py</files>
  <behavior>
    - The four new fields are appended to `ConversionRecoveryStats` immediately after `recovery_draws`, in this order:
      1. `opponent_conversion_pct: float | None`
      2. `opponent_conversion_games: int`
      3. `opponent_recovery_pct: float | None`
      4. `opponent_recovery_games: int`
    - All four fields are REQUIRED (no defaults). This matches the existing `MaterialRow.opponent_score` / `opponent_games` convention.
    - The class docstring is updated to mention the new fields and reference Phase 84.
    - Other Pydantic models in `app/schemas/endgames.py` are unchanged.
    - The change is wire-shape only; no service code is touched in this task — Task 2 supplies the values.
  </behavior>
  <action>
    Edit `app/schemas/endgames.py:19-42`. Append four required fields to `ConversionRecoveryStats` after `recovery_draws` (per D-03):

    - `opponent_conversion_pct: float | None` with inline comment "None when recovery_games < _MIN_OPPONENT_SAMPLE; else opponent's win-rate when opponent entered with eval advantage (Phase 84, mirror identity)"
    - `opponent_conversion_games: int` with inline comment "== recovery_games (mirror sample size, always int, possibly 0)"
    - `opponent_recovery_pct: float | None` with inline comment "None when conversion_games < _MIN_OPPONENT_SAMPLE; else opponent's save-rate when opponent entered with eval deficit"
    - `opponent_recovery_games: int` with inline comment "== conversion_games (mirror sample size, always int, possibly 0)"

    Update the `ConversionRecoveryStats` docstring with a Phase 84 paragraph: "Phase 84: opponent baselines via same-game mirror identity, scoped per endgame class. opponent_conversion = opponent's win rate when opponent entered with eval advantage (derived from user_recovery_*); opponent_recovery = opponent's save rate when opponent entered with eval deficit (derived from user_conversion_*). Gated on _MIN_OPPONENT_SAMPLE against the mirror bucket size."

    Field ordering, naming (verbose `opponent_conversion_*` / `opponent_recovery_*` not the shorthand `opp_conv_*`), and required-without-default shape are LOCKED by D-03. Do NOT add `@field_validator` for range checks (none on `conversion_pct` / `recovery_pct` either — consistency wins, the math is bounded by construction).

    Do NOT modify `EndgameCategoryStats`, `MaterialRow`, or any other schema. The schema extension is the only change in this task.

    Pure schema change: no test must yet pass on values (Task 3 covers values). After this task, `uv run ty check app/` will report `_aggregate_endgame_stats` failing to construct `ConversionRecoveryStats` because the four new required kwargs are missing — Task 2 fixes that. Run ty + a smoke pytest collection only here to confirm syntax.
  </action>
  <verify>
    <automated>uv run python -c "from app.schemas.endgames import ConversionRecoveryStats; fields = ConversionRecoveryStats.model_fields; assert 'opponent_conversion_pct' in fields and 'opponent_conversion_games' in fields and 'opponent_recovery_pct' in fields and 'opponent_recovery_games' in fields, 'missing required field'; assert fields['opponent_conversion_pct'].is_required(); assert fields['opponent_recovery_games'].is_required(); print('schema OK')"</automated>
  </verify>
  <done>
    `ConversionRecoveryStats` exposes the four new fields in the correct order, all REQUIRED, with the documented types. The class docstring references Phase 84. No other file changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire mirror-identity computation into _aggregate_endgame_stats</name>
  <files>app/services/endgame_service.py</files>
  <behavior>
    - For each `endgame_class` in the existing per-class loop, the service computes four new local values BEFORE the `ConversionRecoveryStats(...)` constructor call:
      * `opponent_conversion_pct: float | None` — `round((recovery_games - recovery_wins - recovery_draws) / recovery_games * 100, 1)` when `recovery_games >= _MIN_OPPONENT_SAMPLE`, else `None`.
      * `opponent_conversion_games: int` — always `recovery_games` (possibly `0`, never `None`).
      * `opponent_recovery_pct: float | None` — `round((conversion_losses + conversion_draws) / conversion_games * 100, 1)` when `conversion_games >= _MIN_OPPONENT_SAMPLE`, else `None`.
      * `opponent_recovery_games: int` — always `conversion_games` (possibly `0`, never `None`).
    - The four values are passed to the `ConversionRecoveryStats(...)` constructor as new kwargs.
    - No new module-level constant is introduced. `_MIN_OPPONENT_SAMPLE = 10` at line 233 is reused.
    - No new DB query, no new accumulator key, no new helper function.
    - `_compute_score_gap_material` (around lines 824-855) is NOT modified — Section 2 path is unchanged.
    - `ty check` and `ruff check` pass with zero errors / warnings.
  </behavior>
  <action>
    Edit `app/services/endgame_service.py`. Locate `_aggregate_endgame_stats()` (defined around line 240). Inside the per-class loop, between line 355 (after `recovery_pct = round(...) if recovery_games > 0 else 0.0`) and line 357 (the `conversion_stats = ConversionRecoveryStats(...)` constructor call), insert the mirror-identity block.

    Required structure (per D-04, D-05, D-06, D-07 and RESEARCH.md "Pattern 2"):

    1. Short comment block (3-5 lines, like the Phase 60 comment at `endgame_service.py:824-829`) explaining: "Phase 84: per-class opponent baseline via same-game mirror identity. Conv is a win-rate, Recov is a save-rate — the two mirror formulas are asymmetric. Reuses `_MIN_OPPONENT_SAMPLE` (line 233), gated on the MIRROR bucket size. Phase 60 introduced the pattern for Section 2 at line 824."
    2. Compute `opponent_conversion_pct`:
       - Annotate the local explicitly as `opponent_conversion_pct: float | None` on the if/else (per Pitfall 7 in RESEARCH.md — ty narrowing requires the explicit annotation; matches the pattern at line 840 `opponent_score: float | None = ...`).
       - `if recovery_games >= _MIN_OPPONENT_SAMPLE:` compute `recovery_losses = recovery_games - recovery_wins - recovery_draws`, then `opponent_conversion_pct = round(recovery_losses / recovery_games * 100, 1)`.
       - `else: opponent_conversion_pct = None`.
       - `opponent_conversion_games = recovery_games` (plain int assignment, no annotation needed).
    3. Compute `opponent_recovery_pct` symmetrically (but using the SAVE-RATE numerator `conversion_losses + conversion_draws`, NOT a copy of the Conv formula — see Pitfall 1):
       - Annotate `opponent_recovery_pct: float | None`.
       - `if conversion_games >= _MIN_OPPONENT_SAMPLE:` compute `opponent_recovery_pct = round((conversion_losses + conversion_draws) / conversion_games * 100, 1)`.
       - `else: opponent_recovery_pct = None`.
       - `opponent_recovery_games = conversion_games`.
    4. Pass all four to the existing `ConversionRecoveryStats(...)` constructor call (line 357-368) as new kwargs, AFTER `recovery_draws` to match the schema field order.

    PITFALLS (call out to the executor — these are the failure modes the checker will look for):
    - **Mirror formulas are asymmetric.** `opponent_conversion_pct` uses `(recovery_games - recovery_wins - recovery_draws) / recovery_games`. `opponent_recovery_pct` uses `(conversion_losses + conversion_draws) / conversion_games`. DO NOT copy-paste one formula into the other.
    - **Gate on the MIRROR bucket.** `opponent_conversion_pct` gates on `recovery_games >= _MIN_OPPONENT_SAMPLE` (NOT on `conversion_games`). `opponent_recovery_pct` gates on `conversion_games >= _MIN_OPPONENT_SAMPLE` (NOT on `recovery_games`). The "sample size" for the opponent baseline is the mirror bucket's count.
    - **`_games` is `int`, never `None`.** Emit `recovery_games` and `conversion_games` directly. A zero-sample mirror bucket emits `0`. Do NOT type `int | None`.
    - **Percent scale.** Use `round(x / games * 100, 1)` to match the existing `conversion_pct` / `recovery_pct` convention (0-100 with one decimal). Do NOT switch to 0.0-1.0 score-style — the closest in-repo template at line 824 uses score-style because `MaterialRow.opponent_score` is score-style, but this schema is percent-style.
    - **DivByZero safety.** The `>= _MIN_OPPONENT_SAMPLE (= 10)` gate is strictly tighter than `> 0`. When the gate fails, `_pct` is `None` and the division is never executed. No separate `if games > 0` guard needed — the gate IS the guard. The zero-sample test in Task 3 confirms no `ZeroDivisionError` is raised.
    - **No new constant.** Per D-05, reuse `_MIN_OPPONENT_SAMPLE`. Do NOT introduce `PER_CLASS_OPPONENT_SAMPLE_MIN`.
    - **No helper extraction.** ~20 lines of inline arithmetic is the right shape per CLAUDE.md (nesting depth stays at 2 inside the existing `for endgame_class in wdl:` loop). Do NOT extract a `_compute_per_type_opponent_baseline(...)` helper unless ty forces it.
    - **No accumulator change.** Every numerator and denominator already exists at lines 341-355. Do NOT add a new accumulator key.
    - **Section 2 untouched.** Do NOT modify `_compute_score_gap_material` (line 824). Section 2 already ships the equivalent fields on `MaterialRow`.

    After editing, verify the change with `uv run ty check app/` (must be zero errors) and `uv run ruff check .` + `uv run ruff format --check .` (both must pass).
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run ruff check app/services/endgame_service.py && uv run ruff format --check app/services/endgame_service.py && grep -c "opponent_conversion_pct" app/services/endgame_service.py | grep -qv '^0$' && grep -c "opponent_recovery_pct" app/services/endgame_service.py | grep -qv '^0$'</automated>
  </verify>
  <done>
    `_aggregate_endgame_stats` populates the four new fields via the mirror identities documented in D-04. Gating uses `_MIN_OPPONENT_SAMPLE` against the MIRROR bucket size (not the own bucket). `_games` companions are always `int`. ty check passes with zero errors; ruff passes lint + format. `_compute_score_gap_material` is byte-identical to pre-task. No new constants or accumulator keys.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add per-type opponent-baseline tests + audit deliverable in SUMMARY.md</name>
  <files>tests/test_endgame_service.py, .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md</files>
  <behavior>
    - Five new test methods live inside `TestAggregateEndgameStats` (per RESEARCH.md Open Question 1 recommendation) using the existing bare-tuple row convention from line 184-205. No new helper class.
    - Test 1 (`test_per_type_opponent_baseline_symmetric_60_40`): rook class with user-conv 6W/0D/4L over 10 games AND user-recov 2W/2D/6L over 10 games asserts `opponent_conversion_pct == 60.0` (= 6 user-recov losses / 10 recov games), `opponent_recovery_pct == 40.0` (= (4 user-conv losses + 0 draws) / 10 conv games), `opponent_conversion_games == 10`, `opponent_recovery_games == 10`.
    - Test 2 (`test_per_type_opponent_baseline_below_threshold`): rook class with recovery_games = 9, asserts `opponent_conversion_pct is None` and `opponent_conversion_games == 9`. Also asserts the symmetric Recov-side gate (conversion_games = 9 → `opponent_recovery_pct is None`, `opponent_recovery_games == 9`).
    - Test 3 (`test_per_type_opponent_baseline_at_threshold_10`): mirror bucket has exactly 10 games, asserts `_pct` is non-None and `_games == 10` for both sides.
    - Test 4 (`test_per_type_opponent_baseline_zero_sample`): rook class with conversion_games = 0 and recovery_games = 0 (only parity entries), asserts `opponent_conversion_pct is None`, `opponent_recovery_pct is None`, `opponent_conversion_games == 0`, `opponent_recovery_games == 0`. Asserts no `ZeroDivisionError` is raised.
    - Test 5 (`test_per_type_opponent_baseline_schema_shape`): asserts `ConversionRecoveryStats.model_fields` contains the four new fields with the documented optional/required types. May be a one-liner asserting field presence + type via `pytest.raises(ValidationError)` when omitted, or an `isinstance` check on a populated instance.
    - `tests/test_endgame_service.py::TestAggregateEndgameStats` runs green on the full test method set (existing tests still pass after the schema/service change — no regression).
    - The phase summary file `84-01-SUMMARY.md` (a single SUMMARY for the single plan) carries the Section 2 audit paragraph per D-02 with file:line citations.
  </behavior>
  <action>
    PART A — Tests (`tests/test_endgame_service.py`):

    Locate `TestAggregateEndgameStats` (defined around line 181) and the bare-tuple row convention at lines 184-205. Append the five new test methods INSIDE this class (do NOT create a sibling class — RESEARCH.md Open Question 1 recommendation: co-locate with the function under test, which is `_aggregate_endgame_stats`).

    Row tuple shape (per existing convention): `(game_id, class_int, result, color, eval_cp, eval_mate)` where:
    - `class_int = 1` for rook (per `_INT_TO_CLASS`),
    - eval_cp >= +100 → conversion bucket; eval_cp <= -100 → recovery bucket; otherwise parity,
    - white perspective: `"1-0"` is win, `"0-1"` is loss, `"1/2-1/2"` is draw.

    Test 1 — `test_per_type_opponent_baseline_symmetric_60_40`:
    ```
    Use the docstring: "rook conv 6W/0D/4L (60% win) + recov 2W/2D/6L (40% save-rate) →
    opp_conv == 60.0 (mirror = recov_losses / recov_games), opp_recov == 40.0 (mirror =
    (conv_losses + conv_draws) / conv_games)."
    ```
    Build 10 conversion rows (eval_cp = 150, white) and 10 recovery rows (eval_cp = -150, white). Six conv "1-0", four conv "0-1". Two recov "1-0", two recov "1/2-1/2", six recov "0-1". Call `_aggregate_endgame_stats(...)`, find the rook category, assert via `pytest.approx(60.0, abs=0.05)` and `pytest.approx(40.0, abs=0.05)` plus the two `_games == 10` checks.

    Test 2 — `test_per_type_opponent_baseline_below_threshold`:
    Build rook with conversion_games = 9 (any WDL split — e.g. all losses) AND recovery_games = 9 (any split). Assert BOTH:
    - `rook.conversion.opponent_conversion_pct is None` and `rook.conversion.opponent_conversion_games == 9` (gated on recovery_games = 9 < 10).
    - `rook.conversion.opponent_recovery_pct is None` and `rook.conversion.opponent_recovery_games == 9` (gated on conversion_games = 9 < 10).

    Test 3 — `test_per_type_opponent_baseline_at_threshold_10`:
    Build rook with conversion_games = 10 AND recovery_games = 10. Assert both `_pct` values are non-None (`is not None` checks, not exact values — the goal is the threshold boundary), and both `_games == 10`.

    Test 4 — `test_per_type_opponent_baseline_zero_sample`:
    Build rook with ONLY parity rows (eval_cp = 0 or in [-99, +99], so neither bucket fills). The rook category should still appear in results because `wdl` accumulates parity entries. Assert:
    - `rook.conversion.opponent_conversion_pct is None`
    - `rook.conversion.opponent_recovery_pct is None`
    - `rook.conversion.opponent_conversion_games == 0`
    - `rook.conversion.opponent_recovery_games == 0`
    - Test does NOT raise `ZeroDivisionError` (the gate is the guard).

    If parity rows alone don't trigger inclusion in the returned categories list (depends on the existing `_aggregate_endgame_stats` filter logic — verify by reading lines 240-340 of `endgame_service.py` when implementing), fall back to a class with `conversion_games == 0` and `recovery_games == 0` derived from a single non-bucketed row (e.g. eval_cp = 0), or seed both buckets with sub-threshold counts and dial conversion_games and recovery_games to 0 specifically. The exact row arrangement is an implementation detail; the assertion targets above are the contract.

    Test 5 — `test_per_type_opponent_baseline_schema_shape`:
    Either:
    - A `ConversionRecoveryStats.model_fields` introspection (`assert ConversionRecoveryStats.model_fields["opponent_conversion_pct"].is_required()` etc. for all four), OR
    - Construct a `ConversionRecoveryStats(...)` with all 14 fields and assert `isinstance(stats.opponent_conversion_games, int)` plus `stats.opponent_conversion_pct is None or isinstance(stats.opponent_conversion_pct, float)`.

    Pick whichever is shorter (a single `model_fields` introspection covers all four in three lines).

    PART B — SUMMARY.md audit (`.planning/milestones/v1.17-phases/84-.../84-01-SUMMARY.md`):

    Create the per-plan SUMMARY using the standard `@$HOME/.claude/get-shit-done/templates/summary.md` template. In addition to the standard sections (Goal, Changes, Verification, Notes), include a dedicated audit section per D-02:

    ```
    ## DATA-02 Section 2 audit (already wired)

    Section 2's Conv / Parity / Recov peer bullets and the Skill card's derived Opp Skill
    baseline are already exposed on `/api/endgames/overview` — no new payload field for
    Section 2.

    Evidence:
    - `MaterialRow.opponent_score: float | None` + `MaterialRow.opponent_games: int`
      defined in `app/schemas/endgames.py:215-237`.
    - Populated by `_compute_score_gap_material` in `app/services/endgame_service.py:824-855`
      via Phase 60's swap-bucket mirror identity (`opponent_score = 1.0 - bucket_score[swap_bucket]`,
      gated on `_MIN_OPPONENT_SAMPLE = 10`).
    - Wired into `/api/endgames/overview` via `ScoreGapMaterialResponse.material_rows`
      (`app/schemas/endgames.py:285-303`) and `EndgameOverviewResponse.score_gap_material`
      (`app/schemas/endgames.py:475-491`).
    - Frontend consumes the fields in `frontend/src/components/charts/EndgameScoreGapSection.tsx:111-145`
      via `MIRROR_BUCKET` and `opponentRate()`.

    Skill peer baseline (Opp Skill) — Phase 86 will derive it client-side from
    `MaterialRow[conversion].opponent_score` + `MaterialRow[recovery].opponent_score`
    (mirroring the Section 2 frontend helper). No new payload field is required for
    Skill; the existing two opponent scores suffice. Originating phase: Phase 60.

    ## DATA-02 Section 3 extension (this phase)

    `ConversionRecoveryStats` now carries four new required fields after `recovery_draws`,
    populated by `_aggregate_endgame_stats` via the same-game mirror identity scoped per
    `EndgameClass`:
    - `opponent_conversion_pct: float | None` — opponent's win-rate when opponent entered
      with eval advantage. Mirror identity: `(recovery_games - recovery_wins - recovery_draws)
      / recovery_games`. Gated on `recovery_games >= _MIN_OPPONENT_SAMPLE`.
    - `opponent_conversion_games: int` — `== recovery_games` (mirror sample size).
    - `opponent_recovery_pct: float | None` — opponent's save-rate when opponent entered
      with eval deficit. Mirror identity: `(conversion_losses + conversion_draws) /
      conversion_games`. Gated on `conversion_games >= _MIN_OPPONENT_SAMPLE`.
    - `opponent_recovery_games: int` — `== conversion_games` (mirror sample size).

    The mirror formulas are asymmetric (Conv = win-rate, Recov = save-rate). The
    threshold gate (`>= 10`) is strictly tighter than `> 0`, so no separate
    DivByZero guard is needed. Phase 87 will consume these fields directly for the
    per-type Conv / Recov peer bullets.
    ```

    Include file:line citations exactly as shown above. The audit is short enough (~30 lines) to live inline per D-11's single-plan default.

    Run all verification commands listed in the plan's `<verification>` section before marking the task done.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -k opponent -x && uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -x && uv run ty check app/ tests/ && uv run ruff check . && uv run ruff format --check . && test -f .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md && grep -q "Section 2 audit" .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md && grep -q "opponent_score" .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md</automated>
  </verify>
  <done>
    All five new tests pass. The full `TestAggregateEndgameStats` class is green (no regression on existing tests). ty + ruff are clean. `84-01-SUMMARY.md` exists with the Section 2 audit prose (file:line citations) and the Section 3 extension documentation, per D-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Phase 84 is a backend-internal schema extension + arithmetic addition. No new endpoint, no new request shape, no new authentication surface, no new persistence, no new external HTTP call, no new secrets. The four new fields surface on `/api/endgames/overview` and `/api/endgames/stats` which already enforce FastAPI-Users auth and user-scoped queries — that boundary is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Information Disclosure | `ConversionRecoveryStats.opponent_*` fields on `/api/endgames/overview` | accept | Same trust boundary as existing `MaterialRow.opponent_score` (Phase 60). The four new fields are derived from the same user-scoped accumulator and surfaced through the same auth-gated route — no PII, no cross-user data, no new exposure. The values are statistical aggregates over the requesting user's own games. |
| T-84-02 | Denial of Service | `_aggregate_endgame_stats` mirror arithmetic | mitigate | The arithmetic is O(1) per `EndgameClass` (constant number of classes via the `EndgameClass` Literal). The threshold gate `>= _MIN_OPPONENT_SAMPLE` is strictly tighter than `> 0`, eliminating `ZeroDivisionError` risk. Test 4 in this plan asserts the zero-sample path raises no exception. |
| T-84-03 | Tampering | Pydantic v2 schema validation of the four new fields | mitigate | All four fields are typed (`float \| None`, `int`) and required; Pydantic v2 rejects payloads with wrong types or missing keys on serialization. No `@field_validator` is added because the values come from a trusted in-process accumulator, not user input — consistent with existing `conversion_pct` / `recovery_pct`. |
</threat_model>

<verification>
After all three tasks complete, run from the repo root:

```bash
# Fast feedback (under ~3 seconds)
uv run pytest tests/test_endgame_service.py -k opponent -x

# Full aggregator class (covers all existing + new tests)
uv run pytest tests/test_endgame_service.py::TestAggregateEndgameStats -x

# Phase gate — full suite green
uv run pytest

# Type check (must be zero errors per CLAUDE.md)
uv run ty check app/ tests/

# Lint + format check (must pass cleanly)
uv run ruff check .
uv run ruff format --check .

# Audit deliverable exists
test -f .planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md
```

Out-of-scope check (must be UNCHANGED — these files belong to the pivoted-away DATA-01 scope):
```bash
git diff --name-only HEAD | grep -E "(endgame_zones\.py|gen_endgame_zones_ts\.py|endgameZones\.ts|test_endgame_zones\.py)" || echo "OK: no out-of-scope files touched"
```
The grep must produce no matches (the `|| echo` will run, printing the OK line). If any of those four files appear in the diff, the plan was violated.
</verification>

<success_criteria>
This plan ships DATA-02 in full. Success is measured by:

1. **Audit confirmation prose** — `84-01-SUMMARY.md` contains the Section 2 already-wired evidence (file:line citations for `MaterialRow.opponent_score`, `_compute_score_gap_material`, `EndgameScoreGapSection.tsx`), the `Opp Skill` derivation note for Phase 86, and the Section 3 new-fields documentation. Per D-02, this lives inline in the plan summary, not in `.planning/notes/`.

2. **`ConversionRecoveryStats` carries the 4 new fields on the wire** — `opponent_conversion_pct: float | None`, `opponent_conversion_games: int`, `opponent_recovery_pct: float | None`, `opponent_recovery_games: int`, appended after `recovery_draws`, all required, no defaults. Confirmed by introspection in Test 5.

3. **`_aggregate_endgame_stats` populates the 4 fields** — using the mirror identities from D-04, with `_MIN_OPPONENT_SAMPLE` gating on the MIRROR bucket size (not the own bucket), `round(x, 1)` percent-style scaling, and zero-sample safety (the gate is the guard).

4. **Tests pass; ty + ruff green** — `uv run pytest tests/test_endgame_service.py -k opponent -x` covers both the Phase 60 Section 2 tests and the new Phase 84 Section 3 tests; full `TestAggregateEndgameStats` runs green; `uv run ty check app/ tests/` reports zero errors; ruff lint + format are clean.

5. **No out-of-scope changes** — `app/services/endgame_zones.py`, `scripts/gen_endgame_zones_ts.py`, `frontend/src/generated/endgameZones.ts`, and `tests/services/test_endgame_zones.py` are UNCHANGED (DATA-01 was dropped per the single-bullet doctrine pivot on 2026-05-12). No frontend changes. No DB migration. No new threshold constant. No new accumulator key. No new DB query. No new helper function. `_compute_score_gap_material` (Section 2 path) is byte-identical to pre-phase.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/84-01-SUMMARY.md` (the file is built as part of Task 3, but the standard SUMMARY frontmatter + sections should follow `@$HOME/.claude/get-shit-done/templates/summary.md` with the additional DATA-02 audit content described in Task 3's action block).
</output>
