---
phase: 88.4-time-pressure-card-layout-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/ScoreGapByTimePressureChart.tsx
  - frontend/src/components/charts/__tests__/ScoreGapByTimePressureChart.test.tsx
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "A horizontal score-gap line chart renders 4 ordinal pressure-bucket datapoints (Q4 filtered out)"
    - "The chart shows 3 flat zone bands (danger / neutral [-0.06,+0.06] / success) like EndgameClockDiffOverTimeChart"
    - "Each non-zero-CI datapoint renders a CI whisker via Recharts ErrorBar"
    - "Zero-n quintile bins are omitted, producing a line gap with no dot/whisker at that x-position"
    - "Hovering a datapoint shows a tooltip with signed score gap, opponent score, and game count"
  artifacts:
    - path: "frontend/src/components/charts/ScoreGapByTimePressureChart.tsx"
      provides: "Self-contained ComposedChart score-gap-by-time-pressure component"
      contains: "ErrorBar"
    - path: "frontend/src/components/charts/__tests__/ScoreGapByTimePressureChart.test.tsx"
      provides: "Wave 0 SC-3 test scaffold (4 datapoints, 3 bands, ErrorBar, zero-n gap, collapsed band constant)"
      contains: "vi.mock('recharts'"
  key_links:
    - from: "ScoreGapByTimePressureChart.tsx"
      to: "@/lib/pressureBulletConfig"
      via: "import clampDeltaCi, pressureDeltaZoneColor, PRESSURE_DELTA_DOMAIN"
      pattern: "from '@/lib/pressureBulletConfig'"
    - from: "ScoreGapByTimePressureChart.tsx"
      to: "@/lib/theme"
      via: "import ZONE_DANGER, ZONE_NEUTRAL, ZONE_SUCCESS"
      pattern: "ZONE_DANGER|ZONE_NEUTRAL|ZONE_SUCCESS"
---

<objective>
Build the new self-contained `ScoreGapByTimePressureChart` component (SC-3): a Recharts
`ComposedChart` that replaces the four stacked per-bucket Score-Gap bullet charts with a
single horizontal line chart over the 4 ordinal pressure-bucket labels, reusing the
`EndgameClockDiffOverTimeChart` zone-band / 0-centered-axis visual pattern, adding CI
whiskers via Recharts `ErrorBar`, and surfacing per-bucket stats in a hover tooltip.

Also create the Wave 0 test scaffold `ScoreGapByTimePressureChart.test.tsx` that locks
the SC-3 behaviors before/while the component is built.

Purpose: This component is the contract that Plan 02 (card integration) consumes. It is
built first and standalone so the card refactor in Plan 02 has a finished, tested
component to wire in.

Output: `ScoreGapByTimePressureChart.tsx` (new) + `ScoreGapByTimePressureChart.test.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-RESEARCH.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-PATTERNS.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted by the researcher; do not re-explore. -->

PressureQuintileBullet (frontend/src/types/endgames.ts lines 221–289):
- quintile_index: number (0..4)
- quintile_label: string
- n: number
- delta: number            (fraction, e.g. -0.171)
- p_value: number | null
- ci_low: number | null    (fraction)
- ci_high: number | null   (fraction)
- opp_score: number | null

pressureBulletConfig.ts exports:
- PRESSURE_DELTA_CENTER = 0
- PRESSURE_DELTA_DOMAIN = 0.30
- clampDeltaCi(value: number): number  (clamp to [-1, 1])
- pressureDeltaZoneColor(delta, neutralMin, neutralMax): string  (3-way zone color)

theme.ts exports: ZONE_DANGER, ZONE_NEUTRAL, ZONE_SUCCESS  (ZONE_OPACITY is NOT exported — local constant per chart, value 0.15)

Component prop contract (downstream Plan 02 imports this):
  export interface ScoreGapByTimePressureChartProps {
    quintiles: PressureQuintileBullet[];
    tc: 'bullet' | 'blitz' | 'rapid' | 'classical';
  }
  export function ScoreGapByTimePressureChart(props): JSX.Element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — scaffold ScoreGapByTimePressureChart.test.tsx</name>
  <read_first>
    - 88.4-PATTERNS.md lines 261–379 (full test analog: vi.mock stub, beforeAll matchMedia/ResizeObserver stubs, makeBin fixture builder, FOUR_BIN_FIXTURE, assertion patterns)
    - 88.4-VALIDATION.md lines 40–64 (per-task verification map + Wave 0 requirements)
    - 88.4-RESEARCH.md lines 556–598 (Validation Architecture: framework, test map, Wave 0 gaps)
    - frontend/src/components/charts/__tests__/EndgameClockDiffOverTimeChart.test.tsx (the verbatim vi.mock('recharts') stub + beforeAll source, lines 13–76)
  </read_first>
  <action>
    Create `frontend/src/components/charts/__tests__/ScoreGapByTimePressureChart.test.tsx`.
    Copy the `vi.mock('recharts', ...)` stub and `beforeAll` (matchMedia + ResizeObserverStub)
    and `afterEach(cleanup)` verbatim from `EndgameClockDiffOverTimeChart.test.tsx` per
    PATTERNS.md lines 266–320. Copy the `makeBin` fixture builder and `FOUR_BIN_FIXTURE`
    (5 bins Q0..Q4) per PATTERNS.md lines 322–352, typed against `PressureQuintileBullet`
    from `@/types/endgames`. Import `ScoreGapByTimePressureChart` and the exported
    constants `PRESSURE_SCORE_GAP_NEUTRAL_MIN` / `PRESSURE_SCORE_GAP_NEUTRAL_MAX` from
    `../ScoreGapByTimePressureChart`.

    Write these test cases (RESEARCH.md SC-3 test matrix, lines 578–585):
    1. Renders the chart root testid `score-gap-by-time-pressure-chart-bullet` (tc='bullet').
    2. Renders exactly 3 zone bands: `container.querySelectorAll('.recharts-reference-area-rect').length === 3`.
    3. Line stroke is white: `container.querySelector('.recharts-line-curve')` has `stroke="white"`.
    4. 4-bin fixture (Q0..Q4 all n>0) → exactly 4 rendered data dots (Q4 filtered out): assert
       on the rendered dot count via the dot key prefix or `.recharts-line-dot` count === 4.
    5. ErrorBar renders for bins with non-null CI: assert `.recharts-errorBar` (or the
       errorbar line element class produced by the recharts stub) is present and its count
       matches the 4 non-zero-CI bins.
    6. Zero-n bin omitted: build a fixture where Q1 has `n: 0, ci_low: null, ci_high: null`;
       assert only 3 data dots render and no whisker at that x-position.
    7. Collapsed neutral band constant assertion: `expect(PRESSURE_SCORE_GAP_NEUTRAL_MIN).toBe(-0.06)`
       and `expect(PRESSURE_SCORE_GAP_NEUTRAL_MAX).toBe(0.06)`.
    8. Tooltip surfaces per-bucket stats: render, locate the tooltip content node by
       `data-testid="score-gap-tooltip"` after triggering Recharts active state via the
       analog's tooltip-test approach (mirror how `EndgameClockDiffOverTimeChart.test.tsx`
       asserts its custom tooltip), and assert the tooltip text contains the signed delta %,
       opponent score %, and `n =`.

    Use `@vitest-environment jsdom` directive at the top. No `text-xs`/subjective assertions.
    Tests will fail RED until Task 2 lands the component — that is expected for the Wave 0
    scaffold.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "ScoreGapByTimePressureChart" | grep -c "error TS" | grep -qx 0 && echo "no unrelated TS errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/charts/__tests__/ScoreGapByTimePressureChart.test.tsx` exists.
    - File contains `vi.mock('recharts'` and `@vitest-environment jsdom`.
    - File contains an assertion `toBe(-0.06)` and an assertion `toBe(0.06)` for the exported band constants.
    - File contains an assertion on `.recharts-reference-area-rect` count equal to 3.
    - File contains a `data-testid` query for `score-gap-tooltip`.
    - File contains at least 8 `it(` test cases covering the 8 scenarios listed in the action.
  </acceptance_criteria>
  <done>The Wave 0 SC-3 test scaffold exists with all 8 scenarios; it compiles (TS) and is RED only because the component is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ScoreGapByTimePressureChart.tsx</name>
  <read_first>
    - 88.4-PATTERNS.md lines 24–257 (full chart analog: imports, ZONE_OPACITY local const, TC-collapsed band constants, zoneDotColor helper, data transform, ComposedChart skeleton, custom dot, tooltip, props interface)
    - 88.4-RESEARCH.md lines 136–334 (Q2 reuse target, Q3 field names + bucket count, Q4 zone-band collapse, Q6 patterns/pitfalls)
    - 88.4-RESEARCH.md lines 612–628 (Open Questions — adopt the researcher recommendations: w-1/2 N/A here; chart height h-44; no font-tinting in chart)
    - frontend/src/components/charts/EndgameClockDiffOverTimeChart.tsx (lines 13–316 — the established ComposedChart / ReferenceArea / ReferenceLine / custom-dot / ChartTooltip pattern; reproduce this, do not invent)
    - frontend/src/lib/pressureBulletConfig.ts (full — clampDeltaCi, pressureDeltaZoneColor, PRESSURE_DELTA_DOMAIN signatures)
    - frontend/src/lib/theme.ts (ZONE_DANGER / ZONE_NEUTRAL / ZONE_SUCCESS definitions)
  </read_first>
  <behavior>
    - Given 5 bins Q0..Q4 with n>0 → chart data has exactly 4 points (quintile_index <= 3 filter), Q4 dropped.
    - Given a bin with n === 0 → that bin is omitted from chart data (line breaks via connectNulls={false}).
    - Given a bin with ci_low/ci_high non-null → ciError = [delta - clampDeltaCi(ci_low), clampDeltaCi(ci_high) - delta], both offsets non-negative.
    - Given a bin with null ci_low or ci_high → ciError is undefined (no whisker).
    - Neutral band is the fixed symmetric [-0.06, +0.06] for all 4 x positions (3 flat ReferenceArea bands).
    - Y domain is fixed [-PRESSURE_DELTA_DOMAIN, PRESSURE_DELTA_DOMAIN].
    - Dot color comes from pressureDeltaZoneColor(delta, PRESSURE_SCORE_GAP_NEUTRAL_MIN, PRESSURE_SCORE_GAP_NEUTRAL_MAX).
  </behavior>
  <action>
    Create `frontend/src/components/charts/ScoreGapByTimePressureChart.tsx` following the
    PATTERNS.md analog exactly. Imports: `ComposedChart, Line, ReferenceArea, ReferenceLine,
    XAxis, YAxis, ErrorBar` from `recharts`; `ChartContainer, ChartTooltip` from
    `@/components/ui/chart`; `ZONE_DANGER, ZONE_NEUTRAL, ZONE_SUCCESS` from `@/lib/theme`;
    `PRESSURE_DELTA_DOMAIN, clampDeltaCi, pressureDeltaZoneColor` from
    `@/lib/pressureBulletConfig`; `type PressureQuintileBullet` from `@/types/endgames`.
    Do NOT import `Bar`, `CartesianGrid`, or inactivity helpers.

    Declare these named constants (no magic numbers), each with the benchmark-citation
    comment from PATTERNS.md lines 64–74:
      - `ZONE_OPACITY = 0.15` (local; comment "Matches EndgameClockDiffOverTimeChart")
      - `export const PRESSURE_SCORE_GAP_NEUTRAL_MIN = -0.06`
      - `export const PRESSURE_SCORE_GAP_NEUTRAL_MAX = 0.06`
        (both with the comment block citing reports/benchmarks-latest.md §3.3.3.b opp-quintile
        rerun 2026-05-17 + the TC-collapse decision — REQUIRED, do not omit the citation)
      - `Y_DOMAIN: [number, number] = [-PRESSURE_DELTA_DOMAIN, PRESSURE_DELTA_DOMAIN]`
      - `MAX_VISIBLE_QUINTILE_INDEX = 3` (the Q4 filter threshold; named, not bare 3)
      - `PRESSURE_LABELS: Record<0|1|2|3, string>` redeclared locally with values
        '0-20% Time', '20-40% Time', '40-60% Time', '60-80% Time' (RESEARCH.md lines 210–220;
        redeclare here rather than coupling to the card — keeps the chart self-contained per
        the "build independently" anti-pattern guidance, RESEARCH.md line 450).

    `zoneDotColor(delta)` helper: `return pressureDeltaZoneColor(delta,
    PRESSURE_SCORE_GAP_NEUTRAL_MIN, PRESSURE_SCORE_GAP_NEUTRAL_MAX)`.

    Data transform (PATTERNS.md lines 85–113): filter `quintile_index <=
    MAX_VISIBLE_QUINTILE_INDEX`, then filter `n > 0`, then map to a `ChartPoint` typed
    object: `{ label: PRESSURE_LABELS[bin.quintile_index as 0|1|2|3] ?? '', delta, n,
    opp_score, p_value, ciError?: [number, number] }`. `ciError` = `bin.ci_low != null &&
    bin.ci_high != null ? [bin.delta - clampDeltaCi(bin.ci_low), clampDeltaCi(bin.ci_high)
    - bin.delta] as [number, number] : undefined`. Type `ciError` explicitly as
    `[number, number] | undefined` — never `any` (anti-pattern, RESEARCH.md line 451).

    Render the ComposedChart skeleton per PATTERNS.md lines 116–243: root `<div>` with
    `data-testid={`score-gap-by-time-pressure-chart-${tc}`}`, `role="img"`,
    `aria-label={`Score gap by time pressure — ${tc}`}` (use a hyphen, not an em-dash, in
    user-facing aria copy per CLAUDE.md). `ChartContainer config={{}} className="w-full h-44"`
    with `data-testid="score-gap-by-time-pressure-chart-container"`. Inside the
    ComposedChart: 3 `ReferenceArea` (danger Y_DOMAIN[0]→MIN, neutral MIN→MAX, success
    MAX→Y_DOMAIN[1], each `fill` = the matching ZONE_* constant, `fillOpacity={ZONE_OPACITY}`,
    `ifOverflow="visible"`, `yAxisId="value"`); `XAxis dataKey="label" type="category"`;
    `YAxis yAxisId="value" domain={Y_DOMAIN} width={44}` with `tickFormatter` =
    `(v) => v > 0 ? `+${(v*100).toFixed(0)}%` : `${(v*100).toFixed(0)}%``;
    `ReferenceLine yAxisId="value" y={0}` dashed (`strokeDasharray="3 3"`,
    `strokeOpacity={0.4}`, `stroke="currentColor"`); `ChartTooltip` with the custom content
    from PATTERNS.md lines 215–242 (root `data-testid="score-gap-tooltip"`, `text-xs` is
    permitted here under the CLAUDE.md hover-tooltip exception per PATTERNS.md decision #4 —
    use `text-xs` to match the EndgameClockDiffOverTimeChart tooltip convention; show bucket
    label, colored swatch, "Score gap: {signed%}", "vs opponents at {oppPct}", "n = {n}");
    `Line yAxisId="value" type="monotone" dataKey="delta" stroke="white" strokeWidth={2}
    connectNulls={false} isAnimationActive={false}` with the custom zone-colored `dot`
    render (PATTERNS.md lines 191–212, narrow cx/cy/payload, keyed `<g/>` fallback) and an
    `ErrorBar dataKey="ciError" width={4} stroke="currentColor" strokeOpacity={0.5}
    direction="y"` child.

    Export `ScoreGapByTimePressureChartProps` and the `ScoreGapByTimePressureChart`
    function per the interface contract above. Apply `noUncheckedIndexedAccess` narrowing
    on the `PRESSURE_LABELS` access (cast to `0|1|2|3` only after the `<= 3` filter, with
    `?? ''`). Keep the component within function-size limits — the declarative Recharts
    return tree does not count toward logic LOC; if the data transform + helpers exceed the
    soft limit, extract the transform into a module-level `toChartData(quintiles)` helper
    rather than fragmenting the JSX.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run ScoreGapByTimePressureChart</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run ScoreGapByTimePressureChart` exits 0 (all 8 scaffold scenarios green).
    - `ScoreGapByTimePressureChart.tsx` contains `<ErrorBar`.
    - `ScoreGapByTimePressureChart.tsx` contains exactly 3 `<ReferenceArea` elements.
    - `ScoreGapByTimePressureChart.tsx` exports `PRESSURE_SCORE_GAP_NEUTRAL_MIN` = -0.06 and `PRESSURE_SCORE_GAP_NEUTRAL_MAX` = 0.06 with a comment citing benchmarks-latest.md §3.3.3.b.
    - `ScoreGapByTimePressureChart.tsx` imports `ZONE_DANGER`, `ZONE_NEUTRAL`, `ZONE_SUCCESS` from `@/lib/theme` (no hard-coded semantic colors).
    - `grep -n "text-xs\|text-sm" ScoreGapByTimePressureChart.tsx` shows sub-text-sm only inside the tooltip content node.
    - `cd frontend && npx tsc --noEmit` reports no error in `ScoreGapByTimePressureChart.tsx` (no `any` on `ciError`).
  </acceptance_criteria>
  <done>The component renders 4 zone-banded datapoints with ErrorBar whiskers and a hover tooltip; the Wave 0 test file is fully green; types pass; theme constants reused; no magic numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a client-side rendering component over data already fetched and authorized by the existing endgame stats endpoint. No new network calls, no new endpoint, no new user input beyond hover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.4-01 | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH Package Legitimacy Audit: recharts ^2.15.4 already present, ErrorBar verified in node_modules). No install task; T-{phase}-SC checkpoint not applicable. |
| T-88.4-02 | Information Disclosure | ScoreGapByTimePressureChart render | accept | Renders only fields already present in the authorized `PressureQuintileBullet` API response (delta, ci, n, opp_score). No internal hashes, no new fields, no PII. Reshape of already-exposed data; risk unchanged from Phase 88. |

This is a pure client-side reshape of already-fetched, already-authorized data. No new auth surface, no new data exposure, no untrusted input. Threat model is legitimately minimal; no high-severity threats.
</threat_model>

<verification>
- `cd frontend && npm test -- --run ScoreGapByTimePressureChart` exits 0.
- `cd frontend && npx tsc --noEmit` reports no new errors.
- Component file reuses `@/lib/theme` ZONE_* constants and `@/lib/pressureBulletConfig` helpers (no hard-coded semantic colors, no magic numbers).
</verification>

<success_criteria>
SC-3 (chart component portion): a single horizontal zone-banded 0-centered line chart over
the 4 ordinal pressure-bucket labels, with CI whiskers and a per-bucket hover tooltip,
exists as a self-contained tested component, ready for card integration in Plan 02.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-01-SUMMARY.md` when done.
</output>
