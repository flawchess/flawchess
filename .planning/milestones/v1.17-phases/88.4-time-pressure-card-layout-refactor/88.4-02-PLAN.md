---
phase: 88.4-time-pressure-card-layout-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/EndgameTimePressureSection.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "When exactly 1 TC card is shown the section renders it at half width (w-1/2), not a grid"
    - "When 2 TC cards are shown the grid is full-width 2 columns"
    - "When 3 TC cards are shown the grid is full-width 3 columns"
    - "When 4 TC cards are shown the grid is 2x2 on mid breakpoint and 4x1 at xl"
    - "grandTotal still propagates to every EndgameTimePressureCard unchanged"
  artifacts:
    - path: "frontend/src/components/charts/EndgameTimePressureSection.tsx"
      provides: "Dynamic card-count-driven grid layout"
      contains: "data.cards.length"
    - path: "frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx"
      provides: "SC-1 dynamic grid layout assertions"
      contains: "grid-cols-3"
  key_links:
    - from: "EndgameTimePressureSection.tsx"
      to: "data.cards.length"
      via: "visibleCount drives gridClass selection"
      pattern: "data\\.cards\\.length"
---

<objective>
Refactor `EndgameTimePressureSection.tsx` (SC-1): replace the fixed
`grid-cols-1 lg:grid-cols-2 xl:grid-cols-4` grid with a dynamic layout driven by the
visible card count (`data.cards.length`, which the backend pre-filters by
`MIN_GAMES_PER_TC_CARD`): 1 card → half width, 2 → 2-col, 3 → 3-col, 4 → 2×2/4×1. Update
the section test for the four grid cases.

Purpose: Eliminate the always-4-column layout that leaves empty cells and squeezes cards
when fewer TCs are visible.

Output: modified `EndgameTimePressureSection.tsx` + updated section test.

This plan is independent of Plans 01 and 03 (different file, no overlap) — Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-RESEARCH.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-PATTERNS.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-1 dynamic grid in EndgameTimePressureSection.tsx</name>
  <read_first>
    - 88.4-PATTERNS.md lines 504–541 (SC-1 dynamic grid pattern, existing grid source lines 40–50, the grandTotal computation, the gridClass ternary, the breakpoint-discretion note)
    - 88.4-RESEARCH.md lines 76–101 (Q1 parent section anatomy, grandTotal across ALL cards including sub-threshold, SC-1 grid logic, breakpoint discretion A1)
    - 88.4-RESEARCH.md lines 605–608 + 614–617 (Assumption A1 + Open Question 1 — adopt: w-1/2 left-aligned, no extra alignment wrapper; Phase 89 refines if centering preferred)
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (the file being modified — current grid + grandTotal + map)
    - frontend/src/components/charts/EndgameTypeBreakdownSection.tsx and EndgameMetricsSection.tsx (grid analog references for breakpoint conventions)
  </read_first>
  <action>
    In `EndgameTimePressureSection.tsx`, in the non-empty branch, compute
    `const visibleCount = data.cards.length` (backend pre-filters by MIN_GAMES_PER_TC_CARD,
    so this is the visible count — do NOT attempt to detect null-returning children;
    that anti-pattern is called out in RESEARCH.md line 452). Keep the existing
    `grandTotal = data.cards.reduce((acc, c) => acc + c.total, 0)` computation unchanged
    (it must still sum across all cards and propagate to every card).

    Replace the hard-coded grid `<div className="grid grid-cols-1 lg:grid-cols-2
    xl:grid-cols-4 gap-4 mt-2">` with a `gridClass` selected by `visibleCount` per
    PATTERNS.md lines 522–529:
      - 1  → `'w-1/2 mt-2'`        (single card, half width, default left-aligned flow)
      - 2  → `'grid grid-cols-2 gap-4 mt-2'`
      - 3  → `'grid grid-cols-1 sm:grid-cols-3 gap-4 mt-2'`
      - else (4) → `'grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-4 mt-2'`
    Extract the four class strings into named module-level constants (e.g.
    `GRID_ONE_CARD`, `GRID_TWO_CARDS`, `GRID_THREE_CARDS`, `GRID_FOUR_CARDS`) to satisfy
    the no-magic-strings / no-magic-numbers rule and keep the ternary readable; the
    `visibleCount` thresholds (1/2/3) are layout breakpoints — name them or comment them.
    Apply `gridClass` to the wrapper `<div className={gridClass}>` that maps over
    `data.cards`. Do not change the `data-testid` on the section wrapper or the per-card
    props. Preserve the description `<p>` position so the existing
    `[data-testid="time-pressure-cards-section"] > p + div` test selector still resolves
    (PATTERNS.md line 651).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameTimePressureSection</automated>
  </verify>
  <acceptance_criteria>
    - `EndgameTimePressureSection.tsx` references `data.cards.length` to choose the grid class.
    - The four grid class strings are extracted to named constants (no inline ternary string literals).
    - `grandTotal` reduce over `data.cards` is unchanged and still passed to every `EndgameTimePressureCard`.
    - `npm test -- --run EndgameTimePressureSection` exits 0.
  </acceptance_criteria>
  <done>Section grid is card-count driven; existing grandTotal propagation + section wrapper testids unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: SC-1 grid assertions in EndgameTimePressureSection.test.tsx</name>
  <read_first>
    - 88.4-PATTERNS.md lines 614–651 (the SC-1 grid test assertions block, the `> p + div` selector note, the list of describe blocks that must remain valid)
    - 88.4-VALIDATION.md lines 40–43 (SC-1 verification map entry)
    - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx (the file being modified — existing makePayload helper, grandTotal propagation describe, section wrapper describe)
  </read_first>
  <action>
    Add a `describe('EndgameTimePressureSection — SC-1: dynamic grid layout', ...)` block
    with four cases per PATTERNS.md lines 620–646:
      1. 1-card payload → wrapper `className` contains `w-1/2` and does NOT contain `grid`.
      2. 2-card payload → wrapper `className` contains `grid-cols-2`.
      3. 3-card payload → wrapper `className` contains `grid-cols-3`.
      4. 4-card payload → wrapper `className` contains `grid-cols-4`.
    Use the existing `makePayload(...)` fixture helper (vary the number of TCs:
    `'bullet'`; `'bullet','blitz'`; `'bullet','blitz','rapid'`;
    `'bullet','blitz','rapid','classical'`) and the
    `container.querySelector('[data-testid="time-pressure-cards-section"] > p + div')`
    selector. Verify the selector resolves after the Task 1 refactor; if the description
    `<p>` structure changed, adjust the selector accordingly and note it. Leave the
    existing `grandTotal propagation`, `TC card visibility`, `Empty state`, `All 4 TCs`,
    `Legacy absence`, and `Section wrapper` describe blocks unchanged (PATTERNS.md
    line 649 — still valid).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameTimePressureSection</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains a `describe` titled with "SC-1: dynamic grid layout".
    - That describe contains 4 `it(` cases asserting `w-1/2`, `grid-cols-2`, `grid-cols-3`, `grid-cols-4` respectively.
    - The pre-existing `grandTotal propagation` describe block is unmodified.
    - `npm test -- --run EndgameTimePressureSection` exits 0.
  </acceptance_criteria>
  <done>All four grid layouts are asserted; pre-existing section behaviors still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side CSS-grid layout change in a section orchestrator. No network, no endpoint, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.4-03 | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan. |
| T-88.4-04 | Denial of Service | grid render at extreme card counts | accept | Card count is bounded to ≤4 (four time-control buckets, backend pre-filtered). No unbounded render. |

Pure CSS-grid reshape of an already-rendered section. No new auth surface, no new data exposure, no untrusted input. Threat model legitimately minimal; no high-severity threats.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameTimePressureSection` exits 0.
- Grid class is selected from `data.cards.length`, not hard-coded.
</verification>

<success_criteria>
SC-1: responsive card grid — full-width 2/3/4-column for ≥2 cards, half-width for exactly
1 card, driven by visible card count, with the four grid cases automated-tested.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-02-SUMMARY.md` when done.
</output>
