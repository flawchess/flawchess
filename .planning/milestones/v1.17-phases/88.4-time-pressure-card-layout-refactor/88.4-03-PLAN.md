---
phase: 88.4-time-pressure-card-layout-refactor
plan: 03
type: execute
wave: 2
depends_on: ["88.4-01"]
files_modified:
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - "A 3-column header row (You left / Gap+info center / Opp right) renders directly ABOVE the Clock Gap bullet chart"
    - "The Clock Gap info icon (MetricStatPopover) lives in the centered Gap cell and opens the existing popover unchanged"
    - "The Net flag rate row stays BELOW the Clock Gap bullet, unchanged"
    - "The four stacked per-bucket Score-Gap bullet charts are gone, replaced by one ScoreGapByTimePressureChart"
    - "No per-bucket bullet/info testids remain; knip is clean (no orphaned exports/imports)"
  artifacts:
    - path: "frontend/src/components/charts/EndgameTimePressureCard.tsx"
      provides: "Refactored card: 3-stat header row + integrated score-gap line chart; QuintileRow/EmptyBinRow removed"
      contains: "ScoreGapByTimePressureChart"
    - path: "frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx"
      provides: "Updated SC-2/SC-3 assertions; obsolete bullet-row describe blocks removed"
      contains: "SC-2: 3-column header row"
  key_links:
    - from: "EndgameTimePressureCard.tsx"
      to: "ScoreGapByTimePressureChart"
      via: "import and render with quintiles + tc props"
      pattern: "ScoreGapByTimePressureChart"
    - from: "EndgameTimePressureCard.tsx ClockGapHeaderRow"
      to: "MetricStatPopover"
      via: "Clock Gap popover migrated into centered Gap cell"
      pattern: "MetricStatPopover"
---

<objective>
Refactor `EndgameTimePressureCard.tsx` for SC-2 and SC-3:

- **SC-2:** Replace the "Clock Gap: X%" label line and the `ThreeStatRow` (below the
  bullet) with a new `ClockGapHeaderRow` rendered ABOVE the Clock Gap bullet:
  "You: X% (Ns)" left / "Gap: ±X% <info>" centered (the existing Clock Gap
  `MetricStatPopover` migrates here, font-tinting preserved) / "Opp: X% (Ns)" right.
  Extract the surviving "Net flag rate:" item into a slim `NetFlagRateRow` that stays
  below the bullet, unchanged.
- **SC-3 integration:** Delete `QuintileRow` and `EmptyBinRow`; replace the four stacked
  Score-Gap bullets with `<ScoreGapByTimePressureChart quintiles={card.quintiles}
  tc={card.tc} />` (from Plan 01). Remove now-orphaned imports so knip stays clean.

Update `EndgameTimePressureCard.test.tsx`: delete obsolete bullet-row describe blocks,
add SC-2 header-row + SC-3 chart-integration assertions.

Purpose: Collapse the 6-undifferentiated-rows density problem into a scannable header row
plus one trend chart.

Output: modified `EndgameTimePressureCard.tsx` + updated card test.

Depends on Plan 01 (`ScoreGapByTimePressureChart` must exist) — Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-RESEARCH.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-PATTERNS.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-VALIDATION.md
@.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-01-SUMMARY.md

<interfaces>
From Plan 01 — frontend/src/components/charts/ScoreGapByTimePressureChart.tsx:
  export interface ScoreGapByTimePressureChartProps {
    quintiles: PressureQuintileBullet[];
    tc: 'bullet' | 'blitz' | 'rapid' | 'classical';
  }
  export function ScoreGapByTimePressureChart(props): JSX.Element

Clock Gap MetricStatPopover call to migrate (verbatim props, 88.4-PATTERNS.md lines 681–697):
  name="Clock Gap", explanation=<…>, value={gap.mean_diff_pct}, baseline={0},
  unit="percent", gameCount={gap.n}, level={level}, pValue={gap.p_value},
  vocabulary="score", neutralLower={neutralMin}, neutralUpper={neutralMax},
  baselineLabel="0%", methodology={<…>},
  testId={`time-pressure-card-${tc}-clock-gap-info`}, ariaLabel="What is Clock Gap?"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-2 — ClockGapHeaderRow + NetFlagRateRow refactor</name>
  <read_first>
    - 88.4-PATTERNS.md lines 382–501 (current layout order, new ClockGapHeaderRow pattern, slim NetFlagRateRow pattern, testid naming/migration table, imports-to-remove list)
    - 88.4-PATTERNS.md lines 677–698 (the exact MetricStatPopover props to migrate verbatim)
    - 88.4-RESEARCH.md lines 514–540 (CRITICAL FLAG: SC-2 layout interpretation — before/after structure)
    - 88.4-RESEARCH.md lines 624–628 (Open Question 3 — adopt: preserve the existing ClockGapRow font-tinting triple-gate in the new Gap cell)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (the file being modified — ClockGapRow lines ~113–168, ThreeStatRow lines ~349–397, font-tinting lines ~95–107, formatPctSecs helper)
  </read_first>
  <action>
    In `EndgameTimePressureCard.tsx`, create a `ClockGapHeaderRow({ gap, card })`
    sub-component per PATTERNS.md lines 405–447. It renders a
    `grid grid-cols-3 items-center text-sm tabular-nums mb-2` with
    `data-testid={`time-pressure-card-${card.tc}-clock-gap-header`}`:
      - Left cell: `You: <semibold>{formatPctSecs(card.user_avg_pct,
        card.user_avg_seconds)}</semibold>`, `data-testid=…-my-avg-time`.
      - Center cell: `Gap:` label + the signed `gap.mean_diff_pct` value
        (`data-testid=…-clock-gap-value`) + the migrated Clock Gap `MetricStatPopover`
        (verbatim props from the interfaces block / PATTERNS.md lines 681–697,
        `testId=…-clock-gap-info`, `ariaLabel="What is Clock Gap?"`).
      - Right cell: `Opp: <semibold>{formatPctSecs(card.opp_avg_pct,
        card.opp_avg_seconds)}</semibold>`, `data-testid=…-opp-avg-time`.
    Preserve the existing font-tinting triple-gate from `ClockGapRow` (n >=
    MIN_GAMES_PER_PRESSURE_BIN AND isConfident(level) AND outside the neutral band →
    ZONE_SUCCESS / ZONE_DANGER applied to the Gap value's inline `color`, PATTERNS.md
    lines 409–417). Reuse `CLOCK_GAP_NEUTRAL_MIN/MAX`, `deriveLevel`, `isConfident`,
    `formatPctSecs` already in the file. Keep `text-sm` (no `text-xs`) for this header row
    (it is primary content, not a tooltip).

    Create a slim `NetFlagRateRow({ card })` (PATTERNS.md lines 449–472) holding only the
    surviving "Net flag rate:" span with its `tintForNetTimeoutRate` tint and the
    `InfoPopover` (`data-testid` `…-net-flag-rate` and `…-net-flag-rate-info`, wrapper
    `…-net-flag-rate-row`). Delete the "You:" / "Opponents:" spans from `ThreeStatRow`;
    either delete `ThreeStatRow` entirely in favor of `NetFlagRateRow` or rename+strip it
    (planner decision: delete `ThreeStatRow`, introduce `NetFlagRateRow` — cleaner; update
    any `top-stats` testid references in the same pass per PATTERNS.md decision #3).

    Restructure the "Remaining Time at Endgame Entry" section body so the order is:
    subtitle → `<ClockGapHeaderRow>` → the existing Clock Gap `MiniBulletChart` (unchanged,
    keep `data-testid` `…-clock-gap-bullet`) → `<NetFlagRateRow>`. Remove the old
    `ClockGapRow` "Clock Gap: X%" label span (its popover moved into the header). Do not
    alter the `MiniBulletChart` primitive itself (out of scope per ROADMAP).
    Use commas/periods (not em-dashes) in any user-facing copy touched.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameTimePressureCard</automated>
  </verify>
  <acceptance_criteria>
    - `EndgameTimePressureCard.tsx` contains a `ClockGapHeaderRow` with `grid grid-cols-3` and `data-testid` ending `-clock-gap-header`.
    - The Clock Gap `MetricStatPopover` (testId `…-clock-gap-info`) is rendered inside the centered Gap cell, not in a separate label row.
    - A `NetFlagRateRow` exists; the "You:"/"Opponents:" spans no longer appear below the bullet.
    - The Clock Gap `MiniBulletChart` keeps `data-testid` `…-clock-gap-bullet` and renders between the header row and the net-flag row.
    - Font-tinting triple-gate (n / isConfident / neutral-band) is applied to the Gap value color.
    - `npm test -- --run EndgameTimePressureCard` exits 0 (after Task 3 test update; this task may leave RED until Task 3).
  </acceptance_criteria>
  <done>The 3-stat header row sits above the Clock Gap bullet; Net flag rate stays below; Clock Gap popover migrated with font-tinting preserved.</done>
</task>

<task type="auto">
  <name>Task 2: SC-3 — integrate ScoreGapByTimePressureChart, delete QuintileRow/EmptyBinRow, knip-clean imports</name>
  <read_first>
    - 88.4-PATTERNS.md lines 475–501 (QuintileRow/EmptyBinRow deletion, the chart container replacement snippet, the exact imports-to-remove vs keep list, deleted testid list)
    - 88.4-RESEARCH.md lines 334–362 (Q6 knip-clean requirement, the at-risk imports, the testid deletion list)
    - 88.4-01-PLAN.md (the ScoreGapByTimePressureChart prop contract — quintiles + tc)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (the "Score Gap by Remaining Time" section body lines ~494–507, the import block)
  </read_first>
  <action>
    In `EndgameTimePressureCard.tsx`: delete the `QuintileRow` and `EmptyBinRow`
    sub-components entirely, plus the module-private `pressureLabel()` helper and
    `PRESSURE_LABELS` constant if they become unused after deletion (the new chart
    redeclares its own labels per Plan 01 — do NOT export PRESSURE_LABELS from this file;
    Plan 01 keeps the chart self-contained). Replace the "Score Gap by Remaining Time"
    section body's quintile loop with a single wrapper:
    `<div data-testid={`time-pressure-card-${card.tc}-score-gap-chart`}>
       <ScoreGapByTimePressureChart quintiles={card.quintiles} tc={card.tc} />
     </div>` (PATTERNS.md lines 478–482). Add the import
    `import { ScoreGapByTimePressureChart } from './ScoreGapByTimePressureChart'`.

    Remove now-orphaned imports per PATTERNS.md lines 484–490 (verify each is truly unused
    after the deletions before removing — do not remove ones still used by
    `ClockGapHeaderRow`):
      - REMOVE `clampDeltaCi`, `PRESSURE_DELTA_DOMAIN`, `PRESSURE_DELTA_CENTER` from
        `@/lib/pressureBulletConfig` (moved into the chart component).
      - REMOVE `getPressureBinBand` from `@/generated/endgameZones` (only QuintileRow used it).
      - REMOVE `UNRELIABLE_OPACITY` from `@/lib/theme` (only QuintileRow dimming used it).
      - KEEP `isConfident` from `@/lib/significance` (still used by ClockGapHeaderRow font-tinting).
      - KEEP `Swords` from `lucide-react` (still used in the card header).
    The deleted per-bucket testids (`-bin-{n}`, `-bin-{n}-value`, `-bin-{n}-bullet`,
    `-bin-{n}-info`, `-bin-{n}-n`, `-bin-{n}-empty`) must no longer be emitted anywhere.
    Keep the card within function-size limits — if removing QuintileRow leaves the main
    component within limits, no extra split needed.
  </action>
  <verify>
    <automated>cd frontend && npm run knip && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `EndgameTimePressureCard.tsx` imports and renders `<ScoreGapByTimePressureChart` with `quintiles={card.quintiles}` and `tc={card.tc}`.
    - `QuintileRow` and `EmptyBinRow` no longer exist in the file (`grep -c "QuintileRow\|EmptyBinRow"` returns 0 ignoring comments).
    - No `-bin-` per-bucket testid string is emitted (`grep -n "bin-\${" EndgameTimePressureCard.tsx` returns nothing for the deleted patterns).
    - Removed imports (`clampDeltaCi`, `PRESSURE_DELTA_DOMAIN`, `PRESSURE_DELTA_CENTER`, `getPressureBinBand`, `UNRELIABLE_OPACITY`) are absent from the import block.
    - `cd frontend && npm run knip` exits 0 (no orphaned exports).
    - `cd frontend && npx tsc --noEmit` reports no error in `EndgameTimePressureCard.tsx`.
  </acceptance_criteria>
  <done>The four bullet rows are replaced by the line chart; QuintileRow/EmptyBinRow gone; knip + tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update EndgameTimePressureCard.test.tsx for SC-2 + SC-3</name>
  <read_first>
    - 88.4-PATTERNS.md lines 544–611 (testids-to-delete list, obsolete describe blocks to remove, SC-2/SC-3 describe blocks to add, the Plan 88-14 describe update note)
    - 88.4-VALIDATION.md lines 44–54 (SC-2/SC-3 per-task verification map entries)
    - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx (the file being modified — makeCard/renderCard helpers, the listed obsolete describe blocks at the cited line ranges)
  </read_first>
  <action>
    Delete the obsolete describe blocks that test removed elements (PATTERNS.md
    lines 560–566): "n=0 bin slot", "sparse bins (0 < n < MIN_GAMES_PER_PRESSURE_BIN)",
    "triple-gate font coloring" (the QuintileRow-value variant — the Clock Gap font-tint
    is now tested via the new header-row block instead), "Phase 88.1 opp-quintile rename",
    "Plan 88-13 A-4: Q4 row hidden", "quintile bucket labels". Remove every assertion
    referencing `-bin-{n}`, `-bin-{n}-value`, `-bin-{n}-bullet`, `-bin-{n}-info`,
    `-bin-{n}-n`, `-bin-{n}-empty` testids.

    Add `describe('EndgameTimePressureCard — SC-2: 3-column header row', ...)` with cases
    (PATTERNS.md lines 571–591): (a) `…-clock-gap-header` renders and its textContent
    contains "You", "Gap", "Opp"; (b) the Clock Gap `…-clock-gap-info` MetricStatPopover
    trigger is present in the header; (c) `…-net-flag-rate` still renders and its text
    contains "Net flag rate" (stays below, unchanged).

    Add `describe('EndgameTimePressureCard — SC-3: ScoreGapByTimePressureChart replaces
    bullet stack', ...)` with cases (PATTERNS.md lines 594–605): (a)
    `…-score-gap-chart` container renders; (b) `queryByTestId('…-bin-0-bullet')` and
    `queryByTestId('…-bin-0-info')` are null.

    Update the Plan 88-14 describe block (PATTERNS.md line 610) so any `top-stats` /
    `my-avg-time` / `opp-avg-time` assertions match the new `ClockGapHeaderRow` testids
    (`…-my-avg-time`, `…-opp-avg-time` now live in the header row; drop or relocate any
    `top-stats` wrapper assertion per the decision in PATTERNS.md #3). Use the
    `time-pressure-card-bullet-` prefix (tc='bullet' in makeCard). Leave all other still-valid
    describe blocks unchanged.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameTimePressureCard EndgameTimePressureSection ScoreGapByTimePressureChart</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains a describe titled with "SC-2: 3-column header row" with ≥3 `it(` cases.
    - Test file contains a describe titled with "SC-3: ScoreGapByTimePressureChart replaces bullet stack".
    - No assertion references any `-bin-` per-bucket testid (`grep -c "\-bin-" EndgameTimePressureCard.test.tsx` returns 0).
    - The 6 obsolete describe blocks listed in the action are removed.
    - `npm test -- --run EndgameTimePressureCard EndgameTimePressureSection ScoreGapByTimePressureChart` exits 0.
  </acceptance_criteria>
  <done>Card test reflects SC-2/SC-3; obsolete bullet-row describes removed; the full quick-run command is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side recomposition of an already-rendered card over already-authorized data. No network, no endpoint, no new user input beyond hover (handled in Plan 01's tooltip). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.4-05 | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan. |
| T-88.4-06 | Information Disclosure | ClockGapHeaderRow / chart integration | accept | Renders only fields already present in the authorized `TimePressureTcCard` / `PressureQuintileBullet` response. The MetricStatPopover content is migrated verbatim — no new data surfaced. Risk unchanged from Phase 88. |

Pure client-side card recomposition of already-fetched, already-authorized data. No new auth surface, no new data exposure, no untrusted input. Threat model legitimately minimal; no high-severity threats.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameTimePressureCard EndgameTimePressureSection ScoreGapByTimePressureChart` exits 0.
- `cd frontend && npm run knip` exits 0 (no orphaned per-bucket bullet exports/imports).
- `cd frontend && npx tsc --noEmit` reports no new errors.
- No `-bin-` per-bucket testids remain in card or card test.
</verification>

<success_criteria>
SC-2: 3-stat header row (You / Gap+info / Opp) above the Clock Gap bullet; Net flag rate
stays below, unchanged. SC-3 (integration): the four per-bucket bullets are replaced by
`ScoreGapByTimePressureChart`; QuintileRow/EmptyBinRow removed; knip clean.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88.4-time-pressure-card-layout-refactor/88.4-03-SUMMARY.md` when done.
</output>
