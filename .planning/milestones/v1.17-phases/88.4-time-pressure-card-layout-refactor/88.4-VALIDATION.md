---
phase: 88.4
slug: 88.4-time-pressure-card-layout-refactor
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-18
---

# Phase 88.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Frontend-only reshape. Requirement IDs are TBD (planner drafts them); SC-1/SC-2/SC-3 from the ROADMAP are used as the requirement proxies until then.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest + @testing-library/react |
| **Config file** | `frontend/vite.config.ts` (vitest config embedded) |
| **Quick run command** | `npm test -- --run EndgameTimePressureCard EndgameTimePressureSection ScoreGapByTimePressureChart` |
| **Full suite command** | `npm test -- --run` |
| **Estimated runtime** | ~30 seconds (full frontend suite) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command
- **After every plan wave:** Run the full suite command
- **Before `/gsd:verify-work`:** Full suite must be green + `npm run knip` clean + `npm run lint` clean
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| SC | Behavior | Wave | Test Type | Automated Command | File | Status |
|----|----------|------|-----------|-------------------|------|--------|
| SC-1 | 1-card half-width; 2/3/4-card grid columns; no horizontal scroll | 1 | unit | `npm test -- --run EndgameTimePressureSection` | exists (update) | ⬜ pending |
| SC-1 | No horizontal scroll at any breakpoint | 1 | manual | N/A — real-browser layout | manual | ⬜ pending |
| SC-2 | 3-col header row (You left / Gap+popover center / Opp right) renders ABOVE Clock Gap bullet | 1 | unit | `npm test -- --run EndgameTimePressureCard` | exists (update) | ⬜ pending |
| SC-2 | Gap info icon opens existing `MetricStatPopover` | 1 | unit | `npm test -- --run EndgameTimePressureCard` | exists (update) | ⬜ pending |
| SC-2 | Net flag rate bullet stays BELOW Clock Gap bullet, unchanged | 1 | unit | `npm test -- --run EndgameTimePressureCard` | exists (update) | ⬜ pending |
| SC-3 | `ScoreGapByTimePressureChart` renders 4 datapoints (Q4 filtered) | 1 | unit | `npm test -- --run ScoreGapByTimePressureChart` | **Wave 0 new** | ⬜ pending |
| SC-3 | Renders 3 zone-band `ReferenceArea` elements | 1 | unit | `npm test -- --run ScoreGapByTimePressureChart` | **Wave 0 new** | ⬜ pending |
| SC-3 | Renders CI whisker (`ErrorBar`) for bins with non-null CI | 1 | unit | `npm test -- --run ScoreGapByTimePressureChart` | **Wave 0 new** | ⬜ pending |
| SC-3 | Zero-n bins → gap in line, no error bar | 1 | unit | `npm test -- --run ScoreGapByTimePressureChart` | **Wave 0 new** | ⬜ pending |
| SC-3 | Tooltip surfaces the per-bucket stats formerly in the 4 info icons | 1 | unit | `npm test -- --run ScoreGapByTimePressureChart` | **Wave 0 new** | ⬜ pending |
| SC-3 | No per-bucket bullet/info testids remain (`-bin-{n}-bullet`, `-bin-{n}-info`) | 1 | unit | `npm test -- --run EndgameTimePressureCard` | exists (update) | ⬜ pending |
| SC-3 | Neutral zone band constant is `[-0.06, +0.06]` for all 4 quintiles (TC-collapsed) | 1 | unit | `npm test -- --run ScoreGapByTimePressureChart` | **Wave 0 new** | ⬜ pending |
| SC-3 | knip clean — no orphaned per-bucket bullet exports | gate | CI | `npm run knip` | CI | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `frontend/src/components/charts/__tests__/ScoreGapByTimePressureChart.test.tsx` — new test file scaffolding SC-3 + zone-band + bucket-count assertions. Must reuse the `vi.mock('recharts')` stub pattern from `EndgameClockDiffOverTimeChart.test.tsx`.
- [ ] Update stubs in existing `EndgameTimePressureCard.test.tsx` / `EndgameTimePressureSection.test.tsx` for the SC-1 grid + SC-2 header-row reshape.
- Framework already installed (Vitest) — no install task.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| No horizontal scroll at any breakpoint; 1/2/3/4-card grid reads correctly | SC-1 | Layout/overflow needs a real browser viewport; jsdom has no real layout engine | Load `/endgames/stats` with 1, 2, 3, 4 TC cards visible (vary the TC filter); confirm full-width grid for ≥2, half-width for 1, no horizontal scrollbar at desktop + 375px |
| Tooltip visual legibility + line-chart readability inside the narrow card | SC-3 | Visual judgment (chart height, whisker rendering, zone-band contrast) | Hover each of the 4 datapoints; confirm stats match the old info-icon contents; eyeball chart height against `screenshots/tc-cards.png` |

*375px full sweep is deferred to Phase 89 — only basic correctness is gated here.*

---

## Validation Sign-Off

- [ ] All SCs have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers the new `ScoreGapByTimePressureChart.test.tsx`
- [ ] No watch-mode flags (always `--run`)
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter (planner sets once per-task map is finalized)

**Approval:** pending
