---
phase: 88.4-time-pressure-card-layout-refactor
verified: 2026-05-19T00:35:00Z
status: human_needed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual inspection — responsive card grid at multiple breakpoints"
    expected: "1 card: half-width left-aligned; 2 cards: 2-column full-width; 3 cards: 3-column; 4 cards: 2x2 on md, 4x1 on xl. No horizontal scroll at any breakpoint."
    why_human: "CSS grid layout correctness across breakpoints cannot be verified with grep; requires browser at each viewport width."
  - test: "SC-2 header row visual layout — 3-column You/Gap/Opp row above Clock Gap bullet"
    expected: "'You: X% (Ns)' left-aligned; 'Gap: ±X% <info>' centered with the MetricStatPopover icon; 'Opp: X% (Ns)' right-aligned. Font-tinting applies to the Gap value when outside the neutral band AND n sufficient AND confident."
    why_human: "The header row uses grid-cols-3 and inline style color; pixel-level alignment and tinting can only be confirmed in a real browser."
  - test: "SC-3 line chart visual — zone bands, CI whiskers, and hover tooltip"
    expected: "4 ordinal x-axis datapoints (Q0–Q3); three zone bands (danger/neutral/success at ±0.06); CI whisker on each non-null-CI point; hover tooltip shows signed score gap %, opponent score %, and n."
    why_human: "Recharts rendering behavior (tooltip activation, whisker SVG positions, zone band fill) requires a browser interaction test; jsdom stubs bypass the real render."
  - test: "WR-01: text-xs in chart tooltip — decision on whether to extend or fix"
    expected: "Either: tooltip body is changed to text-sm (CLAUDE.md floor), OR the user explicitly accepts the Recharts chart tooltip exception and adds a CLAUDE.md amendment or inline comment so future reviewers do not re-flag."
    why_human: "This is a policy decision that only the user (Adrian) can make. The plan sanctioned text-xs citing a hover-tooltip exception, but CLAUDE.md's exception is narrowly scoped to Radix popover bodies. No code change blocks the phase; user sign-off is needed on the deviation."
---

# Phase 88.4: Time Pressure Card Layout Refactor — Verification Report

**Phase Goal:** Time Pressure card layout refactor — responsive card grid (3x1/2x2 full-width, half-width when 1 card); "Remaining Time at Endgame Entry" 3-stat header row (You left / Gap+info centered / Opp right) above the Clock Gap bullet, Net flag rate stays below; "Score Gap by Remaining Time" 4 bullet charts replaced by one zone-banded 0-centered line chart over the 4 pressure-bucket labels with CI whiskers + per-bucket stats in the hover tooltip (neutral zone collapsed across TC). Frontend-only, no stats/benchmark change.
**Verified:** 2026-05-19T00:35:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Responsive card grid: full-width 2/3/4-col for ≥2 cards; half-width for 1 card | VERIFIED | `EndgameTimePressureSection.tsx` L50-61: `visibleCount` derived from `data.cards.length`; 4 named grid constants; section test SC-1 describe with 4 passing assertions (`w-1/2` / `grid-cols-2` / `grid-cols-3` / `grid-cols-4`). 11/11 section tests pass. |
| SC-2 | "Remaining Time at Endgame Entry" 3-stat header row: You left / Gap+info centered / Opp right; Net flag rate stays below | VERIFIED | `EndgameTimePressureCard.tsx` L91-154: `ClockGapHeaderRow` with `grid grid-cols-3`; MetricStatPopover inside centered Gap cell (L125); `NetFlagRateRow` L160-192 below bullet. Card test SC-2 describe 3 cases passing. 19/19 card tests pass. |
| SC-3 | 4 per-bucket bullet charts replaced by one zone-banded 0-centered line chart with CI whiskers and hover tooltip; knip clean | VERIFIED | `ScoreGapByTimePressureChart.tsx`: 3 `ReferenceArea` zone bands; `ErrorBar` child of `Line`; `data-testid="score-gap-tooltip"`; `PRESSURE_SCORE_GAP_NEUTRAL_MIN=-0.06` / `_MAX=0.06` exported. Card wires it: L308. `QuintileRow`/`EmptyBinRow` absent. `npm run knip` exits 0. 9/9 chart tests pass. |

**Score:** 3/3 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/components/charts/ScoreGapByTimePressureChart.tsx` | Self-contained ComposedChart score-gap component | VERIFIED | 249 lines; contains `ErrorBar`, 3 `ReferenceArea`, exports `PRESSURE_SCORE_GAP_NEUTRAL_MIN/MAX`; imports `ZONE_DANGER/NEUTRAL/SUCCESS` from theme; no hardcoded semantic colors |
| `frontend/src/components/charts/__tests__/ScoreGapByTimePressureChart.test.tsx` | Wave 0 SC-3 test scaffold (9 tests) | VERIFIED | Contains `vi.mock('recharts'`, `@vitest-environment jsdom`; 9 tests including constant assertions, zone band count, dot count, ErrorBar count, zero-n gap, tooltip testid |
| `frontend/src/components/charts/EndgameTimePressureSection.tsx` | Dynamic card-count-driven grid | VERIFIED | L19-22: 4 named grid constants; L50: `visibleCount = data.cards.length`; ternary selects from constants |
| `frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx` | SC-1 dynamic grid assertions | VERIFIED | SC-1 describe block at L213 with 4 cases asserting `w-1/2`, `grid-cols-2`, `grid-cols-3`, `grid-cols-4` |
| `frontend/src/components/charts/EndgameTimePressureCard.tsx` | Refactored card: ClockGapHeaderRow + ScoreGapByTimePressureChart; QuintileRow/EmptyBinRow removed | VERIFIED | `ClockGapHeaderRow` (L91) + `NetFlagRateRow` (L160); imports `ScoreGapByTimePressureChart` (L19) and renders (L308); `QuintileRow`/`EmptyBinRow` only in comment line 10 (not as functions) |
| `frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` | Updated SC-2/SC-3 assertions; obsolete bullet-row blocks removed | VERIFIED | SC-2 describe (L159), SC-3 describe (L183); 6 obsolete describes confirmed absent; no `-bin-` testid references except in SC-3 null assertions |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ScoreGapByTimePressureChart.tsx` | `@/lib/pressureBulletConfig` | `import clampDeltaCi, pressureDeltaZoneColor, PRESSURE_DELTA_DOMAIN` | WIRED | L27-30 confirmed |
| `ScoreGapByTimePressureChart.tsx` | `@/lib/theme` | `import ZONE_DANGER, ZONE_NEUTRAL, ZONE_SUCCESS` | WIRED | L25 confirmed; used in 3 ReferenceArea fills |
| `EndgameTimePressureSection.tsx` | `data.cards.length` | `visibleCount` drives gridClass selection | WIRED | L50 confirmed |
| `EndgameTimePressureCard.tsx` | `ScoreGapByTimePressureChart` | import + render with `quintiles + tc` props | WIRED | L19 (import), L308 (render with `quintiles={card.quintiles} tc={card.tc}`) |
| `EndgameTimePressureCard.tsx ClockGapHeaderRow` | `MetricStatPopover` | Clock Gap popover in centered Gap cell | WIRED | L125-147 confirmed; `testId` pattern matches expected |

---

### Data-Flow Trace (Level 4)

This is a pure frontend reshape of already-fetched data. The components consume `PressureQuintileBullet[]` and `TimePressureTcCard` from TanStack Query results (API response from the pre-existing endgame stats endpoint). No new data sources are introduced. Level 4 is not applicable — no new fetch paths or state sources.

---

### Behavioral Spot-Checks

| Behavior | Result | Status |
|----------|--------|--------|
| `npm test -- --run ScoreGapByTimePressureChart` | 9/9 tests PASS | PASS |
| `npm test -- --run EndgameTimePressureSection` | 11/11 tests PASS | PASS |
| `npm test -- --run EndgameTimePressureCard` | 19/19 tests PASS | PASS |
| `npm run knip` | exits 0 (no orphaned exports) | PASS |
| `npx tsc --noEmit` | no errors | PASS |

---

### Probe Execution

No probes declared in PLAN.md. No conventional `scripts/*/tests/probe-*.sh` for this frontend-only phase. Step 7c: SKIPPED.

---

### Requirements Coverage

No formal requirement IDs declared in PLAN frontmatter or REQUIREMENTS.md for this phase (ROADMAP notes "Requirements: TBD"). The three Success Criteria (SC-1, SC-2, SC-3) defined in the ROADMAP serve as the verification contract — all three are VERIFIED above.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ScoreGapByTimePressureChart.tsx` | 188 | `text-xs` in Recharts chart hover tooltip | WARNING | Violates CLAUDE.md minimum font-size floor (`text-sm`). The exception in CLAUDE.md covers Radix popover bodies (MetricStatPopover, WdlConfidenceTooltip, etc.) — not Recharts chart tooltips. Plan 01 explicitly instructed `text-xs` here; REVIEW.md WR-01 flagged it. No TBD/FIXME/XXX markers present. |
| `ScoreGapByTimePressureChart.tsx` | 84-99 | `as 0\|1\|2\|3` cast without `>= 0` lower bound | WARNING (advisory) | WR-03 from review: out-of-range `quintile_index` (e.g. negative or > 3 that slips past the `<= 3` filter) maps to `''` label instead of being dropped. Backend currently emits 0–4 so this is latent. Not a BLOCKER. |
| `ScoreGapByTimePressureChart.tsx` | 220-225 | `key` on dot uses `payload.label` (string) | WARNING (advisory) | WR-02 from review: duplicate labels (e.g. two bins mapping to `''`) would collide React keys. Backend data is well-formed currently. Not a BLOCKER. |

No `TBD`, `FIXME`, or `XXX` markers found in any phase-modified file. No unreferenced debt markers. No empty stubs.

---

### Human Verification Required

#### 1. Responsive card grid — visual correctness at real breakpoints

**Test:** Open the Endgame > Time Pressure section in a browser (dev server). Resize from mobile (375px) through tablet (768px) to desktop (1280px+). With 1, 2, 3, and 4 TC cards loaded, confirm grid adapts: 1 card = left half, 2 cards = 2-col, 3 cards = 3-col, 4 cards = 2x2 then 4x1 at xl. Confirm no horizontal scroll at any width.

**Expected:** Grid class switches correctly per card count; no overflow.

**Why human:** CSS breakpoint behavior cannot be verified with grep or jsdom; requires a real browser viewport.

---

#### 2. SC-2 header row visual layout and font-tinting

**Test:** In the browser, find a TC card where the Clock Gap metric is significant and outside the neutral band (e.g. mean_diff_pct > 0.06 or < -0.06 with sufficient n and low p-value). Verify: "You: X% (Ns)" is left-aligned, "Gap: ±X%" is centered with the info icon to its right, "Opp: X% (Ns)" is right-aligned. Verify the Gap value has the correct zone color (green for positive, red for negative). For a neutral / low-n case, confirm no color tint.

**Expected:** Grid-3-column layout renders correctly; font-tinting matches the triple-gate logic (n >= MIN_GAMES_PER_PRESSURE_BIN AND isConfident AND outside neutral band).

**Why human:** Inline `style={{ color }}` and CSS grid alignment require visual confirmation; the test only checks testid presence and text content.

---

#### 3. SC-3 line chart — zone bands, CI whiskers, and hover tooltip

**Test:** In the browser, look at the "Score Gap by Remaining Time" chart on any TC card. Confirm: 4 data points on the x-axis (0-20%, 20-40%, 40-60%, 60-80%); three colored zone bands visible; vertical CI whiskers on each point (or line gap where n=0); hover over a point and confirm the tooltip shows signed score gap %, opponent score %, and "n = X".

**Expected:** Chart matches the EndgameClockDiffOverTimeChart visual pattern; tooltip is readable and data-accurate.

**Why human:** Recharts tooltip activation on hover, whisker SVG geometry, and zone band fill require a real browser; jsdom stubs out ResponsiveContainer and the test uses a synthetic payload wrapper for tooltip content (IN-01 in REVIEW.md).

---

#### 4. WR-01 decision: text-xs in the chart hover tooltip

**Test:** Inspect the chart tooltip text size in a real browser at various DPI settings (standard + HiDPI). Decide: is the `text-xs` (12px) readability acceptable for this Recharts hover tooltip, or should it be changed to `text-sm` (14px)?

**Expected:** Either: (a) the tooltip is deemed readable and Adrian extends the CLAUDE.md exception to cover Recharts chart hover tooltips (add a comment at the use site); or (b) the class is changed to `text-sm` in `ScoreGapByTimePressureChart.tsx:188`.

**Why human:** This is a style policy decision that only the user can make. The plan sanctioned `text-xs` via PATTERNS.md decision #4, but CLAUDE.md's exception is narrowly scoped to Radix popover bodies. The reviewer flagged it as a warning; acceptance or remediation requires user sign-off.

---

### Gaps Summary

No BLOCKER gaps found. All three roadmap Success Criteria are verified in the codebase with passing tests, clean knip, and clean TypeScript. The phase goal is achieved.

Four items require human verification before the phase can be formally closed as `passed`:
1. Responsive grid visual correctness (mandatory SC-1 visual check)
2. Header row alignment and font-tinting (mandatory SC-2 visual check)
3. Line chart visual + tooltip interaction (mandatory SC-3 visual check)
4. WR-01 text-xs decision (policy choice on extending or fixing the CLAUDE.md exception)

Items 1-3 are standard end-of-phase human UAT checks for a visual frontend refactor. Item 4 is a policy decision the plan executor pre-empted; it needs user sign-off or a one-line fix.

---

_Verified: 2026-05-19T00:35:00Z_
_Verifier: Claude (gsd-verifier)_
