---
phase: 87.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/endgame_repository.py
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - tests/repositories/test_endgame_repository.py
  - tests/services/test_endgame_service.py
autonomous: true
requirements:
  - ROADMAP-87.1-§1   # 5 new fields on EndgameCategoryStats populated via compute_paired_difference_test
  - ROADMAP-87.1-§2   # Repo query surfaces next-span entry eval via LEAD()
  - ROADMAP-87.1-§3   # Codebase span definition (GROUP BY game_id, endgame_class) preserved
tags:
  - backend
  - endgame
  - schema
  - repository
  - service
must_haves:
  truths:
    - "query_endgame_entry_rows returns next_entry_eval_cp and next_entry_eval_mate per span via LEAD() window over (game_id, span_min_ply ASC)"
    - "_compute_span_gap returns exit_score - ES_entry, with terminal fallback to game result score and NULL-eval span skipped"
    - "EndgameCategoryStats carries 5 new fields populated by compute_paired_difference_test from app/services/score_confidence.py"
    - "Sign convention positive = user outperformed Stockfish baseline (matches higher_is_better direction on the new ZoneSpec)"
    - "Existing Conv/Recov per-class wiring unaffected by repo row-shape change"
  artifacts:
    - path: "app/repositories/endgame_repository.py"
      provides: "Extended query_endgame_entry_rows with span_min_ply + LEAD-based next-span eval"
      contains: "next_entry_eval_cp"
    - path: "app/services/endgame_service.py"
      provides: "_compute_span_gap helper + per-class gaps accumulator + compute_paired_difference_test call"
      contains: "_compute_span_gap"
    - path: "app/schemas/endgames.py"
      provides: "5 new fields on EndgameCategoryStats (type_achievable_score_gap_mean/_n/_p_value/_ci_low/_ci_high)"
      contains: "type_achievable_score_gap_mean"
    - path: "tests/services/test_endgame_service.py"
      provides: "Unit tests for _compute_span_gap + integration test for per-class aggregation"
      contains: "test_per_span_gap"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/services/score_confidence.py:338"
      via: "compute_paired_difference_test call inside _aggregate_endgame_stats per-class builder"
      pattern: "compute_paired_difference_test\\("
    - from: "app/services/endgame_service.py"
      to: "app/services/eval_utils.py"
      via: "eval_cp_to_expected_score + eval_mate_to_expected_score for ES_entry + exit_score"
      pattern: "(eval_cp|eval_mate)_to_expected_score"
    - from: "app/repositories/endgame_repository.py"
      to: "sqlalchemy.func.lead"
      via: "Window function over span_subq partitioned by game_id ordered by span_min_ply"
      pattern: "func\\.lead\\("
---

<!--
Requirement IDs reference ROADMAP §Phase 87.1 success criteria (REQUIREMENTS.md
has no DELTA-NN entries for Phase 87.1 yet — RESEARCH §REQUIREMENTS.md Gap;
ROADMAP success criteria 1..6 are the source of truth).
-->

<objective>
Land the per-span gap math on the backend: extend `query_endgame_entry_rows` with a LEAD-based
next-span entry eval lookup, add `_compute_span_gap` in `endgame_service.py`, accumulate per-class
gap vectors, call `compute_paired_difference_test` from `app/services/score_confidence.py`, and
populate 5 new fields on `EndgameCategoryStats`. Sign convention `exit_score - ES_entry`
(positive = user outperformed Stockfish baseline), matching the page-level Achievable Score Gap.

Purpose: produces the wire shape Plan 03 (frontend) and Plan 04 (LLM payload) consume. Reuses
the existing statistical helper and sigmoid library — no new statistical primitive, no new
sigmoid math.

Output: 8-column repo row shape, 5 new schema fields, helper + accumulator wiring in service,
unit tests on synthetic spans, integration test on a small fixture, ty-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-VALIDATION.md
@.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-CONTEXT.md

<interfaces>
<!-- Existing contracts this plan consumes -->

From app/services/score_confidence.py:338:
```
def compute_paired_difference_test(
    diffs: Sequence[float],
) -> tuple[float, float | None, float | None, float | None]:
    """Returns (mean_d, p_value, ci_low, ci_high).
    Behavior:
      n=0      -> (0.0, None, None, None)
      n=1      -> (mean, None, None, None)
      n>=2, var=0 -> handled internally
      n>=CONFIDENCE_MIN_N -> p_value populated; ci_low/ci_high populated at n>=2
    """
```

From app/services/eval_utils.py:
```
def eval_cp_to_expected_score(eval_cp: int, user_color: str) -> float
def eval_mate_to_expected_score(eval_mate: int, user_color: str) -> float
```

From app/repositories/endgame_repository.py (current shape, lines 140-230):
```
def query_endgame_entry_rows(...) -> Sequence[Row]
# Returns: (game_id, endgame_class, result, user_color, eval_cp, eval_mate)
# Span definition: GROUP BY game_id, endgame_class HAVING count(ply) >= ENDGAME_PLY_THRESHOLD (=6)
# Index: ix_gp_user_endgame_game INCLUDE(eval_cp, eval_mate)
```

<!-- New contracts produced by this plan, consumed by Plan 03 + Plan 04 -->

From app/schemas/endgames.py (post-edit):
```
class EndgameCategoryStats:
    # ...existing fields...
    type_achievable_score_gap_mean:   float | None = None  # Phase 87.1 D-05
    type_achievable_score_gap_n:      int   | None = None
    type_achievable_score_gap_p_value: float | None = None
    type_achievable_score_gap_ci_low:  float | None = None
    type_achievable_score_gap_ci_high: float | None = None
```

From app/repositories/endgame_repository.py (post-edit):
```
# query_endgame_entry_rows now returns 8 columns per row:
# (game_id, endgame_class, result, user_color, eval_cp, eval_mate,
#  next_entry_eval_cp, next_entry_eval_mate)
# Extra columns are NULL for terminal (last) span per game.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend query_endgame_entry_rows with LEAD-based next-span eval columns</name>
  <files>app/repositories/endgame_repository.py, tests/repositories/test_endgame_repository.py</files>
  <read_first>
    - app/repositories/endgame_repository.py lines 140-230 (query_endgame_entry_rows current shape — span_subq + main query)
    - app/repositories/query_utils.py (apply_game_filters — confirm it stays on the main query, not the span_subq)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"app/repositories/endgame_repository.py" (exact delta + main query rebind)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md §D-06 (extend-in-place rationale, LEAD pattern, index impact analysis)
    - tests/repositories/test_endgame_repository.py (existing test_query_endgame_entry_rows shape)
  </read_first>
  <behavior>
    - Test: query returns rows with `next_entry_eval_cp` and `next_entry_eval_mate` accessible by attribute name.
    - Test: for a game with 2 spans ordered by `span_min_ply`, the first row's `next_entry_eval_cp` equals the second row's `entry_eval_cp` (LEAD over (game_id, span_min_ply ASC)).
    - Test: for the last span in a game (no successor), `next_entry_eval_cp` is None and `next_entry_eval_mate` is None.
    - Test: span definition unchanged — `GROUP BY game_id, endgame_class HAVING count(ply) >= ENDGAME_PLY_THRESHOLD` still holds.
    - Test: existing 6-column attribute access (`row.eval_cp`, `row.eval_mate`, `row.result`, `row.user_color`, `row.game_id`, `row.endgame_class`) continues to work — no break for Phase 87 / Conv/Recov callers.
  </behavior>
  <action>
    Per PATTERNS §"app/repositories/endgame_repository.py" and RESEARCH §D-06:
    (1) In `query_endgame_entry_rows`, add `func.min(GamePosition.ply).label("span_min_ply")` to the `span_subq` SELECT alongside the existing `entry_eval_cp` and `entry_eval_mate` array_agg columns. The min is computed during the existing GROUP BY at no extra scan cost (per RESEARCH §D-06 Index impact).
    (2) After the `span_subq` definition, add a `span_with_next` subquery that wraps span_subq via SQLAlchemy 2.x `func.lead(...).over(partition_by=span_subq.c.game_id, order_by=span_subq.c.span_min_ply.asc())` for both `entry_eval_cp` and `entry_eval_mate`. Label the new columns `next_entry_eval_cp` and `next_entry_eval_mate`. Use `.subquery("span_with_next")`.
    (3) Change the main query's join target from `span_subq` to `span_with_next`. Add the two new LEAD columns to the main SELECT, labeled identically. Keep `apply_game_filters` on the main query (unchanged — RESEARCH §D-06 confirms the filter site is correct).
    (4) Update the docstring to reflect the 8-column row shape and document the terminal-span NULL semantics. Cite Phase 87.1 + SEED-016 + D-06.
    (5) Extend `tests/repositories/test_endgame_repository.py` with the 5 behavior assertions above. Use a fixture that creates 1 game with 2 distinct endgame_class spans (e.g. minor_piece then rook), each with `>= ENDGAME_PLY_THRESHOLD` positions, with distinct `entry_eval_cp` values per span. Assert LEAD ordering matches `span_min_ply ASC`. Add a second game with a single terminal span; assert its `next_entry_eval_cp` is None.
    (6) Do NOT add `asyncio.gather` over the AsyncSession (per CLAUDE.md). Single sequential query; LEAD runs inside Postgres.
    (7) Run `uv run ty check app/repositories/endgame_repository.py tests/repositories/test_endgame_repository.py` — must exit 0.
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_endgame_repository.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func.lead' app/repositories/endgame_repository.py` returns >= 2 (one for eval_cp, one for eval_mate)
    - `grep -c 'span_min_ply' app/repositories/endgame_repository.py` returns >= 2 (in span_subq + LEAD order_by)
    - `grep -c 'next_entry_eval_cp' app/repositories/endgame_repository.py` returns >= 2 (LEAD label + main SELECT)
    - `uv run pytest tests/repositories/test_endgame_repository.py -x` exits 0
    - `uv run ty check app/repositories/endgame_repository.py tests/repositories/test_endgame_repository.py` exits 0
    - Compiled SQL contains `LEAD(` (window function emitted): `uv run python -c "from sqlalchemy.dialects import postgresql; from app.repositories.endgame_repository import _build_endgame_entry_stmt; ..."` — alternatively assert via existing test infrastructure that the query result objects have the new attributes.
  </acceptance_criteria>
  <done>
    8-column row shape live; LEAD-based next-span eval lookup correct; existing callers unaffected; terminal spans return NULL on the new columns; tests green; ty clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _compute_span_gap helper + per-class accumulator + 5 schema fields</name>
  <files>app/services/endgame_service.py, app/schemas/endgames.py, tests/services/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py lines 253-442 (existing _aggregate_endgame_stats loop + categories builder)
    - app/services/endgame_service.py around line 1988 (Phase 85.1 paired-diff accumulator pattern — direct precedent)
    - app/services/score_confidence.py lines 320-410 (compute_paired_difference_test signature + n-gate behavior table)
    - app/services/eval_utils.py (eval_cp_to_expected_score, eval_mate_to_expected_score signatures + clipping conventions; EVAL_CLIP_MAX_CP)
    - app/schemas/endgames.py (EndgameCategoryStats + EndgamePerformanceResponse — note the achievable_score_gap field cluster pattern that the new fields mirror)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"app/services/endgame_service.py" and §"app/schemas/endgames.py" (full deltas with helper signature + import list)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md §D-05, §D-07
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md §"Service-Layer Math" (full helper body)
  </read_first>
  <behavior>
    - Test: `_compute_span_gap` returns None when both `entry_eval_cp` and `entry_eval_mate` are None (NULL-eval span skipped per D-07).
    - Test: pure transitory span (both `next_entry_eval_cp` and `next_entry_eval_mate` non-NULL → only one of the two paths fires; mate takes precedence per `eval_mate_to_expected_score` path) returns `exit_score - ES_entry` matching hand-computed sigmoid values.
    - Test: pure terminal span (`next_entry_eval_cp` and `next_entry_eval_mate` both None) maps `result × user_color` via `{"win": 1.0, "draw": 0.5, "loss": 0.0}` to `exit_score`, then returns `exit_score - ES_entry`.
    - Test: mate at entry — `entry_eval_mate` non-NULL → uses `eval_mate_to_expected_score` for ES_entry (sigmoid clamps to ~1.0 or ~0.0 per user_color).
    - Test: mate at exit — `next_entry_eval_mate` non-NULL → uses `eval_mate_to_expected_score` for exit_score.
    - Test: sign convention — positive `gap_span` means user outperformed Stockfish (exit_score > ES_entry).
    - Test: `_aggregate_endgame_stats` populates all 5 new fields on `EndgameCategoryStats` per class.
    - Test: `compute_paired_difference_test` behavior reflected — n=0 → mean=0.0/p=None/ci=None; n=1 → mean populated/p=None/ci=None; n>=10 → all 4 populated.
    - Test: integration fixture with 2-3 games per class — hand-computed reference matches `type_achievable_score_gap_mean` to within 1e-6.
  </behavior>
  <action>
    Per PATTERNS §"app/services/endgame_service.py" + §"app/schemas/endgames.py" + RESEARCH §"Service-Layer Math" + CONTEXT §D-05/D-07:
    (1) In `app/schemas/endgames.py`, extend `EndgameCategoryStats` with 5 new fields after the existing `score_p_value` field (per PATTERNS Delta):
        ```
        type_achievable_score_gap_mean:   float | None = None
        type_achievable_score_gap_n:      int   | None = None
        type_achievable_score_gap_p_value: float | None = None
        type_achievable_score_gap_ci_low:  float | None = None
        type_achievable_score_gap_ci_high: float | None = None
        ```
        All five default to None for backward compat with existing constructor call sites. Include the comment block from PATTERNS citing Phase 87.1 + SEED-016 D-05 + dual-label rationale ("User-facing label is 'Score Gap' (card row) / 'Endgame Type Score Gap' (concepts). Internal name retains 'achievable' to mark the math-family with achievable_score_gap (Phase 85.1).").
    (2) In `app/services/endgame_service.py`:
        - Add module-level constant `_GAME_RESULT_TO_SCORE: dict[str, float] = {"win": 1.0, "draw": 0.5, "loss": 0.0}` near the top of the file (per CLAUDE.md "no magic numbers").
        - Verify `eval_cp_to_expected_score` and `eval_mate_to_expected_score` are imported from `app.services.eval_utils`. Add `eval_mate_to_expected_score` to the import list if missing.
        - Add `_compute_span_gap(entry_eval_cp, entry_eval_mate, next_entry_eval_cp, next_entry_eval_mate, result, user_color) -> float | None` above `_aggregate_endgame_stats` with the exact body from PATTERNS §"app/services/endgame_service.py" Delta 4. Sign convention: `return exit_score - es_entry` (positive = user outperformed). Docstring cites Phase 87.1 + SEED-016 D-07.
        - Inside `_aggregate_endgame_stats`, before the existing row-loop, add `gaps_by_class: dict[EndgameClass, list[float]] = defaultdict(list)` (import `defaultdict` if missing).
        - Inside the existing for-loop iterating repo rows, after the endgame_class lookup, call `_compute_span_gap(eval_cp, eval_mate, row.next_entry_eval_cp, row.next_entry_eval_mate, result, user_color)` and append non-None results to `gaps_by_class[endgame_class]`. Use attribute access on the row for `next_entry_eval_cp/mate` (the existing positional unpacking handles the first 6 columns).
        - Inside the per-class `EndgameCategoryStats` builder loop, after `score_p_value` is computed, call `compute_paired_difference_test(gaps_by_class[endgame_class])`, capture `(type_gap_mean, type_gap_p, type_gap_ci_low, type_gap_ci_high)`, record `type_gap_n = len(gaps_by_class[endgame_class])`, and pass the five values into the `EndgameCategoryStats(...)` kwargs.
        - Comment block on the new computation: `# Phase 87.1 (SEED-016 D-03/D-05/D-07): per-class mean per-span gap. compute_paired_difference_test from app/services/score_confidence.py (same helper used by Phase 85.1 SEC1-10 for the page-level Achievable Score Gap). Sign: exit_score - ES_entry, positive = user outperformed.`
    (3) Extend `tests/services/test_endgame_service.py` with:
        - Unit tests on `_compute_span_gap` covering all 9 boundary cases from VALIDATION.md §"Nyquist Sampling — Discrete Output Signals" / Math helper.
        - Integration test on `_aggregate_endgame_stats` with a fixture containing 2-3 games per class (rook, minor_piece, queen) where some games have 2 spans (transitory + terminal) and some have 1 span (terminal). Hand-compute the expected `type_achievable_score_gap_mean` for one class and assert equality to 1e-6.
        - Test: at n < `CONFIDENCE_MIN_N` (n=5), `type_achievable_score_gap_p_value` is None.
        - Test: at n >= 10, p-value is populated; at n >= 2, ci_low/ci_high are populated.
        - Test: NULL-eval span correctly excluded — `type_achievable_score_gap_n` reflects only spans where `_compute_span_gap` returned non-None.
    (4) ty + ruff:
        - All new annotations use `float | None` and `int | None` (no `Optional`, no untyped fields).
        - `Sequence[float]` for any helper parameters that accept `compute_paired_difference_test`-shaped input (per CLAUDE.md ty rules).
        - `# ty: ignore[arg-type]` permitted only on the `eval_cp_to_expected_score(entry_eval_cp, ...)` line where the cp value is known non-None inside the else-branch (mirror PATTERNS pattern).
    (5) Sentry: No new exceptional paths. The helper returns None on NULL-eval; the accumulator skips None. `compute_paired_difference_test` handles n=0/n=1/zero-variance internally. Note in PR description that no `sentry_sdk.capture_exception` is needed here.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service.py -x &amp;&amp; uv run ty check app/services/endgame_service.py app/schemas/endgames.py tests/services/test_endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_compute_span_gap' app/services/endgame_service.py` returns >= 3 (def + import or accumulator call + comment)
    - `grep -c 'compute_paired_difference_test' app/services/endgame_service.py` returns >= 2 (existing Phase 85.1 call + new Phase 87.1 call)
    - `grep -c 'type_achievable_score_gap_mean' app/schemas/endgames.py` returns >= 1
    - `grep -c 'type_achievable_score_gap_' app/schemas/endgames.py` returns >= 5 (all 5 fields)
    - `grep -c '_GAME_RESULT_TO_SCORE' app/services/endgame_service.py` returns >= 2 (declaration + use)
    - `grep -cv '^#' app/services/endgame_service.py | head` — verify no bare `1.0`, `0.5`, `0.0` literals appear inline in the gap helper (must reference the named constant)
    - `uv run pytest tests/services/test_endgame_service.py -x` exits 0 (all new unit + integration tests green)
    - `uv run ty check app/services/endgame_service.py app/schemas/endgames.py tests/services/test_endgame_service.py` exits 0
    - Sign convention asserted in a test: a synthetic transitory span where ES_entry=0.4 and exit=ES_sigmoid(next_eval) ≈ 0.7 yields gap ≈ +0.3 (NOT −0.3).
  </acceptance_criteria>
  <done>
    Schema fields live; helper + accumulator + paired-diff call wired in service; sign convention `exit_score - ES_entry` enforced and tested; full unit + integration coverage; ty clean.
  </done>
</task>

</tasks>

<threat_model>
none — purely additive read-path on existing authenticated endpoints. No new user input flows, no new exceptional paths, no new auth surfaces. The new computation runs server-side over data the user already owns (their own game positions). NULL-eval handling is total; statistical helper handles all boundary cases internally per its docstring contract.
</threat_model>

<verification>
- `uv run pytest tests/repositories/test_endgame_repository.py tests/services/test_endgame_service.py -x` exits 0
- `uv run ty check app/repositories/endgame_repository.py app/services/endgame_service.py app/schemas/endgames.py tests/` exits 0
- `uv run ruff check app/repositories/endgame_repository.py app/services/endgame_service.py app/schemas/endgames.py` exits 0
- Spot-check repo row shape — 8 columns including `next_entry_eval_cp`, `next_entry_eval_mate`
- Spot-check `EndgameCategoryStats` carries 5 new `type_achievable_score_gap_*` fields
- Sign convention enforced in tests (positive `gap_span` ⇒ user outperformed Stockfish)
</verification>

<success_criteria>
ROADMAP §1, §2, §3 satisfied:
- §1: 5 new fields populated via `compute_paired_difference_test`; gate behavior matches helper's n-gates.
- §2: Repo query returns next-span entry eval via LEAD() window over `(game_id, span_min_ply ASC)`.
- §3: Codebase span definition `GROUP BY (game_id, endgame_class) HAVING count(ply) >= ENDGAME_PLY_THRESHOLD` preserved.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-02-SUMMARY.md`.
</output>
