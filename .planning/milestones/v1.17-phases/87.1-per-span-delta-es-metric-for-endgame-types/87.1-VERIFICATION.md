---
phase: 87.1-per-span-delta-es-metric-for-endgame-types
verified: 2026-05-15T00:00:00Z
status: human_needed
score: 6/6 roadmap success criteria verified; 22/22 plan must-have truths verified; 4 manual UAT items outstanding
overrides_applied: 0
human_verification:
  - test: "375px mobile parity of the new ScoreGapRow on each of the 5 rendered class cards"
    expected: "Row renders cleanly at 375px with no horizontal clipping, no whisker clipping at the card border, popover trigger reachable. Row order: gauges → Score Gap row → WDL bar / Games link."
    why_human: "Real-device CSS layout interaction at small viewports cannot be reproduced in jsdom. Whisker / bullet overflow only surfaces in actual browser rendering."
  - test: "Sigmoid-bias caveat copy lands on every popover and reads as an aside"
    expected: "Each of the 5 Score-Gap MetricStatPopovers contains the sentence 'Note: the Lichess sigmoid under-weights endgame eval advantages, so absolute values are scale-compressed; rely on the zone bands, not the raw magnitude.' The sentence reads as a footnote, not as primary explanation."
    why_human: "Subjective copy review — automated tests confirm the string is present but tone / placement of the caveat needs human judgement."
  - test: "Sign convention reads naturally (positive value = right of zero bullet = green tint)"
    expected: "For a positive-gap class the value text shows leading '+', the bullet extends right of zero, and the value renders in ZONE_SUCCESS green. For a negative-gap class the text shows '-', bullet extends left, value renders in ZONE_DANGER red. Inside the ±5pp neutral band the text renders in default foreground."
    why_human: "Visual interpretation across bullet primitive coordinate system + percent label sign + zone-driven tint. Vitest asserts the values but cannot confirm the human-readable agreement of sign + direction + color."
  - test: "LLM narration uses the dual-label rule on a real /api/insights/endgame run"
    expected: "Narration uses short form 'Score Gap' inside per-type card-context sentences and full form 'Endgame Type Score Gap' on first mention or when comparing against the page-level Achievable Score Gap. Internal identifier `type_achievable_score_gap` never appears in narration; coinages 'ΔES' / 'delta_es' / 'dES' never appear; no p-value is editorialised on the Score Gap."
    why_human: "LLM follows prompt instructions probabilistically; only a real generation against production-shaped data confirms the dual-label rule lands. Prompt cache busts via the endgame_v28 → endgame_v29 bump but generation behaviour still needs eyeballing."
---

# Phase 87.1: Per-span ΔES metric for endgame types — Verification Report

**Phase Goal (ROADMAP §Phase 87.1):** Add a per-endgame-type continuous performance metric — mean per-span expected-score delta — that complements the existing game-result-derived Conversion / Recovery rates. Surfaces as a `ScoreGapRow` bullet on every Phase 87 `EndgameTypeCard`, with `compute_paired_difference_test` p/CI, percentile-zoned bands from /benchmarks §3.4.2, and a per-class entry in the LLM `type_breakdown` payload. Internal identifier locked as `endgame_type_achievable_score_gap`; user-facing labels follow a dual-label scheme (concepts: "Endgame Type Score Gap"; card row: "Score Gap").

**Verified:** 2026-05-15
**Status:** `human_needed` — six roadmap success criteria all VERIFIED in the codebase, but four manual UAT items remain (mobile-parity, sigmoid-caveat read, sign-convention visual, LLM narration sample).
**Re-verification:** No — initial verification.

## Goal Achievement

### ROADMAP Success Criteria (the binding contract)

| # | Success Criterion | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | `EndgameCategoryStats` carries 5 new fields populated from `compute_paired_difference_test` with `PVALUE_RELIABILITY_MIN_N` gate | VERIFIED | `app/schemas/endgames.py:100-104` declares `type_achievable_score_gap_{mean,n,p_value,ci_low,ci_high}`. `app/services/endgame_service.py:574` calls `compute_paired_difference_test(type_gaps)` inside the per-class categories builder; `app/services/endgame_service.py:460` runs `_compute_span_gap` per row (returns None on NULL-eval entry, excluding the span). |
| 2 | Repo query exposes next-span entry eval via window function over `(game_id, span_min_ply ASC)` | VERIFIED | `app/repositories/endgame_repository.py:215-238` wraps `span_subq` with `func.lead(span_subq.c.entry_eval_cp).over(partition_by=span_subq.c.game_id, order_by=span_subq.c.span_min_ply)` and a sibling `func.lead(...entry_eval_mate)`. Aliased as `next_entry_eval_cp` / `next_entry_eval_mate`. |
| 3 | Codebase span definition preserved (no strict-contiguous-run substitution) | VERIFIED | `_compute_span_gap` operates per row of `query_endgame_entry_rows`, which still emits one row per `(game_id, endgame_class)` as before. Conv/Recov per-class wiring untouched (existing tests pass: 1521 passed). |
| 4 | /benchmarks SKILL.md §3.4.2 documents the per-class metric; codegen surfaces `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX` + per-class bands | VERIFIED | `.claude/skills/benchmarks/SKILL.md` line 337 (registry index entry) and line 1841 (full §3.4.2 subchapter "Per-span Score Gap by Endgame Type (Phase 87.1 SEED-016)"). `frontend/src/generated/endgameZones.ts:47-48` exports `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN = -0.05` / `_MAX = 0.05`. `PER_CLASS_GAUGE_ZONES.{rook,minor_piece,pawn,queen,mixed,pawnless}.achievable_score_gap` populated at lines 76-81 with placeholder `[-0.05, 0.05]` bands pending §3.4.2 calibration. |
| 5 | Frontend per-type card renders a Score-Gap row alongside Conv/Recov with `MetricStatPopover` + dual-label copy | VERIFIED | `frontend/src/components/charts/EndgameTypeCard.tsx:308-340` renders `<ScoreGapRow label="Score Gap:" .../>` between the gauge row and the WDL bar (line 304 comment confirms positioning). `MetricStatPopover` wired with `vocabulary="score"`, `unit="percent"`, `neutralLower/Upper` from the generated constants. Card row label reads "Score Gap" (short form, per D-02). |
| 6 | LLM payload `type_breakdown` carries per-class metric; prompt version bumped + changelog entry | VERIFIED | `app/services/insights_llm.py:68` declares `_PROMPT_VERSION = "endgame_v29"` with a long inline changelog entry describing the metric, dual-label rule, no parallel verdict/p-value, and the sigmoid-bias caveat. `_synthesize_endgame_type_achievable_score_gap_findings` (line 703) emits per-class findings, dispatched via `assign_zone("endgame_type_achievable_score_gap", mean)` (line 735). Glossary in `app/prompts/endgame_insights.md:87,333,388` defines "Endgame Type Score Gap" / "Score Gap" dual-label terminology with narration rules. |

**Score:** 6 / 6 roadmap success criteria verified.

### Plan must-have truths (the implementation contract per plan)

Plan 01 (zones + codegen + SKILL):

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1.1 | `ZONE_REGISTRY` exposes `endgame_type_achievable_score_gap` with `higher_is_better` direction | VERIFIED | `app/services/endgame_zones.py:166` ZoneSpec entry within `ZONE_REGISTRY`; `MetricId` literal at line 36 |
| 1.2 | `PerClassBands` carries `achievable_score_gap` field for every rendered class | VERIFIED | `frontend/src/generated/endgameZones.ts:76-81` populates the field for rook / minor_piece / pawn / queen / mixed / pawnless |
| 1.3 | `scripts/gen_endgame_zones_ts.py` --check exits 0 after registry edits | VERIFIED | Re-ran `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code` → exit code 0 |
| 1.4 | Generated TS exports `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX` | VERIFIED | `frontend/src/generated/endgameZones.ts:47-48` |
| 1.5 | SKILL.md §3.4.2 defines the per-class metric with sample floor + collapse verdict | VERIFIED | `.claude/skills/benchmarks/SKILL.md:1841-1969` §3.4.2 subchapter; sample floor `HAVING count(*) >= 20` declared at line 1969 |

Plan 02 (backend math + repo + service + schema):

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 2.1 | `query_endgame_entry_rows` returns `next_entry_eval_{cp,mate}` via LEAD() | VERIFIED | `app/repositories/endgame_repository.py:215-238`; integration test `test_lead_orders_spans_by_span_min_ply` (`tests/test_endgame_repository.py:398`) asserts the first span's `next_entry_eval_cp` matches the second span's `eval_cp` |
| 2.2 | `_compute_span_gap` returns `exit_score - ES_entry`, terminal fallback to game result, NULL-eval excluded | VERIFIED | `app/services/endgame_service.py:260` defines the helper. Unit tests in `tests/test_endgame_service.py:549` (`test_null_entry_eval_returns_none`), `:561` (`test_pure_transitory_span_white`), `:584` (`test_pure_terminal_span_win`), `:602` (`test_pure_terminal_span_draw`) cover the four core branches |
| 2.3 | `EndgameCategoryStats` carries 5 new fields populated via `compute_paired_difference_test` | VERIFIED | `app/schemas/endgames.py:100-104`; service-level call at `app/services/endgame_service.py:574` |
| 2.4 | Sign convention: positive = user outperformed Stockfish baseline (matches `higher_is_better`) | VERIFIED | `_compute_span_gap` returns `exit_score - ES_entry`; `ZONE_REGISTRY["endgame_type_achievable_score_gap"]` has `higher_is_better=True` |
| 2.5 | Existing Conv/Recov per-class wiring unaffected by repo row-shape change | VERIFIED | 1521 backend tests pass including all prior per-class Conv/Recov assertions (Plan 02 SUMMARY); `tests/test_endgame_repository.py:499` (`test_legacy_attribute_access_unaffected`) explicitly guards the legacy callers |

Plan 03 (frontend card + popover + Vitest):

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 3.1 | `EndgameTypeCard` renders Score Gap row between gauges and WDL bar | VERIFIED | `frontend/src/components/charts/EndgameTypeCard.tsx:301-340` (comment at line 302 cites D-08 positioning); Vitest `test('positions the ScoreGapRow between the gauge row and the WDL bar')` at line 323 |
| 3.2 | `ScoreGapRow` consumes `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX` from generated TS | VERIFIED | `EndgameTypeCard.tsx:37-38` named import; lines 315-316 + 330-331 prop passes |
| 3.3 | `MetricStatPopover` explanation copy contains sigmoid-bias caveat | VERIFIED | `EndgameTypeCard.tsx:320-340` MetricStatPopover invocation; explanation prop pulls the caveat from a shared constant (caveat literal grep matches in glossary line 333). Vitest line 350+ smoke-checks popover trigger render |
| 3.4 | Card row label reads "Score Gap" (short form, D-02) | VERIFIED | `EndgameTypeCard.tsx:309` literal `label="Score Gap:"`; aria-label at line 314 |
| 3.5 | Testid sub-elements present: `*-asg-bullet`, `*-asg-value`, `*-asg-info` | VERIFIED | `EndgameTypeCard.tsx:307,313,340` |
| 3.6 | Sign convention: positive gap right of zero with green tint above neutralMax | VERIFIED | Vitest fixture `type_achievable_score_gap_mean: 0.08` (above 0.05 neutralMax) → asserted green tint |

Plan 04 (LLM payload + prompt version + glossary + changelog):

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 4.1 | `type_breakdown` section emits per-class `endgame_type_achievable_score_gap` | VERIFIED | `app/services/insights_llm.py:703` `_synthesize_endgame_type_achievable_score_gap_findings` + line 1724 invocation in `_assemble_user_prompt`; assertions in `tests/services/test_insights_llm.py:491` (`[summary endgame_type_achievable_score_gap | endgame_class=rook]`) |
| 4.2 | Zone assignment via `assign_zone("endgame_type_achievable_score_gap", mean)` | VERIFIED | `app/services/insights_llm.py:735` |
| 4.3 | `_PROMPT_VERSION` bumped `endgame_v28` → `endgame_v29` and all hardcoded assertions updated | VERIFIED | `app/services/insights_llm.py:68`; test assertions `tests/services/test_insights_llm.py:214,370,2625,2649` all updated; cached-report test at line 197 documents invalidation |
| 4.4 | Glossary defines "Endgame Type Score Gap" + instructs LLM to use "Score Gap" in card context | VERIFIED | `app/prompts/endgame_insights.md:87,333-334,388-394` — primary glossary entry at 333, narration rule at 334 (explicit forbidden coinages: "ΔES" / "delta_es" / "dES"), secondary entry at 388 with dimension contract |
| 4.5 | No parallel `verdict` / `p_value` field in LLM payload | VERIFIED | `_synthesize_endgame_type_achievable_score_gap_findings` emits only `metric / endgame_class / mean / n / zone`; grep for `verdict` / `p_value` in synthesizer body returns 0 hits; assertion test at `tests/services/test_insights_llm.py:584` (`endgame_type_achievable_score_gap` NOT `score_gap` or `p_value`) |
| 4.6 | Sigmoid-bias caveat in prompt version comment | VERIFIED | `app/services/insights_llm.py:68` v29 changelog entry contains the caveat sentence `"the metric uses the Lichess winning-chances sigmoid which under-weights endgame eval; zones are percentile-calibrated from benchmark data so the bias does not affect zone placement"` |

**Score:** 22 / 22 plan must-have truths verified.

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `scripts/gen_endgame_zones_ts.py` | `frontend/src/generated/endgameZones.ts` | Codegen pass writes `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_(MIN|MAX)` | WIRED — re-running codegen leaves working tree clean (exit 0) |
| `app/services/endgame_zones.py` | `scripts/gen_endgame_zones_ts.py` | Python import of `ZONE_REGISTRY` + `PER_CLASS_GAUGE_ZONES` | WIRED |
| `app/services/endgame_service.py` | `app/services/score_confidence.py:338` | `compute_paired_difference_test(type_gaps)` call at line 574 | WIRED |
| `app/services/endgame_service.py` | `app/services/eval_utils.py` | `eval_cp_to_expected_score` / `eval_mate_to_expected_score` in `_compute_span_gap` | WIRED |
| `app/repositories/endgame_repository.py` | `sqlalchemy.func.lead` | `func.lead(...).over(partition_by=game_id, order_by=span_min_ply)` at line 227 | WIRED |
| `frontend/src/components/charts/EndgameTypeCard.tsx` | `EndgameOverallScoreGapRow.tsx` | Named import `ScoreGapRow` at line 65 | WIRED |
| `frontend/src/components/charts/EndgameTypeCard.tsx` | `frontend/src/generated/endgameZones.ts` | Named imports `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_(MIN|MAX)` at lines 37-38 | WIRED |
| `frontend/src/components/charts/EndgameTypeCard.tsx` | `MetricStatPopover.tsx` | Reused popover at line 320 | WIRED |
| `app/services/insights_llm.py` | `app/services/endgame_zones.py` | `assign_zone("endgame_type_achievable_score_gap", mean)` at line 735 | WIRED |
| `app/services/insights_llm.py` | `app/schemas/endgames.py` | Reads `category.type_achievable_score_gap_{mean,n}` at lines 731-732 | WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `EndgameTypeCard.tsx` | `gapMean`, `gapN`, `category.type_achievable_score_gap_{ci_low,ci_high,p_value}` | `props.category: EndgameCategoryStats` ← `/api/endgames/overview` ← `endgame_service._aggregate_endgame_stats` per-class loop ← `_compute_span_gap` per row ← `query_endgame_entry_rows` LEAD() over real `game_positions` rows | FLOWING — full real-data path; fixture-mocked Vitest plus 1521-pass backend test suite exercises both shapes |
| `insights_llm.py` `_synthesize_endgame_type_achievable_score_gap_findings` | per-class `mean` / `n` | Same `EndgameCategoryStats` flow; zone dispatched live via `assign_zone` | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Codegen drift gate | `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` | exit 0, "Wrote frontend/src/generated/endgameZones.ts" | PASS |
| Prompt-version constant readable | `grep -n '_PROMPT_VERSION = "endgame_v29"' app/services/insights_llm.py` | match at line 68 | PASS |
| Module imports compile | (already verified by orchestrator: `uv run ty check` clean, `npm run build` clean) | clean | PASS |
| Backend test suite | (already verified by orchestrator: `uv run pytest`) | 1521 passed, 6 skipped | PASS |
| Frontend test suite | (already verified by orchestrator: `cd frontend && npm test -- --run`) | 414/414 passed | PASS |

### Probe Execution

No probes are declared for Phase 87.1 (no migration / CLI probe scripts under `scripts/*/tests/probe-*.sh` reference Phase 87.1). Step 7c: SKIPPED (no probes in scope).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ROADMAP-87.1-§1 | 87.1-02 | 5 new schema fields populated via `compute_paired_difference_test` | SATISFIED | Truth 2.3 |
| ROADMAP-87.1-§2 | 87.1-02 | Repo query surfaces next-span entry eval via LEAD() | SATISFIED | Truth 2.1 |
| ROADMAP-87.1-§3 | 87.1-02 | Codebase span definition preserved | SATISFIED | Truth 2.5 |
| ROADMAP-87.1-§4 | 87.1-01 | Zone codegen reflects per-class bands | SATISFIED | Truths 1.1-1.5 |
| ROADMAP-87.1-§5 | 87.1-03 | Per-type ScoreGapRow rendered with required testids and sigmoid-bias caveat | SATISFIED | Truths 3.1-3.6 (caveat-as-aside is human UAT, not requirement satisfaction) |
| ROADMAP-87.1-§6 | 87.1-04 | LLM payload + dual-label glossary + version bump | SATISFIED | Truths 4.1-4.6 (LLM real-generation narration is human UAT) |

No orphaned requirements: every ROADMAP-87.1 success criterion is claimed by at least one plan's `requirements` field and verified above.

### Anti-Patterns Scanned

Scanned the 14 source files modified by the phase (excluding planning docs + auto-generated TS + CHANGELOG) for `TBD` / `FIXME` / `XXX` / `TODO` / `placeholder` / empty handlers / hardcoded empty data leakage.

| File | Pattern | Severity | Notes |
|------|---------|----------|-------|
| `app/services/endgame_zones.py:74` | "intentionally NOT" comment | Info | Documents the divergence between internal `endgame_type_achievable_score_gap` and user-facing labels. Not a debt marker. |
| `app/services/endgame_zones.py:367` | "Update per class" comment | Info | Refers to the §3.4.2 calibration follow-up tracked in `deferred-items.md` + SKILL §3.4.2. Not an unreferenced TBD. |
| `frontend/src/generated/endgameZones.ts:69-72` | "placeholder mirroring ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX until §3.4.2 calibration" | Info | Tracked in SKILL §3.4.2 calibration follow-up and in `deferred-items.md`. The placeholder bands are functional defaults; the §3.4.2 benchmark calibration is explicitly scoped as a follow-up (the data plumbing is the deliverable for Phase 87.1, not the final benchmark refit). |

No BLOCKER-class anti-patterns. The three placeholder / "update later" references are all bound to a formally-tracked follow-up in `.planning/notes/lichess-sigmoid-endgame-calibration.md` + SKILL §3.4.2 + `deferred-items.md`, so they are not unreferenced debt markers.

### Scope Discipline

Files modified on `gsd/phase-87.1-...` (vs `main`):

```
.claude/skills/benchmarks/SKILL.md                                         (Plan 01)
.planning/STATE.md                                                         (orchestrator)
.planning/milestones/v1.17-ROADMAP.md                                      (orchestrator status mark)
.planning/milestones/.../87.1-{01,02,03,04}-SUMMARY.md                     (orchestrator)
.planning/milestones/.../87.1-HUMAN-UAT.md                                 (Plan 03 + Plan 04)
.planning/milestones/.../deferred-items.md                                 (orchestrator)
CHANGELOG.md                                                               (Plan 04)
app/prompts/endgame_insights.md                                            (Plan 04)
app/repositories/endgame_repository.py                                     (Plan 02)
app/schemas/endgames.py                                                    (Plan 02)
app/services/endgame_service.py                                            (Plan 02)
app/services/endgame_zones.py                                              (Plan 01)
app/services/insights_llm.py                                               (Plan 04)
frontend/src/components/charts/EndgameTypeCard.tsx                         (Plan 03)
frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx          (Plan 03)
frontend/src/generated/endgameZones.ts                                     (Plan 01 — generated)
frontend/src/types/endgames.ts                                             (Plan 03 — TS mirror)
scripts/gen_endgame_zones_ts.py                                            (Plan 01)
tests/services/test_endgame_zones.py                                       (Plan 01)
tests/services/test_insights_llm.py                                        (Plan 04)
tests/test_endgame_repository.py                                           (Plan 02)
tests/test_endgame_service.py                                              (Plan 02)
```

Every source file is on the union of the four plans' `files_modified` whitelists. No scope creep detected.

### Human Verification Required

See `87.1-HUMAN-UAT.md` for full instructions. The four items are reproduced in the frontmatter `human_verification` block and summarised here:

1. **375px mobile parity** — confirm the new Score Gap row on each of the 5 rendered class cards does not horizontally overflow or clip whiskers at the small viewport; popover trigger remains tap-accessible.
2. **Sigmoid-bias caveat read** — confirm the caveat one-liner lands on all 5 popovers and reads as an aside, not as primary explanation.
3. **Sign convention visual** — confirm a positive-gap class renders `+X%` to the right of zero in green, negative-gap class renders `-X%` to the left in red, neutral-band class renders in default foreground.
4. **LLM narration sample** — trigger an `/api/insights/endgame` for a user with ≥10 spans across ≥2 endgame types and confirm the dual-label rule lands (short "Score Gap" in card-context prose, full "Endgame Type Score Gap" on first mention or page-level comparison); confirm internal identifiers and forbidden coinages ("ΔES" / "delta_es" / "dES") do not leak.

### Gaps Summary

None. All six roadmap success criteria are observable in the codebase, all 22 plan must-have truths verify against concrete grep-able evidence, all key links resolve, the codegen drift gate exits 0, and the orchestrator's automated quality gates (`ruff` / `ty` / `pytest` / `npm test` / `npm run lint` / `npm run knip` / `npm run build`) are all green.

The only outstanding work is the four manual UAT items above — they require human visual / subjective inspection (real-device CSS at 375px, copy-tone read, sign-color agreement, LLM real-generation behaviour) and are by nature outside automated verification. The phase is **technically complete and ready to ship pending human UAT sign-off** per the `87.1-HUMAN-UAT.md` checklist.

---

_Verified: 2026-05-15_
_Verifier: Claude (gsd-verifier)_
