---
phase: 87.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/benchmarks/SKILL.md
  - app/services/endgame_zones.py
  - scripts/gen_endgame_zones_ts.py
  - frontend/src/generated/endgameZones.ts
autonomous: true
requirements:
  - ROADMAP-87.1-§4   # zone codegen reflects per-class bands
tags:
  - benchmarks
  - codegen
  - zones
  - endgame
must_haves:
  truths:
    - "ZONE_REGISTRY exposes endgame_type_achievable_score_gap with higher_is_better direction"
    - "PerClassBands carries achievable_score_gap field for every rendered class"
    - "scripts/gen_endgame_zones_ts.py --check exits 0 after registry edits"
    - "frontend/src/generated/endgameZones.ts exports ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN and ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX"
    - "SKILL.md §3.4.2 defines per_span_achievable_score_gap_by_class metric with sample floor + collapse verdict"
  artifacts:
    - path: ".claude/skills/benchmarks/SKILL.md"
      provides: "§3.4.2 per-span Score Gap by endgame type subchapter"
      contains: "3.4.2"
    - path: "app/services/endgame_zones.py"
      provides: "MetricId literal + ZONE_REGISTRY entry + PerClassBands.achievable_score_gap field"
      contains: "endgame_type_achievable_score_gap"
    - path: "scripts/gen_endgame_zones_ts.py"
      provides: "Codegen emits ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX + per-class achievable_score_gap band"
      contains: "ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "Regenerated TS constants consumed by Plan 03 frontend card"
      contains: "ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN"
  key_links:
    - from: "scripts/gen_endgame_zones_ts.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "codegen script writes TS constants"
      pattern: "ENDGAME_TYPE_SCORE_GAP_NEUTRAL_(MIN|MAX)"
    - from: "app/services/endgame_zones.py"
      to: "scripts/gen_endgame_zones_ts.py"
      via: "Python import of ZONE_REGISTRY + PER_CLASS_GAUGE_ZONES"
      pattern: "from app.services.endgame_zones import"
---

<!--
Requirement IDs reference ROADMAP §Phase 87.1 success criteria (REQUIREMENTS.md
has no DELTA-NN entries for Phase 87.1 yet — RESEARCH §REQUIREMENTS.md Gap;
ROADMAP success criteria 1..6 are the source of truth).
-->

<objective>
Add the per-span, per-type Score Gap metric to the zones registry, extend the codegen script
to emit its TypeScript constants, and regenerate `endgameZones.ts`. Also document the
benchmark calibration method in `.claude/skills/benchmarks/SKILL.md §3.4.2`. This plan
publishes the contract Plan 03 consumes (constant names) and the metric registry Plan 04
dispatches through (`assign_zone("endgame_type_achievable_score_gap", ...)`).

Purpose: unlock parallel work on backend math (Plan 02), frontend card (Plan 03), and LLM payload (Plan 04) by landing the registry + codegen contract first. Placeholder bands `(-0.05, 0.05)` mirror the page-level `achievable_score_gap` band; benchmark calibration may tighten them later without renaming.

Output: registry entry, dataclass field, codegen extensions, regenerated TS file, SKILL.md §3.4.2 subchapter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md

<interfaces>
<!-- Key contracts produced by this plan, consumed by Plans 03 + 04. -->

From app/services/endgame_zones.py (post-edit):
```
MetricId Literal gains: "endgame_type_achievable_score_gap"
ZONE_REGISTRY["endgame_type_achievable_score_gap"] = ZoneSpec(
    typical_lower=-0.05,
    typical_upper=0.05,
    direction="higher_is_better",
)
PerClassBands gains field: achievable_score_gap: tuple[float, float]
PER_CLASS_GAUGE_ZONES[<class>].achievable_score_gap available for: rook, minor_piece, pawn, queen, mixed, pawnless
```

From frontend/src/generated/endgameZones.ts (post-codegen):
```
export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN = -0.05;
export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX =  0.05;
PER_CLASS_GAUGE_ZONES.{rook,minor_piece,pawn,queen,mixed,pawnless} each carry achievable_score_gap: [lower, upper]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MetricId, ZONE_REGISTRY, PerClassBands, PER_CLASS_GAUGE_ZONES in endgame_zones.py</name>
  <files>app/services/endgame_zones.py</files>
  <read_first>
    - app/services/endgame_zones.py (current MetricId, ZoneSpec, ZONE_REGISTRY, PerClassBands, PER_CLASS_GAUGE_ZONES, BucketedMetricId)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"app/services/endgame_zones.py" (lines 240-318 of PATTERNS file — gives exact deltas for Analogs 1-4)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md §D-02, §D-04
  </read_first>
  <action>
    Per D-02 + D-04 + PATTERNS Analogs 1-4:
    (1) Add `"endgame_type_achievable_score_gap"` literal to the `MetricId` Literal in `endgame_zones.py`, placed adjacent to the existing `"achievable_score_gap"` entry. Include the comment block from PATTERNS:
        `# User-facing label is "Endgame Type Score Gap" (concepts) / "Score Gap" (card row). Internal name retains "achievable" to mark the math-family with achievable_score_gap (Phase 85.1).`
    (2) Add sibling `ZONE_REGISTRY` entry `"endgame_type_achievable_score_gap": ZoneSpec(typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better")` directly below the existing `"achievable_score_gap"` ZoneSpec. Comment: `# Phase 87.1 (SEED-016 D-04): per-span, per-type version. Placeholder band ±5pp matches the global achievable_score_gap until §3.4.2 benchmark calibration produces per-class values.`
    (3) Extend the `PerClassBands` dataclass with a new field `achievable_score_gap: tuple[float, float]` after `recovery`. Update the dataclass docstring from "Conversion and Recovery" to "Conversion, Recovery, and Score Gap". Add inline comment `# Phase 87.1 - SEED-016 D-04`.
    (4) Update every entry in `PER_CLASS_GAUGE_ZONES` (rook, minor_piece, pawn, queen, mixed, pawnless) with `achievable_score_gap=(-0.05, 0.05)`. Comment on each line: `# Phase 87.1 placeholder - update after §3.4.2`.
    (5) Do NOT add to `BucketedMetricId` — the metric is per-class only, not per-(class × axis) yet. If benchmark §3.4.2 later requires per-rating-bucket bands, that change goes in a follow-up. Add a comment near `BucketedMetricId` noting the deferral.
    (6) Verify `assign_zone` dispatches correctly via `ZONE_REGISTRY` lookup — no code change needed (per RESEARCH §D-04 Analog 5).
    (7) Run `uv run ty check app/ tests/` — must exit 0. The frozen dataclass + new tuple field is ty-safe.
  </action>
  <verify>
    <automated>uv run ty check app/services/endgame_zones.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"endgame_type_achievable_score_gap"' app/services/endgame_zones.py` returns >= 2 (MetricId literal + ZONE_REGISTRY key)
    - `grep -c 'achievable_score_gap: tuple\[float, float\]' app/services/endgame_zones.py` returns >= 1 (PerClassBands field)
    - `grep -c 'achievable_score_gap=(-0.05, 0.05)' app/services/endgame_zones.py` returns 6 (one per class in PER_CLASS_GAUGE_ZONES)
    - `uv run ty check app/services/endgame_zones.py` exits 0
    - `uv run python -c "from app.services.endgame_zones import ZONE_REGISTRY, PER_CLASS_GAUGE_ZONES; assert 'endgame_type_achievable_score_gap' in ZONE_REGISTRY; assert PER_CLASS_GAUGE_ZONES['rook'].achievable_score_gap == (-0.05, 0.05)"` exits 0
  </acceptance_criteria>
  <done>
    MetricId literal + ZONE_REGISTRY entry present; PerClassBands has the new field; all 6 PER_CLASS_GAUGE_ZONES entries carry placeholder bands; ty clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend gen_endgame_zones_ts.py and regenerate endgameZones.ts</name>
  <files>scripts/gen_endgame_zones_ts.py, frontend/src/generated/endgameZones.ts</files>
  <read_first>
    - scripts/gen_endgame_zones_ts.py (current `_render`, `_format_per_class_gauge_zones`, import block, and existing ACHIEVABLE_SCORE_GAP_NEUTRAL_MIN/MAX emit)
    - frontend/src/generated/endgameZones.ts (current export list — to confirm placement of new constants and existing PER_CLASS_GAUGE_ZONES shape)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"scripts/gen_endgame_zones_ts.py" (Deltas 1-4)
  </read_first>
  <action>
    Per PATTERNS Deltas 1-4:
    (1) In `_format_per_class_gauge_zones`, destructure the new `achievable_score_gap` band: `g_lo, g_hi = bands.achievable_score_gap`. Update the emitted TS object line to include `achievable_score_gap: [{g_lo}, {g_hi}]` alongside the existing `conversion` and `recovery` entries.
    (2) In `_render` (or wherever the existing `ACHIEVABLE_SCORE_GAP_NEUTRAL_MIN/MAX` emit lives), add a local spec alias `_ENDGAME_TYPE_SCORE_GAP_SPEC = ZONE_REGISTRY["endgame_type_achievable_score_gap"]`. Emit two new export lines immediately after the existing `ACHIEVABLE_SCORE_GAP_NEUTRAL_MAX` line:
        - `export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN = {_ENDGAME_TYPE_SCORE_GAP_SPEC.typical_lower};`
        - `export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX = {_ENDGAME_TYPE_SCORE_GAP_SPEC.typical_upper};`
    (3) Update the TS-side inline doc / type comment in `PER_CLASS_GAUGE_ZONES` block: add `achievable_score_gap: [lower, upper]` to the inline "Each entry" doc comment so the generated TS file's TypeScript type continues to match the emitted object shape (per PATTERNS Delta 4).
    (4) Run `uv run python scripts/gen_endgame_zones_ts.py` to regenerate `frontend/src/generated/endgameZones.ts`. Verify the file is updated.
    (5) Run `uv run python scripts/gen_endgame_zones_ts.py --check` — must exit 0 (no drift remaining after regeneration).
    (6) Run `cd frontend && npm run build` — must succeed; tsc will fail if the regenerated file is malformed.
    (7) Do not hand-edit `frontend/src/generated/endgameZones.ts` — it is codegen output per CLAUDE.md and RESEARCH §Pitfall 4.
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py --check &amp;&amp; grep -c 'ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/gen_endgame_zones_ts.py --check` exits 0 (codegen drift gate green)
    - `grep -c 'export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'achievable_score_gap' frontend/src/generated/endgameZones.ts` returns >= 6 (one per class in PER_CLASS_GAUGE_ZONES)
    - `cd frontend &amp;&amp; npm run build` succeeds (tsc compiles the regenerated file)
  </acceptance_criteria>
  <done>
    Codegen script emits the new constants and per-class band; regenerated TS file commits cleanly; drift check passes; frontend build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add §3.4.2 Per-span Score Gap by Endgame Type to benchmarks SKILL.md</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md §3.1.5 (Achievable Score Gap subchapter — the structural template; PATTERNS Analog cites lines 972-1050)
    - .claude/skills/benchmarks/SKILL.md §3.4.1 (existing per-class Conv/Recov subchapter — insertion point is right after this)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §".claude/skills/benchmarks/SKILL.md" (full §3.4.2 template + SQL shape)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md §D-04 "SKILL.md addition: §3.4.2"
  </read_first>
  <action>
    Append §3.4.2 to `.claude/skills/benchmarks/SKILL.md` immediately after the existing §3.4.1 subchapter, using the structural template from §3.1.5 adapted for per-span × per-class grain. Per PATTERNS §"app/services/insights_llm.py" sibling §"benchmarks/SKILL.md":
    (1) Heading: `#### 3.4.2 Per-span Score Gap by Endgame Type (Phase 87.1 SEED-016)`.
    (2) Question paragraph: "How does `per_span_achievable_score_gap_by_class = mean(gap_span)` distribute per user per endgame class, and does it shift across (TC × ELO) cells? Calibrates `PER_CLASS_GAUGE_ZONES[cls].achievable_score_gap` bands and the global `ZONE_REGISTRY["endgame_type_achievable_score_gap"]` ZoneSpec."
    (3) Per-user metric definition (per class): one row per qualifying span `(game_id, endgame_class)` with ≥6 plies; `ES_entry = eval_cp_to_expected_score(entry_eval_cp, user_color)`; transitory `exit_score = ES_sigmoid(next_entry_eval_cp, user_color)` via LEAD() window; terminal `exit_score = {win:1.0, draw:0.5, loss:0.0}` from `game.result × game.user_color`; `gap_span = exit_score − ES_entry` (positive = outperformed); both-NULL `entry_eval_cp/mate` excluded; `per_span_achievable_score_gap_by_class = mean(gap_span)` per user per class. Reference D-07 sign convention explicitly.
    (4) Sample floor: ≥20 qualifying spans per user per class per cell (mirroring §3.1.5 game floor).
    (5) Cohen's-d collapse verdict per axis (TC, ELO) — same 3-level pattern as §3.1.5. Document the decision rule: if both axes collapse, set `PER_CLASS_GAUGE_ZONES[cls].achievable_score_gap` identically across all 6 classes (or keep a single global `ZoneSpec` entry); if "keep separate" on ELO, stratify per class.
    (6) Output instructions: update `PER_CLASS_GAUGE_ZONES` entries and/or `ZONE_REGISTRY["endgame_type_achievable_score_gap"]` in `app/services/endgame_zones.py`; run `uv run python scripts/gen_endgame_zones_ts.py` and commit the regenerated `endgameZones.ts`.
    (7) Embed the SQL shape template from PATTERNS §"benchmarks/SKILL.md" §3.4.2 (the `WITH selected_users / spans / spans_with_next / gap_rows / per_user_class` CTE chain). Equal-footing opponent filter (`abs(opp_rating - user_rating) <= 100`) preserved per memory feedback_260503-fef.
    (8) Note: actually running the benchmark query is OUT OF SCOPE for this task — the SKILL.md update documents the method so a future calibration run can produce real bands. Placeholder bands from Task 1 remain in place until then.
  </action>
  <verify>
    <automated>grep -c '#### 3.4.2 Per-span Score Gap by Endgame Type' .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#### 3.4.2 Per-span Score Gap by Endgame Type' .claude/skills/benchmarks/SKILL.md` returns 1
    - `grep -c 'per_span_achievable_score_gap_by_class' .claude/skills/benchmarks/SKILL.md` returns >= 2 (definition + output section)
    - `grep -c 'gap_span = exit_score' .claude/skills/benchmarks/SKILL.md` returns >= 1 (sign convention recorded inline)
    - `grep -c 'LEAD' .claude/skills/benchmarks/SKILL.md | head -1` returns >= 1 (SQL template references the window function)
    - `grep -c '≥20 qualifying spans' .claude/skills/benchmarks/SKILL.md` returns >= 1 (sample floor recorded)
  </acceptance_criteria>
  <done>
    §3.4.2 subchapter present in SKILL.md with question, per-user metric definition, sample floor, collapse verdict instructions, output instructions, and SQL template. Calibration run itself is deferred.
  </done>
</task>

</tasks>

<threat_model>
none — purely additive change to backend zones registry, codegen script, and a project-local skill markdown document. No new attack surface; no new user input flows; no authentication paths touched. The codegen script reads from Python registry and writes a TypeScript file in the build pipeline only.
</threat_model>

<verification>
- `uv run ty check app/ tests/` exits 0
- `uv run python scripts/gen_endgame_zones_ts.py --check` exits 0
- `cd frontend && npm run build` succeeds
- `grep -c 'endgame_type_achievable_score_gap' app/services/endgame_zones.py` >= 2
- `grep -c 'ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts` == 1
- `grep -c '#### 3.4.2' .claude/skills/benchmarks/SKILL.md` >= 1
</verification>

<success_criteria>
ROADMAP §4 satisfied: zone codegen reflects the new per-type per-class metric; registry surface ready for Plan 03 (frontend constants) and Plan 04 (LLM payload's `assign_zone` dispatch).
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-01-SUMMARY.md`.
</output>
