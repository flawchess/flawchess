---
phase: 87.1
plan: 03
type: execute
wave: 2
depends_on:
  - 87.1-01   # consumes ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX from generated/endgameZones.ts
  - 87.1-02   # consumes 5 new EndgameCategoryStats schema fields
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameTypeCard.tsx
  - frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx
autonomous: true
requirements:
  - ROADMAP-87.1-§5   # Per-type ScoreGapRow rendered on EndgameTypeCard with required testids and sigmoid-bias caveat
tags:
  - frontend
  - card
  - endgame
  - popover
must_haves:
  truths:
    - "EndgameTypeCard renders a Score Gap row directly between the gauge row and the WDL bar"
    - "ScoreGapRow consumes ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX from generated/endgameZones.ts"
    - "MetricStatPopover explanation copy contains the sigmoid-bias caveat one-liner"
    - "Card row label reads 'Score Gap' (short form per D-02)"
    - "Testid sub-elements exist: ${tileTestId}-asg-bullet, ${tileTestId}-asg-value, ${tileTestId}-asg-info"
    - "Sign convention: positive gap renders right of zero with green tint when above neutralMax"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "TS mirror of 5 new EndgameCategoryStats schema fields"
      contains: "type_achievable_score_gap_mean"
    - path: "frontend/src/components/charts/EndgameTypeCard.tsx"
      provides: "ScoreGapRow inserted between the gauge row and the WDL bar, wired to per-class data"
      contains: "ScoreGapRow"
    - path: "frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx"
      provides: "Render + testid + popover + sign-convention assertions for the new row"
      contains: "type_achievable_score_gap"
  key_links:
    - from: "frontend/src/components/charts/EndgameTypeCard.tsx"
      to: "frontend/src/components/charts/EndgameOverallScoreGapRow.tsx"
      via: "Named import `ScoreGapRow`"
      pattern: "import \\{ ScoreGapRow \\} from"
    - from: "frontend/src/components/charts/EndgameTypeCard.tsx"
      to: "frontend/src/generated/endgameZones.ts"
      via: "Named imports of ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX"
      pattern: "ENDGAME_TYPE_SCORE_GAP_NEUTRAL_(MIN|MAX)"
    - from: "frontend/src/components/charts/EndgameTypeCard.tsx"
      to: "frontend/src/components/popovers/MetricStatPopover.tsx"
      via: "Reused consolidated popover with vocabulary='score'"
      pattern: "MetricStatPopover"
---

<!--
Requirement IDs reference ROADMAP §Phase 87.1 success criteria (REQUIREMENTS.md
has no DELTA-NN entries for Phase 87.1 yet — RESEARCH §REQUIREMENTS.md Gap;
ROADMAP success criteria 1..6 are the source of truth).
-->

<objective>
Extend the Phase 87 `EndgameTypeCard` with one additional `ScoreGapRow` bullet positioned
directly between the Conv|Recov gauge row and the WDL bar (per RESEARCH §"Critical Phase 87
Drift" — Phase 87 shipped a single chess-score bullet, not two peer-diff bullets; the new row
slots above the WDL bar block). Lift the visual primitive verbatim from
`EndgameOverallScoreGapRow.tsx` — no new component, no reinvented shape. Wire the
`MetricStatPopover` with the dual-label copy (card-row label "Score Gap", popover explanation
referencing "page-level Achievable Score Gap" family) and append the mandatory sigmoid-bias
caveat one-liner per D-09.

Purpose: surfaces the per-type Score Gap metric to users on every rendered endgame class card.
Mirrors the page-level Achievable Score Gap visual exactly — same `ScoreGapRow`, same
`MetricStatPopover` shell, same zone-only tint convention from Phase 85.1 D-04.

Output: 5 new TS type fields, new ScoreGapRow row + popover wiring per `EndgameTypeCard`, Vitest
coverage of render + testids + popover content + sign-convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-VALIDATION.md
@.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md
@frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/components/charts/EndgameTypeCard.tsx

<interfaces>
<!-- Existing contracts consumed -->

From frontend/src/components/charts/EndgameOverallScoreGapRow.tsx:
```
interface ScoreGapRowProps {
  label: string;
  value: number;
  formatted: string;
  resultColor: string | undefined;
  valueTestId: string;
  ariaLabel: string;
  neutralMin: number;
  neutralMax: number;
  tooltip?: ReactNode;
  valueClassName?: string;
  ciLow?: number;
  ciHigh?: number;
}
export function ScoreGapRow(props: ScoreGapRowProps): JSX.Element;
```

From frontend/src/generated/endgameZones.ts (produced by Plan 01):
```
export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN = -0.05;
export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX =  0.05;
PER_CLASS_GAUGE_ZONES.{rook,minor_piece,pawn,queen,mixed,pawnless}.achievable_score_gap: [number, number]
```

From frontend/src/types/endgames.ts (post-edit, mirrors Plan 02 schema):
```
interface EndgameCategoryStats {
  // ...existing fields...
  type_achievable_score_gap_mean:   number | null;
  type_achievable_score_gap_n:      number | null;
  type_achievable_score_gap_p_value: number | null;
  type_achievable_score_gap_ci_low:  number | null;
  type_achievable_score_gap_ci_high: number | null;
}
```

From frontend/src/components/popovers/MetricStatPopover.tsx (consolidated, post-260514-i3l):
```
props: { name, explanation, value, baseline, unit, gameCount, level, pValue,
         vocabulary, neutralLower, neutralUpper, baselineLabel, methodology, testId, ariaLabel }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror 5 new schema fields into frontend/src/types/endgames.ts</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts (current `EndgameCategoryStats` interface lines ~25-55; existing `EndgamePerformanceResponse` `achievable_score_gap_*` cluster as analog at lines 82-88)
    - app/schemas/endgames.py post-Plan-02 (the 5 new fields the TS interface mirrors)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"frontend/src/types/endgames.ts" (exact delta)
  </read_first>
  <action>
    Per PATTERNS §"frontend/src/types/endgames.ts":
    (1) Extend the `EndgameCategoryStats` interface with 5 new fields after the existing `score_p_value` line:
        ```
        type_achievable_score_gap_mean:   number | null;
        type_achievable_score_gap_n:      number | null;
        type_achievable_score_gap_p_value: number | null;
        type_achievable_score_gap_ci_low:  number | null;
        type_achievable_score_gap_ci_high: number | null;
        ```
    (2) Add the comment block from PATTERNS above the cluster explaining the dual-label rule: "Phase 87.1 (SEED-016, D-05): per-span mean Score Gap for this endgame type. User-facing label: 'Score Gap' (card row) / 'Endgame Type Score Gap' (concepts). Internal name retains 'achievable' to preserve grep-ability with Phase 85.1."
    (3) Field types are `number | null` (TS `null`, not `undefined`) — mirrors backend's `float | None` / `int | None` over JSON.
    (4) Do NOT change `EndgameOverallStats.achievable_score_gap*` — that is the page-level metric and remains untouched.
    (5) Run `cd frontend && npx tsc --noEmit` — must exit 0.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type_achievable_score_gap_mean' frontend/src/types/endgames.ts` returns >= 1
    - `grep -c 'type_achievable_score_gap_' frontend/src/types/endgames.ts` returns >= 5 (all 5 new fields)
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0
    - No new `?` optional markers (fields use explicit `| null` shape per project convention)
  </acceptance_criteria>
  <done>
    TS interface mirrors backend schema; tsc clean; no orphan exports introduced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ScoreGapRow row to EndgameTypeCard.tsx between gauges and WDL bar</name>
  <files>frontend/src/components/charts/EndgameTypeCard.tsx, frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameTypeCard.tsx (full file — note shipped structure per RESEARCH §"Critical Phase 87 Drift": title + InfoPopover + n-chip → gauge row → WDL bar with Games link or standalone Games link → single chess-score bullet)
    - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (ScoreGapRow component to lift verbatim — named export, not default)
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (Achievable Score Gap wiring at lines ~170-208 — the exact prop set + MetricStatPopover call site to mirror; `gapZoneColor` helper at lines 52-56)
    - frontend/src/components/popovers/MetricStatPopover.tsx (consolidated popover shell — confirm prop names)
    - frontend/src/components/popovers/MetricStatTooltip.tsx (tooltip body — see what `vocabulary="score"` produces)
    - frontend/src/generated/endgameZones.ts (post-Plan-01 — confirm ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX exports)
    - frontend/src/lib/theme.ts (ZONE_DANGER, ZONE_SUCCESS constants)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"frontend/src/components/charts/EndgameTypeCard.tsx" (full JSX delta + import block + insertion-point guidance)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md §D-08, §D-09
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md §"Critical Phase 87 Drift: Shipped Card Structure" (insertion point clarification)
  </read_first>
  <behavior>
    - Test: ScoreGapRow renders inside EndgameTypeCard when `category.type_achievable_score_gap_n > 0`.
    - Test: ScoreGapRow is hidden (not in DOM) when `category.type_achievable_score_gap_n === 0`.
    - Test: positive `type_achievable_score_gap_mean` outside neutral band renders with `ZONE_SUCCESS` color; negative outside band renders with `ZONE_DANGER`; value inside band renders with neutral color (resultColor undefined).
    - Test: the row sits in DOM order between the gauge row container and the WDL bar / Games link block (verifiable via DOM query for `*-gauges` next sibling).
    - Test: `${tileTestId}-asg-bullet` wrapper exists; `${tileTestId}-asg-value` is the value span inside ScoreGapRow; `${tileTestId}-asg-info` is the MetricStatPopover trigger.
    - Test: MetricStatPopover explanation copy contains the literal sigmoid-bias caveat string "Note: the Lichess sigmoid under-weights endgame eval advantages".
    - Test: MetricStatPopover `name` prop is the string "Score Gap" (short form per D-02, NOT "Endgame Type Score Gap" — that label belongs in the explanation prose only).
    - Test: at n >= CONFIDENCE_MIN_N (n=10), CI whiskers are present (ScoreGapRow receives non-undefined `ciLow` and `ciHigh`).
    - Test: at n < 2, CI whiskers absent (ciLow/ciHigh fall back to `undefined` via `?? undefined`).
  </behavior>
  <action>
    Per PATTERNS §"frontend/src/components/charts/EndgameTypeCard.tsx" + CONTEXT §D-08/D-09 + RESEARCH §"Critical Phase 87 Drift":
    (1) Imports at top of `EndgameTypeCard.tsx`:
        - `import { ScoreGapRow } from './EndgameOverallScoreGapRow';` (named export, not default).
        - `import { ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN, ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX } from '@/generated/endgameZones';` (alongside the existing PER_CLASS_GAUGE_ZONES import).
        - If `ZONE_DANGER` and `ZONE_SUCCESS` from `@/lib/theme` are not already imported, add them.
        - `MetricStatPopover` is already used in Phase 87's card; reuse the existing import.
        - Add a one-line comment on the constants import: `// Constant names use user-facing 'ENDGAME_TYPE_SCORE_GAP' form; internal identifier is 'endgame_type_achievable_score_gap' (see app/services/endgame_zones.py).`
    (2) In the component body, after the existing `total`/`level` derivations, add the gap derivation block (per PATTERNS):
        ```
        const gapMean = category.type_achievable_score_gap_mean;
        const gapN = category.type_achievable_score_gap_n ?? 0;
        const showGapRow = gapN > 0;
        const gapFormatted = gapMean != null
          ? (gapMean >= 0 ? '+' : '') + `${Math.round(gapMean * 100)}%`
          : '—';
        const gapColor = gapMean != null
          ? (gapMean < ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN
              ? ZONE_DANGER
              : (gapMean >= ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX ? ZONE_SUCCESS : undefined))
          : undefined;
        const gapLevel = deriveLevel(category.type_achievable_score_gap_p_value, gapN);
        ```
        Per Phase 85.1 D-04: zone-only tint, no separate p-value gate on the row's font color. The popover surfaces the significance signal via `gapLevel` informationally.
    (3) Insert the JSX block per PATTERNS, positioned between the gauge row container's closing `</div>` and the WDL bar / Games link block. Per RESEARCH §"Critical Phase 87 Drift": this is "between gauges and WDL bar", NOT "below peer bullets" (peer bullets don't exist in the shipped card).
    (4) ScoreGapRow props (per PATTERNS exact JSX):
        - `label="Score Gap:"` (D-02 short form for the card row).
        - `value={gapMean ?? 0}`.
        - `formatted={gapFormatted}`.
        - `resultColor={gapColor}` (zone-only — undefined when value in neutral band).
        - `valueTestId={`${tileTestId}-asg-value`}`.
        - `ariaLabel={`${category.label} Score Gap: ${gapFormatted}`}`.
        - `neutralMin={ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN}`, `neutralMax={ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX}` (use the global constants; per RESEARCH Open Question #1, start with global — switch to `PER_CLASS_GAUGE_ZONES[class].achievable_score_gap` only if benchmark §3.4.2 produces large per-class variation).
        - `ciLow={category.type_achievable_score_gap_ci_low ?? undefined}`, `ciHigh={category.type_achievable_score_gap_ci_high ?? undefined}`.
        - `tooltip={<MetricStatPopover ... />}` per (5) below.
    (5) MetricStatPopover wiring (per CONTEXT §D-09 + PATTERNS):
        - `name="Score Gap"` (D-02 short form — matches the card row label the user just clicked from).
        - `explanation` prop: the full D-09 copy block — `"Your average per-span Score Gap for ${category.label} games — how much your mid-span and end-of-span outcomes outperformed (or fell short of) the Stockfish baseline you started each ${category.label} span from. Positive = you converted advantages and salvaged disadvantages above expectation. Negative = you gave back expected score during these spans. Computed per span (one per game × endgame type) and averaged; terminal spans use the game result, transitory spans use the next span's entry eval. This is the per-span, per-type version of the page-level Achievable Score Gap. Note: the Lichess sigmoid under-weights endgame eval advantages, so absolute values are scale-compressed; rely on the zone bands, not the raw magnitude."`
        - `value={gapMean ?? 0}`, `baseline={0}`, `baselineLabel="0%"`, `unit="percent"`, `gameCount={gapN}`, `level={gapLevel}`, `pValue={category.type_achievable_score_gap_p_value}`, `vocabulary="score"`.
        - `neutralLower={ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN}`, `neutralUpper={ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX}`.
        - `methodology` prop: 3-line description ("Score: wins + ½ draws (game result for terminal spans). / Test: paired one-sample z-test on per-span (exit − entry expected) diffs vs 0. / Confidence interval: 95% normal-approx on the paired diffs."). Use `<br />` separators inside the React fragment.
        - `testId={`${tileTestId}-asg-info`}`, `ariaLabel={`What is ${category.label} Score Gap?`}`.
    (6) Wrap the row in a `<div data-testid={`${tileTestId}-asg-bullet`}>...</div>` (per D-08 testid spec). Render the wrapper only when `showGapRow` is true.
    (7) No em-dashes in the user-facing strings per CLAUDE.md communication style. The PATTERNS explanation copy already uses commas / periods — verify when copying.
    (8) Minimum font size: ScoreGapRow already uses `text-sm` per the component file inspection; no `text-xs` introduced. MetricStatPopover body is exempt per CLAUDE.md exception.
    (9) Tests in `__tests__/EndgameTypeCard.test.tsx` — extend with the 9 behavior assertions above. Use the existing fixture factory in that test file (mirror the pattern used for the existing chess-score bullet tests). Assert DOM ordering by querying for `*-gauges` element and confirming the gap-bullet wrapper is its `nextElementSibling`.
    (10) Run `cd frontend && npm test -- --run EndgameTypeCard && npm run lint && npm run knip && npx tsc --noEmit && npm run build` — all must pass.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run EndgameTypeCard &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ScoreGapRow' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 2 (import + JSX)
    - `grep -c 'ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 2 (import + JSX)
    - `grep -c '\\-asg\\-bullet' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 1
    - `grep -c '\\-asg\\-value' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 1
    - `grep -c '\\-asg\\-info' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 1
    - `grep -c 'Lichess sigmoid under-weights' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 1 (sigmoid-bias caveat present in explanation copy)
    - `grep -c '"Score Gap:"' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 1 (card row label short form per D-02; trailing colon matches Achievable Score Gap analog)
    - `grep -c 'name="Score Gap"' frontend/src/components/charts/EndgameTypeCard.tsx` returns >= 1 (popover `name` prop)
    - `cd frontend &amp;&amp; npm test -- --run EndgameTypeCard` exits 0 (all new assertions green)
    - `cd frontend &amp;&amp; npm run lint` exits 0
    - `cd frontend &amp;&amp; npm run knip` exits 0 (no orphan exports introduced)
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0
    - DOM order assertion in Vitest passes: `data-testid="${tileTestId}-asg-bullet"` is `nextElementSibling` of the gauge row container (or sits before the WDL bar in DOM)
  </acceptance_criteria>
  <done>
    Per-class card renders one Score Gap bullet row between gauges and WDL bar; row hidden at n=0; popover content includes sigmoid-bias caveat + dual-label rationale; testids match D-08; Vitest + lint + knip + tsc clean.
  </done>
</task>

</tasks>

<threat_model>
none — purely additive read-path UI render on existing authenticated routes. The new row consumes data already returned by `/api/endgames/overview` (Plan 02 added the fields server-side). No new user input, no new client-side state, no new XHR; no new attack surface.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameTypeCard` exits 0
- `cd frontend && npm run lint && npm run knip && npx tsc --noEmit && npm run build` all succeed
- Manual check: open Endgames page with rendered per-type cards; confirm Score Gap row sits in the slot between gauges and WDL bar; popover trigger accessible; sigmoid-bias caveat one-liner reads naturally — note in 87.1-HUMAN-UAT.md if produced
- Sign convention visually correct: positive value renders right of zero with green tint above the neutral band
</verification>

<success_criteria>
ROADMAP §5 satisfied: every rendered `EndgameTypeCard` carries the new Score Gap row with required testids, the dual-label popover copy (D-02), the sigmoid-bias caveat (D-09), and zone-only tinting (Phase 85.1 D-04 convention).
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-03-SUMMARY.md`.
Also append the 375px parity check + sign-convention visual check + popover-caveat check from VALIDATION §"Manual-Only Verifications" into a new `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-HUMAN-UAT.md`.
</output>
