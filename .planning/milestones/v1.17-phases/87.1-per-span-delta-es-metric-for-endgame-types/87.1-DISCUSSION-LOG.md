# Phase 87.1: Per-span ΔES metric for endgame types — Discussion Log

**Date:** 2026-05-15
**Mode:** discuss (standard, single-pass with autonomy directive)

## Context loaded

- `.planning/PROJECT.md`, `REQUIREMENTS.md`, `STATE.md`
- `.planning/milestones/v1.17-ROADMAP.md` §Phase 87.1 (success criteria + scope-risk notes)
- `.planning/seeds/SEED-016-per-span-eval-delta-endgame-metric.md` (formula, sign, span definitions, eval coverage, discarded alternatives)
- `.planning/notes/lichess-sigmoid-endgame-calibration.md` (sigmoid-bias caveat, refit follow-up)
- `.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md` (the per-type card surface this phase extends)
- `.planning/milestones/v1.17-phases/85.1-hypothesis-tests-and-cis-for-endgame-score-differences/85.1-DECISIONS.md` (page-level Achievable Score Gap precedent)
- `app/services/score_confidence.py:338` `compute_paired_difference_test` (the locked stat helper)
- `app/services/endgame_zones.py` (`MetricId` registry, existing `achievable_score_gap` ZoneSpec)
- `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` (page-level ScoreGapRow precedent)

## Memory applied

- `feedback_llm_prompt_design.md` — LLM payload reuses Phase 82 type_breakdown shape, inline zone bounds, no precomputed stories.
- `feedback_llm_significance_signal.md` — no separate verdict / p_value field in LLM payload; tighten the cohort band instead.
- `feedback_wilson_chess_score.md` — trust the established stat method (here: `compute_paired_difference_test`); no methodology editorializing in design docs.
- `feedback_zone_band_judgement.md` — tighten band when small effects are meaningful (likely relevant for this metric given sigmoid compression).
- `feedback_mobile_layout_scope.md` — inside-card layout, no outer-grid restructuring.

## Questions presented and answers

### Q1: Card layout — where does the ΔES gauge sit?
**Options presented:**
- 3-up gauge row (Conv | Recov | ΔES)
- ΔES below the Conv | Recov gauge row
- ΔES + peer bullet row (replace one of Conv/Recov peer bullet styles)

**User selected:** **ΔES below the Conv | Recov gauge row**

Subsequently overridden by Q3 (visual treatment redirected to `ScoreGapRow` bullet, not a gauge). Net effect: a horizontal bullet row directly below the gauges. See CONTEXT D-01.

### Q2: Repo query strategy — extend `query_endgame_entry_rows` vs sibling?
**Options presented:**
- Extend in place with next_entry_eval columns
- Add sibling `query_endgame_span_delta_rows`
- Defer to researcher / planner

**User selected:** **Defer to researcher / planner.**

CONTEXT D-06 captures both options and the criteria the planner should weigh (tuple-shape lock-in, window-function cost, single-pass vs two-pass aggregation).

### Q3: Peer comparison frame for ΔES?
**Options presented:**
- Self-vs-zone only (no peer bullet)
- Self-vs-cohort-p50 bullet (cohort baseline)
- Self-vs-zero bullet (Stockfish-baseline reference)

**User selected:** **"Use the same stats approach and visual display as for the Achievable Score Gap metric"**

Resolved to self-vs-zero bullet via the existing `ScoreGapRow` pattern (page-level Achievable Score Gap is `actual − expected` centred on 0 with CI whiskers and percentile-zoned neutral band). Captured in CONTEXT D-01, D-03, D-08. Sign convention flipped from SEED-016's `ES_entry − exit_score` to `exit_score − ES_entry` so positive = user outperformed, matching the existing metric's `higher_is_better` direction.

### Q4: User-facing label / tooltip framing?
**Options presented:**
- "Score swing"
- "Per-span score delta" / "ΔES"
- "Endgame play quality"

**User selected:** **"Reuse 'Achievable Score Gap' as bullet chart and metric label. Internally, use 'Endgame Type Achievable Score Gap' to distinguish it from 'Endgame Achievable Score Gap'."**

Captured in CONTEXT D-02. UI label = "Achievable Score Gap" (verbatim, matches the existing page-level row). Internal `MetricId` = `endgame_type_achievable_score_gap`. Schema field prefix = `type_achievable_score_gap_*` (per-class scope disambiguator).

## Decisions Claude made under the autonomy directive (no clarifying question asked)

- **Sign convention flip** (CONTEXT D-03 §Sign convention): user said "use the same stats approach and visual display"; for the visual to read identically the sign must be flipped from SEED-016's convention. Locked as `exit_score − ES_entry`.
- **Sigmoid-bias caveat one-liner** in every per-class `MetricStatPopover` (CONTEXT D-09): mirrors the Phase 85.1 04-PLAN treatment for the page-level metric. Mandatory per the calibration note + the existing caveat-on-page-level-metric precedent.
- **LLM payload field shape**: `{ mean, n, zone, neutral_band }` per class (CONTEXT D-10). No `verdict` / `p_value` field per the memory feedback.
- **Prompt version bump** required (CONTEXT D-10): consistent with Phase 82 type_breakdown precedent.
- **Sparse-handling thresholds**: reuse `CONFIDENCE_MIN_N = 10` (already inside the helper); reuse Phase 87 D-13 `opacity-50` for `n == 0` cards. No new threshold constants.
- **Codegen ordering**: Phase 87 lands first; Phase 87.1 layers additively on top. Re-base if Phase 87 hasn't merged when planning starts.

## Deferred ideas captured

- Endgame-specific sigmoid refit (research follow-up tracked in calibration note).
- Strict contiguous-run span definition.
- Sub-6-ply spans / lowering `ENDGAME_PLY_THRESHOLD`.
- Per-move ΔES normalisation.
- Atomic sigmoid cutover across all ES-derived metrics (pre-condition for the refit follow-up).

## Anti-patterns avoided

- No new stat helper introduced — reused the locked `compute_paired_difference_test` per `feedback_wilson_chess_score.md`.
- No separate sig-test verdict in LLM payload per `feedback_llm_significance_signal.md`.
- No outer-grid restructuring per `feedback_mobile_layout_scope.md`.
- No design-doc methodology editorializing.
- No new visual primitive — reused `MiniBulletChart` / `ScoreGapRow`.

## Next step

`/clear` then `/gsd-plan-phase 87.1`

---

*Phase: 87.1-per-span-delta-es-metric-for-endgame-types*
*Discussion logged: 2026-05-15*
