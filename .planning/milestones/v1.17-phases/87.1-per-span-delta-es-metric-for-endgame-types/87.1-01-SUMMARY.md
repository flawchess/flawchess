---
phase: 87.1
plan: 01
subsystem: zones-registry
tags: [benchmarks, codegen, zones, endgame, metric-registry]
dependency_graph:
  requires: []
  provides:
    - "ZONE_REGISTRY['endgame_type_achievable_score_gap'] ZoneSpec"
    - "PerClassBands.achievable_score_gap field on all 6 PER_CLASS_GAUGE_ZONES entries"
    - "ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX TS constants"
    - "Per-class achievable_score_gap band in generated PER_CLASS_GAUGE_ZONES TS literal"
    - ".claude/skills/benchmarks/SKILL.md §3.4.2 (per-span Score Gap by endgame type)"
  affects:
    - "Plan 03 (frontend EndgameTypeCard) — consumes ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX + PER_CLASS_GAUGE_ZONES.*.achievable_score_gap"
    - "Plan 04 (insights_llm.py) — dispatches via assign_zone('endgame_type_achievable_score_gap', ...) and reads ZONE_REGISTRY entry typical_lower/upper for the LLM payload's neutral_band field"
tech-stack:
  added: []
  patterns:
    - "Frozen-dataclass per-class registry (PerClassBands gains a third tuple field)"
    - "Sibling ZoneSpec entry next to achievable_score_gap (same band shape, distinct key)"
    - "Codegen-emitted constant naming uses user-facing label form (ENDGAME_TYPE_SCORE_GAP_*) while internal Python key keeps math-family name (endgame_type_achievable_score_gap)"
    - "SKILL.md §3.4.2 subchapter structured to match §3.1.5 (question, per-user metric, sample floor, Cohen's-d collapse, SQL template, output instructions)"
key-files:
  created:
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-01-SUMMARY.md
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/deferred-items.md
  modified:
    - app/services/endgame_zones.py
    - scripts/gen_endgame_zones_ts.py
    - frontend/src/generated/endgameZones.ts
    - tests/services/test_endgame_zones.py
    - .claude/skills/benchmarks/SKILL.md
decisions:
  - "Used relative paths for all Edit/Write operations after discovering the absolute-path-via-pwd anti-pattern silently routed initial edits to the main repo instead of the worktree (#3099 — see Deviations below)"
  - "Placeholder bands (-0.05, 0.05) shipped uniformly across all 6 PER_CLASS_GAUGE_ZONES entries mirror the global ZONE_REGISTRY band; intentional per CONTEXT D-04 until SKILL.md §3.4.2 calibration produces real values"
  - "Test pin (test_all_scalar_metrics_have_entries) updated to include the new metric id — this is a Rule 3 fix (blocking issue directly caused by adding to the MetricId Literal)"
  - "Codegen drift check uses git diff --exit-code (per script docstring), NOT a --check flag — the plan's mention of `--check` was inaccurate; script has no such flag"
metrics:
  duration_minutes: ~50
  completed_date: 2026-05-15
  tasks_completed: 3
  files_modified: 5
  commits: 4
---

# Phase 87.1 Plan 01: Zones Registry + Codegen + Benchmark SKILL §3.4.2 Summary

JSON-clean registry + codegen + SKILL contract for the per-span, per-type Score Gap metric, landed without touching backend math, frontend cards, or LLM payload (Plans 02-04 scope).

## What Shipped

1. **`MetricId` Literal** gains `"endgame_type_achievable_score_gap"` adjacent to `"achievable_score_gap"`, with a dual-label comment explaining the user-facing vs internal naming divergence (D-02 amended 2026-05-15).
2. **`ZONE_REGISTRY`** gains a sibling `ZoneSpec(typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better")` entry. Placeholder band mirrors the global `achievable_score_gap` until SKILL.md §3.4.2 calibration runs.
3. **`PerClassBands` dataclass** gains a third `achievable_score_gap: tuple[float, float]` field after `recovery`. Docstring updated to reflect the new field.
4. **`PER_CLASS_GAUGE_ZONES`** all 6 entries (rook, minor_piece, pawn, queen, mixed, pawnless) carry `achievable_score_gap=(-0.05, 0.05)` placeholders.
5. **`scripts/gen_endgame_zones_ts.py`** extended:
   - `_format_per_class_gauge_zones` destructures the new `achievable_score_gap` tuple per class and emits it in the TS object literal.
   - `_render` adds `_ENDGAME_TYPE_SCORE_GAP_SPEC` alias and emits two new exports: `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN` / `MAX`.
   - Inline doc comment in the regenerated TS file updated to describe the new field shape.
6. **`frontend/src/generated/endgameZones.ts`** regenerated. Contains the two new exports plus the per-class `achievable_score_gap` literal in every `PER_CLASS_GAUGE_ZONES` entry. Codegen drift check passes idempotently.
7. **`tests/services/test_endgame_zones.py`** registry-pin test updated to include the new metric id (otherwise the set-equality assertion fails). Docstring documents the SEED-016 D-02/D-04 provenance.
8. **`.claude/skills/benchmarks/SKILL.md`** §3.4.2 subchapter added immediately after §3.4.1:
   - Question + per-user metric definition (`gap_span = exit_score − ES_entry`, sign convention per CONTEXT D-07).
   - Sample floor (≥20 qualifying spans per user per class per cell).
   - Cohen's-d collapse verdict per axis (TC, ELO), with decision rule for updating bands.
   - Full SQL template (`selected_users → spans → spans_with_next via LEAD → gap_rows → per_user_class`) with universal equal-footing opponent filter.
   - Output instructions naming the registry entries to update and the codegen command.
   - Sigmoid-bias caveat recorded inline (mandatory for Plan 03/04 copy + prompt version).
   - Live-threshold grep table gains a `3.4.2` row.
   - Per-subchapter overview and Top-axis collapse summary table gain `3.4.2` entries.

## Commits

| # | Hash | Type | Description |
|---|------|------|-------------|
| 1 | `856b1e96` | feat | Add endgame_type_achievable_score_gap to zones registry (MetricId + ZONE_REGISTRY + PerClassBands.achievable_score_gap + 6 PER_CLASS_GAUGE_ZONES placeholders + BucketedMetricId deferral comment) |
| 2 | `1de2cfdc` | feat | Codegen ENDGAME_TYPE_SCORE_GAP constants + per-class band (gen_endgame_zones_ts.py extensions + regenerated endgameZones.ts) |
| 3 | `55f81782` | docs | Add §3.4.2 Per-span Score Gap by Endgame Type to benchmarks SKILL.md (subchapter + SQL template + table rows) |
| 4 | `c101fb1f` | test | Extend test_all_scalar_metrics_have_entries with new metric id (registry-pin fix) |

## Gate Outputs

| Gate | Result |
|------|--------|
| `uv run ty check app/ tests/` | All checks passed |
| `uv run ruff check app/ scripts/` | All checks passed |
| `uv run pytest tests/services/test_endgame_zones.py` | 41 passed |
| `uv run pytest tests/test_insights_schema.py tests/services/test_insights_service.py` | 120 passed |
| `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` | DRIFT_CHECK_PASS (idempotent — second run produces no diff) |
| `cd frontend && npm run lint` | clean (no eslint errors) |
| `cd frontend && npm run build` | built in 4.62s, prerender + PWA SW generated |

### Acceptance grep counts

| Pattern | File | Count |
|---------|------|------:|
| `endgame_type_achievable_score_gap` | app/services/endgame_zones.py | 3 (MetricId literal + ZONE_REGISTRY key + global-band reference in comment) |
| `achievable_score_gap: tuple\[float, float\]` | app/services/endgame_zones.py | 1 (PerClassBands field) |
| `achievable_score_gap=(-0.05, 0.05)` | app/services/endgame_zones.py | 7 (6 PER_CLASS entries + 1 comment) |
| `export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN` | frontend/src/generated/endgameZones.ts | 1 |
| `export const ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX` | frontend/src/generated/endgameZones.ts | 1 |
| `achievable_score_gap` | frontend/src/generated/endgameZones.ts | 9 (1 comment chapter heading + 1 inline doc + 1 declaration heading + 6 PER_CLASS entries) |
| `#### 3.4.2 Per-span Score Gap by Endgame Type` | .claude/skills/benchmarks/SKILL.md | 1 |
| `per_span_achievable_score_gap_by_class` | .claude/skills/benchmarks/SKILL.md | 3 |
| `gap_span = exit_score` | .claude/skills/benchmarks/SKILL.md | 2 |
| `LEAD` or `lead(` | .claude/skills/benchmarks/SKILL.md | 4 |
| `≥20 qualifying spans` | .claude/skills/benchmarks/SKILL.md | 2 |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Worktree absolute-path safety violation] Initial edits silently routed to main repo, not worktree**

- **Found during:** Task 1 (after the first ty check passed but `grep` in the worktree returned zero matches).
- **Issue:** The Edit tool, given absolute paths like `/home/aimfeld/Projects/Python/flawchess/app/services/endgame_zones.py`, wrote to the **main repo** even though the executor was operating inside a worktree at `/home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-a3f6e3ae9461787ba`. This is exactly the cwd-drift / absolute-path safety issue documented in the executor's pre-edit guard (#3099).
- **Fix:** Reverted the main repo file with `git checkout -- app/services/endgame_zones.py`, then re-applied all edits using **relative paths** so the Edit tool's path resolution lands inside the worktree. Verified per edit with `grep -c 'endgame_type_achievable_score_gap' app/services/endgame_zones.py` (returns >0) before proceeding.
- **Files modified:** app/services/endgame_zones.py (originally written to wrong path, reverted, re-applied)
- **Commit:** First successful edit shipped as part of `856b1e96`.
- **Process improvement noted:** For the remainder of execution, **all** Edit / Write calls used relative paths to avoid the same trap.

**2. [Rule 3 - Test pin breakage] `test_all_scalar_metrics_have_entries` failed after adding new MetricId**

- **Found during:** Verification sweep after Task 3.
- **Issue:** The test asserts `set(ZONE_REGISTRY.keys()) == { ... literal set ... }`. Adding `endgame_type_achievable_score_gap` to the registry broke the equality.
- **Fix:** Added the literal to the test's expected set; updated the docstring with the SEED-016 D-02/D-04 provenance.
- **Files modified:** tests/services/test_endgame_zones.py
- **Commit:** `c101fb1f`

### Auto-added Critical Functionality

**3. [Rule 2 - Missing scope-deferral comment] BucketedMetricId omission needed an explicit comment**

- **Issue:** Plan task (1)(5) requires a "comment near BucketedMetricId noting the deferral". Added inline comment block explaining that the new metric is per-class only (via PER_CLASS_GAUGE_ZONES), not per-material-axis, and that follow-up work can add it to BucketedMetricId if benchmark §3.4.2 verdict requires per-rating-bucket bands.
- **Files modified:** app/services/endgame_zones.py
- **Commit:** `856b1e96` (Task 1)

### Plan inaccuracies surfaced and worked around

**4. Plan referenced a `scripts/gen_endgame_zones_ts.py --check` flag that does not exist**

- **Plan said:** "Run `uv run python scripts/gen_endgame_zones_ts.py --check` — must exit 0 (no drift remaining after regeneration)."
- **Reality:** The script has no `--check` flag. Its docstring explicitly documents the CI drift check as `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts`.
- **Resolution:** Followed the documented CI mechanism (`gen ts && git diff --exit-code`). Verified idempotence by running the script twice; second run produces no diff. Functionally equivalent — does not block the gate.

### Authentication Gates

None encountered.

## Threat Flags

None. Plan 01 is purely additive to a Python registry, a codegen script, a generated TS constants file, a test pin, and a project-local skill markdown. No new attack surface, no new user input flow, no auth path touched, no schema fields exposed.

## Known Stubs

None introduced. The placeholder bands `(-0.05, 0.05)` are not stubs — they are functional defaults documented per CONTEXT D-04 and SKILL.md §3.4.2 with an explicit calibration path. Plans 02 / 03 / 04 do not depend on the bands being calibrated; they consume the constants as-is.

## Followups for Plans 02-04

- **Plan 03 (`EndgameTypeCard.tsx`):** Imports must be `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN` / `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX` from `@/generated/endgameZones`. Per-class bands (`PER_CLASS_GAUGE_ZONES.<class>.achievable_score_gap`) are available if Plan 03 wants per-class neutral bands instead of the global one; both shapes are wired.
- **Plan 04 (`insights_llm.py`):** Dispatch via `assign_zone("endgame_type_achievable_score_gap", mean_d)` is unchanged from Plan 04's existing dispatch pattern. The new entry resolves through `_zone_from_spec(ZONE_REGISTRY[metric_id], value)` with no additional code needed (no Analog 5 change required).
- **Benchmark calibration:** Whenever SKILL.md §3.4.2 is executed, update both `PER_CLASS_GAUGE_ZONES.*.achievable_score_gap` and `ZONE_REGISTRY["endgame_type_achievable_score_gap"]` (or just one, per the collapse verdict), then re-run `uv run python scripts/gen_endgame_zones_ts.py`. The generated file's CI drift gate will catch missing regeneration.

## TDD Gate Compliance

Plan is `type=execute` (not `type=tdd`); no RED/GREEN/REFACTOR cycle required. Test changes are reactive (registry-pin update) and committed under `test(...)` prefix per project convention.

## Self-Check: PASSED

All claimed files exist and contain the claimed content. All commits exist in git history. See acceptance grep counts table for verification.
