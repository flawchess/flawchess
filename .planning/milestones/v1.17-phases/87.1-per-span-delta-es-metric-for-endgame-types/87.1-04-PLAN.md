---
phase: 87.1
plan: 04
type: execute
wave: 2
depends_on:
  - 87.1-01   # consumes MetricId "endgame_type_achievable_score_gap" + ZONE_REGISTRY entry for assign_zone dispatch
  - 87.1-02   # consumes 5 new EndgameCategoryStats fields for payload assembly
files_modified:
  - app/services/insights_llm.py
  - app/prompts/endgame_insights.md
  - tests/services/test_insights_llm.py
  - CHANGELOG.md
autonomous: true
requirements:
  - ROADMAP-87.1-§6   # LLM payload type_breakdown carries per-class type_achievable_score_gap with dual-label terminology + version bump
tags:
  - backend
  - llm
  - prompt
  - insights
must_haves:
  truths:
    - "insights_llm.py type_breakdown section emits per-class type_achievable_score_gap with shape { mean, n, zone, neutral_band }"
    - "Zone assignment uses assign_zone('endgame_type_achievable_score_gap', mean)"
    - "_PROMPT_VERSION bumped endgame_v28 -> endgame_v29 and all hardcoded assertions updated"
    - "Glossary block defines 'Endgame Type Score Gap' terminology and instructs LLM to use 'Score Gap' in card-context narration"
    - "No parallel verdict / p_value field in payload (per memory feedback_llm_significance_signal.md)"
    - "Sigmoid-bias caveat documented in prompt version comment"
  artifacts:
    - path: "app/services/insights_llm.py"
      provides: "Per-class type_achievable_score_gap block + _PROMPT_VERSION bump"
      contains: "type_achievable_score_gap"
    - path: "app/prompts/endgame_insights.md"
      provides: "Glossary entry for Endgame Type Score Gap + dual-label narration rule"
      contains: "Endgame Type Score Gap"
    - path: "tests/services/test_insights_llm.py"
      provides: "Payload presence assertions for sparse + full cohorts + prompt version assertion"
      contains: "type_achievable_score_gap"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry under v1.17 documenting the new metric"
      contains: "Endgame Type Score Gap"
  key_links:
    - from: "app/services/insights_llm.py"
      to: "app/services/endgame_zones.py"
      via: "assign_zone('endgame_type_achievable_score_gap', mean) + ZONE_REGISTRY['endgame_type_achievable_score_gap'].typical_lower/upper"
      pattern: "endgame_type_achievable_score_gap"
    - from: "app/services/insights_llm.py"
      to: "app/schemas/endgames.py"
      via: "Reads category.type_achievable_score_gap_mean / _n from EndgameCategoryStats"
      pattern: "type_achievable_score_gap_mean|type_achievable_score_gap_n"
---

<!--
Requirement IDs reference ROADMAP §Phase 87.1 success criteria (REQUIREMENTS.md
has no DELTA-NN entries for Phase 87.1 yet — RESEARCH §REQUIREMENTS.md Gap;
ROADMAP success criteria 1..6 are the source of truth).
-->

<objective>
Extend the `insights_llm.py` `type_breakdown` per-class section with a `type_achievable_score_gap`
block per endgame class — shape `{ mean, n, zone, neutral_band }` — and bump
`_PROMPT_VERSION` from `endgame_v28` to `endgame_v29` so cached LLM reports invalidate. Update the
glossary block in `app/prompts/endgame_insights.md` with the dual-label terminology rule per D-10:
the glossary defines "Endgame Type Score Gap" as the primary term; narrative guidance instructs the
LLM to use "Score Gap" in card-context references (matching the card row label users see) and
"Endgame Type Score Gap" only when introducing the metric or comparing to the page-level
Achievable Score Gap. No parallel `verdict` / `p_value` field per memory
`feedback_llm_significance_signal.md` — the cohort band itself carries the significance signal.

Purpose: surfaces the new per-type metric to LLM narration so the auto-generated endgame insights
report can reference per-type Score Gap performance per class. Cache invalidation guaranteed by the
version bump.

Output: payload extension, prompt glossary update, version bump, test coverage, changelog entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md
@.planning/notes/lichess-sigmoid-endgame-calibration.md
@app/services/insights_llm.py
@app/prompts/endgame_insights.md

<interfaces>
<!-- Existing contracts consumed -->

From app/services/endgame_zones.py (post-Plan-01):
```
ZONE_REGISTRY["endgame_type_achievable_score_gap"] = ZoneSpec(
    typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better"
)
def assign_zone(metric_id: MetricId, value: float) -> Literal["weak", "typical", "strong"]
```

From app/schemas/endgames.py (post-Plan-02):
```
class EndgameCategoryStats:
    type_achievable_score_gap_mean:   float | None = None
    type_achievable_score_gap_n:      int   | None = None
    type_achievable_score_gap_p_value: float | None = None   # unused in payload per D-10
    type_achievable_score_gap_ci_low:  float | None = None   # unused in payload per D-10
    type_achievable_score_gap_ci_high: float | None = None   # unused in payload per D-10
```

From app/services/insights_llm.py (current):
```
_PROMPT_VERSION = "endgame_v28"   # to be bumped to "endgame_v29"
# `type_breakdown` section assembles per-class payload via existing conversion_recovery_by_type
# subsection (lines 1364-1372, 1497-1575).
```

<!-- New contract produced by this plan, consumed by the LLM only -->

Per-class type_breakdown payload shape (post-edit):
```
type_breakdown.<class>.type_achievable_score_gap = {
  "mean": float | None,                # rounded to 4 decimal places when non-None
  "n":    int,                          # 0 when no qualifying spans
  "zone": "weak" | "typical" | "strong" | null,   # null when mean is None
  "neutral_band": [float, float]        # always from ZONE_REGISTRY
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend type_breakdown payload + bump _PROMPT_VERSION in insights_llm.py</name>
  <files>app/services/insights_llm.py, tests/services/test_insights_llm.py</files>
  <read_first>
    - app/services/insights_llm.py lines 1-180 (module header, `_PROMPT_VERSION` constant, imports — confirm where `assign_zone` is or should be imported from `app.services.endgame_zones`)
    - app/services/insights_llm.py lines 1364-1372 (`type_breakdown` tuple definition)
    - app/services/insights_llm.py lines 1497-1575 (`conversion_recovery_by_type` per-class payload assembly — the analog where the new field is added)
    - app/services/endgame_zones.py (assign_zone signature + ZONE_REGISTRY shape)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-PATTERNS.md §"app/services/insights_llm.py" (full delta — Delta 1 payload + Delta 2 version bump)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md §D-10
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-RESEARCH.md §"Open Questions" #2 (hardcoded version assertions search)
    - tests/services/test_insights_llm.py (existing structure — find any `endgame_v28` literal assertions)
  </read_first>
  <behavior>
    - Test: every rendered class in `type_breakdown` carries a `type_achievable_score_gap` block under sparse cohort (n < CONFIDENCE_MIN_N): shape is `{ mean, n, zone, neutral_band }`. `mean` may be float or None; `zone` is `null` when mean is None.
    - Test: same block present under full cohort with `mean` populated, `zone` ∈ {weak, typical, strong}, `n >= CONFIDENCE_MIN_N`, `neutral_band` is the 2-tuple from `ZONE_REGISTRY["endgame_type_achievable_score_gap"]`.
    - Test: payload does NOT contain `p_value` or `verdict` sub-fields in the new block (per memory `feedback_llm_significance_signal.md`).
    - Test: `_PROMPT_VERSION` literal equals `"endgame_v29"`.
    - Test: no remaining `endgame_v28` references anywhere in `tests/services/test_insights_llm.py` (or any source file outside changelogs / comments).
    - Test: payload field name is the internal identifier `type_achievable_score_gap` (NOT `score_gap` or `type_score_gap`) — preserves grep-ability with the page-level `achievable_score_gap` per D-02 / D-10.
  </behavior>
  <action>
    Per PATTERNS §"app/services/insights_llm.py" + CONTEXT §D-10:
    (1) Locate the per-class payload assembly inside the `type_breakdown` section (around lines 1497-1575 — the existing `conversion_recovery_by_type` subsection that emits per-class `conv_pct` / `recov_pct` / `opp_*` fields).
    (2) For each rendered endgame class, compute and emit the new `type_achievable_score_gap` block per PATTERNS Delta 1:
        ```
        gap_mean_raw = category.type_achievable_score_gap_mean
        gap_n = category.type_achievable_score_gap_n or 0
        if gap_mean_raw is not None:
            gap_zone = assign_zone("endgame_type_achievable_score_gap", gap_mean_raw)
            gap_mean_out = round(gap_mean_raw, 4)
        else:
            gap_zone = None
            gap_mean_out = None
        gap_payload = {
            "mean": gap_mean_out,
            "n": gap_n,
            "zone": gap_zone,
            "neutral_band": [
                ZONE_REGISTRY["endgame_type_achievable_score_gap"].typical_lower,
                ZONE_REGISTRY["endgame_type_achievable_score_gap"].typical_upper,
            ],
        }
        ```
        Comment: `# Phase 87.1 (SEED-016 D-10): per-class Score Gap block. Field name uses internal "type_achievable_score_gap" for grep-ability with achievable_score_gap (Phase 85.1). User-facing narration: "Score Gap" (per card row); "Endgame Type Score Gap" when introducing. No p_value / verdict per memory feedback_llm_significance_signal.md.`
        Inject `gap_payload` into the per-class dict the existing subsection emits under the key `type_achievable_score_gap`.
    (3) Confirm `assign_zone` and `ZONE_REGISTRY` are imported from `app.services.endgame_zones` at the top of `insights_llm.py`. Add the imports if missing.
    (4) Bump the `_PROMPT_VERSION` constant from `"endgame_v28"` to `"endgame_v29"` (per PATTERNS Delta 2). Update the multi-line comment near the constant to describe:
        - the new per-class block name (`type_achievable_score_gap`)
        - the dual-label terminology rule ("Score Gap" in card context, "Endgame Type Score Gap" when introducing or comparing to page-level)
        - no p_value/verdict in payload (memory `feedback_llm_significance_signal.md`)
        - sigmoid-bias one-liner: "The metric uses the Lichess winning-chances sigmoid which under-weights endgame eval; zones are percentile-calibrated from benchmark data so the bias does not affect zone placement."
    (5) Search for hardcoded `endgame_v28` references in `tests/services/test_insights_llm.py` (per RESEARCH §"Open Questions" #2) and update to `endgame_v29`. Use grep: `grep -n endgame_v28 tests/services/test_insights_llm.py` then replace each hit. Also grep across the rest of `app/` and `tests/` to confirm no stray references remain.
    (6) Extend `tests/services/test_insights_llm.py`:
        - Add `test_type_breakdown_has_gap_full_cohort` — fixture with n >= CONFIDENCE_MIN_N (e.g. n=15) per class; assert per-class `type_achievable_score_gap` block present with mean, n, zone ∈ {weak/typical/strong}, neutral_band as 2-element list.
        - Add `test_type_breakdown_has_gap_sparse_cohort` — fixture with n < CONFIDENCE_MIN_N (e.g. n=3); assert mean populated but zone reflects raw mean (or None if mean=None); neutral_band still present.
        - Add `test_type_breakdown_gap_block_omits_pvalue_and_verdict` — assert keys are exactly `{"mean", "n", "zone", "neutral_band"}`, no `p_value` / `verdict` / `ci_low` / `ci_high` keys.
        - Add `test_prompt_version_bumped` — assert `_PROMPT_VERSION == "endgame_v29"`.
        - Add `test_payload_field_name_uses_internal_identifier` — assert the payload key is literally `"type_achievable_score_gap"` (not `score_gap` / `type_score_gap`).
    (7) Sentry: No new exceptional paths; `assign_zone` is total over its MetricId domain; `category.type_achievable_score_gap_mean` is `float | None` and handled explicitly.
    (8) `uv run ty check app/services/insights_llm.py tests/services/test_insights_llm.py` — must exit 0. The new payload dict uses `Any`-friendly types; existing payload assembly already uses `dict[str, Any]`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_insights_llm.py -x &amp;&amp; uv run ty check app/services/insights_llm.py tests/services/test_insights_llm.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_PROMPT_VERSION = "endgame_v29"' app/services/insights_llm.py` returns 1
    - `grep -cv '^#' app/services/insights_llm.py | head` — no `_PROMPT_VERSION = "endgame_v28"` line remains active (only references in comments are allowed)
    - `grep -c 'type_achievable_score_gap' app/services/insights_llm.py` returns >= 3 (read from category + payload dict key + comment)
    - `grep -c 'assign_zone' app/services/insights_llm.py` returns >= 1 (or it was already imported — confirm presence in import block)
    - `grep -c '"verdict"' app/services/insights_llm.py | head` shows no `verdict` key added to the new payload block
    - `grep -c '"p_value"' app/services/insights_llm.py | head` shows no new `p_value` key in the new gap block (existing p_value references for other metrics may remain)
    - `grep -rn 'endgame_v28' tests/services/test_insights_llm.py | grep -v '^#'` returns no active matches (all hits replaced)
    - `uv run pytest tests/services/test_insights_llm.py -x` exits 0 (5 new test functions green)
    - `uv run ty check app/services/insights_llm.py tests/services/test_insights_llm.py` exits 0
  </acceptance_criteria>
  <done>
    Payload extended with the new per-class block; version bumped; hardcoded asserts updated; sigmoid-bias one-liner recorded in the version comment; tests cover sparse + full cohorts + key shape + version; ty clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update glossary + dual-label narration rule in endgame_insights prompt + changelog entry</name>
  <files>app/prompts/endgame_insights.md, CHANGELOG.md</files>
  <read_first>
    - app/prompts/endgame_insights.md (current glossary block — find the existing per-class metric definitions: Conversion, Recovery, Endgame Score, Achievable Score Gap; the new entry sits alongside these)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-CONTEXT.md §D-10 (dual-label rule)
    - .planning/notes/lichess-sigmoid-endgame-calibration.md (calibration caveat copy)
    - CHANGELOG.md (current [Unreleased] section — find the existing v1.17 phase entries to mirror format)
    - .planning/STATE.md "Last activity" line for quick task 260514-djw (title-case conventions for endgame concepts)
  </read_first>
  <action>
    Per CONTEXT §D-10 + `feedback_llm_prompt_design.md` memory + CLAUDE.md changelog rules:

    PART A — `app/prompts/endgame_insights.md` glossary + narration rule:
    (1) Find the glossary block (or the closest equivalent — the UI-label table or concepts section the LLM consults for terminology). The existing concepts panel terms (post-260514-djw title-case pass): Endgame Phase, Endgame Type(s), Endgame Sequence, Endgame Entry Eval, Achievable Score, Endgame Score, Non-Endgame Score, Conversion, Parity, Recovery, Endgame Score Gap, Achievable Score Gap. Add a new entry alongside Achievable Score Gap:

        **Endgame Type Score Gap** — Per endgame type (rook, minor piece, pawn, queen, mixed), the average per-span gap between exit score and Stockfish-baseline expected score at span entry. Positive = the user outperformed the Stockfish baseline across spans of this type. Negative = the user gave back expected score. This is the per-span, per-type version of the page-level Achievable Score Gap. The metric uses the Lichess winning-chances sigmoid which under-weights endgame eval advantages; zones are percentile-calibrated from benchmark data so the bias does not affect zone placement. Surfaced as the `type_achievable_score_gap` field per class under `type_breakdown` in the payload (shape: `{ mean, n, zone, neutral_band }`).

    (2) Add a narration-rule paragraph either inside the glossary entry or in a dedicated terminology-rules section:

        Narration rule for the per-type Score Gap metric: when referring to a specific endgame type's Score Gap in narrative context (the user is already in the card scope), use the short form "Score Gap" — this matches the label the user sees on the card row. When introducing the metric for the first time in the narrative, or when comparing it to the page-level Achievable Score Gap, use the full form "Endgame Type Score Gap". Do NOT use the internal identifier `type_achievable_score_gap` in narration. Do NOT use "ΔES", "delta_es", or other internal coinages — the user-facing vocabulary is "Score Gap" / "Endgame Type Score Gap" only.

    (3) Verify the new entry uses title-case for the concept name per the 260514-djw convention, no em-dashes in user-facing copy (prefer commas / periods / parentheses), and reads in the same tone as the surrounding glossary entries.

    (4) Optional but recommended: add an example narration line near the Achievable Score Gap example (if such examples exist in the prompt). Example:
        - Correct: "Your rook Score Gap sits at +6% on the last 3 months..."
        - Correct (introduction): "The Endgame Type Score Gap measures per-span eval movement vs the Stockfish baseline; for your queen endgames it sits at..."
        - Incorrect: "Your rook ΔES is +0.06" (uses internal identifier).
        - Incorrect: "Your achievable score gap for rook is..." (conflates with page-level metric).

    PART B — `CHANGELOG.md` entry:
    (5) Append a bullet under `## [Unreleased]` (under the appropriate v1.17 subsection — likely `### Added`). Tone: terse, user-facing per CLAUDE.md changelog rule. Example:

        ### Added
        - Phase 87.1: Per-type Score Gap row on each Endgame Type breakdown card (rook, minor piece, pawn, queen, mixed) showing the average per-span gap between exit score and the Stockfish-baseline expected score at span entry. Positive values mean the user outperformed expectation in that endgame type; negative values mean they gave back expected score. Auto-generated endgame insights now reference Endgame Type Score Gap per class.

    (6) Bullet ends without an em-dash or trailing notes per CLAUDE.md style. Reference Phase 87.1 explicitly. If no `### Added` subsection exists under `## [Unreleased]` yet, create one.

    (7) Verify `CHANGELOG.md` still parses as Keep-a-Changelog format: `## [Unreleased]` header present, link references at the bottom intact, no version section accidentally edited.
  </action>
  <verify>
    <automated>grep -c 'Endgame Type Score Gap' app/prompts/endgame_insights.md &amp;&amp; grep -c 'Phase 87.1' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Endgame Type Score Gap' app/prompts/endgame_insights.md` returns >= 2 (glossary entry + narration rule)
    - `grep -c 'Score Gap' app/prompts/endgame_insights.md` returns >= 3 (existing Endgame Score Gap, Achievable Score Gap, plus new Endgame Type Score Gap and short-form narration references)
    - `grep -c 'type_achievable_score_gap' app/prompts/endgame_insights.md` returns >= 1 (payload field name documented for the LLM)
    - `grep -c 'Lichess.*sigmoid' app/prompts/endgame_insights.md` returns >= 1 (calibration caveat in glossary entry)
    - `grep -c 'ΔES\|delta_es' app/prompts/endgame_insights.md | head` — no internal coinages appear in user-facing narration sections (may appear only in a forbidden-framing rule context)
    - `grep -c 'Phase 87.1' CHANGELOG.md` returns >= 1 (new Unreleased entry)
    - `grep -c '## \[Unreleased\]' CHANGELOG.md` returns 1 (Unreleased header still present)
    - No em-dashes (`—`) introduced in the new prompt + changelog text (`grep -c '—' app/prompts/endgame_insights.md` count must not increase vs pre-edit; safest is zero new em-dashes in the diff)
    - Glossary entry uses title-case "Endgame Type Score Gap" per the 260514-djw convention
  </acceptance_criteria>
  <done>
    Prompt glossary defines "Endgame Type Score Gap" with calibration caveat; narration rule instructs the LLM on dual-label usage; changelog records the user-facing addition; no em-dashes in new copy.
  </done>
</task>

</tasks>

<threat_model>
none — additive change to LLM payload assembly and prompt template. No new exceptional paths (Sentry not needed); no new auth surfaces; no new user input. Prompt-version bump invalidates cached reports but does not change auth / rate-limit policy.
</threat_model>

<verification>
- `uv run pytest tests/services/test_insights_llm.py -x` exits 0
- `uv run ty check app/services/insights_llm.py tests/services/test_insights_llm.py` exits 0
- `uv run ruff check app/services/insights_llm.py` exits 0
- `grep -c 'endgame_v29' app/services/insights_llm.py` >= 1
- `grep -c 'Endgame Type Score Gap' app/prompts/endgame_insights.md` >= 2
- `grep -c 'Phase 87.1' CHANGELOG.md` >= 1
- Manual: trigger an endgame insights generation against the dev DB (post-Plan-02 backend live); confirm narration uses "Score Gap" in card context and "Endgame Type Score Gap" when introducing — capture in `87.1-HUMAN-UAT.md` (created in Plan 03 output)
</verification>

<success_criteria>
ROADMAP §6 satisfied:
- LLM payload `type_breakdown` carries `type_achievable_score_gap` per class with `{ mean, n, zone, neutral_band }`.
- `_PROMPT_VERSION` bumped `endgame_v28` → `endgame_v29` so cached reports invalidate.
- Glossary defines the dual-label terminology and the calibration caveat for the LLM.
- Changelog entry recorded under `[Unreleased]`.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-04-SUMMARY.md`.
Append "Verify LLM narration uses dual-label rule" as a HUMAN-UAT item in
`.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-HUMAN-UAT.md` (created by Plan 03; if not yet present, create it).
</output>
