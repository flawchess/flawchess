# Phase 87.5 Plan Verification: Goal-Backward Coverage

**Phase:** 87.5 — Rebuild Endgame ELO on Endgame Score Gap  
**Plans submitted:** 3 (01-PLAN.md, 02-PLAN.md, 03-PLAN.md)  
**Verification date:** 2026-05-17  
**Verdict:** ISSUES FOUND — proceed to revision

---

## 1. Success Criteria Coverage

| SC | Description | Plans | Status | Notes |
|-----|-------------|-------|--------|-------|
| SC#1 | Chart named "Endgame ELO Timeline"; grep "Conversion ELO" returns CHANGELOG + supersession only | 02, 03 | ✅ COVERED | Plan 02: component rename + text assertions. Plan 03: grep regression test + prompt rewrite. |
| SC#2 | Chart below Score Gap chart in Overall Performance; removed from Metrics section; mobile parity | 02 | ✅ COVERED | Plan 02 Task 3: relocation + h2 rename + Vitest positioning assertion. |
| SC#3 | Formula `endgame_elo = round(actual_elo + K · eg_score_gap)`; zero gap ⇒ actual | 01 | ✅ COVERED | Plan 01 Task 3: `_endgame_elo_from_score_gap` helper + zero-gap invariant test. |
| SC#4 | K locked with one-line rationale citing §3.1.6 percentile table | 01 | ✅ COVERED | Plan 01 Task 1: K calibration eyeball + comment block with p05/p95 mapping. |
| SC#5 | Phase 87.4 helpers/constants deleted (PIVOT, ALPHA, CALIBRATION_VERSION, 3 functions) | 01 | ✅ COVERED | Plan 01 Task 3: bulk delete from endgame_service.py lines 1270–1417 + grep regression. |
| SC#6 | Schema rename end-to-end (`conversion_elo_timeline` → `endgame_elo_timeline`, types, component file) | 01, 02 | ✅ COVERED | Plan 01 Task 3: schema + service rename. Plan 02 Task 2: component file rename + types rename. |
| SC#7 | LLM payload: MetricId/SubsectionId rename, subsection move `metrics_elo` → `overall`, v32→v33, metaphor restored | 03 | ✅ COVERED | Plan 03 Task 2–3: helper renames + section grouping move + prompt rewrite + "lifts/holds back" prose. |
| SC#8 | Phase 87.4 Conv display-centering untouched | 01, 02 | ⚠️ PARTIAL | Plan 01 leaves Conv/Parity/Recov gauges in place (unchanged). Plan 02 explicitly verifies SECTION2_DISPLAY_SHIFT not touched. ISSUE: Neither plan documents the UNCHANGED decision; default reading suggests the gauges are part of refactored metrics (they're not, but clarity absent). |
| SC#9 | K unit tests + grep regression + Vitest positioning + ty/ruff/knip green | 01, 02, 03 | ✅ COVERED | Plan 01 Task 1: K mapping tests. Plan 02 Task 1: positioning Vitest. Plan 03 Task 1: grep regression. All gates specified. |

---

## 2. Locked Decision Compliance (from CONTEXT.md)

| Decision | Planner's Lock | Plans Cover? | Status |
|----------|---|---|---|
| **Input metric (LOCKED)** — Endgame Score Gap from `_compute_score_gap_timeline` | "eg_score_gap = endgame_score − non_endgame_score, trailing-100-game rolling mean" | Plan 01 Task 3: `get_endgame_elo_timeline` calls `_compute_score_gap_timeline` per combo; `_compute_endgame_elo_weekly_series` consumes ScoreGapTimelinePoint directly. | ✅ LOCKED IMPL |
| **ELO mapping (LOCKED)** — Additive K formula, zero gap → actual ELO | `endgame_elo = round(actual_elo + K · eg_score_gap)` | Plan 01 Task 3: helper `_endgame_elo_from_score_gap` implements exactly this. Test `test_zero_gap_preserves_actual_elo` asserts the invariant. | ✅ LOCKED IMPL |
| **Phase 87.4 deletion (LOCKED)** — Delete PIVOT, ALPHA, CALIBRATION_VERSION, 3 helpers | "delete wholesale, do not retune" | Plan 01 Task 3: deletes lines 1270–1417. Grep regression asserts zero matches post-deletion. | ✅ LOCKED IMPL |
| **Naming + relocation (LOCKED)** — "Endgame ELO Timeline" everywhere; chart moves to Overall Perf below Score Gap | Chart title, popover, LLM glossary, CHANGELOG, Vitest; positioned directly below EndgameScoreOverTimeChart. | Plan 02 Task 2–3: component rename + chart relocation. Plan 03 Task 3: prompt prose rewrite. Vitest assertion at Plan 02 Task 1. | ✅ LOCKED IMPL |
| **LLM payload (LOCKED)** — MetricId/SubsectionId rename, subsection move to `overall`, v33 bump, "lifts/holds back" metaphor | endgame_elo_gap, endgame_elo_timeline, overall section, endgame_v33 | Plan 03 Task 2–3: MetricId/SubsectionId renamed; _SECTION_LAYOUT move from metrics_elo to overall; _PROMPT_VERSION → endgame_v33; prose restores metaphor. | ✅ LOCKED IMPL |
| **Tests (LOCKED)** — K mapping unit tests, schema rename tests, regression test (no "Conversion ELO"), Vitest positioning, knip clean | pytest K mapping, schema field rename assertions, grep regression, Vitest DOM position, knip test | Plan 01 Task 1: Wave 0 RED K tests. Plan 01 Task 3: schema rename assertions. Plan 02 Task 1: positioning Vitest. Plan 03 Task 1: grep regression. Knip gate at Plan 02 Task 3. | ✅ LOCKED IMPL |
| **Conv display-centering (UNCHANGED)** — Phase 87.4's Conv −0.055, Recov +0.06, Parity 0 display shifts stay | No touch to SECTION2_DISPLAY_SHIFT or the display-only affine shifts. | Plan 01: no changes to frontend metrics code. Plan 02 Task 3: explicitly verifies SECTION2_DISPLAY_SHIFT unchanged. BUT no explicit task documenting "leave untouched"; relies on absence of changes. | ⚠️ IMPLIED ONLY |

**Finding:** The UNCHANGED Conv display-centering is **implicitly covered** (absence of changes) but not **explicitly documented** as an intentional decision. All locked decisions are technically implemented.

---

## 3. Wave & Dependency Analysis

```
Wave 1 (depends_on: []):
  - Plan 01: Backend rewire (service + schema + insights + tests)

Wave 2 (depends_on: ["87.5-01"]):
  - Plan 02: Frontend (component rename + relocation + types)
  - Plan 03: LLM payload + CHANGELOG (can run in parallel with Plan 02)
```

**Finding:** Correct 2-wave structure. Plan 02 and Plan 03 have **zero file overlap** and can truly run in parallel after Plan 01 completes. Dependency is on Plan 01 only (schema + service renames must land first before frontend/LLM consumes).

---

## 4. Requirement Coverage Analysis

From ROADMAP Phase 87.5, the requirements mapping:

| Phase Req | Tasks Covering It | Status |
|-----------|---|---|
| EGELO-01a (chart name "Endgame ELO Timeline" on frontend) | Plan 02 Tasks 2–3 | ✅ |
| EGELO-01b (no "Conversion ELO" in code outside CHANGELOG + supersession) | Plan 03 Task 1 + Plan 02 Task 3 | ✅ |
| EGELO-02 (chart renders below Score Gap in Overall Performance) | Plan 02 Task 3 | ✅ |
| EGELO-03 (additive K formula `endgame_elo = round(actual_elo + K·gap)`) | Plan 01 Task 3 | ✅ |
| EGELO-04 (K locked with one-line rationale citing §3.1.6) | Plan 01 Task 1 | ✅ |
| EGELO-05 (Phase 87.4 helpers deleted, not retuned) | Plan 01 Task 3 | ✅ |
| EGELO-06a (backend schema rename) | Plan 01 Task 3 | ✅ |
| EGELO-06b (frontend types + component file rename) | Plan 02 Task 2 | ✅ |
| EGELO-07 (LLM MetricId/SubsectionId rename, section move, v33, metaphor) | Plan 03 Tasks 2–3 | ✅ |
| EGELO-08 (Conv display-centering UNCHANGED) | Plan 01–02 (implicit: no changes) | ⚠️ IMPLIED |
| EGELO-09a (K mapping unit tests) | Plan 01 Task 1 | ✅ |
| EGELO-09b (Vitest positioning) | Plan 02 Task 1 | ✅ |
| EGELO-09c (grep regression test) | Plan 03 Task 1 | ✅ |
| EGELO-CL (CHANGELOG entry) | Plan 03 Task 4 | ✅ |

All requirements have covering tasks.

---

## 5. Task Completeness Spot-Check

**Plan 01, Task 1 (Wave 0 RED tests):**
- Files: test files (tests/services/test_endgame_elo_k_mapping.py, tests/schemas/test_endgames_schema.py) ✅
- Action: K calibration eyeball + test creation ✅
- Verify: `uv run pytest ... 2>&1 | grep -E "FAILED|ERROR"` specified ✅
- Done: List of expected outputs (test files exist, RED state confirmed) ✅

**Plan 01, Task 3 (K + delete + schema rename atomic pass):**
- Files: 11 files listed (endgame_service.py, insights_service.py, insights_llm.py, endgames.py, 6 test files, endgame_repository.py) ✅
- Action: Detailed step-by-step rename strategy + delete list ✅
- Verify: `uv run ty check app/ tests/ && uv run ruff check . && uv run pytest tests/ -x` ✅
- Done: Explicit acceptance criteria (K constant visible, helpers gone, schema fields renamed, tests pass) ✅

**Plan 02, Task 3 (page composition rewire + caller updates):**
- Files: 4 files (Endgames.tsx, Home.tsx, theme.ts, endgameMetrics.ts) ✅
- Action: Step-by-step JSX relocation (DELETE from Metrics section, INSERT into Overall Performance), h2 rename ✅
- Verify: `cd frontend && npx tsc --noEmit && npm run lint && npm run knip && npm test -- --run` ✅
- Done: Specific outcomes (imports updated, chart in new location, header renamed, tests green) ✅

**Sample finding:** All tasks have complete Files + Action + Verify + Done. No vague "implement auth" wording; each action is concrete (e.g., "DELETE lines X–Y", "INSERT at line Z", "grep this pattern").

---

## 6. CLAUDE.md Compliance Check

| Rule | Plans Risk? | Status |
|------|---|---|
| Function size (soft 100 LOC, hard 200) | Plan 01: `_endgame_elo_from_score_gap` is a 3-line helper (~1 LOC); `_compute_endgame_elo_weekly_series` rewrite is ~30–40 LOC per the spec. Both well under limits. | ✅ |
| Type safety / ty compliance | Plan 01 Task 3: "_endgame_elo_from_score_gap must carry `-> int` return type annotation". Atomic rename pass across schema + service + tests keeps ty green. | ✅ |
| No `asyncio.gather` on AsyncSession | Plan 01: `_endgame_elo_from_score_gap` is a pure-CPU function; no DB calls. No risk. | ✅ |
| ruff/knip gates | Plan 01 Task 3: "uv run ruff check ." gate specified. Plan 02 Task 3: "npm run knip" gate specified. Both mandatory. | ✅ |
| No magic numbers | Plan 01: K is a named constant (`K: float = <locked_value>`); calibration percentiles live in comment block + test assertions, not inline. Plan 03: prompt prose uses band terminology ("typical ±10pp") not hardcoded K value. | ✅ |
| Popover copy minimalism | Plan 02 Task 3: "no Wilson/sigmoid/affine jargon; WHAT + sign convention only". Component prose rewrite specifies "lifts up / holds back" metaphor — clean, user-facing. | ✅ |

All CLAUDE.md rules are respected.

---

## 7. Concrete File Path Discipline

Spot-check 5 tasks:

| Task | Concrete Path Discipline | Status |
|------|---|---|
| Plan 01 Task 1: create test file | `tests/services/test_endgame_elo_k_mapping.py` (explicit path) | ✅ |
| Plan 01 Task 3: delete lines | `app/services/endgame_service.py` lines 1270–1271, 1284–1286, 1295–1317, etc. (line numbers from RESEARCH.md) | ✅ |
| Plan 01 Task 3: rename schema field | `app/schemas/endgames.py` line 615 `EndgameOverviewResponse.conversion_elo_timeline` (exact location) | ✅ |
| Plan 02 Task 2: git mv | `frontend/src/components/charts/ConversionEloTimelineSection.tsx` → `EndgameEloTimelineSection.tsx` (full paths) | ✅ |
| Plan 03 Task 2: section move | `app/services/insights_llm.py` lines 1420–1438 `_SECTION_LAYOUT` (exact range) | ✅ |

All tasks cite concrete file paths + line numbers (sourced from RESEARCH.md). No vague "the relevant file" wording.

---

## 8. K Calibration Mechanics

**Question:** Does Plan 01 include the eyeball-real-users calibration step before K is locked?

**Answer:** ✅ **YES**. Plan 01 Task 1 specifies:
- Query dev/benchmark DB for 5–10 representative users spanning rating buckets
- Compute `endgame_elo = round(actual_elo + K_candidate · score_difference)` for K ∈ {300, 400, 450, 500, 600}
- Eyeball criterion: typical points ~30–60 ELO of actual; extremes ~±100 ELO
- Lock K value + document one-line rationale in code comment

The procedure is **concrete and human-driven** (not automated), fitting the CONTEXT.md intent ("eyeball procedure, not a test").

**Question:** Is K calibration a Wave 0 RED-tests artifact or a pre-RED step?

**Answer:** **PRE-RED**. Task 1 action order:
1. K calibration eyeball (done first, one-off script, NOT committed)
2. Lock K value
3. Create RED tests using the locked K value

So K is locked **before** tests are written. This is the correct sequencing: RED tests fail because the helper doesn't exist yet, not because K is unknown.

---

## 9. Grep Regression Test

**Question:** Does Plan 03 include a concrete pytest/CI gate that scans the repo for "Conversion ELO" and asserts only CHANGELOG + supersession comment survive?

**Answer:** ✅ **YES**. Plan 03 Task 1 specifies:
- New file `tests/test_no_conversion_elo_string.py` with two functions:
  - `test_no_conversion_elo_phrase_in_live_source()` — grep for case-insensitive "conversion elo"
  - `test_no_conversion_elo_identifier_in_live_source()` — grep for literal "conversion_elo"
- Allowlist: `CHANGELOG.md` + the test file itself
- Scope: `app/`, `frontend/src/`, `tests/`, `CHANGELOG.md`
- Assumes Plan 01 rewrites the supersession comment to avoid the bare phrase (per RESEARCH.md alternative pattern)

The test is **concrete, runnable at CI time** (`uv run pytest tests/test_no_conversion_elo_string.py -x`), and covers SC#1's requirement.

---

## 10. HIGH Concerns

### Issue 1: SC#8 Implicit Coverage (WARNING, Not Blocking)

**Description:** The UNCHANGED Conv display-centering (Phase 87.4 constants SECTION2_DISPLAY_SHIFT) is verified by absence of changes, not by an explicit task documenting "do not touch these gauges".

**Impact:** LOW. The plans do NOT touch the Conv/Parity/Recov display-shift code. Plan 02 Task 3 explicitly states `git diff frontend/src/lib/endgameMetrics.ts` should show "only comment changes, no logic" — which verifies UNCHANGED. But a reader who skips the detailed action text might assume the gauges are being refactored (they're not).

**Fix:** Add a single-line comment in Plan 02 Task 3 action: "The display-shift constants SECTION2_DISPLAY_SHIFT (Conv −0.055, Recov +0.06, Parity 0) are INDEPENDENT of this phase and remain untouched per SC#8." This clarifies intent without changing the task.

**Severity:** ⚠️ **WARNING** — technically compliant (no changes = unchanged), but could be clearer. Does NOT block execution.

---

### Issue 2: Plan 01 Task 3 — Per-Combo Score-Gap Derivation Needs Verification

**Description:** Plan 01 Task 3 leaves the exact implementation of per-combo score-gap derivation open: "RESEARCH.md §2 Option A / Q1 recommendation: ... either (a) extend the repo query ... or (b) consume score_gap_timeline directly from overview-level _compute_score_gap_timeline ... PREFERRED: route the existing per-combo endgame-classified rows ... If the data flow is awkward, the planner's discretion says inlining the weekly series into the timeline assembly path is acceptable."

**Issue:** The plan defers to "planner's discretion" on a critical architectural decision. The weekly producer must consume per-combo score-gap series to avoid cross-combo contamination (RESEARCH.md §2 caveat). Plan 01 Task 3 notes this but doesn't mandate a concrete resolution.

**Impact:** MEDIUM. If the executor makes a wrong architectural choice (e.g., consumes cross-combo score-gap series from the overview-level call), the Endgame ELO timeline becomes contaminated with data from all combos, not just the current user-filter combo. This would silently produce wrong results.

**Fix Required:** Plan 01 Task 3 action must specify the concrete resolution. Recommended: "Inside `get_endgame_elo_timeline`, after partitioning rows by combo, call `_compute_score_gap_timeline(endgame_rows_for_combo, non_endgame_rows_for_combo, SCORE_GAP_TIMELINE_WINDOW, cutoff_str)` PER COMBO and pass the per-combo score_gap_timeline to the weekly producer." Add an example snippet showing the loop structure.

**Severity:** 🔴 **BLOCKER** — without this clarification, the executor may produce incorrect per-combo data wiring.

---

### Issue 3: Plan 02 Task 1 — Test File Timing

**Description:** Plan 02 Task 1 creates Wave 0 RED tests by renaming `ConversionEloTimelineSection.test.tsx` → `EndgameEloTimelineSection.test.tsx` via `git mv`. But the plan depends on Plan 01 completing (Wave 1), which means the source file `ConversionEloTimelineSection.tsx` should already exist in the repo before the test rename happens.

**Issue:** Plan 02 Task 1 action says "git mv src/components/charts/__tests__/ConversionEloTimelineSection.test.tsx ..." but the task depends on Plan 01 (backend), not on Plan 02 Task 2 (component file rename). If Plan 02 Task 1 runs before Task 2, the test file will be renamed before the source component file is renamed, causing a mismatch in imports within the test.

**Reality Check:** Phase 87.4 Plan 02 02-SUMMARY.md notes "the source file didn't exist (it was created by Phase 87.4 Plan 02 Task 1 per 02-SUMMARY.md)". So the Phase 87.5 test file DOES exist (it was created in Phase 87.4). The test file rename is safe; the import path will be fixed in Task 2 (same plan).

**Impact:** LOW. The test file already exists (created in Phase 87.4 Plan 02); the rename is safe within the plan. The import path update in Task 2 happens in the same plan before the test runs.

**Fix:** Plan 02 Task 1 should add a clarification: "The test file `ConversionEloTimelineSection.test.tsx` already exists from Phase 87.4 Plan 02 Task 1; this task renames it and updates imports. The source component file is renamed in Task 2 of this plan; both are renamed in the same Wave 2 sequence."

**Severity:** ⚠️ **WARNING** — technically correct execution path, but could be clearer. Does NOT block.

---

### Issue 4: Plan 03 Task 4 — Calibration Todo Migration Timing

**Description:** Plan 03 Task 4 migrates `.planning/todos/pending/calibrate-endgame-elo-k.md` → `.planning/todos/done/calibrate-endgame-elo-k.md` and appends an `## Outcome` section with the locked K value "from Plan 01".

**Issue:** Plan 03 Task 4 is in Wave 2 (depends_on: ["87.5-01"]). Plan 01 Task 1 is in Wave 1. The locked K value is known after Plan 01 Task 1 completes. But Plan 03 Task 4's action reads "Substitute the actual locked K value from Plan 01's SUMMARY." This assumes the Plan 01 SUMMARY exists and is available to the Plan 03 executor — which is true IF the plans are executed serially (Plan 01 SUMMARY written before Plan 03 Task 4 runs). If plans run in parallel (Plan 02 + Plan 03 in parallel), this is fine because both depend on Plan 01 completing, which includes writing the SUMMARY.

**Impact:** LOW. The dependency structure ensures Plan 01 is complete before Plan 02 + Plan 03 start. The SUMMARY from Plan 01 is guaranteed to exist (it's a required output of any plan execution). Plan 03 Task 4 action is correct.

**Fix:** Add a note to Plan 03 Task 4: "This task runs after Plan 01 completes (per depends_on). The locked K value is sourced from Plan 01's 01-SUMMARY.md, which will exist by the time this task executes."

**Severity:** ℹ️ **INFO** — no real risk, just could be clearer. Does NOT block.

---

## 11. MEDIUM Concerns

### Issue 5: Knip Test File Naming Convention

**Description:** Plan 02 Task 2 renames the component file `ConversionEloTimelineSection.tsx` → `EndgameEloTimelineSection.tsx`. Plan 02 Task 3 runs `npm run knip` to verify no dead exports remain. But the plan doesn't explicitly state that the OLD file is deleted by the git mv, only that the NEW file exists.

**Issue:** If the git mv is done correctly, knip will see the renamed file and should pass (the export path changes but the import sites are updated in Task 3). However, if the file was copied instead of moved, the old file would remain and knip would flag it as dead.

**Impact:** VERY LOW. The action explicitly says "git mv" (not "copy"), which does the right thing. The verify gate runs knip, which would catch the error if the rename wasn't done correctly.

**Fix:** None needed. The plan is correct; the execution will verify it.

**Severity:** ℹ️ **INFO** — a non-issue once you read the action carefully.

---

### Issue 6: Section Header Rename Discretionary Status

**Description:** Plan 02 Task 3 says "RECOMMENDED to rename 'Endgame Metrics and ELO' → 'Endgame Metrics'" based on RESEARCH.md Q3. But the decision is listed as "planner decides whether to fold it in or defer to Phase 89 polish."

**Issue:** The plan makes the fold-in decision ("fold in") without documenting that this is a planner decision. If a future review questions whether the section header should have been renamed, the decision rationale is missing.

**Impact:** VERY LOW. The CHANGELOG entry (Plan 03 Task 4) includes an optional second bullet for the section-header rename. If the planner decides to skip the fold-in, the second bullet is simply omitted. The code changes (Task 3 action) only rename the h2 IF the decision is to fold in.

**Fix:** Plan 02 Task 3 action should note: "PLANNER DECISION: This plan FOLDS IN the section-header rename 'Endgame Metrics and ELO' → 'Endgame Metrics' (per RESEARCH.md Q3 recommendation). If the planner instead defers to Phase 89 polish, skip the h2 rename at action step 1(e) and omit the second CHANGELOG bullet in Plan 03 Task 4."

**Severity:** ⚠️ **WARNING** — the decision is implicit in the action, but marking it as a decision point would help clarity. Does NOT block execution (the action is concrete either way).

---

## 12. FINAL VERDICT

**Status: REVISE**

**Reason:** Issue #2 above (Plan 01 Task 3 — Per-Combo Score-Gap Derivation) is a **BLOCKER**. Without clarification of the exact architectural pattern for per-combo score-gap derivation, the executor risks producing contaminated Endgame ELO data (cross-combo). This is not a cosmetic issue or a clarity gap — it's a potential correctness failure.

**All other issues are non-blocking:**
- Issue #1 (SC#8 implicit coverage): clarifiable but technically correct
- Issue #3 (test file timing): clarifiable but technically correct
- Issue #4 (calibration todo migration): clarifiable but technically correct
- Issue #5 (knip file naming): non-issue once you read carefully
- Issue #6 (section header discretionary): clarifiable but the action is concrete

**Recommendation:** Planner should revise Plan 01 Task 3 to include a concrete example of the per-combo score-gap derivation loop, specifying the exact wiring between `get_endgame_elo_timeline`, per-combo row partitioning, `_compute_score_gap_timeline` calls, and the weekly producer input.

---

**Prepared:** 2026-05-17  
**Verifier:** Plan Checker (Revision Gate)  
**Max Revisions:** 3 (escalate if this remains unresolved after revision)

---

## Revision 2 (2026-05-17)

**Re-verification context:** Planner revised 87.5-01-PLAN.md and 87.5-02-PLAN.md in response to Revision 1 feedback. Re-checking all 1 blocker + 3 warnings for resolution.

### Blocker Resolution Verification

**Issue #2 (Plan 01 Task 3 — Per-Combo Score-Gap Derivation) — RESOLVED ✅**

Task 3 behavior section now includes:
- **Mandate language** (line ~351): "PLANNER MANDATE — per-combo score-gap derivation, NO discretion"
- **Concrete pseudocode** (lines 355–389): Full loop showing per-combo partition, per-combo `_compute_score_gap_timeline` call, and feed to weekly producer
- **Explicit prohibition** (line 353): "do NOT consume the overview-level `score_gap_material.timeline`"
- **Per-combo isolation test** added to must_haves: `test_per_combo_isolation` in Task 1 (line 200)
- **Bucket-rows deletion** now fully documented (Sub-task lines 391–396): delete `bucket_rows` from repo query, update producer signature

**Severity change:** BLOCKER → RESOLVED. The per-combo score-gap wiring is now concrete and mandatory, not discretionary.

### Warning Resolutions Verification

**Issue #1 (SC#8 implicit coverage) — RESOLVED ✅**

Plan 02 Task 3 now explicitly states (lines 369–370):
- Phase 87.4 `SECTION2_DISPLAY_SHIFT` constant values (Conv −0.055, Recov +0.06, Parity 0) are "INDEPENDENT of this phase and remain untouched per SC#8"
- Verification command added: `git diff frontend/src/lib/endgameMetrics.ts` after plan should show "no functional changes"

**Issue #3 (test file timing) — RESOLVED ✅**

Plan 02 Task 1 now explicitly clarifies (line 201):
- "in Phase 87.5 the source file DOES exist — it was created by Phase 87.4 Plan 02 Task 1"
- Action (line 253): "The test file already exists from Phase 87.4 Plan 02"
- Timing is safe: test exists, rename in this plan, source rename in Task 2 of same plan

**Issue #6 (section header rename discretionary) — RESOLVED ✅**

Plan 02 Task 3 now documents (line 389):
- "PLANNER DECISION: this plan FOLDS IN the 'Endgame Metrics and ELO' → 'Endgame Metrics' h2 rename"
- Rationale documented: "after the chart moves out, 'and ELO' is inaccurate"

### New Concerns Check

Spot-check of surgical revisions for scope creep or regressions:
- Plan 01 Task 1: pseudocode addition does not expand task scope; RED tests still concrete
- Plan 01 Task 3: pseudocode clarifies intent, no new files or logic beyond the already-planned rewire
- Plan 02 Task 1: clarification in behavior prose, no action changes
- Plan 02 Task 3: planner decision documented, action steps unchanged

**No new concerns introduced.** All revisions are clarifications, not scope additions.

### Final Verdict

**Status: PASS**

All 1 BLOCKER + 3 WARNINGS from Revision 1 are now resolved. Plans are ready for `/gsd-execute-phase`.

---

**Re-verification:** 2026-05-17  
**Verifier:** Plan Checker (Revision Gate, Iteration 2 of 3)  
**Next step:** Execute Phase 87.5
