---
phase: 87.5-rebuild-endgame-elo-on-endgame-score-gap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_service.py
  - app/services/endgame_zones.py
  - app/services/insights_service.py
  - app/services/insights_llm.py
  - app/schemas/endgames.py
  - app/repositories/endgame_repository.py
  - tests/services/test_endgame_elo_k_mapping.py
  - tests/services/test_conversion_elo_recenter.py
  - tests/test_endgame_service.py
  - tests/schemas/test_endgames_schema.py
  - tests/services/test_endgame_zones.py
  - tests/services/test_insights_service.py
  - tests/services/test_insights_service_series.py
  - tests/services/test_insights_llm.py
  - tests/test_endgames_router.py
  - tests/test_integration_routers.py
autonomous: true
requirements:
  - EGELO-03  # additive K mapping formula (SC#3)
  - EGELO-04  # locked K constant with one-line rationale (SC#4)
  - EGELO-05  # Phase 87.4 helpers deleted, not retuned (SC#5)
  - EGELO-06  # schema rename conversion_elo_timeline → endgame_elo_timeline (SC#6)
  - EGELO-08  # Phase 87.4 Conv display-centering untouched (SC#8 — verified by absence of changes)
  - EGELO-09a  # K mapping unit tests (SC#9 part)

must_haves:
  truths:
    - "K is a single global float constant locked in code with a one-line rationale comment citing §3.1.6"
    - "_endgame_elo_from_score_gap(actual_elo, 0.0, K) returns int(round(actual_elo)) exactly"
    - "_endgame_elo_from_score_gap(actual_elo, +0.202, K) equals round(actual_elo + K*0.202)"
    - "_endgame_elo_from_score_gap(actual_elo, -0.227, K) equals round(actual_elo + K*-0.227)"
    - "EndgameOverviewResponse JSON contains key 'endgame_elo_timeline' and not 'conversion_elo_timeline'"
    - "EndgameEloTimelinePoint JSON contains key 'endgame_elo' and not 'conversion_elo'"
    - "Grep of app/services/endgame_service.py for 'PIVOT', 'ALPHA', 'CALIBRATION_VERSION', '_affine_recenter_conv_delta', '_windowed_conv_delta_es', '_conversion_elo_from_skill' returns zero matches outside the supersession comment block"
    - "Per-combo Endgame ELO series derives from per-combo eg_score_gap windowed series, NOT from a parallel aggregator"
    - "uv run ty check app/ tests/ exits 0; uv run ruff check . exits 0; uv run pytest tests/ -x exits 0"
  artifacts:
    - path: "app/services/endgame_service.py"
      provides: "K constant + _endgame_elo_from_score_gap helper + rewired _compute_endgame_elo_weekly_series + per-combo score-gap derivation in get_endgame_elo_timeline"
      contains: "K: float\\s*="
    - path: "tests/services/test_endgame_elo_k_mapping.py"
      provides: "Unit tests covering zero-gap invariant, p05/p95 mapping, rounding contract"
      min_lines: 60
    - path: "app/schemas/endgames.py"
      provides: "EndgameEloTimelinePoint.endgame_elo field + EndgameOverviewResponse.endgame_elo_timeline field"
      contains: "endgame_elo_timeline:"
  key_links:
    - from: "app/services/endgame_service.py::get_endgame_elo_timeline"
      to: "_compute_score_gap_timeline"
      via: "per-combo call inside combo iteration loop"
      pattern: "_compute_score_gap_timeline\\("
    - from: "app/services/endgame_service.py::_compute_endgame_elo_weekly_series"
      to: "_endgame_elo_from_score_gap"
      via: "direct call inside per-week loop"
      pattern: "_endgame_elo_from_score_gap\\("
    - from: "app/services/insights_service.py::_findings_conversion_elo_timeline"
      to: "EndgameEloTimelinePoint.endgame_elo"
      via: "field access for gap computation"
      pattern: "last\\.endgame_elo - last\\.actual_elo"
---

<objective>
Land the entire backend rewire for Phase 87.5: lock K via the calibration procedure, delete the Phase 87.4 constants and helpers wholesale, introduce the additive K mapping driven by the existing Endgame Score Gap series, rename the Pydantic field + response field end-to-end, and update every backend caller so `uv run ty check` and `uv run pytest` stay green at each commit boundary.

Purpose: Eliminate the Conv-ΔES sigmoid bias (§3.2.2 ELO d=1.62) that caused Phase 87.4's strong-player 500-ELO gaps. The Endgame Score Gap metric (§3.1.6 ELO d=0.17) is essentially flat across rating buckets, and the additive `actual_elo + K · eg_score_gap` formula returns `actual_elo` exactly at `eg_score_gap = 0` — restoring the "lifts up / holds back" semantics by construction.

Output: K constant locked with one-line rationale; `_endgame_elo_from_score_gap` helper; rewired `_compute_endgame_elo_weekly_series`; renamed Pydantic field/response shape; deleted Phase 87.4 helpers and constants; ty/ruff/pytest green; integration-test JSON key flipped to `endgame_elo_timeline`.

Mirrors Phase 87.4 Plan 01 structure (see `.planning/milestones/v1.17-phases/87.4-drop-endgame-skill-conversion-elo-timeline/01-SUMMARY.md`). The Task 3 "single atomic edit pass" pattern from 87.4 — rename schema field + every caller in the same edit — is reused here because partial renames leave ty broken between commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-CONTEXT.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-RESEARCH.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-PATTERNS.md
@.planning/notes/endgame-elo-rebuild-on-score-gap.md
@.planning/todos/pending/calibrate-endgame-elo-k.md
@.planning/milestones/v1.17-phases/87.4-drop-endgame-skill-conversion-elo-timeline/01-SUMMARY.md

<interfaces>
<!-- Key code surfaces this plan modifies. Extracted from RESEARCH.md to spare the executor a codebase tour. -->

From `app/services/endgame_service.py` (current Phase 87.4 state):
- Lines 826-933: `_compute_score_gap_timeline(endgame_rows, non_endgame_rows, window, cutoff_str=None) -> list[ScoreGapTimelinePoint]` — DRIVER FOR THIS PHASE. Each emitted point carries `endgame_score: float`, `non_endgame_score: float`, `score_difference: float (= endgame_score - non_endgame_score, 4 decimals)`, `date: str (ISO Monday)`.
- Line 336: `SCORE_GAP_TIMELINE_WINDOW = 100` — inherited unchanged.
- Lines 1270-1271: `_ENDGAME_ELO_SKILL_CLAMP_LO = 0.05`, `_ENDGAME_ELO_SKILL_CLAMP_HI = 0.95` — DELETE (only referenced by deleted Phase 57 formula).
- Lines 1284-1286: `PIVOT = -0.0474`, `ALPHA = 2.025`, `CALIBRATION_VERSION = "conv_delta_v1_260516"` — DELETE WHOLESALE per D-05.
- Line 1290: `_ENDGAME_ELO_COMBO_ORDER: list[tuple[str, str]]` — KEEP (combo iteration order, not a Phase 87.4 artifact).
- Lines 1295-1317: `_affine_recenter_conv_delta(conv_delta_es: float) -> float` — DELETE.
- Lines 1320-1342: `_conversion_elo_from_skill(skill: float, actual_elo_at_date: float) -> int` — DELETE (the function; its docstring 1320-1342 contains the Phase 57 supersession block — that comment STAYS as a top-of-file or top-of-section note, but the function body is gone).
- Lines 1345-1417: `_windowed_conv_delta_es(rows: Sequence[...]) -> float | None` — DELETE.
- Lines 1420-1556: `_compute_endgame_elo_weekly_series(bucket_rows, all_rows, window, actual_elo_dates, actual_elo_ratings, cutoff_str=None) -> list[EndgameEloTimelinePoint]` — REWRITE body AND SIGNATURE. New signature: `_compute_endgame_elo_weekly_series(score_gap_timeline: list[ScoreGapTimelinePoint], actual_elo_dates, actual_elo_ratings, cutoff_str=None) -> list[EndgameEloTimelinePoint]`. The old `bucket_rows` parameter is DELETED (no longer consumed — the new producer reads from a pre-computed per-combo `ScoreGapTimelinePoint` list, not raw eval_cp/eval_mate/result rows). The orchestrator `get_endgame_elo_timeline` builds the per-combo score-gap series and feeds it in.
- Lines 2423-2519: `get_endgame_elo_timeline(...)` orchestrator. Keeps `query_endgame_elo_timeline_rows` + per-combo asof-join for Actual ELO. Per-combo score-gap derivation INJECTED here.
- Line 2705: `conversion_elo_timeline = await get_endgame_elo_timeline(...)` — rename local var + kwarg on line 2724.

From `app/schemas/endgames.py`:
- Lines 532-564: `EndgameEloTimelinePoint` Pydantic model. Field `conversion_elo: int` → `endgame_elo: int` (rename). Docstring rewrite from Phase 57 multiplicative formula → additive K formula.
- Lines 567-582: `EndgameEloTimelineCombo` — class name STAYS (already correct), no changes.
- Lines 585-597: `EndgameEloTimelineResponse` — class name STAYS, no changes.
- Line 615: `EndgameOverviewResponse.conversion_elo_timeline: EndgameEloTimelineResponse` → `endgame_elo_timeline: EndgameEloTimelineResponse`.

From `app/services/endgame_zones.py`:
- Line 68: `MetricId` Literal — `"conversion_elo_gap"` → `"endgame_elo_gap"`.
- Line 78: `SubsectionId` Literal — `"conversion_elo_timeline"` → `"endgame_elo_timeline"`.
- Line 142: `NOTABLE_CONVERSION_ELO_DIVERGENCE_THRESHOLD: int = 100` → `NOTABLE_ENDGAME_ELO_DIVERGENCE_THRESHOLD: int = 100`.
- Line 314: ZONE_REGISTRY key `"conversion_elo_gap"` → `"endgame_elo_gap"`.
- Line 378: SAMPLE_QUALITY_BANDS key `"conversion_elo_timeline"` → `"endgame_elo_timeline"`.

From `app/services/insights_service.py` (Plan 01's slice — bare minimum to keep ty green; the section-grouping move + helper rename + prose lives in Plan 03):
- Line 104: `_TIMELINE_SUBSECTION_IDS` frozenset entry `"conversion_elo_timeline"` → `"endgame_elo_timeline"`.
- Line 181: `compute_player_profile(all_time_resp.conversion_elo_timeline.combos)` → `.endgame_elo_timeline.combos`.
- Line 344: `"conversion_elo_timeline"` set literal → `"endgame_elo_timeline"`.
- Line 833: `combos: list[EndgameEloTimelineCombo] = response.conversion_elo_timeline.combos` → `.endgame_elo_timeline.combos`.
- Line 866: `value = float(last.conversion_elo - last.actual_elo)` → `last.endgame_elo - last.actual_elo`.
- Lines 837, 851, 853, 868, 871, 874, 876: string literals `"conversion_elo_timeline"` → `"endgame_elo_timeline"` and `"conversion_elo_gap"` → `"endgame_elo_gap"` (these are typed against the renamed `MetricId` / `SubsectionId` Literals — ty will flag if not renamed in lockstep).
- LEAVE `_findings_conversion_elo_timeline` function name unchanged in this plan — Plan 03 renames it to `_findings_endgame_elo_timeline` and owns the `_SECTION_LAYOUT` grouping move. Lockstep with Plan 87.4 Plan 01 vs Plan 03 split.

From `app/services/insights_llm.py` (Plan 01's slice — string literals only; helper renames + `_PROMPT_VERSION` + prose live in Plan 03):
- Lines 103-105: `_NON_FRACTIONAL_METRICS` set literal `"conversion_elo_gap"` → `"endgame_elo_gap"`.
- Lines 343-344: timeline set literal `"conversion_elo_timeline"` → `"endgame_elo_timeline"`.
- Lines 864, 892, 1072: `metric_id == "conversion_elo_gap"` / `metric == "conversion_elo_gap"` → `"endgame_elo_gap"`.
- Line 1631 (approx): `subsection_id == "conversion_elo_timeline"` → `"endgame_elo_timeline"`.
- Line 1843 (approx): `block_id == "conversion_elo_timeline"` → `"endgame_elo_timeline"`.
- Sentinel copy `"no Conversion ELO trajectory available yet"` → `"no Endgame ELO trajectory available yet"`.
- DO NOT touch `_PROMPT_VERSION`, helper names `_conversion_elo_per_bucket` / `_render_conversion_elo_summary_block`, the rendered header literal `"[summary conversion_elo"`, or the inline prompt blob — Plan 03 owns those.

From `app/repositories/endgame_repository.py`:
- Lines 789-890 (approx): `query_endgame_elo_timeline_rows` — function name STAYS. Currently returns `(bucket_rows, all_rows)`. PLANNER DECISION (per plan-check revision): the `bucket_rows` subquery (and its `eval_cp / eval_mate / result` columns) is now dead weight — the new weekly producer derives ELO from a per-combo `ScoreGapTimelinePoint` series, not bucket-row conv-ΔES. **DELETE the `bucket_rows` subquery from `query_endgame_elo_timeline_rows` and update the return signature to `list[<all_rows_tuple>]` (single sequence, not a 2-tuple).** This keeps the repo query lean and matches the cleaned-up `_compute_endgame_elo_weekly_series` signature. Update every caller in `endgame_service.py` in the same atomic edit pass. The per-combo endgame/non-endgame row partitioning that drives `_compute_score_gap_timeline` comes from the same `all_rows` (each row already carries the per-position material classification used elsewhere in the service for endgame vs non-endgame split — reuse that existing classification helper).
- Update docstring at line 800 if it mentions "Conversion ELO" — replace with "Endgame ELO".
</interfaces>

<chart_data>
<!-- K calibration percentile table from reports/benchmarks-latest.md §3.1.6. Lift verbatim into the K constant comment. -->

Pooled benchmark Lichess sample (n=1,632, sparse cells excluded):

| Percentile | eg_score_gap | round(K · gap) at K=450 |
|------------|--------------|--------------------------|
| p05        | -0.227       | -102 ELO                 |
| p25        | -0.104       | -47 ELO                  |
| p50        | -0.014       | -6 ELO                   |
| p75        | +0.073       | +33 ELO                  |
| p95        | +0.202       | +91 ELO                  |

Mean: -0.0134. eg_mean = 0.5105. non_eg_mean = 0.5239.
</chart_data>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests + K calibration eyeball + lock K constant</name>
  <files>
    tests/services/test_endgame_elo_k_mapping.py,
    tests/services/test_conversion_elo_recenter.py,
    tests/schemas/test_endgames_schema.py
  </files>
  <behavior>
    The K calibration eyeball happens BEFORE the helper lands. Procedure (per `.planning/todos/pending/calibrate-endgame-elo-k.md`):
    - Query the dev database via the `mcp__flawchess-db__query` MCP tool (or `flawchess-benchmark-db` if Lichess benchmark samples are preferred). Run docker-compose dev DB first if needed: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`.
    - Pull 5-10 representative users spanning rating buckets. Example SQL (run via the read-only MCP):
      ```sql
      SELECT u.id, u.email, MIN(white_rating), MAX(white_rating), COUNT(*)
      FROM users u JOIN games g ON g.user_id = u.id
      GROUP BY u.id HAVING COUNT(*) > 200 LIMIT 10;
      ```
    - For each user pick their primary (platform, time_control) combo by game count. For each user × combo:
      * Fetch the per-week ScoreGapTimelinePoint series. Simplest: invoke `_compute_score_gap_timeline` directly in a one-off Python script (`scripts/calibrate_k.py`, not committed) or query `game_positions` directly. Acceptable shortcut: call the running backend `GET /api/endgames/overview` after seeding a dev user, then read `score_gap_material.timeline[*].score_difference`.
      * Pair each point with its asof `actual_elo` from the same `query_endgame_elo_timeline_rows` `all_rows` partition (or look up the latest available rating for that combo as of that ISO Monday).
      * Compute `endgame_elo = round(actual_elo + K_candidate · score_difference)` for K_candidate ∈ {300, 400, 450, 500, 600}.
      * Eyeball: do typical points sit within ~30-60 ELO of actual_elo? Do extremes land around ±100 ELO (not 300, not 50)?
    - Lock K. Record the chosen value with a one-line rationale referencing the percentile table.
    - Note: this calibration is NOT a committed test or script — it's a one-off eyeball that produces a value for the constant. The locked K is then hardcoded into Task 2's constant block. Document the calibration step in the SUMMARY.md "Decisions" section.

    Once K is locked, write the RED tests:

    **`tests/services/test_endgame_elo_k_mapping.py` (new file)** — modelled on Phase 87.4 `test_conversion_elo_recenter.py`:
    - `class TestEndgameEloFromScoreGap`:
      * `test_zero_gap_preserves_actual_elo`: `_endgame_elo_from_score_gap(1500.0, 0.0, K) == 1500`
      * `test_zero_gap_various_actual_elos`: parametrized over actual_elo ∈ {800, 1200, 1500, 2000, 2400}, gap=0.0 ⇒ result == int(round(actual_elo)).
      * `test_positive_p95_gap`: `_endgame_elo_from_score_gap(1500.0, 0.202, K) == round(1500.0 + K * 0.202)`.
      * `test_negative_p05_gap`: `_endgame_elo_from_score_gap(1500.0, -0.227, K) == round(1500.0 + K * -0.227)`.
      * `test_rounds_to_int`: `isinstance(_endgame_elo_from_score_gap(1500.0, 0.001, K), int)`.
      * `test_typical_band_p25_p75`: at p25 (-0.104) and p75 (+0.073), delta ∈ [-50, +40] ELO at the locked K — verifies the eyeball criterion ("typical points within ~30-60 ELO").
      * `test_paired_diff_matches_formula`: for a synthetic series of 10 (actual_elo, eg_score_gap) pairs, the produced series satisfies `endgame_elo[i] - actual_elo[i] == round(K * eg_score_gap[i])` point-by-point. (This is the optional "paired-diff test" the planner endorses — Q5 in RESEARCH.md.)
      * `test_K_constant_locked`: `assert K == <locked value>` so a future drift bumps a visible test, not silent recalibration.
      * `test_per_combo_isolation`: PER-COMBO INVARIANT TEST (added per plan-check revision). Synthesize two `(platform, time_control)` combos with *different* endgame/non-endgame row profiles (e.g., combo A: bullet chess.com with score_gap mean +0.20; combo B: rapid lichess with score_gap mean -0.20). Build the inputs that `get_endgame_elo_timeline` feeds to its per-combo loop (the per-combo `all_rows`, the classification predicate, the actual_elo asof arrays). Call the orchestrator (or, if mocking is simpler, the per-combo loop body extracted to a helper) and assert that the emitted `EndgameEloTimelinePoint` series for combo A reflects ONLY combo A's score-gap profile (every point's `endgame_elo - actual_elo` matches `round(K * combo_A_gap_at_date)` within ±1), and that no point is influenced by combo B's rows. Inverse: do the same for combo B. This test fails if the executor mistakenly consumes a cross-combo score-gap series. Placement: same file `tests/services/test_endgame_elo_k_mapping.py`. If wiring the full orchestrator under unit-test conditions is awkward, an acceptable alternative is to test the new `get_endgame_elo_timeline` path in `tests/test_endgame_service.py::TestEndgameEloTimeline` (Task 3 already rewrites that class — add the per-combo isolation case there and reference the test name in this plan's must_haves). Either placement is fine; document the choice in the SUMMARY.
    - Module imports: `from app.services.endgame_service import K, _endgame_elo_from_score_gap`.

    **`tests/services/test_conversion_elo_recenter.py` — DELETE the entire file.** Its contents test the deleted PIVOT/ALPHA/CALIBRATION_VERSION/_affine_recenter/_conversion_elo_from_skill helpers. Use `git rm tests/services/test_conversion_elo_recenter.py` so git history is clean. The new `test_endgame_elo_k_mapping.py` replaces its coverage.

    **`tests/schemas/test_endgames_schema.py` — APPEND a new test class** (keep existing tests; Phase 87.4 set them on `conversion_elo_*` — those tests are REWRITTEN, not appended-to):
    - `class TestEndgameOverviewResponseEndgameEloTimeline`:
      * `test_overview_response_uses_endgame_elo_timeline`: `assert "endgame_elo_timeline" in EndgameOverviewResponse.model_fields`; `assert "conversion_elo_timeline" not in EndgameOverviewResponse.model_fields`.
    - `class TestEndgameEloTimelinePointFieldRename`:
      * `test_point_has_endgame_elo_field`: instantiate `EndgameEloTimelinePoint(date="2026-05-17", endgame_elo=1500, actual_elo=1500, ...)`; assert `pt.endgame_elo == 1500`.
      * `test_point_rejects_legacy_conversion_elo`: `with pytest.raises(pydantic.ValidationError): EndgameEloTimelinePoint.model_validate({"conversion_elo": 1500, ...})`. Pydantic strict-mode rejection (the field is gone).
    - REMOVE the Phase 87.4 `TestEndgameOverviewResponseConversionTimeline` class wholesale (its inverse assertion lived on the deleted field).

    RED state: all new tests fail because `K`, `_endgame_elo_from_score_gap`, and the renamed fields don't exist yet. Tasks 2-3 turn them GREEN.
  </behavior>
  <action>
    1. Run K calibration eyeball per `.planning/todos/pending/calibrate-endgame-elo-k.md`: query dev DB via `mcp__flawchess-db__query` (or benchmark DB via `bin/benchmark_db.sh start` + `mcp__flawchess-benchmark-db__query` for Lichess users) to pull 5-10 users spanning rating buckets, compute `actual_elo + K_candidate · score_difference` for K ∈ {300, 400, 450, 500, 600}, eyeball typical points ~30-60 ELO of actual and extremes ~±100 ELO. Lock K. Document chosen value + one-line rationale citing §3.1.6 percentile table. The starting candidate K=450 from RESEARCH.md is the back-of-envelope basis; the eyeball decides the final value.
    2. Create `tests/services/test_endgame_elo_k_mapping.py` with the 9 test methods above (imports `K`, `_endgame_elo_from_score_gap` from `app.services.endgame_service`). Use the locked K in `test_K_constant_locked`. Use the §3.1.6 percentile values for p05/p95/p25/p75 tests. The new `test_per_combo_isolation` test (added per plan-check revision) MAY be deferred to `tests/test_endgame_service.py::TestEndgameEloTimeline` if the orchestrator wiring is easier to mock there — either placement satisfies the per-combo invariant in must_haves.
    3. `git rm tests/services/test_conversion_elo_recenter.py`.
    4. Update `tests/schemas/test_endgames_schema.py`: REMOVE existing `TestEndgameOverviewResponseConversionTimeline` and `TestEndgameEloTimelinePointFieldRename` Phase 87.4 classes (whatever they're called — grep for `conversion_elo_timeline` in the file). ADD the two new test classes above. The Phase 87.4 classes assert `conversion_elo` is present and `endgame_elo` is absent — Phase 87.5 inverts that assertion; same structure, swapped field names.
    5. Run `uv run pytest tests/services/test_endgame_elo_k_mapping.py tests/schemas/test_endgames_schema.py -x` — confirm RED (ImportError on `K` / `_endgame_elo_from_score_gap` for the first file; AttributeError/ValidationError on the schema field rename for the second). This is the planned RED state.
    6. Commit: `test(87.5-01): add Wave 0 RED tests for K mapping + schema rename`. Include the calibration rationale in the commit body if helpful for review.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_elo_k_mapping.py tests/schemas/test_endgames_schema.py 2>&1 | grep -E "FAILED|ERROR|ImportError" | wc -l</automated>
    <human-check>K locked value is documented with a one-line rationale citing the §3.1.6 percentile table. The chosen K should produce typical-point ELO deltas in the ~30-60 ELO range and extremes ~±100 ELO when eyeballed against 5-10 real dev/benchmark users.</human-check>
  </verify>
  <done>
    - `tests/services/test_endgame_elo_k_mapping.py` exists with 9 tests (including `test_per_combo_isolation`), all currently failing (import errors expected — `K` and `_endgame_elo_from_score_gap` not yet defined). If `test_per_combo_isolation` is placed in `tests/test_endgame_service.py::TestEndgameEloTimeline` instead, that file carries it; the placement decision is documented in the commit body.
    - `tests/services/test_conversion_elo_recenter.py` removed (git rm).
    - `tests/schemas/test_endgames_schema.py` updated: Phase 87.4 conversion_elo classes removed, two new endgame_elo classes added — these also fail RED (schema field not yet renamed).
    - K value chosen + rationale documented (in commit message body or scratch notes for Task 2's constant block).
    - Commit `test(87.5-01): add Wave 0 RED tests for K mapping + schema rename` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zone-registry rename + endgame_zones Literal/constant renames</name>
  <files>
    app/services/endgame_zones.py,
    tests/services/test_endgame_zones.py
  </files>
  <behavior>
    `endgame_zones.py` is the smallest, leaf-most module touching the renamed `MetricId` / `SubsectionId` Literals — landing it first means the Literal types are correct before `endgame_service.py` and the insights layer reference them in Task 3. This mirrors Phase 87.4 Plan 01 Task 2 ordering.

    Renames in `app/services/endgame_zones.py`:
    - Line 68: `MetricId = Literal["...", "conversion_elo_gap", ...]` → `"endgame_elo_gap"`. Other entries in the Literal stay unchanged.
    - Line 78: `SubsectionId = Literal["...", "conversion_elo_timeline", ...]` → `"endgame_elo_timeline"`.
    - Lines 140-142: rename `NOTABLE_CONVERSION_ELO_DIVERGENCE_THRESHOLD: int = 100` → `NOTABLE_ENDGAME_ELO_DIVERGENCE_THRESHOLD: int = 100`. Update the comment at line 140 to refer to "Endgame ELO" not "Conversion ELO". The constant has no callers as of RESEARCH.md grep — it's exported but unused; the rename is cosmetic but required by the "no Conversion ELO in code" rule.
    - Line 314: `ZONE_REGISTRY["conversion_elo_gap"] = ZoneSpec(...)` → `ZONE_REGISTRY["endgame_elo_gap"] = ZoneSpec(...)`. Keep the ZoneSpec band values unchanged — the zone math is metric-agnostic, just the key is renamed.
    - Line 378: `SAMPLE_QUALITY_BANDS["conversion_elo_timeline"] = (10, 40)` → `SAMPLE_QUALITY_BANDS["endgame_elo_timeline"] = (10, 40)`.
    - No new zone needed for Endgame ELO per RESEARCH.md §1 (the document-why-not is a comment in the file: "no Endgame ELO zone — the metric is the per-week derived series, not a current-state band").

    Tests in `tests/services/test_endgame_zones.py`:
    - Find every occurrence of `"conversion_elo_gap"` and `"conversion_elo_timeline"` (RESEARCH.md §8 lists this file). Rename to the new keys.
    - The registry-keys assertion in `TestRegistrySanity` (or equivalent) lists every MetricId — update accordingly.
    - Function-size: this is a 6-line surgical patch. No new helpers.

    Codegen impact: `scripts/gen_endgame_zones_ts.py` and `frontend/src/generated/endgameZones.ts`. The current generator reads `ZONE_REGISTRY` and emits `*_ZONES` per key. After the key rename `conversion_elo_gap` → `endgame_elo_gap`, the emitted constant name changes too (the generator likely uppercases-and-underscores the key). Check whether the script has a hardcoded reference to `"conversion_elo_gap"` outside `ZONE_REGISTRY.keys()` iteration — if so, rename in lockstep. Run `uv run python scripts/gen_endgame_zones_ts.py` and commit the regenerated `endgameZones.ts`. CI fails on drift (`gen_endgame_zones_ts.py --check`).
  </behavior>
  <action>
    1. In `app/services/endgame_zones.py`: rename the 5 sites listed above (lines 68, 78, 140-142, 314, 378). Preserve every ZoneSpec band value and Literal entry that isn't being renamed.
    2. Run `uv run ty check app/ tests/` — `MetricId` and `SubsectionId` Literals will narrow against any string literal that hasn't been renamed yet; expect failures in `endgame_service.py`, `insights_service.py`, `insights_llm.py`. That's expected — Task 3 will land them in a single atomic edit pass.
    3. Update `tests/services/test_endgame_zones.py`: rename every `"conversion_elo_gap"` / `"conversion_elo_timeline"` quoted-string to the `endgame_elo_*` equivalents. Update the `TestRegistrySanity` (or equivalent) full-key-set assertion. Use a grep to find all sites first.
    4. Regenerate `frontend/src/generated/endgameZones.ts`: `uv run python scripts/gen_endgame_zones_ts.py` and commit the diff. If the script itself contains `"conversion_elo_gap"` outside an iteration over `ZONE_REGISTRY.keys()`, rename inside the script too. Verify with `uv run python scripts/gen_endgame_zones_ts.py --check` — must exit clean.
    5. Run `uv run pytest tests/services/test_endgame_zones.py -x` — must pass (the only deps on this file are the renamed keys, which are now consistent within the test).
    6. Commit: `refactor(87.5-01): zone registry + endgame_zones Literal renames`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_zones.py -x && uv run python scripts/gen_endgame_zones_ts.py --check</automated>
  </verify>
  <done>
    - `app/services/endgame_zones.py` MetricId/SubsectionId Literal entries renamed; ZONE_REGISTRY + SAMPLE_QUALITY_BANDS keys renamed; NOTABLE constant renamed.
    - `frontend/src/generated/endgameZones.ts` regenerated and committed.
    - `tests/services/test_endgame_zones.py` passes locally.
    - Commit `refactor(87.5-01): zone registry + endgame_zones Literal renames` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock K + delete Phase 87.4 helpers + introduce additive K mapping + rewire weekly series + schema rename atomic pass</name>
  <files>
    app/services/endgame_service.py,
    app/services/insights_service.py,
    app/services/insights_llm.py,
    app/schemas/endgames.py,
    app/repositories/endgame_repository.py,
    tests/test_endgame_service.py,
    tests/services/test_insights_service.py,
    tests/services/test_insights_service_series.py,
    tests/services/test_insights_llm.py,
    tests/test_endgames_router.py,
    tests/test_integration_routers.py
  </files>
  <behavior>
    Single atomic edit pass spanning schema + service + repository + insights + tests. The Phase 87.4 Plan 01 Task 3 lesson (01-SUMMARY.md auto-fix #2 + #3): partial renames leave `ty` broken or behavior silently drifting. Land all backend code that touches the renamed schema field in one commit so the working tree never has a half-renamed state.

    **`app/schemas/endgames.py`** (do first within this edit pass):
    - `EndgameEloTimelinePoint.conversion_elo: int` → `endgame_elo: int`. Rewrite the docstring to describe `endgame_elo = round(actual_elo + K · eg_score_gap)` not the Phase 57 multiplicative formula.
    - `EndgameOverviewResponse.conversion_elo_timeline` → `endgame_elo_timeline`. Update the field's docstring/comment to reference Phase 87.5.

    **`app/services/endgame_service.py`** — main rewire:
    - **Add K constant block** at the same location the deleted PIVOT/ALPHA/CALIBRATION_VERSION lived (around line 1284). Format:
      ```
      # Phase 87.5 (D-01): single global K constant — additive ELO offset per unit
      # of windowed Endgame Score Gap. Calibrated against §3.1.6 percentile table:
      # p95 +0.202 → +<K*0.202> ELO; p05 -0.227 → -<K*0.227> ELO; p50 -0.014 → -<K*0.014> ELO.
      # See .planning/todos/done/calibrate-endgame-elo-k.md for the eyeball procedure.
      K: float = <locked_value_from_task_1>
      ```
      Substitute the actual locked value from Task 1's eyeball. Compute the +K*0.202 / -K*0.227 numbers in the comment.
    - **Add `_endgame_elo_from_score_gap` helper** (replaces the deleted `_conversion_elo_from_skill`):
      ```
      def _endgame_elo_from_score_gap(actual_elo: float, eg_score_gap: float, k: float) -> int:
          """Additive Endgame ELO from Endgame Score Gap (Phase 87.5).

          endgame_elo = round(actual_elo + k * eg_score_gap)

          At eg_score_gap = 0, endgame_elo == round(actual_elo) exactly — the
          neutral case is literal zero, not a benchmark-derived constant.
          Phase 87.5 supersedes the Phase 57 multiplicative `400·log10(s/(1-s))`
          formula and the Phase 87.4 affine recenter (PIVOT/ALPHA) wholesale.
          See .planning/notes/endgame-elo-rebuild-on-score-gap.md.
          """
          return int(round(actual_elo + k * eg_score_gap))
      ```
      Explicit `-> int` return type (CLAUDE.md ty rule). Pure function, no clamping (RESEARCH.md §1 confirms `_ENDGAME_ELO_SKILL_CLAMP_LO/HI` go away with the deleted Phase 57 formula; the existing endgame_elo_timeline contract carries no explicit floor/ceiling, only the natural int range — if a user has a 2000 actual_elo and gap=+0.5 (impossible in practice), the result is 2225 which is fine).
    - **DELETE wholesale** (per RESEARCH.md §1 line numbers):
      * Lines 1270-1271: `_ENDGAME_ELO_SKILL_CLAMP_LO`, `_ENDGAME_ELO_SKILL_CLAMP_HI`.
      * Lines 1284-1286: `PIVOT`, `ALPHA`, `CALIBRATION_VERSION` (and the surrounding comment block at lines 1273-1286).
      * Lines 1295-1317: `_affine_recenter_conv_delta`.
      * Lines 1320-1342: `_conversion_elo_from_skill` (the FUNCTION goes; the supersession comment block at the top of the function's docstring is LIFTED out and placed as a top-of-section comment near the new K constant, restructured to read "Phase 57 multiplicative formula RETIRED 2026-05-17 (Phase 87.5); see Phase 87.4 / 87.5 design notes" — this preserves the historical context per CONTEXT.md "supersession comment block stays").
      * Lines 1345-1417: `_windowed_conv_delta_es`.
    - **Rewrite `_compute_endgame_elo_weekly_series`** (replaces lines 1420-1556). New signature — note the `bucket_rows` / `all_rows` / `window` parameters are DELETED (the old function's bucket-row windowed conv-ΔES is gone; the window is already applied inside `_compute_score_gap_timeline` when the orchestrator calls it per combo):
      ```
      def _compute_endgame_elo_weekly_series(
          score_gap_timeline: list[ScoreGapTimelinePoint],
          actual_elo_dates: Sequence[date],
          actual_elo_ratings: Sequence[int],
          cutoff_str: str | None = None,
      ) -> list[EndgameEloTimelinePoint]:
      ```
      Logic:
        - For each `ScoreGapTimelinePoint` in `score_gap_timeline`:
          - `eg_score_gap = pt.endgame_score - pt.non_endgame_score` (equivalently `pt.score_difference`).
          - Find `actual_elo_at_date` via asof-join: largest `actual_elo_dates[i] <= pt.date` (the existing asof-join helper already does this; reuse it).
          - `endgame_elo = _endgame_elo_from_score_gap(actual_elo_at_date, eg_score_gap, K)`.
          - Construct `EndgameEloTimelinePoint(date=pt.date, endgame_elo=endgame_elo, actual_elo=int(round(actual_elo_at_date)), endgame_games_in_window=pt.endgame_game_count, per_week_endgame_games=pt.per_week_total_games or per-week-endgame-only count, ...)` — match the existing field set on `EndgameEloTimelinePoint` exactly. Read the dataclass at `app/schemas/endgames.py` lines 532-564 to get the exact field set after the rename.
        - Skip points where `pt.endgame_game_count < MIN_GAMES_FOR_TIMELINE` (inherited from `_compute_score_gap_timeline`, no double-filtering needed — the producer already enforces this).
        - Apply `cutoff_str` filtering for pre-fill points: drop output points where `pt.date < cutoff_str`.
      Function-size check: this should be ~30-40 lines including the docstring. Below the CLAUDE.md soft-100 limit by a comfortable margin. Nesting depth ≤ 2.
    - **Rewire `get_endgame_elo_timeline`** (lines 2423-2519) — MANDATORY per-combo wiring:
      * Keep the existing `query_endgame_elo_timeline_rows` call. After the `bucket_rows` deletion (see "Sub-task: delete bucket_rows" below), the call returns `all_rows` only. The existing per-combo `all_by_combo` partition on `(platform, time_control)` is reused for both the Actual ELO asof-join AND the new per-combo score-gap derivation.
      * **PLANNER MANDATE — per-combo score-gap derivation, NO discretion**: inside the existing per-combo loop in `get_endgame_elo_timeline` that iterates `all_by_combo.items()` (i.e., the same loop that already calls into `_compute_endgame_elo_weekly_series` today), partition each combo's rows into endgame vs non-endgame using the existing classification helper that `_compute_score_gap_material` already uses (search `_compute_score_gap_material` around line 1099 in the file for the exact classification call). Call `_compute_score_gap_timeline(endgame_rows_for_combo, non_endgame_rows_for_combo, SCORE_GAP_TIMELINE_WINDOW, cutoff_str)` **ONCE PER COMBO**. Feed the resulting per-combo `ScoreGapTimelinePoint` list into `_compute_endgame_elo_weekly_series` for that combo. Emit one `EndgameEloTimelinePoint` per (combo, week).
      * **PROHIBITED**: do NOT consume the overview-level `score_gap_material.timeline` (the unfiltered cross-combo series produced upstream of `get_endgame_elo_timeline`). That series mixes games from all combos and would silently contaminate the per-combo Endgame ELO line with games from other combos. The Phase 57 / 87.4 invariant — Endgame ELO line paired with Actual ELO line per `(platform, TC)` combo — MUST hold.
      * **Concrete per-combo loop pseudocode** (replaces the existing weekly-series call site inside `get_endgame_elo_timeline`):
        ```
        endgame_elo_combos: list[EndgameEloTimelineCombo] = []
        for (platform, time_control), all_rows_for_combo in all_by_combo.items():
            # Partition by endgame classification (reuse the helper _compute_score_gap_material uses).
            endgame_rows_for_combo = [r for r in all_rows_for_combo if is_endgame_row(r)]
            non_endgame_rows_for_combo = [r for r in all_rows_for_combo if not is_endgame_row(r)]

            # Derive PER-COMBO score-gap series. NOT the overview-level cross-combo series.
            score_gap_timeline_for_combo = _compute_score_gap_timeline(
                endgame_rows_for_combo,
                non_endgame_rows_for_combo,
                SCORE_GAP_TIMELINE_WINDOW,
                cutoff_str,
            )

            # Same combo's Actual ELO asof arrays (existing logic — no change).
            actual_elo_dates, actual_elo_ratings = _build_actual_elo_asof_arrays(all_rows_for_combo)

            # Per-combo Endgame ELO weekly series via additive K mapping.
            timeline_for_combo = _compute_endgame_elo_weekly_series(
                score_gap_timeline_for_combo,
                actual_elo_dates,
                actual_elo_ratings,
                cutoff_str=cutoff_str,
            )

            endgame_elo_combos.append(
                EndgameEloTimelineCombo(
                    platform=platform,
                    time_control=time_control,
                    timeline=timeline_for_combo,
                )
            )
        ```
        Substitute `is_endgame_row` / `_build_actual_elo_asof_arrays` with the actual helper names the file already uses (read around line 1099 for the endgame classifier and around lines 2423-2519 for the asof-array builder; the names may be slightly different — keep their behavior verbatim, just route them through the new per-combo wiring).
      * **Sub-task: delete `bucket_rows` from the repo query and producer signature** (RECOMMENDED per researcher; matches the interfaces block above):
        - `app/repositories/endgame_repository.py` `query_endgame_elo_timeline_rows` (around lines 789-890): delete the `bucket_rows` subquery + its column selection. Return `list[<all_rows_tuple>]` (single sequence). Update the function docstring at line 800.
        - `app/services/endgame_service.py` `_compute_endgame_elo_weekly_series` (line 1420 area): drop `bucket_rows` from the signature per the rewritten signature above. The new signature is `_compute_endgame_elo_weekly_series(score_gap_timeline, actual_elo_dates, actual_elo_ratings, cutoff_str=None)`.
        - Update every caller in `endgame_service.py` in the same atomic edit pass (the only caller is `get_endgame_elo_timeline` itself).
        - Rationale: bucket_rows carried `eval_cp / eval_mate / result` columns used exclusively by the deleted `_windowed_conv_delta_es`. With that helper gone, the columns are dead weight. Deleting them keeps both the SQL query and the Python signature clean.
      * Rename local variable `conversion_elo_timeline` → `endgame_elo_timeline` at line 2705. Rename kwarg `conversion_elo_timeline=conversion_elo_timeline` → `endgame_elo_timeline=endgame_elo_timeline` at line 2724.
      * Update the comment block at lines 2698-2704 ("Phase 57 ELO-05 / Phase 87.4 D-06: paired Conversion ELO + Actual ELO timeline"): replace with a Phase 87.5 note referencing the additive K formula AND the per-combo score-gap invariant: "Endgame ELO is derived per (platform, TC) combo from that combo's own ScoreGapTimelinePoint series — never from the cross-combo overview-level score-gap timeline.
    - Remove unused imports if `_conversion_elo_from_skill` was the only consumer of `math.log10` — grep to verify before deleting. Likely `math` is used elsewhere; if so leave the import.

    **`app/services/insights_service.py`** (bare-minimum to keep ty/pytest green; Plan 03 owns the helper rename + `_SECTION_LAYOUT` move):
    - Per `<interfaces>` block above, rename the 7 quoted-string literals + field-access sites (lines 104, 181, 344, 833, 866, 837, 851, 853, 868, 871, 874, 876). Use grep `conversion_elo_gap\|conversion_elo_timeline` against the file to confirm all sites are caught.
    - Leave function name `_findings_conversion_elo_timeline` unchanged in this plan — Plan 03 will rename it to `_findings_endgame_elo_timeline` (avoids a parallel-edit dependency on a function name that Plan 03 will rename mid-air).
    - Update line 93 + line 149 comments to reference the new ID names.

    **`app/services/insights_llm.py`** (string literals only, Plan 03 owns prose + version + helpers):
    - Per `<interfaces>` block, rename the ~6 string-comparison sites (lines 103-105, 343-344, 864, 892, 1072, 1631, 1843). Use grep `"conversion_elo_gap"\|"conversion_elo_timeline"` against the file.
    - Rename the sentinel copy `"no Conversion ELO trajectory available yet"` → `"no Endgame ELO trajectory available yet"` (this is a user-visible string; in Phase 87.4 the equivalent rename happened in Plan 01 too — same pattern).
    - DO NOT touch `_PROMPT_VERSION`, helper names, `[summary conversion_elo` rendered header literal, or the inline blob — Plan 03 owns those.

    **`app/repositories/endgame_repository.py`**:
    - Docstring touch-up at line 800: if it says "Conversion ELO" change to "Endgame ELO". Function name `query_endgame_elo_timeline_rows` stays per RESEARCH.md §3.

    **Tests** — atomic rewrite in lockstep:
    - **`tests/test_endgame_service.py`**:
      * Lines 30-37 imports: remove `_conversion_elo_from_skill`, `_compute_endgame_elo_weekly_series` (the new function name is the same), `_windowed_conv_delta_es`, `_affine_recenter_conv_delta`, `PIVOT`, `ALPHA`, `CALIBRATION_VERSION` if any are imported. Add `K`, `_endgame_elo_from_score_gap` if needed locally (most tests can import from `app.services.endgame_service` directly).
      * `TestConversionElo` class (lines 3307-3343): DELETE wholesale. Coverage moved to `tests/services/test_endgame_elo_k_mapping.py` (Task 1).
      * `TestConversionEloTimeline` class (lines 3405-3810): REWRITE. The old tests fed bucket_rows with `eval_cp / result` for `_windowed_conv_delta_es`. New tests construct synthetic `ScoreGapTimelinePoint` lists + `actual_elo_dates / actual_elo_ratings` arrays and call `_compute_endgame_elo_weekly_series` directly. Rename the class to `TestEndgameEloTimeline`. Test methods to cover:
        - Empty score-gap series ⇒ empty output.
        - Single point at zero gap ⇒ endgame_elo == actual_elo.
        - Sparse-window points (endgame_game_count < MIN_GAMES_FOR_TIMELINE) are filtered out (verify the inherited behavior carries through).
        - cutoff_str drops pre-cutoff points.
        - asof-join uses largest date ≤ point date (test with non-aligned date sequences).
        - Multi-point series produces correct per-point endgame_elo = round(actual_elo + K * gap).
      * `test_endgame_elo_timeline_point_constructs` (line 4106): rename field `conversion_elo` → `endgame_elo` in the constructor.
    - **`tests/services/test_insights_service.py`**: every `"conversion_elo_timeline"` / `"conversion_elo_gap"` string literal renamed; `conversion_elo_timeline` field on mock `EndgameOverviewResponse` renamed. Grep first, rename in bulk.
    - **`tests/services/test_insights_service_series.py`**: rename `conversion_elo` field accesses on `EndgameEloTimelinePoint`; rename `"conversion_elo_timeline"` subsection-id literals. `_series_for_endgame_elo_combo` (RESEARCH.md Q6) — check the function name in the service; if it's already `_series_for_endgame_elo_combo` no rename needed.
    - **`tests/services/test_insights_llm.py`**: rename `MetricId` / `SubsectionId` string literal assertions to the new names. DO NOT rename `endgame_v32` → `endgame_v33` yet (Plan 03 bumps the version). The current `endgame_v32` assertion stays passing through this plan because Plan 01 leaves `_PROMPT_VERSION` untouched.
    - **`tests/test_endgames_router.py`**: any `conversion_elo_timeline` JSON key in HTTP response assertions → `endgame_elo_timeline`. Any `conversion_elo` field in nested points → `endgame_elo`.
    - **`tests/test_integration_routers.py`** (lines 351+ per RESEARCH.md §9): `data["conversion_elo_timeline"]` → `data["endgame_elo_timeline"]`; `point["conversion_elo"]` → `point["endgame_elo"]`.

    Critical CLAUDE.md gates:
    - `uv run ty check app/ tests/` exits 0 at the end of this task — no `# ty: ignore` workarounds.
    - `uv run ruff check .` exits 0.
    - `uv run pytest tests/` exits 0 with NO new failures vs the Phase 87.4 baseline (1527 passed, 6 skipped per 01-SUMMARY.md verification block).
    - The new `_endgame_elo_from_score_gap` carries `-> int` return type annotation.
    - No `asyncio.gather` is introduced on `AsyncSession` (the function is pure-CPU; no DB calls inside the weekly producer).
    - No magic numbers: `K` is the constant; the calibration percentile values (0.202, -0.227, etc.) live only in the comment block + test assertions.
    - Function-size: `_compute_endgame_elo_weekly_series` ≤ 100 logic LOC, nesting ≤ 3.
  </behavior>
  <action>
    1. Edit `app/schemas/endgames.py`: rename `EndgameEloTimelinePoint.conversion_elo` → `endgame_elo`; rename `EndgameOverviewResponse.conversion_elo_timeline` → `endgame_elo_timeline`. Update both docstrings to describe the additive K formula (D-01) and the Phase 87.5 supersession of Phase 87.4 PIVOT/ALPHA.
    2. Edit `app/services/endgame_service.py`:
       a. DELETE lines 1270-1271 (clamp constants), 1284-1286 (PIVOT/ALPHA/CALIBRATION_VERSION + surrounding 1273-1286 comment block), 1295-1317 (_affine_recenter_conv_delta), 1320-1342 (_conversion_elo_from_skill — preserve the supersession comment as a relocated note near the new K constant), 1345-1417 (_windowed_conv_delta_es).
       b. INSERT the new K constant block + `_endgame_elo_from_score_gap` helper at the same location (~line 1270 area) per the behavior spec.
       c. REWRITE `_compute_endgame_elo_weekly_series` per the spec: consume `ScoreGapTimelinePoint` list + actual_elo asof arrays, return `list[EndgameEloTimelinePoint]`.
       d. REWIRE `get_endgame_elo_timeline`: derive per-combo score-gap series by partitioning endgame/non-endgame rows per combo and calling `_compute_score_gap_timeline` per combo, then call the new weekly producer. Rename local var + kwarg at lines 2705 + 2724. Update comment at 2698-2704.
       e. Grep `import math` references and remove if `math.log10` was the only consumer (likely keep — there are other math users in the file).
    3. Edit `app/services/insights_service.py`: rename the 12+ sites per `<interfaces>` block. Do NOT rename the function `_findings_conversion_elo_timeline` (Plan 03 owns that).
    4. Edit `app/services/insights_llm.py`: rename the 6+ string-comparison sites + the sentinel copy per `<interfaces>` block. Do NOT touch `_PROMPT_VERSION`, helper names, header literals, or the inline blob.
    5. Edit `app/repositories/endgame_repository.py`: docstring touch-up at line 800 only.
    6. Update tests in lockstep (per behavior spec): `tests/test_endgame_service.py` (delete TestConversionElo, rewrite TestConversionEloTimeline → TestEndgameEloTimeline, rename field accesses), `tests/services/test_insights_service.py`, `tests/services/test_insights_service_series.py`, `tests/services/test_insights_llm.py`, `tests/test_endgames_router.py`, `tests/test_integration_routers.py`. Use a single grep `conversion_elo_timeline\|conversion_elo_gap\|\.conversion_elo` across `tests/` to enumerate all rename sites.
    7. Run quality gates iteratively as you go:
       - `uv run ty check app/ tests/` — must be clean before commit.
       - `uv run ruff check .` — must be clean.
       - `uv run ruff format app/services/endgame_service.py app/services/insights_service.py app/services/insights_llm.py app/schemas/endgames.py` — apply if formatting drifts.
       - `uv run pytest tests/ -x` — must pass with no new failures vs baseline.
    8. Verify the Wave 0 RED tests from Task 1 now pass:
       `uv run pytest tests/services/test_endgame_elo_k_mapping.py tests/schemas/test_endgames_schema.py -x` — all green.
    9. Verify the regression query: `grep -nE "PIVOT|ALPHA|CALIBRATION_VERSION|_affine_recenter_conv_delta|_windowed_conv_delta_es|_conversion_elo_from_skill" app/services/endgame_service.py | grep -v "^[[:space:]]*#"` — zero non-comment matches. (Comments may reference these names in the supersession note; that's fine — the grep regression test in Plan 03 handles the case-insensitive "Conversion ELO" sweep across all files.)
    10. Commit: `refactor(87.5-01): backend rewire — additive K mapping + Phase 87.4 deletion + schema rename`. Single commit per the atomic-pass rule.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run ruff check . && uv run pytest tests/ -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `app/services/endgame_service.py` contains `K: float = <locked_value>`, `_endgame_elo_from_score_gap` helper, rewired `_compute_endgame_elo_weekly_series`, and per-combo score-gap derivation in `get_endgame_elo_timeline`. Grep for `PIVOT|ALPHA|CALIBRATION_VERSION|_affine_recenter_conv_delta|_windowed_conv_delta_es|_conversion_elo_from_skill` returns zero non-comment matches.
    - `app/schemas/endgames.py` exposes `EndgameOverviewResponse.endgame_elo_timeline` and `EndgameEloTimelinePoint.endgame_elo`; the `conversion_elo*` fields are absent.
    - `app/services/insights_service.py` + `app/services/insights_llm.py` use the renamed Literal IDs throughout (string-literal level); function name `_findings_conversion_elo_timeline` and helper names in insights_llm.py are intentionally left for Plan 03.
    - All backend tests pass: `uv run pytest tests/ -x` exits 0; ty + ruff clean.
    - Wave 0 RED tests from Task 1 now pass GREEN.
    - Commit `refactor(87.5-01): backend rewire — additive K mapping + Phase 87.4 deletion + schema rename` exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → schema | `_compute_endgame_elo_weekly_series` produces `EndgameEloTimelinePoint` instances consumed by the overview API response. Pydantic validates at the boundary. |
| service → DB (read) | `_compute_score_gap_timeline` consumes pre-fetched row sequences from `query_endgame_elo_timeline_rows`. No new SQL paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.5-01 | Tampering | `_endgame_elo_from_score_gap` | accept | Pure function on float inputs from internal services. No user-controlled value reaches `K` or `eg_score_gap`. |
| T-87.5-02 | Information disclosure | `EndgameOverviewResponse.endgame_elo_timeline` JSON | accept | Same per-user data scope as Phase 87.4; the field rename does not widen access. The user-id scope check in `get_endgame_overview` is unchanged. |
| T-87.5-03 | Denial of service | per-combo `_compute_score_gap_timeline` call inside `get_endgame_elo_timeline` | accept | O(N) per combo where N is row count per combo. Combo count is bounded by `len(_ENDGAME_ELO_COMBO_ORDER)` (~6 combos). Worst case = 6× the existing single-call cost; well within the existing endpoint's perf envelope. |
| T-87.5-04 | Repudiation | K constant drift | mitigate | `test_K_constant_locked` asserts the exact value; any future change requires bumping the test, surfacing the recalibration as a visible commit. |
| T-87.5-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced; package legitimacy gate does not apply to this plan. |
</threat_model>

<verification>
After all 3 tasks land:

1. `uv run ty check app/ tests/` — clean.
2. `uv run ruff check .` — clean.
3. `uv run pytest tests/ -x` — pass count ≥ Phase 87.4 baseline (1527 + ~10 new K-mapping tests; ~5-10 test deletions from `TestConversionElo*` removal; net ≥ 1530).
4. `uv run python scripts/gen_endgame_zones_ts.py --check` — clean.
5. Grep regression queries:
   - `grep -nE "PIVOT|ALPHA|CALIBRATION_VERSION|_affine_recenter_conv_delta|_windowed_conv_delta_es|_conversion_elo_from_skill" app/services/endgame_service.py | grep -v '^[[:space:]]*#'` — zero non-comment matches.
   - `grep -n "conversion_elo_timeline\|conversion_elo_gap\|\.conversion_elo" app/` — zero matches (the only remaining Conversion ELO references are the function name `_findings_conversion_elo_timeline` in insights_service.py + helper names in insights_llm.py, which Plan 03 renames).
6. `grep K app/services/endgame_service.py | head` shows the locked K constant with a one-line rationale comment.

Plan 02 (frontend) and Plan 03 (LLM/CHANGELOG) can start once this plan's commits land on the working branch.
</verification>

<success_criteria>
Maps to ROADMAP Phase 87.5 Success Criteria:

- **SC#3**: `endgame_elo = round(actual_elo + K · eg_score_gap)` implemented in `_endgame_elo_from_score_gap` and asserted in `test_zero_gap_preserves_actual_elo` + `test_positive_p95_gap` + `test_negative_p05_gap` + `test_paired_diff_matches_formula`.
- **SC#4**: Single global K constant locked with one-line rationale block citing §3.1.6 percentile table. `test_K_constant_locked` asserts the locked value.
- **SC#5**: PIVOT, ALPHA, CALIBRATION_VERSION, `_affine_recenter_conv_delta`, `_windowed_conv_delta_es`, `_conversion_elo_from_skill` all removed from `app/services/endgame_service.py` (verified by grep). The Phase 57 multiplicative formula is retired.
- **SC#6 (backend half)**: `conversion_elo_timeline` → `endgame_elo_timeline` on `EndgameOverviewResponse`; `EndgameEloTimelinePoint.conversion_elo` → `endgame_elo`. Tests pass. Frontend rename ships in Plan 02.
- **SC#8**: This plan touches none of the Conv/Parity/Recov display-shift code (Phase 87.4 `SECTION2_DISPLAY_SHIFT` in `frontend/src/lib/endgameMetrics.ts` is frontend-only and out of this plan's scope). Verified by absence of changes to those surfaces.
- **SC#9 (backend half)**: K mapping unit tests in `tests/services/test_endgame_elo_k_mapping.py`. The Vitest positioning assertion + the "no Conversion ELO string" grep regression test ship in Plans 02 and 03 respectively.
</success_criteria>

<output>
After all 3 tasks land, create `.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/01-SUMMARY.md` using the standard summary template. Include:
- The locked K value with calibration rationale.
- The list of deleted symbols (constants + functions).
- The single-atomic-pass rename strategy and which sites were renamed in which file.
- Any auto-fixed deviations from the plan (Phase 87.4 Plan 01 had 3 such auto-fixes; expect similar here, especially around test-file rename scope).
- Verification commands and their pass counts.
- Note any Plan 02 / Plan 03 follow-ups required (helper rename `_findings_conversion_elo_timeline` → `_findings_endgame_elo_timeline` deferred to Plan 03; section-grouping move + prompt prose + `_PROMPT_VERSION` bump deferred to Plan 03).
</output>
