---
phase: 87.5-rebuild-endgame-elo-on-endgame-score-gap
plan: 03
type: execute
wave: 2
depends_on:
  - 87.5-01
files_modified:
  - app/services/insights_llm.py
  - app/services/insights_service.py
  - app/prompts/endgame_insights.md
  - tests/prompts/test_endgame_insights_prompt.py
  - tests/services/test_insights_llm.py
  - tests/services/test_insights_service.py
  - tests/test_no_conversion_elo_string.py
  - CHANGELOG.md
  - .planning/todos/pending/calibrate-endgame-elo-k.md
  - .planning/todos/done/calibrate-endgame-elo-k.md
autonomous: true
requirements:
  - EGELO-01b  # no "Conversion ELO" string in code outside CHANGELOG + supersession comment (SC#1)
  - EGELO-07  # LLM payload rename + subsection regrouping + endgame_v33 + prose restored (SC#7)
  - EGELO-09c  # grep regression test (SC#9)
  - EGELO-CL  # CHANGELOG entry under [Unreleased]

must_haves:
  truths:
    - "_PROMPT_VERSION reads 'endgame_v33' on app/services/insights_llm.py line 68"
    - "_SECTION_LAYOUT places ('subsection', 'endgame_elo_timeline') under 'overall', NOT under 'metrics_elo'"
    - "_findings_conversion_elo_timeline function renamed to _findings_endgame_elo_timeline; call site at line ~402 updated"
    - "Helper names _conversion_elo_per_bucket and _render_conversion_elo_summary_block renamed to _endgame_elo_per_bucket and _render_endgame_elo_summary_block"
    - "Rendered prompt header literal '[summary conversion_elo' renamed to '[summary endgame_elo'"
    - "app/prompts/endgame_insights.md glossary entries: 'endgame_elo' + 'endgame_elo_gap' replace 'conversion_elo' + 'conversion_elo_gap'; subsection heading 'Endgame ELO Timeline' replaces 'Conversion ELO Timeline'"
    - "Prompt prose restores 'lifts up / holds back actual rating' headline metaphor (Phase 87.4 traded it away)"
    - "tests/test_no_conversion_elo_string.py greps app/ + frontend/src/ + tests/ and asserts zero matches for case-insensitive 'Conversion ELO' outside the allowlist (CHANGELOG.md + supersession comment block + this regression test file itself)"
    - "CHANGELOG.md [Unreleased] section gains a ### Changed bullet describing Phase 87.5 (additive K, score-gap input, chart relocation, endgame_v33)"
    - "calibrate-endgame-elo-k.md migrates from .planning/todos/pending/ to .planning/todos/done/ via git mv with an ## Outcome section"
    - "uv run ty check app/ tests/ exits 0; uv run pytest tests/ -x exits 0"
  artifacts:
    - path: "app/services/insights_llm.py"
      provides: "_PROMPT_VERSION = 'endgame_v33' with v33 entry prepended at FRONT of inline blob; renamed helpers; updated string literals"
      contains: "_PROMPT_VERSION = \"endgame_v33\""
    - path: "app/prompts/endgame_insights.md"
      provides: "Endgame ELO glossary entries; subsection heading; Endgame Score Gap input framing; 'lifts up / holds back' metaphor"
      contains: "Endgame ELO Timeline"
    - path: "tests/test_no_conversion_elo_string.py"
      provides: "Regression grep test asserting no 'Conversion ELO' outside allowlist"
      min_lines: 40
    - path: "CHANGELOG.md"
      provides: "[Unreleased] ### Changed bullet for Phase 87.5"
      contains: "Phase 87.5"
  key_links:
    - from: "app/services/insights_llm.py::_SECTION_LAYOUT"
      to: "'endgame_elo_timeline' subsection"
      via: "tuple entry under the 'overall' section group, NOT 'metrics_elo'"
      pattern: "\\(\"subsection\", \"endgame_elo_timeline\"\\)"
    - from: "app/services/insights_service.py::_findings_endgame_elo_timeline"
      to: "subsection_id='endgame_elo_timeline'"
      via: "finding emitter for the renamed subsection"
      pattern: "_findings_endgame_elo_timeline"
    - from: "tests/test_no_conversion_elo_string.py"
      to: "supersession comment + CHANGELOG + regression test file allowlist"
      via: "subprocess grep with explicit exempt list"
      pattern: "grep.*-i.*Conversion ELO"
---

<objective>
Land the LLM payload + prompt + CHANGELOG + grep-regression test for Phase 87.5: rename `MetricId conversion_elo_gap → endgame_elo_gap`, rename `SubsectionId conversion_elo_timeline → endgame_elo_timeline`, move the subsection from `metrics_elo` group to `overall` group in `_SECTION_LAYOUT`, rename helpers (`_conversion_elo_per_bucket → _endgame_elo_per_bucket`, `_render_conversion_elo_summary_block → _render_endgame_elo_summary_block`), rewrite the prompt glossary + prose to restore the "lifts up / holds back" metaphor, bump `_PROMPT_VERSION` to `endgame_v33`, add the repo-wide grep regression test, write the CHANGELOG entry, and migrate the K calibration todo.

Purpose: SC#1 enforcement (the "no Conversion ELO outside CHANGELOG + supersession" regression), SC#7 (LLM payload rename + subsection regrouping + endgame_v33 prompt + restored metaphor), and SC#9 (grep regression test).

Output: `endgame_v33` prompt version, prompt prose restored to the original Phase 57 storytelling pattern via the additive K formula, all `conversion_elo_*` identifiers gone from insights_llm.py + insights_service.py + prompt file, regression test enforcing the audit, CHANGELOG bullet under `[Unreleased]`, K calibration todo moved to done/.

Mirrors Phase 87.4 Plan 03 structure (see `.planning/milestones/v1.17-phases/87.4-drop-endgame-skill-conversion-elo-timeline/03-SUMMARY.md`). Key differences: the rename direction reverses (Conv→Endgame in the prompt prose) and a brand-new grep regression test enforces the rule at CI time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-CONTEXT.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-RESEARCH.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-PATTERNS.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-01-PLAN.md
@.planning/milestones/v1.17-phases/87.4-drop-endgame-skill-conversion-elo-timeline/03-SUMMARY.md

<interfaces>
<!-- Key surfaces this plan modifies. Extracted from RESEARCH.md §6. Plan 01 already renamed string literals at the Literal-type level to keep ty clean; this plan owns prose + helper-name + version + section grouping + grep regression. -->

From `app/services/insights_llm.py`:
- Line 68: `_PROMPT_VERSION = "endgame_v32"` → `_PROMPT_VERSION = "endgame_v33"`. The inline append-only comment blob gets a new v33 entry prepended at the FRONT (Phase 87.4 Plan 03 D-08 pattern). Format: `# v33 (260517 Phase 87.5 ...): ... v32 (260516 Phase 87.4 ...): ... v31 ...`. v32 and all prior entries STAY VERBATIM.
- Lines 1166 (approx): `_conversion_elo_per_bucket` function → `_endgame_elo_per_bucket`.
- Lines 1187 (approx): `_render_conversion_elo_summary_block` function → `_render_endgame_elo_summary_block`. Call site inside `_render_subsection_block` updated in lockstep.
- Lines 1255-1256: rendered header literal `"[summary conversion_elo"` → `"[summary endgame_elo"`.
- Lines 1420-1438: `_SECTION_LAYOUT` move. Current state after Plan 01's string-literal rename:
  ```
  ("overall", [
    ("chart", "overall_wdl"),
    ("subsection", "overall"),
    ("subsection", "endgame_start_vs_end"),
    ("subsection", "score_timeline"),
  ]),
  ("metrics_elo", [
    ("subsection", "endgame_metrics"),
    ("subsection", "endgame_elo_timeline"),   # <- Plan 01 renamed this string but did NOT move it
  ]),
  ```
  Target state after this plan:
  ```
  ("overall", [
    ("chart", "overall_wdl"),
    ("subsection", "overall"),
    ("subsection", "endgame_start_vs_end"),
    ("subsection", "score_timeline"),
    ("subsection", "endgame_elo_timeline"),   # <- moved here from metrics_elo per D-07
  ]),
  ("metrics_elo", [
    ("subsection", "endgame_metrics"),
  ]),
  ```
- Docstrings and comments referencing "conversion_elo" / "Conversion ELO" (the ones Plan 01 deliberately left for Plan 03): rename. Includes `_proximity_hint`, `_render_series_block`, the v11 trend-flat-threshold-ELO comment, and the caller-site v11 comment for the timeline subsection.

From `app/services/insights_service.py`:
- Line 814 (approx): rename function `_findings_conversion_elo_timeline` → `_findings_endgame_elo_timeline`. Update the call site (line ~402 per RESEARCH.md §6).
- Internal string literals inside the renamed function (lines 837, 851, 853, 868, 871, 874, 876 — RESEARCH.md §6) were already renamed by Plan 01 to keep ty clean; verify with a grep `"conversion_elo` against the file and rename any leftover.
- Module docstring + section comments: update Phase 87.4 references to Phase 87.5.

From `app/prompts/endgame_insights.md` (RESEARCH.md §6 prompt file location):
- Metric glossary table (L83-L90 approx): rename `conversion_elo` → `endgame_elo`; update label "Conversion ELO" → "Endgame ELO". Rename `conversion_elo_gap` → `endgame_elo_gap`; update label.
- Near-edge marker rule (L99): rename `conversion_elo_gap` → `endgame_elo_gap`.
- Series-block rules (L132 / L138 / L140-L144): rename `conversion_elo_timeline` → `endgame_elo_timeline`; rewire the per-row pairing prose; the "absolute Conversion ELO" sentence becomes "absolute Endgame ELO" with the new definition framing.
- Stale-combo rule (L152): rename `conversion_elo_gap` → `endgame_elo_gap`.
- Cross-section stories rule (L190 / L193): rewire "Conversion lagging rating growth" → "Endgame lagging rating growth"; identifier renames lockstep.
- Multiple-combo rule heading (L251-L259): "Conversion ELO" → "Endgame ELO"; rewire the learning-arc cross-reference.
- **Glossary definitions** (L428-L443) — THE PROSE-SHIFT IS HERE:
  - `endgame_elo` (renamed from `conversion_elo`): "Your actual rating plus a small offset proportional to your Endgame Score Gap (the per-week windowed difference between endgame and non-endgame game outcomes). Positive Endgame Score Gap means your endgame games score better than your non-endgame games — your endgame is *lifting* your rating. Negative means your endgame is *holding back* your rating. The exact mapping is `endgame_elo = round(actual_elo + K * eg_score_gap)` for a single global constant K calibrated so a typical Endgame Score Gap (≈ ±10pp at p25/p75) maps to ±33-47 ELO; extreme gaps (±p05/p95 ≈ ±23pp) map to ±100 ELO." (Phrasing per CONTEXT.md §Specific Ideas; planner's discretion on exact words.)
  - `endgame_elo_gap` (renamed from `conversion_elo_gap`): "`endgame_elo − actual_elo`. Positive = endgame lifting rating; negative = endgame holding rating back." Preserve the zone-interpretation rule and the series-row `gap=` + `elo=` framing.
- Section mapping table (L472): rename `conversion_elo_timeline` → `endgame_elo_timeline`; UPDATE the section column from `metrics_elo` → `overall` (lockstep with `_SECTION_LAYOUT` move).
- `metrics_elo` coverage floor rule (L492): "Conversion ELO per-combo bullets" → "Endgame ELO per-combo bullets"; if the rule mentions the timeline as required-for-coverage in `metrics_elo`, REMOVE that mention (the timeline is no longer in `metrics_elo`).
- Forbidden-token rule (Phase 87.4 D-10): DO NOT use `ΔES` / `delta_es` / `conv_ΔES` in user-facing narration. The Phase 87.5 prose uses "Endgame Score Gap" (the user-facing series name) instead.

From `tests/prompts/test_endgame_insights_prompt.py`:
- Existing Phase 87.4 tests assert `"conversion_elo"`, `"conversion_elo_gap"`, `"conversion_elo_timeline"`, "Conversion ELO Timeline" are PRESENT in the prompt. INVERT these: assert ABSENT.
- Add: `"endgame_elo"`, `"endgame_elo_gap"`, `"endgame_elo_timeline"`, "Endgame ELO Timeline" are PRESENT.
- Add: prose contains both "lift" / "lifting" / "lifts" AND "hold back" / "holding back" / "holds back" (case-insensitive) — verifies the metaphor restoration.
- Add: section mapping table assigns `endgame_elo_timeline` to `overall` (not `metrics_elo`).
- Keep the forbidden-token rule: `ΔES`, `delta_es`, `conv_ΔES` absent in the user-facing glossary/narration sections (the v33 changelog blob in `insights_llm.py` may include `Conv ΔES` as historical reference; the test scopes to `app/prompts/endgame_insights.md` only).

From `tests/services/test_insights_llm.py`:
- Existing test `test_prompt_version_is_v32` → `test_prompt_version_is_v33`. Assertion `_PROMPT_VERSION == "endgame_v33"`.
- `test_prompt_version_bumped_to_v33` (renamed) — extend with Phase 87.5 substring assertions on the v33 entry (e.g., "Phase 87.5", "Endgame Score Gap input", "additive K"). Keep v32 + v31 entries asserted present (verifies the FRONT-prepend pattern).
- `test_non_fractional_metrics_renamed` — assert `"endgame_elo_gap"` in `_NON_FRACTIONAL_METRICS`, `"conversion_elo_gap"` absent.
- Helper-name tests if any directly reference `_conversion_elo_per_bucket` / `_render_conversion_elo_summary_block` — update to the new names.
- `[summary endgame_elo` header-literal tests (Phase 87.4 renamed these to `[summary conversion_elo` in 03; revert to `[summary endgame_elo`).
- Update cache-key / log-row tests that hardcode `"endgame_v32"` → `"endgame_v33"`.

From `tests/services/test_insights_service.py`:
- If any test imports or references `_findings_conversion_elo_timeline`, rename to `_findings_endgame_elo_timeline`.
- Subsection-grouping tests: if any assertion verifies the `_SECTION_LAYOUT` membership for `endgame_elo_timeline`, update from `metrics_elo` → `overall`.

From the NEW file `tests/test_no_conversion_elo_string.py`:
- Standalone pytest module asserting no live "Conversion ELO" / "conversion_elo" strings in source.
- Allowlist exemptions:
  - `CHANGELOG.md` (entire file — historical entries).
  - The supersession comment block in `app/services/endgame_service.py` (whatever Plan 01 placed there documenting the retired Phase 57 + Phase 87.4 helpers). Identify via marker comment `# PHASE 87.5 SUPERSESSION` or similar; Plan 01 should bracket it explicitly to make the test stable.
  - The regression test file itself (`tests/test_no_conversion_elo_string.py`) — its own string literals would otherwise self-fail.
  - This plan file (`87.5-03-PLAN.md`) and other `.planning/` documents — scope the grep to `app/`, `frontend/src/`, `tests/`, `CHANGELOG.md` only.
- Implementation:
  ```python
  import subprocess
  from pathlib import Path

  ALLOWLIST = {
      "CHANGELOG.md",
      "tests/test_no_conversion_elo_string.py",
      # The supersession comment block in endgame_service.py — verified by content marker, not file allowlist
  }

  def test_no_conversion_elo_in_live_source() -> None:
      result = subprocess.run(
          ["git", "grep", "-il", "conversion elo",
           "--", "app/", "frontend/src/", "tests/", "CHANGELOG.md"],
          capture_output=True, text=True, check=False,
      )
      hits = set(result.stdout.strip().splitlines())
      offenders = hits - ALLOWLIST
      # For app/services/endgame_service.py, verify any remaining hit is inside the supersession marker block
      if "app/services/endgame_service.py" in offenders:
          content = Path("app/services/endgame_service.py").read_text()
          # Allow only if every case-insensitive "Conversion ELO" sits between supersession markers
          # (Implementation: parse the marker block bounds, check each match.)
          # If parse confirms all hits are inside the marker block, remove from offenders.
          # Otherwise leave it as a failure.
          ...
      assert not offenders, f"Live 'Conversion ELO' references found in: {sorted(offenders)}"
  ```
  Detail of the supersession-block parsing: read the file, find the `# BEGIN PHASE 87.5 SUPERSESSION` and `# END PHASE 87.5 SUPERSESSION` markers (Plan 01 places these around the lifted comment block from `_conversion_elo_from_skill` lines 1320-1342). Use `re.finditer` to find all case-insensitive "Conversion ELO" occurrences and assert each is within `[BEGIN_offset, END_offset]`.

  Simpler alternative if Plan 01 didn't add explicit markers: use a sentinel comment `# Conversion-ΔES ELO (RETIRED 2026-05-17)` — a deliberate variant that doesn't trip the case-insensitive "Conversion ELO" regex. Then the assertion is strict zero matches everywhere except CHANGELOG + this test file. This is the path RESEARCH.md §9 recommends ("the supersession comment rewritten to avoid the exact phrase 'Conversion ELO' (using 'Conversion-ΔES ELO' or a deliberate variant)").

  Pick the simpler alternative: ASSERT zero matches in `app/`, `frontend/src/`, `tests/` (the regression test file allowlists itself); CHANGELOG.md retains history. Plan 01's supersession comment is rewritten in this plan's Task 1 if needed to avoid the bare phrase.
</interfaces>

<prose_pattern>
<!-- Phase 87.5 LLM prompt prose pattern — for the glossary + subsection narration. -->

The user-facing prose pattern (per CONTEXT.md §Specific Ideas, planner's discretion on exact phrasing):

> "Your Endgame ELO is your actual rating plus a small offset proportional to your Endgame Score Gap. Positive Endgame Score Gap means your endgame games score better than your non-endgame games — your endgame is *lifting* your rating. Negative means your endgame is *holding back* your rating. The constant K is calibrated so a typical Endgame Score Gap (≈ ±10pp at p25/p75) maps to ±33-47 ELO; extreme gaps (±p05/p95 ≈ ±23pp) map to ±100 ELO."

Hard requirements:
- "lifts" / "lift" / "lifting" appears at least once.
- "holds back" / "hold back" / "holding back" appears at least once.
- "Endgame Score Gap" is the user-facing series name.
- No `ΔES`, `delta_es`, `conv_ΔES`, `Conv ΔES` tokens in the user-facing glossary or narration sections.
- No `K = 450` magic number in the prompt — describe the calibration in band terms ("±10pp maps to ±33-47 ELO") so the prompt stays valid if K is retuned later.
- No mention of the deleted Phase 57 multiplicative formula or Phase 87.4 affine recenter — the prose stands on its own.
</prose_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests — prompt prose + version + grep regression</name>
  <files>
    tests/prompts/test_endgame_insights_prompt.py,
    tests/services/test_insights_llm.py,
    tests/test_no_conversion_elo_string.py
  </files>
  <behavior>
    Three test files:

    **A. `tests/prompts/test_endgame_insights_prompt.py`** — INVERT Phase 87.4 assertions + ADD Phase 87.5 metaphor + section-grouping checks. Read the existing file (Phase 87.4 Plan 03 created it with 7 grep-style assertions). Rewrite each:
    - `test_no_conversion_elo_text` — case-insensitive "conversion elo" + literal "conversion_elo" both absent from `app/prompts/endgame_insights.md`.
    - `test_no_conversion_elo_timeline_id` — literal "conversion_elo_timeline" absent.
    - `test_no_conversion_elo_gap_id` — literal "conversion_elo_gap" absent.
    - `test_endgame_elo_timeline_subsection_present` — "Endgame ELO Timeline" heading present.
    - `test_endgame_elo_timeline_id_present` — "endgame_elo_timeline" present in series-block + section table.
    - `test_endgame_elo_gap_id_present` — "endgame_elo_gap" present in metric glossary + zone rule.
    - `test_lifts_up_metaphor_present` — case-insensitive `re.search(r"\blift", prompt)` finds at least one match.
    - `test_holds_back_metaphor_present` — case-insensitive `re.search(r"\bholds?\s+back\b|\bholding\s+back\b", prompt)` finds at least one match.
    - `test_no_delta_es_token` — `re.search(r"ΔES|delta_es|conv_ΔES|Conv ΔES", prompt)` finds zero matches (CLAUDE.md D-10 forbidden-token rule, inherited from Phase 87.4).
    - `test_section_mapping_assigns_endgame_elo_timeline_to_overall` — parse the section mapping table (L472 area) and assert the row `| endgame_elo_timeline | overall |` (or whatever the table format is). A simple substring check is fine: `"| endgame_elo_timeline | overall" in prompt` after normalising whitespace.

    Module docstring at the top: explain that this file enforces SC#1 + SC#7 from Phase 87.5 ROADMAP.

    **B. `tests/services/test_insights_llm.py`** — UPDATE existing Phase 87.4 tests:
    - `test_prompt_version_is_v32` → `test_prompt_version_is_v33`. Body: `assert insights_llm._PROMPT_VERSION == "endgame_v33"`.
    - `test_prompt_version_bumped_to_v32` → `test_prompt_version_bumped_to_v33`. Extend with v33-specific substring assertions:
      ```python
      blob = inspect.getsource(insights_llm)
      assert "v33 (260517 Phase 87.5" in blob
      assert "v32 (260516 Phase 87.4" in blob  # preserved verbatim
      assert "v31 (260515 Phase 87.2" in blob  # preserved verbatim
      assert "Endgame Score Gap input" in blob or "endgame_elo_from_score_gap" in blob  # v33 narrative
      assert "additive K" in blob.lower()  # v33 narrative
      ```
    - Cache-key / log-row tests: `"endgame_v32"` → `"endgame_v33"`.
    - `test_non_fractional_metrics_renamed`: Plan 01 already renamed `_NON_FRACTIONAL_METRICS` string literal to `"endgame_elo_gap"` — this test asserts the set membership.
    - Header-literal tests: `[summary endgame_elo` (replaces `[summary conversion_elo`).
    - Helper-name tests if any reference `_conversion_elo_per_bucket` / `_render_conversion_elo_summary_block`: rename to `_endgame_elo_per_bucket` / `_render_endgame_elo_summary_block`.

    **C. `tests/test_no_conversion_elo_string.py`** — NEW FILE. The SC#1 + SC#9 regression. Implementation per `<interfaces>` block:
    ```python
    """Phase 87.5 regression: no live 'Conversion ELO' string outside the allowlist.

    Asserts the case-insensitive phrase 'Conversion ELO' (and identifier 'conversion_elo')
    does not appear in app/, frontend/src/, or tests/ except where explicitly allowlisted:
    - CHANGELOG.md (historical entries — kept verbatim)
    - This test file itself (would self-fail otherwise)
    """
    from __future__ import annotations
    import subprocess
    from pathlib import Path

    REPO_ROOT = Path(__file__).resolve().parents[1]
    ALLOWLIST_FILES = {
        "CHANGELOG.md",
        "tests/test_no_conversion_elo_string.py",
    }

    def _git_grep(pattern: str) -> set[str]:
        result = subprocess.run(
            ["git", "grep", "-il", pattern, "--",
             "app/", "frontend/src/", "tests/", "CHANGELOG.md"],
            cwd=REPO_ROOT, capture_output=True, text=True, check=False,
        )
        return {line.strip() for line in result.stdout.splitlines() if line.strip()}

    def test_no_conversion_elo_phrase_in_live_source() -> None:
        hits = _git_grep("conversion elo")
        offenders = hits - ALLOWLIST_FILES
        assert not offenders, (
            "Live 'Conversion ELO' references found outside allowlist: "
            f"{sorted(offenders)}. Allowlist: {sorted(ALLOWLIST_FILES)}."
        )

    def test_no_conversion_elo_identifier_in_live_source() -> None:
        hits = _git_grep("conversion_elo")
        offenders = hits - ALLOWLIST_FILES
        assert not offenders, (
            "Live 'conversion_elo' identifier references found outside allowlist: "
            f"{sorted(offenders)}."
        )
    ```

    The regression covers SC#1 ("no Conversion ELO string survives outside CHANGELOG + supersession") and SC#9 ("regression test asserting no rendered surface contains 'Conversion ELO' outside the supersession comment + CHANGELOG"). Note: the "supersession comment" exemption is achieved by Plan 01 rewriting the supersession block to avoid the bare phrase "Conversion ELO" (e.g., "Conversion-ΔES ELO" variant per RESEARCH.md §9), so this test does NOT need a special-cased file-content parse — the grep returns clean.

    If Plan 01's supersession comment still contains the bare phrase, this task MUST verify the marker-block approach: read `app/services/endgame_service.py`, find the marker block, assert all "Conversion ELO" matches sit within the marker. Pragmatic alternative recommended in Plan 01 Task 3: phrase the supersession note without the bare "Conversion ELO" phrase ("the Phase 87.4 Conversion-ΔES-driven ELO mapping ..." or "the prior conv-ΔES affine recenter ..."), so this test stays simple.

    Run `uv run pytest tests/prompts/ tests/services/test_insights_llm.py tests/test_no_conversion_elo_string.py -x` — expect RED on every new/inverted assertion because the source changes (prompt prose + version bump + helper renames + section grouping) haven't landed yet.
  </behavior>
  <action>
    1. Edit `tests/prompts/test_endgame_insights_prompt.py`: invert the 7 existing Phase 87.4 assertions per behavior A; add the 4 new assertions (lifts metaphor, holds-back metaphor, no-delta-es token, section-mapping-to-overall).
    2. Edit `tests/services/test_insights_llm.py`: rename the 2 `_PROMPT_VERSION` tests from v32 → v33 per behavior B; extend the bumped-version test with Phase 87.5 substring assertions; update header-literal + helper-name tests if present; update cache-key + log-row tests from `"endgame_v32"` → `"endgame_v33"`.
    3. Create `tests/test_no_conversion_elo_string.py` from scratch per behavior C. Use `git grep` via subprocess, scope to `app/ frontend/src/ tests/ CHANGELOG.md`, allowlist `CHANGELOG.md` + the test file itself.
    4. Run `uv run pytest tests/prompts/ tests/services/test_insights_llm.py tests/test_no_conversion_elo_string.py -x` — confirm RED.
    5. Commit: `test(87.5-03): Wave 0 RED tests for prompt v33 + metaphor restoration + grep regression`.
  </action>
  <verify>
    <automated>uv run pytest tests/prompts/test_endgame_insights_prompt.py tests/test_no_conversion_elo_string.py -x 2>&1 | grep -E "FAILED|ERROR" | head -10</automated>
  </verify>
  <done>
    - `tests/prompts/test_endgame_insights_prompt.py` inverted + 4 new Phase 87.5 assertions added.
    - `tests/services/test_insights_llm.py` v32 → v33 renames + Phase 87.5 narrative assertions added.
    - `tests/test_no_conversion_elo_string.py` created with two grep-based regression assertions.
    - `uv run pytest tests/prompts/ tests/test_no_conversion_elo_string.py -x` shows RED state — tasks 2-3 turn GREEN.
    - Commit `test(87.5-03): Wave 0 RED tests for prompt v33 + metaphor restoration + grep regression` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insights_llm.py v33 bump + helper renames + _SECTION_LAYOUT subsection move + insights_service.py helper rename</name>
  <files>
    app/services/insights_llm.py,
    app/services/insights_service.py
  </files>
  <behavior>
    **A. `app/services/insights_llm.py`**:

    1. **Prompt version bump** at line 68: `_PROMPT_VERSION = "endgame_v32"` → `_PROMPT_VERSION = "endgame_v33"`. PREPEND the v33 entry at the FRONT of the inline append-only comment blob (Phase 87.4 Plan 03 D-08 pattern). Target format:
       ```
       _PROMPT_VERSION = "endgame_v33"  # v33 (260517 Phase 87.5 Endgame ELO Timeline rebuild on Endgame Score Gap): rebuilt the Endgame ELO Timeline on the additive K mapping `endgame_elo = round(actual_elo + K * eg_score_gap)` where eg_score_gap is the per-week windowed Endgame Score Gap series. Phase 87.4 PIVOT/ALPHA/CALIBRATION_VERSION affine recenter deleted wholesale; Phase 57 multiplicative formula retired. MetricId `conversion_elo_gap` → `endgame_elo_gap`; SubsectionId `conversion_elo_timeline` → `endgame_elo_timeline`; subsection moves from `metrics_elo` to `overall` (co-located with Endgame Score Gap timeline). Prompt prose restores the "lifts up / holds back actual rating" metaphor that v32 traded away. v32 (260516 Phase 87.4 Conversion ELO rewire): ...
       ```
       Length: ~1500-1800 chars (one paragraph). Single space separator between v33 and v32 entries. NO newline reflow inside the blob. v32 and all prior entries preserved verbatim.

    2. **Helper renames** (Plan 01 left these intentionally for Plan 03):
       - `_conversion_elo_per_bucket` (line ~1166) → `_endgame_elo_per_bucket`.
       - `_render_conversion_elo_summary_block` (line ~1187) → `_render_endgame_elo_summary_block`.
       - Call site inside `_render_subsection_block` updated in lockstep.
       - Rendered header literal `"[summary conversion_elo"` (line ~1255-1256) → `"[summary endgame_elo"`.

    3. **`_SECTION_LAYOUT` subsection move** (lines 1420-1438): move `("subsection", "endgame_elo_timeline")` tuple OUT of the `"metrics_elo"` group and INTO the `"overall"` group, appended after `("subsection", "score_timeline")`. The `metrics_elo` group retains only `("subsection", "endgame_metrics")`. Match the target state shown in `<interfaces>` block above.

    4. **Docstring + comment renames** (Plan 01 left these explicitly for Plan 03):
       - `_proximity_hint` docstring: `"conversion_elo_gap"` → `"endgame_elo_gap"`.
       - `_render_series_block` docstring: `"conversion_elo_gap"` variant reference → `"endgame_elo_gap"`.
       - Within-noise-ELO inline comment: rename.
       - v11 trend-flat-threshold-ELO comment: rename `[summary conversion_elo]` → `[summary endgame_elo]`.
       - Caller-site v11 comment for the timeline subsection: rewrite to describe Phase 87.5 D-07 (subsection moved from `metrics_elo` to `overall`).

    **B. `app/services/insights_service.py`**:

    1. Function rename: `_findings_conversion_elo_timeline` (line ~814) → `_findings_endgame_elo_timeline`.
    2. Call site: line ~402 (RESEARCH.md §6), or wherever the function is invoked — grep for `_findings_conversion_elo_timeline` to find all sites.
    3. Module docstring / section header comments: update Phase 87.4 references to Phase 87.5.
    4. Verify no leftover `conversion_elo` string literals — Plan 01 renamed them, but a confirming grep catches drift: `grep -n "conversion_elo" app/services/insights_service.py` should return zero hits.

    Function-size check: the renamed `_findings_endgame_elo_timeline` should retain its existing structure — body content doesn't change. Verify nesting ≤ 3, logic LOC ≤ 100.

    Run iteratively:
    - `uv run ty check app/ tests/` — must be clean after each edit.
    - `uv run pytest tests/services/test_insights_llm.py tests/services/test_insights_service.py tests/services/test_insights_service_series.py -x` — must pass after the renames + the test file updates from Task 1.
  </behavior>
  <action>
    1. Edit `app/services/insights_llm.py`:
       a. Line 68: bump `_PROMPT_VERSION` to `"endgame_v33"`. Prepend the v33 entry at the FRONT of the inline comment blob per the format above. Keep v32 + v31 + all prior entries verbatim. Single space separator.
       b. Rename function `_conversion_elo_per_bucket` → `_endgame_elo_per_bucket` (def line + every call site).
       c. Rename function `_render_conversion_elo_summary_block` → `_render_endgame_elo_summary_block` (def line + every call site).
       d. Update the rendered header literal `"[summary conversion_elo"` → `"[summary endgame_elo"`.
       e. Move the `("subsection", "endgame_elo_timeline")` tuple in `_SECTION_LAYOUT` from `"metrics_elo"` group to `"overall"` group (append after `("subsection", "score_timeline")`).
       f. Rename remaining "conversion_elo" / "Conversion ELO" identifiers in docstrings + comments per RESEARCH.md §6 list. Use grep `conversion_elo\|Conversion ELO` against the file to enumerate.
    2. Edit `app/services/insights_service.py`:
       a. Rename function `_findings_conversion_elo_timeline` → `_findings_endgame_elo_timeline` (def line + call site at ~line 402).
       b. Update module docstring + section comments from Phase 87.4 → Phase 87.5 framing.
       c. Verify `grep -n "conversion_elo" app/services/insights_service.py` returns zero hits.
    3. Run quality gates:
       - `uv run ty check app/ tests/` — clean.
       - `uv run ruff check .` — clean.
       - `uv run ruff format app/services/insights_llm.py app/services/insights_service.py` — apply if drift.
       - `uv run pytest tests/services/test_insights_llm.py tests/services/test_insights_service.py tests/services/test_insights_service_series.py -x` — must pass.
    4. Commit: `refactor(87.5-03): bump _PROMPT_VERSION endgame_v33 + helper renames + subsection regrouping`.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run pytest tests/services/test_insights_llm.py tests/services/test_insights_service.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `app/services/insights_llm.py` line 68 reads `_PROMPT_VERSION = "endgame_v33"`; v33 entry prepended at FRONT of blob; v32 entry preserved verbatim.
    - Helpers `_endgame_elo_per_bucket` + `_render_endgame_elo_summary_block` exist (renamed from `_conversion_*`).
    - Rendered header literal `"[summary endgame_elo"` present.
    - `_SECTION_LAYOUT` places `("subsection", "endgame_elo_timeline")` under `("overall", [...])` group; `("metrics_elo", [...])` retains only `("subsection", "endgame_metrics")`.
    - `app/services/insights_service.py::_findings_endgame_elo_timeline` exists; call site updated; `grep conversion_elo app/services/insights_service.py` returns zero hits.
    - `uv run ty check app/ tests/` + `uv run pytest tests/services/test_insights_llm.py tests/services/test_insights_service.py -x` both green.
    - Commit `refactor(87.5-03): bump _PROMPT_VERSION endgame_v33 + helper renames + subsection regrouping` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: endgame_insights.md prompt rewrite + metaphor restoration + section table update</name>
  <files>
    app/prompts/endgame_insights.md
  </files>
  <behavior>
    Rewrite the prompt file end-to-end per `<interfaces>` and `<prose_pattern>` blocks. The rename direction reverses Phase 87.4: every `conversion_elo*` identifier and "Conversion ELO" label flips back to `endgame_elo*` / "Endgame ELO", AND the glossary prose restores the "lifts up / holds back" metaphor.

    **Section-by-section edits** (L line numbers approximate, follow the actual file):

    1. **Metric glossary table** (L83-L90): rename `conversion_elo` → `endgame_elo`; label "Conversion ELO" → "Endgame ELO". Rename `conversion_elo_gap` → `endgame_elo_gap`; label "Conversion ELO gap" → "Endgame ELO gap".

    2. **Near-edge marker rule** (L99): rename `conversion_elo_gap` → `endgame_elo_gap`.

    3. **Series-block rules** (L132-L144): rename `conversion_elo_timeline` → `endgame_elo_timeline` in the 3-subsection list. The per-row pairing rule: "absolute Conversion ELO" → "absolute Endgame ELO" with the new definition (additive K formula in prose). The "Conversion ELO composite" / "conversion strength is not keeping pace" wording is REPLACED with Phase 87.5 framing: "Endgame ELO offset / your endgame is lifting (or holding back) your rating relative to where your non-endgame games would put you".

    4. **Stale-combo rule** (L152): rename `conversion_elo_gap` → `endgame_elo_gap`.

    5. **Cross-section stories rule** (L190 / L193):
       - "Composure-under-pressure bottleneck": the Phase 87.4 wording referenced "typical-or-strong Conv/Parity/Recov rates" — STAYS unchanged (Phase 87.4 already removed the deleted `endgame_skill` reference; Phase 87.5 doesn't reintroduce it).
       - "Conversion lagging rating growth" → "Endgame lagging rating growth"; identifier rename `conversion_elo*` → `endgame_elo*` in lockstep.

    6. **Multiple-combo rule heading** (L251-L259): "Conversion ELO" → "Endgame ELO" throughout the heading + body. "conversion strength is lagging rating growth" → "endgame is holding back the rating" (restored metaphor framing).

    7. **Glossary definitions** (L428-L443) — THE PROSE REWRITE:
       - `endgame_elo` entry (renamed from `conversion_elo`): write the entry per `<prose_pattern>` above. Required tokens: "Endgame Score Gap" (the user-facing series name), "lifts" / "lifting", "holds back" / "holding back". The exact formula `endgame_elo = round(actual_elo + K * eg_score_gap)` SHOULD appear (it's user-comprehensible and matches the code). Calibration in band terms ("typical ±10pp maps to ±33-47 ELO; extreme ±23pp maps to ±100 ELO"). NO `ΔES` token. NO mention of PIVOT/ALPHA/affine recenter/Phase 57 multiplicative formula.
       - `endgame_elo_gap` entry (renamed from `conversion_elo_gap`): "`endgame_elo - actual_elo`. Positive = endgame lifting rating; negative = endgame holding rating back." Preserve the zone-interpretation rule + the series-row `gap=` + `elo=` framing.

    8. **Section mapping table** (L472): the row for the timeline subsection currently reads `| conversion_elo_timeline | metrics_elo |`. Update to `| endgame_elo_timeline | overall |`. Lockstep with the `_SECTION_LAYOUT` move in Task 2.

    9. **`metrics_elo` coverage floor rule** (L492): "Conversion ELO per-combo bullets" → "Endgame ELO per-combo bullets". IF this rule mentions the timeline as required for `metrics_elo` coverage, REMOVE that mention — the timeline now lives in `overall`, so `metrics_elo` coverage is solely the three Conv/Parity/Recov gauge findings (`endgame_metrics` subsection).

    10. **General hygiene sweep**: case-insensitive grep `grep -in "conversion elo\|conversion_elo" app/prompts/endgame_insights.md` after all edits — expect ZERO matches. If any survive (e.g., in narrative examples or "what the user sees" mock-ups), rename. The `endgame_insights.md` file becomes a complete inversion of the Phase 87.4 rename direction.

    Forbidden tokens check: `grep -E "ΔES|delta_es|conv_ΔES|Conv ΔES" app/prompts/endgame_insights.md` should return ZERO matches in glossary / narration sections (Phase 87.4 D-10 forbidden-token rule).

    Run `uv run pytest tests/prompts/test_endgame_insights_prompt.py -x` after the edit — all 10 assertions from Task 1 should now be GREEN.
  </behavior>
  <action>
    1. Edit `app/prompts/endgame_insights.md` per the 10 section-edits above. Work top-to-bottom through the file.
    2. After each batch of edits, run `grep -in "conversion elo\|conversion_elo" app/prompts/endgame_insights.md` to track remaining hits — target final state is zero matches.
    3. Verify forbidden-token rule: `grep -E "ΔES|delta_es|conv_ΔES" app/prompts/endgame_insights.md` returns zero matches. If hits exist, rewrite using "Endgame Score Gap" terminology.
    4. Run `uv run pytest tests/prompts/test_endgame_insights_prompt.py -x` — all 10 assertions GREEN.
    5. Run `uv run pytest tests/ -x` — full backend test suite must pass. The grep regression test `tests/test_no_conversion_elo_string.py` may still fail at this point because `CHANGELOG.md` hasn't gotten its Phase 87.5 entry yet (Task 4) — but Plan 01 already cleared the bulk of `conversion_elo*` from app/. Acceptable intermediate state: `test_no_conversion_elo_phrase_in_live_source` failures pointing to `app/prompts/endgame_insights.md` (if any survived) or to `app/services/endgame_service.py` (if Plan 01's supersession comment retained the bare phrase — see Task 1 alternative). Re-run after edits.
    6. Commit: `refactor(87.5-03): rewrite endgame_insights.md prompt — Conversion ELO → Endgame ELO + lifts/holds-back metaphor`.
  </action>
  <verify>
    <automated>uv run pytest tests/prompts/test_endgame_insights_prompt.py -x && grep -c "conversion elo\|conversion_elo" app/prompts/endgame_insights.md</automated>
  </verify>
  <done>
    - `app/prompts/endgame_insights.md` glossary entries: `endgame_elo` + `endgame_elo_gap` present with new framing; `conversion_elo` + `conversion_elo_gap` absent.
    - Subsection heading reads "Endgame ELO Timeline".
    - Glossary prose contains both "lift" and "holds back" tokens (case-insensitive).
    - No `ΔES` / `delta_es` / `conv_ΔES` tokens in the narration sections.
    - Section mapping table row reads `| endgame_elo_timeline | overall |`.
    - `grep -in "conversion elo\|conversion_elo" app/prompts/endgame_insights.md` returns zero matches.
    - `uv run pytest tests/prompts/test_endgame_insights_prompt.py -x` — 10/10 green.
    - Commit `refactor(87.5-03): rewrite endgame_insights.md prompt — Conversion ELO → Endgame ELO + lifts/holds-back metaphor` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: CHANGELOG entry + K calibration todo migration + final grep-regression verification</name>
  <files>
    CHANGELOG.md,
    .planning/todos/pending/calibrate-endgame-elo-k.md,
    .planning/todos/done/calibrate-endgame-elo-k.md
  </files>
  <behavior>
    **A. `CHANGELOG.md`** — append Phase 87.5 entry under `## [Unreleased]`, grouped into `### Changed` (alongside or replacing the Phase 87.4 Conversion ELO bullet). Pattern follows the bullet RESEARCH.md §10 drafts:

    Under `### Changed`:
    ```
    - **Phase 87.5: Endgame ELO Timeline rebuilt on Endgame Score Gap.** The timeline now uses an additive formula `endgame_elo = round(actual_elo + K · eg_score_gap)` where `eg_score_gap` is the per-week windowed difference between endgame and non-endgame game scores (the same series that drives the Endgame Score Gap over Time chart). This replaces the Phase 87.4 Conversion ΔES input and the Phase 57 multiplicative formula, eliminating the sigmoid bias that caused strong players' Endgame ELO to diverge from actual ELO by up to 500 points (benchmark §3.2.2 Conv ΔES ELO Cohen's d = 1.62 vs §3.1.6 Endgame Score Gap d = 0.17). At `eg_score_gap = 0` the chart shows Endgame ELO equal to Actual ELO. The chart is renamed "Endgame ELO Timeline" and relocated from the "Endgame Metrics" section (previously "Endgame Metrics and ELO") to "Endgame Overall Performance", directly below the "Endgame Score Gap over Time" chart. LLM Insights updated end-to-end; prompt version bumps `endgame_v32` → `endgame_v33` so prior cached reports regenerate with the restored "lifts up / holds back" framing. See `.planning/notes/endgame-elo-rebuild-on-score-gap.md` for the full rationale.
    ```

    Optional second bullet (if the section-header rename "Endgame Metrics and ELO" → "Endgame Metrics" was folded in by Plan 02 — RECOMMENDED per Plan 02 D-folded-in):
    ```
    - **Phase 87.5: "Endgame Metrics and ELO" section renamed to "Endgame Metrics".** With the Endgame ELO chart relocated to "Endgame Overall Performance", the section heading no longer describes its content.
    ```

    Phase 87.4 Conversion ELO bullet under `### Changed`: STAYS verbatim. Phase 87.5 supersedes it but the historical record is preserved. The Phase 87.4 `### Removed` Endgame Skill bullet ALSO stays.

    Verify the grep regression test now passes: `tests/test_no_conversion_elo_string.py` — `CHANGELOG.md` is allowlisted, so the Phase 87.4 historical bullets don't trip the assertion.

    **B. Migrate K calibration todo** via `git mv`:
    ```bash
    git mv .planning/todos/pending/calibrate-endgame-elo-k.md .planning/todos/done/calibrate-endgame-elo-k.md
    ```
    Append an `## Outcome` section at the bottom of the migrated file:
    ```
    ## Outcome

    Completed in Phase 87.5 Plan 01 Task 1. K locked at `K = <locked_value>` after eyeballing 5-10 representative users from the dev / benchmark DB spanning rating buckets. Rationale: <one-line summary matching the comment block in `app/services/endgame_service.py`>. See `.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/01-SUMMARY.md` for the calibration data.
    ```
    Substitute the actual locked K value from Plan 01.

    **C. Final verification**:
    - `uv run pytest tests/ -x` — full backend suite must pass, including:
      * `tests/test_no_conversion_elo_string.py::test_no_conversion_elo_phrase_in_live_source` — GREEN.
      * `tests/test_no_conversion_elo_string.py::test_no_conversion_elo_identifier_in_live_source` — GREEN.
      * `tests/prompts/test_endgame_insights_prompt.py` — 10/10 GREEN.
      * `tests/services/test_insights_llm.py` — v33 + helper-name + header-literal assertions GREEN.
    - `uv run ty check app/ tests/` — clean.
    - `uv run ruff check .` — clean.
    - `npm test -- --run` (frontend) — Plan 02 should have passed; verify no regression.
    - Final grep regression sweep: `git grep -il "Conversion ELO" -- app/ frontend/src/ tests/ CHANGELOG.md` should return only `CHANGELOG.md` and `tests/test_no_conversion_elo_string.py` (the allowlist).
  </behavior>
  <action>
    1. Edit `CHANGELOG.md`: add the Phase 87.5 bullet under `## [Unreleased]` ### Changed per the wording above. If the section-header rename was folded in by Plan 02, add the second bullet. Keep Phase 87.4 bullets verbatim.
    2. `git mv .planning/todos/pending/calibrate-endgame-elo-k.md .planning/todos/done/calibrate-endgame-elo-k.md`. Verify rename detection: `git status` shows `R ` (renamed) rather than D + A. If the rename similarity is below the threshold, force with `git add` after the move.
    3. Append the `## Outcome` section to the migrated file using the actual locked K value from Plan 01's SUMMARY.
    4. Run the final verification sweep:
       - `uv run ty check app/ tests/` — clean.
       - `uv run ruff check .` — clean.
       - `uv run ruff format --check app/services/insights_llm.py app/services/insights_service.py app/prompts/endgame_insights.md tests/prompts/test_endgame_insights_prompt.py tests/test_no_conversion_elo_string.py tests/services/test_insights_llm.py CHANGELOG.md` — clean for files this plan touched. If drift, apply format.
       - `uv run pytest tests/ -x` — full suite green.
       - `cd frontend && npm test -- --run` — frontend green (verifies Plan 02's tests still pass).
       - `git grep -il "Conversion ELO" -- app/ frontend/src/ tests/ CHANGELOG.md` — returns only `CHANGELOG.md` and `tests/test_no_conversion_elo_string.py`.
    5. Commit: `docs(87.5-03): CHANGELOG entry for Phase 87.5 + migrate K calibration todo + final grep verification`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_no_conversion_elo_string.py tests/prompts/test_endgame_insights_prompt.py tests/services/test_insights_llm.py -x && test -f .planning/todos/done/calibrate-endgame-elo-k.md && ! test -f .planning/todos/pending/calibrate-endgame-elo-k.md && grep -q "Phase 87.5" CHANGELOG.md</automated>
  </verify>
  <done>
    - `CHANGELOG.md` `[Unreleased]` ### Changed contains the Phase 87.5 bullet describing the additive K rebuild, chart relocation, prompt v33 bump.
    - `.planning/todos/done/calibrate-endgame-elo-k.md` exists with `## Outcome` section and the locked K value.
    - `.planning/todos/pending/calibrate-endgame-elo-k.md` no longer exists.
    - `git grep -il "Conversion ELO" -- app/ frontend/src/ tests/ CHANGELOG.md` returns only `CHANGELOG.md` and `tests/test_no_conversion_elo_string.py`.
    - All quality gates green: `uv run ty check`, `uv run ruff check .`, `uv run pytest tests/ -x`, `cd frontend && npm test -- --run`.
    - Commit `docs(87.5-03): CHANGELOG entry for Phase 87.5 + migrate K calibration todo + final grep verification` exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → LLM provider | The prompt text in `endgame_insights.md` + the rendered payload from `_render_endgame_elo_summary_block` are sent to an external LLM service. No user-controlled value reaches the prompt body. |
| service → cached LLM reports | Bumping `_PROMPT_VERSION` to `endgame_v33` invalidates all cached LLM reports keyed on the prior version. Documented intentional behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.5-LLM-01 | Tampering | `_PROMPT_VERSION` cache key | mitigate | Bump from `endgame_v32` → `endgame_v33` is the cache invalidation mechanism; cache-key tests in `test_insights_llm.py` assert the bump propagates to log rows + response-report `prompt_version` field. |
| T-87.5-LLM-02 | Information disclosure | Prompt prose | accept | Glossary describes the formula and the metaphor; no user PII flows through the prompt body. |
| T-87.5-LLM-03 | Repudiation | Prompt drift | mitigate | `_PROMPT_VERSION` blob is append-only at the FRONT; v32 and prior entries preserved verbatim so reading the blob reconstructs full history. |
| T-87.5-LLM-04 | DoS | LLM token budget | accept | The rewritten glossary entries are similar in length to the Phase 87.4 entries (one paragraph each); no significant token-budget shift. |
| T-87.5-LLM-SC | Tampering | Regression test bypass | mitigate | `tests/test_no_conversion_elo_string.py` makes the SC#1 invariant a CI gate; future drift fails the test, not silently passes. |
</threat_model>

<verification>
After all 4 tasks land:

1. `uv run ty check app/ tests/` — clean.
2. `uv run ruff check .` — clean.
3. `uv run pytest tests/ -x` — full backend suite green, including:
   - `tests/test_no_conversion_elo_string.py` (2 tests).
   - `tests/prompts/test_endgame_insights_prompt.py` (10 tests).
   - `tests/services/test_insights_llm.py` (Phase 87.5 v33 assertions).
   - `tests/services/test_insights_service.py` (helper-rename + section-grouping assertions).
4. `cd frontend && npm test -- --run` — frontend suite green (verifies Plan 02 hasn't drifted).
5. Repo-wide regression:
   - `git grep -il "Conversion ELO" -- app/ frontend/src/ tests/ CHANGELOG.md` returns only `CHANGELOG.md` + `tests/test_no_conversion_elo_string.py`.
   - `git grep -il "conversion_elo" -- app/ frontend/src/ tests/ CHANGELOG.md` returns only `CHANGELOG.md` + `tests/test_no_conversion_elo_string.py` (the historical CHANGELOG bullet from Phase 87.4 + this test's own string literals).
6. `.planning/todos/pending/calibrate-endgame-elo-k.md` absent; `.planning/todos/done/calibrate-endgame-elo-k.md` present with `## Outcome` section.
7. `git log --follow .planning/todos/done/calibrate-endgame-elo-k.md` shows a rename record (R-detection), preserving the prior history.
</verification>

<success_criteria>
Maps to ROADMAP Phase 87.5 Success Criteria:

- **SC#1**: `tests/test_no_conversion_elo_string.py` enforces the repo-wide rule: no live "Conversion ELO" string in `app/`, `frontend/src/`, `tests/` outside the allowlist (CHANGELOG.md + the test file itself). Plan 02 already cleared the frontend; this plan clears the LLM/prompt layer and adds the CI gate.
- **SC#7**: `MetricId` `endgame_elo_gap`, `SubsectionId` `endgame_elo_timeline`, subsection moved to `overall` group (verified by `_SECTION_LAYOUT` test + section-mapping table assertion). Prompt prose restored with "lifts" / "holds back" metaphor (verified by `tests/prompts/test_endgame_insights_prompt.py`). `_PROMPT_VERSION` = `endgame_v33` (verified). Phase 87.4 `[summary endgame_elo | …]` literal + `### Subsection:` header + glossary entry kept structurally, rewired to the new metric.
- **SC#9 (final piece)**: grep regression test + the metaphor-restoration assertions in `test_endgame_insights_prompt.py`. Combined with Plan 01's K-mapping unit tests and Plan 02's Vitest positioning test, SC#9 is fully covered.
- **CHANGELOG entry**: under `## [Unreleased]` ### Changed; Phase 87.4 historical bullets preserved verbatim.
</success_criteria>

<output>
After all 4 tasks land, create `.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/03-SUMMARY.md` using the standard summary template. Include:
- The v33 blob position + length (Phase 87.4 Plan 03 documented these explicitly — mirror that).
- The list of identifier renames in `insights_llm.py` + `insights_service.py` + `endgame_insights.md`.
- The `_SECTION_LAYOUT` subsection move from `metrics_elo` to `overall` (lockstep with prompt section table).
- The CHANGELOG bullet wording (verbatim).
- The K calibration todo migration confirmation (`git log --follow` rename record).
- The final grep-regression verification output: which files contain "Conversion ELO" after the phase (expected: `CHANGELOG.md` + `tests/test_no_conversion_elo_string.py`).
- Any auto-fixed deviations from the plan (Phase 87.4 Plan 03 had 4 such auto-fixes — expect similar around test-file rename scope + ruff format).
- Confirm Plan 01 + Plan 02 SUMMARYs are also in place and the phase is ready for `/gsd:verify-phase`.
</output>
