---
phase: 87.5-rebuild-endgame-elo-on-endgame-score-gap
plan: 02
type: execute
wave: 2
depends_on:
  - 87.5-01
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/ConversionEloTimelineSection.tsx
  - frontend/src/components/charts/EndgameEloTimelineSection.tsx
  - frontend/src/components/charts/__tests__/ConversionEloTimelineSection.test.tsx
  - frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/Home.tsx
  - frontend/src/lib/theme.ts
  - frontend/src/lib/endgameMetrics.ts
  - frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx
  - frontend/src/__tests__/noEndgameSkillString.test.tsx
autonomous: true
requirements:
  - EGELO-01a  # chart name "Endgame ELO Timeline" everywhere on frontend (SC#1)
  - EGELO-02  # chart renders below EndgameScoreOverTimeChart in Endgame Overall Performance (SC#2)
  - EGELO-06b  # frontend file rename + types rename (SC#6)
  - EGELO-09b  # Vitest render + DOM positioning test (SC#9)

must_haves:
  truths:
    - "ConversionEloTimelineSection.tsx no longer exists; EndgameEloTimelineSection.tsx exports EndgameEloTimelineSection"
    - "Endgame ELO chart renders directly below EndgameScoreOverTimeChart inside the 'Endgame Overall Performance' DOM container"
    - "Chart heading text reads 'Endgame ELO Timeline'; popover title, error copy, alt text all read 'Endgame ELO'"
    - "TypeScript types: ConversionEloTimelinePoint/Combo/Response renamed to EndgameEloTimelinePoint/Combo/Response; field .conversion_elo renamed to .endgame_elo"
    - "EndgameOverviewResponse consumer reads data.endgame_elo_timeline (matches Plan 01 backend rename)"
    - "Vitest assertion: renamed section appears AFTER EndgameScoreOverTimeChart in DOM order using compareDocumentPosition"
    - "noEndgameSkillString.test.tsx extended to also assert no 'Conversion ELO' string in rendered output (excluding test describe blocks + provenance comments)"
    - "npm run knip exits 0; npx tsc --noEmit exits 0; npm run lint exits 0; npm test -- --run exits 0"
  artifacts:
    - path: "frontend/src/components/charts/EndgameEloTimelineSection.tsx"
      provides: "Renamed timeline component with Endgame ELO branding"
      contains: "export function EndgameEloTimelineSection"
    - path: "frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx"
      provides: "Vitest render + heading + popover + error copy assertions"
      min_lines: 80
    - path: "frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx"
      provides: "DOM positioning assertion: ELO section after EndgameScoreOverTimeChart"
      min_lines: 50
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "frontend/src/components/charts/EndgameEloTimelineSection.tsx"
      via: "ESM import + JSX mount in the 'Endgame Overall Performance' guard block"
      pattern: "import .*EndgameEloTimelineSection.*from"
    - from: "frontend/src/pages/Endgames.tsx"
      to: "data.endgame_elo_timeline"
      via: "TanStack Query data field access for eloTimelineData prop"
      pattern: "endgame_elo_timeline"
---

<objective>
Land the frontend rewire for Phase 87.5: rename the timeline component file + types + every rendered surface from "Conversion ELO" to "Endgame ELO", relocate the chart out of "Endgame Metrics and ELO" into "Endgame Overall Performance" (directly below `EndgameScoreOverTimeChart`), and add the Vitest DOM-position assertion that locks the new layout.

Purpose: Frontend half of SC#1 (no "Conversion ELO" survives in rendered text), SC#2 (chart relocated below score-gap chart), SC#6 (file + types rename), SC#9 (Vitest render + position test).

Output: Renamed component file, types rename, page composition rewired, optional section-header rename "Endgame Metrics and ELO" → "Endgame Metrics" (recommended fold-in per RESEARCH.md Q3 — the ELO label is no longer accurate once the chart leaves), Vitest suite green, knip clean.

Mirrors Phase 87.4 Plan 02 structure (see `.planning/milestones/v1.17-phases/87.4-drop-endgame-skill-conversion-elo-timeline/02-SUMMARY.md`). Key differences: this plan does NOT touch the Conv/Parity/Recov display-shift code (SC#8 — that stays untouched), but it DOES add the chart-positioning Vitest assertion (Phase 87.4 had no positioning assertion because the chart didn't move).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-CONTEXT.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-RESEARCH.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-PATTERNS.md
@.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/87.5-01-PLAN.md
@.planning/milestones/v1.17-phases/87.4-drop-endgame-skill-conversion-elo-timeline/02-SUMMARY.md

<interfaces>
<!-- Key frontend surfaces this plan modifies. Extracted from RESEARCH.md §4-§5 + §9. -->

From `frontend/src/types/endgames.ts`:
- Lines 255-262 (approx): `ConversionEloTimelinePoint` interface — rename to `EndgameEloTimelinePoint`. Field `conversion_elo: number` → `endgame_elo: number`.
- Lines ~285-295: `ConversionEloTimelineCombo` interface — rename to `EndgameEloTimelineCombo`.
- Lines ~300-310: `ConversionEloTimelineResponse` interface — rename to `EndgameEloTimelineResponse`.
- Line 262 (approx): `EndgameOverviewResponse.conversion_elo_timeline: ConversionEloTimelineResponse` → `endgame_elo_timeline: EndgameEloTimelineResponse`.
- Update TSDoc on these types to describe the additive K formula instead of the Phase 87.4 affine recenter. Reference Phase 87.5 D-01 in a comment.

From `frontend/src/components/charts/ConversionEloTimelineSection.tsx` (currently exists; will be renamed):
- Function name: `ConversionEloTimelineSection` → `EndgameEloTimelineSection`.
- Props interface: `ConversionEloTimelineSectionProps` → `EndgameEloTimelineSectionProps`.
- Chart heading text: "Conversion ELO Timeline" → "Endgame ELO Timeline".
- InfoPopover aria-label / testId / body prose: any "Conversion ELO" string → "Endgame ELO".
- Error copy: "Failed to load Conversion ELO timeline" → "Failed to load Endgame ELO timeline".
- TSDoc / comments: rewrite to reference the additive K formula and the new co-location with `EndgameScoreOverTimeChart`.
- Per-point field access: `point.conversion_elo` → `point.endgame_elo` (matches the type rename above).
- The Phase 87.4 popover prose (Conv ΔES framing, affine recenter description) is replaced with the "lifts up / holds back" metaphor + the additive K formula in prose (no internal `ΔES` / `delta_es` tokens; per CLAUDE.md popover copy minimalism — WHAT + sign convention only, no jargon).

From `frontend/src/pages/Endgames.tsx`:
- Line 30 (approx): `import { ConversionEloTimelineSection } from '@/components/charts/ConversionEloTimelineSection';` → `import { EndgameEloTimelineSection } from '@/components/charts/EndgameEloTimelineSection';`.
- Lines ~475-486: existing "Endgame Overall Performance" block, currently:
  ```
  {scoreGapData && (
    <EndgameOverallPerformanceSection data={perfData} scoreGap={scoreGapData} />
  )}
  {scoreGapData && scoreGapData.timeline.length > 0 && (
    <div className="charcoal-texture rounded-md p-4">
      <EndgameScoreOverTimeChart ... />
    </div>
  )}
  ← INSERT new EndgameEloTimelineSection slot HERE, before SectionInsightSlot
  <SectionInsightSlot sectionId="overall" ... />
  ```
- Lines 487-504: existing "Endgame Metrics and ELO" block — DELETE the `<div data-testid="endgame-elo-timeline-section"><ConversionEloTimelineSection ... /></div>` wrapper from inside this block (lines ~494-498). Keep the `<EndgameMetricsSection />` and `<SectionInsightSlot sectionId="metrics_elo" ... />`.
- Line 490 (h2 heading): "Endgame Metrics and ELO" — RECOMMENDED to rename to "Endgame Metrics" (per RESEARCH.md Q3 fold-in). Decision: fold in. The h2 text reads "Endgame Metrics" after this plan.
- Line ~497 / overviewData consumer: `overviewData?.conversion_elo_timeline` → `overviewData?.endgame_elo_timeline`.
- Data wiring for the relocated chart: `eloTimelineData` derived from `data?.endgame_elo_timeline` continues to flow to the renamed component; it just renders in a different DOM location.

From `frontend/src/pages/Home.tsx`:
- Line ~51-54: SEO/alt copy "Conversion ELO timeline" → "Endgame ELO timeline" (or restore the prior "Endgame ELO timeline" wording per the design note). Read the current copy and adjust prose to match Phase 87.5 framing.

From `frontend/src/lib/theme.ts`:
- Line ~104-106: comment block "Conversion ELO Timeline chart combo palette" → "Endgame ELO Timeline chart combo palette". Constant name `ELO_COMBO_COLORS` is generic and stays. Constant name `ENDGAME_VOLUME_BAR_COLOR` stays.

From `frontend/src/lib/endgameMetrics.ts`:
- Line ~70: comment referencing "Conversion ELO" — update to "Endgame ELO". `SECTION2_DISPLAY_SHIFT` constant + values STAY UNTOUCHED (SC#8 — Phase 87.4 Conv/Parity/Recov display-centering is independent of the timeline rewire).
- Verify with `git diff -- frontend/src/lib/endgameMetrics.ts` after the edit: only comments changed, no logic.

From `frontend/src/components/charts/__tests__/ConversionEloTimelineSection.test.tsx` (currently exists; will be renamed via `git mv`):
- File rename: `ConversionEloTimelineSection.test.tsx` → `EndgameEloTimelineSection.test.tsx`.
- `import { ConversionEloTimelineSection }` → `import { EndgameEloTimelineSection }`.
- `buildResponse()` helper: rename `conversion_elo` field → `endgame_elo`; rename type aliases.
- `describe('ConversionEloTimelineSection', ...)` → `describe('EndgameEloTimelineSection', ...)`.
- Assertion text "Conversion ELO Timeline" → "Endgame ELO Timeline" (chart heading), info-popover testid `conversion-elo-timeline-info` → `endgame-elo-timeline-info`, error copy "Failed to load Conversion ELO timeline" → "Failed to load Endgame ELO timeline".
- Inverse assertion `queryAllByText(/Endgame ELO Timeline/i).toHaveLength(0)` → `queryAllByText(/Conversion ELO Timeline/i).toHaveLength(0)` (the absence direction flips). RESEARCH.md §9 describes this swap.
- `beforeAll` matchMedia / ResizeObserver setup STAYS verbatim (vitest boilerplate per Phase 87.4 pattern).

From `frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx`:
- Line ~73-74: `vi.mock(...ConversionEloTimelineSection...)` → `vi.mock(...EndgameEloTimelineSection...)`. Mock function name updated.
- Line 257 (approx): `conversion_elo_timeline: { combos: [], timeline_window: 100 }` → `endgame_elo_timeline: { combos: [], timeline_window: 100 }` in the mock overview data builder.
- ADD a new positioning test: render `Endgames` page (or the "Endgame Overall Performance" section subtree) with both `EndgameScoreOverTimeChart` and `EndgameEloTimelineSection` mounted; assert that the ELO section container appears AFTER the score-over-time chart container in DOM order using `compareDocumentPosition`:
  ```typescript
  const scoreChart = container.querySelector('[data-testid="endgame-score-over-time-chart"]');
  const eloChart = container.querySelector('[data-testid="endgame-elo-timeline-section"]');
  expect(scoreChart && eloChart).toBeTruthy();
  expect(
    scoreChart!.compareDocumentPosition(eloChart!) & Node.DOCUMENT_POSITION_FOLLOWING
  ).toBeTruthy();
  ```
  Also assert that the ELO section is NOT a descendant of the "Endgame Metrics" section: `expect(metricsSection?.contains(eloChart)).toBe(false);`.

From `frontend/src/__tests__/noEndgameSkillString.test.tsx`:
- Mock import path: `ConversionEloTimelineSection` → `EndgameEloTimelineSection`. Mock data field rename.
- EXTEND the regression assertion: currently asserts no "Endgame Skill" string. ADD a parallel assertion that no "Conversion ELO" string (case-insensitive, excluding test scaffolding) appears in rendered output. This is the frontend half of SC#1's regression test — Plan 03 owns the backend Python grep-regression test.
</interfaces>

<dom_structure>
<!-- Target DOM order after Phase 87.5 in the "Endgame Overall Performance" container. -->

```
<section data-testid="endgame-overall-performance">
  <EndgameOverallPerformanceSection />     {/* Cards 1/2/3 + Score Gap row */}
  <div className="charcoal-texture rounded-md p-4">
    <EndgameScoreOverTimeChart                 ← anchor
      data-testid="endgame-score-over-time-chart"
    />
  </div>
  <div className="charcoal-texture rounded-md p-4"
       data-testid="endgame-elo-timeline-section">
    <EndgameEloTimelineSection />              ← NEW position
  </div>
  <SectionInsightSlot sectionId="overall" />
</section>

<section data-testid="endgame-metrics">    {/* renamed from endgame-metrics-and-elo if such testid exists */}
  <h2>Endgame Metrics</h2>                  {/* renamed from "Endgame Metrics and ELO" */}
  <EndgameMetricsSection />                  {/* Conv/Parity/Recov gauges */}
  <SectionInsightSlot sectionId="metrics_elo" />
</section>
```

The `data-testid="endgame-elo-timeline-section"` is preserved verbatim (it already uses Endgame ELO naming — RESEARCH.md §5 notes this); the wrapper just moves to a new parent section.
</dom_structure>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests — renamed Vitest file + new DOM positioning assertion</name>
  <files>
    frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx,
    frontend/src/components/charts/__tests__/ConversionEloTimelineSection.test.tsx,
    frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx,
    frontend/src/__tests__/noEndgameSkillString.test.tsx
  </files>
  <behavior>
    Three test files, all initially RED because Plan 02 source changes haven't landed yet:

    **A. `frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx`** — `git mv` from `ConversionEloTimelineSection.test.tsx` to preserve history (Phase 87.4 Plan 02 attempted this and discovered the source file didn't exist; in Phase 87.5 the source file DOES exist — it was created by Phase 87.4 Plan 02 Task 1 per 02-SUMMARY.md). Update inside the moved file:
    - Import: `import { EndgameEloTimelineSection } from '../EndgameEloTimelineSection'` (path changes too — the source file rename happens in Task 2).
    - Type imports: `EndgameEloTimelineResponse`, `EndgameEloTimelinePoint`.
    - `buildResponse()` helper: field `endgame_elo` (was `conversion_elo`).
    - `describe('EndgameEloTimelineSection', ...)` — block name updated.
    - Test 1: `it('renders the chart heading', ...)` — `expect(screen.getByText('Endgame ELO Timeline')).toBeInTheDocument();`.
    - Test 2: `it('renders the info popover trigger', ...)` — `expect(screen.getByTestId('endgame-elo-timeline-info')).toBeInTheDocument();`.
    - Test 3: `it('renders the error state', ...)` — `expect(screen.getByText('Failed to load Endgame ELO timeline')).toBeInTheDocument();`.
    - Test 4: `it('contains no Conversion ELO string', ...)` — `expect(screen.queryAllByText(/Conversion ELO/i)).toHaveLength(0);` (inverse of the Phase 87.4 absence assertion). Allow exception for matchMedia/ResizeObserver beforeAll boilerplate text if any.
    - `beforeAll(...)` matchMedia + ResizeObserver setup: copy verbatim from the existing file.

    **B. `frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx`** — UPDATE existing file:
    - Line ~73-74: `vi.mock('@/components/charts/ConversionEloTimelineSection', ...)` → `vi.mock('@/components/charts/EndgameEloTimelineSection', ...)`. Update the mock function export name accordingly.
    - Line 257: `conversion_elo_timeline: ...` → `endgame_elo_timeline: ...` in mock overview data.
    - ADD new test `it('renders the Endgame ELO Timeline directly below EndgameScoreOverTimeChart in the Overall Performance section', ...)`:
      ```typescript
      const { container } = render(<Endgames />, { wrapper: AllProviders });
      // Wait for query to resolve and section to render
      await waitFor(() => {
        expect(screen.queryByTestId('endgame-score-over-time-chart')).toBeInTheDocument();
      });
      const scoreChart = container.querySelector('[data-testid="endgame-score-over-time-chart"]');
      const eloSection = container.querySelector('[data-testid="endgame-elo-timeline-section"]');
      expect(scoreChart).toBeTruthy();
      expect(eloSection).toBeTruthy();
      // ELO section appears AFTER score chart in DOM order
      const relation = scoreChart!.compareDocumentPosition(eloSection!);
      expect(relation & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
      // ELO section is a sibling of (not descendant of) the metrics section
      const metricsSection = container.querySelector('[data-testid="endgame-metrics-section"]');
      if (metricsSection) {
        expect(metricsSection.contains(eloSection)).toBe(false);
      }
      ```
      Use whatever `data-testid` value the page actually carries on the metrics section; if absent, look for the `<h2>Endgame Metrics</h2>` heading and assert via `closest('section')` instead.

    **C. `frontend/src/__tests__/noEndgameSkillString.test.tsx`** — EXTEND existing file:
    - Mock import path + mock data field rename per `<interfaces>` block.
    - ADD a new assertion in the existing test (or a new test):
      ```typescript
      it('renders no "Conversion ELO" string after Phase 87.5', () => {
        const { container } = render(...);
        // Exclude test scaffolding by querying rendered text only
        const rendered = container.textContent ?? '';
        expect(rendered.toLowerCase()).not.toContain('conversion elo');
      });
      ```
      Note: the existing `noEndgameSkillString` test imports `EndgameMetricsSection` + the timeline section. After Phase 87.5 the metrics section retains Conv/Parity/Recov gauges with the word "Conversion" in card labels — clarify that the regression checks the literal phrase "Conversion ELO" (not just "Conversion"), so the per-bucket gauge labels are safe.

    Run `cd frontend && npm test -- --run EndgameEloTimelineSection Endgames.overallPerformance noEndgameSkillString` — all 3 files RED at this checkpoint because Plan 02 source changes (file rename, types rename, page composition) haven't landed yet.
  </behavior>
  <action>
    1. `cd frontend && git mv src/components/charts/__tests__/ConversionEloTimelineSection.test.tsx src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx`. This is the source file rename — preserves history per the Phase 87.4 git-mv convention. The test file `ConversionEloTimelineSection.test.tsx` already exists from Phase 87.4 Plan 02 (commit `c399ca67` family). This task renames it; the source component file is renamed in Task 2 of this same plan.
    2. Edit the moved file per behavior A: rename imports, describe block, buildResponse helper, type aliases, and the 4 assertion targets (heading, popover testid, error copy, "Conversion ELO" absence inverse). Update field name `conversion_elo` → `endgame_elo` in the mock data builder. Keep `beforeAll` boilerplate verbatim.
    3. Edit `frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` per behavior B: update mock + ADD positioning test using `compareDocumentPosition`.
    4. Edit `frontend/src/__tests__/noEndgameSkillString.test.tsx` per behavior C: update mock import + data field, ADD "no Conversion ELO" rendered-text assertion.
    5. Run `cd frontend && npm test -- --run` (or scope to the 3 files). Confirm RED state on the new/updated assertions. Some old assertions may already be GREEN on the renamed file (heading "Conversion ELO Timeline" → "Endgame ELO Timeline" will fail because the component hasn't been renamed yet — that's the intended RED state).
    6. Commit: `test(87.5-02): Wave 0 RED tests for frontend rename + DOM positioning`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run 2>&1 | grep -E "FAIL|ENOENT|TypeError" | head -10</automated>
  </verify>
  <done>
    - `frontend/src/components/charts/__tests__/EndgameEloTimelineSection.test.tsx` exists (renamed from ConversionEloTimelineSection.test.tsx via git mv); contains 4 assertions (heading, popover, error, no-Conversion-ELO).
    - `Endgames.overallPerformance.test.tsx` has updated mock + new positioning test using compareDocumentPosition.
    - `noEndgameSkillString.test.tsx` has updated mock + new "no Conversion ELO" assertion.
    - `npm test -- --run` shows RED state on the new assertions (component rename + types rename + page composition not yet done).
    - Commit `test(87.5-02): Wave 0 RED tests for frontend rename + DOM positioning` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Types rename + component file rename + per-point field rename</name>
  <files>
    frontend/src/types/endgames.ts,
    frontend/src/components/charts/ConversionEloTimelineSection.tsx,
    frontend/src/components/charts/EndgameEloTimelineSection.tsx
  </files>
  <behavior>
    Two surgical edits:

    **A. `frontend/src/types/endgames.ts`** — type renames in lockstep:
    - `ConversionEloTimelinePoint` interface → `EndgameEloTimelinePoint`. Field `conversion_elo: number` → `endgame_elo: number`. Update TSDoc to describe the additive K formula (`endgame_elo = round(actual_elo + K · eg_score_gap)`).
    - `ConversionEloTimelineCombo` interface → `EndgameEloTimelineCombo`.
    - `ConversionEloTimelineResponse` interface → `EndgameEloTimelineResponse`.
    - `EndgameOverviewResponse.conversion_elo_timeline: ConversionEloTimelineResponse` → `endgame_elo_timeline: EndgameEloTimelineResponse`.
    - `EloComboKey` STAYS (Phase 87.4 D-06 — combo_key has no endgame semantic).
    - Remove the Phase 87.4 provenance comments that reference the affine recenter / PIVOT / ALPHA — those are obsolete; replace with a one-line Phase 87.5 D-01 reference.

    **B. `frontend/src/components/charts/ConversionEloTimelineSection.tsx` → `EndgameEloTimelineSection.tsx`** — `git mv` the source file (preserves rename detection in git history at ≥ 80% similarity per the Phase 87.4 Plan 02 Task 4 pattern). Inside the renamed file:
    - Function name: `ConversionEloTimelineSection` → `EndgameEloTimelineSection`. Export updated.
    - Props interface name: `ConversionEloTimelineSectionProps` → `EndgameEloTimelineSectionProps`.
    - Type imports from `../../types/endgames`: switch to the renamed types.
    - Per-point field access: `point.conversion_elo` → `point.endgame_elo`.
    - Chart heading: `"Conversion ELO Timeline"` → `"Endgame ELO Timeline"`.
    - InfoPopover aria-label / testId: `conversion-elo-timeline-info` → `endgame-elo-timeline-info`. Body prose REWRITTEN — the Phase 87.4 prose describes Conv ΔES Score Gap framing and the affine recenter; Phase 87.5 prose uses the additive K formula and the "lifts up / holds back" metaphor per CONTEXT.md §Specific Ideas. Per CLAUDE.md popover copy minimalism: WHAT + sign convention only; no Wilson/sigmoid/affine jargon; no caveats. Roughly: "Your Endgame ELO is your actual rating plus a small offset proportional to your Endgame Score Gap (endgame outcomes minus non-endgame outcomes over the trailing window). Positive Score Gap lifts your rating; negative holds it back."
    - Error copy: `"Failed to load Conversion ELO timeline"` → `"Failed to load Endgame ELO timeline"`.
    - Tooltip text: any "Conversion ELO" references swapped to "Endgame ELO".
    - Dynamic chart key / dataKey: if the chart uses `dataKey="conversion_elo"` for the Recharts Line, switch to `dataKey="endgame_elo"` to match the renamed Pydantic field.
    - Subtitle (if present): any "Conversion ELO" mention swapped.
    - Component-level data-testids: `data-testid="endgame-elo-timeline-section"` (wrapper) STAYS — it already uses the desired naming. Other testids (`-error`, `-empty`, `-chart`, `-volume-bars`, `endgame-elo-legend-{combo_key}`) STAY because they already use `endgame-elo-` prefix per Phase 87.4 Plan 02 02-SUMMARY.md Decision #6.
    - Internal function names: any `conversionElo*`-prefixed locals or callbacks renamed `endgameElo*`.
    - Recharts gradient ids / clip-path ids that include `conversion-elo` → `endgame-elo`.

    After this task, ty/tsc and knip MAY still fail because `Endgames.tsx` and `Home.tsx` and `theme.ts` haven't been updated yet (Task 3). The intermediate state has the new types + new component file but old callers — expected; Task 3 fixes it.
  </behavior>
  <action>
    1. Edit `frontend/src/types/endgames.ts`: rename the 3 interfaces, the field on EndgameEloTimelinePoint, the field on EndgameOverviewResponse. Update TSDocs. Keep `EloComboKey` unchanged.
    2. `cd frontend && git mv src/components/charts/ConversionEloTimelineSection.tsx src/components/charts/EndgameEloTimelineSection.tsx`.
    3. Edit the renamed file per behavior B: function name, props interface, type imports, per-point field access, all rendered string surfaces (heading, popover, error, tooltip), Recharts dataKey, internal function names, gradient/clip-path ids. Rewrite popover prose per CONTEXT.md "lifts up / holds back" metaphor + CLAUDE.md popover copy minimalism.
    4. Run `cd frontend && npx tsc --noEmit` — expect failures in `Endgames.tsx` and consumers because they still import from the old path / read the old field. That's the intended intermediate state.
    5. Commit: `refactor(87.5-02): types rename + component file rename (Conversion ELO → Endgame ELO)`.
  </action>
  <verify>
    <automated>test -f frontend/src/components/charts/EndgameEloTimelineSection.tsx && ! test -f frontend/src/components/charts/ConversionEloTimelineSection.tsx && grep -q "export function EndgameEloTimelineSection" frontend/src/components/charts/EndgameEloTimelineSection.tsx && grep -q "endgame_elo_timeline: EndgameEloTimelineResponse" frontend/src/types/endgames.ts</automated>
  </verify>
  <done>
    - `frontend/src/types/endgames.ts` exports `EndgameEloTimelinePoint` / `EndgameEloTimelineCombo` / `EndgameEloTimelineResponse`; `EndgameOverviewResponse.endgame_elo_timeline` field present; legacy `Conversion*` names absent.
    - `frontend/src/components/charts/EndgameEloTimelineSection.tsx` exists (git-mv from old path); function + props interface renamed; all rendered surfaces use "Endgame ELO"; popover prose rewritten per Phase 87.5 framing; Recharts dataKey uses `endgame_elo`.
    - `frontend/src/components/charts/ConversionEloTimelineSection.tsx` no longer exists.
    - Commit `refactor(87.5-02): types rename + component file rename (Conversion ELO → Endgame ELO)` exists.
    - (Intermediate ty/tsc failures expected in Endgames.tsx / Home.tsx — fixed by Task 3.)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Page composition rewire (chart relocation) + caller updates + section-header rename + FE quality gates</name>
  <files>
    frontend/src/pages/Endgames.tsx,
    frontend/src/pages/Home.tsx,
    frontend/src/lib/theme.ts,
    frontend/src/lib/endgameMetrics.ts
  </files>
  <behavior>
    The Phase 87.5 chart relocation lives entirely in `Endgames.tsx`. Mechanics:

    **A. `frontend/src/pages/Endgames.tsx`** — page composition rewire:
    1. Update the import (line 30 approx): `import { EndgameEloTimelineSection } from '@/components/charts/EndgameEloTimelineSection';`.
    2. Update data access: `overviewData?.endgame_elo_timeline` (replaces `.conversion_elo_timeline`). `eloTimelineData` prop derivation flows through the renamed field.
    3. **DELETE** the `<div data-testid="endgame-elo-timeline-section">` wrapper currently inside the "Endgame Metrics and ELO" block (lines ~493-498):
       ```jsx
       <div className="charcoal-texture rounded-md p-4" data-testid="endgame-elo-timeline-section">
         <ConversionEloTimelineSection ... />
       </div>
       ```
    4. **INSERT** the same wrapper into the "Endgame Overall Performance" block, immediately after the `<EndgameScoreOverTimeChart>` wrapper div (lines ~480-484) and before `<SectionInsightSlot sectionId="overall" ... />`:
       ```jsx
       {eloTimelineData && eloTimelineData.combos.length > 0 && (
         <div className="charcoal-texture rounded-md p-4" data-testid="endgame-elo-timeline-section">
           <EndgameEloTimelineSection
             data={eloTimelineData}
             isLoading={isLoading}
             isError={isError}
           />
         </div>
       )}
       ```
       Match the prop shape the renamed component expects (read the component to confirm; props are inherited from the Phase 87.4 ConversionEloTimelineSection — likely `data: EndgameEloTimelineResponse`, `isLoading: boolean`, `isError: boolean`).
    5. **Rename the section header** (line 490): `"Endgame Metrics and ELO"` → `"Endgame Metrics"`. RESEARCH.md Q3 + CONTEXT.md leave this discretionary; planner decision = fold in. Rationale: after the chart moves out, "and ELO" is inaccurate. The `<SectionInsightSlot sectionId="metrics_elo" ... />` `sectionId` value STAYS unchanged (it maps to the LLM-payload `SectionId` Literal, which is internal naming, not user-facing).
    6. Mobile + desktop parity: the chart relocation happens inside the shared section layout; both responsive paths render the same component. If `Endgames.tsx` has separate mobile/desktop branches (RESEARCH.md doesn't flag any), apply the same change to both per CLAUDE.md "always apply changes to mobile too" rule. Read the file before deciding; if the structure is unified, no extra work.
    7. Filter plumbing: both `EndgameScoreOverTimeChart` and `EndgameEloTimelineSection` already consume the page filters via the same `useQuery` / `data` flow — no new filter wiring needed (SC#2 sub-requirement). Verify by reading the existing prop wiring on `EndgameScoreOverTimeChart` and ensuring the relocated section receives the same `isLoading` / `isError` boolean derivations.

    **B. `frontend/src/pages/Home.tsx`** (lines ~51-54):
    - SEO/feature copy referencing "Conversion ELO" or "Conversion ELO timeline" → "Endgame ELO" / "Endgame ELO timeline". Read the existing copy and adjust. Per Phase 87.4 02-SUMMARY.md Task 5, this was previously "Track your Conversion ELO ..."; revert to / update to "Track your Endgame ELO ..." per Phase 87.5 framing.

    **C. `frontend/src/lib/theme.ts`** (line ~104-106):
    - Comment block "Conversion ELO Timeline chart combo palette" → "Endgame ELO Timeline chart combo palette". Constant names `ELO_COMBO_COLORS` + `ENDGAME_VOLUME_BAR_COLOR` STAY (already generic / already correct).

    **D. `frontend/src/lib/endgameMetrics.ts`** (line ~70):
    - Comment touch-up only: any "Conversion ELO" reference → "Endgame ELO". The Phase 87.4 `SECTION2_DISPLAY_SHIFT` constant + values (Conv -0.055, Parity 0, Recov +0.06) stay untouched per SC#8.

    **Quality gates** (run iteratively):
    - `cd frontend && npx tsc --noEmit` — must be clean.
    - `cd frontend && npm run lint` — must be clean.
    - `cd frontend && npm run knip` — must be clean. The renamed types should have zero dead-export warnings; the renamed component file should be imported from exactly one place (`Endgames.tsx`).
    - `cd frontend && npm test -- --run` — all Vitest pass, including the Task 1 RED tests (now GREEN).
    - Regression grep: `grep -ri "Conversion ELO" frontend/src/` should return ONLY intentional provenance comments in summary documents or test describe blocks. Live rendered strings + type names + identifiers should return zero matches. The Plan 03 Python-side grep regression test handles the full repo-wide audit.
  </behavior>
  <action>
    1. Edit `frontend/src/pages/Endgames.tsx`:
       a. Update import path (line 30) to the new component file.
       b. Update data access from `.conversion_elo_timeline` to `.endgame_elo_timeline`.
       c. DELETE the existing `<div ... data-testid="endgame-elo-timeline-section"><ConversionEloTimelineSection ... /></div>` block from inside the "Endgame Metrics and ELO" section.
       d. INSERT the new wrapper inside the "Endgame Overall Performance" block, immediately after the EndgameScoreOverTimeChart wrapper div and before SectionInsightSlot sectionId="overall".
       e. Rename the h2 heading text "Endgame Metrics and ELO" → "Endgame Metrics".
       f. If `Endgames.tsx` has explicit mobile/desktop branches, apply the same change to both. Read the file structure first.
    2. Edit `frontend/src/pages/Home.tsx`: update the "Conversion ELO" SEO/alt copy to "Endgame ELO" wording (read existing line ~51-54 for exact phrasing).
    3. Edit `frontend/src/lib/theme.ts`: update the comment block at lines ~104-106. No constant renames.
    4. Edit `frontend/src/lib/endgameMetrics.ts`: update the comment at line ~70. Leave `SECTION2_DISPLAY_SHIFT` untouched (SC#8). The display-shift constants in `frontend/src/lib/endgameMetrics.ts` (Conv −0.055, Recov +0.06, Parity 0) are INDEPENDENT of this phase and remain untouched per SC#8 — `git diff frontend/src/lib/endgameMetrics.ts` after this plan should show no functional changes. PLANNER DECISION: this plan FOLDS IN the 'Endgame Metrics and ELO' → 'Endgame Metrics' h2 rename rather than deferring to Phase 89, per Phase 87.5 CONTEXT.md and RESEARCH.md Q3 recommendation.
    5. Run quality gates:
       - `cd frontend && npx tsc --noEmit` — must be clean.
       - `cd frontend && npm run lint` — must be clean.
       - `cd frontend && npm run knip` — must be clean.
       - `cd frontend && npm test -- --run` — all tests pass, including Task 1 Wave 0 RED tests now GREEN.
    6. Regression grep: `grep -rn "Conversion ELO\|conversion_elo\|ConversionElo" frontend/src/ | grep -v "__tests__\|\.test\." | head -20` — should return zero matches in live source files. Test files may retain references in describe blocks if intentional (RESEARCH.md acknowledges this).
    7. Commit: `refactor(87.5-02): relocate Endgame ELO Timeline below Score Gap chart + section rename + caller updates`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint && npm run knip && npm test -- --run 2>&1 | tail -5</automated>
    <human-check>Visit `/endgames` in the dev environment (start with `bin/run_local.sh`). Confirm: (a) "Endgame ELO Timeline" heading appears in the "Endgame Overall Performance" section, directly below the "Endgame Score Gap over Time" chart; (b) the "Endgame Metrics" section (renamed from "Endgame Metrics and ELO") contains only the three Conv/Parity/Recov gauges; (c) the chart updates correctly when filters change (TC, recency, platform); (d) mobile layout at 375px keeps the same vertical order with no horizontal scroll.</human-check>
  </verify>
  <done>
    - `frontend/src/pages/Endgames.tsx`: imports `EndgameEloTimelineSection`, accesses `data.endgame_elo_timeline`, renders the chart in the "Endgame Overall Performance" section directly below `EndgameScoreOverTimeChart`, no longer renders it inside the "Endgame Metrics" section. The "Endgame Metrics" h2 header is renamed.
    - `frontend/src/pages/Home.tsx`, `theme.ts`, `endgameMetrics.ts` comments updated.
    - `cd frontend && npx tsc --noEmit && npm run lint && npm run knip && npm test -- --run` all green.
    - Task 1 Wave 0 RED tests are GREEN: chart heading reads "Endgame ELO Timeline", popover testid `endgame-elo-timeline-info` present, error copy correct, `compareDocumentPosition` assertion passes (ELO section follows score chart in DOM), no "Conversion ELO" string in rendered output.
    - Commit `refactor(87.5-02): relocate Endgame ELO Timeline below Score Gap chart + section rename + caller updates` exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → frontend | TanStack Query consumes `EndgameOverviewResponse` JSON; the renamed field `endgame_elo_timeline` is validated by TypeScript at compile time (not runtime). Backend ships in lockstep (Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.5-FE-01 | Spoofing | Renamed `data-testid` selectors | accept | The wrapper `data-testid="endgame-elo-timeline-section"` retains the same value (already correct naming); no browser-automation scripts break. |
| T-87.5-FE-02 | Tampering | Display-shift constants | mitigate | `SECTION2_DISPLAY_SHIFT` is NOT modified by this plan (SC#8). Verified by `git diff frontend/src/lib/endgameMetrics.ts` showing only comment changes. |
| T-87.5-FE-03 | Information disclosure | Vitest mock data | accept | Mock data carries no real user identifiers; the rename surface is test-only. |
| T-87.5-FE-04 | Denial of service | Chart relocation | accept | Same component, same render budget — just rendered under a different parent. No new Recharts subtrees. |
| T-87.5-FE-SC | Tampering | npm installs | mitigate | No new npm packages introduced. Package legitimacy gate N/A. |
</threat_model>

<verification>
After all 3 tasks land:

1. `cd frontend && npx tsc --noEmit` — clean.
2. `cd frontend && npm run lint` — clean.
3. `cd frontend && npm run knip` — clean (the rename removed the legacy `ConversionElo*` exports; new exports are all consumed).
4. `cd frontend && npm test -- --run` — all tests pass; the 4 new assertions in `EndgameEloTimelineSection.test.tsx` pass; the new positioning assertion in `Endgames.overallPerformance.test.tsx` passes; `noEndgameSkillString.test.tsx` extended assertion passes.
5. Grep regression:
   - `grep -rn "Conversion ELO\|conversion_elo_timeline\|conversion_elo_gap\|ConversionEloTimeline" frontend/src/ | grep -v "__tests__\|\.test\." | head -20` — zero live source matches.
   - Test files may retain references in `describe`/`it` block names for documentation; those are intentional.
6. Visual UAT (covered by Task 3 human-check): `/endgames` page renders the chart in the new location with the new heading.

Plan 03 (LLM/CHANGELOG) runs in parallel with this plan — no file overlap.
</verification>

<success_criteria>
Maps to ROADMAP Phase 87.5 Success Criteria:

- **SC#1 (frontend half)**: Chart heading, popover, tooltip, error copy, alt text, and component identifier all read "Endgame ELO". `noEndgameSkillString.test.tsx` extended with the "no Conversion ELO" rendered-text regression assertion. Plan 03 owns the Python-side grep regression test for the full repo audit.
- **SC#2**: `EndgameEloTimelineSection` renders directly below `EndgameScoreOverTimeChart` in the "Endgame Overall Performance" section. Removed from "Endgame Metrics and ELO" (renamed to "Endgame Metrics"). Both charts share filter plumbing. Mobile + desktop parity. Asserted by `Endgames.overallPerformance.test.tsx::it('renders the Endgame ELO Timeline directly below EndgameScoreOverTimeChart...')`.
- **SC#6 (frontend half)**: `ConversionEloTimelineSection.tsx` → `EndgameEloTimelineSection.tsx` file rename; `ConversionEloTimelinePoint/Combo/Response` → `EndgameEloTimelinePoint/Combo/Response`; field `conversion_elo` → `endgame_elo`. `npm run knip` clean.
- **SC#8 verification**: `git diff frontend/src/lib/endgameMetrics.ts` shows only comment changes; `SECTION2_DISPLAY_SHIFT` values unchanged. `frontend/src/components/charts/EndgameMetricCard.tsx` and `EndgameMetricsSection.tsx` are not touched by this plan.
- **SC#9 (Vitest half)**: `EndgameEloTimelineSection.test.tsx` renders the renamed component; `Endgames.overallPerformance.test.tsx` asserts the DOM position via `compareDocumentPosition`.
</success_criteria>

<output>
After all 3 tasks land, create `.planning/milestones/v1.17-phases/87.5-rebuild-endgame-elo-on-endgame-score-gap/02-SUMMARY.md` using the standard summary template. Include:
- The list of renamed types and the component file rename (git mv preserves history).
- The page composition diff: which JSX block moved, which heading was renamed.
- The popover prose rewrite (Phase 87.5 framing).
- Auto-fixed deviations from the plan (Phase 87.4 Plan 02 had 3 such auto-fixes; expect similar — e.g., `EndgameMetricCard.test.tsx` may not need updates this time because SECTION2_DISPLAY_SHIFT isn't changing).
- Verification commands and Vitest pass counts.
- Note any Plan 03 follow-ups: the Python-side "no Conversion ELO in code" grep regression test, prompt prose rewrite, `_PROMPT_VERSION` v33 bump, CHANGELOG entry.
</output>
