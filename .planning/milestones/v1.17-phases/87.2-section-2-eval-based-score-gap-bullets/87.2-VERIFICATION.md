---
status: human_needed
phase: 87.2-section-2-eval-based-score-gap-bullets
score: 7/7 must-haves verified (automated)
human_verification_pending: true
created: "2026-05-15T19:45:00Z"
---

# Phase 87.2 — Verification Report

**Goal:** Replace the rate-based mirror-bucket peer-diff Gap bullet on the four Section 2 cards (Conversion, Parity, Recovery, Endgame Skill) with an eval-based ΔES Score Gap bullet, reusing Phase 87.1 per-span ΔES infrastructure. Retire mirror-bucket plumbing fully across backend, frontend, schemas, and tests.

## Automated Verification Summary

All 7 SEC2-ΔES requirements covered by automated checks below. Manual UAT items (visual / DPR / LLM narration) are captured in `87.2-HUMAN-UAT.md` and pending human verification.

### SEC2-ΔES-01 — 4 cards render one ΔES Score Gap bullet centred on 0 ✓

**Evidence:**
- `frontend/src/components/charts/EndgameMetricCard.tsx`: peer-bullet row replaced with `ScoreGapRow` named import from `EndgameOverallScoreGapRow.tsx` (key_link verified by plan 03).
- `frontend/src/components/charts/EndgameSkillCard.tsx`: peer-bullet row replaced with Skill `ScoreGapRow`.
- `frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx` (15 tests) and `EndgameSkillCard.test.tsx` (14 tests) both green — assert testids `tile-<bucket>-score-gap-{bullet,value,info}` and sign-convention rendering (positive → right-of-zero with ZONE_SUCCESS tint above neutralMax).

### SEC2-ΔES-02 — Backend per-bucket math + Skill aggregator ✓

**Evidence:**
- `app/schemas/endgames.py`: `ScoreGapMaterialResponse` carries 20 new fields `section2_score_gap_{conv,parity,recov,skill}_{mean,n,p_value,ci_low,ci_high}` (grep count: 20).
- `app/services/endgame_service.py`: `_aggregate_endgame_stats` accumulates `gaps_by_bucket` alongside `gaps_by_class`; `_compute_per_bucket_score_gap` calls `compute_paired_difference_test` once per bucket; `_compute_skill_score_gap` implements equal-weighted Skill aggregator with `n_active < 3` denominator drop per D-01.
- `tests/test_endgame_service.py` (845 LOC test file, 1523 backend tests passing) covers per-bucket partitioning, Skill equal-weighted invariants, cohort-empty fallback, mirror-grep.

### SEC2-ΔES-03 — Zone bands + codegen propagation ✓

**Evidence:**
- `app/services/endgame_zones.py`: `MetricId` Literal + `ZONE_REGISTRY` extended with 4 entries (`section2_score_gap_{conv,parity,recov,skill}`) with `higher_is_better` direction and ±0.05 placeholder bands.
- `frontend/src/generated/endgameZones.ts`: 8 new exports — `SECTION2_SCORE_GAP_{CONV,PARITY,RECOV,SKILL}_NEUTRAL_{MIN,MAX}` (all -0.05 / +0.05).
- `scripts/gen_endgame_zones_ts.py --check`: exits 0 (no drift).
- `tests/services/test_endgame_zones.py::TestRegistrySanity::test_all_scalar_metrics_have_entries`: updated post-merge to include the 4 new metric IDs (commit `6dfcdddb`).
- `.claude/skills/benchmarks/SKILL.md` §3.4.4: per-bucket ΔES Score Gap calibration method documented (sample floor, Cohen's-d collapse verdict scaffolding, sigmoid-bias expectation).

### SEC2-ΔES-04 — Mirror-bucket plumbing fully retired ✓

**Evidence:**

Backend deletions (verified by grep):
- `_MIRROR_BUCKET` and `_compute_skill_wire` deleted from `app/services/endgame_service.py` (remaining references are deletion-marker comments).
- `compute_skill_diff_test`, `compute_per_bucket_diff_test`, and 6 shared internals deleted from `app/services/score_confidence.py`.
- 5 fields deleted from `MaterialRow` (`opponent_score`, `opponent_games`, `diff_p_value`, `diff_ci_low`, `diff_ci_high`).
- 5 fields deleted from `ScoreGapMaterialResponse` (`skill`, `opp_skill`, `skill_diff_p_value`, `skill_diff_ci_low`, `skill_diff_ci_high`).

Frontend deletions (verified by grep):
- `MIRROR_BUCKET`, `userRate`, `opponentRate`, `formatDiffPct`, `NEUTRAL_ZONE_MIN`, `NEUTRAL_ZONE_MAX`, `BULLET_DOMAIN`, `MIN_OPPONENT_BASELINE_GAMES` — zero hits in `frontend/src/` (matches are unrelated constants: `EVAL_BULLET_DOMAIN_PAWNS` for openings, `SCORE_BULLET_DOMAIN` for opening cards).
- TypeScript types mirror backend deletions.
- `npm run knip` clean; `npx tsc --noEmit` clean; `npm run build` succeeds.

### SEC2-ΔES-05 — Bucket-specific popover copy with sigmoid caveat ✓

**Evidence:**
- `EndgameMetricCard.tsx` adds bucket-specific `POPOVER_COPY` map (Conversion / Parity / Recovery) with sigmoid-bias caveat one-liner per D-08.
- `EndgameSkillCard.tsx` adds `SKILL_POPOVER_COPY` with equal-weighted description + identical sigmoid caveat.
- No "vs opponents" / "compared to opponents" / "Opp Skill" / "mirror bucket" strings remain (visual / copy quality pending human UAT item 3 in `87.2-HUMAN-UAT.md`).

### SEC2-ΔES-06 — LLM payload + prompt v30→v31 ✓

**Evidence:**
- `app/services/insights_llm.py`: `_PROMPT_VERSION = "endgame_v31"` (verified) with full changelog comment explaining the 4 new findings, sign convention, sigmoid caveat reference, and dual-label terminology rule.
- `app/services/insights_service.py::_findings_endgame_metrics`: emits 4 new `SubsectionFinding(metric="section2_score_gap_{conv,parity,recov,skill}", value=mean_or_nan, zone=assign_zone(...), sample_size=n)` alongside existing rate findings (verified ADDITIVE, not swap, per RESEARCH §Critical Drift). No `p_value` / `verdict` sub-field — band IS the significance signal per memory `feedback_llm_significance_signal.md`.
- `app/prompts/endgame_insights.md`: Section 2 Score Gap family glossary block added — umbrella term + 4 bucket entries + sign convention + sigmoid caveat verbatim from v29.
- Existing 4 rate findings (`endgame_skill`, `conversion_win_pct`, `parity_score_pct`, `recovery_save_pct`) preserved per D-09.
- `tests/services/test_insights_llm.py`: 86 tests green (5 new + version-bump updates).

### SEC2-ΔES-07 — Test coverage + lint gates ✓

**Evidence:**
- Backend: `uv run pytest -q` → **1523 passed, 6 skipped** (covers per-bucket math, Skill equal-weighted invariants, cohort-empty fallback, deletion-grep tests for retired functions/fields, registry sanity).
- Backend: `uv run ty check app/ tests/` → **All checks passed!**
- Frontend: `npm test` → **37 test files, 430 tests, all passing**.
- Frontend: `npx tsc --noEmit` → clean; `npm run knip` → clean; `npm run build` → succeeds.
- Codegen drift: `python scripts/gen_endgame_zones_ts.py --check` → clean.

**Post-merge integration fixes applied:**
- `6dfcdddb` — `tests/services/test_endgame_zones.py` updated to include 4 new metric IDs in registry sanity set.
- `e779ff9a` — `tests/services/test_insights_service.py` updated for 4 new section2_score_gap findings (assert count 4 → 8, filter rate-bucket assertions by metric not dimension).

## Requirement Traceability

| ID | Plans | Status |
|---|---|---|
| SEC2-ΔES-01 | 87.2-03 | ✓ Verified |
| SEC2-ΔES-02 | 87.2-02 | ✓ Verified |
| SEC2-ΔES-03 | 87.2-01 | ✓ Verified |
| SEC2-ΔES-04 | 87.2-02 + 87.2-03 | ✓ Verified |
| SEC2-ΔES-05 | 87.2-03 | ✓ Verified (visual copy review pending UAT) |
| SEC2-ΔES-06 | 87.2-04 | ✓ Verified |
| SEC2-ΔES-07 | 87.2-02 + 87.2-03 + 87.2-04 | ✓ Verified |

## Human Verification Required

`87.2-HUMAN-UAT.md` lists 6 manual items that cannot be automated:
1. 375px parity check (no horizontal scroll, whisker visibility, tap-target sizing).
2. Sign-convention visual check (zone tint matches value vs band on real data).
3. "No vs opponents" framing copy review across all 4 popovers.
4. Section 2 cards render after import on an account with real data.
5. LLM narration dual-label rule + Section 2 Score Gap umbrella usage.
6. Prompt-version cache invalidation after deploy (v30 → v31).

Phase remains in `human_needed` status until UAT items are checked. Verification will re-run as `passed` once UAT closes.

## Cross-Phase Regression Check

Pre-phase: 1516 tests passing on Phase 87.1.
Post-phase: 1523 tests passing (+7 net: 7 new test additions from plans 02 / 04, no removals beyond intentional deletions).
Frontend: 430 tests passing (was 430 pre-phase). EndgameMetricsSection / EndgameMetricCard / EndgameSkillCard test files were rewritten in-place; net count unchanged because removed peer-bullet tests were replaced 1:1 with ScoreGapRow tests.

No regressions detected.
