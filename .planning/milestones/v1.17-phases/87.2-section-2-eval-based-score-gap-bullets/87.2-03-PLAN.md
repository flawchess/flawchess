---
phase: 87.2
plan: 03
type: execute
wave: 2
depends_on:
  - 87.2-01   # consumes 8 new SECTION2_SCORE_GAP_*_NEUTRAL_MIN/MAX exports from generated/endgameZones.ts
  - 87.2-02   # consumes 20 new section2_score_gap_* fields on ScoreGapMaterialResponse + 5+5 deletions
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/lib/endgameMetrics.ts
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/components/charts/EndgameSkillCard.tsx
  - frontend/src/components/charts/EndgameMetricsSection.tsx
  - frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx
  - frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx
autonomous: true
requirements:
  - SEC2-ΔES-01   # 4 cards render one ΔES Score Gap bullet centred on 0 with CI whiskers
  - SEC2-ΔES-04   # Mirror-bucket plumbing retirement (frontend half) — knip clean
  - SEC2-ΔES-05   # MetricStatPopover carries bucket-specific copy per D-08 with sigmoid caveat
  - SEC2-ΔES-07   # Vitest snapshots + render tests + knip + ty (frontend half)
tags:
  - frontend
  - card
  - popover
  - endgame
  - deletion-sweep
must_haves:
  truths:
    - "EndgameMetricCard renders a per-bucket Score Gap bullet (ScoreGapRow) replacing the prior peer-bullet row"
    - "EndgameSkillCard renders a Skill Score Gap bullet (ScoreGapRow) replacing its peer-bullet row"
    - "Each ScoreGapRow consumes SECTION2_SCORE_GAP_<BUCKET>_NEUTRAL_MIN/MAX from generated/endgameZones.ts (per-bucket band)"
    - "MetricStatPopover on each of 4 cards carries bucket-specific D-08 copy + identical sigmoid-bias caveat one-liner"
    - "Testids tile-<bucket>-score-gap-{bullet,value,info} present on all 4 cards"
    - "Sign convention: positive section2_score_gap_*_mean renders right of zero with ZONE_SUCCESS tint when above neutralMax"
    - "Zero references remain to MIRROR_BUCKET, userRate, opponentRate, formatDiffPct, NEUTRAL_ZONE_MIN, NEUTRAL_ZONE_MAX, BULLET_DOMAIN, MIN_OPPONENT_BASELINE_GAMES in frontend/src/ (excluding deletion-grep test)"
    - "TS types mirror backend: 5 deletions on MaterialRow + 5 on ScoreGapMaterialResponse + 20 new section2_score_gap_* fields"
    - "knip and tsc both clean after the deletion sweep"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "TS mirror of 20 new schema fields + 5+5 deletions on MaterialRow / ScoreGapMaterialResponse"
      contains: "section2_score_gap_conv_mean"
    - path: "frontend/src/lib/endgameMetrics.ts"
      provides: "8 deleted symbols per D-03; surviving exports unchanged"
      contains: "BUCKET_DISPLAY_LABELS"
    - path: "frontend/src/components/charts/EndgameMetricCard.tsx"
      provides: "Peer-bullet row replaced with per-bucket ScoreGapRow; mirror prop deleted; bucket-specific MetricStatPopover copy"
      contains: "ScoreGapRow"
    - path: "frontend/src/components/charts/EndgameSkillCard.tsx"
      provides: "Peer-bullet row replaced with Skill ΔES ScoreGapRow; oppSkill/pValue/ciLow/ciHigh props replaced with scoreGap* props"
      contains: "ScoreGapRow"
    - path: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      provides: "MIRROR_BUCKET wiring removed; buildZeroRow updated; new scoreGap* props threaded from response to each card"
      contains: "section2_score_gap_conv_mean"
    - path: "frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx"
      provides: "ScoreGapRow render + testid + popover + sign-convention assertions (replaces peer-bullet assertions)"
      contains: "score-gap-bullet"
    - path: "frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx"
      provides: "Skill ΔES ScoreGapRow render + testid + popover assertions"
      contains: "score-gap-bullet"
  key_links:
    - from: "frontend/src/components/charts/EndgameMetricCard.tsx"
      to: "frontend/src/components/charts/EndgameOverallScoreGapRow.tsx"
      via: "Named import `ScoreGapRow`"
      pattern: "import \\{ ScoreGapRow \\} from"
    - from: "frontend/src/components/charts/EndgameMetricCard.tsx"
      to: "frontend/src/generated/endgameZones.ts"
      via: "Named imports SECTION2_SCORE_GAP_<BUCKET>_NEUTRAL_MIN/MAX"
      pattern: "SECTION2_SCORE_GAP_(CONV|PARITY|RECOV)_NEUTRAL_(MIN|MAX)"
    - from: "frontend/src/components/charts/EndgameSkillCard.tsx"
      to: "frontend/src/generated/endgameZones.ts"
      via: "Named imports SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN/MAX"
      pattern: "SECTION2_SCORE_GAP_SKILL_NEUTRAL_(MIN|MAX)"
    - from: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      to: "frontend/src/types/endgames.ts"
      via: "Reads response.score_gap_material.section2_score_gap_*_* and threads to per-card props"
      pattern: "section2_score_gap_(conv|parity|recov|skill)_"
---

<!--
SEC2-ΔES-01, SEC2-ΔES-04 (frontend half), SEC2-ΔES-05, SEC2-ΔES-07 (frontend half)
drafted from .planning/milestones/v1.17-ROADMAP.md §Phase 87.2 success criteria items 1, 4, 5, 7.
This plan is the frontend half of the deletion sweep: TS types + lib symbols + 2 card refactors + orchestrator wiring + Vitest.
-->

<objective>
Replace the rate-based peer-bullet row in `EndgameMetricCard.tsx` (Conv / Parity / Recov cards) and `EndgameSkillCard.tsx` (Skill card) with a per-bucket eval-based ΔES Score Gap bullet using the Phase 87.1 `ScoreGapRow` primitive verbatim. Delete the 8 mirror-bucket symbols from `endgameMetrics.ts` (D-03), the 5 + 5 schema mirror fields from `endgames.ts` types, the `mirror` prop on `EndgameMetricCardProps`, and the 4 `oppSkill`/`pValue`/`ciLow`/`ciHigh` props on `EndgameSkillCardProps` (D-04). Add bucket-specific `MetricStatPopover` copy per D-08 with the identical sigmoid-bias caveat one-liner across all 4 cards. Thread new ΔES props through `EndgameMetricsSection.tsx`. Update Vitest coverage to reflect the new row, new testids, new popover content, and zone-only font color (Phase 85.1 D-04 inherited).

Purpose: surfaces the new per-bucket ΔES Score Gap on every Section 2 card with the Stockfish-baseline anchor (no "vs opponents" framing per D-08). Eliminates the mathematically-degenerate rate-based peer-diff bullet (Conv-Gap ≡ Recov-Gap, Parity-Gap as gauge affine) per the phase rationale. Knip-clean after the deletion sweep.

Output: 2 modified card components, 1 modified orchestrator, 2 updated Vitest files, deletion sweep across 2 lib + 1 types file, 8 new TS constant imports from `endgameZones.ts`, no knip / tsc / lint / build errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-VALIDATION.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-01-SUMMARY.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-02-SUMMARY.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-03-SUMMARY.md
@frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
@frontend/src/components/charts/EndgameTypeCard.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/EndgameSkillCard.tsx
@frontend/src/components/charts/EndgameMetricsSection.tsx
@frontend/src/lib/endgameMetrics.ts
@frontend/src/types/endgames.ts

<interfaces>
<!-- Existing contracts consumed -->

From frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (UNCHANGED — Phase 85 primitive):
```
interface ScoreGapRowProps {
  label: string | ReactNode;
  value: number;
  formatted: string;
  resultColor: string | undefined;
  valueTestId: string;
  ariaLabel: string;
  neutralMin: number;
  neutralMax: number;
  tooltip?: ReactNode;
  valueClassName?: string;
  ciLow?: number;
  ciHigh?: number;
  domain?: number;
}
export function ScoreGapRow(props: ScoreGapRowProps): JSX.Element;
```

From frontend/src/generated/endgameZones.ts (produced by Plan 01):
```
export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN   = -0.05;
export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MAX   =  0.05;
export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MIN = -0.05;
export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MAX =  0.05;
export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MIN  = -0.05;
export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MAX  =  0.05;
export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN  = -0.05;
export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX  =  0.05;
```

From frontend/src/types/endgames.ts post-edit (mirrors Plan 02 backend schema):
```
interface MaterialRow {
  // DELETED: opponent_score, opponent_games, diff_p_value, diff_ci_low, diff_ci_high
  // KEEP: bucket, value (gauge), score, win_pct, draw_pct, loss_pct, games, etc.
}

interface ScoreGapMaterialResponse {
  // DELETED: skill, opp_skill, skill_diff_p_value, skill_diff_ci_low, skill_diff_ci_high
  // ADDED 20 (4 buckets × 5):
  section2_score_gap_conv_mean:   number | null;
  section2_score_gap_conv_n:      number | null;
  section2_score_gap_conv_p_value: number | null;
  section2_score_gap_conv_ci_low:  number | null;
  section2_score_gap_conv_ci_high: number | null;
  // ... same 5-field cluster for parity, recov, skill (20 fields total)
}
```

From frontend/src/components/popovers/MetricStatPopover.tsx (UNCHANGED, consolidated 260514-i3l):
```
props: { name, explanation, value, baseline, unit, gameCount, level, pValue,
         vocabulary, neutralLower, neutralUpper, baselineLabel, methodology, testId, ariaLabel }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror backend schema changes into frontend/src/types/endgames.ts; delete D-03 symbols from endgameMetrics.ts</name>
  <files>frontend/src/types/endgames.ts, frontend/src/lib/endgameMetrics.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts lines 130-189 (current `MaterialRow` + `ScoreGapMaterialResponse` interfaces — exact fields to delete + add)
    - frontend/src/lib/endgameMetrics.ts (full file, ~120 lines — 8 deletion targets per D-03 and 9 surviving exports per RESEARCH §Deletion Inventory)
    - app/schemas/endgames.py post-Plan-02 (the canonical 20 new fields + 10 deletions; TS mirrors this exactly with `number | null` for floats/ints)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"frontend/src/types/endgames.ts" lines 426-444 + §"frontend/src/lib/endgameMetrics.ts" lines 446-468
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-03, §D-05, §D-06
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Deletion Inventory (verified line refs + consumer table)
  </read_first>
  <action>
    Per D-03 + D-05 + D-06 + PATTERNS deltas:

    PART A — `frontend/src/types/endgames.ts`:
    (1) DELETE on `MaterialRow` (around lines 130-145): `opponent_score`, `opponent_games`, `diff_p_value`, `diff_ci_low`, `diff_ci_high`.
    (2) DELETE on `ScoreGapMaterialResponse` (around lines 175-189): `skill`, `opp_skill`, `skill_diff_p_value`, `skill_diff_ci_low`, `skill_diff_ci_high`.
    (3) ADD on `ScoreGapMaterialResponse`: 20 new fields per Plan 02's backend shape — 4 clusters of 5 fields each (`section2_score_gap_{conv,parity,recov,skill}_{mean,n,p_value,ci_low,ci_high}`). All `number | null` (TS `null`, not `undefined`) to mirror Pydantic `float | None` / `int | None` over JSON. Precede the cluster with a comment block citing Phase 87.2 + D-06 + dual-label rationale (mirrors the comment block in `app/schemas/endgames.py`).
    (4) Field order: keep alphabetical or grouped by bucket — match the Pydantic source-order in `endgames.py` for consistency. Verify by reading the post-Plan-02 schema file.

    PART B — `frontend/src/lib/endgameMetrics.ts`:
    (5) DELETE 8 symbols per D-03:
        - `MIRROR_BUCKET` (Record<MaterialBucket, MaterialBucket>)
        - `userRate(row)` helper
        - `opponentRate(row, mirror)` helper
        - `formatDiffPct(userR, oppR)`
        - `NEUTRAL_ZONE_MIN`
        - `NEUTRAL_ZONE_MAX`
        - `BULLET_DOMAIN`
        - `MIN_OPPONENT_BASELINE_GAMES`
    (6) KEEP: `BUCKET_DISPLAY_LABELS`, `BUCKET_DISPLAY_LABELS_WITH_METRIC`, `FIXED_GAUGE_ZONES`, `ENDGAME_SKILL_ZONES`, `formatScorePct`, `ENDGAME_TYPE_DESCRIPTIONS`, `SHOW_WDL_BAR_IN_TYPE_CARDS`, `ENDGAME_CLASS_TO_SLUG`, `HIDDEN_ENDGAME_CLASSES` — these are still consumed by the gauge / score rows / type cards.
    (7) Remove any now-orphan imports at the top of the file (e.g. `MaterialBucket` may still be needed if `BUCKET_DISPLAY_LABELS` references it — check before removing).

    (8) Run `cd frontend && npx tsc --noEmit` — must exit 0. The TS deletions surface every consumer of the deleted symbols in the card components; those errors will be fixed in Tasks 2 + 3 + 4. To unblock this task's verify gate, ensure tsc is clean against the lib + types files in isolation. If running tsc on the full project surfaces card-file errors, that is expected — Task 2 / 3 / 4 fix them in the same wave. Plan accordingly: run this task's tsc check just on the 2 edited files, or accept the expected breakage and proceed.

    (9) ty / Pydantic boundary: not applicable (frontend-only). Mirror via the canonical backend file (`app/schemas/endgames.py`) — if the backend used `model_config = ConfigDict(extra='forbid')`, the wire payload is strict; otherwise, deleted fields are silently dropped server-side. Frontend types are advisory at runtime — TS strictness catches consumers.
  </action>
  <verify>
    <automated>grep -c 'section2_score_gap_conv_mean' frontend/src/types/endgames.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'section2_score_gap_conv_mean' frontend/src/types/endgames.ts` returns 1
    - `grep -c 'section2_score_gap_' frontend/src/types/endgames.ts` returns >= 20 (4 buckets × 5 fields)
    - `grep -c 'opponent_score' frontend/src/types/endgames.ts` returns 0
    - `grep -c 'opponent_games' frontend/src/types/endgames.ts` returns 0
    - `grep -c 'skill_diff_p_value' frontend/src/types/endgames.ts` returns 0
    - `grep -c 'opp_skill' frontend/src/types/endgames.ts` returns 0
    - `grep -c 'MIRROR_BUCKET' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'userRate' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'opponentRate' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'formatDiffPct' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'NEUTRAL_ZONE_MIN' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'NEUTRAL_ZONE_MAX' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'BULLET_DOMAIN' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'MIN_OPPONENT_BASELINE_GAMES' frontend/src/lib/endgameMetrics.ts` returns 0
    - `grep -c 'BUCKET_DISPLAY_LABELS' frontend/src/lib/endgameMetrics.ts` returns >= 1 (preserved)
    - `grep -c 'FIXED_GAUGE_ZONES' frontend/src/lib/endgameMetrics.ts` returns >= 1 (preserved)
    - `grep -c 'formatScorePct' frontend/src/lib/endgameMetrics.ts` returns >= 1 (preserved)
  </acceptance_criteria>
  <done>
    TS types mirror backend schema (20 new + 10 deletions); 8 deletions complete in `endgameMetrics.ts`; surviving exports preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor EndgameMetricCard.tsx — replace peer-bullet row with per-bucket ScoreGapRow + MetricStatPopover</name>
  <files>frontend/src/components/charts/EndgameMetricCard.tsx, frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricCard.tsx (full file — current peer-bullet row at lines 143-220, `mirror` prop at line 47, import list lines 1-35)
    - frontend/src/components/charts/EndgameTypeCard.tsx lines 387-437 (Phase 87.1 ScoreGapRow JSX precedent — direct template per RESEARCH "Pattern Templates")
    - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (the `ScoreGapRow` primitive — named export, full 92-line file VERIFIED in RESEARCH)
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (gapZoneColor helper at lines 52-56 — zone-only tint convention per Phase 85.1 D-04)
    - frontend/src/components/popovers/MetricStatPopover.tsx (consolidated prop shape)
    - frontend/src/generated/endgameZones.ts post-Plan-01 (SECTION2_SCORE_GAP_<BUCKET>_NEUTRAL_MIN/MAX exports)
    - frontend/src/lib/theme.ts (ZONE_DANGER, ZONE_SUCCESS constants)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"frontend/src/components/charts/EndgameMetricCard.tsx" lines 472-655 (full JSX delta + import block + gap derivation block) + §Shared Patterns lines 750-869 (POPOVER_COPY lookup, sigmoid caveat verbatim, testid scheme, zone-only color)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-04, §D-07, §D-08
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-03-PLAN.md Task 2 (Phase 87.1 frontend analog — same JSX shape)
    - frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx (Phase 87.1 Vitest precedent — assertions for `-asg-bullet/-value/-info` testids; replicate pattern for `-score-gap-*` testids)
  </read_first>
  <action>
    Per PATTERNS deltas + D-04 + D-07 + D-08 + the Phase 87.1 Plan 03 Task 2 precedent:

    PART A — `EndgameMetricCard.tsx` refactor:
    (1) Imports — add at top of file:
        - `import { ScoreGapRow } from './EndgameOverallScoreGapRow';` (named export)
        - `import { SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN, SECTION2_SCORE_GAP_CONV_NEUTRAL_MAX, SECTION2_SCORE_GAP_PARITY_NEUTRAL_MIN, SECTION2_SCORE_GAP_PARITY_NEUTRAL_MAX, SECTION2_SCORE_GAP_RECOV_NEUTRAL_MIN, SECTION2_SCORE_GAP_RECOV_NEUTRAL_MAX } from '@/generated/endgameZones';`
        - `import { ZONE_DANGER, ZONE_SUCCESS } from '@/lib/theme';`
        - `import { deriveLevel } from './EndgameOverallShared';` (the existing `deriveLevel(p_value, n)` helper — verify the import path against current usage in `EndgameTypeCard.tsx`; if it lives elsewhere, follow that path)
        - `import { BUCKET_DISPLAY_LABELS } from '@/lib/endgameMetrics';` (already imported per current file; verify)
        - `import { MetricStatPopover } from '../popovers/MetricStatPopover';` (already imported; verify)
        - Add a one-line code comment on the SECTION2 constants import: `// Per-bucket neutral bands for the Section 2 ΔES Score Gap bullet (D-02 / Plan 01).`
    (2) Imports — REMOVE from current file:
        - `MiniBulletChart` (visual now ScoreGapRow-internal)
        - `formatDiffPct`, `opponentRate`, `userRate` from `@/lib/endgameMetrics` (deleted in Task 1)
        - `MIN_OPPONENT_BASELINE_GAMES`, `NEUTRAL_ZONE_MAX`, `NEUTRAL_ZONE_MIN`, `BULLET_DOMAIN` from `@/lib/endgameMetrics` (deleted in Task 1)
        - `isConfident` if imported (zone-only color per Phase 85.1 D-04 — no sig-gating on the row font color)
    (3) Props — on `EndgameMetricCardProps`:
        - DELETE: `mirror: MaterialRow | undefined` (line 47).
        - The card consumes the new ΔES fields via the existing `row: MaterialRow` prop after Plan 02 grew `ScoreGapMaterialResponse` (which contains the per-bucket cluster); BUT note: per RESEARCH §Schema Shape, the 20 new fields land on `ScoreGapMaterialResponse`, NOT on `MaterialRow`. So `EndgameMetricCard` needs the per-bucket ΔES quartet (`scoreGapMean`, `scoreGapN`, `scoreGapPValue`, `scoreGapCiLow`, `scoreGapCiHigh`) threaded as 5 new props (or one nested object) from the orchestrator (Plan 03 Task 4). Choose the per-prop shape (5 new props each `number | null`) to match the existing `EndgameSkillCardProps` per-field-prop style and ease test-fixture construction. Add these 5 props to `EndgameMetricCardProps`.
    (4) Gap derivation block — insert into the component body after the existing `total`/`level` derivations, mirroring the Phase 87.1 `EndgameTypeCard.tsx` block:
        ```
        const gapMean = scoreGapMean;
        const gapN = scoreGapN ?? 0;
        const showGapRow = gapN > 0;
        const gapFormatted = gapMean != null
          ? (gapMean >= 0 ? '+' : '') + `${Math.round(gapMean * 100)}%`
          : '—';
        const { section2NeutralMin, section2NeutralMax } = useMemo(
          () => ({
            section2NeutralMin: bucket === 'conversion'
              ? SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN
              : bucket === 'parity'
                ? SECTION2_SCORE_GAP_PARITY_NEUTRAL_MIN
                : SECTION2_SCORE_GAP_RECOV_NEUTRAL_MIN,
            section2NeutralMax: bucket === 'conversion'
              ? SECTION2_SCORE_GAP_CONV_NEUTRAL_MAX
              : bucket === 'parity'
                ? SECTION2_SCORE_GAP_PARITY_NEUTRAL_MAX
                : SECTION2_SCORE_GAP_RECOV_NEUTRAL_MAX,
          }),
          [bucket],
        );
        const gapColor = gapMean != null
          ? gapMean < section2NeutralMin
            ? ZONE_DANGER
            : gapMean >= section2NeutralMax
              ? ZONE_SUCCESS
              : undefined
          : undefined;
        const gapLevel = deriveLevel(scoreGapPValue ?? null, gapN);
        ```
        Per Phase 85.1 D-04: zone-only tint; the popover surfaces the significance signal informationally via `gapLevel`.
    (5) Define `POPOVER_COPY` lookup (per PATTERNS §Shared Patterns "Bucket-specific popover copy" — verbatim from D-08):
        ```
        const POPOVER_COPY: Record<MaterialBucket, string> = {
          conversion: "Average per-span Score Gap on endgame spans you entered ahead by ≥1 pawn. Positive = you converted advantages above the Stockfish baseline; negative = you bled away expected score on winning entries. Positive = above the Stockfish baseline; negative = below. Note: the Lichess expected-score sigmoid under-weights endgame eval; rely on the zone bands, not the raw magnitude.",
          parity: "Average per-span Score Gap on endgame spans you entered roughly balanced (eval within ±1 pawn). Positive = you outperformed the baseline from balanced; negative = you underperformed. Positive = above the Stockfish baseline; negative = below. Note: the Lichess expected-score sigmoid under-weights endgame eval; rely on the zone bands, not the raw magnitude.",
          recovery: "Average per-span Score Gap on endgame spans you entered behind by ≥1 pawn. Positive = you salvaged disadvantages above the Stockfish baseline; negative = the position deteriorated further than expected. Positive = above the Stockfish baseline; negative = below. Note: the Lichess expected-score sigmoid under-weights endgame eval; rely on the zone bands, not the raw magnitude.",
        };
        ```
        Place at module scope (above the component). Use the EXACT text from D-08 (no em-dashes, no rewording). The sigmoid-bias caveat suffix is identical across all 3 entries.
    (6) JSX — DELETE the peer-bullet row at lines 143-220 wholesale (the `hasOpponent ? (...) : (...)` block with the `You / Opp / Gap` text + signed `MiniBulletChart` + sparse-fallback span).
    (7) JSX — INSERT the new ΔES row at the same position (between gauge row + WDL bar block):
        ```
        {showGapRow && (
          <div data-testid={`${tileTestId}-score-gap-bullet`}>
            <ScoreGapRow
              label={`${BUCKET_DISPLAY_LABELS[bucket]} Score Gap:`}
              value={gapMean ?? 0}
              formatted={gapFormatted}
              resultColor={gapColor}
              valueTestId={`${tileTestId}-score-gap-value`}
              ariaLabel={`${BUCKET_DISPLAY_LABELS[bucket]} Score Gap: ${gapFormatted}`}
              neutralMin={section2NeutralMin}
              neutralMax={section2NeutralMax}
              ciLow={scoreGapCiLow ?? undefined}
              ciHigh={scoreGapCiHigh ?? undefined}
              tooltip={
                <MetricStatPopover
                  name={`${BUCKET_DISPLAY_LABELS[bucket]} Score Gap`}
                  explanation={POPOVER_COPY[bucket]}
                  value={gapMean ?? 0}
                  baseline={0}
                  unit="percent"
                  gameCount={gapN}
                  level={gapLevel}
                  pValue={scoreGapPValue}
                  vocabulary="score"
                  neutralLower={section2NeutralMin}
                  neutralUpper={section2NeutralMax}
                  baselineLabel="0%"
                  methodology={
                    <>
                      Score: per-span exit score − Stockfish expected score from span entry eval.<br />
                      Test: paired one-sample z-test on per-span ΔES values vs 0.<br />
                      Confidence interval: 95% normal-approx on the paired diffs.
                    </>
                  }
                  testId={`${tileTestId}-score-gap-info`}
                  ariaLabel={`What is ${BUCKET_DISPLAY_LABELS[bucket]} Score Gap?`}
                />
              }
            />
          </div>
        )}
        ```
        Per D-08: the popover `name` prop and the row label use the bucket-specific full form ("Conversion Score Gap" / "Parity Score Gap" / "Recovery Score Gap") via `BUCKET_DISPLAY_LABELS[bucket]`. NOT the Phase 87.1 short form "Score Gap" — Section 2 cards already carry a metric in the gauge, so the explicit bucket framing disambiguates.
    (8) Title `InfoPopover` body (if the title carries one): drop the "compared to opponents" framing if present; replace with Stockfish-baseline framing per D-08 "no vs opponents framing" rule. Reuse the row's popover copy or shorten as appropriate.
    (9) CLAUDE.md compliance:
        - No em-dashes in user-facing strings (the POPOVER_COPY lookup above has none — verify when copying).
        - Minimum font size `text-sm` — `ScoreGapRow` already complies. `MetricStatPopover` body is the documented `text-xs` exception.
        - `data-testid` on every interactive element per CLAUDE.md browser-automation rules: `${tileTestId}-score-gap-bullet` wrapper + `-score-gap-value` (passed through `valueTestId`) + `-score-gap-info` (passed through `testId` to MetricStatPopover) = 3 testids per card.

    PART B — Vitest in `__tests__/EndgameMetricCard.test.tsx`:
    (10) REMOVE existing peer-bullet assertions (those that targeted `${tileTestId}-you`, `-opp`, `-diff`, `-muted` testids).
    (11) ADD new assertions, mirroring Phase 87.1's `EndgameTypeCard.test.tsx` pattern (per RESEARCH "Vitest files — required assertions per D-11"):
        - ScoreGapRow renders when `scoreGapN > 0` and the wrapper testid `${tileTestId}-score-gap-bullet` is in the DOM.
        - ScoreGapRow is absent (no wrapper) when `scoreGapN === 0`.
        - Positive `scoreGapMean` outside neutral band renders with `ZONE_SUCCESS` color; negative outside band → `ZONE_DANGER`; inside band → undefined (neutral). Use `getComputedStyle` or assert on the `style.color` attribute per Vitest+jsdom convention.
        - DOM order: bullet wrapper sits between the gauge row container and the WDL bar block (the same insertion point as the deleted peer-bullet row).
        - `${tileTestId}-score-gap-value` present and contains the formatted value text (`+5%` / `-3%` / `0%`).
        - MetricStatPopover trigger `${tileTestId}-score-gap-info` present.
        - Popover explanation text contains the bucket-specific copy AND the sigmoid-bias caveat string "Note: the Lichess expected-score sigmoid under-weights endgame eval".
        - Popover `name` prop is the bucket-specific full form ("Conversion Score Gap" / "Parity Score Gap" / "Recovery Score Gap"), NOT "Score Gap" short form.
        - At `n >= CONFIDENCE_MIN_N` (10), `ciLow` and `ciHigh` are passed to ScoreGapRow as non-undefined.
        - At `n < 2`, CI props fall back to `undefined`.
        - "No vs opponents framing" — assert the popover explanation does NOT contain "vs opponents" or "opponent" (per D-08).
    (12) Update fixtures: the test file's `MaterialRow` fixture factory must drop the 5 deleted fields (`opponent_score`, `opponent_games`, `diff_*`). Add 5 new ΔES props on the component-render fixture (`scoreGapMean`, `scoreGapN`, `scoreGapPValue`, `scoreGapCiLow`, `scoreGapCiHigh`).
    (13) Run `cd frontend && npm test -- --run EndgameMetricCard` — all new assertions green.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run EndgameMetricCard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ScoreGapRow' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 2 (import + JSX)
    - `grep -c 'SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 1
    - `grep -c '\\-score\\-gap\\-bullet' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 1
    - `grep -c '\\-score\\-gap\\-value' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 1
    - `grep -c '\\-score\\-gap\\-info' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 1
    - `grep -c 'POPOVER_COPY' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 2 (declaration + usage)
    - `grep -c 'Lichess expected-score sigmoid under-weights' frontend/src/components/charts/EndgameMetricCard.tsx` returns >= 1 (caveat present)
    - `grep -c 'MiniBulletChart' frontend/src/components/charts/EndgameMetricCard.tsx` returns 0 (replaced by ScoreGapRow)
    - `grep -c 'formatDiffPct\|userRate\|opponentRate\|MIRROR_BUCKET' frontend/src/components/charts/EndgameMetricCard.tsx` returns 0
    - `grep -c 'mirror:' frontend/src/components/charts/EndgameMetricCard.tsx` returns 0 (prop deleted from EndgameMetricCardProps)
    - `grep -c 'vs opponent\|compared to opponents' frontend/src/components/charts/EndgameMetricCard.tsx` returns 0 (D-08 no-vs-opponents framing)
    - `cd frontend &amp;&amp; npm test -- --run EndgameMetricCard` exits 0
  </acceptance_criteria>
  <done>
    Peer-bullet row replaced with per-bucket ΔES `ScoreGapRow`; `mirror` prop deleted; bucket-specific `POPOVER_COPY` with sigmoid caveat wired; new testids present; Vitest green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor EndgameSkillCard.tsx — replace peer-bullet row with Skill ΔES ScoreGapRow</name>
  <files>frontend/src/components/charts/EndgameSkillCard.tsx, frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameSkillCard.tsx (full file — current peer-bullet row at lines 125-194, `EndgameSkillCardProps` at lines 41-50)
    - frontend/src/components/charts/EndgameMetricCard.tsx (post-Task-2 — the directly analogous refactor; reuse the same imports + JSX shape with Skill-specific tweaks)
    - frontend/src/components/charts/EndgameTypeCard.tsx lines 387-437 (per RESEARCH Pattern Templates)
    - frontend/src/generated/endgameZones.ts post-Plan-01 (`SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN/MAX`)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"frontend/src/components/charts/EndgameSkillCard.tsx" lines 659-683 + §Shared Patterns §SKILL_POPOVER_COPY (line 821)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-04, §D-07, §D-08 (Skill copy verbatim)
    - frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx (existing structure; mirror Task 2 Vitest pattern)
  </read_first>
  <action>
    Per PATTERNS deltas + D-04 + D-07 + D-08 Skill row:

    PART A — `EndgameSkillCard.tsx` refactor:
    (1) Imports — add at top:
        - `import { ScoreGapRow } from './EndgameOverallScoreGapRow';`
        - `import { SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN, SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX } from '@/generated/endgameZones';`
        - `import { ZONE_DANGER, ZONE_SUCCESS } from '@/lib/theme';`
        - `import { deriveLevel } from './EndgameOverallShared';` (same helper path as Task 2)
        - `import { MetricStatPopover } from '../popovers/MetricStatPopover';` (verify if already imported)
    (2) Imports — REMOVE: `MiniBulletChart`, `formatDiffPct` (from `endgameMetrics`), `MIN_OPPONENT_BASELINE_GAMES`, `NEUTRAL_ZONE_MAX`, `NEUTRAL_ZONE_MIN`, `BULLET_DOMAIN`, `isConfident`. KEEP `formatScorePct` (still used for the gauge label).
    (3) Props — on `EndgameSkillCardProps`:
        - DELETE: `oppSkill`, `pValue`, `ciLow`, `ciHigh` (the 4 peer-bullet props).
        - ADD: `scoreGapMean: number | null`, `scoreGapN: number | null`, `scoreGapPValue: number | null`, `scoreGapCiLow: number | null`, `scoreGapCiHigh: number | null` (mirrors the new 5 props on `EndgameMetricCardProps`).
    (4) Title `InfoPopover` body: drop "compared to opponents" / "vs Opp Skill" framing if present; replace with Stockfish-baseline framing per D-08.
    (5) Gap derivation block (similar to Task 2 but no `useMemo` bucket switch — Skill uses fixed constants):
        ```
        const gapMean = scoreGapMean;
        const gapN = scoreGapN ?? 0;
        const showGapRow = gapN > 0;
        const gapFormatted = gapMean != null
          ? (gapMean >= 0 ? '+' : '') + `${Math.round(gapMean * 100)}%`
          : '—';
        const gapColor = gapMean != null
          ? gapMean < SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN
            ? ZONE_DANGER
            : gapMean >= SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX
              ? ZONE_SUCCESS
              : undefined
          : undefined;
        const gapLevel = deriveLevel(scoreGapPValue ?? null, gapN);
        ```
    (6) Define Skill-specific popover copy at module scope (per PATTERNS §Shared Patterns `SKILL_POPOVER_COPY`):
        ```
        const SKILL_POPOVER_COPY = "Equal-weighted average of the three per-bucket Score Gaps above (Conversion + Parity + Recovery, each contributing equally). One-number summary of overall endgame performance vs Stockfish expectations, independent of which entry-eval bucket your endgames cluster in. Positive = above the Stockfish baseline; negative = below. Note: the Lichess expected-score sigmoid under-weights endgame eval; rely on the zone bands, not the raw magnitude.";
        ```
        Use the EXACT text from D-08 (Skill block). Identical sigmoid caveat suffix as the 3 bucket cards.
    (7) JSX — DELETE the peer-bullet row at lines 125-194 wholesale (You/Opp/Gap text + signed MiniBulletChart + sparse fallback).
    (8) JSX — INSERT the Skill ΔES row at the same position (between the gauge row and any subsequent content; Skill card has no WDL bar per Section 2 design):
        ```
        {showGapRow && (
          <div data-testid={`${tileTestId}-score-gap-bullet`}>
            <ScoreGapRow
              label={"Skill Score Gap:"}
              value={gapMean ?? 0}
              formatted={gapFormatted}
              resultColor={gapColor}
              valueTestId={`${tileTestId}-score-gap-value`}
              ariaLabel={`Skill Score Gap: ${gapFormatted}`}
              neutralMin={SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN}
              neutralMax={SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX}
              ciLow={scoreGapCiLow ?? undefined}
              ciHigh={scoreGapCiHigh ?? undefined}
              tooltip={
                <MetricStatPopover
                  name={"Skill Score Gap"}
                  explanation={SKILL_POPOVER_COPY}
                  value={gapMean ?? 0}
                  baseline={0}
                  unit="percent"
                  gameCount={gapN}
                  level={gapLevel}
                  pValue={scoreGapPValue}
                  vocabulary="score"
                  neutralLower={SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN}
                  neutralUpper={SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX}
                  baselineLabel="0%"
                  methodology={
                    <>
                      Score: equal-weighted mean of the three bucket means (Conv + Parity + Recov).<br />
                      Test: variance-of-sum propagation from the three independent paired-z tests.<br />
                      Confidence interval: 95% normal-approx on the propagated SE.
                    </>
                  }
                  testId={`${tileTestId}-score-gap-info`}
                  ariaLabel={"What is Skill Score Gap?"}
                />
              }
            />
          </div>
        )}
        ```
        Per D-07: label is `"Skill Score Gap:"` (NOT "Endgame Skill Score Gap" — the shorter form matches the bucket cards' pattern).
    (9) CLAUDE.md compliance: same as Task 2 (no em-dashes, `text-sm` floor, testids on interactive elements, no `vs opponents` framing).

    PART B — Vitest in `__tests__/EndgameSkillCard.test.tsx`:
    (10) REMOVE existing peer-bullet assertions targeting `${tileTestId}-you`/`-opp`/`-diff`/`-muted` and `oppSkill`/`pValue`/`ciLow`/`ciHigh` prop assertions.
    (11) ADD new assertions, mirroring Task 2's Vitest pattern but Skill-specific:
        - `tile-endgame-skill-score-gap-bullet` present when `scoreGapN > 0`.
        - Absent when `scoreGapN === 0`.
        - Sign convention: positive above neutral → ZONE_SUCCESS; negative below → ZONE_DANGER; inside band → undefined.
        - `tile-endgame-skill-score-gap-value` present with formatted value.
        - `tile-endgame-skill-score-gap-info` MetricStatPopover trigger present.
        - Popover explanation contains the Skill copy verbatim AND the sigmoid caveat.
        - Popover `name` is exactly `"Skill Score Gap"` (D-07).
        - "No vs opponents framing" — assert explanation does NOT contain `vs opponents` or `Opp Skill`.
        - CI whiskers props: passed at n>=10; undefined at n<2.
    (12) Update fixtures: drop deleted props (`oppSkill`, `pValue`, `ciLow`, `ciHigh`); add new ΔES props.
    (13) Run `cd frontend && npm test -- --run EndgameSkillCard` — all green.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run EndgameSkillCard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ScoreGapRow' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 2 (import + JSX)
    - `grep -c 'SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 1
    - `grep -c 'SKILL_POPOVER_COPY' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 2 (declaration + usage)
    - `grep -c '"Skill Score Gap:"' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 1 (label per D-07)
    - `grep -c 'name=.\\"Skill Score Gap\\"' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 1 (popover name per D-07)
    - `grep -c 'Equal-weighted average of the three per-bucket Score Gaps' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 1 (D-08 copy present)
    - `grep -c 'Lichess expected-score sigmoid under-weights' frontend/src/components/charts/EndgameSkillCard.tsx` returns >= 1
    - `grep -c 'MiniBulletChart\|oppSkill\|MIN_OPPONENT_BASELINE_GAMES' frontend/src/components/charts/EndgameSkillCard.tsx` returns 0
    - `grep -c 'vs opponent\|compared to opponents\|Opp Skill' frontend/src/components/charts/EndgameSkillCard.tsx` returns 0 (D-08 framing)
    - `cd frontend &amp;&amp; npm test -- --run EndgameSkillCard` exits 0
  </acceptance_criteria>
  <done>
    Skill peer-bullet row replaced with Skill ΔES `ScoreGapRow` using `SECTION2_SCORE_GAP_SKILL_*` bands and `SKILL_POPOVER_COPY`; props swapped; testids present; Vitest green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update EndgameMetricsSection.tsx orchestrator wiring + run full frontend CI gates</name>
  <files>frontend/src/components/charts/EndgameMetricsSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsSection.tsx (full file — `MIRROR_BUCKET` import at line 23, `buildZeroRow` at lines 111-126, Skill card mounting at lines 174-184, the 3-bucket card mounting around lines 145-170)
    - frontend/src/types/endgames.ts post-Task-1 (`ScoreGapMaterialResponse` shape with 20 new fields)
    - frontend/src/components/charts/EndgameMetricCard.tsx post-Task-2 (new `EndgameMetricCardProps` shape with 5 new `scoreGap*` props, no `mirror` prop)
    - frontend/src/components/charts/EndgameSkillCard.tsx post-Task-3 (new `EndgameSkillCardProps` shape with 5 new `scoreGap*` props, no `oppSkill`/`pValue`/`ciLow`/`ciHigh`)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"frontend/src/components/charts/EndgameMetricsSection.tsx" lines 686-693
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Pitfall 3 (buildZeroRow update + MIRROR_BUCKET usage at line 153)
  </read_first>
  <action>
    Per PATTERNS deltas + RESEARCH §Pitfall 3:
    (1) Delete the `MIRROR_BUCKET` import (line 23) and its usage at line 153 (`MIRROR_BUCKET[bucket]` lookup that fed the deleted `mirror` prop on `EndgameMetricCard`).
    (2) Update `buildZeroRow` (lines 111-126): drop `opponent_score`, `opponent_games`, `diff_p_value`, `diff_ci_low`, `diff_ci_high` from the constructed `MaterialRow` shape. The zero-row builder still constructs the gauge / score / games / WDL fields.
    (3) For each of the 3 bucket cards (Conv / Parity / Recov), update the `<EndgameMetricCard>` mounting to:
        - Remove the `mirror={...}` prop.
        - Add 5 new props sourced from `response.score_gap_material.section2_score_gap_<bucket>_*`:
          - `scoreGapMean={response.score_gap_material.section2_score_gap_conv_mean}` (conv card; similar for parity / recov)
          - `scoreGapN={response.score_gap_material.section2_score_gap_conv_n}`
          - `scoreGapPValue={response.score_gap_material.section2_score_gap_conv_p_value}`
          - `scoreGapCiLow={response.score_gap_material.section2_score_gap_conv_ci_low}`
          - `scoreGapCiHigh={response.score_gap_material.section2_score_gap_conv_ci_high}`
        - Source bucket-to-field mapping correctly: `bucket === "conversion"` → `_conv_*`, `bucket === "parity"` → `_parity_*`, `bucket === "recovery"` → `_recov_*`. Implement as a const lookup or an inline switch — prefer inline since there are only 3 cards mounted statically.
    (4) Update Skill card mounting (lines 174-184):
        - Remove `oppSkill={...}`, `pValue={...}`, `ciLow={...}`, `ciHigh={...}`.
        - Add 5 new props:
          - `scoreGapMean={response.score_gap_material.section2_score_gap_skill_mean}`
          - `scoreGapN={response.score_gap_material.section2_score_gap_skill_n}`
          - `scoreGapPValue={response.score_gap_material.section2_score_gap_skill_p_value}`
          - `scoreGapCiLow={response.score_gap_material.section2_score_gap_skill_ci_low}`
          - `scoreGapCiHigh={response.score_gap_material.section2_score_gap_skill_ci_high}`
    (5) `noUncheckedIndexedAccess` compliance: per CLAUDE.md, every array/Record index access returns `T | undefined`. The `response.score_gap_material.section2_score_gap_*` reads are object property access, NOT index access — they return the typed `number | null` per the TS interface. No narrowing needed for these reads. If any helper accesses a `Record<MaterialBucket, ...>` value, narrow with a local variable before use.
    (6) CLAUDE.md mobile parity: no JSX-structural changes here (orchestrator still mounts 4 cards in the existing responsive grid); the only delta is the per-card prop set. No mobile-specific change needed.
    (7) Full frontend CI gates after all 4 tasks:
        - `cd frontend && npx tsc --noEmit` — must exit 0 (catches any orphan reference to deleted types / props).
        - `cd frontend && npm run lint` — must exit 0.
        - `cd frontend && npm run knip` — must exit 0 (CI gate per CLAUDE.md; deletion sweep is what makes knip happy after Phase 86's mirror plumbing).
        - `cd frontend && npm run build` — must succeed (tsc + Vite production build).
        - `cd frontend && npm test -- --run EndgameMetricCard EndgameSkillCard EndgameMetricsSection` — all relevant component tests green.
    (8) Verify zero references to the deleted symbols remain anywhere in `frontend/src/` (excluding the deletion-grep test added below):
        - `grep -rn 'MIRROR_BUCKET\|userRate\|opponentRate\|formatDiffPct\|NEUTRAL_ZONE_MIN\|NEUTRAL_ZONE_MAX\|BULLET_DOMAIN\|MIN_OPPONENT_BASELINE_GAMES\|opponent_score\|opponent_games\|skill_diff_\|opp_skill' frontend/src/` — must return 0 active matches (only changelog / phase-comment hits acceptable).
        - Add a tiny deletion-grep regression test if helpful — see RESEARCH §"Deletion Inventory" Vitest hint. Optional: a single test file `frontend/src/__tests__/no_mirror_plumbing.test.ts` that asserts via `import.meta.glob` + file-content scan that none of the 12 deleted symbols appear in `frontend/src/**/*.{ts,tsx}` outside `__tests__/` paths. If implementing, scope tightly to the 12 symbols and exclude itself; otherwise rely on the knip CI gate + grep verification.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npm test -- --run EndgameMetricCard EndgameSkillCard EndgameMetricsSection</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MIRROR_BUCKET' frontend/src/components/charts/EndgameMetricsSection.tsx` returns 0
    - `grep -c 'opponent_score\|opponent_games' frontend/src/components/charts/EndgameMetricsSection.tsx` returns 0 (buildZeroRow updated)
    - `grep -c 'scoreGapMean' frontend/src/components/charts/EndgameMetricsSection.tsx` returns >= 4 (3 bucket cards + 1 Skill card)
    - `grep -c 'section2_score_gap_skill_mean' frontend/src/components/charts/EndgameMetricsSection.tsx` returns >= 1
    - `grep -rn 'MIRROR_BUCKET\|formatDiffPct\|opponentRate\|userRate\|BULLET_DOMAIN\|MIN_OPPONENT_BASELINE_GAMES' frontend/src/ | grep -v '__tests__' | grep -v 'no_mirror_plumbing' | grep -v '^.*://'` returns 0 active matches
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0
    - `cd frontend &amp;&amp; npm run lint` exits 0
    - `cd frontend &amp;&amp; npm run knip` exits 0
    - `cd frontend &amp;&amp; npm test -- --run EndgameMetricsSection` exits 0
    - `cd frontend &amp;&amp; npm run build` exits 0
  </acceptance_criteria>
  <done>
    Orchestrator threads 20 new ΔES fields from response to 4 card components; MIRROR_BUCKET usage removed; buildZeroRow updated; knip + tsc + lint + build + Vitest all green; zero references remain to deleted symbols anywhere in frontend.
  </done>
</task>

</tasks>

<threat_model>
none — purely client-side render refactor on existing authenticated `/api/endgames/overview` consumer. The new row consumes data already returned by Plan 02's backend. No new user input, no new client-side state, no new XHR. The deletion sweep tightens the wire-schema surface but does not change auth / access-control / input-validation paths.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameMetricCard EndgameSkillCard EndgameMetricsSection` exits 0
- `cd frontend && npx tsc --noEmit && npm run lint && npm run knip && npm run build` all succeed
- `grep -rn 'MIRROR_BUCKET\|userRate\|opponentRate\|formatDiffPct\|NEUTRAL_ZONE_MIN\|NEUTRAL_ZONE_MAX\|BULLET_DOMAIN\|MIN_OPPONENT_BASELINE_GAMES\|opponent_score\|opponent_games\|skill_diff_\|opp_skill' frontend/src/` returns 0 active matches
- Sign convention visually correct on each of 4 cards: positive ΔES renders right of zero with ZONE_SUCCESS tint above neutralMax
- Manual UAT (Plan 04 will capture): open Endgames page → 4 Section 2 cards render one ΔES Score Gap bullet each → popovers show bucket-specific copy with identical sigmoid caveat → no "vs opponents" wording anywhere
</verification>

<success_criteria>
SEC2-ΔES-01, SEC2-ΔES-04 (frontend), SEC2-ΔES-05, SEC2-ΔES-07 (frontend) satisfied:
- SEC2-ΔES-01: each of 4 cards renders one ScoreGapRow centred on 0 with CI whiskers; replaces the prior peer-bullet row; reuses `ScoreGapRow` + `MiniBulletChart` (no new chart components).
- SEC2-ΔES-04: frontend deletion sweep complete (8 lib symbols, 5 type fields × 2, 4 props × 2, `mirror` prop, `oppSkill`/`pValue`/`ciLow`/`ciHigh`); knip + tsc + lint clean.
- SEC2-ΔES-05: `MetricStatPopover` carries bucket-specific copy per D-08 with identical sigmoid-bias caveat one-liner across all 4 popovers; no "vs opponents" framing anywhere.
- SEC2-ΔES-07: Vitest covers ScoreGapRow render at n>=10 and absence at n=0; sign-convention tint; testid sub-elements; popover content; CI prop threading.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-03-SUMMARY.md`.
Also append the 375px parity check + sign-convention visual check + "no vs opponents framing" copy-review check from VALIDATION §"Manual-Only Verifications" into `.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-HUMAN-UAT.md`.
</output>
