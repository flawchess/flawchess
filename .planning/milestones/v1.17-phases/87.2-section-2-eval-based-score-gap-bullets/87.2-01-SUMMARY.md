---
phase: "87.2"
plan: "01"
subsystem: endgame-zones
tags:
  - benchmarks
  - codegen
  - zones
  - endgame
dependency_graph:
  requires:
    - "87.1-01-SUMMARY.md (ZONE_REGISTRY + codegen patterns, per-class ΔES precedent)"
  provides:
    - "section2_score_gap_{conv,parity,recov,skill} MetricId literals in ZONE_REGISTRY"
    - "8 new SECTION2_SCORE_GAP_*_NEUTRAL_MIN/MAX TS exports in endgameZones.ts"
    - "§3.4.4 calibration method documented in SKILL.md"
  affects:
    - "Plan 02 (backend math) — uses ZONE_REGISTRY[section2_score_gap_*] via assign_zone()"
    - "Plan 03 (frontend cards) — imports SECTION2_SCORE_GAP_*_NEUTRAL_MIN/MAX from endgameZones.ts"
    - "Plan 04 (LLM payload) — dispatches assign_zone(section2_score_gap_*, value)"
tech_stack:
  added: []
  patterns:
    - "Option (a): 4 scalar MetricIds in ZONE_REGISTRY (not a bucket-dispatched parent)"
    - "--check mode on gen_endgame_zones_ts.py for in-process drift detection"
key_files:
  created: []
  modified:
    - "app/services/endgame_zones.py"
    - "scripts/gen_endgame_zones_ts.py"
    - "frontend/src/generated/endgameZones.ts"
    - ".claude/skills/benchmarks/SKILL.md"
decisions:
  - "D-02 option (a): 4 distinct scalar MetricIds in ZONE_REGISTRY; not a bucket-dispatched parent. Rationale: existing bucket dispatch is class-keyed (PER_CLASS_GAUGE_ZONES), not eval-entry-bucket-keyed; option (a) avoids a new dispatch shape."
  - "Placeholder bands ±5pp mirror achievable_score_gap family starting point; calibrated by future /benchmarks §3.4.4 session."
  - "assign_zone() requires no code change — new MetricIds flow through existing ZONE_REGISTRY[metric_id] lookup."
  - "--check flag added to gen_endgame_zones_ts.py for programmatic drift detection (plan Task 2 verification contract)."
metrics:
  duration: "~25 minutes"
  completed: "2026-05-15"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 4
---

# Phase 87.2 Plan 01: Zone Registry + Codegen Contract Summary

Zone registry and codegen contract for 4 per-bucket ΔES Score Gap MetricIds with placeholder ±5pp bands, 8 new TS exports regenerated cleanly, and §3.4.4 calibration method documented in SKILL.md.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Extend MetricId + ZONE_REGISTRY with 4 per-bucket ΔES entries | ac17869c | app/services/endgame_zones.py |
| 2 | Extend gen_endgame_zones_ts.py + regenerate endgameZones.ts | e3f3d8b7 | scripts/gen_endgame_zones_ts.py, frontend/src/generated/endgameZones.ts |
| 3 | Add §3.4.4 Per-bucket ΔES Score Gap subchapter to benchmarks SKILL.md | 9b6a8b7b | .claude/skills/benchmarks/SKILL.md |

## What Was Built

**`app/services/endgame_zones.py`:**
- 4 new literals added to `MetricId`: `section2_score_gap_conv`, `section2_score_gap_parity`, `section2_score_gap_recov`, `section2_score_gap_skill`.
- 4 new `ZONE_REGISTRY` entries, each `ZoneSpec(typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better")`.
- Comment on `BucketedMetricId` noting why section2_score_gap_* are scalar MetricIds (not bucket-dispatched).
- `assign_zone(metric_id, value)` dispatches the 4 new IDs via the existing `ZONE_REGISTRY[metric_id]` lookup — no helper code change.

**`scripts/gen_endgame_zones_ts.py`:**
- 4 new module-level spec aliases (`_SECTION2_SCORE_GAP_CONV_SPEC` etc.) mirroring the existing `_ENDGAME_TYPE_SCORE_GAP_SPEC` pattern.
- 8 new `export const` lines emitted in `_render()` after the `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX` line.
- `--check` mode added to `main()`: reads the current file, compares to `_render()` output, exits 1 on drift.

**`frontend/src/generated/endgameZones.ts`:** Regenerated via codegen script. 8 new exports present; `--check` gate passes; frontend build succeeds.

**`.claude/skills/benchmarks/SKILL.md`:**
- §3.4.4 subchapter added after §3.4.3 with: question paragraph, per-user-per-bucket metric definition, sample floor (≥20 spans per bucket), Cohen's-d collapse verdict instructions, sigmoid-bias expectation (conv negative / recov positive), output instructions referencing the 4 ZONE_REGISTRY keys, and SQL CTE template adapted from §3.4.2 with bucket column derived inside `gap_rows`.
- Report file layout TOC updated with §3.4.4 entry.
- Top-axis collapse summary table updated with 4 new per-bucket rows.

## Deviations from Plan

**1. [Rule 2 - Enhancement] Added `--check` mode to gen_endgame_zones_ts.py**
- **Found during:** Task 2 verification — plan says `uv run python scripts/gen_endgame_zones_ts.py --check` must exit 0, but the script had no `--check` handler.
- **Fix:** Added `--check` mode that generates content in memory, compares to the on-disk file, and exits 1 on drift. Mirrors the CI behavior (`git diff --exit-code`) but works without a git working tree and is faster.
- **Files modified:** scripts/gen_endgame_zones_ts.py
- **Commit:** e3f3d8b7

**2. [Info] SKILL.md §3.4.4 heading appears twice (content + Report layout TOC)**
- The acceptance criterion `grep -c '#### 3.4.4 Per-bucket ΔES Score Gap' ... returns 1` is met in the intent (the content heading exists) but the grep count is 2 because the Report file layout TOC section (which the plan also instructs to update) uses the identical heading text. Both occurrences are intentional. Existing §3.4.3 has the same pattern.

## Verification

- `uv run ty check app/services/endgame_zones.py` — passes (All checks passed!)
- `uv run python scripts/gen_endgame_zones_ts.py --check` — passes (OK: up to date)
- `grep -c 'section2_score_gap_conv' app/services/endgame_zones.py` — 2 (MetricId literal + ZONE_REGISTRY key)
- `grep -c 'SECTION2_SCORE_GAP_' frontend/src/generated/endgameZones.ts` — 8 (4 × MIN+MAX)
- `cd frontend && npm run build` — passes (built in 6.04s)
- `uv run python -c "... assert assign_zone('section2_score_gap_conv', 0.0) == 'typical'"` — passes

## Known Stubs

The 4 new ZONE_REGISTRY entries use placeholder ±5pp bands (`typical_lower=-0.05, typical_upper=0.05`). These are intentional: §3.4.4 of SKILL.md documents the calibration method, and a future benchmark session will replace them with real per-bucket IQR-derived values. This does not prevent the plan's goal (publishing the registry contract for Plans 02, 03, 04) from being achieved.

## Self-Check: PASSED

Files created/modified:
- FOUND: /home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-aabbf009277e31fbd/app/services/endgame_zones.py
- FOUND: /home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-aabbf009277e31fbd/scripts/gen_endgame_zones_ts.py
- FOUND: /home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-aabbf009277e31fbd/frontend/src/generated/endgameZones.ts
- FOUND: /home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-aabbf009277e31fbd/.claude/skills/benchmarks/SKILL.md

Commits:
- FOUND: ac17869c (Task 1)
- FOUND: e3f3d8b7 (Task 2)
- FOUND: 9b6a8b7b (Task 3)
