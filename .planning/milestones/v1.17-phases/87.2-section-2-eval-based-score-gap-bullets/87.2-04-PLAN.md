---
phase: 87.2
plan: 04
type: execute
wave: 2
depends_on:
  - 87.2-01   # consumes 4 new MetricIds + ZONE_REGISTRY entries for assign_zone dispatch
  - 87.2-02   # consumes 20 new ScoreGapMaterialResponse fields for finding emission
files_modified:
  - app/services/insights_service.py
  - app/services/insights_llm.py
  - app/prompts/endgame_insights.md
  - tests/services/test_insights_llm.py
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
autonomous: true
requirements:
  - SEC2-ΔES-06   # LLM payload adds 4 new per-bucket findings + prompt v30 → v31
must_haves:
  truths:
    - "insights_service._findings_endgame_metrics emits 4 new SubsectionFinding objects (section2_score_gap_{conv,parity,recov,skill}) alongside the existing rate findings"
    - "_PROMPT_VERSION bumped endgame_v30 → endgame_v31; cache-bust automatic via prompt-version key"
    - "Endgame insights prompt glossary defines Section 2 Score Gap family per D-10 (umbrella term + 4 bucket entries + sign convention + sigmoid caveat verbatim from v29)"
    - "Existing rate findings (endgame_skill, conversion_win_pct, parity_score_pct, recovery_save_pct) preserved (D-09 ADDITIVE, not swap — per RESEARCH §Critical Drift)"
    - "No p_value / verdict sub-field on the 4 new findings — zone band IS the significance signal per memory feedback_llm_significance_signal.md"
    - "REQUIREMENTS.md appended with SEC2-ΔES-01..07 entries traceable to ROADMAP success criteria"
    - "CHANGELOG.md [Unreleased] section records Phase 87.2 ΔES bullet swap and mirror-bucket retirement"
  artifacts:
    - path: "app/services/insights_service.py"
      provides: "4 new SubsectionFinding emissions in _findings_endgame_metrics"
      contains: "section2_score_gap_conv"
    - path: "app/services/insights_llm.py"
      provides: "_PROMPT_VERSION bump v30→v31 with full changelog comment"
      contains: "endgame_v31"
    - path: "app/prompts/endgame_insights.md"
      provides: "Section 2 Score Gap family glossary block (umbrella + 4 buckets + sigmoid caveat + dual-label rule)"
      contains: "Section 2 Score Gap"
    - path: "tests/services/test_insights_llm.py"
      provides: "Per-finding assertion + version bump + key-shape assertion + grep that endgame_v30 absent"
      contains: "section2_score_gap"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC2-ΔES-01..07 IDs traceable to ROADMAP success criteria"
      contains: "SEC2-ΔES-01"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] entry recording Phase 87.2 ΔES bullet swap + mirror retirement"
      contains: "Phase 87.2"
  key_links:
    - from: "app/services/insights_service.py"
      to: "app/services/endgame_zones.py"
      via: "assign_zone(section2_score_gap_*, mean) dispatched per bucket"
      pattern: "assign_zone\\(\"section2_score_gap_"
    - from: "app/services/insights_service.py"
      to: "app/schemas/endgames.py"
      via: "Reads response.score_gap_material.section2_score_gap_<bucket>_mean / _n"
      pattern: "section2_score_gap_(conv|parity|recov|skill)_(mean|n)"
    - from: "app/services/insights_llm.py"
      to: "app/prompts/endgame_insights.md"
      via: "Prompt version + glossary referenced by the assembled user prompt"
      pattern: "endgame_v31"
---

<!--
SEC2-ΔES-06 drafted from .planning/milestones/v1.17-ROADMAP.md §Phase 87.2 success criteria item #6.
This plan is the LLM payload half: additive findings + prompt v31 + REQUIREMENTS.md IDs + CHANGELOG.

Critical drift from CONTEXT D-09 (per RESEARCH §LLM Payload Critical Drift, §Pitfall 5):
The CONTEXT framing "swap 4 rate-diff fields for 4 ΔES fields in the LLM payload" is incorrect — the
rate-diff fields (MaterialRow.diff_p_value, ScoreGapMaterialResponse.skill_diff_p_value etc.) are NOT
currently emitted to the LLM. Section 2's LLM emission is rate-only (endgame_skill, conversion_win_pct,
parity_score_pct, recovery_save_pct). So D-09 is treated as "ADD 4 new findings alongside the existing
rate findings" — wire-schema cleanup happens in Plan 02 independently.
-->

<objective>
Extend `insights_service._findings_endgame_metrics` with 4 new ΔES findings (`section2_score_gap_{conv,parity,recov,skill}`) wired through `assign_zone` from Plan 01's new MetricIds and the per-bucket means / sample counts from Plan 02's new schema fields. Wire shape per finding: `{mean, n, zone, neutral_band}` only — no `p_value` / `verdict` sub-field (per memory `feedback_llm_significance_signal.md`). Bump `_PROMPT_VERSION` from `endgame_v30` to `endgame_v31` to cache-bust existing LLM reports. Add a "Section 2 Score Gap family" glossary block to `app/prompts/endgame_insights.md` per D-10 — umbrella term + 4 bucket entries + sign convention + sigmoid caveat verbatim from v29 + dual-label terminology rule. Append `SEC2-ΔES-01..07` to `.planning/REQUIREMENTS.md` (drafted from the ROADMAP's 7 success criteria). Record the Phase 87.2 user-facing change in `CHANGELOG.md` `[Unreleased]`.

Purpose: surfaces the new per-bucket ΔES metric family to LLM narration so the auto-generated endgame insights report can reference per-bucket Score Gap performance with the same dual-label terminology rule users see on the cards. Existing rate findings remain — the LLM benefits from both signals: the rate-based Skill / Conv / Parity / Recov give the gauge values users see; the ΔES findings give the Stockfish-baseline-anchored performance signal.

Output: 4 new findings in the emitter, prompt version bump, glossary block, test coverage, REQUIREMENTS.md IDs, CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-VALIDATION.md
@.planning/notes/lichess-sigmoid-endgame-calibration.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-01-SUMMARY.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-02-SUMMARY.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-04-SUMMARY.md
@app/services/insights_service.py
@app/services/insights_llm.py
@app/prompts/endgame_insights.md

<interfaces>
<!-- Existing contracts consumed -->

From app/services/endgame_zones.py (post-Plan-01):
```
ZONE_REGISTRY["section2_score_gap_conv"]   = ZoneSpec(typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better")
ZONE_REGISTRY["section2_score_gap_parity"] = ZoneSpec(...)
ZONE_REGISTRY["section2_score_gap_recov"]  = ZoneSpec(...)
ZONE_REGISTRY["section2_score_gap_skill"]  = ZoneSpec(...)

def assign_zone(metric_id: MetricId, value: float) -> Literal["weak", "typical", "strong"]
```

From app/schemas/endgames.py (post-Plan-02):
```
class ScoreGapMaterialResponse:
    section2_score_gap_conv_mean:    float | None = None
    section2_score_gap_conv_n:       int   | None = None
    section2_score_gap_conv_p_value: float | None = None    # NOT emitted to LLM
    section2_score_gap_conv_ci_low:  float | None = None    # NOT emitted
    section2_score_gap_conv_ci_high: float | None = None    # NOT emitted
    # ... same 5-field cluster for parity, recov, skill (20 fields total)
```

From app/services/insights_service.py (current shape per RESEARCH §LLM Payload):
```
def _findings_endgame_metrics(response, window) -> list[SubsectionFinding]:
    """Current emissions: 1 endgame_skill + 3 per-bucket rate findings.
       Phase 87.2 ADDS 4 ΔES findings (D-09 ADDITIVE, not swap)."""
```

From app/services/insights_llm.py (current):
```
_PROMPT_VERSION = "endgame_v30"   # → bump to "endgame_v31"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 4 new ΔES findings to insights_service._findings_endgame_metrics + extend test coverage</name>
  <files>app/services/insights_service.py, tests/services/test_insights_llm.py</files>
  <read_first>
    - app/services/insights_service.py lines 692-789 (`_findings_endgame_metrics` — current rate-based emissions: 1 endgame_skill + 3 per-bucket rate findings; the new 4 ΔES findings emit alongside, not replacing)
    - app/services/insights_service.py top-of-file imports (verify if `assign_zone` and `sample_quality` are imported; add if missing)
    - app/services/endgame_zones.py post-Plan-01 (`assign_zone` signature + 4 new MetricIds)
    - app/schemas/endgames.py post-Plan-02 (20 new `section2_score_gap_*` fields on `ScoreGapMaterialResponse`)
    - app/services/insights_llm.py:703 `_synthesize_endgame_type_achievable_score_gap_findings` (Phase 87.1 analog — direct emission shape template)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"app/services/insights_service.py" lines 205-245 (exact emission shape) + §LLM payload field naming + sig signal (line 836-843)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §LLM Payload (Critical Drift) — D-09 is ADDITIVE
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-09
    - tests/services/test_insights_llm.py (current structure; find Section-2-emitter tests to extend)
  </read_first>
  <behavior>
    - Test: full-cohort fixture (each bucket n >= CONFIDENCE_MIN_N) emits 4 new `SubsectionFinding` objects with `metric` in {section2_score_gap_conv, _parity, _recov, _skill}, `value` populated from per-bucket mean, `zone` in {"weak","typical","strong"}, `sample_size` populated from per-bucket n.
    - Test: sparse-cohort fixture (each bucket n < 10) emits the same 4 findings — `value` populated, `zone` reflects the raw mean's zone bucket (helper still dispatches), `sample_size` < 10, `is_headline_eligible` False (per `n >= 10` gate).
    - Test: empty-cohort fixture (each bucket n == 0 → `section2_score_gap_<bucket>_mean is None`) emits 4 findings with `value = float("nan")` and `is_headline_eligible = False`. The `assign_zone` call routes NaN → "typical" per existing convention (verify against Phase 87.1's analog behavior).
    - Test: existing rate findings (endgame_skill, conversion_win_pct, parity_score_pct, recovery_save_pct) PRESERVED — total finding count grows by 4, not replaced.
    - Test: NO `p_value` / `verdict` / `ci_low` / `ci_high` key on the new findings — the SubsectionFinding dataclass exposes a fixed schema, but assert via emission shape that no extension fields are leaked through `dimension` or `extra`.
    - Test: dimension is `None` on each new finding (single-bucket-per-finding shape, no need for a `bucket` discriminator since the metric_id encodes the bucket).
    - Test: `is_headline_eligible` is True only when `n >= 10 AND mean is not None` (per the existing Phase 87.1 convention).
  </behavior>
  <action>
    Per D-09 (RESEARCH-corrected to ADDITIVE) + PATTERNS deltas:
    (1) Imports at top of `app/services/insights_service.py` (verify; add if missing):
        - `from app.services.endgame_zones import assign_zone` (existing — verify)
        - `import math` for `math.isnan` if not already imported (the NaN-fallback path uses it)
        - `from app.schemas.insights import SubsectionFinding` (existing — verify the dataclass name and import path)
    (2) Locate `_findings_endgame_metrics` (around line 692). After the existing rate-finding emissions (the loop that emits `endgame_skill`, `conversion_win_pct`, `parity_score_pct`, `recovery_save_pct`), append a new loop emitting 4 ΔES findings per PATTERNS Delta:
        ```
        # Phase 87.2 (D-09 — ADDITIVE per RESEARCH §LLM Payload Critical Drift):
        # Four new ΔES Score Gap findings alongside the existing rate findings.
        # Wire shape: (mean, n, zone, neutral_band). No p_value / verdict — band IS
        # the significance signal (memory feedback_llm_significance_signal.md).
        for bucket_id, metric_id in [
            ("conv",   "section2_score_gap_conv"),
            ("parity", "section2_score_gap_parity"),
            ("recov",  "section2_score_gap_recov"),
            ("skill",  "section2_score_gap_skill"),
        ]:
            mean_attr = f"section2_score_gap_{bucket_id}_mean"
            n_attr    = f"section2_score_gap_{bucket_id}_n"
            mean_raw = getattr(response.score_gap_material, mean_attr)
            n_raw    = getattr(response.score_gap_material, n_attr) or 0
            value_for_payload = mean_raw if mean_raw is not None else float("nan")
            findings.append(SubsectionFinding(
                subsection_id="endgame_metrics",
                parent_subsection_id=None,
                window=window,
                metric=metric_id,
                value=value_for_payload,
                zone=assign_zone(metric_id, value_for_payload),
                trend="n_a",
                weekly_points_in_window=0,
                sample_size=n_raw,
                sample_quality=sample_quality("endgame_metrics", n_raw),
                is_headline_eligible=(n_raw >= 10 and mean_raw is not None),
                dimension=None,
            ))
        ```
        Match the surrounding emitter's exact `SubsectionFinding` field set (the field list above is per RESEARCH §LLM Payload — Wire shape; cross-check against the existing dataclass to catch any field name drift in `subsection_id` / `parent_subsection_id` / `window` / `weekly_points_in_window` etc.).
    (3) Verify the new ΔES findings reach `insights_llm` rendering automatically. The dispatch in `_format_zone_bounds` (`insights_llm.py:~383`) already routes via `metric_id` lookup against `ZONE_REGISTRY` — the 4 new MetricIds from Plan 01 are reachable without further wiring (per RESEARCH §LLM Payload — Wire shape: `_format_zone_bounds` dispatch handles the `neutral_band` inlining). No new wire-shape field needed for `neutral_band` — the existing zone-band rendering injects `[lower, upper]` per `metric_id`.
    (4) Sentry: No new exceptional paths. `assign_zone` is total over its MetricId domain (passing a float, even NaN, returns a zone label). `getattr` with explicit fallback to `0` for n covers the None case.
    (5) ty + ruff:
        - `getattr(...) or 0` collapses `None` / `0` / `False`; if `n_attr` could legitimately be `0` (n=0 case is `int | None` with None default), prefer `(getattr(...) if getattr(...) is not None else 0)`. Simpler form: assign `n_raw_or_none = getattr(response.score_gap_material, n_attr)`; `n_raw = n_raw_or_none if n_raw_or_none is not None else 0`.
        - `value_for_payload` is `float` (NaN or the actual mean). The `value` field on `SubsectionFinding` is `float` per the existing Phase 87.1 emission shape — verify.
        - `# ty: ignore[attr-defined]` permitted only on `getattr(response.score_gap_material, f"section2_score_gap_{bucket_id}_mean")` if ty's narrowing flags the dynamic attribute access; include the rule name and a 1-line reason.
    (6) Tests in `tests/services/test_insights_llm.py`:
        - Add `test_section2_score_gap_findings_full_cohort` — fixture with each bucket n=15, distinct means; assert 4 new findings present with correct metric_id / value / zone / sample_size; assert existing rate findings still present (total finding count includes both sets).
        - Add `test_section2_score_gap_findings_sparse_cohort` — fixture with each bucket n=5; assert 4 findings present with `is_headline_eligible = False` (n < 10 gate).
        - Add `test_section2_score_gap_findings_empty_cohort` — fixture with each bucket n=0 and mean=None; assert 4 findings present with `value = NaN` (use `math.isnan` to verify) and `is_headline_eligible = False`.
        - Add `test_section2_score_gap_findings_skill_metric_present` — separately assert the Skill metric specifically because Skill is the equal-weighted aggregate, not a per-bucket cohort.
        - Add `test_existing_rate_findings_preserved` — assert all 4 pre-existing rate findings (endgame_skill, conversion_win_pct, parity_score_pct, recovery_save_pct) survive the new emission (D-09 ADDITIVE, not swap).
    (7) `uv run pytest tests/services/test_insights_llm.py -x` — must exit 0.
    (8) `uv run ty check app/services/insights_service.py tests/services/test_insights_llm.py` — must exit 0.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_insights_llm.py -x &amp;&amp; uv run ty check app/services/insights_service.py tests/services/test_insights_llm.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'section2_score_gap_conv' app/services/insights_service.py` returns >= 1 (metric_id in the new emission loop)
    - `grep -c 'section2_score_gap_skill' app/services/insights_service.py` returns >= 1
    - `grep -c 'assign_zone' app/services/insights_service.py` returns >= 1 (call inside the new loop; or pre-existing import + reuse)
    - `grep -c 'is_headline_eligible' app/services/insights_service.py` returns >= 1 (new finding gating)
    - `grep -c 'def test_section2_score_gap_findings' tests/services/test_insights_llm.py` returns >= 3 (full, sparse, empty cohorts)
    - `grep -c 'def test_existing_rate_findings_preserved' tests/services/test_insights_llm.py` returns 1
    - `uv run pytest tests/services/test_insights_llm.py -x` exits 0
    - `uv run ty check app/services/insights_service.py tests/services/test_insights_llm.py` exits 0
  </acceptance_criteria>
  <done>
    4 new findings emitted in `_findings_endgame_metrics`; existing rate findings preserved; Vitest covers full / sparse / empty cohorts + preservation; ty clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump _PROMPT_VERSION endgame_v30 → endgame_v31 + extend glossary in endgame_insights.md</name>
  <files>app/services/insights_llm.py, app/prompts/endgame_insights.md, tests/services/test_insights_llm.py</files>
  <read_first>
    - app/services/insights_llm.py lines 60-100 (`_PROMPT_VERSION` constant + the multi-line changelog comment block — Phase 87.1 v29 changelog style is the template for v31)
    - app/services/insights_llm.py around line 1442 (`type_breakdown` emission tuple — verify if any per-card payload assembly needs updating; per RESEARCH the new findings flow through `_findings_endgame_metrics` → `_format_zone_bounds` without explicit per-card wiring)
    - app/prompts/endgame_insights.md (current glossary block — find the Phase 87.1 "Endgame Type Score Gap" entry; the new "Section 2 Score Gap family" block sits alongside)
    - .planning/notes/lichess-sigmoid-endgame-calibration.md (calibration caveat copy — used verbatim across all glossary surfaces)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"app/services/insights_llm.py" lines 248-289 (full v31 changelog comment) + §"app/prompts/endgame_insights.md" lines 291-331 (full glossary block)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-10 (dual-label rule + per-bucket interpretation rules + sigmoid caveat)
    - tests/services/test_insights_llm.py (grep for `endgame_v30` literal assertions to bump)
  </read_first>
  <action>
    Per D-10 + PATTERNS deltas + memory `feedback_llm_prompt_design.md` + memory `feedback_llm_significance_signal.md`:

    PART A — `app/services/insights_llm.py` version bump:
    (1) Locate `_PROMPT_VERSION` constant (around line 68). Bump from `"endgame_v30"` to `"endgame_v31"`. Update the multi-line changelog comment block per PATTERNS §"app/services/insights_llm.py" lines 248-289 — verbatim text covering:
        - The 4 new metric findings `section2_score_gap_{conv,parity,recov,skill}` added to `endgame_metrics` subsection ALONGSIDE the existing rate findings (D-09 ADDITIVE).
        - Wire shape per finding: `(mean, n, zone, neutral_band)`. No `p_value` / `verdict` (memory `feedback_llm_significance_signal.md` — band IS the significance signal).
        - Acknowledge the prior rate-based per-bucket peer-diff Gap on Section 2 cards was mathematically degenerate by construction (Conv-Gap ≡ Recov-Gap by mirror-bucket symmetry; Parity-Gap = 2·user_parity − 1 affine of the gauge). The new ΔES Gap measures eval delta during the span (Stockfish-baseline anchor), making Conv and Recov genuinely independent.
        - Skill uses **equal-weighted mean** of the three bucket means (NOT pooled all-spans — pooled reduces by telescoping to the page-level Achievable Score Gap).
        - Sigmoid-bias caveat carried verbatim from v29.
        - Dual-label terminology rule: glossary umbrella "Section 2 Score Gap"; card-row form "Conversion Score Gap" / "Parity Score Gap" / "Recovery Score Gap" / "Skill Score Gap".
        - Preserve the prior v30 / v29 / v28 changelog text below the new v31 block (do not truncate).
    (2) No new dispatch code in `insights_llm.py` — the existing `_format_zone_bounds` machinery handles the 4 new MetricIds via `ZONE_REGISTRY` lookup (per RESEARCH §LLM Payload — Wire shape note). Verify by running the Task 1 tests after the version bump; if any zone-bound rendering breaks for the new metrics, add a branch in `_format_zone_bounds` (mirror Phase 87.1's `endgame_type_achievable_score_gap` branch — likely no new branch needed since the dispatch is `ZONE_REGISTRY[metric_id]` keyed).

    PART B — `app/prompts/endgame_insights.md` glossary:
    (3) Locate the existing glossary block (concepts section). Find the Phase 87.1 "Endgame Type Score Gap" entry — the new "Section 2 Score Gap family" block sits alongside (after the existing entry).
    (4) Append the new glossary block per PATTERNS §"app/prompts/endgame_insights.md" lines 296-331 (verbatim — do not paraphrase):

        ```
        ### Section 2 Score Gap family (Phase 87.2)

        The four per-card Score Gap metrics on Section 2 (Endgame Metrics) cards each measure
        average per-span expected-score delta restricted to that card's eval-entry bucket.

        - **Conversion Score Gap** (`section2_score_gap_conv`): average per-span Score Gap on
          spans where the user entered the endgame with eval ≥ +1.0 (user ahead). Positive =
          user converted advantages above the Stockfish baseline; negative = bled away.

        - **Parity Score Gap** (`section2_score_gap_parity`): same metric on spans entered with
          |eval| ≤ 1.0 (roughly balanced). Positive = user outperformed baseline from balanced;
          negative = underperformed.

        - **Recovery Score Gap** (`section2_score_gap_recov`): same metric on spans entered with
          eval ≤ −1.0 (user behind). Positive = user salvaged disadvantages above expectation;
          negative = position deteriorated further than expected.

        - **Skill Score Gap** (`section2_score_gap_skill`): equal-weighted mean of the three
          per-bucket Score Gaps above. One-number summary of overall endgame performance vs
          Stockfish expectations, independent of which entry-eval bucket your endgames cluster
          in. Buckets with fewer than 10 spans are dropped from the average.

        Sign convention (all four): positive = above the Stockfish baseline; negative = below.

        Calibration caveat: the metric uses the Lichess expected-score sigmoid which under-weights
        endgame eval; zones are percentile-calibrated from benchmark data so the bias does not
        affect zone placement. Rely on the zone bands, not the raw magnitude.

        Dual-label terminology: the glossary uses "Section 2 Score Gap" (umbrella term, full
        qualifier — disambiguates from the page-level "Endgame Score Gap", "Achievable Score Gap",
        and "Endgame Type Score Gap" terms). Card-row labels use the bucket-specific form
        ("Conversion Score Gap" etc.) because the card title already implies "Section 2".
        ```

    (5) Style compliance:
        - **NO em-dashes** in the new prompt copy per CLAUDE.md style preference. The PATTERNS block above uses periods / commas / parentheses; verify when copying that no em-dash (`—`) sneaks in. Important: the line `"disambiguates from the page-level..."` uses an em-dash in the PATTERNS draft — convert to a comma or parenthetical when writing into the prompt file. Same for any other em-dash in the block. Title-case for "Section 2 Score Gap" / "Conversion Score Gap" / etc. per the 260514-djw convention referenced in Phase 87.1's plan.
        - No internal identifier (`section2_score_gap_*`) in user-narration sections — the snake_case form appears inside backticks in the glossary entry only, NOT in any per-card narration prompt.
        - Sigmoid caveat phrasing is IDENTICAL to Phase 87.1 v29 — verify the existing v29 glossary entry's caveat text matches the block above to-the-letter. If divergent, lift v29's exact phrasing.

    PART C — Test updates for the version bump:
    (6) `grep -rn "endgame_v30" tests/services/test_insights_llm.py` to find every literal assertion. Bump each to `endgame_v31`.
    (7) `grep -rn "endgame_v30" tests/ app/` (outside the `_PROMPT_VERSION` changelog comments and outside CHANGELOG.md) — confirm no other active reference remains. Acceptable hits: docstring history, changelog comments, archived planning notes.
    (8) Add `test_prompt_version_bumped` to `tests/services/test_insights_llm.py` if not added in Task 1 — assert `_PROMPT_VERSION == "endgame_v31"`.
    (9) Add `test_endgame_insights_prompt_carries_section2_glossary` — load `app/prompts/endgame_insights.md` content; assert it contains the string "Section 2 Score Gap family" and "Conversion Score Gap" and the sigmoid caveat phrase "Lichess expected-score sigmoid".
    (10) `uv run pytest tests/services/test_insights_llm.py -x` — must exit 0.
    (11) `uv run ruff check app/services/insights_llm.py` — must exit 0. Ruff catches dead comment blocks if any.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_insights_llm.py -x &amp;&amp; grep -c '_PROMPT_VERSION = "endgame_v31"' app/services/insights_llm.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_PROMPT_VERSION = "endgame_v31"' app/services/insights_llm.py` returns 1
    - `grep -c '_PROMPT_VERSION = "endgame_v30"' app/services/insights_llm.py | head -1` returns 0 (no active `_PROMPT_VERSION = "endgame_v30"` line remains; references inside the new changelog comment block are acceptable but the constant value must be v31)
    - `grep -c 'Section 2 Score Gap family' app/prompts/endgame_insights.md` returns >= 1
    - `grep -c 'Conversion Score Gap' app/prompts/endgame_insights.md` returns >= 1
    - `grep -c 'Parity Score Gap' app/prompts/endgame_insights.md` returns >= 1
    - `grep -c 'Recovery Score Gap' app/prompts/endgame_insights.md` returns >= 1
    - `grep -c 'Skill Score Gap' app/prompts/endgame_insights.md` returns >= 1
    - `grep -c 'equal-weighted mean' app/prompts/endgame_insights.md` returns >= 1 (D-01 documented in glossary)
    - `grep -c 'Lichess expected-score sigmoid' app/prompts/endgame_insights.md` returns >= 1 (sigmoid caveat present)
    - `grep -c '"endgame_v30"' tests/services/test_insights_llm.py | head -1` returns 0 (all literal assertions bumped)
    - No em-dash characters introduced in the new glossary block (`grep -c '—' app/prompts/endgame_insights.md` must not increase vs pre-edit; ideally 0 new em-dashes in the diff)
    - `uv run pytest tests/services/test_insights_llm.py -x` exits 0
    - `uv run ruff check app/services/insights_llm.py` exits 0
  </acceptance_criteria>
  <done>
    Version bumped v30→v31 with full changelog comment; glossary block added with 4 bucket entries + sign convention + sigmoid caveat + dual-label rule; em-dash-free user-facing copy; all literal `endgame_v30` test assertions bumped; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append SEC2-ΔES-01..07 to REQUIREMENTS.md + record Phase 87.2 in CHANGELOG.md [Unreleased]</name>
  <files>.planning/REQUIREMENTS.md, CHANGELOG.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current shape — find the existing SEC2-01..10 block + SEC3 block; the new SEC2-ΔES-01..07 IDs slot alongside SEC2 entries or in a dedicated v1.17-late-addition subsection)
    - .planning/milestones/v1.17-ROADMAP.md lines 165-178 (Phase 87.2 §Success Criteria — the 7 numbered items are the source of truth for the new IDs)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Phase Requirements lines 50-58 (the canonical mapping ROADMAP-1..7 → SEC2-ΔES-01..07)
    - CHANGELOG.md (`[Unreleased]` header + current v1.17 entries — find the existing format used by Phases 86 and 87.1 to match tone)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-04-PLAN.md Task 2 PART B (Phase 87.1 CHANGELOG entry style — direct precedent)
  </read_first>
  <action>
    Per RESEARCH §Phase Requirements mapping + CLAUDE.md changelog rules:

    PART A — `.planning/REQUIREMENTS.md`:
    (1) Locate the v1.17 SEC2 block. Append a new sub-section heading `### Section 2 — eval-based ΔES Score Gap bullets (SEC2-ΔES — Phase 87.2)` after the existing SEC2-01..10 block (or at the appropriate v1.17 hierarchical position).
    (2) Append 7 new requirement IDs verbatim from RESEARCH §Phase Requirements mapping. Use the exact phrasing in the table below (drafted from the ROADMAP success criteria items 1-7):
        - **SEC2-ΔES-01**: Each of the 4 Section 2 cards (Conv / Parity / Recov / Skill) renders one ΔES Score Gap bullet centred on 0 with CI whiskers, replacing the prior `You / Opp / Gap` peer-diff row. Reuses `ScoreGapRow` + `MiniBulletChart` (no new chart components).
        - **SEC2-ΔES-02**: Backend exposes 4 new fields per card (`section2_score_gap_{conv,parity,recov,skill}_{mean,n,p_value,ci_low,ci_high}`) via `compute_paired_difference_test` per bucket. Endgame Skill uses the **equal-weighted mean** of the three bucket means (D-01), with buckets below `CONFIDENCE_MIN_N` dropped from the average.
        - **SEC2-ΔES-03**: 4 new zone-band entries in `ZONE_REGISTRY` keyed `section2_score_gap_{conv,parity,recov,skill}` with Cohen's-d collapse verdicts per /benchmarks §3.4.4; codegen regenerates `frontend/src/generated/endgameZones.ts`.
        - **SEC2-ΔES-04**: Mirror-bucket plumbing retired (D-03 / D-04 / D-05 deletion list): 8 frontend lib symbols + 5 + 5 schema fields + `MIRROR_BUCKET` + `_compute_skill_wire` + `compute_skill_diff_test` + `compute_per_bucket_diff_test` deleted; knip + ty + ruff clean.
        - **SEC2-ΔES-05**: Each card's `MetricStatPopover` carries bucket-specific copy per D-08 with sign-convention sentence and identical sigmoid-bias caveat one-liner across all 4 popovers; no "vs opponents" framing.
        - **SEC2-ΔES-06**: LLM payload adds 4 new `section2_score_gap_*` findings to `_findings_endgame_metrics` alongside the existing rate findings (D-09 ADDITIVE); prompt version bumped `endgame_v30` → `endgame_v31` with full changelog entry + dual-label glossary block.
        - **SEC2-ΔES-07**: Tests cover per-bucket math + Skill equal-weighted invariants (3-component reduction, denominator-drop edge cases, all-empty fallback) + cohort-empty fallback + Vitest snapshots + knip + ty + ruff clean.
    (3) Add a tiny traceability cross-reference at the bottom of the SEC2-ΔES sub-section: cite `.planning/milestones/v1.17-ROADMAP.md §Phase 87.2 success criteria items 1-7` as the source.
    (4) Update the `.planning/REQUIREMENTS.md` `## Traceability` table (if present at the bottom of the file) to add `SEC2-ΔES-01 | Phase 87.2` through `SEC2-ΔES-07 | Phase 87.2` rows. If no traceability table exists, skip this sub-step.

    PART B — `CHANGELOG.md`:
    (5) Locate `## [Unreleased]` section. Find the relevant `### Added` / `### Changed` / `### Removed` subsections (create them under `[Unreleased]` if missing).
    (6) Append bullets under the appropriate subsections, matching the tone of the existing v1.17 Phase 87.1 entry (terse, user-facing, per CLAUDE.md changelog rule):

        Under `### Changed`:
        - "Phase 87.2: Section 2 cards (Conversion, Parity, Recovery, Endgame Skill) now show a per-bucket Score Gap bullet anchored to the Stockfish baseline instead of the previous rate-based peer-diff bullet. Conversion shows score gap on spans you entered ahead; Recovery on spans you entered behind; Parity on balanced spans; Skill is the equal-weighted average of the three. Auto-generated endgame insights reference the new Section 2 Score Gap family."

        Under `### Removed`:
        - "Phase 87.2: Rate-based peer-diff bullet on Section 2 cards (the `You / Opp / Gap` row). The prior framing produced mathematically identical Conversion and Recovery values by construction; the new Stockfish-anchored framing yields genuinely independent per-bucket signals."

    (7) Bullets end without an em-dash or trailing notes per CLAUDE.md style. Reference Phase 87.2 explicitly. Title-case for "Section 2 Score Gap" / "Conversion" / "Parity" / "Recovery" / "Endgame Skill".
    (8) Verify `CHANGELOG.md` still parses as Keep-a-Changelog format: `## [Unreleased]` header intact, link references at the bottom unchanged (Phase 87.2 is not a release; only the milestone close at v1.17 final cuts a version).
    (9) Do NOT touch any released version sections (e.g. `## [v1.16]` etc.) — corrections to released sections go in a new `[Unreleased]` bullet per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c 'SEC2-ΔES-01' .planning/REQUIREMENTS.md &amp;&amp; grep -c 'Phase 87.2' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SEC2-ΔES-01' .planning/REQUIREMENTS.md` returns >= 1
    - `grep -c 'SEC2-ΔES-07' .planning/REQUIREMENTS.md` returns >= 1
    - `grep -cE 'SEC2-ΔES-0[1234567]' .planning/REQUIREMENTS.md` returns >= 7 (all 7 IDs present)
    - `grep -c 'Section 2 — eval-based ΔES Score Gap bullets' .planning/REQUIREMENTS.md` returns >= 1
    - `grep -c 'Phase 87.2' CHANGELOG.md` returns >= 1
    - `grep -c '## \[Unreleased\]' CHANGELOG.md` returns 1 (header preserved)
    - `grep -c 'Stockfish' CHANGELOG.md` returns >= 1 (new ΔES anchor framing in the changelog entry)
    - No em-dashes introduced in the new REQUIREMENTS.md or CHANGELOG.md additions: `git diff .planning/REQUIREMENTS.md CHANGELOG.md | grep -c '^+.*—'` returns 0 (no added lines contain em-dash)
    - The Greek capital delta `Δ` is permitted in `SEC2-ΔES-NN` IDs (matches the existing v1.17 naming scheme; verify it's the same Unicode point as elsewhere in the repo)
  </acceptance_criteria>
  <done>
    7 new SEC2-ΔES-NN IDs appended to REQUIREMENTS.md with traceability to ROADMAP success criteria; CHANGELOG.md `[Unreleased]` records Phase 87.2 ΔES bullet swap + mirror retirement in user-facing tone; no em-dashes added; KAC format preserved.
  </done>
</task>

</tasks>

<threat_model>
none — additive change to LLM payload assembly, prompt template, requirements doc, and changelog. No new exceptional paths (Sentry not needed); no new auth surfaces; no new user input. Prompt-version bump invalidates cached reports but does not change auth / rate-limit policy.
</threat_model>

<verification>
- `uv run pytest tests/services/test_insights_llm.py -x` exits 0
- `uv run ty check app/services/insights_service.py app/services/insights_llm.py tests/services/test_insights_llm.py` exits 0
- `uv run ruff check app/services/insights_service.py app/services/insights_llm.py` exits 0
- `grep -c '_PROMPT_VERSION = "endgame_v31"' app/services/insights_llm.py` >= 1
- `grep -c 'Section 2 Score Gap family' app/prompts/endgame_insights.md` >= 1
- `grep -cE 'SEC2-ΔES-0[1234567]' .planning/REQUIREMENTS.md` >= 7
- `grep -c 'Phase 87.2' CHANGELOG.md` >= 1
- Cache invalidation: trigger an endgame insights generation post-deploy with prompt_version key including `endgame_v31`; confirm prior cached reports under `endgame_v30` do not return.
- Manual UAT (capture in 87.2-HUMAN-UAT.md): generate an endgame insights report on dev DB; confirm narration uses "Section 2 Score Gap" as the umbrella term and "Conversion Score Gap" / "Parity Score Gap" / "Recovery Score Gap" / "Skill Score Gap" in per-card narration; confirm no "vs opponents" wording; confirm sigmoid caveat appears at least once in the narration body.
</verification>

<success_criteria>
SEC2-ΔES-06 satisfied:
- LLM payload `endgame_metrics` subsection carries 4 new `section2_score_gap_*` findings via `assign_zone` dispatch.
- `_PROMPT_VERSION` bumped `endgame_v30` → `endgame_v31` for cache invalidation.
- Glossary in `endgame_insights.md` defines the Section 2 Score Gap family with sign convention + sigmoid caveat + dual-label rule (D-10).
- Existing rate findings preserved (D-09 ADDITIVE per RESEARCH critical-drift correction).
- REQUIREMENTS.md updated with SEC2-ΔES-01..07 IDs.
- CHANGELOG.md `[Unreleased]` records the user-facing change.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-04-SUMMARY.md`.
Append "Verify LLM narration uses Section 2 Score Gap dual-label rule + no `vs opponents` framing + sigmoid caveat present" as a HUMAN-UAT item in `.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-HUMAN-UAT.md` (created by Plan 03; if not yet present, create it).
</output>
