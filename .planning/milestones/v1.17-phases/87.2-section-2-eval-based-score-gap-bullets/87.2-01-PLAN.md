---
phase: 87.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/benchmarks/SKILL.md
  - app/services/endgame_zones.py
  - scripts/gen_endgame_zones_ts.py
  - frontend/src/generated/endgameZones.ts
autonomous: true
requirements:
  - SEC2-ΔES-03   # Zone bands + codegen propagation for the 4 new per-bucket ΔES MetricIds
tags:
  - benchmarks
  - codegen
  - zones
  - endgame
must_haves:
  truths:
    - "ZONE_REGISTRY exposes section2_score_gap_{conv,parity,recov,skill} entries with higher_is_better direction"
    - "scripts/gen_endgame_zones_ts.py --check exits 0 after the 4 new entries land"
    - "frontend/src/generated/endgameZones.ts exports 8 new SECTION2_SCORE_GAP_*_NEUTRAL_MIN/MAX constants"
    - "SKILL.md §3.4.4 defines per-bucket ΔES Score Gap calibration method with sample floor + Cohen's-d collapse verdict scaffolding"
    - "assign_zone dispatch handles the 4 new MetricIds via the existing ZONE_REGISTRY lookup"
  artifacts:
    - path: ".claude/skills/benchmarks/SKILL.md"
      provides: "§3.4.4 Per-bucket ΔES Score Gap (Section 2) subchapter"
      contains: "3.4.4"
    - path: "app/services/endgame_zones.py"
      provides: "MetricId literal + ZONE_REGISTRY entries for the 4 per-bucket ΔES metrics"
      contains: "section2_score_gap_conv"
    - path: "scripts/gen_endgame_zones_ts.py"
      provides: "Codegen emits 8 new SECTION2_SCORE_GAP_*_NEUTRAL_MIN/MAX exports"
      contains: "SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "Regenerated TS constants consumed by Plan 03 frontend cards"
      contains: "SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN"
  key_links:
    - from: "scripts/gen_endgame_zones_ts.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "codegen script writes TS constants"
      pattern: "SECTION2_SCORE_GAP_(CONV|PARITY|RECOV|SKILL)_NEUTRAL_(MIN|MAX)"
    - from: "app/services/endgame_zones.py"
      to: "scripts/gen_endgame_zones_ts.py"
      via: "Python import of ZONE_REGISTRY for spec aliases"
      pattern: "ZONE_REGISTRY\\[\"section2_score_gap_"
---

<!--
SEC2-ΔES-03 is drafted from .planning/milestones/v1.17-ROADMAP.md §Phase 87.2 success criteria item #3.
REQUIREMENTS.md does not yet carry SEC2-ΔES-NN IDs — a follow-on doc task (in Plan 04) appends them.
Plan 01 lands the registry + codegen contract that Plan 03 (frontend cards) imports.
-->

<objective>
Add 4 new per-bucket ΔES Score Gap MetricIds (`section2_score_gap_{conv,parity,recov,skill}`) to the zones registry per D-02 option (a), extend the codegen script to emit their TS neutral-band constants, regenerate `frontend/src/generated/endgameZones.ts`, and document the calibration method in `.claude/skills/benchmarks/SKILL.md §3.4.4`. This plan publishes the contract Plan 03 consumes (TS constant names) and Plan 02 + Plan 04 dispatch through (`assign_zone("section2_score_gap_conv", value)`).

Purpose: unlock parallel work on backend math (Plan 02), frontend cards (Plan 03), and LLM payload (Plan 04) by landing the registry + codegen contract in Wave 1. Placeholder bands `(-0.05, 0.05)` mirror Phase 87.1's `endgame_type_achievable_score_gap` starting point; benchmark calibration tightens them later without renaming.

Output: 4 new MetricId literals + 4 ZONE_REGISTRY entries, 8 new TS exports, regenerated `endgameZones.ts`, SKILL.md §3.4.4 subchapter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-01-SUMMARY.md

<interfaces>
<!-- Existing contracts this plan consumes -->

From app/services/endgame_zones.py (pre-edit, Phase 87.1 precedent):
```
MetricId = Literal[
    "score_gap",
    "achievable_score_gap",
    "endgame_type_achievable_score_gap",   # Phase 87.1
    ...
]
ZONE_REGISTRY["endgame_type_achievable_score_gap"] = ZoneSpec(
    typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better",
)
def assign_zone(metric_id: MetricId, value: float) -> Literal["weak","typical","strong"]
```

<!-- New contracts produced by this plan, consumed by Plans 02, 03, 04 -->

From app/services/endgame_zones.py (post-edit):
```
MetricId Literal gains:
    "section2_score_gap_conv",
    "section2_score_gap_parity",
    "section2_score_gap_recov",
    "section2_score_gap_skill",

ZONE_REGISTRY gains 4 entries each shaped:
    ZoneSpec(typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better")
```

From frontend/src/generated/endgameZones.ts (post-codegen):
```
export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN   = -0.05;
export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MAX   =  0.05;
export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MIN = -0.05;
export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MAX =  0.05;
export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MIN  = -0.05;
export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MAX  =  0.05;
export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN  = -0.05;
export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX  =  0.05;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MetricId + ZONE_REGISTRY in endgame_zones.py with 4 per-bucket ΔES entries</name>
  <files>app/services/endgame_zones.py</files>
  <read_first>
    - app/services/endgame_zones.py (current MetricId Literal at line 30, ZoneSpec dataclass, ZONE_REGISTRY at line 140 — note the existing `endgame_type_achievable_score_gap` entry as the direct analog)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"app/services/endgame_zones.py" lines 140-200 (exact MetricId + ZONE_REGISTRY deltas)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-02, §D-07
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Zone-Registry Shape (recommendation: option (a))
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-01-PLAN.md Task 1 (Phase 87.1 analog — same shape)
  </read_first>
  <action>
    Per D-02 option (a) recommendation, D-07 dual-label rule, and PATTERNS deltas:
    (1) Add 4 new literals to the `MetricId` Literal, placed adjacent to the existing `"endgame_type_achievable_score_gap"` entry: `"section2_score_gap_conv"`, `"section2_score_gap_parity"`, `"section2_score_gap_recov"`, `"section2_score_gap_skill"`. Precede the cluster with the comment block from PATTERNS — cite Phase 87.2, D-02, D-07; note that user-facing labels are "Conversion Score Gap" / "Parity Score Gap" / "Recovery Score Gap" / "Skill Score Gap" (card row) and "Section 2 Score Gap" (glossary umbrella), while the internal snake_case form preserves grep-ability with the achievable_score_gap family.
    (2) Add 4 sibling entries to `ZONE_REGISTRY` directly below the existing `"endgame_type_achievable_score_gap"` ZoneSpec. Each entry: `ZoneSpec(typical_lower=-0.05, typical_upper=0.05, direction="higher_is_better")`. Comment block on the cluster: cite Phase 87.2, D-02, and note that placeholder ±5pp bands match the achievable_score_gap family until /benchmarks §3.4.4 Cohen's-d-collapse calibration in this same plan produces real per-bucket values.
    (3) Do NOT extend `BucketedMetricId` or `BUCKETED_ZONE_REGISTRY` (per RESEARCH §Zone-Registry Shape — option (a) avoids the new bucket-dispatch shape that would be required for option (b)). Add a one-line comment near `BucketedMetricId` noting that Section 2 per-bucket ΔES uses 4 scalar MetricIds, not a bucket-dispatched parent, because the existing dispatch shape is class-keyed (`PER_CLASS_GAUGE_ZONES`), not eval-entry-bucket-keyed.
    (4) Do NOT update `PER_CLASS_GAUGE_ZONES` — these new metrics are not per-endgame-class (rook / minor_piece / pawn / queen / mixed / pawnless); they are per-eval-entry-bucket (conv / parity / recov / skill). Each metric gets its own scalar ZoneSpec, dispatched via `ZONE_REGISTRY` only.
    (5) Verify `assign_zone(metric_id, value)` at the existing dispatch site flows the 4 new MetricIds through the standard `ZONE_REGISTRY[metric_id]` lookup — no helper code change required beyond the Literal extension.
    (6) `uv run ty check app/services/endgame_zones.py` — must exit 0. The 4 new Literal members are ty-safe additions; ZoneSpec is a frozen dataclass with all-positional fields.
  </action>
  <verify>
    <automated>uv run ty check app/services/endgame_zones.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"section2_score_gap_conv"' app/services/endgame_zones.py` returns >= 2 (MetricId literal + ZONE_REGISTRY key)
    - `grep -c '"section2_score_gap_parity"' app/services/endgame_zones.py` returns >= 2
    - `grep -c '"section2_score_gap_recov"' app/services/endgame_zones.py` returns >= 2
    - `grep -c '"section2_score_gap_skill"' app/services/endgame_zones.py` returns >= 2
    - `uv run ty check app/services/endgame_zones.py` exits 0
    - `uv run python -c "from app.services.endgame_zones import ZONE_REGISTRY, assign_zone; assert 'section2_score_gap_conv' in ZONE_REGISTRY; assert ZONE_REGISTRY['section2_score_gap_conv'].direction == 'higher_is_better'; assert assign_zone('section2_score_gap_conv', 0.0) == 'typical'"` exits 0
  </acceptance_criteria>
  <done>
    4 new MetricId literals present; 4 new ZONE_REGISTRY entries present with placeholder ±5pp bands and `higher_is_better` direction; `assign_zone` dispatches via the existing lookup; ty clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend gen_endgame_zones_ts.py and regenerate endgameZones.ts</name>
  <files>scripts/gen_endgame_zones_ts.py, frontend/src/generated/endgameZones.ts</files>
  <read_first>
    - scripts/gen_endgame_zones_ts.py (current `_render` function, the existing `_ENDGAME_TYPE_SCORE_GAP_SPEC` alias, and the ACHIEVABLE_SCORE_GAP / ENDGAME_TYPE_SCORE_GAP emission lines — these are the direct templates)
    - frontend/src/generated/endgameZones.ts (current export list — confirm placement convention; the new 8 exports slot after the existing ENDGAME_TYPE_SCORE_GAP block)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"scripts/gen_endgame_zones_ts.py" lines 396-422 (exact 4 spec aliases + 8 emit lines)
    - .planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-01-PLAN.md Task 2 (Phase 87.1 codegen extension shape — direct precedent)
  </read_first>
  <action>
    Per PATTERNS deltas:
    (1) In `_render` (the function that builds the TS file content), add 4 local spec aliases immediately after the existing `_ENDGAME_TYPE_SCORE_GAP_SPEC` alias:
        - `_SECTION2_SCORE_GAP_CONV_SPEC   = ZONE_REGISTRY["section2_score_gap_conv"]`
        - `_SECTION2_SCORE_GAP_PARITY_SPEC = ZONE_REGISTRY["section2_score_gap_parity"]`
        - `_SECTION2_SCORE_GAP_RECOV_SPEC  = ZONE_REGISTRY["section2_score_gap_recov"]`
        - `_SECTION2_SCORE_GAP_SKILL_SPEC  = ZONE_REGISTRY["section2_score_gap_skill"]`
    (2) Append 8 new export lines to the rendered TS output immediately after the existing `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MAX` emit:
        - `export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN   = {_SECTION2_SCORE_GAP_CONV_SPEC.typical_lower};`
        - `export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MAX   = {_SECTION2_SCORE_GAP_CONV_SPEC.typical_upper};`
        - `export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MIN = {_SECTION2_SCORE_GAP_PARITY_SPEC.typical_lower};`
        - `export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MAX = {_SECTION2_SCORE_GAP_PARITY_SPEC.typical_upper};`
        - `export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MIN  = {_SECTION2_SCORE_GAP_RECOV_SPEC.typical_lower};`
        - `export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MAX  = {_SECTION2_SCORE_GAP_RECOV_SPEC.typical_upper};`
        - `export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN  = {_SECTION2_SCORE_GAP_SKILL_SPEC.typical_lower};`
        - `export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX  = {_SECTION2_SCORE_GAP_SKILL_SPEC.typical_upper};`
        Precede the new block with a single-line code comment `// Phase 87.2 (D-02): per-bucket Section 2 ΔES Score Gap neutral bands.`
    (3) Run `uv run python scripts/gen_endgame_zones_ts.py` to regenerate `frontend/src/generated/endgameZones.ts`. Verify the file is updated and contains all 8 new exports.
    (4) Run `uv run python scripts/gen_endgame_zones_ts.py --check` — must exit 0 (drift gate green after regeneration).
    (5) Run `cd frontend && npm run build` — must succeed. tsc fails if the regenerated file is malformed; this confirms the codegen output type-checks against the TS consumers.
    (6) Do NOT hand-edit `frontend/src/generated/endgameZones.ts` — it is codegen output per CLAUDE.md. Per RESEARCH §Anti-Patterns, always regenerate via the script.
    (7) If the codegen script also emits a typed wrapper (e.g. an EndgameZones object literal), follow the existing pattern for that wrapper; otherwise the bare `export const` lines suffice (mirrors the existing ACHIEVABLE_SCORE_GAP / ENDGAME_TYPE_SCORE_GAP emissions).
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py --check &amp;&amp; grep -c 'SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/gen_endgame_zones_ts.py --check` exits 0 (drift gate green)
    - `grep -c 'export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_CONV_NEUTRAL_MAX' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_PARITY_NEUTRAL_MAX' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_RECOV_NEUTRAL_MAX' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN' frontend/src/generated/endgameZones.ts` returns 1
    - `grep -c 'export const SECTION2_SCORE_GAP_SKILL_NEUTRAL_MAX' frontend/src/generated/endgameZones.ts` returns 1
    - `cd frontend &amp;&amp; npm run build` exits 0
  </acceptance_criteria>
  <done>
    Codegen script emits 8 new constants from the 4 ZONE_REGISTRY entries; regenerated TS file commits cleanly; drift check passes; frontend build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add §3.4.4 Per-bucket ΔES Score Gap subchapter to benchmarks SKILL.md</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md §3.4.2 (Per-span Score Gap by Endgame Type — the direct structural template added by Phase 87.1)
    - .claude/skills/benchmarks/SKILL.md §3.4.1 if present (per-class Conv/Recov subchapter — confirms insertion convention; §3.4.4 sits after §3.4.3 if present, otherwise immediately after §3.4.2)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §/benchmarks Cohen's-d collapse expectation (sigmoid asymmetry rationale for expected per-bucket divergence)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-02 (Cohen's-d collapse verdict drives the placeholder→real band update)
    - app/services/endgame_service.py:196 (`_classify_endgame_bucket` signature — entry-eval-bucket assignment per span)
  </read_first>
  <action>
    Append `#### 3.4.4 Per-bucket ΔES Score Gap (Section 2 — Phase 87.2)` to `.claude/skills/benchmarks/SKILL.md` directly after the §3.4.2 subchapter (or §3.4.3 if present), using the §3.4.2 structural template:
    (1) Heading: `#### 3.4.4 Per-bucket ΔES Score Gap (Section 2 — Phase 87.2)`.
    (2) Question paragraph: how does the per-span ΔES Score Gap, partitioned by the **eval-entry bucket** `_classify_endgame_bucket(eval_cp, eval_mate, user_color)` (conversion / parity / recovery), distribute per user per bucket; what is the per-axis (TC × ELO) Cohen's-d collapse verdict per bucket; what bands should land in `ZONE_REGISTRY["section2_score_gap_{conv,parity,recov,skill}"]`?
    (3) Per-user metric definition (per bucket): one row per qualifying span `(game_id, endgame_class, span_min_ply)` with ≥ ENDGAME_PLY_THRESHOLD (=6) plies; bucket assignment via `_classify_endgame_bucket` at the span entry eval (eval_cp ≥ +100 cp ⇒ conversion; eval ≤ −100 cp ⇒ recovery; else parity); `gap_span = exit_score − ES_entry` per Phase 87.1's `_compute_span_gap` (reused verbatim); `per_user_per_bucket_mean = mean(gap_span over spans in that bucket)`. The Skill aggregate is the equal-weighted mean of the three bucket means (D-01), with buckets below the sample floor dropped from the average (denominator → 2 or 1).
    (4) Sample floor: ≥20 qualifying spans per user per bucket per cell (mirror §3.4.2). For the Skill aggregate, require ≥10 spans per active bucket (matches `CONFIDENCE_MIN_N`) so the equal-weighted mean is computed on stable component means.
    (5) Cohen's-d collapse verdict per axis (TC, ELO) — the 3-level pattern used by §3.4.2. Document the decision rule:
        - If both axes collapse for a bucket, set `ZONE_REGISTRY["section2_score_gap_<bucket>"]` to a single pooled p25/p75 band.
        - If the ELO axis "keeps separate" for a bucket, decide whether to stratify (this phase keeps the scalar MetricId — stratification is deferred to a follow-on phase).
        - **Sigmoid-bias expectation** (per RESEARCH §/benchmarks Cohen's-d collapse expectation): Conv-bucket likely skews negative (ES_entry ≈ 0.6, limited upside); Recov-bucket likely skews positive (ES_entry ≈ 0.4, limited downside); Parity ≈ symmetric. Skill should wash out. If observed verdicts match this prediction, divergent per-bucket bands are likely the right output.
    (6) Output instructions: update the 4 placeholder ZoneSpec entries in `app/services/endgame_zones.py` with calibrated `(typical_lower, typical_upper)` tuples; run `uv run python scripts/gen_endgame_zones_ts.py` and commit the regenerated `endgameZones.ts`. Per memory `feedback_zone_band_judgement.md`: tighten the band when small effects are meaningful, even if Cohen's-d straddles the collapse threshold.
    (7) Embed the SQL shape template from §3.4.2 adapted for per-bucket partitioning: same `WITH selected_users / spans / spans_with_next / gap_rows` CTE chain, with an added `bucket` column derived from the span entry eval inside the `gap_rows` CTE, and the final `per_user_bucket` aggregation grouped by `(user_id, bucket)`. Equal-footing opponent filter (`abs(opp_rating - user_rating) <= 100`) preserved per memory feedback_260503-fef. Reuses the existing `flawchess-benchmark-db` MCP server (port 5433) — no schema changes required (per RESEARCH: Phase 87.1 already populated the per-span ΔES rows).
    (8) Note: actually running the benchmark query is OUT OF SCOPE for this task — the SKILL.md update documents the method so a future calibration session can produce real bands. Placeholder bands from Task 1 remain in place until then; the codegen drift gate from Task 2 ensures any band update flows through to `endgameZones.ts` automatically.
  </action>
  <verify>
    <automated>grep -c '#### 3.4.4 Per-bucket ΔES Score Gap' .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#### 3.4.4 Per-bucket ΔES Score Gap' .claude/skills/benchmarks/SKILL.md` returns 1
    - `grep -c 'section2_score_gap_conv' .claude/skills/benchmarks/SKILL.md` returns >= 1 (registry key documented in the output-instructions section)
    - `grep -c '_classify_endgame_bucket' .claude/skills/benchmarks/SKILL.md` returns >= 1 (bucket-assignment function referenced)
    - `grep -c 'gap_span' .claude/skills/benchmarks/SKILL.md` returns >= 2 (definition + SQL CTE reference; matches §3.4.2 pattern)
    - `grep -c 'equal-weighted' .claude/skills/benchmarks/SKILL.md` returns >= 1 (Skill aggregator method recorded, per D-01)
    - `grep -ci 'cohen' .claude/skills/benchmarks/SKILL.md | head -1` returns >= 2 (collapse verdict rule appears in §3.4.4 alongside the §3.4.2 reference)
  </acceptance_criteria>
  <done>
    §3.4.4 subchapter present with question, per-user-per-bucket metric definition, sample floor, Cohen's-d collapse verdict instructions, sigmoid-bias expectation, output instructions referencing the 4 ZONE_REGISTRY keys, and a SQL CTE template adapted from §3.4.2. Calibration run itself is deferred.
  </done>
</task>

</tasks>

<threat_model>
none — purely additive change to backend zones registry, codegen script, and a project-local skill markdown document. No new attack surface; no new user input flows; no authentication paths touched. The codegen script reads from a Python registry and writes a TypeScript file in the build pipeline only.
</threat_model>

<verification>
- `uv run ty check app/services/endgame_zones.py` exits 0
- `uv run python scripts/gen_endgame_zones_ts.py --check` exits 0
- `cd frontend && npm run build` succeeds
- `grep -c 'section2_score_gap_' app/services/endgame_zones.py` returns >= 8 (4 MetricId literals + 4 ZONE_REGISTRY keys, each grep-able twice)
- `grep -c 'SECTION2_SCORE_GAP_' frontend/src/generated/endgameZones.ts` returns >= 8 (4 buckets × MIN+MAX)
- `grep -c '#### 3.4.4' .claude/skills/benchmarks/SKILL.md` returns 1
</verification>

<success_criteria>
SEC2-ΔES-03 satisfied: 4 per-bucket ΔES MetricIds live in ZONE_REGISTRY with placeholder bands and `higher_is_better` direction; codegen emits the 8 corresponding TS exports; `endgameZones.ts` regenerated cleanly; /benchmarks §3.4.4 calibration method documented for a future actuals pass; registry surface ready for Plan 03 (TS imports) and Plan 04 (`assign_zone` dispatch).
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-01-SUMMARY.md`.
</output>
