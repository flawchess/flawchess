---
phase: 87.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_service.py
  - app/services/score_confidence.py
  - app/schemas/endgames.py
  - tests/test_endgame_service.py
  - tests/services/test_score_confidence.py
autonomous: true
requirements:
  - SEC2-ΔES-02   # 20 new schema fields + per-bucket math + equal-weighted Skill aggregator
  - SEC2-ΔES-04   # Mirror-bucket plumbing retirement (backend half)
  - SEC2-ΔES-07   # Tests cover per-bucket math + Skill invariants + cohort-empty fallback (backend half)
tags:
  - backend
  - endgame
  - schema
  - service
  - math
must_haves:
  truths:
    - "_aggregate_endgame_stats accumulates a gaps_by_bucket dict alongside the existing gaps_by_class and returns both"
    - "_compute_score_gap_material calls compute_paired_difference_test once per bucket (conversion, parity, recovery)"
    - "Skill ΔES = equal-weighted mean of active bucket means with n>=CONFIDENCE_MIN_N (D-01); active=0 → null; active<3 → denominator drops"
    - "ScoreGapMaterialResponse carries 20 new section2_score_gap_{conv,parity,recov,skill}_{mean,n,p_value,ci_low,ci_high} fields"
    - "MaterialRow loses 5 fields (opponent_score, opponent_games, diff_p_value, diff_ci_low, diff_ci_high); ScoreGapMaterialResponse loses 5 fields (skill, opp_skill, skill_diff_p_value, skill_diff_ci_low, skill_diff_ci_high)"
    - "compute_skill_diff_test and compute_per_bucket_diff_test deleted from score_confidence.py with their dedicated test blocks"
    - "_MIRROR_BUCKET and _compute_skill_wire deleted from endgame_service.py; _build_material_rows slimmed"
    - "Sign convention preserved: positive section2_score_gap_*_mean = above Stockfish baseline (matches higher_is_better)"
  artifacts:
    - path: "app/services/endgame_service.py"
      provides: "gaps_by_bucket accumulator + per-bucket paired-z calls + equal-weighted Skill aggregator; mirror-bucket plumbing deleted"
      contains: "gaps_by_bucket"
    - path: "app/services/score_confidence.py"
      provides: "compute_skill_diff_test + compute_per_bucket_diff_test deleted; compute_paired_difference_test unchanged"
      contains: "compute_paired_difference_test"
    - path: "app/schemas/endgames.py"
      provides: "20 new section2_score_gap_* fields on ScoreGapMaterialResponse; 5 deletions on MaterialRow + 5 on ScoreGapMaterialResponse"
      contains: "section2_score_gap_conv_mean"
    - path: "tests/test_endgame_service.py"
      provides: "Per-bucket ΔES partitioning + Skill equal-weighted aggregator + cohort-empty + mirror-grep tests"
      contains: "section2_score_gap"
    - path: "tests/services/test_score_confidence.py"
      provides: "Deleted compute_skill_diff_test + compute_per_bucket_diff_test test blocks"
      contains: "compute_paired_difference_test"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/services/score_confidence.py:338"
      via: "compute_paired_difference_test called 3 times (once per bucket) + Skill aggregator over the 3 results"
      pattern: "compute_paired_difference_test\\("
    - from: "app/services/endgame_service.py"
      to: "app/services/endgame_service.py:196"
      via: "_classify_endgame_bucket called per span at the existing line 437 site; bucket result drives gaps_by_bucket key"
      pattern: "_classify_endgame_bucket"
    - from: "app/schemas/endgames.py"
      to: "ScoreGapMaterialResponse construction in _compute_score_gap_material"
      via: "20 new fields populated from per_bucket + skill_* locals"
      pattern: "section2_score_gap_(conv|parity|recov|skill)_(mean|n|p_value|ci_low|ci_high)"
---

<!--
SEC2-ΔES-02, SEC2-ΔES-04, SEC2-ΔES-07 drafted from .planning/milestones/v1.17-ROADMAP.md §Phase 87.2 success criteria items #2, #4, #7.
This plan is the backend-only half of the deletion sweep: math + schema + service + dedupe tests.
Plan 03 picks up the frontend half (cards + TS mirror + lib symbols).
Plan 04 picks up the LLM payload half (additive findings + prompt v31).
-->

<objective>
Land the backend per-bucket ΔES math + equal-weighted Skill aggregator + schema additions, and retire the rate-based mirror-bucket plumbing from the math + schema layers. Reuses the existing per-span gap series (`gaps_by_class` from Phase 87.1) by adding a parallel `gaps_by_bucket` accumulator in the same iteration. The new Skill aggregator implements D-01: equal-weighted mean of three independent paired-z helper outputs, with CI propagation via variance-of-sum / n_active² (Open Question §3 option A).

Purpose: produces the wire shape Plan 03 (frontend) reads from and Plan 04 (LLM) emits findings from. No new statistical primitive — `compute_paired_difference_test` is the single math helper. Deletion sweep removes 2 score-confidence helpers, 2 endgame_service internals, 5 + 5 schema fields, and the dedicated unit tests for the retired helpers. Verified by cross-phase grep (RESEARCH §Cross-Phase Grep Verification): no Section 3 or other Phase 87.1 consumer of the deleted symbols.

Output: 20 new schema fields on `ScoreGapMaterialResponse`, 10 deleted schema fields (5 on `MaterialRow`, 5 on `ScoreGapMaterialResponse`), `_compute_skill_wire` + `_MIRROR_BUCKET` removed, 2 retired helpers + their tests removed, per-bucket + Skill math wired through `_aggregate_endgame_stats` + `_compute_score_gap_material`, full unit + integration test coverage, ty + ruff clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md
@.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-VALIDATION.md
@.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-02-SUMMARY.md

<interfaces>
<!-- Existing contracts this plan consumes -->

From app/services/score_confidence.py:338 (UNCHANGED — sole math helper):
```
def compute_paired_difference_test(
    diffs: Sequence[float],
) -> tuple[float, float | None, float | None, float | None]:
    """Returns (mean_d, p_value, ci_low, ci_high).
    Behavior:
      n=0      -> (0.0, None, None, None)
      n=1      -> (mean, None, None, None)
      n>=2, var=0 -> handled internally
      n>=CONFIDENCE_MIN_N (=10) -> p_value populated; ci_low/ci_high populated at n>=2
    """

CONFIDENCE_MIN_N: int = 10
CI_Z_95: float = 1.959963984540054  # used for SE-from-CI reverse engineering in Skill propagation
```

From app/services/endgame_service.py:196 (Phase 78/REFAC-02, UNCHANGED):
```
def _classify_endgame_bucket(
    eval_cp: int | None,
    eval_mate: int | None,
    user_color: str,
) -> MaterialBucket:  # Literal["conversion","parity","recovery"]
    """Maps span entry eval to {conversion, parity, recovery} with sign flip per user_color."""
```

From app/services/endgame_service.py:260 (Phase 87.1, UNCHANGED):
```
def _compute_span_gap(...) -> float | None:
    """Returns exit_score − ES_entry per span; None for NULL-eval spans."""
```

<!-- New contracts produced by this plan, consumed by Plans 03 + 04 -->

From app/schemas/endgames.py ScoreGapMaterialResponse (post-edit, 20 new fields, 5 deletions):
```
# DELETED (D-05): skill, opp_skill, skill_diff_p_value, skill_diff_ci_low, skill_diff_ci_high

# Phase 87.2 (D-06): per-bucket Score Gap fields. Flat shape mirrors Phase 87.1's
# type_achievable_score_gap_* on EndgameCategoryStats. Defaults None for backward compat.
section2_score_gap_conv_mean:    float | None = None
section2_score_gap_conv_n:       int   | None = None
section2_score_gap_conv_p_value: float | None = None
section2_score_gap_conv_ci_low:  float | None = None
section2_score_gap_conv_ci_high: float | None = None
# ...same 5-field cluster for parity, recov, skill (20 fields total)
```

From app/schemas/endgames.py MaterialRow (post-edit, 5 deletions):
```
# DELETED (D-05): opponent_score, opponent_games, diff_p_value, diff_ci_low, diff_ci_high
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration — add 20 ScoreGapMaterialResponse fields, delete 5+5 mirror-bucket fields, update fixtures</name>
  <files>app/schemas/endgames.py, tests/test_endgame_service.py</files>
  <read_first>
    - app/schemas/endgames.py lines 280-414 (MaterialBucket, MaterialRow, ScoreGapMaterialResponse — the 3 schema classes touched)
    - app/schemas/endgames.py:62-105 (EndgameCategoryStats and the existing Phase 87.1 `type_achievable_score_gap_*` 5-field cluster — the direct shape template)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"app/schemas/endgames.py" lines 335-393 (exact 20 field additions + 5+5 deletions)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Schema Shape (flat-vs-nested recommendation; flat selected)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-05, §D-06
    - tests/test_endgame_service.py (grep for ScoreGapMaterialResponse constructor call sites + MaterialRow fixtures using `opponent_score`/`opponent_games`/`diff_*`/`skill`/`opp_skill`/`skill_diff_*`)
  </read_first>
  <behavior>
    - Test: `ScoreGapMaterialResponse(...)` constructor accepts zero new-field arguments and defaults all 20 new fields to None (backward compat).
    - Test: every old fixture that previously passed `opponent_score=...`, `opponent_games=...`, `diff_p_value=...`, `diff_ci_low=...`, `diff_ci_high=...` to `MaterialRow(...)` is updated to drop those kwargs.
    - Test: every old fixture that previously passed `skill=...`, `opp_skill=...`, `skill_diff_p_value=...`, `skill_diff_ci_low=...`, `skill_diff_ci_high=...` to `ScoreGapMaterialResponse(...)` is updated to drop those kwargs.
    - Test: setting `section2_score_gap_conv_mean=0.05` and `section2_score_gap_conv_n=42` on a `ScoreGapMaterialResponse` instance round-trips through `.model_dump()` and `.model_validate()`.
    - Test: passing a deleted field (e.g. `opponent_score=0.5`) to `MaterialRow(...)` raises a Pydantic `ValidationError` (extra-field-forbidden semantics if the model uses `model_config = ConfigDict(extra='forbid')`, otherwise field-not-defined). Adjust assertion to match the model's `extra` policy.
  </behavior>
  <action>
    Per PATTERNS deltas + D-05/D-06 + RESEARCH §Schema Shape (flat recommended):
    (1) In `app/schemas/endgames.py`:
        - On `MaterialRow` (around lines 302-312): DELETE 5 fields: `opponent_score`, `opponent_games`, `diff_p_value`, `diff_ci_low`, `diff_ci_high`. Keep all other fields (gauge value, score, win_pct, draw_pct, loss_pct, games count, bucket label).
        - On `ScoreGapMaterialResponse` (around lines 398-414): DELETE 5 fields: `skill`, `opp_skill`, `skill_diff_p_value`, `skill_diff_ci_low`, `skill_diff_ci_high`. ADD 20 new fields per PATTERNS Delta 3 — 4 clusters of 5 fields each (`section2_score_gap_{conv,parity,recov,skill}_{mean,n,p_value,ci_low,ci_high}`), all `float | None = None` except `_n: int | None = None`. Precede the new cluster with the comment block from PATTERNS citing Phase 87.2 + D-01 + D-06 + dual-label rationale (internal `section2_score_gap_*` form preserves grep-ability with the achievable_score_gap family; user-facing labels live in Plan 03).
    (2) ty + ruff cleanliness:
        - All new fields use `float | None = None` and `int | None = None` (no `Optional`, no untyped fields).
        - If `model_config = ConfigDict(extra='forbid')` is set on either model, no changes needed — extras already rejected. If not set, the field-deletion still produces a `ValidationError` when an unknown kwarg is passed if the model has `extra='forbid'`, otherwise the kwarg is silently ignored; either way, fixture cleanup in step (3) ensures no test passes a deleted kwarg.
    (3) Test fixture cleanup in `tests/test_endgame_service.py`:
        - `grep -n "opponent_score\|opponent_games\|diff_p_value\|diff_ci_low\|diff_ci_high\|opp_skill\|skill_diff_" tests/test_endgame_service.py` to enumerate every fixture site.
        - For each site that constructs `MaterialRow(...)`: drop the 5 deleted kwargs.
        - For each site that constructs `ScoreGapMaterialResponse(...)`: drop the 5 deleted kwargs.
        - For the `MaterialRow.skill` field on `ScoreGapMaterialResponse` (the deleted `skill` field, NOT the new `section2_score_gap_skill_*` cluster): trace any test that asserted on `.skill` — most likely deleted alongside the field. If tests asserted on `_compute_skill_wire`'s output, those tests are wholesale deleted (see Task 3).
    (4) Add a single new positive fixture test asserting the 20 new fields exist with defaults and accept a sample payload. Place it near the existing `EndgameCategoryStats` fixture tests (the Phase 87.1 analog).
    (5) `uv run ty check app/schemas/endgames.py tests/test_endgame_service.py` — must exit 0. The 20 new optional fields with `= None` defaults are ty-safe. After deletions, ty surfaces any orphan references — fix in place (these become the test fixture updates above).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -x &amp;&amp; uv run ty check app/schemas/endgames.py tests/test_endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'section2_score_gap_conv_mean' app/schemas/endgames.py` returns 1
    - `grep -c 'section2_score_gap_' app/schemas/endgames.py | head -1` returns >= 20 (4 buckets × 5 fields)
    - `grep -c 'opponent_score' app/schemas/endgames.py` returns 0 (deleted)
    - `grep -c 'opponent_games' app/schemas/endgames.py` returns 0
    - `grep -c 'skill_diff_p_value' app/schemas/endgames.py` returns 0
    - `grep -c 'opp_skill' app/schemas/endgames.py` returns 0
    - `uv run pytest tests/test_endgame_service.py -x` exits 0 (existing fixtures updated, new fixture passes)
    - `uv run ty check app/schemas/endgames.py tests/test_endgame_service.py` exits 0
    - `grep -cE 'opponent_score|opponent_games|skill_diff_|opp_skill' tests/test_endgame_service.py` returns 0 (every fixture site cleaned)
  </acceptance_criteria>
  <done>
    20 new fields live on `ScoreGapMaterialResponse` with sensible defaults; 5 deletions on `MaterialRow` + 5 on `ScoreGapMaterialResponse`; every test fixture updated; ty clean; pytest green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend math — gaps_by_bucket accumulator, per-bucket paired-z, equal-weighted Skill aggregator with CI propagation</name>
  <files>app/services/endgame_service.py, tests/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py lines 196-470 (`_classify_endgame_bucket`, `_compute_span_gap`, the per-span row loop in `_aggregate_endgame_stats` where bucket is already classified at line 437 and Phase 87.1's `gaps_by_class[endgame_class].append(gap)` lands at line 470)
    - app/services/endgame_service.py lines 921-1030 (`_aggregate_bucket_counts`, `_compute_skill_wire` at 993, `_MIRROR_BUCKET` at 1060 — all on the deletion list)
    - app/services/endgame_service.py lines 1067-1230 (`_build_material_rows`, `_compute_score_gap_material` orchestration — the Section 2 builder that consumes `gaps_by_bucket` and constructs `ScoreGapMaterialResponse`)
    - app/services/score_confidence.py lines 1-90 (CONFIDENCE_MIN_N, CI_Z_95, shared internals; identify what's safe to delete vs keep)
    - app/services/score_confidence.py:338-410 (compute_paired_difference_test signature + n-gate behavior table)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-PATTERNS.md §"app/services/endgame_service.py" lines 40-118 (3 deltas: gaps_by_bucket accumulator + tupled return + Skill aggregator block)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Service-Layer Plumbing (full Skill CI propagation algorithm) + §Pitfall 1 + §Pitfall 2 + §Pitfall 6 + §Open Questions §3 (variance-of-sum/n² Option A recommended)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-01, §D-05, §D-06
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-VALIDATION.md (per-bucket math + Skill aggregator test scope)
  </read_first>
  <behavior>
    - Test: `gaps_by_bucket` partition — a fixture with 3 spans (one per bucket, distinct `gap` values) populates `gaps_by_bucket["conversion"] = [g_c]`, `gaps_by_bucket["parity"] = [g_p]`, `gaps_by_bucket["recovery"] = [g_r]`.
    - Test: span-grain bucketing (per RESEARCH §Pitfall 1) — a single game with one Conv-bucket span and one Recov-bucket span contributes one span to each bucket cohort (NOT one game per bucket; the per-game `_aggregate_bucket_counts` reduction does NOT apply to ΔES).
    - Test: NULL-eval spans are excluded from `gaps_by_bucket` — span where both `entry_eval_cp` and `entry_eval_mate` are None does not appear in any bucket cohort.
    - Test: per-bucket paired-z calls — fixture with 15 Conv-bucket spans (all gap = +0.1) yields `section2_score_gap_conv_mean = 0.1`, `section2_score_gap_conv_n = 15`, `section2_score_gap_conv_p_value` populated (n >= 10), `ci_low` and `ci_high` populated (zero-variance case handled by helper).
    - Test: cohort-empty fallback — bucket with `n = 0` returns `section2_score_gap_<bucket>_mean = None` on the wire (NOT 0.0 from `compute_paired_difference_test`'s n=0 return); planner: gate via `if n == 0: mean_out = None else: mean_out = mean_from_helper` to avoid the helper's 0.0 default polluting the wire.
    - Test: Skill equal-weighted aggregator — fixture with 3 buckets of sizes {n_c=12, n_p=15, n_r=20} and means {0.10, 0.05, -0.05} yields `section2_score_gap_skill_mean = (0.10 + 0.05 + -0.05) / 3 = 0.0333...`. Assert to 1e-6.
    - Test: Skill denominator drop (D-01) — fixture with bucket sizes {n_c=12, n_p=3, n_r=20} (parity below `CONFIDENCE_MIN_N`=10) yields `section2_score_gap_skill_mean = (mean_c + mean_r) / 2`, with parity dropped. `section2_score_gap_skill_n = n_c + n_r`.
    - Test: Skill all-empty — fixture with all 3 buckets below CONFIDENCE_MIN_N yields `section2_score_gap_skill_mean = None`, `section2_score_gap_skill_n = 0`, p/ci = None.
    - Test: Skill CI propagation — fixture with bucket SEs {SE_c, SE_p, SE_r} (recovered from each bucket's CI via `(ci_high - ci_low) / (2 * CI_Z_95)`) yields `SE_skill = sqrt(SE_c**2 + SE_p**2 + SE_r**2) / n_active`. Assert `ci_low = mean - CI_Z_95 * SE_skill` and `ci_high = mean + CI_Z_95 * SE_skill` to 1e-6.
    - Test: Skill p_value — `p_value = 2 * (1 - normal_cdf(|mean_skill / SE_skill|))` or equivalent; populated when n_active >= 2 (CI exists for all active components by construction since n >= CONFIDENCE_MIN_N >= 2). None when n_active == 0.
    - Test: sign convention preserved — positive `section2_score_gap_conv_mean` means user converted advantages above the Stockfish baseline.
    - Test: existing Phase 87.1 `gaps_by_class` + `type_achievable_score_gap_*` outputs unaffected — the parallel accumulator does not perturb the per-class aggregation.
  </behavior>
  <action>
    Per PATTERNS deltas + D-01 + RESEARCH §Service-Layer Plumbing + Open Questions §3 Option A:
    (1) Imports at top of `app/services/endgame_service.py` (verify; add if missing):
        - `from collections import defaultdict`
        - `from app.schemas.endgames import MaterialBucket` (already present per RESEARCH; verify)
        - `from app.services.score_confidence import compute_paired_difference_test, CONFIDENCE_MIN_N, CI_Z_95`
        - `import math` (for `math.erfc` / `math.sqrt` used in Skill p-value propagation)
    (2) DELETE `_MIRROR_BUCKET` (lines 1060-1064) and `_compute_skill_wire` (lines 993-1030). Remove their imports and any private helpers used only by these two functions.
    (3) Inside `_aggregate_endgame_stats` (line 341+):
        - Above the existing `for row in rows:` loop, add `gaps_by_bucket: dict[MaterialBucket, list[float]] = defaultdict(list)` next to the existing `gaps_by_class` declaration. Comment: `# Phase 87.2 (D-01): per-bucket ΔES accumulator. Shares iteration with gaps_by_class; bucket already computed at line 437 for the rate-based counts.`
        - Inside the loop, immediately after the existing `gaps_by_class[endgame_class].append(gap)` line, add `gaps_by_bucket[bucket].append(gap)`. The `bucket` variable is already computed from the existing `_classify_endgame_bucket(...)` call earlier in the loop (verify line 437).
        - Change the function's return type from `list[EndgameCategoryStats]` to `tuple[list[EndgameCategoryStats], dict[MaterialBucket, list[float]]]`. Return `(categories, dict(gaps_by_bucket))` — convert the defaultdict to a plain dict to avoid downstream `defaultdict` surprises. Update the single internal call site in `_compute_score_gap_material` to unpack the tuple.
    (4) In `_compute_score_gap_material` (line 1124+):
        - Slim `_build_material_rows`: drop the `swap_bucket` lookup, the `opponent_score` derivation, the `compute_per_bucket_diff_test` call, and the 5 deleted `MaterialRow` fields. The Conv/Parity/Recov `MaterialRow` construction stays — still needed for gauge/games/WDL rendering in Plan 03.
        - Replace the deleted `_compute_skill_wire` block (lines 1198-1200 + the 5 `skill*` kwargs to `ScoreGapMaterialResponse(...)`) with the new per-bucket + Skill block per PATTERNS Delta 4:
          - For each `b in ("conversion", "parity", "recovery")`: capture `gaps = gaps_by_bucket.get(b, [])`; `n_b = len(gaps)`; `(mean_b, p_b, cilo_b, cihi_b) = compute_paired_difference_test(gaps)`. Gate `mean_b` to `None` if `n_b == 0` to avoid leaking the helper's 0.0 default on empty cohorts (per RESEARCH §Pitfall 2).
          - Compute the equal-weighted Skill mean: `active = [b for b in ("conversion","parity","recovery") if per_bucket_n[b] >= CONFIDENCE_MIN_N]`; `n_active = len(active)`.
          - If `n_active == 0`: `skill_mean = None`, `skill_p = skill_ci_low = skill_ci_high = None`, `skill_n = 0`.
          - Else: `skill_mean = sum(per_bucket[b][0] for b in active) / n_active`; `skill_n = sum(per_bucket_n[b] for b in active)`.
            - CI propagation (RESEARCH §Open Questions §3 Option A): for each `b in active`, recover `SE_b = (per_bucket[b][3] - per_bucket[b][2]) / (2 * CI_Z_95)` (helper guarantees ci_low/ci_high populated when n>=2; active buckets satisfy n >= CONFIDENCE_MIN_N >= 2).
            - `SE_skill = math.sqrt(sum(SE_b**2 for ...)) / n_active`.
            - `skill_ci_low = skill_mean - CI_Z_95 * SE_skill`; `skill_ci_high = skill_mean + CI_Z_95 * SE_skill`.
            - `skill_p = 2.0 * (1.0 - normal_cdf(abs(skill_mean / SE_skill)))` — implement via `math.erfc(abs(skill_mean / SE_skill) / math.sqrt(2)) / 1.0` if a `normal_cdf` helper is not already imported. Handle `SE_skill == 0` (degenerate case): set `skill_p = None`.
        - Construct `ScoreGapMaterialResponse(...)` with the 20 new fields populated from `per_bucket[<bucket>]` results and the Skill quartet (`skill_mean`, `skill_n`, `skill_p`, `skill_ci_low`, `skill_ci_high`). Pass `None` for the deleted `skill`/`opp_skill`/`skill_diff_*` — actually, just stop passing them entirely (the fields are gone from the schema).
        - Comment block on the new computation: `# Phase 87.2 (D-01/D-05/D-06): per-bucket ΔES + equal-weighted Skill aggregator. Replaces _compute_skill_wire composite Wald-z (D-05). Sign: exit_score - ES_entry, positive = user outperformed Stockfish baseline. Skill CI: variance-of-sum / n_active² propagation from 3 independent paired-z outputs (Open Q §3 option A).`
    (5) Tests in `tests/test_endgame_service.py`:
        - Add `test_per_bucket_gap_partition` — fixture with 3 spans in 3 distinct buckets; assert each appears in exactly one cohort.
        - Add `test_span_grain_bucketing_one_game_two_buckets` — fixture with 1 game containing 2 spans in different buckets; assert per-span (not per-game) attribution.
        - Add `test_per_bucket_null_eval_excluded` — fixture with NULL-eval span; assert no contribution to any bucket cohort.
        - Add `test_per_bucket_zero_cohort_returns_none_mean` — fixture with all spans in Conv bucket only; assert `section2_score_gap_parity_mean is None`, `section2_score_gap_recov_mean is None`, `_n = 0` on the empty buckets.
        - Add `test_per_bucket_full_cohort_paired_z` — fixture with 15 Conv-bucket spans all gap=+0.1 (zero variance) and 15 Recov-bucket spans all gap=-0.05; assert means + n populated; p/ci handled per helper's variance-0 path.
        - Add `test_skill_equal_weighted_mean` — fixture with 3 buckets, distinct sizes, distinct means; assert `skill_mean = (m_c + m_p + m_r) / 3` to 1e-6.
        - Add `test_skill_denominator_drop_below_floor` — fixture with parity n=3 (< CONFIDENCE_MIN_N); assert `skill_mean = (m_c + m_r) / 2`, `skill_n = n_c + n_r`.
        - Add `test_skill_all_below_floor_returns_none` — fixture with all 3 buckets n=5; assert `skill_mean = None`, `skill_p = None`, `skill_n = 0`.
        - Add `test_skill_ci_propagation` — fixture with 3 active buckets where each SE is recoverable; assert `SE_skill = sqrt(SE_c**2 + SE_p**2 + SE_r**2) / n_active` to 1e-6; assert ci_low/ci_high are symmetric around mean (±CI_Z_95 * SE_skill).
        - Add `test_sign_convention_preserved` — fixture with one transitory Conv-bucket span where ES_entry=0.6 and ES_exit=0.7; assert `section2_score_gap_conv_mean ≈ +0.1` (NOT -0.1).
        - Add `test_phase_87_1_outputs_unaffected` — fixture where 3 spans are also assigned to endgame classes; assert that `type_achievable_score_gap_mean` on `EndgameCategoryStats` matches the Phase 87.1 expected values (the parallel accumulator does not perturb per-class aggregation).
    (6) ty + ruff:
        - All new annotations use `float | None`, `int | None`, `dict[MaterialBucket, list[float]]`, `Sequence[float]`. No `Optional`, no untyped fields.
        - `math.erfc` and `math.sqrt` are unambiguously typed.
        - `# ty: ignore[arg-type]` permitted only at the helper-call boundary if ty surfaces a `Sequence[float]` vs `list[float]` covariance complaint — include a comment with the rule and reason.
    (7) Sentry: No new exceptional paths. `compute_paired_difference_test` handles `n=0`, `n=1`, zero-variance internally. The Skill aggregator's `SE_skill == 0` branch sets `skill_p = None` cleanly. No new `sentry_sdk.capture_exception` sites.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -x &amp;&amp; uv run ty check app/services/endgame_service.py tests/test_endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gaps_by_bucket' app/services/endgame_service.py` returns >= 3 (declaration + append site + return tuple unpack)
    - `grep -c 'compute_paired_difference_test' app/services/endgame_service.py` returns >= 4 (Phase 87.1 per-class + 3 new per-bucket calls; minus the deleted `_compute_skill_wire` call site)
    - `grep -c '_compute_skill_wire' app/services/endgame_service.py` returns 0 (deleted)
    - `grep -c '_MIRROR_BUCKET' app/services/endgame_service.py` returns 0 (deleted)
    - `grep -c 'section2_score_gap_conv_mean' app/services/endgame_service.py` returns >= 1 (ScoreGapMaterialResponse kwarg)
    - `grep -c 'section2_score_gap_skill_mean' app/services/endgame_service.py` returns >= 1
    - `grep -c 'CI_Z_95' app/services/endgame_service.py` returns >= 1 (SE-from-CI reverse engineering in Skill propagation)
    - `uv run pytest tests/test_endgame_service.py -x` exits 0 (all new tests green + existing tests still pass)
    - `uv run ty check app/services/endgame_service.py tests/test_endgame_service.py` exits 0
    - `uv run ruff check app/services/endgame_service.py` exits 0
    - Sign convention test passes: transitory Conv-bucket span with ES_entry=0.6, ES_exit=0.7 yields `section2_score_gap_conv_mean ≈ +0.1`
  </acceptance_criteria>
  <done>
    `gaps_by_bucket` accumulator wired alongside `gaps_by_class`; per-bucket paired-z called 3 times; equal-weighted Skill aggregator with variance-of-sum/n² CI propagation; mirror-bucket internals (`_MIRROR_BUCKET`, `_compute_skill_wire`) deleted; tests cover all D-11 backend-scope cases; ty + ruff clean; pytest green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete compute_skill_diff_test + compute_per_bucket_diff_test from score_confidence.py and their dedicated test blocks</name>
  <files>app/services/score_confidence.py, tests/services/test_score_confidence.py</files>
  <read_first>
    - app/services/score_confidence.py lines 1-90 (shared internals: `_SkillBucketRow`, `_BucketName`, `_headline_rate`, `_opp_headline_rate`, `_headline_rate_variance`, `_opp_headline_rate_variance` — RESEARCH notes "delete only if no other caller" — verify via grep before deletion)
    - app/services/score_confidence.py:408-535 (compute_skill_diff_test — D-05 deletion target)
    - app/services/score_confidence.py:538-679 (compute_per_bucket_diff_test — D-05 deletion target, cross-phase grep confirmed Section-2-only in RESEARCH §Cross-Phase Grep Verification)
    - tests/services/test_score_confidence.py lines 554-958 (dedicated test blocks for the two helpers; full deletion target — RESEARCH §Deletion Inventory)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-RESEARCH.md §Cross-Phase Grep Verification (confirms safe deletion)
    - .planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-CONTEXT.md §D-05
  </read_first>
  <action>
    Per D-05 + PATTERNS §"app/services/score_confidence.py" + RESEARCH §Cross-Phase Grep Verification:
    (1) Pre-deletion grep:
        - `grep -rn "compute_skill_diff_test\|compute_per_bucket_diff_test" app/ tests/ scripts/` — should match only the deletion targets in `score_confidence.py`, the now-deleted call sites in `endgame_service.py` (already removed in Task 2), and the dedicated test blocks. If any other consumer surfaces (other phases, scripts), STOP and surface to the user — do not delete a cross-phase-consumed helper.
        - Per RESEARCH §Cross-Phase Grep Verification, the previous grep at 2026-05-15 found only the expected sites. Re-verify in this task to catch any in-flight changes.
    (2) Delete `compute_skill_diff_test` from `score_confidence.py` (lines 408-535). Delete the immediately-preceding docstring/comment block if any.
    (3) Delete `compute_per_bucket_diff_test` from `score_confidence.py` (lines 538-679). Same docstring cleanup.
    (4) Re-grep the shared internals at lines 60-90 (`_SkillBucketRow`, `_BucketName`, `_headline_rate`, `_opp_headline_rate`, `_headline_rate_variance`, `_opp_headline_rate_variance`):
        - `grep -rn "_SkillBucketRow\|_BucketName\|_headline_rate\|_opp_headline_rate\|_headline_rate_variance\|_opp_headline_rate_variance" app/ tests/` after the two function deletions.
        - If zero hits remain, delete those private symbols as well. If any hit remains (e.g. accidentally used by a Phase 86 fixture), leave the symbol in place and add a `# TODO: orphan after Phase 87.2 — verify and delete in cleanup pass` comment.
    (5) Delete the test blocks for the two helpers in `tests/services/test_score_confidence.py` (RESEARCH-cited lines 554-958, but verify exact line ranges in the current file — they may shift). Grep `def test_compute_skill_diff_test\|def test_compute_per_bucket_diff_test\|class TestComputeSkillDiff\|class TestComputePerBucketDiff` to locate the blocks. Delete from start of the first matching def/class through the end of the last test function in the contiguous block.
    (6) Verify the remaining tests in `tests/services/test_score_confidence.py` still pass — `compute_paired_difference_test` is unchanged and its dedicated tests survive.
    (7) `uv run pytest tests/services/test_score_confidence.py -x` — must exit 0.
    (8) `uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py` — must exit 0. Any orphan import surfaces here.
    (9) `uv run ruff check app/services/score_confidence.py` — must exit 0. Ruff catches dead imports.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_score_confidence.py -x &amp;&amp; uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def compute_skill_diff_test' app/services/score_confidence.py` returns 0
    - `grep -c 'def compute_per_bucket_diff_test' app/services/score_confidence.py` returns 0
    - `grep -c 'def compute_paired_difference_test' app/services/score_confidence.py` returns 1 (preserved)
    - `grep -rn 'compute_skill_diff_test\|compute_per_bucket_diff_test' app/ tests/ scripts/` returns 0 active matches (only changelog / comment hits, if any, are acceptable)
    - `grep -c 'def test_compute_skill_diff_test\|def test_compute_per_bucket_diff_test' tests/services/test_score_confidence.py` returns 0
    - `uv run pytest tests/services/test_score_confidence.py -x` exits 0
    - `uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py` exits 0
    - `uv run ruff check app/services/score_confidence.py` exits 0
  </acceptance_criteria>
  <done>
    2 helpers deleted from `score_confidence.py` with dedicated test blocks; shared internals deleted if orphaned (or flagged); `compute_paired_difference_test` and its tests preserved; ty + ruff + pytest green.
  </done>
</task>

</tasks>

<threat_model>
none — purely server-side math + schema refactor on existing authenticated `/api/endgames/overview` route. No new user input, no new exceptional paths, no new auth surfaces. The new computation runs over data the user already owns (their own game positions). Deletions trim wire-schema fields that were never exposed externally beyond the frontend cards.
</threat_model>

<verification>
- `uv run pytest tests/test_endgame_service.py tests/services/test_score_confidence.py -x` exits 0
- `uv run ty check app/services/endgame_service.py app/services/score_confidence.py app/schemas/endgames.py tests/` exits 0
- `uv run ruff check app/services/endgame_service.py app/services/score_confidence.py app/schemas/endgames.py` exits 0
- `grep -c 'section2_score_gap_' app/schemas/endgames.py` >= 20 (4 buckets × 5 fields)
- `grep -c 'gaps_by_bucket' app/services/endgame_service.py` >= 3
- `grep -rn 'compute_skill_diff_test\|compute_per_bucket_diff_test\|_MIRROR_BUCKET\|_compute_skill_wire\|opponent_score\|opp_skill\|skill_diff_' app/` returns 0 active matches
- Sign convention enforced in tests (positive `section2_score_gap_*_mean` ⇒ user outperformed Stockfish)
- Skill equal-weighted invariant: 3-bucket fixture asserts `skill_mean = (m_c + m_p + m_r) / 3`
- Skill denominator drop: parity n=3 fixture asserts `skill_mean = (m_c + m_r) / 2`
</verification>

<success_criteria>
SEC2-ΔES-02, SEC2-ΔES-04 (backend half), SEC2-ΔES-07 (backend half) satisfied:
- SEC2-ΔES-02: 20 new fields populated via `compute_paired_difference_test` (3 buckets) + equal-weighted Skill aggregator (D-01) with variance-of-sum/n² CI propagation; sign convention `exit_score - ES_entry` preserved.
- SEC2-ΔES-04: `compute_skill_diff_test`, `compute_per_bucket_diff_test`, `_MIRROR_BUCKET`, `_compute_skill_wire`, 5 + 5 schema field deletions all complete; backend ty + ruff clean.
- SEC2-ΔES-07: backend test coverage on per-bucket math, span-grain bucketing, NULL-eval exclusion, Skill equal-weighted aggregator (full + drop + all-empty), Skill CI propagation, sign convention; Phase 87.1 outputs unaffected.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87.2-section-2-eval-based-score-gap-bullets/87.2-02-SUMMARY.md`.
</output>
