# Phase 87.2 Human UAT Checklist

Manual-only verification steps for the Section 2 eval-based Score Gap bullets. These checks cannot be automated (visual layout, DPR, touch targets, copy quality).

## From VALIDATION.md Manual-Only Verifications

### 1. 375px parity check

**Requirement:** SEC2-ΔES-01 -- ScoreGapRow CI whiskers visible, no horizontal scroll, info icons are >= 44px tap targets

**Steps:**
1. Open `/endgames` in DevTools mobile preview at 375 x 667 (or on a real phone)
2. Navigate to "Endgame Metrics and ELO" section
3. Verify: no horizontal scroll in any of the 4 Section 2 cards (Conversion, Parity, Recovery, Skill)
4. Verify: ScoreGapRow CI whisker bars are visible (not clipped at the right edge)
5. Verify: the info icon (HelpCircle trigger) on each ScoreGapRow is >= 44px tap target

**Pass criteria:** No horizontal overflow; whiskers visible; info icon comfortably tappable with a thumb

---

### 2. Sign-convention visual check

**Requirement:** SEC2-ΔES-01 -- positive ΔES renders right of zero with ZONE_SUCCESS tint above neutralMax

**Steps:**
1. Open `/endgames` on an account with real data (games imported from chess.com or lichess)
2. Look at each Section 2 card (Conversion, Parity, Recovery, Skill)
3. For cards where `scoreGapMean > 0.05`: verify the score gap value text is green (ZONE_SUCCESS)
4. For cards where `scoreGapMean < -0.05`: verify the score gap value text is red (ZONE_DANGER)
5. For cards where scoreGapMean is inside `[-0.05, 0.05]`: verify the value text is neutral (no color)
6. Verify the MiniBulletChart bar extends right of center for positive values, left for negative

**Pass criteria:** Zone-only tint consistent with value vs band; bullet bar direction matches sign

---

### 3. No "vs opponents" framing copy review

**Requirement:** SEC2-ΔES-05 / D-08 -- MetricStatPopover carries bucket-specific copy with sigmoid caveat; no "vs opponents" wording anywhere

**Steps:**
1. Open `/endgames` and expand each of the 4 Section 2 card info popovers (click the HelpCircle icon next to "Conversion Score Gap", "Parity Score Gap", "Recovery Score Gap", "Skill Score Gap")
2. Verify each popover:
   - Opens and shows the bucket-specific explanation
   - Contains the sigmoid-bias caveat: "Note: the Lichess expected-score sigmoid under-weights endgame eval; rely on the zone bands, not the raw magnitude."
   - Does NOT contain the words "vs opponents", "compared to opponents", "Opp Skill", "opponentRate", or "mirror bucket"
3. Verify the sub-question line in the section reads "Do you outperform the Stockfish baseline at converting, holding, and recovering?" (not "vs opponents")

**Pass criteria:** All 4 popovers show bucket-specific text + identical sigmoid caveat; zero "vs opponents" framing visible anywhere in the section

---

## Additional Spot Checks (Plan 03 specific)

### 4. Section 2 cards render after import

1. Log in with a test account that has >= 20 games imported
2. Navigate to `/endgames` -> scroll to "Endgame Metrics and ELO" section
3. Verify all 4 cards render (not stuck in loading or error state)
4. Verify the Skill card gauge shows a value (not "Not enough data yet") when there are sufficient games
5. Verify the ScoreGapRow is hidden (not rendered) on any card where there is no span eval data (gapN = 0)

**Pass criteria:** Cards render with real data; empty state only when truly no eval data

---

*Generated by Plan 03 executor, 2026-05-15*
*Append additional checks from future plans as needed.*
