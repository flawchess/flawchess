# Phase 87.2 Human UAT Items

Manual UAT items that require human judgment after deployment.

## LLM Narration Checks

- **Section 2 Score Gap dual-label rule**: Generate an endgame insights report on dev DB.
  Confirm narration uses "Section 2 Score Gap" as the umbrella term and "Conversion Score Gap" /
  "Parity Score Gap" / "Recovery Score Gap" / "Skill Score Gap" in per-card narration.
  Confirm no "vs opponents" wording appears. Confirm the sigmoid caveat appears at least once
  in the narration body (e.g. "zone bands" or "Stockfish baseline" framing).

## Cache Invalidation

- After deploy, verify that prior cached reports under `endgame_v30` do not return for requests
  that would trigger a fresh generation. The cache key includes the prompt_version field;
  `endgame_v31` reports should be generated fresh on first request after the deploy.
