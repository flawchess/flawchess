---
phase: 85-section-1-games-with-vs-without-endgame-cards
plan: 05
type: execute
wave: 4
depends_on: [85-04]
files_modified:
  - frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx
  - frontend/src/components/charts/EndgameStartVsEndSection.tsx
  - frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx
  - frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx
  - frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx
autonomous: true
requirements: [SEC1-01, SEC1-02, SEC1-03, SEC1-04, SEC1-05, SEC1-06, SEC1-07]

must_haves:
  truths:
    - "Endgames page renders a single 3-column section under the 'Endgame Overall Performance' h2 instead of two separate sections."
    - "Card 1 displays 'Games ending in Middlegame' with WDL bar and score bullet anchored at 0.50 (±0.15, sig-gated)."
    - "Card 2 displays 'At Endgame Entry' with two rows (Endgame entry eval + Achievable score) and NO WDL bar."
    - "Card 3 displays 'Endgame results' with WDL bar and score bullet anchored at 0.50 (±0.15, sig-gated)."
    - "The Endgame Score Gap bullet sits visually under Card 2 on desktop and stacked at the bottom on mobile, with label 'Endgame Score Gap'."
    - "No section-level h3 heading or section-level InfoPopover renders inside the new composite section."
    - "The legacy EndgameStartVsEndSection.tsx file no longer exists and is not imported anywhere."
    - "The legacy 'tile-endgame-score' (What you do with it) tile no longer renders anywhere."
    - "Frontend pipeline passes: lint, knip, typecheck, vitest, build."
  artifacts:
    - path: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      provides: "3-card composite section absorbing legacy 'Where you start' tile as Card 2; replaces EndgameGamesWithWithoutSection module name."
      contains: "tile-games-ending-middlegame, tile-at-endgame-entry, tile-endgame-results, endgame-score-gap"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Single mount of EndgameOverallPerformanceSection (no EndgameStartVsEndSection mount remains)."
    - path: "frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx"
      provides: "Component tests for the new 3-card layout, Card 2 rows, score-gap placement."
    - path: "frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx"
      provides: "Page-level tests for the new composite section mount (renamed from Endgames.startVsEnd.test.tsx)."
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgameOverallPerformanceSection"
      via: "single mount under the 'Endgame Overall Performance' h2, gated on scoreGapData && showPerfSection"
      pattern: "<EndgameOverallPerformanceSection"
    - from: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      to: "data.entry_eval_*, data.entry_expected_score_*"
      via: "Card 2 absorbs both rows from the legacy 'Where you start' tile verbatim (including AchievableScorePopover + BulletConfidencePopover + Cpu icon)"
      pattern: "entry-eval-value|achievable-score-value"
    - from: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      to: "scoreGap.score_difference"
      via: "MiniBulletChart in a column-2 row on desktop; stacked at the bottom on mobile"
      pattern: "endgame-score-gap"
---

<objective>
Restructure the shipped Section 1 from a two-card section + a separate EndgameStartVsEndSection into a single 3-column composite section that absorbs Card 2 ("At Endgame Entry") from the legacy "Where you start" tile, deletes the duplicate "What you do with it" tile, and relocates the Endgame Score Gap under Card 2. Implements the `## Redesign Decision (Post-Execution, 2026-05-13)` block in 85-CONTEXT.md.

Purpose: The user-facing redesign collapses two adjacent sections that read awkwardly together (the "What you do with it" tile already duplicates the "with Endgame" card) into one coherent left-to-right narrative: *score without endgame | expected at entry | realized with endgame*, with the gap sitting under the branching moment (Card 2). Removes redundant chrome (section h3 + section InfoPopover) since the question line under the page-level h2 carries the framing.

Output:
- New file `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` (renamed from EndgameGamesWithWithoutSection.tsx, refactored to 3 cards + repositioned score gap).
- Deleted file `frontend/src/components/charts/EndgameStartVsEndSection.tsx`.
- Old `frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` deleted.
- `frontend/src/pages/Endgames.tsx` updated: import + mount swapped, EndgameStartVsEndSection mount removed.
- Renamed test files: `EndgameOverallPerformanceSection.test.tsx` (replaces `EndgameGamesWithWithoutSection.test.tsx`) and `Endgames.overallPerformance.test.tsx` (replaces `Endgames.startVsEnd.test.tsx`). Legacy `EndgameStartVsEndSection.test.tsx` is deleted.
- All frontend gates green: `npm run lint`, `npx tsc --noEmit`, `npm test -- --run`, `npm run knip`, `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-CONTEXT.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-01-SUMMARY.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-02-SUMMARY.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-03-SUMMARY.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-04-SUMMARY.md

@frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx
@frontend/src/components/charts/EndgameStartVsEndSection.tsx
@frontend/src/pages/Endgames.tsx
@frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx
@frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx
@frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx
@frontend/src/types/endgames.ts
@frontend/src/lib/theme.ts
@frontend/src/lib/scoreBulletConfig.ts
@frontend/src/lib/scoreConfidence.ts
@frontend/src/lib/significance.ts
@frontend/src/generated/endgameZones.ts

<interfaces>
<!-- Key types and component contracts the executor needs verbatim. Extracted from codebase. -->

From frontend/src/types/endgames.ts (existing — no changes):
```typescript
interface EndgameWDLSummary {
  wins: number;
  draws: number;
  losses: number;
  total: number;
  win_pct: number;
  draw_pct: number;
  loss_pct: number;
}

interface EndgamePerformanceResponse {
  endgame_wdl: EndgameWDLSummary;
  non_endgame_wdl: EndgameWDLSummary;
  endgame_win_rate: number;
  entry_eval_mean_pawns: number;
  entry_eval_n: number;
  entry_eval_p_value: number | null;
  endgame_score_p_value: number | null;
  non_endgame_score_p_value: number | null;
  entry_eval_ci_low_pawns: number | null;
  entry_eval_ci_high_pawns: number | null;
  entry_expected_score: number;
  entry_expected_score_n: number;
  entry_expected_score_p_value: number | null;
  entry_expected_score_ci_low: number | null;
  entry_expected_score_ci_high: number | null;
}

interface ScoreGapMaterialResponse {
  endgame_score: number;
  non_endgame_score: number;
  score_difference: number;
  material_rows: unknown[];
  timeline: unknown[];
  timeline_window: number;
}
```

Component props (new):
```typescript
interface EndgameOverallPerformanceSectionProps {
  data: EndgamePerformanceResponse;
  scoreGap: ScoreGapMaterialResponse;
}
```

Reusable imports (do not reinvent):
- `MiniBulletChart` from `@/components/charts/MiniBulletChart`
- `MiniWDLBar` from `@/components/stats/MiniWDLBar`
- `BulletConfidencePopover` from `@/components/insights/BulletConfidencePopover`
- `AchievableScorePopover` from `@/components/popovers/AchievableScorePopover`
- `ScoreConfidencePopover` from `@/components/insights/ScoreConfidencePopover` (used in Cards 1 + 3 score row)
- `InfoPopover` from `@/components/ui/info-popover` (only used per-card, NOT at section root)
- `Cpu` icon from `lucide-react` for the Card 2 entry-eval row
- `wilsonBounds`, `ConfidenceLevel` from `@/lib/scoreConfidence`
- `SCORE_BULLET_CENTER, SCORE_BULLET_NEUTRAL_MIN, SCORE_BULLET_NEUTRAL_MAX, clampScoreCi, scoreZoneColor` from `@/lib/scoreBulletConfig`
- `ENTRY_EXPECTED_SCORE_NEUTRAL_MIN, ENTRY_EXPECTED_SCORE_NEUTRAL_MAX, entryExpectedScoreZoneColor, SCORE_GAP_NEUTRAL_MIN, SCORE_GAP_NEUTRAL_MAX` from `@/generated/endgameZones`
- `ENDGAME_ENTRY_EVAL_CENTER, ENDGAME_ENTRY_EVAL_DOMAIN_PAWNS, ENDGAME_ENTRY_EVAL_NEUTRAL_MIN_PAWNS, ENDGAME_ENTRY_EVAL_NEUTRAL_MAX_PAWNS, endgameEntryEvalZoneColor` from `@/lib/endgameEntryEvalZones`
- `formatSignedEvalPawns` from `@/lib/clockFormat`
- `isConfident` from `@/lib/significance`
- `MIN_GAMES_FOR_RELIABLE_STATS, ZONE_NEUTRAL, ZONE_SUCCESS, ZONE_DANGER` from `@/lib/theme`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create EndgameOverallPerformanceSection with 3-card layout, absorb Card 2 from legacy StartVsEnd, reposition score gap under Card 2</name>
  <files>frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx, frontend/src/components/charts/EndgameStartVsEndSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx (full file — current shipped 2-card section + footer gap. Carry forward: deriveLevel helper, ENDGAME_TILE_SCORE_DOMAIN, SCORE_GAP_DOMAIN, CONFIDENCE_HIGH_MAX_P, CONFIDENCE_MEDIUM_MAX_P, the EndgameCard subcomponent shape, and the per-card sig-gating triple verbatim.)
    - frontend/src/components/charts/EndgameStartVsEndSection.tsx (full file — Card 2 absorbs lines 139-240, the "Where you start" tile in its entirety. Preserve: the Cpu icon import, BulletConfidencePopover wiring with `showBaselineTick=false` + `color="white"` fallback, AchievableScorePopover wiring, formatSignedEvalPawns formatting, entryExpectedScoreZoneColor + ENTRY_EXPECTED_SCORE_NEUTRAL_MIN/MAX from endgameZones, the OFFSET-form `neutralMin/Max = ENTRY_EXPECTED_SCORE_NEUTRAL_MIN/MAX - SCORE_BULLET_CENTER` arithmetic on the achievable-score MiniBulletChart (CR-01 fix — do NOT regress to absolute bounds), and the per-row `endgame-achievable-score` wrapper testid.)
    - frontend/src/lib/theme.ts (confirm MIN_GAMES_FOR_RELIABLE_STATS = 10, ZONE_NEUTRAL / ZONE_SUCCESS / ZONE_DANGER constants used for the score-gap font color — no new constants needed)
    - frontend/src/generated/endgameZones.ts (confirm SCORE_GAP_NEUTRAL_MIN / SCORE_GAP_NEUTRAL_MAX / ENTRY_EXPECTED_SCORE_NEUTRAL_MIN / ENTRY_EXPECTED_SCORE_NEUTRAL_MAX / entryExpectedScoreZoneColor exports)
  </read_first>
  <action>
    Create new file `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` exporting `EndgameOverallPerformanceSection({ data, scoreGap })`. Then delete `frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` and `frontend/src/components/charts/EndgameStartVsEndSection.tsx` (legacy "Where you start" content is absorbed; legacy "What you do with it" tile is dropped because Card 3 already renders the same `endgame_wdl` + endgame score bullet — keeping both would be a duplicate widget per the CONTEXT redesign block).

    Structure of the new component (top-down):

    1. Header block above the grid: a single `<p className="text-sm text-muted-foreground">Do you perform better or worse when games reach an endgame?</p>`. **No h3, no section-level InfoPopover.** Wrap the whole section in `<section data-testid="endgame-overall-performance-section">`.

    2. Three-column grid for Cards 1-3: `<div className="grid grid-cols-1 lg:grid-cols-3 gap-4 mt-2">`. On lg+: Card 1 | Card 2 | Card 3. On mobile: stacked top-to-bottom.

    3. **Card 1 — Games ending in Middlegame** (left on desktop):
       - Reuse the existing EndgameCard subcomponent pattern from the legacy `EndgameGamesWithWithoutSection.tsx` (charcoal-texture tile shell, WDL row + score row, sig-gating triple).
       - `data-testid="tile-games-ending-middlegame"`
       - Card title text (verbatim): `Games ending in Middlegame`
       - WDL data: `data.non_endgame_wdl`
       - Score row p-value: `data.non_endgame_score_p_value`
       - Per-row testids: `data-testid="score-value-no"` on the score value span; `data-testid="score-info-no"` on the InfoPopover trigger.
       - InfoPopover copy: identical to the existing legacy copy explaining the 0.50 balanced-WDL natural anchor (carry forward verbatim from `EndgameGamesWithWithoutSection.tsx:156-162`).

    4. **Card 2 — At Endgame Entry** (middle on desktop):
       - Same `charcoal-texture rounded-md p-4` shell.
       - `data-testid="tile-at-endgame-entry"`
       - Card title text (verbatim): `At Endgame Entry`
       - **Two rows only — NO WDL bar row.**
       - Row 1: Endgame entry eval. Pull lines 146-186 from the legacy EndgameStartVsEndSection.tsx verbatim — label "Endgame entry eval:", `formatSignedEvalPawns` value, Cpu icon, BulletConfidencePopover with `color="white"`, `showBaselineTick={false}`, `testId="entry-eval-popover-trigger"`. Sig-gated zone color via `endgameEntryEvalZoneColor` + `deriveLevel(data.entry_eval_p_value, data.entry_eval_n)` + `isConfident`. MiniBulletChart with `ENDGAME_ENTRY_EVAL_CENTER / NEUTRAL_MIN/MAX_PAWNS / DOMAIN_PAWNS`. Preserve the empty-state branch `n < MIN_GAMES_FOR_RELIABLE_STATS → "Not enough data yet"`. Per-row testid `entry-eval-value` on the value span.
       - Row 2: Achievable score. Pull lines 190-238 from the legacy EndgameStartVsEndSection.tsx verbatim — wrapped in `<div data-testid="endgame-achievable-score">`, label "Achievable score:", percentage value with `data-testid="achievable-score-value"`, AchievableScorePopover, MiniBulletChart with `center=SCORE_BULLET_CENTER`, **`neutralMin=ENTRY_EXPECTED_SCORE_NEUTRAL_MIN - SCORE_BULLET_CENTER`** and **`neutralMax=ENTRY_EXPECTED_SCORE_NEUTRAL_MAX - SCORE_BULLET_CENTER`** (the OFFSET-form CR-01 fix — DO NOT regress to absolute bounds), `domain=ENDGAME_TILE_SCORE_DOMAIN` (declare locally as `0.15`), `clampScoreCi` for CI bounds.

    5. **Card 3 — Endgame results** (right on desktop):
       - Reuse the same EndgameCard subcomponent as Card 1.
       - `data-testid="tile-endgame-results"`
       - Card title text (verbatim): `Endgame results`
       - WDL data: `data.endgame_wdl`
       - Score row p-value: `data.endgame_score_p_value`
       - Per-row testids: `data-testid="score-value-yes"` on the score value span; `data-testid="score-info-yes"` on the InfoPopover trigger.
       - InfoPopover copy: same 0.50 natural anchor copy as Card 1.

    6. **Endgame Score Gap row** — positioned under Card 2 on desktop, stacked after Card 3 on mobile:
       - Implementation: declare a second wrapper grid OR (cleaner) make the score-gap an additional grid child explicitly placed via Tailwind column-start utilities. Recommended approach: keep the same `lg:grid-cols-3` parent grid and add a fourth child positioned with `lg:col-start-2` (column 2 on desktop). On mobile (single column), it falls to the bottom naturally after all three cards in DOM order. So the DOM order is: Card 1, Card 2, Card 3, ScoreGap — and Tailwind `lg:col-start-2` lifts ScoreGap into the desktop column-2 slot below Card 2. Add `data-testid="endgame-score-gap"` on the score-gap wrapper.
       - **Tile shell:** `charcoal-texture rounded-md p-4`.
       - **Label:** `Endgame Score Gap` (verbatim; replaces the legacy `Score Gap (Yes − No)` label).
       - Inner layout: reuse the legacy row grid from `EndgameGamesWithWithoutSection.tsx:264-287` — `grid-cols-1 lg:grid-cols-[14rem_minmax(0,1fr)]`, left = "Difference:" label + signed-percent value span, right = `MiniBulletChart` with `center=0, neutralMin=SCORE_GAP_NEUTRAL_MIN, neutralMax=SCORE_GAP_NEUTRAL_MAX, domain=SCORE_GAP_DOMAIN (declare locally as 0.20)`.
       - Font color: legacy zone rule (ZONE_SUCCESS / ZONE_NEUTRAL / ZONE_DANGER based on `score_difference` vs `SCORE_GAP_NEUTRAL_MIN/MAX`). No sig gating per D-04. Per-row testid on the value span: `data-testid="score-gap-difference"` (preserve legacy testid name; CONTEXT-mapped `endgame-score-gap` is on the wrapper, the inner value span keeps `score-gap-difference` to minimize test churn — Card 2 row 1 in test surface already disambiguates).

    7. **Comment block** at the top of the file (replacing the legacy Phase 85 header): note Phase 85 redesign-in-place, reference 85-CONTEXT.md `## Redesign Decision (Post-Execution, 2026-05-13)`, document the 3-card layout + score-gap-under-Card-2 placement + absence of section h3.

    8. **Imports:** carry forward everything the legacy two files needed. Drop any imports that are now unused (e.g., if the legacy section InfoPopover at section-root is removed, you may still need InfoPopover for per-card popovers — keep it). Run `npm run knip` after the file lands; remove anything knip flags.

    9. **Type signature:** export `EndgameOverallPerformanceSection({ data, scoreGap }: { data: EndgamePerformanceResponse; scoreGap: ScoreGapMaterialResponse })`. Both props required (matches legacy `EndgameGamesWithWithoutSection` signature; `scoreGap` was already required there since 85-03).

    Verbatim text strings (locked):
    - Section question (replaces section h3): `Do you perform better or worse when games reach an endgame?`
    - Card 1 title: `Games ending in Middlegame`
    - Card 2 title: `At Endgame Entry`
    - Card 3 title: `Endgame results`
    - Score Gap tile label/h3: `Endgame Score Gap`

    File-deletion sequence: write the new file first, then delete both legacy files (`EndgameGamesWithWithoutSection.tsx` and `EndgameStartVsEndSection.tsx`) so the working tree only contains the new file when test updates run.

    CLAUDE.md compliance notes:
    - Every interactive element (popover triggers, info popovers) must keep its `data-testid` and `aria-label` props.
    - All theme colors come from `theme.ts` / `endgameZones.ts` constants — no inline hex.
    - No magic numbers: `ENDGAME_TILE_SCORE_DOMAIN`, `SCORE_GAP_DOMAIN`, `CONFIDENCE_HIGH_MAX_P`, `CONFIDENCE_MEDIUM_MAX_P` declared as `const` at module scope with the same names + values used by the legacy files.
    - Mobile parity: the 3-col grid stacks to 1-col on `< lg`; the score-gap row falls below Card 3 naturally. Do not introduce a separate mobile-only block.
    - Function nesting depth stays ≤3; if the new component file balloons past ~250 logic LOC, extract the EndgameCard subcomponent and the Card 2 subcomponent as separate helpers in the same file (preserve the legacy `EndgameCard` pattern — that helper is already factored out).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint -- --max-warnings=0</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` exists.
    - File `frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` does NOT exist (verify with `! test -f frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx`).
    - File `frontend/src/components/charts/EndgameStartVsEndSection.tsx` does NOT exist (verify with `! test -f frontend/src/components/charts/EndgameStartVsEndSection.tsx`).
    - `grep -c 'data-testid="endgame-overall-performance-section"' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'data-testid="tile-games-ending-middlegame"' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'data-testid="tile-at-endgame-entry"' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'data-testid="tile-endgame-results"' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'data-testid="endgame-score-gap"' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'Endgame Score Gap' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs at least `1` (and `Score Gap (Yes − No)` does NOT appear: `! grep -q 'Score Gap (Yes' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx`).
    - `grep -c 'Games ending in Middlegame' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'At Endgame Entry' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'Endgame results' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c 'Do you perform better or worse when games reach an endgame' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs `1`.
    - `grep -c '<h3' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` is greater than or equal to 4 (3 card titles + score-gap tile label = 4; no section-root h3) AND `! grep -q 'Games with vs without Endgame' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` (legacy section h3 string gone).
    - `! grep -q 'perf-section-info' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` (section-level InfoPopover trigger gone).
    - `grep -c 'ENTRY_EXPECTED_SCORE_NEUTRAL_MIN - SCORE_BULLET_CENTER' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs at least `1` (CR-01 offset-form arithmetic preserved on the achievable-score MiniBulletChart — do not regress to absolute bounds).
    - `grep -c 'lg:col-start-2' frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` outputs at least `1` (desktop score-gap placement under Card 2; OR equivalent column-2 positioning class — if executor uses a different Tailwind primitive, document in SUMMARY and adjust this criterion).
    - `grep -rn 'EndgameStartVsEndSection\|EndgameGamesWithWithoutSection' frontend/src --include='*.ts' --include='*.tsx' --include='*.test.tsx' | grep -v '\.test\.' | wc -l` outputs `0` (no production imports of either legacy module remain; test files updated in Task 2 may transiently reference them in old paths until that task runs).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run lint -- --max-warnings=0` exits 0.
  </acceptance_criteria>
  <done>
    The new `EndgameOverallPerformanceSection.tsx` is the single source of truth for the 3-card composite section; both legacy module files are deleted; typecheck + lint are green. Tests still reference legacy module paths at this point (fixed in Task 2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Endgames.tsx mount, delete EndgameStartVsEndSection mount, rename and update both test files</name>
  <files>frontend/src/pages/Endgames.tsx, frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx, frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx, frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx, frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx, frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx (lines 1-50 for imports, 335-460 for the mount block — the `EndgameStartVsEndSection` import on line 23, the `EndgameGamesWithWithoutSection` import on line 21, and the two mount sites on lines 421-424)
    - frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx (full file — covers section h3, both card titles, footer-gap title, per-card score math, sig gating triple, empty-state copy. Carry forward most assertions but update titles + testids per the redesign.)
    - frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx (full file — covers tile-entry-eval row 1 + row 2 + tile-endgame-score behavior. Reuse the entry-eval + achievable-score row assertions for new Card 2; delete the tile-endgame-score assertions because that tile no longer exists.)
    - frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx (full file — page-level mount + accordion-order tests. Update for the new single-section mount.)
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (the file created in Task 1 — confirm exported component name + testids match assertions)
  </read_first>
  <action>
    Step A — Update `frontend/src/pages/Endgames.tsx`:
    - Remove the import line `import { EndgameStartVsEndSection } from '@/components/charts/EndgameStartVsEndSection';` (currently line 23).
    - Replace the import line `import { EndgameGamesWithWithoutSection } from '@/components/charts/EndgameGamesWithWithoutSection';` (currently line 21) with `import { EndgameOverallPerformanceSection } from '@/components/charts/EndgameOverallPerformanceSection';`.
    - In the mount block (currently lines 421-424), delete the entire `<EndgameStartVsEndSection data={perfData} />` line. Replace the `<EndgameGamesWithWithoutSection data={perfData} scoreGap={scoreGapData} />` line with `<EndgameOverallPerformanceSection data={perfData} scoreGap={scoreGapData} />`. Keep the existing `{scoreGapData && (...)}` gating wrapper as-is (the new section requires both `data` and `scoreGap`, matching the legacy `EndgameGamesWithWithoutSection` gating).
    - Do NOT touch any other section in `Endgames.tsx` (`EndgameScoreOverTimeChart`, `EndgameScoreGapSection`, `EndgameEloTimelineSection`, accordion concepts block, etc. — all out of scope).

    Step B — Rename and update component-level test file:
    - Delete `frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx`.
    - Delete `frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx`.
    - Create `frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` that:
      - Imports from `../EndgameOverallPerformanceSection` and uses `buildWdl` / `makeData` / `makeScoreGap` fixture builders carried over from the old test files.
      - Preserves the MiniBulletChart `vi.mock` stub and the jsdom `matchMedia` / `ResizeObserver` shims verbatim.
      - **Re-asserts section structure** (replacing old layout assertions):
        - `screen.getByTestId('endgame-overall-performance-section')` exists.
        - `screen.getByTestId('tile-games-ending-middlegame')` exists.
        - `screen.getByTestId('tile-at-endgame-entry')` exists.
        - `screen.getByTestId('tile-endgame-results')` exists.
        - `screen.getByTestId('endgame-score-gap')` exists.
        - `screen.getByText('Games ending in Middlegame')`, `screen.getByText('At Endgame Entry')`, `screen.getByText('Endgame results')`, `screen.getByText('Endgame Score Gap')`, `screen.getByText(/Do you perform better or worse when games reach an endgame/)` all resolve.
        - **Negative assertions:** `screen.queryByText('Games with vs without Endgame')` is null, `screen.queryByTestId('endgame-games-with-without-section')` is null, `screen.queryByTestId('endgame-start-vs-end-section')` is null, `screen.queryByTestId('tile-games-without-endgame')` is null, `screen.queryByTestId('tile-games-with-endgame')` is null, `screen.queryByTestId('tile-entry-eval')` is null, `screen.queryByTestId('tile-endgame-score')` is null, `screen.queryByTestId('score-gap-footer')` is null, `screen.queryByTestId('perf-section-info')` is null.
      - **Carries forward** the score-math assertion (per-card score percentage matches (W + 0.5·D)/n on both Card 1 + Card 3 using the new testids `score-value-no` + `score-value-yes`).
      - **Carries forward** the signed-prefix gap formatting test on `screen.getByTestId('score-gap-difference')` (`+7%` / `-12%`).
      - **Carries forward** the empty-state assertion for Card 1 when `non_endgame_wdl.total === 0` ("Not enough data yet" inside `tile-games-ending-middlegame`).
      - **Carries forward** the three sig-gating assertions on Card 3 (`score-value-yes`):
        - p-value > 0.05 + outside neutral band → no inline color
        - total < MIN_GAMES_FOR_RELIABLE_STATS → score row hidden, "Not enough data yet" shown
        - confident + outside neutral band → `style.color` matches `ZONE_SUCCESS`
      - **Adds new Card 2 assertions** (lifted from the legacy `EndgameStartVsEndSection.test.tsx`):
        - When `entry_eval_n >= 10`, `screen.getByTestId('entry-eval-value')` exists inside `tile-at-endgame-entry`.
        - When `entry_eval_n < 10`, the entry-eval row falls into the "Not enough data yet" empty state inside Card 2.
        - When `entry_expected_score_n >= 10`, `screen.getByTestId('achievable-score-value')` exists inside `tile-at-endgame-entry` (and the `endgame-achievable-score` wrapper testid is present).
        - When `entry_expected_score_n < 10`, the achievable row falls into "Not enough data yet" inside Card 2.
        - **Card 2 does NOT contain a WDL bar** — assert `within(screen.getByTestId('tile-at-endgame-entry')).queryByText('Win / Draw / Loss:')` is null.

    Step C — Rename and update page-level test file:
    - Move `frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx` → `frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` (use `git mv` if possible to preserve history; otherwise delete the old and create the new). Update its file-header comment block to reference the redesigned single composite section instead of the two-section layout.
    - Update assertions:
      - Replace the "renders EndgameStartVsEndSection before EndgameGamesWithWithoutSection (D-01)" test with a new "renders a single EndgameOverallPerformanceSection mount" test that asserts: exactly one `[data-testid="endgame-overall-performance-section"]` exists in the rendered tree AND no `[data-testid="endgame-start-vs-end-section"]` AND no `[data-testid="endgame-games-with-without-section"]` exist.
      - Update the "does NOT render the start-vs-end section when no endgame games" test to assert `[data-testid="endgame-overall-performance-section"]` is null in that branch.
      - Update the "preserves the new Section 1 cards, Score Gap footer, and timeline chart (D-21 negative scope)" test to query the new testids: `tile-games-ending-middlegame`, `tile-at-endgame-entry`, `tile-endgame-results`, `endgame-score-gap`. Drop the queries on `tile-games-without-endgame` and `tile-games-with-endgame`.
      - Keep the accordion-order tests (D-13 / D-14) as-is — they test the page-level concepts accordion paragraphs, which are out of scope for this redesign.
      - Keep the "references the Opponent Strength filter as plain text in the new accordion paragraphs (D-13)" test as-is.
    - The `buildPerf` fixture already includes `non_endgame_score_p_value: 0.001` (shipped in Plan 85-02) — no fixture changes required.

    Step D — Run all frontend gates and fix any fallout:
    - `cd frontend && npm test -- --run` must pass.
    - `cd frontend && npm run knip` must pass (no dead exports — both legacy module files are gone, so any stale imports surface here).
    - `cd frontend && npm run build` must produce a successful production build.

    Out-of-scope guardrails (do NOT touch in this task):
    - Backend code (`app/**`, `tests/**` Python).
    - Other Endgames page sections (Conv/Recov chart, Score Gap timeline, ELO timeline, Insights block).
    - The `EndgameScoreOverTimeChart` component (mounted independently of the redesigned section).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint -- --max-warnings=0 &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run &amp;&amp; npm run knip &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'EndgameStartVsEndSection' frontend/src/pages/Endgames.tsx` (no remaining import or mount of the legacy start-vs-end section).
    - `! grep -q 'EndgameGamesWithWithoutSection' frontend/src/pages/Endgames.tsx` (no remaining import or mount under the old name).
    - `grep -c 'EndgameOverallPerformanceSection' frontend/src/pages/Endgames.tsx` outputs at least `2` (1 import + 1 mount).
    - File `frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` exists.
    - File `frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx` does NOT exist.
    - File `frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx` does NOT exist.
    - File `frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` exists.
    - File `frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx` does NOT exist.
    - `grep -c 'tile-games-ending-middlegame' frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` outputs at least `1`.
    - `grep -c 'tile-at-endgame-entry' frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` outputs at least `1`.
    - `grep -c 'tile-endgame-results' frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` outputs at least `1`.
    - `grep -c 'endgame-score-gap' frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` outputs at least `1`.
    - `grep -c 'entry-eval-value' frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` outputs at least `1` (Card 2 entry-eval row assertions present).
    - `grep -c 'achievable-score-value' frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` outputs at least `1` (Card 2 achievable-score row assertions present).
    - `grep -c 'endgame-overall-performance-section' frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` outputs at least `1`.
    - `! grep -q 'endgame-start-vs-end-section' frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` is false ONLY for negative-assertion lines (i.e. it's allowed to appear inside a `queryByTestId(...).toBeNull()` assertion). Stricter check: `grep -c 'queryByTestId.\\?.endgame-start-vs-end-section.\\?' frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` outputs at least `1` (negative-scope assertion present).
    - `cd frontend && npm run lint -- --max-warnings=0` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm test -- --run` exits 0 with 0 failing tests.
    - `cd frontend && npm run knip` exits 0 (no dead exports introduced; legacy module files fully cleaned up).
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Endgames.tsx mounts only the new EndgameOverallPerformanceSection (no legacy start-vs-end mount remains). Both legacy test files are deleted; the renamed test files cover the 3-card layout, Card 2 rows, score-gap placement, sig-gating, and the page-level single-mount + accordion-order assertions. Full frontend pipeline is green.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint -- --max-warnings=0` exits 0
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm test -- --run` exits 0
- `cd frontend && npm run knip` exits 0
- `cd frontend && npm run build` exits 0
- `! test -f frontend/src/components/charts/EndgameStartVsEndSection.tsx`
- `! test -f frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx`
- `! test -f frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx`
- `! test -f frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx`
- `! test -f frontend/src/pages/__tests__/Endgames.startVsEnd.test.tsx`
- `test -f frontend/src/components/charts/EndgameOverallPerformanceSection.tsx`
- `test -f frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx`
- `test -f frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx`
- `grep -c 'EndgameOverallPerformanceSection' frontend/src/pages/Endgames.tsx` outputs at least 2 (import + mount)
- `! grep -r 'EndgameStartVsEndSection\|EndgameGamesWithWithoutSection' frontend/src` (no remaining references in any file)
</verification>

<success_criteria>
- Endgames page renders a single 3-card composite section ("Games ending in Middlegame" | "At Endgame Entry" | "Endgame results") under the existing "Endgame Overall Performance" h2.
- Section question line "Do you perform better or worse when games reach an endgame?" sits directly under the page-level h2 with no intermediate section h3 or section-level InfoPopover.
- Card 1 + Card 3 render WDL bar + score bullet (anchored 0.5, ±0.15, sig-gated via the existing triple).
- Card 2 renders two rows (Endgame entry eval + Achievable score) with NO WDL bar; absorbs the entire legacy "Where you start" tile content verbatim (Cpu icon, BulletConfidencePopover, AchievableScorePopover, CR-01 OFFSET-form bounds on the MiniBulletChart).
- Endgame Score Gap tile renders under Card 2 on desktop (via `lg:col-start-2` or equivalent) and stacks at the bottom on mobile. Label reads "Endgame Score Gap" (not "Score Gap (Yes − No)").
- Legacy `EndgameStartVsEndSection.tsx` and `EndgameGamesWithWithoutSection.tsx` files are fully deleted; no references anywhere in `frontend/src`.
- All frontend gates pass: lint, typecheck, vitest, knip, build.
- All locked requirement IDs (SEC1-01..SEC1-07) remain satisfied: SEC1-01 (Card 1 + Card 3 side-by-side on lg+, stacked on mobile), SEC1-02 (Card 1 + Card 3 WDL bars), SEC1-03 (per-card chess-score bullet anchored at 0.50 with Wilson CI + p-value), SEC1-04 (Score Gap bullet relocated but still rendered with signed-diff axis), SEC1-05 (per-card InfoPopover explaining 0.50 natural anchor), SEC1-06 (sig-gating triple unchanged), SEC1-07 (legacy table removed — original SEC1-07 still holds; new redesign deletes the absorbed legacy section as additional cleanup).
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-05-SUMMARY.md` summarizing:
- The new EndgameOverallPerformanceSection file shape (3 cards + score gap row placement).
- Files deleted (both legacy module files + both legacy test files).
- Test rename mapping.
- Confirmation of CLAUDE.md compliance (data-testid + ARIA + theme constants + mobile parity).
- Status of all 5 frontend gates (lint / typecheck / vitest / knip / build).
- Any executor-discretion calls made (e.g. exact Tailwind primitive chosen for the column-2 score-gap placement if not `lg:col-start-2`).
- A short note on the v1.17 ROADMAP milestone description amendment status (the "Frontend-only with one backend additive field" note was raised in Plan 85-01; this plan does not re-touch the backend, so the existing amendment still applies).
</output>
