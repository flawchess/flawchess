---
phase: 85
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/test_endgame_service.py
autonomous: true
requirements:
  - SEC1-03
  - SEC1-06
must_haves:
  truths:
    - "Backend `EndgamePerformanceResponse` exposes a `non_endgame_score_p_value` Wilson p-value computed from `non_endgame_wdl.{wins,draws,losses,total}` against H0: score == 0.5"
    - "The new p-value is None when `non_endgame_wdl.total < 10` (same gate as `endgame_score_p_value`); float in [0, 1] otherwise"
    - "Existing `endgame_score_p_value` semantics, defaults, and test coverage are unchanged"
    - "Pytest covers both the gated-None and non-null branches of the new field"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "`non_endgame_score_p_value: float | None = None` field on `EndgamePerformanceResponse`"
      contains: "non_endgame_score_p_value"
    - path: "app/services/endgame_service.py"
      provides: "Wilson p-value computation mirror call on `non_endgame_wdl` + assignment to response"
      contains: "non_endgame_score_p_value"
    - path: "tests/test_endgame_service.py"
      provides: "Test asserting non-null `non_endgame_score_p_value` ∈ [0, 1] and None below n=10 gate"
      contains: "non_endgame_score_p_value"
  key_links:
    - from: "app/services/endgame_service.py (performance aggregator)"
      to: "app/services/score_confidence.py::compute_confidence_bucket"
      via: "(wins, draws, losses, total) on non_endgame_wdl"
      pattern: "compute_confidence_bucket\\(\\s*non_endgame_wdl"
    - from: "app/services/endgame_service.py return EndgamePerformanceResponse(...)"
      to: "non_endgame_score_p_value=..."
      via: "keyword argument on the response constructor"
      pattern: "non_endgame_score_p_value="
---

<objective>
Add `non_endgame_score_p_value: float | None` to `EndgamePerformanceResponse` (Pydantic schema + service computation + test coverage). Mirror of the existing `endgame_score_p_value` Wilson score-test field — same H0 (0.5), same n ≥ 10 wire-format gate, same helper. Enables the Section 1 "Games without Endgame" card to consume an authoritative backend p-value identical in shape to the "Games with Endgame" card.

Purpose: D-01 of the Phase 85 CONTEXT — single derivation site, identical formula across both cards, removes any risk of the two cards' p-values silently disagreeing.
Output: One additive Pydantic field, ~3 lines of service code, one test case extending the existing `endgame_score_p_value` coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-CONTEXT.md
@app/schemas/endgames.py
@app/services/endgame_service.py
@app/services/score_confidence.py
@tests/test_endgame_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add non_endgame_score_p_value to EndgamePerformanceResponse and compute it in the service</name>
  <files>app/schemas/endgames.py, app/services/endgame_service.py</files>
  <read_first>
    - app/schemas/endgames.py (existing `EndgamePerformanceResponse` definition; mirror the docstring + default = None pattern of `endgame_score_p_value` at the existing field)
    - app/services/endgame_service.py around line 1756-1812 (existing `endgame_score_p_value` Wilson computation site and the response constructor)
    - app/services/score_confidence.py (`compute_confidence_bucket` signature: returns `(confidence_bucket, two_sided_p_value, se)` for (W, D, L, N))
  </read_first>
  <behavior>
    - Schema: `EndgamePerformanceResponse.non_endgame_score_p_value: float | None = None` with docstring mirroring the existing `endgame_score_p_value` line (substitute "non_endgame_wdl" + "non_endgame_wdl.total < 10")
    - Service: immediately after the existing `endgame_score_p_value` block (around line 1756-1760), add a sibling block computing `non_endgame_score_p_value` by calling `compute_confidence_bucket(non_endgame_wdl.wins, non_endgame_wdl.draws, non_endgame_wdl.losses, non_endgame_wdl.total)` and gating to `None` when `non_endgame_wdl.total < 10` (mirror the existing gate constant 10 used inline)
    - Response constructor: pass `non_endgame_score_p_value=non_endgame_score_p_value` alongside the existing `endgame_score_p_value=endgame_score_p_value` keyword
    - No behavior change to `endgame_score_p_value`, `entry_eval_*`, or `entry_expected_score_*` fields
  </behavior>
  <action>
    Edit `app/schemas/endgames.py` to add `non_endgame_score_p_value: float | None = None` immediately after the existing `endgame_score_p_value: float | None = None` line on `EndgamePerformanceResponse`. Copy the existing docstring for `endgame_score_p_value` and substitute `endgame_wdl` → `non_endgame_wdl` in both the prose and the gate description ("None when non_endgame_wdl.total < 10").

    Edit `app/services/endgame_service.py` in the performance aggregator function that currently computes `endgame_score_p_value` (search for the comment "Wilson score test of endgame_wdl against 50% (D-08)" near line 1756). Immediately below that block, add a sibling block: call `compute_confidence_bucket(non_endgame_wdl.wins, non_endgame_wdl.draws, non_endgame_wdl.losses, non_endgame_wdl.total)`, bind the second tuple element to a local `p_non_score_raw`, and assign `non_endgame_score_p_value: float | None = p_non_score_raw if non_endgame_wdl.total >= 10 else None`. Use the underscored discard pattern for the first and third tuple elements (`_conf_ns`, `_se_ns`) to match the existing style.

    In the `return EndgamePerformanceResponse(...)` call, add `non_endgame_score_p_value=non_endgame_score_p_value` as a keyword argument adjacent to the existing `endgame_score_p_value=endgame_score_p_value`.

    Reference the decision in a brief inline comment on the new service block: `# Mirror of endgame_score_p_value for the Section 1 "Games without Endgame" card (Phase 85 D-01).`

    Do NOT touch the entry_eval / entry_expected_score computation blocks. Do NOT add a new helper — reuse `compute_confidence_bucket` exactly as the existing `endgame_score_p_value` line does.
  </action>
  <verify>
    <automated>uv run ruff check app/schemas/endgames.py app/services/endgame_service.py &amp;&amp; uv run ruff format --check app/schemas/endgames.py app/services/endgame_service.py &amp;&amp; uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "non_endgame_score_p_value" app/schemas/endgames.py` returns ≥ 1 (the field definition, possibly with a docstring line)
    - `grep -c "non_endgame_score_p_value" app/services/endgame_service.py` returns ≥ 2 (the local assignment + the keyword argument on the response constructor)
    - `grep -c "endgame_score_p_value" app/schemas/endgames.py` and `app/services/endgame_service.py` are unchanged from baseline (no rename or removal of the existing field)
    - `uv run ty check app/ tests/` exits 0 with zero errors
    - The new service block references `compute_confidence_bucket` (not a parallel Wilson helper)
  </acceptance_criteria>
  <done>
    Schema field added, service computes the mirror p-value with the same gate (n ≥ 10), response wires the new field, ruff/format/ty all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pytest coverage for non_endgame_score_p_value gate + numeric range</name>
  <files>tests/test_endgame_service.py</files>
  <read_first>
    - tests/test_endgame_service.py lines 4087-4106 (existing `test_endgame_score_p_value_gated_below_n_ten` — the template to mirror)
    - tests/test_endgame_service.py around line 3950 (the test class header + `_wdl_rows` / `_get_endgame_performance_from_rows` fixture helpers used by the existing p-value test)
  </read_first>
  <behavior>
    - Test passes when `non_endgame_wdl.total < 10` → `non_endgame_score_p_value is None`
    - Test passes when `non_endgame_wdl.total >= 10` → `non_endgame_score_p_value` is a float in [0, 1]
    - Test does NOT depend on the endgame_score_p_value behavior of the same response object (kept independent so a future refactor that splits the two p-values still passes)
  </behavior>
  <action>
    Add a new test method `test_non_endgame_score_p_value_gated_below_n_ten` to the same test class that owns `test_endgame_score_p_value_gated_below_n_ten` (tests/test_endgame_service.py around line 4087). Mirror that test's structure exactly but swap roles: feed the WDL rows into `non_endgame_rows=` (not `endgame_rows=`) and leave `endgame_rows=[]`. Use `self._wdl_rows(wins=3, draws=2, losses=1)` for the below-gate case (total=6) and `self._wdl_rows(wins=10, draws=0, losses=0)` for the at-gate case (total=10). Assert: below-gate `resp.non_endgame_score_p_value is None`; at-gate `resp.non_endgame_score_p_value is not None`, `isinstance(float)`, and `0.0 &lt;= value &lt;= 1.0`.

    Do NOT modify the existing `test_endgame_score_p_value_gated_below_n_ten` test. Keep the docstring style consistent with neighboring tests in the class.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -x -k "non_endgame_score_p_value or endgame_score_p_value"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_endgame_service.py -x -k non_endgame_score_p_value` collects ≥ 1 test and exits 0
    - `uv run pytest tests/test_endgame_service.py -x -k endgame_score_p_value` still collects the original test and passes (no regression)
    - `grep -c "test_non_endgame_score_p_value_gated_below_n_ten" tests/test_endgame_service.py` returns 1
    - `uv run ruff check tests/test_endgame_service.py` exits 0
  </acceptance_criteria>
  <done>
    New test method exists, both gate branches asserted, full pytest suite for the test file passes, no existing test mutated.
  </done>
</task>

</tasks>

<verification>
- `uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest tests/test_endgame_service.py -x` all exit 0
- `grep -n "non_endgame_score_p_value" app/schemas/endgames.py app/services/endgame_service.py tests/test_endgame_service.py` shows the field defined, computed, wired into the response, and asserted in tests
</verification>

<success_criteria>
- Backend ships `non_endgame_score_p_value` on `EndgamePerformanceResponse` with identical n ≥ 10 gating semantics to `endgame_score_p_value`
- Computation reuses `compute_confidence_bucket` (single Wilson code path per project convention)
- Pytest covers both gate branches
- ruff / ruff-format / ty all green
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Note the milestone-boundary callout from D-01 in the SUMMARY's notes section ("v1.17 ROADMAP said 'frontend-only refactor'; Phase 85 adds one additive backend field per user authorization").
</output>
