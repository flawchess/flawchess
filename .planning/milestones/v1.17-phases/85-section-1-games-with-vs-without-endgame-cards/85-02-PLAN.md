---
phase: 85
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameScoreOverTimeChart.tsx
  - frontend/src/components/charts/EndgamePerformanceSection.tsx
autonomous: true
requirements:
  - SEC1-03
must_haves:
  truths:
    - "Frontend `EndgamePerformanceResponse` TS interface carries `non_endgame_score_p_value: number | null` mirroring `endgame_score_p_value`"
    - "`EndgameScoreOverTimeChart`, its `SCORE_BAND_CLASS` export, the `useIsMobile` hook, and the `SCORE_TIMELINE_Y_DOMAIN` / `SCORE_TIMELINE_Y_TICKS` / `MOBILE_BREAKPOINT_PX` constants live in their own file `frontend/src/components/charts/EndgameScoreOverTimeChart.tsx`"
    - "`EndgamePerformanceSection.tsx` no longer contains the chart, the hook, or the timeline-only constants — only the legacy section + its SCORE_GAP_DOMAIN constant remain (the section deletion is Plan 04's job)"
    - "All existing imports of `EndgameScoreOverTimeChart` / `SCORE_BAND_CLASS` resolve to the new file"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "Extended `EndgamePerformanceResponse` interface with `non_endgame_score_p_value: number | null`"
      contains: "non_endgame_score_p_value"
    - path: "frontend/src/components/charts/EndgameScoreOverTimeChart.tsx"
      provides: "Extracted timeline chart component + SCORE_BAND_CLASS export + useIsMobile hook"
      contains: "export function EndgameScoreOverTimeChart"
    - path: "frontend/src/components/charts/EndgamePerformanceSection.tsx"
      provides: "Legacy section (chart removed) — section function itself remains for now (Plan 04 deletes it)"
      contains: "EndgamePerformanceSection"
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "frontend/src/components/charts/EndgameScoreOverTimeChart.tsx (export)"
      via: "import path updated"
      pattern: "from '@/components/charts/EndgameScoreOverTimeChart'"
    - from: "frontend/src/components/charts/EndgameScoreOverTimeChart.tsx"
      to: "frontend/src/types/endgames.ts::ScoreGapTimelinePoint"
      via: "type import"
      pattern: "ScoreGapTimelinePoint"
---

<objective>
Two file-mechanical changes that unblock Wave 2: (1) extend the frontend `EndgamePerformanceResponse` TS interface with the new `non_endgame_score_p_value` field (mirror of `endgame_score_p_value` at line 66), so the Wave-2 Section 1 component compiles against the locked wire shape from Plan 01; (2) extract `EndgameScoreOverTimeChart` (plus `SCORE_BAND_CLASS`, `useIsMobile`, and the three timeline-only module-scope constants) from `EndgamePerformanceSection.tsx` into its own file per D-09, so Plan 04 can delete the legacy section function cleanly without entangling the unrelated timeline chart.

Purpose: Decouples the chart from the doomed section file, eliminates an orphan import path (knip would otherwise flag the chart's continued existence in a section-named file), and gives Wave 2 the typed `non_endgame_score_p_value` to consume.
Output: New file `EndgameScoreOverTimeChart.tsx`; trimmed `EndgamePerformanceSection.tsx`; updated TS interface and import path in `Endgames.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-CONTEXT.md
@frontend/src/types/endgames.ts
@frontend/src/components/charts/EndgamePerformanceSection.tsx
@frontend/src/pages/Endgames.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend EndgamePerformanceResponse TS interface with non_endgame_score_p_value</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts lines 58-76 (the existing `EndgamePerformanceResponse` interface; mirror line 66 `endgame_score_p_value: number | null`)
  </read_first>
  <action>
    Edit `frontend/src/types/endgames.ts`. Inside the `EndgamePerformanceResponse` interface (lines 58-76), immediately after the existing `endgame_score_p_value: number | null;` field at line 66, add a new line `non_endgame_score_p_value: number | null;`. Preserve the existing comment block on line 62 ("Phase 81 (D-11)…") and the existing field ordering — the new field sits adjacent to its sibling. No other interface in this file changes.

    Do NOT touch `ScoreGapMaterialResponse` (lines 142-149), `ScoreGapTimelinePoint`, or any other type — they're untouched by D-01.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -v "EndgameGamesWithWithoutSection" | grep -v "EndgamePerformanceSection" | (! grep -E "error TS") &amp;&amp; grep -c "non_endgame_score_p_value" src/types/endgames.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "non_endgame_score_p_value" frontend/src/types/endgames.ts` shows exactly one match inside the `EndgamePerformanceResponse` interface body
    - `grep -n "endgame_score_p_value" frontend/src/types/endgames.ts` still shows the original `endgame_score_p_value` line unchanged
    - `cd frontend &amp;&amp; npx tsc --noEmit` reports no new TS errors arising from this file (existing project must still type-check)
  </acceptance_criteria>
  <done>
    The TS interface mirrors the backend Pydantic schema from Plan 01; tsc clean on the updated file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract EndgameScoreOverTimeChart + useIsMobile + SCORE_BAND_CLASS into its own file</name>
  <files>frontend/src/components/charts/EndgameScoreOverTimeChart.tsx, frontend/src/components/charts/EndgamePerformanceSection.tsx, frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgamePerformanceSection.tsx full file (the chart spans lines 258-575; the `useIsMobile` hook lines 59-71; the `SCORE_BAND_CLASS` const line 57; the timeline-only constants `SCORE_TIMELINE_Y_DOMAIN` / `SCORE_TIMELINE_Y_TICKS` / `MOBILE_BREAKPOINT_PX` lines 50-52; the Phase-68 comment block lines 281-302)
    - frontend/src/pages/Endgames.tsx lines 21, 426 (the import + mount site for the chart)
  </read_first>
  <action>
    Create new file `frontend/src/components/charts/EndgameScoreOverTimeChart.tsx`. Move into it, intact and unchanged in behavior:
    - The `useEffect`, `useId`, `useMemo`, `useState` React imports needed by the chart and the `useIsMobile` hook (drop `ChartContainer`, `ChartTooltip`, etc. only if they end up unused in the source file after the move; otherwise leave them)
    - Recharts imports actually used by the chart (`Area`, `Bar`, `CartesianGrid`, `ComposedChart`, `Line`, `XAxis`, `YAxis`)
    - `ChartContainer`, `ChartTooltip` from `@/components/ui/chart`
    - `InfoPopover` from `@/components/ui/info-popover`
    - Theme constants used only by the chart: `ENDGAME_VOLUME_BAR_COLOR`, `SCORE_TIMELINE_FILL_ABOVE`, `SCORE_TIMELINE_FILL_BELOW`, `SCORE_TIMELINE_LINE_ENDGAME`, `SCORE_TIMELINE_LINE_NON_ENDGAME` from `@/lib/theme`
    - `createDateTickFormatter`, `formatDateWithYear` from `@/lib/utils`
    - `ScoreGapTimelinePoint` type from `@/types/endgames`
    - Module-scope constants `SCORE_TIMELINE_Y_DOMAIN`, `SCORE_TIMELINE_Y_TICKS`, `MOBILE_BREAKPOINT_PX`
    - Exported constant `SCORE_BAND_CLASS` (keep `export` modifier)
    - `useIsMobile` hook (private to the file, no export — keep as a local helper)
    - Exported interface `EndgameScoreOverTimeChartProps`
    - Private interface `ScoreOverTimeChartPoint`
    - Private interface `GradientStop`
    - The Phase-68 + UAT-diagnosis comment block above the export
    - Exported `EndgameScoreOverTimeChart` function — identical body to the existing implementation

    Edit `frontend/src/components/charts/EndgamePerformanceSection.tsx` to REMOVE everything that was moved: the chart function, `EndgameScoreOverTimeChartProps`, `ScoreOverTimeChartPoint`, `GradientStop`, `SCORE_BAND_CLASS`, `useIsMobile`, `SCORE_TIMELINE_Y_DOMAIN`, `SCORE_TIMELINE_Y_TICKS`, `MOBILE_BREAKPOINT_PX`, the Phase-68 comment block. Also drop now-unused imports (`useEffect`, `useId`, `useMemo`, `useState`, the Recharts imports `Area`/`Bar`/`CartesianGrid`/`ComposedChart`/`Line`/`XAxis`/`YAxis`, `ChartContainer`, `ChartTooltip`, `createDateTickFormatter`, `formatDateWithYear`, `ScoreGapTimelinePoint`, and the theme constants `ENDGAME_VOLUME_BAR_COLOR` / `SCORE_TIMELINE_*` colors).

    Keep in `EndgamePerformanceSection.tsx`: the `EndgamePerformanceSection` function (Plan 04 deletes it), its props interface, the `SCORE_GAP_DOMAIN` constant (only the legacy section uses it), `InfoPopover`, `MiniWDLBar`, `MiniBulletChart`, `SCORE_GAP_NEUTRAL_MIN` / `SCORE_GAP_NEUTRAL_MAX`, `ZONE_DANGER` / `ZONE_NEUTRAL` / `ZONE_SUCCESS`, and the `EndgamePerformanceResponse` / `ScoreGapMaterialResponse` type imports.

    Edit `frontend/src/pages/Endgames.tsx` line 21. Split the current combined import into two lines:
      `import { EndgamePerformanceSection } from '@/components/charts/EndgamePerformanceSection';`
      `import { EndgameScoreOverTimeChart } from '@/components/charts/EndgameScoreOverTimeChart';`
    The mount site at line 426 stays as-is — the JSX `<EndgameScoreOverTimeChart ... />` element is unchanged.

    Add a one-line file header comment to the new `EndgameScoreOverTimeChart.tsx` noting that it was extracted from `EndgamePerformanceSection.tsx` in Phase 85 (D-09) so the legacy section can be deleted cleanly without entangling the unrelated timeline chart.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run --reporter=basic 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/charts/EndgameScoreOverTimeChart.tsx` exists
    - `grep -c "export function EndgameScoreOverTimeChart" frontend/src/components/charts/EndgameScoreOverTimeChart.tsx` returns 1
    - `grep -c "export const SCORE_BAND_CLASS" frontend/src/components/charts/EndgameScoreOverTimeChart.tsx` returns 1
    - `grep -c "function useIsMobile" frontend/src/components/charts/EndgameScoreOverTimeChart.tsx` returns 1
    - `grep -c "EndgameScoreOverTimeChart\|useIsMobile\|SCORE_BAND_CLASS\|SCORE_TIMELINE_Y_DOMAIN\|MOBILE_BREAKPOINT_PX" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns 0 (nothing moved should remain)
    - `grep -c "from '@/components/charts/EndgameScoreOverTimeChart'" frontend/src/pages/Endgames.tsx` returns 1
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0 (no new errors)
    - `cd frontend &amp;&amp; npm run lint` exits 0
    - Existing test file `frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx` continues to pass (test files may need their import path for the chart updated — if so, update to `@/components/charts/EndgameScoreOverTimeChart`)
  </acceptance_criteria>
  <done>
    Chart + hook + chart-only constants live in the new file; legacy section file is trimmed but the section function itself is intact (Plan 04 deletes it); imports in `Endgames.tsx` and any test files updated; lint + tsc + tests green.
  </done>
</task>

</tasks>

<verification>
- `cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run` all exit 0
- `grep -n "non_endgame_score_p_value" frontend/src/types/endgames.ts` shows exactly one match
- The new file's `export function EndgameScoreOverTimeChart(...)` signature and body are byte-equivalent (after import reordering) to the prior implementation
</verification>

<success_criteria>
- TS interface mirror of the Plan-01 backend field in place
- Chart file extracted with all dependencies (hook, constants, comment block) moved
- Legacy section file's chart code fully removed; section function itself preserved for Plan 04 to delete
- Endgames.tsx imports updated to the two-file split
- Frontend build, lint, type-check, and tests all green
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-02-SUMMARY.md` per the template.
</output>
