---
phase: 85
plan: 03
type: execute
wave: 2
depends_on:
  - 85-01
  - 85-02
files_modified:
  - frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx
  - frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx
autonomous: true
requirements:
  - SEC1-01
  - SEC1-02
  - SEC1-03
  - SEC1-04
  - SEC1-05
  - SEC1-06
must_haves:
  truths:
    - "Two side-by-side cards render on lg+ (`grid grid-cols-1 lg:grid-cols-2 gap-4`), stacked on mobile, with left card titled `Games without Endgame` and right card titled `Games with Endgame` (SEC1-01, D-14, D-15)"
    - "Each card shows a `MiniWDLBar` for its WDL subset (SEC1-02)"
    - "Each card shows chess-score `(W + 0.5·D)/n` formatted as a percentage with Wilson 95% CI whiskers from `wilsonBounds(score, total)` and a `MiniBulletChart` anchored at `SCORE_BULLET_CENTER` (0.50), `neutralMin=SCORE_BULLET_NEUTRAL_MIN`, `neutralMax=SCORE_BULLET_NEUTRAL_MAX`, `domain=ENDGAME_TILE_SCORE_DOMAIN` (0.15) (SEC1-03, D-13)"
    - "Per-card font color on the score value is painted only when `n >= MIN_GAMES_FOR_RELIABLE_STATS` AND `isConfident(level)` AND `scoreZoneColor(score) !== ZONE_NEUTRAL`, where `level = deriveLevel(p, n)` — the left card uses `data.non_endgame_score_p_value` + `data.non_endgame_wdl.total`; the right card uses `data.endgame_score_p_value` + `data.endgame_wdl.total` (SEC1-06, D-03)"
    - "Footer Score Gap bullet renders below the two cards spanning full width, with `value=scoreGap.score_difference`, `center=0`, `neutralMin=SCORE_GAP_NEUTRAL_MIN`, `neutralMax=SCORE_GAP_NEUTRAL_MAX`, `domain=SCORE_GAP_DOMAIN=0.20`; font color follows the legacy zone rule (ZONE_SUCCESS / ZONE_NEUTRAL / ZONE_DANGER from `score_difference` vs neutral band) — NO sig gating (SEC1-04, D-04, D-05, D-06)"
    - "Section-level h3 `Games with vs without Endgame` + sub-tagline + section-level `InfoPopover` (legacy copy verbatim) sits above the cards (D-07)"
    - "Each per-card score row has an `InfoPopover` explaining the 0.50 natural anchor (SEC1-05, D-08)"
    - "Empty state per card (`wdl.total == 0`): renders `<p className=\"text-sm text-muted-foreground py-4\">Not enough data yet</p>` (matches `EndgameStartVsEndSection.tsx:187` convention)"
    - "Every interactive element + tile container + popover trigger carries a kebab-case, component-prefixed `data-testid`"
    - "Component tests assert: (a) both card titles render; (b) the score percentage matches `(W + 0.5·D)/n` rounded to 0 decimals; (c) the footer gap shows `score_difference` formatted with sign; (d) per-card empty-state copy renders when `wdl.total === 0`; (e) sig-gating: score value font color is unset when `n < MIN_GAMES_FOR_RELIABLE_STATS` even if zone is colored"
  artifacts:
    - path: "frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx"
      provides: "New Section 1 component — two cards + footer gap bullet"
      contains: "export function EndgameGamesWithWithoutSection"
      min_lines: 150
    - path: "frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx"
      provides: "Component tests for title rendering, score math, sig gating, empty state, footer gap"
      contains: "EndgameGamesWithWithoutSection"
  key_links:
    - from: "EndgameGamesWithWithoutSection (score row)"
      to: "wilsonBounds(score, total)"
      via: "frontend/src/lib/scoreConfidence.ts"
      pattern: "wilsonBounds\\("
    - from: "EndgameGamesWithWithoutSection (score row)"
      to: "MiniBulletChart"
      via: "score bullet with center=SCORE_BULLET_CENTER, domain=ENDGAME_TILE_SCORE_DOMAIN"
      pattern: "ENDGAME_TILE_SCORE_DOMAIN"
    - from: "EndgameGamesWithWithoutSection (footer)"
      to: "scoreGap.score_difference"
      via: "MiniBulletChart with neutralMin=SCORE_GAP_NEUTRAL_MIN, neutralMax=SCORE_GAP_NEUTRAL_MAX, domain=0.20"
      pattern: "SCORE_GAP_NEUTRAL_MIN"
    - from: "EndgameGamesWithWithoutSection (sig gating)"
      to: "isConfident(deriveLevel(p, n))"
      via: "frontend/src/lib/significance.ts + local deriveLevel mirror"
      pattern: "isConfident\\("
---

<objective>
Build the new Section 1 component `EndgameGamesWithWithoutSection.tsx` that replaces the legacy `Games with vs without Endgame` table. Two side-by-side `charcoal-texture` tiles on lg+ (No left, Yes right per D-14), each showing a `MiniWDLBar` and a per-card chess-score row with Wilson CI whiskers + score bullet anchored at 0.50 with the locked endgame `ENDGAME_TILE_SCORE_DOMAIN = 0.15` and the standard `SCORE_BULLET_NEUTRAL_MIN/MAX` band; section-level h3 + `InfoPopover` (legacy copy unchanged) above; full-width footer Score Gap bullet (`score_difference` axis, zone-color font only, no sig gating) below. Per-card score-row `InfoPopover` (new copy from D-08) explains the 0.50 natural anchor. Component tests cover layout, math, sig gating, and empty state.

Purpose: Replicates the locked `EndgameStartVsEndSection` tile + sig-gating template for Section 1, delivering all seven user-visible requirements (SEC1-01 through SEC1-06; SEC1-07 is Plan 04's cleanup).
Output: One new component file + one new test file. The legacy section is NOT touched here (Plan 04 mounts and deletes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-CONTEXT.md
@frontend/src/components/charts/EndgameStartVsEndSection.tsx
@frontend/src/components/charts/EndgamePerformanceSection.tsx
@frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx
@frontend/src/components/charts/MiniBulletChart.tsx
@frontend/src/components/stats/MiniWDLBar.tsx
@frontend/src/components/ui/info-popover.tsx
@frontend/src/lib/scoreConfidence.ts
@frontend/src/lib/scoreBulletConfig.ts
@frontend/src/lib/significance.ts
@frontend/src/lib/theme.ts
@frontend/src/generated/endgameZones.ts
@frontend/src/types/endgames.ts
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Reuse directly. -->

From `frontend/src/types/endgames.ts` (after Plan 02 extension):
```typescript
export interface EndgameWDLSummary {
  wins: number; draws: number; losses: number; total: number;
  win_pct: number; draw_pct: number; loss_pct: number;
}
export interface EndgamePerformanceResponse {
  endgame_wdl: EndgameWDLSummary;
  non_endgame_wdl: EndgameWDLSummary;
  endgame_win_rate: number;
  // ...entry_eval_* and entry_expected_score_* fields (not consumed by this section)
  endgame_score_p_value: number | null;
  non_endgame_score_p_value: number | null;  // added by Plan 02 Task 1
}
export interface ScoreGapMaterialResponse {
  endgame_score: number;
  non_endgame_score: number;
  score_difference: number;  // signed: endgame - non_endgame, scale 0-1
  // ...
}
```

From `frontend/src/lib/scoreConfidence.ts`:
```typescript
export function wilsonBounds(score: number, total: number): [number, number];
export type ConfidenceLevel = 'low' | 'medium' | 'high';
```

From `frontend/src/lib/scoreBulletConfig.ts`:
```typescript
export const SCORE_BULLET_CENTER = 0.5;
export const SCORE_BULLET_NEUTRAL_MIN = -0.05;
export const SCORE_BULLET_NEUTRAL_MAX = 0.05;
export function clampScoreCi(value: number): number;
export function scoreZoneColor(score: number): string;
```

From `frontend/src/lib/significance.ts`:
```typescript
export function isConfident(level: ConfidenceLevel): boolean;  // medium | high -> true
```

From `frontend/src/lib/theme.ts`:
```typescript
export const MIN_GAMES_FOR_RELIABLE_STATS = 10;
export const ZONE_NEUTRAL, ZONE_DANGER, ZONE_SUCCESS: string;  // hex strings
```

From `frontend/src/generated/endgameZones.ts`:
```typescript
export const SCORE_GAP_NEUTRAL_MIN = -0.1;
export const SCORE_GAP_NEUTRAL_MAX = 0.1;
```

`deriveLevel` is NOT exported from `EndgameStartVsEndSection.tsx`; replicate the same 4-line function locally (n &lt; 10 → low; p &lt; 0.01 → high; p &lt; 0.05 → medium; else low) — see `EndgameStartVsEndSection.tsx:73-78`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EndgameGamesWithWithoutSection.tsx with two cards + footer Score Gap bullet</name>
  <files>frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameStartVsEndSection.tsx (full file — the template; reuse `deriveLevel`, the tile shell `charcoal-texture rounded-md p-4`, the row grid `grid-cols-1 lg:grid-cols-[14rem_minmax(0,1fr)] gap-x-3 gap-y-2 items-center`, the sig-gating pattern `isConfident(level) &amp;&amp; isInColoredZone`, and the `ENDGAME_TILE_SCORE_DOMAIN = 0.15` local constant)
    - frontend/src/components/charts/EndgamePerformanceSection.tsx lines 90-122 (current section-level h3 + sub-tagline + `InfoPopover` copy — reuse verbatim per D-07; the legacy `rows[]` derivation showing the (W+0.5D)/n formula at the row map, and `SCORE_GAP_DOMAIN = 0.20` at line 44)
    - frontend/src/components/charts/MiniBulletChart.tsx (props: `value`, `center`, `neutralMin`, `neutralMax`, `domain`, `ciLow`, `ciHigh`, `barColor`, `ariaLabel`)
    - frontend/src/components/stats/MiniWDLBar.tsx (props shape)
    - frontend/src/components/ui/info-popover.tsx (props: `ariaLabel`, `testId`, `side`, children)
    - frontend/src/lib/scoreConfidence.ts (wilsonBounds signature)
    - frontend/src/lib/scoreBulletConfig.ts (SCORE_BULLET_CENTER, SCORE_BULLET_NEUTRAL_MIN/MAX, clampScoreCi, scoreZoneColor)
    - frontend/src/generated/endgameZones.ts (SCORE_GAP_NEUTRAL_MIN/MAX)
    - frontend/src/lib/theme.ts (MIN_GAMES_FOR_RELIABLE_STATS, ZONE_NEUTRAL, ZONE_DANGER, ZONE_SUCCESS)
    - frontend/src/lib/significance.ts (isConfident)
    - frontend/src/types/endgames.ts (EndgamePerformanceResponse, ScoreGapMaterialResponse, EndgameWDLSummary)
  </read_first>
  <behavior>
    - Exports `EndgameGamesWithWithoutSection` function component with props `{ data: EndgamePerformanceResponse; scoreGap: ScoreGapMaterialResponse }`
    - Renders a `<section data-testid="endgame-games-with-without-section">` wrapper
    - Above the cards: an h3 `Games with vs without Endgame` with section-level `InfoPopover` (legacy copy verbatim, see CONTEXT D-07 + EndgamePerformanceSection.tsx lines 110-119) + sub-tagline `<p>Do you perform better or worse when games reach an endgame?</p>`
    - Two-card grid: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-4">` containing two tiles
    - Left tile (`data-testid="tile-games-without-endgame"`): h3 `Games without Endgame`; WDL bar row + score row sourced from `data.non_endgame_wdl`; p-value = `data.non_endgame_score_p_value`
    - Right tile (`data-testid="tile-games-with-endgame"`): h3 `Games with Endgame`; WDL bar row + score row sourced from `data.endgame_wdl`; p-value = `data.endgame_score_p_value`
    - Both tiles use the same internal layout: `flex flex-col gap-4` with two rows. Row 1 is the WDL bar (only when `wdl.total > 0`, else the empty-state copy). Row 2 is the score row (only when `wdl.total >= MIN_GAMES_FOR_RELIABLE_STATS`, else the empty-state copy)
    - Score row layout: `grid grid-cols-1 lg:grid-cols-[14rem_minmax(0,1fr)] gap-x-3 gap-y-2 items-center` with left = `Score:` label + percentage value + per-card `InfoPopover`, right = `MiniBulletChart` (`value=score`, `center=SCORE_BULLET_CENTER`, `neutralMin=SCORE_BULLET_NEUTRAL_MIN`, `neutralMax=SCORE_BULLET_NEUTRAL_MAX`, `domain=ENDGAME_TILE_SCORE_DOMAIN`, `ciLow=clampScoreCi(wilsonLow)`, `ciHigh=clampScoreCi(wilsonHigh)`, `barColor="neutral"`, `ariaLabel="<card title>: score 47%"` style)
    - Score percentage formatted as `${Math.round(score * 100)}%`. Score color: `scoreShowZoneFontColor ? scoreZoneColor(score) : undefined` where `scoreShowZoneFontColor = isConfident(deriveLevel(pValue, total)) &amp;&amp; scoreZoneColor(score) !== ZONE_NEUTRAL`
    - Footer Score Gap bullet (full-width tile below the two-card grid): `<div className="charcoal-texture rounded-md p-4">` with h3 `Score Gap (Yes − No)` (the Claude's-discretion recommendation from CONTEXT) + `MiniBulletChart` with `value=scoreGap.score_difference`, `center=0`, `neutralMin=SCORE_GAP_NEUTRAL_MIN`, `neutralMax=SCORE_GAP_NEUTRAL_MAX`, `domain=SCORE_GAP_DOMAIN` (local const 0.20), `barColor="neutral"`. Also display a signed percentage label `${gapPositive ? '+' : ''}${Math.round(scoreGap.score_difference * 100)}%` painted with `gapColor` derived purely from `scoreGap.score_difference` vs `[SCORE_GAP_NEUTRAL_MIN, SCORE_GAP_NEUTRAL_MAX]` (NO sig gating, per D-04 — reuse the legacy `gapColor` ternary at EndgamePerformanceSection.tsx:82-88). `data-testid="score-gap-footer"`
    - Empty-state copy (when `wdl.total === 0` on a tile, OR `wdl.total < MIN_GAMES_FOR_RELIABLE_STATS` for the score row only): `<p className="text-sm text-muted-foreground py-4">Not enough data yet</p>`
    - File header comment block (mirroring `EndgameStartVsEndSection.tsx:1-19`) referencing Phase 85, the v1.17 single-bullet doctrine (no peer bullet), and the locked card ordering (No left, Yes right)
    - Em-dashes used sparingly in copy (single em-dash per paragraph cap per CLAUDE.md)
    - All interactive elements (popover triggers) have kebab-case `data-testid` attributes: `perf-section-info` for the section header popover (preserve the legacy ID so existing tests / Claude-Chrome automation keep working), `score-info-no` and `score-info-yes` for the two per-card popovers
    - All score values have `data-testid` attributes: `score-value-no`, `score-value-yes`, and `score-gap-difference` (preserve the legacy ID for the gap label so Claude-Chrome automation continues to find it)
  </behavior>
  <action>
    Create the new file `frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx`. Mirror the structure of `EndgameStartVsEndSection.tsx` exactly:
    - Top-of-file JSDoc block referencing Phase 85 + the v1.17 single-bullet doctrine (per CONTEXT "Specifics" — `Comment in the new component referencing Phase 85 + the v1.17 single-bullet doctrine, like the existing Phase 81 / 83 header block`)
    - Imports for `MiniBulletChart`, `MiniWDLBar`, `InfoPopover`, `wilsonBounds`, `ConfidenceLevel`, `isConfident`, `SCORE_BULLET_CENTER`, `SCORE_BULLET_NEUTRAL_MIN`, `SCORE_BULLET_NEUTRAL_MAX`, `clampScoreCi`, `scoreZoneColor`, `MIN_GAMES_FOR_RELIABLE_STATS`, `ZONE_NEUTRAL`, `ZONE_DANGER`, `ZONE_SUCCESS`, `SCORE_GAP_NEUTRAL_MIN`, `SCORE_GAP_NEUTRAL_MAX`, and types `EndgamePerformanceResponse`, `ScoreGapMaterialResponse`, `EndgameWDLSummary`
    - Local module-scope constants: `ENDGAME_TILE_SCORE_DOMAIN = 0.15` and `SCORE_GAP_DOMAIN = 0.20` (do not import from the legacy file — Plan 04 deletes it; declare local copies with comments noting the source-of-truth in CONTEXT D-13 and D-05)
    - Confidence-bucket thresholds `CONFIDENCE_HIGH_MAX_P = 0.01` and `CONFIDENCE_MEDIUM_MAX_P = 0.05` (mirror EndgameStartVsEndSection)
    - Local `deriveLevel(p, n)` helper identical to EndgameStartVsEndSection.tsx:73-78
    - A small internal helper `renderCard({ title, wdl, pValue, scoreTestId, popoverTestId, infoAriaLabel })` is acceptable to keep nesting depth ≤ 3 per CLAUDE.md function-size rules; alternatively inline both cards if total logic LOC stays ≤ 100. Planner's preference: a small `EndgameCard` sub-component or helper function that takes WDL + p-value + identifiers, since the two-card duplication would otherwise force horizontal repetition
    - Section-level `InfoPopover` copy: REUSE verbatim the existing legacy copy at EndgamePerformanceSection.tsx lines 114-117 (two-paragraph `<div className="space-y-2">` with the endgame-phase definition + Score Gap explanation) — `testId="perf-section-info"`
    - Per-card score-row `InfoPopover` copy: use the suggested copy from CONTEXT D-08 / Specifics ("This score's neutral anchor sits at 50% by construction…"). Identical body for both cards. testId differs: `score-info-no` / `score-info-yes`
    - Compute score values for each card: `const score = total > 0 ? (wdl.wins + 0.5 * wdl.draws) / total : 0` and `[ciLow, ciHigh] = wilsonBounds(score, total)`
    - Footer gap section: a separate `<div className="charcoal-texture rounded-md p-4 mt-4">` tile with h3 `Score Gap (Yes − No)`. Inside, render the signed-percentage label with the legacy zone-only color rule and the `MiniBulletChart` for the gap
    - Order in the JSX: section header → two-card grid → footer gap tile. Mobile stacks naturally because the outer grid is `grid-cols-1 lg:grid-cols-2`

    Do NOT inline implementations of `MiniBulletChart` / `MiniWDLBar` / `InfoPopover`. Do NOT recreate `wilsonBounds`. Do NOT hard-code zone colors — read from `ZONE_*` and `scoreZoneColor`. Do NOT use `SCORE_BULLET_DOMAIN = 0.25` (D-13 explicitly locks `ENDGAME_TILE_SCORE_DOMAIN = 0.15` for endgame tiles). Do NOT add a peer bullet (CONTEXT phase-boundary; Section 1 is cohort-only).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | (! grep -E "error TS") &amp;&amp; grep -c "export function EndgameGamesWithWithoutSection" src/components/charts/EndgameGamesWithWithoutSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` exists with `export function EndgameGamesWithWithoutSection({ data, scoreGap }: ...)` signature
    - `grep -c "Games without Endgame" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 1
    - `grep -c "Games with Endgame" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 1
    - `grep -c "ENDGAME_TILE_SCORE_DOMAIN = 0.15" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns 1
    - `grep -c "SCORE_GAP_DOMAIN = 0.20\|SCORE_GAP_DOMAIN = 0\\.20" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns 1
    - `grep -c "SCORE_BULLET_NEUTRAL_MIN\b" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 1
    - `grep -c "SCORE_GAP_NEUTRAL_MIN\b" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 1
    - `grep -c "wilsonBounds(" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 1 (per-card Wilson CI)
    - `grep -c "isConfident(" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 1 (sig gating)
    - `grep -c "data-testid=" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 6 (section, two tiles, two score values, footer, popover triggers)
    - `grep -c "non_endgame_score_p_value\|endgame_score_p_value" frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx` returns ≥ 2 (both p-values consumed)
    - File contains NO `SCORE_BULLET_DOMAIN` reference (the wider 0.25 domain — explicitly forbidden per D-13)
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0 (no new errors)
    - `cd frontend &amp;&amp; npm run lint` exits 0
    - Section header InfoPopover preserves the `testId="perf-section-info"` value (Claude-Chrome / existing tests continue to find it)
  </acceptance_criteria>
  <done>
    The new component renders the two-card section + footer gap bullet to spec, type-checks, lints clean, and all required `data-testid` attributes are present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component tests for EndgameGamesWithWithoutSection</name>
  <files>frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/EndgameStartVsEndSection.test.tsx (the sibling test file — replicate fixture style, render helpers, jest-dom matchers)
    - frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx (the legacy section's tests, for fixture shape reference)
  </read_first>
  <behavior>
    - Test 1 (`renders both card titles + section header`): mounting with non-empty WDLs on both sides renders the section h3, both card h3s (`Games without Endgame`, `Games with Endgame`), and the footer `Score Gap (Yes − No)` h3
    - Test 2 (`score percentage matches (W+0.5D)/n on both cards`): with `non_endgame_wdl={wins:10,draws:0,losses:0,total:10,...}` and `endgame_wdl={wins:5,draws:0,losses:5,total:10,...}`, the left card score reads `100%` and the right reads `50%`
    - Test 3 (`footer gap shows signed score_difference`): with `scoreGap.score_difference = 0.07` the `score-gap-difference` element renders `+7%` (positive sign prefixed); with `score_difference = -0.12` it renders `-12%`
    - Test 4 (`empty state when wdl.total === 0`): mounting with `non_endgame_wdl.total = 0` shows the `Not enough data yet` copy in the left card; the right card is unaffected
    - Test 5 (`sig gating: low-n suppresses zone font color`): with `endgame_wdl.total = 5` (below `MIN_GAMES_FOR_RELIABLE_STATS = 10`) and `endgame_score_p_value = null`, the right card's score row falls into the empty-state branch (per the gate `wdl.total >= MIN_GAMES_FOR_RELIABLE_STATS`), confirming the gate path is reachable; AND with `total = 10` + a non-null but `> 0.05` p-value, the score-value element has NO inline `color` style (sig gating off)
    - Test 6 (`sig gating: medium-confidence + colored zone applies font color`): with `endgame_wdl={wins:9,draws:0,losses:1,total:10}` (score 0.90, well outside the [0.45, 0.55] neutral band) and a non-null `endgame_score_p_value < 0.05`, the right card's score-value element has an inline `color` style equal to `ZONE_SUCCESS` (or whatever `scoreZoneColor(0.9)` returns)
  </behavior>
  <action>
    Create `frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx`. Use the same test framework / render helpers as the sibling `EndgameStartVsEndSection.test.tsx` file (Vitest + Testing Library based on the existing test infrastructure — match what that file uses). Construct minimal `EndgamePerformanceResponse` and `ScoreGapMaterialResponse` fixtures inline; only populate the fields the component reads (the unrelated `entry_eval_*` / `entry_expected_score_*` / `material_rows` / `timeline` fields can default to safe zeros / empty arrays).

    Helper: define a `makeData(overrides?)` factory that returns a complete `EndgamePerformanceResponse` with zero defaults for the entry-eval / expected-score fields and `non_endgame_score_p_value: null` / `endgame_score_p_value: null` by default. Use spread + override pattern. Similarly a `makeScoreGap(overrides?)` factory.

    Query by `data-testid` (`tile-games-without-endgame`, `tile-games-with-endgame`, `score-value-no`, `score-value-yes`, `score-gap-difference`, `score-gap-footer`, `perf-section-info`). For inline-style assertions on font color, use `toHaveStyle({ color: ... })` or assert on the `style.color` attribute.

    Cover the six test cases described under behavior. Do NOT test internal implementation details (e.g., do not assert on `deriveLevel` being called); test only observable rendered behavior.

    Place the test file at `frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx` to match sibling test placement.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx` exits 0 with ≥ 6 test cases reported
    - `cd frontend &amp;&amp; npm test -- --run` (full frontend test suite) exits 0 (no regression in sibling tests)
    - `grep -c "score-value-no\|score-value-yes\|score-gap-difference" frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx` returns ≥ 3
    - `grep -c "Not enough data yet" frontend/src/components/charts/__tests__/EndgameGamesWithWithoutSection.test.tsx` returns ≥ 1
  </acceptance_criteria>
  <done>
    All six test cases pass; sibling tests still pass; the new test file follows the local Vitest/Testing-Library conventions of the neighboring test files.
  </done>
</task>

</tasks>

<verification>
- `cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run` all exit 0
- The new component is type-clean against the Plan-02 extended `EndgamePerformanceResponse`
- Section 1 visual contract: No left, Yes right (D-14), shared tile shell, locked 0.15 endgame domain, footer gap bullet with zone-color font only
</verification>

<success_criteria>
- `EndgameGamesWithWithoutSection.tsx` exists and exports `EndgameGamesWithWithoutSection`
- All SEC1-01 through SEC1-06 user-visible behaviors are wired in code and asserted in tests
- Plan 04 can mount-and-swap without further changes to this file
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-03-SUMMARY.md` per the template. Note any minor copy/test-id adjustments made vs the CONTEXT spec.
</output>
