---
phase: 85
plan: 04
type: execute
wave: 3
depends_on:
  - 85-03
files_modified:
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/EndgamePerformanceSection.tsx
  - frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx
autonomous: false
requirements:
  - SEC1-01
  - SEC1-07
must_haves:
  truths:
    - "`Endgames.tsx` mounts `<EndgameGamesWithWithoutSection data={perfData} scoreGap={scoreGapData} />` at the prior `<EndgamePerformanceSection>` site (around line 422)"
    - "The legacy `EndgamePerformanceSection` function is fully removed from `frontend/src/components/charts/EndgamePerformanceSection.tsx` — the file is deleted entirely if nothing useful remains, or trimmed to only the surviving content"
    - "No code path imports `EndgamePerformanceSection` anywhere in the repo"
    - "`npm run knip` exits 0 (no dead exports / unused dependencies introduced)"
    - "`npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run` all exit 0"
    - "`npm run build` exits 0"
    - "`uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check app/ tests/` all exit 0 (no backend regression)"
  artifacts:
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Mount of the new section; legacy import removed"
      contains: "EndgameGamesWithWithoutSection"
    - path: "frontend/src/components/charts/EndgamePerformanceSection.tsx"
      provides: "Legacy section function deleted (file may be deleted entirely if empty after Plan 02 chart move + this section removal)"
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx"
      via: "named import + JSX mount"
      pattern: "EndgameGamesWithWithoutSection"
    - from: "frontend/src/pages/Endgames.tsx"
      to: "(no longer imports EndgamePerformanceSection)"
      via: "import removed"
      pattern: "EndgamePerformanceSection"
---

<objective>
Wire the new Section 1 component into `Endgames.tsx` and delete the legacy `EndgamePerformanceSection` function (SEC1-07). Verify the full CI gate set passes: lint, tsc, vitest, build, knip on the frontend; ruff + ty + pytest on the backend (defensive against Plan-01 contamination). After this plan, the legacy `EndgamePerformanceSection.tsx` file either ceases to exist (if it has no surviving content beyond the deleted section) or contains only unrelated leftovers — but the section function itself is gone, and no import path in the repo references it.

Purpose: Closes the loop on SEC1-01 (the new section is what users actually see) and SEC1-07 (legacy removal, knip-clean). The Wave-3 placement ensures the mount swap happens after the new component (Plan 03) is built and tested.
Output: Updated `Endgames.tsx`, deleted/trimmed `EndgamePerformanceSection.tsx`, deleted/trimmed test file for it, and a fully-green CI gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-CONTEXT.md
@frontend/src/pages/Endgames.tsx
@frontend/src/components/charts/EndgamePerformanceSection.tsx
@frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx
@frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mount EndgameGamesWithWithoutSection in Endgames.tsx and remove legacy import</name>
  <files>frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx around lines 21 (import; after Plan 02 split this is now two lines: one for `EndgamePerformanceSection`, one for `EndgameScoreOverTimeChart`) and line 422 (mount site for the legacy `<EndgamePerformanceSection>`)
    - frontend/src/components/charts/EndgameGamesWithWithoutSection.tsx (the new export from Plan 03)
  </read_first>
  <action>
    Edit `frontend/src/pages/Endgames.tsx`:
    - Remove the `EndgamePerformanceSection` import (post-Plan-02 this is a dedicated line: `import { EndgamePerformanceSection } from '@/components/charts/EndgamePerformanceSection';`). Replace it with `import { EndgameGamesWithWithoutSection } from '@/components/charts/EndgameGamesWithWithoutSection';`. Leave the `EndgameScoreOverTimeChart` import untouched (it survived Plan 02 as a sibling line).
    - At the mount site (post-Plan-02 still around line 422), replace `<EndgamePerformanceSection data={perfData} scoreGap={scoreGapData} />` with `<EndgameGamesWithWithoutSection data={perfData} scoreGap={scoreGapData} />`. Remove the outer wrapper `<div className="charcoal-texture rounded-md p-4">` — the new section provides its own tile shells per-card, so an outer charcoal wrapper would visually double-nest. Keep the surrounding sibling JSX (the `<EndgameStartVsEndSection>` above, the `<EndgameScoreOverTimeChart>` block below, the `<SectionInsightSlot>` after) unchanged.
    - Note: `scoreGap` was optional in the legacy props but is required by the new component (D-10). The current call site passes `scoreGapData` (which is typed `ScoreGapMaterialResponse | undefined` based on the surrounding code). If TS complains, gate the entire `<EndgameGamesWithWithoutSection>` mount inside `{scoreGapData &amp;&amp; (...)}` — match whatever guard the surrounding mounts use (the `<EndgameScoreOverTimeChart>` block at line 424 already uses `{scoreGapData &amp;&amp; scoreGapData.timeline.length > 0 &amp;&amp; (...)}`, suggesting `scoreGapData` is indeed optional in this scope). Use `{scoreGapData &amp;&amp; (<EndgameGamesWithWithoutSection data={perfData} scoreGap={scoreGapData} />)}` to satisfy TS narrowing.

    Do NOT touch the `<EndgameScoreOverTimeChart>` mount (lines 424-431) — Plan 02 already handled its import; its placement is correct.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | (! grep -E "error TS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EndgameGamesWithWithoutSection" frontend/src/pages/Endgames.tsx` returns 2 (one import, one mount)
    - `grep -c "EndgamePerformanceSection" frontend/src/pages/Endgames.tsx` returns 0 (no import, no mount)
    - `grep -c "EndgameScoreOverTimeChart" frontend/src/pages/Endgames.tsx` returns 2 (one import, one mount — unchanged)
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0
    - `cd frontend &amp;&amp; npm run lint` exits 0
  </acceptance_criteria>
  <done>
    The new section is mounted at the legacy site; no import or mount of the legacy section survives in `Endgames.tsx`; tsc and lint pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the legacy EndgamePerformanceSection function and its test file; verify knip + build + full CI</name>
  <files>frontend/src/components/charts/EndgamePerformanceSection.tsx, frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgamePerformanceSection.tsx (post-Plan-02 state — the chart is already gone; only the legacy section function + its imports + `SCORE_GAP_DOMAIN` remain)
    - frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx (the legacy section's test file)
  </read_first>
  <action>
    Delete `frontend/src/components/charts/EndgamePerformanceSection.tsx` entirely if, after Plan 02's chart extraction, the only remaining export was `EndgamePerformanceSection` itself (no other component, hook, or type is exported from this file). If anything else still resides there (it should not, post-Plan-02), trim only the legacy section function and its supporting imports/constants — but verify with `grep "^export" frontend/src/components/charts/EndgamePerformanceSection.tsx` before deciding. If `grep` shows only `export function EndgamePerformanceSection`, delete the file.

    Delete `frontend/src/components/charts/__tests__/EndgamePerformanceSection.test.tsx` (the tests cover the deleted component). If any test inside that file actually tested `EndgameScoreOverTimeChart` (which Plan 02 moved), those tests should already have been migrated to a sibling `EndgameScoreOverTimeChart.test.tsx` in Plan 02; if Plan 02 did not move them and they cover the chart, move them now to `frontend/src/components/charts/__tests__/EndgameScoreOverTimeChart.test.tsx` and update their import path — only then delete the section test file.

    Run `cd frontend &amp;&amp; npm run knip` and ensure exit 0. If knip reports any newly-orphaned exports (e.g., `SCORE_GAP_DOMAIN` was re-exported from elsewhere), remove them. If knip flags unused dependencies in `package.json`, treat as a regression — investigate and fix.

    Run the full backend gate (`uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest`) and the full frontend gate (`cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; npm run knip`) — all must exit 0.

    Do NOT touch any other section component (`EndgameStartVsEndSection`, `EndgameScoreGapSection`, `EndgameClockPressureSection`, etc.). Do NOT touch the `EndgameScoreOverTimeChart` (Plan 02 owns it).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run &amp;&amp; npm run knip &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Either `frontend/src/components/charts/EndgamePerformanceSection.tsx` does not exist OR `grep -c "export function EndgamePerformanceSection\|export const EndgamePerformanceSection" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns 0
    - `grep -rn "EndgamePerformanceSection" frontend/src/ --include='*.ts' --include='*.tsx' | grep -v '^[^:]*:[[:space:]]*\(//\|\*\)'` returns no matches (no live code references; commented references in unrelated files would be filtered by the grep above)
    - `cd frontend &amp;&amp; npm run knip` exits 0
    - `cd frontend &amp;&amp; npm run build` exits 0
    - `cd frontend &amp;&amp; npm test -- --run` exits 0 with the new `EndgameGamesWithWithoutSection.test.tsx` passing and the deleted `EndgamePerformanceSection.test.tsx` not breaking the suite
    - `uv run pytest -x` exits 0 (Plan 01 backend changes still passing post-merge)
    - `uv run ty check app/ tests/` exits 0
  </acceptance_criteria>
  <done>
    Legacy section function and its test file are gone, knip is clean, full frontend + backend CI gates green, the Endgames page renders the new Section 1 with no orphaned legacy code.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual UAT of Section 1 cards + footer gap</name>
  <files>(no files modified — visual UAT only)</files>
  <what-built>
    The new Section 1 ("Games with vs without Endgame") rendered as two side-by-side cards on lg+ (stacked on mobile) with a full-width Score Gap footer bullet, replacing the legacy table at the top of the Endgames page. Backend supplies `non_endgame_score_p_value` so both cards share identical Wilson sig semantics.
  </what-built>
  <how-to-verify>
    1. Start the dev environment: `bin/run_local.sh` (or `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` + `uv run uvicorn app.main:app --reload` + `cd frontend &amp;&amp; npm run dev`).
    2. Visit http://localhost:5173/endgames (or whatever port `npm run dev` reports) — log in or use guest mode with imported games.
    3. Above the "Endgame Start vs End" twin-tile section, confirm:
       - A section h3 `Games with vs without Endgame` with an info icon to its right (hover → legacy two-paragraph popover renders).
       - A sub-tagline `Do you perform better or worse when games reach an endgame?`
       - Two cards on a wide viewport: LEFT = `Games without Endgame`, RIGHT = `Games with Endgame`. Resize to ≤ 1024px width → cards stack vertically with `Games without Endgame` on top.
       - Each card shows a WDL bar + a `Score: NN%` row with the bullet to its right. Hover the score's info icon → popover explains the 0.50 natural anchor.
       - Below the two cards, a full-width tile titled `Score Gap (Yes − No)` with a signed `+N%` or `-N%` label and a bullet anchored at 0.
    4. Sig gating spot-check: pick a card where the WDL is clearly lopsided (e.g., score 70%+ over many games). The score percentage should be tinted green (or red if below the band). Pick a card where score is near 50% (or n &lt; 10) — value should be the default text color, not zone-tinted.
    5. Em-dash audit: skim the section header popover and the per-card score popover — confirm at most one em-dash per paragraph (CLAUDE.md style rule).
    6. Confirm the legacy table is gone (no `Endgame | Games | Win/Draw/Loss | Score | Score Gap` table headers anywhere on the page).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual regressions / copy issues / sig-gating mismatches.</resume-signal>
  <action>Human visual UAT — see the steps in &lt;how-to-verify&gt; above. No automated implementation in this task; the executor pauses for human approval before the plan completes.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual UAT; awaiting human approval"</automated>
  </verify>
  <done>Human approver replies "approved" or files a defect description.</done>
</task>

</tasks>

<verification>
- `cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run &amp;&amp; npm run knip &amp;&amp; npm run build` all exit 0
- `uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest` all exit 0
- Human-verify checkpoint approved
</verification>

<success_criteria>
- New Section 1 mounted in `Endgames.tsx`; legacy section function fully removed (file deleted or trimmed)
- knip exits 0 (SEC1-07)
- Full frontend + backend CI gates green
- Visual UAT confirms card layout, copy, popovers, sig gating, and footer gap behavior match the locked spec
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/85-section-1-games-with-vs-without-endgame-cards/85-04-SUMMARY.md` per the template. Note the file-deletion decision (whether `EndgamePerformanceSection.tsx` was deleted entirely or trimmed), any test-file relocations, and the human-verify result.
</output>
