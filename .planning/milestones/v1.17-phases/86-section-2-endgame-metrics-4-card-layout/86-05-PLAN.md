---
phase: 86
plan: 05
type: execute
wave: 5
depends_on:
  - 86-04
files_modified:
  - frontend/src/components/charts/EndgameMetricsSection.tsx
  - frontend/src/components/charts/__tests__/EndgameMetricsSection.test.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx
  - frontend/src/components/charts/EndgameScoreGapSection.tsx
autonomous: false
requirements:
  - SEC2-01
  - SEC2-02
  - SEC2-03
  - SEC2-04
  - SEC2-06
  - SEC2-07
  - SEC2-09
  - SEC2-10
user_setup: []
must_haves:
  truths:
    - "`EndgameMetricsSection.tsx` exists as the orchestrator: mounts `EndgameMetricCard` for Conv/Parity/Recov + `EndgameSkillCard` in a `relative grid grid-cols-1 lg:grid-cols-3 gap-4` per D-09, with the Skill card at `lg:col-start-2` (under Parity)"
    - "Section sub-question rendered: \"Do you outperform your opponents at converting, holding, and recovering?\" (D-10) — no section-level h3, no section-level InfoPopover"
    - "SVG connector arrows tie Conversion/Parity/Recovery cards to the Skill card via `<ConnectorArrows>` with `leftCardTestId=\"tile-conversion\"`, `middleCardTestId=\"tile-parity\"`, `rightCardTestId=\"tile-recovery\"`, `targetTileTestId=\"tile-endgame-skill\"` (D-09a)"
    - "Card testids on the new layout: `tile-conversion`, `tile-parity`, `tile-recovery`, `tile-endgame-skill` (D-09b)"
    - "Section is mobile-responsive: single-column stacked on small screens with arrows hidden via the existing mobile-bail check in `ConnectorArrows.compute()`"
    - "`Endgames.tsx` mounts `<EndgameMetricsSection data={scoreGapData} />` in place of `<EndgameScoreGapSection data={scoreGapData} />` at the existing site around line 458-461"
    - "Page-level h2 \"Endgame Metrics and ELO\" at `Endgames.tsx:458` gets a new `<InfoPopover>` trigger with content adapted from `EndgameScoreGapSection.tsx:184-235` (D-11) covering both the Endgame Metrics section and the ELO timeline below it"
    - "`EndgameScoreGapSection.tsx` is deleted along with its dependencies (the file is the only consumer of `endgameSkill()` per D-04)"
    - "Stale test mock in `Endgames.overallPerformance.test.tsx:64-65` is updated to mock `EndgameMetricsSection` (or removed if the mock is no longer needed)"
    - "`npm run knip` reports no unused exports / no unused files (legacy section + retired helper fully removed)"
    - "All gates green: `uv run pytest`, `uv run ty check app/ tests/`, `uv run ruff check .`, `npm test`, `npm run lint`, `npm run knip`, `npm run build`"
  artifacts:
    - path: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      provides: "Orchestrator for the 4-card Endgame Metrics layout"
      min_lines: 80
      contains: "tile-endgame-skill"
    - path: "frontend/src/components/charts/__tests__/EndgameMetricsSection.test.tsx"
      provides: "Integration test for the orchestrator's grid + connector arrows + per-card mounts"
      contains: "EndgameMetricsSection"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Mount swap + new h2 InfoPopover"
      contains: "EndgameMetricsSection"
  key_links:
    - from: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      to: "frontend/src/components/charts/EndgameMetricCard.tsx + EndgameSkillCard.tsx + EndgameOverallConnectorArrows.tsx"
      via: "imports and renders each within the 4-card grid; passes derived props from `ScoreGapMaterialResponse`"
      pattern: "<EndgameMetricCard|<EndgameSkillCard|<ConnectorArrows"
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgameMetricsSection"
      via: "swapped mount at the `<EndgameScoreGapSection ...>` site under the h2"
      pattern: "<EndgameMetricsSection"
    - from: "frontend/src/pages/Endgames.tsx h2 'Endgame Metrics and ELO'"
      to: "<InfoPopover>"
      via: "new popover next to the h2 with lifted bucket-taxonomy + mirror-bucket explainer copy"
      pattern: "Endgame Metrics and ELO"
---

<objective>
Land the 4-card Endgame Metrics layout. Create the `EndgameMetricsSection` orchestrator that derives per-card props from `ScoreGapMaterialResponse` and renders the new 4-card grid (Conv/Parity/Recov on row 1, Skill at `lg:col-start-2` on row 2) with the parameterized `ConnectorArrows` tying the three top cards down to the Skill card per D-09. Swap the mount in `Endgames.tsx`, add the page-level h2 InfoPopover per D-11, delete the legacy `EndgameScoreGapSection.tsx`, update the stale test mock that references it, and run the full gate suite (pytest + ty + ruff + npm test + lint + knip + build).

Phase 86 is functionally complete at the end of this plan: the 9 phase requirements (SEC2-01..04, SEC2-06..10) are visible on screen, the legacy table + 4-gauge strip are gone, knip is clean, all CI gates pass.

This plan has one human-verify checkpoint at the end to confirm the visual rendering of the 4-card grid + connector arrows + h2 InfoPopover before declaring Phase 86 done.

Output: 1 new orchestrator + 1 new test file, 2 modified files (`Endgames.tsx` + the stale test mock file), 1 deleted file (`EndgameScoreGapSection.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md
@frontend/src/pages/Endgames.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/components/charts/EndgameScoreGapSection.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/EndgameSkillCard.tsx
@frontend/src/components/charts/EndgameOverallConnectorArrows.tsx
@frontend/src/lib/endgameMetrics.ts
@frontend/src/types/endgames.ts
@frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx
@frontend/src/components/ui/info-popover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EndgameMetricsSection orchestrator</name>
  <files>frontend/src/components/charts/EndgameMetricsSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (Phase 85 orchestrator — entire file is the structural template for `useRef(gridRef)`, `relative grid grid-cols-1 lg:grid-cols-3 gap-4`, the `<ConnectorArrows ...>` mount at the bottom, and the section-level sub-question paragraph)
    - frontend/src/components/charts/EndgameMetricCard.tsx (Plan 04 — the Conv/Parity/Recov card; for its props shape)
    - frontend/src/components/charts/EndgameSkillCard.tsx (Plan 04 — the Skill card; for its props shape)
    - frontend/src/components/charts/EndgameOverallConnectorArrows.tsx (Plan 03 — the parameterized connector-arrows component)
    - frontend/src/lib/endgameMetrics.ts (Plan 03 — for `MIRROR_BUCKET`, `BUCKET_DISPLAY_LABELS_WITH_METRIC`)
    - frontend/src/types/endgames.ts (`ScoreGapMaterialResponse` with the new Skill fields from Plan 02)
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md §D-07 (component structure), §D-09 (grid layout), §D-09a (connector arrows), §D-09b (testids), §D-10 (sub-question), §D-16 (per-card popover copy)
  </read_first>
  <behavior>
    - Component signature: `export function EndgameMetricsSection({ data }: { data: ScoreGapMaterialResponse })`.
    - Sub-question paragraph (D-10): `<p className="text-sm text-muted-foreground">Do you outperform your opponents at converting, holding, and recovering?</p>`. No section-level h3, no section-level InfoPopover (that moves to the page-level h2 in Task 4).
    - Container: `<section data-testid="endgame-metrics-section">` outer wrapper, then the sub-question paragraph, then the grid.
    - Grid (D-09): `<div ref={gridRef} className="relative grid grid-cols-1 lg:grid-cols-3 gap-4 mt-2">` — same shape as Phase 85 at `EndgameOverallPerformanceSection.tsx:125-128`.
    - Derive per-card data:
      - `totalMaterialGames = data.material_rows.reduce((sum, r) => sum + r.games, 0);`
      - `rowByBucket: Partial<Record<MaterialBucket, MaterialRow>> = {}; for (const r of data.material_rows) rowByBucket[r.bucket] = r;`
      - For each bucket b in `['conversion', 'parity', 'recovery']`: `row = rowByBucket[b]`; `mirror = rowByBucket[MIRROR_BUCKET[b]]`; `sharePct = totalMaterialGames > 0 ? (row.games / totalMaterialGames) * 100 : 0`.
    - Mount three `<EndgameMetricCard>` instances:
      - Conversion: `<EndgameMetricCard bucket="conversion" row={rowByBucket.conversion!} mirror={rowByBucket.recovery} sharePct={...} metricName="Conversion" metricExplanation="Your win rate (only wins count) when you entered the endgame with a Stockfish eval ≥ +1.0, compared to your opponents in the mirror bucket. Filter-responsive." tileTestId="tile-conversion" />`
      - Parity: same shape, `metricExplanation="Your chess score (wins + ½ draws) when you entered the endgame with an eval between −1.0 and +1.0, compared to your opponents in the mirror bucket. Filter-responsive."`, `tileTestId="tile-parity"`.
      - Recovery: `metricExplanation="Your save rate (wins + draws count) when you entered the endgame with an eval ≤ −1.0, compared to your opponents in the mirror bucket. Filter-responsive."`, `tileTestId="tile-recovery"`.
    - Mount `<EndgameSkillCard>` with `className="lg:col-start-2"` wrapper (D-09 — Skill at column 2 on desktop, naturally below Parity):
      - `<EndgameSkillCard skill={data.skill ?? null} oppSkill={data.opp_skill ?? null} totalGames={totalMaterialGames} pValue={data.skill_diff_p_value ?? null} ciLow={data.skill_diff_ci_low ?? null} ciHigh={data.skill_diff_ci_high ?? null} tileTestId="tile-endgame-skill" />`
      - Wrapper: `<div className="lg:col-start-2"> ... </div>` so the Skill card spans only the middle column on lg+ but stacks naturally on mobile.
    - Mount `<ConnectorArrows>` at the end of the grid (mirror `EndgameOverallPerformanceSection.tsx:251`): `<ConnectorArrows containerRef={gridRef} leftCardTestId="tile-conversion" middleCardTestId="tile-parity" rightCardTestId="tile-recovery" targetTileTestId="tile-endgame-skill" />`.
    - Defensive: when `rowByBucket[bucket]` is undefined (response shape error), render a card with a synthesized zero-row (use `row.games === 0` empty-state branch in `EndgameMetricCard`). This shouldn't happen in production but keeps the component robust to test fixtures.
    - Card ordering (DOM): Conv → Parity → Recov → Skill. On lg+ the grid auto-places them as Conv(col1)/Parity(col2)/Recov(col3)/Skill(col2 via lg:col-start-2). On mobile (grid-cols-1), they stack in DOM order.
  </behavior>
  <action>
    Create `frontend/src/components/charts/EndgameMetricsSection.tsx`.

    Header comment block mirroring `EndgameOverallPerformanceSection.tsx:1-23` style: 10-15 lines naming the component (Phase 86 — 4-card Endgame Metrics section), the redesign rationale (replaces `EndgameScoreGapSection` per SEC2-10), the layout (3-col grid on lg+, Skill at col-start-2 on row 2), and the single-bullet doctrine.

    Imports:
    - `import { useRef } from 'react';`
    - `import { ConnectorArrows } from './EndgameOverallConnectorArrows';`
    - `import { EndgameMetricCard } from './EndgameMetricCard';`
    - `import { EndgameSkillCard } from './EndgameSkillCard';`
    - `import { MIRROR_BUCKET } from '@/lib/endgameMetrics';`
    - `import type { MaterialBucket, MaterialRow, ScoreGapMaterialResponse } from '@/types/endgames';`

    Function body: gridRef setup, derive `totalMaterialGames` + `rowByBucket`, then render the section wrapper + sub-question + grid + 4 cards + connector arrows per the behavior block.

    Handle missing `rowByBucket[b]` defensively: render a synthesized zero-row `MaterialRow` (bucket, label = '', games = 0, win_pct/draw_pct/loss_pct/score = 0, opponent_score = null, opponent_games = 0, diff_* = null) so `EndgameMetricCard`'s empty-state branch fires cleanly. Alternatively skip rendering that card entirely — choose the simpler path.

    Use named export only.

    Per CLAUDE.md em-dash rule: prefer commas / colons in the header comment prose.

    Verify every card's `tileTestId` matches the D-09b lock: `tile-conversion`, `tile-parity`, `tile-recovery`, `tile-endgame-skill`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/charts/EndgameMetricsSection.tsx` exits 0
    - `grep -c "tile-conversion\|tile-parity\|tile-recovery\|tile-endgame-skill" frontend/src/components/charts/EndgameMetricsSection.tsx` returns at least 8 (testids on each card prop + the four ConnectorArrows props)
    - `grep -c "<EndgameMetricCard" frontend/src/components/charts/EndgameMetricsSection.tsx` returns 3 (Conv, Parity, Recov)
    - `grep -c "<EndgameSkillCard" frontend/src/components/charts/EndgameMetricsSection.tsx` returns 1
    - `grep -c "<ConnectorArrows" frontend/src/components/charts/EndgameMetricsSection.tsx` returns 1
    - `grep -c "lg:col-start-2" frontend/src/components/charts/EndgameMetricsSection.tsx` returns at least 1 (Skill wrapper class)
    - `grep -c "Do you outperform your opponents" frontend/src/components/charts/EndgameMetricsSection.tsx` returns 1 (D-10 sub-question)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Orchestrator exists with the 4-card grid, D-10 sub-question, D-09 column layout, D-09a connector-arrows mount with the four Phase 86 testids, D-09b card testids on every card, defensive empty-row handling, tsc + lint green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Vitest integration test for EndgameMetricsSection</name>
  <files>frontend/src/components/charts/__tests__/EndgameMetricsSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx (Phase 85 integration-test patterns — mock data builders, render assertion patterns, ConnectorArrows interaction)
    - frontend/src/components/charts/EndgameMetricsSection.tsx (the just-built orchestrator)
    - frontend/src/types/endgames.ts (`ScoreGapMaterialResponse` shape — for the test fixture)
  </read_first>
  <behavior>
    - 3-4 test cases:
      1. **Full-rendering case**: build a `ScoreGapMaterialResponse` with all 3 buckets active (100 games each, healthy diffs), `skill = 0.65`, `opp_skill = 0.55`, all sig fields populated. Render section. Assert: all 4 card testids present (`tile-conversion`, `tile-parity`, `tile-recovery`, `tile-endgame-skill`). Assert sub-question text "Do you outperform your opponents at converting, holding, and recovering?" is present.
      2. **Skill gating**: build response with one active bucket only (conversion has games, parity/recovery rows have games = 0). `data.skill_diff_p_value = null`, `data.skill_diff_ci_low = null`, etc. Render. Assert: `tile-endgame-skill` is present but renders the "Not enough data yet" state (assert via `getByText`).
      3. **Card ordering**: assert that DOM order of the 4 cards is Conversion → Parity → Recovery → Skill (use `screen.getAllByTestId(/^tile-/)` and check ordering, or use `getByTestId` with `compareDocumentPosition`).
      4. **(Optional)** Connector-arrows mount: assert the connector-arrows wrapper is present (testid on the wrapper if accessible — the wrapper currently has no testid, but the `aria-hidden="true"` SVG container is detectable; skip if too brittle).
    - Tests mock `EndgameGauge`, `MiniBulletChart`, `MiniWDLBar`, `MetricStatPopover` minimally (or not at all — the Phase 85 test file lets them render real DOM). Mirror the Phase 85 test pattern.
  </behavior>
  <action>
    Create `frontend/src/components/charts/__tests__/EndgameMetricsSection.test.tsx`.

    Imports same shape as `EndgameOverallPerformanceSection.test.tsx`: `describe`, `it`, `expect` from `vitest`; `render`, `screen` from `@testing-library/react`; the component under test.

    Build a `buildScoreGapResponse(overrides?: Partial<ScoreGapMaterialResponse>): ScoreGapMaterialResponse` factory defaulting to a well-populated 3-bucket response (W/D/L per bucket, `opponent_games = 100`, sig fields populated, `skill = 0.65`, `opp_skill = 0.55`, `skill_diff_p_value = 0.001`, CI bounds bracketing 0.10).

    Write 3 test methods per the behavior block above. Use `screen.getByTestId('tile-endgame-skill')` etc. for testid assertions.

    Skip detailed connector-arrows assertions (geometry is exercised indirectly via Phase 85 tests + the live integration check in Task 5).

    Match the Phase 85 test file's `describe` + `it` structure and naming style.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run EndgameMetricsSection</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/charts/__tests__/EndgameMetricsSection.test.tsx` exits 0
    - `cd frontend && npm test -- --run EndgameMetricsSection` exits 0 and reports at least 3 tests passed
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Three integration tests cover full-render / Skill gating / card ordering; all pass; lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap mount in Endgames.tsx + add page-level h2 InfoPopover (D-11)</name>
  <files>frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx lines 22-30 (imports block — current `EndgameScoreGapSection` import at line 24 needs replacement)
    - frontend/src/pages/Endgames.tsx lines 455-475 (the h2 "Endgame Metrics and ELO" + the legacy section mount at line 458-461 — Task 3 mount swap + Task 3 h2 InfoPopover addition both happen here)
    - frontend/src/components/charts/EndgameScoreGapSection.tsx lines 184-235 (the lifted `<InfoPopover>` content for the page-level h2 trigger per D-11 — bucket taxonomy + mirror-bucket explainer)
    - frontend/src/components/ui/info-popover.tsx (existing `<InfoPopover>` API; mirror the trigger pattern used at `Endgames.tsx:671` etc.)
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md §D-11 (page-level h2 popover scope)
  </read_first>
  <behavior>
    - Replace the import at line 24 (`import { EndgameScoreGapSection } from '@/components/charts/EndgameScoreGapSection';`) with `import { EndgameMetricsSection } from '@/components/charts/EndgameMetricsSection';`.
    - Replace the mount at line 460 (`<EndgameScoreGapSection data={scoreGapData} />`) with `<EndgameMetricsSection data={scoreGapData} />`.
    - Add an `<InfoPopover>` trigger inline with the h2 at line 458 per D-11. The popover content is the bucket taxonomy + mirror-bucket explainer block from `EndgameScoreGapSection.tsx:184-235` (the legacy section-level InfoPopover content), adapted slightly so it covers both the Endgame Metrics section and the ELO timeline below it. The h2 becomes: `<h2 className="text-lg font-semibold text-foreground mt-2"><span className="inline-flex items-center gap-1">Endgame Metrics and ELO <InfoPopover ariaLabel="Endgame Metrics and ELO info" testId="endgame-metrics-and-elo-info" side="top"> ...lifted content... </InfoPopover></span></h2>`. (Mirror the `inline-flex items-center gap-1` wrapper from `EndgameScoreGapSection.tsx:182`.)
    - InfoPopover content adapted from `EndgameScoreGapSection.tsx:189-233`: keep the bucket taxonomy paragraph, the per-bucket rate ul, the "These rates are not the same as the generic Score" sentence, and the gauges-vs-table explainer. The "table" paragraph from line 226-232 is dropped (no table anymore) and replaced with: "The per-card peer bullet compares your rate to your actual opponents' rate in the mirror bucket (e.g. your Conversion vs their Conversion in the games where you faced them, which is your Recovery games, flipped). This baseline is self-calibrating and shifts with filters like Opponent Strength. Hidden when the opponent sample is smaller than 10 games." Add a final sentence noting the ELO timeline below uses the same Skill metric: "The Endgame ELO timeline below uses the same Skill composite to adjust your rating by per-week endgame performance."
    - Do NOT modify the `EndgameEloTimelineSection` mount or the `SectionInsightSlot sectionId="metrics_elo"` mount.
    - The h2 InfoPopover content remains text-xs-tolerant per the MetricStatPopover exception in CLAUDE.md (InfoPopover inherits the same hover-popover pattern).
  </action>
  <action>
    Edit `frontend/src/pages/Endgames.tsx`.

    1. Update import at line 24: replace `import { EndgameScoreGapSection } from '@/components/charts/EndgameScoreGapSection';` with `import { EndgameMetricsSection } from '@/components/charts/EndgameMetricsSection';`. Keep the import alphabetical position (E... already alphabetical).

    2. Update mount at line 460: replace `<EndgameScoreGapSection data={scoreGapData} />` with `<EndgameMetricsSection data={scoreGapData} />`.

    3. Update h2 at line 458 per D-11. Current line: `<h2 className="text-lg font-semibold text-foreground mt-2">Endgame Metrics and ELO</h2>`. Wrap "Endgame Metrics and ELO" in `<span className="inline-flex items-center gap-1">...</span>` and append an `<InfoPopover>` trigger with the adapted bucket-taxonomy content (per the behavior block above). Match the popover trigger style used at `Endgames.tsx:671` (where another h2-level `<InfoPopover>` already lives).

    4. The lifted popover body is a `<div className="space-y-2">` containing 5 paragraphs (taxonomy intro, per-bucket rate ul, "not the same as Score" note, gauges-vs-table explainer adapted, ELO-composite-uses-Skill note). Copy the relevant blocks from `EndgameScoreGapSection.tsx:189-233` verbatim, drop the "table" paragraph (lines 226-232), and substitute the per-card peer-bullet paragraph per the behavior block above.

    Do NOT change any other imports, hooks, JSX blocks, or the `EndgameEloTimelineSection` mount at line 466-470.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EndgameMetricsSection" frontend/src/pages/Endgames.tsx` returns at least 2 (import + mount)
    - `grep -c "EndgameScoreGapSection" frontend/src/pages/Endgames.tsx` returns 0 (legacy import + mount removed)
    - `grep -c "endgame-metrics-and-elo-info" frontend/src/pages/Endgames.tsx` returns 1 (new InfoPopover testid)
    - `grep -c "InfoPopover" frontend/src/pages/Endgames.tsx` is +1 vs baseline (one new InfoPopover added)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Mount swapped; legacy import removed; page-level h2 InfoPopover added next to "Endgame Metrics and ELO" with lifted-and-adapted content covering both sections; tsc + lint green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete EndgameScoreGapSection.tsx + update stale test mock</name>
  <files>frontend/src/components/charts/EndgameScoreGapSection.tsx, frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameScoreGapSection.tsx (entire file — to confirm there are no remaining external consumers before deletion)
    - frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx lines 64-65 (`vi.mock('@/components/charts/EndgameScoreGapSection', () => ({ EndgameScoreGapSection: () => <div data-testid="mock-endgame-score-gap-section" /> }));` — needs to mock `EndgameMetricsSection` instead, or be removed if no longer needed)
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md §SEC2-10 + §D-08 (deletion mandate; verify helpers were lifted in Plan 03 and `endgameSkill()` is dead code)
  </read_first>
  <behavior>
    - Delete `frontend/src/components/charts/EndgameScoreGapSection.tsx` entirely (per SEC2-10 + D-08).
    - The frontend `endgameSkill()` helper at the legacy file's lines 155-165 is implicitly retired (D-04) — no other consumer.
    - Update `Endgames.overallPerformance.test.tsx:64-65` to mock `EndgameMetricsSection` instead: change the import path to `'@/components/charts/EndgameMetricsSection'` and the mock factory to return `{ EndgameMetricsSection: () => <div data-testid="mock-endgame-metrics-section" /> }`. Also update any test assertions that reference `mock-endgame-score-gap-section` to `mock-endgame-metrics-section`.
    - Confirm `npm run knip` reports no orphans: all 13 helpers lifted to `endgameMetrics.ts` in Plan 03 are now consumed (by `EndgameMetricCard` / `EndgameSkillCard` / `EndgameMetricsSection`).
  </behavior>
  <action>
    1. Delete `frontend/src/components/charts/EndgameScoreGapSection.tsx` using `rm`. No git rm — git tracks the deletion via `git status` automatically.

    2. Edit `frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx`:
       - Replace the mock at lines 64-65: change `'@/components/charts/EndgameScoreGapSection'` to `'@/components/charts/EndgameMetricsSection'`, `EndgameScoreGapSection` to `EndgameMetricsSection`, and `mock-endgame-score-gap-section` to `mock-endgame-metrics-section`.
       - Search the rest of the file for any other reference to `EndgameScoreGapSection` or `mock-endgame-score-gap-section` and update / remove them.

    3. Run a workspace search: `grep -rn "EndgameScoreGapSection\|mock-endgame-score-gap-section\|endgame-gauge-strip\|material-table\|material-row-\|material-card-\|endgame-skill-card" frontend/src/` and fix any remaining references (likely none after the mount swap + test mock update; the legacy testids `material-row-{bucket}`, `material-card-{bucket}`, `endgame-skill-card`, `endgame-gauge-strip`, `material-table`, `score-gap-section` go away with the file).

    4. Confirm `endgameSkill()` is gone (it lived only in the deleted file).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/charts/EndgameScoreGapSection.tsx` exits non-zero (file deleted)
    - `grep -rn "EndgameScoreGapSection" frontend/src/` returns no matches
    - `grep -rn "endgameSkill\(" frontend/src/` returns no matches (helper retired)
    - `grep -rn "mock-endgame-score-gap-section\|material-row-conversion\|material-row-parity\|material-row-recovery\|material-card-\|endgame-gauge-strip\|material-table\|score-gap-section\b" frontend/src/` returns no matches (legacy testids gone)
    - `grep -c "EndgameMetricsSection" frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx` returns at least 2 (mock path + factory)
    - `cd frontend && npm run knip` exits 0 with no unused exports / no unused files reported on the affected modules
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm test -- --run` exits 0 (full frontend test suite green)
  </acceptance_criteria>
  <done>
    Legacy file deleted; stale test mock updated to point at the new orchestrator; no orphans (knip clean); all frontend gates green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify the 4-card layout + connector arrows + h2 InfoPopover</name>
  <files>frontend/src/pages/Endgames.tsx (rendered in browser; no code change in this task)</files>
  <action>Pause for human review. Do not proceed to Task 6 until the user confirms the visual checks below pass. The executor performs no file edits in this task; it only stages the dev environment (steps 1-2 below) if not already running.</action>
  <verify>
    <automated>echo "Checkpoint — human verification required; awaiting resume-signal"</automated>
  </verify>
  <done>User types "approved" after visually verifying the 4-card layout, connector arrows, popovers, and legacy removal per the steps below.</done>
  <what-built>
    - 4-card Endgame Metrics section: Conv / Parity / Recov on row 1, Endgame Skill at col-start-2 on row 2 (desktop). All four stacked single-column on mobile.
    - Connector arrows tying Conv (left), Parity (top), Recov (right) into the Skill card.
    - Page-level h2 "Endgame Metrics and ELO" InfoPopover next to the heading; opens on hover/tap with bucket-taxonomy + mirror-bucket explainer + ELO-uses-same-Skill note.
    - Per-card MetricStatPopover with locked D-16 copy.
    - Legacy 4-gauge strip + eval-stratified WDL table fully removed.
  </what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` in one terminal; `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` first if the dev DB isn't running; `uv run uvicorn app.main:app --reload` in another.
    2. Open http://localhost:5173/endgames (sign in or use guest mode; import a few games if a fresh DB).
    3. Scroll to the "Endgame Metrics and ELO" h2.
    4. Verify: on desktop (≥1024px wide), the 4 cards arrange as Conv | Parity | Recov on row 1, with Endgame Skill alone in the middle column on row 2. SVG arrows visibly connect each of the 3 top cards down to the Skill card (left card → Skill left edge with right-pointing arrow, middle → Skill top with down-pointing arrow, right card → Skill right edge with left-pointing arrow).
    5. Resize the browser to mobile width (<1024px). Verify the 4 cards stack single-column in DOM order (Conv → Parity → Recov → Skill) and the connector arrows are hidden.
    6. On any Conv/Parity/Recov card, hover over the HelpCircle popover trigger next to "Diff:". Verify the MetricStatPopover opens with the per-card explanation + methodology block.
    7. On the Skill card, do the same — verify the Skill popover renders the composite explanation.
    8. Hover the new InfoPopover next to the h2 "Endgame Metrics and ELO". Verify the bucket-taxonomy paragraph + mirror-bucket explainer + ELO-Skill-composite sentence are all present.
    9. Apply a filter (e.g. Opponent Strength: Stronger). Verify the peer bullets update (the You / Opp / Diff values change; the gauge stays the same — gauge bands are fixed per D-13 / SEC2-04).
    10. Confirm the legacy 4-gauge strip + eval-stratified table are gone (only the 4 new cards visible under the h2).
  </how-to-verify>
  <resume-signal>Type "approved" if the layout + arrows + popovers render correctly. If issues, describe them and the executor will iterate.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Full gate sweep — pytest + ty + ruff + npm test + lint + knip + build</name>
  <files></files>
  <read_first>
    - CLAUDE.md (gate commands — `uv run ruff check .`, `uv run ty check app/ tests/`, `uv run pytest`, `npm test`, `npm run lint`, `npm run knip`, `npm run build`)
  </read_first>
  <behavior>
    - All seven gates green:
      - `uv run ruff check .`
      - `uv run ruff format --check .`
      - `uv run ty check app/ tests/`
      - `uv run pytest`
      - `cd frontend && npm test -- --run`
      - `cd frontend && npm run lint`
      - `cd frontend && npm run knip`
      - `cd frontend && npm run build`
    - No new warnings introduced (compare to baseline before Phase 86 if practical).
    - All 9 phase requirements satisfied (SEC2-01..04, SEC2-06..10).
  </behavior>
  <action>
    Run each gate command in sequence. For any failure, fix in place (do NOT defer to follow-up phases). Pay particular attention to:

    1. `npm run knip` — the legacy file's deletion removed several testids (`material-row-*`, `material-card-*`, etc.) plus the `endgameSkill()` helper. knip may flag any unused-export it spots; resolve by either removing the dead export from `endgameMetrics.ts` (only if it's truly unused — likely all 13 are now consumed by the orchestrator + cards) or by adding the missing consumer.

    2. `npm run build` — final production build catches anything tsc + lint missed (e.g. circular imports, dynamic-import warnings).

    3. `uv run pytest` — backend changes (Plan 01 + Plan 02) need to still pass on the full suite, not just the targeted test files.

    Run `cd frontend && npm run knip 2>&1 | head -30` first to see if there are pre-existing knip warnings unrelated to this phase — if so, leave them alone and only resolve new ones.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest &amp;&amp; cd frontend &amp;&amp; npm test -- --run &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All seven gate commands exit 0 individually and as a compound command
    - `cd frontend && npm run knip` reports no unused exports / no unused files attributable to Phase 86
    - The legacy file `EndgameScoreGapSection.tsx` is not in the build output (verify via `find frontend/dist -name "*.js" | xargs grep -l EndgameScoreGapSection 2>/dev/null` returning empty)
    - `git status` shows the expected file changes: 5 new files added (lib/endgameMetrics.ts, EndgameMetricCard.tsx, EndgameSkillCard.tsx, EndgameMetricsSection.tsx, 3 new test files), 4 modified files (Endgames.tsx, EndgameOverallConnectorArrows.tsx, EndgameOverallPerformanceSection.tsx, Endgames.overallPerformance.test.tsx), 1 deleted file (EndgameScoreGapSection.tsx)
  </acceptance_criteria>
  <done>
    All gates green; phase functionally complete.
  </done>
</task>

</tasks>

<verification>
- All seven gates exit 0: `uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest && cd frontend && npm test -- --run && npm run lint && npm run knip && npm run build`
- `grep -rn "EndgameScoreGapSection\|endgameSkill\(" frontend/src/` returns no matches
- The Endgames page renders the 4-card layout + connector arrows + h2 InfoPopover per the human-verify checkpoint
- SEC2-01..04 + SEC2-06..10 (9 phase requirements) all addressable on screen and via inspector
</verification>

<success_criteria>
- 4-card Endgame Metrics section ships at `Endgames.tsx:458-461` site; Conv / Parity / Recov / Skill cards mounted with the locked testids
- Connector arrows from row 1 cards to the Skill card on desktop, hidden on mobile
- Page-level h2 InfoPopover lifted from the legacy section-level popover
- Legacy `EndgameScoreGapSection.tsx` deleted; knip clean
- All seven gates green
- Per-card MetricStatPopover content matches D-16 locked copy
- Mobile-responsive (375px parity is a Phase 88 polish target; this plan delivers the structural responsive behavior — 375px deep-dive is deferred)
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-05-SUMMARY.md` capturing:
- Phase 86 done; 9 of 9 SEC2 requirements complete (SEC2-01..04, SEC2-06..10)
- v1.17 ROADMAP description "Frontend-only refactor" is now formally violated by Phase 86 D-01..D-03 + D-06 (5 Skill schema fields + 1 helper + 3 per-MaterialRow diff fields). User-authorized; same caveat as Phase 85 D-01. Recommend amending the milestone description at `/gsd-complete-milestone` time.
- Connector-arrows file kept its existing name (`EndgameOverallConnectorArrows.tsx`); the file now serves both Phase 85 and Phase 86 call sites via 4 testid props. Phase 87 (per-type cards) can reuse it again or branch a different geometry per its layout needs.
- POLISH-01 / POLISH-02 deferred to Phase 88 per CONTEXT D-12 / D-13. Phase 88 will also do the 375px mobile parity check across all 3 sections.
- Phase status: ready for merge.
</output>
