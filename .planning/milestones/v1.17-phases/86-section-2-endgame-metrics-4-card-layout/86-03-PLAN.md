---
phase: 86
plan: 03
type: execute
wave: 3
depends_on:
  - 86-02
files_modified:
  - frontend/src/lib/endgameMetrics.ts
  - frontend/src/components/charts/EndgameOverallConnectorArrows.tsx
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
autonomous: true
requirements:
  - SEC2-04
  - SEC2-07
must_haves:
  truths:
    - "`frontend/src/lib/endgameMetrics.ts` exists and exports `MIRROR_BUCKET`, `userRate`, `opponentRate`, `formatScorePct`, `formatDiffPct`, `BUCKET_DISPLAY_LABELS`, `BUCKET_DISPLAY_LABELS_WITH_METRIC`, `NEUTRAL_ZONE_MIN`, `NEUTRAL_ZONE_MAX`, `BULLET_DOMAIN`, `MIN_OPPONENT_BASELINE_GAMES`, `FIXED_GAUGE_ZONES`, `ENDGAME_SKILL_ZONES` — lifted verbatim from `EndgameScoreGapSection.tsx` (D-08 + D-12)"
    - "`EndgameScoreGapSection.tsx` still exists and still works (untouched in this plan — deletion happens in Plan 86-05) but the helpers used inside it are now also exported from `lib/endgameMetrics.ts`; the legacy file may continue to declare its private copies until deletion"
    - "`EndgameOverallConnectorArrows.tsx` accepts 4 new props `leftCardTestId`, `middleCardTestId`, `rightCardTestId`, `targetTileTestId` (per D-09a) and uses them in its `container.querySelector` calls instead of the hard-coded `tile-games-without-endgame` / `tile-at-endgame-entry` / `tile-games-with-endgame` / `endgame-score-differences` selectors"
    - "`EndgameOverallPerformanceSection.tsx` passes the four Phase 85 testids explicitly (`tile-games-without-endgame`, `tile-at-endgame-entry`, `tile-games-with-endgame`, `endgame-score-differences`) — no behavior change"
    - "Phase 85's frontend tests still pass (the connector-arrows component is exercised indirectly via EndgameOverallPerformanceSection.test.tsx)"
  artifacts:
    - path: "frontend/src/lib/endgameMetrics.ts"
      provides: "Shared helpers + constants for Phase 86 cards + Phase 87 per-type cards"
      min_lines: 80
      contains: "MIRROR_BUCKET"
    - path: "frontend/src/components/charts/EndgameOverallConnectorArrows.tsx"
      provides: "Parameterized `ConnectorArrows` component with 4 testid props"
      contains: "leftCardTestId"
    - path: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      provides: "Phase 85 call site passing the 4 legacy testids explicitly"
      contains: "leftCardTestId="
  key_links:
    - from: "frontend/src/lib/endgameMetrics.ts"
      to: "frontend/src/generated/endgameZones.ts"
      via: "import of `FIXED_GAUGE_ZONES` + `ENDGAME_SKILL_ZONES` registry values + `colorizeGaugeZones` from theme"
      pattern: "from '@/generated/endgameZones'"
    - from: "frontend/src/components/charts/EndgameOverallConnectorArrows.tsx::ConnectorArrows"
      to: "container.querySelector with prop-driven testids"
      via: "Template-literal selectors `[data-testid=\"${leftCardTestId}\"]`"
      pattern: "querySelector.*data-testid.*\\$\\{"
    - from: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      to: "<ConnectorArrows ... leftCardTestId=\"tile-games-without-endgame\" ...>"
      via: "Phase 85's existing 4 card testids passed explicitly so behavior is unchanged"
      pattern: "leftCardTestId=\"tile-games-without-endgame\""
---

<objective>
Prepare the frontend ground for Phase 86's card components. Two parallel-safe scaffolding steps:

1. **Lift shared helpers** (D-08): create `frontend/src/lib/endgameMetrics.ts` containing `MIRROR_BUCKET`, `userRate`, `opponentRate`, `formatScorePct`, `formatDiffPct`, `BUCKET_DISPLAY_LABELS`, `BUCKET_DISPLAY_LABELS_WITH_METRIC`, neutral-band + domain constants (`NEUTRAL_ZONE_MIN/MAX`, `BULLET_DOMAIN`), `MIN_OPPONENT_BASELINE_GAMES`, `FIXED_GAUGE_ZONES`, `ENDGAME_SKILL_ZONES` — exact verbatim copies from `EndgameScoreGapSection.tsx:38-146`. The legacy file keeps its private declarations untouched in this plan (deletion happens in 86-05); the lift is forward-looking so the new cards in Plan 04 can import from one place and Phase 87 can reuse the same module for per-type cards.

2. **Generalize connector arrows** (D-09a): add 4 testid props (`leftCardTestId`, `middleCardTestId`, `rightCardTestId`, `targetTileTestId`) to `EndgameOverallConnectorArrows.tsx` so the same geometry serves both Phase 85's three-card-row + score-differences-below-middle and Phase 86's Conv/Parity/Recov + Skill-below-Parity. Update the Phase 85 call site in `EndgameOverallPerformanceSection.tsx:251` to pass the four existing testids explicitly. Keep the file name (per the planner-chooses note in D-09a — keeping the existing name minimizes diff surface; Phase 86 imports it under a local alias if desired).

Purpose: Plan 04 (card components) and Plan 05 (orchestrator + mount swap) consume both outputs. Splitting this scaffolding off keeps each card-building plan focused on UI rather than refactoring.

Output: 1 new lib module (~80-100 LOC), 2 modified components (~10 LOC delta each).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md
@frontend/src/components/charts/EndgameScoreGapSection.tsx
@frontend/src/components/charts/EndgameOverallConnectorArrows.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/types/endgames.ts
@frontend/src/generated/endgameZones.ts
@frontend/src/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/endgameMetrics.ts with helpers lifted from EndgameScoreGapSection</name>
  <files>frontend/src/lib/endgameMetrics.ts</files>
  <read_first>
    - frontend/src/components/charts/EndgameScoreGapSection.tsx lines 38-165 (the verbatim source for all helpers + constants — copy these blocks)
    - frontend/src/generated/endgameZones.ts (the `FIXED_GAUGE_ZONES` + `ENDGAME_SKILL_ZONES` registry values that the helpers wrap with `colorizeGaugeZones`)
    - frontend/src/lib/theme.ts (`colorizeGaugeZones`, `GaugeZone` type, `ZONE_DANGER`/`ZONE_NEUTRAL`/`ZONE_SUCCESS` — already imported by the legacy file)
    - frontend/src/types/endgames.ts (the `MaterialBucket` and `MaterialRow` types — already imported by the legacy file)
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md §D-08 + §D-12 (exact list of helpers + constants to lift)
  </read_first>
  <behavior>
    - Create `frontend/src/lib/endgameMetrics.ts` containing:
      - `MIRROR_BUCKET: Record<MaterialBucket, MaterialBucket>` (verbatim from line 114-118)
      - `userRate(row: MaterialRow): number` (verbatim from line 127-131)
      - `opponentRate(row: MaterialRow, mirror: MaterialRow | undefined): number | null` (verbatim from line 141-146)
      - `formatScorePct(score: number): string` (verbatim from line 96-98)
      - `formatDiffPct(userR: number, oppR: number): string` (verbatim from line 106-109)
      - `BUCKET_DISPLAY_LABELS: Record<MaterialBucket, string>` (verbatim from line 58-62)
      - `BUCKET_DISPLAY_LABELS_WITH_METRIC: Record<MaterialBucket, string>` (verbatim from line 68-72)
      - `NEUTRAL_ZONE_MIN = -0.05` and `NEUTRAL_ZONE_MAX = 0.05` (verbatim from line 42-43, with header comment from line 38-41)
      - `BULLET_DOMAIN = 0.20` (verbatim from line 48, with header comment)
      - `MIN_OPPONENT_BASELINE_GAMES = 10` (verbatim from line 53)
      - `FIXED_GAUGE_ZONES: Record<MaterialBucket, GaugeZone[]>` (verbatim from line 83-87, including the explanatory header comment from lines 74-82)
      - `ENDGAME_SKILL_ZONES: GaugeZone[]` (verbatim from line 93)
    - Top-of-file docstring explains the module's purpose ("Shared helpers + constants for Phase 86 Endgame Metrics cards and Phase 87 per-type cards. Lifted from `EndgameScoreGapSection.tsx` per Phase 86 D-08 so the new cards and the legacy section can share one source of truth during the transition; legacy file keeps its private copies until deletion in Plan 86-05.") and references D-04 (frontend `endgameSkill()` is NOT lifted — it is retired in favor of the backend `skill` / `opp_skill` fields).
    - Module is named exports only (no default export).
    - Do not include `endgameSkill()` from line 155-165 — that helper is retired per D-04.
  </behavior>
  <action>
    Create new file `frontend/src/lib/endgameMetrics.ts`. Copy the helper bodies and constant declarations from `EndgameScoreGapSection.tsx` verbatim, preserving the explanatory header comments where they exist. The function bodies, constant values, and types must match the legacy declarations byte-for-byte; the only allowed differences are:
    - Add a top-of-file docstring (see behavior block) referencing Phase 86 D-08, D-12, and noting that `endgameSkill()` is intentionally not lifted per D-04.
    - Imports: `import type { GaugeZone } from '@/lib/theme';` and `import { colorizeGaugeZones } from '@/lib/theme';` (split type + value imports). `import { FIXED_GAUGE_ZONES as REGISTRY_FIXED_GAUGE_ZONES, ENDGAME_SKILL_ZONES as REGISTRY_ENDGAME_SKILL_ZONES } from '@/generated/endgameZones';`. `import type { MaterialBucket, MaterialRow } from '@/types/endgames';`. (Mirror the import style of `EndgameScoreGapSection.tsx:21-36`.)
    - All declarations use `export` (named export) — no default export.

    Order matters for readability: header docstring → imports → display-label constants → format helpers → mirror map + rate helpers → neutral-band + domain constants → opponent threshold → gauge-zone constants.

    Do NOT lift `endgameSkill()` (line 155-165) — that function is retired in Plan 04+ in favor of server-side `skill` / `opp_skill` per D-04.

    Do NOT touch `EndgameScoreGapSection.tsx` in this task. The lift is forward-only; legacy file keeps its private declarations and is deleted in Plan 86-05.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/lib/endgameMetrics.ts` exits 0
    - `grep -c "^export " frontend/src/lib/endgameMetrics.ts` returns at least 13 (one per lifted symbol: MIRROR_BUCKET, userRate, opponentRate, formatScorePct, formatDiffPct, BUCKET_DISPLAY_LABELS, BUCKET_DISPLAY_LABELS_WITH_METRIC, NEUTRAL_ZONE_MIN, NEUTRAL_ZONE_MAX, BULLET_DOMAIN, MIN_OPPONENT_BASELINE_GAMES, FIXED_GAUGE_ZONES, ENDGAME_SKILL_ZONES — 13 total, all required)
    - `grep -c "endgameSkill" frontend/src/lib/endgameMetrics.ts` returns 0 (D-04 retirement — not lifted)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
    - `grep -c "MIRROR_BUCKET\|userRate\|opponentRate\|formatScorePct\|formatDiffPct" frontend/src/components/charts/EndgameScoreGapSection.tsx` is unchanged from baseline (legacy file untouched)
  </acceptance_criteria>
  <done>
    Lib module created with 13 verbatim helper exports, legacy file untouched, tsc + lint green. `endgameSkill()` deliberately not lifted (D-04 retirement).
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize EndgameOverallConnectorArrows with 4 testid props</name>
  <files>frontend/src/components/charts/EndgameOverallConnectorArrows.tsx, frontend/src/components/charts/EndgameOverallPerformanceSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallConnectorArrows.tsx (entire file — 188 LOC; the 4 hard-coded `querySelector('[data-testid="..."]')` calls at lines 50-61 are the only swap site, plus the props interface at line 38-40)
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx around lines 41 + 251 (the import + sole call site for `<ConnectorArrows containerRef={gridRef} />`)
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-CONTEXT.md §D-09a (LOCKED — props signature and call-site instruction)
  </read_first>
  <behavior>
    - `ConnectorArrows` accepts 4 new required props: `leftCardTestId: string`, `middleCardTestId: string`, `rightCardTestId: string`, `targetTileTestId: string`. The existing `containerRef` prop stays.
    - The 4 `querySelector` calls at lines 50-61 use template-literal selectors driven by the new props: `container.querySelector<HTMLElement>(\`[data-testid="${leftCardTestId}"]\`)` etc.
    - The mobile-bail check at line 75 (`sgr.left <= c1r.left + 2`) is unchanged — it works for both Phase 85 (score-differences below middle) and Phase 86 (Skill-card below Parity) because both layouts stack to single-column on mobile with the target below-or-equal-to the leftmost card.
    - Geometry constants (`ARROW_BAR_PX`, `ARROW_HEAD_LEN_PX`, `ARROW_HEAD_HALF_HEIGHT_PX`) and the arrow-rectangle construction at lines 100-165 are unchanged.
    - Phase 85's `EndgameOverallPerformanceSection.tsx:251` updated to pass the existing four testids explicitly: `leftCardTestId="tile-games-without-endgame"`, `middleCardTestId="tile-at-endgame-entry"`, `rightCardTestId="tile-games-with-endgame"`, `targetTileTestId="endgame-score-differences"`.
    - No new file name (keeps `EndgameOverallConnectorArrows.tsx` per planner discretion in D-09a — rename adds churn for no real value; Phase 86 imports under its own local alias if helpful).
    - File-header comment block (lines 1-13) updated to note it now serves both Phase 85 and Phase 86 (call sites parameterize testids per Phase 86 D-09a).
  </behavior>
  <action>
    **Edit `EndgameOverallConnectorArrows.tsx`:**

    1. File-header comment (lines 1-13): replace the Phase-85-only framing with a generalized version. Keep the visual-flow description but rephrase as "three top cards + one target tile below the middle card". Add a one-line note: "Phase 86 D-09a parameterized the four testids via props so the same geometry serves both `EndgameOverallPerformanceSection` (Phase 85) and `EndgameMetricsSection` (Phase 86)."

    2. Props interface (line 38-40): extend the existing inline destructured-props shape on `ConnectorArrows`. Define an interface above the function: `interface ConnectorArrowsProps { containerRef: React.RefObject<HTMLDivElement | null>; leftCardTestId: string; middleCardTestId: string; rightCardTestId: string; targetTileTestId: string; }`. Update the function signature to `export function ConnectorArrows({ containerRef, leftCardTestId, middleCardTestId, rightCardTestId, targetTileTestId }: ConnectorArrowsProps)`.

    3. `useEffect` dependency array (line 98): replace `[containerRef]` with `[containerRef, leftCardTestId, middleCardTestId, rightCardTestId, targetTileTestId]` so the layout recomputes if a parent ever reuses the same `<ConnectorArrows>` instance with different testids (defensive).

    4. The 4 `querySelector` calls (lines 50-61): replace the hard-coded `'[data-testid="tile-games-without-endgame"]'` with `` `[data-testid="${leftCardTestId}"]` ``; replace `'[data-testid="tile-at-endgame-entry"]'` with `` `[data-testid="${middleCardTestId}"]` ``; replace `'[data-testid="tile-games-with-endgame"]'` with `` `[data-testid="${rightCardTestId}"]` ``; replace `'[data-testid="endgame-score-differences"]'` with `` `[data-testid="${targetTileTestId}"]` ``.

    5. Keep everything else (geometry constants, ResizeObserver setup, JSX, clip-paths, segmentClass) byte-for-byte identical.

    **Edit `EndgameOverallPerformanceSection.tsx` line 251:**

    Replace `<ConnectorArrows containerRef={gridRef} />` with:

    `<ConnectorArrows containerRef={gridRef} leftCardTestId="tile-games-without-endgame" middleCardTestId="tile-at-endgame-entry" rightCardTestId="tile-games-with-endgame" targetTileTestId="endgame-score-differences" />`

    Do NOT touch any other line in `EndgameOverallPerformanceSection.tsx` — particularly do not modify the `<EndgameCard tileTestId="tile-games-without-endgame">` props or the `<div data-testid="endgame-score-differences">` JSX block.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm test -- --run EndgameOverallPerformanceSection</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "leftCardTestId\|middleCardTestId\|rightCardTestId\|targetTileTestId" frontend/src/components/charts/EndgameOverallConnectorArrows.tsx` returns at least 8 (interface definition × 4 + function destructure × 4 + useEffect deps × 4 + querySelector usages × 4 = at least 16; floor is 8)
    - `grep -c "\\[data-testid=\"tile-games-without-endgame\"\\]\\|\\[data-testid=\"tile-at-endgame-entry\"\\]\\|\\[data-testid=\"tile-games-with-endgame\"\\]\\|\\[data-testid=\"endgame-score-differences\"\\]" frontend/src/components/charts/EndgameOverallConnectorArrows.tsx` returns 0 (no hard-coded selectors remain)
    - `grep -c "leftCardTestId=\"tile-games-without-endgame\"" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` returns 1
    - `grep -c "targetTileTestId=\"endgame-score-differences\"" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` returns 1
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm test -- --run EndgameOverallPerformanceSection` exits 0 (Phase 85's behavior preserved)
  </acceptance_criteria>
  <done>
    `ConnectorArrows` accepts 4 testid props; Phase 85 call site passes the existing testids explicitly; no hard-coded selectors remain in the connector-arrows component; Phase 85 tests still pass.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run` all exit 0
- `grep -n "leftCardTestId" frontend/src/components/charts/EndgameOverallConnectorArrows.tsx frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` shows the prop defined, destructured, used in querySelector, and passed at Phase 85's call site
- `grep -n "MIRROR_BUCKET\|userRate" frontend/src/lib/endgameMetrics.ts` shows the lifted helpers
- Phase 85's connector-arrow geometry visually unchanged (real-device check deferred to Plan 86-05 integration step; this plan's gate is tests + tsc)
</verification>

<success_criteria>
- `lib/endgameMetrics.ts` created with 13 verbatim exports from `EndgameScoreGapSection.tsx`
- `endgameSkill()` deliberately NOT lifted (retired in favor of server-side `skill` / `opp_skill` per D-04)
- `EndgameOverallConnectorArrows.tsx` parameterized with 4 testid props; geometry unchanged
- Phase 85 call site passes the existing testids explicitly; Phase 85 tests pass
- tsc + lint + frontend tests all green
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-03-SUMMARY.md`. Note in the SUMMARY: (a) `endgameSkill()` from legacy file at line 155-165 was NOT lifted to the new module (D-04 retirement; backend now owns the composite via Plan 02); (b) connector-arrows file kept its existing name (D-09a's "planner picks" — keeping the name minimizes diff vs renaming to `ConnectorArrowsToTarget.tsx`).
</output>
