---
phase: 87.3-endgame-skill-v2-conv-parity-percentile-composite
plan: 02
type: execute
wave: 2
depends_on: ["87.3-01"]
files_modified:
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - tests/test_endgame_service.py
autonomous: true
requirements: [SC#1, SC#3, SC#6]
must_haves:
  truths:
    - "The Endgame Skill composite is the equal-weighted mean of Conversion ΔES and Parity ΔES only; Recovery is not in the composite anywhere in the backend"
    - "The ≥10-span floor (CONFIDENCE_MIN_N) is applied per active bucket; a bucket below the floor is dropped from the denominator"
    - "ScoreGapMaterialResponse exposes endgame_skill_percentile (replacing endgame_skill_rate_mean as the gauge driver)"
    - "The Endgame ELO timeline feeds the percentile as s into the unchanged _endgame_elo_from_skill; s ∈ [0,1] and s=0.5 ⇒ endgame_elo == actual_elo"
  artifacts:
    - path: "app/services/endgame_service.py"
      provides: "2-bucket _compute_skill_score_gap, endgame_skill_percentile computation, _endgame_skill_percentile_from_window"
      contains: "_endgame_skill_percentile_from_window"
    - path: "app/schemas/endgames.py"
      provides: "endgame_skill_percentile field on ScoreGapMaterialResponse"
      contains: "endgame_skill_percentile"
    - path: "tests/test_endgame_service.py"
      provides: "SC#1/SC#3 composite-math + ELO-invariant + timeline-non-drift tests"
      contains: "class TestEndgameSkillV2"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app.services.endgame_skill_reference.interpolate_percentile"
      via: "import + call on the Conv+Parity composite"
      pattern: "interpolate_percentile"
    - from: "app/services/endgame_service.py"
      to: "_endgame_elo_from_skill"
      via: "_compute_endgame_elo_weekly_series passes the percentile as s (formula UNCHANGED)"
      pattern: "_endgame_elo_from_skill"
---

<objective>
Redefine the backend Endgame Skill computation: the ΔES Skill bullet becomes a 2-bucket (Conv+Parity) equal-weighted mean (Recovery dropped), the gauge driver becomes `endgame_skill_percentile` (via Plan 01's `interpolate_percentile`), and the ELO timeline rolling window feeds the percentile as `s` into the UNCHANGED Phase 57 `_endgame_elo_from_skill`.

Purpose: SC#1 (Conv+Parity composite, Recovery excluded, ≥10-span floor) and SC#3 (s ∈ [0,1], population median ⇒ 0.5 ⇒ ELO lines coincide; frozen-reference timeline non-drift) are satisfied in the backend.
Output: Modified `endgame_service.py` (Seams 1, 2, 3, 3a per RESEARCH.md), modified `ScoreGapMaterialResponse` schema, and the `TestEndgameSkillV2` test class covering composite math + the ELO invariant + timeline non-drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/notes/endgame-skill-recovery-confound.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-01-SUMMARY.md
@app/services/endgame_service.py
@app/schemas/endgames.py

<interfaces>
<!-- Created by Plan 01 (Wave 1) — consume directly, do not reimplement. -->

From app/services/endgame_skill_reference.py:
  def interpolate_percentile(composite: float) -> float   # piecewise-linear, clamps to [0.0, 1.0], pure
  REFERENCE_VERSION: str

_compute_span_gap POSITIONAL signature (app/services/endgame_service.py:260 — verified):
  _compute_span_gap(entry_eval_cp, entry_eval_mate, next_entry_eval_cp, next_entry_eval_mate, result, user_color) -> float | None
  Terminal span: pass next_entry_eval_cp=None, next_entry_eval_mate=None  =>  exit_score from game result.

_endgame_elo_from_skill (app/services/endgame_service.py:~1350 — REUSED UNCHANGED, do not modify):
  endgame_elo = actual_elo + 400*log10(s/(1-s)); s clamped [0.05, 0.95]; s=0.5 => result == actual_elo.

CONFIDENCE_MIN_N = 10 (already imported in endgame_service.py:74).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — TestEndgameSkillV2 scaffold (composite math, ELO invariant, timeline non-drift)</name>
  <read_first>
    - tests/test_endgame_service.py (lines 1-48 imports; TestClassifyEndgameBucket lines 128-199 boundary-case style; TestAggregateEndgameStats lines 201-450 fixture-row style)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (the tests/test_endgame_service.py EXTEND section, lines 685-739)
    - app/services/endgame_service.py (_compute_skill_score_gap ~1095, _compute_score_gap_material ~1156, _endgame_skill_from_bucket_rows ~1369, _endgame_elo_from_skill ~1350)
    - .planning/notes/endgame-skill-recovery-confound.md (s in [0,1] / median => 0.5 invariant, >=10-span floor)
  </read_first>
  <behavior>
    - test_endgame_elo_invariant: _endgame_elo_from_skill(0.5, 1500.0) == 1500 and (0.5, 2000.0) == 2000 (s=0.5 => lines coincide — SC#3)
    - test_skill_composite_drops_recovery: synthetic spans conv+parity+recovery (each >=10) => section2_score_gap_skill_mean == (conv_mean + parity_mean) / 2, NOT the 3-way mean
    - test_skill_composite_floor_one_active: conv >=10 spans, parity <10 => Skill uses conv only (denominator 1); endgame_skill_percentile == interpolate_percentile(conv_mean)
    - test_skill_composite_all_below_floor: every bucket <10 spans => endgame_skill_percentile is None
    - test_elo_timeline_percentile_path: synthetic window with known conv/parity means => endgame_elo == _endgame_elo_from_skill(expected_percentile, actual_elo)
    - test_elo_timeline_non_drift: generate a timeline; monkeypatch QUANTILE_KNOTS to a shifted reference; assert historical points are recomputed via the SAME committed reference (frozen-by-construction => non-drift)
  </behavior>
  <action>
    Append `class TestEndgameSkillV2` to `tests/test_endgame_service.py`. Write `test_endgame_elo_invariant` fully (it tests existing unchanged code and must pass immediately — it guards the SC#3 contract before any change). Write the other five tests as executable specs against the Task 2/3 target API: `_endgame_skill_percentile_from_window` (new function name), `endgame_skill_percentile` field, 2-bucket `section2_score_gap_skill_mean`. Use the row-fixture / synthetic-span builder pattern from `TestAggregateEndgameStats`. For `test_elo_timeline_non_drift`, use `monkeypatch.setattr` on `app.services.endgame_skill_reference.QUANTILE_KNOTS` to simulate a reference re-version and assert the timeline recomputation path reads the committed reference deterministically (historical points stable). Add imports: `_endgame_skill_percentile_from_window`, `_endgame_elo_from_skill`, `CONFIDENCE_MIN_N` from `app.services.endgame_service`. It is EXPECTED that the five forward-looking tests fail (RED) until Tasks 2-3; `test_endgame_elo_invariant` must be GREEN now.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2::test_endgame_elo_invariant -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2::test_endgame_elo_invariant -x` exits 0 (existing-code invariant guarded)
    - The other five tests exist and FAIL with collection/attribute errors referencing `_endgame_skill_percentile_from_window` / `endgame_skill_percentile` (proves they target the new API, RED state expected)
    - `uv run ruff check tests/test_endgame_service.py` clean
  </acceptance_criteria>
  <done>TestEndgameSkillV2 scaffold committed; ELO invariant test green; forward tests are RED specs for Tasks 2-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop Recovery from the ΔES Skill bullet + add endgame_skill_percentile (Seams 1, 2, 4)</name>
  <read_first>
    - app/services/endgame_service.py (_compute_skill_score_gap lines ~1095-1153 — the `for bucket in (...)` loop; _compute_score_gap_material lines ~1156-1300 — the endgame_skill_rate_mean block ~1255-1266 and the ScoreGapMaterialResponse(...) constructor ~1268-1299)
    - app/schemas/endgames.py (lines 400-436 — section2_score_gap_* field block + the endgame_skill_rate_mean field ~436)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (Seam 1, Seam 2, Seam 4 sections, lines 222-330)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md (Seam 1/2/4; CI propagation: se_skill = sqrt(se_conv^2 + se_parity^2) / 2)
  </read_first>
  <action>
    Seam 1 (`_compute_skill_score_gap`): change the bucket loop from `for bucket in ("conversion", "parity", "recovery"):` to `for bucket in ("conversion", "parity"):`. Leave the rest of the function untouched — the active-bucket-floor denominator (`len(active_buckets)`, gated on `n >= CONFIDENCE_MIN_N`) and the variance-of-sum CI propagation already generalize to 2 buckets (se_skill = sqrt(se_conv^2 + se_parity^2) / 2). Do NOT hardcode 2; rely on `len(active_buckets)`.

    Seam 2 (`_compute_score_gap_material`): add `from app.services.endgame_skill_reference import interpolate_percentile` to the import block. Remove the `endgame_skill_rate_mean` computation block (the quick-260515-wye lines). Add the percentile composite: collect `conv_mean` (only if `conv_n >= CONFIDENCE_MIN_N`) and `parity_mean` (only if `parity_n >= CONFIDENCE_MIN_N`) into `pct_active: list[float]`; set `endgame_skill_percentile: float | None = interpolate_percentile(sum(pct_active) / len(pct_active)) if pct_active else None`. In the `ScoreGapMaterialResponse(...)` constructor call, replace `endgame_skill_rate_mean=...` with `endgame_skill_percentile=endgame_skill_percentile`.

    Seam 4 (`app/schemas/endgames.py`): remove `endgame_skill_rate_mean: float | None = None` and its `quick-260515-wye` comment; add `endgame_skill_percentile: float | None = None` after the `section2_score_gap_skill_*` block, with a field-block comment matching the existing convention: `# Phase 87.3: percentile rank of Conv+Parity composite vs FlawChess Benchmark. Replaces endgame_skill_rate_mean as the gauge driver. Value in [0.0, 1.0]; 0.5 = population median (ELO lines coincide at 0.5).`

    Add explicit return type annotations on any touched helper. Use `# ty: ignore[rule-name]` (with reason) only for unfixable SQLAlchemy/forward-ref errors. Run the targeted composite tests (they should flip RED -> GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2 -k "composite" -x && uv run ty check app/services/endgame_service.py app/schemas/endgames.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_skill_composite_drops_recovery`, `test_skill_composite_floor_one_active`, `test_skill_composite_all_below_floor` all pass
    - `grep -n 'endgame_skill_rate_mean' app/services/endgame_service.py app/schemas/endgames.py` returns no matches (field fully retired)
    - `grep -c '"recovery"' app/services/endgame_service.py` shows the `_compute_skill_score_gap` loop no longer contains "recovery" (verify the specific loop line: `grep -n 'for bucket in ("conversion", "parity")' app/services/endgame_service.py` returns 1)
    - `uv run ty check app/services/endgame_service.py app/schemas/endgames.py` reports zero errors
  </acceptance_criteria>
  <done>ΔES Skill bullet is 2-bucket; endgame_skill_percentile is computed and on the schema; endgame_skill_rate_mean fully removed; composite tests green; ty clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace ELO-timeline skill path with percentile (Seams 3 / 3a)</name>
  <read_first>
    - app/services/endgame_service.py (_endgame_skill_from_bucket_rows lines ~1369-1441 — tuple-unpack + bucket accumulator template; _compute_endgame_elo_weekly_series ~1444 — the caller at ~1525; _classify_endgame_bucket ~196; _compute_span_gap ~260; _endgame_elo_from_skill ~1350)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (Seam 3 section lines 256-307 — _endgame_skill_percentile_from_window template + "Tuple-Unpack Row Pattern" + "note on _compute_span_gap with next_eval=None")
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md (Seam 3, Seam 3a, Open Question 2 — the window is ONE ROW PER GAME; terminal-span game-result is the only exit-score method; no LEAD query change)
  </read_first>
  <action>
    Replace `_endgame_skill_from_bucket_rows` with `_endgame_skill_percentile_from_window(rows: Sequence[Row[Any] | tuple[Any, ...]]) -> float | None`. Per RESEARCH.md Open Question 2 (resolved): each window row is one game's endgame entry (terminal span), so the gap uses the game result, NOT a next-span LEAD — no `query_endgame_elo_timeline_rows` schema change (Seam 3a Option A). Iterate rows with the existing tuple-unpack pattern (underscore-prefix unused fields for ty), `_classify_endgame_bucket(eval_cp, eval_mate, user_color)`, skip buckets not in `("conversion", "parity")`, compute `gap = _compute_span_gap(eval_cp, eval_mate, None, None, result, user_color)` (POSITIONAL — terminal span via next_entry_eval_*=None; confirm `_compute_span_gap` handles the both-None terminal path per its docstring), accumulate `conv_gaps`/`parity_gaps`. Build `active_means`: include conv mean only if `len(conv_gaps) >= CONFIDENCE_MIN_N`, parity mean only if `len(parity_gaps) >= CONFIDENCE_MIN_N`. Return `None` if no active bucket; else `interpolate_percentile(sum(active_means) / len(active_means))`.

    Update the caller in `_compute_endgame_elo_weekly_series` (~line 1525) from `_endgame_skill_from_bucket_rows(...)` to `_endgame_skill_percentile_from_window(...)`. The returned value flows UNCHANGED into `_endgame_elo_from_skill(s, actual_elo)` — do NOT modify `_endgame_elo_from_skill`. Verify no other caller of `_endgame_skill_from_bucket_rows` remains via grep; if a caller exists outside `_compute_endgame_elo_weekly_series`, surface it in the SUMMARY rather than silently broadening scope. Add `Sequence`/`Any`/`Row` imports if not present (use `Sequence`, not `list`, for the covariant param per CLAUDE.md).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2 -x && uv run ty check app/services/endgame_service.py && uv run ruff check app/services/endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - All six `TestEndgameSkillV2` tests pass (including `test_elo_timeline_percentile_path` and `test_elo_timeline_non_drift`)
    - `grep -n '_endgame_skill_from_bucket_rows' app/services/endgame_service.py` returns no matches (old function fully replaced)
    - `grep -n '_endgame_skill_percentile_from_window' app/services/endgame_service.py` returns the definition AND exactly one call site (in `_compute_endgame_elo_weekly_series`)
    - `git diff app/services/endgame_service.py | grep -A2 -B2 '_endgame_elo_from_skill'` shows no modification to `_endgame_elo_from_skill` itself (formula UNCHANGED — SC#3)
    - `uv run ty check app/services/endgame_service.py` and `uv run ruff check app/services/endgame_service.py` clean
  </acceptance_criteria>
  <done>ELO timeline feeds the Conv+Parity percentile as s into the unchanged Phase 57 formula; old rate-based skill path removed; all SC#1/SC#3 backend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen reference module → backend runtime | `interpolate_percentile` consumes a per-user composite float; pure, returns [0,1]; no external/untrusted input |
| backend composite → ELO formula | percentile fed as `s`; already clamped [0.05, 0.95] inside the unchanged `_endgame_elo_from_skill` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-04 | Tampering | Recovery leaking back into the composite via a missed loop/aggregator | mitigate | Task 2 grep gate asserts the `_compute_skill_score_gap` loop is exactly `("conversion", "parity")`; `test_skill_composite_drops_recovery` asserts the 2-way mean numerically |
| T-87.3-05 | Denial of Service | Division by zero / None when all buckets below floor | mitigate | Explicit `if pct_active`/`if not active_means: return None` guards; `test_skill_composite_all_below_floor` covers it |
| T-87.3-06 | Tampering | Accidental modification of the Phase 57 ELO formula | mitigate | Acceptance criterion: `git diff` shows `_endgame_elo_from_skill` unchanged; `test_endgame_elo_invariant` guards the s=0.5 contract |
| T-87.3-SC | Tampering | uv installs | accept | No new packages (RESEARCH.md audit: none); stdlib only |
</threat_model>

<verification>
- `uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2 -x` green (all six — SC#1, SC#3)
- `endgame_skill_rate_mean` fully removed from backend + schema; `endgame_skill_percentile` present
- `_endgame_elo_from_skill` byte-unchanged in git diff (SC#3 formula reuse)
- `uv run ty check app/ tests/` + `uv run ruff check .` clean (SC#6)
</verification>

<success_criteria>
SC#1 (Conv+Parity equal-weight composite, Recovery excluded everywhere in the backend, ≥10-span floor) and the backend half of SC#3 (s ∈ [0,1], median ⇒ 0.5 ⇒ ELO lines coincide, frozen-reference non-drift) are met. The schema exposes `endgame_skill_percentile` for Plans 03/04 to consume.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-02-SUMMARY.md` when done. Record: the final `endgame_skill_percentile` field name/location, confirmation that `_endgame_skill_from_bucket_rows` had no callers beyond `_compute_endgame_elo_weekly_series` (or surface any that did), and any deviation from the terminal-span (game-result) exit-score assumption.
</output>
