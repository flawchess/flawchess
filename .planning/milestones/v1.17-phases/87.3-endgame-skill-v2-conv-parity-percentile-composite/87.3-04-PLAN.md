---
phase: 87.3-endgame-skill-v2-conv-parity-percentile-composite
plan: 04
type: execute
wave: 3
depends_on: ["87.3-02"]
files_modified:
  - app/services/insights_service.py
  - app/services/insights_llm.py
  - app/prompts/endgame_insights.md
  - CHANGELOG.md
  - tests/test_insights_service.py
autonomous: true
requirements: [SC#5, SC#6]
must_haves:
  truths:
    - "The endgame_skill LLM finding value is the percentile (from response.score_gap_material.endgame_skill_percentile), not the rate"
    - "The Recovery Section 2 ΔES finding is descriptive only: is_headline_eligible=False regardless of n"
    - "_PROMPT_VERSION is bumped to endgame_v32 with a changelog narrating the Conv+Parity percentile composite + Recovery exclusion rationale; no parallel verdict/p_value field"
    - "CHANGELOG.md [Unreleased] has a user-facing entry for the Endgame Skill redefinition + Recovery reframe"
  artifacts:
    - path: "app/services/insights_service.py"
      provides: "percentile-driven endgame_skill finding + Recovery descriptive override"
      contains: "endgame_skill_percentile"
    - path: "app/services/insights_llm.py"
      provides: "_PROMPT_VERSION = endgame_v32 + v32 changelog entry"
      contains: "endgame_v32"
    - path: "tests/test_insights_service.py"
      provides: "SC#5 finding test: skill percentile + Recovery non-headline"
      contains: "endgame_skill_percentile"
  key_links:
    - from: "app/services/insights_service.py"
      to: "response.score_gap_material.endgame_skill_percentile"
      via: "_findings_endgame_metrics reads the Plan 02 schema field"
      pattern: "endgame_skill_percentile"
    - from: "app/services/insights_llm.py"
      to: "app/prompts/endgame_insights.md"
      via: "_PROMPT_VERSION cache key + glossary semantics"
      pattern: "endgame_v32"
---

<objective>
Make the LLM payload reflect the new percentile composite: the endgame_skill finding value becomes the percentile (read from the Plan 02 schema field), the Recovery Section 2 ΔES finding becomes descriptive-only (is_headline_eligible=False), the prompt version bumps to endgame_v32 with a changelog narrating the metric change + Recovery exclusion rationale, the prompt glossary is updated, and a user-facing CHANGELOG entry is added.

Purpose: SC#5 (LLM payload Skill value + zone reflect the percentile composite; Recovery framing updated to descriptive; prompt version bumped with changelog) and SC#6 (ty + ruff clean, test coverage).
Output: Modified insights_service.py (Seam 9), insights_llm.py (Seam 10), app/prompts/endgame_insights.md glossary, CHANGELOG.md [Unreleased], and a test_insights_service.py finding test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-CONTEXT.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-02-SUMMARY.md
@app/services/insights_service.py
@app/services/insights_llm.py
@app/prompts/endgame_insights.md

<interfaces>
<!-- Plan 02 (Wave 2) contract — consume directly. -->

response.score_gap_material.endgame_skill_percentile: float | None   (value in [0.0, 1.0]; 0.5 = population median; replaces the removed endgame_skill_rate_mean)

Verified by codebase inspection:
  app/services/insights_service.py:692  def _findings_endgame_metrics(...)
  app/services/insights_service.py:720  skill_value = _endgame_skill_from_material_rows(rows)   (to replace)
  app/services/insights_service.py:795  _SECTION2_BUCKETS  loop emitting section2_score_gap_{conv,parity,recov,skill}
  app/services/insights_service.py:~1342 _endgame_skill_from_material_rows  (candidate for removal — grep callers first)
  app/services/insights_llm.py:68       _PROMPT_VERSION = "endgame_v31"   (bump to "endgame_v32")
  CHANGELOG.md:9                        ## [Unreleased]

D-11 constraint: NO parallel verdict/p_value field — the zone band IS the significance signal (memory feedback_llm_significance_signal). Payload shape {mean,n,zone,neutral_band} is preserved.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skill percentile finding + Recovery descriptive override (Seam 9) + SC#5 test</name>
  <read_first>
    - app/services/insights_service.py (lines 692-828 _findings_endgame_metrics; line 720 skill_value; line ~733 is_headline gate; lines 795-827 _SECTION2_BUCKETS loop; line ~1342 _endgame_skill_from_material_rows)
    - tests/test_insights_service.py (existing endgame-metrics finding test for the fixture-builder + assertion style)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (insights_service.py section lines 389-427 — Change 1 percentile read, Change 2 Recovery is_headline override; "is_headline_eligible Gate Pattern")
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md (Seam 9, Open Question 3 — _endgame_skill_from_material_rows caller check before removal)
  </read_first>
  <behavior>
    - endgame_skill finding value equals response.score_gap_material.endgame_skill_percentile (NaN-safe when None)
    - section2_score_gap_recov finding has is_headline_eligible=False even when n>=10 and mean is not None
    - section2_score_gap_skill / conv / parity findings keep their normal is_headline gate (unchanged)
  </behavior>
  <action>
    Seam 9 Change 1: replace `skill_value = _endgame_skill_from_material_rows(rows)` (line ~720) with reading the percentile directly: `skill_value_raw = response.score_gap_material.endgame_skill_percentile` then `skill_value = skill_value_raw if skill_value_raw is not None else float("nan")`. Keep the existing downstream `is_headline_eligible=is_headline and not math.isnan(skill_value)` gate and the `assign_zone("endgame_skill", skill_value)` call (Plan 01 updated the zone registry to percentile bounds, so the same call now classifies on the percentile scale automatically). After this, grep for remaining callers of `_endgame_skill_from_material_rows`; if NONE outside this site (RESEARCH.md Assumption A3), remove the now-dead function; if other callers exist, leave it and note in the SUMMARY (do not broaden scope silently).

    Seam 9 Change 2: in the `_SECTION2_BUCKETS` loop (lines ~795-827), force the recovery bucket's `is_headline_eligible=False` regardless of n/mean (it is a descriptive tile, not a skill verdict — D-09/D-11). E.g. `is_headline = (n_raw >= 10 and mean_raw is not None) and bucket_id != "recov"`. Do NOT add a verdict/p_value field (D-11 — payload shape stays {mean,n,zone,neutral_band}).

    Add/extend a test in tests/test_insights_service.py (e.g. test_endgame_metrics_findings_skill_percentile) asserting: the endgame_skill finding value tracks endgame_skill_percentile (set a known percentile on the fixture's score_gap_material), and the section2_score_gap_recov finding has is_headline_eligible is False even with n>=10. Use the existing test's fixture-builder style.

    Add explicit return type annotations on touched helpers; use `# ty: ignore[rule]` (with reason) only if unavoidable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_insights_service.py -k endgame_metrics -x</automated>
  </verify>
  <acceptance_criteria>
    - The new/extended test passes; endgame_skill finding value == fixture endgame_skill_percentile
    - section2_score_gap_recov finding has is_headline_eligible is False with n>=10 and mean set
    - `grep -n "_endgame_skill_from_material_rows" app/services/insights_service.py` returns either zero matches (removed, no other callers) OR is accompanied by a SUMMARY note listing the remaining callers
    - No verdict/p_value key added to the Section 2 findings payload (inspect the loop body)
    - `uv run ty check app/services/insights_service.py` reports zero errors
  </acceptance_criteria>
  <done>endgame_skill LLM finding is percentile-driven; Recovery finding is descriptive-only; SC#5 finding test green; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: _PROMPT_VERSION bump to endgame_v32 + glossary + CHANGELOG (Seam 10)</name>
  <read_first>
    - app/services/insights_llm.py (line 68 _PROMPT_VERSION with the inline changelog comment — note the contiguous ". vNN ..." convention)
    - app/prompts/endgame_insights.md (the endgame_skill glossary entry + the Section 2 / Recovery narration instructions + the existing sigmoid-bias caveat phrasing reused since v29)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (insights_llm.py section lines 431-446 — exact v32 changelog text + convention)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-CONTEXT.md (D-11 — narrative content, no parallel verdict/p_value, memory feedback_llm_significance_signal)
    - CHANGELOG.md (lines 1-20 — [Unreleased] structure + ### subsection convention) and CLAUDE.md changelog rules
  </read_first>
  <action>
    insights_llm.py: change `_PROMPT_VERSION = "endgame_v31"` to `"endgame_v32"` and PREPEND the v32 entry to the front of the existing contiguous inline-changelog comment string (do not delete prior entries; follow the exact ". vNN (date Phase ...) ..." style). v32 entry narrates (per D-11): Endgame Skill is now the percentile rank of the equal-weighted Conv ΔES + Parity ΔES composite against the FlawChess Benchmark (Lichess 2300+ sigmoid reference, k=0.00368208, >=10-span floor per active bucket); Recovery dropped from the composite (opponent-confounded — reflects opponent error frequency, not defensive skill); Recovery Score Gap retained as a standalone descriptive tile, its finding carries is_headline_eligible=False; Skill payload value is the percentile in [0,1], zone reflects the new percentile-centred band (centre 0.50 = population median; s=0.50 => ELO lines coincide); no parallel verdict/p_value field — band IS the significance signal.

    app/prompts/endgame_insights.md: update the endgame_skill glossary entry to explain the percentile semantics (percentile rank of the Conv+Parity ΔES composite vs the FlawChess Benchmark; 50 = typical/population median; NOT a rate) and the Conv+Parity basis. Update the Recovery (section2_score_gap_recov) narration instruction to describe it as DESCRIPTIVE (opponent-dependent — how often opponents failed to convert against the user, not a defensive-skill verdict; deliberately excluded from Endgame Skill). Reuse the existing sigmoid-bias caveat one-liner verbatim (do not reword it). Do not add any precomputed story or a verdict/p_value instruction (memory feedback_llm_significance_signal, feedback_llm_prompt_design).

    CHANGELOG.md: under `## [Unreleased]`, add a terse user-facing bullet (group under `### Changed`, create the subsection if absent) referencing Phase 87.3: Endgame Skill is now a percentile ("better than X% of players") of the Conversion + Parity Score Gap composite; Recovery is no longer part of Endgame Skill and is shown as a separate descriptive stat (it reflects opponents failing to convert, not your defensive skill). Keep it user-facing (what changed, not how); em-dash sparingly per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -n endgame_v32 app/services/insights_llm.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'endgame_v32' app/services/insights_llm.py` returns the bumped `_PROMPT_VERSION`
    - `grep -c 'endgame_v31' app/services/insights_llm.py` is >= 1 (prior changelog entries preserved, not deleted)
    - app/prompts/endgame_insights.md endgame_skill glossary describes a percentile (not a rate); Recovery instruction says descriptive/opponent-dependent and "excluded from Endgame Skill"; no new verdict/p_value instruction added
    - CHANGELOG.md `## [Unreleased]` contains a Phase 87.3 user-facing bullet under a `###` subsection
    - `uv run ruff check app/services/insights_llm.py` clean
  </acceptance_criteria>
  <done>Prompt version bumped to endgame_v32 with the D-11 changelog; glossary + Recovery narration updated; user-facing CHANGELOG entry added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend findings → LLM prompt payload | Internal computed values serialized into the prompt; no user-controlled free text introduced |
| prompt version → report cache key | `_PROMPT_VERSION` is the cache-bust key; a missed bump would serve stale reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-10 | Repudiation | Stale cached LLM reports if prompt version not bumped | mitigate | Task 2 bumps `_PROMPT_VERSION` to endgame_v32 (acceptance criterion asserts the literal); cache key is keyed on it |
| T-87.3-11 | Spoofing | LLM narrating Recovery as a skill verdict | mitigate | Glossary/Recovery instruction explicitly marks it descriptive + opponent-dependent; finding `is_headline_eligible=False` (Task 1) prevents headline promotion |
| T-87.3-SC | Tampering | uv installs | accept | No new packages (RESEARCH.md audit: none) |
</threat_model>

<verification>
- `uv run pytest tests/test_insights_service.py -k endgame_metrics -x` green (SC#5)
- `_PROMPT_VERSION == "endgame_v32"`; prior changelog entries preserved
- Glossary describes the percentile; Recovery framed descriptive; CHANGELOG [Unreleased] updated
- `uv run ty check app/ tests/` + `uv run ruff check .` clean (SC#6)
</verification>

<success_criteria>
SC#5 (LLM Skill value + zone reflect the percentile composite; Recovery framing updated to descriptive; prompt version bumped with a changelog explaining the metric change + Recovery exclusion; no parallel verdict/p_value field) is met.
</success_criteria>

<output>
Create .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-04-SUMMARY.md when done. Record: whether _endgame_skill_from_material_rows was removed or retained (with caller list), the exact v32 changelog sentence, and the CHANGELOG bullet wording.
</output>
