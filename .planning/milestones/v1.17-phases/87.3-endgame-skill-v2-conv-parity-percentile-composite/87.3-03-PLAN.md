---
phase: 87.3-endgame-skill-v2-conv-parity-percentile-composite
plan: 03
type: execute
wave: 2
depends_on: ["87.3-01"]
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameSkillCard.tsx
  - frontend/src/components/charts/EndgameMetricsSection.tsx
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/components/charts/EndgameSkillCard.test.tsx
autonomous: true
requirements: [SC#2, SC#4, SC#6]
must_haves:
  truths:
    - "The Endgame Skill gauge shows the percentile on a 0-100 scale with the headline 'Better than X% of players'; 50 = typical"
    - "The Skill card popover cites 'the FlawChess Benchmark' with the verbatim GitHub link and the sigmoid caveat; SKILL_POPOVER_COPY no longer mentions Recovery"
    - "Section 2 reading order is Conversion -> Parity -> Skill -> Recovery with the 3-col grid preserved (no row collapse)"
    - "The Recovery tile keeps full card anatomy (Save% gauge + ΔES bullet); its popover states it is opponent-dependent and deliberately excluded from Endgame Skill"
  artifacts:
    - path: "frontend/src/components/charts/EndgameSkillCard.tsx"
      provides: "percentile gauge driver + 'Better than X%' headline + FlawChess Benchmark popover"
      contains: "Better than"
    - path: "frontend/src/types/endgames.ts"
      provides: "endgame_skill_percentile field (replaces endgame_skill_rate_mean)"
      contains: "endgame_skill_percentile"
    - path: "frontend/src/components/charts/EndgameSkillCard.test.tsx"
      provides: "SC#4 Vitest: gauge value, headline, null state, no-Recovery-in-popover"
      contains: "Better than"
  key_links:
    - from: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      to: "EndgameSkillCard"
      via: "skillPercentile={data.endgame_skill_percentile}"
      pattern: "skillPercentile"
    - from: "frontend/src/components/charts/EndgameSkillCard.tsx"
      to: "ENDGAME_SKILL_ZONES"
      via: "imported from generated endgameZones (Plan 01 percentile bounds)"
      pattern: "ENDGAME_SKILL_ZONES"
---

<objective>
Reframe the Endgame Skill card from a rate gauge to a percentile gauge ("Better than X% of players", D-07), wire the D-10 FlawChess Benchmark popover with the verbatim GitHub link, reorder Section 2 to Conv -> Parity -> Skill -> Recovery (D-05, 3-col grid preserved), make the Recovery tile descriptive (D-09, full card anatomy retained per D-06), and cover it all with Vitest.

Purpose: SC#2 (percentile gauge, neutral zone centred at 50, codegen drift-gate stays green), SC#4 (Recovery standalone descriptive tile with opponent-confound copy, no "vs peer" framing), and the frontend half of SC#6 (knip clean, Vitest).
Output: Modified EndgameSkillCard.tsx, EndgameMetricsSection.tsx, EndgameMetricCard.tsx (Recovery popover via the TITLE_TOOLTIPS constant), types/endgames.ts, and a new EndgameSkillCard.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-CONTEXT.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-01-SUMMARY.md
@frontend/src/components/charts/EndgameSkillCard.tsx
@frontend/src/components/charts/EndgameMetricsSection.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/types/endgames.ts

<interfaces>
<!-- Schema field name is LOCKED (Plan 02 backend contract). Use exactly this name. -->

Backend ScoreGapMaterialResponse exposes: endgame_skill_percentile: number | null   (value in [0.0, 1.0]; 0.5 = population median; replaces removed endgame_skill_rate_mean)

ENDGAME_SKILL_ZONES (frontend/src/generated/endgameZones.ts — regenerated by Plan 01): GaugeZone[] with fractional [0,1] bounds centred on 0.5; EndgameGauge receives value = percentile*100, maxValue=100, compares value/maxValue against zone.from/to. NO gauge-component change needed.

Verbatim D-10 URL (wire EXACTLY; hardcoded, NOT user-controlled): https://github.com/flawchess/flawchess/blob/main/reports/benchmarks-latest.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — EndgameSkillCard.test.tsx scaffold (SC#4 gauge/headline/null/no-Recovery-copy)</name>
  <read_first>
    - frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx (lines 1-100 Vitest+RTL setup; lines 148-188 fixture builder + render pattern)
    - frontend/src/components/charts/EndgameSkillCard.tsx (full file — current props, SKILL_POPOVER_COPY ~46, gaugeValue ~98, hasSkill guard ~77, not-enough-data branch)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (the EndgameSkillCard.test.tsx section, lines 743-809)
  </read_first>
  <behavior>
    - renders percentile gauge at 73: skillPercentile=0.73 => headline testid text contains "73%"
    - renders "Better than X% of players" headline: skillPercentile=0.73 => "Better than 73% of players"
    - renders not-enough-data when skillPercentile is null
    - SKILL_POPOVER_COPY does NOT contain the word "Recovery"
  </behavior>
  <action>
    Create frontend/src/components/charts/EndgameSkillCard.test.tsx with the `// @vitest-environment jsdom` header, RTL imports, afterEach(cleanup), and a renderSkillCard(skillPercentile) helper rendering EndgameSkillCard inside TooltipProvider (copy required props from the current EndgameSkillCardProps shape — endgameWdl, scoreGap*, tileTestId; build minimal fixtures). Write the four behaviors above. The headline assertion targets data-testid tile-endgame-skill-headline (added in Task 2). These tests are RED until Task 2. Do not assert exact zone colors (Plan 01 benchmark band dependent) — assert structural text only.
  </action>
  <verify>
    <automated>cd frontend && npm test -- EndgameSkillCard</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/charts/EndgameSkillCard.test.tsx exists with 4 it(...) cases
    - The 4 cases are collected and FAILING against the current pre-Task-2 component (RED expected — skillPercentile prop / headline testid absent)
    - cd frontend && npx tsc --noEmit does not error on the test file imports
  </acceptance_criteria>
  <done>SC#4 Vitest scaffold committed; RED specs target the Task 2 percentile component.</done>
</task>

<task type="auto">
  <name>Task 2: Percentile gauge + headline + D-10 popover + SKILL_POPOVER_COPY + type swap</name>
  <read_first>
    - frontend/src/components/charts/EndgameSkillCard.tsx (full file: props ~49-65, SKILL_POPOVER_COPY ~46, hasSkill ~77, gaugeValue ~98, InfoPopover content ~104-137, MetricStatPopover methodology ~210-213)
    - frontend/src/types/endgames.ts (lines 206-218 — endgame_skill_rate_mean field block)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (EndgameSkillCard.tsx Changes 1-7 lines 496-572; types/endgames.ts section lines 601-621; InfoPopover space-y-2 + external-link pattern)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-CONTEXT.md (D-07 headline wording, D-10 popover content + verbatim URL)
  </read_first>
  <action>
    types/endgames.ts: remove endgame_skill_rate_mean: number | null and its quick-260515-wye comment; add endgame_skill_percentile: number | null with the Phase 87.3 block comment per RESEARCH.md.

    EndgameSkillCard.tsx:
    1. Rename prop skill: number | null to skillPercentile: number | null (JSDoc: "Percentile rank of Conv+Parity composite vs FlawChess Benchmark [0,1]; null when fewer than CONFIDENCE_MIN_N spans in every active bucket").
    2. hasSkill = skillPercentile !== null.
    3. gaugeValue = (skillPercentile ?? 0) * 100 (same formula, new input). Keep the existing ENDGAME_SKILL_ZONES import and EndgameGauge value/zones props unchanged (Plan 01 already updated the bounds via codegen).
    4. Add a paragraph element directly below the closing EndgameGauge tag, inside the hasSkill branch: text "Better than {Math.round((skillPercentile ?? 0) * 100)}% of players", className "text-sm text-center text-muted-foreground", data-testid set to the tileTestId plus "-headline". Use text-sm (CLAUDE.md font floor); data-testid per browser-automation rules.
    5. Replace the InfoPopover inner content with the D-10 copy: a div with className "space-y-2" containing one paragraph that names "the FlawChess Benchmark" as an anchor (href="https://github.com/flawchess/flawchess/blob/main/reports/benchmarks-latest.md" target="_blank" rel="noopener noreferrer" className="underline", text "FlawChess Benchmark") — URL VERBATIM and hardcoded, never from props/state (threat T-87.3-07) — explaining percentile rank over the equal-weighted Conversion + Parity Score Gap, "50 = typical (population median)"; and a second paragraph with the reused Phase 87.1/87.2 sigmoid caveat one-liner ("the Lichess expected-score sigmoid under-weights endgame eval; rely on the zone bands, not the raw magnitude"). Keep the existing InfoPopover wrapper attributes (ariaLabel, testId, side). Wordsmith within memory feedback_popover_copy_minimalism (WHAT + sign/scale + caveat only; no methodology jargon).
    6. SKILL_POPOVER_COPY: rewrite to start "Equal-weighted average of the two per-bucket Score Gaps above (Conversion + Parity, each contributing equally). ..." — drop every "Recovery" and "three" mention.
    7. MetricStatPopover methodology block: "Score: equal-weighted mean of Conv + Parity bucket means. Test: variance-of-sum propagation from the two independent paired-z tests. Confidence interval: 95% normal-approx on the propagated SE." (drop the 3-bucket framing).

    Do NOT change EndgameGauge.tsx (RESEARCH.md Seam 6 confirms no change needed). No new color constants (gauge zones already theme-driven). Run the Task 1 Vitest (RED -> GREEN).
  </action>
  <verify>
    <automated>cd frontend && npm test -- EndgameSkillCard</automated>
  </verify>
  <acceptance_criteria>
    - All 4 EndgameSkillCard.test.tsx cases pass
    - grep -rn "endgame_skill_rate_mean" frontend/src returns no matches (frontend field fully retired)
    - grep -n "Better than" frontend/src/components/charts/EndgameSkillCard.tsx returns the headline; grep -in "recovery" on SKILL_POPOVER_COPY's value returns nothing
    - The verbatim URL https://github.com/flawchess/flawchess/blob/main/reports/benchmarks-latest.md appears as a string literal in EndgameSkillCard.tsx (not constructed from a variable)
    - cd frontend && npx tsc --noEmit clean for the touched files
  </acceptance_criteria>
  <done>Percentile gauge + "Better than X%" headline + D-10 popover live; SKILL_POPOVER_COPY de-Recovery'd; type field swapped; Vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: Grid reorder (D-05) + skillPercentile prop wiring + Recovery descriptive popover (D-09)</name>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsSection.tsx (lines 51-80 TITLE_TOOLTIPS incl. recovery entry; lines 107-201 grid layout + DOM order + EndgameSkillCard prop at ~180 + ConnectorArrows ~191-197)
    - frontend/src/components/charts/EndgameMetricCard.tsx (full — confirm titleTooltip prop threading; D-09 copy goes in TITLE_TOOLTIPS, not the component)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (EndgameMetricsSection.tsx Change 1+2 + ConnectorArrows note lines 450-492; EndgameMetricCard.tsx Recovery popover lines 575-597)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-CONTEXT.md (D-05 reading order + "no row collapse, no grouping boxes/connectors"; D-06 full card anatomy; D-09 one-liner intent; memory feedback_mobile_layout_scope — inside-card changes, keep outer grid)
  </read_first>
  <action>
    EndgameMetricsSection.tsx:
    1. Swap the EndgameSkillCard prop from skill={data.endgame_skill_rate_mean} to skillPercentile={data.endgame_skill_percentile}.
    2. Reorder DOM + grid placement to the D-05 reading order Conv -> Parity -> Skill -> Recovery, KEEPING the existing grid grid-cols-1 lg:grid-cols-3 (no row collapse, no grouping box). Use RESEARCH.md Pattern 4 Option B: Conversion col 1 row 1, Parity col 2 row 1, Skill lg:col-start-2 lg:row-start-2 (under Parity), Recovery lg:col-start-3 lg:row-start-2 (right of Skill in row 2). DOM order must mirror reading order Conv -> Parity -> Skill -> Recovery so mobile single-column stacking is correct. Preserve all existing data-testid values on the card wrappers (tile-conversion / tile-parity / tile-endgame-skill / tile-recovery).
    3. ConnectorArrows: Recovery is no longer a Skill input. Update the ConnectorArrows props so no connector implies Recovery feeds Skill — remove the rightCardTestId="tile-recovery" -> Skill arrow (keep conv/parity -> skill connectors if they still render coherently after the reorder; if the reorder makes the SVG overlay visually wrong, remove the ConnectorArrows usage entirely rather than draw a misleading arrow — D-05 explicitly forbids connectors implying Recovery feeds Skill). Record the chosen approach in the SUMMARY.
    4. TITLE_TOOLTIPS recovery entry: append a paragraph to the existing recovery tooltip JSX: "Recovery reflects how often opponents failed to convert against you, not your defensive skill — deliberately excluded from Endgame Skill." (em-dash used sparingly per CLAUDE.md; one sentence, no methodology jargon per memory feedback_popover_copy_minimalism; D-09). Keep the existing Recovery gauge + ΔES bullet rendering untouched (D-06 full card anatomy; no "vs peer" framing added).

    Do not introduce text smaller than text-sm. Do not add new theme color literals. After changes, run knip + the Section Vitest + lint.
  </action>
  <verify>
    <automated>cd frontend && npm run knip && npm test -- EndgameMetricsSection EndgameSkillCard && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "skillPercentile=" frontend/src/components/charts/EndgameMetricsSection.tsx returns the wired prop; no remaining skill={data.endgame_skill_rate_mean}
    - The grid container still uses lg:grid-cols-3 (no row collapse): grep -n "lg:grid-cols-3" EndgameMetricsSection.tsx returns 1
    - DOM order of the four card wrappers is Conversion, Parity, Skill, Recovery (verify by reading the JSX order)
    - The Recovery TITLE_TOOLTIPS entry contains "deliberately excluded from Endgame Skill" and no "vs peer"/"vs opponent skill" phrasing was added
    - No ConnectorArrows element draws an arrow from tile-recovery to tile-endgame-skill (either removed or repointed)
    - cd frontend && npm run knip exits 0 (no dead exports from the retired rate path) and npm run lint clean
  </acceptance_criteria>
  <done>Section 2 reads Conv -> Parity -> Skill -> Recovery on the preserved 3-col grid; Skill consumes the percentile; Recovery is a descriptive tile with full anatomy + opponent-confound one-liner; knip + Vitest + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → frontend | `endgame_skill_percentile` consumed as a display number; no user input echoed |
| external GitHub link in popover | Outbound navigation to a fixed, hardcoded URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-07 | Tampering | FlawChess Benchmark popover link target | mitigate | URL is a hardcoded string literal in EndgameSkillCard.tsx (acceptance criterion asserts it is not built from a variable/prop/state) — not user-controlled, no injection surface |
| T-87.3-08 | Information Disclosure | External link leaking referrer | mitigate | Anchor uses `rel="noopener noreferrer"` + `target="_blank"` (project convention) |
| T-87.3-09 | Spoofing | Recovery copy implying it is a skill verdict | mitigate | D-09 one-liner states it is opponent-dependent and excluded from Skill; acceptance asserts no "vs peer" framing added |
| T-87.3-SC | Tampering | npm installs | accept | No new npm packages (RESEARCH.md audit: none) |
</threat_model>

<verification>
- cd frontend && npm test -- EndgameSkillCard EndgameMetricsSection green (SC#2 gauge, SC#4 Recovery copy)
- npm run knip + npm run lint clean (SC#6); no endgame_skill_rate_mean references remain in frontend
- Verbatim D-10 URL present as a literal; Section reads Conv -> Parity -> Skill -> Recovery on the 3-col grid (SC#2/SC#4)
</verification>

<success_criteria>
SC#2 (percentile gauge, "Better than X%" headline, neutral zone centred at 50 via Plan 01 codegen, knip/codegen drift gate green) and SC#4 (Recovery standalone descriptive tile with full card anatomy + opponent-confound popover, no "vs peer" framing) are met on the frontend.
</success_criteria>

<output>
Create .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-03-SUMMARY.md when done. Record: the final grid-cell placement chosen for Skill/Recovery, the ConnectorArrows decision (repointed vs removed), and the exact Recovery one-liner + Skill popover copy as shipped.
</output>
