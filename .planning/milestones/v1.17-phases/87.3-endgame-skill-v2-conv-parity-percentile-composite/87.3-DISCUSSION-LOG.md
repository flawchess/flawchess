# Phase 87.3: Endgame Skill v2 — Conv+Parity percentile composite - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-16
**Phase:** 87.3-endgame-skill-v2-conv-parity-percentile-composite
**Areas discussed:** Frozen reference shape + location + versioning, Recovery standalone tile placement & layout, Endgame Skill headline framing under percentile, Recovery exclusion copy & framing + LLM bump

Note: math substrate, Lichess reference curve, percentile mandate, ≥10-span floor, and the `s∈[0,1]`/median⇒0.5 invariant were LOCKED by `.planning/notes/endgame-skill-recovery-confound.md` before discussion — not re-litigated. Pooling granularity was confirmed LOCKED to single global pooled (SPEC item 2 + invariant), correcting the initial gray-area framing.

---

## Frozen reference shape + location + versioning

| Option | Description | Selected |
|--------|-------------|----------|
| Empirical quantile table | Interpolated quantile knots; no normality assumption; roadmap option (i) | ✓ |
| Full empirical CDF | Every benchmark composite value; finer than signal warrants | |
| μ/σ + assumed-normal Φ | Lightest; assumes normality the composite likely violates | |

| Option | Description | Selected |
|--------|-------------|----------|
| Python constant + TS codegen mirror | Constant + gen script + CI drift-gate, mirrored to FE | ✓ (refined) |
| Versioned data file (app/data/) | JSON/parquet loaded at startup | |
| DB table | Calibration table + migration + per-point version pin | |

| Option | Description | Selected |
|--------|-------------|----------|
| Manual script + version constant, explicit refresh only | Non-drift by construction | ✓ |
| Auto-regenerate on benchmark rebuild | Would drift historical points | |

| Option | Description | Selected |
|--------|-------------|----------|
| Backend-only table; FE gets only the zone band | Percentile computed backend-side; no TS mirror for the table | ✓ |
| Mirror full quantile table to TS | Larger generated file, no FE consumer | |

**User's choice:** Empirical quantile table; backend Python constant + manual gen script + CI git-diff drift-gate on the Python artifact; manual-refresh-only with `REFERENCE_VERSION`; backend-only (FE gets only the Skill neutral-band zone via existing `endgameZones.ts`).
**Notes:** The "Python constant + TS codegen mirror" choice was refined by the follow-up: the drift-gate runs on the Python artifact itself, NOT a TS mirror of the table (no FE consumer).

---

## Recovery standalone tile — placement & layout

| Option | Description | Selected |
|--------|-------------|----------|
| Keep grid, reorder Recovery after Skill | Conv → Parity → Skill → Recovery; no row collapse | ✓ |
| Keep current order, copy/divider only | Recovery still reads as equal sibling | |
| Visually group skill-contributing cards | Biggest layout change; conflicts with keep-the-grid preference | |

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both gauge + Score Gap bullet, relabel descriptive | Consistent card anatomy | ✓ |
| Drop the gauge, keep only the Score Gap bullet | Divergent anatomy; loses Save % rate | |

**User's choice:** Reorder to Conv → Parity → Skill → Recovery, keep 3-col grid; Recovery keeps Save % gauge + ΔES bullet, relabeled descriptive.
**Notes:** Exact grid-cell placement left to planner.

---

## Endgame Skill headline framing under percentile

| Option | Description | Selected |
|--------|-------------|----------|
| Percentile: "Better than X% of players" | Most honest/self-explanatory | ✓ |
| 0–100 "Skill Index", 50 = typical | Hides percentile basis | |
| Keep "Endgame Skill" label, percentile in subtext | Less direct | |

| Option | Description | Selected |
|--------|-------------|----------|
| Editorial band + weak/typical/strong labels | Consistent with other Section 2 gauges; bounds finalized in plan-phase | ✓ |
| Symmetric fixed band (e.g. 40–60) | Ignores actual benchmark spread (d≈1.39) | |

**User's choice:** "Better than X% of players", 0–100 gauge centered at 50; editorial weak/typical/strong band, bounds finalized in plan-phase.
**Notes:** None.

---

## Recovery exclusion copy & framing + LLM bump

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit one-liner: opponent-dependent, excluded from Skill | One sentence, no jargon/hedging | ✓ |
| Soft framing: "comeback" descriptive aside | Too weak for SC#4 | |
| Strong: visual de-emphasis + caveat | Not required; one-liner suffices | |

| Option | Description | Selected |
|--------|-------------|----------|
| "vs a fixed reference of rated players" — plain | Generic, no internal naming | |
| Name the Lichess benchmark reference explicitly | Internal calibration vocabulary | |
| **Other (user free-text)** | Cite "the FlawChess Benchmark" + link to GitHub `benchmarks-latest.md` | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Bump version, changelog metric change + Recovery exclusion | Model gets the "why" | ✓ |
| Bump version, minimal changelog only | LLM loses rationale | |

**User's choice:** Explicit one-liner for Recovery; Endgame Skill popover cites "the FlawChess Benchmark" and links to `https://github.com/flawchess/flawchess/blob/main/reports/benchmarks-latest.md`; bump prompt version with full changelog of the metric change + Recovery-confound rationale.
**Notes:** The percentile-reference disclosure was a user free-text override of the offered options — explicit named attribution + hyperlink, treated as a deliberate transparency choice (labeled source link, not inline jargon). Overrides the generic popover-minimalism default for this specific popover.

---

## Claude's Discretion

- Exact grid-cell layout after the Conv→Parity→Skill→Recovery reorder.
- Skill CI propagation under percentile mapping, ≥10-span-floor edge cases, rolling-window timeline recomputation — governed by LOCKED SPEC + Phase 57/87.2 infra.
- Dedicated backend module name/path for the quantile table.

## Deferred Ideas

None — discussion stayed within phase scope. (Conversion Score Gap display-centering remains a pre-existing separate todo, out of scope per ROADMAP — not a new deferral.)
