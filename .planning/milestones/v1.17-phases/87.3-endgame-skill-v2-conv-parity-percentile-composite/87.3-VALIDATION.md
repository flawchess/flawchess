---
phase: 87.3
slug: endgame-skill-v2-conv-parity-percentile-composite
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-16
---

# Phase 87.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) + vitest (frontend) |
| **Config file** | `pyproject.toml` (pytest) / `frontend/vitest.config.ts` |
| **Quick run command** | `uv run pytest -x` |
| **Full suite command** | `uv run pytest && cd frontend && npm test` |
| **Estimated runtime** | ~120 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest -x` (scoped to the touched test file where possible; frontend tasks run `cd frontend && npm test -- <pattern>`)
- **After every plan wave:** Run `uv run pytest && cd frontend && npm test`
- **Before `/gsd:verify-work`:** Full suite + `uv run ty check app/ tests/` + `uv run ruff check .` + `cd frontend && npm run knip` + `npm run lint` must be green; both drift-gates (`endgameZones.ts`, `endgame_skill_reference.py`) clean
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 87.3-01 | 1 | SC#2 | T-87.3-03 | Pure interpolation, no I/O | Unit | `uv run pytest tests/test_endgame_skill_reference.py -x` | ❌ Wave 0 (this task creates it) | ⬜ pending |
| 01-T2 | 87.3-01 | 1 | SC#2, SC#3 | T-87.3-01, T-87.3-02 | DB only under --refresh; deterministic re-emit | Unit + CLI | `uv run python scripts/gen_endgame_skill_reference.py --check` then `git diff --exit-code app/services/endgame_skill_reference.py` then `uv run pytest tests/test_endgame_skill_reference.py -x` | ✅ (after T1) | ⬜ pending |
| 01-T3 | 87.3-01 | 1 | SC#6 | T-87.3-01 | CI drift-gate blocks silent drift | CI grep | `grep -n gen_endgame_skill_reference.py .github/workflows/ci.yml` | ✅ | ⬜ pending |
| 02-T1 | 87.3-02 | 2 | SC#3 | T-87.3-06 | ELO formula invariant guarded | Unit | `uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2::test_endgame_elo_invariant -x` | ❌ Wave 0 (this task creates it) | ⬜ pending |
| 02-T2 | 87.3-02 | 2 | SC#1, SC#6 | T-87.3-04 | Recovery cannot leak into composite | Unit | `uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2 -k composite -x` | ✅ (after 02-T1) | ⬜ pending |
| 02-T3 | 87.3-02 | 2 | SC#1, SC#3 | T-87.3-05, T-87.3-06 | None-guarded; ELO formula unchanged | Unit | `uv run pytest tests/test_endgame_service.py::TestEndgameSkillV2 -x` | ✅ | ⬜ pending |
| 03-T1 | 87.3-03 | 2 | SC#4 | — | — | Vitest | `cd frontend && npm test -- EndgameSkillCard` | ❌ Wave 0 (this task creates it) | ⬜ pending |
| 03-T2 | 87.3-03 | 2 | SC#2, SC#4 | T-87.3-07, T-87.3-08 | Hardcoded URL; rel=noopener | Vitest | `cd frontend && npm test -- EndgameSkillCard` | ✅ (after 03-T1) | ⬜ pending |
| 03-T3 | 87.3-03 | 2 | SC#2, SC#4, SC#6 | T-87.3-09 | No "vs peer" framing on Recovery | Vitest + knip | `cd frontend && npm run knip` then `npm test -- EndgameMetricsSection EndgameSkillCard` then `npm run lint` | ✅ | ⬜ pending |
| 04-T1 | 87.3-04 | 3 | SC#5, SC#6 | T-87.3-11 | Recovery not headline-eligible | Unit | `uv run pytest tests/test_insights_service.py -k endgame_metrics -x` | Partial (extend existing) | ⬜ pending |
| 04-T2 | 87.3-04 | 3 | SC#5 | T-87.3-10 | Prompt version cache-bust | Grep | `grep -n endgame_v32 app/services/insights_llm.py` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Sampling continuity check:** No 3 consecutive tasks lack an automated verify — every task above has an `<automated>` command. No watch-mode flags used. Max latency < 120s (scoped pytest / vitest runs).

---

## Wave 0 Requirements

Wave 0 scaffolds are created as the FIRST task of each plan (they precede the implementation tasks they gate):

- [ ] **`tests/test_endgame_skill_reference.py`** (Plan 01 Task 1) — SC#2: `interpolate_percentile` monotonic, clamps to [0,1] at tails, median knot ⇒ ≈0.5, exact interior knot. Plus a hand-written placeholder `app/services/endgame_skill_reference.py` (final `interpolate_percentile` logic; `QUANTILE_KNOTS` regenerated in Task 2).
- [ ] **`tests/test_endgame_service.py::TestEndgameSkillV2`** (Plan 02 Task 1) — SC#1/SC#3: `test_endgame_elo_invariant` (s=0.5 ⇒ actual_elo, green immediately, guards the unchanged Phase 57 formula), plus RED specs `test_skill_composite_drops_recovery`, `test_skill_composite_floor_one_active`, `test_skill_composite_all_below_floor`, `test_elo_timeline_percentile_path`, `test_elo_timeline_non_drift`.
- [ ] **`frontend/src/components/charts/EndgameSkillCard.test.tsx`** (Plan 03 Task 1) — SC#4: percentile gauge value, "Better than X% of players" headline, null state, SKILL_POPOVER_COPY has no "Recovery".
- [ ] **`tests/test_insights_service.py`** (Plan 04 Task 1) — SC#5: extend the existing endgame-metrics finding test (endgame_skill value tracks `endgame_skill_percentile`; section2_score_gap_recov `is_headline_eligible is False`).

`wave_0_complete` flips to `true` once all four scaffolds are committed (Task 1 of each plan).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| LLM report narrative reads Endgame Skill as a percentile and treats Recovery as descriptive (not a skill verdict) | SC#5 | LLM output is non-deterministic; copy/tone judged editorially per memory `feedback_llm_prompt_design` | After Plan 04, generate an endgame insights report for a test user with a known percentile; confirm the narrative says "better than X%"-style framing for Skill and frames Recovery as opponent-dependent without a skill verdict |
| Real-data percentile sanity (a known mid-population benchmark user maps near 50) | SC#2/SC#3 | Requires the benchmark DB + a real composite; the unit test only asserts the median-knot round-trip | After Plan 01 `--refresh`, spot-check 2–3 benchmark users of differing ELO: low-ELO < 50, high-ELO > 50, median ≈ 50 |
| Section 2 visual layout at desktop + 375px after the Conv→Parity→Skill→Recovery reorder | SC#4 | CSS grid placement is visual; Vitest asserts DOM/text only | Load the Endgames page; confirm reading order, no row collapse, no connector implying Recovery feeds Skill, no horizontal scroll at 375px (full sweep is Phase 89 scope) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (4 scaffolds, one per plan, each plan's Task 1)
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready (pending execution)
