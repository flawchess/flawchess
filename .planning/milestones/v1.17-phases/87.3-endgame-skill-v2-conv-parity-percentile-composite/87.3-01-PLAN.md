---
phase: 87.3-endgame-skill-v2-conv-parity-percentile-composite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_skill_reference.py
  - scripts/gen_endgame_skill_reference.py
  - app/services/endgame_zones.py
  - frontend/src/generated/endgameZones.ts
  - .github/workflows/ci.yml
  - tests/test_endgame_skill_reference.py
autonomous: true
requirements: [SC#2, SC#3, SC#6]
must_haves:
  truths:
    - "interpolate_percentile maps the benchmark population median composite to 0.5 within tolerance"
    - "interpolate_percentile is monotonic non-decreasing and clamps to [0.0, 1.0] at the tails"
    - "The frozen reference artifact is regenerated only via explicit --refresh; CI drift-gate blocks silent drift"
    - "The Endgame Skill gauge zone band is centred on 0.5 on the percentile scale and derived from the benchmark composite spread"
  artifacts:
    - path: "app/services/endgame_skill_reference.py"
      provides: "Frozen QUANTILE_KNOTS + REFERENCE_VERSION + interpolate_percentile()"
      contains: "REFERENCE_VERSION"
    - path: "scripts/gen_endgame_skill_reference.py"
      provides: "Benchmark-DB-driven gen script with --refresh / --check modes"
      contains: "def main"
    - path: "tests/test_endgame_skill_reference.py"
      provides: "SC#2 percentile invariant tests (monotonic, clamp, median⇒0.5)"
      contains: "class TestInterpolatePercentile"
  key_links:
    - from: "scripts/gen_endgame_skill_reference.py"
      to: "app/services/endgame_skill_reference.py"
      via: "_OUTPUT.write_text deterministic re-emit"
      pattern: "_OUTPUT"
    - from: ".github/workflows/ci.yml"
      to: "app/services/endgame_skill_reference.py"
      via: "git diff --exit-code drift-gate"
      pattern: "endgame_skill_reference"
    - from: "app/services/endgame_zones.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "gen_endgame_zones_ts.py codegen of ENDGAME_SKILL_ZONES"
      pattern: "ENDGAME_SKILL_ZONES"
---

<objective>
Create the frozen, versioned Endgame Skill percentile reference artifact — the first versioned calibration artifact in the project — and derive the D-08 neutral-band bounds from a benchmark-DB query. Everything downstream (Plan 02 backend percentile mapping, Plan 03 gauge, Plan 04 LLM payload) depends on this module existing with a stable `interpolate_percentile` contract and the updated `ZONE_REGISTRY["endgame_skill"]` percentile bounds.

Purpose: SC#2 (percentile gauge from a frozen versioned reference, neutral zone centred at 50) and SC#3 (timeline non-drift by construction) are structurally satisfied by this artifact. The D-08 band derivation resolves RESEARCH.md's single LOW-confidence open item.
Output: `app/services/endgame_skill_reference.py` (frozen Python constant + pure interpolation function), `scripts/gen_endgame_skill_reference.py` (benchmark-DB gen script with `--refresh`/`--check`), updated `ZONE_REGISTRY["endgame_skill"]` percentile bounds + regenerated `endgameZones.ts`, CI drift-gate extension, and the `tests/test_endgame_skill_reference.py` Wave 0 scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/notes/endgame-skill-recovery-confound.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-CONTEXT.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-RESEARCH.md
@.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md
@scripts/gen_endgame_zones_ts.py
@app/services/endgame_zones.py
@.claude/skills/benchmarks/SKILL.md

<interfaces>
<!-- Contracts this plan creates; consumed by Plans 02/03/04. Executor: implement exactly. -->

app/services/endgame_skill_reference.py (NEW):
```python
REFERENCE_VERSION: str  # e.g. "v1" — bumped in the same commit as any --refresh regeneration
QUANTILE_KNOTS: list[tuple[float, float]]  # (composite_value, percentile) sorted ASC; percentile in [0.0, 1.0]
def interpolate_percentile(composite: float) -> float:  # piecewise-linear; clamps to [0.0, 1.0]; pure, no I/O
```

_compute_span_gap signature (app/services/endgame_service.py:260 — POSITIONAL, for the gen-script benchmark query to mirror in SQL):
  _compute_span_gap(entry_eval_cp, entry_eval_mate, next_entry_eval_cp, next_entry_eval_mate, result, user_color)
  Terminal span = both next_entry_eval_* are None ⇒ exit_score from game result.

CONFIDENCE_MIN_N = 10 (the ≥10-span floor per active bucket — import from app/services/endgame_service.py).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — interpolate_percentile test scaffold + placeholder reference module</name>
  <read_first>
    - tests/test_endgame_service.py (lines 1-48 import block, lines 128-199 TestClassifyEndgameBucket — pure-unit boundary-case style to clone)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (the `tests/test_endgame_skill_reference.py` section, lines 624-681 — copy structure verbatim)
    - app/services/endgame_zones.py (lines 1-18 module docstring — "pure Python, no DB or I/O" convention)
  </read_first>
  <behavior>
    - test_clamps_below_min_knot: composite below QUANTILE_KNOTS[0][0] ⇒ 0.0
    - test_clamps_above_max_knot: composite above QUANTILE_KNOTS[-1][0] ⇒ 1.0
    - test_monotonic_over_knot_range: 200-step sweep is non-decreasing
    - test_median_invariant: the median knot composite ⇒ |result − 0.5| < 0.05 (SC#3)
    - test_exact_knot_value: a knot's composite_value ⇒ its exact percentile (±1e-9)
  </behavior>
  <action>
    Create `app/services/endgame_skill_reference.py` as a hand-written PLACEHOLDER (Task 2's gen script will overwrite it once the benchmark query runs). The placeholder MUST define `REFERENCE_VERSION = "v1"`, a small but valid `QUANTILE_KNOTS: list[tuple[float, float]]` (≈11 monotone knots spanning a plausible signed ΔES range with the median knot at percentile 0.5, e.g. `(-0.20, 0.0) … (0.00, 0.5) … (0.18, 1.0)`), and the real `interpolate_percentile(composite: float) -> float` implementation per the PATTERNS.md analog (bisect.bisect_right on knot x-values + linear interpolation, clamps to [0.0, 1.0] at the tails). The function body is FINAL here — Task 2 only regenerates `QUANTILE_KNOTS`/`REFERENCE_VERSION`, never the function logic. Add the module docstring per the `endgame_zones.py` convention ("pure Python with no DB or I/O") plus the AUTO-GENERATED header + `# Regenerate with: uv run python scripts/gen_endgame_skill_reference.py`.

    Create `tests/test_endgame_skill_reference.py` with `class TestInterpolatePercentile` and the five tests above, importing `from app.services.endgame_skill_reference import QUANTILE_KNOTS, interpolate_percentile`. No `@pytest.mark.asyncio` — pure sync function. Tests must pass against the placeholder knots (they assert structural invariants, not specific benchmark numbers, so they stay green after Task 2 regenerates real knots).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_skill_reference.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_endgame_skill_reference.py -x` exits 0 (5 tests pass)
    - `python -c "from app.services.endgame_skill_reference import interpolate_percentile, REFERENCE_VERSION, QUANTILE_KNOTS; assert interpolate_percentile(-99.0)==0.0 and interpolate_percentile(99.0)==1.0 and REFERENCE_VERSION=='v1' and QUANTILE_KNOTS==sorted(QUANTILE_KNOTS)"` exits 0
    - `uv run ty check app/services/endgame_skill_reference.py tests/test_endgame_skill_reference.py` reports zero errors
  </acceptance_criteria>
  <done>Placeholder reference module + passing invariant test scaffold exist; interpolate_percentile logic is final and ty-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Benchmark-DB gen script + regenerate real frozen QUANTILE_KNOTS + derive D-08 band</name>
  <read_first>
    - scripts/gen_endgame_zones_ts.py (full file — exact structural clone: _REPO_ROOT, sys.path.insert, _OUTPUT, _render(), --check / main(), _OUTPUT.write_text)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (the gen-script section, lines 100-188 — module docstring, --refresh guard, _render() pattern)
    - .claude/skills/benchmarks/SKILL.md (Standard CTE `selected_users` with bic.status='completed'; equal-footing filter abs(opp_rating − user_rating) ≤ 100; sparse-cell exclusion NOT (rating_bucket=2400 AND tc_bucket='classical'); §3.2.2 per-bucket ΔES query shape)
    - app/services/endgame_service.py (lines 196-260 _classify_endgame_bucket + _compute_span_gap — replicate the conv/parity bucket boundaries and the ES_entry sigmoid in SQL for the benchmark query)
    - .planning/notes/endgame-skill-recovery-confound.md (LOCKED SPEC — Lichess 2300+ sigmoid k=0.00368208, ≥10-span floor, Conv+Parity equal-weight, Recovery excluded)
  </read_first>
  <action>
    Create `scripts/gen_endgame_skill_reference.py` as a structural clone of `scripts/gen_endgame_zones_ts.py`: `_REPO_ROOT = Path(__file__).resolve().parent.parent`, `sys.path.insert(0, str(_REPO_ROOT))`, `_OUTPUT = _REPO_ROOT / "app" / "services" / "endgame_skill_reference.py"`, a `_render()` that emits the full Python module source deterministically from in-module `QUANTILE_KNOTS`/`REFERENCE_VERSION` constants, and a `main()` with `--check` (exit 1 on `git diff` mismatch, no DB) and plain re-emit (no DB) modes copied verbatim from the analog. Add a `--refresh` guard per D-03: ONLY `--refresh` queries the benchmark DB; default and `--check` re-emit from the already-embedded constants (no DB round-trip — this is what lets CI run it without benchmark DB access, per RESEARCH.md Assumption A4).

    Implement the `--refresh` path: connect to the benchmark DB (port 5433; ensure `bin/benchmark_db.sh start` is running) and run a query that mirrors SKILL.md §3.2.2: `selected_users` CTE (mandatory `bic.status='completed'` join) → spans with LEAD next-entry-eval → per-span ΔES gap (ES_entry = Lichess 2300+ sigmoid k=0.00368208; terminal span ⇒ game-result score) → bucket-assign to conversion (entry_eval ≥ +100cp user-perspective) / parity (−100..+100cp) ONLY (Recovery EXCLUDED) → per (user, bucket) mean with ≥10-span floor (CONFIDENCE_MIN_N) → per-user equal-weighted Conv+Parity composite (only buckets clearing the floor; denominator = count of active buckets) → apply equal-footing filter `abs(opp_rating − user_rating) ≤ 100` and sparse-cell exclusion `NOT (rating_bucket=2400 AND tc_bucket='classical')` in the pooled aggregation. Derive ~101 quantile knots via `percentile_cont(k/100.0) WITHIN GROUP (ORDER BY composite)` for k in 0..100. Emit `(composite_value, k/100.0)` pairs as `QUANTILE_KNOTS`, sorted ASC by composite_value. Bump/set `REFERENCE_VERSION = "v1"` in the emitted module.

    Run `uv run python scripts/gen_endgame_skill_reference.py --refresh` to overwrite the Task 1 placeholder with the real frozen knots. Then run the D-08 band query against the same benchmark DB: get the pooled composite distribution percentiles, and set `ZONE_REGISTRY["endgame_skill"]` in `app/services/endgame_zones.py` to percentile-scale bounds (`typical_lower`/`typical_upper` on the [0,1] percentile axis, centred at 0.5, `direction="higher_is_better"`). Start from [p25, p75] of the percentile distribution; since percentile rank is uniform by construction this is naively [0.25, 0.75] — instead set an EDITORIAL band tighter than naive symmetric per memory `feedback_zone_band_judgement` and the LOCKED SPEC d≈1.39: use the composite-value spread that separates ELO cohorts to choose bounds (e.g. the percentile positions of the benchmark composite ±0.5·within-cohort-SD, finalized as a centred band like [0.40, 0.60] or tighter — record the exact derivation + chosen numbers in the inline `# 260516:` comment following the existing `ZONE_REGISTRY` history-comment convention). Replace the existing `# 260503: lower bound shifted...` comment block with the Phase 87.3 derivation note. Regenerate `frontend/src/generated/endgameZones.ts` via `uv run python scripts/gen_endgame_zones_ts.py` (no gen-script change needed — RESEARCH.md Seam 6 confirms the [0,1] fractional format is already percentile-compatible).

    Re-run `uv run pytest tests/test_endgame_skill_reference.py -x` to confirm the real knots still satisfy the invariants (especially `test_median_invariant` — the benchmark median composite must map to ≈0.5; if it does not, the quantile derivation is wrong, not the test).
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_skill_reference.py --check && git diff --exit-code app/services/endgame_skill_reference.py && uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts && uv run pytest tests/test_endgame_skill_reference.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/gen_endgame_skill_reference.py --check` exits 0 (committed artifact matches re-emit, no DB)
    - `git diff --exit-code app/services/endgame_skill_reference.py` is clean after a plain (no-flag) gen run
    - `app/services/endgame_skill_reference.py` contains ≥50 QUANTILE_KNOTS tuples and `REFERENCE_VERSION = "v1"`
    - `python -c "from app.services.endgame_skill_reference import QUANTILE_KNOTS, interpolate_percentile; mids=[k for k in QUANTILE_KNOTS if abs(k[1]-0.5)<0.011]; assert mids; assert abs(interpolate_percentile(mids[0][0])-0.5)<0.05"` exits 0 (median⇒0.5 invariant on real benchmark knots — SC#3)
    - `ZONE_REGISTRY["endgame_skill"]` typical_lower/typical_upper are on the [0,1] percentile scale, centred such that `(lower+upper)/2 ≈ 0.5`, and NOT the old rate-scale (0.47, 0.55)
    - `git diff --exit-code frontend/src/generated/endgameZones.ts` is clean after `gen_endgame_zones_ts.py` (codegen committed)
    - `--refresh` is the ONLY code path that opens a benchmark DB connection (grep the script: DB connect string appears only under the `--refresh` branch)
  </acceptance_criteria>
  <done>Real frozen QUANTILE_KNOTS regenerated from benchmark DB; D-08 percentile band set and codegen'd into endgameZones.ts; gen script re-emits deterministically without DB; median⇒0.5 holds on real data.</done>
</task>

<task type="auto">
  <name>Task 3: Extend CI Zone-drift-check step with the reference artifact gate</name>
  <read_first>
    - .github/workflows/ci.yml (the "Zone drift check" step, lines ~47-50 — exact current commands)
    - .planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-PATTERNS.md (the ci.yml section, lines 192-214 — exact two-line extension)
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, extend the existing "Zone drift check" step (the one that runs `uv run python scripts/gen_endgame_zones_ts.py` then `git diff --exit-code frontend/src/generated/endgameZones.ts`) by appending two lines to the same `run:` block: `uv run python scripts/gen_endgame_skill_reference.py` then `git diff --exit-code app/services/endgame_skill_reference.py`. Do NOT add `--refresh` (CI has no benchmark DB; the plain run re-emits from embedded constants per Task 2). Do not create a new step or modify any other CI step.
  </action>
  <verify>
    <automated>grep -n 'gen_endgame_skill_reference.py' .github/workflows/ci.yml && grep -A6 'Zone drift check' .github/workflows/ci.yml | grep -q 'git diff --exit-code app/services/endgame_skill_reference.py' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` "Zone drift check" step contains both `uv run python scripts/gen_endgame_skill_reference.py` (no `--refresh`) and `git diff --exit-code app/services/endgame_skill_reference.py`
    - No new CI step was added (the two lines are inside the existing Zone drift check `run:` block)
    - The pre-existing two zone-drift lines are unchanged
  </acceptance_criteria>
  <done>CI drift-gate blocks silent drift of the frozen reference artifact, paralleling the established zone codegen gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark DB → gen script (local, --refresh only) | Read-only query; runs only on a deliberate local refresh, never in CI or at runtime |
| committed reference artifact → backend runtime | Frozen constants consumed by pure interpolation; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-01 | Tampering | Frozen reference artifact silent drift | mitigate | CI `git diff --exit-code app/services/endgame_skill_reference.py` (Task 3) blocks any uncommitted regeneration; D-03 manual-refresh-only versioning |
| T-87.3-02 | Information Disclosure | Benchmark DB credentials in gen script | mitigate | Gen script connects only under `--refresh`; no credentials hardcoded (reuse the project's benchmark DB env/connection convention); CI never runs `--refresh` |
| T-87.3-03 | Tampering | Non-deterministic re-emit causing false CI failures | accept | `_render()` emits sorted knots from embedded constants; deterministic by construction (same pattern as the proven `gen_endgame_zones_ts.py`) |
| T-87.3-SC | Tampering | npm/uv installs | accept | No new packages introduced this phase (RESEARCH.md Package Legitimacy Audit: none); stdlib `bisect` only |
</threat_model>

<verification>
- `uv run pytest tests/test_endgame_skill_reference.py -x` green (SC#2 invariants)
- `uv run python scripts/gen_endgame_skill_reference.py --check` exits 0 with no DB (SC#3 drift-gate)
- `git diff --exit-code app/services/endgame_skill_reference.py` and `frontend/src/generated/endgameZones.ts` both clean after plain gen runs
- median benchmark composite ⇒ interpolate_percentile ≈ 0.5 (SC#3 invariant on real data)
- `uv run ty check app/ tests/` and `uv run ruff check .` clean for the new files (SC#6)
</verification>

<success_criteria>
SC#2 (frozen versioned percentile reference, neutral zone centred at 50) and SC#3 (timeline non-drift by construction via manual-refresh-only versioning + CI drift-gate) are structurally established. The D-08 LOW-confidence open item is resolved with a benchmark-derived editorial band. `interpolate_percentile` provides the stable contract Plans 02/03/04 build on.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/87.3-endgame-skill-v2-conv-parity-percentile-composite/87.3-01-SUMMARY.md` when done. Record: the chosen `REFERENCE_VERSION`, the final `ZONE_REGISTRY["endgame_skill"]` band numbers + the exact D-08 derivation rationale, the benchmark median composite value and its mapped percentile (proving median⇒0.5), and any deviation from the recommended quantile-knot density.
</output>
