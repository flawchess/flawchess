---
phase: 87
plan: 03
type: execute
wave: 3
depends_on:
  - 87-02
files_modified:
  - frontend/src/components/charts/EndgameTypeBreakdownSection.tsx
  - frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/EndgameWDLChart.tsx
  - frontend/src/components/charts/EndgameConvRecovChart.tsx
  - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md
autonomous: false
requirements:
  - SEC3-01
  - SEC3-02
  - SEC3-05
  - SEC3-06
  - SEC3-07
must_haves:
  truths:
    - "`EndgameTypeBreakdownSection.tsx` exists as the orchestrator: filters categories via `HIDDEN_ENDGAME_CLASSES`, computes per-class `sharePct = cat.total / totalGames * 100`, renders a `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` container with one `<EndgameTypeCard>` per surviving class per CONTEXT D-03 / D-06"
    - "Section container has `data-testid=\"endgame-type-breakdown-section\"` (replaces legacy `endgame-wdl-chart` + `conv-recov-chart`) per D-16"
    - "Sub-question paragraph rendered above the grid (D-12): \"Which Endgame Types do you convert best and defend best — and how does each compare to your opponents?\" — no section-level h3 (per CONTEXT.md the section h2 is at the page level in Endgames.tsx)"
    - "5 cards visible when all 6 classes present in data: pawnless is filtered out via `HIDDEN_ENDGAME_CLASSES` (`Endgames.tsx:53`)"
    - "Card props passed per class: `category={cat}`, `sharePct={cat.total / totalGames * 100}`, `onCategorySelect={onCategorySelect}`, `tileTestId={\\`type-card-${ENDGAME_CLASS_TO_SLUG[cat.endgame_class]}\\`}` per D-16"
    - "`Endgames.tsx` mounts `<EndgameTypeBreakdownSection categories={statsData.categories} totalGames={...} onCategorySelect={handleCategorySelect} />` in place of BOTH legacy mounts (`<EndgameWDLChart>` + `<EndgameConvRecovChart>`) at lines 506-518; single charcoal tile wrapper retained"
    - "Page-level h2 `Endgame Type Breakdown` at `Endgames.tsx:507` gets a new `<InfoPopover>` trigger with content adapted from BOTH legacy intros (lifted from `EndgameWDLChart.tsx:275-292` taxonomy + `EndgameConvRecovChart.tsx:38-49` metric defs) per D-12"
    - "URL hydration: `/endgames/games?type=...` slug is read on `Endgames.tsx` mount and pre-seeds the filter state. Concretely: the existing `handleCategorySelect` syncs to filter state; in addition, an effect parses `?type=...` from `window.location.search` or `useSearchParams` on the games-page mount and calls `handleCategorySelect` with the parsed class (mapping slug → EndgameClass via the inverse of `ENDGAME_CLASS_TO_SLUG`). Sharable URLs work end-to-end."
    - "Legacy components deleted: `frontend/src/components/charts/EndgameWDLChart.tsx` (352 LOC) and `frontend/src/components/charts/EndgameConvRecovChart.tsx` (135 LOC) per SEC3-06 / SEC3-07"
    - "All imports of the two deleted files are removed from `Endgames.tsx` (lines 20 + 23). New import added: `import { EndgameTypeBreakdownSection } from '@/components/charts/EndgameTypeBreakdownSection';`"
    - "Vitest at `__tests__/EndgameTypeBreakdownSection.test.tsx` covers: 5 cards rendered when all 6 classes present (pawnless filtered); empty-categories list renders an empty grid (or single empty-state placeholder, planner's call); grid container has the locked Tailwind class string; section container testid present; cards render in the order delivered by the backend (which is total-desc-sorted per `_aggregate_endgame_stats` line 429)"
    - "All seven gates green: `uv run pytest`, `uv run ty check app/ tests/`, `uv run ruff check .`, `uv run ruff format --check .`, `cd frontend && npm test -- --run`, `npm run lint`, `npm run knip`, `npm run build`"
    - "HUMAN-UAT file `87-HUMAN-UAT.md` exists with the mobile real-device density check (375px) as the critical test. If the HUMAN-UAT triggers a flip to `SHOW_WDL_BAR_IN_TYPE_CARDS = false`, the flag is flipped in `lib/endgameMetrics.ts`, the page-level h2 InfoPopover copy is updated to reflect the WDL-bar drop, and frontend tests are re-run"
    - "knip clean: no references to `EndgameWDLChart` or `EndgameConvRecovChart` remain; `ENDGAME_TYPE_DESCRIPTIONS` + `SHOW_WDL_BAR_IN_TYPE_CARDS` + `ENDGAME_CLASS_TO_SLUG` are consumed by `EndgameTypeCard` and / or `EndgameTypeBreakdownSection`; the legacy Phase 84 `opponent_conversion_pct` / `opponent_recovery_pct` 0-100-scale fields remain unused on the frontend (acceptable — they stay on the wire for backward compat per Plan 01 docstring)"
  artifacts:
    - path: "frontend/src/components/charts/EndgameTypeBreakdownSection.tsx"
      provides: "Orchestrator for the 5-card Endgame Type Breakdown section"
      min_lines: 50
      contains: "endgame-type-breakdown-section"
    - path: "frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx"
      provides: "Integration test for the orchestrator's grid + filter + per-card mount"
      contains: "EndgameTypeBreakdownSection"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Mount swap + new h2 InfoPopover + ?type= URL hydration"
      contains: "EndgameTypeBreakdownSection"
    - path: ".planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md"
      provides: "Manual UAT checklist with 375px mobile density check"
      contains: "375px"
  key_links:
    - from: "frontend/src/components/charts/EndgameTypeBreakdownSection.tsx"
      to: "frontend/src/components/charts/EndgameTypeCard.tsx"
      via: "imports and renders one card per surviving class"
      pattern: "<EndgameTypeCard"
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgameTypeBreakdownSection"
      via: "swapped mount at lines 506-518"
      pattern: "<EndgameTypeBreakdownSection"
    - from: "frontend/src/pages/Endgames.tsx h2 'Endgame Type Breakdown'"
      to: "<InfoPopover>"
      via: "new popover next to the h2 carrying taxonomy + metric definitions"
      pattern: "Endgame Type Breakdown"
---

<objective>
Ship the 5-card Endgame Type Breakdown section end-to-end. Build the `EndgameTypeBreakdownSection` orchestrator, swap the mount in `Endgames.tsx`, add the page-level h2 `InfoPopover`, wire `?type=...` URL hydration, delete both legacy components, and run the full gate sweep including the HUMAN-UAT mobile density check that decides the final value of `SHOW_WDL_BAR_IN_TYPE_CARDS`.

Phase 87 is functionally complete at the end of this plan: the 6 phase requirements (SEC3-01, SEC3-02, SEC3-04, SEC3-05, SEC3-06, SEC3-07) are visible on screen, both legacy components are gone, knip is clean, all CI gates pass.

This plan has one human-verify checkpoint (the HUMAN-UAT density check) that may flip `SHOW_WDL_BAR_IN_TYPE_CARDS` before merge.

Output: 1 new orchestrator (~60 LOC) + 1 new test file (~100 LOC); modifications to `Endgames.tsx` (imports + mount swap + h2 InfoPopover + URL hydration effect); 2 deleted legacy files; 1 new HUMAN-UAT file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md
@.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-01-PLAN.md
@.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-02-PLAN.md
@frontend/src/components/charts/EndgameTypeCard.tsx
@frontend/src/components/charts/EndgameMetricsSection.tsx
@frontend/src/components/charts/EndgameWDLChart.tsx
@frontend/src/components/charts/EndgameConvRecovChart.tsx
@frontend/src/pages/Endgames.tsx
@frontend/src/components/ui/info-popover.tsx
@frontend/src/lib/endgameMetrics.ts
@frontend/src/types/endgames.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EndgameTypeBreakdownSection orchestrator</name>
  <files>frontend/src/components/charts/EndgameTypeBreakdownSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsSection.tsx (Phase 86 orchestrator — direct template for the section wrapper, sub-question paragraph pattern, and grid+card composition; entire file is small ~70 LOC)
    - frontend/src/components/charts/EndgameTypeCard.tsx (Plan 02 output — the per-class card; for prop shape)
    - frontend/src/components/charts/EndgameConvRecovChart.tsx lines 65-67 (legacy grid class `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` — locked at D-06)
    - frontend/src/lib/endgameMetrics.ts (the lifted `ENDGAME_CLASS_TO_SLUG` from Plan 02)
    - frontend/src/types/endgames.ts (`EndgameCategoryStats` + `EndgameClass`)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §D-03 + §D-06 + §D-12 (orchestrator scope, grid layout, sub-question copy)
    - frontend/src/pages/Endgames.tsx lines 53 (`HIDDEN_ENDGAME_CLASSES`; the orchestrator imports this constant directly from the consumer page, OR reads a copy lifted into a shared module — planner's call. Recommend importing from `Endgames.tsx` only if Endgames.tsx exports it; otherwise inline a local copy with a comment noting the duplication and a TODO to lift on the next refactor)
  </read_first>
  <behavior>
    - Component signature: `export function EndgameTypeBreakdownSection({ categories, totalGames, onCategorySelect }: EndgameTypeBreakdownSectionProps)`.
    - Props interface:
      ```
      {
        categories: EndgameCategoryStats[];
        totalGames: number;          // ALL filtered games (used for sharePct denominator)
        onCategorySelect: (cls: EndgameClass) => void;
      }
      ```
    - **Hidden-classes filter:** import `HIDDEN_ENDGAME_CLASSES` directly. If `Endgames.tsx` already exports it (likely not, since it's defined at module scope at line 53), lift the constant into a small shared module — recommend `frontend/src/lib/endgameMetrics.ts` since that's where Phase 87 already adds class-related constants, with the new export `HIDDEN_ENDGAME_CLASSES: ReadonlySet<EndgameClass> = new Set(['pawnless'])` and a corresponding import in `Endgames.tsx` that replaces the local definition. Single-source-of-truth pattern. If that lift is judged scope creep, inline a `const HIDDEN_ENDGAME_CLASSES: ReadonlySet<EndgameClass> = new Set(['pawnless']);` at the top of the orchestrator with a `// TODO: lift to lib/endgameMetrics.ts once both call sites stabilize` comment.
    - **Local derivations:**
      - `visibleCategories = categories.filter(c => !HIDDEN_ENDGAME_CLASSES.has(c.endgame_class))` — 5 entries after filter when all 6 classes present
      - Pass `totalGames` straight through as the `sharePct` denominator: `sharePct = totalGames > 0 ? (cat.total / totalGames) * 100 : 0`. The denominator is the total filtered games, NOT the sum of per-type totals (a single game can count in multiple types, so per-type totals over-count when summed; `totalGames` from the parent reflects the actual filtered-games population).
    - **Render structure** (mirror `EndgameMetricsSection.tsx` shape):
      ```jsx
      <section data-testid="endgame-type-breakdown-section">
        <p className="text-sm text-muted-foreground">
          Which Endgame Types do you convert best and defend best, and how does each compare to your opponents?
        </p>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-2">
          {visibleCategories.map(cat => {
            const slug = ENDGAME_CLASS_TO_SLUG[cat.endgame_class];
            const sharePct = totalGames > 0 ? (cat.total / totalGames) * 100 : 0;
            return (
              <EndgameTypeCard
                key={cat.endgame_class}
                category={cat}
                sharePct={sharePct}
                onCategorySelect={onCategorySelect}
                tileTestId={`type-card-${slug}`}
              />
            );
          })}
        </div>
      </section>
      ```
    - **Sub-question copy:** the en-dash in CONTEXT.md's sub-question (`...defend best — and how does each compare...`) is replaced with a comma per CLAUDE.md em-dash rule: "Which Endgame Types do you convert best and defend best, and how does each compare to your opponents?"
    - **Empty `categories` array** (defensive — should never happen in production since Phase 87 still ships 5 cards even with 0-game classes per D-13): when `visibleCategories.length === 0`, render the section wrapper + sub-question + an empty `<div>` (no grid). Single test fixture exercises this.
    - **Section-level h3 / InfoPopover:** NONE. The section h2 is at the page level in `Endgames.tsx:507` (Task 3 attaches the InfoPopover there). Per CONTEXT.md §D-12 the per-card title carries the type-description popover; the section-level explainer (taxonomy + metric defs) lives at h2 level on the page.
    - Named export only.
    - Header comment block (lines 1-12) citing "Phase 87 — Orchestrator for the 5-card Endgame Type Breakdown section. Replaces both legacy EndgameWDLChart (table) and EndgameConvRecovChart (gauge strip) per SEC3-06 / SEC3-07. Layout: 3-col grid on lg+, 2-col on sm, single column on mobile. Pawnless is filtered via HIDDEN_ENDGAME_CLASSES. v1.17 single-bullet doctrine."
  </behavior>
  <action>
    Create `frontend/src/components/charts/EndgameTypeBreakdownSection.tsx`.

    Header comment block mirroring `EndgameMetricsSection.tsx` style (10-12 lines).

    Imports:
    - `import { EndgameTypeCard } from './EndgameTypeCard';`
    - `import { ENDGAME_CLASS_TO_SLUG } from '@/lib/endgameMetrics';`
    - `import type { EndgameCategoryStats, EndgameClass } from '@/types/endgames';`

    For `HIDDEN_ENDGAME_CLASSES`: lift it into `lib/endgameMetrics.ts` as a NEW additional export (extending Plan 02's three new exports). Add it inline in Plan 03 Task 1, alongside the orchestrator creation, so this task touches both files atomically. The lift addition to `lib/endgameMetrics.ts` is:

    ```typescript
    // Phase 87: Pawnless endgames are rare (~0.5% of positions in prod) and
    // sample sizes are usually too small to be meaningful. Hidden in
    // Endgames.tsx UI and in EndgameTypeBreakdownSection. Classification stays
    // in the DB so it can be re-enabled without a reimport.
    export const HIDDEN_ENDGAME_CLASSES: ReadonlySet<EndgameClass> = new Set(['pawnless']);
    ```

    Then update `frontend/src/pages/Endgames.tsx:53` to import from `@/lib/endgameMetrics` instead of defining locally (the `VISIBLE_ENDGAME_CLASS_ENTRIES` derivation on line 55-57 continues to use the imported constant). This update is in scope for Task 1 since both call sites belong to Phase 87.

    Define `EndgameTypeBreakdownSectionProps` interface and `EndgameTypeBreakdownSection` named function per the behavior block.

    Use the locked Tailwind grid class string from CONTEXT.md D-06 verbatim: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`. Add `mt-2` for spacing under the sub-question paragraph (matches Phase 86 `EndgameMetricsSection`).

    Pass the four required `EndgameTypeCard` props per the behavior block. Derive `tileTestId` via `ENDGAME_CLASS_TO_SLUG[cat.endgame_class]` to match D-16.

    Per CLAUDE.md em-dash rule: the sub-question copy uses a comma in place of CONTEXT.md's en-dash. Make this explicit in the source.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` exits 0
    - `grep -c "EndgameTypeBreakdownSection\|EndgameTypeBreakdownSectionProps" frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` returns at least 3
    - `grep -c "endgame-type-breakdown-section" frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` returns at least 1 (testid)
    - `grep -c "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4" frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` returns 1
    - `grep -c "<EndgameTypeCard" frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` returns 1
    - `grep -c "HIDDEN_ENDGAME_CLASSES" frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` returns at least 1
    - `grep -c "^export const HIDDEN_ENDGAME_CLASSES" frontend/src/lib/endgameMetrics.ts` returns 1 (lifted constant)
    - `grep -c "HIDDEN_ENDGAME_CLASSES" frontend/src/pages/Endgames.tsx` returns at least 1 (imported, not defined locally)
    - `grep -c "^const HIDDEN_ENDGAME_CLASSES" frontend/src/pages/Endgames.tsx` returns 0 (local definition removed; only import remains)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Orchestrator exists with the locked grid + sub-question + 5-card render path; `HIDDEN_ENDGAME_CLASSES` lifted to `lib/endgameMetrics.ts` and consumed by both the new section and `Endgames.tsx`; tsc + lint green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest coverage for EndgameTypeBreakdownSection</name>
  <files>frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/EndgameMetricsSection.test.tsx (Phase 86 orchestrator test pattern — direct template for fixture builders + render assertions)
    - frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx (Plan 02 — for `buildCategory` fixture-builder reuse if exported)
    - frontend/src/components/charts/EndgameTypeBreakdownSection.tsx (the just-built orchestrator)
  </read_first>
  <behavior>
    - 4-5 test cases:

      1. **`renders 5 cards when all 6 classes present (pawnless filtered)`** — Build a `categories: EndgameCategoryStats[]` array with one entry per `EndgameClass` (6 entries: rook / minor_piece / pawn / queen / mixed / pawnless), all with healthy game counts. Render the section. Assert: `screen.getAllByTestId(/^type-card-/)` returns exactly 5 entries (pawnless filtered); the 5 testids are `type-card-rook`, `type-card-minor-piece`, `type-card-pawn`, `type-card-queen`, `type-card-mixed`; `screen.queryByTestId('type-card-pawnless')` returns null.

      2. **`renders the locked sub-question copy`** — Assert `screen.getByText(/Which Endgame Types do you convert best and defend best/i)` is present.

      3. **`renders the locked grid container with all Tailwind breakpoints`** — Assert the grid container `<div>` has `className` including all of `grid-cols-1`, `sm:grid-cols-2`, `lg:grid-cols-3`, `gap-4`. Use `container.querySelector('.grid')` or test the `data-testid="endgame-type-breakdown-section"` and walk its child.

      4. **`cards appear in the backend-sorted order (total desc)`** — Build categories with descending totals in this order: mixed (200) → rook (100) → minor_piece (50) → pawn (20) → queen (10) → pawnless (5). Render. Assert `screen.getAllByTestId(/^type-card-/)` returns testids in the order: `type-card-mixed`, `type-card-rook`, `type-card-minor-piece`, `type-card-pawn`, `type-card-queen` (pawnless filtered). Backend sorts by total desc per `_aggregate_endgame_stats:429`; the orchestrator preserves backend order.

      5. **`renders empty grid when categories is empty`** — Render with `categories = []`. Assert section container present (`data-testid="endgame-type-breakdown-section"`); no `type-card-*` testids present.

    - Use `MemoryRouter` wrapper for the cards' `<Link>` children to render correctly.
    - Do NOT mock `EndgameTypeCard` — let it render real DOM so the testid composition is exercised end-to-end.
  </behavior>
  <action>
    Create `frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx`.

    Imports:
    - `import { describe, it, expect, vi } from 'vitest';`
    - `import { render, screen } from '@testing-library/react';`
    - `import { MemoryRouter } from 'react-router-dom';`
    - `import { EndgameTypeBreakdownSection } from '../EndgameTypeBreakdownSection';`
    - `import type { EndgameCategoryStats, EndgameClass } from '@/types/endgames';`

    Build a `buildCategory(endgameClass: EndgameClass, overrides?: Partial<EndgameCategoryStats>): EndgameCategoryStats` factory that defaults to a healthy fixture for the given class. Reuse logic from Plan 02's `EndgameTypeCard.test.tsx` `buildCategory` if extracted, or duplicate (small enough to be fine).

    Build a `buildAllSixCategories(totalsByClass?: Partial<Record<EndgameClass, number>>): EndgameCategoryStats[]` helper.

    Render helper wrapping in `<MemoryRouter>` + the orchestrator with `onCategorySelect={vi.fn()}` and a reasonable `totalGames`.

    Write the 5 test methods per the behavior block. Use descriptive `describe` grouping (`describe('EndgameTypeBreakdownSection', () => { describe('Layout', ...); describe('Filtering', ...); describe('Ordering', ...); })`).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run EndgameTypeBreakdownSection</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx` exits 0
    - `cd frontend && npm test -- --run EndgameTypeBreakdownSection` exits 0 and reports at least 4 tests passed
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Orchestrator tests cover 5-card filter / sub-question / grid class / backend-sorted order / empty-list defensive branch. All green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap mount in Endgames.tsx + add page-level h2 InfoPopover + verify URL hydration</name>
  <files>frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx lines 20-30 (existing imports — `EndgameWDLChart` at line 20, `EndgameConvRecovChart` at line 23 both removed; new `EndgameTypeBreakdownSection` import added)
    - frontend/src/pages/Endgames.tsx lines 506-519 (both legacy mounts — collapse into a single new mount, keep the existing `charcoal-texture rounded-md p-4` wrapper)
    - frontend/src/pages/Endgames.tsx lines 53 (`HIDDEN_ENDGAME_CLASSES` definition — already lifted to lib/endgameMetrics.ts in Task 1; verify the import is in place)
    - frontend/src/pages/Endgames.tsx — search for `handleCategorySelect` definition (likely around line 511 per CONTEXT, or wherever the existing callback is declared) to determine the existing filter-sync behavior
    - frontend/src/pages/Endgames.tsx — search for `useLocation` / `useSearchParams` / `?type=` to see if a URL-param reader already exists for the games sub-route
    - frontend/src/components/charts/EndgameWDLChart.tsx lines 275-292 (legacy section-level h3 InfoPopover content — lift the taxonomy paragraph + per-type description ul into the new h2 popover)
    - frontend/src/components/charts/EndgameConvRecovChart.tsx lines 34-49 (legacy section-level h3 InfoPopover content — lift the Conv + Recov metric definitions + gauge-zone explainer)
    - frontend/src/components/ui/info-popover.tsx (existing `<InfoPopover>` API; mirror the trigger pattern used at `Endgames.tsx:671` and elsewhere on the page)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §D-08 + §D-12 (URL hydration + h2 popover content scope)
  </read_first>
  <behavior>
    - **Step A — Import updates** (lines 20-30):
      - Remove: `import { EndgameWDLChart } from '@/components/charts/EndgameWDLChart';` (line 20)
      - Remove: `import { EndgameConvRecovChart } from '@/components/charts/EndgameConvRecovChart';` (line 23)
      - Add: `import { EndgameTypeBreakdownSection } from '@/components/charts/EndgameTypeBreakdownSection';`
      - Keep all other imports unchanged.
    - **Step B — Mount swap** (lines 506-518): replace the existing block:
      ```jsx
      {/* ── Endgame Type Breakdown ── */}
      <h2 className="text-lg font-semibold text-foreground mt-2">Endgame Type Breakdown</h2>
      <div className="charcoal-texture rounded-md p-4">
        <EndgameWDLChart categories={statsData.categories} onCategorySelect={handleCategorySelect} />
      </div>
      {statsData.categories.length > 0 && (
        <div className="charcoal-texture rounded-md p-4">
          <EndgameConvRecovChart categories={statsData.categories} />
        </div>
      )}
      <SectionInsightSlot sectionId="type_breakdown" data={sectionBySection.type_breakdown} />
      ```
      With:
      ```jsx
      {/* ── Endgame Type Breakdown ── */}
      <h2 className="text-lg font-semibold text-foreground mt-2">
        <span className="inline-flex items-center gap-1">
          Endgame Type Breakdown
          <InfoPopover
            ariaLabel="Endgame Type Breakdown info"
            testId="endgame-type-breakdown-info"
            side="top"
          >
            <div className="space-y-2">
              {/* Lifted from EndgameWDLChart h3 InfoPopover + EndgameConvRecovChart h3 InfoPopover per D-12 */}
              ... (content per Step C below) ...
            </div>
          </InfoPopover>
        </span>
      </h2>
      <div className="charcoal-texture rounded-md p-4">
        <EndgameTypeBreakdownSection
          categories={statsData.categories}
          totalGames={statsData.total_games}
          onCategorySelect={handleCategorySelect}
        />
      </div>
      <SectionInsightSlot sectionId="type_breakdown" data={sectionBySection.type_breakdown} />
      ```
      Note: collapse the two separate `<div className="charcoal-texture rounded-md p-4">` wrappers into ONE (the new orchestrator handles both former concerns). The `{statsData.categories.length > 0 && ...}` guard is dropped because the new section renders a stable 5-card grid even with empty per-class data (per D-13 sparse handling). If `statsData.categories.length === 0` the orchestrator's defensive empty branch (Plan 03 Task 1 Test 5) renders an empty grid with just the sub-question — acceptable UX. If preferred, retain a top-level `{statsData.categories.length > 0 && ...}` guard around the entire new section.
    - **Step C — h2 InfoPopover content** (lifted + adapted from both legacy intros per D-12):
      ```jsx
      <div className="space-y-2">
        {/* Taxonomy block: lifted from EndgameWDLChart.tsx:277-291 */}
        <p>
          Shows your win, draw, and loss percentages for each Endgame Type, along
          with your Conversion (win rate when entering with a clear advantage) and
          Recovery (save rate when entering with a clear disadvantage) rates per
          type. A single game can count toward multiple Endgame Types.
        </p>
        {/* Conv + Recov definitions: lifted from EndgameConvRecovChart.tsx:38-49 */}
        <p>
          <strong>Conversion</strong>: your win rate in this Endgame Type when you
          entered with a Stockfish evaluation of +1.0 or better. <strong>Recovery</strong>:
          your save rate (wins + draws) when you entered with a Stockfish
          evaluation of −1.0 or worse.
        </p>
        {/* Gauge bands explainer: lifted from EndgameConvRecovChart.tsx:46-49 */}
        <p>
          Gauge zones are per-type typical bands sourced from pooled FlawChess
          benchmark data. Blue = typical for that type; red = below; green =
          above. Zones differ by type because each Endgame Type has its own
          natural distribution.
        </p>
        {/* Peer-bullet explainer: NEW per CONTEXT D-12 */}
        <p>
          The Conversion and Recovery peer bullets compare your rate to your
          opponents' rate in the same Endgame Type (via mirror-metric symmetry:
          opponents' Conversion is computed from your Recovery games for that
          type, flipped). The baseline shifts with rating × time control × color
          × opponent-type filters. Hidden when the opponent sample is smaller
          than 10 games.
        </p>
        {/* Per-type entries: lifted from EndgameWDLChart.tsx:287-291 */}
        <p><strong>Rook:</strong> {ENDGAME_TYPE_DESCRIPTIONS.rook}</p>
        <p><strong>Minor Piece:</strong> {ENDGAME_TYPE_DESCRIPTIONS.minor_piece}</p>
        <p><strong>Pawn:</strong> {ENDGAME_TYPE_DESCRIPTIONS.pawn}</p>
        <p><strong>Queen:</strong> {ENDGAME_TYPE_DESCRIPTIONS.queen}</p>
        <p><strong>Mixed:</strong> {ENDGAME_TYPE_DESCRIPTIONS.mixed}</p>
      </div>
      ```
      Import `ENDGAME_TYPE_DESCRIPTIONS` from `@/lib/endgameMetrics` (already exists per Plan 02).

      Per CLAUDE.md em-dash rule: the paragraph copy above uses commas / parentheses / periods. No em-dashes.

    - **Step D — URL hydration verification + addition.** Per CONTEXT.md §D-08 "Claude's Discretion": verify whether `handleCategorySelect` already syncs to URL, OR whether the `/endgames/games` route reads `?type=...`. Concretely:
      1. Locate `handleCategorySelect` in `Endgames.tsx`. Read its body. If it already calls `navigate('/endgames/games?type=...')` or syncs to `searchParams`, no action needed in this task.
      2. If `handleCategorySelect` only sets local React state (likely), the `<Link to="/endgames/games?type=${slug}">` from `EndgameTypeCard` carries the query string in the URL but the games-tab page won't pre-seed the filter from `?type=`. Add a `useEffect` near the top of `Endgames.tsx` (after the existing hook block, before render) that on mount reads `searchParams.get('type')` (use `useSearchParams` from `react-router-dom`), maps slug → `EndgameClass` via the inverse of `ENDGAME_CLASS_TO_SLUG` (e.g. `const SLUG_TO_CLASS = Object.fromEntries(Object.entries(ENDGAME_CLASS_TO_SLUG).map(([k, v]) => [v, k as EndgameClass]))`), and if a valid mapping exists calls `handleCategorySelect(parsedClass)`. Run the effect with dep `[searchParams]` so navigations within the SPA also pick up changes.
      3. If `Endgames.tsx` doesn't yet use `useSearchParams`, add the import and instance.
      4. If `handleCategorySelect` already navigates AND already pre-seeds from URL, document the verification in the SUMMARY and skip the new effect.

      The exact final shape is the planner's call based on what `handleCategorySelect` already does; the must-have is that visiting `/endgames/games?type=rook` directly (e.g. a shared link) sets the type filter to Rook on page load.
  </behavior>
  <action>
    Edit `frontend/src/pages/Endgames.tsx`.

    **Step 1** — Update imports per Step A.

    **Step 2** — Update the section block per Step B. Carry the page-level h2 InfoPopover per Step C.

    **Step 3** — Inspect `handleCategorySelect` (search the file for its definition). Decide URL-hydration handling per Step D. If a new effect is needed, place it near the existing effects (look for `useEffect` hooks) and include a comment block citing "Phase 87 (D-08, SEC3-02): `?type=...` URL hydration so /endgames/games?type=rook deep-links pre-seed the type filter."

    **Step 4** — Verify the local `HIDDEN_ENDGAME_CLASSES` definition at line 53 is removed (Plan 03 Task 1 already lifted it to `lib/endgameMetrics.ts`); the file imports it now.

    Do NOT change any other section mounts (Section 1, Section 2 Phase 86 `EndgameMetricsSection`, EloTimeline, ClockPressure, TimePressure, etc.). Do NOT modify `handleCategorySelect`'s existing body; only ADD URL hydration if missing.

    Do NOT remove `SectionInsightSlot sectionId="type_breakdown"` — it stays in place.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EndgameTypeBreakdownSection" frontend/src/pages/Endgames.tsx` returns at least 2 (import + mount)
    - `grep -c "EndgameWDLChart\|EndgameConvRecovChart" frontend/src/pages/Endgames.tsx` returns 0 (legacy imports + mounts removed)
    - `grep -c "endgame-type-breakdown-info" frontend/src/pages/Endgames.tsx` returns 1 (new h2 InfoPopover testid)
    - `grep -c "ENDGAME_TYPE_DESCRIPTIONS" frontend/src/pages/Endgames.tsx` returns at least 1 (used inside the h2 popover body)
    - `grep -c "type=" frontend/src/pages/Endgames.tsx` returns at least 1 (URL hydration logic or pre-existing reference; if hydration was added, the effect references `searchParams.get('type')` or equivalent)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Mount swapped to the new orchestrator; legacy imports removed; page-level h2 InfoPopover added with content lifted + adapted from both legacy intros; URL hydration confirmed (existing or newly added); tsc + lint green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete legacy EndgameWDLChart.tsx + EndgameConvRecovChart.tsx</name>
  <files>frontend/src/components/charts/EndgameWDLChart.tsx, frontend/src/components/charts/EndgameConvRecovChart.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameWDLChart.tsx (entire file — confirm `ENDGAME_TYPE_DESCRIPTIONS` and `CLASS_TO_SLUG` were lifted in Plan 02; confirm no other external consumers remain by grep)
    - frontend/src/components/charts/EndgameConvRecovChart.tsx (entire file — confirm no logic remaining that isn't already absorbed into `EndgameTypeCard`)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §SEC3-06 / §SEC3-07 (deletion mandate)
  </read_first>
  <behavior>
    - Delete both files entirely via `rm`.
    - Run `grep -rn "EndgameWDLChart\|EndgameConvRecovChart\|endgame-wdl-chart\|conv-recov-chart\|conv-recov-grid\|endgame-chart-info\|conv-recov-chart-info\|endgame-type-table\|endgame-games-link-\|endgame-category-card-\|endgame-category-.*-row\|class-gauge-" frontend/src/` to confirm no stale references remain anywhere in the frontend codebase. Each match must be either:
      1. A legitimate active reference that was missed in Task 3 (fix it).
      2. A stale test fixture / snapshot (delete or update).
      3. A comment referencing the deletion historically (acceptable).
    - Run `grep -rn "EndgameWDLChart\|EndgameConvRecovChart" frontend/src/` again after fixing all 1+2 matches — final count should be 0 or only acceptable historical comments.
    - Confirm `knip` reports no orphaned exports in `lib/endgameMetrics.ts` (the three new Phase 87 exports + `HIDDEN_ENDGAME_CLASSES` must all be consumed by the new card + section + `Endgames.tsx`).
    - The legacy `_aggregate_endgame_stats` Phase 84 fields (`opponent_conversion_pct` / `opponent_recovery_pct` on 0-100 scale) are NOT consumed by any frontend code after this deletion. They remain on the wire for backward compat per Plan 01 docstring. `knip` may not warn about Pydantic-only fields; if any TS code references them, update or remove that reference.
  </behavior>
  <action>
    1. `rm frontend/src/components/charts/EndgameWDLChart.tsx`
    2. `rm frontend/src/components/charts/EndgameConvRecovChart.tsx`
    3. Workspace grep + fix: `grep -rn "EndgameWDLChart\|EndgameConvRecovChart" frontend/src/`. If any matches show up (e.g. stale test snapshots, test mocks like the Phase 86 pattern in `Endgames.overallPerformance.test.tsx`), update them to reference `EndgameTypeBreakdownSection` instead OR remove if no longer needed.
    4. Workspace grep for stale legacy testids: `grep -rn "endgame-wdl-chart\|conv-recov-chart\|conv-recov-grid\|class-gauge-\|endgame-games-link-\|endgame-category-\|endgame-type-table" frontend/src/`. Fix or remove any matches.
    5. Run `cd frontend && npm run knip` and resolve any new orphan warnings.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; test ! -f src/components/charts/EndgameWDLChart.tsx &amp;&amp; test ! -f src/components/charts/EndgameConvRecovChart.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/charts/EndgameWDLChart.tsx` exits non-zero (deleted)
    - `test -f frontend/src/components/charts/EndgameConvRecovChart.tsx` exits non-zero (deleted)
    - `grep -rn "EndgameWDLChart\|EndgameConvRecovChart" frontend/src/` returns no live matches (test fixtures + snapshots cleaned; commented historical references acceptable but should be minimal)
    - `grep -rn "endgame-wdl-chart\|conv-recov-chart\|conv-recov-grid\b\|class-gauge-" frontend/src/` returns no matches
    - `cd frontend && npm run knip` exits 0
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Both legacy files deleted; no stale references; knip clean; tsc + lint green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create 87-HUMAN-UAT.md scaffold</name>
  <files>.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-HUMAN-UAT.md (Phase 86 format — direct template; same frontmatter schema, same "## Tests" + "## Summary" + "## Gaps" structure)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §D-04 + §D-05 + §SEC3-05 (mobile density check is the critical HUMAN-UAT decision; the WDL bar flip is the only fallback in Phase 87)
  </read_first>
  <behavior>
    - File starts with the same `---` frontmatter as Phase 86 HUMAN-UAT but with `phase: 87-section-3-per-type-endgame-type-breakdown-cards`, `source: [87-VERIFICATION.md]`, current ISO timestamp for `started:` and `updated:` (use a `date -Iseconds` substitution at execute time; if the executor can't run `date`, hard-code today's date).
    - 6-7 test entries covering:
      1. **Desktop 3-col grid layout** — at ≥1024px, 5 cards arrange as 3 cards on row 1 (rook | minor_piece | pawn or whatever the backend-sorted order produces), 2 cards on row 2 (left-aligned, last column empty). All 5 cards visible with no horizontal overflow.
      2. **Tablet 2-col grid** — at 768px-1023px width, 5 cards arrange as 2-2-1 (last card on row 3, single column).
      3. **Mobile single-column stack** — at <768px, 5 cards stack single-column in DOM order.
      4. **CRITICAL: Mobile real-device density check at 375px (per SEC3-05)** — On a real iOS or Android device at 375px viewport width (iPhone 12/13 mini / iPhone SE 3rd gen), scroll through the section. Each card carries: 2 gauges + WDL bar + Games deep-link + Conv peer bullet + Recov peer bullet. Decision criteria: if the card is so tall that vertical scrolling becomes uncomfortable (user has to scroll past 3+ screen heights to see all 5 cards), flip `SHOW_WDL_BAR_IN_TYPE_CARDS = false` in `lib/endgameMetrics.ts`. If the density is acceptable, keep `true`. Document the final decision + the device + viewport tested.
      5. **`?type=...` URL deep-link hydration** — Click the Games icon on the Rook card. Verify the URL changes to `/endgames/games?type=rook` and the games tab loads with the type filter pre-set to Rook. Refresh the page at `/endgames/games?type=rook` and verify the type filter is still pre-set on reload.
      6. **Per-card title InfoPopover** — Hover the HelpCircle next to each card's class label. Verify the one-sentence type description renders (matches `ENDGAME_TYPE_DESCRIPTIONS`).
      7. **Per-bullet MetricStatPopover** — Hover the HelpCircle inside each peer-bullet row (Conv + Recov on every card). Verify the explanation + methodology block renders per CONTEXT D-10.
      8. **Page-level h2 InfoPopover (D-12)** — Hover the HelpCircle next to the "Endgame Type Breakdown" h2. Verify the taxonomy + per-type descriptions + Conv/Recov definitions + peer-bullet explainer all render.
      9. **Empty / sparse class handling** — With a filter that produces a class with `total = 0` (or a sparse class with `total < 10`), verify: empty class shows the gauge row at `opacity-50` with "Not enough data yet" body; sparse class shows the `n=N` chip + reduced body opacity, with peer-bullet rows still rendering when opponent sample is large enough.
      10. **Filter responsiveness** — Apply a filter (e.g. Opponent Strength: Stronger). Verify the You / Opp / Diff values on each card update; gauges stay the same (gauge bands are fixed per `PER_CLASS_GAUGE_ZONES`).
      11. **Legacy removal visual confirmation** — Verify both the legacy per-type WDL table (with row-per-class) and the legacy gauge-only mini-card strip are gone; only the new 5-card grid is visible under the h2.
    - Each test entry follows the Phase 86 format:
      ```
      ### N. Test name
      expected: <one-paragraph statement of expected outcome>
      result: [pending]
      ```
    - Summary block at the bottom: `total: 11, passed: 0, issues: 0, pending: 11, skipped: 0, blocked: 0`.
    - Empty `## Gaps` section at the end.
    - The "## Current Test" section reads `[awaiting human testing]` initially.
  </behavior>
  <action>
    Create `.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md` mirroring the Phase 86 format. Adjust counts (`total: 11`).

    Replace `phase: 86-...` with `phase: 87-section-3-per-type-endgame-type-breakdown-cards`. Replace `source: [86-VERIFICATION.md]` with `source: [87-VERIFICATION.md]`. Set `started:` and `updated:` to the current ISO datetime.

    Use the 11 test entries from the behavior block above, written in the same shape: each as a `### N. <name>` heading, an `expected:` paragraph, and `result: [pending]`.

    Add a clear callout under Test 4 (the density check) noting it as the **critical** test for this phase: "If this test triggers `SHOW_WDL_BAR_IN_TYPE_CARDS = false`, the executor must (1) flip the flag in `frontend/src/lib/endgameMetrics.ts`, (2) update the page-level h2 InfoPopover copy to reflect the WDL-bar drop, (3) re-run `npm test -- --run` to ensure the WDL-flag-gating test (Plan 02 Task 3 Test 3) still passes, (4) commit the flip in a separate commit citing 'Phase 87 D-04: real-device density check fired fallback'."

    Per CLAUDE.md em-dash rule: avoid em-dashes in this file's body text (commas/parentheses/periods instead).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md &amp;&amp; grep -c "375px\|Mobile real-device density" .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md    <automated>test -f .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md &amp;&amp; grep -c "375px\|Mobile real-device density" .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md` exits 0
    - `grep -c "375px" .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md` returns at least 1
    - `grep -c "SHOW_WDL_BAR_IN_TYPE_CARDS" .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md` returns at least 1
    - `grep -c "^### " .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md` returns 11 (one heading per test)
    - `grep -c "^total: 11" .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-HUMAN-UAT.md` returns 1
  </acceptance_criteria>
  <done>
    HUMAN-UAT file exists with 11 test entries; the 375px mobile density check is clearly flagged as the critical test that decides `SHOW_WDL_BAR_IN_TYPE_CARDS`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human-verify the 5-card layout + execute 87-HUMAN-UAT real-device density check</name>
  <files>frontend/src/pages/Endgames.tsx (rendered in browser; flag flip applied to frontend/src/lib/endgameMetrics.ts if density check fails)</files>
  <action>Pause for human review. Walk through 87-HUMAN-UAT.md and record pass/fail per test. If Test 4 (375px density) triggers the fallback, flip `SHOW_WDL_BAR_IN_TYPE_CARDS` to `false` in `lib/endgameMetrics.ts`, update the page-level h2 InfoPopover copy in `Endgames.tsx` to mention "Each card shows Conv + Recov gauges and peer bullets only — the per-type WDL bar is omitted on mobile-density grounds; aggregate WDL is available via the Endgame Type filter on the Games tab", re-run `cd frontend && npm test -- --run` to confirm tests still pass, then return to this checkpoint with the flip applied. The executor performs no other file edits in this task; it only stages the dev environment (steps 1-2 below) if not already running.</action>
  <verify>
    <automated>echo "Checkpoint — human verification required; awaiting resume-signal"</automated>
  </verify>
  <done>User types "approved" after walking through 87-HUMAN-UAT.md. If the density test fired, the flag flip is committed to `lib/endgameMetrics.ts` before the user approves.</done>
  <what-built>
    - 5 per-class cards in a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` layout under the "Endgame Type Breakdown" h2.
    - Each card: 2 gauges (Conv + Recov) side-by-side, optional WDL bar (gated by `SHOW_WDL_BAR_IN_TYPE_CARDS`), Games deep-link to `/endgames/games?type={slug}`, Conv peer bullet, Recov peer bullet, per-card title InfoPopover.
    - Page-level h2 InfoPopover next to "Endgame Type Breakdown" carrying taxonomy + Conv/Recov definitions + per-type descriptions + peer-bullet explainer.
    - `?type=...` URL deep-link hydration (sharable URLs pre-seed the games-tab filter).
    - Legacy `EndgameWDLChart` + `EndgameConvRecovChart` fully removed.
  </what-built>
  <how-to-verify>
    1. Run `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` if dev DB isn't running.
    2. Run `cd frontend && npm run dev` in one terminal; `uv run uvicorn app.main:app --reload` in another.
    3. Open http://localhost:5173/endgames (sign in or use guest mode; import a few games if a fresh DB).
    4. Scroll to the "Endgame Type Breakdown" h2 and walk through each test in `87-HUMAN-UAT.md`.
    5. For Test 4, open the page on a real mobile device at 375px viewport width (or use Chrome DevTools device emulation as a proxy if no real device is available — but record this in the SUMMARY). Decide on `SHOW_WDL_BAR_IN_TYPE_CARDS` final value.
    6. Update the `result:` lines in `87-HUMAN-UAT.md` from `[pending]` to `[pass]` / `[fail: <reason>]` / `[skip: <reason>]` per test.
    7. If Test 4 fires the fallback, flip the flag + update copy + re-run tests per the Action section above.
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 tests pass (or pass + fallback applied). If issues, describe them and the executor will iterate.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Full gate sweep — pytest + ty + ruff + npm test + lint + knip + build</name>
  <files></files>
  <read_first>
    - CLAUDE.md (gate commands — `uv run ruff check .`, `uv run ty check app/ tests/`, `uv run pytest`, `npm test`, `npm run lint`, `npm run knip`, `npm run build`)
    - .planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-05-PLAN.md Task 6 (Phase 86 gate-sweep precedent)
  </read_first>
  <behavior>
    - All seven gates green:
      - `uv run ruff check .`
      - `uv run ruff format --check .`
      - `uv run ty check app/ tests/`
      - `uv run pytest`
      - `cd frontend && npm test -- --run`
      - `cd frontend && npm run lint`
      - `cd frontend && npm run knip`
      - `cd frontend && npm run build`
    - No new warnings introduced.
    - All 6 phase requirements satisfied (SEC3-01, SEC3-02, SEC3-04, SEC3-05, SEC3-06, SEC3-07).
  </behavior>
  <action>
    Run each gate command in sequence. For any failure, fix in place (do NOT defer to follow-up phases). Pay particular attention to:

    1. `npm run knip` — the legacy file deletions removed several testids (`endgame-wdl-chart`, `conv-recov-chart`, `class-gauge-*`, etc.) and the legacy `endgameSkill()`-style helpers from `EndgameWDLChart`'s `userScore`/`opponentScore` local helpers. knip may flag any unused-export it spots; resolve by removing dead exports OR by adding the missing consumer. The four new exports in `lib/endgameMetrics.ts` (`ENDGAME_TYPE_DESCRIPTIONS`, `SHOW_WDL_BAR_IN_TYPE_CARDS`, `ENDGAME_CLASS_TO_SLUG`, `HIDDEN_ENDGAME_CLASSES`) should all be consumed by `EndgameTypeCard` / `EndgameTypeBreakdownSection` / `Endgames.tsx`.

    2. `npm run build` — final production build catches anything tsc + lint missed.

    3. `uv run pytest` — backend changes (Plan 01) need to still pass on the full suite, not just the targeted test files.

    Run `cd frontend && npm run knip 2>&1 | head -30` first to see if there are pre-existing knip warnings unrelated to this phase. Only fix new ones.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest &amp;&amp; cd frontend &amp;&amp; npm test -- --run &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All eight gate commands exit 0 individually and as a compound command
    - `cd frontend && npm run knip` reports no unused exports / no unused files attributable to Phase 87
    - The deleted files do not appear in the build output (verify via `find frontend/dist -name "*.js" | xargs grep -l "EndgameWDLChart\|EndgameConvRecovChart" 2>/dev/null` returning empty)
    - `git status` shows the expected file changes: 3 new files (`EndgameTypeCard.tsx`, `EndgameTypeBreakdownSection.tsx`, + 2 new test files), modifications to `app/schemas/endgames.py`, `app/services/endgame_service.py`, `tests/test_endgame_service.py`, `frontend/src/types/endgames.ts`, `frontend/src/lib/endgameMetrics.ts`, `frontend/src/pages/Endgames.tsx`, 2 deleted files (`EndgameWDLChart.tsx`, `EndgameConvRecovChart.tsx`), 1 new HUMAN-UAT file
  </acceptance_criteria>
  <done>
    All gates green; phase functionally complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params | `?type=...` is parsed from `window.location.search` to pre-seed the games-tab filter. Untrusted input crosses the URL→filter-state boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-07 | Tampering / Injection | `?type=...` URL hydration effect | mitigate | Validate the parsed slug against the inverse `ENDGAME_CLASS_TO_SLUG` map; unknown / malformed slugs are ignored (no filter state change). Type is `EndgameClass` literal union, never used as raw string downstream. |
| T-87-08 | Information Disclosure | per-class peer bullets | accept | Same auth gate as `/api/endgames/overview`; per-class opp rates already exposed since Phase 84. No new disclosure surface. |
| T-87-09 | Denial-of-Service | empty / sparse render branches | accept | Defensive empty-class branch in orchestrator + card renders fixed-size empty placeholders; no unbounded work. |
</threat_model>

<verification>
- All eight gates exit 0: `uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest && cd frontend && npm test -- --run && npm run lint && npm run knip && npm run build`
- `grep -rn "EndgameWDLChart\|EndgameConvRecovChart" frontend/src/` returns no live matches
- The Endgames page renders the 5-card grid + h2 InfoPopover + Games deep-link + URL hydration per the human-verify checkpoint
- SEC3-01, SEC3-02, SEC3-04, SEC3-05, SEC3-06, SEC3-07 (6 phase requirements) all addressable on screen and via inspector
- HUMAN-UAT file documents the real-device density-check outcome and any `SHOW_WDL_BAR_IN_TYPE_CARDS` flip
</verification>

<success_criteria>
- 5-card Endgame Type Breakdown section ships at `Endgames.tsx:506-518` site; cards mounted with the locked `type-card-{slug}` testids per D-16
- Grid responsive at lg+ / sm / mobile per D-06
- Page-level h2 InfoPopover lifted + adapted from both legacy intros per D-12
- `?type=...` URL hydration works for shareable deep-links
- Legacy `EndgameWDLChart.tsx` + `EndgameConvRecovChart.tsx` deleted; knip clean
- All seven gates green
- HUMAN-UAT density check executed; `SHOW_WDL_BAR_IN_TYPE_CARDS` final value committed
- POLISH-01..POLISH-04 explicitly deferred to Phase 88 per CONTEXT (no scope creep)
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-03-SUMMARY.md` capturing:
- Phase 87 done; 6 of 6 SEC3 requirements complete (SEC3-01, SEC3-02, SEC3-04, SEC3-05, SEC3-06, SEC3-07).
- The final value of `SHOW_WDL_BAR_IN_TYPE_CARDS` (`true` or `false`) after the HUMAN-UAT density check, and the device + viewport tested.
- The URL hydration approach taken (existing logic verified vs new effect added) and any reverse-mapping helper introduced (e.g. `SLUG_TO_CLASS = Object.fromEntries(Object.entries(ENDGAME_CLASS_TO_SLUG)...)`).
- Whether `HIDDEN_ENDGAME_CLASSES` was lifted to `lib/endgameMetrics.ts` (recommended) or kept inline in the orchestrator with a TODO.
- v1.17 ROADMAP description "Frontend-only refactor" is now violated by Plan 01 (10 backend wire fields + service-level helper wiring). User-authorized at discuss-phase per CONTEXT.md §decisions / D-01 rationale; same caveat as Phase 86 / Phase 85.1. Recommend amending the milestone description at `/gsd-complete-milestone` time.
- POLISH-01..POLISH-04 deferred to Phase 88 per CONTEXT §deferred. Phase 88 will also do the cross-section 375px parity sweep.
- Phase status: ready for merge.
</output>
