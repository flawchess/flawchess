---
phase: 87
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/test_endgame_service.py
  - frontend/src/types/endgames.ts
autonomous: true
requirements:
  - SEC3-04
must_haves:
  truths:
    - "`ConversionRecoveryStats` exposes 10 new additive fields (`opp_conversion_pct`, `opp_recovery_pct`, `opp_conversion_games`, `opp_recovery_games`, `conv_diff_p_value`, `conv_diff_ci_low`, `conv_diff_ci_high`, `recov_diff_p_value`, `recov_diff_ci_low`, `recov_diff_ci_high`) per CONTEXT D-01"
    - "`opp_conversion_pct` and `opp_recovery_pct` semantics match the existing Phase 84 `opponent_conversion_pct` / `opponent_recovery_pct` mirror-identity (kept; new fields are the per-class peer-bullet sig wrap), both expressed on the 0-100 percent scale used by the existing fields"
    - "Per-class Conv peer diff: user side W/D/L = (conversion_wins, conversion_draws, conversion_losses, conversion_games); opp side W/D/L = (recovery_losses, recovery_draws, recovery_wins, recovery_games) — mirror-flipped W↔L on the SAME class's recovery bucket. Fed to existing `compute_score_difference_test` at `app/services/score_confidence.py:255`"
    - "Per-class Recov peer diff: user W/D/L = (recovery_wins + recovery_draws, 0, recovery_losses, recovery_games) — saves-as-W mapping; opp W/D/L = (conversion_losses + conversion_draws, 0, conversion_wins, conversion_games). Fed to existing `compute_score_difference_test`"
    - "Both peer-diff calls reuse `compute_score_difference_test` unchanged; NO new helper (per D-02). The helper's existing gates (`min(eg_n, ne_n) >= CONFIDENCE_MIN_N=10` for p, `min(...) >= 2` for CI) handle sparse-opponent gating per SEC3-04 with no extra guard at the call site"
    - "Wire site: the existing per-class category builder loop in `_aggregate_endgame_stats` at `app/services/endgame_service.py:336-431`. Two `compute_score_difference_test` calls inside the per-class for-loop, immediately before the existing `ConversionRecoveryStats(...)` constructor at line 392. Comment block citing Phase 87 + SEC3-04 + D-01"
    - "TS type `ConversionRecoveryStats` in `frontend/src/types/endgames.ts:9-20` mirrors the 10 new fields with matching `number | null` (sig fields) / `number` (counts) shape and one-line phase-citation comments"
    - "All new schema fields default to `None` / `0` so existing tests that construct `ConversionRecoveryStats` without these fields keep working (mirror the Phase 85.1 / Phase 86 additive-default pattern)"
    - "Pytest covers: 10-field-presence on a fully-populated fixture (all classes with healthy game counts both sides); mirror-flip correctness on Conv (user wins inflated → conv_diff > 0); sparse-opponent gating (recovery_games < 10 → conv_diff_p_value is None, conv_diff CI bounds None when < 2); 0-games safety (0 conversion games → recov_diff fields None); single-class isolation (asserting per-class fields populate independently per class)"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "10 new additive fields on `ConversionRecoveryStats` per Phase 87 D-01"
      contains: "conv_diff_p_value"
    - path: "app/services/endgame_service.py"
      provides: "`_aggregate_endgame_stats` wires two `compute_score_difference_test` calls per class inside the existing per-class builder loop"
      contains: "compute_score_difference_test"
    - path: "tests/test_endgame_service.py"
      provides: "Service-layer tests for the 10 new fields: presence, mirror-flip correctness, sparse-opponent gating, 0-games safety"
      contains: "conv_diff_p_value"
    - path: "frontend/src/types/endgames.ts"
      provides: "TS mirror of the 10 new fields on `ConversionRecoveryStats`"
      contains: "conv_diff_p_value"
  key_links:
    - from: "app/services/endgame_service.py::_aggregate_endgame_stats"
      to: "app/services/score_confidence.py::compute_score_difference_test"
      via: "two calls per class — Conv (user conv W/D/L vs opp = recov W/D/L with W↔L flipped) and Recov (user recov-as-saves W'/D'/L' vs opp = conv-as-saves W'/D'/L')"
      pattern: "compute_score_difference_test\\("
    - from: "frontend/src/types/endgames.ts::ConversionRecoveryStats"
      to: "ConversionRecoveryStats Pydantic schema"
      via: "Wire-format mirror — TS field names match Python field names character-for-character"
      pattern: "conv_diff_p_value"
---

<objective>
Wire backend per-class Conv + Recov peer-bullet sig tests for Phase 87's per-type cards. Add 10 additive fields to `ConversionRecoveryStats`, call the existing `compute_score_difference_test` helper twice per class inside the existing per-class category builder loop in `_aggregate_endgame_stats`, and mirror the 10 new fields into the TS type so the frontend cards (Plans 02 + 03) can consume them.

Per CONTEXT.md D-01..D-02 (LOCKED): reuse `compute_score_difference_test` as-is — no new helper, no new statistical primitive. The mirror-flip math is per CONTEXT.md "Math per class" (Conv = flip W↔L on the same class's recovery bucket; Recov = saves-as-W mapping on both sides). This is simpler than Phase 86's composite Skill helper because each per-class peer diff is a single per-side W/D/L computation, not a multi-bucket composite.

Purpose: single backend computation site for all 5 per-type cards' Conv + Recov peer-bullet sig tests. Frontend plans in Waves 2-3 only consume the wire — they never recompute the math.

Output: 10 new wire fields, ~50 LOC of service code (two helper invocations per class inside the existing loop), ~120 LOC of test code, TS type mirror.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md
@.planning/milestones/v1.17-phases/86-section-2-endgame-metrics-4-card-layout/86-02-PLAN.md
@.planning/notes/v1.17-single-bullet-doctrine.md
@app/schemas/endgames.py
@app/services/endgame_service.py
@app/services/score_confidence.py
@frontend/src/types/endgames.ts
@tests/test_endgame_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ConversionRecoveryStats schema with the 10 new fields</name>
  <files>app/schemas/endgames.py</files>
  <read_first>
    - app/schemas/endgames.py lines 19-59 (`ConversionRecoveryStats` — existing Phase 84 fields `opponent_conversion_pct` / `opponent_conversion_games` / `opponent_recovery_pct` / `opponent_recovery_games` are the immediate precedent for the additive `float | None` / `int` field pattern; field order + docstring style)
    - app/schemas/endgames.py lines 280-286 (`MaterialRow` Phase 86 `diff_p_value` / `diff_ci_low` / `diff_ci_high` triple — exact precedent for the additive `float | None = None` sig-field pattern this plan introduces per-class)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §D-01 (field names, types, gating semantics)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §specifics (full 10-field list)
  </read_first>
  <behavior>
    - Append 10 new optional fields to `ConversionRecoveryStats` after the existing `opponent_recovery_games: int` line (currently line 59). Field order matches CONTEXT D-01 + specifics block exactly:
      1. `opp_conversion_pct: float | None = None` — opponent's win-rate when opponent entered this class with eval advantage (semantic mirror of existing `opponent_conversion_pct`; NEW field name aligns with Phase 86 `opp_*` convention; both old + new names co-exist — old preserved for backward compat; on the 0-1 scale (NOT 0-100) to match Phase 86 wire convention)
      2. `opp_recovery_pct: float | None = None` — opponent's save-rate when opponent entered this class with eval deficit (0-1 scale)
      3. `opp_conversion_games: int = 0` — opp sample size for Conv peer (== recovery_games per mirror identity)
      4. `opp_recovery_games: int = 0` — opp sample size for Recov peer (== conversion_games per mirror identity)
      5. `conv_diff_p_value: float | None = None` — Wald-z p-value on per-class `userConv − oppConv`; None when `min(conversion_games, recovery_games) < 10` per the existing `compute_score_difference_test` gate
      6. `conv_diff_ci_low: float | None = None` — lower bound of 95% CI on the per-class Conv diff
      7. `conv_diff_ci_high: float | None = None` — upper bound
      8. `recov_diff_p_value: float | None = None` — Wald-z p-value on per-class `userRecov − oppRecov`; same min-n gate
      9. `recov_diff_ci_low: float | None = None`
      10. `recov_diff_ci_high: float | None = None`
    - Each new field carries a one-line docstring referencing "Phase 87 (SEC3-04 / D-01)" + the gate ("None when the relevant min-n is below `compute_score_difference_test`'s `CONFIDENCE_MIN_N` / `2` gates"). Mirror the docstring style and length of the Phase 84 `opponent_conversion_pct` / Phase 86 `diff_p_value` triplet exactly.
    - All new fields use defaults so existing test fixtures that construct `ConversionRecoveryStats` without these fields keep working (matches Phase 86 D-01 pattern).
    - Do NOT remove or alter the existing Phase 84 `opponent_conversion_pct` / `opponent_conversion_games` / `opponent_recovery_pct` / `opponent_recovery_games` fields. They remain in use by the existing legacy `EndgameConvRecovChart` until Plan 03 deletes that file. Frontend cards in Plan 02-03 consume the new `opp_*_pct` fields (Phase 86 convention, 0-1 scale).
  </behavior>
  <action>
    Edit `app/schemas/endgames.py`.

    In `ConversionRecoveryStats` (around line 19-59), append 10 new optional fields AFTER the existing `opponent_recovery_games: int` line (currently line 59). Use the exact field order and naming from the behavior block above.

    For each new field, add a one-line docstring (Pydantic field-docstring style — `"""..."""` immediately under the field declaration, mirroring the existing Phase 84 fields at lines 53-59) referencing "Phase 87 (SEC3-04 / D-01)" and the field's semantic + gating rule.

    Notes on scale convention: the existing Phase 84 `opponent_conversion_pct` is on the 0-100 scale (round to 1 decimal place, e.g. `52.3`). The NEW `opp_conversion_pct` / `opp_recovery_pct` fields use the 0-1 scale (e.g. `0.523`) to match Phase 86 convention (`opp_skill: float | None` is 0-1) and to align with how the frontend cards consume the value (passed directly to `MetricStatPopover` and `MiniBulletChart` which expect 0-1). Document the scale difference in each new field's docstring explicitly.

    Mirror the docstring style of the Phase 86 `diff_p_value` / `diff_ci_low` / `diff_ci_high` triple at lines 280-286 (concise inline `# Phase XX (...)` comment + multi-line `"""..."""` field docstring on the line after, with the same wording pattern). Choose either inline-comment-only OR field-docstring style consistently for the 10 new fields — match whichever convention is more prevalent in the existing `ConversionRecoveryStats` body (the Phase 84 fields use `"""..."""` field docstrings; follow that).

    Do NOT reorder existing fields, change defaults on existing fields, or modify any existing docstring on `ConversionRecoveryStats`.
  </action>
  <verify>
    <automated>uv run ruff check app/schemas/endgames.py &amp;&amp; uv run ruff format --check app/schemas/endgames.py &amp;&amp; uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^[[:space:]]*(opp_conversion_pct|opp_recovery_pct|opp_conversion_games|opp_recovery_games|conv_diff_p_value|conv_diff_ci_low|conv_diff_ci_high|recov_diff_p_value|recov_diff_ci_low|recov_diff_ci_high):" app/schemas/endgames.py` returns 10 (each new field on its own field-declaration line)
    - `grep -c "opponent_conversion_pct\|opponent_conversion_games\|opponent_recovery_pct\|opponent_recovery_games" app/schemas/endgames.py` is unchanged from baseline (Phase 84 fields preserved)
    - `uv run ty check app/ tests/` exits 0
    - `uv run ruff check app/schemas/endgames.py` exits 0
    - `uv run ruff format --check app/schemas/endgames.py` exits 0
  </acceptance_criteria>
  <done>
    10 new optional fields added to `ConversionRecoveryStats`, all with Phase 87 docstrings citing D-01, all defaulting to `None` / `0`, ty + ruff green, no existing field touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire two compute_score_difference_test calls per class in _aggregate_endgame_stats</name>
  <files>app/services/endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py lines 40-70 (imports — `compute_score_difference_test` is NOT yet imported here; need to add it from `app.services.score_confidence`)
    - app/services/endgame_service.py lines 336-431 (entire `_aggregate_endgame_stats` per-class category builder loop — the new helper calls land inside this loop, immediately before the existing `ConversionRecoveryStats(...)` constructor at line 392)
    - app/services/endgame_service.py lines 370-390 (existing Phase 84 mirror-identity block computing `opponent_conversion_pct` + `opponent_recovery_pct` per class — exact precedent for the per-class mirror-rate semantic + the `_MIN_OPPONENT_SAMPLE` gate)
    - app/services/score_confidence.py lines 255-335 (the `compute_score_difference_test` helper signature, gating, and returned tuple shape — reused verbatim)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §D-01 + §D-02 (mirror-flip math for both Conv and Recov, locked at planning)
  </read_first>
  <behavior>
    - **Insertion site (single, unambiguous):** both new helper invocations land inside the existing `for endgame_class in wdl:` loop in `_aggregate_endgame_stats` (line 337), immediately AFTER the existing Phase 84 `opponent_recovery_pct` / `opponent_recovery_games` block (after line 390) and BEFORE the `conversion_stats = ConversionRecoveryStats(...)` constructor at line 392. The existing Phase 84 mirror-rate fields stay untouched.
    - **Per-class Conv peer-diff call:** mirror-flip W↔L on the SAME class's recovery bucket per CONTEXT D-01.
      - User side: `(eg_w, eg_d, eg_l, eg_n) = (conversion_wins, conversion_draws, conversion_losses, conversion_games)`
      - Opp side: `(ne_w, ne_d, ne_l, ne_n) = (recovery_losses, recovery_draws, recovery_wins, recovery_games)` — note `recovery_losses` is the existing local variable computed at line 377 inside the Phase 84 block (need to ensure it's in scope; currently computed conditionally under `if recovery_games >= _MIN_OPPONENT_SAMPLE`). Lift the `recovery_losses = recovery_games - recovery_wins - recovery_draws` computation OUT of the conditional so it is unconditionally available before the helper call. The Phase 84 conditional was the original site of the variable; lifting it preserves the existing `opponent_conversion_pct` computation while making `recovery_losses` reusable below.
      - Call: `conv_p, conv_ci_low, conv_ci_high = compute_score_difference_test(conversion_wins, conversion_draws, conversion_losses, conversion_games, recovery_losses, recovery_draws, recovery_wins, recovery_games)`
      - The helper's built-in n-gates handle SEC3-04 sparse-opp gating directly: `None` triple returned when `min(conversion_games, recovery_games) < CONFIDENCE_MIN_N=10` for p_value or `< 2` for CI.
    - **Per-class Recov peer-diff call:** saves-as-W mapping on both sides per CONTEXT D-01 Recov math.
      - User side: `(eg_w, eg_d, eg_l, eg_n) = (recovery_wins + recovery_draws, 0, recovery_losses, recovery_games)` — saves count as W, with D collapsed to 0 and L unchanged. (Note: this differs from the "trinomial chess score" interpretation. We're feeding the helper a binary-coded W/L for the save-rate, with the helper's "score" calculation `(W + 0.5·D)/N` reducing to `W/N` since D=0, which IS the save rate. This is exactly the mapping CONTEXT.md §D-01 specifies.)
      - Opp side: `(ne_w, ne_d, ne_l, ne_n) = (conversion_losses + conversion_draws, 0, conversion_wins, conversion_games)` — symmetric saves-as-W mapping for the mirror class's conversion bucket.
      - Call: `recov_p, recov_ci_low, recov_ci_high = compute_score_difference_test(recovery_wins + recovery_draws, 0, recovery_losses, recovery_games, conversion_losses + conversion_draws, 0, conversion_wins, conversion_games)`
    - **NEW additive field values (on the 0-1 scale per Task 1 docstring):**
      - `opp_conversion_pct_new: float | None = round((recovery_losses / recovery_games), 4) if recovery_games >= _MIN_OPPONENT_SAMPLE else None` — opponent's win-rate (mirror identity) on the 0-1 scale; same gating as Phase 84 `opponent_conversion_pct` but rounded to 4 decimal places (0.1234) and on 0-1 scale
      - `opp_recovery_pct_new: float | None = round((conversion_losses + conversion_draws) / conversion_games, 4) if conversion_games >= _MIN_OPPONENT_SAMPLE else None`
      - `opp_conversion_games_new: int = recovery_games` (== existing Phase 84 `opponent_conversion_games`)
      - `opp_recovery_games_new: int = conversion_games`
      - Use distinct local-variable names (`*_new`) to avoid shadowing the existing Phase 84 locals; pass them as the corresponding new kwargs (`opp_conversion_pct=opp_conversion_pct_new`, etc.) in the constructor.
    - **`ConversionRecoveryStats` constructor update:** append 10 new kwargs to the existing constructor call at lines 392-407 in the order matching Task 1 field order. Existing kwargs untouched.
    - **Comment block** above the new helper calls (lines inserted ~390): single comment block citing "Phase 87 (SEC3-04 / D-01 / D-02): per-class Conv + Recov peer-bullet sig tests. Reuses `compute_score_difference_test` with mirror-flipped W↔L (Conv) and saves-as-W mapping (Recov) per CONTEXT.md D-01 math. Built-in n-gates handle sparse-opponent gating."
  </behavior>
  <action>
    Edit `app/services/endgame_service.py`.

    **Step 1 — Import update** (lines 40-70 area, locate the existing import block that imports schemas + helpers): add `compute_score_difference_test` to the existing `from app.services.score_confidence import (...)` group, OR add a new line `from app.services.score_confidence import compute_score_difference_test` if no such group import exists yet for `score_confidence`. Search for existing imports of `score_confidence` first to determine the right insertion point. Keep alphabetical order within the import group.

    **Step 2 — Lift `recovery_losses` out of the Phase 84 conditional.** At line 376-380 the existing Phase 84 code is:
    ```
    if recovery_games >= _MIN_OPPONENT_SAMPLE:
        recovery_losses = recovery_games - recovery_wins - recovery_draws
        opponent_conversion_pct = round(recovery_losses / recovery_games * 100, 1)
    else:
        opponent_conversion_pct = None
    ```
    Move the `recovery_losses = ...` assignment OUT of the `if` body, placing it at the same indent level as `recovery_games = recov_data["games"]` (line 362) so `recovery_losses` is always defined. Then change the `if`-body to `opponent_conversion_pct = round(recovery_losses / recovery_games * 100, 1)`. This makes `recovery_losses` available to the new helper call below without changing the Phase 84 semantic.

    **Step 3 — Insert the new comment block + two helper calls + four `*_new` locals** immediately after the existing Phase 84 `opponent_recovery_games = conversion_games` line (currently line 390) and BEFORE the `conversion_stats = ConversionRecoveryStats(...)` constructor at line 392.

    Comment block:
    ```
    # Phase 87 (SEC3-04 / D-01 / D-02): per-class Conv + Recov peer-bullet sig tests.
    # Reuses compute_score_difference_test with mirror-flipped W↔L (Conv) and
    # saves-as-W mapping (Recov) per CONTEXT.md D-01 math. Built-in n-gates handle
    # sparse-opponent gating (returns None triple when min(user_n, opp_n) < 10 for p,
    # < 2 for CI). 0-games safety: when either side has games == 0, the helper
    # returns (None, None, None) — no extra guard needed at the call site.
    ```

    Conv peer-diff call:
    ```
    conv_p, conv_ci_low, conv_ci_high = compute_score_difference_test(
        conversion_wins,
        conversion_draws,
        conversion_losses,
        conversion_games,
        recovery_losses,
        recovery_draws,
        recovery_wins,
        recovery_games,
    )
    ```

    Recov peer-diff call:
    ```
    recov_p, recov_ci_low, recov_ci_high = compute_score_difference_test(
        recovery_wins + recovery_draws,
        0,
        recovery_losses,
        recovery_games,
        conversion_losses + conversion_draws,
        0,
        conversion_wins,
        conversion_games,
    )
    ```

    Four new 0-1-scale opp-rate locals (use `opp_*_pct_new` / `opp_*_games_new` names to avoid clashing with existing `opponent_*` locals):
    ```
    # 0-1 scale variants of the Phase 84 mirror-rate fields for Phase 87 wire shape.
    opp_conversion_pct_new: float | None = (
        round(recovery_losses / recovery_games, 4)
        if recovery_games >= _MIN_OPPONENT_SAMPLE
        else None
    )
    opp_recovery_pct_new: float | None = (
        round((conversion_losses + conversion_draws) / conversion_games, 4)
        if conversion_games >= _MIN_OPPONENT_SAMPLE
        else None
    )
    opp_conversion_games_new: int = recovery_games
    opp_recovery_games_new: int = conversion_games
    ```

    **Step 4 — `ConversionRecoveryStats` constructor update.** In the `ConversionRecoveryStats(...)` call at lines 392-407, append 10 new keyword arguments AFTER the existing `opponent_recovery_games=opponent_recovery_games` line (line 406). Order: `opp_conversion_pct=opp_conversion_pct_new`, `opp_recovery_pct=opp_recovery_pct_new`, `opp_conversion_games=opp_conversion_games_new`, `opp_recovery_games=opp_recovery_games_new`, `conv_diff_p_value=conv_p`, `conv_diff_ci_low=conv_ci_low`, `conv_diff_ci_high=conv_ci_high`, `recov_diff_p_value=recov_p`, `recov_diff_ci_low=recov_ci_low`, `recov_diff_ci_high=recov_ci_high`. Keep all existing kwargs untouched.

    Do NOT alter the existing Phase 84 mirror-rate computation (the `opponent_conversion_pct` / `opponent_recovery_pct` 0-100-scale values stay as-is — they remain on the wire for backward compat; the new `opp_*_pct` fields are the 0-1-scale parallel exposed for Phase 87 frontend consumption).
  </action>
  <verify>
    <automated>uv run ruff check app/services/endgame_service.py &amp;&amp; uv run ruff format --check app/services/endgame_service.py &amp;&amp; uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compute_score_difference_test" app/services/endgame_service.py` returns at least 3 (one import + two call sites inside `_aggregate_endgame_stats`)
    - `grep -c "conv_diff_p_value=\|conv_diff_ci_low=\|conv_diff_ci_high=" app/services/endgame_service.py` returns 3 (three Conv kwargs on the constructor)
    - `grep -c "recov_diff_p_value=\|recov_diff_ci_low=\|recov_diff_ci_high=" app/services/endgame_service.py` returns 3 (three Recov kwargs)
    - `grep -c "opp_conversion_pct=opp_conversion_pct_new\|opp_recovery_pct=opp_recovery_pct_new\|opp_conversion_games=opp_conversion_games_new\|opp_recovery_games=opp_recovery_games_new" app/services/endgame_service.py` returns 4
    - `grep -c "opponent_conversion_pct\|opponent_recovery_pct" app/services/endgame_service.py` is unchanged from baseline (Phase 84 fields still computed for backward compat)
    - `uv run ty check app/ tests/` exits 0
    - `uv run ruff check app/services/endgame_service.py` exits 0
    - `uv run ruff format --check app/services/endgame_service.py` exits 0
  </acceptance_criteria>
  <done>
    Both new helper calls land inside the per-class loop with the locked mirror-flip math; 10 new constructor kwargs wired; existing Phase 84 mirror-rate logic untouched; ty + ruff green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pytest coverage for the 10 new wire fields + mirror-flip + sparse-opp + 0-games</name>
  <files>tests/test_endgame_service.py</files>
  <read_first>
    - tests/test_endgame_service.py — locate the existing test class(es) that exercise `_aggregate_endgame_stats` end-to-end (search for `_aggregate_endgame_stats` test calls, or for tests on `EndgameCategoryStats` / `ConversionRecoveryStats` schema fields). Use the existing fixtures as templates for the new fixtures
    - tests/test_endgame_service.py — locate any existing test that asserts on `opponent_conversion_pct` / `opponent_recovery_pct` (Phase 84 coverage) for direct precedent on the per-class mirror-rate assertion pattern
    - app/services/endgame_service.py (the modified `_aggregate_endgame_stats` from Task 2)
    - app/schemas/endgames.py (the modified `ConversionRecoveryStats` from Task 1)
  </read_first>
  <behavior>
    - Four test methods. Add them to whichever existing test class already exercises `_aggregate_endgame_stats` / Phase 84 mirror-rates. If no such class exists, create a new class `TestPerClassPeerDiff` at the appropriate position in the file.

    - **Method 1: `test_conversion_recovery_stats_carries_per_class_diff_fields`** — Build per-class WDL data covering all 5 surviving classes (rook / minor_piece / pawn / queen / mixed) with healthy game counts (≥ 30 per class, mixed outcomes). Invoke `_aggregate_endgame_stats(...)` directly (or via `get_endgame_stats` if the fixture pattern uses the higher-level entry). For each returned `EndgameCategoryStats`, assert: `stats.conversion.opp_conversion_pct is not None` and `0.0 <= stats.conversion.opp_conversion_pct <= 1.0`; same for `opp_recovery_pct`; `opp_conversion_games == stats.conversion.recovery_games` (mirror identity); `opp_recovery_games == stats.conversion.conversion_games`; `conv_diff_p_value is not None` and `0.0 <= conv_diff_p_value <= 1.0`; `conv_diff_ci_low` / `conv_diff_ci_high` non-None and the CI brackets `(userConv − oppConv)`; same triple for `recov_diff_*`.

    - **Method 2: `test_per_class_conv_diff_mirror_flip_correctness`** — Build a fixture where one class (say `rook`) has user-side Conv W=20/D=0/L=0 over 20 games and user-side Recov W=0/D=0/L=20 over 20 games (i.e. user wins all conversions, loses all recoveries). Per mirror-flip semantics, opp-side Conv W/D/L = (user recov L, D, W) = (20, 0, 0) → opp wins all → `opp_conversion_pct = 1.0`. So `userConv − oppConv = 1.0 − 1.0 = 0.0` for this synthetic. To exercise a non-zero diff, instead build user Conv W=18/D=0/L=2 (90% win) and user Recov W=2/D=0/L=18 (10% save = 80% loss → opp wins 80% on the recovery bucket). Then `opp_conversion_pct = 18/20 = 0.9`? — wait, mirror flips W↔L so opp_W = user_L = 18 (oh wait, user recov W=2, L=18 → flipped opp_W=18, opp_L=2 → opp_conv_rate = 18/20 = 0.9). So `userConv = 0.9`, `oppConv = 0.9` → diff = 0. Pick a fixture that breaks the symmetry: user Conv W=18/D=0/L=2 AND user Recov W=10/D=0/L=10. Then opp Conv W/D/L = (user recov L, D, W) = (10, 0, 10) → opp_conv_rate = 10/20 = 0.5. Diff = 0.9 − 0.5 = 0.4. Assert: `conv_diff_ci_low > 0` and `conv_diff_ci_high > 0` (CI excludes 0, signed positive); `conv_diff_p_value < 0.001` (highly significant for diff=0.4, n=20 each side). Similarly assert Recov diff for the same class is non-zero with the symmetric opposite sign.

    - **Method 3: `test_per_class_conv_diff_sparse_opp_gated_to_none`** — Build a fixture where one class has user-side `conversion_games = 50` but `recovery_games = 5` (below the helper's `CONFIDENCE_MIN_N = 10` p-gate but above the `>= 2` CI gate). Assert for that class: `conv_diff_p_value is None` (min(50, 5) = 5 < 10); `conv_diff_ci_low is not None` and `conv_diff_ci_high is not None` (min(50, 5) = 5 >= 2 — CI still computed). Then build a parallel fixture where `recovery_games = 1`: `conv_diff_p_value is None`, `conv_diff_ci_low is None`, `conv_diff_ci_high is None` (both gates fail). Assert the symmetric pattern for `recov_diff_*` when `conversion_games < 10` / `< 2`.

    - **Method 4: `test_per_class_diff_zero_games_safety`** — Build a fixture where one class (say `queen`) has `conversion_games = 0` and `recovery_games = 0` (no eval-stratified games). Assert: `stats.conversion.conv_diff_p_value is None`, `conv_diff_ci_low is None`, `conv_diff_ci_high is None`, `recov_diff_*` all None, `opp_conversion_pct is None`, `opp_recovery_pct is None`. The helper's `eg_n <= 0 or ne_n <= 0` early-return handles this — no extra guard needed. Verify the per-class loop still constructs a valid `ConversionRecoveryStats` for this class (does not crash).

    - All four tests should follow the existing test-style conventions in `tests/test_endgame_service.py` (async functions if needed, fixture builders for WDL accumulators, `await get_endgame_stats(...)` or direct `_aggregate_endgame_stats(...)` call depending on the existing pattern).
  </behavior>
  <action>
    Open `tests/test_endgame_service.py`. Find the existing tests covering `opponent_conversion_pct` / `opponent_recovery_pct` (Phase 84) — that's the closest precedent for fixture shape, since the new tests use the same per-class `conv` / `recov` accumulator dicts as inputs.

    Build (or reuse if already present) a small `_make_conv_recov_dicts(...)` helper that constructs the per-class `conv` and `recov` dicts that `_aggregate_endgame_stats` expects (matching the existing `conv_data["games"]` / `conv_data["wins"]` / etc. shape at lines 354-364 of `endgame_service.py`).

    Implement the 4 test methods per the behavior block. Use descriptive `assert ... is not None` + numeric-range assertions. Mirror the existing Phase 84 test style (likely synchronous if `_aggregate_endgame_stats` is sync; check the function signature).

    For Method 2, hand-compute the expected diff and CI bounds with a calculator before writing the assertion — the helper produces a tight CI for n=20 at p=0.9 vs p=0.5, so a `pytest.approx(0.4, abs=0.01)` assertion is safe for the diff midpoint. Use `pytest.approx` for numeric tolerance everywhere; don't require exact equality.

    Do NOT modify the existing Phase 84 tests for `opponent_conversion_pct` / `opponent_recovery_pct`. Those tests must continue to pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -x -k "per_class_diff or conv_diff or recov_diff or per_class_conv or per_class_recov"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_conversion_recovery_stats_carries_per_class_diff_fields\|test_per_class_conv_diff_mirror_flip_correctness\|test_per_class_conv_diff_sparse_opp_gated_to_none\|test_per_class_diff_zero_games_safety" tests/test_endgame_service.py` returns 4
    - `uv run pytest tests/test_endgame_service.py -x -k "per_class_diff or conv_diff or recov_diff or per_class_conv or per_class_recov"` collects ≥ 4 tests and exits 0
    - `uv run pytest tests/test_endgame_service.py -x -k "opponent_conversion_pct or opponent_recovery_pct"` still exits 0 (Phase 84 tests untouched)
    - `uv run ruff check tests/test_endgame_service.py` exits 0
  </acceptance_criteria>
  <done>
    Four pytest cases cover schema-presence (all 5 classes), mirror-flip correctness (non-zero diff with known math), sparse-opponent gating (asymmetric p-gate vs CI-gate), and 0-games safety. No regression in existing Phase 84 tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mirror the 10 new fields into frontend TS type ConversionRecoveryStats</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts lines 9-20 (`ConversionRecoveryStats` — existing TS shape; the 10 new fields append AFTER `recovery_draws: number` on line 19, matching the Python ordering)
    - frontend/src/types/endgames.ts lines 127-131 (`MaterialRow` Phase 86 `diff_p_value` / `diff_ci_low` / `diff_ci_high` — exact precedent for the additive `number | null` TS field pattern with one-line phase-citation comments)
    - .planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-CONTEXT.md §D-01 (10-field list)
  </read_first>
  <behavior>
    - Append 10 new fields to `ConversionRecoveryStats` (TS interface) AFTER the existing `recovery_draws: number;` line (line 19) in this exact order to match the Python schema:
      1. `opp_conversion_pct: number | null;` — opp win-rate when opp entered with eval advantage (0-1 scale; null when recovery_games < 10)
      2. `opp_recovery_pct: number | null;` — opp save-rate when opp entered with eval deficit (0-1 scale)
      3. `opp_conversion_games: number;` — opp sample size (== recovery_games)
      4. `opp_recovery_games: number;` — opp sample size (== conversion_games)
      5. `conv_diff_p_value: number | null;` — Wald-z p on per-class `userConv − oppConv`; null when min(conversion_games, recovery_games) < 10
      6. `conv_diff_ci_low: number | null;`
      7. `conv_diff_ci_high: number | null;`
      8. `recov_diff_p_value: number | null;` — Wald-z p on per-class `userRecov − oppRecov`; null when min(conversion_games, recovery_games) < 10
      9. `recov_diff_ci_low: number | null;`
      10. `recov_diff_ci_high: number | null;`
    - Each new field has a one-line `// Phase 87 (SEC3-04 / D-01): ...` comment referencing the gate.
  </behavior>
  <action>
    Edit `frontend/src/types/endgames.ts`.

    In `ConversionRecoveryStats` (lines 9-20), append the 10 new fields AFTER `recovery_draws: number;`. Each is `number | null;` (sig + opp_*_pct fields) or `number;` (opp_*_games counts) with a one-line `//` comment referencing "Phase 87 (SEC3-04 / D-01)" and the gating rule.

    Mirror the Phase 86 comment style at lines 127-131:
    ```
    // Phase 87 (SEC3-04 / D-01): opp win-rate on same-class recovery bucket (mirror-flipped);
    // 0-1 scale; null when recovery_games < 10 (helper p-gate).
    opp_conversion_pct: number | null;
    ```

    Keep the file's import block and other interfaces (`EndgameCategoryStats`, `MaterialRow`, etc.) untouched. Do NOT reorder existing fields.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^[[:space:]]*(opp_conversion_pct|opp_recovery_pct|opp_conversion_games|opp_recovery_games|conv_diff_p_value|conv_diff_ci_low|conv_diff_ci_high|recov_diff_p_value|recov_diff_ci_low|recov_diff_ci_high):" frontend/src/types/endgames.ts` returns 10
    - `cd frontend && npx tsc --noEmit` exits 0
    - `grep -c "conversion_pct: number\|conversion_games: number\|recovery_pct: number\|recovery_games: number\|recovery_saves: number\|recovery_wins: number\|recovery_draws: number" frontend/src/types/endgames.ts` is unchanged from baseline (Phase 84 fields preserved)
  </acceptance_criteria>
  <done>
    TS interface `ConversionRecoveryStats` mirrors the 10 new Python fields character-for-character; `tsc --noEmit` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Additive Pydantic fields populated by trusted in-process aggregations; no SQL change, no new endpoint, no new user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information Disclosure | new wire fields | accept | Aggregate floats already derivable from the existing per-class W/D/L counts already on `ConversionRecoveryStats`; no new disclosure surface. Fields are user-scoped (same auth gate as the parent `/api/endgames/overview` endpoint). |
| T-87-02 | Tampering | service-layer arithmetic | accept | All inputs come from trusted accumulators populated by the same SQL cursor as the existing Phase 84 mirror-rate logic. No external input enters the math. |
| T-87-03 | Denial-of-Service | per-class diff test loop | accept | 5 classes × 2 helper calls × constant-time math; bounded work per request. The helper itself is constant-time (no loops over W/D/L counts). |
</threat_model>

<verification>
- `uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest tests/test_endgame_service.py tests/services/test_score_confidence.py -x` all exit 0
- `cd frontend && npx tsc --noEmit` exits 0
- `grep -n "conv_diff_p_value\|recov_diff_p_value\|opp_conversion_pct\|opp_recovery_pct" app/schemas/endgames.py app/services/endgame_service.py frontend/src/types/endgames.ts tests/test_endgame_service.py` shows the new fields defined, wired, mirrored to TS, and asserted in tests
- `grep -n "compute_score_difference_test" app/services/endgame_service.py` shows the helper imported + called twice inside `_aggregate_endgame_stats`
</verification>

<success_criteria>
- Backend exposes 10 new additive fields on `ConversionRecoveryStats` per Phase 87 D-01
- `_aggregate_endgame_stats` calls `compute_score_difference_test` twice per class (Conv mirror-flip W↔L; Recov saves-as-W mapping) per CONTEXT.md D-01 math, using the existing helper unchanged per D-02
- The Phase 84 `opponent_conversion_pct` / `opponent_recovery_pct` fields and computation are preserved (existing wire compat)
- TS type `ConversionRecoveryStats` mirrors the new wire shape with matching `number | null` / `number` semantics
- Pytest covers field presence (all 5 classes), mirror-flip correctness with known math, sparse-opponent gating (asymmetric p vs CI), and 0-games safety
- ruff / ruff-format / ty / tsc / pytest all green
- SEC3-04 backend wire is complete; frontend cards in Plans 02-03 will consume the new wire fields with no math duplicated client-side
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.17-phases/87-section-3-per-type-endgame-type-breakdown-cards/87-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- The dual-scale decision (Phase 84 `opponent_conversion_pct` kept on 0-100 scale for backward compat; new `opp_conversion_pct` on 0-1 scale for Phase 86 consistency).
- The Recov saves-as-W mapping choice per CONTEXT.md D-01 (mapping `(W+D, 0, L)` to the helper's `(w, d, l)` shape — verified the helper's chess-score `(W + 0.5·D)/N` reduces to the save rate `W'/N'` when D'=0). Note this as a viable alternative to introducing a save-rate-specific helper.
- The location of the `recovery_losses` lift out of the Phase 84 conditional, so future readers know the variable is unconditionally in scope.
</output>
</content>
</invoke>