---
phase: 88-time-pressure-stats-rework
plan: 05
type: execute
wave: 2
depends_on:
  - 88-03
files_modified:
  - frontend/src/lib/pressureBulletConfig.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "pressureBulletConfig.ts exports PRESSURE_DELTA_CENTER (0), PRESSURE_DELTA_DOMAIN (0.20), CLOCK_GAP_DOMAIN (0.30)"
    - "pressureBulletConfig.ts exports clampDeltaCi(value) and pressureDeltaZoneColor(delta, neutralMin, neutralMax)"
    - "Module is self-contained; all theme colors imported from @/lib/theme; no hard-coded color literals"
  artifacts:
    - path: "frontend/src/lib/pressureBulletConfig.ts"
      provides: "Domain constants + clampDeltaCi + pressureDeltaZoneColor helpers for the Phase 88 bullets"
      contains: "pressureDeltaZoneColor"
  key_links:
    - from: "frontend/src/lib/pressureBulletConfig.ts"
      to: "frontend/src/lib/theme.ts"
      via: "ZONE_DANGER / ZONE_NEUTRAL / ZONE_SUCCESS imports"
      pattern: "from '@/lib/theme'"
---

<objective>
Add the small frontend config module that the Phase 88 card component will import. Mirrors `frontend/src/lib/scoreBulletConfig.ts` exactly in shape and conventions (one file per metric family), but accepts `(neutralMin, neutralMax)` arguments on `pressureDeltaZoneColor` instead of module-level fixed constants, because the neutral band varies per `(TC, quintile)` (looked up via `PRESSURE_BIN_SCORE_NEUTRAL_ZONES[tc][quintile]`).

Purpose: keeps the card component compact -- `EndgameTimePressureCard` imports a single config module rather than wiring zone-color logic inline. Also locks the bullet axis/center conventions so the Section component (Plan 07) renders consistent geometry across the 4-TC grid.

Output: one new file, ~50 LOC, no other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@frontend/src/lib/scoreBulletConfig.ts
@frontend/src/lib/theme.ts
@frontend/src/generated/endgameZones.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pressureBulletConfig.ts</name>
  <files>frontend/src/lib/pressureBulletConfig.ts</files>
  <read_first>
    - frontend/src/lib/scoreBulletConfig.ts (whole file -- the analog; copy the named-constant + zone-color-function shape)
    - frontend/src/lib/theme.ts (the `ZONE_DANGER`, `ZONE_NEUTRAL`, `ZONE_SUCCESS` exports)
    - frontend/src/generated/endgameZones.ts (confirm `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` and `CLOCK_GAP_NEUTRAL_MIN/MAX` are exported with the expected TS types)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 6 -- exact code excerpt to mirror)
  </read_first>
  <behavior>
    - `PRESSURE_DELTA_CENTER === 0`.
    - `PRESSURE_DELTA_DOMAIN === 0.20`.
    - `CLOCK_GAP_DOMAIN === 0.30`.
    - `clampDeltaCi(1.5)` returns 1; `clampDeltaCi(-1.5)` returns -1; `clampDeltaCi(0.1)` returns 0.1.
    - `pressureDeltaZoneColor(delta, neutralMin, neutralMax)` returns `ZONE_SUCCESS` when `delta >= neutralMax`, `ZONE_DANGER` when `delta <= neutralMin`, `ZONE_NEUTRAL` otherwise.
    - All theme colors are imported from `@/lib/theme`; no hard-coded color literals appear in the file.
  </behavior>
  <action>
    Create `frontend/src/lib/pressureBulletConfig.ts` modelled after `frontend/src/lib/scoreBulletConfig.ts`. Exports:

    1. `export const PRESSURE_DELTA_CENTER = 0;` -- center for the Score-Delta bullet.
    2. `export const PRESSURE_DELTA_DOMAIN = 0.20;` -- half-width on the Score-Delta axis (covers expected +/-15pp IQR generously).
    3. `export const CLOCK_GAP_DOMAIN = 0.30;` -- half-width on the Clock Gap bullet (covers the prod p5/p95 range per Research Q1).
    4. `export function clampDeltaCi(value: number): number` -- clamps to `[-1, 1]`.
    5. `export function pressureDeltaZoneColor(delta: number, neutralMin: number, neutralMax: number): string` -- returns `ZONE_SUCCESS` / `ZONE_DANGER` / `ZONE_NEUTRAL`. neutralMin and neutralMax are passed by the caller from `PRESSURE_BIN_SCORE_NEUTRAL_ZONES[tc][quintile]`.

    Import only `ZONE_DANGER`, `ZONE_NEUTRAL`, `ZONE_SUCCESS` from `@/lib/theme`. No other dependencies. JSDoc lines on each export explaining the purpose, mirroring the existing scoreBulletConfig.ts style (which the analog file already follows).

    Per CLAUDE.md theme-constants rule: no hard-coded color values; per noUncheckedIndexedAccess rule: no array/Record index accesses in this module.

    Knip-clean expectation: this file is consumed by `EndgameTimePressureCard.tsx` in Plan 06. To avoid knip failing during Wave 2 (file exists but no consumer until Wave 3), the executor MUST ensure knip is run only on the full phase (i.e. skip per-task knip in this plan). The Plan 07 final sweep enforces knip-clean for the whole phase.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit src/lib/pressureBulletConfig.ts &amp;&amp; grep -cE "export const PRESSURE_DELTA_CENTER|export const PRESSURE_DELTA_DOMAIN|export const CLOCK_GAP_DOMAIN|export function clampDeltaCi|export function pressureDeltaZoneColor" src/lib/pressureBulletConfig.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/lib/pressureBulletConfig.ts` exists.
    - `grep -c "from '@/lib/theme'" frontend/src/lib/pressureBulletConfig.ts` returns 1.
    - `grep -c "export const PRESSURE_DELTA_CENTER\|export const PRESSURE_DELTA_DOMAIN\|export const CLOCK_GAP_DOMAIN\|export function clampDeltaCi\|export function pressureDeltaZoneColor" frontend/src/lib/pressureBulletConfig.ts` returns 5.
    - `cd frontend &amp;&amp; npx tsc --noEmit src/lib/pressureBulletConfig.ts` exits 0.
    - No hex color literals in the file: `grep -cE "#[0-9a-fA-F]{3,8}" frontend/src/lib/pressureBulletConfig.ts` returns 0.
  </acceptance_criteria>
  <done>Module exists; types compile; theme imports correct; ready for consumption in Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure utility module; no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05-01 | Tampering | Numerical mis-clamp of CI bounds | mitigate | Behaviour block specifies exact clampDeltaCi outputs at edges; type-checked by tsc |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd frontend &amp;&amp; npx tsc --noEmit src/lib/pressureBulletConfig.ts` exits 0.
- File contains the 5 documented exports.
- No hard-coded color values present.
</verification>

<success_criteria>
- Module is self-contained, type-clean, theme-clean.
- Ready for `EndgameTimePressureCard.tsx` (Plan 06) to import without further configuration.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-05-SUMMARY.md` when done.
</output>
</content>
</invoke>