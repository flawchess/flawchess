---
phase: 88-time-pressure-stats-rework
plan: 07
type: execute
wave: 4
depends_on:
  - 88-06
files_modified:
  - frontend/src/components/charts/EndgameTimePressureSection.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/EndgameClockPressureSection.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "EndgameTimePressureSection renders a responsive grid: xl:grid-cols-4, lg:grid-cols-2, base 1-col (D-01)"
    - "Section consumes `time_pressure_cards: TimePressureCardsResponse` from EndgameOverviewResponse and maps to EndgameTimePressureCard"
    - "Endgames.tsx mounts the new section in the slot previously occupied by the two legacy sections; both legacy components are deleted"
    - "knip is clean across the frontend after deletions; no orphan exports or imports remain"
    - "ty + pytest + vitest + tsc + npm run lint + npm run knip all green"
  artifacts:
    - path: "frontend/src/components/charts/EndgameTimePressureSection.tsx"
      provides: "Section orchestrator: section wrapper, sub-question copy, responsive grid over EndgameTimePressureCard"
      contains: "EndgameTimePressureSection"
    - path: "frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx"
      provides: "Grid + visibility tests; legacy testid absence assertion (knip-clean proxy)"
      contains: "EndgameTimePressureSection"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "New section mounted; two legacy section imports + usages removed"
      contains: "EndgameTimePressureSection"
  key_links:
    - from: "Endgames.tsx"
      to: "EndgameTimePressureSection"
      via: "consumes overviewData.time_pressure_cards"
      pattern: "time_pressure_cards"
    - from: "EndgameTimePressureSection"
      to: "EndgameTimePressureCard"
      via: "cards.map -> per-TC card rendering"
      pattern: "<EndgameTimePressureCard"
---

<objective>
Close the frontend slice: build the section orchestrator that maps the four time-pressure cards into a responsive grid, wire it on `Endgames.tsx` in the slot the two legacy sections occupied, delete `EndgameClockPressureSection.tsx` AND the now-superseded `EndgameTimePressureSection.tsx` (the legacy line-chart -- which is replaced by a new file of the same name; the executor must overwrite, NOT preserve the legacy behaviour).

Note on filename reuse: the new section component reuses the legacy filename `EndgameTimePressureSection.tsx` (per CONTEXT.md "Reuse the name" hint). The executor MUST treat this as a full rewrite: open the legacy file, replace its contents with the new section orchestrator's implementation, and ensure no legacy export survives. The legacy `EndgameClockPressureSection.tsx` is deleted outright. After the rewrite + delete, knip sweep should report zero issues.

This plan is the final wave-4 step; it ends with a full-suite verification.

Output: 3 files modified (new section + new test + Endgames.tsx) + 1 file deleted; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@frontend/src/components/charts/EndgameTypeBreakdownSection.tsx
@frontend/src/components/charts/EndgameClockPressureSection.tsx
@frontend/src/components/charts/EndgameTimePressureSection.tsx
@frontend/src/pages/Endgames.tsx
@frontend/src/components/charts/EndgameTimePressureCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite EndgameTimePressureSection.tsx as the new section orchestrator</name>
  <files>frontend/src/components/charts/EndgameTimePressureSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (whole file -- the legacy implementation, to be FULLY REPLACED)
    - frontend/src/components/charts/EndgameTypeBreakdownSection.tsx (whole file -- the orchestrator analog: section wrapper, sub-question copy, responsive grid pattern at line ~55)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (from Plan 06)
    - frontend/src/types/api.ts (TimePressureCardsResponse type)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 7 -- section orchestrator pattern)
  </read_first>
  <action>
    REPLACE the entire contents of `frontend/src/components/charts/EndgameTimePressureSection.tsx` with the new section orchestrator. Do NOT preserve any legacy code; remove all legacy imports.

    Component signature: `export function EndgameTimePressureSection({ data }: { data: TimePressureCardsResponse })` returning `JSX.Element`.

    Body structure (mirror `EndgameTypeBreakdownSection.tsx`):
    1. `<section data-testid="time-pressure-cards-section" aria-labelledby="time-pressure-heading">`.
    2. Section heading (`<h2 id="time-pressure-heading" className="...">` with appropriate text -- "Time pressure" or "How clock pressure shapes your performance"; planner picks consistent with the sibling section headings on the Endgames page).
    3. Sub-question copy: `<p className="text-sm text-muted-foreground">How does your score change as your clock runs down?</p>` (or equivalent; matches the existing "sub-question above the grid" pattern from `EndgameTypeBreakdownSection`).
    4. Responsive grid: `<div className="grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-4 gap-4 mt-2">` mapping `data.cards` to `<EndgameTimePressureCard key={card.tc} card={card} />`.
    5. Empty state: if `data.cards.length === 0`, render a single full-width muted message ("No time-pressure data yet -- import more games to see this section"); apply `data-testid="time-pressure-cards-empty"`.

    Imports:
    ```
    import { EndgameTimePressureCard } from '@/components/charts/EndgameTimePressureCard';
    import type { TimePressureCardsResponse } from '@/types/api';
    ```

    No legacy imports. No legacy state. The component is a thin orchestrator.

    Constraints from CLAUDE.md: semantic HTML (`<section>`, `<h2>`, `<p>`); `data-testid` on every major container; `text-sm` minimum font size; mobile-friendly (grid collapses to 1-col below `lg`); knip-clean (consumed by `Endgames.tsx` in Task 3).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit src/components/charts/EndgameTimePressureSection.tsx 2>&amp;1 | tail -10 ; grep -c "EndgameTimePressureCard\|time-pressure-cards-section\|xl:grid-cols-4" frontend/src/components/charts/EndgameTimePressureSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function EndgameTimePressureSection\|export const EndgameTimePressureSection" frontend/src/components/charts/EndgameTimePressureSection.tsx` returns 1.
    - `grep -c "EndgameTimePressureCard" frontend/src/components/charts/EndgameTimePressureSection.tsx` >= 2 (import + render).
    - `grep -c "xl:grid-cols-4\|lg:grid-cols-2" frontend/src/components/charts/EndgameTimePressureSection.tsx` >= 2.
    - `grep -c "time-pressure-cards-section" frontend/src/components/charts/EndgameTimePressureSection.tsx` >= 1.
    - No legacy code survives: `grep -c "TimePressureChartResponse\|ClockPressureResponse\|LineChart\|ResponsiveContainer" frontend/src/components/charts/EndgameTimePressureSection.tsx` returns 0.
    - `cd frontend && npx tsc --noEmit src/components/charts/EndgameTimePressureSection.tsx` exits 0.
  </acceptance_criteria>
  <done>New section orchestrator in place; no legacy code; ready for page wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EndgameTimePressureSection.test.tsx</name>
  <files>frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx (whole file; boilerplate at lines 1-48 + grid-test patterns)
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (from Task 1)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 8 -- key scenarios)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md (Validation Architecture: Integration 4-TC grid rendering -- 4 scenarios)
  </read_first>
  <behavior>
    - When `data.cards` includes only `bullet` and `rapid` cards: `data-testid="time-pressure-card-bullet"` and `data-testid="time-pressure-card-rapid"` are present; `data-testid="time-pressure-card-blitz"` and `data-testid="time-pressure-card-classical"` are absent.
    - When `data.cards` is empty: `data-testid="time-pressure-cards-empty"` is present.
    - When `data.cards` contains all 4 TCs: all 4 card testids are present in the documented order.
    - Legacy testid `data-testid="clock-pressure-section"` is absent (proxy assertion for knip-clean post-deletion).
    - Section wrapper testid `data-testid="time-pressure-cards-section"` is always present when the component renders.
  </behavior>
  <action>
    Create `frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx`. Copy the `@vitest-environment jsdom` directive and the boilerplate (matchMedia stub, ResizeObserver stub, cleanup) from `EndgameTypeBreakdownSection.test.tsx` verbatim. Wrap renders in the same `MemoryRouter` + `TooltipProvider` (or whatever providers `EndgameTypeBreakdownSection.test.tsx` uses) so child component renders work.

    Provide a `makePayload(...tcs)` helper that returns a `TimePressureCardsResponse` with one card per supplied TC (each card large enough to render -- `total: 100`, all quintile bins filled with `n: 20`, neutral delta/p_value values). Default cards: bullet, blitz, rapid, classical.

    Implement the test methods from `<behavior>`:
    - `renders visible TC cards and omits hidden ones`
    - `renders empty state when cards array is empty`
    - `renders all 4 TC cards in canonical order when payload contains all four`
    - `asserts legacy data-testid="clock-pressure-section" is absent`
    - `wraps content in a section with time-pressure-cards-section testid`

    Tests MUST pass `cd frontend && npm test -- --run src/components/charts/__tests__/EndgameTimePressureSection.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/EndgameTimePressureSection.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/EndgameTimePressureSection.test.tsx` exits 0 with >= 5 tests passing.
    - `grep -c "renders visible TC cards\|renders empty state\|asserts legacy data-testid" frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx` >= 3.
  </acceptance_criteria>
  <done>Section vitest suite green; legacy-absence assertion in place.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Endgames.tsx to the new section + delete legacy sections + final sweep</name>
  <files>frontend/src/pages/Endgames.tsx, frontend/src/components/charts/EndgameClockPressureSection.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx (whole file; specifically the existing references to `overviewData.clock_pressure` at line 285 and `overviewData.time_pressure_chart` at line 286, plus the imports of `EndgameClockPressureSection` and `EndgameTimePressureSection`)
    - frontend/src/components/charts/EndgameClockPressureSection.tsx (to delete -- skim to confirm no surprise consumers; grep -rn its name across `frontend/src/` first)
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (already rewritten in Task 1 -- import path unchanged)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Files to Delete section)
  </read_first>
  <action>
    1. **Update `frontend/src/pages/Endgames.tsx`:**
       - Remove the import of `EndgameClockPressureSection` (whatever path).
       - Keep / update the import of `EndgameTimePressureSection` (path unchanged after the Task 1 rewrite).
       - Replace the two legacy section mounts in the page body with a single `<EndgameTimePressureSection data={overviewData.time_pressure_cards} />` at the same slot.
       - Update the destructuring of `overviewData` to consume `time_pressure_cards` instead of `clock_pressure` + `time_pressure_chart`.
       - If the page has an `isError` / `isLoading` branch for the legacy sections that needs adapting, apply CLAUDE.md's "Always handle isError" rule for the new section too -- but keep the change minimal (reuse the same overview-level error handling that already exists; do not duplicate per-section error UI unless the legacy code did so).

    2. **Delete `frontend/src/components/charts/EndgameClockPressureSection.tsx`** via `git rm` (executor uses the Bash tool: `git rm frontend/src/components/charts/EndgameClockPressureSection.tsx`).

    3. **Run full-suite verification** (this plan is the final wave-4 step):
       - `cd frontend && npm run lint` -- exits 0.
       - `cd frontend && npm run knip` -- exits 0 (no dead exports / unused dependencies introduced by the phase).
       - `cd frontend && npx tsc --noEmit` -- exits 0 across the whole frontend.
       - `cd frontend && npm test -- --run` -- full vitest suite exits 0.
       - `uv run ruff check . && uv run ruff format --check .` -- exits 0.
       - `uv run ty check app/ tests/` -- exits 0.
       - `uv run pytest -x -q` -- exits 0.
       - `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` -- exits 0 (codegen drift gate).

    If any check fails, fix forward within the scope of this plan (no scope creep beyond Phase 88 surfaces). If a check fails for an unrelated reason (a pre-existing bug surfaced by the deletion), surface it in the SUMMARY and confirm with the user before proceeding.

    Per CLAUDE.md: apply changes to mobile too (the new section is mobile-friendly by construction via responsive Tailwind utils; no separate mobile drawer surface for this section based on the analog pattern).
  </action>
  <verify>
    <automated>test ! -f frontend/src/components/charts/EndgameClockPressureSection.tsx &amp;&amp; cd frontend &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/charts/EndgameClockPressureSection.tsx` does not exist (`test ! -f ...` exits 0).
    - `grep -c "EndgameClockPressureSection" frontend/src/pages/Endgames.tsx` returns 0.
    - `grep -c "EndgameTimePressureSection" frontend/src/pages/Endgames.tsx` returns >= 2 (import + usage).
    - `grep -c "clock_pressure\|time_pressure_chart" frontend/src/pages/Endgames.tsx` returns 0.
    - `grep -c "time_pressure_cards" frontend/src/pages/Endgames.tsx` returns >= 1.
    - `grep -rn "EndgameClockPressureSection\|ClockPressureResponse\|TimePressureChartResponse" frontend/src/ app/ tests/` returns 0 matches.
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm run knip` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm test -- --run` exits 0.
    - `uv run ty check app/ tests/` exits 0.
    - `uv run pytest -x -q` exits 0.
    - `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0 (codegen drift gate).
  </acceptance_criteria>
  <done>Endgames.tsx wired to the new section; legacy ClockPressure section deleted; full suite green across backend + frontend; codegen drift-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `EndgameOverviewResponse` -> `Endgames.tsx` -> `EndgameTimePressureSection` | Server-validated by Pydantic; page passes the typed payload through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-07-01 | Tampering | Stale import of deleted `EndgameClockPressureSection` somewhere in the codebase | mitigate | Task 3 grep step asserts zero references across `frontend/src/`, `app/`, `tests/`; knip + tsc + lint enforce the same |
| T-88-07-02 | Information Disclosure | Endgames page rendering with empty `time_pressure_cards` array silently looks broken | mitigate | Empty-state UI with `data-testid="time-pressure-cards-empty"` is rendered when `cards.length === 0`; test `renders empty state when cards array is empty` enforces |
| T-88-07-03 | Denial of Service | `Endgames.tsx` consumer-error path: API returns 500, page surfaces "no games" instead of "failed to load" | mitigate | CLAUDE.md "Always handle isError" rule applied at the page level; reuse the existing overview-level isError branch rather than a per-section one |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Full suite green: `npm run lint`, `npm run knip`, `npx tsc --noEmit`, `npm test -- --run`, `ruff check`, `ty check`, `pytest -x -q`, codegen drift gate.
- Legacy component file gone; no stale references anywhere.
- Endgames page mounts the new section.
</verification>

<success_criteria>
- Endgames page renders one card per TC in a responsive grid (xl 4-col, lg 2-col, base 1-col).
- Legacy `EndgameClockPressureSection` deleted; legacy line-chart implementation overwritten.
- Knip + ty + lint + tests + codegen all green.
- Page-level wiring intact: no broken consumers, no orphan imports.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-07-SUMMARY.md` when done.
</output>
</content>
</invoke>