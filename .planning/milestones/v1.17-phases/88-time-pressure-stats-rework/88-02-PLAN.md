---
phase: 88-time-pressure-stats-rework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/benchmarks/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "/benchmarks skill documents how to compute chess-score-per-pressure-bin per-quintile collapse verdicts (D-03)"
    - "/benchmarks skill documents the new clock-gap-% metric under §3.3.1 (D-03)"
    - "Skill specifies the per-user (W+0.5D)/N per (TC × ELO × quintile) shape and the 4×5 IQR shipped band (ELO pooled by default)"
    - "Skill specifies that cohort_score is NOT in this output — only IQR band bounds"
  artifacts:
    - path: ".claude/skills/benchmarks/SKILL.md"
      provides: "§3.3.3 chess-score-per-pressure-bin section + §3.3.1 clock-gap-% submetric"
      contains: "chess-score-per-pressure-bin"
  key_links:
    - from: "SKILL.md §3.3.3"
      to: "endgame_zones.py PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
      via: "Q1/Q3 per (TC, quintile) zone constants"
      pattern: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
    - from: "SKILL.md §3.3.1 clock-gap-%"
      to: "endgame_zones.py ZONE_REGISTRY['clock_gap_pct']"
      via: "single global ZoneSpec from collapse verdict"
      pattern: "clock_gap_pct"
---

<objective>
Update the `/benchmarks` skill (`.claude/skills/benchmarks/SKILL.md`) with two new metric definitions that feed the Phase 88 zone constants (D-03):

1. **§3.3.3 chess-score-per-pressure-bin** — a new "metric-with-sub-bins" pattern: per-user `user_score = (W + 0.5D) / N` per `(user_id × TC × ELO × quintile)` cell, with per-quintile Cohen's d collapse verdicts (5 independent verdicts, one per quintile) and per-(TC, quintile) Q1/Q3 of per-user scores as the shipped band (ELO pooled by default; any quintile failing ELO collapse promotes to per-ELO faceting).
2. **§3.3.1 extension** — add a `clock-gap-%` submetric: per-user mean `(my_clock - opp_clock) / base_clock` at endgame entry per `(TC, ELO)`. Expected to collapse to a single global zone band.

Purpose: the skill is the recipe the user runs against the benchmark DB to produce real Q1/Q3 / collapse-verdict values that replace the placeholders in Plan 03. This plan ONLY updates the skill documentation — the actual benchmark run + zone-constant update is Plan 08 (sequenced post zone-skeleton).

Output: revised SKILL.md with two new subchapters, no other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@.claude/skills/benchmarks/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add §3.3.3 chess-score-per-pressure-bin to SKILL.md</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (whole file; specifically §3.3.2 time-pressure-vs-performance for the existing query skeleton to extend, and the "Collapse verdict methodology" section for the standard Cohen's d recipe to mirror)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md (§Q4 — full SQL pattern, per-quintile collapse verdict structure, integration with existing skill sections)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 10 — analog match)
  </read_first>
  <action>
    Append a new subchapter `### §3.3.3 chess-score-per-pressure-bin` to SKILL.md immediately after the existing §3.3.2. Use the same heading depth and section structure as §3.3.2 (Shape / SQL skeleton / Collapse verdict / Output).

    Mandatory content elements (per 88-RESEARCH.md §Q4):
    - **Shape** statement: "metric-with-sub-bins" — per-user `user_score = (W + 0.5D) / N` per `(user_id × TC × ELO × quintile)` cell where `quintile = LEAST(4, FLOOR(user_clk_pct / 20.0)::int)` and `user_clk_pct = user_clock_at_endgame_entry / base_clock * 100`.
    - **SQL skeleton** (paste the CTE pattern from 88-RESEARCH.md §Q4 verbatim, anchored on `selected_users` CTE and `endgame_games_with_clock` / `per_user_quintile` CTEs; HAVING `count(*) >= 5` for per-bin sample floor; HAVING `count(*) >= 10` for Cohen's d cells).
    - **Sparse-cell exclusion**: `NOT (elo_bucket = 2400 AND tc = 'classical')` — same as §3.3.2.
    - **Equal-footing filter**: `abs(opp_rating - user_rating) <= 100` — same as §3.3.2.
    - **Per-quintile collapse verdict**: explicit statement that Cohen's d is computed PER QUINTILE INDEPENDENTLY (5 separate verdicts, one per quintile-axis pair), not as a global single verdict. Provide a verdict-table template showing 5 rows (Q0..Q4), each with TC-axis d_max and ELO-axis d_max plus the {collapse | review | keep} thresholds (< 0.2 / 0.2-0.5 / >= 0.5).
    - **Shipped band shape**: 20 entries (4 TC × 5 quintile) keyed `(tc, quintile_index)`, values = Q1/Q3 of per-user `user_score` in that cell, ELO pooled by default. Any quintile where ELO verdict is "keep separate" gets promoted to `(TC × ELO × quintile)` for that quintile only (adds 5 entries per stratified quintile).
    - **Output destination**: `app/services/endgame_zones.py` `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`. State explicitly: "cohort_score reference line is NOT in this output — it comes from the live API mirror-bucket lookup, not benchmark precomputation."
    - **Editorial cap note**: refer to `PRESSURE_BIN_NEUTRAL_CAP = 0.06` half-width cap (defined in `endgame_zones.py`). State: "If `(p75 - p25) / 2 > 0.06`, cap at 0.06 symmetrically around the median."

    Style rules from CLAUDE.md: em-dashes sparingly (use commas/periods/colons in prose); no precomputed stories; no jargon beyond the skill's existing vocabulary; do not editorialize methodology.
  </action>
  <verify>
    <automated>grep -c "chess-score-per-pressure-bin\|PRESSURE_BIN_SCORE_NEUTRAL_ZONES\|per-quintile collapse verdict" .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### §3.3.3 chess-score-per-pressure-bin" .claude/skills/benchmarks/SKILL.md` returns 1.
    - `grep -c "PRESSURE_BIN_SCORE_NEUTRAL_ZONES" .claude/skills/benchmarks/SKILL.md` >= 1.
    - `grep -c "LEAST(4, FLOOR" .claude/skills/benchmarks/SKILL.md` >= 1 (SQL skeleton present).
    - `grep -c "PRESSURE_BIN_NEUTRAL_CAP" .claude/skills/benchmarks/SKILL.md` >= 1.
    - `grep -c "NOT (elo_bucket = 2400 AND tc = 'classical')" .claude/skills/benchmarks/SKILL.md` >= 2 (one for §3.3.2 unchanged, one for new §3.3.3).
    - The new subchapter explicitly states "5 separate verdicts" or equivalent phrasing for per-quintile Cohen's d.
  </acceptance_criteria>
  <done>§3.3.3 exists with SQL skeleton, per-quintile collapse verdict template, 4×5 shipped band shape, and explicit pointer to `endgame_zones.py` zone constants.</done>
</task>

<task type="auto">
  <name>Task 2: Extend §3.3.1 with clock-gap-% submetric</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (the existing §3.3.1 section header and surrounding CTE patterns — locate the time-management section the skill already documents)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md (§Q4 "Additionally, §3.3.1 should add a new clock-gap-% submetric" paragraph; §Q1 distribution table showing skew ≈ -0.05 to -0.09 across TCs)
  </read_first>
  <action>
    Within the existing §3.3.1 section of SKILL.md, append a new submetric subsection titled `#### clock-gap-%` (one heading depth deeper than §3.3.1). Use the same submetric structure already present in §3.3.1 if any; otherwise mirror §3.3.2's Shape / SQL / Collapse verdict / Output structure.

    Mandatory content elements:
    - **Shape**: per-user mean `(my_clock - opp_clock) / base_clock` at endgame entry, computed per `(user_id, TC, ELO)` cell. Single-dimension metric (no sub-bins). Aggregated across users for the cell value used in Cohen's d.
    - **SQL skeleton**: extend the §3.3.1 standard CTE to per-user mean of `(user_clock - opp_clock) / base_clock` at endgame entry, with the same `selected_users` CTE + `first_endgame` join + clock-routing logic already used by `_compute_clock_pressure`.
    - **Expected verdict**: collapse on both TC and ELO axes (cite the prod-DB distribution evidence from 88-RESEARCH.md §Q1 — skew ≈ -0.05 to -0.09, IQR ≈ ±10-16pp).
    - **Output destination**: `app/services/endgame_zones.py` `ZONE_REGISTRY["clock_gap_pct"]` as a single ZoneSpec (typical_lower, typical_upper, direction="higher_is_better"). Emitted as flat constants `CLOCK_GAP_NEUTRAL_MIN / CLOCK_GAP_NEUTRAL_MAX` in `endgameZones.ts` (same pattern as `NEUTRAL_PCT_THRESHOLD`).
    - **Placeholder note**: state that the initial scaffolded values are `(-0.05, 0.05)` (matches existing `NEUTRAL_PCT_THRESHOLD`) and will be replaced by the benchmark-derived band after running the metric on benchmark DB.

    Do NOT modify any pre-existing §3.3.1 content; only append the new `clock-gap-%` submetric subsection.
  </action>
  <verify>
    <automated>grep -c "clock-gap-%\|CLOCK_GAP_NEUTRAL_MIN\|clock_gap_pct" .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clock-gap-%" .claude/skills/benchmarks/SKILL.md` >= 1.
    - `grep -c "clock_gap_pct\|CLOCK_GAP_NEUTRAL" .claude/skills/benchmarks/SKILL.md` >= 1.
    - The new submetric sits within §3.3.1 (not §3.3.2 or §3.3.3) — verify by line ordering: `clock-gap-%` heading appears before `### §3.3.2`.
    - Pre-existing §3.3.2 and other sections are unchanged: `grep -c "### §3.3.2 time-pressure-vs-performance" .claude/skills/benchmarks/SKILL.md` returns 1 (no duplicate from accidental copy).
  </acceptance_criteria>
  <done>§3.3.1 contains a clock-gap-% submetric with the per-user mean shape, the SQL skeleton anchor, expected collapse verdict, and the `ZONE_REGISTRY["clock_gap_pct"]` output destination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change to a developer-facing skill file. No code, no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-02-01 | Information Disclosure | Skill SQL references prod-DB column names | accept | Column names are not secrets; the skill is already a developer reference exposing the same schema |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep -c "### §3.3.3 chess-score-per-pressure-bin" .claude/skills/benchmarks/SKILL.md` returns 1.
- `grep -c "clock-gap-%" .claude/skills/benchmarks/SKILL.md` >= 1.
- File still parses as valid markdown (no broken heading hierarchy): `awk '/^#/{print NR, $0}' .claude/skills/benchmarks/SKILL.md | head -40` shows monotonic depth changes.
</verification>

<success_criteria>
- Two new subchapter additions exist; pre-existing content unchanged.
- Skill is runnable: a downstream user invoking `/benchmarks` can locate the new §3.3.3 SQL skeleton and produce the 4×5 Q1/Q3 + 5 per-quintile collapse verdicts.
- §3.3.1 documents the clock-gap-% submetric with expected-collapse rationale.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-02-SUMMARY.md` when done.
</output>
</content>
</invoke>