---
phase: 88-time-pressure-stats-rework
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/endgame_repository.py
  - app/services/endgame_service.py
  - app/services/score_confidence.py
  - app/schemas/endgames.py
  - app/services/insights_service.py
  - tests/services/test_time_pressure_service.py
  - tests/services/test_score_confidence.py
  - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
autonomous: true
gap_closure: true
requirements:
  - POLISH-01
must_haves:
  truths:
    - "query_cohort_clock_rows is removed from app/repositories/endgame_repository.py (no global unfiltered cohort fetch)"
    - "_compute_cohort_lookup and all cohort_rows/cohort_lookup plumbing is removed from app/services/endgame_service.py"
    - "_iterate_clock_rows (or its successor) builds both user_quintile_wdl[tc][q] AND opp_quintile_wdl[tc][q] in a single pass over query_clock_stats_rows output (same filtered games, independent quintile splits by user vs opponent clock-pct)"
    - "_build_quintile_bullets computes delta = user_score_in_Q − opp_score_in_Q using compute_score_difference_test (unpaired two-sample Wilson on the two independent quintile splits)"
    - "Each PressureQuintileBullet's n-gate is min(n_user_in_Q, n_opp_in_Q) >= MIN_GAMES_PER_PRESSURE_BIN (the unused constant from WR-05 is now wired)"
    - "PressureQuintileBullet schema renames cohort_score → opp_score (backend Pydantic); docstring describes opponent same-game opp-quintile reference, not mirror-bucket cohort"
    - "GET /api/endgames/overview returns time_pressure_cards with opp_score field per quintile (not cohort_score) for a user with games"
    - "Dead constants MIN_GAMES_FOR_CLOCK_STATS / NUM_BUCKETS / BUCKET_WIDTH_PCT / CLOCK_PRESSURE_TIMELINE_WINDOW are removed from endgame_service.py (IN-01)"
    - "compose_findings no longer appends _finding_clock_diff_timeline / _finding_time_pressure_vs_performance; the LLM prompt assembler does not emit those two empty subsections (WR-06)"
    - "compute_score_delta_vs_reference and _wilson_score_test_vs_ref are removed from score_confidence.py (no longer used after cohort layer drop)"
    - "88-CONTEXT.md carries a D-07 (or D-05-revised) entry that explicitly retires D-05 and records the same-game opp-quintile design"
  artifacts:
    - path: "app/repositories/endgame_repository.py"
      provides: "query_clock_stats_rows only (no cohort query)"
      contains: "query_clock_stats_rows"
    - path: "app/services/endgame_service.py"
      provides: "_compute_time_pressure_cards using opp-quintile split (no cohort_lookup param)"
      contains: "opp_quintile_wdl"
    - path: "app/schemas/endgames.py"
      provides: "PressureQuintileBullet with opp_score field"
      contains: "opp_score"
    - path: ".planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md"
      provides: "D-07 retiring D-05; documents same-game opp-quintile design"
      contains: "D-07"
  key_links:
    - from: "_iterate_clock_rows"
      to: "_build_quintile_bullets"
      via: "user_quintile_wdl + opp_quintile_wdl tuple return"
      pattern: "opp_quintile_wdl"
    - from: "_build_quintile_bullets"
      to: "compute_score_difference_test"
      via: "two-sample Wilson on user vs opp WDL counts in matching quintile"
      pattern: "compute_score_difference_test"
---

<objective>
Close CR-01 (and the dependent warnings WR-01, WR-04 backend side, WR-05, WR-06; info IN-01, IN-04) by deleting the global cohort layer and replacing it with a same-game opponent-quintile split. The user's resolution direction (88-VERIFICATION.md lines 177-220) is binding: compare user vs the user's own opponents on the same filtered games, but bucket each side by its OWN clock quintile, then compute delta = user_score_in_Q − opp_score_in_Q with the existing two-sample test.

Purpose: removes the unfiltered global query that triggers OOM risk (CR-01), removes the small-N cohort cell spurious-signal failure (WR-01), wires the unused MIN_GAMES_PER_PRESSURE_BIN (WR-05), drops dead legacy constants (IN-01), resolves the cohort docstring contradiction with CONTEXT D-05 by retiring D-05 (IN-04), and removes the silent empty-finding regression on the LLM prompt (WR-06).

Output: schema rename `cohort_score → opp_score`, opp-quintile parallel structure in `_iterate_clock_rows`, all cohort plumbing removed, CONTEXT amended with D-07 retiring D-05, tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-VERIFICATION.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-REVIEW.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-08-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Reusable math helper. Returns (p_value, ci_low, ci_high) — caller computes the delta. -->

From app/services/score_confidence.py:
```python
def compute_score_difference_test(
    eg_w: int, eg_d: int, eg_l: int, eg_n: int,
    ne_w: int, ne_d: int, ne_l: int, ne_n: int,
) -> tuple[float | None, float | None, float | None]:
    """Independent two-sample z-test on chess-score difference between two
    WDL cohorts. Returns (p_value, ci_low, ci_high).
    H0: score_eg - score_ne == 0 where score_i = (w + 0.5*d) / n.
    N-gates:
      - both n>0 required for any non-None output.
      - min_n < 2: ci_low/ci_high = None.
      - min_n < CONFIDENCE_MIN_N(=10): p_value = None.
    """
```

From app/services/endgame_service.py (signature stays; cohort_lookup param dropped):
```python
def _iterate_clock_rows(rows) -> tuple[
    dict[str, int],                              # tc_total
    dict[str, list[float]],                      # tc_clock_diffs
    dict[tuple[str, int], tuple[int, int, int]], # tc_user_quintile_wdl (renamed from tc_quintile_wdl)
    dict[tuple[str, int], tuple[int, int, int]], # tc_opp_quintile_wdl (NEW)
]: ...

def _build_quintile_bullets(
    tc: str,
    user_quintile_wdl: dict[tuple[str, int], tuple[int, int, int]],
    opp_quintile_wdl: dict[tuple[str, int], tuple[int, int, int]],
) -> list[PressureQuintileBullet]: ...

def _compute_time_pressure_cards(rows) -> TimePressureCardsResponse: ...
    # cohort_lookup parameter removed entirely
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend service rework — drop cohort layer, add opp-quintile split, rename schema</name>
  <files>
    app/services/endgame_service.py,
    app/repositories/endgame_repository.py,
    app/schemas/endgames.py,
    app/services/score_confidence.py,
    tests/services/test_time_pressure_service.py,
    tests/services/test_score_confidence.py
  </files>
  <read_first>
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-VERIFICATION.md (read lines 177-220 "Resolution Direction" — the design is locked there; this is the binding spec, overrides 88-CONTEXT.md D-05)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-REVIEW.md (CR-01, WR-01, WR-05, WR-06, IN-01, IN-04)
    - app/repositories/endgame_repository.py lines 878-943 (query_cohort_clock_rows — to delete) and lines around query_clock_stats_rows (the surviving query)
    - app/services/endgame_service.py lines 1240-1300 (constants block — drop IN-01 dead constants), lines 1451-1680 (_iterate_clock_rows + _build_quintile_bullets + _compute_time_pressure_cards + _compute_cohort_lookup), lines 2410-2420 (compose_endgame_overview cohort wiring at line 2415-2417)
    - app/schemas/endgames.py lines 615-672 (PressureQuintileBullet, TimePressureTcCard, TimePressureCardsResponse)
    - app/services/score_confidence.py lines 124-141 (_wilson_score_test_vs_ref, private helper that becomes unused) and lines 339-388 (compute_score_delta_vs_reference, single-caller helper that becomes unused) and lines 186-266 (compute_score_difference_test — the helper to REUSE)
    - app/services/insights_service.py lines 395-410 (compose_findings call-site) and lines 952-977 (_finding_clock_diff_timeline + _finding_time_pressure_vs_performance — to delete)
    - tests/services/test_time_pressure_service.py (all existing tests assume cohort_lookup; full rewrite of inputs needed)
    - tests/services/test_score_confidence.py lines 551-680 (compute_score_delta_vs_reference tests — to delete entirely)
    - app/services/endgame_service.py imports block around line 76 (imports compute_score_delta_vs_reference + compute_score_difference_test from score_confidence)
  </read_first>
  <behavior>
    Pre-implementation tests (write FIRST in tests/services/test_time_pressure_service.py, run RED):
    - test_user_quintile_and_opp_quintile_are_independent_per_game: a single bullet game where the user spent more clock than the opponent at endgame entry assigns user to a LOW quintile and opp to a HIGH quintile in the parallel structures (same game, two different quintile indices).
    - test_opp_quintile_wdl_uses_inverted_result: a game where the user won (`1-0` with user_color="white") accumulates as a win in user_quintile_wdl AND as a loss in opp_quintile_wdl (same game, opposite outcomes).
    - test_quintile_bullet_delta_is_zero_when_user_and_opp_score_equal_in_same_quintile: a balanced set where both sides have identical WDL in matching quintile Q yields delta=0.0.
    - test_quintile_bullet_returns_none_stats_when_min_n_gate_unmet: if min(n_user_in_Q, n_opp_in_Q) < MIN_GAMES_PER_PRESSURE_BIN, the bullet emits with delta=0.0 (or the raw delta if both sides nonzero) and all stats None — gate prevents spurious signal.
    - test_no_call_to_query_cohort_clock_rows: pytest-mocker confirms `query_cohort_clock_rows` is removed from `endgame_repository.py` namespace (import error or AttributeError on lookup).
    - test_compute_time_pressure_cards_signature: function signature is `_compute_time_pressure_cards(clock_rows)` — no second arg.
  </behavior>
  <action>
    Implement the design from VERIFICATION.md lines 177-220 (binding):

    1. **Delete `query_cohort_clock_rows`** from `app/repositories/endgame_repository.py` (lines 878-943 inclusive). No replacement query; the surviving `query_clock_stats_rows` already returns all the rows needed for both user-side and opp-side bucketing.

    2. **Rewrite `_iterate_clock_rows`** in `app/services/endgame_service.py` so it returns FOUR structures: `tc_total`, `tc_clock_diffs`, `tc_user_quintile_wdl`, `tc_opp_quintile_wdl`. For each row:
       - Compute `user_clk_pct = user_clock / base_time_seconds` and `opp_clk_pct = opp_clock / base_time_seconds` (both clamped through the existing MAX_CLOCK_PCT_OF_BASE guard).
       - Assign `user_quintile = min(4, int(user_clk_pct * 5))` and `opp_quintile = min(4, int(opp_clk_pct * 5))` independently.
       - Derive the user's outcome via `derive_user_result(result, user_color)` (existing helper). For the opponent, invert: win→loss, loss→win, draw→draw.
       - Accumulate into `tc_user_quintile_wdl[(tc, user_quintile)]` and `tc_opp_quintile_wdl[(tc, opp_quintile)]` respectively (same game contributes once to each).

    3. **Delete `_compute_cohort_lookup`** entirely (lines 1621-1677 in current endgame_service.py). Delete the import of `query_cohort_clock_rows` and the call site in `compose_endgame_overview` at line 2415-2416 (`cohort_rows = ...`, `cohort_lookup = _compute_cohort_lookup(cohort_rows)`); pass nothing in its place to `_compute_time_pressure_cards`.

    4. **Rewrite `_build_quintile_bullets`** with the new signature `(tc, user_quintile_wdl, opp_quintile_wdl)`. For each quintile q in 0..4:
       - Read `(u_w, u_d, u_l) = user_quintile_wdl.get((tc, q), (0,0,0))`, compute `n_user = u_w + u_d + u_l`.
       - Read `(o_w, o_d, o_l) = opp_quintile_wdl.get((tc, q), (0,0,0))`, compute `n_opp = o_w + o_d + o_l`.
       - If `min(n_user, n_opp) < MIN_GAMES_PER_PRESSURE_BIN`: emit a PressureQuintileBullet with `n=n_user`, `delta=0.0` (or the computed raw delta if both ns > 0), `p_value=None`, `ci_low=None`, `ci_high=None`, `opp_score=None`.
       - Else: compute `user_score = (u_w + 0.5*u_d) / n_user`, `opp_score = (o_w + 0.5*o_d) / n_opp`, `delta = user_score - opp_score`; call `compute_score_difference_test(u_w, u_d, u_l, n_user, o_w, o_d, o_l, n_opp)` and unpack `(p_value, ci_low, ci_high)`; emit bullet with delta, p_value, ci_low, ci_high, opp_score.

    5. **Rewrite `_compute_time_pressure_cards`** so its signature is `(clock_rows) -> TimePressureCardsResponse` — the `cohort_lookup` parameter is removed. Inside, unpack the four structures from `_iterate_clock_rows` and pass `user_quintile_wdl` + `opp_quintile_wdl` into `_build_quintile_bullets`.

    6. **Rename schema field** `PressureQuintileBullet.cohort_score: float | None` → `PressureQuintileBullet.opp_score: float | None` in `app/schemas/endgames.py` (line 637). Update the class docstring (lines 616-628) to describe the new design: "opp_score is the opponent's same-game score in the matching opponent-clock quintile; delta = user_score - opp_score where each side is bucketed by its own clock-pct at endgame entry, independently of the other." Drop the "mirror-bucket cohort" framing.

    7. **Delete `compute_score_delta_vs_reference`** (score_confidence.py lines 339-388) and **delete `_wilson_score_test_vs_ref`** (lines 124-141). Drop the corresponding test class `TestComputeScoreDeltaVsReference` from `tests/services/test_score_confidence.py` (lines ~551-680). Update the import in `app/services/endgame_service.py` line 76 to drop `compute_score_delta_vs_reference` (keep `compute_score_difference_test`).

    8. **Delete dead constants from endgame_service.py (IN-01)**: `MIN_GAMES_FOR_CLOCK_STATS` (line 1254), `NUM_BUCKETS` (line 1269), `BUCKET_WIDTH_PCT` (line 1270), `CLOCK_PRESSURE_TIMELINE_WINDOW` (line 1291). Grep-confirm none are referenced elsewhere before deletion.

    9. **WR-06 fix (LLM prompt regression)**: in `app/services/insights_service.py`, remove the two lines `findings.append(_finding_clock_diff_timeline(...))` and `findings.append(_finding_time_pressure_vs_performance(...))` from `compose_findings` (lines 401-402). Delete the two helper functions `_finding_clock_diff_timeline` and `_finding_time_pressure_vs_performance` (lines 952-977). Also remove the corresponding tuples `("subsection", "clock_diff_timeline")` and `("chart", "time_pressure_vs_performance")` from the `time_pressure` section block in `app/services/insights_llm.py` (lines 1574-1575). Leave `_TIMELINE_SUBSECTION_IDS` and `endgame_zones.py` neutral-band constants alone (harmless dead config; out of gap-closure scope). Add a one-line comment at each deletion site referencing this change: `# Phase 88.1: removed silent empty-finding regression (REVIEW.md WR-06).`

    10. **Update `tests/services/test_time_pressure_service.py`**: rewrite the test fixtures and assertions to match the new design. The `_empty_cohort` and `_cohort_with` helpers are no longer needed; `_compute_time_pressure_cards(rows)` takes a single argument. All existing tests that asserted on `cohort_score` should assert on `opp_score`. Add the new behavior tests from `<behavior>` above.

    11. Run `uv run ruff check . && uv run ruff format . && uv run ty check app/ tests/` and fix any issues. Run `uv run pytest -x -q` and fix any breakages. Make sure no `compute_score_delta_vs_reference`, `_wilson_score_test_vs_ref`, `query_cohort_clock_rows`, `_compute_cohort_lookup`, `cohort_lookup`, or `cohort_rows` references remain in `app/` or `tests/` after the rewrite.

    12. **Amend `88-CONTEXT.md`** by appending a new section after the existing `<decisions>` block (or inside it, after D-06) documenting the design pivot:

        ```markdown
        ### D-07 (revised cohort design — supersedes D-05; LOCKED 2026-05-17 post-verification)

        The mirror-bucket cohort framing locked in D-05 is retired. The Phase 88
        implementation (88-01..88-08) shipped an unfiltered global cohort query
        that violated D-05 doctrine and risked OOM at production scale (see
        88-VERIFICATION.md CR-01). The Phase 88.1 gap closure replaces the entire
        cohort layer with a same-game opponent-quintile split:

        - **Comparison set:** the user's own filtered games. No cross-user query.
        - **Two parallel quintile splits within the same game-set:**
          - `user_quintile_wdl[tc][q]` — user WDL bucketed by USER's clock-pct at endgame entry.
          - `opp_quintile_wdl[tc][q]` — opponent WDL bucketed by OPPONENT's clock-pct
            at endgame entry, with inverted result (user-win = opp-loss).
        - **Delta:** `delta = user_score_in_Q − opp_score_in_Q` (each side scored
          from its own quintile of the same filtered game-set).
        - **Significance test:** unpaired two-sample Wilson via existing
          `compute_score_difference_test` (the two splits are independent because
          user and opp clocks fall in different quintiles within the same game).
        - **n-gate:** `min(n_user_in_Q, n_opp_in_Q) >= MIN_GAMES_PER_PRESSURE_BIN`.
          Subsumes the small-N cohort cell gate flagged in REVIEW.md WR-01.
        - **Schema:** `PressureQuintileBullet.cohort_score` → `opp_score`.
        - **Popover copy:** "vs cohort" → "vs opponent".

        D-02's per-(TC, quintile) neutral band stays valid as a band on
        `user_score − opp_score` — the band shape is independent of which reference
        is subtracted. A sanity recalibration runs as Plan 88-12 to confirm the
        existing values still apply; expect the ±0.06 editorial cap to dominate
        as before. The retired global cohort query, the `compute_score_delta_vs_reference`
        helper, and the `_compute_cohort_lookup` aggregator are all deleted.
        ```
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_time_pressure_service.py tests/services/test_score_confidence.py tests/services/test_endgame_service.py -x -q ; uv run ty check app/ tests/ ; grep -v '^#' app/repositories/endgame_repository.py app/services/endgame_service.py app/services/score_confidence.py | grep -cE 'query_cohort_clock_rows|_compute_cohort_lookup|compute_score_delta_vs_reference|_wilson_score_test_vs_ref|cohort_lookup|cohort_rows'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_time_pressure_service.py tests/services/test_score_confidence.py -x -q` exits 0.
    - `uv run pytest -x -q` (full backend suite) exits 0.
    - `uv run ty check app/ tests/` exits 0.
    - `grep -v '^#' app/repositories/endgame_repository.py | grep -c 'query_cohort_clock_rows'` returns 0.
    - `grep -v '^#' app/services/endgame_service.py | grep -cE '_compute_cohort_lookup|cohort_lookup|cohort_rows|compute_score_delta_vs_reference'` returns 0.
    - `grep -v '^#' app/services/score_confidence.py | grep -cE 'compute_score_delta_vs_reference|_wilson_score_test_vs_ref'` returns 0.
    - `grep -v '^#' app/services/endgame_service.py | grep -cE '^MIN_GAMES_FOR_CLOCK_STATS|^NUM_BUCKETS|^BUCKET_WIDTH_PCT|^CLOCK_PRESSURE_TIMELINE_WINDOW'` returns 0 (dead constants gone).
    - `grep -c 'opp_score' app/schemas/endgames.py` >= 1; `grep -v '^#' app/schemas/endgames.py | grep -c 'cohort_score'` returns 0.
    - `grep -c 'MIN_GAMES_PER_PRESSURE_BIN' app/services/endgame_service.py` >= 2 (constant declaration + at least one consumer use — WR-05 wired).
    - `grep -v '^#' app/services/insights_service.py | grep -cE '_finding_clock_diff_timeline|_finding_time_pressure_vs_performance'` returns 0.
    - `grep -v '^#' app/services/insights_llm.py | grep -cE '\(.subsection., .clock_diff_timeline.\)|\(.chart., .time_pressure_vs_performance.\)'` returns 0.
    - `grep -c 'D-07' .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md` >= 1.
    - `grep -c 'supersedes D-05' .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md` >= 1.
  </acceptance_criteria>
  <done>Cohort layer fully removed, opp-quintile split shipped, schema renamed, dead code deleted, WR-06 LLM prompt regression closed, CONTEXT amended with D-07 retiring D-05. Frontend will follow in Plan 88-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/endgames/overview | User-supplied filter args flow into query_clock_stats_rows (existing boundary, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-09-01 | Denial of Service | `/api/endgames/overview` | mitigate | Removing `query_cohort_clock_rows` eliminates the unfiltered global query and the OOM risk lineage (CLAUDE.md Phase 41.1 / FLAWCHESS-3Q). The surviving `query_clock_stats_rows` is already filter-responsive. |
| T-88-09-02 | Information Disclosure | LLM prompt assembler | accept | The two empty findings being dropped carry no user data. The prompt-version is unchanged because the prose-relevant subsections in `endgame_insights.md` already tolerate missing subsections (existing A2/A5 filters). |
| T-88-09-03 | Tampering | Test refactor in test_time_pressure_service.py | mitigate | Behavior tests written BEFORE rewriting `_iterate_clock_rows` (tdd="true"); RED→GREEN cycle ensures the new structures behave as documented. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `uv run pytest -x -q` (full backend suite) exits 0.
- `uv run ty check app/ tests/` exits 0.
- Backend grep gates above all clean.
- 88-CONTEXT.md contains D-07 retiring D-05.
</verification>

<success_criteria>
- CR-01 closed: no cross-user query of any kind runs on `/api/endgames/overview`.
- WR-01 closed: small-N spurious signals impossible (no cohort cell to be small).
- WR-05 closed: `MIN_GAMES_PER_PRESSURE_BIN` is referenced as the n-gate inside `_build_quintile_bullets`.
- WR-06 closed: empty findings for `clock_diff_timeline` and `time_pressure_vs_performance` no longer appended to the LLM prompt.
- IN-01 closed: four dead legacy constants removed.
- IN-04 closed (moot): cohort docstring contradiction eliminated by deletion.
- D-05 retired in CONTEXT.md with D-07 documenting the same-game opp-quintile design.
- Backend math correctness preserved (existing two-sample test, well-tested precedent from Phase 85.1).
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-09-SUMMARY.md` when done.
</output>
</content>
