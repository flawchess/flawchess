---
phase: 88-time-pressure-stats-rework
plan: 11
type: execute
wave: 3
depends_on:
  - 88-09
  - 88-10
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/EndgameTimePressureSection.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
autonomous: true
gap_closure: true
requirements:
  - POLISH-01
  - POLISH-03
must_haves:
  truths:
    - "PressureQuintileBullet TS type renames cohort_score → opp_score; type matches backend Pydantic shape shipped in Plan 88-09"
    - "EndgameTimePressureCard popover copy reads 'vs opponent' (not 'vs cohort'); the methodology line reads 'delta = user_score − opp_score' (not 'user_score − cohort_score')"
    - "EndgameTimePressureCard imports MIN_GAMES_PER_TC_CARD, MIN_GAMES_PER_PRESSURE_BIN, and getPressureBinBand from @/generated/endgameZones (the three local declarations/inline assertions are gone)"
    - "EndgameTimePressureCard QuintileRow uses getPressureBinBand(tc, bin.quintile_index) for band lookup; the `!` non-null assertion and the `as 0|1|2|3|4` cast at line 140 are removed"
    - "EndgameTimePressureSection's aria-labelledby is consistent — either the attribute is removed and replaced with aria-label, OR Endgames.tsx adds id=\"time-pressure-heading\" to the <h2>Time Pressure</h2> at line 532"
    - "EndgameTimePressureSection.test.tsx asserts the ARIA wiring (catches IN-05 regression)"
    - "Frontend test suite stays green; tsc --noEmit clean; knip clean"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "PressureQuintileBullet.opp_score: number | null"
      contains: "opp_score"
    - path: "frontend/src/components/charts/EndgameTimePressureCard.tsx"
      provides: "QuintileRow with getPressureBinBand lookup and 'vs opponent' popover copy"
      contains: "getPressureBinBand"
    - path: "frontend/src/components/charts/EndgameTimePressureSection.tsx"
      provides: "Consistent ARIA labelling (no dangling reference)"
      contains: "aria-label"
  key_links:
    - from: "EndgameTimePressureCard"
      to: "@/generated/endgameZones"
      via: "import { MIN_GAMES_PER_TC_CARD, MIN_GAMES_PER_PRESSURE_BIN, getPressureBinBand }"
      pattern: "from '@/generated/endgameZones'"
    - from: "PressureQuintileBullet TS"
      to: "PressureQuintileBullet Pydantic (Plan 88-09 schema)"
      via: "API response JSON; opp_score field name parity"
      pattern: "opp_score"
---

<objective>
Mirror Plan 88-09's schema rename on the frontend, consume the Plan 88-10 codegen exports to eliminate cross-stack drift and the unsafe non-null assertion, fix the broken `aria-labelledby` reference (WR-02), and add the missing ARIA-wiring test (IN-05). Update popover copy from "vs cohort" to "vs opponent" everywhere (the user is now genuinely comparing against the same-game opponent, not a global cohort).

Purpose: closes WR-02 (broken ARIA labelling), WR-03 (unsafe `!` non-null on Record index access, CLAUDE.md `noUncheckedIndexedAccess` violation), WR-04 frontend side (drift risk via local literals), IN-05 (test gap), IN-06 (typed helper consumer).

Output: 6 frontend files modified; type, copy, ARIA, and narrowing aligned with the new opp-quintile design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-VERIFICATION.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- From Plan 88-09 (backend, completed before this plan runs): -->
<!-- Pydantic PressureQuintileBullet now carries opp_score: float | None instead of cohort_score -->

<!-- From Plan 88-10 (codegen, completed before this plan runs): -->
From frontend/src/generated/endgameZones.ts:
```ts
export const MIN_GAMES_PER_TC_CARD: number;
export const MIN_GAMES_PER_PRESSURE_BIN: number;
export function getPressureBinBand(
  tc: 'bullet' | 'blitz' | 'rapid' | 'classical',
  quintile: number,
): { min: number; max: number } | null;
```

<!-- Current code that must change (from EndgameTimePressureCard.tsx lines 22-26, 36, 40-43, 140-156, 182-200): -->
<!-- The unsafe pattern at line 140: -->
<!-- `const neutralBand = PRESSURE_BIN_SCORE_NEUTRAL_ZONES[tc][bin.quintile_index as 0 | 1 | 2 | 3 | 4]!;` -->
<!-- The popover at line 182-200 references "cohort_score" and "vs cohort" copy. -->
<!-- Local literals at lines 40, 43: `MIN_GAMES_PER_TC_CARD = 20; MIN_GAMES_PER_PRESSURE_BIN = 5;` -->

<!-- Current state in EndgameTimePressureSection.tsx line 22: -->
<!-- `aria-labelledby="time-pressure-heading"` — but no element with that id exists. -->

<!-- The <h2> in Endgames.tsx:532 is `<h2 className="text-lg font-semibold text-foreground mt-2">Time Pressure</h2>` (no id). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename opp_score in TS types + update card consumer copy and band lookup</name>
  <files>
    frontend/src/types/endgames.ts,
    frontend/src/components/charts/EndgameTimePressureCard.tsx,
    frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
  </files>
  <read_first>
    - frontend/src/types/endgames.ts lines 220-256 (current PressureQuintileBullet definition with cohort_score)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (entire file — especially lines 22-50 for imports + local constants, lines 134-220 for QuintileRow with the unsafe `!` pattern and the cohort_score popover copy)
    - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx (existing assertions on cohort_score / "vs cohort" copy that must be retargeted)
    - frontend/src/generated/endgameZones.ts (output of Plan 88-10 — confirm MIN_GAMES_PER_TC_CARD, MIN_GAMES_PER_PRESSURE_BIN, and getPressureBinBand are exported before starting)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-REVIEW.md WR-03, WR-04, IN-06
    - CLAUDE.md "noUncheckedIndexedAccess" + "text-sm floor" + "data-testid on every interactive element" rules
  </read_first>
  <behavior>
    Pre-implementation tests (write FIRST in EndgameTimePressureCard.test.tsx, run RED):
    - test_quintile_row_uses_opp_score_field: a card rendered with `opp_score: 0.48` in a quintile bin shows "48.0%" as the baseline label in the popover (not "n/a").
    - test_popover_copy_says_opponent_not_cohort: render the card, open the score-delta popover for any quintile bin, assert popover contains "vs. opponent" or "opponent score" (case-insensitive), and does NOT contain the substring "cohort".
    - test_quintile_row_handles_out_of_range_quintile_gracefully: a malformed `bin` with `quintile_index = 5` produces a `null` band lookup (getPressureBinBand returns null) and the row returns null / renders no glyph (no TypeError thrown).
    - test_card_consumes_codegen_constants: the card no longer declares MIN_GAMES_PER_TC_CARD or MIN_GAMES_PER_PRESSURE_BIN locally — confirmed via static-source assertion (`expect(EndgameTimePressureCard.toString()).not.toMatch(/const MIN_GAMES_PER_TC_CARD = 20/)`) OR via grep gate in the verify command.

    Update existing tests that asserted on `cohort_score` / `cohortPct` / "vs cohort" copy to use `opp_score` and "vs opponent" copy.
  </behavior>
  <action>
    1. **Rename TS type** in `frontend/src/types/endgames.ts` line 225-234:
       - Change `cohort_score: number | null;  // reference line (live mirror-bucket)` to `opp_score: number | null;  // opponent's same-game score in the matching opp-clock quintile`.
       - Update the inline comment on line 229 from `delta: number; // user_score - cohort_score` to `delta: number; // user_score - opp_score (independent quintile splits per side)`.
       - Drop the "Mirror-bucket peer" reference and "live mirror-bucket" framing from the file-level comment (line 222) — replace with a single line noting the Phase 88.1 opp-quintile design with a pointer to 88-CONTEXT.md D-07.

    2. **Update `EndgameTimePressureCard.tsx`**:
       - Replace the imports at lines 22-26 to ALSO bring in `MIN_GAMES_PER_TC_CARD`, `MIN_GAMES_PER_PRESSURE_BIN`, `getPressureBinBand` from `@/generated/endgameZones`. The `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`, `CLOCK_GAP_NEUTRAL_MIN`, `CLOCK_GAP_NEUTRAL_MAX` named imports stay.
       - **Delete** the local declarations at lines 40 and 43: `const MIN_GAMES_PER_TC_CARD = 20;` and `const MIN_GAMES_PER_PRESSURE_BIN = 5;`. Keep the explanatory file-header comment but trim the line about "mirror backend gate" — the codegen does that now.
       - In `QuintileRow` (around line 140), replace:
         ```ts
         const neutralBand = PRESSURE_BIN_SCORE_NEUTRAL_ZONES[tc][bin.quintile_index as 0 | 1 | 2 | 3 | 4]!;
         ```
         with:
         ```ts
         const neutralBand = getPressureBinBand(tc, bin.quintile_index);
         if (!neutralBand) return null;
         ```
         The early-return guards against malformed `quintile_index` (CLAUDE.md `noUncheckedIndexedAccess` compliance).
       - Around line 155-156, replace the `cohortPct` derivation:
         ```ts
         const cohortPct = bin.cohort_score != null ? `${(bin.cohort_score * 100).toFixed(1)}%` : 'n/a';
         ```
         with:
         ```ts
         const oppPct = bin.opp_score != null ? `${(bin.opp_score * 100).toFixed(1)}%` : 'n/a';
         ```
       - Around lines 182-200, update the popover wiring:
         - `explanation` (line 183): change to `Your score vs. your opponents' score when you had ${bin.quintile_label} of your clock remaining at endgame entry, compared against the matching opponent-clock quintile in the same games. Positive = you outperformed your opponents.`
         - `baselineLabel={cohortPct}` → `baselineLabel={oppPct}`
         - `methodology` (lines 195-199): replace with prose describing the new design — exact wording (concise, no jargon per `feedback_popover_copy_minimalism.md`):
           ```
           delta = user_score − opp_score (W+0.5D/N).
           Each side bucketed by its own clock remaining at endgame entry.
           Test: independent two-sample test; same filtered games.
           CI: 95% normal-approximation on the difference.
           ```
       - Update the `data-testid` strings if any embed the literal "cohort" (likely none, but grep-confirm).

    3. **Run the existing card test**: `cd frontend && npm test -- src/components/charts/__tests__/EndgameTimePressureCard.test.tsx --run`. Fix failing assertions by retargeting from cohort_score → opp_score and "vs cohort" → "vs opponent".

    4. **Mobile and design check (visual-only)**: confirm popover prose stays within reasonable width on 375px viewport. Do not change layout; just confirm the new methodology text doesn't visibly overflow. (Per CLAUDE.md "Apply changes to mobile too" — there are no separate desktop/mobile renderers in this card, so the single render path covers both.)

    5. Run `cd frontend && npm run lint && npx tsc --noEmit -p tsconfig.app.json && npm run knip` — fix any issues.
  </action>
  <verify>
    <automated>grep -v '^//' frontend/src/types/endgames.ts frontend/src/components/charts/EndgameTimePressureCard.tsx | grep -cE 'cohort_score|cohortPct' ; grep -c "getPressureBinBand" frontend/src/components/charts/EndgameTimePressureCard.tsx ; grep -cE "\] as 0 \| 1 \| 2 \| 3 \| 4\]!" frontend/src/components/charts/EndgameTimePressureCard.tsx ; grep -cE "^const MIN_GAMES_PER_TC_CARD = 20|^const MIN_GAMES_PER_PRESSURE_BIN = 5" frontend/src/components/charts/EndgameTimePressureCard.tsx ; cd frontend && npm test -- src/components/charts/__tests__/EndgameTimePressureCard.test.tsx --run && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' frontend/src/types/endgames.ts frontend/src/components/charts/EndgameTimePressureCard.tsx | grep -cE 'cohort_score|cohortPct'` returns 0 (the rename is complete; only doc/comment lines may mention cohort historically, and even those should be updated).
    - `grep -c "getPressureBinBand" frontend/src/components/charts/EndgameTimePressureCard.tsx` >= 1.
    - `grep -cE "\] as 0 \| 1 \| 2 \| 3 \| 4\]!" frontend/src/components/charts/EndgameTimePressureCard.tsx` returns 0 (unsafe pattern removed; WR-03 closed).
    - `grep -cE "^const MIN_GAMES_PER_TC_CARD = 20|^const MIN_GAMES_PER_PRESSURE_BIN = 5" frontend/src/components/charts/EndgameTimePressureCard.tsx` returns 0 (local literals removed; codegen exports consumed).
    - `grep -c "opp_score" frontend/src/types/endgames.ts` >= 1.
    - `cd frontend && npm test -- src/components/charts/__tests__/EndgameTimePressureCard.test.tsx --run` exits 0 with all tests passing (including the new behavior tests).
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - `cd frontend && npm run knip` exits 0 (no new dead exports introduced).
  </acceptance_criteria>
  <done>Card consumes the renamed type and codegen exports; unsafe non-null assertion gone; popover copy reads "opponent" not "cohort"; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix broken ARIA and add the missing assertion test</name>
  <files>
    frontend/src/components/charts/EndgameTimePressureSection.tsx,
    frontend/src/pages/Endgames.tsx,
    frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
  </files>
  <read_first>
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (entire file — 44 lines; especially line 22 `aria-labelledby="time-pressure-heading"`)
    - frontend/src/pages/Endgames.tsx lines 529-540 (the `<h2>Time Pressure</h2>` wrapper that should own the id, plus the SectionInsightSlot wiring around it)
    - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx (existing test — find the section where the missing ARIA assertion should land; REVIEW.md IN-05 cites lines 173-178)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-REVIEW.md WR-02 + IN-05
    - CLAUDE.md "Semantic HTML" + "ARIA labels on icon-only buttons" rules
  </read_first>
  <behavior>
    Pre-implementation tests (write FIRST in EndgameTimePressureSection.test.tsx, run RED):
    - test_section_has_consistent_aria_labelling: assert that EITHER (a) the `<section>` element has `aria-label` set to a non-empty string AND no `aria-labelledby` attribute, OR (b) it has `aria-labelledby="time-pressure-heading"` AND `document.getElementById("time-pressure-heading")` returns a non-null element rendered by the section or by its consumer wrapper that the test mounts together.

    Choose ONE implementation and test that exact pattern (the executor picks; the simpler option is (a): `aria-label="Time pressure analysis"` and drop the `aria-labelledby`).
  </behavior>
  <action>
    Pick option **A** (lowest touch — keeps the section self-labelling, no cross-file coordination needed):

    1. In `frontend/src/components/charts/EndgameTimePressureSection.tsx` line 22, replace `aria-labelledby="time-pressure-heading"` with `aria-label="Time pressure analysis"`. Keep `data-testid="time-pressure-cards-section"` unchanged.

    2. In `frontend/src/pages/Endgames.tsx`, **leave the <h2>** at line 532 unchanged (no id needed). The visible `<h2>Time Pressure</h2>` is still the visual section heading; aria-label on the section provides the accessible name for screen readers without depending on a sibling element.

    3. Add the missing test in `frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx` (per REVIEW.md IN-05). Add inside the existing test suite (find the `describe` block that already tests section wrapper presence — line ~173-178 area):
       ```ts
       it('exposes a non-dangling accessible name on the section', () => {
         const { container } = render(<EndgameTimePressureSection data={fakeData} />);
         const section = container.querySelector('[data-testid="time-pressure-cards-section"]')!;
         // After Phase 88.1 WR-02 fix: section uses aria-label (self-contained), not aria-labelledby.
         expect(section.getAttribute('aria-label')).toBe('Time pressure analysis');
         expect(section.getAttribute('aria-labelledby')).toBeNull();
       });
       ```

    4. If `fakeData` is not already in scope at that test, reuse the helper that builds a `TimePressureCardsResponse` for adjacent tests in the same file. Do not introduce a new fixture builder if one exists.

    5. Run:
       ```
       cd frontend && npm test -- src/components/charts/__tests__/EndgameTimePressureSection.test.tsx --run
       cd frontend && npx tsc --noEmit -p tsconfig.app.json
       ```

    6. Sanity-grep for any other dangling `time-pressure-heading` references across the frontend:
       ```
       grep -rn 'time-pressure-heading' frontend/src
       ```
       Expect zero matches after the fix.
  </action>
  <verify>
    <automated>grep -c 'aria-label="Time pressure analysis"' frontend/src/components/charts/EndgameTimePressureSection.tsx ; grep -c 'aria-labelledby="time-pressure-heading"' frontend/src/components/charts/EndgameTimePressureSection.tsx ; grep -rn 'time-pressure-heading' frontend/src | wc -l ; cd frontend && npm test -- src/components/charts/__tests__/EndgameTimePressureSection.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-label="Time pressure analysis"' frontend/src/components/charts/EndgameTimePressureSection.tsx` returns exactly 1.
    - `grep -c 'aria-labelledby="time-pressure-heading"' frontend/src/components/charts/EndgameTimePressureSection.tsx` returns 0.
    - `grep -rn 'time-pressure-heading' frontend/src | wc -l` returns 0 (no dangling references remain anywhere in frontend/src).
    - `cd frontend && npm test -- src/components/charts/__tests__/EndgameTimePressureSection.test.tsx --run` exits 0 with the new ARIA assertion passing.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0.
  </acceptance_criteria>
  <done>WR-02 ARIA fix shipped; IN-05 test gap closed; no dangling id references remain.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green check + final knip pass</name>
  <files></files>
  <read_first>
    - CLAUDE.md (the "Commands" block — full backend + frontend test invocation lines)
    - Plan 88-09 SUMMARY.md (once created — to confirm backend rename landed cleanly before frontend full-suite runs depend on it)
    - Plan 88-10 SUMMARY.md (once created — to confirm codegen exports are present)
  </read_first>
  <action>
    1. Run the full backend test suite plus type-check to confirm Plans 88-09 and 88-10 still pass alongside this plan's edits:
       ```
       uv run pytest -x -q
       uv run ty check app/ tests/
       uv run ruff check . && uv run ruff format --check .
       ```

    2. Run the full frontend test suite plus tsc + lint + knip:
       ```
       cd frontend && npm test -- --run
       cd frontend && npx tsc --noEmit -p tsconfig.app.json
       cd frontend && npm run lint
       cd frontend && npm run knip
       ```

    3. Run the codegen drift gate one more time to confirm no accidental divergence:
       ```
       uv run python scripts/gen_endgame_zones_ts.py
       git diff --exit-code frontend/src/generated/endgameZones.ts
       ```

    4. If any failure surfaces that traces back to Plan 88-09 or 88-10's edits (not this plan's), surface it back to the orchestrator rather than patching it here — the gap-closure waves are separated for a reason.

    5. Commit the three frontend files plus the two test files together with a descriptive message: `feat(88.1): rename cohort_score → opp_score, consume codegen exports, fix ARIA`.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run ty check app/ tests/ && cd frontend && npm test -- --run && npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm run knip && cd .. && uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - Full backend suite exits 0.
    - `uv run ty check app/ tests/` exits 0.
    - Full frontend suite exits 0.
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - `cd frontend && npm run knip` exits 0.
    - Codegen drift gate clean.
  </acceptance_criteria>
  <done>End-to-end green light across both stacks; Plan 88-09 / 88-10 / 88-11 ship as a coherent unit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → frontend TS | Renamed `opp_score` field crosses this boundary; mismatch would surface as a runtime "undefined" in the card; the type rename in `endgames.ts` enforces compile-time parity |
| screen reader → DOM ARIA | Section name now self-contained via `aria-label` (no cross-element dependency) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-11-01 | Information Disclosure | Stale field name in TS | mitigate | TS type rename enforces compile-time check; tsc must pass before commit |
| T-88-11-02 | Tampering | `noUncheckedIndexedAccess` bypass via `!` | mitigate | `getPressureBinBand` typed helper replaces the bypass; runtime returns null on malformed indices instead of throwing |
| T-88-11-03 | Repudiation | Accessibility regression in QA testing | mitigate | New IN-05 test asserts ARIA name is non-null and non-dangling |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Both stacks fully green (backend + frontend).
- No `cohort_score` / `cohortPct` references remain in TS source.
- No `! `non-null assertion on `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`.
- No `aria-labelledby="time-pressure-heading"` anywhere in frontend/src.
- Codegen drift gate clean.
</verification>

<success_criteria>
- WR-02 closed: ARIA labelling consistent and tested.
- WR-03 closed: unsafe `!` pattern replaced with `getPressureBinBand` narrowing.
- WR-04 (frontend side) closed: local MIN_GAMES_* literals replaced with codegen imports.
- IN-05 closed: ARIA-wiring test added.
- IN-06 closed: typed helper consumed at the only callsite.
- Schema rename `cohort_score → opp_score` mirrors Plan 88-09 backend cleanly.
- Popover copy ("vs opponent", new methodology block) matches the new design and respects `feedback_popover_copy_minimalism.md`.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-11-SUMMARY.md` when done.
</output>
</content>
