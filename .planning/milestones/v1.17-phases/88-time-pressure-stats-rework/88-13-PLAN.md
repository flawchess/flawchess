---
phase: 88-time-pressure-stats-rework
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/EndgameTimePressureSection.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
  - frontend/src/lib/pressureBulletConfig.ts
  - frontend/src/pages/Endgames.tsx
autonomous: true
requirements: [A-1, A-4, A-5]
tags: [endgame-analytics, time-pressure, frontend-polish, scope-amendment]

must_haves:
  truths:
    - "Each of the up-to-4 Time Pressure TC cards displays as a visually separate charcoal container, not nested inside an outer charcoal wrap."
    - "Each TC card shows 1 Clock Gap bullet + exactly 4 Score-Delta quintile bullets (Q0..Q3); the Q4 (80-100%) row is not rendered."
    - "The 4 visible quintile rows use the new copy: 'High Pressure (0-20%)', 'Medium Pressure (20-40%)', 'Low Pressure (40-60%)', 'Very Low Pressure (60-80%)'."
    - "The quintile bullet axis spans ±30% (PRESSURE_DELTA_DOMAIN = 0.30) instead of the prior ±20%."
    - "Neutral band thresholds (D-02 editorial cap, ±0.06) are unchanged; band paints visually thinner inside the widened axis."
  artifacts:
    - path: "frontend/src/lib/pressureBulletConfig.ts"
      provides: "PRESSURE_DELTA_DOMAIN = 0.30 (was 0.20)"
      contains: "PRESSURE_DELTA_DOMAIN = 0.30"
    - path: "frontend/src/components/charts/EndgameTimePressureCard.tsx"
      provides: "Quintile filter to Q0..Q3; new pressure-label copy; ARIA labels updated"
      contains: "High Pressure"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Time Pressure section without outer charcoal wrap (cards stand alone)"
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgameTimePressureSection"
      via: "no outer charcoal wrapper div"
      pattern: "EndgameTimePressureSection data="
    - from: "EndgameTimePressureCard"
      to: "card.quintiles"
      via: "filter(bin => bin.quintile_index < 4)"
      pattern: "quintile_index < 4|filter.*bin\\.quintile_index"
---

<objective>
Apply three frontend-only refinements to the Phase 88 Time Pressure cards section:

1. **A-1 — Card chrome:** make each TC card display as its own charcoal container by removing the outer charcoal wrap around the section on `Endgames.tsx`. Cards already carry `charcoal-texture` internally (matches the EndgameTypeBreakdownSection convention).
2. **A-4 — 4 visible quintiles + new labels:** hide Q4 (80-100% clock remaining) and rename the displayed labels of Q0..Q3 to "High Pressure (0-20%)", "Medium Pressure (20-40%)", "Low Pressure (40-60%)", "Very Low Pressure (60-80%)". Backend keeps computing all 5 quintiles (per CONTEXT §2 clarification #2); the filter is purely frontend.
3. **A-5 — Wider quintile axis:** bump `PRESSURE_DELTA_DOMAIN` from 0.20 to 0.30 so real-world score deltas >20% are no longer clipped. Neutral band edges (D-02 editorial cap at ±0.06) are unchanged; the colored side-zones grow from ~70% of axis to ~80%, the neutral strip shrinks from 30% to 20% of axis width.

Purpose: ship the user-approved scope amendment locked in `88-CONTEXT.md` §2 (2026-05-17). This is a layout / copy / constants slice — no schema, no backend, no math. CHANGELOG entry must note A-4's relabeling (the prior "0-20%" pure-range labels gave no qualitative cue).

Output: PR-ready frontend changes plus updated tests covering each refinement. Re-verification of Phase 88 follows after Plans 88-13..15 ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-VERIFICATION.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@CLAUDE.md

# Source files to modify
@frontend/src/components/charts/EndgameTimePressureCard.tsx
@frontend/src/components/charts/EndgameTimePressureSection.tsx
@frontend/src/lib/pressureBulletConfig.ts
@frontend/src/pages/Endgames.tsx
@frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
@frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx

<interfaces>
<!-- Card component public surface (extracted from EndgameTimePressureCard.tsx, post-88-12). -->

From frontend/src/types/endgames.ts:226-257:
- `PressureQuintileBullet` has `quintile_index: number` (0..4), `quintile_label: string`, `n`, `delta`, `p_value`, `ci_low`, `ci_high`, `opp_score: number | null`.
- `TimePressureTcCard` has `tc`, `total`, `clock_gap: ClockGapBullet`, `quintiles: PressureQuintileBullet[]` (always 5, ordered Q0..Q4).
- `TimePressureCardsResponse` has `cards: TimePressureTcCard[]`.

From frontend/src/lib/pressureBulletConfig.ts (current):
- `export const PRESSURE_DELTA_CENTER = 0;`
- `export const PRESSURE_DELTA_DOMAIN = 0.20;`  ← changing to 0.30
- `export const CLOCK_GAP_DOMAIN = 0.30;`
- `export function clampDeltaCi(value: number): number`
- `export function pressureDeltaZoneColor(delta, neutralMin, neutralMax): string`

From frontend/src/components/charts/EndgameTimePressureCard.tsx:
- Card renders 1 `ClockGapRow` + maps `card.quintiles` to `EmptyBinRow` (when `bin.n === 0`) or `QuintileRow`.
- Card outer div uses `className="charcoal-texture rounded-md p-4"` (already correct).
- The InfoPopover body in the title (lines 277-280) currently says "Q0 = 0-20% clock remaining (maximum pressure), Q4 = 80-100% (minimum pressure)" — must update for A-4 (Q4 no longer rendered).
- `QuintileRow` uses `bin.quintile_label` in:
  - Visible row label (line 169): `<span>{bin.quintile_label}:</span>`
  - Popover `name` (line 186): `Score Delta (${bin.quintile_label} pressure)`
  - Popover `explanation` (line 187): includes `${bin.quintile_label} of your clock remaining`
  - `MiniBulletChart` `ariaLabel` (line 222): `Score delta at ${bin.quintile_label} pressure: ${signedDelta}`
- `EmptyBinRow` uses `bin.quintile_label` for the dash-label row.

From frontend/src/pages/Endgames.tsx:530-538:
```tsx
{showTimePressureCards && timePressureCardsData && (
  <>
    <h2 className="text-lg font-semibold text-foreground mt-2">Time Pressure</h2>
    <div className="charcoal-texture rounded-md p-4">
      <EndgameTimePressureSection data={timePressureCardsData} />
    </div>
    <SectionInsightSlot sectionId="time_pressure" data={sectionBySection.time_pressure} />
  </>
)}
```
The `<div className="charcoal-texture rounded-md p-4">` wrapper is what A-1 removes.

For reference — analog (EndgameTypeBreakdownSection has NO outer wrap on Endgames.tsx:576-580): cards-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend refinements (A-1 + A-4 + A-5) with tests</name>
  <files>
    frontend/src/lib/pressureBulletConfig.ts,
    frontend/src/components/charts/EndgameTimePressureCard.tsx,
    frontend/src/pages/Endgames.tsx,
    frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx,
    frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
  </files>
  <behavior>
    - **A-5 axis widening:** `PRESSURE_DELTA_DOMAIN` exported from `pressureBulletConfig.ts` equals `0.30`. The `MiniBulletChart` in `QuintileRow` receives `domain={PRESSURE_DELTA_DOMAIN}` (already does — no consumer change needed, just the constant flips). Update the JSDoc comment above the constant to reflect the ±30% rationale (covers real-world deltas >20% per CONTEXT §2 clarification #1).
    - **A-4 visible-quintile filter:** `EndgameTimePressureCard` filters `card.quintiles` to entries with `quintile_index < 4` before mapping to rows. Q4 (index 4) is never rendered as either `EmptyBinRow` or `QuintileRow`. Backend still ships 5 entries; the filter is at the render call site.
    - **A-4 label remap:** introduce a `PRESSURE_LABELS: Record<0|1|2|3, string>` constant inside `EndgameTimePressureCard.tsx` (or co-located helper file) mapping `0 → "High Pressure (0-20%)"`, `1 → "Medium Pressure (20-40%)"`, `2 → "Low Pressure (40-60%)"`, `3 → "Very Low Pressure (60-80%)"`. The visible row label, popover `name`, popover `explanation`, `MiniBulletChart` `ariaLabel`, the dimmed-bullet `data-testid` chip, and the `EmptyBinRow` label ALL use this new label instead of `bin.quintile_label` (the raw "0-20%" range string from the backend). Backend's `quintile_label` field stays in the type — it's just not displayed.
    - **A-4 title popover copy:** update `EndgameTimePressureCard`'s `InfoPopover` body (currently mentions "Q0 = 0-20% ... Q4 = 80-100%") to describe only the 4 displayed quintiles in plain language. Suggested: "How your chess score changes with the clock remaining at endgame entry, from High Pressure (0-20% clock left) to Very Low Pressure (60-80% clock left)." Honest framing: the 80-100% bin is intentionally hidden (low-pressure tail where signal is muted).
    - **A-1 outer-wrap removal:** in `Endgames.tsx` around lines 530-538, replace the `<div className="charcoal-texture rounded-md p-4">` outer wrap with a Fragment or a `<div>` that carries NO `charcoal-texture` styling. Cards retain their own `charcoal-texture` (already on `EndgameTimePressureCard`). Keep the `data-testid` value `time-pressure-cards-section` reachable (it lives on the inner `<section>` in `EndgameTimePressureSection`).
    - **Tests (additions):**
      - `EndgameTimePressureCard.test.tsx`:
        - "renders only 4 quintile rows (Q4 hidden)" — asserts `getByTestId('time-pressure-card-bullet-bin-0-value')` etc. through `bin-3-value` exist and `queryByTestId('time-pressure-card-bullet-bin-4-value')` is null.
        - "uses new pressure labels" — asserts the visible row label text includes "High Pressure (0-20%)" for `quintile_index=0`, "Very Low Pressure (60-80%)" for `quintile_index=3`. Use `getByText` or check label text via the bin testid container.
        - "EmptyBinRow uses new label for hidden-Q4 case" — fixture with `quintile_index=0` and `n=0` shows `High Pressure (0-20%):` followed by `—  no games`.
      - `EndgameTimePressureCard.test.tsx`:
        - "popover for Q0 says 'High Pressure'" — open the popover via fireEvent on `time-pressure-card-bullet-bin-0-info` and assert content includes "High Pressure".
      - `EndgameTimePressureSection.test.tsx` (or a new `Endgames` test if easier):
        - Adjust any existing test fixtures that had 5 visible quintile bullets to expect 4 visible bullets. Update assertions on row counts accordingly.
    - **Tests (assertions to update, not add):**
      - Any existing test asserting `quintile_label` text like "0-20%" in the card body or popover must be updated to the new "High Pressure (0-20%)" form (the raw "0-20%" range is no longer surfaced in UI; it's still on the backend payload).
    - **Out-of-scope guards:**
      - Do NOT modify `app/services/endgame_zones.py` or `frontend/src/generated/endgameZones.ts`. `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` stays as-is (D-02 ±0.06 cap stays — only the axis widens, not the band).
      - Do NOT change `bin.quintile_label` on the backend payload. Frontend ignores it for display but the field remains for API stability.
      - Do NOT modify `EndgameTypeBreakdownSection`, `EndgameTypeCard`, or any other card module — the A-1 fix is strictly the Endgames.tsx outer-wrap removal.
  </behavior>
  <action>
Implement A-1 + A-4 + A-5 in a single task. The three refinements are tightly coupled (same component file dominates) and small enough to commit as one feature change.

**Step 1 — A-5 axis (smallest):** in `frontend/src/lib/pressureBulletConfig.ts`, change `PRESSURE_DELTA_DOMAIN` from `0.20` to `0.30`. Update the JSDoc above the constant to reflect "±30% covers real-world score deltas exceeding ±20% (CONTEXT §2 A-5, 2026-05-17). The ±0.06 D-02 editorial neutral cap stays unchanged; the colored side-zones widen, the neutral strip shrinks." Verify no other module needs to change — the consumer `EndgameTimePressureCard.tsx` already passes `domain={PRESSURE_DELTA_DOMAIN}` to `MiniBulletChart`.

**Step 2 — A-4 label map + filter:** in `EndgameTimePressureCard.tsx`, near the existing `TC_LABELS` constant (line 45), add:
```ts
const PRESSURE_LABELS: Record<0 | 1 | 2 | 3, string> = {
  0: 'High Pressure (0-20%)',
  1: 'Medium Pressure (20-40%)',
  2: 'Low Pressure (40-60%)',
  3: 'Very Low Pressure (60-80%)',
};
```
Add a typed helper `function pressureLabel(quintileIndex: number): string | null` that returns the mapped label for 0..3 and `null` for 4 (or any out-of-range index). Both `QuintileRow` and `EmptyBinRow` consume this helper instead of `bin.quintile_label`. Inside `QuintileRow` and `EmptyBinRow`, if the helper returns `null`, early-return `null` (defense: the parent filter prevents this, but the type-narrowing makes the helper clean).

In the main component (`EndgameTimePressureCard`), change `card.quintiles.map(...)` to `card.quintiles.filter((bin) => bin.quintile_index < 4).map(...)`. Q4 entries never reach the row components.

Replace every consumer of `bin.quintile_label` inside `QuintileRow` (visible row label, popover name, popover explanation, MiniBulletChart ariaLabel) with the new label from the helper. Same for `EmptyBinRow`'s visible row label.

Update the card-title InfoPopover body (currently around lines 277-280, "Q0 = 0-20% clock remaining (maximum pressure), Q4 = 80-100% (minimum pressure).") to: "How your chess score changes with the clock remaining at endgame entry, from High Pressure (0-20% clock left) to Very Low Pressure (60-80% clock left)." Honest about the hidden 80-100% bin: it's intentionally not surfaced (per CONTEXT §2 A-4).

**Step 3 — A-1 outer-wrap removal:** in `frontend/src/pages/Endgames.tsx`, around lines 530-538, change:
```tsx
<>
  <h2 className="text-lg font-semibold text-foreground mt-2">Time Pressure</h2>
  <div className="charcoal-texture rounded-md p-4">
    <EndgameTimePressureSection data={timePressureCardsData} />
  </div>
  <SectionInsightSlot sectionId="time_pressure" data={sectionBySection.time_pressure} />
</>
```
to drop the outer `<div className="charcoal-texture rounded-md p-4">` wrap entirely. Replace with a direct render: `<EndgameTimePressureSection data={timePressureCardsData} />`. Match the EndgameTypeBreakdownSection convention (Endgames.tsx:576-580 has no outer wrap). The cards' internal `charcoal-texture` is what gives the user the "4 separate charcoal containers" effect.

**Step 4 — Tests:** add the assertions in `<behavior>` to the two existing test files. Existing test fixtures that build a `TimePressureTcCard` with 5 quintile entries can stay as-is on the backend side; assertions on rendered row counts must be 4 (not 5) after this plan. Use `getAllByTestId` with a pattern match to count visible quintile rows (e.g. all matching `^time-pressure-card-bullet-bin-\d+-` testids should map to indices 0..3 only).

Add one new test in `EndgameTimePressureCard.test.tsx`:
- `it('hides the Q4 (80-100%) row entirely')` — render a card with 5 quintiles (any data), assert `queryByTestId('time-pressure-card-bullet-bin-4-value')` is null AND `queryByTestId('time-pressure-card-bullet-bin-4-empty')` is null.
- `it('uses High Pressure / Very Low Pressure labels')` — assert the rendered text contains the new labels.

Add one new test in `EndgameTimePressureCard.test.tsx`:
- `it('widens the quintile bullet axis to ±30%')` — import `PRESSURE_DELTA_DOMAIN` and assert it equals `0.30`. (This is a guard against an inadvertent revert; the visual axis test belongs in human-UAT.)

For `EndgameTimePressureSection.test.tsx`: no behavior change in the section orchestrator itself (it still iterates cards). Existing tests should pass unchanged.

For `Endgames.tsx`: search for any existing test fixture mocking `time-pressure-cards-section` and confirm the test-ids still resolve after the wrap removal. If the wrap was being asserted on, remove that assertion.

**Step 5 — verification commands:**
- `cd frontend && npm test -- --run EndgameTimePressureCard.test.tsx EndgameTimePressureSection.test.tsx` (expect green).
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` (expect exit 0).
- `cd frontend && npm run lint` (expect exit 0).
- `cd frontend && npm run knip` (expect exit 0 — no new unused exports).
- `cd frontend && npm run build` (expect exit 0 — production bundle compiles).
- `uv run python scripts/gen_endgame_zones_ts.py --check` (expect "up to date" — this plan does NOT touch the codegen pipeline; running the check confirms no accidental drift).

**Step 6 — CHANGELOG:** append a single bullet under `## [Unreleased]` → `### Changed` in `CHANGELOG.md`:
- "Time Pressure cards: relabel quintile bullets with qualitative names (High / Medium / Low / Very Low Pressure), hide the 80-100% clock-remaining bin (low-signal tail), widen the score-delta bullet axis to ±30%, and split each TC card into its own charcoal container."

Do NOT walk back the bullet-card framing; this is a polish/relabel slice, not the SC #1 walk-back (that lives in 88-15's CHANGELOG entry for the line chart, and 88-14's CHANGELOG for the top-zone stats).

**Out-of-scope confirmations:**
- No backend, no schema, no codegen, no benchmark rerun.
- D-02 ±0.06 neutral band stays. D-07 opp-quintile semantics stay. MIN_GAMES_* gates stay.
- The 4-visible / 5-computed asymmetry is intentional per CONTEXT §2 clarification #2.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm test -- --run EndgameTimePressureCard.test.tsx EndgameTimePressureSection.test.tsx 2>&1 | tail -15 && npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm run knip && grep -c "PRESSURE_DELTA_DOMAIN = 0.30" src/lib/pressureBulletConfig.ts && grep -c "High Pressure (0-20%)" src/components/charts/EndgameTimePressureCard.tsx && grep -c "Very Low Pressure (60-80%)" src/components/charts/EndgameTimePressureCard.tsx && grep -v '^//' src/pages/Endgames.tsx | grep -B1 -A1 "EndgameTimePressureSection data=" | grep -c "charcoal-texture rounded-md p-4"</automated>
  </verify>
  <done>
    - `PRESSURE_DELTA_DOMAIN` is `0.30` in `frontend/src/lib/pressureBulletConfig.ts`.
    - `EndgameTimePressureCard.tsx` filters out `quintile_index === 4` rows; renders 4 quintile bullets per card.
    - The 4 visible quintile rows show "High Pressure (0-20%)", "Medium Pressure (20-40%)", "Low Pressure (40-60%)", "Very Low Pressure (60-80%)" — not the raw "0-20%" range strings.
    - The card-title InfoPopover body no longer mentions Q4 or "80-100%".
    - `frontend/src/pages/Endgames.tsx` does NOT wrap `<EndgameTimePressureSection>` in an outer `<div className="charcoal-texture rounded-md p-4">`.
    - All updated and new tests pass.
    - `tsc --noEmit`, `lint`, `knip`, `build`, and `pytest scripts/gen_endgame_zones_ts.py --check` all exit 0.
    - CHANGELOG `## [Unreleased]` gains the bullet describing the relabel + Q4 hide + axis widen + card-chrome split.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → frontend bundle | No new trust boundary crossed. All changes are layout, constants, and display copy in already-served, already-authenticated frontend code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-13-01 | Information disclosure | Quintile filter | accept | Q4 is filtered from display only; the data still flows in `card.quintiles`. No PII or sensitive data is involved (the user's own game stats, already shown elsewhere on the page). |
| T-88-13-02 | Tampering | Display labels | accept | Pressure labels are display strings only; flipping them does not affect any business logic, stats, or backend behavior. |

No new external HTTP, no auth changes, no user-input parsing. Extends existing authenticated overview payload's display layer; no new attack surface.
</threat_model>

<verification>
- `cd frontend && npm test -- --run` (all frontend tests pass — 494+ tests, will be 497+ after additions).
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` (TypeScript clean).
- `cd frontend && npm run lint` (ESLint clean).
- `cd frontend && npm run knip` (no new dead exports).
- `cd frontend && npm run build` (production bundle compiles).
- `uv run python scripts/gen_endgame_zones_ts.py --check` (codegen drift gate clean — no Python→TS changes in this plan).
- Manual diff check on `pressureBulletConfig.ts`: only the numeric value of `PRESSURE_DELTA_DOMAIN` and its JSDoc comment changed.
- Manual diff check on `EndgameTimePressureCard.tsx`: new `PRESSURE_LABELS` const + helper, filter on quintiles, label-source swap inside QuintileRow + EmptyBinRow, updated title popover body. No structural changes to ClockGapRow.
- Manual diff check on `Endgames.tsx`: only the `<div className="charcoal-texture rounded-md p-4">` wrapper around `<EndgameTimePressureSection>` is removed; section data wiring unchanged.
- CHANGELOG bullet appended under `## [Unreleased]` → `### Changed`.
</verification>

<success_criteria>
- All must-have truths and artifacts in frontmatter present in the committed code.
- All automated verification commands exit 0.
- Test count grows by at least 3 (Q4-hidden assertion, new-labels assertion, ±30% axis-domain assertion).
- No backend file modified.
- No codegen file modified (`endgame_zones.py`, `endgameZones.ts`, `scripts/gen_endgame_zones_ts.py` unchanged).
- CHANGELOG entry under `## [Unreleased]` is honest about the visible relabel and the deliberate Q4 hide.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-13-SUMMARY.md` when done. Note in the SUMMARY that 88-13 lands a frontend-only polish slice and that 88-14 + 88-15 will follow (top-zone stats + line-chart restoration respectively, both implementing the user-approved SC #1 amendment recorded in 88-CONTEXT.md §2).
</output>
