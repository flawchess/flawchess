---
phase: 88-time-pressure-stats-rework
plan: 08
type: execute
wave: 5
depends_on:
  - 88-02
  - 88-03
  - 88-07
files_modified:
  - reports/benchmarks-latest.md
  - app/services/endgame_zones.py
  - frontend/src/generated/endgameZones.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "/benchmarks §3.3.3 chess-score-per-pressure-bin run against benchmark DB; per-quintile Cohen's d verdicts recorded in reports/benchmarks-latest.md"
    - "/benchmarks §3.3.1 clock-gap-% submetric run; collapse verdict recorded"
    - "PRESSURE_BIN_SCORE_NEUTRAL_ZONES placeholder values replaced with calibrated p25/p75 per (TC, quintile), with editorial cap applied"
    - "If any quintile's ELO collapse verdict is 'keep separate', that quintile is promoted to (TC, ELO, quintile) faceting -- requires schema/codegen extension, plan flags this back to the user"
    - "CLOCK_GAP_NEUTRAL_MIN/MAX in ZONE_REGISTRY['clock_gap_pct'] replaced with benchmark-calibrated values from clock-gap-% collapse verdict"
    - "Codegen regenerated; CI drift gate clean; existing tests still green (test_pressure_bin_zones_shape uses PRESSURE_BIN_NEUTRAL_CAP symbolically)"
  artifacts:
    - path: "reports/benchmarks-latest.md"
      provides: "§3.3.3 + clock-gap-% sections with calibrated tables"
      contains: "chess-score-per-pressure-bin"
    - path: "app/services/endgame_zones.py"
      provides: "Calibrated PRESSURE_BIN_SCORE_NEUTRAL_ZONES values + calibrated clock_gap_pct ZoneSpec"
      contains: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "Regenerated TS constants matching the new calibrated Python values"
      contains: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
  key_links:
    - from: "reports/benchmarks-latest.md §3.3.3"
      to: "app/services/endgame_zones.py PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
      via: "manual transcription of per-(TC, quintile) Q1/Q3 + editorial cap"
      pattern: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
    - from: "app/services/endgame_zones.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "scripts/gen_endgame_zones_ts.py"
      pattern: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
---

<objective>
Replace the placeholder zone constants from Plan 03 with benchmark-calibrated values. Sequence:

1. Run `/benchmarks chess-score-per-pressure-bin` (§3.3.3, added in Plan 02) against the benchmark DB.
2. Run `/benchmarks clock-gap-%` (§3.3.1 submetric, added in Plan 02) against the benchmark DB.
3. Review the per-quintile Cohen's d collapse verdicts. For chess-score-per-pressure-bin: confirm ELO collapse per quintile; for any quintile failing the collapse threshold (`d >= 0.5`), surface a `checkpoint:decision` to the user about promoting to per-ELO faceting (this would require additional schema/codegen work outside the current 4x5 shape).
4. Transcribe Q1/Q3 of per-user score per (TC, quintile) into `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` (apply `PRESSURE_BIN_NEUTRAL_CAP = 0.06` half-width cap symmetrically around the median).
5. Transcribe the global clock-gap-% band into `ZONE_REGISTRY["clock_gap_pct"]`.
6. Regenerate `frontend/src/generated/endgameZones.ts` via `scripts/gen_endgame_zones_ts.py`; commit alongside.
7. Final test sweep: ensure no consumer code changes; only the underlying numbers move; existing tests still pass (registry-shape test uses `PRESSURE_BIN_NEUTRAL_CAP` symbolically per Plan 03 design).

Purpose: ships real numbers. The placeholder run already proves the surface works; this plan converts placeholders to calibrated values without touching consumer code.

Output: 3 files modified, codegen drift-clean, tests still green.

This plan is `autonomous: false` because Step 3 requires a human checkpoint when the collapse verdict mandates schema-level changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@.claude/skills/benchmarks/SKILL.md
@app/services/endgame_zones.py
@reports/benchmarks-latest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /benchmarks §3.3.3 + clock-gap-% against benchmark DB</name>
  <files>reports/benchmarks-latest.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (specifically the new §3.3.3 chess-score-per-pressure-bin section and the §3.3.1 clock-gap-% submetric added by Plan 02; the standard CTE + sparse-cell exclusion + equal-footing filter conventions)
    - reports/benchmarks-latest.md (existing structure; §3.3.2 sits next to where §3.3.3 will be appended)
    - CLAUDE.md (the `bin/benchmark_db.sh start` command for bringing the benchmark DB up; the `mcp__flawchess-benchmark-db__query` MCP tool for read-only queries; the benchmark DB read-only role)
  </read_first>
  <action>
    Bring up the benchmark DB if not already running:
    ```
    bin/benchmark_db.sh start
    ```

    Run the §3.3.3 chess-score-per-pressure-bin query per the SKILL.md skeleton. Use the `mcp__flawchess-benchmark-db__query` MCP tool for read-only SELECTs. Expected output structure:
    - Per-(TC, ELO, quintile) intermediate cells (4 x 5 x 5 = 100 cells; sparse cells excluded per `NOT (elo_bucket = 2400 AND tc = 'classical')`).
    - Per-quintile Cohen's d collapse verdicts on TC axis (5 verdicts).
    - Per-quintile Cohen's d collapse verdicts on ELO axis (5 verdicts).
    - Per-(TC, quintile) Q1/Q3 of per-user score (ELO pooled by default) -- this is the 4x5=20 shipped band table.

    Run the §3.3.1 clock-gap-% query. Expected output:
    - Per-(TC, ELO) cells of per-user mean `(my_clock - opp_clock) / base_clock`.
    - Cohen's d collapse verdicts on TC + ELO axes.
    - If global collapse confirmed: single (typical_lower, typical_upper) pair = `(p25, p75)` of the pooled distribution.

    Append both sections to `reports/benchmarks-latest.md`:
    - New `### §3.3.3 chess-score-per-pressure-bin` section after the existing §3.3.2, with: SQL queries used, intermediate (TC, ELO, quintile) cells, per-quintile collapse verdicts, final 4x5 IQR band table (pre-cap and post-cap).
    - New subsection under §3.3.1 documenting the clock-gap-% submetric output: per-(TC, ELO) cells, collapse verdicts, final global band.

    Report the dates / DB snapshot used. Cite the row counts.
  </action>
  <verify>
    <automated>grep -c "§3.3.3 chess-score-per-pressure-bin\|clock-gap-%" reports/benchmarks-latest.md ; grep -cE "Q1|Q3|p25|p75" reports/benchmarks-latest.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "§3.3.3" reports/benchmarks-latest.md` >= 1.
    - `grep -c "chess-score-per-pressure-bin" reports/benchmarks-latest.md` >= 1.
    - `grep -c "clock-gap-%" reports/benchmarks-latest.md` >= 1.
    - The new §3.3.3 contains 5 per-quintile collapse verdicts on each of TC + ELO axes (5 + 5 = 10 verdicts total).
    - The §3.3.3 contains a 4x5 (TC, quintile) Q1/Q3 table.
    - The clock-gap-% submetric reports a single global band or per-(TC, ELO) bands depending on the verdict.
  </acceptance_criteria>
  <done>Benchmark report contains calibrated tables; raw data available for Tasks 2-3.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Verify collapse verdicts; decide on per-ELO faceting promotion if any quintile fails</name>
  <decision>Did any quintile's ELO collapse verdict for chess-score-per-pressure-bin fail (Cohen's d >= 0.5)? If so, should the failing quintile(s) be promoted to (TC, ELO, quintile) faceting, requiring schema + codegen extension?</decision>
  <context>
    Plan 03 scaffolded a 4x5 (TC, quintile) shipped band shape with ELO pooled. 88-RESEARCH.md Q4 expected ELO to collapse per quintile (current §3.3.2 already showed d=0.17 pooled). If any quintile diverges, the default 4x5 shape is wrong for that quintile.

    Three resolution paths:
    1. **All quintiles collapsed (expected):** transcribe 4x5 = 20 bands; proceed to Task 3.
    2. **One or more quintiles failed:** EITHER promote that quintile to (TC, ELO, quintile) faceting (requires `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` schema extension + codegen extension; out-of-scope for this plan and probably this phase), OR accept the pooled band as-is with a documented caveat.
    3. **Most quintiles collapsed, edge case at Q0 or Q4:** apply the editorial cap aggressively at the failing quintile (cap protects against the divergent ELO band widening uselessly).

    Read `reports/benchmarks-latest.md` §3.3.3 verdicts (just appended in Task 1) before deciding.
  </context>
  <options>
    <option id="collapse-clean">
      <name>All quintiles collapsed; proceed with 4x5 shape</name>
      <pros>Matches Plan 03 scaffolding; zero schema/codegen changes required</pros>
      <cons>Only valid if data confirms ELO collapse for all 5 quintiles</cons>
    </option>
    <option id="accept-pooled-with-caveat">
      <name>One+ quintiles failed; accept pooled band, document caveat in CHANGELOG</name>
      <pros>Ships the phase; calibration imperfect but actionable; editorial cap mitigates</pros>
      <cons>Loses signal in the failing quintile; should be revisited in a future polish phase</cons>
    </option>
    <option id="promote-to-elo-faceting">
      <name>Promote failing quintile(s) to (TC, ELO, quintile) faceting -- requires schema extension</name>
      <pros>Correct shape for divergent quintiles</pros>
      <cons>Out-of-scope for Phase 88; requires re-planning + extending PressureBinBand registry, codegen, frontend lookup, and component lookup logic; likely a Phase 88.1 split</cons>
    </option>
  </options>
  <resume-signal>Select: collapse-clean, accept-pooled-with-caveat, or promote-to-elo-faceting. If promote-to-elo-faceting is chosen, the executor stops here and the user runs `/gsd:plan-phase 88.1` to extend the schema before this plan resumes.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Transcribe calibrated values into endgame_zones.py + regenerate TS</name>
  <files>app/services/endgame_zones.py, frontend/src/generated/endgameZones.ts</files>
  <read_first>
    - reports/benchmarks-latest.md (the §3.3.3 4x5 IQR table and the clock-gap-% global band from Task 1)
    - app/services/endgame_zones.py (the placeholder PRESSURE_BIN_SCORE_NEUTRAL_ZONES grid and clock_gap_pct ZoneSpec from Plan 03)
    - scripts/gen_endgame_zones_ts.py (codegen entrypoint -- ensure no script changes are needed; only data changes)
  </read_first>
  <action>
    1. For each of the 20 (TC, quintile) cells in §3.3.3, transcribe the calibrated Q1/Q3 into the `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` mapping. Apply the editorial cap symmetrically around the median: if `(p75 - p25) / 2 > PRESSURE_BIN_NEUTRAL_CAP`, replace the band with `(median - PRESSURE_BIN_NEUTRAL_CAP, median + PRESSURE_BIN_NEUTRAL_CAP)`.

    2. Update the per-cell band comments (if any) to cite the data source: "Calibrated from `reports/benchmarks-latest.md` §3.3.3 (YYYY-MM-DD benchmark snapshot)". Drop the "PLACEHOLDER" annotations.

    3. For `ZONE_REGISTRY["clock_gap_pct"]`, replace the placeholder `(-NEUTRAL_PCT_THRESHOLD, NEUTRAL_PCT_THRESHOLD)` with the calibrated global band from the §3.3.1 clock-gap-% submetric. Cite the data source in the comment.

    4. Regenerate `frontend/src/generated/endgameZones.ts`:
       ```
       uv run python scripts/gen_endgame_zones_ts.py
       ```

    5. Commit both files together (drift gate).

    Do NOT modify any consumer code, schema, codegen script, or test. The placeholder-to-calibrated swap is data-only.

    If Task 2 resolution was `accept-pooled-with-caveat`, add a CHANGELOG entry (per CLAUDE.md per-phase changelog convention) flagging the partial-collapse caveat under the `## [Unreleased]` section with subsection `### Changed`. (Phase 88 ship-level CHANGELOG entry happens at phase-merge time; this is just the technical caveat.)
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py ; git diff --exit-code frontend/src/generated/endgameZones.ts ; uv run pytest tests/services/test_endgame_zones.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PressureBinBand(-0.06, 0.06)" app/services/endgame_zones.py` -- typically returns 0 (all placeholders replaced) UNLESS a quintile's calibrated half-width legitimately hits the editorial cap. If the count is > 0, each remaining placeholder-shaped cell MUST have a comment citing "editorial cap activated" with the underlying Q1/Q3 values.
    - `grep -c "PLACEHOLDER\|placeholder" app/services/endgame_zones.py` returns 0 in the PRESSURE_BIN_SCORE_NEUTRAL_ZONES section (annotations stripped).
    - `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0.
    - `uv run pytest tests/services/test_endgame_zones.py -x -q` exits 0 (the symbolic-cap test still passes because it asserts against `PRESSURE_BIN_NEUTRAL_CAP`, not the placeholder value).
    - Full backend + frontend suite (`uv run pytest -x -q && cd frontend && npm test -- --run`) exits 0.
  </acceptance_criteria>
  <done>Calibrated values shipped; codegen drift-clean; no consumer code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark DB -> reports/benchmarks-latest.md | Read-only MCP query against benchmark Postgres (port 5433); the benchmark DB is isolated, no prod/PII data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-08-01 | Tampering | Transcription errors when copying 20 cells from report to Python | mitigate | Editorial cap test (`test_pressure_bin_zones_shape`) catches lower >= upper or half-width > cap; reviewer reads the report alongside the diff |
| T-88-08-02 | Information Disclosure | Benchmark DB query exposes per-user data | accept | Benchmark DB uses synthetic / lichess-pull users only (per CLAUDE.md project_benchmark_db_ro_password); read-only role; aggregate-only output |
| T-88-08-03 | Repudiation | Calibrated values silently changing across runs | mitigate | Codegen drift gate fails on commit-time mismatch; report `reports/benchmarks-latest.md` cites the snapshot date |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `reports/benchmarks-latest.md` contains the new §3.3.3 + clock-gap-% sections with verdicts.
- `app/services/endgame_zones.py` carries calibrated values (or editorial-cap-justified cells); `PLACEHOLDER` text removed.
- Codegen drift gate clean: `git diff --exit-code frontend/src/generated/endgameZones.ts` after running the script.
- Full backend + frontend suite green.
</verification>

<success_criteria>
- Zone constants reflect benchmark-calibrated reality (Phase 88 ships honest data).
- No consumer code changes -- only data moved.
- Codegen drift-clean and tests still pass.
- If a quintile diverged across ELO, the decision is recorded (either accepted with caveat or split into Phase 88.1).
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-08-SUMMARY.md` when done.
</output>
</content>
</invoke>