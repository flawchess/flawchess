---
phase: 88-time-pressure-stats-rework
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_zones.py
  - scripts/gen_endgame_zones_ts.py
  - frontend/src/generated/endgameZones.ts
  - tests/services/test_endgame_zones.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "PRESSURE_BIN_SCORE_NEUTRAL_ZONES exists in endgame_zones.py keyed by (TC, quintile_index) with 4x5=20 entries (D-02)"
    - "PRESSURE_BIN_NEUTRAL_CAP = 0.06 constant defined alongside the registry"
    - "MetricId Literal includes 'clock_gap_pct' and ZONE_REGISTRY has a ZoneSpec for it"
    - "gen_endgame_zones_ts.py emits the new constants into endgameZones.ts and CI drift gate passes"
    - "test_endgame_zones.py asserts the 4x5 shape, lower<upper invariant, and editorial-cap invariant"
  artifacts:
    - path: "app/services/endgame_zones.py"
      provides: "PressureBinBand dataclass, PRESSURE_BIN_SCORE_NEUTRAL_ZONES mapping, PRESSURE_BIN_NEUTRAL_CAP constant, clock_gap_pct in MetricId + ZONE_REGISTRY"
      contains: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
    - path: "scripts/gen_endgame_zones_ts.py"
      provides: "_format_pressure_bin_zones() + clock_gap emission appended to _render()"
      contains: "_format_pressure_bin_zones"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "exported PRESSURE_BIN_SCORE_NEUTRAL_ZONES and CLOCK_GAP_NEUTRAL_MIN/MAX TS constants"
      contains: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
    - path: "tests/services/test_endgame_zones.py"
      provides: "test_pressure_bin_zones_shape + clock_gap_pct registry-key assertion"
      contains: "test_pressure_bin_zones_shape"
  key_links:
    - from: "endgame_zones.py PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
      to: "endgameZones.ts PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
      via: "gen_endgame_zones_ts.py codegen pipeline"
      pattern: "_format_pressure_bin_zones"
    - from: "endgame_zones.py ZONE_REGISTRY clock_gap_pct"
      to: "endgameZones.ts CLOCK_GAP_NEUTRAL_MIN / CLOCK_GAP_NEUTRAL_MAX"
      via: "scalar ZoneSpec emission in _render()"
      pattern: "CLOCK_GAP_NEUTRAL"
---

<objective>
Scaffold the zone-constant infrastructure for Phase 88 with PLACEHOLDER values (per 88-RESEARCH.md Q5): add `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` (4 TC x 5 quintile = 20 entries, each at the editorial cap +/-0.06), the `PRESSURE_BIN_NEUTRAL_CAP = 0.06` constant, the new `PressureBinBand` frozen dataclass, the `clock_gap_pct` MetricId + `ZONE_REGISTRY` entry (placeholder -0.05/+0.05); extend `scripts/gen_endgame_zones_ts.py` to emit both into `frontend/src/generated/endgameZones.ts`; commit the regenerated TS file (CI drift gate); add registry-sanity tests.

This plan ships placeholder values intentionally. The real benchmark-calibrated values land in Plan 08 after running `/benchmarks` against the benchmark DB.

Purpose: unblocks downstream frontend tasks (Plans 05-07) that import `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` and the clock-gap constants from `endgameZones.ts`. With placeholders in place, the card component can be built and tested against a known shape; the values are swapped in Plan 08 without touching any consumer code.

Output: 4 files modified, codegen drift-clean (regenerated TS committed alongside Python source), zone-shape test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-ROADMAP.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@app/services/endgame_zones.py
@scripts/gen_endgame_zones_ts.py
@frontend/src/generated/endgameZones.ts
@tests/services/test_endgame_zones.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PressureBinBand + PRESSURE_BIN_SCORE_NEUTRAL_ZONES + clock_gap_pct to endgame_zones.py</name>
  <files>app/services/endgame_zones.py</files>
  <read_first>
    - app/services/endgame_zones.py (whole file; specifically the `MetricId` Literal near line 30, `ZoneSpec` dataclass + `ZONE_REGISTRY` at lines 161-331, `PerClassBands` dataclass + `PER_CLASS_GAUGE_ZONES` at lines 406-464, and the `NEUTRAL_PCT_THRESHOLD` constant)
    - app/services/insights_service.py (search `compute_findings` and `MetricId` consumers to confirm new `clock_gap_pct` does not auto-fire an LLM finding; time-pressure LLM narration is deferred)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 4 -- exact Python signatures and patterns to copy)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md (Q5 -- placeholder grid; Open items item 3 -- MetricId scope check)
  </read_first>
  <action>
    Add four new symbols to `app/services/endgame_zones.py`:

    1. `PRESSURE_BIN_NEUTRAL_CAP: float = 0.06` constant -- co-locate with the other top-of-file zone constants (near `NEUTRAL_PCT_THRESHOLD`). Add a comment citing D-02 (CONTEXT.md) and benchmarks 3.3.3.

    2. `@dataclass(frozen=True)` `PressureBinBand` with fields `lower: float` and `upper: float` -- mirror the `PerClassBands` declaration style.

    3. `PRESSURE_BIN_SCORE_NEUTRAL_ZONES: Mapping[Literal["bullet", "blitz", "rapid", "classical"], Mapping[Literal[0, 1, 2, 3, 4], PressureBinBand]]` -- 4x5=20 entries, every band set to `PressureBinBand(-0.06, 0.06)` (the editorial-cap placeholder). Add a docstring/comment stating these are PLACEHOLDER values, calibrated by benchmarks 3.3.3 in Plan 08; quintile index 0 = 0-20% clock remaining (max pressure), 4 = 80-100% (min); ELO is pooled by default.

    4. Add `"clock_gap_pct"` to the `MetricId` Literal (preserve the existing ordering pattern). Add a `ZONE_REGISTRY["clock_gap_pct"] = ZoneSpec(typical_lower=-NEUTRAL_PCT_THRESHOLD, typical_upper=NEUTRAL_PCT_THRESHOLD, direction="higher_is_better")` entry with a placeholder comment until benchmarks 3.3.1 clock-gap-% runs.

    Before adding `clock_gap_pct` to `MetricId`, read `app/services/insights_service.py` and confirm the finding loop is allow-list based (does not iterate the full `MetricId` Literal). If the consumer auto-iterates `MetricId`, add an exclusion guard for `clock_gap_pct` so no LLM finding fires (time-pressure LLM narration is deferred per CONTEXT.md Deferred Ideas).

    All symbols MUST pass `uv run ty check app/services/endgame_zones.py`. Reuse the existing `from typing import Literal, Mapping` imports if already present.
  </action>
  <verify>
    <automated>uv run ty check app/services/endgame_zones.py 2>&amp;1 | tail -5 ; grep -cE "PRESSURE_BIN_SCORE_NEUTRAL_ZONES|PRESSURE_BIN_NEUTRAL_CAP|class PressureBinBand|clock_gap_pct" app/services/endgame_zones.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PRESSURE_BIN_NEUTRAL_CAP" app/services/endgame_zones.py` >= 1.
    - `grep -c "class PressureBinBand" app/services/endgame_zones.py` returns 1.
    - `grep -c "PRESSURE_BIN_SCORE_NEUTRAL_ZONES" app/services/endgame_zones.py` >= 1.
    - `grep -c "clock_gap_pct" app/services/endgame_zones.py` >= 2 (one in MetricId Literal, one in ZONE_REGISTRY entry).
    - `uv run ty check app/services/endgame_zones.py` exits 0.
    - `python -c "from app.services.endgame_zones import PRESSURE_BIN_SCORE_NEUTRAL_ZONES; assert len(PRESSURE_BIN_SCORE_NEUTRAL_ZONES) == 4; assert all(len(v) == 5 for v in PRESSURE_BIN_SCORE_NEUTRAL_ZONES.values())"` exits 0.
  </acceptance_criteria>
  <done>Four new symbols exist with the documented shape; ty clean; no accidental LLM-finding regression.</done>
</task>

<task type="auto">
  <name>Task 2: Extend gen_endgame_zones_ts.py + regenerate endgameZones.ts</name>
  <files>scripts/gen_endgame_zones_ts.py, frontend/src/generated/endgameZones.ts</files>
  <read_first>
    - scripts/gen_endgame_zones_ts.py (whole file; specifically the import block at lines 30-36, `_format_per_class_gauge_zones()` at lines 90-106, scalar emission at lines 137-155, and `_render()` at lines 180+ for the string-concatenation pattern)
    - frontend/src/generated/endgameZones.ts (whole file; observe the existing export conventions, comment style, `as const` usage)
    - app/services/endgame_zones.py (the just-added PRESSURE_BIN_SCORE_NEUTRAL_ZONES and clock_gap_pct ZoneSpec from Task 1)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 5 -- codegen extension pattern)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md (Q5 -- generated TS shape including `Record<TC, Record<0|1|2|3|4, {min, max}>>`)
  </read_first>
  <action>
    Extend `scripts/gen_endgame_zones_ts.py`:

    1. Import `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` from `app.services.endgame_zones` alongside the existing imports.
    2. Add a new function `_format_pressure_bin_zones() -> str` that iterates `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`, emits one TS line per TC keyed by quintile index, with each band rendered as `{ min: <lower>, max: <upper> }`. Mirror the structure of `_format_per_class_gauge_zones()`.
    3. In `_render()`, append a new emission block after the `PER_CLASS_GAUGE_ZONES` block: header comment "Phase 88 D-02: per-(TC, pressure-quintile) neutral bands.", then the TS export `export const PRESSURE_BIN_SCORE_NEUTRAL_ZONES: Record<'bullet' | 'blitz' | 'rapid' | 'classical', Record<0 | 1 | 2 | 3 | 4, { min: number; max: number }>> = { ... } as const;`.
    4. In `_render()`, add scalar emission for `clock_gap_pct` from `ZONE_REGISTRY` using the same pattern as the existing `NEUTRAL_PCT_THRESHOLD` flat constants: `CLOCK_GAP_NEUTRAL_MIN` and `CLOCK_GAP_NEUTRAL_MAX`.

    After editing the script, run `uv run python scripts/gen_endgame_zones_ts.py` to regenerate `frontend/src/generated/endgameZones.ts`. Commit the regenerated TS file in the same change as the script edit (CI runs the codegen in `--check` mode and fails on drift).

    Do NOT hand-edit `endgameZones.ts` directly; it must be a clean output of the script.
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py ; git diff --exit-code frontend/src/generated/endgameZones.ts ; grep -cE "PRESSURE_BIN_SCORE_NEUTRAL_ZONES|CLOCK_GAP_NEUTRAL_MIN|CLOCK_GAP_NEUTRAL_MAX" frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _format_pressure_bin_zones" scripts/gen_endgame_zones_ts.py` returns 1.
    - `grep -c "PRESSURE_BIN_SCORE_NEUTRAL_ZONES" scripts/gen_endgame_zones_ts.py` >= 2 (one import, one emission).
    - Running `uv run python scripts/gen_endgame_zones_ts.py` produces no diff against the committed `endgameZones.ts` (`git diff --exit-code` exits 0).
    - `grep -c "export const PRESSURE_BIN_SCORE_NEUTRAL_ZONES" frontend/src/generated/endgameZones.ts` returns 1.
    - `grep -c "CLOCK_GAP_NEUTRAL_MIN\|CLOCK_GAP_NEUTRAL_MAX" frontend/src/generated/endgameZones.ts` returns 2.
    - The TS file still type-checks: `cd frontend && npx tsc --noEmit src/generated/endgameZones.ts` exits 0.
  </acceptance_criteria>
  <done>Codegen drift-clean; both new constants exported from `endgameZones.ts` with correct TS types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add registry-sanity tests for the new zones</name>
  <files>tests/services/test_endgame_zones.py</files>
  <read_first>
    - tests/services/test_endgame_zones.py (whole file; specifically `TestRegistrySanity.test_all_scalar_metrics_have_entries` at line ~196 for the set-equality pattern to extend)
    - app/services/endgame_zones.py (the new constants from Task 1)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 9 -- test_pressure_bin_zones_shape sketch)
  </read_first>
  <behavior>
    - `test_pressure_bin_zones_shape` asserts: `set(PRESSURE_BIN_SCORE_NEUTRAL_ZONES.keys()) == {"bullet", "blitz", "rapid", "classical"}`; each TC maps to exactly `{0, 1, 2, 3, 4}`; for every (tc, q): `band.lower < band.upper`; half-width `(band.upper - band.lower) / 2 <= PRESSURE_BIN_NEUTRAL_CAP + 1e-9`.
    - `test_all_scalar_metrics_have_entries` (or a peer test in `TestRegistrySanity`) asserts `"clock_gap_pct" in ZONE_REGISTRY` and the corresponding ZoneSpec has `direction == "higher_is_better"` and finite `typical_lower < typical_upper`.
  </behavior>
  <action>
    Extend `tests/services/test_endgame_zones.py`:

    1. Update `TestRegistrySanity.test_all_scalar_metrics_have_entries` (or equivalent) to include `"clock_gap_pct"` in the expected set of MetricId keys.
    2. Add a new test method `test_pressure_bin_zones_shape` to `TestRegistrySanity` (or a new `TestPressureBinZones` class) that imports `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` and `PRESSURE_BIN_NEUTRAL_CAP`, then asserts the shape invariants listed in `<behavior>`.

    Use the existing `pytest` test style in the file. Add the import line `from app.services.endgame_zones import PRESSURE_BIN_NEUTRAL_CAP, PRESSURE_BIN_SCORE_NEUTRAL_ZONES` (alongside existing imports). Do NOT depend on the placeholder value `0.06` directly in assertions (use `PRESSURE_BIN_NEUTRAL_CAP`); this keeps the test green when Plan 08 swaps in real benchmark-calibrated values that may use a different cap.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_zones.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_endgame_zones.py -x -q` exits 0; new tests are listed in the output.
    - `grep -c "test_pressure_bin_zones_shape" tests/services/test_endgame_zones.py` returns 1.
    - `grep -c "clock_gap_pct" tests/services/test_endgame_zones.py` >= 1.
    - `uv run ty check tests/services/test_endgame_zones.py` exits 0.
  </acceptance_criteria>
  <done>Registry shape invariants are unit-tested; existing tests still pass; ty clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal constants + codegen; no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03-01 | Tampering | Drift between `endgame_zones.py` and `endgameZones.ts` | mitigate | Task 2 verify step runs `git diff --exit-code` after regenerating TS; CI repeats the same check on push |
| T-88-03-02 | Information Disclosure | New `clock_gap_pct` MetricId accidentally auto-fires an LLM finding (time-pressure LLM narration is deferred) | mitigate | Task 1 read_first includes insights_service.py inspection; guard added if consumer auto-iterates MetricId |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `uv run python scripts/gen_endgame_zones_ts.py` produces no diff against committed `endgameZones.ts`.
- `uv run pytest tests/services/test_endgame_zones.py -x -q` exits 0.
- `uv run ty check app/ tests/` exits 0.
- Downstream consumers can import `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`, `CLOCK_GAP_NEUTRAL_MIN`, `CLOCK_GAP_NEUTRAL_MAX` from `@/generated/endgameZones` without TS errors.
</verification>

<success_criteria>
- 4 files modified with placeholder values; codegen drift-clean.
- Registry shape and editorial-cap invariants pass tests.
- `clock_gap_pct` MetricId added without triggering LLM-finding regression.
- Plan 08 can later swap values without touching consumer code.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-03-SUMMARY.md` when done.
</output>
</content>
</invoke>