---
phase: 88-time-pressure-stats-rework
plan: 06
type: execute
wave: 3
depends_on:
  - 88-04
  - 88-05
files_modified:
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
  - frontend/src/types/api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "EndgameTimePressureCard renders 1 Clock Gap bullet + 5 Score-Delta bullets stacked vertically (D-01 doctrine)"
    - "Card returns null when card.total < MIN_GAMES_PER_TC_CARD (TC-level hide)"
    - "Per-bin: n=0 renders dash + 'no games' inline with the axis tick; 0 < n < MIN_GAMES_PER_PRESSURE_BIN renders a dimmed bullet at UNRELIABLE_OPACITY with an n=X chip; triple-gate font coloring fires only when n >= MIN_GAMES_PER_PRESSURE_BIN AND p < 0.05 AND outside neutral band (D-01)"
    - "Card heights are equal across the 4-TC grid by construction (n=0 row slot is preserved with the axis tick)"
    - "Every interactive element has data-testid; popovers carry aria-label; semantic HTML used throughout"
  artifacts:
    - path: "frontend/src/components/charts/EndgameTimePressureCard.tsx"
      provides: "Per-TC card component with 6 bullets (1 Clock Gap + 5 Score-Delta) and sparse handling"
      contains: "EndgameTimePressureCard"
    - path: "frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx"
      provides: "Vitest coverage of TC-card hide, n=0 row preservation, dimming, triple-gate"
      contains: "EndgameTimePressureCard"
    - path: "frontend/src/types/api.ts"
      provides: "TS types matching TimePressureCardsResponse / TimePressureTcCard / ClockGapBullet / PressureQuintileBullet"
      contains: "TimePressureCardsResponse"
  key_links:
    - from: "EndgameTimePressureCard"
      to: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES"
      via: "per-(TC, quintile) neutral band lookup"
      pattern: "PRESSURE_BIN_SCORE_NEUTRAL_ZONES\\[card.tc\\]"
    - from: "EndgameTimePressureCard"
      to: "MiniBulletChart"
      via: "bullet rendering primitive"
      pattern: "<MiniBulletChart"
    - from: "EndgameTimePressureCard"
      to: "isConfident"
      via: "triple-gate font coloring"
      pattern: "isConfident"
---

<objective>
Implement the per-TC card component `frontend/src/components/charts/EndgameTimePressureCard.tsx` and its Vitest suite. Mirrors `EndgameTypeCard.tsx` for the sparse-handling + triple-gate + MetricStatPopover patterns, but stacks 6 horizontal bullets (1 Clock Gap + 5 Score-Delta) rather than the type-card layout. Also adds the matching TS types in `frontend/src/types/api.ts` (or wherever the existing Endgames response types live).

Purpose: this is the visual unit. The Section component (Plan 07) is a thin grid over these cards.

Output: 3 files (component + test + types), all type-clean, vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-CONTEXT.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md
@.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md
@frontend/src/components/charts/EndgameTypeCard.tsx
@frontend/src/components/charts/MiniBulletChart.tsx
@frontend/src/components/popovers/MetricStatPopover.tsx
@frontend/src/lib/significance.ts
@frontend/src/lib/theme.ts
@frontend/src/lib/pressureBulletConfig.ts
@frontend/src/generated/endgameZones.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TimePressureCardsResponse / TimePressureTcCard / ClockGapBullet / PressureQuintileBullet TS types</name>
  <files>frontend/src/types/api.ts</files>
  <read_first>
    - frontend/src/types/api.ts (whole file -- locate the existing `EndgameOverviewResponse` type. If types are split across files, grep `grep -rn "EndgameOverviewResponse" frontend/src/` to find the canonical home)
    - app/schemas/endgames.py (the new Pydantic models from Plan 04 Task 1 -- TS types must mirror these exactly: field names, optionality, Literal values)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 3 -- expected field shapes)
  </read_first>
  <action>
    In the file that exports `EndgameOverviewResponse` (typically `frontend/src/types/api.ts`):

    1. REMOVE the `ClockPressureResponse` and `TimePressureChartResponse` TS interfaces/types and any helper types used only by them.
    2. ADD four new TS interfaces mirroring the Pydantic shapes:
       - `PressureQuintileBullet` with fields `quintile_index: number; quintile_label: string; n: number; delta: number; p_value: number | null; ci_low: number | null; ci_high: number | null; cohort_score: number | null;`.
       - `ClockGapBullet` with fields `n: number; mean_diff_pct: number; p_value: number | null; ci_low: number | null; ci_high: number | null;`.
       - `TimePressureTcCard` with fields `tc: 'bullet' | 'blitz' | 'rapid' | 'classical'; total: number; clock_gap: ClockGapBullet; quintiles: PressureQuintileBullet[];`.
       - `TimePressureCardsResponse` with fields `cards: TimePressureTcCard[];`.
    3. MODIFY `EndgameOverviewResponse`: remove `clock_pressure` and `time_pressure_chart` fields; add `time_pressure_cards: TimePressureCardsResponse`.

    Before deleting, `grep -rn "ClockPressureResponse\|TimePressureChartResponse" frontend/src/` to confirm no out-of-scope consumer; if found, surface in SUMMARY (Plan 07 will handle the final consumer migration). Acceptable temporary breakage: types removed but consumers in `Endgames.tsx` + legacy components still reference them -- those break compilations are healed in Plan 07. Document this as expected in the SUMMARY.

    Field names MUST match the snake_case Pydantic output exactly (TS field names are snake_case here to match the API response, matching the existing convention in `api.ts`).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -cE "TimePressureCardsResponse|TimePressureTcCard|ClockGapBullet|PressureQuintileBullet" src/types/api.ts ; grep -c "ClockPressureResponse\|TimePressureChartResponse" src/types/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TimePressureCardsResponse\|TimePressureTcCard\|ClockGapBullet\|PressureQuintileBullet" frontend/src/types/api.ts` returns >= 4.
    - `grep -c "ClockPressureResponse\|TimePressureChartResponse" frontend/src/types/api.ts` returns 0.
    - `EndgameOverviewResponse` has the new `time_pressure_cards: TimePressureCardsResponse` field.
    - Temporary type breakage in legacy consumers is documented; Plan 07 closes the loop.
  </acceptance_criteria>
  <done>TS types mirror the backend; legacy types removed; ready for component import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EndgameTimePressureCard.tsx</name>
  <files>frontend/src/components/charts/EndgameTimePressureCard.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameTypeCard.tsx (whole file; specifically the imports at lines 23-71, sparse-handling at lines 95-100, triple-gate at lines 107-111, MiniBulletChart usage at lines 372-384)
    - frontend/src/components/charts/MiniBulletChart.tsx (the component API at lines 90-103 -- props: value / neutralMin / neutralMax / domain / center / ciLow / ciHigh / barColor / ariaLabel)
    - frontend/src/components/popovers/MetricStatPopover.tsx (the popover convention -- per-bullet popover used since Phase 85.1)
    - frontend/src/lib/pressureBulletConfig.ts (the new config module from Plan 05 -- import all 5 exports)
    - frontend/src/lib/theme.ts (UNRELIABLE_OPACITY, MIN_GAMES_FOR_RELIABLE_STATS)
    - frontend/src/lib/significance.ts (isConfident + deriveLevel)
    - frontend/src/generated/endgameZones.ts (PRESSURE_BIN_SCORE_NEUTRAL_ZONES + CLOCK_GAP_NEUTRAL_MIN/MAX)
    - frontend/src/types/api.ts (the new types from Task 1)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 7 -- exact imports, sparse-handling, triple-gate, data-testid naming convention)
  </read_first>
  <action>
    Create `frontend/src/components/charts/EndgameTimePressureCard.tsx`. Add two module-level constants at the top:
    - `MIN_GAMES_PER_TC_CARD = 20` (matches backend gate).
    - `MIN_GAMES_PER_PRESSURE_BIN = 5` (matches backend gate).

    Component signature: `export function EndgameTimePressureCard({ card }: { card: TimePressureTcCard })` returning `JSX.Element | null`.

    Body structure (mirror `EndgameTypeCard.tsx`):

    1. **TC-level hide:** `if (card.total < MIN_GAMES_PER_TC_CARD) return null;`
    2. **Card root:** wrap in the same Card/CardContent shell used by `EndgameTypeCard` (or whatever Card primitive the analog uses). Apply `data-testid={`time-pressure-card-${card.tc}`}` on the root.
    3. **Title row:** TC name (e.g. "Bullet", "Blitz", "Rapid", "Classical") -- planner picks formatting from CONTEXT.md Claude's Discretion (TC name only is the suggestion; the executor MAY add a total-games subtitle if consistent with the existing per-TC sibling). Include an `InfoPopover` describing the card-level interpretation.
    4. **Clock Gap bullet row** (always renders when card renders):
       - `data-testid={`time-pressure-card-${card.tc}-clock-gap`}`
       - Label: "Clock gap" or "Time gap vs opponent" (planner picks; consistent with the existing `EndgameClockPressureSection` label if appropriate).
       - `<MiniBulletChart value={card.clock_gap.mean_diff_pct} center={0} neutralMin={CLOCK_GAP_NEUTRAL_MIN} neutralMax={CLOCK_GAP_NEUTRAL_MAX} domain={CLOCK_GAP_DOMAIN} ciLow={card.clock_gap.ci_low ?? undefined} ciHigh={card.clock_gap.ci_high ?? undefined} ariaLabel="..." />`.
       - `MetricStatPopover` per the per-bullet convention. Triple-gate via `isConfident(deriveLevel(card.clock_gap.p_value, card.clock_gap.n))` AND outside-neutral check for font coloring.
    5. **Score-Delta bullet rows (5x, one per quintile, ordered Q0..Q4):**
       - For each `bin` in `card.quintiles` (length must equal 5; assert defensively or trust the contract from Plan 04):
         - Axis tick + label (e.g. "0-20%", "20-40%", "40-60%", "60-80%", "80-100%") -- always rendered to preserve uniform card height.
         - If `bin.n === 0`: render a dash (`<span className="text-muted-foreground text-sm">no games</span>` or similar) inline with the label slot. NO bullet glyph. Apply `data-testid={`time-pressure-card-${card.tc}-bin-${bin.quintile_index}-empty`}`.
         - Else: render a bullet row. Compute `binStyle: CSSProperties | undefined = bin.n > 0 && bin.n < MIN_GAMES_PER_PRESSURE_BIN ? { opacity: UNRELIABLE_OPACITY } : undefined`. Look up `neutralBand = PRESSURE_BIN_SCORE_NEUTRAL_ZONES[card.tc][bin.quintile_index as 0 | 1 | 2 | 3 | 4]`. Render `<MiniBulletChart value={bin.delta} center={PRESSURE_DELTA_CENTER} neutralMin={neutralBand.min} neutralMax={neutralBand.max} domain={PRESSURE_DELTA_DOMAIN} ciLow={bin.ci_low != null ? clampDeltaCi(bin.ci_low) : undefined} ciHigh={bin.ci_high != null ? clampDeltaCi(bin.ci_high) : undefined} ariaLabel="..." />` wrapped in a div with `style={binStyle}`. Apply `data-testid={`time-pressure-card-${card.tc}-bin-${bin.quintile_index}`}`. When `bin.n > 0 && bin.n < MIN_GAMES_PER_PRESSURE_BIN`, also render an `n=${bin.n}` chip inline with `data-testid={`time-pressure-card-${card.tc}-bin-${bin.quintile_index}-n`}`.
         - **Triple-gate font coloring:** compute `level = deriveLevel(bin.p_value, bin.n)`; `isInColoredZone = bin.delta >= neutralBand.max || bin.delta <= neutralBand.min`; `showFontColor = bin.n >= MIN_GAMES_PER_PRESSURE_BIN && isConfident(level) && isInColoredZone`. Apply via `pressureDeltaZoneColor(...)` when `showFontColor` is true.

    6. **Per-bullet popovers:** every bullet row carries a `MetricStatPopover` reading the bin's n / p_value / CI / cohort_score. Popover copy follows the existing convention -- short, no jargon, "what + sign convention only" (per the popover-copy memory). Lookup tables are fine.

    Imports:
    ```
    import type { CSSProperties } from 'react';
    import { MiniBulletChart } from '@/components/charts/MiniBulletChart';
    import { MetricStatPopover } from '@/components/popovers/MetricStatPopover';
    import { InfoPopover } from '@/components/...';  // whatever the EndgameTypeCard analog uses
    import {
      PRESSURE_BIN_SCORE_NEUTRAL_ZONES,
      CLOCK_GAP_NEUTRAL_MIN,
      CLOCK_GAP_NEUTRAL_MAX,
    } from '@/generated/endgameZones';
    import {
      PRESSURE_DELTA_CENTER,
      PRESSURE_DELTA_DOMAIN,
      CLOCK_GAP_DOMAIN,
      clampDeltaCi,
      pressureDeltaZoneColor,
    } from '@/lib/pressureBulletConfig';
    import { isConfident, deriveLevel } from '@/lib/significance';
    import { UNRELIABLE_OPACITY } from '@/lib/theme';
    import type { TimePressureTcCard } from '@/types/api';
    ```

    Constraints from CLAUDE.md: every interactive element has `data-testid` (kebab-case, prefixed `time-pressure-card-`); icon-only buttons have `aria-label`; semantic HTML (`<button>`, `<section>` if applicable); minimum font size `text-sm` for all visible labels (popover bodies may use `text-xs`); no hex color literals (use `@/lib/theme` and `pressureBulletConfig`); apply changes for mobile too (this is single layout though; small-screen styling reuses Card + Tailwind responsive utils).

    Logic-LOC limit of 200 from CLAUDE.md: if the body exceeds 200 lines, extract `ClockGapRow`, `QuintileRow`, and `EmptyBinRow` as sibling sub-components in the same file. Vertical-nesting limit of 3.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit src/components/charts/EndgameTimePressureCard.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/charts/EndgameTimePressureCard.tsx` exists.
    - `grep -c "export function EndgameTimePressureCard\|export const EndgameTimePressureCard" frontend/src/components/charts/EndgameTimePressureCard.tsx` returns 1.
    - `grep -c "MIN_GAMES_PER_TC_CARD\|MIN_GAMES_PER_PRESSURE_BIN" frontend/src/components/charts/EndgameTimePressureCard.tsx` >= 2.
    - `grep -c "PRESSURE_BIN_SCORE_NEUTRAL_ZONES\|CLOCK_GAP_NEUTRAL_MIN\|CLOCK_GAP_NEUTRAL_MAX" frontend/src/components/charts/EndgameTimePressureCard.tsx` >= 3.
    - `grep -cE "data-testid=\"time-pressure-card-" frontend/src/components/charts/EndgameTimePressureCard.tsx` >= 3 (root + clock-gap + at least one bin testid).
    - `grep -c "text-xs" frontend/src/components/charts/EndgameTimePressureCard.tsx` returns 0 (or only inside popover bodies; verify by grep -A1 if any hits).
    - `cd frontend && npx tsc --noEmit src/components/charts/EndgameTimePressureCard.tsx` exits 0.
  </acceptance_criteria>
  <done>Card component renders all 6 bullets with sparse handling, triple-gate, popovers, and data-testid coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EndgameTimePressureCard.test.tsx</name>
  <files>frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx (whole file; specifically the boilerplate at lines 1-58 -- copy exactly: vitest-environment jsdom, beforeAll matchMedia + ResizeObserver stubs, afterEach cleanup)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (from Task 2)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-PATTERNS.md (Layer 8 -- key scenarios listed)
    - .planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-RESEARCH.md (Validation Architecture: Frontend sparse-bin rendering contract -- 6 scenarios)
  </read_first>
  <behavior>
    - When `card.total < 20` (MIN_GAMES_PER_TC_CARD): component returns null; root `data-testid="time-pressure-card-bullet"` is absent.
    - When `card.total >= 20`: root testid present.
    - When `bin.n === 0`: empty testid `time-pressure-card-{tc}-bin-{q}-empty` is present and bullet glyph is absent for that bin.
    - When `0 < bin.n < 5`: bullet renders at `UNRELIABLE_OPACITY`; n-chip testid `time-pressure-card-{tc}-bin-{q}-n` is present.
    - When `bin.n >= 5` but `p_value === null` or > 0.05: bullet renders at full opacity; no zone-colored font class on the delta label.
    - When `bin.n >= 5` AND `p_value < 0.05` AND `delta` outside neutral band: zone-colored font class applied to the delta label (assert via class presence or computed style).
    - Clock Gap bullet testid `time-pressure-card-{tc}-clock-gap` is present whenever the card is visible (regardless of bin sparsity).
  </behavior>
  <action>
    Create `frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx`. Copy the `// @vitest-environment jsdom` directive and the `beforeAll` / `afterEach` boilerplate from `EndgameTypeCard.test.tsx` verbatim (matchMedia stub, ResizeObserver stub, cleanup).

    Provide a builder helper `makeCard(overrides?: Partial<TimePressureTcCard>): TimePressureTcCard` that returns a fully-populated card with sensible defaults (e.g. `tc: 'bullet', total: 100, clock_gap: { n: 100, mean_diff_pct: 0, p_value: 0.5, ci_low: -0.02, ci_high: 0.02 }, quintiles: [...5 bins...]`). Provide `makeBin(quintile_index, n, delta, p_value)` for per-bin overrides.

    Implement at minimum these test methods (per the `<behavior>` block):
    - `hides card when total < MIN_GAMES_PER_TC_CARD`
    - `renders dash for n=0 bin, preserving slot`
    - `dims bullet at UNRELIABLE_OPACITY when 0 < bin.n < MIN_GAMES_PER_PRESSURE_BIN`
    - `renders n=X chip on dimmed bullet rows`
    - `applies font color when triple-gate passes`
    - `no font color when p_value above 0.05`
    - `no font color when delta inside neutral band even if p < 0.05`
    - `always renders clock-gap bullet when card is visible`

    Use `screen.queryByTestId` / `screen.getByTestId` assertions. For dimming check, query the bin row and assert its inline `style.opacity` equals `UNRELIABLE_OPACITY`. For the color-gate test, assert against the className or computed-style of the delta label.

    Tests MUST pass `cd frontend && npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` and `cd frontend && npx tsc --noEmit src/components/charts/__tests__/EndgameTimePressureCard.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` exits 0 with >= 7 tests passing.
    - `grep -c "hides card\|renders dash\|dims bullet\|applies font color\|always renders clock-gap" frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` >= 5.
    - `grep -c "MIN_GAMES_PER_TC_CARD\|MIN_GAMES_PER_PRESSURE_BIN" frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` >= 1 (test references the constants symbolically rather than hardcoded 20 / 5).
  </acceptance_criteria>
  <done>Vitest suite green; behaviour from `<behavior>` block enforced; no hard-coded magic-number assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> client component | `card` prop comes from `EndgameOverviewResponse.time_pressure_cards` which is server-validated by Pydantic. Frontend trusts the shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-06-01 | Tampering | `card.quintiles` array length != 5 (server contract violation) | mitigate | Component should be defensive: if `quintiles.length < 5` render only what is present. Test `renders dash for n=0 bin` validates the per-bin path; contract violation surfaces as missing rows |
| T-88-06-02 | Information Disclosure | Misleading triple-gate (font color suggests significance when bin.n is below threshold) | mitigate | n-gate is the first short-circuit: `showFontColor = bin.n >= MIN_GAMES_PER_PRESSURE_BIN && isConfident(level) && isInColoredZone` -- the test `no font color when p_value above 0.05` and the dimming behaviour together enforce this |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 across the new files.
- `cd frontend && npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` exits 0.
- Component imports compile (depends on Plans 03, 04, 05 having landed).
- All data-testid values follow the documented naming convention.
</verification>

<success_criteria>
- Card component matches v1.17 doctrine (6 bullets, sparse handling, triple-gate).
- Vitest suite green covering all behaviour scenarios.
- TS types align with backend Pydantic shapes.
</success_criteria>

<output>
Create `.planning/milestones/v1.17-phases/88-time-pressure-stats-rework/88-06-SUMMARY.md` when done.
</output>
</content>
</invoke>