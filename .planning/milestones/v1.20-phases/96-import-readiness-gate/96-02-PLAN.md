---
phase: 96-import-readiness-gate
plan: 02
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - frontend/src/components/EndgamesProcessingState.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/Import.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/__tests__/Endgames.readinessGate.test.tsx
  - frontend/src/pages/__tests__/Import.stateMachine.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "First-import users hitting /openings or /overview are redirected to /import (route gate keyed off tier1)"
    - "Endgames page renders a whole-page processing state when tier2 is false (first-import AND incremental, one component) per D-01/D-02"
    - "Endgames nav link stays clickable during an incremental import; the page carries the locked state"
    - "Import page shows an in-page Explore Openings CTA at Tier 1 and never says Import complete at hot-import done"
    - "A sonner Explore Endgames action toast fires once when tier2 transitions false->true, suppressed when already on /endgames"
  artifacts:
    - path: "frontend/src/components/EndgamesProcessingState.tsx"
      provides: "Whole-page Endgames locked state (D-01/D-02)"
      contains: "endgames-processing-state"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "in-place tier2 gate at top of page"
      contains: "useReadiness"
    - path: "frontend/src/pages/Import.tsx"
      provides: "readiness-driven state machine + Explore Openings CTA"
      contains: "btn-explore-openings"
    - path: "frontend/src/App.tsx"
      provides: "tier1 route/nav gate + tier2 toast"
      contains: "useReadiness"
  key_links:
    - from: "frontend/src/App.tsx ImportRequiredRoute"
      to: "useReadiness tier1"
      via: "replaces profileHasCompletedImport as redirect signal"
      pattern: "tier1"
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgamesProcessingState"
      via: "early return when !tier2"
      pattern: "EndgamesProcessingState"
---

<objective>
Wire the Tier-1 route/nav gate and the Tier-2 Endgames lock against the `useReadiness`
contract from Plan 01. Swap `profileHasCompletedImport()` for `readiness.tier1` as the
redirect/nav-lock signal, add the whole-page `EndgamesProcessingState` (one component for
first-import and incremental per D-01/D-02), drive the Import page as a readiness state
machine with the in-page "Explore Openings" CTA, and add the fire-once Tier-2
"Explore Endgames" sonner action toast.

Purpose: Hold first-import users on /import until Tier 1, lock Endgames whole-page until
Tier 2, and unlock via user-initiated CTAs + reactive poll reveal — no forced reload
(Constraints 2, 3, 4; D-01, D-02).
Output: Working route gating, Endgames lock, import state machine, and Tier-2 toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/notes/import-readiness-gate.md
@.planning/phases/96-import-readiness-gate/96-CONTEXT.md
@.planning/phases/96-import-readiness-gate/96-RESEARCH.md
@.planning/phases/96-import-readiness-gate/96-PATTERNS.md
@.planning/phases/96-import-readiness-gate/96-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EndgamesProcessingState component + wire Endgames in-place tier2 gate</name>
  <files>frontend/src/components/EndgamesProcessingState.tsx, frontend/src/pages/Endgames.tsx, frontend/src/pages/__tests__/Endgames.readinessGate.test.tsx</files>
  <read_first>
    - frontend/src/components/EvalCoverageHeader.tsx (lines 16, 21-30 — amber accent classes + Cpu counter copy idiom + `Math.max(totalCount - pendingCount, 0)` derivation)
    - frontend/src/pages/Endgames.tsx (top of component — where the gate early-return goes; current structure)
    - frontend/src/hooks/useReadiness.ts (the hook contract from Plan 01: returns {tier1, tier2, pendingCount, totalCount})
    - .planning/phases/96-import-readiness-gate/96-UI-SPEC.md (EndgamesProcessingState Component Inventory + Copywriting Contract)
    - .planning/phases/96-import-readiness-gate/96-PATTERNS.md (EndgamesProcessingState + Endgames.tsx sections)
  </read_first>
  <action>
    Create `frontend/src/components/EndgamesProcessingState.tsx`. Props `{ pendingCount: number; totalCount: number }`. Centered full-height flex block (page viewport minus nav). Elements per UI-SPEC: `Cpu` from lucide-react `h-8 w-8 text-amber-600 animate-pulse`; heading "Analyzing endgames" in `text-xl font-semibold text-foreground`; subtext "Stockfish: {analysedCount} / {totalCount} games" using `analysedCount = Math.max(totalCount - pendingCount, 0)` and `.toLocaleString()` (bold the numbers via `<strong>`), `text-sm text-muted-foreground`; body line "Endgame analysis will be available once Stockfish finishes evaluating your games." in `text-sm text-muted-foreground`. Wrapper carries `data-testid="endgames-processing-state"`. NO CTA button. One component used for both first-import and incremental (D-01/D-02) — no returning-user-specific copy. Amber accent matches EvalCoverageHeader (`border-amber-400/40 bg-amber-50/60 text-amber-700` family / `text-amber-600` icon). Use a heading element and semantic HTML.

    In `frontend/src/pages/Endgames.tsx` add at the TOP of the page component: `const { tier2, pendingCount, totalCount } = useReadiness();` then `if (!tier2) { return <EndgamesProcessingState pendingCount={pendingCount} totalCount={totalCount} />; }`. This is the whole-page lock — uniform for first-import and incremental (D-01/D-02).

    Create `frontend/src/pages/__tests__/Endgames.readinessGate.test.tsx`: mock `useReadiness`; assert (1) when tier2=false the `endgames-processing-state` testid renders and the real Endgames content does NOT; (2) when tier2=true the processing state is absent and real content renders; (3) subtext shows the analysed/total counts.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run Endgames.readinessGate</automated>
  </verify>
  <acceptance_criteria>
    - `EndgamesProcessingState.tsx` wrapper has `data-testid="endgames-processing-state"`, renders heading text "Analyzing endgames", and contains NO button element
    - `EndgamesProcessingState.tsx` subtext renders `Math.max(totalCount - pendingCount, 0)` and `totalCount`
    - `Endgames.tsx` early-returns `<EndgamesProcessingState>` when `!tier2` from `useReadiness`
    - Test asserts processing state present when tier2=false and absent when tier2=true
    - `cd frontend && npm test -- --run Endgames.readinessGate` passes
  </acceptance_criteria>
  <done>EndgamesProcessingState renders the locked state; Endgames page gates whole-page on tier2; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Swap App.tsx gate signal to tier1 + add fire-once Tier-2 toast</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (lines 40-42 `profileHasCompletedImport`; line 107 + mobile-nav/drawer `locked`/`hasCompletedImport` usages; `ImportRequiredRoute` lines 453-471; `ImportJobWatcher` transition effect lines 46-56; `useQueryClient` in AppRoutes ~line 496; `toast` import line 8; `hasRestoredRef` token-reset pattern)
    - frontend/src/hooks/useReadiness.ts (contract from Plan 01)
    - frontend/src/hooks/useEvalCoverage.ts (the `evalCompletionReloadFired` module dedup idiom referenced as the toast model — but the toast uses a useRef, not a module bool)
    - .planning/phases/96-import-readiness-gate/96-RESEARCH.md (Wiring Decision 3 ImportRequiredRoute swap; Pattern 1 toast lines 402-422; Pitfall 2 isLoading guard; Pitfall 4 useRef transition guard)
    - .planning/phases/96-import-readiness-gate/96-UI-SPEC.md (Tier-2 toast section + Interaction Contracts nav rules)
  </read_first>
  <action>
    Replace the `profileHasCompletedImport()` gating signal with `readiness.tier1` from `useReadiness()`. In `ImportRequiredRoute`, keep the structure and the `isLoading` early-return (use `readiness.isLoading` to prevent content flash, Pitfall 2): redirect to `/import` via `<Navigate to="/import" replace />` when `!isLoading && !readiness.tier1`. Re-point every nav-lock `locked`/`hasCompletedImport` usage (desktop NavHeader line ~107/125, mobile bottom nav, AND the drawer — apply to all three per CLAUDE.md "always apply to mobile too") at `readiness.tier1`. The `*_last_sync_at` timestamps stay available for `formatLastSync` display; only the GATING stops using them. `profileHasCompletedImport` can be removed if no other consumer remains; otherwise inline its replacement.

    Note: the Endgames nav link stays ENABLED during an incremental import (tier1=true) — Tier-2 gating happens in-page (Task 1), not at the nav. Only first-import users (tier1=false) get all non-import links disabled (existing behavior, new signal source).

    Add the Tier-2 toast watcher in `AppRoutes` (where `useLocation`, `navigate`, `useQueryClient` are already in scope). Use a `useRef` `wasTier2False` (NOT a module boolean): only fire when `tier2` transitions false→true in-session; if `tier2` is already true on the first fetch, never fire. Suppress when `location.pathname.startsWith('/endgames')`. Reset the ref on token change (mirror `hasRestoredRef`). On fire: `toast('Endgame analysis complete!', { action: { label: 'Explore Endgames', onClick: () => { navigate('/endgames'); queryClient.invalidateQueries({ queryKey: ['endgameOverview'] }); } } })`. Copy strings exactly per UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` `ImportRequiredRoute` redirects based on `readiness.tier1` (grep shows `tier1` used in the redirect condition) and retains an `isLoading` early-return
    - All three nav surfaces (desktop, mobile bottom nav, drawer) derive `locked` from `readiness.tier1`, not `profileHasCompletedImport`
    - A `useRef`-guarded Tier-2 toast exists that calls `toast('Endgame analysis complete!'` with an action label `'Explore Endgames'`
    - The toast effect checks `location.pathname.startsWith('/endgames')` for suppression
    - `App.tsx` contains NO `window.location.reload`
    - `cd frontend && npm run lint` and `npx tsc --noEmit` both pass
  </acceptance_criteria>
  <done>Route + nav gates keyed off tier1; Tier-2 toast fires once on transition, suppressed on /endgames; no reload.</done>
</task>

<task type="auto">
  <name>Task 3: Drive Import page as a readiness state machine with Explore Openings CTA</name>
  <files>frontend/src/pages/Import.tsx, frontend/src/pages/__tests__/Import.stateMachine.test.tsx</files>
  <read_first>
    - frontend/src/pages/Import.tsx (current `progressText` block lines 91-95; the polling-interval invalidation block lines 73-81 that invalidates ['imports','eval-coverage'] and ['endgameOverview'])
    - frontend/src/hooks/useReadiness.ts (contract from Plan 01)
    - .planning/phases/96-import-readiness-gate/96-UI-SPEC.md (Import Page State Machine table + Copywriting Contract — copy copy strings verbatim)
    - .planning/phases/96-import-readiness-gate/96-RESEARCH.md (Import-page state machine table lines 530-541, Constraint 3 over-claim fix)
  </read_first>
  <action>
    Replace the `isDone` branch of `progressText` that over-claims completion ("Imported N games from {platform}", Constraint 3 violation). Drive the page off `useReadiness` per the UI-SPEC state-machine table. States and exact copy (no em-dashes, no "complete" until Tier 2): "No games yet" (totalCount==0, no active job) = existing import form; "Importing" (active job) = existing `ImportProgressBar`; "Tier 1 reached" (tier1=true, tier2=false) heading "Games imported. Openings ready." with an in-page CTA `<Button variant="default" data-testid="btn-explore-openings">Explore Openings</Button>` that navigates to `/openings` (NOT a toast — user is already on the import page, Constraint 4); "Analyzing endgames" (tier1=true, tier2=false, pendingCount>0) below the CTA show "Analyzing endgames ({analysedCount} / {totalCount})" with `analysedCount = Math.max(totalCount - pendingCount, 0)`; "Fully ready" (tier2=true) "Ready. All analysis complete." (nav fully unlocked, no forced navigation).

    Add `['imports','readiness']` to the existing polling-interval `invalidateQueries` set (lines 73-81) so the import page reacts to tier transitions alongside the existing eval-coverage + endgameOverview invalidations.

    Use exact UI-SPEC copy strings. Match the analysing-count derivation used in EndgamesProcessingState.

    Create `frontend/src/pages/__tests__/Import.stateMachine.test.tsx`: mock `useReadiness` (and import polling as needed). Assert (1) at Tier 1 (tier1=true, tier2=false) the `btn-explore-openings` CTA renders and the copy does NOT contain the word "complete"; (2) at Tier 1 with pendingCount>0 the "Analyzing endgames (X / Y)" text shows; (3) the hot-import done message does not claim completion.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run Import.stateMachine</automated>
  </verify>
  <acceptance_criteria>
    - `Import.tsx` no longer renders "Imported {N} games" as the hot-import-done message; the Tier-1 copy is "Games imported. Openings ready."
    - An in-page CTA with `data-testid="btn-explore-openings"` and `variant="default"` navigates to `/openings`
    - The "Analyzing endgames ({X} / {Y})" copy renders when `tier1 && !tier2 && pendingCount > 0`
    - The polling invalidation set includes `['imports','readiness']`
    - Test asserts the Tier-1 CTA renders and no "complete" appears at hot-import done; `cd frontend && npm test -- --run Import.stateMachine` passes
  </acceptance_criteria>
  <done>Import page is a readiness state machine; Explore Openings CTA at Tier 1; no over-claimed completion; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client router → gated routes | Route access decided client-side from `readiness` flags; the authoritative gate is still the backend (data endpoints are user-scoped). Client gating is UX, not a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Information Disclosure | client-side route gate | accept | Route gating is UX-only; bypassing it cannot expose another user's data because every data endpoint is independently user-scoped (Plan 01 T-96-01). A user bypassing the Endgames gate sees only their own (possibly partial) data — the gate exists to avoid misleading THEM, not to protect others. |
| T-96-05 | Tampering | tier2 toast / reactive reveal | mitigate | No `window.location.reload()` (Constraint 4 / SC-5) — grep App.tsx confirms absence. Reveal is via poll + query invalidation only, no forced full-page reload that could lose state. |
| T-96-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `cd frontend && npm run lint` — clean
- `cd frontend && npx tsc --noEmit` — zero errors
- `cd frontend && npm test -- --run Endgames.readinessGate Import.stateMachine` — green
- grep `frontend/src/App.tsx` confirms no `window.location.reload`
- Manual: first-import user redirected to /import on /openings; incremental user sees Endgames processing state but Openings stays usable
</verification>

<success_criteria>
- First-import users are held on /import (tier1 gate); incremental users keep Openings/Overview usable.
- Endgames renders the whole-page processing state until tier2, identical for first-import and incremental (D-01/D-02).
- Import page never over-claims completion; Tier-1 Explore Openings CTA present; Tier-2 toast fires once.
</success_criteria>

<output>
Create `.planning/phases/96-import-readiness-gate/96-02-SUMMARY.md` when done.
</output>
