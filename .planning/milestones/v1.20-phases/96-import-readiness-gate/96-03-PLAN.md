---
phase: 96-import-readiness-gate
plan: 03
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - frontend/src/components/stats/EvalCpuPlaceholder.tsx
  - frontend/src/components/stats/OpeningStatsCard.tsx
  - frontend/src/components/insights/EvalConfidenceTooltip.tsx
  - frontend/src/components/insights/BulletConfidencePopover.tsx
  - frontend/src/hooks/useEvalCoverage.ts
  - frontend/src/components/stats/__tests__/EvalCpuPlaceholder.test.tsx
  - frontend/src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "OpeningStatsCard shows a pulsating-Cpu placeholder bar instead of the eval row when tier2 is false (Constraint 6)"
    - "The WDL score row in OpeningStatsCard is unaffected by tier2 (only eval-dependent rows are gated)"
    - "EvalConfidenceTooltip no longer renders the live eval counter / pending caveat (Constraint 7)"
    - "useEvalCoverage no longer calls window.location.reload (Constraint 4 / SC-5) but still feeds EvalCoverageHeader unchanged"
    - "npm run knip passes after removing the isPending/pendingCount prop surfaces"
  artifacts:
    - path: "frontend/src/components/stats/EvalCpuPlaceholder.tsx"
      provides: "inline pulsating-Cpu placeholder bar"
      contains: "eval-cpu-placeholder"
    - path: "frontend/src/components/stats/OpeningStatsCard.tsx"
      provides: "tier2-gated eval row swap"
      contains: "useReadiness"
    - path: "frontend/src/hooks/useEvalCoverage.ts"
      provides: "auto-reload-free eval-coverage hook (header still works)"
  key_links:
    - from: "frontend/src/components/stats/OpeningStatsCard.tsx"
      to: "useReadiness tier2"
      via: "replaces useEvalCoverage isPending/pendingCount"
      pattern: "tier2"
    - from: "frontend/src/components/stats/OpeningStatsCard.tsx"
      to: "EvalCpuPlaceholder"
      via: "renders when !tier2"
      pattern: "EvalCpuPlaceholder"
---

<objective>
Hide Openings eval-based metrics behind a pulsating-Cpu placeholder until Tier 2
(Constraint 6), remove the live eval counter from the eval tooltips (Constraint 7), and
retire the `window.location.reload()` auto-reload in `useEvalCoverage` (Constraint 4 / SC-5)
while keeping the hook intact for the global `EvalCoverageHeader` bar (Constraint 5).

Purpose: With the global progress bar (Constraint 5) and the per-row Cpu placeholder
(Constraint 6) now carrying the in-progress signal, the tooltip counter is redundant and the
forced reload is replaced by reactive reveal. The WDL score row stays visible because it is
not eval-dependent.
Output: EvalCpuPlaceholder component, tier2-gated OpeningStatsCard, counter-free tooltips,
reload-free useEvalCoverage, knip-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/notes/import-readiness-gate.md
@.planning/phases/96-import-readiness-gate/96-CONTEXT.md
@.planning/phases/96-import-readiness-gate/96-RESEARCH.md
@.planning/phases/96-import-readiness-gate/96-PATTERNS.md
@.planning/phases/96-import-readiness-gate/96-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EvalCpuPlaceholder + gate OpeningStatsCard eval row on tier2</name>
  <files>frontend/src/components/stats/EvalCpuPlaceholder.tsx, frontend/src/components/stats/OpeningStatsCard.tsx, frontend/src/components/stats/__tests__/EvalCpuPlaceholder.test.tsx</files>
  <read_first>
    - frontend/src/components/EvalCoverageHeader.tsx (lines 17-30 — the amber-bar container is the literal styling template for EvalCpuPlaceholder)
    - frontend/src/components/stats/OpeningStatsCard.tsx (line 1 `Cpu` import; line 9 `useEvalCoverage` import; line 60 `const { isPending, pendingCount } = useEvalCoverage()`; line 179 grid `grid-cols-[minmax(0,1fr)_auto]`; score row 180-217; eval bullet row 218-224; eval-text+popover row 225-243 incl. the `<BulletConfidencePopover>` call at 239-240)
    - frontend/src/hooks/useReadiness.ts (contract from Plan 01)
    - frontend/src/lib/theme.ts (check whether amber eval-progress constants already exist before adding)
    - .planning/phases/96-import-readiness-gate/96-UI-SPEC.md (EvalCpuPlaceholder Component Inventory + Amber Token Note)
    - .planning/phases/96-import-readiness-gate/96-PATTERNS.md (EvalCpuPlaceholder + OpeningStatsCard sections)
  </read_first>
  <action>
    Create `frontend/src/components/stats/EvalCpuPlaceholder.tsx` matching EvalCoverageHeader styling exactly: `<div className="flex items-center gap-2 rounded-md border border-amber-400/40 bg-amber-50/60 px-3 py-2 text-sm" data-testid="eval-cpu-placeholder">` containing `<Cpu className="h-4 w-4 shrink-0 text-amber-700 animate-pulse" aria-hidden="true" />` and `<span className="text-muted-foreground text-sm truncate">Analyzing…</span>`. Per CLAUDE.md theme-constant rule: if `theme.ts` already exports eval-progress amber constants, import them; if not, inline Tailwind classes are acceptable for these 2 sites (EvalCoverageHeader + EvalCpuPlaceholder) per the UI-SPEC Amber Token Note — executor's call, but do NOT hard-code amber in a third location without a constant.

    In `OpeningStatsCard.tsx`: replace the `import { useEvalCoverage } from '@/hooks/useEvalCoverage'` (line 9) with `useReadiness`; replace `const { isPending, pendingCount } = useEvalCoverage()` (line 60) with `const { tier2 } = useReadiness()`. When `!tier2`, replace BOTH the eval bullet row (218-224) AND the eval-text+popover row (225-243) with a single `<EvalCpuPlaceholder />` spanning the full 2-col grid (`grid-cols-[minmax(0,1fr)_auto]`). The score row (180-217) stays UNCHANGED — WDL is not eval-dependent. Remove the `isPending`/`pendingCount` props passed to `<BulletConfidencePopover>` (lines 239-240); those props are deleted from the popover in Task 2.

    Create `frontend/src/components/stats/__tests__/EvalCpuPlaceholder.test.tsx`: render EvalCpuPlaceholder, assert the `eval-cpu-placeholder` testid, the "Analyzing…" label, and the pulsating Cpu icon presence. Add an OpeningStatsCard case (mock `useReadiness`): when tier2=false the `eval-cpu-placeholder` renders and the eval-text row does NOT; when tier2=true the placeholder is absent and the eval metric renders; the WDL score row is present in both cases.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EvalCpuPlaceholder</automated>
  </verify>
  <acceptance_criteria>
    - `EvalCpuPlaceholder.tsx` wrapper has `data-testid="eval-cpu-placeholder"`, renders the "Analyzing…" label, and uses the amber classes `border-amber-400/40 bg-amber-50/60` with a `Cpu ... animate-pulse` icon
    - `OpeningStatsCard.tsx` imports `useReadiness` (not `useEvalCoverage`) and destructures `tier2`
    - `OpeningStatsCard.tsx` renders `<EvalCpuPlaceholder />` when `!tier2` in place of the eval bullet + eval-text rows; the score row is rendered unconditionally
    - `OpeningStatsCard.tsx` no longer passes `isPending`/`pendingCount` to `<BulletConfidencePopover>`
    - `cd frontend && npm test -- --run EvalCpuPlaceholder` passes (including the OpeningStatsCard tier2 cases)
  </acceptance_criteria>
  <done>EvalCpuPlaceholder replaces eval rows when !tier2; WDL score row unaffected; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove eval counter from EvalConfidenceTooltip + BulletConfidencePopover props</name>
  <files>frontend/src/components/insights/EvalConfidenceTooltip.tsx, frontend/src/components/insights/BulletConfidencePopover.tsx, frontend/src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx</files>
  <read_first>
    - frontend/src/components/insights/EvalConfidenceTooltip.tsx (props interface lines 55-59, destructure ~81-82, the pending-caveat JSX block lines 114-128 with `data-testid="eval-pending-caveat"`, and the `AlertTriangle` import)
    - frontend/src/components/insights/BulletConfidencePopover.tsx (interface lines 26-30, destructure lines 47-48, forwarded props on `<EvalConfidenceTooltip>` lines 104-105)
    - frontend/src/components/__tests__/Openings.statsBoard.test.tsx (~line 168 renders BulletConfidencePopover directly — confirm/remove any passed isPending/pendingCount)
    - .planning/phases/96-import-readiness-gate/96-PATTERNS.md (EvalConfidenceTooltip + BulletConfidencePopover deletion section; KNIP note)
  </read_first>
  <action>
    In `EvalConfidenceTooltip.tsx`: delete the `isPending`/`pendingCount` props from the props interface (lines 55-59), remove them from the destructure (~81-82), and delete the entire pending-caveat JSX block (lines 114-128, the element with `data-testid="eval-pending-caveat"`). Remove the now-unused `AlertTriangle` import (Constraint 7 — the global bar + Cpu placeholder carry the in-progress signal now).

    In `BulletConfidencePopover.tsx`: delete `isPending`/`pendingCount` from the interface (lines 26-30), the destructure (lines 47-48), and the forwarded props on `<EvalConfidenceTooltip>` (lines 104-105).

    In `Openings.statsBoard.test.tsx` (~line 168): remove any `isPending`/`pendingCount` passed to the directly-rendered `BulletConfidencePopover` so the removed prop surface does not trip knip (SC-8 / Pitfall 5).

    Create/extend `frontend/src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx`: assert that rendering the tooltip does NOT produce an element with `data-testid="eval-pending-caveat"` (the counter is gone) under any prop combination it still accepts.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EvalConfidenceTooltip</automated>
  </verify>
  <acceptance_criteria>
    - `EvalConfidenceTooltip.tsx` contains NO `eval-pending-caveat` testid and NO `isPending`/`pendingCount` props
    - `BulletConfidencePopover.tsx` interface contains NO `isPending`/`pendingCount`
    - `EvalConfidenceTooltip.tsx` no longer imports `AlertTriangle` (grep returns no match unless used elsewhere in the file)
    - Test asserts `data-testid="eval-pending-caveat"` is absent from the rendered tooltip
    - `cd frontend && npm test -- --run EvalConfidenceTooltip` passes
  </acceptance_criteria>
  <done>Eval counter removed from tooltip + popover prop surfaces; test confirms absence; downstream test usages updated.</done>
</task>

<task type="auto">
  <name>Task 3: Retire window.location.reload from useEvalCoverage + verify knip/build green</name>
  <files>frontend/src/hooks/useEvalCoverage.ts</files>
  <read_first>
    - frontend/src/hooks/useEvalCoverage.ts (entire file — the module-level `evalCompletionReloadFired` guard lines 9-11, the `wasPendingRef` + `useEffect` calling `window.location.reload()` lines 45-65; the returned shape lines 67-73 that EvalCoverageHeader depends on)
    - frontend/src/hooks/__tests__/useEvalCoverage.test.tsx (existing polling + default-values tests must remain valid; delete only any auto-reload test case)
    - frontend/src/components/EvalCoverageHeader.tsx (confirm it only consumes the returned `{ pendingCount, totalCount, pct, isPending, isLoading }` shape — must stay unchanged)
    - .planning/phases/96-import-readiness-gate/96-RESEARCH.md (Auto-reload retirement lines 254-259; Current Code Inventory lines 481-496)
  </read_first>
  <action>
    Delete the auto-reload entirely from `useEvalCoverage.ts`: the module-level `evalCompletionReloadFired` boolean (lines 9-11), the `wasPendingRef` `useRef`, and the `useEffect` that calls `window.location.reload()` (lines 45-65). Drop the now-unused `useEffect`/`useRef` imports if nothing else uses them. The hook MUST still return the same `{ pendingCount, totalCount, pct, isPending, isLoading }` shape so `EvalCoverageHeader` (Constraint 5) is unaffected and keeps showing on all pages. Do NOT change the polling/refetchInterval logic — only remove the reload side effect.

    In `useEvalCoverage.test.tsx`: keep the polling + default-value tests as-is; delete only the auto-reload test case if one exists. If practical, add an assertion that `window.location.reload` is not called after a pending→done transition.

    After all edits across the plan, run `npm run knip` to confirm the removed prop surfaces (Task 2) and any dead exports do not fail CI (SC-8).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run useEvalCoverage && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `useEvalCoverage.ts` contains NO `window.location.reload` and NO `evalCompletionReloadFired`
    - `useEvalCoverage.ts` still returns an object including `pendingCount`, `totalCount`, `pct`, `isPending`, `isLoading` (EvalCoverageHeader contract intact)
    - `EvalCoverageHeader.tsx` is unmodified by this task
    - `cd frontend && npm test -- --run useEvalCoverage` passes (existing polling/default tests still green)
    - `cd frontend && npm run knip` exits 0
  </acceptance_criteria>
  <done>Auto-reload removed, hook shape preserved for the header, knip clean, eval-coverage tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client poll → reactive reveal | Eval-metric reveal driven by `useReadiness` poll; no server trust boundary crossed (display-only gating of the user's own data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06 | Tampering | useEvalCoverage reload removal | mitigate | Removing `window.location.reload()` eliminates a forced full-page reload that discarded in-memory state (Constraint 4 / SC-5). Reveal is reactive via poll. Asserted by grep for absence of `window.location.reload` + `evalCompletionReloadFired`. |
| T-96-07 | Information Disclosure | partial eval data on Openings | mitigate | When `!tier2`, eval-based rows are replaced by `EvalCpuPlaceholder` so partial/biased eval metrics are not shown; the WDL score row (not eval-dependent) is intentionally shown. This is the core correctness goal of the phase. |
| T-96-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `cd frontend && npm test -- --run EvalCpuPlaceholder EvalConfidenceTooltip useEvalCoverage` — green
- `cd frontend && npm run knip` — exits 0 (SC-8)
- `cd frontend && npm run lint && npx tsc --noEmit` — clean
- grep `frontend/src/hooks/useEvalCoverage.ts` confirms no `window.location.reload`
- grep `frontend/src/components/insights/EvalConfidenceTooltip.tsx` confirms no `eval-pending-caveat`
</verification>

<success_criteria>
- Openings eval rows are hidden behind a single Cpu placeholder bar until Tier 2; WDL score stays visible.
- Eval counter removed from tooltips; the global bar + Cpu placeholder are the only in-progress signals.
- useEvalCoverage no longer force-reloads but still drives EvalCoverageHeader; knip passes.
</success_criteria>

<output>
Create `.planning/phases/96-import-readiness-gate/96-03-SUMMARY.md` when done.
</output>
