# Roadmap: v1.17 — Endgame Stats Card Redesign

**Opened:** 2026-05-12
**Source:** `.planning/notes/endgame-stats-card-redesign.md` → `.planning/REQUIREMENTS.md` (27 v1 requirements after 2026-05-12 single-bullet doctrine pivot; was 30)
**Granularity:** coarse (per `.planning/config.json`)
**Phase numbering:** continues from v1.16 (last phase 83); v1.17 starts at Phase 84.
**Design pivot 2026-05-12:** Single-bullet doctrine adopted. Cohort/p50 bullets dropped from Section 2 and Section 3 (rating-tier confound; roughly redundant with ELO). Each Conv/Parity/Recov/Skill card carries one peer bullet only. See `.planning/notes/v1.17-single-bullet-doctrine.md`. Phase 84 plans regenerated.

## Goal

Replace three table-driven sections on the Endgames page with the established WDL + ScoreBullet card pattern from `EndgameStartVsEndSection.tsx` / `OpeningStatsCard.tsx`. Single-bullet doctrine on Conv/Parity/Recov/Skill: each card carries one peer bullet (`You − Opp` vs 0) using the mirror-bucket opponent baseline. The peer frame is rating-tier-conditioned by construction via the mirror bucket, filter-responsive, and self-calibrating.

## Phase Summary

- [ ] **Phase 84: Data plumbing — mirror-rate audit** — Audit that mirror-bucket peer rates (`opponent_score` / `opponent_games`) are already exposed on `/api/endgames/overview` for Section 2 (Conv / Parity / Recov / Skill components) and Section 3 (per-type Conv / Recov). Per-type cohort p50 codegen no longer needed. UI hint: no.
- [ ] **Phase 85: Section 1 — Games with vs without Endgame cards** — Replace `EndgamePerformanceSection` table with two side-by-side cards (WDL + Score bullet vs 0.50 natural anchor) plus full-width Score Gap footer bullet. UI hint: yes.
- [ ] **Phase 85.1: Hypothesis tests + CIs for Endgame Score Differences (INSERTED 2026-05-14)** — Add p-values and 95% CIs to the two derived metrics in the Phase 85 "Endgame Score Differences" card: Endgame Score Gap (independent two-sample z) and Achievable Score Gap (paired one-sample z on per-game `actual − expected`). Surface CIs as bullet whiskers. UI hint: yes.
- [ ] **Phase 86: Section 2 — Endgame Metrics 4-card layout (Conv / Parity / Recov / Skill)** — Replace `EndgameScoreGapSection` table + 4-gauge strip with 4 cards; single peer bullet (`You − Opp` vs 0) on every card. Skill card peer bullet uses derived `Opp Skill = composite(opp_conv, opp_recov)` computed frontend-side from mirror-bucket components. UI hint: yes.
- [ ] **Phase 87: Section 3 — Per-type Endgame Type Breakdown cards** — Replace `EndgameWDLChart` and extend `EndgameConvRecovChart` into 5 full per-type cards (rook / minor_piece / pawn / queen / mixed) with side-by-side Conv+Recov gauges, WDL bar, Conv+Recov peer bullets, Games deep-link; mobile density real-device check (lower risk than pre-pivot — fewer elements per card). UI hint: yes.
- [ ] **Phase 87.1: Per-span ΔES metric for endgame types (INSERTED 2026-05-15)** — Add a per-endgame-type continuous performance metric — mean per-span expected-score delta (ΔES) — that complements the existing game-result-derived Conv/Recov rates. ΔES = `ES_sigmoid(entry_eval) − exit_score`, with terminal spans falling back to `game_result_score`. Slots in as an additional gauge on the Phase 87 per-type cards. Source: [[SEED-016]]. UI hint: yes.
- [ ] **Phase 87.2: Section 2 — eval-based ΔES Score Gap on Conv/Parity/Recov + Endgame Skill cards (INSERTED 2026-05-15)** — Retire the rate-based mirror-bucket peer-diff Gap bullet on the four Section 2 cards (Conversion, Parity, Recovery, Endgame Skill). The current peer-diff is mathematically degenerate: Conv-Gap ≡ Recov-Gap by mirror symmetry (`opp_conv_win% = user_recov_loss%`), and Parity-Gap is a deterministic affine of `user_parity` (`opp_parity ≡ 1 − user_parity` by chess-score symmetry). Replace each card's `MiniBulletChart` peer-diff row with a per-bucket eval-based ΔES Score Gap bullet (mean per-span ΔES restricted to that card's eval-entry bucket: ≥ +1 for Conv, |eval| ≤ 1 for Parity, ≤ −1 for Recov, pooled across all endgame spans for Skill). Reuses Phase 87.1's per-span ΔES infrastructure, paired-difference test, and bullet primitives. Retires `MIRROR_BUCKET`, `formatDiffPct`, `compute_skill_diff_test`, `BULLET_DOMAIN`/`NEUTRAL_ZONE_*`, and the backend mirror-bucket plumbing once the four cards switch over (knip-clean). UI hint: yes.
- [ ] **Phase 88: Time Pressure stats rework with hypothesis tests + CIs (INSERTED 2026-05-14)** — Replace Time Pressure at Endgame Entry table and Time Pressure vs Performance line chart with one bullet-stack card per TC (bullet/blitz/rapid/classical). Each card: 1 Clock Gap bullet (mean `(my_clock − opp_clock)/base_clock` vs 0%) + 5 stacked Score-Delta bullets (one per 20% pressure quintile, `user_score − cohort_score` with Wilson CI, cohort N treated as fixed reference). Pre-req inside scope: /benchmarks skill update for clock-gap-% and chess-score-per-pressure-bin. UI hint: yes.
- [ ] **Phase 89: Polish — popovers, gating decisions, automation rules, 375px parity** — Resolve POLISH decisions (peer-bullet neutral band, gauge sig gating), apply `data-testid` / ARIA / semantic HTML per CLAUDE.md, verify 375px mobile parity across all four sections (incl. new Time Pressure cards). UI hint: yes.

## Phase Details

### Phase 84: Data plumbing — mirror-rate audit

**Goal:** Audit that mirror-bucket peer rates (`opponent_score` / `opponent_games`) are already exposed on `/api/endgames/overview` for Section 2 (Conv / Parity / Recov rows, plus `opp_conv` + `opp_recov` consumed by the derived Skill peer baseline) and Section 3 (per-type Conv / Recov). Prerequisite for the peer bullets in Section 2 + 3.
**Depends on:** Nothing (first v1.17 phase)
**Requirements:** DATA-02
**Success Criteria** (what must be TRUE):
  1. A written audit confirms `opponent_score` + `opponent_games` are already populated on every `MaterialRow` (Section 2 Conv / Parity / Recov) and on each per-type breakdown (Section 3) of `/api/endgames/overview`, including the component values needed to derive `Opp Skill` frontend-side — or, if any are missing, a minimal backend extension is shipped to expose them.
**Plans:** TBD (prior plans deleted 2026-05-12 after single-bullet doctrine pivot; supporting docs in `84-data-plumbing-per-type-cohort-p50-and-mirror-rate-audit/` reflect pre-pivot assumptions and should be regenerated on next `/gsd-plan-phase`)
**Context:** The Section 2 `MaterialRow` schema already carries `opponent_score` + `opponent_games`, so DATA-02 is almost certainly an audit-only confirmation; per-type mirror data needs verification too. Phase may collapse into Phase 86 plan-phase as a precheck if confirmed already-present. The per-type cohort p50 codegen extension (formerly DATA-01) is dropped — cohort bullets removed from Sections 2 + 3 per the single-bullet doctrine. The phase directory name still references the pre-pivot scope and can be renamed when the phase is re-planned.

### Phase 85: Section 1 — Games with vs without Endgame cards

**Goal:** Replace the `EndgamePerformanceSection` table with two side-by-side cards (No / Yes) on lg+, stacked on mobile. Each card carries a WDL bar + Score row + score bullet vs 0.50 (natural balanced-WDL anchor, not a population statistic). A full-width Score Gap footer bullet (Yes − No vs 0) spans both cards. Significance gating follows `EndgameStartVsEndSection` convention (`n ≥ 10` ∧ `p < 0.05` ∧ value outside neutral band).
**Depends on:** Phase 84 (mirror-rate audit complete — not strictly blocking since Section 1 uses no peer bullet)
**Requirements:** SEC1-01, SEC1-02, SEC1-03, SEC1-04, SEC1-05, SEC1-06, SEC1-07
**Success Criteria** (what must be TRUE):
  1. On lg+ the Endgames page shows two side-by-side cards labelled "Games without Endgame" / "Games with Endgame", stacked on mobile (375px parity).
  2. Each card shows its WDL bar and a Score row with chess score `(W + 0.5·D)/n` + Wilson CI whiskers + score bullet centred at 0.50 with the standard neutral band.
  3. A full-width footer Score Gap bullet (Yes − No, centre 0, neutral band `[SCORE_GAP_NEUTRAL_MIN, SCORE_GAP_NEUTRAL_MAX]`) spans both cards.
  4. Card score-row font color paints only when `n ≥ MIN_GAMES_FOR_RELIABLE_STATS` AND `p < 0.05` AND value lands outside the neutral band; an `InfoPopover` on each Score row explains that 0.50 is the balanced-WDL natural anchor (no rating-tier confound applies — this is conceptually different from the dropped global p50 frame).
  5. The legacy `EndgamePerformanceSection` table component is removed from the codebase (knip clean).
**Plans:** 4 plans
- [ ] 85-01-PLAN.md — Backend: add non_endgame_score_p_value field + service mirror + test (D-01)
- [ ] 85-02-PLAN.md — Frontend type extension + extract EndgameScoreOverTimeChart to its own file (D-09)
- [ ] 85-03-PLAN.md — Build EndgameGamesWithWithoutSection (two cards + footer gap bullet) + tests
- [ ] 85-04-PLAN.md — Mount swap in Endgames.tsx + delete legacy section + knip/build verification (SEC1-07)
**UI hint:** yes

### Phase 85.1: Hypothesis tests + CIs for Endgame Score Differences (INSERTED 2026-05-14)

**Goal:** Add two-sided p-values and 95% CIs to the two derived metrics in the Phase 85 "Endgame Score Differences" card. Endgame Score Gap (Endgame Score − Non-Endgame Score) is an **independent two-sample** z-test on the chess-score difference, using the trinomial empirical variance per side. Achievable Score Gap (Endgame Score − Achievable Score) is a **paired one-sample** z-test on per-game differences `d_i = actual_score_i − expected_score_i` over the same `bucket_rows` cursor that already feeds `entry_expected_score`. Surface CIs as bullet whiskers on each `ScoreGapRow`. Optionally tighten the gap font color via the new significance signal (reopens Phase 85 D-04 zone-only coloring) — decision recorded in plan-phase.

**Depends on:** Phase 85 (3-card composite section, `ScoreGapRow`, and `EndgameOverallPerformanceSection` shipped).

**Requirements:** SEC1-08, SEC1-09, SEC1-10, SEC1-11

**Success Criteria** (what must be TRUE):
  1. `ScoreGapMaterialResponse` carries `score_difference_p_value`, `score_difference_ci_low`, `score_difference_ci_high` computed from the independent two-sample z-test on `endgame_score − non_endgame_score`, with the gating policy from SEC1-08 / SEC1-09.
  2. `EndgamePerformanceResponse` carries `achievable_score_gap`, `achievable_score_gap_p_value`, `achievable_score_gap_ci_low`, `achievable_score_gap_ci_high` computed from the paired z-test on per-game `(actual_i − expected_i)` over the existing `bucket_rows` cursor, with the gating policy from SEC1-10. The frontend derivation in `EndgameOverallPerformanceSection.tsx:80-81` is removed in favor of the server value.
  3. Test math lives in `app/services/score_confidence.py` (or a sibling) as two new helpers — `compute_score_difference_test(eg_w, eg_d, eg_l, eg_n, ne_w, ne_d, ne_l, ne_n)` returning `(p_value, ci_low, ci_high)`, and `compute_paired_difference_test(diffs: Sequence[float])` returning `(mean_d, p_value, ci_low, ci_high)`. Single source of truth, unit-tested at boundaries (n=0, all-wins both sides, all-equal pairs, n=1 paired).
  4. Both `ScoreGapRow` rows in `EndgameOverallPerformanceSection` render 95% CI whiskers on the bullet, mirroring the Score-row whiskers from Phase 85 (SEC1-03).
  5. The plan-phase decision log records whether Phase 85 D-04 (gap font color is zone-only) is reopened. If kept, sig + CI are informational only via the existing `InfoPopover`. If reopened, the same triple gating used elsewhere in v1.17 applies (`n ≥ MIN_GAMES_FOR_RELIABLE_STATS` AND `p < 0.05` AND value outside neutral band).
  6. Tests cover the new test helpers, the service-layer wiring, the response-schema gates (None vs value at the n thresholds), and the frontend CI whisker rendering.

**Plans:** 4 plans
- [ ] 85.1-01-PLAN.md — Backend math helpers: `compute_score_difference_test` + `compute_paired_difference_test` in `score_confidence.py` with boundary unit tests
- [ ] 85.1-02-PLAN.md — Service wiring + schema fields + `PVALUE_RELIABILITY_MIN_N` extraction (IN-01); paired-diff loop merged into existing `ex_sum`/`ex_n` accumulator
- [ ] 85.1-03-PLAN.md — Frontend TS types + CI whisker prop threading + server-side `achievable_score_gap` migration (delete `EndgameOverallPerformanceSection.tsx:80-81` derivation); folds in WR-04 + IN-02
- [ ] 85.1-04-PLAN.md — D-04 reopen decision (recommend keep zone-only) + InfoPopover sigmoid-bias caveat + verification sweep

**UI hint:** yes

**Context:** Math is straightforward — both tests are textbook and the codebase already has the trinomial empirical variance (`compute_confidence_bucket`) and Wilson CI helper (`wilson_bounds`). Two scope risks: (a) Phase 85 D-04 explicitly chose zone-only coloring for gaps; this phase reopens that, which is a normal in-milestone iteration but worth flagging. (b) The Achievable test treats Stockfish-derived expected scores (Lichess sigmoid) as ground truth; sigmoid bias per rating bucket would surface as user-skill signal. Document the caveat in the InfoPopover but don't block on it. Future iteration FUT-05 could quantify sigmoid bias on the benchmark cohort and adjust the test, but that's a benchmarks-skill rework.

### Phase 86: Section 2 — Endgame Metrics 4-card layout (Conv / Parity / Recov / Skill)

**Goal:** Replace `EndgameScoreGapSection` table + 4-gauge strip with 4 side-by-side cards on lg+, stacked on mobile, in order Conversion → Parity → Recovery → Endgame Skill. Conv/Parity/Recov share an identical layout (gauge → percent + games → WDL bar → single peer bullet `You − Opp` vs 0). Skill card has no WDL bar; its peer bullet uses derived `Opp Skill = composite(opp_conv, opp_recov)` computed frontend-side using the same Skill composite formula as `Your Skill`. Mirror-bucket opponent baseline logic (`opponentRate` / `MIRROR_BUCKET` / `MIN_OPPONENT_BASELINE_GAMES`) is preserved and feeds the peer bullets. Open question on Skill peer-bullet sig-test handling resolved during plan-phase per SEC2-08.
**Depends on:** Phase 84 (DATA-02 mirror-rate audit) and Phase 85 (Section 1 score-bullet pattern locked in for reuse).
**Requirements:** SEC2-01, SEC2-02, SEC2-03, SEC2-04, SEC2-06, SEC2-07, SEC2-08, SEC2-09, SEC2-10
**Success Criteria** (what must be TRUE):
  1. User sees 4 side-by-side cards (Conv / Parity / Recov / Skill) on lg+, stacked on mobile; Conv/Parity/Recov each show gauge + percent + WDL bar + single peer bullet `You − Opp` vs 0.
  2. Skill card shows gauge + percent + peer bullet `Your Skill − Opp Skill` vs 0 (no WDL bar); `Opp Skill` is derived frontend-side from `opp_conv` + `opp_recov` using the existing Skill composite formula; sig-test handling is documented in the plan-phase decision log.
  3. Peer bullets use the mirror-bucket opponent rate per-card with Wald-z sig test on the signed difference vs 0, gated on `MIN_OPPONENT_BASELINE_GAMES`; gauge bands continue to use `FIXED_GAUGE_ZONES.{conversion,parity,recovery}` (p25/p75) unchanged.
  4. An `InfoPopover` on each peer bullet explains the filter-responsive mirror-bucket interpretation (baseline shifts with rating × TC × color × opponent-type filters).
  5. The legacy `EndgameScoreGapSection` table and its 4-gauge strip are removed (knip clean).
**Plans:** 5 plans
- [ ] 86-01-PLAN.md — Backend math: `compute_skill_diff_test` helper + pytest boundary cases (D-01, SEC2-08)
- [ ] 86-02-PLAN.md — Backend wire: 5 Skill fields + 3 per-MaterialRow diff fields on `ScoreGapMaterialResponse`; service wires `compute_skill_diff_test` + per-bucket `compute_score_difference_test`; TS types mirror (D-02, D-03, D-06, SEC2-03, SEC2-06..08)
- [ ] 86-03-PLAN.md — Frontend prep: lift `EndgameScoreGapSection` helpers to `lib/endgameMetrics.ts` (D-08) + parameterize `EndgameOverallConnectorArrows` with 4 testid props (D-09a)
- [ ] 86-04-PLAN.md — Frontend cards: `EndgameMetricCard` (Conv/Parity/Recov shared shell) + `EndgameSkillCard` (Skill variant, no WDL) + Vitest coverage (SEC2-02, SEC2-03, SEC2-04, SEC2-09; D-07, D-14..D-17)
- [ ] 86-05-PLAN.md — Orchestrator + mount + delete + gates: `EndgameMetricsSection` with the 4-card grid, swap mount in `Endgames.tsx`, page-level h2 InfoPopover, delete `EndgameScoreGapSection.tsx`, full gate sweep (SEC2-01, SEC2-07, SEC2-10; D-09, D-09b, D-10, D-11)
**UI hint:** yes
**Context:** Smaller than the pre-pivot two-bullet version of this phase, but Skill peer-bullet sig-test methodology (SEC2-08) remains the main scope risk — options are Wald-z directly on the derived difference / propagation from component CIs / computation on raw underlying outcomes. The Skill composite formula is already implemented elsewhere; the plan-phase should reference it rather than redefining.

### Phase 87: Section 3 — Per-type Endgame Type Breakdown cards

**Goal:** Replace the grouped `EndgameWDLChart` and extend `EndgameConvRecovChart` into 5 full per-type cards (rook / minor_piece / pawn / queen / mixed; pawnless hidden per `HIDDEN_ENDGAME_CLASSES`). Each card carries side-by-side Conv + Recov gauges, a WDL bar, Conv peer bullet (`You − Opp` vs 0), Recov peer bullet (`You − Opp` vs 0), and a Games deep-link to `/endgames/games?type=…`. No cohort bullets (single-bullet doctrine). Mobile density check on real device before locking; fewer elements per card than the pre-pivot design, so the fallback path is to drop the WDL bar (not the peer bullets) if scroll bloat is still unacceptable.
**Depends on:** Phase 86 (single peer-bullet doctrine and mirror-bucket peer-bullet plumbing established).
**Requirements:** SEC3-01, SEC3-02, SEC3-04, SEC3-05, SEC3-06, SEC3-07
**Success Criteria** (what must be TRUE):
  1. User sees 5 per-type cards in a 3-col grid on lg+, 2-col on sm, 1-col on mobile, each labelled by class with side-by-side Conv + Recov gauges, a WDL bar, two peer bullets (Conv + Recov), and a Games deep-link `/endgames/games?type=…`. No cohort bullets.
  2. Per-type peer bullets use the per-type mirror-class rate, gated on `MIN_OPPONENT_BASELINE_GAMES` with a sparse-n indicator. Gauge bands continue to use `PER_CLASS_GAUGE_ZONES[<class>].{conversion,recovery}` (p25/p75) — no per-class p50 needed.
  3. Real-device mobile density check is performed and documented; if WDL bar is dropped under the fallback path, the decision log records the loss and updates the `InfoPopover` copy accordingly.
  4. The legacy `EndgameWDLChart` component is removed; `EndgameConvRecovChart` no longer exists as a standalone gauge-only component (extended into the per-type card shell). Knip clean.
**Plans:** 3
**UI hint:** yes

### Phase 87.1: Per-span ΔES metric for endgame types (INSERTED 2026-05-15)

**Goal:** Add a new per-endgame-type performance metric — mean per-span expected-score delta (ΔES) — that complements the existing game-result-derived Conversion/Recovery rates. For each endgame sequence: `ΔES_span = ES_sigmoid(entry_eval, user_perspective) − exit_score`, where `exit_score = ES_sigmoid(next_span_entry_eval)` for transitory spans (followed by another span in the same game) and `exit_score = game_result_score` (1.0 / 0.5 / 0.0 from user perspective) for terminal spans. Aggregate as mean per (user, endgame_class). Display as a percentile-zoned gauge alongside Conv/Recov on the Phase 87 per-type cards. ΔES isolates per-span quality from game-outcome noise, eliminates the ±1.0 threshold cliff in Conv/Recov, and uses every span (no cohort thinning). Sigmoid choice and calibration caveat documented in `.planning/notes/lichess-sigmoid-endgame-calibration.md`.

**Depends on:** Phase 87 (per-type card layout shipped — ΔES gauge slots into the existing card shell).

**Requirements:** TBD (DELTA-01..DELTA-NN to be drafted during spec-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. `EndgameCategoryStats` carries `delta_es_mean`, `delta_es_n`, `delta_es_p_value`, `delta_es_ci_low`, `delta_es_ci_high` per endgame_class, computed from per-span ΔES values using `compute_paired_difference_test` (or equivalent) from `app/services/score_confidence.py`. NULL eval at either endpoint excludes the span from the cohort. `PVALUE_RELIABILITY_MIN_N` gate applies.
  2. Repo query (`query_endgame_entry_rows` or a sibling) returns each span's entry_eval plus the next span's entry_eval per game, computed via a window function over `(game_id, span_min_ply ASC)`. Terminal spans return NULL for `next_entry_eval`; the service layer falls back to deriving exit_score from `game.result` and `game.user_color`.
  3. Span definition matches the existing codebase span (`GROUP BY game_id, endgame_class`) for cohort consistency with Conv/Recov. Strict contiguous-run definition documented as a follow-up if zones look off.
  4. /benchmarks skill ships a new `per_span_delta_es_by_class` metric with per-endgame-class Cohen's-d collapse verdict; generated zone constants surface in `frontend/src/generated/endgameZones.ts` and feed the per-type cards via `PER_CLASS_GAUGE_ZONES[<class>].delta_es`.
  5. Frontend per-type cards from Phase 87 gain a ΔES gauge alongside Conv/Recov, with InfoPopover copy explaining the metric, the "complement, not replacement" framing vs Conv/Recov, and a one-line link to the Lichess-sigmoid calibration caveat.
  6. LLM payload (`insights_llm.py` `type_breakdown` section) includes per-type ΔES, zone label, and benchmark band; prompt version bumped with a changelog entry describing the metric and the calibration caveat.
  7. Tests cover: per-span ΔES math on synthetic spans (entry-only, terminal, transitory, mate at either endpoint, NULL-eval skip), service-layer aggregation invariants (cohort size > 0 ⇒ mean in [−1, +1]), benchmark zone-band ingestion, and LLM payload presence under sparse and full cohorts.

**Plans:** 4 plans
- [ ] 87.1-01-PLAN.md — /benchmarks SKILL.md §3.4.2 + zone registry + codegen (regen endgameZones.ts)
- [x] 87.1-02-PLAN.md — Backend: repo LEAD-based next-eval, _compute_span_gap helper, 5 schema fields, paired-diff wiring (2026-05-15 — commits `884589d8` + `be0150b0`; success criteria 1, 2, 3 satisfied; 18 new tests; ROADMAP-1/2/3 ✓)
- [ ] 87.1-03-PLAN.md — Frontend: EndgameTypeCard ScoreGapRow + MetricStatPopover (dual-label copy, sigmoid-bias caveat) + Vitest
- [ ] 87.1-04-PLAN.md — LLM payload type_breakdown per-class type_achievable_score_gap + endgame_v29 prompt bump + glossary + changelog

**UI hint:** yes

**Context:** Source: [[SEED-016-per-span-eval-delta-endgame-metric]]. Prod eval-coverage check 2026-05-15 confirms ~100% coverage on ≥6-ply spans (337,345 / 337,350 under codebase span definition; 353,060 / 353,065 under strict contiguous-run), so eval availability is not a blocker. Fits naturally alongside Phase 85.1 (hypothesis tests + CIs) and Phase 88 (new /benchmarks metrics) as part of the v1.17 statistical-rigor pass.

Two scope risks worth surfacing at plan-phase:
- **(a) Sigmoid calibration.** The Lichess sigmoid in `app/services/eval_utils.py` was fitted on middlegame-dominated positions and under-weights endgame advantages. Mitigated by displaying percentile-based zones (self-calibrating, like `entry_expected_score`) rather than raw values. Refit to an endgame-specific sigmoid is a deferred follow-up tracked in the calibration note.
- **(b) Span definition choice.** Codebase span (`GROUP BY game_id, endgame_class`) vs strict contiguous-run (gaps-and-islands by consecutive ply) yields ~5% more spans under the strict definition but makes "next span entry eval" ambiguous when the next span shares an endgame_class. Recommend defaulting to codebase definition for cohort consistency with Conv/Recov; revisit if zones look misleading.

### Phase 87.2: Section 2 — eval-based ΔES Score Gap on Conv/Parity/Recov + Endgame Skill cards (INSERTED 2026-05-15)

**Goal:** Replace the rate-based mirror-bucket peer-diff Gap bullet (`You / Opp / Gap` row + `MiniBulletChart` centred on 0) on each of the four Section 2 cards — Conversion, Parity, Recovery, and Endgame Skill — with an **eval-based ΔES Score Gap bullet** that reuses the Phase 87.1 per-span ΔES infrastructure already shipped for Section 3 per-type cards. The current rate-based peer-diff is mathematically degenerate by construction:

- **Conv-Gap ≡ Recov-Gap.** Because `opp_conv_win% = user_recov_loss%` (mirror-bucket same-game symmetry) and `opp_recov_save% = user_conv_loss% + user_conv_draw%`, the two Gaps are algebraically identical for every user / filter combination. The "Conv vs Recov" framing is illusory.
- **Parity-Gap is a deterministic affine of the gauge.** `opp_parity = 1 − user_parity` by chess-score symmetry, so `Parity-Gap = 2·user_parity − 1`. The Gap bullet plots the same information as the absolute Parity gauge through a confusing mirror frame.
- **Skill-Gap inherits both pathologies.** `Skill = mean(Conv, Parity, Recov)` ⇒ `Skill-Gap = (2·Conv-Gap + Parity-Gap) / 3`, i.e. two degrees of freedom and one of them (`user_parity`) is already on the Parity gauge.

The ΔES Score Gap sidesteps all three: it measures eval delta *during the span* (entry-eval expected-score minus exit-score), not symmetric outcomes of a shared set of games, so Conv-bucket spans and Recov-bucket spans yield genuinely independent values.

Each Section 2 card gets one ΔES bullet restricted to the eval-entry bucket that defines the card:

| Card | ΔES cohort |
|---|---|
| Conversion | spans with `entry_eval ≥ +100 cp` (user perspective) |
| Parity | spans with `−100 cp ≤ entry_eval ≤ +100 cp` |
| Recovery | spans with `entry_eval ≤ −100 cp` |
| Endgame Skill | all endgame spans (games-weighted mean across the three buckets) |

Per-bucket zone bands live in `BUCKETED_ZONE_REGISTRY` alongside the existing fixed gauge bands — parallel to how `PER_CLASS_GAUGE_ZONES[<class>].achievable_score_gap` is defined per material class in Phase 87.1.

**Depends on:** Phase 86 (Section 2 4-card layout shipped); Phase 87.1 (per-span ΔES backend math, repo LEAD-based next-eval, `_compute_span_gap` helper, `ScoreGapRow` frontend primitive, paired-difference test wiring).

**Requirements:** TBD (SEC2-ΔES-01..NN to be drafted during spec-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. Each of the four Section 2 cards (Conv / Parity / Recov / Skill) renders one ΔES Score Gap bullet centred on 0, with Wilson/paired-z CI whiskers, replacing the prior `You / Opp / Gap` peer-diff row and `MiniBulletChart`. Bullet primitives (`ScoreGapRow` + `MiniBulletChart`) reuse the Phase 87.1 pattern; no new chart components.
  2. Backend: `EndgameOverview` (or the Section 2 schema) exposes four new fields per card (`delta_es_mean`, `delta_es_n`, `delta_es_p_value`, `delta_es_ci_low`, `delta_es_ci_high`), computed from per-span ΔES restricted to the card's eval-entry bucket using `compute_paired_difference_test` from `app/services/score_confidence.py`. Endgame Skill uses the games-weighted mean across all three buckets, with the paired-diff test run on the pooled span population.
  3. Zone bands for the four ΔES Gap bullets are added to `BUCKETED_ZONE_REGISTRY` (`section2_delta_es_gap.{conv,parity,recov,skill}` or similar key shape) with Cohen's-d-based per-bucket collapse verdicts surfaced via the /benchmarks skill; codegen regenerates `frontend/src/generated/endgameZones.ts`.
  4. Rate-based peer-diff plumbing is fully retired once the four cards switch over: `EndgameMetricCard`'s `You/Opp/Gap` row + `MiniBulletChart` peer-diff path, `EndgameSkillCard`'s peer-bullet row + `compute_skill_diff_test` backend, `MIRROR_BUCKET` map + `userRate`/`opponentRate` helpers in `endgameMetrics.ts`, `formatDiffPct`, `BULLET_DOMAIN`/`NEUTRAL_ZONE_MIN`/`NEUTRAL_ZONE_MAX`/`MIN_OPPONENT_BASELINE_GAMES` constants, and the `opponent_score`/`opponent_games` mirror-rate fields on Section 2 schemas (Section 3 still uses them per Phase 87 plan; verify before deletion). Knip clean.
  5. InfoPopover copy on each card explains: (a) what the ΔES Gap means in this card's eval-entry bucket (e.g. "how much winning-probability you bled away on spans you entered ahead by ≥1 pawn"); (b) sign convention (+ = above expectation, − = below); (c) one-line link to the Lichess-sigmoid calibration caveat. No "vs opponents" framing — the comparison is now self vs zero, not self vs peer mirror.
  6. LLM payload: the Section 2 / overview payload swaps the rate-based peer-diff fields for the four ΔES Gap values + zone labels. `endgame_v29` prompt is bumped to `endgame_v30` (or a follow-on Section-2 bump) with a changelog entry explaining the metric change and that Conv/Recov are now genuinely independent signals.
  7. Tests cover: per-bucket ΔES math on synthetic spans (entry-bucket filter + paired-diff p-value), per-card cohort-empty fallback (no spans in bucket ⇒ NULL bullet, opacity-50 row), Skill pooled-aggregate invariants (games-weighted mean equals component-mean when buckets equal-sized), Vitest snapshot + render tests on `EndgameMetricCard` + `EndgameSkillCard` with new ΔES props, knip + ty + ruff clean.

**Plans:** 4 plans

- [ ] 87.2-01-PLAN.md — Benchmarks SKILL.md §3.4.4 + 4 new MetricIds in ZONE_REGISTRY + codegen of 8 TS neutral-band constants
- [x] 87.2-02-PLAN.md — Backend math (gaps_by_bucket accumulator + per-bucket paired-z + equal-weighted Skill aggregator) + schema (20 new fields, 10 deletions) + delete 2 retired helpers
- [ ] 87.2-03-PLAN.md — Frontend: TS types mirror + 8 lib symbol deletions + EndgameMetricCard + EndgameSkillCard + EndgameMetricsSection refactor + Vitest
- [ ] 87.2-04-PLAN.md — LLM payload: 4 new findings in insights_service (ADDITIVE) + prompt v30→v31 + glossary block + SEC2-ΔES-01..07 in REQUIREMENTS + CHANGELOG

**UI hint:** yes

**Context:** Direct conceptual outcome of the Phase 87.1 rework discussion 2026-05-15. The user observed that the Conv-Gap / Recov-Gap coupling has "always bugged" them and that the Phase 87.1 Section 3 cards (which use eval-based ΔES) feel cleanly independent in a way the Section 2 cards do not. Decomposing the rate-based Gap exposed the mirror symmetry and the Parity-Gap affine-identity, confirming the coupling is by-design rather than a bug to patch. This phase backports the Section 3 design pattern to Section 2.

Two scope considerations for plan-phase:
- **(a) Skill card cohort definition.** The Endgame Skill ΔES could be either (i) the per-game games-weighted mean of the three bucket ΔES means or (ii) the pooled-span mean over all endgame spans regardless of entry-eval bucket. The latter is simpler and matches Phase 87.1's per-class pooled aggregation; the former lets the Skill bullet decompose cleanly into the three component bullets. Recommend (ii) for consistency with Phase 87.1's "mean per (user, endgame_class)" pattern, with the decomposition spelled out in the InfoPopover.
- **(b) Mirror-bucket plumbing retirement.** Section 3 per-type cards still consume `opponent_score`/`opponent_games` for their Conv+Recov peer bullets (Phase 87 plan). Verify before plan-phase whether Phase 87 actually shipped peer-bullet retirement (single-bullet doctrine + Phase 87.1 ΔES gauge replaced them) or whether the mirror-bucket fields are still load-bearing for Section 3 too. If retired everywhere, this phase can also remove the backend mirror-bucket repository plumbing; if not, scope the deletion to Section 2 fields only.

### Phase 87.3: Endgame Skill v2 — Conv+Parity percentile composite (INSERTED 2026-05-16)

**Goal:** Redefine **Endgame Skill** so it is an honest skill measure and feeds a non-degenerate Endgame ELO timeline. The current composite is the equal-weighted mean of Conversion/Parity/Recovery (rate-based, §3.2.1). The Recovery component is opponent-confounded: its ΔES gap *inverts* with ELO (benchmark `d≈0.85`, weak cohorts highest) because the Recovery bucket sits in the deep-losing sigmoid floor where the outcome is driven by the opponent's failure to convert, not the user's defense. Equal-weighting it actively cancels Conversion's clean skill signal inside the composite. A validation spike (`reports/spike-clamped-recovery-elo-ramp-2026-05-16.md`) proved the confound is **not** removable by clamping the Recovery bucket (every lower-bound cut −400…−200 still inverts; clamping also collapses 800-cohort coverage). Decision: **drop Recovery from the composite entirely**; it survives only as a standalone descriptive tile.

The redefined Endgame Skill is the **pooled percentile rank** of a per-user (and per-rolling-window) composite = the equal-weighted mean of **Conversion ΔES** and **Parity ΔES** (the Phase 87.2 eval-based per-span ΔES, Lichess 2300+ expected-score sigmoid reference), Recovery excluded, ≥10-span floor per active bucket. Empirical basis (benchmark DB, 2026-05-16): the Lichess-referenced gap discriminates ELO at `d≈1.39` vs `d≈0.90` for any Stockfish-perfect anchor (a perfect-play reference is saturated ≈1.0 over the conversion region and cannot net out entry difficulty); A2 (absolute `100+100·gap`) vs percentile is a presentation choice on the *same* substrate, and percentile is **mandatory** for the timeline (see below).

The percentile feeds the **unchanged Phase 57 `_endgame_elo_from_skill`** formula (`app/services/endgame_service.py:1348`): `endgame_elo = actual_elo + 400·log10(s/(1−s))`, `s` clamped `[0.05, 0.95]`, `s=0.5 ⇒ lines coincide`. This formula structurally requires `s ∈ [0,1]` with population median `0.5` — percentile rank satisfies this natively; the raw absolute gap (~0.96-centred) would peg the clamp at +510 Elo for every player and break the timeline.

Full derivation and rejected alternatives: `.planning/notes/endgame-skill-recovery-confound.md` (LOCKED SPEC, confirmed 2026-05-16).

**Depends on:** Phase 87.2 (eval-based per-span ΔES Score Gap shipped — Conversion/Parity/Skill bullets, `_compute_span_gap`, `BUCKETED_ZONE_REGISTRY`, `ScoreGapRow`, paired-diff wiring; PR #98 merged 2026-05-16). Phase 57 / 57.1 (`_endgame_elo_from_skill` Endgame ELO formula — reused unchanged).

**Requirements:** TBD (ESKILL-01..NN to be drafted during spec-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. The Endgame Skill composite is the equal-weighted mean of **Conversion ΔES** and **Parity ΔES** only (Lichess 2300+ sigmoid reference, ≥10-span floor per active bucket). Recovery ΔES is **not** in the composite anywhere in backend, frontend, or LLM payload.
  2. The Endgame Skill gauge + headline number is the **pooled percentile rank** of that composite against a **frozen, versioned** benchmark reference distribution; the neutral zone is centred at 50; `noUncheckedIndexedAccess` and zone-registry codegen drift gate stay green.
  3. The Endgame ELO timeline reuses the unchanged `_endgame_elo_from_skill` formula with `s` = the percentile. An explicit invariant is asserted in code + tests: `s ∈ [0,1]`, and the benchmark-reference percentile of the population median is `0.5` (so a typical player's Endgame ELO equals their actual ELO — the "matched" contract Phase 57 documents). The frozen reference guarantees historical timeline points do not drift on benchmark rebuild (versioned reference artifact, regenerated only on explicit benchmark refresh).
  4. The **Recovery Score Gap** is retained as a standalone descriptive tile, with InfoPopover copy explicitly stating it is opponent-dependent (reflects how often opponents failed to convert against you, not a defensive-skill verdict) and that it is deliberately excluded from Endgame Skill. No "vs peer" framing implying it is a skill measure.
  5. LLM payload: Endgame Skill value + zone label reflect the new percentile composite; Recovery framing updated to the descriptive-tile language; prompt version bumped with a changelog entry explaining the metric change and the Recovery exclusion rationale.
  6. Tests cover: composite math on synthetic spans (Conv+Parity equal-weight, Recovery excluded, ≥10-span floor), percentile mapping against a fixed reference fixture (median ⇒ 0.5, monotonic, clamped endpoints), the `s ∈ [0,1] / median⇒0.5` Endgame ELO invariant, timeline non-drift under a simulated reference re-version, and Vitest render of the Skill gauge + standalone Recovery tile copy. ty + ruff + knip clean.

**Out of scope:** Conversion Score Gap display-centering (tracked separately as `.planning/todos/pending/2026-05-16-conversion-score-gap-display-centering.md`); no recalibration of `section2_score_gap_recov` (the descriptive Recovery band stays as shipped).

**Plans:** 4 plans

- [ ] 87.3-01-PLAN.md — Frozen percentile reference artifact (gen script + benchmark-DB knots + D-08 band derivation + CI drift-gate)
- [ ] 87.3-02-PLAN.md — Backend: Conv+Parity composite (Recovery dropped), endgame_skill_percentile schema, ELO-timeline percentile path
- [ ] 87.3-03-PLAN.md — Frontend: percentile gauge + "Better than X%" headline + D-10 popover + Conv→Parity→Skill→Recovery reorder + Recovery descriptive tile + Vitest
- [ ] 87.3-04-PLAN.md — LLM payload: percentile skill finding + Recovery descriptive override + endgame_v32 prompt bump + glossary + CHANGELOG

**UI hint:** yes

**Context:** Direct outcome of the 2026-05-16 `/gsd-explore` thread on Endgame Metrics & ELO (benchmarks §3.2). The Recovery opponent-confound was diagnosed from the §3.2.2 ELO inversion, a clamp fix was hypothesised and then **falsified** by a benchmark sweep spike (FAIL — confound is result-based, not a sigmoid-floor artifact). The reference-curve choice (Lichess 2300+ vs Stockfish-perfect) and the gauge mapping (percentile vs absolute A2) were each settled with benchmark Cohen's-d comparisons, then the Phase 57 Endgame ELO formula was found to *mandate* a 0.5-centred `s ∈ [0,1]`, which forces the percentile mapping for the timeline. All decisions and rejected alternatives are recorded in `.planning/notes/endgame-skill-recovery-confound.md`.

One scope consideration for plan-phase:
- **Frozen reference artifact shape.** The percentile reference can be (i) a stored empirical CDF / quantile table of the composite over the benchmark population, or (ii) μ/σ per sub-bucket + an assumed-normal Φ map. (i) is distribution-honest and timeline-stable but is a new versioned calibration artifact (regenerate only on explicit benchmark refresh, like the zone bands); (ii) is lighter but assumes normality the composite may not satisfy. Recommend (i) for timeline non-drift integrity; confirm artifact location + versioning convention during plan-phase (parallels the `endgameZones.ts` codegen drift-gate pattern).

### Phase 87.4: Drop Endgame Skill — rewire timeline as Conversion ELO from Conv ΔES (INSERTED 2026-05-16)

**Goal:** Retract Phase 87.3's percentile-of-Conv+Par-ΔES Endgame Skill design and remove **Endgame Skill as a concept** from the product. Live UAT review of 87.3 surfaced two failure modes with a shared root cause: (1) weak players posted high percentile Endgame Skill — the composite's terminal score is opponent-dependent (weak opponents err more → inflates weak players' composite), and the Lichess-2300+ reference removes only entry difficulty, not opponent strength; (2) the Endgame ELO timeline became volatile — the empirical-quantile transform is steepest where benchmark density is highest (the median), so per-rolling-window noise gets amplified before the `logit→ELO` map. The LOCKED SPEC was validated on cross-sectional between-bucket Cohen's d, which speaks neither to individual absolute-skill labelling nor to within-user temporal stability.

No composite definition survives scrutiny on all four axes (cohort de-confounding, individual absolute-skill interpretation, per-window stability, Phase 57 median-coincide invariant). Empirical verification on the benchmark DB (2026-05-16): a Conv+Par rate average lands at d=0.80 ELO ramp but pools to ≈ 0.61 (not 0.50, breaking the Phase 57 invariant for free) and dilutes Conv's strong signal (d=0.83) with Parity's weak signal (d=0.49); no non-negative weighting can recenter without dropping Conv. The "triad makes a better product story" argument now cuts the other way: the single confected number is the cause of every failure mode.

**Decision:** Drop Endgame Skill entirely. Conversion remains the actual skill axis (user's technique dominates variance); Parity and Recovery stay as **descriptive tiles** (near-coin signal / opponent-dependent comeback). The **Endgame ELO Timeline is renamed Conversion ELO Timeline** and fed from the already-shipped **Conv ΔES Score Gap** (Phase 87.2, Lichess-2300+ reference, calibrated band `[−0.11, 0.00]`, pop p50 = `−0.0474`, **d=1.62** between 800 and 2400 — the strongest ELO ramp in the entire benchmark report). Phase 57 `_endgame_elo_from_skill` is reused unchanged; only its input source is rewired. Conversion ΔES is structurally promoted to the **spine** of the Endgame metrics section.

Full derivation and rejected alternatives: `.planning/notes/endgame-skill-dropped-conversion-elo.md` (supersedes the prior LOCKED SPEC at `.planning/notes/endgame-skill-recovery-confound.md`).

**Depends on:** Phase 87.2 (Conv ΔES Score Gap shipped — `_compute_span_gap`, `section2_score_gap_conv` zone band, `ScoreGapRow`, paired-diff wiring; PR #98 merged 2026-05-16). Phase 57 / 57.1 (`_endgame_elo_from_skill` formula reused unchanged — only its input source changes). Phase 87.3 (the surfaces being retracted live in branch `gsd/phase-87.3-endgame-skill-v2-conv-parity-percentile-composite`; 87.4 unwinds those surfaces and lands a cleaner replacement).

**Requirements:** TBD (CONVELO-01..NN to be drafted during spec-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. **Endgame Skill is gone from every surface.** No Endgame Skill card, no Endgame Skill Score Gap card, no `endgame_skill_percentile` field on the insights/endgame API response, no Skill LLM payload field. The frozen percentile reference artifact + gen script + CI drift-gate from 87.3 are removed. Backend `_endgame_skill_from_bucket_rows` and its callers are deleted or replaced.
  2. **Section 2 renders as three cards only**: Conversion (rate gauge + ΔES bullet, skill framing), Parity (rate gauge + ΔES bullet, descriptive framing — "near-coin signal, opponent variance"), Recovery (rate gauge + ΔES bullet, descriptive framing — "opponent-dependent comeback, not a skill verdict"). Reading order is Conversion → Parity → Recovery. Mobile + desktop parity. No "Skill" header anywhere.
  3. **The Endgame ELO Timeline chart is renamed Conversion ELO Timeline** in all copy (chart title, popover, LLM glossary, CHANGELOG). The chart contract (Phase 57) is unchanged: it shows actual ELO and the derived ELO over time with `_endgame_elo_from_skill`.
  4. **`s` is computed from Conv ΔES via an affine recenter, frozen constants**: `s = clamp(0.5 + α · (conv_ΔES − PIVOT), 0.05, 0.95)` with `PIVOT = −0.0474` (benchmark p50) and `α` chosen so the calibrated band `[−0.108, +0.002]` maps to s ∈ ~[0.40, 0.60]. Constants live in a small backend module (suggested `app/services/conversion_elo.py` — planner finalizes) with explicit `CALIBRATION_VERSION` for traceability. Frozen constants ⇒ historical timeline points non-drifting on benchmark rebuild.
  5. **Phase 57 invariant preserved**: a typical-skill player (`conv_ΔES = PIVOT`) yields `s = 0.5` ⇒ Conversion ELO equals actual ELO. Asserted in code + tests.
  6. **LLM payload**: the Endgame Skill finding is removed entirely; in its place, the Conversion ΔES Score Gap finding is promoted to the primary Section 2 finding (already populated from Phase 87.2 — relabel/rerank only). Recovery and Parity LLM findings reflect the descriptive framing. Prompt version bumped (`endgame_v32` → `endgame_v33` — planner reads current value at `app/services/insights_llm.py`). CHANGELOG entry narrates: Endgame Skill concept dropped (no composite definition survived); Endgame ELO Timeline renamed to Conversion ELO Timeline, fed from Conv ΔES; rationale links to the supersession note.
  7. **Conv ΔES Score Gap display-centering folded in.** The pending todo `.planning/todos/pending/2026-05-16-conversion-score-gap-display-centering.md` is promoted from optional polish to in-scope for this phase: subtract the band midpoint (`(−0.11 + 0.00) / 2 = −0.055`) from the displayed Conv ΔES value and band so `0` renders as "typical population result." Underlying MetricId value, calibration band, and Cohen's d unchanged. Apply uniformly across Conversion / Parity / Recovery bullets per the todo's "scope notes" recommendation.
  8. **Tests cover**: affine-recenter math on synthetic spans (typical → s=0.5; band edges → expected s; clamp boundaries), the Phase 57 invariant under the new input, ty + ruff + knip clean (especially knip, since Skill exports are being removed wholesale), Vitest render of the three remaining cards + the renamed Conversion ELO Timeline chart, and a regression test that no rendered surface contains the string "Endgame Skill" (case-insensitive) outside of CHANGELOG / glossary historical sections.

**Out of scope:**
  - Per-move centipawn-loss / engine-accuracy endgame skill metric (would be its own milestone — requires per-ply Stockfish eval the current span-eval architecture does not have).
  - Recalibration of `section2_score_gap_conv` / `_parity` / `_recov` zone bands (the Phase 87.2 bands stay as shipped).
  - Any change to Phase 57 `_endgame_elo_from_skill` formula itself.

**Plans:** TBD (drafted during plan-phase; expected 3 plans — backend rewire + display-centering, frontend card removal + chart rename + Conv display-centering, LLM payload + prompt-version + CHANGELOG)

**UI hint:** yes

**Context:** Direct outcome of the 2026-05-16 `/gsd-explore` thread post-87.3-implementation, after the user observed weak players posting high percentile Endgame Skill and a volatile Endgame ELO timeline on real gauges. Empirical follow-up (benchmark DB, equal-footing, sparse cell excluded) measured the Conv+Par rate composite as d=0.80 but pooling to 0.61, and confirmed no non-negative weighting recenters it without dropping Conversion. Full reasoning + rejected alternatives + the d-table in `.planning/notes/endgame-skill-dropped-conversion-elo.md`.

### Phase 87.5: Rebuild Endgame ELO on Endgame Score Gap (INSERTED 2026-05-17)

**Goal:** Replace Phase 87.4's Conversion-ΔES-driven affine recenter with a direct additive mapping from the existing **Endgame Score Gap** series (`eg_score_gap = endgame_score − non_endgame_score`, outcome-based windowed mean already shipped on the Endgames page) onto an ELO delta around the user's actual rating. Live UAT of Phase 87.4 surfaced two failure modes traced to the input metric: (1) strong players posted Conversion ELO 200–500 ELO above actual ELO regardless of how they were playing, because the Phase 87.4 affine recenter inherited the conv-ΔES sigmoid bias (benchmark §3.2.2: Conv ΔES ELO Cohen's d = **1.62**, p50 sweeps from −14.0pp at 800 to −0.3pp at 2400); (2) the timeline became visibly volatile because conv-bucket-only spans are a small fraction of the rolling window. The fix is to switch the input metric to Endgame Score Gap (§3.1.6: ELO d = **0.17**, p50 sweeps from −3.6pp to +0.1pp — essentially flat by construction) and replace the multiplicative Phase 57 formula with an additive mapping `endgame_elo = actual_elo + K · eg_score_gap`.

**Rename + relocate.** The chart is renamed **Conversion ELO Timeline → Endgame ELO Timeline** in every surface (chart title, popover, LLM glossary, CHANGELOG) and moves out of the "Endgame Metrics and ELO" section into "Endgame Overall Performance," positioned directly below the existing "Endgame Score Gap over Time" chart. Rationale: the two charts now share an input series and tell the same story in different units (score-points vs ELO points); co-locating them lets the user read score gap and ELO impact at the same scroll position.

Full derivation, rejected alternatives, and the §3.1.6-vs-§3.2.2 d-table: `.planning/notes/endgame-elo-rebuild-on-score-gap.md`. K calibration procedure: `.planning/todos/pending/calibrate-endgame-elo-k.md`.

**Depends on:** Phase 87.4 (Conversion ELO Timeline scaffolding to overwrite — `_conversion_elo_from_skill`, `_affine_recenter_conv_delta`, `_windowed_conv_delta_es`, `PIVOT`, `ALPHA`, `CALIBRATION_VERSION`, `_compute_conversion_elo_weekly_series`, `conversion_elo_timeline` schema field + frontend `ConversionEloTimelineSection`; PR #104). Phase 68 / 85 (`_compute_score_gap_timeline`, `ScoreGapTimelinePoint`, `score_gap_material.timeline`, `SCORE_GAP_TIMELINE_WINDOW = 100`, `EndgameScoreOverTimeChart` — the input series + co-location anchor). Phase 57 (`get_endgame_elo_timeline` paired-series query / `query_endgame_elo_timeline_rows` — repo layer reused for the Actual ELO line; only the Endgame ELO arithmetic changes). Phase 87.2 (Conv ΔES Score Gap stays untouched as a Section 2 metric — only its role as the timeline driver is retired).

**Requirements:** TBD (EGELO-01..NN to be drafted during spec-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. **The chart is named "Endgame ELO Timeline" on every surface.** Chart title, surrounding section heading, info popover body, LLM glossary entry, prompt prose, CHANGELOG bullet, and Vitest assertions all read "Endgame ELO" — no "Conversion ELO" string survives outside of CHANGELOG historical sections and the supersession comment block in code. Repo-wide grep for `Conversion ELO` returns only the supersession comment + CHANGELOG history (case-insensitive).
  2. **The Endgame ELO Timeline renders directly below the "Endgame Score Gap over Time" chart in the "Endgame Overall Performance" section.** It is removed from the "Endgame Metrics and ELO" section (which retains the Conv / Parity / Recov gauges only — the surrounding section header may be renamed to "Endgame Metrics" with the ELO label excised; planner decides whether that rename is in-scope or deferred to Phase 89 polish). Mobile + desktop parity. Both charts share the same recency / TC / platform filters via the existing page filter plumbing.
  3. **Backend Endgame ELO computation is `endgame_elo = round(actual_elo + K · eg_score_gap)`,** where `eg_score_gap` is the per-week windowed mean of the per-side rolling means (endgame_score − non_endgame_score) that already drives `_compute_score_gap_timeline`. The Endgame ELO timeline derives its series from this same producer — no parallel aggregator. At `eg_score_gap = 0`, the rendered Endgame ELO equals Actual ELO point-for-point (asserted in code + tests).
  4. **A single global K constant is locked and documented in code with a one-line rationale.** Calibration follows `.planning/todos/pending/calibrate-endgame-elo-k.md`: pull 5–10 representative users spanning rating buckets from dev / benchmark DB, eyeball whether `K · eg_score_gap` produces believable ELO deltas (typical points within ~30–60 ELO of actual; extremes around ±100 ELO; nothing approaching Phase 87.4's 500-ELO gaps). Starting candidate `K ≈ 450` from §3.1.6 (p95 ≈ +0.20 → +91 ELO; p05 ≈ −0.23 → −102 ELO). Finalized value lives next to the constant with a comment block citing the benchmark percentile mapping. Rounding rule and floor/ceiling match the existing endgame_elo timeline contract (positive integer).
  5. **All Phase 87.4 calibration constants and helpers are deleted, not retuned**: `PIVOT`, `ALPHA`, `CALIBRATION_VERSION`, `_affine_recenter_conv_delta`, `_windowed_conv_delta_es`, `_conversion_elo_from_skill` (renamed back to `_endgame_elo_from_score_gap` with the new additive math) — removed wholesale from `app/services/endgame_service.py` and any callers. The Phase 57 multiplicative `400·log10(s/(1−s))` ELO mapping is retired; the additive K mapping is the only ELO arithmetic.
  6. **Schema rename, end-to-end.** `conversion_elo_timeline` → `endgame_elo_timeline` on `EndgameOverviewResponse` / API response shape; the underlying repo function and row query keep working (rename `get_conversion_elo_timeline` → `get_endgame_elo_timeline` only if it was renamed away from that name in 87.4 — otherwise leave). Frontend component file `ConversionEloTimelineSection.tsx` → `EndgameEloTimelineSection.tsx`, types/props updated, knip stays clean. TypeScript types regenerated from the backend schema follow.
  7. **LLM payload rename and re-grouping.** `MetricId` literal `conversion_elo_gap` → `endgame_elo_gap`; `SubsectionId` literal `conversion_elo_timeline` → `endgame_elo_timeline`. The subsection moves from `metrics_elo` to `overall` in the section-by-section grouping (Phase 57.1's "Endgame Overall Performance" subsection ownership). Prompt prose rewritten to reference Endgame Score Gap as the input series and restore the "lifts up / holds back actual rating" headline metaphor that Phase 87.4 traded away. The Phase 87.4 Section-3 LLM surface (`[summary endgame_elo | …]`, `### Subsection:` header, glossary entry) is kept but rewired to the new metric. `_PROMPT_VERSION` bumps to `endgame_v33`.
  8. **Phase 87.4 Conv display-centering stays in place.** The Conv / Parity / Recov bullet display-only affine shifts shipped in Phase 87.4 (Conv −0.055, Recov +0.06, Parity 0) are untouched — they are independent of the timeline rewire. The "frozen calibration constants" guarantee documented in Phase 87.4 D-04 is also retired with the constants themselves.
  9. **Tests cover:** the additive K mapping on synthetic windowed score-gap series (zero gap ⇒ ELO equals actual; ±p95 gap ⇒ ELO equals actual ± K·p95 rounded); a regression test asserting no rendered surface contains the string "Conversion ELO" outside the supersession comment + CHANGELOG; Vitest render of the renamed `EndgameEloTimelineSection` in its new DOM position below `EndgameScoreOverTimeChart`; ty + ruff + knip clean (especially knip, since `_affine_recenter_conv_delta` / `_windowed_conv_delta_es` / `PIVOT` / `ALPHA` / `CALIBRATION_VERSION` exports are being removed wholesale and `ConversionEloTimelineSection` is renamed).

**Out of scope:**
  - Conv / Recov gauge re-centering. The §3.2.2 benchmark explicitly says these bands *should* be off-center (sigmoid bias on conversions near the score ceiling, recoveries near the floor); re-centering them would lie about what's typical. Discussed and deferred in the design note.
  - Per-(platform, TC) K tuning. Endgame Score Gap is ELO-collapse and only TC-review (max d = 0.34); a single global K is justified for v1. Per-TC K flagged as a future option only if UAT reveals systematic per-TC drift.
  - "Endgame Metrics and ELO" → "Endgame Metrics" section-header rename is suggested but not required; planner finalizes whether to fold it in or punt to Phase 89 polish.
  - Any change to the Conv ΔES Score Gap card / band / Cohen's d in Section 2 (Phase 87.2 surface stays as shipped — only its retired *secondary* role as the timeline driver is removed).

**Plans:** TBD (drafted during plan-phase; expected 3 plans — backend rewire + Phase 87.4 constant deletion + schema rename, frontend chart rename + repositioning + tests, LLM payload rename + endgame_v33 prompt + CHANGELOG).

**UI hint:** yes

**Context:** Direct outcome of the 2026-05-17 `/gsd-explore` thread post-Phase-87.4 UAT review. Two real-user failure modes (strong players consistently 200–500 ELO above actual; visibly volatile timeline) traced to the input metric, not the affine recenter or the K knob: benchmark §3.2.2 measures the conv-ΔES ELO ramp at d=1.62 — the sigmoid baseline structurally over-rates strong players because it does not condition on rating. Switching to Endgame Score Gap (§3.1.6, ELO d=0.17 — essentially flat) eliminates the bias by construction (both terms are realized outcomes, no sigmoid involved at all; the user's own non-endgame outcomes act as the within-player baseline). Also restores the original Phase 57 storytelling: positive gap ⇒ endgame is lifting your rating; negative ⇒ holding it back. Full reasoning + rejected alternatives in `.planning/notes/endgame-elo-rebuild-on-score-gap.md`.

### Phase 87.6: Endgame ELO via Performance Rating — drop K, dual PR lines + signed band (INSERTED 2026-05-17)

**Goal:** Replace Phase 87.5's free-constant additive mapping (`endgame_elo = actual_elo + K · eg_score_gap` with K=450 eyeball-calibrated against §3.1.6 percentiles) with the FIDE **Performance Rating** formula applied independently to endgame and non-endgame game subsets:

```
endgame_elo     = R_opp_avg_E + 400 · log10(s_E* / (1 − s_E*))
non_endgame_elo = R_opp_avg_N + 400 · log10(s_N* / (1 − s_N*))
```

where `s_E*` and `s_N*` are Laplace-smoothed per-side scores (`(wins + 0.5·draws + 1) / (n + 2)`) over the trailing 100-game window, and `R_opp_avg_E`/`R_opp_avg_N` are per-side average opponent ratings over the same window. The `400` is fixed by Elo's logistic-skill assumption (the same `400` in the Elo expected-score formula) — it is not a calibration knob. **K is deleted, not retuned.** By the midpoint property (actual_elo ≈ midpoint(PR_E, PR_N) in the score-gap range of |gap| < 0.23), the Actual ELO line is bracketed by Endgame ELO and Non-Endgame ELO by construction, preserving Phase 87.5's "lifts up / holds back actual rating" semantics without K.

**Chart redesign.** The Endgame ELO Timeline chart gains two fine lines per combo (Endgame ELO, Non-Endgame ELO) drawn in the combo color, with a **signed band** between them: green when Endgame ELO ≥ Non-Endgame ELO, red when below. The Actual ELO line stays as the bold central reference (combo color, `strokeWidth ≈ 2`). The signed-fill algorithm — a single `<Area>` with a horizontal `<linearGradient>` whose stops flip color at crossover x-positions — is reused from `EndgameScoreOverTimeChart.tsx:99-165` and **extracted into a shared util** (`frontend/src/lib/signedBandGradient.ts` or equivalent) so both charts call the same helper. Crowding control via existing `hiddenKeys` legend toggles is unchanged.

**LLM payload.** Option (b) — `eg_score_gap` remains the primary signal (the `endgame_v33` prompt's §3.1.6 zone bands stay valid in score-gap units); the LLM payload **adds** `endgame_elo` / `non_endgame_elo` / `actual_elo` as supplementary fields for narration. The "lifts/holds" headline metaphor stays; the prompt prose gains "Your Endgame ELO sits at X, vs Y in non-endgame games" style sentences. `_PROMPT_VERSION` bumps to `endgame_v34`.

Full derivation, worked midpoint example, gap-amplification table (~1.5× vs Phase 87.5 at p25/p75), rejected alternatives, and visual-design layering: `.planning/notes/endgame-elo-pr-direct-rebuild.md`. Supersedes (deletes) `.planning/todos/pending/calibrate-endgame-elo-k.md`.

**Depends on:** Phase 87.5 (Endgame ELO Timeline scaffolding to overwrite — `K`, `_endgame_elo_from_score_gap`, `_compute_endgame_elo_weekly_series`, `endgame_elo_timeline` schema field + frontend `EndgameEloTimelineSection`; just-shipped 2026-05-17). Phase 68 / 85 (`_compute_score_gap_timeline`, `ScoreGapTimelinePoint`, `query_endgame_performance_rows` — the input row stream that needs `white_rating`/`black_rating` added to its SELECT). Phase 57 (`query_endgame_elo_timeline_rows` — repo layer reused for the Actual ELO line; untouched). Phase 68 again (`EndgameScoreOverTimeChart.tsx` signed-gradient algorithm — the visual primitive being extracted and reused).

**Requirements:** TBD (EGELO-NN to be drafted during plan-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. **Endgame ELO computation is `R_opp_avg_E + 400·log10(s_E*/(1−s_E*))`** with Laplace smoothing `s_E* = (wins_E + 0.5·draws_E + 1) / (n_E + 2)` over the same trailing 100-game window the score-gap producer uses. Symmetric definition for Non-Endgame ELO over non-endgame games in the same window. Both PRs computed per-week in `_compute_endgame_elo_weekly_series` (or successor) and emitted on `EndgameEloTimelinePoint` alongside `actual_elo`.
  2. **The free constant `K: float = 450.0` is deleted from `app/services/endgame_service.py`** (currently at line 1305) along with its comment block (lines 1283–1304). `_endgame_elo_from_score_gap` is removed or rewritten to the PR formula with a new name (`_performance_rating` or `_pr_from_window`), and the docstring carries the one-paragraph derivation pointing at `.planning/notes/endgame-elo-pr-direct-rebuild.md`. Repo-wide grep for `K · eg_score_gap` returns zero hits outside CHANGELOG.
  3. **`query_endgame_performance_rows` is extended with `white_rating`, `black_rating` columns** on both endgame and non-endgame SELECT branches. The service-layer accumulator derives `opp_rating = white_rating if user_color == 'black' else black_rating` per game and threads per-side opponent-rating running sums through the trailing-window accumulator. Existing callers of this function (Time Pressure sections per the docstring) ignore the new columns; planner verifies no breakage.
  4. **`ScoreGapTimelinePoint` (or a parallel `EndgameEloInputPoint`) carries per-week `endgame_opp_rating_avg` and `non_endgame_opp_rating_avg`** alongside the existing per-side scores and counts. Pydantic v2 schema, ty-clean. Tests cover the asof-join behaviour at week boundaries (matches the existing window-count semantics).
  5. **Frontend `EndgameEloTimelineSection.tsx` renders three lines per visible combo + a signed band:** bold Actual ELO (`strokeWidth ≈ 2`, combo color), fine Endgame ELO and Non-Endgame ELO (`strokeWidth ≈ 1`, same combo color), and an `<Area>` band between the two PRs filled by a horizontal `<linearGradient>` (green on Endgame-leads segments, red on Non-Endgame-leads segments). Z-order: Area → PR fine lines → bold Actual ELO line, so Actual stays readable through the band. Tooltip surfaces all three ELOs + per-week endgame game count. Mobile + desktop parity.
  6. **Shared signed-band-gradient helper extracted.** The crossover-stops algorithm at `EndgameScoreOverTimeChart.tsx:99-165` moves to `frontend/src/lib/signedBandGradient.ts` (or planner-chosen path) with a typed signature `(rows: { x: number; y_low: number; y_high: number; sign: 1 | -1 | 0 }[]) => GradientStop[]`. Both `EndgameScoreOverTimeChart` and `EndgameEloTimelineSection` import and call it. Unit tests cover: monotone-positive, monotone-negative, single crossover, multiple crossovers, exact-zero handling (sign=0 inherits previous-segment color, per Phase 68 UAT fix). Knip stays clean.
  7. **LLM payload gains `endgame_elo`, `non_endgame_elo`, `actual_elo` per timeline point** alongside the existing `score_difference` and counts. `eg_score_gap` remains the primary signal in the prompt; PR-ELO fields are narrational. `_PROMPT_VERSION` bumps to `endgame_v34`. The `endgame_elo_timeline` `SubsectionId` stays in `overall`; the prose template gains "Your Endgame ELO sits at X, vs Y in non-endgame games" style sentences.
  8. **`.planning/todos/pending/calibrate-endgame-elo-k.md` is deleted** (K is removed wholesale; no calibration possible). The Phase 87.5 in-code comment block at `endgame_service.py:1283–1304` referencing it is replaced with a one-paragraph derivation pointing at `.planning/notes/endgame-elo-pr-direct-rebuild.md`.
  9. **Tests cover:** Laplace-smoothed PR on synthetic windowed scores (s=0.5 ⇒ PR = R_opp; s=1.0 (100/0) ⇒ PR bounded near R_opp + ~900; s=0.0 ⇒ PR bounded near R_opp − ~900); midpoint property within tolerance on real-shaped synthetic data with |gap| < 0.25; signed-band gradient stops at crossovers via the shared helper; Vitest render of the three-lines-plus-band layout per combo, including the green-and-red sign-aware fill on a mixed-sign fixture; chart info-popover text mentions the Performance Rating formula in plain language; ty + ruff + knip clean.

**Out of scope:**
  - Re-calibration of §3.1.6 score-gap zone bands into PR-ELO units. The score gap remains the primary LLM signal and the zone bands stay valid in their existing units. A separate PR-ELO zone calibration is a future option only if UAT reveals the prose lands wrong.
  - Per-(platform, TC) PR slope tuning. The `400` is theory-fixed; no knob exists.
  - Replacement of Actual ELO with PR_all (combined-games PR). Actual ELO is what users recognise from the platforms; the chart keeps it as the bold reference line.
  - Naming pedantry. Surfaces use "Endgame ELO / Non-Endgame ELO / Actual ELO" in plain terms; the formal "Performance Rating" distinction lives in the info popover body only.
  - Phase 87.5's chart positioning / section ownership. The chart stays directly under "Endgame Score Gap over Time" in the "Endgame Overall Performance" section.

**Plans:** TBD (drafted during plan-phase; expected 3 plans — backend rewire of the score-gap producer + repo query extension + K deletion + schema additions; frontend dual-line + signed-band rebuild + shared gradient helper extraction + tests; LLM payload extension + `endgame_v34` prompt + CHANGELOG + todo deletion).

**UI hint:** yes

**Context:** Direct outcome of the 2026-05-17 `/gsd-explore` thread reviewing the just-shipped Phase 87.5. Adrian's question — "K seems arbitrary; can the Endgame Score derive an Endgame ELO directly?" — surfaced that the FIDE Performance Rating formula does exactly this with no free parameter. The midpoint property (actual_elo ≈ midpoint(PR_E, PR_N) in the score-gap range) preserves the "lifts up / holds back" semantics that Phase 87.5 was built to restore. The visual redesign (bold Actual + fine PR pair + signed green/red band) makes the endgame-vs-non-endgame gap salient by construction without losing combo-color identification. Full reasoning + worked midpoint example + gap-amplification table + rejected alternatives in `.planning/notes/endgame-elo-pr-direct-rebuild.md`.

### Phase 88: Time Pressure stats rework with hypothesis tests + CIs (INSERTED 2026-05-14)

**Goal:** Replace the Time Pressure at Endgame Entry table and the Time Pressure vs Performance line chart with a unified per-TC card design that mirrors the v1.17 single-bullet doctrine. One card per time control (bullet / blitz / rapid / classical) in a 4-col grid on `xl`, 2-col on `lg`, 1-col below. Each card stacks 6 horizontal bullets: 1 Clock Gap bullet at the top (mean `(my_clock − opp_clock) / base_clock` at endgame entry, centred on 0%, Wilson-style CI as whiskers, color zone from benchmark global distribution) + 5 Score-Delta bullets below (one per 20% pressure quintile measured as base-clock remaining at endgame entry, metric `user_score − cohort_score` centred on 0, Wilson CI from user bin score with cohort N treated as fixed reference, color zone from per-bin cohort inter-user spread). Closes v1.17 endgame rework arc by applying CI + hypothesis-testing rigor to the last table-driven Endgames-page section.

**Depends on:** Phase 87 (per-TC card layout pattern + bullet primitives + mirror-bucket plumbing established). Compatible with Phase 89 Polish running after to extend popover / gating / 375px sweep to the new card surfaces.

**Requirements:** TBD (TIME-01..TIME-NN to be drafted during spec-phase; not yet appended to `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. Endgames page Time Pressure section renders one card per TC; legacy `TimePressureAtEndgameEntrySection` table and `TimePressureVsPerformanceChart` line chart are removed (knip clean).
  2. Each rendered TC card carries 6 bullets: 1 Clock Gap bullet (centre 0%, Wilson-style CI whiskers, color zone from benchmark global distribution per /benchmarks collapse verdict — expected single global zone) + 5 stacked Score-Delta bullets, one per 20% pressure quintile (centre 0, Wilson CI from user bin score, color zone from per-bin cohort inter-user spread).
  3. Sparse-TC gating: the entire TC card hides when total user games for this TC < `MIN_GAMES_PER_TC_CARD` (proposed 20). Individual Score-Delta bullets within a rendered card show a dash + sample count when their bin n < `MIN_GAMES_PER_PRESSURE_BIN` (proposed 5). Clock Gap bullet always renders when the card renders.
  4. /benchmarks skill ships two new metrics: clock-gap-% (per TC × ELO; expected to collapse to a single global zone) and chess-score-per-pressure-bin (per TC × ELO × pressure bin; per-bin Cohen's d collapse verdict). Generated zone constants are surfaced in `frontend/src/generated/endgameZones.ts` (or sibling) and consumed by the bullets.
  5. Backend math reuses Phase 85.1 helpers where possible (`compute_paired_difference_test` for Clock Gap). A new helper `compute_score_delta_vs_reference(user_w, user_d, user_l, user_n, cohort_score)` returns `(delta, p_value, ci_low, ci_high)` treating the cohort score as a fixed reference — unit-tested at boundaries.
  6. InfoPopovers on every bullet explain the reference (0% / cohort baseline), the filter-responsive interpretation, and sparse-n handling. Significance gating follows the established v1.17 triple gate (`n ≥ threshold` ∧ `p < 0.05` ∧ outside neutral band).

**Plans:** 15 plans (8 original + 4 gap-closure + 3 scope-amendment)
- [ ] 88-01-PLAN.md — Backend math helper (compute_score_delta_vs_reference + Wilson test vs arbitrary reference) + unit tests
- [ ] 88-02-PLAN.md — /benchmarks SKILL.md update: §3.3.3 chess-score-per-pressure-bin + §3.3.1 clock-gap-% submetric
- [ ] 88-03-PLAN.md — Zone registry skeleton (placeholders): PRESSURE_BIN_SCORE_NEUTRAL_ZONES + clock_gap_pct + codegen + generated TS + shape tests
- [ ] 88-04-PLAN.md — Backend service _compute_time_pressure_cards + Pydantic schemas + mirror-bucket quintile extension + legacy function deletion + service tests
- [ ] 88-05-PLAN.md — Frontend pressureBulletConfig.ts (domain constants + clampDeltaCi + pressureDeltaZoneColor)
- [ ] 88-06-PLAN.md — EndgameTimePressureCard.tsx (6 bullets, sparse handling, triple-gate) + TS types + Vitest suite
- [ ] 88-07-PLAN.md — EndgameTimePressureSection.tsx orchestrator + Endgames.tsx wiring + EndgameClockPressureSection deletion + full-suite sweep
- [ ] 88-08-PLAN.md — Run /benchmarks §3.3.3 + clock-gap-%, transcribe calibrated values into zone constants, regenerate TS

Gap closure (Phase 88.1, INSERTED 2026-05-17 after VERIFICATION + REVIEW found CR-01 + 6 warnings):
- [ ] 88-09-PLAN.md — Backend rework: drop cohort layer (query_cohort_clock_rows + _compute_cohort_lookup deleted); add same-game opp-quintile parallel split in _iterate_clock_rows; rename schema cohort_score → opp_score; wire MIN_GAMES_PER_PRESSURE_BIN n-gate; remove dead constants (IN-01); drop empty LLM findings (WR-06); amend CONTEXT.md with D-07 retiring D-05
- [ ] 88-10-PLAN.md — Codegen extension: lift MIN_GAMES_PER_TC_CARD + MIN_GAMES_PER_PRESSURE_BIN to endgame_zones.py; emit them + typed getPressureBinBand helper from scripts/gen_endgame_zones_ts.py (WR-04, IN-06); strip stale PLACEHOLDER comments (IN-02)
- [ ] 88-11-PLAN.md — Frontend rename + safety fixes: cohort_score → opp_score TS type + popover copy ("vs opponent"); consume codegen exports (MIN_GAMES_*, getPressureBinBand); fix broken aria-labelledby (WR-02); replace unsafe  non-null assertion with getPressureBinBand narrowing (WR-03); add ARIA test (IN-05)
- [ ] 88-12-PLAN.md — Sanity recalibration: re-run /benchmarks §3.3.3 under opp-quintile semantics; verdict-driven keep-as-is or retune of PRESSURE_BIN_SCORE_NEUTRAL_ZONES; codegen drift gate clean either way

Scope amendment (Phase 88.2, INSERTED 2026-05-17 after re-verification returned human_needed + user raised 5 post-VERIFICATION refinements; LOCKED in 88-CONTEXT.md §2):
- [ ] 88-13-PLAN.md — Frontend polish (A-1 + A-4 + A-5): each TC card stands alone (outer charcoal wrap removed from Endgames.tsx Time Pressure block); hide Q4 quintile + relabel Q0..Q3 as High / Medium / Low / Very Low Pressure; widen quintile-bullet axis to ±30% (PRESSURE_DELTA_DOMAIN = 0.30); ±0.06 D-02 neutral band unchanged
- [ ] 88-14-PLAN.md — Card top-zone restored (A-3): add user_avg_pct/seconds, opp_avg_pct/seconds, avg_clock_diff_seconds, net_timeout_rate to TimePressureTcCard (single-pass aggregator extension in _iterate_clock_rows; no new repo function); frontend top-zone splits Clock Gap bullet + 3-stat row above the quintile bullets
- [ ] 88-15-PLAN.md — Average Clock Difference over Time line chart restored (A-2): new ClockDiffTimelinePoint + ClockDiffTimelineResponse schemas (replaces orphan ClockPressureTimelinePoint/ClockPressureResponse); _compute_clock_diff_timeline aggregator (ISO-week rolling 100-game window); new EndgameClockDiffOverTimeChart.tsx Recharts component; Endgames.tsx renders below the cards grid

**UI hint:** yes

**Context:** Largest phase in v1.17 by anticipated plan count. Two scope risks: (a) The /benchmarks skill update introduces a new "metric-with-sub-bins" pattern the current skill doesn't have — Cohen's d collapse verdicts must run per-pressure-bin separately, not just per-metric. Scope as part of the phase, not a separate pre-req phase. (b) Rapid/classical sample sizes per user often fall below the sparse-TC threshold; worth a quick prod-DB sanity query during plan-phase to size how often each TC card will actually render. Increment handling: `base_clock = initial time only` (no increment) — benchmark data shows this normalization collapses cleanly across ELO and TC. Visual density: 24 bullets on screen at once in the xl breakpoint is intentional — same visual grammar as Phases 85–87, no variety-for-variety bars. See `.planning/notes/time-pressure-stats-rework.md` for full design discussion (delta-vs-paired reasoning, statistical foundation, open plan-phase decisions).

### Phase 88.3: Endgame Stats viz refinements (INSERTED 2026-05-18)

**Goal:** Three independent visual-representation fixes on `/endgames/stats`, surfaced in a `/gsd-explore` chart-quality review. No backend stats, no benchmark refresh — frontend chart/layout work only.

**Depends on:** Phase 88 (Time Pressure cards shipped; this phase does not touch them). Independent of Phase 87.6. Runs before Phase 89 Polish; Phase 89's 375px sweep should cover the restructured Section-1 card.

**Requirements:** TBD (drafted at spec/plan-phase; not yet in `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. **Inactivity-gap annotations** — the over-time timeline charts ("Endgame Score Gap over Time", "Endgame ELO Timeline") keep their ordinal-by-activity x-axis but render a visible axis-break glyph (standard zigzag / double-slash) plus a compact label (e.g. "≈3.1y inactive") at each inactivity gap. Annotations are sized to remain legible and uncrowded at 375px (no overlap with axis ticks, respects the `text-sm` floor / tooltip exception per CLAUDE.md).
  4. **Break-label glyph + reach (SCOPE-AMENDED 2026-05-18 via execute-phase verification feedback; user-confirmed)** — the SC-1 break glyph is the lucide `Palmtree` icon (replacing the zigzag/double-slash) with an enlarged label font (still ≥ `text-sm`). The inactivity-gap break-label annotation is extracted into one reusable chart sub-component and applied to ALL ordinal-axis timeline charts, not just the two endgame ones: + "Bookmarked Openings: Score over Time", "Average Clock Gap over Time", "Chess.com Rating", "Lichess Rating" (6 charts total). This intentionally overrides the original Out-of-scope exclusion of "Average Clock Gap over Time" below.
  2. **ELO Timeline default series** — on first render the ELO Timeline shows only the single most-active series; all other (platform × TC) series remain user-toggleable via the existing legend/toggle. No tabs introduced. Auto-behaviour when only one series exists is unchanged.
  3. **Endgame Overall Performance restructure** — the arrow-flow layout and empty black flanking boxes are removed and replaced by one responsive card. Desktop: two columns separated by a vertical divider. Mobile: stacked, horizontal dividers. Column 1: "Games without Endgame" / "Games with Endgame" (stacked, divider-separated). Column 2: "Eval at Endgame Entry" / "Endgame Score Differences" (stacked, divider-separated). Both columns carry exactly 2 section titles + 4 charts → equal height by construction. No metric/chart content changes — pure layout reorganization. knip clean (no orphaned flow-diagram components/exports).

**Out of scope:** Conv/Parity/Recov + per-type gauges (deferred — see `.planning/seeds/SEED-021-gauges-to-bullet-charts.md`); colour-blind mode (on user's separate to-do, not GSD-tracked). ~~the Time Pressure "Average Clock Gap over Time" chart (explicitly no change)~~ — SUPERSEDED 2026-05-18: SC-4 now includes this chart in the break-label rollout (user-confirmed scope expansion at the execute-phase verification checkpoint).

**Plans:** 4/4 complete (3 original + 1 gap-closure) — completed 2026-05-18 (verification: passed, 4 UAT items user-confirmed)
- [x] 88.3-01-PLAN.md — SC-1: inactivity-gap annotations on both over-time charts + new computeInactivityGaps helper (Wave 0 test)
- [x] 88.3-02-PLAN.md — SC-2: ELO Timeline defaults to single most-active series (MAX_DEFAULT_VISIBLE 3→1)
- [x] 88.3-03-PLAN.md — SC-3: Endgame Overall Performance 2-column card restructure; delete ConnectorArrows
- [x] 88.3-04-PLAN.md — SC-4 (gap closure): Palmtree glyph (BREAK_LABEL_GLYPH_SIZE=22) + BREAK_LABEL_FONT_SIZE (14px) label, compact "Ny"/"Nmo" text; extract shared inactivityGapReferenceLines helper; refactor 2 endgame charts + wire ScoreChart, EndgameClockDiffOverTimeChart, both RatingChart instances (6 charts total). GAP-1/GAP-2 resolved.

**UI hint:** yes

**Context:** Source: `/gsd-explore` session 2026-05-18 reviewing all `/endgames/stats` charts. Full rationale (why keep gauges where non-comparable, ordinal-axis + visible break glyph over a true time scale, default-series over tabs, the height-balanced 2-column card arrangement) in `.planning/notes/endgame-stats-viz-decisions.md` (threads 2, 3, 6). The three changes are independent and can be planned as parallel waves.

### Phase 88.4: Time Pressure card layout refactor (INSERTED 2026-05-18)

**Goal:** Restructure the per-TC Time Pressure cards (`EndgameTimePressureCard.tsx`) shipped in Phase 88 for readability: responsive card grid, a 3-stat header row over the Clock Gap bullet, and a single zone-banded line chart replacing the four stacked Score-Gap bullets. Frontend-only — no backend stats, no benchmark refresh (reuses the existing `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`, collapsed across TC).

**Depends on:** Phase 88 (Time Pressure cards shipped — this phase reshapes them). Independent of Phase 88.3 / 87.6. Runs before Phase 89 Polish; Phase 89's 375px sweep should cover the refactored cards.

**Requirements:** TBD (drafted at spec/plan-phase; not yet in `.planning/REQUIREMENTS.md`)

**Success Criteria** (what must be TRUE):
  1. **Responsive card grid** — when ≥2 TC cards are shown the grid is full-width (3×1 or 2×2 depending on card count); when exactly 1 card is shown it renders at half width. No horizontal scroll at any breakpoint.
  2. **"Remaining Time at Endgame Entry" header row** — the three figures sit on one row directly above the Clock Gap bullet chart: "You: 33% (99s)" left-aligned, "Gap: −22.4% <info icon>" centered (info icon retained, opens the existing popover), "Opp: 57% (170s)" right-aligned. The Net flag rate bullet stays below the Clock Gap bullet chart, unchanged.
  3. **"Score Gap by Remaining Time" line chart** — the four stacked per-bucket bullet charts are replaced by one horizontal line chart reusing the "Average Clock Gap over Time" (`EndgameClockDiffOverTimeChart`) visual layout: colored zone bands + 0-centered y-axis, but the x-axis is the 4 ordinal pressure-bucket labels (not dates). Each of the 4 datapoints renders a CI whisker. The neutral zone is collapsed across TC (§3.3.3 in `reports/benchmarks-latest.md` — TC d_max within collapse threshold; bounds close enough to pool, user-confirmed). Hover tooltip surfaces the per-bucket statistics currently behind the four info icons (the info icons themselves are removed, superseded by the tooltip). knip clean (no orphaned per-bucket bullet components/exports).

**Out of scope:** Backend stats / new statistical methods / benchmark refresh (none — pure frontend reshape of already-computed values); the Clock Gap and Net flag rate bullet primitives themselves (only their surrounding layout changes); full 375px parity audit (deferred to Phase 89, though basic responsive correctness per SC-1 is in scope).

**Plans:** 3/3 complete — completed 2026-05-19, shipped in PR #109 (verification: passed, 3/3 must-haves; UAT: 4/4 passed after in-browser polish rounds; WR-01 resolved — text-xs to match the sibling chart, documented inline)
- [x] 88.4-01-PLAN.md — New ScoreGapByTimePressureChart component + Wave 0 test scaffold (SC-3 chart)
- [x] 88.4-02-PLAN.md — EndgameTimePressureSection dynamic responsive grid (SC-1)
- [x] 88.4-03-PLAN.md — EndgameTimePressureCard 3-stat header row + chart integration, knip-clean (SC-2, SC-3)

**UI hint:** yes

**Context:** Source: user layout review of `screenshots/tc-cards.png` 2026-05-18 (the 6-undifferentiated-rows-per-card density problem; the score-gap section answers a trend question but hides the trend behind 4 disconnected gauges). The line-chart swap reuses the established zone-band/0-line chart pattern so Blitz vs Rapid become directly comparable row-for-row. TC-collapse for the score-gap neutral zone is the user's explicit call (§3.3.3 bounds close enough).

### Phase 89: Polish — popovers, gating decisions, automation rules, 375px parity

**Goal:** Resolve the two polish decisions left for plan-phase (peer-bullet neutral band; gauge significance gating), apply CLAUDE.md browser-automation rules (`data-testid`, ARIA labels, semantic HTML) consistently across all new card surfaces, and verify mobile parity at 375px across Sections 1 / 2 / 3 + the Phase 88 Time Pressure cards.
**Depends on:** Phases 85, 86, 87, 88 (all section card layouts shipped, including Time Pressure)
**Requirements:** POLISH-01, POLISH-02, POLISH-03, POLISH-04
**Success Criteria** (what must be TRUE):
  1. Peer-bullet neutral band decision is documented and applied uniformly (either reuse `SCORE_GAP_NEUTRAL_MIN/MAX` across Conv/Parity/Recov, or introduce dedicated `CONV_DIFF_NEUTRAL_*` / `RECOV_DIFF_NEUTRAL_*` from the benchmarks distribution).
  2. Gauge significance gating decision is documented and applied (keep gauge always-colored with bullet font-gated, or extend gating to the gauge for visual consistency).
  3. Every interactive element on the new card surfaces has a `data-testid`, every icon-only button has an `aria-label`, and semantic HTML (`<button>` / `<a>` / `<nav>` / `<main>` / `<form>`) is used per CLAUDE.md browser automation rules.
  4. All four sections (Sections 1 / 2 / 3 + Phase 88 Time Pressure) render correctly at 375px (no horizontal scroll, 44px tappable targets, popovers reachable, peer-bullet fallback documented if triggered in Phase 87).
**Plans:** TBD
**UI hint:** yes

## Traceability

| Requirement | Phase |
|-------------|-------|
| SEC1-01 | Phase 85 |
| SEC1-02 | Phase 85 |
| SEC1-03 | Phase 85 |
| SEC1-04 | Phase 85 |
| SEC1-05 | Phase 85 |
| SEC1-06 | Phase 85 |
| SEC1-07 | Phase 85 |
| SEC1-08 | Phase 85.1 |
| SEC1-09 | Phase 85.1 |
| SEC1-10 | Phase 85.1 |
| SEC1-11 | Phase 85.1 |
| SEC2-01 | Phase 86 |
| SEC2-02 | Phase 86 |
| SEC2-03 | Phase 86 |
| SEC2-04 | Phase 86 |
| SEC2-06 | Phase 86 |
| SEC2-07 | Phase 86 |
| SEC2-08 | Phase 86 |
| SEC2-09 | Phase 86 |
| SEC2-10 | Phase 86 |
| SEC3-01 | Phase 87 |
| SEC3-02 | Phase 87 |
| SEC3-04 | Phase 87 |
| SEC3-05 | Phase 87 |
| SEC3-06 | Phase 87 |
| SEC3-07 | Phase 87 |
| DATA-02 | Phase 84 |
| POLISH-01 | Phase 89 |
| POLISH-02 | Phase 89 |
| POLISH-03 | Phase 89 |
| POLISH-04 | Phase 89 |

**Coverage:** 31 / 31 v1 requirements mapped. No orphans. (Was 30; SEC2-05, SEC3-03, DATA-01 dropped 2026-05-12 per single-bullet doctrine pivot — see `.planning/notes/v1.17-single-bullet-doctrine.md`. +4 on 2026-05-14 with SEC1-08..11 for Phase 85.1.)

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 84. Data plumbing — mirror-rate audit | 1/1 | Complete | 2026-05-13 |
| 85. Section 1 — Games with vs without Endgame cards | 0/TBD | Not started | — |
| 85.1. Hypothesis tests + CIs for Endgame Score Differences (INSERTED) | 0/4 | Not started | — |
| 86. Section 2 — Endgame Metrics 4-card layout | 0/5 | Not started | — |
| 87. Section 3 — Per-type Endgame Type Breakdown cards | 0/3 | Not started | — |
| 87.1. Per-span ΔES metric for endgame types (INSERTED) | 1/4 | Plan 02 shipped (backend math+repo+service+schema); Plan 01 + Wave 2 pending | — |
| 88. Time Pressure stats rework with hypothesis tests + CIs (INSERTED) | 0/TBD | Not started | — |
| 89. Polish — popovers, gating, automation, 375px parity | 0/TBD | Not started | — |
