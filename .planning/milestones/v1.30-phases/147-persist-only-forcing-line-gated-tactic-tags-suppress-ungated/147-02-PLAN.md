---
phase: 147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/
  - tests/test_migration_suppress_ungated_tactic_tags.py
autonomous: true
requirements: [SEED-074]
user_setup:
  - service: prod-db (read-only)
    why: "Confirm the old-corpus candidate row count before shipping (migration duration sizing, D-04 planner constraint)"
    dashboard_config:
      - task: "Run the RESEARCH.md candidate-count query via bin/prod_db_tunnel.sh + flawchess-prod-db MCP; record the count. Batch size 100000 is index-driven and adequate regardless — this is sizing confirmation, not a gate."
        location: "bin/prod_db_tunnel.sh (localhost:15432) → mcp__flawchess-prod-db__query"

must_haves:
  truths:
    - "The Alembic upgrade() suppresses (sets to NULL) allowed/missed tactic columns ONLY for cp-based rows: `<orientation>_pv_lines IS NULL` AND joined game_positions.eval_cp IS NOT NULL AND `<orientation>_tactic_motif IS NOT NULL`, applied per orientation."
    - "Mate-adjacent rows (joined game_positions.eval_cp IS NULL) KEEP their raw tactic tags (D-06)."
    - "D-06 `[]`-sentinel rows (allowed_pv_lines = '[]'::jsonb, which is NOT NULL) KEEP their raw tactic tags."
    - "Rows with blobs already populated (`<orientation>_pv_lines` NOT NULL) are untouched."
    - "Re-running the migration SQL a second time updates zero rows (idempotent)."
  artifacts:
    - "alembic/versions/<generated>_suppress_ungated_tactic_tags.py — batched DO $$ ... WHILE rows_updated > 0 LOOP UPDATE joined to game_positions; downgrade() a documented no-op"
    - "tests/test_migration_suppress_ungated_tactic_tags.py — seeds every carve-out case + idempotency re-run"
  key_links:
    - "game_flaws.(user_id, game_id, ply-1) JOIN game_positions PK → pre_flaw_eval_cp; the existing partial index ix_game_flaws_blob_backfill (WHERE allowed_pv_lines IS NULL) drives the candidate scan (no new index) (D-04)."
  prohibitions:
    - "MUST NOT use a bare `UPDATE game_flaws WHERE allowed_pv_lines IS NULL AND allowed_tactic_motif IS NOT NULL` without the game_positions join — that wipes mate-adjacent tags (Pitfall 2)."
    - "MUST NOT use an `id`-surrogate batching pattern — game_flaws PK is composite (user_id, game_id, ply); batch on the composite key (Pitfall 5)."
    - "MUST NOT create a new index (existing partial index already covers the predicate) (D-04)."
    - "MUST NOT match the `[]` sentinel: `allowed_pv_lines IS NULL` already excludes an empty JSONB array; do not add a predicate that would catch it."
---

<objective>
Part A old-corpus data migration (D-03, D-04): a guarded, batched Alembic DATA migration that suppresses
raw ungated cp-based tactic tags on the pre-Phase-142 corpus, with the SAME carve-outs as the go-forward
path — KEEP mate-adjacent (`pre_flaw_eval_cp IS NULL`) and D-06 `[]`-sentinel tags. Runs automatically at
container startup; must be index-driven and batched so it does not stall startup.

Purpose: deliver the "no ungated tags anywhere" invariant immediately for already-imported games rather
than waiting on tier-4 to drain the whole corpus (D-03 OVERRIDES the seed's go-forward-only lean).
Output: the migration + a migration test covering every carve-out and idempotency.

## Artifacts this plan produces
- New Alembic revision file `alembic/versions/<generated>_suppress_ungated_tactic_tags.py` (revision id
  auto-generated by `alembic revision`; description "suppress ungated tactic tags old corpus").
- `tests/test_migration_suppress_ungated_tactic_tags.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated/147-CONTEXT.md
@.planning/phases/147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated/147-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the batched old-corpus suppression migration</name>
  <files>alembic/versions/</files>
  <read_first>
    - RESEARCH.md "Part A — Old-Corpus Alembic Data Migration": the full tested `upgrade()` SQL (dry-run
      verified on dev, rolled back), the per-orientation predicate, the `game_positions` join, and the
      no-op downgrade rationale. Use this SQL verbatim as the reference.
    - alembic/versions/20260327_124752_fb62990270fd_reclassify_pawnless_endgames_from_mixed.py — the
      repo's established `DO $$ ... WHILE rows_updated > 0 LOOP ... GET DIAGNOSTICS rows_updated = ROW_COUNT`
      batched idiom (batch_size := 100000) wrapped in a single `op.execute()`.
    - alembic/versions/20260630_220000_c3f5d1e8a092_ix_game_flaws_blob_backfill.py — confirms the partial
      index `WHERE allowed_pv_lines IS NULL` exists and the ~3.18M prod row estimate.
    - app/models/game_flaw.py and app/models/game_position.py — column names + the composite PK join key.
  </read_first>
  <action>
    Generate a new revision with `uv run alembic revision -m "suppress ungated tactic tags old corpus"`
    (down_revision = current head). Implement `upgrade()` as a single `op.execute()` containing the
    RESEARCH.md tested SQL: a `DO $$` block with `batch_size CONSTANT int := 100000` looping `WHILE
    rows_updated > 0`, selecting a `batch` CTE keyed on the composite PK `(user_id, game_id, ply)`, joined
    to `game_positions gp ON gp.user_id=gf.user_id AND gp.game_id=gf.game_id AND gp.ply = gf.ply - 1`,
    computing `suppress_allowed` and `suppress_missed` flags PER ORIENTATION (each gated on its OWN blob
    column: `allowed_pv_lines IS NULL` / `missed_pv_lines IS NULL`, plus `gp.eval_cp IS NOT NULL` and the
    respective motif NOT NULL), `LIMIT batch_size`; then `UPDATE game_flaws ... FROM batch` setting the
    four allowed_* columns and four missed_* columns to NULL via `CASE WHEN suppress_<o> THEN NULL ELSE
    <col> END`, with `GET DIAGNOSTICS rows_updated = ROW_COUNT`. Implement `downgrade()` as `pass` with a
    comment stating the raw pre-gate motif is unrecoverable (lossy one-way data migration, matching the
    repo's precedent). Add a module docstring citing D-03/D-04, the carve-outs, the index-driven plan, and
    the idempotency property. Do NOT create a new index. Do NOT use an `id` surrogate for batching.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic current</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies cleanly on the per-run test DB (`alembic upgrade head` succeeds).
    - Predicate joins game_positions for the cp check and gates each orientation on its own blob column.
    - Batching uses the composite PK, batch_size 100000, no new index, no `id` surrogate.
    - `downgrade()` is a documented no-op.
  </acceptance_criteria>
  <done>The batched, index-driven, carve-out-correct migration exists and upgrades head.</done>
</task>

<task type="auto">
  <name>Task 2: Migration test — carve-outs + idempotency</name>
  <files>tests/test_migration_suppress_ungated_tactic_tags.py</files>
  <read_first>
    - tests/test_migration_wipe_eval_only_residue.py — the exact template: seed rows via the ORM in the
      `db_session` fixture, execute the migration SQL verbatim (kept in sync via a mirrored comment),
      assert only the correct rows changed.
    - RESEARCH.md "Migration test (pattern already established in this repo)" — the five seed cases to cover.
  </read_first>
  <behavior>
    - Seed (1) cp-based flaw (`allowed_pv_lines IS NULL`, `allowed_tactic_motif` set, pre-flaw `eval_cp`
      non-NULL) → suppressed to NULL after migration.
    - Seed (2) mate-adjacent flaw (game_positions row with `eval_cp=NULL, eval_mate=<int>`,
      `allowed_tactic_motif` set) → KEPT after migration.
    - Seed (3) D-06 sentinel (`allowed_pv_lines = '[]'::jsonb`, `allowed_tactic_motif` set) → KEPT.
    - Seed (4) blobs-populated row (`allowed_pv_lines` non-NULL) → KEPT (out of scope for suppression).
    - Re-run (5): executing the migration SQL a second time updates zero rows (idempotent).
  </behavior>
  <action>
    Create the test mirroring `test_migration_wipe_eval_only_residue.py`: seed the five cases above (both
    the `allowed` and `missed` orientation for at least the suppress + one KEEP case, since suppression is
    per orientation), run the migration SQL, and assert each row's post-state. Include an idempotency
    assertion that re-executing the loop body changes zero rows. Keep the SQL string mirrored from the
    migration file with a sync comment. Respect per-run-DB isolation (do not reset the dev DB).
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_suppress_ungated_tactic_tags.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All five seed cases assert correctly, including per-orientation suppression.
    - Idempotency re-run asserts zero rows changed.
    - Test passes under the per-run cloned DB fixture.
  </acceptance_criteria>
  <done>The migration is proven to suppress exactly the cp candidates, preserve all carve-outs, and be idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy/entrypoint.sh migration replay → DB | runs against a quiescent backend before uvicorn starts; risk is duration, not live-traffic locks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-05 | Tampering/Integrity | old-corpus UPDATE predicate | high | mitigate | Join to game_positions for the cp check; gate each orientation on its own blob column; `IS NULL` never matches the `[]` sentinel — preserves mate-adjacent + sentinel tags (Pitfall 2). |
| T-147-07 | Denial of Service | migration on high-cardinality game_flaws at container startup | medium | mitigate | Batched loop (batch_size 100000) + existing partial index `ix_game_flaws_blob_backfill` makes each batch a fast index-driven UPDATE; prod count sizing confirmed via read-only MCP (user_setup). |
| T-147-SC | Tampering | dependency installs | high | accept | No new packages this plan. |
</threat_model>

<verification>
- `uv run alembic upgrade head` on the test DB succeeds.
- `uv run pytest tests/test_migration_suppress_ungated_tactic_tags.py -x` passes.
- `uv run ty check app/ tests/` (zero errors).
- Manual (user_setup): prod candidate-count query recorded before shipping; confirm 100000 batch is adequate.
</verification>

<success_criteria>
The old corpus carries no raw ungated cp tactic tags after migration, mate-adjacent and `[]`-sentinel
tags are preserved, and the migration is idempotent and index-driven.
</success_criteria>

<output>
Create `.planning/phases/147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated/147-02-SUMMARY.md` when done
</output>
