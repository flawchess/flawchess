---
phase: 144-user-28-a-b-validation
plan: 02
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - reports/retag/ab-validation-2026-06-30.md
  - app/services/forcing_line_gate.py
autonomous: false
requirements: [VALID-02]
user_setup:
  - service: dev-postgres
    why: "The harness SELECTs user-28's 216 blob-bearing flaws from the running dev DB"
    dashboard_config:
      - task: "Ensure the dev DB is up before running the harness"
        location: "docker compose -f docker-compose.dev.yml -p flawchess-dev up -d"
must_haves:
  truths:
    - "Running the harness against the live dev DB for user 28 at margin 0.35 produces a committed reports/retag/ markdown with non-empty per-motif tables and a dropped-case list."
    - "At least one lichess deep-link in the report is manually confirmed to open the correct position in-browser before the report is committed."
    - "The hand-check produces an explicit false-negative count (good tags killed) folded into the A/B summary — a HUMAN-UAT judgment, never fabricated by the executor."
    - "D-04a: the hand-check covers ALL dropped cases when the drop set is ≤30; only if the gate drops more than ~30 does it fall back to a motif-stratified ~30 sample so the noisy motifs (sacrifice / clearance / capturing-defender) get eyeballs."
    - "ONLY_MOVE_WIN_PROB_MARGIN is committed: 0.35 confirmed (default) via a pointer comment + A/B summary justification, OR changed only if the hand-check shows 0.35 fails."
  artifacts:
    - "reports/retag/ab-validation-YYYY-MM-DD.md (the committed A/B + hand-check deliverable)"
    - "app/services/forcing_line_gate.py line 52 pointer comment referencing the report"
  key_links:
    - "report dropped-case lichess link -> human adjudication (FN vs correct drop)"
    - "hand-check FN count -> A/B summary -> ONLY_MOVE_WIN_PROB_MARGIN decision (keep 0.35 default)"
---

<objective>
Run the Plan 01 harness against the live dev DB for user 28, commit the resulting `reports/retag/ab-validation-YYYY-MM-DD.md`, then perform the HUMAN-UAT hand-check of the dropped cases (false-negative count) and commit the final `ONLY_MOVE_WIN_PROB_MARGIN` decision with the A/B summary as justification.

Purpose: VALID-02 SC3 (hand-check → explicit false-negative count) and SC4 (final margin committed). Per D-03/D-03a the default is **keep 0.35**: the "commit" when 0.35 is confirmed is the recorded justification (A/B summary + a pointer comment at the constant), not necessarily a value change.
Output: committed report; `forcing_line_gate.py` pointer comment; filled-in FN count + A/B summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-user-28-a-b-validation/144-CONTEXT.md
@.planning/phases/144-user-28-a-b-validation/144-RESEARCH.md
@app/services/forcing_line_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the A/B report against dev user-28</name>
  <files>reports/retag/ab-validation-2026-06-30.md</files>
  <read_first>
    - scripts/ab_validate_gate.py (the harness from Plan 01 — CLI flags, run entrypoint)
    - 144-RESEARCH.md §Pattern 7 (report section set), §Pitfall 5 (small-N caveats N<10)
    - CLAUDE.md "Test isolation / dev DB" (dev DB must be running; never reset it — feedback_no_dev_db_reset_in_plans)
  </read_first>
  <action>
    Confirm the dev DB is running (`docker compose -f docker-compose.dev.yml -p flawchess-dev ps`); if not, start it with the compose up command — do NOT run bin/reset_db.sh (the 216 blobs live in the existing dev DB). Run `uv run python scripts/ab_validate_gate.py --db dev --user-id 28 --margin 0.35` (optionally add `--neighbourhood` to include the 0.30/0.40 context columns per D-03). This produces `reports/retag/ab-validation-2026-06-30.md` (date is `datetime.now`; the filename in front-matter is illustrative — use whatever date the run emits). Sanity-check the output: ~216 flaws examined, per-motif tables populated for both orientations, dropped-case list present, motifs with N<10 flagged. Do NOT hand-fill the FN count or margin decision yet — those are Task 2/3. Commit the report as-is (the executor's mechanical artifact).
    If the harness raises (e.g. dev DB unreachable, zero blob rows because the local dev DB differs from the feasibility snapshot), STOP and surface the error in the SUMMARY rather than fabricating a report — the 216-flaw universe is a load-bearing assumption from CONTEXT.md.
  </action>
  <acceptance_criteria>
    - A file matching `reports/retag/ab-validation-*.md` exists and is non-empty.
    - `grep -c 'Per-Motif' reports/retag/ab-validation-*.md` ≥ 1 and the Dropped Cases section is present.
    - The report header records `Margin tested: 0.35` and a flaw-examined count > 0.
    - No DB write occurred (the script is read-only by construction from Plan 01).
  </acceptance_criteria>
  <verify>
    <automated>ls reports/retag/ab-validation-*.md >/dev/null 2>&1 && grep -lq 'Dropped Cases' reports/retag/ab-validation-*.md</automated>
  </verify>
  <done>The committed A/B report exists with populated per-motif tables, depth distribution, and a dropped-case list with lichess links; FN/summary sections still blank placeholders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Hand-check dropped cases → false-negative count + margin decision (HUMAN-UAT)</name>
  <action>Human-only gate: adjudicate dropped cases via lichess deep-links and decide the margin. See what-built / how-to-verify / resume-signal below. The executor performs no automated work in this task; it pauses for the human signal and resumes into Task 3.</action>
  <what-built>
    The A/B report at `reports/retag/ab-validation-YYYY-MM-DD.md` lists every dropped case (a tag the ungated arm produced that the gated arm at 0.35 suppressed) with motif, FEN, PV line, and a lichess analysis deep-link.
  </what-built>
  <how-to-verify>
    1. Open the report. First, click ONE lichess deep-link (any dropped case) and confirm it opens the correct position on the lichess analysis board — this validates the [ASSUMED] URL format (research A1, LOW confidence). If links are broken, note the corrected format so Task 3 can fix the helper.
    2. Per D-04a: if the drop set is ≤30 cases, hand-check ALL of them; if >30, hand-check a motif-stratified ~30 (ensure the noisy motifs — sacrifice / clearance / capturing-defender — get eyeballs).
    3. For each checked case, judge via the lichess board: is this a FALSE NEGATIVE (a genuinely good tactic the gate killed) or a CORRECT DROP (noise the gate rightly removed)? Tally the false-negative count.
    4. Decide the margin per D-03: KEEP 0.35 unless the hand-check shows 0.35 is clearly killing good tags (high FN rate) or leaving obvious noise. Note any GATEX-01 signal (FN rate >~10% on the both-winning-captures class) for Phase 145, but do not act on it here.
  </how-to-verify>
  <resume-signal>Reply with: (a) lichess links OK or the corrected URL format; (b) total dropped + false-negative count; (c) margin decision — "keep 0.35" or "change to X" with one-line rationale.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the final margin decision + fill the A/B summary</name>
  <files>app/services/forcing_line_gate.py, reports/retag/ab-validation-2026-06-30.md</files>
  <read_first>
    - app/services/forcing_line_gate.py lines 49-52 (the `ONLY_MOVE_WIN_PROB_MARGIN = 0.35` constant + its D-07 provisional comment)
    - the generated reports/retag/ab-validation-*.md (the placeholder FN + A/B Summary sections to fill)
    - 144-RESEARCH.md §Code Examples (the pointer-comment wording at line 52)
  </read_first>
  <action>
    Using the human's resume-signal values from Task 2:
    (1) Fill the report's "False Negative Count (HUMAN-UAT)" section with the total-dropped and false-negative counts, and the "A/B Summary & Margin Justification" section with the keep/change decision and one-line rationale. If the human reported a corrected lichess URL format, fix `lichess_analysis_url` in scripts/ab_validate_gate.py and re-run the harness to regenerate links before finalizing.
    (2) In app/services/forcing_line_gate.py at the `ONLY_MOVE_WIN_PROB_MARGIN` constant (line ~52): if the decision is KEEP 0.35, leave the value at 0.35 and ADD a pointer comment recording that Phase 144's A/B hand-check confirmed it, referencing the report path (per D-03a the commit IS this recorded justification, no value change). If the decision is CHANGE, set the new value AND add the same pointer comment with the rationale. Do NOT invent or auto-decide either the FN count or the margin — both come from the human signal.
    (3) Re-run `uv run pytest tests/scripts/test_ab_validate_gate.py -x` (constant change, if any, must not break the gate's existing unit tests — `uv run pytest -n auto -x` for the wave merge). Commit report + forcing_line_gate.py together.
  </action>
  <acceptance_criteria>
    - The report's FN-count and A/B-summary sections are filled with the human-provided numbers/decision (no remaining `_[fill in]_` placeholders).
    - `grep -n 'Phase 144' app/services/forcing_line_gate.py` returns a match at the constant (pointer comment present) and `grep -n 'ONLY_MOVE_WIN_PROB_MARGIN' app/services/forcing_line_gate.py` shows the committed value (0.35 unless the human chose to change it).
    - `uv run pytest -n auto -x` green (gate unit tests + A/B tests).
    - `uv run ruff check app/ scripts/` and `uv run ty check app/ tests/` pass.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Phase 144' app/services/forcing_line_gate.py && uv run pytest tests/scripts/test_ab_validate_gate.py -x</automated>
  </verify>
  <done>VALID-02 closed: the report carries the explicit false-negative count + A/B summary, and ONLY_MOVE_WIN_PROB_MARGIN is committed (0.35 confirmed with justification, or changed) with a pointer comment to the report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → dev DB | Read-only SELECT of user-28 dev data + a committed markdown report. No write-back, no network surface. |
| lichess deep-link → browser | Outbound URL the developer clicks for in-browser adjudication; FEN is the developer's own dev position. No credentials, no callback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-04 | Tampering | ONLY_MOVE_WIN_PROB_MARGIN constant | medium | mitigate | The margin change (or confirm) is gated on the human hand-check (D-03); the executor never auto-decides the value. The gate's existing unit tests re-run after any change to catch a regression. |
| T-144-05 | Information Disclosure | committed report + lichess links | low | accept | Report holds only the developer's own dev FENs/PVs and public lichess analysis links; committed intentionally as the deliverable. No secrets/PII. |
| T-144-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — stdlib + project-internal only (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `reports/retag/ab-validation-*.md` exists, non-empty, with filled FN + summary sections.
- One lichess link manually confirmed (human gate).
- `forcing_line_gate.py` pointer comment present; constant committed.
- `uv run pytest -n auto -x` green; ruff + ty clean.
</verification>

<success_criteria>
- The hand-check produced an explicit false-negative count folded into the A/B summary (VALID-02 SC3).
- ONLY_MOVE_WIN_PROB_MARGIN is committed with A/B-summary justification — 0.35 confirmed by default, changed only if the hand-check failed it (VALID-02 SC4).
</success_criteria>

<output>
Create `.planning/phases/144-user-28-a-b-validation/144-02-SUMMARY.md` when done.
</output>
