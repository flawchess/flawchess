---
phase: 175-board-filter-gem-great-consumption
plan: 05
type: execute
wave: 4
depends_on: ["175-03"]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/hooks/useGemSweep.ts
  - frontend/src/hooks/__tests__/useGemSweep.test.ts
  - frontend/src/pages/__tests__/Analysis.test.tsx
autonomous: false
requirements: [BOARD-02]

must_haves:
  truths:
    - "For an analyzed game's mainline ply, the board / move-list / chart / popover render gem AND great directly from EvalPoint.best_move_tier (stored), not from a live classifyGem/classifyGreat compute (D-01/D-03)."
    - "A mainline ply with NO stored row (best_move_tier null) renders no marker and triggers NO live sweep or live-at-cursor grade — row-absence is authoritative (BOARD-02 'no background sweep delay')."
    - "The live-engine fallback (useGemSweep AND the Phase-163 live-at-cursor gemC1/gemGrading/gemByNode mechanism) fires ONLY for positions with no stored best-move row: off-mainline variations, free-play/pasted-FEN, and unanalyzed bot games (D-01/D-03c). BOTH mechanisms gate on 'no stored tier' (Pitfall 2)."
    - "useGemSweep is demoted (not deleted) to a documented free-play-only fallback; SEED-107 closes as superseded (BOARD-02, D-01)."
    - "WR-06 is fixed: the sweep's resolveCandidate and the CR-03 watchdog/fast-fail effects use useCallback([]) and are listed in their effect deps (stale-closure trap killed, D-01a)."
  prohibitions:
    - "The stored mainline path must NOT call classifyGem/classifyGreat (D-03c) — those run only when no stored tier exists for the node."
    - "The sweep must NOT run over the mainline plies of an analyzed game (WR-01/03/05 are mooted by demotion — the sweep no longer sweeps a stored mainline at all, D-01a)."
  artifacts:
    - "A per-node stored-tier resolution in Analysis.tsx (mainline gem/great from EvalPoint.best_move_tier + maia_prob)."
    - "useGemSweep.enabled + needParentGemGrade fallback-only gates (no stored tier / unanalyzed)."
    - "WR-06 useCallback([]) wrap + IN-01/03/04 cleanups on the fallback path."
  key_links:
    - "EvalPoint.best_move_tier (Plan 02/03) -> Analysis per-node mainline tier resolution -> board/move/chart/popover markers."
    - "'node has stored tier?' signal -> gates useGemSweep.enabled AND needParentGemGrade (both live mechanisms, Pitfall 2)."
---

<objective>
Complete BOARD-01/BOARD-02 on the analysis board: for an analyzed game's mainline,
render gem AND great from the stored `EvalPoint.best_move_tier` instantly, and demote
BOTH live gem mechanisms (the Phase-172 `useGemSweep` sweep-ahead AND the Phase-163
`gemC1`/`gemGrading`/`gemByNode` live-at-cursor path) to a documented fallback that
fires only where no stored row exists (off-mainline variations, free-play, unanalyzed
bot games). Fix the deferred WR-06 stale-closure trap and the IN-* cleanups on the
retained fallback path.

Purpose: markers appear on first paint with no live Maia/Stockfish round-trip on
mainline navigation, satisfying the "no background sweep delay" criterion by
construction. SEED-107 closes as superseded.
Output: mainline stored-tier consumption + both live mechanisms gated to fallback-only
+ WR-06/IN fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-board-filter-gem-great-consumption/175-CONTEXT.md
@.planning/phases/175-board-filter-gem-great-consumption/175-RESEARCH.md
@.planning/phases/175-board-filter-gem-great-consumption/175-PATTERNS.md
@.planning/todos/pending/172-deferred-review-findings.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)
- Per-node mainline stored-tier resolution in Analysis.tsx (gem/great + maia_prob from `EvalPoint`).
- Fallback-only gates on `useGemSweep.enabled` and `needParentGemGrade` (+ `gemC1`/`gemGrading`).
- WR-06 `useCallback([])` on `resolveCandidate` + CR-03 watchdog/fast-fail effects; IN-01/03/04 cleanups.
- `useGemSweep.ts` docstring demotion note (free-play-only fallback; SEED-107 superseded).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render mainline gem/great from stored EvalPoint.best_move_tier</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/pages/__tests__/Analysis.test.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx — the gem consumption sites: the display memo `qualityBySanWithGem` (~1214-1246), the `resolveGemFor`/`gemByNode`/`sweep.gemByPly` precedence (~1683-1696), `boardSquareMarkers`/`moveListMarkers` folds (grep for both), and how `gameData.eval_series` (EvalPoint[]) is already read (~1300-1312, ~1617).
    - frontend/src/types/library.ts EvalPoint (now with `best_move_tier`/`maia_prob` from Plan 03).
    - frontend/src/components/board/boardMarkers.tsx (`SquareMarker.great` from Plan 03), UnifiedMovePopover (`isGreat` from Plan 03), VariationTree (great fields from Plan 03), moveQuality 'great' (Plan 03).
    - frontend/src/components/analysis/GemMoveBadge.tsx (tier prop from Plan 03) for the popover maia_prob stat.
    - 175-RESEARCH.md Pitfall 2 and Open Question 1 (both mechanisms in scope), Pitfall 3 (row-absence authoritative).
  </read_first>
  <behavior>
    - Test (Analysis.test.tsx, page-level): loading an analyzed game whose eval_series has a ply with best_move_tier='great' renders the great badge on the board and move list for that ply, sourced from stored data (no worker activity mocked/needed).
    - Test: a ply with best_move_tier='gem' renders the gem badge (parity with today).
    - Test: a mainline ply with best_move_tier=null renders NO marker and does not schedule a live grade.
    - Test: the popover for a stored gem/great shows the maia_prob stat from EvalPoint.maia_prob.
  </behavior>
  <action>
    Build a per-node mainline tier resolution in Analysis.tsx: from `gameData.eval_series`
    (EvalPoint[], ply-ascending) derive a `ply -> { tier, maiaProb }` lookup for the loaded
    game, and a `storedTierForNode(nodeId, ply)` that returns the stored `best_move_tier` +
    `maia_prob` when the node is on the analyzed mainline (its ply has an EvalPoint), else null.
    Route the board corner badge (`boardSquareMarkers`) and the move-list glyph
    (`moveListMarkers`) through this stored resolution for mainline nodes, emitting a
    `SquareMarker.gem` OR `SquareMarker.great` (and the VariationTree great field) per the stored
    tier — reuse the existing `resolveGemFor` precedence site so stored-tier wins for mainline
    nodes and the live fallback (gemByNode/sweep.gemByPly) is consulted only when there is no
    stored tier. Extend `qualityBySanWithGem` so a stored mainline gem/great recolors the
    reconciled-best candidate directly from the stored tier instead of calling classifyGem.
    Wire the popover (UnifiedMovePopover isGreat / GemMoveBadge tier) to the stored maia_prob for
    the "~X% of rating-peers" stat. Handle great everywhere gem is handled (D-02b). Add
    page-level tests to Analysis.test.tsx.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run Analysis && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "best_move_tier" frontend/src/pages/Analysis.tsx` shows the stored-tier lookup driving the board/move-list markers.
    - `cd frontend && npm test -- --run Analysis` passes, including the stored-great, stored-gem, null-tier-no-marker, and popover-maia_prob cases.
    - Temporarily forcing `storedTierForNode` to return null (simulating no stored data) makes the stored-great page test fail — proving the marker comes from stored data, not a live path.
    - `npx tsc -b` clean.
  </acceptance_criteria>
  <done>An analyzed game's mainline renders gem AND great from stored EvalPoint data on the board, move list, chart recolor, and popover, with null tiers rendering nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Demote both live gem mechanisms to fallback-only + WR-06/IN fixes</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/hooks/useGemSweep.ts, frontend/src/hooks/__tests__/useGemSweep.test.ts</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx — `needParentGemGrade` (~1592-1595), `gemC1` (~1562), `gemGrading` `enabled` (~1718), and `useGemSweep` `enabled` (~1666-1673).
    - frontend/src/hooks/useGemSweep.ts — `resolveCandidate` (~217), the CR-03 watchdog effect (~316-328) and the fast-fail effect (~334-339), `UseGemSweepOptions` (~109), `effectiveEnabled` (~169).
    - .planning/todos/pending/172-deferred-review-findings.md — WR-06 (useCallback([])), IN-01 (resolveMarkerIcon dead isBook field), IN-03 (SweepDispatch done vs idle), IN-04 (book fold ply:-1). Note D-01a: WR-01/03/05 are MOOT (do not implement — the sweep no longer sweeps a stored mainline).
    - 175-RESEARCH.md Pitfall 2 (BOTH mechanisms must gate on "no stored row").
  </read_first>
  <behavior>
    - Test (useGemSweep.test.ts): with a "game has stored best-move data" signal true, effectiveEnabled is false (sweep does not run over an analyzed mainline).
    - Test: with no stored data (unanalyzed game), the sweep runs as before (fallback preserved).
    - Test: resolveCandidate identity is stable across renders (useCallback([])) — eslint-plugin-react-hooks exhaustive-deps passes with resolveCandidate listed in the watchdog/fast-fail effect deps.
  </behavior>
  <action>
    Gate the Phase-163 live-at-cursor mechanism: extend `needParentGemGrade` with a
    `&& !currentNodeHasStoredTier` clause (the node is off-mainline / unanalyzed — no stored
    EvalPoint tier), so `gemC1`/`gemGrading` never fire on a mainline ply of an analyzed game.
    Gate the Phase-172 sweep: add a `gameHasStoredBestMoveData` signal (true when the loaded
    game is analyzed and its eval_series carries best_move_tier fields) and AND `!
    gameHasStoredBestMoveData` into `useGemSweep`'s `enabled` prop, so the sweep runs only for
    games whose mainline is NOT stored. Add a docstring note to useGemSweep.ts declaring it a
    demoted free-play-only fallback (SEED-107 superseded). Fix WR-06: wrap `resolveCandidate` in
    `useCallback([])` (it touches only stable refs) and list it in the CR-03 watchdog + fast-fail
    effect dep arrays, removing the latent stale-closure trap. Apply IN-01 (drop the unread
    `isBook` return field from resolveMarkerIcon), IN-03 (distinguish SweepDispatch `done` from
    `idle` at the call site or document why not), and IN-04 (use the real ply index in the book
    fold instead of synthesizing `ply: -1`) where they still apply to the fallback path. Do NOT
    implement WR-01/03/05 (mooted by demotion). Add tests to useGemSweep.test.ts.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run useGemSweep && npm run lint && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "needParentGemGrade" frontend/src/pages/Analysis.tsx` shows the added "no stored tier" clause; `grep -n "enabled" frontend/src/pages/Analysis.tsx | grep -i sweep` (or the useGemSweep call) shows the `!gameHasStoredBestMoveData` gate.
    - `grep -n "useCallback" frontend/src/hooks/useGemSweep.ts` shows resolveCandidate wrapped; `npm run lint` (react-hooks/exhaustive-deps) passes with no eslint-disable added for it.
    - `grep -n "ply: -1\|ply:-1" frontend/src/pages/Analysis.tsx` returns nothing (IN-04 fixed); `grep -n "isBook" frontend/src/components/analysis/VariationTree.tsx` shows the dead field removed (IN-01).
    - `cd frontend && npm test -- --run useGemSweep` passes (stored-data-disables-sweep, unanalyzed-still-runs, stable-callback cases); `npx tsc -b` + `npm run lint` clean.
  </acceptance_criteria>
  <done>Both live gem mechanisms fire only for no-stored-row positions, the sweep is a documented fallback, WR-06/IN-01/03/04 are fixed, and no live compute runs on an analyzed mainline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify instant stored-tier markers + fallback-only live compute</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/hooks/useGemSweep.ts</files>
  <action>
    Human verifies the board renders gem/great from stored data with no live compute on the mainline:
    1. Start the dev DB (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`), backend (`uv run uvicorn app.main:app --reload`), and frontend (`cd frontend && npm run dev`).
    2. Open an already-analyzed game on /analysis known to contain a gem or great move.
    3. Confirm the gem/great badge appears ON THE FIRST PAINT of the mainline position — no visible delay, no badge popping in after a beat.
    4. Open DevTools (Network + Workers/Performance) and navigate along the mainline: confirm NO Maia/Stockfish worker activity fires for mainline navigation (the stored path does no live compute).
    5. Navigate into a user-created variation (off-mainline) or load a fresh unanalyzed bot game: confirm the live fallback still resolves gem/great there (worker activity expected in this case only).
    6. Spot-check the Library "has gem"/"has great" filter toggles return the expected games.
  </action>
  <verify>User confirms markers paint instantly on the mainline with no live worker activity, and the fallback still works off-mainline.</verify>
  <done>Instant stored-tier markers + fallback-only live compute approved by the user (backstop for the non-mechanical timing claim).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → client render | mainline markers come from stored `EvalPoint` fields; the fallback path computes locally with no server data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-10 | Information Disclosure | mainline marker rendering | low | accept | Renders only already-authorized `EvalPoint` fields for the user's own loaded game; the fallback path uses client-side engines on positions the user is already viewing. No new data crosses a trust boundary. |
| T-175-11 | Denial of Service (self) | live fallback firing on a stored mainline | low | mitigate | Gating both live mechanisms on "no stored tier" removes the redundant Maia/Stockfish round-trips on analyzed-mainline navigation (the BOARD-02 delay), reducing client CPU/worker load rather than adding it. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run Analysis useGemSweep` green.
- `cd frontend && npm run lint && npx tsc -b` clean.
- Human-verify checkpoint approved (instant marker paint + no mainline worker activity).
</verification>

<success_criteria>
BOARD-01/BOARD-02 are met: analyzed-game mainlines render gem/great instantly from stored data with no live engine call, both live mechanisms are demoted to a no-stored-row fallback, and the WR-06/IN deferred findings are resolved. Instant-marker paint confirmed by the human-verify checkpoint (backstop for the non-mechanical timing claim).
</success_criteria>

<output>
Create `.planning/phases/175-board-filter-gem-great-consumption/175-05-SUMMARY.md` when done.
</output>
