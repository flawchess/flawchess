---
phase: 177-worker-side-multipv2-gem-candidates
plan: 05
type: execute
wave: 4
depends_on: ["177-01", "177-02", "177-03", "177-04"]
files_modified:
  - .planning/phases/177-worker-side-multipv2-gem-candidates/177-MEASUREMENT.md
autonomous: false
requirements: [MEAS-01]
must_haves:
  truths:
    - "A post-deploy before/after comparison is recorded against the SEED-111 2026-07-17 baseline (games/h stamped, worker engine busy %, server pool utilization %, worker-submit fallback count, double-claim rate)."
    - "The worker-submit-fallback Sentry dimension is confirmed ~zero steady-state; any sustained non-zero is flagged as the D-06 regression signal."
    - "The double-claim rate is recorded so D-08 (deferred TTL-lease escalation) can be decided on real data."
  artifacts:
    - ".planning/phases/177-worker-side-multipv2-gem-candidates/177-MEASUREMENT.md — the recorded before/after table."
  key_links:
    - "Sentry source tags (worker-submit-fallback vs drain-local, Plans 01/03) are the regression-watch instrument read here."
---

<objective>
Record the D-07 post-deploy measurement: confirm the shift actually moved fleet engines from
~68% to ~95%+ busy, freed the server pool, and drove worker-submit fallbacks to ~zero, against the
exact SEED-111 2026-07-17 baseline.

Purpose: This phase's entire justification is a throughput/utilization improvement. D-07 makes the
before/after measurement an explicit deliverable, and D-08 depends on the double-claim rate captured
here to decide whether TTL leases become a follow-up. This is inherently a live-prod observation and
cannot be automated — it runs after the code ships via bin/deploy.sh and BEST_MOVE_BACKFILL_ENABLED
is flipped on in prod as a separately observed step.

Output: a MEASUREMENT.md recording the comparison and the fallback/double-claim verdicts.
</objective>

<artifacts_produced>
Symbols this plan CREATES (source-grounding: exclude from drift verification):
- Doc: `.planning/phases/177-worker-side-multipv2-gem-candidates/177-MEASUREMENT.md`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-worker-side-multipv2-gem-candidates/177-CONTEXT.md
@.planning/phases/177-worker-side-multipv2-gem-candidates/177-RESEARCH.md
@.planning/seeds/SEED-111-worker-side-multipv2-gem-candidates.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Post-deploy before/after measurement vs SEED-111 baseline (HUMAN-UAT)</name>
  <files>.planning/phases/177-worker-side-multipv2-gem-candidates/177-MEASUREMENT.md</files>
  <action>
    After the phase code ships to prod (bin/deploy.sh) and BEST_MOVE_BACKFILL_ENABLED is flipped on
    as a separately observed step, collect the six figures below from Sentry + the read-only prod DB
    (NOT prod docker logs — ~1h retention) and write them into 177-MEASUREMENT.md as a before/after
    table against the SEED-111 "Measured baseline (2026-07-17)" numbers, with the fallback and
    double-claim verdicts. This task is human-driven live-prod observation; there is no code to write.
  </action>
  <what-built>
    Protocol v2: worker-computed gem-candidate MultiPV-2 (fresh lane), a dedicated tier-4b
    lease/submit pair, a tier-aware drain minimal path, lease-level version gating, and
    source-tagged fallback instrumentation. All shipped to prod (bin/deploy.sh) with
    BEST_MOVE_BACKFILL_ENABLED flipped on as a separately observed step.
  </what-built>
  <how-to-verify>
    After deploy + flag-on, measure and record in 177-MEASUREMENT.md against the SEED-111
    "Measured baseline (2026-07-17)" numbers:
    1. games/h stamped (best_moves_completed_at growth rate) — baseline ~550/h.
    2. Worker submits/h — baseline 629/h across the 4 hosts (194.191.211.24 local box + 3 Hetzner).
    3. Server pool utilization — baseline 8 engines ~92% busy; expect a large drop.
    4. Local worker engine busy % over a 60s sample — baseline ~68%; target ~95%+.
    5. Worker-submit-fallback count (Sentry, source='worker-submit-fallback' dimension ONLY,
       NOT drain-local) — expect ~zero steady-state; any sustained non-zero is the D-06 regression
       signal → investigate (likely a lingering worker still landing on the old ungated path).
    6. Double-claim rate — baseline ~12%/h; record for the D-08 TTL-lease decision.
    Use Sentry / the DB (not prod docker logs — ~1h retention) for the fallback and rate figures.
    Confirm blob backfill (tier-4) throughput did NOT regress (rung-5 is after rung-4).
  </how-to-verify>
  <verify>
    <human-check>177-MEASUREMENT.md records all six figures with before/after values; fallback verdict and double-claim verdict stated; blob-backfill non-regression confirmed.</human-check>
  </verify>
  <acceptance_criteria>
    - 177-MEASUREMENT.md exists with a before/after table covering games/h, worker submits/h, server pool %, worker busy %, worker-submit fallback count, and double-claim rate.
    - The worker-submit-fallback verdict is recorded (expected ~zero; deviation flagged).
    - The double-claim figure is recorded with a D-08 recommendation (defer vs escalate).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the measurement is recorded, or describe anomalies (e.g. non-zero worker-submit fallback, blob-backfill regression) to investigate before closing the phase.</resume-signal>
  <done>177-MEASUREMENT.md records all six before/after figures with the worker-submit-fallback verdict, the double-claim/D-08 recommendation, and blob-backfill non-regression confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod observability | Read-only measurement (Sentry, prod DB read-only role). No new attack surface; documents the post-deploy state of the boundaries hardened in Plans 01-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-177-15 | Information Disclosure | reading prod metrics for measurement | low | accept | Read-only Sentry + read-only prod DB role; no secrets surfaced in the recorded doc. |
| T-177-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages — measurement only. |
</threat_model>

<verification>
- 177-MEASUREMENT.md committed with the before/after table and verdicts.
- Regression watch: worker-submit-fallback dimension ~zero; blob backfill un-regressed.
</verification>

<success_criteria>
- The phase's throughput/utilization claim is confirmed against real prod numbers, or deviations are documented for follow-up.
- The double-claim rate is captured to inform D-08.
</success_criteria>

<output>
Create `.planning/phases/177-worker-side-multipv2-gem-candidates/177-05-SUMMARY.md` when done.
</output>
