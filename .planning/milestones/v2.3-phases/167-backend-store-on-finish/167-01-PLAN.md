---
phase: 167-backend-store-on-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/normalization.py
  - app/schemas/bots.py
  - app/models/bot_game_settings.py
  - alembic/env.py
  - alembic/versions/
  - tests/schemas/test_bots.py
  - tests/repositories/test_bot_game_settings_repository.py
autonomous: true
requirements: [STORE-03, STORE-04, STORE-05]
must_haves:
  truths:
    - "The Platform Literal accepts 'flawchess' and `uv run ty check app/ tests/` stays green (D-17)."
    - "StoreBotGameRequest validates game_uuid is a UUID and rejects a non-UUID string (D-14)."
    - "bot_game_settings table exists after `alembic upgrade head` with a FK game_id ON DELETE CASCADE and a CHECK constraint on rating_source (D-16)."
  artifacts:
    - "app/schemas/bots.py — StoreBotGameRequest, StoreBotGameResponse"
    - "app/models/bot_game_settings.py — BotGameSettings model + bot_game_settings table"
    - "New Alembic migration creating bot_game_settings"
    - "'flawchess' member added to Platform Literal in app/schemas/normalization.py"
  key_links:
    - "alembic/env.py must import BotGameSettings so autogenerate/metadata sees the table"
    - "bot_game_settings.game_id FK -> games.id ON DELETE CASCADE"
  prohibitions:
    - "StoreBotGameRequest has NO rating, platform, user_id, is_computer_game, or username field — the server derives all of those authoritatively (D-05/D-08/D-14)."
    - "rating_source column rejects any value outside ('lichess','chesscom','blended') via CHECK (D-07/D-16); NULL is allowed."
    - "The migration must NOT use a native PostgreSQL ENUM for rating_source — TEXT + CHECK only (DB rules / D-16)."
---

<objective>
Lay the schema + storage foundation for the bot-game store endpoint: extend the
shared `Platform` Literal with `"flawchess"` (D-17), add the request/response
boundary schemas (D-14), and create the `bot_game_settings` one-to-one side table
(D-16) with its Alembic migration.

Purpose: Everything downstream (the normalizer, the store service, the router in
Plan 03) depends on these types and this table existing. This plan produces no
runtime behavior on its own — it is the type + DDL substrate.

Output: Extended `Platform` Literal, `app/schemas/bots.py`,
`app/models/bot_game_settings.py`, a new migration, and `alembic/env.py`
registration.
</objective>

## Artifacts this plan produces

| Symbol | Kind | File |
|--------|------|------|
| `"flawchess"` Platform member | Literal member | `app/schemas/normalization.py` |
| `StoreBotGameRequest` | Pydantic v2 schema | `app/schemas/bots.py` (NEW) |
| `StoreBotGameResponse` | Pydantic v2 schema | `app/schemas/bots.py` (NEW) |
| `BotGameSettings` | SQLAlchemy model | `app/models/bot_game_settings.py` (NEW) |
| `bot_game_settings` | DB table | new Alembic migration |
| `ck_bot_game_settings_rating_source` | CHECK constraint | new Alembic migration |
| `tests/schemas/test_bots.py` | test file | NEW |
| `tests/repositories/test_bot_game_settings_repository.py` | test file | NEW |

Deferred to Plan 03: `normalize_flawchess_game`, `store_bot_game`, the `bots` router,
`FLAWCHESS_BOT_USERNAME`, the `game_repository` id-lookup helper.
Deferred to Plan 02: `DEFAULT_EXCLUDED_PLATFORMS`, the `library_service` rating guard.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-backend-store-on-finish/167-CONTEXT.md
@.planning/phases/167-backend-store-on-finish/167-RESEARCH.md
@.planning/phases/167-backend-store-on-finish/167-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Platform Literal + add bot request/response schemas</name>
  <files>app/schemas/normalization.py, app/schemas/bots.py, tests/schemas/test_bots.py</files>
  <read_first>
    - app/schemas/normalization.py (lines 12-16 — the Platform / Color Literals; the NormalizedGame model for BaseModel conventions)
    - app/schemas/feedback.py (FeedbackCreate — the Pydantic v2 boundary-schema analog named in PATTERNS.md)
    - 167-PATTERNS.md section "app/schemas/normalization.py — extend Platform Literal (D-17)" (the exact new-schema shape, lines 168-195)
    - 167-RESEARCH.md Pitfall 4 (which Platform literals to touch and which to leave alone)
  </read_first>
  <behavior>
    - Platform Literal now includes "flawchess" alongside "chess.com", "lichess".
    - StoreBotGameRequest accepts a valid UUIDv4 string for game_uuid; a malformed
      game_uuid raises a Pydantic ValidationError (maps to 422 at the router).
    - StoreBotGameRequest rejects play_style_blend outside [0.0, 1.0].
    - StoreBotGameRequest rejects a pgn longer than the declared max_length bound.
    - StoreBotGameRequest has no rating/platform/user_id/username/is_computer_game field.
  </behavior>
  <action>
    In app/schemas/normalization.py, change the `Platform` Literal to
    `Literal["chess.com", "lichess", "flawchess"]` (D-17). Do NOT modify the inline
    `Literal["chess.com", "lichess"]` request-filter types elsewhere (insights.py,
    imports.py, opening_insights.py, openings.py, endgames.py) — per RESEARCH Pitfall 4
    those are unrelated user-facing filter literals; adding flawchess there would make
    bot games user-selectable on analytics surfaces, violating STORE-07.

    Create app/schemas/bots.py with two Pydantic v2 BaseModel classes:
    - StoreBotGameRequest with fields: game_uuid (str, validated as a UUID — use a
      field validator that calls uuid.UUID(game_uuid) and re-raises as ValueError so
      Pydantic surfaces a 422; do NOT capture_exception on this expected validation
      error), pgn (str with a max_length bound — define a module constant
      MAX_BOT_PGN_LENGTH, e.g. 100_000, to bound the DoS surface per RESEARCH Security),
      user_color (reuse the Color Literal from normalization), bot_elo (int, add a
      sane ge/le bound covering the milestone's 600–2600 range), play_style_blend
      (float, ge=0.0 le=1.0 — the b ∈ [0,1] blend, D-14/STORE-04), tc_preset (str).
    - StoreBotGameResponse with fields: game_id (int), created (bool).
    Import Color and Platform from app.schemas.normalization. No rating/platform/
    user_id field on the request (server derives all — D-05/D-08).

    Add tests/schemas/test_bots.py asserting: a valid request parses; a non-UUID
    game_uuid raises ValidationError; play_style_blend=1.5 raises; an over-length pgn
    raises. Create tests/schemas/__init__.py if the directory does not exist.
  </action>
  <verify>
    <automated>uv run pytest tests/schemas/test_bots.py -x && uv run ty check app/schemas/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'flawchess' app/schemas/normalization.py` and the Platform Literal line contains all three members.
    - tests/schemas/test_bots.py passes and includes a non-UUID rejection case and a play_style_blend out-of-range case.
    - `uv run ty check app/schemas/` reports zero errors.
    - StoreBotGameRequest source contains no field named rating, platform, user_id, username, or is_computer_game.
  </acceptance_criteria>
  <done>Platform Literal extended; StoreBotGameRequest/StoreBotGameResponse exist and validate UUID + blend range + PGN size; ty green.</done>
</task>

<task type="auto">
  <name>Task 2: BotGameSettings model + Alembic migration</name>
  <files>app/models/bot_game_settings.py, alembic/env.py, alembic/versions/, tests/repositories/test_bot_game_settings_repository.py</files>
  <read_first>
    - app/models/feedback.py (FK + mapped_column style analog)
    - alembic/env.py (lines 10-25 — the `from app.models.X import Y  # noqa: F401` registration list; the `from app.models.feedback import Feedback` line is the exact precedent)
    - alembic/versions/20260615_192047_60160718b3b3_phase_122_feedback_table.py (migration template: op.create_table + ForeignKeyConstraint ondelete CASCADE + PrimaryKeyConstraint)
    - 167-PATTERNS.md section "app/models/bot_game_settings.py (NEW model) + Alembic migration" (lines 199-249 — the exact model + migration shape, including the CheckConstraint)
    - 167-RESEARCH.md Pattern 3 (no existing CheckConstraint precedent — write it directly; autogenerate may miss the CHECK, hand-add/verify it)
  </read_first>
  <action>
    Create app/models/bot_game_settings.py defining BotGameSettings (table
    "bot_game_settings") per D-16: game_id Mapped[int] as PRIMARY KEY and
    ForeignKey("games.id", ondelete="CASCADE"); nominal_elo SmallInteger NOT NULL;
    play_style_blend REAL NOT NULL (postgresql.REAL); tc_preset Text NOT NULL;
    rating_source Text nullable. Add __table_args__ with a CheckConstraint
    "rating_source IN ('lichess', 'chesscom', 'blended')" named
    ck_bot_game_settings_rating_source. TEXT + CHECK, NOT a native ENUM.

    Register the model for metadata: add `from app.models.bot_game_settings import
    BotGameSettings  # noqa: F401` to alembic/env.py (mirroring the Feedback line) so
    the table is visible to Alembic's target_metadata. Also add it to
    app/models/__init__.py for consistency with the other exported models.

    Generate the migration: run `uv run alembic revision --autogenerate -m "phase 167
    bot_game_settings table"`. Inspect the generated file — autogenerate may omit the
    CheckConstraint; if so, hand-add `sa.CheckConstraint("rating_source IN
    ('lichess', 'chesscom', 'blended')", name="ck_bot_game_settings_rating_source")`
    inside op.create_table, and confirm the FK carries `ondelete="CASCADE"`. Verify
    down_revision points at the current head (run `uv run alembic heads`; expected
    "12d3df9c5373" but re-verify in case another phase landed first). Then
    `uv run alembic upgrade head`.

    Add tests/repositories/test_bot_game_settings_repository.py: insert one games row
    (reuse existing seed helpers / conftest fixtures — do NOT reset the DB), insert a
    BotGameSettings row referencing it, commit, read it back, and assert a second
    insert with rating_source="bogus" raises an IntegrityError (the CHECK fires).
    Assert deleting the parent games row cascades away the settings row.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run pytest tests/repositories/test_bot_game_settings_repository.py -x && uv run ty check app/models/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` succeeds and creates table bot_game_settings.
    - The generated migration file contains `ondelete="CASCADE"` and a CheckConstraint named ck_bot_game_settings_rating_source; grep confirms no `ENUM`/`postgresql.ENUM` in the migration.
    - tests/repositories/test_bot_game_settings_repository.py passes, including the CHECK-violation IntegrityError case and the FK-CASCADE delete case.
    - alembic/env.py imports BotGameSettings.
  </acceptance_criteria>
  <done>bot_game_settings table created via migration with FK CASCADE + TEXT/CHECK rating_source; model registered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Plan 03 endpoint) | The request schema defined here is the untrusted-input contract; this plan constrains what the client may send. |
| DB CHECK constraint | rating_source values are constrained at the storage layer independent of application code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-01 | Tampering | StoreBotGameRequest | high | mitigate | Schema has NO rating/platform/user_id/is_computer_game/username field — server derives all authoritatively (D-05/D-08); client cannot inject any of them. |
| T-167-02 | Input Validation | game_uuid field | medium | mitigate | UUID field validator rejects non-UUID strings with a 422 (D-14); no capture_exception on this expected error. |
| T-167-03 | Denial of Service | pgn field | medium | mitigate | max_length bound (MAX_BOT_PGN_LENGTH) caps the PGN payload size before any parse (RESEARCH Security). |
| T-167-04 | Tampering | rating_source column | low | mitigate | TEXT + CHECK constraint rejects out-of-vocabulary values at the DB layer (D-16). |
</threat_model>

<verification>
- `uv run ty check app/ tests/` zero errors.
- `uv run alembic upgrade head` clean; `bot_game_settings` present.
- `uv run pytest tests/schemas/test_bots.py tests/repositories/test_bot_game_settings_repository.py -x` green.
</verification>

<success_criteria>
Platform Literal includes flawchess (ty green); StoreBotGameRequest/Response exist
with UUID + blend-range + PGN-size validation and no server-owned fields;
bot_game_settings table created via a reviewed migration with FK CASCADE and a
TEXT+CHECK rating_source column.
</success_criteria>

<output>
Create `.planning/phases/167-backend-store-on-finish/167-01-SUMMARY.md` when done.
</output>
