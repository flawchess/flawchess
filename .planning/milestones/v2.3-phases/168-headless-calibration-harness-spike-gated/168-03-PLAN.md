---
phase: 168-headless-calibration-harness-spike-gated
plan: 03
type: execute
wave: 3
depends_on: [168-02]
files_modified:
  - scripts/lib/stockfish-pool.mjs
  - scripts/calibration-harness.mjs
  - scripts/lib/calibration-elo.mjs
  - scripts/lib/calibration-elo.check.mjs
autonomous: true
requirements: [CAL-01]
must_haves:
  truths:
    - "A multi-process Stockfish pool (default 4 child processes, --stockfish-procs configurable) mirrors workerPool.ts's slot-queue: grade()/anchor/adjudication go's are dispatched to a free process, each process resets its setoption state before every go, and SearchBudget.concurrency is set to the pool size so mctsSearch's grade() calls parallelize across processes."
    - "The pool measurably improves throughput in the most-expensive cell (blend=1) versus the single-process baseline (re-measured moves/sec reported)."
    - "The harness sweeps the coarse grid — bot ELO {1100,1500,1900} x blend {0,0.5,1.0}, each vs raw-Maia argmax rungs {1100,1300,1500,1700,1900} + Stockfish skill {0,3,5} — playing color-balanced games/cell over the seeded opening book, every axis CLI-configurable with cheap defaults (D-07)."
    - "The primary raw results-matrix TSV (one row per bot-cell x anchor: games, W/D/L, score, color split, run metadata incl. stockfish-procs) is written durably per-row to reports/data/ following gem-elo conventions (D-04/D-06)."
    - "A secondary advisory -summary.tsv carries a per-cell ELO estimate via anchor-logistic inversion, clamped against 0/1 extremes, with the SEED-091 approximate-anchor caveat in metadata (D-05)."
    - "Bot ELO rungs are validated against MAIA_ELO_LADDER and malformed CLI flags throw rather than silently coercing (D-08 / WR-02)."
  artifacts:
    - scripts/lib/stockfish-pool.mjs
    - scripts/calibration-harness.mjs
    - scripts/lib/calibration-elo.mjs
    - scripts/lib/calibration-elo.check.mjs
  key_links:
    - "concurrency > 1 is now SAFE because there are N independent Stockfish processes (each still serves one go at a time) — this supersedes Plan 02's SEARCH_CONCURRENCY=1 landmine, which existed only because there was a single shared process."
    - "Color alternates and openings cycle deterministically by game index under --seed so blend=1 cells are not identical replays (D-09)."
    - "invertAnchorElo clamps observed score into [epsilon, 1-epsilon] before log inversion; combineAnchorEstimates weights by inverse Wilson-CI width (imported wilsonBounds)."
---

<objective>
Deliver CAL-01: sweep the coarse (ELO x play-style) grid against the anchors and emit the strength-map TSV — on the throughput path chosen at the Plan 02 spike gate.

**Re-scoped by the D-03 go/no-go decision (2026-07-11):** the single-shared-Stockfish WASM path made the full grid infeasible (~59 days; grade() serialization is the bottleneck, NOT Maia/ONNX). The human chose: build a **multi-process Stockfish pool** first, then run a bounded first grid. So this plan adds `scripts/lib/stockfish-pool.mjs` (Task 1) BEFORE the grid sweep, re-measures throughput, then sweeps a bounded first map (Task 2) and emits the advisory ELO summary (Task 3). The full-resolution grid remains a later CLI re-run (bigger `--games-per-cell` / more cells), not a code change.

Extend `scripts/calibration-harness.mjs` with the pool-backed engine setup, the outer grid sweep (bot cells x anchors x color-balanced games over the seeded opening book), the finalized CLI axis flags with strict validation (D-07/D-08/WR-02), and the durable per-row main results TSV (D-04/D-06). Add `scripts/lib/calibration-elo.mjs` (anchor-logistic inversion -> advisory per-cell ELO estimate, D-05) with a pure-math assertion, and emit the sibling `-summary.tsv` carrying that estimate plus the SEED-091 caveat.

Reuses `mulberry32`/`wilsonBounds`/`validateRungs`/the gem-elo TSV writer conventions and mirrors `workerPool.ts`'s slot-queue for the pool — imports/analogs, not re-derivations.

Purpose: the strength map is the engine test bench (SEED-091 #3) — measuring the unknown real strength of the (ELO x play-style) engine.
Output: the Stockfish pool + full grid orchestration + two TSV artifacts in `reports/data/` + the ELO-inversion module and its check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-CONTEXT.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-RESEARCH.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-PATTERNS.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-02-SUMMARY.md
</context>

<artifacts_produced>
New files / symbols (exclude from drift checks — this phase's own new code):
- `scripts/lib/stockfish-pool.mjs` — `createStockfishPool({ size })` returning a pool object with an async `grade(fen, candidateUcis)` / `evalPosition(fen)` / `skillMove(fen, level)` slot-queue API and `quitAll()`; mirrors `frontend/src/lib/engine/workerPool.ts`'s free-slot queue. Named constant `STOCKFISH_POOL_DEFAULT_SIZE=4`.
- `scripts/lib/calibration-elo.mjs` — `invertAnchorElo(observedScore, anchorRating, games)`, `combineAnchorEstimates(perAnchor)`; named constants `SCORE_CLAMP_EPSILON_DIVISOR=2`, `MIN_CI_WIDTH`.
- `scripts/lib/calibration-elo.check.mjs` — pure-math assertion (no engines).
- Extends `scripts/calibration-harness.mjs` — pool-backed provider wiring, `SearchBudget.concurrency` set to pool size, outer grid sweep, finalized CLI flags (`--out-dir`, `--stockfish-procs` added), `validateRungs` on bot ELOs, durable main TSV writer + advisory `-summary.tsv`; named default-grid constants `BOT_ELO_DEFAULT=[1100,1500,1900]`, `BLEND_DEFAULT=[0,0.5,1.0]`, `MAIA_ANCHOR_RUNGS=[1100,1300,1500,1700,1900]`, `SF_ANCHOR_SKILLS=[0,3,5]`, `GAMES_PER_CELL_DEFAULT=20`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Multi-process Stockfish pool (workerPool.ts slot-queue analog)</name>
  <files>scripts/lib/stockfish-pool.mjs, scripts/calibration-harness.mjs</files>
  <read_first>
    - frontend/src/lib/engine/workerPool.ts — the slot-queue design to mirror (free-slot queue, sendGo/handleLine, searchmoves-restricted, depth-carrying, pv[0] UCI keying). This is the CORRECT grade() shape (168-RESEARCH.md Pitfall 1).
    - scripts/lib/node-engine-providers.mjs (Plan 01) — the existing single-process StockfishUciEngine spawn + stopAndSync + gradePosition/eval building blocks the pool wraps N of.
    - scripts/calibration-harness.mjs (Plan 02) — current setupHarnessEngines(): single spawnStockfish + makeNodeProviders; the setoption-reset-before-every-go discipline (Pitfall 2) each pool process must preserve; SEARCH_CONCURRENCY constant.
    - 168-RESEARCH.md Pitfall 3 (multi-process Stockfish pool is THE throughput lever, not onnxruntime-node) and 168-02-SUMMARY.md (spike outcome + decision).
    - 168-CONTEXT.md D-11 (per-move budget stays FIXED — the pool changes throughput, NOT the budget/strength).
  </read_first>
  <action>
    Write `scripts/lib/stockfish-pool.mjs` exporting `createStockfishPool({ size = STOCKFISH_POOL_DEFAULT_SIZE })` that spawns `size` independent Stockfish child processes (reuse the Plan-01 spawn/StockfishUciEngine bring-up per process) and exposes an async, promise-based slot-queue API mirroring `workerPool.ts`: a free-process queue where each incoming request (grade / adjudication eval / anchor skill-move) acquires a free process, runs its `go`, and releases it; requests beyond `size` queue until a process frees. CRITICAL invariants: (a) each process resets its option state (`Skill Level` / `UCI_LimitStrength`, and any MultiPV/searchmoves) before every `go` so a weakened anchor level never leaks into bot grading (Pitfall 2) — the reset is per-process, per-go; (b) `grade(fen, candidateUcis)` keeps the Plan-01 UCI-keyed, searchmoves-restricted, depth-carrying shape (Pitfall 1); (c) `quitAll()` cleanly terminates every process. Then rewire `setupHarnessEngines()` in `calibration-harness.mjs` to build the providers off the pool (`providers.grade` → pool.grade), route anchor `stockfishSkillMove` and adjudication evals through the pool too, and set `SearchBudget.concurrency` to the pool size (so `mctsSearch` dispatches up to `size` parallel grade() calls). Add a `--stockfish-procs` CLI flag (default `STOCKFISH_POOL_DEFAULT_SIZE=4`, validated via the WR-02 discipline). Name `STOCKFISH_POOL_DEFAULT_SIZE=4`. Keep pool internals small (extract the slot-acquire/release and per-process go into named helpers; respect nesting/logic-LOC limits).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1500 --blends 0 --anchors sf0 --games-per-cell 1 --seed 1 --stockfish-procs 2 2>&1 | grep -Eq 'result[:=].*(win|loss|draw)'</automated>
  </verify>
  <acceptance_criteria>
    - A game runs to completion with `--stockfish-procs 2` (pool of 2), producing a win|loss|draw result — proving the pool serves bot grade(), anchor moves, and adjudication without state leaking (determinism check from Plan 02 still passes).
    - `SearchBudget.concurrency` equals the pool size (executor confirms the wiring); each pool process resets setoption before every go.
    - Re-measuring the most-expensive cell (blend=1) with `--stockfish-procs 4` reports a HIGHER moves/sec than the single-process spike baseline (executor records the before/after number in the SUMMARY).
    - `grade()` keeps the UCI-keyed / searchmoves-restricted / depth-carrying shape (not SAN-keyed).
  </acceptance_criteria>
  <done>The harness grades through an N-process Stockfish pool that parallelizes the bottleneck grade() calls, with per-process option hygiene preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Grid sweep + strict CLI axes + durable main results TSV (D-04/D-06/D-07/D-08)</name>
  <files>scripts/calibration-harness.mjs</files>
  <read_first>
    - scripts/gem-elo-calibration.mjs lines 93-190 (requireFlagValue/parsePositiveIntFlag/parseArgs/validateRungs), 566-627 (buildTimestamp/tsvColumns/tsvRowLine/openTsvWriter — durable writer), 712+ (main() setup order).
    - 168-PATTERNS.md "calibration-harness.mjs" sections (lines 50-80, CLI validation + durable TSV writer + main orchestration).
    - 168-CONTEXT.md D-07 (grid), D-08 (rungs in MAIA_ELO_LADDER), D-09 (color alternation + opening cycling), D-04 (row shape), D-06 (TSV conventions).
    - frontend/src/lib/engine/botSampling.ts:121 (mulberry32 — import, do not re-add a copy).
  </read_first>
  <action>
    Extend `main()` in `calibration-harness.mjs` into the full grid sweep. Finalize the CLI: `--elo`, `--blends`, `--anchors`, `--games-per-cell`, `--seed`, `--out-dir`, `--stockfish-procs`, each parsed via the `requireFlagValue`/`parsePositiveIntFlag` discipline (a missing/malformed value THROWS, never coerces to NaN/undefined — WR-02); default to the named grid constants (BOT_ELO_DEFAULT / BLEND_DEFAULT / MAIA_ANCHOR_RUNGS / SF_ANCHOR_SKILLS / GAMES_PER_CELL_DEFAULT). Validate bot ELO rungs against `MAIA_ELO_LADDER` using the imported `validateRungs` (D-08). Sweep order: for each (botElo x blend) cell, for each anchor (5 Maia rungs + 3 SF skills), for each game in `games-per-cell` — alternate `botIsWhite` every game and pick `OPENING_BOOK[gameIndex % OPENING_BOOK.length]` (deterministic; a per-game `mulberry32` derived from `--seed` + indices drives selectBotMove's rng, D-09). Call `playGame` (Plan 02) and tally W/D/L, points, and the as-White/as-Black split into the (cell x anchor) row. Open a durable per-row TSV writer (mirror `openTsvWriter`: header written immediately, `writeRow` after EVERY completed game so a mid-sweep crash keeps completed rows — WR-01/D-06) writing to `reports/data/` with a timestamped filename; the row schema is D-04 (bot elo, blend, anchor id, games, W/D/L, score, white-split, black-split, seed, per-move budget, stockfish-procs, git SHA, grid params). Keep the sweep helpers small (extract the per-cell and per-game inner loops into named helpers to respect nesting/logic-LOC limits).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1500 --blends 0 --anchors sf0 --games-per-cell 2 --seed 3 --out-dir reports/data --stockfish-procs 2 && ls -t reports/data/*calibration*.tsv | head -1 | xargs head -1 | grep -qi anchor</automated>
  </verify>
  <acceptance_criteria>
    - A tiny sweep writes a timestamped main TSV under `reports/data/` whose header includes the D-04 columns (bot elo, blend, anchor, games, W/D/L, score, color split, run metadata incl. stockfish-procs).
    - Rows are appended per completed game (durable) — the file exists and grows before the run finishes.
    - `--seed abc` (or a missing flag value) exits non-zero with a validation error, not a silent empty TSV.
    - Bot ELO rungs outside `MAIA_ELO_LADDER` are rejected by `validateRungs`.
    - The verification sweep is deliberately SMALL (a few cells / 1-2 anchors / 1-2 games) so it completes in minutes — the full-resolution grid is a documented background CLI re-run, NOT run inside this task.
  </acceptance_criteria>
  <done>Running the harness plays a (bounded) coarse grid vs anchors through the pool and emits the durable raw strength-map TSV (CAL-01).</done>
</task>

<task type="auto">
  <name>Task 3: Advisory per-cell ELO estimate (calibration-elo.mjs) + -summary.tsv (D-05)</name>
  <files>scripts/lib/calibration-elo.mjs, scripts/lib/calibration-elo.check.mjs, scripts/calibration-harness.mjs</files>
  <read_first>
    - 168-RESEARCH.md "Weighted-mean anchor-Elo combination" (lines 372-402, invertAnchorElo/combineAnchorEstimates) and Pitfall 4 (lines 356-360, clamp at score 0/1).
    - 168-PATTERNS.md "calibration-elo.mjs" (lines 211-222) and "calibration-parity.check.mjs" fold-in note (lines 205-207).
    - frontend/src/lib/scoreConfidence.ts (wilsonBounds — import via @/, do not re-derive; "Trust the established Wilson stat method" memory).
    - scripts/gem-elo-calibration.mjs lines 635-674 (emitSummary shape — write-once sibling summary TSV).
    - 168-CONTEXT.md D-05 (anchor-logistic inversion + SEED-091 caveat); RESEARCH Open Question 2 (SF_SKILL_ELO anchor ratings, approximate).
  </read_first>
  <action>
    Write `scripts/lib/calibration-elo.mjs` exporting `invertAnchorElo(observedScore, anchorRating, games)` — clamp the score into `[epsilon, 1-epsilon]` with `epsilon = 1/(SCORE_CLAMP_EPSILON_DIVISOR*games)` (Pitfall 4) then return `anchorRating - 400*log10(1/clamped - 1)` — and `combineAnchorEstimates(perAnchor)` — per-anchor invert, weight each by inverse squared Wilson-CI width (`wilsonBounds` imported from `@/lib/scoreConfidence`, guarded by `MIN_CI_WIDTH`), return the weighted-mean point estimate (null if no anchors). Anchor known ratings: Maia rung X -> rating X; SF skill -> `SF_SKILL_ELO` (approximate, from Plan 01). Name `SCORE_CLAMP_EPSILON_DIVISOR=2` and `MIN_CI_WIDTH`. Write `scripts/lib/calibration-elo.check.mjs` (mirror gem-parity.check.mjs; no engines): assert `invertAnchorElo(0.5, 1500, 20)` is within a small tolerance of 1500; assert a lopsided `score=0` (or 1) yields a FINITE (never NaN/Infinity) estimate after clamping; assert `combineAnchorEstimates` returns a finite estimate for a mixed synthetic anchor set; print `PASS:` + exit 0. Finally, in `calibration-harness.mjs`, after the sweep, compute `combineAnchorEstimates` per bot cell and write the sibling `-summary.tsv` (write-once, like `emitSummary`) with columns for bot elo/blend, the advisory ELO estimate, whether any anchor row was clamped, and a metadata line carrying the SEED-091 approximate-anchor caveat (coarse estimate, anchors themselves approximate — not a precise ELO).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-elo.check.mjs 2>&1 | grep -q '^PASS:'</automated>
  </verify>
  <acceptance_criteria>
    - `calibration-elo.check.mjs` prints `PASS:` and exits 0: `invertAnchorElo(0.5,1500,20)` ~= 1500; a 0/1 extreme score yields a finite (clamped) estimate; `combineAnchorEstimates` is finite for synthetic inputs.
    - A tiny sweep additionally emits a `-summary.tsv` sibling with a per-cell advisory ELO estimate column, a clamped-flag column, and the SEED-091 caveat in metadata.
    - `wilsonBounds` is imported from `@/lib/scoreConfidence`, not re-derived.
  </acceptance_criteria>
  <done>The advisory per-cell ELO estimate is produced (clamped, Wilson-weighted) and written to a caveated -summary.tsv alongside the raw matrix (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI -> harness | Offline dev tool; only untrusted input is developer-supplied flags. No network, no auth, no persisted multi-user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-04 | Tampering | concurrency > 1 across the Stockfish pool | medium | mitigate | concurrency == pool size is safe BECAUSE each of the N processes serves one `go` at a time via the slot-queue (no overlapping `go` on a single process). Per-process setoption reset before every `go` (Pitfall 2) keeps anchor/bot/adjudication roles from leaking. Supersedes Plan 02's single-process SEARCH_CONCURRENCY=1 constraint. |
| T-168-05 | Tampering | unvalidated CLI flag -> empty/garbage TSV | medium | mitigate | Every value-consuming flag (incl. `--stockfish-procs`) validates before use (WR-02 requireFlagValue/parsePositiveIntFlag); a missing/malformed value throws, so a bad run cannot silently produce a misleading strength map. Task 2 verify asserts the throw. |
| T-168-06 | Denial (of artifact) | mid-sweep crash discards hours of grading | low | mitigate | Durable per-row TSV streaming (WR-01/D-06) — header up front, append per completed game. |
| T-168-07 | Tampering | ELO inversion NaN/Infinity at extreme scores | low | mitigate | Clamp score into [epsilon,1-epsilon] before log inversion (Pitfall 4); check asserts finiteness. Advisory-only, low blast radius (D-05). |
| T-168-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Task 1 + Task 2 + Task 3 automated checks pass.
- The pool runs a game with `--stockfish-procs 2`; blend=1 throughput with `--stockfish-procs 4` beats the single-process baseline.
- A tiny sweep produces both a durable main TSV and a caveated `-summary.tsv` under `reports/data/`.
- Malformed CLI flags throw; bot rungs validated against MAIA_ELO_LADDER.
</verification>

<success_criteria>
The harness grades through a multi-process Stockfish pool, sweeps the coarse grid vs anchors, and emits the durable raw results-matrix TSV (D-04) plus the advisory per-cell ELO-estimate `-summary.tsv` (D-05) to `reports/data/`, with strict CLI validation (WR-02) and seeded reproducibility (D-09). The full-resolution grid is a documented background CLI re-run.
</success_criteria>

<output>
Create `.planning/phases/168-headless-calibration-harness-spike-gated/168-03-SUMMARY.md` when done.
</output>
