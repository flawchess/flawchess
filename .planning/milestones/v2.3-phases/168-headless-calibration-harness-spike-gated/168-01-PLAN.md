---
phase: 168-headless-calibration-harness-spike-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/node-engine-providers.mjs
  - scripts/gem-elo-calibration.mjs
  - scripts/lib/calibration-providers.mjs
  - scripts/lib/calibration-anchors.mjs
  - scripts/lib/calibration-openings.mjs
  - scripts/lib/calibration-parity.check.mjs
autonomous: true
requirements: [CAL-02]
must_haves:
  truths:
    - "The shared Maia ONNX session + Stockfish UCI process bring-up is available as one importable Node module used by BOTH gem-elo and the new harness (no duplicated engine bring-up)."
    - "A Node EngineProviders adapter satisfies the frozen policy(fen,elo,side)/grade(fen,candidateUcis) contract UCI-keyed, searchmoves-restricted, depth-carrying."
    - "A parity check imports the live selectBotMove/mctsSearch/maskAndSoftmax via @/ and fails if the harness ever re-derives move-selection logic (CAL-02 anti-drift gate)."
  artifacts:
    - scripts/lib/node-engine-providers.mjs
    - scripts/lib/calibration-providers.mjs
    - scripts/lib/calibration-anchors.mjs
    - scripts/lib/calibration-openings.mjs
    - scripts/lib/calibration-parity.check.mjs
  key_links:
    - "nodeGrade keys results by parsed.pv[0] (UCI), never by the multipv rank field — mctsSearch looks up children by UCI."
    - "gem-elo-calibration.mjs re-imports the extracted bring-up helpers and still runs unchanged (behavior-preserving refactor)."
---

<objective>
Build the reusable engine foundation the calibration harness composes over, and the CAL-02 anti-drift gate.

This plan (a) refactors the verbatim-identical Maia-session / Stockfish-UCI bring-up OUT of `scripts/gem-elo-calibration.mjs` into a shared `scripts/lib/node-engine-providers.mjs` (re-imported back into gem-elo, behavior-preserving), (b) builds the Node `EngineProviders` adapter (`nodePolicy`/`nodeGrade`) against the FROZEN contract — mirroring `workerPool.ts` (UCI-keyed, `searchmoves`-restricted), NOT gem-elo's SAN-keyed `gradePosition`, (c) builds the anchor move-choosers and the curated opening book, and (d) writes the `calibration-parity.check.mjs` gate that imports the live `selectBotMove`/`mctsSearch`/`maskAndSoftmax` and asserts zero move-selection reimplementation (CAL-02).

Purpose: every downstream plan (spike, grid) composes these primitives; the parity gate is the standing guarantee that measured strength reflects the code users actually play against.
Output: 5 new `scripts/lib/*.mjs` files + a mechanical edit to `gem-elo-calibration.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-CONTEXT.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-RESEARCH.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-PATTERNS.md
</context>

<artifacts_produced>
New files (exclude these symbols from any drift/reimplementation check — they are this phase's own new code, not forks of frontend logic):
- `scripts/lib/node-engine-providers.mjs` — exports `resolveFrontendModule`, `createMaiaSession`, `StockfishUciEngine`, `spawnStockfish`, `STOCKFISH_INIT_TIMEOUT_MS` (extracted verbatim from gem-elo).
- `scripts/lib/calibration-providers.mjs` — exports `makeNodeProviders(session, ort, stockfish)` returning `{ policy: nodePolicy, grade: nodeGrade }`; named constants `GRADING_TARGET_DEPTH`, `GRADING_MOVETIME_SAFETY_CAP_MS`, `SLACK_MS`.
- `scripts/lib/calibration-anchors.mjs` — exports `maiaArgmaxMove(providers, fen, rungElo)`, `stockfishSkillMove(stockfish, fen, skillLevel)`; named constant `ANCHOR_MOVETIME_MS`, `SF_SKILL_ELO` map.
- `scripts/lib/calibration-openings.mjs` — exports `OPENING_BOOK` (array of `{ name, eco, fen }`).
- `scripts/lib/calibration-parity.check.mjs` — CAL-02 assertion script.
Modified: `scripts/gem-elo-calibration.mjs` (re-imports extracted helpers).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extract engine bring-up into scripts/lib/node-engine-providers.mjs</name>
  <files>scripts/lib/node-engine-providers.mjs, scripts/gem-elo-calibration.mjs</files>
  <read_first>
    - scripts/gem-elo-calibration.mjs lines 375-507 (resolveFrontendModule, createMaiaSession, StockfishUciEngine, spawnStockfish) and line 76 (STOCKFISH_INIT_TIMEOUT_MS) — the exact functions to extract.
    - 168-PATTERNS.md "scripts/lib/node-engine-providers.mjs" section (lines 84-114) — the extraction map + the `.cjs`/`.wasm` basename-copy trick (Pitfall 5).
    - 168-RESEARCH.md "Recommendation on shared vs duplicated scaffolding" (lines 181-184).
  </read_first>
  <action>
    Move `resolveFrontendModule`, `createMaiaSession`, the `StockfishUciEngine` class (`send`/`onLine`/`waitFor`/`init`/`stopAndSync`/`terminate`), and `spawnStockfish` VERBATIM out of `gem-elo-calibration.mjs` into a new `scripts/lib/node-engine-providers.mjs`, exporting each plus the `STOCKFISH_INIT_TIMEOUT_MS` constant. Preserve the `.cjs`/`.wasm` basename-copy-to-tmpdir logic byte-for-byte (Pitfall 5 — the Emscripten glue looks up the `.wasm` by the SAME basename minus extension; any change to the temp-file naming silently breaks the spawn). Keep `FRONTEND_DIR` resolution consistent (export or re-derive it in the lib module). Then edit `gem-elo-calibration.mjs` to import these four symbols + the constant from `./lib/node-engine-providers.mjs` instead of defining them locally; delete the now-moved definitions. This is a behavior-preserving mechanical refactor — do not alter any logic. gem-elo-calibration.mjs is a Phase-165-frozen sibling; the ONLY permitted change is swapping local defs for the shared import.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs -e "const m = await import('./scripts/lib/node-engine-providers.mjs'); const { ort, session } = await m.createMaiaSession(); const sf = await m.spawnStockfish(); await sf.init(); sf.send('isready'); await sf.waitFor(l => l === 'readyok', 10000); console.log('BRINGUP-OK'); sf.terminate();" 2>&1 | grep -q BRINGUP-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --import ./scripts/lib/frontend-alias-hook.mjs -e "await import('./scripts/lib/node-engine-providers.mjs')"` loads without error.
    - The smoke above prints BRINGUP-OK (Maia session created + Stockfish reaches `readyok`).
    - `gem-elo-calibration.mjs` no longer defines the moved functions locally and imports them from `./lib/node-engine-providers.mjs`; a small gem-elo smoke invocation still runs (executor confirms one tiny run produces its usual TSV).
  </acceptance_criteria>
  <done>Bring-up code lives in one shared lib module, imported by gem-elo (unchanged behavior) and available to the harness.</done>
</task>

<task type="auto">
  <name>Task 2: Node EngineProviders adapter, anchor movers, and opening book</name>
  <files>scripts/lib/calibration-providers.mjs, scripts/lib/calibration-anchors.mjs, scripts/lib/calibration-openings.mjs</files>
  <read_first>
    - 168-PATTERNS.md "calibration-providers.mjs" section (lines 117-164) — the `nodeGrade` = workerPool `sendGo`/`handleLine` mapping, `nodePolicy` = adapted `maiaProbsForPosition`, SAN→UCI via `sanToUci`.
    - frontend/src/lib/engine/workerPool.ts lines 36, 39 (GRADING_TARGET_DEPTH=14, GRADING_MOVETIME_SAFETY_CAP_MS=2500), 210-219 (sendGo), 249-267 (handleLine — key by parsed.pv[0], filter bound==='exact', whitePov sign).
    - frontend/src/lib/engine/types.ts — EngineProviders (policy(fen,elo,side), grade(fen,candidateUcis)) and MoveGrade ({evalCp,evalMate,depth,pv?}).
    - frontend/src/lib/maiaEncoding.ts — encodeBoard/maskAndSoftmax/eloToInput + NUM_SQUARES/PLANES_PER_SQUARE/POLICY_VOCAB_SIZE; frontend/src/lib/sanToSquares.ts:63 (sanToUci).
    - 168-PATTERNS.md "calibration-anchors.mjs" (lines 167-179) and "calibration-openings.mjs" (lines 183-185); 168-RESEARCH.md Open Question 1 (lines 421-424, opening-book recommendation) and Open Question 2 (lines 426-429, SF-skill Elo mapping).
  </read_first>
  <action>
    Build `calibration-providers.mjs` exporting `makeNodeProviders(session, ort, stockfish)` → `{ policy, grade }`. `policy(fen, elo, side)` runs ONE Maia forward pass (adapt gem-elo's `maiaProbsForPosition` single-rung path), converts each `maskAndSoftmax` SAN key to UCI via `sanToUci` (imported from `@/lib/sanToSquares`), and returns a UCI-keyed probability object; no async queue is needed since only one call is ever in flight (concurrency 1). `grade(fen, candidateUcis)` MUST mirror `workerPool.ts`'s `sendGo`/`handleLine` — return early with an empty Map on zero candidates; reset the shared engine to full strength FIRST (`setoption name Skill Level value 20`, `setoption name UCI_LimitStrength value false` — Pitfall 2), set `MultiPV` to the candidate count, `position fen`, then `go depth GRADING_TARGET_DEPTH searchmoves <ucis> movetime GRADING_MOVETIME_SAFETY_CAP_MS`; accumulate `info` lines via `parseInfoLine` (imported from `@/hooks/uciParser`), keep only `bound==='exact'`, key each grade by `parsed.pv[0]` (the UCI — NEVER the multipv rank), apply the white-POV sign from the FEN side-to-move, and carry `depth`. Name `GRADING_TARGET_DEPTH`, `GRADING_MOVETIME_SAFETY_CAP_MS`, `SLACK_MS` as constants mirroring the app's values. Do NOT reuse gem-elo's `gradePosition` (SAN-keyed, all-legal, no depth — wrong contract, Pitfall 1).
    Build `calibration-anchors.mjs`: `maiaArgmaxMove(providers, fen, rungElo)` calls `providers.policy` and returns the argmax UCI (deterministic tie-break by sorting UCI keys before scanning; fall back to `fallbackMove` from `@/lib/engine/botSampling` on a degenerate empty policy); `stockfishSkillMove(stockfish, fen, skillLevel)` sets `Skill Level` + `UCI_LimitStrength false` on every call (Pitfall 2), `go movetime ANCHOR_MOVETIME_MS`, and returns `parseBestmove(line)` (from `@/hooks/uciParser`). Define `SF_SKILL_ELO = {0:1320, 3:1750, 5:2200}` as a documented-approximate named map (per Open Question 2 / SEED-091 ±100-150 caveat) and `ANCHOR_MOVETIME_MS` as a named constant.
    Build `calibration-openings.mjs`: export `OPENING_BOOK`, a hand-curated ~20-30 entry array of `{ name, eco, fen }` covering 1.e4/1.d4/1.c4/1.Nf3 first-move families and a spread of open/semi-open/closed structures (per Open Question 1's list). Add a header comment stating these are standard public opening lines written from confirmed chess theory, not copied from a licensed database (mirror the maiaEncoding.ts header discipline).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs -e "const eng=await import('./scripts/lib/node-engine-providers.mjs'); const prov=await import('./scripts/lib/calibration-providers.mjs'); const open=await import('./scripts/lib/calibration-openings.mjs'); const {ort,session}=await eng.createMaiaSession(); const sf=await eng.spawnStockfish(); await sf.init(); const p=prov.makeNodeProviders(session,ort,sf); const START='rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1'; const pol=await p.policy(START,1500,'w'); const keys=Object.keys(pol); const g=await p.grade(START,[keys[0],keys[1]]); if(keys.length>0 && g.get(keys[0]) && typeof g.get(keys[0]).depth==='number' && open.OPENING_BOOK.length>=20){console.log('PROV-OK')} sf.terminate();" 2>&1 | grep -q PROV-OK</automated>
  </verify>
  <acceptance_criteria>
    - `policy(startFen,1500,'w')` returns a non-empty object whose keys are legal UCI moves.
    - `grade(startFen,[uciA,uciB])` returns a Map keyed by those two UCIs, each value carrying numeric `evalCp` (white-POV) and a numeric `depth`.
    - `OPENING_BOOK` has ≥20 entries spanning at least e4/d4/c4/Nf3 first moves; every FEN is legal (loads in chess.js).
    - No SAN-keyed grade path; no `multipv`-rank keying anywhere.
  </acceptance_criteria>
  <done>The Node providers satisfy the frozen EngineProviders contract; anchors and openings are ready for the game loop.</done>
</task>

<task type="auto">
  <name>Task 3: CAL-02 parity gate (calibration-parity.check.mjs)</name>
  <files>scripts/lib/calibration-parity.check.mjs</files>
  <read_first>
    - scripts/lib/gem-parity.check.mjs (full file, 128 lines) — the exact structure to mirror: doc-comment, `@/`-imported live fns, `node:assert/strict` fixtures, `console.log('PASS: ...')` + `process.exit(0)`.
    - frontend/src/lib/engine/selectBotMove.ts lines 61-102 (BotSettings/BotMoveDeps/signature) — how to call the live fn with stub providers.
    - 168-PATTERNS.md "calibration-parity.check.mjs" (lines 189-207); guidance point 3 (CAL-02 anti-drift gate).
  </read_first>
  <action>
    Write `calibration-parity.check.mjs` mirroring `gem-parity.check.mjs` exactly. Import the LIVE `selectBotMove` (`@/lib/engine/selectBotMove`), `mctsSearch` (`@/lib/engine/mctsSearch`), and `maskAndSoftmax` (`@/lib/maiaEncoding`) — never re-derive any of them. Assert, with `node:assert/strict` against hand-derived fixtures and DETERMINISTIC STUB providers (a fixed policy map + a fixed grade Map, plus a seeded `mulberry32` from `@/lib/engine/botSampling`), that: (1) at `blend=0` `selectBotMove` returns the policy-driven choice the stub policy dictates, and (2) at `blend=1` it returns the deterministic argmax the stub grades dictate — proving the harness runs the REAL regime dispatch, not a fork. Include a lightweight structural assertion that these symbols are functions imported from the frontend source (guarding against accidental local re-definition). Print `PASS: calibration parity — selectBotMove/mctsSearch/maskAndSoftmax imported live, zero reimplementation` and `process.exit(0)` on success; throw (non-zero exit) on any mismatch. Use stub providers (no real engines) so this runs in well under a second.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-parity.check.mjs 2>&1 | grep -q '^PASS:'</automated>
  </verify>
  <acceptance_criteria>
    - The check exits 0 and prints a `PASS:` line naming the live-imported symbols.
    - Deliberately editing the check to call a local re-implementation of the regime dispatch makes it fail (executor mentally confirms the assertions are load-bearing, not vacuous).
    - No real Maia/Stockfish process is spawned (stub providers only); runtime < 1s.
  </acceptance_criteria>
  <done>CAL-02 has a standing, fast anti-drift gate that fails if the harness ever forks selectBotMove/mctsSearch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → harness lib | Only untrusted input is developer-supplied flags/args to the smoke commands; no network, no auth, no persisted multi-user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-01 | Tampering | nodeGrade key derivation | medium | mitigate | Key grades by `parsed.pv[0]` (UCI), never the `multipv` rank; filter `bound==='exact'` — a mis-key silently corrupts every measurement (Pitfall 1/2). Asserted by Task 2 verify. |
| T-168-02 | Tampering | shared Stockfish option state | medium | mitigate | Every `go` resets the options it depends on first (grade → full strength; anchor → its skill level) — Pitfall 2. Offline harness, no security exposure; the risk is artifact trustworthiness. |
| T-168-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs ZERO new packages (RESEARCH Package Legitimacy Audit) — no supply-chain surface. |
</threat_model>

<verification>
- All three task automated checks pass.
- `gem-elo-calibration.mjs` still produces its usual output after the refactor (one tiny smoke run).
- No new npm dependency introduced.
</verification>

<success_criteria>
Shared bring-up extracted and re-imported; Node EngineProviders adapter satisfies the frozen contract UCI-keyed/searchmoves-restricted/depth-carrying; anchors + opening book ready; CAL-02 parity gate green.
</success_criteria>

<output>
Create `.planning/phases/168-headless-calibration-harness-spike-gated/168-01-SUMMARY.md` when done.
</output>
