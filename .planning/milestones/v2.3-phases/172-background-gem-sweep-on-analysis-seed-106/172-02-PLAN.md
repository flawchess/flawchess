---
phase: 172-background-gem-sweep-on-analysis-seed-106
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/gemMove.ts
  - frontend/src/lib/__tests__/gemMove.test.ts
  - frontend/src/hooks/useMaiaEloDefault.ts
  - frontend/src/hooks/__tests__/useMaiaEloDefault.test.ts
  - frontend/src/lib/gemSweep.ts
  - frontend/src/lib/__tests__/gemSweep.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-07: `GEM_MAIA_MAX_PROB` is 0.2 — a Maia probability of 0.15 that failed C1 before now passes it."
    - "D-01: `deriveRawDefault` and `clampToLadderBounds` are exported from `useMaiaEloDefault.ts`, so the gem rung can be pinned to each mover's own rating-at-game-time without re-deriving the `*_lichess_blitz ?? raw` fallback chain a second time."
    - "D-04: `selectSweepCandidates` keeps only plies where the played move (SAN, converted via `sanToUci`) equals `EvalPoint.best_move` (UCI) AND the ply is out of book (index >= openingPlyCount)."
    - "D-05 (pure half): `nextSweepDispatch` returns no dispatch whenever the live path is busy, the tab is hidden, the sweep is disabled, or a sweep request is already in flight — the yield-to-cursor decision is a pure, worker-free function, testable without mocking React or Workers."
  artifacts:
    - frontend/src/lib/gemSweep.ts (new pure module — prefilter + scheduler decision)
    - frontend/src/lib/__tests__/gemSweep.test.ts (new — Wave 0 gap)
    - frontend/src/lib/gemMove.ts (GEM_MAIA_MAX_PROB = 0.2)
    - frontend/src/hooks/useMaiaEloDefault.ts (two new exports)
  key_links:
    - "`sanToUci(parentFen, playedSan)` from `frontend/src/lib/sanToSquares.ts:63` is the ONLY correct way to compare the played move to `EvalPoint.best_move` — a direct string compare never matches and silently no-ops the whole cascade (RESEARCH Pitfall 2)."
    - "`gemMove.test.ts:58-59` hardcodes `GEM_MAIA_MAX_PROB === 0.1` and MUST be updated to 0.2 in the same commit as the constant (RESEARCH Pitfall 6). This is a deliberate test update, not a regression."
    - "Plans 04 and 05 consume every export created here; nothing here imports React or a Worker."
---

<objective>
Land the three pure primitives the sweep is built from, with zero React and zero Worker involvement: the raised gem threshold (D-07), the exported rating-pin helpers (D-01), and the new `gemSweep.ts` module carrying the free prefilter (D-04) and the yield-to-cursor scheduler decision (D-05).

Purpose: These are the seams where the phase's two silent-failure modes are actually provable — the SAN-vs-UCI trap and the yield-to-cursor invariant — precisely because nothing here needs a mock.
Output: `gemSweep.ts` + its test file, a bumped threshold, and two exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-CONTEXT.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-RESEARCH.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-PATTERNS.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-VALIDATION.md
</context>

## Artifacts this phase produces (plan 02 slice)

| Symbol | Kind | File |
|---|---|---|
| `GEM_MAIA_MAX_PROB = 0.2` | changed constant | `frontend/src/lib/gemMove.ts` |
| `deriveRawDefault` | newly exported function | `frontend/src/hooks/useMaiaEloDefault.ts` |
| `clampToLadderBounds` | newly exported function | `frontend/src/hooks/useMaiaEloDefault.ts` |
| `SweepCandidate` | new interface | `frontend/src/lib/gemSweep.ts` |
| `selectSweepCandidates` | new function | `frontend/src/lib/gemSweep.ts` |
| `SweepDispatchInput` / `SweepDispatch` | new types | `frontend/src/lib/gemSweep.ts` |
| `nextSweepDispatch` | new function | `frontend/src/lib/gemSweep.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raise `GEM_MAIA_MAX_PROB` to 0.2 (D-07) and pin the new literal in its test</name>
  <files>frontend/src/lib/gemMove.ts, frontend/src/lib/__tests__/gemMove.test.ts</files>
  <read_first>
    - `frontend/src/lib/gemMove.ts` — the whole file (92 lines): the constant at line 25, its doc-comment citing SEED-092 D-07/D-08, and `classifyGem`'s `maiaProbability > GEM_MAIA_MAX_PROB` C1 check.
    - `frontend/src/lib/__tests__/gemMove.test.ts` — the `it('D-07: GEM_MAIA_MAX_PROB is exactly 0.1', ...)` case at lines ~58-59 and the boundary cases around it (`GEM_MAIA_MAX_PROB + 0.001`, "returns true at exactly the boundary").
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-CONTEXT.md` § D-07 — the calibration rationale and the enriched-sample caveat.
  </read_first>
  <behavior>
    - `GEM_MAIA_MAX_PROB` equals exactly 0.2.
    - A `maiaProbability` of 0.15 with a passing C2 now classifies as a gem (it did not before).
    - The existing boundary semantics are unchanged: `<=` passes, `> GEM_MAIA_MAX_PROB` fails.
  </behavior>
  <action>
    Change `GEM_MAIA_MAX_PROB` from `0.1` to `0.2` in `frontend/src/lib/gemMove.ts`. Update its doc-comment: cite Phase 172 / SEED-106 D-07, state the new meaning ("fewer than 1 in 5 rating-peers would play it"), and record the measured effect from the Phase 165 calibration TSV — gem frequency x1.35 at Maia-600 rising to ~1.8x at 2200-2600, and the 600-vs-2600 Elo skew NARROWING from 3.8x to 2.9x. Note the caveat that the TSV's sample is enriched, so only ratios transfer, not absolute frequencies.

    In `frontend/src/lib/__tests__/gemMove.test.ts`, update the hardcoded pin test: its description and its expected literal both move from `0.1` to `0.2`, and its citation becomes D-07 (Phase 172). This test is the pin — it is SUPPOSED to fail against the old value; do not "fix" it back. Add one new case asserting that a `maiaProbability` of 0.15 with a passing C2 now returns `true` (it would have returned `false` at the old threshold) — this is the behavioral proof of the raise, not just a constant-equality restatement. Leave the relative boundary cases (`GEM_MAIA_MAX_PROB + 0.001`, exact-boundary) alone: they are written against the constant, not the literal, and remain correct.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/gemMove.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export const GEM_MAIA_MAX_PROB = 0.2;' frontend/src/lib/gemMove.ts` == 1.
    - `grep -c 'GEM_MAIA_MAX_PROB = 0.1' frontend/src/lib/gemMove.ts` == 0.
    - `grep -c 'toBe(0.1)' frontend/src/lib/__tests__/gemMove.test.ts` == 0 (the stale pin is gone, not merely commented out).
    - `frontend/src/lib/__tests__/gemMove.test.ts` contains a case whose inputs include `maiaProbability: 0.15` and which expects `true`.
    - `npm test -- --run src/lib/__tests__/gemMove.test.ts` is green.
  </acceptance_criteria>
  <done>ROADMAP SC5 is met: the threshold is 0.2 and a 0.15-probability move now classifies as a gem, proven by a behavioral test, not only a constant assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export the rating-pin helpers (D-01) and prove the slider cannot move a pinned rung</name>
  <files>frontend/src/hooks/useMaiaEloDefault.ts, frontend/src/hooks/__tests__/useMaiaEloDefault.test.ts</files>
  <read_first>
    - `frontend/src/hooks/useMaiaEloDefault.ts` — the whole file (159 lines): the module-private `clampToLadderBounds` (94-98) and `deriveRawDefault` (104-119), `FREE_PLAY_DEFAULT_ELO`, the `MaiaEloGameData` / `MaiaEloProfile` shapes, and the `userOverrodeRef` override-precedence logic (which stays exactly as-is — the slider still owns the live exploration overlay).
    - `frontend/src/hooks/__tests__/useMaiaEloDefault.test.ts` — the existing case list and how `MaiaEloGameData` fixtures are built.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-CONTEXT.md` § D-01 — "a gem is a property of the game, not of the view"; both movers matter.
  </read_first>
  <behavior>
    - `deriveRawDefault(true, gameData, undefined, 'white')` returns `gameData.white_rating_lichess_blitz`, falling back to `gameData.white_rating` when the normalized value is null.
    - `deriveRawDefault(true, gameData, undefined, 'black')` returns the black-side equivalent — the SAME gameData yields two DIFFERENT rungs depending on the mover.
    - `deriveRawDefault(true, undefined, ...)` returns null (game data not loaded yet).
    - `clampToLadderBounds` clamps to the ladder's outer bounds without step-snapping.
    - **The D-01 regression case:** given a fixed `sideToMove`, the value returned by `deriveRawDefault` is a pure function of `(isGameMode, gameData, profile, sideToMove)` — it does not read, and cannot be perturbed by, any slider / `selectedElo` state. Calling the hook's `setSelectedElo` with an arbitrary value leaves `deriveRawDefault(...)`'s result for that same mover unchanged.
  </behavior>
  <action>
    Change `function clampToLadderBounds(` and `function deriveRawDefault(` to `export function ...` in `frontend/src/hooks/useMaiaEloDefault.ts`. No other change to the hook: the `selectedElo` state, the user-override precedence, and the `defaultElo` / `resetToDefault` contract are untouched — D-01 removes the slider from the GEM rung only, never from the live exploration overlay.

    Update the module doc-comment to record that these two helpers are now exported and why: Phase 172 / SEED-106 D-01 pins the gem rung to each MOVER's own rating-at-game-time, and re-deriving the `*_lichess_blitz ?? raw` fallback chain a second time in the sweep would drift the moment Phase 164's normalization edge cases change (RESEARCH § Don't Hand-Roll).

    Extend `frontend/src/hooks/__tests__/useMaiaEloDefault.test.ts` with a direct-call `describe` block covering every case in `<behavior>` above, including the D-01 regression: render the hook, call `setSelectedElo(2600)`, and assert that `deriveRawDefault(true, gameData, undefined, 'white')` still returns the white player's rating — i.e. the pinned rung is structurally independent of the slider. Reference D-01 in the test descriptions so a future reader knows the slider-linkage was removed on purpose (CONTEXT § Risk Summary: "any test or UAT expectation that assumed slider-linked gems is now wrong on purpose").
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useMaiaEloDefault.test.ts && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function deriveRawDefault' frontend/src/hooks/useMaiaEloDefault.ts` == 1.
    - `grep -c 'export function clampToLadderBounds' frontend/src/hooks/useMaiaEloDefault.ts` == 1.
    - The test file imports both symbols by name from `@/hooks/useMaiaEloDefault`.
    - A test asserts that the same `gameData` yields DIFFERENT rungs for `sideToMove: 'white'` vs `'black'` (both movers matter — D-01).
    - A test asserts that calling `setSelectedElo` does not change `deriveRawDefault`'s result for a fixed mover.
    - `npm test -- --run src/hooks/__tests__/useMaiaEloDefault.test.ts` green; `npx tsc -b --noEmit` zero errors.
  </acceptance_criteria>
  <done>The pin helpers are importable and their slider-independence is proven by test — the D-01 behavior change has a regression guard before any call site consumes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `gemSweep.ts` — the free prefilter (D-04) and the yield-to-cursor scheduler decision (D-05)</name>
  <files>frontend/src/lib/gemSweep.ts, frontend/src/lib/__tests__/gemSweep.test.ts</files>
  <read_first>
    - `frontend/src/lib/gemMove.ts` — the module idiom to mirror: doc-comment citing the phase/decision IDs, plain exported pure functions, explicit "pure, worker-free" rationale, no React/Worker imports.
    - `frontend/src/lib/sanToSquares.ts` lines 55-75 — `sanToUci(fen, san): string | null`, the exact helper to import (do NOT hand-roll SAN→UCI).
    - `frontend/src/types/library.ts` — `EvalPoint` (`es`, `eval_cp`, `best_move`) and the `moves: string[]` alignment comment (moves[i] and eval_series[i] share the same 0-based ply index).
    - `frontend/src/lib/engine/workerPool.ts` lines 127-171 — `dequeueHighestPriority`, for the array-scan-with-tie-break shape only. The sweep does NOT use the pool (see the discretion note in the objective of plan 04); this is a style reference.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-RESEARCH.md` § Common Pitfalls 2 and 4, and § Architecture Patterns "D-05 — the scheduler / contention model".
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-VALIDATION.md` § Half-Invariant Risk — why a "the sweep resolves gems" test proves nothing.
  </read_first>
  <behavior>
    `selectSweepCandidates(moves, evalSeries, openingPlyCount, fenAtPly)`:
    - A ply whose played SAN converts (via `sanToUci` against its PARENT fen) to exactly `evalSeries[i].best_move` survives.
    - A ply whose played SAN differs from `best_move` is dropped.
    - **The SAN/UCI trap, asserted directly:** a fixture where `moves[i] = 'Nf3'` and `best_move = 'g1f3'` MUST survive. A test written as a naive string compare would drop it — this case is the pitfall's regression guard, and it must fail if the `sanToUci` call is removed.
    - A ply with index `< openingPlyCount` is dropped even when the played move IS the best move (D-04 book gate).
    - A ply with `best_move === null` is dropped.
    - An out-of-range / undefined `evalSeries[i]` or `moves[i]` is skipped without throwing (`noUncheckedIndexedAccess` narrowing).
    - Candidates come back in ascending ply order, each carrying `plyIndex`, `parentFen`, `playedSan`.

    `nextSweepDispatch(input)`:
    - Returns `{ kind: 'idle' }` when `liveBusy` is true — **even when unresolved candidates exist and nothing is in flight.** This is the yield-to-cursor invariant in its purest, most revertible form.
    - Returns `{ kind: 'idle' }` when `tabHidden`, when `enabled` is false, or when `inFlight` is non-null (one candidate at a time).
    - Otherwise returns `{ kind: 'dispatch', candidate }` for the lowest-`plyIndex` candidate not present in `resolvedPlyIndices`.
    - Returns `{ kind: 'done' }` when every candidate is resolved and nothing is in flight.
  </behavior>
  <action>
    Create `frontend/src/lib/gemSweep.ts` as a pure, worker-free module in `gemMove.ts`'s idiom. Header doc-comment: cite Phase 172 / SEED-106 D-04 and D-05, and state the "worker-free so the yield-to-cursor invariant is testable without mocking React or Workers" rationale explicitly.

    Export `SweepCandidate { plyIndex: number; parentFen: string; playedSan: string }` and `selectSweepCandidates(moves: string[], evalSeries: EvalPoint[], openingPlyCount: number, fenAtPly: (i: number) => string | null): SweepCandidate[]` implementing the free prefilter (D-04): skip `i < openingPlyCount`, skip a null/absent `best_move` or `moves[i]`, resolve the parent FEN via the injected `fenAtPly` (skip when it returns null), convert the played SAN with `sanToUci(parentFen, playedSan)`, and keep only strict equality against `best_move`. Strict equality (not an es-loss band) is deliberate and fails safe — the backend searched deeper than the live grading run, so on disagreement we lose a rare gem rather than invent one. Comment that reasoning at the comparison.

    Export the scheduler decision as a pure function: `SweepDispatchInput { candidates: SweepCandidate[]; resolvedPlyIndices: ReadonlySet<number>; inFlight: SweepCandidate | null; liveBusy: boolean; tabHidden: boolean; enabled: boolean }` and `SweepDispatch = { kind: 'idle' } | { kind: 'done' } | { kind: 'dispatch'; candidate: SweepCandidate }`, plus `nextSweepDispatch(input: SweepDispatchInput): SweepDispatch` implementing the `<behavior>` contract above. Order the guards so `liveBusy` is checked BEFORE any candidate selection, and comment it as the D-05 yield-to-cursor invariant: the live position always wins, and this function is the only place that decides whether background work may start.

    Keep nesting depth <= 3 and use `Literal`-style discriminated unions (no loose strings) per CLAUDE.md. No magic numbers.

    Create `frontend/src/lib/__tests__/gemSweep.test.ts` (Wave 0 gap from VALIDATION.md) covering every case in `<behavior>`, in `gemMove.test.ts`'s idiom. Two cases are load-bearing and must be labelled as such in their descriptions:
    1. The SAN-vs-UCI case (`Nf3` vs `g1f3` survives) — the guard against RESEARCH Pitfall 2.
    2. The `liveBusy` case — `nextSweepDispatch` returns `idle` with unresolved candidates present and nothing in flight. Name it as the D-05 yield-to-cursor invariant and note in a comment that deleting the `liveBusy` guard MUST turn this test red (the revert-and-fail-red discipline from the project's mutation-test-gap-closure memory).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/gemSweep.test.ts && npx tsc -b --noEmit && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/gemSweep.ts` exists and imports NOTHING from `react`, `@/hooks/*`, or any Worker: `grep -cE "from 'react'|/hooks/|new Worker" frontend/src/lib/gemSweep.ts` == 0.
    - `grep -c 'sanToUci' frontend/src/lib/gemSweep.ts` >= 1 (the prefilter converts before comparing).
    - `gemSweep.test.ts` contains a case whose fixture pairs a SAN played move with a UCI `best_move` for the same move and expects a surviving candidate.
    - `gemSweep.test.ts` contains a case asserting `nextSweepDispatch({ ..., liveBusy: true, inFlight: null, candidates: [<>=1 unresolved] })` returns `kind: 'idle'`.
    - **Revert proof (manual, recorded in the SUMMARY):** temporarily delete the `liveBusy` guard from `nextSweepDispatch`, run the file, confirm the yield-to-cursor case goes RED, restore the guard, confirm green. Paste both outcomes into the plan SUMMARY. Grep/symbol-presence is NOT acceptable evidence for this criterion.
    - `npm test -- --run src/lib/__tests__/gemSweep.test.ts` green; `npx tsc -b --noEmit`, `npm run lint`, `npm run knip` all clean.
  </acceptance_criteria>
  <done>The prefilter and the scheduler decision exist as pure functions, the SAN/UCI trap has a regression test, and the yield-to-cursor invariant is proven red-then-green at the only seam where it can be proven without mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds pure client-side functions. No network call, no auth, no user input, no persistence, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-03 | Denial of Service (against the user's own device) | `nextSweepDispatch` | medium | mitigate | This function is the phase's only admission gate for background engine work. It hard-caps concurrency at one in-flight candidate (`inFlight != null` → idle) and refuses to dispatch while the live path is busy, the tab is hidden, or the sweep is disabled. An unbounded worker spawn is structurally impossible through this API — there is no code path that returns more than one candidate per call. Enforced by the unit tests in Task 3. |
| T-172-04 | Tampering | `GEM_MAIA_MAX_PROB` threshold change | low | accept | A display-classification constant with no security effect. The badge is advisory; no authorization, spend, or data access depends on it. |

Honest scope note: this phase adds no new network surface, no auth change, no user input, no persistence, and no new dependency. Client-side self-DoS via unbounded worker spawn is the only real threat, and it is mitigated at this plan's `nextSweepDispatch` gate plus plan 04's device/visibility gates. No threats were invented to fill the register.
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/lib/__tests__/gemSweep.test.ts src/lib/__tests__/gemMove.test.ts src/hooks/__tests__/useMaiaEloDefault.test.ts` green.
- `npx tsc -b --noEmit` — zero errors (lint and unit tests do NOT type-check; this step is mandatory per CLAUDE.md).
- `npm run lint` and `npm run knip` clean. (Test-only consumers are acceptable to knip — 54 exports in the shipped tree are already consumed only by their test files and knip is green. Do NOT delete an export because plans 04/05 have not imported it yet.)
- **Structural blindness callout:** `tsc`, `eslint`, `knip` and every pre-existing test are blind to the yield-to-cursor invariant. Only Task 3's revert-and-fail-red proof establishes it at this layer; plan 05's contention test establishes the other half (that the sweep does not share a hook instance with the live path).
</verification>

<success_criteria>
- ROADMAP SC5 (threshold raise) fully met.
- ROADMAP SC1 (free prefilter, SAN→UCI, book gate) proven at unit level.
- ROADMAP SC2 (yield-to-cursor) proven at the pure-scheduler level, red-then-green.
- ROADMAP SC3 (rung pin) has its exported helpers and its slider-independence regression test.
- D-01, D-04, D-05 (pure half), D-07 implemented.
</success_criteria>

<output>
Create `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-02-SUMMARY.md` when done. It MUST include the revert-proof evidence (RED output, then GREEN output) for the yield-to-cursor case.
</output>
