---
phase: 172-background-gem-sweep-on-analysis-seed-106
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/lib/bookGlyph.ts
  - frontend/src/components/icons/BookIcon.tsx
  - frontend/src/components/analysis/VariationTree.tsx
  - frontend/src/components/analysis/__tests__/VariationTree.test.tsx
  - frontend/src/components/board/boardMarkers.tsx
  - frontend/src/components/board/__tests__/boardMarkers.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-08: the book marker renders on BOTH surfaces gems already render on — the `VariationTree` move-list marker AND the board corner marker — with precedence `severity > gem > book`."
    - "D-08: severity overrides the book icon. A book move that is also a blunder/mistake shows the flaw, not the reassurance that it was theory."
    - "The book marker is glance-only: no popover, no click target, no data-testid (it mirrors BlunderIcon/MistakeIcon, not GemMoveBadge)."
    - "`BOOK_MARKER_COLOR` lives in `theme.ts` and reaches both consumers through the single `BOOK_GLYPH` record, so the list icon and the board badge can never drift."
  artifacts:
    - frontend/src/lib/theme.ts (`BOOK_MARKER_COLOR`)
    - frontend/src/lib/bookGlyph.ts (new — `BOOK_GLYPH`)
    - frontend/src/components/icons/BookIcon.tsx (new)
    - frontend/src/components/analysis/VariationTree.tsx (`resolveMarkerIcon` gains the book clause; `FlawMarkerEntry.book`)
    - frontend/src/components/board/boardMarkers.tsx (`SquareMarker.book`; `SquareMarkerBadge` book branch)
  key_links:
    - "The two marker surfaces implement precedence DIFFERENTLY: `VariationTree.resolveMarkerIcon` has real runtime branching (a merged entry can carry both severity and gem), while `boardMarkers` relies on construction-time mutual exclusivity enforced at the Analysis.tsx call site. One shared fix does NOT cover both — this plan fixes the component halves; plan 05 fixes the board call site."
    - "Plan 05 is what actually SETS `book` (from `plyIndex < opening_ply_count`). This plan makes both surfaces able to render it."
---

<objective>
Build the book marker's display layer (D-08): a new `BOOK_MARKER_COLOR` theme token, the `BOOK_GLYPH` one-record-two-consumers spec, a `BookIcon` component, and the `severity > gem > book` precedence in BOTH marker surfaces.

Purpose: `opening_ply_count` earns itself twice — it gates the sweep (D-04) and it marks theory plies (D-08). This plan is the second half, and it is deliberately isolated from `Analysis.tsx` so it can land in parallel with the backend field and the pure sweep primitives.
Output: A renderable book marker on the move list and the board, with precedence tests on both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-CONTEXT.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-UI-SPEC.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-PATTERNS.md
</context>

## Artifacts this phase produces (plan 03 slice)

| Symbol | Kind | File |
|---|---|---|
| `BOOK_MARKER_COLOR = 'oklch(0.60 0.04 250)'` | new theme token | `frontend/src/lib/theme.ts` |
| `BOOK_GLYPH` | new glyph record | `frontend/src/lib/bookGlyph.ts` |
| `BookIcon` / `BookIconProps` | new component | `frontend/src/components/icons/BookIcon.tsx` |
| `FlawMarkerEntry.book?: boolean` | new field | `frontend/src/components/analysis/VariationTree.tsx` |
| `resolveMarkerIcon(...).isBook` | new return field | `frontend/src/components/analysis/VariationTree.tsx` |
| `SquareMarker.book?: boolean` | new field | `frontend/src/components/board/boardMarkers.tsx` |

<!-- planner-discipline-allow: data-testid -->

<tasks>

<task type="auto">
  <name>Task 1: `BOOK_MARKER_COLOR` + `bookGlyph.ts` + `BookIcon.tsx`</name>
  <files>frontend/src/lib/theme.ts, frontend/src/lib/bookGlyph.ts, frontend/src/components/icons/BookIcon.tsx</files>
  <read_first>
    - `frontend/src/lib/gemGlyph.ts` — the whole file. Its "one record, two consumers" doc-comment and its react-refresh rationale (glyph spec lives in a plain module, not the icon component file) are the exact pattern to copy.
    - `frontend/src/components/icons/GemIcon.tsx` — the whole file. The 24-unit viewBox, the `r=11` circle, the inner-icon `x/y/width/height = 5,5,14,14` geometry, the `aria-hidden` default, and the `<title>` element.
    - `frontend/src/lib/theme.ts` — the `MAIA_ACCENT` / `STOCKFISH_ACCENT` / `FLAWCHESS_ENGINE_ACCENT` block (where the new token is grouped) and the near-zero-chroma precedents `MOVE_QUALITY_PENDING` / `EVAL_CHART_PHASE_LABEL`.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-UI-SPEC.md` § Color and § Component Contract — the token value, its chroma rationale, and the accessible-title copy. This is the design contract; do not deviate from it.
  </read_first>
  <action>
    Add `export const BOOK_MARKER_COLOR = 'oklch(0.60 0.04 250)';` to `frontend/src/lib/theme.ts`, grouped with the other corner-marker identity colors. Doc-comment it per UI-SPEC: chroma 0.04 is deliberately the LOWEST of any corner-marker hue (gem 0.20, Stockfish 0.16, FlawChess 0.14, severities 0.13-0.19) so the book badge visually recedes behind every alert/insight badge; hue 250 sits in the cool-blue family without colliding with Stockfish's 255; it is reserved EXCLUSIVELY for the book/opening-theory marker family and must not be reused as a general-purpose muted blue.

    Create `frontend/src/lib/bookGlyph.ts` as a verbatim structural copy of `gemGlyph.ts`: import `BOOK_MARKER_COLOR` from `@/lib/theme`, export `BOOK_GLYPH: { color: string }`. Keep the same "one record, two consumers" doc-comment rationale (its consumers are `BookIcon` and `boardMarkers`'s book branch, so the two can never drift).

    Create `frontend/src/components/icons/BookIcon.tsx` as a verbatim structural copy of `GemIcon.tsx`, swapping: lucide `Gem` → `BookOpen`, `GEM_GLYPH` → `BOOK_GLYPH`, `GemIconProps`/`GemIcon` → `BookIconProps`/`BookIcon`, and the SVG `<title>` to exactly `Opening theory` (UI-SPEC copywriting contract). Same viewBox, same circle radius, same inner-icon geometry, same `aria-hidden` default.

    Do NOT add a `data-testid` to the book glyph and do NOT build a `BookMoveBadge` popover — UI-SPEC settles both explicitly: the book marker is non-interactive and glance-only, mirroring `BlunderIcon`/`MistakeIcon` (neither of which carries a testid), and it has nothing to disclose ("this ply is inside known opening theory" is the entire fact). `BookOpen` is the codebase's established "opening" glyph — do not substitute the plain `Book`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BOOK_MARKER_COLOR = 'oklch(0.60 0.04 250)'" frontend/src/lib/theme.ts` == 1.
    - `frontend/src/lib/bookGlyph.ts` imports `BOOK_MARKER_COLOR` from the theme and contains no raw color literal: `grep -cE 'oklch|#[0-9a-fA-F]{3,8}|rgb' frontend/src/lib/bookGlyph.ts` == 0.
    - `frontend/src/components/icons/BookIcon.tsx` imports `BookOpen` from `lucide-react` and `BOOK_GLYPH` from `@/lib/bookGlyph`; it contains no raw color literal.
    - `grep -c 'Opening theory' frontend/src/components/icons/BookIcon.tsx` == 1.
    - `grep -c 'data-testid' frontend/src/components/icons/BookIcon.tsx` == 0.
    - `npx tsc -b --noEmit` and `npm run lint` clean.
  </acceptance_criteria>
  <done>The book glyph exists as a single color record with one icon consumer, matching the gem precedent exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `severity > gem > book` in the `VariationTree` move-list marker</name>
  <files>frontend/src/components/analysis/VariationTree.tsx, frontend/src/components/analysis/__tests__/VariationTree.test.tsx</files>
  <read_first>
    - `frontend/src/components/analysis/VariationTree.tsx` — `resolveMarkerIcon` (lines 59-69) and its doc-comment (which explains WHY severity wins: a backend severity and the live WASM gem can legitimately disagree), `MoveListMarker` (77-97, the `isGem` popover branch vs the plain-icon fall-through), and `FlawMarkerEntry` (140-179, the doc-comment style of `gem?` / `gemMaiaProbability?` / `gemElo?` / `gemByOpponent?`).
    - `frontend/src/components/analysis/__tests__/VariationTree.test.tsx` — the existing marker cases and how `FlawMarkerEntry` fixtures are built.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-UI-SPEC.md` § Precedence wiring — the exact target shape of `resolveMarkerIcon`.
  </read_first>
  <behavior>
    - An entry with `severity: 'blunder'` and `book: true` renders the blunder glyph, NOT the book icon (D-08: severity overrides book — a book move can still be an inaccuracy, and the user needs the flaw).
    - An entry with `severity: 'mistake'` and `book: true` renders the mistake glyph.
    - An entry with `gem: true` and `book: true` renders the gem icon (the chain is stated in full even though D-04 means this pair never actually arises in production, since book plies are skipped before classification).
    - An entry with only `book: true` renders `BookIcon`, with `show: true`, `isGem: false`, `isBook: true`.
    - An entry with none of the three renders nothing (`show: false`).
    - A book-only entry takes the PLAIN icon path in `MoveListMarker` — it is not wrapped in the gem popover.
  </behavior>
  <action>
    Add `book?: boolean;` to `FlawMarkerEntry` in `VariationTree.tsx`, doc-commented in the file's existing style: Phase 172 / SEED-106 D-08, true when this node's ply is within `opening_ply_count` (set at the `Analysis.tsx` call site that builds `moveListMarkers`); lowest precedence — severity and gem both override it.

    Extend `resolveMarkerIcon`'s return type with `isBook: boolean` and add the book clause as the LAST branch, after both the severity branch and the gem branch, returning `{ show: true, Icon: BookIcon, isGem: false, isBook: true }`. Every existing return path must also carry `isBook: false`. Update the function's doc-comment to state the full chain `severity > gem > book` and cite D-08.

    `MoveListMarker` needs NO new branch: it already branches on `isGem` for the popover-wrapped path and falls through to the plain `<Icon className={className} aria-hidden />` render, which is exactly what book needs. Confirm this by reading it — do not add a book branch there.

    Check for duplicated desktop/mobile marker markup in this component (CLAUDE.md: "always apply changes to mobile too"). Both lists render through `MoveListMarker`, so a single `resolveMarkerIcon` change covers both — VERIFY this by grepping the file for other `resolveMarkerIcon` / icon-render call sites before declaring the task done, and state the finding in the SUMMARY.

    Extend `VariationTree.test.tsx` with the cases in `<behavior>` above, including explicitly the `severity + book` case (severity wins) and the book-only case.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/VariationTree.test.tsx && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `resolveMarkerIcon`'s book clause appears AFTER both the severity and gem clauses in source order: `grep -n "flaw?.gem\|flaw?.book\|flaw.severity === 'blunder'" frontend/src/components/analysis/VariationTree.tsx` shows severity first, gem second, book third.
    - `grep -c 'isBook' frontend/src/components/analysis/VariationTree.tsx` >= 4 (return type + all return paths).
    - `grep -c 'BookIcon' frontend/src/components/analysis/VariationTree.tsx` >= 2 (import + use).
    - `VariationTree.test.tsx` contains a case with BOTH `severity` and `book: true` asserting the severity glyph renders and `isBook` is false.
    - `VariationTree.test.tsx` contains a book-only case asserting the book icon renders.
    - `npm test -- --run src/components/analysis/__tests__/VariationTree.test.tsx` green.
  </acceptance_criteria>
  <done>The move list renders a book marker at the bottom of the precedence chain, with the severity-wins case pinned by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The board corner book marker</name>
  <files>frontend/src/components/board/boardMarkers.tsx, frontend/src/components/board/__tests__/boardMarkers.test.tsx</files>
  <read_first>
    - `frontend/src/components/board/boardMarkers.tsx` — the whole file (184 lines). Note especially: `SquareMarker` (24-33) and its doc-comment stating that severity/gem are "mutually exclusive by construction — no runtime assertion enforces the exclusivity, callers only ever set one or the other"; `SquareMarkerBadge`'s `if (marker.gem)` block (116-131) with its `MARKER_STROKE` / `GEM_ICON_DIAMETER_RATIO` / `r` / `cx` / `cy` geometry; and the severity fallback below it.
    - `frontend/src/components/board/__tests__/boardMarkers.test.tsx` — how existing gem/severity marker render cases are asserted.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-UI-SPEC.md` § Spacing Scale (marker geometry table) — the book badge reuses `MARKER_RADIUS` / `MARKER_RADIUS_SMALL` / `MARKER_CORNER_OVERLAP` / `MARKER_STROKE` / `GEM_ICON_DIAMETER_RATIO` VERBATIM. Do not introduce a `BOOK_ICON_DIAMETER_RATIO` or any new geometry constant.
  </read_first>
  <behavior>
    - A `SquareMarker` with `book: true` renders a circle filled with `BOOK_GLYPH.color` and a white `BookOpen` glyph, at the same radius / corner offset / stroke as the gem badge.
    - A marker with `gem: true` still renders the gem badge (the book branch must be inserted AFTER the gem branch, before the severity fallback).
    - A marker with neither `severity` nor `gem` nor `book` still returns null (the existing defensive guard).
  </behavior>
  <action>
    Add `book?: boolean;` to `SquareMarker` in `boardMarkers.tsx`, doc-commented like `gem?`: Phase 172 / SEED-106 D-08, renders the muted book badge instead of a severity glyph; mutually exclusive with `severity`/`gem` BY CONSTRUCTION at the call site (no runtime assertion — same contract the gem field already carries; the guard lives in `Analysis.tsx`'s `boardSquareMarkers` memo, wired in plan 05).

    Add a third branch to `SquareMarkerBadge`, structurally identical to the existing `if (marker.gem)` block and inserted immediately after it (before the severity fallback): same `iconSize` computation from `GEM_ICON_DIAMETER_RATIO`, same `<circle>` with `MARKER_STROKE` and `strokeWidth={1}`, swapping the fill to `BOOK_GLYPH.color` and the glyph to lucide `BookOpen` with `stroke="#fff"`. Import `BOOK_GLYPH` from `../../lib/bookGlyph` alongside the existing `GEM_GLYPH` import, and `BookOpen` alongside `Gem` from `lucide-react`.

    Update the file's `SquareMarker` doc-comment so the mutual-exclusivity note covers three fields, not two.

    Extend `boardMarkers.test.tsx` with a book-marker render case asserting the badge circle carries the book color and a `BookOpen` glyph is present, plus a case confirming a `gem: true` marker still renders the gem badge (ordering regression guard).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/board/__tests__/boardMarkers.test.tsx && npx tsc -b --noEmit && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'marker.gem\|marker.book\|marker.severity' frontend/src/components/board/boardMarkers.tsx` shows the gem branch BEFORE the book branch, and the severity fallback last.
    - `grep -c 'BOOK_GLYPH' frontend/src/components/board/boardMarkers.tsx` >= 2 (import + fill).
    - `boardMarkers.tsx` introduces no new geometry constant: `grep -c 'BOOK_ICON_DIAMETER_RATIO\|BOOK_MARKER_RADIUS' frontend/src/components/board/boardMarkers.tsx` == 0.
    - `boardMarkers.test.tsx` contains a `book: true` render case and a `gem: true` regression case.
    - `npm test -- --run src/components/board/__tests__/boardMarkers.test.tsx` green; `npx tsc -b --noEmit`, `npm run lint`, `npm run knip` clean.
  </acceptance_criteria>
  <done>Both marker surfaces can render the book badge. The two implement precedence differently (real branching vs construction-time exclusivity) and both were fixed on their own terms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure display components. No network call, no auth, no user input, no persistence, no new dependency (lucide-react and the theme module are already shipped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-05 | Spoofing (of signal, not identity) | Marker precedence in `resolveMarkerIcon` / `SquareMarkerBadge` | low | mitigate | A book badge shown INSTEAD of a blunder badge would suppress a real, actionable warning — the closest thing to a security-relevant failure in a display-only plan. Mitigated by making severity the first clause in both surfaces and pinning the `severity + book` case with a test in each. |
| T-172-06 | Information Disclosure | `BookIcon` accessible `<title>` | low | accept | Renders the fixed string "Opening theory". No user or game data reaches the DOM through this component. |

No package installs in this plan. This phase adds no new network surface, no auth change, no user input, and no persistence — the register is short because the honest surface is small.
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/analysis/__tests__/VariationTree.test.tsx src/components/board/__tests__/boardMarkers.test.tsx` green.
- `npx tsc -b --noEmit` zero errors; `npm run lint`; `npm run knip` clean (`BookIcon` is consumed by `VariationTree`, `BOOK_GLYPH` by `BookIcon` and `boardMarkers`, `BOOK_MARKER_COLOR` by `bookGlyph` — every new export has a real consumer inside this plan).
- Mobile parity: state in the SUMMARY which render paths were checked in `VariationTree.tsx` (desktop list, mobile list) and confirm both route through `MoveListMarker`.
</verification>

<success_criteria>
- ROADMAP SC6 (marker half): every ply within `opening_ply_count` CAN render a book marker on both the `VariationTree` and the board corner marker, with precedence `severity > gem > book`. (Plan 05 supplies the `book` value itself.)
- D-08 implemented on both surfaces, each on its own precedence mechanism.
</success_criteria>

<output>
Create `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-03-SUMMARY.md` when done.
</output>
