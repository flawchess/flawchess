---
phase: 172-background-gem-sweep-on-analysis-seed-106
plan: 05
type: execute
wave: 3
depends_on:
  - 172-01
  - 172-02
  - 172-03
  - 172-04
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/pages/__tests__/Analysis.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-01: the live gem rung is each MOVER's own Lichess-blitz-normalized rating-at-game-time, not `selectedElo`. Moving the ELO slider no longer changes which moves are gems. The slider still drives the live exploration overlay (Maia chart, WDL bar, FlawChess engine) exactly as before."
    - "D-03: the sweep starts on the analysis-readiness FALSE→TRUE TRANSITION, not on mount — a bot game opened while its tier-1 analysis is still running is swept the moment the evals land, without a reload."
    - "D-03: an unanalyzed game with no active job is never swept — it keeps today's lazy per-node behavior and the existing one-click Analyze pill, untouched."
    - "D-05: the live `gemGrading` / `grading` / `maia` hook instances are never driven with a sweep FEN. The sweep's own instances are separate, and this is proven by a contention test that fails if the instances are shared."
    - "D-08: `book` is set from `plyIndex < opening_ply_count` on BOTH marker surfaces, and on the board it is appended only when the square carries neither a severity nor a gem marker."
    - "D-02: gems remain analysis-move-list-only — no persistence, no backend gem store, no Library-card or stats surfacing is added by this wiring."
    - "D-06/D-07 consumed end to end: `opening_ply_count` gates the sweep's prefilter and paints the book markers; the raised `GEM_MAIA_MAX_PROB` is what the sweep's C1 tier applies."
  artifacts:
    - frontend/src/pages/Analysis.tsx (rung pin, sweep wiring, readiness transition, marker call sites)
    - frontend/src/pages/__tests__/Analysis.test.tsx (contention, transition, lazy, slider-independence cases)
  key_links:
    - "`evalChartReady` (Analysis.tsx:2214) is the readiness signal, already delivered by `useLibraryGame(gameId, { live: true })` (line 597-600, quick 260714-rj5). It must be HOISTED above the sweep wiring to be usable as the sweep's gate — no new polling machinery."
    - "Sweep results are merged into the DISPLAY maps (`moveListMarkers`, `boardSquareMarkers`) alongside `gemByNode` — they are NOT written into `gemByNode` itself, which shares a 256-entry FIFO budget with free-variation exploration (Pitfall 4)."
    - "`fenAtPly(i)` = the FEN of `mainLine[i-1]` (MoveNode.fen is the position AFTER the move), or the root FEN for i === 0."
---

<objective>
Wire the sweep into `/analysis`: pin the gem rung to the mover (D-01), start the sweep on the analysis-readiness transition (D-03), feed it the free-prefilter candidates (D-04), merge its results into the two display surfaces alongside the book markers (D-08), and prove — with a test that goes red if the instances are shared — that the live position's engines are never driven by sweep work (D-05).

Purpose: This is where all seven success criteria become observable in the product, and where the phase's two silent-failure modes would actually bite.
Output: Gem badges that fill in ahead of the cursor; book badges on theory plies; a slider that no longer moves gems.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-CONTEXT.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-RESEARCH.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-PATTERNS.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-UI-SPEC.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-VALIDATION.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-02-SUMMARY.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-03-SUMMARY.md
@.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-04-SUMMARY.md
</context>

## Artifacts this phase produces (plan 05 slice)

| Symbol | Kind | File |
|---|---|---|
| `pinnedEloForMover` (or equivalent module-scope helper) | new helper | `frontend/src/pages/Analysis.tsx` |
| `sweepCandidates` memo | new memo | `frontend/src/pages/Analysis.tsx` |
| `sweep` (the `useGemSweep` call) | new hook call | `frontend/src/pages/Analysis.tsx` |
| `sweptGameIdRef` | new ref (D-03 transition guard) | `frontend/src/pages/Analysis.tsx` |
| `book` field population on `moveListMarkers` / `boardSquareMarkers` | new wiring | `frontend/src/pages/Analysis.tsx` |

**Refactor constraint (CLAUDE.md):** `Analysis.tsx` is already very large. The sweep's logic lives in `useGemSweep` (plan 04) and `gemSweep.ts` (plan 02) — this plan adds only glue. If any memo/effect added or edited here exceeds nesting depth 3 or ~40 logic LOC, extract it to a module-scope helper in this file (the `findFocusedFlaw` precedent at line ~368) rather than inflating the component body. Do not refactor unrelated parts of the file.

<!-- planner-discipline-allow: selectedElo -->
<!-- planner-discipline-allow: deriveRawDefault -->
<!-- planner-discipline-allow: clampToLadderBounds -->
<!-- planner-discipline-allow: ELO-slider change can newly qualify -->
<!-- planner-discipline-allow: lichess_blitz -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the live gem rung to the mover (D-01) and paint the book markers (D-08)</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 1414-1539 — the live gem block end to end. Three sites read the slider: `gemC1`'s `nearestByElo(parentCurve, selectedElo)` (line 1451), `parentGemCandidateSans`'s `selectCandidatesByMass(parentCurve, selectedElo, ...)` (line 1468), and the C2 effect's `nearestByElo(parentCurve ?? [], selectedElo)` + `elo: selectedElo` in the stamped `GemDetail` (lines 1501, 1516). All four are D-01 targets. The effect ALREADY computes `const mover = sideToMoveFromFen(parentFen)` at line 1495 and uses it for `byOpponent` at 1509-1512 — the machinery for "classify against whoever actually moved" exists; only the RUNG lookup is wrong.
    - `frontend/src/pages/Analysis.tsx` lines 594-620 — the `useLibraryGame` fetch and the `useMaiaEloDefault` call. `selectedElo` must KEEP driving the Maia chart / WDL bar / FlawChess engine (`maia`, `flawChessEngine`, `moveProbabilities`) — D-01 removes it from the GEM rung only.
    - `frontend/src/pages/Analysis.tsx` lines 1548-1610 — `moveListMarkers` and its `addGem` merge (note the "a backend severity wins" guard).
    - `frontend/src/pages/Analysis.tsx` lines 2046-2064 — the `boardSquareMarkers` memo and the gem append's `!base.some((m) => m.square === lastMove.to && m.severity != null)` guard.
    - `frontend/src/hooks/useMaiaEloDefault.ts` — the now-exported `deriveRawDefault` / `clampToLadderBounds` / `FREE_PLAY_DEFAULT_ELO` (plan 02).
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-UI-SPEC.md` § Precedence wiring — the exact board-append guard shape for book.
  </read_first>
  <behavior>
    - Moving the ELO slider does not change which moves are gems, and does not re-resolve or un-resolve any node's gem state.
    - Moving the ELO slider still changes the Maia chart, the WDL bar, and the FlawChess engine's lines (unchanged behavior).
    - A gem played by White is classified at White's rating-at-game-time; the same game's Black gem is classified at Black's. The stamped `GemDetail.elo` reports the rung it was actually detected at (the mover's), which is what the gem popover discloses.
    - A ply with `plyIndex < opening_ply_count` and no severity and no gem renders the book marker in the move list and on the board corner.
    - A book ply that IS a blunder/mistake renders the severity glyph, not the book icon (both surfaces).
  </behavior>
  <action>
    **D-01 (rung pin).** Import `deriveRawDefault`, `clampToLadderBounds` and `FREE_PLAY_DEFAULT_ELO` from `@/hooks/useMaiaEloDefault`. Add a module-scope (or `useCallback`) helper that maps a parent FEN to its pinned rung: `clampToLadderBounds(deriveRawDefault(isGameMode, gameData, userProfile, sideToMoveFromFen(parentFen)) ?? FREE_PLAY_DEFAULT_ELO)`. Replace `selectedElo` with this pinned rung at ALL FOUR gem sites listed in `<read_first>` — `gemC1`'s probability lookup, `parentGemCandidateSans`'s `selectCandidatesByMass`, and the C2 effect's probability lookup and its stamped `GemDetail.elo`. Update each memo/effect's dependency array accordingly: `selectedElo` comes OUT of the gem hooks' deps, `gameData` / `isGameMode` / `userProfile` go in.

    Update the stale comments in that block that describe slider-linked behavior — specifically the `gemC1` doc-comment ("Re-reads at the current selectedElo so an ELO-slider change can newly qualify a move") and the `boardSquareMarkers` comment ("An ELO change can still newly qualify an as-yet-unresolved node"). Both are now false by design. Replace them with the D-01 rationale: a gem is a property of the game, not of the view, and pinning is what makes a background sweep cacheable at all — otherwise every slider nudge would invalidate the whole sweep. Do NOT touch `selectedElo`'s other consumers.

    **D-08 (book markers).** In `moveListMarkers`, set `book: true` on the entry for a mainline node whose ply index is `< gameData.opening_ply_count`, merged with the same "existing severity wins / existing gem wins" discipline the `addGem` path already uses — book is the LOWEST precedence, so it must not overwrite either. (`resolveMarkerIcon` in `VariationTree.tsx`, from plan 03, enforces the render-time chain; this call site must not hand it a contradictory entry.)

    In `boardSquareMarkers`, add a THIRD append after the existing gem append, gated on the current node being a mainline ply inside the book AND the square carrying neither a severity nor a gem marker: `!base.some((m) => m.square === lastMove.to && (m.severity != null || m.gem === true))`. This mirrors the gem append's construction-time-exclusivity pattern exactly — `boardMarkers.tsx` does NOT branch on precedence internally (plan 03 only added the render branch), so the guard MUST live here.

    Ply-index math: a mainline node's ply is its 0-based index in `mainLine`, which is the same index `gameData.moves` and `gameData.eval_series` use. A node is in book when that index is `< opening_ply_count`. Free-variation nodes are never in book (they are not mainline plies) — do not paint book markers on them.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx && npx tsc -b --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The gem block no longer reads the slider: in the region between `const gemActive` and the end of the C2 effect, `grep -c 'selectedElo'` == 0 (verify with `sed -n '1414,1545p' frontend/src/pages/Analysis.tsx | grep -c selectedElo` == 0, adjusting for line drift).
    - `selectedElo` is still consumed elsewhere in the file (the live overlay is intact): `grep -c 'selectedElo' frontend/src/pages/Analysis.tsx` >= 8.
    - `grep -c 'deriveRawDefault' frontend/src/pages/Analysis.tsx` >= 1 and `grep -c 'clampToLadderBounds' frontend/src/pages/Analysis.tsx` >= 1 — the fallback chain is imported, not reimplemented. `grep -c 'lichess_blitz' frontend/src/pages/Analysis.tsx` == 0 (no second copy of the chain).
    - The board's book append guards on BOTH severity and gem: `grep -c "m.gem === true" frontend/src/pages/Analysis.tsx` >= 1.
    - `book: true` is set from a comparison against `opening_ply_count` (`grep -c 'opening_ply_count' frontend/src/pages/Analysis.tsx` >= 1).
    - The stale slider-linked gem comments are gone: `grep -c 'ELO-slider change can newly qualify' frontend/src/pages/Analysis.tsx` == 0.
    - `npx tsc -b --noEmit` zero errors.
  </acceptance_criteria>
  <done>Gems are pinned to the mover, the slider is out of the gem path, and both marker surfaces paint book plies at the bottom of the precedence chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the sweep — readiness transition, candidates, and the display merge</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/hooks/useGemSweep.ts` (plan 04) — its full options/state contract, and the `SWEEP_GRADING_MOVETIME_MS` value recorded in `172-04-SUMMARY.md`.
    - `frontend/src/lib/gemSweep.ts` (plan 02) — `selectSweepCandidates`'s exact signature, including the injected `fenAtPly`.
    - `frontend/src/pages/Analysis.tsx` lines 2206-2226 — `evalChartReady`'s definition and the `evalPending` pill just below it. This is the readiness signal; it is currently declared FAR below where the sweep must consume it.
    - `frontend/src/pages/Analysis.tsx` lines 594-600 — `useLibraryGame(gameId, { live: true })`, which already delivers the FALSE→TRUE transition via its poll (quick 260714-rj5). No new query machinery is needed.
    - `frontend/src/hooks/useAnalysisBoard.ts` lines 23-35 — `MoveNode.fen` is the FEN of the position AFTER the move, so the PARENT fen of `mainLine[i]` is `nodes.get(mainLine[i-1])!.fen`, and the root FEN for `i === 0`.
    - `frontend/src/pages/Analysis.tsx` lines 1460-1478 — `needParentGemGrade`, the live gate the sweep must not fight with.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-RESEARCH.md` § "D-03 — the readiness transition" and § Common Pitfalls 5 (mount-gating kills the bot-game case) and 4 (cache eviction).
  </read_first>
  <behavior>
    - Opening an ALREADY-analyzed game starts the sweep; gem badges appear on plies the cursor has not yet reached.
    - Opening a game whose tier-1 analysis is still running does NOT sweep at mount, and DOES sweep the moment `eval_series` arrives via the existing poll — no reload, no navigation.
    - The sweep starts at most once per `gameId`: later poll-driven re-renders (e.g. flaw markers updating) do not re-trigger it.
    - A game with `analysis_state === 'no_engine_analysis'` and no active job never sweeps; it keeps the lazy per-node path and the Analyze pill.
    - The sweep is suppressed while the live gem-grading path is confirming the CURRENT node (`needParentGemGrade`).
    - A swept ply's gem badge shows in the move list AND on the board corner when the cursor reaches it; the live per-node path does not re-grade a ply the sweep already resolved.
    - Sweep results survive heavy free-variation exploration on a long game (they do not share the 256-entry FIFO budget).
  </behavior>
  <action>
    **Hoist `evalChartReady`.** Move its declaration from ~line 2214 up to just after the `useLibraryGame` fetch (it depends only on `isGameMode`, `gameId`, `gameData`). Leave its existing consumers (the eval-chart render, `evalPending`) reading the hoisted const — one declaration, no duplicate. Keep its doc-comment (it explains that this is "the EVAL DATA is ready", not "the game is ready").

    **D-03 (the transition, not the mount).** Add `const sweptGameIdRef = useRef<number | null>(null)` and an effect keyed on `[evalChartReady, gameId]` that sets `sweptGameIdRef.current = gameId` the first time `evalChartReady` is true for that id. Do NOT use a mount-only effect (`useEffect(..., [])`): that fires once while `eval_series` is still null, sees nothing, and never re-checks — which is exactly how the single most likely game to be opened mid-analysis (a bot game the user just played) would be the one that never gets swept. Use the resulting "sweep armed for this gameId" state as part of the sweep's `enabled`.

    **D-04 (candidates).** Add a `useMemo` computing `selectSweepCandidates(gameData.moves, gameData.eval_series, gameData.opening_ply_count, fenAtPly)` where `fenAtPly(i)` returns the root FEN for `i === 0` and `nodes.get(mainLine[i - 1])?.fen ?? null` otherwise. Guard every index access (`noUncheckedIndexedAccess` — narrow, never `@ts-ignore`). Returns `[]` whenever the game is not ready.

    **The sweep call.** `useGemSweep({ enabled: <armed && evalChartReady && gradingEnabled>, sweepKey: gameId, candidates: sweepCandidates, pinnedEloForPly, liveBusy: needParentGemGrade, userColor: gameData?.user_color ?? null })`. `pinnedEloForPly(i)` reuses Task 1's pinned-rung helper against `fenAtPly(i)` — the SAME function, so the sweep and the live path can never classify the same ply at different rungs. `liveBusy` is `needParentGemGrade`: the live gem-grading path is the one thing the sweep must never race.

    **Display merge (Pitfall 4 — do NOT write into `gemByNode`).** In `moveListMarkers`, fold `sweep.gemByPly` in as a SECOND gem source, mapping `plyIndex → mainLine[plyIndex]` to get the nodeId, reusing the existing `addGem` helper and its severity-wins guard. In `boardSquareMarkers`, resolve the current node's gem as `gemByNode.get(currentNodeId) ?? sweep.gemByPly.get(<current mainline ply>)` (live resolution wins if both exist; they agree by construction since both use the same pinned rung). The sweep's map is the hook's own, bounded to the mainline and never FIFO-evicted — writing sweep results into `gemByNode` would put a move-8 gem at risk of eviction before the user reaches move 60. Comment that.

    **Avoid double work.** Extend `needParentGemGrade` so it is false when the sweep has already resolved the current node's ply (mainline only) — the sticky resolution already exists, there is no reason to re-grade it live.

    Keep the added glue small; if a memo grows past ~40 logic LOC or nesting depth 3, extract it to a module-scope helper in this file.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx && npx tsc -b --noEmit && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useGemSweep' frontend/src/pages/Analysis.tsx` >= 2 (import + call).
    - `grep -c 'selectSweepCandidates' frontend/src/pages/Analysis.tsx` >= 2.
    - `evalChartReady` is declared exactly once: `grep -c 'const evalChartReady' frontend/src/pages/Analysis.tsx` == 1, and its declaration line number is LESS than the line number of the `useGemSweep(` call.
    - The sweep-start effect depends on the readiness value, not on mount: the effect's dependency array contains `evalChartReady` and `gameId` (`grep -B 2 -A 8 'sweptGameIdRef' frontend/src/pages/Analysis.tsx` shows `[evalChartReady, gameId]`).
    - The sweep does NOT write into the FIFO caches: `grep -c 'setGemByNode' frontend/src/pages/Analysis.tsx` == 1 (only the pre-existing live C2 effect writes it).
    - `liveBusy: needParentGemGrade` is passed to `useGemSweep`.
    - `npx tsc -b --noEmit`, `npm run lint`, `npm run knip` clean.
  </acceptance_criteria>
  <done>The sweep runs on the readiness transition, feeds on the free prefilter, and its results reach both display surfaces without touching the shared FIFO caches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Page-level proof — instance isolation, the readiness transition, and the lazy path</name>
  <files>frontend/src/pages/__tests__/Analysis.test.tsx</files>
  <read_first>
    - `frontend/src/pages/__tests__/Analysis.test.tsx` — the whole file (1243 lines). It ALREADY mocks `useStockfishGradingEngine` / `useMaiaEngine` / `useStockfishEngine` module-wide, and it already contains a case for the rj5 "card flips from unanalyzed to analyzed" transition. Mirror that case's structure for SC7 rather than inventing a new harness.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-VALIDATION.md` § Half-Invariant Risk and § Manual-Only Verifications.
    - `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-RESEARCH.md` § Common Pitfalls 1 — the precise mechanism a shared instance would break.
  </read_first>
  <behavior>
    - **SC2 / instance isolation (LOAD-BEARING).** Render the page on an analyzed game so the sweep is running, then put the cursor on a node whose arrival move needs a live gem grade. Assert, by inspecting the recorded options of every `useStockfishGradingEngine` and `useMaiaEngine` call in the render: (a) MORE THAN ONE enabled instance of each exists; (b) the LIVE grading instance's `fen` is the live parent FEN — never a sweep candidate's FEN; (c) the LIVE Maia instance's `fen` is the current board position — never a sweep candidate's FEN. If the sweep were driven through the live instances, the live instance's `fen` would take a sweep FEN and this assertion fails.
    - **SC7 / the transition.** Mount with a card whose `eval_series` is null and `active_eval_status: 'leased'` (a bot game mid-tier-1-analysis). Assert no sweep work is dispatched. Then update the query data so `eval_series` / `flaw_markers` / `phase_transitions` / `moves` are populated (mirroring the existing rj5 transition test), and assert the sweep now dispatches — with no remount and no navigation.
    - **SC4 / lazy path.** A card with `analysis_state === 'no_engine_analysis'` and `active_eval_status: null` never dispatches sweep work, and the existing Analyze pill still renders.
    - **SC3 / slider independence at the page level.** With a resolved gem visible, changing `selectedElo` via the ELO selector does not remove the gem badge and does not change its stamped rung.
  </behavior>
  <action>
    Extend `frontend/src/pages/__tests__/Analysis.test.tsx` with the four cases in `<behavior>`. Upgrade the existing `useStockfishGradingEngine` / `useMaiaEngine` module mocks so each call's options object is pushed into a per-hook array (reset in `beforeEach`), which is what makes "which instance got which FEN" observable — this is the entire mechanism by which the isolation invariant is checkable.

    Label the isolation case explicitly as the D-05 / SC2 proof and note in a comment that `tsc`, `eslint`, `knip` and every pre-existing test pass a sweep that shares a hook instance with the live path — this test is the only structural guard.

    **Revert proof (mandatory, recorded in the SUMMARY):** temporarily rewire the sweep to drive the LIVE `gemGrading` / `maia` instances (i.e. pass a sweep candidate FEN into the existing `useStockfishGradingEngine` / `useMaiaEngine` calls instead of `useGemSweep`'s own), run this file, confirm the isolation case goes RED. Restore, confirm GREEN. Paste both outputs into the SUMMARY. Grep or symbol presence is NOT acceptable evidence for SC2.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx && npx tsc -b --noEmit && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `Analysis.test.tsx` contains a case asserting the LIVE grading instance's `fen` is never a sweep candidate FEN while the sweep is active.
    - `Analysis.test.tsx` contains a case asserting the LIVE Maia instance's `fen` is never a sweep candidate FEN while the sweep is active.
    - `Analysis.test.tsx` contains a case where `eval_series` transitions null → populated on an already-mounted page and the sweep dispatches only after the transition.
    - `Analysis.test.tsx` contains a case where an unanalyzed, unqueued card dispatches no sweep work and still renders the Analyze pill.
    - `Analysis.test.tsx` contains a case where changing `selectedElo` leaves a resolved gem badge and its stamped rung unchanged.
    - The SUMMARY contains the RED output (instances shared) and the GREEN output (instances dedicated) for the isolation case.
    - `npm test -- --run src/pages/__tests__/Analysis.test.tsx` green.
  </acceptance_criteria>
  <done>The yield-to-cursor invariant is proven at the layer where it would actually be violated, and the bot-game transition, the lazy path, and slider independence all have regression guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is page wiring. It adds no network call, no auth change, no user input, no persistence, and no new dependency. `opening_ply_count` arrives on a response body that already crosses an existing, already-authorized boundary (plan 01's T-172-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-10 | Denial of Service (self-DoS: the live position starved by background engine work) | `useGemSweep` wiring in `Analysis.tsx` | high | mitigate | The sweep is given its own worker instances (plan 04), is armed only on the analysis-readiness transition, and is suppressed while `needParentGemGrade` is true. The mitigation is verified — not assumed — by Task 3's red-then-green instance-isolation test, which fails if the sweep is routed through the live hook instances. |
| T-172-11 | Denial of Service (unbounded background work on a long game) | `sweepCandidates` memo | medium | mitigate | Candidate count is bounded by the mainline length and cut hard by the free prefilter (played === best_move AND out of book), which eliminates the large majority of plies with zero engine work. Downstream, `useGemSweep` caps concurrency at one candidate in flight and gates off on low-power devices. |
| T-172-12 | Spoofing (of signal) | Marker precedence at the `boardSquareMarkers` / `moveListMarkers` call sites | low | mitigate | `boardMarkers.tsx` does NOT enforce precedence internally — it relies on construction-time exclusivity at these call sites. A book marker appended over a blunder square would suppress a real warning. Mitigated by guarding the book append on the absence of BOTH severity and gem, and by plan 03's `severity + book` render tests. |

No package installs in this plan. This phase adds no new network surface, no auth change, no user input, and no persistence — the register is short because that is the honest surface.
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx` green, including the red-then-green isolation proof recorded in the SUMMARY.
- Full frontend gate: `npm run lint && npm run knip && npx tsc -b --noEmit && npm test -- --run`.
- Full backend gate (phase-wide, since plan 01 touched it): `uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -n auto -x`.
- **Structural blindness callout (repeat, deliberately):** `tsc`, `eslint`, `knip`, and every test written before this phase will ALL pass a sweep that shares a worker or hook instance with the live path. The invariant is invisible to every automated gate except the two contention tests (plan 04 Task 3 at the hook layer, plan 05 Task 3 at the page layer). Neither may be waived, and neither may be satisfied by grep.
- **Manual UAT (from VALIDATION.md § Manual-Only Verifications — perceived latency is not unit-testable):**
  1. Open a real, already-analyzed game; step briskly forward through the mainline. Gem and book badges must already be rendered on plies AHEAD of the cursor, and stepping must not feel slower than today.
  2. Play a bot game, open it from the Library while its tier-1 analysis is still running, wait for the evals to land, and confirm the sweep starts with no reload.
  3. Judge absolute gem frequency on real games at the raised 0.20 threshold (deliberately not measured pre-ship — the Phase 165 TSV is an enriched sample, only its ratios transfer).
  4. Open a long game (100+ plies), explore several sidelines, step to the end, scroll back, and confirm an early gem badge is STILL present (the Pitfall-4 eviction check).
  5. Move the ELO slider on a game with a visible gem: the gem must NOT appear/disappear (D-01 behavior change), while the Maia chart and WDL bar must still respond.
</verification>

<success_criteria>
All seven ROADMAP success criteria are now observable in the product:
- SC1 cascade (free → cheap → expensive) fills badges ahead of the cursor.
- SC2 yield-to-cursor, proven by contention tests at two layers.
- SC3 gem rung pinned to the mover, not the slider.
- SC4 unanalyzed games stay lazy with the Analyze pill.
- SC5 `GEM_MAIA_MAX_PROB` = 0.2 (plan 02) applied by the sweep's C1 tier.
- SC6 `opening_ply_count` (plan 01) gates the sweep and paints book markers on both surfaces with `severity > gem > book`.
- SC7 a bot game opened mid-tier-1-analysis is swept the moment the evals land.
Locked decisions D-01-D-08 are all implemented.
</success_criteria>

<output>
Create `.planning/phases/172-background-gem-sweep-on-analysis-seed-106/172-05-SUMMARY.md` when done. It MUST include the red-then-green revert evidence for the instance-isolation case, and it MUST list the manual UAT steps still outstanding.
</output>
