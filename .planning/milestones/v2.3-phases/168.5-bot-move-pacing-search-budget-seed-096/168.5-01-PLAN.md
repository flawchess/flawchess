---
phase: 168.5-bot-move-pacing-search-budget-seed-096
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/seeds/SEED-091-flawchess-bot-play-milestone.md
autonomous: true
requirements: [SC1]

must_haves:
  truths:
    - "ROADMAP.md Phase 169 SC1 reads the D-04 amended sentence (fixed shipped budget, randomized reveal delay floor, fraction-of-remaining synthetic clock debit, bot never flags)."
    - "The clock/fixed-strength fork (SEED-091 deferred question) is recorded as DECIDED = fixed strength + synthetic bot clock (D-01), not left open."
  artifacts:
    - ".planning/ROADMAP.md (Phase 169 SC1 amended)"
    - ".planning/seeds/SEED-091-flawchess-bot-play-milestone.md (fork resolution recorded)"
  key_links:
    - "Phase 169 SC1 wording is the contract Phase 169's useBotGame implements — it must match D-01..D-04b exactly."
---

<objective>
Settle SEED-091's deferred clock/fixed-strength fork and amend Phase 169's SC1 pacing sentence to match the locked decision. This is SC1 of the phase and is a pure documentation edit — no code changes.

Purpose: The clock fork is the upstream decision the whole phase sits behind (SEED-096 sequencing step 1). Recording it and rewording Phase 169 SC1 stops Phase 169 from implementing a clock-derived search budget that D-01 explicitly rejects.
Output: ROADMAP.md Phase 169 SC1 rewritten to D-04's sentence; SEED-091's open fork marked resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md
</context>

<artifacts_produced>
This plan produces NO new code symbols. It edits prose only:
- ROADMAP.md Phase 169 SC1 bullet (reworded)
- SEED-091 "Deferred / uncertain" clock-fork resolution note (added)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Amend Phase 169 SC1 in ROADMAP.md to the D-04 sentence</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 169 section, lines ~138-152 — the current SC1 bullet)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md (D-01..D-04b, the exact D-04 amended-sentence text)
  </read_first>
  <action>
    In ROADMAP.md's `### Phase 169: Clocked Board + Game Loop (useBotGame)` block, replace the SC1 bullet's second clause. The current SC1 ends with "...the bot paces its replies (not instant) with a think-time budget derived from its remaining clock." Replace the "with a think-time budget derived from its remaining clock" clause (which contradicts D-01) with D-04's locked wording: the bot paces its replies (not instant); search runs at the fixed shipped budget regardless of clock; a small randomized reveal delay floors fast moves; and the bot's clock is debited a fraction-of-remaining synthetic think time (the bot never flags). Keep the first clause (live board, dual Fischer-increment clocks, drag/click, turn-gated to legal moves) unchanged. Do not touch Phase 169 SC2-SC5. Cite D-04 inline in the edit (e.g. a trailing "(168.5 D-04)").
  </action>
  <verify>
    <automated>grep -q "fixed shipped budget" .planning/ROADMAP.md && grep -q "never flags\|bot never flags" .planning/ROADMAP.md && ! grep -q "think-time budget derived from its remaining clock" .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <done>Phase 169 SC1 contains the D-04 sentence (fixed shipped budget + randomized reveal delay + fraction-of-remaining synthetic debit + never flags); the old "derived from its remaining clock" clause is gone; SC2-SC5 unchanged.</done>
  <acceptance_criteria>
    - `grep "fixed shipped budget" .planning/ROADMAP.md` matches within the Phase 169 block.
    - `grep "think-time budget derived from its remaining clock" .planning/ROADMAP.md` returns no match (old wording removed).
    - The D-04 decision id is cited at the edit site.
    - Phase 169 SC2, SC3, SC4, SC5 bullets are byte-identical to before.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record the fork resolution in SEED-091</name>
  <files>.planning/seeds/SEED-091-flawchess-bot-play-milestone.md</files>
  <read_first>
    - .planning/seeds/SEED-091-flawchess-bot-play-milestone.md (the "Deferred / uncertain" / clock-fork section)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md (D-01, D-02, D-03, D-04b)
  </read_first>
  <action>
    Append a short "Resolved 2026-07-12 (Phase 168.5)" note to SEED-091 at the clock-fork / think-time-pacing deferred item: the fork is DECIDED as fixed strength + synthetic bot clock (D-01) — one global search budget maps to one calibrated ELO per config; the bot's search never reads the clock; the displayed bot clock is debited a fraction-of-remaining synthetic think time (D-02) so the bot cannot flag; a small TC-independent randomized reveal delay floors near-instant moves (D-03); pacing theater is UX-only and absent from the harness (D-04b). Reference Phase 168.5 CONTEXT.md as the decision record. Do not delete the original deferred prose — annotate it as resolved so the history stays legible.
  </action>
  <verify>
    <automated>grep -qi "Resolved.*168.5\|Phase 168.5" .planning/seeds/SEED-091-flawchess-bot-play-milestone.md && grep -qi "fixed strength" .planning/seeds/SEED-091-flawchess-bot-play-milestone.md && echo PASS</automated>
  </verify>
  <done>SEED-091 records the clock fork as resolved (fixed strength + synthetic clock, D-01/D-02/D-03/D-04b), pointing at Phase 168.5 CONTEXT.md.</done>
  <acceptance_criteria>
    - SEED-091 contains a resolution note naming Phase 168.5 and "fixed strength".
    - The original deferred wording is annotated, not deleted.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edits to planning markdown. No code, no runtime, no user input, no network or persistence surface crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168.5-01-01 | Tampering | .planning docs | low | accept | Git-tracked markdown; any incorrect edit is reviewable in the diff and revertible. No attacker-reachable surface. |

No package installs in this plan — supply-chain (T-*-SC) not applicable.
</threat_model>

<verification>
- ROADMAP.md Phase 169 SC1 matches the D-04 sentence; old clock-derived-budget clause removed.
- SEED-091 fork resolution recorded.
- No source files changed (frontend/backend build gates not applicable to this doc-only plan).
</verification>

<success_criteria>
Phase SC1 is satisfied: the clock/fixed-strength fork is decided and recorded, and Phase 169's SC1 pacing wording is amended to match D-04.
</success_criteria>

<output>
Create `.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-01-SUMMARY.md` when done.
</output>
