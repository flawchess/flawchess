---
phase: 168.5-bot-move-pacing-search-budget-seed-096
plan: 05
type: execute
wave: 3
depends_on: [168.5-04]
files_modified:
  - scripts/calibration-harness.mjs
  - scripts/lib/calibration-anchors.mjs
  - scripts/lib/calibration-determinism.check.mjs
  - scripts/lib/calibration-pruning.check.mjs
autonomous: true
requirements: [SC4, SC5]

must_haves:
  truths:
    - "The sweep prunes anchors two ways (static ELO-window bracketing + ascending-order dynamic cutoff after a 0% / 100% result), and every skipped matchup is a real TSV row with a skip_reason — no silent coverage gaps (D-15)."
    - "A D-15-pruned run still round-trips through --resume (the grid-membership invariant holds because skipped anchors remain rows in gridKeys/the TSV, Pitfall 4)."
    - "The D-09 determinism check passes at the NEW bot budget + stop rule (SC5): the same seed reproduces a byte-identical game."
    - "The full ~24h re-calibration sweep is documented as an operator-launched, out-of-band step (D-16) — no executor task blocks on it; a bounded pilot confirms the blend>0 per-move median lands in the D-17 3-8s band."
    - "The operator runbook specifies the D-14 coarse grid exactly: ELO {1100, 1500, 1900} × blend {0, 0.5, 1} × trimmed anchor set (Maia rungs bracketing each cell; Stockfish-skill anchors droppable) × ~10 games/cell — the FIRST real strength map (168-03 never launched the grid run) (D-14)."
  artifacts:
    - "scripts/calibration-harness.mjs (anchor-ordered pruning loop + skip_reason TSV column)"
    - "scripts/lib/calibration-anchors.mjs (rating-ordered anchor helper, if extracted)"
    - "scripts/lib/calibration-determinism.check.mjs (re-run at the bot budget + stop rule)"
    - "scripts/lib/calibration-pruning.check.mjs (skip-log + --resume round-trip check)"
    - "Operator sweep-launch runbook + D-17 pilot result recorded in the SUMMARY"
  key_links:
    - "Anchor ordering uses the existing SF_SKILL_ELO / MAIA_ELO_LADDER rating tables — no hand-rolled rating lookup (Don't Hand-Roll)."
    - "The determinism check must construct its playGame budget with stopRule + the bot maxNodes/concurrency, or SC5 proves determinism of the wrong bot."
---

<objective>
Harden the sweep for a realistic run (D-15 anchor pruning with logged skips), prove determinism at the shipped budget (SC5), and hand off the full ~24h re-calibration sweep as an operator step (SC4/D-16). A bounded pilot confirms the pacing target (D-17) before the operator launches the long run.

Purpose: The full grid at the old budget was a multi-month run; pruning + the smaller bot budget make a coarse strength map fit the ~24h envelope. The executor must NOT block on that run (D-16), so this plan ships the pruning code + the determinism gate + a pilot validation and documents the operator launch. SC4's strength-map artifact comes from the operator run, reviewed post-hoc; the map documents real strength at the shipped budget (D-18 — no in-phase retune loop).
Output: Anchor-pruning grid loop with skip_reason rows, a passing D-09 check at the new budget, a D-17-passing pilot, and an operator runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-RESEARCH.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-PATTERNS.md
@scripts/calibration-harness.mjs
@scripts/lib/calibration-anchors.mjs
@scripts/lib/calibration-determinism.check.mjs
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-04-SUMMARY.md
</context>

<artifacts_produced>
New symbols / changes this plan creates (exclude from drift verification):
- `scripts/calibration-harness.mjs`: anchor-ordered pruning in the grid loop (static ELO-window + dynamic ascending-order cutoff); new TSV column `skip_reason`; new constants `ANCHOR_ELO_WINDOW`, `DYNAMIC_CUTOFF_SCORE_EPS`; skipped anchors emitted as `games=0` rows.
- `scripts/lib/calibration-anchors.mjs`: a rating-ordered anchor helper (if extracted) using SF_SKILL_ELO / MAIA_ELO_LADDER.
- `scripts/lib/calibration-determinism.check.mjs`: budget updated to the bot budget + stopRule.
- `scripts/lib/calibration-pruning.check.mjs`: new standalone assert-script for skip-log + --resume round-trip.
- An operator sweep-launch runbook (in the SUMMARY / a notes file), not a code artifact.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Anchor pruning (static window + dynamic cutoff) with logged skip_reason rows</name>
  <files>scripts/calibration-harness.mjs, scripts/lib/calibration-anchors.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (main() grid loop lines ~895-960 where playCell is called per cell; gridKeys enumeration lines ~253-262; mainTsvColumns/mainTsvRowLine schema lines ~533-566; the --resume grid guard "is not in the current grid" line ~186)
    - scripts/lib/calibration-anchors.mjs (SF_SKILL_ELO line ~37; MAIA_ELO_LADDER import — the rating tables to order anchors by)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-RESEARCH.md (Pitfall 4 — the durability/--resume invariant D-15 must not break; Open Question 3 — skip_reason as a full TSV row)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-PATTERNS.md (calibration-harness.mjs section — gridKeys, playCell, anchor rating table)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md (D-15 both mechanisms; "no silent coverage gaps"; window / cutoff eps are named constants)
  </read_first>
  <action>
    Implement D-15's two pruning mechanisms in main()'s grid loop WITHOUT shrinking gridKeys at runtime (Pitfall 4). Add a skip_reason column to the main TSV schema (mainTsvColumns + mainTsvRowLine), empty for played cells. Static bracketing: per bot cell, sort that cell's anchors ascending by known rating (raw-Maia rung ELO via MAIA_ELO_LADDER; Stockfish skill via SF_SKILL_ELO — reuse those tables, do not hand-roll a rating map); skip any anchor whose rating is outside ANCHOR_ELO_WINDOW of the bot cell's nominal ELO, writing a games=0 row with skip_reason set to the out-of-window marker. Dynamic cutoff: iterate the in-window anchors in ascending strength; after the bot scores near 0 (within DYNAMIC_CUTOFF_SCORE_EPS of 0) against an anchor, skip all STRONGER remaining anchors (skip_reason = lost-cutoff marker); symmetrically after a near-100% sweep, skip all WEAKER anchors (skip_reason = won-cutoff marker). Every skipped anchor still gets a real TSV row (games=0) so gridKeys membership and --resume both stay intact. Add ANCHOR_ELO_WINDOW and DYNAMIC_CUTOFF_SCORE_EPS as named constants with D-15 cites. Preserve the global gameIndex discipline: a skipped cell plays no games, so it must consume zero gameIndex slices on BOTH fresh and resumed runs — confirm the resume replay stays byte-identical.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && grep -q "skip_reason" scripts/calibration-harness.mjs && grep -q "ANCHOR_ELO_WINDOW" scripts/calibration-harness.mjs && grep -q "DYNAMIC_CUTOFF_SCORE_EPS" scripts/calibration-harness.mjs && node --check scripts/calibration-harness.mjs && echo PASS</automated>
  </verify>
  <done>Both pruning mechanisms are implemented against the existing rating tables; skipped anchors are games=0 rows with a populated skip_reason; gridKeys is not shrunk at runtime; ANCHOR_ELO_WINDOW / DYNAMIC_CUTOFF_SCORE_EPS are cited named constants; node --check passes.</done>
  <acceptance_criteria>
    - skip_reason is a column in mainTsvColumns and rendered by mainTsvRowLine.
    - ANCHOR_ELO_WINDOW and DYNAMIC_CUTOFF_SCORE_EPS are named constants with D-15 cites.
    - Anchor ordering references SF_SKILL_ELO / MAIA_ELO_LADDER (no hand-rolled rating map).
    - gridKeys is still built from the full cross-product (not shrunk mid-run).
    - node --check scripts/calibration-harness.mjs exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: --resume round-trips after a pruned run + skip_reason coverage check</name>
  <files>scripts/lib/calibration-pruning.check.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (loadPriorSweep lines ~162-195 — grid-membership + budget guards; cellKey; the append path)
    - scripts/lib/calibration-determinism.check.mjs (the standalone .check.mjs assert-script pattern to mirror)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-RESEARCH.md (Wave 0 gap: a new check verifying skip-log populates + --resume round-trips post-pruning)
  </read_first>
  <action>
    Add scripts/lib/calibration-pruning.check.mjs (a standalone assert-script mirroring the existing .check.mjs style). It verifies, using a small synthesized TSV fixture (no real engines needed for the round-trip portion): (a) every gridKey has a row and the pruned/out-of-window/dynamic-cutoff rows carry a non-empty skip_reason (no silent gaps); (b) feeding that TSV back through loadPriorSweep with the same grid + budget does NOT throw the "is not in the current grid" or budget-mismatch errors (the grid-membership invariant survives pruning). If loadPriorSweep is not exported, export it (additive) or replicate its guard logic against the fixture. Register the exact run command in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && node --check scripts/lib/calibration-pruning.check.mjs && OUT=$(node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-pruning.check.mjs 2>&1) && echo "$OUT" | tail -15</automated>
  </verify>
  <done>A new check asserts skip_reason coverage and that a pruned TSV round-trips through --resume without a grid-membership/budget error; it passes.</done>
  <acceptance_criteria>
    - scripts/lib/calibration-pruning.check.mjs exists and runs to a passing assertion.
    - The check asserts every gridKey has a row and skipped rows have a non-empty skip_reason.
    - The check asserts loadPriorSweep accepts the pruned TSV without throwing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SC5 determinism check at the new budget + operator sweep runbook + D-17 pilot</name>
  <files>scripts/lib/calibration-determinism.check.mjs</files>
  <read_first>
    - scripts/lib/calibration-determinism.check.mjs (DETERMINISM_MAX_NODES=20 / _MAX_PLIES=8 overrides lines ~64-65; both playGame calls lines ~80-108 — they pass maxNodes/maxPlies but NOT stopRule/concurrency)
    - scripts/calibration-harness.mjs (the FLAWCHESS_BOT_* mirror + playGame stopRule/concurrency wiring landed in Plan 04)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md (D-13 on-demand not CI; D-16 ~24h operator-launched, --resume + retry loop; D-17 pass band median 3-8s, p95<=20s; D-18 accept & record)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-04-SUMMARY.md (locked bot budget values)
  </read_first>
  <action>
    Update calibration-determinism.check.mjs so both playGame invocations construct the budget at the SHIPPED bot budget + stop rule: use FLAWCHESS_BOT_MAX_NODES / FLAWCHESS_BOT_MAX_PLIES and pass stopRule (FLAWCHESS_BOT_STOP_RULE) + the pinned FLAWCHESS_BOT_CONCURRENCY, replacing the old DETERMINISM_MAX_NODES=20 override (or set the override equal to the bot budget with a cite) so SC5 proves determinism of the bot that ships. Run the check (same seed must reproduce a byte-identical blend=1 game) and record the result in the SUMMARY. Then run a bounded pilot (a handful of blend>0 bot moves on the reference box) to confirm the per-move median lands in the D-17 3-8s band and p95<=20s — record the numbers. Finally, write an operator runbook in the SUMMARY (and/or a short .planning/notes file) for the full D-16 sweep: the exact harness command with the coarse grid (ELO {1100,1500,1900} x blend {0,0.5,1} x trimmed bracketing anchors x ~10 games/cell), --resume usage, the retry loop, the ~24h envelope, and the note that this run is OPERATOR-launched and out-of-band — no executor task blocks on it (D-16), and its strength map is accepted-and-recorded per D-18. Do NOT launch the full grid from this plan.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && grep -q "FLAWCHESS_BOT_MAX_NODES\|FLAWCHESS_BOT_STOP_RULE" scripts/lib/calibration-determinism.check.mjs && node --check scripts/lib/calibration-determinism.check.mjs && OUT=$(node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs 2>&1) && echo "$OUT" | tail -15</automated>
  </verify>
  <done>The determinism check runs at the shipped bot budget + stop rule and passes (byte-identical replay); a bounded pilot confirms the D-17 3-8s median / p95<=20s band; an operator runbook for the full D-16 sweep is recorded; the full grid is NOT launched here.</done>
  <acceptance_criteria>
    - calibration-determinism.check.mjs constructs its budget from FLAWCHESS_BOT_* + stopRule (not the old 20-node override).
    - The determinism check passes (same seed reproduces a byte-identical game) — result recorded in the SUMMARY.
    - The SUMMARY records the pilot per-move median (in 3-8s) and p95 (<=20s), or documents a measured deviation with the D-18 accept-and-record disposition.
    - The SUMMARY contains the operator runbook (exact command + grid + --resume + envelope) and states the sweep is operator-launched / out-of-band (D-16).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → local Node harness → child Stockfish/Maia processes | Operator-run, local-only calibration sweep. No network, no untrusted input; grid inputs come from validated CLI flags and the harness's own opening book. The out-of-band ~24h run is operator-launched, not agent-driven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168.5-05-01 | Repudiation | silent coverage gaps from pruning | high | mitigate | Every skipped anchor is a games=0 TSV row with a populated skip_reason; the pruning check asserts every gridKey has a row (D-15 "no silent gaps"). |
| T-168.5-05-02 | Tampering | --resume grid invariant broken by runtime pruning | high | mitigate | gridKeys is built from the full cross-product and never shrunk at runtime; skipped anchors remain rows; the pruning check asserts loadPriorSweep round-trips (Pitfall 4). |
| T-168.5-05-03 | Tampering | SC5 proving determinism of the wrong bot | medium | mitigate | The determinism check is updated to construct the budget from FLAWCHESS_BOT_* + stopRule so it proves the shipped bot's determinism (grep gate + acceptance criteria). |
| T-168.5-05-04 | Denial of Service | executor blocked on the ~24h sweep | medium | mitigate | The full grid is operator-launched and out-of-band (D-16); the plan ships pruning + a bounded pilot only, never the long run. |

No package installs in this plan — supply-chain (T-*-SC) not applicable.
</threat_model>

<verification>
- node --check passes on all edited harness files.
- Pruning: skip_reason column + both mechanisms + named constants present; gridKeys not shrunk.
- Pruning check + determinism check both run to passing assertions on demand.
- SC5 determinism check constructs the bot budget + stopRule and reproduces a byte-identical game.
- D-17 pilot band recorded; operator runbook recorded; full grid NOT launched.
</verification>

<success_criteria>
Phase SC4 (anchor-pruned sweep code + operator handoff + D-17 pilot) and SC5 (D-09 determinism check passes at the new budget + stop rule) are satisfied. The full strength map is produced by the operator-launched out-of-band sweep (D-16), accepted-and-recorded per D-18.
</success_criteria>

<output>
Create `.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-05-SUMMARY.md` when done (record the determinism-check result, the D-17 pilot numbers, and the operator sweep runbook).
</output>
