---
phase: 168.5-bot-move-pacing-search-budget-seed-096
fixed_at: 2026-07-12T14:50:00Z
review_path: .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-REVIEW.md
iteration: 1
fix_scope: critical_warning
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 168.5: Code Review Fix Report

**Fixed at:** 2026-07-12T14:50:00Z
**Source review:** .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (WR-01..WR-05; the 8 IN-* items are out of scope for `critical_warning`)
- Fixed: 5
- Skipped: 0

All fixes were applied in an isolated worktree and fast-forwarded onto the
phase branch. Phase invariants preserved: `FLAWCHESS_ENGINE_MAX_NODES=400` /
`FLAWCHESS_ENGINE_MAX_PLIES=8` untouched in `useFlawChessEngine.ts`; the bot
budget (50 nodes / 8 plies / 4 top moves) and all four stop-rule thresholds
remain byte-equal in value; no wall-clock API entered `mctsSearch.ts`.

## Fixed Issues

### WR-01: `--resume` with a non-`.tsv` path silently overwrites the main results TSV

**Files modified:** `scripts/calibration-harness.mjs`
**Commit:** 3d6c93e5
**Applied fix:** `parseArgs`'s `case 'resume'` now rejects any resume path not
ending in `.tsv` (with a comment explaining that the summary sibling path is
derived by extension replace, so a non-.tsv path would make `emitEloSummary`
truncate the results file itself).
**Verification:** `node --check scripts/calibration-harness.mjs` clean.

### WR-02: `setupHarnessEngines` leaks the spawned Stockfish pool when a later bring-up step throws

**Files modified:** `scripts/calibration-harness.mjs`
**Commit:** 4e015ec6
**Applied fix:** Wrapped the post-pool bring-up steps (`resolveFrontendModule('chess.js')`,
`makeNodeProviders`) in try/catch with `pool.quitAll()` on failure, with a
comment referencing the file's existing CR-01/CR-02 seam pattern.
**Verification:** `node --check` clean.

### WR-03: stop-rule block nests 6 levels deep (hard limit 4)

**Files modified:** `frontend/src/lib/engine/mctsSearch.ts`
**Commit:** 1ede6196
**Applied fix:** Extracted a `StopRuleState` interface + pure `stopRuleSatisfied(root, rule, nodesEvaluated, state)`
helper (placed beside `rootChildValueExtremes`); the loop body now reads
`if (budget.stopRule && stopRuleSatisfied(...)) earlyStop = true;`.
Behavior-identical: same stability-counter update semantics (no-op reassignment
on equality, reset-to-1 on change), same min-nodes/stability-window gating, same
clear-winner/near-tie disjunction, no wall-clock APIs. Nesting inside `mctsSearch`
drops to 3 at the call site.
**Verification:** `npx tsc -b` clean; vitest `mctsSearch.test.ts` +
`selectBotMove.test.ts` + `useFlawChessEngine.test.tsx` — 42/42 passed
(includes the omitted-vs-explicitly-undefined stopRule byte-identity test).

### WR-04: no drift guard between the hook's `FLAWCHESS_BOT_*` constants and the harness mirror block

**Files modified:** `frontend/src/lib/engine/botBudget.ts` (new),
`frontend/src/hooks/useFlawChessEngine.ts`, `scripts/calibration-harness.mjs`
**Commit:** 2d520f59
**Applied fix:** Took the review's preferred fix: created dependency-light
`frontend/src/lib/engine/botBudget.ts` (types-only import) holding the four
`FLAWCHESS_BOT_*` constants with the 168.5-04 measurement doc.
`useFlawChessEngine.ts` re-exports them for app callers (Phase 169's
`useBotGame`); the harness imports them via its `@/` alias hook and re-exports
so `calibration-determinism.check.mjs` / `calibration-pruning.check.mjs` keep
importing from the harness module. The hand-maintained mirror block was
deleted. Values byte-equal to before (50/8/4 + {0.05, 0.02, 3, 8}).
**Verification:** `node --check` clean; `npx tsc -b` clean; full hook + engine
vitest set 175/175 passed; `npm run knip` clean (no dead exports); the
structural pruning check (engine-free) ran green end-to-end through the alias
import chain.

### WR-05: pruning check's coverage assertion was tautological

**Files modified:** `scripts/lib/calibration-pruning.check.mjs`
**Commit:** b5e85f75
**Applied fix:** `gridKeys` is now derived from a standalone `FIXTURE_ANCHORS`
axis constant (mirroring how the real sweep builds its grid from CLI axes, not
from emitted rows), and coverage is asserted exactly in both directions
(every gridKey has a row; every row's key is in the grid; row keys unique).
PASS message reworded to state the genuine claim.
**Verification:** `node --check` clean; check executed green
(`PASS: skip_reason coverage — rows cover the axis-derived gridKeys exactly (4/4)`
+ the untouched round-trip PASS).

## Skipped Issues

None.

---

_Fixed: 2026-07-12T14:50:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
