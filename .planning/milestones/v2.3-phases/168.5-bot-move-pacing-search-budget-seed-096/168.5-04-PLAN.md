---
phase: 168.5-bot-move-pacing-search-budget-seed-096
plan: 04
type: execute
wave: 2
depends_on: [168.5-02, 168.5-03]
files_modified:
  - frontend/src/hooks/useFlawChessEngine.ts
  - scripts/calibration-harness.mjs
autonomous: true
requirements: [SC3, SC4]

must_haves:
  truths:
    - "FLAWCHESS_BOT_MAX_NODES and the stop-rule thresholds are LOCKED from a bounded measurement that genuinely exercises the early-stop dynamics (a standalone probe builds a SearchBudget carrying stopRule directly, since playGame is not stop-rule-wired until Task 2) so a contested blend>0 move stays near the ~15s worst case and the median trends toward ~5s (D-07)."
    - "The harness mirrors the FLAWCHESS_BOT_* constants (comment-mirror convention, cited file+line) so the sweep measures the exact shipped bot — app == harness holds (D-11/CAL-02)."
    - "The harness bot move is played with the bot budget: retuned maxNodes, the stopRule, and the pinned bot concurrency (D-09) — NOT the old 400-node analysis budget."
  artifacts:
    - "frontend/src/hooks/useFlawChessEngine.ts (final locked FLAWCHESS_BOT_* values)"
    - "scripts/calibration-harness.mjs (FLAWCHESS_BOT_* mirror block + playGame SearchBudget wiring)"
  key_links:
    - "Task 1's measurement probe must set stopRule on the SearchBudget it hands to selectBotMove ITSELF (playGame is not yet wired for it) — otherwise the locked values are tuned against full-budget-no-stop-rule search and SC3/SC4 targets are invalid."
    - "playGame's SearchBudget must carry stopRule + the pinned concurrency, not just maxNodes/maxPlies, or the sweep measures a bot without the stop rule."
    - "The mirror constants must equal the hook's locked values exactly (a divergence calibrates a bot that does not ship)."
---

<objective>
Lock the bot-play budget from measurement, mirror it into the harness, and wire the harness bot move to actually use it (retuned MAX_NODES + stop rule + pinned concurrency). This completes SC3 (the app==harness half) and prepares SC4's sweep.

Purpose: Plan 03 shipped the mechanism with PROVISIONAL values; SEED-096 mandates "measure, don't trust". With the determinism fix (Plan 02) in place, a bounded harness run gives the real per-move cost so FLAWCHESS_BOT_MAX_NODES + the stop-rule thresholds can be set to hit the ~5s-median / ~15s-worst-case target (D-07). The harness must then measure the SHIPPED bot, so the mirror + playGame wiring are mandatory.
Output: Locked FLAWCHESS_BOT_* values, a mirrored harness constant block, and a playGame SearchBudget carrying the stop rule + pinned concurrency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-RESEARCH.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-PATTERNS.md
@frontend/src/hooks/useFlawChessEngine.ts
@scripts/calibration-harness.mjs
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-03-SUMMARY.md
@.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-02-SUMMARY.md
</context>

<artifacts_produced>
New symbols / changes this plan creates (exclude from drift verification):
- `scripts/calibration-harness.mjs`: mirror constants `FLAWCHESS_BOT_MAX_NODES`, `FLAWCHESS_BOT_MAX_PLIES`, `FLAWCHESS_BOT_CONCURRENCY`, `FLAWCHESS_BOT_STOP_RULE` (comment-mirror of useFlawChessEngine.ts); playGame SearchBudget extended with `stopRule` + pinned `concurrency`.
- `frontend/src/hooks/useFlawChessEngine.ts`: PROVISIONAL markers removed; FLAWCHESS_BOT_* values locked from measurement.
- Locked-value rationale recorded in the plan SUMMARY.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bounded measurement then lock the FLAWCHESS_BOT_* values</name>
  <files>frontend/src/hooks/useFlawChessEngine.ts</files>
  <read_first>
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-02-SUMMARY.md (measured per-expansion cost WITH the Clear-Hash determinism fix — the real cost basis)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-03-SUMMARY.md (provisional values shipped in the hook)
    - frontend/src/hooks/useFlawChessEngine.ts (the FLAWCHESS_BOT_* block to lock; the untouched analysis constants)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md (D-07 target 5s median / 15s worst-case; D-08 same rule at all blends; D-17 pass band 3-8s median, p95<=20s)
    - frontend/src/lib/engine/mctsSearch.ts + frontend/src/lib/engine/selectBotMove.ts (the SearchBudget shape and the entry point the probe calls directly — the SearchBudget's optional `stopRule`/`concurrency`/`maxNodes` fields the probe sets by hand)
    - scripts/calibration-harness.mjs (how it constructs the Stockfish/Maia providers + pool the probe reuses; the per-move moveMs stats shape — the probe records the SAME moveMs number)
  </read_first>
  <action>
    IMPORTANT — ordering/wiring caveat: at this point the harness's `playGame` does NOT yet pass `stopRule`/pinned-concurrency to `selectBotMove` (that wiring is Task 2, and it deliberately mirrors THIS task's locked values). So Task 1 must NOT rely on a plain `calibration-harness.mjs` grid run — that would measure the OLD full-budget-no-stop-rule bot (and possibly the 400-node analysis budget). Instead run a STANDALONE ad-hoc probe (a small throwaway `.mjs` under scripts/, or an inline node script) that constructs the Stockfish/Maia providers + pool exactly as the harness does (reuse its provider/pool helpers), then calls `selectBotMove`/`mctsSearch` on a few blend>0 positions with a `SearchBudget` you build BY HAND carrying `stopRule` (the provisional BotStopRule), `maxNodes` (the provisional bot cap), and the pinned `concurrency` (D-09) — i.e. the probe genuinely exercises the early-stop dynamics BEFORE any constant is locked. Capture per-move moveMs the same way the harness does.
    Use the probe to LOCK: FLAWCHESS_BOT_MAX_NODES so a full-budget contested move stays near the ~15s worst case on the reference box; FLAWCHESS_BOT_STOP_RULE.marginThreshold / epsilonThreshold / stabilityWindow / minNodes so routine and near-tie positions settle fast (median trending to ~5s) while genuinely contested positions run toward the cap — validate the two-sided rule actually separates the two (a near-tie opening must stop early, a sharp middlegame must not); and FLAWCHESS_BOT_CONCURRENCY pinned to the reference-box value the sweep will use (D-09). Edit useFlawChessEngine.ts to replace each provisional value and remove the PROVISIONAL marker, replacing it with a one-line "locked 168.5-04 from measurement" note. Record the measured median / worst-case, the probe method (standalone, hand-built SearchBudget with stopRule), and the chosen values in the SUMMARY. Do NOT change FLAWCHESS_ENGINE_MAX_NODES (400) or FLAWCHESS_ENGINE_MAX_PLIES (8). If you created a throwaway probe script, delete it before finishing (or leave it under scripts/ only if it is genuinely reusable — Claude's discretion).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && ! grep -q "PROVISIONAL" src/hooks/useFlawChessEngine.ts && grep -q "FLAWCHESS_ENGINE_MAX_NODES = 400" src/hooks/useFlawChessEngine.ts && npx tsc -b --noEmit && npx vitest run src/hooks/useFlawChessEngine.test.tsx</automated>
  </verify>
  <done>FLAWCHESS_BOT_* values are locked from a bounded measurement (PROVISIONAL markers removed); analysis constants unchanged; tsc clean; hook test passes; measurement recorded in SUMMARY.</done>
  <acceptance_criteria>
    - `grep "PROVISIONAL" src/hooks/useFlawChessEngine.ts` returns no match.
    - `grep "FLAWCHESS_ENGINE_MAX_NODES = 400" src/hooks/useFlawChessEngine.ts` still matches.
    - The SUMMARY records measured median/worst-case moveMs and the locked constant values with rationale.
    - `npx tsc -b --noEmit` zero errors; hook test passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mirror FLAWCHESS_BOT_* into the harness and wire playGame's budget</name>
  <files>scripts/calibration-harness.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (the D-11 mirror block lines ~85-104; playGame's SearchBudget construction lines ~443-461 where maxNodes/maxPlies/concurrency are passed to selectBotMove)
    - frontend/src/hooks/useFlawChessEngine.ts (the locked FLAWCHESS_BOT_* values from Task 1 — the mirror source of truth)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-PATTERNS.md (calibration-harness.mjs section: the "mirrors X — file line N" comment convention; the playCell/playGame budget shape)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-RESEARCH.md (Pattern 1 mirror-not-import discussion; D-09 pinned concurrency; the "app == harness" discipline)
  </read_first>
  <action>
    In calibration-harness.mjs's mirror block, add `FLAWCHESS_BOT_MAX_NODES`, `FLAWCHESS_BOT_MAX_PLIES`, `FLAWCHESS_BOT_CONCURRENCY`, and `FLAWCHESS_BOT_STOP_RULE` as exported mirror constants, each with the existing "mirrors <NAME> — useFlawChessEngine.ts line <N>" comment convention and values EQUAL to the hook's locked values. Repoint the harness's default bot budget from FLAWCHESS_ENGINE_MAX_NODES/_MAX_PLIES to the FLAWCHESS_BOT_* mirrors: playGame's `maxNodes` default becomes FLAWCHESS_BOT_MAX_NODES, and the SearchBudget it hands to selectBotMove gains `stopRule: FLAWCHESS_BOT_STOP_RULE` and `concurrency: FLAWCHESS_BOT_CONCURRENCY` (the pinned D-09 value) instead of `pool.size`. Keep maxPlies at FLAWCHESS_BOT_MAX_PLIES (8, unchanged). Leave the anchor-move path untouched. The `budgetExhausted`/moveMs TSV recording keeps working; if a per-move stop-reason column is trivially available from the snapshot, adding it is Claude's-discretion (D-15 schema note) but not required here. Do NOT delete the old FLAWCHESS_ENGINE_MAX_* mirror constants if the --resume budget guard (loadPriorSweep) reads them — instead update that guard to compare against the bot budget so a resumed sweep refuses to mix the old 400-node budget with the new bot budget.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && grep -q "FLAWCHESS_BOT_MAX_NODES" scripts/calibration-harness.mjs && grep -q "FLAWCHESS_BOT_STOP_RULE" scripts/calibration-harness.mjs && grep -q "mirrors.*useFlawChessEngine.ts" scripts/calibration-harness.mjs && node --check scripts/calibration-harness.mjs && echo PASS</automated>
  </verify>
  <done>The harness mirrors the locked FLAWCHESS_BOT_* constants with cited comments; playGame constructs a SearchBudget carrying the retuned maxNodes, stopRule, and pinned concurrency; the --resume budget guard checks the bot budget; `node --check` passes.</done>
  <acceptance_criteria>
    - `FLAWCHESS_BOT_MAX_NODES` / `FLAWCHESS_BOT_STOP_RULE` / `FLAWCHESS_BOT_CONCURRENCY` exist as mirror constants with the "mirrors ... useFlawChessEngine.ts line N" comment style.
    - The mirror values equal the hook's locked values (reviewer cross-checks the two files; state the numbers in the SUMMARY).
    - playGame's SearchBudget passes `stopRule` and the pinned `concurrency` (not `pool.size`).
    - The --resume budget-mismatch guard references the bot budget.
    - `node --check scripts/calibration-harness.mjs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-browser bot budget constants → operator Node harness (mirror) | The harness mirrors client constants by value (no import). No network, no untrusted input; the only integrity concern is mirror drift (harness measuring a bot the app does not ship). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168.5-04-01 | Tampering | app==harness constant drift | high | mitigate | Mirror constants carry a cited "mirrors X — file line N" comment; values are cross-checked against the hook and stated in the SUMMARY; the --resume budget guard refuses a mismatched budget. |
| T-168.5-04-02 | Repudiation | sweep measures a bot without the stop rule | high | mitigate | playGame's SearchBudget is asserted to carry `stopRule` + pinned `concurrency`, not just maxNodes/maxPlies (acceptance criteria + grep). |
| T-168.5-04-03 | Input Validation | harness CLI flags | low | accept | No new CLI flags; existing validators cover the probe invocation. |

No package installs in this plan — supply-chain (T-*-SC) not applicable.
</threat_model>

<verification>
- Hook: PROVISIONAL markers gone; analysis constants (400/8) unchanged; tsc clean; hook test passes.
- Harness: bot mirror constants present with cited comments; playGame budget carries stopRule + pinned concurrency; `node --check` passes.
- Mirror values equal the hook's locked values (SUMMARY records both).
</verification>

<success_criteria>
SC3 is fully satisfied (app == harness at the shipped budget: retuned MAX_NODES + stop rule mirrored and wired), and SC4 is prepared (the harness now plays the shipped bot).
</success_criteria>

<output>
Create `.planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-04-SUMMARY.md` when done (record locked values + measurement + the app/harness value cross-check).
</output>
