---
phase: 168.5-bot-move-pacing-search-budget-seed-096
verified: 2026-07-12T14:28:49Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 168.5: Bot Move Pacing & Search Budget (SEED-096) Verification Report

**Phase Goal:** A bot move at any play-style blend returns in seconds, not minutes, via a shipped per-move search budget plus confidence-based early stopping, with the engine's strength re-measured by the harness at exactly the shipped budget (app == harness, D-11).
**Verified:** 2026-07-12T14:28:49Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria SC1-SC5)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | The clock/fixed-strength fork (SEED-091 deferred question) is decided and recorded, and Phase 169's SC1 pacing wording is amended to match the decision. | VERIFIED | `.planning/ROADMAP.md` Phase 169 SC1 reads the D-04 sentence verbatim ("...search runs at the fixed shipped budget regardless of clock, a small randomized reveal delay floors fast moves, and the bot's clock is debited a fraction-of-remaining synthetic think time (the bot never flags) (168.5 D-04)"). `SEED-091-flawchess-bot-play-milestone.md` line 86 explicitly records "Resolved 2026-07-12 (Phase 168.5): the clock/fixed-strength fork is DECIDED as fixed strength + synthetic bot clock (D-01)". Commits `88fd6be6`/`ddbefa37` verified present in git history. |
| SC2 | Harness Stockfish grades are deterministic under load (per-`go` hash reset or depth-capped adjudication) and a single late engine reply no longer kills a sweep (watchdog headroom or retry-in-place). | VERIFIED | `scripts/lib/calibration-providers.mjs`: `nodeGrade`/`evalPositionCp` both send `setoption name Clear Hash` then a depth-only `go depth N` (no `movetime` token) — confirmed by direct grep, no movetime clause remains on either grading path. Independent `GRADING_WATCHDOG_TIMEOUT_MS=60_000`/`ADJUDICATION_WATCHDOG_TIMEOUT_MS=20_000` constants (not movetime-derived). `scripts/lib/stockfish-pool.mjs`: `runWithRetry` retries a `waitFor` timeout via `stopAndSync()` up to `ENGINE_RETRY_ATTEMPTS=2` times, gated by `TIMEOUT_ERROR_PATTERN`, never a bare catch-all. Commits `4abbb064`/`9a871169` verified present. |
| SC3 | `mctsSearch` supports grade-margin (confidence-based) early stopping evaluated in canonical apply order; `MAX_NODES` is retuned from measured per-move cost; both ship in `useFlawChessEngine.ts` and are mirrored into the harness's D-11 constants. | VERIFIED | `mctsSearch.ts:477-508` evaluates the two-sided stop rule (clear-winner margin OR near-tie flatness, gated by a shared min-nodes floor) inside the same canonical `for (const result of results)` apply loop that fires `onSnapshot` — no wall-clock signal present in the file. `useFlawChessEngine.ts` exports `FLAWCHESS_BOT_MAX_NODES=50`/`_MAX_PLIES=8`/`_CONCURRENCY=4`/`_STOP_RULE={marginThreshold:0.05, epsilonThreshold:0.02, stabilityWindow:3, minNodes:8}` additively — the untouched analysis-board `FLAWCHESS_ENGINE_MAX_NODES=400`/`_MAX_PLIES=8`/`computePoolSize()` call site (line 263-265) is byte-identical to before. `scripts/calibration-harness.mjs` mirrors all four constants with cited line numbers (`useFlawChessEngine.ts line 71/74/77/80`) — I independently confirmed the cited line numbers are exact and the mirrored values are byte-identical to the hook's. Ran `npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts src/hooks/useFlawChessEngine.test.tsx` myself: 27/27 pass (clear-winner, near-tie-flatness, min-nodes-floor, undefined-byte-identical-no-op, bot-profile-distinctness all present as real assertions, not stubs). |
| SC4 | A bounded re-calibration sweep at the shipped budget produces a strength map, and the blend>0 per-move median on the reference box lands near the pacing target. Per this task's explicit scope note, the in-phase deliverable is the pruning code + a D-17-passing bounded pilot + the operator runbook — NOT the completed ~24h sweep (D-16, operator-launched out-of-band). | VERIFIED | Pruning code: `scripts/calibration-harness.mjs` implements static ELO-window bracketing (`ANCHOR_ELO_WINDOW=400`) and dynamic ascending-cutoff (`DYNAMIC_CUTOFF_SCORE_EPS=0.05`), every pruned anchor logged as a `games=0` row with a populated `skip_reason` (`out_of_window`/`lost_cutoff`/`won_cutoff`). I ran `scripts/lib/calibration-pruning.check.mjs` myself: **PASS** on both the skip_reason-coverage assertion and the `--resume` round-trip assertion (exit 0). Operator runbook: present verbatim in `168.5-05-SUMMARY.md` (exact command, `--resume` recipe, retry loop, wall-clock envelope, D-18 acceptance note) — not launched in-phase (`reports/data/calibration-harness-2026-07-12T10-24-36-664Z.tsv` is a small 21-line file, consistent with a bounded pilot/prior unrelated run, not an 810-game sweep). D-17 pilot: I independently ran a live bounded harness cell (`--elo 1500 --blends 1 --anchors maia1500 --games-per-cell 1 --stockfish-procs 2`) and observed 12 real bot-move times in the 2.20s-13.40s range — consistent with the SUMMARY's claimed 43-move pilot (median 3.32s, p95 16.72s), and squarely inside the D-17 3-8s median / ≤20s p95 band, and an order of magnitude faster than the pre-phase ~110s baseline. |
| SC5 | The D-09 determinism check passes at the new budget and stop condition. | VERIFIED (live, not just SUMMARY claim) | I ran `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs` myself against real engines: **PASS: calibration determinism — same seed reproduced an identical 29-ply blend=1 game (result=win, reason=checkmate, stockfish-procs=2, budget: FLAWCHESS_BOT_MAX_NODES=50 FLAWCHESS_BOT_MAX_PLIES=8 FLAWCHESS_BOT_CONCURRENCY=4 FLAWCHESS_BOT_STOP_RULE={...})`, exit 0. `calibration-determinism.check.mjs` no longer overrides `maxNodes`/`maxPlies` (the old `DETERMINISM_MAX_NODES=20` Plan-02-era override was removed), so this proves determinism of the actual SHIPPED bot budget, not a stand-in. |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/ROADMAP.md` | Phase 169 SC1 amended to D-04 sentence | VERIFIED | Confirmed exact wording match, SC2-SC5 untouched |
| `.planning/seeds/SEED-091-flawchess-bot-play-milestone.md` | Clock fork resolution recorded | VERIFIED | "Resolved 2026-07-12 (Phase 168.5)" annotation present, original deferred prose preserved |
| `scripts/lib/calibration-providers.mjs` | Depth-only `go` + Clear Hash; independent watchdog constants | VERIFIED | Confirmed via grep + read; `node --check` passes |
| `scripts/lib/stockfish-pool.mjs` | Retry-in-place wrapper around `withEngine` | VERIFIED | `runWithRetry` present, gated to timeout error class; `node --check` passes |
| `frontend/src/lib/engine/types.ts` | `SearchBudget.stopRule`, `EngineSnapshot.stopReason`, `BotStopRule` | VERIFIED | All three symbols present at cited lines |
| `frontend/src/lib/engine/mctsSearch.ts` | Two-sided stop-rule check in apply loop | VERIFIED | Confirmed at lines 477-508; wired into `onSnapshot`/`stopReason()` |
| `frontend/src/hooks/useFlawChessEngine.ts` | Exported `FLAWCHESS_BOT_*` profile, additive | VERIFIED | 4 exports confirmed, analysis-board constants untouched |
| `scripts/calibration-harness.mjs` | Mirror constants + playGame budget wiring + D-15 pruning | VERIFIED | Mirror values byte-identical to hook; `playGame` builds `stopRule`/pinned `concurrency` into its `SearchBudget`; pruning constants/helpers present |
| `scripts/lib/calibration-determinism.check.mjs` | Re-run at bot budget + stop rule | VERIFIED (ran live) | PASS, byte-identical game, no override present |
| `scripts/lib/calibration-pruning.check.mjs` | Skip-log + `--resume` round-trip check | VERIFIED (ran live) | PASS, exit 0 (Note: code review WR-05 flags part (a) of this check's assertion as tautological — see Anti-Patterns below; the round-trip half is genuine) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `useFlawChessEngine.ts` `FLAWCHESS_BOT_*` | `scripts/calibration-harness.mjs` mirror block | Hand-copied constants with cited line-number comments | VERIFIED | Values compared byte-for-byte, identical; cited line numbers (71/74/77/80) confirmed exact against current file state |
| `scripts/calibration-harness.mjs` `playGame` | `mctsSearch.ts` stop rule | `SearchBudget.stopRule: FLAWCHESS_BOT_STOP_RULE` passed through `selectBotMove`'s `Omit<SearchBudget, 'elo'|'policyTemperature'>` (stopRule NOT excluded) | VERIFIED | Confirmed `Omit` clause excludes only `elo`/`policyTemperature`; `stopRule` reaches `mctsSearch` unmodified |
| `mctsSearch.ts` apply loop | `EngineSnapshot.stopReason` | `stopReason()` closure fired on every `onSnapshot` call | VERIFIED | Confirmed at line 506 |
| `calibration-determinism.check.mjs` | Shipped bot budget | No `maxNodes`/`maxPlies` override; relies on `FLAWCHESS_BOT_MAX_NODES`/`_MAX_PLIES` defaults | VERIFIED (ran live) | Confirmed via source inspection AND a live PASS run logging the exact budget under test |

### Behavioral Spot-Checks (run live by this verifier, not sourced from SUMMARY claims)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `mctsSearch` stop-rule unit tests | `npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts src/hooks/useFlawChessEngine.test.tsx` | 2 files / 27 tests passed | PASS |
| Frontend full suite regression | `npx vitest run` (frontend) | 142 files / 1812 tests passed | PASS |
| Frontend type-check | `npx tsc -b --noEmit` | Clean, zero errors | PASS |
| Frontend dead-export scan | `npm run knip` | Exit 0, no output | PASS |
| D-09 determinism check at shipped budget (SC5) | `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs` | PASS: byte-identical 29-ply game, exit 0 | PASS |
| D-15 anchor-pruning structural check | `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-pruning.check.mjs` | Both PASS lines printed, exit 0 | PASS |
| Live bounded harness cell (bot-move pacing, SC4/D-17) | `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1500 --blends 1 --anchors maia1500 --games-per-cell 1 --stockfish-procs 2` | 12 bot moves observed, 2.20s-13.40s range (vs. pre-phase ~110s baseline) | PASS |

### Requirements Coverage

No formal REQ-IDs for this phase — SC1-SC5 (ROADMAP Success Criteria) serve as the requirement identifiers, per the phase's own scoping. All five are covered above with VERIFIED status.

**Note on REQUIREMENTS.md drift (informational, not a gap):** `.planning/REQUIREMENTS.md` line 25 (`PLAY-05`) still reads the pre-D-04 wording ("...with a think-time budget derived from its remaining clock"), which now contradicts the amended ROADMAP Phase 169 SC1. This is correctly logged in `deferred-items.md` as an out-of-scope discovery (Plan 01's `files_modified` only covered `ROADMAP.md`/`SEED-091-...md`) for Phase 169 or a future requirements-sync task to fix. Not a gap in 168.5's own scope.

### Anti-Patterns Found

The phase's own `168.5-REVIEW.md` (code review agent, standard depth, 13 files) found 0 critical issues and 5 warnings, all pre-existing at time of this verification (no fix commits found after the review commit `8e01dd91`, which is HEAD on this branch). I independently re-confirmed the most consequential one:

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/src/lib/engine/mctsSearch.ts` | 489-504 | Stop-rule check nests 6 levels deep (`while`→`for`→`if(stopRule)`→`if(extremes)`→`if(minNodes&&window)`→`if(clearWinner\|\|nearTieFlatness)`) inside the `mctsSearch` function body | WARNING | Breaches CLAUDE.md's hard nesting-depth limit of 4. Confirmed still present at HEAD by direct read. Functionally correct (tests pass, live-verified behavior is correct) but a CLAUDE.md coding-guideline violation left unaddressed post-review. Extraction to a `stopRuleSatisfied()` helper (as the review's own fix suggests) would resolve it without behavior change. |
| `scripts/calibration-harness.mjs` | 1257 | `--resume` with a non-`.tsv` path can silently truncate the main results TSV via `emitEloSummary` | WARNING | Data-loss edge case in the operator-launched sweep path (SC4); low likelihood (operator must supply a malformed `--resume` path) but high impact (destroys a multi-hour sweep) if triggered |
| `scripts/calibration-harness.mjs` | 314-320 | `setupHarnessEngines` leaks the Stockfish pool and hangs if a later bring-up step throws after `createStockfishPool` succeeds | WARNING | Affects the harness/checks calling `setupHarnessEngines`; the two other bring-up seams in the same file already guard this failure class, this middle seam was missed |
| `frontend/src/hooks/useFlawChessEngine.ts` / `scripts/calibration-harness.mjs` | 71-85 / 113-128 | No automated drift guard between the hook's `FLAWCHESS_BOT_*` values and the harness's hand-copied mirror | WARNING | A future one-sided edit to either file would silently break app==harness (D-11) with no test catching it; I confirmed by direct comparison the values are currently identical, but nothing enforces this going forward |
| `scripts/lib/calibration-pruning.check.mjs` | 98-116 | Part (a) of the coverage assertion is tautological (`gridKeys` built from the same fixture rows it then checks membership against) | WARNING | The check's PASS message overstates what it proves for D-15's "no silent coverage gaps" requirement; the `--resume` round-trip half (part b) is a genuine assertion and I confirmed it actually passes |

None of these five warnings are goal-blocking: the phase's functional deliverables (pacing, determinism, pruning, mirroring) all work as measured live. They are pre-existing, documented code-quality debt items that should be addressed in a near-term follow-up, ideally before or shortly after the Phase 169 build that depends on this phase's constants.

### Deviations from ROADMAP Scope Note (informational)

The task brief for this verification explicitly notes: "SC4's full strength map comes from an operator-launched out-of-band ~24h sweep (D-16, documented in the 168.5-05 SUMMARY runbook) — the in-phase deliverable is the pruning code, a D-17-passing bounded pilot, and the runbook, NOT the completed sweep." This was honored: no full sweep artifact exists in the repo (only a small unrelated/prior 21-line TSV), and the runbook explicitly states it is operator-launched and not executor-blocking. This is consistent with D-16 and is not treated as a gap.

## Human Verification Required

None. All must-haves for this phase are either directly grep/read-verifiable or were independently re-run live against real engines by this verifier (not sourced solely from SUMMARY.md narrative).

## Gaps Summary

No gaps. All 5 ROADMAP Success Criteria (SC1-SC5) are verified against the actual codebase and, where the criterion concerns runtime behavior (SC3/SC4/SC5), against a live re-run of the phase's own checks/harness by this verifier — not merely SUMMARY.md's claims. Five pre-existing code-review warnings (nesting-depth violation, two harness robustness edges, a mirror-drift guard gap, one tautological check assertion) remain unaddressed but do not block the phase goal; they are recommended as near-term follow-up work, most urgently the nesting-depth fix (a direct CLAUDE.md hard-limit violation) and the drift guard between the hook and harness mirror (protects the app==harness invariant this whole phase exists to establish).

---

_Verified: 2026-07-12T14:28:49Z_
_Verifier: Claude (gsd-verifier)_
