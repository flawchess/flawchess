---
status: complete
phase: 171-bots-page-setup-screen-nav
source: [171-VERIFICATION.md]
started: 2026-07-14T00:00:00Z
updated: 2026-07-14T18:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. A guest plays a full bot game end-to-end and it lands in their Library
expected: Home -> btn-guest -> /bots -> setup -> Start -> play to mate/resign -> "Saved to your Library" link appears AND the not-auto-analyzed caveat shows -> the link opens /library/games and the finished game is there
result: pass

### 2. A logged-in user's finished game appears in the Library Games tab
expected: Log in -> /bots -> play a short game (3+0) to a finish -> "Saved to your Library" link appears -> the row is visible in the /library Games tab
result: pass
note: "Originally reported as an issue (analysis board did not flip to the player's colour). Fixed in plan 171-08 and mutation-verified twice: reverting the settings.userColor argument in Bots.tsx makes the dedicated orientation tests fail. Gap 1 resolved."

### 3. Human-mode (blend = 0) pacing does not feel robotic
expected: A 3+0 game played with the Human preset has an acceptable reveal tempo. Per D-02, if it feels wrong, tune the chessClock.ts constant — do not add a new pacing mechanism.
result: pass
note: "Pacing accepted. The separate gap raised during this test (bot board did not highlight the last played move) was fixed in plan 171-09 and mutation-verified: reverting either the useBotGame lastMove derivation or the Bots.tsx prop makes the dedicated tests fail. Gap 2 resolved."

### 4. The 30+0 preset's known TC-bucket quirk is not surfacing as a user-visible bug
expected: A stored 30+0 game appears under a Library "rapid" filter (accepted, documented quirk from normalization.py's estimated<=1800 boundary rule), not under "classical", and reads as intentional rather than broken
result: pass

### 5. Mobile: bottom nav and TC chip grid lay out correctly
expected: On a phone viewport the bottom bar (Library/Bots/Openings/Endgames + More) is legible and tappable; on /bots the TC chip grids show 4 columns (blitz), 3 (rapid), 2 (classical) with no wrap/gap; the Start button is visible without scrolling and not occluded by the nav
result: pass
note: "Originally reported: Start button pushed under the main nav, not visible without scrolling. Re-tested on a real device after the 171-10 fix and confirmed passing. Fix applied in plan 171-10 (pb-20 sm:pb-4 clearance on SetupScreen + BotsGame, chips 44->40px, Start 32->48px, gap-4->gap-3, inline TC sub-headers). Class-presence tests pin the classes, but jsdom performs no layout, so on-screen visibility could only be settled on a real device — and it was."

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Opening the finished bot game in the analysis board shows it from the player's own perspective"
  status: resolved
  resolved_by: "171-08 (commits 35d32764, 7d76602c) — mutation-verified: deleting settings.userColor from the buildAnalysisLineUrl call in Bots.tsx makes the dedicated orientation tests fail."
  reason: "User reported: When clicking on \"Analyze this game\" directly after the game, the analysis board opens, but it doesn't flip the board to the player's perspective if they are black"
  severity: major
  test: 2
  root_cause: "The bot Analyze button navigates to /analysis in free-play (?line=) mode, but Analysis.tsx's only auto-flip effect is gated on isGameMode (?game_id=) and sources color from backend gameData.user_color. Free play has NO orientation input at all — the URL contract has no color param — so boardFlipped stays at its useState(false) default."
  artifacts:
    - path: "frontend/src/pages/Bots.tsx:309-311"
      issue: "handleAnalyze calls buildAnalysisLineUrl(game.moveHistory) — carries only the move list. settings.userColor is in scope four lines below and is dropped."
    - path: "frontend/src/lib/analysisUrl.ts:24-28,39-52"
      issue: "URL contract has line/game_id/ply/fen only. buildAnalysisLineUrl(sans) has no color/orientation parameter."
    - path: "frontend/src/pages/Analysis.tsx:666-670"
      issue: "Auto-flip effect early-returns for any ?line= URL (comment: 'Free play stays white'). boardFlipped useState(false) at :475 is never overridden in free play."
  missing:
    - "analysisUrl.ts: add an ORIENTATION_PARAM to the URL contract, an optional orientation arg on buildAnalysisLineUrl(sans, orientation?: 'white'|'black'), and a parseAnalysisOrientationParam() guard mirroring parseAnalysisFenParam's tolerance. Optional arg keeps the Openings.tsx:570 caller compiling unchanged."
    - "Bots.tsx:309-311: pass settings.userColor into buildAnalysisLineUrl and add it to the useCallback deps."
    - "Analysis.tsx: read the param alongside the existing four (~446-460) and compute a single autoOrientation (isGameMode ? gameData?.user_color : urlOrientation) driving one effect. Keep the hasAutoFlipped ref guard so a manual flip still wins. Do NOT add a second competing effect."
  constraint: "Do NOT re-point handleAnalyze at buildGameAnalysisUrl(storedGameId) to get auto-flip for free — ruled out by D-20/D-21 and pinned by test V-17 (GameResultDialog.test.tsx:94). Analyze must work for guests and when the store failed, so free-play mode must become orientable."
  debug_session: ".planning/debug/bot-analyze-board-orientation.md"

- truth: "The bot game board highlights the last played move (yellow from/to squares)"
  status: resolved
  resolved_by: "171-09 (commits 35229f55, 9f461295) — mutation-verified twice: deriving lastMove from the live tail instead of viewedPly breaks the anti-stale-highlight pin, and deleting the lastMove prop in Bots.tsx breaks the wiring test."
  reason: "User reported: pass, but the board should highlight the last played move with yellow square highlighting"
  severity: minor
  test: 3
  root_cause: "A pure wiring gap, not a bug: the shared ChessBoard component already implements last-move highlighting via an optional lastMove={{from,to}} prop, but Bots.tsx is the ONLY ChessBoard caller in the app that never passes it — and useBotGame never exposes a lastMove at all (its fenAtPly helper replays SAN and throws away the resulting Move objects)."
  artifacts:
    - path: "frontend/src/pages/Bots.tsx:335"
      issue: "<ChessBoard position onPieceDrop flipped /> — no lastMove prop. Every other caller passes one (Openings.tsx:951,1076; Analysis.tsx:2048; LibraryGameCard.tsx:1013,1087; TrainSketchPage.tsx:142)."
    - path: "frontend/src/hooks/useBotGame.ts:180-230"
      issue: "UseBotGameState has no lastMove field — nothing for Bots.tsx to pass."
    - path: "frontend/src/hooks/useBotGame.ts:235-242"
      issue: "fenAtPly() replays SAN and returns only chess.fen(); the chess.move() return value carrying .from/.to is discarded. The needed data is computed and thrown away."
  missing:
    - "REUSE the existing pattern, do not invent one. Highlight rendering already exists at ChessBoard.tsx:66-73 (prop) and :336-353 (memoized squareStyles). The derivation reference is useChessGame.ts:95-118 (replayTo captures move.from/move.to when i === ply-1). useBotGame.fenAtPly is the same loop minus this capture."
    - "useBotGame.ts:235-242: have fenAtPly also return the last move ({fen, lastMove}), capturing chess.move()'s return on the final iteration. Guard ply === 0 -> null."
    - "useBotGame.ts:496: have the existing useMemo produce both values (one replay, not two); destructure position + lastMove."
    - "useBotGame.ts:180-230: add lastMove to UseBotGameState and return it. No snapshot/migration surface — BotGameSnapshot is PGN-based, so a derived field is free and works on resume via restoredHistory."
    - "Bots.tsx:335: pass lastMove={game.lastMove}. The board const is shared by renderDesktopLayout and renderMobileLayout, so this one edit satisfies the mobile-parity rule."
    - "Pass lastMove ONLY — inherit ChessBoard's default MOVE_HIGHLIGHT_SQUARE (theme.ts:16). No new theme constant, no lastMoveColor override, no hard-coded color."
    - "Tests: useBotGame.test.ts — assert lastMove is null at start, tracks from/to after a move, and follows viewPly() back into history (returns the earlier ply's move, not the live one)."
  decision: "Derive lastMove from viewedPly, NOT the live tail — bots has move-list navigation, so the highlight must follow the move list (lichess/chess.com behavior, consistent with useChessGame). Deriving from the live game would leave a stale highlight when scrubbing history."
  debug_session: ""

- truth: "On a phone viewport the /bots setup screen's Start button is visible without scrolling, not hidden behind the bottom nav"
  status: resolved
  resolved_by: "171-10 (commits 09116704, 96d2842f) — pb-20 sm:pb-4 clearance on SetupScreen + BotsGame, chips 44->40px, Start 32->48px, gap-4->gap-3, inline TC sub-headers. Class-presence tests pass (27/27), but jsdom performs no layout, so on-screen visibility/occlusion is NOT proven by tests. CONFIRMED on a real device in the 171 UAT re-test (test 5 pass)."
  followup_seed: "SEED-105 — App.tsx:567 <main> bottom padding is not safe-area-composed (latent app-wide, deliberately out of scope for 171-10)."
  reason: "User reported: The mobile TC layout looks good, but it takes a bit too much vertical space. The start button is pushed under the main nav and is not visible without scrolling"
  severity: major
  test: 5
  root_cause: "BOTH occlusion and below-fold density. SetupScreen reserves only p-4 (16px) below the Start CTA and is the ONE screen in the app that omits the established pb-20 bottom-nav clearance; meanwhile the setup stack costs ~632px on a phone. So the CTA is below the fold on a real phone viewport AND genuinely occluded behind the nav in the PWA/safe-area shell (App.tsx's flat pb-16 under-reserves against the nav's 4rem + env(safe-area-inset-bottom))."
  artifacts:
    - path: "frontend/src/components/bots/SetupScreen.tsx:216"
      issue: "Root is 'mx-auto flex max-w-md flex-col gap-4 p-4' — no pb-20. The only page in the app missing the bottom-nav clearance (cf. Endgames.tsx:812, Analysis.tsx:2660, GamesTab.tsx:385, FlawsTab.tsx:362, all pb-20 md:pb-6)."
    - path: "frontend/src/components/bots/SetupScreen.tsx:260-270 and :160-195 (TcBucketGroup)"
      issue: "3 stacked bucket groups x (24px sub-header + 44px chip row + 8px mb-2) = 228px — the single biggest density cost, ~40% of the screen."
    - path: "frontend/src/pages/Bots.tsx:355"
      issue: "BotsGame root has the same bare p-4 — same missing clearance for the in-game view."
    - path: "frontend/src/components/feedback/FeedbackButton.tsx:36-39"
      issue: "FAB at bottom-[4.5rem] (72px) sits inside the 80px band under the Start CTA, overlaying its right edge."
    - path: "frontend/src/App.tsx:567"
      issue: "<main className='pb-16 sm:pb-0'> is not safe-area-composed — equals the nav's nominal 4rem but not 4rem + pb-safe. Latent app-wide, masked everywhere else by each page's pb-20."
  missing:
    - "OCCLUSION (mandatory, ~0 risk): SetupScreen.tsx:216 -> add 'pb-20 sm:pb-4', mirroring Endgames.tsx:812 / GamesTab.tsx:385. Gives 144px total clearance vs the ~98px worst-case nav. Costs nothing above the CTA (padding is below it). Apply the same one-liner to Bots.tsx:355 for the game view."
    - "DENSITY (user-directed, see design_direction): shrink the setup controls and inline the TC sub-headers to pull the CTA above the fold."
    - "Do NOT restructure the screen. Do NOT touch App.tsx:567 (safe-area hardening is broader scope — flag as a seed, do not fix here)."
  design_direction: "USER DECISION (UAT session): make the setup controls a bit smaller on mobile, but make the Start button TALLER, not smaller. Start is currently h-8 (32px) — the smallest button on a screen where every chip is h-11 (44px), which is backwards for the primary CTA. Target: chips/sliders 44 -> 40px (40px is the FLOOR, do not go below — 44px is the WCAG 2.5.5 / Apple HIG touch-target minimum and 40px is already a deliberate mild deviation); Start button 32 -> 48px; root gap-4 -> gap-3 (-16px); TcBucketGroup sub-headers stacked -> inline left of the chip grid (-72px, preserves the Blitz/Rapid/Classical grouping); TcBucketGroup mb-2 -> mb-1 (-12px). Keep the 3 TC groups — do NOT collapse to a single 9-preset grid (loses a spec'd grouping affordance)."
  verification_note: "A 390x844 DevTools emulation will NOT reproduce the bug (no URL bar, no safe-area inset). Verify against a real phone viewport / installed PWA, or emulate with the URL bar and safe-area insets accounted for."
  debug_session: ".planning/debug/bots-setup-start-below-fold.md"
