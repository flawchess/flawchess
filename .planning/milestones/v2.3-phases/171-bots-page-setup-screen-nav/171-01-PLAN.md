---
phase: 171-bots-page-setup-screen-nav
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/chessClock.ts
  - frontend/src/hooks/useBotGame.ts
autonomous: true
requirements: [PLAY-02]
must_haves:
  truths:
    - "A reader of chessClock.ts's D-16 header learns that the think deadline is enforced ONLY when blend > 0, and that at blend = 0 it is computed and never consulted (SEED-100)."
    - "A reader of BotGameSettings.blend (useBotGame.ts) learns that blend = 0 is a distinct no-search regime exempt from the D-16 deadline, and that selectBotMove.test.ts's blend=0 test is what pins it."
    - "The blend=0 'deps.search zero times' invariant is proven live by a revert-and-observe-RED mutation run, not by grep."
  artifacts:
    - "frontend/src/lib/chessClock.ts — corrected D-16 header comment (D-03 step 1)"
    - "frontend/src/hooks/useBotGame.ts — blend-0 exemption note on BotGameSettings.blend (D-03 step 1)"
    - "The mutation-proof transcript (revert → RED → re-apply → GREEN) recorded verbatim in the plan SUMMARY"
  key_links:
    - "chessClock.ts's D-16 claim <-> selectBotMove.ts's blend<=0 early-return: the comment is only true on the blend > 0 branch."
    - "BotGameSettings.blend (useBotGame.ts:161-162) <-> BotSettings.blend (selectBotMove.ts): the exemption note goes on the FORMER, per D-03."
  prohibitions:
    - "Does NOT modify selectBotMove.ts logic. The blend<=0 early-return (selectBotMove.ts:113-118) is reverted ONLY transiently during the mutation proof and MUST be restored byte-identically; `git diff --exit-code -- frontend/src/lib/engine/selectBotMove.ts` must be clean at commit time."
    - "Does NOT write a new test for the blend=0 invariant — selectBotMove.test.ts:85-96 already contains it (RESEARCH.md Pitfall 1). Duplicating it is forbidden."
    - "Does NOT add a hook-level (useBotGame.test.ts) test for this invariant — that suite mocks selectBotMove wholesale and can only test the mock (RESEARCH.md Pitfall 2)."
    - "Does NOT implement SEED-100 fix (a) — racing selectBotMove against the deadline was considered and REJECTED (degrades to a random fallbackMove)."
    - "Does NOT add a new pacing mechanism (per-move randomized delays) — D-02 rides the existing computeRevealDelayMs floor."
---

<objective>
Resolve SEED-100, the declared Phase 171 roadmap blocker, via the seed's fix (b): document + pin.

Exposing the play-style slider (D-01) makes `blend = 0` user-reachable for the first time.
At `blend = 0`, `selectBotMove` returns from a single Maia policy sample **before** `deps.search`
is ever consulted, so the Phase 169 D-16 think deadline is computed and thrown away — a blend-0
bot has an honest, flaggable clock with **no pacing mechanism**. `chessClock.ts`'s header currently
asserts the opposite. That false comment is the actual defect; the behavior is fine (~0.09s/move,
measured in 168.5).

Purpose: unblock the rest of the phase with a truthful invariant that a future reader cannot
mis-trust, proven by mutation rather than by symbol presence.
Output: two corrected doc comments and a recorded red-then-green mutation transcript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-bots-page-setup-screen-nav/171-CONTEXT.md
@.planning/phases/171-bots-page-setup-screen-nav/171-RESEARCH.md
@.planning/seeds/SEED-100-blend-zero-bot-has-no-pacing-mechanism.md
</context>

## Artifacts this phase produces (Plan 01)

| Artifact | Kind | Status |
|---|---|---|
| `frontend/src/lib/chessClock.ts` D-16 header comment | doc comment | MODIFIED |
| `frontend/src/hooks/useBotGame.ts` `BotGameSettings.blend` doc | doc comment | MODIFIED |
| Mutation-proof transcript in `171-01-SUMMARY.md` | evidence | NEW |

No new symbols, files, constants, or exports. `selectBotMove.ts` ends this plan byte-identical to its start.

<tasks>

<task type="auto">
  <name>Task 1: Correct the false D-16 comment and document the blend-0 exemption</name>

  <read_first>
    - `frontend/src/lib/chessClock.ts` (whole file — the D-16 paragraph at ~:34-39 is the false claim; the D-19 paragraph below it and `REVEAL_DELAY_MIN_MS`/`REVEAL_DELAY_MAX_MS` are the D-02 pacing floor the corrected text must point at)
    - `frontend/src/lib/engine/selectBotMove.ts` lines 97-147 (READ ONLY — the three-way regime dispatch; `blend <= 0` returns from `deps.policy` at :113-118 before `deps.search` is resolved at :119)
    - `frontend/src/hooks/useBotGame.ts` lines 155-170 (`BotGameSettings`, whose `blend` field doc at :161-162 gets the exemption note) and the `computeThinkDeadlineMs` / `createDeadlineSearch` call sites (search for both — the deadline is built unconditionally, which is exactly what the corrected comment must say)
    - `.planning/phases/171-bots-page-setup-screen-nav/171-RESEARCH.md` § "Code Examples > D-03: chessClock.ts corrected D-16 header comment" (the illustrative target text)
    - `.planning/seeds/SEED-100-blend-zero-bot-has-no-pacing-mechanism.md` (its "Verification note" is binding)
  </read_first>

  <files>frontend/src/lib/chessClock.ts, frontend/src/hooks/useBotGame.ts</files>

  <action>
    Edit the D-16 paragraph of `chessClock.ts`'s module header (currently at ~:34-39, the sentence ending
    "enforces from OUTSIDE the frozen search core by cutting the in-flight search and returning its
    best-so-far result"). Extend it so it states, in prose:
      - the deadline is enforced ONLY when `deps.search` is actually consulted, i.e. `blend > 0`;
      - at `blend = 0` (SEED-100, Phase 171 D-03) `selectBotMove.ts` returns from a single Maia policy
        sample before `deps.search` is ever called, so `useBotGame` still computes the deadline and still
        builds the deadline-wrapped search, but it is never enforced;
      - the consequence: a Human-preset bot has an honest, flaggable clock with NO pacing mechanism of
        its own — its speed is governed entirely by `computeRevealDelayMs`'s reveal-delay floor (D-02)
        and the measured ~0.09s Maia inference cost, both far under any realistic blitz/rapid budget;
      - the invariant is pinned by `selectBotMove.test.ts`'s blend=0 "deps.search zero times" assertion —
        a future reader must not restore a smaller ad-hoc claim here.
    Do NOT delete the surrounding D-19 paragraph or the `formatClockLabel` NOTE.

    In `useBotGame.ts`, extend the doc comment on `BotGameSettings.blend` (currently "0 = full-human,
    1 = full-stockfish (selectBotMove's regime blend)") to record the exemption: `blend` is a three-way
    REGIME DISPATCH, not a mix; `0` runs one Maia policy call with no MCTS and is therefore exempt from
    the D-16 think deadline (which is computed and unused for that regime); anything `> 0` runs the full
    search under the deadline. Cross-reference SEED-100 / Phase 171 D-03 and name
    `selectBotMove.test.ts`'s blend=0 test as the pin. Per D-03 this note goes on
    `BotGameSettings.blend` (useBotGame.ts), NOT on `BotSettings.blend` in `selectBotMove.ts`.

    Touch nothing else in either file: no constant values, no logic, no signatures.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/chessClock.test.ts && npx tsc -b && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'blend' frontend/src/lib/chessClock.ts` returns at least one hit inside the D-16 header paragraph (today it returns none in that paragraph).
    - `grep -c 'SEED-100' frontend/src/lib/chessClock.ts frontend/src/hooks/useBotGame.ts` reports >= 1 for each file.
    - `git diff --numstat -- frontend/src/lib/chessClock.ts frontend/src/hooks/useBotGame.ts` shows only comment-line changes; `git diff -- frontend/src/lib/chessClock.ts frontend/src/hooks/useBotGame.ts | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vE '^[+-]\s*(\*|//|/\*)' | wc -l` == 0 (no non-comment line changed).
    - `git diff --exit-code -- frontend/src/lib/engine/selectBotMove.ts` exits 0 (untouched).
    - `npx vitest run src/lib/__tests__/chessClock.test.ts` passes; `npx tsc -b` exits 0.
    - Covers V-03.
  </acceptance_criteria>

  <done>Both doc comments state the blend-0 exemption; no runtime line changed in either file; selectBotMove.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the blend=0 invariant by mutation (revert → RED → re-apply → GREEN)</name>

  <read_first>
    - `frontend/src/lib/engine/__tests__/selectBotMove.test.ts` lines 60-100 (the existing
      `describe('selectBotMove — blend=0 (full-human)')` block; its first `it(...)` asserts
      `expect(policy).toHaveBeenCalledTimes(1)` and `expect(search).toHaveBeenCalledTimes(0)` — this IS
      SEED-100's pin, already shipped. Do not rewrite it.)
    - `frontend/src/lib/engine/selectBotMove.ts` lines 110-125 (the `if (blend <= 0) { ... }` early-return
      block — the mutation target)
    - Project memory rule `feedback_mutation_test_gap_closures`: a gap fix is proven by REVERTING it and
      confirming the test fails. Grep / symbol presence is NOT acceptable evidence. Phase 169 burned three
      rounds on exactly this shape.
  </read_first>

  <files>frontend/src/lib/engine/selectBotMove.ts (transiently mutated, then restored byte-identically)</files>

  <action>
    Perform the mutation proof as an explicit, non-optional, observable sequence. Do NOT substitute
    reasoning, grep, or a code read for any of these four steps.

    1. GREEN baseline: run `cd frontend && npx vitest run src/lib/engine/__tests__/selectBotMove.test.ts -t "blend=0"`.
       Capture the output. It must PASS.
    2. MUTATE: in `selectBotMove.ts`, comment out (or delete) the `if (blend <= 0) { ... }` early-return
       block at ~:113-118 so a `blend = 0` call falls through into the `const search = deps.search ?? mctsSearch;`
       MCTS path.
    3. RED observation: re-run the exact same command. It must FAIL, and the failure must be on the
       `expect(search).toHaveBeenCalledTimes(0)` assertion (search called 1 time instead of 0). Capture the
       failing output verbatim. If it PASSES here, STOP — the invariant is not actually pinned and the plan
       must be escalated, not marked done.
    4. RESTORE: `git checkout -- frontend/src/lib/engine/selectBotMove.ts`. Re-run the command; it must PASS
       again. Confirm `git diff --exit-code -- frontend/src/lib/engine/selectBotMove.ts` exits 0.

    Record all four captured outputs (baseline PASS, the mutated RED failure message, the restored PASS, and
    the clean `git diff --exit-code`) verbatim in `171-01-SUMMARY.md` under a `## Mutation proof (SEED-100 / V-02)`
    heading. The SUMMARY is the deliverable here; a claim without the transcript does not satisfy this task.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/selectBotMove.test.ts -t "blend=0" && git diff --exit-code -- src/lib/engine/selectBotMove.ts</automated>
  </verify>

  <acceptance_criteria>
    - `171-01-SUMMARY.md` contains a `## Mutation proof (SEED-100 / V-02)` section with FOUR captured
      transcripts: (a) baseline PASS, (b) the RED run showing the `toHaveBeenCalledTimes(0)` assertion failing
      with `search` called 1 time, (c) the post-restore PASS, (d) a clean `git diff --exit-code` on
      `selectBotMove.ts`.
    - The RED transcript names the failing assertion. A summary that says "confirmed by inspection", "the test
      exists", or "grep shows the early-return is present" FAILS this criterion outright.
    - `git diff --exit-code -- frontend/src/lib/engine/selectBotMove.ts` exits 0 at commit time.
    - `npx vitest run src/lib/engine/__tests__/selectBotMove.test.ts` (whole file) passes.
    - Covers V-01 and V-02.
  </acceptance_criteria>

  <done>Red-then-green mutation transcript recorded in the SUMMARY; selectBotMove.ts restored byte-identically; the blend=0 test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes only doc comments. No data crosses any boundary that did not cross it before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-01-01 | Tampering | `selectBotMove.ts` left mutated by an incomplete mutation proof (search path silently enabled at blend=0, +950 ELO regression shipped) | high | mitigate | Task 2's restore step + a `git diff --exit-code -- frontend/src/lib/engine/selectBotMove.ts` acceptance gate that must exit 0 at commit time. |
| T-171-01-02 | Repudiation | A future reader trusts the (currently false) D-16 comment and removes the reveal-delay floor as "redundant", leaving a blend-0 bot with no pacing at all | medium | mitigate | Task 1's corrected comment names `computeRevealDelayMs` as the ONLY pacing mechanism at blend 0 and names the pinning test. |
| T-171-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — RESEARCH.md § Package Legitimacy Audit: "Not applicable this phase. No new npm or pip packages are introduced." No `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/selectBotMove.test.ts src/lib/__tests__/chessClock.test.ts` — all green.
- `cd frontend && npx tsc -b && npm run lint` — clean.
- `git diff --exit-code -- frontend/src/lib/engine/selectBotMove.ts` — exits 0.
- `171-01-SUMMARY.md` carries the four-part mutation transcript.
</verification>

<success_criteria>
SEED-100's blocker line on the Phase 171 roadmap entry is answerable with: the false comment is corrected,
the exemption is documented on `BotGameSettings.blend`, and the blend=0 "never consults deps.search"
invariant is proven RED-then-GREEN by an actual revert — not by grep.
</success_criteria>

<output>
Create `.planning/phases/171-bots-page-setup-screen-nav/171-01-SUMMARY.md` when done, including the
`## Mutation proof (SEED-100 / V-02)` transcript section.
</output>
