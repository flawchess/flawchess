---
phase: 173-anchor-ladder-self-calibration-seed-101
plan: 04
type: execute
wave: 3
depends_on: ["173-02", "173-03"]
files_modified:
  - scripts/lib/calibration-internal-scale.mjs
  - reports/data/anchor-ladder-internal-scale.json
  - .planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md
autonomous: false
requirements: [D-11, D-12, D-13]
must_haves:
  truths:
    - "The real anchor-vs-anchor sweep has been executed, producing a committed raw per-game TSV in reports/data/."
    - "The fit has run on that TSV, producing a committed machine-readable internal-scale table at a stable path for SEED-102."
    - "A findings note states the ladder spacing + the Maia-3 compression verdict and closes 168-RESEARCH Open Question 2."
  artifacts:
    - scripts/lib/calibration-internal-scale.mjs
    - reports/data/anchor-ladder-internal-scale.json
    - .planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md
  key_links:
    - "calibration-internal-scale.mjs is the stable Node-importable artifact SEED-102's harness work will consume."
    - "The findings note closes 168-RESEARCH.md Open Question 2 (SF Skill → ELO folklore) as a blocker for relative work."
---

<objective>
Execute the phase's actual deliverable (D-11): run the anchor-vs-anchor sweep,
fit the internal scale, and produce the three D-12 artifacts with the D-13 caveat
— the answer to "is the Maia-3 argmax ladder compressed like Maia-1's?".

Purpose: The tooling (Plans 01-03) is a means; the phase goal is the measured
ladder spacing + compression verdict that unblocks SEED-102. This plan runs the
hours-long, resumable sweep on Adrian's machine, then derives and commits the
answer.

Output: The committed internal-scale JS module + JSON sibling, and the findings
note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-CONTEXT.md
@.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-RESEARCH.md
@.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-02-SUMMARY.md
@.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-03-SUMMARY.md
@.planning/notes/2026-07-13-bot-calibration-findings.md
</context>

## Artifacts this phase produces (Plan 04)

New files created by this plan (exclude from source-drift verification):
- `scripts/lib/calibration-internal-scale.mjs` — `export const INTERNAL_RATING = { <anchor>: <rating>, ... }` (D-12 artifact 2, the Node-consumable stable path for SEED-102), with the D-13 caveat in its module docstring.
- `reports/data/anchor-ladder-internal-scale.json` — the human/Python-readable sibling (ratings + CIs + residuals), D-13-labeled.
- `reports/data/anchor-ladder-<ts>.tsv` + `-pairs.tsv` — the raw run output (D-12 artifact 1), committed.
- `.planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md` — the findings note (D-12 artifact 3).

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute the real anchor-vs-anchor sweep (D-11, multi-hour, resumable)</name>
  <action>Human-verify checkpoint: after all Plan 01/02/03 unit checks are green, run the anchor-ladder orchestrator to produce the raw + pairs TSV, then confirm completion. See how-to-verify for the exact commands.</action>
  <what-built>
    The standalone anchor-vs-anchor orchestrator (Plan 02) + the stdlib fit
    (Plan 03), both unit-green. This checkpoint runs the REAL multi-hour sweep —
    it is the phase's deliverable, not a test, and must run only after every
    unit check is green (running a buggy sweep wastes exactly the CPU-hours D-01
    exists to conserve).
  </what-built>
  <how-to-verify>
    1. Confirm all Plan 01/02/03 checks are green first:
       - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-anchor-schedule.check.mjs`
       - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-game-loop.check.mjs`
       - `uv run pytest tests/scripts/test_calibration_anchor_fit.py -x`
    2. Run the sweep (hours; anchor-vs-anchor has no MCTS, so expect hours not days).
       Use as many stockfish procs as cores allow (16 available):
       `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-anchor-ladder.mjs --stockfish-procs 12`
       It is resumable: if interrupted, re-run with
       `--resume reports/data/anchor-ladder-<ts>.tsv` (same seed).
    3. Confirm completion: `reports/data/anchor-ladder-<ts>.tsv` (raw per-game)
       and `reports/data/anchor-ladder-<ts>-pairs.tsv` (per-pair aggregate) both
       exist, the raw ledger's last line ends with a newline (not truncated), and
       the run printed a connectivity-OK line (>= 2 cross-family links survived).
    Report the exact `<ts>` filename of the completed raw ledger so the fit task
    can consume it.
  </how-to-verify>
  <resume-signal>Type "approved" with the completed raw-ledger `<ts>` filename, or describe issues (disconnected graph, lopsided cross-family pairs, crash) for re-targeting.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fit the internal scale + emit the two machine-readable artifacts (D-12 #1/#2, D-13)</name>
  <files>scripts/lib/calibration-internal-scale.mjs, reports/data/anchor-ladder-internal-scale.json</files>
  <read_first>
    - scripts/calibration_anchor_fit.py (the fit CLI + --out-js/--out-json flags from Plan 03)
    - scripts/lib/calibration-anchors.mjs (the SF_SKILL_ELO export SHAPE to mirror for INTERNAL_RATING — plain object keyed by anchor label)
    - .planning/phases/173-anchor-ladder-self-calibration-seed-101/173-PATTERNS.md (calibration-internal-scale.mjs analog: the export const + D-13 docstring)
    - reports/data/anchor-ladder-<ts>.tsv (the raw ledger produced by Task 1 — pass the exact filename reported at the checkpoint)
  </read_first>
  <action>
    Run the Plan 03 fit against the completed raw ledger:
    `uv run python scripts/calibration_anchor_fit.py --input reports/data/anchor-ladder-<ts>.tsv --out-js scripts/lib/calibration-internal-scale.mjs --out-json reports/data/anchor-ladder-internal-scale.json`.
    Verify the fit ran the connectivity guard (no RuntimeError) and pinned
    maia1500 = 1500. Confirm `scripts/lib/calibration-internal-scale.mjs` exports
    `INTERNAL_RATING` as a plain object keyed by every anchor label
    (maia700/…/maia2300, sf0/sf3/sf5/sf8/sf10) with the fitted ratings (no
    remaining `0` placeholders), carrying the D-13 "INTERNAL SCALE — NOT human
    ELO; maia1500 = 1500" note in its module docstring. Confirm the JSON sibling
    carries per-anchor ratings, bootstrap CIs, and per-pair residuals (cross-family
    flagged), also D-13-labeled. Stage the raw + pairs TSV, the JS module, and the
    JSON for commit (all three D-12 artifacts committed, not left uncommitted).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs -e "import('./scripts/lib/calibration-internal-scale.mjs').then(m=>{const r=m.INTERNAL_RATING; if(r.maia1500!==1500) throw new Error('scale not pinned'); if(Object.keys(r).length<10) throw new Error('missing anchors'); console.log('SCALE-OK', JSON.stringify(r));})"</automated>
  </verify>
  <acceptance_criteria>
    - `calibration-internal-scale.mjs` exports `INTERNAL_RATING` with `maia1500 === 1500` and a numeric (non-placeholder) rating for all 10 anchors.
    - The module docstring contains the D-13 "NOT human ELO" caveat.
    - `reports/data/anchor-ladder-internal-scale.json` contains ratings, bootstrap CIs, and cross-family-flagged residuals.
    - The raw `anchor-ladder-<ts>.tsv`, its `-pairs.tsv`, the JS module, and the JSON are all staged/committed.
  </acceptance_criteria>
  <done>The fitted internal scale exists at a stable Node-importable path (+ JSON sibling), all D-12 machine-readable artifacts committed with the D-13 caveat.</done>
</task>

<task type="auto">
  <name>Task 3: Write the findings note — spacing + compression verdict, close 168 OQ2 (D-12 #3)</name>
  <files>.planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md</files>
  <read_first>
    - .planning/notes/2026-07-13-bot-calibration-findings.md (the numbered-findings structure to mirror exactly — each finding: what was measured / what it means / what it unblocks)
    - reports/data/anchor-ladder-internal-scale.json (the fitted ratings + CIs + residuals to report)
    - .planning/seeds/SEED-101-anchor-ladder-self-calibration.md (the compression hypothesis: is the Maia-3 argmax ladder compressed like Maia-1's?)
    - .planning/milestones/v2.3-phases/168-headless-calibration-harness-spike-gated/168-RESEARCH.md (Open Question 2 — SF Skill → ELO folklore; this note closes it as a blocker for relative work)
  </read_first>
  <action>
    Write `.planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md`
    mirroring `2026-07-13-bot-calibration-findings.md`'s numbered-findings shape.
    Report, from the fitted JSON: (1) the internal rating of every anchor with its
    bootstrap CI; (2) the measured adjacent-Maia-rung spacing vs the nominal 400-pt
    steps → the explicit compression verdict (is the Maia-3 argmax ladder
    compressed like Maia-1's? by how much?); (3) the cross-family residuals,
    prominently — large ones are the style-confounding signal the seed calls out
    (D-06); (4) a closing finding that retires 168-RESEARCH.md Open Question 2 (SF
    Skill → ELO is now measured on the internal scale, no longer folklore for
    RELATIVE work) and states what this unblocks for SEED-102. Carry the D-13
    caveat verbatim ("internal scale — NOT human ELO; scale fixed at maia1500 =
    1500"). Keep the tone terse and factual, matching the analog. Do not editorialize
    methodology beyond what the fit produced.
  </action>
  <verify>
    <automated>test -f .planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md && grep -qi "compress" .planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md && grep -qi "NOT human ELO" .planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md && grep -qi "Open Question 2" .planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md && echo NOTE-OK</automated>
  </verify>
  <acceptance_criteria>
    - The note exists and states an explicit compression verdict (contains the measured adjacent-rung deltas vs nominal 400).
    - It reports per-anchor ratings + CIs and the cross-family residuals prominently.
    - It carries the D-13 "NOT human ELO" caveat and explicitly closes 168-RESEARCH.md Open Question 2 as a blocker for relative work.
    - It follows the numbered-findings structure of the 2026-07-13 analog.
  </acceptance_criteria>
  <done>The phase's answer is documented: ladder spacing, compression verdict, cross-family style signal, and OQ2 closure — SEED-102 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → run | The multi-hour sweep is launched by Adrian on his own machine; no network, no PII, no prod DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-09 | Tampering | Fit run on a disconnected/lopsided real graph → meaningless published scale | high | mitigate | The fit's `check_connectivity` fail-loud guard (Plan 03) runs before fitting; the checkpoint requires the run to report >= 2 surviving cross-family links. |
| T-173-10 | Information Disclosure | Internal scale mis-consumed downstream as human ELO | medium | mitigate | D-13 caveat carried verbatim into the JS module docstring, JSON header, and findings note. |
| T-173-11 | Denial of Service | A killed multi-hour run loses all progress | low | mitigate | `--resume` from the durable ledger (Plan 02) — completed games are never lost. |
| T-173-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages — runs existing scripts only (D-07); no supply-chain surface. |
</threat_model>

<verification>
- The checkpoint gates on all unit checks green + a completed, connected, non-truncated raw ledger.
- Task 2's node import assertion proves the scale is pinned and complete.
- Task 3's grep gate proves the compression verdict, caveat, and OQ2 closure are present.
</verification>

<success_criteria>
- The real sweep executed; the fitted internal scale is committed at a stable path; the findings note states the compression verdict and closes 168 OQ2. SEED-102 unblocked.
</success_criteria>

<output>
Create `.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-04-SUMMARY.md` when done.
</output>
