---
phase: 173-anchor-ladder-self-calibration-seed-101
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/calibration-game-loop.mjs
  - scripts/calibration-harness.mjs
  - scripts/lib/calibration-anchors.mjs
  - scripts/lib/calibration-game-loop.check.mjs
  - scripts/lib/calibration-anchors.check.mjs
autonomous: true
requirements: [D-08, D-09, D-10]
must_haves:
  truths:
    - "The existing bot harness still plays byte-identical games after the game loop is extracted (no behavior change)."
    - "A mover-agnostic two-mover game loop exists that any script can drive with an arbitrary mover on each color."
    - "parseAnchorSpec accepts sf8 and sf10 tokens without throwing."
  artifacts:
    - scripts/lib/calibration-game-loop.mjs
    - scripts/lib/calibration-game-loop.check.mjs
    - scripts/lib/calibration-anchors.check.mjs
  key_links:
    - "calibration-harness.mjs playGame delegates to calibration-game-loop.mjs playTwoMoverGame."
    - "SF_SKILL_ELO gains 8/10 entries so parseAnchorSpec's `skillLevel in SF_SKILL_ELO` gate passes."
---

<objective>
Lay the reusable Node foundation for the anchor-vs-anchor harness (D-08) by
extracting `calibration-harness.mjs`'s game loop into a mover-agnostic
`scripts/lib/calibration-game-loop.mjs`, and wire Stockfish skills 8/10 into the
anchor tables (D-09) — with NO behavior change to the existing bot harness (D-10).

Purpose: Phase 173's new anchor-ladder orchestrator (Plan 02) needs a two-mover
game loop and sf8/sf10 anchor support. Both are generalizations of code that
already exists for one bot + one anchor; extracting instead of copy-pasting keeps
a single adjudication/terminal implementation (D-08).

Output: A new extracted game-loop module + its structural check, the SF_SKILL_ELO
8/10 extension + an anchors parse check, and CLI-helper/parseAnchorSpec exports
the orchestrator will import.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-CONTEXT.md
@.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01)

New symbols/files created by this plan (exclude from source-drift verification):
- `scripts/lib/calibration-game-loop.mjs` exporting `playTwoMoverGame` and the
  extracted helpers it needs (`classifyTerminalResultWhitePov`,
  `evaluateNonTerminalCutoffsWhitePov`, `updateSustainState`, `applyUciMove`).
- `scripts/lib/calibration-game-loop.check.mjs` (structural check, new file).
- `scripts/lib/calibration-anchors.check.mjs` (parse/rating check, new file).
- New keys `8` and `10` on the existing `SF_SKILL_ELO` export in
  `scripts/lib/calibration-anchors.mjs`.
- New named exports from `scripts/calibration-harness.mjs`: `requireFlagValue`,
  `parsePositiveIntFlag`, `parseIntList`, `parseFloatList`, `deriveGameSeed`,
  `SEED_GAME_INDEX_MULTIPLIER`, `resolveGitSha`, `buildTimestamp` (currently
  unexported module-locals), so Plan 02 imports them instead of duplicating
  (D-08 anti-drift).

<tasks>

<task type="auto">
  <name>Task 1: Wire Stockfish skills 8/10 into the anchor tables (D-09) + anchors parse check</name>
  <files>scripts/lib/calibration-anchors.mjs, scripts/lib/calibration-anchors.check.mjs</files>
  <read_first>
    - scripts/lib/calibration-anchors.mjs (the SF_SKILL_ELO table at line 37, anchorRatingFor, the header doc comment lines 29-36)
    - scripts/calibration-harness.mjs (parseAnchorSpec at lines 304-320 — the `skillLevel in SF_SKILL_ELO` gate; already exported)
    - scripts/lib/calibration-elo.check.mjs (the canned-fixture `.check.mjs` assertion style: node:assert/strict, PASS log, process.exit(0))
    - scripts/lib/frontend-alias-hook.mjs (how `.check.mjs` files are run: `node --import ./scripts/lib/frontend-alias-hook.mjs <file>`)
  </read_first>
  <action>
    Extend the existing `SF_SKILL_ELO` object export in
    `scripts/lib/calibration-anchors.mjs` from `{ 0: 1320, 3: 1750, 5: 2200 }` to
    add keys `8: 2600` and `10: 2800` (RESEARCH.md Assumption A1 — round,
    explicitly-approximate continuations of the existing spacing). Update the
    doc comment directly above the table to state that sf8/sf10 are `[ASSUMED]`
    round continuations used ONLY for labels/ordering and the scheduler's initial
    cross-family pairing guess — never as a fit input (Pitfall 3). Do NOT change
    `parseAnchorSpec` — its `skillLevel in SF_SKILL_ELO` gate now passes for
    sf8/sf10 automatically (D-09: "fix it at the map, not the parser").

    Create `scripts/lib/calibration-anchors.check.mjs` mirroring
    `calibration-elo.check.mjs`'s structure (shebang, module doc comment naming
    the Run-via command, `node:assert/strict`, `console.log('PASS: ...')`,
    `process.exit(0)`). Import `SF_SKILL_ELO`/`anchorRatingFor` from
    `./calibration-anchors.mjs` and `parseAnchorSpec` from
    `../calibration-harness.mjs`. Assert: SF_SKILL_ELO has numeric entries for
    keys 8 and 10; `parseAnchorSpec('sf8')` and `parseAnchorSpec('sf10')` return
    `{ kind: 'sf', skillLevel: 8|10 }` without throwing; `anchorRatingFor` on
    those parsed specs returns 2600/2800; and a pre-existing token
    (`parseAnchorSpec('maia1500')`) still parses to `{ kind: 'maia', rungElo: 1500 }`.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-anchors.check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `SF_SKILL_ELO[8] === 2600` and `SF_SKILL_ELO[10] === 2800` in scripts/lib/calibration-anchors.mjs.
    - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-anchors.check.mjs` exits 0 and prints a PASS line.
    - The doc comment above SF_SKILL_ELO mentions sf8/sf10 are `[ASSUMED]` labels/ordering-only, never a fit input.
    - parseAnchorSpec is NOT modified (git diff shows no change to lines 304-320 of calibration-harness.mjs).
  </acceptance_criteria>
  <done>sf8/sf10 are valid anchor tokens with nominal ratings; the anchors check passes; the folklore-only discipline is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the mover-agnostic game loop (D-08) + rewire the bot harness as a thin wrapper + export CLI helpers</name>
  <files>scripts/lib/calibration-game-loop.mjs, scripts/calibration-harness.mjs, scripts/lib/calibration-game-loop.check.mjs</files>
  <read_first>
    - scripts/calibration-harness.mjs (lines 348-542: classifyTerminalResult, updateSustainState, adjudicatedResult, evaluateNonTerminalCutoffs, applyUciMove, playGame; the ADJUDICATION_CP_THRESHOLD/ADJUDICATION_SUSTAIN_PLIES/PLY_CAP exports lines 121-130; the requireFlagValue/parsePositiveIntFlag/parseIntList/parseFloatList helpers lines 176-210; the SEED_GAME_INDEX_MULTIPLIER/deriveGameSeed module-locals lines 135-138; resolveGitSha/buildTimestamp lines 599-610 — all currently unexported)
    - scripts/lib/calibration-pruning.check.mjs (the synthesized-fixture structural-check style: mkdtempSync/rmSync in finally if on-disk state is needed; node:assert/strict)
    - scripts/lib/calibration-determinism.check.mjs (the EXISTING real-engine check that must still pass after extraction — it imports playGame from ../calibration-harness.mjs)
    - .planning/phases/173-anchor-ladder-self-calibration-seed-101/173-PATTERNS.md (Pattern 1: the playTwoMoverGame shape and the color-keyed vs bot-relative result-mapping requirement)
  </read_first>
  <action>
    Create `scripts/lib/calibration-game-loop.mjs` exporting
    `playTwoMoverGame({ Chess, pool, moverWhite, moverBlack, startFen, gameRng, onPly, maxPlies })`.
    Move the game-loop body out of `calibration-harness.mjs`'s `playGame`
    (the `for(;;)` ply loop, `pool.newGameAll()` TT-clear, `applyUciMove`, the
    terminal→non-terminal-cutoff→continue order, and the `onPly` callback) into
    this new module, generalized so the mover is chosen by COLOR
    (`whiteToMove ? moverWhite : moverBlack`) rather than bot-vs-anchor role.
    Generalize the terminal/adjudication helpers to WHITE-POV color-keyed results:
    `classifyTerminalResultWhitePov(chess)` returns `{ result: 'white_win'|'black_win'|'draw', reason }`
    (drop the `botIsWhite` framing — a checkmate with white to move is `black_win`),
    and `evaluateNonTerminalCutoffsWhitePov({ pool, fen, ply, sustainState, maxPlies })`
    returns the color-keyed adjudicated/ply-cap result or null. Keep
    `updateSustainState` and `applyUciMove` behavior identical; import the three
    adjudication constants (ADJUDICATION_CP_THRESHOLD, ADJUDICATION_SUSTAIN_PLIES,
    PLY_CAP) from `../calibration-harness.mjs` (do NOT redefine them — D-10). The
    loop must accept an optional `maxPlies` (defaulting to PLY_CAP) so callers can
    keep the current cap; the returned object shape is `{ result, reason, plies, moveUcis }`.

    Rewrite `calibration-harness.mjs`'s `playGame` to be a thin wrapper: derive
    `moverWhite`/`moverBlack` from `botIsWhite` (one side is the `selectBotMove`
    closure with the existing budget wiring, the other is the `playAnchorMove`
    closure), call `playTwoMoverGame`, then MAP the color-keyed result back to the
    bot-relative `{ result: 'win'|'loss'|'draw', reason }` shape the harness's
    tally/`isUnanimousWin`/`isUnanimousLoss` logic already expects
    (`white_win` → bot `win` iff botIsWhite, etc.). `playGame`'s exported
    signature, its `onPly` payload (`{ ply, mover, uci, moveMs }`), and its return
    shape (`{ result, reason, plies, moveUcis }`) MUST be unchanged. The extracted
    helpers `classifyTerminalResult`/`evaluateNonTerminalCutoffs`/`updateSustainState`/
    `applyUciMove` that now live in the loop module should be imported back into the
    harness if still referenced, or removed if fully superseded — no dead duplicates.

    Also add `export` to the currently-module-local CLI/seed/build helpers in
    calibration-harness.mjs — `requireFlagValue`, `parsePositiveIntFlag`,
    `parseIntList`, `parseFloatList`, `deriveGameSeed`,
    `SEED_GAME_INDEX_MULTIPLIER`, `resolveGitSha`, `buildTimestamp` — so Plan 02
    imports them instead of duplicating the seed-derivation/git-sha/timestamp
    logic (D-08 anti-drift); do not change their bodies. `deriveGameSeed` closes
    over `SEED_GAME_INDEX_MULTIPLIER`, so both must be exported together.

    Create `scripts/lib/calibration-game-loop.check.mjs` mirroring
    `calibration-pruning.check.mjs`: drive `playTwoMoverGame` with two synthesized
    deterministic in-memory movers (no real engines — e.g. movers that replay a
    fixed UCI script leading to a known checkmate/stalemate, and a `pool` stub
    whose `newGameAll`/`evalPosition` are no-ops returning a fixed white-POV cp)
    and assert the color-keyed result/reason/plies for: a white checkmate
    (`black_win`), a stalemate (`draw`), and an adjudication cutoff when the stub
    eval stays past the threshold for ADJUDICATION_SUSTAIN_PLIES consecutive plies.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-game-loop.check.mjs && node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-pruning.check.mjs && node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/calibration-game-loop.mjs` exports `playTwoMoverGame`; calibration-harness.mjs imports it and playGame's body no longer contains the `for(;;)` loop (it delegates).
    - The NEW game-loop check exits 0 (color-keyed checkmate/stalemate/adjudication assertions pass).
    - The EXISTING `calibration-determinism.check.mjs` still exits 0 (byte-identical moveUcis — proves no behavior change, D-08/D-10).
    - The EXISTING `calibration-pruning.check.mjs` still exits 0.
    - `requireFlagValue`, `parsePositiveIntFlag`, `parseIntList`, `parseFloatList`, `deriveGameSeed`, `SEED_GAME_INDEX_MULTIPLIER`, `resolveGitSha`, `buildTimestamp` are named exports of calibration-harness.mjs (grep: `grep -Ec '^export (const|function) (requireFlagValue|parsePositiveIntFlag|parseIntList|parseFloatList|deriveGameSeed|SEED_GAME_INDEX_MULTIPLIER|resolveGitSha|buildTimestamp)\b' scripts/calibration-harness.mjs` returns 8).
    - No duplicated adjudication constants: ADJUDICATION_CP_THRESHOLD/ADJUDICATION_SUSTAIN_PLIES/PLY_CAP are still declared once, in calibration-harness.mjs, and imported by the loop module.
  </acceptance_criteria>
  <done>The game loop is a single mover-agnostic implementation; the bot harness delegates to it with identical observable behavior; the two-mover loop is proven on synthesized fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → CLI flags | Only untrusted input is CLI flags typed by Adrian himself; no network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-01 | Tampering | Extracted game loop changes harness behavior | medium | mitigate | Existing `calibration-determinism.check.mjs` + `calibration-pruning.check.mjs` must pass unchanged (D-08 no-behavior-change gate). |
| T-173-02 | Denial of Service | Malformed CLI flag silently coerced | low | accept | This plan only touches libs/checks (no new CLI surface); the existing fail-loud `requireFlagValue` discipline is preserved by re-exporting, not rewriting. |
| T-173-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages installed in this phase (stdlib/existing-modules only, D-07); no supply-chain surface. |
</threat_model>

<verification>
- Run all three Node checks in the Task 2 verify command; all exit 0.
- `git diff scripts/calibration-harness.mjs` shows playGame delegating (no inline loop), the four CLI helpers exported, and adjudication constants still single-sourced.
</verification>

<success_criteria>
- A mover-agnostic `playTwoMoverGame` exists and is proven on synthesized fixtures.
- The bot harness is byte-identical (determinism + pruning checks green).
- sf8/sf10 are valid anchor tokens; the anchors check is green.
</success_criteria>

<output>
Create `.planning/phases/173-anchor-ladder-self-calibration-seed-101/173-01-SUMMARY.md` when done.
</output>
