---
phase: 169.5-bot-opening-book
verified: 2026-07-13T18:24:36Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: null
---

# Phase 169.5: Bot Opening Book Verification Report

**Phase Goal:** In the opening the bot answers from a book instead of searching, so its early moves are near-instant, cost it almost none of its clock, and vary across games.
**Verified:** 2026-07-13T18:24:36Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | While in book, bot runs no MCTS search / no Stockfish leaf evals; reply lands in reveal band; clock debit is a small fraction of a searched move | ✓ VERIFIED | `useBotGame.ts:829-834` (`resolveMove` calls `resolveBookMove` first, only falls through to `selectBotMove` on `null`); `useBotGame.ts:933` (`if (fromBook) return;` suppresses the post-commit `pool.grade` call unconditionally on book plies); `useBotGame.ts:853` (`delay(Math.min(computeRevealDelayMs(Math.random), deadlineMs))` — reveal band from `chessClock.ts:63,67` `REVEAL_DELAY_MIN_MS=500`/`REVEAL_DELAY_MAX_MS=1500`); test `useBotGame.test.ts:874-903` asserts `mockSelectBotMove` and `mockGrade` are BOTH never called on a book ply, with a positive companion proving the search re-engages when the book declines; test `useBotGame.test.ts:905-947` asserts the book debit stays inside the reveal band and is ≥3x cheaper than a searched move through the identical commit pipeline (`commitMove` at `useBotGame.ts:918`, shared, no bypass path) |
| 2 | Book candidates are exactly the legal moves keeping the game a prefix of an `openings.tsv` line; move is sampled weighted by Maia policy at the bot's ELO | ✓ VERIFIED | `openingBook.ts:106-117` (`getBookCandidates` filters legal moves by `prefixSet.has([...history, san].join(' '))`); `openings.ts:37-74` (`buildLookup` builds `prefixSet` from every prefix of every `openings.tsv` line, single parse); `frontend/public/openings.tsv` confirmed 3,641 data lines (3,642 incl. header) matching the roadmap's stated corpus size; `openingBook.ts:87-94` (`maiaPolicyWeighting` restricts the raw Maia policy to book-candidate UCIs, `samplePolicy` in `botSampling.ts:55-56` draws proportionally — not argmax, not uniform); `openingBook.test.ts` `getBookCandidates` tests (lines ~40-60 area) pass |
| 3 | Repeated games do not always open identically; opening is ECO-nameable | ✓ VERIFIED | `openingBook.test.ts:129-170` (`describe('variety')`) — 400 seeded `mulberry32` draws over unequal stubbed weights; asserts ≥2 distinct moves AND each candidate's empirical frequency tracks its stubbed weight within an 8% tolerance band (rules out both an argmax collapse and a uniform spray). Independently re-ran this test live: passes. Since candidates are drawn only from `prefixSet` membership (built from real `openings.tsv` lines), any book move is by construction a prefix of a named ECO line, satisfying "nameable" |
| 4 | Bot leaves book — and resumes normal search — as soon as no in-book continuation clears the Maia policy floor, or the ply cap is reached, whichever comes first; leaving book is one-way within a game | ✓ VERIFIED | `openingBook.ts:139` (ply cap check, `BOOK_PLY_CAP=16`), `:142` (empty candidates), `:154-155` (raw-policy floor check against `BOOK_POLICY_FLOOR=0.05`, deliberately computed on `rawPolicy` before `weighting()` — see in-code comment on why post-renormalization floor checks are un-triggerable for a single candidate); `useBotGame.ts:341` (`hasLeftBookRef`), `:830-834` (latch set to `true` at the single decline point, and gates all subsequent turns via `if (!hasLeftBookRef.current)`); `useBotGame.ts:653` (`hasLeftBookRef.current = false` in `newGame()`). **Independently mutation-tested**: removed the `hasLeftBookRef.current = true` line from `useBotGame.ts` and re-ran `useBotGame.test.ts -t "one-way exit"` — the test goes RED (`expected mockSelectBotMove to be called 2 times, but got 1` — the book re-engaged on turn 3 instead of staying left). File restored via `git checkout --`, confirmed clean (`git status`) |
| 5 | Stockfish `WorkerPool` + Maia session are warmed during the book window, so the first searched move doesn't additionally pay worker-spawn + ONNX-load cold start | ✓ VERIFIED | `workerPool.ts:109-124` (`warm(): void` on the `WorkerPool` interface, implemented as bare `ensureSpawned()` forward at `:479-481`); `maiaQueue.ts:59-72`/`:295-297` (same shape); `useBotGame.ts:774-775` (`pool.warm(); queue.warm();` called in the provider bring-up effect, which runs on mount — i.e. during the book window, not gated on any move having happened). **Independently mutation-tested the `grade(fen, [])` no-op trap** the doc comment warns about: replaced `warm()`'s body with `void grade('startpos', [])` in `workerPool.ts` and re-ran `workerPool.test.ts -t "warm"` — 3 of 3 warm-related tests go RED (0 workers spawned instead of 4). File restored, confirmed clean |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified)

### Locked-Decision Spot-Checks

| Decision | Check | Result |
|---|---|---|
| D-05 (`applyPolicyTemperature` must NOT be used in the book path) | `grep -rn "applyPolicyTemperature"` across `openingBook.ts`, `useBotGame.ts`'s book branch, `botSampling.ts` | Zero hits. The only importers/callers of `applyPolicyTemperature` are `mctsSearch.ts` and `fallbackExpectimax.ts` — the analysis/search path, untouched by this phase. Confirmed. |
| Scope containment (04-SUMMARY claim: no `app/`/`scripts/` files touched, `botBudget.ts`/`mctsSearch.ts`/`selectBotMove.ts`/`chessClock.ts` byte-unchanged) | `git diff --stat $(git merge-base main HEAD) HEAD` | Confirmed — the 32 changed files are all under `.planning/` or `frontend/src/{hooks,lib}/`; no `app/`, no `scripts/`, none of the four named files appear in the diff. |
| Corpus size claim (3,641 lines) | `wc -l frontend/public/openings.tsv` | 3,642 lines total (header + 3,641 data rows) — matches. |
| Queens-off edge case (`openings.tsv:1065`) | `sed -n '1065p' openings.tsv` | `B10 Caro-Kann Defense: Endgame Variation — 1. e4 c6 2. Nf3 d5 3. d3 dxe4 4. dxe4 Qxd1+ 5. Kxd1` — 9 plies, queens traded at plies 8-9, inside `BOOK_PLY_CAP=16`. Confirms the D-06 draw-gate sentinel fix (`lastRootPracticalScoreRef: number \| null`, `wouldBotAcceptDraw` refusing on `null`) is addressing a real, reachable case, not a hypothetical. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `frontend/src/lib/openings.ts` | `loadOpeningPrefixSet()`, `OpeningLookup{fullLineMap,prefixSet}` | ✓ VERIFIED | Present, substantive, wired (imported by `useBotGame.ts` and `openingBook.test.ts`'s indirect real-corpus test) |
| `frontend/src/lib/engine/openingBook.ts` | `getBookCandidates`, `selectBookMove`, `BookWeightingFn`, `maiaPolicyWeighting`, `BOOK_POLICY_FLOOR`, `BOOK_PLY_CAP` | ✓ VERIFIED | Present, substantive (161 lines, no stubs), wired into `useBotGame.ts`'s `resolveBookMove` |
| `frontend/src/lib/engine/workerPool.ts` | `WorkerPool.warm()` | ✓ VERIFIED | Present, substantive, wired (called in `useBotGame.ts` mount effect); mutation-tested |
| `frontend/src/lib/engine/maiaQueue.ts` | `MaiaQueue.warm()` | ✓ VERIFIED | Present, substantive, wired (called in `useBotGame.ts` mount effect) |
| `frontend/src/hooks/useBotGame.ts` | book branch, `hasLeftBookRef`, prewarm-on-mount, `fromBook` gate | ✓ VERIFIED | Present, substantive, wired; mutation-tested (latch) |
| `frontend/src/lib/botDrawGate.ts` | `wouldBotAcceptDraw(number \| null, ...)` sentinel | ✓ VERIFIED | Present, substantive, wired; unit test covers the queens-off + null case |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `useBotGame.ts` `resolveMove()` | `openingBook.ts` `selectBookMove` | `resolveBookMove()` module helper | ✓ WIRED | Live SAN history (`chess.history()`), live legal moves, prefix set, raw Maia policy all passed through correctly |
| `useBotGame.ts` mount effect | `workerPool.ts`/`maiaQueue.ts` `.warm()` | Direct call in provider bring-up effect | ✓ WIRED | `pool.warm(); queue.warm();` at `useBotGame.ts:774-775`, unconditional on mount |
| `useBotGame.ts` post-commit grade | `botDrawGate.ts` `wouldBotAcceptDraw` | `lastRootPracticalScoreRef` (now `number \| null`) | ✓ WIRED | `fromBook` gate suppresses the ref update on book plies; `wouldBotAcceptDraw` refuses on the `null` sentinel — verified by dedicated hook test using the real queens-off ECO line |
| `openings.ts` `buildLookup` (single parse) | both `fullLineMap` (naming) and `prefixSet` (book) | Shared `OpeningLookup` return | ✓ WIRED | One fetch/parse serves both consumers; `findOpening`/`preloadOpenings` regression-checked unchanged |

### Behavioral Spot-Checks (independently re-run, not trusted from SUMMARY)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full 6-file test suite for this phase | `npx vitest run <6 files>` | 6 files / 107 tests, all passing | ✓ PASS |
| One-way latch is load-bearing | Removed `hasLeftBookRef.current = true`, re-ran `-t "one-way exit"` | Test goes RED (1 vs 2 expected calls) | ✓ PASS (mutation confirmed) |
| `grade(fen, [])` no-op trap is real and `warm()` avoids it | Replaced `warm()` body with `void grade('startpos', [])`, re-ran `-t "warm"` | 3/3 warm tests go RED (0 workers spawned) | ✓ PASS (mutation confirmed) |
| `tsc -b` clean | `npx tsc -b` | Zero output / exit 0 | ✓ PASS |
| No `applyPolicyTemperature` in book path | `grep -rn` across book files | Zero hits outside `mctsSearch.ts`/`fallbackExpectimax.ts` | ✓ PASS |
| Scope containment | `git diff --stat` vs `main` | Only `.planning/` + `frontend/src/{hooks,lib}/`; no `app/`/`scripts/` | ✓ PASS |

Both source files mutated during verification (`useBotGame.ts`, `workerPool.ts`) were restored via `git checkout --` immediately after their respective test runs; `git status --short` confirmed clean before proceeding.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| PLAY-11 | 169.5-01..04 | Bot plays from a book in the opening — near-instant, no search budget, cheap clock, varied | ✓ SATISFIED (code) | All 5 roadmap success criteria verified above. **Note:** `.planning/REQUIREMENTS.md:31` still shows `PLAY-11` as an unchecked `- [ ]` checkbox with status `Pending` in its coverage table (line 103) — this is a documentation-sync gap, not a code gap (consistent with this project's convention of updating REQUIREMENTS.md at a separate step, not at phase-verify time; PLAY-01/02/10 for the still-open Phases 170/171 show the same unchecked pattern). Flagged for the ship/close step to update, not a phase-goal blocker. |

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers, no empty handlers, no hardcoded-empty stub returns in any of the 8 modified/created source files (`openings.ts`, `openingBook.ts`, `workerPool.ts`, `maiaQueue.ts`, `useBotGame.ts`, `botDrawGate.ts`, plus their 6 test files).

### Human Verification Required

None required for this verification pass — the phase's own UAT (`169.5-UAT.md`) already recorded a passed live-browser confirmation (checkpoint 19: near-instant varied nameable openings across several games, normal search resumption after leaving book), and every other deliverable (1-18) is deterministically covered by unit/hook tests that this verification independently re-ran and, for the two highest-risk behaviors (one-way latch, warm() no-op trap), independently mutation-tested against the committed code rather than trusting the SUMMARY narration.

### Gaps Summary

No gaps. All 5 ROADMAP success criteria are verified against the actual implementation (not just SUMMARY claims), the two most failure-prone behaviors (one-way book exit latch; engine prewarm avoiding the `grade(fen, [])` silent no-op) were independently reverted and confirmed to turn the relevant test red, the D-05 locked decision (`applyPolicyTemperature` excluded from the bot path) holds, and the diff is scope-contained to exactly the files the SUMMARYs claim. The only discrepancy found — `REQUIREMENTS.md`'s PLAY-11 checkbox/status not yet flipped — is a documentation bookkeeping item, not a functional gap, and does not block phase completion.

---

_Verified: 2026-07-13T18:24:36Z_
_Verifier: Claude (gsd-verifier)_
