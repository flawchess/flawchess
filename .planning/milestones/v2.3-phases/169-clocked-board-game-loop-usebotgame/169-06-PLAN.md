---
phase: 169-clocked-board-game-loop-usebotgame
plan: 06
type: execute
wave: 4
depends_on: [169-04, 169-05]
files_modified:
  - frontend/src/components/bots/GameResultDialog.tsx
  - frontend/src/components/bots/GameResultStrip.tsx
  - frontend/src/pages/Bots.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [PLAY-09]
must_haves:
  truths:
    - "On game end a dismissible result dialog shows the outcome+reason (colored win/loss/draw) with 'New game' and 'Analyze this game' actions"
    - "Dismissing the dialog reveals the final position and a persistent inline result strip keeps both actions reachable"
    - "'Analyze this game' deep-links to /analysis with the game's move line; 'New game' restarts the hardcoded-settings game"
    - "The game ships on a real lazy-loaded /bots route, unlinked from nav, behind a minimal hardcoded-settings start stub"
  artifacts:
    - frontend/src/components/bots/GameResultDialog.tsx
    - frontend/src/components/bots/GameResultStrip.tsx
    - frontend/src/pages/Bots.tsx
    - frontend/src/App.tsx (lazy /bots route)
  key_links:
    - "Bots.tsx wires useBotGame (plan 04) + ClockDisplay/MoveListPanel/GameControls (plan 05) + result components + ChessBoard, gating board input on turn+live"
    - "Analyze CTA uses buildAnalysisLineUrl (existing); New game restarts via useBotGame.newGame()"
    - "Route is lazy + unlinked from nav (D-14)"
---

<objective>
Build the game-end result UI (`GameResultDialog`, `GameResultStrip`) and the
`/bots` page + route that assembles the whole phase: `ChessBoard` (turn/live
gated) + `ClockDisplay` x2 + `MoveListPanel` + `GameControls` + the result
surfaces, driven by `useBotGame` behind a minimal hardcoded-settings start
stub. Registers the lazy `/bots` route unlinked from nav (D-14).

Purpose: The assembly layer that makes the phase playable end-to-end in the
browser for UAT. Delivers the D-11 result dialog + strip, the D-12 Analyze
deep-link, and the D-14 stub route.

Output: two result components, `Bots.tsx`, and the `App.tsx` lazy-route
registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md
@frontend/src/pages/Analysis.tsx
@frontend/src/App.tsx
@frontend/src/lib/analysisUrl.ts
@frontend/src/components/board/ChessBoard.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/lib/theme.ts
@frontend/src/hooks/useBotGame.ts
@frontend/src/lib/sounds.ts
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — not drift):
- `frontend/src/components/bots/GameResultDialog.tsx`: `GameResultDialog` (props: outcome, userColor, open, onDismiss, onNewGame, onAnalyze)
- `frontend/src/components/bots/GameResultStrip.tsx`: `GameResultStrip` (props: outcome, userColor, onNewGame, onAnalyze)
- `frontend/src/pages/Bots.tsx`: default-exported `BotsPage` (D-14 stub with hardcoded BotGameSettings)
- `frontend/src/App.tsx`: NEW lazy import `BotsPage = lazy(() => import('./pages/Bots'))` + a `<Route path="/bots" ...>` entry, unlinked from nav
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GameResultDialog + GameResultStrip (D-11, D-12, PLAY-09)</name>
  <files>frontend/src/components/bots/GameResultDialog.tsx, frontend/src/components/bots/GameResultStrip.tsx</files>
  <read_first>
    - frontend/src/components/ui/dialog.tsx (the Dialog primitive: close-on-X + close-on-outside; action-row convention primary-on-right)
    - frontend/src/lib/theme.ts (WDL_WIN/WDL_DRAW/WDL_LOSS — the outcome title colors)
    - frontend/src/lib/botGameEnd.ts (resultCopy(outcome, userColor) + BotGameOutcome — plan 02; the dialog title text comes from here, not re-derived)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md (GameResultDialog + GameResultStrip sections: title colored by WDL token, body terse, actions "New game" variant=default primary-right + "Analyze this game" variant=brand-outline; strip = thin --secondary bar, Body-size text, same two actions; Copywriting Contract)
  </read_first>
  <action>
    Create GameResultDialog.tsx using the existing ui/dialog Dialog. Title =
    resultCopy(outcome, userColor) (plan 02) colored via the WDL token matching
    the outcome (win→WDL_WIN, loss→WDL_LOSS, draw→WDL_DRAW) as text color only
    (not a full-dialog tint). Body is terse (no extra prose — the title carries
    the message, per popover-copy-minimalism). Actions row: "New game"
    (variant="default", brand primary, on the right) calling onNewGame, and
    "Analyze this game" (variant="brand-outline", secondary) calling onAnalyze.
    Dialog is dismissible via X / outside-click (onDismiss) revealing the final
    board underneath (D-11). Create GameResultStrip.tsx: a thin inline bar
    (--secondary bg, sm vertical / md horizontal padding) with the same
    resultCopy text at Body size (text-sm) and the same two actions as compact
    buttons; persists post-dismiss until New game (D-11). data-testids:
    result-dialog, btn-new-game, btn-analyze-game (dialog) and
    result-strip + strip-scoped action testids. text-sm floor throughout.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Both components derive their outcome text from resultCopy (grep confirms import from botGameEnd), not hard-coded strings.
    - Title color uses WDL_WIN/WDL_LOSS/WDL_DRAW from theme (grep confirms).
    - "New game" is variant="default" and "Analyze this game" is variant="brand-outline" in both surfaces.
    - data-testids result-dialog, result-strip, btn-new-game, btn-analyze-game present.
    - `grep -n "text-xs" frontend/src/components/bots/GameResult*.tsx` returns nothing.
    - `cd frontend && npx tsc -b && npm run lint` clean.
  </acceptance_criteria>
  <done>The result dialog (dismissible, WDL-colored title, New game + Analyze actions) and the persistent post-dismiss strip both render resultCopy from botGameEnd and use the correct Button variants (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: Bots.tsx page — assemble board + clocks + move list + controls + result surfaces behind the D-14 stub</name>
  <files>frontend/src/pages/Bots.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lazy-route page structure, ChessBoard + hook wiring, responsive board+side-panel layout, deep-link param handling)
    - frontend/src/hooks/useBotGame.ts (the full state+callbacks contract this page consumes — plan 04)
    - frontend/src/components/board/ChessBoard.tsx (onPieceDrop `(source,target)=>boolean` — the page passes useBotGame.attemptMove; when viewing a scrolled-back ply the page shows the viewed FEN and attemptMove already returns false off-live)
    - frontend/src/lib/analysisUrl.ts (buildAnalysisLineUrl — the D-12 Analyze CTA target; call navigate(buildAnalysisLineUrl(sanMoveHistory)))
    - frontend/src/lib/sounds.ts (unlockAudio — call on the page's first user gesture / Start; useMuted for the GameControls mute prop)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md (D-13 board input disabled off live; D-14 hardcoded-settings stub — Claude's discretion on exact ELO/blend/TC; promotion auto-queen)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md (layout: bot clock above board / user clock below on mobile, side column on desktop; visual hierarchy — board is primary)
  </read_first>
  <action>
    Create frontend/src/pages/Bots.tsx (default export for lazy loading).
    D-14 stub: instantiate useBotGame with a hardcoded BotGameSettings
    (Claude's discretion — e.g. botElo 1500, blend 0.5, a lichess 5+3 preset =
    baseSeconds 300 / incrementSeconds 3, userColor 'white'); a minimal "Start
    game"/"New game" affordance is acceptable but the stub may auto-start on
    load (Phase 171 replaces this with the real setup screen). Render:
    ChessBoard (position = the viewed FEN from the hook; onPieceDrop =
    attemptMove; flipped when userColor black), two ClockDisplay instances
    (bot above / user below on mobile, side column on desktop per UI-SPEC),
    MoveListPanel (wired to liveGamePly/viewedPly/viewPly/returnToLive),
    GameControls (canOfferDraw/muted from hook+useMuted; onToggleMute via
    setMuted; onResignConfirmed=resign; onOfferDraw=offerDraw). On game end,
    show GameResultDialog (open until dismissed) and, once dismissed,
    GameResultStrip; both onNewGame=newGame, onAnalyze=navigate(
    buildAnalysisLineUrl(moveHistory)) (D-12). Call unlockAudio() on the first
    user interaction (Pitfall 4). Board input is naturally disabled off the live
    position because attemptMove returns false there (D-13). data-testid the
    page container "bots-page". Keep the page a thin orchestrator — push any
    data-shaping into the hook; respect CLAUDE.md LOC/nesting (split desktop/
    mobile render helpers if either exceeds ~40 logic LOC).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Bots.tsx default-exports a page and calls useBotGame with a hardcoded BotGameSettings (grep shows the settings object; TC stored as base-seconds+increment-seconds, not a minutes label).
    - ChessBoard onPieceDrop is wired to useBotGame.attemptMove.
    - The Analyze action calls buildAnalysisLineUrl (grep confirms) — the D-12 deep-link.
    - unlockAudio is called on a user gesture (grep confirms).
    - data-testid="bots-page" present on the page container.
    - `cd frontend && npx tsc -b && npm run lint` clean.
  </acceptance_criteria>
  <done>Bots.tsx assembles the board (turn/live-gated), dual clocks, move list, controls, and result surfaces behind a hardcoded-settings stub, with the Analyze deep-link and audio unlock wired (D-12/D-14).</done>
</task>

<task type="auto">
  <name>Task 3: Register the lazy /bots route in App.tsx, unlinked from nav (D-14)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (line 42 `const AnalysisPage = lazy(...)` pattern; lines 713-737 the <Routes> block + how /analysis is registered via AnalysisRoute; the ProtectedLayout wrapper; note /analysis is NOT under ImportRequiredRoute so guest/zero-game users can reach it)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md (D-14 — real /bots route, lazy-loaded, UNLINKED from nav; guest flow is Phase 171 so keep the route reachable without an import requirement, mirroring /analysis)
    - frontend/src/pages/Bots.tsx (the default export to lazy-import — Task 2)
  </read_first>
  <action>
    In frontend/src/App.tsx add `const BotsPage = lazy(() => import('./pages/Bots'));`
    alongside the other lazy page imports (mirror the AnalysisPage line). Add a
    `<Route path="/bots" element={<BotsPage />} />` in the <Routes> block. Match
    /analysis's access posture: reachable without ImportRequiredRoute (free/
    guest play is valid — D-14 note; full guest wiring is Phase 171) and not
    behind SuperuserRoute. Do NOT add any nav link / `nav-*` data-testid for
    /bots (D-14: unlinked from nav this phase; Phase 171 adds the nav entry).
    Place it consistently with the existing route ordering. Change nothing else
    in App.tsx.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import('./pages/Bots')" frontend/src/App.tsx` succeeds (lazy import added).
    - `grep -n 'path="/bots"' frontend/src/App.tsx` succeeds (route registered).
    - No `nav-` data-testid or nav link referencing /bots was added (grep for `/bots` shows only the lazy import + route).
    - `cd frontend && npx tsc -b` passes and `npm run build` succeeds (lazy chunk builds).
  </acceptance_criteria>
  <done>/bots is a lazy-loaded route reachable in the browser, unlinked from nav, with the same guest-friendly access posture as /analysis (D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /analysis deep-link | The Analyze CTA builds a client-side /analysis?line= URL from the local move history; no server call, no untrusted external input (moves are the user's own game). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-169-05 | Information Disclosure | /bots route exposed unlinked | low | accept | Route is client-only play with no server data; unlinked from nav intentionally (D-14) so it is discoverable only by direct URL for this phase's UAT. No sensitive data served. |
| T-169-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed (react-router/lucide already present). No supply-chain surface added. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npm run lint && npm run build` all succeed.
- `cd frontend && npm test -- --run` full frontend suite green (wave-merge gate).
- Manual: visiting /bots renders a playable clocked game; game-end shows the dialog then strip; Analyze navigates to /analysis with the line.
</verification>

<success_criteria>
/bots is a playable, lazy-loaded, nav-unlinked clocked bot game with a
dismissible WDL-colored result dialog + persistent strip and a working Analyze
deep-link + New game restart (D-11/D-12/D-14, PLAY-09).
</success_criteria>

<output>
Create `.planning/phases/169-clocked-board-game-loop-usebotgame/169-06-SUMMARY.md` when done.
</output>
