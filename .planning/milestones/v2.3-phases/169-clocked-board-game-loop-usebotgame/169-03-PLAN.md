---
phase: 169-clocked-board-game-loop-usebotgame
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/sound/
  - frontend/src/lib/sounds.ts
  - frontend/src/lib/__tests__/sounds.test.ts
  - README.md
autonomous: true
requirements: [PLAY-08]
must_haves:
  truths:
    - "Move, capture, check, and game-end sounds are vendored and play on demand"
    - "Two extra events exist: a once-only low-time warning and a bot-draw-declined blip (D-09)"
    - "Sounds default ON; a boolean mute toggle persists to localStorage across reloads and works for guests"
    - "The audio unlock workaround exists so bot-initiated sounds fire on iOS Safari after the first user gesture"
  artifacts:
    - frontend/public/sound/ (vendored AGPLv3+ lila sfx set)
    - frontend/src/lib/sounds.ts
    - frontend/src/lib/__tests__/sounds.test.ts
    - README.md (## Sound Assets attribution section)
  key_links:
    - "sounds.ts play/useMuted/setMuted are consumed by useBotGame (plan 04, fires events) and GameControls (plan 05, mute toggle)"
---

<objective>
Build the net-new client audio module `sounds.ts` plus the vendored,
license-corrected lila sound assets, covering PLAY-08 (move/capture/check/
game-end sounds + mute) and D-09's two extra events (low-time warning,
draw-declined blip). No audio infrastructure exists in the frontend today —
this is a small standalone util future surfaces can reuse.

Purpose: A thin HTMLAudioElement module with a guest-usable, default-ON,
localStorage-persisted mute toggle and an iOS autoplay-unlock workaround, so
the plan-04 hook can fire one-shot sounds on game events.

Output: `frontend/public/sound/*` (vendored assets), `sounds.ts`,
`sounds.test.ts`, and a README "## Sound Assets" attribution section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md
@frontend/src/hooks/useUserFlag.ts
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — not drift):
- `frontend/public/sound/` — vendored AGPLv3+ lila `sfx` clips (Move, Capture, Check, game-end variants, LowTime, GenericNotify) in mp3 (+ogg optional)
- `frontend/src/lib/sounds.ts`: type `SoundEvent` ('move'|'capture'|'check'|'game-end'|'low-time'|'draw-declined'); constant `MUTE_KEY='flawchess_bot_sound_muted'`; functions `playSound(event)`, `useMuted()`, `setMuted(muted)`, `unlockAudio()`
- `frontend/src/lib/__tests__/sounds.test.ts`
- `README.md` "## Sound Assets" section
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the license-correct lila sfx sound set + README attribution (D-08 corrected)</name>
  <files>frontend/public/sound/, README.md</files>
  <read_first>
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md (Pitfall 1 — D-08's "standard" set is NON-FREE; vendor `sfx` (or piano/futuristic/nes), all AGPLv3+ by Enigmahack, IDENTICAL filenames; Open Question 1 — sfx is the most likely tonal fit)
    - README.md (the existing "## Engine Binaries (GPLv3 License Note)" section to mirror for the new "## Sound Assets" section)
    - frontend/public/ (existing public asset layout — engine/, maia/ dirs)
  </read_first>
  <action>
    Vendor lila's AGPLv3+ `sfx` sound directory into frontend/public/sound/
    (NOT the non-free "standard" set D-08 names literally — RESEARCH Pitfall 1
    is the license correction; cite it in the commit + README). Fetch from
    github.com/lichess-org/lila/tree/master/public/sound/sfx the clips needed:
    Move, Capture, Check, Checkmate, Victory, Defeat, Draw (game-end variants),
    LowTime (D-09 low-time warning), GenericNotify (D-09 draw-declined blip).
    Prefer .mp3 (add .ogg if trivially available). Add a "## Sound Assets"
    section to README.md crediting "Enigmahack" and the AGPLv3+ license,
    mirroring the existing "## Engine Binaries (GPLv3 License Note)" precedent —
    this is a licensing REQUIREMENT (D-08), not decoration. No build step;
    these are static public assets served directly.
  </action>
  <verify>
    <automated>test -f frontend/public/sound/Move.mp3 && test -f frontend/public/sound/Capture.mp3 && test -f frontend/public/sound/Check.mp3 && test -f frontend/public/sound/LowTime.mp3 && test -f frontend/public/sound/GenericNotify.mp3 && grep -q "Sound Assets" README.md && echo VENDORED_OK</automated>
  </verify>
  <acceptance_criteria>
    - Files Move.mp3, Capture.mp3, Check.mp3, LowTime.mp3, GenericNotify.mp3 (and at least one game-end clip: Checkmate/Victory/Defeat/Draw) exist under frontend/public/sound/.
    - README.md contains a "## Sound Assets" section crediting Enigmahack + AGPLv3+ and naming the `sfx` source directory (not "standard").
    - No new npm package added (grep frontend/package.json shows no howler/audio dependency added).
  </acceptance_criteria>
  <done>The AGPLv3+ lila sfx clips are vendored under frontend/public/sound/ with README attribution; the non-free "standard" set is NOT used (Pitfall 1 correction applied).</done>
</task>

<task type="auto">
  <name>Task 2: sounds.ts audio module — event playback, mute persistence, iOS unlock (PLAY-08, D-09/D-10)</name>
  <files>frontend/src/lib/sounds.ts, frontend/src/lib/__tests__/sounds.test.ts</files>
  <read_first>
    - frontend/src/hooks/useUserFlag.ts (full file — the useSyncExternalStore + localStorage listener SHAPE to mirror; deviate: flat non-email-scoped key, real toggle semantics, default UNMUTED which inverts useUserFlag's default-false sense)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md (D-09 two extra events; D-10 default-ON boolean mute persisted, no volume slider, guest-usable)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md (Pitfall 4 iOS autoplay unlock; "Mute toggle persistence (D-10)" code-example section — new hook, NOT useUserFlag)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md (sounds.ts section — localStorage + useSyncExternalStore pattern)
  </read_first>
  <action>
    Create frontend/src/lib/sounds.ts exporting: a `SoundEvent` union type
    mapping to asset filenames (move→Move, capture→Capture, check→Check,
    game-end→a game-end clip, low-time→LowTime, draw-declined→GenericNotify);
    `MUTE_KEY='flawchess_bot_sound_muted'` (flat, non-email-scoped — guests need
    mute, D-10); `useMuted()`/`setMuted()` following useUserFlag's
    useSyncExternalStore+listener shape but with real toggle semantics and
    DEFAULT UNMUTED (storage '1' = muted, absence = unmuted — invert
    useUserFlag's sense, D-10); `playSound(event)` that no-ops when muted else
    plays a per-event preloaded `new Audio('/sound/<File>.mp3')`; `unlockAudio()`
    that plays+immediately pauses each Audio instance once (Pitfall 4 iOS
    autoplay unlock, to be called from the first user gesture in plan 04/06).
    Wrap localStorage access in try/catch (mirror useUserFlag). Keep functions
    small. The two D-09 events (low-time, draw-declined) are distinct SoundEvent
    members. In the test file, stub the global Audio constructor with a mock
    exposing play/pause spies; assert: playSound('move') calls play with the
    Move asset; playSound no-ops after setMuted(true); setMuted round-trips
    through localStorage and notifies subscribers; unlockAudio calls play then
    pause on each instance; default state is unmuted when the key is absent.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/sounds.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/sounds.test.ts` passes.
    - A test asserts default (no localStorage key) is UNMUTED and playSound triggers Audio.play.
    - A test asserts playSound is a no-op after setMuted(true) and audible again after setMuted(false).
    - A test asserts setMuted persists to localStorage under MUTE_KEY and notifies useSyncExternalStore subscribers.
    - A test asserts unlockAudio calls play then pause on each preloaded clip (Pitfall 4).
    - SoundEvent includes 'low-time' and 'draw-declined' (D-09).
    - `cd frontend && npx tsc -b` passes.
  </acceptance_criteria>
  <done>sounds.ts plays per-event clips, honors a default-ON guest-usable persisted mute, exposes an iOS unlock, and includes the two D-09 events; its test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored third-party assets → repo | Sound files are copied from an external repo (lila) into frontend/public/. This is a supply-chain-adjacent ingestion of binary assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-169-SC | Tampering | Vendored lila `sfx` audio assets (binary, from GitHub) | medium | mitigate | Source only from the known lila repo path `public/sound/sfx` (AGPLv3+, Enigmahack) verified in RESEARCH Pitfall 1; record provenance + license in the README "## Sound Assets" section; assets are inert audio (no executable code). No npm/pip/cargo package is installed — the SC vector here is asset provenance, mitigated by pinned-path sourcing + attribution. |
| T-169-02 | Denial of Service | localStorage access throwing (private-mode/quota) | low | mitigate | All localStorage reads/writes wrapped in try/catch (mirroring useUserFlag) so a storage failure degrades to default-unmuted, never crashes playback. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/sounds.test.ts` green.
- Vendored files present under frontend/public/sound/; README "## Sound Assets" section present.
- `cd frontend && npx tsc -b && npm run lint` clean for the new module.
</verification>

<success_criteria>
The required four sounds plus the two D-09 extras play through a mute-aware
module with default-ON guest-persisted mute and an iOS unlock path; assets are
the license-correct AGPLv3+ sfx set with README attribution (PLAY-08, D-08/09/10).
</success_criteria>

<output>
Create `.planning/phases/169-clocked-board-game-loop-usebotgame/169-03-SUMMARY.md` when done.
</output>
