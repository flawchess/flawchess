---
phase: 169-clocked-board-game-loop-usebotgame
plan: 05
type: execute
wave: 3
depends_on: [169-04]
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/components/bots/ClockDisplay.tsx
  - frontend/src/components/bots/MoveListPanel.tsx
  - frontend/src/components/bots/GameControls.tsx
autonomous: true
requirements: [PLAY-03, PLAY-07, PLAY-08]
must_haves:
  truths:
    - "Each side has a clock card showing tabular-nums digits, an active-side highlight, a bot-thinking pulsing dot, and a low-time red/tenths urgent state"
    - "A linear SAN move list highlights the live ply, supports click + arrow-key view-only scroll-back, and offers a visible return-to-live affordance"
    - "A controls row exposes Resign (two-step confirm), Offer draw (throttle-disabled with tooltip), and a Mute toggle, each with data-testid + aria-label"
  artifacts:
    - frontend/src/lib/theme.ts (CLOCK_LOW_TIME_URGENT constant)
    - frontend/src/components/bots/ClockDisplay.tsx
    - frontend/src/components/bots/MoveListPanel.tsx
    - frontend/src/components/bots/GameControls.tsx
  key_links:
    - "These components render useBotGame state (plan 04) and are assembled by Bots.tsx (plan 06)"
    - "Low-time color comes from theme.ts, never hard-coded (CLAUDE.md)"
---

<objective>
Build the three in-game presentational components that render `useBotGame`
state (plan 04): `ClockDisplay` (dual clocks + thinking dot + low-time urgent),
`MoveListPanel` (linear SAN list + view-only scroll-back), and `GameControls`
(resign confirm + throttled draw offer + mute toggle). Add the one new theme
constant `CLOCK_LOW_TIME_URGENT` these need.

Purpose: The visual/interaction layer of the live game, per the 169-UI-SPEC
contract. Covers the clock/thinking/low-time UI (D-06/D-07), the move-list
view-only scroll-back (D-13, PLAY-03), and the resign/draw/mute controls
(D-04/D-10, PLAY-07/PLAY-08).

Output: `theme.ts` (+1 constant), and three components under `components/bots/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md
@frontend/src/lib/theme.ts
@frontend/src/components/board/MoveList.tsx
@frontend/src/components/board/HorizontalMoveList.tsx
@frontend/src/components/board/PlayerBar.tsx
@frontend/src/components/board/BoardControls.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/tooltip.tsx
@frontend/src/lib/chessClock.ts
@frontend/src/lib/sounds.ts
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — not drift):
- `frontend/src/lib/theme.ts`: NEW constant `CLOCK_LOW_TIME_URGENT` (D-07 destructive-red low-time color)
- `frontend/src/components/bots/ClockDisplay.tsx`: `ClockDisplay` component (props: sideLabel, remainingMs, isActive, isThinking)
- `frontend/src/components/bots/MoveListPanel.tsx`: `MoveListPanel` component (props: moveHistory, liveGamePly, viewedPly, onViewPly, onReturnToLive)
- `frontend/src/components/bots/GameControls.tsx`: `GameControls` component (props: canOfferDraw, drawCooldownActive, muted, onResignConfirmed, onOfferDraw, onToggleMute)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: theme.ts CLOCK_LOW_TIME_URGENT + ClockDisplay (D-06/D-07)</name>
  <files>frontend/src/lib/theme.ts, frontend/src/components/bots/ClockDisplay.tsx</files>
  <read_first>
    - frontend/src/lib/theme.ts (WDL_WIN/WDL_LOSS + MOVE_HIGHLIGHT_SQUARE — the named semantic-color-constant precedent and file location for the new constant)
    - frontend/src/components/board/PlayerBar.tsx (the adjacent-to-board info-row layout/spacing conventions to mirror)
    - frontend/src/lib/chessClock.ts (formatClockLabel, isLowTime, LOW_TIME_THRESHOLD_MS — the display formatter + threshold; ClockDisplay must NOT reimplement formatting)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md (ClockDisplay section: Card + md padding, side label 14px/500, digits 20px/700 tabular-nums, active = --secondary tint NOT brand, thinking = brand-brown animate-pulse dot, low-time = --destructive tenths + ring; Typography + Color sections)
  </read_first>
  <action>
    Add a NEW exported constant CLOCK_LOW_TIME_URGENT to frontend/src/lib/theme.ts
    for D-07's low-time red (use the shadcn --destructive token value / an oklch
    red consistent with WDL_LOSS; add a doc comment citing D-07). Create
    frontend/src/components/bots/ClockDisplay.tsx: a single-side clock card
    (Card bg, md/16px padding, existing --radius) taking sideLabel, remainingMs,
    isActive, isThinking. Digits use formatClockLabel(remainingMs) from
    chessClock (do NOT reimplement) at text-xl/font-bold with tabular-nums
    (no jitter). Side label at text-sm/font-medium (text-sm floor — never shrink
    the clock, widen the card per UI-SPEC). Active side gets a --secondary
    background tint (NOT brand-brown — accent budget reserved). When isThinking,
    render a small brand-brown animate-pulse dot next to the label (D-06); add an
    aria-live "Bot is thinking" region for a11y (UI-SPEC). When
    isLowTime(remainingMs), color the digits with CLOCK_LOW_TIME_URGENT and add
    a ring-2 urgent treatment (D-07); the bot's clamped clock can show red but
    must not flicker at exactly 0:00 (never reaches true zero). Add
    data-testid="clock-<side>" and keep the component small/presentational.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/theme.ts` exports CLOCK_LOW_TIME_URGENT.
    - ClockDisplay imports formatClockLabel + isLowTime from chessClock (grep confirms) and does not reimplement clock formatting.
    - `grep -n "text-xs\|font-size" frontend/src/components/bots/ClockDisplay.tsx` returns nothing (text-sm floor).
    - No hard-coded low-time color hex/oklch literal in ClockDisplay (grep shows it imports CLOCK_LOW_TIME_URGENT from theme).
    - ClockDisplay uses tabular-nums for digits and renders the thinking dot only when isThinking.
    - `cd frontend && npx tsc -b && npm run lint` clean.
  </acceptance_criteria>
  <done>ClockDisplay renders a tabular-nums clock card with active tint, brand-brown thinking dot (D-06), and theme-sourced low-time red/tenths urgent state (D-07); the new theme constant exists.</done>
</task>

<task type="auto">
  <name>Task 2: MoveListPanel — linear SAN list + view-only scroll-back (D-13, PLAY-03)</name>
  <files>frontend/src/components/bots/MoveListPanel.tsx</files>
  <read_first>
    - frontend/src/components/board/MoveList.tsx (full file — the SAN-list mapping to HorizontalMoveItem, testId `move-${ply}`, ariaLabel; reuse near-verbatim)
    - frontend/src/components/board/HorizontalMoveList.tsx (the item renderer + onMoveClick contract)
    - frontend/src/hooks/useChessGame.ts (lines 262-279 — arrow-key nav guard against capturing keys while typing in an input; reuse)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md (MoveListPanel section: two-column ply layout, active LIVE-position row = brand-brown low-alpha highlight, view-only scroll-back with a visible "Return to live position" affordance — do NOT silently disable the board without it)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md (MoveListPanel section — two-state liveGamePly vs viewedPly model)
  </read_first>
  <action>
    Create frontend/src/components/bots/MoveListPanel.tsx taking moveHistory,
    liveGamePly, viewedPly, onViewPly(ply), onReturnToLive(). Reuse MoveList.tsx's
    mapping shape (numberLabel/san/isCurrent/testId `move-${ply}`/ariaLabel)
    near-verbatim. The active-highlight row is the LIVE position row when viewing
    live, and the viewedPly row when scrolled back — but the brand-brown
    active-ply highlight marks the live game position per UI-SPEC (background
    tint at low alpha). Clicking a row calls onViewPly(ply); wire ArrowLeft/Right
    keydown (guarded against INPUT/TEXTAREA/SELECT targets per useChessGame) to
    step viewedPly. When viewedPly !== liveGamePly, render a visible "Return to
    live position" text link/button (data-testid="btn-return-live") calling
    onReturnToLive — never silently disable the board without this affordance
    (D-13). text-sm floor for rows. Keep it presentational (all state lives in
    the plan-04 hook).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - MoveListPanel reuses the `move-${ply}` testId convention (grep confirms).
    - When viewedPly !== liveGamePly a "Return to live position" control with data-testid="btn-return-live" renders and calls onReturnToLive.
    - Arrow-key handler skips when the event target is INPUT/TEXTAREA/SELECT (grep confirms the guard).
    - `grep -n "text-xs" frontend/src/components/bots/MoveListPanel.tsx` returns nothing (text-sm floor).
    - `cd frontend && npx tsc -b && npm run lint` clean.
  </acceptance_criteria>
  <done>MoveListPanel shows a linear SAN list with a live-ply highlight, click + guarded arrow-key view-only scroll-back, and an always-visible return-to-live affordance (D-13).</done>
</task>

<task type="auto">
  <name>Task 3: GameControls — resign confirm, throttled draw offer, mute toggle (D-04/D-10, PLAY-07/08)</name>
  <files>frontend/src/components/bots/GameControls.tsx</files>
  <read_first>
    - frontend/src/components/board/BoardControls.tsx (Button variant + data-testid/aria-label conventions for a board-adjacent control row)
    - frontend/src/components/ui/dialog.tsx (the existing Dialog primitive — use for the resign confirmation, do NOT hand-roll a modal)
    - frontend/src/components/ui/button.tsx (variants: default=brand primary, brand-outline=secondary, destructive, ghost; icon sizes)
    - frontend/src/components/ui/tooltip.tsx (for the disabled draw-offer tooltip)
    - frontend/src/lib/sounds.ts (useMuted/setMuted — the mute state source)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-UI-SPEC.md (GameControls section: Resign brand-outline at rest → destructive confirm dialog; Offer draw brand-outline disabled+tooltip during cooldown; Mute ghost icon Volume2/VolumeX; Copywriting Contract exact strings; data-testid board-btn-resign/board-btn-offer-draw/board-btn-mute)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md (D-04 resign requires one confirmation; draw throttle; D-10 boolean mute)
  </read_first>
  <action>
    Create frontend/src/components/bots/GameControls.tsx taking canOfferDraw,
    drawCooldownActive, muted, onResignConfirmed(), onOfferDraw(),
    onToggleMute(). Resign is a two-step (D-04): the trigger button is
    variant="brand-outline" (NOT pre-colored destructive — CLAUDE.md
    primary/secondary rule), opening the existing ui/dialog Dialog with the
    exact UI-SPEC copy ("Resign this game?" / "You'll lose this game against the
    bot.") whose confirm button is variant="destructive" labeled "Resign" and
    calls onResignConfirmed; Cancel closes. Offer draw is variant="brand-outline",
    disabled when !canOfferDraw (cooldown) with a Tooltip reading "Wait a few
    more moves before offering again" (UI-SPEC); enabled → onOfferDraw. Mute is
    an icon-only ghost Button swapping lucide Volume2/VolumeX by `muted`, with
    aria-label "Mute sounds"/"Unmute sounds" (CLAUDE.md ARIA for icon buttons),
    calling onToggleMute. data-testid: board-btn-resign, board-btn-offer-draw,
    board-btn-mute (and a confirm-dialog testid). All labels text-sm floor.
    Apply the same markup to any mobile/desktop variant if the layout differs
    (CLAUDE.md mobile parity) — keep it one responsive row.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Resign trigger is variant="brand-outline"; the confirm action inside the Dialog is variant="destructive" (grep confirms both; trigger NOT pre-colored destructive).
    - Offer-draw button is disabled when !canOfferDraw and shows the cooldown Tooltip copy.
    - Mute button is icon-only ghost with aria-label toggling between "Mute sounds"/"Unmute sounds" and swaps Volume2/VolumeX.
    - data-testids board-btn-resign, board-btn-offer-draw, board-btn-mute all present.
    - Resign confirmation copy matches the UI-SPEC Copywriting Contract verbatim.
    - `cd frontend && npx tsc -b && npm run lint` clean.
  </acceptance_criteria>
  <done>GameControls gives a two-step destructive resign confirm, a cooldown-disabled draw offer with tooltip, and an aria-labeled mute toggle — all data-testid'd per CLAUDE.md and using existing Button/Dialog/Tooltip primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational components rendering hook state and invoking hook callbacks; no I/O, network, or input parsing beyond click/keydown that the hook validates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-169-04 | Tampering | Misclick resign (accidental game loss) | low | mitigate | D-04 two-step confirmation dialog (destructive confirm button) prevents a single-click fatal resign. |
| T-169-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed (lucide-react/radix already present). No supply-chain surface added. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npm run lint` clean for all four files.
- Manual/plan-06 UAT confirms the visual states (thinking dot, low-time red, resign confirm, disabled draw offer, mute swap).
</verification>

<success_criteria>
The in-game UI renders clocks with thinking + low-time states (D-06/D-07),
a view-only-navigable move list with return-to-live (D-13), and resign/draw/
mute controls (D-04/D-10) — all text-sm floor, theme-sourced colors, and
data-testid'd per CLAUDE.md.
</success_criteria>

<output>
Create `.planning/phases/169-clocked-board-game-loop-usebotgame/169-05-SUMMARY.md` when done.
</output>
