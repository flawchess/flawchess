---
phase: 183-persona-registry-bots-page
plan: 05
type: execute
wave: 3
depends_on: ["183-03", "183-04"]
files_modified:
  - frontend/src/components/bots/BotDrawOfferBanner.tsx
  - frontend/src/components/bots/ClockDisplay.tsx
  - frontend/src/components/bots/GameResultDialog.tsx
  - frontend/src/components/bots/GameResultStrip.tsx
  - frontend/src/lib/botGameEnd.ts
  - frontend/src/pages/Bots.tsx
  - frontend/src/components/bots/__tests__/BotDrawOfferBanner.test.tsx
  - frontend/src/components/bots/__tests__/ClockDisplay.test.tsx
autonomous: true
requirements: [PERS-02, AVAT-01, AVAT-02]
must_haves:
  truths:
    - "The bot clock strip shows the persona avatar + name for a persona game and falls back to 'FlawChess Bot' for a Custom game, on desktop AND mobile (D-06)"
    - "When the bot offers a draw, a non-blocking inline banner near the board reads '<Persona name> offers a draw' with Accept and Decline; Accept ends the game as an agreed draw, Decline dismisses it, and it auto-expires on the user's next move — play continues while it is up (D-07)"
    - "The result dialog and result strip name the persona in the outcome copy (e.g. 'Riko the Raccoon wins on time' / '<Persona> — checkmate') for a persona game, keeping the generic copy for a Custom game (D-06)"
    - "The result surfaces offer 'Rematch <Persona>' (same pinned config + same color/TC via handleStart) alongside the existing analyze action, and a 'New opponent' path back to the persona grid (D-08)"
    - "All persona-aware UI resolves identity through one shared personaFor(settings) helper — clock strip, banner, and result surfaces never re-implement the settings.personaId → PERSONA_REGISTRY lookup inline"
  artifacts:
    - frontend/src/components/bots/BotDrawOfferBanner.tsx
  key_links:
    - "personaFor(settings) → PERSONA_REGISTRY[settings.personaId] (shared, single lookup)"
    - "BotDrawOfferBanner Accept/Decline → useBotGame acceptBotDraw/declineBotDraw (Plan 03)"
    - "Rematch → BotsPage.handleStart(sameSettings)"
  backstop:
    - statement: "A resumed game whose personaId is no longer present in PERSONA_REGISTRY renders the unstyled 'FlawChess Bot' fallback everywhere (clock, banner, result copy) and never crashes"
      verification: backstop
  prohibitions:
    - statement: "Persona name/bio is rendered only as escaped JSX text — never via raw HTML injection"
      status: flagged-unverified
      verification: source-grep
    - statement: "'New opponent' reuses the existing onNewGame path (the grid is now the default setup view) — no parallel routing added"
      status: flagged-unverified
      verification: source-review
    - statement: "resultCopy is extended in-place (one copy-generation path) — no forked second copy generator; GameResultStrip renders the SAME persona-aware string as GameResultDialog (mobile parity)"
      status: flagged-unverified
      verification: source-review
---

<objective>
Carry the persona's presence through the live game and the post-game surfaces: avatar + name in the bot clock strip, the outgoing draw-offer banner (D-07's UI over Plan 03's state), persona-named result copy, and Rematch / New opponent. This closes the in-game half of the phase (D-06/D-07/D-08).

Purpose: PERS-02's "the persona is the complete opponent" only feels real if the character is visible while playing and named at the end. D-06 (presence), D-07 (draw banner), D-08 (rematch/new opponent).
Output: new BotDrawOfferBanner; extended ClockDisplay, GameResultDialog, GameResultStrip, botGameEnd.resultCopy, and Bots.tsx game render; a shared personaFor helper (add to personaRegistry.ts) used by every consumer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/183-persona-registry-bots-page/183-CONTEXT.md
@.planning/phases/183-persona-registry-bots-page/183-PATTERNS.md
@frontend/src/lib/personas/personaRegistry.ts
@frontend/src/hooks/useBotGame.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persona presence in the bot clock strip (D-06, desktop + mobile)</name>
  <files>frontend/src/components/bots/ClockDisplay.tsx, frontend/src/pages/Bots.tsx, frontend/src/components/bots/__tests__/ClockDisplay.test.tsx, frontend/src/lib/personas/personaRegistry.ts</files>
  <read_first>
    - frontend/src/components/bots/ClockDisplay.tsx (the sideLabel slot + the flex span at line 48 where an avatar composes beside the label and the thinking dot)
    - frontend/src/pages/Bots.tsx lines 312-349 (botClock/userClock construction; botClock currently hard-codes sideLabel="FlawChess Bot") and lines 421-423 (renderDesktopLayout/renderMobileLayout both receive botClock — one construction feeds both, so mobile parity is automatic)
    - frontend/src/lib/personas/personaRegistry.ts (add the shared personaFor helper here)
    - frontend/src/hooks/useBotGame.ts lines 159-193 (BotGameSettings incl. personaId?)
  </read_first>
  <action>
    Add `personaFor(settings: BotGameSettings): Persona | undefined` to `personaRegistry.ts` — `settings.personaId ? personaForId(settings.personaId) : undefined` — the ONE shared lookup every consumer uses (avoids the 4-call-site drift RESEARCH warns about). Extend `ClockDisplay` with an optional `avatarEmoji?: string` (and optional tint) rendered inside the existing label span beside `sideLabel` and the thinking dot — no layout restructuring, `text-sm` floor. In `Bots.tsx`, compute `const persona = personaFor(settings)` once and build `botClock` with `sideLabel={persona?.name ?? 'FlawChess Bot'}` and `avatarEmoji={persona ? placeholderAvatarFor(persona).emoji : undefined}`. Because the single botClock element feeds both renderDesktopLayout and renderMobileLayout, desktop + mobile parity is structural. Add ClockDisplay test cases: renders avatar + persona name when given them; renders the plain label when not. Add a Bots-level assertion (or extend the existing Bots test) that a persona game's bot clock shows the persona name and a Custom game shows 'FlawChess Bot'.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/bots/__tests__/ClockDisplay.test.tsx && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>The bot clock strip shows the persona avatar + name (persona game) or 'FlawChess Bot' (Custom), identically on desktop and mobile, via the shared personaFor helper.</done>
  <acceptance_criteria>
    - `npm test -- --run src/components/bots/__tests__/ClockDisplay.test.tsx` exits 0.
    - Behavior: ClockDisplay given an avatarEmoji + persona-name sideLabel renders both; given neither renders just the label (covered by test).
    - Source: `personaFor` is defined once in personaRegistry.ts and imported by Bots.tsx (no inline `settings.personaId ? PERSONA_REGISTRY[...] : undefined` ternary in components).
    - Source: no sub-`text-sm` utilities added to ClockDisplay.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BotDrawOfferBanner + wiring (D-07)</name>
  <files>frontend/src/components/bots/BotDrawOfferBanner.tsx, frontend/src/pages/Bots.tsx, frontend/src/components/bots/__tests__/BotDrawOfferBanner.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useBotGame.ts (the botDrawOffer state + acceptBotDraw/declineBotDraw callbacks from Plan 03; and lines 421-448 render region in Bots.tsx where the banner slots near the board/clocks)
    - frontend/src/components/bots/GameControls.tsx (Button variants + data-testid conventions for the Accept/Decline actions; brand-outline vs default per CLAUDE.md primary/secondary rule)
    - frontend/src/components/bots/ClockDisplay.tsx (near-board status-UI placement reference)
    - frontend/src/lib/personas/personaRegistry.ts (personaFor from Task 1)
  </read_first>
  <action>
    Create `BotDrawOfferBanner.tsx`: a non-blocking inline banner (`data-testid="bot-draw-offer-banner"`, semantic markup, `aria-live="polite"`) shown only when a bot offer is live, reading "<persona name> offers a draw" (falling back to "The bot offers a draw" when no persona) with an Accept button (`variant="default"`, `data-testid="btn-accept-bot-draw"`) and a Decline button (`variant="brand-outline"`, `data-testid="btn-decline-bot-draw"`), `text-sm` floor. Props: `personaName: string | null`, `onAccept`, `onDecline`. In `Bots.tsx`, render it near the board/clock region (inside the game view, not a modal — play continues), fed by `game.botDrawOffer`, `personaName={persona?.name ?? null}`, `onAccept={game.acceptBotDraw}`, `onDecline={game.declineBotDraw}`. The hook already auto-expires the offer on the user's next move (Plan 03), so the banner disappears without extra page logic. Write RTL tests: renders the persona-named prompt with both buttons; Accept calls onAccept, Decline calls onDecline; renders nothing when no offer.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/bots/__tests__/BotDrawOfferBanner.test.tsx && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>A non-blocking inline banner shows the bot's persona-named draw offer with working Accept/Decline near the board; play continues; it auto-expires via the hook on the user's next move.</done>
  <acceptance_criteria>
    - `npm test -- --run src/components/bots/__tests__/BotDrawOfferBanner.test.tsx` exits 0.
    - Behavior: banner text includes the persona name when supplied; Accept/Decline invoke their callbacks (covered by test).
    - Behavior: banner renders nothing when there is no live offer.
    - Source: banner is rendered in the game view region of Bots.tsx (not inside a blocking Dialog).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Persona-named result copy + Rematch / New opponent (D-06/D-08)</name>
  <files>frontend/src/lib/botGameEnd.ts, frontend/src/components/bots/GameResultDialog.tsx, frontend/src/components/bots/GameResultStrip.tsx, frontend/src/pages/Bots.tsx</files>
  <read_first>
    - frontend/src/lib/botGameEnd.ts lines 81-112 (resultCopy — the single copy-generation path; extend to accept an optional persona name without forking a second generator)
    - frontend/src/components/bots/GameResultDialog.tsx lines 76-134 (DialogFooter action-button pattern: Analyze = brand-outline, New game = default; the resultCopy title at line 73)
    - frontend/src/components/bots/GameResultStrip.tsx (must render the SAME resultCopy string + mirror the same action set — mobile parity contract, header comment lines 16-19)
    - frontend/src/pages/Bots.tsx lines 382-448 (GameResultStrip/GameResultDialog wiring, onNewGame/handleAnalyze; handleStart at 513-516 is the Rematch mechanism per RESEARCH Open Question 2)
  </read_first>
  <action>
    Extend `resultCopy` in `botGameEnd.ts` to take an optional persona name and, when present, substitute it for the generic "the bot"/"You lost"-style subject where a persona game's bot is the actor (e.g. "<Persona> wins on time", "<Persona> — checkmate") while keeping the user-side copy and the Custom-game generic copy unchanged — one function, no second generator. In `GameResultDialog` and `GameResultStrip`, pass the persona name through so both render the identical persona-aware title (mobile parity), and add a "Rematch <Persona>" action (persona games only) alongside the existing Analyze/New actions using only the sanctioned Button variants (never hand-rolled colors). Relabel/route the existing "New game" as the "New opponent" path back to the grid (it already returns to the setup branch, which is now the grid by default — reuse onNewGame, no new route). In `Bots.tsx`, wire Rematch to `handleStart(settings)` (same pinned config + same color/TC — the existing single start path, not the hook's dormant newGame()), passing the current game's `settings` and the resolved persona name into both result surfaces. Keep Rematch hidden for Custom games (no persona).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Bots.test.tsx 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>Result dialog + strip name the persona in the outcome copy and offer Rematch <Persona> (via handleStart) + New opponent (via onNewGame); Custom games keep generic copy and no Rematch; strip and dialog render the identical string.</done>
  <acceptance_criteria>
    - `npx tsc -b` zero errors; existing Bots/result tests pass (extend for persona copy + Rematch).
    - Behavior: resultCopy with a persona name substitutes it into the bot-actor copy; without one returns today's exact strings (covered by test).
    - Source: resultCopy remains the single copy generator (GameResultStrip imports it, no duplicate string table).
    - Behavior: Rematch calls handleStart with the current settings (same personaId/botElo/blend/color/TC); it is absent for a Custom game.
    - Source: New opponent uses the existing onNewGame callback (no new navigation route added).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resumed personaId → in-game UI | A tampered/removed personaId reaches the clock strip, banner, and result copy on resume |
| authored persona name → DOM | Persona name renders in the clock strip, banner, and result title |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-183-12 | Tampering | resumed/removed personaId | medium | mitigate | Every consumer resolves identity via personaFor and null-coalesces to 'FlawChess Bot'/generic copy — an unrecognized id degrades gracefully everywhere, never crashes (shared-helper backstop) |
| T-183-13 | Information Disclosure | persona name in banner/title | low | mitigate | Rendered as escaped JSX text (React auto-escape), never dangerouslySetInnerHTML; authored content, not user input |
| T-183-14 | Elevation of Privilege | Rematch start path | low | mitigate | Rematch routes through the single existing handleStart — no parallel start/route added; New opponent reuses onNewGame |
| T-183-SC | Tampering | npm installs | low | accept | Zero new packages installed by this plan |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/bots/__tests__/BotDrawOfferBanner.test.tsx src/components/bots/__tests__/ClockDisplay.test.tsx src/pages/__tests__/Bots.test.tsx`
- `cd frontend && npx tsc -b` (zero errors — shared botGameEnd/ClockDisplay signatures)
- `cd frontend && npm run lint && npm test -- --run && npm run knip` (full frontend suite + dead-export check before phase gate)
</verification>

<success_criteria>
- Persona avatar + name show in the bot clock strip on desktop + mobile (D-06); result copy names the persona.
- The bot's outgoing draw offer surfaces as a working non-blocking banner (D-07).
- Rematch <Persona> (same config) + New opponent (back to grid) work via the existing single start / new-game paths (D-08).
- All persona lookups go through the one shared personaFor helper; unknown ids degrade gracefully.
</success_criteria>

<output>
Create `.planning/phases/183-persona-registry-bots-page/183-05-SUMMARY.md` when done.
</output>
