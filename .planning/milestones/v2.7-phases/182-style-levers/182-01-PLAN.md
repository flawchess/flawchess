---
phase: 182-style-levers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/engine/types.ts
  - frontend/src/lib/engine/treeCommon.ts
  - frontend/src/lib/engine/__tests__/treeCommon.test.ts
autonomous: true
requirements: [STYLE-04]
must_haves:
  truths:
    - "buildRankedLines populates childScoreSpread on every RankedLine, computed as the max−min of the root child's own graded grandchildren values (grandchildren of root)."
    - "childScoreSpread is null when a root child has 0 or 1 own children (no meaningful spread signal), never 0-as-a-signal for the 0-child case."
    - "Every existing RankedLine consumer (analysis board, agreement verdict, eval bar) compiles and behaves unchanged — the field is additive-optional, never read by pre-existing code."
    - { statement: "A shaped consumer reading childScoreSpread for a low-visit root child receives null rather than a thrown TypeError (STYLE-04 boundary edge).", verification: backstop }
    - { statement: "childScoreSpread values are not comparable across differing budget.concurrency levels; consumers must not assume cross-concurrency stability (STYLE-04 precision edge).", verification: backstop }
  artifacts:
    - frontend/src/lib/engine/types.ts
    - frontend/src/lib/engine/treeCommon.ts
  key_links:
    - "buildRankedLines → computeChildScoreSpread(child) → RankedLine.childScoreSpread"
---

<objective>
Add the variance signal that Light/Deep-rung score shaping (STYLE-04) reads: an additive optional `childScoreSpread: number | null` field on `RankedLine` (D-10), computed by `buildRankedLines` in `treeCommon.ts` from a statistic the search tree already materializes.

Purpose: STYLE-04's variance-preference lever needs a per-root-candidate "sharpness" number. D-10 fixes it as an additive field so every existing `RankedLine` consumer is untouched (zero blast radius) and the analysis board keeps compiling and behaving byte-identically.
Output: `RankedLine.childScoreSpread` field + `computeChildScoreSpread` helper in `treeCommon.ts` + extended `treeCommon.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-style-levers/182-CONTEXT.md
@.planning/phases/182-style-levers/182-RESEARCH.md
@.planning/phases/182-style-levers/182-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols this phase creates (for source-grounding / drift exclusion across all 182 plans):
- `frontend/src/lib/engine/types.ts`: `RankedLine.childScoreSpread` (field)
- `frontend/src/lib/engine/treeCommon.ts`: `computeChildScoreSpread` (private helper)
- `frontend/src/lib/engine/botStyle.ts`: `BotStyleParams`, `MoveFeatures`, `classifyMoveFeatures`, `applyStylePriorReweighting`, `applyStyleScoreShaping`, `styleBookWeighting`, `PIECE_VALUE`
- `frontend/src/lib/engine/botStyleBundles.ts`: `ATTACKER_STYLE`, `TRICKSTER_STYLE`, `GRINDER_STYLE`, `WALL_STYLE`, `BOT_STYLE_BUNDLES`
- `frontend/src/lib/engine/styleOpeningLines.ts`: per-style×color SAN-prefix `ReadonlySet<string>` constants + `styleLinesFor`
- `frontend/src/lib/botDrawGate.ts`: `wouldBotResign` (fn), `contempt` param on `wouldBotAcceptDraw`, resign tuning constants
- `frontend/src/lib/engine/selectBotMove.ts`: `BotSettings.style` (field)
- `frontend/src/hooks/useBotGame.ts`: `BotGameSettings.style` (field), `consecutiveLowScoreTurnsRef` (ref)
- `scripts/style-lever-measurement.mjs`: new measurement script

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the RankedLine.childScoreSpread field with a fixed, documented semantic</name>
  <files>frontend/src/lib/engine/types.ts</files>
  <read_first>
    - frontend/src/lib/engine/types.ts (the frozen contract; every field cites the decision that fixes its shape — header lines 1-14)
    - frontend/src/lib/engine/treeCommon.ts (SearchTreeNode shape, `value` field, buildModalPath, buildRankedLines)
  </read_first>
  <behavior>
    - types.ts RankedLine gains `childScoreSpread: number | null`.
    - The doc comment fixes ONE precise semantic (per RESEARCH Open Question #1): the max−min over the root child's OWN children's `.value` (grandchildren of root), null when the child has 0 or 1 children.
    - The doc comment cites D-10 and notes the concurrency caveat (values differ across budget.concurrency levels — expected, not a bug — Pitfall 4).
  </behavior>
  <action>
    In `frontend/src/lib/engine/types.ts`, add `childScoreSpread: number | null;` to the `RankedLine` interface, following the file's established per-field doc-comment convention (see the `objectiveEvalMate` field for the citation style). The doc comment MUST: cite D-10 (Phase 182); state the exact semantic — the max−min spread of the root candidate's own children's `.value`s (the grandchildren of the search root), which is the "sharpness/variance" proxy STYLE-04's variance-preference lever reads; state that it is `null` when the root child has 0 or 1 children; and note that the value is only comparable within a single concurrency level (`budget.concurrency`), never across levels. Describe the concept in prose only — do NOT reproduce any literal that a later negative-grep gate might target.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "childScoreSpread" frontend/src/lib/engine/types.ts` returns ≥ 1.
    - The RankedLine field type is exactly `number | null` (source assertion).
    - `cd frontend && npx tsc -b` reports zero errors (the additive-optional field breaks no existing consumer).
  </acceptance_criteria>
  <done>RankedLine carries a documented `childScoreSpread: number | null` field; the project type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute childScoreSpread in buildRankedLines + extend treeCommon tests</name>
  <files>frontend/src/lib/engine/treeCommon.ts, frontend/src/lib/engine/__tests__/treeCommon.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/treeCommon.ts (buildModalPath lines ~176-204, buildRankedLines lines ~214-242, SearchTreeNode.children/value)
    - frontend/src/lib/engine/__tests__/treeCommon.test.ts (existing buildRankedLines / buildSnapshot test fixtures and node-builder helpers)
  </read_first>
  <behavior>
    - New private helper `computeChildScoreSpread(node)`: returns `null` when `node.children.size <= 1`; otherwise `max(child.value) − min(child.value)` over `node.children.values()`.
    - buildRankedLines sets `childScoreSpread: computeChildScoreSpread(child)` in the RankedLine object literal for each root child.
    - Test: a root child with ≥2 own children reports the exact max−min of their values.
    - Test: a root child with 0 children reports null; with exactly 1 child reports null (STYLE-04 boundary/empty edge).
    - Test: an existing buildRankedLines assertion still passes unchanged (regression — additive only).
  </behavior>
  <action>
    In `frontend/src/lib/engine/treeCommon.ts`, add a small pure private helper `computeChildScoreSpread<N extends SearchTreeNode<N>>(node: N): number | null` placed near `buildModalPath`. It returns `null` when `node.children.size` is 0 or 1 (no meaningful spread — Pitfall 4: never 0-as-a-signal for the empty case), otherwise the difference between the maximum and minimum `.value` across `node.children.values()`. Wire it into `buildRankedLines`'s RankedLine object literal as `childScoreSpread: computeChildScoreSpread(child)` (the `child` here is a root child, so its own children are the grandchildren of root — the D-10 semantic). Do not change the existing sort, `practicalScore` assignment, or any other field. Then extend `frontend/src/lib/engine/__tests__/treeCommon.test.ts` with the four behavior cases above, reusing the existing node-builder fixture pattern in that file.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/treeCommon.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New tests assert: multi-child spread == exact max−min of grandchild values; 0-child and 1-child cases == null.
    - At least one pre-existing treeCommon buildRankedLines/buildSnapshot test remains green (regression proof).
    - Reverting the `computeChildScoreSpread` call in buildRankedLines makes the new tests fail (mutation-proof — the assertion is behavioral, not symbol-presence).
  </acceptance_criteria>
  <done>buildRankedLines emits a correct, null-safe childScoreSpread; treeCommon.test.ts green including regression and null-boundary cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search tree → RankedLine consumers | Internal in-memory data; no untrusted input crosses here (pure client engine). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-01 | Denial of Service | computeChildScoreSpread over a low-visit child | low | mitigate | Return `null` for 0/1 children so downstream shaping applies no bonus and never dereferences a missing value (no TypeError on low-visit lines — Pitfall 4). |
| T-182-02 | Tampering | additive field silently altering existing consumers | low | accept | Field is optional/additive and unread by pre-existing code; `tsc -b` + a retained regression test prove no behavior change. |

No package installs in this plan — legitimacy gate not applicable.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` zero errors.
- `cd frontend && npx vitest run src/lib/engine/__tests__/treeCommon.test.ts` green.
</verification>

<success_criteria>
RankedLine exposes a documented, null-safe `childScoreSpread`; every existing consumer is untouched; treeCommon tests cover the multi-child, single-child, and empty-child cases.
</success_criteria>

<output>
Create `.planning/phases/182-style-levers/182-01-SUMMARY.md` when done.
</output>
