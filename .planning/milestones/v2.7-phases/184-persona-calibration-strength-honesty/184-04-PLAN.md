---
phase: 184-persona-calibration-strength-honesty
plan: 04
type: execute
wave: 4
depends_on: [184-02, 184-03]
files_modified:
  - reports/data/persona-calibration-cells.tsv
  - reports/data/persona-calibration.json
  - frontend/src/generated/personaCalibration.ts
autonomous: false
requirements: [CAL-04, CAL-05]
user_setup:
  - service: operator-machine
    why: "The ~2 overnight persona-calibration sweeps run on the operator's own machine (onnxruntime-web Maia + Stockfish WASM pool); there is no CLI Claude can invoke to completion — this is the D-09 HUMAN-UAT gate."

must_haves:
  truths:
    - "The operator runs the ~24-persona-cell sweep (~4 anchors × ~24 games) as ~2 overnight runs via bin/run_persona_calibration_sweep.sh under bin/preset-supervisor.sh, producing reports/data/persona-calibration-cells.tsv."
    - "After the sweep, calibration_persona_fit.py + gen_persona_calibration.py regenerate persona-calibration.json and personaCalibration.ts from the REAL measured ledger, replacing the bootstrap values with measured/fitted labels."
    - "The final labels honor CAL-05: no label exceeds ~1800 (D-07), the 800 rung shows its measured/extrapolated ~900-class value with the floor acknowledged in the D-06 popover, and labels are weakly monotone within each style column (D-04)."
    - "The aggregate TSV has exactly 24 distinct persona row-groups and the fit output has exactly 24 entries — a collision or dropped persona is detected (Warning sign from Pitfall 1)."
    - "D-09: the sweep is operator-run under the resume-on-crash supervisor with a committed runbook (Phase 180 model); the phase gates on this HUMAN-UAT step, and the final fit/regenerate task runs only after the operator confirms the ledger is complete."
  prohibitions:
    - "Never run the bare harness/scheduler driver for the overnight sweep — always launch through bin/preset-supervisor.sh so the onnxruntime-web wasm OOB crash (~5-6h into blend>0 runs) auto-resumes via the append-mode ledger."
  artifacts:
    - "reports/data/persona-calibration-cells.tsv (real measured aggregate, replacing the bootstrap fixture)"
  key_links:
    - "run_persona_calibration_sweep.sh → preset-supervisor.sh → harness persona mode → ledger → fit → gen → personaCalibration.ts."
    - "final personaCalibration.ts → personaRegistry.ts labels (Plan 03 consumer) → PersonaCard/PersonaDetailSurface."
---

<objective>
Run the operator overnight persona-calibration sweep (HUMAN-UAT gate), then refit and regenerate the calibration artifacts from the real measured ledger so the shipped labels are measured, not provisional.

Purpose: This closes CAL-04 (labels are actually measured on the internal anchor scale with per-persona style offsets) and finalizes CAL-05's honest floor/ceiling labels. D-02: one-shot measurement — whatever each persona measures is its label.
Output: A real persona-calibration-cells.tsv, refitted persona-calibration.json, and the final personaCalibration.ts consumed by the registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-persona-calibration-strength-honesty/184-CONTEXT.md
@.planning/phases/184-persona-calibration-strength-honesty/184-RESEARCH.md
@.planning/phases/184-persona-calibration-strength-honesty/184-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 04)

No new symbols — this plan produces measured DATA that replaces the Plan-02 bootstrap:
- `reports/data/persona-calibration-cells.tsv` — real per-(persona,anchor) aggregate from the overnight sweep.
- Refitted `reports/data/persona-calibration.json` and regenerated `frontend/src/generated/personaCalibration.ts`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: operator runs the overnight persona-calibration sweep (HUMAN-UAT, D-09)</name>
  <action>Operator-only gate — no code change. The operator runs the ~2 overnight sweeps via bin/run_persona_calibration_sweep.sh under bin/preset-supervisor.sh and confirms reports/data/persona-calibration-cells.tsv has 24 distinct persona row-groups before the refit task proceeds.</action>
  <what-built>The supervised persona-calibration sweep runbook (bin/run_persona_calibration_sweep.sh, Plan 02), the 24-persona-cell PersonaId-keyed schedule (Plan 01), and the style-threaded harness. Everything needed to measure all 24 personas is in place; the actual overnight measurement is operator work.</what-built>
  <how-to-verify>
    1. From the repo root, launch the sweep under the supervisor (NEVER the bare driver — the onnxruntime-web wasm OOB crash ~5-6h into blend>0 runs needs auto-resume): run `bin/run_persona_calibration_sweep.sh` (it wraps `bin/preset-supervisor.sh` and uses `--games-per-cell 24`). Budget ~2 overnight runs per SEED-098.
    2. If a run crashes with `RuntimeError: memory access out of bounds` (Maia policy inference), the supervisor's `--resume` relaunch self-heals via the append-mode ledger — let it continue; do not restart from scratch.
    3. When complete, confirm `reports/data/persona-calibration-cells.tsv` exists and has exactly 24 distinct `persona_id` row-groups (fewer = a Pitfall-1 collision; investigate before proceeding).
    4. Sanity-check the fitted values landing inside plausible bands (Human ~900-1400, Light ~1500-1600, Deep ~1600-1800 pre-cap; a persona flagged `beyond_ladder` whose fitted value lands comfortably mid-range, or vice-versa, is a Pitfall-4 advisory to note, not a blocker).
    5. Reply "approved" (optionally noting any persona that landed far off its rung — per D-02 that is the honest label, no correction pass) or describe any run failure.
  </how-to-verify>
  <resume-signal>Type "approved" once the sweep completed and persona-calibration-cells.tsv has 24 persona groups, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task: Refit + regenerate calibration artifacts from the real ledger</name>
  <files>reports/data/persona-calibration.json, frontend/src/generated/personaCalibration.ts</files>
  <read_first>
    - reports/data/persona-calibration-cells.tsv (the real measured aggregate produced by the operator sweep)
    - scripts/calibration_persona_fit.py (the fit entry point — run without --bootstrap against the real TSV)
    - scripts/gen_persona_calibration.py (the codegen + --check drift mode)
    - frontend/src/lib/personas/__tests__/personaRegistry.test.ts (the CAL-05 ceiling/floor/completeness assertions that must still pass with real labels)
  </read_first>
  <action>
    Run `uv run python scripts/calibration_persona_fit.py` (no --bootstrap) against the real `reports/data/persona-calibration-cells.tsv` to produce the measured `reports/data/persona-calibration.json` (24 personas, rating_vs_maia/rating_vs_sf fitted per family, approx_blitz converted via pooled g_preset_combined + BLITZ_OFFSET_C, beyond_ladder flags carried). Then run `uv run python scripts/gen_persona_calibration.py` to regenerate `frontend/src/generated/personaCalibration.ts` (PAVA per style column, 1800 clamp post-pool, round-50). Confirm `--check` is clean and `git diff --exit-code` on both artifacts shows no residual drift. Run the frontend persona/bots test suites to confirm the CAL-05 assertions (ceiling ≤ ~1800, floor label present, 24-slot completeness, within-style monotonicity) still hold with the real measured labels. Do NOT run a correction pass on far-off personas — per D-02 the measured value is the honest label.
  </action>
  <verify>
    <automated>uv run python scripts/gen_persona_calibration.py --check && git diff --exit-code frontend/src/generated/personaCalibration.ts reports/data/persona-calibration.json && cd frontend && npm test -- --run src/lib/personas src/components/bots && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `reports/data/persona-calibration.json` and `personaCalibration.ts` contain 24 entries derived from the real ledger (not the bootstrap `approx_blitz = rung` values).
    - `gen_persona_calibration.py --check` exits 0 and `git diff --exit-code` is clean on both artifacts.
    - Every label ≤ `~1800`; the 800-rung personas carry a measured/extrapolated label (floor honesty); within-style labels are non-decreasing by rung.
    - The frontend persona/bots suites and `npx tsc -b` pass with the real labels.
  </acceptance_criteria>
  <done>The shipped calibration artifacts are refit from the real overnight sweep, drift-clean, and pass every CAL-04/CAL-05 assertion; the registry now shows measured labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator sweep ledger → committed artifacts | The overnight measurement crosses into the shipped labels; a collision, truncated ledger, or unsupervised crash would ship wrong or partial strength claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-184-04-01 | Tampering | sweep ledger integrity | high | mitigate | Fail-loud ledger/TSV validation (header/truncation/24-count) + the checkpoint's explicit 24-persona-group check before refit; PersonaId keying prevents silent overwrite. |
| T-184-04-02 | Denial of service | wasm OOB crash mid-sweep | medium | mitigate | Always run under bin/preset-supervisor.sh; append-mode ledger resume self-heals (documented in the runbook + checkpoint steps). |
| T-184-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs — runs existing scripts/tooling only (Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Operator confirms the sweep completed with 24 persona groups (HUMAN-UAT checkpoint).
- `uv run python scripts/gen_persona_calibration.py --check` + `git diff --exit-code` clean on both artifacts.
- Frontend persona/bots suites + `npx tsc -b` green with real labels.
</verification>

<success_criteria>
- All 24 personas measured via the supervised overnight sweep; labels are measured, not provisional.
- Final artifacts drift-clean; CAL-04 (measured with per-persona offset) and CAL-05 (floor/ceiling honesty) satisfied.
</success_criteria>

<output>
Create `.planning/phases/184-persona-calibration-strength-honesty/184-04-SUMMARY.md` when done.
</output>
