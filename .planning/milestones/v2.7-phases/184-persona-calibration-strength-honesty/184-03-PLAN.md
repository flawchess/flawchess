---
phase: 184-persona-calibration-strength-honesty
plan: 03
type: execute
wave: 3
depends_on: [184-02]
files_modified:
  - frontend/src/lib/personas/personaRegistry.ts
  - frontend/src/lib/engine/botStyleBundles.ts
  - frontend/src/components/bots/PersonaCard.tsx
  - frontend/src/components/bots/PersonaDetailSurface.tsx
  - frontend/src/components/bots/PersonaEloDisclosurePopover.tsx
  - frontend/src/lib/personas/__tests__/personaRegistry.test.ts
  - frontend/src/components/bots/__tests__/PersonaCard.test.tsx
  - frontend/src/components/bots/__tests__/PersonaDetailSurface.test.tsx
autonomous: true
requirements: [CAL-05]

must_haves:
  truths:
    - "personaRegistry.ts sources each persona's botElo and display label from PERSONA_CALIBRATION (frontend/src/generated/personaCalibration.ts) keyed by PersonaId; the Record<PersonaId, Persona> exhaustiveness still forces all 24 slots."
    - "PersonaCard renders the calibrated label (with the ~ prefix, text-sm text-muted-foreground) instead of `~${persona.rung}`, and its aria-label reads the same calibrated value."
    - "PersonaDetailSurface renders the calibrated label and a reusable measurement-disclosure popover (D-08) adjacent to the ELO text on every persona; the bottom-rung (800) variant appends the ~900 measured-floor acknowledgment line (D-06)."
    - "Every one of the 24 PersonaId slots resolves to a defined calibrated botElo and label — a missing persona is a compile/CI error via the Record<PersonaId> type, never a blank label (empty/missing-input guard)."
    - "The persona `rung` field is unchanged in meaning — it stays the structural grid-position key; botElo and label are calibration outputs."
    - { statement: "Persona display labels are ASCII tilde-prefixed integers (~NNNN, rounded to nearest 50); label equality is exact string comparison with no unicode/normalization ambiguity", verification: backstop }
  prohibitions:
    - "Never leave the stale A1 (botElo === rung) placeholder prose in personaRegistry.ts alongside live calibrated data — replace the header comment with calibration provenance + the D-11 staleness policy."
    - "Never use text-xs anywhere except inside the disclosure-popover body (the documented CLAUDE.md popover exception); card and detail labels stay text-sm."
    - "Never repurpose the persona `rung` field — it remains the structural grid-position key, not a strength value."
  artifacts:
    - frontend/src/components/bots/PersonaEloDisclosurePopover.tsx
    - "New `calibratedLabel: string` field on the Persona interface"
  key_links:
    - "personaRegistry.ts ← PERSONA_CALIBRATION[personaId].{ botElo, label } from the generated file."
    - "PersonaDetailSurface.tsx → PersonaEloDisclosurePopover (mirrors MetricStatPopover hover/tap shell)."
---

<assumption_delta_decision>
Trigger: the registry's `botElo` transitions from a DERIVED value (`=== rung`, A1 placeholder) to a CHOSEN/measured calibration output (`chosen` family: a derived value becomes a fitted/measured value).
- Primary noun (unchanged): `PersonaId` remains the primary identity; `rung` remains the structural grid-position key.
- Decision: `no-change`. `botElo` and `label` are demoted to per-persona calibration ATTRIBUTES sourced from the generated file keyed by `PersonaId` — no new identity is introduced and no old primary is displaced. The exhaustive `Record<PersonaId, ...>` already encodes the invariant that every variant (all 24 slots) round-trips through the primary identity, so no add-alongside debt accrues.
- Invariant guard (accepted): the `personaRegistry.test.ts` completeness + floor/ceiling assertions go red if a future change reintroduces `botElo === rung` or drops a persona.
</assumption_delta_decision>

<objective>
Wire the generated calibration file into the persona registry and Bots-page surfaces: swap the provisional `~${rung}` labels for calibrated labels on the card and detail surface, add the D-08 measurement-disclosure popover (with the D-06 bottom-rung floor variant), and record the D-11 staleness policy.

Purpose: This is where CAL-05's honesty surfaces become visible to users — uniform tilde labels, the ~1800 ceiling and ~900 floor acknowledgment, and an honest disclosure of what the number means.
Output: A calibration-sourced registry, a reusable disclosure popover, and updated card/detail rendering + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-persona-calibration-strength-honesty/184-CONTEXT.md
@.planning/phases/184-persona-calibration-strength-honesty/184-RESEARCH.md
@.planning/phases/184-persona-calibration-strength-honesty/184-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 03)

New symbols (exclude from drift verification):
- `frontend/src/components/bots/PersonaEloDisclosurePopover.tsx` — reusable D-08 popover (mirrors MetricStatPopover), `data-testid="persona-elo-disclosure"`.
- New `calibratedLabel: string` field on the `Persona` interface (personaRegistry.ts).
- Registry consumption of `PERSONA_CALIBRATION` from the generated file.

<tasks>

<task type="auto">
  <name>Task 1: Source botElo + calibrated label from the generated file; record staleness policy</name>
  <files>frontend/src/lib/personas/personaRegistry.ts, frontend/src/lib/engine/botStyleBundles.ts, frontend/src/lib/personas/__tests__/personaRegistry.test.ts</files>
  <read_first>
    - frontend/src/lib/personas/personaRegistry.ts (the A1 header comment lines 14-22, the Persona interface lines 58-85, RUNG_BLEND, and the 24 entries whose botElo currently === rung)
    - frontend/src/generated/personaCalibration.ts (PERSONA_CALIBRATION shape — botElo + label per PersonaId; from Plan 02)
    - frontend/src/lib/engine/botStyleBundles.ts (its header comment — gains the D-11 staleness note)
    - frontend/src/lib/personas/__tests__/personaRegistry.test.ts (existing assertions to extend for CAL-05)
  </read_first>
  <action>
    Add a `calibratedLabel: string` field to the `Persona` interface. Change each of the 24 registry entries so `botElo` reads `PERSONA_CALIBRATION[<personaId>].botElo` and `calibratedLabel` reads `PERSONA_CALIBRATION[<personaId>].label` (import `PERSONA_CALIBRATION` from `@/generated/personaCalibration`), rather than the literal `botElo: <rung>`. Keep `rung` and `blend` exactly as-is (structural key + preset). Replace the A1 header doc comment (lines 14-22) with calibration provenance: the botElo/label now come from the generated `personaCalibration.ts` (produced by scripts/calibration_persona_fit.py + gen_persona_calibration.py from the operator sweep), `rung` stays the grid key, and the D-11 staleness policy (changing botStyleBundles.ts style params or the ladder invalidates calibration — re-run the sweep). Update the Persona.botElo field doc to drop the "always === rung" language. Add the same D-11 staleness note to botStyleBundles.ts's header comment. Extend personaRegistry.test.ts with CAL-05 assertions: no calibratedLabel exceeds `~1800` (ceiling, D-07); every persona has a defined calibratedLabel and botElo (24-slot completeness); the 800-rung personas carry a label (floor acknowledged in the label, not dropped, D-06); and labels are non-decreasing by rung within each style column (D-04 monotonicity spot-check).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/personas && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Grep confirms no registry entry has a literal `botElo: 800`/`botElo: 1000`/... (all read from PERSONA_CALIBRATION); the A1 "botElo === rung" prose is gone.
    - personaRegistry.test.ts asserts the ~1800 ceiling, 24-slot completeness, and a floor-rung label; the suite passes.
    - `npx tsc -b` passes (Record<PersonaId> completeness holds; new field typed).
    - botStyleBundles.ts header contains the D-11 staleness note.
  </acceptance_criteria>
  <done>The registry consumes calibrated botElo + label from the generated file, the A1 placeholder prose is replaced with provenance + staleness policy, and CAL-05 assertions guard the ceiling/floor/completeness.</done>
</task>

<task type="auto">
  <name>Task 2: PersonaCard renders the calibrated label</name>
  <files>frontend/src/components/bots/PersonaCard.tsx, frontend/src/components/bots/__tests__/PersonaCard.test.tsx</files>
  <read_first>
    - frontend/src/components/bots/PersonaCard.tsx (line 38 aria-label, line 58 label render, the header doc comment referencing the A1 placeholder)
    - frontend/src/components/bots/__tests__/PersonaCard.test.tsx (existing render assertions to update)
  </read_first>
  <action>
    Replace `{`~${persona.rung}`}` on line 58 with `{persona.calibratedLabel}` (the generated label already includes the `~` prefix), keeping the exact `text-sm text-muted-foreground` classes unchanged. Update the aria-label on line 38 to read the calibrated value instead of `persona.rung` (e.g. `aria-label={`${persona.name}, ${persona.calibratedLabel} ELO`}`) so the accessible name matches the visible label. Update the card's header doc comment to drop the A1-placeholder reference and note the label is now the calibrated value. Update PersonaCard.test.tsx to assert the calibrated label renders (feed a persona whose calibratedLabel differs from `~${rung}` and assert the calibrated string appears, not the rung).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/bots/__tests__/PersonaCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PersonaCard.tsx no longer references `persona.rung` for display or aria-label; both read `persona.calibratedLabel`.
    - The card label stays `text-sm text-muted-foreground` (no text-xs introduced).
    - PersonaCard.test.tsx asserts the calibrated label (distinct from `~${rung}`) renders; suite passes.
  </acceptance_criteria>
  <done>The card shows the calibrated label and matching aria-label; tests prove the swap.</done>
</task>

<task type="auto">
  <name>Task 3: Detail-surface label swap + reusable measurement-disclosure popover (D-08/D-06)</name>
  <files>frontend/src/components/bots/PersonaDetailSurface.tsx, frontend/src/components/bots/PersonaEloDisclosurePopover.tsx, frontend/src/components/bots/__tests__/PersonaDetailSurface.test.tsx</files>
  <read_first>
    - frontend/src/components/bots/PersonaDetailSurface.tsx (lines 219-223 the display-only ELO/style meta text — the swap site + popover mount point)
    - frontend/src/components/popovers/MetricStatPopover.tsx (the popover shell to copy verbatim: 100ms hover-open timeout, PopoverPrimitive Root/Trigger/Portal/Content, side="top" sideOffset={4}, animation classes, text-xs body, Search icon trigger)
    - frontend/src/components/bots/__tests__/PersonaDetailSurface.test.tsx (existing assertions to extend)
    - frontend/src/lib/personas/personaRegistry.ts (Persona.rung to detect the bottom rung for the D-06 floor variant)
  </read_first>
  <action>
    Create `frontend/src/components/bots/PersonaEloDisclosurePopover.tsx` by copying the MetricStatPopover shell (100ms HOVER_OPEN_DELAY_MS, Root/Trigger/Portal/Content, side="top" sideOffset={4}, the exact animation className set, text-xs body — the documented popover exception, Search icon trigger with role="button" tabIndex={0}). Its body copy (D-08, disclosure overrides minimalism per the percentile-chip precedent): the number is measured in bot-vs-engine games on the internal anchor ladder, an approximate blitz scale. Accept a prop (e.g. `isFloorRung: boolean`) that, when true, appends the D-06 variant line acknowledging the ~900 measured floor (the two weakest Human rungs are beyond-ladder extrapolations). Give the trigger `data-testid="persona-elo-disclosure"` and an aria-label. In PersonaDetailSurface.tsx, swap `${persona.style} · ~${persona.rung}` (line 222) for `${persona.style} · ${persona.calibratedLabel}` and mount the popover trigger inline adjacent to that `persona-detail-meta` text (supplementary disclosure, not replacing the visible label), passing `isFloorRung={persona.rung === 800}`. Keep text-sm on the meta line. Extend PersonaDetailSurface.test.tsx: the calibrated label renders in the meta line; the disclosure popover trigger (`persona-elo-disclosure`) is present for every persona; and a bottom-rung (800) persona's popover body includes the floor-acknowledgment copy while a non-floor persona's does not.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/bots/__tests__/PersonaDetailSurface.test.tsx && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - PersonaEloDisclosurePopover.tsx exists, mirrors the MetricStatPopover shell, uses `data-testid="persona-elo-disclosure"`, and confines text-xs to its popover body.
    - PersonaDetailSurface meta line shows `${style} · ${calibratedLabel}` (no `~${rung}`) and mounts the disclosure trigger for every persona.
    - The 800-rung popover body includes the ~900 floor acknowledgment; a mid-rung one does not (test asserts both).
    - knip passes (new export is consumed).
  </acceptance_criteria>
  <done>The detail surface shows the calibrated label plus a reusable D-08 disclosure popover with the D-06 floor variant on the bottom rung; tests prove both the label and the floor/non-floor popover copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated file → registry render | The generated calibration data drives user-visible strength claims; a stale/incomplete file would mislabel opponents (data-integrity, not a network boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-184-03-01 | Tampering | registry ← generated file | medium | mitigate | Record<PersonaId> exhaustiveness + tsc-b + personaRegistry.test.ts completeness/ceiling/floor assertions block a missing/inflated label. |
| T-184-03-02 | Information disclosure | ELO label honesty | low | mitigate | D-08 disclosure popover + D-06 floor note make the approximate/measured nature explicit to users. |
| T-184-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs — reuses existing Radix popover + generated file (Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/lib/personas src/components/bots` passes.
- `cd frontend && npx tsc -b` passes (shared-type change; per project rule, type-check when touching shared types).
- `cd frontend && npm run knip` and `npm run lint` clean.
</verification>

<success_criteria>
- Registry, card, and detail surface all read the calibrated botElo/label from the generated file.
- A1 placeholder prose replaced with provenance + D-11 staleness; rung unchanged in meaning.
- D-08 disclosure popover with D-06 floor variant renders on every persona's detail ELO label; ceiling/floor honesty asserted by tests.
</success_criteria>

<output>
Create `.planning/phases/184-persona-calibration-strength-honesty/184-03-SUMMARY.md` when done.
</output>
