---
phase: 163-gem-moves-maia-findability-move-badges-on-analysis-seed-092
plan: 03
type: execute
wave: 2
depends_on: [163-01]
files_modified:
  - frontend/src/components/analysis/MovesByRatingChart.tsx
  - frontend/src/components/analysis/UnifiedMovePopover.tsx
  - frontend/src/components/analysis/MaiaMoveQualityBar.tsx
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "colorForQuality('gem') returns MAIA_ACCENT so a gem candidate's curve + end-of-line SAN label render violet in MovesByRatingChart"
    - "qualityWord('gem') reads 'Gem' in the chart tooltip (existing capitalize helper already handles it)"
    - "The played/best stroke emphasis still fires for a gem move because emphasis keys off the bestSan prop, not the quality string — a gem is by construction the best, so it keeps its emphasized stroke"
    - "UnifiedMovePopover shows a short gem copy line when its new isGem prop is true (violet), and omits it otherwise"
    - "MaiaMoveQualityBar's per-candidate UnifiedMovePopover passes isGem = (qualityBySan.get(move.san)?.quality === 'gem'); its bar SEGMENTS are unchanged (a gem folds into the existing green 'good' segment via bucketKeyForQuality from Plan 01)"
  artifacts: []
  key_links:
    - "MovesByRatingChart imports MAIA_ACCENT from @/lib/theme (already imported) for the new 'gem' color case"
    - "Analysis (Plan 04) forwards qualityBySanWithGem through MaiaHumanPanel to both MovesByRatingChart and MaiaMoveQualityBar; MaiaMoveQualityBar reads quality==='gem' per candidate and sets the popover's isGem"
---

<objective>
Make the current-position quality surfaces gem-aware: recolor a gem candidate's curve + SAN label violet in `MovesByRatingChart` (`colorForQuality`'s new `'gem'` case), and add a short gem copy line to `UnifiedMovePopover` gated on a new `isGem` prop. These components accept the `'gem'` `MoveQuality` value (from Plan 01) — they render it correctly. Analysis passing gem-bearing quality data is Plan 04's job; this plan only makes the components handle it. Also confirm (no code change) that `MaiaMoveQualityBar` folds a gem move into its green "good" segment via `bucketKeyForQuality`.

Purpose: The chart curve rendered violet is the visual justification for the badge (seed); the popover names the gem for the hovered move.
Output: `colorForQuality`/`qualityWord` gem handling in the chart; the `isGem` gem copy line in the popover.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-CONTEXT.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-PATTERNS.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-01-SUMMARY.md
</context>

## Artifacts this phase produces

See Plan 01's "Artifacts this phase produces" list. This plan introduces the `UnifiedMovePopover` `isGem` prop and the `colorForQuality('gem')` branch. It consumes the `'gem'` `MoveQuality` value created in Plan 01.

<tasks>

<task type="auto">
  <name>Task 1: MovesByRatingChart colorForQuality/qualityWord gem handling + Pitfall-5 audit</name>
  <files>frontend/src/components/analysis/MovesByRatingChart.tsx</files>
  <read_first>
    - frontend/src/components/analysis/MovesByRatingChart.tsx (colorForQuality lines 179-194; qualityWord lines 207-210; the MAIA_ACCENT import line 35; the stroke-emphasis logic ~line 576 `const emphasized = san === playedSan || san === bestSan`)
    - frontend/src/lib/moveQuality.ts (the widened MoveQuality type from Plan 01 including 'gem')
    - .planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md (Pitfall 5: audit every === 'best' / case 'best' before relabeling)
  </read_first>
  <action>
    In `frontend/src/components/analysis/MovesByRatingChart.tsx`, add `case 'gem': return MAIA_ACCENT;` to `colorForQuality` (MAIA_ACCENT is already imported at line 35), placed alongside the other quality cases before the `default: return MOVE_QUALITY_PENDING`. Do NOT change `qualityWord` — its existing `quality[0].toUpperCase() + quality.slice(1)` already produces 'Gem' from 'gem'. Perform the Pitfall-5 audit and record the finding in the SUMMARY: the ONLY quality-string branch is `colorForQuality`'s switch; the played/best stroke emphasis at ~line 576 keys off `san === playedSan || san === bestSan` (the SAN props), NOT `quality === 'best'`, so a gem move — which is by construction the reconciled best SAN — keeps its emphasized stroke with no change needed. Make NO change to the emphasis logic. Confirm there is no other literal `case 'best':` or `=== 'best'` in this file that would need a parallel `'gem'` case.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npx vitest run src/components/analysis</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "case 'gem'" frontend/src/components/analysis/MovesByRatingChart.tsx` shows exactly one match, inside colorForQuality, returning MAIA_ACCENT.
    - `grep -n "=== 'best'\|case 'best'" frontend/src/components/analysis/MovesByRatingChart.tsx` shows only the colorForQuality `case 'best':` (line ~181); the emphasis logic at ~line 576 still reads `san === playedSan || san === bestSan` unchanged.
    - `cd frontend && npx tsc -b` clean; `npm run lint` clean; existing MovesByRatingChart tests pass.
  </acceptance_criteria>
  <done>colorForQuality returns MAIA_ACCENT for 'gem'; qualityWord unchanged (yields 'Gem'); stroke emphasis unchanged and still fires for the gem/best SAN; Pitfall-5 audit recorded.</done>
</task>

<task type="auto">
  <name>Task 2: UnifiedMovePopover gem copy line (isGem prop) + wire it from MaiaMoveQualityBar</name>
  <files>frontend/src/components/analysis/UnifiedMovePopover.tsx, frontend/src/components/analysis/MaiaMoveQualityBar.tsx</files>
  <read_first>
    - frontend/src/components/analysis/UnifiedMovePopover.tsx (full file: UnifiedMovePopoverProps interface ~line 39, the 2-column table body, the existing per-source row pattern, MAIA_ACCENT import line 27, lucide import line 25, ICON_CLASS ~line 29)
    - frontend/src/components/analysis/MaiaMoveQualityBar.tsx (the UnifiedMovePopover render at ~line 205; the qualityBySan prop ~line 97; the hovered-move `move` object with `.san`; confirm the bar SEGMENT loop uses bucketMovesByQuality with QUALITY_BUCKET_ORDER and has no local quality-string switch)
    - .planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-CONTEXT.md (Specific Ideas: copy "Gem — players at your rating almost never find this."; text-sm floor except sanctioned info-popover)
  </read_first>
  <action>
    In `frontend/src/components/analysis/UnifiedMovePopover.tsx`, add an optional `isGem?: boolean` field to `UnifiedMovePopoverProps` with a JSDoc ("When true, prepends a gem copy line; set by MaiaMoveQualityBar when the hovered move's quality is 'gem'."). Add `Gem` to the existing `import { ChessKnight, Cpu, User } from 'lucide-react'`. When `isGem` is true, render a leading table row (above the practical/objective/maia rows) styled `color: MAIA_ACCENT`, showing the `<Gem>` icon (reuse the file's `ICON_CLASS`) + the copy "Gem — players at your rating almost never find this." This popover body is one of the sanctioned info-popover surfaces (Radix popover body per the project text-xs exception list), so the component's existing `text-xs` sizing is acceptable; keep the gem row consistent with the sibling rows rather than shrinking it further. When `isGem` is falsy, omit the row. In `frontend/src/components/analysis/MaiaMoveQualityBar.tsx`, at the `UnifiedMovePopover` render (~line 205), add `isGem={qualityBySan.get(move.san)?.quality === 'gem'}` (qualityBySan is already a prop). Make NO change to the bar's segment logic — `bucketKeyForQuality('gem')` already returns 'good' (Plan 01), so a gem-quality SAN already lands in the existing green "Good Moves" segment via `bucketMovesByQuality`; do not add a gem segment or a local quality switch.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npx vitest run src/components/analysis</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isGem" frontend/src/components/analysis/UnifiedMovePopover.tsx` matches in both the props interface and the render body.
    - `grep -n "isGem" frontend/src/components/analysis/MaiaMoveQualityBar.tsx` shows the `qualityBySan.get(move.san)?.quality === 'gem'` wiring at the popover render.
    - The gem copy string "players at your rating almost never find this" is present in UnifiedMovePopover.
    - `grep -n "QUALITY_BUCKET_ORDER\|bucketMovesByQuality" frontend/src/components/analysis/MaiaMoveQualityBar.tsx` still shows the unchanged segment loop; no new gem segment/case was added.
    - `cd frontend && npx tsc -b` clean; `npm run lint` clean; existing analysis component tests pass.
  </acceptance_criteria>
  <done>UnifiedMovePopover renders a violet gem copy line when isGem is true and omits it otherwise; MaiaMoveQualityBar sets isGem per hovered candidate while its segments fold gem into good unchanged; tsc + lint + tests clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational chart/popover rendering of client-computed quality labels. No input, no data flow, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-03 | Tampering | popover/chart gem label | low | accept | A manipulated client label has no security consequence (presentational only). RESEARCH Security Domain: no ASVS category applies. |

No `npm install` — no Package Legitimacy Gate required.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/analysis` green.
- `cd frontend && npx tsc -b && npm run lint` clean.
</verification>

<success_criteria>
A gem candidate's curve + SAN label render MAIA_ACCENT violet with a 'Gem' tooltip word; the popover shows the gem copy line when isGem is set; MaiaMoveQualityBar folds gem into its good segment unchanged; stroke emphasis still fires for the gem/best move.
</success_criteria>

<output>
Create `.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-03-SUMMARY.md` when done
</output>
