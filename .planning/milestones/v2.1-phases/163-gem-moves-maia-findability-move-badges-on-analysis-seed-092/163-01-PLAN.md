---
phase: 163-gem-moves-maia-findability-move-badges-on-analysis-seed-092
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/gemMove.ts
  - frontend/src/lib/__tests__/gemMove.test.ts
  - frontend/src/lib/moveQuality.ts
  - frontend/src/lib/__tests__/moveQuality.test.ts
autonomous: true
requirements: [D-01, D-02, D-04, D-07, D-08]
must_haves:
  truths:
    - "classifyGem returns true only when the played move's Maia probability <= GEM_MAIA_MAX_PROB (C1) AND it is the graded best with bestEs - secondBestEs >= MISTAKE_DROP (C2)"
    - "A lost-position best-try (bestEs 0.20 vs secondBestEs 0.05) still qualifies — no still-losing exclusion (D-01)"
    - "A move at any ply qualifies with no opening-ply guard (D-02); a near-saturation gap (+800 vs +400 cp) and a high-Maia-probability forced recapture both fail to qualify (free-lunch guards)"
    - "summarizeForGem returns {bestSan, bestEs, secondBestEs} in one pass over a per-SAN grade map using evalToExpectedScore, mover-agnostic (D-04)"
    - "MoveQuality includes a 6th 'gem' value; bucketKeyForQuality maps 'gem' to the 'good' display bucket (never the 'pending' default)"
  artifacts:
    - "frontend/src/lib/gemMove.ts"
    - "frontend/src/lib/__tests__/gemMove.test.ts"
  key_links:
    - "gemMove.ts imports evalToExpectedScore + MoverColor from @/lib/liveFlaw and MISTAKE_DROP from @/generated/flawThresholds — no re-derived sigmoid or hand-copied threshold"
    - "MoveQuality 'gem' flows through bucketKeyForQuality into the existing 'good' display segment consumed by MaiaMoveQualityBar"
---

<objective>
Build the pure detection foundation for gem moves: a worker-free `gemMove.ts` module exposing `GEM_MAIA_MAX_PROB` (D-07), `classifyGem(...)` (C1 + C2 predicate), and `summarizeForGem(...)` (best/second-best expected-score reducer), plus extend `MoveQuality` with a 6th `'gem'` value and route it through `bucketKeyForQuality`. Everything here is pure math over already-resolved primitives — no hook/engine/React coupling — so it is trivially unit-testable and reused by the Analysis wiring (Plan 04) and the chart/popover surfaces (Plan 03).

Purpose: One canonical, tested detection predicate so no display consumer re-derives the argmax or the sigmoid (Phase 162's single-source-of-truth discipline).
Output: `gemMove.ts` + its test, and the `'gem'` bucket extension to `moveQuality.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-CONTEXT.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols introduced across Phase 163 (excluded from source-drift verification):
- `GEM_MAIA_MAX_PROB`, `classifyGem`, `summarizeForGem` (frontend/src/lib/gemMove.ts — NEW, this plan)
- `MoveQuality` gains the `'gem'` value (frontend/src/lib/moveQuality.ts — this plan)
- `GEM_GLYPH`, `GemIcon`, `SquareMarker.gem`, gem `GemBadgeContent` render branch (Plan 02)
- `maiaCurveByFen`, `gradeSummaryByFen`, `gemByNode`, `gemCandidate`, `qualityBySanWithGem`, gem `gemActive` gate (Plan 04, frontend/src/pages/Analysis.tsx)
- `UnifiedMovePopover` `isGem` prop (Plan 03), `FlawMarkerEntry.gem` field (Plan 04)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gemMove.ts — GEM_MAIA_MAX_PROB, classifyGem, summarizeForGem + unit tests</name>
  <files>frontend/src/lib/gemMove.ts, frontend/src/lib/__tests__/gemMove.test.ts</files>
  <read_first>
    - frontend/src/lib/liveFlaw.ts (evalToExpectedScore signature, MoverColor type, classifyLiveSeverity shape, MISTAKE_DROP/BLUNDER_DROP import chain)
    - frontend/src/generated/flawThresholds.ts (MISTAKE_DROP value and export)
    - frontend/src/lib/moveQuality.ts (constant-with-docstring convention lines 55-59; MoveGrade type; the existing summarize-style reduce in classifyMoveQuality lines 86-96)
    - frontend/src/lib/__tests__/moveQuality.test.ts (test file structure: describe/it blocks, the CUMULATIVE_MASS_THRESHOLD/CANDIDATE_HARD_CAP constant-assertion tests to mirror)
    - .planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md (Pattern 1 full classifyGem sketch; Code Examples summarizeForGem sketch; free-lunch guards)
  </read_first>
  <behavior>
    - classifyGem returns false when maiaProbability is null OR maiaProbability > GEM_MAIA_MAX_PROB (C1 fail).
    - classifyGem returns false when !playedIsBest OR bestEs null OR secondBestEs null.
    - classifyGem returns true when playedIsBest AND (bestEs - secondBestEs) >= MISTAKE_DROP AND maiaProbability <= GEM_MAIA_MAX_PROB.
    - D-01: bestEs=0.20, secondBestEs=0.05, maiaProbability=0.02, playedIsBest=true → true (lost-position best-try qualifies; no still-losing exclusion).
    - D-02: same qualifying inputs with no ply parameter available → true (there is no ply argument, so no opening guard can exist).
    - D-04: qualifying inputs classify identically regardless of mover (the function has no color parameter — assert via summarizeForGem being called with both 'white' and 'black' movers producing symmetric best/second-best).
    - D-07: GEM_MAIA_MAX_PROB === 0.03 (exact constant assertion).
    - Free-lunch guard 1 (saturation): bestEs and secondBestEs both near 1.0 with gap < MISTAKE_DROP (e.g. es(+800)≈es(+400) in ES space) → false even at low Maia probability.
    - Free-lunch guard 2 (forced recapture): maiaProbability=0.85 (well above ceiling) with a large ES gap → false (C1 fails).
    - summarizeForGem over a Map of {evalCp, evalMate} entries returns the argmax SAN as bestSan, its ES as bestEs, and the runner-up ES as secondBestEs; single-entry map → secondBestEs null; empty map → all null.
  </behavior>
  <action>
    Create `frontend/src/lib/gemMove.ts` as a pure module mirroring `liveFlaw.ts`'s `classifyLiveSeverity` shape (primitive args in, boolean/struct out, no lookups, no hook import). Export `GEM_MAIA_MAX_PROB = 0.03` as a top-of-file const with a one-line JSDoc citing D-07 (strict-side v1 constant; note the future iso-rarity curve is deferred per D-08, so this stays a flat constant and must NOT reference P_REF_ANCHORS). Export `classifyGem(params: { maiaProbability: number | null; playedIsBest: boolean; bestEs: number | null; secondBestEs: number | null }): boolean` implementing: return false if maiaProbability is null or > GEM_MAIA_MAX_PROB (C1); return false if !playedIsBest or bestEs is null or secondBestEs is null; otherwise return `bestEs - secondBestEs >= MISTAKE_DROP` (C2). Import `MISTAKE_DROP` from `@/generated/flawThresholds` and `evalToExpectedScore`, `type MoverColor` from `@/lib/liveFlaw`. Export `summarizeForGem(gradeBySan: Map<string, { evalCp: number | null; evalMate: number | null }>, mover: MoverColor): { bestSan: string | null; bestEs: number | null; secondBestEs: number | null }` computing per-SAN ES via `evalToExpectedScore(evalCp, evalMate, mover)` in one pass, tracking argmax (bestSan/bestEs) and runner-up (secondBestEs); return bestEs null when bestSan is null (empty map) and secondBestEs null when no runner-up exists (use a `-Infinity` sentinel internally, map to null on return) — follow the RESEARCH Code Examples `summarizeForGem` sketch verbatim in structure. Add no error handling beyond the null early-returns (pure math over validated primitives). Write `gemMove.test.ts` with describe/it blocks named per D-number and per free-lunch guard so the file traces 1:1 to the RESEARCH Phase-Requirements → Test map (D-01, D-02, D-04, D-07, saturation guard, forced-recapture guard, summarizeForGem argmax/runner-up/empty). Include an explicit `GEM_MAIA_MAX_PROB === 0.03` assertion mirroring moveQuality.test.ts's constant tests. Do NOT inline the numeric threshold anywhere — import it. Do NOT place the literal exact-comparison string that any test negative-greps for inside prose.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/gemMove.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/gemMove.test.ts` passes with all described cases green.
    - `grep -n "GEM_MAIA_MAX_PROB = 0.03" frontend/src/lib/gemMove.ts` returns exactly one match.
    - `grep -c "0.03" frontend/src/lib/gemMove.ts` shows the literal appears only at the constant definition (import-not-inline discipline): no bare `0.03` in classifyGem's body.
    - `grep -n "P_REF_ANCHORS\|pRefForElo" frontend/src/lib/gemMove.ts` returns no matches (D-07 anti-pattern).
    - `grep -n "from '@/generated/flawThresholds'" frontend/src/lib/gemMove.ts` and `grep -n "evalToExpectedScore" frontend/src/lib/gemMove.ts` both match (reuses canonical sigmoid + threshold, never re-derived).
    - classifyGem is a pure function: `grep -n "useMemo\|useEffect\|import.*hooks" frontend/src/lib/gemMove.ts` returns no matches.
  </acceptance_criteria>
  <done>gemMove.ts exports GEM_MAIA_MAX_PROB (0.03), classifyGem, summarizeForGem; all unit tests including both free-lunch guards and D-01/D-02/D-04/D-07 pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend MoveQuality with 'gem' + route through bucketKeyForQuality</name>
  <files>frontend/src/lib/moveQuality.ts, frontend/src/lib/__tests__/moveQuality.test.ts</files>
  <read_first>
    - frontend/src/lib/moveQuality.ts (MoveQuality type line 31; bucketKeyForQuality switch lines 197-211; QualityBucketKey line 172; module docstring lines 20-23 forbidding FlawSeverity extension)
    - frontend/src/lib/__tests__/moveQuality.test.ts (existing describe blocks for classifyMoveQuality and bucketMovesByQuality to extend)
    - .planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md (Pitfall 4: unmodified switch silently swallows 'gem' into 'pending'; Open Question 3: keep classifyMoveQuality unchanged, override in Analysis)
  </read_first>
  <action>
    In `frontend/src/lib/moveQuality.ts`, extend the `MoveQuality` type (line 31) to `'best' | 'good' | 'inaccuracy' | 'mistake' | 'blunder' | 'gem'` and update its JSDoc to say 6-bucket (was 5-bucket), noting 'gem' is a positive bucket that overrides 'best' at the display layer and is applied by the Analysis wrapper post-hoc (do NOT teach classifyMoveQuality itself about gem — it stays single-position, Maia-unaware, per Open Question 3). In `bucketKeyForQuality` (lines 197-211) add `case 'gem':` folding into the existing `return 'good'` display bucket alongside `case 'best'` and `case 'good'` (per CONTEXT.md discretion note "into the green good display segment") — this is mandatory so the `default: return 'pending'` branch never silently swallows a gem (Pitfall 4). Do NOT extend `FlawSeverity` (cross-stack contract) and do NOT add a new QualityBucketKey. In `moveQuality.test.ts`, add assertions that `bucketKeyForQuality('gem')` returns `'good'`, and that a `qualityBySan` entry with quality 'gem' is grouped into the 'good' bucket by `bucketMovesByQuality` (extend the existing bucket test with a 'gem'-quality SAN).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/moveQuality.test.ts && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'gem'" frontend/src/lib/moveQuality.ts` shows 'gem' in the MoveQuality union AND a `case 'gem':` in bucketKeyForQuality.
    - `cd frontend && npx vitest run src/lib/__tests__/moveQuality.test.ts` passes including the new 'gem' → 'good' bucket assertions.
    - `cd frontend && npx tsc -b` compiles with zero errors (the type widening does not break existing exhaustive switches — colorForQuality etc. still fall through to their defaults until Plan 03).
    - `grep -n "gem" frontend/src/types/library.ts` returns no matches (FlawSeverity untouched).
    - classifyMoveQuality signature is unchanged: `grep -n "export function classifyMoveQuality" frontend/src/lib/moveQuality.ts` still shows the 3-arg (gradeMap, mover, designatedBestSan?) form.
  </acceptance_criteria>
  <done>MoveQuality includes 'gem'; bucketKeyForQuality maps 'gem' → 'good'; moveQuality tests pass; tsc -b clean; FlawSeverity and classifyMoveQuality unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side math over already-trusted, already-on-page engine primitives (FEN, evals, Maia probabilities). No user input, no new data entering/leaving the browser, no auth/session surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-01 | Tampering | gemMove.ts threshold constant | low | accept | Constant is client-side and presentational only (badge visibility); no security consequence to a manipulated value. RESEARCH Security Domain: no ASVS category applies. |

No `npm install` in this phase — no Package Legitimacy Gate required (`lucide-react` already installed).
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/gemMove.test.ts src/lib/__tests__/moveQuality.test.ts` green.
- `cd frontend && npx tsc -b` clean.
</verification>

<success_criteria>
GEM_MAIA_MAX_PROB (0.03), classifyGem, summarizeForGem exist and are fully unit-tested (both free-lunch guards + D-01/D-02/D-04/D-07); MoveQuality carries 'gem' folding to the 'good' display bucket; no FlawSeverity or classifyMoveQuality signature change.
</success_criteria>

<output>
Create `.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-01-SUMMARY.md` when done
</output>
