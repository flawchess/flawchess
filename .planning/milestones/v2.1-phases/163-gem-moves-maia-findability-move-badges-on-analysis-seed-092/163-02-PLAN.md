---
phase: 163-gem-moves-maia-findability-move-badges-on-analysis-seed-092
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/gemGlyph.ts
  - frontend/src/components/icons/GemIcon.tsx
  - frontend/src/components/board/boardMarkers.tsx
  - frontend/src/components/board/__tests__/boardMarkers.test.tsx
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "A single GEM_GLYPH record (color = MAIA_ACCENT) is the one source of truth for the gem badge, consumed by both the React GemIcon and the on-board SquareMarker gem branch so they never drift"
    - "GemIcon renders the white lucide Gem inside a MAIA_ACCENT circle, shape-compatible with SeverityGlyphIcon (className/style/aria-hidden), droppable into VariationTree's severity-icon call sites"
    - "SquareMarker gains an additive gem?: boolean; severity becomes optional; SquareMarkerBadge branches to the icon-content gem variant using the existing MARKER_RADIUS/MARKER_CORNER_OVERLAP geometry constants (no new geometry constants)"
    - "A gem SquareMarker with no severity renders the violet Gem badge without throwing (existing severity call sites still render ??/?/!? unchanged)"
  artifacts:
    - "frontend/src/lib/gemGlyph.ts"
    - "frontend/src/components/icons/GemIcon.tsx"
    - "frontend/src/components/board/__tests__/boardMarkers.test.tsx"
  key_links:
    - "gemGlyph.ts imports MAIA_ACCENT from @/lib/theme (no hard-coded violet); GemIcon and boardMarkers both import GEM_GLYPH.color from it"
    - "boardMarkers gem badge reuses lucide-react's Gem icon (already an installed dependency) — no new package"
---

<objective>
Build the gem badge visual primitives, following the existing severity-glyph single-source pattern (SEVERITY_GLYPH → SeverityGlyphIcon + boardMarkers). Add a `GEM_GLYPH` record (color = `MAIA_ACCENT`, seed-locked violet), a `GemIcon` React component (white lucide `Gem` in a violet circle, shape-compatible with `SeverityGlyphIcon`), and an additive icon-content gem variant on the board's `SquareMarker`/`SquareMarkerBadge`. These primitives are consumed later by VariationTree's move list (Plan 04) and Analysis's board `squareMarkers` union (Plan 04); nothing here depends on the `MoveQuality` type or the detection lib, so it runs fully parallel in Wave 1.

Purpose: One drift-proof gem badge (icon + on-board marker) reusing the proven glyph geometry, so the move list and board never disagree.
Output: `gemGlyph.ts`, `GemIcon.tsx`, the `SquareMarker.gem` extension + render branch, and a boardMarkers test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-CONTEXT.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md
@.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-PATTERNS.md
</context>

## Artifacts this phase produces

See Plan 01's "Artifacts this phase produces" list. This plan introduces: `GEM_GLYPH` (frontend/src/lib/gemGlyph.ts), `GemIcon` (frontend/src/components/icons/GemIcon.tsx), and `SquareMarker.gem` + the gem render branch (frontend/src/components/board/boardMarkers.tsx).

<tasks>

<task type="auto">
  <name>Task 1: gemGlyph.ts single-source record + GemIcon.tsx (white lucide Gem in a violet circle)</name>
  <files>frontend/src/lib/gemGlyph.ts, frontend/src/components/icons/GemIcon.tsx</files>
  <read_first>
    - frontend/src/lib/severityGlyph.ts (the single-source SEVERITY_GLYPH record pattern + its "plain module not component file, react-refresh component-only-export rule" docstring rationale)
    - frontend/src/components/icons/SeverityGlyphIcon.tsx (full file: SeverityGlyphIconProps shape, the GlyphBadge svg viewBox="0 0 24 24" + circle r=11 structure, the self-color rationale lines 8-11)
    - frontend/src/lib/theme.ts (MAIA_ACCENT line 74)
    - frontend/src/components/analysis/UnifiedMovePopover.tsx (line 25: existing `import { ChessKnight, Cpu, User } from 'lucide-react'` — confirms lucide-react is the icon source and Gem is available)
    - .planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-PATTERNS.md (GemIcon.tsx + severityGlyph sibling sections)
  </read_first>
  <action>
    Create `frontend/src/lib/gemGlyph.ts` as a plain module (not the icon component file, matching severityGlyph.ts's react-refresh rationale) exporting `GEM_GLYPH: { color: string }` where `color` imports `MAIA_ACCENT` from `@/lib/theme` (no hard-coded oklch literal). Add a module docstring stating this is the single source of truth for the gem badge color, consumed by both GemIcon and boardMarkers so they never drift, and that gem has no text symbol (it is an SVG icon, not a NAG character). Create `frontend/src/components/icons/GemIcon.tsx` exporting `GemIcon(props: { className?: string; style?: CSSProperties; 'aria-hidden'?: boolean | 'true' | 'false' })` — reuse the same prop shape as `SeverityGlyphIconProps` so it drops into VariationTree's `BlunderIcon`/`MistakeIcon` call sites. Render an `<svg viewBox="0 0 24 24" role="img">` containing `<circle cx="12" cy="12" r="11" fill={GEM_GLYPH.color} />` and, centered inside it, the lucide `Gem` icon in white: import `{ Gem } from 'lucide-react'` and render `<Gem x={5} y={5} width={14} height={14} stroke="#fff" />` (lucide spreads x/y/width/height onto its own root svg; the 14px icon offset to 5 spans 5..19, centered at 12). Bake the violet in from GEM_GLYPH (not caller-overridable), matching SeverityGlyphIcon's self-color rationale. Default `aria-hidden` to true. Add a JSDoc noting the AGPL-clean rationale is moot here (lucide Gem is MIT, unlike the drawn-from-scratch severity glyphs) but the shape parity is intentional.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAIA_ACCENT" frontend/src/lib/gemGlyph.ts` matches and `grep -n "oklch" frontend/src/lib/gemGlyph.ts` returns no matches (color only via theme import).
    - `grep -n "from 'lucide-react'" frontend/src/components/icons/GemIcon.tsx` matches `Gem` and `grep -n "GEM_GLYPH" frontend/src/components/icons/GemIcon.tsx` matches (single-source color).
    - `grep -n "className\|style\|aria-hidden" frontend/src/components/icons/GemIcon.tsx` shows the prop shape mirrors SeverityGlyphIconProps.
    - `cd frontend && npx tsc -b` clean and `npm run lint` clean (no unused exports; GemIcon will be consumed by Plan 04 — if knip flags it as unused at this wave, that is expected until Plan 04 merges; note it in the SUMMARY rather than deleting the export).
  </acceptance_criteria>
  <done>gemGlyph.ts exports GEM_GLYPH (violet from theme); GemIcon renders a white lucide Gem in a MAIA_ACCENT circle with SeverityGlyphIcon-compatible props; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: SquareMarker gem variant on boardMarkers.tsx + test</name>
  <files>frontend/src/components/board/boardMarkers.tsx, frontend/src/components/board/__tests__/boardMarkers.test.tsx</files>
  <read_first>
    - frontend/src/components/board/boardMarkers.tsx (full file: SquareMarker interface lines 16-23, geometry constants lines 40-45, SquareMarkerBadge lines 84-122, SquareMarkerGroup lines 124-148)
    - frontend/src/lib/gemGlyph.ts (from Task 1 — GEM_GLYPH.color)
    - frontend/src/components/board/__tests__/PlayerBar.test.tsx (an existing board test in the same __tests__ dir — for the render/test harness pattern, imports, and vitest conventions used here)
    - .planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-RESEARCH.md (Pattern 4: additive optional field, lower-risk than a discriminated union; hand-draw vs nested-lucide both viable)
  </read_first>
  <action>
    In `frontend/src/components/board/boardMarkers.tsx`, extend the `SquareMarker` interface additively: make `severity?: FlawSeverity` optional and add `gem?: boolean` (mutually exclusive with severity in practice — no runtime assertion needed per RESEARCH Open Question 2). In `SquareMarkerBadge`, branch on `marker.gem`: when true, render the gem variant — a `<circle cx={cx} cy={cy} r={r} fill={GEM_GLYPH.color} stroke={MARKER_STROKE} strokeWidth={1} />` (import GEM_GLYPH from `@/lib/gemGlyph`) with the lucide `Gem` centered on it in white via a nested `<Gem x={cx - iconSize/2} y={cy - iconSize/2} width={iconSize} height={iconSize} stroke="#fff" />` where `iconSize = 2 * r * 0.8` (icon at ~80% of the circle diameter so it sits inside the stroke). Reuse the EXISTING geometry (`MARKER_RADIUS`/`MARKER_RADIUS_SMALL`/`MARKER_CORNER_OVERLAP`, the same `cx`/`cy`/`r` computation already at lines 95-102) — do NOT introduce new geometry constants. When `marker.gem` is falsy, keep the current severity `<text>` glyph path exactly as-is, but guard the `SEVERITY_GLYPH[marker.severity]` lookup so a severity-less marker (only possible when gem is set) never indexes with undefined — read `glyph` only inside the non-gem branch. Leave `DepthLabel`/`SquareMarkerGroup` unchanged (a gem marker carries no depth label). Create `frontend/src/components/board/__tests__/boardMarkers.test.tsx` rendering `SquareMarkerGroup` with a `{ square: 'e4', gem: true }` marker and asserting: a `<circle>` with the MAIA_ACCENT fill is present, a nested gem icon element renders, and no severity `<text>` glyph ('??'/'?'/'!?') is emitted; plus a regression case rendering a `{ square: 'e4', severity: 'blunder' }` marker still emits the '??' text glyph. Use the same render harness the sibling board tests use (jsdom; wrap the group in an `<svg>` container for mounting).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/board/__tests__/boardMarkers.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gem?: boolean" frontend/src/components/board/boardMarkers.tsx` matches and `severity` in the SquareMarker interface is now optional (`severity?:`).
    - `grep -n "GEM_GLYPH" frontend/src/components/board/boardMarkers.tsx` matches (single-source color, no hard-coded violet).
    - `grep -n "MARKER_RADIUS\|MARKER_CORNER_OVERLAP" frontend/src/components/board/boardMarkers.tsx` still shows only the pre-existing constants (no new geometry constant added for gem).
    - `cd frontend && npx vitest run src/components/board/__tests__/boardMarkers.test.tsx` passes: gem marker renders the violet circle + gem icon and no severity text; the blunder regression case still renders '??'.
    - `cd frontend && npx tsc -b` clean (existing severity-only SquareMarker call sites still compile with severity now optional).
  </acceptance_criteria>
  <done>SquareMarker has an additive gem?: boolean; SquareMarkerBadge renders the violet lucide-Gem badge for gem markers and the unchanged severity glyph otherwise; boardMarkers test covers both branches; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Purely presentational SVG rendering of client-side-derived flags. No input, no data flow, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-02 | Information Disclosure | on-board gem badge | low | accept | The badge only reveals a client-computed presentational label about the user's own analysis position; nothing sensitive crosses a boundary. RESEARCH Security Domain: no ASVS category applies. |

No `npm install` — lucide-react is an existing dependency; no Package Legitimacy Gate required.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/board/__tests__/boardMarkers.test.tsx` green.
- `cd frontend && npx tsc -b && npm run lint` clean.
</verification>

<success_criteria>
A drift-proof gem badge exists as both a React `GemIcon` and an on-board `SquareMarker.gem` variant, sharing the single `GEM_GLYPH` (MAIA_ACCENT) source, reusing the proven marker geometry, with both the gem and severity render branches test-covered.
</success_criteria>

<output>
Create `.planning/phases/163-gem-moves-maia-findability-move-badges-on-analysis-seed-092/163-02-SUMMARY.md` when done
</output>
