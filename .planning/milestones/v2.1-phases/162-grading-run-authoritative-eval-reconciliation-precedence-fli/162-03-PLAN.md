---
phase: 162-grading-run-authoritative-eval-reconciliation-precedence-fli
plan: 03
type: execute
wave: 3
depends_on: [162-01, 162-02]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/pages/__tests__/Analysis.test.tsx
  - frontend/src/hooks/__tests__/useGameOverlay.test.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-12, D-13]
must_haves:
  truths:
    - "The green Stockfish best-move arrow targets the true global reconciled argmax UCI, even when that move is not one of the Stockfish card's 2 displayed lines"
    - "The agreement verdict's Stockfish side names the reconciled-argmax move with its reconciled eval, not raw engine.pvLines[0]"
    - "Off the game main line, the right eval bar refines once to the reconciled best move's eval when grading lands"
    - "The Stockfish card's two lines read reconciled evals and re-sort by them; the card headline keeps the free run's depth"
  artifacts:
    - "Analysis.tsx defines reconciledStockfishLine, reconciledBestEval, reconciledPvLines memos threaded to the verdict, useGameOverlay, and EngineLines"
    - "useGameOverlay.test.ts covers the reconciled eval passthrough on the off-main-line branch"
  key_links:
    - "arrow, verdict, eval bar, and card all consume the SINGLE reconciledBestUci from 162-02 (no independent argmax re-derivation)"
    - "useGameOverlay internal logic is unchanged; only the engineEvalCp/engineEvalMate/engineDepth params passed from Analysis.tsx change (D-08)"
    - "FC hover previews still read the reconciled lookup, never the MCTS pool grade (D-06 carried)"
---

<objective>
Thread the single canonical reconciled-best value (from 162-02) into every remaining display consumer so arrow, verdict, eval bar, card, chart, and labels agree by construction: the green SF arrow follows the true global reconciled argmax (D-07/D-12), the agreement verdict's Stockfish side is re-sourced to that argmax move (D-13), the off-main-line eval bar refines to the reconciled best's eval (D-08), and the Stockfish card reads reconciled evals and re-sorts its 2 lines (D-04) while keeping the free-run depth label (D-05). Source C stays display-excluded (D-06).

Purpose: These are the four call sites that would otherwise silently reintroduce the exact bug this phase closes — two of them (the verdict's `stockfishLine` and `useGameOverlay`'s eval passthrough) bypass `evalLookup` entirely today and were only harmless under the OLD free-run-first precedence (RESEARCH Pitfall 1).
Output: New reconciled memos threaded into the verdict, useGameOverlay call site, and EngineLines (both desktop and mobile), plus integration/unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-CONTEXT.md
@.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-PATTERNS.md
@.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-RESEARCH.md
@.planning/seeds/SEED-090-grading-run-authoritative-eval-precedence-flip.md
@frontend/src/lib/engineEvalLookup.ts
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (exclude from cross-phase drift verification):
- `reconciledStockfishLine` — new `useMemo` in `Analysis.tsx` (PvLine-shaped object from reconciledBestUci + its lookup eval) fed to `FlawChessAgreementVerdict.stockfishLine`.
- `reconciledBestEval` — new derivation in `Analysis.tsx` (getByUci over reconciledBestUci) fed to `useGameOverlay`'s engineEvalCp/engineEvalMate/engineDepth params.
- `reconciledPvLines` — new `useMemo` in `Analysis.tsx` (engine.pvLines mapped through evalLookup, re-sorted by reconciled expected score) fed to `EngineLines` desktop + mobile.
- New test cases in `Analysis.test.tsx` (arrow + verdict provenance) and `useGameOverlay.test.ts` (reconciled passthrough).
Consumes from 162-01/162-02: `resolveReconciledBest`, `getByUci`, `reconciledBestUci`, grading-first `buildEvalLookup`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SF arrow follows global reconciled argmax + verdict re-sourced (D-07/D-12/D-13)</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/pages/__tests__/Analysis.test.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lines 1275-1306 engineArrows SF branch; 1840-1891 verdict wiring with stockfishLine={engine.pvLines[0]}; 840-851 reconciledRankedLines "resolve rootMove UCI through evalLookup, spread into display object" pattern to mirror)
    - frontend/src/components/analysis/FlawChessAgreementVerdict.tsx (stockfishLine prop shape — what PvLine fields the verdict reads)
    - frontend/src/lib/engineEvalLookup.ts (getByUci, resolveReconciledBest)
    - frontend/src/hooks/uciParser.ts (PvLine type — fields needed to construct a synthetic line)
    - .planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-RESEARCH.md (Pitfall 1 stockfishLine bypass; Pitfall 2 + Open Q1/Q2 — D-12/D-13 resolve to the TRUE global argmax; ARROW_COUNT=1 single-substitution note)
  </read_first>
  <behavior>
    - The green SF arrow (layerKey sf-0) points to the from/to squares of reconciledBestUci (the global argmax), which may be a move outside the Stockfish card's 2 lines (accepted, documented edge case D-12).
    - The agreement verdict's stockfishLine names the reconciled-argmax move with its reconciled eval (constructed synthetic PvLine), not raw engine.pvLines[0].
    - When grading has not yet produced a best (reconciledBestUci null), the arrow/verdict fall back to the free-run engine.pvLines[0] move so first paint still shows an arrow (no regression, no crash).
    - Provenance test: when the SAME move is graded to a DIFFERENT value by the two sources, the verdict shows the grading (reconciled) value, not the stale free-run value.
  </behavior>
  <action>
    Reuse the single `reconciledBestUci` memo from 162-02 (do NOT re-derive an argmax here — RESEARCH Anti-Pattern). In the `engineArrows` SF branch (~1291-1303, `ARROW_COUNT === 1` so the loop runs once at i=0), replace `engine.pvLines[i]?.moves[0]` with `reconciledBestUci ?? engine.pvLines[0]?.moves[0] ?? null` before `uciToSquares(...)`. Add `reconciledBestUci` to the memo dependency array. Keep the amber FC arrow branch untouched (practical pick, D-07).

    Add a new small `reconciledStockfishLine` useMemo that builds a PvLine-shaped object from the reconciled best: resolve `getByUci(evalLookup, reconciledBestUci)` for its eval, and construct `{ multipv: 1, depth: <free-run depth>, moves: [reconciledBestUci], evalCp, evalMate }` (mirror the reconciledRankedLines spread pattern + the pvLine() test fixture shape). Return null when reconciledBestUci is null (verdict already handles a null stockfishLine — falls back to the free-run object only if you choose; prefer null so the verdict's own gating shows its "grading pending" state consistently). Replace `stockfishLine={engine.pvLines[0] ?? null}` (~1881) with `stockfishLine={reconciledStockfishLine ?? (engine.pvLines[0] ?? null)}` so first paint keeps an arrow-consistent value until grading lands. Update the verdict comment block (currently says it reads "engine.pvLines[0] (D-01)") to cite D-13/Phase 162: the SF side is now the reconciled argmax, which may name a different move.

    Extend the "Reconciled eval provenance" describe block in Analysis.test.tsx: (1) a same-move-different-value case where the grading run grades the free-run best move to a different eval, asserting the verdict prose shows the reconciled (grading) number — the coverage gap RESEARCH Pitfall 1 identified in the existing test at line ~443; (2) an arrow case asserting the sf-0 arrow targets the reconciled-argmax square when a Maia/FC-only candidate is the global argmax (D-12).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` passes including both new cases.
    - `grep -n "reconciledStockfishLine" frontend/src/pages/Analysis.tsx` shows the memo fed to FlawChessAgreementVerdict.
    - The engineArrows SF branch references `reconciledBestUci` (grep confirms), not a fresh argmax loop.
    - `stockfishLine={engine.pvLines[0]` no longer appears as the sole verdict source (grep shows reconciledStockfishLine in the prop).
  </acceptance_criteria>
  <done>Arrow and verdict both consume the single reconciledBestUci; the verdict's Stockfish side shows the reconciled move + eval; the same-move-different-value provenance gap is now tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eval bar via useGameOverlay + Stockfish card reconciled re-sort (D-08/D-04/D-05/D-06)</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/pages/__tests__/Analysis.test.tsx, frontend/src/hooks/__tests__/useGameOverlay.test.ts</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lines 1002-1015 useGameOverlay call site passing engineEvalCp/engineEvalMate/engineDepth; 2261 depth label; 2278-2285 desktop EngineLines; 1956 mobile EngineLines mobileEngineLines block)
    - frontend/src/hooks/useGameOverlay.ts (lines 100-102 params; 178-186 enginePassthrough off-main-line branch; 313-324 on-main-line precomputed-vs-engine pattern — the hook needs NO internal change, only caller params)
    - frontend/src/components/analysis/EngineLines.tsx (reads pvLines[i].evalCp/evalMate directly; re-sorting must happen at the caller)
    - frontend/src/hooks/__tests__/useGameOverlay.test.ts (existing enginePassthrough coverage to extend)
    - .planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-RESEARCH.md (Pitfall 1 useGameOverlay bypass; D-08 refine-once; D-05 depth-label-stays-free-run)
  </read_first>
  <behavior>
    - Off the game main line (enginePassthrough branch), the eval bar's cp/mate refine once to the reconciled best move's eval when grading lands; before grading, they show the free-run value (D-08 refine-once, first paint unchanged).
    - When gradingEnabled is false, the eval bar stays free-run (reconciledBestEval falls back to the raw free-run eval — natural lookup fallback, no special-casing).
    - The Stockfish card's two lines display reconciled evals (via evalLookup) and are re-ordered by reconciled expected score, so card line 1 agrees with the chart's Best crown on near-ties (D-04); the card's PV move-sequence text stays free-run.
    - The card headline still shows the free run's depth (D-05 — `engine.depth`, unchanged).
    - FC card hover previews still read the reconciled lookup, never the MCTS pool grade (D-06 — verify no pool-grade path is introduced).
  </behavior>
  <action>
    Add a `reconciledBestEval` derivation: `getByUci(evalLookup, reconciledBestUci)` when reconciledBestUci is non-null, else fall back to the raw free-run `{ evalCp: engine.evalCp, evalMate: engine.evalMate, depth: engine.depth }`. Thread its `evalCp`/`evalMate`/`depth` into the `useGameOverlay({...})` call (~1012-1014) as `engineEvalCp`/`engineEvalMate`/`engineDepth`, replacing the raw `engine.evalCp`/`engine.evalMate`/`engine.depth`. Do NOT modify `useGameOverlay.ts` internals — the on-main-line precomputed branch still wins; only the off-main-line passthrough source changes (RESEARCH Pitfall 1 fix is caller-side).

    Add a `reconciledPvLines` useMemo: map each `engine.pvLines` entry to a copy with its eval replaced by `getByUci(evalLookup, line.moves[0])` (falling back to the line's own free-run eval when not yet graded — a no-op pre-grading since free-run values fill the map), then re-sort the (≤2) lines by reconciled expected score (reuse `evalToExpectedScore` + `sideToMoveFromFen(position)`; keep `multipv`/`moves`/`depth` from the source line so PV text stays free-run per D-04). Pass `reconciledPvLines` to BOTH the desktop `<EngineLines pvLines=...>` (~2278) and the mobile `mobileEngineLines` `<EngineLines pvLines=...>` (~1956) — do not change one without the other (CLAUDE.md mobile-parity rule). Leave the depth label at line ~2261 reading `engine.depth` (D-05). Do not touch the FC hover path (D-06 — it already reads reconciledRankedLines/evalLookup; confirm no MCTS pool grade is newly introduced).

    Extend `useGameOverlay.test.ts` to assert the off-main-line `enginePassthrough` branch surfaces whatever `engineEvalCp`/`engineEvalMate`/`engineDepth` the caller passes (proving the reconciled value flows through unchanged) — if existing coverage already asserts this passthrough, add the reconciled-value case; otherwise add a focused test for the passthrough branch. Optionally add an Analysis.test.tsx assertion that the desktop and mobile EngineLines both receive reconciled-eval lines.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useGameOverlay.test.ts src/pages/__tests__/Analysis.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/__tests__/useGameOverlay.test.ts src/pages/__tests__/Analysis.test.tsx` passes.
    - `grep -n "reconciledBestEval" frontend/src/pages/Analysis.tsx` shows it threaded into the useGameOverlay call; `engine.evalCp` is no longer passed raw to useGameOverlay's engineEvalCp param.
    - `grep -n "reconciledPvLines" frontend/src/pages/Analysis.tsx` shows it passed to EngineLines in BOTH the desktop and mobile render sites (two matches as props).
    - The depth label at ~2261 still reads `engine.depth` (D-05 unchanged).
    - `useGameOverlay.ts` has no diff (caller-side change only) — `git diff --quiet frontend/src/hooks/useGameOverlay.ts` returns 0.
  </acceptance_criteria>
  <done>The off-main-line eval bar refines once to the reconciled best's eval; the Stockfish card reads reconciled evals and re-sorts its 2 lines with the free-run depth label intact; mobile + desktop updated together; useGameOverlay internals untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Arrow/verdict/eval-bar/card all render already-validated engine SAN/UCI strings and numeric evals; no new input, network, persistence, or unescaped-string path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-03 | Information Disclosure | reconciled eval display (arrow/verdict/bar/card) | low | accept | All displayed values derive from the local WASM engine; no cross-user or sensitive data. Strings render as auto-escaped React children (T-137-03 already mitigated), unchanged by this plan. No mitigation required. |

No package installs in this plan.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/hooks/__tests__/useGameOverlay.test.ts src/pages/__tests__/Analysis.test.tsx` green.
- `cd frontend && npm test -- --run` full frontend suite green.
- `cd frontend && npm run lint` clean.
- `cd frontend && npx tsc -b` clean (the only type gate).
</verification>

<success_criteria>
- Arrow (D-07/D-12), verdict (D-13), eval bar (D-08), and card (D-04) all consume the single reconciledBestUci / reconciled evals — no independent argmax.
- Depth label stays free-run (D-05); Source C stays display-excluded (D-06); useGameOverlay internals unchanged.
- Mobile + desktop EngineLines updated together.
- Full frontend suite + lint + tsc green.
</success_criteria>

<output>
Create `.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-03-SUMMARY.md` when done.

## Post-phase manual UAT (D-10/D-12, from 162-VALIDATION.md — not blocking plan completion)
- On `/analysis`, load a position, watch Best/Good labels and the green arrow settle once grading lands (~4s); confirm no "Good" move ever shows a higher number than the "Best" move.
- On the SEED-089 screenshot position, confirm the Stockfish card, FC card prose, and Maia tooltip show identical numbers for the same moves, ordering consistent with labels; confirm the arrow may point at a move the Stockfish card doesn't list (accepted D-12 edge case), not a surprise.
- Rapid game navigation leaves no orphaned grades; card first paint stays <100ms.
</output>
