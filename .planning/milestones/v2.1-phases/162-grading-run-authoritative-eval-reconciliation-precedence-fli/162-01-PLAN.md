---
phase: 162-grading-run-authoritative-eval-reconciliation-precedence-fli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/engineEvalLookup.ts
  - frontend/src/lib/engineEvalLookup.test.ts
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "buildEvalLookup resolves a move present in BOTH sources to the grading value, not the free-run value"
    - "buildEvalLookup still fills not-yet-graded moves from the free run (placeholder role preserved)"
    - "resolveReconciledBest returns the UCI with the highest reconciled expected score, tie-break toward the free-run pin"
  artifacts:
    - "frontend/src/lib/engineEvalLookup.ts exports resolveReconciledBest"
    - "frontend/src/lib/engineEvalLookup.test.ts asserts grading-first precedence and the mirror-image best case"
  key_links:
    - "resolveReconciledBest reuses evalToExpectedScore from @/lib/liveFlaw (single sigmoid source)"
    - "buildEvalLookup keeps the !lookup.has(uci) insertion-order-wins idiom, only the two loops reorder"
---

<objective>
Establish the pure-lib foundation for the precedence flip: make the grading run authoritative inside `buildEvalLookup` (D-01) and add the single canonical "reconciled best move" resolver `resolveReconciledBest` (D-03) that every downstream display consumer (labels, arrow, verdict, eval bar) will thread through in later waves.

Purpose: These are worker-free, deterministic, unit-testable functions. Landing them first (Wave 1) lets the Analysis.tsx wiring plans (Waves 2-3) import a proven `resolveReconciledBest` and rely on grading-first precedence, instead of re-deriving argmax at each display site (the Phase 158 anti-pattern this phase exists to kill).
Output: A grading-first `buildEvalLookup`, a new exported `resolveReconciledBest` helper, and inverted/extended unit tests proving both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-CONTEXT.md
@.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-PATTERNS.md
@.planning/seeds/SEED-090-grading-run-authoritative-eval-precedence-flip.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (exclude from cross-phase drift verification):
- `resolveReconciledBest(evalLookup, candidateUcis, mover, tieBreakUci): string | null` — new exported function in `frontend/src/lib/engineEvalLookup.ts`.
- New/renamed `describe` blocks and test cases in `frontend/src/lib/engineEvalLookup.test.ts` (grading-first precedence; resolveReconciledBest argmax + tie-break + mirror-image best case).
Downstream (Waves 2-3) will introduce: `reconciledBestUci`, `reconciledStockfishLine`, `reconciledPvLines`, `reconciledBestEval`, `freeRunCommitted` memos in `Analysis.tsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip buildEvalLookup to grading-first precedence (D-01)</name>
  <files>frontend/src/lib/engineEvalLookup.ts, frontend/src/lib/engineEvalLookup.test.ts</files>
  <read_first>
    - frontend/src/lib/engineEvalLookup.ts (the function + module docstring being changed — currently states free-run-first as LOCKED in 3 places)
    - frontend/src/lib/engineEvalLookup.test.ts (the 2 precedence-overlap assertions to invert + describe titles to rename)
    - .planning/seeds/SEED-090-grading-run-authoritative-eval-precedence-flip.md (change 2: grading-first rationale, depth-parity justification)
    - .planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-PATTERNS.md (buildEvalLookup precedence-flip section — "swap loop order only, preserve !lookup.has idiom verbatim")
  </read_first>
  <behavior>
    - A UCI present in BOTH pvLines and gradeMapBySan now resolves to the grading (gradeMap) value, not the free-run pvLines value.
    - A UCI present ONLY in pvLines (not yet graded) still resolves to the free-run value (placeholder role intact).
    - A UCI present ONLY in gradeMapBySan resolves via sanToUci to the grading value (unchanged from today).
    - An unresolvable SAN in gradeMapBySan is skipped silently, no throw (unchanged).
    - A UCI in neither source resolves to null via getByUci (no pool-grade fallback — unchanged).
  </behavior>
  <action>
    Reorder the two loops in `buildEvalLookup` so the `gradeMapBySan` loop runs FIRST (inserting each grading grade under its `sanToUci(baseFen, san)` UCI key) and the `pvLines` loop runs SECOND (inserting each `moves[0]` free-run entry only when `!lookup.has(uci)`). Keep the exact `!lookup.has(uci)` insertion-order-wins guard on BOTH loops and the silent `continue` on `uci === undefined` / `uci === null` — this is the load-bearing idiom; do not change guard shape, do not add unconditional overwrite. The net effect is grading-wins-on-overlap, free-run-fills-gaps.

    Rewrite the module docstring (top-of-file block and the `buildEvalLookup` doc comment) to describe grading-first precedence and cite SEED-090 / Phase 162 as the source, replacing every "free-run-first ... LOCKED" / "always resolves to the free-run value" statement. Keep the accurate parts: single UCI-keyed lookup, `sanToUci` reuse, no MCTS pool grade parameter (Source C exclusion, D-06), null-on-absent.

    In `engineEvalLookup.test.ts`: invert the two overlap assertions in the "free-run-first precedence" describe block (the both-sources case must now expect the grading value 999, and the never-regresses case must now expect the gradeMap value for the same move) and rename the describe block titles to "grading-first precedence". Preserve the gradeMap-only, unresolvable-SAN, no-pool-fallback, and getBySan tests as-is (their expectations are unchanged by the flip; only the shared-move precedence outcome inverts).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engineEvalLookup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engineEvalLookup.test.ts` passes.
    - The both-sources overlap test asserts the grading value (e.g. `getByUci(lookup, 'e2e4')` returns the `grade(...)` value, not the `pvLine(...)` value).
    - `grep -n "grading-first" frontend/src/lib/engineEvalLookup.ts` returns at least one match; `grep -c "free-run-first" frontend/src/lib/engineEvalLookup.ts` returns 0.
    - No new SAN↔UCI conversion helper is introduced (still imports `sanToUci` from `@/lib/sanToSquares`).
  </acceptance_criteria>
  <done>buildEvalLookup resolves overlapping moves to the grading value while still filling not-yet-graded moves from the free run; docstring and tests describe grading-first precedence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resolveReconciledBest canonical argmax helper (D-03)</name>
  <files>frontend/src/lib/engineEvalLookup.ts, frontend/src/lib/engineEvalLookup.test.ts</files>
  <read_first>
    - frontend/src/lib/engineEvalLookup.ts (co-location target — already imports MoveGrade)
    - frontend/src/lib/moveQuality.ts (lines 73-106 — classifyMoveQuality's internal top-scorer + designated-pin loop is the exact shape to mirror; MoverColor/evalToExpectedScore usage)
    - frontend/src/lib/liveFlaw.ts (evalToExpectedScore + MoverColor — the ONLY sigmoid, do not re-derive)
    - .planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-PATTERNS.md (resolveReconciledBest section — suggested signature + adaptation notes)
  </read_first>
  <behavior>
    - Given an evalLookup Map and a list of candidate UCIs, returns the UCI whose reconciled eval has the highest expected score for the given mover.
    - A candidate UCI absent from evalLookup is skipped (never selected).
    - On an EXACT expected-score tie between two candidates, the UCI equal to tieBreakUci wins; otherwise the first-seen higher score wins.
    - Returns null when no candidate resolves to a grade.
    - Mirror-image best case: given a grading map where a non-tieBreak move has the strictly higher expected score, that move is returned (NOT tieBreakUci).
  </behavior>
  <action>
    Add an exported `resolveReconciledBest(evalLookup: Map<string, MoveGrade>, candidateUcis: string[], mover: MoverColor, tieBreakUci: string | null): string | null` to `engineEvalLookup.ts`. Loop `candidateUcis`, `continue` on a UCI absent from `evalLookup` (skip-silently convention), compute expected score via `evalToExpectedScore(grade.evalCp, grade.evalMate, mover)` from `@/lib/liveFlaw`, track a running `bestEs`/`bestUci`; on `es > bestEs` take it, on the exact-equality branch `es === bestEs && uci === tieBreakUci` prefer the tie-break UCI. Return `bestUci` (null when nothing resolved). Import `evalToExpectedScore` and the `MoverColor` type from `@/lib/liveFlaw`. Add an explicit return type. Do NOT re-implement the sigmoid or a second SAN/UCI converter.

    Add a new `describe('resolveReconciledBest', ...)` block to `engineEvalLookup.test.ts` covering: (1) argmax picks the highest-expected-score UCI; (2) the mandatory mirror-image best case — a candidate that is NOT the tieBreakUci but has the top reconciled eval is returned; (3) exact-tie resolves toward tieBreakUci; (4) an all-absent candidate list returns null; (5) a candidate absent from the lookup is skipped. Build fixtures from the existing `grade(...)`/`buildEvalLookup(...)` helpers; use white-POV cp values so the mover→expected-score direction is unambiguous.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engineEvalLookup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engineEvalLookup.test.ts` passes including the new resolveReconciledBest block.
    - `grep -n "export function resolveReconciledBest" frontend/src/lib/engineEvalLookup.ts` returns one match.
    - `grep -n "evalToExpectedScore" frontend/src/lib/engineEvalLookup.ts` shows it is imported/used (no re-derived sigmoid).
    - The mirror-image test asserts a non-tieBreak UCI is returned when it has the strictly higher expected score.
  </acceptance_criteria>
  <done>resolveReconciledBest returns the reconciled argmax UCI with a tie-break toward the free-run pin, proven by unit tests including the mirror-image case; sigmoid math is reused, not re-derived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes precedence/argmax over numbers already computed locally by the in-browser WASM engine. No new user input, network call, persistence, or string-rendering path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-01 | Tampering | resolveReconciledBest / buildEvalLookup inputs | low | accept | All inputs are engine-produced, already-validated SAN/UCI strings and numeric evals flowing through existing `sanToUci`/`chess.js` paths; no new external input is parsed. No mitigation required. |

No package installs in this plan (no Package Legitimacy Audit applies).
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engineEvalLookup.test.ts` green.
- `cd frontend && npx tsc -b` clean (esbuild strips types; tsc is the only type gate — per feedback_frontend_run_tsc_build).
</verification>

<success_criteria>
- buildEvalLookup is grading-first; overlapping moves resolve to the grading value.
- resolveReconciledBest exported and unit-proven (argmax + tie-break + mirror-image).
- No new sigmoid or SAN/UCI converter; `free-run-first` no longer appears in the module.
- tsc clean.
</success_criteria>

<output>
Create `.planning/phases/162-grading-run-authoritative-eval-reconciliation-precedence-fli/162-01-SUMMARY.md` when done.
</output>
