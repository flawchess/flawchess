---
phase: 151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart
verified: 2026-07-05T13:00:00Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 151.1: Stockfish-graded Maia moves on the Moves-by-Rating chart — Verification Report

**Phase Goal:** Grade each shown Maia candidate move with the existing client Stockfish and
recolor the "Moves by Rating" chart lines by move quality (dark-green best → red blunder), and
replace the fixed top-6 cap with a "moves people actually play" candidate set (Maia
cumulative-probability ≥ 0.95 ∪ {SF-best}). Browser-only, ephemeral, zero DB writes.

**Verified:** 2026-07-05T13:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria SC1–SC4)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1 — Each shown Maia move (line + end-of-line SAN label) is colored by Stockfish quality in 5 buckets on FlawChess's own `liveFlaw.ts`/`flawThresholds.ts` thresholds; color=quality, SAN=identity, stroke=played/best emphasis; tokens live in `theme.ts` | ✓ VERIFIED | `moveQuality.ts:classifyMoveQuality` (61-87) computes best + 4 severity buckets via `evalToExpectedScore`/`classifyLiveSeverity` reused verbatim; `theme.ts` exports `MOVE_QUALITY_BEST/GOOD/INACCURACY/MISTAKE/BLUNDER/PENDING` (376-381), the 3 severity tokens alias `SEV_*` verbatim; `MovesByRatingChart.tsx:colorForQuality` (138-153) maps quality→color, stroke width is a separate `emphasized` boolean (402-410) keyed on played/best only — color and stroke are decoupled. Unit tests: 18/18 in `moveQuality.test.ts` (all 4 boundary thresholds straddled), 4 chart tests (blunder/pending/emphasis) + 4 tooltip-format tests, all passing (independently re-run, see below). |
| 2 | SC2 — Candidate/line set is Maia cumulative-probability ≥0.95 (at selected ELO) ∪ {SF-best}, replacing the fixed top-6-by-peak cap | ✓ VERIFIED | `moveQuality.ts:selectCandidatesByMass` (106-129) implements the 0.95-mass cut at the nearest-ELO rung, capped to top-5, unioned with `bestSan`/`playedSan`. `capMovesByPeak`/`collectCandidateSans`/`peakProbability` no longer exist anywhere in `frontend/src/` (grep confirmed, only comment references to the superseded name remain). `Analysis.tsx:521-524` computes `shownSans` via `selectCandidatesByMass` once and feeds both the grading hook and the chart. 6/6 `selectCandidatesByMass` unit tests passing. |
| 3 | SC3 — Stockfish grades every shown candidate via a SINGLE root search (`searchmoves`+`MultiPV=|candidates|`), confirmed on the vendored `stockfish-18-lite` WASM, without disturbing the eval bar/engine-card variation tree | ✓ VERIFIED | Engine-level mechanism: `useStockfishGradingEngine.ts` sends exactly one `setoption name MultiPV value <N>` + one `go depth 14 searchmoves <ucis> movetime 2500` per settled (fen, candidateSans) (232-236); keys the grade map by `pv[0]`'s replayed SAN, never by `multipv` index (319-323, matches Plan 01's confirmed real-binary landmine); it is a wholly separate `new Worker(ENGINE_PATH)` instance (291) that never imports/reads `useStockfishEngine` state. `git log` confirms zero commits touched `useStockfishEngine.ts` or its test file during this phase — the primary engine is provably untouched. MockWorker tests (10/10) prove the search-command shape, pv[0]-vs-multipv-index keying under simulated reordering, white-POV sign, cache short-circuit, and the FLAWCHESS-7V stop-before-go stale-guard. The engine-level `searchmoves`+`MultiPV>2` premise was independently confirmed headlessly against the real vendored binary in Plan 01 (verdict YES, probe deleted after sign-off — confirmed absent from disk, present only as create+delete commits in git history). Live real-WASM correctness + isolation (colors refine with depth, played bad move shows red/orange, SF-best dark-green, eval bar/engine card unchanged) was signed off by the user in a live desktop Chrome + mobile Safari session (151.1-04-SUMMARY.md coverage D5, `human_judgment: true`, `status: pass`) — not re-litigated here per the human-verification-already-occurred stance for this run; only the supporting code was re-verified. |
| 4 | SC4 — Per-navigation grading latency (desktop + mobile Safari) is measured and acceptable, or degrades gracefully (cap candidates / lower depth / debounce) | ✓ VERIFIED | Named degradation knobs exist as constants: `GRADING_TARGET_DEPTH=14`, `GRADING_MOVETIME_SAFETY_CAP_MS=2500`, `GRADE_CACHE_MAX=256`, `RAPID_STEP_DEBOUNCE_MS=150` (`useStockfishGradingEngine.ts:40-49`), plus `CANDIDATE_HARD_CAP=5` (`moveQuality.ts:51`) — all tunable without touching logic, satisfying the "or degrade" contract. ELO-drag cache short-circuit (no re-search when the displayed subset is already graded) is implemented (211-219) and unit-tested. Per-navigation latency measurement itself is a live-device metric (desktop Chrome + mobile Safari, triple-worker load) that cannot be produced in jsdom; the user measured and judged it acceptable at the default budget with no knob changes (151.1-04-SUMMARY.md coverage D6, `human_judgment: true`, `status: pass`). Treated as satisfied per the already-signed-off UAT for this run. |

**Score:** 4/4 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/lib/moveQuality.ts` | `MoveQuality` type (NOT extending `FlawSeverity`), `classifyMoveQuality`, `selectCandidatesByMass`, `CUMULATIVE_MASS_THRESHOLD=0.95`, `CANDIDATE_HARD_CAP=5` | ✓ VERIFIED | All exports present; confirmed `FlawSeverity` (types/library.ts) has zero references from `moveQuality.ts` other than the doc comment explaining it is NOT extended. |
| `frontend/src/lib/sanToSquares.ts` | `sanToUci(fen, san): string \| null` | ✓ VERIFIED | Present (line 63), try/catch→null pattern confirmed, imported and used by the grading hook. |
| `frontend/src/lib/theme.ts` | 6 `MOVE_QUALITY_*` tokens; dead `MOVES_BY_RATING_PLAYED_LINE/BEST_LINE/OTHER_LINES` removed | ✓ VERIFIED | 6 tokens present (376-381); dead tokens absent from exports (only referenced in a historical comment). `npm run knip` clean. |
| `frontend/src/hooks/useStockfishGradingEngine.ts` | Second independent worker hook `{ gradeMap, isGrading, isReady }` | ✓ VERIFIED | 423-line hook; separate `Worker(ENGINE_PATH)` instance; pv[0]-keyed cache; stop-before-go serialization; tab-hide pause; unmount cleanup — all present and structurally isolated from `useStockfishEngine`. |
| `frontend/src/components/analysis/MovesByRatingChart.tsx` | `shownSans`/`qualityBySan` props, `colorForQuality()`, decoupled stroke, D-08 tooltip | ✓ VERIFIED | All present; `capMovesByPeak`/`collectCandidateSans`/`peakProbability` fully removed. |
| `frontend/src/pages/Analysis.tsx` | `shownSans`/`qualityBySan` computed once, passed to BOTH `MaiaHumanPanel` call sites | ✓ VERIFIED | Desktop call site line 1104-1111, mobile call site line 1254-1261, both receive `shownSans`+`qualityBySan`. |
| `frontend/src/components/analysis/MaiaHumanPanel.tsx` | Pass-through props for `shownSans`/`qualityBySan` | ✓ VERIFIED | Props declared (36-39) and threaded to `MovesByRatingChart` (98-99). |
| `frontend/public/engine/searchmoves-probe.html` | Throwaway probe, deleted after Plan 01 sign-off | ✓ VERIFIED | Absent from disk; git history shows create (`d7397b65`) then delete (`6fa9798f`) commits — no dead public asset shipped. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `Analysis.tsx` | `selectCandidatesByMass` | `shownSans = useMemo(() => selectCandidatesByMass(maia.perElo, selectedElo, playedSan, bestSan), ...)` | ✓ WIRED | Line 521-524, real `maia.perElo`/`selectedElo`/`playedSan`/`bestSan` inputs (no hardcoded/empty values). |
| `Analysis.tsx` | `useStockfishGradingEngine` | `grading = useStockfishGradingEngine({ fen: engineEnabled ? position : null, candidateSans: shownSans, enabled: engineEnabled })` | ✓ WIRED | Line 530-534, gated on the same `engineEnabled` toggle as the primary engine. |
| `Analysis.tsx` | `classifyMoveQuality` | `qualityBySan = classifyMoveQuality(grading.gradeMap, sideToMoveFromFen(position))` merged with raw evals | ✓ WIRED | Line 538-550, consumes the live `grading.gradeMap`, not a static value. |
| `Analysis.tsx` | `MaiaHumanPanel` (desktop) | `shownSans`/`qualityBySan` props at line ~1104-1111 | ✓ WIRED | Confirmed by grep + read. |
| `Analysis.tsx` | `MaiaHumanPanel` (mobile) | `shownSans`/`qualityBySan` props at line ~1254-1261 | ✓ WIRED | Confirmed by grep + read — both surfaces receive identical new props (CLAUDE.md mobile-parity rule honored). |
| `MaiaHumanPanel.tsx` | `MovesByRatingChart` | pass-through of `shownSans`/`qualityBySan` | ✓ WIRED | Line 93-100. |
| `useStockfishGradingEngine.ts` | `sanToUci`/`uciToSquares` (sanToSquares.ts) | building `searchmoves` list + pv[0]→SAN replay | ✓ WIRED | Imports confirmed line 31; used at 225, 89. |
| `useStockfishGradingEngine.ts` | `parseInfoLine` (uciParser.ts) | info-line tokenizing reused verbatim | ✓ WIRED | Import confirmed line 30; used at 312. |

### Data-Flow Trace (Level 4)

`qualityBySan` (rendered by the chart) traces to `grading.gradeMap`, which is populated exclusively
from real `info`-line UCI parses inside the grading Worker's `onmessage` handler (`handleLine`,
309-349) — no static/hardcoded fallback exists in this path. `shownSans` traces to
`maia.perElo` (the live `useMaiaEngine` ELO-ladder inference output), not a static array. Both
data sources are real, non-empty in the running app; both are gated correctly (`engineEnabled`).
Status: ✓ FLOWING.

### Behavioral Spot-Checks (independently re-run, not taken from SUMMARY claims)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `npx tsc -b` | type-check the whole frontend | zero errors | ✓ PASS |
| `npm run lint` | eslint . | 0 errors (3 pre-existing unrelated `coverage/*` warnings) | ✓ PASS |
| `npm run knip` | dead-export/dependency scan | 0 issues (no output) | ✓ PASS |
| `npm test -- --run` | full vitest suite | 115 files / 1348 tests, all pass | ✓ PASS |
| `moveQuality.test.ts` (18 tests) | boundary/edge-case classification + selection | all pass | ✓ PASS |
| `useStockfishGradingEngine.test.ts` (10 tests, MockWorker) | search command shape, pv[0]-vs-multipv-index keying, white-POV sign, cache short-circuit, FLAWCHESS-7V stale-guard, tab-hide, unmount | all pass | ✓ PASS |
| `MovesByRatingChart.test.tsx` (8 tests) + `MovesByRatingTooltipContent` (4 tests) | exact shownSans rendering, color/stroke decoupling, PENDING for ungraded, D-08 tooltip format | all pass | ✓ PASS |
| `git log` for `useStockfishEngine.ts`/`useStockfishEngine.test.ts` since phase start | primary engine untouched (SC3 isolation) | zero matching commits | ✓ PASS |
| `git log` for `searchmoves-probe.html` + disk check | throwaway probe deleted after sign-off | create+delete commits found, file absent from disk | ✓ PASS |

This satisfies the behavior-dependent-truth bar (Step 3/7b): the state-transition and
cancellation/cleanup invariants called out in the plan (D-05 progressive streaming + cancel-on-nav,
Pitfall 2 cache short-circuit, Pitfall 5/FLAWCHESS-7V stop-before-go serialization) each have a
passing MockWorker test that exercises the transition directly (not merely symbol presence):
"stale-guard: a new FEN while thinking sends stop; deferred re-go fires only after the stale
bestmove", "cache short-circuit: re-rendering with an already-graded subset sends NO new go", and
"visibility hidden sends stop without terminating the Worker" all passed under the independent
re-run above.

### Requirements Coverage

No REQUIREMENTS.md IDs are mapped to Phase 151.1 (goal-backward verification from the 4 ROADMAP
success criteria per the phase's own framing) — this is expected and not a gap. For traceability:
Phase 151's `SURF-03` ("top-N-by-peak cap, default N≈6") remains marked Complete under Phase 151 in
REQUIREMENTS.md; this phase's D-02/D-06 explicitly supersede that cap rule for the Moves-by-Rating
chart surface specifically (documented in 151.1-CONTEXT.md and the ROADMAP phase block), which is
an intentional, pre-approved design supersession, not an unaddressed regression — `capMovesByPeak`
is provably deleted and its replacement (`selectCandidatesByMass`) is fully wired and tested.

### Anti-Patterns Found

None. Grep for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|coming soon|not yet implemented` across all
7 phase-modified core files returned zero debt markers. The only "placeholder" string matches are
legitimate prose describing the existing loading-skeleton UI pattern (`MovesByRatingChart.tsx:330`,
`349`; `MaiaHumanPanel.tsx:32`) and an unrelated pre-existing free-play ply placeholder comment in
`Analysis.tsx:774` — none are stubs in the code paths this phase touches.

### Human Verification — Already Signed Off (Prior UAT, Not Re-Escalated)

Two of the four success criteria are inherently measure-and-judge (mirroring Phase 151's VALID-01
posture) and were already verified live by the user during Plan 04's blocking
`checkpoint:human-verify` task, with the outcome captured in `151.1-04-SUMMARY.md`'s coverage block
(`D5`/`D6`, `human_judgment: true`, `status: pass`):

1. **SC3 live correctness + isolation** — desktop Chrome + mobile Safari: colors refine with depth,
   a played-but-bad move renders red/orange, SF-best renders dark-green, and the eval bar / engine
   card / variation tree behave exactly as before. Signed off, no issues reported.
2. **SC4 per-navigation latency** — desktop + mobile Safari: judged acceptable at the default
   grading budget (`GRADING_TARGET_DEPTH=14`, `GRADING_MOVETIME_SAFETY_CAP_MS=2500`,
   `CANDIDATE_HARD_CAP=5`); no degradation knobs were changed.

Per this verification run's scope, these are not re-opened as pending `human_verification` items —
only the code that supports them (the second grading worker's search-command shape, the pv[0]
keying, the isolation from the primary engine, and the named degradation-knob constants) was
independently re-verified above, and it matches what the signed-off UAT exercised.

### Gaps Summary

None. All 4 ROADMAP success criteria have either direct automated evidence (SC1, SC2, and the
engine-level mechanism of SC3) or documented, already-approved human sign-off (the live-browser
correctness portion of SC3 and all of SC4) plus supporting-code re-verification. The full automated
gate (`tsc -b`, `lint`, `knip`, full test suite — 115 files / 1348 tests) was independently
re-executed during this verification and is green. The primary `useStockfishEngine` instance and
its test suite are provably untouched (zero commits during this phase). The throwaway Plan 01 probe
is confirmed deleted. No debt markers, no stubs, no orphaned wiring found.

---

*Verified: 2026-07-05T13:00:00Z*
*Verifier: Claude (gsd-verifier)*
