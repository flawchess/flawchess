# Phase 151.1: Stockfish-graded Maia moves on the Moves-by-Rating chart - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-05
**Phase:** 151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart
**Areas discussed:** Pre-grade chart state, Line-count cap, Played-move handling, Tooltip / label content

> Note: D-01…D-04 (surface = chart-only, candidate set = 0.95-mass ∪ {best}, 5 colors on own
> thresholds, eval via one `searchmoves`+MultiPV search) were pre-locked in the SEED-083
> /gsd-explore session and NOT re-discussed here.

---

## Pre-grade chart state

| Option | Description | Selected |
|--------|-------------|----------|
| Neutral then recolor (silent) | Gray lines on Maia-ready, single silent recolor when SF finishes | |
| Neutral + 'grading…' cue | Same, plus an explicit grading indicator | |
| Hold until graded | Keep skeleton until both Maia + SF done | |

**User's choice:** Free-text override — progressive/streaming grading (rejected all three as framed).
**Notes:** "maiachess.com shows early stockfish grading results very quickly and updates them as
deeper stockfish depths are reached (similar to how we keep updating the 2 best stockfish lines).
Let's do the same." → Captured as D-05: lines colored from the first shallow eval, colors refine as
depth increases, mirroring `useStockfishEngine`'s streaming MultiPV updates; new nav cancels via
stale-guard.

---

## Line-count cap

| Option | Description | Selected |
|--------|-------------|----------|
| Cap at ~5 | Top-5 by prob from the 0.95-mass set ∪ {best}; ~5–6 max | ✓ |
| No cap (pure 0.95-mass) | Show the whole 0.95-mass ∪ {best} set | |
| Prob-floor instead | Drop moves below an absolute prob floor (e.g. ≥5%) ∪ {best} | |

**User's choice:** Cap at ~5 (recommended).
**Notes:** Bounds legibility at ~360px and grading cost to ≤~6 moves/nav. → D-06.

---

## Played-move handling

| Option | Description | Selected |
|--------|-------------|----------|
| Always show + keep stroke emphasis | Played move always drawn+graded even below cut; played & best keep thick stroke | ✓ |
| Always show, no stroke emphasis | Played always shown but no thicker stroke (color/label/tooltip only) | |
| Only show played if in set | Played appears only if it made the cut | |

**User's choice:** Always show + keep stroke emphasis.
**Notes:** The "you played a red move here" case is the highest-value coaching signal; honors
SURF-02. Color = quality, stroke width = role. → D-07.

---

## Tooltip / label content

| Option | Description | Selected |
|--------|-------------|----------|
| Quality word + eval in tooltip | Tooltip adds classification word + per-move eval; chart face unchanged | ✓ |
| Quality word only (no number) | Tooltip adds severity word, no cp number | |
| Color only (tooltip as-is) | Rely on color; leave tooltip probability-only | |

**User's choice:** Quality word + eval in tooltip.
**Notes:** Per-move eval = white-POV eval Stockfish assigns the move; quality word from
`classifyLiveSeverity` on the mover's expected-score drop vs best. SAN end-labels stay colored-SAN
only. → D-08.

## Claude's Discretion

- Engine contention / second analysis mode (dedicated grading worker vs. mode-switch) without
  disturbing the eval-bar/engine-card.
- Grade cache scope (position-keyed, ELO-independent, ephemeral board-session FIFO).
- Grading search depth / stop condition.
- Exact 5-color theme tokens in `theme.ts`.

## Deferred Ideas

- maiachess-style move-list table (rejected for this phase; chart-only D-1).
- Phase 152 flaw-overlay verdict banner + Maia-WDL practical-severity reframe.
