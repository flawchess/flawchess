---
phase: 151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart
plan: 03
type: execute
wave: 2
depends_on: [151.1-01, 151.1-02]
files_modified:
  - frontend/src/hooks/useStockfishGradingEngine.ts
  - frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts
autonomous: true
requirements: [SC3, SC4]
must_haves:
  truths:
    - "useStockfishGradingEngine creates its OWN second Worker(ENGINE_PATH) instance — a structural sibling of useStockfishEngine — and never touches the primary engine's worker, state machine, or consumers (SC3 isolation)."
    - "For a (fen, candidateSans) pair it sends `setoption name MultiPV value <N>` then `go depth <GRADING_TARGET_DEPTH> searchmoves <candidate ucis> movetime <GRADING_MOVETIME_SAFETY_CAP_MS>` — ONE root search, not N child searches (SC3, D-04)."
    - "It keys the grade map by `pv[0]` converted to SAN — NEVER by the `multipv` index (Pitfall 1: index is rank, reorders with depth)."
    - "It stores white-POV normalized evalCp/evalMate per SAN (negated when black is to move), so downstream expected-score + tooltip evals are correct on every ply (D-08)."
    - "It streams progressively: the grade map updates on every `info` line and refines as depth climbs; a new FEN cancels the in-flight search (D-05)."
    - "Grades are cached per-FEN; dragging the ELO slider (which only changes the displayed candidate subset, not the eval) does NOT re-issue a search when the newly-shown SANs are already graded (Pitfall 2)."
    - "The stop-before-go serialization (idle/thinking/stopping + stopPendingRef + deferred re-go on the stale bestmove) is carried over verbatim from useStockfishEngine (Pitfall 5 / FLAWCHESS-7V guard)."
    - "Degradation knobs (GRADING_TARGET_DEPTH, GRADING_MOVETIME_SAFETY_CAP_MS, and the candidate cap from Plan 02) are named constants, tunable for SC4 without touching logic."
  artifacts:
    - "frontend/src/hooks/useStockfishGradingEngine.ts — hook returning { gradeMap: Map<string, MoveGrade>; isGrading: boolean; isReady: boolean }"
    - "frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts — MockWorker unit tests"
  key_links:
    - "Consumes MoveGrade from Plan 02's moveQuality.ts and sanToUci from Plan 02's sanToSquares.ts."
    - "Reuses parseInfoLine (uciParser.ts) verbatim for info-line tokenizing."
    - "Depends on Plan 01's YES verdict that the vendored binary honors searchmoves + MultiPV>2."
---

<objective>
Build the grading engine: a SECOND, fully independent Stockfish WASM Web Worker
(`useStockfishGradingEngine`) that grades the shown Maia candidate moves via ONE
searchmoves-restricted MultiPV root search, streaming progressive `info`-line updates keyed
by `pv[0]` into a per-FEN grade cache — structurally isolated from the primary eval-bar /
engine-card `useStockfishEngine` instance so SC3's "without disturbing the eval bar / engine
card" holds by construction.

Purpose: Turn the shown candidate set (Plan 02's selectCandidatesByMass) into a live,
depth-refining `Map<SAN, MoveGrade>` the chart (Plan 04) colors from.
Output: useStockfishGradingEngine.ts + its MockWorker test suite.

Gate: Plan 01 must have returned "searchmoves works" before this plan proceeds. If Plan 01
flagged a failure, STOP and surface the D-04 fallback discussion instead of building this hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-RESEARCH.md
@.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-01-SUMMARY.md
@frontend/src/hooks/useStockfishEngine.ts
@frontend/src/hooks/useMaiaEngine.ts
@frontend/src/hooks/uciParser.ts
@frontend/src/lib/sanToSquares.ts
@frontend/src/lib/moveQuality.ts
@frontend/src/hooks/__tests__/useStockfishEngine.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useStockfishGradingEngine — second worker, searchmoves+MultiPV, pv[0]-keyed grade cache</name>
  <files>frontend/src/hooks/useStockfishGradingEngine.ts</files>
  <read_first>
    - frontend/src/hooks/useStockfishEngine.ts (COPY the worker lifecycle + state machine shape: ENGINE_PATH, classic `new Worker` with no module option, uci→uciok→setoption MultiPV→isready→readyok init, the idle/thinking/stopping stateRef + stopPendingRef + deferred re-go on stale bestmove FLAWCHESS-7V guard, currentFenRef/isReadyRef ref-for-latest-value, adaptive RAPID_STEP_DEBOUNCE_MS debounce, commit-on-info + commit-on-bestmove, analyzedSideToMoveRef white-POV normalization, tab-hide pause, unmount stop+terminate)
    - frontend/src/hooks/useMaiaEngine.ts (the per-FEN FIFO cache pattern: cacheRef Map + cacheResult with MAIA_CACHE_MAX eviction — mirror for the grade cache)
    - frontend/src/hooks/uciParser.ts (parseInfoLine → ParsedInfoLine { depth, multipv, scoreCp, scoreMate, bound, pv }; reuse verbatim)
    - frontend/src/lib/sanToSquares.ts (sanToUci for building the searchmoves list; uciToSquares for pv[0]→squares) and frontend/src/pages/Analysis.tsx bestSanFromPv (the uci→SAN replay logic to convert pv[0] back to SAN — extract/mirror it)
    - frontend/src/lib/moveQuality.ts (MoveGrade type; CANDIDATE_HARD_CAP)
  </read_first>
  <action>
    Create useStockfishGradingEngine.ts as a structural sibling of useStockfishEngine. Interface:
    `useStockfishGradingEngine({ fen: string | null; candidateSans: string[]; enabled: boolean }): { gradeMap: Map<string, MoveGrade>; isGrading: boolean; isReady: boolean }`.

    Named constants (no magic numbers): ENGINE_PATH = '/engine/stockfish-18-lite-single.js';
    GRADING_TARGET_DEPTH = 14 (SC4 tuning knob — conservative start per RESEARCH Open Question 1);
    GRADING_MOVETIME_SAFETY_CAP_MS = 2500 (wall-clock safety valve); GRADE_CACHE_MAX = 256 (mirrors MAIA_CACHE_MAX); RAPID_STEP_DEBOUNCE_MS = 150.

    Behavior:
    - Worker lifecycle + UCI init copied from useStockfishEngine, EXCEPT MultiPV is set dynamically to the candidate count before each search (send `setoption name MultiPV value <candidateSans.length>` as part of preparing a go, since N varies per position), and the go line is `go depth ${GRADING_TARGET_DEPTH} searchmoves ${candidateUcis.join(' ')} movetime ${GRADING_MOVETIME_SAFETY_CAP_MS}` where candidateUcis = candidateSans mapped through sanToUci(fen, san) (drop any that return null).
    - Per-FEN grade cache: cacheRef: Map<fen, Map<san, MoveGrade>>. On each info line, parseInfoLine → take parsed.pv[0], convert to SAN via the bestSanFromPv-style replay against the current grading FEN, and set cache[fen].set(san, { evalCp: toWhitePov(parsed.scoreCp), evalMate: toWhitePov(parsed.scoreMate), depth: parsed.depth }). KEY BY THE SAN (from pv[0]) — never by parsed.multipv (Pitfall 1). Normalize to white-POV using the analyzed FEN's side to move (mirror analyzedSideToMoveRef). Expose the current FEN's grade map as `gradeMap` state (updated on each commit so the chart refines live — D-05).
    - Cache-aware search gating (Pitfall 2): when (fen, candidateSans) settles, compute the set of candidateSans NOT yet present in cache[fen]. If that ungraded set is empty, do NOT send a new go (pure ELO-drag / back-nav case → serve cache). If non-empty, issue ONE go with searchmoves = the UNION of already-graded ∪ ungraded SANs for a single coherent MultiPV ranking. Debounce the (fen, candidateSans)-change trigger with RAPID_STEP_DEBOUNCE_MS so a held ELO-slider drag coalesces to one search.
    - Stale-guard / stop-before-go: copy useStockfishEngine's idle/thinking/stopping + stopPendingRef + "defer the new go until the stale bestmove arrives" discipline verbatim (Pitfall 5 / FLAWCHESS-7V). A new FEN clears the displayed gradeMap immediately (D-05 cancel) and re-targets the search.
    - Tab-hide pause + unmount stop+terminate: copy verbatim.
    - Do NOT import from, mutate, or reference useStockfishEngine's state — this is a separate instance (SC3).
    Keep functions shallow (≤3 nesting, extract handleInfoLine / prepareSearch helpers as needed).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Hook exports the { gradeMap, isGrading, isReady } shape and takes { fen, candidateSans, enabled }.
    - Grade map keyed by pv[0]-derived SAN, white-POV normalized, updated per info line.
    - Single searchmoves+MultiPV go per position; cache short-circuits ELO-drag re-search.
    - stop-before-go serialization present; no reference to useStockfishEngine internals.
    - All tuning knobs are named constants.
    - tsc + lint clean.
  </acceptance_criteria>
  <done>The grading hook compiles, lints, and structurally mirrors useStockfishEngine with a searchmoves+MultiPV config and a per-FEN pv[0]-keyed cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MockWorker unit tests for the grading hook</name>
  <files>frontend/src/hooks/__tests__/useStockfishGradingEngine.test.ts</files>
  <read_first>
    - frontend/src/hooks/__tests__/useStockfishEngine.test.ts (COPY the MockWorker class, driveInit helper, vi.useFakeTimers({ now: 0 }) setup, vi.stubGlobal('Worker', …) pattern, and the message-assertion style)
    - frontend/src/hooks/useStockfishGradingEngine.ts (the hook under test, from Task 1)
  </read_first>
  <behavior>
    Using the MockWorker pattern (no real WASM), assert:
    - Init: on mount sends `uci`; on `uciok` the init completes; isReady flips false→true on `readyok`.
    - Search command: for a FEN + 3 candidate SANs, after settle the worker receives `setoption name MultiPV value 3` and a `go depth 14 searchmoves <3 ucis> movetime 2500` line (assert the searchmoves ucis match sanToUci of the candidates, and depth/movetime match the named constants).
    - pv[0] keying: simulate two `info` lines for the SAME multipv index but DIFFERENT pv[0] moves at increasing depth; assert the grade map keys track the MOVE (pv[0]-derived SAN), not the multipv index — i.e. both moves appear as distinct keys, colors do not collapse onto one index.
    - White-POV normalization: for a black-to-move FEN, a `score cp 50` info line yields a NEGATIVE white-POV evalCp in the grade map.
    - Cache short-circuit: after grading a FEN's candidates, re-render with a SUBSET of the same candidates (same FEN) and assert NO new `go` is sent (grade served from cache).
    - Stale-guard: while thinking, a new FEN sends `stop` and sets the stopping state; the deferred re-go fires only after the stale `bestmove` arrives (mirror useStockfishEngine.test.ts's stop-pending discard test).
  </behavior>
  <action>
    Create useStockfishGradingEngine.test.ts mirroring useStockfishEngine.test.ts's MockWorker harness. Implement each assertion in the behavior block. Use fake timers for the debounce, exactly as the sibling test does. Do not test real WASM behavior here (that is Plan 01's spike + Plan 04's SC3 HUMAN-UAT).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useStockfishGradingEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior-block assertions pass, including the pv[0]-not-multipv keying test and the cache short-circuit test.
  </acceptance_criteria>
  <done>The grading hook's wiring (UCI commands, pv[0] keying, white-POV sign, cache gating, stop/go serialization) is proven under MockWorker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → second WASM worker | Same vendored, pinned engine asset as the primary worker; a second caller instance, no new asset, no network, no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151.1-03 | Denial of Service | triple-worker load (primary SF + grading SF + Maia ONNX) on mobile Safari | low | mitigate | Named degradation knobs (GRADING_TARGET_DEPTH, GRADING_MOVETIME_SAFETY_CAP_MS, candidate cap) built in from day one; measured + tuned in Plan 04 SC4. It is the user's own tab (single-tenant), not a shared resource. |
| T-151.1-04 | Denial of Service | stop/go race trapping the WASM engine (FLAWCHESS-7V) | low | mitigate | idle/thinking/stopping serialization copied verbatim from the already-hardened primary engine. |

No new packages (T-151.1-SC N/A). No high-severity threats (RESEARCH §Security Domain: no new trust boundary crossed).
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/hooks/__tests__/useStockfishGradingEngine.test.ts` green.
- `cd frontend && npm run lint && npx tsc -b` clean.
- Existing `useStockfishEngine.test.ts` suite stays green, unmodified (SC3 isolation — the primary engine is untouched).
</verification>

<success_criteria>
- SC3 grading mechanism (single searchmoves+MultiPV root search, pv[0]-keyed) implemented + unit-proven.
- SC4 degradation knobs present as named constants.
- Primary engine + its tests untouched.
</success_criteria>

<output>
Create `.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-03-SUMMARY.md` when done.
</output>
