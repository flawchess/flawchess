---
phase: 151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart
plan: 04
type: execute
wave: 3
depends_on: [151.1-02, 151.1-03]
files_modified:
  - frontend/src/components/analysis/MovesByRatingChart.tsx
  - frontend/src/components/analysis/__tests__/MovesByRatingChart.test.tsx
  - frontend/src/components/analysis/MaiaHumanPanel.tsx
  - frontend/src/pages/Analysis.tsx
  - frontend/src/lib/theme.ts
autonomous: false
requirements: [SC1, SC2, SC3, SC4]
must_haves:
  truths:
    - "Each shown chart line + its end-of-line SAN label is colored by its MoveQuality bucket via a colorForQuality() mapping to the MOVE_QUALITY_* tokens; ungraded moves render in MOVE_QUALITY_PENDING neutral until the first grade arrives (SC1, D-03, D-05)."
    - "Color encodes quality ONLY; stroke width encodes played/best emphasis ONLY (decoupled) — played and SF-best keep EMPHASIZED_STROKE_WIDTH regardless of their quality color (SC1, D-01, D-07)."
    - "The shown line set is selectCandidatesByMass(perElo, selectedElo, playedSan, bestSan) — computed ONCE upstream in Analysis and passed to both the grading hook (as candidateSans) and the chart (as shownSans); capMovesByPeak's top-6-by-peak rule is removed (SC2, D-02/D-06/D-07)."
    - "The hover tooltip adds, per move, its quality word (best/good/inaccuracy/mistake/blunder) and its white-POV eval; the chart face (colored SAN labels) is otherwise unchanged; prob% stays in the tooltip (SC1, D-08)."
    - "Grading runs alongside the existing useStockfishEngine instance without altering the eval bar / engine card / variation tree (SC3 isolation, verified at UAT)."
    - "Per-navigation grading latency is measured on desktop + mobile Safari and acceptable, or the degradation knobs are exercised (SC4)."
    - "Dead identity-palette tokens (MOVES_BY_RATING_PLAYED_LINE/BEST_LINE/OTHER_LINES) are removed once the chart no longer imports them (knip clean)."
  artifacts:
    - "MovesByRatingChart.tsx — new props shownSans + qualityBySan; colorForQuality(); decoupled stroke; quality+eval tooltip; new data-testids"
    - "Analysis.tsx — useStockfishGradingEngine instance; shownSans + qualityBySan computed via selectCandidatesByMass + classifyMoveQuality"
    - "MaiaHumanPanel.tsx — shownSans + qualityBySan pass-through props"
    - "theme.ts — MOVES_BY_RATING_PLAYED_LINE/BEST_LINE/OTHER_LINES removed"
  key_links:
    - "Analysis: shownSans = selectCandidatesByMass(maia.perElo, selectedElo, playedSan, bestSan) → candidateSans into useStockfishGradingEngine → gradeMap → classifyMoveQuality(gradeMap, sideToMoveFromFen(position)) → qualityBySan → MaiaHumanPanel → MovesByRatingChart."
    - "Both the desktop left column AND the mobile Human-tab MaiaHumanPanel call sites get the identical new props (CLAUDE.md: apply to both surfaces)."
---

<objective>
Wire the foundation (Plan 02) and grading hook (Plan 03) into the live surface: recolor the
Moves-by-Rating chart lines + SAN labels by Stockfish quality (SC1), replace the top-6-by-peak
cap with the 0.95-mass candidate set (SC2), stream grades from the second worker without
disturbing the primary eval bar / engine card (SC3), add the quality+eval tooltip (D-08), and
run the real-browser SC3/SC4 verification (searchmoves correctness + desktop/mobile latency).

Purpose: Deliver the visible feature — the human-trap chart — end to end.
Output: recolored chart, wired Analysis/MaiaHumanPanel, dead-token cleanup, and signed-off
real-WASM + latency UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-CONTEXT.md
@.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-RESEARCH.md
@frontend/src/components/analysis/MovesByRatingChart.tsx
@frontend/src/components/analysis/__tests__/MovesByRatingChart.test.tsx
@frontend/src/components/analysis/MaiaHumanPanel.tsx
@frontend/src/lib/moveQuality.ts
@frontend/src/lib/theme.ts
@frontend/src/hooks/useStockfishGradingEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor MovesByRatingChart by quality + quality/eval tooltip + shownSans/qualityBySan props</name>
  <files>frontend/src/components/analysis/MovesByRatingChart.tsx, frontend/src/components/analysis/__tests__/MovesByRatingChart.test.tsx</files>
  <read_first>
    - frontend/src/components/analysis/MovesByRatingChart.tsx (current: capMovesByPeak selection, colorForSan identity-coloring, colorForSan+stroke entangled in the render map, endOfLineLabel, movesTooltipContent, pivotRows, computeYAxis, EMPHASIZED_STROKE_WIDTH/OTHER_STROKE_WIDTH)
    - frontend/src/lib/moveQuality.ts (MoveQuality, MoveQualityInfo, MoveGrade)
    - frontend/src/lib/theme.ts (MOVE_QUALITY_BEST/GOOD/INACCURACY/MISTAKE/BLUNDER/PENDING)
    - frontend/src/components/analysis/__tests__/MovesByRatingChart.test.tsx (existing capMovesByPeak-based tests + the recharts ResponsiveContainer stub pattern)
  </read_first>
  <action>
    Change MovesByRatingChartProps: REMOVE the internal capMovesByPeak call; ADD `shownSans: string[]`
    (the already-selected candidate set, computed upstream) and `qualityBySan: Map<string, { quality: MoveQuality; evalCp: number | null; evalMate: number | null }>`
    (per-SAN quality + white-POV eval for coloring + tooltip). Keep perElo, playedSan, bestSan, selectedElo.
    - Delete capMovesByPeak, collectCandidateSans, peakProbability (now unused — selection moved to selectCandidatesByMass in Plan 02). Render exactly `shownSans` (pivotRows over shownSans).
    - Add `colorForQuality(quality: MoveQuality | undefined): string` mapping best→MOVE_QUALITY_BEST, good→MOVE_QUALITY_GOOD, inaccuracy→MOVE_QUALITY_INACCURACY, mistake→MOVE_QUALITY_MISTAKE, blunder→MOVE_QUALITY_BLUNDER, and undefined (not-yet-graded)→MOVE_QUALITY_PENDING (D-05 neutral). Replace colorForSan entirely with a lookup of qualityBySan.get(san)?.quality → colorForQuality.
    - DECOUPLE stroke from color: strokeWidth = (san === playedSan || san === bestSan) ? EMPHASIZED_STROKE_WIDTH : OTHER_STROKE_WIDTH; stroke color = colorForQuality(...) independent of emphasis (D-01/D-07). The end-of-line SAN label uses the SAME quality color (identity carried by the label text, color by quality).
    - Tooltip (D-08): extend movesTooltipContent to show, per row, `${san}: ${qualityWord} · ${evalText} · ${prob}%` where qualityWord is the MoveQuality bucket from qualityBySan and evalText is the white-POV eval formatted (cp as +1.2 / mate as #N; "—" when ungraded). Keep the ELO header + prob%. Keep the played/best role suffix. Add data-testid="moves-by-rating-tooltip-row-{san}" per row. The tooltip may use text-xs (hover-tooltip exception, D-08). Keep the chart face (labels) as colored SAN only — no extra on-face text.
    - Update MovesByRatingChart.test.tsx: drop the capMovesByPeak import/tests; test the chart with explicit shownSans + qualityBySan props — assert (a) exactly shownSans lines render, (b) a blunder-graded move's line uses MOVE_QUALITY_BLUNDER stroke while a played blunder still gets EMPHASIZED_STROKE_WIDTH (color/stroke decoupled), (c) an ungraded shown move renders MOVE_QUALITY_PENDING, (d) the tooltip row includes the quality word + eval. Keep the recharts stub + you-are-here reference-line coverage.
    - data-testid discipline: keep existing chart testids; interactive/label elements keep/gain kebab-case testids.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/MovesByRatingChart.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Chart renders exactly `shownSans`; color from quality; stroke from played/best (decoupled).
    - Ungraded moves render MOVE_QUALITY_PENDING.
    - Tooltip shows quality word + white-POV eval + prob% per move.
    - capMovesByPeak/collectCandidateSans/peakProbability removed; tests updated + green; tsc clean.
  </acceptance_criteria>
  <done>The chart colors by quality, emphasizes played/best by stroke, and its tooltip carries the D-08 quality word + eval — all under the new shownSans/qualityBySan props.</done>
</task>

<task type="auto">
  <name>Task 2: Wire grading into Analysis + MaiaHumanPanel; remove dead identity-palette tokens</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/components/analysis/MaiaHumanPanel.tsx, frontend/src/lib/theme.ts</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (existing useStockfishEngine + useMaiaEngine instances; playedSan (line ~504), bestSan useMemo (line ~508), selectedElo, position, sideToMoveFromFen usage; the TWO MaiaHumanPanel call sites at lines ~1065 desktop and ~1213 mobile; engineEnabled gate)
    - frontend/src/components/analysis/MaiaHumanPanel.tsx (props: selectedElo/onEloChange/perElo/playedSan/bestSan; passes them to MovesByRatingChart at both desktop + mobile Human-tab call sites in Analysis)
    - frontend/src/lib/moveQuality.ts (selectCandidatesByMass, classifyMoveQuality, MoveQualityInfo)
    - frontend/src/hooks/useStockfishGradingEngine.ts (interface { fen, candidateSans, enabled } → { gradeMap, isGrading, isReady })
    - frontend/src/lib/liveFlaw.ts (sideToMoveFromFen)
    - frontend/src/lib/theme.ts lines ~354-371 (the MOVES_BY_RATING_PLAYED_LINE/BEST_LINE/OTHER_LINES to remove; keep MOVES_BY_RATING_REFERENCE_LINE — still used by the you-are-here line)
  </read_first>
  <action>
    In Analysis.tsx:
    - Compute `shownSans = useMemo(() => selectCandidatesByMass(maia.perElo, selectedElo, playedSan, bestSan), [maia.perElo, selectedElo, playedSan, bestSan])`.
    - Instantiate the grading hook: `const grading = useStockfishGradingEngine({ fen: engineEnabled ? position : null, candidateSans: shownSans, enabled: engineEnabled })` (gate on the SAME engineEnabled toggle so grading is off when the primary engine is off; it must NOT alter the existing `engine` instance).
    - Compute `qualityBySan = useMemo(() => { const info = classifyMoveQuality(grading.gradeMap, sideToMoveFromFen(position)); merge each entry with the raw grade's evalCp/evalMate from grading.gradeMap into Map<string,{quality,evalCp,evalMate}> }, [grading.gradeMap, position])`.
    - Pass `shownSans` + `qualityBySan` (plus existing selectedElo/perElo/playedSan/bestSan) to BOTH MaiaHumanPanel call sites (desktop ~1065 AND mobile ~1213 — CLAUDE.md: apply to both surfaces).
    In MaiaHumanPanel.tsx: add `shownSans: string[]` and `qualityBySan` props, thread them into MovesByRatingChart (which no longer computes its own cap).
    In theme.ts: remove the now-unused MOVES_BY_RATING_PLAYED_LINE, MOVES_BY_RATING_BEST_LINE, MOVES_BY_RATING_OTHER_LINES exports (knip fails on dead exports). Keep MOVES_BY_RATING_REFERENCE_LINE. Update the block comment to describe the D-03 quality-coloring model.
    Keep the primary `engine` (useStockfishEngine) instance and all its consumers (EngineLines, VariationTree, EvalBar, bestSanFromPv) byte-for-byte unchanged (SC3).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npm run knip && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Grading hook instantiated and gated on engineEnabled; primary engine untouched.
    - shownSans + qualityBySan flow to BOTH MaiaHumanPanel call sites → chart.
    - Dead identity-palette tokens removed; knip clean; tsc + lint + full frontend suite green.
  </acceptance_criteria>
  <done>The live /analysis chart draws the 0.95-mass candidate set colored by streamed Stockfish quality on both desktop and mobile, with the primary eval bar/engine card unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-WASM SC3 correctness + SC4 desktop/mobile latency sign-off</name>
  <what-built>The full graded Moves-by-Rating chart: the second grading worker streams searchmoves+MultiPV grades that recolor the 0.95-mass candidate lines (dark-green best → red blunder), stroke-emphasize played/best, and populate the D-08 quality+eval tooltip — running alongside the untouched primary eval bar / engine card.</what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev`; open /analysis with a real game (game mode) in desktop Chrome.
    2. SC3 real-WASM correctness: navigate to a middlegame position. Confirm each shown line gets a DISTINCT, plausible quality color that REFINES as depth climbs (neutral → colored → settling), and that the move you actually played is graded (e.g. a popular-but-bad move shows red/orange — the "human trap"). Confirm the SF-best line is dark-green (note: per RESEARCH Pitfall 3 the primary-engine's thick "best" stroke may occasionally grade "good" not "best" — a known non-bug; flag only if colors are wildly wrong).
    3. SC3 isolation: while grading streams, confirm the eval bar, engine card lines, and variation tree behave EXACTLY as before (no flicker, no changed values, no stalls).
    4. SC1/D-01/D-07: confirm color=quality, SAN labels carry identity, played + best stay thicker regardless of color; hover a line → tooltip shows quality word + eval + prob%.
    5. SC2: drag the ELO slider — confirm the shown line SET adapts (sharp positions few lines, quiet more) and dragging does NOT visibly re-run grading (cached); back/forward nav cancels in-flight grading cleanly.
    6. Mobile: repeat 2-5 on the mobile Human tab (game + free-play) on a real mobile Safari device.
    7. SC4 latency: with devtools Performance (or console `performance.now()` timers around navigation → first-grade → settle), record per-navigation grading latency on desktop AND mobile Safari. Judge acceptable (target: first shallow grade within ~1-2s of Maia ready). If unacceptable, exercise the degradation knobs (lower GRADING_TARGET_DEPTH, tighten GRADING_MOVETIME_SAFETY_CAP_MS, or reduce the candidate cap) and re-measure; record the final values.
    8. Also confirm the throwaway probe from Plan 01 (frontend/public/engine/searchmoves-probe.html) is deleted (if not already).
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded desktop + mobile latency numbers and any degradation-knob changes, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser tab (client-only) | All computation is client-side, ephemeral, board-session scoped; zero backend/DB/network/persistence added (RESEARCH §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151.1-05 | Denial of Service | grading latency on mobile Safari (triple-worker) | low | mitigate | SC4 measure-and-degrade in this plan's checkpoint (named knobs from Plan 03). User's own single-tenant tab. |
| T-151.1-06 | Tampering | MoveQuality vs FlawSeverity contract | low | accept | Plan 02 introduced MoveQuality as a separate frontend-only type; FlawSeverity's cross-stack contract is untouched — verified at integration (tsc). |

No new packages (T-151.1-SC N/A). No high-severity threats.
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npm run lint && npm run knip && npm test -- --run` all green.
- Existing useStockfishEngine.test.ts + engine-card behavior unchanged (SC3 isolation).
- Desktop + mobile Safari SC3 correctness + SC4 latency signed off at the checkpoint.
</verification>

<success_criteria>
- SC1 quality coloring live on both surfaces; SC2 0.95-mass line set replacing the peak cap; SC3 grading confirmed on real WASM without disturbing the primary engine; SC4 latency measured + acceptable (or degraded).
- Dead identity-palette tokens removed; full frontend gate green.
</success_criteria>

<output>
Create `.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-04-SUMMARY.md` when done, recording the desktop + mobile latency numbers and any degradation-knob final values.
</output>
