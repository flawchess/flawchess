---
phase: 151-maia-in-the-browser-all-position-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
  - frontend/src/components/analysis/MaiaAttribution.tsx
  - frontend/src/components/analysis/__tests__/MaiaAttribution.test.tsx
requirements: [LIC-01, LIC-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The repository LICENSE is AGPL-3.0 (not MIT)"
    - "README license references (badge, prose, License section) state AGPL-3.0"
    - "A visible MaiaAttribution component cites the CSSLab source repo, the AGPL license, the model artifact path, and the Chessformer paper (arXiv:2605.19091)"
  artifacts:
    - "LICENSE (full AGPL-3.0 text)"
    - "frontend/src/components/analysis/MaiaAttribution.tsx"
  key_links:
    - "MaiaAttribution links to github.com/CSSLab/maia3 and the AGPL-3.0 license text; Plan 06 mounts it on the analysis surface"
  prohibitions:
    - "No CSSLab source code is copied into the repo — the attribution references it by link only"
---

<objective>
Satisfy the license half of the phase: relicense the repo MIT -> AGPL-3.0 (LIC-01) and build a
visible Maia attribution + offer-source notice (LIC-02) citing the CSSLab source repo, the AGPL
license, the unmodified model artifact, and the Chessformer paper. The notice ships as a
standalone `MaiaAttribution` component so it is independent of the analysis-layout rework (Plan 06
mounts it). Fully independent of the ONNX contract — runs in parallel in Wave 1.

Purpose: AGPL compliance for shipping the Maia-3 model unmodified in the browser bundle.
Output: AGPL LICENSE + README, and a reusable visible attribution component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-CONTEXT.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md
@.planning/spikes/005-agpl-client-bundle-gate/README.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relicense repo MIT -> AGPL-3.0 (LICENSE + README)</name>
  <files>LICENSE, README.md</files>
  <read_first>
    - LICENSE (current MIT text, Copyright (c) 2026 Adrian Imfeld)
    - README.md lines ~19 (license badge), ~41 ("MIT licensed"), ~55 (Engine Binaries GPLv3 note — mirror its structure), ~152-154 (License section)
    - .planning/spikes/005-agpl-client-bundle-gate/README.md (AGPL 4-condition posture; the milestone chose full relicense which removes the ambiguity)
  </read_first>
  <action>
    Replace `LICENSE` contents with the full canonical GNU Affero General Public License v3.0 text,
    preserving a copyright line for Adrian Imfeld (2026). In `README.md`: change the license shield
    from `license-MIT-blue` to `license-AGPL--3.0-blue` (or equivalent AGPL badge), update the
    "Open source ... MIT licensed" bullet to AGPL-3.0, and replace the bottom "## License" section
    body ("MIT — see LICENSE") with an AGPL-3.0 statement. Leave the existing "Engine Binaries
    (GPLv3 License Note)" Stockfish section intact (Stockfish is separately GPL-3.0 in its Worker).
    Do NOT touch package.json/pyproject license fields unless they currently declare a license
    (they do not per RESEARCH — confirm with a grep before editing).
  </action>
  <verify>
    <automated>head -3 LICENSE | grep -qi 'AFFERO' && grep -qi 'AGPL' README.md && ! grep -q 'license-MIT' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` begins with the GNU Affero General Public License header and contains the full AGPL-3.0 body.
    - README license badge, the open-source bullet, and the License section all say AGPL-3.0; no residual `license-MIT` badge remains.
    - The Stockfish GPLv3 engine-binaries note is unchanged.
  </acceptance_criteria>
  <done>Repo is AGPL-3.0 in LICENSE and README (LIC-01).</done>
</task>

<task type="auto">
  <name>Task 2: MaiaAttribution component — visible offer-source + citation notice</name>
  <files>frontend/src/components/analysis/MaiaAttribution.tsx, frontend/src/components/analysis/__tests__/MaiaAttribution.test.tsx</files>
  <read_first>
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md (README Attribution Section example under Code Examples — mirror its content for the in-app notice)
    - frontend/src/lib/theme.ts (colors — no hard-coded semantic colors)
    - frontend/src/components/analysis/EvalBar.tsx (an existing small analysis component for style/testid conventions)
    - frontend/src/components/analysis/__tests__/EvalBar.test.tsx (test conventions: render + query)
  </read_first>
  <action>
    Create `frontend/src/components/analysis/MaiaAttribution.tsx` exporting a `MaiaAttribution`
    component: a compact, visible notice (a Radix info popover trigger or an inline block — planner's
    choice; keep it unobtrusive but readable, text-sm floor except a hover-tooltip body may use
    text-xs per CLAUDE.md) that states the app uses the unmodified Maia-3 model
    (`frontend/public/maia/maia3_simplified.onnx`) from CSSLab under AGPL-3.0, that FlawChess is
    AGPL-3.0 with source available, and cites: an anchor to github.com/CSSLab/maia3 (source /
    offer-source), an anchor to the AGPL-3.0 license text (gnu.org/licenses/agpl-3.0.html), and the
    Chessformer paper (Monroe et al., arXiv:2605.19091). Give the container
    `data-testid="maia-attribution"` and every anchor a `data-testid` (`maia-attr-link-source`,
    `maia-attr-link-license`, `maia-attr-link-paper`) with real `href`s and `aria-label`s on
    icon-only triggers. Use `theme.ts` for any accent color. Add
    `frontend/src/components/analysis/__tests__/MaiaAttribution.test.tsx` asserting the three links
    render with the expected hrefs and the paper citation text is present. Do NOT mount it anywhere
    yet — Plan 06 wires it into the analysis layout.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/MaiaAttribution.test.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `MaiaAttribution.test.tsx` passes: the three anchors render with hrefs to CSSLab/maia3, the AGPL-3.0 text, and arXiv:2605.19091.
    - Container carries `data-testid="maia-attribution"`; icon-only triggers (if any) have `aria-label`.
    - `npm run knip` finds no dead export (the component is exported and imported by its test; Plan 06 adds the runtime import).
    - No hard-coded semantic color literals; `text-*` >= text-sm outside a hover-tooltip body.
  </acceptance_criteria>
  <done>A reusable, tested, visible Maia attribution notice exists (LIC-02 component half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public | License terms under which the shipped Maia model + app source are distributed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-03 | Repudiation | AGPL compliance for shipped model | medium | mitigate | Full AGPL-3.0 LICENSE + visible offer-source notice citing CSSLab source + license + paper |
| T-151-04 | Information disclosure | External anchor targets | low | accept | Attribution links are public canonical URLs (CSSLab repo, gnu.org, arXiv); no user data |
</threat_model>

<verification>
- `LICENSE` is AGPL-3.0; README references updated; Stockfish note intact.
- `MaiaAttribution` renders three correct citation links; component + knip + lint green.
</verification>

<success_criteria>
Repo is AGPL-3.0 and a reusable visible Maia attribution/offer-source notice exists, ready for Plan 06 to mount (LIC-01/02).
</success_criteria>

## Artifacts this phase produces (Plan 02)
- `LICENSE` — replaced with AGPL-3.0 text
- README.md license references updated to AGPL-3.0
- `frontend/src/components/analysis/MaiaAttribution.tsx` — visible attribution/offer-source component (mounted by Plan 06)
- `frontend/src/components/analysis/__tests__/MaiaAttribution.test.tsx`

<output>
Create `.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-02-SUMMARY.md` when done.
</output>
