---
phase: 151-maia-in-the-browser-all-position-surfaces
plan: 06
type: execute
wave: 4
depends_on: ["151-02", "151-03", "151-04", "151-05"]
files_modified:
  - frontend/src/hooks/useMaiaEloDefault.ts
  - frontend/src/hooks/__tests__/useMaiaEloDefault.test.ts
  - frontend/src/pages/Analysis.tsx
requirements: [SURF-04, SURF-05, MAIA-04, MAIA-05, MAIA-06, LIC-02, VALID-01]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Desktop is a 3-column layout: left = Moves-by-Rating chart, center = Maia bar + board + Stockfish bar, right = existing engine/variation-tree/controls panel"
    - "The Maia expected-score bar renders on the LEFT of the board and the Stockfish bar on the RIGHT, both live for every position (game mode and free play)"
    - "Mobile gains a 4th 'Human' tab (order Moves | Eval | Human | Tags) holding the chart; free play gets a minimal Moves | Human tab pair; both eval bars stay above the tab strip"
    - "The ELO selector drives the you-are-here line; game-mode default = user's color rating-at-game-time, free-play default = current_rating else 1500"
    - "The chart + Maia bar recompute live on every board navigation with no server round-trip; useMaiaEngine is lazy (only when the analysis board is open)"
    - "A visible MaiaAttribution notice is mounted on the analysis surface"
  artifacts:
    - "frontend/src/hooks/useMaiaEloDefault.ts (ELO default-derivation hook — keeps the logic out of Analysis.tsx's render)"
    - "frontend/src/pages/Analysis.tsx integrated 3-column + mobile-tab layout wiring useMaiaEngine, MaiaEvalBar, MovesByRatingChart, EloSelector, MaiaAttribution"
    - ".planning/phases/151-.../151-MAIA-MEASUREMENTS.md (MAIA-06 size + latency measurements)"
  key_links:
    - "useMaiaEngine(fen=position, enabled=analysis-open, selectedElo) feeds MovesByRatingChart (perElo) + Maia bar (expectedScoreAtSelectedElo)"
    - "selectedElo default: game mode from gameData.white_rating/black_rating by user color; free play from useUserProfile().data.current_rating ?? 1500"
  prohibitions:
    - "No server round-trip for Maia; no persistence of Maia signals; no game_flaws / DB / schema change"
    - "Do NOT introduce COOP/COEP headers"
    - "Do NOT add a Phase 152 flaw verdict/banner — this phase is the always-on chart+bar only"
---

<objective>
Wire everything into the `/analysis` page and prove it live. Rework the desktop layout into the
D-01 3-column arrangement (left = Moves-by-Rating chart | center = Maia bar + board + Stockfish bar
| right = existing engine/tree/controls panel), add the D-03 mobile "Human" tab (and a minimal
Moves | Human tab pair for free play — planner decision, kept consistent with the tabbed surface),
mount `useMaiaEngine` lazily, render the Maia expected-score bar on the LEFT (EvalBar
whiteFraction) and keep the Stockfish bar on the RIGHT, feed `MovesByRatingChart` + `EloSelector`,
default the ELO per D-07 (game-mode rating-at-game-time, free-play current_rating else 1500), and
mount `MaiaAttribution` (LIC-02). The D-07 ELO default-derivation is extracted into a small
`useMaiaEloDefault` hook (Task 1) rather than inlined into the already-large `Analysis.tsx` render,
and the layout rework follows CLAUDE.md's "refactor bloated code on sight" (extract Maia surfaces
into a subcomponent if it would breach the nesting/LOC soft limits). Then run the VALID-01 live
calibration eyeball + MAIA-06 size/latency measurement gate.

Purpose: The user-visible payoff and the calibration gate that proves Maia is trustworthy enough
to build Phase 152 on. Output: a small ELO-default hook + integrated Analysis.tsx + a measurements
doc + human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-CONTEXT.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-04-SUMMARY.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-05-SUMMARY.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md
@frontend/src/pages/Analysis.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useMaiaEloDefault hook — encapsulate the ELO default-derivation logic (D-06/D-07)</name>
  <files>frontend/src/hooks/useMaiaEloDefault.ts, frontend/src/hooks/__tests__/useMaiaEloDefault.test.ts</files>
  <read_first>
    - frontend/src/hooks/useUserProfile.ts + frontend/src/types/users.ts (current_rating from Plan 03)
    - frontend/src/pages/Analysis.tsx (gameData shape from useLibraryGame: user_color, white_rating, black_rating; isGameMode gate)
    - frontend/src/lib/maiaEncoding.ts (MAIA_ELO_LADDER — the confirmed range the resolved default is snapped/clamped into)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-CONTEXT.md (D-07 default rules; user-override precedence)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md (Pitfall 2 — free-play rating source)
  </read_first>
  <behavior>
    - Game mode, white user with white_rating=1720 -> resolved default 1720 (clamped to the ladder).
    - Game mode, black user -> resolves from black_rating.
    - Free play, profile.current_rating=1850 -> resolved default 1850.
    - Free play, current_rating null -> resolved default FREE_PLAY_DEFAULT_ELO (1500).
    - Once the user picks a value via EloSelector, the hook's `selectedElo` returns the user's pick and no longer re-derives from data (user-override precedence); a later data load does NOT clobber it.
    - Re-derivation happens when gameData/profile FIRST load (default was still the untouched initial), not on every render.
  </behavior>
  <action>
    Create `frontend/src/hooks/useMaiaEloDefault.ts` exporting `useMaiaEloDefault({ isGameMode,
    gameData, profile }): { selectedElo: number; setSelectedElo: (elo: number) => void }` and a named
    `FREE_PLAY_DEFAULT_ELO = 1500` constant. Internally: hold `selectedElo` state plus a
    `userOverrodeRef` flag; compute the derived default (game mode = user's color rating-at-game-time
    `gameData.user_color === 'white' ? gameData.white_rating : gameData.black_rating`; free play =
    `profile?.current_rating ?? FREE_PLAY_DEFAULT_ELO`), clamped/snapped into `MAIA_ELO_LADDER`; in a
    `useEffect` keyed on the derived default, set `selectedElo` to it ONLY while `userOverrodeRef` is
    false (so a late gameData/profile load still seeds the default, but a user pick wins permanently).
    `setSelectedElo` flips `userOverrodeRef` true. Keep the hook small (no nesting past depth 2). Write
    `useMaiaEloDefault.test.ts` covering all six <behavior> bullets (renderHook + rerender with data
    arriving after mount, and a user-pick-then-data-load ordering case). This keeps the derivation OUT
    of Analysis.tsx's already-large render (CLAUDE.md "refactor bloated code on sight").
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useMaiaEloDefault.test.ts && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - All six <behavior> cases pass, including the user-override-then-late-data-load ordering (data does not clobber a user pick).
    - Default derives from rating-at-game-time (game mode) and current_rating ?? FREE_PLAY_DEFAULT_ELO (free play), clamped to MAIA_ELO_LADDER.
    - The hook body stays shallow (no branch nesting past depth 2); `npx tsc -b` clean; no `any`.
  </acceptance_criteria>
  <done>ELO default derivation lives in a small tested hook, not inlined in Analysis.tsx (D-06/D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Mount useMaiaEngine + Maia bar + chart + ELO selector; rework desktop to 3 columns (D-01, SURF-04/05, MAIA-04/05)</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (boardRow ~L845-884 with the existing right-side EvalBar; desktop layout ~L1082-1195 board column + lg:w-[360px] side panel; position/currentNodeId/parentFen; useLibraryGame gameData.white_rating/black_rating; useUserProfile; isMobile; LIVE_EVAL_CACHE_MAX)
    - frontend/src/hooks/useMaiaEloDefault.ts (Task 1 — selectedElo/setSelectedElo + FREE_PLAY_DEFAULT_ELO)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-04-SUMMARY.md (useMaiaEngine signature + return shape)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-05-SUMMARY.md (MovesByRatingChart, EloSelector, EvalBar whiteFraction/testId props)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-CONTEXT.md (D-01/D-05/D-06/D-07)
    - CLAUDE.md "Keep functions small and shallow" + "Refactor bloated code on sight" (nesting soft-3/hard-4, logic-LOC soft-100/hard-200)
  </read_first>
  <action>
    In `Analysis.tsx`: call `useMaiaEngine({ fen: position, enabled: <analysis-board-open>, selectedElo })`
    (enabled mirrors how the Stockfish engine is gated so the model lazy-loads only on this page —
    MAIA-02 boundary is the route-level React.lazy already covering Analysis; keep the worker created
    only while mounted). Obtain `{ selectedElo, setSelectedElo }` from `useMaiaEloDefault({ isGameMode,
    gameData, profile: useUserProfile().data })` (Task 1) — do NOT inline the D-07 derivation here.
    In `boardRow`, add a SECOND EvalBar on the LEFT of the board driven by
    `whiteFraction={maia.expectedScoreAtSelectedElo}` `flipped={boardFlipped}`
    `testId="analysis-maia-eval-bar"`; keep the existing Stockfish EvalBar on the RIGHT (SURF-04).
    Restructure the desktop return (the non-mobile branch) from 2 columns into 3: NEW left column
    (~340-380px) containing `<EloSelector value={selectedElo} onChange={setSelectedElo}>` +
    `<MovesByRatingChart perElo={maia.perElo} playedSan={...} bestSan={...} selectedElo={selectedElo}>`;
    center column = the board column (player bars + boardRow now with both bars + existing eval
    chart/tags); right column = the current engine/variation-tree/controls panel unchanged. Derive
    `playedSan` (game mode: the move into currentNode; free play: last played move) and `bestSan`
    (engine top line's first move) from existing state. The chart/bar recompute automatically because
    `position` drives useMaiaEngine (SURF-05). Cache is the hook's ephemeral Map (MAIA-05) — do not
    persist. Give the new left column `data-testid="analysis-human-column"`. NESTING/LOC DISCIPLINE:
    Analysis.tsx is already ~1195 lines. If adding the third column pushes any function past
    CLAUDE.md's soft limits (nesting depth >3, logic-LOC >100), EXTRACT the Maia left-column surfaces
    into a dedicated subcomponent (e.g. `MaiaSurfacesPanel` / `AnalysisHumanColumn`) rather than
    growing the page render — this is expected per "refactor bloated code on sight", not optional
    scope creep. Prefer the existing helper-const pattern (like `boardRow`/`variationTree`) or a new
    component file over deepening the JSX tree.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Desktop renders three columns; the left column carries `data-testid="analysis-human-column"` with the ELO selector + chart; center has both eval bars flanking the board (`analysis-maia-eval-bar` left, `analysis-eval-bar` right).
    - `selectedElo` comes from `useMaiaEloDefault` (no D-07 derivation inlined in the render); EloSelector changes route through `setSelectedElo`.
    - No function in Analysis.tsx exceeds CLAUDE.md soft limits after the change; if it would, the Maia surfaces are extracted into a subcomponent (verify by inspection that the desktop human column is a helper-const or its own component, not deep inline JSX).
    - `npx tsc -b`, `npm run lint`, `npm run build` all pass; no COOP/COEP added.
  </acceptance_criteria>
  <done>Desktop 3-column layout live: Maia chart+selector left, both bars flanking the board, engine panel right, all recomputing per navigation, without bloating Analysis.tsx past CLAUDE.md limits (D-01, SURF-04/05, MAIA-04/05).</done>
</task>

<task type="auto">
  <name>Task 3: Mobile 4th "Human" tab + free-play tab pair + mount MaiaAttribution (D-03, LIC-02)</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (mobile branch ~L996-1080: engine lines, boardRow, the Tabs block with Moves|Eval|Tags, and the free-play else-branch that shows only the move list)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-CONTEXT.md (D-03 order Moves|Eval|Human|Tags; free-play open detail)
    - frontend/src/components/analysis/MaiaAttribution.tsx (Plan 02 — the notice to mount)
    - frontend/src/components/analysis/MovesByRatingChart.tsx + EloSelector.tsx (Plan 05)
  </read_first>
  <action>
    In the mobile branch: keep the board + BOTH eval bars always-visible above the tab strip (boardRow
    already includes both bars from Task 2). Add a 4th `<TabsTrigger value="human"
    data-testid="analysis-tab-human">Human</TabsTrigger>` so the game-mode order is Moves | Eval |
    Human | Tags, and a `<TabsContent value="human">` holding `<EloSelector>` + `<MovesByRatingChart>`
    full-width. For FREE PLAY (the `else` branch that currently shows only the move list), introduce a
    minimal Moves | Human tab pair (consistent with the tabbed game-mode surface — planner decision per
    D-03 open detail) so the Human chart appears in free play too; both bars stay above it. Mount
    `<MaiaAttribution />` on the analysis surface (e.g. inside the Human tab content and/or the desktop
    human column) so LIC-02's visible-notice requirement is satisfied on both layouts. Apply the SAME
    change to desktop where the CONTEXT requires mobile+desktop parity (attribution visible in both).
    NESTING/LOC DISCIPLINE: the mobile branch already nests Tabs/TabsContent deeply; if the 4th tab +
    free-play tab pair push the mobile render past CLAUDE.md soft limits, reuse the shared
    helper-const pattern (like the existing `evalChart`/`tagsPanel`/`variationTree` helpers) or extract
    a `MaiaHumanTab` subcomponent rather than deepening the inline JSX.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npm run knip && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Mobile game-mode tab order is Moves | Eval | Human | Tags with `data-testid="analysis-tab-human"`; the Human tab shows the ELO selector + chart full-width.
    - Free play shows a Moves | Human tab pair (chart reachable in free play); both eval bars remain above the tab strip in all mobile modes.
    - `MaiaAttribution` is mounted and visible on both desktop and mobile analysis layouts (LIC-02).
    - The Human chart/tab markup reuses a shared helper-const or subcomponent (not duplicated deep inline JSX); no function exceeds CLAUDE.md soft limits after the change.
    - `knip` finds no dead exports (MaiaAttribution/MovesByRatingChart/EloSelector now imported at runtime); build + tsc + lint pass.
  </acceptance_criteria>
  <done>Mobile 4th Human tab + free-play tab pair, both bars persistent, visible Maia attribution on both layouts (D-03, LIC-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: VALID-01 live calibration eyeball + MAIA-06 size/latency measurement</name>
  <what-built>
    The full Maia surface is live on `/analysis`: 3-column desktop, mobile Human tab, Maia bar LEFT +
    Stockfish bar RIGHT, Moves-by-Rating chart, ELO selector, all recomputing live per navigation with
    no server round-trip and nothing persisted. This gate is the phase's quality bar (D-10:
    measure-and-judge, NOT a green-checkmark-only gate).
  </what-built>
  <action>Blocking-human quality gate: run the app, perform the live calibration eyeball + size/latency measurement per the how-to-verify steps, record 151-MAIA-MEASUREMENTS.md, and only then sign off (D-10 measure-and-judge).</action>
  <how-to-verify>
    1. Run the app; open `/analysis` in game mode on a representative game and in free play.
    2. CALIBRATION (VALID-01, answers Q-014/Q-015): step through several positions. Confirm the Maia
       expected-score bar (LEFT) moves in a plausible human direction vs the Stockfish bar (RIGHT), and
       that the chart's per-move probabilities look sane across the ELO ladder (e.g. beginner-trap moves
       peak at low ELO, principled moves rise with ELO). Cross-check WDL sign/order is correct
       (Assumption A5) — a flipped bar is the tell. Eyeball at 2-3 ELO settings via the selector.
    3. WEBGPU vs WASM: confirm the reported backend (from useMaiaEngine/worker) and eyeball that
       WebGPU and WASM produce consistent output on the same position (Pitfall 4).
    4. MEASUREMENT (MAIA-06): record in `.planning/phases/151-.../151-MAIA-MEASUREMENTS.md` — model
       download size, cold-load time, and per-position latency on desktop AND a phone, for WASM vs
       WebGPU and single-call vs ELO-sweep. Confirm `window.crossOriginIsolated === false` (no
       COOP/COEP) and no unsupported-op console errors.
    5. DECISION (D-10): if calibration is poor, note whether to upgrade to a larger model (23M/79M) —
       do NOT auto-upgrade; flag it. Otherwise sign off.
  </how-to-verify>
  <resume-signal>Type "approved" once calibration is acceptable and 151-MAIA-MEASUREMENTS.md is filled, or describe issues (calibration, latency, backend inconsistency).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board state -> Maia worker | Local FEN drives client-side inference; no network |
| page -> browser isolation | Must remain crossOriginIsolated=false (no COOP/COEP) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-13 | Elevation/Availability | COOP/COEP regression from integration breaking OAuth/iOS | high | mitigate | Verify `crossOriginIsolated === false` in Task 3; CI guard backstop; no header edits |
| T-151-14 | Tampering | Wrong WDL order flips the bar (miscalibration shipped) | medium | mitigate | VALID-01 live eyeball explicitly checks bar direction + WDL sign (Assumption A5) |
| T-151-15 | Information disclosure | Maia signal persisted unexpectedly | medium | mitigate | Ephemeral hook cache only; grep confirms no localStorage/DB write; OUT-OF-SCOPE fence honored |
</threat_model>

<verification>
- `npx tsc -b`, `npm run lint`, `npm run knip`, `npm run build` all green.
- Both eval bars flank the board; 3 desktop columns; mobile Human tab + free-play tab pair; attribution visible.
- VALID-01 human sign-off recorded; `151-MAIA-MEASUREMENTS.md` filled (MAIA-06); crossOriginIsolated=false confirmed.
</verification>

<success_criteria>
On every position (game mode + free play), the Moves-by-Rating chart + LEFT Maia bar render live
with no server round-trip; the ELO selector drives the you-are-here line with correct defaults; the
attribution is visible; and Maia's calibration + size/latency are measured and judged acceptable
(SURF-04/05, MAIA-04/05/06, LIC-02, VALID-01).
</success_criteria>

## Artifacts this phase produces (Plan 06)
- `frontend/src/hooks/useMaiaEloDefault.ts` — ELO default-derivation hook (`selectedElo`/`setSelectedElo`, `FREE_PLAY_DEFAULT_ELO`)
- `frontend/src/pages/Analysis.tsx` — 3-column desktop + mobile Human-tab integration wiring `useMaiaEngine`, `useMaiaEloDefault`, second (Maia) EvalBar, `MovesByRatingChart`, `EloSelector`, `MaiaAttribution`
- Possible extracted subcomponent (e.g. `AnalysisHumanColumn` / `MaiaHumanTab`) if the layout rework would push Analysis.tsx past CLAUDE.md soft limits
- New testids: `analysis-human-column`, `analysis-maia-eval-bar`, `analysis-tab-human`
- `.planning/phases/151-.../151-MAIA-MEASUREMENTS.md` — MAIA-06 size + latency measurements

<output>
Create `.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-06-SUMMARY.md` when done.
Record the VALID-01 calibration verdict and the D-10 model-size decision.
</output>
