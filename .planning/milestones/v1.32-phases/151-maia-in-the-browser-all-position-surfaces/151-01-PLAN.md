---
phase: 151-maia-in-the-browser-all-position-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/public/maia/maia3_simplified.onnx
  - frontend/public/maia/ort-wasm-simd-threaded.mjs
  - frontend/public/maia/README.md
  - scripts/inspect_maia_onnx.mjs
  - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-MAIA-CONTRACT.md
autonomous: false
requirements: [MAIA-01, MAIA-06]
user_setup: []

must_haves:
  truths:
    - "The version-pinned maia3_simplified.onnx is committed under frontend/public/maia/ with a recorded SHA-256"
    - "A machine-readable contract doc records the model's exact input names/shapes (incl. how ELO is fed), output names/shapes, policy layout (64x64 vs flat 4096), and WDL logit order"
    - "The model loads via onnxruntime with no unsupported-op errors on the WASM execution provider"
  artifacts:
    - "frontend/public/maia/maia3_simplified.onnx (pinned model)"
    - "frontend/public/maia/ort-wasm-simd-threaded.mjs (vendored onnxruntime-web wasm runtime asset)"
    - "scripts/inspect_maia_onnx.mjs (contract inspection script)"
    - ".planning/phases/151-maia-in-the-browser-all-position-surfaces/151-MAIA-CONTRACT.md (confirmed tensor contract)"
  key_links:
    - "vite.config.ts globIgnores excludes .onnx + onnxruntime .wasm from the PWA precache manifest"
    - "vite.config.ts optimizeDeps.exclude includes onnxruntime-web"
  prohibitions:
    - "The .onnx model artifact is committed UNMODIFIED — no fine-tuning, quantization-in-place, or graph edits (AGPL S13)"
    - "No CSSLab AGPL JavaScript (encoding/inference utilities) is copied or vendored — only the model data asset and the MIT onnxruntime-web runtime"
---

<objective>
De-risk the entire phase by resolving the one genuinely-uncertain unknown FIRST: the exact
input/output tensor contract of `maia3_simplified.onnx`. Obtain the unmodified, version-pinned
model, vendor it plus the onnxruntime-web runtime assets under `frontend/public/maia/`, and
confirm — hands-on, against the model itself and the CSSLab reference client — its input encoding
(board planes + how ELO is fed: raw scalar vs precomputed embedding), output tensor layout
(policy 64x64 vs flat 4096; WDL logit order W/D/L), batch-dimension support, and that it loads
with no unsupported-op errors. Record the confirmed contract in `151-MAIA-CONTRACT.md` — every
downstream plan (glue math, worker, chart, bar) is shape-dependent on this file.

Purpose: Everything else in the phase assumes a tensor layout. This plan replaces assumption with
a recorded, verified contract so Wave 2+ builds against fact, not the paper's training-time
architecture (which conventionally differs from the ONNX export — RESEARCH Pitfall 3).
Output: Pinned model asset, vendored ort runtime, inspection script, and the confirmed contract doc.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-CONTEXT.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md
@.planning/spikes/004-maia3-onnx-browser-feasibility/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — onnxruntime-web / onnxruntime-node [SUS]</name>
  <what-built>
    Nothing yet — this gate precedes the install. RESEARCH's Package Legitimacy Audit flags
    `onnxruntime-web` as [SUS] via the identical false-positive pattern seen for `stockfish` in
    Phase 136 (the legitimacy checker keys on the latest version's publish date, not package age).
    It is Microsoft's official ONNX Runtime Web build, MIT-licensed, ~2.5M weekly downloads, with
    no postinstall script. The Node-side inspection helper uses the sibling `onnxruntime-node`
    (same Microsoft org/repo). Threat T-151-SC (supply chain) covers both.
  </what-built>
  <action>Blocking-human legitimacy gate: pause before any `npm install` and have a human verify onnxruntime-web/onnxruntime-node provenance per the how-to-verify steps. Not auto-approvable (workflow.auto_advance ignored).</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/onnxruntime-web — confirm publisher is Microsoft, license MIT, repo github.com/microsoft/onnxruntime.
    2. Run `npm view onnxruntime-web scripts.postinstall` — confirm it returns empty (no postinstall).
    3. Same checks for https://www.npmjs.com/package/onnxruntime-node.
    4. Confirm the pinned version (~1.27.0) matches the current published version.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install onnxruntime-web` + `npm install -D onnxruntime-node`, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install onnxruntime-web, obtain + pin the model, vendor runtime assets</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/public/maia/maia3_simplified.onnx, frontend/public/maia/ort-wasm-simd-threaded.mjs, frontend/public/maia/README.md</files>
  <read_first>
    - frontend/vite.config.ts (existing optimizeDeps.exclude:['stockfish'], globIgnores:['**/*.wasm','**/*.html'])
    - frontend/public/engine/ (Stockfish vendoring precedent — how runtime data assets are served un-bundled)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md (Standard Stack; Pitfall 5 model-size vs PWA precache; Open Questions 2 and 3)
    - README.md "Engine Binaries (GPLv3 License Note)" section (vendoring-precedent reasoning: reproducible, no runtime third-party fetch)
  </read_first>
  <action>
    In `frontend/`, run `npm install onnxruntime-web` (pin the resolved version in package.json,
    verify `npm view onnxruntime-web license` is MIT) and `npm install -D onnxruntime-node` (dev-only,
    for the Node inspection/integration path). Obtain the unmodified `maia3_simplified.onnx` — the
    smallest Maia-3, the artifact maiachess.com ships, per D-09 — from the
    CSSLab source (github.com/CSSLab/maia-platform-frontend served asset, or huggingface.co/UofTCSSLab
    weights per canonical_refs) and commit it to `frontend/public/maia/maia3_simplified.onnx` — do NOT
    modify, quantize, or re-export it (AGPL S13; MAIA-01 "unmodified"). Copy the onnxruntime-web WASM
    runtime asset(s) (`ort-wasm-simd-threaded.mjs` and the matching `.wasm`, whose filenames differ from
    Stockfish's) from `node_modules/onnxruntime-web/dist/` into `frontend/public/maia/` so the Worker can
    `importScripts`/import them at a fixed path without bundler processing. Record the model's SHA-256,
    byte size, and source URL in `frontend/public/maia/README.md`. In `frontend/vite.config.ts`: add
    `onnxruntime-web` to `optimizeDeps.exclude`; broaden `globIgnores` so BOTH `.onnx` and the
    onnxruntime `.wasm` filename pattern are excluded from the Workbox precache manifest (mirrors the
    existing `**/*.wasm` entry — verify the onnxruntime wasm name actually matches the glob; if the
    glob is too narrow, widen it). Do NOT add COOP/COEP headers anywhere.
  </action>
  <verify>
    <automated>cd frontend && npm run build && node -e "const fs=require('fs');const sw=fs.readFileSync('dist/sw.js','utf8');if(/\.onnx/.test(sw)||/ort-wasm.*\.wasm/.test(sw)){console.error('FAIL: onnx/ort wasm precached in sw.js');process.exit(1)}console.log('PASS: model+ort wasm excluded from precache')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/public/maia/maia3_simplified.onnx` exists and its SHA-256 + size + source URL are recorded in `frontend/public/maia/README.md`.
    - `npm ls onnxruntime-web` resolves a pinned MIT version; `onnxruntime-node` is under devDependencies.
    - `npm run build` succeeds and `dist/sw.js` contains no `.onnx` or `ort-wasm*.wasm` precache entries (automated check passes).
    - No COOP/COEP header appears in vite.config.ts (the CI guard still passes).
  </acceptance_criteria>
  <done>Pinned model + ort runtime vendored under public/maia/; PWA precache excludes them; onnxruntime installed and pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Inspect + confirm the tensor contract, record 151-MAIA-CONTRACT.md</name>
  <files>scripts/inspect_maia_onnx.mjs, .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-MAIA-CONTRACT.md</files>
  <read_first>
    - frontend/public/maia/maia3_simplified.onnx (the artifact from Task 2)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md (Pattern 3 board encoding; Pitfall 3 paper-vs-export; Pitfall 4 WebGPU op gaps; Assumptions A1/A2/A5; Open Question 1 batch dim)
    - .planning/research/questions.md (Q-013 per-move prob extraction, Q-015 ELO conditioning)
    - CSSLab reference client encoding (github.com/CSSLab/maia-platform-frontend — READ for contract confirmation only; do NOT copy its code)
  </read_first>
  <action>
    Write `scripts/inspect_maia_onnx.mjs`: load `frontend/public/maia/maia3_simplified.onnx` with
    `onnxruntime-node`, print `session.inputNames`, `session.outputNames`, and each tensor's declared
    dims + element type; then run one real inference on the standard start position and print output
    tensor shapes + a slice of values. Use the script's output PLUS a read of the CSSLab reference
    client to CONFIRM and RECORD in `.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-MAIA-CONTRACT.md`:
    (a) exact input tensor name(s) and shape(s) for the board planes (12-dim one-hot; side-to-move
    flip; whether history n>0 is required or n=0 "simplified"); (b) how ELO is fed — raw scalar
    input name(s) (e.g. white_elo/black_elo) vs a caller-precomputed embedding vector (Assumption A1);
    (c) the confirmed supported/trained ELO range + sensible ladder granularity (D-08 — do NOT hard-code
    the provisional 1100-2000 without confirming); (d) policy output name + layout (64x64 from->to matrix
    vs flat 4096) and how promotions/castling/en-passant map; (e) WDL output name + confirmed logit order
    (W/D/L vs a permutation — Assumption A5); (f) whether the graph declares a batch dimension usable for
    a one-call ELO sweep (Open Question 1). Mark any item the model itself cannot answer as
    "CONFIRM-AT-RUNTIME" with the reason.
  </action>
  <verify>
    <automated>node scripts/inspect_maia_onnx.mjs && grep -viE '^\s*(#|//|\*)' .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-MAIA-CONTRACT.md | grep -qiE 'WDL|policy' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/inspect_maia_onnx.mjs` runs to completion, printing real inputNames/outputNames/dims and a start-position inference with NO unsupported-op error (proves MAIA-06 op-support on the CPU/WASM path).
    - `151-MAIA-CONTRACT.md` records concrete answers (or explicit CONFIRM-AT-RUNTIME notes) for all six items (a)-(f), including the confirmed ELO range for D-08.
    - The doc states unambiguously the policy layout (64x64 vs 4096) and the WDL logit order — the two facts Wave 2 encoding glue cannot proceed without.
  </acceptance_criteria>
  <done>The confirmed tensor contract is recorded; downstream plans can build board->tensor + mask + softmax against fact, not assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | Third-party install code enters the build (onnxruntime-web/-node) |
| CSSLab/HF -> repo | Third-party model artifact vendored into git and shipped to browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-SC | Tampering | npm installs (onnxruntime-web/-node) | high | mitigate | slopcheck + blocking-human checkpoint (Task 1); onnxruntime-web is [SUS] false-positive (Phase 136 stockfish precedent); no postinstall script |
| T-151-01 | Tampering | maia3_simplified.onnx artifact (supply chain) | high | mitigate | Commit unmodified + version-pinned with recorded SHA-256; no runtime third-party fetch (Stockfish precedent) |
| T-151-02 | Elevation/Availability | Accidental COOP/COEP reintroduction breaking OAuth + iOS Safari | high | mitigate | Do not add COOP/COEP in vite.config; existing CI guard (ci.yml) fails the build on regression |
</threat_model>

<verification>
- `npm run build` green; `dist/sw.js` free of `.onnx`/`ort-wasm*.wasm` precache entries.
- `node scripts/inspect_maia_onnx.mjs` loads + runs the model with no unsupported-op error.
- `151-MAIA-CONTRACT.md` answers all six contract items (or flags CONFIRM-AT-RUNTIME).
- CI COOP/COEP guard still passes (no headers added).
</verification>

<success_criteria>
The exact `maia3_simplified.onnx` input encoding + output layout (policy shape, WDL order) and the
confirmed ELO range are recorded and verified against a real load+inference — Wave 2 is unblocked.
</success_criteria>

## Artifacts this phase produces (Plan 01)
- `frontend/public/maia/maia3_simplified.onnx` — pinned unmodified model artifact
- `frontend/public/maia/ort-wasm-simd-threaded.mjs` + matching `.wasm` — vendored onnxruntime-web runtime
- `frontend/public/maia/README.md` — SHA-256 / size / source provenance
- `scripts/inspect_maia_onnx.mjs` — ONNX contract inspection script (dev tool)
- `.planning/phases/151-.../151-MAIA-CONTRACT.md` — confirmed tensor contract (consumed by Plans 04/05/06)
- vite.config.ts changes: `optimizeDeps.exclude += onnxruntime-web`; `globIgnores` broadened for `.onnx` + ort `.wasm`

<output>
Create `.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-01-SUMMARY.md` when done.
Record the confirmed policy layout, WDL order, ELO input mechanism, and ELO range prominently — Wave 2 reads these.
</output>
