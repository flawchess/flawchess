---
phase: 151-maia-in-the-browser-all-position-surfaces
plan: 05
type: execute
wave: 3
depends_on: ["151-04"]
files_modified:
  - frontend/src/components/analysis/EvalBar.tsx
  - frontend/src/components/analysis/__tests__/EvalBar.test.tsx
  - frontend/src/components/analysis/MovesByRatingChart.tsx
  - frontend/src/components/analysis/__tests__/MovesByRatingChart.test.tsx
  - frontend/src/components/analysis/EloSelector.tsx
  - frontend/src/components/analysis/__tests__/EloSelector.test.tsx
  - frontend/src/lib/theme.ts
requirements: [SURF-01, SURF-02, SURF-03, SURF-04]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "EvalBar accepts an optional whiteFraction override that bypasses evalCp/evalMate and drives the fill directly (0..1), keeping the flipped/testid contract"
    - "MovesByRatingChart renders one line per candidate move over the ELO ladder, capped at top-N-by-peak unioned with {played, best}, with a 'you are here' vertical reference line and played/best emphasized"
    - "EloSelector is an interactive control that drives the selected ELO and reports changes"
  artifacts:
    - "frontend/src/components/analysis/MovesByRatingChart.tsx"
    - "frontend/src/components/analysis/EloSelector.tsx"
    - "EvalBar whiteFraction prop"
  key_links:
    - "MovesByRatingChart consumes useMaiaEngine's perElo shape; the Maia bar reuses EvalBar with whiteFraction=expectedScoreAtSelectedElo; Plan 06 wires them into the layout"
  prohibitions:
    - "Do NOT port spike 006's imperative SVG — implement with Recharts primitives per EvalChart.tsx conventions"
    - "No hard-coded semantic colors — chart line/emphasis colors come from theme.ts; text >= text-sm"
    - "Do NOT fork EvalBar into a second bar component — extend it"
---

<objective>
Build the two visible Maia surfaces plus the shared ELO control as standalone, tested components,
against the `useMaiaEngine` output shape from Plan 04 (read its SUMMARY): (1) extend `EvalBar` with
an optional `whiteFraction` override so the Maia expected-score bar is a clean mirror of the
Stockfish bar sharing one flip/testid contract (SURF-04, D-04/D-05); (2) `MovesByRatingChart`, a
Recharts port of spike 006 — one probability line per candidate move over the ELO ladder, top-N-by-
peak union {played, best} cap, "you are here" ReferenceLine, played/best emphasized (SURF-01/02/03);
(3) `EloSelector`, the interactive control that drives the selected ELO (D-06). Components only —
Plan 06 mounts and wires them into the analysis layout.

Purpose: Isolate the presentational surfaces so they are unit-tested before the risky layout rework.
Output: extended EvalBar, chart component, ELO selector, all with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-04-SUMMARY.md
@.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md
@.planning/spikes/006-moves-by-rating-chart/README.md
@frontend/src/components/library/EvalChart.tsx
@frontend/src/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EvalBar with a whiteFraction override (SURF-04, D-04/D-05)</name>
  <files>frontend/src/components/analysis/EvalBar.tsx, frontend/src/components/analysis/__tests__/EvalBar.test.tsx</files>
  <read_first>
    - frontend/src/components/analysis/EvalBar.tsx (computeWhiteFraction; EvalBarProps; flipped logic; data-testid="analysis-eval-bar")
    - frontend/src/components/analysis/__tests__/EvalBar.test.tsx (existing tests to extend, keep passing)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md (Pattern 5 EvalBar generalization)
  </read_first>
  <action>
    Add an optional `whiteFraction?: number` to `EvalBarProps` and a `testId?: string` (default
    `analysis-eval-bar`) so two instances have distinct testids. In `computeWhiteFraction`, when
    `whiteFraction !== undefined` return it clamped to 0..1 and bypass the evalCp/evalMate/depth
    sigmoid entirely (the Maia bar already supplies E = W + 0.5*D as 0..1). Preserve the existing
    `flipped` orientation behavior unchanged. Update the aria-label to read a Maia-appropriate string
    when driven by whiteFraction (e.g. "Maia expected score: NN%"). Extend `EvalBar.test.tsx`:
    whiteFraction=0.75 -> white fill height ~75% regardless of evalCp; whiteFraction respects
    `flipped`; the Stockfish (no-whiteFraction) path is unchanged.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/EvalBar.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - whiteFraction override drives the fill directly and ignores evalCp/evalMate; existing Stockfish-bar tests still pass.
    - A distinct `testId` prop lets Plan 06 mount the Maia bar (e.g. `analysis-maia-eval-bar`) alongside the Stockfish bar.
    - `npx tsc -b` clean.
  </acceptance_criteria>
  <done>One EvalBar component serves both bars via a whiteFraction override, sharing flip/testid contract (SURF-04, D-04/D-05).</done>
</task>

<task type="auto">
  <name>Task 2: MovesByRatingChart — Recharts port of spike 006 (SURF-01/02/03)</name>
  <files>frontend/src/components/analysis/MovesByRatingChart.tsx, frontend/src/components/analysis/__tests__/MovesByRatingChart.test.tsx, frontend/src/lib/theme.ts</files>
  <read_first>
    - .planning/spikes/006-moves-by-rating-chart/README.md AND index.html (VISUAL/BEHAVIORAL spec only — cap rule, colors, you-are-here marker, tooltip content; do NOT copy the imperative SVG)
    - frontend/src/components/library/EvalChart.tsx (Recharts API precedent: LineChart/Line/ReferenceLine/XAxis/YAxis, custom tooltip via ChartTooltipBox, ResponsiveContainer usage, data-testid convention)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-04-SUMMARY.md (perElo element shape: { elo, moveProbabilities: Record<san,number> }[])
    - frontend/src/lib/theme.ts (WDL_* + accent colors; add any new chart-line constants HERE if needed)
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-RESEARCH.md (Pattern 6 chart; Pitfall 6 throwaway-spike; D-02 legibility at ~360px)
  </read_first>
  <action>
    Create `frontend/src/components/analysis/MovesByRatingChart.tsx` exporting `MovesByRatingChart`
    with props `{ perElo, playedSan, bestSan, selectedElo }`. Pivot `perElo` into Recharts row data
    keyed by ELO with one series per SAN. Implement the cap in a pure helper: sort candidate moves by
    peak probability, take top-N (named constant `TOP_N`, default ~6), union with `{playedSan,
    bestSan}` when present (SURF-03). Render a `<LineChart>`/`<ResponsiveContainer>` (or ComposedChart
    per EvalChart precedent) with one `<Line dot={false}>` per shown SAN; emphasize played + best with
    thicker `strokeWidth` and a distinct theme accent (colors from theme.ts — add named constants
    there if the palette needs new entries; never inline oklch/hex). Add a `<ReferenceLine
    x={selectedElo}>` "you are here" marker (SURF-02). Keep the x-axis compact/legible at ~360px
    (D-02): few ticks, formatted ELO labels. Provide a custom tooltip (move SAN + probability) via the
    `content` prop like EvalChart. `data-testid="moves-by-rating-chart"`; keep text >= text-sm. Write
    `MovesByRatingChart.test.tsx`: given a fixture perElo where a non-top-N move is the played move,
    assert the rendered line set = top-N union {played, best} (count + membership), and that the
    reference line reflects selectedElo.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/MovesByRatingChart.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - The cap helper returns exactly top-N-by-peak unioned with {played, best}; a played move outside top-N is still shown (test asserts membership + count).
    - A vertical reference line is placed at selectedElo; played + best lines are visually emphasized (thicker stroke / accent).
    - No imperative SVG copied from the spike; colors sourced from theme.ts; container has `data-testid="moves-by-rating-chart"`.
    - Tests + `npx tsc -b` pass.
  </acceptance_criteria>
  <done>A Recharts "Moves by Rating" chart with the top-N union {played,best} cap and you-are-here line (SURF-01/02/03).</done>
</task>

<task type="auto">
  <name>Task 3: EloSelector — interactive ELO control (D-06)</name>
  <files>frontend/src/components/analysis/EloSelector.tsx, frontend/src/components/analysis/__tests__/EloSelector.test.tsx</files>
  <read_first>
    - .planning/phases/151-maia-in-the-browser-all-position-surfaces/151-04-SUMMARY.md (MAIA_ELO_LADDER / confirmed range for the selector bounds)
    - frontend/src/components/ui/slider.tsx AND select.tsx (pick the primitive that fits a discrete ELO ladder — a slider snapped to ladder steps or a select)
    - frontend/src/lib/maiaEncoding.ts (MAIA_ELO_LADDER — the source of truth for bounds/steps; do NOT hard-code 1100-2000)
  </read_first>
  <action>
    Create `frontend/src/components/analysis/EloSelector.tsx` exporting `EloSelector` with props
    `{ value, onChange, ladder? }` (defaults to `MAIA_ELO_LADDER`). Render an accessible control
    (slider snapped to ladder steps, or a select) bounded by the confirmed ladder, showing the
    current ELO, and calling `onChange(elo)` on interaction. `data-testid="analysis-elo-selector"`
    and `aria-label`. Use Button/UI primitives and theme.ts (no hand-rolled colors). Write
    `EloSelector.test.tsx`: renders the current value; interacting fires onChange with a ladder value;
    respects ladder bounds.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/EloSelector.test.tsx && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - EloSelector shows the current value and fires onChange with a valid ladder ELO on interaction.
    - Bounds derive from MAIA_ELO_LADDER (not a hard-coded range); `data-testid="analysis-elo-selector"` + aria-label present.
    - lint + knip clean (component exported and imported by its test; Plan 06 adds the runtime import).
  </acceptance_criteria>
  <done>An interactive, ladder-bounded ELO selector driving the "you are here" line (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook output -> chart/bar | In-memory Maia result rendered as UI; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-11 | Tampering | Chart mis-rendering hides played/best move | low | mitigate | SURF-03 cap always unions {played,best}; unit test asserts membership |
| T-151-12 | Information disclosure | none (pure presentation of local data) | low | accept | No network, no user data beyond the current board |
</threat_model>

<verification>
- `npm test` green for EvalBar, MovesByRatingChart, EloSelector; `npx tsc -b` clean; lint + knip clean.
- Chart colors from theme.ts; no text below text-sm; testids on all interactive/container elements.
</verification>

<success_criteria>
The Maia bar (EvalBar whiteFraction), the "Moves by Rating" chart (top-N union {played,best},
you-are-here line), and the ELO selector exist as tested components ready for Plan 06 to wire
(SURF-01/02/03/04, D-06).
</success_criteria>

## Artifacts this phase produces (Plan 05)
- `frontend/src/components/analysis/EvalBar.tsx` — new `whiteFraction?` + `testId?` props
- `frontend/src/components/analysis/MovesByRatingChart.tsx` — Recharts chart + cap helper
- `frontend/src/components/analysis/EloSelector.tsx` — interactive ELO control
- Possible new named line/accent color constants in `frontend/src/lib/theme.ts`
- Test files for all three components

<output>
Create `.planning/phases/151-maia-in-the-browser-all-position-surfaces/151-05-SUMMARY.md` when done.
Record the exact props each component expects — Plan 06 wires them.
</output>
