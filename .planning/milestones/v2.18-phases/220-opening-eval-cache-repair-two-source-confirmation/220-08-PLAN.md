---
phase: 220-opening-eval-cache-repair-two-source-confirmation
plan: 08
type: execute
wave: 7
depends_on: [220-07]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [CACHEFIX-08, CACHEFIX-11]

user_setup:
  - service: production-deploy
    why: "Release 2's migration adds seven columns to a 2.57M-row table and rewrites ~2.5M rows during the Alembic step on container startup"
    dashboard_config:
      - task: "Run the /deploy skill (main -> production PR, squash-merge, bin/deploy.sh) and confirm the verified server SHA; expect the deploy to pause for the measured marking-walk duration recorded in 220-07-SUMMARY.md"
        location: "local terminal + github.com/<repo>/pulls"
  - service: prod-db-tunnel
    why: "Post-deploy verification queries the confirmed / candidate split and the benchmark lane's cache growth"
    dashboard_config:
      - task: "Start bin/prod_db_tunnel.sh for the verification queries; start bin/benchmark_db.sh for the benchmark-lane check"
        location: "local terminal"

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full CLAUDE.md pre-merge gate is green immediately before the Release 2 squash-merge, as a whole run, not a subset."
    - "`CHANGELOG.md` carries a Release 2 entry under `## [Unreleased]`; because two-source confirmation is not user-visible, a `### Changed` chore-level line suffices and the user-facing bullet stays the Release 1 one."
    - "Release 2 is one squash commit on `main`, `main` and `origin/main` agree, and `origin/production` carries it after the deploy."
    - "After the deploy, production's `opening_position_eval` shows a large `confirmed=true` population matching the repair's `screened_clean + confirmed_clean + repaired` audit count, and everything else as candidates."
    - "The benchmark database's `opening_position_eval` is growing (it was 0 rows before Release 1), proving the submit-path cache write is firing in a remote-only lane."
    - "No benchmark DB re-clone and no `gen_benchmarks` run was triggered anywhere in this phase."
  artifacts:
    - CHANGELOG.md
    - .planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-08-SUMMARY.md
  key_links:
    - "`opening_cache_audit.status` counts from `220-06-SUMMARY.md` -> the post-deploy `confirmed=true` count on prod (they must agree)"
    - "plan 02's benchmark-DB baseline (0 rows) -> the post-Release-1 growth check (CACHEFIX-12 acceptance queries 11 and 12)"
---

<objective>
Ship Release 2: the full pre-merge gate, the changelog entry, the squash-merge into `main`,
the production deploy, and the post-deploy verification that the confirmed population
matches the audit trail and the benchmark lane is finally caching openings.

Purpose: the hardening is only real once it is running, and two of the phase's acceptance
assertions (the confirmed/candidate split and the benchmark-lane cache growth) can only be
checked after it deploys.

Output: production running two-source confirmation, with the numbers to prove it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-CONTEXT.md
@.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-06-SUMMARY.md
@.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-07-SUMMARY.md
@docs/git-workflow.md
@docs/production-runbook.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan's share (phase-wide roster: `220-01-PLAN.md` §"Artifacts this phase produces"):

| Kind | Symbol / path |
|---|---|
| Changelog | a `### Changed` chore-level line for Release 2 under `## [Unreleased]` |
| Git | one squash-merge commit on `main` carrying Release 2 |
| Deployment | production running two-source confirmation with the provenance columns populated |
| Evidence | the post-deploy confirmed/candidate split and the benchmark-lane cache growth, pasted into `220-08-SUMMARY.md` |

## Decisions recorded by this plan

- **D-01's ship order completes here.** Release 1 (repair) shipped in plan 05, the prod
  repair ran in plan 06, and Release 2 (hardening) ships now, so `confirmed` was never
  forced onto unscreened rows and prod never spent a multi-day window with opening dedup
  effectively off. Reversibility: **costly** — this ordering cannot be replayed differently
  after the fact.
- **Changelog: a chore-level line, not a second user-facing bullet.** Two-source
  confirmation changes nothing a user can see; the user-facing bullet was Release 1's flaw
  count correction. CACHEFIX-11's changelog requirement is satisfied by that bullet.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `main` -> `production` | The release PR is cut from `origin/main`; unpushed local commits are silently dropped |
| Alembic on container startup -> a 2.57M-row table | The marking walk runs during the deploy, so the backend is down for its duration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-12 | Tampering | release contents | high | mitigate | `git rev-list --left-right --count main...origin/main` must read `0 0` before cutting the release PR; the full seven-command pre-merge gate runs immediately before the squash-merge, not a subset |
| T-220-16 | Denial of service | deploy stalled by the marking walk | medium | mitigate | The dev wall clock was measured in plan 07 and is stated in the deploy instructions, so the operator knows the expected downtime before starting; the walk is 50k-row statements, each holding only `ROW EXCLUSIVE` |
| T-220-19 | Repudiation | shipping hardening without proving it took effect | high | mitigate | Post-deploy verification compares the prod `confirmed=true` count against the audit statuses recorded in `220-06-SUMMARY.md` and checks the benchmark lane's cache growth; both are pasted into the summary |
| T-220-SC | Tampering | package-manager installs | low | accept | No external packages installed in this phase |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Changelog entry, full pre-merge gate, squash-merge Release 2 into `main`</name>
  <read_first>
    - `CHANGELOG.md` lines 1-30 (the `## [Unreleased]` heading, the Release 1 bullet under `### Fixed`, and whether a `### Changed` subsection exists)
    - `docs/git-workflow.md` (GitLab Flow; local squash-merge into `main`, no GitHub PR for phase work)
    - `CLAUDE.md` §"Pre-merge gate (MANDATORY before squash-merging to `main`)"
    - `.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-07-SUMMARY.md`
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Add a `### Changed` line under `## [Unreleased]` describing Release 2 at chore level: the
shared opening-evaluation cache now requires two independent engine evaluations to agree
before a position is trusted, and records where each value came from. One line, no
em-dashes. Do not add a second user-facing bullet — nothing a user can see changes; the
user-facing bullet remains Release 1's flaw-count correction, which is what CACHEFIX-11
requires.

Then run the FULL CLAUDE.md pre-merge gate, in order, resolving every output:

```
uv run ruff format app/ tests/ scripts/ analysis/
uv run ruff check . --fix
uv run ty check app/ tests/ scripts/
uv run --project analysis --with ty ty check analysis/
uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200
uv run pytest -n auto -x
( cd frontend && npm run lint && npm test -- --run )
```

Then squash-merge the phase branch into `main`, one commit, delete the branch, push. Verify
afterwards that `git branch --show-current` reads `main` — the GSD commit helper can check
out a fresh `gsd/phase-220-...` branch and commit there under the `phase` branching strategy
— and that `git rev-list --left-right --count main...origin/main` reads `0	0`, because the
release PR is cut from `origin/main` and any unpushed local commit would be silently
dropped.
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` has exactly one `## [Unreleased]` heading, containing both the Release 1 `### Fixed` bullet and the new Release 2 `### Changed` line.
    - The full seven-command pre-merge gate exits 0 at every step.
    - After the merge, `git branch --show-current` reads `main` and `git rev-list --left-right --count main...origin/main` reads `0	0`.
    - `git log --oneline -1 main` names phase 220 Release 2.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format app/ tests/ scripts/ analysis/ &amp;&amp; uv run ruff check . --fix &amp;&amp; uv run ty check app/ tests/ scripts/ &amp;&amp; uv run --project analysis --with ty ty check analysis/ &amp;&amp; uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200 &amp;&amp; uv run pytest -n auto -x &amp;&amp; ( cd frontend &amp;&amp; npm run lint &amp;&amp; npm test -- --run )</automated>
    <fails_when>non-zero exit at any step, any "error[" line from ty, any "FAILED" line from pytest, or ruff reporting reformatted files after a clean commit</fails_when>
    <automated>test "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" -eq 1 &amp;&amp; git rev-list --left-right --count main...origin/main | grep -qE '^0[[:space:]]+0$' &amp;&amp; test "$(git branch --show-current)" = main &amp;&amp; echo RELEASE2-MERGED-OK</automated>
    <fails_when>non-zero exit, or "RELEASE2-MERGED-OK" absent from stdout (a non-`0 0` divergence, or the commit helper having left the working tree on a recreated phase branch)</fails_when>
  </verify>
  <done>Release 2 is one squash commit on `main` with a green full gate and a chore-level changelog line, and `main` and `origin/main` agree.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Deploy Release 2 to production</name>
  <precondition>`git rev-list --left-right --count main...origin/main` reads `0	0` and `220-07-SUMMARY.md` records the dev-measured wall clock of the marking keyset walk.</precondition>
  <read_first>
    - `docs/git-workflow.md`, `docs/production-runbook.md`
    - `.claude/skills/deploy/SKILL.md`
    - `.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-07-SUMMARY.md` (the expected marking-walk duration)
    - `CLAUDE.md` §"Scripts & tooling" (`bin/deploy.sh` is the only sanctioned deploy path)
  </read_first>
  <action>
Deploy `main` to production. Unlike Release 1's deploy, this one pauses on the Alembic step
for the duration of the ~2.5M-row marking walk, so the operator needs to know the expected
window before starting.
  </action>
  <instructions>
Already automated: the release is fully scripted.

The one thing requiring a human: triggering the release with the expected downtime in mind,
and confirming the verified server SHA.

1. Read the dev-measured marking-walk duration from `220-07-SUMMARY.md`. Prod has roughly
   24x the dev row count, so scale that estimate and pick a low-traffic window.
2. Run the `/deploy` skill (or `bin/deploy.sh`). Never deploy by direct SSH.
3. Watch the backend container's startup log through the Alembic step; the marking walk
   prints per-batch progress. Note the real duration.
4. Confirm the verified server SHA matches `git rev-parse origin/production`.
5. Forward-port immediately after the release — skipping it makes the next release conflict
   on its PR.
  </instructions>
  <verification>
An agent can check afterwards: `git log origin/production --oneline -3` names the Release 2
commit, and through the tunnel the seven provenance columns exist on
`opening_position_eval`.
  </verification>
  <acceptance_criteria>
    - `git log origin/production --oneline -3` contains the Release 2 squash commit.
    - Through the prod tunnel, all seven of `confirmed`, `n_sources`, `disagreements`, `engine_version`, `written_at`, `confirmed_at`, `source_game_id` are present on `opening_position_eval`.
    - The real marking-walk duration is recorded in `220-08-SUMMARY.md` next to the dev estimate.
    - The verified server SHA matches `git rev-parse origin/production`.
  </acceptance_criteria>
  <verify>
    <automated>git fetch origin production --quiet &amp;&amp; git log origin/production --oneline -3</automated>
    <fails_when>non-zero exit, or the Release 2 commit subject absent from the three listed commits</fails_when>
    <human-check>The operator confirms the deploy reported a verified server SHA matching `origin/production`, states the real marking-walk duration, and confirms the forward-port ran.</human-check>
  </verify>
  <resume-signal>Type "deployed" with the verified server SHA and the marking-walk duration.</resume-signal>
  <done>Production runs two-source confirmation with all seven provenance columns populated, and the real migration duration is on record for the next large-table change.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Post-deploy verification — the confirmed split, the benchmark lane, and the disagreement watch</name>
  <precondition>`origin/production` carries the Release 2 commit and the prod tunnel is running.</precondition>
  <read_first>
    - `.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-06-SUMMARY.md` (the audit status counts the confirmed population must match, and acceptance queries 11 and 12)
    - `.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-02-SUMMARY.md` (the benchmark DB's pre-Release-1 `opening_position_eval` count, expected to be 0)
    - `.claude/skills/db-report/SKILL.md` §3 Checks C and D
    - `docs/dev-tooling.md` (`bin/benchmark_db.sh start`, `bin/prod_db_tunnel.sh`)
  </read_first>
  <action>
Confirm the hardening actually took effect and that the phase's two deferred acceptance
assertions now hold. The queries are agent-runnable read-only through the tunnel; what needs
a human is starting the tunnel and the benchmark DB, and the judgement on the disagreement
counter after a few days of live traffic.
  </action>
  <instructions>
Already automated: the queries below are read-only and an agent runs and pastes them.

The one thing requiring a human: starting `bin/prod_db_tunnel.sh` and
`bin/benchmark_db.sh start`, and returning after a few days of live traffic for step 4.

1. Confirmed split on prod: `SELECT confirmed, count(*) FROM opening_position_eval GROUP BY
   confirmed;`. The `true` count must equal the sum of `screened_clean + confirmed_clean +
   repaired` recorded in `220-06-SUMMARY.md`, allowing for rows inserted after the audit seed
   (which are candidates by design). A materially smaller `true` count means the marking walk
   did not finish.
2. Self-promotion sanity: `SELECT count(*) FROM opening_position_eval WHERE confirmed AND
   n_sources < 2;` must be 0.
3. Benchmark lane (CACHEFIX-12 acceptance queries 11 and 12): with `bin/benchmark_db.sh
   start`, run `SELECT count(*) FROM opening_position_eval;` against `localhost:5433`. The
   baseline in `220-02-SUMMARY.md` is 0 after ~91k engine games; it must now be growing.
   Also confirm a leased game whose opening positions are cached carries fewer engine targets
   than an uncached one.
4. After a few days of live traffic, run `SELECT count(*) FROM opening_position_eval WHERE
   disagreements >= 1;` and `... WHERE disagreements >= 2;`, and check Sentry for the
   opening-cache message. A handful of `>= 1` rows is expected noise at a p99 floor; a
   growing `>= 2` population is the misalignment signature D-12 exists to surface and is a
   new seed, not a silent tolerance.
5. Attest that no benchmark DB re-clone and no `gen_benchmarks` run happened in this phase:
   `git log --oneline -40 -- reports/benchmark/ analysis/`.
  </instructions>
  <verification>
Every query above is read-only and re-runnable; an agent can diff a re-run against what was
pasted.
  </verification>
  <acceptance_criteria>
    - The prod `confirmed=true` count is recorded and agrees with the `screened_clean + confirmed_clean + repaired` total from `220-06-SUMMARY.md`.
    - `SELECT count(*) FROM opening_position_eval WHERE confirmed AND n_sources < 2` on prod returns 0.
    - The benchmark DB's `opening_position_eval` count is non-zero and recorded next to the 0-row baseline.
    - The `disagreements >= 1` and `>= 2` counts are recorded with the date they were taken.
    - `git log --oneline -40 -- reports/benchmark/ analysis/` shows no regeneration commit from this phase, and that is stated in the summary.
  </acceptance_criteria>
  <verify>
    <automated>git log --oneline -40 -- reports/benchmark/ analysis/</automated>
    <fails_when>non-zero exit, or a commit subject in the output naming phase 220 or a benchmark regeneration</fails_when>
    <human-check>The operator confirms the confirmed count matches the audit totals, `n_sources < 2` among confirmed rows is 0, the benchmark DB cache is growing from its 0-row baseline, and states the disagreement counts with their date.</human-check>
  </verify>
  <resume-signal>Paste the five results and type "hardening verified", or describe the mismatch.</resume-signal>
  <done>Production's confirmed population provably comes from the repair's audit trail, no confirmed row has fewer than two sources, the benchmark lane caches openings for the first time, and the disagreement counter is being watched.</done>
</task>

</tasks>

## Spec-less probe ledger (this plan)

Probe items owned here: 0 — all 46 are distributed across plans 01, 02, 03, 04, 06 and 07.
Authored: 0. Flagged assumptions: 0. Equality holds trivially: 0 == 0 + 0.

Prohibitions minted here: 0. See `220-01-PLAN.md` for the phase-wide prohibition ledger.

<verification>
- The full pre-merge gate exited 0 at every step immediately before the squash-merge.
- `origin/production` carries Release 2 and the verified server SHA matches.
- The prod confirmed count agrees with the audit totals and no confirmed row has `n_sources < 2`.
- The benchmark DB's cache is growing from its 0-row baseline.
- No benchmark regeneration commit exists in this phase's history.
</verification>

<success_criteria>
Two-source confirmation is live on production with its provenance populated from the
repair's own audit trail, the remote-only benchmark lane is caching openings for the first
time, and the disagreement counter is in place as the standing early-warning signal for the
next misalignment.
</success_criteria>

<output>
Create `.planning/phases/220-opening-eval-cache-repair-two-source-confirmation/220-08-SUMMARY.md` when done.
Record in it: the pre-merge gate summary, the Release 2 squash SHA, the verified prod server
SHA, the real marking-walk duration next to the dev estimate, the confirmed/candidate split
against the audit totals, the benchmark DB cache count, and the first disagreement readings
with their date.
</output>
