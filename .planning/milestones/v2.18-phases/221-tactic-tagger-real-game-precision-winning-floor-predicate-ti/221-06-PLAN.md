---
phase: 221-tactic-tagger-real-game-precision-winning-floor-predicate-ti
plan: 06
type: execute
wave: 5
depends_on: [221-03, 221-05]
files_modified:
  - tests/scripts/tagger/precision_floors.py
  - tests/services/test_tactic_detector.py
  - app/services/tactic_detector.py
  - app/repositories/library_repository.py
  - tests/services/test_tactic_comparison_service.py
  - frontend/src/lib/tacticComparisonMeta.ts
  - frontend/src/lib/tacticMotifDefinitions.ts
  - CHANGELOG.md
autonomous: true
requirements: [TAGFIX-04, TAGFIX-05, TAGFIX-07, TAGFIX-09]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- goal-backward truths (TAGFIX-05 SC1: oracle parity) ---
    - "`uv run python scripts/research/oracle_compare.py` reports 0 'cook only' rows for deflection, fork and trapped-piece, 0 'ours only' rows for discovered-attack, and the boden/double-bishop split matching cook; the full divergence table is pasted into the SUMMARY."
    - "The only remaining oracle divergences are the two documented deliberate ones — sacrifice (D-05 persistence + D-06 cap) and clearance (D-06 cap + D-07 clauses) — each named in the SUMMARY with its row count."
    # --- goal-backward truths (TAGFIX-07: post-fix floors) ---
    - "Every `REALGAME_REAL_SHARE_FLOOR` entry is re-seeded from the post-fix measurement at roughly 0.05 below the measured real-share, with the measurement in its trailing comment, and `uv run pytest tests/scripts/tagger -q` passes against the raised values."
    - "Every `PRECISION_FLOOR` entry whose counts moved across plans 02, 04 and 05 carries its final measured TRAIN/TEST numbers; no value was lowered anywhere in the phase."
    - "Success criterion 2 is evaluated with numbers: sacrifice real-share is at or above 0.80, clearance real-share is at or above 0.80 OR clearance is suppressed, and no other motif's real-share fell below its plan-01 frozen BEFORE value."
    # --- goal-backward truths (TAGFIX-04: the decision) ---
    - "Exactly one branch of D-07 is executed and the reason is a recorded number: KEEP when the measured clearance real-game real-share is at or above 0.80 with at least `REALGAME_MIN_ROWS_FOR_FLOOR` surviving rows, SUPPRESS otherwise."
    - "If SUPPRESS: `clearance` is in `SUPPRESSED_MOTIFS`, its `PRECISION_FLOOR` and `REALGAME_REAL_SHARE_FLOOR` entries are commented out with a dated reason, its `_TIER3_REGISTRY` tuple is removed so no PV can dispatch it, `int(TacticMotifInt.CLEARANCE)` still encodes and decodes, the `clearance` family is gone from `FAMILY_TO_MOTIF_INTS`, the two family tests are updated to 20-minus-one keys and a suppressed-int set of two, and the four `tacticComparisonMeta.ts` sites plus the `tacticMotifDefinitions.ts` copy string are removed with `npm run build` green."
    - "If KEEP: no suppression touchpoint is edited, `clearance` keeps its family and its two floors, and the recorded number justifying the keep is in the SUMMARY."
    # --- goal-backward truths (TAGFIX-09: dev smoke + changelog) ---
    - "A bounded dev retag (`--db dev` with `--limit`) runs to completion, writes a `reports/retag/retag-*.md` with all four bucket columns, and its per-motif deltas are compared against the review's simulation (sacrifice roughly -90%, clearance roughly -40% or suppressed, tier-2 motifs no worse than -5%) with every deviation explained."
    - "The dev §2.1 acceptance query is re-run after the dev retag and its before/after losing-line shares per motif are recorded; the count of flaws carrying the `[]` PV-blob sentinel is recorded so the size of the D-03 interpretation's exposure is visible."
    - "`CHANGELOG.md` carries a user-facing bullet under `## [Unreleased]` describing the tactic-tag accuracy improvement in product language."
    - "The full CLAUDE.md pre-merge gate passes, INCLUDING `uv run pytest tests/scripts/tagger -q` and, when clearance was suppressed, the frontend leg `npm run lint && npm test -- --run && npm run build`."
    # --- edge-probe derived (TAGFIX-04 bar) ---
    - "boundary: a measured clearance real-share of exactly 0.80 KEEPS the motif; 0.79 suppresses it (the comparison is `>= 0.8`, as D-07 words it). The measured value and the branch taken are both recorded."
    - "empty: a clearance surviving-row count below `REALGAME_MIN_ROWS_FOR_FLOOR` is treated as too thin to clear the bar and takes the suppress branch, with that reason recorded — a 1-of-1 real row is not a passing measurement."
  prohibitions:
    - statement: "MUST NOT adjust a label in `fixtures/tagger/realgame_tags.csv`, a `REALGAME_REAL_SHARE_FLOOR` value, or the 0.8 bar in order to change which D-07 branch is taken. The measured number decides; both outcomes are recorded with numbers."
      status: unverified
      verification: flagged
    - statement: "MUST NOT re-run `scripts/research/sample_realgame_tags.py`. The sample was frozen before any detector edit (D-13) and re-drawing it now would score before and after on different inputs."
      status: unverified
      verification: flagged
    - statement: "MUST NOT regenerate `fixtures/tagger/detector_fixture_train.csv` or `detector_fixture_test.csv`; the final floors are measured against the committed CSVs."
      status: unverified
      verification: flagged
    - statement: "MUST NOT lower any `PRECISION_FLOOR` value to make the final gate green."
      status: unverified
      verification: flagged
    - statement: "MUST NOT issue any statement against the prod database in this plan; the smoke and the acceptance query both run against `--db dev`."
      status: unverified
      verification: flagged
    - statement: "MUST NOT run `bin/reset_db.sh` or gate any step on a clean dev database; the smoke runs against the existing dev data with a bounded `--limit`."
      status: unverified
      verification: flagged
    - statement: "MUST NOT delete `TacticMotifInt.CLEARANCE` (15), `_INT_TO_MOTIF[15]` or `detect_clearance`, even on the suppress branch — persisted rows reference the int until the prod retag overwrites them."
      status: unverified
      verification: flagged
  artifacts:
    - tests/scripts/tagger/precision_floors.py
    - CHANGELOG.md
    - reports/tactic-tagger/tactic-tagger-YYYY-MM-DD.md
    - reports/retag/retag-YYYY-MM-DD.md
  key_links:
    - "`scripts/research/oracle_compare.py` -> the local lichess-puzzler clone (the SC1 parity instrument; analysis-only, never in CI)"
    - "`tests/scripts/tagger/precision_floors.py::REALGAME_REAL_SHARE_FLOOR` -> `test_realgame_real_share_floor` (the never-regress gate whose values this plan finalises)"
    - "`app/repositories/library_repository.py::FAMILY_TO_MOTIF_INTS` <-> `frontend/src/lib/tacticComparisonMeta.ts` (a string-for-string cross-stack contract asserted by `test_family_mapping_ten_families`; the suppress branch must change both sides in one commit)"
    - "`scripts/retag_flaws.py --db dev --limit N` -> `reports/retag/retag-*.md` (the dev smoke that proves the writing-run report and the four buckets before any prod write)"
---

<objective>
Close the phase's measurement loop: prove cook parity, finalise the never-regress floors,
execute exactly one branch of the clearance decision on a recorded number, smoke the retag on
dev, and land the changelog entry so the release can be cut.

Purpose: every code change is in. What is missing is the evidence that the changes did what
the review predicted (success criteria 1-3), the decision D-07 deferred to a measurement, and
the two artifacts a release needs — a green pre-merge gate and a `[Unreleased]` bullet. This
is also the last chance to catch a regression before a 3.18M-row prod write.

Output: the oracle divergence table, the final floors, one executed clearance branch, a dev
retag delta compared against the review's simulation, and a changelog entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-CONTEXT.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-RESEARCH.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-01-SUMMARY.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-02-SUMMARY.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-03-SUMMARY.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-04-SUMMARY.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-05-SUMMARY.md
@.claude/skills/tactic-tagger-report/SKILL.md
@CLAUDE.md
@frontend/CLAUDE.md
</context>

## Artifacts this phase produces

Phase-wide roster with per-plan attribution: `221-01-PLAN.md` § Artifacts this phase
produces. This plan's share:

| Kind | Symbol / path | Status |
|---|---|---|
| Floors finalised | `REALGAME_REAL_SHARE_FLOOR` post-fix values; every changed `PRECISION_FLOOR` comment | changed |
| Report | `reports/tactic-tagger/tactic-tagger-YYYY-MM-DD.md` (final, with the real-game section) | new |
| Report | `reports/retag/retag-YYYY-MM-DD.md` (dev smoke, four bucket columns) | new |
| Doc | `CHANGELOG.md` `[Unreleased]` bullet | new |
| Conditional (SUPPRESS branch only) | `SUPPRESSED_MOTIFS + "clearance"`; `clearance` `PRECISION_FLOOR` and `REALGAME_REAL_SHARE_FLOOR` entries commented out; `clearance` tuple removed from `_TIER3_REGISTRY`; `clearance` family removed from `FAMILY_TO_MOTIF_INTS`; `test_family_mapping_ten_families` expected-key set and docstring count; `test_family_mapping_excludes_suppressed_tier3` suppressed-int set; `TacticFamily` union, `TACTIC_FAMILY_COLORS`, `TACTIC_FAMILY_ICON` and the Advanced-group entry in `tacticComparisonMeta.ts`; the `clearance` copy string in `tacticMotifDefinitions.ts`; plan 02's registry-exclusion test extended to cover int 15 | conditional |
| Kept on both branches | `TacticMotifInt.CLEARANCE = 15`, `_INT_TO_MOTIF[15]`, `detect_clearance` | unchanged |

## The D-07 branch contract

Both branches are fully specified here so neither needs a replan. The input is two numbers
recorded by plan 05: the measured clearance real-game `real_share` and its `surviving` row
count.

**KEEP** when `real_share >= 0.80` AND `surviving >= REALGAME_MIN_ROWS_FOR_FLOOR`. No
suppression touchpoint is edited. `clearance` keeps its family mapping, its frontend chip, its
`PRECISION_FLOOR` entry and its `REALGAME_REAL_SHARE_FLOOR` entry (re-seeded from the post-fix
measurement like every other motif). Record the number that justified the keep.

**SUPPRESS** otherwise — including when the surviving count is below the minimum, because a
handful of rows is not a passing measurement. Eight touchpoints, all in one commit:

1. `tests/scripts/tagger/precision_floors.py` — add `"clearance"` to `SUPPRESSED_MOTIFS` with
   a dated inline comment inside the set body (the file's convention for recording membership
   changes without deleting lines), and comment out its `PRECISION_FLOOR` and
   `REALGAME_REAL_SHARE_FLOOR` entries with the measured real-share and the D-07 reference.
2. `app/services/tactic_detector.py` — remove the `("clearance", TacticMotifInt.CLEARANCE)`
   tuple from `_TIER3_REGISTRY`, exactly as plan 02 removed `self-interference`. This is what
   makes D-07's "retag clears rows" true: the dispatcher iterates the registry, so with the
   tuple gone no PV can produce int 15 and the full refresh overwrites every persisted 15.
   KEEP the enum member, `_INT_TO_MOTIF[15]`, `_MOTIF_TO_INT`, `detect_clearance` and the
   `_TIER3_DETECTOR_FNS` entry; add a comment at the registry recording the removal, the
   measured number and the plan. Note that plan 04's derived `_MOTIF_FLOOR_CP` loses its
   clearance entry automatically, which is the derivation behaving correctly — an
   undispatchable motif needs no floor.
3. `tests/services/test_tactic_detector.py` — extend plan 02's
   `test_tier3_registry_excludes_self_interference` to assert both int 14 and int 15 are absent
   from `_TIER3_REGISTRY` while both remain encodable, decodable and directly callable; rename
   it to reflect that it now covers a set of undispatchable ints.
4. `app/repositories/library_repository.py` — delete the `"clearance"` family entry.
5. `tests/services/test_tactic_comparison_service.py::test_family_mapping_ten_families` —
   remove `"clearance"` from `expected_keys` and update the docstring's key count from 20 to
   19, recording why.
6. `tests/services/test_tactic_comparison_service.py::test_family_mapping_excludes_suppressed_tier3`
   — change `suppressed_tier3_ints` from a single-int set to `{14, 15}` and update the
   docstring.
7. `frontend/src/lib/tacticComparisonMeta.ts` — remove `'clearance'` from the `TacticFamily`
   union, from `TACTIC_FAMILY_COLORS`, from `TACTIC_FAMILY_ICON`, and remove the Advanced-group
   entry; update the doc comment that names the family count and lists the Advanced display
   order (that comment is already stale at 20, so fix it to the true post-change count).
8. `frontend/src/lib/tacticMotifDefinitions.ts` — remove the `clearance` copy string.

Then grep for the now-unused imports and constants (`DoorOpen`, `TAC_CLEARANCE`,
`TAC_CLEARANCE_BG`) and remove them. `npm run lint` catches unused imports but does NOT
type-check, so `npm run build` is mandatory on this branch — a missed usage elsewhere in the
frontend surfaces only there. No generated file drifts: `frontend/src/generated/` carries no
tactic families and no `scripts/gen_*.py` emits one, so the CI drift gate is not in play.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| the local AGPL lichess-puzzler clone -> the parity measurement | An external oracle consulted at analysis time only |
| a measured number -> a shipped product decision | The D-07 bar; the risk is the measurement being bent to fit a preferred answer |
| dev database -> a bounded write run | The smoke performs real UPDATEs, on dev only |
| backend family map <-> frontend family union | A string-for-string cross-stack contract that breaks the UI if the two sides diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-221-27 | Tampering (measurement integrity) | the D-07 bar | high | mitigate | The branch predicate is stated as an inequality over two numbers recorded by plan 05 before this plan ran; the prohibition block forbids editing a label, a floor or the bar to change the outcome; both branches are pre-specified so neither is cheaper to reach; the measured number and the branch taken are both recorded in the SUMMARY. |
| T-221-28 | Tampering (data integrity) | cross-stack family contract | high | mitigate | On the suppress branch all eight touchpoints land in one commit, `test_family_mapping_ten_families` asserts exact key-set equality, and `npm run build` type-checks the frontend union (`npm run lint` and `npm test` do not type-check — memory note `frontend-run-tsc-build`). |
| T-221-29 | Tampering | wrong `--db` on the smoke | high | mitigate | Every command in this plan passes `--db dev` explicitly; `--db` is `required=True` with no default; the resolved target is logged before the first page; the smoke is bounded by `--limit`. |
| T-221-30 | Denial of service | the dev smoke's runtime | medium | mitigate | The smoke is bounded by `--limit` and `--workers`, sized to finish well inside a single tool call; a full unbounded dev retag is deliberately NOT run here (memory note `executor-sse-timeout-long-plans`: long DB-heavy runs die in a subagent). |
| T-221-31 | Tampering (legal/compliance) | AGPL boundary during the parity run | high | mitigate | `oracle_compare.py` imports the clone by path and exits cleanly when absent; it is not in CI; no cook source is copied; only the divergence COUNTS are pasted into the SUMMARY, never cook code. |
| T-221-32 | Information disclosure | committed reports | low | accept | `reports/tactic-tagger/*` contains motif names, counts and fixture ids; `reports/retag/*` contains motif names and counts. No PII, no usernames. |
| T-221-SC | Tampering | package-manager installs | low | accept | No package install in this plan. The suppress branch touches TypeScript source only; `npm ci` is not run and `package-lock.json` is untouched, so the package-legitimacy gate has no input. |
</threat_model>

<tasks>

<task type="auto">
  <name>T-221-06-01: Oracle parity (SC1) and the final floors</name>
  <precondition>The local lichess-puzzler clone directory named by `_PUZZLER_CLONE` in `scripts/research/oracle_compare.py` exists and is a directory — without it the script exits 0 by design and produces no parity table, so SC1 cannot be evaluated.</precondition>
  <read_first>
    - `.planning/phases/221-.../221-02-SUMMARY.md`, `221-04-SUMMARY.md`, `221-05-SUMMARY.md` (every measured number this task consolidates)
    - `.planning/phases/221-.../221-01-SUMMARY.md` (the frozen BEFORE real-game table — the comparison basis)
    - `.planning/phases/221-.../221-RESEARCH.md` § Measured Simulation, § Pitfall 6, § Pitfall 10
    - `reports/tactic-tagger/tactic-tagger-review-2026-09-12.md` § 4 (the oracle divergence table the parity claim is measured against)
    - `scripts/research/oracle_compare.py` (whole file — how to read its output)
    - `tests/scripts/tagger/precision_floors.py` (`PRECISION_FLOOR`, `REALGAME_REAL_SHARE_FLOOR`, `REALGAME_MIN_ROWS_FOR_FLOOR` and the comment conventions)
    - `.planning/ROADMAP.md` § Phase 221 success criteria 1 and 2 (the exact claims being evaluated)
  </read_first>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <action>
**Run the oracle comparison and evaluate SC1.** Execute
`uv run python scripts/research/oracle_compare.py` and read its divergence table. Success
criterion 1 requires: 0 "cook only" rows for deflection, 0 for fork, 0 for trapped-piece, and
0 "ours only" rows for discovered-attack. Paste the full table into the SUMMARY. For every
remaining non-zero cell, classify it as one of the two documented deliberate divergences
(sacrifice's D-05 persistence plus D-06 cap; clearance's D-06 cap plus D-07 clauses) with its
row count, or as an unexplained residual. An unexplained residual is a finding: record it with
the motif, the direction and the count, and state plainly whether it blocks the release rather
than rounding it away. If a target cell is non-zero, name which port fix did not land as
intended — that is a plan-02 defect to fix, not a number to reinterpret.

**Finalise `REALGAME_REAL_SHARE_FLOOR`.** Run `uv run pytest tests/scripts/tagger -q` and read
the real-game table. For every motif with at least `REALGAME_MIN_ROWS_FOR_FLOOR` surviving
rows, re-seed its floor at roughly 0.05 below the measured post-fix `real_share`, clamped to
0.0-1.0, and replace its trailing comment with the post-fix measurement (surviving,
real_surviving, suppressed, real_share) and the note that this supersedes plan 01's pre-fix
baseline. A motif whose surviving count dropped below the minimum loses its floor entry and
gains a dated comment saying why. Raising these floors is the never-regress lock D-13 asks for.

**Finalise `PRECISION_FLOOR`.** Consolidate plans 02, 04 and 05: every entry whose TP/FP
counts moved gets its final measured TRAIN and TEST numbers and a one-clause note naming the
change. Raise a floor where precision rose. Never lower one — if a measured precision is
below its floor the gate is telling you a change is wrong, and the fix is in the predicate.

**Evaluate success criterion 2 explicitly, with numbers.** In the SUMMARY, state: sacrifice
real-share before and after and whether it is at or above 0.80; clearance real-share before and
after, its surviving count, and the branch task 2 will therefore take; and a per-motif table
showing that no other motif's real-share fell below its plan-01 frozen BEFORE value. Name any
motif that did fall, with the count and the likely cause — a real-share regression on a control
motif (fork, hanging-piece, mate) is a signal that one of the fixes over-reached.

Write the final report with `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py` and
record its path.
  </action>
  <acceptance_criteria>
    - `uv run python scripts/research/oracle_compare.py` output is pasted into the SUMMARY, showing 0 cook-only rows for deflection, fork and trapped-piece and 0 ours-only rows for discovered-attack; every remaining non-zero cell is classified as a named deliberate divergence with a count, or explicitly flagged as an unexplained residual.
    - `uv run pytest tests/scripts/tagger -q` passes against the RAISED `REALGAME_REAL_SHARE_FLOOR` values; lowering any one measured real-share by hand in a scratch run makes the gate fail with that motif named (fail-first proof, then revert).
    - `git diff "$(git merge-base HEAD main)"..HEAD -- tests/scripts/tagger/precision_floors.py` shows no `PRECISION_FLOOR` value decreased and every changed entry carrying a new measurement in its comment.
    - Every `REALGAME_REAL_SHARE_FLOOR` key has at least `REALGAME_MIN_ROWS_FOR_FLOOR` surviving rows in the post-fix measurement; any key removed carries a dated reason comment.
    - `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py` exits 0 and the report path plus the real-game section are recorded.
    - The SUMMARY states sacrifice's and clearance's real-share as numbers, the D-07 branch that follows, and a per-motif before/after real-share comparison against plan 01's frozen values.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/scripts/tagger -q</automated>
    <fails_when>non-zero exit, a "TRAIN precision floor(s) not met" line, or a real-game floor failure line</fails_when>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py</automated>
    <fails_when>non-zero exit, or the command printing no `Wrote ` line</fails_when>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or any "error[" line in the ty output</fails_when>
  </verify>
  <done>Oracle parity is measured and every residual divergence is either a named deliberate one or an explicit flagged finding; both floor dictionaries carry their final post-fix measurements with nothing lowered; success criterion 2 is answered with numbers and the D-07 branch is determined.</done>
</task>

<task type="auto">
  <name>T-221-06-02: Execute exactly one branch of the clearance decision (D-07 / TAGFIX-04)</name>
  <reversibility rating="costly">The SUPPRESS branch removes a shipped tactic family from the backend map and the frontend type union, and — via the `_TIER3_REGISTRY` removal — makes the prod retag overwrite every persisted clearance row. Reverting after the prod retag means restoring eight touchpoints AND running the full retag again. D-07 locked the decision rule and the bar, so the number decides and no checkpoint re-asks it; the rating is recorded for the reader.</reversibility>
  <read_first>
    - This plan's `## The D-07 branch contract` section (the eight suppression touchpoints, in order)
    - `.planning/phases/221-.../221-RESEARCH.md` § Clearance suppression checklist (only if D-07's bar is missed) — the grep-verified touchpoint list and the "no generated file drifts" finding
    - `.planning/phases/221-.../221-05-SUMMARY.md` (the measured clearance real-share and surviving count — the branch input)
    - `tests/scripts/tagger/precision_floors.py` lines ~195-250 (`SUPPRESSED_MOTIFS` and its dated-inline-comment convention; the Phase-134 precedent recorded there)
    - `app/services/tactic_detector.py` lines 2373-2400 (`_TIER3_REGISTRY`, and plan 02's removal comment as the precedent to mirror)
    - `app/repositories/library_repository.py` lines 165-180 (the `clearance` family entry)
    - `tests/services/test_tactic_comparison_service.py` lines 155-220 (both family tests, their docstrings and their exact assertions)
    - `frontend/src/lib/tacticComparisonMeta.ts` lines 115-125 (the stale family-count doc comment), 135-145 (`TacticFamily`), 165-180 (`TACTIC_FAMILY_COLORS`), 190-205 (`TACTIC_FAMILY_ICON`), 375-392 (the Advanced-group entries)
    - `frontend/src/lib/tacticMotifDefinitions.ts` line 44 (the `clearance` copy string)
    - `frontend/CLAUDE.md` (frontend rules; and the standing memory note that `npm run lint` plus `npm test` do NOT type-check — `npm run build` is required)
  </read_first>
  <files>tests/scripts/tagger/precision_floors.py, app/services/tactic_detector.py, tests/services/test_tactic_detector.py, app/repositories/library_repository.py, tests/services/test_tactic_comparison_service.py, frontend/src/lib/tacticComparisonMeta.ts, frontend/src/lib/tacticMotifDefinitions.ts</files>
  <action>
Read the two numbers plan 05 recorded — clearance's measured real-game `real_share` and its
`surviving` count — and execute exactly one branch of this plan's `## The D-07 branch
contract`. Do not re-derive the numbers, do not re-measure to get a different answer, and do
not touch a label or a floor to move the bar.

**KEEP branch** (`real_share >= 0.80` AND `surviving >= REALGAME_MIN_ROWS_FOR_FLOOR`): edit
nothing in this task beyond recording the decision. State in the SUMMARY the measured
real-share, the surviving count, and that clearance ships as a family with its floors
finalised by task 1. Then skip to the verification.

**SUPPRESS branch** (otherwise, including a surviving count below the minimum): work the eight
touchpoints in the order listed in the branch contract, in ONE commit, so the cross-stack
family contract is never momentarily inconsistent. The four load-bearing details:

- The `_TIER3_REGISTRY` tuple removal is what makes D-07's "retag clears rows" true, and it is
  the same mechanism plan 02 used for `self-interference`. Everything else about int 15 stays:
  the enum member, `_INT_TO_MOTIF[15]`, `_MOTIF_TO_INT`, `detect_clearance` and its
  `_TIER3_DETECTOR_FNS` entry, because persisted rows reference the int until the prod retag
  overwrites them and three existing tests pin the encoding.
- `test_family_mapping_ten_families` asserts exact set equality on 20 keys and states the count
  in its docstring; both must change together or the test lies about what it checks.
- `test_family_mapping_excludes_suppressed_tier3` currently asserts a single-int suppressed set;
  it becomes a two-int set, and its docstring must say why 15 joined 14.
- The frontend has four sites plus a copy string plus a stale count comment. After editing,
  grep for `DoorOpen`, `TAC_CLEARANCE` and `TAC_CLEARANCE_BG` and remove whatever is now
  unused. `npm run lint` finds unused imports; it does NOT type-check, so run `npm run build`
  — a `TacticFamily` usage elsewhere in the frontend surfaces only there.

On EITHER branch, record in the SUMMARY: the measured number, the bar, the branch taken, and —
for the suppress branch — the list of touchpoints actually edited, checked against the eight in
the contract. A partially-executed suppression is the failure mode this checklist exists to
prevent.
  </action>
  <acceptance_criteria>
    - The SUMMARY states the measured clearance `real_share`, the `surviving` count, the bar (`>= 0.80` and `>= REALGAME_MIN_ROWS_FOR_FLOOR`) and the branch taken, before any edit is described.
    - `uv run pytest tests/services/test_tactic_comparison_service.py tests/services/test_tactic_detector.py -q` passes.
    - `uv run pytest tests/scripts/tagger -q` passes.
    - KEEP branch: the commit this task produces touches no file other than a SUMMARY — record its sha as `$TASK_SHA` and verify with `git diff --name-only "$TASK_SHA^".."$TASK_SHA"`; `uv run python -c "import sys;sys.path.insert(0,'.');from app.repositories.library_repository import FAMILY_TO_MOTIF_INTS as F;assert 'clearance' in F"` exits 0.
    - SUPPRESS branch: `uv run python -c "import sys;sys.path.insert(0,'.');from app.services.tactic_detector import _TIER3_REGISTRY as R, TacticMotifInt as T, _INT_TO_MOTIF as M, detect_clearance;ints={int(i) for _,i in R};assert int(T.CLEARANCE) not in ints;assert M[15]=='clearance';assert callable(detect_clearance);from app.repositories.library_repository import FAMILY_TO_MOTIF_INTS as F;assert 'clearance' not in F"` exits 0 (structural, over the registry tuple set and the family dict keys, not over source text); `test "$(grep -c "family: 'clearance'" frontend/src/lib/tacticComparisonMeta.ts)" -eq 0` exits 0 — the Advanced-group entry is gone, and the literal `family: 'clearance'` appears in no prose in this plan so a historical comment cannot satisfy it; `npm --prefix frontend run build` exits 0; and all eight touchpoints appear in `git diff --name-only "$TASK_SHA^".."$TASK_SHA"`, where `$TASK_SHA` is the sha of the single commit this task produces and is recorded in the SUMMARY.
    - SUPPRESS branch: `npm --prefix frontend run lint` and `npm --prefix frontend test -- --run` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_comparison_service.py tests/services/test_tactic_detector.py -q &amp;&amp; uv run pytest tests/scripts/tagger -q</automated>
    <fails_when>non-zero exit, a "TRAIN precision floor(s) not met" line, or an AssertionError naming `expected_keys` or `suppressed_tier3_ints`</fails_when>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or any "error[" line in the ty output</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output. On the KEEP branch this command is still run as a no-regression check and must pass unchanged.</fails_when>
  </verify>
  <done>Exactly one D-07 branch is executed, justified by a number recorded before the edit; on the suppress branch all eight touchpoints changed in one commit with the backend/frontend family contract still asserted green and the frontend type-checked; int 15 remains encodable, decodable and callable either way.</done>
</task>

<task type="auto">
  <name>T-221-06-03: Dev retag smoke, dev acceptance query, changelog, and the full pre-merge gate</name>
  <precondition>The dev Postgres container is up (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`) and holds the existing dev flaw corpus — the smoke runs against it with a bounded `--limit` and must not be gated on a fresh database.</precondition>
  <read_first>
    - `.planning/phases/221-.../221-RESEARCH.md` § The acceptance instrument (§2.1) with the dev baseline table measured 2026-09-12, § Dev orientation asymmetry, § Pitfall 10 (dev numbers will not match prod), § Implementation Map 9
    - `.planning/phases/221-.../221-03-SUMMARY.md` (the measured per-page cost, the `FLAWS_PER_BATCH` value in force, and the new four-column report sample)
    - `.planning/ROADMAP.md` § Phase 221 success criterion 3 and TAGFIX-09
    - `scripts/retag_flaws.py` lines 40-100 (the usage block and the corrected tunnel paragraph), lines 170-250 (every flag)
    - `CHANGELOG.md` (the `## [Unreleased]` section and the surrounding bullet style)
    - `CLAUDE.md` § Pre-merge gate (the exact command list) and § Version Control
    - `.claude/skills/db-report/SKILL.md` (only for the dev connection convention used to run the acceptance SQL)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
**Run the dev retag smoke, bounded.** Execute `uv run python scripts/retag_flaws.py --db dev`
with a `--limit` and a `--workers` value sized to finish comfortably inside one tool call —
start from the throughput plan 03 measured and pick a limit that lands under about five
minutes. Run it WITHOUT `--dry-run` so the writing-run report path is exercised (that is the
TAGFIX-09 code change plan 03 made), and WITHOUT `--only-tagged` so it behaves like the prod
full refresh. Do NOT background the run and do not attempt a full unbounded dev retag here: a
long DB-heavy run inside an executor is lost when the agent returns.

Read the generated `reports/retag/retag-*.md` and compare its per-motif deltas against the
review's simulation, stating each comparison as a number: sacrifice roughly -90%, clearance
roughly -40% or fully cleared if suppressed, tier-2 motifs no worse than -5%. Explain every
deviation beyond those bands — and expect the sacrifice cut to differ from the review's dev
simulation, because the TRAIN fixture's short lines make D-05's line-ends branch fire often
(RESEARCH Open Question 3 measured 177 survivors of 409 on TRAIN against the review's 54 of
697 on dev). If the cut is materially too soft, record it as a finding and name
`SACRIFICE_CLEARANCE_MAX_DEPTH` as the lever — do NOT relax D-05.

Spot-check survivors. Sample a handful of surviving sacrifice rows and a handful of surviving
clearance rows (or, if clearance was suppressed, a handful of cleared ones) from the dev
database, render their PV lines, and state in the SUMMARY whether they read as real tactics.
The reference set for "what a good sacrifice tag looks like" is in CONTEXT § Specific Ideas:
queen and exchange sacrifices into mate, `Bxh6 gxh6 Rd1`, `Rh3` queen traps.

**Re-run the dev §2.1 acceptance query** (the SQL in RESEARCH § The acceptance instrument,
verified runnable on dev) for both orientations, and record a before/after table of
`losing_pct` per motif against the dev baseline already measured on 2026-09-12 (sacribe 79.1%,
clearance 46.8%, intermezzo 25.8%, x-ray 57.1%, under-promotion 80.0%). Also record two
diagnostics the phase needs: the count of dev flaws whose PV blob is the `[]` sentinel (the
size of the D-03 interpretation's exposure, flagged in plan 01) and the allowed-versus-missed
counts for intermezzo and hanging-piece. State clearly that dev is directionally comparable to
prod and never numerically equal, because `eval_cp` is not reproducible across machines.

**Write the changelog bullet.** Add one user-facing bullet under `## [Unreleased]` in
`CHANGELOG.md`, in product language, describing that tactic tags now only appear when the
tagging side is actually winning where the tactic fires, that sacrifice and clearance tags are
much stricter, that missed-tactic detection reaches lines it previously could not, and — if
clearance was suppressed — that the clearance chip is retired. No requirement IDs, no file
paths, no em-dashes beyond the project's sparing-use rule.

**Run the full CLAUDE.md pre-merge gate** for the whole phase, in order, resolving every
output: `uv run ruff format app/ tests/ scripts/ analysis/`; `uv run ruff check . --fix`;
`uv run ty check app/ tests/ scripts/`;
`uv run --project analysis --with ty ty check analysis/`;
`uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200`;
`uv run pytest -n auto -x`; and — the addition this phase requires, because
`pyproject.toml` excludes it from the default run — `uv run pytest tests/scripts/tagger -q`.
Run the frontend leg (`npm --prefix frontend run lint`,
`npm --prefix frontend test -- --run`, `npm --prefix frontend run build`) unconditionally; it
is mandatory if clearance was suppressed and a cheap no-regression check otherwise. If any
step modifies files, commit with a `style(...)` or `chore(...)` prefix.

State in the SUMMARY that the phase is ready to squash-merge to `main`, and that the prod
retag is plan 07's work, gated behind the deploy checkpoint.
  </action>
  <acceptance_criteria>
    - A `reports/retag/retag-*.md` file exists from a WRITING dev run, with a `Mode:` line indicating rows were written and per-motif columns for previously tagged, gate suppressed, survived, motif shifted and depth shifted.
    - The SUMMARY states the measured per-motif dev delta percentages against the review's simulation bands (sacrifice, clearance, and each tier-2 motif), with every out-of-band deviation explained.
    - The SUMMARY carries the dev §2.1 before/after `losing_pct` table for both orientations, the `[]`-sentinel flaw count, and the allowed-versus-missed counts for intermezzo and hanging-piece.
    - The SUMMARY records the survivor spot-check with at least three sacrifice examples rendered as SAN lines and a verdict on each.
    - `grep -n "tactic" CHANGELOG.md` shows a new bullet under `## [Unreleased]`.
    - Every pre-merge-gate command exits 0, including `uv run pytest tests/scripts/tagger -q` and `uv run --project analysis --with ty ty check analysis/`; the frontend leg exits 0.
    - `git status --porcelain` is clean at the end of the task (any formatter-modified file was committed).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -n auto -x &amp;&amp; uv run pytest tests/scripts/tagger -q</automated>
    <fails_when>non-zero exit, or either summary line reporting a failure or error</fails_when>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check app/ tests/ scripts/ analysis/ &amp;&amp; uv run ty check app/ tests/ scripts/ &amp;&amp; uv run --project analysis --with ty ty check analysis/ &amp;&amp; uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, any "error[" line in either ty output, a `Would reformat:` line from ruff, or a breach line from the function-size script</fails_when>
    <automated>npm --prefix frontend run lint &amp;&amp; npm --prefix frontend test -- --run &amp;&amp; npm --prefix frontend run build</automated>
    <fails_when>non-zero exit, any line containing "error TS", or the vitest summary reporting a failed test</fails_when>
    <automated>test "$(grep -c 'tactic' CHANGELOG.md)" -gt 0 &amp;&amp; ls reports/retag/retag-*.md</automated>
    <fails_when>non-zero exit — the changelog bullet is missing, or no dev retag report was produced</fails_when>
  </verify>
  <done>A bounded writing dev retag produced a four-bucket report whose deltas are compared to the review's simulation with every deviation explained; the dev acceptance query, the `[]`-sentinel count and the orientation ratios are recorded; the changelog bullet is in; the full pre-merge gate including the tagger and frontend legs is green.</done>
</task>

</tasks>

<verification>
- `uv run python scripts/research/oracle_compare.py` — SC1 parity; requires the local clone.
- `uv run pytest tests/scripts/tagger -q` — the CC0 gate plus the real-game gate at final
  floors.
- `uv run pytest -n auto -x` — the full backend suite.
- `uv run ruff check .`, `uv run ruff format --check app/ tests/ scripts/ analysis/`,
  `uv run ty check app/ tests/ scripts/`,
  `uv run --project analysis --with ty ty check analysis/`,
  `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200`.
- `npm --prefix frontend run lint`, `npm --prefix frontend test -- --run`,
  `npm --prefix frontend run build`.
- `uv run python scripts/retag_flaws.py --db dev --limit <N>` — a WRITING run that produces a
  four-bucket report.
- The dev §2.1 acceptance SQL, both orientations.
</verification>

<success_criteria>
- Oracle parity measured: 0 cook-only for deflection/fork/trapped-piece, 0 ours-only for
  discovered-attack, every residual divergence named as deliberate or flagged as a finding.
- Both floor dictionaries carry final post-fix measurements; nothing was lowered anywhere in
  the phase; the real-game gate is fail-first proven at the new values.
- Exactly one D-07 branch executed, justified by a number recorded before the edit, with all
  eight touchpoints changed in one commit on the suppress branch and the frontend type-checked.
- A bounded writing dev retag produced a four-bucket report; its deltas are compared to the
  review's simulation and every deviation is explained; survivors were spot-checked.
- The dev acceptance table, the `[]`-sentinel count and the orientation ratios are recorded.
- `CHANGELOG.md` `[Unreleased]` carries a user-facing bullet.
- The full pre-merge gate, including the tagger and frontend legs, is green and the tree is
  clean.
</success_criteria>

<output>
Create `.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-06-SUMMARY.md` when done.

Record in it: the full oracle divergence table with every residual classified; the final
`PRECISION_FLOOR` and `REALGAME_REAL_SHARE_FLOOR` values with their measurements; the success
criterion 2 evaluation per motif against plan 01's frozen BEFORE values; the clearance number,
the bar, the branch taken and (if suppressed) the eight touchpoints checked off; the dev retag
report excerpt and the simulation-band comparison; the dev §2.1 before/after `losing_pct`
table for both orientations; the `[]`-sentinel flaw count; the intermezzo and hanging-piece
allowed-versus-missed counts; the survivor spot-check SAN lines and verdicts; the changelog
bullet text; and an explicit statement that the phase is ready to squash-merge and that the
prod retag is gated behind plan 07's deploy checkpoint.
</output>
