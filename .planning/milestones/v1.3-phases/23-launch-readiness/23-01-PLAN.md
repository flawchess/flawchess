---
phase: 23-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/PublicHeader.tsx
  - frontend/src/pages/Home.tsx
  - frontend/src/components/ui/accordion.tsx
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "Unauthenticated visitors see the public homepage at /"
    - "Authenticated users are redirected from / to /openings"
    - "Homepage shows tagline, 5 feature sections, FAQ, and Sign up free CTA"
    - "PublicHeader shows FlawChess logo, Log in button, and Sign up free button"
    - "FAQ accordion expands/collapses with all 5 questions from D-08"
  artifacts:
    - path: "frontend/src/pages/Home.tsx"
      provides: "Public homepage with hero, features, FAQ, footer CTA"
      min_lines: 100
    - path: "frontend/src/components/layout/PublicHeader.tsx"
      provides: "Header for public pages with logo + login/signup"
      exports: ["PublicHeader"]
    - path: "frontend/src/components/ui/accordion.tsx"
      provides: "shadcn Accordion component for FAQ"
    - path: "frontend/src/App.tsx"
      provides: "Restructured routes with / outside ProtectedLayout"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Home.tsx"
      via: "Route element"
      pattern: '<Route path="/" element={<HomePage'
    - from: "frontend/src/pages/Home.tsx"
      to: "frontend/src/hooks/useAuth.ts"
      via: "useAuth().token check for redirect"
      pattern: "useAuth.*token"
---

<objective>
Create the public homepage at `/` with routing restructure, PublicHeader component, and full homepage content including hero section, 5 feature blocks, FAQ accordion, and CTA.

Purpose: Unauthenticated visitors need a landing page that explains what FlawChess is and drives sign-ups. Currently `/` is protected and redirects to login.
Output: Working homepage at `/` with all content per D-01 through D-08.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-launch-readiness/23-CONTEXT.md
@.planning/phases/23-launch-readiness/23-RESEARCH.md
@.planning/phases/23-launch-readiness/23-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/hooks/useAuth.ts:
```typescript
export function useAuth(): AuthState;
// AuthState includes: token: string | null, login, logout, etc.
```

From frontend/src/lib/theme.ts:
```typescript
export const PRIMARY_BUTTON_CLASS = 'bg-[#8B5E3C] hover:bg-[#6B4226] text-white';
```

From frontend/src/components/ui/button.tsx:
```typescript
// shadcn Button with variant="ghost", size="sm", size="lg", asChild props
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure routing and create PublicHeader</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/hooks/useAuth.ts
    - frontend/src/lib/theme.ts
    - .planning/phases/23-launch-readiness/23-UI-SPEC.md
    - .planning/phases/23-launch-readiness/23-CONTEXT.md
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/components/layout/PublicHeader.tsx</files>
  <action>
1. Install shadcn Accordion component (needed for homepage FAQ in Task 2):
   ```bash
   cd frontend && npx shadcn@latest add accordion
   ```

2. Create `frontend/src/components/layout/PublicHeader.tsx`:
   - Export `PublicHeader` component
   - Sticky header with `border-b border-border bg-background px-6`
   - Inner container: `mx-auto flex max-w-7xl items-center justify-between py-2`
   - Left: `<Link to="/">` with `<img src="/icons/logo-128.png" alt="" className="h-10 w-10" aria-hidden="true" />` and `<span className="text-lg tracking-tight font-brand">FlawChess</span>` with `gap-1.5`
   - Right: two buttons in `flex items-center gap-2`:
     - "Log in": `<Button variant="ghost" size="sm" asChild data-testid="nav-login"><Link to="/login">Log in</Link></Button>`
     - "Sign up free": `<Button size="sm" asChild className={PRIMARY_BUTTON_CLASS} data-testid="nav-signup"><Link to="/login?tab=register">Sign up free</Link></Button>`
   - Import `PRIMARY_BUTTON_CLASS` from `@/lib/theme`
   - `data-testid="public-header"` on the outer `<header>`

3. Modify `frontend/src/App.tsx`:
   - Add imports for `HomePage` (from `@/pages/Home`) and `PrivacyPage` (from `@/pages/Privacy` — will be created in Plan 03, just add the import and route now with a placeholder inline component `function PrivacyPage() { return <div>Privacy Policy</div>; }` that will be replaced)
   - Move `/` route OUTSIDE `ProtectedLayout`: `<Route path="/" element={<HomePage />} />`
   - Add `/privacy` route outside ProtectedLayout: `<Route path="/privacy" element={<PrivacyPage />} />`
   - Keep `/login` and `/auth/callback` routes outside ProtectedLayout (already there)
   - Remove `HomeRedirect` component entirely (no longer used)
   - Remove the `<Route path="/" element={<HomeRedirect />} />` from inside ProtectedLayout
   - Change catch-all from `<Navigate to="/openings" replace />` to `<Navigate to="/" replace />` (per Research Pitfall 3)
   - Keep all other ProtectedLayout routes unchanged (/import, /openings/*, /rating, /global-stats)
   - Remove `useUserProfile` import if HomeRedirect was the only consumer (check first)

Route structure after changes:
```tsx
<Routes>
  {/* Public routes */}
  <Route path="/" element={<HomePage />} />
  <Route path="/privacy" element={<PrivacyPage />} />
  <Route path="/login" element={<AuthPage />} />
  <Route path="/auth/callback" element={<OAuthCallbackPage />} />

  {/* Protected routes */}
  <Route element={<ProtectedLayout />}>
    <Route path="/import" element={<ImportPage ... />} />
    <Route path="/openings/*" element={<OpeningsPage />} />
    <Route path="/rating" element={<Navigate to="/global-stats" replace />} />
    <Route path="/global-stats" element={<GlobalStatsPage />} />
  </Route>

  <Route path="*" element={<Navigate to="/" replace />} />
</Routes>
```
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/layout/PublicHeader.tsx exists and contains `export function PublicHeader`
    - frontend/src/components/layout/PublicHeader.tsx contains `data-testid="public-header"`
    - frontend/src/components/layout/PublicHeader.tsx contains `data-testid="nav-login"`
    - frontend/src/components/layout/PublicHeader.tsx contains `data-testid="nav-signup"`
    - frontend/src/components/layout/PublicHeader.tsx contains `PRIMARY_BUTTON_CLASS`
    - frontend/src/App.tsx contains `<Route path="/" element={<HomePage`
    - frontend/src/App.tsx contains `<Route path="/privacy"`
    - frontend/src/App.tsx does NOT contain `HomeRedirect`
    - frontend/src/App.tsx catch-all redirects to `/` not `/openings`: `<Navigate to="/" replace />`
    - frontend/src/components/ui/accordion.tsx exists (shadcn installed)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Public routes exist outside ProtectedLayout. PublicHeader renders logo + login/signup buttons. shadcn Accordion installed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Homepage content</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/components/layout/PublicHeader.tsx
    - frontend/src/lib/theme.ts
    - frontend/src/components/ui/accordion.tsx
    - .planning/phases/23-launch-readiness/23-UI-SPEC.md
    - .planning/phases/23-launch-readiness/23-CONTEXT.md
  </read_first>
  <files>frontend/src/pages/Home.tsx</files>
  <action>
Create `frontend/src/pages/Home.tsx` with the following structure (per D-01 through D-08 and UI-SPEC):

1. **Auth redirect logic** (per D-01):
   ```tsx
   export function HomePage() {
     const { token } = useAuth();
     if (token) return <Navigate to="/openings" replace />;
     return <HomePageContent />;
   }
   ```

2. **HomePageContent** — full page wrapped in `<>` with PublicHeader:
   - Import `PublicHeader` from `@/components/layout/PublicHeader`
   - Import `Button` from `@/components/ui/button`
   - Import `Accordion, AccordionContent, AccordionItem, AccordionTrigger` from `@/components/ui/accordion`
   - Import `PRIMARY_BUTTON_CLASS` from `@/lib/theme`
   - Import `cn` from `@/lib/utils`
   - Import `Link` from `react-router-dom`

3. **Hero section** (`data-testid="hero-section"`):
   - Container: `max-w-3xl mx-auto px-4 py-16 lg:py-24 text-center`
   - Headline: `<h1 className="text-4xl lg:text-5xl font-bold leading-tight font-brand">Engines are flawless, humans play FlawChess</h1>` (per D-06, Display role)
   - Sub-tagline: `<p className="mt-4 text-base leading-relaxed text-muted-foreground">Analyze your opening positions by move, not just name. Import games from chess.com and lichess to discover where you really lose.</p>`
   - CTA: `<Button size="lg" asChild className={cn(PRIMARY_BUTTON_CLASS, 'min-h-11 mt-8')} data-testid="hero-cta-signup"><Link to="/login?tab=register">Sign up free</Link></Button>`
   - Callout pills below CTA: `<div className="mt-4 flex justify-center gap-2">` with two `<span className="bg-muted text-muted-foreground rounded-full px-3 py-1 text-sm">` for "Open source and free" and "Mobile friendly" (per D-07)

4. **Screenshots section** (`data-testid="screenshots-section"`):
   - Container: `max-w-5xl mx-auto px-4 py-12`
   - Placeholder text: `<p className="text-center text-muted-foreground text-sm">Screenshots coming soon</p>`
   - (User will provide manual assets later per D-05)

5. **Feature sections** — 5 blocks inside `<div className="max-w-5xl mx-auto px-4 py-12">`:
   - Use `<div className="grid gap-12 lg:grid-cols-2 lg:gap-8">` for layout
   - Each feature is a `<section data-testid="feature-{slug}">` with:
     - `<h2 className="text-xl font-bold">` (Heading role)
     - `<p className="mt-2 text-base leading-relaxed text-muted-foreground">` (Body role)
   - Feature content (per D-03):
     - slug `weaknesses`: heading "Find weaknesses in your openings", desc "Discover which moves you struggle against, which gambits work for you, and how your repertoire performs at different rating levels."
     - slug `scout`: heading "Scout your opponents", desc "Prepare for a match by exploring their opening weaknesses and tendencies."
     - slug `move-explorer`: heading "Interactive move explorer", desc "Step through any position and see your win/draw/loss rate for every move you've played."
     - slug `cross-platform`: heading "Cross-platform analysis", desc "Import games from chess.com and lichess into one place — analyze your complete history regardless of platform."
     - slug `filters`: heading "Powerful filters", desc "Slice your games by time control, rating range, color, opponent type, and time period to find exactly the patterns you're looking for."

6. **FAQ section** (`data-testid="faq-accordion"` on `<Accordion>`):
   - Container: `<section className="max-w-2xl mx-auto px-4 py-12">`
   - Heading: `<h2 className="text-xl font-bold mb-6">Frequently asked questions</h2>`
   - `<Accordion type="single" collapsible data-testid="faq-accordion">`
   - 5 AccordionItems with `data-testid="faq-item-{slug}"`:
     - `data`: Q "What data do you access from my chess.com or lichess account?" / A "Only your games — no passwords or personal information. Your games are publicly accessible via their APIs, and FlawChess reads them just like any other analysis tool."
     - `free`: Q "Is it free?" / A "Yes, FlawChess is completely free to use."
     - `mobile`: Q "Can I use it on mobile?" / A "Yes. FlawChess is a Progressive Web App — install it from your browser for a native-like experience on iPhone and Android."
     - `requests`: Q "Where can I make feature requests?" / A with link: "Open an issue on <a href='https://github.com/flawchess/flawchess' ...>GitHub</a>. Contributions and feedback are welcome." (use `<a>` with `text-primary underline-offset-4 hover:underline` styling, `target="_blank" rel="noopener noreferrer"`)
     - `who`: Q "Who develops FlawChess?" / A "FlawChess is an open source project developed independently. Find the code, contribute, or reach out at <a href='mailto:support@flawchess.com' ...>support@flawchess.com</a>."

7. **Footer CTA** (`data-testid="footer-cta"`):
   - Container: `<section className="text-center py-16" data-testid="footer-cta">`
   - `<p className="text-muted-foreground mb-4">Free to use. No credit card required.</p>`
   - Same CTA button as hero: `<Button size="lg" asChild className={cn(PRIMARY_BUTTON_CLASS, 'min-h-11')} data-testid="footer-cta-signup"><Link to="/login?tab=register">Sign up free</Link></Button>`

8. **Page footer** (`data-testid="page-footer"`):
   - `<footer className="text-sm text-muted-foreground text-center py-6 border-t border-border" data-testid="page-footer">`
   - Copyright: `© {new Date().getFullYear()} FlawChess`
   - Privacy link: `<Link to="/privacy" className="ml-4 text-primary underline-offset-4 hover:underline">Privacy Policy</Link>`
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Home.tsx exists and contains `export function HomePage`
    - frontend/src/pages/Home.tsx contains `useAuth` for token-based redirect
    - frontend/src/pages/Home.tsx contains `Navigate to="/openings"`
    - frontend/src/pages/Home.tsx contains `data-testid="hero-section"`
    - frontend/src/pages/Home.tsx contains `Engines are flawless, humans play FlawChess`
    - frontend/src/pages/Home.tsx contains `data-testid="hero-cta-signup"`
    - frontend/src/pages/Home.tsx contains `data-testid="screenshots-section"`
    - frontend/src/pages/Home.tsx contains `data-testid="feature-weaknesses"`
    - frontend/src/pages/Home.tsx contains `data-testid="feature-scout"`
    - frontend/src/pages/Home.tsx contains `data-testid="feature-move-explorer"`
    - frontend/src/pages/Home.tsx contains `data-testid="feature-cross-platform"`
    - frontend/src/pages/Home.tsx contains `data-testid="feature-filters"`
    - frontend/src/pages/Home.tsx contains `data-testid="faq-accordion"`
    - frontend/src/pages/Home.tsx contains `data-testid="faq-item-data"`
    - frontend/src/pages/Home.tsx contains `data-testid="faq-item-free"`
    - frontend/src/pages/Home.tsx contains `data-testid="faq-item-mobile"`
    - frontend/src/pages/Home.tsx contains `data-testid="faq-item-requests"`
    - frontend/src/pages/Home.tsx contains `data-testid="faq-item-who"`
    - frontend/src/pages/Home.tsx contains `data-testid="footer-cta"`
    - frontend/src/pages/Home.tsx contains `data-testid="page-footer"`
    - frontend/src/pages/Home.tsx contains `PRIMARY_BUTTON_CLASS`
    - frontend/src/pages/Home.tsx contains `Open source and free`
    - frontend/src/pages/Home.tsx contains `Mobile friendly`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Homepage renders all required content: hero with tagline and CTA, 5 feature sections, FAQ accordion with 5 items, footer CTA, page footer with privacy link. Authenticated users redirect to /openings. Build passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passes
- `grep -c "data-testid" frontend/src/pages/Home.tsx` returns >= 15
- `grep "HomeRedirect" frontend/src/App.tsx` returns no matches
- `grep 'path="/"' frontend/src/App.tsx` shows route outside ProtectedLayout
</verification>

<success_criteria>
- Unauthenticated visitors see the full homepage at /
- Authenticated users are redirected to /openings
- All 5 feature sections rendered with correct content per D-03
- FAQ accordion with 5 items per D-08
- PublicHeader with logo, login, and signup buttons
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-readiness/23-01-SUMMARY.md`
</output>
