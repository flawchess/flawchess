---
phase: 20-rename-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/main.py
  - app/core/config.py
  - app/services/chesscom_client.py
  - app/services/zobrist.py
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/src/App.tsx
  - frontend/src/pages/Auth.tsx
  - frontend/src/components/install/InstallPromptBanner.tsx
  - frontend/src/lib/zobrist.ts
  - tests/conftest.py
  - tests/test_chesscom_client.py
  - .env.example
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
  - frontend/public/icons/apple-touch-icon.png
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "No instance of 'Chessalytics' or 'chessalytics' remains in tracked source/config/doc files (excluding .planning/milestones/, .planning/quick/, .planning/research/)"
    - "PWA manifest shows 'FlawChess' as name and short_name"
    - "Browser tab title reads 'FlawChess'"
    - "App header shows 'FlawChess' on both desktop and mobile"
    - "apple-touch-icon.png exists at frontend/public/icons/"
    - "All existing tests pass after rename"
  artifacts:
    - path: "frontend/public/icons/apple-touch-icon.png"
      provides: "iOS home screen icon placeholder"
    - path: "pyproject.toml"
      contains: 'name = "flawchess"'
    - path: "app/main.py"
      contains: 'title="FlawChess"'
    - path: "frontend/vite.config.ts"
      contains: "name: 'FlawChess'"
    - path: "frontend/index.html"
      contains: "<title>FlawChess</title>"
  key_links:
    - from: "app/services/chesscom_client.py"
      to: "tests/test_chesscom_client.py"
      via: "USER_AGENT constant matched by test assertion"
      pattern: 'FlawChess.*User-Agent'
    - from: "app/core/config.py"
      to: ".env.example"
      via: "DATABASE_URL default value alignment"
      pattern: "flawchess"
---

<objective>
Rename the entire codebase from Chessalytics to FlawChess and create the apple-touch-icon placeholder.

Purpose: Establish the FlawChess brand identity across all code, config, docs, and PWA manifest before any external services (Sentry, Plausible, domain) are created — names embed at creation time and are hard to change later.

Output: All tracked files use "FlawChess"/"flawchess" exclusively. PWA manifest updated. Placeholder apple-touch-icon created. Tests pass.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-rename-branding/20-CONTEXT.md
@.planning/phases/20-rename-branding/20-RESEARCH.md
@.planning/phases/20-rename-branding/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all backend, frontend, config, and doc files from Chessalytics to FlawChess</name>
  <files>
    pyproject.toml, app/main.py, app/core/config.py, app/services/chesscom_client.py,
    app/services/zobrist.py, frontend/index.html, frontend/vite.config.ts,
    frontend/src/App.tsx, frontend/src/pages/Auth.tsx,
    frontend/src/components/install/InstallPromptBanner.tsx,
    frontend/src/lib/zobrist.ts, tests/conftest.py, tests/test_chesscom_client.py,
    .env.example, CLAUDE.md, .planning/PROJECT.md, .planning/STATE.md,
    .planning/ROADMAP.md, .planning/MILESTONES.md
  </files>
  <read_first>
    pyproject.toml, app/main.py, app/core/config.py, app/services/chesscom_client.py,
    app/services/zobrist.py, frontend/index.html, frontend/vite.config.ts,
    frontend/src/App.tsx, frontend/src/pages/Auth.tsx,
    frontend/src/components/install/InstallPromptBanner.tsx,
    frontend/src/lib/zobrist.ts, tests/conftest.py, tests/test_chesscom_client.py,
    .env.example, CLAUDE.md, .planning/PROJECT.md, .planning/STATE.md,
    .planning/ROADMAP.md, .planning/MILESTONES.md
  </read_first>
  <action>
Perform the following exact replacements in each file. Read every file first, then apply changes.

**Backend files:**

1. `pyproject.toml`: Change `name = "chessalytics"` to `name = "flawchess"`

2. `app/main.py`: Change `title="Chessalytics"` to `title="FlawChess"`

3. `app/core/config.py`:
   - Change default DATABASE_URL from `chessalytics` to `flawchess` (in the postgresql connection string)
   - Change default TEST_DATABASE_URL from `chessalytics_test` to `flawchess_test`

4. `app/services/chesscom_client.py`: Change `USER_AGENT = "Chessalytics/1.0 (github.com/chessalytics)"` to `USER_AGENT = "FlawChess/1.0 (github.com/flawchess/flawchess)"`

5. `app/services/zobrist.py`: Change "Chessalytics" to "FlawChess" in the module docstring

**Frontend files:**

6. `frontend/index.html`:
   - Change `<title>Chessalytics</title>` to `<title>FlawChess</title>`
   - Update `<link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />` (if href currently points to icon-192.png, change it to `/icons/apple-touch-icon.png`)

7. `frontend/vite.config.ts`: In the VitePWA manifest config:
   - Change `name: 'Chessalytics'` to `name: 'FlawChess'`
   - Change `short_name: 'Chessalytics'` to `short_name: 'FlawChess'`

8. `frontend/src/App.tsx`: Find ALL occurrences of "Chessalytics" in both NavHeader and MobileHeader components. Change each to "FlawChess". IMPORTANT: There are two separate components in this file — update both.

9. `frontend/src/pages/Auth.tsx`: Change `<h1>Chessalytics</h1>` (or the h1 containing "Chessalytics") to `<h1>FlawChess</h1>`

10. `frontend/src/components/install/InstallPromptBanner.tsx`: Change `Install Chessalytics` to `Install FlawChess` in the DrawerTitle

11. `frontend/src/lib/zobrist.ts`: Change "Chessalytics" to "FlawChess" in any file comments

**Test files:**

12. `tests/conftest.py`: Update any comments referencing "chessalytics_test" to "flawchess_test"

13. `tests/test_chesscom_client.py`: Change `assert "Chessalytics" in headers["User-Agent"]` to `assert "FlawChess" in headers["User-Agent"]`. Also update any other "chessalytics" string references.

**Config files:**

14. `.env.example`: Change all `chessalytics` references in DATABASE_URL to `flawchess` and `chessalytics_test` to `flawchess_test`

15. `CLAUDE.md`:
    - Change `## Project` description from "Chessalytics" to "FlawChess"
    - Replace all other occurrences of "Chessalytics" with "FlawChess" and "chessalytics" with "flawchess" — except leave the `chessalytics` in filesystem path references (those change when user renames directory)
    - Update the `User-Agent` example if present

**Active planning docs:**

16. `.planning/PROJECT.md`: Change heading and description from "Chessalytics" to "FlawChess"

17. `.planning/STATE.md`: Remove "(formerly Chessalytics)" from the heading if present. Update any remaining "Chessalytics" references to "FlawChess".

18. `.planning/ROADMAP.md`: Update any "Chessalytics" references in active content (not in the success criteria text that references grep commands — those should show the old name as the search target)

19. `.planning/MILESTONES.md`: Change heading from "# Milestones: Chessalytics" to "# Milestones: FlawChess"

**Do NOT modify:**
- `.claude/settings.local.json` (filesystem paths, user renames directory manually)
- `.env` (local file, not tracked — user handles manually)
- Any files under `.planning/milestones/`, `.planning/quick/`, `.planning/research/` (historical records)
- Any files under `.planning/phases/` for completed phases (historical records)
- `frontend/dist/` (gitignored, regenerated by build)

After all changes, run the grep verification command to catch any remaining occurrences:
```bash
grep -ri chessalytics . \
  --include="*.py" --include="*.ts" --include="*.tsx" --include="*.html" \
  --include="*.toml" --include="*.md" --include="*.json" --include="*.yaml" \
  --include="*.env*" \
  --exclude-dir=node_modules --exclude-dir=.git \
  --exclude-dir=".planning/milestones" --exclude-dir=".planning/quick" \
  --exclude-dir=".planning/research"
```

If any matches remain in files outside the exclusion list (and outside `.planning/phases/` completed phase docs), fix them.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics && grep -ri chessalytics . --include="*.py" --include="*.ts" --include="*.tsx" --include="*.html" --include="*.toml" --include="*.yaml" --include="*.env*" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=".planning/milestones" --exclude-dir=".planning/quick" --exclude-dir=".planning/research" | grep -v ".planning/phases/" | grep -v "grep -ri chessalytics" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "flawchess"`
    - app/main.py contains `title="FlawChess"`
    - app/core/config.py contains `flawchess` in DATABASE_URL default and `flawchess_test` in TEST_DATABASE_URL default
    - app/services/chesscom_client.py contains `FlawChess/1.0 (github.com/flawchess/flawchess)`
    - frontend/index.html contains `<title>FlawChess</title>`
    - frontend/vite.config.ts contains `name: 'FlawChess'` and `short_name: 'FlawChess'`
    - frontend/src/App.tsx contains "FlawChess" in both NavHeader and MobileHeader sections and does NOT contain "Chessalytics"
    - frontend/src/pages/Auth.tsx contains "FlawChess" and does NOT contain "Chessalytics"
    - frontend/src/components/install/InstallPromptBanner.tsx contains "Install FlawChess"
    - tests/test_chesscom_client.py contains `assert "FlawChess" in`
    - .env.example contains `flawchess` (not `chessalytics`) in DATABASE_URL
    - CLAUDE.md first heading or project description contains "FlawChess"
    - grep verification command returns zero matches (exit code 1 from grep = no matches found)
  </acceptance_criteria>
  <done>Every tracked source, config, test, and active planning file uses "FlawChess"/"flawchess" exclusively. No instance of "Chessalytics"/"chessalytics" remains in scope (verified by grep).</done>
</task>

<task type="auto">
  <name>Task 2: Create apple-touch-icon placeholder and verify tests pass</name>
  <files>frontend/public/icons/apple-touch-icon.png, frontend/index.html</files>
  <read_first>frontend/index.html, frontend/public/icons/</read_first>
  <action>
1. Copy `frontend/public/icons/icon-192.png` to `frontend/public/icons/apple-touch-icon.png`. This is a placeholder — the user will provide the final 180x180 asset later. Using the 192x192 as-is is close enough for placeholder purposes.

2. In `frontend/index.html`, verify the apple-touch-icon link tag points to `/icons/apple-touch-icon.png`:
   ```html
   <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
   ```
   If the href currently points to `/icons/icon-192.png`, update it to `/icons/apple-touch-icon.png`.

3. Run the full backend test suite to confirm all renames from Task 1 are consistent:
   ```bash
   cd /home/aimfeld/Projects/Python/chessalytics && uv run pytest -x
   ```

4. Run the frontend build to confirm no TypeScript/build errors:
   ```bash
   cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build
   ```
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics && test -f frontend/public/icons/apple-touch-icon.png && uv run pytest -x && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/public/icons/apple-touch-icon.png exists (file size > 0 bytes)
    - frontend/index.html contains `href="/icons/apple-touch-icon.png"` in the apple-touch-icon link tag
    - `uv run pytest` exits with code 0 (all tests pass)
    - `npm run build` exits with code 0 (frontend builds successfully)
  </acceptance_criteria>
  <done>apple-touch-icon.png placeholder exists. All backend tests pass. Frontend builds cleanly with FlawChess branding.</done>
</task>

</tasks>

<verification>
1. `grep -ri chessalytics` (with exclusions) returns zero matches in tracked source files
2. `uv run pytest` passes — confirms User-Agent rename and config changes are consistent
3. `npm run build` succeeds — confirms frontend compiles with all renames
4. `frontend/public/icons/apple-touch-icon.png` exists
5. PWA manifest in vite.config.ts shows "FlawChess" for name and short_name
</verification>

<success_criteria>
- Zero grep matches for "chessalytics" in tracked source/config/doc files (excluding historical planning docs)
- All backend tests pass
- Frontend builds successfully
- apple-touch-icon.png exists at correct path
- PWA manifest name/short_name are "FlawChess"
- Browser tab title is "FlawChess"
</success_criteria>

<output>
After completion, create `.planning/phases/20-rename-branding/20-01-SUMMARY.md`
</output>
