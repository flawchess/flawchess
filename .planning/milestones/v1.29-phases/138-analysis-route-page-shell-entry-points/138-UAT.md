---
status: deferred
phase: 138-analysis-route-page-shell-entry-points
source: [138-VERIFICATION.md]
deferred_to_phase: 139
started: 2026-06-26T19:30:00Z
updated: 2026-06-26T19:32:00Z
---

> **DEFERRED to Phase 139 (decision 2026-06-26).** All four items exercise the
> shared `/analysis` page surface, which Phase 139 (Tactic Mode Overlay) builds
> directly on top of. To avoid testing the same surface twice, these run as a
> combined browser/device UAT pass after Phase 139. Phase 138 code is complete
> and all automated gates pass (1217 tests, tsc/lint/knip, code review fixed);
> it stays pending-UAT until that combined pass. Run `/gsd-verify-work 138`
> (alongside Phase 139 verification) to close these out.

## Current Test

number: 1
name: SC#1 — Lazy-load boundary: no stockfish bundle on other routes
expected: |
  Open DevTools Network tab. Visit /library, /openings, /endgames, then /analysis.
  stockfish-18-lite-single.js and .wasm do NOT appear in the network log on the
  non-analysis routes; both fetch exactly once on the first /analysis visit.
awaiting: user response

## Tests

### 1. SC#1 — Lazy-load boundary: no stockfish bundle on other routes
expected: On /library, /openings, /endgames the stockfish JS/WASM files are absent from the Network tab; they fetch exactly once on the first /analysis visit (React.lazy code-split boundary enforced).
result: [pending]

### 2. SC#3 — On-device interactivity during WASM init (iOS Safari / low-end Android)
expected: Load /analysis fresh (no cache) on a real mobile device. "Loading engine..." spinner shows in the eval panel; board responds to piece drag/click and Back/Forward/Flip buttons are clickable immediately (before engine ready); EngineLines appear within ~3s; text-sm floor visually holds on EvalBar / EngineLines / VariationTree.
result: [pending]

### 3. SC#4 — crossOriginIsolated check and Google OAuth flow
expected: On /analysis, `window.crossOriginIsolated` returns false in the console. Signing out and completing the full Google OAuth login flow from /analysis and from /openings succeeds without errors or blocked popups.
result: [pending]

### 4. SC#2 — Opening-position entry end-to-end smoke test
expected: On the Openings Explorer tab (desktop and mobile), navigate to a non-start position (e.g. after 1. e4 e5) and click "Analyze position." URL becomes /analysis?fen=<url-encoded FEN> and the analysis board shows the Explorer position (not the start), matching the FEN in the URL.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
