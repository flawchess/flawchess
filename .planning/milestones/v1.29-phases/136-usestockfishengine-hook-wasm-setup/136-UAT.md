---
status: complete
phase: 136-usestockfishengine-hook-wasm-setup
source: [136-VERIFICATION.md]
started: 2026-06-26T12:45:00Z
updated: 2026-06-26T13:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Tab visible auto re-go after hidden pause
expected: With the engine active (isReady=true, isAnalyzing=true on a real board position), hide the browser tab. Verify the engine stops (isAnalyzing goes false). Make the tab visible again. Verify the engine re-starts analysis of the current position (isAnalyzing goes true and pvLines/evalCp are eventually updated). The visible→re-go transition is coded (analyzeRef.current(current) in handleVisibility) but not exercised by any test. Watch specifically for: stale-eval jitter or a missed re-analysis from the double-invoke when the tab becomes visible while stateRef is 'stopping' (direct visible-handler analyze + stale-bestmove-discard re-analyze).
result: skipped
reason: "Deferred to phases 137/138 per user decision. The test requires a real board UI to observe tab-hide/visible behavior, but this phase ships the hook as data-only (rendering deferred to 137/138) — there is no UI to test against yet. While reviewing the code the visible→re-go path was traced and CONFIRMED to contain a real double-go bug (see Gaps); the fix is deliberately deferred to 137/138 when the UI is wired and the path becomes observable. Captured as SEED-068 so it surfaces at 137/138 planning."

## Summary

total: 1
passed: 0
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps

### Deferred (not a blocking gap — accepted by user, carried to 137/138)

- truth: "On tab hidden the hook sends stop and keeps the Worker alive; on visible it auto re-go on the current FEN (D-04), without double-analyzing."
  status: confirmed-bug-deferred
  severity: minor
  test: 1
  reason: |
    CONFIRMED double-go bug in the visible→re-go path. When the tab becomes visible during
    the brief 'stopping' window (after a hide-triggered stop, before the stale bestmove
    arrives): handleVisibility's visible branch calls analyze(current); analyze() only
    guards the 'thinking' state (useStockfishEngine.ts:154), NOT 'stopping', so it falls
    through and sends position+go while stopPendingRef is still true. The stale bestmove
    then hits the discard branch (line 225-235), sets state to 'idle', and re-analyzes
    again → a SECOND position+go with no intervening stop (UCI protocol violation; Stockfish
    requires stop before a new go). Manifests as stale-eval jitter or a missed re-analysis.
  fix: |
    In analyze() add an early return when stateRef.current === 'stopping' — the pending
    stop's bestmove-discard handler (useStockfishEngine.ts:231-234) already re-analyzes
    currentFenRef once visible, yielding a single clean re-go. Cover with a deterministic
    mock-Worker unit test: hide-during-thinking, then visible-during-stopping, assert
    exactly ONE position+go is sent after the stale bestmove. No real browser required.
  artifacts: [frontend/src/hooks/useStockfishEngine.ts]
  missing: [frontend/src/hooks/__tests__/useStockfishEngine.test.ts (visible-during-stopping case)]
