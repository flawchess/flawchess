---
phase: 139-tactic-mode-overlay-phase-135-subsume
plan: 03
type: execute
wave: 2
depends_on: ['139-01', '139-02']
files_modified:
  - frontend/src/components/library/TacticLineExplorer.tsx
  - frontend/src/hooks/useTacticLine.ts
  - frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx
  - frontend/src/hooks/__tests__/useTacticLine.test.tsx
autonomous: true
requirements: [TACTIC-03]
must_haves:
  truths:
    - "TacticLineExplorer.tsx and useTacticLine.ts (and their __tests__) no longer exist"
    - "npm run knip passes clean (no dead exports / unused files introduced by the deletion)"
    - "npx tsc -b and the full frontend gate (lint + tests) pass after deletion"
  artifacts:
    - path: "frontend/src/hooks/useLibrary.ts"
      provides: "useTacticLines still has a live caller (Analysis.tsx) after the modal is gone — not flagged by knip"
      contains: "useTacticLines"
  key_links:
    - from: "deletion"
      to: "knip"
      via: "removing the modal + retired hook leaves no dead exports"
      pattern: "knip"
---

<objective>
Retire the standalone Tactic Line Explorer now that tactic-mode parity is verified (Plan 01
regression tests) and all entry points are repointed (Plan 02). Delete `TacticLineExplorer.tsx`,
`useTacticLine.ts`, and their tests; run the deletion gate (`knip` + `tsc -b` + full frontend gate).
This is the LAST plan in the phase (Pitfall 9 — deletion only after parity). TACTIC-03 completion.

Purpose: remove the duplicate board surface so the route is the single tactic UI.
Output: deleted files + a green deletion gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the modal + retired hook and pass the deletion gate</name>
  <files>frontend/src/components/library/TacticLineExplorer.tsx, frontend/src/hooks/useTacticLine.ts, frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx, frontend/src/hooks/__tests__/useTacticLine.test.tsx</files>
  <read_first>
    - .planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md (deletion gate: TACTIC-03 / SC#4 — knip clean; the 4 regression behaviors must already verify against the overlay, which Plan 01 delivered)
    - .planning/research/PITFALLS.md (Pitfall 9 — never delete before parity; this plan depends_on 139-01 + 139-02)
    - frontend/src/hooks/useLibrary.ts (confirm useTacticLines is still exported and now called by Analysis.tsx, so it survives knip)
    - frontend/knip config (package.json `knip` script / knip.json / knip.config.ts — confirm how knip is invoked)
    - CLAUDE.md (run tsc -b before integrating; npm run knip must pass clean after the deletion; full pre-merge gate)
  </read_first>
  <action>
    Confirm (via grep) that nothing imports `TacticLineExplorer` or `useTacticLine` anymore (Plan 02
    removed the FlawCard + LibraryGameCard usages; Plan 01 replaced the modal's data path with
    Analysis.tsx). Then delete `frontend/src/components/library/TacticLineExplorer.tsx`,
    `frontend/src/hooks/useTacticLine.ts`, `frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx`,
    and `frontend/src/hooks/__tests__/useTacticLine.test.tsx`. If knip then reports any export that
    became dead solely because it was only used by the deleted files (e.g. a helper exported only
    for the modal), remove that now-dead export too — but do NOT remove anything still used by
    `Analysis.tsx`/`TacticModeOverlay.tsx` (e.g. `useTacticLines`, `resolveVisibleTactic`,
    `TacticMotifChip`, `tacticDepth` exports, `formatFlawEval`, `moveLabel`, theme tokens). Run the
    full frontend gate.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -5 && npm run knip 2>&1 | tail -15 && npm run lint 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f frontend/src/components/library/TacticLineExplorer.tsx && echo gone` prints `gone`
    - `test ! -f frontend/src/hooks/useTacticLine.ts && echo gone` prints `gone`
    - `test ! -f frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx && echo gone` prints `gone`
    - `test ! -f frontend/src/hooks/__tests__/useTacticLine.test.tsx && echo gone` prints `gone`
    - `grep -rn "TacticLineExplorer\|useTacticLine\b" frontend/src --include=*.ts --include=*.tsx` returns no matches (useTacticLine**s** plural is unaffected — that is a different hook)
    - `cd frontend && npm run knip` exits 0 (no unused files/exports reported)
    - `cd frontend && npx tsc -b --noEmit` exits 0; `npm run lint` clean; `npm test -- --run` all green
  </acceptance_criteria>
  <done>The modal and its retired stepper hook (and their tests) are deleted; knip, tsc -b, lint, and the full frontend test suite pass; useTacticLines survives via its Analysis.tsx caller.</done>
</task>

</tasks>

<verification>
- Deletion-before-parity is structurally prevented: this plan `depends_on: ['139-01', '139-02']` (wave 2), so the regression tests (139-01) and entry repoint (139-02) are already done.
- `cd frontend && npm run knip && npx tsc -b --noEmit && npm run lint && npm test -- --run` all pass.
</verification>

<success_criteria>
TacticLineExplorer.tsx + useTacticLine.ts (+ tests) deleted; knip clean; full frontend gate green;
TACTIC-03 satisfied (modal retired, all entry points on /analysis).
</success_criteria>

<output>
Create `.planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-03-SUMMARY.md` when done.
</output>
