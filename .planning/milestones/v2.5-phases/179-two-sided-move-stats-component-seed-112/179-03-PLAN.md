---
phase: 179-two-sided-move-stats-component-seed-112
plan: 03
type: execute
wave: 3
depends_on: ["179-02"]
files_modified:
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/analysis/AnalysisTagsPanel.tsx
  - frontend/src/pages/Analysis.tsx
  - frontend/src/components/library/MoveStats.tsx
  - frontend/src/components/results/__tests__/LibraryGameCard.test.tsx
  - frontend/src/components/analysis/__tests__/AnalysisTagsPanel.test.tsx
  - frontend/src/components/library/GemGreatBadge.tsx
  - frontend/src/components/library/__tests__/GemGreatBadge.test.tsx
autonomous: false
requirements: [D-03, D-06, D-07, D-08, D-09, D-10, D-11]
must_haves:
  truths:
    - "The Library game card renders MoveStats on BOTH its mobile body and its desktop body; the old SeverityBadge/GemGreatBadge rows are gone from this file (SEED-112, CLAUDE.md both-bodies rule)"
    - "The analysis board tags panel renders MoveStats and always shows the 7-row table for any analyzed game (D-03) — the markers-empty early-return is removed (RESEARCH Pitfall 2)"
    - "Mobile collapsed default shows the accuracy strip + the USER's 3 severity counts (I/M/B); tap expands to the full two-sided 7-row table (D-06)"
    - "An unanalyzed game (analysis_state === 'no_engine_analysis') shows NO strip and NO table — only the existing analyze pill (D-07)"
    - "Clicking a non-zero cell cycles per (category × side): new cell jumps to first ply, re-click advances + wraps, a different cell resets; zero cells are inert (D-09)"
    - "Analysis board cell activation drives goToNode; Library card activation drives the miniboard + eval-chart tooltip; hovering a cell dims non-matching eval-chart markers (D-09)"
    - "The global flaw filter (useFlawFilterStore) stays user-scoped and rings ONLY the player-side cell of the matching row; opponent cells are independently clickable but never targeted by the filter; outlinedPlies behaves as today (D-10)"
    - "Tactic motif chips + context tags remain as a chip section (D-11)"
    - "GemGreatBadge.tsx and its test are deleted; SeverityBadge.tsx is preserved (still used by FlawCard.tsx); knip passes (RESEARCH Pitfall 3)"
  artifacts:
    - "LibraryGameCard.tsx consuming <MoveStats> on mobile + desktop with the unified (category × side) FlawRef"
    - "AnalysisTagsPanel.tsx consuming <MoveStats> with the empty-early-return removed"
    - "Analysis.tsx cycling/highlight wiring routed to MoveStats cell refs (goToNode)"
    - "Deleted: frontend/src/components/library/GemGreatBadge.tsx + __tests__/GemGreatBadge.test.tsx"
  key_links:
    - "MoveStats onCellActivate → LibraryGameCard cycle state → miniboard + eval-chart tooltip"
    - "MoveStats onCellActivate → AnalysisTagsPanel/Analysis.tsx cycle state → goToNode"
    - "useFlawFilterStore matchingFilterKeys → MoveStats outlinedRef (player-side cell only, D-10)"
  prohibitions:
    - "The global filter must NEVER target/ring an opponent-side cell (D-10)"
    - "Do NOT delete or modify SeverityBadge.tsx — FlawCard.tsx (out of scope) still consumes it (RESEARCH Pitfall 3)"
    - "Do NOT reintroduce the count>0 row-hide or the markers-empty early-return (D-03)"
    - "Do NOT apply a change to only one of the mobile/desktop bodies — both must render MoveStats (CLAUDE.md)"
    - "Do NOT surface ACPL and do NOT fall back to *_imported accuracy (D-01/D-02)"
    - "No text-xs outside hover/tap tooltip bodies (CLAUDE.md)"
  layout_decision:
    - "DESKTOP LAYOUT (planner decision, resolving RESEARCH Pitfall 4 / open question 1): MoveStats replaces the existing severity/gem-great badge column ONLY (~225px beside the miniboard); the eval chart stays exactly where it is today; the tactic chip section stays (D-11). This is the minimal-disruption arrangement; confirm visually at the human-verify checkpoint."
---

<objective>
Wire the shared MoveStats component (Plan 02) into both consumer surfaces, replacing the
old badge rows, and rebuild the per-cell (category × side) cycling, hover-dim, mobile
collapse, and user-scoped filter ring around the unified FlawRef. Then delete the now-orphaned
GemGreatBadge and confirm knip is clean.

Purpose: This is the user-visible payoff of the phase — one two-sided Move Stats component
on the Library card and the analysis board, with opponent tiers surfaced (D-08), a stable
always-7-row table (D-03), a mobile-scannable collapsed default (D-06), and cycling/filter
reworked around (category × side) (D-09/D-10). RESEARCH confirmed both files already carry
near-identical FlawRef + cycle/highlight machinery, so this is a mechanical-but-careful
migration, not a new pattern.

Output: LibraryGameCard + AnalysisTagsPanel render MoveStats (mobile + desktop), cycling and
filter reworked, GemGreatBadge deleted, tests extended, visual UAT signed off.

## Artifacts this phase produces (Plan 03)
- Unified `FlawRef` variant `{ kind: 'category'; category: MoveStatCategory; side: MoveStatSide }` replacing the separate `severity`/`bestMove` kinds in both files
- `<MoveStats>` consumption in LibraryGameCard.tsx (mobile collapsed + expanded, desktop) and AnalysisTagsPanel.tsx
- Analysis.tsx cell-ref → goToNode cycling wiring
- Deletions: `GemGreatBadge.tsx`, `GemGreatBadge.test.tsx`
- New `data-testid`: `move-stats-expand-toggle` (mobile collapse control)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-two-sided-move-stats-component-seed-112/179-CONTEXT.md
@.planning/phases/179-two-sided-move-stats-component-seed-112/179-RESEARCH.md
@frontend/src/hooks/useFlawFilterStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate LibraryGameCard to MoveStats — mobile collapse (D-06), desktop, cycling (D-09), user-scoped filter ring (D-10)</name>
  <read_first>
    - frontend/src/components/results/LibraryGameCard.tsx (FlawRef at ~180; user_color narrowing at ~255-261; severityBadges at ~915; bestMoveBadges at ~942 incl. the count>0 filter to drop; mobile body composition at ~1050-1057; desktop body at ~1198-1258 incl. eval chart + severityBadges/bestMoveBadges at ~1240-1241; setHighlight/handleActivate + isUserPly usage at ~439-454; eval-chart hover-dim wiring)
    - frontend/src/components/library/MoveStats.tsx (Plan 02 — props: onCellActivate, onCellHover, activeRef, outlinedRef, collapsed)
    - frontend/src/lib/moveStatsCounts.ts (MoveStatCategory, MoveStatSide)
    - frontend/src/hooks/useFlawFilterStore.ts (outlinedPlies, matchingFilterKeys — user-scoped; the ring targets the player-side cell only)
    - frontend/src/components/library/SeverityBadge.tsx (KEEP — used for the mobile-collapsed user I/M/B row if reused; do not delete)
    - .planning/phases/179-two-sided-move-stats-component-seed-112/179-CONTEXT.md (D-06/D-07/D-09/D-10/D-11)
  </read_first>
  <behavior>
    - An analyzed game on desktop renders MoveStats in place of the old badge column; the eval chart and tactic chips still render (D-11, layout_decision)
    - Mobile collapsed default renders the accuracy strip + the user's 3 severity counts (I/M/B) and a move-stats-expand-toggle; tapping expands to the full two-sided 7-row table (D-06)
    - An unanalyzed game (analysis_state==='no_engine_analysis') renders neither strip nor table — only the existing analyze pill (D-07)
    - Clicking a new non-zero cell jumps the miniboard/eval-chart tooltip to the first matching ply; re-clicking the same cell advances and wraps; clicking a different cell resets to first; zero cells do nothing (D-09)
    - Hovering a cell dims non-matching eval-chart markers as today (D-09)
    - When the global filter matches a category, only the player-side cell of that row shows the filter ring; the opponent-side cell of the same row is still clickable for cycling but never ringed (D-10)
  </behavior>
  <action>
    Replace the local `FlawRef` union's `{ kind:'severity' }` and `{ kind:'bestMove' }` variants with the
    single `{ kind:'category'; category: MoveStatCategory; side: MoveStatSide }` (keep `tag`/`motif` variants).
    Update `pliesForRef`/`handleActivate`/`sameFlawRef`/`setHighlight` to dispatch on the unified ref: resolve
    plies for a (category, side) by filtering flaw_markers (I/M/B) or eval_series (gem/great/best/good) to that
    category AND `moverColorAtPly === side` (both sides now, D-08 — drop the isUserPly gate for cell resolution).
    Remove `severityBadges`/`bestMoveBadges` (and the `count>0` filter) and the GemGreatBadge/SeverityBadge
    imports used for the card rows; render `<MoveStats>` in BOTH the mobile body (~1050) and desktop body (~1240),
    passing the cycle `activeRef`, the `onCellActivate`/`onCellHover` handlers (driving miniboard + eval-chart
    tooltip + hover-dim), and an `outlinedRef` derived from `useFlawFilterStore` that only ever names the
    player-side cell (`side === userColor`) of the matching category (D-10). For mobile, pass `collapsed` state
    with a `move-stats-expand-toggle` control (aria-label + data-testid); collapsed shows the strip + the user's
    I/M/B counts (reuse SeverityBadge for that compact row per Claude's discretion, or a compact MoveStats mode —
    either is acceptable, SeverityBadge stays in the tree regardless). Keep the tactic chip section unchanged (D-11).
    Do not modify the desktop eval-chart placement (layout_decision). Extend LibraryGameCard.test.tsx for D-06
    (collapse/expand), D-07 (unanalyzed pill only), D-09 (cycle wrap/reset, inert zero cell), and D-10 (ring on
    player-side cell only).
  </action>
  <acceptance_criteria>
    - grep confirms LibraryGameCard.tsx no longer imports GemGreatBadge and no longer renders `severityBadges`/`bestMoveBadges` card rows; both mobile (~1050) and desktop (~1240) render `<MoveStats>`
    - grep confirms no `count>0` / `.length > 0` row-hide filter remains around the category rows
    - Test: mobile collapsed shows strip + user I/M/B + `move-stats-expand-toggle`; expand shows the 7-row table (D-06)
    - Test: analysis_state==='no_engine_analysis' renders the analyze pill and no MoveStats (D-07)
    - Test: cycling a cell jumps→advances→wraps, a different cell resets, a zero cell is inert (D-09)
    - Test: filter match rings only the player-side cell; opponent-side cell not ringed but still clickable (D-10)
    - `cd frontend && npm test -- --run LibraryGameCard && npm run lint && npx tsc -b` all pass
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- --run LibraryGameCard && npm run lint && npx tsc -b</automated>
  </verify>
  <done>The Library card renders MoveStats on mobile (collapsible) and desktop with (category × side) cycling and a strictly user-scoped filter ring; unanalyzed games show only the analyze pill; chips and eval chart unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate AnalysisTagsPanel to MoveStats (remove empty early-return, D-03), wire goToNode cycling, delete GemGreatBadge</name>
  <read_first>
    - frontend/src/components/analysis/AnalysisTagsPanel.tsx (FlawRef at ~34; the early-return at ~239-241 `if (game.analysis_state !== 'analyzed' || (markers.length === 0 && !hasBestMovePlies)) return null` to change; SeverityBadge/GemGreatBadge render at ~271/~289; the "trivially safe copies" convention docstring at ~24-25)
    - frontend/src/pages/Analysis.tsx (onCyclePly / onHighlightChange wiring passed to AnalysisTagsPanel; goToNode; where gameData: GameFlawCard is passed ~line 2986-3000)
    - frontend/src/components/library/MoveStats.tsx (Plan 02)
    - frontend/src/lib/moveStatsCounts.ts
    - frontend/src/components/library/FlawCard.tsx (~349-354 — confirm it still imports SeverityBadge; do NOT touch this file, it proves SeverityBadge must survive)
    - .planning/phases/179-two-sided-move-stats-component-seed-112/179-RESEARCH.md (Pitfall 2 early-return, Pitfall 3 GemGreatBadge deletion / SeverityBadge preservation)
  </read_first>
  <behavior>
    - A flawless analyzed game (zero flaws AND zero gem/great plies) renders the full 7-row all-zero table instead of nothing (D-03, Pitfall 2)
    - The panel's mount condition is exactly `game.analysis_state === 'analyzed'` (no markers/tiers emptiness clause)
    - Opponent-side positive tiers are shown on /analysis (D-08)
    - Clicking a non-zero cell drives goToNode to the first matching ply, re-click advances+wraps, different cell resets, zero cells inert (D-09)
    - GemGreatBadge.tsx + its test are gone; SeverityBadge.tsx remains and FlawCard.tsx still compiles; knip is clean
  </behavior>
  <action>
    In `AnalysisTagsPanel.tsx`, replace the local `severity`/`bestMove` FlawRef variants with the unified
    `{ kind:'category'; category; side }` variant, mirroring Task 1. Change the mount guard at ~240 to simply
    `if (game.analysis_state !== 'analyzed') return null;` — drop the `markers.length === 0 && !hasBestMovePlies`
    clause (Pitfall 2 / D-03) so a flawless analyzed game renders the all-zero 7-row table. Remove the inline
    SeverityBadge/GemGreatBadge render blocks and render `<MoveStats>` (always full table on /analysis, no collapse
    per SEED-112). Route `onCellActivate`/`onCellHover` through the existing `onCyclePly`/`onHighlightChange`
    props to `goToNode` in `Analysis.tsx`, resolving plies for a (category, side) the same way as Task 1.
    Keep the tactic chip section (D-11). Then delete `frontend/src/components/library/GemGreatBadge.tsx` and
    `frontend/src/components/library/__tests__/GemGreatBadge.test.tsx`, and update any `BestMoveTier` type import
    (it came from GemGreatBadge) to the Plan 02 `MoveStatCategory`. Do NOT touch `SeverityBadge.tsx` or
    `FlawCard.tsx`. Run `npm run knip` to confirm no orphaned exports. Extend AnalysisTagsPanel.test.tsx for the
    removed early-return (flawless game renders all-zero table) and D-08 opponent-tier display.
  </action>
  <acceptance_criteria>
    - `AnalysisTagsPanel.tsx` mount guard is `game.analysis_state !== 'analyzed'` only (grep: no `markers.length === 0` in the guard)
    - Test: a flawless analyzed game renders 7 all-zero rows (not null) (D-03/Pitfall 2)
    - Test: opponent-ply gem/great appears on /analysis (D-08); cell click drives goToNode with wrap/reset, zero cells inert (D-09)
    - `frontend/src/components/library/GemGreatBadge.tsx` and its test no longer exist; `SeverityBadge.tsx` still exists; grep confirms FlawCard.tsx still imports SeverityBadge
    - `cd frontend && npm run knip` passes (no orphaned GemGreatBadge export)
    - `cd frontend && npm test -- --run AnalysisTagsPanel && npm run lint && npx tsc -b` all pass
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- --run AnalysisTagsPanel && npm run knip && npm run lint && npx tsc -b</automated>
  </verify>
  <done>The analysis panel always renders the 7-row two-sided table for analyzed games (incl. flawless), cycling drives goToNode with opponent tiers surfaced; GemGreatBadge is deleted knip-clean and SeverityBadge preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Move Stats component on Library + analysis surfaces</name>
  <action>Pause for human visual/interaction UAT of the Move Stats component on the Library card (mobile + desktop) and the analysis board, per the how-to-verify steps below. Do not proceed to phase completion until approved.</action>
  <what-built>
    The two-sided Move Stats component is now live on the Library game card (mobile collapsible +
    desktop) and the analysis board tags panel: accuracy strip with player-color-coded cells, an
    always-7-row Gem/Great/Best/Good/Inaccuracy/Mistake/Blunder table with two per-player count columns,
    (category × side) click-to-cycle, hover-dim, a user-scoped filter ring, and the retained tactic chips.
  </what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev`, open the Library page (Games subtab) on a desktop viewport.
    2. Confirm each analyzed game card shows: the accuracy strip (white cell = white player bg, dark cell = black player bg, player column on the left), a full 7-row table with muted 0s where empty, the eval chart still in place, and tactic chips still present (D-11). A game with no canonical accuracy shows a muted em-dash, not a number (D-01).
    3. Click a non-zero count cell → the miniboard/eval-chart tooltip jumps to that move; click again → advances and wraps; click a different cell → resets. Confirm a zero cell does nothing (D-09).
    4. Apply a global flaw filter → confirm only the player-side cell of the matching row is ringed; the opponent-side cell of that row is not ringed but still clickable (D-10).
    5. Shrink to a mobile viewport → each card collapses to the accuracy strip + the user's I/M/B counts; tap the expand control → the full two-sided table appears (D-06).
    6. Find an unanalyzed game → confirm it shows only the analyze pill, no strip/table (D-07).
    7. Open `/analysis?game_id=X` for an analyzed game → confirm the full 7-row two-sided table renders (including opponent gems/greats, D-08); clicking a cell navigates the board (goToNode). If you have a flawless analyzed game, confirm it shows an all-zero table rather than nothing (D-03).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/interaction issues (especially the desktop MoveStats-vs-eval-chart arrangement).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameFlawCard payload → client render/interaction | already-fetched, user-owned data rendered + navigated in the browser; no new fetch/input/auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-179-03 | Information Disclosure | opponent-tier display on both surfaces (D-08) | low | accept | Opponent tiers already ship on the payload (eval_series both-sided) via an intentionally shareable endpoint; D-08 is the explicit decision to show them here. No new data crosses the boundary. |
| T-179-04 | Tampering | filter ring must stay user-scoped (D-10) | low | mitigate | Ring `outlinedRef` derived only where `side === game.user_color`; unit test asserts opponent cells are never ringed — a purely presentational invariant, no security impact but enforced by test. |
| T-179-SC | Tampering | npm installs | n/a | n/a | No new packages (RESEARCH: not applicable). Deletion of GemGreatBadge removes code, adds none. |

Purely presentational/interaction rework over already-fetched, user-owned data on authenticated surfaces. No new auth, input, network, or dependency. Minimal by construction.
</threat_model>

<verification>
- `cd frontend && npm test -- --run LibraryGameCard` and `--run AnalysisTagsPanel` green
- `cd frontend && npm run knip && npm run lint && npx tsc -b` all clean (GemGreatBadge orphan removed)
- Pre-merge gate before any squash to main: `( cd frontend && npm run lint && npm test -- --run )` plus backend gate per CLAUDE.md
- Human-verify checkpoint approved (D-01/D-03/D-06/D-07/D-08/D-09/D-10/D-11 visually confirmed, desktop layout signed off)
</verification>

<success_criteria>
Both surfaces render the shared two-sided Move Stats component with correct mobile/desktop behavior, (category × side) cycling, user-scoped filter ring, retained chips, opponent tiers surfaced, and a stable always-7-row table; GemGreatBadge is deleted knip-clean, SeverityBadge preserved; visual UAT approved.
</success_criteria>

<output>
Create `.planning/phases/179-two-sided-move-stats-component-seed-112/179-03-SUMMARY.md` when done
</output>
