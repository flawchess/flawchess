# v1.22 Maintenance — Test Isolation & Frontend Major Upgrades — Requirements (ARCHIVED)

**Shipped:** 2026-05-31
**Phases:** 100, 101

## No formal requirements

v1.22 Maintenance was a standalone test-infrastructure + dependency-maintenance milestone. Both phases (100 Isolated Test DB Per Run / SEED-031, 101 Frontend Major Dependency Upgrades / SEED-032) are sourced from seeds and carry **no formal requirement IDs** — the same pattern as the v1.20 close. Success was tracked via per-phase Success Criteria (all met; see `v1.22-ROADMAP.md`), not a traceability table.

## Note on the live REQUIREMENTS.md (NOT deleted at this close)

Unlike the standard milestone-close flow, the project's live `.planning/REQUIREMENTS.md` was **intentionally kept in place** at the v1.22 close rather than archived-and-deleted.

The live `REQUIREMENTS.md` documents the **LLM Statistical Reasoning** scope (LLM-01 .. LLM-07). That scope was the *originally planned* v1.22 milestone; on 2026-05-31 it was deprioritized in favour of this Maintenance milestone and parked as ROADMAP **Phase 999.7 (BACKLOG)**, with all seven requirements still `Pending`. Those requirements are forward-looking and must stay live-tracked for a future feature milestone, so deleting them at this close would have lost that tracking. They remain in `.planning/REQUIREMENTS.md` (which already records its own deprioritized/backlog status) until promoted via `/gsd-review-backlog`.

## Source seeds

- **SEED-031** — Isolated test DB per run (→ Phase 100)
- **SEED-032** — Frontend major dependency upgrades, clustered/sequential (→ Phase 101)

_For current project status, see .planning/ROADMAP.md_
