---
phase: 94.3-per-tc-percentile-chips-time-pressure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - reports/benchmarks-gap-metrics-percentile-candidacy.md
autonomous: true
requirements: [SEED-025, TPCTL-07]

must_haves:
  truths:
    - "reports/benchmarks-gap-metrics-percentile-candidacy.md contains a new top-level section titled '## Time Pressure metric family (Phase 94.3)' (or equivalent), placed after the existing 4-metric ΔES content and before any closing '## SQL provenance' style appendix."
    - "The new section contains a 12-row table covering every {metric_family, tc} cell — 3 families (`time_pressure_score_gap`, `clock_gap`, `net_flag_rate`) × 4 TCs (`bullet`, `blitz`, `rapid`, `classical`) — with columns: metric × TC, n_users (deduped by lichess_username, post per-pressure-cell ≥30 floor for time_pressure_score_gap and post pooled-set ≥30 floor for the other two), pooled median, Cohen's d (800 ↔ 2400 ELO buckets), tier label, tooltip 4th-bullet copy."
    - "Every Cohen's d cell carries a tier label drawn verbatim from the existing report's ladder (`rating-invariant` d ≤ 0.20 / `mild coupling` d ≤ 0.35 / `moderate coupling` d ≤ 0.60 / `heavy rating-proxy` d > 0.60) — the planner does not invent new tier names or thresholds."
    - "Each cell's tooltip 4th-bullet copy is a single sentence obeying `feedback_popover_copy_minimalism` (WHAT + sign convention only, no jargon, no caveats) and is per-(metric × TC) not generic — `net_flag_rate_bullet` may read 'tracks rating strongly' while `net_flag_rate_classical` may read 'is mostly independent of rating' if the d values differ."
    - "The section's methodology paragraph cites the canonical-slice CTE (universal filters + ±100 equal-footing + 36-month recency + 1000/TC cap) with the explicit per-TC restriction `g.time_control_bucket = '{tc}'`, mirroring the existing report's methodology language. Bullet-TC-cohort-only and classical-TC-cohort-only paragraphs are not produced — this is a per-cell table, not 4 per-TC subsections."
    - "If any cell's n_users drops below 150 (the RESEARCH §Pitfall 8 classical-TC threshold), the cell is flagged inline with a note such as 'n_users < 150 — chip suppression candidate (RESEARCH §Pitfall 8)' so Plan F can decide whether to ship that chip."
  artifacts:
    - path: reports/benchmarks-gap-metrics-percentile-candidacy.md
      provides: 12-cell per-(metric × TC) Cohen's d candidacy table for the Time Pressure family
      contains: "Time Pressure metric family"
      min_lines: 30
  key_links:
    - from: reports/benchmarks-gap-metrics-percentile-candidacy.md
      to: frontend/src/components/charts/PercentileChip.tsx
      via: tooltip 4th-bullet copy strings consumed by RATING_NOTE_BY_FLAVOR
      pattern: "RATING_NOTE_BY_FLAVOR"
---

<objective>
Extend `reports/benchmarks-gap-metrics-percentile-candidacy.md` with a 12-cell per-(metric × TC) Cohen's d candidacy table for the Time Pressure metric family, computed against the benchmark DB (Docker `flawchess-benchmark-db-1` on port 5433). The table calibrates the per-chip 4th tooltip bullet (rating-correlation framing) Plan F will populate into `RATING_NOTE_BY_FLAVOR` in `PercentileChip.tsx`.

Purpose: Net Flag Rate at bullet likely tracks rating much harder than at classical — the per-cell calibration is required for honest disclosure on the chip surface, and the Phase 94.2 candidacy report's single-cell-per-metric shape does not generalise. Plan F reads exact copy strings from this table; without it, the planner would either invent rating-correlation framings unaccountable to data or copy-paste a generic line across 12 distinct chips.

Output: A new `## Time Pressure metric family (Phase 94.3)` section appended to the candidacy report containing one 12-row tier table plus a methodology paragraph + (optional) per-cell pooled distribution stats for cells whose Cohen's d sits near a tier boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-CONTEXT.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md
@reports/benchmarks-gap-metrics-percentile-candidacy.md
@.claude/skills/benchmarks/SKILL.md

<interfaces>
<!-- Existing tier-ladder thresholds from reports/benchmarks-gap-metrics-percentile-candidacy.md lines 14-21
     (verified in PATTERNS.md). Plan A must reuse these labels verbatim — DO NOT invent new
     thresholds or relabel the tiers. -->

Tier ladder (existing report):
  d ≤ 0.20 → "COLLAPSE on the ELO axis" / rating-invariant
  d ≤ 0.35 → "REVIEW" / mild coupling
  d ≤ 0.60 → "REVIEW" / moderate coupling
  d > 0.60 → "KEEP SEPARATE (large effect)" / heavy rating-proxy

Existing headline-table column order (lines 14-22): Metric | Pooled n | Pooled median | Skew | Excess kurt | 800 median | 2400 median | d_max (800↔2400) | Verdict | SEED-019 tier
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking-human">
  <name>Task 0: Schema probe — verify game_positions clock column names before writing SQL</name>
  <files>(no file modified — investigation only, results captured in task notes)</files>
  <read_first>
    - app/services/endgame_service.py (lines 1455-1957 — _compute_time_pressure_cards; the authoritative source for which columns to read for per-game clock-pct + base-clock + termination data)
    - app/repositories/endgame_repository.py (or wherever `query_clock_stats_rows` lives — search via `grep -rn 'query_clock_stats_rows' app/`)
    - app/services/canonical_slice_sql.py (Phase 94.2 post-state — `_recent_capped_cte` pattern; `equal_footing_filter_sql`; `_recency_window_sql`)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pitfall 5, §Open Question 1, §Pattern 1 (lines 257-388)
  </read_first>
  <action>
    Run `mcp__flawchess-benchmark-db__query` (or `psql` against `flawchess-benchmark-db-1` on port 5433 with the read-only role) with `\d game_positions` and `\d games` to enumerate the exact column names and units for: (a) per-position user clock seconds, (b) per-position opp clock seconds, (c) base/initial clock seconds, (d) game termination column distinguishing time-forfeit outcomes, (e) endgame-entry detection column (already known from Phase 94.2 — `endgame_class IS NOT NULL` + `count(ply) >= 6`). Cross-reference with `endgame_service._compute_time_pressure_cards` and `query_clock_stats_rows` to confirm which columns are read for clock-pct + net-flag-rate. Capture the verified column names + units in the plan summary so Plan B can reference them verbatim. If any inferred column from RESEARCH §Pattern 1 (`gp.user_clock_seconds`, `gp.opp_clock_seconds`, `gp.base_clock_seconds`) does NOT exist, document the actual column name. Mark this task complete only when you can write the SQL builders in Plan B without further schema discovery. Bench DB must be running (`bin/benchmark_db.sh start`); if it's down, surface that as a blocker per RESEARCH §Environment Availability.
  </action>
  <verify>
    <automated>node -e 'console.log("Task 0 verification is the documented column-name table in the plan summary; this command exists only to satisfy the Nyquist verify contract.")'</automated>
    <human-check>The plan summary contains a "Verified column names" subsection listing exact column names and units for user clock, opp clock, base clock, termination, endgame-entry detection. Each column is annotated with the table it lives on (games vs game_positions).</human-check>
  </verify>
  <acceptance_criteria>
    - The verified-column-names list documents at least 5 columns with table + units (e.g. `game_positions.user_clock_seconds : INT, seconds`).
    - Every column listed in RESEARCH §Pattern 1's SQL sketches has either been confirmed verbatim OR replaced with the actual column name discovered in this task.
    - If termination data lives on `games.termination` vs `game_positions.termination`, the location is explicit (Plan B needs to know which table to join).
  </acceptance_criteria>
  <done>The plan summary contains a verified-column-name table; no further schema probes are required during Plan B.</done>
</task>

<task type="auto">
  <name>Task 1: Compute 12 (metric × TC) Cohen's d cells against benchmark DB and append the table</name>
  <files>reports/benchmarks-gap-metrics-percentile-candidacy.md</files>
  <read_first>
    - reports/benchmarks-gap-metrics-percentile-candidacy.md (full file — methodology section ~line 236+, existing per-metric sections lines 26-208)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §"reports/benchmarks-gap-metrics-percentile-candidacy.md" (lines 1095-1161)
    - .claude/skills/benchmarks/SKILL.md Chapter 1 + Chapter 4 (canonical-slice CTE construction)
    - app/services/canonical_slice_sql.py (Phase 94.2 post-state — selected_users_cte / _recent_capped_cte / equal_footing_filter_sql for reuse in the candidacy SQL)
    - Task 0's verified column-name table (in this plan's running notes)
  </read_first>
  <action>
    For each of the 12 cells (`{time_pressure_score_gap, clock_gap, net_flag_rate} × {bullet, blitz, rapid, classical}`), run a Cohen's d SQL against the benchmark DB by adapting the existing report's per-metric SQL (canonical-slice CTE — `bic.status='completed'`, ±100 equal-footing, 36-month recency, 1000/TC cap, sparse-cell `(2400, classical)` exclusion only on the cohort selection step) with one additional predicate `AND g.time_control_bucket = '{tc}'` on the games table. Compute per-bucket means and SDs at ELO bucket 800 and ELO bucket 2400, then `d = (mean(2400) - mean(800)) / pooled_sd`. Apply the per-pressure-cell ≥30 floor for time_pressure_score_gap (≥30 in user-pressured AND ≥30 in opp-pressured) and the pooled-set ≥30 floor for clock_gap + net_flag_rate. Append a new section `## Time Pressure metric family (Phase 94.3)` to `reports/benchmarks-gap-metrics-percentile-candidacy.md` (place it AFTER the existing 4-metric content, BEFORE the SQL provenance / metadata appendix). The section contains: (a) a methodology paragraph naming the canonical-slice CTE and the per-TC restriction; (b) a single 12-row table with columns `metric × TC | n_users | pooled median | skew | 800 median | 2400 median | Cohen's d | Tier | Tooltip 4th-bullet copy`; (c) optional per-cell prose only for cells whose d sits within 0.05 of a tier boundary (to justify the assignment). For each cell, write the tooltip 4th-bullet copy as a single sentence obeying the tier-templates from RESEARCH §Pattern 8 (lines 791-805) and `feedback_popover_copy_minimalism` (WHAT + sign convention only, no jargon, no caveats). For Net Flag Rate cells specifically, recall the D-3 "Lower is better" line is rendered separately by Plan F as a prepended line — the 4th-bullet copy still describes rating correlation only. Tier labels MUST come from the existing ladder verbatim — do not invent new labels. Flag any cell whose `n_users < 150` inline with the note "n_users < 150 — chip suppression candidate (RESEARCH §Pitfall 8)". Per `project_benchmark_db_ro_password`, use `mcp__flawchess-benchmark-db__query` for the probes (read-only role; password is local-only).
  </action>
  <verify>
    <automated>grep -E '(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)' reports/benchmarks-gap-metrics-percentile-candidacy.md | grep -v '^#' | wc -l | awk '$1 >= 12 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - The report contains a new section `## Time Pressure metric family (Phase 94.3)` placed after the existing 4-metric content.
    - The 12-row table has exactly one row per (metric_family × tc) combination — verifiable by `grep -E '(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)' reports/benchmarks-gap-metrics-percentile-candidacy.md | grep -v '^#' | wc -l` ≥ 12.
    - Every row carries a tier label drawn verbatim from the existing ladder (`rating-invariant` / `mild coupling` / `moderate coupling` / `heavy rating-proxy`) — `grep -cE '(rating-invariant|mild coupling|moderate coupling|heavy rating-proxy)' reports/benchmarks-gap-metrics-percentile-candidacy.md` increases by at least 12.
    - Every row carries a non-empty tooltip 4th-bullet copy string (≥ 3 words; no "TBD" / "…" placeholders).
    - The methodology paragraph names the canonical-slice CTE and the per-TC restriction explicitly.
    - Any cell with `n_users < 150` is annotated inline as a chip-suppression candidate.
    - The existing 4-metric content (Endgame Score Gap, Achievable, Parity, Conversion) is unchanged — `git diff` shows only additive changes in the new section.
  </acceptance_criteria>
  <done>The candidacy report now carries 12 calibrated cells; Plan F's tooltip copy can be lifted verbatim from the table.</done>
</task>

</tasks>

<verification>
- Run `grep -E '(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)' reports/benchmarks-gap-metrics-percentile-candidacy.md | grep -v '^#' | wc -l` and confirm the count is ≥ 12.
- Run `grep -c 'Time Pressure metric family' reports/benchmarks-gap-metrics-percentile-candidacy.md` and confirm exactly 1 occurrence (the section header).
- Run `git diff reports/benchmarks-gap-metrics-percentile-candidacy.md` and confirm changes are additive only (no deletions in the existing 4-metric content).
- Open the report and spot-read 3 cells (`time_pressure_score_gap_bullet`, `clock_gap_classical`, `net_flag_rate_bullet`) — confirm tooltip copy is per-cell and not duplicated verbatim across cells.
</verification>

<success_criteria>
- The 12-cell table exists, is grep-discoverable, and Plan F can resolve every `RATING_NOTE_BY_FLAVOR[…]` value from a single read of the report.
- Tier labels come from the existing ladder verbatim.
- Cells with n_users < 150 are flagged for Plan F's awareness (chip-suppression escape hatch per RESEARCH §Pitfall 8).
</success_criteria>

<output>
Create `.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-01-SUMMARY.md` when done. The summary must include:
- The "Verified column names" table from Task 0 (Plan B needs this).
- The full 12-row tier table (copy from the report).
- A "Cells flagged for suppression" subsection listing any cell with n_users < 150 (empty if none).
</output>
