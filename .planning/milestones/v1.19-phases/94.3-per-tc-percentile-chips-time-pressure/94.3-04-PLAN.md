---
phase: 94.3-per-tc-percentile-chips-time-pressure
plan: 04
type: execute
wave: 1
depends_on: [94.3-02, 94.3-03]
files_modified:
  - alembic/versions/(NEW migration file — extend_benchmark_metric_for_tc_pressure)
  - app/models/user_benchmark_percentile.py
  - app/services/user_benchmark_percentiles_service.py
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - frontend/src/types/endgames.ts
  - tests/services/test_user_benchmark_percentiles_service.py
  - tests/services/test_endgame_service.py
autonomous: true
requirements: [SEED-025, TPCTL-02, TPCTL-04, TPCTL-06]

must_haves:
  truths:
    - "A new Alembic migration `alembic/versions/<TS>_extend_benchmark_metric_for_tc_pressure.py` exists, with `down_revision` pointing to the most recent post-94.2 head (verify via `uv run alembic heads` at write time — likely `b8f4d92c1e25` per RESEARCH §Pattern 2). The `upgrade()` body executes 12 `ALTER TYPE benchmark_metric ADD VALUE IF NOT EXISTS '<value>'` statements iterated from a `_NEW_VALUES` tuple. The `downgrade()` body is an intentional no-op stub whose docstring explicitly states: 'Postgres cannot remove ENUM values without a table rewrite. Rollback path is `git revert` of the upgrade migration BEFORE any prod row uses one of the new ENUM values.' This wording is non-negotiable per CONTEXT.md D-7 and RESEARCH §Pattern 2."
    - "`app/models/user_benchmark_percentile.py:benchmark_metric_enum` is widened from 4 to 16 string arguments. `create_type=False` is preserved (Alembic owns the lifecycle)."
    - "`app/services/user_benchmark_percentiles_service.py:STAGE_B_METRICS` tuple widens from 3 to 15 entries: the existing 3 (`achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`) followed by the 12 new per-TC metrics in the order specified by Plan C's IN_SCOPE_METRICS. `STAGE_A_METRIC == 'score_gap'` is UNCHANGED (per RESEARCH §Pitfall 9). `compute_stage_b` and `_compute_metric_for_user` signatures are UNCHANGED."
    - "`app/schemas/endgames.py:TimePressureTcCard` gains exactly 3 new nullable fields with `= None` defaults: `time_pressure_score_gap_percentile: float | None = None`, `clock_gap_percentile: float | None = None`, `net_flag_rate_percentile: float | None = None`. The existing field order and the B-2 lock (default-None for back-compat) are preserved."
    - "`app/services/endgame_service.py:_compute_time_pressure_cards` signature widens to accept `percentile_rows: Mapping[CdfMetricId, PercentileRow] | None = None` (keyword-only). For each per-TC card constructed, the function looks up `{time_pressure_score_gap, clock_gap, net_flag_rate}_{tc}` keys in `percentile_rows` and attaches `<row>.percentile` (or `None` if no row) to the corresponding nullable field. The pattern mirrors `_compute_score_gap_material` lines 1262-1414 verbatim (PATTERNS.md lines 548-672)."
    - "`get_endgame_overview` is updated to thread `percentile_rows` through to `_compute_time_pressure_cards`. The existing fetch site (lines 2801-2806 — `fetch_for_user(session, current_user.id)`) is reused; no new repository call. The 12 new ENUM keys flow through the existing dict automatically (RESEARCH §Pattern 5)."
    - "Frontend type codegen has been run after the schema change. `frontend/src/types/endgames.ts` carries the 3 new nullable fields on `TimePressureTcCard`. RESEARCH §Pitfall 10 is mitigated by running the codegen at the END of this plan, BEFORE Plan F begins."
    - "Unit tests assert: (a) `STAGE_B_METRICS` has length 15; (b) `STAGE_A_METRIC == 'score_gap'`; (c) `_compute_time_pressure_cards(clock_rows, percentile_rows={...})` populates the 3 new percentile fields when the corresponding keys are present in `percentile_rows`, and leaves them `None` otherwise; (d) the migration upgrade + downgrade + upgrade cycle is reversible at the file-loading level (downgrade is a no-op stub, but the migration imports cleanly)."
  artifacts:
    - path: alembic/versions/<TS>_extend_benchmark_metric_for_tc_pressure.py
      provides: ENUM extension migration adding 12 new values to benchmark_metric
      contains: "ALTER TYPE benchmark_metric ADD VALUE"
    - path: app/models/user_benchmark_percentile.py
      provides: widened benchmark_metric_enum descriptor (16 values)
    - path: app/services/user_benchmark_percentiles_service.py
      provides: widened STAGE_B_METRICS tuple (15 entries)
      contains: "STAGE_B_METRICS"
    - path: app/schemas/endgames.py
      provides: 3 new nullable percentile fields on TimePressureTcCard
      contains: "time_pressure_score_gap_percentile"
    - path: app/services/endgame_service.py
      provides: percentile_rows kwarg on _compute_time_pressure_cards + per-card attach loop
    - path: frontend/src/types/endgames.ts
      provides: regenerated TypeScript types covering the 3 new card fields
      contains: "time_pressure_score_gap_percentile"
  key_links:
    - from: app/services/user_benchmark_percentiles_service.py
      to: app/services/canonical_slice_sql.py
      via: per_user_cte_for dispatcher (Plan B)
      pattern: "per_user_cte_for\\("
    - from: app/services/endgame_service.py
      to: app/repositories/user_benchmark_percentiles_repository.py
      via: fetch_for_user returning percentile_rows
      pattern: "fetch_for_user"
    - from: app/services/endgame_service.py
      to: app/schemas/endgames.py
      via: TimePressureTcCard construction with 3 new percentile fields
      pattern: "TimePressureTcCard\\("
---

<objective>
Land the database-and-backend slice of the Wave-1 atomic cutover: extend the `benchmark_metric` Postgres ENUM with 12 new values via a reversible-by-design Alembic migration, widen `STAGE_B_METRICS` from 3 to 15 entries, add 3 new nullable percentile fields to the `TimePressureTcCard` Pydantic schema, attach those percentiles inside `_compute_time_pressure_cards` from the existing `fetch_for_user` percentile_rows dict, and regenerate the frontend TypeScript types so Plan F can begin without compile errors.

Purpose: This is the third commit of the Wave-1 atomic-cutover trio (B → C → D). Plan B made the SQL builders dispatchable; Plan C made the CDF interpolate against them; Plan D makes Stage B compute against them, persist results to `user_benchmark_percentiles` under 12 new ENUM keys, and surface them in the per-card response. The PR encompassing B + C + D ships as one squash-merge so no green commit on `main` holds an incoherent state (RESEARCH §"Wave-1 coordination").

Output: A new Alembic migration; widened ENUM descriptor + STAGE_B_METRICS tuple; 3 new schema fields; new percentile_rows kwarg on `_compute_time_pressure_cards`; regenerated frontend TypeScript types; new unit-test cases covering the widened constants and the attach behaviour. Backfill script extension is Plan E; chip rendering is Plan F.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-CONTEXT.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-02-SUMMARY.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-03-SUMMARY.md
@app/models/user_benchmark_percentile.py
@app/services/user_benchmark_percentiles_service.py
@app/schemas/endgames.py
@app/services/endgame_service.py
@app/repositories/user_benchmark_percentiles_repository.py

<interfaces>
<!-- Existing 94.2 service surface — Plan D widens only the constants and adds the kwarg.
     Extracted from PATTERNS.md §`app/services/user_benchmark_percentiles_service.py` and
     §`app/services/endgame_service.py`. -->

# app/services/user_benchmark_percentiles_service.py
STAGE_A_METRIC: CdfMetricId = "score_gap"
STAGE_B_METRICS: tuple[CdfMetricId, ...] = (
    "achievable_score_gap",
    "section2_score_gap_conv",
    "section2_score_gap_parity",
)
# After Plan D: 15 entries total — append the 12 new IDs in the Plan C grouping order.

async def _compute_metric_for_user(
    session: AsyncSession, user_id: int, metric_id: CdfMetricId,
) -> tuple[float, int] | None:  # SIGNATURE UNCHANGED — 12 new IDs flow through per_user_cte_for

async def compute_stage_b(
    user_id: int, *, session_maker: ... | None = None,
) -> None:  # SIGNATURE UNCHANGED — loops over STAGE_B_METRICS

# app/schemas/endgames.py — current TimePressureTcCard (lines 755-793):
class TimePressureTcCard(BaseModel):
    tc: Literal["bullet", "blitz", "rapid", "classical"]
    total: int
    user_avg_pct: float | None = None
    user_avg_seconds: float | None = None
    opp_avg_pct: float | None = None
    opp_avg_seconds: float | None = None
    avg_clock_diff_seconds: float | None = None
    net_timeout_rate: float = 0.0
    clock_gap: ClockGapBullet
    quintiles: list[PressureQuintileBullet]
    # Plan D adds 3 new nullable fields per PATTERNS.md lines 534-544.

# app/services/endgame_service.py — current _compute_time_pressure_cards signature
# (lines 1889-1961). Plan D widens with `percentile_rows` kwarg per PATTERNS.md lines 622-670.

# Existing fetch site in get_endgame_overview (lines 2801-2821):
percentile_rows = await user_benchmark_percentiles_repository.fetch_for_user(
    session, current_user.id
)
# Plan D adds percentile_rows kwarg to _compute_time_pressure_cards(clock_rows, ...) call.

# app/repositories/user_benchmark_percentiles_repository.py:fetch_for_user returns
# Mapping[CdfMetricId, PercentileRow] — the 12 new keys flow through automatically once
# the ENUM accepts them (RESEARCH §Pattern 5 + Assumption A5).

# app/models/user_benchmark_percentile.py:benchmark_metric_enum (lines 36-43) —
# widen to 16 string args; create_type=False preserved.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Alembic ENUM-extension migration</name>
  <files>alembic/versions/<TS>_extend_benchmark_metric_for_tc_pressure.py (NEW)</files>
  <read_first>
    - alembic/versions/20260524_000000_add_user_benchmark_percentiles.py (ENUM creation pattern)
    - alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py (reversibility-doc style; single-change migration)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`alembic/versions/{TS}_extend_benchmark_metric_for_tc_pressure.py` (lines 418-499)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pattern 2 (lines 391-457)
  </read_first>
  <action>
    Generate the migration file. Determine `down_revision` by running `uv run alembic heads` (do NOT hard-code the revision id from this plan's text — the head may have shifted). Recommended generation method: `uv run alembic revision -m "extend benchmark_metric for TC pressure"` (do NOT use `--autogenerate` — autogenerate does not detect Postgres ENUM additions). Open the generated file and write the migration body following PATTERNS.md §lines 462-497 verbatim. Required elements: (a) module docstring referencing CONTEXT.md D-7; (b) `_NEW_VALUES: tuple[str, ...] = (...)` tuple containing all 12 metric IDs in Plan C's grouping order; (c) `upgrade()` function iterating `for value in _NEW_VALUES: op.execute(f"ALTER TYPE benchmark_metric ADD VALUE IF NOT EXISTS '{value}'")` with a docstring including the exact wording from must_haves §"the `downgrade()` body" about the irreversibility caveat — the docstring MUST literally contain the phrase 'cannot remove ENUM values without a table rewrite' AND the phrase 'git revert' AND 'BEFORE any prod row'. The `downgrade()` body is `pass` with an explanatory docstring referencing the same caveat. Run `uv run alembic upgrade head` against the dev DB to confirm the migration applies cleanly. Run `uv run alembic downgrade -1` (no-op stub completes silently) and `uv run alembic upgrade head` (idempotent — `IF NOT EXISTS` skips duplicates) to confirm the up-down-up cycle is clean.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic current 2>&1 | grep -c 'extend_benchmark_metric_for_tc_pressure\|extend benchmark_metric for TC pressure'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new migration file exists under `alembic/versions/` whose filename contains `extend_benchmark_metric_for_tc_pressure` (or the slug derived from `alembic revision -m` argument).
    - `grep -cE "ALTER TYPE benchmark_metric ADD VALUE" <new migration file>` returns ≥ 1 (the loop body — may render as a single line containing the f-string).
    - The migration's `_NEW_VALUES` tuple contains all 12 expected literals: `grep -cE '"(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)"' <new migration file>` returns 12.
    - The `downgrade` function docstring contains the literal phrases 'cannot remove ENUM values without a table rewrite' AND 'git revert' AND 'BEFORE any prod row' — `grep -c 'cannot remove ENUM values without a table rewrite' <new migration file>` returns ≥ 1; `grep -c 'git revert' <new migration file>` returns ≥ 1; `grep -c 'BEFORE any prod row' <new migration file>` returns ≥ 1.
    - `uv run alembic upgrade head` succeeds against dev DB.
    - `psql` to dev DB `SELECT unnest(enum_range(NULL::benchmark_metric))` returns 16 values (4 existing + 12 new).
    - `uv run alembic downgrade -1` succeeds (no-op stub) and `uv run alembic upgrade head` re-applies idempotently.
  </acceptance_criteria>
  <done>Migration applies cleanly; ENUM accepts 16 values; reversibility caveat documented in code; downgrade no-op stub is intentional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen ORM ENUM descriptor + STAGE_B_METRICS + add Pydantic fields + thread percentile_rows through endgame_service</name>
  <files>app/models/user_benchmark_percentile.py, app/services/user_benchmark_percentiles_service.py, app/schemas/endgames.py, app/services/endgame_service.py, tests/services/test_user_benchmark_percentiles_service.py, tests/services/test_endgame_service.py</files>
  <read_first>
    - app/models/user_benchmark_percentile.py (full file — `benchmark_metric_enum` lines 36-43)
    - app/services/user_benchmark_percentiles_service.py (full file — STAGE_A_METRIC + STAGE_B_METRICS + compute_stage_b loop)
    - app/schemas/endgames.py (lines 740-822 — TimePressureTcCard + TimePressureCardsResponse + EndgameOverviewResponse)
    - app/services/endgame_service.py (lines 1262-1414 for the `_compute_score_gap_material` pattern; lines 1889-1961 for `_compute_time_pressure_cards`; lines 2797-2821 for the existing `fetch_for_user` call + multi-consumer-pass pattern)
    - app/repositories/user_benchmark_percentiles_repository.py (full file — confirm `fetch_for_user` returns `Mapping[CdfMetricId, PercentileRow]`)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`app/models/user_benchmark_percentile.py` (lines 397-415), §`app/services/user_benchmark_percentiles_service.py` (lines 307-394), §`app/schemas/endgames.py` (lines 503-545), §`app/services/endgame_service.py` (lines 548-670)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pattern 4, §Pattern 5, §Pitfall 9
  </read_first>
  <behavior>
    - Test 1: `from app.services.user_benchmark_percentiles_service import STAGE_B_METRICS` returns a tuple of length 15; the tuple contains all 12 new metric IDs.
    - Test 2: `from app.services.user_benchmark_percentiles_service import STAGE_A_METRIC` returns the string `'score_gap'` (UNCHANGED per RESEARCH §Pitfall 9).
    - Test 3: For each of the 12 new metric IDs, `STAGE_A_METRIC != <metric_id>` (the per-TC metrics are NOT in Stage A).
    - Test 4: Building `TimePressureTcCard(tc='bullet', total=100, clock_gap=<...>, quintiles=[...])` succeeds with all 3 new percentile fields defaulting to `None` (B-2 lock preserved — existing keyword fixtures don't break).
    - Test 5: Building `TimePressureTcCard(tc='bullet', total=100, time_pressure_score_gap_percentile=42.0, clock_gap_percentile=None, net_flag_rate_percentile=8.5, clock_gap=<...>, quintiles=[...])` produces a model whose three new fields read back as `42.0`, `None`, `8.5`.
    - Test 6: `_compute_time_pressure_cards(<minimal clock_rows fixture>, percentile_rows=None)` returns a `TimePressureCardsResponse` with each card's 3 new percentile fields equal to `None` (default-when-missing).
    - Test 7: `_compute_time_pressure_cards(<clock_rows fixture covering all 4 TCs>, percentile_rows={'time_pressure_score_gap_bullet': PercentileRow(value=..., percentile=42.0, ...), 'clock_gap_bullet': PercentileRow(value=..., percentile=88.0, ...), 'net_flag_rate_classical': PercentileRow(value=..., percentile=3.0, ...)})` returns a response where the `bullet` card's `time_pressure_score_gap_percentile == 42.0` and `clock_gap_percentile == 88.0` and `net_flag_rate_percentile is None`; the `classical` card's `net_flag_rate_percentile == 3.0` and the other two are `None`; the `blitz` and `rapid` cards all 3 percentile fields are `None`.
    - Test 8: `get_endgame_overview` (or whatever the route handler test calls into) end-to-end with a mocked `fetch_for_user` returning a `Mapping` containing per-TC keys yields a response whose `time_pressure_cards` carries the percentiles on each card (integration test — may live in a different test file; check existing endgame service test layout).
  </behavior>
  <action>
    1. **`app/models/user_benchmark_percentile.py`**: Widen `benchmark_metric_enum` descriptor from 4 to 16 string arguments. Preserve `create_type=False`. Update the docstring (if present) to reference Phase 94.3 per-TC additions.
    2. **`app/services/user_benchmark_percentiles_service.py`**: Append the 12 new metric IDs to `STAGE_B_METRICS` in Plan C's grouping order. Leave `STAGE_A_METRIC` unchanged. Leave `compute_stage_b` and `_compute_metric_for_user` signatures untouched.
    3. **`app/schemas/endgames.py`**: Add 3 new nullable fields to `TimePressureTcCard` per PATTERNS.md lines 534-544 verbatim. Place them at the end of the field list. Add an explanatory comment per the suggested form. Preserve `= None` defaults (B-2 lock).
    4. **`app/services/endgame_service.py`**: Widen `_compute_time_pressure_cards(clock_rows)` to `_compute_time_pressure_cards(clock_rows, *, percentile_rows: Mapping[CdfMetricId, PercentileRow] | None = None)`. Inside the per-TC card loop, build a typed TC-to-metric-key mapping (PATTERNS.md lines 540-544 recommends `TC_TO_METRIC_KEYS: dict[str, tuple[CdfMetricId, CdfMetricId, CdfMetricId]]` — this avoids `# type: ignore[assignment]` on f-string Literal inference). Attach each card's three percentile fields from `percentile_rows.get(...)` per PATTERNS.md lines 624-651. Update the `get_endgame_overview` call site (around line 2820) to pass `percentile_rows=percentile_rows` — the variable already exists from the prior call to `_compute_score_gap_material`.
    5. **Frontend type codegen**: Run the OpenAPI schema dump and frontend codegen — typical commands: `uv run python scripts/generate_openapi_schema.py` then `cd frontend && npm run codegen` (verify the exact script name in `frontend/package.json`; common alternates include `npm run generate-types`). Commit the regenerated `frontend/src/types/endgames.ts`. Confirm via `cd frontend && npx tsc --noEmit` that the frontend type-checks cleanly with the new fields. **This step is the RESEARCH §Pitfall 10 mitigation — Plan F's first task assumes these types already exist.**
    6. **Tests**: Add the 8 unit/integration tests above to `tests/services/test_user_benchmark_percentiles_service.py` and `tests/services/test_endgame_service.py` (split test files by which subject-under-test the case exercises). Use existing fixture patterns. Pay particular attention to Test 7 — it's the structural assertion that the per-TC lookup logic in `_compute_time_pressure_cards` reads the correct keys per card.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_user_benchmark_percentiles_service.py tests/services/test_endgame_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)"' app/models/user_benchmark_percentile.py` returns 12 (descriptor widening).
    - `grep -cE '"(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)"' app/services/user_benchmark_percentiles_service.py` returns ≥ 12 (STAGE_B_METRICS widening).
    - `python -c "from app.services.user_benchmark_percentiles_service import STAGE_A_METRIC, STAGE_B_METRICS; assert STAGE_A_METRIC == 'score_gap'; assert len(STAGE_B_METRICS) == 15"` exits 0.
    - `grep -cE '(time_pressure_score_gap_percentile|clock_gap_percentile|net_flag_rate_percentile)' app/schemas/endgames.py` returns ≥ 3.
    - `grep -c 'percentile_rows' app/services/endgame_service.py` increases (at least one new occurrence in `_compute_time_pressure_cards` signature).
    - `grep -cE '(time_pressure_score_gap_percentile|clock_gap_percentile|net_flag_rate_percentile)' frontend/src/types/endgames.ts` returns ≥ 3 (regenerated TypeScript types include the new fields).
    - `uv run pytest tests/services/test_user_benchmark_percentiles_service.py tests/services/test_endgame_service.py -x` exits 0.
    - `uv run pytest -x` (full backend suite) exits 0.
    - `uv run ty check app/ tests/` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0 (frontend type-checks against the new fields).
    - `uv run ruff check . && uv run ruff format --check .` clean.
    - `cd frontend && npm run lint` clean (no new dead-code warnings from knip — `npm run knip` clean as well).
  </acceptance_criteria>
  <done>ENUM widened, STAGE_B_METRICS at 15 entries, 3 new schema fields wired end-to-end, type codegen complete; Plan F is unblocked.</done>
</task>

</tasks>

<verification>
- `uv run alembic upgrade head` against dev DB succeeds and `psql` confirms 16 ENUM values.
- `uv run pytest -x` (full backend suite) exits 0.
- `uv run ty check app/ tests/` exits 0.
- `cd frontend && npx tsc --noEmit && npm run lint && npm run knip` all clean.
- The 3 new percentile fields appear in `frontend/src/types/endgames.ts` — Plan F can build against them immediately.
- The migration's `downgrade` docstring carries the irreversibility caveat verbatim.
- `STAGE_A_METRIC == 'score_gap'` (unchanged); `len(STAGE_B_METRICS) == 15`.
</verification>

<success_criteria>
- The Wave-1 atomic-cutover trio (B + C + D) collectively produces a coherent system after squash-merge: SQL builders dispatch the 12 new IDs, CDF interpolates them, ENUM accepts them, Stage B persists them, the schema exposes them, and frontend types know about them.
- No incoherent intermediate state exists on `main` after the PR merges.
- Plan E (backfill) and Plan F (frontend chips) are now unblocked.
</success_criteria>

<output>
Create `.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-04-SUMMARY.md` when done. The summary must include:
- The new migration's exact filename + `revision` id + `down_revision` id (so future migrations can reference).
- A confirmation that the downgrade docstring carries the irreversibility caveat verbatim.
- The exact STAGE_B_METRICS tuple as committed (so Plan E's backfill summary can preview the 16-row count).
- A confirmation that `frontend/src/types/endgames.ts` includes the 3 new fields and the frontend tsc compile is clean — Plan F's first task can skip the type-existence check.
</output>
