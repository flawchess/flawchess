---
status: partial
phase: 94.3-per-tc-percentile-chips-time-pressure
source: [94.3-05-PLAN.md Task 3]
started: 2026-05-24T18:00:00Z
updated: 2026-05-24T18:00:00Z
---

## Current Test

[awaiting operator — run after the rest of phase 94.3 wraps]

## Tests

### 1. Production backfill rerun (deferred to user post-phase)
expected: |
  Operator runs `scripts/backfill_user_percentiles.py --target prod` via
  `bin/prod_db_tunnel.sh` against the production PostgreSQL after Plan D's
  ENUM-extension migration has shipped to prod (the migration applies on
  container startup via `deploy/entrypoint.sh` during `bin/deploy.sh`).

  The rerun lights up the 12 new per-(metric × TC) percentile rows for
  existing users. Without it, the chips on Time Pressure cards remain empty
  (`null` percentiles → no chip rendered) for the entire user base — Stage B
  triggers only cover NEW imports + future eval-drain completions, so the
  one-shot backfill is the only mechanism that retro-populates the chips at
  rollout.

  Runbook (mirrors Plan 05 Task 3 `<how-to-verify>`):

  1. Confirm Plan D's migration is live in prod:
     `ssh flawchess "cd /opt/flawchess && docker compose exec backend uv run alembic current"`
     should show the `extend_benchmark_metric_for_tc_pressure` revision
     (`fd5b551f381c`).
  2. Open the prod tunnel: `bin/prod_db_tunnel.sh` (or
     `bin/prod_db_tunnel.sh start`). Confirm `lsof -i :15432` lists the SSH
     process (tunnel up).
  3. Run the prod backfill: `uv run python scripts/backfill_user_percentiles.py --target prod`.
     The run touches the entire prod user base; expect several minutes per
     user × number of users.
  4. Capture the full stdout summary. For each of the 16 metric rows, confirm
     `upserted + total_skipped` equals the total user count processed.
  5. Cross-reference the 12 new per-TC rows against the dev summary captured
     in `94.3-05-SUMMARY.md` (Task 2) and the candidacy report from Plan A:
     per-cell `upserted` counts should be proportional to the cohort sizes
     documented there (prod likely larger than dev).
  6. Close the tunnel: `bin/prod_db_tunnel.sh stop`. Confirm `lsof -i :15432`
     is empty.
  7. Verify the chips render on flawchess.com Time Pressure cards.
  8. Report any cell whose prod `upserted` count is materially below dev's —
     that signals prod-specific cohort thinness (e.g. classical) Plan F (or a
     follow-up) needs to know about for chip-suppression awareness.

  Append the captured stdout + any per-cell concerns to `94.3-05-SUMMARY.md`
  under a "Prod backfill summary" subsection, then flip this entry's `result`
  to "passed".

result: pending — operator chose "skip HUMAN UAT, I'll do it when you're
        finished with the whole phase" at the Plan 05 execute boundary. The
        prod chips will render empty for existing users until this rerun
        completes; required before declaring phase 94.3 user-visible.

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

- The dev backfill (Plan 05 Task 2) has run; the prod backfill has NOT.
- Plan F (frontend chip render) ships safely independent of this UAT: chips
  simply suppress when no percentile row exists. The user experience remains
  correct (no broken state); the chip surface is just empty for existing
  users until the backfill runs.
