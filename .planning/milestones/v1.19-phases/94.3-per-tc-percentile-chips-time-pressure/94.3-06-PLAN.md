---
phase: 94.3-per-tc-percentile-chips-time-pressure
plan: 06
type: execute
wave: 2
depends_on: [94.3-01, 94.3-04]
files_modified:
  - frontend/src/components/charts/PercentileChip.tsx
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
autonomous: true
requirements: [SEED-025, TPCTL-06, TPCTL-07]

must_haves:
  truths:
    - "`PercentileChipFlavor` type union in `frontend/src/components/charts/PercentileChip.tsx` (NOT `frontend/src/components/score-gap/PercentileChip.tsx` — the latter does not exist per RESEARCH.md correction at line 255) widens from 4 to 16 variants. Existing 4 variants (`'score-gap'` | `'achievable'` | `'parity'` | `'conversion'`) are preserved verbatim. The 12 new variants are snake_case literal strings matching the backend metric IDs: `'time_pressure_score_gap_bullet'` through `'net_flag_rate_classical'`. The kebab-case-vs-snake-case mismatch with the existing 4 variants is intentional (RESEARCH §Pattern 7 lines 587-589) — keeps backend↔flavor grep trivial."
    - "A new `DIRECTION_BY_FLAVOR` constant exists in `PercentileChip.tsx`, typed `as const satisfies Record<PercentileChipFlavor, 'higher_is_better' | 'lower_is_better'>`. Exactly 4 entries are `'lower_is_better'` — the four `net_flag_rate_{tc}` flavors per D-2. The other 12 entries are `'higher_is_better'`. The `satisfies` exhaustiveness check is non-negotiable per RESEARCH §Pitfall 3 — failing to use `satisfies` lets new flavors silently miss the map."
    - "Three rendering helpers branch on direction: `deriveBandColor(pct, direction)` returns `ZONE_SUCCESS` at low pct + `ZONE_DANGER` at high pct under `'lower_is_better'` (flipped from the higher-is-better branch); `deriveFlameCount(pct, direction)` uses p1/p5/p10 thresholds under `'lower_is_better'` (flipped from p99/p95/p90); `formatTopXPercent(pct, direction)` returns `Top ${round(pct)}%` under `'lower_is_better'` (vs `Top ${round(100 - pct)}%` for higher-is-better). All three helper signatures gain `direction` as the second argument (PATTERNS.md lines 813-816, RESEARCH §Pattern 7 lines 591-651)."
    - "The chip popover body adds a leading `<p>` line ONLY when the flavor's direction is `'lower_is_better'` — the Net Flag chips ONLY (per D-3). The line reads: `Lower is better — you have fewer net timeouts than ${formatBetterThanPercent(100 - percentile)} of ${tcFromFlavor(flavor)} players.` per RESEARCH §Pattern 7 lines 653-665. A helper `tcFromFlavor(flavor: PercentileChipFlavor): string | undefined` extracts the TC suffix from per-TC flavors and returns `undefined` for the original 4 flavors."
    - "Bullets 1 and 2 of the popover body become TC-scoped for the 12 new flavors. Bullet 1: `Your {metricLabel} is better than {X}% of benchmarked Lichess players in {tc}, all ratings.` Bullet 2: `Uses your most recent 1000 rated games in {tc} (last 36 months), played against opponents of similar strength (+/-100 ELO).` Bullet 3 (filter independence) unchanged. Bullet 4 (rating note) populated per-(metric × TC) from `RATING_NOTE_BY_FLAVOR` widened to 16 entries — copy strings lifted verbatim from Plan A's candidacy report extension (94.3-01-SUMMARY.md). The 4-bullet structure for the original 4 flavors is unchanged."
    - "`EndgameTimePressureCard.tsx` renders 3 chips per card: Clock Gap chip on `ClockGapHeaderRow` (alongside the centre Gap value, after the MetricStatPopover), Net Flag Rate chip at the right edge of `NetFlagRateRow` via `<span className='ml-auto'>`, Time Pressure Score Gap chip at the right edge of the Quintiles subtitle `<p>` line (the existing `inline-flex` may need to become `flex` so `ml-auto` takes effect — verify visually). Each chip is gated on `card.<metric>_percentile != null` — below floor, no chip renders. Per CLAUDE.md browser-automation rules, each chip carries a `data-testid` following the `time-pressure-card-${tc}-{metric}-chip` pattern."
    - "Mobile parity verified at 375px (DevTools viewport). Chip stays visually associated with its metric label across all 4 cards; wrap to a second line is acceptable on narrow widths as long as the chip stays in the same row block."
    - "Vitest test suite covers: (a) 4 direction-inversion cases on a Net Flag flavor — Top X% label flip, band color flip, flame trigger flip, popover 'Lower is better' line presence; (b) 12 per-(metric × TC) tooltip-content cases — bullets 1 and 2 contain the TC name + the bullet 4 copy matches Plan A's tier; (c) chip-render-and-suppression cases on `EndgameTimePressureCard` — when all 3 percentiles are non-null, all 3 chips render; when any percentile is null, that chip is absent; (d) data-testid presence for every chip slot."
    - "If Plan A's candidacy report OR Plan E's prod backfill flagged any per-(metric × TC) cell as low-coverage (n_users < 150 OR upserted_prod materially below dev), the executor applies a chip-suppression escape hatch: either (i) suppress the chip for that specific (metric × TC) cell only via `card.tc !== '<flagged>' && card.<metric>_percentile != null` gate, or (ii) leave it on and surface the concern in the plan summary. The decision is the executor's; whichever path is chosen must be documented."
  artifacts:
    - path: frontend/src/components/charts/PercentileChip.tsx
      provides: widened PercentileChipFlavor (16), DIRECTION_BY_FLAVOR map, direction-branched rendering helpers, Net Flag "Lower is better" prepended popover line, TC-scoped bullets 1 + 2
      contains: "DIRECTION_BY_FLAVOR"
    - path: frontend/src/components/charts/EndgameTimePressureCard.tsx
      provides: 3 chip slot insertions per card (Clock Gap on header row, Net Flag on net-flag row, Score Gap on quintiles subtitle)
      contains: "PercentileChip"
    - path: frontend/src/components/charts/__tests__/PercentileChip.test.tsx
      provides: direction-inversion tests + per-(metric × TC) tooltip-content tests
    - path: frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
      provides: 3-chip-slot render tests + below-floor suppression tests
  key_links:
    - from: frontend/src/components/charts/EndgameTimePressureCard.tsx
      to: frontend/src/components/charts/PercentileChip.tsx
      via: PercentileChip component used 3 times per card
      pattern: "PercentileChip"
    - from: frontend/src/components/charts/PercentileChip.tsx
      to: reports/benchmarks-gap-metrics-percentile-candidacy.md
      via: RATING_NOTE_BY_FLAVOR copy strings lifted from Plan A's 12-cell table
      pattern: "RATING_NOTE_BY_FLAVOR"
---

<objective>
Wire 3 percentile chips onto each of the 4 `TimePressureTcCard` instances (= 12 chip placements total) by widening `PercentileChip` from 4 to 16 flavor variants, adding a flavor-bound `DIRECTION_BY_FLAVOR` axis that flips the chip's text formatter, band color, and flame trigger for the 4 `net_flag_rate_{tc}` variants (D-2), adding a prepended "Lower is better — you have fewer net timeouts than X% of {tc} players." line to the Net Flag chip's popover body (D-3), and making bullets 1 and 2 of every per-TC chip's popover TC-scoped (D-13). Inclusion-floor gating reuses the existing `<...> != null` pattern verbatim. Per-quintile bullet chart beneath each card is untouched (D-15).

Purpose: This plan turns the database+backend wiring (Plans B+C+D) and the operational backfill (Plan E) into a user-visible feature. The chip component already implements the popover shell + flame stack + theme integration; the parameterised extension here is small but covers a 16-variant exhaustiveness contract (RESEARCH §Pitfall 3) — `as const satisfies Record<PercentileChipFlavor, ...>` discipline applies to every flavor-keyed map.

Output: 4 modified files (component + card + 2 test files), 16 chip placements visible at desktop + 375px, ≥ 20 new Vitest cases passing, `knip` and `tsc` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-CONTEXT.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-01-SUMMARY.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-03-SUMMARY.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-04-SUMMARY.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-05-SUMMARY.md
@frontend/src/components/charts/PercentileChip.tsx
@frontend/src/components/charts/EndgameTimePressureCard.tsx
@frontend/src/components/charts/__tests__/PercentileChip.test.tsx
@frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
@frontend/src/types/endgames.ts
@frontend/src/lib/theme.ts

<interfaces>
<!-- Current 94.2 chip surface (PATTERNS.md §`frontend/src/components/charts/PercentileChip.tsx`
     lines 724-849). Plan F widens this surface; existing call sites for the 4 ΔES chips are not
     touched. -->

// Current 94.2 type union (lines 77, 82-106 of PercentileChip.tsx):
export type PercentileChipFlavor = 'score-gap' | 'achievable' | 'parity' | 'conversion';

export const PERCENTILE_METRIC_LABELS = { /* 4 entries */ } as const satisfies Record<PercentileChipFlavor, string>;
const RATING_NOTE_BY_FLAVOR = { /* 4 entries */ } as const satisfies Record<PercentileChipFlavor, string>;

// Current 94.2 helper signatures (lines 119-134) — Plan F adds `direction` arg:
function deriveBandColor(pct: number): string {...}                  // → (pct, direction)
function deriveFlameCount(pct: number): 0 | 1 | 2 | 3 {...}          // → (pct, direction)
function formatTopXPercent(pct: number): string {...}                // → (pct, direction)

// Current 94.2 popover body (lines 136-157) — Plan F extends with prepended Net Flag line
// + TC-scoped bullets 1 + 2 for per-TC flavors.

// Chip props (current public surface — unchanged in Plan F):
export interface PercentileChipProps {
  percentile: number;
  flavor: PercentileChipFlavor;
  metricLabel: string;
  testId?: string;
}

// Card structure (PATTERNS.md §`frontend/src/components/charts/EndgameTimePressureCard.tsx`
// lines 853-981). Three insertion sites:
//   Slot 1 — ClockGapHeaderRow (lines 109-155) — chip inline in the centre cell.
//   Slot 2 — NetFlagRateRow (lines 162-194) — chip right-aligned via `<span className="ml-auto">`.
//   Slot 3 — Quintiles subtitle `<p>` (lines 285-311) — chip right-aligned via `<span className="ml-auto">`;
//            the outer `<p>` may need `inline-flex` → `flex` for `ml-auto` to take effect.

// New chip slot data plumbing (read from card props — fields added by Plan D):
card.clock_gap_percentile          // float | null
card.net_flag_rate_percentile      // float | null
card.time_pressure_score_gap_percentile  // float | null

// Browser-automation data-testid convention (CLAUDE.md):
// `time-pressure-card-${tc}-clock-gap-chip`
// `time-pressure-card-${tc}-net-flag-rate-chip`
// `time-pressure-card-${tc}-time-pressure-score-gap-chip`

// Theme tokens (UNCHANGED — reuse via direction branch):
import { ZONE_SUCCESS, ZONE_DANGER, GAUGE_NEUTRAL } from '@/lib/theme';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen PercentileChipFlavor, add DIRECTION_BY_FLAVOR, branch the 3 rendering helpers on direction</name>
  <files>frontend/src/components/charts/PercentileChip.tsx, frontend/src/components/charts/__tests__/PercentileChip.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/PercentileChip.tsx (full file — particularly type union, `PERCENTILE_METRIC_LABELS`, `RATING_NOTE_BY_FLAVOR`, `deriveBandColor`, `deriveFlameCount`, `formatTopXPercent`)
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx (existing tests — `renderChip` helper, existing describe blocks)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`frontend/src/components/charts/PercentileChip.tsx` (lines 724-849), §`frontend/src/components/charts/__tests__/PercentileChip.test.tsx` (lines 985-1062)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pattern 7 (lines 560-665), §Pitfall 3 (exhaustiveness), §Pitfall 7 (Net Flag 0.0 vs null)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-01-SUMMARY.md (12 tooltip 4th-bullet copy strings)
    - frontend/src/lib/theme.ts (ZONE_SUCCESS, ZONE_DANGER, GAUGE_NEUTRAL token names)
  </read_first>
  <behavior>
    - Test 1: `renderChip(5, 'net_flag_rate_bullet')` produces a chip whose text content contains `Top 5%` (NOT `Top 95%` — direction inverted).
    - Test 2: `renderChip(5, 'net_flag_rate_bullet')` produces a chip whose inline style contains `ZONE_SUCCESS` (green at low raw percentile — good for lower-is-better).
    - Test 3: `renderChip(99, 'net_flag_rate_bullet')` produces a chip whose inline style contains `ZONE_DANGER` (red at high raw percentile — bad).
    - Test 4: `renderChip(1, 'net_flag_rate_bullet')` renders exactly 3 flame icons (p1 → tier 3 under lower-is-better).
    - Test 5: `renderChip(5, 'net_flag_rate_bullet')` renders exactly 2 flame icons (p5 → tier 2).
    - Test 6: `renderChip(50, 'net_flag_rate_bullet')` renders 0 flame icons.
    - Test 7: `renderChip(73, 'score-gap')` continues to render `Top 27%` (higher-is-better path unchanged — regression).
    - Test 8: `renderChip(99, 'score-gap')` continues to render 3 flame icons (regression).
    - Test 9 (popover): `renderChip(20, 'net_flag_rate_bullet')` after hover-popover-open contains the literal text `Lower is better — you have fewer net timeouts than 80% of bullet players.` (the formatBetterThanPercent(100 - 20) = 80%; tcFromFlavor('net_flag_rate_bullet') = 'bullet').
    - Test 10 (popover bullet 1+2 TC scope): `renderChip(40, 'time_pressure_score_gap_blitz')` after popover-open contains the TC name `blitz` in BOTH the metric-comparison sentence (bullet 1) and the recent-games-basis sentence (bullet 2).
    - Test 11 (popover bullet 4 — Plan A copy): `renderChip(40, 'net_flag_rate_bullet')` after popover-open contains the exact 4th-bullet copy string from 94.3-01-SUMMARY.md's `net_flag_rate_bullet` row (the planner reads the cell and asserts substring presence).
    - Test 12 (exhaustiveness, compile-time): `DIRECTION_BY_FLAVOR` typed `as const satisfies Record<PercentileChipFlavor, ...>` — if a future PR adds a 17th flavor variant without updating DIRECTION_BY_FLAVOR, `tsc` MUST fail. (This is a structural test — the type-level guarantee, not runtime; covered by `cd frontend && npx tsc --noEmit` exiting 0 in the success-criteria verification, AND by a runtime test that `Object.keys(DIRECTION_BY_FLAVOR).length === 16`.)
    - Test 13: `renderChip(0, 'net_flag_rate_bullet')` renders `Top 1%` (clamped at MIN_TOP_PERCENT — per RESEARCH §Pitfall 7).
  </behavior>
  <action>
    Widen the `PercentileChipFlavor` type union by appending 12 new snake_case literal strings: `'time_pressure_score_gap_bullet'`, `'time_pressure_score_gap_blitz'`, `'time_pressure_score_gap_rapid'`, `'time_pressure_score_gap_classical'`, `'clock_gap_bullet'`, ..., `'net_flag_rate_classical'` (16 total). Widen `PERCENTILE_METRIC_LABELS` to 16 entries with user-readable names: `'time_pressure_score_gap_bullet': 'Time Pressure Score Gap (bullet)'`, `'clock_gap_classical': 'Clock Gap (classical)'`, `'net_flag_rate_blitz': 'Net Flag Rate (blitz)'`, etc. Preserve `as const satisfies Record<PercentileChipFlavor, string>`. Widen `RATING_NOTE_BY_FLAVOR` to 16 entries — for the 12 new entries, lift copy verbatim from 94.3-01-SUMMARY.md's `Tooltip 4th-bullet copy` column. Preserve the `as const satisfies` exhaustiveness check.

    Add a new constant `DIRECTION_BY_FLAVOR` per PATTERNS.md lines 782-790: object literal with 16 entries; the 4 `net_flag_rate_{tc}` entries map to `'lower_is_better'`; all other entries (4 original ΔES + 4 time_pressure_score_gap_{tc} + 4 clock_gap_{tc}) map to `'higher_is_better'`. Type the constant `as const satisfies Record<PercentileChipFlavor, 'higher_is_better' | 'lower_is_better'>` per RESEARCH §Pitfall 3 — non-negotiable.

    Add a `PercentileChipDirection` type alias if it makes the helper signatures clearer. Branch the three rendering helpers per RESEARCH §Pattern 7 lines 619-651: `deriveBandColor(pct, direction)`, `deriveFlameCount(pct, direction)`, `formatTopXPercent(pct, direction)`. For `lower_is_better`: band thresholds flip (green at low pct, red at high pct), flame thresholds become p1/p5/p10, label uses raw `Math.round(pct)` instead of `Math.round(100 - pct)`. Update the internal chip-render call site to look up `direction = DIRECTION_BY_FLAVOR[flavor]` once and thread it through the three helpers.

    Add a helper `tcFromFlavor(flavor: PercentileChipFlavor): string | undefined` that extracts the TC suffix from per-TC flavors via a regex match or suffix split. For the 4 original ΔES flavors it returns `undefined`. Used by the popover body to render the TC name in bullets 1, 2, and the Net Flag prepended line.

    Update `PercentileChipPopoverBody` per RESEARCH §Pattern 7 lines 656-665 + PATTERNS.md lines 844-849:
    (a) If `DIRECTION_BY_FLAVOR[flavor] === 'lower_is_better'`, prepend a `<p>` line with the D-3 wording: `Lower is better — you have fewer net timeouts than ${formatBetterThanPercent(100 - percentile)} of ${tcFromFlavor(flavor)} players.` This applies only to the 4 Net Flag flavors (the only `lower_is_better` flavors).
    (b) For per-TC flavors (any `tcFromFlavor(flavor)` is truthy), the existing bullet 1 sentence becomes TC-scoped: `Your ${metricLabel} is better than ${formatBetterThanPercent(percentile)} of benchmarked Lichess players in ${tcFromFlavor(flavor)}, all ratings.` Bullet 2 (recent games basis) reads: `Uses your most recent 1000 rated games in ${tcFromFlavor(flavor)} (last 36 months), played against opponents of similar strength (+/-100 ELO).` Recommend adding a `recentGamesBasisFor(tc?: string)` helper that returns the per-TC variant when a TC is passed and the original `COPY_RECENT_GAMES_BASIS` constant when called with no TC — covers both the original 4 flavors and the 12 new ones. Bullets 3 (`COPY_FILTER_INDEPENDENCE`) and 4 (`RATING_NOTE_BY_FLAVOR[flavor]`) unchanged in structure; the 4th now reads per-(metric × TC) copy.

    Add the 13 new test cases to `__tests__/PercentileChip.test.tsx` in a new describe block `'PercentileChip — direction inversion + per-TC popover (Phase 94.3)'`. Reuse the existing `renderChip` helper (or extend its signature to accept a flavor override). For popover-content tests, use `fireEvent.mouseEnter(...)` + waiting per the existing 94.2 pattern (PATTERNS.md lines 1054-1062 + lines 720-743 of RESEARCH).

    Verify `Object.keys(DIRECTION_BY_FLAVOR).length === 16` via a runtime test as belt-and-suspenders alongside the `satisfies` compile-time check.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)'" frontend/src/components/charts/PercentileChip.tsx` returns ≥ 36 (12 entries × 3 maps: PercentileChipFlavor union + PERCENTILE_METRIC_LABELS + RATING_NOTE_BY_FLAVOR + DIRECTION_BY_FLAVOR — so actual count is ~48; assert ≥ 36 to be safe).
    - `grep -c 'DIRECTION_BY_FLAVOR' frontend/src/components/charts/PercentileChip.tsx` returns ≥ 2 (declaration + at least one usage).
    - `grep -cE "satisfies Record<PercentileChipFlavor" frontend/src/components/charts/PercentileChip.tsx` returns ≥ 3 (PERCENTILE_METRIC_LABELS + RATING_NOTE_BY_FLAVOR + DIRECTION_BY_FLAVOR all carry `satisfies` discipline).
    - `grep -c "'lower_is_better'" frontend/src/components/charts/PercentileChip.tsx` returns ≥ 4 (the 4 net_flag_rate entries).
    - `grep -c 'Lower is better' frontend/src/components/charts/PercentileChip.tsx` returns ≥ 1 (the prepended Net Flag line literal).
    - `cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx` exits 0; the new describe block contains at least 13 passing tests.
    - `cd frontend && npx tsc --noEmit` exits 0 (the `satisfies` exhaustiveness check passes).
    - `cd frontend && npm run lint` exits 0.
    - The minimum-font-size rule (CLAUDE.md text-sm floor) is respected — `grep -E '(text-xs|text-\[0\\.7|font-size:\\s*(10|11|12|13)px)' frontend/src/components/charts/PercentileChip.tsx` only matches inside the popover body (CLAUDE.md "hover/tap-activated info tooltips" exception explicitly covers PercentileChip).
  </acceptance_criteria>
  <done>PercentileChip exposes 16 flavors with direction-aware rendering + Net Flag "Lower is better" line + TC-scoped bullets 1 + 2; ≥ 13 new vitest cases pass; tsc/lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire 3 chip slots into EndgameTimePressureCard with mobile-parity verification</name>
  <files>frontend/src/components/charts/EndgameTimePressureCard.tsx, frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (full file — ClockGapHeaderRow lines 93-156, NetFlagRateRow lines 162-194, Quintiles subtitle lines 285-311; also confirm card props are typed with the new percentile fields from Plan D)
    - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx (existing fixture builders lines 49-80; existing `screen.getByTestId` / `queryByTestId` patterns)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`frontend/src/components/charts/EndgameTimePressureCard.tsx` (lines 853-981), §`__tests__/EndgameTimePressureCard.test.tsx` (lines 1066-1093)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pattern 7 (lines 678-714), §Pitfall 7 (0.0 vs null)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-04-SUMMARY.md (confirm frontend types include the 3 new fields)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-01-SUMMARY.md "Cells flagged for suppression"
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-05-SUMMARY.md "Cells flagged for suppression in production"
  </read_first>
  <behavior>
    - Test 1: Card with all 3 percentile fields non-null renders all 3 chips — `getByTestId('time-pressure-card-bullet-clock-gap-chip')`, `getByTestId('time-pressure-card-bullet-net-flag-rate-chip')`, `getByTestId('time-pressure-card-bullet-time-pressure-score-gap-chip')` all return DOM elements.
    - Test 2: Card with `clock_gap_percentile === null` and other two non-null suppresses ONLY the Clock Gap chip — `queryByTestId(...clock-gap-chip)` is null; the other two chips render.
    - Test 3: Card with all 3 percentile fields null suppresses all 3 chips — all 3 `queryByTestId(...)` calls return null.
    - Test 4: Card with `net_flag_rate_percentile === 0` (NOT null) renders the Net Flag chip — RESEARCH §Pitfall 7 — `getByTestId(...net-flag-rate-chip)` returns a DOM element; the chip label clamps to `Top 1%` per Task 1's MIN_TOP_PERCENT behavior.
    - Test 5: For each of the 4 TCs (bullet, blitz, rapid, classical), render the card with all 3 percentiles non-null and assert the 3 chips are present per TC — 12 placement assertions total.
    - Test 6: Card with TC === 'classical' AND `clock_gap_percentile` non-null renders the Clock Gap chip UNLESS Plan A or Plan E flagged `clock_gap_classical` for suppression — in which case the executor decided to either suppress (chip absent — adjust this test to `queryByTestId(...) === null`) or leave on (chip present). The plan summary documents which path was chosen.
  </behavior>
  <action>
    Modify `EndgameTimePressureCard.tsx`:
    1. **Slot 1 (Clock Gap chip on `ClockGapHeaderRow`)** — Inside the centre `<span className="text-center flex items-center justify-center gap-1">` cell (lines ~109-150), after the existing `MetricStatPopover`, add the conditional chip render per PATTERNS.md lines 887-897. Wrap in `{card.clock_gap_percentile != null && ( <PercentileChip percentile={card.clock_gap_percentile} flavor={\`clock_gap_${card.tc}\`} metricLabel="Clock Gap" testId={\`time-pressure-card-${card.tc}-clock-gap-chip\`} /> )}`. The `flavor` template literal needs a `cast`/`as` to PercentileChipFlavor — TypeScript can't infer Literal types from template-literal concatenation in older versions; if `tsc` complains, use a typed dispatch `const CLOCK_GAP_FLAVOR_BY_TC: Record<TimePressureTcCard['tc'], 'clock_gap_bullet'|'clock_gap_blitz'|'clock_gap_rapid'|'clock_gap_classical'> = { bullet: 'clock_gap_bullet', ... }` (PATTERNS.md §Pattern 7 trade-off).
    2. **Slot 2 (Net Flag chip on `NetFlagRateRow`)** — Inside the row's outer flex container (lines ~162-194), after the existing label + value + info popover trigger, append `{card.net_flag_rate_percentile != null && ( <span className="ml-auto"><PercentileChip ... /></span> )}` per PATTERNS.md lines 926-938. Use the same typed dispatch pattern for the flavor literal if needed.
    3. **Slot 3 (Time Pressure Score Gap chip on Quintiles subtitle)** — Inside the subtitle `<p>` (lines ~285-311), after the existing `InfoPopover`, append `{card.time_pressure_score_gap_percentile != null && ( <span className="ml-auto"><PercentileChip ... /></span> )}` per PATTERNS.md lines 968-981. If the `inline-flex` container does not honor `ml-auto`, change it to `flex` and verify with the desktop screenshot that the prose still flows correctly.

    All three chips carry `data-testid` per the naming convention (CLAUDE.md browser-automation rule).

    **Conditional chip suppression escape hatch (per RESEARCH §Pitfall 8)**: If Plan A's 94.3-01-SUMMARY.md OR Plan E's 94.3-05-SUMMARY.md flagged a (metric × TC) cell as low-coverage, the executor decides whether to suppress at that cell explicitly (e.g. `card.tc !== 'classical' && card.clock_gap_percentile != null` — gates render). Whichever decision is made, document it in 94.3-06-SUMMARY.md so the planning trail is clear.

    Extend `__tests__/EndgameTimePressureCard.test.tsx` with a `makeCard(overrides?: Partial<TimePressureTcCard>)` helper that defaults the 3 new percentile fields to `null` (chip-suppressed default). Add the 6+ test groups described in `<behavior>`. For Test 4 (the 0 vs null distinction), pass `net_flag_rate_percentile: 0` explicitly and assert the chip renders — this is the regression coverage for RESEARCH §Pitfall 7.

    Manual mobile-parity verification (recorded in the plan summary): open the dashboard at desktop 1440px and DevTools viewport 375px. Navigate to the endgame page for a user with full per-TC data. Confirm all 12 chip placements render and each chip stays visually associated with its metric label. Wrap-to-second-line at 375px is acceptable as long as the chip stays in the same row block as the label.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx</automated>
    <human-check>The plan summary contains a screenshot reference OR a one-paragraph note from the operator confirming mobile parity at 375px across all 4 cards. The operator may also note any chip-suppression decision (per Pitfall 8 escape hatch) and the cells affected.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'time-pressure-card-\${card\.tc}-(clock-gap|net-flag-rate|time-pressure-score-gap)-chip' frontend/src/components/charts/EndgameTimePressureCard.tsx` returns ≥ 3 (one testid per chip slot, three chip slots).
    - `grep -c 'PercentileChip' frontend/src/components/charts/EndgameTimePressureCard.tsx` increases by ≥ 3 (three new chip render invocations).
    - `grep -cE '(card\.clock_gap_percentile|card\.net_flag_rate_percentile|card\.time_pressure_score_gap_percentile)\s*!=\s*null' frontend/src/components/charts/EndgameTimePressureCard.tsx` returns ≥ 3 (three explicit null-gates — the suppression contract).
    - `cd frontend && npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` exits 0; the file gains at least 12 new test cases (3 chips × 4 TCs minimum).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run lint && npm run knip` both clean.
    - Mobile parity at 375px verified — the plan summary contains the operator's note or screenshot reference.
  </acceptance_criteria>
  <done>3 chips × 4 cards = 12 chip placements visible; null-gating suppresses chips below the inclusion floor; mobile parity at 375px confirmed; ≥ 12 new vitest cases pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run` (full frontend suite) exits 0.
- `cd frontend && npm run lint && npm run knip && npx tsc --noEmit` all clean.
- `uv run pytest -x && uv run ty check app/ tests/ && uv run ruff check . && uv run ruff format --check .` all clean (backend regression safety after the type-codegen rerun in Plan D).
- Browse the running dashboard at `http://localhost:5173/` (or whatever the dev port is per `bin/run_local.sh`); navigate to a seeded user with full data; visually confirm 12 chip placements at desktop 1440px and at DevTools 375px.
- The Net Flag chip's popover (hover the chip) shows the "Lower is better — …" line FIRST, then the 4 standard bullets.
- The Time Pressure Score Gap chip's popover (hover) shows TC-scoped bullets 1 and 2 (`in bullet`, `most recent 1000 games in bullet`).
- The Bullet 4 per-chip copy matches the corresponding cell in `reports/benchmarks-gap-metrics-percentile-candidacy.md`.
</verification>

<success_criteria>
- 12 chip placements (3 chips × 4 cards) render at desktop + 375px with mobile parity.
- Direction inversion is structurally correct for the 4 Net Flag chips (band color flipped, flame thresholds flipped, Top X% label flipped).
- 4-bullet tooltip disclosure satisfied for all 12 per-TC chips; Net Flag chips prepend the D-3 "Lower is better" line; bullets 1 + 2 are TC-scoped.
- `as const satisfies Record<PercentileChipFlavor, ...>` discipline protects future flavor additions from silently missing the maps.
- `knip` / `tsc` / `lint` / vitest all clean.
- Chip-suppression escape hatch (RESEARCH §Pitfall 8) was either applied (with affected cells documented) or explicitly waived (with the affected cells documented as acceptable).
</success_criteria>

<output>
Create `.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-06-SUMMARY.md` when done. The summary must include:
- A confirmation that all 4 TimePressureTcCard instances render 3 chips at desktop + 375px (operator's note or screenshot reference).
- A list of any (metric × TC) cells where the executor decided to suppress chips (with rationale tied to Plan A's flagged cells or Plan E's prod backfill output). Empty if no suppressions applied.
- A confirmation that the Net Flag chip's popover prepends the "Lower is better" line for all 4 net_flag_rate_{tc} flavors.
- A confirmation that bullet 4 copy for each of the 12 new flavors was lifted verbatim from `reports/benchmarks-gap-metrics-percentile-candidacy.md` per Plan A's table.
- Total new vitest cases added across PercentileChip.test.tsx and EndgameTimePressureCard.test.tsx (target: ≥ 25 combined).
</output>
