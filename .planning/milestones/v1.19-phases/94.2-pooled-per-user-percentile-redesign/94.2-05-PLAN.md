---
phase: 94.2-pooled-per-user-percentile-redesign
plan: 05
type: execute
wave: 2
depends_on: [94.2-01, 94.2-02, 94.2-03]
files_modified:
  - frontend/src/components/charts/PercentileChip.tsx
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
autonomous: true
requirements: [PCTL-08, PCTL-09]

must_haves:
  truths:
    - "PercentileChip popover body discloses the 4 D-4 bullets for the metric: benchmark composition, recent-games basis, filter independence, per-metric rating-correlation framing calibrated per Cohen's d (0.19 / 0.32 / 0.30 / 1.37)."
    - "PercentileChip API gains a flavor variant for each of the 4 chipped metrics so the per-metric rating-correlation framing differs: score_gap (d=0.19) no rating-correlation note; achievable (d=0.32) soft note; parity (d=0.30) soft note; conv (d=1.37) honest 'tracks rating strongly' note."
    - "Call sites: 4 chips total — 2 in EndgameOverallPerformanceSection.tsx (Achievable + Endgame Score Gap), 2 in EndgameMetricCard.tsx (Parity + Conversion). Each call site passes the metric-correct flavor."
    - "PercentileChip frontend logic (chip rendering, banded colors, flame tiers, hover/tap mechanics) is unchanged per D-14. Only popover body copy strings move (and the flavor enum widens to 4 variants)."
  artifacts:
    - path: frontend/src/components/charts/PercentileChip.tsx
      provides: 4-flavor popover body with per-metric rating-correlation framing
      contains: "PercentileChipFlavor"
    - path: frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
      provides: 2 chip call sites with per-metric flavor
    - path: frontend/src/components/charts/EndgameMetricCard.tsx
      provides: 2 chip call sites with per-metric flavor (one per bucket — parity / conversion)
    - path: frontend/src/components/charts/__tests__/PercentileChip.test.tsx
      provides: popover body assertions for all 4 flavor variants
  key_links:
    - from: frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
      to: frontend/src/components/charts/PercentileChip.tsx
      via: <PercentileChip flavor=... />
      pattern: "PercentileChip"
    - from: frontend/src/components/charts/EndgameMetricCard.tsx
      to: frontend/src/components/charts/PercentileChip.tsx
      via: <PercentileChip flavor=... />
      pattern: "PercentileChip"
---

<objective>
Rewrite the PercentileChip popover body to disclose the four D-4 bullets per metric (benchmark composition, recent-games basis, filter independence, per-metric rating-correlation framing). Extend the `PercentileChipFlavor` type to carry exactly 4 metric-specific variants matching the four chipped metrics (Endgame Score Gap, Achievable Score Gap, Parity ΔES, Conversion ΔES) with rating-correlation framing calibrated per Cohen's d (per CONTEXT D-4 / `reports/benchmarks-gap-metrics-percentile-candidacy.md`). Update the 4 call sites — 2 in `EndgameOverallPerformanceSection.tsx` (Achievable + Endgame Score Gap), 2 in `EndgameMetricCard.tsx` (Parity + Conversion) — to pass the metric-correct flavor. Update unit tests.

Purpose: The chip's popover body IS the methodology-disclosure surface (per `feedback_percentile_chip_tooltip_disclosure` — the sanctioned exception to `feedback_popover_copy_minimalism`). Without per-metric framing a 1200 user reads a Conversion-Score-Gap percentile as a skill judgment it can't make. The frontend logic doesn't move per D-14; this is a pure copy + flavor-enum widening.

Output: Updated PercentileChip with 4-flavor copy, updated 4 call sites, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md
@reports/benchmarks-gap-metrics-percentile-candidacy.md
@frontend/src/components/charts/PercentileChip.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/__tests__/PercentileChip.test.tsx

<interfaces>
<!-- Current PercentileChip surface (frontend/src/components/charts/PercentileChip.tsx):
       export type PercentileChipFlavor = 'skill-isolating' | 'improvement-focus';
       export interface PercentileChipProps {
         percentile: number;
         flavor: PercentileChipFlavor;
         metricLabel: string;
         testId: string;
       }

     CURRENT call sites use the 2-flavor enum:
     - EndgameOverallPerformanceSection.tsx line 187: flavor="skill-isolating" (Achievable)
     - EndgameOverallPerformanceSection.tsx line 237: flavor="skill-isolating" (Endgame Score Gap)
     - EndgameMetricCard.tsx line ~233: flavor passed dynamically per bucket

     POST-94.2 surface (this plan):
       export type PercentileChipFlavor = 'score-gap' | 'achievable' | 'parity' | 'conversion';
     OR (more semantic):
       export type PercentileChipFlavor = 'rating-invariant' | 'mild-rating-coupling' | 'small-effect' | 'heavy-rating-proxy';
     PLANNER CHOICE: prefer metric-named flavors ('score-gap' / 'achievable' / 'parity' / 'conversion') for direct grep-ability with backend metric IDs.
-->

<!-- Per-metric Cohen's d (from reports/benchmarks-gap-metrics-percentile-candidacy.md):
       score_gap                  d = 0.19  (rating-invariant; no special note)
       achievable_score_gap       d = 0.32  (mild coupling; soft mention)
       section2_score_gap_parity  d = 0.30  (small effect; soft mention)
       section2_score_gap_conv    d = 1.37  (heavy rating-proxy; honest framing) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen PercentileChipFlavor to 4 metric-named variants and rewrite popover body copy per D-4</name>
  <files>frontend/src/components/charts/PercentileChip.tsx</files>
  <read_first>
    - frontend/src/components/charts/PercentileChip.tsx (full file — current 2-flavor implementation)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-4, D-14)
    - reports/benchmarks-gap-metrics-percentile-candidacy.md (per-metric Cohen's d source of truth)
    - CLAUDE.md §Frontend (text-xs allowed inside hover/tap-activated info tooltips — PercentileChip popover body is explicitly listed)
  </read_first>
  <behavior>
    - The popover body displays four content blocks for every flavor:
      1. Benchmark composition line ("vs benchmarked Lichess players of all ratings and time controls" or equivalent).
      2. Recent-games basis line ("most recent 1000 games per time control played, last 36 months" or equivalent).
      3. Filter independence line ("UI filters do not affect this percentile").
      4. Per-metric rating-correlation framing — different per flavor:
         - score-gap (d=0.19): no rating-correlation paragraph (skip the fourth block or use a neutral paragraph like "Reflects endgame ability separate from overall strength.").
         - achievable (d=0.32): soft rating mention ("Achievable Score Gap mildly correlates with rating.").
         - parity (d=0.30): soft rating mention ("Parity ΔES mildly correlates with rating.").
         - conversion (d=1.37): honest framing ("Conversion ΔES tracks rating strongly — stronger players tend to score higher because they blunder less when up material.").
    - Test 1: Rendering a chip with flavor='score-gap' opens a popover whose body contains the benchmark-composition line, the recent-games-basis line, and the filter-independence line.
    - Test 2: Rendering with flavor='conversion' opens a popover containing all four blocks including the "tracks rating strongly" framing.
    - Test 3: Rendering with flavor='achievable' OR flavor='parity' contains the soft rating-coupling mention (not the heavy-rating-proxy line).
    - Test 4: Calling `<PercentileChip flavor="skill-isolating" ... />` (the old 2-flavor enum value) is a TypeScript error at build time (the type narrowed).
    - Test 5: PercentileChipProps still requires `percentile`, `flavor`, `metricLabel`, `testId` (no shape change beyond flavor).
  </behavior>
  <action>
    Refactor PercentileChip.tsx:

    1. **Widen the flavor type**: replace `export type PercentileChipFlavor = 'skill-isolating' | 'improvement-focus';` with `export type PercentileChipFlavor = 'score-gap' | 'achievable' | 'parity' | 'conversion';`. The four flavor names map 1:1 to the four chipped metric IDs (`score_gap`, `achievable_score_gap`, `section2_score_gap_parity`, `section2_score_gap_conv`) for grep-ability.

    2. **Add module-level copy constants** (CLAUDE.md no-magic-strings; one constant per content block):
       - `COPY_BENCHMARK_COMPOSITION`: "Compared against benchmarked Lichess players of all ratings and time controls."
       - `COPY_RECENT_GAMES_BASIS`: "Uses your most recent 1000 games per time control played, last 36 months."
       - `COPY_FILTER_INDEPENDENCE`: "UI filters do not affect this percentile."
       - `COPY_RATING_NOTE_SCORE_GAP`: null (no rating note) OR "Reflects endgame ability mostly independent of rating." (planner picks; recommend the latter for visual symmetry with the other three flavors — keeps the 4-paragraph structure consistent).
       - `COPY_RATING_NOTE_ACHIEVABLE`: "Achievable Score Gap mildly correlates with rating."
       - `COPY_RATING_NOTE_PARITY`: "Parity ΔES mildly correlates with rating."
       - `COPY_RATING_NOTE_CONVERSION`: "Conversion ΔES tracks rating strongly — stronger players tend to score higher because they blunder less when up material."

    3. **Rewrite `PercentileChipPopoverBody`**: switch on flavor; render the four content blocks as separate `<p>` elements (the popover content already has `text-xs` and `max-w-xs` constraints — per CLAUDE.md exception, text-xs is allowed inside PercentileChip popovers). Layout suggestion: stack four `<p>` blocks with `mb-1.5` between them (read existing layout patterns from the current file's `<p>` styling).

       ```tsx
       function PercentileChipPopoverBody({ flavor }: { flavor: PercentileChipFlavor }): React.ReactElement {
         const ratingNote = ratingNoteForFlavor(flavor);
         return (
           <div className="space-y-1.5">
             <p>{COPY_BENCHMARK_COMPOSITION}</p>
             <p>{COPY_RECENT_GAMES_BASIS}</p>
             <p>{COPY_FILTER_INDEPENDENCE}</p>
             {ratingNote != null && <p>{ratingNote}</p>}
           </div>
         );
       }
       ```

       Helper `ratingNoteForFlavor(flavor)` returns the per-metric COPY_RATING_NOTE_* constant or null for score-gap.

    4. **Comment block at module top**: add a brief comment block referencing D-4 and `feedback_percentile_chip_tooltip_disclosure.md`:
       ```
       // Phase 94.2 (D-4): popover body discloses 4 bullets per metric —
       // benchmark composition, recent-games basis, filter independence,
       // per-metric rating-correlation framing calibrated per Cohen's d
       // (reports/benchmarks-gap-metrics-percentile-candidacy.md). This is
       // the sanctioned exception to feedback_popover_copy_minimalism
       // (see feedback_percentile_chip_tooltip_disclosure.md).
       ```

    5. **DO NOT TOUCH** `deriveBandColor`, `deriveFlameCount`, `formatTopXPercent`, the Popover hover-mechanics, the chip trigger element, or any other render logic — D-14 explicitly fences chip rendering as unchanged.

    6. **Type safety** (CLAUDE.md `noUncheckedIndexedAccess` is enabled): the flavor switch is on a `Literal` union, exhaustive over 4 values. Use a `satisfies Record<PercentileChipFlavor, string | null>` typed object for the rating-note lookup to get exhaustiveness checking from the compiler.

    7. **Minimum font size exception**: the `text-xs` on the popover Content remains (CLAUDE.md explicit exception for hover/tap-activated info tooltips). Do not change to `text-sm` — the popover is dense by design.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'score-gap'\|'achievable'\|'parity'\|'conversion'" frontend/src/components/charts/PercentileChip.tsx` ≥ 4 (flavor type lists all 4).
    - `grep -c "'skill-isolating'\|'improvement-focus'" frontend/src/components/charts/PercentileChip.tsx` returns 0 (old flavor names gone).
    - `grep -c "COPY_BENCHMARK_COMPOSITION\|COPY_RECENT_GAMES_BASIS\|COPY_FILTER_INDEPENDENCE" frontend/src/components/charts/PercentileChip.tsx` ≥ 3.
    - `cd frontend && npx tsc --noEmit` exits 0 (proves the flavor-type widening type-checks against itself; downstream call sites are updated in Task 2 — TS error here means the chip itself is internally consistent only).
    - `cd frontend && npm run lint` exits 0 (knip is not run yet — wait until Task 2 / 3 to confirm exports are wired).
  </acceptance_criteria>
  <done>PercentileChip flavor enum widened to 4 metric-named variants; popover body discloses 4 D-4 bullets per metric with per-metric rating-correlation calibration; chip rendering logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update call sites in EndgameOverallPerformanceSection.tsx and EndgameMetricCard.tsx</name>
  <files>frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/charts/EndgameMetricCard.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (PercentileChip call sites at lines ~184 and ~234; current flavor='skill-isolating')
    - frontend/src/components/charts/EndgameMetricCard.tsx (PercentileChip call site at line ~233; current flavor dispatched per bucket — need to inspect the dispatch logic)
    - frontend/src/components/charts/PercentileChip.tsx (post-Task 1 — new flavor type)
  </read_first>
  <action>
    Update the 4 call sites to pass the new metric-named flavor:

    1. **EndgameOverallPerformanceSection.tsx**:
       - Line ~187: `flavor="skill-isolating"` for Achievable Score Gap → `flavor="achievable"`.
       - Line ~237: `flavor="skill-isolating"` for Endgame Score Gap → `flavor="score-gap"`.

    2. **EndgameMetricCard.tsx**:
       - Locate the `PercentileChip` call at ~line 233 with `flavor={scoreGapPercentile ...}` dispatch.
       - The card renders both the Parity bucket and the Conversion bucket. Inspect how the flavor is currently chosen (the existing dispatch probably uses `bucket === 'parity' ? 'skill-isolating' : 'improvement-focus'` or similar).
       - Rewrite the dispatch: `bucket === 'parity' ? 'parity' : 'conversion'` (or whatever the bucket variable in scope is — read the file to find the correct identifier).
       - If the dispatch lives in a parent component (props chain), trace upward via grep and update at the source.

    3. **Do NOT touch any other props** on the PercentileChip calls — `percentile`, `metricLabel`, `testId` stay verbatim.

    4. **CLAUDE.md "Always apply changes to mobile too"**: the chip already renders identically across desktop / mobile (no separate mobile copy of the call site exists for these two components — verified during phase research). No additional changes needed.

    5. **Knip cleanup**: after all 4 call sites are updated, run `cd frontend && npx knip` to confirm no dead exports — the old flavor literal strings should be gone.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npx knip</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flavor=\"skill-isolating\"\|flavor=\"improvement-focus\"\|flavor='skill-isolating'\|flavor='improvement-focus'" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx frontend/src/components/charts/EndgameMetricCard.tsx` returns 0.
    - `grep -c "flavor=\"score-gap\"\|flavor=\"achievable\"" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` returns 2.
    - `grep -c "'parity'\|'conversion'\|\"parity\"\|\"conversion\"" frontend/src/components/charts/EndgameMetricCard.tsx` shows at least one literal pair (the bucket-to-flavor dispatch).
    - `cd frontend && npx tsc --noEmit` exits 0 (no TS errors anywhere in the project — the flavor-type widening propagates cleanly to all call sites).
    - `cd frontend && npx knip` exits 0 (no dead exports introduced).
  </acceptance_criteria>
  <done>All 4 PercentileChip call sites pass metric-named flavors; old flavor strings purged from the codebase; TS + knip green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update PercentileChip.test.tsx popover-body assertions for 4 flavors</name>
  <files>frontend/src/components/charts/__tests__/PercentileChip.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx (existing 2-flavor tests; replace with 4-flavor assertions)
    - frontend/src/components/charts/PercentileChip.tsx (post-Task 1 — copy constants to assert against)
  </read_first>
  <behavior>
    - Replace any reference to flavor='skill-isolating' or flavor='improvement-focus' in the existing tests.
    - Add `it.each([...])` parametrized tests over the 4 flavor values that each:
      1. Render the chip with that flavor.
      2. Open the popover (`fireEvent.mouseEnter`).
      3. Use `screen.findByText(...)` to assert presence of the 3 always-present strings (benchmark composition, recent-games basis, filter independence).
    - Per-flavor assertion: render with flavor='conversion' and assert the popover body contains the literal substring "tracks rating strongly".
    - Per-flavor assertion: render with flavor='achievable' AND with flavor='parity', assert each popover body contains "mildly correlates with rating" (or the equivalent soft-mention substring chosen in Task 1).
    - Preserve the existing band-color, flame-tier, label-formatter tests verbatim — those exercise the unchanged render logic per D-14.
  </behavior>
  <action>
    Edit the test file:
    1. Update the `renderChip` helper signature to accept `flavor: PercentileChipFlavor` defaulting to `'score-gap'` (was `'skill-isolating'`).
    2. Replace any existing flavor-routed body tests with the new 4-flavor parametrized structure.
    3. Use `screen.findByText` (case-insensitive substring matching via regex if exact-match feels fragile — but prefer exact-match against the copy constants imported from PercentileChip.tsx, so the test breaks if copy drifts unintentionally).
    4. Add a TypeScript-only check (no runtime test needed): the test file imports `PercentileChipFlavor` from PercentileChip.tsx; if the type is `'score-gap' | 'achievable' | 'parity' | 'conversion'` the parametrized values type-check; if Task 1 left the old names in place, the tsc step in the verify command will fail.
    5. Preserve every test outside the popover-body block (band color, flame count, label format, aria-label, data-testid) verbatim.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx` exits 0.
    - `grep -c "'score-gap'\|'achievable'\|'parity'\|'conversion'" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` ≥ 4.
    - `grep -c "tracks rating strongly\|mildly correlates" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` ≥ 2.
    - `cd frontend && npm test -- --run` (full frontend suite) exits 0 — the call-site components in Task 2 still render correctly under the new flavor API.
  </acceptance_criteria>
  <done>Test suite green; popover body assertions cover all 4 flavors; band-color / flame-tier / label-format tests preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → PercentileChip popover | Static rendered copy; no user input flows in this surface. The `percentile` numeric value is server-emitted, no PII in copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.2-05-01 | Information Disclosure (misleading copy) | Popover body wording | mitigate | Per-metric rating-correlation framing calibrated to Cohen's d from `reports/benchmarks-gap-metrics-percentile-candidacy.md`. Conversion metric receives the honest "tracks rating strongly" framing; a 1200 user is not misled into reading a heavy-rating-proxy score as a pure skill judgment. (D-4) |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run` exits 0.
- `cd frontend && npx tsc --noEmit` exits 0 (flavor-type widening propagates cleanly).
- `cd frontend && npx knip` exits 0 (no dead exports).
- Manual visual spot-check: hover or tap each of the 4 chips in a local dev server; confirm the popover body shows the 4 D-4 bullets and the per-metric rating-correlation framing differs as specified. (Operator step — does not gate plan completion since the unit test asserts the strings.)
</verification>

<success_criteria>
PercentileChipFlavor widened to 4 metric-named variants; popover body discloses the 4 D-4 bullets with per-metric rating-correlation calibration; all 4 call sites updated; chip rendering logic unchanged per D-14; tests green; type system + knip green.
</success_criteria>

<output>
Create `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-05-SUMMARY.md` when done.
</output>
