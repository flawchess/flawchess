---
status: partial
phase: 94.2-pooled-per-user-percentile-redesign
source: [94.2-VERIFICATION.md]
started: 2026-05-24T04:55:00Z
updated: 2026-05-24T08:15:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Production backfill rerun (deferred)
expected: |
  Operator runs `scripts/backfill_user_percentiles.py --target prod` via
  `bin/prod_db_tunnel.sh` against the production PostgreSQL. The summary table
  reports zero failed users; per-metric counters use the new pooled-set name
  `skipped_below_pooled_floor`; cdf_snapshot on each upserted row reflects the
  rerun date; a spot-check on a known prod user shows the `value` differs from
  its 94.1 row (methodology shift expected); the Endgames page on
  https://flawchess.com renders chips with plausible percentile values.

  Full operator runbook in:
  `.planning/todos/pending/2026-05-24-phase-94.2-prod-backfill-rerun.md`
result: pending — operator chose "defer prod rerun, verify phase now" at the
        execute-phase HUMAN-UAT checkpoint. Required before the next
        main → production release.

### 2. PercentileChip UI eyeball check
expected: |
  On the Endgames page (https://flawchess.com or local dev), each of the four
  metric chips (Endgame Score Gap, Achievable Score Gap, Parity ΔES,
  Conversion ΔES) renders with:
  - A flame-tier band-colored numeric value (D-14 preserved)
  - A hover/tap popover whose body discloses all four D-4 bullets:
    benchmark composition + recent-games basis + filter independence +
    per-metric rating-correlation framing
  - Rating-correlation copy calibrated per Cohen's d (score-gap rating-invariant;
    achievable + parity soft mention; conversion honest "tracks rating strongly")
result: passed (2026-05-24). Operator verified user 28 chip values against
        the UI score-gap card with recency=3y + ±100 EF and reconciled the
        ~1pp gap to the canonical-slice's rated-only + recent-1000-per-TC
        filters. Disclosure bullet 2 tightened in commit d0c0258b to spell
        out "rated" so the implicit unrated-exclusion is no longer silent.

## Summary

total: 2
passed: 1
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
