---
phase: 94.2-pooled-per-user-percentile-redesign
verified: 2026-05-24T06:41:48Z
status: human_needed
score: 8/8 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Production backfill rerun of scripts/backfill_user_percentiles.py --target prod"
    expected: "All existing prod users' user_benchmark_percentiles rows are recomputed with pooled values and the new n_games column populated (currently default-0 from the migration)"
    why_human: "Tracked HUMAN-UAT step explicitly deferred by operator. Captured at .planning/todos/pending/2026-05-24-phase-94.2-prod-backfill-rerun.md (resolves_phase: 94.2). Must run before next main→production release; requires bin/prod_db_tunnel.sh and operator-controlled execution. Per verification context this is NOT a phase gap."
  - test: "Eyeball the 4 PercentileChip flavors in the running UI"
    expected: "Each metric chip on the Endgame Overview shows the 4-bullet popover with the correct per-metric rating-correlation note (score-gap=rating-invariant, achievable/parity=mild, conversion=tracks rating strongly)"
    why_human: "Visual/UX verification — automated tests assert string presence but humans confirm the popover renders correctly, the trigger feels right on hover, and the disclosure copy reads naturally in context."
---

# Phase 94.2: Pooled-Per-User Percentile Redesign — Verification Report

**Phase Goal:** Replace Phase 94.1's per-cell stratified methodology with a one-point-per-user pooled model on both the CDF construction side and the per-user lookup side. Pool games per subject (cap 1000/TC, ≤36 months), compute the metric once, build the CDF as percentile_cont over those values. Single ≥30-games inclusion floor on the pool.

**Verified:** 2026-05-24T06:41:48Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (from ROADMAP success criteria) | Status | Evidence |
|---|---------------------------------------|--------|----------|
| 1 | `canonical_slice_sql.py` produces single pooled aggregate per subject; 1000/TC cap + 36-month recency applied identically on benchmark and single_user paths; `apply_floor` removed; single ≥30 inclusion-floor gate; drift between paths structurally impossible (shared builder) | VERIFIED | `app/services/canonical_slice_sql.py` lines 237-259 (`_recent_capped_cte`), 262-320 (score_gap), 323-393 (achievable), 396-505 (section2); `per_user_cte_for` accepts no `apply_floor`; both consumers import from this module |
| 2 | `_build_metric_breakpoint_query` builds CDF from one row per cohort user (deduped); `n_users` reflects distinct user count; byte-identical regression test regenerated | VERIFIED | `scripts/gen_global_percentile_cdf.py:281-298` composes `selected_users_cte(source="benchmark")` (with `GROUP BY lower(lichess_username)` dedup) + `per_user_cte_for(...source="benchmark"...)`; `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` runs green (4 passed); `n_users` for the 4 metrics: 1726 / 1786 / 1642 / 1419 |
| 3 | `_compute_metric_for_user` is a single pooled query; `n_cells_floor` and `apply_floor` removed from codebase; Plan 13 fix is moot | VERIFIED | `app/services/user_benchmark_percentiles_service.py:88-124` runs one `WITH selected_users, per_user_values ... SELECT metric_value, n_games LIMIT 1`; `grep apply_floor app/services scripts` returns only docstring/comment references (no code); return type widened to `tuple[float, int] \| None` per D-9-amend |
| 4 | `GLOBAL_PERCENTILE_CDF` regenerated and committed; breakpoints shifted from 94.1 values | VERIFIED | `app/services/global_percentile_cdf.py` lines 121-544 contain regenerated registry with `n_users` populated; 4 entries present, breakpoint magnitudes consistent with pooled-per-user shape; regen banner present in `reports/global-percentile-cdf-latest.md` (per Plan 02 SUMMARY) |
| 5 | `backfill_user_percentiles.py` updated for new compute path; re-run on dev; per-metric summary shows users-included / floor-rejected counts | VERIFIED | `scripts/backfill_user_percentiles.py:237-261` `_MetricSummary` renamed `skipped_below_pooled_floor`; classifier tests pass; dev rerun completed per Plan 06 SUMMARY. Prod rerun deferred (see human_verification) |
| 6 | SKILL.md methodology updated to describe one-point-per-user pooled model | VERIFIED (override-style scoping) | `.claude/skills/benchmarks/SKILL.md:8` top-of-file callout disclosing the production chip's pooled methodology + pointer to `canonical_slice_sql.py`. Per D-13-amend the scope was deliberately narrowed to callout + Chapter 4 correspondence-claim corrections (not a full chapter rewrite). The amended decision is captured in 94.2-CONTEXT.md and ROADMAP success criterion 6 is interpreted accordingly. |
| 7 | `PercentileChip` tooltip copy reviewed against new semantics; no UI logic changes; chip still reads from `user_benchmark_percentiles.percentile` | VERIFIED | `frontend/src/components/charts/PercentileChip.tsx` widens flavor enum to 4 metric-named variants (`score-gap`, `achievable`, `parity`, `conversion`); popover body discloses all 4 D-4 bullets per metric; chip mechanics (`deriveBandColor`, `deriveFlameCount`, hover/tap, trigger styling) unchanged from 94.1; 21 PercentileChip tests pass |
| 8 | Open-question investigation captured: distinct-user count per metric post-dedup, tail behaviour, stability check | VERIFIED | `n_users` for the 4 deduped pooled cohorts (1419-1786) is well above any plausible stability concern; documented in regenerated CDF report and RESEARCH §Validation Architecture. No metric dropped below a stability threshold; researcher proceeded per D-2 (no formal blocker). |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/services/canonical_slice_sql.py` | Pooled per-user CTE builders with snapshot_date | VERIFIED | 541 LOC, all 4 builders + `_recent_capped_cte` helper + `_recency_window_sql` + dedup cohort CTE; `RECENT_GAMES_PER_TC_CAP=1000`, `RECENCY_WINDOW_MONTHS=36`, all 4 ≥30 inclusion-floor constants present. `apply_floor` invocations removed (docstring mentions only). |
| `app/services/user_benchmark_percentiles_service.py` | Pooled compute service, `tuple[float, int] \| None` return | VERIFIED | `_compute_metric_for_user` runs single pooled query; both `compute_stage_a` and `compute_stage_b` destructure `(value, n_games)` and pass both to `upsert_percentile`. Stage A → score_gap; Stage B → 3 eval-dependent metrics. |
| `app/services/global_percentile_cdf.py` | Regenerated GLOBAL_PERCENTILE_CDF literal | VERIFIED | 99 breakpoints per metric, sentinel-delimited registry block; `interpolate_percentile` signature unchanged. |
| `scripts/gen_global_percentile_cdf.py` | Pooled breakpoint query + `--snapshot-date` CLI | VERIFIED | `_build_metric_breakpoint_query(metric_id, *, snapshot_date)` calls `per_user_cte_for(..., source="benchmark", snapshot_date=...)`; `_default_snapshot_date` anchors to `BENCHMARK_DB_SNAPSHOT_MONTH` last day; `_build_per_bucket_sanity_query` preserved as zero-row diagnostic. |
| `scripts/backfill_user_percentiles.py` | Pooled-semantics summary classifier | VERIFIED | `_MetricSummary.skipped_below_pooled_floor` counter renamed (RESEARCH Pitfall 7); per-user backfill uses `compute_stage_a` / `compute_stage_b` directly; backfill-local session_maker pattern preserved. |
| `frontend/src/components/charts/PercentileChip.tsx` | 4 metric-named flavors + D-4 disclosure | VERIFIED | `PercentileChipFlavor = 'score-gap' \| 'achievable' \| 'parity' \| 'conversion'`; `RATING_NOTE_BY_FLAVOR` uses `as const satisfies Record<Flavor, string>` for exhaustive coverage; 4 D-4 disclosure constants present. |
| `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` | 2 chip call sites widened to metric-named flavors | VERIFIED | `flavor="achievable"` (line 187) + `flavor="score-gap"` (line 237) |
| `frontend/src/components/charts/EndgameMetricCard.tsx` | 2 chip call sites on Parity + Conversion | VERIFIED | `flavor={bucket === 'conversion' ? 'conversion' : 'parity'}` (line 235) |
| `.claude/skills/benchmarks/SKILL.md` | Production-vs-analysis boundary callout | VERIFIED | Top-of-file callout (line 8) + Chapter 4 correspondence-claim corrections per D-13-amend. |
| `alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py` | `n_games INT NOT NULL DEFAULT 0` migration | VERIFIED | Revision `b8f4d92c1e25` → `a7f3c9b82e14` parent; `ADD COLUMN n_games INT NOT NULL DEFAULT 0`; downgrade present. Schema drift check passed per verification context. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `gen_global_percentile_cdf.py:_build_metric_breakpoint_query` | `canonical_slice_sql.per_user_cte_for` | Direct import + call with `source="benchmark"` | WIRED | line 293 |
| `user_benchmark_percentiles_service._compute_metric_for_user` | `canonical_slice_sql.per_user_cte_for` | Direct import + call with `source="single_user"` | WIRED | line 113 |
| `_compute_metric_for_user` | `upsert_percentile` | Destructures `(value, n_games)` and passes `n_games=` kwarg | WIRED | service line 156-164 (Stage A), 212-220 (Stage B); repository line 52 accepts `n_games: int` (required) |
| `upsert_percentile` | `user_benchmark_percentiles.n_games` column | INSERT + ON CONFLICT DO UPDATE includes `n_games` | WIRED | repository lines 85, 93 |
| `import_service._complete_import_job` | `compute_stage_a` | `asyncio.create_task(compute_stage_a(job.user_id))` | WIRED | import_service.py:504 |
| `eval_drain` worker | `compute_stage_b` | `asyncio.create_task(compute_stage_b(uid))` | WIRED | eval_drain.py:568 |
| `backfill_user_percentiles.py` | `compute_stage_a` / `compute_stage_b` | Imports both, passes injected `session_maker` | WIRED | backfill lines 95-96 |
| `PercentileChip` | `EndgameOverallPerformanceSection` + `EndgameMetricCard` | Import + render in 4 call sites | WIRED | All 4 chip call sites use metric-named flavors |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `PercentileChip` (rendered chip) | `percentile: number` prop | `user_benchmark_percentiles.percentile` column via endgame service payload | YES — populated by Stage A/B compute writing real `interpolate_percentile()` output | FLOWING |
| `GLOBAL_PERCENTILE_CDF` lookup | `breakpoints` tuple | Regenerated by `scripts/gen_global_percentile_cdf.py` against benchmark DB | YES — 1419-1786 distinct users per metric, breakpoints span realistic Δ ranges | FLOWING |
| `compute_stage_a/b` | `(value, n_games)` from pooled CTE | `recent_capped` → `scored` / `entry_rows` / `gap_rows` → `per_user` (HAVING ≥30) | YES — query against `games` + `game_positions` real tables; HAVING clause means rows only emitted for users above floor | FLOWING (None correctly handled as below-floor → no row written → chip suppressed) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Pooled SQL contains 1000-cap, 36-month, NOW(), ≥30, metric_value+n_games | `python -c "...per_user_cte_for('score_gap', source='single_user')..."` | All 7 substring checks pass | PASS |
| snapshot_date threads into SQL as literal date | `python -c "...snapshot_date=date(2026,3,31)..."` | Contains `DATE '2026-03-31'` | PASS |
| Benchmark cohort dedup CTE present | `python -c "...selected_users_cte(source='benchmark')..."` | Contains `lower(bsu.lichess_username)` + `bool_or(NOT ...)` | PASS |
| `apply_floor` argument raises TypeError | `python -c "...per_user_cte_for('score_gap', source='single_user', apply_floor=True)..."` | TypeError raised as expected | PASS |
| `GLOBAL_PERCENTILE_CDF` registry populated for 4 metrics with sensible n_users | `python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF..."` | n_users = 1726/1786/1642/1419; breakpoint ranges plausible | PASS |
| `user_benchmark_percentiles.n_games` column present | `python -c "from app.models.user_benchmark_percentile import UserBenchmarkPercentile; ..."` | Columns include `n_games` | PASS |
| Pooled CTE and service unit tests green | `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_user_benchmark_percentiles_service.py -x` | 94 passed | PASS |
| Byte-identical CDF regression goldens green | `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` | 4 passed | PASS |
| PercentileChip frontend tests (4 flavors + D-4 disclosure) green | `npx vitest run src/components/charts/__tests__/PercentileChip.test.tsx` | 21 passed | PASS |

### Probe Execution

No phase-declared probes. Convention `scripts/*/tests/probe-*.sh` does not match this codebase. Skipped.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PCTL-08 | 94.2-01, 94.2-03, 94.2-06 | `user_benchmark_percentiles` table persists (user_id, metric, value, percentile, **n_games**, cdf_snapshot, computed_at) | SATISFIED | All 7 columns present on model (verified via Python introspection); migration `b8f4d92c1e25` adds back `n_games`; UPSERT writes `n_games` per row (semantics shifted from per-cell count in 94.1 to pooled per-user count per D-9-amend) |
| PCTL-09 | 94.2-01, 94.2-03 | Stage A (score_gap at import-job completion, background) + Stage B (3 eval-dependent at cold-drain completion, background) | SATISFIED | `compute_stage_a` wired via `asyncio.create_task` in `import_service.py:504`; `compute_stage_b` wired in `eval_drain.py:568`. Per-user pooled SQL replaces the per-cell methodology behind the same trigger contract. |

**Note:** REQUIREMENTS.md still maps PCTL-08 / PCTL-09 to "Phase 94.1" in the table at lines 64-65. Per the verification-context note these IDs were re-claimed by Phase 94.2 plans as a methodology refresh (value/percentile semantics shift, n_games re-add). The implementation evidence covers both the original 94.1 contract and the 94.2 methodology shift; the table mapping is documentation drift, not a phase gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | No `TBD`/`FIXME`/`XXX` markers in any file modified by this phase. No hollow stubs. `apply_floor` mentions in `canonical_slice_sql.py` and `user_benchmark_percentiles_service.py` are docstring-only references explaining the removal — not code paths. |

### Human Verification Required

#### 1. Production backfill rerun

**Test:** Run `scripts/backfill_user_percentiles.py --target prod` via `bin/prod_db_tunnel.sh`.
**Expected:** All existing prod users' `user_benchmark_percentiles` rows are recomputed with pooled values, `n_games` populated (currently default-0 from migration). Per-metric summary log line confirms users-included / skipped_below_pooled_floor counts.
**Why human:** Tracked HUMAN-UAT step explicitly deferred by operator. Captured at `.planning/todos/pending/2026-05-24-phase-94.2-prod-backfill-rerun.md` (resolves_phase: 94.2). Must run before next main→production release; requires tunnel + operator-controlled execution.

#### 2. PercentileChip UX in running UI

**Test:** Open the Endgame Overview page in the running frontend; hover/tap each of the 4 chips (Endgame Score Gap, Achievable Score Gap, Parity ΔES, Conversion ΔES).
**Expected:** Each popover shows the 4 bullets (benchmark composition, recent-games basis, filter independence, per-metric rating note). Conversion chip carries the honest "tracks rating strongly" framing; score-gap chip notes rating-invariance; achievable / parity chips show soft mild-coupling note. Mobile + desktop both render correctly.
**Why human:** Visual/UX verification — automated tests assert string presence and exhaustive flavor coverage, but humans confirm the popover renders correctly, hover delay feels right, and disclosure copy reads naturally in context.

### Gaps Summary

No code/wiring gaps. All 8 ROADMAP success criteria have observable evidence in the codebase. The methodology cutover is structurally complete:

- **D-3a atomic cutover verified:** Plan 01 (builder) → Plan 02 (CDF script + regen) → Plan 03 (service consumer + n_games) form a coherent triple. Both consumers import from `canonical_slice_sql`; the shared builder makes drift structurally impossible (D-10).
- **D-9-amend correctly executed:** `n_games` re-added via migration `b8f4d92c1e25`, threaded through compute → repository → table; `_compute_metric_for_user` returns `tuple[float, int] | None`; Stage A/B both destructure and forward; None correctly suppresses row write (chip suppression contract preserved).
- **D-4 disclosure complete:** PercentileChipFlavor widened to 4 metric-named variants with exhaustive `satisfies` type guard; popover body emits all 4 D-4 bullets per metric; rating-correlation notes calibrated per Cohen's d ladder.
- **D-13-amend honored:** SKILL.md scoped to top-of-file callout + Chapter 4 correspondence-claim corrections (~30 LOC), not a full chapter rewrite.
- **TC-mix heterogeneity risk:** Documented as an unaddressed risk in `.planning/notes/per-user-percentile-pooled-redesign.md` per commit `0568240c`. Per the verification context this is intentionally not a gap.

Phase goal is achieved. Two human-verification items (deferred prod backfill + UI eyeball check) drive the `human_needed` status per the verification process step-9 decision tree — `passed` is only valid when the human verification section is empty.

---

_Verified: 2026-05-24T06:41:48Z_
_Verifier: Claude (gsd-verifier)_
