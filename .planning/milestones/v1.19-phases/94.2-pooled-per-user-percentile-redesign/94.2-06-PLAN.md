---
phase: 94.2-pooled-per-user-percentile-redesign
plan: 06
type: execute
wave: 3
depends_on: [94.2-01, 94.2-02, 94.2-03]
files_modified:
  - scripts/backfill_user_percentiles.py
autonomous: false
requirements: [PCTL-08, PCTL-09]

must_haves:
  truths:
    - "scripts/backfill_user_percentiles.py compute path delegates to the new pooled _compute_metric_for_user from Plan 03 — no code change inside the script's compute call, but the _MetricSummary semantics shift from cell-based to pooled-set classification (RESEARCH §Pitfall 7)."
    - "Per-metric summary table emitted by the script shows users-included / floor-rejected counts that semantically reflect the pooled inclusion floor (≥30 of the relevant unit on the pool), not per-cell membership."
    - "After the backfill rerun, every row in user_benchmark_percentiles carries a populated n_games value (the column is added by Plan 03 Task 0's migration with server_default=0; the rerun populates correct per-user counts per D-9-amend)."
    - "scripts/backfill_user_percentiles.py --target dev runs cleanly against the dev DB and emits a coherent summary table; the dev DB's user_benchmark_percentiles rows reflect Phase 94.2 values after the rerun."
    - "scripts/backfill_user_percentiles.py --target prod is documented as a HUMAN-UAT step (requires bin/prod_db_tunnel.sh) — the dev rerun is autonomous; the prod rerun is operator-gated and presented as a checkpoint."
  artifacts:
    - path: scripts/backfill_user_percentiles.py
      provides: rerun harness updated to reflect pooled summary semantics
  key_links:
    - from: scripts/backfill_user_percentiles.py
      to: app/services/user_benchmark_percentiles_service.py
      via: compute_stage_a / compute_stage_b
      pattern: "compute_stage_a\\|compute_stage_b"
---

<objective>
Rerun `scripts/backfill_user_percentiles.py` against the dev DB so existing `user_benchmark_percentiles` rows reflect the new pooled methodology. Update the script's per-metric summary semantics so users-included / floor-rejected counts semantically reflect the pooled-set inclusion floor (not per-cell membership) per RESEARCH §Pitfall 7. The prod rerun is a HUMAN-UAT step (operator-gated via `bin/prod_db_tunnel.sh`) documented as a checkpoint at the end of this plan.

Purpose: Without this rerun, the dev DB still carries 94.1 per-cell-derived rows; the chip will display 94.1 values against the 94.2 CDF (silent miscalibration). The prod rerun is the deploy-side handoff; the operator gates it because production data touches need an explicit hand on the tunnel.

Output: Updated script (counter-classification semantics), green dev rerun, summary log captured to the plan summary, HUMAN-UAT checkpoint for the prod rerun.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-03-PLAN.md
@scripts/backfill_user_percentiles.py
@app/services/user_benchmark_percentiles_service.py
@app/repositories/user_benchmark_percentiles_repository.py

<interfaces>
<!-- scripts/backfill_user_percentiles.py:
       async def main(target: Literal["dev", "prod"], user_id_filter: int | None, metric_filter: CdfMetricId | None) -> None
       class _MetricSummary:
           upserted: int
           skipped_no_canonical_games: int   # rename / reframe per RESEARCH §Pitfall 7
           skipped_no_eval: int               # preserve (Stage B users with pending evals)
       async def _backfill_user(...) -> None
       async def _compute_and_count(...) -> bool
     Safety guards: _db_url + _assert_target_safe (unchanged) — `dev` requires :5432 in URL; `prod` requires :15432.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update _MetricSummary classification semantics for pooled model</name>
  <files>scripts/backfill_user_percentiles.py</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (full file — _MetricSummary class around line 237, main loop summary emission around line 330, _backfill_user classification logic around line 367)
    - app/services/user_benchmark_percentiles_service.py (post-Plan 03 — _compute_metric_for_user returns float | None; None = below pooled ≥30 floor, not "no canonical games")
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Pitfall 7
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-6, Claude's Discretion §Backfill operational details)
  </read_first>
  <action>
    Update the _MetricSummary class and the summary-table emission to reflect pooled-model semantics:

    1. **Rename `skipped_no_canonical_games` → `skipped_below_pooled_floor`** (semantic shift per RESEARCH §Pitfall 7): under the pooled model, a `_compute_metric_for_user` return of None means the user is below the ≥30 inclusion floor on the pool — NOT "no canonical games." Update the class attribute, the property `total_skipped`, the increment site in `_backfill_user` (around line 379 + line 397), and the summary-emission reason string in `main` (around line 336).

    2. **Preserve `skipped_no_eval`** verbatim — that semantic (Stage B users with `count_pending_evals > 0`) is unchanged.

    3. **Update the class docstring** to describe the new classification: """Accumulates upserted / skipped counts for one metric under the Phase 94.2 pooled methodology. `skipped_below_pooled_floor` is the count of users whose recent-1000-per-TC × 36-month pool did not meet the ≥30 inclusion floor for this metric (see app/services/canonical_slice_sql.py constants SCORE_GAP_MIN_ENDGAME_N, ACHIEVABLE_MIN_GAMES, SECTION2_MIN_SPANS_PER_BUCKET, all =30 in 94.2)."""

    4. **No change to `upserted` semantics** — UPSERT-success count remains the natural per-metric integer.

    5. **Update the summary-table emission format** (the `print(...)` block in `main` around line 330):
       ```
       Backfill complete (target=dev, took NN.Ns)
         score_gap                            upserted=X, skipped=Y (below_pooled_floor=Z)
         achievable_score_gap                 upserted=X, skipped=Y (below_pooled_floor=Z, no_eval=W)
         section2_score_gap_conv              upserted=X, skipped=Y (below_pooled_floor=Z, no_eval=W)
         section2_score_gap_parity            upserted=X, skipped=Y (below_pooled_floor=Z, no_eval=W)
       ```
       Use the `below_pooled_floor=` label literal (replaces 94.1's `no_canonical_games=`).

    6. **No CLI flag changes** — `--target dev|prod`, `--user-id`, `--metric` all preserved. Safety guards (`_assert_target_safe`, `_assert_benchmark_db`-style logic on `--target prod`) unchanged.

    7. **ty + ruff** compliance: explicit return types, no magic numbers (the inclusion-floor constants live in `app.services.canonical_slice_sql`; reference them by import if needed in the docstring or class comment).
  </action>
  <verify>
    <automated>uv run ruff format scripts/backfill_user_percentiles.py &amp;&amp; uv run ruff check scripts/backfill_user_percentiles.py &amp;&amp; uv run ty check scripts/backfill_user_percentiles.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skipped_no_canonical_games\|no_canonical_games=" scripts/backfill_user_percentiles.py` returns 0.
    - `grep -c "skipped_below_pooled_floor\|below_pooled_floor=" scripts/backfill_user_percentiles.py` ≥ 3 (class attribute + property + summary emission).
    - `grep -c "Phase 94.2\|pooled" scripts/backfill_user_percentiles.py` ≥ 2 (docstring references the methodology).
    - `python -c "import ast; ast.parse(open('scripts/backfill_user_percentiles.py').read())"` exits 0.
    - `uv run ty check scripts/backfill_user_percentiles.py` exits 0.
    - `uv run pytest tests/scripts/ -x` exits 0 if any backfill script tests exist (find with `find tests/scripts/ -name '*backfill*'`); if none exist, skip this assertion.
  </acceptance_criteria>
  <done>_MetricSummary classification reflects pooled semantics; summary table emits below_pooled_floor labels; backwards-incompatible no_canonical_games removed.</done>
</task>

<task type="auto">
  <name>Task 2: Run dev backfill against existing dev DB and capture summary</name>
  <files></files>
  <read_first>
    - scripts/backfill_user_percentiles.py (post-Task 1)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Environment Availability (dev DB confirmed up)
    - CLAUDE.md (feedback_no_dev_db_reset_in_plans — do NOT run bin/reset_db.sh; the rerun is a recompute against existing rows)
  </read_first>
  <action>
    Pre-flight (do NOT reset the dev DB per `feedback_no_dev_db_reset_in_plans`):

    1. Confirm dev DB is up: `docker ps --filter "name=flawchess-dev-postgres-1" --format '{{.Status}}'`. If not running, surface a HUMAN-ACTION to start it (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`); do not auto-start in the plan.

    2. Confirm `BACKFILL_DEV_DB_URL` is resolvable (likely from `.env`).

    3. Optional pre-flight smoke: pick one existing dev user_id and run a narrow backfill first to catch contract drift early:
       `uv run python scripts/backfill_user_percentiles.py --target dev --user-id 1 --metric score_gap`
       Expect either `upserted=1` or `skipped=1 (below_pooled_floor=1)` — both are valid outcomes; the test is that the script runs cleanly without exception. If exception fires, halt and surface the failure (this catches issues like Plan 03's contract drift slipping through).

    Execution:

    4. Run the full dev backfill: `uv run python scripts/backfill_user_percentiles.py --target dev | tee .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-dev-backfill.log`.

    5. After completion, verify summary table format matches Task 1's specification: each of the 4 metrics shows `upserted=`, `skipped=`, and one or more reason labels.

    6. Capture the summary output into the plan summary (94.2-06-SUMMARY.md). Include:
       - Total user count processed.
       - Per-metric upserted / below_pooled_floor / no_eval counts.
       - Wall-clock duration.
       - Any failed user_ids (should be zero post-Plan 03).

    7. Sanity SQL: connect to dev DB and count rows by metric: `mcp__flawchess-db__query "SELECT metric::text, count(*) FROM user_benchmark_percentiles GROUP BY metric ORDER BY metric"`. Confirm the four metrics' row counts match the summary's `upserted` columns.

    8. Spot-check: pick one user with non-null `score_gap` value and verify the stored `value` is within the global CDF's range (`mcp__flawchess-db__query "SELECT user_id, value, percentile, n_games, cdf_snapshot FROM user_benchmark_percentiles WHERE metric = 'score_gap' ORDER BY computed_at DESC LIMIT 5"`). Confirm percentile is in [0, 100], and `n_games` is a non-negative integer.

       **Note:** The `n_games` column on this SELECT depends on Plan 03 Task 0's Alembic migration having run (`alembic upgrade head` adds the column with `server_default="0"`). Plan 03 Task 0 runs before Plan 06 because Plan 06 depends on Plan 03 (per `depends_on` frontmatter); the migration is included in `bin/deploy.sh`'s container-entrypoint Alembic run on prod. For the dev backfill in this plan, confirm `\d user_benchmark_percentiles` shows the `n_games` column before invoking the rerun.

    Do NOT run the prod backfill in this task — that's the explicit HUMAN-UAT checkpoint in Task 3.
  </action>
  <verify>
    <automated>tail -n 20 .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-dev-backfill.log | grep -c "upserted=\|below_pooled_floor"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-dev-backfill.log` exists and contains the "Backfill complete" line.
    - The log shows all 4 metrics with `upserted=N` (N ≥ 0) and `skipped=M` (M ≥ 0).
    - Zero `FAILED users` lines.
    - Sanity-SQL row counts on `user_benchmark_percentiles` are non-zero for at least one metric (assumes at least one dev user passes the ≥30 pooled floor).
    - For at least one upserted row, the stored `percentile` is in `[0, 100]`.
  </acceptance_criteria>
  <done>Dev rerun complete; rows reflect Phase 94.2 methodology; summary captured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: HUMAN-UAT — operator runs prod backfill via SSH tunnel</name>
  <files>.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-prod-backfill.log (created by operator)</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (post-Task 1)
    - CLAUDE.md §Production Server (prod-tunnel + bin/prod_db_tunnel.sh contract)
    - bin/prod_db_tunnel.sh (lifecycle)
  </read_first>
  <what-built>
    Task 1 updated the backfill script with pooled-semantics summary classification. Task 2 ran the dev backfill autonomously and the dev DB'''s `user_benchmark_percentiles` rows now reflect Phase 94.2 methodology.
  </what-built>
  <how-to-verify>
    1. After the prod backfill log captures: open `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-prod-backfill.log`. Confirm "Backfill complete (target=prod, took N.Ns)" appears.
    2. Confirm the 4-metric summary table at the end matches the format from Task 1'''s spec.
    3. Confirm zero `FAILED users` lines.
    4. Spot-check a known prod user: `mcp__flawchess-prod-db__query "SELECT metric::text, value, percentile, n_games, cdf_snapshot FROM user_benchmark_percentiles WHERE user_id = <test_user_id> ORDER BY metric"`. Confirm the row'''s `value` differs from the 94.1 value (the methodology change should produce material drift for most users); confirm the `cdf_snapshot` column reflects the date of the prod backfill run.
    5. Visual UAT: open https://flawchess.com, navigate to an existing user'''s Endgames page, confirm the percentile chips render under the new copy from Plan 05 and the percentile values are plausible (not all 0, not all 100, not all 50).
  </how-to-verify>
  <action>
    Operator-only — Claude does NOT auto-execute this task. The operator runs:

    1. Open the prod tunnel: `bin/prod_db_tunnel.sh` (forwards `localhost:15432` → prod PostgreSQL).
    2. Confirm `BACKFILL_PROD_DB_URL` is set and includes `:15432`.
    3. Run: `uv run python scripts/backfill_user_percentiles.py --target prod | tee .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-prod-backfill.log`.
    4. Verify the summary log shows zero failed users and reasonable upserted counts (compared against `mcp__flawchess-prod-db__query "SELECT count(*) FROM users WHERE deleted_at IS NULL"`).
    5. Close the tunnel: `bin/prod_db_tunnel.sh stop`.
    6. Execute the 5-step visual + SQL UAT in `<how-to-verify>` above; record findings in 94.2-06-SUMMARY.md.
  </action>
  <verify>
    <human-check>Operator confirms prod backfill log shows zero failed users, summary table format matches Task 1 spec, and flawchess.com visual UAT confirms chip rendering with plausible percentile values.</human-check>
  </verify>
  <done>Prod backfill log captured; zero failed users; visual UAT passes; PR is ready for squash-merge to main per D-3a.</done>
  <resume-signal>Type "prod-backfill-approved" once the prod rerun log shows zero failures and the visual UAT on flawchess.com confirms chip rendering. The plan completes at that point; type "issues: <description>" if anything regressed (the rollback path per D-3b is `git revert` the PR + rerun this backfill against the restored 94.1 code path).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `bin/prod_db_tunnel.sh` | Trusted operator-controlled SSH; preserves the 94.1 prod-touch contract. |
| script → prod DB (via tunnel) | `_assert_target_safe(url, "prod")` requires `:15432` in the URL; refuses to write to anything else. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.2-06-01 | Tampering (cross-environment write) | `_assert_target_safe` | mitigate | Guard unchanged from 94.1; refuses to UPSERT against any URL not matching the `--target` port. Operator-gated via the explicit Task 3 checkpoint. |
| T-94.2-06-02 | Repudiation (silent partial rerun) | `failed_user_ids` exit code | mitigate | Script exits 1 if any user fails (preserved from 94.1); Sentry captures per-user failures. Task 3 verification confirms zero failed users in the prod log. |
| T-94.2-06-03 | Tampering (semantic mis-classification in summary) | `_MetricSummary` rename | accept | Task 1 renames the counter; absent rename, the summary would mis-attribute pooled-floor rejections as "no canonical games" (RESEARCH §Pitfall 7). Caught by Task 1's grep acceptance criterion. |
</threat_model>

<verification>
- Task 1: ruff + ty clean; counter renames complete.
- Task 2: dev backfill log captured; non-zero row counts on `user_benchmark_percentiles`; spot-check percentile in [0, 100].
- Task 3 (HUMAN-UAT): operator-confirmed prod backfill log captured; flawchess.com chip rendering visually verified.
- After Task 3 resume, this plan is the last of Phase 94.2; the entire phase is complete and ready for `/gsd-verify-work` followed by PR squash-merge to main per D-3a.
</verification>

<success_criteria>
Backfill script counter semantics reflect pooled-floor classification; dev DB rerun complete and rows visibly reflect Phase 94.2 methodology; prod rerun executed by the operator with the script log captured and zero failures; visual UAT on production confirms chip rendering is intact and percentiles are plausible.
</success_criteria>

<output>
Create `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-06-SUMMARY.md` when done. Embed the dev + prod backfill log tails (Task 2 + Task 3 outputs) in the summary so future readers can audit the numerical shift from Phase 94.1.
</output>
