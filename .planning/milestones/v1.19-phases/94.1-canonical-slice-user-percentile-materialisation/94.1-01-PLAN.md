---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/services/test_canonical_slice_sql.py
  - tests/integration/test_alembic_migrations.py
  - tests/integration/test_benchmark_metric_enum.py
  - tests/repositories/test_user_benchmark_percentiles_repository.py
  - tests/scripts/test_gen_global_percentile_cdf_unchanged.py
autonomous: true
requirements:
  - PCTL-08
must_haves:
  truths:
    - "Wave 0 test files exist and FAIL with clear 'not implemented' or import errors before downstream waves land"
    - "test_canonical_slice_sql exercises every shared fragment for byte-identical strings between benchmark and single_user sources (D-11)"
    - "test_alembic_migrations covers upgrade + downgrade round-trip and surfaces the table-before-type drop ordering (Pitfall 4)"
    - "test_benchmark_metric_enum reads pg_enum and asserts exactly the 4 expected labels"
    - "test_user_benchmark_percentiles_repository covers UPSERT idempotency, percentile=NULL handling, FK CASCADE"
    - "test_gen_global_percentile_cdf_unchanged captures pre-refactor SQL/CDF snapshot used by Plan 03 regression gate"
  artifacts:
    - path: "tests/services/test_canonical_slice_sql.py"
      provides: "Pure-Python SQL builder tests + cross-consumer fragment-identity assertions"
    - path: "tests/integration/test_alembic_migrations.py"
      provides: "test_user_benchmark_percentiles_upgrade + test_user_benchmark_percentiles_upgrade_downgrade"
    - path: "tests/integration/test_benchmark_metric_enum.py"
      provides: "pg_enum SELECT against 4 expected labels"
    - path: "tests/repositories/test_user_benchmark_percentiles_repository.py"
      provides: "UPSERT idempotency + FK CASCADE + NULL percentile coverage"
    - path: "tests/scripts/test_gen_global_percentile_cdf_unchanged.py"
      provides: "Regression snapshot of pre-refactor CDF SQL output"
  key_links:
    - from: "tests/scripts/test_gen_global_percentile_cdf_unchanged.py"
      to: "scripts/gen_global_percentile_cdf.py (pre-refactor snapshot)"
      via: "captured fixture string committed alongside test"
      pattern: "BUILD_METRIC_BREAKPOINT_QUERY_BASELINE"
---

<objective>
Wave 0 scaffolding for the schema + repository + shared-SQL slice of the Nyquist test plan (per VALIDATION.md). Every test file here MUST exist before Plans 03, 04 land. Tests should currently FAIL via import errors or skip markers; downstream waves flip them green.

Purpose: Nyquist rule. The executor for Plans 03/04 receives ready-made tests that define the contract; no scavenger hunt.
Output: 5 net-new test files committed and failing for the expected "not yet implemented" reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-VALIDATION.md

<interfaces>
Re-used from project:
- `app.services.global_percentile_cdf.CdfMetricId` — Literal["score_gap","achievable_score_gap","section2_score_gap_conv","section2_score_gap_parity"]
- `app.repositories.game_repository.count_pending_evals(session, user_id) -> int` (verbatim reuse, Wave 3 trigger)
- Existing test fixture conftest at `tests/conftest.py` provides `async_session` + `migrated_db` (consult before authoring new fixtures)

Will be created (Plans 03/04 — these tests target their shapes):
- `app.services.canonical_slice_sql.selected_users_cte(*, source: Literal["benchmark","single_user"]) -> str`
- `app.services.canonical_slice_sql.elo_bucket_expr(user_elo_alias: str) -> str`
- `app.services.canonical_slice_sql.equal_footing_filter_sql() -> str`
- `app.services.canonical_slice_sql.sparse_exclusion_sql(elo_col: str, tc_col: str) -> str`
- `app.services.canonical_slice_sql.per_user_cte_for(metric_id: CdfMetricId, *, source: Literal["benchmark","single_user"]) -> str`
- `app.models.user_benchmark_percentile.UserBenchmarkPercentile` with PK `(user_id, metric)`, columns per CONTEXT D-08
- `app.repositories.user_benchmark_percentiles_repository.upsert_percentile(session, *, user_id, metric, value, percentile, n_games, cdf_snapshot) -> None`
- `app.repositories.user_benchmark_percentiles_repository.fetch_for_user(session, *, user_id) -> dict[CdfMetricId, PercentileRow]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold canonical-slice SQL + gen-script regression tests</name>
  <files>tests/services/test_canonical_slice_sql.py, tests/scripts/test_gen_global_percentile_cdf_unchanged.py, tests/scripts/__init__.py</files>
  <read_first>
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 5 + §Common Pitfalls Pitfall 8)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`app/services/canonical_slice_sql.py` analog section)
    - scripts/gen_global_percentile_cdf.py (lines 200-540 — capture the current `_build_metric_breakpoint_query` output for each of the 4 metric IDs as the regression baseline)
    - tests/conftest.py (existing test infra; confirm whether `tests/scripts/` needs an __init__.py)
  </read_first>
  <action>
    Create `tests/services/test_canonical_slice_sql.py` covering D-11 fragment identity:

    - `test_equal_footing_filter_sql_is_constant` — asserts repeated calls return identical string; planner's recommendation: snapshot the exact string against a literal expected value derived from `scripts/gen_global_percentile_cdf.py` line 257-265.
    - `test_sparse_exclusion_sql_parametrises_columns` — calls with two distinct column-name pairs, asserts the output substitutes the column names cleanly, no SQL injection escape vectors (per Security V5 threat).
    - `test_elo_bucket_expr_format` — asserts the CASE WHEN structure matches the canonical bucketing.
    - `test_selected_users_cte_benchmark_vs_single_user` — calls `selected_users_cte(source="benchmark")` and `source="single_user"`, asserts shape (benchmark = JOIN to benchmark_selected_users + benchmark_ingest_checkpoints; single_user = `SELECT :user_id::int AS user_id`).
    - `test_per_user_cte_for_all_four_metrics_byte_identical_shared_fragments` — for each metric in the 4-value CdfMetricId set, build benchmark and single_user variants, assert that the substrings of `equal_footing_filter_sql()`, `sparse_exclusion_sql(...)`, `elo_bucket_expr(...)` appear byte-identically in BOTH variants (the explicit cross-consumer drift guard from RESEARCH Pitfall 8).
    - `test_per_user_cte_single_user_drops_tc_bucket_predicate` — asserts the `g.time_control_bucket::text = su.tc_bucket` predicate is present in `source="benchmark"` and ABSENT in `source="single_user"` (D-09 pooling).

    All tests target `app.services.canonical_slice_sql` which does not exist yet — Plan 03 lands the module. Mark the test module with `pytest.importorskip("app.services.canonical_slice_sql")` at the top so the suite stays green pre-Plan-03 but lights up immediately once the module ships.

    Create `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` — the cross-refactor regression gate:

    - Before authoring this test, run `python scripts/gen_global_percentile_cdf.py --dry-run-emit-sql` (or equivalent introspection — if no `--dry-run-emit-sql` exists, import `_build_metric_breakpoint_query` directly from the script and call it for each CdfMetricId to capture the raw SQL string).
    - Commit the captured strings as a fixture (either inline `EXPECTED_*` constants in the test module or a `tests/scripts/fixtures/gen_global_percentile_cdf_pre_refactor.json`).
    - The test calls `_build_metric_breakpoint_query(metric_id)` for each of the 4 metrics and asserts byte-identical equality against the fixture (post-Plan-03 refactor must preserve output exactly).
    - `pytest.importorskip` is NOT used here — this test must run after Plan 03 lands; until then mark with `@pytest.mark.skip("baseline captured; expected to pass post-Plan-03")` and leave a TODO referencing Plan 03.

    Create `tests/scripts/__init__.py` (empty) if absent — needed for test discovery.

    Security note: no SQL builders accept user input here; all `:user_id` parameters are SQLAlchemy bindparams resolved later (V5 mitigation lives at the binding seam, not in the string builder).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` discovers all 6 tests in test_canonical_slice_sql.py and the 4 metric-variants in test_gen_global_percentile_cdf_unchanged.py
    - test_canonical_slice_sql.py imports `pytest.importorskip("app.services.canonical_slice_sql")` at module top (verifies skip path on green CI pre-Plan-03)
    - `grep -n "byte-identical\|byte_identical" tests/services/test_canonical_slice_sql.py` returns at least one match (drift guard documented in test name)
    - Regression fixture string is committed (either inline or under `tests/scripts/fixtures/`); `git status` shows it tracked
    - No new dependencies added to `pyproject.toml`
  </acceptance_criteria>
  <done>5 test functions in canonical_slice_sql test; 4 parametrised cases in gen_global_percentile_cdf regression test; both files added to git index; ruff/ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold migration + repository + ENUM introspection tests</name>
  <files>tests/integration/test_alembic_migrations.py, tests/integration/test_benchmark_metric_enum.py, tests/repositories/test_user_benchmark_percentiles_repository.py</files>
  <read_first>
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 1, §Pattern 2, §Common Pitfalls Pitfall 3 + Pitfall 4)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (migration analog + repository analog sections)
    - alembic/versions/20260408_155149_3e4018d62102_convert_result_user_color_termination_.py (ENUM lifecycle precedent — verbatim shape this phase reuses)
    - app/repositories/game_repository.py (lines 11-53 — UPSERT precedent + count_pending_evals)
    - tests/conftest.py (look for `migrated_db` / `async_session` / `engine` fixtures)
  </read_first>
  <action>
    Augment `tests/integration/test_alembic_migrations.py` (file may not exist — create if absent; this is the project's first migration-round-trip test):

    - `test_user_benchmark_percentiles_upgrade` — runs `alembic upgrade head` against a fresh test DB, then `SELECT * FROM information_schema.tables WHERE table_name='user_benchmark_percentiles'` returns one row; asserts column names + types match D-08 spec (`user_id`/INTEGER, `metric`/USER-DEFINED enum, `value`/DOUBLE PRECISION, `percentile`/DOUBLE PRECISION NULL, `n_games`/INTEGER, `cdf_snapshot`/DATE, `computed_at`/TIMESTAMPTZ); PK is composite on (user_id, metric).
    - `test_user_benchmark_percentiles_upgrade_downgrade` — upgrade to head, downgrade ONE step (the new migration), assert table AND benchmark_metric ENUM type are both gone (`SELECT * FROM pg_type WHERE typname='benchmark_metric'` returns 0 rows). This is the Pitfall 4 guard.

    Use `pytest.importorskip("app.models.user_benchmark_percentile")` guard at module top; mark both tests with `@pytest.mark.skipif(not _migration_present(), reason="migration not landed yet")` helper that checks for the migration filename pattern under `alembic/versions/*user_benchmark_percentiles*`.

    Create `tests/integration/test_benchmark_metric_enum.py`:

    - `test_benchmark_metric_enum_has_exactly_four_labels` — after migrating, executes `SELECT enumlabel FROM pg_enum e JOIN pg_type t ON e.enumtypid = t.oid WHERE t.typname = 'benchmark_metric' ORDER BY enumlabel`; asserts the result list equals `["achievable_score_gap", "score_gap", "section2_score_gap_conv", "section2_score_gap_parity"]` (alphabetically ordered).
    - This catches Pitfall 3 (ENUM rejects valid values) and the discretion-item "assert ENUM contains exactly 4 values" gate.

    Create `tests/repositories/test_user_benchmark_percentiles_repository.py`:

    - `test_upsert_inserts_when_no_row_exists` — call `upsert_percentile(...)`, SELECT, assert row present with correct values.
    - `test_upsert_overwrites_existing_row` — insert once, upsert with new `value`/`percentile`/`n_games`/`cdf_snapshot`, SELECT, assert all 4 mutable columns updated; `computed_at` advances (use `>` comparison).
    - `test_upsert_handles_percentile_null` — upsert with `percentile=None`, SELECT, assert `percentile IS NULL` returned as Python `None`; `value` + `n_games` still required.
    - `test_fk_cascade_on_user_delete` — insert a user, insert 4 percentile rows, DELETE the user, assert 0 percentile rows remain (ON DELETE CASCADE).
    - `test_pk_rejects_duplicate_metric_for_same_user_via_plain_insert` — plain `INSERT` without `on_conflict_do_update` fails with `IntegrityError` (sanity check that UPSERT is the only safe write path).
    - `test_fetch_for_user_returns_dict_keyed_by_metric_id` — insert 2 of 4 metrics, call `fetch_for_user(session, user_id=X)`, assert returned dict has exactly the 2 metric IDs and matching `(value, percentile)`.

    All repository tests use `pytest.importorskip("app.repositories.user_benchmark_percentiles_repository")` so the suite stays green until Plan 04 lands.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_alembic_migrations.py tests/integration/test_benchmark_metric_enum.py tests/repositories/test_user_benchmark_percentiles_repository.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files committed
    - test_alembic_migrations.py contains both `test_user_benchmark_percentiles_upgrade` AND `test_user_benchmark_percentiles_upgrade_downgrade`
    - test_benchmark_metric_enum.py asserts EXACTLY 4 labels (alphabetical ordering)
    - test_user_benchmark_percentiles_repository.py contains the 6 listed tests; FK CASCADE test verifies row count drops to 0
    - All tests use `pytest.importorskip` or `@pytest.mark.skipif` to stay green pre-implementation (no false reds on CI before Plans 03/04 land)
    - `grep -n "ON DELETE CASCADE\|cascade\|CASCADE" tests/repositories/test_user_benchmark_percentiles_repository.py` returns at least one match in the FK test docstring
    - `uv run ruff check tests/` exits 0; `uv run ty check tests/` exits 0
  </acceptance_criteria>
  <done>Three Wave-0 test files committed; collection succeeds; tests skip cleanly on green CI; Plans 03/04 will flip them to running tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → DB | Test fixtures write to migrated test DB; no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-01 | Tampering (V5) | shared SQL builders (target of Plan 03 — tested here) | mitigate | Test `test_sparse_exclusion_sql_parametrises_columns` documents the V5 boundary; only column-name identifiers are templated, never user input. User-id binding is via `:user_id` bindparam, exercised in compute-service tests (Plan 02 scaffolding). |
| T-94.1-02 | Tampering (V4) | ENUM rejects invalid metric values at DB layer | mitigate | `test_benchmark_metric_enum_has_exactly_four_labels` enforces the input-validation contract; downstream code paths using string literals outside the 4-value set will fail at INSERT (Pitfall 3 guard). |
</threat_model>

<verification>
- All 5 test files committed to `tests/` tree
- `uv run pytest --collect-only -q` discovers all new tests
- Pre-PR gate: `uv run ruff format --check tests/ && uv run ruff check tests/ && uv run ty check tests/` exits 0
- No production code touched in this plan
</verification>

<success_criteria>
- 5 net-new test files exist (3 in this plan + 2 in Plan 02)
- Each file maps to a row in VALIDATION.md §Per-Task Verification Map
- Suite stays green pre-Plan-03/04 (skip markers / importorskip on absent modules)
- Pre-PR gate clean
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-01-SUMMARY.md` when done — list the 5 net-new test files, the count of test functions in each, and a one-liner confirming `pytest --collect-only` succeeded.
</output>
