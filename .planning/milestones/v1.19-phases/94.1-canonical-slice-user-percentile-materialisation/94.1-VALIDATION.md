---
phase: 94.1
slug: canonical-slice-user-percentile-materialisation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-23
---

# Phase 94.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.0.0+ + pytest-asyncio 0.23.0+ (already installed) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`, `asyncio_default_test_loop_scope = "session"`) |
| **Quick run command** | `uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x` |
| **Full suite command** | `uv run pytest -x` |
| **Estimated runtime** | ~90 seconds (full backend) + ~15 seconds (frontend chip copy test) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x` + `uv run ruff check` + `uv run ty check app/ tests/`
- **After every plan wave:** Run `uv run pytest -x` (full backend) + `cd frontend && npm test -- --run` (FE chip copy test)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds for the per-task quick command; ~90 seconds for the wave-merge full suite.

---

## Per-Task Verification Map

> Filled in by gsd-planner during plan creation. Each plan task that mutates production code must point at one of the rows below (or a Wave 0 stub) before its `<acceptance_criteria>` can include "test exits 0".

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {tbd} | 01 (shared SQL) | 0/1 | D-11 | — | static SQL only; never f-string user_id | unit | `pytest tests/services/test_canonical_slice_sql.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 01 (shared SQL) | 0/1 | D-11 / SC-7 | — | extraction is byte-identical for CDF gen | regression | `pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 02 (migration) | 1 | PCTL-08 / D-05..D-08 | T-PII (V5) | ENUM enforces 4 metric values at DB layer | integration | `pytest tests/integration/test_alembic_migrations.py::test_user_benchmark_percentiles_upgrade -x` | ❌ W0 | ⬜ pending |
| {tbd} | 02 (migration) | 1 | PCTL-08 / D-05 | — | upgrade + downgrade round-trip clean | integration | `pytest tests/integration/test_alembic_migrations.py::test_user_benchmark_percentiles_upgrade_downgrade -x` | ❌ W0 | ⬜ pending |
| {tbd} | 02 (migration) | 1 | D-05 | — | `pg_enum` contains exactly 4 expected values | integration | `pytest tests/integration/test_benchmark_metric_enum.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 03 (repo + UPSERT) | 1 | PCTL-08 / D-08 | T-Cross-user-read (V4) | UPSERT idempotency + FK CASCADE | integration | `pytest tests/repositories/test_user_benchmark_percentiles_repository.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 04 (compute service) | 2 | PCTL-09 / D-10 | — | `compute_stage_a`/`compute_stage_b` happy + below-floor | integration | `pytest tests/services/test_user_benchmark_percentiles_service.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 04 (compute service) | 2 | SC-7 | — | per-user value matches benchmark CDF aggregation | property | `pytest tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 05 (Stage A hook) | 3 | PCTL-09 / D-03 | — | fires AFTER commit, errors do not propagate | integration | `pytest tests/services/test_import_service_stage_a.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 06 (Stage B hook) | 3 | PCTL-09 / D-01 | — | fires only on pending count → 0 | integration | `pytest tests/services/test_eval_drain_stage_b.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 07 (chip rewire) | 4 | PCTL-07 / D-12 | T-Cross-user-read (V4) | toggling filters does not move chip; SELECT carries WHERE user_id | integration | `pytest tests/services/test_endgame_service_chip_decoupling.py -x` | ❌ W0 | ⬜ pending |
| {tbd} | 07 (tooltip copy) | 4 | D-13 | — | canonical-slice framing present in popover string | unit (FE) | `cd frontend && npm test PercentileChip -- --run` | ✅ extend | ⬜ pending |
| {tbd} | 08 (backfill script) | 4 | PCTL-10 / D-14 | T-WrongDB (V4) | idempotent re-run; `--target prod` refuses without tunnel; per-metric summary table emitted | unit + integration | `pytest tests/scripts/test_backfill_user_percentiles.py -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Tests scaffolded BEFORE the implementation wave they back. RESEARCH lists 10 net-new test files:

- [ ] `tests/services/test_canonical_slice_sql.py` — pure-Python SQL builder tests + cross-consumer fragment identity assertion (D-11)
- [ ] `tests/services/test_user_benchmark_percentiles_service.py` — `compute_stage_a` + `compute_stage_b` happy path + below-floor path (PCTL-09 / D-10)
- [ ] `tests/services/test_import_service_stage_a.py` — Stage A hook fires AFTER commit, errors don't propagate (PCTL-09 / D-03 / D-04)
- [ ] `tests/services/test_eval_drain_stage_b.py` — Stage B triggers only on pending count → 0 transition (PCTL-09 / D-01)
- [ ] `tests/services/test_endgame_service_chip_decoupling.py` — toggling filter inputs does not change `{metric}_percentile` (PCTL-07 / D-12)
- [ ] `tests/repositories/test_user_benchmark_percentiles_repository.py` — UPSERT idempotency, NULL handling, FK CASCADE (PCTL-08 / D-08)
- [ ] `tests/integration/test_alembic_migrations.py::test_user_benchmark_percentiles_upgrade` — full upgrade+downgrade cycle (PCTL-08 / D-05)
- [ ] `tests/integration/test_benchmark_metric_enum.py` — `SELECT enumlabel FROM pg_enum WHERE enumtypid = ...` returns exactly the 4 expected values
- [ ] `tests/scripts/test_backfill_user_percentiles.py` — idempotent re-run, `--user-id` filtering, `--target prod` refuses without tunnel
- [ ] `tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py` — synthetic DB → run both paths → values match (property gate, SC-7)
- [ ] `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` — regression: byte-identical CDF after D-11 extraction
- [ ] No framework install needed — pytest + pytest-asyncio already present.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Chip stays put when user toggles filters in the live UI | PCTL-07 / D-12 | Per `feedback_no_dev_db_reset_in_plans.md`, end-to-end UI behaviour against the real chip is HUMAN-UAT. The automated `test_endgame_service_chip_decoupling.py` exercises the service-layer contract; the visual confirmation that the chip number does NOT change is human-checked. | After Wave 4 merges to `main`: open `/endgames` for a user with ≥30 endgame games, observe the percentile chip value, toggle Recency / Opponent Strength / TC / Platform / Rated / Opponent Type, confirm the chip does not change. The row's filter-applied score *will* change — only the chip stays put. |
| Tooltip copy reads naturally on hover | D-13 | Copy reviews are subjective; `feedback_popover_copy_minimalism.md` rules are guidelines, not auto-testable. | After Wave 4 merges: hover the chip on desktop and tap on mobile, confirm the canonical-slice framing reads cleanly and within the minimalism discipline (WHAT + sign convention only, no jargon, no caveats). |
| Production backfill rollout health spot-check | PCTL-10 / SC-6 | The backfill summary table is for operator inspection — not automatable past the unit-test level. | After deploy: `bin/prod_db_tunnel.sh start && uv run python scripts/backfill_user_percentiles.py --target prod` ; review the per-metric per-skip-reason summary table; sanity-check upserted counts against `SELECT COUNT(*) FROM users WHERE games_completed > 30`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (`npm test -- --run` is the non-watch form; `pytest -x` is single-run)
- [ ] Feedback latency < 15s for the per-task quick command
- [ ] `nyquist_compliant: true` set in frontmatter (gsd-planner flips this after wave 0 is wired)

**Approval:** pending
