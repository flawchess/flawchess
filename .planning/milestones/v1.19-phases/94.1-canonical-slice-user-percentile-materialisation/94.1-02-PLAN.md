---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/services/test_user_benchmark_percentiles_service.py
  - tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py
  - tests/services/test_import_service_stage_a.py
  - tests/services/test_eval_drain_stage_b.py
  - tests/services/test_endgame_service_chip_decoupling.py
  - tests/scripts/test_backfill_user_percentiles.py
  - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
autonomous: true
requirements:
  - PCTL-07
  - PCTL-09
  - PCTL-10
must_haves:
  truths:
    - "Wave 0 service + hook + chip + backfill test files exist before Plans 05/06/07/08 land"
    - "Stage A and Stage B hook tests assert error non-propagation (D-04) and ordering (after-commit per D-03 / pending=0 transition per D-01)"
    - "Chip decoupling test exercises filter toggles against a frozen materialised value (PCTL-07 / D-12)"
    - "Backfill script test exercises `--target prod` refusal without tunnel (Pitfall 6 / SC-6)"
    - "Per-user-vs-benchmark parity test pins SC-7 (CTE-sharing methodology drift guard)"
    - "Frontend PercentileChip test extended for canonical-slice tooltip framing (D-13)"
  artifacts:
    - path: "tests/services/test_user_benchmark_percentiles_service.py"
      provides: "Compute happy + below-floor + zero-canonical-slice-games paths"
    - path: "tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py"
      provides: "Property gate: per-user value matches benchmark CDF aggregation for the same user_id"
    - path: "tests/services/test_import_service_stage_a.py"
      provides: "Hook fires AFTER commit; errors don't propagate"
    - path: "tests/services/test_eval_drain_stage_b.py"
      provides: "Hook fires only on pending-count→0 transition"
    - path: "tests/services/test_endgame_service_chip_decoupling.py"
      provides: "Filter toggles do not move {metric}_percentile"
    - path: "tests/scripts/test_backfill_user_percentiles.py"
      provides: "Idempotency + --target prod tunnel-down refusal + per-metric summary table"
    - path: "frontend/src/components/charts/__tests__/PercentileChip.test.tsx"
      provides: "Tooltip-copy assertion for canonical-slice clarifier (D-13)"
  key_links:
    - from: "tests/services/test_endgame_service_chip_decoupling.py"
      to: "user_benchmark_percentiles table (frozen row)"
      via: "filter-toggle assertion compares response.score_gap_percentile across distinct filter sets"
      pattern: "assert response_a.score_gap_percentile == response_b.score_gap_percentile"
    - from: "tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py"
      to: "scripts/gen_global_percentile_cdf.py + app/services/canonical_slice_sql.py"
      via: "synthetic DB seed → both consumers → assert same value emerges"
      pattern: "abs(per_user_value - benchmark_per_user_value_for_same_user_id) < 1e-9"
---

<objective>
Wave 0 scaffolding for the compute service + hook wiring + chip rewire + backfill slice. Six backend test files and one frontend test addition committed before Plans 05/06/07/08 land. Per VALIDATION.md, the per-user-vs-benchmark parity test is the SC-7 property gate — it's non-trivial because it requires synthetic seed data spanning the canonical slice (multiple ELO buckets, multiple TC buckets, ≥30 endgame games).

Purpose: Nyquist test scaffolding. Plans 05/06/07/08 inherit ready-made contracts.
Output: 7 net-new test files (6 backend + 1 frontend) committed and failing for "not yet implemented" reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-VALIDATION.md

<interfaces>
Will be created (these tests target their shapes):
- `app.services.user_benchmark_percentiles_service.compute_stage_a(user_id: int) -> None`
- `app.services.user_benchmark_percentiles_service.compute_stage_b(user_id: int) -> None`
- `app.services.user_benchmark_percentiles_service.STAGE_A_METRIC: CdfMetricId = "score_gap"`
- `app.services.user_benchmark_percentiles_service.STAGE_B_METRICS: tuple[CdfMetricId, ...]`
- `scripts.backfill_user_percentiles.main(target: Literal["dev","prod"], user_id: int | None, metric: CdfMetricId | None) -> None`
- `scripts.backfill_user_percentiles._assert_target_safe(url: str, target: str) -> None`

Re-used from project:
- `app.services.import_service._complete_import_job` (Stage A hook site)
- `app.services.eval_drain` (Stage B hook site after `_mark_evals_completed`)
- `app.services.endgame_service` chip emission sites at lines 1369-1394 and 2344-2348
- `frontend/src/components/charts/PercentileChip.tsx:71-86` (`PercentileChipPopoverBody`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold compute-service + parity + hook tests</name>
  <files>tests/services/test_user_benchmark_percentiles_service.py, tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py, tests/services/test_import_service_stage_a.py, tests/services/test_eval_drain_stage_b.py</files>
  <read_first>
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 3, §Pattern 4, §Pattern 5, §Common Pitfalls 1+2+5)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`app/services/user_benchmark_percentiles_service.py` analog + Stage A/B hook sections)
    - tests/services/test_eval_drain.py (existing integration-test scaffold; reuse session/engine fixtures)
    - tests/services/test_import_service.py (if present — same purpose)
    - app/services/eval_drain.py (lines 511-566 — hook target site for Stage B)
    - app/services/import_service.py (lines 455-499 — hook target site for Stage A)
    - app/services/global_percentile_cdf.py (CdfMetricId, BENCHMARK_DB_SNAPSHOT_MONTH, interpolate_percentile)
  </read_first>
  <action>
    Create `tests/services/test_user_benchmark_percentiles_service.py`:

    - `test_compute_stage_a_happy_path` — seed user + ≥30 endgame + ≥30 non-endgame canonical-slice games; call `compute_stage_a(user_id)`; assert row exists in user_benchmark_percentiles with metric='score_gap', non-null value, non-null percentile, n_games≥30, cdf_snapshot=date.today().
    - `test_compute_stage_a_below_floor` — seed user with 5 endgame + 5 non-endgame games (below 30/30 floor); call `compute_stage_a`; assert row exists with percentile=NULL but value still stored (per CONTEXT discretion item, recommendation: percentile=NULL + value stored when value computable).
    - `test_compute_stage_a_zero_canonical_games` — seed user with 0 canonical-slice games (e.g., all opponent-strength outside ±100); call `compute_stage_a`; assert NO row written.
    - `test_compute_stage_a_swallows_exception_and_captures_sentry` — monkey-patch `interpolate_percentile` to raise `RuntimeError`; call `compute_stage_a`; assert it returns None (no exception propagates) AND `sentry_sdk.capture_exception` was called with `set_context("percentile_compute", {"user_id": ..., "stage": "A"})`. Per D-04 + CLAUDE.md Sentry rules; assert message string does NOT contain `user_id` (V4 information-disclosure guard).
    - `test_compute_stage_b_writes_three_metrics` — seed user with sufficient eval-bearing canonical-slice games for all 3 eval-dependent metrics; call `compute_stage_b`; assert 3 rows written, all with non-null percentile.
    - `test_compute_stage_b_metric_below_floor_writes_null_percentile` — seed 100 achievable-floor games but only 5 parity spans; assert achievable row has percentile non-null, parity row has percentile=NULL, conv row depends on synthetic data shape (document choice in test).
    - `test_compute_stage_b_swallows_exception_and_captures_sentry` — monkey-patch one metric's CTE execution to raise; assert other 2 metrics still UPSERT; assert Sentry captured exactly once with `stage='B'`.

    Use `pytest.importorskip("app.services.user_benchmark_percentiles_service")` at module top.

    Create `tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py` (the SC-7 property gate):

    - `test_per_user_value_matches_benchmark_per_user_for_same_user_id` — seed synthetic DB with 5 users spanning 3 ELO buckets × 2 TC buckets, each with ≥30 endgame + ≥30 non-endgame games. Run the benchmark CDF generator's per-user aggregation against this DB (calling into the soon-to-be-shared `canonical_slice_sql.per_user_cte_for(...,source="benchmark")`), capture the per-user `metric_value` for each of the 5 user_ids. Then run the per-user compute path (`canonical_slice_sql.per_user_cte_for(...,source="single_user")`) for the same 5 user_ids; assert the values match within float epsilon (1e-9). Repeat for each of the 4 metrics.
    - Critical: per RESEARCH §Open Question 2, document in the test docstring that per-user pooled value is `avg(metric_value)` across cells (unweighted), matching the benchmark's `percentile_cont` treatment of per-(user, cell) rows.
    - This is the test the executor for Plan 03 must turn green; it's the canonical "did the extraction preserve semantics" gate.

    `pytest.importorskip("app.services.canonical_slice_sql")`.

    Create `tests/services/test_import_service_stage_a.py`:

    - `test_stage_a_fires_after_import_commit` — patch `compute_stage_a` to a recording spy; trigger `_complete_import_job` for a synthetic job; assert spy called exactly once with the correct `user_id`; assert the call occurs AFTER `session.commit()` (use a recorder on both `session.commit` and the spy to capture call ordering — the existing pytest-asyncio `freezegun` or call-order capture pattern via `unittest.mock.call_args_list` is fine).
    - `test_stage_a_runs_outside_import_transaction` — patch the import-tx session to raise on a final `await session.commit()` AFTER our hook captures the user_id; assert `compute_stage_a` was NOT called (hook is OUTSIDE the txn → it fires only after commit succeeds). Document the inverse: if hook were INSIDE the txn, our spy would have been called even on the failure. (ROADMAP SC 3.)
    - `test_stage_a_exception_does_not_break_import_completion` — patch `compute_stage_a` to raise `RuntimeError`; trigger `_complete_import_job`; assert `_complete_import_job` returns successfully (does NOT propagate the exception). Asyncio.create_task isolates the exception by design; this test pins the behaviour.

    `pytest.importorskip("app.services.user_benchmark_percentiles_service")`.

    Create `tests/services/test_eval_drain_stage_b.py`:

    - `test_stage_b_fires_only_for_users_whose_pending_count_drops_to_zero` — seed 2 users: user A with 10 games (drains all in one batch → pending after = 0), user B with 100 games (drains 10 in batch → pending after = 90). Run one drain tick; assert `compute_stage_b` was called for user A but NOT for user B (D-01).
    - `test_stage_b_fires_once_per_user_per_completion` — multi-user single-batch case; assert no duplicate `compute_stage_b(user_a)` calls when batch has multiple games for user A.
    - `test_stage_b_per_user_count_query_uses_partial_index` — sanity check: monkey-patch / spy on the SQL string or use `EXPLAIN` (if feasible against the test DB); assert the query targets `ix_games_evals_pending`. Defensible alternative: just assert `count_pending_evals` from game_repository.py was called with the correct user_id (the partial-index optimisation lives at the repo layer).
    - `test_stage_b_exception_does_not_crash_drain_coroutine` — patch `compute_stage_b` to raise; run one drain tick; assert drain returns successfully and continues to the next iteration (the asyncio.create_task fire-and-forget pattern isolates it).

    `pytest.importorskip("app.services.user_benchmark_percentiles_service")`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_user_benchmark_percentiles_service.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files committed
    - test_user_benchmark_percentiles_service.py has the 7 listed test functions
    - test_canonical_slice_per_user_vs_benchmark_parity.py parametrises across all 4 CdfMetricId values
    - test_import_service_stage_a.py asserts BOTH after-commit ordering AND no-propagation
    - test_eval_drain_stage_b.py covers pending=0 transition + multi-user + no-duplicate-fire + exception-isolation
    - All tests use `pytest.importorskip` so the suite is green pre-Plan-05
    - `grep -c "set_context.*percentile_compute" tests/services/test_user_benchmark_percentiles_service.py` returns ≥2 (covers both Stage A + Stage B Sentry assertions)
    - `grep -n "after.*commit\|AFTER.*commit" tests/services/test_import_service_stage_a.py` returns at least 1 match (after-commit ordering documented)
    - `uv run ruff check tests/services/` exits 0; `uv run ty check tests/services/` exits 0
  </acceptance_criteria>
  <done>Four Wave-0 backend test files committed; collection succeeds; Sentry-with-set_context + non-propagation contracts captured in tests.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold chip-decoupling + backfill-script + frontend tooltip tests</name>
  <files>tests/services/test_endgame_service_chip_decoupling.py, tests/scripts/test_backfill_user_percentiles.py, frontend/src/components/charts/__tests__/PercentileChip.test.tsx</files>
  <read_first>
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Security Domain V4, §Example 4, §Common Pitfalls 6+7)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (chip rewire + backfill script + tooltip copy sections)
    - app/services/endgame_service.py:1360-1400 and :2335-2360 (the 4 per-request interpolate_percentile call sites)
    - scripts/backfill_eval.py (lines 1-100 — _db_url + _assert_*_db + _mask_password patterns to reuse)
    - scripts/import_stress_monitor.py:1-50 (--target dev|prod argparse)
    - frontend/src/components/charts/PercentileChip.tsx (lines 60-100 — PercentileChipPopoverBody)
    - frontend/src/components/charts/__tests__/ (or wherever PercentileChip test currently lives — check `grep -rn "PercentileChip" frontend/src/components/charts/__tests__/`)
  </read_first>
  <action>
    Create `tests/services/test_endgame_service_chip_decoupling.py` (PCTL-07 / D-12 gate):

    - `test_chip_percentile_unchanged_across_filter_toggles` — seed user_benchmark_percentiles with a known `(value, percentile)` for the test user; make a GET to the endgame endpoint twice with two DIFFERENT filter sets (e.g., `recency=last_3_months` vs `recency=all_time`, `time_control=blitz` vs `time_control=rapid`); assert `response_a.score_gap_percentile == response_b.score_gap_percentile` and same for all 4 chipped metrics. The row's filter-applied `score_difference` value WILL differ — only the percentile is locked.
    - `test_chip_percentile_scopes_by_authenticated_user_id` (V4 Information Disclosure guard) — seed 2 users with distinct percentiles; authenticate as user A; assert response contains user A's percentile, NOT user B's; assert the SELECT in `endgame_service.py` filters `WHERE user_id = current_user.id` (verifiable via SQL log capture or repository-call inspection).
    - `test_chip_percentile_is_none_when_no_row_in_table` — seed user with no row in user_benchmark_percentiles; assert response has `{metric}_percentile: None` for all 4 chipped metrics (graceful degradation per Phase 94 contract).
    - `test_chip_percentile_is_none_when_percentile_column_is_null` — seed row with `value=X, percentile=NULL` (below-floor case); assert response field is `None`.

    Use `pytest.importorskip` on the repository module. Reuse existing endgame-router test fixtures (look for `tests/routers/test_endgames.py` or `tests/services/test_endgame_service.py`).

    Create `tests/scripts/test_backfill_user_percentiles.py` (PCTL-10 gate):

    - `test_backfill_is_idempotent_when_run_twice` — seed 3 users; run `main(target="dev", user_id=None, metric=None)` against test DB; capture row counts + computed_at values; run again; assert row counts unchanged, values unchanged (or computed_at advances but value/percentile match).
    - `test_backfill_with_user_id_filter_only_processes_that_user` — seed 3 users; run with `user_id=<user_2>`; assert only user_2's rows touched (computed_at advances only for user_2; users 1 and 3 unchanged or absent).
    - `test_backfill_with_metric_filter_only_processes_that_metric` — analogous; assert other 3 metrics untouched.
    - `test_backfill_target_prod_refuses_when_tunnel_down` — bypass actual prod connection; call `_assert_target_safe(url_with_port_15432, "prod")` with no socket listener on 15432; assert `SystemExit` with refusal message; assert message string contains the substring `bin/prod_db_tunnel.sh` for operator clarity.
    - `test_backfill_target_dev_refuses_url_without_port_5432` — call `_assert_target_safe("postgresql://localhost:99999/x", "dev")` → SystemExit; assert message contains `5432`.
    - `test_backfill_emits_per_metric_summary` — capture stdout; run backfill on 3-user fixture; assert output contains 4 metric labels and at least `upserted=` + `skipped=` tokens per metric (matches CONTEXT §Specifics summary shape).
    - `test_backfill_handles_user_with_zero_canonical_slice_games` — seed user with games all outside ±100 ELO opponent band; assert backfill writes NO row for that user (per CONTEXT discretion: "if value itself isn't computable, no row"); assert summary table reports `no_canonical_games=1`.

    `pytest.importorskip("scripts.backfill_user_percentiles")` — note the script lives under `scripts/` so importing it directly requires `sys.path` adjustment via conftest, OR the test invokes `main()` via subprocess (recommended subprocess for env-bootstrap clarity; the script's `_assert_target_safe` helper can be tested by importing it directly via `importlib`).

    Extend `frontend/src/components/charts/__tests__/PercentileChip.test.tsx` (CREATE if absent — check existing FE test layout first):

    - `it("renders skill-isolating popover with canonical-slice clarifier")` — render `PercentileChipPopoverBody` with `flavor="skill-isolating"`; assert popover text contains a substring matching the canonical-slice framing clarifier. Planner-recommended substring: `/career under matched conditions|canonical conditions|not the current filtered view/i` (any of these; gives Plan 07 latitude on exact wording within `feedback_popover_copy_minimalism.md` discipline).
    - `it("renders improvement-focus popover with canonical-slice clarifier")` — same for `flavor="improvement-focus"`.
    - `it("popover body remains within minimalism budget")` — render popover; assert text length is ≤ a constant `POPOVER_MAX_CHARS = 280` (1-2 short sentences, matches Phase 94 existing length per Pitfall 7). Comment in test source: "Pitfall 7 guard — popover MUST NOT bloat into a methodology paragraph."

    Use `pytest.importorskip` equivalent for FE: skip the new `it` blocks via `it.skip(...)` until Plan 07 lands. Existing PercentileChip tests (if any) MUST continue to pass — don't break the green frontend suite.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service_chip_decoupling.py tests/scripts/test_backfill_user_percentiles.py --collect-only -q && cd frontend && npx vitest run src/components/charts/__tests__/PercentileChip.test.tsx --reporter=verbose 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - 3 net-new test files committed (2 backend + 1 frontend)
    - test_endgame_service_chip_decoupling.py has the 4 listed tests; V4 cross-user-read guard test present
    - test_backfill_user_percentiles.py has the 7 listed tests including BOTH tunnel-refusal AND port-token refusal AND summary-stdout
    - PercentileChip.test.tsx contains 3 new `it` blocks; minimalism-budget test references `POPOVER_MAX_CHARS = 280` constant
    - `grep -n "WHERE user_id\|current_user.id\|authenticated user" tests/services/test_endgame_service_chip_decoupling.py` matches at least once (V4 documented)
    - `grep -n "tunnel\|15432\|prod_db_tunnel" tests/scripts/test_backfill_user_percentiles.py` returns multiple matches (Pitfall 6 covered)
    - Frontend test file passes `npm run lint` (no unused vars)
    - `uv run ruff check tests/` exits 0; `uv run ty check tests/` exits 0
  </acceptance_criteria>
  <done>3 net-new test files committed; tests skip cleanly pre-implementation; downstream Plans 07/08 have ready-made gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → endgame API | Authenticated request via FastAPI-Users dep; `user_id` derived server-side (V4) |
| CLI → backfill script DB | `--target dev|prod` flag → strict URL guard before any I/O (V4 Tampering) |
| Background task → Sentry | Error payload must not leak user_id in message (V4 Information Disclosure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-03 | Information Disclosure (V4) | endgame_service chip read path | mitigate | `test_chip_percentile_scopes_by_authenticated_user_id` verifies SELECT filters by `current_user.id`; user_id is NEVER accepted as a query param. |
| T-94.1-04 | Tampering (V4) | backfill script DB target resolution | mitigate | `test_backfill_target_prod_refuses_when_tunnel_down` + `test_backfill_target_dev_refuses_url_without_port_5432` pin the `_assert_target_safe` guard per Pitfall 6. |
| T-94.1-05 | Information Disclosure (V4) | Sentry capture in Stage A / B failure | mitigate | `test_compute_stage_*_swallows_exception_and_captures_sentry` asserts message string does NOT contain user_id; only `set_context` carries it. CLAUDE.md Sentry rule. |
</threat_model>

<verification>
- 7 net-new test files committed
- `uv run pytest --collect-only -q` succeeds across all new tests
- Frontend `npm test` collection includes the new PercentileChip cases
- Pre-PR gate: ruff/ty/pytest -x green on tests-only
</verification>

<success_criteria>
- Every Wave 0 test file from VALIDATION.md exists by end of Plans 01 + 02 (11 total per VALIDATION list, this plan adds 7, Plan 01 adds 5 — overlap on test_alembic_migrations.py: Plan 01 owns alembic + repo + canonical_slice_sql + gen-script regression + ENUM; Plan 02 owns service + parity + hooks + chip + backfill + frontend tooltip)
- Test functions exercise the explicit threat-model rows (V4 cross-user, Sentry leak, tunnel mismatch)
- Suite stays green pre-Plans-05/06/07/08
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-02-SUMMARY.md` when done — list the 7 net-new test files, count test functions per file, and confirm `pytest --collect-only` + `npx vitest run --reporter=verbose` discover all new cases.
</output>
