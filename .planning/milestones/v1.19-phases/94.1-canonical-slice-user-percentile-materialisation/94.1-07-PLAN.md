---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 07
type: execute
wave: 4
depends_on:
  - 94.1-04
  - 94.1-06
files_modified:
  - app/services/endgame_service.py
  - frontend/src/components/charts/PercentileChip.tsx
autonomous: true
requirements:
  - PCTL-07
must_haves:
  truths:
    - "The 4 per-request `interpolate_percentile(metric_id, filter_applied_value)` calls in endgame_service.py are removed (D-12)"
    - "The 4 `{metric}_percentile` response fields read `(value, percentile)` from `user_benchmark_percentiles` via `fetch_for_user(session, user_id=current_user.id)` — filter-independent"
    - "The SELECT scopes by the authenticated user_id from FastAPI-Users dep — never accepts user_id as a query param (V4)"
    - "One SELECT round-trip per request fetches all 4 metrics (not 4 round trips)"
    - "PercentileChip.tsx tooltip copy adds a canonical-slice clarifier within `feedback_popover_copy_minimalism.md` discipline — WHAT + sign convention only, no jargon, no caveats, ≤ 280 chars (D-13)"
    - "The Phase 94 `interpolate_percentile` helper remains in `app/services/global_percentile_cdf.py` (still consumed by Plan 05 compute service); only the 4 per-request call sites in endgame_service are removed"
    - "Toggling UI filters does not change the chip — verified by `test_endgame_service_chip_decoupling.py`"
  artifacts:
    - path: "app/services/endgame_service.py"
      provides: "Chip read path rewired to user_benchmark_percentiles per-user SELECT"
    - path: "frontend/src/components/charts/PercentileChip.tsx"
      provides: "Updated popover body with canonical-slice clarifier (≤ 280 chars)"
  key_links:
    - from: "endgame_service.py chip emission"
      to: "user_benchmark_percentiles_repository.fetch_for_user (Plan 04)"
      via: "single SELECT per request, scoped WHERE user_id = current_user.id"
      pattern: "fetch_for_user(session, user_id="
    - from: "PercentileChip popover body"
      to: "feedback_popover_copy_minimalism.md discipline"
      via: "1-2 sentence copy; no jargon ('canonical slice', 'sigmoid', 'Wilson' forbidden); no caveats"
      pattern: "≤ POPOVER_MAX_CHARS"
---

<objective>
Rewire the chip from per-request `interpolate_percentile()` to per-user materialised table read. 4 backend call sites collapse to 1 SELECT per request; 1 frontend popover body adds a canonical-slice clarifier.

This is PCTL-07 + D-12 + D-13. The chip becomes a trait of the user, not a view of their data.

Purpose: Final user-visible behavior change. Filters now move the row's value but NOT the chip.
Output: 2 modified files; Plan 02's `test_endgame_service_chip_decoupling.py` + `PercentileChip.test.tsx` (the 3 new `it` blocks) turning green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md

<interfaces>
Existing — consumed:
- `app.repositories.user_benchmark_percentiles_repository.fetch_for_user(session, *, user_id: int) -> dict[CdfMetricId, PercentileRow]` (Plan 04)
- `app.repositories.user_benchmark_percentiles_repository.PercentileRow` TypedDict (Plan 04)
- The 4 `{metric}_percentile` response fields already on the Phase 94 response models — unchanged in shape, only the data source moves
- FastAPI-Users `current_active_user` dep — already wired into the endgame router upstream of `endgame_service`; the `current_user` is already in scope at the call sites (or one frame up — verify)

Existing — to be removed/edited:
- `app/services/endgame_service.py:1369-1394` — 4 per-request `interpolate_percentile(metric_id, value)` calls for the 2 page-level chips (Endgame Score Gap + Achievable Score Gap)
- `app/services/endgame_service.py:2344-2348` — 2 per-request `interpolate_percentile` calls for Section 2 ΔES chips (Parity + Conversion)
- `app/services/global_percentile_cdf.py:interpolate_percentile` — STAYS (still consumed by Plan 05 compute service)

Frontend:
- `frontend/src/components/charts/PercentileChip.tsx:71-86` — `PercentileChipPopoverBody({ flavor })` returns `<p>...</p>` per flavor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire endgame_service chip emission to table-backed read</name>
  <files>app/services/endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py (lines 1340-1420 — the 2 page-level chip call sites for score_gap + achievable_score_gap)
    - app/services/endgame_service.py (lines 2320-2380 — the 2 Section 2 chip call sites for section2_score_gap_parity + section2_score_gap_conv)
    - app/services/endgame_service.py (locate the function signatures containing those line ranges — what params does each take? Is `current_user` already in scope or must it be threaded via a new param? Trace one frame up to the router caller if needed)
    - app/repositories/user_benchmark_percentiles_repository.py (Plan 04 — `fetch_for_user` signature + PercentileRow TypedDict)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`app/services/endgame_service.py:1369-1394, 2344-2348` rewire section)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Security Domain V4 — SELECT MUST scope by current_user.id)
    - tests/services/test_endgame_service_chip_decoupling.py (Plan 02 — the contract this satisfies)
  </read_first>
  <action>
    Rewire the chip read path:

    1. Import `fetch_for_user` from `app.repositories.user_benchmark_percentiles_repository` at the top of `endgame_service.py`.

    2. Identify where the 4 chip emissions live. They are likely in 2 distinct functions:
       - One function emitting the 2 page-level chip percentiles (lines ~1369-1394)
       - One function emitting the 2 Section 2 chip percentiles (lines ~2344-2348)

    3. For each function, identify whether `current_user.id` (or equivalent) is already in scope:
       - If YES (already a param or accessible via a context object): use it directly.
       - If NO: thread it as a new keyword argument from the router-facing entry point down to the function. The router already has FastAPI-Users `current_active_user` dep injected — verify the call chain and add the param where needed. Document the threading in the diff if multiple call sites change.

    4. At the top of each emission function (or once per request — preferably at the router-facing entry point so a single SELECT serves all 4 metrics):
       ```
       # Phase 94.1 D-12 / PCTL-07: chip is a trait, not a view. Read materialised
       # (value, percentile) per metric from user_benchmark_percentiles; scoped by
       # current_user.id (V4 — never accept user_id as query param).
       percentile_rows = await fetch_for_user(session, user_id=current_user.id)
       ```

    5. Replace each `interpolate_percentile(metric_id, score_difference) if ... else None` call with:
       ```
       <metric>_percentile = percentile_rows.get(<metric_id_literal>, {}).get("percentile")
       ```
       Or, if PercentileRow is a dataclass instead of TypedDict, adjust accessor accordingly.

       Concretely:
       - `score_gap_percentile = percentile_rows.get("score_gap", {}).get("percentile")`
       - `achievable_score_gap_percentile = percentile_rows.get("achievable_score_gap", {}).get("percentile")`
       - `section2_score_gap_parity_percentile = percentile_rows.get("section2_score_gap_parity", {}).get("percentile")`
       - `section2_score_gap_conv_percentile = percentile_rows.get("section2_score_gap_conv", {}).get("percentile")`

    6. Delete the 4 per-request `interpolate_percentile(...)` call lines. Do NOT delete the import — confirm whether `interpolate_percentile` is used elsewhere in the file before removing the import. If unused after this rewire, remove the import too (knip equivalent — keeps `ty` and ruff happy).

    7. Verify the `WHERE user_id = current_user.id` scoping (V4) — Plan 04's `fetch_for_user` already enforces the keyword-only `user_id` param; pass `current_user.id` directly, NEVER a value from a query param or path param.

    8. The pre-existing Phase 94 N-gate logic (e.g., `min(endgame_wdl.total, non_endgame_wdl.total) >= PVALUE_RELIABILITY_MIN_N`) becomes redundant for the chip emission — the table's `percentile` column is already NULL when below floor (set during Stage A/B compute per Plan 05). Either:
       - Keep the N-gate check as a defense-in-depth (acceptable — adds belt-and-suspenders), OR
       - Remove it since the table is authoritative (cleaner; aligns with D-12 "chip is a trait").
       Recommend REMOVE the N-gate check at the chip emission since it duplicates a check Stage A/B already enforces at write time. Document the choice inline.

    9. The row's filter-applied `score_difference` value continues to render from the existing per-request compute — DO NOT touch any code computing the row's value; only the percentile field source moves.

    Verify by re-reading the diff:
    - 4 `interpolate_percentile(...)` calls removed (in endgame_service.py).
    - 1 `fetch_for_user(session, user_id=current_user.id)` added (or 2 if the 2 emission functions can't share one SELECT — document why).
    - V4 compliance: `user_id` flows from FastAPI-Users dep, never from request input.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service_chip_decoupling.py -x && uv run ty check app/services/endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interpolate_percentile(" app/services/endgame_service.py` returns 0 (all 4 call sites removed)
    - `grep -n "fetch_for_user(.*user_id=" app/services/endgame_service.py` returns at least 1 match
    - `grep -n "fetch_for_user(.*user_id=\(request\.\|payload\.\|body\.\|query\)" app/services/endgame_service.py` returns 0 (V4: user_id never sourced from request)
    - The 4 `{metric}_percentile` response field names are UNCHANGED on the API response (verifiable by `grep -c "score_gap_percentile\|achievable_score_gap_percentile\|section2_score_gap_parity_percentile\|section2_score_gap_conv_percentile" app/services/endgame_service.py` — counts unchanged)
    - All 4 tests in `test_endgame_service_chip_decoupling.py` pass: filter-toggle invariance, V4 cross-user-scope, no-row→None, NULL-percentile→None
    - `uv run ty check app/services/endgame_service.py` exits 0
    - `app/services/global_percentile_cdf.py:interpolate_percentile` is UNCHANGED (still consumed by Plan 05): `git diff app/services/global_percentile_cdf.py` should show no changes from this task
  </acceptance_criteria>
  <done>Chip is now a trait, not a view; filter toggles do not change the percentile; V4 scoping enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Update PercentileChip tooltip copy with canonical-slice clarifier</name>
  <files>frontend/src/components/charts/PercentileChip.tsx</files>
  <read_first>
    - frontend/src/components/charts/PercentileChip.tsx (full file — confirm `PercentileChipPopoverBody` at lines 71-86 and how `flavor` is plumbed through)
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx (Plan 02 — the 3 new `it` blocks; check what substring regex they assert, and adjust copy to satisfy)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Common Pitfalls 7 — verbosity guard; §A4 — fit within minimalism budget)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`frontend/src/components/charts/PercentileChip.tsx:71-86` section — suggested phrase: "Reflects your career under matched conditions, not the current filtered view")
    - .claude project memories: `feedback_popover_copy_minimalism.md` discipline — WHAT + sign convention only, no jargon, no caveats; `feedback_benchmark_source_of_truth.md` — verify any normative copy against `reports/benchmarks-latest.md`
  </read_first>
  <action>
    Update `PercentileChipPopoverBody` for both flavors.

    Required additions per D-13:
    - Each flavor's `<p>` content adds ONE phrase clarifying the canonical-slice framing — that the chip reflects the user's career under matched conditions (status='completed', ±100 ELO opponents, recent games), independent of UI filter toggles.
    - The row's filter-applied metric value continues to render per the existing per-request compute; the chip's tooltip explains the dual-value framing.

    Discipline (per `feedback_popover_copy_minimalism.md` + Pitfall 7):
    - Total popover body length per flavor ≤ 280 characters (matches Plan 02's `POPOVER_MAX_CHARS = 280` invariant).
    - No jargon: NEVER use "canonical slice", "Wilson", "sigmoid", "CDF", "interpolation", "filter-independent".
    - No caveats: NEVER use "approximately", "roughly", "tends to", "in most cases".
    - WHAT + sign convention only. The existing copy already does this; the new clarifier rides alongside.

    Suggested form (planner-recommended — exact strings are executor's call within the discipline):

    For `flavor === 'skill-isolating'`:
    ```
    Where you rank vs all players. Mostly independent of rating, reveals endgame ability separate
    from overall strength. Reflects your career under matched conditions, not the current filtered view.
    ```

    For `flavor === 'improvement-focus'`:
    ```
    Where you rank vs all players. Conversion tracks rating closely. If you're in the lower tiers here,
    this is one of the biggest single improvements available to your ELO. Reflects your career under
    matched conditions, not the current filtered view.
    ```

    Verify against `feedback_popover_copy_minimalism.md` discipline: count chars per body, ensure ≤ 280; confirm no forbidden jargon tokens.

    The PercentileChip component shape is otherwise UNCHANGED — no prop changes, no new component, just string edits.

    Mobile + desktop apply identically (same component renders both).

    Pre-PR gate at end of Task 2:
    - `uv run ruff format/check + ty check app/` — already covered by Task 1 if Task 2 runs after; rerun if needed.
    - `cd frontend && npm run lint` (knip + tsc + eslint per CLAUDE.md frontend gates)
    - `cd frontend && npm test -- --run` (full FE suite; ensure Plan 02's 3 new `it` blocks now pass)
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "canonical slice\|Wilson\|sigmoid\|CDF\|interpolation" frontend/src/components/charts/PercentileChip.tsx` returns 0 (no jargon)
    - `grep -c "matched conditions\|filtered view\|under matched\|career under" frontend/src/components/charts/PercentileChip.tsx` returns ≥ 2 (one per flavor) — the regex Plan 02's `PercentileChip.test.tsx` asserts
    - Each flavor's popover body string ≤ 280 chars (Plan 02's `POPOVER_MAX_CHARS` invariant)
    - All 3 new `it` blocks in `PercentileChip.test.tsx` pass: skill-isolating clarifier present, improvement-focus clarifier present, body within minimalism budget
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm test -- --run` shows the PercentileChip test file all-green
    - Pre-PR gate clean (frontend lint + test gates)
  </acceptance_criteria>
  <done>Tooltip copy updated with canonical-slice clarifier within minimalism discipline; all FE chip-copy tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → endgame_service | `current_user.id` derived server-side from FastAPI-Users dep, NEVER from request body / query / path (V4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-15 | Information Disclosure (V4) | endgame_service chip read path | mitigate | `fetch_for_user(session, user_id=current_user.id)` — the SELECT scopes by the authenticated user's ID, derived from the FastAPI-Users dep. Plan 02 `test_chip_percentile_scopes_by_authenticated_user_id` enforces the contract. RESEARCH §Security Domain row 2. |
</threat_model>

<verification>
- 4 `interpolate_percentile(...)` call sites removed from endgame_service.py
- 1 `fetch_for_user(...)` call added, scoped by `current_user.id`
- `interpolate_percentile` helper itself unchanged in `global_percentile_cdf.py` (still consumed by Plan 05)
- All Plan 02 chip-decoupling backend tests + frontend tooltip tests pass
- Frontend lint + test gates clean
</verification>

<success_criteria>
- Filter toggle does not change the chip (PCTL-07 / D-12)
- V4 cross-user-read enforced
- Tooltip copy adds canonical-slice clarifier within minimalism discipline (D-13)
- API response field names unchanged (Phase 94 FE contract preserved)
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-07-SUMMARY.md` — record the exact tooltip copy chosen for both flavors with char counts, confirm 4 `interpolate_percentile` call sites removed, confirm chip-decoupling test green, document any threading of `current_user.id` through endgame_service signatures.
</output>
