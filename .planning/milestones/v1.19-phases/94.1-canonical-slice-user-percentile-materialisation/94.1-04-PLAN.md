---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 04
type: execute
wave: 1
depends_on:
  - 94.1-01
files_modified:
  - alembic/versions/20260524_user_benchmark_percentiles.py
  - app/models/user_benchmark_percentile.py
  - app/models/__init__.py
  - app/repositories/user_benchmark_percentiles_repository.py
autonomous: true
requirements:
  - PCTL-08
must_haves:
  truths:
    - "Alembic migration creates Postgres ENUM type `benchmark_metric` with exactly 4 labels BEFORE creating the table that references it"
    - "Migration downgrade drops the table THEN the type (Pitfall 4 ordering)"
    - "ORM model `UserBenchmarkPercentile` uses `create_type=False` per existing project precedent (app/models/game.py)"
    - "Repository exports `upsert_percentile` (on_conflict_do_update) + `fetch_for_user` (per-user dict-keyed SELECT)"
    - "Migration runs cleanly on dev DB without `bin/reset_db.sh` (forward `alembic upgrade head` only — per `feedback_no_dev_db_reset_in_plans`)"
    - "ENUM contains exactly the 4 expected labels — `test_benchmark_metric_enum.py` passes"
  artifacts:
    - path: "alembic/versions/20260524_user_benchmark_percentiles.py"
      provides: "ENUM type + table creation/destruction lifecycle"
    - path: "app/models/user_benchmark_percentile.py"
      provides: "SQLAlchemy ORM model + benchmark_metric_enum SAEnum"
    - path: "app/repositories/user_benchmark_percentiles_repository.py"
      provides: "upsert_percentile + fetch_for_user helpers + PercentileRow dataclass/TypedDict"
  key_links:
    - from: "app/models/user_benchmark_percentile.py"
      to: "users.id (FK CASCADE)"
      via: "ForeignKey('users.id', ondelete='CASCADE')"
      pattern: "ondelete=\"CASCADE\""
    - from: "alembic upgrade head"
      to: "Postgres benchmark_metric ENUM + user_benchmark_percentiles table"
      via: "benchmark_metric_enum.create(op.get_bind(), checkfirst=True) THEN op.create_table"
      pattern: "create(op.get_bind(), checkfirst=True)"
---

<objective>
Land the schema: 1 Alembic migration creating the `benchmark_metric` Postgres ENUM type + `user_benchmark_percentiles` table; 1 SQLAlchemy ORM model with `create_type=False`; 1 repository with `upsert_percentile` (on_conflict_do_update) + `fetch_for_user` (per-user dict-keyed SELECT). Plan 01's 3 migration/ENUM/repo tests turn green.

This is PCTL-08 + D-05 + D-06 + D-07 + D-08 from CONTEXT.md.

Purpose: Persistent home for canonical-slice values + percentiles. Phase 94.1's central artifact.
Output: 1 new migration + 1 new model + 1 new repository module + model __init__ registration; 3 Wave-0 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md

<interfaces>
Reuse from project:
- `app.models.game.game_result_enum` + `app.models.game` ENUM precedent — `SAEnum(... name="...", create_type=False)`
- `alembic/versions/20260408_155149_3e4018d62102_convert_result_user_color_termination_.py` — migration ENUM lifecycle precedent
- `app.repositories.game_repository:11-35` — `pg_insert().on_conflict_do_nothing()` precedent (extend to `on_conflict_do_update`)
- `app.services.global_percentile_cdf.CdfMetricId` Literal — the type for `metric` column
- `users.id` (INTEGER) — FK target

Will be created:
- `app.models.user_benchmark_percentile.UserBenchmarkPercentile` ORM model
- `app.models.user_benchmark_percentile.benchmark_metric_enum` SAEnum
- `app.repositories.user_benchmark_percentiles_repository.upsert_percentile(session, *, user_id: int, metric: CdfMetricId, value: float, percentile: float | None, n_games: int, cdf_snapshot: date) -> None`
- `app.repositories.user_benchmark_percentiles_repository.fetch_for_user(session, *, user_id: int) -> dict[CdfMetricId, PercentileRow]`
- `app.repositories.user_benchmark_percentiles_repository.PercentileRow` — TypedDict or dataclass with `value: float`, `percentile: float | None`, `n_games: int`, `cdf_snapshot: date`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration — benchmark_metric ENUM + user_benchmark_percentiles table</name>
  <files>alembic/versions/20260524_user_benchmark_percentiles.py</files>
  <read_first>
    - alembic/versions/20260408_155149_3e4018d62102_convert_result_user_color_termination_.py (lines 1-100 — the ENUM lifecycle precedent: order of create/drop, `create_type=False` pattern, downgrade ordering)
    - alembic/versions/ (recent migrations — find the latest `down_revision` to use as parent; check `alembic heads` if uncertain)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`alembic/versions/XXXX_user_benchmark_percentiles.py` section — verbatim shape)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 2 + §Common Pitfalls 3+4)
    - tests/integration/test_alembic_migrations.py (Plan 01 — the contract this migration satisfies)
    - tests/integration/test_benchmark_metric_enum.py (Plan 01 — pg_enum introspection contract)
  </read_first>
  <action>
    Author the migration at `alembic/versions/20260524_user_benchmark_percentiles.py` (filename pattern: `YYYYMMDD_HHMMSS_<revisionid>_add_user_benchmark_percentiles.py` — generate via `uv run alembic revision -m "add user_benchmark_percentiles"` then edit; OR follow the existing naming convention you observe in `ls alembic/versions/`).

    Header:
    - `revision = "<auto-generated>"`
    - `down_revision = "<latest head from alembic/versions>"` — verify with `uv run alembic heads` or `ls -t alembic/versions/ | head -5`
    - `branch_labels = None`
    - `depends_on = None`

    Body — Postgres ENUM type lifecycle per Pattern 2:

    Define `benchmark_metric_enum = postgresql.ENUM("score_gap", "achievable_score_gap", "section2_score_gap_conv", "section2_score_gap_parity", name="benchmark_metric", create_type=False)` at module level.

    `upgrade()` — ORDER MATTERS (Pitfall 3):
    1. `benchmark_metric_enum.create(op.get_bind(), checkfirst=True)` — type FIRST
    2. `op.create_table("user_benchmark_percentiles", ...)` with columns per D-08:
       - `user_id` — `sa.Integer()`, `sa.ForeignKey("users.id", ondelete="CASCADE")`, `nullable=False`
       - `metric` — `benchmark_metric_enum`, `nullable=False`
       - `value` — `sa.Float()` (= DOUBLE PRECISION on Postgres), `nullable=False`
       - `percentile` — `sa.Float()`, `nullable=True`
       - `n_games` — `sa.Integer()`, `nullable=False`
       - `cdf_snapshot` — `sa.Date()`, `nullable=False`
       - `computed_at` — `sa.DateTime(timezone=True)`, `server_default=sa.func.now()`, `nullable=False`
       - `sa.PrimaryKeyConstraint("user_id", "metric")` (composite PK; no surrogate id per ROADMAP SC 1)

    `downgrade()` — ORDER MATTERS (Pitfall 4):
    1. `op.drop_table("user_benchmark_percentiles")` — table FIRST
    2. `benchmark_metric_enum.drop(op.get_bind(), checkfirst=True)` — type SECOND

    Comment block at top of file referencing D-05 + the project-first-ENUM rationale: "This is FlawChess's first new Postgres ENUM type since the 2026-04-08 enum-conversion migration. The 4-value set mirrors `CdfMetricId` Literal in app/services/global_percentile_cdf.py; future metric extensions require `ALTER TYPE benchmark_metric ADD VALUE 'new_metric'` in a follow-up migration. See CONTEXT.md D-05."

    Verify forward-only migration works on dev (per `feedback_no_dev_db_reset_in_plans` — NO `bin/reset_db.sh`):
    - `uv run alembic upgrade head` — succeeds against dev DB without reset.
    - `uv run alembic downgrade -1` — succeeds; table + type gone.
    - `uv run alembic upgrade head` — succeeds again (idempotent on the +1/-1 cycle).
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run pytest tests/integration/test_alembic_migrations.py tests/integration/test_benchmark_metric_enum.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `alembic/versions/*user_benchmark_percentiles*.py`
    - `benchmark_metric_enum` defined with `create_type=False` at module level
    - `upgrade()` calls `benchmark_metric_enum.create(...)` BEFORE `op.create_table(...)` (verify by `grep -n` for the two lines and confirming line numbers)
    - `downgrade()` calls `op.drop_table(...)` BEFORE `benchmark_metric_enum.drop(...)` (Pitfall 4)
    - Composite PK on `(user_id, metric)`; NO surrogate `id` column
    - FK constraint `user_id → users.id` has `ondelete="CASCADE"` (verify by `grep -n "ondelete" alembic/versions/*user_benchmark_percentiles*.py`)
    - `uv run alembic upgrade head` succeeds without `bin/reset_db.sh`
    - `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trip succeeds
    - `uv run pytest tests/integration/test_alembic_migrations.py tests/integration/test_benchmark_metric_enum.py -x` exits 0
    - `psql ... -c "SELECT enumlabel FROM pg_enum e JOIN pg_type t ON e.enumtypid=t.oid WHERE t.typname='benchmark_metric' ORDER BY enumlabel"` returns exactly 4 rows
  </acceptance_criteria>
  <done>Migration committed; upgrade+downgrade round-trip works; ENUM has exactly 4 labels; Plan 01 alembic + ENUM tests green.</done>
</task>

<task type="auto">
  <name>Task 2: ORM model + repository module + model registration</name>
  <files>app/models/user_benchmark_percentile.py, app/models/__init__.py, app/repositories/user_benchmark_percentiles_repository.py</files>
  <read_first>
    - app/models/game.py (lines 1-80 — `SAEnum(create_type=False)` pattern + ForeignKey + Mapped column patterns)
    - app/models/__init__.py (registration shape — confirm new model needs to be imported here for Alembic autogenerate to see it on future revisions)
    - app/repositories/game_repository.py (lines 1-60 — `pg_insert().on_conflict_do_nothing` precedent + `count_pending_evals` shape)
    - app/services/global_percentile_cdf.py (CdfMetricId Literal — re-import; do not redefine)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (model + repository analog sections)
    - tests/repositories/test_user_benchmark_percentiles_repository.py (Plan 01 — the contract this module satisfies)
  </read_first>
  <action>
    Author `app/models/user_benchmark_percentile.py`:

    1. Imports: `from sqlalchemy import Enum as SAEnum, ForeignKey, Float, Integer, Date, DateTime, func` + `from sqlalchemy.orm import Mapped, mapped_column` + `from datetime import date, datetime` + `from typing import Literal` + `from app.core.database import Base` (confirm Base import path from existing models) + `from app.services.global_percentile_cdf import CdfMetricId`.

    2. Module-level `benchmark_metric_enum = SAEnum("score_gap", "achievable_score_gap", "section2_score_gap_conv", "section2_score_gap_parity", name="benchmark_metric", create_type=False)` — `create_type=False` per Pattern 2 (Alembic owns the lifecycle).

    3. Class `UserBenchmarkPercentile(Base)`:
       - `__tablename__ = "user_benchmark_percentiles"`
       - `user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id", ondelete="CASCADE"), primary_key=True)`
       - `metric: Mapped[CdfMetricId] = mapped_column(benchmark_metric_enum, primary_key=True)`
       - `value: Mapped[float] = mapped_column(Float, nullable=False)`
       - `percentile: Mapped[float | None] = mapped_column(Float, nullable=True)`
       - `n_games: Mapped[int] = mapped_column(Integer, nullable=False)`
       - `cdf_snapshot: Mapped[date] = mapped_column(Date, nullable=False)`
       - `computed_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)`

    4. Update `app/models/__init__.py` — add `from app.models.user_benchmark_percentile import UserBenchmarkPercentile` (or the equivalent re-export pattern observed in the existing file).

    Author `app/repositories/user_benchmark_percentiles_repository.py`:

    1. Imports: `pg_insert` from `sqlalchemy.dialects.postgresql` + `select` + `func` from `sqlalchemy.sql` + `AsyncSession` + `TypedDict` from typing + `date` from datetime + `CdfMetricId` + `UserBenchmarkPercentile`.

    2. `class PercentileRow(TypedDict): value: float; percentile: float | None; n_games: int; cdf_snapshot: date` — internal TypedDict matching SELECT projection per CLAUDE.md TypedDict-for-internal-structured-data rule.

    3. `async def upsert_percentile(session: AsyncSession, *, user_id: int, metric: CdfMetricId, value: float, percentile: float | None, n_games: int, cdf_snapshot: date) -> None`:
       - Build `pg_insert(UserBenchmarkPercentile).values(user_id=user_id, metric=metric, value=value, percentile=percentile, n_games=n_games, cdf_snapshot=cdf_snapshot)`
       - Chain `.on_conflict_do_update(index_elements=["user_id", "metric"], set_={"value": stmt.excluded.value, "percentile": stmt.excluded.percentile, "n_games": stmt.excluded.n_games, "cdf_snapshot": stmt.excluded.cdf_snapshot, "computed_at": func.now()})`
       - `await session.execute(stmt)` — caller commits.

    4. `async def fetch_for_user(session: AsyncSession, *, user_id: int) -> dict[CdfMetricId, PercentileRow]`:
       - `SELECT metric, value, percentile, n_games, cdf_snapshot FROM user_benchmark_percentiles WHERE user_id = :user_id` — use SQLAlchemy `select()` API per CLAUDE.md "not legacy 1.x".
       - V4 Information Disclosure mitigation: this function REQUIRES `user_id` as a keyword arg; the caller in Plan 07 will pass `current_user.id` from the FastAPI-Users dep. Document in docstring: "Caller MUST pass the authenticated user's ID; never accept user_id as a query param."
       - Returns dict keyed by metric ID, with PercentileRow values.

    5. Type-safety per CLAUDE.md — every function has explicit return type annotations; no bare `str`.

    Pre-PR gate at the end of Task 2:
    - `uv run ruff format app/models/user_benchmark_percentile.py app/models/__init__.py app/repositories/user_benchmark_percentiles_repository.py`
    - `uv run ruff check app/models/ app/repositories/`
    - `uv run ty check app/`
    - `uv run pytest tests/integration/test_alembic_migrations.py tests/integration/test_benchmark_metric_enum.py tests/repositories/test_user_benchmark_percentiles_repository.py -x`
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_user_benchmark_percentiles_repository.py -x && uv run ty check app/models/ app/repositories/</automated>
  </verify>
  <acceptance_criteria>
    - 3 files committed: model + model __init__ update + repository
    - Model uses `SAEnum(..., create_type=False)` (verify `grep -n "create_type=False" app/models/user_benchmark_percentile.py`)
    - Model PK is composite via `primary_key=True` on both `user_id` and `metric` columns (no `PrimaryKeyConstraint` separate stanza needed when using `primary_key=True` on Mapped columns)
    - `app/models/__init__.py` imports UserBenchmarkPercentile (verify `grep -n "UserBenchmarkPercentile" app/models/__init__.py`)
    - Repository `upsert_percentile` uses `on_conflict_do_update` (NOT `on_conflict_do_nothing`); `computed_at` updates via `func.now()` in the `set_` dict
    - Repository `fetch_for_user` docstring documents V4 mitigation ("caller must pass authenticated user's ID; never accept as query param")
    - `PercentileRow` is `TypedDict` (per CLAUDE.md internal-structured-data rule)
    - All Plan 01 repo tests pass: `uv run pytest tests/repositories/test_user_benchmark_percentiles_repository.py -x` exits 0 (6 tests green)
    - `uv run ty check app/models/ app/repositories/` exits 0
    - Pre-PR gate clean
  </acceptance_criteria>
  <done>Model + repository committed; FK CASCADE + UPSERT idempotency tests green; Plan 05 can now write to the table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository `fetch_for_user` caller → DB | Caller must supply authenticated `user_id`; never propagate query-string user_id (V4) |
| migration → ENUM creation | ENUM type must exist before column references it (V5 Tampering — bad ordering = broken schema) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-08 | Tampering (V5) | benchmark_metric ENUM | mitigate | Migration creates ENUM with exactly 4 labels via `postgresql.ENUM(..., create_type=False)` + `enum.create(checkfirst=True)`; Plan 01's `test_benchmark_metric_enum.py` enforces the 4-label invariant against `pg_enum`. Pitfall 3 + Pitfall 4 guards. |
| T-94.1-09 | Information Disclosure (V4) | fetch_for_user | mitigate | Function signature requires `user_id` keyword arg; docstring documents caller contract: "Caller MUST pass authenticated user's ID; never accept user_id as a query param." Plan 07 chip rewire wires this to `current_user.id` from FastAPI-Users dep. |
</threat_model>

<verification>
- `uv run alembic upgrade head` succeeds on dev DB (no `bin/reset_db.sh`)
- `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trip succeeds
- ENUM contains exactly 4 expected labels
- Plan 01's 3 schema/ENUM/repo tests are GREEN
- Pre-PR gate: ruff/ty/pytest all green
</verification>

<success_criteria>
- Migration applies forward and rolls back cleanly
- Model uses project ENUM precedent (`create_type=False`)
- Repository UPSERT is idempotent and atomic
- FK CASCADE deletes percentile rows on user deletion
- Plan 05 has a working write path; Plan 07 has a working read path
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-04-SUMMARY.md` — record migration filename + revision id, list ENUM labels confirmed in pg_enum, confirm upgrade+downgrade round-trip, list test functions newly green.
</output>
