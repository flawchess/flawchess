---
phase: 94-backend-frontend-percentile-annotations
plan: 03
type: execute
wave: 3
depends_on: [94-02]
files_modified:
  - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/components/charts/EndgameMetricsSection.tsx
  - frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx
  - frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx
autonomous: false
requirements: [PCTL-03, PCTL-04, PCTL-05, PCTL-06]
tags: [frontend, react, percentile, chip, wiring, uat, mobile]

must_haves:
  truths:
    - "`ScoreGapRow` accepts a new optional `chipSlot?: ReactNode` prop that defaults to `undefined`, leaving all existing consumers (especially `EndgameTypeCard.tsx`) pixel-identical (RESEARCH §Pitfall 2)"
    - "`EndgameOverallPerformanceSection` passes a `PercentileChip` to BOTH the Achievable Score Gap row and the Endgame Score Gap row when their respective `*_percentile` field is non-null"
    - "Both page-level chips use `flavor=\"skill-isolating\"` (D-08)"
    - "`EndgameMetricCard` accepts a new `scoreGapPercentile: number | null` prop and renders a `PercentileChip` via `ScoreGapRow.chipSlot` for the conversion + parity buckets only — Recovery is statically excluded via a `bucket !== 'recovery'` guard (D-12, Pitfall 5)"
    - "Conversion bucket uses `flavor=\"improvement-focus\"`; parity bucket uses `flavor=\"skill-isolating\"` (D-08)"
    - "`EndgameMetricsSection` (the parent) routes `scoreGapPercentile={section2_score_gap_conv_percentile}` to the conversion card, `={section2_score_gap_parity_percentile}` to the parity card, and `={null}` to the recovery card"
    - "Recovery card NEVER renders a chip, even if a future backend change emitted a non-null recovery percentile — defensive `bucket !== 'recovery'` guard enforced in component code AND asserted in tests (Pitfall 5)"
    - "When `*_percentile` is `null`, no chip renders and the row is visually identical to its pre-Phase-94 state"
    - "Chip renders on desktop AND at 375px mobile width (PCTL-05 mobile parity) — chip allowed to flex-wrap to a second line on narrow widths per D-05"
  artifacts:
    - path: "frontend/src/components/charts/EndgameOverallScoreGapRow.tsx"
      provides: "new optional `chipSlot?: ReactNode` prop, rendered as `<span className=\"ml-auto\">{chipSlot}</span>` between value and tooltip"
      contains: "chipSlot"
    - path: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      provides: "2 PercentileChip insertions (Endgame Score Gap row + Achievable Score Gap row) gated on non-null fields"
      contains: "PercentileChip"
    - path: "frontend/src/components/charts/EndgameMetricCard.tsx"
      provides: "new `scoreGapPercentile` prop + chip-routing logic with `bucket !== 'recovery'` guard"
      contains: "scoreGapPercentile"
    - path: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      provides: "per-bucket routing of `scoreGapPercentile` (conv / parity / recovery=null)"
      contains: "section2_score_gap_conv_percentile"
  key_links:
    - from: "frontend/src/components/charts/EndgameOverallPerformanceSection.tsx"
      to: "frontend/src/components/charts/PercentileChip.tsx"
      via: "import PercentileChip + 2 conditional render sites"
      pattern: "PercentileChip"
    - from: "frontend/src/components/charts/EndgameMetricCard.tsx"
      to: "frontend/src/components/charts/PercentileChip.tsx"
      via: "import PercentileChip + 1 bucket-gated render site"
      pattern: "PercentileChip"
    - from: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      to: "frontend/src/components/charts/EndgameMetricCard.tsx"
      via: "per-bucket scoreGapPercentile prop routing"
      pattern: "scoreGapPercentile"
---

<objective>
Wire `PercentileChip` (built in Plan 94-02) into the 4 in-scope render sites: 2 page-level rows in `EndgameOverallPerformanceSection.tsx` (via the existing `ScoreGapRow` primitive, extended with an optional `chipSlot` prop), and 2 Section 2 cards in `EndgameMetricCard.tsx` (conv + parity buckets only; recovery excluded). Extend the existing component tests with chip-present / chip-absent / recovery-no-chip assertions. Add a final blocking checkpoint for mobile parity UAT at 375px and popover-copy companion-read.

Purpose: PCTL-03 wiring (chip actually renders beside the metric values), PCTL-04 wiring (per-metric flavor routing at the call sites), PCTL-05 (mobile parity), PCTL-06 (chip absent when null + recovery never chipped).

Output: A working end-to-end chip rendered on the 4 affected rows, with desktop + mobile parity verified by human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-RESEARCH.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-UI-SPEC.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-VALIDATION.md
@frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/PercentileChip.tsx
@frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx

<interfaces>
<!-- Contracts the executor needs. -->

From frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (existing slot pattern — lines 50-54):
```
startSlot?: ReactNode;   // optional left slot
endSlot?: ReactNode;     // optional right slot
```
Both default `undefined`; rendered only when provided. The new `chipSlot?: ReactNode` follows the exact same opt-in semantics.

Existing single-line render branch (~lines 97-108):
```
<span className="flex items-center gap-1 text-sm tabular-nums w-full">
  <span className="text-muted-foreground">{label}</span>
  <span className="font-semibold ..." data-testid={valueTestId}>{formatted}</span>
  {tooltip}
</span>
```
The `chipSlot` insertion goes between `{formatted}` and `{tooltip}` wrapped in `<span className="ml-auto">` so it floats to the right edge per D-05 without displacing the existing HelpCircle tooltip.

From frontend/src/components/charts/PercentileChip.tsx (Plan 94-02 output):
```
type PercentileChipFlavor = 'skill-isolating' | 'improvement-focus';
interface PercentileChipProps { percentile: number; flavor: PercentileChipFlavor; metricLabel: string; testId: string; }
```
Callers MUST gate on `percentile != null` before constructing the chip (the component requires `number`, not `number | null`).

From frontend/src/components/charts/EndgameMetricCard.tsx (existing structure — ~lines 205-242):
- `bucket: MaterialBucket` prop, where `MaterialBucket = 'conversion' | 'parity' | 'recovery'`.
- `BUCKET_DISPLAY_LABELS[bucket]` provides a display string.
- `tileTestId` is used for data-testid namespacing.
- The existing ScoreGapRow call passes value, formatted, neutralMin/Max, ciLow/High, tooltip — chipSlot is the new addition.

Per-bucket flavor routing (locked at this call site, per D-08):
- `bucket === 'conversion'` → `flavor="improvement-focus"`
- `bucket === 'parity'`     → `flavor="skill-isolating"`
- `bucket === 'recovery'`   → NO chip rendered (guarded both at the chip conditional AND by the parent passing `scoreGapPercentile={null}`)

Per-row testId convention (per 94-UI-SPEC §Data-TestID Contract):
- `endgame-score-gap-percentile-chip` + `-popover`
- `achievable-score-gap-percentile-chip` + `-popover`
- `${tileTestId}-percentile-chip` + `-popover` (resolves to `parity-score-gap-percentile-chip` / `conversion-score-gap-percentile-chip` etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `chipSlot?: ReactNode` prop to `ScoreGapRow` (opt-in slot pattern; default `undefined` keeps all 5 existing consumers pixel-identical)</name>
  <files>frontend/src/components/charts/EndgameOverallScoreGapRow.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (full file — focus on props interface ~lines 30-60 for the existing `startSlot` / `endSlot` opt-in pattern, and the single-line render branch ~lines 97-108)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md (§`EndgameOverallScoreGapRow.tsx` section — copy the chipSlot insertion pattern with `ml-auto` wrapper verbatim)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-RESEARCH.md (Pitfall 2 — `ScoreGapRow` is shared infrastructure; default `undefined` is the safety guard against leaking the chip into `EndgameTypeCard`)
  </read_first>
  <action>
    1. Add a new optional prop `chipSlot?: ReactNode` to the `ScoreGapRow` props interface. Place it immediately after the existing `endSlot?: ReactNode` declaration. JSDoc:
       `/** Optional chip rendered after the value, right-aligned via ml-auto, before the existing tooltip. Used by EndgameOverallPerformanceSection (page-level rows) and EndgameMetricCard (conv/parity buckets) to surface a PercentileChip. Defaults to undefined so EndgameTypeCard + Recovery card render pixel-identical. */`

    2. In the single-line render branch (around lines 97-108), insert `{chipSlot && <span className="ml-auto">{chipSlot}</span>}` between the `{formatted}` value span and the `{tooltip}` slot. The `ml-auto` wrapper is the critical detail per RESEARCH §Pattern 2 — pushing the chip to the right edge without displacing the HelpCircle tooltip that follows.

    3. If `ScoreGapRow` has a multi-line / stacked render branch (some rows use a different layout when CI bounds are present — check the file for a `hasSlots` or `multiLine` branch), apply the same `{chipSlot && <span className="ml-auto">{chipSlot}</span>}` insertion in the analogous position in that branch as well. The chip should appear in the same logical row position regardless of layout variant. Confirm by reading the file end-to-end before editing.

    4. Do NOT change the prop API of any other existing prop. Do NOT modify any consumer in this task — that's Tasks 2 and 3.

    5. Run `cd frontend && npx tsc --noEmit && npm test -- --run EndgameOverallScoreGapRow` after edits. The existing `EndgameOverallScoreGapRow.test.tsx` should still pass with no changes (since `chipSlot` defaults to `undefined` and existing test cases don't pass it).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run EndgameOverallScoreGapRow</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm test -- --run EndgameOverallScoreGapRow` exits 0 with all pre-existing tests passing.
    - `grep -c "chipSlot" frontend/src/components/charts/EndgameOverallScoreGapRow.tsx` returns ≥3 (prop declaration + render-branch usage; ≥4 if a second render branch exists and was also updated).
    - `grep -n "ml-auto" frontend/src/components/charts/EndgameOverallScoreGapRow.tsx` shows the `chipSlot` wrapper class.
    - `cd frontend && npm run lint` exits 0.
    - Reading `EndgameTypeCard.tsx`'s `ScoreGapRow` call site confirms the chipSlot prop is NOT being passed (defensive: keep per-type cards unchanged).
  </acceptance_criteria>
  <done>chipSlot prop added with opt-in semantics; existing consumers unchanged; tsc + tests + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `PercentileChip` into the 4 in-scope render sites + extend component tests for chip-present / chip-absent / recovery-no-chip assertions</name>
  <files>frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/charts/EndgameMetricCard.tsx, frontend/src/components/charts/EndgameMetricsSection.tsx, frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx, frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (full file — find the 2 ScoreGapRow call sites for Endgame Score Gap and Achievable Score Gap)
    - frontend/src/components/charts/EndgameMetricCard.tsx (focus on the ScoreGapRow call site ~lines 205-242, the bucket prop usage, BUCKET_DISPLAY_LABELS, SECTION2_DISPLAY_SHIFT — note that SECTION2_DISPLAY_SHIFT does NOT apply to the chip, only to displayed value / band / CI per RESEARCH §Pitfall 3)
    - frontend/src/components/charts/EndgameMetricsSection.tsx (the parent that instantiates EndgameMetricCard per bucket — find the existing `<EndgameMetricCard bucket="conversion" ... />` etc. calls)
    - frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx (full file — existing render scaffold + prop fixtures)
    - frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx (full file — existing render scaffold + per-bucket prop fixtures)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md (§`EndgameOverallPerformanceSection.tsx` and §`EndgameMetricCard.tsx` sections — copy the conditional chipSlot expressions verbatim)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md (D-08 flavors, D-12 recovery exclusion)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-UI-SPEC.md (§Data-TestID Contract — exact testId strings)
  </read_first>
  <behavior>
    Behavior the wiring + tests must enforce:
    - On `EndgameOverallPerformanceSection`: chip renders on Endgame Score Gap row when `scoreGap.score_gap_percentile != null`; chip renders on Achievable Score Gap row when `data.achievable_score_gap_percentile != null`. Both use `flavor="skill-isolating"`. Chip absent when the respective field is `null`.
    - On `EndgameMetricCard`: chip renders on the `ScoreGapRow` when `scoreGapPercentile != null && bucket !== 'recovery'`. `bucket === 'conversion'` → `flavor="improvement-focus"`; `bucket === 'parity'` → `flavor="skill-isolating"`. Recovery: NEVER renders a chip (even if `scoreGapPercentile` is somehow non-null — defensive guard per Pitfall 5).
    - On `EndgameMetricsSection`: passes `scoreGapPercentile={scoreGap.section2_score_gap_conv_percentile}` to the conversion card, `={scoreGap.section2_score_gap_parity_percentile}` to the parity card, and `={null}` to the recovery card.

    Test cases to add (extend existing `*.test.tsx` files — do NOT create new files):
    - `EndgameOverallPerformanceSection.test.tsx`:
      - `renders Endgame Score Gap percentile chip when score_gap_percentile is non-null` — pass a fixture with a numeric percentile, assert `screen.getByTestId('endgame-score-gap-percentile-chip')` present.
      - `does not render Endgame Score Gap percentile chip when score_gap_percentile is null` — pass `null`, assert `screen.queryByTestId('endgame-score-gap-percentile-chip')` is null.
      - Same pair for `achievable-score-gap-percentile-chip` + `achievable_score_gap_percentile`.
    - `EndgameMetricCard.test.tsx`:
      - `renders chip with improvement-focus flavor on bucket="conversion" when scoreGapPercentile non-null` — assert chip present + popover content matches improvement-focus copy after triggering open.
      - `renders chip with skill-isolating flavor on bucket="parity" when scoreGapPercentile non-null` — same with skill-isolating copy.
      - `does not render chip on bucket="recovery" even when scoreGapPercentile non-null` — pass a non-null value, assert no chip testId present (defensive guard for the future-CDF case per Pitfall 5).
      - `does not render chip on any bucket when scoreGapPercentile is null` — single test parameterised across the 3 buckets.
  </behavior>
  <action>
    1. **`EndgameOverallPerformanceSection.tsx`**: add `import { PercentileChip } from './PercentileChip';` to the import block. For each of the 2 in-scope `ScoreGapRow` calls (Endgame Score Gap + Achievable Score Gap), append a new `chipSlot={...}` prop with a conditional expression: `field != null ? <PercentileChip percentile={field} flavor="skill-isolating" metricLabel="..." testId="..." /> : undefined`. Use the exact testId strings from 94-UI-SPEC §Data-TestID Contract (`endgame-score-gap-percentile-chip` and `achievable-score-gap-percentile-chip`). For `metricLabel`, use the human-readable row label (`"Endgame Score Gap"`, `"Achievable Score Gap"`). Do NOT change any existing props (value, tooltip, etc.).

    2. **`EndgameMetricCard.tsx`**: add `import { PercentileChip } from './PercentileChip';` (or wherever the `from './PercentileChip'` line should go). Add a new prop `scoreGapPercentile: number | null;` to the `EndgameMetricCardProps` interface (place it adjacent to the existing scoreGap*-related props such as `scoreGapCiLow`, `scoreGapCiHigh`). Inside the existing `ScoreGapRow` call (around lines 205-242), append `chipSlot={scoreGapPercentile != null && bucket !== 'recovery' ? <PercentileChip percentile={scoreGapPercentile} flavor={bucket === 'conversion' ? 'improvement-focus' : 'skill-isolating'} metricLabel={`${BUCKET_DISPLAY_LABELS[bucket]} Score Gap`} testId={`${tileTestId}-percentile-chip`} /> : undefined}`. The `bucket !== 'recovery'` guard is the defensive layer per Pitfall 5 — even if a future CDF expansion populates `scoreGapPercentile` for a recovery card, the chip stays suppressed at the component level.

    3. **`EndgameMetricsSection.tsx`**: find the existing `<EndgameMetricCard bucket="conversion" ... />`, `bucket="parity"`, and `bucket="recovery"` calls. Add the new `scoreGapPercentile` prop to each, sourced from the parent's scoreGap response object: `scoreGapPercentile={scoreGap.section2_score_gap_conv_percentile}` (conv), `={scoreGap.section2_score_gap_parity_percentile}` (parity), `={null}` (recovery — explicit `null` documents the D-12 exclusion at the call site, in addition to the component-level guard from step 2).

    4. **Extend `EndgameOverallPerformanceSection.test.tsx`**: locate the existing render scaffold. Add 4 new `it(...)` blocks per the `<behavior>` list above. Use the existing prop fixture builder pattern in the file (most likely a `makeProps({ overrides })` helper or inline-constructed prop objects). For the "chip present" assertions, use `screen.getByTestId(...)`. For "chip absent", use `screen.queryByTestId(...)` and assert `.toBeNull()` or `.not.toBeInTheDocument()`.

    5. **Extend `EndgameMetricCard.test.tsx`**: same approach. Add 4 new `it(...)` blocks per the `<behavior>` list. For the popover-flavor assertions, fire a focus or click event on the chip trigger, wait a tick (or use `findByTestId` with the popover testId), then assert the popover body text matches the expected flavor's copy substring (e.g. "Mostly independent of rating" for skill-isolating, "tracks rating closely" for improvement-focus). For the Recovery defensive guard, render with `bucket="recovery"` AND `scoreGapPercentile={42}` AND assert `screen.queryByTestId(/-percentile-chip$/)` returns null.

    6. Run `cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run EndgameOverallPerformanceSection EndgameMetricCard PercentileChip` after edits.

    7. Run `cd frontend && npm run knip` — confirm `PercentileChip` is no longer flagged as dead (Plan 94-02's export is now imported by 2 files).

    8. Confirm no regressions in adjacent component tests: `cd frontend && npm test -- --run charts` (or the project's broader chart-test command). In particular, `EndgameTypeCard.test.tsx` must remain green — that's the key Pitfall-2 regression target proving the chipSlot didn't leak into per-type cards.

    9. Backend regression: `uv run pytest -x` must still be green (no backend changes in this plan, but a smoke check confirms nothing accidentally diverged via shared schema mismatches).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run EndgameOverallPerformanceSection EndgameMetricCard PercentileChip EndgameTypeCard && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm test -- --run` (full frontend suite) exits 0.
    - `cd frontend && npm run knip` reports zero unused exports for `PercentileChip` and its related symbols.
    - `grep -c "PercentileChip" frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` returns ≥3 (import + 2 chip insertions).
    - `grep -c "PercentileChip" frontend/src/components/charts/EndgameMetricCard.tsx` returns ≥2 (import + 1 chip insertion).
    - `grep -E "scoreGapPercentile=\\{scoreGap\\.section2_score_gap_(conv|parity)_percentile\\}" frontend/src/components/charts/EndgameMetricsSection.tsx` returns 2 matches.
    - `grep -E "scoreGapPercentile=\\{null\\}" frontend/src/components/charts/EndgameMetricsSection.tsx` returns at least 1 match (the recovery card).
    - `grep -E "bucket !== 'recovery'" frontend/src/components/charts/EndgameMetricCard.tsx` returns ≥1 (the defensive guard).
    - At least 4 new `it(...)` blocks added to each of `EndgameOverallPerformanceSection.test.tsx` and `EndgameMetricCard.test.tsx`, covering chip-present, chip-absent, flavor routing, and recovery-no-chip.
    - `EndgameTypeCard.test.tsx` still green (proves Pitfall 2 regression guard).
    - `uv run pytest -x` still exits 0 (backend regression check).
  </acceptance_criteria>
  <done>4 chips wired with correct flavors and gates; recovery defensively excluded; tests extended; full frontend + backend suites green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — mobile parity at 375px + popover-copy companion read</name>
  <files>(none — manual verification)</files>
  <read_first>
    - .planning/phases/94-backend-frontend-percentile-annotations/94-UI-SPEC.md (§Mobile Parity 375px, §Copywriting Contract)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-VALIDATION.md (§Manual-Only Verifications table)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md (D-05 mobile wrap behavior, D-08 popover flavors must read as deliberate companions)
  </read_first>
  <what-built>
    - Backend: 4 new nullable `*_percentile` fields on endgame response schemas, gated by `PVALUE_RELIABILITY_MIN_N = 10` (Plan 94-01).
    - Frontend component: `PercentileChip` with banded color pill, flame tier escalation (p90/p95/p99), Radix popover shell, two flavor-routed body strings (Plan 94-02).
    - Frontend wiring: chips render on 4 rows — Endgame Score Gap, Achievable Score Gap (both `EndgameOverallPerformanceSection.tsx`, skill-isolating), Parity ΔES (`EndgameMetricCard` parity bucket, skill-isolating), Conversion ΔES (`EndgameMetricCard` conversion bucket, improvement-focus). Recovery card + per-type cards + raw % gauges unchanged.

    Run `bin/run_local.sh` (or `npm run dev` in `frontend/` + `uv run uvicorn app.main:app --reload` in the repo root) to start dev backend + frontend. Sign in (or guest-session) and navigate to a user account with enough endgame games for the reliability gates to clear (`endgame_wdl.total ≥ 10` AND `non_endgame_wdl.total ≥ 10`; section 2 spans ≥ 10 for conv + parity). The user's own admin / impersonation account on dev typically works; otherwise sign in via Google SSO and use the existing primary account.
  </what-built>
  <how-to-verify>
    Open the Stats page → Endgames tab. Verify the 4 chips render at desktop width (≥1280px):

    1. **Visual checks (desktop):**
       - Endgame Score Gap row: `Top X%` chip appears right-aligned (via `ml-auto`) between the value and the existing HelpCircle tooltip.
       - Achievable Score Gap row: same chip layout.
       - Section 2 Parity card: chip on the ΔES bullet row inside the card.
       - Section 2 Conversion card: chip on the ΔES bullet row inside the card.
       - Section 2 Recovery card: **NO chip** on the ΔES bullet row.
       - Raw % gauges (Conv Win %, Parity Win %, Recov Save %): **NO chips** — these gauges are untouched.
       - Per-type cards (Rook / Minor / Pawn / Queen / Mixed): **NO chips** on any ScoreGapRow (Pitfall 2 regression check).
       - Timeline charts + Time Pressure section: **NO chips**.

    2. **Color band sanity check (desktop):**
       - Find a user (or override via dev tools if needed) whose Endgame Score Gap chip renders in each band: red (p<25), blue (p25..p75), green (p>75). Verify the colors match the existing zone band palette on the same cards (the chip's blue should read as the same hue family as the gauges' "typical" blue, per A2).
       - At p≥90: 1 flame icon inside the chip. At p≥95: 2 flames. At p≥99: 3 flames. Only the highest tier renders — never 6 flames.

    3. **Popover companion read (desktop):**
       - Hover the Endgame Score Gap chip → popover opens after ~100ms with skill-isolating copy: "Where you rank vs all players. Mostly independent of rating — reveals endgame ability separate from overall strength."
       - Hover the Conversion ΔES chip (Section 2) → popover opens with improvement-focus copy: "Where you rank vs all players. Conversion tracks rating closely — if you're in the lower tiers here, this is one of the biggest single improvements available to your ELO."
       - Read both popover bodies in sequence. Confirm they read as deliberate companions (skill-isolating describes one metric type, improvement-focus describes another), NOT as contradictions. If they feel contradictory, flag for copy rework before approval.

    4. **Mobile parity at 375px:**
       - Open Chrome DevTools, set viewport to 375 × 667 (iPhone SE). Reload the Stats page.
       - All 4 chips render with the same color bands and flame tiers as desktop.
       - Chip stays inline on the row when there's space; when the row gets narrow, the chip wraps to a second line with the tooltip — readable, no overflow, no truncation (per D-05).
       - Tap the chip → popover opens. Tap elsewhere → popover closes. (`onOpenChange` handles tap toggling.)
       - Flame icons remain legible at `h-3 w-3` (12px) even with 3 flames inline.

    5. **Null-percentile fallback (desktop):**
       - Find or simulate a user with low sample size (< 10 endgame games on at least one of the gated metrics — e.g. a freshly imported account). Verify NO chip renders on the affected row, but the row's existing value + tooltip remain present and unchanged. The row should look pixel-identical to its pre-Phase-94 state.

    6. **Accessibility smoke check:**
       - Tab through the page with the keyboard. Each chip is keyboard-focusable, shows a focus ring, and Enter or Space opens the popover.
       - VoiceOver / NVDA / Chrome screen reader announces the chip via its `aria-label` (e.g. "Endgame Score Gap percentile: Top 12%").

    If any verification step fails, capture a screenshot and the affected URL/scenario, then describe the failure in the resume signal so Plan 94-03 can be revised before phase close.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 verification sections pass. Type "issues: [description]" with screenshots/scenarios if any check fails — Plan 94-03 will be revised to address.</resume-signal>
  <action>Run the local dev stack (`bin/run_local.sh` or backend + frontend separately) and walk through all 6 verification sections in the `<how-to-verify>` block above on both desktop (≥1280px) and 375×667 mobile viewports. Capture screenshots for any failure. This is a human-driven manual UAT — no code changes by the executor.</action>
  <verify>
    <automated>MISSING — manual human checkpoint (visual + interaction verification at desktop + 375px mobile, popover companion read, accessibility smoke check). The previous auto task already enforces all automated gates (tsc + lint + tests + knip + ruff + ty + pytest).</automated>
    <human-check>All 6 verification sections in `<how-to-verify>` pass on desktop AND 375px mobile, including the popover companion read and the accessibility smoke check.</human-check>
  </verify>
  <done>All 6 verification sections approved; any copy / wrap / a11y issues either resolved in a follow-up commit or flagged via the resume signal for plan revision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → frontend | The 4 percentile fields arrive as `number | null`; the wiring layer trusts the backend gate (no second gate in JS — Pitfall: don't gate twice). |
| user → chip popover | The chip is a clickable / hoverable trigger; popover bodies are static text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | XSS | Chip popover body in production rendering | mitigate | Both popover-body strings are static React children (Plan 94-02). The wiring layer adds no user-controlled content — `metricLabel` is a hard-coded row label ("Endgame Score Gap" / "Achievable Score Gap" / `BUCKET_DISPLAY_LABELS[bucket]`), all build-time constants. No `dangerouslySetInnerHTML` anywhere. |
| T-94-08 | Information Disclosure | Recovery chip leak (defensive future-proofing) | mitigate | Even if a future backend phase emits a non-null `section2_score_gap_recov_percentile`, the wiring code includes a `bucket !== 'recovery'` guard at the chip conditional in `EndgameMetricCard.tsx` AND the parent `EndgameMetricsSection.tsx` passes `scoreGapPercentile={null}` for the recovery card. Two-layer defense; tested by the "Recovery never chips" assertion in `EndgameMetricCard.test.tsx`. |
| T-94-09 | Tampering | Wiring layer accidentally surfaces a chip on per-type cards | mitigate | `ScoreGapRow.chipSlot` defaults to `undefined`. `EndgameTypeCard.tsx` does not pass `chipSlot`. The Pitfall-2 regression assertion in `EndgameTypeCard.test.tsx` proves the per-type cards stay chip-free. |
| T-94-SC | Tampering | npm/pip/cargo installs | n/a | Phase 94 installs no new packages. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run` exits 0 (full frontend suite).
- `cd frontend && npm run knip` reports zero unused exports.
- `uv run pytest -x` still exits 0 (backend regression).
- Pre-PR checklist: `uv run ruff format app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -x && (cd frontend && npm run lint && npm test -- --run && npm run knip)` all green.
- Human checkpoint approved (Task 3) with desktop + 375px mobile verified.
</verification>

<success_criteria>
- Stats page (Endgames tab) renders percentile chips on exactly 4 rows for a user with sufficient sample, with the correct flavor routing (skill-isolating × 3, improvement-focus × 1).
- Recovery card, per-type cards, raw % gauges, timelines, and Time Pressure section render no chips.
- Mobile at 375px matches desktop in chip presence, color bands, and flame counts (with optional flex-wrap to a second line).
- Popover copy reads as deliberate companions across the skill-isolating and improvement-focus flavors.
- Chip-absent path verified visually: when a metric's `*_percentile` is `null`, the row renders pixel-identical to its pre-Phase-94 state.
- Defensive `bucket !== 'recovery'` guard prevents future regressions (asserted in tests + verified during UAT).
</success_criteria>

<output>
Create `.planning/phases/94-backend-frontend-percentile-annotations/94-03-SUMMARY.md` when done. Capture any popover copy adjustments made during the human checkpoint, any chip-wrap behavior at 375px (does it actually wrap, or does it stay inline?), and any reliability-floor adjustments suggested by UAT (e.g. "n=10 was fine; no need for stricter Section 2 floor"). Confirm the Phase 94 end state: 5 requirements (PCTL-02..06) all satisfied; recovery & per-type cards & raw gauges unchanged.
</output>
