---
phase: 94-backend-frontend-percentile-annotations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/schemas/test_endgames_schema.py
  - tests/test_endgame_service.py
autonomous: true
requirements: [PCTL-02, PCTL-06]
tags: [backend, endgame, percentile, pydantic, fastapi]

must_haves:
  truths:
    - "API response carries `score_gap_percentile`, `achievable_score_gap_percentile`, `section2_score_gap_conv_percentile`, `section2_score_gap_parity_percentile` as nullable floats in [0, 100]"
    - "Each of the 4 percentile fields is `None` on the wire when the relevant N is below `PVALUE_RELIABILITY_MIN_N`"
    - "Endgame Score Gap percentile uses a dual-N gate over `endgame_wdl.total` AND `non_endgame_wdl.total`"
    - "Section 2 conv/parity percentile guards `mean is not None` before calling `interpolate_percentile` (D-13)"
    - "Backend imports `interpolate_percentile` from `app/services/global_percentile_cdf.py`; no re-implementation (D-13)"
    - "No `section2_score_gap_recov_percentile` field is added (D-12: Recovery is out of scope)"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "4 new nullable `*_percentile` fields with docstrings"
      contains: "score_gap_percentile"
    - path: "app/services/endgame_service.py"
      provides: "gated interpolate_percentile() calls at 2 compute sites"
      contains: "interpolate_percentile"
    - path: "tests/schemas/test_endgames_schema.py"
      provides: "field-presence assertions for the 4 new percentile fields"
      contains: "score_gap_percentile"
    - path: "tests/test_endgame_service.py"
      provides: "gate-semantics tests (single-N at n=9/n=10; dual-N for score_gap)"
      contains: "percentile"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/services/global_percentile_cdf.py"
      via: "interpolate_percentile import"
      pattern: "from app.services.global_percentile_cdf import interpolate_percentile"
    - from: "app/services/endgame_service.py"
      to: "app/services/endgame_service.py:205"
      via: "PVALUE_RELIABILITY_MIN_N reuse"
      pattern: "PVALUE_RELIABILITY_MIN_N"
---

<objective>
Extend the endgame API surface with 4 nullable `*_percentile` fields (in [0, 100]) on the existing endgame response schemas, computed at the existing 2 service compute sites via the Phase 93 `interpolate_percentile` helper and gated by `PVALUE_RELIABILITY_MIN_N` (same gate already used for `_p_value` / `_ci_*` siblings).

Purpose: PCTL-02 (backend emits nullable percentile per the 4 in-scope metrics) + PCTL-06 (misleading percentile is worse than none — explicit minimum-N reliability gate).

Output: Additive, non-breaking schema fields + service wiring + tests, ready for the frontend to consume in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-RESEARCH.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-VALIDATION.md
@app/schemas/endgames.py
@app/services/endgame_service.py
@app/services/global_percentile_cdf.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase + research. -->

From app/services/global_percentile_cdf.py (Phase 93 output — DO NOT touch):
- `interpolate_percentile(metric_id: MetricId, value: float) -> float | None`
- Returns None for: (a) metric_id not in GLOBAL_PERCENTILE_CDF (covers "recovery", "endgame_score", any non-chipped MetricId), (b) `math.isnan(value)` input.
- Edge-clamps to [0, 100]. The N-floor reliability gate is layered ON TOP — the helper itself does not gate by N.

From app/services/endgame_service.py:205 (existing):
- `PVALUE_RELIABILITY_MIN_N: int = 10`
- Already gates `_p_value` and `_ci_*` for the same 4 metrics; reused verbatim for percentile per D-10.

From app/schemas/endgames.py (existing sibling fields):
- `ScoreGapMaterialResponse`: `score_difference: float`, `score_difference_p_value: float | None`, `score_difference_ci_low/_high: float | None` (lines ~421-429); `section2_score_gap_{conv,parity,recov}_mean/_n/_p_value/_ci_low/_ci_high` groups (lines ~437-455).
- `EndgamePerformanceResponse`: `achievable_score_gap: float`, `achievable_score_gap_p_value/_ci_low/_ci_high: float | None` (lines ~241-254).

Metric ID → field-name mapping (per D-11 — name divergence on Endgame Score Gap is intentional):
- MetricId `"score_gap"`           → field `score_gap_percentile` on `ScoreGapMaterialResponse` (sibling group is `score_difference_*` — name diverges by design)
- MetricId `"achievable_score_gap"` → field `achievable_score_gap_percentile` on `EndgamePerformanceResponse`
- MetricId `"section2_score_gap_conv"`   → field `section2_score_gap_conv_percentile` on `ScoreGapMaterialResponse`
- MetricId `"section2_score_gap_parity"` → field `section2_score_gap_parity_percentile` on `ScoreGapMaterialResponse`
- Recovery: NO field added (D-12).

Compute sites (per 94-PATTERNS.md):
- Site A (single-N gate on `ex_n`): `_get_endgame_performance_from_rows` in `app/services/endgame_service.py` (~lines 2270-2322). Existing siblings already gated by `ex_n >= PVALUE_RELIABILITY_MIN_N`.
- Site B (dual-N + 2× single-N gates): `_compute_score_gap_material` in `app/services/endgame_service.py` (~lines 1310-1419). `endgame_wdl.total` + `non_endgame_wdl.total` already in scope; `conv_mean/conv_n` / `parity_mean/parity_n` unpacked at ~lines 1352-1354.

Existing schema test scaffold pattern (`tests/schemas/test_endgames_schema.py` lines 24-38):
- `set(Response.model_fields.keys())` introspection + `(required - keys) == set()` assertion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 4 nullable `*_percentile` fields to endgame Pydantic schemas with field-presence tests (Wave 0 — interface contract)</name>
  <files>app/schemas/endgames.py, tests/schemas/test_endgames_schema.py</files>
  <read_first>
    - app/schemas/endgames.py (full file, but focus on `ScoreGapMaterialResponse` ~lines 410-460 and `EndgamePerformanceResponse` ~lines 230-260 — these are the sibling groups the new fields land next to)
    - tests/schemas/test_endgames_schema.py (existing `TestScoreGapMaterialResponseSkillDropped` class at lines 24-38 — the analog assertion pattern)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md (§`app/schemas/endgames.py` and §`tests/schemas/test_endgames_schema.py` sections — copy the field-shape and assertion pattern verbatim)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md (§D-11 field naming, §D-12 Recovery exclusion)
  </read_first>
  <behavior>
    - `ScoreGapMaterialResponse.model_fields` MUST contain: `score_gap_percentile`, `section2_score_gap_conv_percentile`, `section2_score_gap_parity_percentile`.
    - `ScoreGapMaterialResponse.model_fields` MUST NOT contain `section2_score_gap_recov_percentile` (D-12).
    - `EndgamePerformanceResponse.model_fields` MUST contain `achievable_score_gap_percentile`.
    - All 4 new fields default to `None` and accept `float | None`.
    - Existing fields (`score_difference`, `score_difference_p_value`, `achievable_score_gap_p_value`, etc.) remain present and untouched.
    - A new `TestPercentileFieldsPresent` class (or equivalent in the file's existing class style) asserts all four presence requirements and the recovery non-presence guard.
  </behavior>
  <action>
    Add 4 nullable Pydantic v2 fields. Field shape: `{name}: float | None = None` with a triple-quoted docstring explaining the gate semantics and the [0, 100] value range. Place each new field adjacent to its existing `_p_value` / `_ci_*` sibling group:

    1. On `ScoreGapMaterialResponse` — add `score_gap_percentile` next to the `score_difference_p_value` / `_ci_low` / `_ci_high` group. The docstring MUST explicitly note the deliberate name divergence: the field name mirrors the MetricId literal `"score_gap"` (the CDF key per D-11), not the wire-format sibling `score_difference`. Gate clause in docstring: "None when min(endgame_wdl.total, non_endgame_wdl.total) < PVALUE_RELIABILITY_MIN_N (=10) — same dual-N gate as score_difference_p_value / _ci_*."

    2. On `ScoreGapMaterialResponse` — add `section2_score_gap_conv_percentile` next to its `section2_score_gap_conv_p_value` / `_ci_low` / `_ci_high` sibling group. Gate clause in docstring: "None when section2_score_gap_conv_n < PVALUE_RELIABILITY_MIN_N (=10) or conv_mean is None — same single-N gate as section2_score_gap_conv_p_value / _ci_*."

    3. On `ScoreGapMaterialResponse` — add `section2_score_gap_parity_percentile` next to its `section2_score_gap_parity_p_value` / `_ci_low` / `_ci_high` sibling group. Same gate-clause language with parity substitution.

    4. On `EndgamePerformanceResponse` — add `achievable_score_gap_percentile` next to `achievable_score_gap_p_value` / `_ci_low` / `_ci_high`. Gate clause in docstring: "None when ex_n < PVALUE_RELIABILITY_MIN_N (=10) — same single-N gate as achievable_score_gap_p_value / _ci_*."

    Do NOT add `section2_score_gap_recov_percentile` — Recovery is explicitly out of scope per D-12.

    Then extend `tests/schemas/test_endgames_schema.py` with a new class `TestPercentileFieldsPresent` (mirroring the `TestScoreGapMaterialResponseSkillDropped` shape at lines 24-38). Two test methods:
    - `test_score_gap_response_has_percentile_fields` — assert the 3 required fields are present on `ScoreGapMaterialResponse.model_fields` AND that `section2_score_gap_recov_percentile` is NOT present (defensive against future "let's add recovery" mistakes per Pitfall 5).
    - `test_performance_response_has_percentile_field` — assert `achievable_score_gap_percentile` is on `EndgamePerformanceResponse.model_fields`.

    Run `uv run ruff format app/ tests/` and `uv run ruff check app/ tests/ --fix` after edits. Run `uv run ty check app/ tests/` and fix any drift (e.g. if Pydantic v2 model field ordering matters in the file's existing patterns).
  </action>
  <verify>
    <automated>uv run pytest tests/schemas/test_endgames_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/schemas/test_endgames_schema.py::TestPercentileFieldsPresent -x` exits 0 with 2 test methods passing.
    - `uv run ty check app/schemas/endgames.py tests/schemas/test_endgames_schema.py` reports zero errors.
    - `python -c "from app.schemas.endgames import ScoreGapMaterialResponse, EndgamePerformanceResponse; assert 'score_gap_percentile' in ScoreGapMaterialResponse.model_fields; assert 'section2_score_gap_conv_percentile' in ScoreGapMaterialResponse.model_fields; assert 'section2_score_gap_parity_percentile' in ScoreGapMaterialResponse.model_fields; assert 'section2_score_gap_recov_percentile' not in ScoreGapMaterialResponse.model_fields; assert 'achievable_score_gap_percentile' in EndgamePerformanceResponse.model_fields; print('OK')"` prints `OK`.
    - All existing tests in `tests/schemas/test_endgames_schema.py` still pass (`uv run pytest tests/schemas/test_endgames_schema.py -x` exits 0 with 0 failures).
  </acceptance_criteria>
  <done>Fields present, defaults `None`, recovery excluded, ty clean, schema tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `interpolate_percentile` into the 2 compute sites with single-N + dual-N reliability gates and gate-semantics tests</name>
  <files>app/services/endgame_service.py, tests/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py (lines 200-220 for `PVALUE_RELIABILITY_MIN_N` constant; lines 1310-1420 for `_compute_score_gap_material`; lines 2270-2325 for `_get_endgame_performance_from_rows`)
    - app/services/global_percentile_cdf.py (the `interpolate_percentile` function signature and docstring — confirm NaN handling + unknown-metric → None semantics)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md (§site A and §site B sections — copy the gate + interpolate snippets verbatim with variable substitution)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-RESEARCH.md (Pitfall 6 — gate-then-compute pattern; Pitfall 1 — score_gap vs score_difference name divergence)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md (§D-10 reliability gate, §D-13 interpolate_percentile import)
    - tests/test_endgame_service.py (open and grep for `PVALUE_RELIABILITY_MIN_N` / existing gate tests — copy the existing test scaffolding pattern for the new percentile gate tests)
  </read_first>
  <behavior>
    - At site A (`_get_endgame_performance_from_rows`), the response's `achievable_score_gap_percentile` is `None` iff `ex_n < PVALUE_RELIABILITY_MIN_N`; otherwise it's the value returned by `interpolate_percentile("achievable_score_gap", achievable_score_gap)` (which may itself be `None` if the value is NaN — that's a separate failure mode the gate alone does not prevent).
    - At site B (`_compute_score_gap_material`):
      - `score_gap_percentile` is `None` iff `min(endgame_wdl.total, non_endgame_wdl.total) < PVALUE_RELIABILITY_MIN_N` (dual-N gate per D-10).
      - `section2_score_gap_conv_percentile` is `None` iff `conv_mean is None` OR `conv_n is None` OR `conv_n < PVALUE_RELIABILITY_MIN_N`.
      - `section2_score_gap_parity_percentile` is `None` iff `parity_mean is None` OR `parity_n is None` OR `parity_n < PVALUE_RELIABILITY_MIN_N`.
      - `section2_score_gap_recov_percentile` is NOT computed and NOT attached to the response (D-12).
    - All 4 fields are passed by keyword into the `ScoreGapMaterialResponse(...)` / `EndgamePerformanceResponse(...)` constructor calls at the existing return sites.
    - No new constants — `PVALUE_RELIABILITY_MIN_N` is reused; no per-metric override (planner reviewed D-10's "may argue for stricter floor" discretion and elected the default per the Phase 93 CDF inclusion floors of ≥30 endgame/non_endgame and ≥20 spans being already stricter than the chip gate, so the gate at N=10 only fires when the CDF interpolation is itself well-supported).
  </behavior>
  <action>
    1. At the top of `endgame_service.py`, add `from app.services.global_percentile_cdf import interpolate_percentile` to the existing app-internal import block (alphabetically among the `from app.services...` imports).

    2. In `_get_endgame_performance_from_rows` (~line 2270-2322): after the existing `achievable_score_gap` computation and before the `return EndgamePerformanceResponse(...)` call, add a gated assignment:
       - Define `achievable_score_gap_percentile: float | None` via a conditional expression: call `interpolate_percentile("achievable_score_gap", achievable_score_gap)` if `ex_n >= PVALUE_RELIABILITY_MIN_N` else `None`.
       - Add the field to the `EndgamePerformanceResponse(...)` keyword arguments at the existing return statement.
       - Mirror the variable naming and gate-clause style of the existing `entry_expected_score_p_value` line at ~2285-2289.

    3. In `_compute_score_gap_material` (~line 1310-1419): after the existing `score_difference` / per-bucket destructuring and before the `return ScoreGapMaterialResponse(...)` call, add three gated assignments:
       - `score_gap_percentile`: dual-N gate, calling `interpolate_percentile("score_gap", score_difference)` only when `min(endgame_wdl.total, non_endgame_wdl.total) >= PVALUE_RELIABILITY_MIN_N`.
       - `section2_score_gap_conv_percentile`: triple guard (`conv_mean is not None and conv_n is not None and conv_n >= PVALUE_RELIABILITY_MIN_N`) → `interpolate_percentile("section2_score_gap_conv", conv_mean)`, else `None`.
       - `section2_score_gap_parity_percentile`: same pattern with parity substitution.
       - Add the three fields to the `ScoreGapMaterialResponse(...)` keyword arguments at the existing return statement. Do NOT add a recovery percentile (D-12).

    4. Add a small inline comment at each gate-clause site referencing the D-10 / D-12 decision and the `_p_value` sibling that gates the same N — e.g. `# Same dual-N gate as score_difference_p_value (Phase 94 D-10).`

    5. Add gate-semantics tests to `tests/test_endgame_service.py`. Follow the existing `async def test_*` + AsyncSession fixture pattern already established in the file. Cover four cases (5 sub-assertions):
       - `test_percentile_gate_achievable_below_floor` — `ex_n = PVALUE_RELIABILITY_MIN_N - 1` → assert response `achievable_score_gap_percentile is None`.
       - `test_percentile_gate_achievable_at_floor` — `ex_n = PVALUE_RELIABILITY_MIN_N` → assert response `achievable_score_gap_percentile is not None` AND is in [0, 100].
       - `test_score_gap_percentile_dual_gate` — assert that when `endgame_wdl.total = 9, non_endgame_wdl.total = 10` → `None`; when `endgame_wdl.total = 10, non_endgame_wdl.total = 9` → `None`; when both `= 10` → not `None`.
       - `test_section2_percentile_gates_conv_and_parity` — single-N gate at n=9 → None, n=10 → not None, for both conv and parity. Add a sub-assertion that `mean=None` (empty cohort) → percentile is `None` even when `n >= floor`.

       Where existing fixture scaffolding does not directly support synthesising the precise N values, follow the project's existing pattern of building fixtures from `EndgameWDLSummary` + per-bucket means directly via the helper functions, rather than going through full DB ingest. Reuse the scaffolding pattern already present in `tests/test_endgame_service.py` for `_p_value` gate tests if one exists; otherwise mock the per-bucket helpers with `unittest.mock.patch` to control N + mean inputs to the gated assignments.

    6. Run `uv run ruff format app/ tests/` and `uv run ruff check app/ tests/ --fix` after edits. Run `uv run ty check app/ tests/` and fix any drift.

    Do NOT touch `section2_score_gap_recov_*` anything. Do NOT modify any router. Do NOT modify `app/services/global_percentile_cdf.py` (Phase 93 output, frozen).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py tests/schemas/test_endgames_schema.py tests/services/test_global_percentile_cdf.py -x -v -k "percentile or score_gap"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_endgame_service.py -k "percentile" -x` exits 0 with at least 4 new test methods passing (the four bullets above).
    - `uv run ty check app/services/endgame_service.py tests/test_endgame_service.py` reports zero errors.
    - `grep -n "from app.services.global_percentile_cdf import interpolate_percentile" app/services/endgame_service.py` returns exactly 1 line.
    - `grep -c "section2_score_gap_recov_percentile" app/services/endgame_service.py app/schemas/endgames.py` returns 0 (recovery exclusion guard per D-12).
    - `grep -E "interpolate_percentile\\(\"(score_gap|achievable_score_gap|section2_score_gap_conv|section2_score_gap_parity)\"" app/services/endgame_service.py` returns exactly 4 hits.
    - `uv run pytest tests/services/test_global_percentile_cdf.py -x` still exits 0 (Phase 93 regression suite must remain green).
    - `uv run pytest tests/ -x` (full backend suite) exits 0.
  </acceptance_criteria>
  <done>4 gated interpolate_percentile() calls live at the 2 compute sites; 4 new keyword args on the existing response constructors; gate tests green; recovery field never appears anywhere; ty + pytest + ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted query params reach `/api/endgames/*`. New fields are server-derived, NOT user-controlled. |
| service → wire | New nullable fields are emitted to authenticated clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Information Disclosure | `ScoreGapMaterialResponse` / `EndgamePerformanceResponse` percentile field | accept | Percentile is a scalar in [0, 100] computed from a global CDF cohort. It does NOT identify the user, does NOT leak cohort membership, does NOT carry PII. Already authenticated endpoint; no new authorization surface. (Per 94-RESEARCH.md §Security Domain.) |
| T-94-02 | Tampering | `interpolate_percentile` input value | mitigate | Helper is NaN-safe (returns None on `math.isnan(value)`). Input values (`score_difference`, `achievable_score_gap`, `conv_mean`, `parity_mean`) come from the existing service computations, NOT user input. Pydantic v2 validates the resulting `float | None` field on the wire. |
| T-94-03 | Information Disclosure | Reliability gate bypass | mitigate | Gate is applied in the service layer at the same site as the existing `_p_value` / `_ci_*` gate. Below `PVALUE_RELIABILITY_MIN_N`, the field is `None` on the wire, so unauthorized cohort-membership inference from a too-small sample is structurally impossible. Gate tests (Task 2) enforce this. |
| T-94-SC | Tampering | npm/pip/cargo installs | n/a | Phase 94 installs no new packages (RESEARCH §Package Legitimacy Audit). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- Backend full suite green: `uv run pytest -x`
- ty clean: `uv run ty check app/ tests/`
- ruff clean: `uv run ruff format app/ tests/ && uv run ruff check app/ tests/`
- All 4 new schema fields present; recovery percentile field absent.
- Reliability gate behavior verified at n=9 (None) and n=10 (not None) for all 4 metrics, including the dual-N variant for Endgame Score Gap.
- Phase 93 regression suite (`tests/services/test_global_percentile_cdf.py`) remains green.
</verification>

<success_criteria>
- `score_gap_percentile`, `achievable_score_gap_percentile`, `section2_score_gap_conv_percentile`, `section2_score_gap_parity_percentile` appear on the wire for authenticated requests against an endgame endpoint with sufficient sample.
- All four are `None` when their respective N is below `PVALUE_RELIABILITY_MIN_N` (=10).
- No `section2_score_gap_recov_percentile` field exists anywhere in the codebase.
- Backend imports `interpolate_percentile` from `app/services/global_percentile_cdf.py`; no re-implementation (D-13).
</success_criteria>

<output>
Create `.planning/phases/94-backend-frontend-percentile-annotations/94-01-SUMMARY.md` when done. Note any reliability-floor adjustment (default reuse of `PVALUE_RELIABILITY_MIN_N = 10`) and confirm the dual-N gate decision for `score_gap`.
</output>
