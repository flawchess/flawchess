---
phase: 94-backend-frontend-percentile-annotations
plan: 02
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/PercentileChip.tsx
  - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
autonomous: true
requirements: [PCTL-03, PCTL-04, PCTL-05]
tags: [frontend, react, percentile, chip, popover, lucide, theme]

must_haves:
  truths:
    - "TypeScript types for the 4 new backend fields exist on `EndgamePerformanceResponse` and `ScoreGapMaterialResponse` (manual mirror — project does NOT run OpenAPI codegen per RESEARCH §Pitfall 4)"
    - "`PercentileChip` is a standalone reusable component at `frontend/src/components/charts/PercentileChip.tsx`"
    - "Chip label uses the formula `Top ${Math.max(1, Math.round(100 - percentile))}%` — integer percent, no decimals, floored at 1 (per D-06, D-07, Pitfall 7)"
    - "Chip background fill is driven from `ZONE_DANGER` / `GAUGE_NEUTRAL` / `ZONE_SUCCESS` in `theme.ts` — no hard-coded colors (D-02, D-04, PCTL-05)"
    - "Flame tier dispatch: 1 flame at p≥90, 2 at p≥95, 3 at p≥99, 0 otherwise — highest tier only (D-03)"
    - "Popover trigger is the chip span itself (`role=\"button\"`, `tabIndex={0}`), NOT an adjacent HelpCircle (D-01)"
    - "Popover hover-open uses `HOVER_OPEN_DELAY_MS = 100`, mirroring `MetricStatPopover` shell mechanics"
    - "Two popover-body flavors exist — skill-isolating + improvement-focus — routed by the `flavor` prop (D-08)"
    - "Chip pill text is `text-sm` (not `text-xs`); popover body uses `text-xs` per the documented CLAUDE.md popover exception"
    - "Chip carries `aria-label` and `data-testid`; popover Content carries `${testId}-popover` data-testid"
  artifacts:
    - path: "frontend/src/types/endgames.ts"
      provides: "4 new nullable field declarations mirroring the Pydantic schemas from Plan 94-01"
      contains: "achievable_score_gap_percentile"
    - path: "frontend/src/components/charts/PercentileChip.tsx"
      provides: "PercentileChip component + PercentileChipFlavor type + (inline) popover body"
      exports: ["PercentileChip", "PercentileChipFlavor"]
      min_lines: 100
    - path: "frontend/src/components/charts/__tests__/PercentileChip.test.tsx"
      provides: "unit tests for label formatter, band-color routing, flame-tier dispatch, popover-flavor routing, edge cases (p0, p99.9)"
      contains: "PercentileChip"
  key_links:
    - from: "frontend/src/components/charts/PercentileChip.tsx"
      to: "frontend/src/lib/theme.ts"
      via: "imports ZONE_DANGER, GAUGE_NEUTRAL, ZONE_SUCCESS"
      pattern: "from '@/lib/theme'"
    - from: "frontend/src/components/charts/PercentileChip.tsx"
      to: "radix-ui Popover"
      via: "PopoverPrimitive.Root + Trigger + Portal + Content"
      pattern: "from 'radix-ui'"
    - from: "frontend/src/components/charts/PercentileChip.tsx"
      to: "lucide-react"
      via: "Flame icon import"
      pattern: "from 'lucide-react'"
---

<objective>
Mirror the 4 new backend percentile fields into the manual TS type file, then build the reusable `PercentileChip` component (banded color pill + flame tier + Radix popover shell + flavor-routed body), with unit tests covering label formatting, color band, flame dispatch, flavor routing, and edge-case rounding.

Purpose: PCTL-03 (chip renders "top X%" with honest rounding) + PCTL-04 (metric-aware popover flavor routing) + PCTL-05 (theme-driven colors, desktop+mobile parity).

Output: A single `PercentileChip` import-ready for Wave 3 to wire into the 4 row sites, plus TS types so the frontend can compile against the new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-RESEARCH.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-UI-SPEC.md
@.planning/phases/94-backend-frontend-percentile-annotations/94-VALIDATION.md
@frontend/src/types/endgames.ts
@frontend/src/components/popovers/MetricStatPopover.tsx
@frontend/src/lib/theme.ts
@frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx

<interfaces>
<!-- Contracts the executor needs. -->

From frontend/src/lib/theme.ts (existing constants — reuse, do NOT redefine):
- `ZONE_DANGER` = `oklch(0.50 0.15 25)` (red — for p<25)
- `GAUGE_NEUTRAL` = `oklch(0.55 0.18 260)` (blue — for p25..p75 — preferred per 94-UI-SPEC §Color Assumption A2)
- `ZONE_SUCCESS` = `oklch(0.50 0.14 145)` (green — for p>75)

From frontend/src/components/popovers/MetricStatPopover.tsx (shell mechanics — mirror):
- `HOVER_OPEN_DELAY_MS = 100` (line 28)
- `const [open, setOpen] = React.useState(false)` + `hoverTimeout = useRef<ReturnType<typeof setTimeout> | null>(null)`
- `handleMouseEnter` / `handleMouseLeave` pattern (lines 45-55)
- Radix `PopoverPrimitive.Root open={open} onOpenChange={setOpen}` + `Trigger asChild` + `Portal` + `Content side="top" sideOffset={4}` (lines 57-93)
- Content className shell: `'z-50 max-w-xs rounded-md border-0 outline-none bg-foreground px-3 py-1.5 text-xs text-background'` + animation classes (lines 80-92)

From frontend/src/types/endgames.ts (existing siblings — extend):
- On `EndgamePerformanceResponse` (after line ~115): add `achievable_score_gap_percentile: number | null;`
- On `ScoreGapMaterialResponse`: add `score_gap_percentile: number | null;`, `section2_score_gap_conv_percentile: number | null;`, `section2_score_gap_parity_percentile: number | null;` next to the existing `score_difference_*` and `section2_score_gap_*` sibling groups. NO `section2_score_gap_recov_percentile`.

Component public API (locked):
```
type PercentileChipFlavor = 'skill-isolating' | 'improvement-focus';

interface PercentileChipProps {
  percentile: number;          // [0, 100], required; callers gate on `!= null` before rendering
  flavor: PercentileChipFlavor;
  metricLabel: string;         // used in aria-label + optional popover heading
  testId: string;              // becomes data-testid on the trigger; popover Content gets `${testId}-popover`
}
```

Named constants (lives inside `PercentileChip.tsx` per CLAUDE.md "No magic numbers"):
- `PERCENTILE_BAND_LOW = 25`
- `PERCENTILE_BAND_HIGH = 75`
- `FLAME_TIER_1 = 90`
- `FLAME_TIER_2 = 95`
- `FLAME_TIER_3 = 99`
- `HOVER_OPEN_DELAY_MS = 100`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror 4 new backend percentile fields into `frontend/src/types/endgames.ts` (interface contract for Wave 3 wiring)</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts (full file — find `EndgamePerformanceResponse` interface and the `achievable_score_gap_*` group at ~lines 112-115; find `ScoreGapMaterialResponse` interface and its `score_difference_*` / `section2_score_gap_*` groups)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md (§`frontend/src/types/endgames.ts` section — exact field declarations to copy)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-RESEARCH.md (Pitfall 4 — TS types are manually maintained, NOT codegen'd)
  </read_first>
  <action>
    Mirror the 4 new Pydantic fields from Plan 94-01 into the manual TS type file. Add each declaration adjacent to its existing sibling group, matching the placement convention already used by the project:

    1. On the `EndgamePerformanceResponse` interface (after the existing `achievable_score_gap_ci_high: number | null;` line at ~line 115): add `achievable_score_gap_percentile: number | null;`.

    2. On the `ScoreGapMaterialResponse` interface: add `score_gap_percentile: number | null;` adjacent to the `score_difference_p_value` / `_ci_low` / `_ci_high` group. Then add `section2_score_gap_conv_percentile: number | null;` and `section2_score_gap_parity_percentile: number | null;` adjacent to their respective Section 2 sibling groups.

    Do NOT add `section2_score_gap_recov_percentile` — Recovery is out of scope (D-12; mirror the backend exclusion).

    Add a brief 1-line `/** ... */` JSDoc on each new field referencing the gate clause from the backend, e.g. `/** Cohort percentile [0,100] vs Phase 93 CDF. null when min(endgame_total, non_endgame_total) < 10. */`.

    No new exports, no new types. Pure interface extensions.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0 with zero TS errors.
    - `grep -c "achievable_score_gap_percentile" frontend/src/types/endgames.ts` returns 1.
    - `grep -c "score_gap_percentile" frontend/src/types/endgames.ts` returns ≥2 (one on `ScoreGapMaterialResponse`, plus the `achievable_score_gap_percentile` match).
    - `grep -c "section2_score_gap_recov_percentile" frontend/src/types/endgames.ts` returns 0 (recovery exclusion).
    - `grep -c "section2_score_gap_conv_percentile\\|section2_score_gap_parity_percentile" frontend/src/types/endgames.ts` returns 2.
    - `cd frontend && npm run lint` exits 0 (no new lint errors introduced).
  </acceptance_criteria>
  <done>4 new nullable fields present in TS interface mirror, recovery excluded, tsc clean, lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build `PercentileChip` component (banded color pill + flame stack + Radix popover shell + flavor-routed body) with unit tests</name>
  <files>frontend/src/components/charts/PercentileChip.tsx, frontend/src/components/charts/__tests__/PercentileChip.test.tsx</files>
  <read_first>
    - frontend/src/components/popovers/MetricStatPopover.tsx (full file — copy the popover shell mechanics: state hooks, hover timeout, Radix Root/Trigger/Portal/Content, animation classes)
    - frontend/src/lib/theme.ts (verify the exact constant names `ZONE_DANGER`, `GAUGE_NEUTRAL`, `ZONE_SUCCESS` exist and re-export from `@/lib/theme`)
    - frontend/src/lib/utils.ts (confirm the `cn()` helper signature for class composition)
    - frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx (test scaffolding pattern — `@vitest-environment jsdom`, matchMedia + ResizeObserver stubs, `afterEach(cleanup + vi.clearAllMocks)`)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-UI-SPEC.md (full file — typography, color band, spacing, named constants, data-testid contract, copywriting contract)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-PATTERNS.md (§`PercentileChip.tsx` section — copy the trigger and Content shell pattern verbatim)
    - .planning/phases/94-backend-frontend-percentile-annotations/94-CONTEXT.md (D-01 chip is the trigger, D-02 banded colors, D-03 flame tiers, D-04 pill fill, D-08 popover flavors)
  </read_first>
  <behavior>
    Test cases for `PercentileChip.test.tsx` (each as a `it(...)` block):
    - `renders "Top 27%" for percentile=73` — assert chip text content
    - `renders "Top 50%" for percentile=50` (literal render near median per D-07) — assert text content
    - `renders "Top 100%" for percentile=0` (honest literal at lower edge per D-06) — assert text content
    - `floors at "Top 1%" for percentile=99.9` (no "Top 0%" per Pitfall 7) — assert text content
    - `routes red band background for percentile=10` — assert `getByTestId(testId).style.backgroundColor` matches the `ZONE_DANGER` oklch string
    - `routes blue neutral band for percentile=50` — assert background matches `GAUGE_NEUTRAL`
    - `routes green band for percentile=85` — assert background matches `ZONE_SUCCESS`
    - `renders 0 flame icons for percentile=89` — assert no `<svg>` flame children
    - `renders 1 flame icon for percentile=90` — assert exactly 1 Flame
    - `renders 2 flame icons for percentile=95` — assert exactly 2 Flames
    - `renders 3 flame icons for percentile=99` — assert exactly 3 Flames (highest tier only — NOT 6 from 1+2+3)
    - `opens popover with skill-isolating copy when flavor="skill-isolating"` — render with user.click + assert popover body text matches the skill-isolating string
    - `opens popover with improvement-focus copy when flavor="improvement-focus"` — same with the improvement-focus string
    - `chip trigger has aria-label including the metricLabel and the rendered percentile label`
    - `chip trigger has the supplied data-testid; popover Content has data-testid `${testId}-popover``
  </behavior>
  <action>
    Create `frontend/src/components/charts/PercentileChip.tsx` as a self-contained component file. Structure:

    1. Imports: `import * as React from 'react';`, `import { Popover as PopoverPrimitive } from 'radix-ui';`, `import { Flame } from 'lucide-react';`, `import { cn } from '@/lib/utils';`, `import { ZONE_DANGER, GAUGE_NEUTRAL, ZONE_SUCCESS } from '@/lib/theme';`.

    2. Named constants at module top (per CLAUDE.md "No magic numbers"): `HOVER_OPEN_DELAY_MS = 100`, `PERCENTILE_BAND_LOW = 25`, `PERCENTILE_BAND_HIGH = 75`, `FLAME_TIER_1 = 90`, `FLAME_TIER_2 = 95`, `FLAME_TIER_3 = 99`. Add a `CHIP_TEXT_COLOR = 'oklch(0.98 0 0)'` constant for the near-white pill text (per 94-UI-SPEC §Chip Color Bands). Mark `CHIP_TEXT_COLOR` as the lone hard-coded color exception and add a one-line comment justifying it (near-white text is universal across all 3 band colors and reads as the chip's text-on-fill convention rather than a semantic theme token).

    3. Exported type: `export type PercentileChipFlavor = 'skill-isolating' | 'improvement-focus';`.

    4. Props interface: `PercentileChipProps` per the locked API in the `<interfaces>` block above. All 4 props required (callers gate on `!= null` before rendering).

    5. Three pure helper functions, each typed:
       - `deriveBandColor(pct: number): string` — returns `ZONE_DANGER` when `pct < PERCENTILE_BAND_LOW`, `ZONE_SUCCESS` when `pct > PERCENTILE_BAND_HIGH`, else `GAUGE_NEUTRAL`.
       - `deriveFlameCount(pct: number): 0 | 1 | 2 | 3` — cascade `if (pct >= FLAME_TIER_3) return 3` → `... TIER_2 → 2` → `... TIER_1 → 1` → `return 0`.
       - `formatTopXPercent(pct: number): string` — returns `` `Top ${Math.max(1, Math.round(100 - pct))}%` `` (per Pitfall 7 — `Math.max(1, ...)` prevents "Top 0%" at p99.9).

    6. The `PercentileChip` function component:
       - `useState` for `open`, `useRef` for `hoverTimeout` (typed `ReturnType<typeof setTimeout> | null`).
       - `handleMouseEnter` / `handleMouseLeave` callbacks mirroring `MetricStatPopover` exactly.
       - Compute `label = formatTopXPercent(percentile)`, `bandColor = deriveBandColor(percentile)`, `flameCount = deriveFlameCount(percentile)` once per render.
       - JSX: `PopoverPrimitive.Root open onOpenChange` wrapping `PopoverPrimitive.Trigger asChild` → `<span role="button" tabIndex={0} aria-label={`${metricLabel} percentile: ${label}`} data-testid={testId} onMouseEnter onMouseLeave className={cn('inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-sm font-normal cursor-pointer', 'focus:outline-none focus-visible:ring-2 focus-visible:ring-ring')} style={{ backgroundColor: bandColor, color: CHIP_TEXT_COLOR }}>` → conditional `{flameCount > 0 && <span className="inline-flex" aria-hidden="true">` containing `Array.from({length: flameCount}).map((_, i) => <Flame key={i} className="h-3 w-3" />)` → `<span>{label}</span>`.
       - `PopoverPrimitive.Portal` → `PopoverPrimitive.Content` with `side="top" sideOffset={4}`, `onMouseEnter` (clears the timeout to keep the popover open when hovering over the content), `onMouseLeave={handleMouseLeave}`, the exact className shell from `MetricStatPopover` lines 80-92 (`'z-50 max-w-xs rounded-md border-0 outline-none bg-foreground px-3 py-1.5 text-xs text-background'` + animation classes), and `data-testid={`${testId}-popover`}`. The Content children render `<PercentileChipPopoverBody flavor={flavor} />`.

    7. Internal `PercentileChipPopoverBody({ flavor }: { flavor: PercentileChipFlavor })` — defined in the same file (no separate file per RESEARCH §Assumption A4). Two branches:
       - `flavor === 'skill-isolating'` → `<p>Where you rank vs all players. Mostly independent of rating — reveals endgame ability separate from overall strength.</p>` (per 94-UI-SPEC §Copywriting Contract).
       - else (`'improvement-focus'`) → `<p>Where you rank vs all players. Conversion tracks rating closely — if you're in the lower tiers here, this is one of the biggest single improvements available to your ELO.</p>` (per 94-UI-SPEC §Copywriting Contract).
       Both wrappers omit any "n=", "sigmoid", "Wilson", or methodology jargon per `feedback_popover_copy_minimalism.md`.

    8. Create `frontend/src/components/charts/__tests__/PercentileChip.test.tsx`. Use the scaffolding from `EndgameOverallScoreGapRow.test.tsx` lines 1-53 verbatim (`@vitest-environment jsdom` directive, matchMedia stub, ResizeObserver stub, `afterEach(cleanup + vi.clearAllMocks)`). Then add one `describe('PercentileChip', () => { ... })` block with one `it(...)` per behavior listed in the `<behavior>` section above. For popover-opening tests, prefer firing a focus or click event on the chip trigger via `@testing-library/react` `fireEvent` or `userEvent`, then querying the popover by `screen.queryByTestId(`${testId}-popover`)` once Radix portals it. For band-color assertions, read `getByTestId(testId).style.backgroundColor` and assert the substring `oklch` is present plus the specific token color string match (or, if jsdom strips oklch, fall back to asserting the inline style includes the exact constant value imported from `theme.ts`).

    9. Run `cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run PercentileChip` after edits. Fix any drift.

    10. After the component compiles and tests pass, run `cd frontend && npm run knip` to confirm no dead exports. The `PercentileChip` export will appear unused until Plan 94-03 wires it in — add a one-line `// knip-ignore-next-line` comment or equivalent suppression on the export line, OR (preferred) accept that knip will flag this until Plan 94-03 merges and the wiring imports it. If knip is run in CI between Wave 2 and Wave 3, suppress the export specifically; otherwise leave it and rely on Plan 94-03's import to satisfy knip in the same PR.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run PercentileChip</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm test -- --run PercentileChip` exits 0 with all `it(...)` blocks from the `<behavior>` list passing (target: 12+ test cases).
    - `cd frontend && npm run lint` exits 0.
    - `grep -E "ZONE_DANGER|GAUGE_NEUTRAL|ZONE_SUCCESS" frontend/src/components/charts/PercentileChip.tsx` returns ≥3 hits (theme constants imported and used).
    - `grep -E "text-xs|text-sm" frontend/src/components/charts/PercentileChip.tsx` shows the chip pill text class is `text-sm` (popover body inherits `text-xs` from the Content className shell — that's the documented exception).
    - `grep -c "section2_score_gap_recov" frontend/src/components/charts/PercentileChip.tsx` returns 0 (chip is metric-agnostic — the recovery exclusion is at the call-site routing layer in Plan 94-03, not in this component).
    - The flame test for `percentile=99` asserts exactly 3 Flame icons render — NOT 6 (proves the "highest tier only" dispatch per D-03).
    - The `formatTopXPercent` floor test for `percentile=99.9` asserts text `Top 1%` (NOT `Top 0%`) per Pitfall 7.
    - No hard-coded hex / rgb / oklch color values appear in the component file EXCEPT the single `CHIP_TEXT_COLOR` constant for near-white text (documented exception).
  </acceptance_criteria>
  <done>PercentileChip + tests + types compile; 12+ test cases green; theme-driven colors; flame tiers and label formatter behave per spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → frontend | The 4 new percentile fields arrive over the wire as `number | null`. Frontend treats them as untrusted only insofar as TS validates the shape; no further sanitization needed. |
| user → popover content | The popover bodies render static strings (no user-controlled content). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | XSS | PercentileChip popover body | mitigate | Both popover-body branches use React JSX `<p>{static-string}</p>` — no `dangerouslySetInnerHTML`, no user-controlled content. React text rendering auto-escapes. Verified by inspection in Task 2. |
| T-94-05 | Information Disclosure | aria-label content | accept | `aria-label` contains the metricLabel + the formatted "Top X%" — same info already visible in the chip. No PII, no cohort identity leak. |
| T-94-06 | Tampering | Imported theme constants | mitigate | `ZONE_DANGER` / `GAUGE_NEUTRAL` / `ZONE_SUCCESS` are imported from `frontend/src/lib/theme.ts` at build time. No runtime mutation. Hard-coded `CHIP_TEXT_COLOR` is local to the module and reviewed inline. |
| T-94-SC | Tampering | npm/pip/cargo installs | n/a | Phase 94 installs no new packages. `lucide-react`, `radix-ui`, `clsx`, `tailwind-merge` all already in the project lockfile (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (TS types + new component compile).
- `cd frontend && npm test -- --run PercentileChip` exits 0 with all assertions green.
- `cd frontend && npm run lint` exits 0.
- TS-type-mirror task verified by `grep`-style assertions on the 4 field names and the recovery-exclusion guard.
- All chip colors come from `theme.ts` imports (only `CHIP_TEXT_COLOR` is module-local and documented).
</verification>

<success_criteria>
- The 4 new percentile fields are accessible from React components as typed `number | null` properties on `EndgamePerformanceResponse` and `ScoreGapMaterialResponse`.
- `PercentileChip` renders a banded color pill with optional flame stack and the "Top X%" label, opens a flavor-routed popover on hover/tap, and obeys the locked design (D-01..D-08, PCTL-03..PCTL-05).
- The component is ready to be imported by `EndgameOverallPerformanceSection.tsx` and `EndgameMetricCard.tsx` in Wave 3 without further refactoring.
</success_criteria>

<output>
Create `.planning/phases/94-backend-frontend-percentile-annotations/94-02-SUMMARY.md` when done. Confirm chosen theme constants (`GAUGE_NEUTRAL` vs `ZONE_NEUTRAL` for the middle band) and note any knip suppression for the unused export.
</output>
