---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 05b
type: execute
wave: 2
depends_on: [94.4-01, 94.4-02, 94.4-03, 94.4-04, 94.4-05a]
files_modified:
  - app/services/user_benchmark_percentiles_service.py
  - tests/services/test_user_benchmark_percentiles_service.py
autonomous: true
requirements: [PRPCR-03, PRPCR-04]

must_haves:
  truths:
    - "`app/services/user_benchmark_percentiles_service.py` rewrites: Stage A extension computes anchors FIRST then per-TC `score_gap` percentiles per RESEARCH Pitfall 9 ordering. `STAGE_B_METRICS` 12-tuple (post-94.3) retires; replaced by `STAGE_B_METRIC_FAMILIES` 7-tuple per PATTERNS lines 419-425 (achievable_score_gap, section2_score_gap_conv, section2_score_gap_parity, recovery_score_gap, time_pressure_score_gap, clock_gap, net_flag_rate); iteration is `for family in families: for tc in user_tcs: _compute_metric_for_user_per_tc(session, user_id, family, tc); interpolate_cohort_percentile(family, value, anchor.anchor_rating, tc); upsert_percentile(...)`."
    - "Lichess-precedence rule (CONTEXT D-12) lives in the new `compute_anchors_for_user(session, user_id)` helper: select source_platform='lichess' if user has any Lichess games in TC, else source_platform='chesscom' and call `convert_chesscom_to_lichess` (Plan 01 module) on the per-TC chess.com rating. The raw chess.com median rating (BEFORE conversion) is captured into the `chesscom_raw_rating: int | None` slot on `RatingAnchorRow` and persisted via `upsert_anchor(..., chesscom_raw_rating=...)` per D-07 bullet 4 — the tooltip in Plan 07 renders it inline. For Lichess-anchored rows, `chesscom_raw_rating=None`."
    - "Sentry context-not-message pattern preserved per CLAUDE.md + PATTERNS line 822 — `sentry_sdk.set_context(\"percentile_compute\", {\"user_id\": ..., \"stage\": \"A/B\", \"metric\": ..., \"tc\": ...})` then `sentry_sdk.capture_exception(exc)` inside the existing except blocks; no variables in error messages."
    - "Legacy stubs from Plan 04 fully retired in this plan: `grep -rn 'interpolate_percentile\\b' app/ tests/ scripts/` returns ONLY `interpolate_cohort_percentile` matches; `grep -rn 'GLOBAL_PERCENTILE_CDF\\b' app/ tests/ scripts/` returns ONLY `COHORT_PERCENTILE_CDF` matches."
    - "pytest coverage in `tests/services/test_user_benchmark_percentiles_service.py` rewrites assertions for per-(metric, tc) lookup pattern + Stage A anchor compute extension. Lichess-precedence rule covered: users with both platforms see source_platform='lichess'; chesscom-only path captures `chesscom_raw_rating` (raw rating before ChessGoals conversion); chess.com Daily suppresses (convert_chesscom_to_lichess returns None for source_tc='daily'). Sentry capture asserted via monkey-patch on at least one stage-failure case."
  artifacts:
    - path: app/services/user_benchmark_percentiles_service.py
      provides: Stage A anchor compute + Stage B per-(family × tc) iteration + Lichess-precedence rule + chesscom_raw_rating capture
      contains: "compute_anchors_for_user\\|STAGE_B_METRIC_FAMILIES\\|interpolate_cohort_percentile\\|chesscom_raw_rating"
      min_lines: 300
    - path: tests/services/test_user_benchmark_percentiles_service.py
      provides: rewritten test assertions for per-(metric, tc) lookup + Lichess-precedence + chesscom_raw_rating
      min_lines: 200
  key_links:
    - from: app/services/user_benchmark_percentiles_service.py compute_anchors_for_user
      to: app/services/chesscom_to_lichess.py convert_chesscom_to_lichess (Plan 01)
      via: chess.com-anchored TCs convert raw rating to Lichess-equivalent (D-12)
      pattern: "convert_chesscom_to_lichess"
    - from: app/services/user_benchmark_percentiles_service.py compute_anchors_for_user
      to: app/repositories/user_rating_anchors_repository.py upsert_anchor (Plan 02 — widened with chesscom_raw_rating)
      via: persisted anchor including raw chess.com rating for D-07 bullet 4 disclosure
      pattern: "upsert_anchor\\|chesscom_raw_rating"
    - from: app/services/user_benchmark_percentiles_service.py
      to: app/repositories/user_benchmark_percentiles_repository.py (Plan 05a — widened upsert with time_control_bucket)
      via: per-(user, metric, tc) percentile persistence
      pattern: "upsert_percentile.*time_control_bucket"
---

<objective>
Second of the 3-plan Wave 2 commit cluster (05a → 05b → 05c). Rewrite the per-user percentile service to consume Plan 04's `COHORT_PERCENTILE_CDF` via Plan 02's anchors and Plan 03's per-TC builders. Adds `compute_anchors_for_user` with the Lichess-precedence rule (D-12) + `chesscom_raw_rating` capture per D-07 bullet 4. Retires the legacy `interpolate_percentile` + `GLOBAL_PERCENTILE_CDF` stubs Plan 04 left in place.

Purpose: B5 split — the service rewrite isolated into its own context-bounded plan to avoid context exhaustion. Consumes Plan 05a's widened repository (nested dict + 3-column upsert) and Plan 02's widened anchor repository (chesscom_raw_rating column). Produces the Stage A/B compute path that Plan 06 reruns at backfill.

Output: 1 service file rewrite + 1 test file rewrite. After this plan, Plan 05c can wire the API shaper aggregation + schema + frontend types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@app/services/user_benchmark_percentiles_service.py
@app/repositories/user_benchmark_percentiles_repository.py
@app/repositories/user_rating_anchors_repository.py
@app/services/chesscom_to_lichess.py
@app/services/canonical_slice_sql.py
@app/services/global_percentile_cdf.py
@CLAUDE.md

<interfaces>
<!-- Verified anchors:
     - PATTERNS lines 372-426: full service rewrite blueprint
     - app/services/user_benchmark_percentiles_service.py:75-76 — STAGE_A_METRIC + STAGE_B_METRICS
     - app/services/user_benchmark_percentiles_service.py:151-194 — compute_stage_a / compute_stage_b
     - app/repositories/user_rating_anchors_repository.py (Plan 02 — widened with chesscom_raw_rating per B3) -->

Service rewrite (PATTERNS lines 400-414, RESEARCH Pitfall 9):
  async def compute_stage_a(user_id, *, session_maker=None) -> None:
      async with maker() as session:
          # 1. Compute and upsert per-(user, TC) anchors FIRST (Pitfall 9 ordering).
          anchors = await compute_anchors_for_user(session, user_id)
          # 2. For each TC with an anchor, compute per-TC score_gap percentile.
          for tc, anchor in anchors.items():
              result = await _compute_metric_for_user_per_tc(session, user_id, "score_gap", tc)
              if result is None: continue
              value, n_games = result
              percentile = interpolate_cohort_percentile("score_gap", value, anchor.anchor_rating, tc)
              await upsert_percentile(session, user_id=user_id, metric="score_gap",
                                      time_control_bucket=tc, value=value, n_games=n_games,
                                      percentile=percentile, cdf_snapshot=date.today())
          await session.commit()

Stage B iteration (PATTERNS lines 419-425):
  STAGE_B_METRIC_FAMILIES: tuple[CdfMetricId, ...] = (
      "achievable_score_gap", "section2_score_gap_conv", "section2_score_gap_parity",
      "recovery_score_gap", "time_pressure_score_gap", "clock_gap", "net_flag_rate",
  )

Lichess-precedence + chesscom_raw_rating capture (CONTEXT D-07 bullet 4 + D-12):
  async def compute_anchors_for_user(session, user_id) -> dict[TimeControlBucket, RatingAnchorRow]:
      # For each TC:
      # 1. Does this user have ANY Lichess games in TC? (single existence query)
      # 2a. If yes: source_platform='lichess', call per_user_cte_median_anchor(tc, source='single_user', platform='lichess').
      #     Capture anchor_rating, set chesscom_raw_rating=None (Lichess-direct, no conversion).
      # 2b. If no: source_platform='chesscom'.
      #     i. Call per_user_cte_median_anchor(tc, source='single_user', platform='chesscom') — returns raw chess.com median.
      #     ii. Store as `chesscom_raw_rating` (PRE-conversion value; D-07 bullet 4 tooltip disclosure source).
      #     iii. Convert via convert_chesscom_to_lichess(raw_rating, source_tc=tc, target_tc=tc) → Lichess-equivalent.
      #     iv. If convert returns None (Daily, or out-of-range): skip the anchor entirely (chip suppresses).
      # 3. upsert_anchor(session, user_id=user_id, time_control_bucket=tc, anchor_rating=lichess_equivalent OR lichess_direct,
      #                  source_platform=..., chesscom_raw_rating=raw_or_None, n_games=...)
      # 4. Returns dict for the percentile compute loop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app/services/user_benchmark_percentiles_service.py — compute_anchors_for_user + Stage A anchor compute + Stage B per-(family × tc) iteration; capture chesscom_raw_rating; retire interpolate_percentile stub from Plan 04</name>
  <files>app/services/user_benchmark_percentiles_service.py</files>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py (in full — current post-94.3 form, especially lines 75-194)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 372-426 — full rewrite blueprint)
    - app/services/chesscom_to_lichess.py (Plan 01 — `convert_chesscom_to_lichess` signature)
    - app/repositories/user_rating_anchors_repository.py (Plan 02 — `upsert_anchor` + `RatingAnchorRow` — note the new `chesscom_raw_rating: int | None` field per B3)
    - app/services/canonical_slice_sql.py (Plan 02 — `per_user_cte_median_anchor`; Plan 03 — `per_user_cte_*_tc` builders)
    - app/services/global_percentile_cdf.py (Plan 04 — `interpolate_cohort_percentile`)
    - CLAUDE.md §"Error Handling & Sentry"
  </read_first>
  <action>
    Major rewrite of `app/services/user_benchmark_percentiles_service.py`. Preserve from the existing service: import boilerplate, `_default_session_maker`, async/await discipline, `STAGE_A_METRIC: CdfMetricId = "score_gap"` constant. REPLACE the 12-tuple `STAGE_B_METRICS` with the 7-tuple `STAGE_B_METRIC_FAMILIES` per PATTERNS lines 419-424.

    **Add `compute_anchors_for_user(session, user_id) -> dict[TimeControlBucket, RatingAnchorRow]`** implementing the Lichess-precedence rule per CONTEXT D-12 + the D-07 bullet 4 raw-rating capture:
    ```python
    async def compute_anchors_for_user(session, user_id) -> dict[TimeControlBucket, RatingAnchorRow]:
        """Compute per-(user, TC) anchors, Lichess-precedence rule (D-12) + chesscom_raw_rating capture (D-07 bullet 4).

        For each TC:
        1. Query: does user have ANY Lichess games in this TC? (single existence query)
        2. If yes: source='lichess', call per_user_cte_median_anchor(tc, source='single_user', platform='lichess').
                   Set chesscom_raw_rating=None (Lichess-direct path; no conversion involved).
        3. If no: source='chesscom'.
           a. Call per_user_cte_median_anchor(tc, source='single_user', platform='chesscom') → returns raw chess.com median.
           b. Store the raw value as chesscom_raw_rating (PRE-conversion, surfaces in tooltip per D-07 bullet 4).
           c. Call convert_chesscom_to_lichess(raw, source_tc=tc, target_tc=tc) → Lichess-equivalent.
           d. If convert returns None (Daily, or out-of-anchor-range): skip this TC entirely (chip suppresses).
        4. upsert_anchor via Plan 02's repository (widened with chesscom_raw_rating per B3).
        5. Returns dict for Stage A's score_gap percentile loop.
        """
        from app.services.canonical_slice_sql import per_user_cte_median_anchor, MEDIAN_ANCHOR_MIN_GAMES
        from app.services.chesscom_to_lichess import convert_chesscom_to_lichess
        from app.repositories.user_rating_anchors_repository import upsert_anchor, RatingAnchorRow
        ...
    ```
    Implementation: for each TC in `("bullet", "blitz", "rapid", "classical")`: (a) execute a Lichess-existence query (`SELECT 1 FROM games WHERE user_id=:user_id AND platform='lichess' AND time_control_bucket=:tc LIMIT 1`); (b) dispatch to per-platform anchor compute via `per_user_cte_median_anchor(tc, source='single_user', snapshot_date=date.today(), platform='lichess' or 'chesscom')`; (c) execute the SQL → returns first row with `(anchor_rating, n_games)`; (d) **chess.com branch only:** capture `chesscom_raw_rating = raw_anchor_rating` BEFORE converting, then `lichess_equivalent = convert_chesscom_to_lichess(raw_anchor_rating, source_tc=tc, target_tc=tc)`; if `lichess_equivalent is None` skip this TC (Daily/out-of-range — chip suppresses); else use `lichess_equivalent` as the persisted `anchor_rating`; (e) **lichess branch:** `chesscom_raw_rating = None`, `anchor_rating = raw_anchor_rating` (no conversion); (f) call `upsert_anchor(session, user_id=user_id, time_control_bucket=tc, anchor_rating=anchor_rating, source_platform=source_platform, chesscom_raw_rating=chesscom_raw_rating, n_games=n_games)` — the keyword arg name matches Plan 02's B3-widened signature; (g) build the return dict using `RatingAnchorRow(anchor_rating, source_platform, chesscom_raw_rating, n_games)`.

    **Rewrite `compute_stage_a`** per PATTERNS lines 400-414: compute anchors FIRST then per-TC `score_gap` percentiles. For each TC with an anchor: (1) `_compute_metric_for_user_per_tc(session, user_id, "score_gap", tc)` returns `(value, n_games) | None`; (2) `percentile = interpolate_cohort_percentile("score_gap", value, anchor.anchor_rating, tc)`; (3) `upsert_percentile(session, user_id=user_id, metric="score_gap", time_control_bucket=tc, value=value, n_games=n_games, percentile=percentile, cdf_snapshot=date.today())`. Sentry context-not-message pattern: `sentry_sdk.set_context("percentile_compute", {"user_id": user_id, "stage": "A", "metric": "score_gap", "tc": tc})` + `sentry_sdk.capture_exception(exc)` inside `except Exception as exc:`.

    **Rewrite `compute_stage_b`** per PATTERNS lines 419-425: iterate `for family in STAGE_B_METRIC_FAMILIES: for tc in anchors.keys(): _compute_metric_for_user_per_tc(session, user_id, family, tc); interpolate_cohort_percentile(family, value, anchor.anchor_rating, tc); upsert_percentile(...)`. The anchors are fetched once at the top via `fetch_anchors_for_user(session, user_id=user_id)` (Plan 02's helper); if no anchors exist (Stage A hasn't run yet, or all TCs suppressed), return early. Same Sentry pattern.

    **Add `_compute_metric_for_user_per_tc(session, user_id, family, tc) -> tuple[float, int] | None`**: dispatches `family` to the right per-TC builder via the same dispatch table Plan 04's gen script uses. Executes the SQL against `session` (single-user CTE source), reads the first row, returns `(metric_value, n_games)` or None.

    **Retire legacy stubs**: delete the `interpolate_percentile` stub Plan 04 left in `app/services/global_percentile_cdf.py` AS PART OF THIS TASK if no remaining caller exists after the rewrite. Run `grep -rn 'interpolate_percentile\b' app/ tests/ scripts/` and replace each non-stub usage with `interpolate_cohort_percentile`. Same for `GLOBAL_PERCENTILE_CDF`. Both legacy names must return ZERO non-comment matches after this task.

    Sentry rules per CLAUDE.md: no new capture sites beyond what the existing Stage A/B already had — same Sentry shape, same context keys, plus the new `tc` dimension added to `set_context`.
  </action>
  <verify>
    <automated>uv run ty check app/services/user_benchmark_percentiles_service.py && uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def compute_anchors_for_user" app/services/user_benchmark_percentiles_service.py` is exactly 1.
    - `grep -c "STAGE_B_METRIC_FAMILIES" app/services/user_benchmark_percentiles_service.py` is ≥ 2 (declaration + use).
    - `grep -c "STAGE_B_METRICS\b" app/services/user_benchmark_percentiles_service.py | grep -v '^#'` is 0 (legacy retired).
    - `grep -c "interpolate_cohort_percentile" app/services/user_benchmark_percentiles_service.py` is ≥ 2.
    - `grep -c "convert_chesscom_to_lichess" app/services/user_benchmark_percentiles_service.py` is ≥ 1 (Plan 01 imported and called).
    - `grep -c "upsert_anchor" app/services/user_benchmark_percentiles_service.py` is ≥ 1 (Plan 02 imported and called).
    - `grep -c "chesscom_raw_rating" app/services/user_benchmark_percentiles_service.py` is ≥ 2 (captured pre-conversion + passed to upsert_anchor per D-07 bullet 4).
    - `grep -c "per_user_cte_median_anchor" app/services/user_benchmark_percentiles_service.py` is ≥ 1 (Plan 02 imported).
    - Legacy-stub retirement (filter out the substring matches that should remain): `grep -rEn "interpolate_percentile\b|GLOBAL_PERCENTILE_CDF\b" app/ tests/ scripts/ | grep -v "interpolate_cohort_percentile|COHORT_PERCENTILE_CDF" | grep -vE "^[^:]+:\s*#" | grep -v "\.md:"` returns ZERO lines.
    - `uv run ty check app/services/user_benchmark_percentiles_service.py` returns zero errors.
    - Sentry pattern preserved: `grep -c "sentry_sdk.set_context\|sentry_sdk.capture_exception" app/services/user_benchmark_percentiles_service.py` is ≥ 4 (Stage A + Stage B except blocks, anchor compute except blocks).
  </acceptance_criteria>
  <done>Service rewrite complete; Lichess-precedence + chesscom_raw_rating capture + Stage A/B iteration + legacy retirement all landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite tests/services/test_user_benchmark_percentiles_service.py — per-(metric, tc) lookup + Lichess-precedence + chesscom_raw_rating cases</name>
  <files>tests/services/test_user_benchmark_percentiles_service.py</files>
  <read_first>
    - tests/services/test_user_benchmark_percentiles_service.py (in full — current post-94.3 assertions; understand the fixture shape, mock patterns, existing test discipline)
    - app/services/user_benchmark_percentiles_service.py (just rewritten in Task 1)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 372-426 — the service rewrite the tests verify)
  </read_first>
  <behavior>
    - Test 1: Stage A run on a user with only Lichess games in rapid creates an anchor with `source_platform='lichess'`, `chesscom_raw_rating=None`.
    - Test 2: Stage A run on a user with only chess.com games in blitz creates an anchor with `source_platform='chesscom'`, `chesscom_raw_rating=<the raw chess.com median>`, `anchor_rating=<Lichess-equivalent from convert_chesscom_to_lichess>` (the persisted anchor_rating differs from chesscom_raw_rating per D-07 bullet 4 disclosure).
    - Test 3: Stage A run on a user with BOTH Lichess and chess.com games in rapid creates an anchor with `source_platform='lichess'` and `chesscom_raw_rating=None` (Lichess-precedence rule D-12).
    - Test 4: Stage A run on a user with only chess.com Daily games (no other TC) creates NO classical anchor (the SQL drops Daily AND convert_chesscom_to_lichess returns None for source_tc='daily'; chip suppresses).
    - Test 5: Stage A computes score_gap percentile only for TCs where an anchor exists.
    - Test 6: Stage B run on a user with anchors in {bullet, blitz} computes per-(family, tc) percentiles for each (family ∈ STAGE_B_METRIC_FAMILIES) × {bullet, blitz}.
    - Test 7: `upsert_percentile` called with the new 3-column PK tuple (user_id, metric, time_control_bucket).
    - Test 8: When `interpolate_cohort_percentile` returns None (suppressed cell), the row still upserts with `percentile=None` and `value` + `n_games` populated.
    - Test 9: Sentry capture_exception is called on metric-compute failure (Sentry-context-not-message pattern per CLAUDE.md).
    - Test 10: chesscom_raw_rating round-trip — fetched anchor's `chesscom_raw_rating` matches the chess.com median rating BEFORE the ChessGoals conversion (Test 2 setup verified by reading from the anchor table via `fetch_anchors_for_user` and asserting the field is populated; lichess setup verified by asserting the field is None).
  </behavior>
  <action>
    Rewrite `tests/services/test_user_benchmark_percentiles_service.py` against the new service shape. The 94.3 test assertions on `STAGE_B_METRICS` (12-tuple) retire; replace with `STAGE_B_METRIC_FAMILIES` (7-tuple). Use the existing async DB fixtures + the user-creation helper. Add the 10 behavior cases above. For Tests 1-4, set up game fixtures (chess.com games in some TCs, Lichess games in others) — the existing test file likely has a `_create_game(user_id, platform, time_control_bucket, ...)` helper; if not, mirror the conftest pattern. For Test 9, monkey-patch `sentry_sdk.capture_exception` and assert it's called when `_compute_metric_for_user_per_tc` raises. For Test 10, assert via `fetch_anchors_for_user` post-Stage-A that the `chesscom_raw_rating` field is populated for chess.com-anchored users and is None for Lichess-anchored.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 10 behavior cases pass.
    - `grep -c "def test_" tests/services/test_user_benchmark_percentiles_service.py` is ≥ 10.
    - At least one test exercises chess.com-Daily-suppression (Test 4).
    - At least one test exercises Lichess-precedence on mixed-platform user (Test 3).
    - At least one test monkey-patches sentry and asserts capture_exception is called (Test 9).
    - At least one test asserts `chesscom_raw_rating` round-trip for chess.com-anchored user (Test 10).
    - `STAGE_B_METRIC_FAMILIES` referenced in at least one test.
    - `uv run ty check tests/services/test_user_benchmark_percentiles_service.py` returns zero errors.
  </acceptance_criteria>
  <done>Service tests rewritten; 10 cases cover Lichess-precedence, Daily-suppression, per-(family, tc) Stage B iteration, suppression-bypass UPSERT, Sentry capture, chesscom_raw_rating round-trip.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-PR checklist (intermediate) — ruff, ty, scoped pytest</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist"
  </read_first>
  <action>
    `uv run ruff format app/services/user_benchmark_percentiles_service.py tests/services/test_user_benchmark_percentiles_service.py` + `uv run ruff check --fix` on same paths + `uv run ty check app/ tests/` (must be zero errors) + `uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x`. Full `uv run pytest -x` is DEFERRED to Plan 05c's terminal task — at this point the endgame_service.py page-level chip slots still consume the legacy flat-dict shape; that's resolved by 05c.
  </action>
  <verify>
    <automated>uv run ruff format --check app/services/user_benchmark_percentiles_service.py tests/services/test_user_benchmark_percentiles_service.py && uv run ruff check app/services/user_benchmark_percentiles_service.py tests/services/test_user_benchmark_percentiles_service.py && uv run ty check app/ tests/ && uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - ruff format + ruff check clean for the two touched files.
    - ty check zero errors across `app/` + `tests/`.
    - Scoped pytest green: `tests/services/test_user_benchmark_percentiles_service.py` all cases pass.
    - Full pytest is DEFERRED to Plan 05c.
  </acceptance_criteria>
  <done>Service rewrite green; Plan 05c (API shaper aggregation + schema + types) is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → service → repository | Authenticated `current_user.id` from FastAPI-Users dep is the sole `user_id` source; never accepted as a query/path parameter (V4 mitigation, preserved). |
| Python → Postgres | Static SQL + bindparams + `Literal`-typed parameters preserve injection safety from Plan 03. |
| Service → external snapshot (ChessGoals) | `convert_chesscom_to_lichess` is a pure-Python lookup against the locked snapshot (Plan 01); no live HTTP call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-05b-01 | Tampering | ENUM value injection via API | mitigate | All metric IDs and TC values produced by backend code; `fetch_for_user` filters by `user_id` only. |
| T-94.4-05b-02 | Information disclosure | `fetch_anchors_for_user(user_id=...)` requires authenticated `user_id` per V4 | mitigate | `user_id` keyword-only; sourced from FastAPI-Users `current_user.id` dep. Same as Phase 94.1 / 94.2 / 94.3. |
| T-94.4-05b-03 | Tampering | Sentry message embeds variable data fragmenting issue grouping | mitigate | CLAUDE.md Sentry rule preserved: `sentry_sdk.set_context("percentile_compute", {...})` followed by `sentry_sdk.capture_exception(exc)`. No variables in error messages. |
| T-94.4-05b-04 | Information disclosure | chesscom_raw_rating leaks per-user chess.com rating to tooltip | accept | Per D-07 bullet 4: the user's own raw chess.com rating IS the intended disclosure surface (tooltip explains "your chess.com blitz = 1830 → 1920 Lichess-equivalent"). Filtered by `current_user.id` at API; never cross-user. |
| T-94.4-05b-SC | Tampering | npm/pip installs | accept | No new packages. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x` is green.
- `uv run ty check app/ tests/` reports zero errors.
- `grep -rEn "interpolate_percentile\b|GLOBAL_PERCENTILE_CDF\b" app/ tests/ scripts/ | grep -v "interpolate_cohort_percentile|COHORT_PERCENTILE_CDF" | grep -vE "^[^:]+:\s*#" | grep -v "\.md:"` returns ZERO lines.
- `compute_anchors_for_user` captures `chesscom_raw_rating` from the pre-conversion chess.com median; persists it via `upsert_anchor`.
- Sentry context+capture preserved across Stage A/B/anchor-compute except blocks.
</verification>

<success_criteria>
PRPCR-03 fully implemented (Stage A anchor compute + Lichess-precedence rule + chesscom_raw_rating capture live). PRPCR-04 partially implemented (interpolate_cohort_percentile wired in per-user service; per-TC ΔES builders consumed). Legacy `interpolate_percentile` + `GLOBAL_PERCENTILE_CDF` fully retired. Backend service layer ready for Plan 05c's API shaper aggregation + frontend type regen.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-05b-SUMMARY.md` when done. Record: final grep snapshot confirming no legacy `interpolate_percentile` / `GLOBAL_PERCENTILE_CDF` references remain; test count + runtime; sample chesscom_raw_rating round-trip output from one test case (anchor row before/after conversion to verify the field is populated).
</output>
