---
phase: 94.4-peer-relative-percentile-chip-refinement
verified: 2026-05-27T14:15:00Z
status: human_needed
score: 9/9 must-haves verified (automated); 1 human-needed item deferred to prod deploy
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "9/9 must-haves verified (SC-9 scaffold-only — operator UAT pending)"
  gaps_closed:
    - "D-12 Reversal Amendment fully verified: blended schema, SQL builder blend=True, API/TS types, composition tooltip, dev backfill, doc loop"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "PRPCR-09 / Plan 12 UAT: Prod backfill run + mixed-platform chip credibility check"
    expected: "After bin/deploy.sh ships c70f5d94b243 migration + prod backfill runs: (1) mixed-platform account tooltip bullet 4 renders blended composition prose with 5 numbers; (2) pure-lichess + pure-chess.com accounts show unchanged / near-unchanged anchors; (3) chess.com-Daily-only classical user has no classical chip; (4) mobile parity at 375px; (5) NO flame icon at any percentile."
    why_human: "Requires prod deployment + prod backfill to run. Dev backfill is green (19 users processed, 13 mixed-user anchor rows confirmed). Prod chips remain blank until c70f5d94b243 migration + backfill complete. Tracked in .planning/todos/pending/2026-05-27-phase-94.4-prod-backfill-and-d12-reversal-uat.md and 94.4-12-HUMAN-UAT.md."
  - test: "Plan 06 UAT: Elite-user cohort-relative framing (PRPCR-09 SC-9)"
    expected: "A 2400-rated user seeing p15 Endgame Score Gap reads tooltip bullet 1 as 'vs other ~2400-rated players' (not 'vs all FlawChess users'). Mobile parity at 375px. Anchor source disclosed in bullet 4."
    why_human: "Requires prod deployment with live user data. Dev backfill is green. UAT scaffold is in 94.4-06-HUMAN-UAT.md. Both Plan 06 and Plan 12 UAT can run together at the v1.19 milestone-cut deploy."
---

# Phase 94.4: Peer-Relative Percentile Chip Refinement Verification Report

**Phase Goal:** Peer-Relative Percentile Chip refinement: cohort-based percentile CDFs anchored on user's per-TC rating with rating-anchor disclosure in chip tooltip; the D-12 Reversal Amendment locked a GAME-WEIGHTED BLENDED anchor (replacing the original Lichess-precedence rule) that pools converted-chess.com ratings with native-lichess ratings into a single median per user x TC, with tooltip bullet 4 disclosing the per-platform composition.

**Verified:** 2026-05-27T14:15:00Z
**Status:** human_needed
**Re-verification:** Yes — previous verification covered Plans 01-08 only; this verification covers the full 15-plan set including the D-12 Reversal Amendment (Plans 09-13).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `user_rating_anchors` has 5 blended columns (`anchor_rating`, `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native`) and NO old columns (`source_platform`, `chesscom_raw_rating`, `anchor_source` ENUM) | VERIFIED | `app/models/user_rating_anchors.py` lines 107-110 confirm exact 5-field model. Alembic migration `c70f5d94b243` drops old table + ENUM, recreates with new schema. Grep for `source_platform`/`chesscom_raw_rating` returns zero hits in `app/` and `frontend/src/` production code. |
| 2 | `per_user_cte_median_anchor(blend=True)` pools per-game converted chess.com + native lichess ratings and takes the pooled median | VERIFIED | `app/services/canonical_slice_sql.py` lines 1307-1341: `per_game_blended_rating` CTE UNION-ALLs chess.com (LATERAL nearest-anchor join to `chesscom_conversion_lookup`) + lichess (native passthrough). `per_user_anchor` takes `percentile_cont(0.5)` on the union. `_chesscom_conversion_values_sql()` emits VALUES lookup from `CHESSCOM_BLITZ_TO_LICHESS`. |
| 3 | `RatingAnchorOut` (Python) and TS `RatingAnchorOut` both have the 5-field blended shape; no `source_platform`/`chesscom_raw_rating` references in production code | VERIFIED | `app/schemas/endgames.py` lines 51-55. `frontend/src/types/endgames.ts` lines 374-380. Both have exactly `anchor_rating`, `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native`. `tsc --noEmit` exits 0. |
| 4 | `PercentileChip` tooltip renders 3 composition branches (mixed / pure-lichess / pure-chess.com) and has NO flame icon at any percentile | VERIFIED | `frontend/src/components/charts/PercentileChip.tsx` lines 199-208: 4-branch `bullet4` lambda. No `Flame` import anywhere in file. Vitest C4 regression test (line 259) asserts no flame for percentiles [1, 23, 50, 75, 90, 95, 99]. C1-C6 composition tests pass (677/677). |
| 5 | Dev backfill ran successfully: exit 0, 19 users processed, 13 mixed-user anchor rows, composition-typed summary table printed | VERIFIED | `94.4-12-HUMAN-UAT.md` lines 13-26: composition table reproduced with 13 mixed rows confirmed. `scripts/backfill_user_percentiles.py` implements `AnchorComposition` Literal and `_classify_composition`. |
| 6 | REQUIREMENTS.md PRPCR-03 reflects blended schema (not stale Lichess-precedence) | VERIFIED | `REQUIREMENTS.md` line 48: PRPCR-03 describes 5 blended columns + D-12 Reversal Amendment citation. Old `source_platform`/`anchor_source` wording is removed. |
| 7 | ROADMAP Phase 94.4 entry has D-12 amendment note + Plans 09-13 | VERIFIED | `ROADMAP.md` line 33: "(amended 2026-05-27 with D-12 reversal (game-weighted blended anchor); see CONTEXT §Amendment + `.planning/notes/percentile-anchor-d12-reversal.md`)". Plans 09-13 listed at lines 253-257. |
| 8 | CHANGELOG `[Unreleased]` has the blended-anchor entry | VERIFIED | `CHANGELOG.md` line 21: "Percentile chip anchor: switch to game-weighted blended anchor for mixed-platform users (GSD Phase 94.4 amendment, D-12 reversal)..." |
| 9 | Deferral is tracked, not lost: `94.4-12-HUMAN-UAT.md` has DEFERRED status; TODO file in `.planning/todos/pending/` | VERIFIED | `94.4-12-HUMAN-UAT.md` line 3: "Status: DEFERRED". `.planning/todos/pending/2026-05-27-phase-94.4-prod-backfill-and-d12-reversal-uat.md` confirmed present with full prod backfill procedure. |

**Score:** 9/9 truths verified

### Deferred Items

Items not yet met but explicitly addressed at the v1.19 milestone-cut deploy.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | PRPCR-09: Prod backfill + mixed-platform UAT (Plans 12 + 06) | v1.19 milestone-cut deploy | `.planning/todos/pending/2026-05-27-phase-94.4-prod-backfill-and-d12-reversal-uat.md` + `94.4-12-HUMAN-UAT.md` §Why Prod Backfill + UAT Are Deferred |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/models/user_rating_anchors.py` | 5-field blended ORM model, no source_platform | VERIFIED | Lines 107-110: `anchor_rating`, `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native`. Module docstring documents D-12 Reversal Amendment algorithm. |
| `app/repositories/user_rating_anchors_repository.py` | Blended row shape, upsert_anchor + fetch_anchors_for_user | VERIFIED | `RatingAnchorRow` dataclass has 5 fields. `upsert_anchor` accepts all 5. `fetch_anchors_for_user` returns dict keyed by TC. |
| `app/services/canonical_slice_sql.py` | blend=True mode + _chesscom_conversion_values_sql | VERIFIED | `_chesscom_conversion_values_sql` at line 1086. `per_user_cte_median_anchor(blend=True)` dispatches to `_per_user_cte_median_anchor_blended` at line 1237. Blended SQL at lines 1297-1341. |
| `app/services/user_benchmark_percentiles_service.py` | compute_anchors_for_user blended path | VERIFIED | Lines 231-307: iterates all TCs, calls `per_user_cte_median_anchor(blend=True, ...)`, upserts 5-field anchor rows. |
| `app/schemas/endgames.py` | RatingAnchorOut blended fields | VERIFIED | Lines 27-55: 5-field Pydantic model with docstring covering 4 disclosure branches (mixed / pure-lichess / pure-chess.com / suppressed). |
| `app/services/endgame_service.py` | No source_platform/chesscom_raw_rating references | VERIFIED | Lines 3044-3052: builds `RatingAnchorOut` from `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native`. Zero hits for old field names in production code. |
| `frontend/src/types/endgames.ts` | TS RatingAnchorOut blended shape | VERIFIED | Lines 374-380: 5-field interface. `rating_anchors: Partial<Record<...>>` at line 395. `tsc --noEmit` exits 0. |
| `frontend/src/components/charts/PercentileChip.tsx` | bullet 4 composition prose (3 branches), no flame | VERIFIED | Lines 199-208: mixed/pure-lichess/pure-chess.com branches. Only `BadgePercent` imported from lucide-react (no `Flame`). |
| `scripts/backfill_user_percentiles.py` | Composition-typed summary table | VERIFIED | Lines 178-183: `AnchorComposition` Literal. Lines 325-342: `_classify_composition`. Dev run output confirmed in HUMAN-UAT scaffold (13 mixed rows). |
| `alembic/versions/20260527_125014_c70f5d94b243_reshape_user_rating_anchors_for_blended_.py` | Drop/recreate migration, TRUNCATEs both tables | VERIFIED | Lines 63-115: TRUNCATE `user_benchmark_percentiles` (step 1), TRUNCATE `user_rating_anchors` (step 2), DROP TABLE, DROP CAST, DROP anchor_source ENUM, CREATE TABLE with 5-field blended schema. down_revision = `4c42ebc87b7f`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `compute_anchors_for_user` | `per_user_cte_median_anchor` | `blend=True` kwarg | VERIFIED | `user_benchmark_percentiles_service.py` line 266: `blend=True` passed. |
| `per_user_cte_median_anchor(blend=True)` | `CHESSCOM_BLITZ_TO_LICHESS` | `_chesscom_conversion_values_sql` | VERIFIED | `canonical_slice_sql.py` line 91 imports `CHESSCOM_BLITZ_TO_LICHESS`; line 1296 calls `_chesscom_conversion_values_sql(tc)`. |
| `endgame_service.py` | `RatingAnchorOut` (schema) | 5 blended fields | VERIFIED | Lines 3044-3052: all 5 fields populated from repository `RatingAnchorRow`. |
| `PercentileChip.tsx` | `RatingAnchorOut` (TS) | `nChesscomGames`, `nLichessGames` props | VERIFIED | Component props lines 107-116. `EndgameTimePressureCard.tsx` lines 191-194 pass `ratingAnchor.n_chesscom_games` / `n_lichess_games`. |
| Migration `c70f5d94b243` | Cascade-truncates `user_benchmark_percentiles` | `op.execute` | VERIFIED | Migration line 63. Both tables cleared as a unit before schema change. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `PercentileChip.tsx` | `nChesscomGames`, `nLichessGames`, `anchorRating` | `RatingAnchorOut` from `EndgameOverviewResponse.rating_anchors` | Dev backfill: 37 anchor rows across 19 users, 13 mixed-user rows. Blended SQL path exercised end-to-end. | VERIFIED (dev); prod data awaits v1.19 deploy |
| `_per_user_cte_median_anchor_blended` SQL | `anchor_rating`, `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native` | `per_game_blended_rating` CTE over `games` + `users` tables | 6-column SQL CTE at lines 1328-1341; HAVING clause enforces `MEDIAN_ANCHOR_MIN_GAMES` minimum | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TS type check (no stale field types) | `cd frontend && npx tsc --noEmit` | Exit 0, no output | PASS |
| Frontend tests (677 total including C1-C6 composition branches) | `cd frontend && npm test -- --run` | 677 passed (56 test files) | PASS |
| No Flame import in PercentileChip | `grep "Flame" frontend/src/components/charts/PercentileChip.tsx` | Only in comment lines (constraint documentation) | PASS |
| No source_platform in production code | `grep -rn "source_platform" app/ frontend/src/` (excluding tests) | Zero hits in production code | PASS |
| No chesscom_raw_rating in production code | `grep -rn "chesscom_raw_rating" app/ frontend/src/` (excluding tests) | Zero hits in production code | PASS |
| Alembic migration chain coherent | `c70f5d94b243` down_revision = `4c42ebc87b7f` | Confirmed — file locatable and readable | PASS |

### Probe Execution

No phase-declared probes. Backend tests (2187 passed per orchestrator pre-PR gates at plan-level) and frontend tests (677 passed) serve as the automated gate. Backend test run not re-executed here (requires dev DB; pre-PR gates confirmed clean per CONTEXT + SUMMARY metadata).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PRPCR-01 | 04 | Cohort CDF with sliding-window protocol, COHORT_PERCENTILE_CDF registry | VERIFIED (with WARNING) | `global_percentile_cdf.py` (97,598 lines): COHORT_PERCENTILE_CDF populated. `interpolate_cohort_percentile` at line 97556. Constants evolved from original spec (see WARNING below). |
| PRPCR-02 | 01 | `chesscom_to_lichess.py` conversion module | VERIFIED | `CHESSCOM_BLITZ_TO_LICHESS`, `CHESSCOM_INTRA_TC`, `convert_chesscom_to_lichess` confirmed. Snapshot date 2026-05-27 vs spec's 2026-05-26 — 1-day drift, not material. |
| PRPCR-03 | 02, 09 | `user_rating_anchors` blended-anchor schema | VERIFIED | 5-field ORM model + Alembic migration `c70f5d94b243` + REQUIREMENTS.md updated. |
| PRPCR-04 | 03, 05a | `interpolate_cohort_percentile`, per-TC ΔES CTEs, user_id widening | VERIFIED | `global_percentile_cdf.py` line 97556. Per-TC builders verified in `canonical_slice_sql.py`. |
| PRPCR-05 | 05a | `user_benchmark_percentiles` 8-value ENUM + 3-column PK | VERIFIED | Alembic `1945ae56aa20`: 8 ENUM values, PK `(user_id, metric, time_control_bucket)`. |
| PRPCR-06 | 07 | `PercentileChip` bare `p23` form, no flame icons | VERIFIED | `formatPercentileValue` returns bare integer, `BadgePercent` icon only. C4 regression guard active. |
| PRPCR-07 | 07, 11 | 4-bullet tooltip body with composition prose (3 branches) | VERIFIED | Lines 199-208 in PercentileChip.tsx. C1-C6 Vitest tests cover all 3 composition branches + suppression + no-flame. |
| PRPCR-08 | 08, 13 | SEED-019 supersession banner + memory amendment | VERIFIED | `SEED-019` line 14: superseded banner. Memory file updated per Plan 13 SUMMARY (outside git). |
| PRPCR-09 | 06, 12 | Prod backfill + HUMAN-UAT sign-off | HUMAN-NEEDED | Dev backfill confirmed green. Prod backfill deferred to v1.19 deploy. Tracked in `pending/` TODO file. |
| SEED-026 | all | Per-(user, TC) rating anchor, cohort CDF, peer-relative tooltip | VERIFIED | Full end-to-end chain from SQL anchor compute through CDF lookup through chip tooltip implemented and tested. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx` | 117-122 | Fixture uses stale `source_platform`, `chesscom_raw_rating`, `n_games` — missing `n_chesscom_games`, `n_lichess_games` | WARNING | Runtime `nChesscomGames` and `nLichessGames` are `undefined` when chips render in these tests; bullet 4 falls silently to the suppression (empty string) branch. TSC accepts via structural subtyping. Does NOT affect prod runtime — real API data has correct fields. Dedicated `PercentileChip.test.tsx` C1-C6 tests cover all composition branches with correct fixtures. |
| `frontend/src/components/charts/__tests__/EndgameMetricCard.test.tsx` | 98-104 | Same stale fixture pattern | WARNING | Same impact as above. |
| `frontend/src/components/charts/__tests__/EndgameOverallPerformanceSection.test.tsx` | 37-43 | Same stale fixture pattern | WARNING | Same impact as above. |
| `scripts/gen_global_percentile_cdf.py` | 120-131 | `COHORT_MAX_WINDOW_ELO=200` (PRPCR-01 specified 150); MIN/MAX model replaces single-K=200 | WARNING | Design improved during execution (commit `9cf2b77d`): variable-size cohorts (MIN=100, MAX=500) are strictly better. PRPCR-01 in REQUIREMENTS.md not updated. Goal achieved — cohort CDFs exist and power the chip. Recommend `/gsd-quick` to update PRPCR-01 wording post-merge. |

No `TBD`, `FIXME`, or `XXX` debt markers found in any D-12 Reversal Amendment files.

### Human Verification Required

#### 1. Prod Backfill + D-12 Reversal Mixed-Platform UAT

**Test:** At v1.19 milestone-cut deploy, after `bin/deploy.sh` applies migration `c70f5d94b243` and `scripts/backfill_user_percentiles.py --target prod` completes:
1. Log in as a mixed-platform account on `https://flawchess.com`. Navigate to Endgames. Open a page-level chip tooltip.
2. Verify bullet 4 renders: "Anchored at ~{X} Elo, blending {N} chess.com games (median {Y}, converted) with {M} lichess games (median {Z})."
3. Verify pure-lichess account shows "native rating" clause only (no "converted").
4. Verify chess.com-Daily-only classical user has NO classical chip.
5. Mobile parity at 375px; no flame icon at any percentile (p05, p23, p50, p77, p95).
6. Record results in `94.4-12-HUMAN-UAT.md` §UAT Recording.

**Expected:** All checks PASS. Mixed-platform anchor is higher than the pre-amendment lichess-only anchor when chess.com strength exceeds lichess. Pure-platform users see unchanged or near-unchanged anchors.

**Why human:** Requires prod deployment + prod backfill. Dev backfill is green (19 users, 13 mixed-user rows). Prod chips remain blank until migration + backfill complete. Full procedure in `94.4-12-HUMAN-UAT.md`.

#### 2. Plan 06 UAT: Elite-User Cohort-Relative Framing (PRPCR-09 / SC-9)

**Test:** Find a 2400-rated user account on `https://flawchess.com`. Inspect a page-level ΔES chip showing a lower-half percentile.

**Expected:** Tooltip bullet 1 reads "vs other ~2400-rated players in [tc]" (not "vs all FlawChess users"). Bullet 4 discloses anchor source matching the user's actual rating. Mobile parity at 375px.

**Why human:** Requires prod deployment with live elite-user data. Can run together with Plan 12 UAT at the v1.19 milestone-cut deploy. Scaffold in `94.4-06-HUMAN-UAT.md`.

### Gaps Summary

No implementation gaps. All 9 must-have truths are verified. The only outstanding items are the two operator-deferred prod UATs, tracked via:
- `94.4-12-HUMAN-UAT.md` (DEFERRED status, full UAT procedure)
- `.planning/todos/pending/2026-05-27-phase-94.4-prod-backfill-and-d12-reversal-uat.md` (step-by-step prod backfill commands + UAT procedure)

Three test fixture files carry stale field names from the pre-amendment schema. These are harmless at runtime and do not invalidate the phase (TSC passes; 677 tests pass; `PercentileChip.test.tsx` C1-C6 tests independently cover all 3 composition branches with correct fixtures). Recommend updating the 3 fixture objects as a cleanup task.

PRPCR-01 requirement text in REQUIREMENTS.md still references original K=200/±150-Elo constants. The code evolved to a better MIN=100/MAX=500 variable-size model (commit `9cf2b77d`) but PRPCR-01 was not updated. The cohort CDF goal is fully achieved. Recommend a `/gsd-quick` to update PRPCR-01 wording.

---

_Verified: 2026-05-27T14:15:00Z_
_Verifier: Claude (gsd-verifier) — post-D-12-Reversal-Amendment (Plans 01-13 complete)_
