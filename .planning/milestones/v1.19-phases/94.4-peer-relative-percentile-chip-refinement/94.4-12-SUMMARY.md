---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 12
subsystem: backfill-script + dev-execution
tags: [backfill, d12-reversal, blended-anchor, composition-summary, percentile-chip]
dependency_graph:
  requires: [94.4-09, 94.4-10, 94.4-11]
  provides: [dev-backfill-complete, composition-summary-schema]
  affects: [user_rating_anchors, user_benchmark_percentiles]
tech_stack:
  added: []
  patterns: [upsert-idempotent-backfill, composition-typing-from-game-counts]
key_files:
  modified:
    - scripts/backfill_user_percentiles.py
decisions:
  - Inline composition classification in _classify_anchor_rows using SQL row game counts (no full RatingAnchorRow construction in the query loop); _classify_composition helper is still defined and called for DRY reads from the probe path
  - --snapshot-date arg accepted and logged for run documentation but not passed to compute_anchors_for_user (which always uses the current conversion snapshot)
metrics:
  duration: "partial (Tasks 1+2 complete; Tasks 3+4 HUMAN-GATED)"
  completed: "2026-05-27"
  tasks_completed: 2
  tasks_total: 4
  files_modified: 1
---

# Phase 94.4 Plan 12: D-12 Reversal Backfill + UAT Summary

One-liner: Extended backfill script with composition-typed anchor summary (mixed/pure_lichess/pure_chesscom/no_anchor), then ran clean dev backfill with 19 users in 496s; prod backfill and mixed-platform UAT are HUMAN-GATED.

## Tasks Completed

### Task 1: Extend backfill_user_percentiles.py with composition-typed anchor summary

**Commit:** `190ccb2a` — `chore(94.4-12): replace per-platform anchor summary with composition-typed table`

**What changed:**

- Removed `AnchorSource` import (Plan 09 dropped `source_platform` from the table schema)
- Removed `_ALL_ANCHOR_SOURCES` constant (no longer needed)
- Added `RatingAnchorRow` import from `app.repositories.user_rating_anchors_repository`
- Added `AnchorComposition = Literal["mixed", "pure_lichess", "pure_chesscom", "no_anchor"]` type
- Added `_ALL_ANCHOR_COMPOSITIONS` constant
- Added `_classify_composition(row: RatingAnchorRow) -> AnchorComposition` helper
- Replaced `_AnchorSummary` cells dict from `(TC x AnchorSource)` to `(TC x AnchorComposition)` — merged the old `suppressed` dict into the `cells` dict as the `"no_anchor"` bucket
- Updated `_classify_anchor_rows()` to query `n_chesscom_games/n_lichess_games` instead of `source_platform` and call `_classify_composition` via a stub `RatingAnchorRow`
- Updated `_print_anchor_summary()` to output the composition table header per plan `<interfaces>` spec
- Added `--snapshot-date YYYY-MM-DD` CLI argument for run documentation
- Updated module docstring for Plan 12 / D-12 Reversal Amendment

**Acceptance criteria verified:**

- `grep -c 'pure_lichess|pure_chesscom|composition|_classify_composition' scripts/backfill_user_percentiles.py` = 34 (>= 3 required)
- `grep -c 'source_platform' scripts/backfill_user_percentiles.py` = 0
- `grep -c 'n_chesscom_games|n_lichess_games' scripts/backfill_user_percentiles.py` = 9 (>= 2 required)
- `--target prod` URL guard preserved (`_assert_target_safe`)
- `--skip-anchors` flag preserved
- `--help` shows composition-summary line
- `uv run ty check scripts/backfill_user_percentiles.py` = 0 errors

### Task 2: Dev backfill run

**No file commit (execution-only task)**

**Pre-flight:**
- Dev DB running: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` — Container was already Running
- Migrations at head: `uv run alembic upgrade head` — already at head
- Schema confirmed: `user_rating_anchors` has `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native` — no `source_platform` column

**Backfill run:**
```
uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-27
```
- Exit code: 0 (clean, first run, no re-run needed)
- Users processed: 19
- Duration: 496.0s (~8 min for a dev DB with real game data)

**Composition summary printed:**
```
Anchor Composition Summary
--------------------------
  TC              mixed   pure_lichess  pure_chesscom    no_anchor
  bullet              1              0              5           13
  blitz               5              0              8            6
  rapid               7              4              6            2
  classical           0              1              0           18
```

**Observations:**
- `classical` row: 18/19 users have `no_anchor` for classical — expected (few classical games in dev DB; chess.com Daily-only contributes 0 games per the amendment suppression rule)
- `mixed` users: 1+5+7+0 = 13 mixed anchor rows across all TCs — confirms D-12 Reversal Amendment is functioning (blending both platforms)
- `pure_lichess` = 0 in bullet/blitz — the dev DB users appear to be primarily chess.com or mixed users in those TCs

**Post-run DB sanity checks:**
- `SELECT count(*) FROM user_rating_anchors` = 37 (> 0)
- `SELECT count(*) FROM user_benchmark_percentiles` = 237 (> 0)
- `SELECT count(*) FROM user_rating_anchors WHERE n_chesscom_games > 0 AND n_lichess_games > 0` = 13 (> 0 — mixed users exist)

**Sample mixed-user rows (spot check):**

| user_id | TC    | anchor_rating | n_cc | n_li | cc_median_native | li_median_native |
|---------|-------|---------------|------|------|-----------------|-----------------|
| 2       | blitz | 1075          | 309  | 6    | 642             | 1466            |
| 2       | rapid | 1795          | 2977 | 23   | 1227            | 1553            |
| 13      | blitz | 2560          | 617  | 403  | 2621            | 2618            |
| 14      | bullet| 2344          | 1320 | 883  | 2231            | 2373            |
| 14      | blitz | 2235          | 1277 | 496  | 2173            | 2193            |

For user 2 blitz: 309 chess.com games (raw median 642) vs 6 Lichess games (native 1466). The blended anchor of 1075 is heavily chess.com-weighted (309 >> 6 games). With the ChessGoals conversion, the converted chess.com value pulls the anchor significantly below the Lichess native — consistent with a heavy weighting toward chess.com.

For user 13 blitz: near-equal game counts (617 cc, 403 li) with near-identical native medians (2621, 2618) → anchor 2560 is coherent (blended median of the pool is near both natives).

**Both composition summary and percentile summary printed without errors.**

## Deviations from Plan

### Auto-applied changes

**1. [Rule 1 - Bug] AnchorSource no longer exported from models (Plan 09 schema)**
- Found during: Task 1 pre-coding investigation
- Issue: The existing script imported `AnchorSource` from `app.models.user_rating_anchors`, but Plan 09's schema reshape removed `source_platform` and its associated `AnchorSource` type from the model. The import would have caused a runtime `ImportError`.
- Fix: Removed the `AnchorSource` import; added `RatingAnchorRow` import from the repository instead (needed for the new `_classify_composition` helper). Added `AnchorComposition = Literal[...]` inline in the script as a local type.
- Files modified: `scripts/backfill_user_percentiles.py`
- Commit: `190ccb2a`

**2. [Rule 3 - Blocking] _AnchorSummary.suppressed dict removed (merged into cells)**
- Found during: Task 1 implementation
- Issue: Old `_AnchorSummary` had a separate `suppressed: dict[TimeControlBucket, int]` for no-anchor users. The new composition approach needs a unified `cells` dict with all 4 composition categories including `"no_anchor"`.
- Fix: Merged the old `suppressed` dict into `cells` as the `"no_anchor"` category key; updated all references.
- Commit: `190ccb2a`

## Tasks Pending (HUMAN-GATED)

### Task 3: Prod backfill (CHECKPOINT: human-verify)

The Wave 1 + Wave 2 PR (Plans 09 + 10 + 11) must be merged to `main`, promoted to `production`, and deployed via `bin/deploy.sh` before the prod backfill can run. See plan task for the full procedure including the prod recovery contract.

### Task 4: Mixed-platform UAT — PRPCR-09 credibility re-check (CHECKPOINT: human-verify)

After prod backfill completes, the operator runs the UAT on `https://flawchess.com` against a mixed-platform account and records results in `94.4-12-HUMAN-UAT.md`.

## Known Stubs

None. The backfill script changes are purely operational (data population); no UI stubs introduced.

## Threat Flags

None. No new network endpoints or auth paths introduced. The `--snapshot-date` arg is informational only and does not affect DB writes.

## Self-Check: PASSED

- `scripts/backfill_user_percentiles.py` modified: FOUND
- Task 1 commit `190ccb2a` exists: FOUND (via `git log --oneline -5`)
- Dev backfill exit code 0: CONFIRMED (task notification shows exit code 0)
- Composition summary table printed: CONFIRMED (logged to /tmp/d12_reversal_backfill_dev.log)
- `SELECT count(*) FROM user_rating_anchors` = 37 > 0: CONFIRMED
- `SELECT count(*) FROM user_benchmark_percentiles` = 237 > 0: CONFIRMED
- Mixed users in dev DB: 13 rows with n_chesscom_games > 0 AND n_lichess_games > 0: CONFIRMED
