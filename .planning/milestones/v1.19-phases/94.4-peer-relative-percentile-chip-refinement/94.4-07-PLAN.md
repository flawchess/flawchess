---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 07
type: execute
wave: 3
depends_on: [94.4-05c]
files_modified:
  - frontend/src/components/charts/PercentileChip.tsx
  - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
autonomous: true
requirements: [PRPCR-06, PRPCR-07]

must_haves:
  truths:
    - "`PercentileChip.tsx` chip face renders the bare `p23` form per CONTEXT D-06. NO direction word (no 'Top X%' / 'Bottom X%'). NO flame icons (HARD CONSTRAINT per CONTEXT D-06 + commit 6766898c — `grep -c 'Flame\\|flame' frontend/src/components/charts/PercentileChip.tsx | grep -v '^\\s*//' | grep -v '^\\s*\\*'` returns 0; NO `Flame` import from `lucide-react`; NO `flameLevel` / `tierIcon` / `TIER_BREAKPOINTS` / `THRESHOLDS` consts). Color band (red < p25 / neutral p25-p75 / green > p75) carries direction. `MIN_PERCENT = 1` floor and p99 ceiling preserved per D-06a. `aria-label` preserves a direction word for screen readers per D-06b (e.g. `\"score gap percentile: p23 in bullet, bottom of cohort\"`)."
    - "`PercentileChipFlavor` collapses from 16 variants (post-94.3) to exactly 8 per RESEARCH Pattern 7 + CONTEXT D-07a: `'score-gap' | 'achievable' | 'parity' | 'conversion' | 'recovery' | 'time-pressure-score-gap' | 'clock-gap' | 'net-flag-rate'`. The 12 composite TC-suffixed flavors retire. Optional `tc?: TimeControlBucket` prop on per-TC chips (`time-pressure-score-gap`, `clock-gap`, `net-flag-rate`). Page-level ΔES chips (`score-gap`, `achievable`, `parity`, `conversion`, `recovery`) omit the `tc` prop entirely (chip is aggregated; tooltip frames as multi-TC)."
    - "Tooltip body is 4 bullets per CONTEXT D-07 + RESEARCH Pattern 7 lines 728-754:\n   1. Cohort framing inline with anchor + TC. Per-TC: `\"Compared to other ~{anchor_rating}-rated players in {tc}.\"`. Aggregated: `\"Compared to other ~{anchor_rating}-rated players, aggregated across the time controls you play.\"` (D-07b — aggregated framing).\n   2. Recent-games basis (TC-scoped). Per-TC: `\"Based on your most recent 1000 rated games in {tc} over the last 36 months, vs opponents within +/-100 Elo.\"`. Aggregated: `\"Based on your most recent 1000 rated games per time control over the last 36 months, vs opponents within +/-100 Elo.\"`.\n   3. Filter independence — `COPY_FILTER_INDEPENDENCE` kept verbatim from the post-94.3 module (preserves the existing UI filter-independence promise).\n   4. Rating-anchor disclosure. Lichess-anchored: `\"Anchored on your Lichess {tc-or-rating-per-TC} ({anchor_rating}).\"`. chess.com-anchored: `\"Anchored on your chess.com {tc-or-rating-per-TC} ({chesscom_raw_rating} -> {anchor_rating} Lichess-equivalent via ChessGoals snapshot 2026-05-26).\"` if `chesscom_raw_rating` is populated; otherwise omit the raw rating fragment."
    - "Retired: `RATING_NOTE_BY_FLAVOR` map, all 12 `COPY_RATING_NOTE_*` constants (`COPY_RATING_NOTE_SCORE_GAP` through `COPY_RATING_NOTE_NFR_CLASSICAL`), `DIRECTION_BY_FLAVOR` map per CONTEXT D-07a + RESEARCH Pattern 7 / Pattern 8 — all 8 flavors are `higher_is_better` post-94.4. The `PercentileChipDirection` type retires. `formatTopXPercent` retires, replaced by `formatBarePercentile(pct) => 'p{Math.max(MIN_PERCENT, Math.min(99, Math.round(pct)))}'`. `formatBottomOrTopPhrase` retires (or stays only for aria-label use)."
    - "`npm run knip` stays green after the retirement — the retired constants have NO external imports. Run `grep -rn 'RATING_NOTE_BY_FLAVOR\\|COPY_RATING_NOTE_\\|DIRECTION_BY_FLAVOR\\|PercentileChipDirection' frontend/src/` before retirement to confirm no external callers (RESEARCH Pitfall 6)."
    - "Per-TC chip call sites in `EndgameTimePressureCard.tsx` reshape from composite flavor → flavor + `tc` prop per RESEARCH Pattern 7 lines 753-759: `<PercentileChip flavor=\"time-pressure-score-gap\" tc=\"bullet\" percentile={...} anchorRating={...} anchorSource={...} />` replaces `<PercentileChip flavor=\"time_pressure_score_gap_bullet\" percentile={...} />`. Existing `data-testid` plumbing (`time-pressure-card-${tc}-${metric}-chip`) preserved verbatim per RESEARCH Project Constraints."
    - "`EndgameOverallScoreGapRow.tsx` and `EndgameMetricCard.tsx` chip slots wire to the new aggregated chip pattern (no `tc` prop). `EndgameMetricCard.tsx` gains a NEW chip slot for `recovery` per CONTEXT D-05a (Recovery rescued from v1 drop list). Anchor props (`anchorRating`, `anchorSource`, `chesscomRawRating`) sourced from `EndgameOverviewResponse.rating_anchors[dominant_tc]` where `dominant_tc` is the TC with the highest game count for the user — or, per CONTEXT 'Claude's Discretion' line 104, the planner can choose to display the per-chip 4th-bullet anchor disclosure with the dominant TC's anchor inline + a footnote for other TCs. Simplest implementation: pass the dominant TC's anchor for aggregated chips, name only that TC's anchor in the tooltip's 4th bullet, accept the loss of multi-TC precision in the disclosure (it remains honest because bullet 1 already says 'aggregated across the time controls you play')."
    - "Vitest tests in `PercentileChip.test.tsx` rewrite: 9 assertions per RESEARCH Pitfall 6 + PATTERNS lines 766-776. Critical regression: TEST 9 explicitly asserts NO flame icon renders at any percentile (`{99}`, `{95}`, `{90}`, `{50}`, `{1}`) — `queryByTestId(/flame/i)` returns null AND no `<svg>` child with a flame-related class. This is the locked regression guard for commit 6766898c."
    - "All 8 flavors render the same single-branch `deriveBandColor` (no direction-conditional branching — all flavors are `higher_is_better`). For Net Flag Rate, the underlying metric inverts at the data layer (smaller raw value → higher percentile) — the chip stays `higher_is_better` because the percentile is computed against a CDF that's already inverted at the Plan 04 gen layer. (Verify against the 94.3 implementation: if Net Flag Rate was `lower_is_better` in 94.3 via `DIRECTION_BY_FLAVOR`, this plan SWITCHES it to `higher_is_better` because the cohort CDF approach in Plan 04 should invert at the data layer. If it does NOT — pause and confirm with the planner before retiring `DIRECTION_BY_FLAVOR`.)"
  artifacts:
    - path: frontend/src/components/charts/PercentileChip.tsx
      provides: rewritten chip face + 4-bullet peer-relative tooltip + 8-flavor enum + tc prop + no-flame regression
      contains: "formatBarePercentile\\|p\\${\\|tc\\?: TimeControlBucket\\|anchorRating\\|anchorSource"
      min_lines: 250
    - path: frontend/src/components/charts/EndgameTimePressureCard.tsx
      provides: per-TC chip slots reshaped from composite flavor to flavor + tc prop
      contains: "flavor=\"time-pressure-score-gap\"\\|flavor=\"clock-gap\"\\|flavor=\"net-flag-rate\""
    - path: frontend/src/components/charts/EndgameMetricCard.tsx
      provides: page-level ΔES chip slots (Parity, Conversion, Recovery) wired to aggregated chip pattern
      contains: "flavor=\"recovery\"\\|flavor=\"parity\"\\|flavor=\"conversion\""
    - path: frontend/src/components/charts/__tests__/PercentileChip.test.tsx
      provides: rewritten assertions including no-flame regression guard
      contains: "queryByTestId(/flame/i)\\|p99\\|p23\\|no flame"
      min_lines: 200
  key_links:
    - from: frontend/src/components/charts/PercentileChip.tsx
      to: app/schemas/endgames.py RatingAnchorOut (Plan 05)
      via: anchorRating + anchorSource + chesscomRawRating props sourced from rating_anchors block
      pattern: "rating_anchors\\|anchorRating\\|anchorSource"
    - from: frontend/src/components/charts/EndgameTimePressureCard.tsx + EndgameMetricCard.tsx + EndgameOverallScoreGapRow.tsx
      to: frontend/src/components/charts/PercentileChip.tsx
      via: PercentileChip flavor + tc + anchor props
      pattern: "<PercentileChip"
---

<objective>
Frontend pivot for Phase 94.4. Rewrite `PercentileChip.tsx` chip face (bare `p23` pill, NO flame icons, NO direction word, color band only) and tooltip body (4 bullets, peer-relative framing, rating-anchor disclosure). Collapse the 16-variant `PercentileChipFlavor` enum to 8 + an optional `tc` prop per RESEARCH Pattern 7. Retire `RATING_NOTE_BY_FLAVOR`, all 12 `COPY_RATING_NOTE_*` constants, `DIRECTION_BY_FLAVOR`, and the `PercentileChipDirection` type per CONTEXT D-07a — all 8 flavors are `higher_is_better` post-94.4 (the data layer in Plan 04 handles Net Flag Rate inversion at CDF-gen time). Add a new `recovery` chip slot on `EndgameMetricCard.tsx` per CONTEXT D-05a. Vitest test suite rewrites to assert the new format AND a locked regression guard against the commit-6766898c flame icon return.

Purpose: This is the only frontend plan in Phase 94.4. Wave 3 lands AFTER the Wave 2 atomic-cutover quartet (Plans 03 → 04 → 05 → 06) per RESEARCH coordination line 1678 — frontend tolerates missing-percentile cells (chip suppresses when `null` arrives), so it can technically land in the same PR as Wave 2 or as a follow-up. Recommend same PR (single deploy lights up the new chip UX end-to-end).

Output: 4 frontend file edits + 1 test file rewrite. `npm run knip` stays green after the retirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@frontend/src/components/charts/PercentileChip.tsx
@frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/EndgameTimePressureCard.tsx
@frontend/src/components/charts/__tests__/PercentileChip.test.tsx
@frontend/src/types/endgames.ts
@frontend/src/lib/theme.ts
@CLAUDE.md

<interfaces>
<!-- Verified anchors in current PercentileChip.tsx (PATTERNS lines 632-736):
     - line 44: HOVER_OPEN_DELAY_MS = 100 (KEEP)
     - line 45-48: PERCENTILE_BAND_LOW = 25, PERCENTILE_BAND_HIGH = 75, MIN_PERCENT = 1 (KEEP)
     - line 54: CHIP_TEXT_COLOR (KEEP)
     - line 62: COPY_FILTER_INDEPENDENCE (KEEP)
     - lines 100-117: PercentileChipFlavor 16-variant enum (REPLACE with 8-variant)
     - line 121: PercentileChipDirection type (REMOVE)
     - lines 149-186: COPY_RATING_NOTE_* (12 constants — REMOVE)
     - lines 191-208: RATING_NOTE_BY_FLAVOR map (REMOVE)
     - lines 220-242: DIRECTION_BY_FLAVOR map (REMOVE)
     - lines 257-266: PercentileChipProps (REPLACE per Pattern 7)
     - lines 268-279: deriveBandColor (collapse to single-branch — all higher_is_better)
     - lines 281-293: formatTopXPercent (REPLACE with formatBarePercentile)
     - lines 82-91: formatBottomOrTopPhrase (KEEP only if needed by aria-label)
     - lines 295-334: popover body (REPLACE with 4-bullet peer-relative — PercentileChipPopoverBody)
     - lines 348-355: cleanup useEffect (KEEP)
     - lines 357-370: hover-tap mechanics (KEEP)
     - line 373: chip face label (REPLACE chip text with formatBarePercentile)
     - line 382: aria-label (REPLACE with peer-relative form)
     - lines 377-424: Radix Popover.Root/Trigger/Portal/Content shell (KEEP) -->

8-value flavor enum (RESEARCH Pattern 7 lines 656-665):
  export type PercentileChipFlavor =
    | 'score-gap'
    | 'achievable'
    | 'parity'
    | 'conversion'
    | 'recovery'              // NEW (rescued under peer-relative per D-05a)
    | 'time-pressure-score-gap'
    | 'clock-gap'
    | 'net-flag-rate';

Props (RESEARCH Pattern 7 lines 669-682):
  export interface PercentileChipProps {
    percentile: number;
    flavor: PercentileChipFlavor;
    tc?: TimeControlBucket;             // required for per-TC chips
    anchorRating: number;
    anchorSource: 'lichess' | 'chesscom';
    chesscomRawRating?: number;
    metricLabel: string;
    testId: string;
  }

Chip face text (RESEARCH Pattern 7 lines 687-691):
  function formatBarePercentile(pct: number): string {
    return `p${Math.max(MIN_PERCENT, Math.min(99, Math.round(pct)))}`;
  }

Color band (RESEARCH Pattern 7 lines 695-700, all higher_is_better):
  function deriveBandColor(pct: number): string {
    if (pct < PERCENTILE_BAND_LOW) return ZONE_DANGER;
    if (pct > PERCENTILE_BAND_HIGH) return ZONE_SUCCESS;
    return GAUGE_NEUTRAL;
  }

aria-label (CONTEXT D-06b):
  aria-label={`${metricLabel} percentile: p${rounded}${tc !== undefined ? ` in ${tc}` : ''}, ${rounded < 50 ? 'bottom' : 'top'} of cohort`}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight knip + grep audit of retiring constants (RESEARCH Pitfall 6)</name>
  <files>(no file modified — investigation only)</files>
  <read_first>
    - frontend/src/components/charts/PercentileChip.tsx (in full — confirm current exports and which constants are exported)
  </read_first>
  <action>
    Before retiring constants, verify they have NO external imports across the frontend codebase. Run: `grep -rE 'RATING_NOTE_BY_FLAVOR|COPY_RATING_NOTE_|DIRECTION_BY_FLAVOR|PercentileChipDirection|formatTopXPercent|formatBottomOrTopPhrase' frontend/src/ | grep -v 'charts/PercentileChip\.tsx'`. (W6 fix: do NOT exclude `PercentileChip.test.tsx` — test-only callers must also be caught and migrated; only the production module `PercentileChip.tsx` itself is excluded since it is the file whose internals are being retired.) If any external imports surface, surface as a blocker and update those files in this plan. (Expected: zero external imports — these are PercentileChip-internal per RESEARCH Pitfall 6, but verify empirically.) Then run `( cd frontend && npm run knip )` to capture the current baseline — note any pre-existing warnings; the Task 6 final knip should NOT add new warnings.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && grep -rE 'RATING_NOTE_BY_FLAVOR|COPY_RATING_NOTE_|DIRECTION_BY_FLAVOR|PercentileChipDirection' src/ | grep -v 'charts/PercentileChip\.tsx' | tee /tmp/percentile_external_refs.txt; test ! -s /tmp/percentile_external_refs.txt</automated>
  </verify>
  <acceptance_criteria>
    - Zero references to `RATING_NOTE_BY_FLAVOR`, `COPY_RATING_NOTE_*`, `DIRECTION_BY_FLAVOR`, `PercentileChipDirection` outside `charts/PercentileChip.tsx` (test file IS scanned per W6; if test file currently imports these, the rewrite must migrate them in the same plan).
    - If any external imports exist, surface as a blocker before continuing.
    - Baseline knip output captured (warnings count noted for comparison after Task 6).
  </acceptance_criteria>
  <done>Audit confirms safe-to-retire; no external callers; ready to rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite frontend/src/components/charts/PercentileChip.tsx — collapse flavors to 8 + tc prop + p23 chip face + 4-bullet popover + NO flame icon</name>
  <files>frontend/src/components/charts/PercentileChip.tsx</files>
  <read_first>
    - frontend/src/components/charts/PercentileChip.tsx (in full — line-by-line; the `<interfaces>` block has exact line anchors)
    - frontend/src/lib/theme.ts (verify `ZONE_DANGER`, `ZONE_SUCCESS`, `GAUGE_NEUTRAL`, `CHIP_TEXT_COLOR` are still the right exports for the band-color path)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 632-736 — full rewrite blueprint)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pattern 7 lines 671-758 — code blocks for formatBarePercentile, popover body, deriveBandColor, aria-label)
  </read_first>
  <behavior>
    - Test 1: Chip face renders `p${rounded}` form for any percentile input — e.g. `percentile={23}` renders `p23`.
    - Test 2: `MIN_PERCENT=1` floor — `percentile={0}` renders `p1`; `percentile={0.4}` renders `p1`.
    - Test 3: p99 ceiling — `percentile={99.6}` renders `p99`; `percentile={100}` renders `p99`.
    - Test 4: Color band — `percentile={20}` uses `ZONE_DANGER`; `percentile={50}` uses `GAUGE_NEUTRAL`; `percentile={80}` uses `ZONE_SUCCESS`.
    - Test 5: aria-label contains direction word — `percentile={23}` aria-label includes `"bottom"`; `percentile={70}` aria-label includes `"top"`.
    - Test 6: Per-TC chip with `tc="bullet"` — aria-label includes `"in bullet"`.
    - Test 7: Popover body bullet 1 with `tc="bullet"`, `anchorRating={1600}` reads `"Compared to other ~1600-rated players in bullet."`.
    - Test 8: Popover body bullet 1 with `tc` undefined and `anchorRating={1600}` reads `"Compared to other ~1600-rated players, aggregated across the time controls you play."`.
    - Test 9 (REGRESSION GUARD): NO flame icon renders at ANY percentile. Test cases at `{percentile: 1, 23, 50, 75, 90, 95, 99}` all assert `queryByTestId(/flame/i) === null` AND `container.querySelector('[class*="flame"], [class*="Flame"]') === null`.
    - Test 10: Lichess-anchored bullet 4 reads `"Anchored on your Lichess bullet (1600)."` for `tc="bullet"`, `anchorRating={1600}`, `anchorSource="lichess"`.
    - Test 11: chess.com-anchored bullet 4 reads `"Anchored on your chess.com blitz (1830 -> 1920 Lichess-equivalent via ChessGoals snapshot 2026-05-26)."` for `tc="blitz"`, `anchorRating={1920}`, `anchorSource="chesscom"`, `chesscomRawRating={1830}`.
    - Test 12: chess.com-anchored WITHOUT `chesscomRawRating` reads simpler form: `"Anchored on your chess.com rapid (1920)."`.
    - Test 13: Bullet 3 (filter independence) is `COPY_FILTER_INDEPENDENCE` verbatim from the post-94.3 module.
  </behavior>
  <action>
    Rewrite `frontend/src/components/charts/PercentileChip.tsx` in-place. KEEP verbatim from the post-94.3 module (per PATTERNS lines 644-653):
    - `HOVER_OPEN_DELAY_MS = 100` (line 44)
    - `PERCENTILE_BAND_LOW = 25`, `PERCENTILE_BAND_HIGH = 75`, `MIN_PERCENT = 1` (lines 45-48)
    - `CHIP_TEXT_COLOR` (line 54)
    - `COPY_FILTER_INDEPENDENCE` (line 62)
    - Radix `Popover.Root` / `Trigger` / `Portal` / `Content` shell (lines 377-424)
    - `handleMouseEnter` / `handleMouseLeave` hover-tap mechanics (lines 357-370)
    - Cleanup `useEffect` for `hoverTimeout` ref (lines 348-355)
    - All `data-testid` plumbing (line 383, 406)
    
    REMOVE (per PATTERNS lines 636-642):
    - `COPY_RATING_NOTE_*` (12 constants, lines 149-186)
    - `RATING_NOTE_BY_FLAVOR` (lines 191-208)
    - `DIRECTION_BY_FLAVOR` (lines 220-242)
    - `formatTopXPercent` (lines 281-293)
    - `PercentileChipDirection` type (line 121)
    
    Confirm `formatBottomOrTopPhrase` is either retired or used only by `aria-label`; if retained, simplify it to a single-branch form (no direction-conditional).
    
    REPLACE `PercentileChipFlavor` (lines 100-117) with the 8-value form per `<interfaces>` block. REPLACE `PercentileChipProps` (lines 257-266) with the new 8-field shape per `<interfaces>`. ADD `formatBarePercentile(pct: number): string` per `<interfaces>`. REPLACE `deriveBandColor` (lines 268-279) with the single-branch form (no direction-conditional). REPLACE the chip face label (line 373) with `formatBarePercentile(percentile)`. REPLACE `aria-label` (line 382) with the new template per `<interfaces>`.
    
    REPLACE the popover body (lines 295-334) with a new `PercentileChipPopoverBody({ flavor, tc, metricLabel, anchorRating, anchorSource, chesscomRawRating })` component per PATTERNS lines 704-725:
    ```tsx
    function PercentileChipPopoverBody({ flavor, tc, metricLabel, anchorRating, anchorSource, chesscomRawRating }: PopoverBodyProps): React.ReactElement {
      const tcOrAgg = tc !== undefined ? tc : 'rating per time control';
      const bullet1 = tc !== undefined
        ? `Compared to other ~${anchorRating}-rated players in ${tc}.`
        : `Compared to other ~${anchorRating}-rated players, aggregated across the time controls you play.`;
      const bullet2 = tc !== undefined
        ? `Based on your most recent 1000 rated games in ${tc} over the last 36 months, vs opponents within +/-100 Elo.`
        : `Based on your most recent 1000 rated games per time control over the last 36 months, vs opponents within +/-100 Elo.`;
      const bullet3 = COPY_FILTER_INDEPENDENCE;
      const bullet4 = anchorSource === 'lichess'
        ? `Anchored on your Lichess ${tcOrAgg} (${anchorRating}).`
        : (chesscomRawRating !== undefined
            ? `Anchored on your chess.com ${tcOrAgg} (${chesscomRawRating} -> ${anchorRating} Lichess-equivalent via ChessGoals snapshot 2026-05-26).`
            : `Anchored on your chess.com ${tcOrAgg} (${anchorRating}).`);
      return (
        <div className="space-y-1.5">
          <p>{bullet1}</p>
          <p>{bullet2}</p>
          <p>{bullet3}</p>
          <p>{bullet4}</p>
        </div>
      );
    }
    ```
    
    **HARD CONSTRAINT — NO FLAME ICON.** Do NOT add `Flame` import from `lucide-react`. Do NOT add tier thresholds (no `TIER_BREAKPOINTS`, no `p90` / `p95` / `p99` icon overlays). The peer-relative pivot does NOT change this — color band carries direction; tail emphasis is no longer expressed via icon. Flame logic was removed in commit `6766898c` and remains forbidden per CONTEXT D-06. Verify by `grep -c '[fF]lame' frontend/src/components/charts/PercentileChip.tsx | grep -v '^\s*//' | grep -v '^\s*\*'` returns 0 in the final file.
    
    Cite CONTEXT D-06 + D-06a + D-06b + D-07 + D-07a in module-level comments. Cite CLAUDE.md `text-sm` minimum font-size rule (the chip is small; ensure no `text-xs` styling sneaks in — popover bodies are the only sanctioned `text-xs` exception per CLAUDE.md frontend rules).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PercentileChipFlavor' frontend/src/components/charts/PercentileChip.tsx | head -1` is ≥ 1; flavor enum has exactly 8 values.
    - `grep -c "'score-gap'\|'achievable'\|'parity'\|'conversion'\|'recovery'\|'time-pressure-score-gap'\|'clock-gap'\|'net-flag-rate'" frontend/src/components/charts/PercentileChip.tsx` is ≥ 8.
    - No flame import: `grep -c 'Flame\|flame' frontend/src/components/charts/PercentileChip.tsx | grep -v '^\s*\(//\|\*\)'` is 0.
    - No retired constants: `grep -c 'RATING_NOTE_BY_FLAVOR\|COPY_RATING_NOTE_\|DIRECTION_BY_FLAVOR\|PercentileChipDirection' frontend/src/components/charts/PercentileChip.tsx` is 0.
    - `grep -c 'formatBarePercentile\|p\\\\\${' frontend/src/components/charts/PercentileChip.tsx` is ≥ 2 (function defined and called).
    - `grep -c 'anchorRating\|anchorSource\|chesscomRawRating' frontend/src/components/charts/PercentileChip.tsx` is ≥ 3 (new props).
    - `grep -c 'tc?: TimeControlBucket\|tc?:\s*TimeControlBucket' frontend/src/components/charts/PercentileChip.tsx` is ≥ 1.
    - `grep -c 'ChessGoals snapshot 2026-05-26' frontend/src/components/charts/PercentileChip.tsx` is ≥ 1 (bullet 4 chess.com-anchored case includes the snapshot date).
    - `( cd frontend && npx tsc --noEmit )` is clean.
    - All Task 4 tests pass (Tests 1-13).
  </acceptance_criteria>
  <done>PercentileChip rewritten; 8 flavors; tc prop; p23 chip face; 4-bullet popover; no flame icon; all 13 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Reshape EndgameTimePressureCard.tsx — per-TC chip slots from composite flavor → flavor + tc prop</name>
  <files>frontend/src/components/charts/EndgameTimePressureCard.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (in full — current 94.3 chip slots using composite TC-suffixed flavors like `flavor="time_pressure_score_gap_bullet"`)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 750-759 — example before/after)
    - frontend/src/types/endgames.ts (verify the new `rating_anchors` field is typed and accessible)
  </read_first>
  <action>
    Find all `<PercentileChip flavor="...">` call sites in `EndgameTimePressureCard.tsx`. Each call currently uses a composite TC-suffixed flavor (`time_pressure_score_gap_bullet`, `time_pressure_score_gap_blitz`, `time_pressure_score_gap_rapid`, `time_pressure_score_gap_classical`, same for `clock_gap_*` and `net_flag_rate_*` — 12 call sites total). Reshape each: replace the composite flavor with the base flavor and add a `tc` prop. Example per PATTERNS lines 754-759:
    ```tsx
    // 94.3:
    <PercentileChip flavor="time_pressure_score_gap_bullet" percentile={tps_bullet_percentile} ... />
    // 94.4:
    <PercentileChip flavor="time-pressure-score-gap" tc="bullet" percentile={tps_bullet_percentile} anchorRating={ratingAnchors.bullet?.anchor_rating ?? 0} anchorSource={ratingAnchors.bullet?.source_platform ?? 'lichess'} chesscomRawRating={ratingAnchors.bullet?.chesscom_raw_rating ?? undefined} metricLabel="Time Pressure Score Gap" testId={`time-pressure-card-bullet-tps-chip`} />
    ```
    All 12 call sites get the same reshape. `data-testid` plumbing (`time-pressure-card-${tc}-${metric}-chip`) preserved verbatim per RESEARCH Project Constraints. Anchor props are sourced from the new `rating_anchors` block on `EndgameOverviewResponse` (Plan 05 added it).
    
    Handle missing anchors gracefully: if `ratingAnchors[tc]` is undefined (anchor suppressed for that TC), the chip MUST suppress (don't render the chip at all — let the row render without the chip slot). Pattern:
    ```tsx
    {ratingAnchors.bullet !== undefined && tps_bullet_percentile !== null && (
      <PercentileChip flavor="time-pressure-score-gap" tc="bullet" percentile={tps_bullet_percentile} anchorRating={ratingAnchors.bullet.anchor_rating} anchorSource={ratingAnchors.bullet.source_platform} chesscomRawRating={ratingAnchors.bullet.chesscom_raw_rating ?? undefined} metricLabel="Time Pressure Score Gap" testId="time-pressure-card-bullet-tps-chip" />
    )}
    ```
    Apply CLAUDE.md "Always apply changes to mobile too" — if the component has separate desktop and mobile sections, reshape both.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit && npm test -- --run src/components/charts/__tests__/EndgameTimePressureCard.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'flavor="time-pressure-score-gap"\|flavor="clock-gap"\|flavor="net-flag-rate"' frontend/src/components/charts/EndgameTimePressureCard.tsx` is ≥ 12 (3 metric families × 4 TCs).
    - No composite TC-suffixed flavors remain: `grep -c 'flavor="time_pressure_score_gap_\|flavor="clock_gap_\|flavor="net_flag_rate_' frontend/src/components/charts/EndgameTimePressureCard.tsx` is 0.
    - `grep -c 'tc="bullet"\|tc="blitz"\|tc="rapid"\|tc="classical"' frontend/src/components/charts/EndgameTimePressureCard.tsx` is ≥ 12.
    - `grep -c 'anchorRating\|anchorSource' frontend/src/components/charts/EndgameTimePressureCard.tsx` is ≥ 4 (anchor props threaded through).
    - `data-testid="time-pressure-card-` plumbing preserved (`grep -c 'data-testid="time-pressure-card-' frontend/src/components/charts/EndgameTimePressureCard.tsx` is ≥ 12).
    - Suppression branch present (`grep -c 'ratingAnchors\.\(bullet\|blitz\|rapid\|classical\) !== undefined\|ratingAnchors\[' frontend/src/components/charts/EndgameTimePressureCard.tsx` is ≥ 1).
    - TS compile clean.
    - Component-level tests pass (if any exist).
  </acceptance_criteria>
  <done>12 per-TC chip slots reshaped to flavor + tc; anchor props wired; data-testid plumbing preserved; mobile parity applied.</done>
</task>

<task type="auto">
  <name>Task 4: Wire EndgameMetricCard.tsx + EndgameOverallScoreGapRow.tsx for aggregated page-level ΔES chips including new Recovery slot</name>
  <files>frontend/src/components/charts/EndgameMetricCard.tsx, frontend/src/components/charts/EndgameOverallScoreGapRow.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricCard.tsx (in full — current Parity / Conversion / Recovery? bullet structure)
    - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (in full — current Endgame Score Gap chip slot wiring)
    - frontend/src/types/endgames.ts (verify aggregated `*_percentile` field types + `rating_anchors` block)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 740-749)
  </read_first>
  <action>
    For each of the 5 page-level ΔES chip slots (Endgame Score Gap on `EndgameOverallScoreGapRow.tsx`; Achievable Score Gap presumably on the same row or its sibling; Parity / Conversion on `EndgameMetricCard.tsx`; **Recovery NEW slot on `EndgameMetricCard.tsx`** per CONTEXT D-05a):
    1. Update the flavor prop to the new short form: `score-gap`, `achievable`, `parity`, `conversion`, `recovery`. NO `tc` prop (aggregated chips omit `tc`).
    2. Anchor props sourced from the dominant-TC anchor: `const dominantTcAnchor = pickDominantTcAnchor(ratingAnchors)` where `pickDominantTcAnchor(anchors)` is a small helper that returns the anchor for the TC with the highest game count among the user's anchors. Pass `anchorRating={dominantTcAnchor.anchor_rating}`, `anchorSource={dominantTcAnchor.source_platform}`, `chesscomRawRating={dominantTcAnchor.chesscom_raw_rating ?? undefined}`. If `dominantTcAnchor` is undefined (user has no anchors at all — Stage A hasn't run, or all TCs suppressed), the chip suppresses entirely. Place the `pickDominantTcAnchor` helper in `frontend/src/lib/percentileAnchor.ts` (NEW file) so both row + card files can import it.
    3. **Recovery chip slot on `EndgameMetricCard.tsx`** is NEW. Add it alongside Parity / Conversion slots — the Recovery card already exists from earlier phases (see CONTEXT canonical_refs line 151 noting `EndgameMetricCard.tsx` is the file). Wire `<PercentileChip flavor="recovery" percentile={...} anchorRating={...} ...>` to the recovery_score_gap aggregated percentile field.
    4. CLAUDE.md "Always apply changes to mobile too" — both desktop and mobile renderings of each card get the chip slot.
    5. CLAUDE.md `text-sm` minimum — verify no `text-xs` styling on chips outside the sanctioned popover-body exception.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit && npm test -- --run src/components/charts/__tests__/EndgameMetricCard.test.tsx 2>&1 | tail -10 && npm test -- --run src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'flavor="recovery"' frontend/src/components/charts/EndgameMetricCard.tsx` is ≥ 1 (new chip slot per D-05a).
    - `grep -c 'flavor="parity"\|flavor="conversion"' frontend/src/components/charts/EndgameMetricCard.tsx` is ≥ 2 (existing slots reshaped).
    - `grep -c 'flavor="score-gap"\|flavor="achievable"' frontend/src/components/charts/EndgameOverallScoreGapRow.tsx` is ≥ 1.
    - `pickDominantTcAnchor` helper exists: `grep -c 'pickDominantTcAnchor' frontend/src/lib/percentileAnchor.ts` is ≥ 1.
    - Aggregated chips omit `tc` prop (`grep -c 'tc="' frontend/src/components/charts/EndgameMetricCard.tsx` for the ΔES card chips returns 0 — verify carefully against the file's structure).
    - Mobile + desktop parity (both render the chip slots; CLAUDE.md rule applied).
    - TS compile clean.
  </acceptance_criteria>
  <done>5 page-level chip slots wired with aggregated chip pattern; Recovery rescue NEW slot added; dominant-TC anchor helper extracted to a lib file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Rewrite frontend/src/components/charts/__tests__/PercentileChip.test.tsx with 13 assertions including no-flame regression guard</name>
  <files>frontend/src/components/charts/__tests__/PercentileChip.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx (in full — current 94.3 assertions; need to retire those that test `RATING_NOTE_BY_FLAVOR` / `DIRECTION_BY_FLAVOR` and add the new 94.4 assertions)
    - frontend/src/components/charts/PercentileChip.tsx (just rewritten in Task 2)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 762-776 — assertion list with explicit no-flame regression guard)
  </read_first>
  <behavior>
    Cover the 13 behavior cases from Task 2's `<behavior>` block. Critical regression: **Test 9 NO flame icon at any percentile** — explicit per-percentile assertion sweep.
  </behavior>
  <action>
    Rewrite `frontend/src/components/charts/__tests__/PercentileChip.test.tsx`. Retire ALL existing assertions tied to `RATING_NOTE_BY_FLAVOR`, `DIRECTION_BY_FLAVOR`, `Top X%` / `Bottom X%` phrase formats — those constants and formats retire in 94.4.
    
    Add the 13 tests per Task 2 `<behavior>`:
    1. Chip face renders `p${rounded}` form — parametrize over `[1, 10, 23, 50, 75, 90, 99]`.
    2. `MIN_PERCENT=1` floor for `[0, 0.4, -5]` → `p1`.
    3. p99 ceiling for `[99.6, 100, 105]` → `p99`.
    4. Color band correctness for percentiles below 25 / between 25-75 / above 75.
    5. aria-label direction word for low (< 50) and high (>= 50) percentiles.
    6. Per-TC chip aria-label includes `"in ${tc}"`.
    7. Per-TC popover bullet 1.
    8. Aggregated (no `tc`) popover bullet 1.
    9. **REGRESSION GUARD — NO flame icon at any percentile.** Parametrize over `[1, 23, 50, 75, 90, 95, 99]`. For each: `render(<PercentileChip percentile={pct} flavor="score-gap" anchorRating={1500} anchorSource="lichess" metricLabel="X" testId="x" />); expect(screen.queryByTestId(/flame/i)).toBeNull(); expect(container.querySelector('[class*="flame"], [class*="Flame"]')).toBeNull();`. This is the locked regression guard for commit-6766898c flame removal.
    10. Lichess-anchored bullet 4.
    11. chess.com-anchored bullet 4 WITH `chesscomRawRating`.
    12. chess.com-anchored bullet 4 WITHOUT `chesscomRawRating`.
    13. Bullet 3 is `COPY_FILTER_INDEPENDENCE` verbatim.
    
    Use `@testing-library/react` + Vitest `expect` + `render` + `screen` + `queryByTestId` per the existing test conventions.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx -t "" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 13 behavior cases pass.
    - `grep -c 'queryByTestId(/flame/i)\|queryByTestId(/Flame/)' frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is ≥ 1 (the no-flame regression assertion).
    - `grep -c "p23\|p99\|p1\|p50" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is ≥ 4 (sample percentiles explicit).
    - `grep -c "ChessGoals snapshot 2026-05-26" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is ≥ 1 (chess.com-anchored Test 11).
    - `grep -c "RATING_NOTE_BY_FLAVOR\|DIRECTION_BY_FLAVOR\|Top X%\|Bottom X%" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is 0 (legacy assertions retired).
    - `( cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx )` exits 0.
  </acceptance_criteria>
  <done>13 tests + no-flame regression guard + legacy assertion retirement complete.</done>
</task>

<task type="auto">
  <name>Task 6: Run knip + lint + full frontend test suite + tsc — verify nothing regressed</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist" (frontend gates)
  </read_first>
  <action>
    Run the full frontend gate suite: `( cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run && npm run knip )`. The knip check is critical — RESEARCH Pitfall 6 requires no new dead exports after the constant retirement. If knip surfaces new warnings (vs the Task 1 baseline), surface and resolve them in this task. If lint surfaces new issues (e.g. unused import after removing `Flame`), fix them. If TS surfaces new errors (e.g. `tc` prop missing on a Time Pressure call site), fix them. If Vitest surfaces new failures in `EndgameTimePressureCard.test.tsx` or related component tests (because the chip prop shape changed), update those tests to use the new prop signatures.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit && npm run lint && npm test -- --run && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` is clean.
    - `npm run lint` is clean.
    - `npm test -- --run` all tests pass.
    - `npm run knip` is clean — no new warnings vs Task 1 baseline.
  </acceptance_criteria>
  <done>All frontend gates green; knip stays clean; PercentileChip rewrite + 4 component reshapes + test rewrite end-to-end coherent.</done>
</task>

<task type="auto">
  <name>Task 7: Pre-PR checklist for Plan 07 — full ruff/ty/pytest + frontend gates</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist"
  </read_first>
  <action>
    Run the full backend gate suite too — Plan 07 doesn't touch backend files, but the Pre-PR checklist mandate is unconditional: `uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -x`. Then frontend gates (already run in Task 6 — re-run to confirm). All green ⇒ Plan 07 ready to merge.
  </action>
  <verify>
    <automated>uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -x && ( cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run && npm run knip )</automated>
  </verify>
  <acceptance_criteria>
    - All backend and frontend gates green.
  </acceptance_criteria>
  <done>Plan 07 ready to merge as the final Wave 3 deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → frontend | Anchor rating + source_platform + chesscom_raw_rating ship through the typed `rating_anchors` block on `EndgameOverviewResponse`; frontend treats as display-only — no input crosses back. |
| User → DOM | Tooltip body strings are template-interpolated with backend-supplied ints; no user-controlled HTML; React's auto-escaping handles strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-07-01 | Tampering | Flame icon regression via future refactor | mitigate | Vitest Test 9 explicitly asserts NO flame icon at every percentile in `[1, 23, 50, 75, 90, 95, 99]`. CI failure surfaces if reintroduced. CONTEXT D-06 + commit 6766898c lock the constraint at the documentation level. |
| T-94.4-07-02 | Information disclosure | Tooltip shows another user's anchor rating | mitigate | Anchor props sourced from `EndgameOverviewResponse.rating_anchors` which is filtered by `current_user.id` at the API layer (Plan 05 V4 mitigation). |
| T-94.4-07-03 | Tampering | XSS via metricLabel or tooltip string interpolation | mitigate | React's JSX auto-escapes string values. `metricLabel` is a hardcoded constant from the parent component, not user-supplied. |
| T-94.4-07-SC | Tampering | npm package installs | accept | No new npm packages introduced. lucide-react already in package.json (existing Flame import retired, no new import added). |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx` green (13 cases including the no-flame regression guard).
- `cd frontend && npm run knip` clean (no new dead exports after RATING_NOTE_BY_FLAVOR + COPY_RATING_NOTE_* + DIRECTION_BY_FLAVOR retirement).
- `cd frontend && npx tsc --noEmit && npm run lint && npm test -- --run` all green.
- `grep -c '[fF]lame' frontend/src/components/charts/PercentileChip.tsx | head -1` is 0 (HARD CONSTRAINT verified).
- `PercentileChipFlavor` has exactly 8 values.
- All 12 Time Pressure per-TC chip slots reshape to flavor + tc prop.
- New Recovery chip slot on `EndgameMetricCard.tsx` per D-05a.
</verification>

<success_criteria>
PRPCR-06 fully implemented (chip pill = `p23`, no flame, no direction word, color band only, MIN_PERCENT=1, p99 ceiling, aria-label preserves direction). PRPCR-07 fully implemented (4-bullet peer-relative tooltip, rating-anchor disclosure, RATING_NOTE_BY_FLAVOR + COPY_RATING_NOTE_* + DIRECTION_BY_FLAVOR retire, knip green, 8-value flavor enum + tc prop). New Recovery chip slot rescued per D-05a. No flame icon at any tier (commit 6766898c regression guard locked).
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-07-SUMMARY.md` when done. Record: final test count in PercentileChip.test.tsx; knip baseline + final warning count; confirm no-flame regression test asserts on `[1, 23, 50, 75, 90, 95, 99]`; chip slot count per component (TimePressureCard 12, MetricCard 3, OverallScoreGapRow 1-2).
</output>
