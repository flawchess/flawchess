# Phase 94.4 Plan 12 — HUMAN-UAT Scaffold (PRPCR-03 / PRPCR-09 — D-12 Reversal Amendment)

**Status:** DEFERRED — prod backfill (Task 3) and mixed-platform UAT (Task 4) deferred per `/gsd-execute-phase 94.4` checkpoint on 2026-05-27 (operator chose "skip prod work — finalize Plan 12 as dev-only").

**Tracked TODO:** `.planning/todos/pending/2026-05-27-phase-94.4-prod-backfill-and-d12-reversal-uat.md`

---

## What Tasks 1+2 Delivered (Autonomous, Already Shipped)

1. `scripts/backfill_user_percentiles.py` extended with composition-typed anchor summary (`mixed` / `pure_lichess` / `pure_chesscom` / `no_anchor`). Plan 06's per-`(tc, source_platform)` summary is replaced with a per-`(tc, composition)` summary aligned with the D-12 Reversal Amendment schema.
2. Dev backfill run against the existing dev DB (no `bin/reset_db.sh` per memory `feedback_no_dev_db_reset_in_plans`). Exit 0; 19 users processed, 37 anchor rows, 237 percentile rows.
3. Composition table from dev confirming the amendment works:

   ```
   Anchor Composition Summary
   --------------------------
     TC              mixed   pure_lichess  pure_chesscom    no_anchor
     bullet              1              0              5           13
     blitz               5              0              8            6
     rapid               7              4              6            2
     classical           0              1              0           18
   ```

   13 mixed-user anchor rows confirm the blended-anchor SQL path is exercised end-to-end against real data.

---

## Why Prod Backfill + UAT Are Deferred

The D-12 Reversal Amendment ships a destructive schema migration (`reshape_user_rating_anchors_for_blended_anchor`, revision `c70f5d94b243`) that TRUNCATEs both `user_rating_anchors` and `user_benchmark_percentiles`. Until prod runs the migration + backfill, prod chips remain on the pre-amendment schema and the user-visible behavior change does not land for real users.

The deferral is operator-driven, not a phase failure:

- Dev backfill is green end-to-end.
- The CHANGELOG + REQUIREMENTS + ROADMAP doc loop (Plan 13) does not depend on prod backfill — it documents the amendment schema and behavior change as an unreleased entry.
- Releases for this milestone (v1.19) cut at the milestone boundary, not per-phase, so the prod backfill can run as part of the milestone-cut deploy.

The tracked TODO captures the prod-backfill + UAT obligation so it does not silently drop.

---

## UAT Procedure (when Tasks 3+4 run — paralleling Plan 06's UAT but tuned to the D-12 reversal)

1. Pre-flight: confirm the Wave 1+2 PR (Plans 09 + 10 + 11) is merged to `main`, promoted to `production`, and `bin/deploy.sh` has completed. Verify the Alembic head includes `reshape_user_rating_anchors_for_blended_anchor` (revision `c70f5d94b243`).
2. Pick a MIXED-PLATFORM account — one that has both chess.com games and lichess games in at least one TC. The operator's own account qualifies if it satisfies this; otherwise pick a known mixed-platform test fixture.
3. Log in on `https://flawchess.com`. Navigate to the Endgames page. Inspect the page-level ΔES chips (Endgame Score Gap, Achievable Score Gap, Section-2 cards).
4. Tooltip bullet 4 (rating-anchor disclosure) MUST render the blended-composition prose, e.g.:

   > "Anchored at ~1834 Elo, blending 247 chess.com games (median 1830, converted via ChessGoals snapshot) with 412 lichess games (median 1860)."

5. **Risk-profile checks** (the amendment was designed to surgically improve mixed-platform users; pure-platform users should see negligible change):
   - **Mixed user (target population):** anchor should be HIGHER than the pre-amendment lichess-only anchor when chess.com strength exceeds lichess strength; percentile DECREASES (user competes against stronger cohort peers). Record observed shift direction + rough magnitude vs expectation.
   - **Pure-lichess user (regression guard):** anchor + percentile UNCHANGED vs pre-amendment. Pull a snapshot from a second account if available.
   - **Pure-chess.com user (regression guard):** anchor NEARLY-UNCHANGED (median commutativity holds; ≤ ~20 Elo shift expected from the per-game vs per-user-median conversion path).
6. **Suppression intact:** a chess.com-Daily-only classical user should have NO classical chip (no anchor row written for that `(user, classical)`).
7. **Mobile parity at 375px** (Chrome DevTools mobile or real phone): chip remains compact; tooltip bullet 4 reads without horizontal scroll.
8. **No flame icon at any percentile** — HARD CONSTRAINT per CONTEXT D-06, regression guard from commit `6766898c` enforced by Plan 07 + Plan 11 test C4/9. Confirm visually across p05, p23, p50, p77, p95.
9. **Tooltip readability on mobile** — bullet 4 wraps cleanly; no clipped text; no overflow.

---

## UAT Recording (to be filled in by the operator when Tasks 3+4 run)

**Date tested:** _TBD_

**Sentry monitoring during prod backfill:** _no exception flood expected; record observed Sentry behavior_

**Mixed-platform user tested:**
- Rating (composition): _TBD_
- Composition disclosed in tooltip bullet 4: _TBD_
- Anchor shift vs pre-amendment (expected: higher if chess.com stronger than lichess): _TBD_
- Percentile shift vs pre-amendment (expected: lower if anchor moved higher): _TBD_

**Pure-lichess regression user:** _TBD — anchor + percentile UNCHANGED expected_

**Pure-chess.com regression user:** _TBD — ≤ ~20 Elo anchor shift expected_

**Suppression check (chess.com-Daily-only classical):** _TBD — no chip expected_

**Mobile parity (375px):** _TBD_

**No-flame regression:** _TBD — visually confirmed across p05/p23/p50/p77/p95_

**Tooltip bullet 4 readability:** _TBD_

**Verdict:** _PASS | FAIL | PARTIAL_

**Operator initials + timestamp:** _TBD_
