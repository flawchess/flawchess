---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 04
type: execute
wave: 2
depends_on: [94.4-03]
files_modified:
  - scripts/gen_global_percentile_cdf.py
  - app/services/global_percentile_cdf.py
  - reports/cohort-percentile-cdf-latest.md
  - tests/scripts/test_gen_global_percentile_cdf_unchanged.py
autonomous: false
requirements: [PRPCR-01, PRPCR-04]

must_haves:
  truths:
    - "`scripts/gen_global_percentile_cdf.py` is rewritten per CONTEXT D-11 + RESEARCH Pitfall 8 (in-Python ranking, not per-anchor SQL — 1,050 queries collapses to ~32 queries). 5 module-level constants drive the strict-default sliding-window cohort policy per CONTEXT D-11: `COHORT_K_USERS_PER_ANCHOR: Final[int] = 200`, `COHORT_MAX_WINDOW_ELO: Final[int] = 150`, `COHORT_ANCHOR_STEP_ELO: Final[int] = 50`, `COHORT_ANCHOR_MIN_ELO: Final[int] = 800`, `COHORT_ANCHOR_MAX_ELO: Final[int] = 2400`. Constants are one-line-tunable for post-Plan-04 operator review; the regen report (Task 4) surfaces per-(metric, anchor, TC) suppression flags as a TOP-LINE TABLE (not in an appendix) so the operator can decide whether to relax before Plan 06 backfill runs."
    - "`app/services/global_percentile_cdf.py` is rewritten: `GLOBAL_PERCENTILE_CDF` retires. `COHORT_PERCENTILE_CDF: Mapping[CdfMetricId, Mapping[tuple[int, TimeControlBucket], CdfTable]]` replaces it (per CONTEXT D-09 + RESEARCH Pattern 1). `CdfMetricId` Literal collapses from 16 values (post-94.3) to exactly 8 values per CONTEXT D-13: `score_gap`, `achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`, `recovery_score_gap`, `time_pressure_score_gap`, `clock_gap`, `net_flag_rate`. `interpolate_percentile(metric, value)` retires. `interpolate_cohort_percentile(metric: CdfMetricId, value: float, anchor: int, tc: TimeControlBucket) -> float | None` ships (per CONTEXT D-09a). Anchor rounds to nearest 50 (50-Elo grid); returns None when no CDF exists at that (metric, anchor, tc) cell."
    - "Per-(metric, anchor, TC) cohort CDF generation runs as a 3-level loop: outer `for metric in 8 metrics: for tc in 4 TCs:` issues ONE benchmark-DB query against the JOIN of `per_user_values(user_id, metric_value, n_games)` × `per_user_anchor(user_id, anchor_rating, n_games)` (the Plan 03 + Plan 02 builders) returning ~K_floor users with `(metric_value, anchor_rating)` per user. Inner Python loop sweeps `anchor in range(COHORT_ANCHOR_MIN_ELO, COHORT_ANCHOR_MAX_ELO + 1, COHORT_ANCHOR_STEP_ELO)` (33 anchors): `ranked = sorted(rows, key=lambda r: (abs(r.anchor_rating - anchor), r.user_id))[:COHORT_K_USERS_PER_ANCHOR]` — the `user_id` tiebreaker is load-bearing for byte-identical regen determinism (W3 fix); if `len(ranked) < COHORT_K_USERS_PER_ANCHOR` OR `abs(ranked[-1].anchor_rating - anchor) > COHORT_MAX_WINDOW_ELO`, mark suppressed (skip; emit nothing). Otherwise compute 99-breakpoint CDF via `tuple(float(numpy.percentile(values, p)) for p in range(1, 100))` and store in `result[anchor]`. RESEARCH Code Examples lines 1346-1393 + Pattern 1 lines 297-359 are the verbatim blueprint."
    - "Regenerated `app/services/global_percentile_cdf.py` is byte-identical to a re-execution of the regen script (deterministic regen test passes). The byte-identical regression test `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` is REGENERATED to match the new 3-level shape — it was expected RED after Plan 03 (RESEARCH Wave-2 coordination line 1676); this task turns it back to GREEN against the new goldens."
    - "Regen report `reports/cohort-percentile-cdf-latest.md` is created (or replaces the 94.3 `global-percentile-cdf-latest.md`). The 94.3 report is archived to `reports/archive/` per RESEARCH Plan-Boundary line 1620. The report's TOP-LINE section is a per-(metric, anchor, TC) suppression-flag table covering all 8 × 33 × 4 = 1,056 cells with: anchor | TC | suppressed Y/N | suppression reason (`n < 200` OR `k-th distance > 150 Elo`) | k-th distance (Elo) | n_users. Operator reviews this table before Plan 06 backfill runs (HUMAN-VERIFY checkpoint at end of plan)."
    - "All 8 × 33 × 4 = 1,056 cells are attempted. Cells passing K=200 + ±150-Elo strict default produce a 99-breakpoint CDF entry; cells failing emit a suppression-flag row in the report and ARE NOT keyed in `COHORT_PERCENTILE_CDF` (missing key → `interpolate_cohort_percentile` returns None → chip suppresses). Expected coverage per RESEARCH Pattern 4: heavy suppression in bullet, blitz, and most classical anchors; rapid mid-range (1200-2000) is the only band with comfortable coverage. The strict-default policy is deliberate (D-11) — operator can relax K/window after reviewing the report; constants are module-level for one-line tuning."
    - "`autonomous: false` — last task is a HUMAN-VERIFY checkpoint blocking Plan 06's backfill. The operator reads the regen report's suppression table and decides whether to (a) ship strict defaults (Plan 06 proceeds), (b) relax `COHORT_K_USERS_PER_ANCHOR` or `COHORT_MAX_WINDOW_ELO` and rerun this plan's Task 3, or (c) defer Plan 06 entirely pending a future research cycle. Per CONTEXT D-11, the strict default is the ship state unless the operator explicitly relaxes."
  artifacts:
    - path: scripts/gen_global_percentile_cdf.py
      provides: cohort CDF regeneration with 5 module-level constants + 3-level loop + in-Python K=200 ranking
      contains: "COHORT_K_USERS_PER_ANCHOR\\|COHORT_MAX_WINDOW_ELO\\|COHORT_ANCHOR_STEP_ELO\\|COHORT_ANCHOR_MIN_ELO\\|COHORT_ANCHOR_MAX_ELO"
      min_lines: 300
    - path: app/services/global_percentile_cdf.py
      provides: COHORT_PERCENTILE_CDF registry + interpolate_cohort_percentile helper + 8-value CdfMetricId Literal
      contains: "COHORT_PERCENTILE_CDF\\|interpolate_cohort_percentile\\|CdfMetricId"
      min_lines: 500
    - path: reports/cohort-percentile-cdf-latest.md
      provides: regen report with per-(metric, anchor, TC) suppression-flag table as top-line
      contains: "suppression\\|Suppressed"
      min_lines: 100
    - path: tests/scripts/test_gen_global_percentile_cdf_unchanged.py
      provides: byte-identical regen regression test (regenerated goldens against the new 3-level shape)
      min_lines: 30
  key_links:
    - from: scripts/gen_global_percentile_cdf.py
      to: app/services/canonical_slice_sql.py per_user_cte_median_anchor + per-TC ΔES builders (Plans 02 + 03)
      via: per_user_values × per_user_anchor JOIN by user_id
      pattern: "per_user_cte_median_anchor\\|per_user_cte_score_gap_tc\\|per_user_cte_achievable_tc\\|per_user_cte_section2"
    - from: app/services/global_percentile_cdf.py interpolate_cohort_percentile
      to: app/services/user_benchmark_percentiles_service.py (Plan 05)
      via: Stage A/B per-user percentile lookup against (metric, anchor, tc) cell
      pattern: "interpolate_cohort_percentile"
---

<objective>
Rewrite `scripts/gen_global_percentile_cdf.py` and `app/services/global_percentile_cdf.py` for the cohort-CDF architecture per CONTEXT D-09 / D-09a / D-11 / D-13 and RESEARCH Pattern 1 + Pattern 4. The 94.3 `GLOBAL_PERCENTILE_CDF` (16-key flat registry) retires; `COHORT_PERCENTILE_CDF` (8 metrics × ~33 anchors × 4 TCs ≈ 1,056 cells minus suppressed) replaces it. The 16-value `CdfMetricId` Literal collapses to exactly 8 values per D-13. `interpolate_cohort_percentile(metric, value, anchor, tc)` replaces `interpolate_percentile(metric, value)`. The regen script switches from per-anchor SQL queries (1,050 queries) to in-Python ranking after a single per-(metric, TC) query (~32 queries — RESEARCH Pitfall 8).

Purpose: This is the second commit in the Wave 2 atomic-cutover quartet. Plan 03 widened the per-TC builders with `user_id`; this plan consumes them in the cohort-CDF JOIN against Plan 02's `per_user_cte_median_anchor`. The regen report's suppression-flag table is the operator's primary signal for whether to ship strict K=200/±150 defaults or relax (CONTEXT D-11 deferred-relaxation discipline). After this plan, Plan 05 can rewrite the per-user service against the new `COHORT_PERCENTILE_CDF` shape.

Output: rewritten gen script + rewritten registry module + new regen report + REGENERATED byte-identical regression test goldens (the test was EXPECTED RED after Plan 03 — this task turns it GREEN against the new shape). Last task is a HUMAN-VERIFY checkpoint blocking Plan 06's backfill (`autonomous: false`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@scripts/gen_global_percentile_cdf.py
@app/services/global_percentile_cdf.py
@app/services/canonical_slice_sql.py
@tests/scripts/test_gen_global_percentile_cdf_unchanged.py
@CLAUDE.md

<interfaces>
<!-- Verified anchors:
     - RESEARCH Pattern 1 (lines 297-362): full cohort CDF gen design + index shape
     - RESEARCH Pitfall 4 (lines 1202-1216): K=200 / ±150 Elo viability probe results — heavy suppression expected outside rapid mid-range
     - RESEARCH Pitfall 8 (lines 1257-1268): regeneration cost — collapse from 1,050 queries to 32 by in-Python ranking
     - RESEARCH Code Examples lines 1346-1393: verbatim 94.3 CDF main loop (reference for the rewrite)
     - PATTERNS lines 560-614: rewrite blueprint for gen_global_percentile_cdf.py
     - PATTERNS lines 337-368: registry shape + helper signature in global_percentile_cdf.py -->

Module-level constants (PATTERNS lines 604-611, CONTEXT D-11):
  COHORT_K_USERS_PER_ANCHOR: Final[int] = 200
  COHORT_MAX_WINDOW_ELO: Final[int] = 150
  COHORT_ANCHOR_STEP_ELO: Final[int] = 50
  COHORT_ANCHOR_MIN_ELO: Final[int] = 800
  COHORT_ANCHOR_MAX_ELO: Final[int] = 2400

Registry shape (CONTEXT D-09, RESEARCH Pattern 1):
  COHORT_PERCENTILE_CDF: Mapping[CdfMetricId, Mapping[tuple[int, TimeControlBucket], CdfTable]] = {
      "score_gap": {
          (1200, "blitz"): CdfTable(breakpoints=(... 99 floats ...), n_users=200),
          (1250, "blitz"): CdfTable(breakpoints=(...), n_users=200),
          ...
      },
      ...
  }

8-value CdfMetricId (CONTEXT D-13):
  CdfMetricId = Literal[
      "score_gap",
      "achievable_score_gap",
      "section2_score_gap_conv",
      "section2_score_gap_parity",
      "recovery_score_gap",
      "time_pressure_score_gap",
      "clock_gap",
      "net_flag_rate",
  ]

Helper (CONTEXT D-09a):
  def interpolate_cohort_percentile(
      metric: CdfMetricId,
      value: float,
      anchor: int,
      tc: TimeControlBucket,
  ) -> float | None:
      """Round anchor to nearest 50. Return None if (metric, rounded_anchor, tc) absent.
      Otherwise linear-interpolate value against the 99-breakpoint CDF.
      """

3-level loop blueprint (PATTERNS lines 577-602, RESEARCH Pattern 1):
  for metric in CdfMetricId.__args__:                     # 8 metrics
      for tc in ("bullet", "blitz", "rapid", "classical"):  # 4 TCs
          rows = await session.execute(text(_build_per_user_with_anchor_query(
              metric, tc, snapshot_date=snapshot_date,
          ))).all()
          # rows is list of (user_id, metric_value, n_games, anchor_rating)
          per_metric_per_tc: dict[int, CdfTable] = {}
          for anchor in range(COHORT_ANCHOR_MIN_ELO, COHORT_ANCHOR_MAX_ELO + 1, COHORT_ANCHOR_STEP_ELO):
              ranked = sorted(rows, key=lambda r: (abs(r.anchor_rating - anchor), r.user_id))[:COHORT_K_USERS_PER_ANCHOR]  # user_id tiebreaker for deterministic regen
              if len(ranked) < COHORT_K_USERS_PER_ANCHOR:
                  continue  # suppressed (insufficient K)
              kth_dist = abs(ranked[-1].anchor_rating - anchor)
              if kth_dist > COHORT_MAX_WINDOW_ELO:
                  continue  # suppressed (window too wide)
              values = sorted(r.metric_value for r in ranked)
              breakpoints = tuple(float(numpy.percentile(values, p)) for p in range(1, 100))
              per_metric_per_tc[anchor] = CdfTable(breakpoints=breakpoints, n_users=len(ranked))
          # Store per_metric_per_tc keyed by (anchor, tc) under metric
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app/services/global_percentile_cdf.py — collapse CdfMetricId to 8 values, replace registry shape, swap helper signature</name>
  <files>app/services/global_percentile_cdf.py</files>
  <read_first>
    - app/services/global_percentile_cdf.py (in full — current post-94.3 state with 16-value CdfMetricId + flat registry + interpolate_percentile)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 337-368 — registry + helper rewrite pattern)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pattern 1 lines 344-359 for COHORT_PERCENTILE_CDF shape; CONTEXT D-13 for 8-value count)
  </read_first>
  <action>
    Rewrite `app/services/global_percentile_cdf.py`:
    1. `CdfMetricId` Literal collapses from 16 values to exactly 8 per CONTEXT D-13: `score_gap`, `achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`, `recovery_score_gap`, `time_pressure_score_gap`, `clock_gap`, `net_flag_rate`. The 12 Phase 94.3 composite TC-suffixed values (`*_bullet`, `*_blitz`, `*_rapid`, `*_classical` for the 3 time-pressure metric families) retire entirely. Add a module-level comment citing CONTEXT D-13.
    2. `GLOBAL_PERCENTILE_CDF` retires entirely — DELETE the variable and any reference. Replace with `COHORT_PERCENTILE_CDF: Mapping[CdfMetricId, Mapping[tuple[int, TimeControlBucket], CdfTable]] = {...}`. The 1,056-cell-minus-suppressed body is REGENERATED by `scripts/gen_global_percentile_cdf.py` in Task 3 of this plan — for now leave a placeholder `COHORT_PERCENTILE_CDF: Mapping[CdfMetricId, Mapping[tuple[int, TimeControlBucket], CdfTable]] = {}` so the module imports cleanly. The script in Task 3 overwrites this with the populated registry.
    3. `interpolate_percentile(metric, value)` retires entirely — DELETE the function and any reference. Replace with `interpolate_cohort_percentile(metric: CdfMetricId, value: float, anchor: int, tc: TimeControlBucket) -> float | None` per PATTERNS lines 359-368. Body: round `anchor` to nearest `COHORT_ANCHOR_STEP_ELO` (= 50); look up `COHORT_PERCENTILE_CDF.get(metric, {}).get((rounded_anchor, tc))`; if None return None; otherwise linear-interpolate `value` against the 99-breakpoint CDF using the existing `_interpolate_99_breakpoints` helper (read the file to find it — it's the same interpolation math, just wrapped in a different lookup). Cite D-09a in the docstring.
    4. Add module-level constants imported from the gen script (or duplicate inline with a comment that they MUST stay in sync — `COHORT_ANCHOR_STEP_ELO: Final[int] = 50` is needed for the round-to-nearest-50 logic in the helper).
    5. Run `grep -rn "from app.services.global_percentile_cdf import\|from app.services import global_percentile_cdf\|GLOBAL_PERCENTILE_CDF\|interpolate_percentile" app/ tests/ scripts/ | grep -v 'app/services/global_percentile_cdf.py'` to surface ALL callers — the legacy names must be removed downstream in Plan 05. For this task, leave each broken call site annotated with `# TODO Plan 05 (94.4-05): cut over to interpolate_cohort_percentile` and a noqa or pragma so ty/ruff stays clean. (Plan 05 finishes the cutover.) Do NOT actually break call sites in this task — instead, keep a `interpolate_percentile(metric, value)` STUB that warns + returns None so ty/import doesn't fail; Plan 05 deletes the stub.
    6. Cite D-09 / D-09a / D-13 in module-level docstrings.
  </action>
  <verify>
    <automated>uv run ty check app/services/global_percentile_cdf.py && uv run python -c "from app.services.global_percentile_cdf import CdfMetricId, COHORT_PERCENTILE_CDF, interpolate_cohort_percentile; import typing; print(len(typing.get_args(CdfMetricId)))"</automated>
  </verify>
  <acceptance_criteria>
    - The Python one-liner prints `8` (CdfMetricId has exactly 8 values per D-13).
    - `grep -c "COHORT_PERCENTILE_CDF\b" app/services/global_percentile_cdf.py` is ≥ 2 (declaration + at least one reference).
    - `grep -c "def interpolate_cohort_percentile" app/services/global_percentile_cdf.py` is exactly 1.
    - `grep -c "GLOBAL_PERCENTILE_CDF\b" app/services/global_percentile_cdf.py | grep -v '^#'` is 0 (legacy registry retired in the module proper; only legitimate references in comments are tolerated).
    - `interpolate_percentile` is a STUB or removed (Task ships either; Plan 05 finishes cutover).
    - `uv run ty check app/services/global_percentile_cdf.py` returns zero errors.
    - Module-level docstrings cite D-09 / D-09a / D-13.
  </acceptance_criteria>
  <done>Registry module rewritten with 8-value CdfMetricId, COHORT_PERCENTILE_CDF placeholder, interpolate_cohort_percentile helper.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite scripts/gen_global_percentile_cdf.py — 5 module constants, 3-level loop, in-Python K=200 ranking, suppression flags</name>
  <files>scripts/gen_global_percentile_cdf.py</files>
  <read_first>
    - scripts/gen_global_percentile_cdf.py (in full — current post-94.3 state; need to understand the existing CLI shape, --target dev|prod URL guards, the `_assert_benchmark_db` helper, async session setup, and the existing `_build_metric_breakpoint_query` at lines ~300-317)
    - app/services/canonical_slice_sql.py (verify the new per-TC builders from Plan 03 are present: `per_user_cte_score_gap_tc`, `per_user_cte_achievable_tc`, `per_user_cte_section2_tc`, `per_user_cte_section2(..., bucket_label='recovery')`, and Plan 02's `per_user_cte_median_anchor`)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 560-614 — rewrite blueprint)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pattern 1 lines 297-362 + Pitfall 8 lines 1257-1268 + Code Examples lines 1346-1393)
  </read_first>
  <action>
    Rewrite `scripts/gen_global_percentile_cdf.py`. Preserve from the existing script: CLI shape (`--target dev|prod` argparse + URL guards from 94.1 Plan 09 fix), `_assert_benchmark_db(url)` guard requiring `'flawchess_benchmark'` AND `:5433`, async session boilerplate, snapshot_date arg + default-to-today, the existing CdfTable dataclass definition (or import from the registry module). REMOVE the existing `_build_metric_breakpoint_query` body that emitted per-metric `percentile_cont` directly.
    
    Add 5 module-level constants at the top per PATTERNS lines 604-611:
    ```
    COHORT_K_USERS_PER_ANCHOR: Final[int] = 200
    COHORT_MAX_WINDOW_ELO: Final[int] = 150
    COHORT_ANCHOR_STEP_ELO: Final[int] = 50
    COHORT_ANCHOR_MIN_ELO: Final[int] = 800
    COHORT_ANCHOR_MAX_ELO: Final[int] = 2400
    ```
    Each constant has a comment citing CONTEXT D-11 (strict default, deferred relaxation policy). 
    
    Add `_build_per_user_with_anchor_query(metric: CdfMetricId, tc: TimeControlBucket, *, snapshot_date)` per PATTERNS lines 579-588: emits `WITH {selected_users_cte(source="benchmark")}, {per_user_cte_for(metric, source="benchmark", tc=tc, snapshot_date=snapshot_date)}, {per_user_cte_median_anchor(tc, source="benchmark", snapshot_date=snapshot_date)} SELECT puv.user_id, puv.metric_value, puv.n_games, pua.anchor_rating FROM per_user_values puv JOIN per_user_anchor pua USING (user_id)`. Define a `per_user_cte_for(metric, ...)` dispatch helper that returns the correct per-TC builder for each of the 8 metrics — `score_gap` → `per_user_cte_score_gap_tc`, `achievable_score_gap` → `per_user_cte_achievable_tc`, `section2_score_gap_conv` → `per_user_cte_section2_tc(..., bucket_label='conversion')`, `section2_score_gap_parity` → `per_user_cte_section2_tc(..., bucket_label='parity')`, `recovery_score_gap` → `per_user_cte_section2_tc(..., bucket_label='recovery')` (per RESEARCH Open Question 3), `time_pressure_score_gap` → `per_user_cte_time_pressure_score_gap` (Plan 03 widened it), `clock_gap` → `per_user_cte_clock_gap`, `net_flag_rate` → `per_user_cte_net_flag_rate`.
    
    Add `async def _build_cohort_cdf_for(session, metric, tc, snapshot_date) -> dict[int, CdfTable]` per PATTERNS lines 590-602: the 3-level loop body. For each `anchor in range(COHORT_ANCHOR_MIN_ELO, COHORT_ANCHOR_MAX_ELO + 1, COHORT_ANCHOR_STEP_ELO)`, `sorted(rows, key=lambda r: (abs(r.anchor_rating - anchor), r.user_id))[:K]` — the `user_id` tiebreaker is REQUIRED for byte-identical regen determinism (Task 3's golden regression test depends on it). Suppress if `len(ranked) < K` or `kth_dist > COHORT_MAX_WINDOW_ELO`. Otherwise compute 99-breakpoint CDF via `tuple(float(numpy.percentile(values, p)) for p in range(1, 100))` and store. Capture a parallel `suppression_log: dict[CdfMetricId, dict[tuple[int, TimeControlBucket], dict]]` that records, for EVERY anchor (suppressed and non-suppressed): `n_users` (after K-clipping), `kth_dist_elo`, `suppressed` (bool), `suppression_reason` ('insufficient_k' | 'window_too_wide' | None). This log feeds Task 4's regen report.
    
    Add `async def regenerate(target: Literal["benchmark"]) -> tuple[str, dict]` orchestrating the 3-level loop (8 metrics × 4 TCs = 32 queries) and returning (the generated Python source for `app/services/global_percentile_cdf.py` body, the suppression_log). Write the generated source to `app/services/global_percentile_cdf.py` overwriting the placeholder COHORT_PERCENTILE_CDF — preserving the rest of the module's content (CdfMetricId Literal, interpolate_cohort_percentile, comments). Use a marker-block approach: keep `# === BEGIN GENERATED ===` / `# === END GENERATED ===` sentinels in the module so the regen rewrites only the generated section. (If the existing 94.3 script used a different approach for the rewrite-in-place, mirror it.)
    
    Add `--regen-report-path` argparse arg defaulting to `reports/cohort-percentile-cdf-latest.md`. Pass the suppression_log to a `_render_regen_report(log, snapshot_date)` helper that emits the markdown for Task 4 (this helper can live in the same script).
    
    Cross-check: the existing 94.3 script's `_build_metric_breakpoint_query` issued one SQL per metric (16 queries); the rewrite issues 32 queries (8 × 4) but each query returns more data; the per-anchor SQL approach the 94.3 script used would have required 8 × 33 × 4 ≈ 1,056 queries — the in-Python ranking is the Pitfall 8 cost mitigation.
  </action>
  <verify>
    <automated>uv run ty check scripts/gen_global_percentile_cdf.py && uv run python scripts/gen_global_percentile_cdf.py --help 2>&1 | grep -c "target\|regen-report-path\|snapshot-date"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COHORT_K_USERS_PER_ANCHOR\|COHORT_MAX_WINDOW_ELO\|COHORT_ANCHOR_STEP_ELO\|COHORT_ANCHOR_MIN_ELO\|COHORT_ANCHOR_MAX_ELO" scripts/gen_global_percentile_cdf.py` is ≥ 5.
    - `grep -c "def _build_per_user_with_anchor_query\|async def _build_cohort_cdf_for\|async def regenerate\|def _render_regen_report" scripts/gen_global_percentile_cdf.py` is ≥ 4.
    - `grep -c "per_user_cte_median_anchor\|per_user_cte_score_gap_tc\|per_user_cte_achievable_tc\|per_user_cte_section2" scripts/gen_global_percentile_cdf.py` is ≥ 4 (script imports/calls the Plan 02 + Plan 03 builders).
    - `grep -c "abs(r.anchor_rating - anchor)\|kth_dist" scripts/gen_global_percentile_cdf.py` is ≥ 1 (in-Python ranking is present).
    - `grep -c "key=lambda r: (abs(r.anchor_rating - anchor), r.user_id)" scripts/gen_global_percentile_cdf.py` is ≥ 1 (deterministic tiebreaker locked per W3).
    - `grep -c "numpy.percentile\|np.percentile" scripts/gen_global_percentile_cdf.py` is ≥ 1.
    - Each of the 5 module constants is annotated with `Final[int]` and has a comment citing CONTEXT D-11.
    - `--target dev|prod` URL guard preserved (the 94.1 Plan 09 fix).
    - `uv run ty check scripts/gen_global_percentile_cdf.py` returns zero errors.
  </acceptance_criteria>
  <done>Script rewritten; 5 constants present; 3-level loop + in-Python ranking + suppression-log capture implemented; ty-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Execute regen against benchmark DB; rewrite app/services/global_percentile_cdf.py with the populated COHORT_PERCENTILE_CDF; regenerate the byte-identical regression test goldens</name>
  <files>app/services/global_percentile_cdf.py, tests/scripts/test_gen_global_percentile_cdf_unchanged.py</files>
  <read_first>
    - tests/scripts/test_gen_global_percentile_cdf_unchanged.py (in full — the byte-identical regen pattern; this test was EXPECTED RED after Plan 03; this task makes it GREEN against the new shape)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 612-614 — regen report top-line table requirement)
  </read_first>
  <action>
    Ensure the benchmark DB is running: `bin/benchmark_db.sh start` (if not already up). Run `uv run python scripts/gen_global_percentile_cdf.py --target benchmark --snapshot-date 2026-05-26 --regen-report-path reports/cohort-percentile-cdf-latest.md`. The script will: (1) issue 32 SQL queries (8 metrics × 4 TCs) against the benchmark DB; (2) in-Python rank each query's results across 33 anchors; (3) write the populated `COHORT_PERCENTILE_CDF` block into `app/services/global_percentile_cdf.py` (between the marker sentinels); (4) write the regen report to `reports/cohort-percentile-cdf-latest.md`.
    
    After regen completes, REGENERATE the byte-identical golden in `tests/scripts/test_gen_global_percentile_cdf_unchanged.py`. The test was originally for the 94.3 16-key flat registry; it must now assert the new 3-level shape regenerates byte-identically. Read the existing test and adapt: (a) the regen script signature changed; (b) the output module's CdfMetricId + COHORT_PERCENTILE_CDF block is what gets compared. The simplest approach: re-execute the regen via a subprocess inside the test, then compare the resulting module against the committed version with `assert generated_source == committed_source`. If the existing test used a different mechanism (e.g. importlib + AST diff), preserve it but update the golden. The point: the regen is deterministic, and any drift will surface in this test.
    
    Run the regression test to confirm it's now GREEN: `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x`. If it's still RED, confirm the tiebreaker is in place (sort key for `rows` in `_build_cohort_cdf_for` MUST be `sorted(rows, key=lambda r: (abs(r.anchor_rating - anchor), r.user_id))` — Task 2 locks this; Task 3's golden regen depends on it being deterministic when multiple users share the same anchor distance).
    
    Commit the regen output: `app/services/global_percentile_cdf.py` (populated), `reports/cohort-percentile-cdf-latest.md`, `tests/scripts/test_gen_global_percentile_cdf_unchanged.py`. NOTE: per RESEARCH Plan-Boundary line 1620, the prior 94.3 `reports/global-percentile-cdf-latest.md` (if it exists under that name) is moved to `reports/archive/global-percentile-cdf-2026-05-XX.md` (use the prior snapshot date in the filename) — confirm via `ls reports/global-percentile-cdf-latest.md` before deciding whether to archive. If no such file exists, skip the archive step.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/global_percentile_cdf.py` contains a populated `COHORT_PERCENTILE_CDF` dict (NOT an empty placeholder); `grep -c '(1[0-9][0-9][0-9],' app/services/global_percentile_cdf.py` is ≥ 10 (multiple anchor keys present in the dict — sanity check that the regen wrote real data).
    - `reports/cohort-percentile-cdf-latest.md` exists.
    - `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` passes (the regen is deterministic).
    - Module imports cleanly: `uv run python -c "from app.services.global_percentile_cdf import COHORT_PERCENTILE_CDF, interpolate_cohort_percentile; print(len(COHORT_PERCENTILE_CDF))"` prints `8` (all 8 metrics present; some may have very few or zero non-suppressed anchors).
    - `uv run ty check app/services/global_percentile_cdf.py` returns zero errors after the regen overwrites the module.
  </acceptance_criteria>
  <done>Regen executes cleanly; COHORT_PERCENTILE_CDF populated; byte-identical test GREEN; regen report exists.</done>
</task>

<task type="auto">
  <name>Task 4: Populate reports/cohort-percentile-cdf-latest.md with per-(metric, anchor, TC) suppression-flag table as TOP-LINE</name>
  <files>reports/cohort-percentile-cdf-latest.md</files>
  <read_first>
    - reports/cohort-percentile-cdf-latest.md (just created in Task 3 — verify the suppression table is present)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pitfall 4 lines 1202-1216 — what to expect on heavy suppression in bullet, blitz, classical)
    - CONTEXT D-11 (the regen report MUST surface suppression flags as a TOP-LINE table, not in an appendix)
  </read_first>
  <action>
    Verify the regen report's structure matches CONTEXT D-11. The TOP-LINE section (right after a brief intro paragraph and the snapshot/source metadata) MUST be a table with columns: metric | anchor (Elo) | TC | suppressed (Y/N) | suppression reason (`insufficient_k` for n < 200, `window_too_wide` for k-th distance > 150 Elo, or `—` if not suppressed) | k-th user distance (Elo) | n_users. 1,056 rows total (8 metrics × 33 anchors × 4 TCs), sorted by metric ASC, anchor ASC, TC in canonical order (bullet, blitz, rapid, classical). The table is the primary output of the regen; operator reads it to decide whether to relax the strict K=200 / ±150-Elo defaults before Plan 06 backfill runs. Below the suppression table, include:
    - A summary section: "Per-metric coverage" — for each metric, the count of non-suppressed cells (e.g. `score_gap: 47/132 cells non-suppressed; bullet 0/33, blitz 4/33, rapid 32/33, classical 11/33`).
    - The 5 module-level constants verbatim (so the operator sees the current policy at a glance).
    - A short "Relaxation guidance" paragraph: if rapid mid-range coverage is good but bullet / classical are mostly suppressed, the operator can either accept the loss (chip suppresses for affected users — Phase 94.4 ships with strict defaults), OR relax `COHORT_K_USERS_PER_ANCHOR` to 150 / 100 and rerun this plan's Task 3, OR widen `COHORT_MAX_WINDOW_ELO` to 200 Elo. Cite CONTEXT D-11.
    - Snapshot metadata: snapshot_date, source (= benchmark DB), git commit hash of the gen script, the regen runtime.
    If Task 3's regen helper already produces this format, just verify and lightly edit for clarity. If not, edit the helper in `scripts/gen_global_percentile_cdf.py` to produce the spec'd format and re-run regen (Task 3).
  </action>
  <verify>
    <automated>test -f reports/cohort-percentile-cdf-latest.md && grep -c "suppressed\|Suppressed\|insufficient_k\|window_too_wide" reports/cohort-percentile-cdf-latest.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists.
    - The first H2 (`## `) or H3 (`### `) heading after the metadata is the per-(metric, anchor, TC) suppression table (TOP-LINE per D-11).
    - The suppression table has 1,056 data rows (8 × 33 × 4) — `grep -c '^| ' reports/cohort-percentile-cdf-latest.md` is ≥ 1056 (accounting for header rows; rough check).
    - The 5 module-level constants appear verbatim in the report (`grep -c "COHORT_K_USERS_PER_ANCHOR.*200\|COHORT_MAX_WINDOW_ELO.*150\|COHORT_ANCHOR_STEP_ELO.*50\|COHORT_ANCHOR_MIN_ELO.*800\|COHORT_ANCHOR_MAX_ELO.*2400" reports/cohort-percentile-cdf-latest.md` is ≥ 5).
    - Per-metric coverage summary is present (`grep -c "Per-metric coverage\|cells non-suppressed" reports/cohort-percentile-cdf-latest.md` is ≥ 1).
    - Relaxation-guidance paragraph cites CONTEXT D-11.
  </acceptance_criteria>
  <done>Regen report ships the suppression flag table as top-line; operator-readable; cites D-11.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: HUMAN-VERIFY — Operator reviews suppression report and decides ship-vs-relax</name>
  <files>reports/cohort-percentile-cdf-latest.md (operator appends a `## Operator Decision` heading recording the decision)</files>
  <action>Operator review only — Claude does not write code in this task. See `<how-to-verify>` for the operator's review procedure.</action>
  <what-built>Task 1-4 deliverables: rewritten gen script, populated COHORT_PERCENTILE_CDF registry, new interpolate_cohort_percentile helper, regen report with 1,056-row suppression table.</what-built>
  <how-to-verify>
    1. Open `reports/cohort-percentile-cdf-latest.md` in any markdown viewer.
    2. Read the TOP-LINE per-(metric, anchor, TC) suppression-flag table. Pay attention to: (a) which TC bands have heavy suppression (likely bullet, blitz, classical at edges per RESEARCH Pitfall 4); (b) the per-metric coverage summary — how many cells are non-suppressed per metric; (c) which anchors at the rating extremes (sub-1000, super-2200) suppress most metrics.
    3. Decision tree (per CONTEXT D-11):
       - **Option A — Ship strict defaults.** Coverage is acceptable; users in suppressed bands will see no chip for that (metric, TC), but the chip rendering tolerates this (`interpolate_cohort_percentile` returns None → frontend suppresses chip). Approve Plan 06 to proceed with backfill.
       - **Option B — Relax K or window and re-regen.** If suppression is severe enough that the elite-user UAT goal (SC-9) is at risk, relax one constant in `scripts/gen_global_percentile_cdf.py` (e.g. `COHORT_K_USERS_PER_ANCHOR=150` OR `COHORT_MAX_WINDOW_ELO=200`) and re-run Plan 04 Task 3.
       - **Option C — Defer Plan 06 entirely.** If coverage is too thin to justify shipping cohort percentiles at all, abort the Wave 2 atomic-cutover. (Unlikely; benchmark DB has ~60k users per RESEARCH Pattern 4, so rapid mid-range coverage is robust.)
    4. Record the decision in a paragraph appended to `reports/cohort-percentile-cdf-latest.md` under a new `## Operator Decision` heading: "Reviewed YYYY-MM-DD. Decision: {A | B | C}. Rationale: {short paragraph}. Next step: {Plan 06 proceeds | Re-regen with {constant=value} | Defer}."
  </how-to-verify>
  <resume-signal>Type "approved" (Option A → Plan 06 proceeds) OR "relax K=150" / "relax window=200" / "relax both" (Option B → re-run Task 3 with the new constant, then re-checkpoint) OR "defer" (Option C → abort Wave 2 atomic-cutover; surface to user for milestone-level decision).</resume-signal>
  <verify>
    <human-check>Operator has read `reports/cohort-percentile-cdf-latest.md`, made the ship/relax/defer decision per CONTEXT D-11, and appended the `## Operator Decision` paragraph to the report. The decision unblocks Plan 06.</human-check>
  </verify>
  <done>The regen report contains an `## Operator Decision` paragraph with a clear A/B/C decision and rationale; Plan 06 may proceed (or re-regen runs first, or Wave 2 cutover is aborted).</done>
</task>

<task type="auto">
  <name>Task 6: Pre-PR checklist for Plan 04 — ruff format, ruff check, ty check, pytest</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist"
  </read_first>
  <action>
    Run the four backend gates: `uv run ruff format app/services/global_percentile_cdf.py scripts/gen_global_percentile_cdf.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py`, then `uv run ruff check ... --fix`, then `uv run ty check app/ scripts/ tests/`, then `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` AND `uv run pytest tests/services/ -x` (full services tests; any test that imports `interpolate_percentile` from the legacy registry will fail and surface as a Plan 05 cutover dependency — annotate the failure but do NOT fix in this plan, Plan 05 owns the cutover).
  </action>
  <verify>
    <automated>uv run ruff format --check app/services/global_percentile_cdf.py scripts/gen_global_percentile_cdf.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py && uv run ruff check app/services/global_percentile_cdf.py scripts/gen_global_percentile_cdf.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py && uv run ty check app/ scripts/ tests/ && uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x</automated>
  </verify>
  <acceptance_criteria>
    - ruff format check clean for the touched files.
    - ruff check clean for the touched files.
    - ty check zero errors across `app/`, `scripts/`, `tests/`.
    - `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` passes.
    - Any test failures in the broader `tests/services/` run that reference `interpolate_percentile` or `GLOBAL_PERCENTILE_CDF` are EXPECTED and surface as Plan 05 cutover work (annotate the count + names; do not fix here).
  </acceptance_criteria>
  <done>All Plan 04 gates green; expected cutover failures in `tests/services/` documented for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → benchmark DB | `--target benchmark` requires `_assert_benchmark_db(url)` to verify the URL contains `flawchess_benchmark` AND `:5433` (94.1 Plan 09 mitigation, preserved). |
| Python → Postgres | SQL fragments use `Literal`-typed parameters; `bindparams(:user_id, :snapshot_date)` for runtime values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-04-01 | Tampering | Benchmark CDF script accidentally hitting prod DB | mitigate | `_assert_benchmark_db(url)` requires BOTH `'flawchess_benchmark'` AND `:5433` in the URL. Preserved from 94.1 / 94.3 verbatim. |
| T-94.4-04-02 | Information disclosure | Regen report leaks per-user benchmark data | mitigate | The suppression table contains aggregate counts (n_users, k-th distance) and per-(metric, anchor, TC) flags — NO per-user data. Same discipline as the 94.3 `global-percentile-cdf-latest.md`. |
| T-94.4-04-03 | Tampering | `numpy.percentile` non-determinism across versions | accept | `numpy` is pinned in `pyproject.toml`; the byte-identical regen test (Task 3) catches any drift; pinned version stays for the milestone. |
| T-94.4-04-SC | Tampering | npm/pip installs | accept | No new packages. numpy already in `pyproject.toml`. |
</threat_model>

<verification>
- `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` is green (the byte-identical regen regression passes).
- `app/services/global_percentile_cdf.py` exports `COHORT_PERCENTILE_CDF`, `interpolate_cohort_percentile`, 8-value `CdfMetricId`; does NOT export `GLOBAL_PERCENTILE_CDF` from its module surface (legacy retires).
- `scripts/gen_global_percentile_cdf.py` contains all 5 module-level constants (`grep -c` ≥ 5 across `COHORT_K_USERS_PER_ANCHOR`, `COHORT_MAX_WINDOW_ELO`, `COHORT_ANCHOR_STEP_ELO`, `COHORT_ANCHOR_MIN_ELO`, `COHORT_ANCHOR_MAX_ELO`).
- `reports/cohort-percentile-cdf-latest.md` ships the per-(metric, anchor, TC) suppression-flag table as TOP-LINE per D-11.
- Operator HUMAN-VERIFY checkpoint passed with explicit Option A / B / C decision recorded in the regen report.
- `uv run ty check app/ scripts/ tests/` returns zero errors.
</verification>

<success_criteria>
PRPCR-01 implemented. PRPCR-04 partially implemented (the cohort-CDF artifact + helper ready; Plan 05 wires per-user lookup). `COHORT_PERCENTILE_CDF` replaces `GLOBAL_PERCENTILE_CDF` with shape `Mapping[CdfMetricId, Mapping[tuple[int, TimeControlBucket], CdfTable]]`. 5 module-level constants drive the strict-default sliding-window policy per CONTEXT D-11. `interpolate_cohort_percentile(metric, value, anchor, tc) -> float | None` per CONTEXT D-09a. Regen report surfaces suppression flags as top-line per CONTEXT D-11. Operator HUMAN-VERIFY checkpoint blocks Plan 06 backfill pending review.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-04-SUMMARY.md` when done. Record: per-metric × per-TC suppression coverage summary copied verbatim from the regen report; the operator's decision (Option A / B / C); any constant relaxation applied (with rationale); the snapshot date used; the byte count of `app/services/global_percentile_cdf.py` post-regen (for downstream sanity checks).
</output>
