---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/user_rating_anchors.py
  - app/repositories/user_rating_anchors_repository.py
  - alembic/versions/(NEW migration — add_user_rating_anchors)
  - app/services/canonical_slice_sql.py
  - tests/repositories/test_user_rating_anchors_repository.py
  - tests/services/test_canonical_slice_sql.py
autonomous: true
requirements: [PRPCR-03, PRPCR-04]

must_haves:
  truths:
    - "`app/models/user_rating_anchors.py` exists with `UserRatingAnchor` SQLAlchemy model. PK is composite `(user_id, time_control_bucket)` per D-04 with `user_id` FK to `users.id` ON DELETE CASCADE. Columns: `anchor_rating INTEGER NOT NULL`, `source_platform anchor_source NOT NULL` (new Postgres ENUM with values `lichess` / `chesscom`), `n_games INTEGER NOT NULL`, `computed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. `time_control_bucket` reuses `time_control_bucket_enum` from `app/models/game.py:33` via `create_type=False` import."
    - "A new Postgres ENUM `anchor_source` with exactly two values (`lichess`, `chesscom`) is defined as `SAEnum(\"lichess\", \"chesscom\", name=\"anchor_source\", create_type=False)` in the new model file (per Pattern Map line 75 + CONTEXT D-04). `create_type=False` because Alembic owns lifecycle."
    - "A new Alembic migration `alembic/versions/{TS}_add_user_rating_anchors.py` exists with the three-step upgrade (ENUM create → implicit CAST `CREATE CAST (varchar AS anchor_source) WITH INOUT AS IMPLICIT` → table create) and three-step downgrade (drop table → drop CAST → drop ENUM) mirroring `alembic/versions/20260524_000000_add_user_benchmark_percentiles.py` lines 44-104. Migration cross-references the existing `time_control_bucket` Postgres ENUM via `postgresql.ENUM(name='time_control_bucket', create_type=False)` and does NOT recreate it."
    - "`app/repositories/user_rating_anchors_repository.py` exists with a frozen dataclass `RatingAnchorRow(anchor_rating: int, source_platform: Literal['lichess', 'chesscom'], n_games: int)` + `upsert_anchor(session, *, user_id, time_control_bucket, anchor_rating, source_platform, n_games)` using `pg_insert(...).on_conflict_do_update(index_elements=['user_id', 'time_control_bucket'], ...)` + `fetch_anchors_for_user(session, *, user_id) -> dict[TimeControlBucket, RatingAnchorRow]`. `user_id` is keyword-only per V4 mitigation (PATTERNS line 140 / 813)."
    - "`app/services/canonical_slice_sql.py` gains `per_user_cte_median_anchor(tc, *, source, snapshot_date, platform, min_games)` per PATTERNS lines 296-326 / RESEARCH Pattern 6 lines 622-666. Builder emits a `per_user_anchor(user_id, anchor_rating INT, n_games INT)` CTE via `percentile_cont(0.5) WITHIN GROUP (ORDER BY {_user_elo_at_game_expr()})::int` over the `_recent_capped_per_tc_cte` pool, with a mandatory `WHERE NOT (g.platform = 'chess.com' AND g.time_control_str LIKE '1/%')` Daily-classical drop (RESEARCH Pitfall 11 / Pitfall 2) and an optional `platform` filter (`'lichess' | 'chesscom' | None`)."
    - "Module-level constant `MEDIAN_ANCHOR_MIN_GAMES: Final[int] = 30` defined in `canonical_slice_sql.py` per CLAUDE.md no-magic-numbers rule. Used as the default `min_games` floor for the median-anchor builder."
    - "Migration upgrade runs cleanly against dev (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` + `uv run alembic upgrade head` succeeds, `user_rating_anchors` table appears, `anchor_source` ENUM exists)."
    - "pytest covers repository round-trip: `upsert_anchor` then `fetch_anchors_for_user` returns dict with the inserted row; `source_platform` ENUM round-trips for both `lichess` and `chesscom`; second `upsert_anchor` on same (user_id, tc) updates in-place (PK widening + UPSERT semantics confirmed)."
    - "pytest covers the median-anchor SQL builder: emits `per_user_anchor` CTE; output includes `user_id` + `anchor_rating` + `n_games`; Daily-classical drop WHERE clause is present verbatim for tc='classical'; `platform` filter when supplied restricts the join; `min_games` HAVING clause is parameter-driven."
  artifacts:
    - path: app/models/user_rating_anchors.py
      provides: UserRatingAnchor model + anchor_source Postgres ENUM
      exports: ["UserRatingAnchor", "anchor_source_enum"]
      contains: "class UserRatingAnchor"
      min_lines: 60
    - path: app/repositories/user_rating_anchors_repository.py
      provides: upsert + fetch helpers (V4-safe, user_id keyword-only)
      exports: ["RatingAnchorRow", "upsert_anchor", "fetch_anchors_for_user"]
      min_lines: 80
    - path: alembic/versions/(NEW)
      provides: Alembic migration creating user_rating_anchors + anchor_source ENUM + implicit CAST
      contains: "user_rating_anchors"
      min_lines: 60
    - path: app/services/canonical_slice_sql.py
      provides: per_user_cte_median_anchor SQL builder + MEDIAN_ANCHOR_MIN_GAMES constant
      contains: "per_user_cte_median_anchor"
      min_lines: 800
  key_links:
    - from: app/models/user_rating_anchors.py
      to: alembic/versions/(NEW)
      via: anchor_source Postgres ENUM created by migration, referenced by model with create_type=False
      pattern: "anchor_source"
    - from: app/services/canonical_slice_sql.py::per_user_cte_median_anchor
      to: scripts/gen_global_percentile_cdf.py (Plan 04)
      via: cohort CDF generation joins per_user_values × per_user_anchor by user_id
      pattern: "per_user_cte_median_anchor"
    - from: app/services/canonical_slice_sql.py::per_user_cte_median_anchor
      to: app/services/user_benchmark_percentiles_service.py (Plan 05)
      via: Stage A anchor compute calls the median-anchor SQL to populate user_rating_anchors
      pattern: "per_user_cte_median_anchor"
---

<objective>
Land the `user_rating_anchors` table + model + repository + median-anchor SQL builder in a single parallel-safe plan. Stage A wiring (the consumer of the anchor) lives in Plan 05's atomic-cutover wave — this plan ships the storage primitive + the SQL primitive that Plan 04 (CDF gen) and Plan 05 (service rewrite) both consume.

Purpose: The peer-relative pivot needs a per-(user, TC) rating anchor stored at import time, so the live percentile lookup can look up `COHORT_PERCENTILE_CDF[metric][(anchor, tc)]` deterministically. This plan delivers the storage shape + the SQL that computes the median anchor over the user's recent-1000-per-TC × 36-month pool (RESEARCH Pattern 6, lines 622-666) with the Daily-classical drop baked into the SQL (RESEARCH Pitfall 11). Lichess-precedence (D-12) is implemented in the Python wrapper that calls this builder — that wrapper lives in Plan 05. This plan keeps the SQL platform-parameterised so both call paths (Plan 04 benchmark-side + Plan 05 single-user-side) reuse the same builder.

Output: NEW model file + NEW repository file + NEW Alembic migration + EXTENSION to `canonical_slice_sql.py` adding `per_user_cte_median_anchor` + `MEDIAN_ANCHOR_MIN_GAMES` constant. pytest covers repository CRUD + SQL builder shape. Independent of Plan 01 (conversion module) — both Wave 1 plans are parallel-safe; no shared files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@app/models/user_benchmark_percentile.py
@app/models/game.py
@app/repositories/user_benchmark_percentiles_repository.py
@alembic/versions/20260524_000000_add_user_benchmark_percentiles.py
@app/services/canonical_slice_sql.py
@CLAUDE.md

<interfaces>
<!-- All line numbers verified against current main:
     - app/models/user_benchmark_percentile.py:38-63 (benchmark_metric ENUM descriptor)
     - app/models/user_benchmark_percentile.py:90-106 (composite PK + FK + server-default timestamp)
     - app/models/game.py:33 (time_control_bucket_enum descriptor)
     - app/repositories/user_benchmark_percentiles_repository.py:30-99 (dataclass + UPSERT)
     - app/repositories/user_benchmark_percentiles_repository.py:102-126 (V4 mitigation: user_id keyword-only)
     - alembic/versions/20260524_000000_add_user_benchmark_percentiles.py:31-104 (3-step upgrade/downgrade)
     - app/services/canonical_slice_sql.py:277 (_recent_capped_per_tc_cte helper)
     - app/services/canonical_slice_sql.py:587-666 (per_user_cte_time_pressure_score_gap — analog for new builder docstring + Literal signature) -->

Model signature (analog: user_benchmark_percentile.py:90-106):
  class UserRatingAnchor(Base):
      __tablename__ = "user_rating_anchors"
      user_id: Mapped[int] = mapped_column(
          Integer, ForeignKey("users.id", ondelete="CASCADE"), primary_key=True,
      )
      time_control_bucket: Mapped[TimeControlBucket] = mapped_column(
          time_control_bucket_enum, primary_key=True,
      )
      anchor_rating: Mapped[int] = mapped_column(Integer, nullable=False)
      source_platform: Mapped[AnchorSource] = mapped_column(
          anchor_source_enum, nullable=False,
      )
      n_games: Mapped[int] = mapped_column(Integer, nullable=False)
      computed_at: Mapped[datetime.datetime] = mapped_column(
          DateTime(timezone=True), server_default=func.now(), nullable=False,
      )

ENUM descriptor (analog: user_benchmark_percentile.py:38-63):
  AnchorSource = Literal["lichess", "chesscom"]
  anchor_source_enum = SAEnum(
      "lichess", "chesscom",
      name="anchor_source",
      create_type=False,
  )

Repository signatures (analog: user_benchmark_percentiles_repository.py:30-99):
  @dataclass(frozen=True)
  class RatingAnchorRow:
      anchor_rating: int
      source_platform: AnchorSource
      n_games: int

  async def upsert_anchor(
      session: AsyncSession, *,
      user_id: int,
      time_control_bucket: TimeControlBucket,
      anchor_rating: int,
      source_platform: AnchorSource,
      n_games: int,
  ) -> None: ...

  async def fetch_anchors_for_user(
      session: AsyncSession, *, user_id: int,
  ) -> dict[TimeControlBucket, RatingAnchorRow]: ...

Migration three-step (analog: 20260524_000000_add_user_benchmark_percentiles.py:44-104):
  def upgrade() -> None:
      anchor_source_enum.create(op.get_bind(), checkfirst=True)
      op.execute("CREATE CAST (varchar AS anchor_source) WITH INOUT AS IMPLICIT")
      op.create_table(
          "user_rating_anchors",
          sa.Column("user_id", sa.Integer(), sa.ForeignKey("users.id", ondelete="CASCADE"), nullable=False),
          sa.Column("time_control_bucket", time_control_bucket_enum, nullable=False),
          sa.Column("anchor_rating", sa.Integer(), nullable=False),
          sa.Column("source_platform", anchor_source_enum, nullable=False),
          sa.Column("n_games", sa.Integer(), nullable=False),
          sa.Column("computed_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
          sa.PrimaryKeyConstraint("user_id", "time_control_bucket"),
      )

  def downgrade() -> None:
      op.drop_table("user_rating_anchors")
      op.execute("DROP CAST IF EXISTS (varchar AS anchor_source)")
      anchor_source_enum.drop(op.get_bind(), checkfirst=True)

Median-anchor SQL builder (analog: canonical_slice_sql.py:587-619 per_user_cte_time_pressure_score_gap):
  def per_user_cte_median_anchor(
      tc: TimeControlBucket,
      *,
      source: Literal["benchmark", "single_user"],
      snapshot_date: date | None = None,
      platform: Literal["lichess", "chesscom"] | None = None,
      min_games: int = MEDIAN_ANCHOR_MIN_GAMES,
  ) -> str:
      """Per-(user, TC) median rating anchor over recent-1000-per-TC × 36-month pool.
      Output CTE: per_user_anchor(user_id, anchor_rating INT, n_games INT).
      Drops chess.com Daily games (Pitfall 11).
      """
      _ = source  # cohort difference is in selected_users_cte
      platform_filter = "" if platform is None else f"AND g.platform = '{platform}'"
      return f"""{_recent_capped_per_tc_cte(snapshot_date, tc)},
recent_capped_no_daily AS (
  SELECT rc.* FROM recent_capped rc
  JOIN games g ON g.id = rc.id
  WHERE NOT (g.platform = 'chess.com' AND g.time_control_str LIKE '1/%')
    {platform_filter}
),
per_user_anchor AS (
  SELECT
    user_id,
    percentile_cont(0.5) WITHIN GROUP (ORDER BY {_user_elo_at_game_expr()})::int AS anchor_rating,
    count(*) AS n_games
  FROM recent_capped_no_daily rc
  JOIN games g ON g.id = rc.id
  GROUP BY user_id
  HAVING count(*) >= {min_games}
)"""
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/models/user_rating_anchors.py with UserRatingAnchor model + anchor_source ENUM</name>
  <files>app/models/user_rating_anchors.py</files>
  <read_first>
    - app/models/user_benchmark_percentile.py (in full — exact model analog: composite PK, ENUM descriptor with create_type=False, server-default timestamp, FK with ondelete=CASCADE)
    - app/models/game.py (lines 30-40 — `time_control_bucket_enum` descriptor; need exact import path for cross-import)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 63-99 — pattern assignment for this file)
  </read_first>
  <action>
    Create `app/models/user_rating_anchors.py` mirroring `app/models/user_benchmark_percentile.py` structure verbatim. Imports: `from __future__ import annotations`; `import datetime`; `from typing import Literal`; `from sqlalchemy import Enum as SAEnum, Integer, ForeignKey, DateTime, func`; `from sqlalchemy.orm import Mapped, mapped_column`; `from app.db.base import Base` (or whichever Base class user_benchmark_percentile.py imports — check first); `from app.models.game import time_control_bucket_enum, TimeControlBucket`. Define `AnchorSource = Literal["lichess", "chesscom"]` then `anchor_source_enum = SAEnum("lichess", "chesscom", name="anchor_source", create_type=False)`. Define `UserRatingAnchor(Base)` with `__tablename__ = "user_rating_anchors"` and the 7 columns per the `<interfaces>` block: composite PK on `(user_id, time_control_bucket)`, FK on `user_id → users.id ON DELETE CASCADE`, `anchor_rating INT NOT NULL`, `source_platform anchor_source NOT NULL`, `chesscom_raw_rating INT NULL` (raw chess.com rating BEFORE ChessGoals conversion — populated when source_platform=chesscom, NULL when source_platform=lichess; per D-07 bullet 4), `n_games INT NOT NULL`, `computed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Class docstring opens with: write path (Stage A trigger — wired in Plan 05); suppression semantics (no row → no anchor → chip suppresses); per-(user, TC) precedence rule (Lichess wins per D-04 + D-12; chess.com source means rating was converted via ChessGoals snapshot — see `app/services/chesscom_to_lichess.py`); Daily-classical suppression (RESEARCH Pitfall 11 — chess.com Daily games are bucketed `classical` but the median-anchor builder drops them, so classical anchors may be absent for users who play only Daily — chip suppresses for that user/TC). Export `__all__ = ["UserRatingAnchor", "AnchorSource", "anchor_source_enum"]`. Cite D-04 (table existence) + D-07 (bullet 4 chesscom_raw_rating disclosure) + D-12 (Lichess-precedence rule) in the docstring.
  </action>
  <verify>
    <automated>uv run ty check app/models/user_rating_anchors.py && uv run python -c "from app.models.user_rating_anchors import UserRatingAnchor, anchor_source_enum; print(UserRatingAnchor.__tablename__, anchor_source_enum.name)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class UserRatingAnchor" app/models/user_rating_anchors.py` is ≥ 1.
    - `grep -c "name=\"anchor_source\"" app/models/user_rating_anchors.py` is ≥ 1.
    - `grep -c "create_type=False" app/models/user_rating_anchors.py` is ≥ 1.
    - `grep -c "primary_key=True" app/models/user_rating_anchors.py` is ≥ 2 (composite PK on 2 columns).
    - `grep -c "ondelete=\"CASCADE\"" app/models/user_rating_anchors.py` is ≥ 1.
    - `grep -c "time_control_bucket_enum" app/models/user_rating_anchors.py` is ≥ 1 (cross-import, not re-declare).
    - `grep -c "server_default=func.now()" app/models/user_rating_anchors.py` is ≥ 1.
    - `grep -c "chesscom_raw_rating" app/models/user_rating_anchors.py` is ≥ 1 (D-07 bullet 4 column).
    - Class docstring cites D-04, D-07 (bullet 4), and D-12 + RESEARCH Pitfall 11.
    - `uv run ty check app/models/user_rating_anchors.py` returns zero errors.
  </acceptance_criteria>
  <done>Model file compiles ty-clean; ENUM descriptor + composite PK + FK ondelete=CASCADE + cross-imported time_control_bucket_enum + server-default timestamp all present.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration alembic/versions/{TS}_add_user_rating_anchors.py</name>
  <files>alembic/versions/(NEW)</files>
  <read_first>
    - alembic/versions/20260524_000000_add_user_benchmark_percentiles.py (in full — exact analog: 3-step upgrade with ENUM create + CAST + table; 3-step downgrade in reverse; module-level ENUM descriptor with create_type=False)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 144-176 — migration pattern assignment + three-step upgrade/downgrade requirement)
    - alembic.ini + alembic/env.py (just to confirm migration file naming convention — autogenerate with `uv run alembic revision -m "add user_rating_anchors" --rev-id={generate}` OR manually mimic the `20260524_000000_add_user_benchmark_percentiles.py` timestamp pattern)
  </read_first>
  <action>
    Generate the migration via `uv run alembic revision -m "add_user_rating_anchors"` (or copy the analog file's name pattern). The migration file MUST follow the three-step upgrade per PATTERNS line 158-175: (1) `anchor_source_enum = postgresql.ENUM("lichess", "chesscom", name="anchor_source", create_type=False); anchor_source_enum.create(op.get_bind(), checkfirst=True)` (module-level descriptor + explicit create with checkfirst=True); (2) `op.execute("CREATE CAST (varchar AS anchor_source) WITH INOUT AS IMPLICIT")` (asyncpg compatibility); (3) `op.create_table("user_rating_anchors", ...)` with the 7 columns per `<interfaces>` block (including `chesscom_raw_rating INTEGER NULL` per D-07 bullet 4 — populated only for source_platform=chesscom rows). Cross-import the existing `time_control_bucket` Postgres ENUM via `postgresql.ENUM(name="time_control_bucket", create_type=False)` and do NOT recreate it (it was created by an earlier migration). Add `sa.PrimaryKeyConstraint("user_id", "time_control_bucket")`. Three-step downgrade in reverse order: drop_table → DROP CAST → ENUM.drop(checkfirst=True). Set `down_revision` to the current head (run `uv run alembic heads` to confirm). Module docstring opens with: "Add user_rating_anchors table per Phase 94.4 D-04. Stores per-(user, TC) rating anchors populated by Stage A at import time. Lichess-precedence rule (D-12) lives in the Python wrapper (Plan 05), not in this migration." Save with the standard alembic versioning timestamp prefix.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run python -c "from sqlalchemy import create_engine, text; import os; eng = create_engine(os.environ['DATABASE_URL'].replace('+asyncpg','')); print(list(eng.execute(text('SELECT 1 FROM user_rating_anchors LIMIT 0')))); print(list(eng.execute(text(\"SELECT unnest(enum_range(NULL::anchor_source))\"))))" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists in `alembic/versions/` matching glob `*_add_user_rating_anchors.py`.
    - `grep -c "anchor_source_enum.create(op.get_bind(), checkfirst=True)" alembic/versions/*_add_user_rating_anchors.py` is ≥ 1.
    - `grep -c "CREATE CAST (varchar AS anchor_source) WITH INOUT AS IMPLICIT" alembic/versions/*_add_user_rating_anchors.py` is ≥ 1.
    - `grep -c "name=\"time_control_bucket\".*create_type=False\|postgresql.ENUM(name=\"time_control_bucket\"" alembic/versions/*_add_user_rating_anchors.py` is ≥ 1.
    - `grep -c "DROP CAST IF EXISTS (varchar AS anchor_source)" alembic/versions/*_add_user_rating_anchors.py` is ≥ 1.
    - `grep -c "anchor_source_enum.drop(op.get_bind(), checkfirst=True)" alembic/versions/*_add_user_rating_anchors.py` is ≥ 1.
    - `grep -c "chesscom_raw_rating" alembic/versions/*_add_user_rating_anchors.py` is ≥ 1 (D-07 bullet 4 column present).
    - `uv run alembic upgrade head` succeeds against dev DB (table appears, ENUM appears).
    - `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips cleanly (downgrade is functional, not no-op).
  </acceptance_criteria>
  <done>Migration upgrades and downgrades cleanly; user_rating_anchors table exists with composite PK, anchor_source ENUM, implicit CAST.</done>
</task>

<task type="auto">
  <name>Task 3: Create app/repositories/user_rating_anchors_repository.py with upsert + fetch helpers</name>
  <files>app/repositories/user_rating_anchors_repository.py</files>
  <read_first>
    - app/repositories/user_benchmark_percentiles_repository.py (in full — exact analog: frozen dataclass PercentileRow, upsert_percentile with on_conflict_do_update, fetch_for_user, V4 mitigation comments at lines 102-126)
    - app/models/user_rating_anchors.py (just created in Task 1)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 102-140 — pattern assignment + V4 mitigation requirement)
  </read_first>
  <action>
    Create `app/repositories/user_rating_anchors_repository.py` mirroring `app/repositories/user_benchmark_percentiles_repository.py` verbatim in structure. Imports: `from __future__ import annotations`; `from dataclasses import dataclass`; `from sqlalchemy.dialects.postgresql import insert as pg_insert`; `from sqlalchemy import select, func`; `from sqlalchemy.ext.asyncio import AsyncSession`; `from app.models.user_rating_anchors import UserRatingAnchor, AnchorSource`; `from app.models.game import TimeControlBucket`. Define `@dataclass(frozen=True) class RatingAnchorRow` with four fields: `anchor_rating: int`, `source_platform: AnchorSource`, `chesscom_raw_rating: int | None`, `n_games: int`. Implement `async def upsert_anchor(session: AsyncSession, *, user_id: int, time_control_bucket: TimeControlBucket, anchor_rating: int, source_platform: AnchorSource, chesscom_raw_rating: int | None, n_games: int) -> None` using `pg_insert(UserRatingAnchor).values(...)` + `.on_conflict_do_update(index_elements=["user_id", "time_control_bucket"], set_={"anchor_rating": stmt.excluded.anchor_rating, "source_platform": stmt.excluded.source_platform, "chesscom_raw_rating": stmt.excluded.chesscom_raw_rating, "n_games": stmt.excluded.n_games, "computed_at": func.now()})` then `await session.execute(stmt)`. Implement `async def fetch_anchors_for_user(session: AsyncSession, *, user_id: int) -> dict[TimeControlBucket, RatingAnchorRow]` selecting all rows where `user_id == :user_id` and returning a dict keyed by `time_control_bucket`. Both functions take `user_id` as keyword-only per V4 mitigation — mirror the docstring at user_benchmark_percentiles_repository.py:102-126 verbatim: "`user_id` MUST come from the authenticated `current_user.id` dep; never accept as a query/path parameter (V4 Information Disclosure)." Cite PATTERNS line 813 in the docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_user_rating_anchors_repository.py -x 2>&1 | tail -5 || echo "Test file created in Task 4"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@dataclass(frozen=True)" app/repositories/user_rating_anchors_repository.py` is ≥ 1.
    - `grep -c "class RatingAnchorRow" app/repositories/user_rating_anchors_repository.py` is ≥ 1.
    - `grep -c "async def upsert_anchor\|async def fetch_anchors_for_user" app/repositories/user_rating_anchors_repository.py` is ≥ 2.
    - `grep -c "index_elements=\[\"user_id\", \"time_control_bucket\"\]" app/repositories/user_rating_anchors_repository.py` is ≥ 1.
    - `grep -c "user_id: int," app/repositories/user_rating_anchors_repository.py | grep -v '^#'` — `user_id` appears keyword-only (after `*,`) in both signatures.
    - V4 mitigation docstring cites "V4" + keyword-only `user_id`.
    - `uv run ty check app/repositories/user_rating_anchors_repository.py` returns zero errors.
  </acceptance_criteria>
  <done>Repository file compiles ty-clean; dataclass + UPSERT helper + fetch helper present; V4 mitigation preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write tests/repositories/test_user_rating_anchors_repository.py covering UPSERT round-trip + ENUM serialization</name>
  <files>tests/repositories/test_user_rating_anchors_repository.py</files>
  <read_first>
    - tests/repositories/test_user_benchmark_percentiles_repository.py (in full — exact analog test patterns for UPSERT, fetch dict shape, ENUM round-trip; fixture usage for `async_session` from conftest)
    - app/repositories/user_rating_anchors_repository.py (just created in Task 3)
    - tests/conftest.py (skim — confirm `async_session` fixture name + user-creation fixture; mirror what user_benchmark_percentiles_repository tests use)
  </read_first>
  <behavior>
    - Test 1: `upsert_anchor` inserts a new row; subsequent `fetch_anchors_for_user` returns dict with the inserted RatingAnchorRow.
    - Test 2: Second `upsert_anchor` for the same `(user_id, time_control_bucket)` updates in-place (PK widening works; `n_games` and `anchor_rating` update; `computed_at` advances).
    - Test 3: `source_platform='lichess'` round-trips correctly through the anchor_source ENUM.
    - Test 4: `source_platform='chesscom'` round-trips correctly through the anchor_source ENUM.
    - Test 5: `fetch_anchors_for_user` returns up to 4 rows in a single dict — one per TC the user has anchors for; missing TCs are absent from the dict (no `None` placeholders).
    - Test 6: `fetch_anchors_for_user` returns `{}` (empty dict) when the user has no anchors.
    - Test 7: Inserting a duplicate `(user_id, time_control_bucket)` with `source_platform='lichess'` then `source_platform='chesscom'` correctly updates the source_platform via the UPSERT.
    - Test 8: `chesscom_raw_rating=1830` round-trips correctly when `source_platform='chesscom'`; `chesscom_raw_rating=None` round-trips when `source_platform='lichess'` (D-07 bullet 4 tooltip-disclosure source).
  </behavior>
  <action>
    Create `tests/repositories/test_user_rating_anchors_repository.py` mirroring `tests/repositories/test_user_benchmark_percentiles_repository.py` structure. Use the same `async_session` + user-creation fixtures (check conftest first; if there's a `make_user` factory or similar, reuse it). Cover the 7 behavior cases above. Each test follows the pattern: `async def test_<behavior>(async_session): user_id = await _make_user(async_session); await upsert_anchor(async_session, user_id=user_id, time_control_bucket='rapid', anchor_rating=1600, source_platform='lichess', n_games=500); result = await fetch_anchors_for_user(async_session, user_id=user_id); assert result['rapid'] == RatingAnchorRow(anchor_rating=1600, source_platform='lichess', n_games=500)`. For Test 2 (in-place update), insert then re-insert with different values and assert `fetch_anchors_for_user` returns the second-insert values. For Test 5 (multi-TC), insert 4 anchors for one user across 4 TCs, assert dict has 4 keys. Type all fixtures/params per CLAUDE.md.
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_user_rating_anchors_repository.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 8 behavior cases pass.
    - `grep -c "def test_" tests/repositories/test_user_rating_anchors_repository.py` is ≥ 8.
    - At least one test asserts the in-place UPSERT (Test 2) — `grep "computed_at\|RatingAnchorRow" tests/repositories/test_user_rating_anchors_repository.py` finds matches.
    - Both ENUM values exercised: `grep -c "source_platform='lichess'\|source_platform='chesscom'\|source_platform=\"lichess\"\|source_platform=\"chesscom\"" tests/repositories/test_user_rating_anchors_repository.py` is ≥ 2.
    - `uv run ty check tests/repositories/test_user_rating_anchors_repository.py` returns zero errors.
  </acceptance_criteria>
  <done>All 7 cases green; UPSERT in-place + ENUM round-trip + multi-TC dict shape + empty-dict-on-no-rows all verified.</done>
</task>

<task type="auto">
  <name>Task 5: Extend app/services/canonical_slice_sql.py with per_user_cte_median_anchor builder + MEDIAN_ANCHOR_MIN_GAMES constant</name>
  <files>app/services/canonical_slice_sql.py</files>
  <read_first>
    - app/services/canonical_slice_sql.py (in full — read lines 1-280 for imports + constants + helpers; lines 277-365 for `_recent_capped_per_tc_cte` + section2 builder pattern; lines 587-666 for `per_user_cte_time_pressure_score_gap` — the closest analog for the new builder docstring + Literal-parameter security guarantee)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 252-334 — pattern assignment for the new median-anchor builder, including the verbatim SQL skeleton from RESEARCH Pattern 6 lines 622-666)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pattern 6 lines 622-666 for the full SQL + Pitfall 11 lines 1307-1316 for the Daily-classical drop justification + Pitfall 2 lines 1178-1188 for the LIKE '1/%' pattern)
  </read_first>
  <action>
    Add to `app/services/canonical_slice_sql.py` (do NOT touch the existing per-TC builders in this plan — Plan 03 owns the `per_user_values` widening for Pitfall 1). At module-level (near the existing constants — find them around line 50-100), add `MEDIAN_ANCHOR_MIN_GAMES: Final[int] = 30` with a comment citing CLAUDE.md no-magic-numbers + the rationale (30 games is the lower floor for a stable median; tighter floors should be planner-tuned post-Plan-04 regen report). Then append the `per_user_cte_median_anchor` function (place it near the existing per-TC builders for code locality — after `per_user_cte_time_pressure_score_gap` around line 666, or wherever the per-TC builder family lives). Signature exactly as in `<interfaces>` block: `def per_user_cte_median_anchor(tc: TimeControlBucket, *, source: Literal["benchmark", "single_user"], snapshot_date: date | None = None, platform: Literal["lichess", "chesscom"] | None = None, min_games: int = MEDIAN_ANCHOR_MIN_GAMES) -> str:`. Docstring mirrors `per_user_cte_time_pressure_score_gap` (lines 588-617) in structure: explain output CTE (`per_user_anchor(user_id, anchor_rating INT, n_games INT)`); explain restricted pool (`_recent_capped_per_tc_cte` per TC); explain Daily-classical drop (RESEARCH Pitfall 11 — `g.platform = 'chess.com' AND g.time_control_str LIKE '1/%'`); explain Lichess-precedence note (the policy lives in the Python wrapper in Plan 05; this builder takes platform as a parameter); explain security guarantee (`tc` and `platform` are `Literal` parameters so f-string interpolation is safe — 4-value × 3-value matrix of known strings). Body follows PATTERNS lines 310-326 verbatim — the `recent_capped_no_daily` intermediate CTE applies the Daily WHERE and optional platform filter, then `per_user_anchor` applies `percentile_cont(0.5) WITHIN GROUP (ORDER BY {_user_elo_at_game_expr()})::int AS anchor_rating` + `count(*) AS n_games` + `HAVING count(*) >= {min_games}`. Use the form `LIKE '1/%'` (single `%`) as the locked convention for this file. The `canonical_slice_sql.py` builders return raw f-string SQL that is later passed verbatim to asyncpg via SQLAlchemy `text()` without `%`-style parameter interpolation (`bindparams` use `:name` syntax), so the `%` in `'1/%'` is not consumed by Python `%` formatting — no `%%` doubling needed. Add this convention to the builder docstring. `_ = source` discard line matches analog. Module imports may need updating: ensure `Literal` is already imported (search for `from typing import` block) and add `Final` to the import if missing. Do not edit any existing function in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x -k median_anchor 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def per_user_cte_median_anchor" app/services/canonical_slice_sql.py` is ≥ 1.
    - `grep -c "MEDIAN_ANCHOR_MIN_GAMES" app/services/canonical_slice_sql.py` is ≥ 2 (constant declaration + use as default).
    - `grep -c "platform: Literal\[\"lichess\", \"chesscom\"\] \| None" app/services/canonical_slice_sql.py` is ≥ 1.
    - `grep -c "time_control_str LIKE '1/%'" app/services/canonical_slice_sql.py | grep -v '^#'` is ≥ 1 (Daily-classical drop SQL is present with the locked single-`%` form).
    - `grep -c "percentile_cont(0.5) WITHIN GROUP" app/services/canonical_slice_sql.py | grep -v '^#'` is ≥ 1.
    - `grep -c "per_user_anchor AS" app/services/canonical_slice_sql.py | grep -v '^#'` is ≥ 1.
    - Builder docstring cites RESEARCH Pitfall 11 and CONTEXT D-04 / D-12.
    - `uv run ty check app/services/canonical_slice_sql.py` returns zero errors.
  </acceptance_criteria>
  <done>Median-anchor builder + MEDIAN_ANCHOR_MIN_GAMES constant present; ty-clean; existing per-TC builders untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Extend tests/services/test_canonical_slice_sql.py with per_user_cte_median_anchor unit tests</name>
  <files>tests/services/test_canonical_slice_sql.py</files>
  <read_first>
    - tests/services/test_canonical_slice_sql.py (in full — existing per-TC builder tests for Phase 94.3 Time Pressure family; pattern: assert generated SQL contains expected fragments like `per_user_values AS`, builder is called with each of the 4 TCs)
    - app/services/canonical_slice_sql.py (just modified in Task 5)
  </read_first>
  <behavior>
    - Test 1 (parametrized over 4 TCs): `per_user_cte_median_anchor(tc, source='benchmark')` emits a `per_user_anchor` CTE.
    - Test 2: Output includes `percentile_cont(0.5) WITHIN GROUP` (median aggregate).
    - Test 3: Output includes the Daily-classical drop WHERE clause (`g.platform = 'chess.com'` + `time_control_str LIKE '1/%'`) for ALL 4 TCs (the drop is unconditional on the TC parameter — Daily can be miscategorized as classical but the drop runs everywhere for symmetry).
    - Test 4: `platform='lichess'` produces a `g.platform = 'lichess'` filter; `platform=None` produces no platform filter.
    - Test 5: Default `min_games` is sourced from the `MEDIAN_ANCHOR_MIN_GAMES` constant; explicit override via `min_games=50` puts `>= 50` in the HAVING clause.
    - Test 6: Output projects `user_id`, `anchor_rating`, `n_games` (the 3 columns the JOIN against `per_user_values` needs).
  </behavior>
  <action>
    Append new test cases to `tests/services/test_canonical_slice_sql.py`. Mirror the per-TC builder test pattern from Phase 94.3 (search the file for `per_user_cte_time_pressure_score_gap` test cases to find the analog). Use `pytest.mark.parametrize("tc", ["bullet", "blitz", "rapid", "classical"])` for the 4-TC sweep. For each behavior case, the test calls the builder and asserts on substrings of the generated SQL string. Example:
    ```
    @pytest.mark.parametrize("tc", ["bullet", "blitz", "rapid", "classical"])
    def test_median_anchor_emits_cte_per_tc(tc):
        sql = per_user_cte_median_anchor(tc, source="benchmark")
        assert "per_user_anchor AS" in sql
        assert "percentile_cont(0.5)" in sql
        assert "WITHIN GROUP" in sql
    ```
    For Test 4: `assert "g.platform = 'lichess'" in per_user_cte_median_anchor("rapid", source="benchmark", platform="lichess")` and `assert "g.platform = 'lichess'" not in per_user_cte_median_anchor("rapid", source="benchmark", platform=None)`. For Test 5: `assert ">= 30" in per_user_cte_median_anchor("rapid", source="benchmark")` (default 30) and `assert ">= 50" in per_user_cte_median_anchor("rapid", source="benchmark", min_games=50)`. These are SQL-string assertions, fast, no DB needed.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x -v -k median_anchor</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behavior cases pass (with 4-TC parametrize multipliers where applicable).
    - `grep -c "def test_median_anchor\|def test_per_user_cte_median_anchor" tests/services/test_canonical_slice_sql.py` is ≥ 4.
    - At least one parametrized 4-TC test asserts the Daily-classical drop substring `time_control_str LIKE` is present in all 4 TC outputs.
    - `min_games` override and default-from-constant cases both covered.
  </acceptance_criteria>
  <done>6 behavior cases green; per_user_cte_median_anchor verified across 4 TCs, both platform values + None, default + override min_games.</done>
</task>

<task type="auto">
  <name>Task 7: Pre-PR checklist for Plan 02 — ruff format, ruff check, ty check, pytest</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist" (the mandatory gate list)
  </read_first>
  <action>
    Run the four backend gates locally per CLAUDE.md pre-PR discipline against the files this plan touched: `uv run ruff format app/models/user_rating_anchors.py app/repositories/user_rating_anchors_repository.py app/services/canonical_slice_sql.py alembic/versions/*_add_user_rating_anchors.py tests/repositories/test_user_rating_anchors_repository.py tests/services/test_canonical_slice_sql.py`, then `uv run ruff check ... --fix` on the same paths, then `uv run ty check app/ tests/` (full backend ty check because canonical_slice_sql.py is imported widely; ty must be zero errors), then `uv run pytest tests/repositories/test_user_rating_anchors_repository.py tests/services/test_canonical_slice_sql.py -x`. If ruff format modifies files, commit under `style(94.4-02):` prefix. If ty reports any error outside the new files, surface it as a blocker (do not fix unrelated drift in this plan). If pytest fails, fix it.
  </action>
  <verify>
    <automated>uv run ruff format --check app/models/user_rating_anchors.py app/repositories/user_rating_anchors_repository.py app/services/canonical_slice_sql.py alembic/versions/*_add_user_rating_anchors.py tests/repositories/test_user_rating_anchors_repository.py tests/services/test_canonical_slice_sql.py && uv run ruff check app/models/user_rating_anchors.py app/repositories/user_rating_anchors_repository.py app/services/canonical_slice_sql.py alembic/versions/*_add_user_rating_anchors.py tests/repositories/test_user_rating_anchors_repository.py tests/services/test_canonical_slice_sql.py && uv run ty check app/ tests/ && uv run pytest tests/repositories/test_user_rating_anchors_repository.py tests/services/test_canonical_slice_sql.py -x</automated>
  </verify>
  <acceptance_criteria>
    - ruff format check clean.
    - ruff check clean.
    - ty check zero errors across `app/` and `tests/`.
    - pytest passes all cases in the two test files.
  </acceptance_criteria>
  <done>All gates green; Plan 02 ready for the Wave 2 atomic-cutover PR to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → repository | Authenticated `current_user.id` from FastAPI-Users dep is the sole `user_id` source; never accepted as a query/path parameter (V4 mitigation, preserved from Phase 94.1). |
| Python → Postgres | All SQL fragments contain STATIC SQL + named bindparams (`:user_id`, `:snapshot_date`) + Python-typed `Literal` parameters for f-string interpolation; no user-supplied string crosses into SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-02-01 | Tampering | SQL injection via `tc` parameter on `per_user_cte_median_anchor` | mitigate | `tc: TimeControlBucket = Literal["bullet", "blitz", "rapid", "classical"]` — ty + Pydantic enforce 4-value set. F-string into SQL is safe (only 4 known strings can interpolate). Documented in builder docstring. |
| T-94.4-02-02 | Tampering | SQL injection via `platform` parameter | mitigate | `platform: Literal["lichess", "chesscom"] \| None` — same Literal discipline. |
| T-94.4-02-03 | Information disclosure | `user_id` accepted from query/path parameter | mitigate | V4 mitigation preserved verbatim from Phase 94.1: `user_id` keyword-only in both repository helpers; sourced from `current_user.id` dep at the API layer. |
| T-94.4-02-04 | Tampering | ENUM value injection via API | mitigate | `source_platform` ENUM has exactly 2 values; no API surface accepts arbitrary strings — backend code produces all values. |
| T-94.4-02-SC | Tampering | npm/pip installs | accept | No new package installs. SQLAlchemy, asyncpg, alembic, pytest already in `pyproject.toml`. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `uv run alembic upgrade head` runs cleanly on dev; `user_rating_anchors` table exists with composite PK (user_id, time_control_bucket); `anchor_source` Postgres ENUM exists with values (`lichess`, `chesscom`); implicit varchar→anchor_source CAST exists.
- `uv run pytest tests/repositories/test_user_rating_anchors_repository.py tests/services/test_canonical_slice_sql.py -x` is green (≥ 13 new cases between the two files).
- `uv run ty check app/ tests/` reports zero errors.
- `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips the new migration cleanly.
- `per_user_cte_median_anchor` builder emits the expected CTE shape; SQL contains `percentile_cont(0.5)`, `time_control_str LIKE '1/%'` (Daily drop), `HAVING count(*) >= 30` (default min_games).
</verification>

<success_criteria>
PRPCR-03 partially implemented (storage primitive ready; Stage A wiring lands in Plan 05). PRPCR-04 partially implemented (median-anchor SQL builder ready; cohort-CDF helper + per-TC ΔES builders + JOIN land in Plans 03/04/05). `user_rating_anchors` table exists in dev with PK `(user_id, time_control_bucket)` and `anchor_source` ENUM per D-04. `per_user_cte_median_anchor` builder drops chess.com Daily classical games per RESEARCH Pitfall 11. Repository helpers preserve V4 mitigation. Both files independent of Plan 01 (Wave 1 parallel-safe).
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-02-SUMMARY.md` when done. Record: migration revision ID + parent revision; verified `psql -c '\d user_rating_anchors'` output snippet; confirm the Daily-classical drop substring is in the median-anchor SQL output (paste a sample of the generated SQL); list the test count and runtime.
</output>
