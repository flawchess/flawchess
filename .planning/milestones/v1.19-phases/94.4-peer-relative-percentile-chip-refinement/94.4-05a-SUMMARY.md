---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 05a
subsystem: storage
tags: [migration, alembic, postgres-enum, schema-reshape, user-benchmark-percentiles]
requires:
  - 94.4-02 (user_rating_anchors migration; provided previous alembic head fc000b5d0134)
  - 94.4-03 (PercentileChip cleanup; orthogonal)
  - 94.4-04 (legacy interpolate_percentile stub; dormant during this plan)
provides:
  - 8-value benchmark_metric ENUM per CONTEXT D-13
  - user_benchmark_percentiles with widened (user_id, metric, time_control_bucket) PK
  - PercentileRow with n_games field per CONTEXT D-08
  - fetch_for_user returning dict[CdfMetricId, dict[TimeControlBucket, PercentileRow]]
  - upsert_percentile signature widened with time_control_bucket keyword param
affects:
  - 94.4-05b (consumes nested-dict fetch + 3-column PK upsert)
  - 94.4-05c (consumes PercentileRow.n_games in API shaper aggregator)
  - 94.4-06 (backfill writes against post-reshape table)
tech-stack:
  added: []
  patterns:
    - Drop/recreate Postgres ENUM via DROP TYPE ... CASCADE per RESEARCH Pattern 3
    - Forward-only destructive migration with no-op downgrade stub per CONTEXT D-02
    - SQLAlchemy ENUM cross-import (create_type=False) following game.py:33 precedent
key-files:
  created:
    - alembic/versions/20260526_222651_1945ae56aa20_reshape_user_benchmark_percentiles.py
  modified:
    - app/models/user_benchmark_percentile.py
    - app/repositories/user_benchmark_percentiles_repository.py
decisions:
  - "Destructive reshape over additive ADD VALUE: keeps ENUM at 8 logical values rather than carrying 16 legacy + 8 new + dead-row migration; legacy data is empty in dev and non-critical in prod (D-02)."
  - "downgrade() is a no-op stub: backfill is the canonical recovery path; storing prior-shape data for a reversible migration would not be justified for a derived surface (D-02)."
metrics:
  duration: ~25 min
  completed_date: 2026-05-27
  tasks_completed: 4
  files_created: 1
  files_modified: 2
---

# Phase 94.4 Plan 05a: User Benchmark Percentiles Schema Reshape Summary

## One-liner

Destructively reshapes `user_benchmark_percentiles` + `benchmark_metric` ENUM: PK widens to `(user_id, metric, time_control_bucket)`, ENUM collapses from 16 → 8 values (rescuing `recovery_score_gap` per D-05a), and `PercentileRow` gains `n_games` for the Plan 05c API shaper.

## What was built

This is the first plan in the Wave 2 atomic-cutover cluster (05a → 05b → 05c, D-10a). It ships the schema primitive only; service rewrite lands in 05b and API shaper + frontend in 05c. The three plans land together in one squash-merged PR — there is no intermediate deploy.

### 1. Destructive Alembic migration

`alembic/versions/20260526_222651_1945ae56aa20_reshape_user_benchmark_percentiles.py`

- Module docstring opens with a `DESTRUCTIVE — backfill required` banner per RESEARCH Pitfall 7.
- `upgrade()` follows the PATTERNS lines 183-213 drop/recreate recipe verbatim:
  1. `op.drop_table("user_benchmark_percentiles")`
  2. `op.execute("DROP CAST IF EXISTS (varchar AS benchmark_metric)")`
  3. `op.execute("DROP TYPE IF EXISTS benchmark_metric CASCADE")` (CASCADE is defensive)
  4. Module-level `postgresql.ENUM(...create_type=False)` descriptor with exactly 8 values per D-13
  5. `new_benchmark_metric.create(op.get_bind(), checkfirst=True)`
  6. `op.execute("CREATE CAST (varchar AS benchmark_metric) WITH INOUT AS IMPLICIT")` (asyncpg compatibility)
  7. `op.create_table(...)` with the 8-column shape + `PrimaryKeyConstraint("user_id", "metric", "time_control_bucket")`
- The `time_control_bucket` Postgres ENUM (name `timecontrolbucket`) is cross-referenced with `create_type=False`; an earlier migration owns its lifecycle.
- `downgrade()` is a no-op stub: forward-only per D-02; backfill is recovery.

**Revision graph:**
- Revision ID: `1945ae56aa20`
- Parent: `fc000b5d0134` (94.4-02 `add_user_rating_anchors`)

### 2. Model reshape (`app/models/user_benchmark_percentile.py`)

- `benchmark_metric_enum = SAEnum(...)` reshapes from 16 → 8 values per D-13. The 12 composite TC-suffixed values are deleted; per-TC dimensionality now lives in the new PK column. `recovery_score_gap` is rescued from the v1 drop list (D-05a) and joins the ENUM.
- PK widens by adding `time_control_bucket: Mapped[TimeControlBucket]` (PK). Cross-imports `time_control_bucket_enum` from `app.models.game` and the `TimeControlBucket` Literal alias from `app.models.user_rating_anchors`.
- Module + class docstrings cite D-01 (PK widening) + D-13 (8-value ENUM) + D-05a (Recovery rescue).
- `UserBenchmarkPercentile.__table__.primary_key.columns` confirmed to be `('user_id', 'metric', 'time_control_bucket')`.

### 3. Repository extension (`app/repositories/user_benchmark_percentiles_repository.py`)

- `PercentileRow` dataclass gains `n_games: int` (RESEARCH Pattern 9 / D-08) so the Plan 05c API shaper can compute the game-count-weighted mean across per-TC percentiles.
- `upsert_percentile` adds `time_control_bucket: TimeControlBucket` keyword param; `pg_insert(...).values(...)` includes `time_control_bucket`; `on_conflict_do_update(index_elements=["user_id", "metric", "time_control_bucket"], ...)` widens to 3 columns.
- `fetch_for_user` return shape changes to `dict[CdfMetricId, dict[TimeControlBucket, PercentileRow]]` (RESEARCH Pattern 5 Option B). Build pattern: `out.setdefault(row.metric, {})[row.time_control_bucket] = PercentileRow(...)`.
- V4 mitigation preserved verbatim: `user_id` is keyword-only on both `upsert_percentile` and `fetch_for_user`.

## Verification

### Migration round-trip

- `uv run alembic upgrade head`: clean upgrade from `fc000b5d0134` → `1945ae56aa20` on dev DB.
- `uv run alembic downgrade -1`: succeeds (downgrade is a no-op stub; alembic just rewinds version_num).
- `uv run alembic upgrade head` again: re-applies cleanly.

### Postgres state post-upgrade

```
psql> SELECT unnest(enum_range(NULL::benchmark_metric));
          unnest
---------------------------
 score_gap
 achievable_score_gap
 section2_score_gap_conv
 section2_score_gap_parity
 recovery_score_gap
 time_pressure_score_gap
 clock_gap
 net_flag_rate
(8 rows)
```

```
psql> \d user_benchmark_percentiles
       Column        |           Type           | Nullable
---------------------+--------------------------+----------
 user_id             | integer                  | not null
 metric              | benchmark_metric         | not null
 time_control_bucket | timecontrolbucket        | not null
 value               | double precision         | not null
 n_games             | integer                  | not null
 percentile          | double precision         |
 cdf_snapshot        | date                     | not null
 computed_at         | timestamp with time zone | not null
Indexes:
    "user_benchmark_percentiles_pkey" PRIMARY KEY, btree (user_id, metric, time_control_bucket)
Foreign-key constraints:
    "user_benchmark_percentiles_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
```

### Lint / type / format

- `uv run ruff format --check` clean across all three touched files.
- `uv run ruff check` clean.
- `uv run ty check app/models/ app/repositories/ alembic/` returns zero errors.

### Full pytest

DEFERRED to Plan 05c per the plan's Task 4: in this intermediate state, `app/services/user_benchmark_percentiles_service.py` still references the legacy 16-value ENUM and the 2-column UPSERT signature; running pytest now would report expected failures that 05b resolves. The 05a → 05b → 05c cluster lands in one squash-merged PR, so the intermediate failures never appear on `main`.

## Deviations from Plan

None — plan executed exactly as written. The migration recipe in PATTERNS (lines 183-213) was applied verbatim, the model reshape matches PATTERNS lines 429-456, and the repository extension matches PATTERNS lines 460-501. No auto-fixes or scope changes.

## Commits

| Task | Description | Commit |
| ---- | ----------- | ------ |
| 1 | Destructive reshape migration (NEW) | `9224d383` |
| 2 | Model reshape (8-value ENUM + 3-column PK) | `3f94fb7d` |
| 3 | Repository widening (n_games, 3-col PK, nested fetch) | `78392339` |
| 4 | Pre-PR intermediate check (verification only, no commit) | — |

## Threat surface

No new surface introduced beyond what's already in the plan's `<threat_model>`. Specifically:

- T-94.4-05a-01 (destructive migration on prod loses data): accepted per D-02; recovery via Plan 06 backfill.
- T-94.4-05a-02 (ENUM injection): mitigated — all 8 values produced by backend code; no API surface accepts arbitrary metric strings.
- T-94.4-05a-03 (info disclosure via user_id parameter): mitigated — V4 keyword-only guard preserved verbatim on both `upsert_percentile` and `fetch_for_user`.

## Known stubs

None. The intermediate dormant state of `app/services/user_benchmark_percentiles_service.py` (still on the legacy ENUM) is by design — it's the next plan's task (05b) and the atomic-cutover discipline (D-10a) ensures the three plans land together.

## Self-Check: PASSED

- `alembic/versions/20260526_222651_1945ae56aa20_reshape_user_benchmark_percentiles.py` FOUND
- `app/models/user_benchmark_percentile.py` FOUND (modified)
- `app/repositories/user_benchmark_percentiles_repository.py` FOUND (modified)
- Commit `9224d383` FOUND on branch
- Commit `3f94fb7d` FOUND on branch
- Commit `78392339` FOUND on branch
