---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 05c
type: execute
wave: 2
depends_on: [94.4-05a, 94.4-05b]
files_modified:
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - frontend/src/types/endgames.ts
  - tests/services/test_endgame_service.py
autonomous: true
requirements: [PRPCR-04, PRPCR-05]

must_haves:
  truths:
    - "`app/services/endgame_service.py` adds `_aggregate_per_tc_percentile(per_tc_rows: Mapping[TimeControlBucket, PercentileRow] | None) -> float | None` helper per CONTEXT D-08 / D-08b + PATTERNS lines 511-532. Game-count-weighted mean across per-TC rows; below-floor TCs drop (no row → implicit zero weight per D-08a). Call sites: `_compute_score_gap_material` + the equivalent per-metric API shapers for Achievable / Parity / Conversion / Recovery all wrap their `fetch_for_user` results with `_aggregate_per_tc_percentile` for the page-level chip scalar."
    - "Time Pressure family chips bypass aggregation per D-08: `_compute_time_pressure_cards` reads per-TC directly via `rows.get('time_pressure_score_gap', {}).get(tc)` (same for `clock_gap` and `net_flag_rate`)."
    - "`app/schemas/endgames.py` adds `RatingAnchorOut(anchor_rating: int, source_platform: Literal['lichess', 'chesscom'], chesscom_raw_rating: int | None = None, n_games: int)` and a top-level `rating_anchors: dict[TimeControlBucket, RatingAnchorOut] = Field(default_factory=dict)` field on `EndgameOverviewResponse` per RESEARCH Open Question 4. `chesscom_raw_rating` populates from the new column on `user_rating_anchors` (Plan 02 widening per B3) when source_platform=chesscom; tooltip renders inline per D-07 bullet 4."
    - "Frontend type codegen runs after schema changes — `frontend/src/types/endgames.ts` regenerated; verified that existing `*_percentile` nullable field types stay shape-compatible (RESEARCH Pitfall 10) so Plan 07 has zero merge conflicts on the type file. The new TS interface `RatingAnchorOut` carries `chesscom_raw_rating: number | null`."
    - "pytest coverage in `tests/services/test_endgame_service.py` extends with `_aggregate_per_tc_percentile` tests: weighted-mean correctness (3 rows with percentiles 20/40/60 and n_games 30/20/50 → weighted mean = (20*30 + 40*20 + 60*50) / 100 = 44.0); empty dict returns None; all-None-percentile rows return None; single-row dict returns that row's percentile unchanged."
    - "Full pre-PR sweep runs in the terminal task: `uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -x && ( cd frontend && npm run lint && npm test -- --run )` — this is the atomic-cutover linchpin gate. After Plan 05c lands, Plan 06 can backfill."
  artifacts:
    - path: app/services/endgame_service.py
      provides: _aggregate_per_tc_percentile + 5 page-level ΔES call-site wraps + rating_anchors population
      contains: "_aggregate_per_tc_percentile\\|rating_anchors"
      min_lines_added: 80
    - path: app/schemas/endgames.py
      provides: RatingAnchorOut + rating_anchors block on EndgameOverviewResponse
      contains: "RatingAnchorOut\\|rating_anchors"
      min_lines_added: 30
    - path: frontend/src/types/endgames.ts
      provides: regenerated types reflecting RatingAnchorOut + rating_anchors
      contains: "RatingAnchorOut\\|rating_anchors\\|ratingAnchors"
      min_lines_added: 30
    - path: tests/services/test_endgame_service.py
      provides: _aggregate_per_tc_percentile unit tests
      contains: "_aggregate_per_tc_percentile"
      min_lines_added: 80
  key_links:
    - from: app/services/endgame_service.py _aggregate_per_tc_percentile
      to: app/schemas/endgames.py EndgameOverviewStats (existing *_percentile fields)
      via: page-level ΔES chip scalar
      pattern: "_aggregate_per_tc_percentile"
    - from: app/schemas/endgames.py RatingAnchorOut
      to: app/repositories/user_rating_anchors_repository.py RatingAnchorRow (Plan 02 — widened with chesscom_raw_rating)
      via: API shaper reads anchors then projects to wire schema including chesscom_raw_rating
      pattern: "RatingAnchorOut\\|chesscom_raw_rating"
    - from: app/services/endgame_service.py rating_anchors population
      to: frontend/src/components/charts/PercentileChip.tsx (Plan 07 — chesscomRawRating prop)
      via: tooltip bullet 4 renders raw chess.com rating per D-07 bullet 4
      pattern: "rating_anchors"
---

<objective>
Third (and terminal) of the 3-plan Wave 2 commit cluster (05a → 05b → 05c). Add the API shaper aggregation helper `_aggregate_per_tc_percentile` for page-level ΔES chips per D-08/D-08b, extend the API schema for anchor disclosure per D-07 bullet 4 (including `chesscom_raw_rating`), regenerate frontend types, and run the full Pre-PR sweep that gates the atomic-cutover PR.

Purpose: B5 split — API shaper + schema + type regen isolated into its own context-bounded plan. Consumes Plan 05a's widened repository (nested dict) and Plan 05b's service rewrite (compute_anchors_for_user with chesscom_raw_rating capture). Produces the scalar percentile shape the frontend chips consume + the `rating_anchors` block the tooltip reads.

Output: 1 schema extension + 1 service helper + 1 frontend type regen + 1 test file extension + 1 terminal full Pre-PR sweep. After this plan, the backend cohort-CDF + anchor + per-(user, metric, tc) percentile stack is fully populated end-to-end; Plan 06 can backfill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@app/services/endgame_service.py
@app/schemas/endgames.py
@app/repositories/user_benchmark_percentiles_repository.py
@app/repositories/user_rating_anchors_repository.py
@frontend/src/types/endgames.ts
@CLAUDE.md

<interfaces>
<!-- Verified anchors:
     - PATTERNS lines 505-557: endgame_service aggregation helper + Pydantic schema extension
     - app/repositories/user_rating_anchors_repository.py (Plan 02 — RatingAnchorRow with chesscom_raw_rating per B3) -->

Aggregation helper (PATTERNS lines 510-532, CONTEXT D-08 / D-08b):
  def _aggregate_per_tc_percentile(
      per_tc_rows: Mapping[TimeControlBucket, PercentileRow] | None,
  ) -> float | None:
      """Game-count-weighted mean per CONTEXT D-08."""
      if per_tc_rows is None or not per_tc_rows:
          return None
      weighted_sum = 0.0
      weight_sum = 0
      for tc_row in per_tc_rows.values():
          if tc_row.percentile is None: continue
          weighted_sum += tc_row.percentile * tc_row.n_games
          weight_sum += tc_row.n_games
      if weight_sum == 0: return None
      return weighted_sum / weight_sum

Schema extension (PATTERNS lines 546-554, RESEARCH Open Question 4, CONTEXT D-07 bullet 4):
  class RatingAnchorOut(BaseModel):
      anchor_rating: int
      source_platform: Literal["lichess", "chesscom"]
      chesscom_raw_rating: int | None = None
      n_games: int

  # On EndgameOverviewResponse:
  rating_anchors: dict[TimeControlBucket, RatingAnchorOut] = Field(default_factory=dict)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RatingAnchorOut + rating_anchors to app/schemas/endgames.py</name>
  <files>app/schemas/endgames.py</files>
  <read_first>
    - app/schemas/endgames.py (in full — find `EndgameOverviewResponse` and its sibling schemas; the patterns map points to lines 230-510 + 700-830 for the existing chip-bearing schemas)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 541-557 — schema extension pattern)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Open Question 4 lines 1446-1452 — top-level rating_anchors block)
    - app/repositories/user_rating_anchors_repository.py (Plan 02 — verify RatingAnchorRow has `chesscom_raw_rating: int | None`)
  </read_first>
  <action>
    Edit `app/schemas/endgames.py`:
    1. Add `class RatingAnchorOut(BaseModel)` per PATTERNS lines 546-552:
       ```
       class RatingAnchorOut(BaseModel):
           anchor_rating: int
           source_platform: Literal["lichess", "chesscom"]
           chesscom_raw_rating: int | None = None
           n_games: int
       ```
       Add a class docstring citing CONTEXT D-07 bullet 4 — `chesscom_raw_rating` is the pre-conversion chess.com rating that the tooltip discloses ("Anchored on your chess.com {tc} ({raw_rating} → {anchor_rating} Lichess-equivalent ...)").
    2. Add `rating_anchors: dict[TimeControlBucket, RatingAnchorOut] = Field(default_factory=dict)` to `EndgameOverviewResponse` (or whichever top-level response schema the dashboard reads — search via grep `class EndgameOverviewResponse` in the file). Add a docstring on the field citing CONTEXT D-07 (tooltip body bullet 4 uses this) + RESEARCH Open Question 4 (top-level block, not embedded per-chip).
    3. Existing `*_percentile` fields on `EndgameOverviewStats` (or wherever the page-level ΔES scalars ship) stay shape-compatible — they're already `float | None` since 94.3.
  </action>
  <verify>
    <automated>uv run ty check app/schemas/endgames.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class RatingAnchorOut\|RatingAnchorOut(BaseModel)" app/schemas/endgames.py` is ≥ 1.
    - `grep -c "chesscom_raw_rating: int | None" app/schemas/endgames.py` is ≥ 1.
    - `grep -c "rating_anchors.*dict\[TimeControlBucket, RatingAnchorOut\]" app/schemas/endgames.py` is ≥ 1.
    - `uv run ty check app/schemas/endgames.py` returns zero errors.
    - Class docstring cites D-07 bullet 4.
  </acceptance_criteria>
  <done>Schema extension complete; tooltip-disclosure surface ready for Plan 07 to consume via frontend type regen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _aggregate_per_tc_percentile helper to app/services/endgame_service.py; wire 5 page-level ΔES call sites; populate rating_anchors with chesscom_raw_rating</name>
  <files>app/services/endgame_service.py, tests/services/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py (in full — particularly `_compute_score_gap_material` ~lines 1366-1424 per RESEARCH Sources; the 94.3 percentile-attach pattern in `_compute_time_pressure_cards` around lines 1455-1957)
    - app/repositories/user_benchmark_percentiles_repository.py (Plan 05a — nested-dict fetch shape)
    - app/repositories/user_rating_anchors_repository.py (Plan 02 — fetch_anchors_for_user returns RatingAnchorRow with chesscom_raw_rating)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 505-537 — aggregation helper + call-site changes)
  </read_first>
  <behavior>
    - Test 1: `_aggregate_per_tc_percentile({})` returns None (empty dict).
    - Test 2: `_aggregate_per_tc_percentile(None)` returns None.
    - Test 3: `_aggregate_per_tc_percentile({"bullet": PercentileRow(value=0.0, percentile=20.0, cdf_snapshot=date.today(), n_games=30), "blitz": PercentileRow(value=0.0, percentile=40.0, cdf_snapshot=date.today(), n_games=20), "rapid": PercentileRow(value=0.0, percentile=60.0, cdf_snapshot=date.today(), n_games=50)})` returns `(20*30 + 40*20 + 60*50) / 100 = 44.0` (weighted mean).
    - Test 4: All-None percentiles return None: `_aggregate_per_tc_percentile({"bullet": PercentileRow(value=0.0, percentile=None, cdf_snapshot=date.today(), n_games=30), "blitz": PercentileRow(value=0.0, percentile=None, cdf_snapshot=date.today(), n_games=20)})` returns None.
    - Test 5: Mixed None + valid: `{"bullet": PercentileRow(value=0.0, percentile=None, ...), "blitz": PercentileRow(value=0.0, percentile=50.0, ..., n_games=20)}` returns 50.0 (only the non-None row contributes; n_games of the None row is ignored — D-08a implicit zero weight).
    - Test 6: Single-row dict returns that row's percentile unchanged: `{"rapid": PercentileRow(value=0.0, percentile=37.5, ..., n_games=100)}` returns 37.5.
  </behavior>
  <action>
    Add `_aggregate_per_tc_percentile(per_tc_rows: Mapping[TimeControlBucket, PercentileRow] | None) -> float | None` to `app/services/endgame_service.py` per PATTERNS lines 510-532 verbatim. Cite CONTEXT D-08 + D-08a + D-08b in the docstring.

    Wire the 5 page-level ΔES call sites per PATTERNS line 535-537:
    - In `_compute_score_gap_material` (or whatever shaper handles the headline Endgame Score Gap): replace `effective_rows.get("score_gap")` (which was a single PercentileRow under the 94.3 flat dict shape) with `_aggregate_per_tc_percentile(effective_rows.get("score_gap", {}))` (now a `dict[TimeControlBucket, PercentileRow]` under Plan 05a's nested-dict shape). The result is one scalar `float | None` which becomes the page-level chip's percentile value.
    - Same wrap for Achievable Score Gap.
    - Same wrap for Parity ΔES (`section2_score_gap_parity`).
    - Same wrap for Conversion ΔES (`section2_score_gap_conv`).
    - Same wrap for Recovery ΔES (`recovery_score_gap`) — NEW chip slot rescued per CONTEXT D-05a.

    Time Pressure family chips BYPASS aggregation per CONTEXT D-08 + RESEARCH line 1117. In `_compute_time_pressure_cards`, read per-TC directly: `effective_rows.get("time_pressure_score_gap", {}).get(tc)` returns a `PercentileRow | None`; the chip's percentile = `.percentile` of that row (or None). Same for `clock_gap` and `net_flag_rate`. No `_aggregate_per_tc_percentile` call for these.

    Populate `rating_anchors` on the response: `anchors = await fetch_anchors_for_user(session, user_id=current_user.id)` (Plan 02's repository — now returns `RatingAnchorRow` with `chesscom_raw_rating: int | None` per B3). Project into wire schema: `rating_anchors = {tc: RatingAnchorOut(anchor_rating=row.anchor_rating, source_platform=row.source_platform, chesscom_raw_rating=row.chesscom_raw_rating, n_games=row.n_games) for tc, row in anchors.items()}`. Pass into `EndgameOverviewResponse(rating_anchors=rating_anchors, ...)`. The `chesscom_raw_rating` flows through unchanged from the persisted anchor row (Plan 02 stores it; Plan 05b captures it at compute time).

    **W4 disposition (multi-TC footnote deferral):** for aggregated page-level chips, the dominant-TC anchor approach lives at the frontend (Plan 07 picks the dominant TC's anchor). The "+ N other TCs" footnote is intentionally DEFERRED to a v1.1 of the tooltip — current scope ships the dominant-TC disclosure with the aggregated framing of bullet 1 ("Compared to other ~{anchor}-rated players, aggregated across the time controls you play.") covering honesty. Add a code comment in the rating_anchors population block citing this deferral + linking to Plan 07's tooltip body builder.

    Add the 6 behavior tests to `tests/services/test_endgame_service.py` extending the existing test file. Use the existing pytest fixtures + style.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service.py -x -k aggregate -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _aggregate_per_tc_percentile" app/services/endgame_service.py` is exactly 1.
    - `grep -c "_aggregate_per_tc_percentile(" app/services/endgame_service.py` is ≥ 6 (1 definition + 5 page-level ΔES call sites).
    - Recovery Score Gap chip slot present: `grep -c "recovery_score_gap" app/services/endgame_service.py` is ≥ 1.
    - Time Pressure family does NOT wrap with `_aggregate_per_tc_percentile`: scan the file body and verify `time_pressure_score_gap` / `clock_gap` / `net_flag_rate` are NOT wrapped (only commented annotation tolerated). Acceptance: `awk '/time_pressure_score_gap|clock_gap|net_flag_rate/ && /_aggregate_per_tc_percentile/' app/services/endgame_service.py | wc -l` is 0.
    - All 6 behavior tests pass.
    - `grep -c "rating_anchors" app/services/endgame_service.py` is ≥ 1 (population of the new schema field).
    - `grep -c "chesscom_raw_rating" app/services/endgame_service.py` is ≥ 1 (raw rating flows through into the API response per D-07 bullet 4).
    - `uv run ty check app/services/endgame_service.py` returns zero errors.
    - W4 deferral comment present: `grep -c "v1.1\|deferred.*footnote\|multi-TC.*footnote" app/services/endgame_service.py` is ≥ 1.
  </acceptance_criteria>
  <done>Aggregation helper + 5 wrap sites + Time Pressure bypass + rating_anchors population (with chesscom_raw_rating per D-07 bullet 4) complete; 6 tests green; W4 multi-TC footnote deferral documented in-code.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate frontend/src/types/endgames.ts to reflect RatingAnchorOut + rating_anchors</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - app/schemas/endgames.py (just modified in Task 1)
    - frontend/src/types/endgames.ts (current — confirm how it's generated; if there's a codegen script in `package.json`, this is the trigger to regenerate)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 541-557)
  </read_first>
  <action>
    Run the codegen mechanism (likely `npm run gen-types` or similar — check `package.json`; the existing 94.3 plans should document this). If no codegen script exists, edit `frontend/src/types/endgames.ts` manually to add the matching TypeScript interface:
    ```typescript
    export interface RatingAnchorOut {
      anchor_rating: number;
      source_platform: 'lichess' | 'chesscom';
      chesscom_raw_rating: number | null;
      n_games: number;
    }
    // EndgameOverviewResponse gains:
    rating_anchors: Partial<Record<TimeControlBucket, RatingAnchorOut>>;
    ```
    Ensure the TS field is `Partial<Record<...>>` (or `Record<TimeControlBucket, RatingAnchorOut | undefined>`) so missing-TC keys are properly typed as undefined. Existing `*_percentile` typing should already be `number | null` — verify no drift (RESEARCH Pitfall 10).
  </action>
  <verify>
    <automated>( cd frontend && npx tsc --noEmit )</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RatingAnchorOut\|ratingAnchors\|rating_anchors" frontend/src/types/endgames.ts` is ≥ 2.
    - `grep -c "chesscom_raw_rating: number | null\|chesscomRawRating: number | null" frontend/src/types/endgames.ts` is ≥ 1 (D-07 bullet 4 surface in the TS type).
    - `( cd frontend && npx tsc --noEmit )` is clean (no TS errors after the type regen).
  </acceptance_criteria>
  <done>Frontend type regen complete; Plan 07 has zero merge conflicts on the type file and can consume `chesscomRawRating` for the tooltip's 4th bullet.</done>
</task>

<task type="auto">
  <name>Task 4: Terminal Pre-PR checklist — full ruff/ty/pytest + frontend gates (atomic-cutover linchpin)</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist"
  </read_first>
  <action>
    This is the linchpin gate for the 05a + 05b + 05c atomic-cutover PR. Run the full pre-PR sequence: `uv run ruff format app/ tests/`, then `uv run ruff check app/ tests/ --fix`, then `uv run ty check app/ tests/` (must be zero errors), then `uv run pytest -x` (full backend suite; expect green now that legacy stubs are retired and the byte-identical regen test is green from Plan 04), then `( cd frontend && npm run lint && npm test -- --run )`. If any gate fails, fix in-plan; no broken commits allowed at the cutover boundary.
  </action>
  <verify>
    <automated>uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -x && ( cd frontend && npm run lint && npm test -- --run )</automated>
  </verify>
  <acceptance_criteria>
    - ruff format check clean across `app/` + `tests/`.
    - ruff check clean.
    - ty check zero errors across `app/` + `tests/`.
    - Full backend pytest green.
    - Frontend lint + tests green (no chip changes in Plan 05c; Plan 07 owns the chip rewrite — frontend tests should pass against unchanged chip code).
  </acceptance_criteria>
  <done>All gates green; schema reshape + service rewrite + aggregation + schema extension + type regen all coherent; Plan 06 ready to backfill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → service → repository | Authenticated `current_user.id` from FastAPI-Users dep is the sole `user_id` source; never accepted as a query/path parameter (V4 mitigation, preserved). |
| Service → wire schema | RatingAnchorOut + rating_anchors block: backend-produced data; `current_user.id` filtered at the API layer; no cross-user data crosses the wire. |
| Backend → frontend | `chesscom_raw_rating` is the user's own pre-conversion rating per D-07 bullet 4 disclosure design — intended surface, not a leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-05c-01 | Information disclosure | rating_anchors leaks another user's anchor rating | mitigate | `fetch_anchors_for_user(user_id=current_user.id)` — V4 mitigation preserved. |
| T-94.4-05c-02 | Tampering | XSS via schema field interpolation | mitigate | Pydantic validates all wire fields; React JSX auto-escapes string values on the frontend. |
| T-94.4-05c-SC | Tampering | npm/pip installs | accept | No new packages introduced in this plan. |
</threat_model>

<verification>
- `_aggregate_per_tc_percentile` exists in `endgame_service.py` and is called by exactly 5 page-level ΔES shapers; Time Pressure family bypasses.
- `app/schemas/endgames.py` exposes `RatingAnchorOut` with `chesscom_raw_rating: int | None`; `EndgameOverviewResponse.rating_anchors` field present.
- `frontend/src/types/endgames.ts` reflects the new types; `npx tsc --noEmit` clean.
- Full pytest + frontend gates green.
</verification>

<success_criteria>
PRPCR-04 fully implemented (aggregation at API shaper; per-TC ΔES builders consumed end-to-end). PRPCR-05 fully implemented (schema + types reflect the 8-value ENUM + widened PK + Recovery rescue, with `rating_anchors` + `chesscom_raw_rating` per D-07 bullet 4). Backend end-to-end ready for Plan 06's backfill against the new shape.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-05c-SUMMARY.md` when done. Record: full pytest run count + runtime; frontend test count; confirm `_aggregate_per_tc_percentile` wired at exactly 5 sites; confirm `chesscom_raw_rating` flows from anchor row → `RatingAnchorOut` → frontend type.
</output>
