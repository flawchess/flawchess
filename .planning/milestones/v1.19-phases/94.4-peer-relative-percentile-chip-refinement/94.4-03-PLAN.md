---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 03
type: execute
wave: 2
depends_on: [94.4-02]
files_modified:
  - app/services/canonical_slice_sql.py
  - tests/services/test_canonical_slice_sql.py
  - tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py
autonomous: true
requirements: [PRPCR-04]

must_haves:
  truths:
    - "`app/services/canonical_slice_sql.py` gains 4 new per-TC ΔES builders mirroring the 94.3 per-TC Time Pressure builder pattern (analog: lines 587-666 `per_user_cte_time_pressure_score_gap`): `per_user_cte_score_gap_tc(tc, *, source, snapshot_date)`, `per_user_cte_achievable_tc(tc, *, source, snapshot_date)`, and an extended `per_user_cte_section2(...)` (or sibling `per_user_cte_section2_tc(tc, *, source, snapshot_date, bucket_label)`) where the `bucket_label` Literal is widened to include `'recovery'` per RESEARCH Open Question 3. All 4 ΔES families are accessible per-TC: `score_gap`, `achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`, `recovery_score_gap` (via section2 with bucket_label='recovery')."
    - "Pitfall 1 widening: ALL 7 per-TC builders (3 existing 94.3 + 4 new 94.4) expose `user_id` in their `per_user_values` CTE. The widening is mechanical — change `SELECT (eg_score - non_eg_score) AS metric_value, eg_n AS n_games FROM per_user` to `SELECT user_id, (eg_score - non_eg_score) AS metric_value, eg_n AS n_games FROM per_user` (and add `user_id` to the upstream `per_user` aggregate's GROUP BY / projection list as needed). Existing 94.3 builders (`per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`) are widened in this same plan because Plan 04's cohort-CDF JOIN against `per_user_anchor` needs `user_id` on ALL metric families, not just the 4 new ones. (RESEARCH Pitfall 1 lines 1168-1177.)"
    - "All 4 new builders use the same Literal-typed `tc: TimeControlBucket` parameter the 94.3 builders established — f-string interpolation into SQL is safe (Literal enforces 4-value set at every call site). Each builder docstring carries the security guarantee paragraph verbatim from the 94.3 analog (lines 613-616)."
    - "Test coverage in `tests/services/test_canonical_slice_sql.py`: 4 new builders × 4 TCs = 16 new test cases asserting per-TC output shape (per_user_values present, restricted to one TC via `_recent_capped_per_tc_cte`, metric_value projection correct for each family). Plus a regression sweep that asserts `user_id` appears in `per_user_values` for ALL 7 per-TC builders (3 existing + 4 new) — this is the Pitfall 1 regression guard."
    - "Test coverage in `tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py`: per-TC parity assertions for the 4 new families (one per family × 4 TCs = 16 assertions). Each parity test compares `per_user_cte_<family>_tc(tc, source='benchmark', ...)` against `per_user_cte_<family>_tc(tc, source='single_user', ...)` and asserts the pooled body (everything after the selected_users CTE switch) is byte-identical — the 94.3 parity pattern verbatim."
    - "`recovery_score_gap` per-TC support: extend `per_user_cte_section2`'s `bucket_label` Literal from `Literal['conversion', 'parity']` to `Literal['conversion', 'parity', 'recovery']`. The `recovery` bucket reuses the section2 pooled body with the same eval-derived endgame-entry detection but flips the directional sign convention (recovery: from material deficit; conversion: from material edge; parity: zero-eval endgame entry). Match the existing section2 convention exactly — if the existing builder dispatches `bucket_label` to per-bucket WHERE clauses, add the recovery branch following the same pattern."
    - "No call sites are added in this plan; the new builders are dormant until Plan 04 (CDF gen) and Plan 05 (per-user service rewrite) consume them. Existing call sites (Phase 94.1/94.2/94.3 consumers of the section2 builder) MUST continue to pass identical SQL — `bucket_label` defaults to its current default OR all current call sites pass an explicit `bucket_label` argument and are reviewed to confirm no behavioral drift."
    - "Wave-2 atomic-cutover coordination: the byte-identical regression test `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` is EXPECTED RED between the Plan 03 merge and the Plan 04 merge (Plan 03 changes per-TC builder shape; Plan 04 regenerates the goldens). This is an intentional, documented intermediate state per RESEARCH Wave-2 coordination line 1676 — the atomic-cutover discipline (D-10a) treats Plans 03 → 04 → 05 → 06 as one logical commit chain on a single feature branch / single PR."
  artifacts:
    - path: app/services/canonical_slice_sql.py
      provides: 4 new per-TC ΔES builders + Pitfall 1 user_id widening across all 7 per-TC builders
      contains: "per_user_cte_score_gap_tc\\|per_user_cte_achievable_tc"
      min_lines_added: 50  # delta, not absolute — net new SQL builders + user_id widening (N2 fix)
    - path: tests/services/test_canonical_slice_sql.py
      provides: 16+ new builder unit tests + Pitfall 1 regression guard
      min_lines: 400
    - path: tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py
      provides: 16+ new parity assertions for the 4 new per-TC ΔES families
      min_lines: 200
  key_links:
    - from: app/services/canonical_slice_sql.py::per_user_cte_score_gap_tc
      to: scripts/gen_global_percentile_cdf.py (Plan 04)
      via: CDF generation joins per_user_values × per_user_anchor by user_id (Pitfall 1)
      pattern: "per_user_cte_score_gap_tc\\|per_user_cte_achievable_tc\\|per_user_cte_section2"
    - from: app/services/canonical_slice_sql.py per_user_values widening
      to: scripts/gen_global_percentile_cdf.py (Plan 04) AND app/services/user_benchmark_percentiles_service.py (Plan 05)
      via: user_id projection enables the (user_id) JOIN with per_user_anchor
      pattern: "SELECT user_id,"
---

<objective>
Extend `app/services/canonical_slice_sql.py` with the 4 new per-TC ΔES builders required by the cohort-CDF JOIN architecture (RESEARCH Pattern 6 lines 569-669) and apply the Pitfall 1 `user_id` widening across ALL 7 per-TC builders (3 existing 94.3 + 4 new 94.4). This is the first commit in the Wave 2 atomic-cutover quartet (Plans 03 → 04 → 05 → 06) — Plan 04 generates the cohort CDF against these builders; Plan 05 consumes them per-user; Plan 06 backfills.

Purpose: The peer-relative pivot needs every metric family computable per-TC so the cohort CDF can be built at `(metric, anchor, tc)` granularity. The 94.3 per-TC builder family already exists for Time Pressure (`time_pressure_score_gap`, `clock_gap`, `net_flag_rate`); this plan extends symmetrically to the 4 page-level ΔES metrics that were previously TC-aggregated (`score_gap`, `achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`) and adds the `recovery_score_gap` rescue (RESEARCH Open Question 3) by extending the `bucket_label` Literal on `per_user_cte_section2`. The Pitfall 1 widening exposes `user_id` in `per_user_values` for ALL per-TC builders — without this widening, Plan 04's cohort-CDF JOIN against `per_user_anchor` would lose user identity and the K=200 ranking by anchor distance would be impossible.

Output: 4 new per-TC builders + Pitfall 1 widening + 16+ unit tests + 16+ parity tests. No call sites added (builders are dormant until Plans 04/05 consume them). Plan 03 is sequential after Plan 02 (which adds `per_user_cte_median_anchor` to the same file) — to avoid merge conflicts in `canonical_slice_sql.py`, run Plan 03 only after Plan 02 is merged to the feature branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@app/services/canonical_slice_sql.py
@tests/services/test_canonical_slice_sql.py
@tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py
@CLAUDE.md

<interfaces>
<!-- Verified line anchors in app/services/canonical_slice_sql.py (current main):
     - Line 277: def _recent_capped_per_tc_cte(snapshot_date, tc) — per-TC restriction helper
     - Line 365: per_user_values AS — existing builder (score_gap pooled-aggregate)
     - Line 437: per_user_values AS — existing builder (achievable)
     - Line 446: def per_user_cte_section2 — section2 pooled builder
     - Line 549: per_user_values AS — section2
     - Line 587: def per_user_cte_time_pressure_score_gap — 94.3 per-TC analog (LINE-ANCHORED for new builders)
     - Line 661: per_user_values AS — 94.3 time_pressure
     - Line 718: per_user_values AS — 94.3 clock_gap
     - Line 781: per_user_values AS — 94.3 net_flag_rate -->

New builder signatures (mirror line 587-619 verbatim):
  def per_user_cte_score_gap_tc(
      tc: TimeControlBucket,
      *,
      source: Literal["benchmark", "single_user"],
      snapshot_date: date | None = None,
  ) -> str:
      """Per-TC pooled per_user_values for score_gap.

      Security: tc is constrained to a 4-value Literal so f-string
      interpolation into SQL is safe (no injection vector). The pooled body
      after the selected_users CTE is byte-identical between source='benchmark'
      and source='single_user' — Plan 04 parity test enforces this.
      """
      _ = source

  def per_user_cte_achievable_tc(...) -> str: ...  # same shape

Section2 bucket_label widening (existing builder at line 446):
  # Before: bucket_label: Literal["conversion", "parity"]
  # After:  bucket_label: Literal["conversion", "parity", "recovery"]
  # Plus a new per-TC variant per_user_cte_section2_tc(tc, *, source, snapshot_date, bucket_label).

Pitfall 1 widening (ALL 7 per-TC builders, including the 3 existing 94.3 builders):
  # Existing per_user_values AS body:
  per_user_values AS (
    SELECT (eg_score - non_eg_score) AS metric_value, eg_n AS n_games FROM per_user
  )
  # Widened to:
  per_user_values AS (
    SELECT user_id, (eg_score - non_eg_score) AS metric_value, eg_n AS n_games FROM per_user
  )
  # ALSO requires per_user upstream to GROUP BY user_id (verify the upstream aggregate's
  # GROUP BY clause already groups by user_id — it should, since the existing per_user
  # is "per user" by name).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen per_user_values to project user_id in ALL 7 existing+new per-TC builders (Pitfall 1)</name>
  <files>app/services/canonical_slice_sql.py, tests/services/test_canonical_slice_sql.py</files>
  <read_first>
    - app/services/canonical_slice_sql.py (in full — read lines 277-800 to identify ALL `per_user_values AS (...)` CTE bodies; verified at lines 365, 437, 549, 661, 718, 781 from grep)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pitfall 1 lines 1168-1177 for the full justification; Pattern 6 lines 569-669 for the JOIN shape that needs user_id)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 286-294 — Pitfall 1 widening directive)
  </read_first>
  <behavior>
    - Test 1 (parametrized over the 3 existing 94.3 per-TC builders × 4 TCs = 12 cases): generated SQL contains `SELECT user_id,` (or equivalent — `SELECT user_id ,` / `SELECT\n  user_id,`) in the `per_user_values` CTE body.
    - Test 2: existing non-per-TC builders (e.g. `per_user_cte_score_gap`, the original `per_user_cte_section2` pooled builder) are NOT touched in this task — their `per_user_values` may or may not have `user_id` depending on whether they're consumed by the cohort-CDF JOIN. (Plan 04 only consumes per-TC builders.) Verify by reading their bodies and asserting their existing behavior is preserved.
    - Test 3: SQL execution against dev DB (smoke test): `per_user_cte_time_pressure_score_gap('blitz', source='single_user', snapshot_date=date.today())` wrapped in a minimal `SELECT * FROM per_user_values LIMIT 1` parses and executes (the asyncpg planner accepts the new SELECT shape).
  </behavior>
  <action>
    Edit `app/services/canonical_slice_sql.py`. For EACH of the 3 existing 94.3 per-TC builders (`per_user_cte_time_pressure_score_gap` at line ~587, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`), find the `per_user_values AS (...)` CTE body (lines ~661, ~718, ~781 from grep) and prepend `user_id, ` to the SELECT projection. The change is mechanical: `SELECT (eg_score - non_eg_score) AS metric_value, eg_n AS n_games FROM per_user` becomes `SELECT user_id, (eg_score - non_eg_score) AS metric_value, eg_n AS n_games FROM per_user`. Critical verification: the upstream `per_user` CTE MUST already project `user_id` (it's a per-user aggregate; if it doesn't, this task widens that CTE too). Read each `per_user AS (...)` body in turn and confirm `user_id` is already in the GROUP BY + projection — it almost certainly is, since the aggregate is "per user" by construction. Add a one-line comment above each widened `per_user_values` body: `-- user_id widened per Phase 94.4 Pitfall 1 (cohort-CDF JOIN against per_user_anchor)`. Do NOT touch the 4 non-per-TC builders (the original `per_user_cte_*` family that the 94.2 / 94.3 plans left at line 365 / 437 / 549). Add the 12 parametrized test cases in `tests/services/test_canonical_slice_sql.py` asserting `"SELECT user_id,"` (or a tolerant regex `r"SELECT\s+user_id\b"`) is present in each per-TC builder's output.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x -k "per_tc_user_id or pitfall_1" -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 4 'per_user_values AS' app/services/canonical_slice_sql.py | grep -c 'SELECT user_id\|SELECT\s*\n\s*user_id'` is ≥ 3 (the 3 existing 94.3 per-TC builders are all widened — 4 new ones will add 4 more in Task 2).
    - 12 parametrized Pitfall 1 regression tests pass.
    - `grep -c "user_id widened per Phase 94.4 Pitfall 1" app/services/canonical_slice_sql.py` is ≥ 3.
    - No existing test in `test_canonical_slice_sql.py` regresses (run full file).
    - `uv run ty check app/services/canonical_slice_sql.py` returns zero errors.
  </acceptance_criteria>
  <done>3 existing per-TC builders widened; Pitfall 1 regression guard in place; existing 94.3 tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 4 new per-TC ΔES builders + extend section2 bucket_label to include 'recovery'</name>
  <files>app/services/canonical_slice_sql.py, tests/services/test_canonical_slice_sql.py</files>
  <read_first>
    - app/services/canonical_slice_sql.py (in full again — focus on lines 587-666 for `per_user_cte_time_pressure_score_gap` as the verbatim analog; lines 365-440 for the existing non-per-TC `score_gap` and `achievable` builders to mirror their metric_value projection; lines 446-549 for the existing `per_user_cte_section2` builder to understand `bucket_label` dispatch)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 252-294 — pattern assignment for new builders)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pattern 6 lines 569-669; Open Question 3 lines 1430-1445 for the recovery bucket extension)
  </read_first>
  <behavior>
    - Test 1 (parametrized over 4 TCs): `per_user_cte_score_gap_tc(tc, source='benchmark', snapshot_date=date.today())` emits a `per_user_values` CTE projecting `user_id`, `metric_value`, `n_games`; restricted to one TC via `_recent_capped_per_tc_cte(snapshot_date, tc)`; metric_value formula matches the non-per-TC `per_user_cte_score_gap`'s formula (`eg_score - non_eg_score` or equivalent — copy verbatim from the existing builder).
    - Test 2 (parametrized over 4 TCs): `per_user_cte_achievable_tc(tc, source='benchmark', snapshot_date=date.today())` analogous shape.
    - Test 3 (parametrized over 4 TCs × 3 bucket_label values `'conversion'`, `'parity'`, `'recovery'`): `per_user_cte_section2_tc(tc, source='benchmark', bucket_label=label)` emits valid SQL for each combination; `'recovery'` produces a per_user_values body with metric_value matching the section2 recovery formula (whatever the existing section2 builder uses, just per-TC restricted).
    - Test 4: All new builders project `user_id` in `per_user_values` (Pitfall 1 applies to new builders too).
    - Test 5: Source-mode parity (RESEARCH Pattern 6 lines 595-619 design): `per_user_cte_score_gap_tc(tc, source='benchmark', ...)` and `per_user_cte_score_gap_tc(tc, source='single_user', ...)` produce IDENTICAL pooled bodies (the difference is in the upstream `selected_users_cte`, not in the per-TC pooled body) — assert by comparing the substring starting from `per_user_values AS` between the two.
    - Test 6: Security guarantee — passing an invalid TC like `'xxx'` would be caught by ty at compile time (this is a static guarantee, not a runtime test); the docstring SHOULD cite this. Verify docstring content via `grep`.
  </behavior>
  <action>
    Add 4 new builders to `app/services/canonical_slice_sql.py`. Place them after the existing 94.3 per-TC builders (around line 800+, after `per_user_cte_net_flag_rate`) for code locality with the per-TC family.
    1. **`per_user_cte_score_gap_tc(tc, *, source, snapshot_date)`**: copy the body of the existing non-per-TC `per_user_cte_score_gap` (line 365 region) verbatim, but replace its `_recent_capped_cte` reference with `_recent_capped_per_tc_cte(snapshot_date, tc)`. Project `user_id` in `per_user_values`. Docstring mirrors `per_user_cte_time_pressure_score_gap` (lines 588-617): explain per-TC restriction, security guarantee on `tc: Literal`, source-parity guarantee, Pitfall 1 user_id widening. Add `_ = source` discard line.
    2. **`per_user_cte_achievable_tc(tc, *, source, snapshot_date)`**: same mechanical extension of the existing non-per-TC `per_user_cte_achievable` (line 437 region).
    3. **Widen the existing `per_user_cte_section2(...)` signature** at line 446: change `bucket_label: Literal["conversion", "parity"]` to `bucket_label: Literal["conversion", "parity", "recovery"]`. NO structural changes to the SQL body are required — the existing `gap_rows` CTE at lines 491-538 already classifies `bucket = 'recovery'` via the inline CASE expression (lines 494-507) when `swn.entry_eval_cp * user_color_sign <= -100` OR `swn.entry_eval_mate * user_color_sign < 0` (user is down material at endgame entry; matches D-05a recovery semantics). The downstream `per_user_bucket` GROUP BY user_id, bucket (line 540) and `per_user_values WHERE bucket = '{bucket_label}'` (line 554) already handle dispatch parametrically. The widening is therefore purely at the Literal type level — Python f-string interpolates `'recovery'` into the WHERE clause; the SQL builder already does the right thing.

  **Locked predicate (cite in the action commit message + docstring):** recovery rows are the rows where `gap_rows.bucket = 'recovery'`, where bucket is computed by the inline CASE at lines 494-507 as: `entry_eval_mate < 0 (user color signed)` → 'recovery', OR `entry_eval_cp * user_color_sign <= -100` cp (user is down ≥ 1 pawn) → 'recovery'. This matches D-05a (recovery rescue from material deficit). No new WHERE predicate is added; no sibling `per_user_cte_section2_recovery` is needed. Plan 04 Task 2 dispatch table maps `recovery_score_gap` → `per_user_cte_section2(..., bucket_label='recovery')` (NOT a sibling — confirmed by this lock).
    4. **`per_user_cte_section2_tc(tc, *, source, snapshot_date, bucket_label)`**: per-TC sibling of `per_user_cte_section2`. Same as the per_user_cte_section2 body but with `_recent_capped_per_tc_cte` and `user_id` projection. Accepts the same 3-value `bucket_label` Literal.
    All 4 new builders use 4-space indentation, type-annotated explicitly, docstring + security guarantee block. Add the 16 + 12 + parity tests per the `<behavior>` block to `tests/services/test_canonical_slice_sql.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x -k "score_gap_tc or achievable_tc or section2_tc or section2 and recovery" -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def per_user_cte_score_gap_tc\|def per_user_cte_achievable_tc\|def per_user_cte_section2_tc" app/services/canonical_slice_sql.py` is ≥ 3.
    - `grep -c "Literal\[\"conversion\", \"parity\", \"recovery\"\]\|Literal\['conversion', 'parity', 'recovery'\]" app/services/canonical_slice_sql.py` is ≥ 1 (section2 bucket_label widened).
    - `grep -c "user_id widened per Phase 94.4 Pitfall 1\|SELECT user_id," app/services/canonical_slice_sql.py | grep -v '^#'` is ≥ 7 (3 existing + 4 new per-TC builders all widened — accept either the comment count or the SELECT count).
    - All new builder unit tests (16 cases) pass.
    - All new parity tests in Task 4 / Test 5 pass.
    - Section2 recovery branch produces non-empty SQL containing `WHERE bucket = 'recovery'`: `assert "WHERE bucket = 'recovery'" in per_user_cte_section2(source='benchmark', bucket_label='recovery')`. The existing `gap_rows.bucket` CASE (lines 494-507) classifies recovery rows; the WHERE dispatch substring is the load-bearing assertion.
    - No regression in existing 94.2 / 94.3 `test_canonical_slice_sql.py` cases.
    - `uv run ty check app/services/canonical_slice_sql.py` returns zero errors.
  </acceptance_criteria>
  <done>4 new per-TC ΔES builders shipped; section2 bucket_label widened to include 'recovery'; all builders pitfall-1-widened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py with 16+ new parity assertions for the 4 new per-TC ΔES families</name>
  <files>tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py</files>
  <read_first>
    - tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py (in full — existing parity assertions for Phase 94.2 / 94.3 builders; pattern: `assert benchmark_sql.replace(selected_users_benchmark, '') == single_user_sql.replace(selected_users_single, '')` or a similar substring-comparison shape)
    - app/services/canonical_slice_sql.py (just modified — the 4 new per-TC ΔES builders)
  </read_first>
  <behavior>
    - Test 1 (parametrized over 4 TCs): `per_user_cte_score_gap_tc(tc, source='benchmark', snapshot_date=date.today())` and `per_user_cte_score_gap_tc(tc, source='single_user', snapshot_date=date.today())` have IDENTICAL pooled bodies after stripping the `selected_users_cte` block.
    - Test 2 (parametrized over 4 TCs): same for `per_user_cte_achievable_tc`.
    - Test 3 (parametrized over 4 TCs × 3 bucket_labels): same for `per_user_cte_section2_tc`. Total: 12 cases here.
    - Test 4 (regression for existing 94.3 builders that this plan widened with user_id): `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate` STILL show source-mode parity after the Pitfall 1 widening (the widening applies to both source modes equally, so parity is preserved).
  </behavior>
  <action>
    Extend `tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py` with new parametrized parity assertions. Mirror the existing 94.3 parity pattern (search for the existing parity helper or comparison shape in the file). The parity check is: for each (builder, tc, …) tuple, generate the SQL for both source modes, normalize whitespace, strip the `selected_users` CTE (everything before the first per_user_values reference), and assert the remainder is byte-identical. Use `pytest.mark.parametrize` for the 4-TC sweep + 3-bucket sweep. Total new assertions: 4 (score_gap_tc × 4 TC) + 4 (achievable_tc × 4 TC) + 12 (section2_tc × 4 TC × 3 bucket) + 12 (regression for the 3 existing 94.3 builders × 4 TC) = 32 new test cases. Existing parity tests stay untouched.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - At least 32 new parity test cases pass (`pytest -v` count should exceed previous baseline by 32+).
    - `grep -c "def test_" tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py` increases by at least 4 new functions (parametrize multiplies cases inside each `def`).
    - All existing parity tests still pass.
  </acceptance_criteria>
  <done>32+ new parity cases green; source-mode parity preserved across all 7 per-TC builders.</done>
</task>

<task type="auto">
  <name>Task 4: Pre-PR checklist for Plan 03 — ruff format, ruff check, ty check, pytest</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist"
  </read_first>
  <action>
    Run the four backend gates: `uv run ruff format app/services/canonical_slice_sql.py tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py`, then `uv run ruff check ... --fix`, then `uv run ty check app/ tests/` (full backend ty check; canonical_slice_sql.py is imported widely so any signature drift surfaces here), then `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -x`. Then run the broader pytest suite to catch any indirect regression: `uv run pytest tests/services/ -x` (services tests; ~few minutes). If anything breaks elsewhere, surface as a Wave 2 atomic-cutover blocker (Plan 04 depends on this plan's stability). Plan 03 is the first commit in the atomic-cutover sequence; the canonical_slice_sql.py changes here flow downstream to Plan 04 (CDF gen) and Plan 05 (service rewrite).
  </action>
  <verify>
    <automated>uv run ruff format --check app/services/canonical_slice_sql.py tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py && uv run ruff check app/services/canonical_slice_sql.py tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py && uv run ty check app/ tests/ && uv run pytest tests/services/ -x</automated>
  </verify>
  <acceptance_criteria>
    - ruff format check clean.
    - ruff check clean.
    - ty check zero errors across `app/` and `tests/`.
    - Full `tests/services/` pytest run green.
    - The byte-identical regression test `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` may go RED — this is EXPECTED per RESEARCH Wave-2 coordination (line 1676); Plan 04 regenerates the goldens. Run this test separately and CONFIRM it's the only red test; if any other test goes red, surface as a blocker.
  </acceptance_criteria>
  <done>All gates green except the expected RED `test_gen_global_percentile_cdf_unchanged.py` (regenerated in Plan 04); Plan 03 ready for Plan 04 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Postgres | All SQL fragments use STATIC SQL + named bindparams + `Literal`-typed parameters for f-string interpolation. No user-supplied string crosses into SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-03-01 | Tampering | SQL injection via `tc` Literal on new per-TC builders | mitigate | All 4 new builders take `tc: TimeControlBucket = Literal["bullet", "blitz", "rapid", "classical"]`. ty enforces. F-string interpolation safe (4 known strings). Documented in each builder docstring. |
| T-94.4-03-02 | Tampering | SQL injection via `bucket_label` widening | mitigate | `bucket_label: Literal["conversion", "parity", "recovery"]` — 3-value set, ty-checked. F-string interpolation safe. |
| T-94.4-03-03 | Information disclosure | `user_id` widening exposes cross-user data in pooled query | accept | The `per_user_values` CTE was already pooled-aggregate (one row per user); adding `user_id` to the projection does NOT change the query's data scope. The downstream JOIN in Plan 04 (cohort CDF) is intentional and scoped to the benchmark population — Phase 94.4 architecture (RESEARCH Pattern 6). |
| T-94.4-03-SC | Tampering | npm/pip installs | accept | No new packages. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -x` green (existing + 32+ new cases).
- `uv run ty check app/ tests/` reports zero errors.
- `grep -c "def per_user_cte_score_gap_tc\|def per_user_cte_achievable_tc\|def per_user_cte_section2_tc" app/services/canonical_slice_sql.py` is ≥ 3.
- `grep -A 4 'per_user_values AS' app/services/canonical_slice_sql.py | grep -c 'SELECT user_id\|SELECT\s\+user_id'` is ≥ 7 (all 7 per-TC builders widened per Pitfall 1).
- `per_user_cte_section2(... bucket_label='recovery')` returns valid SQL that runs against dev (smoke-execute against a single user is acceptable).
- The byte-identical regression test `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` MAY be red — this is expected per Wave-2 coordination (Plan 04 regenerates).
</verification>

<success_criteria>
PRPCR-04 partially implemented (the per-TC SQL primitive ready for Plan 04 CDF gen + Plan 05 per-user service). Per-TC ΔES builder family extended to 4 new families (score_gap_tc, achievable_tc, section2_tc with bucket_label=conversion|parity|recovery). Pitfall 1 widening applied to ALL 7 per-TC builders so the cohort-CDF JOIN against `per_user_anchor` can locate users by ID. Source-mode parity preserved. No call sites added (dormant until Plan 04/05).
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-03-SUMMARY.md` when done. Record: count of new builder functions added; count of pitfall-1-widened per_user_values blocks (should be 7 — 3 existing + 4 new); test count delta in test_canonical_slice_sql.py and the parity file; confirm the section2 'recovery' branch added in-place (Literal widening only; no sibling builder, per B4 lock); note the expected RED status of test_gen_global_percentile_cdf_unchanged.py (regenerated in Plan 04).
</output>
