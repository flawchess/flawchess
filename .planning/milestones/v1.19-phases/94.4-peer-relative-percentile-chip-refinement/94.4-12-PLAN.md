---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 12
type: execute
wave: 3
depends_on: [94.4-09, 94.4-10, 94.4-11]
files_modified:
  - scripts/backfill_user_percentiles.py
  - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-12-HUMAN-UAT.md
autonomous: false
requirements: [PRPCR-03, PRPCR-09]

must_haves:
  truths:
    - "`scripts/backfill_user_percentiles.py` is extended to align with the D-12 Reversal Amendment schema. The existing 3-stage flow from Plan 06 stays (compute_anchors_for_user -> compute_stage_a -> compute_stage_b), but the per-(TC × source_platform) anchor summary table emitted at the end of the run is REPLACED with a per-TC anchor summary that breaks down by composition type — `pure_lichess`, `pure_chesscom`, `mixed`, `suppressed`. The four categories derive from `n_chesscom_games` / `n_lichess_games` on the upserted RatingAnchorRow (suppressed = no row produced for that user × TC)."
    - "Per-user transactional commit discipline preserved verbatim from Plan 06 — each user's anchors + percentile rows commit in a single transaction; partial failures do NOT leave inconsistent state. Plan 09's migration already TRUNCATEd both `user_rating_anchors` and `user_benchmark_percentiles`, so this backfill is a from-scratch repopulate. The backfill script is idempotent at row level (UPSERT semantics on both tables); a half-completed run can be safely re-run end-to-end without corrupting already-written rows."
    - "Dev backfill executes cleanly against the existing dev DB (NO `bin/reset_db.sh` per memory `feedback_no_dev_db_reset_in_plans`). Steps: ensure dev DB is up via `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`; ensure migrations are applied via `uv run alembic upgrade head` (Plan 09's migration ships in the merged PR — `head` includes the reshape migration); run `uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-27`. The new composition summary table appears in the log. **Recovery contract**: if the dev backfill exits non-zero, re-run end-to-end (UPSERT-idempotent) — do NOT advance to the prod gate until a clean exit-0 dev run is achieved."
    - "Prod backfill is HUMAN-GATED. The Wave 1 + Wave 2 PR (Plans 09 + 10 + 11) must be merged to `main`, promoted to `production`, deployed via `bin/deploy.sh` (which runs the Alembic migration on container start via the deploy entrypoint, TRUNCATing prod's UBP + URA tables). Then the operator opens the SSH tunnel via `bin/prod_db_tunnel.sh`, runs `uv run python scripts/backfill_user_percentiles.py --target prod --snapshot-date 2026-05-27`, monitors Sentry during the run, and closes the tunnel. Per CLAUDE.md prod-deploy discipline, this task is `autonomous: false`. **Prod recovery contract**: if prod backfill fails mid-run, the half-populated state is detectable via `SELECT COUNT(*) FROM user_rating_anchors WHERE computed_at > NOW() - INTERVAL '1 hour'`; re-run via the tunnel is safe (UPSERT). Operator MUST NOT modify production data manually."
    - "Final HUMAN-UAT re-runs the PRPCR-09 credibility check on a MIXED-PLATFORM user (the population whose anchor changes under the D-12 reversal). Specifically: pick an account that has BOTH chess.com and lichess games in at least one TC, open the chip tooltip, and confirm bullet 4 reads the blended-composition prose ('Anchored at ~X Elo, blending N chess.com games (median Y, converted) with M lichess games (median Z).'). The user's PERCEIVED PERCENTILE SHIFT vs the pre-amendment state is informally checked — for a user whose chess.com strength is significantly higher than their lichess strength, the new anchor should be HIGHER than the previous lichess-only anchor, and the percentile (within the cohort CDF) should DECREASE (the user is now competing against stronger cohort peers). The UAT records the observed shift direction + magnitude vs expectation."
    - "UAT also confirms: (a) pure-lichess users see UNCHANGED anchor + percentile vs pre-amendment (regression guard for the amendment's 'risk profile' guarantee); (b) pure-chess.com users see NEARLY-UNCHANGED anchor (median commutativity holds; ≤ ~20 Elo shift expected); (c) chess.com-Daily-only classical user has no classical chip (suppression intact); (d) mobile parity at 375px; (e) NO flame icon at any percentile (Plan 07 / Plan 11 regression guard verified visually); (f) tooltip bullet 4 is readable on mobile without horizontal scroll."
  artifacts:
    - path: scripts/backfill_user_percentiles.py
      provides: extended backfill with composition-typed summary table (pure_lichess / pure_chesscom / mixed / suppressed)
      contains: "pure_lichess\\|pure_chesscom\\|mixed\\|suppressed"
    - path: .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-12-HUMAN-UAT.md
      provides: UAT outcome record (operator-completed)
      contains: "mixed-platform\\|UAT\\|blending"
      min_lines: 40
  key_links:
    - from: scripts/backfill_user_percentiles.py
      to: app/services/user_benchmark_percentiles_service.py compute_anchors_for_user (Plan 10)
      via: backfill iterates users and calls the blended-anchor compute path
      pattern: "compute_anchors_for_user"
    - from: 94.4-12-HUMAN-UAT.md
      to: .planning/notes/percentile-anchor-d12-reversal.md §"Risk profile"
      via: UAT verifies the surgical-fix risk profile (mixed shifts, pure unchanged) holds in practice
      pattern: "mixed\\|pure\\|risk profile"
---

<objective>
Execute the D-12 reversal backfill against dev (autonomous) and prod (HUMAN-gated), then run a final HUMAN-UAT confirming the amendment's risk profile in practice: mixed-platform users see the expected anchor shift; pure-platform users see negligible change; NO flame icons; tooltip composition disclosure reads correctly on mobile + desktop.

Purpose: Plan 09's migration TRUNCATEd both derived tables — the production user base has no anchors and no percentiles until this backfill runs. The backfill is the final step that turns the schema + compute changes into observable user-facing changes. The HUMAN-UAT is the credibility-recovery sign-off for the amendment, paralleling Plan 06's PRPCR-09 UAT but specifically targeted at the mixed-platform population that the amendment was designed to fix.

Output: 1 backfill script edit + 1 NEW HUMAN-UAT record file + 1 dev backfill run (autonomous) + 1 prod backfill run (HUMAN-gated) + 1 mixed-platform UAT session (HUMAN-gated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/notes/percentile-anchor-d12-reversal.md
@scripts/backfill_user_percentiles.py
@app/services/user_benchmark_percentiles_service.py
@app/repositories/user_rating_anchors_repository.py
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-06-HUMAN-UAT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-06-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Anchors:
     - scripts/backfill_user_percentiles.py current shape post-Plan 06 — per-(TC × source_platform) anchor summary
     - The composition derives from RatingAnchorRow fields written by compute_anchors_for_user (Plan 10)
     - HUMAN-UAT file pattern: .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-06-HUMAN-UAT.md (Plan 06's output) -->

Composition classification (Python):
  if row.n_chesscom_games > 0 and row.n_lichess_games > 0:
      composition = "mixed"
  elif row.n_chesscom_games > 0:
      composition = "pure_chesscom"
  elif row.n_lichess_games > 0:
      composition = "pure_lichess"
  else:
      composition = "suppressed"  # shouldn't materialise — no row produced

Composition summary table format (printed at the end of the backfill run):
  Anchor Composition Summary
  --------------------------
  TC          mixed    pure_lichess    pure_chesscom    no_anchor
  bullet      ...      ...             ...              ...
  blitz       ...      ...             ...              ...
  rapid       ...      ...             ...              ...
  classical   ...      ...             ...              ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/backfill_user_percentiles.py with composition-typed anchor summary</name>
  <files>scripts/backfill_user_percentiles.py</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (full file — find the existing anchor summary block, likely a `_AnchorSummary` dataclass or dict; understand how it currently groups by source_platform)
    - app/repositories/user_rating_anchors_repository.py (post-Plan 09 — the 5-field RatingAnchorRow shape; n_chesscom_games + n_lichess_games drive composition)
  </read_first>
  <action>
    Edit `scripts/backfill_user_percentiles.py`:

    1. Find the existing anchor summary tracker — likely a dataclass or dict that increments per `(tc, source_platform)`. Replace it with a per-`(tc, composition)` counter where `composition` is one of `"mixed" | "pure_lichess" | "pure_chesscom" | "no_anchor"`. Implementation: a small `_classify_composition(row: RatingAnchorRow) -> str` helper per the `<interfaces>` block. The `"no_anchor"` bucket counts users who got NO anchor for that TC (compute_anchors_for_user returned a dict without that TC) — track this via a per-TC counter incremented when the dict misses the key.

    2. Within the per-user loop, after `compute_anchors_for_user` returns the dict, iterate the 4 TCs:
       ```python
       for tc in _ALL_TIME_CONTROLS:
           anchor_row = anchors.get(tc)
           if anchor_row is None:
               composition_counts[tc]["no_anchor"] += 1
           else:
               composition_counts[tc][_classify_composition(anchor_row)] += 1
       ```

    3. At the end of the run, print the composition summary table per `<interfaces>` (4 rows × 4 cols, padded for readability). Use the same printing style as the existing percentile summary table — `print(f"{tc:<12}{mixed:<8}{pure_lichess:<16}{pure_chesscom:<16}{no_anchor:<10}")` or similar.

    4. The existing per-(metric × TC) PERCENTILE summary table (Plan 06's main output) STAYS UNCHANGED. Only the ANCHOR summary table changes — the percentile compute path is downstream of the anchor and reads it via the dict, so the percentile flow doesn't care about composition.

    5. `--skip-anchors` flag from Plan 06 stays — used for percentile-only re-runs after the anchor table is populated.

    6. `--target dev|prod` URL guard preserved verbatim (the V5 mitigation from Plan 06).

    7. Update the CLI usage docstring at the top of the script to document the new composition-typed summary.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_user_percentiles.py --help 2>&1 | head -30 && grep -c 'composition\|pure_lichess\|pure_chesscom\|mixed.*no_anchor' scripts/backfill_user_percentiles.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pure_lichess\|pure_chesscom\|composition\|_classify_composition' scripts/backfill_user_percentiles.py` is >= 3.
    - `grep -c 'source_platform' scripts/backfill_user_percentiles.py` is 0 (legacy grouping removed).
    - `grep -c 'n_chesscom_games\|n_lichess_games' scripts/backfill_user_percentiles.py` is >= 2.
    - `--target prod` URL guard preserved (grep for `_assert_prod_db` or the equivalent).
    - `--skip-anchors` flag still present.
    - `uv run python scripts/backfill_user_percentiles.py --help` shows the composition-summary line.
    - `uv run ty check scripts/backfill_user_percentiles.py` returns zero errors.
  </acceptance_criteria>
  <done>Backfill script anchor summary reshaped for composition typing; percentile path untouched; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Dev backfill — run backfill_user_percentiles.py --target dev; verify composition summary; spot-check a mixed user via MCP query</name>
  <files>(no file modified — execution + verification only)</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (post-Task 1)
    - .planning/notes/percentile-anchor-d12-reversal.md (worked example — pick a comparable dev user to spot-check if possible)
  </read_first>
  <action>
    Pre-flight:
    1. `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` (ensure dev DB up).
    2. `uv run alembic upgrade head` (apply Plan 09's reshape migration if not already applied; the migration TRUNCATEs `user_rating_anchors` and `user_benchmark_percentiles`).
    3. Confirm migration applied: `psql 'postgresql://flawchess:flawchess@localhost:5432/flawchess' -c "\d user_rating_anchors"` shows the new column set (n_chesscom_games etc., no source_platform).

    Run the backfill:
    `uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-27 2>&1 | tee /tmp/d12_reversal_backfill_dev.log`

    Monitor the output:
    - Per-100-users progress line appears periodically.
    - Sentry exceptions are minimal (a handful of edge cases at most).
    - Final composition summary table prints with 4 TCs × 4 categories.
    - Final percentile summary table prints (preserved from Plan 06).

    Post-run sanity-checks via `mcp__flawchess-db__query`:
    - `SELECT count(*) FROM user_rating_anchors` > 0.
    - `SELECT count(*) FROM user_benchmark_percentiles` > 0.
    - `SELECT count(*) FROM user_rating_anchors WHERE n_chesscom_games > 0 AND n_lichess_games > 0` — count of mixed users; should be non-zero for a dev DB with realistic test data.
    - `SELECT user_id, time_control_bucket, anchor_rating, n_chesscom_games, n_lichess_games, chesscom_median_native, lichess_median_native FROM user_rating_anchors WHERE n_chesscom_games > 0 AND n_lichess_games > 0 LIMIT 5` — sample 5 mixed-user rows and confirm:
      (a) `anchor_rating` is between `lichess_median_native` and the chess.com lichess-equivalent (a manual conversion check using the snapshot).
      (b) `n_chesscom_games + n_lichess_games >= MEDIAN_ANCHOR_MIN_GAMES`.
      (c) Both native medians are populated (not NULL) for mixed users.

    Smoke-test the API end-to-end:
    - Start backend: `uv run uvicorn app.main:app --reload`.
    - Hit a known endpoint that returns `rating_anchors` (e.g. the endgame overview endpoint — find via grep on `rating_anchors`).
    - Inspect JSON for the new field set: `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native`. Confirm `source_platform` is GONE from the response.

    If anything breaks (Sentry exception storm, schema mismatch, API 500), debug HERE before unblocking the prod gate.

    **Recovery — dev backfill non-zero exit**:
    If the backfill exits with a non-zero code, the dev DB is left in a partially-populated state. The script is idempotent (UPSERT semantics on both `user_rating_anchors` and `user_benchmark_percentiles`) — re-running the backfill end-to-end will populate missing rows AND overwrite previously-written rows with the same correct values; it will NOT corrupt already-written rows.

    Standard recovery procedure:
    1. Inspect the Sentry / stderr exceptions. If a pattern emerges (e.g., all failures concentrated on `classical` TC, or only on chess.com-Daily-only users, or all involving a particular field type), it indicates an underlying bug in `compute_anchors_for_user` (Plan 10) or in the SQL builder (Plan 10 Task 1). Investigate and FIX THE UNDERLYING EXCEPTION first.
    2. If exceptions look transient (e.g., DB connection blip, a single corrupted user record), the bug is environmental, not in code. Skip the fix step.
    3. Re-run end-to-end: `uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-27 2>&1 | tee -a /tmp/d12_reversal_backfill_dev_rerun.log`. The `-a` append flag preserves the original log for forensics.
    4. Confirm clean exit-0 with the composition summary table fully populated.

    DO NOT advance to Task 3 (prod gate) until a clean exit-0 dev run is achieved. The Wave 1 + Wave 2 PR (Plans 09 + 10 + 11) must not be merged to production while the dev backfill is broken — a broken dev backfill almost always indicates a code-level bug that will reproduce on prod.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-27 2>&1 | tee /tmp/d12_reversal_backfill_dev.log | tail -40 && grep -c 'mixed\|pure_lichess\|pure_chesscom' /tmp/d12_reversal_backfill_dev.log
</automated>
  </verify>
  <acceptance_criteria>
    - Backfill completes without unhandled exceptions; exit code 0 (after at most one re-run on transient failure per the Recovery section).
    - Final composition summary table prints with all 4 TCs.
    - `SELECT count(*) FROM user_rating_anchors WHERE n_chesscom_games > 0 AND n_lichess_games > 0` > 0 on the dev DB (mixed users exist).
    - For at least one sampled mixed-user row: `anchor_rating` falls between `lichess_median_native` and the converted chess.com value (a manual calc using `CHESSCOM_BLITZ_TO_LICHESS`).
    - API smoke test: a `rating_anchors` JSON block returns the new 5-field shape; no `source_platform` field present.
    - If a Sentry pattern was observed and an underlying bug was fixed, the SUMMARY.md records: (a) the pattern observed; (b) the file + symbol of the underlying bug; (c) the fix; (d) confirmation the re-run is clean.
  </acceptance_criteria>
  <done>Dev backfill complete with clean exit-0 (after at most one re-run on transient failure); composition summary printed; mixed-user spot-check confirms blended anchor falls between per-platform medians; API smoke green. Prod gate (Task 3) is unblocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-VERIFY — Operator opens prod tunnel, runs prod backfill, monitors Sentry, closes tunnel</name>
  <files>(no file modified — operator-driven script execution via SSH tunnel)</files>
  <action>Operator-driven prod backfill. See `<how-to-verify>` for the exact procedure, including the **prod-specific recovery contract** for mid-run failures. Claude does not write code in this task.</action>
  <what-built>Plans 09 + 10 + 11 merged to main; promoted to production; deployed via `bin/deploy.sh` which runs the Alembic migration on container start (Plan 09's migration TRUNCATEs both UBP and URA on prod). This task repopulates them via the SSH tunnel.</what-built>
  <how-to-verify>
    1. **Pre-flight — confirm the Wave 1 + Wave 2 PR is shipped to prod**: `gh run list --limit 5` shows a recent successful deploy on the `production` branch. `ssh flawchess "cd /opt/flawchess && docker compose logs --tail=80 backend | grep -i 'alembic\\|migration'"` confirms Plan 09's reshape migration ran (look for "reshape_user_rating_anchors_for_blended_anchor" in the alembic output).
    2. **Open the tunnel**: `bin/prod_db_tunnel.sh` (forwards prod Postgres to `localhost:15432`). Verify: `psql 'postgresql://flawchess_ro:<password>@localhost:15432/flawchess' -c "\d user_rating_anchors"` shows the new column set with `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native` and NO `source_platform`.
    3. **Sanity-check prod DB state**: `SELECT count(*) FROM user_rating_anchors` returns 0 (truncated by migration). `SELECT count(*) FROM user_benchmark_percentiles` returns 0.
    4. **Run the backfill**: `uv run python scripts/backfill_user_percentiles.py --target prod --snapshot-date 2026-05-27`. Estimated runtime: depends on user count × per-user transaction time; typically 5-30 minutes for a few hundred prod users.
    5. **Monitor Sentry** during the run at `https://flawchess.sentry.io`. A handful of edge-case exceptions is expected; a flood is a red flag — Ctrl-C, investigate, fix, re-run per the Recovery contract below.
    6. **Inspect summary tables**: at the end, the script prints the composition summary (4 TCs × 4 categories) + the percentile summary (8 metrics × 4 TCs). Confirm totals roughly match expectation — for a typical prod user base, expect `mixed` to be a meaningful fraction (the population the amendment targets).
    7. **Post-backfill sanity**:
       - `SELECT count(*) FROM user_rating_anchors GROUP BY (n_chesscom_games > 0, n_lichess_games > 0)` — counts the 4 composition categories.
       - `SELECT count(*) FROM user_benchmark_percentiles GROUP BY metric` — counts roughly equal to prod user count × (1 - suppression_rate) for each metric.
    8. **Close the tunnel**: `bin/prod_db_tunnel.sh stop`.
    9. **Post-deploy smoke test on prod**: open `https://flawchess.com`, log in (a mixed-platform account is best — has both chess.com and lichess games), inspect the chip + tooltip on the Endgames page. Confirm the new bullet 4 prose renders ("blending N chess.com games... with M lichess games..."). If the operator's account is pure-platform, this smoke is partial; Task 4 covers the mixed-platform UAT specifically.

    **Recovery — prod backfill mid-run failure (CRITICAL operational discipline)**:

    If the prod backfill exits non-zero or is Ctrl-C'd due to a Sentry exception flood, the prod database is in a HALF-POPULATED state: some users have anchors + percentiles, others do not. This is RECOVERABLE; do not panic.

    Recovery procedure:
    1. **Verify half-populated state is detectable**: with the tunnel open, run:
       ```sql
       SELECT COUNT(*) FROM user_rating_anchors WHERE computed_at > NOW() - INTERVAL '1 hour';
       ```
       A non-zero count confirms partial progress. (The migration set all rows' `computed_at` to the migration moment; subsequent backfill writes set `computed_at = NOW()` per the upsert helper. Any rows with `computed_at` within the last hour were written by THIS backfill run, not the migration.)
    2. **DO NOT modify production data manually** — no manual UPDATE / DELETE / TRUNCATE / INSERT against `user_rating_anchors` or `user_benchmark_percentiles` from psql or the MCP tool. Manual edits desync the anchor + percentile tables, and the next backfill run will produce inconsistent state for affected users.
    3. **Investigate the failure cause from the script's stderr + Sentry**:
       - If Sentry shows a uniform pattern (e.g., all failures on a single TC, all failures involving NULL `chesscom_median_native` for a malformed user), the bug is in code (Plan 10's anchor compute or Plan 11's schema). Open a follow-up `/gsd-quick` task to investigate and fix; DO NOT proceed with the backfill until the fix is shipped (PR + deploy + migration).
       - If Sentry shows a transient pattern (e.g., DB connection blip, single corrupted user record, single asyncio cancellation), the bug is environmental. Skip the fix step.
    4. **Re-run via the tunnel** (after re-opening if necessary): `uv run python scripts/backfill_user_percentiles.py --target prod --snapshot-date 2026-05-27 2>&1 | tee -a /tmp/d12_reversal_backfill_prod_rerun.log`. The script is idempotent (UPSERT on `(user_id, time_control_bucket)` for anchors and `(user_id, metric, time_control_bucket)` for percentiles); re-running is safe and will populate the missing rows while overwriting the existing rows with the same correct values.
    5. **Confirm clean exit-0** with both summary tables printed.
    6. **If a code-level fix was required between attempts**, document in 94.4-12-SUMMARY.md: (a) the Sentry pattern; (b) the underlying bug + fix PR link; (c) the prod-tunnel re-run timestamp + outcome.

    The fundamental safety property: `user_rating_anchors` and `user_benchmark_percentiles` are DERIVED tables. Worst case (catastrophic recovery), TRUNCATE both via the Alembic migration's downgrade path or a hotfix migration, then re-run the backfill from scratch. The script is the source of truth for these tables; the games table (the primary source) is untouched by this backfill.
  </how-to-verify>
  <resume-signal>Type "prod backfill complete" if everything ran cleanly (with at most one re-run per the Recovery contract). Type "abort: {issue}" if Sentry showed an exception flood that requires a code-level fix before re-running — the fix goes in a follow-up hotfix PR.</resume-signal>
  <verify>
    <human-check>Operator confirms: (1) prod tunnel opened + closed; (2) Plan 09 migration verified to have TRUNCATEd both tables; (3) backfill completed with both summary tables printed (after at most one re-run via the documented Recovery procedure on transient failure); (4) Sentry shows no exception flood during the FINAL successful backfill window; (5) `SELECT count(*) FROM user_rating_anchors` and `SELECT count(*) FROM user_benchmark_percentiles` on prod return > 0 row counts consistent with the user base; (6) at least one mixed-platform sample row shows non-NULL native medians for both platforms; (7) NO manual data modification was performed against prod — any partial-state recovery used the script re-run path only.</human-check>
  </verify>
  <done>Prod backfill complete; both derived tables repopulated with blended-anchor shape; Sentry clean on the final run; tunnel closed; no manual data edits performed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-VERIFY — Mixed-platform UAT (PRPCR-09 credibility re-check under the D-12 reversal)</name>
  <files>.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-12-HUMAN-UAT.md (NEW — operator records UAT outcome)</files>
  <action>Operator-driven UAT against a mixed-platform account on `https://flawchess.com`. Claude does not write code in this task. The operator records the outcome in `94.4-12-HUMAN-UAT.md`.</action>
  <what-built>Post-prod-backfill, the cohort-percentile chip tooltip surfaces the blended-anchor composition. PRPCR-09 is re-verified, this time targeting the population the D-12 reversal specifically affects: mixed-platform users.</what-built>
  <how-to-verify>
    Identify a MIXED-PLATFORM user account on prod — one with games on BOTH chess.com and lichess in at least one TC. If the operator's own account qualifies, use it; otherwise pick a known elite-user or test fixture that has dual-platform activity.

    1. **Mixed-user composition disclosure**: log in as the mixed-platform user. Navigate to the Endgames page. Hover or tap a chip to open the tooltip. Confirm bullet 4 reads the blended-composition prose (paraphrased): "Anchored at ~{X} Elo, blending {N} chess.com games (median {Y}, converted) with {M} lichess games (median {Z})." Record the actual rendered text in the UAT file.
    2. **Numeric sanity**: confirm the 5 numbers in bullet 4 are internally consistent:
       - `X` (anchor_rating) is plausibly between `Y` (after ChessGoals conversion from chess.com TC -> lichess TC; look up the snapshot row mentally or via the conversion function) and `Z` (lichess_median_native).
       - `N + M >= 30` (MEDIAN_ANCHOR_MIN_GAMES floor).
       - `X` is a sensible game-count-weighted median given `N` games at ~converted-`Y` + `M` games at ~`Z`.
    3. **Pre-vs-post comparison (informal)**: if the operator remembers or has a screenshot of this account's pre-amendment percentile (when the anchor was lichess-only), compare. For a user with significantly stronger chess.com play than lichess play, expect:
       - New anchor HIGHER than old anchor (the chess.com strength now pulls the anchor up).
       - New percentile within the cohort CDF LOWER than old percentile (stronger cohort peers = harder to be high-percentile).
       Record observed shift direction + rough magnitude. If shift is OPPOSITE to expectation, surface as a UAT failure — investigate before signing off.
    4. **Pure-lichess regression guard**: switch to (or have on hand) a pure-lichess account. Confirm the bullet 4 reads "Anchored at ~{X} Elo from {M} lichess games (native rating)." Confirm anchor_rating + percentile are UNCHANGED vs pre-amendment (the amendment's risk profile guarantees this). Record observed values.
    5. **Pure-chess.com regression guard**: ditto for a pure-chess.com account. Bullet 4 reads "Anchored at ~{X} Elo from {N} chess.com games (median {Y}, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26)." Anchor should be NEARLY UNCHANGED vs pre-amendment (median commutativity; expect <= ~20 Elo shift).
    6. **chess.com-Daily-only classical suppression**: if a Daily-only account or test fixture is available, confirm classical chip is suppressed (no chip rendered, no tooltip).
    7. **Mobile parity at 375px**: Chrome DevTools mobile emulation (or actual phone). Tooltip body remains readable within viewport; no horizontal scroll; the blended-composition prose wraps gracefully.
    8. **NO flame icon at any percentile**: scroll the page and confirm visually that no chip renders a flame, even at high-percentile cells like `p95` or `p99`. (HARD CONSTRAINT per Plan 07 / Plan 11 regression guard.)
    9. **Filter independence**: change a UI filter (time control / platform / recency). Confirm the chip + tooltip values do NOT change (filter independence per bullet 3).

    Record the UAT outcome in `94.4-12-HUMAN-UAT.md` with:
    - Tested user count + platform mix per tested account.
    - For each tested account: composition tag (mixed / pure-lichess / pure-chess.com), anchor_rating observed, n_chesscom_games + n_lichess_games observed, native medians observed, bullet 4 text transcribed verbatim.
    - Pre-vs-post comparison for at least one mixed-platform account (qualitative if no exact pre-amendment screenshot is available).
    - Mobile parity check at 375px (pass / fail + notes).
    - No-flame visual confirmation (pass / fail).
    - Any anomalies (unexpected shift direction, missing native medians on a mixed row, popover layout overflow on mobile, etc.).

    PRPCR-09 credibility-recovery sign-off is APPROVED iff:
    (a) bullet 4 prose reads correctly for all 3 composition branches;
    (b) the mixed-user shift direction matches expectation (or the absence of shift is justified — e.g. the test mixed user has near-equal native ratings across platforms);
    (c) no flame icon at any percentile;
    (d) mobile parity holds at 375px.

    If any of the four conditions fails, the UAT result is FAILED and the operator opens a follow-up plan or hotfix.
  </how-to-verify>
  <resume-signal>Type "UAT approved" + 1-2 line observation summary. OR "UAT failed: {what's wrong}" + propose follow-up.</resume-signal>
  <verify>
    <human-check>Operator confirms on `https://flawchess.com`: (1) mixed-platform tooltip bullet 4 renders blended-composition prose with all 5 numbers; (2) pure-lichess + pure-chess.com tooltips render their respective shortened bullet-4 copies; (3) pre-vs-post comparison on a mixed-platform account shows shift in the expected direction (or is justifiably negligible); (4) no flame icon at any percentile; (5) mobile parity at 375px holds; (6) chess.com-Daily-only classical suppresses (if testable).</human-check>
  </verify>
  <done>D-12 reversal UAT signed off; 94.4-12-HUMAN-UAT.md records observations; Phase 94.4 amendment scope verified end-to-end in production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local script -> prod Postgres | Via `bin/prod_db_tunnel.sh` SSH tunnel; URL guard ensures script only runs against the tunneled prod URL pattern (V5 mitigation, Plan 06 / Plan 09 lineage). |
| Operator -> prod backfill | Manual gate per CLAUDE.md prod-deploy discipline. |
| Operator -> prod data (recovery path) | Recovery from mid-run failure MUST be via script re-run only; manual UPDATE / DELETE / TRUNCATE against prod is explicitly prohibited (would desync derived tables). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-12-01 | Tampering | Backfill targets the wrong DB | mitigate | `--target dev\|prod` URL guard from Plan 06 preserved; `--target prod` calls `_assert_prod_db(url)` checking URL pattern. Operator manually opens tunnel before `--target prod`. |
| T-94.4-12-02 | Denial of service | Prod backfill saturates DB pool during business hours | accept | Per-user transactional commits + per-user session lifecycle; pool exhaustion unlikely on the upgraded CPX42. Operator can Ctrl-C to abort. |
| T-94.4-12-03 | Information disclosure | Backfill log contains user-identifying data | accept | Aggregate counts; per-user error logs use Sentry context (not message) — consistent with Phase 94.1/94.2/94.3/Plan-06 backfill discipline. |
| T-94.4-12-04 | Tampering | Operator manually edits prod data during recovery, desyncing derived tables | mitigate | Recovery contract in Task 3 `<how-to-verify>` explicitly prohibits manual data edits and documents the safe re-run path; UPSERT idempotence makes script re-run the canonical recovery. |
| T-94.4-12-SC | Tampering | npm/pip installs | accept | No new packages. |
</threat_model>

<verification>
- Dev backfill completes cleanly (after at most one re-run on transient failure per the Recovery contract); composition summary table prints.
- Prod backfill HUMAN-VERIFY checkpoint passed; Sentry has no exception flood on the final run; both derived tables repopulated under the new schema; no manual data edits performed during any recovery path.
- Mixed-platform UAT HUMAN-VERIFY checkpoint passed; tooltip bullet 4 renders correctly for all 3 composition branches; pre-vs-post shift on mixed users matches the amendment's expected direction; no flame icon at any percentile; mobile parity holds.
- 94.4-12-HUMAN-UAT.md records observations for traceability.
</verification>

<success_criteria>
PRPCR-03 + PRPCR-09 ROADMAP scope is now fully implemented end-to-end under the D-12 reversal. Both dev and prod databases hold the blended-anchor shape; both percentile and anchor data are repopulated; the user-facing chip tooltip discloses the per-platform composition truthfully. The mixed-platform population — the surgical target of the amendment — sees their anchor shift toward the platform contributing more games, with the percentile lookup against the cohort CDF now reading honestly. The Phase 94.4 amendment scope is operationally complete; Plan 13 closes the documentation loop. Operational discipline: recovery from any mid-run failure used the documented script-re-run path (UPSERT-idempotent), not manual SQL edits.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-12-SUMMARY.md` when done. Record: dev backfill runtime + composition summary table verbatim + any re-runs needed and their root cause; prod backfill runtime + same summary + any re-runs needed and their root cause; UAT outcome with tested account count + observed shifts on mixed accounts; any Sentry exceptions captured during prod backfill and the follow-up tickets opened.
</output>
