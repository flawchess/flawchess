---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 08
type: execute
wave: 4
depends_on: [94.4-06, 94.4-07]
files_modified:
  - .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md
  - $HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md
  - .planning/ROADMAP.md
  - CHANGELOG.md
autonomous: true
requirements: [PRPCR-08]

must_haves:
  truths:
    - "`.planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md` opens with a `## Superseded by SEED-026 v2 (Phase 94.4)` banner at the top of the file per ROADMAP SC-8 + PRPCR-08. The 'Final Tier-1 chip set' section is annotated as superseded; historical Cohen's d / Spearman ρ analysis is preserved as context (no longer load-bearing — cohort absorbs coupling)."
    - "`$HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md` is amended to reflect the new 4-bullet contract per CONTEXT D-07a + CONTEXT line 150 + 157. The 4th bullet flips from per-metric rating-correlation disclosure to rating-anchor disclosure (Lichess-anchored vs chess.com-anchored-and-converted). The memory file's footer adds an amendment line: `Amended 2026-05-XX (Phase 94.4): 4th bullet content changed from per-metric rating-correlation to rating-anchor disclosure per CONTEXT D-07. Reason: peer-relative cohort absorbs rating coupling.`"
    - "`.planning/ROADMAP.md` Phase 94.4 'Plans' line finalizes to a concrete plan-block listing the 10 plans (`94.4-01-PLAN.md`, `94.4-02-PLAN.md`, `94.4-03-PLAN.md`, `94.4-04-PLAN.md`, `94.4-05a-PLAN.md`, `94.4-05b-PLAN.md`, `94.4-05c-PLAN.md`, `94.4-06-PLAN.md`, `94.4-07-PLAN.md`, `94.4-08-PLAN.md`) with one-line objectives. Plan 05 was split into 05a/05b/05c per checker B5 fix (context-cost split; atomic-cutover preserved via depends_on + same-PR discipline). The phase is NOT marked complete here — completion is a future operator action via `/gsd:close-phase` after Plan 06's HUMAN-UAT signs off."
    - "`CHANGELOG.md` `## [Unreleased]` section gains a Phase 94.4 entry under `### Changed` (chip UX shrink + tooltip rewrite) and `### Added` (Recovery Score Gap chip slot rescued) per CLAUDE.md 'Per-phase' changelog discipline. Tone is user-facing and terse — what changed, not how."
  artifacts:
    - path: .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md
      provides: Superseded banner + Tier-1 chip set annotation
      contains: "Superseded by SEED-026"
    - path: $HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md
      provides: 4-bullet contract amendment
      contains: "Amended 2026-05\\|rating-anchor disclosure"
    - path: .planning/ROADMAP.md
      provides: finalized Plans block for Phase 94.4
      contains: "94.4-01-PLAN.md\\|94.4-02-PLAN.md\\|94.4-03-PLAN.md\\|94.4-04-PLAN.md\\|94.4-05-PLAN.md\\|94.4-06-PLAN.md\\|94.4-07-PLAN.md\\|94.4-08-PLAN.md"
    - path: CHANGELOG.md
      provides: Phase 94.4 entry under [Unreleased]
      contains: "Phase 94.4\\|peer-relative"
  key_links:
    - from: .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md
      to: .planning/seeds/SEED-026-percentile-chip-refinement.md
      via: Supersession banner links to the seed that drove Phase 94.4
      pattern: "SEED-026"
---

<objective>
Post-merge housekeeping for Phase 94.4. Marks SEED-019 §"Final Tier-1 chip set" as superseded per PRPCR-08 / ROADMAP SC-8, amends the `feedback_percentile_chip_tooltip_disclosure.md` user-memory to reflect the new 4-bullet contract per CONTEXT D-07a, finalizes the ROADMAP Plans block for Phase 94.4, and appends the CHANGELOG `[Unreleased]` entry per CLAUDE.md per-phase changelog discipline.

Purpose: PRPCR-08 is a documentation-only requirement that closes the loop on the v1 framing. SEED-019 § "Final Tier-1 chip set" recommended a 4-chip set computed under global-pool framing with explicit Recovery / Conversion drops; SEED-026 v2 (this phase) reverses the recovery/conversion drops and ships under peer-relative framing. Without the supersession marker, future readers may assume SEED-019's recommendations still apply. The memory amendment closes a similar loop on the 4th-bullet contract.

Output: 4 file edits, no code. Wave 4 — runs AFTER Wave 2 (Plan 06's prod backfill + UAT) AND Wave 3 (Plan 07's frontend chip rewrite) merge, because the CHANGELOG entry summarizes the full Phase 94.4 surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md
@.planning/seeds/SEED-026-percentile-chip-refinement.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Superseded by SEED-026 v2 (Phase 94.4)" banner to SEED-019 + annotate "Final Tier-1 chip set" section</name>
  <files>.planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md</files>
  <read_first>
    - .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md (in full — locate the "Final Tier-1 chip set" section and the top of the file for banner placement)
    - .planning/seeds/SEED-026-percentile-chip-refinement.md (skim — confirm SEED-026 v2 is the canonical successor)
  </read_first>
  <action>
    Edit `.planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md`. At the very top of the file (immediately after the title H1), insert:
    ```markdown
    > **⚠ Superseded by [SEED-026 v2](./SEED-026-percentile-chip-refinement.md) (Phase 94.4, shipped 2026-05-XX).**
    >
    > Phase 94.4 pivoted the chip from global-pool to peer-relative cohort framing. The "Final Tier-1 chip set" section below is superseded — Recovery Score Gap and Conversion Score Gap are rescued from the v1 drop list under peer-relative framing (per CONTEXT D-05 / D-05a). The Cohen's d / Spearman ρ analysis is preserved as historical context but is no longer load-bearing — the cohort comparison absorbs the rating coupling the candidacy table was designed to flag.
    >
    > For the current Phase 94.4 design source-of-truth, see SEED-026 v2.
    ```
    
    Find the "Final Tier-1 chip set" section (search for the heading text). Just under the heading, insert:
    ```markdown
    > **SUPERSEDED 2026-05-XX (Phase 94.4):** the 4-chip set recommended in this section was computed under global-pool framing. Phase 94.4 ships 8 chip families under peer-relative framing — Endgame Score Gap, Achievable Score Gap, Parity ΔES, Conversion ΔES, **Recovery ΔES (rescued)**, plus per-TC Time Pressure family. See SEED-026 v2 for current design.
    ```
    Use today's date (replace XX with the actual day-of-month at execution time). Do NOT delete the original section content — it's preserved as historical context per the supersession banner.
  </action>
  <verify>
    <automated>grep -c "Superseded by SEED-026\|SUPERSEDED 2026-05" .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md</automated>
  </verify>
  <acceptance_criteria>
    - Top-of-file banner present (`grep -c "Superseded by SEED-026"` is ≥ 1).
    - Inline annotation on "Final Tier-1 chip set" section (`grep -c "SUPERSEDED 2026-05"` is ≥ 1).
    - Original section content is preserved (not deleted) — `wc -l` on the file is roughly the prior count + ~10 lines (banner + annotation).
    - The supersession links to SEED-026 v2.
  </acceptance_criteria>
  <done>SEED-019 supersession marker landed; PRPCR-08 / ROADMAP SC-8 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Amend feedback_percentile_chip_tooltip_disclosure.md memory to reflect the new 4-bullet contract</name>
  <files>$HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md</files>
  <read_first>
    - $HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md (in full — confirm the current 4-bullet contract definition; the v1 4th bullet was per-metric rating-correlation framing per Phase 94.3)
    - CONTEXT D-07 + D-07a (the locked decision to flip the 4th bullet from rating-correlation to rating-anchor disclosure)
  </read_first>
  <action>
    Edit `$HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md`. Append an "Amendment 2026-05-XX (Phase 94.4)" section at the end of the file:
    ```markdown
    ## Amendment 2026-05-XX (Phase 94.4)
    
    The 4-bullet contract is updated under peer-relative framing per Phase 94.4 CONTEXT D-07 / D-07a. Bullet content evolves as follows:
    
    1. **Cohort framing** (NEW) — leads with anchor + TC inline ("Compared to other ~{anchor}-rated players in {tc}." OR "...aggregated across the time controls you play." for aggregated chips).
    2. **Recent-games basis** (PRESERVED, TC-scoped form) — "Based on your most recent 1000 rated games in {tc} over the last 36 months, vs opponents within +/-100 Elo."
    3. **Filter independence** (PRESERVED VERBATIM) — `COPY_FILTER_INDEPENDENCE` from `PercentileChip.tsx`.
    4. **Rating-anchor disclosure** (REPLACES per-metric rating-correlation) — Lichess-anchored: "Anchored on your Lichess {tc} ({anchor_rating})." OR chess.com-anchored: "Anchored on your chess.com {tc} ({raw_rating} -> {anchor_rating} Lichess-equivalent via ChessGoals snapshot 2026-05-26)."
    
    The v1 4th bullet (per-metric rating-correlation framing, sourced from `RATING_NOTE_BY_FLAVOR` map) retires. Cohort absorbs ELO coupling, so the v1 hedge ("this metric is mostly independent of rating" / "tracks rating strongly") is no longer load-bearing. The `RATING_NOTE_BY_FLAVOR` map + 12 `COPY_RATING_NOTE_*` constants are deleted from `PercentileChip.tsx`.
    
    **Reason:** Phase 94.4 pivots from global-pool to peer-relative cohort framing. The percentile is computed against same-rated-cohort users, not the full benchmark population — the rating-correlation footnote that v1 added to honest disclosure is no longer needed.
    ```
    Use today's date for `2026-05-XX`. Preserve everything above the amendment.
  </action>
  <verify>
    <automated>grep -c "Amendment 2026-05\|rating-anchor disclosure\|RATING_NOTE_BY_FLAVOR" "$HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md"</automated>
  </verify>
  <acceptance_criteria>
    - Amendment section appended (`grep -c "Amendment 2026-05"` is ≥ 1).
    - 4-bullet contract restated under peer-relative (`grep -c "rating-anchor disclosure\|Cohort framing"` is ≥ 1).
    - Reason paragraph cites peer-relative pivot.
    - Original memory content preserved above the amendment.
  </acceptance_criteria>
  <done>Memory amendment landed; future GSD work will read the updated 4-bullet contract.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize ROADMAP.md Phase 94.4 Plans block (8 plans listed concretely)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 221-241 — current Phase 94.4 block with "Plans: TBD" placeholder at line 240)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/ (verify all 8 plan files exist with their objectives)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. Find the Phase 94.4 `**Plans**:` line. Replace the placeholder with a concrete list of the 10 plans (no checkboxes; status tracked via `/gsd:close-phase`):
    ```
    **Plans**: 10 plans (Plan 05 split into 05a/05b/05c per checker B5 — atomic cutover preserved at PR level)

    Plans:
    - [ ] 94.4-01-PLAN.md — chesscom_to_lichess.py conversion module + 3 ChessGoals tables + USCF/FIDE accessors per D-14
    - [ ] 94.4-02-PLAN.md — user_rating_anchors table (+ chesscom_raw_rating column per D-07 bullet 4) + Alembic migration + median-anchor SQL builder
    - [ ] 94.4-03-PLAN.md — 4 new per-TC ΔES SQL builders + Pitfall 1 user_id widening across all 7 per-TC builders
    - [ ] 94.4-04-PLAN.md — cohort CDF regen (8 metrics × ~33 anchors × 4 TCs) + suppression-flag report + HUMAN-VERIFY checkpoint per D-11
    - [ ] 94.4-05a-PLAN.md — user_benchmark_percentiles schema reshape (drop/recreate per D-02) + 8-value ENUM + 3-column PK + repository nested-dict fetch
    - [ ] 94.4-05b-PLAN.md — Stage A/B service rewrite (compute_anchors_for_user + chesscom_raw_rating capture per D-07 bullet 4) + legacy-stub retirement
    - [ ] 94.4-05c-PLAN.md — API shaper _aggregate_per_tc_percentile per D-08/D-08b + RatingAnchorOut schema + frontend type regen + terminal Pre-PR
    - [ ] 94.4-06-PLAN.md — backfill script extension + dev rerun + prod rerun via tunnel after HUMAN-UAT (PRPCR-09 credibility check)
    - [ ] 94.4-07-PLAN.md — PercentileChip rewrite (p23 face, NO flame, 8 flavors + tc prop, 4-bullet peer-relative popover) + 4 component reshapes
    - [ ] 94.4-08-PLAN.md — SEED-019 supersession banner + feedback_percentile_chip_tooltip_disclosure amendment + CHANGELOG
    ```
    Update the count at the start of the `**Plans**:` line. Preserve the surrounding Phase 94.4 block (Goal, Depends on, Requirements, Success Criteria, UI hint).
  </action>
  <verify>
    <automated>grep -c "94.4-01-PLAN.md\|94.4-02-PLAN.md\|94.4-03-PLAN.md\|94.4-04-PLAN.md\|94.4-05a-PLAN.md\|94.4-05b-PLAN.md\|94.4-05c-PLAN.md\|94.4-06-PLAN.md\|94.4-07-PLAN.md\|94.4-08-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All 10 plan filenames present in ROADMAP.md (count of `94.4-{01..08,05a,05b,05c}-PLAN.md` substrings is ≥ 10).
    - `**Plans**:` line updated to "10 plans" form.
    - Each plan has a one-line objective.
    - Surrounding Phase 94.4 block (Goal, Depends on, Requirements, Success Criteria) preserved.
  </acceptance_criteria>
  <done>ROADMAP Plans block finalized; future `/gsd:close-phase` can flip checkboxes as plans land.</done>
</task>

<task type="auto">
  <name>Task 4: Append Phase 94.4 entry to CHANGELOG.md [Unreleased]</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (in full — confirm `## [Unreleased]` section exists; understand the existing subsection conventions `### Added` / `### Changed` / `### Fixed`)
    - CLAUDE.md §"Changelog & Releases" (the per-phase discipline)
  </read_first>
  <action>
    Edit `CHANGELOG.md`. Under `## [Unreleased]`, in the appropriate subsections (`### Changed` and `### Added`), append:
    ```markdown
    ### Changed
    
    - **Endgame percentile chip pivoted from global-pool to peer-relative cohort framing.** Phase 94.4: the chip now compares each user against same-rated-cohort peers (per-(metric, ELO anchor, TC) cohort CDFs, K=200 sliding windows at 50-Elo anchors). Chip face shrinks to a bare `p23` pill (no flame icons, no direction word); tooltip rewrites to a 4-bullet peer-relative form including rating-anchor disclosure ("Anchored on your Lichess rapid rating" / "Anchored on your chess.com blitz rating, converted via ChessGoals snapshot"). 2400-rated users no longer see globally-low percentiles that demoralize honest skill — the percentile is now relative to other 2400s.
    
    ### Added
    
    - **Recovery Score Gap chip slot on the Endgames page** (Phase 94.4) — rescued from the v1 drop list under peer-relative framing. Cohort comparison normalises the opponent-rating selection bias that drove the v1 drop, surfacing how a user recovers from material deficit vs other same-rated players.
    - **Rating-anchor disclosure surfaces in the chip tooltip** (Phase 94.4) — for chess.com-anchored users, the tooltip shows the raw chess.com rating and the Lichess-equivalent computed via a hardcoded ChessGoals snapshot. Lichess-anchored users see their Lichess rating directly.
    ```
    Preserve everything above and below `## [Unreleased]`. If the `### Changed` or `### Added` subsections don't exist yet under `[Unreleased]`, create them.
  </action>
  <verify>
    <automated>grep -c "Phase 94.4\|peer-relative\|Recovery Score Gap" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `## [Unreleased]` section contains a Phase 94.4 entry (`grep -c "Phase 94.4\|peer-relative"` is ≥ 2).
    - `### Changed` subsection lists the chip pivot.
    - `### Added` subsection lists the Recovery chip slot + rating-anchor disclosure.
    - Tone is user-facing (what changed, not how) per CLAUDE.md per-phase discipline.
    - No `[vX.Y]` version section created — release promotion is a milestone-close action, not a per-phase action.
  </acceptance_criteria>
  <done>CHANGELOG entry landed; ready for v1.19 milestone close to promote to a versioned section.</done>
</task>

<task type="auto">
  <name>Task 5: Final verification — all PRPCR requirements have at least one merged plan; ROADMAP / REQUIREMENTS / CHANGELOG / memory all consistent</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (PRPCR-01..PRPCR-09 status in the Traceability table)
    - .planning/ROADMAP.md (finalized Plans block)
    - CHANGELOG.md (Phase 94.4 entry)
    - .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md (supersession banner)
    - $HOME/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md (amendment)
  </read_first>
  <action>
    Run a sanity sweep to confirm Phase 94.4 closure is consistent across artifacts:
    1. Every PRPCR requirement (01-09) appears in at least one plan's `requirements:` frontmatter. Run `grep -l 'PRPCR-' .planning/phases/94.4-peer-relative-percentile-chip-refinement/*-PLAN.md` and confirm count matches expectations.
    2. Every plan listed in ROADMAP.md Plans block exists as a file.
    3. The CHANGELOG entry mentions Phase 94.4 and the key user-facing changes.
    4. SEED-019 has the supersession banner.
    5. The memory file has the amendment.
    6. Update the REQUIREMENTS.md Traceability table — flip PRPCR-01..PRPCR-09 status from `Pending` to `Pending` (it stays Pending here because the operator decides actual completion via `/gsd:close-phase` after Plan 06's UAT signs off). Verify the table maps each PRPCR row to "Phase 94.4" in the Phase column.
    
    If any inconsistency surfaces (e.g. a PRPCR requirement isn't in any plan's frontmatter), surface as a blocker and patch the relevant plan's frontmatter.
  </action>
  <verify>
    <automated>for req in PRPCR-01 PRPCR-02 PRPCR-03 PRPCR-04 PRPCR-05 PRPCR-06 PRPCR-07 PRPCR-08 PRPCR-09; do echo -n "$req: "; grep -l "$req" .planning/phases/94.4-peer-relative-percentile-chip-refinement/*-PLAN.md 2>/dev/null | wc -l; done | awk -F: '{if ($2 == 0) print "MISSING " $1; else print "OK    " $1}'</automated>
  </verify>
  <acceptance_criteria>
    - All 9 PRPCR requirements appear in ≥ 1 plan's frontmatter (every line is "OK", none are "MISSING").
    - All 10 plan filenames listed in ROADMAP.md exist as files in `.planning/phases/94.4-peer-relative-percentile-chip-refinement/`.
    - CHANGELOG entry mentions Phase 94.4 + peer-relative + Recovery rescue.
    - SEED-019 has the supersession banner.
    - Memory file has the amendment.
    - REQUIREMENTS.md Traceability table maps PRPCR-01..PRPCR-09 to Phase 94.4.
  </acceptance_criteria>
  <done>All Phase 94.4 closure artifacts internally consistent; phase is ready for `/gsd:close-phase` after Plan 06 UAT sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan; no trust boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-08-SC | Tampering | npm/pip installs | accept | No new package installs. Documentation-only changes. |
</threat_model>

<verification>
- SEED-019 supersession banner present (`grep -c "Superseded by SEED-026"` is ≥ 1).
- Memory amendment present (`grep -c "Amendment 2026-05.*Phase 94.4"` in the memory file is ≥ 1).
- ROADMAP.md Plans block lists all 8 plans with one-line objectives.
- CHANGELOG.md `[Unreleased]` contains Phase 94.4 entry under `### Changed` + `### Added`.
- Every PRPCR-01..PRPCR-09 has ≥ 1 plan frontmatter mention.
</verification>

<success_criteria>
PRPCR-08 fully implemented. SEED-019 §"Final Tier-1 chip set" marked superseded; historical analysis preserved as context. `feedback_percentile_chip_tooltip_disclosure.md` user-memory amended for the new 4-bullet contract. ROADMAP Plans block finalized. CHANGELOG `[Unreleased]` entry landed for v1.19 milestone close.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-08-SUMMARY.md` when done. Record: snapshot of the supersession banner text; amendment date used; per-PRPCR plan-coverage matrix; CHANGELOG entry snippet (verbatim).
</output>
