---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 06
type: execute
wave: 2
depends_on: [94.4-01, 94.4-02, 94.4-03, 94.4-04, 94.4-05a, 94.4-05b, 94.4-05c]
files_modified:
  - scripts/backfill_user_percentiles.py
autonomous: false
requirements: [PRPCR-03, PRPCR-04, PRPCR-05, PRPCR-09]

must_haves:
  truths:
    - "`scripts/backfill_user_percentiles.py` extends from the post-94.3 per-(user, metric) iteration shape to per-(user, metric, tc) per CONTEXT D-01 + the Plan 05 schema reshape. Anchor backfill is included in the same script — for each user, anchors are computed FIRST then per-(family, tc) percentiles per RESEARCH Pitfall 9 ordering. Same dev-then-prod-after-sign-off flow that 94.3 established."
    - "Backfill summary log emits per-(metric × TC) counts: users-included, users-floor-rejected, users-suppressed (no CDF cell at their anchor), per-100-users progress checkpoint. Mirrors the 94.3 backfill summary discipline."
    - "`--target dev|prod` argparse + URL guards from 94.1 Plan 09 preserved verbatim. `--target prod` requires the SSH tunnel (`bin/prod_db_tunnel.sh` forwards prod DB to `localhost:15432`)."
    - "Dev rerun executes cleanly: `uv run python scripts/backfill_user_percentiles.py --target dev` populates `user_rating_anchors` + `user_benchmark_percentiles` for ALL users in dev DB; per-metric × per-TC summary table renders at the end."
    - "Prod rerun is gated behind a HUMAN-VERIFY checkpoint: operator opens the SSH tunnel via `bin/prod_db_tunnel.sh`, runs `uv run python scripts/backfill_user_percentiles.py --target prod`, monitors the summary log, and closes the tunnel via `bin/prod_db_tunnel.sh stop`. Per CONTEXT D-10a + CLAUDE.md prod-deploy discipline, this task is `autonomous: false`."
    - "Elite-user UAT (PRPCR-09 / Success Criterion 9 / SC-9 per ROADMAP §Phase 94.4) is a manual session against a 2400-rated user (chess.com OR Lichess) after the prod backfill completes. The operator opens the dashboard, inspects the chip + tooltip, and confirms the chip reads as 'vs other ~2400s' not 'vs all FlawChess users'. Mobile + desktop parity at 375px and desktop widths confirmed in the same session. This is the credibility-recovery sign-off."
    - "Per-user transactional commits: each user's anchors + percentile rows commit in a single transaction (or per-user transaction-per-stage if memory pressure surfaces) — partial backfill failures do NOT leave a user with anchors but no percentiles, or vice versa. RESEARCH Pitfall 9 ordering preserved (anchor compute before percentile compute within the same per-user transaction)."
  artifacts:
    - path: scripts/backfill_user_percentiles.py
      provides: extended per-(user, metric, tc) backfill + anchor populate + per-(metric × TC) summary
      contains: "user_rating_anchors\\|compute_anchors_for_user\\|STAGE_B_METRIC_FAMILIES"
      min_lines: 400
  key_links:
    - from: scripts/backfill_user_percentiles.py
      to: app/services/user_benchmark_percentiles_service.py (Plan 05 — compute_anchors_for_user + compute_stage_a + compute_stage_b)
      via: backfill calls the same Stage A/B compute functions used in the import-time fire-and-forget path
      pattern: "compute_anchors_for_user\\|compute_stage_a\\|compute_stage_b"
    - from: scripts/backfill_user_percentiles.py
      to: app/repositories/user_rating_anchors_repository.py (Plan 02 — upsert_anchor)
      via: anchor populate during backfill
      pattern: "upsert_anchor"
---

<objective>
Extend `scripts/backfill_user_percentiles.py` from per-(user, metric) to per-(user, metric, tc) iteration and add anchor backfill in the same script. After Plan 05's schema reshape, `user_benchmark_percentiles` is EMPTY and `user_rating_anchors` is EMPTY — this plan repopulates both via the same Stage A/B compute path that the import-time fire-and-forget uses. Dev rerun is autonomous; prod rerun is gated behind a HUMAN-VERIFY checkpoint + manual SSH-tunnel flow per CLAUDE.md prod-deploy discipline.

Purpose: This is the final commit in the Wave 2 atomic-cutover quartet. Without this backfill, the chip lights up only for users who import games AFTER Plan 05's migration deploys. The backfill ensures the entire user base sees the chip on the next page load. After this plan, the backend cohort-CDF + anchor + per-(user, metric, tc) percentile stack is fully populated. Plan 07 (frontend, Wave 3) ships the new chip face + 4-bullet tooltip rewrite that consumes the new types.

Output: extended backfill script + dev rerun (autonomous) + prod rerun (HUMAN-VERIFY) + elite-user UAT (HUMAN-VERIFY for PRPCR-09 credibility check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md
@scripts/backfill_user_percentiles.py
@app/services/user_benchmark_percentiles_service.py
@app/repositories/user_rating_anchors_repository.py
@CLAUDE.md

<interfaces>
<!-- Verified anchors:
     - PATTERNS lines 617-628: backfill script self-extend pattern
     - RESEARCH Pitfall 9 (lines 1269-1296): anchor backfill must precede percentile backfill within a per-user transaction
     - CLAUDE.md "Production Server": SSH-tunnel discipline for prod operations -->

Existing script CLI (post-94.3):
  --target {dev|prod}             # 94.1 Plan 09 URL-guard fix
  --user-id INT (optional)        # narrow to single user for testing
  --metric METRIC (optional)      # narrow to single metric
  --snapshot-date YYYY-MM-DD      # default today

Extension surface:
  --target {dev|prod}             # preserved
  --user-id INT                   # preserved
  --metric METRIC (optional)      # accepts 8-value CdfMetricId now (D-13); legacy 16-value composite IDs reject
  --tc {bullet|blitz|rapid|classical} (optional)  # NEW — narrow to one TC for testing
  --skip-anchors (optional)       # NEW — skip the anchor backfill if user_rating_anchors is already populated (idempotency optimization)
  --snapshot-date YYYY-MM-DD

Per-user transaction flow (RESEARCH Pitfall 9):
  for user in users:
      async with session_maker() as session:
          # 1. Anchor compute first (Plan 05's compute_anchors_for_user)
          anchors = await compute_anchors_for_user(session, user.id)
          # 2. Then Stage A: score_gap percentiles for TCs with anchors
          await compute_stage_a(user.id, session_maker=...)
          # 3. Then Stage B: per-(family, tc) percentiles for the 7 metric families
          await compute_stage_b(user.id, session_maker=...)
          # All in one transaction; commit at the end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/backfill_user_percentiles.py — per-(user, metric, tc) iteration + anchor backfill + summary table</name>
  <files>scripts/backfill_user_percentiles.py</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (in full — post-94.3 form with `_MetricSummary`, `--metric` argparse choices, `--target dev|prod` URL gates, per-100-users progress logging)
    - app/services/user_benchmark_percentiles_service.py (Plan 05 — `compute_anchors_for_user`, rewritten `compute_stage_a` and `compute_stage_b`, `STAGE_B_METRIC_FAMILIES`)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-PATTERNS.md (lines 617-628 — extension pattern)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-RESEARCH.md (Pitfall 9 lines 1269-1296 — backfill ordering)
  </read_first>
  <action>
    Extend `scripts/backfill_user_percentiles.py`:
    1. Update `--metric` argparse `choices` to the 8-value list per CONTEXT D-13: `["score_gap", "achievable_score_gap", "section2_score_gap_conv", "section2_score_gap_parity", "recovery_score_gap", "time_pressure_score_gap", "clock_gap", "net_flag_rate"]`. The 12 composite TC-suffixed legacy values retire.
    2. Add `--tc {bullet|blitz|rapid|classical}` argparse arg (optional) for narrow-testing.
    3. Add `--skip-anchors` argparse flag for re-runs where anchors are already populated.
    4. Per-user backfill body switches to the 3-stage flow per `<interfaces>` block: (a) `compute_anchors_for_user(session, user.id)` — populates `user_rating_anchors`; if `--skip-anchors`, fetch existing anchors via `fetch_anchors_for_user`; (b) `compute_stage_a(user.id, session_maker=...)` — populates score_gap per-TC rows; (c) `compute_stage_b(user.id, session_maker=...)` — populates the 7 metric families × per-TC rows. Wrap all 3 stages in a single async session per user (one transaction per user) so partial failures don't leave inconsistent state.
    5. Extend `_MetricSummary` (or whatever the existing summary type is named) to track per-(metric × TC) counts: users-included, users-floor-rejected, users-suppressed (no CDF cell at user's anchor — `interpolate_cohort_percentile` returned None). Add a parallel `_AnchorSummary` tracking per-(TC × source_platform) counts for the anchor backfill.
    6. Final summary print MUST render two tables side-by-side: (a) anchor summary (TC × source_platform counts); (b) percentile summary (metric × TC × included|rejected|suppressed counts). Keep the per-100-users progress log line that 94.3 established.
    7. Per-user commit-then-continue discipline: errors in one user's backfill do NOT abort the run (Sentry-capture the exception per the existing service-layer Sentry hooks, log a single ERROR line with user_id + stage + metric + tc context, continue to next user). The 94.3 backfill harness already does this; preserve verbatim.
    8. `--target dev|prod` URL guards from 94.1 Plan 09 preserved verbatim. `--target prod` calls `_assert_prod_db(url)` (or whatever the existing guard is named) to confirm `DATABASE_URL` matches the prod-tunnel pattern.
    9. CLI usage docstring at the top of the script updates to document the 3-stage flow + `--skip-anchors` + `--tc` flag + the 8 new metric values.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_user_percentiles.py --help 2>&1 | tee /tmp/backfill_help.txt && grep -c "skip-anchors\|--tc\|score_gap\|achievable_score_gap\|section2_score_gap_conv\|section2_score_gap_parity\|recovery_score_gap\|time_pressure_score_gap\|clock_gap\|net_flag_rate" /tmp/backfill_help.txt</automated>
  </verify>
  <acceptance_criteria>
    - `--skip-anchors` argparse flag present in --help output.
    - `--tc` argparse arg present in --help output.
    - `--metric` choices show all 8 new values; no composite TC-suffixed legacy value remains.
    - `grep -c "compute_anchors_for_user\|compute_stage_a\|compute_stage_b" scripts/backfill_user_percentiles.py` is ≥ 3.
    - `grep -c "STAGE_B_METRIC_FAMILIES\b\|STAGE_B_METRICS\b" scripts/backfill_user_percentiles.py | grep -v '^#'` — if the script references the metric-tuple constant, it MUST be the new `STAGE_B_METRIC_FAMILIES`, not the legacy `STAGE_B_METRICS`.
    - `grep -c "_AnchorSummary\|anchor_summary\|anchor[Ss]ummary" scripts/backfill_user_percentiles.py` is ≥ 1 (anchor summary tracker present).
    - `--target prod` URL guard preserved (the 94.1 Plan 09 mitigation).
    - `uv run ty check scripts/backfill_user_percentiles.py` returns zero errors.
  </acceptance_criteria>
  <done>Script extended; 3-stage per-user flow + per-(metric × TC) summary + 8-value metric choices + skip-anchors + per-TC narrow all wired.</done>
</task>

<task type="auto">
  <name>Task 2: Dev rerun — execute backfill against local dev DB; verify per-(metric × TC) summary; confirm interpolate_cohort_percentile coverage matches the suppression report</name>
  <files>(no file modified — execution + verification only)</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (just extended in Task 1)
    - reports/cohort-percentile-cdf-latest.md (Plan 04 output — operator already decided strict-default vs relax)
  </read_first>
  <action>
    Ensure dev DB is up: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`. Run the backfill: `uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-26`. Monitor the output:
    1. Per-100-users progress line appears periodically.
    2. Sentry exceptions are minimal — at most a handful of edge-case users with malformed data.
    3. Final summary tables print: anchor counts per (TC × source_platform); percentile counts per (metric × TC × included|rejected|suppressed).
    
    Sanity-check the database state after the backfill: query `SELECT COUNT(*) FROM user_rating_anchors;` and `SELECT COUNT(*) FROM user_benchmark_percentiles;` via psql or MCP. Compare against expectations: the dev DB should have <100 users typically (depending on dev seed); each user with games in N TCs gets up to N anchor rows and up to N × 8 = N × 8 percentile rows (minus suppressions). Cross-reference suppression counts against the Plan 04 regen report — heavily-suppressed (metric, TC) cells in the report should correspond to high suppressed counts in the backfill summary.
    
    Smoke-test the API end-to-end: start the backend (`uv run uvicorn app.main:app --reload`), hit the endgame overview endpoint for a test user with a known anchor (e.g. `curl http://localhost:8000/api/endgames/overview?user_id=...` — adapt to the actual auth/route shape), inspect the JSON for `rating_anchors`, `*_percentile` fields, etc. Confirm the new schema fields appear and are populated.
    
    If anything breaks (Sentry exception storm, summary table shape wrong, API 500s), debug in this task before unblocking the prod gate.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_user_percentiles.py --target dev --snapshot-date 2026-05-26 2>&1 | tee /tmp/backfill_dev.log | tail -30 && grep -c "SUMMARY\|Anchor summary\|Percentile summary\|users_included\|users_suppressed" /tmp/backfill_dev.log</automated>
  </verify>
  <acceptance_criteria>
    - Backfill completes without unhandled exceptions; exit code 0.
    - Final summary tables visible in the log (anchor + percentile counts).
    - `SELECT COUNT(*) FROM user_benchmark_percentiles` > 0.
    - `SELECT COUNT(*) FROM user_rating_anchors` > 0.
    - API smoke test: endgame overview endpoint returns 200 + populated `rating_anchors` + populated `*_percentile` fields for at least one test user.
    - Suppression counts in the summary match the per-(metric, anchor, TC) suppression report from Plan 04 (within rounding — the same K=200 / ±150-Elo policy applies).
  </acceptance_criteria>
  <done>Dev backfill complete; database populated; API smoke test green; suppression counts consistent with Plan 04 regen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-VERIFY — Operator opens prod tunnel, runs prod backfill, closes tunnel, monitors Sentry</name>
  <files>(no file modified — operator-driven script execution against prod via SSH tunnel; results captured in 94.4-06-SUMMARY.md)</files>
  <action>Operator-driven prod backfill via `bin/prod_db_tunnel.sh` + `uv run python scripts/backfill_user_percentiles.py --target prod`. Claude does not write code in this task. See `<how-to-verify>` for the exact operator procedure.</action>
  <what-built>Plan 05a's destructive migration ran on prod via the deploy entrypoint (Alembic auto-runs on container start); `user_benchmark_percentiles` is empty in prod. This task runs the backfill via the SSH tunnel.</what-built>
  <how-to-verify>
    1. **Pre-flight:** confirm `bin/deploy.sh` has shipped the Wave 2 atomic-cutover cluster (Plans 03 + 04 + 05a + 05b + 05c) to prod. Run `gh run list --limit 5` to confirm the most recent deploy is green. Run `ssh flawchess "cd /opt/flawchess && docker compose logs --tail=50 backend | grep -i 'alembic\|migration'"` to confirm migrations ran cleanly on the prod container.
    2. **Open the tunnel:** `bin/prod_db_tunnel.sh` (forwards prod Postgres to `localhost:15432`). Verify the tunnel is up: `psql 'postgresql://flawchess_ro:<password>@localhost:15432/flawchess' -c "SELECT count(*) FROM users;"` returns the prod user count.
    3. **Sanity-check prod DB state:** `SELECT COUNT(*) FROM user_benchmark_percentiles;` returns 0 (empty after the Plan 05 migration). `SELECT COUNT(*) FROM user_rating_anchors;` returns 0.
    4. **Run the backfill:** `uv run python scripts/backfill_user_percentiles.py --target prod --snapshot-date 2026-05-26`. Watch the progress log. Estimated runtime: linearly proportional to user count × per-user transaction time; for a few hundred prod users this is typically 5-30 minutes. Keep the tunnel open the whole time.
    5. **Monitor Sentry** during the backfill: open `https://flawchess.sentry.io` and watch for percentile_compute exceptions. A handful of edge-case users is expected (users with truly malformed data); a flood of exceptions is a red flag — abort the backfill (Ctrl-C), investigate, fix in a follow-up commit, re-run.
    6. **Inspect summary tables:** at the end of the backfill, the script prints two tables (anchor + percentile counts per dimension). Verify the totals make sense vs the prod user count.
    7. **Sanity-check post-backfill DB state:** `SELECT COUNT(*) FROM user_benchmark_percentiles GROUP BY metric;` returns counts roughly equal to the prod user count × (1 - suppression_rate) for each metric. `SELECT COUNT(*) FROM user_rating_anchors GROUP BY source_platform;` returns Lichess + chess.com counts (Lichess-precedence rule means most users with both platforms appear under `lichess`).
    8. **Close the tunnel:** `bin/prod_db_tunnel.sh stop`.
    9. **Post-deploy smoke test:** open `https://flawchess.com` in a browser, log in as a known elite-user account (or a personal account), navigate to the Endgames page, inspect the chip — confirm a `p23`-shaped chip renders on at least one page-level ΔES row. (Plan 07 ships the new chip face; if Plan 07 hasn't deployed yet, the chip renders in the old `Top X%` / `Bottom X%` form and the new shape lights up after Plan 07 ships — this is acceptable per RESEARCH Wave-3 parallelism line 1678.)
  </how-to-verify>
  <resume-signal>Type "prod backfill complete" if everything ran cleanly. Type "abort" + describe the Sentry exception flood (if any) — fixes go in a follow-up plan or hotfix commit before re-running.</resume-signal>
  <verify>
    <human-check>Operator confirms: (1) prod tunnel opened and closed via `bin/prod_db_tunnel.sh`; (2) backfill completed with summary tables printed; (3) Sentry shows no exception flood during the backfill window; (4) `SELECT COUNT(*) FROM user_benchmark_percentiles` and `SELECT COUNT(*) FROM user_rating_anchors` on prod return > 0 row counts consistent with the user base.</human-check>
  </verify>
  <done>Prod backfill complete; user_benchmark_percentiles + user_rating_anchors populated on prod; Sentry clean; tunnel closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-VERIFY — Elite-user UAT for PRPCR-09 (credibility recovery)</name>
  <files>.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-06-HUMAN-UAT.md (NEW — operator records UAT outcome here)</files>
  <action>Operator-driven manual UAT against a 2400-rated user account on `https://flawchess.com`. Claude does not write code in this task. See `<how-to-verify>` for the exact UAT procedure. The operator records the outcome in `94.4-06-HUMAN-UAT.md`.</action>
  <what-built>Post-prod-backfill, the cohort-percentile chip is populated for the entire user base. PRPCR-09 / ROADMAP SC-9 requires UAT confirmation that the chip reads as cohort-relative on elite users, not as a global ranking that demoralizes 2400-rated players.</what-built>
  <how-to-verify>
    1. Find a 2400-rated user account (the operator's own account if applicable, OR a known elite-user fixture). Log in as that user on `https://flawchess.com`.
    2. Navigate to the Endgames page. Look at the page-level ΔES chips (Endgame Score Gap, Achievable Score Gap, the Section-2 cards).
    3. **Critical check:** if a chip shows `p15` (or any percentile in the lower-half range), does the tooltip frame this as "you sit at p15 vs other ~2400-rated players in rapid" rather than "you sit at p15 vs all FlawChess users"? The tooltip bullet 1 (cohort framing) MUST mention the anchor rating concretely.
    4. **Mobile parity:** repeat the inspection at 375px viewport width (Chrome DevTools mobile emulation or actual phone). Chip remains compact and readable; tooltip stays within viewport bounds.
    5. **Anchor disclosure:** tooltip bullet 4 names the anchor source — "Anchored on your Lichess rapid rating (2400)" for Lichess-anchored users, or "Anchored on your chess.com blitz rating (1830 → 1920 Lichess-equivalent via ChessGoals snapshot 2026-05-26)" for chess.com-anchored users. Verify the value matches the user's actual rating.
    6. **No flame icon:** confirm visually that NO flame icon (or any tier-icon decoration) renders on any chip at any percentile, including chips showing high percentiles like `p95` or `p99`. (HARD CONSTRAINT per CONTEXT D-06 + Plan 07 regression test.)
    7. **Optional — multi-account check:** if a 1200-rated test account exists, inspect their chips to confirm the framing reads symmetrically — "p35 vs other ~1200-rated players in blitz" — not "p35 vs all FlawChess users."
    
    Record the UAT outcome in `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-06-HUMAN-UAT.md` with: tested user (rating + platform); chip values observed; tooltip body text observed (transcribe one or two); mobile-parity check pass/fail; any anomalies. PRPCR-09 / SC-9 is APPROVED iff the cohort framing is honest, the anchor disclosure is correct, and no flame icon renders.
  </how-to-verify>
  <resume-signal>Type "UAT approved" + brief observations OR "UAT failed: {what's wrong}" + propose follow-up plan or hotfix.</resume-signal>
  <verify>
    <human-check>Operator confirms on `https://flawchess.com` (or equivalent prod URL): (1) chip face renders bare `p23` form for at least one page-level ΔES row; (2) tooltip bullet 1 frames as cohort-relative with the anchor rating named ("~2400-rated players"); (3) tooltip bullet 4 names the anchor source (Lichess or chess.com + conversion); (4) NO flame icon visible at any percentile including high tiers; (5) mobile parity at 375px viewport.</human-check>
  </verify>
  <done>PRPCR-09 / SC-9 credibility-recovery sign-off recorded in `94.4-06-HUMAN-UAT.md`. Phase 94.4 may proceed to Wave 4 housekeeping (Plan 08) once Plan 07 frontend has shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local script → prod Postgres | Via `bin/prod_db_tunnel.sh` SSH tunnel; URL guard ensures script only runs against the tunneled prod URL pattern. |
| Operator → prod backfill | Manual gate per CLAUDE.md prod-deploy discipline; no autonomous prod write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-06-01 | Tampering | Backfill accidentally hits the wrong DB | mitigate | `--target dev|prod` URL guard from 94.1 Plan 09 preserved; `--target prod` calls `_assert_prod_db(url)` checking URL pattern. Operator manually opens tunnel before `--target prod`. |
| T-94.4-06-02 | Denial of service | Prod backfill saturates DB connection pool during business hours | accept | Per-user transactional commits + per-user session lifecycle; pool exhaustion unlikely. CLAUDE.md production-server section documents pool limits (SQLAlchemy `pool_size=10, max_overflow=10 = 20` ceiling). Operator can `Ctrl-C` to abort. |
| T-94.4-06-03 | Information disclosure | Backfill log contains user-identifying data | accept | Per-100-users summary log uses aggregate counts; per-user error logs include `user_id` via Sentry context (not message), consistent with Phase 94.1/94.2/94.3 backfill discipline. |
| T-94.4-06-SC | Tampering | npm/pip installs | accept | No new packages. |
</threat_model>

<verification>
- Dev backfill completes cleanly; final summary tables print.
- Prod backfill HUMAN-VERIFY checkpoint passed; Sentry has no exception flood; `user_benchmark_percentiles` and `user_rating_anchors` populated in prod.
- Elite-user UAT HUMAN-VERIFY checkpoint passed; cohort framing + anchor disclosure honest; no flame icon.
- The cohort-percentile chip surface is end-to-end lit up across the user base.
</verification>

<success_criteria>
PRPCR-03 / PRPCR-04 / PRPCR-05 fully implemented across dev + prod. PRPCR-09 / SC-9 (credibility recovery) APPROVED via the elite-user UAT. Wave 2 atomic-cutover (Plans 03 → 04 → 05a → 05b → 05c → 06) complete in a single squash-merged PR. Plan 07 (Wave 3 frontend) is now safe to land — frontend tolerates missing-percentile cells (chip suppresses) and consumes the new `rating_anchors` block + `*_percentile` aggregated scalars.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-06-SUMMARY.md` when done. Record: dev backfill runtime + summary table (paste anchor + percentile count tables verbatim); prod backfill runtime + same summary; elite-user UAT outcome with tested user count + observations; any Sentry exceptions captured during prod backfill and follow-up tickets opened.
</output>
