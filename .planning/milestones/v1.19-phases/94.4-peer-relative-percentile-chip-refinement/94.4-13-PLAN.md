---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 13
type: execute
wave: 4
depends_on: [94.4-12]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CHANGELOG.md
  - /home/aimfeld/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md
autonomous: true
requirements: [PRPCR-03, PRPCR-07]

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` PRPCR-03 is rewritten to match the D-12 Reversal Amendment schema. The current PRPCR-03 wording (line 48 — references `source_platform anchor_source NOT NULL ENUM('lichess', 'chesscom')`, `n_games INT`, and the Lichess-precedence rule) is OBSOLETE. The new wording describes: PK `(user_id, time_control_bucket)`, columns `anchor_rating INT NOT NULL`, `n_chesscom_games INT NOT NULL`, `n_lichess_games INT NOT NULL`, `chesscom_median_native INT NULL`, `lichess_median_native INT NULL`, `computed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `user_id` with `ON DELETE CASCADE`. Anchor compute: per-game ChessGoals conversion for chess.com (non-Daily) games, native passthrough for lichess games, pooled median over the union. Suppression rule: no anchor row when `n_chesscom_games + n_lichess_games < MEDIAN_ANCHOR_MIN_GAMES`. Citation: the D-12 Reversal Amendment in `.planning/phases/94.4-.../94.4-CONTEXT.md` + the design-decision record `.planning/notes/percentile-anchor-d12-reversal.md`."
    - "`.planning/REQUIREMENTS.md` PRPCR-07 is updated to reflect the bullet 4 contract revision. The current PRPCR-07 wording (line 52 — bullet 4 reads 'rating-anchor disclosure (Lichess-anchored vs chess.com-anchored-and-converted phrasing)') is augmented: bullet 4 now also covers the BLENDED composition for mixed-platform users — 'rating-anchor disclosure: composition prose for mixed users (blending N chess.com + M lichess games), shortened form for pure-platform users.' The 3 prose branches (mixed / pure-lichess / pure-chess.com) are enumerated. The HARD CONSTRAINT against flame icons is restated."
    - "`.planning/ROADMAP.md` Phase 94.4 section gets an additive note recording the D-12 reversal amendment. The line documenting the phase completion (currently 'completed 2026-05-27') stays, but a new sentence appends: '— amended 2026-05-27 with D-12 reversal (game-weighted blended anchor); see CONTEXT §Amendment + .planning/notes/percentile-anchor-d12-reversal.md.' The 10 existing plan-list entries (01-08) STAY UNTOUCHED. Add 5 NEW lines for Plans 09-13 with brief one-line descriptions matching their actual scope."
    - "`CHANGELOG.md` `[Unreleased]` section gains a bullet under `### Changed`: 'Percentile chip anchor: switch to game-weighted blended anchor (mix of chess.com-converted + lichess native, per-game conversion). Mixed-platform users now see an anchor that reflects their full play distribution, not just their lichess ladder position. Pure-platform users are unaffected. Tooltip composition disclosure rewrites to surface the per-platform game counts + native medians.' Citation to GSD Phase 94.4 amendment."
    - "User-memory file `~/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md` is amended to update the bullet 4 contract: the 4-bullet disclosure mandate stays, but bullet 4 now MUST disclose the COMPOSITION of the blended anchor (not just the anchor value). The 3 prose branches (mixed / pure-lichess / pure-chess.com) are documented. Cross-reference to CONTEXT §Amendment + design-decision record. The memory's existing CRITICAL disclosure-contract semantics carry verbatim — this amendment widens the contract, doesn't loosen it."
    - "`.planning/STATE.md` is NOT modified by this plan (the orchestrator handles state transitions on phase completion). However, the executor should verify that STATE.md still cleanly reflects Phase 94.4 as needing review / pending close — if STATE shows the phase as fully closed prematurely, surface to the user before continuing."
    - "No code files are modified by this plan. All changes are documentation/metadata."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: PRPCR-03 + PRPCR-07 wording updated for D-12 reversal
      contains: "n_chesscom_games\\|n_lichess_games\\|chesscom_median_native\\|lichess_median_native\\|blending"
    - path: .planning/ROADMAP.md
      provides: Phase 94.4 amendment note + plan list entries for 09-13
      contains: "amended 2026-05-27\\|D-12 reversal\\|94.4-09-PLAN\\|94.4-10-PLAN\\|94.4-11-PLAN\\|94.4-12-PLAN\\|94.4-13-PLAN"
    - path: CHANGELOG.md
      provides: Unreleased bullet documenting the D-12 reversal
      contains: "game-weighted blended anchor"
    - path: /home/aimfeld/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md
      provides: memory amendment — bullet 4 contract widens to composition disclosure
      contains: "composition\\|blending\\|n_chesscom_games\\|n_lichess_games"
  key_links:
    - from: .planning/REQUIREMENTS.md PRPCR-03
      to: .planning/notes/percentile-anchor-d12-reversal.md
      via: schema description matches the design-decision record verbatim
      pattern: "n_chesscom_games\\|n_lichess_games"
    - from: feedback_percentile_chip_tooltip_disclosure.md
      to: frontend/src/components/charts/PercentileChip.tsx PercentileChipPopoverBody bullet 4
      via: memory's bullet 4 contract is implemented by the component's bullet 4 conditional
      pattern: "composition\\|blending"
---

<objective>
Close the documentation loop for the D-12 Reversal Amendment. Update the four canonical project documentation surfaces (REQUIREMENTS.md, ROADMAP.md, CHANGELOG.md, the user-memory disclosure-contract file) so that the project's source-of-truth artifacts agree with what shipped. After this plan, a future reader of REQUIREMENTS.md PRPCR-03 sees the blended-anchor schema (not the stale Lichess-precedence schema); ROADMAP.md records the amendment in the Phase 94.4 entry; CHANGELOG.md surfaces the user-visible behavior change; and the user-memory disclosure contract reflects the new bullet 4 composition mandate.

Purpose: Documentation drift is invisible until it bites. PRPCR-03's stale wording is the highest-priority debt — anyone planning a future phase that touches `user_rating_anchors` reads PRPCR-03 first and would build on the wrong schema. The other three updates are lower-stakes but standard hygiene under the GSD workflow.

Output: 4 documentation file edits. No code changes. Plan 13 is the cleanest plan in the cluster and runs sub-10-minute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/notes/percentile-anchor-d12-reversal.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md PRPCR-03 + augment PRPCR-07 for the blended anchor + composition disclosure</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find PRPCR-03 and PRPCR-07 entries; verify the existing PRPCR-XX numbering style + bullet format so the rewrite matches)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md §Amendment (locked schema + tooltip contract)
    - .planning/notes/percentile-anchor-d12-reversal.md (design-decision record — citation target)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Find the PRPCR-03 line (currently around line 48). Replace the bullet body with the new schema description per `must_haves.truths`:

    > **PRPCR-03**: A `user_rating_anchors` table exists with PK `(user_id, time_control_bucket)`, columns `anchor_rating INT NOT NULL`, `n_chesscom_games INT NOT NULL`, `n_lichess_games INT NOT NULL`, `chesscom_median_native INT NULL`, `lichess_median_native INT NULL`, `computed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `user_id` with `ON DELETE CASCADE` to `users.id`. Up to 4 rows per user. Anchor compute: per-game ChessGoals conversion for chess.com (non-Daily) games, native passthrough for lichess games, pooled median over the union (D-12 Reversal Amendment 2026-05-27; supersedes the original D-12 Lichess-precedence rule). Suppression: no anchor row when `n_chesscom_games + n_lichess_games < MEDIAN_ANCHOR_MIN_GAMES`. chess.com Daily games (`time_control_str LIKE '1/%' AND platform='chess.com'`) are dropped at the SQL level from both the anchor pool and the per-platform game counts. See `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md §Amendment` and `.planning/notes/percentile-anchor-d12-reversal.md` for the rationale, risk profile, and known imperfections. (ROADMAP SC-3, amended 2026-05-27)

    Find the PRPCR-07 line (currently around line 52). Augment the bullet 4 description to enumerate the 3 composition branches:

    > **PRPCR-07**: `PercentileChip.tsx` tooltip body is 4 bullets under peer-relative framing per CONTEXT D-07 (amended 2026-05-27): (1) cohort framing inline with anchor + TC; (2) recent-games basis (TC-scoped); (3) filter independence (verbatim from `COPY_FILTER_INDEPENDENCE`); (4) **rating-anchor disclosure with COMPOSITION prose for mixed users** (3 prose branches: mixed = "blending N chess.com games (median X, converted) with M lichess games (median Y)", pure-lichess = "from N lichess games (native rating)", pure-chess.com = "from N chess.com games (median X, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26)"). `COPY_RATING_NOTE_*` (×12) and `RATING_NOTE_BY_FLAVOR` retire entirely per D-07a; `DIRECTION_BY_FLAVOR` retires (all 8 flavors are `higher_is_better` post-94.3 UAT). `PercentileChipFlavor` collapses from 16 variants to 8 (`score-gap`, `achievable`, `parity`, `conversion`, `recovery`, `time-pressure-score-gap`, `clock-gap`, `net-flag-rate`) plus an optional `tc?: TimeControlBucket` prop. **HARD CONSTRAINT: NO flame icons at any percentile** (regression guard locked in PercentileChip.test.tsx). `npm run knip` stays green after retirement. (ROADMAP SC-7, amended 2026-05-27)

    Find the PRPCR status table further down (around line 105-115). Confirm PRPCR-03, PRPCR-07, PRPCR-09 still show "Pending" or update to "Complete" depending on the project's convention — check the table column header for the right value. If unclear, leave the status fields unchanged and surface the question in the SUMMARY.

    No other PRPCR entries change. Other phases' requirements are out of scope.
  </action>
  <verify>
    <automated>grep -A 2 'PRPCR-03' /home/aimfeld/Projects/Python/flawchess/.planning/REQUIREMENTS.md | head -10 && grep -c 'n_chesscom_games\|n_lichess_games\|chesscom_median_native\|lichess_median_native' /home/aimfeld/Projects/Python/flawchess/.planning/REQUIREMENTS.md
</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'n_chesscom_games\|n_lichess_games\|chesscom_median_native\|lichess_median_native' .planning/REQUIREMENTS.md` is >= 4.
    - `grep -c 'source_platform anchor_source' .planning/REQUIREMENTS.md` is 0 (legacy schema language removed from PRPCR-03).
    - `grep -c 'D-12 Reversal\|amended 2026-05-27' .planning/REQUIREMENTS.md` is >= 1.
    - `grep -c 'blending\|composition prose' .planning/REQUIREMENTS.md` is >= 1 (PRPCR-07 augmented).
    - `grep -c 'HARD CONSTRAINT.*flame\|NO flame' .planning/REQUIREMENTS.md` is >= 1 (regression-guard line preserved in PRPCR-07).
    - PRPCR-03 + PRPCR-07 entries are internally consistent with `app/models/user_rating_anchors.py` (post-Plan 09) and `frontend/src/components/charts/PercentileChip.tsx` (post-Plan 11).
  </acceptance_criteria>
  <done>PRPCR-03 + PRPCR-07 entries rewritten; legacy schema language removed; amendment citations present.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-12 reversal note + Plans 09-13 lines to ROADMAP.md Phase 94.4 entry</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 33 + 221-260 — the Phase 94.4 milestone-roadmap bullet AND the detailed phase entry with plan list)
  </read_first>
  <action>
    Open `.planning/ROADMAP.md`. Two edits:

    **Edit A — line 33** (the top-of-roadmap milestone bullet for Phase 94.4): currently ends with "(completed 2026-05-27)". Append " — amended 2026-05-27 (D-12 reversal: game-weighted blended anchor; see CONTEXT §Amendment + `.planning/notes/percentile-anchor-d12-reversal.md`)." The bullet remains a single line; the parenthetical augments the completion stamp.

    **Edit B — lines 243-252** (the detailed plan list under "Plans:" in §Phase 94.4). After the existing 10 entries (01 through 08 including 05a/b/c), ADD 5 new entries for Plans 09-13. Each entry follows the existing format: `- [ ] 94.4-XX-PLAN.md — <brief description>` (use unchecked `[ ]` since these plans haven't executed yet — the executor of THIS plan unchecks them; Plan 12's HUMAN-UAT signoff in the orchestrator marks them complete later). Lines:

    ```
    - [ ] 94.4-09-PLAN.md — D-12 reversal: schema reshape (drop source_platform/chesscom_raw_rating; add per-platform game counts + native medians) + ORM + repository + Alembic migration + TRUNCATE UBP+URA
    - [ ] 94.4-10-PLAN.md — D-12 reversal: per-game ChessGoals conversion + pooled-median SQL builder (blend=True mode) + compute_anchors_for_user rewrite
    - [ ] 94.4-11-PLAN.md — D-12 reversal: RatingAnchorOut Pydantic schema + frontend TS type + PercentileChip tooltip body composition disclosure (3 prose branches)
    - [ ] 94.4-12-PLAN.md — D-12 reversal: backfill harness extension (composition-typed summary) + dev rerun + prod rerun via tunnel + mixed-platform HUMAN-UAT
    - [ ] 94.4-13-PLAN.md — D-12 reversal: documentation closure (REQUIREMENTS.md PRPCR-03/PRPCR-07 + ROADMAP note + CHANGELOG + memory amendment)
    ```

    DO NOT modify the existing 10 entries (01-08). DO NOT mark line 33's bullet as unchecked — the phase IS complete; just augmenting with the amendment record.

    If the ROADMAP has a "Plans:" count summary line for Phase 94.4 (e.g. "**Plans:** 10 plans"), update it to 15.

    No other ROADMAP sections change. Other phases out of scope.
  </action>
  <verify>
    <automated>grep -c '94.4-09-PLAN\|94.4-10-PLAN\|94.4-11-PLAN\|94.4-12-PLAN\|94.4-13-PLAN' /home/aimfeld/Projects/Python/flawchess/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '94.4-09-PLAN\|94.4-10-PLAN\|94.4-11-PLAN\|94.4-12-PLAN\|94.4-13-PLAN' .planning/ROADMAP.md` is >= 5 (all 5 new plan lines present).
    - `grep -c 'amended 2026-05-27\|D-12 reversal' .planning/ROADMAP.md` is >= 1 (amendment note on line 33).
    - The existing 10 plan entries (01 through 08 incl. 05a/b/c) remain UNTOUCHED — verify via `git diff .planning/ROADMAP.md | grep '^-' | grep -v '^---'` shows no removals of pre-existing plan lines.
    - If a "Plans:" count line exists for Phase 94.4, it reflects 15.
  </acceptance_criteria>
  <done>ROADMAP reflects the amendment + lists Plans 09-13; existing entries untouched; plan count updated.</done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG.md `[Unreleased]` bullet for the D-12 reversal</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — find the `## [Unreleased]` section; understand the existing `### Added` / `### Changed` / `### Fixed` subsection convention from CLAUDE.md "Changelog & Releases")
  </read_first>
  <action>
    Open `CHANGELOG.md`. Find the `## [Unreleased]` section. Under `### Changed` (create the subsection if it doesn't exist), append:

    ```
    - Percentile chip anchor: switch to game-weighted blended anchor for mixed-platform users (per-game ChessGoals conversion for chess.com + native passthrough for lichess, pooled median). Mixed-platform users now see an anchor that reflects their full play distribution, not just their lichess ladder position. Pure-platform users are unaffected (median commutativity within the ChessGoals snapshot). Tooltip rewrites bullet 4 to disclose the per-platform composition: mixed users see "blending N chess.com games (median X, converted) with M lichess games (median Y)"; pure-platform users see a shortened single-platform form. (GSD Phase 94.4 amendment, D-12 reversal — see `.planning/notes/percentile-anchor-d12-reversal.md`)
    ```

    Per CLAUDE.md "Changelog & Releases" §"Per-phase": the bullet is user-facing prose (what changed, not how). No tooling tweaks / refactor language. The amendment is a behavior change for the mixed-platform population, so it earns a CHANGELOG entry (the amendment guidance in CLAUDE.md says "Skip this for /gsd-quick / /gsd-fast tasks that don't meaningfully change behavior" — this IS a meaningful behavior change).

    If the `[Unreleased]` section has no `### Changed` subsection yet, add it (the convention is documented in CLAUDE.md and visible in the existing CHANGELOG body).

    Do NOT add bullets under other categories (no `### Added` since this is a behavioral change to an existing feature; no `### Fixed` since this isn't a bug per se but a design correction).
  </action>
  <verify>
    <automated>grep -A 30 '## \[Unreleased\]' /home/aimfeld/Projects/Python/flawchess/CHANGELOG.md | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'game-weighted blended anchor\|D-12 reversal' CHANGELOG.md` is >= 1.
    - The new bullet lives under `## [Unreleased]` -> `### Changed` (not under another section / version heading).
    - Bullet prose is user-facing (no implementation-detail jargon beyond what a curious user would google).
    - Cross-reference to the design-decision record path included.
  </acceptance_criteria>
  <done>CHANGELOG records the D-12 reversal as a Changed bullet under [Unreleased].</done>
</task>

<task type="auto">
  <name>Task 4: Amend feedback_percentile_chip_tooltip_disclosure user-memory for the composition-disclosure contract</name>
  <files>/home/aimfeld/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md</files>
  <read_first>
    - /home/aimfeld/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md (full file — understand the existing CRITICAL disclosure mandate phrasing)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md §Amendment (the new bullet 4 contract)
  </read_first>
  <action>
    Open the user-memory file. The file currently locks the 4-bullet disclosure contract on the percentile chip tooltip. The bullet 4 description is stale post-D-12-reversal.

    Two edits:

    **Edit A — bullet 4 contract**: Find the section that describes bullet 4 (rating-anchor disclosure). Update the description to enumerate the 3 prose branches:
    - **Mixed-platform** (n_chesscom_games > 0 AND n_lichess_games > 0): bullet 4 reads "Anchored at ~{anchor_rating} Elo, blending {n_chesscom_games} chess.com games (median {chesscom_median_native}, converted) with {n_lichess_games} lichess games (median {lichess_median_native})."
    - **Pure-lichess** (n_chesscom_games == 0): "Anchored at ~{anchor_rating} Elo from {n_lichess_games} lichess games (native rating)."
    - **Pure-chess.com** (n_lichess_games == 0): "Anchored at ~{anchor_rating} Elo from {n_chesscom_games} chess.com games (median {chesscom_median_native}, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26)."
    - **Suppression** (both zero): chip suppresses entirely; no tooltip.

    **Edit B — add an "Amendment history" footer**: at the bottom of the memory file, add a small section:

    ```
    ## Amendment history

    - 2026-05-27: bullet 4 contract WIDENED for the D-12 reversal (game-weighted blended anchor). Mixed-platform users now disclose per-platform game counts + native medians; pure-platform users disclose shortened single-platform forms. See `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md §Amendment` and `.planning/notes/percentile-anchor-d12-reversal.md`.
    ```

    The CRITICAL disclosure-contract language (per the memory's existing instruction) is PRESERVED — the memory still overrides `feedback_popover_copy_minimalism` for this surface; the contract widens to 5 numbers per mixed user (anchor_rating, n_chesscom_games, chesscom_median_native, n_lichess_games, lichess_median_native) rather than the 2 numbers (anchor_rating, chesscom_raw_rating) of the prior version. The widening does NOT loosen the disclosure mandate — it tightens transparency.

    DO NOT remove the bullet 1-3 descriptions or other unrelated memory content. Surgical edit only.
  </action>
  <verify>
    <automated>grep -c 'blending\|n_chesscom_games\|n_lichess_games\|composition' /home/aimfeld/.claude/projects/-home-aimfeld-Projects-Python-flawchess/memory/feedback_percentile_chip_tooltip_disclosure.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'blending\|n_chesscom_games\|n_lichess_games\|composition' feedback_percentile_chip_tooltip_disclosure.md` is >= 3.
    - All 3 prose branches (mixed / pure-lichess / pure-chess.com) are documented in the memory.
    - "Amendment history" section is present with the 2026-05-27 entry.
    - Pre-existing CRITICAL disclosure-mandate language preserved (the memory still claims the 4-bullet contract is mandatory).
    - No removal of unrelated memory content (`git diff` shows only additions + the bullet-4-description rewrite).
  </acceptance_criteria>
  <done>User memory updated with the 3-branch composition contract + amendment-history footer; disclosure mandate preserved.</done>
</task>

<task type="auto">
  <name>Task 5: Plan 13 final sanity gate + git commit prep</name>
  <files>(no file modified — verification only; commit happens in the orchestrator's post-plan-completion hook OR via this task if explicitly requested)</files>
  <read_first>
    - CLAUDE.md §"Version Control" (commit discipline)
  </read_first>
  <action>
    Verify the 4 documentation edits are coherent:

    1. `grep -c 'D-12 reversal\|D-12 Reversal\|amended 2026-05-27' .planning/REQUIREMENTS.md .planning/ROADMAP.md CHANGELOG.md` returns >= 3 (each file has at least one citation).
    2. `git diff .planning/REQUIREMENTS.md .planning/ROADMAP.md CHANGELOG.md` shows ONLY additions + the targeted PRPCR-03/PRPCR-07 rewrites (no accidental edits elsewhere).
    3. The 5 NEW PLAN.md files (09-13) all exist in `.planning/phases/94.4-peer-relative-percentile-chip-refinement/` with the expected names.

    No commit happens automatically. The user/orchestrator will commit after reviewing the diffs. If the user explicitly requests a commit in this plan's execution, follow CLAUDE.md commit discipline:
    - Stage only the documentation files modified by Plans 09-13 + the 5 new PLAN.md files.
    - Use a `docs(94.4):` commit message prefix (NOT `feat`, since no code change).
    - Suggested message: "docs(94.4): record D-12 reversal amendment in REQUIREMENTS + ROADMAP + CHANGELOG + memory".

    Per CLAUDE.md the commit must NOT be automatic — only the user/orchestrator triggers it.
  </action>
  <verify>
    <automated>ls .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-09-PLAN.md .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-10-PLAN.md .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-11-PLAN.md .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-12-PLAN.md .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-13-PLAN.md 2>&1 | wc -l && grep -c 'D-12' .planning/REQUIREMENTS.md .planning/ROADMAP.md CHANGELOG.md 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new PLAN.md files (09-13) exist.
    - Each of REQUIREMENTS.md, ROADMAP.md, CHANGELOG.md has at least one D-12 reversal citation.
    - No accidental edits in unrelated sections of the 4 documentation files.
  </acceptance_criteria>
  <done>Documentation closure verified; all 4 surfaces consistent; ready for git commit at orchestrator discretion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project documentation -> future planner | Documentation drift causes future GSD phases to build on stale assumptions. Mitigated by keeping the 4 canonical surfaces consistent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-13-01 | Repudiation | Future planner reads stale PRPCR-03 and builds wrong schema | mitigate | This plan rewrites PRPCR-03 to match the shipped schema. Audit gate (Task 5) confirms no stale schema language remains. |
| T-94.4-13-SC | Tampering | npm/pip installs | accept | No code changes; no packages touched. |
</threat_model>

<verification>
- REQUIREMENTS.md PRPCR-03 + PRPCR-07 reflect the D-12 reversal schema + tooltip contract.
- ROADMAP.md Phase 94.4 entry records the amendment + lists Plans 09-13.
- CHANGELOG.md `[Unreleased]` has a `### Changed` bullet describing the user-visible behavior.
- User-memory `feedback_percentile_chip_tooltip_disclosure.md` documents the 3 prose branches + amendment history.
- No unintended edits in the 4 documentation files.
</verification>

<success_criteria>
The Phase 94.4 documentation surfaces now agree with the D-12 reversal scope. A future planner reading REQUIREMENTS.md PRPCR-03 sees the blended-anchor schema (not the stale Lichess-precedence schema) and can build downstream phases on a correct foundation. The user memory's bullet 4 contract widens to require composition disclosure, locking the tooltip semantics for future PercentileChip edits. The Phase 94.4 amendment scope is documentation-complete; Phase 94.4 is fully closed.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-13-SUMMARY.md` when done. Record: 4 files modified with diff summaries; PRPCR-03 + PRPCR-07 final wording (paste verbatim); ROADMAP plan-list final state; CHANGELOG bullet final text; user-memory amendment-history entry. Confirm no accidental edits outside the targeted blocks (cite `git diff --stat` output).
</output>
