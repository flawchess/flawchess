---
phase: 06-optimize-ui-for-claude-chrome-extension-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/board/ChessBoard.tsx
  - CLAUDE.md
autonomous: true
requirements: [TEST-04, TEST-05]

must_haves:
  truths:
    - "User can make moves by clicking source square then target square (two-click)"
    - "Selected square is visually highlighted with a yellow tint"
    - "Board container has data-testid='chessboard'"
    - "CLAUDE.md contains Browser Automation Rules section"
    - "Click-to-move and drag-and-drop both work for making moves"
  artifacts:
    - path: "frontend/src/components/board/ChessBoard.tsx"
      provides: "Click-to-move via onSquareClick, board data-testid, id option"
      contains: "onSquareClick"
    - path: "CLAUDE.md"
      provides: "Browser Automation Rules section mandating data-testid, semantic HTML, ARIA"
      contains: "Browser Automation Rules"
  key_links:
    - from: "frontend/src/components/board/ChessBoard.tsx"
      to: "onPieceDrop prop"
      via: "handleSquareClick calls onPieceDrop(source, target)"
      pattern: "onPieceDrop.*selectedSquare"
    - from: "CLAUDE.md"
      to: "frontend development workflow"
      via: "mandatory rules for all future frontend code"
      pattern: "data-testid.*every interactive element"
---

<objective>
Implement click-to-move on the chess board and add the board's data-testid/id attributes, plus add Browser Automation Rules to CLAUDE.md.

Purpose: Enable the Claude Chrome extension to interact with the chess board via click (not just drag) and establish permanent rules for automation-friendly frontend code.
Output: ChessBoard.tsx with two-click move support, and CLAUDE.md with Browser Automation Rules section.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-optimize-ui-for-claude-chrome-extension-testing/06-CONTEXT.md
@.planning/phases/06-optimize-ui-for-claude-chrome-extension-testing/06-RESEARCH.md

<interfaces>
<!-- react-chessboard v5 options API (from RESEARCH.md): -->
<!-- onSquareClick?: ({ piece, square }: SquareHandlerArgs) => void -->
<!-- squareRenderer?: ({ piece, square, children }) => ReactNode -->
<!-- id?: string  (generates ${id}-board, ${id}-square-${squareId} DOM IDs) -->
<!-- Existing ChessBoard props: position, onPieceDrop, flipped, lastMove -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement click-to-move and board data-testid in ChessBoard.tsx</name>
  <files>frontend/src/components/board/ChessBoard.tsx</files>
  <action>
Add click-to-move (two-click) support to ChessBoard.tsx using the react-chessboard v5 `onSquareClick` option:

1. **State:** Add `const [selectedSquare, setSelectedSquare] = useState<string | null>(null);`

2. **Reset on position change:** Add `useEffect(() => { setSelectedSquare(null); }, [position]);` — this prevents stale selection when navigating move history.

3. **handleSquareClick callback:**
```typescript
const handleSquareClick = useCallback(
  ({ square, piece }: { square: string; piece: string | null }) => {
    if (selectedSquare === null) {
      // First click: select a square that has a piece
      if (piece) setSelectedSquare(square);
    } else if (square === selectedSquare) {
      // Clicked same square: deselect
      setSelectedSquare(null);
    } else {
      // Second click: attempt move from selectedSquare to target
      const success = onPieceDrop(selectedSquare, square);
      setSelectedSquare(null);
      if (!success && piece) {
        // Failed move but clicked another piece — reselect that piece
        setSelectedSquare(square);
      }
    }
  },
  [selectedSquare, onPieceDrop],
);
```

4. **Selection highlight in squareStyles:** Add yellow highlight on selectedSquare:
```typescript
if (selectedSquare) {
  squareStyles[selectedSquare] = {
    ...squareStyles[selectedSquare],
    backgroundColor: 'rgba(255, 255, 0, 0.5)',
  };
}
```
Use spread to merge with any existing lastMove highlight on the same square.

5. **Board options:** Add to the Chessboard options object:
   - `onSquareClick: handleSquareClick,`
   - `id: "chessboard",` (generates stable DOM IDs like `chessboard-square-e4`)

6. **Container data-testid:** Add `data-testid="chessboard"` to the outer `<div ref={containerRef}>`.

7. **Import updates:** Add `useCallback` to the imports from React (already has `useRef, useEffect, useState`).

Do NOT add a squareRenderer for custom `data-testid` on individual squares — the library already provides `data-square="e4"` natively on each square element, and the `id="chessboard"` option generates `id="chessboard-square-e4"` per square. Adding a squareRenderer wrapper risks breaking board layout (see RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build && npm run lint</automated>
  </verify>
  <done>Chess board supports two-click moves (click source piece, click target square). Selected square shows yellow highlight. Board container has `data-testid="chessboard"` and `id="chessboard"` option set. Drag-and-drop still works alongside click-to-move. Build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Browser Automation Rules section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add a new `## Browser Automation Rules` section to CLAUDE.md, placed after the existing `## Critical Constraints` section. The content must match the user's locked decision: strictly instruct that all future frontend code MUST include data-testid attributes, adhere to WCAG semantic HTML, and include ARIA labels.

Add exactly this section:

```markdown
## Browser Automation Rules

These rules ensure the UI remains compatible with the Claude Chrome extension and other automated testing tools.

### Required on All New Frontend Code

1. **`data-testid` on every interactive element** — buttons, links, inputs, select triggers, toggle items, and collapsible triggers. Use kebab-case, component-prefixed format: `data-testid="btn-import"`, `data-testid="nav-bookmarks"`, `data-testid="filter-time-control-bullet"`.

2. **Semantic HTML** — use `<button>` for clickable non-link elements, `<a>` for navigation, `<nav>` for navigation regions, `<main>` for page content, `<form>` for data entry. Never use `<div onClick>` or `<span onClick>`.

3. **ARIA labels on icon-only buttons** — any button without visible text must have `aria-label`. Example: `<Button aria-label="Flip board" data-testid="board-btn-flip">`.

4. **Major layout containers** — page containers, section headings, and modal dialogs must have `data-testid`. Example: `data-testid="dashboard-page"`, `data-testid="import-modal"`.

5. **Chess board** — the board container must have `data-testid="chessboard"` and the `id="chessboard"` option set (generates stable square IDs like `chessboard-square-e4`). Board moves must support both drag-drop and click-to-click (two clicks: source then target).

### Naming Convention
- `btn-{action}` — standalone action buttons
- `nav-{page}` — navigation links
- `filter-{name}` — filter controls
- `board-btn-{action}` — board control buttons
- `{component}-{element}-{id?}` — dynamic elements (e.g., `bookmark-card-3`)
- `square-{coord}` — chess squares (e.g., `square-e4`)
```
  </action>
  <verify>
    <automated>grep -c "Browser Automation Rules" /home/aimfeld/Projects/Python/chessalytics/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md contains a "Browser Automation Rules" section with mandatory data-testid, semantic HTML, ARIA, and chess board rules. The section is placed after Critical Constraints.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm run lint` passes
- ChessBoard.tsx contains `onSquareClick`, `selectedSquare` state, `id: "chessboard"`, and `data-testid="chessboard"`
- CLAUDE.md contains "Browser Automation Rules" section with all required rules
</verification>

<success_criteria>
Chess board supports click-to-move (two-click pattern) alongside existing drag-and-drop. Board has stable selectors for automation. CLAUDE.md permanently mandates automation-friendly frontend patterns for all future development.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimize-ui-for-claude-chrome-extension-testing/06-02-SUMMARY.md`
</output>
