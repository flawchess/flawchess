---
phase: 117-priority-queue-flaw-integration
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - app/services/eval_queue_service.py
  - app/routers/admin.py
  - tests/services/test_eval_queue.py
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03, QUEUE-05, QUEUE-06, QUEUE-08]
must_haves:
  truths:
    - "A worker claims the next job via SELECT FOR UPDATE SKIP LOCKED: tier-1 before tier-2 before tier-3; within a tier the longest-waiting user (round-robin); within a user classical>rapid>blitz>bullet then most-recent (D-117-04)"
    - "Tier-3 is a DERIVED pick over games WHERE full_evals_completed_at IS NULL AND NOT guest — no eval_jobs rows pre-populated; the claim returns the same (game_id, user_id, is_analyzed) triple regardless of tier"
    - "A leased job can be reported complete; an expired lease (lease_expiry < now) is requeued to pending by a sweep at the top of each claim"
    - "No guest game is ever claimed or enqueued by any path (QUEUE-08) — joins exclude users.is_guest and enqueue refuses guests"
    - "enqueue_tier1_game inserts an idempotent tier-1 row (ON CONFLICT DO NOTHING on the active-job partial unique); the admin-only trigger calls it (superuser-gated, not a public endpoint)"
  artifacts:
    - path: "app/services/eval_queue_service.py"
      provides: "claim_eval_job (SKIP LOCKED tiered pick + tier-3 fallback), report_job_complete, requeue_expired_leases, enqueue_tier1_game"
      contains: "FOR UPDATE OF"
    - path: "app/routers/admin.py"
      provides: "superuser-gated POST /admin/eval/enqueue-tier1/{game_id} internal trigger"
      contains: "enqueue_tier1_game"
    - path: "tests/services/test_eval_queue.py"
      provides: "Wave 0 queue tests — tier priority, round-robin, TC ordering, tier3 derived, lease expiry, guest exclusion"
      contains: "def test"
  key_links:
    - from: "app/services/eval_queue_service.py"
      to: "eval_jobs table"
      via: "sa.text CTE with FOR UPDATE OF ej SKIP LOCKED"
      pattern: "SKIP LOCKED"
    - from: "app/routers/admin.py"
      to: "app.services.eval_queue_service.enqueue_tier1_game"
      via: "superuser-gated admin endpoint"
      pattern: "enqueue_tier1_game"
    - from: "app/services/eval_queue_service.py"
      to: "users.is_guest"
      via: "guest exclusion join/filter in pick + enqueue"
      pattern: "is_guest"
---

<objective>
Build the tiered priority queue and its lease/report pluggable-worker contract: a SKIP-LOCKED claim primitive that serves tier-1 > tier-2 > tier-3 with round-robin per-user fairness and TC-weighted+recency ordering within a user (D-117-04); a derived tier-3 backlog pick (no pre-populated rows); lease-then-report with expired-lease requeue; guest exclusion on every path (QUEUE-08); and the tier-1 enqueue function plus a minimal superuser-gated internal trigger to exercise QUEUE-03's ~10s fan-out live (D-117-05). This plan delivers the queue as a standalone, fully-tested service; plan 117-03 wires it into the drain.

Purpose: QUEUE-01/02/05/06/08 + the QUEUE-03 enqueue mechanism. The claim function is the single seam the drain consumes — built and unit-tested here in isolation against synthetic eval_jobs and games rows.
Output: app/services/eval_queue_service.py, the admin trigger, tests/services/test_eval_queue.py (Wave 0).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-priority-queue-flaw-integration/117-CONTEXT.md
@.planning/phases/117-priority-queue-flaw-integration/117-RESEARCH.md
@.planning/phases/117-priority-queue-flaw-integration/117-PATTERNS.md
@.planning/phases/117-priority-queue-flaw-integration/117-VALIDATION.md

# Depends on 117-01's schema
@.planning/phases/117-priority-queue-flaw-integration/117-01-SUMMARY.md
@app/models/eval_jobs.py

# Analogs
@app/services/eval_drain.py
@app/routers/admin.py
@tests/services/test_full_eval_drain.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Queue service — claim (SKIP LOCKED), report, requeue, enqueue</name>
  <files>app/services/eval_queue_service.py</files>
  <read_first>
    117-RESEARCH.md §3 (the full SKIP LOCKED CTE SQL + SQLAlchemy async implementation _claim_eval_job, tier-3 derived-pick recommendation, enqueue_tier1_game with pg_insert on_conflict_do_nothing, lease TTL 120s, requeue sweep), 117-PATTERNS.md §"eval_queue_service.py" (short-session discipline, sa.text for SKIP LOCKED, pg_insert import), §"Net-New Patterns", app/services/eval_drain.py lines 909-937 (short-session pick discipline to mirror), app/models/eval_jobs.py, app/models/game.py (time_control_bucket enum values, played_at), app/models/user.py (is_guest).
  </read_first>
  <behavior>
    - claim_eval_job returns the tier-1/tier-2 row when one is pending (tier ASC), else falls through to the tier-3 derived pick.
    - Within a tier, the user with the oldest pending job (MIN(created_at) per user) is served first (round-robin approximation, QUEUE-02).
    - Within a user, ordering is CASE time_control_bucket classical=0 rapid=1 blitz=2 bullet=3 else=4 ASC, then played_at DESC NULLS LAST (D-117-04).
    - Guest users are excluded from both the tier-1/2 claim (JOIN users, NOT is_guest) and the tier-3 derived pick (QUEUE-08).
    - report_job_complete sets status='completed', completed_at=now for a leased job.
    - requeue_expired_leases resets status='pending' (clears leased_by/lease_expiry) for rows WHERE status='leased' AND lease_expiry < now.
    - enqueue_tier1_game inserts (tier=1, user_id, game_id, status='pending') with ON CONFLICT DO NOTHING on the active-job partial unique; it refuses to enqueue a guest's game (no-op or raises — choose no-op + Sentry-free, guests simply never enter the queue).
  </behavior>
  <action>
    Create app/services/eval_queue_service.py. Use module-level constants (no magic numbers): LEASE_TTL_SECONDS = 120 (2× the ~60s slowest tier-2/3 game; comment the derivation), WORKER_ID_SERVER_POOL = "server-pool". Define a Literal type EvalJobStatus = Literal["pending","leased","completed","failed"] and a Literal EvalTier or use the integer constants TIER_EXPLICIT=1, TIER_AUTO_WINDOW=2, TIER_IDLE=3.

    Implement these async functions, each opening its OWN short session and committing before returning (Pitfall 1 — never hold the SKIP LOCKED lock across the engine gather):
    1. async def claim_eval_job(worker_id: str = WORKER_ID_SERVER_POOL) -> ClaimedJob | None — first calls requeue_expired_leases (or inlines the sweep), then runs the tier-1/2 CTE from RESEARCH §3 verbatim via sa.text (FOR UPDATE OF ej SKIP LOCKED, parameterized worker_id + ttl, guest-excluding JOIN). If it returns a row → ClaimedJob(job_id, game_id, user_id, tier, is_analyzed). If no tier-1/2 row → run the tier-3 derived pick: a select over Game JOIN User WHERE Game.full_evals_completed_at IS NULL AND NOT User.is_guest, ordered by the SAME TC-weighted CASE + played_at DESC (no round-robin needed across the whole backlog — but DO order by user fairness if cheap; otherwise document that tier-3 is best-effort coverage). Tier-3 returns ClaimedJob with job_id=None (no eval_jobs row — the drain marks full_evals_completed_at directly; nothing to report back). Define ClaimedJob as a frozen dataclass or NamedTuple with explicit types (game_id:int, user_id:int, tier:int, is_analyzed:bool, job_id:int|None). is_analyzed MUST be computed as Game.lichess_evals_at IS NOT NULL (D-117-07 — NOT white_blunders).
    2. async def report_job_complete(job_id: int) -> None.
    3. async def requeue_expired_leases() -> int (returns count requeued; uses update(EvalJob) WHERE status='leased' AND lease_expiry < now()).
    4. async def enqueue_tier1_game(game_id: int, user_id: int) -> bool — guest-guard (look up user.is_guest; skip if guest), then pg_insert(EvalJob).on_conflict_do_nothing on the active-job index. Return whether a row was inserted.

    Follow CLAUDE.md: async SQLAlchemy 2.x select() API, Sequence (not list) for any sequence params, explicit return types, ty-clean (use # ty: ignore[invalid-argument-type] on update(Model.__table__) per existing convention). For the SKIP LOCKED CTE use sa.text(...) with bound params (no f-string interpolation — V5 SQL-injection safety, RESEARCH §Security). Keep functions shallow (extract the tier-1/2 CTE and the tier-3 derived pick into private helpers _claim_queued_job / _claim_tier3_derived if the top-level claim exceeds the nesting/LOC budget).
  </action>
  <verify>
    <automated>uv run ty check app/services/eval_queue_service.py && uv run ruff check app/services/eval_queue_service.py && uv run python -c "from app.services.eval_queue_service import claim_eval_job, report_job_complete, requeue_expired_leases, enqueue_tier1_game; print('queue svc OK')"</automated>
  </verify>
  <done>eval_queue_service.py exports claim_eval_job/report_job_complete/requeue_expired_leases/enqueue_tier1_game; SKIP LOCKED CTE present with bound params; is_analyzed derived from lichess_evals_at; guest exclusion on claim + enqueue; ty + ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Internal/admin tier-1 trigger (QUEUE-03 / D-117-05)</name>
  <files>app/routers/admin.py</files>
  <read_first>
    117-RESEARCH.md §8 (admin trigger shape), app/routers/admin.py (existing router prefix="/admin", current_superuser dependency pattern — Annotated[User, Depends(current_superuser)], get_async_session dep, the "superuser/lookups are EXPECTED — do NOT capture_exception" header note), 117-CONTEXT.md D-117-05.
  </read_first>
  <action>
    Add to app/routers/admin.py a POST route (relative path "/eval/enqueue-tier1/{game_id}" — never embed the /admin prefix per the router convention) gated by Annotated[User, Depends(current_superuser)]. Look up the Game via session.get(Game, game_id); if None, raise HTTPException(404) (an EXPECTED condition — do NOT wrap in Sentry per the admin router header note). Call enqueue_tier1_game(game_id=game_id, user_id=game.user_id). Return a small typed dict / Pydantic response indicating {"status": "enqueued"|"skipped_guest"|..., "game_id": game_id}. This is internal/admin only — NOT the user-facing "analyze this game" endpoint (that is Phase 118). Add data-testid is N/A (backend). Keep the handler thin (validation → service call → response shaping per the router convention).
  </action>
  <verify>
    <automated>uv run ty check app/routers/admin.py && uv run ruff check app/routers/admin.py && uv run python -c "from app.main import app; paths=[r.path for r in app.routes]; assert any('enqueue-tier1' in p for p in paths), paths; print('admin trigger registered')"</automated>
  </verify>
  <done>POST /admin/eval/enqueue-tier1/{game_id} exists, superuser-gated, calls enqueue_tier1_game, 404s on missing game; ty + ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 queue tests (tests/services/test_eval_queue.py)</name>
  <files>tests/services/test_eval_queue.py</files>
  <read_first>
    117-VALIDATION.md (the QUEUE-01/02/05/06/08 test rows + -k names), 117-RESEARCH.md §Validation, tests/services/test_full_eval_drain.py (session fixture, user fixture, _insert_game helper, async_session_maker monkeypatch pattern), 117-PATTERNS.md §"tests/services/test_eval_queue.py" (use a unique _TEST_USER_ID e.g. 99201 to avoid FK collisions; monkeypatch app.services.eval_queue_service.async_session_maker).
  </read_first>
  <behavior>
    - tier_priority: with a pending tier-1 row and tier-3-eligible games present, claim_eval_job returns the tier-1 game first.
    - round_robin: two users each with a pending tier-2 row; after user A's job is claimed, the next claim serves user B (oldest-pending-per-user).
    - tc_ordering: one user with classical + bullet pending tier-2 rows; classical is claimed before bullet (D-117-04 CASE).
    - tier3_derived: a game with full_evals_completed_at IS NULL and NO eval_jobs row is returned by claim_eval_job (job_id is None) when no tier-1/2 rows exist.
    - lease_expiry: claim a job (status→leased, lease_expiry set); fast-forward by setting lease_expiry into the past; requeue_expired_leases (or the next claim's sweep) returns it to pending and it is claimable again.
    - guest_exclusion: a guest user's game is NEVER claimed (tier-3) and enqueue_tier1_game on a guest game inserts no row.
  </behavior>
  <action>
    Create tests/services/test_eval_queue.py mirroring test_full_eval_drain.py's fixtures. Insert real users (one guest via is_guest=True), games with the relevant time_control_bucket + played_at, and eval_jobs rows directly, then call the queue service functions and assert DB/return state. Cover each -k name from VALIDATION.md exactly (tier_priority, round_robin, tc_ordering, tier3_derived, lease_expiry, guest_exclusion). Monkeypatch the service's async_session_maker to the test session maker. Use the per-run isolated DB (conftest.py) — do NOT reset the dev DB. Keep each test independent (clean up or use distinct game_ids per test).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_queue.py -x</automated>
  </verify>
  <done>test_eval_queue.py passes all six behaviors; the QUEUE-01/02/05/06/08 VALIDATION rows are green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **Service:** `app/services/eval_queue_service.py` — `claim_eval_job` (SKIP LOCKED tiered pick + tier-3 derived fallback), `report_job_complete`, `requeue_expired_leases`, `enqueue_tier1_game`, `ClaimedJob`, constants `LEASE_TTL_SECONDS`/`WORKER_ID_SERVER_POOL`/tier constants.
- **Endpoint:** `POST /admin/eval/enqueue-tier1/{game_id}` (superuser-gated internal trigger, D-117-05).
- **Test:** `tests/services/test_eval_queue.py`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → tier-1 trigger | Superuser-authenticated enqueue; the ONLY enqueue path exposed in Phase 117 (user-facing trigger is Phase 118) |
| future external worker → lease/report | A browser/external worker would claim+report jobs; in v1 only the server EnginePool is a worker, but the lease contract defines the boundary |
| eval_jobs claim → Postgres | FOR UPDATE SKIP LOCKED atomic claim; parameterized SQL only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-04 | Elevation of privilege | tier-1 enqueue trigger | mitigate | Route gated by Depends(current_superuser); NOT a public/unauthenticated endpoint (D-117-05). A non-admin cannot enqueue. |
| T-117-05 | Denial of service | unbounded tier-1 fan-out | mitigate | Only superusers can enqueue tier-1 in Phase 117; the active-job partial unique (uq_eval_jobs_game_active) prevents duplicate enqueues for the same game (Pitfall 6). No user-facing fan-out path exists until Phase 118 (which owns rate-limiting the user trigger). |
| T-117-06 | Information disclosure / cleanup waste | guest games entering the queue | mitigate | QUEUE-08: claim joins exclude users.is_guest on every tier; enqueue_tier1_game guest-guards. No guest game is ever claimed or enqueued. |
| T-117-07 | Tampering (SQL injection) | SKIP LOCKED CTE via sa.text | mitigate | All values bound as parameters (worker_id, ttl) — no string interpolation (RESEARCH §Security V5). |
| T-117-08 | Tampering | future external worker leasing jobs | accept (Phase 117) | leased_by is server-assigned ("server-pool"); no user-controlled worker input in v1. The external-worker auth/identity boundary is a Phase-118+ concern (SEED-012 D-8 phase 2) — noted, not built here. |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (SQLAlchemy, sa.text, pg_insert all in-tree). No legitimacy gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_eval_queue.py -x` green (all six behaviors).
- `uv run ty check app/ tests/` zero errors; `uv run ruff check app/ tests/` clean.
- Admin endpoint registered and superuser-gated (import-time route assertion in Task 2 verify).
- SKIP LOCKED CTE uses bound params only (manual grep: no f-string in sa.text).
</verification>

<success_criteria>
- claim_eval_job honors tier-1 > tier-2 > tier-3, round-robin per user, TC-weighted+recency within user (D-117-04), with guest exclusion on every tier.
- Tier-3 is a derived pick (job_id=None, no pre-populated rows).
- Lease/report contract works: claim → report complete; expired lease requeued.
- enqueue_tier1_game is idempotent and guest-safe; the superuser-only admin trigger calls it.
- Wave 0 queue tests green.
</success_criteria>

<output>
Create `.planning/phases/117-priority-queue-flaw-integration/117-02-SUMMARY.md` when done.
</output>
