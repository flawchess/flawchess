---
phase: 117-priority-queue-flaw-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/20260613_120000_phase_117_queue_pv.py
  - app/models/game.py
  - app/models/game_position.py
  - app/models/eval_jobs.py
  - app/models/__init__.py
  - tests/test_migration_117.py
autonomous: true
requirements: [EVAL-04, EVAL-06, QUEUE-01, QUEUE-06]
must_haves:
  truths:
    - "The four new nullable columns exist after migration: game_positions.best_move, game_positions.pv, games.lichess_evals_at, games.full_pv_completed_at"
    - "The eval_jobs queue table exists with tier/user_id/game_id/status/leased_by/lease_expiry/created_at/completed_at and the partial unique constraint + pick/leased indexes"
    - "The D-117-10 backfill sets lichess_evals_at only for games where white_blunders IS NOT NULL (pre-existing lichess-analyzed set), leaving other games NULL"
    - "ix_games_full_pv_pending partial index exists (WHERE full_pv_completed_at IS NULL)"
    - "ORM models import cleanly: EvalJob is registered on Base.metadata; Game/GamePosition carry the new mapped columns"
  artifacts:
    - path: "alembic/versions/20260613_120000_phase_117_queue_pv.py"
      provides: "Migration adding 4 columns, eval_jobs table, indexes, D-117-10 backfill"
      contains: "def upgrade"
    - path: "app/models/eval_jobs.py"
      provides: "EvalJob ORM model with __table_args__ unique constraint + partial indexes"
      contains: "class EvalJob"
    - path: "app/models/game.py"
      provides: "lichess_evals_at + full_pv_completed_at mapped columns"
      contains: "lichess_evals_at"
    - path: "app/models/game_position.py"
      provides: "best_move + pv mapped columns"
      contains: "best_move"
    - path: "tests/test_migration_117.py"
      provides: "Wave 0 migration test — column existence + backfill correctness"
      contains: "def test"
  key_links:
    - from: "app/models/eval_jobs.py"
      to: "users.id"
      via: "ForeignKey ondelete=CASCADE"
      pattern: "ForeignKey.*users\\.id"
    - from: "alembic/versions/20260613_120000_phase_117_queue_pv.py"
      to: "20260612120000"
      via: "down_revision chain to Phase 116 head"
      pattern: "down_revision.*20260612120000"
---

<objective>
Lay the schema foundation for Phase 117: add the four new nullable columns (best_move, pv, lichess_evals_at, full_pv_completed_at), the eval_jobs queue/lease table, the PV-pending partial index, and the D-117-10 one-time lichess_evals_at backfill — all in one Alembic migration that mirrors the Phase 116 migration exactly. Register the matching ORM model fields and the new EvalJob model.

Purpose: Every downstream plan (queue service, drain integration) reads these columns and this table. Nullable-column adds on PostgreSQL 18 are instant metadata operations (no table rewrite even on the 44.4M-row game_positions table), so the migration is safe to run inline at deploy time.
Output: Migration file, EvalJob model, modified Game/GamePosition models, Wave 0 migration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-priority-queue-flaw-integration/117-CONTEXT.md
@.planning/phases/117-priority-queue-flaw-integration/117-RESEARCH.md
@.planning/phases/117-priority-queue-flaw-integration/117-PATTERNS.md
@.planning/phases/117-priority-queue-flaw-integration/117-VALIDATION.md

# Exact analogs to mirror
@alembic/versions/20260612_120000_add_full_evals_completed_at.py
@app/models/import_job.py
@app/models/game.py
@app/models/game_position.py
@tests/test_migration_116_full_evals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: EvalJob model + new column declarations on Game and GamePosition</name>
  <files>app/models/eval_jobs.py, app/models/game.py, app/models/game_position.py, app/models/__init__.py</files>
  <read_first>
    app/models/import_job.py (FK/status/timestamp pattern), app/models/game.py (full_evals_completed_at column block lines ~154-160, __table_args__ partial-index tuple lines ~47-68, is_analyzed hybrid lines ~166-184), app/models/game_position.py (eval_cp/eval_mate column block), app/models/__init__.py (model registration), 117-PATTERNS.md §"app/models/eval_jobs.py" and §"app/models/game.py"/"game_position.py".
  </read_first>
  <action>
    Create app/models/eval_jobs.py with an EvalJob(Base) model, __tablename__ = "eval_jobs". Columns per RESEARCH §3 and PATTERNS: id BigInteger autoincrement PK; tier SmallInteger NOT NULL (1=explicit, 2=auto-window, 3=idle — comment the meaning, no magic numbers in consumers); user_id ForeignKey("users.id", ondelete="CASCADE") NOT NULL index=True (QUEUE-08 cascade); game_id ForeignKey("games.id", ondelete="CASCADE") NOT NULL (FK constraints are mandatory per CLAUDE.md); status String(20) NOT NULL server_default 'pending' (use Literal-typed values pending|leased|completed|failed at the service layer, but the column itself is String(20)); leased_by String(100) nullable; lease_expiry DateTime(timezone=True) nullable; created_at DateTime(timezone=True) NOT NULL server_default func.now(); completed_at DateTime(timezone=True) nullable. Add __table_args__ mirroring the game.py tuple pattern: a partial UniqueConstraint on game_id is NOT directly expressible as UniqueConstraint, so use Index("uq_eval_jobs_game_active", "game_id", unique=True, postgresql_where=sa.text("status IN ('pending','leased')")) to permit re-enqueue after completion (resolves RESEARCH Open Question 1 + Pitfall 6 — partial unique on active jobs only); Index("ix_eval_jobs_pick", "tier", "user_id", "created_at", postgresql_where=sa.text("status = 'pending'")); Index("ix_eval_jobs_leased", "lease_expiry", postgresql_where=sa.text("status = 'leased'")). Add explicit return-type-free mapped_column declarations with Mapped[...] annotations (ty requires explicit types). Register EvalJob in app/models/__init__.py alongside the other models so Base.metadata includes it.

    In app/models/game.py: add two columns after full_evals_completed_at, mirroring its block exactly — lichess_evals_at: Mapped[datetime.datetime | None] = mapped_column(sa.DateTime(timezone=True), nullable=True) with a comment "D-117-06: provenance — set ONLY at import when lichess %evals are ingested; NULL = engine-written, transplant-safe for WR-02"; and full_pv_completed_at: Mapped[datetime.datetime | None] = mapped_column(sa.DateTime(timezone=True), nullable=True) with comment "D-117-12: PV/best_move completion dimension, parallel to full_evals_completed_at; a game can be eval-complete but PV-missing". Do NOT touch the is_analyzed hybrid — per D-117-09 it correctly stays white_blunders IS NOT NULL.

    In app/models/game_position.py: add two columns after eval_mate — best_move: Mapped[str | None] = mapped_column(String(5), nullable=True) (comment "D-117-01 + D-117-03: PV[0] UCI (D-117-03 locks UCI not SAN), 4 chars normal / 5 promo; transplanted via full_hash dedup in opening region") and pv: Mapped[str | None] = mapped_column(Text, nullable=True) (comment "D-117-02: ~12-ply UCI refutation line, stored only at flaw-adjacent ply (N+1)"). Ensure Text and String are imported.
  </action>
  <verify>
    <automated>uv run ty check app/ 2>&1 | tail -5 && uv run python -c "from app.models.eval_jobs import EvalJob; from app.models.game import Game; from app.models.game_position import GamePosition; assert hasattr(Game, 'lichess_evals_at'); assert hasattr(Game, 'full_pv_completed_at'); assert hasattr(GamePosition, 'best_move'); assert hasattr(GamePosition, 'pv'); print('models OK')"</automated>
  </verify>
  <done>EvalJob model exists and is registered on Base.metadata; Game has lichess_evals_at + full_pv_completed_at; GamePosition has best_move + pv; ty passes with zero errors; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration — columns, eval_jobs table, indexes, D-117-10 backfill</name>
  <files>alembic/versions/20260613_120000_phase_117_queue_pv.py</files>
  <read_first>
    alembic/versions/20260612_120000_add_full_evals_completed_at.py (exact mirror — header, revision/down_revision, op.add_column, op.create_index postgresql_where, inline op.execute backfill, downgrade ordering), 117-RESEARCH.md §4 (full migration structure + backfill SQL + Pitfall 5 ordering), 117-PATTERNS.md §migration. Confirm current head: down_revision MUST be "20260612120000".
  </read_first>
  <action>
    Create alembic/versions/20260613_120000_phase_117_queue_pv.py with revision "20260613120000", down_revision "20260612120000" (the Phase 116 head — VERIFIED). Module docstring cites Phase 117 EVAL-04/EVAL-06/QUEUE-01/06 + D-117-01/02/06/10/12. upgrade() steps in this order (Pitfall 5 — backfill LAST):
    (1) op.add_column game_positions.best_move sa.String(5) nullable=True; op.add_column game_positions.pv sa.Text() nullable=True.
    (2) op.add_column games.lichess_evals_at sa.DateTime(timezone=True) nullable=True; op.add_column games.full_pv_completed_at sa.DateTime(timezone=True) nullable=True.
    (3) op.create_index "ix_games_full_pv_pending" on games ["id"] postgresql_where="full_pv_completed_at IS NULL".
    (4) op.create_table "eval_jobs" with the columns matching the EvalJob model (id BigInteger autoincrement PK, tier SmallInteger, user_id Integer FK users.id ondelete CASCADE, game_id Integer FK games.id ondelete CASCADE, status String(20) server_default 'pending', leased_by String(100), lease_expiry/created_at/completed_at timestamptz; created_at server_default sa.func.now()). Then op.create_index "uq_eval_jobs_game_active" unique=True postgresql_where="status IN ('pending','leased')"; "ix_eval_jobs_pick" on (tier,user_id,created_at) postgresql_where="status = 'pending'"; "ix_eval_jobs_leased" on (lease_expiry) postgresql_where="status = 'leased'".
    (5) op.execute the D-117-10 backfill verbatim: UPDATE games SET lichess_evals_at = COALESCE(imported_at, NOW()) WHERE white_blunders IS NOT NULL AND lichess_evals_at IS NULL. (games is ~558k rows, not 44M — no batching needed; A3 in RESEARCH — if a prod row-count check is wanted that is a deploy-time note, not a code gate.)
    downgrade(): reverse order — drop the three eval_jobs indexes, drop_table eval_jobs, drop_index ix_games_full_pv_pending, drop the four columns. No data restoration for the backfill (irreversible by design — note in docstring).
    Run the migration against the dev DB to confirm it applies cleanly (do NOT reset the DB — apply forward only): docker compose dev DB must be up, then uv run alembic upgrade head.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml -p flawchess-dev up -d >/dev/null 2>&1; uv run alembic upgrade head && uv run alembic current</automated>
  </verify>
  <done>Migration applies cleanly to dev DB; alembic current shows revision 20260613120000; downgrade()/upgrade() are symmetric in index/table ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 migration test (tests/test_migration_117.py)</name>
  <files>tests/test_migration_117.py</files>
  <read_first>
    tests/test_migration_116_full_evals.py (exact structural mirror — _make_alembic_cfg, _run_upgrade, _column_exists via information_schema, backfill correctness via insert-before-upgrade), 117-RESEARCH.md §Validation, 117-VALIDATION.md row "D-117-10". tests/conftest.py (per-run isolated DB clones a migrated template — the migration is ALREADY applied in the test DB; tests assert post-state, they do not re-run upgrade from scratch unless following the 116 test's pattern).
  </read_first>
  <behavior>
    - Test: all four new columns exist (information_schema.columns) — game_positions.best_move, game_positions.pv, games.lichess_evals_at, games.full_pv_completed_at.
    - Test: eval_jobs table exists with the expected columns.
    - Test (D-117-10 backfill correctness): a game with white_blunders IS NOT NULL has lichess_evals_at NOT NULL after migration; a game with white_blunders IS NULL has lichess_evals_at NULL. Mirror the 116 test's insert-then-assert approach (or assert against the migrated template state — follow whichever pattern the 116 test uses for backfill verification).
    - Test: ix_games_full_pv_pending and the three eval_jobs indexes exist (pg_indexes).
  </behavior>
  <action>
    Create tests/test_migration_117.py mirroring tests/test_migration_116_full_evals.py. Set BASE_REVISION = "20260612120000" and TARGET_REVISION = "head" (or "20260613120000"). Assert the four columns + eval_jobs table + the four indexes exist, and verify the D-117-10 backfill semantics (white_blunders-set ⇒ lichess_evals_at-set; white_blunders-NULL ⇒ lichess_evals_at-NULL). Use the same Alembic-config + information_schema/pg_indexes query helpers as the 116 test. Keep the test self-contained (no dependency on other test modules' fixtures); use a dedicated test user/game id range to avoid FK collisions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_117.py -x</automated>
  </verify>
  <done>tests/test_migration_117.py passes; covers column existence, eval_jobs table, the four indexes, and D-117-10 backfill correctness (the VALIDATION.md "D-117-10" row is green).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
(Cumulative across the phase — this plan delivers the schema substrate.)
- **Table:** `eval_jobs` (queue/lease) — id, tier, user_id, game_id, status, leased_by, lease_expiry, created_at, completed_at.
- **Columns:** `game_positions.best_move` (varchar(5) UCI), `game_positions.pv` (Text UCI), `games.lichess_evals_at` (timestamptz), `games.full_pv_completed_at` (timestamptz).
- **Indexes:** `ix_games_full_pv_pending`, `uq_eval_jobs_game_active` (partial unique), `ix_eval_jobs_pick`, `ix_eval_jobs_leased`.
- **Model:** `app/models/eval_jobs.py::EvalJob`.
- **Migration:** `alembic/versions/20260613_120000_phase_117_queue_pv.py`.
- **Test:** `tests/test_migration_117.py`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → prod DB | DDL + a single backfill UPDATE on the ~558k-row games table; instant nullable adds, no table rewrite |
| eval_jobs.user_id → users | FK with ON DELETE CASCADE — a deleted user's queued jobs vanish (no orphan rows, supports QUEUE-08 cleanup) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Denial of Service | migration backfill UPDATE on games | accept | games is ~558k rows (not the 44M game_positions); single-statement UPDATE is well within safe bounds; nullable column adds are instant metadata ops (no rewrite). A3 in RESEARCH flags a prod row-count sanity check as a deploy-time note. |
| T-117-02 | Tampering | eval_jobs orphan rows | mitigate | user_id and game_id are FKs with ON DELETE CASCADE — referential integrity enforced by Postgres; no bare integer references (CLAUDE.md DB rule). |
| T-117-03 | Information disclosure | new columns | accept | best_move/pv/timestamps are non-sensitive analysis outputs; no PII. |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (Alembic, SQLAlchemy, sa.text already in-tree). No legitimacy gate needed. |
</threat_model>

<verification>
- `uv run alembic upgrade head` applies cleanly; `alembic current` = 20260613120000.
- `uv run pytest tests/test_migration_117.py -x` green.
- `uv run ty check app/ tests/` zero errors; `uv run ruff check app/ tests/` clean.
- `uv run python -c "import app.models"` — EvalJob registered, no import errors.
</verification>

<success_criteria>
- Four new nullable columns + eval_jobs table + four indexes exist after migration.
- D-117-10 backfill marks exactly the white_blunders-set population (and only that population) with lichess_evals_at.
- EvalJob/Game/GamePosition ORM models match the migrated schema; ty + ruff clean.
- Wave 0 migration test green.
</success_criteria>

<output>
Create `.planning/phases/117-priority-queue-flaw-integration/117-01-SUMMARY.md` when done.
</output>
