---
phase: 118-demand-ux-auto-enqueue
plan: 03
type: execute
wave: 3
depends_on: ["118-02"]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/hooks/useEvalCoverage.ts
  - frontend/src/hooks/useEnqueueGame.ts
  - frontend/src/components/library/analysisCoverageCopy.tsx
  - frontend/src/components/library/NoAnalysisState.tsx
  - frontend/src/components/library/NoEngineAnalysisFlawsState.tsx
  - frontend/src/components/library/FlawStatsPanel.tsx
  - frontend/src/components/library/FlawComparisonGrid.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/hooks/__tests__/useEvalCoverage.test.tsx
  - frontend/src/components/library/__tests__/NoAnalysisState.test.tsx
autonomous: false
requirements: [EVUX-01, EVUX-02, EVUX-03]
user_setup: []
must_haves:
  truths:
    - "useEvalCoverage exposes analyzedCount and inFlightCount and keeps polling while in_flight_count > 0"
    - "useTier1Enqueue and useTier2Enqueue POST to the new endpoints and invalidate ['imports','eval-coverage'] on success"
    - "Library flaw surfaces show real 'N of M analyzed' (+ 'K in progress' when in-flight), never 'coming soon'"
    - "Coverage falls below LOW_COVERAGE_THRESHOLD → a non-guest, not-in-flight user sees an 'Analyze more' CTA"
    - "Per-game NoAnalysisState shows: guest → sign-up CTA; not-analyzed → 'Analyze this game'; in-flight → 'Analyzing…'; analyzed → nothing"
    - "Bulk 'Analyze more' is disabled/replaced by 'Analyzing your recent games… N of M' while the user has a tier-2 job in-flight, re-enabled when drained"
    - "Guests see 'Sign up to unlock full-game analysis' in every analyze slot, never a silent no-op"
  artifacts:
    - path: "frontend/src/hooks/useEnqueueGame.ts"
      provides: "useTier1Enqueue, useTier2Enqueue mutations with cache invalidation"
      contains: "useTier2Enqueue"
    - path: "frontend/src/components/library/analysisCoverageCopy.tsx"
      provides: "LOW_COVERAGE_THRESHOLD constant + real coverage copy (no 'coming soon')"
      contains: "LOW_COVERAGE_THRESHOLD"
  key_links:
    - from: "frontend/src/hooks/useEnqueueGame.ts"
      to: "/imports/eval/tier1 and /imports/eval/tier2"
      via: "apiClient.post in mutationFn"
      pattern: "imports/eval/tier"
    - from: "useTier1Enqueue/useTier2Enqueue onSuccess"
      to: "['imports','eval-coverage'] query"
      via: "queryClient.invalidateQueries"
      pattern: "invalidateQueries"
    - from: "NoAnalysisState / FlawDenominatorPill / NoEngineAnalysisFlawsState"
      to: "useEvalCoverage + useUserProfile"
      via: "inFlightCount/analyzedCount + is_guest gating"
      pattern: "useEvalCoverage|is_guest"
    - from: "FlawComparisonGrid"
      to: "useEvalCoverage + analysisCoverageCopy"
      via: "real coverage display, removed stale 'coming soon' imports, isError branch"
      pattern: "useEvalCoverage"
---

<objective>
Build the frontend demand-UX surfaces on top of the extended /imports/eval-coverage contract: extend the polling hook and types, add the tier-1/tier-2 mutation hooks, replace the stale "coming soon" coverage copy with real "N of M analyzed", and wire the four Library flaw surfaces with coverage badges, low-coverage CTAs, per-game analyze buttons, in-flight states, and guest sign-up promotion. Scope is strictly the Library flaw surfaces — Endgames/Openings/GlobalStats gates are untouched.

Purpose: EVUX-01 (analyze affordances + progress, incl. the D-118-06 bulk disabled-until-drained UX), EVUX-02 (coverage indicators + CTA), EVUX-03 (in-flight status without blind refresh, consuming the D-118-12 extended response), ROADMAP-118 criterion 5 / D-118-13 (guest sign-up promotion in every analyze slot).
Output: extended useEvalCoverage, new useEnqueueGame hook, repurposed analysisCoverageCopy with LOW_COVERAGE_THRESHOLD, updated NoAnalysisState / NoEngineAnalysisFlawsState / FlawDenominatorPill (in FlawStatsPanel) / FlawComparisonGrid, plus the per-game button wired into LibraryGameCard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-demand-ux-auto-enqueue/118-CONTEXT.md
@.planning/phases/118-demand-ux-auto-enqueue/118-RESEARCH.md
@.planning/phases/118-demand-ux-auto-enqueue/118-PATTERNS.md
@.planning/phases/118-demand-ux-auto-enqueue/118-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extend types + useEvalCoverage + new useEnqueueGame mutations</name>
  <files>frontend/src/types/api.ts, frontend/src/hooks/useEvalCoverage.ts, frontend/src/hooks/useEnqueueGame.ts, frontend/src/hooks/__tests__/useEvalCoverage.test.tsx</files>
  <read_first>
    - frontend/src/types/api.ts (EvalCoverageResponse interface ~lines 192-196 — the type to extend; source-of-truth analog)
    - frontend/src/hooks/useEvalCoverage.ts (full file ~53 lines — the hook to extend; EVAL_COVERAGE_POLL_INTERVAL_MS=3000 already defined, reuse)
    - frontend/src/hooks/useImport.ts (useImportTrigger ~lines 24-31 — the useMutation analog)
    - frontend/src/hooks/usePositionBookmarks.ts (~lines 14-22 — useMutation with onSuccess invalidateQueries pattern)
    - frontend/src/hooks/__tests__/useEvalCoverage.test.tsx (existing hook test — file to extend)
  </read_first>
  <behavior>
    - Test: useEvalCoverage returns analyzedCount and inFlightCount from the response (defaulting to 0 when absent).
    - Test: the refetchInterval stop condition keeps polling when in_flight_count > 0 even if pct_complete === 100; stops only when pct_complete === 100 AND total_count > 0 AND in_flight_count === 0.
  </behavior>
  <action>
    In frontend/src/types/api.ts (PATTERNS.md "Extended EvalCoverageResponse"): add analyzed_count: number and in_flight_count: number to EvalCoverageResponse. Add EnqueueTier1Response { status: 'enqueued' | 'skipped_guest' | 'already_queued'; game_id: number } and EnqueueTier2Response { status: 'enqueued' | 'in_flight' | 'nothing_to_enqueue' | 'skipped_guest'; enqueued_count: number } matching the backend Literals exactly.

    In useEvalCoverage.ts (PATTERNS.md "useEvalCoverage extension"): extend the refetchInterval guard to `&& data.in_flight_count === 0` (keep polling while analysis is in-flight — RESEARCH.md Open Question 2). Add analyzedCount: data?.analyzed_count ?? 0 and inFlightCount: data?.in_flight_count ?? 0 to the return object. Leave pendingCount/totalCount/pct/isPending/isLoading unchanged (existing Endgames/Openings/GlobalStats consumers must be unaffected).

    Create frontend/src/hooks/useEnqueueGame.ts (PATTERNS.md "new useEnqueueGame.ts"): export useTier1Enqueue() returning a useMutation whose mutationFn POSTs to `/imports/eval/tier1/${gameId}` (gameId passed as the mutation variable, typed EnqueueTier1Response) and onSuccess calls queryClient.invalidateQueries({ queryKey: ['imports', 'eval-coverage'] }). Export useTier2Enqueue() with mutationFn POSTing to `/imports/eval/tier2` (no body, typed EnqueueTier2Response) and the same onSuccess invalidation (RESEARCH.md Pitfall 5 — button disables within one render cycle, no waiting on the 3s poll). Use useQueryClient from @tanstack/react-query. Knip-clean: every export must be imported by a consumer in Task 2/3.

    Extend useEvalCoverage.test.tsx with the two behavior tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useEvalCoverage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - useEvalCoverage.test.tsx passes
    - useEnqueueGame.ts exports useTier1Enqueue and useTier2Enqueue, both invalidating ['imports','eval-coverage']
    - EvalCoverageResponse type contains analyzed_count and in_flight_count
    - `cd frontend && npm run lint` passes (knip not yet — exports are consumed in later tasks of this plan)
  </acceptance_criteria>
  <done>The hook layer exposes the new counts + correct polling lifecycle, and the two enqueue mutations exist with optimistic cache invalidation.</done>
</task>

<task type="auto">
  <name>Task 2: repurpose coverage copy + wire FlawStatsPanel/FlawComparisonGrid coverage badge + bulk CTA + in-flight</name>
  <files>frontend/src/components/library/analysisCoverageCopy.tsx, frontend/src/components/library/FlawStatsPanel.tsx, frontend/src/components/library/FlawComparisonGrid.tsx, frontend/src/components/library/NoEngineAnalysisFlawsState.tsx</files>
  <read_first>
    - frontend/src/components/library/analysisCoverageCopy.tsx (full file ~23 lines — the stale "coming soon" copy to replace; its three consumers)
    - frontend/src/components/library/FlawStatsPanel.tsx (FlawDenominatorPill ~lines 27-44 — the badge to extend; source-of-truth analog)
    - frontend/src/components/library/NoEngineAnalysisFlawsState.tsx (full ~30 lines — empty-state to replace)
    - frontend/src/components/library/FlawComparisonGrid.tsx (where coverage copy/badge is consumed)
    - frontend/src/pages/Import.tsx (~lines 292-311 — guest-promo CTA pattern: useUserProfile, navigate to /login?tab=register)
    - frontend/src/lib/theme.ts (color constants — never hard-code semantic colors)
    - frontend/src/components/ui/button.tsx (variant="default" = primary brand brown; variant="brand-outline" = secondary — never hand-roll button colors)
    - frontend/src/hooks/useEnqueueGame.ts (useTier2Enqueue from Task 1)
  </read_first>
  <action>
    Repurpose analysisCoverageCopy.tsx (PATTERNS.md "analysisCoverageCopy.tsx replacement"): remove the ANALYSIS_COVERAGE_PARAGRAPHS "partial / coming soon" array and the static ANALYSIS_COVERAGE_COPY JSX. Export `LOW_COVERAGE_THRESHOLD: number = 0.8` (D-118-09 named constant, ~<80%). If any short shared InfoPopover copy is still needed, export it as a real (non-"coming soon") string/JSX. Update all three consumers to stop importing the removed symbols (knip-clean — CI fails on dead exports).

    Extend FlawDenominatorPill in FlawStatsPanel.tsx (PATTERNS.md "FlawDenominatorPill"): add inFlightCount: number and isGuest: boolean props. After the "Games" span, when inFlightCount > 0 append " · {inFlightCount} in progress" text and update aria-label accordingly. When analyzedN / totalN < LOW_COVERAGE_THRESHOLD AND inFlightCount === 0 AND !isGuest, render a secondary "Analyze more" button (variant="brand-outline", data-testid="btn-analyze-more-pill", aria-label="Analyze more games") wired to useTier2Enqueue(). When isGuest, that low-coverage slot shows a "Sign up to unlock full-game analysis" link to /login?tab=register instead (data-testid="btn-signup-for-analysis-pill"). Replace the removed ANALYSIS_COVERAGE_COPY InfoPopover body with updated copy that does not mention "coming soon". Source inFlightCount/analyzedN/totalN from useEvalCoverage at the panel level and pass down (do not call the hook inside the leaf pill if the panel already has the data — follow the existing prop-drilling shape). Use the floor text-sm (CLAUDE.md), data-testid + aria-label on every interactive element.

    Replace NoEngineAnalysisFlawsState body (PATTERNS.md "NoEngineAnalysisFlawsState replacement"): add isGuest: boolean and inFlightCount: number props plus the analyzed/total counts. Branch: isGuest → heading "Sign up to unlock full-game analysis" + variant="brand-outline" link to /login?tab=register (data-testid="btn-signup-for-analysis-flaws"); !isGuest && inFlightCount > 0 → heading "Analyzing your games…" + "N of M analyzed" text, NO button; !isGuest && inFlightCount === 0 → heading "Analyze your games" + variant="default" primary button calling useTier2Enqueue() (data-testid="btn-analyze-more", aria-label="Analyze more games"). Keep the outer div + icon + data-testid="flaws-no-engine-analysis". Source counts from useEvalCoverage / useUserProfile at the FlawsTab level or inside this component per the existing call shape.

    Update FlawComparisonGrid.tsx where it consumed the old copy so it shows real coverage / no longer imports removed symbols. Apply the SAME change to mobile and desktop branches if the component has both (CLAUDE.md mirror rule).

    isError handling (CLAUDE.md, T-118-13): every surface in this task that renders from useEvalCoverage data (FlawDenominatorPill, NoEngineAnalysisFlawsState, FlawComparisonGrid) must include an isError branch showing "Failed to load analysis status. Something went wrong. Please try again in a moment." — NEVER let an eval-coverage query error fall through to "0 of 0 analyzed" / an empty state, which would misread as "no data". Expose isError from useEvalCoverage if not already returned.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` passes and `npm run knip` reports no dead exports / unused deps
    - analysisCoverageCopy.tsx exports LOW_COVERAGE_THRESHOLD and contains no "coming soon" string (`grep -ci "coming soon" frontend/src/components/library/analysisCoverageCopy.tsx` == 0)
    - FlawDenominatorPill renders "· N in progress" when inFlightCount > 0 and an "Analyze more" CTA below threshold (data-testid="btn-analyze-more-pill")
    - NoEngineAnalysisFlawsState branches guest / in-flight / analyze-CTA with the three data-testids
    - FlawComparisonGrid no longer imports the removed analysisCoverageCopy symbols and renders real coverage (not "coming soon"); change applied to both desktop and mobile branches if present
    - every surface rendering from useEvalCoverage (FlawDenominatorPill, NoEngineAnalysisFlawsState, FlawComparisonGrid) has an isError branch showing "Failed to load analysis status…", not a fall-through to "0 of 0 analyzed"
    - every new button/link has data-testid + (icon-only) aria-label; no text-xs introduced (except permitted tooltip surfaces); button colors use Button variants, not bg-* overrides
  </acceptance_criteria>
  <done>The aggregate coverage surfaces show honest "N of M analyzed · K in progress", a low-coverage/zero-analyzed CTA wired to tier-2, and guest promotion — all "coming soon" copy gone.</done>
</task>

<task type="auto">
  <name>Task 3: per-game NoAnalysisState branching (tier-1 / guest / in-flight) + LibraryGameCard wiring</name>
  <files>frontend/src/components/library/NoAnalysisState.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/__tests__/NoAnalysisState.test.tsx</files>
  <read_first>
    - frontend/src/components/library/NoAnalysisState.tsx (full ~37 lines — the component to branch; source-of-truth analog)
    - frontend/src/components/results/LibraryGameCard.tsx (the two NoAnalysisState call sites ~lines 536, 654; game.analysis_state field; game.game_id)
    - frontend/src/types/library.ts (GameFlawCard.analysis_state: 'analyzed' | 'no_engine_analysis' — note: 'analyzed' already covers lichess-eval games, so D-118-07 maps directly: 'no_engine_analysis' → not-analyzed; 'analyzed' → no affordance)
    - frontend/src/pages/Import.tsx (~lines 292-311 — guest CTA pattern)
    - frontend/src/hooks/useEnqueueGame.ts (useTier1Enqueue from Task 1) + frontend/src/hooks/useEvalCoverage.ts (inFlightCount for the in-flight state)
    - frontend/src/components/ui/button.tsx (variants)
  </read_first>
  <action>
    Branch NoAnalysisState (PATTERNS.md "NoAnalysisState extension" + D-118-07/11/13): add props isGuest: boolean, isAnalyzed: boolean, isInFlight?: boolean (keep gameId). Render: isGuest → "Sign up to unlock analysis" link (variant="brand-outline") to /login?tab=register, data-testid="btn-signup-for-analysis", aria-label="Sign up to unlock game analysis"; !isAnalyzed && !isInFlight → "Analyze this game" button (variant="default" or "brand-outline" per visual fit) calling useTier1Enqueue() with this gameId, data-testid={`btn-analyze-game-${gameId}`}, aria-label; !isAnalyzed && isInFlight → pulsing "Analyzing…" text span (no button), data-testid={`analyzing-${gameId}`}; isAnalyzed → return null (the caller already only renders this component for non-analyzed games, but the null branch is the D-118-07 belt-and-suspenders for lichess-eval "analyzed" games). Keep the existing outer pill span styling for the non-button states. Stop importing the removed ANALYSIS_COVERAGE_COPY.

    Wire LibraryGameCard.tsx: at the two NoAnalysisState call sites, pass isAnalyzed derived from game.analysis_state === 'analyzed', isGuest from useUserProfile().data?.is_guest, and isInFlight by checking whether this game is the one the user just tier-1-clicked (localized — D-118-11 says NO per-card spinners across the archive; the in-flight pulse is only for the specific clicked game, so track the clicked gameId in local state set by the tier-1 mutation, or derive from the mutation's pending/variables). Do NOT add a global per-card spinner driven by aggregate inFlightCount. Apply to BOTH call sites (CLAUDE.md mirror rule — desktop + mobile/compact card layouts).

    Create frontend/src/components/library/__tests__/NoAnalysisState.test.tsx covering the four branches (guest CTA, analyze button present + fires mutation, in-flight pulse, analyzed → null).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/library/__tests__/NoAnalysisState.test.tsx && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - NoAnalysisState.test.tsx passes all four branch tests
    - NoAnalysisState renders btn-analyze-game-{gameId} only when !isAnalyzed && !isInFlight; renders btn-signup-for-analysis for guests; returns null when isAnalyzed
    - LibraryGameCard passes isAnalyzed/isGuest/isInFlight at BOTH NoAnalysisState call sites
    - the in-flight pulse is localized to the clicked game, not driven by aggregate inFlightCount across all cards
    - `cd frontend && npm run lint && npm run knip` clean
  </acceptance_criteria>
  <done>Per-game analyze affordance follows the D-118-07 visibility rule (guest → CTA; not-analyzed → tier-1 button; analyzed incl. lichess-eval → nothing), with a localized "Analyzing…" state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: human visual verification of demand-UX surfaces</name>
  <action>Pause for human visual verification — no automated assertion can confirm the coverage copy, in-flight badge animation, disabled-until-drained bulk button, localized per-game "Analyzing…" state, and guest promotion render correctly in the browser. Present the what-built + how-to-verify below and wait for the resume signal.</action>
  <what-built>
    The four Library flaw surfaces now render real "N of M analyzed · K in progress" coverage, a low-coverage / zero-analyzed "Analyze more" tier-2 CTA (disabled→"Analyzing your recent games…" while a tier-2 job is in-flight, re-enabled when drained), per-game "Analyze this game" tier-1 buttons with a localized "Analyzing…" state, and guest "Sign up to unlock full-game analysis" promotion in every analyze slot. Polling drives the badge and disabled-state without page reload; tier-1/tier-2 mutations invalidate the eval-coverage query so the UI updates within one render cycle.
  </what-built>
  <how-to-verify>
    Run the app locally (`bin/run_local.sh` or `npm run dev` + backend). Against the existing dev DB (do NOT reset the DB):
    1. As a signed-in non-guest with some unanalyzed games, open Library → Flaws. Confirm the coverage badge shows real "N of M analyzed" (NOT "coming soon").
    2. If coverage is below 80%, confirm an "Analyze more" CTA appears. Click it; confirm the badge shows "· K in progress" and the bulk button becomes "Analyzing your recent games… N of M" / disabled WITHOUT reloading the page. Confirm it re-enables once the tier-2 window drains.
    3. Open a game card with no engine analysis: confirm a "Analyze this game" button. Click it; confirm a localized "Analyzing…" state on THAT card only (no spinners on other cards). Cross-check the ~10s tier-1 feel.
    4. Open a lichess-eval (already "analyzed") game: confirm NO "Analyze this game" button.
    5. Log out / use as guest: confirm "Sign up to unlock full-game analysis" replaces both the bulk and per-game analyze affordances (never a silent no-op), linking to the register tab.
    6. Confirm Endgames / Openings / GlobalStats coverage messaging is UNCHANGED (D-118-08 scope guard).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which surface, expected vs actual)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /imports/eval/tier1 \| tier2 \| eval-coverage | client triggers analysis + reads coverage; server is the authority |
| guest session → analyze affordance slots | guest must be promoted, never silently no-op'd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-11 | Elevation of Privilege | guest exclusion enforced only in UI | mitigate | UI shows a sign-up CTA, but the SERVER (Plan 02) is the real gate — both endpoints return skipped_guest server-side regardless of what the client sends. The frontend gate is UX, not security; the backend guest guard (T-118-07) is authoritative. |
| T-118-12 | Denial of Service | rapid tier-2 re-clicks before poll reflects in-flight | mitigate | onSuccess invalidateQueries forces an immediate refetch so the button disables within one render cycle (RESEARCH.md Pitfall 5); backend in-flight gate (T-118-08) is the hard stop. |
| T-118-13 | Information Disclosure | coverage badge revealing data when query errors | mitigate | Every useQuery loading/data/empty chain on these surfaces must include an isError branch (CLAUDE.md) — show "Failed to load… try again", never fall through to "no games" or leak partial counts. |
| T-118-SC | Tampering | npm installs | accept | Zero new frontend dependencies this phase. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npm run knip` all green
- `cd frontend && npm run build` succeeds (type-check + bundle)
- Human checkpoint (Task 4) approved
</verification>

<success_criteria>
- All Library flaw surfaces show honest is_analyzed coverage + in-flight + CTAs; zero "coming soon" copy remains
- Per-game and bulk analyze affordances follow the D-118-05/06/07 visibility + disabled-until-drained rules
- Guests are promoted, never silently no-op'd, in every slot
- Endgames/Openings/GlobalStats untouched
</success_criteria>

<output>
Create `.planning/phases/118-demand-ux-auto-enqueue/118-03-SUMMARY.md` when done
</output>
