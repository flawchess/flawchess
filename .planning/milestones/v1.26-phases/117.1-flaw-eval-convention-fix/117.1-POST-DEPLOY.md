# Phase 117.1 — Post-Deploy Verification (EVALFIX-05, non-blocking)

These items run AFTER the phase ships to prod (clean-slate re-eval via the background drain).
They are HUMAN-UAT / monitoring items, NOT blocking dev tasks, and do NOT gate phase
completion. Do not run `bin/reset_db.sh`.

## Re-eval (automatic, ~24h)

The clean-slate migration (Plan 02) wipes engine eval/flaw data on deploy. The background
drain re-materializes everything under the post-move convention automatically. The prior
engine eval run (~24h) is explicitly acceptable to lose (CONTEXT.md, user-confirmed).

- [ ] After deploy, confirm the drain is re-enqueuing/processing engine games (eval_jobs
      refilling, `full_evals_completed_at` repopulating for engine games).
- [ ] Spot-check the three regression fixtures on PROD once re-eval reaches them:
      games 1420780 + 1073118 now show non-zero, coherent engine-game flaws; 640092 stays at 3.

## Flaw-PV coverage re-verification (EVALFIX-05)

- [ ] Once re-eval has progressed materially, re-verify the ~32% flaw-PV coverage TODO
      (git `4b40960e`). The off-by-one is the suspected cause; confirm coverage improves
      under the corrected post-move convention. If still low, capture a follow-up seed —
      do NOT expand scope here (SEED-043 lichess best_move/PV backfill is separate).

## Notes

- Re-eval throughput baseline: 5.83 pos/s on 6 SCHED_IDLE workers ≈ 8.4k games/day
  (STATE.md / CLAUDE.md Phase 116 accounting).
- Watch prod container RSS during the re-eval surge (prod OOM history — see CLAUDE.md
  Production Server notes); the drain yields to imports/entry-ply work by design (QUEUE-07).
