---
phase: 117.1-flaw-eval-convention-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/20260614_120000_phase_117_1_eval_convention_cleanslate.py
  - tests/test_migration_eval_convention_cleanslate.py
autonomous: true
requirements: [EVALFIX-04, EVALFIX-05]
must_haves:
  truths:
    - "A data-only migration NULLs eval_cp/eval_mate/best_move/pv on game_positions rows belonging to engine games (lichess_evals_at IS NULL, full_evals_completed_at IS NOT NULL)."
    - "The migration clears games.full_evals_completed_at and games.full_pv_completed_at for those engine games and DELETEs their game_flaws rows."
    - "The migration TRUNCATEs the eval_jobs queue/lease table (clean slate for re-enqueue under the new convention)."
    - "Lichess games' eval data (lichess_evals_at IS NOT NULL) is NEVER touched."
    - "The large game_positions UPDATE is batched/documented for the 44.4M-row table; the downgrade is a documented no-op (irreversible data wipe)."
  artifacts:
    - path: "alembic/versions/20260614_120000_phase_117_1_eval_convention_cleanslate.py"
      provides: "Clean-slate data migration on top of the Phase 117 head (down_revision = 20260613120000)"
      contains: "def upgrade"
    - path: "tests/test_migration_eval_convention_cleanslate.py"
      provides: "Test asserting engine-game eval data is cleared and lichess data is preserved"
  key_links:
    - from: "alembic upgrade head"
      to: "game_positions / games / game_flaws / eval_jobs"
      via: "data-only UPDATE/DELETE/TRUNCATE gated on lichess_evals_at IS NULL"
      pattern: "lichess_evals_at IS NULL"
---

<objective>
Ship the clean-slate data migration that wipes the mis-stored (pre-push) engine eval data so the corrected post-move drain (Plan 01) re-materializes everything from scratch on deploy. Columns already exist (Phase 117 head) — this is DATA-ONLY.

Purpose: Removes the off-by-one engine eval/flaw data so re-eval under the post-move convention produces correct flaw stats. The background drain re-fills everything automatically on prod after deploy (the ~24h prior engine run is explicitly acceptable to lose — CONTEXT.md). Closes EVALFIX-04 and enables the EVALFIX-05 post-deploy verification.
Output: One Alembic data migration + a migration test proving engine data is cleared and lichess data is preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117.1-flaw-eval-convention-fix/117.1-CONTEXT.md

# Migration pattern + current head (read first)
@alembic/versions/20260613_120000_phase_117_queue_pv.py
@alembic/versions/20260612_120000_add_full_evals_completed_at.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean-slate data migration (engine eval/flaw wipe + eval_jobs truncate)</name>
  <files>alembic/versions/20260614_120000_phase_117_1_eval_convention_cleanslate.py</files>
  <read_first>
    alembic/versions/20260613_120000_phase_117_queue_pv.py (revision id 20260613120000 = current head, becomes down_revision; note the op.execute UPDATE pattern + the "44.4M-row game_positions" + "558k games" sizing comments);
    .planning/phases/117.1-flaw-eval-convention-fix/117.1-CONTEXT.md "Migration (clean slate)" section (the exact wipe spec).
  </read_first>
  <action>
    Create a new Alembic data-only migration (no schema changes — columns already exist on the Phase 117 head).

    - `revision = "20260614120000"`, `down_revision = "20260613120000"` (the Phase 117 head). Filename `20260614_120000_phase_117_1_eval_convention_cleanslate.py`. Confirm it becomes the single new head with `uv run alembic heads` (no branching).
    - `upgrade()` performs, in order, gated on ENGINE games only (`lichess_evals_at IS NULL`):
      1. DELETE `game_flaws` for engine games with `full_evals_completed_at IS NOT NULL` (delete flaws before clearing the markers so the gating predicate still selects them). Gate via a subquery/join on `games` where `lichess_evals_at IS NULL AND full_evals_completed_at IS NOT NULL`.
      2. UPDATE `game_positions` SET `eval_cp = NULL, eval_mate = NULL, best_move = NULL, pv = NULL` for rows whose game is an engine game with `full_evals_completed_at IS NOT NULL`. This is the large statement against the 44.4M-row table.
      3. UPDATE `games` SET `full_evals_completed_at = NULL, full_pv_completed_at = NULL` WHERE `lichess_evals_at IS NULL AND full_evals_completed_at IS NOT NULL`.
      4. `TRUNCATE eval_jobs` (clears stale leases/jobs so games re-enqueue cleanly under the new convention). Use `op.execute("TRUNCATE TABLE eval_jobs")` (or `... RESTART IDENTITY` to reset the BigInteger PK sequence — note which in a comment).
    - NEVER touch lichess games (`lichess_evals_at IS NOT NULL`). Every game_positions/games/game_flaws statement MUST carry the `lichess_evals_at IS NULL` engine-game gate. Add a comment stating lichess eval data is preserved (it comes from lichess annotations and cannot be re-evaluated).
    - BATCHING / LOCKING (44.4M-row game_positions): the game_positions UPDATE is the heavy statement. Implement it in batches by game_id (or by a bounded id range) rather than one monolithic UPDATE that holds a long transaction / bloats WAL on prod. Pattern: iterate over the affected engine game_ids in chunks (a named `_BATCH_SIZE` constant, no magic number) and run a bounded UPDATE per chunk, committing per batch via `op.get_bind()` / `connection.execute` + explicit commit boundaries appropriate for a migration. Document the chosen batch size and why (mirror the import-pipeline batch-size reasoning: keep per-statement lock + WAL footprint bounded on the live prod box). If a single gated UPDATE is judged safe enough at deploy time, document that decision and the row-estimate explicitly in the migration docstring instead — but default to batching given the table size.
    - `downgrade()`: an IRREVERSIBLE data wipe → downgrade is a NO-OP with a clear comment ("clean-slate data wipe is irreversible; the background drain re-materializes engine evals/flaws under the post-move convention — downgrade does nothing"). Mirror the Phase 117 migration's irreversible-backfill comment style.
    - Migrations run automatically on backend container startup via `deploy/entrypoint.sh` — keep the upgrade idempotent/safe to re-run (the gates naturally no-op on a second run once markers are cleared).
    - No Sentry needed (migration context). ty/ruff clean.
  </action>
  <verify>
    <automated>uv run alembic heads | grep -q 20260614120000 && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    Single new head `20260614120000` on top of `20260613120000`; upgrade clears engine eval/flaw data + truncates eval_jobs, never touches lichess rows; game_positions UPDATE is batched (or batching-vs-single decision documented with row estimate); downgrade is a documented no-op; ruff + ty clean; upgrade runs against the dev/test DB.
  </acceptance_criteria>
  <done>Migration NULLs engine eval columns, clears completion markers, deletes engine game_flaws, truncates eval_jobs, preserves all lichess data, with a no-op downgrade.</done>
</task>

<task type="auto">
  <name>Task 2: Migration data-correctness test</name>
  <files>tests/test_migration_eval_convention_cleanslate.py</files>
  <read_first>
    tests/conftest.py (per-run DB / template refresh — the migration head is already applied to the test DB; seed rows then run the migration body or assert against a seeded fixture);
    the new migration's upgrade() (the exact gating predicates to mirror in assertions).
  </read_first>
  <action>
    Add a test that proves the clean-slate gating is correct WITHOUT depending on live prod data:

    - Seed (committed test rows): one ENGINE game (`lichess_evals_at IS NULL`, `full_evals_completed_at` set, non-NULL eval_cp/best_move on its game_positions, a game_flaws row, an eval_jobs row) and one LICHESS game (`lichess_evals_at IS NOT NULL`, non-NULL eval_cp on its positions, its own game_flaws row).
    - Exercise the migration's clearing logic. Prefer invoking the migration's data statements directly (import the module's helper, or replicate the gated SQL the upgrade runs) against the seeded rows, rather than re-running full `alembic upgrade` inside the test (the test DB is already at head). If the upgrade body is structured as a callable taking a connection, call it; otherwise assert the equivalent gated SQL.
    - Assert AFTER:
        * Engine game: game_positions eval_cp/eval_mate/best_move/pv are NULL; games.full_evals_completed_at and full_pv_completed_at are NULL; its game_flaws rows are gone.
        * Lichess game: eval_cp, lichess_evals_at, and its game_flaws rows are UNCHANGED (the over-touch guard).
        * eval_jobs is empty.
    - Use unique test user/game IDs (mirror the `_TEST_USER_ID` pattern) to avoid FK conflicts. Run via `uv run pytest -n auto`.
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/test_migration_eval_convention_cleanslate.py -x</automated>
  </verify>
  <acceptance_criteria>
    Test seeds one engine + one lichess game, runs the migration's clearing logic, and asserts engine eval/flaw data cleared + eval_jobs empty + lichess data fully preserved. Passes under `-n auto`.
  </acceptance_criteria>
  <done>The clean-slate gating is proven correct (engine cleared, lichess preserved) by an isolated test.</done>
</task>

</tasks>

<artifacts_produced>
- `alembic/versions/20260614_120000_phase_117_1_eval_convention_cleanslate.py` — revision `20260614120000` (down_revision `20260613120000`); data-only clean-slate wipe of engine eval/flaw data + eval_jobs TRUNCATE; batched game_positions UPDATE; no-op downgrade.
- `_BATCH_SIZE` (migration-local constant) — game_id chunk size for the batched game_positions UPDATE.
- `tests/test_migration_eval_convention_cleanslate.py` — isolated test: engine data cleared, lichess data preserved, eval_jobs emptied.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → prod DB | data-only UPDATE/DELETE/TRUNCATE on 44.4M-row game_positions + games + game_flaws + eval_jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117.1-04 | Tampering | over-deleting lichess eval data (irrecoverable — lichess evals can't be re-evaluated) | mitigate | every statement gated on `lichess_evals_at IS NULL`; migration test asserts lichess game untouched |
| T-117.1-05 | Denial of service | monolithic UPDATE on 44.4M rows holds a long lock / bloats WAL → prod stall or OOM (prod OOM history) | mitigate | batched UPDATE by game_id with a bounded `_BATCH_SIZE` and per-batch commit; row-estimate documented |
| T-117.1-06 | Repudiation | irreversible downgrade silently loses data | accept | downgrade is a documented no-op; re-eval re-materializes engine data; loss is user-confirmed acceptable |
</threat_model>

<verification>
- `uv run ruff format alembic/ tests/ && uv run ruff check alembic/ tests/ --fix`
- `uv run ty check app/ tests/` — zero errors
- `uv run alembic heads` → single head `20260614120000`
- `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` (clean round-trip; downgrade no-op)
- `uv run pytest -n auto tests/test_migration_eval_convention_cleanslate.py -x`
- Do NOT run `bin/reset_db.sh` (per project rule — design against the existing dev DB).
</verification>

<success_criteria>
- New head `20260614120000` chains off the Phase 117 head with no branching.
- Engine games: eval_cp/eval_mate/best_move/pv NULLed, completion markers cleared, game_flaws deleted.
- eval_jobs truncated.
- Lichess eval data untouched (proven by test).
- game_positions UPDATE batched (or single-statement decision documented with row estimate).
- Downgrade is a documented no-op.
- ruff + ty clean; migration test green under `-n auto`.
</success_criteria>

<output>
Create `.planning/phases/117.1-flaw-eval-convention-fix/117.1-02-SUMMARY.md` when done.
</output>
