# 117.1-02 Summary — Clean-slate data migration

**Status:** Complete (executed inline; full local gate green)
**Requirements:** EVALFIX-04, EVALFIX-05

## What changed

- `alembic/versions/20260614_120000_phase_117_1_eval_convention_cleanslate.py` — revision `20260614120000`, down_revision `20260613120000` (single head). Data-only. For engine games (`lichess_evals_at IS NULL AND full_evals_completed_at IS NOT NULL`):
  1. DELETE their `game_flaws`.
  2. NULL `game_positions.eval_cp/eval_mate/best_move/pv`.
  3. Clear `games.full_evals_completed_at` / `full_pv_completed_at` (tier-3 idle drain re-picks them).
  4. `TRUNCATE eval_jobs RESTART IDENTITY`.
  - Lichess data never touched (every gated statement carries `lichess_evals_at IS NULL`). No-op downgrade (irreversible clean slate). Single gated statement per table — prod-measured scope is ~218k positions / ~12.8k flaws / ~4.5k games / ~101 jobs, so no batching.

## Tests

- `tests/test_migration_eval_convention_cleanslate.py` — seeds one engine + one lichess game, runs the migration's gated statements, asserts engine eval/flaw data cleared + eval_jobs emptied + lichess data preserved.
- `tests/test_migration_116_full_evals.py` — adjusted the Phase 116 backfill assertion to its own revision (the clean-slate migration correctly clears engine markers at head).

## Verification

- `alembic heads` → single `20260614120000`; upgrade/downgrade/upgrade round-trip clean on dev DB. ruff + ty clean.

## Post-deploy (see 117.1-POST-DEPLOY.md)

- After deploy, the migration wipes prod engine eval/flaw data; the background drain re-materializes under post-move (~4.5k engine games + ongoing). Re-verify flaw-PV coverage (~32% TODO) once re-eval progresses.
