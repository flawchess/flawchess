# Milestone v1.13: Opening Insights

**Status:** 🚧 IN PLANNING (opened 2026-04-26; Phases 72-74 descoped 2026-04-27)
**Phases:** 70, 71, 71.1 active. Phases 72, 73, 74 descoped (rationale below).
**Total Plans:** 14 (Phase 70: 5; Phase 71: 6; Phase 71.1: 3) — all complete.
**Source:** SEED-005 — Opening weakness and strength insights

## Overview

v1.13 surfaces opening-line strengths and weaknesses for each user via auto-scanning of most-played and bookmarked openings, with templated findings and deep-links into the Move Explorer at the implicated entry position. It mirrors the v1.11 endgame-insights *placement* idiom (in-tab block, no dedicated nav) but starts as **pure templated/rule-based** — no LLM in v1.13. LLM wrap-up is deferred to v1.13.x or v1.14 once findings are in real users' hands.

**Self-referential is sufficient.** Opening positions live in book-move territory; engine eval ≈ 0.0 by construction. Absolute under-/over-performance over n ≥ 10 games is actionable for *that player* regardless of population behavior. v1.13 deliberately does NOT consume v1.12's benchmark DB output.

**Pre-v1.13 quick tasks (NOT in roadmap, gating Phase 70):**
- **PRE-01**: Drop the parity filter from `query_top_openings_sql_wdl` (`app/repositories/stats_repository.py:264-265`). Phase 70 reuses this service-layer call; the bug must be fixed first or the scan input is wrong.

## Phases

### Phase 70: Backend opening insights service

**Goal**: A user-scoped `opening_insights_service` produces ranked, deduplicated, structured `OpeningInsightFinding` payloads for every (entry_position, candidate_move) pair that classifies as a weakness or strength against the user's filtered game history.
**Depends on**: PRE-01 fix landed (top-10 parity bug); reuses existing `query_top_openings_sql_wdl` service-layer call, `apply_game_filters`, and `game_positions` Zobrist-hash schema. No new schema or migration.
**Requirements**: INSIGHT-CORE-01, INSIGHT-CORE-02, INSIGHT-CORE-03, INSIGHT-CORE-04, INSIGHT-CORE-05, INSIGHT-CORE-06, INSIGHT-CORE-07, INSIGHT-CORE-08, INSIGHT-CORE-09
**Plans**: TBD

**Success Criteria** (what must be TRUE):
1. `POST /api/insights/openings` (or equivalent) returns a structured `OpeningInsightFinding[]` payload for an authenticated user under their active filter set; equivalent filter states return equivalent rankings.
2. The scan input is a single SQL transition aggregation per (user, color) over `game_positions` for entry plies in `[3, 16]` inclusive; the configurable `MIN_GAMES_PER_CANDIDATE` floor (default 20) is enforced at the SQL HAVING level. Bookmarks are NOT consumed as algorithmic input.
3. Each (entry_position, candidate_move) pair with n ≥ 10 games is classified `weakness` (loss_rate ≥ 0.55), `strength` (score ≥ 0.60), or dropped as neutral; findings are deduplicated by Zobrist hash with deepest-opening attribution.
4. Findings are ranked within each color section by `(severity desc, n_games desc)` and capped at `WEAKNESS_CAP_PER_COLOR = 5` weaknesses + `STRENGTH_CAP_PER_COLOR = 3` strengths per color (visible ceiling 16 findings on screen).
5. Latency budget — typical user (≤ ~2k games) sees on-the-fly responses without precompute; service-layer caching is added only if heavy users (10k+) breach the budget.

**Plans**: TBD

---

### Phase 71: Frontend Stats subtab — `OpeningInsightsBlock`

**Goal**: Users see ranked weakness and strength bullets on Openings → Stats subtab, with deep-links that navigate to Openings → Moves pre-loaded at the entry FEN with the candidate move highlighted.
**Depends on**: Phase 70 (consumes `OpeningInsightFinding` payload)
**Requirements**: INSIGHT-STATS-01, INSIGHT-STATS-02, INSIGHT-STATS-03, INSIGHT-STATS-04, INSIGHT-STATS-05, INSIGHT-STATS-06
**Plans**: TBD

**Success Criteria** (what must be TRUE):
1. Authenticated user with at least one qualifying finding sees an `OpeningInsightsBlock` on Openings → Stats subtab with templated bullets like "You lose 62% as Black after 1.e4 c5 2.Nf3 d6 3.d4 cxd4 4.Nxd4 (n=18) → [open in Move Explorer]" using the existing red/green semantic theme colors.
2. Clicking a finding's deep-link navigates to Openings → Moves with the chessboard pre-loaded at the entry FEN and the candidate move visibly highlighted.
3. When the active filter set yields no findings, the block renders a clear empty-state message naming the threshold (loss_rate ≥ 0.55 / score ≥ 0.60) and the min-games floor.
4. Changing filters (color, time_control, recency, opponent_type/strength, rated) refreshes the block; loading and error states match v1.11 EndgameInsightsBlock conventions.
5. Block renders cleanly inside the mobile drawer / single-column layout — verified at 375px width with no horizontal scroll, all interactive elements ≥ 44px touch target, semantic HTML + `data-testid` per CLAUDE.md frontend rules.

**Plans**: TBD
**UI hint**: yes

---

### ~~Phase 72: Frontend Moves subtab — inline weakness/strength bullets~~ (DESCOPED 2026-04-27)

**Original goal:** Inline weakness/strength bullets next to candidate-move arrows on Openings → Moves, scoped to the currently displayed position.
**Original requirements:** INSIGHT-MOVES-01, INSIGHT-MOVES-02, INSIGHT-MOVES-03 (all moved to REQUIREMENTS.md Out of Scope).
**Rationale for descope:** The existing `MoveExplorer` row tint via `getArrowColor` (`frontend/src/components/move-explorer/MoveExplorer.tsx:228-230`) already colors every reliable candidate row red/green by win/loss rate at the displayed position. The deep-link landing case is handled by the sticky severity tint + one-shot pulse added in quick-task 260427-j41. Layering an additional bullet marker on top of an already-tinted row was redundant signal in a dense tabular UI; the n≥10 vs n≥20 threshold distinction the bullet would have surfaced is not perceptually meaningful to users. Reopen if real-user feedback shows the curated finding signal at the position is missed.

---

### ~~Phase 73 (stretch): Meta-recommendation aggregate finding~~ (DESCOPED 2026-04-27)

**Original goal:** Aggregate-level sentence above the per-finding list summarizing repertoire-level patterns.
**Original requirements:** INSIGHT-META-01 (moved to REQUIREMENTS.md Out of Scope).
**Rationale for descope:** Phase 71's per-finding cards already convey actionable per-opening weakness/strength signal at a finer granularity than an aggregate sentence. The aggregate framing is interesting but speculative — defer until real-user feedback on the per-finding view shows the aggregate is wanted.

---

### ~~Phase 74 (stretch): Bookmark-card weakness badge~~ (DESCOPED 2026-04-27)

**Original goal:** Red dot + count chip on bookmark cards surfacing weakness findings.
**Original requirements:** INSIGHT-BADGE-01 (moved to REQUIREMENTS.md Out of Scope).
**Rationale for descope:** Notification-dot density on the global nav is already high (Endgames + Openings tabs have dots); adding a third badge channel on individual bookmark cards risks alert fatigue. The Stats subtab block already lists weaknesses by opening name, which covers the discoverability gap.

---

## Coverage

All v1.13 active requirements (15 total) are mapped to exactly one phase. Pre-v1.13 quick task PRE-01 is intentionally **not** mapped — it is a pre-roadmap quick task gating Phase 70's start. INSIGHT-MOVES-01..03, INSIGHT-META-01, and INSIGHT-BADGE-01 were moved to Out of Scope on 2026-04-27 (see REQUIREMENTS.md and Phase 72/73/74 descope notes above).

| REQ-ID | Phase |
|--------|-------|
| INSIGHT-CORE-01 | 70 |
| INSIGHT-CORE-02 | 70 |
| INSIGHT-CORE-03 | 70 |
| INSIGHT-CORE-04 | 70 |
| INSIGHT-CORE-05 | 70 |
| INSIGHT-CORE-06 | 70 |
| INSIGHT-CORE-07 | 70 |
| INSIGHT-CORE-08 | 70 |
| INSIGHT-CORE-09 | 70 |
| INSIGHT-STATS-01 | 71 |
| INSIGHT-STATS-02 | 71 |
| INSIGHT-STATS-03 | 71 |
| INSIGHT-STATS-04 | 71 |
| INSIGHT-STATS-05 | 71 |
| INSIGHT-STATS-06 | 71 |
| ~~INSIGHT-MOVES-01..03~~ | ~~72~~ — descoped (covered by MoveExplorer row tint) |
| ~~INSIGHT-META-01~~ | ~~73 (stretch)~~ — descoped |
| ~~INSIGHT-BADGE-01~~ | ~~74 (stretch)~~ — descoped |

## Open Questions (resolved during `/gsd-discuss-phase`)

Carried over from SEED-005:
1. Importance ranking formula (frequency × severity vs. recency-weighted vs. severity-only) — Phase 70.
2. Min-games thresholds (entry-position floor + candidate-move floor) — Phase 70.
3. Stats-tab top-10 algorithm reuse vs. refactor — Phase 70 entry.
4. Filter-object compatibility with the rest of the openings stats — Phase 70.
5. Color-symmetry dedupe edge case — Phase 70.
6. LLM wrap-up timing (deliberately deferred to v1.13.x / v1.14, not v1.13) — out of scope.
7. Per-entry vs. per-user min-games floor — Phase 70.
8. Strength inclusion in v1.13 vs. weaknesses-only first — Phase 70.

---

_For current project status, see .planning/ROADMAP.md_
