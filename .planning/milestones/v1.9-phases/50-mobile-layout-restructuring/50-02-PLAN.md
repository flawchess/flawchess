---
phase: 50-mobile-layout-restructuring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Endgames.tsx
autonomous: true
requirements:
  - EGAM-01

must_haves:
  truths:
    - "User sees the Endgames mobile sticky top row with the same translucent blurred surface as the new Openings mobile layout"
    - "The Endgames mobile filter button meets the 44px touch-target minimum (h-11 w-11)"
    - "The Endgames mobile sticky row is exactly 44px tall (h-11) so it reads as a visual sibling of the Openings mobile unified row"
    - "The Endgames mobile TabsList fills the full 44px row height"
    - "Endgames mobile has no new buttons, no removed buttons, and no structural changes beyond the visual alignment"
    - "The desktop layout (`hidden md:` branch) of Endgames is not modified"
  artifacts:
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Endgames mobile sticky row with backdrop-blur, 44px row height, and 44px filter button"
      contains: "data-testid=\"endgames-mobile-control-row\""
  key_links:
    - from: "frontend/src/pages/Endgames.tsx (mobile sticky row)"
      to: "Backdrop-blur translucent surface matching Openings mobile pattern"
      via: "bg-background/80 backdrop-blur-md border-b border-border classname"
      pattern: "bg-background/80 backdrop-blur-md border-b border-border"
    - from: "Endgames mobile sticky row"
      to: "44px touch target on filter button"
      via: "h-11 w-11 className on Button"
      pattern: "h-11 w-11 shrink-0 bg-toggle-active"
---

<objective>
Apply a visual-alignment pass to the `md:hidden` branch of the Endgames page so its sticky top row reads as a visual sibling of the new Openings mobile unified control row from Plan 50-01. Change the sticky row's background to `bg-background/80 backdrop-blur-md border-b border-border`, bump the row height to `h-11` (44px), and enlarge the filter button from `h-9 w-9` to `h-11 w-11`. Add a new `data-testid="endgames-mobile-control-row"` to the sticky row container. No structural changes. No new buttons.

Purpose: Deliver EGAM-01. The Endgames mobile layout is already structurally close to what Openings delivers post-Plan-50-01 (sticky row with TabsList + filter button). This plan makes the two pages visually consistent by adopting the same backdrop-blur surface and matching row height.

Output: Modified `frontend/src/pages/Endgames.tsx` (mobile branch only). Desktop layout untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-mobile-layout-restructuring/50-CONTEXT.md
@.planning/phases/50-mobile-layout-restructuring/50-UI-SPEC.md
@CLAUDE.md
@frontend/src/pages/Endgames.tsx
@frontend/src/components/layout/SidebarLayout.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase and UI-SPEC. -->

From frontend/src/pages/Endgames.tsx (current mobile sticky row, lines 346-373):
```tsx
{/* Mobile: single column */}
<div className="md:hidden flex flex-col min-w-0">
  <Tabs value={activeTab} onValueChange={(val) => { navigate(`/endgames/${val}`); window.scrollTo({ top: 0 }); }}>
    {/* Sticky sub-navigation + filter button */}
    <div className="sticky top-0 z-20 flex items-center gap-2 pb-2">
      <TabsList variant="brand" className="flex-1 h-9!" data-testid="endgames-tabs-mobile">
        <TabsTrigger value="stats" className="flex-1" data-testid="tab-stats-mobile">
          <BarChart2Icon className="mr-1.5 h-4 w-4" />
          Stats
        </TabsTrigger>
        <TabsTrigger value="games" className="flex-1" data-testid="tab-games-mobile">
          <Gamepad2Icon className="mr-1.5 h-4 w-4" />
          Games
        </TabsTrigger>
      </TabsList>
      <Tooltip content="Open filters" side="left">
        <Button
          variant="ghost"
          size="icon"
          className="h-9 w-9 shrink-0 bg-toggle-active text-toggle-active-foreground hover:bg-toggle-active/80"
          onClick={() => setMobileFiltersOpen(true)}
          data-testid="btn-open-filter-drawer"
          aria-label="Open filters"
        >
          <SlidersHorizontal className="h-4 w-4" />
        </Button>
      </Tooltip>
    </div>
    {/* ... Drawer, TabsContent follow ... */}
  </Tabs>
</div>
```

The handlers, state, TabsList children (2 tabs: stats + games), and drawer wiring are ALL unchanged. Only three classNames and one new `data-testid` attribute on the sticky row container.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply visual-alignment pass to Endgames mobile sticky row</name>
  <files>frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx (lines 340-400 — the mobile branch sticky row and immediate surroundings)
    - .planning/phases/50-mobile-layout-restructuring/50-UI-SPEC.md (sections: "Interaction Contract > Endgames Mobile — Visual Alignment Only", "Data Testid Contract", "Spacing Scale", "Color > Sticky wrapper border", "Success Metric Traceability > EGAM-01")
    - .planning/phases/50-mobile-layout-restructuring/50-CONTEXT.md (D-13, D-14, D-16, D-17, D-18)
    - frontend/src/components/layout/SidebarLayout.tsx (lines 100-130 — the desktop sidebar panel className is the visual reference for the backdrop-blur)
    - CLAUDE.md (Browser Automation Rules section)
  </read_first>
  <behavior>
    - The `md:hidden` sticky row container has `data-testid="endgames-mobile-control-row"` and uses classes `sticky top-0 z-20 flex items-center gap-2 h-11 bg-background/80 backdrop-blur-md border-b border-border px-1`
    - The TabsList uses `flex-1 h-full` (fills the 44px row height) instead of `flex-1 h-9!`
    - The filter button is `h-11 w-11 shrink-0 bg-toggle-active text-toggle-active-foreground hover:bg-toggle-active/80` (bumped from `h-9 w-9`)
    - The 2 TabsTriggers (`tab-stats-mobile` and `tab-games-mobile`), their icon children (`BarChart2Icon`, `Gamepad2Icon`), and their text labels ("Stats", "Games") are preserved verbatim
    - The Tooltip wrapper, Button component, onClick handler, `data-testid="btn-open-filter-drawer"`, `aria-label="Open filters"`, and `SlidersHorizontal` icon are all preserved verbatim
    - The Drawer subtree below the sticky row is not touched (D-16)
    - The `TabsContent` blocks (stats, games) are not touched
    - The desktop `hidden md:` branch of Endgames.tsx is not modified
  </behavior>
  <action>
    Edit the `md:hidden` branch of `frontend/src/pages/Endgames.tsx` (currently around line 347). Only the sticky row container and its two direct children (TabsList + filter Button) are modified. The Drawer subtree and TabsContent blocks below the sticky row are untouched.

    **Step A — Change the sticky row container.** Find the line:

    ```tsx
    <div className="sticky top-0 z-20 flex items-center gap-2 pb-2">
    ```

    Replace it with:

    ```tsx
    <div className="sticky top-0 z-20 flex items-center gap-2 h-11 bg-background/80 backdrop-blur-md border-b border-border px-1" data-testid="endgames-mobile-control-row">
    ```

    Changes: removed `pb-2` (no longer needed since the row is now fixed-height); added `h-11` (44px row height); added `bg-background/80 backdrop-blur-md` (translucent blurred surface matching Openings mobile pattern and desktop SidebarLayout); added `border-b border-border` (replaces the implicit visual separation the previous layout relied on); added `px-1` (small horizontal inner padding so icons don't hug the edge — matches the Openings unified row); added `data-testid="endgames-mobile-control-row"` (new container testid per UI-SPEC Data Testid Contract).

    **Step B — Change the TabsList height.** Find the line:

    ```tsx
    <TabsList variant="brand" className="flex-1 h-9!" data-testid="endgames-tabs-mobile">
    ```

    Replace it with:

    ```tsx
    <TabsList variant="brand" className="flex-1 h-full" data-testid="endgames-tabs-mobile">
    ```

    Change: `h-9!` → `h-full`. The TabsList now fills the parent row's 44px height.

    **Step C — Change the filter button size.** Find the line:

    ```tsx
    className="h-9 w-9 shrink-0 bg-toggle-active text-toggle-active-foreground hover:bg-toggle-active/80"
    ```

    (inside the Button under `<Tooltip content="Open filters" side="left">`, around line 365)

    Replace it with:

    ```tsx
    className="h-11 w-11 shrink-0 bg-toggle-active text-toggle-active-foreground hover:bg-toggle-active/80"
    ```

    Change: `h-9 w-9` → `h-11 w-11`. All other Button attributes (`variant="ghost"`, `size="icon"`, `onClick={() => setMobileFiltersOpen(true)}`, `data-testid="btn-open-filter-drawer"`, `aria-label="Open filters"`) and its `SlidersHorizontal` child icon are unchanged.

    **CRITICAL — do NOT touch:**
    - The desktop `hidden md:` branch (above line 346)
    - The Drawer subtree below the sticky row (Drawer, DrawerContent, DrawerHeader, DrawerTitle, DrawerClose, FilterPanel)
    - The TabsContent blocks (`<TabsContent value="stats">` and `<TabsContent value="games">`)
    - The outer `<Tabs>` wrapper on line 348 and its `onValueChange` handler
    - The outer `<div className="md:hidden flex flex-col min-w-0">` wrapper on line 347
    - The 2 TabsTriggers inside TabsList — their children (icon + text), className, value, and data-testid are all preserved verbatim
    - The SlidersHorizontal icon inside the filter button
    - Any imports — no new imports required
    - The onClick handler `() => setMobileFiltersOpen(true)` — the open logic is unchanged
    - The page's state, handlers, and data flow — none of it changes

    **What NOT to add (scope guardrails):**
    - Do NOT add a bookmark button (Endgames has no bookmarks — D-13)
    - Do NOT add a color toggle (Endgames has no board to flip — D-13)
    - Do NOT add a collapse handle (Endgames has no board — D-13)
    - Do NOT add a new tab (no structural change — D-13, D-14)
    - Do NOT touch the FilterPanel content (D-15)
    - Do NOT touch the Drawer direction or size (D-16)
  </action>
  <verify>
    <automated>cd frontend && grep -q 'data-testid="endgames-mobile-control-row"' src/pages/Endgames.tsx && grep -q 'sticky top-0 z-20 flex items-center gap-2 h-11 bg-background/80 backdrop-blur-md border-b border-border px-1' src/pages/Endgames.tsx && grep -q 'h-11 w-11 shrink-0 bg-toggle-active' src/pages/Endgames.tsx && grep -q 'flex-1 h-full" data-testid="endgames-tabs-mobile"' src/pages/Endgames.tsx && npm run lint && npx tsc --noEmit && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    New testid and classes present:
    - `grep -c 'data-testid="endgames-mobile-control-row"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -q 'sticky top-0 z-20 flex items-center gap-2 h-11 bg-background/80 backdrop-blur-md border-b border-border px-1' frontend/src/pages/Endgames.tsx`
    - `grep -q 'flex-1 h-full" data-testid="endgames-tabs-mobile"' frontend/src/pages/Endgames.tsx`
    - `grep -q 'h-11 w-11 shrink-0 bg-toggle-active text-toggle-active-foreground hover:bg-toggle-active/80' frontend/src/pages/Endgames.tsx`

    Pre-change classes removed (no stragglers):
    - `grep -c 'sticky top-0 z-20 flex items-center gap-2 pb-2' frontend/src/pages/Endgames.tsx` outputs exactly `0`
    - `grep -c 'flex-1 h-9!" data-testid="endgames-tabs-mobile"' frontend/src/pages/Endgames.tsx` outputs exactly `0`
    - `grep -c 'h-9 w-9 shrink-0 bg-toggle-active' frontend/src/pages/Endgames.tsx` outputs exactly `0` (the old 36px filter button is gone)

    Preserved data-testid values (exact counts — none renamed during the visual pass):
    - `grep -c 'data-testid="endgames-tabs-mobile"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -c 'data-testid="tab-stats-mobile"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -c 'data-testid="tab-games-mobile"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -c 'data-testid="btn-open-filter-drawer"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -c 'data-testid="drawer-filter-sidebar"' frontend/src/pages/Endgames.tsx` outputs exactly `1` (drawer testid unchanged)
    - `grep -c 'data-testid="btn-close-filter-drawer"' frontend/src/pages/Endgames.tsx` outputs exactly `1` (drawer close button testid unchanged)

    Preserved TabsList children (no new buttons, no removed buttons):
    - `grep -c '<TabsTrigger value="stats"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -c '<TabsTrigger value="games"' frontend/src/pages/Endgames.tsx` outputs exactly `1`
    - `grep -q 'BarChart2Icon className="mr-1.5 h-4 w-4"' frontend/src/pages/Endgames.tsx` (stats tab icon preserved)
    - `grep -q 'Gamepad2Icon className="mr-1.5 h-4 w-4"' frontend/src/pages/Endgames.tsx` (games tab icon preserved)
    - `grep -q 'SlidersHorizontal className="h-4 w-4"' frontend/src/pages/Endgames.tsx` (filter button icon preserved)

    Scope guardrails (no new structural elements):
    - `grep -c 'btn-toggle-played-as' frontend/src/pages/Endgames.tsx` outputs exactly `0` (no color toggle added — Endgames has no board)
    - `grep -c 'btn-open-bookmark-sidebar' frontend/src/pages/Endgames.tsx` outputs exactly `0` (no bookmark button added)
    - `grep -c 'btn-board-collapse-handle' frontend/src/pages/Endgames.tsx` outputs exactly `0` (no collapse handle — no board)
    - `grep -c 'overflow-x-auto' frontend/src/pages/Endgames.tsx` outputs exactly `0` (no horizontal scroll introduced)

    Drawer/TabsContent untouched:
    - `git diff -- frontend/src/pages/Endgames.tsx` shows changed lines ONLY inside the sticky row container and its 2 direct children (TabsList + filter Button). No lines changed inside `<Drawer>`, `<DrawerContent>`, `<DrawerHeader>`, `<DrawerTitle>`, `<FilterPanel>`, `<TabsContent value="stats">`, or `<TabsContent value="games">`.

    Desktop branch untouched:
    - `git diff -- frontend/src/pages/Endgames.tsx` shows no lines changed in any `hidden md:` block.

    Tooling gates:
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run knip` exits 0
  </acceptance_criteria>
  <done>
    The Endgames mobile sticky row uses the new backdrop-blur surface, matches the 44px row height of the Openings mobile unified row, has a 44px filter button, and carries the new `endgames-mobile-control-row` testid. All 2 TabsTriggers and the filter button are preserved with their original testids, icons, and handlers. No structural changes. Drawer and TabsContent subtrees are untouched. Desktop branch is untouched. Lint, typecheck, and knip all pass.
  </done>
</task>

</tasks>

<verification>
Phase-wide verification for Plan 50-02:

1. **Structural check (scope guardrail):** Read the modified `md:hidden` block in Endgames.tsx. The sticky row container must have exactly 2 direct children: `<TabsList>` and `<Tooltip>(Button)`. No additional buttons. No bookmark button. No color toggle. No collapse handle.

2. **Visual consistency check:** Compare the sticky row className in Endgames.tsx against the UI-SPEC "Endgames Mobile — Visual Alignment Only" section. The classNames must match exactly: `sticky top-0 z-20 flex items-center gap-2 h-11 bg-background/80 backdrop-blur-md border-b border-border px-1`.

3. **Tooling gates:**
   ```bash
   cd frontend && npm run lint && npx tsc --noEmit && npm run knip
   ```
   All three must exit 0.

4. **Diff scope check:**
   ```bash
   git diff --stat -- frontend/src/pages/Endgames.tsx
   ```
   Only this file should be in the diff for this plan. No other frontend file.

   ```bash
   git diff -- frontend/src/pages/Endgames.tsx
   ```
   Inspect and confirm all changes are within the sticky row container lines (around 350-373 in the pre-change file), not in the Drawer subtree or TabsContent blocks.

5. **Tests (if any cover these files):**
   ```bash
   cd frontend && npm test -- --run
   ```
   Should pass — all data-testid values are preserved.
</verification>

<success_criteria>
- Endgames mobile sticky row uses `h-11 bg-background/80 backdrop-blur-md border-b border-border` (Task 1 done).
- Endgames mobile filter button is `h-11 w-11` (Task 1 done).
- Endgames mobile TabsList uses `flex-1 h-full` (fills the 44px row height).
- New `endgames-mobile-control-row` testid is present exactly once.
- All 6 preserved data-testid values (`endgames-tabs-mobile`, `tab-stats-mobile`, `tab-games-mobile`, `btn-open-filter-drawer`, `drawer-filter-sidebar`, `btn-close-filter-drawer`) are still present exactly once each.
- No bookmark button, no color toggle, no collapse handle added to Endgames (scope guardrail).
- Desktop `hidden md:` branch is byte-identical to pre-change state.
- Drawer and TabsContent subtrees are byte-identical to pre-change state.
- `npm run lint && npx tsc --noEmit && npm run knip` all exit 0.
- EGAM-01 is satisfied: Endgames mobile layout is adjusted to be visually consistent with the new Openings mobile layout (same backdrop-blur surface, same 44px row height, same 44px filter button size).
</success_criteria>

<output>
After completion, create `.planning/phases/50-mobile-layout-restructuring/50-02-SUMMARY.md` documenting:
- Files modified (exact list — should be just frontend/src/pages/Endgames.tsx)
- Key decisions implemented (D-13, D-14 — visual alignment only)
- Preserved data-testid audit (grep counts)
- Any deviations from the UI-SPEC and why
- Verification output (lint, tsc, knip results)
</output>
