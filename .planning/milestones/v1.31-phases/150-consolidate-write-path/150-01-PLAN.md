---
phase: 150-consolidate-write-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen_write_path_golden.py
  - tests/services/test_flaw_upsert_equivalence.py
  - tests/fixtures/write_path_golden/
  - tests/test_eval_worker_endpoints.py
requirements: [WRITE-03]
autonomous: true

must_haves:
  truths:
    - "Running `uv run pytest tests/services/test_flaw_upsert_equivalence.py` is GREEN against current HEAD (goldens reproduce the current production write path exactly)."
    - "Re-running the committed generator against current HEAD regenerates byte-identical goldens (zero git diff in tests/fixtures/write_path_golden/)."
    - "All 8 scenarios have a committed golden fixture: 7 from D-02 plus the inverse-of-#3 flip-IN scenario."
    - "The equivalence test asserts blob columns expected NULL with an explicit `is None` check, not a truthy/equality comparison (so the JSONB null::jsonb pitfall is catchable)."
  artifacts:
    - scripts/gen_write_path_golden.py
    - tests/services/test_flaw_upsert_equivalence.py
    - tests/fixtures/write_path_golden/scenario_1_fresh_full_submit.json
    - tests/fixtures/write_path_golden/scenario_2_residual_hole_retry.json
    - tests/fixtures/write_path_golden/scenario_3_flip_out.json
    - tests/fixtures/write_path_golden/scenario_4_flip_in.json
    - tests/fixtures/write_path_golden/scenario_5_entry_pass_replaced.json
    - tests/fixtures/write_path_golden/scenario_6_dedup_transplant_no_sentinel.json
    - tests/fixtures/write_path_golden/scenario_7_blobs_pending_suppression.json
  key_links:
    - "The equivalence test drives the production submit/classify path (currently the atomic-submit orchestration) and dumps game_flaws — it does NOT call a copy of the logic, so when Plan 04 swaps in the diff/upsert the SAME test re-runs against the new code."
    - "A fixture-completeness guard test asserts exactly one golden file per named scenario constant (mirrors test_gen_global_percentile_cdf_unchanged.py)."
  prohibitions:
    - "The generator MUST NOT depend on or invoke bin/reset_db.sh, and MUST NOT read/mutate production data — use the per-run-DB isolation fixtures / async_session_maker only."
---

<objective>
Build the WRITE-03 golden-snapshot equivalence harness FIRST, against pristine HEAD (post-149, post-FLAWCHESS-8D-fix). This captures the CORRECT current `game_flaws` output of the delete-then-insert + snapshot/restore write path as committed golden fixtures, plus a reproducible committed generator and a drift-check equivalence test. Plan 04 (R3) later swaps `_classify_and_fill_oracle` to a diff/upsert; this same test must stay byte-identical green, proving no behavior change.

Purpose: The equivalence proof's entire value is that the golden reflects pre-refactor behavior. If goldens were generated after R1/R4/R3 touch the write path, a regression would be baked into the golden and the test could not catch it. Hence this is Wave 1, before any production write-path edit (D-01/D-02, RESEARCH "Sequencing implication").
Output: `scripts/gen_write_path_golden.py`, `tests/services/test_flaw_upsert_equivalence.py`, 8 committed golden JSON fixtures.
</objective>

<artifacts_produced>
- `scripts/gen_write_path_golden.py` — committed reproducible generator (regeneration one-liner in its module docstring).
- `tests/services/test_flaw_upsert_equivalence.py` — parametrized equivalence test + fixture-completeness guard.
- `tests/fixtures/write_path_golden/*.json` — 8 generated golden fixtures (one per scenario).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-consolidate-write-path/150-CONTEXT.md
@.planning/phases/150-consolidate-write-path/150-RESEARCH.md
@.planning/phases/150-consolidate-write-path/150-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committed generator + 8 golden fixtures from current HEAD</name>
  <files>scripts/gen_write_path_golden.py, tests/fixtures/write_path_golden/, tests/test_eval_worker_endpoints.py</files>
  <read_first>
    - `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` and `scripts/gen_global_percentile_cdf.py` — the exact committed-generator + drift-check + completeness-guard precedent to mirror (regeneration one-liner lives in the script/module docstring).
    - `tests/test_eval_worker_endpoints.py` — harvest the D-02 scenario setups; confirm the exact current names of the builders `_insert_game`, `_insert_game_positions`, `_atomic_request`, `_BLUNDER_SUBMIT_EVALS_142`, `_FLAT_SUBMIT_EVALS_142`, `_insert_opening_cache_with_pv` and the tests at ~3681 (fresh), ~3807 (residual-hole preserve), ~3895 (flip-OUT / FLAWCHESS-8D), ~4012 (dedup transplant no sentinel).
    - `tests/services/test_full_eval_drain.py` — `TestClassifyHook` (~1743, entry-pass replaced by oracle pass) and `TestLocalDrainBlobsPendingSuppression` (~1851, blobs_pending suppression) setups.
    - `tests/conftest.py` — per-run DB isolation fixtures (eval_worker_session_maker / full_drain_session_maker); this is the ONLY DB machinery to use (no reset_db.sh, per CLAUDE.md "no dev DB reset in plans").
    - `app/routers/eval_remote.py:1060` `_apply_atomic_submit` — the production submit path the generator drives end-to-end.
    - `app/repositories/game_flaws_repository.py` — the game_flaws columns to dump (all columns, ordered by ply).
  </read_first>
  <action>
    Create `scripts/gen_write_path_golden.py`, a reproducible generator that, for each of 8 scenarios, reconstructs the SAME game+position+submit sequence as the existing tests above, runs it against the CURRENT production write path (delete-then-insert `_classify_and_fill_oracle` plus the `_snapshot_preserved_flaw_blobs`/`_restore_preserved_flaw_blobs` compensation in `_apply_atomic_submit`), then dumps the resulting `game_flaws` rows (every column, ordered by ply) to one committed JSON fixture per scenario in `tests/fixtures/write_path_golden/`.

    Harvest scenario setups, do not invent new fixtures: scenario 1 = fresh full submit; 2 = residual-hole retry (preserve already-blobbed midgame flaws + tags, SEED-076); 3 = borderline ply flips OUT of flaw (FLAWCHESS-8D, clean delete, no StaleDataError); 4 = borderline ply flips IN (NEW — author it as the inverse ordering of scenario 3: submit the flat/no-flaw evals first, then the blunder evals); 5 = entry-pass NULL-tactic rows replaced by the full/oracle pass; 6 = opening dedup-transplant plies (pv stays NULL, no `[]`-sentinel taint, quick 260703-qgp); 7 = `blobs_pending=True` suppression (cp-flaw continuation blob deferred to tier-4 → suppressed to NULL, Phase 147 D-01/D-03).

    If the builders are not importable cross-file, factor the minimal shared builders into a small importable test-utils module first and check for import cycles between test files before doing so (RESEARCH R3 test-construction note). Prefer editing `tests/test_eval_worker_endpoints.py` to expose them over duplicating.

    Resolve RESEARCH Open Question 2 by making the generator use the per-run-DB-isolation fixtures / `async_session_maker` against the isolated test DB — NOT `bin/reset_db.sh`, NOT production data. Put the regeneration one-liner (e.g. `uv run python -m scripts.gen_write_path_golden`) in the module docstring, and state that it must reproduce byte-identical goldens on unchanged HEAD.

    Dump JSON so the NULL-vs-empty distinction is preserved: a SQL NULL blob column serializes as JSON `null` and an empty D-06 sentinel serializes as `[]` — these must be distinguishable in the fixture (per project landmine `project_asyncpg_jsonb_null_vs_sql_null`).
  </action>
  <verify>
    <automated>uv run python -m scripts.gen_write_path_golden && git status --porcelain tests/fixtures/write_path_golden/ | grep -v '^??' | grep -c . | grep -qx 0 && echo REGEN_STABLE</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/write_path_golden/` contains exactly 8 JSON fixtures, one per named scenario.
    - Re-running the generator on unchanged HEAD produces zero diff in the committed fixtures (byte-identical reproducibility).
    - The generator uses isolated test-DB machinery only; no reference to `reset_db.sh` and no production connection.
    - Scenario-4 (flip IN) setup exists and is the inverse ordering of scenario 3.
  </acceptance_criteria>
  <done>8 golden fixtures committed, generated from current HEAD via a committed reproducible generator that regenerates them byte-identically.</done>
</task>

<task type="auto">
  <name>Task 2: Equivalence test + completeness guard, green against HEAD</name>
  <files>tests/services/test_flaw_upsert_equivalence.py</files>
  <read_first>
    - `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` — the parametrized drift-check + `test_fixture_directory_covers_all_in_scope_cells` completeness-guard shape to mirror.
    - The generator authored in Task 1 (reuse its scenario builders + the game_flaws-dump helper so the test and generator cannot drift apart).
  </read_first>
  <action>
    Create `tests/services/test_flaw_upsert_equivalence.py`, parametrized over the 8 committed golden fixtures. For each scenario it re-runs the SAME setup through the PRODUCTION submit/classify path (the same entry point the generator drives — currently the atomic-submit orchestration; keep the call indirection so Plan 04's diff/upsert swap and Plan 05's move to `eval_apply.py` are picked up automatically), loads the golden JSON, and asserts the live `game_flaws` state matches byte-for-byte after JSON round-trip.

    Assert exact equality INCLUDING null-ness: for every blob column expected NULL, assert `row.<col> is None` explicitly (never a truthy/`==`-falsy check) so a future regression that writes `null::jsonb` instead of SQL NULL, or a `[]` sentinel where NULL is expected, is caught (RESEARCH Pitfall 2 warning-sign). Distinguish SQL NULL from the `[]` D-06 sentinel in the assertions.

    Add a completeness-guard test asserting the fixture directory has exactly one file per named scenario constant, so a future 9th scenario cannot silently ship without a golden. Cross-reference the generator's regeneration one-liner in the test module docstring: "if you intentionally change classify/gate/gate-margin logic, regenerate via `uv run python -m scripts.gen_write_path_golden` and review the diff before committing."

    This test MUST be green against current HEAD (new == current, both are the delete-then-insert path) — it only becomes a real equivalence proof once Plan 04 swaps in the diff/upsert.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_flaw_upsert_equivalence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 8 parametrized scenarios pass against current HEAD.
    - Blob-column NULL assertions use explicit `is None`, not falsy-equality.
    - The completeness-guard test fails if a scenario fixture is missing.
    - `uv run ruff check` and `uv run ty check app/ tests/` are clean for the new files.
  </acceptance_criteria>
  <done>Equivalence test + completeness guard green against HEAD, driving the production write path via a stable entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/generator → isolated test DB | Generator writes only to the per-run isolated test DB; must never touch production data. |

## STRIDE Threat Register

This is test/generator tooling only — no runtime attack surface, no new external input, no auth/endpoint/schema change. The honest threat model is minimal.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-150-01-DATA | Tampering | gen_write_path_golden.py DB target | low | mitigate | Generator uses per-run-DB isolation / test session makers only; no production DSN, no reset_db.sh — asserted by review + the "no prod connection" acceptance criterion. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_flaw_upsert_equivalence.py -x` green.
- `uv run python -m scripts.gen_write_path_golden` regenerates byte-identical goldens (zero git diff).
- `uv run ruff check .` and `uv run ty check app/ tests/` clean.
</verification>

<success_criteria>
8 committed golden fixtures capturing correct pre-refactor `game_flaws` output; a reproducible committed generator; an equivalence test + completeness guard green against HEAD, wired to the production write path so Plan 04 inherits it as the WRITE-03 proof.
</success_criteria>

<output>
Create `.planning/phases/150-consolidate-write-path/150-01-SUMMARY.md` when done.
</output>
