---
phase: 150-consolidate-write-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/engine.py
  - app/services/eval_queue_service.py
  - tests/services/test_engine.py
  - tests/services/test_eval_queue.py
requirements: [WRITE-05, WRITE-06]
autonomous: true

must_haves:
  truths:
    - "`EnginePool` exposes exactly one generic acquire/analyse/restart method (`_acquire_and_analyse`); the 3 old private copies no longer each carry their own acquire/timeout/restart/release skeleton."
    - "`evaluate()`, `evaluate_nodes()`, `evaluate_nodes_with_pv()`, `evaluate_nodes_multipv2()` preserve their exact current return shapes and failure sentinels ((cp,mate) tuple vs bare InfoDict vs list[InfoDict] vs None)."
    - "The tier-3 and tier-4 Efraimidis-Spirakis lotteries call shared parameterized building blocks (`_es_weighted_user_pick`, `_es_weighted_game_pick`); the -ln(random())/weight key and TC-multiplier CASE block exist in one place."
    - "`uv run pytest tests/services/test_engine.py tests/services/test_eval_queue.py -x` is green; the tier-3 residual-fallback stage and tier-4 no-fallback behavior are both preserved."
  artifacts:
    - "app/services/engine.py::EnginePool._acquire_and_analyse"
    - "app/services/eval_queue_service.py::_es_weighted_user_pick"
    - "app/services/eval_queue_service.py::_es_weighted_game_pick"
  key_links:
    - "The shared ES building blocks bind all params via sa.text dict params — never f-string interpolation (preserve the existing discipline; QUEUE-08)."
    - "Tier-3 anchors decay on played_at with a residual PV-backfill fallback stage; tier-4 anchors on full_evals_completed_at with no fallback and no tc_multiplier semantics change."
  prohibitions:
    - "No behavior change: TC weights (classical=8/rapid=4/blitz=2/bullet=1/other=0.5), half-life/floor constants, and WHERE/EXISTS predicates stay identical per tier."
---

<objective>
Two low-risk mechanical ride-along consolidations on files disjoint from the write-path chain (D-06), so they run in Wave 1 in parallel:
- R5 / WRITE-05: collapse `EnginePool`'s 3 near-identical acquire/analyse/restart private methods into one generic `_acquire_and_analyse` parameterized on `multipv`.
- R6 / WRITE-06: parameterize the tier-3/tier-4 ES lottery into shared weighted-user-pick + weighted-game-pick building blocks.

Purpose: Shrink duplicated engine + lottery surface with no behavior change. RESEARCH confirmed both are genuinely near-identical with no subtle correctness differences.
Output: one generic EnginePool method; two shared ES building blocks; existing engine + queue suites green.
</objective>

<artifacts_produced>
- `EnginePool._acquire_and_analyse(board, limit, timeout, *, multipv: int | None = None)` in `app/services/engine.py`.
- `_es_weighted_user_pick(...)` and `_es_weighted_game_pick(...)` in `app/services/eval_queue_service.py`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-consolidate-write-path/150-RESEARCH.md
@.planning/phases/150-consolidate-write-path/150-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EnginePool generic acquire/analyse/restart method (R5)</name>
  <files>app/services/engine.py, tests/services/test_engine.py</files>
  <read_first>
    - `app/services/engine.py` — `EnginePool._analyse` (L464, scalar analyse → `_score_to_cp_mate`), `_analyse_with_pv` (L515, scalar analyse → raw `InfoDict | None`), `_analyse_multipv2` (L573, `analyse(..., multipv=2)` → `list[InfoDict] | None`); the public callers `evaluate()` (L244), `evaluate_nodes()` (L260), `evaluate_nodes_with_pv()` (L273), `evaluate_nodes_multipv2()` (L294); `_score_to_cp_mate` (L337); `_restart_worker`.
    - `tests/services/test_engine.py` — existing per-method contract tests (e.g. single-legal-move second-sentinel), to confirm return-shape expectations before refactoring.
  </read_first>
  <action>
    Introduce one `_acquire_and_analyse(board, limit, timeout, *, multipv: int | None = None) -> chess.engine.InfoDict | list[chess.engine.InfoDict] | None` holding the single shared 14-line skeleton: not-started guard, acquire idx from `self._available`, None-protocol guard, `asyncio.wait_for(protocol.analyse(board, limit, multipv=multipv) if multipv else protocol.analyse(board, limit), timeout=timeout)`, on `(asyncio.TimeoutError, chess.engine.EngineError, chess.engine.EngineTerminatedError)` call `_restart_worker(idx)` and return the failure sentinel, `finally` re-queue idx.

    Keep the three distinct public contracts by moving the per-method post-processing OUT to the callers: scalar callers apply `_score_to_cp_mate()` (returning `(cp, mate)` and `(None, None)` on failure); the raw-PV caller returns the `InfoDict | None`; `evaluate_nodes_multipv2()` post-processes the `list[InfoDict]`. The generic method returns bare `None` on failure; the scalar wrapper maps that to `(None, None)`. Remove the now-redundant `_analyse` / `_analyse_with_pv` / `_analyse_multipv2` bodies (or reduce each to a one-line delegation only if a call site genuinely still needs a named alias — prefer full removal).

    No behavior change — RESEARCH confirmed the only differences are (a) whether `multipv=2` is passed, (b) failure-sentinel shape, (c) where `_score_to_cp_mate` runs; all three are now handled by the parameter + caller post-processing.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one method on `EnginePool` contains the acquire/timeout/restart/release skeleton.
    - `evaluate`, `evaluate_nodes`, `evaluate_nodes_with_pv`, `evaluate_nodes_multipv2` return the same shapes/sentinels as before.
    - `uv run pytest tests/services/test_engine.py -x` green; `uv run ty check app/ tests/` clean for engine.py.
  </acceptance_criteria>
  <done>One generic acquire/analyse method backs all engine analyse call sites with unchanged contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize tier-3/tier-4 ES lottery (R6)</name>
  <files>app/services/eval_queue_service.py, tests/services/test_eval_queue.py</files>
  <read_first>
    - `app/services/eval_queue_service.py` — `_claim_tier3_derived` (L277, 3 stages: weighted user pick over needs-engine games, weighted game pick, residual PV-backfill fallback game pick) and `_claim_tier4_blob` (L489, 2 stages: user pick over analyzed games with a NULL-blob flaw, game pick anchored on `full_evals_completed_at`, no fallback); the shared `GAME_TC_WEIGHTS` CASE block and `-ln(random())/weight` key; the tunable half-life/floor constants for each tier.
    - `tests/services/test_eval_queue.py` — existing tier-3 and tier-4 lottery tests (anti-starvation/recency-preference) to preserve.
  </read_first>
  <action>
    Extract two generic building blocks: `_es_weighted_user_pick(session, *, candidate_exists_sql, recency_col_sql, tau_seconds, floor) -> int | None` and `_es_weighted_game_pick(session, *, game_where_sql, recency_col_sql, tau_seconds, game_floor, tc_weights) -> int | None`. Both carry the shared TC-multiplier CASE and ES key, parameterized purely via `sa.text` bound-param dicts — never f-string interpolation (preserve the existing inline-comment discipline, QUEUE-08).

    Rewire `_claim_tier3_derived` to: user-pick → game-pick → game-pick again with the residual-fallback WHERE predicate (keep the fallback stage). Rewire `_claim_tier4_blob` to: user-pick → game-pick (no fallback). Preserve each tier's exact predicates, recency-anchor column (tier-3 played_at, tier-4 full_evals_completed_at), and named half-life/floor constants — the only thing shared is the query SHAPE, not the parameter values.

    No behavior change. Per D-06 this ride-along may only be deferred if R3 balloons; it runs in its own Wave 1 plan independent of R3, so do NOT defer — complete it here.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_queue.py -x -k "lottery or tier3 or tier4 or claim"</automated>
  </verify>
  <acceptance_criteria>
    - The ES key and TC-weight CASE block exist in exactly one place (the shared building blocks).
    - Tier-3 keeps its residual-fallback stage; tier-4 keeps its no-fallback 2-stage shape and `full_evals_completed_at` anchor.
    - All bound params flow through `sa.text` dict params; no f-string SQL interpolation.
    - `uv run pytest tests/services/test_eval_queue.py -x` green; `uv run ty check app/ tests/` clean.
  </acceptance_criteria>
  <done>Tier-3 and tier-4 lotteries share two parameterized ES building blocks with identical behavior to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval_queue_service → DB | Weighted-select SQL; parameterization must stay bound-param (no injection surface introduced). |

## STRIDE Threat Register

Structure-only refactor; no new external input, no auth/endpoint/schema change.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-150-02-SQLI | Tampering | shared ES building blocks | low | mitigate | All values bound via sa.text dict params, never f-string interpolation — the existing discipline is carried into the shared helpers and asserted by acceptance criterion. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_engine.py tests/services/test_eval_queue.py -x` green.
- `uv run ruff check app/services/engine.py app/services/eval_queue_service.py` and `uv run ty check app/ tests/` clean.
</verification>

<success_criteria>
One generic `EnginePool` analyse method and two shared ES lottery building blocks, both no-behavior-change, with existing engine + queue suites green.
</success_criteria>

<output>
Create `.planning/phases/150-consolidate-write-path/150-02-SUMMARY.md` when done.
</output>
