---
phase: 150-consolidate-write-path
plan: 05
type: execute
wave: 4
depends_on: ["150-04"]
files_modified:
  - app/services/eval_apply.py
  - app/services/eval_drain.py
  - app/routers/eval_remote.py
  - tests/services/test_flaw_upsert_equivalence.py
  - tests/test_eval_worker_endpoints.py
requirements: [WRITE-04]
autonomous: true

must_haves:
  truths:
    - "`app/services/eval_apply.py` exists and exposes `apply_full_eval(...)` (the post-R1/R3/R4 `_apply_atomic_submit` body), consumed by BOTH `_full_drain_tick` and the router `atomic_submit_eval`."
    - "The shared-write-path primitives are physically MOVED into eval_apply.py, not re-exported — there is no `eval_drain.py -> eval_apply.py -> eval_drain.py` circular import."
    - "`app/routers/eval_remote.py` no longer imports private (underscore-prefixed) helpers from eval_drain.py; it imports only from eval_apply.py and is a thin HTTP layer."
    - "`apply_full_eval` takes an explicit `update_opening_cache` parameter defaulting each caller to its current behavior (drain-tick True, atomic-submit False) — the opening-cache write is NOT silently extended to the atomic path (no behavior change)."
    - "The full backend suite and the golden equivalence test are green after the move; import order is clean (ty/ruff pass, app starts)."
  artifacts:
    - "app/services/eval_apply.py (new module)"
    - "app/services/eval_apply.py::apply_full_eval"
  key_links:
    - "eval_drain.py and eval_remote.py both become leaf importers of eval_apply.py; neither direction reverses (Pitfall 3)."
    - "apply_full_eval preserves the read-session -> CPU (no session) -> single write_session sequencing and the fail-closed contract (WR-01, T-117-11)."
  prohibitions:
    - "No thin re-export shim by default — a clean physical move IS achievable per RESEARCH; only fall back to a shim if genuinely blocked, and FLAG it in the SUMMARY rather than leaving the private-import leak silently."
    - "Do NOT unconditionally call the opening-cache upsert from apply_full_eval (Pitfall 4 — that is a forbidden behavior change)."
---

<!-- planner-discipline-allow: eval_drain -->

<objective>
R7 / WRITE-04: extract the shared submit/tick orchestration into a new `app/services/eval_apply.py` exposing `apply_full_eval(...)`, physically moving the shared-write-path primitives out of `eval_drain.py` so both `_full_drain_tick` and the router call one entry point, and remove the router's private-helper import leak so `eval_remote.py` returns to a thin HTTP layer.

Purpose: The router currently reaches into 21 private `eval_drain.py` symbols — a violation of the CLAUDE.md router convention and the last structural duplication. Moving (not re-exporting) the primitives into a shared leaf module resolves both the leak and the circular-import risk. R1/R3/R4 already shrank this body, so the move is now mechanical.
Output: `eval_apply.py` with `apply_full_eval`; slimmed `_full_drain_tick`; thin `eval_remote.py`.
</objective>

<artifacts_produced>
- `app/services/eval_apply.py` — new shared-write-path module exposing `apply_full_eval(..., *, update_opening_cache: bool)`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-consolidate-write-path/150-CONTEXT.md
@.planning/phases/150-consolidate-write-path/150-RESEARCH.md
@.planning/phases/150-consolidate-write-path/150-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eval_apply.py, move shared primitives, expose apply_full_eval, kill the router import leak</name>
  <files>app/services/eval_apply.py, app/services/eval_drain.py, app/routers/eval_remote.py</files>
  <read_first>
    - RESEARCH.md "R7 — Module Split" section — the full symbol inventory to MOVE (shared write path): `_FullPlyEvalTarget`, `_collect_full_ply_targets`, `_fetch_dedup_evals`, `_resolve_full_eval`, `_post_move_eval`, `_batch_update_best_move_rows`, `_batch_update_pv_rows`, `_batch_update_eval_rows`, `_apply_full_eval_results`, `_mark_full_evals_completed`, `_mark_full_pv_completed`, `_classify_and_fill_oracle` (now the diff/upsert), the R4 preamble helper, `_flaw_engine_plies`, `_build_flaw_multipv2_blobs`, `_derive_atomic_sentinel_lines`, `_batch_update_flaw_pv_lines`, `_run_multipv2_pass`, `_GameColorView`, `_signal_flaw_completion`, `apply_completion_decision`, `_parse_token`, `_assemble_flaw_blobs_from_submit`, `_assemble_one_line_blob`.
    - `app/routers/eval_remote.py` — the private-helper import block (L81-107, 21 symbols + 3 constants) and `_apply_atomic_submit` (L1060) whose body becomes `apply_full_eval`; `atomic_submit_eval` (L1363), the thin-router target shape; the `upsert_worker_heartbeat` sibling call.
    - `app/services/eval_drain.py` — `_full_drain_tick` (L2513, the other consumer) and `_upsert_opening_cache` (called by the tick but NOT by atomic-submit today — Pitfall 4).
    - RESEARCH Pitfall 3 (circular import) and Pitfall 4 (`_upsert_opening_cache` drain-tick-only).
  </read_first>
  <action>
    Create `app/services/eval_apply.py` and physically MOVE the shared-write-path primitives listed in `read_first` out of `eval_drain.py` into it (move, not re-export — a re-export creates a cycle, Pitfall 3). Define `apply_full_eval(...)` in eval_apply.py as the post-R1/R3/R4 `_apply_atomic_submit` body, preserving the read-session → CPU (no session held) → single write_session sequencing, the fail-closed contract (WR-01), and `apply_completion_decision(...)` + `upsert_worker_heartbeat(...)` as sibling calls before commit.

    Give `apply_full_eval` an explicit `update_opening_cache: bool` parameter (Pitfall 4): `_full_drain_tick` passes True (its current behavior), the router/atomic-submit passes False (its current behavior). Do NOT unconditionally call the opening-cache upsert — that is a forbidden behavior change. Record this parameter choice as a decision in the SUMMARY.

    Rewire consumers: `eval_drain.py`'s `_full_drain_tick` imports FROM eval_apply.py and calls `apply_full_eval(..., update_opening_cache=True)`; `eval_remote.py`'s `atomic_submit_eval` imports FROM eval_apply.py and calls `apply_full_eval(..., update_opening_cache=False)`. Delete the private-helper import block from eval_remote.py so the router imports only public entry points from eval_apply.py and stops reaching into eval_drain.py internals (thin HTTP layer, CLAUDE.md router convention). Both eval_drain.py and eval_remote.py become leaf importers of eval_apply.py; neither reverses.

    Sequence the edits so there is no broken intermediate: move the functions and update BOTH call sites before running the suite (Pitfall 3 warning — ty/ruff surface a cycle or missing import loudly). Update `tests/services/test_flaw_upsert_equivalence.py`'s entry-point import if it referenced a now-moved symbol (it should still drive the same production submit path, now via eval_apply).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_flaw_upsert_equivalence.py tests/test_eval_worker_endpoints.py tests/services/test_full_eval_drain.py -x && python -c "import app.main" && test $(grep -cE 'from[[:space:]]+app\.services\.eval_drain[[:space:]]+import[[:space:]]+_' app/routers/eval_remote.py) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/eval_apply.py` exists and defines `apply_full_eval` with an `update_opening_cache` parameter.
    - `eval_remote.py` imports no private (underscore-prefixed) symbols from eval_drain.py; the L81-107 leak block is gone.
    - `import app.main` succeeds (no circular import); ty/ruff clean.
    - The opening-cache upsert is NOT invoked on the atomic-submit path (update_opening_cache=False there).
    - Golden equivalence + atomic-submit + drain suites green.
  </acceptance_criteria>
  <done>Shared write path lives in eval_apply.py, both lanes call apply_full_eval, and the router no longer imports private drain helpers — no circular import, no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Split eval_drain.py entry-lane vs full-lane (or flag descope)</name>
  <files>app/services/eval_drain.py, app/services/eval_entry.py, app/services/import_service.py, app/routers/eval_remote.py</files>
  <read_first>
    - RESEARCH.md "R7 — entry-lane vs full-lane vs shared-write-path split" — the entry-lane symbol inventory (`_EvalTarget`, `_TargetSpec`, `_collect_target_specs`, `_snapshot_boards`, `_collect_eval_targets_per_game`, `_collect_midgame_eval_targets`, `_collect_endgame_span_eval_targets`, `_split_into_contiguous_islands`, `_batch_update_entry_eval_rows`, `_apply_eval_results`, `_claim_entry_eval_games`, `_pick_pending_game_ids`, `_load_pgns_for_games`, `_mark_evals_completed`, `_collect_eval_targets_from_db`, `_classify_and_insert_flaws`) and the full-lane orchestration that stays (`run_eval_drain`, `_full_drain_tick`, `run_full_eval_drain`, `resweep_holed_games`, `_any_active_import_or_entry_ply_pending`, `_upsert_opening_cache`).
    - Consumers of the entry-lane symbols: `entry_submit_eval` (eval_remote.py) and `import_service.py`.
  </read_first>
  <action>
    Complete the D-05 3-way split: relocate the entry-lane symbols into their own module (e.g. `app/services/eval_entry.py`) so `eval_drain.py` retains only full-lane orchestration, with the shared write path already in eval_apply.py (Task 1). Update all importers (`entry_submit_eval`, `import_service.py`) to the new module path. Keep the split mechanical and import-clean (no cycles): entry-lane and full-lane both depend on eval_apply.py; entry-lane does not import full-lane and vice versa where avoidable.

    If, during execution, this split materially balloons context or surfaces an unexpected coupling that would risk the no-behavior-change contract, it is an ACCEPTABLE partial-R7 descope per D-05 — but you MUST flag it explicitly in the SUMMARY (state exactly what was and was not split and why), never land it silently as "R7 done." The Task 1 deliverable (apply_full_eval + killing the router import leak) is the non-negotiable core of WRITE-04; this file split is the completion of it.
  </action>
  <verify>
    <automated>uv run pytest -n auto -x</automated>
  </verify>
  <acceptance_criteria>
    - Entry-lane symbols live in their own module; eval_drain.py holds only full-lane orchestration (or a flagged, documented partial descope in the SUMMARY).
    - All importers updated; `import app.main` succeeds; no circular imports.
    - Full backend suite green; `uv run ty check app/ tests/` clean.
  </acceptance_criteria>
  <done>eval_drain.py is split into entry-lane / full-lane / shared-write-path (eval_apply.py), or the residual is explicitly flagged as a documented partial descope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → service | Router must delegate to eval_apply.py, not reach into private service internals. |
| token parse → write | `_parse_token` validate-before-write ordering must be preserved across the move. |

## STRIDE Threat Register

Structure-only relocation; no new external input, no auth/endpoint/schema change.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-150-TOKEN | Tampering/Spoofing | relocated _parse_token | high | mitigate | The move preserves the validate-before-write call ordering (in-range ply check before any write); regression-covered by existing tamper-guard tests (T-147-02, T-145-09). |
| T-150-STARTUP | Denial of Service | circular import at startup | low | mitigate | Move (not re-export) resolves the cycle; `import app.main` succeeds as an acceptance check; ty/ruff surface any residual cycle loudly. |
</threat_model>

<verification>
- `uv run pytest -n auto -x` green (full suite — this phase touches high-fan-out shared code).
- `import app.main` succeeds; `grep` confirms no private eval_drain imports remain in eval_remote.py.
- `uv run ruff check .`, `uv run ty check app/ tests/` clean.
- Frontend gate runs as a no-op (backend-only phase) per the pre-merge gate before the single local squash-merge to main (D-07).
</verification>

<success_criteria>
Shared submit/tick orchestration lives in `app/services/eval_apply.py` via `apply_full_eval`, eval_drain.py is split (entry lane / full lane / shared write path) or the residual is flagged, the router imports no private drain helpers, and there is no circular import or behavior change.
</success_criteria>

<output>
Create `.planning/phases/150-consolidate-write-path/150-05-SUMMARY.md` when done.
</output>
