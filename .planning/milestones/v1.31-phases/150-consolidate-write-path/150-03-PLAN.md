---
phase: 150-consolidate-write-path
plan: 03
type: execute
wave: 2
depends_on: ["150-01"]
files_modified:
  - app/services/eval_drain.py
  - app/routers/eval_remote.py
  - tests/services/test_full_eval_drain.py
  - tests/test_eval_worker_endpoints.py
requirements: [WRITE-01, WRITE-02]
autonomous: true

must_haves:
  truths:
    - "The Path A/B/C completion decision + guarded eval_jobs stamp lives in exactly one `apply_completion_decision(...)` function; both former inline copies (`_full_drain_tick` and `_apply_atomic_submit`) now call it."
    - "The guarded stamp keeps the `WHERE ... status = 'leased'` predicate so a late/expired-lease submit remains a no-op (T-150-LEASE)."
    - "Each caller keeps its own Path-C reporting: the in-process drain uses logger.warning (FLAWCHESS-5V quota fix), the router uses sentry capture_message — threaded via an injectable callback/source param, not unified to one mechanism."
    - "The classify preamble (load positions + optional in-memory post-move overlay + classify) runs through one shared helper with an `overlay` parameter; the 3 overlay sites pass overlay=True and `_flaw_engine_plies` passes overlay=False."
    - "Lichess-eval-game flaw-PV coverage is preserved (the overlay=False path does NOT zero stored %eval); a targeted test asserts this."
    - "`uv run pytest tests/services/test_full_eval_drain.py tests/test_eval_worker_endpoints.py -x` is green; the golden equivalence test from Plan 01 stays green."
  artifacts:
    - "apply_completion_decision(...) (single definition)"
    - "the unified classify-preamble helper (single definition, overlay-parameterized)"
  key_links:
    - "engine_result_map / flaw_pv_blobs (pv_by_ply overlay) must thread through the unified preamble into classify_game_flaws unchanged — load-bearing for live tactic tagging (260618-aiq) and sentinel derivation."
    - "upsert_worker_heartbeat stays a sibling call in the router orchestration (PRUNE-06 telemetry), NOT folded into apply_completion_decision."
  prohibitions:
    - "R1 consolidates 2 live copies, not 3 (Phase 149 already deleted the Gen-1 `_apply_submit` copy); `entry_submit_eval` has no Path A/B/C block and is out of scope."
    - "Do NOT collapse `_flaw_engine_plies` into 'pass an empty pos_eval' — that returns (None,None) for every ply and reproduces the Phase 117 0%-lichess-flaw-PV regression."
---

<objective>
The R1 → R4 "shrink prep" (D-07): extract the two write-path duplications that make R3 and R7 smaller and safer. R1 unifies the Path A/B/C completion decision; R4 unifies the classify preamble. Both are no-behavior-change; the Plan 01 golden equivalence test is the safety net that R4 did not alter classify output.

Purpose: Each step shrinks the next. R1 removes ~55 lines of duplicated completion logic; R4 removes 3 copies of the load+overlay+classify block. RESEARCH corrected two CONTEXT drifts baked into the tasks below: R1 is 2 copies not 3, and R4's 4th site (`_flaw_engine_plies`) is structurally different by necessity.
Output: `apply_completion_decision(...)` and one overlay-parameterized classify-preamble helper, both called from all sites.
</objective>

<artifacts_produced>
- `apply_completion_decision(write_session, *, game_id, job_id, failed_ply_count, current_attempts, source, on_path_c_capacity_reached) -> bool` in `app/services/eval_drain.py`.
- A unified classify-preamble helper (e.g. `_classify_with_overlay(game_id, positions_loader, *, overlay: bool, pos_eval=None) -> FlawResult | None`) in `app/services/eval_drain.py`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-consolidate-write-path/150-CONTEXT.md
@.planning/phases/150-consolidate-write-path/150-RESEARCH.md
@.planning/phases/150-consolidate-write-path/150-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract apply_completion_decision (R1, 2 copies)</name>
  <files>app/services/eval_drain.py, app/routers/eval_remote.py</files>
  <read_first>
    - `app/services/eval_drain.py` — `_full_drain_tick` (L2513) Path A/B/C block (L2767-2821), the local `from app.models.eval_jobs import EvalJob` import inside the function body (~L2808), `MAX_EVAL_ATTEMPTS` (L111), `_mark_full_evals_completed` / `_mark_full_pv_completed`.
    - `app/routers/eval_remote.py` — `_apply_atomic_submit` (L1060) Path A/B/C block (L1281-1334), its `sentry_sdk.set_context/set_tag/capture_message` Path-C reporting, the `upsert_worker_heartbeat(...)` sibling call before commit, the module-level `EvalJob` import (L108).
    - `app/routers/eval_remote.py:531` `entry_submit_eval` — confirm it has NO Path A/B/C block (flat stamp, L640-645) and is correctly out of scope.
  </read_first>
  <action>
    Add `apply_completion_decision(write_session, *, game_id, job_id, failed_ply_count, current_attempts, source: Literal["full_eval_drain", "remote_eval_worker"], on_path_c_capacity_reached) -> bool` encoding the shared shape: Path A (failed_ply_count == 0 → mark both markers complete, stamp_complete=True); Path B (new_attempts < MAX_EVAL_ATTEMPTS → UPDATE games full_eval_attempts, stamp_complete=False); Path C (cap reached → mark both markers complete, invoke the injected `on_path_c_capacity_reached` callback, stamp_complete=True). Then, when stamp_complete and job_id is not None, `UPDATE eval_jobs SET status='completed', completed_at=now() WHERE id=:job_id AND status='leased'` — keep the leased-guard predicate verbatim. Return stamp_complete.

    Do NOT unify the Path-C reporting mechanism: pass `on_path_c_capacity_reached` as an injectable callback so `_full_drain_tick` keeps `logger.warning` (deliberate FLAWCHESS-5V quota fix) and `_apply_atomic_submit` keeps its sentry `capture_message(level="warning")`. Thread the `source` tag as a parameter. Resolve RESEARCH Open Question 1 this way (injectable callback per caller) rather than forcing one mechanism.

    Replace both inline blocks with a call to the new function. Keep `upsert_worker_heartbeat(...)` as a sibling call in `_apply_atomic_submit`'s orchestration (it is PRUNE-06 telemetry, not part of the decision). For the `EvalJob` import: move to module scope if no circular import reappears (RESEARCH A1 — ty/ruff will catch a cycle loudly); if a cycle does reappear, keep a local import inside the function and note why in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_full_eval_drain.py::TestHoleAwareCompletionGate tests/test_eval_worker_endpoints.py -x && test $(grep -c 'def apply_completion_decision' app/services/eval_drain.py) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `def apply_completion_decision` in the codebase; both `_full_drain_tick` and `_apply_atomic_submit` call it.
    - The eval_jobs stamp keeps `AND status = 'leased'`.
    - Drain Path-C still uses logger.warning; router Path-C still uses sentry capture_message.
    - `upsert_worker_heartbeat` remains a sibling call, not folded in.
    - Existing completion-gate + atomic-submit suites green.
  </acceptance_criteria>
  <done>Path A/B/C completion decision lives in one guarded function called by both live submit lanes, per-caller Path-C reporting preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Unify classify preamble with overlay parameter (R4)</name>
  <files>app/services/eval_drain.py, tests/services/test_full_eval_drain.py</files>
  <read_first>
    - `app/services/eval_drain.py` — `_missing_flaw_pv_targets` (L979), `_build_flaw_multipv2_blobs` (L1216), `_derive_atomic_sentinel_lines` (L1340): the identical load + `_post_move_eval` overlay + `classify_game_flaws` block; `_reconstruct_pos_eval` (L954), `_post_move_eval`.
    - `_flaw_engine_plies` (L901) and its docstring ("0% flaw-PV coverage for analyzed lichess games") — the structurally-different pre-gather, no-overlay, lichess-eval-only site.
    - `_classify_and_fill_oracle` (L705) `pv_by_ply` overlay from `engine_result_map` (L775-777) — confirm the preamble threads `pv_by_ply`/`flaw_pv_blobs` into classify unchanged.
    - `app/services/flaws_service.py` — `classify_game_flaws` signature (blobs_pending / pv threading).
    - `tests/services/test_full_eval_drain.py` — `TestFlawPv` and any lichess-flaw-PV-coverage assertion; confirm whether the overlay=False path is already covered before deciding if a new test is needed.
  </read_first>
  <action>
    Introduce one preamble helper, `_classify_with_overlay(game_id, positions_loader, *, overlay: bool, pos_eval=None) -> FlawResult | None`, that loads the Game + ordered GamePosition rows, and — only when `overlay=True` — runs the `_post_move_eval` mutation loop overwriting `pos.eval_cp/eval_mate`, then calls `classify_game_flaws(...)` (threading `pv_by_ply`/`flaw_pv_blobs`/`engine_result_map` unchanged), returning None on a `"reason"` result.

    Rewire the 3 overlay sites (`_missing_flaw_pv_targets`, `_build_flaw_multipv2_blobs`, `_derive_atomic_sentinel_lines`) to call it with `overlay=True` and their computed `pos_eval`. Rewire `_flaw_engine_plies` to call it with `overlay=False` (skip the mutation loop entirely, classify on DB-stored lichess %eval as-is). Do NOT collapse `_flaw_engine_plies` into "pass an empty pos_eval" — `_post_move_eval({}, ply)` returns (None,None) and would zero every lichess eval, reproducing the Phase 117 regression. The four sites keep their distinct downstream handling of `flaw_result`; only the load+optional-overlay+classify block is shared.

    Add a targeted regression test asserting lichess-eval-game flaw-PV coverage survives the merge (the overlay=False path) — unless `read_first` confirms `TestFlawPv` already exercises it explicitly, in which case cite that test in the SUMMARY instead of adding a redundant one.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_full_eval_drain.py -x -k "flaw or preamble or lichess or pv" && uv run pytest tests/services/test_flaw_upsert_equivalence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - One overlay-parameterized preamble helper; all 4 sites call it (3 overlay=True, 1 overlay=False).
    - A test asserts lichess flaw-PV coverage is preserved on the overlay=False path.
    - The Plan 01 golden equivalence test stays green (R4 did not change classify output).
    - `uv run ty check app/ tests/` clean.
  </acceptance_criteria>
  <done>Classify preamble load+overlay+classify runs through one helper; the lichess non-overlay case is preserved and guarded by a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker submit → eval_jobs stamp | A late/expired-lease submit must not complete an unrelated job. |

## STRIDE Threat Register

Structure-only refactor; no new external input, no auth/endpoint/schema change. The real invariants to preserve are the leased-guard and the worker-trust boundary.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-150-LEASE | Tampering | apply_completion_decision eval_jobs UPDATE | high | mitigate | Keep the `WHERE id=:job_id AND status='leased'` guard verbatim so a late/expired-lease submit is a no-op; regression-covered by existing atomic-submit tests. |
| T-150-TRUST | Tampering | unified classify preamble | high | mitigate | The preamble still calls the server's own `classify_game_flaws`; worker-submitted flaw claims are never trusted (T-147-03 boundary preserved). |
</threat_model>

<verification>
- `uv run pytest tests/services/test_full_eval_drain.py tests/test_eval_worker_endpoints.py tests/services/test_flaw_upsert_equivalence.py -x` green.
- `uv run ruff check app/services/eval_drain.py app/routers/eval_remote.py` and `uv run ty check app/ tests/` clean.
</verification>

<success_criteria>
One `apply_completion_decision` and one overlay-parameterized classify-preamble helper, both no-behavior-change, with the golden equivalence test and existing write-path suites green.
</success_criteria>

<output>
Create `.planning/phases/150-consolidate-write-path/150-03-SUMMARY.md` when done.
</output>
