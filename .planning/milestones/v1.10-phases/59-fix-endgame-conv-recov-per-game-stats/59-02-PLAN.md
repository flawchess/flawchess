---
phase: 59-fix-endgame-conv-recov-per-game-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx
  - frontend/src/components/charts/EndgamePerformanceSection.tsx
autonomous: true
requirements: []
tags: [frontend, endgame, cleanup, deletion]

must_haves:
  truths:
    - "The admin-gated 'Conversion and Recovery' section (gauges + rolling-window timeline) no longer renders for any user, admin or otherwise"
    - "frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx no longer exists"
    - "The EndgameGaugesSection component (defined inside EndgamePerformanceSection.tsx) is deleted; EndgamePerformanceSection itself remains unchanged"
    - "frontend/src/pages/Endgames.tsx no longer references EndgameGaugesSection, EndgameConvRecovTimelineChart, showPerfSection, showConvRecovTimeline, or convRecovData"
    - "npm run build, npm run lint, and npm run knip all succeed after deletion"
  artifacts:
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Endgames page with admin-gated Conv/Recov section removed"
      min_lines: 200
    - path: "frontend/src/components/charts/EndgamePerformanceSection.tsx"
      provides: "EndgamePerformanceSection only (gauges section exported removed)"
      contains: "export function EndgamePerformanceSection"
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgamePerformanceSection"
      via: "import still works"
      pattern: "import \\{ EndgamePerformanceSection"
---

<objective>
Delete the admin-gated "Conversion and Recovery" UI section end-to-end on the frontend: remove `EndgameConvRecovTimelineChart.tsx`, remove the `EndgameGaugesSection` export (and its supporting constants/imports) from `EndgamePerformanceSection.tsx`, and strip the `isAdmin && (showPerfSection || showConvRecovTimeline)` block plus its dependent variables and imports from `Endgames.tsx`. Do NOT touch the backend in this plan (schema field removals + service cleanup happen in Plan 59-03 after this plan lands to avoid breaking the frontend compile while backend still serves the deprecated fields).

Purpose: Closes success criterion #3 (frontend half) from Phase 59. Must run BEFORE Plan 59-03 so the backend can remove schema fields without breaking the frontend type-check.
Output: Slimmer Endgames page, deleted ConvRecov timeline component, and `EndgamePerformanceSection.tsx` trimmed to only export the still-used `EndgamePerformanceSection`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-fix-endgame-conv-recov-per-game-stats/59-CONTEXT.md
@frontend/src/pages/Endgames.tsx
@frontend/src/components/charts/EndgamePerformanceSection.tsx
@frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx
@frontend/src/types/endgames.ts

<interfaces>
Key types and imports preserved by this plan (do NOT break these):

From `frontend/src/components/charts/EndgamePerformanceSection.tsx` (AFTER this plan):
- `export function EndgamePerformanceSection(props: { data: EndgamePerformanceResponse; scoreGap?: ScoreGapMaterialResponse })` — KEEP
- `export const MATERIAL_ADVANTAGE_POINTS = 1` — KEEP (still imported by EndgameConvRecovChart.tsx)
- `export const PERSISTENCE_MOVES = 2` — KEEP (same reason)
- `export function EndgameGaugesSection(...)` — REMOVE

From `frontend/src/pages/Endgames.tsx` (AFTER this plan):
- Still renders: `<EndgamePerformanceSection data={perfData} scoreGap={scoreGapData} />`, `<EndgameScoreGapSection>`, `<EndgameClockPressureSection>`, `<EndgameTimePressureSection>`, `<EndgameConvRecovChart>`, `<EndgameTimelineChart>`, `<EndgameWDLChart>`.
- No longer renders the `isAdmin && (showPerfSection || showConvRecovTimeline)` block at lines 263-278.

Fields on `EndgamePerformanceResponse` consumed by the surviving `EndgamePerformanceSection` (Plan 59-03 must NOT remove these):
- `data.endgame_wdl` (used extensively)
- `data.non_endgame_wdl` (used extensively)

Fields on `EndgamePerformanceResponse` consumed ONLY by the removed `EndgameGaugesSection`:
- `aggregate_conversion_pct`, `aggregate_conversion_wins`, `aggregate_conversion_games`
- `aggregate_recovery_pct`, `aggregate_recovery_saves`, `aggregate_recovery_games`
- `endgame_skill`
(These plus `relative_strength` and `overall_win_rate` — the latter two had no consumer even before this plan — are all safe to delete from the backend schema in Plan 59-03.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove EndgameGaugesSection export and its supporting code from EndgamePerformanceSection.tsx</name>
  <files>frontend/src/components/charts/EndgamePerformanceSection.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgamePerformanceSection.tsx (full file, ~316 lines)
    - .planning/phases/59-fix-endgame-conv-recov-per-game-stats/59-CONTEXT.md (decision 3)
  </read_first>
  <action>
Edit `frontend/src/components/charts/EndgamePerformanceSection.tsx` with the following precise changes:

1. **Remove the `EndgameGauge` import** (line 7):
   - Delete: `import { EndgameGauge, type GaugeZone } from '@/components/charts/EndgameGauge';`
   - The `EndgameGauge` component is only used by the `EndgameGaugesSection` function being deleted.

2. **Remove the theme gauge color imports** (line 11):
   - Delete: `import { GAUGE_DANGER, GAUGE_WARNING, GAUGE_SUCCESS } from '@/lib/theme';`
   - These are only used by the gauge zone constants being deleted.

3. **Remove the three `GaugeZone[]` constants** (lines 23-40):
   - Delete `CONVERSION_ZONES`, `RECOVERY_ZONES`, `ENDGAME_SKILL_ZONES` (including the preceding comment block "// Per-gauge zone definitions — thresholds differ per metric, colors from theme constants").

4. **Remove the `EndgameGaugesSectionProps` interface** (lines 47-49):
   - Delete:
     ```
     interface EndgameGaugesSectionProps {
       data: EndgamePerformanceResponse;
     }
     ```

5. **Remove the entire `EndgameGaugesSection` function** (lines ~248-316, from the JSDoc comment `/** Gauge charts for Conversion, Recovery, and Endgame Skill.` through the closing `}` at EOF).

6. **Update the top-of-file JSDoc** (lines 1-5):
   - Replace:
     ```
     /**
      * Endgame Performance section (D-03 through D-07):
      * - Endgame vs Non-Endgame WDL comparison table (games, WDL, score, score diff)
      * - Three semicircle gauge charts (Conversion, Recovery, Endgame Skill) in a single row
      */
     ```
   - With:
     ```
     /**
      * Endgame Performance section:
      * - Endgame vs Non-Endgame WDL comparison table (games, WDL, score, score diff)
      *
      * Phase 59 removed the admin-only gauge charts (Conversion, Recovery, Endgame Skill);
      * the associated EndgameGaugesSection and its gauge-zone constants were deleted.
      */
     ```

7. **KEEP unchanged**:
   - `export const MATERIAL_ADVANTAGE_POINTS = 1;` (line 18) — still imported by `EndgameConvRecovChart.tsx` and `Endgames.tsx`.
   - `export const PERSISTENCE_MOVES = 2;` (line 21) — same reason.
   - `export function EndgamePerformanceSection(...)` and all its helper constants (`SCORE_DIFF_NEUTRAL_MIN`, `SCORE_DIFF_NEUTRAL_MAX`), props interface, desktop/mobile JSX — all untouched.
   - Imports: `InfoPopover`, `MiniWDLBar`, `MiniBulletChart`, `EndgamePerformanceResponse`, `ScoreGapMaterialResponse` — all still used by `EndgamePerformanceSection`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit --project tsconfig.json 2>&amp;1 | grep -E "EndgamePerformanceSection\\.tsx|EndgameGaugesSection" | head -5 &amp;&amp; npm run lint -- --max-warnings=0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EndgameGaugesSection" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns ZERO matches.
    - `grep -n "CONVERSION_ZONES\\|RECOVERY_ZONES\\|ENDGAME_SKILL_ZONES" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns ZERO matches.
    - `grep -n "EndgameGauge" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns ZERO matches (neither the component nor the GaugeZone type should be imported).
    - `grep -n "GAUGE_DANGER\\|GAUGE_WARNING\\|GAUGE_SUCCESS" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns ZERO matches.
    - `grep -n "export function EndgamePerformanceSection" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns EXACTLY ONE match (function still exists).
    - `grep -n "export const MATERIAL_ADVANTAGE_POINTS\\|export const PERSISTENCE_MOVES" frontend/src/components/charts/EndgamePerformanceSection.tsx` returns exactly two matches (still exported).
    - `cd frontend && npm run lint` exits 0.
  </acceptance_criteria>
  <done>EndgameGaugesSection and its supporting constants/imports are removed; EndgamePerformanceSection and MATERIAL_ADVANTAGE_POINTS/PERSISTENCE_MOVES remain; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete EndgameConvRecovTimelineChart.tsx and strip the admin-gated section + dependent vars from Endgames.tsx</name>
  <files>frontend/src/pages/Endgames.tsx, frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx lines 1-100 (imports + state) and lines 125-300 (the admin-gated block and surrounding render tree)
    - frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx (confirm its exports before deletion)
    - .planning/phases/59-fix-endgame-conv-recov-per-game-stats/59-CONTEXT.md (decision 3, frontend scope)
  </read_first>
  <action>
A) **Delete `frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx` entirely.** Use the `rm` command via Bash or equivalent file-delete. Do NOT leave an empty file or a stub — remove the path from the filesystem.

B) **Edit `frontend/src/pages/Endgames.tsx`** with the following precise changes:

1. **Update line 20** — remove `EndgameGaugesSection` from the named-imports list:
   - Before: `import { EndgamePerformanceSection, EndgameGaugesSection, MATERIAL_ADVANTAGE_POINTS, PERSISTENCE_MOVES } from '@/components/charts/EndgamePerformanceSection';`
   - After:  `import { EndgamePerformanceSection, MATERIAL_ADVANTAGE_POINTS, PERSISTENCE_MOVES } from '@/components/charts/EndgamePerformanceSection';`

2. **Delete line 23** entirely:
   - Delete: `import { EndgameConvRecovTimelineChart } from '@/components/charts/EndgameConvRecovTimelineChart';`

3. **Delete line 140** (the `convRecovData` derivation):
   - Delete: `const convRecovData = overviewData?.conv_recov_timeline;`
   - Note the surrounding `perfData`, `timelineData`, `statsData`, `scoreGapData` assignments in that block — keep them untouched; only remove the `convRecovData` line.

4. **Delete line 192** (the `showConvRecovTimeline` flag):
   - Delete: `const showConvRecovTimeline = !!(convRecovData && (convRecovData.conversion.length > 0 || convRecovData.recovery.length > 0));`

5. **Delete the admin-gated block at lines 263-278** (the entire block starting with `{/* ── Conversion and Recovery (admin only) ── */}` through the closing `)}` of the `{isAdmin && (showPerfSection || showConvRecovTimeline) && (...)}` expression). The block to remove is:

```tsx
          {/* ── Conversion and Recovery (admin only) ── */}
          {isAdmin && (showPerfSection || showConvRecovTimeline) && (
            <>
              <h2 className="text-lg font-semibold text-foreground mt-2">Conversion and Recovery</h2>
              {showPerfSection && (
                <div className="charcoal-texture rounded-md p-4">
                  <EndgameGaugesSection data={perfData} />
                </div>
              )}
              {showConvRecovTimeline && (
                <div className="charcoal-texture rounded-md p-4">
                  <EndgameConvRecovTimelineChart data={convRecovData} />
                </div>
              )}
            </>
          )}
```

6. **Verify `isAdmin` is still used elsewhere in Endgames.tsx.** Run `grep -n "isAdmin" frontend/src/pages/Endgames.tsx` AFTER the above edits:
   - If `isAdmin` still has other callsites, keep its declaration (the `useUserProfile` import and derivation) untouched.
   - If `isAdmin` becomes unused (zero other callsites), ALSO delete its declaration line(s) and drop `useUserProfile` from imports IF nothing else in the file uses `useUserProfile`. Verify with `grep -n "useUserProfile" frontend/src/pages/Endgames.tsx` before removing the import.

7. **Do NOT touch** `showPerfSection` (line 191) — it is still used by the still-rendered `EndgamePerformanceSection` block at lines 204-261. Verify by reading that block. Only `showConvRecovTimeline` should be removed.

8. **Do NOT touch** the `conv_recov_timeline` field reference inside `useEndgameOverview` output consumption elsewhere — grep confirms there is only ONE site: line 140, which we are deleting. If `grep -n "conv_recov_timeline" frontend/src/pages/Endgames.tsx` returns anything after the edit, investigate and remove.

9. **Do NOT yet delete** `frontend/src/types/endgames.ts` entries for `ConvRecovTimelinePoint`, `ConvRecovTimelineResponse`, or the `conv_recov_timeline` field on `EndgameOverviewResponse`. These are tied to the backend schema shape and will be removed in Plan 59-03 together with the backend schema fields. Removing them here would create a type mismatch (backend still returns them until 59-03 lands). Knip may warn about them — that warning is expected and resolves when 59-03 lands. If knip fails CI on this plan, explicitly add these three names to the knip ignore list in `frontend/knip.json` (or `knip.config.ts`, whichever exists) with a comment: `// Transiently unused; removed in Phase 59 Plan 03`. Prefer the ignore-list approach to keeping types in sync with the running backend.
  </action>
  <verify>
    <automated>test ! -f frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx &amp;&amp; cd frontend &amp;&amp; npm run lint -- --max-warnings=0 &amp;&amp; npm run build &amp;&amp; npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f frontend/src/components/charts/EndgameConvRecovTimelineChart.tsx` succeeds (file is gone).
    - `grep -rn "EndgameConvRecovTimelineChart" frontend/src` returns ZERO matches.
    - `grep -rn "EndgameGaugesSection" frontend/src` returns ZERO matches.
    - `grep -n "showConvRecovTimeline" frontend/src/pages/Endgames.tsx` returns ZERO matches.
    - `grep -n "convRecovData" frontend/src/pages/Endgames.tsx` returns ZERO matches.
    - `grep -n "Conversion and Recovery" frontend/src/pages/Endgames.tsx` returns ZERO matches (the admin-gated heading is gone).
    - `grep -n "EndgamePerformanceSection" frontend/src/pages/Endgames.tsx` returns AT LEAST ONE match (still used). Line 20 import still present minus the gauges export.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm run knip` exits 0 (add targeted ignores for `ConvRecovTimelinePoint`, `ConvRecovTimelineResponse`, and `conv_recov_timeline` in types/endgames.ts only if knip reports them — document reason as "Removed in Plan 59-03").
  </acceptance_criteria>
  <done>ConvRecov timeline chart file deleted, admin-gated section + its dependent vars stripped from Endgames.tsx, lint + build + knip all green, surviving imports intact.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint` — exits 0.
- `cd frontend && npm run build` — exits 0 (production bundle compiles).
- `cd frontend && npm run knip` — exits 0 (with at most three documented ignores if backend schema still has the fields at merge time).
- Manual smoke (post-deploy of this plan, not blocking): load `/endgames` page as admin user — the "Conversion and Recovery" heading is gone; all other sections (Games with vs without Endgame, Time Pressure, Endgame Type Breakdown, WDL, Timeline) render correctly.
- `grep -rn "EndgameConvRecovTimelineChart\\|EndgameGaugesSection" frontend/src` — zero matches.
</verification>

<success_criteria>
- `EndgameConvRecovTimelineChart.tsx` no longer exists.
- `EndgameGaugesSection` export removed from `EndgamePerformanceSection.tsx`; `EndgamePerformanceSection` itself unchanged.
- Admin-gated "Conversion and Recovery" section no longer renders for any user.
- Frontend build, lint, knip all pass.
- Plan 59-03 can now safely remove the orphaned backend fields without breaking the frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/59-fix-endgame-conv-recov-per-game-stats/59-02-SUMMARY.md`
</output>
