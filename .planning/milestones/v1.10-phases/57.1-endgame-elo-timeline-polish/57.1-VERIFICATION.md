---
phase: 57.1-endgame-elo-timeline-polish
verified: 2026-04-18T00:15:00Z
status: human_needed
score: 8/8 must-haves verified (backend) + 12/12 must-haves verified (frontend) + 4/4 ROADMAP SCs verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Volume bars render at chart bottom (~20%) without overlapping line series"
    expected: "Muted bars at bottom ~20% of chart canvas, hidden right Y-axis, visible on a live user dashboard with >=12 weeks of endgame data"
    why_human: "Visual layout — depends on Recharts axis configuration and rendered DOM, not data shape"
  - test: "Info popover copy frames metric as skill-adjusted rating"
    expected: "Popover paragraphs 1-3 match D-12/D-13/D-14 verbatim; 'performance rating' phrase absent; 50% skill => zero-delta intuition conveyed"
    why_human: "Subjective copy review — popover text renders as HTML with entities; visual verification ensures the HTML entities (&middot;, &minus;, &mdash;, &ndash;) display as the intended glyphs"
  - test: "Legend toggle updates bar heights in real time"
    expected: "Toggling a combo off via legend shrinks bar heights (that combo's per-week count removed from the sum); 'Games this week' tooltip number updates"
    why_human: "Interactive DOM behavior — legend click handler + useMemo reactivity must work end-to-end in a real browser"
  - test: "Bright Actual ELO line matches the user's real per-combo rating curve (not a rolling-mean approximation)"
    expected: "For a live user, compare the Endgame ELO chart's bright line to the Global Stats RatingChart for the same date range — curves should match closely (asof anchor)"
    why_human: "Requires a live user account with imported games to see the shape of the curve"
---

# Phase 57.1: Endgame ELO Timeline Polish Verification Report

**Phase Goal:** Switch Endgame ELO Timeline from rolling-mean opponent-rating anchor to per-combo asof-joined user-rating anchor, add per-week volume bars, and rewrite info popover / subtitle copy to drop "performance rating" framing.

**Verified:** 2026-04-18
**Status:** human_needed (all automated checks pass; visual verification of chart rendering + popover copy deferred to HUMAN-UAT)
**Re-verification:** No — initial verification

## Goal Achievement

### ROADMAP Success Criteria (the contract)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Bright Actual ELO line equals user's latest per-combo rating at each emitted weekly date (forward-filled), not a rolling mean | VERIFIED | `_compute_endgame_elo_weekly_series` lines 1030-1036 uses `bisect.bisect_right(actual_elo_dates, next_monday_dt)` and stores `actual_elo_at_date = actual_elo_ratings[idx - 1]` — strict asof-join with forward-fill via re-using the prior idx-1 when the week has no games. `test_actual_elo_asof_forward_fill` + `test_actual_elo_uses_asof_not_rolling_mean` cover it. |
| 2 | Dark dashed Endgame ELO line equals `actual_elo + 400·log10(skill/(1-skill))` | VERIFIED | `_endgame_elo_from_skill` line 869 computes `round(actual_elo_at_date + 400 * math.log10(s / (1 - s)))` with clamp `[0.05, 0.95]`. `test_neutral_skill_lines_coincide` asserts skill=0.5 => endgame_elo == actual_elo (log10(1) = 0). |
| 3 | Chart renders a muted volume bar per emitted week showing that week's qualifying endgame game count | VERIFIED | Backend: `per_week_endgame_games: int` on schema line 359; frontend: `<Bar yAxisId="bars" dataKey="per_week_total_visible" fill={ENDGAME_VOLUME_BAR_COLOR} ... data-testid="endgame-elo-volume-bars" />` at EndgameEloTimelineSection.tsx:396-403. Bar height aggregates across visible combos via `barChartData` useMemo keyed on `hiddenKeys`. Visual confirmation deferred to human step 1. |
| 4 | Info popover no longer calls the metric a performance rating; frames it as skill-adjusted rating with 50% skill => zero-delta intuition | VERIFIED | `grep -c "performance rating" frontend/src/components/charts/EndgameEloTimelineSection.tsx` returns 0 (also 0 in schemas/endgames.py and service). D-12/D-13/D-14 paragraph text present verbatim (formula "actual_elo + 400 &middot; log10(skill / (1 &minus; skill))" at line 133 of UI-SPEC; "75% skill puts Endgame ELO roughly 190 Elo above" and "5&ndash;95%" clamp present in JSX). Visual validation of entities (&mdash;, &ndash;, &middot;) deferred to human step 2. |

**ROADMAP SC score:** 4/4 verified programmatically (visual confirmation pending HUMAN-UAT).

### Plan 01 (Backend) Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `endgame_elo = round(actual_elo_at_date + 400 * log10(skill / (1 - skill)))` — anchored on actual rating, not opponent rolling mean | VERIFIED | endgame_service.py:868-869 + docstring explicitly names D-01 shift |
| 2 | `actual_elo` equals user's rating from latest game played on or before point's ISO-week-end date, per combo (asof with forward-fill) | VERIFIED | endgame_service.py:1030-1036 implements bisect.bisect_right on per-combo sorted (dates, ratings). Orchestrator lines 1793-1810 builds asof arrays per combo |
| 3 | When skill = 0.5 exactly, endgame_elo == actual_elo | VERIFIED | log10(0.5/0.5) = log10(1) = 0, so formula returns actual_elo unchanged. Covered by `test_neutral_skill_lines_coincide` (line 3455) |
| 4 | `per_week_endgame_games` counts ONLY that ISO week's qualifying endgame games (NOT trailing 100-game window) | VERIFIED | endgame_service.py:1008-1011: counter keyed on `(iso_year_evt, iso_week_evt)` incremented on every endgame event. Emission line 1045 uses `per_week_count.get((iso_year, iso_week), 0)` — per-week key, not window length. Covered by `test_per_week_count_is_not_trailing` |
| 5 | `endgame_games_in_window` remains on each point | VERIFIED | endgame_service.py:1044 `"endgame_games_in_window": len(endgame_window)` |
| 6 | Per-combo asof isolation | VERIFIED | Orchestrator partitions `asof_by_combo` by `(platform, tc)` key before pre-computing arrays (line 1797-1810). Covered by `test_per_combo_asof_isolation` |
| 7 | UI-SPEC Wire Schema + Copywriting Contract sections updated inline | VERIFIED | 57-UI-SPEC.md line 115 contains "Bars show endgame games per week..."; formula at line 133 uses HTML entities; trailer comment "Revised in Phase 57.1" present |
| 8 | HUMAN-UAT items 1, 3 superseded + new volume-bars item added | VERIFIED | 57-HUMAN-UAT.md line 47 has "### 9. Volume bars render at chart bottom, scale with visible combos"; trailer comment "Revised in Phase 57.1" present; "performance rating" absent |

**Plan 01 score:** 8/8 truths verified.

### Plan 02 (Frontend) Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `EndgameEloTimelinePoint` TS interface gains `per_week_endgame_games: number` | VERIFIED | endgames.ts:200 |
| 2 | Chart uses Recharts `<ComposedChart>` (not `<LineChart>`) | VERIFIED | EndgameEloTimelineSection.tsx:321 + import on line 22; `grep -c LineChart` returns 0 |
| 3 | Muted volume bars render at bottom ~20% via hidden right Y-axis with `domain={[0, barMax * 5]}` | VERIFIED | EndgameEloTimelineSection.tsx:328 (`<YAxis yAxisId="bars" orientation="right" hide domain={[0, barMax * 5]} />`) + barMax useMemo with `Math.max(m, 1)` floor on line 156 |
| 4 | Bar height per week = sum of `per_week_endgame_games` across currently-visible combos | VERIFIED | EndgameEloTimelineSection.tsx:127-147 `barChartData` useMemo iterates `data?.combos`, skips `hiddenKeys.has(combo_key)`, sums `per_week_games` into `per_week_total_visible`. Keyed on `[chartData, data?.combos, hiddenKeys]` so legend toggles trigger re-aggregation |
| 5 | Every `<Line>` gets `yAxisId="elo"`; main `<YAxis>` gets `yAxisId="elo"`; new `<YAxis yAxisId="bars" hide>` added | VERIFIED | Both Line elements at lines 416, 428 carry `yAxisId="elo"`; main YAxis at line 324 has `yAxisId="elo"`; bars axis at line 328 |
| 6 | `<Bar>` uses `legendType="none"` to stay out of the legend | VERIFIED | EndgameEloTimelineSection.tsx:401 |
| 7 | Tooltip gains top line "Games this week: N (visible combos)" | VERIFIED | EndgameEloTimelineSection.tsx:345 |
| 8 | Info popover copy matches D-12/D-13/D-14 verbatim (paragraph 4 unchanged) | VERIFIED | Popover contains "your Actual ELO shifted by how much your" (D-12), "75% skill puts Endgame ELO roughly 190 Elo above" (D-13), "Skill is clamped to the 5&ndash;95%" (D-14); "performance rating" absent |
| 9 | Chart subtitle matches D-16 verbatim | VERIFIED | EndgameEloTimelineSection.tsx:208 "Bars show endgame games per week. Bright lines are Actual ELO, dark dashed lines are Endgame ELO." |
| 10 | Old prose about `avg_opponent_rating` and "performance rating" removed | VERIFIED | `grep -c "performance rating\|avg_opponent_rating"` returns 0 in the component |
| 11 | Bar fill uses `ENDGAME_VOLUME_BAR_COLOR` theme constant (no inline oklch for the bar) | VERIFIED | theme.ts:107 defines the constant; EndgameEloTimelineSection.tsx:399 uses `fill={ENDGAME_VOLUME_BAR_COLOR}` |
| 12 | `data-testid="endgame-elo-volume-bars"` added near the `<Bar>` element | VERIFIED | EndgameEloTimelineSection.tsx:402 (NOTE: REVIEW IN-03 flags that Recharts may not forward this prop to the DOM — see Anti-Patterns below for follow-up) |

**Plan 02 score:** 12/12 truths verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/schemas/endgames.py` | Adds `per_week_endgame_games: int`; rewrites docstring | VERIFIED | Line 359 contains the field; docstring at lines 332-353 uses "skill-adjusted rating" framing |
| `app/services/endgame_service.py` | Signature swap (avg_opp -> actual_elo_at_date), bisect asof helper, per-week counter, orchestrator asof pre-compute | VERIFIED | `import bisect` at line 15; _endgame_elo_from_skill rewritten at lines 853-869; `_compute_endgame_elo_weekly_series` accepts new params (lines 950-957); orchestrator builds `asof_by_combo` (lines 1793-1810) |
| `tests/test_endgame_service.py` | 6 new D-18 tests + renamed rolling-mean test | VERIFIED | `test_actual_elo_asof_forward_fill`, `test_asof_same_day_uses_last_game`, `test_neutral_skill_lines_coincide`, `test_per_combo_asof_isolation`, `test_per_week_count_is_not_trailing`, `test_no_prior_game_does_not_emit`, `test_actual_elo_uses_asof_not_rolling_mean` all grep-present |
| `tests/test_integration_routers.py` | Field-presence assertion on `/api/endgames/overview` | VERIFIED | `test_endgame_overview_elo_timeline_includes_per_week_count` at line 422 asserts field presence + int type + >= 0 |
| `frontend/src/types/endgames.ts` | TS interface mirrors backend | VERIFIED | Line 200 |
| `frontend/src/lib/theme.ts` | `ENDGAME_VOLUME_BAR_COLOR` export | VERIFIED | Line 107 with 5-line comment explaining L / chroma / alpha choices |
| `frontend/src/components/charts/EndgameEloTimelineSection.tsx` | ComposedChart swap + Bar + axis migration + copy rewrite | VERIFIED | All 12 Plan 02 truths confirmed via grep |
| `57-UI-SPEC.md` | Inline supersede per D-12/D-13/D-14/D-16 with trailer comment | VERIFIED | `grep "Revised in Phase 57.1"` matches; locked strings present |
| `57-HUMAN-UAT.md` | Items 1, 3 superseded; new item 9 for volume bars | VERIFIED | Item 9 present; trailer comment matches |

All artifacts VERIFIED at Levels 1-3 (exist, substantive, wired).

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `get_endgame_elo_timeline` orchestrator | `_compute_endgame_elo_weekly_series` | pre-computed per-combo `(dates, ratings)` arrays | VERIFIED | Call site at line 1816-1823 passes `asof_dates, asof_ratings` positionally |
| `_endgame_elo_from_skill` | `math.log10` | `round(actual_elo_at_date + 400 * log10(s / (1 - s)))` | VERIFIED | Line 869 |
| `EndgameEloTimelineSection.tsx` | `recharts::ComposedChart` | import + JSX root | VERIFIED | import line 22, JSX line 321 |
| `EndgameEloTimelineSection.tsx` | `theme.ts::ENDGAME_VOLUME_BAR_COLOR` | import + Bar fill prop | VERIFIED | import line 25, fill at line 399 |
| `EndgameEloTimelineSection.tsx` | `types/endgames.ts::EndgameEloTimelinePoint::per_week_endgame_games` | data binding (bar series + tooltip) | VERIFIED | row mapping at line 119 reads `pt.per_week_endgame_games`; tooltip reads `per_week_total_visible` (aggregate) at line 345 |
| `Endgames.tsx` page | `EndgameEloTimelineSection` component | import + JSX render | VERIFIED | `frontend/src/pages/Endgames.tsx:26` + line 291 |

All key links WIRED.

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `EndgameEloTimelineSection` | `data.combos[*].points[*].per_week_endgame_games` | `/api/endgames/overview` -> `get_endgame_overview` -> `get_endgame_elo_timeline` -> `_compute_endgame_elo_weekly_series` | Yes — counter incremented on every endgame event (line 1008-1011) and emitted on the dict at line 1045 | FLOWING |
| `EndgameEloTimelineSection` | `actual_elo` (per point) | asof-join via `bisect.bisect_right` on per-combo `(dates, ratings)` arrays derived from repo `all_rows` | Yes — real DB rows via `query_endgame_elo_timeline_rows` | FLOWING |
| `barChartData` (frontend) | `per_week_total_visible` | `chartData` + `data?.combos` + `hiddenKeys` useMemo aggregation | Yes — sums real per-point values from server response; respects hidden state | FLOWING |
| `barMax` (frontend) | clamped max | `barChartData` useMemo with `Math.max(m, 1)` floor | Yes (computed from real data; safe fallback prevents [0,0] Recharts domain) | FLOWING |

All dynamic-rendering artifacts have real data flow. No hollow props, no static fallbacks on the main paths.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend type check passes | `uv run ty check app/ tests/` | "All checks passed!" | PASS |
| Endgame ELO timeline unit + integration tests | `uv run pytest tests/test_endgame_service.py::TestEndgameEloTimeline tests/test_integration_routers.py::TestEndgameEloTimelineRouter -x -q` | 15 passed in 0.46s | PASS |
| Full backend test suite | `uv run pytest -x -q` | 836 passed in 12.79s | PASS |
| Frontend vitest suite | `cd frontend && npm test -- --run` | 83 passed across 6 test files | PASS |
| Frontend production build | `cd frontend && npm run build` | built in 4.24s, prerender OK, PWA precache 11 entries | PASS |
| "performance rating" fully removed | `grep -c "performance rating" frontend/src/components/charts/EndgameEloTimelineSection.tsx` | 0 | PASS |
| "performance rating" removed from backend | `grep -c "performance rating" app/schemas/endgames.py app/services/endgame_service.py` | 0 / 0 | PASS |
| UI-SPEC supersede trailer comment present | `grep "Revised in Phase 57.1" .planning/phases/57-endgame-elo-timeline-chart/57-UI-SPEC.md` | match | PASS |
| HUMAN-UAT new item 9 present | `grep "### 9. Volume bars render at chart bottom" 57-HUMAN-UAT.md` | match | PASS |

All automated behavioral checks pass.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ELO-05 | Both plans (refinement) | Endgame ELO timeline chart with color-matched paired lines | SATISFIED (refined) | Phase 57 closed ELO-05 initial delivery; Phase 57.1 refines the anchor semantics (D-01/D-04 superseded) and adds volume bars without introducing new requirement IDs. All 4 ROADMAP SCs VERIFIED. |

No orphaned requirements — Phase 57.1 is explicitly a refinement with no new requirement IDs per CONTEXT line 14 ("OUT OF SCOPE: Adding new requirement IDs").

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| app/services/endgame_service.py | 879 | Pre-existing TODO comment (Phase 56 dedup with `endgame_skill()`) | Info | Not introduced by Phase 57.1. Unrelated to this phase's anchor change. |
| app/services/endgame_service.py | 1023-1030 | WR-01 (REVIEW): asof cutoff `next_monday_dt` preserves time-of-day of triggering `played_at` — can bleed into next ISO week by up to 24h | Warning | Real correctness edge case. Flagged in REVIEW.md. Not exercised by current fixtures. Per orchestrator note, route to human follow-up via `/gsd-code-review-fix 57.1` or future polish. |
| app/services/endgame_service.py | 1004-1046 | WR-02 (REVIEW): per-week count can report 0 when an "all" event triggers emission for a future ISO week with no endgame games yet | Warning | Can produce tooltip "Games this week: 0" on a point that implies non-zero endgame activity. Flagged in REVIEW.md. Not exercised by current fixtures. Per orchestrator note, route to human follow-up. |
| frontend/src/components/charts/EndgameEloTimelineSection.tsx | 402 | IN-03 (REVIEW): `data-testid` on `<Bar>` may not forward to the DOM in this Recharts version | Info | Browser automation selector may match nothing. No tests currently depend on it. Optional fix: move to container g. |
| frontend/src/components/charts/EndgameEloTimelineSection.tsx | 116-119, 348-351 | IN-04 (REVIEW): template-literal record keys (`${combo_key}_per_week_games` etc.) repeated across 3 sites without a typed extractor | Info | Minor maintainability risk. Optional extraction. |
| app/services/endgame_service.py | 1007 | IN-01 (REVIEW): `endgame_window[-window:]` O(n) slice on every endgame event | Info | Style — `deque(maxlen=window)` would be O(1). Not a correctness issue. |
| frontend/src/components/charts/EndgameEloTimelineSection.tsx | 392 | IN-02 (REVIEW): `renderLegend()` called on every render | Info | Minor perf; React reconciliation handles it. Optional fix: pass the function reference. |

All code review findings are Warning / Info severity; zero Critical / Blocker. Per REVIEW.md classification `status: issues_found` (2 warnings, 4 info), none block phase closure. Orchestrator explicitly notes these are acceptable edge cases for human follow-up.

### Human Verification Required

Automated verification cannot cover:

#### 1. Volume bars visual rendering

**Test:** Open `/endgames` for a user with >=12 weeks of endgame data across multiple combos.
**Expected:** Muted volume bars render at the bottom ~20% of the chart canvas. Bars should read as a low-opacity neutral gray against the charcoal-texture card surface — "context, not data". No overlap with either the bright Actual ELO or dark dashed Endgame ELO lines. Right Y-axis (bar axis) hidden.
**Why human:** Layout and color perception — the `domain={[0, barMax * 5]}` trick and the oklch `'oklch(0.55 0 0 / 0.25)'` fill must render correctly on real pixels; grep confirms the configuration but not the visual outcome.

#### 2. Info popover copy rendering

**Test:** Click the info popover icon on the Endgame ELO Timeline card. Read all 4 paragraphs.
**Expected:**
- Paragraph 1 contains "**Endgame ELO** is your Actual ELO shifted by how much your Endgame Skill exceeds (or falls short of) the 50% neutral mark" and the formula rendered as "actual_elo + 400 · log10(skill / (1 − skill))" (HTML entities `&middot;` and `&minus;` render as glyphs)
- Paragraph 2 contains "If your Endgame Skill is exactly 50% the two lines touch; 75% skill puts Endgame ELO roughly 190 Elo above, 25% skill puts it roughly 190 Elo below."
- Paragraph 3 mentions "trailing 100 endgame games", "fewer than 10 qualifying", "5–95% range" (en-dash via `&ndash;`)
- Paragraph 4 (Glicko caveat) unchanged
- The phrase "performance rating" does NOT appear
**Why human:** HTML-entity rendering + typographic glyph verification cannot be automated cleanly from source.

#### 3. Legend toggle reactivity

**Test:** With the chart open, toggle individual combos on/off via the legend chips.
**Expected:**
- Toggling a combo OFF: that combo's bright + dark lines disappear; volume bars SHRINK (that combo's per-week count removed from the sum); tooltip "Games this week" number updates
- Toggling ALL combos OFF: bars disappear (or render at zero height)
- Toggling back ON: bars + lines restore to previous heights
**Why human:** Interactive DOM behavior — `hiddenKeys` state + `barChartData` useMemo reactivity must work in a real browser.

#### 4. Anchor-change correctness (visual comparison with RatingChart)

**Test:** Compare the bright Actual ELO line shape on the Endgame ELO Timeline to the user's actual rating curve on the Global Stats RatingChart for the same time range.
**Expected:** The two curves should match closely — the asof anchor is now the user's real rating at each date (not the rolling mean Phase 57 shipped). This is the visible payoff of the anchor change.
**Why human:** Requires a live user account with imported games; comparison is visual.

### Gaps Summary

**No blocking gaps.** All automated must-haves verified: 4/4 ROADMAP SCs, 8/8 Plan 01 truths, 12/12 Plan 02 truths, all artifacts wired, all tests green (836 backend + 83 frontend), production build succeeds, `ty check` clean, "performance rating" eliminated from all code paths.

Phase is behaviorally complete at the code level. Remaining steps are visual + interactive verification (HUMAN-UAT items 1, 3, 4, 9 per updated 57-HUMAN-UAT.md) which are appropriately captured as human verification items above.

Two REVIEW.md warnings (WR-01 asof cutoff time-of-day, WR-02 per-week count on all-event emission) are real correctness edge cases not exercised by current fixtures. Per orchestrator direction, these are noted for human follow-up via `/gsd-code-review-fix 57.1` or a future polish phase and do NOT block phase closure.

---

_Verified: 2026-04-18_
_Verifier: Claude (gsd-verifier)_
