---
phase: 102-endgame-llm-statistical-reasoning-rework-v1-23
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - app/prompts/endgame_insights.md
  - app/services/insights_llm.py
  - tests/services/test_insights_llm.py
autonomous: true
requirements: [LLM-02, LLM-03, LLM-04, LLM-05, LLM-06]
must_haves:
  truths:
    - "The system prompt teaches the LLM to read pctl= and weave it in with cohort framing, never global-pool"
    - "The prompt explicitly states an extreme percentile inside a typical zone does NOT open the narration gate"
    - "The prompt teaches the new Score-Gap-by-time chart and the now-real Net Flag Rate metric"
    - "All stale references to the removed time_pressure_vs_performance chart and [low-time-gap] tag are purged"
    - "The overview cap is relaxed to a signal-gated ceiling with all existing guards preserved"
    - "Prompt vocabulary matches the concepts accordion AND the five tooltip popover bodies"
    - "_PROMPT_VERSION is endgame_v36 with a v36 rationale prepended to the inline changelog"
  artifacts:
    - path: "app/prompts/endgame_insights.md"
      provides: "Percentile + time-pressure teaching, vocab audit fixes, relaxed overview cap"
      contains: "pctl"
    - path: "app/services/insights_llm.py"
      provides: "Prompt version bumped so cache invalidates exactly once"
      contains: "endgame_v36"
  key_links:
    - from: "app/prompts/endgame_insights.md percentile teaching"
      to: "the pctl= token emitted by Plan 01"
      via: "glossary + gate-rule prose referencing pctl="
      pattern: "pctl"
    - from: "app/services/insights_llm.py _PROMPT_VERSION"
      to: "LLM cache key"
      via: "version string bump endgame_v35 -> endgame_v36"
      pattern: "endgame_v36"
---

<objective>
Teach the system prompt to reason over the payload additions Plan 01 wired in, audit the prompt's
UI vocabulary against the live user-facing copy, relax the overview length cap, and bump the prompt
version so the cache invalidates exactly once. No payload/service changes here — Plan 01 owns those.

Five concrete prompt changes, all from CONTEXT.md D-04/D-05/D-06/D-08/D-09/D-10 + 102-PATTERNS.md:
1. **Percentile teaching** (D-04/D-05, LLM-02/LLM-05): how to read `pctl=`, weave it with cohort
   framing ("vs ~{anchor}-rated {tc} peers"), and the explicit gate rule that percentile is NEVER a
   second significance signal.
2. **Time-pressure teaching** (D-06, LLM-04): teach the new Score-Gap-by-time chart and the
   now-real Net Flag Rate; purge the stale `time_pressure_vs_performance` / `[low-time-gap]`
   references that point at a chart Phase 88.1 deleted.
3. **Overview length relaxation** (D-08): signal-gated ceiling, all guards preserved.
4. **Vocabulary audit** (D-10): align prompt terms + sign conventions with the concepts accordion
   and the five tooltip popover bodies.
5. **Version bump** (D-09, LLM-06): `endgame_v35` → `endgame_v36` with a v36 changelog entry.

Purpose: the report narrates the new data correctly and never contradicts the hover-help.
Output: an updated system prompt + a single clean cache invalidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-CONTEXT.md
@.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-PATTERNS.md
@app/prompts/endgame_insights.md
@.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Percentile + time-pressure narration teaching; purge stale chart refs</name>
  <files>app/prompts/endgame_insights.md</files>
  <read_first>
    - app/services/insights_llm.py `_summary_window_line` (as modified by Plan 01) — confirm the
      EXACT `pctl=N (vs ~A-rated TC peers)` token shape so the prompt teaches the real wire format,
      and the EXACT new chart header `### Chart: time_pressure_score_gap_by_time` + its columns.
      Read the Plan 01 SUMMARY for the final field names.
    - app/prompts/endgame_insights.md STALE references to purge (verified present at plan time):
      lines ~23, ~172, ~192, ~296, ~453, ~455-459, ~478, ~485 all mention the removed
      `time_pressure_vs_performance` chart and/or the `[low-time-gap]` caption tag. Phase 88.1
      deleted that chart from the payload; these instructions point at nothing.
    - app/prompts/endgame_insights.md glossary entries: `avg_clock_diff_pct` (~449-453),
      `net_timeout_rate` (~461), and the section-layout table (~478, ~485).
    - 102-CONTEXT.md D-04 (gate rule), D-05 (cohort framing), D-06 (per-TC TPCTL no weighting vs
      page-level weighted), and 102-PATTERNS.md §"endgame_insights.md" #2 and #3.
    - Memory cross-refs to honor: feedback_llm_significance_signal (no parallel sig signal),
      feedback_percentile_chip_tooltip_disclosure (cohort framing), feedback_llm_prompt_design
      (no precomputed stories), feedback_popover_copy_minimalism (no "Wilson"/"sigmoid"/CI jargon).
  </read_first>
  <action>
    (A) Add a "Percentile annotations" teaching block near the within-noise / overview rules. Teach:
    `pctl=N` appears after `quality=` on a [summary] window line and tells you WHERE in the cohort the
    value sits. State the gate rule verbatim in intent (D-04): "A `pctl=` value is NOT a gate on
    whether to narrate — that remains the `zone` field exclusively. A `pctl=5` inside a `zone=typical`
    finding does NOT open the gate or license narration; percentile informs only HOW you phrase a
    finding the zone has already opened." State the framing rule (D-05): use cohort framing
    "vs other ~{N}-rated {tc} players" sourced from the payload's `(vs ~…-rated … peers)` suffix;
    forbid "globally" / "among all players". Give one worked example weaving zone + band + pctl.
    Add the granularity rule (D-06): time-pressure per-TC percentiles are the direct per-TC value
    (no weighting); page-level metrics use the game-count-weighted value — narrate each at the
    granularity its pctl is reported.

    (B) Replace the time-pressure chart teaching. Remove every reference to
    `time_pressure_vs_performance` and the `[low-time-gap]` tag (lines ~23, ~172, ~192, ~296,
    ~453-459, ~478, ~485). In their place, teach the new `### Chart: time_pressure_score_gap_by_time`
    block: per-TC, 5 quintiles of remaining clock (0-20% = max pressure … 80-100%), each row showing
    user_score / opp_score / score_delta / n / n_opp / typical_band; the central story is divergence
    in low-clock quintiles; quote the per-row typical_band, don't redo arithmetic; tie claims to
    quintiles that actually have sample (skip-when-None rows simply won't appear). Update the
    `avg_clock_diff_pct` glossary entry so its "read the time_pressure_vs_performance chart" pointer
    points at the new chart instead. Update the `net_timeout_rate` glossary entry to state it is now
    a real emitted scalar (was previously always empty) — keep the sign convention
    "(timeout_wins − timeout_losses) / total × 100, higher is better". Update the section-layout
    table rows to list `time_pressure_score_gap_by_time` instead of `time_pressure_vs_performance`.
    Update the time-pressure recommendation trigger (line ~23) to key off `avg_clock_diff_pct` weak
    and/or a weak low-clock quintile in the new chart, dropping the `[low-time-gap]` verdict phrasing.
  </action>
  <verify>
    <automated>grep -c "time_pressure_vs_performance\|low-time-gap" app/prompts/endgame_insights.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "time_pressure_vs_performance\|low-time-gap" app/prompts/endgame_insights.md` returns 0
      (all stale references purged).
    - `grep -n "time_pressure_score_gap_by_time" app/prompts/endgame_insights.md` returns ≥1 (new
      chart taught).
    - `grep -niE "pctl|percentile" app/prompts/endgame_insights.md` shows the percentile teaching
      block AND the explicit gate rule (`grep -i "does NOT open the gate\|not a gate" app/prompts/endgame_insights.md` returns a hit).
    - `grep -niE "vs other ~|rated .* (players|peers)" app/prompts/endgame_insights.md` shows cohort
      framing; `grep -ni "globally\|among all players" app/prompts/endgame_insights.md` returns 0 in
      the new percentile teaching (no global-pool framing).
    - No CI/jargon leakage in new prose: `grep -ni "Wilson\|sigmoid.*CI\|95% CI\|p-value\|p<0" app/prompts/endgame_insights.md`
      shows no NEW occurrences in the percentile/time-pressure sections (pre-existing Section-2
      caveat lines about p-values being forbidden may remain — they instruct the LLM NOT to use them).
  </acceptance_criteria>
  <done>Prompt teaches pctl= with cohort framing + the zone-as-sole-gate rule, teaches the new Score-Gap-by-time chart + real Net Flag Rate, and contains zero stale time_pressure_vs_performance / [low-time-gap] references.</done>
</task>

<task type="auto">
  <name>Task 2: Relax the overview cap (D-08) + vocabulary audit (D-10)</name>
  <files>app/prompts/endgame_insights.md</files>
  <read_first>
    - app/prompts/endgame_insights.md overview cap at line ~9 AND line ~190 (both say "1-3 short
      paragraphs … ~300 words"). Both must change consistently (102-PATTERNS.md §"overview cap").
    - app/prompts/endgame_insights.md within-noise rule (~46-62), flat-trend / silence-is-not-valid
      guards — these MUST be preserved; D-08 is permission to go longer, not to drop guards.
    - VOCABULARY AUDIT SOURCES (read-only, do NOT modify these files):
      - frontend/src/pages/Endgames.tsx concepts accordion (~lines 382-580) — authoritative
        label/definition source: Score, Score Gap, Eval, Eval Score, Eval Score Gap, Conversion,
        Parity, Recovery, Entry Eval, Entry Eval Score, Endgame Score, Endgame Score Gap, Endgame ELO /
        Non-Endgame ELO, Percentile Badges, Color Zones, Benchmark.
      - Tooltip popover bodies (search frontend/src for these components): `MetricStatPopover`,
        `WdlConfidenceTooltip`, `EvalConfidenceTooltip`, `AchievableScorePopover`, and the
        percentile-chip tooltip. Read each body's prose + sign conventions.
      - reports/benchmark/benchmarks-latest.md — source of truth for "typical"/normative copy
        (feedback_benchmark_source_of_truth). NOTE the path is `reports/benchmark/…`, not `reports/`.
    - 102-CONTEXT.md D-08 (signal-gated ceiling) and D-10 (audit BOTH surfaces); known drift items
      in 102-PATTERNS.md §"Vocabulary audit targets" ("Endgame Type" vs "Endgame Class").
  </read_first>
  <action>
    (A) Overview cap (D-08): rewrite BOTH line ~9 and line ~190 to the signal-gated ceiling:
    default stays "~250-300 words, 1-3 short paragraphs" for typical reports; the model MAY extend
    to "up to ~500 words / up to 5 short paragraphs ONLY when ≥3 distinct, non-overlapping
    narratable signals exist (e.g. an overall-gap story + a time-pressure story + a type-weakness
    story, each in a non-typical zone)". State explicitly this is permission, not a mandate; weak or
    redundant paragraphs remain forbidden; silence-is-not-valid, no-fabrication, within-noise, and
    flat-trend guards all still apply. Keep both occurrences worded consistently.

    (B) Vocabulary audit (D-10): read the concepts accordion + the five tooltip bodies +
    benchmarks-latest.md, then correct every prompt term/sign-convention that drifts from what the
    user sees. Minimum fixes: ensure the prompt uses the exact accordion labels (e.g. "Endgame Type"
    not "Endgame Class" in user-facing narration vocabulary; "Eval Score Gap", "Endgame Score Gap",
    "Entry Eval", "Percentile Badges"/"percentile" framing) and that every metric's
    higher-is-better / sign convention in the glossary matches its tooltip body. Where the tooltip
    explains a term the prompt narrates, make the prompt's wording consistent with the tooltip so the
    report and hover-help never contradict. Record the audit findings (which terms checked, which
    fixed) in the Plan 02 SUMMARY. Do NOT introduce jargon the tooltips avoid (no "Wilson",
    "sigmoid", raw CI bounds in narration — feedback_popover_copy_minimalism).
  </action>
  <verify>
    <automated>grep -c "~300 words" app/prompts/endgame_insights.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "~300 words" app/prompts/endgame_insights.md` returns 0 (the old flat cap is gone from
      both occurrences).
    - `grep -niE "up to ~?500 words|up to 5 (short )?paragraphs" app/prompts/endgame_insights.md`
      returns ≥1 AND `grep -ni "≥ ?3|at least 3|three distinct" app/prompts/endgame_insights.md`
      shows the signal gate.
    - The four guards survive: `grep -ni "silence is not" app/prompts/endgame_insights.md`,
      `grep -ni "within-noise" app/prompts/endgame_insights.md`, and the no-fabrication / flat-trend
      rules each still return hits.
    - `grep -c "Endgame Class" app/prompts/endgame_insights.md` is 0 in user-facing narration
      vocabulary (internal identifier references like `endgame_class` in `dimension={...}` payload
      shape may remain — those are payload keys, not narration terms).
    - The Plan 02 SUMMARY lists the audited terms (concepts accordion + the 5 tooltip bodies) and
      the corrections applied.
  </acceptance_criteria>
  <done>The overview cap is a signal-gated ≤500-word ceiling with every guard preserved; prompt vocabulary + sign conventions match the concepts accordion and the five tooltip popover bodies; audit recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Bump _PROMPT_VERSION to endgame_v36</name>
  <files>app/services/insights_llm.py, tests/services/test_insights_llm.py</files>
  <read_first>
    - app/services/insights_llm.py `_PROMPT_VERSION` (line 69) — the inline comment carries the full
      per-version changelog; v36 prepends a new entry in the same style (102-PATTERNS.md
      §"_PROMPT_VERSION bump").
    - 102-CONTEXT.md D-09 (bump v35 → v36, automatic cache invalidation, no retroactive purge) and
      LLM-06 (backwards compat preserved by the cache-key mechanism).
    - tests/services/test_insights_llm.py — check whether any test asserts the version string; if so
      it must be updated to endgame_v36.
  </read_first>
  <action>
    Change `_PROMPT_VERSION = "endgame_v35"` to `"endgame_v36"`. Prepend a `v36 (YYYYMMDD Phase 102
    statistical-reasoning rework): …` entry to the BEGINNING of the existing inline comment,
    preserving the full prior history. The rationale must capture, in the established terse style:
    percentile annotations added (page-level game-count-weighted per metric; per-TC TPCTL direct,
    no weighting) under the zone-as-sole-gate rule; time-pressure narration wired — Score-Gap-by-time
    per-quintile (5 quintiles) chart restored to the payload, Net Flag Rate now a real scalar (was an
    empty stub), Clock Gap unchanged; stale time_pressure_vs_performance / [low-time-gap] references
    purged from the prompt; overview cap relaxed to ≤500 words / ≤5 paragraphs when ≥3 distinct
    signals exist; vocabulary audit pass against the concepts accordion + tooltip popovers. Note
    cache invalidation is automatic via the version cache key; no retroactive purge. Update any test
    asserting the version string to `endgame_v36`.
  </action>
  <verify>
    <automated>grep -c "endgame_v36" app/services/insights_llm.py</automated>
    <automated>uv run pytest tests/services/test_insights_llm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'endgame_v35"' app/services/insights_llm.py` returns 0 for the constant assignment
      (the string still appears inside the changelog comment history, which is fine — verify the
      ASSIGNMENT line reads `endgame_v36`).
    - `grep -n 'v36 (.*Phase 102' app/services/insights_llm.py` returns a hit (changelog entry
      prepended).
    - `uv run pytest tests/services/test_insights_llm.py -x -q` exits 0 (any version-asserting test
      updated).
    - `uv run ty check app/ tests/` exits 0.
  </acceptance_criteria>
  <done>_PROMPT_VERSION is endgame_v36 with a prepended v36 changelog entry; the prior history is intact; tests green; cache invalidates exactly once on deploy.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols / strings introduced by this plan:
- `_PROMPT_VERSION = "endgame_v36"` (was `endgame_v35`) + a prepended `v36 (… Phase 102 …)` changelog entry.
- New prompt sections in `endgame_insights.md`: a "Percentile annotations" teaching block (with the
  zone-as-sole-gate rule and cohort framing), a rewritten time-pressure teaching block referencing
  `### Chart: time_pressure_score_gap_by_time`, and a relaxed signal-gated overview cap.
- Removal artifact: zero remaining `time_pressure_vs_performance` / `[low-time-gap]` references.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system prompt → LLM | static prompt text; no user input; output handling unchanged |
| _PROMPT_VERSION → cache key | server-controlled constant; bump triggers normal cache invalidation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Tampering | endgame_insights.md prompt text | accept | Static server-side prompt; no untrusted input is interpolated into it. |
| T-102-05 | Denial of Service | relaxed overview cap (≤500 words) | accept | The ceiling is bounded and signal-gated; output-token cost rises modestly, well within existing _OUTPUT_RETRIES/model limits. No unbounded growth. |
| T-102-06 | Repudiation | version bump cache behavior | accept | Bump only invalidates forward via the existing cache-key mechanism (LLM-06); prior reports are not retroactively altered. |
| T-102-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. If an executor proposes one, stop and flag. |
</threat_model>

<verification>
- `grep -c "time_pressure_vs_performance\|low-time-gap" app/prompts/endgame_insights.md` == 0.
- `grep -c "~300 words" app/prompts/endgame_insights.md` == 0.
- `grep -c "endgame_v36" app/services/insights_llm.py` ≥ 1 (assignment + changelog).
- `uv run pytest tests/services/test_insights_llm.py -x -q` exits 0.
- `uv run ty check app/ tests/` exits 0; `uv run ruff check app/ tests/` clean.
</verification>

<success_criteria>
LLM-02 (prompt teaches percentile reasoning under the zone gate, no re-licensed small-but-significant
pattern), LLM-03 (explicit "extreme pctl in typical zone does not narrate" rule present), LLM-04
(Score-Gap-by-time chart + real Net Flag Rate taught; stale chart refs purged), LLM-05 (cohort framing
taught), LLM-06 (clean single version bump, automatic cache invalidation, no retroactive purge) all
satisfied at the prompt layer.
</success_criteria>

<output>
Create `.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-02-SUMMARY.md` when done — include the D-10 vocabulary-audit record (terms checked across the concepts accordion + 5 tooltip bodies, corrections applied).
</output>
