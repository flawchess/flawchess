---
phase: 102-endgame-llm-statistical-reasoning-rework-v1-23
plan: 03
type: execute
wave: 3
depends_on: ["102-01", "102-02"]
files_modified:
  - .planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-HUMAN-UAT.md
autonomous: false
requirements: [LLM-07]
must_haves:
  truths:
    - "A representative sample of real production users generates the new endgame_v36 report"
    - "Each report references the percentile rank the user sees on the page, with cohort framing"
    - "Each report narrates time pressure (Score Gap by Remaining Time / Clock Gap / Net Flag Rate) when the zone opens the gate"
    - "Longer Data Analysis narration fires only when ≥3 distinct signals exist; sparse users stay concise"
    - "The report vocabulary never contradicts the concepts accordion or the tooltip hover-help"
  artifacts:
    - path: ".planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-HUMAN-UAT.md"
      provides: "UAT scenario checklist + recorded results across short-history / sparse-section / full-history users"
      contains: "endgame_v36"
  key_links:
    - from: "real prod user (admin impersonation)"
      to: "POST /api/insights/endgame returning an endgame_v36 report"
      via: "generate report in the running app"
      pattern: "endgame_v36"
---

<objective>
LLM-07 is the PRIMARY verification for this phase — it is UAT-dominated. Plans 01-02 are
payload + prompt code with automated payload-shape tests; only a human reading real reports can
confirm the rework lands an observable narration improvement and does not regress (over-narration
on sparse users, percentile/zone contradictions, vocabulary drift vs the hover-help).

This plan is a structured HUMAN-UAT checkpoint, not autonomous work. It defines representative
scenarios, has the user generate `endgame_v36` reports for real production users, and records
pass/fail per scenario. It is explicitly NOT gated on a dev-DB reset (project rule
`feedback_no_dev_db_reset_in_plans`) — UAT runs against existing prod data via admin impersonation.

Purpose: confirm the narration improvement is real and the guards hold before the phase ships.
Output: a completed 102-HUMAN-UAT.md with recorded results; any gaps feed a follow-up gap-closure pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-CONTEXT.md
@.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-01-SUMMARY.md
@.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the UAT scenario scaffold + select representative users</name>
  <files>.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-HUMAN-UAT.md</files>
  <read_first>
    - 102-CONTEXT.md D-03/D-04/D-05/D-06/D-08/D-10 — the behaviors UAT must confirm.
    - .planning/phases/102-…/102-01-SUMMARY.md + 102-02-SUMMARY.md — the exact payload fields,
      chart name, pctl token shape, and prompt sections that shipped (so the checklist asserts the
      real artifacts).
    - CLAUDE.md "Production Server" + "Database Access (MCP)" — admin impersonation exists; the
      prod read-only MCP (`flawchess-prod-db`) via `bin/prod_db_tunnel.sh` can identify candidate
      users by history depth and section sparsity. Do NOT reset any DB.
    - feedback_benchmark_source_of_truth — when judging "typical" framing, check
      reports/benchmark/benchmarks-latest.md.
  </read_first>
  <action>
    Create `102-HUMAN-UAT.md` with three user cohorts (the representative sample from LLM-07):
    (1) short-history user (few weekly buckets), (2) sparse-section user (some endgame sections
    have thin/empty data, e.g. little time-pressure data), (3) full-history user (rich data across
    all sections). For each cohort, propose 1-2 concrete candidate production users — identify them
    via the read-only prod MCP (query game counts / section coverage), NOT by mutating data. List
    each candidate's user_id + why they fit the cohort.

    For each scenario, write an explicit checklist of pass criteria tied to the locked decisions:
    - Percentile (D-03/D-05): the report references the percentile rank shown on the page, using
      cohort framing ("vs ~{anchor}-rated {tc} players"), never global-pool; the pctl the report
      cites matches the chip on the page.
    - Zone-as-gate (D-04): no metric is narrated solely because its percentile is extreme while its
      zone is typical; spot-check at least one typical-zone-extreme-pctl metric is NOT narrated.
    - Time pressure (D-06): when the zone opens it, the report narrates Score Gap by Remaining Time
      (low-clock quintiles), Clock Gap, and/or Net Flag Rate; Net Flag Rate is actually mentioned for
      a user who has it (regression check — it was invisible pre-phase).
    - Overview length (D-08): the full-history (≥3-signal) user MAY get a longer Data Analysis card;
      the sparse user stays concise and does NOT pad. Explicitly flag any padded/redundant paragraph.
    - Vocabulary (D-10): the report's terms + sign conventions match the concepts accordion and the
      tooltip hover-help; no contradiction (e.g. report says "higher is better" where the tooltip says
      the same).
    Add a results table (scenario | user_id | pass/fail | notes) for the user to fill in, and a
    "How to run" section (impersonate user → open Endgames tab → generate insights → confirm the
    report is endgame_v36, e.g. via the llm_logs prompt_version or the cache key).
  </action>
  <verify>
    <automated>test -f .planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-HUMAN-UAT.md && grep -c "endgame_v36" .planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 102-HUMAN-UAT.md exists and names three cohorts (short-history, sparse-section, full-history)
      each with ≥1 concrete candidate prod user_id and a rationale.
    - The checklist explicitly covers D-03, D-04, D-05, D-06, D-08, D-10 (one criterion each, grep
      the doc for each decision's behavior).
    - `grep -c "endgame_v36" …/102-HUMAN-UAT.md` ≥ 1 (the doc tells the verifier to confirm the
      report version).
    - The doc states UAT runs against existing prod data via impersonation and does NOT require a
      dev-DB reset.
  </acceptance_criteria>
  <done>102-HUMAN-UAT.md exists with three cohorts, concrete candidate users, decision-tied pass criteria, an empty results table, and a no-DB-reset run procedure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN-UAT — generate and judge endgame_v36 reports</name>
  <files>.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-HUMAN-UAT.md</files>
  <action>
    This is a blocking human-verification checkpoint, not autonomous work. Pause and have the user
    follow the `<how-to-verify>` steps below: generate endgame_v36 insights reports for the three
    candidate cohorts selected in Task 1 (via admin impersonation against existing prod data, no
    dev-DB reset), judge each report against the decision-tied checklist in 102-HUMAN-UAT.md, and
    record pass/fail + notes in the results table. Do not proceed past this checkpoint until the
    user replies per the resume signal.
  </action>
  <what-built>
    Plans 01-02 shipped the endgame_v36 payload + prompt: real Net Flag Rate, a 5-quintile
    Score-Gap-by-time chart, cohort-framed pctl= annotations, a signal-gated longer overview, and a
    vocabulary-audited prompt. Task 1 built the scenario checklist in 102-HUMAN-UAT.md.
  </what-built>
  <how-to-verify>
    1. Ensure the running app serves the new prompt (the backend has the endgame_v36 build; for prod
       this is post-deploy, for local it is the current working tree with the dev DB already running —
       no reset).
    2. For each of the three cohorts in 102-HUMAN-UAT.md, impersonate the candidate prod user, open
       the Endgames tab, and generate the endgame insights report. Confirm the report is endgame_v36
       (check llm_logs.prompt_version or force a fresh generation past the cache).
    3. Walk the per-scenario checklist: percentile cohort framing matches the chip; zone-as-gate
       holds (no typical-zone-extreme-pctl narration); time pressure narrates Score-Gap-by-time /
       Clock Gap / Net Flag Rate when the zone opens; the full-history user may get a longer Data
       Analysis card while the sparse user stays concise (flag any padding); report vocabulary never
       contradicts the concepts accordion or the tooltip hover-help.
    4. Record pass/fail + notes in the 102-HUMAN-UAT.md results table for every scenario.
  </how-to-verify>
  <verify>
    <human-check>All three cohort scenarios recorded pass in the 102-HUMAN-UAT.md results table; any failure is listed for a --gaps closure pass.</human-check>
  </verify>
  <done>The human has generated endgame_v36 reports for all three cohorts and recorded a pass/fail verdict per scenario in 102-HUMAN-UAT.md.</done>
  <resume-signal>
    Reply "approved" if all scenarios pass, or list the specific failures (which scenario, which
    decision criterion, what the report said wrong). Failures become a `/gsd-plan-phase 102 --gaps`
    closure pass; do not mark the phase complete with open UAT failures.
  </resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `102-HUMAN-UAT.md` — the completed UAT record (three cohorts, candidate prod users, decision-tied
  pass criteria, filled results table). No new code symbols (this plan is verification-only).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin impersonation → real user data | existing FastAPI-Users admin impersonation; read-only for UAT; no data mutation |
| prod read-only MCP → candidate selection | `flawchess-prod-db` is a read-only role; selection queries cannot mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-07 | Information Disclosure | impersonating real users for UAT | accept | Admin impersonation is an existing, audited capability used for prior LLM UAT (endgame_v23→v35 cadence); no new exposure. UAT is read-only report generation. |
| T-102-08 | Tampering | UAT must not mutate prod/dev data | mitigate | Candidate selection uses the read-only prod MCP; the plan forbids any dev-DB reset; report generation is the normal user-facing path. |
| T-102-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- 102-HUMAN-UAT.md exists, covers three cohorts and all six load-bearing decisions, and references
  endgame_v36.
- The human checkpoint is completed with a pass/fail recorded per scenario.
- Any failure routes to a `--gaps` closure pass; the phase is not marked complete with open failures.
</verification>

<success_criteria>
LLM-07 satisfied: a representative sample of real production users (short-history, sparse-section,
full-history) generated endgame_v36 reports, a human judged each against the locked-decision
criteria, and results are recorded. The narration improvement is observable and the guards
(zone-as-gate, no padding on sparse users, vocabulary consistency) hold.
</success_criteria>

<output>
Append the completed results table to 102-HUMAN-UAT.md and create
`.planning/phases/102-endgame-llm-statistical-reasoning-rework-v1-23/102-03-SUMMARY.md` recording the UAT outcome.
</output>
