---
phase: 125-backfill-tactic-motifs
plan: 03
type: execute
wave: 2
depends_on: ["125-02"]
files_modified:
  - .planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md
autonomous: true
requirements: [TACSCH-03]

must_haves:
  truths:
    - "A prod runbook documents the exact command sequence to backfill prod: bring up bin/prod_db_tunnel.sh, dry-run smoke, full backfill --db prod --full-evald-only, coverage report --db prod, close tunnel"
    - "The runbook records expected scale (~131k full-eval'd games), the let-it-rip concurrency posture (D-03), and the BACKFILL_GAMES_PER_BATCH=100 batching mandate"
    - "The runbook lists prod verification queries (coverage report sections + expected ~81% no-PV ratio mirroring dev) so the operator can confirm honest coverage on prod"
    - "The runbook states prod execution is a DEFERRED operational step outside the Phase 125 completion gate (D-01)"
  artifacts:
    - path: ".planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md"
      provides: "Self-contained, copy-pasteable prod backfill runbook for the deferred prod run"
      contains: "prod_db_tunnel"
  key_links:
    - from: ".planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md"
      to: "scripts/backfill_flaws.py"
      via: "documents the exact --db prod --full-evald-only invocation"
      pattern: "backfill_flaws.py"
    - from: ".planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md"
      to: "scripts/coverage_report_tactic_motifs.py"
      via: "documents the --db prod verification step"
      pattern: "coverage_report_tactic_motifs.py"
---

<objective>
Produce the deferred-prod runbook: a self-contained, copy-pasteable document an operator can
follow later to backfill prod tactic motifs and verify honest coverage, using the same tooling
and posture proven on dev in Plan 02.

Purpose: D-01 — Phase 125 completes on dev; the prod run is a later, low-risk operational step.
The deliverable is a documentation artifact, NOT an executable task. It captures the exact
commands, expected load, batching mandate, and verification queries so the prod run needs no
re-derivation.

Output: `.planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-backfill-tactic-motifs/125-CONTEXT.md
@.planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md
@.planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PROD-RUNBOOK.md for the deferred prod tactic-motif backfill (D-01)</name>
  <files>.planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md "Prod Runbook Inputs" section (DB target localhost:15432 via bin/prod_db_tunnel.sh; ~131k full-eval'd games; BACKFILL_GAMES_PER_BATCH=100; let-it-rip posture; the runbook template) and "Pitfall 5" (tunnel must be up before --db prod)
    - .planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md (the OBSERVED dev numbers: overall non-NULL %, by-motif distribution, no-PV ratio, run time — use these to set prod expectations)
    - scripts/backfill_eval.py lines 1-52 (the prod-tunnel runbook docstring precedent — three-round dev/benchmark/prod structure, STOCKFISH_PATH note is N/A here since no Stockfish)
    - scripts/backfill_flaws.py lines 1-28 (DB target host:port mapping; usage block)
    - CLAUDE.md "Production Server" + "Database Access (MCP)" (bin/prod_db_tunnel.sh forwards localhost:15432 -> prod; stop with `bin/prod_db_tunnel.sh stop`; DATABASE_URL_PROD must be in .env)
    - .planning/phases/125-backfill-tactic-motifs/125-CONTEXT.md D-01, D-03
  </read_first>
  <action>
    Write `.planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md` as a self-contained operator runbook. Use the RESEARCH "Prod Runbook Inputs" template as the skeleton and fill it with the ACTUAL dev observations from the Plan 02 SUMMARY (do not leave placeholder numbers). Sections:

    1. Status banner: "DEFERRED — prod execution is OUTSIDE the Phase 125 completion gate (D-01). Phase 125 is complete on dev; run this when ready."
    2. Prerequisites: SSH tunnel up via `bin/prod_db_tunnel.sh`; verify with a read-only count query against `localhost:15432`; confirm `.env` has `DATABASE_URL_PROD`; confirm `flawchess` SSH host reachable.
    3. Commands (copy-pasteable, in order): (a) `bin/prod_db_tunnel.sh`, (b) dry-run smoke `uv run python scripts/backfill_flaws.py --db prod --full-evald-only --dry-run --limit 50`, (c) full run `uv run python scripts/backfill_flaws.py --db prod --full-evald-only`, (d) coverage report `uv run python scripts/coverage_report_tactic_motifs.py --db prod`, (e) `bin/prod_db_tunnel.sh stop`.
    4. Expected load & posture: ~131k full-eval'd games (cite the architecture-note date); let-it-rip (D-03 — no Stockfish, pure CPU, benign race with the live eval drain); BACKFILL_GAMES_PER_BATCH=100 is the OOM-safe default — only tune if a prod soak shows pressure; rough run-time estimate extrapolated from the dev run time recorded in Plan 02 (state it as an estimate).
    5. Verification: the coverage report's four sections; expected ~81% no-PV ratio mirroring dev (state this is a ratio expectation, not a hard gate — the bar is "NULL = honest, not skipped"); the by-motif distribution should roughly track dev's shape. Idempotency note: safe to re-run (delete-then-insert).
    6. Rollback / safety note: the backfill is idempotent and scoped to (game_id, user_id); a partial/interrupted run is resumable by simply re-running (ordered by game id). No destructive operation beyond the per-game delete-then-insert recompute.

    Keep prose terse and user-facing (CLAUDE.md style: sparing em-dashes). This is documentation only — do NOT execute any prod command in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md && grep -q 'prod_db_tunnel' .planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md && grep -q 'backfill_flaws.py --db prod --full-evald-only' .planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md && grep -q 'coverage_report_tactic_motifs.py --db prod' .planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md && grep -qi 'defer' .planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - PROD-RUNBOOK.md exists and contains all five command-sequence steps (tunnel up, dry-run smoke, full run, coverage report, tunnel stop) in copy-pasteable form.
    - It references `bin/prod_db_tunnel.sh`, the exact `backfill_flaws.py --db prod --full-evald-only` invocation, and the `coverage_report_tactic_motifs.py --db prod` verification.
    - It explicitly states prod execution is DEFERRED / outside the Phase 125 completion gate (D-01).
    - Expected scale (~131k), batching mandate (BACKFILL_GAMES_PER_BATCH=100), and let-it-rip posture (D-03) are documented, and the expected numbers are the ACTUAL dev observations from the Plan 02 SUMMARY (no placeholder values).
  </acceptance_criteria>
  <done>A self-contained, copy-pasteable prod runbook exists with the exact commands, expected load, batching/concurrency posture, and verification queries; prod execution is clearly marked deferred (D-01).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates `.planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md` (NEW doc). No
source symbols. The phase-wide net-new artifact list lives in Plan 01's
`<artifacts_this_phase_produces>`.
</artifacts_this_phase_produces>

<verification>
- PROD-RUNBOOK.md exists with the five-step command sequence, deferred-status banner, expected
  scale/posture, and verification queries (the automated grep chain passes).
</verification>

<success_criteria>
The deferred prod step is fully documented (D-01): an operator can backfill prod and verify
honest coverage by following the runbook, with no re-derivation. Phase 125 completion criteria
(dev backfill + honest coverage + idempotency + prod runbook) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/125-backfill-tactic-motifs/125-03-SUMMARY.md` when done.
</output>