---
phase: 133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tactic_detector.py
autonomous: true
requirements: []   # none mapped; traceability via the investigation-note locked decisions (Group-A geometry fixes)
nyquist_compliant: true

must_haves:
  truths:
    - "attraction detector fires on its TRAIN fixture rows (was 0/1603 → now ~1603/1603 TP) with 0 FP"
    - "arabian-mate detector fires on its TRAIN fixture rows (was 0/553 → now ~553/553 TP) with 0 FP"
    - "boden-mate detector fires on its TRAIN fixture rows (was 0/437 → now ~435/437 TP) with 0 FP"
    - "dovetail-mate detector fires only on true positives (was 0 TP / 23 FP → now ~543/544 TP, 0 FP)"
    - "no shipped motif regresses below its existing PRECISION_FLOOR after these edits"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "Corrected attraction cond-5 board index + cook-faithful arabian/boden/dovetail mate geometry"
      contains: "boards[k + 3]"
  key_links:
    - from: "app/services/tactic_detector.py::_attraction_fires_at"
      to: "tests/scripts/tagger/test_detector_precision.py"
      via: "harness scores attraction TRAIN precision/recall"
      pattern: "attraction"
    - from: "app/services/tactic_detector.py::detect_arabian_mate"
      to: "tests/scripts/tagger/test_detector_precision.py"
      via: "harness scores arabian-mate TRAIN precision/recall"
      pattern: "arabian"
---

<objective>
Apply the four empirically-validated detector code fixes (RESEARCH §"Solved Items" attraction + §"Mate Geometry Ports") to `app/services/tactic_detector.py`:
1. attraction: one-line off-by-one — the cond-5 attacker board `boards[k+2]` → `boards[k+3]`.
2. arabian-mate: replace the hypothetical-knight-on-king-square check with cook's "pov attackers of the rook square that are knights at (rank_diff==2, file_diff==2) from the king".
3. boden-mate: replace the "both bishops attack king" filter with cook's "all pov attackers of every square within distance < 2 of the king must be bishops".
4. dovetail-mate: invert the inverted queen-adjacency reject, add the same-file/same-rank reject, the distance≤1 require, and cook's adjacent-square escape loop.

Purpose: These four detectors currently never fire correctly (attraction/arabian/boden never fire; dovetail fires only false positives). RESEARCH probed the corrected predicates to ~1.000 precision against the committed CC0 TRAIN fixture. This plan is the detector-code half; Plan 02 unsuppresses and surfaces them.

Output: Edited `app/services/tactic_detector.py` with no other behavior changed. The harness must show attraction/arabian/boden/dovetail with non-NaN, near-1.000 TRAIN precision and no regression on any already-shipped motif.

AGPL boundary (131 D-10): reimplement cook's predicates from the prose/pseudocode in 133-RESEARCH.md and the cook-alignment note. Copy NO source lines from cook.py. The pseudocode in RESEARCH is original-prose paraphrase; treat it as a spec, not as text to paste.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp/133-RESEARCH.md
@.planning/notes/suppressed-tactic-gaps-investigation.md
@.planning/notes/tactic-tagger-cook-alignment.md
</context>

<artifacts_this_phase_produces>
This plan produces NO new exported symbols. It edits the bodies of four existing
functions in `app/services/tactic_detector.py`:
- `_attraction_fires_at` (cond-5 board index)
- `detect_arabian_mate` (knight geometry)
- `detect_boden_or_double_bishop_mate` (bishop-only near-king attacker check)
- `detect_dovetail_mate` (queen adjacency + escape-square loop)
Public signatures, return tuples, and the `TacticMotifInt` enum are unchanged.
New PRECISION_FLOOR keys, family-map keys, and frontend entries are added in Plan 02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fix attraction cond-5 off-by-one (boards[k+2] → boards[k+3])</name>
  <read_first>
    - app/services/tactic_detector.py lines 1491-1549 (`_attraction_fires_at`) — the cond-5 attacker check is at the `board_k2 = boards[k + 2]` / `attackers(pov, attracted_to_sq)` lines (~1534-1536).
    - 133-RESEARCH.md §"Attraction: one-line off-by-one fix" — cook uses `next_node.board()` = board AFTER pov's follow-up = our `boards[k+3]`.
    - suppressed-tactic-gaps-investigation.md §attraction — root-cause prose.
  </read_first>
  <files>app/services/tactic_detector.py</files>
  <action>
    In `_attraction_fires_at`, the cond-5 check reads the attacker board as `boards[k + 2]` (the board BEFORE pov's follow-up move, where pov's piece is still on its origin square so it is never in `attackers(pov, attracted_to_sq)`). Change ONLY the attacker board read to `boards[k + 3]` (the board AFTER pov's follow-up move) per the investigation-note locked decision. The KING short-circuit (cond-9) and the k+4 queen/rook branch (cond-10) are unaffected and must NOT change.
    Update the local variable name from `board_k2` to `board_k3` (and its single use) so the code reads honestly. Update the docstring index-convention comment that currently says `boards[k+2] = board AFTER opponent captures (read pov's attackers here)` to reflect that the attacker check now reads `boards[k+3]` (board after pov's follow-up). Reference: investigation note prose, "off-by-one: boards[k+2] should be boards[k+3]; fixed in Phase 133".
    Bounds: the existing guard `if k + 2 >= len(moves): return None` already guarantees `k + 3 <= len(moves)`, which is a valid index into `boards` (len `len(moves)+1`). Confirm this holds; do NOT add a redundant guard unless the harness shows an IndexError.
    Do NOT touch `detect_attraction` (the loop wrapper) — only `_attraction_fires_at`.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py -s 2>&1 | grep -E "attraction"</automated>
  </verify>
  <acceptance_criteria>
    - The harness TRAIN table shows `attraction` with TP > 0 (RESEARCH expects ~1603 TP) and FP == 0 → TRAIN precision == 1.000 (or ≥ 0.93). No longer NaN / 0 TP.
    - `attraction` TEST precision is printed (held-out, not gated) and is high (RESEARCH: 677/677 TP standalone).
    - The string `boards[k + 3]` appears in `_attraction_fires_at`; the cond-5 attacker board is no longer `boards[k + 2]`.
    - `detect_attraction` is unchanged; the KING short-circuit and k+4 Q/R branch are unchanged.
  </acceptance_criteria>
  <done>attraction fires on its fixture rows at ~1.000 TRAIN precision; the single board-index edit is the only behavioral change in the function.</done>
</task>

<task type="auto">
  <name>Task 2: Port cook arabian-mate knight geometry</name>
  <read_first>
    - app/services/tactic_detector.py lines 1254-1292 (`detect_arabian_mate`) — the current knight check is the `chess.BB_KNIGHT_ATTACKS[opp_king_sq]` loop at ~1286-1290.
    - 133-RESEARCH.md §"1. Arabian-Mate" — full predicate + port guidance + the y7IDV worked example (king=h8, rook g8, knight f6).
  </read_first>
  <files>app/services/tactic_detector.py</files>
  <action>
    Reimplement the knight check in `detect_arabian_mate` from cook's prose (AGPL boundary — no source copied). Keep the corner-king check, the last-move-is-rook check, and the rook-adjacent-to-king check (`square_distance == 1`, currently expressed as `rook_sq in boards[-1].attacks(opp_king_sq)`) exactly as they are.
    Replace ONLY the knight loop (the `knights = boards[-1].pieces(...)` + `BB_KNIGHT_ATTACKS` block). New logic: iterate over `boards[-1].attackers(pov, rook_sq)` (pov pieces that actually attack the rook's landing square); for each, if `boards[-1].piece_at(sq)` is a KNIGHT and `abs(square_rank(sq) - square_rank(opp_king_sq)) == 2` and `abs(square_file(sq) - square_file(opp_king_sq)) == 2`, return `True, chess.ROOK, len(moves) - 1`. If none match, return `False, None, None`.
    Update the function docstring to describe the cook-faithful geometry (pov knight attacks the rook square AND is exactly (2,2) rank/file from the king), citing "Phase 133 cook arabian-mate port".
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py -s 2>&1 | grep -E "arabian-mate"</automated>
  </verify>
  <acceptance_criteria>
    - The harness TRAIN table shows `arabian-mate` with TP > 0 (RESEARCH expects ~553 TP) and FP == 0 → TRAIN precision == 1.000. No longer NaN / never-fires.
    - The function reads `boards[-1].attackers(pov, rook_sq)` and checks `(rank_diff == 2, file_diff == 2)`; `chess.BB_KNIGHT_ATTACKS` is no longer used in `detect_arabian_mate`.
  </acceptance_criteria>
  <done>arabian-mate fires at ~1.000 TRAIN precision via the cook attacker-of-rook-square knight geometry.</done>
</task>

<task type="auto">
  <name>Task 3: Port cook boden/double-bishop near-king bishop-only check</name>
  <read_first>
    - app/services/tactic_detector.py lines 1295-1328 (`detect_boden_or_double_bishop_mate`) — the current `attacking_bishops` filter at ~1312-1317.
    - 133-RESEARCH.md §"2. Boden-Mate / Double-Bishop-Mate" — full predicate + port guidance + the c3LlW worked example (king=c1, bishops a3/g6).
  </read_first>
  <files>app/services/tactic_detector.py</files>
  <action>
    Reimplement the near-king attacker test in `detect_boden_or_double_bishop_mate` from cook's prose (AGPL boundary — no source copied). Keep the `is_checkmate()` guard, the `opp_king_sq` lookup, the `pov_bishops` collection, and the existing boden-vs-double-bishop classification by king-file side (lines ~1319-1328) unchanged.
    Replace ONLY the `attacking_bishops` filter and its `< 2` early return. New logic: require at least two pov bishops on the board (`len(pov_bishops) < 2 → return None, None, None`). Then, for every square `sq` in `chess.SQUARES` with `chess.square_distance(sq, opp_king_sq) < 2` (the king plus its 8 neighbours), iterate `boards[-1].attackers(pov, sq)`; if any attacker's `piece_at` is None or not a BISHOP, `return None, None, None`. If the loop completes, fall through to the existing classification block.
    Update the docstring to describe the cook-faithful check (all pov attackers of every square within distance < 2 of the king must be bishops), citing "Phase 133 cook boden-mate port".
    NOTE: this is region-scoped — `double-bishop-mate` (int 23) stays unvalidated/unsuppressed-as-no-floor; only `boden-mate` (int 22) gets a floor in Plan 02. The classification block already returns both strings; do not change that.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py -s 2>&1 | grep -E "boden-mate"</automated>
  </verify>
  <acceptance_criteria>
    - The harness TRAIN table shows `boden-mate` with TP > 0 (RESEARCH expects ~435 TP) and FP == 0 → TRAIN precision == 1.000 (RESEARCH notes ≤2 FN that classify as double-bishop-mate, which are NOT boden FPs).
    - The function iterates `chess.SQUARES` with `square_distance(sq, opp_king_sq) < 2` and rejects any non-bishop pov attacker; the old `attacking_bishops = [... opp_king_sq in boards[-1].attacks(sq)]` filter is gone.
  </acceptance_criteria>
  <done>boden-mate fires at ~1.000 TRAIN precision via the cook near-king bishop-only attacker check; classification logic untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Port cook dovetail-mate queen-adjacency + escape-square loop</name>
  <read_first>
    - app/services/tactic_detector.py lines 1331-1362 (`detect_dovetail_mate`) — the current inverted `if queen_sq in boards[-1].attacks(opp_king_sq): return False` at ~1357-1360.
    - 133-RESEARCH.md §"3. Dovetail-Mate" — both bugs (A: inverted adjacency reject; B: missing same-file/rank + distance check), the full predicate, the DrMhn FP example (king=f6, queen=h6, same rank distance 2) and the wsRgK TP example (king=b6, queen=a5 diagonal).
  </read_first>
  <files>app/services/tactic_detector.py</files>
  <action>
    Reimplement the body of `detect_dovetail_mate` from cook's prose (AGPL boundary — no source copied). Keep: the `is_checkmate()` + `moves` guard, the mating-piece-is-QUEEN check, the `opp_king_sq` lookup, and the king-not-on-edge check (king_file/king_rank not in 0/7) unchanged.
    Then REMOVE the current inverted reject `if queen_sq in boards[-1].attacks(opp_king_sq): return False, None, None` (Bug A). Replace it with cook's three new conditions, in order:
    1. Same-file/same-rank reject (diagonal-only adjacency): if `chess.square_file(queen_sq) == chess.square_file(opp_king_sq) or chess.square_rank(queen_sq) == chess.square_rank(opp_king_sq): return False, None, None`. (Bug B — this rejects the DrMhn same-rank FP.)
    2. Distance reject: if `chess.square_distance(queen_sq, opp_king_sq) > 1: return False, None, None`.
    3. Escape-square loop: for every `sq` in `chess.SQUARES` with `chess.square_distance(sq, opp_king_sq) == 1` and `sq != queen_sq`: let `attackers = list(boards[-1].attackers(pov, sq))`; if `attackers == [queen_sq]` and `boards[-1].piece_at(sq) is not None: return False, None, None`; elif `attackers: return False, None, None`.
    If the loop completes, `return True, chess.QUEEN, len(moves) - 1`.
    Update the docstring to describe the cook-faithful predicate (queen diagonally adjacent to a non-edge king; each remaining escape square either uncovered-or-queen-covered-and-empty), citing "Phase 133 cook dovetail-mate port (fixes inverted adjacency)".
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py -s 2>&1 | grep -E "dovetail-mate"</automated>
  </verify>
  <acceptance_criteria>
    - The harness TRAIN table shows `dovetail-mate` with TP > 0 (RESEARCH expects ~543 TP) and FP == 0 → TRAIN precision == 1.000 (was 0 TP / 23 FP). The 23 prior false positives are gone.
    - The inverted `if queen_sq in boards[-1].attacks(opp_king_sq): return False` is removed; the same-file/same-rank reject, the distance>1 reject, and the escape-square loop are present.
  </acceptance_criteria>
  <done>dovetail-mate fires only on true positives at ~1.000 TRAIN precision via the cook escape-square predicate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CC0 fixture → detector | Untrusted-shaped PGN/FEN strings parsed into chess.Board; already handled by existing harness parsing (`_parse_pv`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | detector geometry edits silently regress a shipped motif | mitigate | harness asserts every shipped motif's TRAIN floor on every run; `uv run pytest -n auto -x` full suite before merge |
| T-133-02 | Information disclosure | AGPL cook.py source accidentally copied | mitigate | reimplement from RESEARCH prose only; reviewer diff-checks against cook.py; 131 D-10 rule |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (pure source edits to one existing file) |
</threat_model>

<verification>
After all four tasks:
- `uv run pytest tests/scripts/tagger/test_detector_precision.py -s` — attraction, arabian-mate, boden-mate, dovetail-mate all show TP > 0 and FP == 0 on TRAIN; NO already-shipped motif drops below its existing PRECISION_FLOOR (the harness asserts this and will fail the run if any does).
- `uv run ruff format app/ && uv run ruff check app/ --fix && uv run ty check app/` — zero errors.
- The four motifs are still in SUPPRESSED_MOTIFS at this point (no floor assertion yet) — so the harness will NOT gate them here; their precision is read from the printed table. They are unsuppressed + floored in Plan 02.
</verification>

<success_criteria>
- attraction, arabian-mate, boden-mate, dovetail-mate each fire on their TRAIN fixture rows with 0 FP (near-1.000 precision), matching RESEARCH's probed numbers.
- No shipped motif regresses (full harness run green on existing floors).
- `uv run ty check app/` and `ruff` clean.
- No cook.py source copied (AGPL boundary respected).
</success_criteria>

<output>
Create `.planning/phases/133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp/133-01-SUMMARY.md` when done. Record the measured TRAIN precision/TP/FP for attraction, arabian-mate, boden-mate, dovetail-mate from the harness run — Plan 02 needs these numbers to set the floors ~5-8pp below the measured TRAIN value.
</output>
