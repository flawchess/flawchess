---
phase: 135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se
plan: 03
type: execute
wave: 2
depends_on:
  - 135-01
  - 135-02
files_modified:
  - frontend/src/types/library.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/components/library/SanLadder.tsx
  - frontend/src/components/library/TacticLineExplorer.tsx
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx
  - frontend/src/components/library/__tests__/FlawCard.test.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Clicking Explore on a tagged flaw card opens the explorer (Dialog on desktop, Drawer on mobile per D-05) showing the missed line by default with the depth counter at the punchline"
    - "The Missed/Allowed toggle appears only when both lines exist; single-line flaws open the available line with the toggle hidden; switching resets the stepper to root"
    - "The SAN ladder labels move numbers from the real game ply (not 1), highlights the current ply, click-jumps, and colors the depth-0 punchline"
    - "Flaw cards show a dedicated button row (D-04): Explore+Game for tagged, Game-only for untagged, both brand-outline, on BOTH mobile and desktop"
    - "The game-card Explore button (D-03) is always visible, disabled with a tooltip when the parked position is not a tagged user flaw, and opens the explorer stacked over the Game modal returning to it on close (D-01)"
  artifacts:
    - path: "frontend/src/components/library/TacticLineExplorer.tsx"
      provides: "Dialog/Drawer explorer composing ChessBoard + BoardControls + SanLadder + toggle"
      contains: "TacticLineExplorer"
    - path: "frontend/src/components/library/SanLadder.tsx"
      provides: "linear real-ply-anchored SAN list with highlight + click-jump"
      contains: "SanLadder"
    - path: "frontend/src/hooks/useLibrary.ts"
      provides: "useTacticLines TanStack Query hook (lazy, enabled on open)"
      contains: "useTacticLines"
    - path: "frontend/src/api/client.ts"
      provides: "libraryApi.getTacticLines"
      contains: "getTacticLines"
    - path: "frontend/src/types/library.ts"
      provides: "TacticLinesResponse TS type"
      contains: "TacticLinesResponse"
    - path: "frontend/src/components/library/FlawCard.tsx"
      provides: "D-04 button row (Explore+Game / Game-only)"
      contains: "flaw-btn-explore"
    - path: "frontend/src/components/results/LibraryGameCard.tsx"
      provides: "D-03 Explore button wired to the selected flaw"
      contains: "game-card-btn-explore"
  key_links:
    - from: "frontend/src/components/library/TacticLineExplorer.tsx"
      to: "frontend/src/hooks/useTacticLines (useLibrary.ts)"
      via: "useTacticLines(gameId, ply, enabled=open) lazy fetch"
      pattern: "useTacticLines"
    - from: "frontend/src/components/library/TacticLineExplorer.tsx"
      to: "frontend/src/hooks/useTacticLine.ts"
      via: "useTacticLine(moves, rootFen, tacticDepthRaw, orientation)"
      pattern: "useTacticLine"
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "frontend/src/components/library/TacticLineExplorer.tsx"
      via: "Explore button opens TacticLineExplorer"
      pattern: "TacticLineExplorer"
    - from: "frontend/src/components/results/LibraryGameCard.tsx"
      to: "frontend/src/components/library/TacticLineExplorer.tsx"
      via: "game-card Explore opens TacticLineExplorer stacked over Game modal"
      pattern: "TacticLineExplorer"
---

<objective>
Assemble the Tactic Line Explorer and wire its two entry points: the data layer (`getTacticLines` client + `useTacticLines` query + `TacticLinesResponse` type), the `TacticLineExplorer` component (Dialog desktop / Drawer mobile per D-05, missed/allowed toggle, root + payoff arrows, depth readout) with its `SanLadder`, and the entry-point refactors — FlawCard button row (D-04) and LibraryGameCard Explore button (D-03 placement, D-02 disabled state, D-01 stacking/return-to-Game).

Purpose: This is the user-facing payload of the phase — it turns every tagged flaw into a walkable lesson. It composes Plan 01's endpoint and Plan 02's hook + board primitives. All locked decisions D-01..D-05 land here.
Output: data layer + explorer + ladder + both entry-point refactors + component/unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-RESEARCH.md
@.planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-PATTERNS.md
@.planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-UI-SPEC.md
@.planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-CONTEXT.md
</context>

<artifacts_produced>
This phase plan produces:
- `TacticLinesResponse` TypeScript type in `frontend/src/types/library.ts` (mirrors the backend Pydantic model from Plan 01).
- `libraryApi.getTacticLines(gameId, ply)` in `frontend/src/api/client.ts`.
- `useTacticLines(gameId, ply, enabled)` TanStack Query hook in `frontend/src/hooks/useLibrary.ts`.
- New component `SanLadder` (`frontend/src/components/library/SanLadder.tsx`) with props {moves, currentPly, tacticPlyIndex, orientation, flawPly, onGoToMove}.
- New component `TacticLineExplorer` (`frontend/src/components/library/TacticLineExplorer.tsx`) with props {open, onOpenChange, gameId, ply} composing ChessBoard + BoardControls + SanLadder + Missed/Allowed toggle; local `useIsMobile()` (MOBILE_BREAKPOINT_PX=768) switching Dialog vs Drawer.
- D-04 button row in `FlawCard.tsx` (state `exploreOpen`; testids `flaw-btn-explore`, `flaw-btn-game`).
- D-03 Explore button in `LibraryGameCard.tsx` (testid `game-card-btn-explore`; tracks the eval-chart's selected/parked flaw; disabled + tooltip when not a tagged user flaw).
- New data-testids: `tactic-explorer-dialog`, `tactic-explorer-drawer`, `tactic-explorer-board` (also `id="tactic-explorer-board"`), `tactic-toggle-missed`, `tactic-toggle-allowed`, `tactic-san-ladder`, `tactic-san-move-{ply}`, `tactic-depth-readout`, `flaw-btn-explore`, `flaw-btn-game`, `game-card-btn-explore`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Data layer — TacticLinesResponse type, getTacticLines client, useTacticLines query</name>
  <files>frontend/src/types/library.ts, frontend/src/api/client.ts, frontend/src/hooks/useLibrary.ts</files>
  <read_first>
    - frontend/src/types/library.ts (existing response types; TacticOrientation; FlawMarker shape)
    - frontend/src/api/client.ts (libraryApi.getGame lines 377-400 — GET-with-path-params analog; type import line 16)
    - frontend/src/hooks/useLibrary.ts (useLibraryGame lines 193-218 — lazy-fetch query with enabled guard + LIBRARY_STALE_TIME + refetchOnWindowFocus:false)
    - .planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-PATTERNS.md (exact client + hook snippets)
    - app/schemas/library.py (the backend TacticLinesResponse from Plan 01 — mirror field names/nullability exactly)
  </read_first>
  <action>
    Add `TacticLinesResponse` TS interface to frontend/src/types/library.ts mirroring the backend model field-for-field (missed_moves: string[] | null, missed_depth: number | null, missed_tactic_ply_index: number | null, missed_motif: string | null, allowed_* mirror, position_fen: string, flaw_move_san: string | null, best_move_uci: string | null, flaw_ply: number). Reuse the existing motif string type if one already exists in this file.

    Add `getTacticLines: (gameId: number, ply: number) => apiClient.get<TacticLinesResponse>(\`/library/flaws/${gameId}/${ply}/tactic-lines\`).then(r => r.data)` to the `libraryApi` object in client.ts; add TacticLinesResponse to the type import.

    Add `useTacticLines(gameId: number | null, ply: number | null, enabled: boolean)` to useLibrary.ts cloning useLibraryGame: queryKey ['tactic-lines', gameId, ply], queryFn calls libraryApi.getTacticLines(gameId!, ply!), `enabled: enabled && gameId != null && ply != null`, staleTime LIBRARY_STALE_TIME, refetchOnWindowFocus:false. The enabled-on-open gate ensures lazy fetch only when the explorer opens.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TacticLinesResponse" frontend/src/types/library.ts` and `grep -n "getTacticLines" frontend/src/api/client.ts` and `grep -n "useTacticLines" frontend/src/hooks/useLibrary.ts` all match.
    - `cd frontend && npx tsc -b` clean (type mirrors backend; consumed in Task 2/3).
    - `cd frontend && npm run lint` clean.
  </acceptance_criteria>
  <done>Frontend can lazy-fetch the tactic-lines endpoint with a typed response gated on explorer open.</done>
</task>

<task type="auto">
  <name>Task 2: SanLadder + TacticLineExplorer (Dialog/Drawer, toggle, arrows, depth readout) + component test</name>
  <files>frontend/src/components/library/SanLadder.tsx, frontend/src/components/library/TacticLineExplorer.tsx, frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx</files>
  <read_first>
    - .planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-UI-SPEC.md (FULL — layout desktop/mobile, arrow spec at root vs ply1+ vs payoff, depth readout copy, SAN ladder spec, toggle, testids, ARIA, empty/error/loading copy)
    - .planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-PATTERNS.md (TacticLineExplorer Dialog/Drawer/useIsMobile/arrow/toggle/BoardControls snippets; SanLadder shape + move-number computation + row-highlight)
    - frontend/src/hooks/useTacticLine.ts (Plan 02 hook — the stepper this composes)
    - frontend/src/components/board/ChessBoard.tsx (Plan 02 — id prop + labelled arrows)
    - frontend/src/components/board/BoardControls.tsx (infoSlot, size variants, back/forward/reset/flip)
    - frontend/src/components/library/FlawCard.tsx lines 442-477 (Dialog usage analog) + frontend/src/App.tsx lines 339-395 (MobileMoreDrawer analog) + frontend/src/components/ui/drawer.tsx (w-full / max-h override)
    - frontend/src/lib/sanToSquares.ts (uciToSquares, sanToSquares) + frontend/src/lib/theme.ts (TAC_MISSED/TAC_ALLOWED/BEST_MOVE_ARROW + label colors) + frontend/src/lib/tacticDepth.ts (toDisplayDepthForOrientation) + frontend/src/components/charts/ScoreChart.tsx lines 34-46 (useIsMobile pattern)
    - frontend/src/components/library/TacticMotifChip.tsx (header motif badge)
  </read_first>
  <action>
    Build SanLadder.tsx as a stateless display component (props per PATTERNS): linear `<ul role="list">` (no branch columns). Move numbers anchored to the real game ply via `flawPly` (full move number = ceil((flawPly + i + 1)/2), black suffix '...') — NOT restarted at 1. Each row `role="listitem"`, `data-testid="tactic-san-move-{flawPly + i}"`, `aria-current="step"` when it is the current ply, `onClick={() => onGoToMove(i + 1)}`. Current ply highlighted `border-l-2 border-brand-brown bg-brand-brown/10`. The depth-0 punchline move (index === tacticPlyIndex) colored via TAC_MISSED (missed) or TAC_ALLOWED (allowed) text; payoff moves (index > tacticPlyIndex) dimmed `text-muted-foreground`. Container `data-testid="tactic-san-ladder"`. text-sm floor (no text-xs).

    Build TacticLineExplorer.tsx (props {open, onOpenChange, gameId, ply}):
    - Define local `useIsMobile()` (MOBILE_BREAKPOINT_PX = 768 named constant) per ScoreChart pattern; render Drawer (direction="bottom", DrawerContent `w-full max-h-[95vh]`, testid `tactic-explorer-drawer`) on mobile, Dialog (DialogContent `no-scrollbar max-w-[calc(100%-1rem)] sm:max-w-4xl overflow-y-auto max-h-[90vh] sm:p-6`, testid `tactic-explorer-dialog`) on desktop. Title "Explore Tactic" (text-base font-semibold) with TacticMotifChip for the active orientation's motif.
    - Data: `const { data, isLoading, isError } = useTacticLines(gameId, ply, open)`. Handle isError → "Failed to load tactic line. Please try again." (CLAUDE.md isError branch). Loading → board skeleton + spinner, aria-label "Loading tactic line". Empty (active line null) → "Tactic line not available for this flaw."
    - Orientation state defaults to 'missed'; if only one of data.missed_moves / data.allowed_moves is non-null, force that orientation and HIDE the toggle (Pitfall 5 / Claude's-Discretion single-line). Toggle visible only when BOTH non-null. Toggle = two `variant="ghost"` Buttons, aria-pressed, testids tactic-toggle-missed / tactic-toggle-allowed; switching calls setOrientation + the hook's reset() (reset stepper to root — UI-SPEC).
    - Compose `useTacticLine({ moves: activeMoves, rootFen: data.position_fen, tacticDepthRaw: activeDepthRaw, orientation })`. activeMoves/activeDepthRaw pick missed vs allowed by orientation.
    - Arrows: at ply 0 build BOTH arrows from data.best_move_uci (blue, label = display depth via toDisplayDepthForOrientation, labelColor TAC_MISSED-label) and data.flaw_move_san (red, label = allowed display depth, labelColor TAC_ALLOWED-label) using uciToSquares/sanToSquares — matching the miniboard. At ply 1+ build a single blue engine arrow from hook.lastMove with the decrementing depth label; at depth-0 add the brand-brown punchline ring; payoff plies (isPayoff) render lighter blue with NO label (UI-SPEC). All colors from theme.ts (no hex literals).
    - ChessBoard with `id="tactic-explorer-board"`, `position={hook.position}`, `onPieceDrop={() => false}` (read-only), `lastMove`, `arrows`, `flipped` state (BoardControls onFlip). The explorer container holds the hook's containerRef so scoped keyboard works.
    - BoardControls: onBack/onForward/onReset/onFlip from hook + flip state, canGoBack/canGoForward from hook, size mobile 'md'/desktop 'sm', infoSlot = `<span data-testid="tactic-depth-readout" className="text-sm font-medium">{hook.isPayoff ? 'Payoff' : \`Depth: ${hook.displayDepth}\`}</span>`.
    - Layout: desktop two-column (board ~55% + SanLadder ~45%, gap-8) with BoardControls spanning below; mobile stacked board → BoardControls → SanLadder, board `w-full aspect-square`.

    Write TacticLineExplorer.test.tsx (vitest + testing-library, mock useTacticLines): (a) renders Dialog on desktop / Drawer on mobile by mocking matchMedia; (b) toggle hidden when only one line, visible when both; (c) toggle switch resets to root (depth readout back to root depth); (d) empty state copy when active line null; (e) isError copy on query error. Keep functions shallow — extract the arrow-building into a helper (CLAUDE.md nesting/LOC limits).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run TacticLineExplorer && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tactic-explorer-dialog" frontend/src/components/library/TacticLineExplorer.tsx` and `grep -n "tactic-explorer-drawer"` both match (both surfaces present per D-05).
    - `grep -n "tactic-san-ladder" frontend/src/components/library/SanLadder.tsx` matches.
    - `grep -c -E "#[0-9a-fA-F]{6}|rgba\(" frontend/src/components/library/TacticLineExplorer.tsx` returns 0 (colors from theme.ts only — CLAUDE.md).
    - `cd frontend && npm test -- --run TacticLineExplorer` passes (Dialog/Drawer switch, toggle visibility, reset, empty, isError).
    - `cd frontend && npx tsc -b` clean.
  </acceptance_criteria>
  <done>Explorer renders the correct surface per breakpoint, defaults to missed, hides the toggle for single-line flaws, resets on toggle, draws root/PV/payoff arrows + depth readout from theme colors, and handles loading/empty/error; SanLadder anchors to real game ply with highlight + click-jump; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Entry points — FlawCard button row (D-04) + LibraryGameCard Explore (D-03/D-02/D-01) + tests</name>
  <files>frontend/src/components/library/FlawCard.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/__tests__/FlawCard.test.tsx</files>
  <read_first>
    - frontend/src/components/library/FlawCard.tsx (FULL — viewGameButton lines 207-218, CardHeader usage line 254, mobile sm:hidden block ~410, desktop hidden sm:flex block ~427, existing Dialog block 442-477, isTagged gate `missed_tactic_motif != null || allowed_tactic_motif != null`)
    - frontend/src/components/results/LibraryGameCard.tsx (FULL — eval chart + buildPerPly line ~85, selected-ply state from chart slider, flaw_markers, the area below the eval chart adjacent to flaw-marker nav for D-03 placement)
    - frontend/src/components/library/__tests__/FlawCard.test.tsx (existing spec to EXTEND, not replace)
    - .planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-PATTERNS.md (D-04 button-row snippet + D-03 disabled-button+Tooltip snippet)
    - .planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-CONTEXT.md (D-01..D-05 verbatim — stacking, disabled state, placement, button row, mobile surface)
    - frontend/src/components/ui/button.tsx (brand-outline variant) + frontend/src/components/ui/tooltip.tsx
  </read_first>
  <action>
    FlawCard.tsx (D-04): Remove `viewGameButton` from the CardHeader (the in-header raw `<button>`). Add a `buttonRow` (`<div className="flex gap-2 mt-2 px-3 pb-3">`) rendered in BOTH the mobile (sm:hidden) and desktop (hidden sm:flex) sections (CLAUDE.md mobile parity — search both blocks). Compute `isTagged = flaw.missed_tactic_motif != null || flaw.allowed_tactic_motif != null`. Tagged → Explore (`Button variant="brand-outline" size="sm" data-testid="flaw-btn-explore"`, Telescope icon from lucide, fallback Play) + Game (`variant="brand-outline" size="sm" data-testid="flaw-btn-game"`, Swords icon). Untagged → Game only. Add `const [exploreOpen, setExploreOpen] = useState(false)`; Explore onClick sets it true; render `<TacticLineExplorer open={exploreOpen} onOpenChange={setExploreOpen} gameId={flaw.game_id} ply={flaw.ply} />`. Game button keeps opening the existing game Dialog. Both buttons brand-outline (never hand-rolled bg-*). text-sm floor.

    LibraryGameCard.tsx (D-03/D-02/D-01): Track the eval-chart's currently selected/parked ply and resolve its flaw_markers entry (`selectedFlaw`). Compute `isTaggedFlaw = selectedFlaw != null && (selectedFlaw.missed_tactic_motif != null || selectedFlaw.allowed_tactic_motif != null) && selectedFlaw.is_user` (user flaw only). Render an Explore button BELOW the eval chart, adjacent to the flaw-marker navigation (D-03), in BOTH mobile and desktop sections. Always visible; `disabled={!isTaggedFlaw}` with opacity-50; wrap in Tooltip showing "Select a tagged flaw to explore" only when disabled; `aria-label` = "Explore tactic" enabled / "Select a tagged flaw to explore" disabled; `data-testid="game-card-btn-explore"`; `variant="brand-outline" size="sm"`, Telescope icon. onClick (enabled) opens a `TacticLineExplorer` for `selectedFlaw.ply`. D-01 stacking: this explorer renders as a SECOND surface over the existing Game modal context — on close (onOpenChange false) it closes only the explorer and the Game view remains (Dialog/Drawer Radix portals handle this; do NOT close the underlying Game). On mobile the explorer is a Drawer (Plan 02/Task 2 handles surface selection internally), resolving the nested-dialog mobile risk.

    Extend FlawCard.test.tsx: assert a tagged flaw renders both flaw-btn-explore and flaw-btn-game; an untagged flaw renders flaw-btn-game only and NO flaw-btn-explore. (LibraryGameCard disabled-state assertion may live in the TacticLineExplorer/component test or a new case — at minimum assert the button is disabled when no tagged flaw is selected.)
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run FlawCard && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flaw-btn-explore" frontend/src/components/library/FlawCard.tsx` returns 1 (single button-row source; rendered in both responsive blocks via shared JSX or appears in both — executor's structure, but the testid must be reachable on mobile and desktop).
    - `grep -n "game-card-btn-explore" frontend/src/components/results/LibraryGameCard.tsx` matches; the button has a `disabled` binding tied to the tagged-user-flaw check.
    - FlawCard.test.tsx asserts Explore+Game for tagged and Game-only for untagged.
    - `cd frontend && npm run lint && npm test -- --run FlawCard && npx tsc -b` all clean.
  </acceptance_criteria>
  <done>Flaw cards show the D-04 button row (tagged/untagged variants) on both surfaces; the game card shows the D-03 Explore button with the D-02 disabled+tooltip behavior; both entry points open the explorer, and the game-card path stacks over and returns to the Game view (D-01); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Explorer fetches `GET /library/flaws/{game_id}/{ply}/tactic-lines` — the boundary is enforced server-side in Plan 01 (T-135-01..04). The client passes gameId/ply it already holds from the user's own flaw list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-07 | Information Disclosure | useTacticLines fetch | mitigate | The endpoint's IDOR guard (T-135-01, Plan 01) is the authoritative control; the client cannot request another user's data because the backend filters on JWT user_id and 404s otherwise. No client-side trust assumption. |
| T-135-08 | Elevation of Privilege | game-card Explore disabled state | accept | The D-02 disabled state is UX only, not a security control; the real gate is server-side ownership. A user toggling the disabled attribute in devtools still hits the IDOR-guarded endpoint. No additional mitigation needed. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` green for the whole frontend (CLAUDE.md run-tsc when shared types change — TacticLinesResponse is shared).
- `cd frontend && npm run knip` clean: all new exports (TacticLineExplorer, SanLadder, useTacticLines, getTacticLines, useTacticLine) are now consumed — knip should pass at phase end.
- Manual-only (from VALIDATION.md): nested Dialog→Drawer stacking + return-to-Game on a real mobile device; arrow/depth-label visual fidelity vs the miniboard for the same flaw.
</verification>

<success_criteria>
- Explore opens the correct surface (Dialog desktop / Drawer mobile) and walks the missed line by default with a floored depth counter and punchline highlight.
- Toggle appears only for two-line flaws and resets the stepper on switch; single-line flaws open with the toggle hidden.
- SAN ladder anchors to the real game ply with highlight + click-jump + punchline coloring.
- FlawCard D-04 button row and LibraryGameCard D-03/D-02/D-01 behavior land on both mobile and desktop.
- Frontend lint + tests + tsc -b + knip all clean.
</success_criteria>

<output>
Create `.planning/phases/135-tactic-line-explorer-walkable-pv-stepper-for-tagged-flaws-se/135-03-SUMMARY.md` when done.
</output>
