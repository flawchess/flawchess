---
phase: 127-detector-hardening-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tactic_detector.py
  - app/services/flaws_service.py
  - app/repositories/game_flaws_repository.py
  - app/models/game_flaw.py
  - alembic/versions/20260619_HHMMSS_phase_127_tactic_depth.py
  - tests/services/test_tactic_detector.py
autonomous: true
requirements: [SC#1, SC#3]
must_haves:
  truths:
    - "Every detect_* function returns a tuple whose last element is the half-move depth (int or None) at which the motif fired"
    - "detect_tactic_motif returns a 4-tuple (motif_int, piece, confidence, depth)"
    - "A non-mate motif buried in a non-winning continuation no longer fires (relevance gate), while a real deep combination still tags at its true depth"
    - "When two non-mate motifs fire, the shallower (min depth) wins; equal depth breaks by the existing priority tier order; exactly one motif per flaw is preserved"
    - "A forced mate in the line still dominates (D-07) and is never gated or depth-displaced"
    - "game_flaws has a nullable tactic_depth SmallInteger column; pre-existing rows are NULL"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "4-tuple detector contract, relevance gate, min-depth dispatch"
      contains: "tactic_depth"
    - path: "app/models/game_flaw.py"
      provides: "tactic_depth column"
      contains: "tactic_depth"
    - path: "alembic/versions/20260619_HHMMSS_phase_127_tactic_depth.py"
      provides: "Alembic migration adding tactic_depth"
      contains: "add_column"
  key_links:
    - from: "app/services/flaws_service.py"
      to: "app/services/tactic_detector.py"
      via: "_detect_tactic_for_flaw unpacks the 4-tuple from detect_tactic_motif"
      pattern: "detect_tactic_motif"
    - from: "app/repositories/game_flaws_repository.py"
      to: "app/models/game_flaw.py"
      via: "flaw_record_to_row maps tactic_depth into the insert dict"
      pattern: "tactic_depth"
---

<objective>
Build the hardened, depth-carrying detector as one atomic unit: the 4-tuple contract (depth return), the relevance gate that kills deep-scan / loose-pin false positives, the min-depth dispatcher with priority tiebreak, the new `tactic_depth` column + migration, and the full integration ripple through `flaws_service` and the repository write path.

Purpose: SC#1 (every detector returns depth; `tactic_depth` stored) and SC#3 (fork/pin false positives fixed) are one task per D-05/RESEARCH §Summary — the depth return IS the input min-depth dispatch needs, and the relevance gate (D-01) is the required companion to min-depth dispatch (D-02). Neither half works correctly alone.

Output: the corrected detector ready for the harness (Plan 02/03) to measure, and the schema ready for the dev re-backfill (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-detector-hardening-validation/127-CONTEXT.md
@.planning/phases/127-detector-hardening-validation/127-RESEARCH.md
@.planning/notes/tactic-detector-precision-gaps.md
@.planning/notes/tactic-tagging-architecture.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- **Column** `game_flaws.tactic_depth` — nullable `SmallInteger`, raw half-move ply index from `flaw_ply+1` (D-04). NULL on pre-existing rows.
- **Migration** `alembic/versions/20260619_HHMMSS_phase_127_tactic_depth.py` — single `op.add_column`, no backfill, `down_revision = "20260617_120000_phase_124"`.
- **Detector contract change** — every `detect_*` returns a depth-carrying tuple; core-8 + named-mate become `(fired, piece, depth)`; tier-3 become `(fired, piece, confidence, depth)`; `detect_boden_or_double_bishop_mate` becomes `(motif_or_None, piece, depth)`.
- **Dispatcher return** `detect_tactic_motif(...) -> tuple[int | None, int | None, int | None, int | None]` = `(motif_int, piece, confidence, depth)`.
- **`FlawRecord` TypedDict** gains `tactic_depth: int | None` (in `flaws_service.py`).
- No new symbols in the harness — that is Plan 02/03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4-tuple detector contract + depth extraction + relevance gate + min-depth dispatch</name>
  <files>app/services/tactic_detector.py, tests/services/test_tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py — read in full: module docstring (return convention), `_material_diff` (line ~217), `detect_fork` (~243-256), `detect_pin` (~308-336), `detect_skewer`, `detect_discovered_attack`, `detect_double_check`, `detect_hanging_piece`, the named-mate + tier-3 detectors, `detect_boden_or_double_bishop_mate` (~the unique-shape one), the registries `_NAMED_MATE_REGISTRY` / `_GEOMETRIC_REGISTRY` / `_TIER3_REGISTRY` and the `_*_DETECTOR_FNS` maps, and the dispatcher `detect_tactic_motif` (1227-1308).
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"Detector Code Reality", §"Dispatcher Change", §"Relevance Gate", §"Architecture Patterns" (Collector Pattern, Material Delta Gate), and §"Common Pitfalls" (Pitfalls 2-7: per-detector depth mapping).
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-01, D-02, D-03, D-04, D-05.
    - tests/services/test_tactic_detector.py — read the existing fixture format `(fen_after_flaw, pv_uci, expected_motif)` and the hard-negative cases; these tests call detectors directly and will break on the tuple-shape change.
  </read_first>
  <action>
    Make depth a first-class detector output and add the relevance gate, in one pass.

    Contract change (per RESEARCH §"Phase 127 Contract"): every core-8 and named-mate `detect_*` returns `(fired: bool, piece: int | None, depth: int | None)` — depth is the loop index at fire time. Every tier-3 `detect_*` returns `(fired: bool, piece: int | None, confidence: int, depth: int | None)`. `detect_boden_or_double_bishop_mate` returns `(motif: TacticMotif | None, piece: int | None, depth: int | None)`. Update every return statement AND every explicit return-type annotation; update the module docstring's documented return convention to the new shapes (do NOT inline the new contract sentence as a literal the harness will negative-grep — describe it by concept). Per-detector depth mapping is in RESEARCH Pitfalls 2-7: `detect_fork` depth = loop index `i`; `detect_pin` restructure the `for board in boards` scan to track the board/move index so depth is the pov-move index where the pin is created; `detect_skewer` depth = `i`; `detect_double_check` depth = `i - 1`; mates depth = `len(moves) - 1`; boden/double-bishop depth = `len(moves) - 1`.

    Relevance gate (D-01, Claude's-discretion form per RESEARCH §"Relevance Gate" recommendation): reuse the existing `_material_diff(board, pov)`. For `detect_fork`, `detect_skewer`, `detect_discovered_attack`: a firing at depth `i` counts only if `_material_diff(boards[i + 1], pov) > _material_diff(boards[0], pov)` OR `i == 0` (first pov move is the motif). For `detect_pin` (the loose-pin offender at line 308): the pin fires only if pov captures the pinned piece in a later pov move in the PV, falling back to the same `_material_diff` non-loss check at the pin board index — add a small pure helper `_pin_wins_material(...)` (nesting <= 3). Document the gate rationale at each fix site with a comment naming the deep-scan / Case-B bug it removes (per the project bug-comment rule). Extract the gate's material-delta direction as no magic numbers beyond the existing `_material_diff` semantics.

    Min-depth dispatch (D-02/D-05) in `detect_tactic_motif`: keep the mate tier exactly as-is (Tier 1 mates checked first, returned immediately with their depth appended — D-03 dominance, mates never enter the candidate pool). After mates, COLLECT every non-mate firing (geometric, tier-3, hanging-piece) into a candidate list carrying `(priority_rank, piece, confidence, depth, motif_int)`, then select `min(candidates, key=lambda c: (depth, priority_rank))` so the shallowest motif wins and equal depth breaks by the existing registry priority order. Return the 4-tuple `(motif_int, piece, confidence, depth)`. Preserve "exactly one motif per flaw": `min()` yields exactly one winner; ties are deterministic by rank. Empty pool returns `(None, None, None, None)`. Keep all early-return guards returning the 4-None shape.

    Update `tests/services/test_tactic_detector.py` to unpack the new tuple shapes wherever it calls detectors directly (signature regression only — do NOT change fixture provenance or precision bars here; the circular-fixture docstring supersession is Plan 03). Where a positive fixture asserts a motif, also assert the returned depth is a non-negative int; where a hard-negative asserts None, assert the 4-None dispatcher shape.
  </action>
  <behavior>
    - Test: a fork as the first pov move (i==0) returns `(True, piece, 0)`.
    - Test: a fork that appears only at a deep non-winning continuation (material not gained vs start, i>0) returns `(True/False, _, _)` with the gate suppressing it (does not fire as the dispatcher winner).
    - Test: the literal "pin exists, that's enough" loose case — a geometric pin in the PV where pov never wins the pinned material — no longer fires.
    - Test: when both a depth-0 pin and a depth-4 fork fire, dispatcher returns the pin (min depth); when both fire at equal depth, priority order (fork before pin) decides.
    - Test: a forced mate in the line is returned regardless of any shallower non-mate firing (D-03/D-07), with its depth populated.
    - Test: malformed/empty PV returns the 4-None shape.
  </behavior>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
    <automated>uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - Every core-8 and named-mate `detect_*` has return annotation ending in `int | None` depth and returns a 3-tuple `(fired, piece, depth)`.
    - Every tier-3 `detect_*` returns a 4-tuple `(fired, piece, confidence, depth)`.
    - `detect_boden_or_double_bishop_mate` returns `(motif_or_None, piece, depth)`.
    - `detect_tactic_motif` return annotation is `tuple[int | None, int | None, int | None, int | None]` and every return path emits 4 elements.
    - `detect_pin` no longer returns on bare geometric presence — it has a material/capture gate; the relevance gate reuses `_material_diff`.
    - The dispatcher collects all non-mate firings and selects by `min((depth, priority_rank))`; mates short-circuit before the candidate pool.
    - `uv run pytest tests/services/test_tactic_detector.py -x` passes; `uv run ty check app/ tests/` reports zero errors.
  </acceptance_criteria>
  <done>
    All detectors and the dispatcher carry depth in their return; the relevance gate suppresses non-winning/loose firings; min-depth dispatch with priority tiebreak preserves single-motif cardinality; mates retain dominance; existing detector tests pass under the new shapes; ty clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: tactic_depth column + migration + FlawRecord/repository write path</name>
  <files>app/models/game_flaw.py, alembic/versions/20260619_HHMMSS_phase_127_tactic_depth.py, app/services/flaws_service.py, app/repositories/game_flaws_repository.py</files>
  <read_first>
    - app/models/game_flaw.py — the Tactic-family block (lines 68-76): `tactic_motif` / `tactic_piece` / `tactic_confidence` nullable SmallInteger pattern to mirror.
    - alembic/versions/20260617_120000_phase_124_tactic_motifs.py — the exact migration template (naming, `op.add_column`, `down_revision`). Confirmed current head is `20260617_120000_phase_124`.
    - app/services/flaws_service.py — `FlawRecord` TypedDict, `_detect_tactic_for_flaw` (360-405, returns the 3-tuple today), `_build_flaw_record` (408-440, unpacks + constructs FlawRecord).
    - app/repositories/game_flaws_repository.py — `flaw_record_to_row` mapping FlawRecord -> insert dict.
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"Integration Ripple" and §"Alembic Migration".
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-04 (raw half-move ply index, no player-facing unit).
  </read_first>
  <action>
    Add `tactic_depth: Mapped[Optional[int]] = mapped_column(SmallInteger, nullable=True)` to `GameFlaw` directly after `tactic_confidence`, with a comment that it stores the raw half-move ply index from `flaw_ply+1` (D-04) and that the player-facing unit is deferred to Phase 129. Do NOT add an index.

    Autogenerate the migration: `uv run alembic revision --autogenerate -m "phase 127 tactic_depth"`, then verify the generated file contains exactly one `op.add_column("game_flaws", sa.Column("tactic_depth", sa.SmallInteger(), nullable=True))` and a matching `op.drop_column` in `downgrade`, with `down_revision` pointing at the Phase 124 head. Rename the file to the `20260619_HHMMSS_phase_127_tactic_depth.py` convention. No data backfill in the migration (pre-existing rows stay NULL — honest per D-04). Run `uv run alembic upgrade head` against the dev DB (additive nullable column, catalog-only, safe; no DB reset).

    Thread `tactic_depth` through the write path: add `tactic_depth: int | None` to the `FlawRecord` TypedDict; change `_detect_tactic_for_flaw` to unpack and return a 4-tuple (its return annotation and the two early-return `None` tuples become 4-None); change `_build_flaw_record` to unpack the 4th value and pass `tactic_depth=tactic_depth` into `FlawRecord(...)`; add `"tactic_depth": flaw["tactic_depth"]` to the dict in `flaw_record_to_row`.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/game_flaw.py` declares `tactic_depth: Mapped[Optional[int]] = mapped_column(SmallInteger, nullable=True)`.
    - The migration file is named `...phase_127_tactic_depth.py`, has `down_revision = "20260617_120000_phase_124"`, one `add_column` for `tactic_depth` (SmallInteger, nullable), and a `drop_column` downgrade.
    - `alembic upgrade head → downgrade -1 → upgrade head` round-trips cleanly on the dev DB.
    - `FlawRecord` has a `tactic_depth: int | None` key; `_detect_tactic_for_flaw` returns a 4-tuple; `flaw_record_to_row` emits `tactic_depth`.
    - `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>
    `tactic_depth` exists on the model and in the DB via a reversible migration; the FlawRecord -> insert path carries depth end-to-end; ty clean; detector tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored PV string -> detector | `pv_str` from `game_positions.pv` is parsed by python-chess; malformed input must not raise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Denial of Service | `detect_tactic_motif` PV parse | mitigate | Existing `try/except ValueError` around `_parse_pv` retained; all guard returns emit the 4-None shape (no new raise paths in the dispatcher) |
| T-127-02 | Tampering | Alembic migration on `game_flaws` | accept | Additive nullable column, catalog-only, reversible downgrade; no data rewrite, no destructive op |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps per D-11/RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `uv run pytest tests/services/test_tactic_detector.py -x` (4-tuple signatures, gate behavior, dispatch)
- `uv run ty check app/ tests/` zero errors
- `uv run alembic upgrade head` then `downgrade -1` then `upgrade head` round-trips
- `uv run pytest -n auto -x` (full default suite green after the wave — excludes tagger by design)
</verification>

<success_criteria>
- SC#1 partial: every detector + dispatcher returns depth; `tactic_depth` column exists and is wired into the write path.
- SC#3 partial: deep-scan fork and loose-pin firings are gated (validated quantitatively by the harness in Plan 03).
- Single-motif-per-flaw cardinality preserved; mates dominate (D-03/D-07).
</success_criteria>

<output>
Create `.planning/phases/127-detector-hardening-validation/127-01-SUMMARY.md` when done
</output>
