---
phase: 128-missed-opportunity-tagging
plan: 04
type: execute
wave: 3
depends_on:
  - 128-02
  - 128-03
files_modified:
  - scripts/backfill_flaws.py
  - .planning/phases/128-missed-opportunity-tagging/128-PROD-RUNBOOK.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Running the dev backfill fills missed_* and allowed_tactic_depth over tagged dev games; this is also the real-data validation of the missed pass (SC#5, D-11)"
    - "The backfill is idempotent: re-running produces the same result without corrupting existing rows (SC#5, D-11)"
    - "No SEED-054 pre-flight/coverage gating is built; rows lacking a flaw_ply PV resolve to NULL missed_* (honest, D-12, D-13)"
    - "The folded 127+128 prod re-backfill is documented as a deferred runbook step, NOT executed in-phase (D-12)"
  artifacts:
    - path: ".planning/phases/128-missed-opportunity-tagging/128-PROD-RUNBOOK.md"
      provides: "deferred prod runbook folding SEED-054 PV + 127/128 classify re-sweep into one pass (D-12)"
  key_links:
    - from: "scripts/backfill_flaws.py"
      to: "app/services/flaws_service.py classify_game_flaws"
      via: "full recompute path runs both detector passes (delete + re-insert)"
      pattern: "classify_game_flaws"
---

<objective>
Run the dev backfill that fills `missed_*` and `allowed_tactic_depth` over the tagged dev games (the phase gate, D-11), proving honest coverage and idempotency, and serving as the real-data validation of the missed pass. Document the folded 127+128 prod re-backfill as a deferred runbook step (D-12) — do NOT execute it in-phase.

Purpose: D-11 (dev backfill is the gate; reuse scripts/backfill_flaws.py + classify_game_flaws — no new script for the missed pass), D-12 (assume the SEED-054 flaw_ply PV is available; build NO pre-flight coverage check / gating / blocking logic; fold the prod runbook with 127's into one classify re-sweep), D-13 (NULL missed_* on PV-less rows is honest, never fabricated).

Output: dev game_flaws with missed_* + allowed_tactic_depth populated where the flaw_ply PV exists; a deferred prod runbook; verification of honest coverage + idempotency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md
@scripts/backfill_flaws.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm backfill recompute path runs both passes; write the deferred prod runbook</name>
  <files>scripts/backfill_flaws.py, .planning/phases/128-missed-opportunity-tagging/128-PROD-RUNBOOK.md</files>
  <read_first>
    - scripts/backfill_flaws.py (full file: it loads positions, calls classify_game_flaws(game, positions), then delete_flaws_for_game + bulk_insert_game_flaws — a full recompute per game, so once Plan 02 wires the missed pass into classify_game_flaws/_build_flaw_record, this script fills both column sets and allowed_tactic_depth with NO script change unless backfill reads positions[n].pv correctly)
    - app/services/flaws_service.py (_detect_tactic_for_flaw backfill branch: missed reads positions[n].pv, allowed reads positions[n+1].pv — confirm both PVs are persisted on dev rows by the SEED-054 drain so backfill picks them up)
    - .planning/phases/127-detector-hardening-validation/127-04-PLAN.md or its SUMMARY (the 127 deferred prod runbook this folds into)
    - .planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md (D-11, D-12, D-13)
  </read_first>
  <action>
    Verify scripts/backfill_flaws.py's recompute path (delete_flaws_for_game → classify_game_flaws → bulk_insert_game_flaws) already drives the missed pass through classify_game_flaws after Plan 02 — it should require no logic change because it recomputes the full FlawRecord per game. If the backfill calls classify_game_flaws WITHOUT pv_by_ply (it does — backfill relies on persisted positions[n].pv / positions[n+1].pv), confirm the missed branch in _detect_tactic_for_flaw reads positions[n].pv on the backfill path. Make only the minimal change needed (if any) for the missed pass to read the flaw_ply PV in backfill mode; do NOT add a SEED-054 pre-flight coverage check, a gate, or any blocking logic (D-12) — a row without a flaw_ply PV simply yields NULL missed_* (D-13). Author .planning/phases/128-missed-opportunity-tagging/128-PROD-RUNBOOK.md documenting the deferred, folded 127+128 prod re-backfill: a SINGLE prod classify re-sweep (after the SEED-054 prod PV backfill completes) that fills corrected allowed_* (127), allowed_tactic_depth, AND missed_* in one pass via scripts/backfill_flaws.py --db prod (behind bin/prod_db_tunnel.sh), explicitly noting it is NOT run as part of this phase gate.
  </action>
  <verify>
    <automated>grep -c "classify_game_flaws" scripts/backfill_flaws.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/backfill_flaws.py recompute path is confirmed to drive both detector passes via classify_game_flaws (no separate missed-pass script added).
    - No pre-flight SEED-054 coverage check / gate / blocking logic is added to the backfill (grep shows no new "coverage" gate around the missed pass): the backfill reads the PV and lets absent rows resolve to NULL.
    - 128-PROD-RUNBOOK.md exists and describes the folded single-pass 127+128 prod re-backfill as a DEFERRED step, not executed in-phase.
    - `uv run ty check app/ exits 0`; `uv run ruff check scripts/backfill_flaws.py` clean (if changed).
  </acceptance_criteria>
  <done>The recompute path drives both passes with no new gating; the deferred folded prod runbook is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run + verify the dev backfill (missed_* + allowed_tactic_depth fill, idempotency)</name>
  <action>Run the dev backfill and verify the missed_*/allowed_* coverage distribution, allowed_tactic_depth fill, and idempotency per the steps below; this is a human-verify checkpoint, not autonomous work.</action>
  <what-built>
    The dev backfill run that fills missed_* and allowed_tactic_depth over tagged dev games (the phase gate, D-11), with honest-coverage and idempotency verification.
  </what-built>
  <how-to-verify>
    1. Ensure the dev DB is running: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` and the migration from Plan 01 is applied (`uv run alembic upgrade head`).
    2. Dry-run first: `uv run python scripts/backfill_flaws.py --db dev --dry-run` and confirm it reports a sane game count without writing.
    3. Run the dev backfill: `uv run python scripts/backfill_flaws.py --db dev`.
    4. Honest coverage (via mcp__flawchess-db__query or psql): confirm `SELECT count(*) FROM game_flaws WHERE missed_tactic_motif IS NOT NULL` > 0 AND there exist rows with `missed_tactic_motif IS NOT NULL AND allowed_tactic_motif IS NULL` (a flaw the mover MISSED but did not get punished for — proves the two passes are independent), and rows where both are non-NULL, and rows where both are NULL (flaw_ply PV absent — honest, D-13). Confirm `allowed_tactic_depth IS NOT NULL` count is now > 0 on dev (the D-02 fourth-rename fill).
    5. Idempotency: re-run `uv run python scripts/backfill_flaws.py --db dev` and confirm the missed_*/allowed_* counts from step 4 are UNCHANGED (re-running does not corrupt or duplicate rows).
    6. Confirm NO row has a fabricated missed_* tag where its flaw_ply PV is NULL (spot-check a row with `positions[ply].pv IS NULL` → missed_tactic_motif IS NULL).
  </how-to-verify>
  <resume-signal>Type "approved" once the dev backfill shows the neither/one/both distribution, allowed_tactic_depth filled, and idempotency holds — or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill script → game_flaws | a bulk recompute over user-owned rows; the risk is corrupting existing allowed_* tags or fabricating missed_* tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-07 | Tampering (data corruption) | dev backfill recompute | mitigate | full delete+recompute per game via the SAME classify_game_flaws kernel (no fork); idempotency verified by a re-run count check (D-11) |
| T-128-08 | Spoofing (fabricated tag) | missed_* on PV-less rows | accept | NULL on absent flaw_ply PV is honest (D-13); the human checkpoint spot-checks that no PV-less row carries a missed_* tag |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs; if introduced, route through slopcheck + a blocking human checkpoint |
</threat_model>

<verification>
- `uv run python scripts/backfill_flaws.py --db dev --dry-run` reports a sane count.
- Post-backfill SQL shows the neither/one/both missed_*/allowed_* distribution + allowed_tactic_depth filled (human checkpoint).
- Re-run is idempotent (human checkpoint).
- `uv run ty check app/ tests/` exits 0; `uv run ruff check scripts/backfill_flaws.py` clean.
</verification>

<success_criteria>
Dev game_flaws carries missed_* + allowed_tactic_depth where the flaw_ply PV exists; coverage is honest (NULL on PV-less rows); the backfill is idempotent; the folded prod re-backfill is a documented deferred runbook step (SC#5, D-11, D-12, D-13).
</success_criteria>

<output>
Create `.planning/phases/128-missed-opportunity-tagging/128-04-SUMMARY.md` when done, recording the dev backfill coverage distribution and confirming the prod runbook is deferred.
</output>
