---
phase: 128-missed-opportunity-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/game_flaw.py
  - alembic/versions/
  - app/services/flaws_service.py
  - app/repositories/game_flaws_repository.py
  - tests/repositories/test_game_flaws_repository.py
  - tests/services/test_flaws_service.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 4 existing tactic_* columns are renamed to allowed_tactic_* with all Phase 124/125/127 data preserved (SC#1, D-02)"
    - "4 new nullable missed_tactic_* SmallInteger columns exist on game_flaws, NULL on all pre-existing rows (SC#1, D-01)"
    - "The Alembic migration round-trips up→down→up cleanly without data loss on allowed_* (D-02)"
    - "FlawRecord carries both allowed_* and missed_* 4-tuples; the write path maps all 8 to DB columns (SC#2 contract, D-06)"
  artifacts:
    - path: "app/models/game_flaw.py"
      provides: "8 inline tactic columns: allowed_tactic_{motif,piece,confidence,depth} + missed_tactic_{motif,piece,confidence,depth} (D-01)"
      contains: "missed_tactic_motif"
    - path: "alembic/versions/"
      provides: "data-preserving rename+add migration (4 ALTER RENAME + 4 ADD COLUMN), down_revision 9be5294cfe3c (D-02)"
    - path: "app/repositories/game_flaws_repository.py"
      provides: "flaw_record_to_row maps all 8 tactic fields (D-06)"
      contains: "missed_tactic_motif"
  key_links:
    - from: "app/services/flaws_service.py"
      to: "app/repositories/game_flaws_repository.py"
      via: "FlawRecord allowed_*/missed_* keys consumed by flaw_record_to_row"
      pattern: "missed_tactic"
---

<objective>
Establish the storage contract for missed/allowed tactic orientations: rename the 4 existing `tactic_*` columns on `game_flaws` to mover-relative `allowed_tactic_*` (data preserved), add a parallel `missed_tactic_*` set (4 nullable SmallInteger columns), and extend the `FlawRecord` TypedDict + write-path row mapping so both 4-tuples flow to the DB. This is the foundation Wave 2 (detector second pass, filter/schema) builds on.

Purpose: D-01 (inline 8 columns, not a child table) and D-02 (all four existing columns are data-preserving renames, including `tactic_depth → allowed_tactic_depth`). No detector logic and no query/filter changes here — only the column shape, the in-memory record contract, and the write mapping.

Output: migrated `game_flaws` schema with 8 inline tactic columns; `FlawRecord` with 8 tactic keys; `flaw_record_to_row` writing all 8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md
@.planning/notes/missed-vs-allowed-tactic-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename + add 8 inline tactic columns on game_flaws (model + migration)</name>
  <files>app/models/game_flaw.py, alembic/versions/</files>
  <read_first>
    - app/models/game_flaw.py (lines 68-78: the four existing tactic_* columns + their doc comments)
    - alembic/versions/20260619_134442_9be5294cfe3c_add_tactic_depth_to_game_flaws.py (the current head; revision id 9be5294cfe3c — its add_column pattern is the analog for the 4 new missed_* columns)
    - alembic/versions/20260617_120000_phase_124_tactic_motifs.py (Phase 124 migration — analog for the original tactic column adds)
    - .planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    In app/models/game_flaw.py rename the four mapped_column attributes tactic_motif, tactic_piece, tactic_confidence, tactic_depth to allowed_tactic_motif, allowed_tactic_piece, allowed_tactic_confidence, allowed_tactic_depth (all keep their existing nullable SmallInteger type). Add four new mapped_column attributes missed_tactic_motif, missed_tactic_piece, missed_tactic_confidence, missed_tactic_depth, all Optional[int] / nullable SmallInteger. Update the block doc comment: allowed_* are the refutation tag from the flaw_ply+1 PV; missed_* are the "instead-of" tag from the flaw_ply PV (mover POV). Per D-05, document that allowed_tactic_depth is the loop index within the flaw_ply+1 PV while missed_tactic_depth is the loop index within the flaw_ply PV (one ply earlier, at the decision position) — both are detector-loop indices within their own PV, neither an absolute game ply.
    Generate the migration with `uv run alembic revision --autogenerate -m "rename tactic cols to allowed, add missed tactic cols"`. Autogenerate will emit drop+add for the renames — this is WRONG and loses data. HAND-EDIT the migration body so upgrade() performs exactly four op.alter_column(..., new_column_name=...) calls (table "game_flaws"): tactic_motif→allowed_tactic_motif, tactic_piece→allowed_tactic_piece, tactic_confidence→allowed_tactic_confidence, tactic_depth→allowed_tactic_depth; then four op.add_column calls adding nullable sa.SmallInteger() columns missed_tactic_motif/piece/confidence/depth. downgrade() must reverse exactly: drop the four missed_* columns, then alter_column the four allowed_* back to their original tactic_* names. Confirm down_revision is set to '9be5294cfe3c' (the current head).
  </action>
  <verify>
    <automated>grep -c "missed_tactic_motif\|missed_tactic_piece\|missed_tactic_confidence\|missed_tactic_depth\|allowed_tactic_motif\|allowed_tactic_piece\|allowed_tactic_confidence\|allowed_tactic_depth" app/models/game_flaw.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' app/models/game_flaw.py | grep -c 'mapped_column' ` shows the 4 allowed_* + 4 missed_* attributes present; a bare `tactic_motif:` / `tactic_piece:` / `tactic_confidence:` / `tactic_depth:` column attribute no longer appears (the renames are complete).
    - The new alembic revision file's upgrade() uses op.alter_column with new_column_name for all 4 renames (NOT op.drop_column+op.add_column for the renamed set) and op.add_column for the 4 missed_* columns.
    - The new revision's down_revision == '9be5294cfe3c'.
    - `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` exits 0 with no error (round-trip clean; against the existing dev DB — do NOT reset the DB).
    - After up→down→up, an existing dev game_flaws row that had a non-NULL allowed-side tag still has it (data preserved): a spot SQL `SELECT count(*) FROM game_flaws WHERE allowed_tactic_motif IS NOT NULL` returns the same count as before the migration.
    - `uv run ty check app/ exits 0`.
  </acceptance_criteria>
  <done>game_flaws has 8 inline tactic columns; the migration renames (preserving data) and adds; round-trips cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend FlawRecord + write-path mapping for both orientations</name>
  <files>app/services/flaws_service.py, app/repositories/game_flaws_repository.py, tests/repositories/test_game_flaws_repository.py</files>
  <read_first>
    - app/services/flaws_service.py (lines 121-141: FlawRecord TypedDict tactic fields; line 429-445: _build_flaw_record return)
    - app/repositories/game_flaws_repository.py (lines 100-123: flaw_record_to_row tactic mapping)
    - tests/repositories/test_game_flaws_repository.py (existing tactic row-mapping assertions — the analog to extend)
    - .planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md (D-06)
  </read_first>
  <action>
    In app/services/flaws_service.py, rename the existing FlawRecord tactic keys tactic_motif_int / tactic_piece / tactic_confidence / tactic_depth to allowed_tactic_motif_int / allowed_tactic_piece / allowed_tactic_confidence / allowed_tactic_depth, and add four new optional keys missed_tactic_motif_int / missed_tactic_piece / missed_tactic_confidence / missed_tactic_depth (each `int | None`). Update the FlawRecord doc comment to describe both orientations. In _build_flaw_record, populate the four allowed_* keys from the existing detection call (the detector logic split into a second pass is Plan 02's job — for THIS plan, set the four missed_* keys to None so the contract compiles and the write path is exercised; Plan 02 will fill them). Update the FlawRecord(...) construction accordingly.
    In app/repositories/game_flaws_repository.py flaw_record_to_row, map all 8: "allowed_tactic_motif": flaw.get("allowed_tactic_motif_int"), "allowed_tactic_piece"/"allowed_tactic_confidence"/"allowed_tactic_depth" from the renamed keys, and "missed_tactic_motif": flaw.get("missed_tactic_motif_int"), "missed_tactic_piece"/"missed_tactic_confidence"/"missed_tactic_depth". Keep the .get() defensive pattern. Extend tests/repositories/test_game_flaws_repository.py to assert the row dict carries all 8 DB-column keys with the correct mapping from FlawRecord keys (including the int→motif key-name shift for the two motif columns).
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_game_flaws_repository.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "missed_tactic_motif_int\|allowed_tactic_motif_int" app/services/flaws_service.py` >= 2 (both renamed/new keys present in the TypedDict).
    - flaw_record_to_row output dict contains keys allowed_tactic_motif, allowed_tactic_piece, allowed_tactic_confidence, allowed_tactic_depth, missed_tactic_motif, missed_tactic_piece, missed_tactic_confidence, missed_tactic_depth (asserted by the extended test).
    - `uv run pytest tests/repositories/test_game_flaws_repository.py -x` exits 0.
    - `uv run ty check app/ tests/ exits 0` (the bare tactic_motif_int key no longer referenced anywhere → ty would flag a stale TypedDict key access otherwise).
  </acceptance_criteria>
  <done>FlawRecord carries both 4-tuples; flaw_record_to_row writes all 8 columns; missed_* are None pending Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → live game_flaws table | a schema change over existing user-owned data; the risk is destructive data loss on the renamed allowed_* columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Denial of Service (data loss) | Alembic rename migration | mitigate | use op.alter_column rename (data-preserving), NOT drop+add; acceptance asserts the up→down→up round-trip and a pre/post non-NULL allowed_* count match (D-02) |
| T-128-02 | Tampering | new missed_* columns | accept | nullable SmallInteger ints written only by the trusted classify/backfill path; no external input reaches them in this plan |
</threat_model>

<verification>
- `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips clean.
- `uv run pytest tests/repositories/test_game_flaws_repository.py tests/services/test_flaws_service.py -x` exits 0.
- `uv run ty check app/ tests/` exits 0.
- `uv run ruff check app/ tests/` clean.
</verification>

<success_criteria>
game_flaws carries 8 inline tactic columns; the migration is data-preserving and reversible; FlawRecord + the write path expose both orientations (SC#1 + the storage half of SC#2).
</success_criteria>

<output>
Create `.planning/phases/128-missed-opportunity-tagging/128-01-SUMMARY.md` when done, recording the new Alembic revision id and the renamed/added column names.
</output>
