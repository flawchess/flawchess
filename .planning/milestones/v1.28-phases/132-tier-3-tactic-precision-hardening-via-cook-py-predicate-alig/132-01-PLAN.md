---
phase: 132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/tactic_tagger_report.py
autonomous: true
requirements: [D-01, D-05]
must_haves:
  truths:
    - "GOALS dict in tactic_tagger_report.py targets precision 0.90 (recall None) for all six in-scope firing Tier-3 motifs: deflection, clearance, capturing-defender, attraction, intermezzo, x-ray"
    - "The --check-goals harness can be run on --eval-set train and --eval-set test and reports per-motif post-dispatch precision"
    - "A documented baseline snapshot of post-dispatch P(train)/P(test) for the six in-scope motifs + interference exists for later waves to compare against"
  artifacts:
    - path: "scripts/tactic_tagger_report.py"
      provides: "GOALS dict raised to 0.90 precision for the six in-scope Tier-3 motifs"
      contains: "deflection"
  key_links:
    - from: "scripts/tactic_tagger_report.py"
      to: "tests/scripts/tagger/precision_floors.py"
      via: "imports SUPPRESSED_MOTIFS to mark suppressed rows"
      pattern: "from tests.scripts.tagger.precision_floors import SUPPRESSED_MOTIFS"
  prohibitions:
    - "Must NOT edit detect_tactic_motif (the shallowest-wins dispatcher)"
    - "Must NOT edit any detector function in this plan (harness-only)"
    - "Must NOT copy cook.py source (AGPL-3.0)"
    - "Must NOT change tag cardinality / GROUP BY tactic_motif semantics"
---

<objective>
Raise the `GOALS` dict in `scripts/tactic_tagger_report.py` to precision 0.90 (recall ungated) for the six in-scope firing Tier-3 motifs, and capture a fresh post-dispatch baseline snapshot for both eval splits. This is the harness setup that every subsequent detector-rewrite wave optimizes against.

Purpose: Establish the measurement bar (D-05 post-dispatch winner scoring, precision-first per D-01) before any detector code changes, so each later wave has a stable, raised goal and a recorded baseline to compare ΔP against.
Output: Updated GOALS dict + a recorded baseline of P(train)/P(test) per in-scope motif (in the SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-CONTEXT.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise GOALS to 0.90 for the six in-scope Tier-3 motifs</name>
  <files>scripts/tactic_tagger_report.py</files>
  <read_first>
    - scripts/tactic_tagger_report.py (the `GOALS` dict ~line 116, `GoalSpec` type, the `--check-goals` / `--eval-set` mechanics, and the post-dispatch winner call at ~line 193 `detect_tactic_motif(board, row["pv"])` — D-05)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Harness Mechanics — Measurement and Regression": the exact GOALS entries to raise; "Per-Motif Baseline and n(test)": the current numbers)
  </read_first>
  <action>
    In the `GOALS` dict, set these six entries to `{"precision": 0.90, "recall": None}`: `deflection` (currently 0.50), `clearance` (currently 0.70), `capturing-defender`, `attraction`, `intermezzo`, `x-ray` (the last four are currently absent from GOALS / suppressed — add them). Leave `interference` at its current 0.80 entry (it is the regression lock, not an optimization target). Do NOT add a `sacrifice` GOALS entry yet — per D-02 it is added only if it achieves any TP (handled in a later wave). Update the inline tier-3 comment to note these were raised to 0.90 per Phase 132 D-01 (precision-first, recall ungated). Keep `recall` at `None` for all six (recall is explicitly ungated this phase).
  </action>
  <verify>
    <automated>grep -A1 '"deflection"' scripts/tactic_tagger_report.py | grep -q '0.90' && grep -A1 '"x-ray"' scripts/tactic_tagger_report.py | grep -q '0.90' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six entries (`deflection`, `clearance`, `capturing-defender`, `attraction`, `intermezzo`, `x-ray`) are present in GOALS with `"precision": 0.90, "recall": None`.
    - `interference` GOALS entry is unchanged (still 0.80).
    - No `sacrifice` GOALS entry added.
    - `uv run ruff check scripts/tactic_tagger_report.py` passes.
  </acceptance_criteria>
  <done>GOALS targets the six in-scope Tier-3 motifs at 0.90 precision, recall ungated; interference untouched; sacrifice deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Record fresh post-dispatch baseline for both eval splits</name>
  <files>scripts/tactic_tagger_report.py</files>
  <read_first>
    - scripts/tactic_tagger_report.py (the `--check-goals` / `--eval-set` CLI; this task RUNS it, no further edit unless the run reveals a bug)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Validation Architecture" → the exact TRAIN/TEST commands; "Per-Motif Baseline and n(test)" table to sanity-check the numbers match ~deflection 0.21 TEST etc.)
  </read_first>
  <action>
    Run the harness on both splits and capture the per-motif post-dispatch P(train)/P(test) for the six in-scope motifs PLUS `interference` and `sacrifice`, recording them verbatim in the plan SUMMARY as the Phase 132 baseline. Commands: `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set train` and `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test`. Expect exit 1 on both (goals unmet — the detectors are not yet rewritten); that is the EXPECTED pre-port state, not a failure. Confirm the printed baseline roughly matches the RESEARCH.md baseline table (deflection ~0.21 TEST, clearance ~0.37 TEST, interference 1.00 TEST). If interference is NOT ~1.00 on TEST, STOP and flag — the regression lock is already broken before any work starts.
  </action>
  <verify>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test; test $? -le 1 && echo "ran (exit<=1 expected pre-port)"</automated>
  </verify>
  <acceptance_criteria>
    - Both `--eval-set train` and `--eval-set test` runs complete and print a per-motif table.
    - `interference` shows ~1.00 P(test) (regression lock baseline confirmed intact).
    - The baseline P(train)/P(test) for all six in-scope motifs + interference + sacrifice is recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <done>A recorded, reproducible post-dispatch baseline exists for both splits; interference confirmed at ~1.00 TEST before any detector edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure-CPU script/config edit; no untrusted input crosses any boundary this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Tampering | GOALS dict edit | accept | No external input; pure deterministic config. CC0 fixtures are committed, read-only. |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH.md "Package Legitimacy Audit": N/A — zero new packages). |
</threat_model>

<verification>
- `grep` confirms all six GOALS entries at 0.90.
- Both harness splits run; baseline recorded; interference at ~1.00 TEST.
- `uv run ruff check scripts/tactic_tagger_report.py` and `uv run ty check app/` pass.
</verification>

<success_criteria>
GOALS raised to 0.90 for the six in-scope motifs (recall ungated), sacrifice deferred, interference untouched, and a reproducible post-dispatch baseline recorded for both eval splits.
</success_criteria>

<output>
Create `.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-01-SUMMARY.md` when done — include the verbatim baseline P(train)/P(test) table.
</output>

---

## Artifacts this plan produces

- `scripts/tactic_tagger_report.py` GOALS dict: `deflection`, `clearance`, `capturing-defender`, `attraction`, `intermezzo`, `x-ray` raised to `{"precision": 0.90, "recall": None}` (newly added/changed entries — exclude from drift verification). No `sacrifice` GOALS entry yet.
- A recorded Phase 132 baseline (in the SUMMARY): post-dispatch P(train)/P(test) per in-scope motif + interference + sacrifice.
