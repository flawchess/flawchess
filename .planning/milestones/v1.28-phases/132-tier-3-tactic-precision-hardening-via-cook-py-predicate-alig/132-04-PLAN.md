---
phase: 132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig
plan: 04
type: execute
wave: 4
depends_on: ["132-03"]
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/precision_floors.py
  - scripts/tactic_tagger_report.py
autonomous: true
requirements: [D-01, D-02, D-03, D-05]
must_haves:
  truths:
    - "detect_attraction enforces cook's exact AND-chain (attracted piece in {K,Q,R}, pov attacks the attracted square on its next move, king short-circuit vs queen/rook two-move follow-up) and returns TACTIC_CONFIDENCE_HIGH when it fires"
    - "detect_x_ray enforces cook's three-same-square AND-chain (moves[k-2].to == moves[k-1].to == moves[k].to) plus the between-square geometry; if it shows 0 TP on TRAIN at full port the D-03 PV-divergence cutoff is invoked and x-ray is suppressed without further investment"
    - "detect_sacrifice matches cook's simple material-diff predicate (>=2 below initial after a 2nd+ pov move, opponent-promotion guard on odd indices)"
    - "Each of attraction, x-ray, sacrifice is measured on TEST and shipped-or-suppressed by its number"
    - "interference is asserted >=0.99 on TEST with ZERO detector edits to detect_interference"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "Cook-aligned detect_attraction, detect_x_ray, detect_sacrifice; detect_interference unchanged"
      contains: "def detect_attraction"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Final SUPPRESSED_MOTIFS / PRECISION_FLOOR for the likely-suppress trio"
      contains: "SUPPRESSED_MOTIFS"
  key_links:
    - from: "app/services/tactic_detector.py"
      to: "scripts/tactic_tagger_report.py"
      via: "detect_tactic_motif post-dispatch winner scored at line ~193"
      pattern: "detect_tactic_motif"
  prohibitions:
    - "Must NOT edit detect_tactic_motif (the shallowest-wins dispatcher) — read only"
    - "Must NOT edit detect_interference — assert its floor only; add a DO-NOT-EDIT comment marker"
    - "Must NOT copy cook.py source (AGPL-3.0) — reimplement from RESEARCH.md pseudocode only"
    - "Must NOT reintroduce a _grade(met, total) voting scheme in the rewritten detectors"
    - "Must NOT over-invest in x-ray once the D-03 0-TP-on-TRAIN cutoff signal fires"
    - "Must NOT reset the dev DB"
---

<objective>
Complete the full-port-then-suppress sweep for the likely-suppress trio (`attraction`, `x-ray`, `sacrifice`) at full cook fidelity, apply the D-03 x-ray PV-divergence cutoff, and lock `detect_interference` against regression. Per D-01/D-02 these are attempted at full fidelity to learn the real ceiling, then shipped or (more likely) suppressed honestly by their TEST number.

Purpose: Finish the per-motif detector work, encode the x-ray early-cutoff so the executor does not over-invest (D-03), and assert the interference floor stays ≥0.99 with no edits to it (D-05 carry-forward).
Output: Cook-aligned `detect_attraction` / `detect_x_ray` / `detect_sacrifice`; finalized suppression set for the trio; interference DO-NOT-EDIT marker + asserted floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-CONTEXT.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md
@.planning/notes/tactic-tagger-cook-alignment.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite detect_attraction (cook AND-chain) and detect_x_ray (with D-03 cutoff)</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_attraction` ~line 1421 and `detect_x_ray` ~line 1507 — current voting bodies; `detect_interference` template; `engine.PV_CAP_PLIES` context for x-ray depth; `TACTIC_CONFIDENCE_HIGH`)
    - app/services/engine.py (`PV_CAP_PLIES = 12` — the x-ray depth ceiling, D-03)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Per-Motif cook.py Pseudocode" → section 4 `attraction` (attracted ∈ {K,Q,R}, pov's k+2 move lands on a square attacking the attracted square, king short-circuit vs queen/rook k+4 follow-up) and section 6 `x-ray` (three-same-square condition `moves[k-2].to == moves[k-1].to == moves[k].to`, opponent recapturer not a king, between-square geometry); "x-ray PV-Divergence Ceiling (D-03)" — the 0-TP-on-TRAIN cutoff and the `range(2, 8, 2)` k≤6 mitigation; Pitfalls 1, 4, 6)
  </read_first>
  <behavior>
    - attraction fires (returns `(True, attracted_piece_type, TACTIC_CONFIDENCE_HIGH, k)`) only when: `moves[k+1].to_square == moves[k].to_square`; attracted piece type ∈ {KING, QUEEN, ROOK}; pov's `moves[k+2]` lands on a square in `boards[k+2].attackers(pov, attracted_square)`; AND either attracted is KING (short-circuit True) OR `moves[k+4].to_square == attracted_square` (queen/rook two-move follow-up). Otherwise `(False, None, 0, None)`.
    - x-ray fires (returns `(True, None, TACTIC_CONFIDENCE_HIGH, k)`) only when: this pov move is a capture; `moves[k-2].to_square == moves[k-1].to_square == moves[k].to_square` (three captures on the same square — Pitfall 4); the opponent recapturer (`moves[k-1]`) is not a king; AND `moves[k-1].from_square in chess.SquareSet.between(moves[k].from_square, moves[k].to_square)`. Otherwise `(False, None, 0, None)`.
  </behavior>
  <action>
    Rewrite `detect_attraction` to cook's exact AND-chain (RESEARCH.md section 4, index mapping `moves[k+1]`/`moves[k+2]`/`moves[k+4]`, bounds-guard k+4 < len(moves) for the queen/rook branch). Rewrite `detect_x_ray` to cook's three-same-square AND-chain (RESEARCH.md section 6); make the three-same-square equality the FIRST guard (Pitfall 4); use `chess.SquareSet.between` for the geometry. For x-ray, FIRST try the full `range(2, len(moves), 2)` scan; per the D-03 cutoff, if TRAIN shows 0 TP for x-ray after the full port, try the `range(2, 8, 2)` (k≤6) restriction once; if still 0 TP on TRAIN, STOP investing and leave x-ray to be suppressed in Task 3 — record the cutoff decision in the SUMMARY. Both detectors return `TACTIC_CONFIDENCE_HIGH` on a full match; remove any `_grade(...)`. Determine captures from the board before the move (Pitfall 6). Keep nesting ≤4 (extract `_attraction_fires_at` / `_x_ray_fires_at` helpers if needed). Comment-cite Phase 132 D-01/D-03 and the relevant cook lines.
  </action>
  <verify>
    <automated>uv run ty check app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k "attraction or x_ray or x-ray"</automated>
  </verify>
  <acceptance_criteria>
    - `detect_attraction` and `detect_x_ray` contain no `_grade(` and no `met >= ` voting threshold.
    - x-ray's three-same-square equality (`moves[k-2].to_square == moves[k-1].to_square == moves[k].to_square`) is present as a leading guard.
    - The SUMMARY records whether the D-03 x-ray cutoff (0 TP on TRAIN) fired and which scan range was used.
    - attraction + x-ray fixture/fast-guard tests pass.
    - `uv run ty check app/services/tactic_detector.py` reports zero errors.
  </acceptance_criteria>
  <done>detect_attraction and detect_x_ray are cook-aligned AND-chains; the D-03 x-ray cutoff is encoded and its outcome recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align detect_sacrifice to cook's simple material-diff predicate (D-02)</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_sacrifice` ~line 1748 — current "max sacrifice" approach; `_material_diff`; `TACTIC_CONFIDENCE_HIGH`)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Per-Motif cook.py Pseudocode" → section 7 `sacrifice` — `initial = _material_diff(boards[0], pov)`; for pov move k in `range(2, len(moves), 2)` check `_material_diff(boards[k+1], pov) - initial <= -2`; promotion guard `not any(m.promotion for m in moves[1::2])` (OPPONENT moves, odd indices — Pitfall 7); "Co-tag structural problem" — likely ends NaN/suppressed because shallower tactics win dispatch)
  </read_first>
  <behavior>
    - Fires (returns `(True, sacrificed_piece_type, TACTIC_CONFIDENCE_HIGH, k)`) when, for some pov move k ≥ 2 (even), `_material_diff(boards[k+1], pov) - _material_diff(boards[0], pov) <= -2` AND no opponent move promotes (`not any(m.promotion for m in moves[1::2])` — Pitfall 7).
    - Returns `(False, None, 0, None)` otherwise.
  </behavior>
  <action>
    Simplify `detect_sacrifice` to cook's predicate (RESEARCH.md section 7): compute `initial = _material_diff(boards[0], pov)`, scan `range(2, len(moves), 2)`, fire on the first k where `_material_diff(boards[k+1], pov) - initial <= -2`, guarded by the opponent-promotion check on ODD indices `moves[1::2]` (Pitfall 7 — NOT `moves[::2]`). Extract a `MIN_SACRIFICE_DROP = 2` named constant (no magic number) for the `<= -2` threshold. Return `TACTIC_CONFIDENCE_HIGH`. Note in a comment (per D-02) that this is a dispatch-capped co-tag likely to end suppressed.
  </action>
  <verify>
    <automated>uv run ty check app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k sacrifice</automated>
  </verify>
  <acceptance_criteria>
    - `detect_sacrifice` uses the simple cook material-diff predicate with a named `MIN_SACRIFICE_DROP` constant.
    - The promotion guard indexes `moves[1::2]` (opponent moves), not pov moves (Pitfall 7).
    - sacrifice fixture/fast-guard tests pass.
    - `uv run ty check app/services/tactic_detector.py` reports zero errors.
  </acceptance_criteria>
  <done>detect_sacrifice matches cook's simple material-diff predicate with the correct opponent-promotion guard.</done>
</task>

<task type="auto">
  <name>Task 3: Lock interference, then measure + finalize ship/suppress for attraction, x-ray, sacrifice</name>
  <files>app/services/tactic_detector.py, tests/scripts/tagger/precision_floors.py, scripts/tactic_tagger_report.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_interference` ~line 1544 — add the DO-NOT-EDIT marker comment ONLY; do not change logic)
    - tests/scripts/tagger/precision_floors.py (`SUPPRESSED_MOTIFS` — attraction/x-ray/sacrifice all currently suppressed; `PRECISION_FLOOR`)
    - scripts/tactic_tagger_report.py (GOALS — add a `sacrifice` entry ONLY if it achieves any TP per D-02)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("tactic_confidence Suppression Lever" → ship/suppress two-file edit; "Common Pitfalls" 8 — interference must NOT be edited)
  </read_first>
  <action>
    First, add the comment `# DO NOT EDIT — interference regression lock (Phase 132, 1.00 TEST)` directly above `detect_interference` (Pitfall 8). Then run `--check-goals --eval-set train` and `--eval-set test`. For EACH of attraction, x-ray, sacrifice apply the TEST-driven decision (D-01/D-02): if P(test) ≥ 0.9 with non-trivial volume, REMOVE from `SUPPRESSED_MOTIFS` + add a `PRECISION_FLOOR`; otherwise KEEP in `SUPPRESSED_MOTIFS` with an updated `(Phase 132 full cook port — 0.XX TEST)` comment. For x-ray specifically, if the D-03 cutoff fired (0 TP on TRAIN), suppress with a `# D-03 PV-divergence cutoff — 0 TP on TRAIN at full port` comment. For sacrifice: only add a GOALS entry if it achieved any TP; if it stays NaN/0-TP (the expected co-tag outcome), leave it suppressed and document the dispatch-cap reason. CRITICAL: assert `interference` shows P(test) ≥ 0.99 in the final run (regression lock) — if it dropped, STOP and investigate (no detector edit should have touched it). Record the final per-motif Status table in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q "DO NOT EDIT — interference regression lock" app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k "attraction or x_ray or x-ray or sacrifice or interference"</automated>
  </verify>
  <acceptance_criteria>
    - `detect_interference` carries the `# DO NOT EDIT — interference regression lock (Phase 132, 1.00 TEST)` marker and its logic is byte-for-byte unchanged.
    - attraction, x-ray, sacrifice each have an explicit, recorded ship-or-suppress decision tied to a TEST number; x-ray's decision references the D-03 cutoff if it fired.
    - `interference` P(test) is ≥ 0.99 in the final harness run (regression lock holds).
    - A `sacrifice` GOALS entry exists only if sacrifice achieved any TP; otherwise it stays out of GOALS and in SUPPRESSED_MOTIFS.
    - The fixture suite (`-k "attraction or x_ray or x-ray or sacrifice or interference"`) passes.
  </acceptance_criteria>
  <done>interference is marked and asserted ≥0.99 TEST with no logic edits; the likely-suppress trio is honestly resolved by its TEST numbers (x-ray honoring the D-03 cutoff).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure-CPU detector + config edit; no untrusted input, no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-04 | Tampering | detector predicate logic | accept | Deterministic CPU over committed CC0 fixtures; harness-validated; interference logic frozen. |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- `grep` confirms no `_grade(` / `met >= ` in detect_attraction, detect_x_ray, detect_sacrifice.
- `grep` confirms the interference DO-NOT-EDIT marker exists.
- `uv run pytest tests/scripts/tagger/ -v -k "attraction or x_ray or x-ray or sacrifice or interference"` passes.
- `uv run ty check app/ tests/` and `uv run ruff check app/ tests/ scripts/` pass.
- `--eval-set test` recorded; interference ≥0.99.
</verification>

<success_criteria>
The likely-suppress trio is ported at full cook fidelity and honestly resolved by their TEST numbers (x-ray honoring the D-03 PV-divergence cutoff), and detect_interference is marked DO-NOT-EDIT and asserted ≥0.99 on TEST with no logic changes.
</success_criteria>

<output>
Create `.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-04-SUMMARY.md` when done — include the final per-motif Status table (shipped/suppressed) with TP/FP/P(train)/P(test) for attraction, x-ray, sacrifice, plus the interference floor confirmation.
</output>

---

## Artifacts this plan produces

- `app/services/tactic_detector.py`: `detect_attraction` rewritten to cook's AND-chain (attracted ∈ {K,Q,R}, king short-circuit vs queen/rook k+4 follow-up); `detect_x_ray` rewritten to cook's three-same-square AND-chain (D-03 cutoff applied); `detect_sacrifice` simplified to cook's material-diff predicate with new constant `MIN_SACRIFICE_DROP = 2`; `detect_interference` UNCHANGED logic + new `# DO NOT EDIT — interference regression lock (Phase 132, 1.00 TEST)` marker comment. Optional helpers `_attraction_fires_at` / `_x_ray_fires_at` (names may vary).
- `tests/scripts/tagger/precision_floors.py`: final `SUPPRESSED_MOTIFS` / `PRECISION_FLOOR` edits for attraction, x-ray, sacrifice.
- `scripts/tactic_tagger_report.py`: `sacrifice` GOALS entry added ONLY if it achieved any TP (else absent).
- (Newly-created/modified symbols — exclude from drift verification.)