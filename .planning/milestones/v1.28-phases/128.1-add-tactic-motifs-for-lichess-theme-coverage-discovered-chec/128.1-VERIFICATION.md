---
phase: 128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec
verified: 2026-06-20T09:30:00Z
status: verified
human_resolved: 2026-06-20T04:05:00Z
human_resolution: "All human-verification items closed via /gsd-verify-work — see 128.1-UAT.md. Verified deterministically against the CC0 puzzle fixtures by rendering the 5 new motifs in the tactic-tagger-report (reports/tactic-tagger/tactic-tagger-2026-06-20.md), not a live import. discovered-check 0.880/0.833 precision over 1401 GT puzzles; trapped-piece/en-passant confirmed behaving exactly as the suppression design predicts (only-FP / never-fires-shadowed). Persistence seam proven motif-agnostic (flaws_service → game_flaws_repository, no write-time suppression, D-11)."
score: 13/13
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Run a game containing a trapped-piece position through the full import pipeline and verify the tactic tag visible in the Library UI reads 'trapped-piece' rather than 'hanging-piece'."
    expected: "The position where an opponent piece has all escapes losing material tags as trapped-piece in the stored game_flaws row, not hanging-piece, even when both detectors would fire."
    why_human: "trapped-piece is in SUPPRESSED_MOTIFS (only-FP on the harness fixture), so the precision gate does not confirm correct firing end-to-end. The test suite confirms dispatch priority in isolation, but the full pipeline (import → flaw detection → DB write → UI read) cannot be probed without a running server and real game data."
  - test: "Play or import a game containing an en-passant capture that is also the refuting move in a flaw PV (with no real tactic on that PV). Verify the Library UI shows 'en-passant' as the tactic tag."
    expected: "The en-passant move in a quiet position (no fork/pin/etc.) stores en-passant (motif_int=27) in the allowed_tactic_motif or missed_tactic_motif column."
    why_human: "en-passant is in SUPPRESSED_MOTIFS (NaN — never fires on the harness because all 12 enPassant-labeled rows have a higher-priority real tactic). The detector is structurally validated by fast-guard fixtures, but real-world firing on a genuine residual PV requires a live import."
behavior_unverified_items:
  - truth: "trapped-piece does NOT fire on a piece that is capturable-for-free right now (D-07 distinction from hanging-piece)"
    test: "Trigger the full import pipeline on a game with a free-hanging piece (no escape) and confirm it is tagged hanging-piece, not trapped-piece."
    expected: "The hanging piece tags as hanging-piece (motif_int=2). detect_trapped_piece returns fired=False because the D-07 exclusion (piece is already capturable for free) blocks it."
    why_human: "The test suite confirms the D-07 boundary with a single fast-guard fixture. Presence + wiring are verified. The SUPPRESSED status of trapped-piece means the harness cannot confirm the distinction across real TRAIN data — only that the isolated fixture passes."
  - truth: "detect_tactic_motif emits 'en-passant' when the refuting move is an en-passant capture and no real tactic fired"
    test: "Import or construct a game where the flaw's refuting PV opens with an en-passant capture and no Tier 1-4 tactic fires. Confirm motif_int=27 is stored."
    expected: "en-passant (27) stored in allowed_tactic_motif or missed_tactic_motif for that flaw."
    why_human: "en-passant is in SUPPRESSED_MOTIFS (NaN). The fast-guard fixture confirms structural firing. Real-world end-to-end confirmation requires a live import where no real tactic shadows the en-passant move."
---

# Phase 128.1: Add Tactic Motifs for Lichess Theme Coverage Verification Report

**Phase Goal:** Close lichess `puzzleTheme.xml` coverage gaps in `detect_tactic_motif` by adding five deterministically-detectable motifs — split `discovered-check` out of `discovered-attack`, a new `trapped-piece` detector (escape-square enumeration), and trivial `en-passant` + `promotion`/`under-promotion` tags.
**Verified:** 2026-06-20T09:30:00Z
**Status:** verified (13/13 truths verified; the 2 SUPPRESSED-in-harness truths were closed on 2026-06-20 via /gsd-verify-work — see 128.1-UAT.md and the Resolution note below)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `detect_tactic_motif` emits 'discovered-check' when pov's first move gives a discovered check | VERIFIED | `detect_discovered_check()` at tactic_detector.py:566; GEOMETRIC_REGISTRY rank 4; 13 positive fixtures pass; dispatcher test `test_discovered_check_dominates_discovered_attack` green |
| 2 | discovered-check ranks ABOVE discovered-attack in Tier 2 (D-03) | VERIFIED | `_GEOMETRIC_REGISTRY` rank 4 (discovered-check) < rank 5 (discovered-attack); programmatically confirmed |
| 3 | `detect_tactic_motif` emits 'trapped-piece' when all escapes lose material | VERIFIED | `detect_trapped_piece()` at tactic_detector.py:723; D-06 strict SEE gate (`_escape_squares_all_lose_material`); 13 positive fixtures pass |
| 4 | trapped-piece ranks ABOVE the Tier-4 hanging-piece catch-all (D-03) | VERIFIED | TIER2=2 < TIER4=4 in `_sort_key`; `test_trapped_piece_dominates_hanging_piece` priority assertion green |
| 5 | trapped-piece does NOT fire on a piece capturable-for-free right now (D-07 distinction) | PRESENT_BEHAVIOR_UNVERIFIED | Acceptance-criteria fixture passes; trapped-piece in SUPPRESSED_MOTIFS (0 TP, 9 FP on harness) — D-07 isolation confirmed by fast-guard but not by harness coverage |
| 6 | Both allowed_* and missed_* PV passes emit the two new motifs for free (same dispatcher) | VERIFIED | Both passes call `detect_tactic_motif()`; no per-pass code added (confirmed by git diff bad29aa8, 9e173c8b) |
| 7 | `detect_tactic_motif` emits 'en-passant' when the refuting move is an e.p. capture and no real tactic fired | PRESENT_BEHAVIOR_UNVERIFIED | `detect_en_passant()` at tactic_detector.py:1511 structurally correct; en-passant in SUPPRESSED_MOTIFS (NaN — all 12 TRAIN rows shadowed); fast-guard fixtures confirm structural firing |
| 8 | `detect_tactic_motif` emits 'promotion' when the refuting move promotes to queen and no real tactic fired | VERIFIED | `detect_promotion()` at tactic_detector.py:1526; promotion floor 0.60 (1 TP, 0 FP on TRAIN); priority test green |
| 9 | `detect_tactic_motif` emits 'under-promotion' when the refuting move promotes to a non-queen and no real tactic fired (D-01) | VERIFIED | `detect_under_promotion()` at tactic_detector.py:1545; mutual exclusion with detect_promotion confirmed; _UNDER_PROMOTION_FIXTURES fast-guard green |
| 10 | A non-queen promotion NEVER tags as 'promotion' — it tags as 'under-promotion' (D-01 dominance) | VERIFIED | Detection conditions are mutually exclusive (QUEEN vs non-QUEEN check); registry rank 0 (under-promotion) < rank 1 (promotion); `test_under_promotion_dominates_promotion` priority test green |
| 11 | Move-type motifs sit at TIER_MOVE_TYPE=5, a new lowest tier BELOW hanging-piece (D-03/D-04) | VERIFIED | `TIER_MOVE_TYPE = 5` constant at line 1660; TIER4=4 < TIER_MOVE_TYPE=5; Tier-5 block sits after Tier-4 hanging-piece in dispatcher; `test_real_tactic_beats_promotion_motif` priority test green |
| 12 | en-passant / promotion / under-promotion belong to `MOVE_TYPE_MOTIFS` frozenset mirroring MATE_MOTIFS (D-02) | VERIFIED | `MOVE_TYPE_MOTIFS = frozenset({"en-passant", "promotion", "under-promotion"})` at line 209; directly below MATE_MOTIFS (line 190); discovered-check and trapped-piece NOT in it |
| 13 | All five new motifs have a precision floor or documented no-regression treatment; no existing floor lowered (D-08) | VERIFIED | discovered-check=0.80, promotion=0.60 in PRECISION_FLOOR; trapped-piece/en-passant/under-promotion in SUPPRESSED_MOTIFS with dated measurement notes; discovered-attack floor stayed at 0.15 (was 0.15 before phase; measured 0.163 post-split, still passes) |

**Score:** 13/13 truths verified (11 fully VERIFIED, 2 PRESENT_BEHAVIOR_UNVERIFIED — structurally correct but SUPPRESSED in harness)

---

### Deferred Items

None — all five motifs fully implemented in this phase.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/services/tactic_detector.py` | DISCOVERED_CHECK=25, TRAPPED_PIECE=26; EN_PASSANT=27, PROMOTION=28, UNDER_PROMOTION=29; MOVE_TYPE_MOTIFS frozenset; TIER_MOVE_TYPE=5; five detector functions; Tier-2 and Tier-5 dispatch | VERIFIED | All IntEnum values, Literals, _INT_TO_MOTIF entries, detector functions, and registry entries confirmed present; dispatch wiring confirmed |
| `tests/scripts/tagger/motif_theme_map.py` | discoveredCheck, trappedPiece, enPassant, promotion, underPromotion entries | VERIFIED | All five theme-map entries present at lines 52-54, 78-80 |
| `tests/scripts/tagger/precision_floors.py` | PRECISION_FLOOR entries for discovered-check and promotion; trapped-piece/en-passant/under-promotion in SUPPRESSED_MOTIFS; dated measurement note | VERIFIED | discovered-check=0.80, promotion=0.60 in PRECISION_FLOOR; three motifs in SUPPRESSED_MOTIFS; Phase 128.1-02 measurement note dated 2026-06-20 present |
| `tests/services/test_tactic_detector.py` | 29-motif encoding count; positive/negative fixtures; partition assertion including move-type; priority dispatch tests | VERIFIED | `assert len(_INT_TO_MOTIF) == 29` at line 1475; _DISCOVERED_CHECK_FIXTURES (13), _TRAPPED_PIECE_FIXTURES (13), _EN_PASSANT_FIXTURES, _PROMOTION_FIXTURES, _UNDER_PROMOTION_FIXTURES; _MOVE_TYPE_FIXTURE_SETS partition; priority tests all green |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `detect_tactic_motif` dispatcher | `_GEOMETRIC_REGISTRY` (Tier 2) | discovered-check (rank 4) and trapped-piece (rank 6) in registry | WIRED | Confirmed in _GEOMETRIC_REGISTRY list and _GEOMETRIC_DETECTOR_FNS dict |
| `detect_tactic_motif` dispatcher | `_MOVE_TYPE_REGISTRY` (Tier 5) | under-promotion (rank 0), promotion (rank 1), en-passant (rank 2) in registry | WIRED | Confirmed in _MOVE_TYPE_REGISTRY and _MOVE_TYPE_DETECTOR_FNS; Tier 5 block after Tier 4 hanging-piece |
| `tests/scripts/tagger/test_detector_precision.py` | `precision_floors.py` PRECISION_FLOOR | TRAIN floor gate asserts discovered-check ≥ 0.80, promotion ≥ 0.60 | WIRED | `uv run pytest -n auto tests/scripts/tagger/test_detector_precision.py -x -q` → 1 passed |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Encoding count = 29 | `uv run pytest tests/services/test_tactic_detector.py::TestIntMotifEncoding -x -q` (within full suite) | 62 passed, 5 skipped | PASS |
| Precision floor gate (discovered-check ≥ 0.80, promotion ≥ 0.60) | `uv run pytest -n auto tests/scripts/tagger/test_detector_precision.py -x -q` | 1 passed in 20s | PASS |
| Full type check | `uv run ty check app/ tests/` | All checks passed | PASS |
| Full backend suite | `uv run pytest -n auto -x -q` | 2796 passed, 15 skipped, 3 warnings | PASS |

---

### Requirements Coverage

No formal REQ-IDs (seed-derived). Verification driven from D-01..D-09 in 128.1-CONTEXT.md.

| Decision | Description | Status | Evidence |
|----------|-------------|--------|---------|
| D-01 | under-promotion dominates promotion | SATISFIED | Mutual exclusion detection conditions + rank 0 vs rank 1 in _MOVE_TYPE_REGISTRY |
| D-02 | MOVE_TYPE_MOTIFS frozenset groups en-passant/promotion/under-promotion | SATISFIED | frozenset at line 209, directly below MATE_MOTIFS |
| D-03 | Tier assignment: discovered-check/trapped-piece → Tier 2; move-type → new lowest tier | SATISFIED | TIER2=2, TIER4=4, TIER_MOVE_TYPE=5; confirmed by _sort_key logic |
| D-04 | Real tactic wins, move-type lowest | SATISFIED | Tier 5 > Tier 4 > Tier 2; priority test confirms real tactic beats promotion-motif |
| D-06 | Strict material gate on trapped-piece (all escapes lose material) | SATISFIED | `_escape_squares_all_lose_material()` with v3 SEE gate (victim_val > cheapest_pov_attacker) |
| D-07 | trapped-piece distinct from hanging-piece | SATISFIED | D-07 exclusion in detector: piece with no legal moves (mate) excluded; piece capturable for free (hanging) excluded |
| D-08 | Precision floors from fresh TRAIN run; never lower existing floor | SATISFIED | Fresh run 2026-06-20; discovered-attack floor maintained at 0.15 (measured 0.163); no existing floor lowered |
| D-09 | No UI/chip code in this phase | SATISFIED | No frontend files modified; MOVE_TYPE_MOTIFS frozenset available for Phase 129 |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/services/tactic_detector.py` | 1533-1535 | `detect_promotion` docstring says "this function is only called if moves[0].promotion == chess.QUEEN" which is factually wrong — the loop always calls all three move-type detectors | Warning (WR-01 from REVIEW.md) | No functional impact; D-01 is enforced by mutually exclusive detection conditions, not short-circuit evaluation. A future developer may misunderstand the rank order as load-bearing. |
| `app/repositories/library_repository.py` | 63-65 | Comment mentions motifs 25/26 are not in FAMILY_TO_MOTIF_INTS but omits 27/28/29 (added in Plan 02, same phase) | Warning (WR-02 from REVIEW.md) | Incomplete documentation — a reader relying on this comment to understand chip-family coverage will not know 27-29 are also unassigned. No functional impact. |
| `tests/services/test_tactic_comparison_service.py` | 164 | `test_family_mapping_covers_all_24_motifs` name is now stale — test correctly covers only 1-24 but name implies completeness | Info (IN-01 from REVIEW.md) | Minor misleading test name; behavior is correct |

No TBD/FIXME/XXX debt markers found in modified files. No new Alembic migrations created.

---

### Human Verification Required

#### 1. End-to-end trapped-piece tagging in the full import pipeline

**Test:** Import a game (or use `reimport_games.py`) containing a board position where the opponent has a piece under attack with all legal escape squares losing material. Inspect the resulting `game_flaws` row's `allowed_tactic_motif` or `missed_tactic_motif` column.
**Expected:** The row stores motif_int=26 (trapped-piece), not motif_int=2 (hanging-piece).
**Why human:** trapped-piece is in SUPPRESSED_MOTIFS (0 TP, 9 FP on TRAIN) because the D-06 strict gate does not match the broader lichess `trappedPiece` definition. The fast-guard fixtures in `_TRAPPED_PIECE_FIXTURES` confirm isolated dispatcher behavior, but real-world end-to-end confirmation requires importing actual game data through the full pipeline with a running server.

#### 2. End-to-end en-passant tagging

**Test:** Construct or find a game with an en-passant capture as the flaw's refuting first move in a position where no fork/pin/skewer/mate or other Tier 1-4 tactic fires. Import the game and inspect the resulting `game_flaws` row.
**Expected:** The row stores motif_int=27 (en-passant).
**Why human:** en-passant is in SUPPRESSED_MOTIFS (NaN — never fires on TRAIN because all 12 enPassant-labeled puzzle positions have a higher-priority real tactic). The detector is structurally correct (confirmed by `_EN_PASSANT_FIXTURES` fast-guard), but real-world firing on a genuine residual PV requires a live import where no real tactic shadows the en-passant move.

---

### Gaps Summary

No functional gaps. The two PRESENT_BEHAVIOR_UNVERIFIED items (trapped-piece D-07 boundary and en-passant real-world firing) are expected outcomes of the D-08 precision-first posture: both detectors are in SUPPRESSED_MOTIFS precisely because the harness cannot validate them statistically. Their structural correctness is confirmed by fast-guard fixtures. These are human-verification items, not blockers.

The two code-review warnings (WR-01: detect_promotion docstring inaccuracy; WR-02: library_repository.py comment incomplete for motifs 27-29) are documentation defects only — no functional behavior is affected.

---

_Verified: 2026-06-20T09:30:00Z_
_Verifier: Claude (gsd-verifier)_
