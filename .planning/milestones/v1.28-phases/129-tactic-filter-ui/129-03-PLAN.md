---
phase: 129-tactic-filter-ui
plan: 03
type: execute
wave: 3
depends_on: ["129-01", "129-02"]
files_modified:
  - frontend/src/components/library/TacticComparisonGrid.tsx
  - frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx
autonomous: true
requirements: [TACUI-08]
must_haves:
  truths:
    - "Each family card in the comparison grid shows two bullet rows: 'Missed {Family}' and 'Allowed {Family}' (per TACUI-08 / D-13)"
    - "The grid renders the server's top-6-by-Missed families first; the remaining families live in a collapsible 'More Tactics' accordion (per TACUI-08 / D-14)"
    - "The grid has no orientation toggle and is independent of the Flaws-tab depth/orientation filters; existing game filters still apply (per TACUI-08 / D-09/D-13)"
    - "Two-bullet cards stack correctly at 375px and the accordion matches the Endgame Statistics Concepts pattern (per TACUI-08 / SC#4)"
  artifacts:
    - path: "frontend/src/components/library/TacticComparisonGrid.tsx"
      provides: "family-grouped two-bullet cards + More Tactics accordion, grouping the up-to-12 orientation-tagged bullets by family"
      contains: "More Tactics"
  key_links:
    - from: "frontend/src/components/library/TacticComparisonGrid.tsx"
      to: "frontend/src/hooks/useLibrary.ts"
      via: "useTacticComparison self-fetch (no orientation arg — grid shows both); bullets grouped by family"
      pattern: "useTacticComparison"
  prohibitions:
    - "No orientation toggle on the comparison grid (D-09)"
    - "No client-side re-sort of families — render server order (top-6-by-Missed first), matching the existing GridBody no-resort contract"
    - "No compact card variant — reuse the same FamilyCard renderer for top-6 and overflow (CONTEXT discretion)"
    - "No new theme tokens; no text-xs; bullet row labels are text-sm regular (do NOT extend the existing font-medium family label to the new rows)"
    - "No useAuth().user.beta_enabled — keep useUserProfile().data.beta_enabled"
---

<objective>
Restructure `TacticComparisonGrid` (TACUI-08) to render two bullet charts per family card ("Missed
{Family}" + "Allowed {Family}", D-13), select the server's top-6-by-Missed families for the main grid,
and move the rest into a collapsible "More Tactics" accordion (D-14) cloned from the Endgame
Statistics Concepts pattern. The grid has no orientation toggle and is independent of the Flaws-tab
filters (D-09).

Depends on plan 01 (the dual-orientation comparison response with `orientation`-tagged bullets, top-6
families first) and plan 02 (the `TacticBullet.orientation` type mirror).

Purpose: the you-vs-opponent comparison surface must show both orientations per motif and rank the
trainable Missed orientation, completing the third filter/display axis on the stats page.
Output: a two-bullet-per-card grid + More Tactics accordion, mobile-parity, type-checked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-tactic-filter-ui/129-CONTEXT.md
@.planning/phases/129-tactic-filter-ui/129-RESEARCH.md
@.planning/phases/129-tactic-filter-ui/129-PATTERNS.md
@.planning/phases/129-tactic-filter-ui/129-UI-SPEC.md
@.planning/phases/129-tactic-filter-ui/129-01-SUMMARY.md
@.planning/phases/129-tactic-filter-ui/129-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-bullet family cards + More Tactics accordion</name>
  <read_first>
    - .planning/phases/129-tactic-filter-ui/129-01-SUMMARY.md (bullets ordering contract: top-6-by-Missed families first, both orientations each; orientation field present)
    - frontend/src/components/library/TacticComparisonGrid.tsx (full — self-fetch via useTacticComparison; beta gate; TacticBulletRow :189-248; GridBody :261-280, no-resort comment :256-260; TACTIC_COMPARISON_FAMILIES / TACTIC_FAMILY_COLORS / TACTIC_FAMILY_ICON / isTacticDeltaSignificant / tacticDeltaZoneColor; empty/error states)
    - frontend/src/pages/Endgames.tsx:385-400 (Accordion "Endgame Statistics Concepts" clone target — charcoal-texture rounded-md overflow-hidden border-none, AccordionTrigger band, AccordionContent p-4)
    - frontend/src/components/charts/MiniBulletChart.tsx (per-bullet chart + zero-event placeholder — unchanged)
    - frontend/src/components/ui/accordion.tsx
    - frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx (existing — extend, don't create; tests live in the __tests__/ subdir)
    - 129-PATTERNS.md "TacticComparisonGrid.tsx" assignment
    - 129-UI-SPEC.md TacticComparisonGrid section (two-bullet structure, top-6+More Tactics, copywriting bullet-row labels, data-testid table: tactic-grid-missed-{family}/tactic-grid-allowed-{family}/tactic-grid-more-tactics, mobile contract)
  </read_first>
  <files>frontend/src/components/library/TacticComparisonGrid.tsx, frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx</files>
  <behavior>
    - A family with both a missed and an allowed bullet renders one Card with two TacticBulletRows labeled "Missed {Family}" / "Allowed {Family}".
    - The first up-to-6 families (server order) render in the main grid; any further families render inside the "More Tactics" accordion using the same card renderer.
    - When there are 6 or fewer families, no "More Tactics" accordion renders (or it renders empty-collapsed — your discretion, but do not show an empty accordion).
    - Empty/error states use the UI-SPEC copy; beta gate unchanged.
  </behavior>
  <action>
Group `data.bullets` (now up to 12, each carrying `orientation` from plan 01) by `bullet.family`, preserving server order of first appearance (no client re-sort — D-14 ranking is server-side; matches the existing GridBody no-resort comment). Build a `FamilyCard` renderer: one `Card` (`data-testid={`tactic-family-card-${family}`}`) with the family name header (keep the existing `font-medium` family label as-is) and a body containing two `TacticBulletRow`s — the missed bullet first (label "Missed {Family}", testid `tactic-grid-missed-{family}`) then the allowed bullet (label "Allowed {Family}", testid `tactic-grid-allowed-{family}`). New row labels are `text-sm text-muted-foreground` Regular 400 — do NOT extend the existing `font-medium` to them. Reuse `TacticBulletRow` (:189-248) and `MiniBulletChart` unchanged (their delta/popover/zero-event placeholder logic carries over per bullet). Capitalize only the family name in the label ("Missed Fork", not "missed fork").

Split the grouped families into the first 6 (main grid) and the rest (overflow). Render the main grid in the existing `grid grid-cols-1 lg:grid-cols-3 gap-4` container (`data-testid="tactic-comparison-grid"`). Render the overflow families inside an `Accordion` cloned verbatim from `Endgames.tsx:385-400`: `<AccordionItem value="more-tactics" className="charcoal-texture rounded-md overflow-hidden border-none" data-testid="tactic-grid-more-tactics">`, `<AccordionTrigger band>` with `<h3 className="text-base font-semibold text-foreground">More Tactics</h3>`, `<AccordionContent className="p-4">` rendering the SAME `FamilyCard` for each overflow family (no compact variant — CONTEXT discretion). Only render the accordion when there is at least one overflow family.

Keep the self-fetch `useTacticComparison(filters, flawFilter, [])` with NO orientation argument (grid shows both — D-09); the grid is independent of plan 02's depth/orientation store fields. Keep the beta gate `useUserProfile().data.beta_enabled`. Keep the existing empty-state, below-gate, and error-state branches, using the UI-SPEC copy ("Not enough tactic data yet. Import more games or remove filters to see motif rates." / "Failed to load tactic stats. Something went wrong. Please try again in a moment."). No em-dashes; no jargon (no "Wilson"/"CI"/"p-value") in any new copy; min text-sm.

Extend `src/components/library/__tests__/TacticComparisonGrid.test.tsx`: with a mocked dual-orientation response (>6 families), assert (a) each rendered card has both a `tactic-grid-missed-{family}` and `tactic-grid-allowed-{family}` row; (b) exactly the first 6 families render in the main grid and the rest inside `tactic-grid-more-tactics`; (c) the families render in server order (no re-sort); (d) ≤6 families → no More Tactics accordion; (e) no orientation toggle is present in the grid.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/library/__tests__/TacticComparisonGrid.test.tsx</automated>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>Each card shows two orientation-labeled bullets; top-6-by-Missed in the main grid, overflow in a "More Tactics" accordion (same renderer); no orientation toggle; server order preserved; component test + tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend gate (lint + full vitest + tsc -b)</name>
  <read_first>
    - CLAUDE.md "Pre-PR checklist" frontend block + project memory frontend_run_tsc_build
  </read_first>
  <files>(no new files — verification task)</files>
  <action>
Run the full frontend gate to confirm the grid restructure is type-safe, lint-clean, and knip-clean, and did not regress plan 02's surfaces. This is the phase-final frontend merge gate (per RESEARCH Validation Architecture phase gate).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npx tsc -b</automated>
  </verify>
  <done>Frontend lint clean (incl. knip), full vitest green, tsc -b zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comparison API → grid render | aggregate dual-orientation bullets (player-scoped, post-gate) rendered client-side |
| beta gate → grid visibility | grid visible only when `useUserProfile().data.beta_enabled` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-08 | Information disclosure | comparison grid render | accept | Bullets are aggregate you-vs-opponent rates already gated server-side (analyzed_n floor) + beta-gated client-side (126 D-01); no per-game or cross-user detail exposed. Same posture as shipped 126 grid. |
| T-129-09 | Spoofing / EoP | beta-gate bypass | accept | Frontend-only gate by design (126 D-01); no change to posture; new code keeps `useUserProfile().data.beta_enabled`. |
| T-129-SC | Tampering | npm installs | mitigate | No new dependencies (Accordion/Slider/etc. installed in 126); no package install in this plan. |
</threat_model>

<verification>
- `npx tsc -b` zero errors.
- `npm run lint` clean incl. knip.
- `npm test -- --run` green incl. extended grid test.
- Manual/UAT (backstop, SC#4): two-bullet cards stack at 375px; More Tactics accordion matches the Endgame Statistics Concepts visual; grid unaffected by Flaws-tab depth/orientation changes.
</verification>

<success_criteria>
- Two bullets per family card ("Missed {Family}" / "Allowed {Family}").
- Top-6-by-Missed families in the main grid; remaining families in a "More Tactics" accordion (same card renderer).
- No orientation toggle on the grid; independent of Flaws-tab filters.
- All new interactive/layout elements have data-testid; mobile parity at 375px.
</success_criteria>

<output>
Create `.planning/phases/129-tactic-filter-ui/129-03-SUMMARY.md` when done. Note any UAT-only items
(375px visual parity, beta-toggle end-to-end) for the phase verification step.
</output>
