---
phase: 129-tactic-filter-ui
plan: 02
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - frontend/src/types/library.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/hooks/useFlawFilterStore.ts
  - frontend/src/lib/tacticDepth.ts
  - frontend/src/components/filters/TacticDepthFilter.tsx
  - frontend/src/components/filters/FlawFilterControl.tsx
  - frontend/src/components/library/TacticMotifChip.tsx
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/pages/library/FlawsTab.tsx
  - frontend/src/lib/__tests__/tacticDepth.test.ts
  - frontend/src/hooks/__tests__/useFlawFilterStore.test.ts
  - frontend/src/components/library/__tests__/TacticMotifChip.test.tsx
  - frontend/src/components/library/__tests__/FlawCard.test.tsx
autonomous: true
requirements: [TACUI-06, TACUI-07]
must_haves:
  truths:
    - "The Flaws-tab filter shows a Tactic Difficulty depth control (Beginner/Intermediate/Advanced presets + single-handle slider) defaulting to Intermediate (per TACUI-06 / D-01/D-02)"
    - "The Flaws-tab filter shows an Either/Missed/Allowed orientation toggle defaulting to Either, above the Tactic Motif section (per TACUI-06 / D-06/D-07)"
    - "Changing depth or orientation refetches the flaw list (values are in the TanStack query key) (per TACUI-06)"
    - "Flaw chips on the Flaws-tab list-row card (FlawCard) carry a missed:/allowed: text prefix; both render under Either when both exist, one under Missed/Allowed (per TACUI-07 / D-10/D-11). The single-game view (LibraryGameCard/EvalChart) keeps its existing unprefixed chips — out of scope, per RESEARCH Open Questions RESOLVED §3"
    - "isFlawFilterNonDefault treats Either + Intermediate as default (always-on depth filter does not light the dot) (per TACUI-06 / D-02)"
    - "All new controls render at 375px with data-testid + ARIA and in the mobile drawer (per TACUI-06 / SC#4)"
  artifacts:
    - path: "frontend/src/lib/tacticDepth.ts"
      provides: "named depth constants (DEPTH_PRESET_*_MAX in half-plies, DEPTH_SLIDER_*_MOVES in full moves, HALF_PLIES_PER_MOVE, DEPTH_DEFAULT_PRESET), TacticDepthPreset/TacticDepthValue types, derivePreset/presetToMax/sliderToMax (full-move→half-ply) /maxToSlider (half-ply→full-move) /formatDepthSummary (full moves) /depthToQueryParam (half-ply passthrough)"
      min_lines: 40
    - path: "frontend/src/components/filters/TacticDepthFilter.tsx"
      provides: "single-handle depth filter control cloned from OpponentStrengthFilter"
      min_lines: 40
    - path: "frontend/src/types/library.ts"
      provides: "TacticOrientation 3-value union; orientation field on TacticBullet mirroring backend (locks shared-type boundary)"
      contains: "orientation"
  key_links:
    - from: "frontend/src/components/filters/FlawFilterControl.tsx"
      to: "frontend/src/hooks/useFlawFilterStore.ts"
      via: "orientation toggle + TacticDepthFilter write tacticOrientation / tacticDepthPreset / tacticDepthMax into the store via props threaded from FlawsTab"
      pattern: "tacticOrientation"
    - from: "frontend/src/hooks/useLibrary.ts"
      to: "frontend/src/api/client.ts"
      via: "useLibraryFlaws threads tactic_orientation + max_tactic_depth into getFlaws AND the query key"
      pattern: "max_tactic_depth"
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "frontend/src/components/library/TacticMotifChip.tsx"
      via: "dual-chip branch on flawFilter.tacticOrientation passes orientation prop to each chip + orientation-prefixed motifs to TagLegend"
      pattern: "orientation="
  prohibitions:
    - "No new Popover/Radix import in TacticMotifChip.tsx — D-12 narration is chip label + existing shared TagLegend, NOT a re-added per-chip popover (Phase 126 UAT removed it)"
    - "Do NOT modify LibraryGameCard.tsx or EvalChart.tsx — the single-game view diverges (per-game motif aggregation + cycle/hover, no orientation toggle) and is out of scope for the orientation prefix (RESEARCH Open Questions RESOLVED §3); the optional orientation prop keeps those callers backward-compatible"
    - "No new theme tokens — chip color stays the family color; the missed:/allowed: prefix is text only (D-10)"
    - "No text-xs outside the documented TacticMotifChip exception"
    - "No useAuth().user.beta_enabled — beta gate reads useUserProfile().data.beta_enabled (always-null otherwise)"
    - "No magic numbers — all depth thresholds are named constants in tacticDepth.ts"
    - "API param max_tactic_depth is in HALF-MOVES (1:1 with the column). The slider operates and displays in FULL MOVES (D-03); sliderToMax/maxToSlider bridge full-move slider position ↔ half-ply API value via *HALF_PLIES_PER_MOVE. Slider scale, ticks, thumb aria-label, and summary ALL read in full moves and must agree."
---

<objective>
Wire the Flaws-tab filter UI (TACUI-06) and orientation-prefixed flaw chips (TACUI-07): the depth
(difficulty) control, the Either/Missed/Allowed toggle, the store fields, the TanStack query
threading, and the dual-chip rendering — desktop AND mobile drawer. Mirror plan 01's locked schema
into `types/library.ts` (the shared-type boundary; run `tsc -b`).

Every new surface clones an existing twin: the depth control clones `OpponentStrengthFilter` +
`opponentStrength.ts` (single-handle); the toggle clones the "Played as" `ToggleGroup`
(`FilterPanel.tsx:265-286`); the chip gains an optional `orientation` prop.

Purpose: deliver the three filter axes (motif × missed/allowed × depth) on the player's own Flaws
tab, with the always-on Intermediate-default depth filter and Either-default orientation per D-02/D-07.
Output: a usable, mobile-parity filter + correctly-prefixed dual chips, type-checked against plan 01's
schema.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-tactic-filter-ui/129-CONTEXT.md
@.planning/phases/129-tactic-filter-ui/129-RESEARCH.md
@.planning/phases/129-tactic-filter-ui/129-PATTERNS.md
@.planning/phases/129-tactic-filter-ui/129-UI-SPEC.md
@.planning/phases/129-tactic-filter-ui/129-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared types + depth lib (tacticDepth.ts) + store fields + query threading</name>
  <read_first>
    - .planning/phases/129-tactic-filter-ui/129-01-SUMMARY.md (locked half-move unit + bullet.orientation field)
    - frontend/src/types/library.ts:95-322 (FlawListItem/FlawMarker both-orientation fields; TacticBullet :297; TacticComparisonResponse :321)
    - frontend/src/lib/opponentStrength.ts (full — the clone template: constants :6-34, derivePreset :40-46, sliderToRange/rangeToSlider :57-69, formatRangeSummary :87-94, rangeToQueryParams :101-108)
    - frontend/src/hooks/useFlawFilterStore.ts (full — state :7-22, DEFAULT :24-28, isFlawFilterNonDefault :38-46, useSyncExternalStore :48-88)
    - frontend/src/hooks/useLibrary.ts:23 (buildLibraryParams), :135-151 (useTacticComparison key), :176-193 (useLibraryFlaws params + key)
    - frontend/src/api/client.ts:293-340 (getTacticComparison :303, getFlaws :329 tactic_family pattern)
    - 129-PATTERNS.md "tacticDepth.ts" + "useFlawFilterStore.ts" + "Query-threading note" assignments
    - 129-UI-SPEC.md "Companion lib: tacticDepth.ts" constants table + "FlawFilterStore additions"
  </read_first>
  <files>frontend/src/types/library.ts, frontend/src/lib/tacticDepth.ts, frontend/src/lib/__tests__/tacticDepth.test.ts, frontend/src/hooks/useFlawFilterStore.ts, frontend/src/hooks/__tests__/useFlawFilterStore.test.ts, frontend/src/api/client.ts, frontend/src/hooks/useLibrary.ts</files>
  <behavior>
    - UNIT CONTRACT (D-03 — LOCKED): the slider operates and displays in FULL MOVES (slider domain 1..5 moves); the value stored as `maxMoves` and sent to the API is HALF-PLIES (the 1:1 column value). The two are bridged by *2 (full move → half-ply) and the slider-max → null (no cap) escape. So `maxMoves` is always a half-ply integer or null; the slider position is always a full-move integer 1..DEPTH_SLIDER_MAX_MOVES.
    - tacticDepth.derivePreset (operates on the half-ply maxMoves): derivePreset(null)='advanced'; derivePreset(2)='beginner'; derivePreset(6)='intermediate'; derivePreset(4)=null (custom, 2 full moves). These preserve the half-ply column values (2/6/null) at the API boundary.
    - sliderToMax (full-move slider position → half-ply maxMoves): sliderToMax(DEPTH_SLIDER_MAX_MOVES)=null (no cap = Advanced); sliderToMax(1)=2; sliderToMax(3)=6; sliderToMax(2)=4.
    - maxToSlider (half-ply maxMoves → full-move slider position): maxToSlider(null)=DEPTH_SLIDER_MAX_MOVES; maxToSlider(2)=1; maxToSlider(6)=3; maxToSlider(4)=2 (round-trips with sliderToMax for in-domain even values).
    - depthToQueryParam(null)={} (omit); depthToQueryParam(6)={max_tactic_depth:6} (the half-ply value goes to the API unchanged).
    - formatDepthSummary reads in FULL MOVES via the UI-SPEC Copywriting strings ("Beginner (1 move)", "Intermediate (≤ 3 moves deep)", "Advanced (all)", "Custom (≤ N moves)" where N = full moves = ⌈maxMoves/2⌉). The slider ticks, thumb aria-label, and summary ALL read in full moves and agree.
    - store: DEFAULT_FLAW_FILTER includes tacticOrientation='either', tacticDepthPreset='intermediate', tacticDepthMax=DEPTH_PRESET_INTERMEDIATE_MAX (6 half-plies). isFlawFilterNonDefault returns false at those defaults; true when tacticOrientation!=='either' OR tacticDepthPreset!=='intermediate'.
    - query: changing tactic_orientation or max_tactic_depth changes the useLibraryFlaws query key (refetch); the comparison hook key is unchanged (grid shows both).
  </behavior>
  <action>
Types (`types/library.ts`): add `export type TacticOrientation = 'either' | 'missed' | 'allowed';`. Add `orientation: 'missed' | 'allowed'` to the `TacticBullet` type to mirror plan 01's schema option A (per 129-01-SUMMARY). FlawListItem/FlawMarker already expose both motif fields — no change there. This is the shared-type boundary.

`lib/tacticDepth.ts` (NEW): clone `opponentStrength.ts`, collapsing the two-bound range to a single half-ply value `maxMoves: number | null` (the API-boundary value). **The slider domain is FULL MOVES (D-03); the stored/API value is HALF-PLIES.** Named constants (no magic numbers — reconcile the UI-SPEC's mixed-unit numbers per this LOCKED contract):

| Constant | Value | Unit | Rationale |
|----------|-------|------|-----------|
| `DEPTH_PRESET_BEGINNER_MAX` | `2` | half-plies (API) | 1 full move — preserves the 127/128 half-ply column value |
| `DEPTH_PRESET_INTERMEDIATE_MAX` | `6` | half-plies (API) | 3 full moves — default preset |
| `DEPTH_PRESET_ADVANCED_MAX` | `null` | — | no cap |
| `DEPTH_SLIDER_MIN_MOVES` | `1` | full moves (slider) | 1 move deep minimum |
| `DEPTH_SLIDER_MAX_MOVES` | `5` | full moves (slider) | 5 full moves = 10 half-plies; Phase 127 shows near-zero counts beyond ~8 half-plies; slider at this position = Advanced/no-cap |
| `DEPTH_SLIDER_STEP` | `1` | full moves | 1 move per step |
| `HALF_PLIES_PER_MOVE` | `2` | — | full-move ↔ half-ply conversion factor |
| `DEPTH_DEFAULT_PRESET` | `'intermediate'` | — | D-02: always-on, default = Intermediate |

(Reconciliation note for the executor: the UI-SPEC slider table lists `DEPTH_SLIDER_MIN=1`/`DEPTH_SLIDER_MAX=10`/`DEPTH_SLIDER_STEP=1` and labels them ambiguously as both "moves" and "half-plies" — that mixed-unit table is the WARNING-2 defect. **This plan task is the executor's contract and supersedes the UI-SPEC slider numbers:** the slider runs 1..5 in full moves, the API value runs in half-plies via *2. The UI-SPEC summary/copy strings are correct as-is because they are already in full moves.)

Export `type TacticDepthPreset = 'beginner'|'intermediate'|'advanced'` and `type TacticDepthValue = { preset: TacticDepthPreset; maxMoves: number | null }` (`maxMoves` = half-plies, despite the name — keep the field name for continuity but document the unit). Functions:
- `derivePreset(maxMoves)` — half-ply value → preset (matches 2/6/null exactly, else null=custom).
- `presetToMax(preset)` — preset → half-ply value.
- `sliderToMax(sliderMoves)` — **full-move slider position → half-ply value**: `sliderMoves >= DEPTH_SLIDER_MAX_MOVES ? null : sliderMoves * HALF_PLIES_PER_MOVE`.
- `maxToSlider(maxMoves)` — **half-ply value → full-move slider position**: `maxMoves === null ? DEPTH_SLIDER_MAX_MOVES : Math.ceil(maxMoves / HALF_PLIES_PER_MOVE)`.
- `formatDepthSummary(value)` — uses the Copywriting strings IN FULL MOVES ("Beginner (1 move)", "Intermediate (≤ 3 moves deep)", "Advanced (all)", "Custom (≤ N moves)" where N = `Math.ceil(maxMoves / HALF_PLIES_PER_MOVE)`).
- `depthToQueryParam(maxMoves) -> { max_tactic_depth?: number }` — passes the half-ply value through unchanged; omit when null.

LOCKED (D-03): the slider scale, ticks, thumb aria-label ("Maximum tactic depth in moves"), and summary string ALL read consistently in FULL MOVES; the half-ply column values (2, 6, null) are preserved exactly at the API boundary via the `sliderToMax`/`maxToSlider` *2 conversion. Document this unit split in a file-head comment. No em-dashes in the summary strings.

Tests `lib/__tests__/tacticDepth.test.ts` (NEW, Wave 0 gap): assert the full-move-slider ↔ half-ply-API conversion explicitly — `sliderToMax(1)===2`, `sliderToMax(3)===6`, `sliderToMax(DEPTH_SLIDER_MAX_MOVES)===null`; `maxToSlider(2)===1`, `maxToSlider(6)===3`, `maxToSlider(null)===DEPTH_SLIDER_MAX_MOVES`; round-trip `maxToSlider(sliderToMax(n))` for n in 1..5. Assert `derivePreset(2)==='beginner'`, `derivePreset(6)==='intermediate'`, `derivePreset(null)==='advanced'`, `derivePreset(4)===null`. Assert `depthToQueryParam(null)` omits and `depthToQueryParam(6)` yields `{max_tactic_depth:6}` (half-ply passthrough). Assert the full-move summary strings match the UI-SPEC Copywriting table exactly.

Store (`useFlawFilterStore.ts`): add the three fields to `FlawFilterState`, `DEFAULT_FLAW_FILTER`, and update `isFlawFilterNonDefault` to add `|| (filter.tacticOrientation ?? 'either') !== 'either' || (filter.tacticDepthPreset ?? 'intermediate') !== 'intermediate'` (default-guard with optional-chaining, matching the existing `?.length ?? 0` defensiveness). CRITICAL (D-02): non-default = NOT Intermediate / NOT Either, never merely "set". Add `useFlawFilterStore.test.ts` (NEW, Wave 0 gap) asserting defaults read as non-default=false and each off-default value flips it true.

Query threading (`api/client.ts` + `useLibrary.ts`): in `getFlaws`, conditionally include `tactic_orientation` and `max_tactic_depth` in the request params (mirror the existing `tactic_family` conditional-spread idiom). In `useLibraryFlaws`, build those params from the store value via `depthToQueryParam` + `tacticOrientation`, and APPEND both to the query key array (or fold into the params object) so a change refetches (pitfall 4). Leave `useTacticComparison` and `getTacticComparison` unchanged (grid shows both orientations — D-09).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/tacticDepth.test.ts src/hooks/__tests__/useFlawFilterStore.test.ts</automated>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>tacticDepth lib + store + query threading land; new unit tests pass; tsc -b clean against plan 01's schema.</done>
</task>

<task type="auto">
  <name>Task 2: TacticDepthFilter control + orientation toggle in FlawFilterControl + FlawsTab wiring</name>
  <read_first>
    - frontend/src/components/filters/OpponentStrengthFilter.tsx (full — clone: imports/props :1-22, label+InfoPopover+summary :45-90, preset grid :92-114, slider block :116-133)
    - frontend/src/components/filters/FilterPanel.tsx:258-292 ("Played as" ToggleGroup — :265-286 the exact clone target, incl. the `if(!v) return` deselect guard)
    - frontend/src/components/filters/FlawFilterControl.tsx (full — showTacticFilter gating; severity/tags/tacticFamilies sections; where Tactic Motif renders)
    - frontend/src/pages/library/FlawsTab.tsx:45 (NO_FLAW_FILTER literal), :94 (URL-param reconstruction), :213 (pending/applied), :289 (desktop FlawFilterControl), :504 (mobile-drawer FlawFilterControl)
    - frontend/src/components/ui/slider.tsx, info-popover.tsx, toggle-group.tsx
    - 129-PATTERNS.md "TacticDepthFilter.tsx" + "FlawFilterControl.tsx" assignments
    - 129-UI-SPEC.md component inventory (TacticDepthFilter structure, FlawFilterControl placement order, copywriting, data-testid table, mobile contract)
  </read_first>
  <files>frontend/src/components/filters/TacticDepthFilter.tsx, frontend/src/components/filters/FlawFilterControl.tsx, frontend/src/pages/library/FlawsTab.tsx</files>
  <behavior>
    - TacticDepthFilter: the slider operates in FULL MOVES (1..DEPTH_SLIDER_MAX_MOVES). Clicking a preset chip snaps the slider + maxMoves atomically and updates the summary; dragging the slider clears the active preset (custom summary); slider at DEPTH_SLIDER_MAX_MOVES = Advanced/no cap (maxMoves=null); summary text goes text-toggle-active when preset !== Intermediate. Slider ticks/thumb-aria/summary all read in full moves and agree (WARNING-2 fix).
    - Orientation toggle: single-select, cannot deselect-all (empty onValueChange ignored); default Either active.
    - Both sections render only when showTacticFilter, above the Tactic Motif section, in BOTH desktop and mobile-drawer FlawFilterControl instances.
  </behavior>
  <action>
`TacticDepthFilter.tsx` (NEW): clone `OpponentStrengthFilter.tsx`. Props `{ value: TacticDepthValue; onChange: (next: TacticDepthValue) => void }`. Single-handle slider operating in FULL MOVES (D-03): `min={DEPTH_SLIDER_MIN_MOVES}` (1), `max={DEPTH_SLIDER_MAX_MOVES}` (5), `step={DEPTH_SLIDER_STEP}`, `value={[maxToSlider(value.maxMoves)]}`, single `thumbLabels={['Maximum tactic depth in moves']}`, drop `minStepsBetweenThumbs`; in the change handler read `values[0]` (a full-move position) and map through `sliderToMax` to the half-ply `maxMoves`, then `onChange({ preset: derivePreset(max) ?? value.preset, maxMoves: max })`. Preset chips in `grid grid-cols-3 gap-1 mb-3` (Beginner/Intermediate/Advanced — note the 3-col exception vs the 4-col opponent grid), each a `<button type="button">` with `aria-pressed`, `h-11 sm:h-7 text-sm`, the active/inactive class pair from the pattern; a preset click sets `{ preset, maxMoves: presetToMax(preset) }`. Label "Tactic Difficulty" with the `InfoPopover` (testId `filter-tactic-depth-info`) whose body is the UI-SPEC copy INCLUDING the mate-exempt sentence "Forced mates always show, regardless of difficulty." (D-04). Summary `<span data-testid="filter-tactic-depth-summary">` = `formatDepthSummary(value)` (reads in full moves), `text-toggle-active` when the derived preset is not Intermediate (D-02 non-default rule). Root `<div data-testid="filter-tactic-depth">`. Slider tick labels show the full-move endpoints `DEPTH_SLIDER_MIN_MOVES` (`1`) / `DEPTH_SLIDER_MAX_MOVES` (`5`) — NOT half-plies; the ticks, thumb aria-label, and summary must all read in full moves and agree (WARNING-2 fix). data-testids per the UI-SPEC table (`filter-tactic-depth-presets`, `filter-tactic-depth-preset-{beginner|intermediate|advanced}`, `filter-tactic-depth-slider`). No text-xs. No em-dashes.

`FlawFilterControl.tsx`: add four props — `orientation?: TacticOrientation` (default 'either'), `onOrientationChange?`, `tacticDepth?: TacticDepthValue`, `onTacticDepthChange?`. Insert two `showTacticFilter`-gated sections ABOVE the existing Tactic Motif section, in this order: (1) Orientation `ToggleGroup` cloned verbatim from `FilterPanel.tsx:265-286` — relabel "Orientation", values `either/missed/allowed`, testIds `filter-tactic-orientation` + `-either/-missed/-allowed`, items `min-h-11 sm:min-h-0 flex-1 text-sm`, KEEP the `if (!v) return;` deselect guard (D-06). (2) `<TacticDepthFilter value={tacticDepth} onChange={onTacticDepthChange} />`. Both render automatically in the mobile drawer because the drawer renders `FlawFilterControl` (no separate path).

`FlawsTab.tsx`: thread the new store fields through. Update the `NO_FLAW_FILTER` literal (:45) and the URL-param reconstruction (:94) to include `tacticOrientation`/`tacticDepthPreset`/`tacticDepthMax` (tsc -b will catch omissions; remember `tacticDepthMax` is the HALF-PLY value). Carry the new fields through the pending/applied filter pattern (`pendingFlawFilter`/`appliedFilters`, :213) and pass `orientation`/`onOrientationChange`/`tacticDepth`/`onTacticDepthChange` to BOTH the desktop (:289) and mobile-drawer (:504) `FlawFilterControl` instances. Construct the `TacticDepthValue` passed down as `{ preset: tacticDepthPreset, maxMoves: tacticDepthMax }`; the `onTacticDepthChange` callback receives the next `TacticDepthValue` (already reconciled by `TacticDepthFilter` via `presetToMax`/`sliderToMax`) and writes its `preset` + `maxMoves` (half-ply) back into the store. The control owns all full-move↔half-ply conversion (do NOT convert in FlawsTab — it stores/threads half-plies). Keep desktop + mobile in lock-step (CLAUDE.md mobile-parity rule).
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>Depth control + orientation toggle render in FlawFilterControl (desktop + mobile drawer) above Tactic Motif, gated on showTacticFilter, with full data-testid/ARIA; FlawsTab threads the new fields through pending/applied + both control instances; lint + tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orientation-prefixed chips + dual-chip rendering on FlawCard</name>
  <read_first>
    - frontend/src/components/library/TacticMotifChip.tsx (full — label/aria/testid :91-92,124; text-xs font-bold :80; NO popover — definitions via TagLegend)
    - frontend/src/components/library/FlawCard.tsx:270-298 (single-chip render, beta gate useUserProfile().data :278, TagLegend feed :289-298)
    - frontend/src/components/library/__tests__/TacticMotifChip.test.tsx, frontend/src/components/library/__tests__/FlawCard.test.tsx (existing — extend, don't create; tests live in the __tests__/ subdir)
    - 129-PATTERNS.md "TacticMotifChip.tsx" + "FlawCard.tsx" assignments (LANDMINE: no re-added popover)
    - 129-UI-SPEC.md TacticMotifChip + dual-chip rules + copywriting chip-label table
    - 129-RESEARCH.md Pitfall 1 (UI-SPEC popover staleness) + Open Questions (RESOLVED) §3 (single-game card path scope)
  </read_first>
  <files>frontend/src/components/library/TacticMotifChip.tsx, frontend/src/components/library/FlawCard.tsx, frontend/src/components/library/__tests__/TacticMotifChip.test.tsx, frontend/src/components/library/__tests__/FlawCard.test.tsx</files>
  <behavior>
    - Chip with orientation='missed': label "missed: {motif}"; aria-label "Tactic: missed {motif} — {definition}" (space, not colon, in aria); testid chip-tactic-missed-{motif}-{flawId}.
    - Chip without orientation: unchanged label/aria/testid (existing callers unaffected).
    - FlawCard under Either: both chips when both motifs non-null, one when one. Under Missed: missed chip only. Under Allowed: allowed chip only. TagLegend receives orientation-prefixed motif entries.
  </behavior>
  <action>
`TacticMotifChip.tsx`: add optional `orientation?: 'missed' | 'allowed'`. When set: label = `${orientation}: ${motif}`; aria-label = `Tactic: ${orientation} ${motif} — ${definition}` (space in aria, colon in visible label per UI-SPEC); data-testid = `chip-tactic-${orientation}-${motif}-${flawId}`. When unset: keep the exact current label/aria/testid (backward compatible). Color/icon/size unchanged; the prefix inherits the existing `text-xs font-bold`. PROHIBITED: adding any Popover/Radix import — D-12 narration is the chip label + the existing shared `TagLegend`, NOT a re-added per-chip popover (pitfall 1).

`FlawCard.tsx` (the Flaws-tab list-row card — the ONLY orientation-prefix site, per RESEARCH Open Questions RESOLVED §3): accept the `flawFilter` (or just `tacticOrientation`) as a prop threaded from `FlawsTab` (keep the card pure — RESEARCH A6). Branch the chip render on the orientation: Missed → render the missed chip only (`flaw.missed_tactic_motif`, orientation="missed") when non-null; Allowed → allowed chip only; Either → both chips when both motifs non-null, one when one. Pass `orientation` to each `TacticMotifChip` (currently the card renders a single unprefixed `allowed_tactic_motif` chip at `FlawCard.tsx:278-279`). Feed the `TagLegend` (`FlawCard.tsx:289-298`) the orientation-prefixed motif list matching the rendered chips (so the legend explains "missed: fork" / "allowed: fork"). Keep the beta gate as `useUserProfile().data.beta_enabled`. `FlawsTab.tsx:383` renders `<FlawCard>` at a single site (no separate mobile render path — the grid reflows), so thread the orientation prop there once.

SCOPE (RESOLVED Open Question 3 — no in-execution confirmation needed): the orientation prefix + dual-chip logic lands in `FlawCard.tsx` ONLY. The single-game view (`LibraryGameCard.tsx` chip row at :628-649, and the `EvalChart.tsx` tooltip at :638-893) does NOT reuse `TacticMotifChip` the same way — it aggregates *unique* `allowed_tactic_motif` values per-game and drives click-to-cycle/hover on the eval chart, and it has no orientation toggle in its context (D-09). Those sites keep their current unprefixed chips (`TacticMotifChip` with no `orientation` prop — backward-compatible because the prop is optional). They already satisfy D-12's narration mechanism (chip + shared `TagLegend`, no inline sentence). Do NOT modify `LibraryGameCard.tsx` or `EvalChart.tsx` in this plan — they are intentionally absent from `files_modified`.

Extend `src/components/library/__tests__/TacticMotifChip.test.tsx`: assert prefixed label/aria/testid when orientation set, and unchanged output when unset. Extend `src/components/library/__tests__/FlawCard.test.tsx`: assert the D-11 dual-chip matrix (Either→both/one, Missed→missed-only, Allowed→allowed-only) and that no per-chip popover is rendered.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/library/__tests__/TacticMotifChip.test.tsx src/components/library/__tests__/FlawCard.test.tsx</automated>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>Chips carry the orientation prefix in label/aria/testid; FlawCard renders the D-11 dual-chip matrix; no re-added popover; TagLegend gets prefixed motifs; component tests + tsc pass.</done>
</task>

<task type="auto">
  <name>Task 4: Frontend gate (lint + full vitest + tsc -b)</name>
  <read_first>
    - CLAUDE.md "Pre-PR checklist" frontend block + "Frontend > Browser Automation Rules" + project memory frontend_run_tsc_build
  </read_first>
  <files>(no new files — verification task)</files>
  <action>
Run the full frontend gate to confirm tasks 1-3 are type-safe and lint-clean and that knip finds no dead exports (new tacticDepth exports must be imported; remove any unused). esbuild strips types so `npx tsc -b` is the only type gate — it is non-negotiable here (shared-type boundary). This is the per-wave merge gate.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npx tsc -b</automated>
  </verify>
  <done>Frontend lint clean (incl. knip), full vitest green, tsc -b zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client filter state → API query params | depth/orientation store values become `max_tactic_depth` + `tactic_orientation` request params (validated server-side in plan 01) |
| beta gate → tactic UI visibility | new controls/chips visible only when `useUserProfile().data.beta_enabled` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-05 | Spoofing / EoP | beta-gate bypass | accept | Frontend-only gate by design (126 D-01); tactic data is not hard-gated server-side (deferred). No new exposure — same posture as shipped 126/128. New code keeps `useUserProfile().data.beta_enabled` (not the always-null `useAuth().user`). |
| T-129-06 | Tampering | client-crafted depth/orientation params | mitigate | Validation happens server-side (plan 01 T-129-01/02: closed enum + bound int). Frontend sends well-formed values via `depthToQueryParam` (omits when null); no trust placed in client beyond what the server re-validates. |
| T-129-07 | Information disclosure | flaw chip rendering | accept | Flaws list is player-only (`player_only_gate`); the card only renders the user's own motifs already returned by the API. No cross-user data. |
| T-129-SC | Tampering | npm installs | mitigate | No new dependencies (RESEARCH: all primitives installed in 126); no package install in this plan. |
</threat_model>

<verification>
- `npx tsc -b` zero errors (shared-type boundary — the only type gate).
- `npm run lint` clean incl. knip (no dead tacticDepth exports).
- `npm test -- --run` green incl. new tacticDepth/store tests + extended chip/card tests.
- Manual/UAT (backstop, SC#4): controls render at 375px in the mobile drawer; filter dot does NOT light at Either+Intermediate default.
</verification>

<success_criteria>
- Tactic Difficulty depth control (presets + single-handle slider, Intermediate default) + Either/Missed/Allowed toggle (Either default) render above Tactic Motif in the Flaws-tab filter, desktop + mobile drawer.
- Changing depth/orientation refetches the flaw list (query key includes them).
- Chips carry the missed:/allowed: prefix; D-11 dual-chip matrix correct; narration via chip + TagLegend (no re-added popover).
- isFlawFilterNonDefault treats Either + Intermediate as default.
- All new interactive elements have data-testid + ARIA + semantic HTML.
</success_criteria>

<output>
Create `.planning/phases/129-tactic-filter-ui/129-02-SUMMARY.md` when done. Note the final
`TacticDepthValue` shape + the `flawFilter`/orientation prop threading into `FlawCard` so plan 03's
checker can confirm grid independence from these filters.
</output>
