---
phase: 131-tactic-precision-hardening-cook-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 131-02
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/precision_floors.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "back-rank-mate fires only when all three of the king's forward squares are blocked by the defender's OWN pieces and at least one checker is on the back rank (D-09)."
    - "anastasia-mate validates the opponent blocker at king+1 and the pov knight at king+3 in the file-normalized position (D-09)."
    - "hook-mate validates the full rook-knight-pawn defender chain (D-09)."
    - "The lock-against-regression motifs hold their floors: mate 1.00, smothered-mate 1.00, double-check 1.00, discovered-check ≥0.85, hanging-piece 0.95 (D-09)."
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "Hardened detect_back_rank_mate (own-blocker test), detect_anastasia_mate, detect_hook_mate"
      contains: "def detect_back_rank_mate"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Raised floors for back-rank/anastasia/hook; discovered-check floor locked ≥0.85"
  key_links:
    - from: "detect_back_rank_mate"
      to: "king forward-square own-blocker test"
      via: "relative-rank arithmetic per pov color (Pitfall 3)"
      pattern: "back_rank|forward"
---

<objective>
Harden the three named-mate motifs that fall short of 0.90: back-rank-mate (0.27 — the biggest
mate fix), anastasia-mate (0.86), hook-mate (0.84). back-rank's 0.271 is driven by firing on
corner mates with no forward squares; the fix is cook's own-blocker test plus the back-rank-checker
requirement (D-09). Then lock the never-regress floors for the already-strong motifs (mate 1.00,
smothered 1.00, double-check 1.00, discovered-check ≥0.85, hanging-piece 0.95).

This plan runs in its own wave (Wave 3) because it edits the same files as plan 02
(tactic_detector.py + precision_floors.py) — sequential to avoid file conflicts.

Purpose: bring all in-scope mate motifs to the 0.90 bar. Output: hardened mate detectors +
locked regression floors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md
@.planning/notes/tactic-tagger-cook-alignment.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden detect_back_rank_mate with cook's own-blocker test + back-rank-checker requirement</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (detect_back_rank_mate ~773-801)
    - .planning/notes/tactic-tagger-cook-alignment.md (back-rank-mate section — the three-forward-square own-blocker test)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md ("back-rank-mate" divergence audit; Pitfall 3 — the per-pov-color relative-rank arithmetic, the critical off-by-one)
  </read_first>
  <action>
    Rebuild `detect_back_rank_mate` to cook (D-10, from prose): require the final board `is_checkmate` (or the `has_forced_mate` override from plan 01) AND the defender king on its back rank; build the king's three FORWARD squares (one straight + two diagonals, clipped at the a/h files) using the per-pov relative-rank offset per Pitfall 3 (for pov=White delivering mate, the Black king is on rank 7 and forward = `king - 8`; for pov=Black, White king on rank 0, forward = `king + 8`); for each forward square, if it is empty OR holds a pov piece OR is attacked by pov → NOT a back-rank mate (the three squares must all be the defender's OWN blockers); finally require at least one checker ON the back rank. Extract the forward-offset and clip-file values to named constants (no magic numbers). Verify against a known back-rank-mate fixture that the own-blocker test eliminates the corner-mate false positives that produced 0.271.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
  </verify>
  <acceptance_criteria>
    - back-rank-mate TEST precision rises above 0.90 in the harness report (was 0.271).
    - The own-blocker test rejects corner mates (a forward square that is empty or pov-attacked → not fired) — covered by a fixture or the precision move.
    - `uv run ty check app/ tests/` exits 0.
    - No cook.py source pasted (D-10).
  </acceptance_criteria>
  <done>back-rank-mate has the own-blocker test + back-rank-checker requirement with correct per-pov-color arithmetic; TEST precision >0.90.</done>
</task>

<task type="auto">
  <name>Task 2: Lift detect_anastasia_mate and detect_hook_mate to >0.90</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (detect_anastasia_mate ~864-899; detect_hook_mate ~902-946)
    - .planning/notes/tactic-tagger-cook-alignment.md (anastasia-mate + hook-mate sections — the king+1 blocker / king+3 knight geometry; the rook-knight-pawn chain)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md ("anastasia-mate", "hook-mate" divergence audits)
  </read_first>
  <action>
    Tighten `detect_anastasia_mate` to cook (D-10, from prose): king on the a- or h-file but NOT a corner; mating move is a queen or rook on the king's file; normalize by flipping to the h-file; require an OPPONENT blocker on `king+1` and a POV KNIGHT specifically on `king+3` (the current code accepts any pov knight anywhere — tighten to the king+3 geometry in the normalized position). Tighten `detect_hook_mate` to cook: confirm the rook-adjacent-to-king test uses `boards[-1].attacks(opp_king_sq)` (already correct per RESEARCH); harden the multi-knight / multi-pawn handling of the rook←knight←pawn defender chain so a spurious unrelated knight/pawn does not satisfy the chain. Extract any new offset constants (king+1, king+3) to named constants. Both are small lifts (0.86→>0.9, 0.84→>0.9).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
  </verify>
  <acceptance_criteria>
    - anastasia-mate and hook-mate TEST precision each rise above 0.90 in the harness report.
    - anastasia-mate validates the king+3 knight in the normalized position (not any pov knight); hook-mate validates the specific defender chain.
    - `uv run ty check app/ tests/` exits 0.
    - No cook.py source pasted (D-10).
  </acceptance_criteria>
  <done>anastasia-mate and hook-mate cleared to >0.90 TEST via the cook geometry tightening.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the never-regress floors for the strong motifs</name>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <read_first>
    - tests/scripts/tagger/precision_floors.py (PRECISION_FLOOR lines 149-175; the discovered-check floor at 0.80)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md (D-09 lock-against-regression set: mate 1.00, smothered 1.00, double-check 1.00, discovered-check ≥0.85, hanging-piece 0.95)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md ("Motifs that must NOT regress" table)
  </read_first>
  <action>
    In tests/scripts/tagger/precision_floors.py, lock the regression set per D-09: confirm/raise floors so mate, smothered-mate, and double-check remain gated at their 1.00 measurements (floors ~5-8pp below as already set); RAISE the discovered-check floor to lock ≥0.85 (currently 0.80) so the plan-02 discovered-attack rebuild cannot silently regress it; confirm hanging-piece floor locks the 0.95 puzzle precision. Also raise the back-rank/anastasia/hook floors to ~5-8pp below the new measured TEST values from Tasks 1-2. Record the final per-motif TEST precision table for all in-scope motifs in the SUMMARY (the D-09 ship audit).
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py</automated>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'discovered-check' tests/scripts/tagger/precision_floors.py` shows a floor ≥0.85.
    - mate, smothered-mate, double-check, hanging-piece floors lock their measured values (no regression).
    - `uv run pytest tests/scripts/tagger/test_detector_precision.py` passes.
    - The SUMMARY records the final per-motif TEST precision for all in-scope motifs (D-09 audit).
  </acceptance_criteria>
  <done>Regression floors locked (discovered-check ≥0.85, hanging-piece 0.95, mates at measured); per-motif TEST audit recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as plans 01/02: pure-CPU detector edits, no new endpoints/input/auth/network. No new
trust boundary.

| Boundary | Description |
|----------|-------------|
| stored PV → detector | already-validated data read at classify time; no new ingress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04 | Tampering (data-integrity, NOT security) | hardened mate detectors (wrong visible mate chip) | mitigate | >0.90 TEST precision gate for back-rank/anastasia/hook + the never-regress floor lock (D-09). Correctness defect, not an attack vector. |
| T-131-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. No install tasks → no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `uv run ty check app/ tests/` exits 0.
- `uv run pytest tests/services/test_tactic_detector.py -x` passes.
- `uv run pytest tests/scripts/tagger/test_detector_precision.py` passes.
- `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test` — back-rank/anastasia/hook at >0.90 TEST; regression set holds floors.
- No cook.py source pasted (D-10).
</verification>

<success_criteria>
- back-rank-mate, anastasia-mate, hook-mate all >0.90 on TEST.
- Regression set holds: mate/smothered/double-check at 1.00, discovered-check ≥0.85, hanging-piece 0.95.
- Final per-motif TEST precision audit recorded.
</success_criteria>

<output>
Create `.planning/phases/131-tactic-precision-hardening-cook-alignment/131-03-SUMMARY.md` when done
</output>
