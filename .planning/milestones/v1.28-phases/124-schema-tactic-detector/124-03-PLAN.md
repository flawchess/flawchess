---
phase: 124-schema-tactic-detector
plan: 03
type: execute
wave: 3
depends_on: ["124-02"]
files_modified:
  - tests/services/test_tactic_detector.py
autonomous: true
requirements: [TACDET-03]
must_haves:
  truths:
    - "Each motif has 10-15 hand-labeled positive fixtures drawn from inspection of own prod flaws, NOT from cook.py output (D-09)"
    - "Core 8 detectors achieve >=90% precision; tier-3 and named-mate detectors achieve >=95% precision (D-10)"
    - "A shared hard-negative set (quiet errors, simple recaptures) returns None for every detector — the hanging-piece catch-all does not over-fire"
    - "A motif that misses its precision bar is documented as query-suppressed (stored but never surfaced) rather than mis-tagged (D-10)"
    - "Recall is NOT gated (precision-first, D-10)"
  artifacts:
    - path: "tests/services/test_tactic_detector.py"
      provides: "Per-motif fixture sets + shared hard-negatives + precision harness + priority-order tests"
      contains: "def _compute_precision"
    - path: "tests/services/test_tactic_detector.py"
      provides: "_QUERY_SUPPRESSED_MOTIFS frozenset documenting which detectors are query-suppressed (Phase 125/126 gate input)"
      contains: "_QUERY_SUPPRESSED_MOTIFS"
  key_links:
    - from: "tests/services/test_tactic_detector.py"
      to: "app/services/tactic_detector.py:detect_tactic_motif"
      via: "fixture-driven precision assertions over the public detector"
      pattern: "detect_tactic_motif"
---

<objective>
Build the hand-labeled fixture validation set and the precision-measurement harness that enforces the D-10 bars (Core 8 >=90%, tier-3 + named-mate >=95%), proving the detector is precision-first (TACDET-03). Fixtures are labeled by inspection of our own prod flaws (game_flaws + game_positions.pv on the dev DB), NEVER by running cook.py. Detectors that miss their bar are left query-suppressed (documented), never mis-tagged.

Purpose: this is the accuracy gate that Phase 125 backfill depends on; it is the named technical risk of the milestone (Q-011).
Output: `tests/services/test_tactic_detector.py` with per-motif positives, a shared hard-negative set, the precision harness, and priority-order tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-schema-tactic-detector/124-CONTEXT.md
@.planning/phases/124-schema-tactic-detector/124-RESEARCH.md
@.planning/phases/124-schema-tactic-detector/124-PATTERNS.md
@app/services/tactic_detector.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Label fixtures from prod flaws + build the test scaffold</name>
  <files>tests/services/test_tactic_detector.py</files>
  <read_first>
    - tests/services/test_flaws_service.py (the _make_pos pattern + class-per-concern layout; the closest precedent)
    - .planning/phases/124-schema-tactic-detector/124-RESEARCH.md (Fixture Validation Approach: fixture format (fen_before_flaw, move_san_of_flaw, pv_str, expected_motif), the precision harness, the hard-negative set; the verified prod example game 975197 user 44)
    - .planning/notes/tactic-tagging-architecture.md (the prod flaw_ply+1 mapping table — how to read pv at flaw_ply+1 when sampling fixtures)
  </read_first>
  <action>
    Create `tests/services/test_tactic_detector.py`. Source candidate fixtures by querying the dev DB via the `mcp__flawchess-db__query` tool: join `game_flaws` (severity in mistake/blunder) to `game_positions.pv` at `flaw_ply + 1`, pull the FEN-before (from game_flaws.fen), move_san (from game_positions at flaw_ply), and pv string, for a spread of games. Label each candidate BY INSPECTION (set up the board, eyeball the refutation line, assign the motif) — NOT by running cook.py (D-09). Build per-motif positive fixture lists `_FORK_FIXTURES`, `_PIN_FIXTURES`, ... each a `list[tuple[str, str, str, str]]` of `(fen_before_flaw, move_san_of_flaw, pv_str, expected_motif)`, targeting 10-15 positives per motif (Core 8 + named-mate + tier-3). For motifs where 10-15 clean prod positives cannot be found, supplement with hand-constructed textbook positions (still hand-labeled, still not cook-derived) and note the source in a comment. Add the shared `_HARD_NEGATIVES: list[tuple[str, str, str]]` (quiet positional errors, simple recaptures) that must all return None. Add the `_compute_precision(results) -> float` harness (RESEARCH: TP/(TP+FP)) and a `_run_fixtures(fixtures) -> list[tuple[str|None, str|None]]` helper that reconstructs board_after_flaw (parse move_san onto fen), calls detect_tactic_motif, and maps the returned int back to a motif string via _INT_TO_MOTIF for comparison. Lay out test classes per RESEARCH/PATTERNS (TestTacticMotifInt, one Test<Motif>Detector per family, TestPriorityOrder, TestPrecisionBars). This task delivers the scaffold + fixtures + at least the Core-8 fixture lists populated; tier-3/named-mate fixtures may be stubbed lists this task and completed in Task 2.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check tests/services/test_tactic_detector.py && uv run pytest -n auto tests/services/test_tactic_detector.py -k "Fork or HangingPiece or Pin or Skewer or DoubleCheck or Discovered or Mate or Priority" -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/services/test_tactic_detector.py` exists and `uv run ty check` passes for it.
    - Each Core-8 motif has a populated fixture list with >=10 entries (`grep -c '("' tests/services/test_tactic_detector.py` reflects the populated fixtures; reviewer confirms >=10 per Core-8 motif).
    - `_compute_precision` and `_run_fixtures` are defined; the harness maps detector int output back to motif strings via `_INT_TO_MOTIF`.
    - The Core-8 + priority + mate test selection runs (passing or with documented query-suppressed motifs — see Task 2 for the final bar enforcement).
    - A comment block records the labeling source (prod dev-DB sample + any textbook supplements) and explicitly states fixtures were NOT cook.py-derived.
  </acceptance_criteria>
  <done>Test file exists with the harness, Core-8 fixtures (>=10 each), hard-negatives, and class layout; labeled by inspection, not cook.py.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-3 + named-mate fixtures and enforce the D-10 precision bars</name>
  <files>tests/services/test_tactic_detector.py</files>
  <read_first>
    - tests/services/test_tactic_detector.py (the scaffold + harness from Task 1)
    - .planning/phases/124-schema-tactic-detector/124-CONTEXT.md (D-10 tiered bars: Core >=90%, tier-3 + named-mate >=95%; query-suppression policy; D-08 provisional intra-tier order)
    - .planning/phases/124-schema-tactic-detector/124-RESEARCH.md (Open Question 2: some tier-3 detectors may not clear the bar on first attempt — plan for query-suppression, do not lower the bar)
  </read_first>
  <action>
    Populate the tier-3 (deflection, attraction, intermezzo, x-ray, interference, self-interference, clearance, capturing-defender, sacrifice) and named-mate (smothered, anastasia, hook, arabian, boden, double-bishop, dovetail) fixture lists (10-15 positives each, same hand-labeled discipline). Add `TestPrecisionBars` assertions: a parametrized test per motif computing `_compute_precision` over (that motif's positives + the shared hard-negatives) and asserting `>= CORE_PRECISION_BAR` (0.90) for Core 8 or `>= TIER3_PRECISION_BAR` (0.95) for tier-3 + named-mate. Define those two bars as named module constants. For any detector that genuinely cannot clear its bar after honest tuning, mark it query-suppressed: add it to a `_QUERY_SUPPRESSED_MOTIFS: frozenset[TacticMotif]` constant WITH a comment explaining why, assert it explicitly (its precision is recorded but the bar assertion is xfail/skip with a documented reason), and confirm the detector still STORES (returns a motif int) — suppression is a query-time decision, never a detect-time NULL (D-11). Do NOT lower a bar to make a flaky detector pass. Add a TestPriorityOrder case proving a forced-mate-plus-fork PV returns the mate motif (D-07 mate dominance) and a hanging-only PV returns hanging-piece (catch-all last). Recall is NOT asserted anywhere (precision-first, D-10).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check tests/services/test_tactic_detector.py && uv run pytest -n auto tests/services/test_tactic_detector.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -n auto tests/services/test_tactic_detector.py -q` is GREEN (passing, with any sub-bar detectors marked xfail/skip carrying a documented query-suppression reason — never a silently-lowered bar).
    - `CORE_PRECISION_BAR == 0.90` and `TIER3_PRECISION_BAR == 0.95` exist as named constants; the per-motif precision tests assert against them.
    - Tier-3 and named-mate motifs each have >=10 hand-labeled positive fixtures.
    - The shared hard-negative set returns None for every motif (no catch-all over-fire) — asserted.
    - Any query-suppressed motif is in `_QUERY_SUPPRESSED_MOTIFS` with a reason comment AND a test confirming it still returns a non-None motif int (stored, not detect-time NULL).
    - Priority-order tests pass: mate-dominance and hanging-piece-last both asserted.
    - No test asserts recall (grep confirms precision-only).
  </acceptance_criteria>
  <done>All motifs have hand-labeled fixtures; the D-10 bars are enforced; sub-bar detectors are query-suppressed with documented reasons; priority order proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No runtime input, network, auth, or user surface. Fixtures are static hand-labeled data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-05 | Tampering | fixture provenance (AGPL) | mitigate | Fixtures labeled by human inspection of own prod flaws / textbook positions; explicitly NOT cook.py output (D-09) — recorded in a file comment. No AGPL-derived labels. |
| T-124-06 | Repudiation | precision bar integrity | mitigate | Bars are named constants (0.90 / 0.95); a sub-bar detector is query-suppressed with a documented reason, never passed by lowering the constant. Reviewer-checkable. |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No package installs. No supply-chain surface. |
</threat_model>

<verification>
- `uv run ty check tests/services/test_tactic_detector.py` zero errors.
- `uv run pytest -n auto tests/services/test_tactic_detector.py -q` green.
- Every motif has >=10 hand-labeled positives; hard-negatives all return None.
- D-10 bars enforced as named constants; query-suppressed motifs documented + still storing.
- Priority order (mate dominance, hanging-last) asserted.
</verification>

<success_criteria>
- Hand-labeled per-motif fixtures (10-15 each) from own prod flaws, not cook.py (TACDET-03, D-09).
- Core >=90% / tier-3 + named-mate >=95% precision enforced; sub-bar detectors query-suppressed (D-10).
- Recall not gated; catch-all does not over-fire.
</success_criteria>

<output>
Create `.planning/phases/124-schema-tactic-detector/124-03-SUMMARY.md` when done.
</output>
