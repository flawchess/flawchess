---
phase: 124-schema-tactic-detector
plan: 04
type: execute
wave: 4
depends_on: ["124-01", "124-02", "124-03"]
files_modified:
  - app/services/flaws_service.py
  - tests/services/test_flaws_service.py
autonomous: true
requirements: [TACDET-04]
must_haves:
  truths:
    - "detect_tactic_motif runs inside _build_flaw_record so every classify_game_flaws call populates the three tactic fields for both the player's and the opponent's flaws (Phase 113 both-color pass)"
    - "backfill_flaws.py inherits detection with zero new wiring (it calls the same classify_game_flaws path)"
    - "No new Stockfish/engine invocation — the detector reads the already-stored game_positions.pv at flaw_ply+1"
    - "pv is None (lichess-eval-only flaws), or a malformed move_san/FEN, leaves all three tactic fields None without raising"
  artifacts:
    - path: "app/services/flaws_service.py"
      provides: "_build_flaw_record calls detect_tactic_motif and populates the FlawRecord tactic fields"
      contains: "detect_tactic_motif"
  key_links:
    - from: "app/services/flaws_service.py:_build_flaw_record"
      to: "app/services/tactic_detector.py:detect_tactic_motif"
      via: "reads positions[n+1].pv, reconstructs board_after_flaw, calls the detector"
      pattern: "detect_tactic_motif"
---

<objective>
Wire the detector into the single classify path so both `classify_game_flaws` (eval-drain flow-through) and `backfill_flaws.py` (recompute) tag motifs for BOTH colors' flaws with no new engine call (TACDET-04). The integration is localized to `_build_flaw_record` — RESEARCH confirms the change flows through to both call sites automatically because the all-moves pass (Phase 113) already iterates both colors and backfill runs the same function.

Purpose: this connects the detector (plan 02) and schema plumbing (plan 01) into the live classify path; without it the columns stay perpetually NULL. Depends on plan 03 as well: live wiring only happens after every motif clears (or is explicitly query-suppressed against) its D-10 precision bar in the 124-03 fixture harness — we never tag prod flaws with an unvalidated detector.
Output: `_build_flaw_record` populates the three tactic fields; a flaws_service test proves both-color detection + the None-PV guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-schema-tactic-detector/124-CONTEXT.md
@.planning/phases/124-schema-tactic-detector/124-RESEARCH.md
@.planning/phases/124-schema-tactic-detector/124-PATTERNS.md
@app/services/flaws_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Call detect_tactic_motif inside _build_flaw_record</name>
  <files>app/services/flaws_service.py</files>
  <read_first>
    - app/services/flaws_service.py (the `_build_flaw_record` function and its `return FlawRecord(...)`; the FlawRecord TypedDict extended in plan 01 with the three tactic fields; `chess` is already imported)
    - .planning/phases/124-schema-tactic-detector/124-RESEARCH.md (Integration Point in flaws_service.py: the board-reconstruction snippet, the `n + 1 < len(positions)` guard, the try/except for malformed move_san/FEN; Pitfalls 1 and 6)
    - .planning/phases/124-schema-tactic-detector/124-PATTERNS.md (flaws_service.py section: the exact detector-call insertion before the return, and the import line)
  </read_first>
  <action>
    In `_build_flaw_record`, before the `return FlawRecord(...)`, reconstruct the post-flaw board and call the detector per RESEARCH/PATTERNS. Read `pv = positions[n + 1].pv if n + 1 < len(positions) else None` (Pitfall 1 guard). Read `move_san_of_flaw = positions[n].move_san` and `fen_before_flaw = fen_map.get(n, "")`. Only when all three are truthy: build `board_before = chess.Board(fen_before_flaw)`, `flaw_move = board_before.parse_san(move_san_of_flaw)`, push onto a copy to get `board_after_flaw`, then call `detect_tactic_motif(board_after_flaw, pv)` to get `(tactic_motif_int, tactic_piece_val, tactic_confidence)`. Wrap the parse/push in `try/except (ValueError, chess.IllegalMoveError): pass` so a malformed move_san/FEN leaves all three None (Pitfall 6). Pass the three values into the `FlawRecord(...)` constructor (the fields exist from plan 01). Add the import `from app.services.tactic_detector import detect_tactic_motif` at the top. Do NOT add any engine/Stockfish call — the only data source is the stored pv. Keep `_build_flaw_record` within the LOC/nesting limits; if the detector block pushes it over, extract a small `_detect_tactic_for_flaw(n, fen_map, positions) -> tuple[int|None, int|None, int|None]` helper and call it from `_build_flaw_record`.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/flaws_service.py && uv run ruff check app/services/flaws_service.py && grep -c 'detect_tactic_motif' app/services/flaws_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/services/flaws_service.py` passes zero errors.
    - `uv run ruff check app/services/flaws_service.py` clean.
    - `_build_flaw_record` (or its extracted helper) calls `detect_tactic_motif` and the result populates the FlawRecord tactic fields (`grep -c 'detect_tactic_motif' app/services/flaws_service.py` >= 2: the import + the call).
    - The detector call is guarded by `n + 1 < len(positions)` and a `try/except (ValueError, chess.IllegalMoveError)` so None/malformed inputs leave the fields None (reviewer confirms; covered by Task 2 test).
    - No new engine/Stockfish call added (`grep -c 'engine\|stockfish\|evaluate' ` of the new lines is 0 — the only new dependency is tactic_detector).
  </acceptance_criteria>
  <done>_build_flaw_record populates the three tactic fields from the stored PV with a None/malformed guard and no new engine call.</done>
</task>

<task type="auto">
  <name>Task 2: Both-color + None-PV integration test in test_flaws_service.py</name>
  <files>tests/services/test_flaws_service.py</files>
  <read_first>
    - tests/services/test_flaws_service.py (the existing `_make_pos` helper and classify_game_flaws test pattern — extend `_make_pos` to set `pv` and `move_san`, or add `_make_pos_with_pv` per PATTERNS)
    - .planning/phases/124-schema-tactic-detector/124-RESEARCH.md (Confirmed: backfill requires zero new wiring; both colors covered by the all-moves pass; the _make_pos_with_pv field list)
  </read_first>
  <action>
    Add a test class (e.g. `TestTacticIntegration`) in `tests/services/test_flaws_service.py` proving the wiring: construct a small game (positions list with pv set at flaw_ply+1) where a WHITE flaw and a BLACK flaw both exist, run `classify_game_flaws`, and assert the returned FlawRecords carry `tactic_motif_int` populated for BOTH colors' flaws (Phase 113 both-color coverage). Add a second test asserting that a flaw whose `positions[n+1].pv is None` (lichess-eval-only case) yields a FlawRecord with `tactic_motif_int is None` and no exception. Add a third test that a malformed move_san leaves the tactic fields None without raising. Use the `_make_pos_with_pv` helper from PATTERNS (set pv, move_san, and every other column the service reads). Pick fixture positions where the detector reliably fires a Core motif (reuse a known-good fork/hanging line from plan 03 or a simple textbook line) so the assertion is deterministic.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check tests/services/test_flaws_service.py && uv run pytest -n auto tests/services/test_flaws_service.py -k "Tactic or tactic" -q && uv run pytest -n auto tests/services/test_flaws_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -n auto tests/services/test_flaws_service.py -k "Tactic or tactic"` passes: both-color population test + None-PV test + malformed-move_san test all green.
    - The both-color test asserts a non-None `tactic_motif_int` on a WHITE flaw AND a BLACK flaw from one game (proves Phase 113 both-color coverage flows through).
    - The None-PV test asserts `tactic_motif_int is None` with no exception raised.
    - The full `uv run pytest -n auto tests/services/test_flaws_service.py -q` stays green (no regression).
  </acceptance_criteria>
  <done>Tests prove both-color detection through classify_game_flaws and the None/malformed-PV guard; existing suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored PV / SAN to classify path | The pv and move_san are internally-computed (engine PV + import-normalized SAN), not user input. The realistic failure is a None/malformed value crashing classify_game_flaws during eval-drain or backfill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-07 | Denial of Service | _build_flaw_record detector call during classify/backfill | mitigate | `n + 1 < len(positions)` guard (Pitfall 1) + try/except (ValueError, chess.IllegalMoveError) (Pitfall 6) + the detector's own None/malformed-PV guard (plan 02). A bad row leaves fields None; classify never crashes. Asserted by the None-PV + malformed-SAN tests. |
| T-124-08 | Elevation of Privilege | engine invocation | accept | No new engine/Stockfish call — the detector is pure CPU over stored pv. No new compute/OOM surface (the historical OOM risk is import/engine, not this path). |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No package installs. No supply-chain surface. |
</threat_model>

<verification>
- `uv run ty check app/services/flaws_service.py tests/services/test_flaws_service.py` zero errors.
- `uv run ruff check app/services/flaws_service.py` clean.
- Both-color population test + None-PV + malformed-SAN tests green.
- Full test_flaws_service.py suite green.
- No new engine call introduced.
</verification>

<success_criteria>
- detect_tactic_motif runs in _build_flaw_record; classify_game_flaws + backfill_flaws.py both populate the tactic fields for both colors with no new engine call (TACDET-04).
- None/malformed PV is safe (fields None, no raise).
</success_criteria>

<output>
Create `.planning/phases/124-schema-tactic-detector/124-04-SUMMARY.md` when done.
</output>
