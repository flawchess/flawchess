---
phase: 222-train-bot-narrated-onboarding-and-verdicts
plan: 01
type: execute
wave: 1
depends_on: []
coupling_justified: ["02: 01 adds the client `stampOnboarding` call for `POST /train/onboarding/{step}` that 02 implements; no file overlap, both sides additive and inert until the other lands"]
files_modified:
  - frontend/src/lib/personas/personaRegistry.ts
  - frontend/src/lib/trainBotCopy.ts
  - frontend/src/components/train/trainBubbleState.ts
  - frontend/src/components/train/TrainBotBubble.tsx
  - frontend/src/components/train/TrainBotStepper.tsx
  - frontend/src/lib/trainGuessLabels.ts
  - frontend/src/lib/theme.ts
  - frontend/src/pages/Home.tsx
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/types/train.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useTrainOnboarding.ts
  - frontend/src/lib/__tests__/trainBotCopy.test.ts
  - frontend/src/components/train/__tests__/trainBubbleState.test.ts
  - frontend/src/components/train/__tests__/TrainBotBubble.test.tsx
  - frontend/src/lib/personas/__tests__/personaRegistry.test.ts
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/hooks/__tests__/useTrainSession.test.ts
  - frontend/src/pages/__tests__/Train.solveLoop.test.tsx
autonomous: true
requirements: [TRAINBOT-01, TRAINBOT-02, TRAINBOT-03, TRAINBOT-04, TRAINBOT-07, TRAINBOT-08]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every puzzle's guess question is spoken by a bot chat row under the board, inside TrainSolveScreen's measured board column, never in TrainReveal (SC2, D-07, RESEARCH Finding A)."
    - "The guess buttons read 'Only one' and 'Several'; the reveal's guess card header reads the longer call form (SC1, D-09)."
    - "All 24 personas carry a temperament; the stern/friendly/smart casting sets are derived from that field, never a hand-maintained id list (SC7, D-01)."
    - "The verdict copy, the return statement and the score-screen copy are pure functions of values the client already holds; a mastered or parked item never gets a return promise (SC3, D-15, D-16, D-23)."
    - "TrainSolveScreen's measured cyclomatic complexity is strictly lower after the bubble lands than the 68 pinned in eslint.config.js (ROADMAP cross-cutting constraint, RESEARCH Finding B)."
  artifacts:
    - frontend/src/lib/trainBotCopy.ts
    - frontend/src/components/train/trainBubbleState.ts
    - frontend/src/components/train/TrainBotBubble.tsx
    - frontend/src/components/train/TrainBotStepper.tsx
    - frontend/src/hooks/useTrainOnboarding.ts
    - frontend/src/lib/__tests__/trainBotCopy.test.ts
    - frontend/src/components/train/__tests__/trainBubbleState.test.ts
    - frontend/src/components/train/__tests__/TrainBotBubble.test.tsx
  key_links:
    - "personaRegistry.ts `temperament` -> trainBotCopy.ts BY_TEMPERAMENT pools -> pickBot() -> TrainBotBubble persona prop."
    - "trainBubbleState.resolveBubbleState() -> the single <TrainBotBubble> mount in TrainSolveScreen's board column (columnRef)."
    - "trainGuessLabels.GUESS_LABELS -> the guess buttons; trainGuessLabels.GUESS_CALL_LABELS -> TrainReveal's guess card header."
    - "api/client.ts trainApi.stampOnboarding -> useTrainOnboarding -> queryClient.setQueryData(TRAIN_SETTINGS_QUERY_KEY)."
  prohibitions:
    - statement: "No board overlay: the bubble sits under the board and never covers the position."
      status: resolved
      verification: explicit
    - statement: "No copy is authored per persona; bot identity carries the face and name only (D-06)."
      status: resolved
      verification: explicit
    - statement: "No re-baselining of the frontend complexity gate — eslint.config.js is not edited."
      status: resolved
      verification: explicit
    - statement: "No client clock math for return dates: never new Date(), never Date.now() in the return-phrase resolver (D-15)."
      status: resolved
      verification: explicit
    - statement: "No Umami events for any of this (D-20)."
      status: resolved
      verification: backstop
    - statement: "No new npm or python package is installed in this phase."
      status: resolved
      verification: explicit
    - statement: "Existing testids are never renamed while nodes are relocated (RESEARCH Finding F)."
      status: resolved
      verification: explicit
---

<objective>
Build the phase's foundation and prove its load-bearing architectural bet with one
end-to-end tracer: a bot chat row that asks the guess question from inside
`TrainSolveScreen`'s measured board column, replacing the five sibling JSX guard blocks
with a single component — and measurably LOWERING the component's cyclomatic complexity
while doing it.

Purpose: RESEARCH Finding B pins `TrainSolveScreen` and `TrainReveal` at complexity
exactly 68 in `frontend/eslint.config.js`, with zero headroom and an explicit
`frontend/CLAUDE.md` prohibition on re-baselining. Assumption A2 ("removing the guess
block, move prompt, grading block and action row nets enough headroom") is the single
assumption that can invalidate every other frontend plan in this phase. The tracer
settles it on the first commit, not the tenth. It also settles RESEARCH Finding A (the
row is a board-column citizen, not a `TrainReveal` citizen) and Finding F (~140 existing
assertions stay green because testids move unchanged).

Output: the persona `temperament` field, the pure copy/state modules, the presentational
bubble and stepper components, the D-09 vocabulary change everywhere it surfaces, the
frontend seen-state plumbing (types, API client, stamp hook), and a live guess bubble on
every regular puzzle.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-CONTEXT.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-RESEARCH.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-PATTERNS.md
@frontend/CLAUDE.md
</context>

## Requirements (minted)

No active `REQUIREMENTS.md` exists for this milestone (same pattern as Phases 204–221).
These IDs are minted here and are the canonical set for Phase 222. Every ID appears in at
least one plan's `requirements` frontmatter.

| ID | Description | Plans |
|----|-------------|-------|
| TRAINBOT-01 | A first-time user cannot reach a silent failure: the first puzzle opens with the bot intro stepper, the guess buttons sit inside the last bubble, and a piece dropped before the guess visibly nudges the bubble and swaps its copy (SC1, D-05, D-07, D-08, D-22) | 01, 04 |
| TRAINBOT-02 | Every puzzle in every session shows the guess prompt inside a bot bubble; the board stays fully visible above it on a 375px viewport with the longest first-session copy loaded (SC2, D-02, D-07) | 01, 04, 07 |
| TRAINBOT-03 | Every reveal shows an outcome-matched bot line with inline point pills ending in the return statement for SR items, with distinct no-return variants for herring, filler, mastered and parked; the copy never comments on the user (SC3, D-03, D-15, D-16, D-23) | 01, 04 |
| TRAINBOT-04 | The score screen states what returns and when before the reminder ask; the first completed session gets the full spaced-repetition explanation, later ones a single line (SC4, D-04, D-17, D-18, D-25) | 01, 02, 06 |
| TRAINBOT-05 | The explanation-seen state is stored server-side on `train_settings` and survives a device switch; only a completed stepper stamps, and never via `PUT /train/settings` (SC5, D-11, D-12, D-13, D-14) | 02, 04, 05, 06 |
| TRAINBOT-06 | Both funnel metrics have a recorded baseline and a repeatable query on the superuser activity dashboard, so the post-change reading is a single run (SC6, D-19, D-20) | 03 |
| TRAINBOT-07 | Bot copy is authored per outcome bucket with a few variants, never per persona; the stern/friendly/smart sets derive from a `temperament` field on all 24 personas (SC7, D-01, D-06, D-21) | 01 |
| TRAINBOT-08 | The guess vocabulary is "Only one" / "Several" everywhere it is surfaced, with a longer call form on the reveal's guess card header (D-09) | 01 |
| TRAINBOT-09 | Analyze / Next / Solution move inside the verdict bubble; the reveal's mute toggle is retired and its re-homing is recorded as a follow-up seed (D-10) | 04, 07 |
| TRAINBOT-10 | The first reveal runs a three-step Hilda walkthrough of the solution screen, each step spotlighting its target element (ROADMAP scope B, D-24) | 05 |

## Planning notes (recorded once, here)

- **Spec-less probe fallback skipped:** phase has no requirement IDs to probe (visible
  skip). `must_haves.truths` are derived directly from ROADMAP success criteria 1–7 and
  CONTEXT decisions D-01..D-25.
- **`<assumption_delta_decision>`:** noun = none; decision = `no-change`; rationale =
  "'second-session' is a funnel cohort label; no primary key, identity model or source of
  truth is generalized in this phase." Advisory only.
- **Open questions resolved by the planner** (CONTEXT "Claude's Discretion", RESEARCH
  §Open Questions):
  1. `frontend/src/pages/Home.tsx:68` IS updated to the new vocabulary in the same task
     that changes `GUESS_LABELS` — leaving it stale contradicts SC7's single-source intent
     and it is one string.
  2. Hilda hosts the first puzzle by construction (the intro stepper renders her row for
     all three steps and the guess buttons live in her step-3 bubble). Removes a whole
     suppressed-random-host state and one branch from the complexity budget.
  3. The score screen is fed by BOTH halves of RESEARCH Finding C: a live
     `solvedOutcomes: SolvedResult[]` accumulator in `useTrainSession` (plan 06) AND the
     D-17 schema extension (plan 02) that seeds it on the restore path.
  4. The verdict chat row lives in `TrainSolveScreen`'s board column; the walkthrough step
     index is therefore owned by `TrainSolveScreen` and passed to `TrainReveal` as one new
     optional prop (plan 05).
  5. `item_status` checks run BEFORE any `due_date` comparison in the return-phrase
     resolver — mastered and parked items keep a stale `due_date`
     (RESEARCH Finding E).
- **Package legitimacy:** no `npm install`, no `uv add` anywhere in this phase (RESEARCH
  §Package Legitimacy Audit: not applicable). The `T-222-NN-SC` row in each plan's threat
  model is discharged by a lockfile-diff gate rather than a per-package audit.

## Artifacts this phase produces (plan 01 share)

New symbols and paths created by this plan:

- `frontend/src/lib/personas/personaRegistry.ts`: `export type Temperament = 'stern' | 'friendly' | 'smart'`; `Persona.temperament` (required); `temperament` set on all 24 registry entries.
- `frontend/src/lib/trainBotCopy.ts` (NEW): `BY_TEMPERAMENT`, `pickBot(temperament, rng?)`, `HILDA_ID`, `TANK_ID`, `introCopy(step)`, `promptCopy(sideToMove)`, `dropNudgeCopy(sideToMove)`, `movePromptCopy(sideToMove)`, `GRADING_COPY`, `verdictCopy(points, correctGuess, moveQuality, rng?)`, `lookCloserCopy(points)`, `returnPhrase({...})`, `walkthroughCopy(step)`, `scoreBubbleCopy({...})`, `type VerdictCopy`, `type ReturnPhraseInput`, `type ScoreBubbleInput`.
- `frontend/src/components/train/trainBubbleState.ts` (NEW): `type TrainBubbleState`, `resolveBubbleState(input)`.
- `frontend/src/components/train/TrainBotBubble.tsx` (NEW): `TrainBotBubble`, `TrainBotBubbleProps`.
- `frontend/src/components/train/TrainBotStepper.tsx` (NEW): `TrainBotStepper`, `TrainBotStepperProps`.
- `frontend/src/lib/trainGuessLabels.ts`: `GUESS_CALL_LABELS` (new export); `GUESS_LABELS` values changed.
- `frontend/src/lib/theme.ts`: `TRAIN_BUBBLE_BORDER`, `TRAIN_BUBBLE_NUDGE_BORDER`.
- `frontend/src/components/train/TrainReveal.tsx`: `TrainScoreChip` promoted from module-private to exported.
- `frontend/src/hooks/useTrainOnboarding.ts` (NEW): `useTrainOnboarding()`, `type OnboardingStep = 'intro' | 'reveal_walkthrough' | 'sr_explained'`.
- `frontend/src/api/client.ts`: `trainApi.stampOnboarding(step)`.
- `frontend/src/types/train.ts`: `TrainSettingsResponse.intro_seen_at`, `.reveal_walkthrough_seen_at`, `.sr_explained_at`; `SolvedResult.source`, `.item_status`, `.due_date`.
- New testids: `train-bot-bubble`, `train-bot-avatar`, `train-bot-name`, `train-bot-copy`, `btn-train-bot-step-next`, `btn-train-bot-step-done`.
- New test files: `frontend/src/lib/__tests__/trainBotCopy.test.ts`, `frontend/src/components/train/__tests__/trainBubbleState.test.ts`, `frontend/src/components/train/__tests__/TrainBotBubble.test.tsx`.

<tasks>

<task type="tracer">
  <name>T-222-01-01: End-to-end "a bot asks the guess question" — one path, board column, net-negative complexity</name>
  <files>frontend/src/lib/personas/personaRegistry.ts, frontend/src/lib/trainBotCopy.ts, frontend/src/components/train/trainBubbleState.ts, frontend/src/components/train/TrainBotBubble.tsx, frontend/src/lib/trainGuessLabels.ts, frontend/src/lib/theme.ts, frontend/src/pages/Home.tsx, frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx, frontend/src/lib/personas/__tests__/personaRegistry.test.ts</files>
  <read_first>
    - `frontend/src/components/train/TrainSolveScreen.tsx` lines 1–140 (module docstring, constants incl. `TRAIN_BOARD_MIN_WIDTH_PX` at :136), 943–970 (the `lg:flex-row` container and the measured board column `columnRef`), 1040–1200 (the action row at :1048-1105 and the five guard blocks at :1128-1175 this task collapses).
    - `frontend/src/lib/personas/personaRegistry.ts` lines 70–110 (the `Persona` interface and its doc-comment convention) and 440–470 (an entry, e.g. `wall-1800` Hilda).
    - `frontend/src/lib/trainGuessLabels.ts` in full — the "shared copy table + pure resolver, no React import" module convention and the LOCKED `guessFeedbackProse` sentences that are NOT touched.
    - `frontend/src/components/bots/PersonaCard.tsx` lines 100–150 — the avatar render idiom (`placeholderAvatarFor` / `resolveAvatarSrc`, tint on the wrapper, px size in `style`, `aria-hidden`, `alt=""`, `loading="lazy"`) to copy verbatim.
    - `frontend/src/components/train/TrainReveal.tsx` lines 315–340 (`TrainScoreChip`) and 1200–1225 (the `Guess: {GUESS_LABELS[guess]}` card header).
    - `.planning/phases/.../222-CONTEXT.md` §D-01, D-02, D-06, D-07, D-09, D-21, D-22 and the reviewed temperament table in `<specifics>`.
    - `.planning/phases/.../222-RESEARCH.md` §Finding A, §Finding B, §Finding F, §Pattern 1, §Pattern 2, §Pattern 3, §Code Example 1, §Code Example 3, §Code Example 5, §Pitfall 8.
    - `.planning/phases/.../222-PATTERNS.md` §`TrainBotBubble.tsx`, §`trainBotCopy.ts + trainBubbleState.ts`, §`personaRegistry.ts`.
    - `frontend/eslint.config.js` lines 210–225 (the pinned `complexity: ['error', 68]` override region — read it, do not edit it).
  </read_first>
  <action>
Wire ONE path end to end: registry data -> pure copy module -> presentational component -> the measured board column, for the regular-session guess prompt only. No intro stepper, no drop nudge, no verdict states in this task.

1. `personaRegistry.ts`: add `export type Temperament = 'stern' | 'friendly' | 'smart'` and a required `temperament: Temperament` field on `Persona`, with a doc comment in the file's existing provenance style stating it is the D-01 casting source and that the Train sets are derived from it, never from a hand-maintained id list. Set `temperament` on all 24 entries exactly per the reviewed table in CONTEXT `<specifics>` (8 stern, 7 smart, 9 friendly). `Record<PersonaId, Persona>` makes a missed entry a compile error — that is the D-01 guarantee. Extend `personaRegistry.test.ts` to assert all 24 carry a valid temperament, that each of the three pools is non-empty, that `wall-1800` (Hilda) is `smart` and `grinder-1600` (Tank) is `stern`.

2. `trainBotCopy.ts` (NEW, no React import — mirror `trainGuessLabels.ts`'s module shape): export `BY_TEMPERAMENT` built by reducing `Object.values(PERSONA_REGISTRY)` into three `Persona[]` pools, `pickBot(temperament, rng: () => number = Math.random)` per RESEARCH Code Example 3 (narrow the indexed access, `noUncheckedIndexedAccess` is on), and `HILDA_ID = 'wall-1800'` / `TANK_ID = 'grinder-1600'` as named constants. Add only this task's copy: `promptCopy(sideToMove)` returning D-22's regular prompt verbatim, `movePromptCopy(sideToMove)` returning D-22's after-guess line, and `GRADING_COPY` returning D-22's grading line. Copy strings come from D-22 verbatim; tighten only within the D-21 rules (no em-dashes, never a comment on the user, forward-looking).

3. `trainBubbleState.ts` (NEW, pure, guard-clause returns, no nesting) per RESEARCH Code Example 1: `TrainBubbleState` as a discriminated union over `intro` (with a `step`), `prompt`, `drop-nudge`, `move`, `grading`, `verdict`, and `resolveBubbleState(input)` with the precedence order from that example (verdict, grading, move, intro, drop-nudge, prompt). Only the `prompt`, `move` and `grading` arms are reachable after this task; the others are wired in plan 04. Extracting this function OUT of the component is the mechanism that keeps `TrainSolveScreen`'s own cyclomatic complexity from rising — do not inline it.

4. `theme.ts`: add `TRAIN_BUBBLE_BORDER` and `TRAIN_BUBBLE_NUDGE_BORDER` as named oklch constants beside the existing `TRAIN_*` block. No inline hex anywhere in the new component.

5. `TrainBotBubble.tsx` (NEW, presentational only, no data fetching, no Train domain logic, target cognitive complexity <= 8): a left avatar column (56px per CONTEXT "Claude's Discretion", the persona name under it at `text-sm`) and a right speech bubble with a left tail, rendering `children` as the copy and an optional `actions` slot rendered as its own row INSIDE the bubble. Props: `persona`, `state` (for the `data-state` attribute and the nudge styling), `nudgeNonce` (used as a React `key` on the bubble element so a repeated nudge remounts and replays the animation — RESEARCH Pitfall 3), `children`, `actions`. Copy the avatar idiom verbatim from `PersonaCard.tsx` including the emoji fallback branch. Testids: `train-bot-bubble` on the root (carrying `data-state` and `data-nudge`), `train-bot-avatar`, `train-bot-name`, `train-bot-copy`. Gate any pulse/animation CLASS (never the element) on `prefersReducedMotion()` from `@/lib/confetti`, the `TrainScoreScreen.tsx:116,126` precedent.

6. `trainGuessLabels.ts` (D-09): change `GUESS_LABELS` to `critical: 'Only one'` / `several: 'Several'`, and add a new exported `GUESS_CALL_LABELS: Record<Guess, string>` with the longer reveal form (`critical: 'only one good move'`, `several: 'several good moves'`). Do NOT overload `GUESS_LABELS` with both forms. `guessFeedbackProse` and its six LOCKED sentences are untouched. Update the module docstring's vocabulary quote.

7. D-09 call sites: `TrainReveal.tsx` guess card header becomes the `Your call: {GUESS_CALL_LABELS[guess]}` form; `TrainSolveScreen.tsx` module docstring lines 6–7 stop quoting the retired vocabulary; `frontend/src/pages/Home.tsx:68` marketing line is rewritten to the new vocabulary (planner resolution of RESEARCH Open Question 1 — one string, and leaving it stale contradicts SC7's single-source intent); `TrainReveal.test.tsx:273` and `:281` assertions move to the new call-form strings.

8. `TrainSolveScreen.tsx` — the tracer's integration point. Inside the measured board column (`columnRef`, the element at ~:963), replace the guess block (:1128-1156), the move prompt (:1159-1164) and the grading indicator (:1166-1171) with ONE `<TrainBotBubble>` mount driven by `resolveBubbleState(...)`, hosted by `pickBot('smart')` memoised per puzzle (D-02: `Math.random`, not seeded, not persisted; a reload may recast, which is accepted). The guess `Button` pair moves INTO the bubble's `actions` slot with `variant="brand-outline"` and its `data-testid` values (`btn-train-guess-critical`, `btn-train-guess-several`) unchanged; `train-guess-prompt`, `train-move-prompt` and `train-grading-indicator` stay on the bubble's copy nodes. Leave the engine-error, engine-loading and grading-error blocks exactly where they are — they are not bubble states. Leave the action row at :1048-1105 alone in this task (plan 04 owns it). Update `TrainSolveScreen.test.tsx:1418-1433`, whose two exact-string assertions on `train-move-prompt` pin the old move-prompt wording.

9. Measure and record the complexity before and after with `npx eslint --no-inline-config --rule 'complexity: ["error", 1]'` on both Train files and put both numbers in the SUMMARY. `frontend/eslint.config.js` must not be edited — if the net does not come out below 68, stop and report rather than re-baselining.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/components/train/__tests__/TrainSolveScreen.test.tsx src/components/train/__tests__/TrainReveal.test.tsx src/lib/personas/__tests__/personaRegistry.test.ts src/pages/__tests__/Train.solveLoop.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line, or any assertion naming a `btn-train-guess-*`, `train-guess-prompt`, `train-move-prompt` or `train-grading-indicator` testid reports "Unable to find an element"</fails_when>
    <automated>( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 67]' src/components/train/TrainSolveScreen.tsx src/components/train/TrainReveal.tsx )</automated>
    <fails_when>non-zero exit, or any output line containing "has a complexity of" — either means the net branch count did not fall below the pinned 68 baseline</fails_when>
    <automated>( cd frontend && npm run lint ) && git diff --quiet -- frontend/eslint.config.js</automated>
    <fails_when>non-zero exit from eslint, or a non-empty diff on frontend/eslint.config.js (the gate was re-baselined instead of satisfied)</fails_when>
    <automated>( cd frontend && npm run build )</automated>
    <fails_when>non-zero exit, or any `error TS` line in the tsc output (shared types changed: `Persona` gained a required field)</fails_when>
  </verify>
  <done>A regular Train puzzle renders one bot chat row under the board carrying the D-22 prompt and the two guess buttons inside the bubble, then the move prompt, then the grading line, all in the same slot with no layout jump; the buttons read "Only one" / "Several"; `TrainSolveScreen` measures strictly below complexity 68 with `eslint.config.js` unchanged; every pre-existing Train testid assertion is still green.</done>
  <acceptance_criteria>
    - `frontend/src/lib/personas/personaRegistry.ts` contains `temperament:` on 24 entries (`grep -c "temperament:" frontend/src/lib/personas/personaRegistry.ts` returns 25 — 24 entries plus the interface field).
    - `personaForId('wall-1800').temperament === 'smart'` and `personaForId('grinder-1600').temperament === 'stern'` are asserted in `personaRegistry.test.ts`.
    - `frontend/src/lib/trainBotCopy.ts` exists, contains no `import ... from 'react'` line, and exports `pickBot` with an `rng` parameter defaulting to `Math.random`.
    - `frontend/src/components/train/trainBubbleState.ts` exports `resolveBubbleState` and its body contains no `else` keyword (guard-clause returns only).
    - `TrainBotBubble.tsx` contains no hex or `oklch(` literal — every color reads a `@/lib/theme` constant.
    - Searching `frontend/src/` for the retired guess vocabulary on non-comment lines (`grep -rv -E "^[[:space:]]*(//|\\*|/\\*)" --include='*.ts' --include='*.tsx' frontend/src | grep -c 'critical move'`) returns 0, and `frontend/src/pages/Home.tsx` no longer carries the old marketing sentence.
    - `frontend/src/components/train/TrainSolveScreen.tsx` contains exactly one `<TrainBotBubble` occurrence, and it sits inside the element carrying `ref={columnRef}`.
    - `frontend/src/components/train/TrainReveal.tsx` contains no `<TrainBotBubble` occurrence (RESEARCH Finding A / Pitfall 1).
    - `git diff --stat -- frontend/eslint.config.js` is empty.
    - Both complexity numbers (before and after, from the `complexity: ["error", 1]` run) are recorded in the plan SUMMARY.
  </acceptance_criteria>
  <reversibility rating="reversible">Adding a required `temperament` field is a typed, mechanical edit across a closed registry; reverting is the same 24-entry sweep with no persisted state involved.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>T-222-01-02: The full copy tables and pure resolvers, with the Wave 0 unit tests they need</name>
  <files>frontend/src/lib/trainBotCopy.ts, frontend/src/lib/__tests__/trainBotCopy.test.ts, frontend/src/components/train/__tests__/trainBubbleState.test.ts, frontend/src/components/train/__tests__/TrainBotBubble.test.tsx</files>
  <read_first>
    - `.planning/phases/.../222-CONTEXT.md` §D-15, D-16, D-18, D-21, D-22, D-23, D-24, D-25 — the copy is taken from these verbatim.
    - `.planning/phases/.../222-RESEARCH.md` §Finding E (the full `due_date` / `expires_on` / mastered / parked semantics, and the verified date-fns behaviour), §Code Example 2, §Code Example 3, §Pitfall 7 (all three verdict fields can be `undefined` at runtime on a pre-206 cached reveal).
    - `frontend/src/lib/trainScore.ts` — `scorePuzzle`, `GUESS_POINTS`, `MOVE_TIER_POINTS`, `TRAIN_POINTS_PER_PUZZLE`; the declared single source of truth for points, never re-derived here.
    - `frontend/src/lib/__tests__/trainScore.test.ts` lines 1–30 — the pure-module test template (docstring naming the requirement, `@/` alias imports, one case per behaviour bullet plus boundaries).
    - `frontend/src/components/bots/__tests__/SetupScreen.test.tsx` lines 130–150 — the `vi.spyOn(Math, 'random')` form for rendered casts.
    - `frontend/src/types/train.ts` lines 110–135 — which `SolveResponse` fields are required vs optional on the TS type and why.
  </read_first>
  <behavior>
    - `returnPhrase`: a `red_herring` or `sharp_filler` source returns the warm-up tail regardless of `due_date`.
    - `returnPhrase`: `item_status === 'mastered'` returns the mastered tail EVEN WHEN `due_date` is in the past or equal to `expires_on` (the stale-date trap, RESEARCH Finding E).
    - `returnPhrase`: `item_status === 'parked'` returns the parked tail under the same stale-date conditions.
    - `returnPhrase`: `due_date <= expires_on` returns the next-session tail; `due_date > expires_on` returns the "in N days" tail with N from `differenceInCalendarDays(due_date, session_date)`.
    - `returnPhrase`: `due_date == null` with an active status returns an empty string (no return promise), never a crash.
    - `returnPhrase`: `source`, `item_status` and `due_date` all `undefined` returns an empty string.
    - `verdictCopy`: each of the four point buckets (0, 1, 2, 3) returns an opener from that bucket's variant list and the clause matching the (correctGuess, moveQuality) pair; the 0-point and 1-point buckets carry the look-closer line, the 2 and 3 buckets do not.
    - `pickBot`: with an injected `rng` returning 0 and returning 0.999, both results are members of the requested temperament pool and no other pool.
    - `scoreBubbleCopy`: first completed session returns the full explanation; a later session returns the one-liner; a warm-up session (`isWarmup`, no SR items) returns the warm-up variant; "nothing missed" returns its own variant; the return counts are grouped by when, never by position identifier.
    - `resolveBubbleState`: the six-way precedence holds — verdict beats grading beats move beats intro beats drop-nudge beats prompt.
    - `TrainBotBubble`: renders the persona name and an `<img>` when `resolveAvatarSrc` resolves, the emoji fallback when it does not, and renders the `actions` slot inside the bubble element.
  </behavior>
  <action>
Complete `trainBotCopy.ts` with the remaining copy tables and resolvers, then write the three Wave 0 test files RED-first.

Resolvers to add (all pure, guard-clause returns, no React import, no `new Date()` / `Date.now()` anywhere in the file):
- `introCopy(step: 0 | 1 | 2)` returning D-22's three intro bodies plus the speaking persona id (Tank for step 0, Hilda for steps 1 and 2, per D-05).
- `dropNudgeCopy(sideToMove)` returning D-22's "Decide first, then move" line.
- `verdictCopy(points, correctGuess, moveQuality, rng)` implementing D-23: two opener variants per bucket picked with the injected `rng`, the clause table keyed on the (correctGuess, moveQuality) pair, and the look-closer line for the 0 and 1 buckets only. Points come from `scorePuzzle` at the call site — never re-derived inside this module.
- `returnPhrase(input)` per RESEARCH Code Example 2, with the D-16 status checks strictly BEFORE any date comparison, using `differenceInCalendarDays(parseISO(a), parseISO(b))` from `date-fns` (never manual millisecond arithmetic — `new Date('YYYY-MM-DD')` parses as UTC midnight and `parseISO` as local midnight, which silently shifts a day). Tolerate `undefined` for `source`, `item_status` and `due_date` with a single nullish default at the consumption site (RESEARCH Pitfall 7).
- `walkthroughCopy(step: 0 | 1 | 2)` returning D-24's three steps and their spotlight target keys (`verdict`, `lines`, `actions`).
- `scoreBubbleCopy(input)` implementing D-25 and D-18: counts grouped by when, never per-position identifiers, with the four variants (first completed session full explanation, later one-liner, nothing-missed, warm-up). The three messages D-21 says must survive any edit — these are not ordinary puzzles and come from your own mistakes; the goal is understanding patterns with the analysis tools, not memorizing positions; reminders build the habit that makes Train work — must be present in the first-completed-session variant.

Test files: `frontend/src/lib/__tests__/trainBotCopy.test.ts` (the full D-15/D-16 truth table above plus the verdict buckets and the injected-rng picker), `frontend/src/components/train/__tests__/trainBubbleState.test.ts` (the six-way precedence), and `frontend/src/components/train/__tests__/TrainBotBubble.test.tsx` (the presentational contract: avatar, fallback, name, actions slot, testids, `data-state`). Assert the nudge through the copy swap and the `data-nudge` attribute, never through an animation class — the existing suite's `matchMedia` stub returns `matches: true` for every query, so `prefersReducedMotion()` is true inside these files (RESEARCH Finding G / Pitfall 5). Never add a per-file or per-test timeout; hoist any `await import` into `beforeAll`.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/lib/__tests__/trainBotCopy.test.ts src/components/train/__tests__/trainBubbleState.test.ts src/components/train/__tests__/TrainBotBubble.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line</fails_when>
    <automated>( cd frontend && grep -v -E "^[[:space:]]*(//|\*|/\*)" src/lib/trainBotCopy.ts | grep -q -e "new Date(" -e "Date.now(" ; test $? -eq 1 )</automated>
    <fails_when>the compound command exits non-zero, meaning a clock call appears on a NON-COMMENT line of the return-phrase module (D-15 forbids client clock math; comment lines are filtered out so a rationale comment cannot trip the gate)</fails_when>
    <automated>( cd frontend && npm run lint && npm run knip )</automated>
    <fails_when>non-zero exit, or any "Unused export" line naming a `trainBotCopy` or `trainBubbleState` symbol</fails_when>
  </verify>
  <done>Every D-22..D-25 copy table and every resolver exists as a pure function with unit coverage; the D-15/D-16 truth table passes including the two stale-`due_date` cases (mastered, parked); `pickBot` is deterministic under an injected rng and never draws outside its pool.</done>
  <acceptance_criteria>
    - `frontend/src/lib/__tests__/trainBotCopy.test.ts` contains a case asserting `returnPhrase` returns the mastered tail when `item_status: 'mastered'` AND `due_date` is <= `expiresOn` (proving status is checked before the date).
    - `frontend/src/lib/__tests__/trainBotCopy.test.ts` contains a case asserting `returnPhrase` returns the warm-up tail for `source: 'sharp_filler'` with a non-null `due_date`.
    - `frontend/src/lib/__tests__/trainBotCopy.test.ts` contains a case passing `{}` (all three verdict fields `undefined`) and asserting an empty string rather than a throw.
    - `pickBot('stern', () => 0)` and `pickBot('stern', () => 0.999)` both return personas whose `temperament === 'stern'`, asserted in the test file.
    - `scoreBubbleCopy` output for the first completed session contains all three D-21 must-survive messages, asserted by three separate substring expectations.
    - `frontend/src/lib/trainBotCopy.ts` imports `differenceInCalendarDays` and `parseISO` from `date-fns` and contains no manual `86400000` or `/ 1000 / 60` arithmetic.
    - `npm run knip` reports no unused export from the two new lib modules.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-222-01-03: Onboarding stepper component, seen-state client plumbing, and the SolvedResult type extension</name>
  <files>frontend/src/components/train/TrainBotStepper.tsx, frontend/src/types/train.ts, frontend/src/api/client.ts, frontend/src/hooks/useTrainOnboarding.ts, frontend/src/components/train/__tests__/TrainBotBubble.test.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx, frontend/src/hooks/__tests__/useTrainSession.test.ts, frontend/src/pages/__tests__/Train.solveLoop.test.tsx</files>
  <read_first>
    - `frontend/src/hooks/useTrainSettings.ts` lines 1–90 — `TRAIN_SETTINGS_QUERY_KEY`, the `useQuery`, and the mutation's `onSuccess` `setQueryData` idiom (RESEARCH Finding D requires the stamp to refresh the same shared cache); also its module docstring on why there is no local `captureException` (the global `MutationCache.onError` captures once).
    - `frontend/src/api/client.ts` lines 280–295 — `trainApi.getSettings` / `updateSettings`, the one-line-per-call convention.
    - `frontend/src/types/train.ts` lines 110–200 — `SolveResponse`, `SolvedResult`, `TrainSettingsResponse`, `TrainSettingsUpdate`.
    - `.planning/phases/.../222-CONTEXT.md` §D-11, D-12, D-14, D-17.
    - `.planning/phases/.../222-RESEARCH.md` §Finding D, §Pitfall 4 (`TrainLineStepper` is a chess-move stepper and must NOT be extended or have its testids reused), §Pitfall 11 (the five `SolvedResult` construction sites).
    - `frontend/src/components/train/TrainLineStepper.tsx` lines 250–310 — read ONLY to confirm its `btn-train-step-prev` / `btn-train-step-next` testids exist so the new controls avoid them.
  </read_first>
  <action>
1. `TrainBotStepper.tsx` (NEW): a forward-only stepper — a `useState<number>` step index, a `Next` button on every step except the last, and a caller-supplied last-step control. Props: `stepCount`, `step`, `onNext`, `lastControl`, `children`. It owns NO copy; it renders `children` for the current step. Do NOT extend or wrap `TrainLineStepper` (it replays SAN from a FEN onto the shared board — an entirely different contract). New testids `btn-train-bot-step-next` and `btn-train-bot-step-done`; never reuse `btn-train-step-prev` / `btn-train-step-next`. Buttons follow the frontend rule: `variant="default"` for the forward action, `TRAIN_BUTTON_CLASS` for sizing. Add the stepper's contract cases to `TrainBotBubble.test.tsx` (or a sibling describe block in the same file).

2. `types/train.ts`: add `intro_seen_at: string | null`, `reveal_walkthrough_seen_at: string | null`, `sr_explained_at: string | null` to `TrainSettingsResponse`. `TrainSettingsUpdate` is NOT touched — the read/write schema split is what makes a PUT unable to smuggle a server-owned field (D-12). Add `source`, `item_status` and `due_date` to `SolvedResult`, mirroring `SolveResponse`'s own declarations (`source` required, `item_status` and `due_date` nullable).

3. Fix all five `SolvedResult` construction sites so `tsc -b` passes (RESEARCH Pitfall 11): the `makeSolvedResult` factories in `frontend/src/hooks/__tests__/useTrainSession.test.ts` and `frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx`, and the three inline literal arrays in `frontend/src/pages/__tests__/Train.solveLoop.test.tsx`. Give the factories sensible defaults (`source: 'sr_item'`, `item_status: 'active'`, a fixed `due_date`) and an override parameter so later plans can vary them.

4. `api/client.ts`: add `stampOnboarding: (step: OnboardingStep) => apiClient.post<TrainSettingsResponse>(\`/train/onboarding/${step}\`, ...)` as one more line in the `trainApi` object, typed to `TrainSettingsResponse`. The path and the three step names (`intro`, `reveal_walkthrough`, `sr_explained`) are this phase's planner resolution of CONTEXT's "exact endpoint path/verb and Literal step names are Claude's discretion"; plan 02 implements the matching server side with the same names.

5. `hooks/useTrainOnboarding.ts` (NEW): export `type OnboardingStep = 'intro' | 'reveal_walkthrough' | 'sr_explained'` and `useTrainOnboarding()` returning `{ stamp, isPending }`, a `useMutation` over `trainApi.stampOnboarding` whose `onSuccess` does `queryClient.setQueryData(TRAIN_SETTINGS_QUERY_KEY, data)` — the exact idiom `useTrainSettings`'s PUT already uses, so the shared app-wide cache updates with no refetch. No local Sentry capture (the global `MutationCache.onError` already captures each mutation error once). The hook is imported by plans 04, 05 and 06; knip will flag it until then, so import it from `TrainSolveScreen` in plan 04 — if knip fails at the end of THIS plan, note it in the SUMMARY rather than deleting the export.
  </action>
  <verify>
    <automated>( cd frontend && npm run build )</automated>
    <fails_when>non-zero exit, or any `error TS` line — in particular a `SolvedResult` missing-property error at any of the five fixture sites</fails_when>
    <automated>( cd frontend && npx vitest run src/hooks/__tests__/useTrainSession.test.ts src/pages/__tests__/Train.solveLoop.test.tsx src/components/train/__tests__/TrainSolveScreen.test.tsx src/components/train/__tests__/TrainBotBubble.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line</fails_when>
    <automated>( cd frontend && grep -n "intro_seen_at\|reveal_walkthrough_seen_at\|sr_explained_at" src/types/train.ts )</automated>
    <fails_when>non-zero exit (grep found nothing), or any matching line appearing inside the `TrainSettingsUpdate` interface body rather than `TrainSettingsResponse`</fails_when>
  </verify>
  <done>The stepper component, the stamp API call, the stamp hook and the extended TS types all exist and compile; `TrainSettingsUpdate` is unchanged; all five `SolvedResult` fixture sites carry the three new fields.</done>
  <acceptance_criteria>
    - `frontend/src/hooks/useTrainOnboarding.ts` contains `setQueryData(TRAIN_SETTINGS_QUERY_KEY` and does not contain `invalidateQueries({ queryKey: TRAIN_SETTINGS_QUERY_KEY`.
    - `frontend/src/api/client.ts` contains exactly one `'/train/onboarding/` occurrence.
    - `frontend/src/components/train/TrainBotStepper.tsx` contains no `btn-train-step-prev` and no `btn-train-step-next` string, and does not import `TrainLineStepper`.
    - `grep -c "source:" frontend/src/pages/__tests__/Train.solveLoop.test.tsx` returns at least 3 (the three inline `SolvedResult` literal arrays now carry the field).
    - `npm run build` exits 0 with `SolvedResult`'s three new fields declared required-or-nullable exactly as `SolveResponse` declares them.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> browser (`SolveResponse` / `TrainSessionResponse`) | Train verdict data crosses into client-side copy resolvers in this plan; the risk is re-rendering a field the user should not yet see. |
| npm registry -> repository | Any new package would cross an unaudited boundary. This plan installs none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-01-01 | Information Disclosure | `trainBotCopy.ts` verdict and return-phrase resolvers | medium | mitigate | The resolvers consume only `correct_guess`, `move_quality`, `source`, `item_status`, `due_date` — every one already delivered to this client by `SolveResponse` for the same position (`app/schemas/train.py:219-222`). No resolver reads `game_id`, `ply`, a best move, or any unattempted puzzle. Acceptance criteria pin the input shape. |
| T-222-01-02 | Information Disclosure | `SolvedResult` TS type extension (`frontend/src/types/train.ts`) | medium | mitigate | Mirrors the server schema plan 02 ships; the non-leak argument is the schema docstring's own standard (`app/schemas/train.py:65-73`) and is re-checked field by field there. The TS type adds no field the server does not send. |
| T-222-01-03 | Tampering | `pickBot` / `Math.random` casting | low | accept | The cast is cosmetic (face and name only, D-06). No authorization, scoring or scheduling decision keys on it; a manipulated `Math.random` changes only which avatar the user sees. |
| T-222-01-04 | Spoofing | `useTrainOnboarding` stamp call from the client | low | mitigate | Mitigated by existing pattern on the server side: the handler takes `user.id` from `current_active_user` only and `_reject_guest` is its first statement (`app/routers/train.py:48-55`). The client sends no user identifier at all — the step name is the entire request. |
| T-222-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs nothing (RESEARCH §Package Legitimacy Audit: not applicable, no `npm install`, no `uv add`). Enforced by a lockfile-diff gate at plan close rather than a package audit table. |

Supply-chain gate (discharges T-222-01-SC): `git diff --exit-code -- frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` must exit 0 at plan close. A non-empty diff is a blocking finding, not a warning.
</threat_model>

<verification>
- `( cd frontend && npm run lint && npm test -- --run && npm run build )` — the plan's own full frontend gate.
- `( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 67]' src/components/train/TrainSolveScreen.tsx src/components/train/TrainReveal.tsx )` exits 0.
- `git diff --exit-code -- frontend/eslint.config.js frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` exits 0.
- No `bin/reset_db.sh` invocation anywhere in this plan.
</verification>

<success_criteria>
- A bot chat row asks the guess question on every regular puzzle, inside the board column, with the buttons in the bubble and every pre-existing testid intact.
- `TrainSolveScreen` measures strictly below complexity 68 without `eslint.config.js` being touched.
- The guess vocabulary is "Only one" / "Several" at every surfaced call site including the homepage.
- All 24 personas carry a temperament; the three casting pools derive from it.
- The full D-22..D-25 copy set and the D-15/D-16 return-phrase truth table exist as tested pure functions.
- The stamp hook, API call and extended TS types compile against the contract plan 02 implements.
</success_criteria>

<output>
Create `.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-01-SUMMARY.md` when done.
Record the before/after complexity numbers for both Train components in it.
</output>
