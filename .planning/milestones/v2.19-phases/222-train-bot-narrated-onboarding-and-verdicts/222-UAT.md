---
status: complete
phase: 222-train-bot-narrated-onboarding-and-verdicts
source: [222-VERIFICATION.md]
started: 2026-09-13T18:12:08Z
updated: 2026-09-14T16:41:15Z
---

## Current Test

[testing complete]

## Tests

### 1. Score screen full-explanation and one-liner variants on a real (non-warm-up) account
expected: First completed session (sr_explained_at NULL, missed items > 0, not warm-up) renders the full D-25 explanation and stamps sr_explained_at exactly once; a later session with sr_explained_at already set renders only the one-liner. (Only the warm-up variant was observed live; the other two are unit-tested in TrainScoreScreen.test.tsx.) Suggested account: user 28 on the dev DB (already reset by the plan 06 executor).
result: passed (owner, 2026-09-14): the full-explanation variant was exercised live in UAT round 5 on a real account (user 99 after reset_train_state.py) where it exposed the sr_explained latch bug; the fix (firstExplanationLatch) carries a regression test and the one-liner variant is unit-tested in TrainScoreScreen.test.tsx. Accepted for ship after six UAT rounds.

### 2. SC5 phone handoff — explanation-seen state survives a device switch
expected: Complete the first-session intro stepper on a desktop browser signed into a real account, then open /train on a phone signed into the same account: the intro does not replay (intro_seen_at is a server-side timestamp, not device-local). Needs two physical devices.
result: passed (owner, 2026-09-14): accepted on the server-side design (intro_seen_at / sr_explained_at / reveal_walkthrough_seen_at are train_settings columns stamped via the /train/settings/onboarding-seen endpoint, covered by backend tests; nothing is device-local). Not observed on two physical devices in this session; owner shipped after six UAT rounds.

### 3. SC6 Train funnel card on /activity as a superuser
expected: The Train sessions section's funnel card shows both shares (first-session 0-solve, second-session return) with numerator/denominator plus a live all-time line; moving the date-range window changes the windowed numbers while the all-time line holds. (Query verified against the dev DB at 7d/30d/90d; only the superuser page render is unconfirmed.)
result: passed (owner, 2026-09-14): fetch_train_funnel verified against the dev DB at 7d/30d/90d and the ActivityPage funnel card is unit-tested; the superuser page render was not screenshot-verified in this session. Owner shipped after six UAT rounds.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

## UAT round 2 (2026-09-13, phone screenshots from the user)

Feedback items and what changed (all verified in the browser at 375x667 via a same-origin iframe on the dev deployment, user t2 after `scripts/reset_train_state.py --user-id 98 --reset-settings`, and at 1100px desktop):

| # | Feedback | Resolution |
|---|----------|------------|
| 1 | Board + bubble leave large margins on iPhone SE | Height fit now runs only on `lg+` (`useFitBoardToViewport` gained `enabled`); below `lg` the board is width-bound. The plan 06 `MOBILE_BOTTOM_BAR_PX` gutter and `useMediaQuery` hook are gone. |
| 2 | Progress bar too tall | "n of m", the bar and "x / y pts" share one row. |
| 3 | Avatar 30% bigger | 56 -> 72px on `sm+`, 32 -> 42px on phones. |
| 4 | Buttons bottom-right in the bubble | Bubble action row is `justify-end`. |
| 5 | "Only one" / "Several" same width | Two-column `auto-cols-fr` grid. |
| 6 | Show the call on the move prompt | `movePromptCopy(side, guess)`: "Your call: only one good move. Now play a move for white." |
| 7 | Avatar + bubble should not be sticky | Only progress row + board are pinned; the measured column is `display: contents` below `lg` so the pinned block is a direct child of the page-long container (sticky only works within its parent's box). |
| 8 | Line-card tip unclear | Walkthrough is six steps: verdict, tap-a-card, arrows-step-the-line, free play + eval bar (rings the board row, new `board` spotlight target), understanding-not-memorizing, actions. |
| 9 | Points pop should show the avatar (sketch 004) | Pop is verdict bot face + "+N". |
| 10 | Messy last-step button arrangement | Ringed real action row and "Got it" share one right-aligned row. |

## UAT round 5 (2026-09-14, desktop install-first)

| # | Feedback | Resolution |
|---|----------|------------|
| 1 | The onboarding ending should push the PWA install, not desktop reminders; desktop push only fires while the browser is running | Desktop (not mobile, not standalone) has no "Remind me" on the score screen any more (desktop push stays reachable from Train settings). The row is just "Done". Below it: the phone QR handoff (caption "Scan to install FlawChess on your phone and turn on reminders there.") in every slot state including hidden (push denied/unsupported/unconfigured), OR, once the account has any push subscription from a mobile browser, "Reminders on — HH:00 on your phone". That account-level fact is new: `has_mobile_subscription` on `TrainSettingsResponse`, derived server-side from `push_subscriptions.user_agent` (`push_repository.has_mobile_subscription`, same Android/iPhone/iPad/iPod regex as `useInstallPrompt.isMobile`). The score bubble's ask follows the same gate (`ReminderAsk`: remind_me / scan_qr / none). Android and iOS are unchanged. |
| 2 | "I only get the one-liner" after `reset_train_state.py --user-id 99`, twice | Real bug, not the reset script: `TrainScoreScreen` stamped `sr_explained` on mount, the stamp's `setQueryData` flipped `sr_explained_at` in the shared settings cache, and the live-derived `firstExplanation` collapsed the full explanation into the one-liner on the same screen. The first-session decision is now latched from the first resolved settings value (`firstExplanationLatch`) and never re-derived. Regression test: "the full explanation survives the stamp writing sr_explained_at back into the settings cache". |
| 3 | The feedback sentence should comment on the score (red/yellow/green copy), on every session | `scoreBubbleCopy` takes the session `band`; every variant except nothing-missed (which is its own comment) opens with "Strong session." / "Solid session, with a few that got away." / "Tough session. That is exactly what Train is for." (neutral "Session done." when nothing was scored): first, later and warm-up sessions alike. |

Verified by unit tests (frontend 702 across Train/pages/copy, backend 227 across train/push/reset). Browser pass of the two desktop states: see round-5 notes below once driven.
