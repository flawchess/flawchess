---
phase: 65-llm-endpoint-with-pydantic-ai-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - app/core/config.py
  - .env.example
  - app/services/insights_prompts/endgame_v1.md
  - tests/conftest.py
autonomous: true
requirements:
  - LLM-02
  - LLM-03
user_setup:
  - service: anthropic
    why: "pydantic-ai Agent makes real LLM calls at runtime against Anthropic Claude Haiku 4.5 (production) — phase 65 tests use the pydantic-ai `test:` provider and do NOT need a real key"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → Settings → API Keys (https://console.anthropic.com/settings/keys)"
      - name: PYDANTIC_AI_MODEL_INSIGHTS
        source: "Set to `anthropic:claude-haiku-4-5-20251001` (default) or any pydantic-ai model string"
    dashboard_config: []
must_haves:
  truths:
    - "Backend refuses to start when PYDANTIC_AI_MODEL_INSIGHTS is empty or invalid"
    - "System prompt lives in a versioned .md file, never inline string literal in .py"
    - "Test suite continues to pass with `PYDANTIC_AI_MODEL_INSIGHTS=test` set before app imports"
  artifacts:
    - path: "pyproject.toml"
      provides: "pydantic-ai-slim[anthropic,google] dependency pin >=1.85,<2.0"
      contains: "pydantic-ai-slim"
    - path: "uv.lock"
      provides: "Resolved lockfile with pydantic-ai transitive deps"
    - path: "app/core/config.py"
      provides: "PYDANTIC_AI_MODEL_INSIGHTS + INSIGHTS_HIDE_OVERVIEW settings"
      contains: "PYDANTIC_AI_MODEL_INSIGHTS"
    - path: ".env.example"
      provides: "Documented env vars for LLM insights"
      contains: "PYDANTIC_AI_MODEL_INSIGHTS"
    - path: "app/services/insights_prompts/endgame_v1.md"
      provides: "Phase 65 system prompt (role + output contract + metric glossary + section gating + overview rule)"
      contains: "## Metric glossary"
    - path: "tests/conftest.py"
      provides: "PYDANTIC_AI_MODEL_INSIGHTS=test env var before app imports"
      contains: "PYDANTIC_AI_MODEL_INSIGHTS"
  key_links:
    - from: "app/core/config.py"
      to: ".env"
      via: "pydantic-settings BaseSettings env_file"
      pattern: "PYDANTIC_AI_MODEL_INSIGHTS"
    - from: "tests/conftest.py"
      to: "app.main / app.services.insights_llm imports"
      via: "env-var-before-import pattern"
      pattern: "os.environ\\[\"PYDANTIC_AI_MODEL_INSIGHTS\"\\]"
---

<objective>
Wave-1 prep: install pydantic-ai, expose two new Settings fields, ship the Phase 65 system prompt file, document the env vars in `.env.example`, and extend `tests/conftest.py` so every subsequent wave's tests import cleanly.

Purpose: Without the dependency + config + prompt file + test env var in place, Wave 2+ cannot import `app.services.insights_llm` without crashing at module import time (missing package, missing prompt file, missing Settings attribute). This plan unblocks everything.

Output: Dependency pinned in pyproject.toml + uv.lock; Settings extended; `.env.example` documented; system prompt Markdown file committed; conftest env var in place. All 950 existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-llm-endpoint-with-pydantic-ai-agent/65-CONTEXT.md
@.planning/phases/65-llm-endpoint-with-pydantic-ai-agent/65-RESEARCH.md
@.planning/phases/65-llm-endpoint-with-pydantic-ai-agent/65-PATTERNS.md
@.planning/phases/65-llm-endpoint-with-pydantic-ai-agent/65-VALIDATION.md
@app/core/config.py
@tests/conftest.py

<interfaces>
From app/core/config.py (current state — verbatim, extend by appending new fields):
```python
class Settings(BaseSettings):
    DATABASE_URL: str = "postgresql+asyncpg://flawchess:flawchess@localhost:5432/flawchess"
    TEST_DATABASE_URL: str = "postgresql+asyncpg://postgres:postgres@localhost:5432/flawchess_test"
    DB_ECHO: bool = False
    SECRET_KEY: str = "change-me-in-production"
    GOOGLE_OAUTH_CLIENT_ID: str = ""
    GOOGLE_OAUTH_CLIENT_SECRET: str = ""
    BACKEND_URL: str = "http://localhost:8000"
    FRONTEND_URL: str = "http://localhost:5173"
    ENVIRONMENT: str = "production"
    SENTRY_DSN: str = ""
    SENTRY_TRACES_SAMPLE_RATE: float = 0.0

    model_config = SettingsConfigDict(env_file=".env", extra="ignore")
```

From tests/conftest.py (lines 1-14, verbatim — insert new env var BELOW line 13 and ABOVE `import chess`):
```python
import asyncio
import os
import uuid

os.environ["SENTRY_DSN"] = ""
os.environ["SECRET_KEY"] = "test-secret-key-32-bytes-exactly-ok-for-hs256-tests"

import chess
import pytest
...
```

Pydantic-ai model string format (from Phase 64 + researcher verification):
- `"<provider>:<model>"` e.g. `"anthropic:claude-haiku-4-5-20251001"`, `"google-gla:gemini-2.5-flash"`
- Special test provider: `"test"` — pydantic-ai's built-in test provider, passes Agent(...) construction without any API key. Used by conftest.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add pydantic-ai-slim dependency and regenerate lockfile</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>pyproject.toml</read_first>
  <action>
    Add `"pydantic-ai-slim[anthropic,google]>=1.85,<2.0"` to the `dependencies = [...]` list in pyproject.toml. Keep alphabetical/existing ordering consistent with neighbors — insert next to the other Pydantic-ecosystem deps (pydantic, pydantic-settings). Then run `uv lock` (NOT `uv sync` — we only need the lockfile update at this step; `uv sync` will be exercised implicitly when tests run).

    Rationale: Slim extras `[anthropic,google]` are correct per D-23 + RESEARCH.md §8 — we use `anthropic:*` and `google-gla:*` model strings. The full `pydantic-ai` pulls OpenAI/Mistral/Cohere/Bedrock we don't need.

    Cap `<2.0` because pydantic-ai has renamed public API twice (result_type→output_type, result_retries→output_retries); a 2.0 bump needs manual review before adoption.
  </action>
  <verify>
    <automated>grep -q 'pydantic-ai-slim\[anthropic,google\]' pyproject.toml &amp;&amp; grep -q 'pydantic-ai-slim' uv.lock &amp;&amp; uv run python -c "import pydantic_ai; from pydantic_ai.models.test import TestModel; from pydantic_ai import Agent; print(pydantic_ai.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'pydantic-ai-slim\[anthropic,google\]>=1.85,<2.0' pyproject.toml` matches exactly one line
    - `uv.lock` contains a `[[package]]` block with `name = "pydantic-ai-slim"` and version 1.85.x
    - `uv run python -c "from pydantic_ai import Agent; from pydantic_ai.models.test import TestModel"` exits 0
    - `uv run python -c "import pydantic_ai; print(pydantic_ai.__version__)"` prints a version `>= 1.85` and `< 2.0`
    - No other dependency versions changed in `uv.lock` (verify with `git diff uv.lock` — only pydantic-ai-slim and its new transitive deps should appear)
  </acceptance_criteria>
  <done>pydantic-ai-slim installed and importable; lockfile regenerated; no unrelated dep churn.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add PYDANTIC_AI_MODEL_INSIGHTS + INSIGHTS_HIDE_OVERVIEW to Settings</name>
  <files>app/core/config.py</files>
  <read_first>app/core/config.py</read_first>
  <action>
    Append two new fields to the `Settings(BaseSettings)` class in `app/core/config.py`, AFTER the existing `SENTRY_TRACES_SAMPLE_RATE` line and BEFORE the `model_config = ...` line. Exact content to add (preserve indentation — 4 spaces):

    ```python
        # Pydantic-AI model string for endgame insights (Phase 65). Empty string
        # is treated as "unconfigured" — app/main.py lifespan calls
        # get_insights_agent() which raises UserError on the empty string, aborting
        # startup (D-22/D-23). Accepts any pydantic-ai model string, e.g.
        # "anthropic:claude-haiku-4-5-20251001", "google-gla:gemini-2.5-flash".
        # Tests use the built-in "test" provider (see tests/conftest.py).
        PYDANTIC_AI_MODEL_INSIGHTS: str = ""
        # When true, insights service strips `report.overview = ""` before
        # returning to client. Full overview still persists in llm_logs.response_json
        # for offline analysis — log is source of truth, response is policy-gated
        # view (D-18, BETA-02).
        INSIGHTS_HIDE_OVERVIEW: bool = False
    ```

    Do NOT add any Literal typing on `PYDANTIC_AI_MODEL_INSIGHTS` — pydantic-ai accepts arbitrary `provider:model` strings at runtime (including new providers shipped after this phase).
  </action>
  <verify>
    <automated>uv run python -c "from app.core.config import settings; assert hasattr(settings, 'PYDANTIC_AI_MODEL_INSIGHTS'); assert hasattr(settings, 'INSIGHTS_HIDE_OVERVIEW'); assert settings.INSIGHTS_HIDE_OVERVIEW is False or settings.INSIGHTS_HIDE_OVERVIEW is True; print('ok')" &amp;&amp; uv run ty check app/core/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'PYDANTIC_AI_MODEL_INSIGHTS: str = ""' app/core/config.py` matches exactly one line
    - `grep 'INSIGHTS_HIDE_OVERVIEW: bool = False' app/core/config.py` matches exactly one line
    - `uv run python -c "from app.core.config import settings; print(type(settings.INSIGHTS_HIDE_OVERVIEW).__name__)"` prints `bool`
    - `uv run ty check app/core/config.py` exits 0 (zero errors)
    - `uv run ruff check app/core/config.py` exits 0
  </acceptance_criteria>
  <done>Settings exposes both fields with documented defaults; ty-clean; ruff-clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document new env vars in .env.example</name>
  <files>.env.example</files>
  <read_first>.env.example</read_first>
  <action>
    Append a new section to `.env.example` (at the bottom, after existing entries). Preserve the file's existing comment style (look at the existing entries first; match their style). Add exactly this block:

    ```
    # LLM insights (Phase 65+)
    # Supports any pydantic-ai model string; requires matching provider API key set.
    # Tests use the built-in "test" provider (set in tests/conftest.py) — no real
    # API key needed to run the test suite.
    PYDANTIC_AI_MODEL_INSIGHTS=anthropic:claude-haiku-4-5-20251001
    ANTHROPIC_API_KEY=sk-ant-...
    # Set to true to strip report.overview from the API response (full overview is
    # still captured in llm_logs.response_json for offline analysis).
    INSIGHTS_HIDE_OVERVIEW=false
    ```

    NOTE: If `.env.example` already contains `ANTHROPIC_API_KEY` (possible but unlikely — it's new for Phase 65), do NOT duplicate; just add the other three lines.

    Per CLAUDE.md §Version Control: `.env.example` is the ONLY env-related file committed. Real secrets go in `.env` (gitignored). Production secrets live on the Hetzner server at `/opt/flawchess/.env`.
  </action>
  <verify>
    <automated>grep -q 'PYDANTIC_AI_MODEL_INSIGHTS=anthropic:claude-haiku-4-5-20251001' .env.example &amp;&amp; grep -q 'INSIGHTS_HIDE_OVERVIEW=false' .env.example &amp;&amp; grep -q 'ANTHROPIC_API_KEY=' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^PYDANTIC_AI_MODEL_INSIGHTS=anthropic:claude-haiku-4-5-20251001$' .env.example` matches exactly one line
    - `grep '^INSIGHTS_HIDE_OVERVIEW=false$' .env.example` matches exactly one line
    - `grep '^ANTHROPIC_API_KEY=' .env.example` matches at least one line
    - `grep -c '^PYDANTIC_AI_MODEL_INSIGHTS=' .env.example` prints `1` (no duplicates)
  </acceptance_criteria>
  <done>.env.example documents the three new vars; no duplicates.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create the Phase 65 system prompt file (endgame_v1.md)</name>
  <files>app/services/insights_prompts/endgame_v1.md, app/services/insights_prompts/__init__.py</files>
  <read_first>.planning/phases/65-llm-endpoint-with-pydantic-ai-agent/65-CONTEXT.md, .planning/seeds/SEED-003-llm-based-insights.md</read_first>
  <action>
    Create the directory `app/services/insights_prompts/` if it does not exist. Inside, create two files:

    (a) `app/services/insights_prompts/__init__.py` — empty file (makes the directory a Python package so `Path(__file__).parent / "insights_prompts"` works cleanly from `insights_llm.py`).

    (b) `app/services/insights_prompts/endgame_v1.md` — the Phase 65 system prompt. The file MUST contain all five required sections below, in order. Use this exact skeleton (fill in the metric glossary with concrete entries derived from Phase 63's four sections + subsection registry):

    ```markdown
    # Endgame Insights — System Prompt v1

    You are an analyst narrating a chess player's **endgame performance** from precomputed findings. Your output is a structured `EndgameInsightsReport` (JSON). You do NOT write free text; you return fields.

    ## Output contract

    Return exactly this shape:
    - `overview`: a single paragraph of at most 150 words that summarizes the most important endgame signals for this user. ALWAYS populate this field — never return an empty string, never return null. When no strong cross-section signal is present, summarize the per-section findings instead. Silence is not a valid output.
    - `sections`: between 1 and 4 `SectionInsight` entries, each with a unique `section_id` from the enum {overall, metrics_elo, time_pressure, type_breakdown}. Each section has:
      - `headline`: at most 12 words, present-tense, descriptive (not imperative)
      - `bullets`: 0-2 bullets, each at most 20 words
    - `model_used`: echo back the `model_used` value provided in the user message
    - `prompt_version`: echo back the `prompt_version` value provided in the user message (always `endgame_v1`)

    ## Section gating

    Include a section ONLY when at least one of its underlying subsection findings has `sample_size > 0` AND `sample_quality != "thin"`. If a section's underlying subsections are all thin or empty, omit the section entirely. Do NOT fabricate content to fill sections.

    ## Cross-section flags

    The user message includes a `Flags:` line listing deterministically precomputed cross-section flags. Trust these — they encode correctness guardrails the computation derived from the raw data:

    - `baseline_lift_mutes_score_gap` — the user's Score Gap is partly explained by an opponent-strength baseline lift. Do NOT narrate Score Gap as pure skill when this flag is present.
    - `clock_entry_advantage` — user enters the endgame with more clock than opponent on average. Safe to narrate composure/time advantage.
    - `no_clock_entry_advantage` — user does NOT enter the endgame with a clock advantage. Do NOT claim a clock-management edge.
    - `notable_endgame_elo_divergence` — actual Elo and endgame Elo differ by >100 points. Worth narrating.

    ## Series interpretation

    Four subsections carry a raw timeseries under a `### Series` block: `score_gap_timeline`, `clock_diff_timeline`, `endgame_elo_timeline`, `type_win_rate_timeline`. Each point has `bucket_start` (YYYY-MM-DD, weekly for last_3mo, first-of-month for all_time), `value`, and `n` (sample size).

    Derive trend and volatility from the series yourself. Give more narrative weight to buckets with higher `n`. Do NOT narrate a trend when fewer than ~5 buckets are present, and do NOT narrate short-term wiggles — focus on multi-bucket direction.

    ## Overview rule

    The overview is always 1-2 paragraphs totalling at most 150 words. When a cross-section story emerges (e.g. strong endgame skill + weak clock = composure under time pressure), lead with it. When no cross-section story emerges, summarize the per-section findings in priority order (overall → metrics_elo → time_pressure → type_breakdown).

    ## Metric glossary

    Interpret each metric using the definitions below. These match the user-facing info popovers on the Endgame tab — narrative must stay consistent with the UI.

    - **score_gap**: percentage-point difference between user's Score % and opponent's Score % over the same games. Positive = user outperforms opponent.
    - **conversion_win_pct**: win rate when user enters the endgame with a material advantage.
    - **parity_score_pct**: Score % when user enters the endgame at material parity.
    - **recovery_save_pct**: save rate (draw or win) when user enters the endgame with a material deficit.
    - **endgame_skill**: composite of conversion/parity/recovery, normalized to 0-100.
    - **endgame_elo**: user's estimated Elo when endgame games are weighted by outcome.
    - **endgame_elo_gap**: `endgame_elo - actual_elo`. Positive = user's endgame rating is higher than their overall rating (strong endgame). Negative = weaker.
    - **avg_clock_diff_pct**: percentage of base time remaining when user enters endgame, relative to opponent. Positive = user enters with more clock than opponent.
    - **net_timeout_rate**: rate at which user loses on time in endgames minus rate at which opponents do.
    - **win_rate** (per type): win rate in a specific endgame type bucket (pawn, rook, minor-piece, queen, mixed).

    ## Tone

    Factual, present-tense, descriptive. No imperatives ("you should…"). No hollow praise. No "average" as a filler descriptor when a zone is `typical` — use the zone label. No style policing beyond this section.
    ```

    This content mirrors CONTEXT.md D-29 (user-message format contract) + D-30 (metric glossary lives inline in the prompt) + the overview rule from INS-06. Total ~80-120 lines of Markdown; loaded once at startup (D-27).

    Wave 2+ (PLAN 05) will import this file via `Path(__file__).parent / "insights_prompts" / "endgame_v1.md"`.
  </action>
  <verify>
    <automated>test -f app/services/insights_prompts/__init__.py &amp;&amp; test -f app/services/insights_prompts/endgame_v1.md &amp;&amp; grep -q '^## Metric glossary' app/services/insights_prompts/endgame_v1.md &amp;&amp; grep -q '^## Output contract' app/services/insights_prompts/endgame_v1.md &amp;&amp; grep -q '^## Section gating' app/services/insights_prompts/endgame_v1.md &amp;&amp; grep -q '^## Cross-section flags' app/services/insights_prompts/endgame_v1.md &amp;&amp; grep -q '^## Series interpretation' app/services/insights_prompts/endgame_v1.md &amp;&amp; test $(wc -l &lt; app/services/insights_prompts/endgame_v1.md) -ge 40</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/services/insights_prompts/endgame_v1.md` exits 0
    - `test -f app/services/insights_prompts/__init__.py` exits 0
    - `grep -c '^## ' app/services/insights_prompts/endgame_v1.md` returns `>= 6` (Output contract, Section gating, Cross-section flags, Series interpretation, Overview rule, Metric glossary, Tone — at least six `## ` headings)
    - `grep 'baseline_lift_mutes_score_gap\|clock_entry_advantage\|no_clock_entry_advantage\|notable_endgame_elo_divergence' app/services/insights_prompts/endgame_v1.md` matches all four flag IDs
    - `grep 'score_gap_timeline\|clock_diff_timeline\|endgame_elo_timeline\|type_win_rate_timeline' app/services/insights_prompts/endgame_v1.md` matches all four timeline subsection IDs
    - `wc -l app/services/insights_prompts/endgame_v1.md` prints at least 40 lines (prompt has real content, not a stub)
    - `app/services/insights_prompts/__init__.py` is empty (0-byte file) OR contains only a docstring — no code
  </acceptance_criteria>
  <done>endgame_v1.md exists with all required sections; package __init__.py created; file is loadable by Wave 2+.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Extend tests/conftest.py with PYDANTIC_AI_MODEL_INSIGHTS env var</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py</read_first>
  <action>
    Insert the following block into `tests/conftest.py` DIRECTLY BELOW the existing `os.environ["SECRET_KEY"] = "test-secret-key-32-bytes-exactly-ok-for-hs256-tests"` line (currently line 13) and BEFORE the `import chess` line (currently line 15). Preserve the blank line above `import chess`:

    ```python
    # Pydantic-AI's built-in "test" provider prefix passes Agent(...) startup
    # validation without requiring any real API key. Individual tests override
    # via monkeypatch + TestModel/FunctionModel (see `fake_insights_agent`
    # fixture added in a later plan). Must precede any `from app...` import so
    # app/services/insights_llm module-level Agent construction (and the
    # lifespan-time get_insights_agent() call in app/main.py) see the env var.
    os.environ["PYDANTIC_AI_MODEL_INSIGHTS"] = "test"
    ```

    Do NOT change any other part of `conftest.py` in this task — the `fake_insights_agent` fixture is added in a later plan (PLAN 05) to keep this plan's blast radius small.

    Run the full test suite to confirm no regressions. Should print 950 passed (or higher if anything new has landed since 2026-04-20).
  </action>
  <verify>
    <automated>grep -A1 'SECRET_KEY.*hs256-tests' tests/conftest.py | grep -q 'PYDANTIC_AI_MODEL_INSIGHTS' || grep -B5 'import chess' tests/conftest.py | grep -q 'PYDANTIC_AI_MODEL_INSIGHTS'; uv run pytest -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'os.environ\["PYDANTIC_AI_MODEL_INSIGHTS"\] = "test"' tests/conftest.py` matches exactly one line
    - That line appears BEFORE `import chess` (verify by: `grep -n 'PYDANTIC_AI_MODEL_INSIGHTS\|import chess' tests/conftest.py | head -2` — PYDANTIC_AI line number &lt; import chess line number)
    - That line appears AFTER `os.environ["SECRET_KEY"]` (verify by: `grep -n 'SECRET_KEY\|PYDANTIC_AI_MODEL_INSIGHTS' tests/conftest.py | head -2` — SECRET_KEY line number &lt; PYDANTIC_AI line number)
    - `uv run pytest -x -q` exits 0 with the existing test count (≥ 950 passed)
    - `uv run ruff check tests/conftest.py` exits 0
    - `uv run ty check tests/conftest.py` exits 0
  </acceptance_criteria>
  <done>conftest sets PYDANTIC_AI_MODEL_INSIGHTS=test before app imports; full suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→.env | `.env` (gitignored) + `.env.example` (committed, no secrets). No production secrets land in git via this plan. |
| module-import→filesystem | `endgame_v1.md` is read at module import in PLAN 05. File missing → startup fails by design (D-27). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-06 | Tampering (startup misconfiguration) | app/core/config.py PYDANTIC_AI_MODEL_INSIGHTS default | mitigate | Default is empty string `""`. PLAN 05's `get_insights_agent()` treats empty → `UserError` via pydantic-ai, aborting lifespan startup (D-22). Enforced by a PLAN 05 test; this plan only prepares the default. |
| T-65-08 | Information Disclosure (secret leak via .env.example) | .env.example | mitigate | `.env.example` includes `ANTHROPIC_API_KEY=sk-ant-...` as a PLACEHOLDER string, never a real key. Real key lives in `/opt/flawchess/.env` on prod and developer-local `.env` (gitignored). Task 3 acceptance verifies placeholder, not real value. |
| T-65-09 | Tampering (dependency supply chain) | pyproject.toml pydantic-ai-slim pin | mitigate | Version bounds `>=1.85,<2.0` prevent a 2.0 breaking change (API has renamed twice historically per RESEARCH.md §2). Also covered by existing pip-audit CI step (see STATE.md "Quick Tasks Completed" 260420-je6). |
</threat_model>

<verification>
- `uv run pytest -x` → 950+ passed (no regressions)
- `uv run ty check app/ tests/` → 0 errors
- `uv run ruff check .` → 0 errors
- `uv run python -c "from pydantic_ai import Agent; from app.core.config import settings; print(settings.PYDANTIC_AI_MODEL_INSIGHTS)"` → prints empty string (when `.env` absent) or configured value (when `.env` present in dev shell)
- `cat app/services/insights_prompts/endgame_v1.md | wc -l` → ≥ 40
</verification>

<success_criteria>
1. pydantic-ai-slim[anthropic,google] installed, importable, version 1.85.x
2. app.core.config.settings exposes PYDANTIC_AI_MODEL_INSIGHTS (default "") and INSIGHTS_HIDE_OVERVIEW (default False) with ty-clean annotations
3. .env.example documents all three new vars (PYDANTIC_AI_MODEL_INSIGHTS, ANTHROPIC_API_KEY, INSIGHTS_HIDE_OVERVIEW) without real secrets
4. app/services/insights_prompts/endgame_v1.md exists, ≥40 lines, has the 6 required `## ` sections and all 4 flag IDs and all 4 timeline subsection IDs
5. tests/conftest.py sets `os.environ["PYDANTIC_AI_MODEL_INSIGHTS"] = "test"` before any `from app...` import
6. Full existing test suite still passes (baseline ≥ 950 tests, all green)
</success_criteria>

<output>
After completion, create `.planning/phases/65-llm-endpoint-with-pydantic-ai-agent/65-01-SUMMARY.md` capturing:
- pydantic-ai-slim version actually installed (from `uv.lock`)
- Confirmation the prompt file has the required section count
- Test suite count before/after (should be identical — this plan adds no new tests)
- Any deviations from the plan (should be none)
</output>
