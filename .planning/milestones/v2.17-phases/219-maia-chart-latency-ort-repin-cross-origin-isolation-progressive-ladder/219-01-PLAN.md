---
phase: 219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/public/maia/ort.wasm.min.js
  - frontend/public/maia/ort.webgpu.min.js
  - frontend/public/maia/ort-wasm-simd-threaded.mjs
  - frontend/public/maia/ort-wasm-simd-threaded.wasm
  - frontend/public/maia/ort-wasm-simd-threaded.asyncify.mjs
  - frontend/public/maia/ort-wasm-simd-threaded.asyncify.wasm
  - frontend/public/maia/README.md
  - frontend/src/lib/engine/engineAssetCache.ts
  - frontend/src/hooks/useMaiaEngine.ts
  - scripts/bench_maia_ort_wasm.mjs
  - renovate.json
  - CHANGELOG.md
autonomous: true
requirements: [MAIAPERF-01, MAIAPERF-02, MAIAPERF-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`frontend/package.json` pins `onnxruntime-web` to the exact string 1.27.0 (no caret, no tilde) and `frontend/package-lock.json` resolves 1.27.0 for it (D-01)."
    - "The six vendored runtime files under `frontend/public/maia/` are byte-identical to `frontend/node_modules/onnxruntime-web/dist/` at 1.27.0 (D-01)."
    - "`frontend/public/maia/README.md`'s six-row SHA-256 table, its per-file byte sizes, and its bundle-to-binary pairing table all reproduce from the files on disk — re-running `sha256sum` over the six files yields exactly the table's values, so a half-applied re-vendor is detectable rather than silent (D-01; MAIAPERF-01 concurrency predicate)."
    - "`ENGINE_ASSET_CACHE_VERSION` in `frontend/src/lib/engine/engineAssetCache.ts` is 4 (read from disk as 3 first, never assumed) and the bump lands in the same commit as the six-file swap (D-02)."
    - "`scripts/package.json` is unchanged by this plan — `onnxruntime-node` stays at its Phase 218 value (RESEARCH Pitfall 7)."
    - "`node scripts/bench_maia_ort_wasm.mjs` prints a table with one row per (threads, batch) pair in a fixed, deterministic order: 1-thread-batch-21, 1-thread-batch-1, 4-thread-batch-21, 4-thread-batch-1 (D-03; MAIAPERF-02 ordering predicate)."
    - "Two configurations whose median timings come out numerically equal are still printed as two separate rows — the script never collapses, merges or dedupes rows on equal values (MAIAPERF-02 adjacency predicate)."
    - "`node scripts/bench_maia_ort_wasm.mjs` exits non-zero and prints a message naming the missing path when `frontend/public/maia/maia3_simplified.onnx` is absent (MAIAPERF-02 empty predicate)."
    - "The script's header comment carries the `219-MEASUREMENTS.md` 1.27.0 reference numbers and states that it is the mandatory manual gate before merging any future `onnxruntime-web` bump (D-03)."
    - "`renovate.json` keeps `onnxruntime-web` out of the grouped `minor and patch (non-major)` PR so a bump always lands in its own reviewable PR (D-04)."
    - "`useMaiaEngine.ts` emits one `[maia-timing]` console line per pipeline phase (exact rung, prefetch, ladder) with elapsed milliseconds, only when `import.meta.env.DEV` is true — production bundles carry no timing output (D-15 harness)."
    - "A MAIAPERF-07 number that lands exactly on its D-15 target counts as MET — the targets are `<=`, not `<` (MAIAPERF-07 adjacency predicate)."
    - "The plan summary reports the three D-15 numbers in D-15's own order (full ladder, first chart paint, exact-rung call), each paired with its `219-MEASUREMENTS.md` baseline (MAIAPERF-07 ordering predicate)."
    - statement: "Any D-15 target that could not be measured at this wave is named explicitly in the summary with the reason, rather than omitted."
      verification: backstop
    - statement: "No backend, migration or dev-database work is performed by this plan (D-16); `bin/reset_db.sh` is never run."
      verification: backstop
  artifacts:
    - scripts/bench_maia_ort_wasm.mjs
    - frontend/public/maia/README.md
    - frontend/src/lib/engine/engineAssetCache.ts
    - renovate.json
  key_links:
    - "`frontend/package.json` pin -> `frontend/node_modules/onnxruntime-web/dist/` -> the six files under `frontend/public/maia/` (a stale link here ships 1.29 bytes under a 1.27 pin)."
    - "`ENGINE_ASSET_CACHE_VERSION` -> `ENGINE_ASSET_VERSION_QUERY` -> every `/maia/*` URL the app and the worker fetch (a missed bump serves cached 1.29 bytes to returning browsers)."
    - "`scripts/bench_maia_ort_wasm.mjs` -> `frontend/node_modules/onnxruntime-web` resolved via `createRequire` from `frontend/package.json` (the script must never gain its own `scripts/package.json` dependency)."
---

<objective>
Re-pin `onnxruntime-web` from 1.29.0 back to 1.27.0, re-vendor the six runtime files
under `frontend/public/maia/`, bump the engine-asset cache version in the same commit,
and add the headless Node benchmark that makes this regression impossible to
re-introduce silently.

Purpose: 1.29.0's wasm build is 1.5-2.3x slower single-threaded than 1.27.0 and gains
nothing from threads on the reference box (`219-MEASUREMENTS.md`). This is the largest
latency win in the phase for the least risk, and it gives waves 2 and 3 a sane baseline
to measure against (D-14).

Output: a 1.27.0 frontend, a regenerated vendoring table, `scripts/bench_maia_ort_wasm.mjs`,
a Renovate rule that forces `onnxruntime-web` into its own PR, dev-only pipeline timing
instrumentation, and a squash-merge to `main` with the recorded benchmark table.

Decisions implemented by this plan:
- D-01: re-pin `onnxruntime-web` to an exact 1.27.0 and re-vendor the six runtime files with the README SHA/size/pairing tables regenerated (Tasks 1 and 2).
- D-02: bump `ENGINE_ASSET_CACHE_VERSION` in the same commit as the file swap, reading the current value off disk; record the post-deploy Cloudflare purge of `/maia/*` as a release step (Tasks 1 and 4).
- D-03: add the headless Node benchmark timing the 21-rung and 1-rung wasm calls at 1 and 4 threads, as a documented manual gate rather than a CI gate (Task 1).
- D-04: planner's call, recorded — ADD the Renovate rule that keeps `onnxruntime-web` out of the grouped minor/patch PR (Task 2).
- D-14: this plan is the first of the three sequential squash-merges and runs the full pre-merge gate plus `npm run build` (Task 4).
- D-15: measured, not asserted — the benchmark table and the wave-1 reference-box browser numbers are both recorded (Tasks 1 and 3).
- D-16: no backend, migration or dev-database work; `bin/reset_db.sh` is never run (whole plan).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder/219-CONTEXT.md
@.planning/phases/219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder/219-RESEARCH.md
@.planning/phases/219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder/219-PATTERNS.md
@.planning/phases/219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder/219-MEASUREMENTS.md
</context>

## Artifacts this plan produces

| Kind | Name | Where |
|------|------|-------|
| New file | `scripts/bench_maia_ort_wasm.mjs` | repo root `scripts/` (NOT `frontend/scripts/`, which does not exist) |
| Function | `timeRun(modelBytes, numThreads, elos)` | `scripts/bench_maia_ort_wasm.mjs` |
| Function | `main()` | `scripts/bench_maia_ort_wasm.mjs` |
| Constant | `BENCH_LADDER_ELOS` (the 21 ladder rungs) | `scripts/bench_maia_ort_wasm.mjs` |
| Constant | `BENCH_THREAD_COUNTS` (`[1, 4]`) | `scripts/bench_maia_ort_wasm.mjs` |
| Constant | `BENCH_TIMED_RUNS` (`3`, median reported) | `scripts/bench_maia_ort_wasm.mjs` |
| Changed constant | `ENGINE_ASSET_CACHE_VERSION` 3 -> 4 | `frontend/src/lib/engine/engineAssetCache.ts` |
| Changed dependency pin | `onnxruntime-web` `1.29.0` -> `1.27.0` | `frontend/package.json` |
| Regenerated table | six-row SHA-256 + size table at v1.27.0 | `frontend/public/maia/README.md` |
| Regenerated table | bundle-to-binary pairing table (re-grepped) | `frontend/public/maia/README.md` |
| New config entry | `packageRules` entry matching `onnxruntime-web` | `renovate.json` |
| Constant | `MAIA_TIMING_LOG_PREFIX` (`'[maia-timing]'`) | `frontend/src/hooks/useMaiaEngine.ts` |
| Function | `logMaiaPhaseTiming(phase, elapsedMs)` (dev-only) | `frontend/src/hooks/useMaiaEngine.ts` |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end 1.27.0 re-pin — dependency, vendored bytes, cache version, and a benchmark that proves it</name>
  <precondition>The npm registry is reachable from this machine (`npm view onnxruntime-web@1.27.0 version` prints `1.27.0`); the re-pin cannot be performed offline.</precondition>
  <files>frontend/package.json, frontend/package-lock.json, frontend/public/maia/ort.wasm.min.js, frontend/public/maia/ort.webgpu.min.js, frontend/public/maia/ort-wasm-simd-threaded.mjs, frontend/public/maia/ort-wasm-simd-threaded.wasm, frontend/public/maia/ort-wasm-simd-threaded.asyncify.mjs, frontend/public/maia/ort-wasm-simd-threaded.asyncify.wasm, frontend/src/lib/engine/engineAssetCache.ts, scripts/bench_maia_ort_wasm.mjs</files>
  <read_first>
    - `frontend/package.json` (the `onnxruntime-web` line, currently `1.29.0` at line 34)
    - `frontend/public/maia/README.md` in full — the "Runtime binary ownership" section documents the re-vendor procedure and the pairing-verification method
    - `frontend/src/lib/engine/engineAssetCache.ts` lines 30-80 — read the CURRENT value of `ENGINE_ASSET_CACHE_VERSION` off disk; RESEARCH Pitfall 1 records it as 3, but re-read it rather than trusting any document
    - `scripts/inspect_maia_onnx.mjs` in full — the resolve-`onnxruntime-web`-from-`frontend/node_modules` pattern this new script copies verbatim
    - `frontend/public/maia/maia-worker.js` lines 540-600 — the exact feed shape (`tokens` float32 `[B, 64, 12]`, `elo_self` float32 `[B]`, `elo_oppo` float32 `[B]`) the benchmark must build
    - `219-MEASUREMENTS.md` in full — the reference numbers that go in the new script's header comment
    - `219-RESEARCH.md` Pitfalls 1, 7 and 8
  </read_first>
  <action>
Implements D-01, D-02, D-03, D-16.

Step 1 — re-pin. From `frontend/`, run `npm install onnxruntime-web@1.27.0`. The
resulting `frontend/package.json` entry must be the exact string `1.27.0` (no caret,
no tilde), matching the pre-6f19e0567 style. Let npm rewrite `package-lock.json`; never
hand-edit the lockfile. Do NOT touch `scripts/package.json` — its `onnxruntime-node`
entry is a different package moved by Phase 218 for an unrelated reason (RESEARCH
Pitfall 7); a diff that touches it is a scope error.

Step 2 — re-vendor. Copy all six runtime files from
`frontend/node_modules/onnxruntime-web/dist/` into `frontend/public/maia/`:
`ort.wasm.min.js`, `ort.webgpu.min.js`, `ort-wasm-simd-threaded.mjs`,
`ort-wasm-simd-threaded.wasm`, `ort-wasm-simd-threaded.asyncify.mjs`,
`ort-wasm-simd-threaded.asyncify.wasm`. Confirm with `cmp` that each vendored file is
byte-identical to its `dist/` source.

Step 3 — cache version, same commit (D-02). Bump `ENGINE_ASSET_CACHE_VERSION` from the
value you read in step 0 to that value plus one (expected 3 -> 4). Extend the existing
changelog-style comment block above the constant with a new entry in the same prose
style as its "1 -> 2" and "2 -> 3" entries, attributing the bump to this phase and the
1.27.0 re-vendor. The six-file swap and this bump must be in ONE commit — a returning
browser must never be able to pair a 1.27 pin with cached 1.29 bytes.

Step 4 — the benchmark (D-03). Create `scripts/bench_maia_ort_wasm.mjs` at the repo
root `scripts/` directory (there is no `frontend/scripts/`, RESEARCH Pitfall 8). Copy
`scripts/inspect_maia_onnx.mjs`'s import block verbatim: `createRequire` against
`path.join(FRONTEND_DIR, 'package.json')`, then a dynamic `import` of the resolved
`onnxruntime-web` entry via `pathToFileURL`. The script must NOT add a dependency to
`scripts/package.json`.

Behavior of the script:
  - Named constants only (CLAUDE.md no-magic-numbers): `BENCH_THREAD_COUNTS = [1, 4]`,
    `BENCH_TIMED_RUNS = 3`, `BENCH_LADDER_ELOS` = 600..2600 step 100 (21 values,
    generated from named min/max/step constants, mirroring `maiaEncoding.ts`'s own
    generation formula), `BENCH_SINGLE_ELOS` = the single middle rung.
  - Resolve the model at `frontend/public/maia/maia3_simplified.onnx`. If that path does
    not exist, print a one-line message that names the missing absolute path and call
    `process.exit(1)` before creating any session.
  - `timeRun(modelBytes, numThreads, elos)`: set `ort.env.wasm.numThreads = numThreads`,
    create an `InferenceSession` with `executionProviders: ['wasm']`, build feeds of
    `tokens` (float32, dims `[elos.length, 64, 12]`, all zeros — timing only, logits are
    irrelevant), `elo_self` and `elo_oppo` (float32, dims `[elos.length]`, the rung
    values), run ONE warmup, then `BENCH_TIMED_RUNS` timed `run()` calls measured with
    `performance.now()`, and return the median. Dispose the tensors and the session.
  - `main()` iterates `BENCH_THREAD_COUNTS` outer and batch size inner, so rows print in
    the fixed order: 1/21, 1/1, 4/21, 4/1. Print exactly one row per (threads, batch)
    pair — never merge, collapse or dedupe two rows whose medians are numerically equal.
  - Header comment: state that this is the mandatory manual gate to run and paste into
    the phase/PR summary before merging ANY future `onnxruntime-web` bump, that it is
    deliberately not a CI gate because timing on shared runners is noise, and quote the
    `219-MEASUREMENTS.md` 1.27.0 reference numbers (21-rung batch ~1,731 ms at 1 thread
    and ~912 ms at 4 threads; 1-rung ~63 ms at 4 threads) as the comparison baseline.
  - Node needs no `crossOriginIsolated` gating — that check exists only on the
    browser/worker codepath (RESEARCH Pitfall 3). Set `numThreads` directly. If the
    4-thread Node run throws a threading error (RESEARCH assumption A1), do not silence
    it: catch it, print the error text against that row, keep the 1-thread rows, and
    record the discrepancy in the plan summary — the browser numbers remain the
    authoritative D-15 gate.

Step 5 — run it. `node scripts/bench_maia_ort_wasm.mjs` from the repo root. Keep the
full printed table for the summary; the 1.27.0 rows must be within run-to-run noise of
the `219-MEASUREMENTS.md` 1.27 rows.
  </action>
  <acceptance_criteria>
    - `grep -c '"onnxruntime-web": "1.27.0"' frontend/package.json` prints `1`
    - `node -e "console.log(require('./frontend/package-lock.json').packages['node_modules/onnxruntime-web'].version)"` prints `1.27.0`
    - For each of the six runtime files, `cmp frontend/public/maia/<f> frontend/node_modules/onnxruntime-web/dist/<f>` exits 0
    - `grep -c 'const ENGINE_ASSET_CACHE_VERSION = 4;' frontend/src/lib/engine/engineAssetCache.ts` prints `1`
    - `git diff --name-only -- scripts/package.json` prints nothing
    - `test -f scripts/bench_maia_ort_wasm.mjs` exits 0
    - `grep -v '^\s*[/*]' scripts/bench_maia_ort_wasm.mjs | grep -c 'onnxruntime-web'` is at least `1` and `grep -c 'onnxruntime-node' scripts/bench_maia_ort_wasm.mjs` prints `0`
    - `node scripts/bench_maia_ort_wasm.mjs` exits 0 and prints four timing rows
    - Renaming the model file aside and re-running the script exits non-zero and prints the missing path; the model file is restored afterwards
  </acceptance_criteria>
  <verify>
    <automated>node scripts/bench_maia_ort_wasm.mjs</automated>
    <fails_when>non-zero exit, or fewer than four timing rows in the output, or the 1-thread 21-rung median exceeds 3,000 ms (which would mean 1.29 bytes are still being loaded rather than 1.27)</fails_when>
  </verify>
  <verify>
    <automated>( cd frontend && npm run build )</automated>
    <fails_when>non-zero exit, or `error TS` / `Cannot find module 'onnxruntime-web'` in the tsc or vite output</fails_when>
  </verify>
  <verify>
    <automated>( cd frontend && npx vitest run src/lib/engine/__tests__/engineAssetCache.test.ts src/lib/engine/__tests__/ortRuntimeSource.test.ts src/lib/engine/__tests__/maiaWorkerScript.test.ts )</automated>
    <fails_when>non-zero exit, or `failed` appears in the vitest summary line</fails_when>
  </verify>
  <reversibility rating="reversible">A pin, six vendored files and one integer constant — `git revert` of the single commit restores 1.29.0 exactly.</reversibility>
  <done>`onnxruntime-web` resolves 1.27.0, the six vendored files match `dist/` byte-for-byte, `ENGINE_ASSET_CACHE_VERSION` is 4 in the same commit, and `node scripts/bench_maia_ort_wasm.mjs` prints a four-row table whose 1.27.0 numbers sit within noise of `219-MEASUREMENTS.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the vendoring tables in README.md and force onnxruntime-web into its own Renovate PR</name>
  <files>frontend/public/maia/README.md, renovate.json</files>
  <read_first>
    - `frontend/public/maia/README.md` in full — specifically the SHA-256 table at ~lines 122-134 (currently headed "at v1.29.0"), the bundle-to-binary pairing table at ~lines 100-103 with its byte sizes, and the model-artifact table at ~lines 16-38 whose formatting style the regenerated tables must match
    - `renovate.json` in full — the existing three-entry `packageRules` array; note there is no `automerge` key anywhere in the file today
    - `219-RESEARCH.md` "Open Questions" item 2 (the D-04 discretion analysis)
  </read_first>
  <action>
Implements D-01 (vendoring table half of it) and D-04.

README tables. Run `sha256sum` over the six re-vendored files and `stat -c '%s'` for
their byte sizes, and rewrite the six-row SHA-256 table with the new hashes and sizes.
Change the table's heading so it states v1.27.0 rather than v1.29.0, and update the
surrounding prose sentence that names the version. Then re-derive the bundle-to-binary
pairing table rather than assuming it carried over: grep each API bundle
(`ort.wasm.min.js`, `ort.webgpu.min.js`) at 1.27.0 for the `.wasm` filename it actually
loads, and write the pairing table from what you find, with the paired binary's real
byte size. D-01 is explicit that the pairing must be re-grepped, not assumed — the
`.mjs`/`.wasm` pairing can shift between ORT versions.

Also add one short line under the SHA table pointing at `scripts/bench_maia_ort_wasm.mjs`
as the timing gate a future re-vendor must run.

Do NOT touch the "Phase 136" single-threading rationale prose in this README — that
sweep belongs to plan 219-02, which is the plan that actually makes the rationale
obsolete by shipping the isolation headers. Removing it here would leave the repo
asserting something untrue for one merge cycle.

Renovate (D-04) — the planner's call, recorded here: ADD the rule. Reasoning to carry
into the summary: `renovate.json` has no `automerge` key today, so every Renovate PR
already needs a human merge; the real risk is not automerge but the existing
`minor and patch (non-major)` grouped rule, which would sweep a future
`onnxruntime-web` 1.27 -> 1.28 bump into a multi-package PR where the benchmark gate is
easy to skip past. Add a fourth `packageRules` entry with `matchPackageNames` of exactly
`["onnxruntime-web"]` and a `groupName` string that names the benchmark script and this
phase, so the bump always lands in its own PR whose title tells the reviewer which
command to run. Cost is five lines of JSON and zero runtime risk. Keep the rule AFTER
the existing grouped rule in the array so it wins by Renovate's last-match-wins ordering.
  </action>
  <acceptance_criteria>
    - For each of the six vendored files, the SHA-256 printed by `sha256sum` appears verbatim in `frontend/public/maia/README.md`
    - `grep -c 'v1.29.0' frontend/public/maia/README.md` prints `0`
    - `grep -c 'bench_maia_ort_wasm.mjs' frontend/public/maia/README.md` is at least `1`
    - `node -e "const r=require('./renovate.json'); const m=r.packageRules.filter(x=>JSON.stringify(x.matchPackageNames||[])==='[\"onnxruntime-web\"]'); if(m.length!==1) process.exit(1); if(!/bench_maia_ort_wasm/.test(m[0].groupName)) process.exit(1)"` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('renovate.json','utf8'))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>for f in ort.wasm.min.js ort.webgpu.min.js ort-wasm-simd-threaded.mjs ort-wasm-simd-threaded.wasm ort-wasm-simd-threaded.asyncify.mjs ort-wasm-simd-threaded.asyncify.wasm; do h=$(sha256sum "frontend/public/maia/$f" | cut -d' ' -f1); grep -q "$h" frontend/public/maia/README.md || { echo "SHA-MISSING $f"; exit 1; }; done; echo "SHA-TABLE-OK"</automated>
    <fails_when>the output contains a `SHA-MISSING` line, or `SHA-TABLE-OK` is absent, or the command exits non-zero</fails_when>
  </verify>
  <verify>
    <automated>node -e "const r=require('./renovate.json'); const m=r.packageRules.filter(x=>JSON.stringify(x.matchPackageNames||[])==='[\"onnxruntime-web\"]'); if(m.length!==1){console.log('RENOVATE-RULE-MISSING');process.exit(1)} if(!/bench_maia_ort_wasm/.test(m[0].groupName||'')){console.log('RENOVATE-RULE-UNLABELLED');process.exit(1)} console.log('RENOVATE-RULE-OK')"</automated>
    <fails_when>the output is `RENOVATE-RULE-MISSING` or `RENOVATE-RULE-UNLABELLED`, or the command exits non-zero (malformed JSON)</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation tables and one JSON object — revertible in one commit.</reversibility>
  <done>The README's SHA table, sizes and pairing table all reproduce from the 1.27.0 files on disk, and `renovate.json` carries a single `onnxruntime-web` rule whose group name names the benchmark script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dev-only Maia pipeline timing instrumentation, and the first MAIAPERF-07 reference-box reading</name>
  <files>frontend/src/hooks/useMaiaEngine.ts, frontend/src/hooks/__tests__/useMaiaEngine.test.ts</files>
  <read_first>
    - `frontend/src/hooks/useMaiaEngine.ts` in full — the module header describing the three-phase pipeline, `planNextRequest` (~lines 190-211), and the effect that issues the planned request and folds the result
    - `frontend/src/hooks/__tests__/useMaiaEngine.test.ts` lines 1-120 — the `FakeLease` harness (manually-resolvable `analyze()` queue and `whenReady()`)
    - `frontend/src/lib/engine/maiaWorkerHost.ts` around line 531 — the existing `console.info` idiom this instrumentation mirrors
    - `219-CONTEXT.md` D-15 (the three targets and the measurement conditions)
    - `219-MEASUREMENTS.md` "Live product numbers today" (the baseline this reading is compared against)
  </read_first>
  <behavior>
    - When `import.meta.env.DEV` is false, no `[maia-timing]` line is emitted for any pipeline phase.
    - When `import.meta.env.DEV` is true, each completed live request emits exactly one line whose text contains the phase label and an integer millisecond count.
    - A prefetch request emits its own line labelled as the prefetch phase, distinct from the live-position labels.
    - A request whose result is discarded as stale emits no timing line (a discarded result has no meaningful elapsed time to report).
  </behavior>
  <action>
Implements the D-15 measurement harness leg of MAIAPERF-07.

There is no existing timing surface anywhere in the Maia pipeline — the "Exact rung
257 ms / prefetch 283 ms / remaining ladder 4.3 s" figures in `219-MEASUREMENTS.md` were
taken ad hoc. D-15 requires a browser measurement per plan against the same three
targets in all three waves, so build the instrument once, here, and reuse it in 219-02
and 219-03.

Add to `useMaiaEngine.ts`, near the file's other module constants:
`MAIA_TIMING_LOG_PREFIX` set to the string `[maia-timing]`, and a small helper
`logMaiaPhaseTiming(phase: string, elapsedMs: number): void` that returns immediately
unless `import.meta.env.DEV` is true, and otherwise emits a single `console.info` line
containing the prefix, the phase label and the rounded elapsed milliseconds. Keep the
helper under five statements.

Capture `performance.now()` when the pipeline issues a planned request, and call
`logMaiaPhaseTiming` once when that request's result is folded in — not when it is
discarded as stale. Label the phases with named string constants, one per pipeline
phase: the exact selected-ELO rung on the live position, the exact rung on the prefetch
position, and the remaining-ladder request. Do not change any pipeline behavior,
ordering, or return value: this task adds observation only.

Then take the reading. Start the dev server (`cd frontend && npm run dev`), open the
analysis board on a game position in a cold session with the position not already
cached, and record from the browser console: the exact-rung milliseconds, the ladder
milliseconds, and the wall time from the position settling to the chart first painting.
Use the claude-in-chrome extension to drive the browser and read the console rather than
asking a human, per the project's device-UAT convention. Note in the summary that at
this wave the worker is still single-threaded — cross-origin isolation ships in 219-02 —
so the ladder number is the 1-thread 1.27.0 figure, and say so explicitly rather than
presenting it as the final MAIAPERF-07 result.

Record all three numbers next to their `219-MEASUREMENTS.md` baselines, in D-15's order,
in the plan summary. A number exactly equal to its target counts as met. If a target
cannot be read at this wave, name it and the reason; never drop it silently.
  </action>
  <acceptance_criteria>
    - `grep -c "MAIA_TIMING_LOG_PREFIX" frontend/src/hooks/useMaiaEngine.ts` is at least `2` (declaration plus use)
    - `grep -c "import.meta.env.DEV" frontend/src/hooks/useMaiaEngine.ts` is at least `1`
    - `( cd frontend && npx vitest run src/hooks/__tests__/useMaiaEngine.test.ts )` exits 0
    - `( cd frontend && npm run build && grep -rc 'maia-timing' dist/assets/*.js | grep -v ':0$' )` finds no match — the prefix string is absent from the production bundle
    - `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200` still exits 0 (unchanged; backend untouched)
    - The plan summary contains three labelled numbers with their baselines
  </acceptance_criteria>
  <verify>
    <automated>( cd frontend && npx vitest run src/hooks/__tests__/useMaiaEngine.test.ts )</automated>
    <fails_when>non-zero exit, or `failed` appears in the vitest summary line</fails_when>
  </verify>
  <verify>
    <automated>( cd frontend && npm run build >/dev/null 2>&1 ) && if grep -rq 'maia-timing' frontend/dist/assets/ 2>/dev/null; then echo "TIMING-LEAKED-TO-PROD"; else echo "TIMING-DEV-ONLY-OK"; fi</automated>
    <fails_when>the output is `TIMING-LEAKED-TO-PROD`, or `TIMING-DEV-ONLY-OK` is absent from the output</fails_when>
  </verify>
  <verify>
    <human-check>Reference-box browser reading taken via claude-in-chrome on the dev server: exact-rung ms, ladder ms, and position-settled-to-first-chart-paint ms, each recorded against its `219-MEASUREMENTS.md` baseline in the plan summary, with an explicit note that the worker is still single-threaded at this wave.</human-check>
  </verify>
  <reversibility rating="reversible">A dev-gated logging helper and two `performance.now()` reads; deleting them restores the previous behavior exactly.</reversibility>
  <done>`useMaiaEngine.ts` emits one `[maia-timing]` line per completed pipeline phase in dev and nothing in a production build, and the three MAIAPERF-07 numbers for wave 1 are recorded in the summary with their baselines.</done>
</task>

<task type="auto">
  <name>Task 4: Pre-merge gate, CHANGELOG entry, squash-merge to main</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CLAUDE.md` — the "Pre-merge gate (MANDATORY before squash-merging to `main`)" block, verbatim; run every command in it
    - `CHANGELOG.md` lines 1-40 — the `## [Unreleased]` section and the heading style used under it
    - `docs/git-workflow.md` — the squash-merge and changelog discipline
    - `219-CONTEXT.md` D-02 (the Cloudflare purge release step this summary must record) and D-14
  </read_first>
  <action>
Implements D-14 (this plan's own squash-merge gate) and records D-02's release step.

Run the full CLAUDE.md pre-merge gate, in order, and resolve every output:
`uv run ruff format app/ tests/ scripts/ analysis/`; `uv run ruff check . --fix`;
`uv run ty check app/ tests/ scripts/`;
`uv run --project analysis --with ty ty check analysis/`;
`uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200`;
`uv run pytest -n auto -x`; and from `frontend/`, `npm run lint` and `npm test -- --run`.
Then add the D-14 extra: `( cd frontend && npm run build )`. If any step rewrites files,
commit that separately with a `style(...)` or `chore(...)` prefix.

CHANGELOG: append a user-facing bullet under `## [Unreleased]` in the existing heading
style, in plain product language — the Maia human-move chart is roughly twice as fast
because the browser inference runtime was returned to the faster version. Do not name
internal constants in user-facing copy.

Squash-merge. Capture the current branch name first with `git branch --show-current`;
under this project's `phase` branching strategy that is the phase branch, and the GSD
commit helper will recreate it after the merge (which is why the branch is deleted and
re-cut from `main` below, so plan 219-02 stacks cleanly instead of replaying this
plan's diff). Then: check out `main`, `git merge --squash <branch>`, commit with a
`perf(219-01): ...` subject, delete the branch with `git branch -D <branch>`, re-create
it from the new `main` with `git checkout -b <branch>`, and `git push origin main`.
Verify with `git rev-list --left-right --count main...<branch>` that it prints two
zeros, and with `git branch --show-current` which branch you actually ended on before
reporting the merge done.

Summary must carry: the full `scripts/bench_maia_ort_wasm.mjs` table (D-03), the three
wave-1 MAIAPERF-07 numbers with baselines (D-15), the D-04 Renovate reasoning, the
`ENGINE_ASSET_CACHE_VERSION` old and new values, and — as a RELEASE STEP, not a task
that runs in this phase — that a Cloudflare purge of `/maia/*` should follow the eventual
production deploy (D-02), together with RESEARCH Pitfall 6's finding that the shipped
`?v=<n>` query mechanism already makes stale edge entries structurally unservable, so the
purge reclaims edge storage rather than fixing correctness. This phase does not deploy.
  </action>
  <acceptance_criteria>
    - `uv run ruff check .` exits 0
    - `uv run ty check app/ tests/ scripts/` exits 0
    - `uv run pytest -n auto -x` exits 0
    - `( cd frontend && npm run lint )` exits 0
    - `( cd frontend && npm test -- --run )` exits 0
    - `( cd frontend && npm run build )` exits 0
    - `git log -1 --format=%s main` starts with `perf(219-01)`
    - `git rev-list --left-right --count main...$(git branch --show-current)` prints two zeros
    - `sed -n '/## \[Unreleased\]/,/^## /p' CHANGELOG.md` contains a bullet mentioning the Maia chart speed-up
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check . && uv run ty check app/ tests/ scripts/ && uv run pytest -n auto -x && ( cd frontend && npm run lint && npm test -- --run && npm run build )</automated>
    <fails_when>non-zero exit at any stage, or `error:` / `failed` / `FAILED` in the ruff, ty, pytest, eslint, vitest or tsc output</fails_when>
  </verify>
  <verify>
    <automated>git log -1 --format=%s main | grep -q '^perf(219-01)' && git rev-list --left-right --count main...$(git branch --show-current) | grep -qP '^0\t0$' && echo "MERGE-CLEAN"</automated>
    <fails_when>`MERGE-CLEAN` is absent from the output, or the command exits non-zero (subject prefix wrong, or the branch and `main` have diverged)</fails_when>
  </verify>
  <done>The full pre-merge gate plus `npm run build` is green, `CHANGELOG.md` carries the `[Unreleased]` bullet, and plan 219-01's work is one squash commit on `main` with the working branch re-cut from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> `frontend/node_modules` -> `frontend/public/maia/` | Third-party bytes enter the repo and are then served verbatim to every browser |
| local filesystem -> `scripts/bench_maia_ort_wasm.mjs` | The script reads one local model file and executes the vendored runtime in Node; no network, no user input |
| browser cache / Cloudflare edge -> browser | Version-keyed URLs decide which runtime bytes a returning visitor executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Tampering | The six re-vendored runtime files | medium | mitigate | Task 2's regenerated SHA-256 table is derived from the files on disk and cross-checked against `frontend/node_modules/onnxruntime-web/dist/` with `cmp` in Task 1; a substituted file changes a published hash |
| T-219-02 | Tampering | `onnxruntime-web@1.27.0` from npm | low | accept | Not a new package: this is a downgrade to the exact version this codebase ran on until 2026-09-05, already audited in Phases 151 and 217. No Package Legitimacy Gate applies (RESEARCH: audit N/A) |
| T-219-03 | Denial of Service (self-inflicted) | `ENGINE_ASSET_CACHE_VERSION` | medium | mitigate | Bumped in the same commit as the file swap (D-02) so no browser can pair a 1.27 pin with cached 1.29 bytes; the `?v=<n>` query makes stale entries at all three cache layers structurally unservable |
| T-219-04 | Information Disclosure | Dev-only timing console output | low | mitigate | `logMaiaPhaseTiming` returns early unless `import.meta.env.DEV`; Task 3's verify greps the production bundle for the prefix string and fails if present. The values are wall-clock durations, not user data |
| T-219-05 | Tampering | Future silent `onnxruntime-web` bump re-introducing the regression | medium | mitigate | D-04's Renovate rule forces the bump into its own PR whose group name names the benchmark; D-03's script is the documented gate to run before approving it |
| T-219-SC | Tampering | npm install of `onnxruntime-web@1.27.0` | low | accept | No NEW package is introduced — a version pin moves backwards to a previously-shipped, already-audited version. RESEARCH's Package Legitimacy Audit records this phase as N/A with no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries, so no blocking legitimacy checkpoint is required |
</threat_model>

<verification>
- `onnxruntime-web` resolves 1.27.0 in both `frontend/package.json` and `frontend/package-lock.json`
- The six vendored files are byte-identical to `dist/`, and the README's SHA/size/pairing tables reproduce from disk
- `ENGINE_ASSET_CACHE_VERSION` is 4, bumped in the same commit as the file swap
- `scripts/package.json` is untouched
- `node scripts/bench_maia_ort_wasm.mjs` prints four rows in fixed order, exits non-zero on a missing model
- `renovate.json` carries exactly one `onnxruntime-web` rule naming the benchmark
- The dev-only timing prefix is absent from the production bundle
- The full CLAUDE.md pre-merge gate plus `npm run build` is green; the work is one squash commit on `main`
</verification>

<success_criteria>
MAIAPERF-01 and MAIAPERF-02 are both satisfied and land as one squash-merge with the full
pre-merge gate plus `npm run build` green (roadmap success criterion 1). The benchmark
table for 1.27.0 is pasted in the summary and sits within run-to-run noise of the
`219-MEASUREMENTS.md` 1.27 rows. The wave-1 MAIAPERF-07 numbers are recorded with their
baselines and with an explicit note that the worker is still single-threaded at this wave.
`CHANGELOG.md` carries the `[Unreleased]` bullet and the Cloudflare purge is recorded as
a release step, not performed.
</success_criteria>

<output>
Create `.planning/phases/219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder/219-01-SUMMARY.md` when done
</output>
