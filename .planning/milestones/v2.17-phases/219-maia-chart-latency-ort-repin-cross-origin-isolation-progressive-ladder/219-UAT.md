---
status: complete
phase: 219-maia-chart-latency-ort-repin-cross-origin-isolation-progressive-ladder
plan: 02
source: [219-02-PLAN.md Task 3, 219-03-PLAN.md Task 3, 219-VERIFICATION.md]
started: 2026-09-06T13:00:00Z
updated: 2026-09-06T17:55:00Z
---

## Current Test

[testing complete]

## Executor's note (read before the six legs below)

This plan's executor session had no claude-in-chrome browser-automation tool
available (see 219-02-SUMMARY.md Deviations). Per the plan's own instruction,
every leg below that requires reading a live DOM/console/network-panel value
is recorded as **pending**, not fabricated and not marked passed. What COULD
be verified without a browser — the headers a server actually sends, and
whether the third-party subresources this phase depends on are reachable and
carry the expected response headers — was verified via `curl` and is recorded
as supporting evidence under each leg. None of this evidence is a substitute
for the real UAT leg; it only narrows what could go wrong when the browser
pass runs.

**Automated verify already run (plan's own automated check):** the dev
server already running on `http://localhost:5173` (left running per the
orchestrator's own instruction — not started or killed by this session)
responds with both isolation headers:

```
$ curl -sfI http://localhost:5173/ | tr -d '\r' | grep -Eic '^cross-origin-(opener-policy: same-origin|embedder-policy: require-corp)$'
2
```

Full response:
```
HTTP/1.1 200 OK
Vary: Origin
Content-Type: text/html
Cache-Control: no-cache
Etag: W/"ccc-ecm64t98SQ4yRJau+GxtvmZ/Mz0"
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Embedder-Policy: require-corp
Date: Sun, 06 Sep 2026 13:00:44 GMT
```

This is the precondition for every leg below (`self.crossOriginIsolated`
can only ever be `true` if the document response carries both headers) —
confirmed present, but the actual `self.crossOriginIsolated` read, the
Google OAuth redirect, the Umami/font network panel, the worker's reported
thread count, and the WebGPU respawn all require a real browser tab, which
this session does not have.

## Tests

### 1. Fresh navigation: self.crossOriginIsolated
expected: `self.crossOriginIsolated === true` in the page console.
result: pass (orchestrator browser pass below)
  Executor note: requires reading a live page's console; not
  measurable via curl. Precondition (both headers on the document response)
  confirmed present above.

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **PASS** — `self.crossOriginIsolated === true`, `typeof SharedArrayBuffer === 'function'` on a fresh `/analysis` load (`navigator.hardwareConcurrency` 16).

### 1b. Service-worker-served offline navigation: self.crossOriginIsolated
expected: `true` after loading once online (populates the `html-shell`
  Workbox runtime cache), then going offline in DevTools and reloading —
  per 219-RESEARCH.md Pitfall 2, the `NetworkFirst` route stores whatever
  headers were present on the last successful online fetch, so a shell
  cached AFTER this phase's headers shipped should still read `true` when
  served from cache. A device that goes offline before ever loading this
  build gets a pre-change cached shell without the headers and degrades to
  one thread via `chooseWasmThreadCount()`'s fail-safe — expected, not a bug.
result: pass (evidence: cached html-shell responses carry both headers; literal offline reload not driven — see browser pass #2 below)

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **still pending** — DevTools offline throttling is not reachable from the extension; HUMAN-UAT.

  **Orchestrator browser pass #2 (2026-09-06 17:05, claude-in-chrome, dev server, phase branch at `c0fcdc992`):** **PASS (by cached-response evidence; the literal DevTools offline reload was not driven).** Read the Workbox `html-shell` runtime cache from the page via `caches.open('html-shell')`: the entries fetched AFTER this phase's headers shipped (`/` and `/analysis`) carry `cross-origin-opener-policy: same-origin` and `cross-origin-embedder-policy: require-corp` on the cached `Response`; three entries cached BEFORE the phase (`/bots`, `/analysis?game_id=824230`, `/analysis?line=e2e4,c7c5,...`) carry neither — exactly the pre-change-shell case 219-RESEARCH.md Pitfall 2 predicts, which `NetworkFirst` refreshes on the next online visit and `chooseWasmThreadCount()` degrades to one thread in the meantime. Since a SW-served navigation applies the policies from the response the SW hands back, a post-phase cached shell yields `crossOriginIsolated === true` offline. `self.crossOriginIsolated === true` and `SharedArrayBuffer` present on the live page were re-confirmed in the same session. Owner may downgrade to pending if the literal offline reload is wanted.

### 2. Google login round-trip under COOP
expected: opening the login page and starting the Google flow performs a
  full-page redirect to the identity provider and returns (COOP
  `same-origin` only governs popup/opener relationships, so a redirect flow
  is structurally unaffected — 219-RESEARCH.md "State of the Art",
  `LoginForm.tsx`/`RegisterForm.tsx` verified as `window.location.href =
  ...` at research time).
result: pass
source: owner-override
note: marked passed by the owner on 2026-09-06 (redirect-based OAuth flow is structurally unaffected by COOP same-origin; `LoginForm.tsx`/`RegisterForm.tsx` use `window.location.href`, verified at research time). Executor note: requires driving the OAuth flow in a real browser and
  observing the navigation; not measurable via curl (the flow needs a live
  Google identity-provider round trip and cookies/session state this
  session cannot drive).

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **still pending** — needs a real Google account round-trip; HUMAN-UAT (never sign in with credentials from automation).

### 3. Umami script load under COEP
expected: a network entry for `analytics.flawchess.com/script.js` with a
  200 status and no COEP-blocking console error.
result: pass (orchestrator browser pass below)
  Executor note: pending for the actual browser network-panel/console
  observation. Supporting `curl` evidence gathered this session:
  ```
  $ curl -sI --max-time 5 https://analytics.flawchess.com/script.js
  HTTP/2 200
  content-type: application/javascript; charset=UTF-8
  access-control-allow-origin: *
  ```
  The response does **not yet** carry `cross-origin-resource-policy:
  cross-origin` — expected, since this phase's `deploy/Caddyfile` change has
  not been deployed to production yet (this phase ends at the squash-merge,
  per D-14/219-02-PLAN.md Task 4 — the Caddy CORP header is a post-deploy
  item). The `access-control-allow-origin: *` the response already sends is
  what the `crossorigin="anonymous"` attribute added to `frontend/index.html`
  this task (219-02 Task 2) relies on to satisfy COEP via CORS in dev,
  preview, and pre-deploy production, ahead of the Caddy fix landing.
  Tracking itself (events reaching the Umami dashboard) is a separate,
  post-deploy-only check — `data-domains="flawchess.com"` on the script tag
  suppresses tracking on localhost regardless of headers, so this is
  HUMAN-UAT deferred to the eventual release, not something any pre-deploy
  session (browser or headless) can observe.

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **PASS** — `performance.getEntriesByType('resource')` shows `analytics.flawchess.com/script.js` with `responseStatus` 200 (initiatorType `script`) on the isolated page, no COEP block. Tracking itself stays post-deploy-only as noted.

### 4. Google Fonts load under COEP
expected: both Google Fonts links load with no COEP error, and headings
  render in the expected typeface rather than a fallback.
result: pass (orchestrator browser pass below)
  Executor note: pending for the actual rendered-typeface observation (requires
  visual inspection of a real page). Supporting `curl` evidence gathered
  this session confirms the CSS response already carries the CORP header
  219-MEASUREMENTS.md recorded:
  ```
  $ curl -sI --max-time 5 "https://fonts.googleapis.com/css2?family=Fredoka:wght@600"
  HTTP/2 200
  cross-origin-resource-policy: cross-origin
  access-control-allow-origin: *
  ```

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **PASS** — both `fonts.gstatic.com` woff2 responses show `responseStatus` 200 under COEP, `document.fonts.check('600 16px Fredoka')` and `check('400 16px "Nunito Sans"')` are both `true` after `document.fonts.ready`.

### 5. Maia worker thread count reported
expected: the `ready` console line this plan's Task 1 added
  (`[maia-worker] ready — backend=... numThreads=...`) reports a number
  greater than 1 on the reference box (16 hardware threads, isolated —
  expect 4 per `chooseWasmThreadCount()`'s formula).
result: pass (orchestrator browser pass below)
  Executor note: requires opening the analysis board in a real browser
  and reading the console; not measurable via curl or Node (the formula
  itself is exercised by 9 new automated boundary-case tests in
  `maiaWorkerScript.test.ts`, all passing — see 219-02-SUMMARY.md — but a
  passing unit test over a sandboxed `node:vm` `self.crossOriginIsolated`
  flag is not the same claim as a real Worker choosing 4 threads in a real
  isolated browser tab).

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **PASS** — after a fresh `acquireMaiaWorker()` the host logged `[maia-worker] ready — backend=wasm numThreads=4` (host `whenReady()` 1108 ms); a raw `new Worker('/maia/maia-worker.js?v=4')` sent the plan's `init` message returned `{type:'ready', backend:'wasm', numThreads:4}` in 1184 ms. Direct worker timings at 4 threads: 1 rung 152 ms, 21 rungs 903 ms (single-thread control from wave 1: 190 ms / 1720 ms). See the stale-cache finding in 219-02-SUMMARY.md — the first attempt hung until the browser's `?v=4` cache entries were refreshed.

### 6. WebGPU-unavailable to wasm respawn path
expected: driving the fallback deliberately confirms the worker respawns
  pinned to wasm and reports `ready` again, with WebGPU behavior itself
  unregressed.
result: pass (respawn path driven by a REAL WebGPU failure; a WebGPU-success run remains unverified on this hardware — see browser pass #3)
  **Orchestrator browser pass #3 (2026-09-06 17:50, claude-in-chrome, Chrome 152/Linux, dev server, phase branch at `ca060b80e`):** the box's `requestAdapter()` had returned null because Chrome's GPU process ran without Vulkan (`chrome://gpu`: "Vulkan: Disabled", Dawn saw only an OpenGL ES compatibility adapter). After the owner enabled `chrome://flags/#enable-vulkan` and relaunched, `requestAdapter()` returned the AMD Radeon 780M (rdna-3) with `shader-f16` advertised, so `probeOrtBackend()` chose `webgpu`. With `window.Worker` instrumented BEFORE an in-app navigation from `/library` to `/analysis`, the host's full sequence was captured:
  - Worker #1 created at t=1259 ms, `init backend=webgpu` (24,254,953-byte asyncify runtime transferred). At t=2801 ms it posted `{type:'webgpu-unavailable', message:"failed to call OrtRun(). ERROR_CODE: 1 ... Program Cast requires f16 but the device does not support it."}` and the host terminated it in the same tick.
  - Worker #2 created at t=2817 ms (16 ms later), `init backend=wasm` (13,479,978-byte wasm-only runtime — a DIFFERENT binary, per Phase 213-09), `ready backend=wasm numThreads=4` at t=3503 ms, then serviced the already-queued `analyze` requests (results streaming from t=3582 ms). Console: `[maia-worker] ready — backend=wasm numThreads=4`. Chart, verdict and bar rendered as in leg 7.
  - Not verifiable here: a SUCCESSFUL WebGPU inference. ORT's WebGPU EP rejects f16 on this RADV device even though the adapter advertises `shader-f16`, so "WebGPU behavior unregressed" still needs a device where the WebGPU EP works (e.g. Windows/NVIDIA). Follow-up seed (out of phase scope): every page load on such a device pays ~1.5 s for the doomed WebGPU attempt before the wasm respawn because `webgpuFailed` is page-session state; persisting the pin (or probing device-level f16) would remove that cost.
  Executor note: requires triggering the respawn in a real browser tab
  and observing the worker's second `ready` message; not measurable via
  curl or Node. The respawn's message-protocol correctness (independent of
  a real GPU) is covered by existing automated tests in
  `maiaWorkerHost.test.ts` (the `webgpu-unavailable` respawn tests, all
  passing and unmodified by this plan's changes).

  **Orchestrator browser pass (2026-09-06, claude-in-chrome, Chrome/Linux, dev server, `main` at `cb3d7549a`):** **still pending (hardware)** — this box has `navigator.gpu` but `requestAdapter()` resolves null, and the worker selects wasm up front on `backend:'auto'` (raw worker: `ready backend=wasm numThreads=4`, no `webgpu-unavailable` message), so the respawn cannot be driven here; same deferral as the Phase 217 UAT. The wasm-pinned respawn message protocol remains covered by `maiaWorkerHost.test.ts`.

### 7. Post-review-fix browser smoke (CR-01, WR-01, WR-02, WR-03 at 398be42ff..c0fcdc992)
expected: On the dev server with the four `fix(219)` commits checked out: the
  Maia worker still reaches `ready` with `backend=wasm numThreads=4` (WR-01's
  `initTimeout`/`forceSingleThread` plumbing did not regress the normal
  threaded init); after a move on the Human tab the chart still paints
  coarse-then-fill and the position verdict appears only once the ladder is
  complete; navigating to a NEW position immediately clears the quality bar
  and verdict (CR-01: no stale previous-position ladder rendered against the
  new candidates) until the new fill pass lands.
result: pass
  **Orchestrator browser pass (2026-09-06 17:13, claude-in-chrome, Chrome/Linux, dev server, phase branch at `c0fcdc992`, tab VISIBLE — a hidden tab pauses the pump by design and shows the "Waiting for Maia analysis..." skeleton indefinitely; this is the Phase 217 "automation artefact" explained):**
  - WR-01: console `[maia-worker] ready — backend=wasm numThreads=4`; no timeout/retry breadcrumb, no `error` message — the bounded `initTimeout` + `forceSingleThread` plumbing did not regress normal threaded init.
  - Coarse-then-fill: `[maia-timing]` lines per position: `exact rung 180ms / coarse 661ms / fill 608ms` (fresh load, 1.e4), `78 / 320 / 370` (start position), `56 / 429 / 305` (after 1.e4 e5). Chart leader labels appear at ~240-290 ms after navigation (coarse paint), verdict + quality bar at ~940-1035 ms (fill complete).
  - CR-01: a 40 ms DOM sampler across two navigations (1.e4 -> start via "Previous move"; 1.e4 -> 1.e4 e5 via click-to-move) recorded the verdict and `…of shown moves` bar going to NULL within 136-204 ms of the navigation and staying null through the entire coarse pass; they reappeared only with the NEW position's text ("All solid — e4 and d4 keep it simple." / "All solid — Nf3, Nc3 and Bc4 keep it simple."). The previous position's verdict ("…e5, d5, c5 and e6…") was never rendered against the new candidates.
  - Screenshot after 1.e4 e5: Maia chart (Nf3/Nc3/Bc4/d4/d3 curves), 100% Good Moves bar, and verdict all rendered; FlawChess and Stockfish cards unaffected.
  - Out of scope, pre-existing (VariationTree.tsx, last touched Phase 200): React dev error "<button> cannot be a descendant of <button>" from the mobile `HorizontalMoveList` (`variation-node-1` wraps `btn-delete-line-1`). Not a Phase 219 gap; captured for a follow-up.

## Wave-2 MAIAPERF-07 reading (D-15)

In D-15's order, each against its `219-MEASUREMENTS.md` baseline and the
wave-1 (219-01, single-thread 1.27.0) reading from `219-01-SUMMARY.md`:

| D-15 target | Baseline (1.29.0, before this phase) | Wave-1 (1.27.0, single-thread, 219-01) | Wave-2 (this plan, multi-thread) |
|---|---|---|---|
| Exact-rung call ≤ 100 ms | ≈257 ms | 200 ms (190 ms direct control) | **192 ms** (152 ms direct control) — NOT MET; a 1-position, 1-rung inference barely parallelises |
| Full 21-rung ladder ≤ 1.5 s | ≈4.3 s | ≈2.2 s (200 ms + 1984 ms) | **≈0.84 s** (192 ms + remaining 20 rungs 650 ms; 903 ms for 21 rungs in one direct call) — MET |
| Position-settled → first chart paint ≤ 0.8 s | ≈4.5 s | 2.17 s | **915 ms** — NOT MET yet (the chart still waits for the complete ladder; 219-03's coarse-first paint is the remaining lever) |

Wave-2 harness (orchestrator, 2026-09-06): dev server on the reference box, Chrome on Linux, `crossOriginIsolated` true, worker `backend=wasm numThreads=4`, fresh `/analysis` load, Human tab, ELO 1500, 1. e4 played on the board (click-to-move), `[maia-timing]` console lines captured in-page, first paint stamped by a MutationObserver on the first `moves-by-rating-leader-*` label after the move list showed `1. e4`. Same method as the wave-1 addendum in 219-01-SUMMARY.md.

The chart still waits for the complete ladder at this wave (progressive
paint is 219-03), so the first-paint number is expected to keep tracking the
ladder number, per the plan's own note.

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

Legs 1, 1b, 3, 4, 5, 6, 7 passed in the orchestrator's browser passes (2026-09-06); leg 2 passed by owner override. Deferred to a WebGPU-capable device: a successful WebGPU inference run (leg 6 second half). Deferred to post-deploy: Umami dashboard event delivery under production CORP (leg 3 note).

## Gaps

None in shipped code — every automated, non-browser-dependent verification
in 219-02-PLAN.md Tasks 1 and 2 passed (67 vitest cases in the two files
Task 1 touched, 3956/3956 in the full frontend suite, `npm run lint` clean,
both Caddy/CI acceptance-criteria greps, and the CI-guard fail/pass proof
against a real preview server with and without the Vite headers). All six
D-10 legs and the three wave-2 D-15 numbers require a real browser tab
(`self.crossOriginIsolated`, a live Google OAuth redirect, a network panel,
a rendered typeface, a worker console line, and a real respawn) that this
executor session does not have access to. HUMAN-UAT / orchestrator legs, not
silently dropped:
- All 6 legs above (claude-in-chrome or a human, against the dev server).
- The wave-2 MAIAPERF-07 browser reading (same harness as 219-01's
  addendum: dev server, fresh load, cold FEN, `[maia-timing]` console
  lines).
- The Umami dashboard leg (post-deploy only — localhost tracking is
  suppressed by design).
- The production Caddy header check (post-deploy only — this phase ends at
  the squash-merge and does not deploy).

## Plan 03 (progressive ladder paint) — MAIAPERF-07 wave-3 reading

**Executor's note:** this executor session also has no claude-in-chrome
browser-automation tool available (the same limitation 219-01 and 219-02
recorded). The `[maia-timing]` instrumentation was extended this plan with
distinct `coarse`/`fill` phase labels (replacing the old single `ladder`
label) so the coarse-pass number can be read separately from the full-ladder
number in the console — this labeling change itself is verified by
`useMaiaEngine.test.ts`'s dev-only-timing describe block (all passing) but
the actual reference-box browser reading is **not measured by the
executor — orchestrator takes it after this plan**, mirroring how 219-01's
wave-1 and 219-02's wave-2 readings were completed as addenda to their own
SUMMARYs.

In D-15's order, each against its `219-MEASUREMENTS.md` baseline and the
wave-1/wave-2 readings already recorded above:

| D-15 target | Baseline (1.29.0) | Wave 1 (1.27.0, 1 thread) | Wave 2 (4 threads) | Wave 3 (coarse-first paint, this plan) |
|---|---|---|---|---|
| Full 21-rung ladder ≤ 1.5 s | ≈4.3 s | ≈2.2 s | ≈0.84 s (192 ms exact + 650 ms remaining 20 rungs) — MET | **0.94 s** median of 991/938/811 ms (idle box, 3 runs) — MET |
| First chart paint ≤ 0.8 s | ≈4.5 s | 2.17 s | 915 ms (chart waited for the full ladder) | **639 ms** median of 678/639/581 ms to the coarse curves appearing — MET |
| Exact-rung call ≤ 100 ms | ≈257 ms | 200 ms | 192 ms (152 ms calling the worker directly) | **81 ms** median of 83/81/70 ms — MET |

**Orchestrator browser pass (2026-09-06, claude-in-chrome, idle reference box after the owner stopped the local remote eval worker):** all three wave-3 numbers taken; method and caveats in the 219-03-SUMMARY.md addendum. Behavioral confirmations: the chart refines in place (skeleton → labels → coarse curves → refined curves, no placeholder swap, no unmount) and the verdict text appears only after the fill pass, never before, in all three runs. PASS.

**Confirmed in the orchestrator's browser pass (originally deferred by the executor):** whether the chart visibly refines in place from the coarse curve to
the full curve with no placeholder swap and no restarted animation, and
whether the position verdict text is identical before and after the fill
pass lands (T-219-14's frozen-state guard is proven by an automated
mutation test in `MaiaMoveQualityBar.test.tsx`, not by a real-browser visual
observation — the plan's own `<verify><human-check>` for Task 3 asks for
both).

**Automated verify already run (this plan's own automated check):**
```
$ grep -c 'maia-timing' frontend/src/hooks/useMaiaEngine.ts
2
```
`useMaiaEngine.test.ts`'s dev-only pipeline timing tests pass (31/31 in the
full file), confirming the coarse and fill phases emit distinct labels and
that the relabelling did not break the existing exact-rung/prefetch timing
behavior.
