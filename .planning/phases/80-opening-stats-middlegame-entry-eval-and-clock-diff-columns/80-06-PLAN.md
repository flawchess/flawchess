---
phase: 80-opening-stats-middlegame-entry-eval-and-clock-diff-columns
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]
tags: [smoke, regression, changelog, checkpoint]

must_haves:
  truths:
    - "Full backend test suite (uv run pytest) exits 0"
    - "uv run ty check app/ tests/ reports zero errors"
    - "uv run ruff check . exits 0; uv run ruff format --check . exits 0"
    - "cd frontend && npm test --run exits 0"
    - "cd frontend && npm run build exits 0"
    - "cd frontend && npm run lint exits 0"
    - "cd frontend && npm run knip exits 0"
    - "CHANGELOG.md [Unreleased] section has a Phase 80 entry under Added subsection"
    - "EXPLAIN ANALYZE on /api/stats/most-played-openings for Adrian's user is no more than 1.5x the pre-Phase-80 baseline (recorded in 02-SUMMARY) — note: query now scans phase IN (1, 2) per D-09"
    - "D-08 manual sanity: in the Openings page DB sample, no opening row's eval mean is dragged by an outlier (|cp| >= 2000); the trim is invisible to the UI but should never produce values like ±20 pawns"
    - "D-09 manual sanity: EG-entry bullet chart renders with correct domain (±3.50 pawns) and neutral zone (±0.35); it should look distinct from MG (which uses ±1.50 / ±0.20) and visibly wider"
    - "D-09 manual sanity: EG confidence pill renders independently of MG pill — for an opening with high MG-entry sample but low EG-entry sample (or vice versa), the two pills can show different levels"
    - "D-06 manual sanity: mobile line 3 (EG pair) renders below mobile line 2 (MG triple) at 320px viewport with the 'EG entry' label visible"
    - "D-10 manual sanity: MG-entry header tooltip contains the literal 'across analyzed games' phrase; EG-entry header tooltip does NOT (avoid copy-paste error)"
    - "Manual UI smoke (human-verify): board hidden on Stats subtab desktop, mobile unchanged, all five new cells render per row (MG bullet | MG pill | clock-diff | EG bullet | EG pill), bookmarked + most-played tables both show new cells, column-header InfoPopovers visible (5 of them), mobile two-line stack at 320px viewport"
  artifacts:
    - path: CHANGELOG.md
      provides: "Phase 80 user-facing entry in Added subsection of [Unreleased]"
      contains: "middlegame-entry"
  key_links: []
---

<objective>
Final phase gate: confirm every CI gate green across both stacks, perf check on the new aggregation (now scanning `phase IN (1, 2)` per D-09), append a CHANGELOG.md entry per CLAUDE.md release process, and run a human UI smoke checkpoint covering the manual-only verifications listed in `80-VALIDATION.md` (CI whisker visual at extreme domain edges, mobile two-line at 320px, confidence pill colors matching `OpeningFindingCard`, EG bullet domain visibly wider than MG, D-10 tooltip wording).

This plan has a checkpoint task — Claude automates everything before the checkpoint, then the user verifies the visual rendering and approves before Phase 80 is marked complete.

Purpose: Catch regressions before merge; deliver a user-facing CHANGELOG entry; close the phase with explicit human approval of the visual quality (impossible to assert in unit tests).
Output: Updated `CHANGELOG.md`; pass/fail signal on the regression suite + perf check; user approval gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-CONTEXT.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-VALIDATION.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-01-SUMMARY.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-02-SUMMARY.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-03-SUMMARY.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-04-SUMMARY.md
@.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-05-SUMMARY.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression matrix (backend + frontend) and capture results</name>
  <files>(none modified — read-only verification)</files>

  <read_first>
    - .planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-VALIDATION.md (lines 17-32 — quick + full suite commands; sampling rate)
    - CLAUDE.md (top-level commands section — confirms exact CI gate commands)
  </read_first>

  <action>
    Run the full regression matrix in this exact order. Capture stdout+stderr to a session log; if any command fails, STOP and report the failure rather than proceeding.

    **Backend gates:**
    1. `uv run ruff check .` — must exit 0.
    2. `uv run ruff format --check .` — must exit 0.
    3. `uv run ty check app/ tests/` — must report zero errors.
    4. `uv run pytest -x -q` — full backend suite must exit 0.

    **Frontend gates (run in a subshell so the parent cwd is preserved):**
    5. `(cd frontend && npm test --run)` — full frontend suite must exit 0.
    6. `(cd frontend && npm run lint)` — must exit 0.
    7. `(cd frontend && npm run knip)` — must exit 0.
    8. `(cd frontend && npm run build)` — must exit 0 (production build sanity check).

    **Perf check (manual record from Plan 02 SUMMARY):**
    9. Read `.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-02-SUMMARY.md`. If it contains an EXPLAIN ANALYZE timing for Adrian's user (~30k games), confirm `total_ms_post / total_ms_pre <= 1.5`. The new query scans `phase IN (1, 2)` (D-09) which is roughly twice the row count of the prior `phase = 1` query — single pass with FILTER is cheaper than two passes, but verify the planner cooperates. If the SUMMARY notes the perf check was deferred to pre-merge review, that's acceptable — flag it for the human checkpoint in Task 3.

    Record the outcome in a structured block (paste into the Plan 06 SUMMARY at the end):
    ```
    ## Regression Matrix Results
    | Gate | Command | Status | Duration | Notes |
    |------|---------|--------|----------|-------|
    | ruff-check | uv run ruff check . | ✅/❌ | Xs | ... |
    | ruff-format | uv run ruff format --check . | ✅/❌ | Xs | ... |
    | ty | uv run ty check app/ tests/ | ✅/❌ | Xs | 0 errors |
    | pytest | uv run pytest -x -q | ✅/❌ | Xs | N tests |
    | vitest | (cd frontend && npm test --run) | ✅/❌ | Xs | N tests |
    | lint | (cd frontend && npm run lint) | ✅/❌ | Xs | ... |
    | knip | (cd frontend && npm run knip) | ✅/❌ | Xs | 0 dead exports |
    | build | (cd frontend && npm run build) | ✅/❌ | Xs | bundle size delta |
    | perf | EXPLAIN ANALYZE comparison (phase IN (1,2)) | ✅/❌/⏭ | — | pre/post ratio or deferred |
    ```
  </action>

  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest -x -q && (cd frontend && npm test --run && npm run lint && npm run knip && npm run build)</automated>
  </verify>

  <acceptance_criteria>
    - `uv run ruff check .` exits 0.
    - `uv run ruff format --check .` exits 0.
    - `uv run ty check app/ tests/` reports zero errors (CI gate).
    - `uv run pytest -x -q` exits 0 with all tests passing (no skips for new tests).
    - `(cd frontend && npm test --run)` exits 0.
    - `(cd frontend && npm run lint)` exits 0.
    - `(cd frontend && npm run knip)` exits 0.
    - `(cd frontend && npm run build)` exits 0.
    - Regression matrix table populated in subsequent SUMMARY artifact (Task 3 gate).
  </acceptance_criteria>

  <done>
    All 8 CI gates green. Perf check confirmed within 1.5x baseline OR explicitly flagged for human checkpoint approval if deferred. Note: query now scans `phase IN (1, 2)` per D-09 — flag any unexpected regression here.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 80 entry to CHANGELOG.md [Unreleased]</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - CHANGELOG.md (full file — confirm the [Unreleased] section structure and recent entries; mirror tone)
    - CLAUDE.md (Changelog & Releases section — "Append one or more bullets under ## [Unreleased] in CHANGELOG.md, grouped into ### Added / ### Changed / ### Fixed / ### Removed / ### Security / ### Tests subsections. Reference the phase number. Keep the tone terse and user-facing")
  </read_first>

  <action>
    1. Read `CHANGELOG.md`. Locate the `## [Unreleased]` section. If it doesn't have an `### Added` subsection yet, add one.

    2. Append the following bullets under `### Added` (or merge into existing bullets if the section is already populated for v1.16):
       ```
       - **Openings → Stats subtab** (Phase 80): five new column cells on bookmarked-openings and most-played-openings tables — average Stockfish evaluation at middlegame entry (signed from your perspective, with 95% CI whisker on a bullet chart), one-sample t-test confidence pill for the middlegame metric, average clock difference at middlegame entry (% of base time + absolute seconds), average Stockfish evaluation at endgame entry (parallel bullet chart with its own wider domain), and a separate confidence pill for the endgame metric. Outlier evaluations (>= 20 pawns) are trimmed from the means. Mobile rows get two stacked lines: middlegame triple (bullet + pill + clock-diff) and endgame pair (bullet + pill). The chess board is hidden on the Stats subtab on desktop to make horizontal room.
       ```

    3. If a `### Changed` subsection makes sense for the cross-cutting refactors:
       ```
       - **Code organization** (Phase 80): extracted `<ConfidencePill>` to a shared component (used by Opening Insights cards and the new Openings → Stats columns) and extracted clock-formatting helpers (`formatSignedSeconds`, `formatSignedPct1`) to `frontend/src/lib/clockFormat.ts` so the two clock-diff cells across the app render identically.
       ```

    4. Keep the tone user-facing (what changed, not how). No phase IDs in the prose other than the parenthetical "(Phase 80)".

    5. No em-dashes more than once per bullet (CLAUDE.md communication style).

    6. Write the file via Edit (not Write) — preserve all existing entries.
  </action>

  <verify>
    <automated>grep -c "Phase 80" CHANGELOG.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "## \[Unreleased\]" CHANGELOG.md` >= 1 (section preserved).
    - `grep -c "Phase 80" CHANGELOG.md` >= 1 (phase reference present).
    - `grep -c "middlegame-entry\|middlegame entry\|endgame entry\|endgame-entry" CHANGELOG.md` >= 1 (user-facing description present).
    - `grep -c "bullet chart\|confidence pill\|clock difference" CHANGELOG.md` >= 1 (concrete features described).
    - File diff (`git diff CHANGELOG.md`) shows additions to `[Unreleased]` only — no edits to released sections.
  </acceptance_criteria>

  <done>
    CHANGELOG.md `[Unreleased]` section has a user-facing Phase 80 entry under `### Added` (and optionally `### Changed`). Both pillars (middlegame and endgame entry) referenced in the user-facing copy. No retroactive edits to released sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UI smoke checkpoint — verify all manual-only items including new EG column, two-line mobile stack, and D-10 tooltip wording</name>

  <what-built>
    Plans 01-05 landed the full Phase 80 stack: backend helper + repository (single SQL pass over `phase IN (1, 2)` with `FILTER (WHERE phase = 1/2)` partitioning) + service wiring (Plans 01-02), frontend whisker primitive + calibrated zone constants for both phases + board hide (Plans 03-04), the user-visible table extension with **five** new column cells per row + **two** new mobile lines + shared ConfidencePill (used 4x per row) + shared clock formatters (Plan 05). Task 1 of this plan confirmed all 8 CI gates green; Task 2 added the CHANGELOG entry. This checkpoint verifies the visual quality, responsive layout, and D-10 tooltip wording that no unit test can fully assert.
  </what-built>

  <how-to-verify>
    1. Start the local stack:
       ```bash
       docker compose -f docker-compose.dev.yml -p flawchess-dev up -d
       bin/run_local.sh
       ```
       Wait until both backend and frontend are responsive (visit http://localhost:5173).

    2. Sign in / use a test user with at least one bookmarked opening and at least 10 games per opening for some openings (so confidence buckets render at all three levels).

    3. **D-03 (board hide on Stats subtab desktop):** Navigate to `/openings/explorer`. Confirm the chess board is visible on the left at the lg+ breakpoint. Click to `/openings/stats`. Confirm the chess board is now hidden, leaving the table area full-width. Click back to `/openings/explorer` — the board reappears with **the same position** (chess.js state preserved per Pitfall 7). Resize to mobile (320-768px) — board layout behavior should be unchanged from before Phase 80.

    4. **D-01 (avg eval bullet chart, MG):** On `/openings/stats`, find an opening with `eval_n >= 30` games. The MG bullet chart cell should show:
       - A signed value bar positioned left/right of zero based on user-perspective.
       - A horizontal CI whisker overlay, with end caps when within domain (±1.50 pawns).
       - For the same opening played as both white and black, eval should flip sign visually.

    5. **D-02 + Pitfall 6 (open-ended whisker):** Find an opening with very few games. The whisker should be wide. If either CI bound exceeds ±1.50 pawns (MG) or ±3.50 pawns (EG), the cap on that side should be SUPPRESSED.

    6. **D-04 (confidence pill, MG):** Confirm:
       - "high" pill (n≥10, p<0.05) renders with the same visual style as on `OpeningFindingCard` (compare side-by-side with an `/openings/insights` finding card).
       - "medium" and "low" pills render with their respective styles.
       - "low" rows have the MG bullet chart cell DIMMED (UNRELIABLE_OPACITY = 0.5).
       - `eval_n === 0` rows show "—" instead of an MG bullet chart.
       - Hovering the pill shows the tooltip with p-value + game count.

    7. **D-05 (clock diff at MG):** Confirm a non-zero clock-diff cell renders as `+8.2% (+24s)` — single signed pct with absolute seconds in muted-foreground parentheses. Compare visual style with the same metric on the Endgames page (Time Pressure at Endgame Entry section) — the two cells should look identical.

    8. **D-08 (outlier trim sanity):** Spot-check one or two rows. The MG and EG bullet values should never visually exceed the chart domain by an absurd amount caused by single decisive positions. (Trim is upstream so this is a sanity check, not a strict assertion.)

    9. **D-09 (avg eval bullet chart + confidence pill, EG):** On the same `/openings/stats` page, find an opening with `eval_endgame_n >= 30` games. The EG bullet chart cell should:
       - Render to the right of the MG cluster (after the clock-diff column).
       - Use a visibly **wider domain** than MG (±3.50 vs ±1.50 pawns) — for the same eval value, the bar position will differ between MG and EG cells.
       - Have its OWN confidence pill, which can show a different level than the MG pill on the same row (e.g. an opening Adrian plays a lot but rarely reaches the endgame can have MG=high, EG=low or vice versa).

    10. **D-06 (mobile two-line stack):** Open Chrome DevTools, set viewport to **320px** wide. Navigate to `/openings/stats`. For each opening row:
        - First line keeps the existing 3-col layout (name | games | WDL bar).
        - **Second line:** small "MG entry" label + bullet chart + confidence pill + clock-diff (4 cells with the leading label, or 3 metric cells with the label inside the first).
        - **Third line:** small "EG entry" label + bullet chart + confidence pill (3 cells with leading label, or 2 metric cells with the label inside the first).
        - No horizontal overflow.
        - Both bookmarked-openings and most-played-openings sections show the same three-line treatment.

    11. **D-10 (tooltip wording):** Click the InfoPopover icon next to the MG eval column header. Confirm the tooltip text contains the verbatim phrase **"across analyzed games"**. Click the InfoPopover icon next to the EG eval column header. Confirm the tooltip text **does NOT** contain "across analyzed games" (different copy, no coverage caveat).

    12. **Bookmarked + most-played parity:** Both tables should render the same five new cells and the same mobile two-line treatment.

    13. **Sentry sanity (no errors generated):** Open browser DevTools → Console. Refresh `/openings/stats` and click between subtabs. No JS errors in console; no failed network calls in Network tab.

    Approve only when all 12 items pass. Describe any issues precisely.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 12 items pass.

    If any items fail, describe the issue in the form: "Item X — ⟨observation⟩ ⟨specific cell / opening / viewport⟩". Plan 06 will be revised based on the description.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Smoke + checkpoint only. No code changes beyond CHANGELOG.md (text-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-13 | Repudiation | Manual checkpoint approval not recorded | mitigate | Approval is captured in the Plan 06 SUMMARY artifact + commit message. Future debugging can reference the approval timestamp + tester (Adrian — solo developer). |
</threat_model>

<verification>
- All 8 CI gates from Task 1 green.
- CHANGELOG entry committed.
- Human approval signal received for the 12 manual UI smoke items (D-01 through D-10).
</verification>

<success_criteria>
- Backend: ruff, ty, pytest all green.
- Frontend: vitest, lint, knip, build all green.
- Perf: post-Phase-80 EXPLAIN ANALYZE on Adrian's user is within 1.5x baseline (or explicitly approved by human checkpoint if deferred). Note: query now scans `phase IN (1, 2)` per D-09.
- CHANGELOG.md `[Unreleased]` has a user-facing Phase 80 entry covering both pillars (MG + EG).
- Human approval recorded for the 12 manual UI smoke items (D-01 through D-10 visual quality + InfoPopovers + bookmarked/most-played parity + console-error sanity + D-10 tooltip wording verbatim).
</success_criteria>

<output>
After completion, create `.planning/phases/80-opening-stats-middlegame-entry-eval-and-clock-diff-columns/80-06-SUMMARY.md` with:
- Regression matrix table (8 gates, pass/fail, durations).
- Perf check result (pre/post timings or deferral note) — explicitly call out the `phase IN (1, 2)` scan vs prior `phase = 1`.
- CHANGELOG diff (paste the added bullet text).
- Human checkpoint approval text (verbatim resume-signal response).
- Phase 80 closure note: "All D-01..D-10 verified, ready to merge."
- Final commit hash.
</output>
