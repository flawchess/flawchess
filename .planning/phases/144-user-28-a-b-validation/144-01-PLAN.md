---
phase: 144-user-28-a-b-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ab_validate_gate.py
  - tests/scripts/test_ab_validate_gate.py
autonomous: true
requirements: [VALID-01, VALID-02]
must_haves:
  truths:
    - "The ungated arm runs `_detect_tactic_for_flaw` directly and NEVER invokes `apply_forcing_line_filter` (genuinely pre-143, not `--margin 0` — D-02a)."
    - "The gated arm runs `_classify_tactic_gated` at the test margin; both arms read the SAME stored blobs, so no engine and no `eval_cp` cross-machine variance enters the comparison."
    - "For any flaw, the gated arm tags a subset of what the ungated arm tags (gated ≤ ungated per orientation)."
    - "The harness emits per-motif ungated/suppressed/survived counts and a depth distribution per arm, plus a dropped-case list (orientation, motif, FEN, depth, PV line, lichess link)."
    - "The script performs zero DB writes (no commit, no UPDATE) and never instantiates an EnginePool / calls Stockfish."
  artifacts:
    - "scripts/ab_validate_gate.py (read-only A/B harness with CLI: --db, --user-id, --margin, optional neighbourhood sweep)"
    - "tests/scripts/test_ab_validate_gate.py (Wave 0 unit + seeded-integration tests)"
  key_links:
    - "ungated arm -> app.services.flaws_service._detect_tactic_for_flaw (no gate)"
    - "gated arm -> app.services.flaws_service._classify_tactic_gated(margin=...) -> apply_forcing_line_filter"
    - "blob loading -> single non-paginated select on game_flaws (allowed/missed_pv_lines deferred cols, projected explicitly) + game_positions eval_cp/pv"
    - "motif name decode -> TacticMotifInt(motif_int).name"
---

<objective>
Build the engine-free A/B harness `scripts/ab_validate_gate.py` that loads user-28's MultiPV-blob-bearing dev flaws once and runs tactic detection twice over the same stored inputs — **ungated** (old arm, `_detect_tactic_for_flaw` directly) vs **gated-at-margin** (new arm, `_classify_tactic_gated`) — fully in-memory, with zero DB writes and no engine call. The harness diffs the two arms and produces the measurement machinery: per-motif ungated/suppressed/survived counts, a per-arm depth distribution, a dropped-case list (with lichess deep-links), and a committed-markdown report writer. Ship it with a Wave 0 test scaffold first.

Purpose: VALID-01 (isolate the gate's effect from `eval_cp` non-determinism by replaying identical stored blobs; dev-28 only per D-01a, the 216-blob universe per D-01, dedicated standalone script per D-02) and the measurement half of VALID-02 (per-motif removed/survived + depth-shift + dropped-case surface with lichess deep-links per D-04). The hand-check and final-margin commit are Plan 02.
Output: `scripts/ab_validate_gate.py`, `tests/scripts/test_ab_validate_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-user-28-a-b-validation/144-CONTEXT.md
@.planning/phases/144-user-28-a-b-validation/144-RESEARCH.md
@scripts/retag_flaws.py
@app/services/flaws_service.py
@app/services/forcing_line_gate.py
@tests/scripts/test_retag_flaws.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing test scaffold for the A/B harness</name>
  <files>tests/scripts/test_ab_validate_gate.py</files>
  <read_first>
    - tests/scripts/test_retag_flaws.py (session-maker injection, `session_factory`/`test_engine` fixtures, `_FORCING_BLOB`/`_NON_FORCING_BLOB`, committed-fixture + finally-cleanup pattern, `_SEED_MOTIF_INT`)
    - scripts/retag_flaws.py lines 611-650 (`run_backfill` signature: `session_maker` + `report_dir` injection contract to mirror)
    - app/services/flaws_service.py lines 406-560 (`_detect_tactic_for_flaw`, `_solver_color_for`, `_classify_tactic_gated` signatures)
    - pyproject.toml (asyncio_mode="auto"; no module-level pytestmark needed)
  </read_first>
  <action>
    Create tests/scripts/test_ab_validate_gate.py with the six Nyquist tests named in 144-RESEARCH.md §Validation Architecture. Borrow the committed-fixture + finally-cleanup pattern from test_retag_flaws.py verbatim (a unique `_TEST_USER_ID` distinct from 143_030, one Game, two GamePosition rows at ply and ply+1 carrying `eval_cp`/`pv`/`move_san`/`eval_mate`, two GameFlaw rows with `allowed_pv_lines`/`missed_pv_lines` set to `_FORCING_BLOB`/`_NON_FORCING_BLOB`, all deleted in a finally block per the eval-lottery isolation memory). Inject the harness's `session_maker` and a tmp `report_dir` exactly as test_retag_flaws injects them into `run_backfill`.
    Tests to author (all importing from scripts.ab_validate_gate):
    (1) test_ungated_arm_bypasses_gate — monkeypatch `app.services.forcing_line_gate.apply_forcing_line_filter` to record calls (or assert via a spy); assert the ungated arm path produces a tag for a NON-forcing blob where the gated arm suppresses it, proving the gate was never consulted for the ungated arm.
    (2) test_both_arms_use_same_blobs — assert both arms are invoked with the identical blob object/list loaded once (no second load, no engine import); assert no EnginePool/Stockfish symbol is referenced (e.g. assert the module has no `EnginePool` attribute / no `chess.engine` import).
    (3) test_gated_lte_ungated — over the seeded fixture, assert per-orientation gated tag count <= ungated tag count.
    (4) test_report_output — run the harness against the injected session_maker + tmp report_dir; assert the report file is created and contains the required section headers (Executive Summary, Per-Motif, Depth-Shift, Dropped Cases, A/B Summary).
    (5) test_dropped_case_fields — assert each dropped case row exposes orientation, motif name, FEN, depth, PV line, and a lichess URL beginning `https://lichess.org/analysis?fen=`.
    (6) test_lichess_url_encodes_fen — unit-test the `lichess_analysis_url` helper: board_fen + ply parity -> `{placement} {w|b} - - 0 1`, spaces percent-encoded (`%20`, not `_`).
    Tests fail initially with ImportError (module not yet written) — that is the expected RED. Do NOT mark integration tests with a real `--db dev` dependency; the seeded session_maker test IS the integration coverage (CI runs the per-run cloned test DB, which has no user-28).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/scripts/test_ab_validate_gate.py --co -q` collects 6 tests (collection succeeds even while the module is absent only if written as import-at-call; otherwise the run errors with ImportError referencing scripts.ab_validate_gate — either is an acceptable RED state and must be noted in the SUMMARY).
    - The test file references `_detect_tactic_for_flaw`/`_classify_tactic_gated` only indirectly through the harness API, not by re-implementing detection.
    - No test uses watch mode; no test depends on a populated dev DB.
    - `grep -n 'lichess.org/analysis?fen=' tests/scripts/test_ab_validate_gate.py` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/scripts/test_ab_validate_gate.py --co -q 2>&1 | grep -Eq 'test_(ungated_arm_bypasses_gate|report_output|dropped_case_fields)' || uv run pytest tests/scripts/test_ab_validate_gate.py -x 2>&1 | grep -q 'ab_validate_gate'</automated>
  </verify>
  <done>Six tests authored; suite is RED for the right reason (harness not yet implemented), not for fixture/import-typo reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scripts/ab_validate_gate.py to green</name>
  <files>scripts/ab_validate_gate.py, tests/scripts/test_ab_validate_gate.py</files>
  <read_first>
    - scripts/retag_flaws.py lines 94-163 (imports, project-root bootstrap, `_PosRow`/`_EMPTY_POS`, FLAWS-style named constants), 256-433 (`_FlawWork`/`_make_works`/`_fetch_flaw_page`/`_load_positions_for_page` query shapes), 462-608 (`_accumulate_motif_counts` motif-decode + `_write_retag_report` header/table/footer convention), 611-799 (`run_backfill` session_maker/report_dir injection + `__main__`/argparse scaffolding)
    - app/services/flaws_service.py lines 406-560 (both arm kernels + `_solver_color_for`)
    - app/services/forcing_line_gate.py lines 49-87 (`ONLY_MOVE_WIN_PROB_MARGIN`, `PvNode` shape)
    - app/repositories/game_flaws_repository.py lines 154-163 (`TACTIC_TAG_COLUMNS`)
    - app/services/tactic_detector.py lines 88-107 (`TacticMotifInt` names)
  </read_first>
  <action>
    Write the standalone read-only harness. Reuse the project-root sys.path bootstrap and the full model-registration import chain (Game/User/OAuthAccount) from retag_flaws.py so `select()` compiles. Define a local minimal `_PosRow`/`_EMPTY_POS` (move_san/pv/eval_mate/eval_cp) rather than importing the private symbols from retag_flaws.
    Loading (single shot, no keyset paging — 216 rows): `_load_ab_flaws(session, user_id)` selects user_id/game_id/ply/fen + the 8 `TACTIC_TAG_COLUMNS` + `GameFlaw.allowed_pv_lines` + `GameFlaw.missed_pv_lines` (project deferred cols explicitly — asyncpg auto-deserializes JSONB to list[dict]) WHERE `user_id == user_id` AND `or_(allowed_pv_lines.isnot(None), missed_pv_lines.isnot(None))`, ordered by PK. `_load_positions(session, flaws)` mirrors `_load_positions_for_page` (project ply and ply+1, return `dict[(uid,gid,ply)] -> _PosRow`).
    Two arms per flaw, per orientation (allowed, missed): build the sparse `positions` list (length ply+2, `_EMPTY_POS` filler, real `_PosRow` at ply and ply+1) and `fen_map = {ply: flaw.fen}` exactly as `_worker_recompute` does. UNGATED arm = `_detect_tactic_for_flaw(ply, fen_map, positions, None, orientation=...)` — never call the gate. GATED arm = `_classify_tactic_gated(ply, fen_map, positions, orientation, pv_blob=<that orientation's blob>, pre_flaw_eval_cp=<cur.eval_cp>, margin=margin)`. pre_flaw_eval_cp is `cur.eval_cp` (None for mate-adjacent flaws — both arms then agree, gate skipped; assert 0 suppression in that case).
    Diff + aggregation: a flaw is a DROPPED case for an orientation when ungated motif is not None AND gated motif is None. Accumulate per-motif (decode via `TacticMotifInt(int).name`, fall back to `str(int)` on ValueError) into ungated/suppressed/survived `Counter`s, separately for allowed and missed. Build a per-arm depth distribution keyed by motif: bucket the 4th tuple element (tactic_depth) into 0/1/2/3+ and track mean; survived-case depth must match between arms (same firing node). Collect a `@dataclass(frozen=True) _AbCase` per dropped case: orientation, motif_name, fen, ply, depth, pv_line (positions[ply+1].pv for allowed, positions[ply].pv for missed), lichess_url.
    `lichess_analysis_url(board_fen, ply)`: side = "w" if ply even else "b"; full_fen = f"{board_fen} {side} - - 0 1"; return f"https://lichess.org/analysis?fen={urllib.parse.quote(full_fen, safe='')}". Tag the URL format [ASSUMED] in a comment (LOW confidence per research A1) — Plan 02 manually verifies one link.
    Report: `_write_ab_report(...)` following the `_write_retag_report` header/pipe-table/footer convention with the richer section set from 144-RESEARCH.md §Pattern 7 — Executive Summary table, Per-Motif Allowed, Per-Motif Missed (columns: Motif | Ungated | Suppressed | Survived | Suppression % | small-N flag when N<10), Depth-Shift Distribution (Motif | Arm | Depth 0..3+ | Mean), Dropped Cases table + a Full-PV detail block per case, and placeholder sections "False Negative Count (HUMAN-UAT — fill in after hand-check)" and "A/B Summary & Margin Justification". Default filename `reports/retag/ab-validation-YYYY-MM-DD.md` via `datetime.now(timezone.utc)`; `report_dir` injectable (default committed reports/retag/), same as retag.
    Orchestration: `run_ab_validation(*, db, user_id=28, margin=ONLY_MOVE_WIN_PROB_MARGIN, neighbourhood=False, session_maker=None, report_dir=None)` — when session_maker is None build it from `db_url_for_target(db)` + `create_async_engine` + `async_sessionmaker` (mirror retag). Read-only: NO commit, NO bulk update, NO EnginePool. Optional `neighbourhood=True` also reports the 0.30/0.40 columns for context (D-03). argparse `__main__` with `--db` (default dev), `--user-id` (default 28), `--margin` (default ONLY_MOVE_WIN_PROB_MARGIN), `--neighbourhood` (store_true); `asyncio.run(run_ab_validation(...))`. Keep functions within nesting soft-3/logic-LOC soft-100 (split the report formatter into per-section helpers like retag's nested `_motif_table`). Sentry: wrap only genuine unexpected errors with `sentry_sdk.capture_exception`; skip expected ValueError from motif decode. No `asyncio.gather` on the session (sequential).
    A module docstring must state: read-only, engine-free, dev-28 only, AGPL boundary (heuristics/constants/names only — copy NO lichess-puzzler source).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/scripts/test_ab_validate_gate.py -x` is GREEN (all 6 tests pass).
    - `grep -nE 'EnginePool|chess\.engine|\.commit\(|bulk_update' scripts/ab_validate_gate.py` returns no matches (read-only, engine-free).
    - `grep -n '_detect_tactic_for_flaw' scripts/ab_validate_gate.py` and `grep -n '_classify_tactic_gated' scripts/ab_validate_gate.py` each return a match (both arms wired through the project kernels).
    - `uv run ruff check scripts/ab_validate_gate.py tests/scripts/test_ab_validate_gate.py` and `uv run ruff format --check scripts/ab_validate_gate.py tests/scripts/test_ab_validate_gate.py` pass.
    - `uv run ty check app/ tests/` passes with zero errors (note: scripts/ is not in the ty target set per CLAUDE.md; ensure no app/ or tests/ regressions).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/scripts/test_ab_validate_gate.py -x && uv run ruff check scripts/ab_validate_gate.py tests/scripts/test_ab_validate_gate.py</automated>
  </verify>
  <done>The harness loads blobs once, runs both arms over identical inputs, emits per-motif removed/survived + depth distribution + dropped-case list, writes the committed-markdown report, performs zero writes and no engine call; full quick suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → dev DB | The only input is local CLI flags (`--db dev --user-id 28 --margin`); the script SELECTs from the dev DB and writes a committed markdown file. No network endpoint, no auth surface, no untrusted user input, no DB writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-01 | Tampering | AGPL boundary (lichess-puzzler source) | medium | mitigate | Gate logic is already implemented in `forcing_line_gate.py`; the harness only calls existing project code. Do NOT read or copy lichess-puzzler source. Heuristics/constants/names are facts and fine; source is not. Module docstring records the boundary. |
| T-144-02 | Information Disclosure | report content | low | accept | Report contains only user-28 dev FENs/PVs (the developer's own data) + lichess links; committed to the repo intentionally as the deliverable. No secrets, no PII. |
| T-144-03 | Tampering | accidental DB write | low | mitigate | Harness is read-only by construction: no `commit()`, no UPDATE, no bulk_update. Acceptance grep gate enforces absence of write calls. |
| T-144-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — all deps are project-internal (SQLAlchemy/asyncpg/python-chess) or stdlib (argparse/urllib/collections/pathlib). RESEARCH §Package Legitimacy Audit confirms zero new packages. |
</threat_model>

<verification>
- `uv run pytest tests/scripts/test_ab_validate_gate.py -x` green.
- `uv run pytest -n auto -x` (wave merge) green — no regressions.
- Read-only/engine-free greps return no matches (T-144-01/03).
- `uv run ruff check .` + `uv run ruff format --check` clean.
</verification>

<success_criteria>
- A standalone `scripts/ab_validate_gate.py` exists that replays the 216 stored blobs through both arms with no engine and no writes (VALID-01).
- It emits per-motif ungated/suppressed/survived counts, a per-arm depth distribution, and a dropped-case list with lichess links — the measurement machinery for VALID-02.
- Six Nyquist tests pass; full suite green.
</success_criteria>

<output>
Create `.planning/phases/144-user-28-a-b-validation/144-01-SUMMARY.md` when done.
</output>
