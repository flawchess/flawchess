---
phase: 26-position-classifier-schema
plan: "02"
subsystem: backend
tags: [database, schema, alembic, sqlalchemy]
dependency_graph:
  requires: []
  provides: [game_positions-position-metadata-columns, bulk_insert_positions-15col-chunk_size]
  affects: [Phase 27 import wiring, Phase 28 endgame analytics]
tech_stack:
  added: []
  patterns: [alembic-autogenerate, sqlalchemy-mapped-column-nullable]
key_files:
  created:
    - alembic/versions/20260323_213217_38239eef59a8_add_position_metadata_columns.py
  modified:
    - app/models/game_position.py
    - app/repositories/game_repository.py
decisions:
  - "String(40) for material_signature — starting position signature is 33 chars (KQRRBBNNPPPPPPPP_KQRRBBNNPPPPPPPP), exceeds original estimate of 20"
  - "chunk_size reduced 4000 -> 2100 to stay within asyncpg 32767 arg limit with 15 columns per row"
metrics:
  duration: "~8 minutes"
  completed: "2026-03-23"
  tasks: 2
  files: 3
---

# Phase 26 Plan 02: Schema — Add Position Metadata Columns to game_positions Summary

## One-liner

Added 7 nullable columns to game_positions (game_phase, material_signature, material_imbalance, endgame_class, 3 bishop-pair indicators) via autogenerated Alembic migration, and reduced bulk_insert_positions chunk_size from 4000 to 2100 for 15-column rows.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add 7 nullable columns to GamePosition model | 0a123a7 | app/models/game_position.py |
| 2 | Generate Alembic migration and update chunk_size | d8b773b | alembic/versions/20260323_213217_38239eef59a8_add_position_metadata_columns.py, app/repositories/game_repository.py |

## What Was Built

**Task 1 — GamePosition model changes:**
- Added `Boolean` and `Integer` to the `from sqlalchemy import` line
- Added 4 position metadata columns (after `clock_seconds`):
  - `game_phase: Mapped[Optional[str]] = mapped_column(String(12), nullable=True)`
  - `material_signature: Mapped[Optional[str]] = mapped_column(String(40), nullable=True)` — String(40) to accommodate full opening signatures (33 chars)
  - `material_imbalance: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)`
  - `endgame_class: Mapped[Optional[str]] = mapped_column(String(12), nullable=True)`
- Added 3 tactical indicator columns:
  - `has_bishop_pair_white: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)`
  - `has_bishop_pair_black: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)`
  - `has_opposite_color_bishops: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)`

**Task 2 — Migration and chunk_size:**
- Alembic autogenerated migration `38239eef59a8_add_position_metadata_columns` with 7 `op.add_column` calls and 7 `op.drop_column` calls in downgrade
- Migration applied cleanly against dev database
- Updated `bulk_insert_positions` chunk_size from 4000 to 2100 with updated comment explaining 15 columns (32767/15=2184, use 2100 for margin)
- Updated docstring to enumerate all 15 position row keys including the 7 new optional ones
- All 314 existing tests pass

## Verification

- `python -c "from app.models.game_position import GamePosition"` exits 0
- `uv run alembic upgrade head` exits 0 — migration applies cleanly
- `uv run pytest -x` — 314 passed, 0 failures

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all 7 columns are nullable by design. They will be populated in Phase 27 when position_classifier.py is wired into the import pipeline.

## Self-Check: PASSED

- app/models/game_position.py exists and contains all 7 new columns
- app/repositories/game_repository.py contains `chunk_size = 2100`
- alembic/versions/20260323_213217_38239eef59a8_add_position_metadata_columns.py exists
- Commits 0a123a7 and d8b773b verified in git log
