---
phase: 171-bots-page-setup-screen-nav
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/botTimeControlPresets.ts
  - frontend/src/lib/playStyle.ts
  - frontend/src/lib/botSetupSettings.ts
  - frontend/src/lib/__tests__/botTimeControlPresets.test.ts
  - frontend/src/lib/__tests__/playStyle.test.ts
  - frontend/src/lib/__tests__/botSetupSettings.test.ts
  - frontend/src/components/bots/PlayStyleControl.tsx
  - frontend/src/components/bots/__tests__/PlayStyleControl.test.tsx
autonomous: true
requirements: [PLAY-02]
must_haves:
  truths:
    - "Each of the 9 lichess TC presets converts to a concrete {baseSeconds, incrementSeconds} pair — the display label never reaches BotGameSettings or the store request."
    - "The play-style control offers exactly two presets (Human -> blend 0, Engine -> blend 1) above a single-thumb slider spanning 0.05 to 1.00 in 0.05 steps."
    - "blend = 0 is UNREACHABLE by dragging the slider. It is reachable only via the Human preset button."
    - "Last-used setup settings (ELO, blend, baseSeconds, incrementSeconds, color preference) round-trip through localStorage under an owner-scoped key that is distinct from both botGameSnapshot's and botPendingStore's."
    - "A corrupt or wrong-shaped settings blob degrades to null (prefill falls back to defaults) and is cleared, never thrown."
  artifacts:
    - "frontend/src/lib/botTimeControlPresets.ts — `TimeControlPreset`, `TIME_CONTROL_PRESETS` (9 entries), `DEFAULT_TC_PRESET_LABEL = '10+0'`, `findPresetByLabel()`"
    - "frontend/src/lib/playStyle.ts — `PLAY_STYLE_MIN = 0.05`, `PLAY_STYLE_MAX = 1`, `PLAY_STYLE_STEP = 0.05`, `PLAY_STYLE_DEFAULT_BLEND = 0.5`, `HUMAN_BLEND = 0`, `ENGINE_BLEND = 1`, `deriveActivePlayStylePreset()`, `formatPlayStyleSummary()`"
    - "frontend/src/lib/botSetupSettings.ts — `BOT_SETUP_SETTINGS_KEY_PREFIX = 'flawchess_bot_setup_settings:'`, `BotSetupSettings` interface, `readSetupSettings()`, `writeSetupSettings()`, `BOT_SETUP_DEFAULT_ELO = 1500`, `resolveDefaultBotElo()`, `DEFAULT_BOT_SETUP_SETTINGS`"
    - "frontend/src/components/bots/PlayStyleControl.tsx — the single-thumb preset+slider control"
    - "4 new test files (3 lib, 1 component)"
  key_links:
    - "TIME_CONTROL_PRESETS entry -> {baseSeconds, incrementSeconds} -> BotGameSettings -> toBackendTcStr() at store time. The label is display-only and must never be threaded through as a TC string."
    - "resolveDefaultBotElo(profile.lichess_blitz_equivalent_rating) -> MAIA_ELO_LADDER-clamped default -> EloSelector's `value`/`defaultElo` in Plan 05."
    - "BOT_SETUP_SETTINGS_KEY_PREFIX must not equal BOT_GAME_SNAPSHOT_KEY_PREFIX ('flawchess_bot_game:') or BOT_PENDING_STORE_KEY_PREFIX ('flawchess_bot_pending_store:')."
  prohibitions:
    - "Does NOT widen PresetRangeFilter. Its `slider.value` is hard-typed `[number, number]` with a required `minStepsBetweenThumbs` — a two-thumb-only contract with other consumers. PlayStyleControl is a SIBLING component, not a prop variant."
    - "Does NOT put 0 on the slider domain. blend 0 -> blend 0.05 is a ~+950 ELO regime cliff (a different code path in selectBotMove, not a slider step); advertising it as a continuum is the specific dishonesty D-01 forbids. Slider min is 0.05."
    - "Does NOT restore the three-discrete-mode (Human/Balanced/Sharp) picker. That design was proposed and explicitly REVERSED by the user."
    - "Does NOT reuse botGameSnapshot.ts's key or its `isValidSnapshotShape` validator — a settings-shaped object fails that validator's pgn/whiteClockMs checks and would be Sentry-flagged as corrupt on the next /bots visit."
    - "Does NOT change parse_time_control or any backend TC bucketing. The 30+0 -> `rapid` bucket mismatch is an accepted, documented quirk (RESEARCH.md Pitfall 4) — comment it, never fix it."
    - "Does NOT map displayed ELO to a corrected Maia rung using the 2026-07-12 harness table (D-06). Every cell in that table is a clamped bound."
---

<objective>
Build the three pure/primitive modules the setup screen composes, plus the one genuinely new control.

D-14's nine lichess time-control presets need a label→seconds table (the display label must never reach the
wire — `types/bots.ts` carries an in-repo warning about exactly that bug). D-01's play-style control is a
single-thumb sibling of `PresetRangeFilter`, whose slider deliberately cannot reach 0. D-10 needs a third,
separate owner-scoped localStorage key for last-used settings.

Purpose: keep the setup screen (Plan 05) thin and declarative by pushing every derivation into unit-testable
pure functions.
Output: 3 lib modules, 1 component, 4 test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-bots-page-setup-screen-nav/171-CONTEXT.md
@.planning/phases/171-bots-page-setup-screen-nav/171-RESEARCH.md
@.planning/phases/171-bots-page-setup-screen-nav/171-PATTERNS.md
@.planning/phases/171-bots-page-setup-screen-nav/171-UI-SPEC.md
</context>

## Artifacts this plan produces

| Symbol | Kind | File | Status |
|---|---|---|---|
| `TimeControlPreset` | interface | `frontend/src/lib/botTimeControlPresets.ts` | NEW |
| `TIME_CONTROL_PRESETS` | readonly const (9 entries) | same | NEW |
| `DEFAULT_TC_PRESET_LABEL` (`'10+0'`) | const | same | NEW |
| `findPresetByLabel(label)` | pure fn | same | NEW |
| `HUMAN_BLEND` (0), `ENGINE_BLEND` (1) | consts | `frontend/src/lib/playStyle.ts` | NEW |
| `PLAY_STYLE_MIN` (0.05), `PLAY_STYLE_MAX` (1), `PLAY_STYLE_STEP` (0.05), `PLAY_STYLE_DEFAULT_BLEND` (0.5) | consts | same | NEW |
| `deriveActivePlayStylePreset(blend)` → `'human' \| 'engine' \| null` | pure fn | same | NEW |
| `formatPlayStyleSummary(blend)` | pure fn | same | NEW |
| `BOT_SETUP_SETTINGS_KEY_PREFIX` (`'flawchess_bot_setup_settings:'`) | const | `frontend/src/lib/botSetupSettings.ts` | NEW |
| `BotSetupSettings` | interface | same | NEW |
| `DEFAULT_BOT_SETUP_SETTINGS` | const | same | NEW |
| `BOT_SETUP_DEFAULT_ELO` (1500) | const | same | NEW |
| `resolveDefaultBotElo(normalizedRating)` | pure fn | same | NEW |
| `readSetupSettings(ownerKey)` / `writeSetupSettings(ownerKey, settings)` | fns | same | NEW |
| `PlayStyleControl` | React component | `frontend/src/components/bots/PlayStyleControl.tsx` | NEW |

New `data-testid` values (PlayStyleControl): `setup-play-style` (root), `setup-play-style-info`,
`setup-play-style-summary`, `setup-play-style-presets`, `setup-play-style-preset-human`,
`setup-play-style-preset-engine`, `setup-play-style-slider`.

New localStorage key: `flawchess_bot_setup_settings:{ownerKey ?? 'anon'}`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: botTimeControlPresets.ts + playStyle.ts (pure tables and derivations)</name>

  <read_first>
    - `frontend/src/types/bots.ts` lines 20-40 — the in-repo doc comment warning that the display label ("10+5") must NOT be sent as the TC string; conversion to base-SECONDS happens first
    - `frontend/src/lib/botGamePgn.ts` ~:112-114 — `toBackendTcStr(baseSeconds, incrementSeconds)`, the EXISTING seconds→wire-string function. Do not duplicate its logic in the preset table.
    - `app/services/normalization.py` lines 58-101 — `parse_time_control`'s frozen bucket rule (`estimated = base + inc * 40`; `estimated <= 1800 -> rapid`). This is why 30+0 buckets as `rapid`, not `classical`.
    - `frontend/src/lib/opponentStrength.ts` — the presets↔value↔summary derivation-table pattern to mirror (pure functions colocated with the constants, imported by both the control and its tests)
    - `frontend/src/lib/engine/selectBotMove.ts` lines 110-147 — the three-way regime dispatch that justifies the 0.05 slider floor (blend<=0 = no search; >0 = full MCTS at tau = 0.1 * (1 - blend))
    - `.planning/phases/171-bots-page-setup-screen-nav/171-PATTERNS.md` § `frontend/src/lib/botTimeControlPresets.ts` (contains the exact 9-row table to use)
    - `.planning/phases/171-bots-page-setup-screen-nav/171-UI-SPEC.md` § Copywriting Contract (the play-style summary strings)
  </read_first>

  <files>frontend/src/lib/botTimeControlPresets.ts, frontend/src/lib/playStyle.ts, frontend/src/lib/__tests__/botTimeControlPresets.test.ts, frontend/src/lib/__tests__/playStyle.test.ts</files>

  <behavior>
    botTimeControlPresets:
    - `TIME_CONTROL_PRESETS` has exactly 9 entries in D-14's order: 3+0, 3+2, 5+0, 5+3 (blitz) · 10+0, 10+5, 15+10 (rapid) · 30+0, 30+20 (classical). No bullet entry exists.
    - Every entry's `baseSeconds` equals `minutes * 60` and `incrementSeconds` equals the label's increment: e.g. 15+10 → `{ baseSeconds: 900, incrementSeconds: 10 }`.
    - `DEFAULT_TC_PRESET_LABEL === '10+0'` and `findPresetByLabel(DEFAULT_TC_PRESET_LABEL)` returns `{ baseSeconds: 600, incrementSeconds: 0 }`.
    - `findPresetByLabel('bogus')` returns `undefined` (callers fall back to the default).

    playStyle:
    - `deriveActivePlayStylePreset(0) === 'human'`; `deriveActivePlayStylePreset(1) === 'engine'`; `deriveActivePlayStylePreset(0.5) === null`; `deriveActivePlayStylePreset(0.05) === null`.
    - `formatPlayStyleSummary(0)` returns the Human summary line (no numeric blend value).
    - `formatPlayStyleSummary(0.5)` returns a numeric-first summary containing `0.50` and `50%`.
    - `PLAY_STYLE_MIN === 0.05` (strictly greater than `HUMAN_BLEND`), `PLAY_STYLE_STEP === 0.05`, `PLAY_STYLE_MAX === 1`, `PLAY_STYLE_DEFAULT_BLEND === 0.5`.
  </behavior>

  <action>
    Create `frontend/src/lib/botTimeControlPresets.ts` with the `TimeControlPreset` interface
    (`label: string; baseSeconds: number; incrementSeconds: number; bucket: 'blitz' | 'rapid' | 'classical'`),
    the 9-row `TIME_CONTROL_PRESETS` readonly table exactly as given in PATTERNS.md, `DEFAULT_TC_PRESET_LABEL = '10+0'`,
    and `findPresetByLabel(label: string): TimeControlPreset | undefined`.

    Put a comment at the `30+0` row (and in the `bucket` field's doc) recording the accepted quirk: `bucket` here is a
    UI display-grouping label ONLY. The backend's frozen `parse_time_control` computes `estimated = 1800 + 0*40 = 1800`,
    and its boundary rule sends `estimated <= 1800` to the rapid bucket — so a stored 30+0 bot game carries
    `time_control_bucket = 'rapid'` and its player-rating anchor is looked up under `rapid`. That is expected and
    documented (RESEARCH.md Pitfall 4); do not "fix" it here or in `normalization.py`.

    Create `frontend/src/lib/playStyle.ts` with the constants and the two pure functions listed in the artifacts
    table. Give the module a header comment stating why the slider floor is 0.05 and not 0: `selectBotMove` is a
    three-way REGIME DISPATCH, not a mix — `blend <= 0` runs one Maia policy call with no search at all, anything
    `> 0` runs full MCTS. The 0 → 0.05 step is a ~+950 ELO cliff at rung 1500 (harness: ~980 → ~1938), while the
    remaining 95% of the axis is a gentle ramp. Putting 0 on the slider would advertise a continuum that does not
    exist; it belongs on a button. Cross-reference D-01 and SEED-100.

    Copy the summary strings verbatim from UI-SPEC.md's Copywriting Contract (Human summary; and the
    `"{value} — blends style with {N}% search"` form where N = round(blend * 100)).

    Write both test files covering every `<behavior>` bullet. The `PLAY_STYLE_MIN > HUMAN_BLEND` assertion is the
    one that pins D-01's "slider cannot reach 0" invariant at the constant level.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/botTimeControlPresets.test.ts src/lib/__tests__/playStyle.test.ts && npx tsc -b</automated>
  </verify>

  <acceptance_criteria>
    - `TIME_CONTROL_PRESETS.length === 9` is asserted, and a test asserts NO entry has `baseSeconds < 180` (no bullet).
    - A test asserts `PLAY_STYLE_MIN > HUMAN_BLEND` (0.05 > 0) — the constant-level pin of "the slider cannot reach 0".
    - `grep -c 'rapid' frontend/src/lib/botTimeControlPresets.ts` >= 4 (3 rapid rows + the 30+0 quirk comment naming the rapid bucket).
    - `git diff --exit-code -- app/services/normalization.py` exits 0 (backend TC bucketing untouched).
    - `npx vitest run src/lib/__tests__/botTimeControlPresets.test.ts src/lib/__tests__/playStyle.test.ts` passes; `npx tsc -b` exits 0.
  </acceptance_criteria>

  <done>Both pure modules exist with full unit coverage; the 30+0 bucket quirk is documented in code; normalization.py untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: botSetupSettings.ts — the third owner-scoped localStorage key (D-10, V-10)</name>

  <read_first>
    - `frontend/src/lib/botGameSnapshot.ts` (WHOLE FILE — this is the exact shape to mirror: `BOT_GAME_SNAPSHOT_KEY_PREFIX = 'flawchess_bot_game:'`, `snapshotKey(ownerKey)` = `${PREFIX}${ownerKey ?? 'anon'}`, the SSR `typeof localStorage === 'undefined'` guard, the try/catch read, `JSON.parse` failure → `clearCorruptSnapshot` + Sentry-once, the shape validator, and the silent-no-op write)
    - `frontend/src/lib/botPendingStore.ts` (WHOLE FILE — the SIBLING precedent: same guards, DIFFERENT key `flawchess_bot_pending_store:`, different container type. `isValidSettingsShape` there is the validator style to imitate.)
    - `frontend/src/lib/__tests__/botGameSnapshot.test.ts` — the test shape to mirror (localStorage stubbing, corruption cases)
    - `frontend/src/lib/maiaEncoding.ts` — `MAIA_ELO_LADDER` (600–2600, step 100) and its extrapolation-caveat comments
    - `frontend/src/hooks/useMaiaEloDefault.ts` :29 (`FREE_PLAY_DEFAULT_ELO = 1500`) and :78 (`clampToLadderBounds`, module-PRIVATE — do not import it; `resolveDefaultBotElo` reimplements the same clamp+snap rule locally and says so in a comment)
    - `frontend/src/hooks/useBotGame.ts` :155-170 — `BotGameSettings`, the shape `BotSetupSettings` must be convertible into
    - `.planning/phases/171-bots-page-setup-screen-nav/171-PATTERNS.md` § `frontend/src/lib/botSetupSettings.ts`
  </read_first>

  <files>frontend/src/lib/botSetupSettings.ts, frontend/src/lib/__tests__/botSetupSettings.test.ts</files>

  <behavior>
    - `writeSetupSettings('a@b.com', s)` then `readSetupSettings('a@b.com')` returns `s` (round-trip).
    - `readSetupSettings('other@b.com')` returns `null` — settings are owner-scoped; one user's config never prefills another's.
    - `readSetupSettings(null)` uses the `anon` suffix (guest scope), matching `botGameSnapshot`'s convention.
    - The written key is exactly `flawchess_bot_setup_settings:{owner}` — and is NEITHER `flawchess_bot_game:{owner}` NOR `flawchess_bot_pending_store:{owner}`. A test asserts all three prefixes are pairwise distinct by importing all three constants.
    - Writing settings does NOT overwrite or clear an existing snapshot or pending-store entry for the same owner (assert both survive a `writeSetupSettings` call).
    - A garbage blob (`'not json'`) → `readSetupSettings` returns `null`, the key is removed, and Sentry is captured once.
    - A well-formed-JSON but wrong-shaped blob (e.g. `{"pgn":"..."}`) → `null` + cleared, no throw.
    - `resolveDefaultBotElo(1650)` snaps into the ladder (1700 or 1600 per the chosen rounding — pin whichever you implement); `resolveDefaultBotElo(null)` === 1500; `resolveDefaultBotElo(120)` === the ladder minimum (600); `resolveDefaultBotElo(3400)` === the ladder maximum (2600).
    - `DEFAULT_BOT_SETUP_SETTINGS` has `blend === PLAY_STYLE_DEFAULT_BLEND` (0.5), the 10+0 preset's seconds, and `colorPreference === 'random'` or `'white'` (pick one; pin it in the test).
  </behavior>

  <action>
    Create `frontend/src/lib/botSetupSettings.ts` mirroring `botGameSnapshot.ts`'s module shape VERBATIM (SSR guard →
    try/catch → JSON.parse → shape validator → Sentry-once-on-corruption → silent-no-op write), with a **distinct** key
    prefix `BOT_SETUP_SETTINGS_KEY_PREFIX = 'flawchess_bot_setup_settings:'`.

    `BotSetupSettings` holds exactly what the setup screen needs to prefill:
    `{ botElo: number; blend: number; baseSeconds: number; incrementSeconds: number; colorPreference: 'white' | 'black' | 'random' }`.
    Note that `colorPreference` deliberately includes `'random'` — this is a SETUP-screen preference, not a
    `BotGameSettings.userColor`. D-12 requires Random to be resolved to a concrete color at Start, BEFORE `useBotGame`
    mounts, so the snapshot and the exported PGN carry the actual color played and never "random". State that in the
    field's doc comment.

    Add a header comment marking this as the THIRD, separate key (170 D-05/D-12 keep the in-progress snapshot and the
    pending-store queue on their own keys) and stating why reuse is forbidden: `botGameSnapshot`'s
    `isValidSnapshotShape` requires `pgn`/`whiteClockMs`, so a settings-shaped object under that key would be treated
    as corrupt and Sentry-flagged on the next `/bots` visit.

    Add `BOT_SETUP_DEFAULT_ELO = 1500` and `resolveDefaultBotElo(normalizedRating: number | null | undefined): number`
    — clamps to `MAIA_ELO_LADDER`'s bounds and snaps to the nearest rung, falling back to `BOT_SETUP_DEFAULT_ELO` when
    the input is null/undefined/non-finite. Its doc comment carries the BOT-03 note: this rating is a **UI DEFAULT
    ONLY** — user-visible, user-overridable, fixed for the game; it is never fed into the bot's move selection, so
    BOT-03's "the bot never adapts to player strength" invariant still holds. A future reviewer must not read this as
    an adaptivity violation. It also carries D-06's prohibition: no per-mode ELO correction, no mapping of displayed
    ELO to a Maia rung via the 2026-07-12 harness table (every cell there is a clamped bound; that is SEED-104's job).

    Export `DEFAULT_BOT_SETUP_SETTINGS` built from `PLAY_STYLE_DEFAULT_BLEND` and the `DEFAULT_TC_PRESET_LABEL` preset.

    Write `botSetupSettings.test.ts` covering every `<behavior>` bullet, mirroring `botGameSnapshot.test.ts`'s
    localStorage-stubbing style.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/botSetupSettings.test.ts && npx tsc -b && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - The test file imports all three prefixes (`BOT_SETUP_SETTINGS_KEY_PREFIX`, `BOT_GAME_SNAPSHOT_KEY_PREFIX`, `BOT_PENDING_STORE_KEY_PREFIX`) and asserts they are pairwise distinct — a hard-coded string comparison in the test is NOT sufficient; it must compare the imported constants.
    - A test asserts that after `writeSetupSettings(owner, s)`, a pre-existing `botGameSnapshot` value AND a pre-existing `botPendingStore` value for the same owner both still read back intact.
    - Corruption cases assert BOTH `readSetupSettings(...) === null` AND that the key was removed from localStorage.
    - `grep -c 'BOT-03' frontend/src/lib/botSetupSettings.ts` >= 1 (the UI-DEFAULT-ONLY comment on `resolveDefaultBotElo`).
    - `grep -ci 'harness' frontend/src/lib/botSetupSettings.ts` >= 1 (the D-06 no-ELO-correction prohibition is recorded in code).
    - `npx vitest run src/lib/__tests__/botSetupSettings.test.ts` passes; `npx tsc -b` and `npm run lint` clean.
    - Covers V-10 and the setup half of V-13's default-ELO rule.
  </acceptance_criteria>

  <done>A third, distinct, owner-scoped settings key round-trips with corruption recovery; resolveDefaultBotElo pins the D-07 default rule with its BOT-03 and D-06 comments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlayStyleControl.tsx — single-thumb preset+slider sibling of PresetRangeFilter (D-01, V-09)</name>

  <read_first>
    - `frontend/src/components/filters/PresetRangeFilter.tsx` (WHOLE FILE — the shell to copy: label + `InfoPopover` + summary row at :69-88, the preset chip grid at :91-112 with its `h-11 sm:h-7` responsive touch-target sizing and active/inactive class pairs, and the `activePreset: string | null` contract. NOTE its `slider.value` is hard-typed `[number, number]` with a required `minStepsBetweenThumbs` — this is why you are writing a sibling, not widening it.)
    - `frontend/src/components/filters/OpponentStrengthFilter.tsx` (WHOLE FILE — the worked consumer: presets ↔ slider value ↔ derived active preset ↔ summary)
    - `frontend/src/components/ui/slider.tsx` (single-thumb support: thumb count derives from the `value` array length; the 44px thumb hit target and `data-[disabled]:opacity-50` convention)
    - `frontend/src/components/analysis/EloSelector.tsx` :95-110 — a live single-thumb `Slider` usage (`value={[v]}`, `thumbLabels`)
    - `frontend/src/lib/playStyle.ts` (from Task 1 — the constants and derivations this component consumes)
    - `.planning/phases/171-bots-page-setup-screen-nav/171-UI-SPEC.md` § "Play-style control (D-01)" and § Copywriting Contract (exact label, popover, preset, and summary copy)
    - `.planning/phases/171-bots-page-setup-screen-nav/171-PATTERNS.md` § `frontend/src/components/bots/PlayStyleControl.tsx`
    - `CLAUDE.md` § Frontend — `data-testid` on every interactive element; no `text-xs` outside popover bodies; theme tokens only, never hand-rolled colors.
  </read_first>

  <files>frontend/src/components/bots/PlayStyleControl.tsx, frontend/src/components/bots/__tests__/PlayStyleControl.test.tsx</files>

  <behavior>
    - Renders a "Play style" label with an `InfoPopover` (testid `setup-play-style-info`) carrying UI-SPEC's play-style popover copy.
    - Renders exactly two preset buttons in a `grid-cols-2`: `setup-play-style-preset-human` and `setup-play-style-preset-engine`.
    - Clicking Human calls `onChange(0)`. Clicking Engine calls `onChange(1)`.
    - With `blend === 0`: the Human button has `aria-pressed="true"`, the Engine button `aria-pressed="false"`, the summary shows the Human line (no numeric value), and the slider block carries the dimmed (`opacity-50`) treatment with its thumb parked at the slider minimum (0.05) — 0 is not a representable slider position.
    - With `blend === 1`: Engine is `aria-pressed="true"`.
    - With `blend === 0.5`: NEITHER preset is pressed, and the summary is the numeric form containing `0.50` and `50%`.
    - The slider (`setup-play-style-slider`) exposes `min=0.05`, `max=1`, `step=0.05`. Assert the rendered Radix slider's `aria-valuemin` is `0.05` — i.e. dragging can never produce 0. This is V-09's load-bearing assertion.
    - Moving the slider while Human is active exits Human mode immediately: `onValueChange` calls `onChange` with the dragged position (no separate "unlock" step).
  </behavior>

  <action>
    Create `frontend/src/components/bots/PlayStyleControl.tsx` with props
    `{ blend: number; onChange: (blend: number) => void }`. Copy `PresetRangeFilter`'s visual shell structure
    (label + `InfoPopover` + right-aligned summary row; preset chip grid; slider) but wire a single-thumb
    `Slider` with `value={[blend === HUMAN_BLEND ? PLAY_STYLE_MIN : blend]}`, `min={PLAY_STYLE_MIN}`,
    `max={PLAY_STYLE_MAX}`, `step={PLAY_STYLE_STEP}`, `thumbLabels={['Play style']}`, and the `opacity-50`
    dim applied when `blend === HUMAN_BLEND`.

    Derive the active preset via `deriveActivePlayStylePreset(blend)` and the summary via
    `formatPlayStyleSummary(blend)` — both imported from `lib/playStyle.ts`, never re-derived inline.
    Guard the `onValueChange` array index (`const next = values[0]; if (next !== undefined) onChange(next);`)
    — `noUncheckedIndexedAccess` is on.

    Use the exact `data-testid` values listed in this plan's artifacts table. Use `Button`-family/theme classes
    from the `PresetRangeFilter` chip pattern (`bg-toggle-active` / `bg-inactive-bg` + `border-toggle-active` /
    `border-border`); never hand-roll a `bg-*` hex. Keep every visible string at `text-sm` or larger — the only
    `text-xs` permitted is inside the `InfoPopover` body.

    Write `PlayStyleControl.test.tsx` covering every `<behavior>` bullet with RTL + `userEvent`.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/bots/__tests__/PlayStyleControl.test.tsx && npx tsc -b && npm run lint && npm run knip</automated>
  </verify>

  <acceptance_criteria>
    - A test asserts the rendered slider's `aria-valuemin` is `0.05` (NOT `0`) — dragging can never reach the Human regime. This assertion must fail if someone changes `PLAY_STYLE_MIN` to 0.
    - A test asserts clicking `setup-play-style-preset-human` calls `onChange` with exactly `0`.
    - `grep -c 'text-xs' frontend/src/components/bots/PlayStyleControl.tsx` — any hit must be inside the `InfoPopover` body only; verify by reading the surrounding JSX (CLAUDE.md's text-sm floor).
    - `grep -ciE 'bg-\[#|bg-\[rgb|#[0-9a-f]{6}' frontend/src/components/bots/PlayStyleControl.tsx` == 0 (no hand-rolled colors).
    - `grep -cE "^import .*from '@/components/filters/" frontend/src/components/bots/PlayStyleControl.tsx` == 0 — the control is a SIBLING, not a wrapper: it copies the shell's markup but imports nothing from the two-thumb range component. (A prose reference in a comment is fine and encouraged; what is forbidden is an import.) AND `git diff --exit-code -- frontend/src/components/filters/PresetRangeFilter.tsx` exits 0 (the shared range component is untouched).
    - Every interactive element (2 preset buttons, the slider, the popover trigger) has a `data-testid`.
    - `npx vitest run src/components/bots/__tests__/PlayStyleControl.test.tsx` passes; `npx tsc -b`, `npm run lint`, `npm run knip` clean.
    - Covers V-09.
  </acceptance_criteria>

  <done>A single-thumb play-style control whose slider provably cannot reach blend 0; PresetRangeFilter untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | A user-writable, corruptible blob crosses into the app on every setup-screen mount. Untrusted by construction. |
| setup UI → `BotGameSettings` → (later) `POST /bots/games` | Values chosen here eventually reach the wire. All are drawn from closed sets (9 TC presets, 3 colors, a clamped 0.05-step slider, a ladder-snapped ELO). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-04-01 | Tampering | A hand-edited / corrupt `flawchess_bot_setup_settings:*` blob prefilling the setup screen with out-of-domain values (NaN blend, negative clock) | medium | mitigate | `readSetupSettings`'s type-guard validator rejects any wrong-shaped object → `null` → `DEFAULT_BOT_SETUP_SETTINGS`. `selectBotMove` additionally clamps a non-finite `blend` to 1 (existing, unchanged). Server-side, `StoreBotGameRequest`'s Pydantic bounds (Phase 167) are the authoritative gate. |
| T-171-04-02 | Information Disclosure | Cross-account settings bleed (user A's config prefilling user B's setup screen on a shared browser) | medium | mitigate | Owner-scoped key (`${PREFIX}${ownerKey ?? 'anon'}`), asserted by a dedicated "other owner reads null" test. |
| T-171-04-03 | Denial of Service | Settings write clobbering the in-progress snapshot or the pending-store queue (a finished, unstored game lost forever) | high | mitigate | A physically separate third key prefix, asserted pairwise-distinct against the imported `BOT_GAME_SNAPSHOT_KEY_PREFIX` and `BOT_PENDING_STORE_KEY_PREFIX` constants, plus a test that both survive a settings write. |
| T-171-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — Radix `Slider`, `InfoPopover`, and `Button` all already exist in `components/ui/`. RESEARCH.md § Package Legitimacy Audit: not applicable this phase. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/botTimeControlPresets.test.ts src/lib/__tests__/playStyle.test.ts src/lib/__tests__/botSetupSettings.test.ts src/components/bots/__tests__/PlayStyleControl.test.tsx` — all green.
- `cd frontend && npx tsc -b && npm run lint && npm run knip` — clean.
- `git diff --exit-code -- frontend/src/components/filters/PresetRangeFilter.tsx app/services/normalization.py` — exits 0.
</verification>

<success_criteria>
The setup screen's four primitives exist, each unit-tested: a 9-preset TC table that converts to seconds, a
play-style control whose slider provably floors at 0.05, and a third owner-scoped settings key that cannot
collide with the snapshot or the pending-store queue. V-09 and V-10 green; V-13's setup half pinned.
</success_criteria>

<output>
Create `.planning/phases/171-bots-page-setup-screen-nav/171-04-SUMMARY.md` when done.
</output>
