---
phase: 137-useanalysisboard-hook-analysis-display-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/components/analysis/EvalBar.tsx
  - frontend/src/components/analysis/EngineLines.tsx
  - frontend/src/components/analysis/__tests__/EvalBar.test.tsx
  - frontend/src/components/analysis/__tests__/EngineLines.test.tsx
autonomous: true
requirements: [BOARD-02, BOARD-03]
must_haves:
  truths:
    - "EvalBar renders a white-POV vertical fill whose white fraction follows the sigmoid of evalCp (white always on top, never flips — D-04)"
    - "EvalBar shows the mate label only when evalMate !== null AND depth >= 8 (D-04)"
    - "EngineLines renders up to 2 PV lines, each with its own score, a single depth badge, and clickable move chips"
    - "Clicking a PV move chip calls onMoveClick(from, to) with from/to derived from the UCI string (D-03)"
    - "EngineLines shows the Analyzing indicator only when isAnalyzing && pvLines.length === 0"
  artifacts:
    - path: "frontend/src/lib/theme.ts"
      provides: "EVAL_BAR_WHITE / EVAL_BAR_BLACK semantic re-exports"
      contains: "EVAL_BAR_WHITE"
    - path: "frontend/src/components/analysis/EvalBar.tsx"
      provides: "White-POV sigmoid centipawn bar with depth-gated mate label"
      exports: ["EvalBar"]
    - path: "frontend/src/components/analysis/EngineLines.tsx"
      provides: "Top 1-2 PV lines with per-line score, depth badge, analyzing indicator, clickable chips"
      exports: ["EngineLines"]
  key_links:
    - from: "frontend/src/components/analysis/EvalBar.tsx"
      to: "frontend/src/lib/theme.ts"
      via: "import { EVAL_BAR_WHITE, EVAL_BAR_BLACK }"
      pattern: "EVAL_BAR_WHITE"
    - from: "frontend/src/components/analysis/EngineLines.tsx"
      to: "PvLine per-line score"
      via: "read pvLines[i].evalCp / pvLines[i].evalMate (NOT a non-existent pvLine.score)"
      pattern: "\\.evalCp|\\.evalMate"
---

<objective>
Build the two prop-driven analysis viz components that render `useStockfishEngine` output (Phase 136):
`EvalBar.tsx` (vertical white-POV sigmoid centipawn bar with a depth-gated mate label, D-04) and
`EngineLines.tsx` (top 1-2 PV lines with per-line score, a depth badge, an "Analyzing…" indicator,
and clickable PV move chips, D-03). Also add the two `EVAL_BAR_WHITE` / `EVAL_BAR_BLACK` semantic
re-exports to `theme.ts` that EvalBar imports.

Both components are pure presentational: they render from props only and install no keyboard
handlers. They have NO dependency on the move-tree hook, so this plan runs in Wave 1 alongside
Plan 01. Verified by Vitest render tests against fixture props (D-05).

Purpose: these are the engine-output surfaces the Phase 138 page wires to the live engine.
Output: theme re-exports, EvalBar, EngineLines, and their co-located render tests.

Implements: BOARD-02 (EvalBar is white-POV fixed regardless of board flip — D-04), BOARD-03
(EngineLines PV chips are clickable and call `onMoveClick(from, to)` → `makeMove`, the lichess /
chess.com convention — D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-useanalysisboard-hook-analysis-display-components/137-CONTEXT.md
@.planning/phases/137-useanalysisboard-hook-analysis-display-components/137-PATTERNS.md
@.planning/phases/137-useanalysisboard-hook-analysis-display-components/137-UI-SPEC.md
@.planning/phases/136-usestockfishengine-hook-wasm-setup/136-CONTEXT.md
</context>

<artifacts_produced>
This plan creates / modifies:
- `frontend/src/lib/theme.ts` — adds `export const EVAL_BAR_WHITE` and `export const EVAL_BAR_BLACK` (semantic re-exports of the existing `EVAL_CHART_AREA_WHITE_AHEAD` / `EVAL_CHART_AREA_BLACK_AHEAD` constants).
- `frontend/src/components/analysis/EvalBar.tsx` — `export function EvalBar`; `interface EvalBarProps { evalCp: number | null; evalMate: number | null; depth: number; className?: string }`. data-testid: `analysis-eval-bar`.
- `frontend/src/components/analysis/EngineLines.tsx` — `export function EngineLines`; `interface EngineLinesProps { pvLines: PvLine[]; depth: number; isAnalyzing: boolean; startPly?: number; onMoveClick: (from: string, to: string) => void }`. data-testids: `analysis-engine-lines`, `engine-lines-analyzing`, `engine-line-{N}-move-{M}`.
- `frontend/src/components/analysis/__tests__/EvalBar.test.tsx`
- `frontend/src/components/analysis/__tests__/EngineLines.test.tsx`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: theme.ts EvalBar re-exports + EvalBar component + render test</name>
  <files>frontend/src/lib/theme.ts, frontend/src/components/analysis/EvalBar.tsx, frontend/src/components/analysis/__tests__/EvalBar.test.tsx</files>
  <read_first>
    - frontend/src/lib/theme.ts lines 40-70 (existing EVAL_CHART_AREA_WHITE_AHEAD / EVAL_CHART_AREA_BLACK_AHEAD at lines 48-49 — the source values to re-export)
    - .planning/phases/137-useanalysisboard-hook-analysis-display-components/137-UI-SPEC.md lines 131-184 (EvalBar full contract: container classes, sigmoid formula, white-fraction logic, depth-8 mate gate, props, data-testid, ARIA) and lines 437-441 (theme re-export text)
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx lines 1-50 (test analog: jsdom header, matchMedia + ResizeObserver stubs, render/screen/cleanup idiom)
  </read_first>
  <action>
    First, append two semantic re-exports to theme.ts immediately after the existing eval-chart
    constants (lines 48-49): `export const EVAL_BAR_WHITE = EVAL_CHART_AREA_WHITE_AHEAD;` and
    `export const EVAL_BAR_BLACK = EVAL_CHART_AREA_BLACK_AHEAD;` with a one-line comment that both
    surfaces share one palette entry. Do NOT introduce new oklch literals.

    Create EvalBar.tsx as a pure presentational component (props only; no hook, no state beyond
    derived values). Props per UI-SPEC lines 169-177: `{ evalCp: number | null; evalMate: number |
    null; depth: number; className?: string }`. Import EVAL_BAR_WHITE / EVAL_BAR_BLACK from
    '@/lib/theme' — never the chart constants directly.

    Layout per UI-SPEC lines 133-139: outer container
    `relative flex flex-col border border-border rounded overflow-hidden w-4` plus the className
    override; a white fill div absolutely positioned top with height = whiteFraction*100% and inline
    `style={{ background: EVAL_BAR_WHITE }}`, and a black fill div at bottom with the remainder and
    `EVAL_BAR_BLACK`. Use `transition-[height] duration-150` on the white fill.

    White-fraction logic (UI-SPEC lines 142-156), with a named `SIGMOID_SCALE = 400` const (no magic
    number): cpToFraction(cp) = 1 / (1 + exp(-cp / SIGMOID_SCALE)). If evalMate != null and depth >= 8:
    evalMate > 0 → 1.0, evalMate < 0 → 0.0, evalMate === 0 → 0.5. Else if evalCp != null →
    cpToFraction(evalCp). Else (or evalMate != null but depth < 8 with evalCp null) → 0.5.

    Mate label (D-04, depth >= 8 gate, UI-SPEC lines 158-167): render a centered overlaid `<span>` ONLY
    when `evalMate !== null && depth >= 8`. Content `M{Math.abs(evalMate)}`; position `top-2` for
    evalMate > 0 (white region) or `bottom-2` for evalMate < 0; class `text-sm font-semibold` with
    contrasting text color (dark text in the white region, light text in the black region) and
    `absolute left-1/2 -translate-x-1/2`. When depth < 8 and evalMate !== null, do NOT show the label.

    White-POV rule (D-04): EvalBar takes NO orientation prop and never flips — white fill is always at
    top regardless of board orientation. data-testid="analysis-eval-bar" on the outer container;
    `role="img"` and a dynamic `aria-label="Engine evaluation: {score}"` updated each render (derive
    score text from evalCp/evalMate: a centipawn like +1.42 / -0.33, or M{n}). text-sm floor; no text-xs.

    Then create EvalBar.test.tsx (jsdom header + matchMedia/ResizeObserver stubs from the
    PercentileChip analog; afterEach(cleanup)). Assert against fixture props: (a) a positive evalCp
    yields a taller white fill than a negative evalCp (assert via the inline style height or computed
    fraction surfaced in a testable way — query by data-testid and read the fill height attribute/
    style); (b) evalMate=3 with depth=10 renders an "M3" label; (c) evalMate=3 with depth=5 renders NO
    mate label; (d) both null → no mate label and a balanced (0.5) fill. This test file is the in-phase
    importer of EvalBar and the theme constants (knip).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/analysis/__tests__/EvalBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `theme.ts` exports `EVAL_BAR_WHITE` and `EVAL_BAR_BLACK`: `grep -c 'export const EVAL_BAR_\(WHITE\|BLACK\)' src/lib/theme.ts` returns 2.
    - EvalBar imports the bar constants, not the chart constants: `grep -c 'EVAL_CHART_AREA' src/components/analysis/EvalBar.tsx` returns 0; `grep -c 'EVAL_BAR_WHITE' src/components/analysis/EvalBar.tsx` >= 1.
    - EvalBar contains no oklch literal: `grep -c 'oklch' src/components/analysis/EvalBar.tsx` returns 0.
    - EvalBar takes no `orientation` prop: `grep -c 'orientation' src/components/analysis/EvalBar.tsx` returns 0.
    - `data-testid="analysis-eval-bar"` present; `SIGMOID_SCALE` is a named const (no bare 400 in the formula).
    - Test asserts the depth-8 mate gate (M-label present at depth 10, absent at depth 5) and passes; `npx tsc -b` clean.
  </acceptance_criteria>
  <done>EvalBar renders a white-POV sigmoid fill from theme constants, gates the mate label at depth >= 8, and its render test + tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: EngineLines component (per-line score, depth badge, clickable PV chips, analyzing state) + render test</name>
  <files>frontend/src/components/analysis/EngineLines.tsx, frontend/src/components/analysis/__tests__/EngineLines.test.tsx</files>
  <read_first>
    - frontend/src/hooks/uciParser.ts lines 14-24 (PvLine type — multipv, depth, moves[], evalCp, evalMate; CONFIRM there is NO `score` field)
    - .planning/phases/137-useanalysisboard-hook-analysis-display-components/137-UI-SPEC.md lines 187-253 (EngineLines contract: score-format table, depth badge, analyzing state, PV chip interaction, truncation, props, data-testids, ARIA)
    - .planning/phases/137-useanalysisboard-hook-analysis-display-components/137-PATTERNS.md lines 13-33 + 171-196 (CRITICAL CONTRACT MISMATCH — read per-line evalCp/evalMate, NOT pvLine.score; chip class string; UCI→from/to derivation; Loader2 a11y)
    - frontend/src/components/board/HorizontalMoveList.tsx lines 100-104 (the shared chip class string to reuse for PV chips)
    - frontend/src/lib/moveNumberLabel.ts (moveLabel(startPly, index) → "12." / "12...")
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx lines 1-50 (test analog setup)
  </read_first>
  <action>
    Create EngineLines.tsx as a pure presentational component. Props (resolve the UI-SPEC contract
    mismatch — see PATTERNS CRITICAL CONTRACT MISMATCH): `interface EngineLinesProps { pvLines:
    PvLine[]; depth: number; isAnalyzing: boolean; startPly?: number; onMoveClick: (from: string, to:
    string) => void }`. DROP the redundant top-level `evalCp`/`evalMate` props the UI-SPEC's
    EngineLinesProps lists — each line's score comes from its own `pvLines[i].evalCp` /
    `pvLines[i].evalMate`, since the real Phase 136 PvLine has no `score` field. Import PvLine from
    '@/hooks/uciParser' and moveLabel from '@/lib/moveNumberLabel'. startPly defaults to 0.

    Render the outer container with data-testid="analysis-engine-lines", `aria-label="Engine lines"`,
    `aria-live="polite"`. Show at most the first 2 PV lines (index 0..1). Per line, render a header
    with the formatted per-line score and, for line 0 only, a single depth badge `d{depth}`
    (`text-sm text-muted-foreground`). Per-line score format (UI-SPEC lines 200-207, read from
    line.evalMate then line.evalCp): evalMate > 0 → `#+{evalMate}`; evalMate < 0 →
    `#-{abs(evalMate)}`; evalCp >= 0 → `+{(evalCp/100).toFixed(2)}`; evalCp < 0 →
    `{(evalCp/100).toFixed(2)}`; both null → `…`. Score text `text-sm font-semibold text-foreground`
    (EvalBar owns the directional color signal — no color encoding here). Separate lines with
    `border-t border-border`.

    Per-line move chips (D-03, UI-SPEC lines 222-227): render the first 5 plies of `line.moves` (all
    if fewer) as `<button>` chips using the HorizontalMoveList chip class string (PATTERNS line 176:
    `inline-flex items-center gap-0.5 rounded px-1 py-0.5 font-mono transition-colors hover:bg-accent`)
    plus `text-sm`. PV chips get NO active highlight. Derive from/to/promotion from the UCI string:
    `move.slice(0,2)`, `move.slice(2,4)`, `move[4]` if present; `onClick={() => onMoveClick(from, to)}`.
    data-testid={`engine-line-${lineIndex}-move-${moveIndex}`}; aria-label={`Play ${san-or-uci}`}.
    Optionally prefix white moves with moveLabel(startPly, plyIndex) as a non-button muted span.

    Analyzing state (UI-SPEC lines 213-219): when `isAnalyzing && pvLines.length === 0`, render a
    container with data-testid="engine-lines-analyzing" holding a lucide `Loader2`
    (`h-4 w-4 animate-spin text-muted-foreground`) wrapped in `<span aria-label="Analyzing"
    aria-busy="true">` and the text "Analyzing…" (`text-sm text-muted-foreground`). When
    `isAnalyzing && pvLines.length > 0`, render the lines normally with NO spinner (the incrementing
    depth badge is the progress signal). When `!isAnalyzing && pvLines.length === 0`, render empty
    content (no error copy — Phase 138 owns engine-loading chrome).

    Narrow every array index per noUncheckedIndexedAccess: `const line = pvLines[i]; if (!line)
    return null;` and the loop-bound `moves[j]!` idiom with a `// safe: loop bound` comment — never a
    bare `pvLines[i]` / `moves[j]`. text-sm floor (no text-xs). Keep nesting <= 3; extract a
    per-line subcomponent or helper if the body grows past the CLAUDE.md soft LOC limit.

    Then create EngineLines.test.tsx (jsdom header + stubs + afterEach(cleanup), PercentileChip
    analog). Build PvLine[] fixtures and assert: (a) two fixture lines render two chip rows; the score
    text matches the format table (e.g. evalCp 142 → "+1.42", evalMate 3 → "#+3"); (b) the depth badge
    `d{depth}` appears once; (c) fireEvent.click on `engine-line-0-move-0` calls onMoveClick with the
    correct derived from/to (e.g. UCI "e2e4" → "e2","e4") via a vi.fn(); (d) isAnalyzing && empty
    pvLines renders `engine-lines-analyzing`, while isAnalyzing && non-empty pvLines does NOT. This
    test file is the in-phase importer of EngineLines (knip).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/analysis/__tests__/EngineLines.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EngineLines reads per-line scores, not a non-existent score field: `grep -c 'pvLine\.score\|\.score\b' src/components/analysis/EngineLines.tsx` returns 0; `grep -cE '\.evalCp|\.evalMate' src/components/analysis/EngineLines.tsx` >= 1.
    - data-testids present: `analysis-engine-lines`, `engine-lines-analyzing`, and a templated `engine-line-${...}-move-${...}`.
    - Every PV move is a `<button>` (semantic HTML); no `<div onClick` / `<span onClick`: `grep -cE '<(div|span)[^>]*onClick' src/components/analysis/EngineLines.tsx` returns 0.
    - No text-xs: `grep -c 'text-xs' src/components/analysis/EngineLines.tsx` returns 0.
    - Test asserts onMoveClick("e2","e4") fires on the line-0 move-0 click, the score-format strings, the single depth badge, and the analyzing-state gate; all pass. `npx tsc -b` clean.
  </acceptance_criteria>
  <done>EngineLines renders per-line scores from PvLine.evalCp/evalMate, a single depth badge, clickable PV chips wired to onMoveClick, and the correctly-gated analyzing indicator — render test + tsc pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Stockfish worker → component DOM | Engine-derived strings (SAN/UCI move text, mate/centipawn labels, depth) from the Phase 136 worker are rendered into the DOM by EvalBar/EngineLines. The only plausible surface is rendering these strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-03 | Tampering / Information disclosure (XSS) | EngineLines / EvalBar string rendering | mitigate | Render all engine-derived text via normal JSX children (React auto-escapes). NO `dangerouslySetInnerHTML` anywhere in either component. |
| T-137-04 | Denial of service | progressive engine output re-render | accept | Components render from props; truncate PV display to the first 5 plies and at most 2 lines — bounded DOM regardless of engine output volume. |

No package installs in this plan (lucide-react / react / chess.js already present) — no package legitimacy gate required.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` — zero type errors.
- `cd frontend && npx vitest run src/components/analysis/__tests__/EvalBar.test.tsx src/components/analysis/__tests__/EngineLines.test.tsx` — all pass.
- `cd frontend && npm run knip` — EVAL_BAR_WHITE/EVAL_BAR_BLACK imported by EvalBar; EvalBar/EngineLines imported by their tests; no dead exports.
- `cd frontend && npm run lint` — clean.
- No `dangerouslySetInnerHTML`: `grep -rc 'dangerouslySetInnerHTML' src/components/analysis/EvalBar.tsx src/components/analysis/EngineLines.tsx` returns 0 for both.
</verification>

<success_criteria>
EvalBar renders a fixed white-POV sigmoid centipawn bar with a depth-8-gated mate label from theme
constants; EngineLines renders up to 2 PV lines with correct per-line scores, a single depth badge,
clickable PV chips wired to onMoveClick, and a correctly-gated Analyzing indicator. Both pass their
Vitest render tests, tsc, knip, and lint, and use no dangerouslySetInnerHTML.
</success_criteria>

<output>
Create `.planning/phases/137-useanalysisboard-hook-analysis-display-components/137-02-SUMMARY.md` when done.
</output>
