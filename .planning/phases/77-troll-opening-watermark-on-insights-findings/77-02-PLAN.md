---
phase: 77-troll-opening-watermark-on-insights-findings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/scripts/curate-troll-openings.ts
  - frontend/src/data/trollOpenings.ts
autonomous: false
requirements: []
requirements_addressed: [D-01, D-09]

must_haves:
  truths:
    - "frontend/scripts/curate-troll-openings.ts exists and is runnable via `npx tsx`"
    - "Running the script emits a candidate list to stdout — does NOT auto-write the data file"
    - "The candidate list is surfaced to the user for hand-pruning approval before commit (D-01 hard checkpoint)"
    - "frontend/src/data/trollOpenings.ts exists with WHITE_TROLL_KEYS and BLACK_TROLL_KEYS as ReadonlySet<string>"
    - "The committed data file reflects strict Bongcloud-tier pruning (Bongcloud, Grob, Borg, Halloween, Barnes, Fred — excludes Englund, Latvian, From's, Schliemann, Damiano)"
  artifacts:
    - path: "frontend/scripts/curate-troll-openings.ts"
      provides: "Reproducible curation script that fetches/caches cEDAMVBB.pgn, derives side-only keys, prints candidates"
      contains: "deriveUserSideKey"
    - path: "frontend/src/data/trollOpenings.ts"
      provides: "Curated WHITE_TROLL_KEYS and BLACK_TROLL_KEYS as ReadonlySet<string>"
      exports: ["WHITE_TROLL_KEYS", "BLACK_TROLL_KEYS"]
  key_links:
    - from: "frontend/scripts/curate-troll-openings.ts"
      to: "https://lichess.org/study/cEDAMVBB.pgn"
      via: "fetch with local cache at frontend/scripts/.cache/cEDAMVBB.pgn"
      pattern: "cEDAMVBB"
    - from: "frontend/src/data/trollOpenings.ts"
      to: "frontend/src/lib/trollOpenings.ts (Plan 01 consumer)"
      via: "Set<string> exports consumed by isTrollPosition"
      pattern: "ReadonlySet<string>"
---

<objective>
Build the offline curation pipeline (D-09) and ship the curated static data module (D-01). The script reads the Lichess study `cEDAMVBB`, walks each chapter via chess.js, derives the side-only FEN key for every mainline ply, and prints the full candidate list to stdout. A human reviewer then hand-prunes to the strict Bongcloud-tier set before the executor writes `frontend/src/data/trollOpenings.ts`.

Purpose: D-01 mandates hand-pruning before commit — automation alone cannot decide whether the Englund Gambit is "fun but legit" or troll-tier. D-09 mandates a reproducible script alongside the data file so the curation path stays auditable.

Output: A re-runnable curation script + a hand-pruned static `ReadonlySet<string>` data module that the helpers in Plan 01 consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-troll-opening-watermark-on-insights-findings/77-CONTEXT.md
@.planning/phases/77-troll-opening-watermark-on-insights-findings/77-RESEARCH.md
@.planning/phases/77-troll-opening-watermark-on-insights-findings/77-PATTERNS.md
@CLAUDE.md
@scripts/gen_endgame_zones_ts.py
@frontend/src/generated/endgameZones.ts
@frontend/src/components/move-explorer/MoveExplorer.tsx

<interfaces>
<!-- Required exports from frontend/src/data/trollOpenings.ts (consumed by Plan 01's lib): -->
```typescript
export const WHITE_TROLL_KEYS: ReadonlySet<string>;
export const BLACK_TROLL_KEYS: ReadonlySet<string>;
```

<!-- chess.js usage (already a dep, version 1.4.0): -->
```typescript
import { Chess } from 'chess.js';
const chess = new Chess();
chess.loadPgn(chapterText);  // throws on invalid PGN per RESEARCH.md Pitfall 6
const headers = chess.header();
const sanHistory = chess.history();
const finalFen = chess.fen();
```

<!-- Color type from Plan 01 / existing api.ts: -->
```typescript
type Color = 'white' | 'black';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create curation script that emits candidates to stdout</name>
  <files>
    - frontend/scripts/curate-troll-openings.ts (new)
    - frontend/scripts/.cache/.gitignore (new — ignore the cached PGN)
  </files>
  <read_first>
    - .planning/phases/77-troll-opening-watermark-on-insights-findings/77-RESEARCH.md (Curation Script Skeleton, Pitfalls 1, 2, 6)
    - .planning/phases/77-troll-opening-watermark-on-insights-findings/77-PATTERNS.md (`frontend/scripts/curate_troll_openings.ts` section — note PATTERNS uses underscore filename; this plan uses kebab-case `curate-troll-openings.ts` for repo convention consistency)
    - scripts/gen_endgame_zones_ts.py (Python analog: docstring + usage convention)
    - frontend/src/components/move-explorer/MoveExplorer.tsx (chess.js usage pattern at lines 68-72)
  </read_first>
  <action>
    1. Run `mkdir -p frontend/scripts/.cache` (the directory does NOT exist yet).
    2. Create `frontend/scripts/.cache/.gitignore` with the single line `*.pgn` (so the cached PGN is not committed).
    3. Create `frontend/scripts/curate-troll-openings.ts`. The script MUST:
       - Use top-of-file JSDoc explaining purpose, run command (`npx tsx frontend/scripts/curate-troll-openings.ts`), and the D-01/D-09 human-pruning requirement.
       - Fetch `https://lichess.org/study/cEDAMVBB.pgn` if cache absent; otherwise read from `frontend/scripts/.cache/cEDAMVBB.pgn`.
       - Split the multi-game PGN on the chapter boundary regex `/\n\n(?=\[Event )/g`.
       - For each chapter: instantiate `new Chess()`, call `chess.loadPgn(chapterText)` inside try/catch (Pitfall 6); on success, iterate `chess.history({ verbose: true })` to walk each mainline ply; for EACH ply, replay onto a fresh `Chess()` to get `board.fen()` at that ply, then derive the side-only key for BOTH white and black sides (let the human reviewer pick the right ply + side later).
       - Embed `deriveUserSideKey` and `canonicalizeRank` inline (do NOT import from `@/lib/trollOpenings.ts` — the script runs under `npx tsx` and the path alias may not resolve; copy the function bodies verbatim from RESEARCH.md Pattern 1). Add a comment noting the duplication is intentional and the test in Plan 01 covers the canonical implementation.
       - Print a human-readable candidate list to stdout in the format:
         ```
         === TROLL OPENING CANDIDATES (review and prune per D-01) ===
         [white] <chapter title> after ply <N>: <SAN sequence>
           key: <derived key>
         [black] <chapter title> after ply <N>: <SAN sequence>
           key: <derived key>
         ...
         Total candidates: <count>. Hand-prune to strict Bongcloud-tier set per D-01 before committing.
         ```
       - Do NOT write `frontend/src/data/trollOpenings.ts` automatically (Pitfall in RESEARCH.md anti-patterns).
       - Apply `noUncheckedIndexedAccess` discipline: every array index access uses `// safe:` comments + `!` non-null assertion or explicit narrowing.
    4. Type signatures: top-level `async function main(): Promise<void>` invoked at file end with `.catch(err => { console.error(err); process.exit(1); })`.
    5. Make the script idempotent: rerunning produces the same output (deterministic ordering by chapter index in PGN).
  </action>
  <verify>
    <automated>test -f frontend/scripts/curate-troll-openings.ts && grep -q "TROLL OPENING CANDIDATES" frontend/scripts/curate-troll-openings.ts && grep -q "deriveUserSideKey" frontend/scripts/curate-troll-openings.ts && grep -q "do NOT auto-write" frontend/scripts/curate-troll-openings.ts -i || grep -qi "human review" frontend/scripts/curate-troll-openings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/scripts/curate-troll-openings.ts` exists
    - `frontend/scripts/.cache/.gitignore` exists and contains `*.pgn`
    - Script contains a JSDoc top-of-file comment naming the run command `npx tsx frontend/scripts/curate-troll-openings.ts`
    - Script contains an embedded `deriveUserSideKey` function (verified by `grep -c "function deriveUserSideKey"` returning 1)
    - Script contains the literal stdout header `=== TROLL OPENING CANDIDATES (review and prune per D-01) ===`
    - Script does NOT call `writeFileSync` on any path under `frontend/src/data/` — `grep -E "writeFileSync.*src/data" frontend/scripts/curate-troll-openings.ts` exits non-zero (no match)
    - Running `cd frontend && npx tsx scripts/curate-troll-openings.ts > /tmp/troll-candidates.txt 2>&1` exits 0 (allow network failure to cache the PGN; if offline, document in SUMMARY)
    - The output file `/tmp/troll-candidates.txt` contains the literal `Total candidates:` line
  </acceptance_criteria>
  <done>The curation script is committable, re-runnable, and prints candidates to stdout without ever writing the production data file. The Bongcloud is in the candidate list (sanity check: search the output for the literal key `8/8/8/8/4P3/8/PPPPKPPP/RNBQ1BNR`).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Surface candidate list to user for hand-pruning approval (D-01 checkpoint)</name>
  <files>
    - /tmp/troll-candidates.txt (transient script output — surfaced to user; not committed)
  </files>
  <action>
    Run `cd frontend && npx tsx scripts/curate-troll-openings.ts > /tmp/troll-candidates.txt 2>&1`, then PAUSE and surface the candidate list to the user verbatim (paste the file contents into the chat). Wait for the user's approved pruned list before proceeding to Task 3. This task is `autonomous: false` per CONTEXT.md D-01 — hand-pruning is a hard human checkpoint.
  </action>
  <verify>
    <automated>test -f /tmp/troll-candidates.txt && grep -q "Total candidates:" /tmp/troll-candidates.txt</automated>
  </verify>
  <done>User has reviewed the candidate list and replied with explicit approval (either a pasted curated set or "go ahead with [list]" using canonical defining positions). The approved entries are captured in the conversation for Task 3 to consume.</done>
  <what-built>
    Curation script `frontend/scripts/curate-troll-openings.ts` from Task 1, plus a candidate-list output captured in `/tmp/troll-candidates.txt` (or pasted inline if no file).
  </what-built>
  <how-to-verify>
    1. Re-run the script if needed: `cd frontend && npx tsx scripts/curate-troll-openings.ts > /tmp/troll-candidates.txt 2>&1`
    2. Open `/tmp/troll-candidates.txt` and review every candidate row. The reviewer (you, Adrian) decides:
       - Which entries qualify as strict Bongcloud-tier (KEEP).
       - Which entries are "fun but legit" gambits (DROP — Englund, Latvian, From's, Schliemann, Damiano per D-01).
       - For each KEPT entry, which ply is the canonical "defining position" (Pitfall 2 — usually the first move that establishes the troll identity, NOT the deep mainline tail).
       - Which color (white_hash, black_hash, or both) the entry belongs to.
    3. Reply with:
       - The pruned list of `(side, key, comment)` tuples to commit, OR
       - A paste-ready snippet of the two `Set<string>` literals.
    4. The executor will paste your approved entries into `frontend/src/data/trollOpenings.ts` in Task 3.

    Reference openings to look for in the candidate list (your minimum target set per D-01):
    - **Bongcloud Attack** (white) — `1.e4 e5 2.Ke2`
    - **Grob** (white) — `1.g4`
    - **Borg / Reversed Grob** (black) — `1.e4 g5`
    - **Halloween Gambit** (white) — `1.e4 e5 2.Nf3 Nc6 3.Nc3 Nf6 4.Nxe5`
    - **Barnes Opening** (white) — `1.f3`
    - **Fred Defense** (black) — `1.e4 f5`

    If any of these are MISSING from the candidate list, the script's chapter parsing has a gap — flag it and the executor will iterate.
  </how-to-verify>
  <resume-signal>
    Type "approved with the following entries: ..." (paste the curated set) or "go ahead with [Bongcloud, Grob, Borg, Halloween, Barnes, Fred] using the canonical defining positions" if you trust the executor to pick the right plies, or describe specific changes needed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the hand-pruned data module</name>
  <files>
    - frontend/src/data/trollOpenings.ts (new)
  </files>
  <read_first>
    - The user's response to the Task 2 checkpoint (curated entries)
    - frontend/src/generated/endgameZones.ts (analog: AUTO-GENERATED header pattern)
    - .planning/phases/77-troll-opening-watermark-on-insights-findings/77-PATTERNS.md (`frontend/src/data/trollOpenings.ts` section — header style + ReadonlySet shape)
    - .planning/phases/77-troll-opening-watermark-on-insights-findings/77-RESEARCH.md (Output Data Module example, Pitfall 5 about knip)
  </read_first>
  <action>
    1. Run `mkdir -p frontend/src/data` (the directory does NOT exist yet).
    2. Create `frontend/src/data/trollOpenings.ts` with this exact shape:
       ```typescript
       // Curated troll-opening positions, keyed by user-side-only FEN piece-placement.
       // Source: lichess.org/study/cEDAMVBB.pgn (hand-pruned to strict Bongcloud-tier per Phase 77 D-01).
       // Regenerate with: npx tsx frontend/scripts/curate-troll-openings.ts
       //   then hand-prune the candidate list before pasting into the Set literals below.
       //
       // Keys are derived via deriveUserSideKey(fen, side) in @/lib/trollOpenings — they are
       // the FEN piece-placement field with opponent pieces stripped and empty squares
       // re-canonicalized. Stable across opponent variations of the same user-side position.

       export const WHITE_TROLL_KEYS: ReadonlySet<string> = new Set([
         // <inline comments name the opening + move sequence per Pattern 3 in PATTERNS.md>
         // Bongcloud Attack — after 1.e4 e5 2.Ke2
         '8/8/8/8/4P3/8/PPPPKPPP/RNBQ1BNR',
         // ... (paste user-approved white-side keys here)
       ]);

       export const BLACK_TROLL_KEYS: ReadonlySet<string> = new Set([
         // ... (paste user-approved black-side keys here; may be empty if user approves only white openings)
       ]);
       ```
    3. Paste the user-approved entries verbatim. Each entry MUST have an inline comment naming the opening and the move sequence that produces it.
    4. If the user approved an empty BLACK_TROLL_KEYS, KEEP THE EXPORT (it must remain `export const BLACK_TROLL_KEYS: ReadonlySet<string> = new Set([])`). Pitfall 5: dropping an empty export would break knip's import-graph since `frontend/src/lib/trollOpenings.ts` references both names. The shared `isTrollPosition` consumer keeps both alive.
    5. Run `cd frontend && npm test -- --run src/lib/trollOpenings.test.ts` to confirm Plan 01's tests still pass — the test mocks the data module, so this is a regression check on import-time wiring (the test SHOULD pass with the real data module loaded too because the mock takes precedence).
    6. Run `cd frontend && npm run knip` to confirm no unused exports.
    7. Run `cd frontend && npx tsc --noEmit -p tsconfig.app.json` to confirm typecheck passes (this resolves the Plan 01 Task 2 dependency note).
  </action>
  <verify>
    <automated>test -f frontend/src/data/trollOpenings.ts && grep -q "export const WHITE_TROLL_KEYS: ReadonlySet<string>" frontend/src/data/trollOpenings.ts && grep -q "export const BLACK_TROLL_KEYS: ReadonlySet<string>" frontend/src/data/trollOpenings.ts && grep -q "8/8/8/8/4P3/8/PPPPKPPP/RNBQ1BNR" frontend/src/data/trollOpenings.ts && cd frontend && npm test -- --run src/lib/trollOpenings.test.ts && npm run knip && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/data/trollOpenings.ts` exists
    - File contains `export const WHITE_TROLL_KEYS: ReadonlySet<string>`
    - File contains `export const BLACK_TROLL_KEYS: ReadonlySet<string>`
    - File contains the Bongcloud key `8/8/8/8/4P3/8/PPPPKPPP/RNBQ1BNR` (canonical strict-tier sentinel)
    - File header documents the regeneration command
    - `cd frontend && npm test -- --run src/lib/trollOpenings.test.ts` exits 0
    - `cd frontend && npm run knip` exits 0 (no unused exports)
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0
    - Each entry in the Set literals has an inline `// <Opening Name> — after <SAN sequence>` comment immediately above
  </acceptance_criteria>
  <done>The curated data module is committed, both Set exports are typed as `ReadonlySet<string>`, knip + tsc are green, and the Plan 01 unit tests still pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsx scripts/curate-troll-openings.ts | head -20` shows the candidate-list header
- `cd frontend && npm test -- --run src/lib/trollOpenings.test.ts` exits 0 (Plan 01 tests against the real data import)
- `cd frontend && npm run knip` exits 0
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0 (resolves the dependency from Plan 01)
- The committed `frontend/src/data/trollOpenings.ts` reflects strict Bongcloud-tier scope (no Englund/Latvian/From's/Schliemann/Damiano)
</verification>

<success_criteria>
1. Curation script is committed and re-runnable via `npx tsx` (D-09) ✓
2. Script emits to stdout only — never auto-writes the data file (D-09 anti-pattern avoided) ✓
3. Hand-pruning checkpoint executed with explicit user approval before commit (D-01) ✓
4. `frontend/src/data/trollOpenings.ts` exports `WHITE_TROLL_KEYS` and `BLACK_TROLL_KEYS` as `ReadonlySet<string>` ✓
5. The Bongcloud sentinel key is present in the curated set (canary that the strict tier was correctly identified) ✓
6. Knip + tsc green; Plan 01 unit tests still pass against the real data module ✓
</success_criteria>

<output>
After completion, create `.planning/phases/77-troll-opening-watermark-on-insights-findings/77-02-SUMMARY.md` documenting:
- Final list of openings included in WHITE_TROLL_KEYS and BLACK_TROLL_KEYS (names + SAN sequences from inline comments)
- Total candidate count from the script run vs. final pruned count (e.g., "47 candidates → 6 strict-tier keys")
- Whether any expected canonical entries (Bongcloud, Grob, Borg, Halloween, Barnes, Fred) were MISSING from the script output and how that gap was handled
- The cache state of `frontend/scripts/.cache/cEDAMVBB.pgn` (cached locally, gitignored)
</output>
