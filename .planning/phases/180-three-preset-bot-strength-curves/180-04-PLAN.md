---
phase: 180-three-preset-bot-strength-curves
plan: 04
type: execute
wave: 3
depends_on:
  - 180-02
  - 180-03
files_modified:
  - .planning/notes/2026-07-XX-three-preset-bot-strength-curves-findings.md
  - reports/data/bot-curves-internal-scale.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "the full engine-free logic layer (all calibration .check.mjs + fit pytest) is green before any real engine runs (D-02a gate)"
    - "a small real-engine pilot (1-2 cells, low N) confirms sane non-clamped ratings, correct INTERNAL_RATING windowing, BOTH anchor families firing, and byte-identical --resume continuation (D-02b HUMAN-UAT)"
    - "the phase COMPLETES at the proven pilot; the full ~1,440-game (~18-22h) sweep + fitted per-preset curves + G_preset + findings note are a folded-in operator-run HUMAN-UAT step, not on the interactive critical path (D-01)"
    - "the operator sweep runs three per-preset non-uniform grids (D-03: separate rows, Human low / Light mid / Deep high, overlapping in the middle) as three per-preset invocations with the D-04 grids (Human 700/1100/1500/1900/2300 @ blend 0; Light 1100/1300/1500/1700/1900 @ blend 0.05; Deep 1100/1500/1900/2300/2600 @ blend 0.5) at 24 games/(cell,anchor) baseline (30 for brackets short of 4 anchors) (D-03, D-04, D-05)"
    - "the findings note mirrors the 173 note and bakes in the interpretation caveats (Deep is a ceiling not deeper; sf0/maia700 style-outlier cells; G_preset subtracted not ignored due to non-transitivity)"
  artifacts:
    - reports/data/bot-curves-internal-scale.json
    - .planning/notes/2026-07-XX-three-preset-bot-strength-curves-findings.md
  key_links:
    - "the pilot exercises the full Plan 01 + 02 + 03 chain end-to-end (schedule module -> harness two-pass -> fit) on real engines"
    - "the bot-curves JSON is the SEED-104 interface (per-preset per-bot_elo ratings + CIs + G_preset)"
---

<objective>
Prove the harness on a pilot (the phase's actual completion gate, D-01/D-02) and fold in the operator-run HUMAN-UAT full sweep. Two-layer gate: (a) the engine-free logic layer must be fully green (automated); (b) a small real-engine pilot confirms the machinery behaves on real Maia/Stockfish (blocking human-verify). Then the folded-in operator step runs the overnight sweep, the extended fitter, and writes the findings note.

Purpose: Guarantee the harness fixes are real before committing an 18-22h sweep, and deliver the downstream artifact + human-readable findings.
Output: A validated pilot, the bot-curves JSON, and a findings note.

**D-01 boundary:** The phase is COMPLETE once the pilot passes. The overnight sweep + fitted curves + `G_preset` + findings note are explicitly HUMAN-UAT (operator-run, off the interactive critical path) — this plan is `autonomous: false` for that reason.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-three-preset-bot-strength-curves/180-CONTEXT.md
@.planning/phases/180-three-preset-bot-strength-curves/180-RESEARCH.md
@.planning/phases/180-three-preset-bot-strength-curves/180-VALIDATION.md
@.planning/notes/2026-07-15-anchor-ladder-self-calibration-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green the full engine-free logic layer (D-02a gate)</name>
  <files>(no source edits — verification gate)</files>
  <read_first>
    - 180-VALIDATION.md (Sampling Rate, Phase gate, Wave 0 Requirements)
    - scripts/lib/calibration-bot-cell-schedule.check.mjs (Plan 01)
    - scripts/lib/calibration-near-free-metrics.check.mjs (Plan 03)
    - tests/scripts/test_calibration_anchor_fit.py (Plan 02)
  </read_first>
  <action>
    Run the complete engine-free logic layer as the pre-pilot gate: every new/touched calibration `.check.mjs` (bot-cell schedule + near-free metrics) via `node --import ./scripts/lib/frontend-alias-hook.mjs <file>`, plus the fitter pytest selection. All must be green before any real engine is spawned. If any is red, STOP — the pilot is not authorized until the logic layer passes (D-02a precedes D-02b).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-bot-cell-schedule.check.mjs && node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-near-free-metrics.check.mjs && uv run pytest tests/scripts/test_calibration_anchor_fit.py -k "fit_bot_cell_rating or g_preset" -x</automated>
  </verify>
  <acceptance_criteria>
    - Both `.check.mjs` scripts exit 0 and the fitter pytest selection exits 0.
    - No real Maia/Stockfish engine was spawned by this task (logic layer only).
  </acceptance_criteria>
  <done>The entire engine-free logic layer is green; the real-engine pilot is authorized.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-02(b) real-engine pilot (phase completion gate)</name>
  <action>Run the small real-engine pilot per the steps below and confirm sane non-clamped ratings, both anchor families firing, correct internal-scale windowing, and byte-identical --resume. This is the phase's completion gate (D-01/D-02).</action>
  <what-built>The internal-scale two-pass harness (Plan 03) + the fit extension (Plan 02), proven engine-free (Task 1). This checkpoint runs the D-02(b) small real-engine pilot: 1-2 real cells at low N against the 10 measured anchors, then the fit on that pilot TSV.</what-built>
  <how-to-verify>
    1. Run a low-N pilot on 1-2 cells whose true internal rating is expected to sit INSIDE the 1069-1908 anchor range (e.g. Light blend 0.05 at bot_elo 1500, and Human blend 0 at bot_elo 1500), e.g.:
       `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1500 --blends 0,0.05 --games-per-cell 8 --stockfish-procs 4 --out-dir reports/data`
    2. Inspect the emitted TSV and confirm ALL of:
       - each measured cell's rows include BOTH `maiaNNNN` and `sfN` anchor labels (both families fired);
       - anchors were bracketed on INTERNAL_RATING (the bracket sits near the cell's located rating, not clamped to the nominal bot_elo — no `any_clamped=true` on a well-chosen in-range cell, Pitfall 6);
       - the near-free metric columns (draw rate, game length, ACPL, blunder rate, SF/Maia agreement) are populated and differ between blend 0 and blend 0.05 (the presets play differently);
       - `beyond_ladder` is false for these in-range cells.
    3. Re-run the SAME command with `--resume <that-tsv>` and confirm it continues byte-identically (no replayed locate games, no gameIndex corruption — Pitfall 5).
    4. Run the fitter on the pilot TSV: `uv run python scripts/calibration_anchor_fit.py --bot-input <pilot.tsv> --out-bot-curves reports/data/bot-curves-pilot.json` and confirm it emits `rating_vs_maia`, `rating_vs_sf`, and `g_preset` per cell (sane magnitudes; Human's G_preset near 0).
  </how-to-verify>
  <resume-signal>Type "approved" if the pilot shows sane non-clamped ratings, both families, correct internal-scale windowing, and clean --resume; otherwise describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Folded-in HUMAN-UAT operator sweep + fitted curves + findings note (D-01, off critical path)</name>
  <action>Operator runs the full overnight sweep (three per-preset invocations), the extended fitter, and writes the findings note per the steps below. This is off the interactive critical path — may be deferred; the phase is already complete at Task 2.</action>
  <what-built>The validated harness + fitter. This checkpoint is the D-01 folded-in operator-run HUMAN-UAT step: the full overnight sweep, the fitted per-preset curves + G_preset, and the findings note. It is OFF the interactive critical path — the phase is already complete at the pilot; this delivers the downstream artifact when the operator chooses to run it.</what-built>
  <how-to-verify>
    1. Operator runs the full sweep as THREE per-preset invocations (D-03/D-04 per-preset non-uniform grids; they cannot be one cross-product because each preset has different bot_elo points), each appending to a resumable ledger at 24 games/(cell,anchor) baseline (bump to 30 for any cell whose bracket has fewer than 4 anchors, D-05):
       - Human:  `--blends 0    --elo 700,1100,1500,1900,2300`
       - Light:  `--blends 0.05 --elo 1100,1300,1500,1700,1900`
       - Deep:   `--blends 0.5  --elo 1100,1500,1900,2300,2600`
       (Deep's 2600 is deliberate extrapolation past sf10's 1907.93 ceiling — expect `beyond_ladder=true` there, not a bug.)
    2. Run the fitter over the combined sweep TSV: `uv run python scripts/calibration_anchor_fit.py --bot-input <sweep.tsv> --out-bot-curves reports/data/bot-curves-internal-scale.json`. Confirm the JSON carries per-cell `rating_vs_maia`/`rating_vs_sf`/`g_preset`/CIs/`beyond_ladder` + the per-preset combined `G_preset`, and the INTERNAL-SCALE-NOT-human-ELO caveat header.
    3. Write `.planning/notes/2026-07-XX-three-preset-bot-strength-curves-findings.md` (rename XX to the run date) mirroring `2026-07-15-anchor-ladder-self-calibration-findings.md`, baking in the interpretation caveats: "Deep is a ceiling, not deeper" (blend inside (0,1) is only a softmax temperature dial; the only qualitative cliff is blend 0 vs >0); read any cell leaning on sf0 or maia700 with the Finding-4 style-outlier caveat; non-transitivity means the presets' raw internal rating is search-inflated vs search-less anchors, which is why `G_preset` is SUBTRACTED (not ignored) in SEED-104's offset model.
  </how-to-verify>
  <resume-signal>Type "approved" once the sweep, bot-curves JSON, and findings note exist and read sanely; or "defer" to complete the phase at the pilot and run the sweep later (D-01 allows this — the operator step is off the critical path).</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- **Validated pilot** (D-02b): a low-N real-engine run proving both families fire, internal-scale windowing is correct (non-clamped in-range cells), and `--resume` is byte-identical.
- **Output artifact** `reports/data/bot-curves-internal-scale.json` (operator sweep): per-cell `rating_vs_maia`/`rating_vs_sf`/`g_preset`/CIs/`beyond_ladder` + per-preset combined `G_preset` — the SEED-104 interface.
- **Findings note** `.planning/notes/2026-07-XX-three-preset-bot-strength-curves-findings.md` mirroring the 173 note, with the Deep-is-a-ceiling / style-outlier / non-transitivity caveats baked in.
- No new symbols (this plan runs and interprets the Plan 01-03 machinery).
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local engine subprocesses -> harness | The pilot and sweep spawn local Stockfish/ONNX-Maia; output written to local `reports/data/`. No network, no untrusted input. |
| bot-curves JSON -> SEED-104 | The published internal-scale artifact is consumed by a future phase. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-06 | Information Disclosure | bot-curves JSON / findings note mis-read downstream as human ELO | low | mitigate | INTERNAL-SCALE-NOT-human-ELO caveat carried verbatim in the JSON header and the findings note. |
| T-180-07 | Denial of Service | a killed multi-hour operator sweep loses all progress | low | mitigate | `--resume` from the durable raw ledger (Plan 03) lets the sweep continue byte-identically. |

No high-severity threats: local operator-run calibration, no network exposure, no secrets, no persisted user data.
</threat_model>

<verification>
Task 1 gate is fully automated (all `.check.mjs` + fitter pytest green). Tasks 2-3 are blocking human-verify checkpoints (real engines / multi-hour operator run) per D-02b / D-01 — the phase completes at the pilot (Task 2); Task 3 is folded-in HUMAN-UAT and may be deferred.
</verification>

<success_criteria>
The engine-free logic layer is green; the real-engine pilot shows sane non-clamped ratings, both anchor families, correct internal-scale bracketing, and clean `--resume`. The operator sweep (when run) produces a caveated bot-curves JSON with `G_preset` and a findings note mirroring the 173 note.
</success_criteria>

<output>
Create `.planning/phases/180-three-preset-bot-strength-curves/180-04-SUMMARY.md` when done.
</output>
