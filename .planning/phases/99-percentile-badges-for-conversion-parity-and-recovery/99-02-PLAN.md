---
phase: 99-percentile-badges-for-conversion-parity-and-recovery
plan: 02
type: execute
wave: 1
depends_on: ["99-01"]
files_modified:
  - app/services/canonical_slice_sql.py
  - app/services/global_percentile_cdf.py
  - app/models/user_benchmark_percentile.py
  - alembic/versions/20260530_extend_benchmark_metric_for_rate_percentiles.py
autonomous: true
requirements: []   # standalone — coverage driven by SC-2
must_haves:
  truths:
    - "Three new pooled-per-user rate builders exist, parameterised by TC, producing (user_id, metric_value=raw rate, n_games=spans) (SC-2)"
    - "Each rate builder's pooled body is byte-identical across source='benchmark' and source='single_user' — drift structurally impossible (SC-2)"
    - "CdfMetricId widened to include conversion_rate, parity_rate, recovery_rate (3 families, NOT 12 TC-suffixed names)"
    - "The 12 TC-suffixed ENUM values exist in both the Postgres benchmark_metric type and the SAEnum descriptor (SC-2)"
  artifacts:
    - path: "app/services/canonical_slice_sql.py"
      provides: "per_user_cte_conv_rate_tc / parity_rate / recovery_rate builders + MINIMUM_RATE_BUCKET_SPANS"
      contains: "def per_user_cte_conv_rate_tc"
    - path: "alembic/versions/20260530_extend_benchmark_metric_for_rate_percentiles.py"
      provides: "ALTER TYPE benchmark_metric ADD VALUE for the 12 rate metrics"
      contains: "ADD VALUE IF NOT EXISTS"
    - path: "app/models/user_benchmark_percentile.py"
      provides: "SAEnum widened with the 12 rate values"
      contains: "conversion_rate_bullet"
    - path: "app/services/global_percentile_cdf.py"
      provides: "CdfMetricId Literal widened with 3 rate families"
      contains: "conversion_rate"
  key_links:
    - from: "app/services/global_percentile_cdf.py"
      to: "app/models/user_benchmark_percentile.py"
      via: "CdfMetricId imported as the metric column type — ty enforces SAEnum values are a superset"
      pattern: "CdfMetricId"
---

<objective>
Wave 1 backend contract layer. This plan defines every interface the rest of the phase consumes — the three rate SQL builders, the floor constant, the widened `CdfMetricId` Literal, the widened SAEnum, and the Alembic migration that extends the Postgres ENUM. Nothing downstream can be built until these contracts exist, so this is interface-first.

The drift-proof invariant (SC-2) is established here: ONE builder per rate family feeds BOTH cohort-CDF construction (Plan 05's regen) and per-user lookup (Plan 03's dispatch). The builder's pooled body must be identical regardless of `source` — the cohort difference lives only in `selected_users_cte`.

Purpose: lock the rate-builder + ENUM contracts so Plans 03/04/05 implement against them without re-deriving SQL or guessing field names.
Output: 3 new SQL builders, 1 named constant, CdfMetricId +3, SAEnum +12, 1 migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-RESEARCH.md
@.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three rate builders + MINIMUM_RATE_BUCKET_SPANS in canonical_slice_sql.py</name>
  <read_first>
    - app/services/canonical_slice_sql.py lines 76-83 (security note on tc f-string safety — TimeControlBucket is a closed 4-value Literal), lines 119-138 (constant block where MINIMUM_RATE_BUCKET_SPANS belongs), lines 726-836 (per_user_cte_score_gap_bucket_tc — the exact analog: signature, _ = source idiom, _recent_capped_per_tc_cte, spans CTE, per_user_values projection with the "user_id widened per Phase 94.4 Pitfall 1" comment at line 829)
    - app/services/endgame_service.py lines 2562-2574 (terminal-span semantics confirming game-result outcome per span — no lead() needed)
    - 99-RESEARCH.md "Pattern 1" + "Code Examples" (full builder template) + Pitfall 1 (user_id projection) + Pitfall 4/Anti-Patterns (no lead(), no TC-in-metric-name)
    - 99-PATTERNS.md "canonical_slice_sql.py" section (constant placement, signature pattern, per-family formula table)
  </read_first>
  <action>
    Add the named constant MINIMUM_RATE_BUCKET_SPANS: int = 30 to the constant block (after SCORE_GAP_BUCKET_MIN_SPANS, ~line 123). Same value, separate named constant — NOT a magic number (CLAUDE.md), and distinct from SCORE_GAP_BUCKET_MIN_SPANS so a future tuning of one does not silently move the other.
    Add three module-level builders mirroring per_user_cte_score_gap_bucket_tc's signature (tc: TimeControlBucket, *, source: Literal["benchmark","single_user"], snapshot_date: date | None = None) -> str:
      - per_user_cte_conv_rate_tc
      - per_user_cte_parity_rate_tc
      - per_user_cte_recovery_rate_tc
    Each uses the `_ = source` idiom, calls _recent_capped_per_tc_cte(snapshot_date, tc), and _user_elo_at_game_expr() for the sub-800 floor gate (>= _SUB_800_FLOOR). Structural diff from the analog: the spans CTE captures only entry_eval_cp / entry_eval_mate (array_agg ORDER BY ply ASC)[1] with HAVING count(ply) >= 6 — NO lead()/spans_with_next (rate is measured from game outcome, not ΔES delta). Then a bucket_rows CTE classifies each span into conversion/parity/recovery via the entry-eval sign convention (color-flipped to user perspective: mate-sign > 0 OR cp >= 100 → conversion; cp <= -100 → recovery; else parity) and computes is_win / is_draw flags from g.result + g.user_color. Then per_user aggregates with FILTER on the family's bucket and a HAVING count(*) FILTER (WHERE <bucket>) >= MINIMUM_RATE_BUCKET_SPANS. Then per_user_values projects (user_id, metric_value, n_games) — user_id MANDATORY (Pitfall 1; carry the "user_id widened per Phase 94.4 Pitfall 1" comment). Per-family metric_value formula:
      - conversion: conv_wins / NULLIF(conv_n, 0), n_games = conv_n
      - parity: (parity_wins + 0.5 * parity_draws) / NULLIF(parity_n, 0), n_games = parity_n
      - recovery: (recov_wins + recov_draws) / NULLIF(recov_n, 0), n_games = recov_n
    All three are higher_is_better (range 0-1) — no CDF inversion. Keep each function under the CLAUDE.md nesting/LOC limits (each is ~40 lines of f-string SQL — fine). Reference 99-RESEARCH.md "Code Examples" for the exact SQL shape; do NOT invent a parallel path.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x -q -k "rate"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def per_user_cte_conv_rate_tc\|def per_user_cte_parity_rate_tc\|def per_user_cte_recovery_rate_tc" app/services/canonical_slice_sql.py returns 3.
    - grep -c "MINIMUM_RATE_BUCKET_SPANS" app/services/canonical_slice_sql.py returns >= 4 (1 def + 3 HAVING uses).
    - grep -v '^#' app/services/canonical_slice_sql.py | grep -c "lead(" — the count for the three new builders must be 0 (verify the new functions contain no lead(); the analog may still use it).
    - The Wave-0 source-parity tests pass: `uv run pytest tests/services/test_canonical_slice_sql.py -x -q -k "source_parity"` exits 0 (proves the pooled body is source-independent — SC-2).
    - The Wave-0 floor tests pass: `uv run pytest tests/services/test_canonical_slice_sql.py -x -q -k "floor"` exits 0.
    - `uv run ty check app/services/canonical_slice_sql.py` exits 0.
  </acceptance_criteria>
  <done>The three builders exist, render source-independent pooled SQL with the ≥30-span floor and correct per-family rate formulas, project user_id, and the Wave-0 source-parity + floor tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Widen CdfMetricId + SAEnum + write the Alembic migration</name>
  <read_first>
    - app/services/global_percentile_cdf.py lines 101-110 (CdfMetricId Literal — append 3 family names, NOT TC-suffixed)
    - app/models/user_benchmark_percentile.py lines 1-31 (docstring to update: 8→11 families), lines 44 + 54-65 (SAEnum benchmark_metric_enum, create_type=False) + line 112 (metric: Mapped[CdfMetricId])
    - alembic/versions/20260524_170733_fd5b551f381c_extend_benchmark_metric_for_tc_pressure.py (FULL FILE — the canonical ADD VALUE migration to copy verbatim: _NEW_VALUES tuple, upgrade() loop, downgrade() pass)
    - 99-RESEARCH.md "Pattern 2" + "Pattern 3" + Pitfalls 2/3/4 (transaction visibility, SAEnum sync, autogenerate cannot detect ADD VALUE)
    - 99-PATTERNS.md "user_benchmark_percentile.py" + "alembic migration" + "global_percentile_cdf.py" sections (exact value ordering)
  </read_first>
  <action>
    In app/services/global_percentile_cdf.py: append "conversion_rate", "parity_rate", "recovery_rate" to the CdfMetricId Literal (3 family names — TC is an outer registry key per Phase 94.4 D-13, NEVER a metric-name suffix). Do NOT touch the COHORT_PERCENTILE_CDF registry between the BEGIN/END GENERATED REGISTRY sentinels — that is regenerated in Plan 05.
    In app/models/user_benchmark_percentile.py: append the 12 TC-suffixed string values to the benchmark_metric_enum SAEnum call, in the exact family-then-TC order (conversion_rate_bullet, _blitz, _rapid, _classical, then parity_rate_*, then recovery_rate_*). Keep create_type=False unchanged. Update the module/class docstring to reference Phase 99 and the new family count. No change needed to the metric: Mapped[CdfMetricId] column beyond the CdfMetricId widening above (ty enforces consistency).
    Create alembic/versions/20260530_extend_benchmark_metric_for_rate_percentiles.py by copying the fd5b551f381c structure verbatim: set down_revision = "c70f5d94b243" (current head, verified 2026-05-30), generate a fresh revision id, set _NEW_VALUES to the 12 rate values in the SAME order as the SAEnum, upgrade() loops op.execute(f"ALTER TYPE benchmark_metric ADD VALUE IF NOT EXISTS '{value}'"), downgrade() is pass with the "Postgres cannot remove ENUM values — git revert instead" comment. The migration writes NO data rows (Pitfall 2 — ENUM value visibility is deferred to a new transaction; backfill runs in Plan 05 as a separate process). Do NOT run --autogenerate (Pitfall 4 — it cannot detect ADD VALUE).
  </action>
  <verify>
    <automated>uv run pytest tests/models/test_user_benchmark_percentile.py -x -q && uv run alembic upgrade head && uv run alembic check</automated>
  </verify>
  <acceptance_criteria>
    - The Wave-0 ENUM membership test passes: `uv run pytest tests/models/test_user_benchmark_percentile.py -x -q` exits 0 (all 12 members present in the SAEnum).
    - grep -c "conversion_rate\|parity_rate\|recovery_rate" on the CdfMetricId block of app/services/global_percentile_cdf.py confirms exactly 3 family names added (no TC suffixes in CdfMetricId).
    - File alembic/versions/20260530_extend_benchmark_metric_for_rate_percentiles.py exists, contains down_revision = "c70f5d94b243", and grep -c "ADD VALUE IF NOT EXISTS" returns 12 (or 1 if rendered in a loop — confirm the _NEW_VALUES tuple has 12 entries via grep -c "_rate_" >= 12).
    - `uv run alembic upgrade head` applies cleanly against the dev DB (Docker on 5432 must be up); `uv run alembic check` reports no pending model/migration drift for the ENUM.
    - `uv run ty check app/ tests/` exits 0 (CdfMetricId widening + SAEnum stay consistent).
  </acceptance_criteria>
  <done>CdfMetricId has the 3 new families, the SAEnum has the 12 values, the migration applies on dev and the Wave-0 ENUM test is green; ty check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (existing) | The `tc` value reaches SQL builders via the request path; it is the only externally-influenced input near the new f-string SQL. |
| backfill/regen → DB | Migration + future backfill write to user_benchmark_percentiles / the ENUM type. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-SQLi | Tampering | per_user_cte_*_rate_tc f-string SQL | mitigate | `tc: TimeControlBucket` is a closed 4-value Literal; ty enforces the closed set at every call site; no user string is interpolated. Identical to all existing builders (canonical_slice_sql.py lines 76-83). |
| T-99-ENUM | Tampering | Alembic ENUM migration | accept | Migration only ADDs values, writes no data rows; ADD VALUE IF NOT EXISTS is idempotent; downgrade is a no-op (git revert is the rollback path). No HIGH severity. |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit skipped). No install task → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/models/test_user_benchmark_percentile.py -x -q` green (source-parity + floor + ENUM membership).
- `uv run alembic upgrade head && uv run alembic check` clean on dev DB.
- `uv run ruff format app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/` all clean.
</verification>

<success_criteria>
SC-2 partially satisfied: the shared builder + drift-proof source parity + the 12 ENUM members + the widened CdfMetricId all exist and are test-covered. The contracts for Plans 03/04/05 are locked.
</success_criteria>

<output>
Create `.planning/phases/99-percentile-badges-for-conversion-parity-and-recovery/99-02-SUMMARY.md` — record the new migration revision id (downstream plans reference it), the builder signatures, and the final CdfMetricId / SAEnum value list.
</output>
