---
phase: 91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/hooks/useEvalCoverage.ts
  - frontend/src/components/EvalCoverageHeader.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/openings/StatsTab.tsx
  - frontend/src/hooks/__tests__/useEvalCoverage.test.tsx
  - frontend/src/components/__tests__/EvalCoverageHeader.test.tsx
autonomous: true
requirements:
  - "Phase 91 Scope #4 (frontend header bar — Endgames + Openings/Stats only)"
  - "CONTEXT.md D-02 (page-level header, not global)"
  - "CONTEXT.md D-03 (10s staleTime + 10s refetchInterval; stops at 100%)"
  - "CONTEXT.md D-04 (exact copy: `<Cpu /> Stockfish analysis: N% complete (M games pending)`, plural-aware, Cpu h-3.5 w-3.5)"
  - "CONTEXT.md D-05 (centralised `useEvalCoverage` hook returning `{pendingCount, totalCount, pct, isPending}`)"

tags:
  - frontend
  - tanstack-query
  - header-bar
  - eval-coverage

must_haves:
  truths:
    - "`useEvalCoverage` hook polls `GET /imports/eval-coverage` every 10 s while `pct_complete < 100`."
    - "When `pct_complete === 100`, `refetchInterval` returns `false` (polling stops)."
    - "TanStack queryKey is `['imports', 'eval-coverage']` — exactly one HTTP call per page regardless of how many consumers."
    - "Hook default values when loading: `pct: 100`, `isPending: false` (prevents caveat flashing pre-fetch)."
    - "`EvalCoverageHeader` renders `<div role=\"status\" data-testid=\"eval-coverage-header\">` with `<Cpu h-3.5 w-3.5 />` + copy `Stockfish analysis: {pct}% complete ({pendingCount.toLocaleString()} {games|game} pending)`."
    - "Header returns `null` when `isPending === false` (hidden at 100%)."
    - "Header is mounted on Endgames.tsx AND Openings/StatsTab.tsx; NOT in global topbar, NOT on Import page."
    - "Plural-aware: `pendingCount === 1` → `game`, else → `games`."
    - "Magic numbers extracted: `EVAL_COVERAGE_POLL_INTERVAL_MS = 10_000`, `EVAL_COVERAGE_STALE_TIME_MS = 10_000`."
    - "Locked decisions honoured: D-02 (page-level header mounted on Endgames + Openings/Stats only — NOT global topbar, NOT Import page), D-03 (10s staleTime + 10s refetchInterval; polling stops at 100%), D-04 (exact copy `<Cpu /> Stockfish analysis: N% complete (M games pending)`, plural-aware, Cpu icon h-3.5 w-3.5), D-05 (centralised `useEvalCoverage` hook with shared queryKey — one HTTP call per page regardless of consumer count)."
  artifacts:
    - path: "frontend/src/hooks/useEvalCoverage.ts"
      provides: "TanStack Query hook"
      min_lines: 25
    - path: "frontend/src/components/EvalCoverageHeader.tsx"
      provides: "Page-level header bar"
      min_lines: 15
    - path: "frontend/src/types/api.ts"
      provides: "EvalCoverageResponse type"
      contains: "EvalCoverageResponse"
  key_links:
    - from: "EvalCoverageHeader"
      to: "useEvalCoverage"
      via: "hook call inside component"
      pattern: "useEvalCoverage()"
    - from: "Endgames.tsx + StatsTab.tsx"
      to: "EvalCoverageHeader"
      via: "JSX mount at top of page render"
      pattern: "<EvalCoverageHeader"
---

<objective>
Build the frontend data plumbing for Phase 91: the shared `useEvalCoverage` TanStack Query hook, the `EvalCoverageHeader` component, and its mount points on the two pages where Stockfish-dependent metrics live.

Purpose: Without this, the cold drain runs invisibly. The user must see Stockfish analysis progress while it advances and have the indicator vanish at 100%.

Output: New types entry, new hook, new component, two page mounts, and RTL tests for the hook (poll-stop logic) and the component (hidden-when-zero render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-CONTEXT.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-RESEARCH.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md
@CLAUDE.md
@frontend/src/types/api.ts
@frontend/src/hooks/useImport.ts
@frontend/src/components/charts/PositionResultsPanel.tsx
@frontend/src/pages/Endgames.tsx

<interfaces>
<!-- TanStack Query pattern from useImport.ts -->
- `useQuery<T>({ queryKey, queryFn, staleTime, refetchInterval })` — refetchInterval can be a `(query) => number | false` callback to stop polling conditionally
- `apiClient` from `@/api/client` — wraps axios/fetch with auth

<!-- Cpu icon convention -->
- `import { Cpu } from 'lucide-react'`
- Existing usages: `frontend/src/components/charts/PositionResultsPanel.tsx:198`, `frontend/src/components/insights/OpeningFindingCard.tsx:139`, `frontend/src/components/charts/EndgameOverallEntryCard.tsx:79/136`, `frontend/src/components/charts/EndgameMetricCard.tsx:207`, `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx:166`, `frontend/src/components/charts/EndgameTypeCard.tsx:445`, `frontend/src/components/stats/OpeningStatsCard.tsx:118` — all use `className="h-3.5 w-3.5"` exactly
- Tailwind class for primary copy: `text-sm` (CLAUDE.md minimum font-size rule)

<!-- Mount targets -->
- `frontend/src/pages/Endgames.tsx` — insert below page title, above the tab bar
- `frontend/src/pages/openings/StatsTab.tsx` — insert at top of StatsTab render, before section headings
  (Verify the actual filename — could be `OpeningsStatsTab.tsx` or under `pages/openings/`. Use `find frontend/src/pages -type f -iname '*stats*'` to confirm.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 6.1: Add EvalCoverageResponse type + useEvalCoverage hook + hook test</name>
  <files>frontend/src/types/api.ts, frontend/src/hooks/useEvalCoverage.ts, frontend/src/hooks/__tests__/useEvalCoverage.test.tsx</files>
  <read_first>
    - frontend/src/types/api.ts (full file — locate the Imports section near lines 162-188, append `EvalCoverageResponse` interface at end of that block)
    - frontend/src/hooks/useImport.ts (full file — replicate `useImportPolling` shape for `useEvalCoverage`)
    - frontend/src/api/client.ts or wherever `apiClient` lives (confirm import path and method signatures)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (sections "frontend/src/types/api.ts" + "frontend/src/hooks/useEvalCoverage.ts")
    - CLAUDE.md (Frontend → Code Style & Safety: `noUncheckedIndexedAccess`, no magic numbers)
  </read_first>
  <behavior>
    Types: Add `export interface EvalCoverageResponse { pending_count: number; total_count: number; pct_complete: number; }` to the Imports section in `frontend/src/types/api.ts`.
    Hook: Create `frontend/src/hooks/useEvalCoverage.ts` exporting `useEvalCoverage()`. Internals:
    - Constants at top: `const EVAL_COVERAGE_POLL_INTERVAL_MS = 10_000;` and `const EVAL_COVERAGE_STALE_TIME_MS = 10_000;`.
    - `useQuery<EvalCoverageResponse>` with `queryKey: ['imports', 'eval-coverage']`, `queryFn` calling `apiClient.get<EvalCoverageResponse>('/imports/eval-coverage').then(r => r.data)`, `staleTime: EVAL_COVERAGE_STALE_TIME_MS`, `refetchInterval: (query) => query.state.data?.pct_complete === 100 ? false : EVAL_COVERAGE_POLL_INTERVAL_MS`.
    - Return `{ pendingCount: data?.pending_count ?? 0, totalCount: data?.total_count ?? 0, pct: data?.pct_complete ?? 100, isPending: (data?.pct_complete ?? 100) < 100, isLoading: query.isLoading }`.
    Test: Create `frontend/src/hooks/__tests__/useEvalCoverage.test.tsx`:
    - `test_polls_at_interval_when_pending` — set up MSW or mock `apiClient.get` to return `{pending_count: 5, total_count: 10, pct_complete: 50}`; render hook in `QueryClientProvider`; advance fake timers by 10 s twice; assert `apiClient.get` was called >= 3 times (initial + 2 polls).
    - `test_stops_polling_at_complete` — mock to return `{pending_count: 0, total_count: 10, pct_complete: 100}`; render; advance timers by 30 s; assert `apiClient.get` was called exactly once (no polls after).
    - `test_default_values_pre_load` — render hook before first fetch resolves; assert `result.current.pct === 100 && result.current.isPending === false` (prevents caveat flash).
  </behavior>
  <action>
    Step A — types: Open `frontend/src/types/api.ts`. Find the Imports section header `// ─── Imports ───`. Append at end of section: `export interface EvalCoverageResponse { pending_count: number; total_count: number; pct_complete: number; }` with a one-line trailing comment `// 0–100, rounded` on the `pct_complete` line.
    Step B — hook: Create `frontend/src/hooks/useEvalCoverage.ts`. Match the formatting + import style of `useImport.ts`. The two `_MS` constants must be defined as top-level `const` (not inside the function body). Use `useQuery` from `@tanstack/react-query`. Return the destructured shape per `behavior` above. No `Sentry.captureException` in the hook (global handler covers it per CLAUDE.md).
    Step C — hook test: Create `frontend/src/hooks/__tests__/useEvalCoverage.test.tsx`. Use Vitest + React Testing Library + `@tanstack/react-query`'s `QueryClient` wrapper. For timer control, use `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()`. Mock `apiClient.get` via `vi.mock('@/api/client', ...)` OR via MSW handler if that's the project's convention (read sibling test files in `frontend/src/hooks/__tests__/` to determine). Wrap the renderHook call with a fresh `QueryClientProvider` per test (do NOT share QueryClient across tests — stale cache would break the second test).
    Run `npm run lint --prefix frontend` and `npm test --prefix frontend -- --run src/hooks/__tests__/useEvalCoverage.test.tsx` — both must pass.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run lint</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm test -- --run src/hooks/__tests__/useEvalCoverage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export interface EvalCoverageResponse" frontend/src/types/api.ts` returns 1.
    - `grep -c "^export function useEvalCoverage" frontend/src/hooks/useEvalCoverage.ts` returns 1.
    - `grep -c "EVAL_COVERAGE_POLL_INTERVAL_MS\|EVAL_COVERAGE_STALE_TIME_MS" frontend/src/hooks/useEvalCoverage.ts` returns >= 4 (two declarations + two usages).
    - `grep -E "queryKey: \[\s*'imports',\s*'eval-coverage'\s*\]" frontend/src/hooks/useEvalCoverage.ts` matches.
    - Hook file contains the literal `pct_complete === 100 ? false : EVAL_COVERAGE_POLL_INTERVAL_MS` (or equivalent expression).
    - Hook file contains NO numeric literal `10000` or `10_000` outside the two named constants.
    - `cd frontend && npm test -- --run src/hooks/__tests__/useEvalCoverage.test.tsx` exits 0.
    - `cd frontend && npm run lint` exits 0.
  </acceptance_criteria>
  <done>Type + hook + hook tests all green; polling-stop behaviour verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.2: Build EvalCoverageHeader component + component test</name>
  <files>frontend/src/components/EvalCoverageHeader.tsx, frontend/src/components/__tests__/EvalCoverageHeader.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useEvalCoverage.ts (final state from Task 6.1)
    - frontend/src/components/charts/PositionResultsPanel.tsx (Cpu icon usage at line ~198 — match h-3.5 w-3.5 exactly)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (section "frontend/src/components/EvalCoverageHeader.tsx" — full component body)
    - CLAUDE.md (Frontend → UI & Components → `text-sm` minimum, `data-testid` on every interactive element, semantic HTML, plural-aware copy)
  </read_first>
  <behavior>
    Create `frontend/src/components/EvalCoverageHeader.tsx`:
    - `import { Cpu } from 'lucide-react'; import { useEvalCoverage } from '@/hooks/useEvalCoverage';`
    - `export function EvalCoverageHeader()`:
      - `const { pendingCount, pct, isPending } = useEvalCoverage();`
      - `if (!isPending) return null;`
      - `const gamesLabel = pendingCount === 1 ? 'game' : 'games';`
      - return `<div role="status" data-testid="eval-coverage-header" className="flex items-center gap-1.5 text-sm text-muted-foreground mb-3"><Cpu className="h-3.5 w-3.5" aria-hidden="true" />Stockfish analysis: {pct}% complete ({pendingCount.toLocaleString()} {gamesLabel} pending)</div>`
    Test: Create `frontend/src/components/__tests__/EvalCoverageHeader.test.tsx`:
    - `test_hidden_when_not_pending` — mock `useEvalCoverage` to return `{isPending: false, pendingCount: 0, pct: 100}`; render; assert `queryByTestId('eval-coverage-header')` is null.
    - `test_renders_with_plural_label` — mock to return `{isPending: true, pendingCount: 1432, pct: 87}`; render; assert text contains `"Stockfish analysis: 87% complete (1,432 games pending)"` (comma-separated, plural).
    - `test_renders_with_singular_label` — mock to return `{isPending: true, pendingCount: 1, pct: 99}`; render; assert text contains `"(1 game pending)"` (singular).
    - `test_has_status_role_and_testid` — mock to return pending state; render; assert the root element has `role="status"` AND `data-testid="eval-coverage-header"`.
  </behavior>
  <action>
    Create the component. Use the exact copy from D-04. The `Cpu` icon MUST use `className="h-3.5 w-3.5" aria-hidden="true"` (matches existing convention, accessible). Component uses `text-sm` (CLAUDE.md primary content). Use `text-muted-foreground` (existing Tailwind token in this codebase — confirm via grep against other Cpu+text combinations like in `PositionResultsPanel.tsx`).
    Create the test. Use `vi.mock('@/hooks/useEvalCoverage', () => ({ useEvalCoverage: vi.fn() }))` and `(useEvalCoverage as Mock).mockReturnValue(...)` per test. RTL `render(<EvalCoverageHeader />)` + `screen.queryByTestId(...)` / `screen.getByText(...)`. No `QueryClientProvider` needed (hook is mocked).
    Run lint + the new test file. Optionally `npm run knip` to confirm the new exports are referenced (will be by Task 6.3 mounts).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run lint</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm test -- --run src/components/__tests__/EvalCoverageHeader.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function EvalCoverageHeader" frontend/src/components/EvalCoverageHeader.tsx` returns 1.
    - `grep -c 'role="status"' frontend/src/components/EvalCoverageHeader.tsx` returns 1.
    - `grep -c 'data-testid="eval-coverage-header"' frontend/src/components/EvalCoverageHeader.tsx` returns 1.
    - `grep -c 'className="h-3.5 w-3.5"' frontend/src/components/EvalCoverageHeader.tsx` returns 1.
    - `grep -c "text-sm" frontend/src/components/EvalCoverageHeader.tsx` returns 1 (and no `text-xs` in this file).
    - Component contains the literal substring `Stockfish analysis:`.
    - Component contains plural-aware logic referencing `=== 1`.
    - `cd frontend && npm test -- --run src/components/__tests__/EvalCoverageHeader.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Component renders correctly in all four states (hidden/plural/singular/role-testid), all RTL tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.3: Mount EvalCoverageHeader on Endgames page and Openings Stats subtab</name>
  <files>frontend/src/pages/Endgames.tsx, frontend/src/pages/openings/StatsTab.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx (full file — locate the page-level heading area and the tab bar; insert `<EvalCoverageHeader />` between them)
    - Resolve actual Openings Stats subtab file path: `find frontend/src/pages -type f -iname '*stats*' -o -iname '*opening*'` to identify the correct mount file (could be `pages/openings/StatsTab.tsx`, `pages/Openings/StatsTab.tsx`, or `pages/openings/tabs/StatsTab.tsx`)
    - frontend/src/components/EvalCoverageHeader.tsx (final state from Task 6.2)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (Mount Points section)
    - CLAUDE.md (Frontend → "Always apply changes to mobile too" — if either page has a separate mobile branch, mount in both)
  </read_first>
  <action>
    Step A: In `frontend/src/pages/Endgames.tsx`: Add `import { EvalCoverageHeader } from '@/components/EvalCoverageHeader';` to the imports. Locate the JSX return — find the page-level heading region (e.g. `<h1>` or page title block) and the tab bar (`<Tabs>` or equivalent). Insert `<EvalCoverageHeader />` between them. If the file has separate desktop and mobile layout sections (check for `lg:` / `md:` Tailwind breakpoint branches or explicit `<DesktopLayout>` / `<MobileLayout>` components), mount in BOTH branches per CLAUDE.md frontend rule. The component is self-managing (calls the hook internally, hides at 100%), so no props are needed at mount sites.
    Step B: Identify the Openings Stats subtab file (verify path via `find` command above; do NOT guess). Open it and apply the same mount pattern: insert `<EvalCoverageHeader />` at the top of the subtab's JSX return, before any section headings. Add the import. If a separate mobile layout exists in this file, mount in both.
    Step C: Run `cd frontend && npm run lint` and confirm zero errors. Run `cd frontend && npm run build` (or `npm run dev` briefly + visual check) to confirm the imports resolve and the build succeeds.
    Step D: Run `cd frontend && npm run knip` to confirm `EvalCoverageHeader` is now referenced (Task 6.2 export goes from orphan to used).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run lint</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run build</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run knip 2&gt;&amp;1 | (grep -i "EvalCoverageHeader" || echo "knip clean")</automated>
    <automated>grep -c "EvalCoverageHeader" frontend/src/pages/Endgames.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import.*EvalCoverageHeader" frontend/src/pages/Endgames.tsx` returns 1.
    - `grep -c "<EvalCoverageHeader" frontend/src/pages/Endgames.tsx` returns >= 1 (1 if no mobile-split, 2 if mobile-split).
    - The Openings Stats subtab file (whichever path resolves) contains `<EvalCoverageHeader` at least once.
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npm run knip` does NOT report `EvalCoverageHeader` as an unused export.
    - NO mount of `EvalCoverageHeader` on the Import page (`Import.tsx`) — per D-02 (verify: `grep -c EvalCoverageHeader frontend/src/pages/Import.tsx` returns 0).
    - NO mount in the global topbar/layout (`AppLayout.tsx`, `Navbar.tsx`, or whatever the equivalent is — search for the topbar file and confirm).
  </acceptance_criteria>
  <done>Header is mounted on Endgames + Openings/Stats only, knip clean, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | TanStack Query authenticated request via `apiClient`; 401 on missing token |
| Frontend → DOM | React renders sanitised text; `pendingCount` is a number, `toLocaleString` is safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-22 | Denial of Service | Polling continues forever if user leaves the page open | mitigate | `refetchInterval` stops at `pct_complete === 100`. Endpoint COUNT queries are fast (T-91-16 disposition). |
| T-91-23 | Information Disclosure | Header reveals user's data state to onlookers | accept | Same risk surface as any other authenticated UI element. No remediation needed. |
| T-91-24 | Tampering | Hook called outside QueryClientProvider | accept | TanStack Query raises a clear runtime error in that case. Provider is set up at app root; this is not a realistic failure mode. |
</threat_model>

<verification>
- `cd frontend && npm run lint` passes.
- `cd frontend && npm run build` passes.
- `cd frontend && npm test -- --run src/hooks/__tests__/useEvalCoverage.test.tsx src/components/__tests__/EvalCoverageHeader.test.tsx` passes.
- `cd frontend && npm run knip` does not flag any new orphan exports.
- Manual smoke: `npm run dev`, log in as a user with pending evals, visit Endgames + Openings/Stats; header bar visible; visit Bookmarks/Import/Library; header bar absent.
</verification>

<success_criteria>
Plan complete when:
- `EvalCoverageResponse` type, `useEvalCoverage` hook, `EvalCoverageHeader` component exist.
- Hook polls every 10 s while pending, stops at 100%.
- Component renders with `role="status"`, `data-testid="eval-coverage-header"`, plural-aware copy, exact `Cpu h-3.5 w-3.5` icon.
- Component is mounted on Endgames + Openings/Stats only (not Import, not global).
- RTL tests cover hook poll-stop logic and component hidden/plural/singular states.
- Frontend build, lint, knip all clean.
</success_criteria>

<output>
Create `.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-06-SUMMARY.md` when done. Record: exact mount sites (filename + line numbers), test file paths, any mobile-branch duplication encountered.
</output>
