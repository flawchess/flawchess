---
phase: 91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai
plan: 07
type: execute
wave: 2
depends_on:
  - 91-06
files_modified:
  - frontend/src/components/insights/EvalConfidenceTooltip.tsx
  - frontend/src/components/popovers/MetricStatTooltip.tsx
  - frontend/src/components/popovers/MetricStatPopover.tsx
  - frontend/src/components/insights/BulletConfidencePopover.tsx
  - frontend/src/components/charts/PositionResultsPanel.tsx
  - frontend/src/components/insights/OpeningFindingCard.tsx
  - frontend/src/components/charts/EndgameOverallEntryCard.tsx
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/components/charts/EndgameTypeCard.tsx
  - frontend/src/components/stats/OpeningStatsCard.tsx
  - frontend/src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx
  - frontend/src/components/popovers/__tests__/MetricStatTooltip.test.tsx
autonomous: true
requirements:
  - "Phase 91 Scope #5 (per-metric pending caveat in popover bodies)"
  - "CONTEXT.md D-05 (centralised useEvalCoverage hook — one HTTP call per page; same queryKey shared)"
  - "CONTEXT.md D-06 (one-line addendum in existing popover bodies; NO new popover family)"
  - "CONTEXT.md D-07 (touch sites: EvalConfidenceTooltip, BulletConfidencePopover, PositionResultsPanel, OpeningFindingCard, EndgameOverallEntryCard, EndgameOverallPerformanceSection, EndgameMetricCard, EndgameTypeCard, OpeningStatsCard — EXCLUDE EndgameTimePressureCard per RESEARCH OQ-1)"

tags:
  - frontend
  - popover
  - eval-caveat

must_haves:
  truths:
    - "When `useEvalCoverage().isPending === true && pendingCount > 0`, every Cpu-iconed metric popover body shows a one-line caveat: `Based on currently-evaluated games. {pendingCount.toLocaleString()} more being analysed — refresh in a few minutes for updated values.`"
    - "When `isPending === false`, the caveat `<p>` is absent (not just hidden via CSS — removed from the DOM)."
    - "EndgameTimePressureCard.tsx is NOT modified — Clock Gap is not Stockfish-dependent (RESEARCH OQ-1; imports `Swords`, not `Cpu`)."
    - "All caveat-bearing consumers call `useEvalCoverage()` — TanStack Query dedupes by `['imports', 'eval-coverage']` queryKey so HTTP call count is unchanged (1 per page)."
    - "Caveat copy uses `text-xs` only inside popover bodies (CLAUDE.md popover exception); no `text-xs` introduced outside popovers."
    - "Caveat copy uses `opacity-70` matching the existing methodology-footer styling in EvalConfidenceTooltip."
    - "MetricStatTooltip + EvalConfidenceTooltip accept optional `isPending?: boolean` and `pendingCount?: number` props (backwards-compatible defaults)."
    - "Locked decisions honoured: D-05 (consumers share the centralised useEvalCoverage hook + queryKey), D-06 (caveat is a one-line addendum inside existing popover bodies — no new popover component family), D-07 (touch sites enumerated; EndgameTimePressureCard explicitly excluded per RESEARCH OQ-1)."
  artifacts:
    - path: "frontend/src/components/insights/EvalConfidenceTooltip.tsx"
      provides: "Caveat-aware tooltip body"
      contains: "isPending"
    - path: "frontend/src/components/popovers/MetricStatTooltip.tsx"
      provides: "Caveat-aware metric tooltip body"
      contains: "isPending"
  key_links:
    - from: "Every Cpu-bearing component"
      to: "useEvalCoverage"
      via: "hook call + isPending/pendingCount prop threading"
      pattern: "useEvalCoverage()"
---

<objective>
Inject the per-metric pending caveat into the existing popover bodies on every Stockfish-dependent surface that displays a `<Cpu />` icon.

Purpose: With the page-level header bar (Plan 91-06) the user knows analysis is in progress; but inside each metric popover, the body talks about a specific value computed from current data. The caveat tells the user "this value will change as more games get analysed" — honest sample-size labeling that matches FlawChess's "engines are flawless, humans play FlawChess" framing.

Output: Updated tooltip/popover types, conditional `<p>` injection in two tooltip body components, prop threading through every Cpu-bearing parent, and RTL tests proving caveat appears iff `isPending`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-CONTEXT.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-RESEARCH.md
@.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md
@CLAUDE.md
@frontend/src/components/insights/EvalConfidenceTooltip.tsx
@frontend/src/components/popovers/MetricStatTooltip.tsx
@frontend/src/components/popovers/MetricStatPopover.tsx
@frontend/src/components/insights/BulletConfidencePopover.tsx
@frontend/src/hooks/useEvalCoverage.ts

<interfaces>
<!-- The 8 Cpu-bearing components confirmed via grep -->
- frontend/src/components/charts/EndgameMetricCard.tsx:207
- frontend/src/components/charts/PositionResultsPanel.tsx:198
- frontend/src/components/charts/EndgameOverallEntryCard.tsx:79, 136 (two Cpu sites)
- frontend/src/components/insights/OpeningFindingCard.tsx:139
- frontend/src/components/charts/EndgameOverallPerformanceSection.tsx:166
- frontend/src/components/stats/OpeningStatsCard.tsx:118
- frontend/src/components/charts/EndgameTypeCard.tsx:445

<!-- Two tooltip body components that receive the caveat -->
- frontend/src/components/insights/EvalConfidenceTooltip.tsx (props ~lines 38-54, body ~97-105 with existing italic footer)
- frontend/src/components/popovers/MetricStatTooltip.tsx (props ~53-92, body footer ~218)

<!-- Popover wrappers that forward props to tooltips -->
- frontend/src/components/popovers/MetricStatPopover.tsx (forwards via spread to MetricStatTooltip)
- frontend/src/components/insights/BulletConfidencePopover.tsx (forwards to EvalConfidenceTooltip)

<!-- The hook (from Plan 91-06) -->
- `useEvalCoverage()` returns `{ pendingCount, totalCount, pct, isPending, isLoading }`
- Default values pre-fetch: `pct: 100`, `isPending: false` (caveat will not flash before load resolves)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 7.1: Add isPending/pendingCount props to EvalConfidenceTooltip + MetricStatTooltip + their wrappers</name>
  <files>frontend/src/components/insights/EvalConfidenceTooltip.tsx, frontend/src/components/popovers/MetricStatTooltip.tsx, frontend/src/components/popovers/MetricStatPopover.tsx, frontend/src/components/insights/BulletConfidencePopover.tsx</files>
  <read_first>
    - frontend/src/components/insights/EvalConfidenceTooltip.tsx (full file — locate `EvalConfidenceTooltipProps` interface and the methodology footer `<p>`)
    - frontend/src/components/popovers/MetricStatTooltip.tsx (full file — locate props interface and the methodology `<p>`)
    - frontend/src/components/popovers/MetricStatPopover.tsx (full file — locate prop forwarding to MetricStatTooltip; usually via `{...tooltipProps}` spread)
    - frontend/src/components/insights/BulletConfidencePopover.tsx (full file — locate prop forwarding to EvalConfidenceTooltip)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (sections for these four components — verbatim modification patterns)
    - CLAUDE.md (Frontend → text-sm minimum + popover exception for text-xs)
  </read_first>
  <behavior>
    For each of the four files:
    - Add optional props `isPending?: boolean` and `pendingCount?: number` to the relevant props interface.
    - For wrapper components (`MetricStatPopover`, `BulletConfidencePopover`): forward the new props through to the inner tooltip component. If the wrapper already uses a `{...tooltipProps}` spread to the tooltip, the new props naturally pass through and the only change needed is adding them to the wrapper's TypeScript prop interface (no behavior change in render). If the wrapper passes props individually, add explicit forwarding.
    - For body components (`EvalConfidenceTooltip`, `MetricStatTooltip`): consume the new props and render a conditional `<p>` (added in Task 7.2). For this task, only update the props interface to accept them and destructure them in the function signature (do NOT yet render the `<p>` — that's the next task).
  </behavior>
  <action>
    Step A — EvalConfidenceTooltip.tsx: Locate `interface EvalConfidenceTooltipProps`. Add two optional fields: `isPending?: boolean;` and `pendingCount?: number;`. In the function signature, destructure them with default values: `({ ..., isPending = false, pendingCount = 0 })`. (Defaults ensure that legacy callers that don't pass these props get no caveat — backwards-compatible.)
    Step B — MetricStatTooltip.tsx: Same as Step A applied to `MetricStatTooltipProps`.
    Step C — MetricStatPopover.tsx: Add `isPending?: boolean;` and `pendingCount?: number;` to `MetricStatPopoverProps`. Forward to `<MetricStatTooltip>` — if spread `{...tooltipProps}` is already in place, just add the fields to the props passed in (or extend the type definition to include them in `tooltipProps`). If forwarding is explicit, add two explicit `isPending={isPending} pendingCount={pendingCount}` props on the MetricStatTooltip JSX element.
    Step D — BulletConfidencePopover.tsx: Same as Step C applied to `BulletConfidencePopoverProps` and `<EvalConfidenceTooltip>`.
    Run `cd frontend && npm run lint` and ensure zero errors. Run `cd frontend && npm run build` to catch TypeScript errors.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run lint</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run build</automated>
    <automated>grep -c "isPending" frontend/src/components/insights/EvalConfidenceTooltip.tsx frontend/src/components/popovers/MetricStatTooltip.tsx frontend/src/components/popovers/MetricStatPopover.tsx frontend/src/components/insights/BulletConfidencePopover.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isPending" frontend/src/components/insights/EvalConfidenceTooltip.tsx` returns >= 2 (interface + destructure).
    - `grep -c "isPending" frontend/src/components/popovers/MetricStatTooltip.tsx` returns >= 2.
    - `grep -c "isPending" frontend/src/components/popovers/MetricStatPopover.tsx` returns >= 1.
    - `grep -c "isPending" frontend/src/components/insights/BulletConfidencePopover.tsx` returns >= 1.
    - `grep -c "pendingCount" frontend/src/components/insights/EvalConfidenceTooltip.tsx` returns >= 2.
    - All four files compile (build passes).
    - No existing tests broken: `cd frontend && npm test -- --run src/components/insights src/components/popovers` exits 0.
  </acceptance_criteria>
  <done>Props plumbing through tooltips + popover wrappers is in place; build green; no behavior change yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7.2: Inject conditional caveat <p> into EvalConfidenceTooltip + MetricStatTooltip bodies; add caveat tests</name>
  <files>frontend/src/components/insights/EvalConfidenceTooltip.tsx, frontend/src/components/popovers/MetricStatTooltip.tsx, frontend/src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx, frontend/src/components/popovers/__tests__/MetricStatTooltip.test.tsx</files>
  <read_first>
    - frontend/src/components/insights/EvalConfidenceTooltip.tsx (current state after Task 7.1 — locate methodology `<p>` line ~97-105)
    - frontend/src/components/popovers/MetricStatTooltip.tsx (current state after Task 7.1 — locate methodology line ~218)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (Shared Patterns → "Popover body pending caveat <p>")
    - Existing test patterns in frontend/src/components/insights/__tests__/ or frontend/src/components/popovers/__tests__/ (to mirror conventions)
    - CLAUDE.md (popover exception for text-xs; popover-copy-minimalism feedback in user context)
  </read_first>
  <behavior>
    Inside each tooltip body component, AFTER the existing methodology `<p>` footer (which is currently the last child of the body container, e.g. `<div className="text-left space-y-1">`), inject:
    ```
    {isPending === true && (pendingCount ?? 0) > 0 && (
      <p className="opacity-70">
        Based on currently-evaluated games. {(pendingCount ?? 0).toLocaleString()} more being analysed — refresh in a few minutes for updated values.
      </p>
    )}
    ```
    Note: copy contains a single em-dash. Per the user's communication-style memory, em-dashes are used sparingly; this is one per paragraph and it is the canonical phrasing locked by D-06 (do not paraphrase). Caveat copy is identical in both tooltip files.
    Tests for `EvalConfidenceTooltip`:
    - `test_caveat_shown_when_pending` — render with `isPending={true} pendingCount={1432}` plus minimal required props (whatever the tooltip needs to render — copy from an existing test in the file if present; otherwise construct minimal props); assert `screen.getByText(/Based on currently-evaluated games/).textContent` includes `1,432 more being analysed`.
    - `test_caveat_absent_when_not_pending` — render with `isPending={false} pendingCount={0}`; assert `screen.queryByText(/Based on currently-evaluated games/)` is null.
    - `test_caveat_absent_when_pending_count_zero` — render with `isPending={true} pendingCount={0}`; assert caveat absent (defensive: even if isPending is true, never show caveat with zero count).
    Same three tests for `MetricStatTooltip`.
  </behavior>
  <action>
    Step A — EvalConfidenceTooltip.tsx: Locate the existing methodology `<p className="opacity-70 italic">` (lines ~97-105). Immediately after its closing `</p>`, inside the same parent `<div>`, insert the conditional `<p>` exactly as specified in `behavior`. The conditional uses `=== true` (not truthy check) to be explicit about the optional prop's tri-state (`true | false | undefined`).
    Step B — MetricStatTooltip.tsx: Locate the existing methodology `<p>` (line ~218). Insert the same conditional `<p>` immediately after.
    Step C — Tests: Create `frontend/src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx` if it doesn't exist, or extend the existing one. Same for `MetricStatTooltip.test.tsx`. Use Vitest + RTL. Mock the minimum-needed props per existing test patterns in adjacent files. Use `screen.getByText(/regex/)` for caveat presence and `screen.queryByText(/regex/)` for absence.
    Step D: Run `npm run lint`, `npm run build`, and the two new test files. All must pass.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run lint</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run build</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm test -- --run src/components/insights/__tests__/EvalConfidenceTooltip.test.tsx src/components/popovers/__tests__/MetricStatTooltip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Based on currently-evaluated games" frontend/src/components/insights/EvalConfidenceTooltip.tsx` returns 1.
    - `grep -c "Based on currently-evaluated games" frontend/src/components/popovers/MetricStatTooltip.tsx` returns 1.
    - Both files use `(pendingCount ?? 0).toLocaleString()` — verify via grep `toLocaleString`.
    - Test files exist and contain `test_caveat_shown_when_pending`, `test_caveat_absent_when_not_pending`, `test_caveat_absent_when_pending_count_zero` (or matching names).
    - All six tests pass.
    - The caveat copy contains exactly one em-dash (per CLAUDE.md communication style: sparingly).
  </acceptance_criteria>
  <done>Caveat injected into both tooltip bodies with three-way state tests for each.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7.3: Thread useEvalCoverage through all Cpu-bearing consumer components</name>
  <files>frontend/src/components/charts/PositionResultsPanel.tsx, frontend/src/components/insights/OpeningFindingCard.tsx, frontend/src/components/charts/EndgameOverallEntryCard.tsx, frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/charts/EndgameMetricCard.tsx, frontend/src/components/charts/EndgameTypeCard.tsx, frontend/src/components/stats/OpeningStatsCard.tsx</files>
  <read_first>
    - Each of the 7 component files listed above (read full file for each — locate `<Cpu />` icon site and the popover JSX that needs prop threading)
    - frontend/src/hooks/useEvalCoverage.ts (final state from Plan 91-06)
    - frontend/src/components/insights/EvalConfidenceTooltip.tsx + popovers/MetricStatTooltip.tsx (final state from Task 7.2 — confirm the prop names `isPending` and `pendingCount`)
    - .planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-PATTERNS.md (section "frontend/src/components/insights/BulletConfidencePopover.tsx + other Cpu-bearing components")
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (CONFIRM: imports `Swords` not `Cpu` — this file MUST NOT be modified)
  </read_first>
  <action>
    For each of the 7 files:
    Step A: Add `import { useEvalCoverage } from '@/hooks/useEvalCoverage';` to the imports (group with other `@/hooks` imports).
    Step B: Inside the component function body (at the top, before any JSX or other hook calls that might depend on it — though TanStack Query's order is flexible), add `const { isPending, pendingCount } = useEvalCoverage();`.
    Step C: Locate the `<MetricStatPopover ...>` or `<BulletConfidencePopover ...>` (or wherever the Cpu icon's parent popover lives) and pass two additional props: `isPending={isPending} pendingCount={pendingCount}`. If the file has MULTIPLE `<MetricStatPopover>` instances (EndgameOverallEntryCard has two per RESEARCH; EndgameOverallPerformanceSection has two), thread the props to ALL of them.
    Step D: After modifying all 7 files, run `cd frontend && npm run lint && npm run build`. Run all existing tests in `frontend/src/components/{charts,insights,stats}/__tests__/` to ensure no regressions: `npm test -- --run src/components/charts src/components/insights src/components/stats`.
    EXPLICITLY DO NOT TOUCH: `frontend/src/components/charts/EndgameTimePressureCard.tsx` — per RESEARCH OQ-1, Clock Gap is not Stockfish-dependent. Add to acceptance gate: grep this file and confirm no `useEvalCoverage` import is added.
    Confirm TanStack Query deduplication: after the build, run a quick smoke test (optional) loading the Endgames page in dev — open DevTools Network tab, observe `/imports/eval-coverage` is called ONCE per polling interval despite N popovers using `useEvalCoverage`. (Not a CI-automatable check; manual sanity for verify-work.)
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run lint</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm run build</automated>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend &amp;&amp; npm test -- --run src/components/charts src/components/insights src/components/stats</automated>
    <automated>grep -c "useEvalCoverage" frontend/src/components/charts/EndgameTimePressureCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 7 listed files contain `import { useEvalCoverage } from '@/hooks/useEvalCoverage';` (one match per file).
    - All 7 listed files contain a `useEvalCoverage()` call.
    - All 7 listed files contain at least one `isPending={isPending}` AND `pendingCount={pendingCount}` prop assignment on a popover JSX element.
    - `frontend/src/components/charts/EndgameTimePressureCard.tsx` contains ZERO `useEvalCoverage` references (grep returns 0).
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npm test -- --run src/components/charts src/components/insights src/components/stats` exits 0.
    - `cd frontend && npm run knip` exits 0 (no new unused exports).
  </acceptance_criteria>
  <done>Seven Cpu-bearing consumer components thread isPending/pendingCount; EndgameTimePressureCard correctly excluded; build + lint + tests + knip all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook → popover bodies | Prop drilling within React component tree; trusted |
| Popover body → user | Text content; React escapes by default; `toLocaleString` on integer is safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-25 | Tampering (regression) | Future edit adds an eighth Cpu site without caveat plumbing | mitigate | Phase 91 documents the touch sites in D-07; verify-work phase reviews D-07 vs `grep -rn '<Cpu' frontend/src/`. No automated CI gate possible without component-discovery tooling. |
| T-91-26 | Information Disclosure | Caveat reveals user's pending count to onlookers | accept | Same surface as the page-level header bar (T-91-23). |
| T-91-27 | Tampering | Caveat copy paraphrased away from locked D-06 | mitigate | Acceptance criteria includes `grep "Based on currently-evaluated games"` literal-string match. Verify-work phase MUST flag any deviation. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm run build && npm test -- --run` all pass.
- `cd frontend && npm run knip` clean.
- 7 components thread the props; EndgameTimePressureCard is untouched.
- Caveat appears conditionally in two tooltip body components.
- Manual smoke: open a popover on Endgames page while a 20k-game import drains; caveat present; let drain finish; refresh page; caveat absent.
</verification>

<success_criteria>
Plan complete when:
- Two tooltip body components render the caveat `<p>` conditional on `isPending && pendingCount > 0`.
- Seven Cpu-bearing consumer components thread `isPending`/`pendingCount` to their popovers.
- EndgameTimePressureCard is unmodified.
- RTL tests cover caveat-shown / caveat-absent / pendingCount-zero edge cases for both tooltips.
- Locked D-06 copy is preserved verbatim.
- Build + lint + tests + knip all green.
</success_criteria>

<output>
Create `.planning/phases/91-two-lane-import-defer-stockfish-eval-to-in-process-cold-drai/91-07-SUMMARY.md` when done. Record: list of 7 files modified, count of `useEvalCoverage` callsites added, count of `MetricStatPopover`/`BulletConfidencePopover` instances that received the new props, screenshot or copy-paste of the rendered caveat for the SUMMARY.
</output>
