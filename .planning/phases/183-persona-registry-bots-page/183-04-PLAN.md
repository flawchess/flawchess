---
phase: 183-persona-registry-bots-page
plan: 04
type: execute
wave: 2
depends_on: ["183-01"]
files_modified:
  - frontend/src/components/bots/PersonaGrid.tsx
  - frontend/src/components/bots/PersonaCard.tsx
  - frontend/src/components/bots/PersonaDetailSurface.tsx
  - frontend/src/pages/Bots.tsx
  - frontend/src/components/bots/__tests__/PersonaGrid.test.tsx
  - frontend/src/components/bots/__tests__/PersonaCard.test.tsx
  - frontend/src/components/bots/__tests__/PersonaDetailSurface.test.tsx
autonomous: true
requirements: [PERS-01, PERS-02, PERS-04, AVAT-01, AVAT-02]
must_haves:
  truths:
    - "The /bots setup phase renders PersonaGrid by default with 4 style sections in STYLE_SECTION_ORDER, each showing its 6 personas ascending 800→1800 (PERS-01, D-01/D-02)"
    - "A clearly-visible Custom entry (card or button) opens the existing SetupScreen unchanged (D-01/PERS-04)"
    - "Each card shows the placeholder avatar (species emoji on the per-style tint), name, and tilde ELO label; tapping opens the detail surface (D-03/D-04/D-18)"
    - "The detail surface shows the full bio, color chips and TC chips (reused CHIP_* classes), and a single Play button — defaulting color/TC to the persisted last-used values (D-03/D-05)"
    - "Tapping Play builds a complete BotGameSettings (botElo=rung, blend=RUNG_BLEND[rung] or the persona's 1600 choice, style=BOT_STYLE_BUNDLES[persona.style] by reference, userColor, base/incrementSeconds from the chosen TC, personaId=persona.id) and calls the existing BotsPage.handleStart exactly once (PERS-02)"
    - "Every persona renders a non-empty name + bio (AVAT-02) and a placeholder avatar (AVAT-01 partial per D-16)"
  artifacts:
    - frontend/src/components/bots/PersonaGrid.tsx
    - frontend/src/components/bots/PersonaCard.tsx
    - frontend/src/components/bots/PersonaDetailSurface.tsx
  key_links:
    - "PersonaDetailSurface Play → buildPersonaSettings → BotsPage.handleStart (the single existing start path)"
    - "detail surface color/TC → botPersonaSetupSettings read/write (last-used defaults)"
    - "PersonaCard avatar → placeholderAvatarFor(persona) (emoji + per-style tint)"
  backstop:
    - statement: "A placeholder avatar renders for every persona; a persona whose real-art avatarSrc is absent/pending never crashes the card (emoji fallback always present)"
      verification: backstop
  prohibitions:
    - statement: "The detail surface never imports EloSelector or PlayStyleControl — no persona × strength picker; ELO/style are display-only (Pitfall 3, PERS-02)"
      status: flagged-unverified
      verification: source-grep
    - statement: "No second start path — the Play button and the Custom entry both route through BotsPage.handleStart (Phase 171 invariant)"
      status: flagged-unverified
      verification: source-review
    - statement: "SetupScreen.tsx stays byte-unchanged this plan — Custom mode is routed to, not modified (PERS-04)"
      status: flagged-unverified
      verification: source-grep
    - statement: "Custom-built settings carry no personaId and no style (undefined by construction)"
      status: flagged-unverified
      verification: source-review
---

<objective>
Build the persona browsing + one-action-start UI and slot it into the Bots page's existing setup-phase branch as the new default view, with Custom as a routed escape hatch. This is the PERS-01/PERS-02/PERS-04 vertical slice: browse 24 personas → tap → pick color/TC → Play → existing game start.

Purpose: PERS-01 (browse the roster), PERS-02 (start in one action with the full pinned config, no strength step), PERS-04 (Custom untouched, routed), plus the AVAT-01/AVAT-02 display of placeholder avatars, names, and bios.
Output: PersonaGrid, PersonaCard, PersonaDetailSurface components + the Bots.tsx setup-branch swap, with RTL tests.

Note on D-08 "New opponent" (RESEARCH Open Question 3): once this plan makes PersonaGrid the default setup view, the existing onNewGame path already returns to the grid — Plan 05 wires the result-surface "New opponent"/"Rematch" against that path; this plan only establishes the grid-as-default so that routing is free.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/183-persona-registry-bots-page/183-CONTEXT.md
@.planning/phases/183-persona-registry-bots-page/183-PATTERNS.md
@frontend/src/lib/personas/personaRegistry.ts
@frontend/src/lib/personas/personaAvatars.ts
@frontend/src/lib/personas/botPersonaSetupSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonaGrid + PersonaCard (browse, 4 sections × 6 rungs)</name>
  <files>frontend/src/components/bots/PersonaGrid.tsx, frontend/src/components/bots/PersonaCard.tsx, frontend/src/components/bots/__tests__/PersonaGrid.test.tsx, frontend/src/components/bots/__tests__/PersonaCard.test.tsx</files>
  <read_first>
    - frontend/src/lib/personas/personaRegistry.ts (PERSONA_REGISTRY, STYLE_SECTION_ORDER, personasForSection, Persona type)
    - frontend/src/lib/personas/personaAvatars.ts (placeholderAvatarFor, PERSONA_STYLE_TINT)
    - frontend/src/components/bots/SetupScreen.tsx (screen-level composition + the mx-auto/pb-20 bottom-nav clearance root pattern to mirror)
    - frontend/src/components/bots/chipStyles.ts (chip button visual language, if used for card affordances)
    - frontend/src/lib/theme.ts (per-style accent constants added in Plan 01 Task 2)
  </read_first>
  <action>
    Create `PersonaCard.tsx`: a compact tappable card (semantic `<button>`, `data-testid="bots-persona-card-{id}"`, `aria-label` naming the persona) showing the placeholder avatar (species emoji on the per-style tint via placeholderAvatarFor), the persona name, and the tilde ELO label rendered as tilde-prefixed rung text (D-04). Text at the `text-sm` floor; per-style accent from theme.ts. Props: `persona: Persona`, `onSelect: (persona) => void`. Create `PersonaGrid.tsx`: iterate STYLE_SECTION_ORDER, render a labeled section per style (section heading uses the style's display name — "Wall", D-14 — and its accent) containing that style's 6 PersonaCards ascending by rung (personasForSection); a responsive mobile-first grid (column count is Claude's discretion). Render one clearly-visible Custom entry (a distinct card/button, `data-testid="bots-persona-custom"`). Props: `onSelectPersona`, `onSelectCustom`, plus `data-testid="bots-persona-grid"` on the container. Write RTL tests: grid renders exactly 24 persona cards in 4 sections in order; each card shows name + tilde label + an avatar node; the Custom entry is present and calls onSelectCustom.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/bots/__tests__/PersonaGrid.test.tsx src/components/bots/__tests__/PersonaCard.test.tsx</automated>
  </verify>
  <done>Grid renders 24 cards in 4 ordered style sections with placeholder avatars, names, and tilde ELO labels, plus a Custom entry; card tap fires onSelect.</done>
  <acceptance_criteria>
    - `npm test -- --run src/components/bots/__tests__/PersonaGrid.test.tsx src/components/bots/__tests__/PersonaCard.test.tsx` exits 0.
    - Behavior: exactly 24 `bots-persona-card-*` testids render, grouped into 4 sections in STYLE_SECTION_ORDER (test asserts DOM order).
    - Source: no `text-xs` or sub-`text-sm` utilities on card/label text.
    - Behavior: a `bots-persona-custom` element exists and invokes onSelectCustom on click.
    - Behavior: every card renders a non-empty name and a `~<rung>`-formatted label (covered by test).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PersonaDetailSurface (bio + color/TC chips + one-action Play)</name>
  <files>frontend/src/components/bots/PersonaDetailSurface.tsx, frontend/src/components/bots/__tests__/PersonaDetailSurface.test.tsx</files>
  <read_first>
    - frontend/src/components/bots/SetupScreen.tsx lines 153-277 (TcBucketGroup chip grid + COLOR_OPTIONS color-chip block — reuse this exact markup shape; note it imports CHIP_* from chipStyles.ts and TIME_CONTROL_PRESETS/bucket helpers)
    - frontend/src/components/bots/GameResultDialog.tsx (the shadcn Dialog shell composition + controlled open/onOpenChange + top-[30%] sm:top-1/2 mobile-anchor pattern)
    - frontend/src/components/bots/chipStyles.ts (CHIP_BASE_CLASS/CHIP_ACTIVE_CLASS/CHIP_INACTIVE_CLASS)
    - frontend/src/lib/botTimeControlPresets.ts (TIME_CONTROL_PRESETS, findPresetByLabel, DEFAULT_TC_PRESET_LABEL)
    - frontend/src/lib/personas/botPersonaSetupSettings.ts (read/write last-used color+tcLabel)
    - frontend/src/lib/personas/personaRegistry.ts (Persona, RUNG_BLEND) and frontend/src/lib/engine/botStyleBundles.ts (BOT_STYLE_BUNDLES for the by-reference style resolve)
    - frontend/src/hooks/useBotGame.ts lines 159-193 (BotGameSettings shape incl. the personaId? field from Plan 03)
  </read_first>
  <action>
    Create `PersonaDetailSurface.tsx` as a controlled dialog (or drawer — Claude's discretion; mirror GameResultDialog's Dialog shell + mobile anchor) opened for a selected persona. It renders: the persona avatar + name, the tilde ELO label and style name as DISPLAY-ONLY text, the full bio, a color-chip row and a TC-preset chip grid (reuse the SetupScreen chip markup + CHIP_* classes verbatim; `data-testid`s like `persona-color-{key}` / `persona-tc-{label}`), and one primary Play button (`variant="default"`, `data-testid="btn-persona-play"`). Color/TC default to the persisted last-used values via readPersonaSetupSettings(ownerKey) falling back to DEFAULT_PERSONA_SETUP_SETTINGS; a Random color preference resolves to a concrete color at Play time (mirror SetupScreen's D-12 resolve-before-start). On Play: resolve the TC label to `{baseSeconds, incrementSeconds}` via findPresetByLabel, build `BotGameSettings` = `{ botElo: persona.botElo, blend: persona.blend, style: BOT_STYLE_BUNDLES[persona.style], userColor: resolvedColor, baseSeconds, incrementSeconds, personaId: persona.id }`, call writePersonaSetupSettings(ownerKey, {colorPreference, tcLabel}), then call the passed `onStart(settings)` exactly once and close. Props: `persona: Persona | null`, `ownerKey`, `open`, `onOpenChange`, `onStart`. Do NOT import EloSelector or PlayStyleControl (Pitfall 3). Write RTL tests: rendering shows bio + color chips + TC chips + Play; Play builds settings with the pinned botElo/blend/personaId and the by-reference style, calls onStart once, and no ELO/strength control is present.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/bots/__tests__/PersonaDetailSurface.test.tsx</automated>
  </verify>
  <done>Detail surface shows bio + reused color/TC chips + one Play button, defaults to persisted last-used, and Play builds the fully-pinned BotGameSettings (with personaId + by-reference style) and calls onStart once; no strength picker present.</done>
  <acceptance_criteria>
    - `npm test -- --run src/components/bots/__tests__/PersonaDetailSurface.test.tsx` exits 0.
    - Source: `PersonaDetailSurface.tsx` imports neither `EloSelector` nor `PlayStyleControl` (grep).
    - Behavior: clicking Play calls onStart exactly once with settings whose `botElo === persona.botElo`, `blend === persona.blend`, `personaId === persona.id`, and `style === BOT_STYLE_BUNDLES[persona.style]` (reference identity, covered by test).
    - Source: color/TC chips use CHIP_BASE_CLASS/CHIP_ACTIVE_CLASS/CHIP_INACTIVE_CLASS from chipStyles.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Bots.tsx setup branch renders PersonaGrid by default (Custom routed)</name>
  <files>frontend/src/pages/Bots.tsx</files>
  <read_first>
    - frontend/src/pages/Bots.tsx lines 510-593 (handleStart / handleNewGame / handleDiscard, the boot/snapshot precedence, and the `startedSettings === null` setup branch currently rendering SetupScreen)
    - frontend/src/components/bots/PersonaGrid.tsx + PersonaDetailSurface.tsx (from Tasks 1-2)
    - frontend/src/components/bots/SetupScreen.tsx (unchanged — the Custom target)
  </read_first>
  <action>
    In `Bots.tsx`, replace the `startedSettings === null` branch's default render (currently `SetupScreen`) with a persona-first setup view that keeps the snapshot-precedence and boot plumbing UNCHANGED (D-01: snapshot/resume still wins, handleDiscard/handleNewGame untouched). Introduce local UI state for "which persona is open in the detail surface" and "is Custom mode showing". Default view: `PersonaGrid` (passing `onSelectPersona` → open the PersonaDetailSurface for that persona, `onSelectCustom` → show `SetupScreen`). Render `PersonaDetailSurface` controlled by that state, passing `ownerKey`, and `onStart={handleStart}` (the SINGLE existing start path — the detail Play button and SetupScreen's Start both call it). When Custom is showing, render the unchanged `SetupScreen` with the same `ownerKey`/`normalizedRating`/`onStart={handleStart}` props it has today, plus a way back to the grid. Do NOT add a second start path and do NOT modify SetupScreen. Keep the mobile bottom-nav clearance root pattern.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/__tests__/Bots.test.tsx 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>The /bots setup phase shows the persona grid by default; selecting a persona opens the detail surface whose Play routes through handleStart; the Custom entry opens the unchanged SetupScreen; snapshot/resume precedence and existing Bots tests still pass.</done>
  <acceptance_criteria>
    - `npx tsc -b` zero errors; existing `Bots.test.tsx` suite passes (extend as needed for the new default view).
    - Behavior: with no snapshot and no started game, the page renders `bots-persona-grid` (not SetupScreen) by default.
    - Behavior: selecting the Custom entry renders `setup-screen` (SetupScreen unchanged).
    - Source: `Bots.tsx` still has exactly one settings-consuming start entry (`handleStart`) — Play and Start both call it; no parallel start callback added.
    - Source: `SetupScreen.tsx` is not in this plan's diff.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → detail surface | Last-used color/TC preference is attacker-editable and re-read to seed defaults |
| authored bio/name → DOM | Persona bio/name strings render in the card and detail surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-183-09 | Information Disclosure | persona bio/name render | low | mitigate | Rendered via normal JSX text interpolation (React auto-escapes) — never dangerouslySetInnerHTML; content is Claude-authored, not user input (D-13) |
| T-183-10 | Tampering | last-used color/TC seed | low | mitigate | readPersonaSetupSettings validates + falls back to DEFAULT_PERSONA_SETUP_SETTINGS; an unrecognized tcLabel re-resolves via findPresetByLabel to the default, never breaks Play |
| T-183-11 | Elevation of Privilege | second start path | medium | mitigate | Play and Custom both funnel through the single handleStart entry — no parallel game-start route added (Phase 171 invariant) |
| T-183-SC | Tampering | npm installs | low | accept | Zero new packages installed by this plan |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/bots/__tests__/PersonaGrid.test.tsx src/components/bots/__tests__/PersonaCard.test.tsx src/components/bots/__tests__/PersonaDetailSurface.test.tsx src/pages/__tests__/Bots.test.tsx`
- `cd frontend && npx tsc -b` (zero errors)
- `cd frontend && npm run lint && npm run knip`
</verification>

<success_criteria>
- The persona grid is the default /bots view (4 sections × 6 rungs) with a routed Custom escape hatch (PERS-01/PERS-04).
- Tapping a persona → detail → Play starts a game in one action with the fully-pinned config and no strength step (PERS-02).
- Placeholder avatars, names, and bios display (AVAT-01 partial / AVAT-02); SetupScreen untouched.
</success_criteria>

<output>
Create `.planning/phases/183-persona-registry-bots-page/183-04-SUMMARY.md` when done.
</output>
