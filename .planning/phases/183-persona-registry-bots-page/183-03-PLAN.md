---
phase: 183-persona-registry-bots-page
plan: 03
type: execute
wave: 2
depends_on: ["183-01", "183-02"]
files_modified:
  - frontend/src/hooks/useBotGame.ts
  - frontend/src/lib/__tests__/botGameSnapshot.test.ts
autonomous: true
requirements: [PERS-02, PERS-04]
must_haves:
  truths:
    - "BotGameSettings gains an optional personaId?: PersonaId field; a Custom-mode game never sets it (undefined) by construction, mirroring style?'s D-03 optional-everywhere contract (PERS-04)"
    - "A pre-183 snapshot lacking personaId round-trips through readSnapshot/isValidSnapshotShape to settings.personaId === undefined, with CURRENT_SNAPSHOT_VERSION unchanged at 1 (Pitfall 5)"
    - "The bot's outgoing draw-offer is computed inside the SAME post-move pool.grade().then() callback as wouldBotResign, under the same settings.style gate and the same controller.signal.aborted staleness guard — no second pool.grade() call site (CR-02 discipline)"
    - "The hook exposes bot-draw-offer state (a pending flag identifying the offer) plus acceptBotDraw (ends the game as an agreed draw) and declineBotDraw (dismiss + reset the own-offer cooldown); a bot offer auto-clears on the user's next move"
    - "A fresh game (newGame) resets the bot-offer state and the own-offer cooldown counter — no leak across games (mirrors consecutiveLowScoreTurnsRef reset)"
  artifacts:
    - frontend/src/hooks/useBotGame.ts
    - frontend/src/lib/__tests__/botGameSnapshot.test.ts
  key_links:
    - "settings.personaId → looked up via PERSONA_REGISTRY by consumers (Plan 05), stored as the id string only"
    - "wouldBotOfferDraw (Plan 02) wired into the runBotTurn grade callback beside wouldBotResign"
  backstop:
    - statement: "A pool.grade() continuation that resolves after newGame()/resign() (controller aborted) never sets bot-offer state or ends the new game"
      verification: backstop
  prohibitions:
    - statement: "CURRENT_SNAPSHOT_VERSION is NOT bumped for the additive optional personaId field (Pitfall 5 — a bump silently drops every in-progress game)"
      status: flagged-unverified
      verification: source-review
    - statement: "The whole Persona object is never serialized into BotGameSettings or the snapshot — only the personaId string is carried (registry lookup at render time)"
      status: flagged-unverified
      verification: source-review
    - statement: "personaId is an independent field, never derived from styleNameFor(settings.style) — 6 personas share one style bundle so the reverse-lookup cannot disambiguate rung (Pitfall 4/Pattern 2)"
      status: flagged-unverified
      verification: source-review
---

<objective>
Thread persona identity and the bot's outgoing draw-offer through the game hook: add `BotGameSettings.personaId?`, verify the snapshot round-trips it without a version bump, and wire `wouldBotOfferDraw` (Plan 02) into the existing post-move grade callback with the accept/decline/auto-expire state the Plan 05 banner will render.

Purpose: PERS-02 (persona pins the full opponent, including draw-offer policy) and PERS-04 (Custom never sets personaId). D-07 needs the bot to actually decide to offer, using Plan 02's predicate at the SAME seam that already computes `wouldBotResign`.
Output: extended `BotGameSettings`, new bot-offer state + acceptBotDraw/declineBotDraw callbacks in `useBotGame.ts`, and a snapshot round-trip test proving the additive field is version-safe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/183-persona-registry-bots-page/183-RESEARCH.md
@.planning/phases/183-persona-registry-bots-page/183-PATTERNS.md
@frontend/src/lib/personas/personaRegistry.ts
@frontend/src/lib/botDrawGate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BotGameSettings.personaId? and verify snapshot round-trip</name>
  <files>frontend/src/hooks/useBotGame.ts, frontend/src/lib/__tests__/botGameSnapshot.test.ts</files>
  <read_first>
    - frontend/src/hooks/useBotGame.ts lines 159-193 (BotGameSettings interface + the style? optional-field doc-comment convention to mirror)
    - frontend/src/lib/botGameSnapshot.ts lines 44-92 (BotGameSnapshot shape, isValidSettingsShape/isValidSnapshotShape assert ONLY pre-existing required fields, CURRENT_SNAPSHOT_VERSION=1)
    - frontend/src/lib/__tests__/botGameSnapshot.test.ts (existing round-trip + version-mismatch test patterns)
    - frontend/src/lib/personas/personaRegistry.ts (the PersonaId type to import)
  </read_first>
  <behavior>
    - Test: a BotGameSettings with a personaId writes to a snapshot and reads back with personaId intact (via writeSnapshot/readSnapshot).
    - Test: an object shaped like a pre-183 snapshot (no personaId key, version === 1) still validates via isValidSnapshotShape and readSnapshot returns it with settings.personaId === undefined — no version bump, no corruption clear, no Sentry capture.
    - Test: a Custom-mode settings object (no personaId, no style) still round-trips unchanged.
  </behavior>
  <action>
    Extend `BotGameSettings` in `useBotGame.ts` with `personaId?: PersonaId` (import the type from `@/lib/personas/personaRegistry`), documenting — mirroring the `style?` doc comment — that `undefined` means a Custom-mode game (PERS-04, by construction), that a game's persona identity is looked up ONCE via PERSONA_REGISTRY wherever a component needs it, and that the whole Persona object is never re-serialized. Do NOT touch `botGameSnapshot.ts`'s validators or `CURRENT_SNAPSHOT_VERSION` — the field flows through automatically because the validators only assert pre-existing required fields. Add the round-trip + backward-compat tests to `botGameSnapshot.test.ts` proving version safety.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/botGameSnapshot.test.ts && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>personaId is an optional field on BotGameSettings; snapshot round-trips it and still accepts pre-183 personaId-less snapshots at version 1; tsc clean.</done>
  <acceptance_criteria>
    - `npm test -- --run src/lib/__tests__/botGameSnapshot.test.ts` exits 0.
    - Source: `CURRENT_SNAPSHOT_VERSION` value in `botGameSnapshot.ts` is still 1 (unchanged by this plan's diff).
    - Behavior: a personaId-less snapshot object validates and reads to `settings.personaId === undefined` (covered by test).
    - `npx tsc -b` zero errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire wouldBotOfferDraw + bot-offer state into the grade callback</name>
  <files>frontend/src/hooks/useBotGame.ts</files>
  <read_first>
    - frontend/src/hooks/useBotGame.ts lines 1300-1393 (the post-move pool.grade().then() callback: the CR-02 controller.signal.aborted staleness guard, the settings.style-gated wouldBotResign block with consecutiveLowScoreTurnsRef, and finalizeGame usage)
    - frontend/src/hooks/useBotGame.ts lines 900-970 (the user draw-offer resolution effect + offerDraw + newGame reset — the state/ref patterns to mirror for the bot's own offer)
    - frontend/src/lib/botDrawGate.ts (wouldBotOfferDraw signature + BOT_DRAW_OFFER_* constants from Plan 02)
  </read_first>
  <action>
    Add hook state for the bot's OUTGOING offer: a `botDrawOffer` state (null when no offer is live; a small object/flag when the bot has offered, sufficient for the banner to render), and a caller-owned `movesSinceOwnOfferRef` counter reset in `newGame()` alongside `consecutiveLowScoreTurnsRef`. Inside the EXISTING `pool.grade(fen,[uci]).then(...)` callback, AFTER the `wouldBotResign` block and still under the same `if (settings.style)` gate and the same `controller.signal.aborted` early-return, compute `wouldBotOfferDraw(score, chessRef.current, settings.style.contempt, movesSinceOwnOfferRef.current)`. If it returns true and no offer is already live and the game is not over, set `botDrawOffer` to the live-offer state and reset `movesSinceOwnOfferRef` to 0 (so the cooldown restarts); otherwise increment/maintain the counter per the caller-owned pattern. Do NOT add a second `pool.grade()` call. Expose `acceptBotDraw()` (guard on outcomeRef like the user-offer effect, then `finalizeGame({ reason: 'draw', drawReason: 'agreement' })` and clear botDrawOffer) and `declineBotDraw()` (clear botDrawOffer; the cooldown already reset when it was raised). Auto-expire the bot offer when the user commits their next move (clear botDrawOffer at the user-move commit seam, matching D-07's "expires on the user's next move"). Add `botDrawOffer`, `acceptBotDraw`, `declineBotDraw` to the returned `UseBotGameState`, and reset `botDrawOffer` + `movesSinceOwnOfferRef` in `newGame()`. Respect CLAUDE.md nesting/logic-LOC limits — extract a small helper if the grade callback grows past the soft limit.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useBotGame.test.tsx 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>The hook computes the bot's outgoing offer at the existing grade seam (single call site, staleness-guarded), exposes accept/decline, auto-expires on the user's next move, and resets cleanly on newGame; existing useBotGame tests still pass; tsc clean.</done>
  <acceptance_criteria>
    - `npx tsc -b` reports zero errors.
    - Existing `useBotGame` test suite passes unchanged (no regression to resign/accept-draw behavior).
    - Source: exactly one `pool.grade(` call site remains in `runBotTurn` (grep count unchanged from pre-plan).
    - Source: `botDrawOffer` and `movesSinceOwnOfferRef` are both reset inside `newGame()`.
    - Behavior/source: `acceptBotDraw` guards on `outcomeRef.current` before `finalizeGame` (mirrors the user-offer resolution effect).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage snapshot → hook | A resumed snapshot's settings.personaId is attacker-editable and re-read on resume |
| async grade RPC → game state | A late-resolving pool.grade continuation could mutate a new game's state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-183-06 | Tampering | resumed personaId | low | mitigate | personaId is looked up (not trusted) via PERSONA_REGISTRY by consumers; an unrecognized id resolves to the unstyled fallback (Plan 05 backstop), never crashes |
| T-183-07 | Tampering | stale grade continuation | medium | mitigate | Reuse the existing CR-02 controller.signal.aborted guard — a continuation for a discarded turn bails before touching botDrawOffer/finalizeGame; no second grade call site added |
| T-183-08 | Tampering | snapshot version drop | medium | mitigate | Do NOT bump CURRENT_SNAPSHOT_VERSION for the additive optional field; a version-safety test proves pre-183 snapshots still resume |
| T-183-SC | Tampering | npm installs | low | accept | Zero new packages installed by this plan |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/hooks/__tests__/useBotGame.test.tsx src/lib/__tests__/botGameSnapshot.test.ts`
- `cd frontend && npx tsc -b` (zero errors — shared BotGameSettings type change)
- `cd frontend && npm run lint`
</verification>

<success_criteria>
- BotGameSettings carries an optional personaId; snapshots resume old and new games safely without a version bump.
- The bot's outgoing draw offer fires at the existing single grade seam with accept/decline/auto-expire and clean per-game reset.
- No whole-Persona serialization; personaId never derived from the style reverse-lookup.
</success_criteria>

<output>
Create `.planning/phases/183-persona-registry-bots-page/183-03-SUMMARY.md` when done.
</output>
