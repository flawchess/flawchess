---
status: testing
phase: 122-in-app-feedback-button-seed-049
source: [122-VERIFICATION.md]
started: 2026-06-15T20:09:27Z
updated: 2026-06-15T20:09:27Z
---

## Current Test

number: 1
name: iOS safe-area clearance in installed PWA mode
expected: |
  On a real iPhone with FlawChess installed as a PWA (standalone mode), the floating
  feedback button sits above the home-indicator / safe-area inset (`pb-safe` +
  `env(safe-area-inset-bottom)`) and is fully tappable, not clipped by the bottom edge.
awaiting: user response

## Tests

### 1. iOS safe-area clearance in installed PWA mode
expected: On a real iPhone in PWA standalone mode, the floating feedback button clears the home-indicator safe-area inset and is fully tappable (not clipped).
result: [pending]

### 2. Floating button yields to real overlays (drawer/modal collision)
expected: On a real browser, opening a drawer or modal (filter drawer, bookmarks sidebar, any Radix dialog) hides the floating feedback button; closing the overlay brings it back. The MutationObserver in useOverlayOpen reacts to actual Radix portaling (jsdom tests only mock this).
result: [pending]

### 3. Live Sentry signal with correct tags on prod submission
expected: Submitting feedback in production fires a Sentry "feedback submitted" info signal tagged with source=feedback plus username / ELO bucket / platform, and it appears correctly in the Sentry dashboard (flawchess.sentry.io).
result: [pending]

### 4. Visual bottom-bar clearance on real mobile viewport
expected: On a real mobile viewport, the floating button at `bottom-[4.5rem]` clears the MobileBottomBar (4rem) without overlap or visual crowding; on desktop it sits at `bottom-4`.
result: [pending]

### 5. Mobile keyboard dismissal keeps the draft (Radix outside-interaction guard)
expected: On a real mobile device, open the feedback modal, type some text, then tap empty space to dismiss the on-screen keyboard. The keyboard closes but the modal stays open and the typed text is preserved (outside-dismiss is blocked while a draft exists). With the textarea empty, tapping outside still closes the modal. The X and Cancel buttons always close it. (Hard to cover in jsdom — Radix DismissableLayer behavior.)
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
