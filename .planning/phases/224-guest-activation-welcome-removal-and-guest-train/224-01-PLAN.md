---
phase: 224-guest-activation-welcome-removal-and-guest-train
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/train.py
  - tests/routers/test_train.py
  - frontend/src/App.tsx
  - frontend/src/App.test.tsx
  - frontend/src/pages/Train.tsx
files_deleted:
  - frontend/src/components/train/TrainGuestGate.tsx
  - frontend/src/pages/__tests__/Train.guestGate.test.tsx
autonomous: true
requirements: [GUESTACT-02, GUESTACT-03, GUESTACT-04, GUESTACT-14]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A guest account receives 200 (not 403) from all seven /train/* handlers (S-2, D-01)"
    - "A zero-game guest composes a warm-up session, solves a puzzle and advances streak 0 -> 1 over HTTP (ROADMAP SC 2)"
    - "The Train nav item is never aria-disabled for a zero-game account on any of the three nav surfaces (D-01)"
    - "Opening /train with zero games renders the Train landing, not a redirect to /library/import and not a guest gate (D-01, S-2)"
    - "app/routers/train.py's module docstring records guest admission plus the D-09 abuse backstop (guest_create_limiter 5/hour/IP, Cf-Connecting-Ip aware, and the one-open-session-per-user partial unique index)"
  artifacts:
    - path: "app/routers/train.py"
      provides: "Seven /train/* handlers with no guest authorization gate; rewritten module docstring"
      contains: "guest_create_limiter"
    - path: "tests/routers/test_train.py"
      provides: "Inverted guest tests (200) plus the zero-game guest end-to-end warm-up session test"
      contains: "test_guest_zero_game_warmup_end_to_end"
    - path: "frontend/src/App.tsx"
      provides: "/train in IMPORT_EXEMPT_ROUTES and an unwrapped /train/* route"
      contains: "'/train'"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Train.tsx"
      via: "/train/* route element no longer wrapped in ImportRequiredRoute"
      pattern: "path=\"/train/\\*\""
    - from: "frontend/src/pages/Train.tsx"
      to: "app/routers/train.py"
      via: "POST /train/sessions issued for a guest profile and answered 200"
      pattern: "useTrainSession"
  prohibitions:
    - statement: "No Train-specific rate limiter, guest ceiling or filler-only mode is added for guests (D-09)"
      status: resolved
      verification: test
    - statement: "frontend/src/pages/Train.tsx is not refactored; only the guest-gate branch is removed (baselined complexity 24)"
      status: resolved
      verification: test
    - statement: "The Phase 222 reworked-Train nav dot and the nav waiting-count badge stay gated on navUnlocked and !is_guest; neither is enabled for guests or zero-game accounts in this phase"
      status: resolved
      verification: judgment
---

<!-- planner-discipline-allow: _reject_guest -->

<objective>
TRACER SLICE. Open Train to every zero-game account, guest or registered, end to end: the FastAPI
authorization gate comes out of all seven `/train/*` handlers (S-2), the SPA route guard and nav
lock stop covering `/train` (D-01), and the guest landing component is deleted (S-2). Proven by a
real HTTP round trip: a zero-game guest composes a warm-up session, solves a puzzle and advances
its streak.

Purpose: this is the one vertical path the rest of the phase expands from. Every other plan
(copy branch, sign-up asks, purge semantics, metrics) assumes a guest can actually reach and
complete a Train session. If the compose path does not work for a zero-game guest, that is an
architectural dead end and it must surface on the first commit, not the tenth.

Output: guest-reachable Train, backed by an inverted backend test set and a new end-to-end
integration test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-CONTEXT.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-RESEARCH.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-PATTERNS.md
@CLAUDE.md
@frontend/CLAUDE.md
</context>

## Spec-less probe fallback skipped

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Notes |
|---|---|---|
| `tests/routers/test_train.py::test_guest_zero_game_warmup_end_to_end` | new test | GUESTACT-04, the tracer's end-to-end proof |
| `tests/routers/test_train.py::test_guest_composes_session_200` | renamed test | was `test_403_guest` |
| `tests/routers/test_train.py::test_guest_settings_round_trip_200` | renamed test | was `test_settings_403_guest` |
| `tests/routers/test_train.py::test_guest_onboarding_stamp_200` | renamed test | was `test_onboarding_403_guest` |
| `tests/routers/test_train.py::test_guest_progress_200` | renamed test | was `test_progress_403_guest` |
| `frontend/src/components/train/TrainGuestGate.tsx` | DELETED | S-2 |
| `frontend/src/pages/__tests__/Train.guestGate.test.tsx` | DELETED | S-2; closes the Phase 215 deferred contamination flake |

<tasks>

<task type="tracer">
  <name>Task 1: Zero-game guest reaches and runs Train, end to end (one path, every layer)</name>
  <reversibility rating="reversible">D-01's route/nav opening is a two-line revert in App.tsx and the backend gate is a single helper; nothing persists that a revert could not undo.</reversibility>
  <files>app/routers/train.py, tests/routers/test_train.py, frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/pages/Train.tsx, frontend/src/components/train/TrainGuestGate.tsx, frontend/src/pages/__tests__/Train.guestGate.test.tsx</files>
  <read_first>
    - `app/routers/train.py` lines 1-60 (module docstring + the guest-rejection helper) and the seven handler bodies at lines 61, 125, 178, 216, 279, 291, 341
    - `tests/routers/test_train.py` lines 1-120 (module docstring), 390 (`_set_guest`), 967, 2551, 2580, 2992 (the four guest tests)
    - `frontend/src/App.tsx` lines 160-164 (`IMPORT_EXEMPT_ROUTES`, `isNavLocked`), 200-233 (`navUnlocked`, `useTrainProgress`, `showTrainDot`), 785-805 (`ImportRequiredRoute`), 964-985 (the `/train/*` route)
    - `frontend/src/App.test.tsx` lines 494-570 (the `190-03: Train gating (NAV-02)` locked/unlocked pair and the null-profile case)
    - `frontend/src/pages/Train.tsx` lines 60-95 (`canTrain`, the FLAWCHESS-64 comment block) and 176-190 (the guest-gate early return)
    - `224-CONTEXT.md` decisions S-2, D-01, D-02, D-09 and the "Claude's Discretion" list
    - `224-RESEARCH.md` Finding A (the seven call sites), Finding B, Finding H (the limiter chain), Pitfall 6 (the App.test inversion), Pitfall 7 (the guest-gate flake)
  </read_first>
  <action>
    Backend (GUESTACT-03, GUESTACT-14, per S-2 and D-09):
    Delete the module-level guest-rejection helper in `app/routers/train.py` and its call at the top of
    all seven handlers: `compose_or_resume_session`, `solve_puzzle`, `reveal_puzzle`,
    `get_train_progress`, `get_train_settings`, `update_train_settings`, `stamp_onboarding_step`.
    Touch nothing else in those bodies: every existing `try`/`except` + `sentry_sdk.capture_exception()`
    block, every `HTTPException(404)`/`HTTPException(409)`, and every `user.id` scoping predicate stays
    byte-identical. Keep the `HTTPException` and `User` imports (both still used).
    Rewrite the module docstring's first paragraph so it records the new rule: Train is open to guest
    accounts as of Phase 224 (reverses Phase 189 D-05); every handler still scopes strictly by
    `current_active_user.id`, never a client-supplied id; and the abuse backstop for guest-created
    accounts is `guest_create_limiter` in `app/core/ip_rate_limiter.py` (5 creates per hour per IP,
    keyed on the Cloudflare-resolved client IP via `Cf-Connecting-Ip` -> leftmost `X-Forwarded-For` ->
    `request.client.host`) plus the one-open-session-per-user partial unique index
    `uq_drill_sessions_user_open`, per Phase 224 D-09. State explicitly that no Train-specific guest
    ceiling or filler-only mode is added. Keep the existing `dev_now_utc` paragraph verbatim.

    Backend tests (same commit, or the four existing tests fail):
    Invert and rename the four guest tests in `tests/routers/test_train.py`:
    `test_403_guest` -> `test_guest_composes_session_200` (assert 200, `body["session_id"] is not None`,
    `body["is_warmup"] is True`; opt into filler with `_install_sharp_fixture(monkeypatch)` and clean up
    via `_delete_sessions`); `test_settings_403_guest` -> `test_guest_settings_round_trip_200` (GET 200,
    PUT 200, and the PUT's echoed `weekday_mask`/`puzzles_per_session` match what was sent);
    `test_onboarding_403_guest` -> `test_guest_onboarding_stamp_200` (200, and a second GET /train/settings
    shows the stamped column non-null); `test_progress_403_guest` -> `test_guest_progress_200` (200 with
    the progress payload's documented key set). Rewrite each docstring to state the new rule instead of
    the old one. Update the four stale module-docstring bullets at lines ~12, ~54, ~65, ~80 and ~104 to
    match the new names and semantics.

    Frontend route + nav (GUESTACT-02, per D-01):
    Add `'/train'` to `IMPORT_EXEMPT_ROUTES` in `frontend/src/App.tsx` and remove the
    `ImportRequiredRoute` wrapper from the `/train/*` route (keep the `Suspense` fallback with
    `data-testid="train-loading"` exactly as it is). Update the comment above the route, which currently
    says Train is deliberately NOT exempt, to state the D-01 rule: every account may open Train with
    zero games, because composition serves red herrings and sharp filler with no rating and no game
    input. DISCRETION, decided here and do NOT change: the nav waiting-count badge
    (`useTrainProgress({ enabled: navUnlocked && profile != null && !profile.is_guest })`) and the
    Phase 222 reworked-Train dot (`showTrainDot = navUnlocked && ...`) keep their current gating —
    `waiting_count` is provably 0 for every warm-up-only account, and D-02 fixes discovery as the
    unlocked nav item alone. Leave `useReminderResurfaceRedirect` and `useDevicePushResync` guest-gated
    (S-3, no push for guests).

    Frontend nav tests (same commit as the `IMPORT_EXEMPT_ROUTES` edit — Pitfall 6):
    In `frontend/src/App.test.tsx`, invert the `190-03: Train gating (NAV-02)` locked-state test: with a
    zero-game non-guest profile and `tier1State = false`, `nav-train` / `mobile-nav-train` /
    `drawer-nav-train` must have a null `aria-disabled` and a null `title`, while `nav-openings` and
    `nav-endgames` must still be `aria-disabled="true"` — keep that control assertion so the test still
    proves the lock mechanism works, mirroring the existing control-assertion pattern in the same file.
    Rename the test to say Train is exempt. Do the same inversion for the `190-03: empty profile ...
    renders Train locked` case (null profile): Train not disabled, Openings/Endgames still disabled;
    rename it accordingly. Leave the unlocked-state test as is.

    Frontend page (per S-2):
    In `frontend/src/pages/Train.tsx` delete the `if (isGuest) return <TrainGuestGate />` early return
    and its import, and simplify `canTrain` to `profile != null`. KEEP `const isGuest = profile?.is_guest
    === true;` — Plan 04 and Plan 06 consume it. Replace the two stale FLAWCHESS-64 comment blocks (the
    one above `canTrain` and the one above the start-session effect) with a short note that the 403
    source is gone in Phase 224 and `canTrain` now only waits for the profile to resolve. Do NOT
    otherwise refactor this file: it sits at its baselined complexity ceiling of 24 and this change only
    removes branches.
    Delete `frontend/src/components/train/TrainGuestGate.tsx` and
    `frontend/src/pages/__tests__/Train.guestGate.test.tsx`; the deletion also closes the Phase 215
    deferred cross-test contamination flake recorded in
    `.planning/milestones/v2.15-phases/215-frontend-god-file-decomposition/deferred-items.md`, so do not
    attempt to "fix the flake" first.
  </action>
  <acceptance_criteria>
    - `grep -c "_reject_guest" app/routers/train.py` returns 0 and `grep -rc "_reject_guest" tests/routers/test_train.py` returns 0
    - `app/routers/train.py` contains the literal `guest_create_limiter` in its module docstring
    - `app/routers/train.py` still contains `sentry_sdk.capture_exception()` at the same number of sites as before the edit
    - `frontend/src/App.tsx` line 160's `IMPORT_EXEMPT_ROUTES` set contains `'/train'`
    - `frontend/src/App.tsx` has no `<ImportRequiredRoute>` wrapping the `path="/train/*"` route element
    - `frontend/src/components/train/TrainGuestGate.tsx` does not exist
    - `frontend/src/pages/__tests__/Train.guestGate.test.tsx` does not exist
    - `frontend/src/pages/Train.tsx` still declares `const isGuest = profile?.is_guest === true;`
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 24]' src/pages/Train.tsx` reports no complexity error from `frontend/`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/routers/test_train.py -k guest -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran" / "0 passed"</fails_when>
    <automated>! grep -rn "_reject_guest" app/routers/train.py tests/routers/test_train.py</automated>
    <fails_when>non-zero exit (at least one occurrence of the removed helper name is still present)</fails_when>
    <automated>test ! -f frontend/src/components/train/TrainGuestGate.tsx &amp;&amp; test ! -f frontend/src/pages/__tests__/Train.guestGate.test.tsx</automated>
    <fails_when>non-zero exit (one of the two deleted files still exists)</fails_when>
    <automated>( cd frontend &amp;&amp; npx vitest run src/App.test.tsx )</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>( cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run knip )</automated>
    <fails_when>non-zero exit, or knip prints an "Unused files" / "Unused exports" section naming a Train or welcome module</fails_when>
    <automated>( cd frontend &amp;&amp; npx eslint --no-inline-config --rule 'complexity: ["error", 24]' src/pages/Train.tsx )</automated>
    <fails_when>non-zero exit, or any line containing "complexity" in the output</fails_when>
    <automated>uv run ty check app/ tests/ scripts/ &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or "Would reformat" in the ruff format output</fails_when>
  </verify>
  <done>A guest token gets 200 from every /train/* handler, the Train nav item is reachable for a zero-game account on all three nav surfaces, the guest gate component and its test file are gone, and both stacks' gates are green.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end guest warm-up session integration test (compose, solve, streak, cadence)</name>
  <files>tests/routers/test_train.py</files>
  <read_first>
    - `tests/routers/test_train.py` lines 176-225 (`_TEST_SHARP_PUZZLES`, `_install_sharp_fixture`, the autouse `_default_empty_sharp_set` fixture), 230 (`_register_and_login`), 390 (`_set_guest`), 663 (`_solve`), 697 (`_get_settings`), 704 (`_get_progress`), 768 (`_put_settings`), 833-880 (`test_session_response_exposes_is_warmup` — the zero-game filler-only composition this test copies)
    - `tests/routers/test_train.py` line 1303 `test_solve_records_and_advances_streak` — the solve-loop and `try/finally` cleanup shape
    - `224-RESEARCH.md` Finding C (all four legs of the zero-game compose path, including why a filler-only session still stamps the eligibility floor)
    - `224-ROADMAP` success criterion 2 via `.planning/ROADMAP.md`
  </read_first>
  <action>
    Add `test_guest_zero_game_warmup_end_to_end(test_engine, monkeypatch)` to
    `tests/routers/test_train.py`, next to the renamed guest tests. This is GUESTACT-04 and ROADMAP
    SC 2's automated proof. Shape:
    register a user with `_register_and_login`, flip it with `_set_guest(test_engine, user_id)`, opt
    into filler with `_install_sharp_fixture(monkeypatch)` (the autouse fixture empties the sharp set by
    default), seed NO game and NO drill item — the zero-game path is the point. Then, over
    `httpx.AsyncClient(transport=httpx.ASGITransport(app=app))` with the guest bearer token:
    1. `POST /api/train/sessions` returns 200 with a non-null `session_id`, `puzzle_count > 0` and
       `is_warmup is True` (Finding C leg 4: the warm-up discriminant is `len(surviving_sr_keys) == 0`).
    2. `_solve(token, session_id, 0, guess=..., move_quality="good")` returns 200 and the response's
       `streak` is 1 — Finding C leg 1, `sharp_filler_available()` counts as material so a filler-only
       session still stamps the eligibility floor. Assert the streak from the solve response, not from a
       recomputation.
    3. `_get_progress(token)` returns 200 and reports the same non-zero streak, proving the row persists
       for the guest (ROADMAP SC 2 "a drill_sessions row plus streak state persist for that guest").
    4. `_get_settings(token)` returns 200 and `_put_settings(token, timezone="UTC", weekday_mask=62,
       puzzles_per_session=10)` returns 200, then a fresh `_get_settings` echoes `weekday_mask == 62` —
       this is the "weekday cadence on the next visit" leg of SC 2, asserted through the persisted
       `train_settings` row rather than by advancing the clock.
    Wrap everything after the composition in `try/finally` and clean up with `_delete_sessions` so no
    Train row leaks into another test's lottery (MEMORY `project_eval_lottery_test_isolation`). Reference
    GUESTACT-04, ROADMAP SC 2 and CONTEXT S-2 in the docstring, and state that the guest user row itself
    is left behind deliberately (the module's existing guest rows behave the same way).
  </action>
  <acceptance_criteria>
    - `tests/routers/test_train.py` defines `async def test_guest_zero_game_warmup_end_to_end`
    - The test calls `_set_guest`, `_install_sharp_fixture`, `_solve`, `_get_progress`, `_get_settings` and `_put_settings`
    - The test seeds no `games` row and calls neither `_seed_game_with_blunder` nor `_seed_drill_item`
    - The test body contains a `finally:` block calling `_delete_sessions`
    - `uv run pytest tests/routers/test_train.py -k guest_zero_game -x` exits 0 with exactly 1 test selected
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/routers/test_train.py -k guest_zero_game -x -q</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran" instead of "1 passed"</fails_when>
    <automated>uv run pytest tests/routers/test_train.py -x -q</automated>
    <fails_when>non-zero exit, or the summary line contains "failed" or "error"</fails_when>
    <automated>uv run ty check app/ tests/ scripts/ &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or "Would reformat" in the ruff format output</fails_when>
  </verify>
  <done>A zero-game guest composes a warm-up session, solves a puzzle to streak 1, reads that streak back from /train/progress, and round-trips a weekday mask through /train/settings, all over HTTP in one test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> FastAPI `/train/*` | Bearer-token requests from an account type that was previously rejected at the router |
| anonymous internet -> `POST /auth/guest` | Guest account creation, the supply side of the widened audience |
| SPA route guard (`ImportRequiredRoute`) -> page render | Client-side routing only; never an authorization boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-224-01 | Elevation of Privilege | `app/routers/train.py` seven handlers | high | mitigate | Removal is scoped to the account-type gate only; every handler keeps `user: Annotated[User, Depends(current_active_user)]` and derives the user id from it. Acceptance criteria forbid touching the scoping predicates; `uv run pytest tests/routers/test_train.py -x` covers the existing IDOR tests (foreign-session 404). |
| T-224-02 | Information Disclosure | `load_session_puzzles` / session reads | high | mitigate | The existing V4 IDOR guard (`user_id` predicate IN ADDITION to `session_id`) is untouched; the full `tests/routers/test_train.py` run in Task 2's verify includes the foreign-session tests. |
| T-224-03 | Denial of Service | cross-user red-herring pool now reachable by throwaway guests | medium | accept | D-09: `guest_create_limiter` (5/hour/IP on the Cloudflare-resolved client IP) plus the `uq_drill_sessions_user_open` one-open-session-per-user partial unique index and the one-session-per-day compose rule are the accepted backstop. No new guard is added; the acceptance is written into the module docstring. |
| T-224-04 | Denial of Service | in-process limiter window resets on deploy restart | low | accept | Documented in `app/core/ip_rate_limiter.py`'s own docstring; single-process uvicorn deployment. |
| T-224-05 | Spoofing | client-side `IMPORT_EXEMPT_ROUTES` treated as authorization | medium | mitigate | The route guard is navigation UX only; the API never gated on game count. The docstring rewrite states that authorization stays at the router (ASVS V1). |
| T-224-SC | Tampering | npm/pip/cargo installs | high | accept | This plan runs no `npm install`, `uv add` or lockfile change; `224-RESEARCH.md` Package Legitimacy Audit records "not applicable — this phase installs no external packages". No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `uv run pytest tests/routers/test_train.py -x` green (all guest tests inverted, the new e2e test passing)
- `uv run ty check app/ tests/ scripts/` zero errors
- `uv run ruff check .` and `uv run ruff format --check app/ tests/ scripts/` clean
- `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200` clean
- `( cd frontend && npm run lint && npm run build && npm run knip )` green
- `( cd frontend && npx vitest run src/App.test.tsx )` green
</verification>

<success_criteria>
- All seven `/train/*` handlers answer a guest bearer token with 2xx (GUESTACT-03)
- A zero-game guest completes compose -> solve -> streak -> settings round trip in one integration test (GUESTACT-04, ROADMAP SC 2)
- `/train` is exempt from the import lock in both the nav and the route (GUESTACT-02, D-01)
- The module docstring names the D-09 backstop and rules out a Train-specific ceiling (GUESTACT-14)
- `TrainGuestGate.tsx` and `Train.guestGate.test.tsx` no longer exist (S-2)
</success_criteria>

<output>
Create `.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-01-SUMMARY.md` when done
</output>
