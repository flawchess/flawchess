---
phase: 224-guest-activation-welcome-removal-and-guest-train
plan: 06
type: execute
wave: 3
depends_on: ["224-02", "224-04"]
files_modified:
  - frontend/src/components/train/SignupAskActions.tsx
  - frontend/src/components/train/__tests__/SignupAskActions.test.tsx
  - frontend/src/components/train/TrainScoreScreen.tsx
  - frontend/src/components/train/__tests__/TrainScoreScreen.test.tsx
  - frontend/src/components/import/ImportGuestPromoBubble.tsx
  - frontend/src/components/import/__tests__/ImportGuestPromoBubble.test.tsx
  - frontend/src/pages/Import.tsx
  - CHANGELOG.md
autonomous: true
requirements: [GUESTACT-05, GUESTACT-07, GUESTACT-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "On the guest score screen the bot bubble carries the sign-up ask with Why? and Sign up free in its actions slot (ROADMAP SC 3, S-3, S-4)"
    - "On the guest score screen no reminder ask, no reminder control, no QR/install block and no push prompt renders (ROADMAP SC 3, S-3)"
    - "The registered-user score screen is byte-identical to before: same bubble copy, no actions row, the reminder slot and Done row unchanged (ROADMAP SC 3)"
    - "Done stays alone in the row below the bubble, unchanged (S-4)"
    - "The Import page shows a friendly-bot bubble with the same button pair on every guest visit; the old Alert and its testids are gone (ROADMAP SC 5, S-6)"
    - "Why? navigates to /welcome with no umami attribute; Sign up free carries data-umami-event=signup-cta with data-umami-event-source train-score or import-promo (ROADMAP SC 9, S-7, D-05)"
    - "Sign up free uses the existing logoutForPromotion() then /login?tab=register flow, unchanged (S-4, D-07)"
  artifacts:
    - path: "frontend/src/components/train/SignupAskActions.tsx"
      provides: "The shared Why? plus Sign up free action pair for both surfaces"
      exports: ["SignupAskActions"]
    - path: "frontend/src/components/import/ImportGuestPromoBubble.tsx"
      provides: "Friendly-bot bubble hosting the import-specific sign-up ask"
      exports: ["ImportGuestPromoBubble", "IMPORT_GUEST_SIGNUP_COPY"]
    - path: "frontend/src/components/train/__tests__/SignupAskActions.test.tsx"
      provides: "Umami attribute contract and promotion-handoff coverage for both sources"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/train/TrainScoreScreen.tsx"
      to: "frontend/src/components/train/SignupAskActions.tsx"
      via: "actions={isGuest ? <SignupAskActions source=\"train-score\" /> : undefined} on the existing TrainBotBubble"
      pattern: "SignupAskActions"
    - from: "frontend/src/pages/Import.tsx"
      to: "frontend/src/components/import/ImportGuestPromoBubble.tsx"
      via: "the guest branch renders the extracted bubble instead of an inline Alert"
      pattern: "ImportGuestPromoBubble"
    - from: "frontend/src/components/train/SignupAskActions.tsx"
      to: "frontend/src/hooks/useAuth.ts"
      via: "logoutForPromotion() then window.location.href = '/login?tab=register'"
      pattern: "logoutForPromotion"
  prohibitions:
    - statement: "No data-umami-event attribute is placed on an internal react-router <Link>; the Why? control is a Button with an onClick navigate and carries no umami attribute (frontend/CLAUDE.md)"
      status: resolved
      verification: test
    - statement: "No new umami event name is introduced; only new sources on the existing signup-cta event"
      status: resolved
      verification: test
    - statement: "No guest branch is inlined into ImportPage's body; it sits at its baselined complexity of 29 with zero headroom"
      status: resolved
      verification: test
    - statement: "The useTrainReminderSlot() hook call is never skipped conditionally; its output is discarded for guests (rules of hooks)"
      status: resolved
      verification: test
    - statement: "No return-to parameter is threaded through promote_intent, LoginForm or googleAuth; post-sign-up landing stays the existing / flow (D-07)"
      status: resolved
      verification: judgment
    - statement: "The Import bubble pitches no Train feature and carries no third button (D-02)"
      status: resolved
      verification: test
  em_dash_rule: "No em-dashes in any new UI string (CLAUDE.md communication style)"
---

<objective>
Put the sign-up ask where the product has just delivered value. One shared action pair
(`SignupAskActions`) renders into `TrainBotBubble`'s existing `actions` slot on two surfaces:
the Train score screen, where for a guest it REPLACES the reminder ask and suppresses the
reminder slot and QR block entirely (S-3, S-4), and the Import page, where it replaces today's
`Alert` inside a friendly-bot bubble (S-6, D-06). Both carry `signup-cta` with a per-surface
source so lever B is attributable (S-7, ROADMAP SC 9).

Purpose: this is the conversion moment the whole phase exists to create. It must be purely
additive on the score screen: `TrainBotBubble` receives no `actions` today, so a guarded
`actions` prop cannot change one byte of the registered-user render (ROADMAP SC 3).

Output: two new components, two new test files, a deleted `Alert`, and the changelog entry for
the phase.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-CONTEXT.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-RESEARCH.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-PATTERNS.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-02-SUMMARY.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-04-SUMMARY.md
@frontend/CLAUDE.md
@frontend/src/components/bots/BotDrawOfferActions.tsx
</context>

## Spec-less probe fallback skipped

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Notes |
|---|---|---|
| `SignupAskActions({ source })` | new component, `frontend/src/components/train/SignupAskActions.tsx` | `source: 'train-score' \| 'import-promo'` |
| `btn-signup-why-train-score`, `btn-signup-free-train-score` | new testids | score-screen pair |
| `btn-signup-why-import-promo`, `btn-signup-free-import-promo` | new testids | import-page pair |
| `ImportGuestPromoBubble` + `IMPORT_GUEST_SIGNUP_COPY` | new component + exported copy constant, `frontend/src/components/import/ImportGuestPromoBubble.tsx` | D-06 |
| `import-guest-promo-bubble` | new testid | replaces `import-guest-promo-info` / `import-guest-promo-link` |
| `frontend/src/components/train/__tests__/SignupAskActions.test.tsx` | new test file | GUESTACT-11 |
| `frontend/src/components/import/__tests__/ImportGuestPromoBubble.test.tsx` | new test file | GUESTACT-07 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The shared Why? plus Sign up free action pair</name>
  <files>frontend/src/components/train/SignupAskActions.tsx, frontend/src/components/train/__tests__/SignupAskActions.test.tsx</files>
  <read_first>
    - `frontend/src/components/bots/BotDrawOfferActions.tsx` (whole file, 48 LOC — the exact analog: a two-`Button` fragment with no wrapper div, rendered into a bot bubble's `actions` slot)
    - `frontend/src/components/train/buttonStyles.ts` line 19 (`TRAIN_BUTTON_CLASS`)
    - `frontend/src/hooks/useAuth.ts` lines 165-185 (`logoutForPromotion` and the ordering it guarantees)
    - `frontend/src/pages/Import.tsx` lines 418-434 (the promotion handoff and umami attributes to lift verbatim)
    - `frontend/CLAUDE.md` — the button-variant rule, the `data-testid` rule, and the rule that `data-umami-event` must never sit on an internal react-router `<Link>`
    - `224-CONTEXT.md` S-4, D-05, D-07
  </read_first>
  <behavior>
    - Rendering with `source="train-score"` produces exactly two buttons, in order: "Why?" then "Sign up free"
    - "Why?" carries `data-testid="btn-signup-why-train-score"`, `variant="brand-outline"`, and NO `data-umami-event` and NO `data-umami-event-source` attribute
    - Clicking "Why?" calls the mocked `navigate` with `/welcome` and does not call `logoutForPromotion`
    - "Sign up free" carries `data-testid="btn-signup-free-train-score"`, `variant="default"`, `data-umami-event="signup-cta"` and `data-umami-event-source="train-score"`
    - Clicking "Sign up free" calls `logoutForPromotion` before assigning the register URL
    - With `source="import-promo"` the two testids and the `data-umami-event-source` value switch to the `import-promo` form, and `data-umami-event` stays `signup-cta`
    - The component renders no wrapping `div`; the bubble supplies the row layout
  </behavior>
  <action>
    Create `frontend/src/components/train/SignupAskActions.tsx` exporting
    `SignupAskActions({ source }: { source: SignupAskSource }): ReactElement` with an exported
    `type SignupAskSource = 'train-score' | 'import-promo'` (a `Literal`-style union, never a bare
    `string`). Copy `BotDrawOfferActions`'s shape verbatim: a fragment of two `Button`s, no wrapper
    element, an explicit `ReactElement` return type. Two deltas from the analog, both deliberate:
    do NOT import `BOT_ACTION_BUTTON_CLASS` from `components/bots/chipStyles` (bots-scoped) — use
    `TRAIN_BUTTON_CLASS` from `@/components/train/buttonStyles`; and the emphasis order is reversed
    relative to the analog, "Why?" (`brand-outline`) first and "Sign up free" (`default`) second,
    per S-4.

    "Why?": `data-testid={`btn-signup-why-${source}`}`, `onClick={() => navigate('/welcome')}` using
    `useNavigate()` from react-router. It carries NO umami attribute: it is internal navigation, and
    frontend/CLAUDE.md documents that `data-umami-event` on an internal route control downgrades to a
    full page reload. Record that reason in a comment at the call site so nobody "fixes" the omission.

    "Sign up free": `data-testid={`btn-signup-free-${source}`}`, `data-umami-event="signup-cta"`,
    `data-umami-event-source={source}`, and an `onClick` lifted verbatim from `Import.tsx:422-424` —
    `logoutForPromotion()` then `window.location.href = '/login?tab=register'`, in that order, from
    `useAuth()`. Do not re-author the handoff and do not thread a return-to parameter (D-07).

    Give the file a module docstring naming both host surfaces, S-4 and the umami source contract.

    Create `frontend/src/components/train/__tests__/SignupAskActions.test.tsx` covering the
    `<behavior>` block above. Start it with `// @vitest-environment jsdom`. Mock `react-router`'s
    `useNavigate` and `@/hooks/useAuth` using the mock shape already in `Welcome.test.tsx`, and stub
    `window.location` assignment so the hard navigation is observable without leaving the jsdom page.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/train/SignupAskActions.tsx` exports `SignupAskActions` and `SignupAskSource`
    - The file contains `data-umami-event="signup-cta"` exactly once and `data-umami-event-source={source}` exactly once
    - The file contains no `<Link` element
    - The file's `Why?` button element contains no `data-umami` attribute
    - The file contains `variant="brand-outline"` and `variant="default"` exactly once each
    - `frontend/src/components/train/__tests__/SignupAskActions.test.tsx` exercises both `source` values
    - No new string in the file contains an em-dash character
  </acceptance_criteria>
  <verify>
    <automated>( cd frontend &amp;&amp; npx vitest run src/components/train/__tests__/SignupAskActions.test.tsx )</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>! grep -q "&lt;Link" frontend/src/components/train/SignupAskActions.tsx &amp;&amp; [ "$(grep -c 'data-umami-event=' frontend/src/components/train/SignupAskActions.tsx)" -eq 1 ]</automated>
    <fails_when>non-zero exit (a react-router Link appears, or the umami event attribute count is not exactly 1)</fails_when>
    <automated>( cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build )</automated>
    <fails_when>non-zero exit, or "error TS" in the build output</fails_when>
  </verify>
  <done>One component renders the S-4 button pair for either surface, with the umami contract and the promotion handoff asserted by tests.</done>
</task>

<task type="auto">
  <name>Task 2: The guest score screen asks for the sign-up and drops the reminder surface</name>
  <files>frontend/src/components/train/TrainScoreScreen.tsx, frontend/src/components/train/__tests__/TrainScoreScreen.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainScoreScreen.tsx` lines 140-230 (signature with the `hasGames`/`isGuest` props Plan 04 added, the `useTrainReminderSlot()` call at ~228, the `scoreBubbleCopy` call), 245-322 (the render: the `TrainBotBubble` at ~260 that today passes NO `actions`, and the two-cell button row plus `reminderBelowRow` at ~307-320)
    - `frontend/src/components/train/TrainBotBubble.tsx` lines 54-78 (props) and 148-152 (the `actions !== undefined` render of the row)
    - `frontend/src/components/train/__tests__/TrainScoreScreen.test.tsx` lines 1-100 (the `useTrainReminderSlot` / `useTrainSettings` / `useTrainOnboarding` mocks and the default fixtures)
    - `224-RESEARCH.md` Pitfall 3 (how SC 3's byte-identity is broken by accident) and the "Additive `actions`" code example
    - `224-CONTEXT.md` S-3, S-4, ROADMAP success criterion 3
  </read_first>
  <action>
    In `frontend/src/components/train/TrainScoreScreen.tsx` make exactly three guarded, additive
    changes (GUESTACT-05, per S-3 and S-4):
    1. Pass `actions={isGuest ? <SignupAskActions source="train-score" /> : undefined}` to the existing
       `<TrainBotBubble persona={bot} state="verdict">`. Today it passes no `actions` at all, so for a
       registered user the prop is `undefined` and `TrainBotBubble`'s `actions !== undefined` guard
       renders nothing new: the registered DOM is unchanged (ROADMAP SC 3).
    2. Render `{isGuest ? null : reminderControl}` in the two-cell button row and
       `{isGuest ? null : reminderBelowRow}` below it. Keep calling `useTrainReminderSlot()`
       unconditionally at the top of the component and simply discard its output for a guest — never
       wrap the hook call in a condition (rules of hooks). Done stays exactly where it is, keeps
       `variant="default"`, `className={cn('flex-1', TRAIN_BUTTON_CLASS)}` and
       `data-testid="btn-train-done"`, and for a guest it is the row's only cell (S-4: Done stays alone
       in the row below, unchanged).
    3. Nothing else. Do not reorder the `useTrainReminderSlot` call, do not change `resolveReminderAsk`,
       do not rewrap the bubble, and do not add any other branch: the component has two branches of
       headroom against the un-baselined cap of 15 and these two ternaries consume them.
    The guest bubble COPY itself needs no change here: Plan 04 already made `scoreBubbleCopy`'s warm-up
    variant return `GUEST_SIGNUP_ASK_SCORE` as its second line whenever `audience.isGuest`.

    Extend `frontend/src/components/train/__tests__/TrainScoreScreen.test.tsx`:
    - A guest fixture (`isGuest: true`, warm-up session) renders `btn-signup-why-train-score` and
      `btn-signup-free-train-score` inside `train-score-bubble`, renders the exported
      `GUEST_SIGNUP_ASK_SCORE` string (imported from `@/lib/trainBotCopy`, never re-typed), and renders
      NEITHER the stubbed reminder control NOR the stubbed `belowRow` content, with both reminder mocks
      configured to return non-null stubs so their absence is caused by the guest branch and not by an
      empty mock.
    - The same guest fixture still renders `btn-train-done`.
    - A registered fixture with the identical non-null reminder mocks renders the reminder control and
      `belowRow` as today, and renders NO element whose testid starts with `btn-signup-`.
    - A registered byte-identity assertion: render the registered fixture and snapshot the
      `train-score-bubble` subtree's `innerHTML`, asserting it equals the same subtree rendered with the
      guest prop set to `false` through the pre-existing props path — i.e. assert the bubble subtree
      contains no `div` matching the bubble's actions-row class for the registered case. Write this as
      an explicit structural assertion, not as prose in a comment.
    Every pre-existing test in the file keeps its current fixture and must stay green untouched.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/train/TrainScoreScreen.tsx` contains `actions={isGuest ?` exactly once
    - The file contains `{isGuest ? null : reminderControl}` and `{isGuest ? null : reminderBelowRow}`
    - `useTrainReminderSlot()` is still called unconditionally at the top level of the component
    - `data-testid="btn-train-done"` and its `variant="default"` are unchanged
    - The test file imports `GUEST_SIGNUP_ASK_SCORE` from `@/lib/trainBotCopy` rather than re-typing the sentence
    - The test file asserts absence of any `btn-signup-` testid for the registered fixture
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/components/train/TrainScoreScreen.tsx` from `frontend/` reports no complexity error
  </acceptance_criteria>
  <verify>
    <automated>( cd frontend &amp;&amp; npx vitest run src/components/train/__tests__/TrainScoreScreen.test.tsx )</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>( cd frontend &amp;&amp; npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/components/train/TrainScoreScreen.tsx )</automated>
    <fails_when>non-zero exit, or any line containing "complexity" in the output</fails_when>
    <automated>grep -q "const { control: reminderControl, belowRow: reminderBelowRow } = useTrainReminderSlot();" frontend/src/components/train/TrainScoreScreen.tsx</automated>
    <fails_when>non-zero exit (the reminder-slot hook call was moved, renamed or made conditional)</fails_when>
    <automated>( cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build )</automated>
    <fails_when>non-zero exit, or "error TS" in the build output</fails_when>
  </verify>
  <done>A guest finishing a warm-up session sees the sign-up ask with Why? and Sign up free inside the bot bubble and no reminder or QR surface at all; the registered screen renders exactly what it rendered before.</done>
</task>

<task type="auto">
  <name>Task 3: The Import page guest promo becomes a friendly-bot bubble</name>
  <files>frontend/src/components/import/ImportGuestPromoBubble.tsx, frontend/src/components/import/__tests__/ImportGuestPromoBubble.test.tsx, frontend/src/pages/Import.tsx</files>
  <read_first>
    - `frontend/src/pages/Import.tsx` lines 1-30 (imports), 250 and 580 (the two other `Alert` uses that must survive), 255-262 (`logoutForPromotion` destructuring), 416-436 (the guest `Alert` being replaced)
    - `frontend/src/components/train/TrainScoreScreen.tsx` lines 152-160 (the `useMemo(() => pickBot('smart'), [])` persona-memo comment to mirror)
    - `frontend/src/components/train/TrainBotBubble.tsx` lines 40-80 (props) and `frontend/src/components/train/trainBubbleState.ts` (the `kind` union)
    - `frontend/src/components/bots/__tests__/BotGameBubble.test.tsx` (the actions-slot describe block and its two rules: use a real registry persona, and assert copy against the module's own exported constant)
    - `224-CONTEXT.md` S-6, D-02, D-06 and the discretion note on the Import bubble's `state` kind and persona stability
    - `224-RESEARCH.md` Pitfall 1 (ImportPage is at complexity 29 with a baseline of exactly 29) and Pitfall 4 (the imports that go unused once the Alert is gone)
  </read_first>
  <action>
    Create `frontend/src/components/import/ImportGuestPromoBubble.tsx` exporting
    `ImportGuestPromoBubble(): ReactElement` and the module-level copy constant
    `IMPORT_GUEST_SIGNUP_COPY` (exported so the test asserts against it rather than a re-typed literal).
    The copy is the D-06 import-specific payoff, today's alert message in bot voice, with no em-dash and
    no sentence shared with the score-screen ask:
      'Sign up free and every game you import gets a full Stockfish analysis on its own, with no
      per-game Analyze click. Your account works on any device you log in from.'
    Compose it exactly like `BotGameDesktopLayout` composes its bubble: the parent supplies persona,
    copy and actions, and the actions component carries no logic.
      - persona: `const bot = useMemo(() => pickBot('friendly'), []);` with the same rationale comment
        `TrainScoreScreen` carries — `ImportPage` re-renders on every import-job poll tick, so without
        the memo the persona would recast mid-read. This is DISCRETION, decided here as memoise-once.
      - `<TrainBotBubble persona={bot} state="prompt" actions={<SignupAskActions source="import-promo" />}>`
        wrapped in a container carrying `data-testid="import-guest-promo-bubble"`. DISCRETION, decided
        here: `state="prompt"` is the neutral, non-pulsing kind; `verdict` is the score screen's and
        carries the wrong connotation on an import page.
      - The bubble renders `IMPORT_GUEST_SIGNUP_COPY` and nothing else. No Train pitch, no third button,
        no "Try Train" link (D-02).
    The component reads no profile: the caller decides whether a guest is present.

    In `frontend/src/pages/Import.tsx`, replace the whole `{profile?.is_guest && (<Alert ...>...</Alert>)}`
    block with `{profile?.is_guest && <ImportGuestPromoBubble />}`. The branch stays a single `&&`, so
    `ImportPage` goes from complexity 29 to 28 rather than gaining anything: its baseline is exactly 29
    and frontend/CLAUDE.md forbids widening it. Then delete the imports that become unused in that file
    — the `DoorOpen` icon from the lucide import list, and the `useAuth` import together with the
    `logoutForPromotion` destructuring at line ~259 — but KEEP the `Alert` import, which is still used at
    lines 250 and 580. The testids `import-guest-promo-info` and `import-guest-promo-link` disappear with
    the markup (ROADMAP SC 5).

    Create `frontend/src/components/import/__tests__/ImportGuestPromoBubble.test.tsx` (new directory),
    starting with `// @vitest-environment jsdom`, following `BotGameBubble.test.tsx`'s conventions:
    the bubble renders `import-guest-promo-bubble`; it renders the exported `IMPORT_GUEST_SIGNUP_COPY`
    text; it renders `btn-signup-why-import-promo` and `btn-signup-free-import-promo` and exactly two
    buttons in total (proving D-02's no-third-button rule); the sign-up button carries
    `data-umami-event="signup-cta"` and `data-umami-event-source="import-promo"`; and the persona name
    rendered on a first mount is still the same after a re-render (the memo).
  </action>
  <acceptance_criteria>
    - `frontend/src/components/import/ImportGuestPromoBubble.tsx` exports `ImportGuestPromoBubble` and `IMPORT_GUEST_SIGNUP_COPY`
    - It calls `useMemo(() => pickBot('friendly'), [])`
    - `frontend/src/pages/Import.tsx` contains `ImportGuestPromoBubble` and no `import-guest-promo-info` or `import-guest-promo-link`
    - `frontend/src/pages/Import.tsx` contains no `DoorOpen` and no `logoutForPromotion`
    - `frontend/src/pages/Import.tsx` still imports `Alert` and still contains `data-testid="import-concurrent-notice"` and `data-testid="import-info"`
    - The new test asserts exactly two buttons render inside the bubble
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 29]' src/pages/Import.tsx` from `frontend/` reports no complexity error
    - No new string in the new component contains an em-dash character
  </acceptance_criteria>
  <verify>
    <automated>( cd frontend &amp;&amp; npx vitest run src/components/import/__tests__/ImportGuestPromoBubble.test.tsx )</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>! grep -Eq 'import-guest-promo-info|import-guest-promo-link|DoorOpen|logoutForPromotion' frontend/src/pages/Import.tsx &amp;&amp; grep -q "import { Alert }" frontend/src/pages/Import.tsx</automated>
    <fails_when>non-zero exit (a removed testid or import survives, or the still-needed Alert import was deleted)</fails_when>
    <automated>( cd frontend &amp;&amp; npx eslint --no-inline-config --rule 'complexity: ["error", 29]' src/pages/Import.tsx )</automated>
    <fails_when>non-zero exit, or any line containing "complexity" in the output</fails_when>
    <automated>( cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run knip )</automated>
    <fails_when>non-zero exit, or knip prints an "Unused files", "Unused exports" or "Unused dependencies" section</fails_when>
    <automated>( cd frontend &amp;&amp; npx vitest run src/pages/__tests__/Import.stateMachine.test.tsx src/pages/__tests__/Import.queuedState.test.tsx src/pages/__tests__/Import.pasteHandoff.test.tsx )</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>Every guest visit to the import page shows a friendly bot making the import-specific case with the same Why? plus Sign up free pair; the old Alert, its two testids and the now-unused imports are gone.</done>
</task>

<task type="auto">
  <name>Task 4: Changelog entry for the phase</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-25 (the `## [Unreleased]` block and the Added / Changed sections already populated by Phase 223) for the voice and bullet style
    - `.planning/ROADMAP.md` the Phase 224 block (goal and the ten success criteria) for what is user-facing
    - `224-CONTEXT.md` `<domain>` for the two levers stated in user terms
  </read_first>
  <action>
    Append user-facing bullets to `CHANGELOG.md` under the existing `## [Unreleased]` heading. Do not
    create a new version heading and do not disturb Phase 223's existing bullets. Write them in the
    file's established product voice (a reader who is a chess player, not a developer), with em-dashes
    used sparingly or not at all, and cover exactly what a user would notice:
      Added — Train is now open to everyone, including guests and accounts that have not imported a
      game yet: a daily warm-up session of sharp practice puzzles, with the same streak and weekday
      schedule as a full account, and the streak and solve history carry over when you sign up.
      Changed — signing in as a guest no longer routes you through an explainer page before you can
      import; you land straight on the import screen, and the page that compared guest and signed-up
      accounts is now a short four-point summary of what changes when you sign up, reachable from a
      "Why?" button next to the sign-up prompt.
      Changed — the sign-up prompt now comes from one of the FlawChess bots, on the training score
      screen and on the import page, instead of an information banner.
      Changed — for a guest, training reminders and the phone install prompt are hidden, because
      reminders are only sent to signed-up accounts.
      Changed — a guest account that stays inactive for 30 days now has its training sessions and
      settings removed along with its games, so "nothing is deleted after 30 days of inactivity"
      is a real difference between a guest and a signed-up account.
    Mention no requirement ID, no file path, no internal decision label.
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` still contains exactly one `## [Unreleased]` heading
    - The Phase 223 bullets under `[Unreleased]` are still present verbatim, including the bot-opponents line and the phone game-screen line
    - At least five new bullets are added, split across the existing `### Added` and `### Changed` subsections
    - No new bullet contains a file path, a `GUESTACT-` id, a `D-` decision label or the word "phase"
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" -eq 1 ] &amp;&amp; ! grep -nE 'GUESTACT-|\.tsx|\.py' CHANGELOG.md</automated>
    <fails_when>non-zero exit (the Unreleased heading was duplicated, or an internal identifier or file path leaked into the changelog)</fails_when>
    <automated>grep -q 'The bot opponents now talk during a game' CHANGELOG.md &amp;&amp; grep -qi 'Train is now open to everyone' CHANGELOG.md</automated>
    <fails_when>non-zero exit (a Phase 223 bullet was dropped, or the new Train-for-everyone bullet is absent)</fails_when>
  </verify>
  <done>The Unreleased changelog block describes both levers in product language, with Phase 223's entries untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-app control -> `/login?tab=register` | The promotion handoff: auth token and query cache cleared, then a hard navigation out of the SPA |
| analytics attribute -> Umami | Event name and source travel from the DOM to a third-party tracker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-224-22 | Session Management (ASVS V3) | `SignupAskActions`' sign-up handoff | high | mitigate | `logoutForPromotion()` is lifted verbatim, preserving its ordering (clear `localStorage.auth_token` and the query cache, set `sessionStorage.promote_intent`, then hard-navigate). Task 1 forbids re-authoring it; the behaviour block asserts `logoutForPromotion` runs before the URL assignment. No gating on `guest_token` presence is reintroduced (the documented 2026-06-23 regression). |
| T-224-23 | Tampering | `data-umami-event` on an internal route control | medium | mitigate | Documented full-page-reload bug: the tracker `preventDefault()`s and assigns `location.href`. The Why? control is a `<Button onClick={navigate}>` with no umami attribute, asserted both by the test and by a grep that no `<Link` exists in the file. |
| T-224-24 | Repudiation | unattributable conversions | low | mitigate | Each surface carries its own `data-umami-event-source` (`train-score`, `import-promo`), so lever B is attributable per surface (ROADMAP SC 9, S-7). The retired `train-gate` source is recorded in the Plan 03 baseline note so historical rows are not summed into the after-reading. |
| T-224-25 | Information Disclosure | bot copy promising something the backend cannot deliver | medium | mitigate | The score-screen ask promises only in-place promotion effects (streak and solves preserved), proven by Plan 05's promotion test; the import ask promises automatic analysis only AFTER sign-up, which matches `eval_queue_service`'s guest exclusion. |
| T-224-26 | Elevation of Privilege | UI-level guest branch mistaken for authorization | low | accept | Both branches are presentational. Authorization lives at the router and is unchanged by this plan. |
| T-224-SC | Tampering | npm/pip/cargo installs | high | accept | This plan runs no `npm install` or lockfile change; `224-RESEARCH.md` Package Legitimacy Audit records "not applicable — this phase installs no external packages". |
</threat_model>

<verification>
- `( cd frontend && npx vitest run src/components/train/__tests__/SignupAskActions.test.tsx src/components/train/__tests__/TrainScoreScreen.test.tsx src/components/import/__tests__/ImportGuestPromoBubble.test.tsx )` green
- `( cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run )` green
- `git diff --quiet "$(git merge-base HEAD main)"..HEAD -- frontend/eslint.config.js` (no complexity baseline drift across the whole phase)
- Full pre-merge gate from CLAUDE.md before the phase merges
</verification>

<success_criteria>
- The guest score screen carries the sign-up ask in the bubble's actions slot with no reminder, QR or push surface; the registered screen is unchanged (ROADMAP SC 3, GUESTACT-05)
- The Import page renders the friendly-bot bubble on every guest visit and the old Alert testids are gone (ROADMAP SC 5, GUESTACT-07)
- `signup-cta` carries `train-score` and `import-promo` sources (ROADMAP SC 9, GUESTACT-11)
- `CHANGELOG.md` `[Unreleased]` describes both levers in product language
</success_criteria>

<output>
Create `.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-06-SUMMARY.md` when done
</output>
