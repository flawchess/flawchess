---
phase: 224-guest-activation-welcome-removal-and-guest-train
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/activity_queries.py
  - app/services/activity_stats.py
  - tests/test_admin_activity_stats.py
  - frontend/src/pages/activity/render.js
  - frontend/src/pages/activity/ActivityPage.tsx
  - reports/growth/guest-activation-baseline-2026-09-17.md
autonomous: false
requirements: [GUESTACT-12, GUESTACT-13]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The Activity dashboard's Train card reports sessions completed while still a guest and how many guests produced them, without any hand-run SQL (D-10b)"
    - "The guest Train reading counts only sessions dated before the user's promotion, for guests created inside the window (D-11)"
    - "Users with games_purged_at set are excluded from the guest Train reading and the exclusion count is footnoted (D-12)"
    - "A dated note under reports/growth/ holds the pre-merge numbers for both levers AND the exact SQL that produced them, so the after-reading runs the same query (D-10a, ROADMAP SC 10)"
    - "Lever A and lever B are reported as four separate numbers; no combined guest conversion number appears anywhere (S-7, ROADMAP SC 10)"
  artifacts:
    - path: "app/services/activity_queries.py"
      provides: "fetch_guest_train raw-SQL reader plus the guest_train Payload key"
      contains: "async def fetch_guest_train"
    - path: "app/services/activity_stats.py"
      provides: "guest_train wired into build_payload"
      contains: "fetch_guest_train"
    - path: "reports/growth/guest-activation-baseline-2026-09-17.md"
      provides: "Pre-merge baseline numbers for both levers with the committed SQL"
      min_lines: 60
  key_links:
    - from: "app/services/activity_stats.py"
      to: "app/services/activity_queries.py"
      via: "guest_train=await queries.fetch_guest_train(conn, window.window_start)"
      pattern: "fetch_guest_train"
    - from: "frontend/src/pages/activity/render.js"
      to: "frontend/src/pages/activity/ActivityPage.tsx"
      via: "renderTrainCard writes the guest line into the #tr-guest-note element"
      pattern: "tr-guest-note"
  prohibitions:
    - statement: "No combined guest conversion number is computed or reported; lever A and lever B stay four separate readings (S-7, ROADMAP SC 10)"
      status: resolved
      verification: judgment
    - statement: "No new chart series is added to the Activity Train card; the guest cohort is text and table only, because charts are geometry-validated by check:activity-layout at four phone widths"
      status: resolved
      verification: test
    - statement: "The /welcome landings baseline is never fetched automatically; it is an operator reading from Umami, recorded by hand"
      status: resolved
      verification: judgment
    - statement: "No retained lifetime guest-session counter column is added to users (D-12)"
      status: resolved
      verification: test
---

<objective>
Make both levers readable before and after, without a combined number (S-7, D-10..D-12). Two
halves: (a) the Activity dashboard's Train card gains a guest-cohort reading so the "after" needs
no hand-run SQL, and (b) a dated note under `reports/growth/` records the pre-merge numbers for
all four metrics with the exact SQL committed beside them.

Purpose: ROADMAP SC 10 is the only criterion this phase cannot satisfy after merge. If the
baseline is not recorded before the change ships, neither lever can ever be evaluated.

Output: `fetch_guest_train` on the dashboard, and `reports/growth/guest-activation-baseline-2026-09-17.md`
with real numbers in it.

This plan touches no file any other Phase 224 plan touches and can execute in parallel with them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-CONTEXT.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-RESEARCH.md
@.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-PATTERNS.md
@reports/growth/growth-recommendations-2026-09-15.md
@CLAUDE.md
</context>

## Spec-less probe fallback skipped

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Notes |
|---|---|---|
| `app/services/activity_queries.py::fetch_guest_train` | new async function | D-11/D-12 reader, returns `{"sessions_completed": int, "users": int}` |
| `Payload["guest_train"]` | new TypedDict key | `dict[str, Any]`, wired in `build_payload` and `fake_payload` |
| `#tr-guest-note` | new DOM id in the Activity Train card | rendered by `renderTrainCard` |
| `reports/growth/guest-activation-baseline-2026-09-17.md` | new doc | GUESTACT-12; four numbers plus four committed queries |

<tasks>

<task type="auto">
  <name>Task 1: fetch_guest_train reader and its Payload wiring</name>
  <files>app/services/activity_queries.py, app/services/activity_stats.py, tests/test_admin_activity_stats.py</files>
  <read_first>
    - `app/services/activity_queries.py` lines 45-90 (`_PROMOTED_GUEST`, `_GUEST_COHORT`, the `Payload` TypedDict), 284-302 (`fetch_train` — note it has no `users` join at all), 504-531 (`fetch_purged_excluded`), 631-661 (`fetch_conversion` — the closest analog by cohort and return shape)
    - `app/services/activity_stats.py` lines 57-95 (`build_payload`)
    - `tests/test_admin_activity_stats.py` lines 100-132 (`fake_payload`)
    - `224-RESEARCH.md` Finding J, including the proposed D-11 SQL and the two type notes (`drill_sessions.session_date` is a `Date`, `users.promoted_at` is `DateTime(timezone=True)`, hence the explicit `::date` cast; `drill_sessions.status` is TEXT with a CHECK so `'completed'` is the correct literal)
    - `224-CONTEXT.md` D-10, D-11, D-12
  </read_first>
  <action>
    Add `async def fetch_guest_train(conn: AsyncConnection, window_start: datetime.date) -> dict[str, Any]`
    to `app/services/activity_queries.py`, placed next to `fetch_conversion` and written in that
    function's exact style: an f-string embedding the `_GUEST_COHORT` constant (never an inline
    re-spelling of the predicate), `CAST(:first AS date)` named parameters (never f-string
    interpolation of a value), execution through `_rows(conn, sql, **params)`, and `int(row[n])`
    coercion on the way out. The query is GUESTACT-13 and implements D-11 and D-12 literally:

      SELECT count(*) AS sessions_completed, count(DISTINCT d.user_id) AS users
      FROM drill_sessions d
      JOIN users u ON u.id = d.user_id
      WHERE u.created_at >= CAST(:first AS date)
        AND {_GUEST_COHORT}
        AND u.games_purged_at IS NULL
        AND d.status = 'completed'
        AND d.session_date &lt; COALESCE(u.promoted_at::date, 'infinity'::date)

    Return `{"sessions_completed": int(row[0]), "users": int(row[1])}`. Give it a docstring that states
    the D-11 definition in words (a promoted user's later sessions count as registered, not as guest),
    the D-12 exclusion and which existing count footnotes it (`fetch_purged_excluded`'s `guest` key),
    and that this is a NEW query rather than a filter tweak because `fetch_train` carries no `users`
    join.

    Add `guest_train: dict[str, Any]` to the `Payload` TypedDict, next to `conversion` and
    `purged_excluded`. Add `guest_train=await queries.fetch_guest_train(conn, window.window_start),` to
    `build_payload` in `app/services/activity_stats.py`, in the same position relative to `conversion`.

    Add `guest_train={},` to `fake_payload` in `tests/test_admin_activity_stats.py` (omitting it fails
    roughly eight tests that build a schema-complete Payload). Then add a real test in the same file
    asserting the D-11/D-12 semantics against seeded rows: a guest created in the window with one
    `completed` session counts; a session dated on or after that user's `promoted_at` does NOT count; a
    user with `games_purged_at` set does NOT count; a session with status `open` or `expired` does NOT
    count. Follow the file's existing fixture conventions and clean up every non-guest `users`/`games`
    row it creates in a `finally` block (MEMORY `project_eval_lottery_test_isolation`).
  </action>
  <acceptance_criteria>
    - `app/services/activity_queries.py` defines `async def fetch_guest_train` with an explicit `-> dict[str, Any]` return annotation
    - The new SQL contains the literals `games_purged_at IS NULL`, `d.status = 'completed'` and `COALESCE(u.promoted_at::date, 'infinity'::date)`
    - The new SQL interpolates `{_GUEST_COHORT}` rather than re-spelling the predicate
    - `app/services/activity_queries.py`'s `Payload` contains a `guest_train:` key
    - `app/services/activity_stats.py`'s `build_payload` contains `fetch_guest_train`
    - `tests/test_admin_activity_stats.py`'s `fake_payload` contains `guest_train=`
    - `uv run ty check app/ tests/ scripts/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admin_activity_stats.py -x -q</automated>
    <fails_when>non-zero exit, or the summary line contains "failed" or "error"</fails_when>
    <automated>grep -q "fetch_guest_train" app/services/activity_stats.py &amp;&amp; grep -q "guest_train=" tests/test_admin_activity_stats.py</automated>
    <fails_when>non-zero exit (the payload key is not wired into build_payload or into the test fixture)</fails_when>
    <automated>uv run ty check app/ tests/ scripts/ &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or "Would reformat" in the ruff format output</fails_when>
  </verify>
  <done>The dashboard payload carries a guest_train reading whose D-11 and D-12 semantics are asserted by seeded-row tests.</done>
</task>

<task type="auto">
  <name>Task 2: Render the guest cohort line in the Activity Train card</name>
  <files>frontend/src/pages/activity/render.js, frontend/src/pages/activity/ActivityPage.tsx</files>
  <read_first>
    - `frontend/src/pages/activity/render.js` lines 17 (`DASH`), 29-45 (the payload destructuring block), 160-172 (the existing purged-excluded footnote lines, the `num()` usage pattern), 308-325 (`renderTrainCard`), 331-352 (`renderTrainFunnelCard` — the text-only hero/note idiom this task copies)
    - `frontend/src/pages/activity/ActivityPage.tsx` lines 455-480 (the Train sessions section markup)
    - `frontend/scripts/check-activity-layout.mjs` lines 1-31 (what the harness validates, and why a new chart series is forbidden here)
    - `224-CONTEXT.md` D-10b, D-12
  </read_first>
  <action>
    In `frontend/src/pages/activity/render.js`, add `GUESTTRAIN` to the module-level payload state
    declared around line 29 and assign it from `payload.guest_train` in the same block that assigns
    `TRAIN`, `CONV` and `PURGED`. At the end of `renderTrainCard`, write a single note line into a new
    `#tr-guest-note` element in the existing text style of the purged footnote: the number of sessions
    completed while still a guest, the number of guests that produced them, and the D-12 footnote naming
    the purged-guest exclusion count already available as `PURGED.guest`. Fall back to `DASH` when
    `GUESTTRAIN` is null, and keep the existing early-return branch working (when `TRAIN` is empty the
    note must still resolve, not be left stale). Use the file's own `num()` formatting helper.
    Do NOT add a chart series, a new legend or a new `#c-*` element: the chart primitives are geometry
    validated by `npm run check:activity-layout` at four phone widths, and a guest row buys no
    analytical value from a chart.

    In `frontend/src/pages/activity/ActivityPage.tsx`, add `<p className="note" id="tr-guest-note"></p>`
    immediately after the `<details>` block inside the "Sessions per day by outcome" card, so the note
    sits under the table wrap. Change nothing else in that section.
  </action>
  <acceptance_criteria>
    - `frontend/src/pages/activity/render.js` references `payload.guest_train` and writes into `#tr-guest-note`
    - `frontend/src/pages/activity/ActivityPage.tsx` contains `id="tr-guest-note"`
    - `frontend/src/pages/activity/render.js` contains no new `barChart(`, `hbar(` or `legend(` call inside `renderTrainCard` (the existing three-series `barChart` call stays exactly as it is)
    - `npm run check:activity-layout` from `frontend/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>( cd frontend &amp;&amp; npm run check:activity-layout )</automated>
    <fails_when>non-zero exit, or the output names an overflow/geometry violation at any of the four phone widths</fails_when>
    <automated>grep -q "tr-guest-note" frontend/src/pages/activity/render.js &amp;&amp; grep -q 'id="tr-guest-note"' frontend/src/pages/activity/ActivityPage.tsx</automated>
    <fails_when>non-zero exit (the note element or its writer is missing)</fails_when>
    <automated>( cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build )</automated>
    <fails_when>non-zero exit, or "error TS" in the build output</fails_when>
  </verify>
  <done>The Activity Train card shows a guest-cohort line under its table, with the purged-guest exclusion footnoted, and the chart-layout harness is unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 3: Author the baseline note with its four queries and fill every number reachable from here</name>
  <precondition>The prod read-only tunnel is reachable: `bin/prod_db_tunnel.sh` starts (or is already running) and the `flawchess-prod-db` MCP server answers a `SELECT 1`. If it does not, stop and hand the unresolved numbers to Task 4 rather than inventing them.</precondition>
  <files>reports/growth/guest-activation-baseline-2026-09-17.md</files>
  <read_first>
    - `reports/growth/growth-recommendations-2026-09-15.md` (the sibling report: its dated-filename convention, the sources line, the top-of-file dated correction block, and finding 2's prose numbers — note it commits NO SQL, which is the deliberate divergence here)
    - `app/services/activity_queries.py` lines 471-491 (`fetch_funnel` — the cohort and "started an import" predicate to reuse verbatim for lever A metric 1) and 631-661 (`fetch_conversion` — lever B metric 1)
    - `224-RESEARCH.md` Finding I (why the growth report's numbers are not reproducible without new SQL, and the 2026-09-17 purged-rows correction) and Open Questions 3 and 4
    - `224-CONTEXT.md` D-10, S-7
    - `docs/dev-tooling.md` section on the three query-only PostgreSQL MCP servers
  </read_first>
  <action>
    Create `reports/growth/guest-activation-baseline-2026-09-17.md` (GUESTACT-12, per D-10a and
    ROADMAP SC 10). Structure, in this order:

    1. A header block naming the date, the window (the same 90-day window the 2026-09-15 growth report
       used, stated as an explicit date range so the after-reading is reproducible) and the sources
       (prod DB via the read-only `flawchess-prod-db` MCP over `bin/prod_db_tunnel.sh`; Umami, read by an
       operator). State that lever A and lever B are reported separately and that no combined "guest
       conversion" number is produced (S-7).
    2. **Lever A, metric 1 — guest import-start rate.** Commit the SQL, built by reusing `fetch_funnel`'s
       predicate verbatim: cohort `u.created_at >= CAST(:first AS date) AND u.games_purged_at IS NULL`,
       guest split on the `_GUEST_COHORT` predicate, started on
       `(u.first_import_started_at IS NOT NULL OR u.linked)`. Report started / cohort and the percentage.
       Note the prior published reading (226 of 468, 48%) for comparison.
    3. **Lever A, metric 2 — `/welcome` landings.** Leave the two numbers as an explicit
       `PENDING OPERATOR READING` placeholder resolved by Task 4, and commit the Umami query text /
       UI path that produces them so the after-reading is the same measurement. Note the prior published
       reading (390 of 1,051).
    4. **Lever B, metric 1 — guest promotion rate.** Commit `fetch_conversion`'s query verbatim and
       record `converted` / `sessions` plus the percentage. State that this number is already on the
       Activity dashboard's conversion card, so no hand-run SQL is needed after merge.
    5. **Lever B, metric 2 — guest Train sessions completed.** Commit the `fetch_guest_train` SQL from
       Task 1 verbatim and record its two numbers. Expect this to be 0 or near 0 pre-merge, and say so
       explicitly so a reader does not mistake a zero for a failed query.
    6. A short "what changed and what it invalidates" section recording: the `train-gate` `signup-cta`
       umami source stops being emitted by this phase (its historical rows stay in Umami and must not be
       summed into the after-reading), `welcome` survives as a source, and `train-score` is new.
    7. A closing note recording the D-09 acceptance: no new abuse guard is added; the backstop for the
       widened audience is `guest_create_limiter` (5 creates per hour per IP on the Cloudflare-resolved
       client IP) plus the one-open-session-per-user index and the one-session-per-day compose rule.

    Then start the tunnel and run the three DB-reachable queries through the read-only prod MCP, writing
    the real numbers into sections 2, 4 and 5. Do NOT invent, round from memory, or copy the growth
    report's prose numbers into the results rows — the whole point is that these were measured by these
    committed queries. If a query cannot run, write `UNRESOLVED — see checkpoint` in its result row
    rather than a number, and surface it at Task 4.
  </action>
  <acceptance_criteria>
    - `reports/growth/guest-activation-baseline-2026-09-17.md` exists and contains four fenced ```sql blocks
    - The file contains the section headings for all four metrics and the string "no combined"
    - Sections 2, 4 and 5 contain either a measured number or the exact string `UNRESOLVED — see checkpoint`
    - The file contains no number presented as measured that was copied from `growth-recommendations-2026-09-15.md`; prior published readings appear only in explicitly labelled comparison lines
    - `grep -n 'PENDING OPERATOR READING' reports/growth/guest-activation-baseline-2026-09-17.md` prints at least one line at the end of this task, in the lever A metric 2 section
  </acceptance_criteria>
  <verify>
    <automated>test -f reports/growth/guest-activation-baseline-2026-09-17.md &amp;&amp; [ "$(grep -n '```sql' reports/growth/guest-activation-baseline-2026-09-17.md | wc -l)" -ge 4 ]</automated>
    <fails_when>non-zero exit (the note is missing, or fewer than four SQL blocks are committed in it)</fails_when>
    <automated>grep -q 'no combined' reports/growth/guest-activation-baseline-2026-09-17.md &amp;&amp; grep -q 'guest_create_limiter' reports/growth/guest-activation-baseline-2026-09-17.md</automated>
    <fails_when>non-zero exit (the S-7 separation statement or the D-09 acceptance note is absent)</fails_when>
  </verify>
  <done>The baseline note exists with four committed queries and every number this environment can measure filled in.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Operator reading of the /welcome landings baseline from Umami</name>
  <action>Read two numbers out of Umami and paste them back, so the lever A landing baseline is recorded before this phase merges.</action>
  <instructions>
    Everything automatable is already done: `reports/growth/guest-activation-baseline-2026-09-17.md` is
    written, all four queries are committed in it, and the three prod-database numbers are filled in.
    Umami runs on its own database with credentials in `.env`, which the secret-read guard denies, so
    this one reading cannot be automated.

    Open https://analytics.flawchess.com (or `ssh flawchess` then
    `docker compose exec db psql -U "$UMAMI_DB_USER" umami`) and select the SAME window the note's header
    states (the 90-day window used by the 2026-09-15 growth report). Then read exactly TWO numbers:

      1. **`/welcome` landings** — the number of pageviews (or sessions, state which one you used) whose
         URL path is `/welcome` in that window. Prior published reading: 390.
      2. **Home visitors who clicked anything** — the denominator that 390 was reported against: visitors
         who landed on `/` and produced at least one subsequent event in that window. Prior published
         reading: 1,051.

    Paste them in the form `landings=<N> denominator=<M> basis=<pageviews|sessions>`.

    I will then replace the `PENDING OPERATOR READING` placeholder in section 3 of the note with those
    two numbers, the percentage, the basis you used, and the date of the reading, and commit the note.

    If `bin/prod_db_tunnel.sh` was not reachable during the previous task, any row reading
    `UNRESOLVED — see checkpoint` also needs you to either start the tunnel and say "tunnel up" (I will
    re-run those queries) or paste the numbers yourself.
  </instructions>
  <verification>After you paste them, `grep -c 'PENDING OPERATOR READING' reports/growth/guest-activation-baseline-2026-09-17.md` returns 0 and `grep -c 'UNRESOLVED' reports/growth/guest-activation-baseline-2026-09-17.md` returns 0.</verification>
  <resume-signal>Paste `landings=<N> denominator=<M> basis=<pageviews|sessions>` (add `tunnel up` if you started the tunnel).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> `/activity` payload | Aggregate analytics served to a superuser; no per-user identifiers cross it |
| operator laptop -> prod Postgres (read-only role, SSH tunnel) | Baseline measurement path |
| operator laptop -> Umami UI | Separate database, credentials never read by the agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-224-09 | Information Disclosure | new `fetch_guest_train` SQL | medium | mitigate | The query returns two aggregate integers and no user identifier, email or session id. It runs on the same read-only connection (`default_transaction_read_only=on`) every other dashboard query uses, and the endpoint is already superuser-gated. |
| T-224-10 | Tampering | SQL injection through the window parameter | high | mitigate | `CAST(:first AS date)` named binding, never f-string interpolation of a value; the only f-string content is the module-level `_GUEST_COHORT` constant. Task 1's acceptance criteria assert both. |
| T-224-11 | Information Disclosure | prod credentials / `.env` | high | mitigate | The agent never reads `.env`; Umami access is an operator checkpoint, and the prod DB is reached through the existing read-only MCP role over the tunnel. |
| T-224-12 | Repudiation | a baseline number with no reproducible provenance | medium | mitigate | Every metric's SQL (or Umami query path) is committed next to its number, and Task 3 forbids copying the prior report's prose numbers into the measured rows. |
| T-224-SC | Tampering | npm/pip/cargo installs | high | accept | This plan runs no `npm install`, `uv add` or lockfile change; `224-RESEARCH.md` Package Legitimacy Audit records "not applicable — this phase installs no external packages". |
</threat_model>

<verification>
- `uv run pytest tests/test_admin_activity_stats.py -x` green
- `uv run ty check app/ tests/ scripts/` zero errors
- `( cd frontend && npm run lint && npm run build && npm run check:activity-layout )` green
- `reports/growth/guest-activation-baseline-2026-09-17.md` contains four numbers and four SQL blocks, no placeholder left
</verification>

<success_criteria>
- The Activity Train card reports the guest cohort with the purged exclusion footnoted (GUESTACT-13, D-10b/D-11/D-12)
- The baseline note holds all four pre-merge numbers with their queries committed (GUESTACT-12, ROADMAP SC 10)
- No combined guest conversion number is produced anywhere (S-7)
</success_criteria>

<output>
Create `.planning/phases/224-guest-activation-welcome-removal-and-guest-train/224-03-SUMMARY.md` when done
</output>
