---
phase: 23-launch-readiness
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - frontend/src/pages/Privacy.tsx
  - frontend/src/App.tsx
  - frontend/index.html
  - frontend/public/robots.txt
  - frontend/public/sitemap.xml
autonomous: true
requirements: [CONT-03, CONT-02]

must_haves:
  truths:
    - "Privacy policy page is accessible at /privacy without authentication"
    - "Privacy page lists all data collected and third-party processors per D-17 and D-18"
    - "index.html has correct title, meta description, and Open Graph tags"
    - "robots.txt allows / and /privacy, disallows authenticated routes"
    - "sitemap.xml lists / and /privacy with correct XML namespace"
  artifacts:
    - path: "frontend/src/pages/Privacy.tsx"
      provides: "Privacy policy page with all required sections"
      exports: ["PrivacyPage"]
      min_lines: 60
    - path: "frontend/index.html"
      provides: "SEO meta tags and Open Graph tags"
      contains: "og:title"
    - path: "frontend/public/robots.txt"
      provides: "Search engine crawling rules"
      contains: "Sitemap:"
    - path: "frontend/public/sitemap.xml"
      provides: "URL index for search engines"
      contains: "sitemaps.org"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Privacy.tsx"
      via: "Route path=/privacy"
      pattern: '<Route path="/privacy"'
    - from: "frontend/public/sitemap.xml"
      to: "frontend/public/robots.txt"
      via: "Sitemap: URL in robots.txt"
      pattern: "Sitemap: https://flawchess.com/sitemap.xml"
---

<objective>
Create the privacy policy page and add SEO fundamentals (meta tags, robots.txt, sitemap.xml).

Purpose: Legal compliance requires a privacy policy naming data processors. SEO tags enable proper social media previews and search engine indexing of public pages.
Output: Privacy page at /privacy, OG tags in index.html, robots.txt and sitemap.xml in public/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-launch-readiness/23-CONTEXT.md
@.planning/phases/23-launch-readiness/23-RESEARCH.md
@.planning/phases/23-launch-readiness/23-UI-SPEC.md
@.planning/phases/23-launch-readiness/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From frontend/src/components/layout/PublicHeader.tsx:
```typescript
export function PublicHeader(): JSX.Element;
// Header with logo + login/signup for public pages
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Privacy policy page</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/components/layout/PublicHeader.tsx
    - frontend/src/pages/Home.tsx
    - .planning/phases/23-launch-readiness/23-CONTEXT.md
    - .planning/phases/23-launch-readiness/23-UI-SPEC.md
  </read_first>
  <files>frontend/src/pages/Privacy.tsx, frontend/src/App.tsx</files>
  <action>
1. Create `frontend/src/pages/Privacy.tsx`:
   - Export `PrivacyPage` component
   - Import and render `PublicHeader` at top (same as homepage)
   - Content wrapper: `<main className="max-w-2xl mx-auto px-4 py-12" data-testid="privacy-page">`
   - Page heading: `<h1 className="text-4xl font-bold">Privacy Policy</h1>` (Display role, left-aligned per UI-SPEC)
   - Subheading: `<p className="mt-2 text-sm text-muted-foreground">Last updated: March 2026</p>`

   Sections (per D-15 through D-20), each as `<section className="mt-8">` with `<h2 className="text-xl font-bold mb-3">`:

   **"What we collect"** section:
   - Chess.com and lichess usernames you enter (publicly available information)
   - Game data imported from those platforms (publicly accessible via their APIs)
   - Email address and password if you register with email (password is hashed using bcrypt, never stored in plain text)
   - Google account email address if you sign in with Google

   **"Who we share it with"** section:
   - **Sentry** (sentry.io) — Error monitoring. When something goes wrong, Sentry may capture your IP address, browser information, and the action that triggered the error. This helps us fix bugs.
   - **Hetzner** (hetzner.com) — Hosting. Our server is located in Germany (EU). Hetzner processes data as required to host the application.
   - We do not sell, rent, or share your data with anyone else. We do not run advertising. We do not use tracking cookies.

   **"Your rights"** section:
   - You can request deletion of your account and all associated data by emailing `<a href="mailto:support@flawchess.com" className="text-primary underline-offset-4 hover:underline">support@flawchess.com</a>` (per D-19).
   - Upon request, we will delete your account, all imported games, and any associated data.

   **"Open source"** section (per D-20):
   - FlawChess is open source. You can verify exactly what data we collect and how we handle it by reading the code on `<a href="https://github.com/flawchess/flawchess" target="_blank" rel="noopener noreferrer" className="text-primary underline-offset-4 hover:underline">GitHub</a>`.

   Contact section at bottom: `<section className="mt-8" data-testid="privacy-contact">` with:
   - "Questions? Contact us at `<a href="mailto:support@flawchess.com" ...>support@flawchess.com</a>`."

   Page footer: same minimal footer as homepage (copyright + link back to homepage).

   **Browser tab title (D-14):** Add a `useEffect` to set `document.title = 'Privacy Policy | FlawChess'` on mount, and restore the default title on unmount (`return () => { document.title = 'FlawChess — Chess Opening Analysis'; }`). This satisfies D-14 without a head management library (D-09 compliant).

2. Update `frontend/src/App.tsx`:
   - Replace the inline PrivacyPage placeholder (if created in Plan 01) with the real import:
     `import { PrivacyPage } from '@/pages/Privacy';`
   - Remove any inline placeholder PrivacyPage function.
   - The route `<Route path="/privacy" element={<PrivacyPage />} />` should already exist from Plan 01.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Privacy.tsx exists and contains `export function PrivacyPage`
    - frontend/src/pages/Privacy.tsx contains `data-testid="privacy-page"`
    - frontend/src/pages/Privacy.tsx contains `data-testid="privacy-contact"`
    - frontend/src/pages/Privacy.tsx contains `What we collect`
    - frontend/src/pages/Privacy.tsx contains `Who we share it with`
    - frontend/src/pages/Privacy.tsx contains `Your rights`
    - frontend/src/pages/Privacy.tsx contains `Open source`
    - frontend/src/pages/Privacy.tsx contains `support@flawchess.com`
    - frontend/src/pages/Privacy.tsx contains `Sentry`
    - frontend/src/pages/Privacy.tsx contains `Hetzner`
    - frontend/src/pages/Privacy.tsx contains `PublicHeader`
    - frontend/src/pages/Privacy.tsx contains `document.title`
    - frontend/src/pages/Privacy.tsx contains `Privacy Policy | FlawChess`
    - frontend/src/App.tsx imports `PrivacyPage` from `@/pages/Privacy` (not inline)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Privacy policy page at /privacy with all required sections: data collected, third-party processors (Sentry, Hetzner), deletion rights, open source mention. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEO meta tags, robots.txt, and sitemap.xml</name>
  <read_first>
    - frontend/index.html
    - .planning/phases/23-launch-readiness/23-CONTEXT.md
    - .planning/phases/23-launch-readiness/23-RESEARCH.md
    - .planning/phases/23-launch-readiness/23-UI-SPEC.md
  </read_first>
  <files>frontend/index.html, frontend/public/robots.txt, frontend/public/sitemap.xml</files>
  <action>
1. Modify `frontend/index.html` — add meta tags inside `<head>`, replacing existing `<title>`:
   ```html
   <title>FlawChess — Chess Opening Analysis</title>
   <meta name="description" content="Analyze your chess openings by position, not just name. Import games from chess.com and lichess to discover where you really lose." />

   <!-- Open Graph -->
   <meta property="og:title" content="FlawChess — Chess Opening Analysis" />
   <meta property="og:description" content="Analyze your chess openings by position, not just name. Import games from chess.com and lichess to discover where you really lose." />
   <meta property="og:image" content="https://flawchess.com/og-image.jpg" />
   <meta property="og:url" content="https://flawchess.com/" />
   <meta property="og:type" content="website" />

   <!-- Twitter/X Card -->
   <meta name="twitter:card" content="summary_large_image" />
   <meta name="twitter:title" content="FlawChess — Chess Opening Analysis" />
   <meta name="twitter:description" content="Analyze your chess openings by position, not just name. Import games from chess.com and lichess to discover where you really lose." />
   <meta name="twitter:image" content="https://flawchess.com/og-image.jpg" />
   ```
   Keep all existing meta tags (charset, viewport, theme-color) and links (icon, fonts, apple-touch-icon, manifest). Only replace the `<title>` line and add the new meta/OG tags.

2. Create `frontend/public/robots.txt`:
   ```
   User-agent: *
   Allow: /
   Allow: /privacy
   Disallow: /import
   Disallow: /openings
   Disallow: /global-stats
   Disallow: /api/

   Sitemap: https://flawchess.com/sitemap.xml
   ```

3. Create `frontend/public/sitemap.xml`:
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
     <url><loc>https://flawchess.com/</loc></url>
     <url><loc>https://flawchess.com/privacy</loc></url>
   </urlset>
   ```

   Note: No Caddyfile changes needed — the existing `@static file` matcher in the Caddyfile already serves any file in `/srv` (which is where Vite's `public/` contents are copied during build). robots.txt and sitemap.xml will be served automatically.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run build && test -f dist/robots.txt && test -f dist/sitemap.xml</automated>
  </verify>
  <acceptance_criteria>
    - frontend/index.html contains `<title>FlawChess — Chess Opening Analysis</title>`
    - frontend/index.html contains `og:title`
    - frontend/index.html contains `og:description`
    - frontend/index.html contains `og:image" content="https://flawchess.com/og-image.jpg"`
    - frontend/index.html contains `og:url" content="https://flawchess.com/"`
    - frontend/index.html contains `og:type" content="website"`
    - frontend/index.html contains `twitter:card" content="summary_large_image"`
    - frontend/index.html contains `name="description"`
    - frontend/public/robots.txt exists and contains `Sitemap: https://flawchess.com/sitemap.xml`
    - frontend/public/robots.txt contains `Disallow: /api/`
    - frontend/public/robots.txt contains `Allow: /privacy`
    - frontend/public/sitemap.xml exists and contains `xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"`
    - frontend/public/sitemap.xml contains `https://flawchess.com/`
    - frontend/public/sitemap.xml contains `https://flawchess.com/privacy`
    - After `npm run build`, dist/robots.txt and dist/sitemap.xml exist
  </acceptance_criteria>
  <done>SEO meta tags in index.html with OG and Twitter cards. robots.txt and sitemap.xml in public/ directory, served as static files. Build includes both files in dist/.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passes
- `test -f frontend/dist/robots.txt && test -f frontend/dist/sitemap.xml` both exist
- `grep "og:title" frontend/index.html` returns match
- `grep "Privacy Policy" frontend/src/pages/Privacy.tsx` returns match
</verification>

<success_criteria>
- Privacy page accessible at /privacy with all required sections
- index.html has title, description, OG tags, and Twitter cards
- robots.txt and sitemap.xml served as static files from build output
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-readiness/23-03-SUMMARY.md`
</output>
