---
phase: 125-backfill-tactic-motifs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_backfill_flaws.py
  - scripts/coverage_report_tactic_motifs.py
autonomous: true
requirements: [TACSCH-03]

must_haves:
  truths:
    - "A pytest test seeds a GamePosition with a pv at flaw_ply+1, runs run_backfill, and asserts the resulting GameFlaw row has tactic_motif IS NOT NULL when a detector fires"
    - "The same test asserts tactic_motif IS NULL when no pv is present at flaw_ply+1 (the no-PV NULL bucket)"
    - "The existing Phase 108 dry-run / real-run / idempotency tests in tests/test_backfill_flaws.py still pass"
    - "scripts/coverage_report_tactic_motifs.py runs read-only SQL and prints the four D-04 sections: overall coverage %, by-motif counts, NULL split (no-PV vs PV-but-no-fire), and a spot-check sample per NULL bucket"
    - "The coverage report accepts --db dev|benchmark|prod (re-runnable on prod) and writes no rows"
  artifacts:
    - path: "tests/test_backfill_flaws.py"
      provides: "Tactic-column integration assertion extending the Phase 108 suite"
      contains: "tactic_motif"
    - path: "scripts/coverage_report_tactic_motifs.py"
      provides: "Read-only D-04 coverage + NULL-breakdown report, re-runnable on prod"
      contains: "full_evals_completed_at"
  key_links:
    - from: "scripts/coverage_report_tactic_motifs.py"
      to: "app/core/config.py"
      via: "db_url_for_target(target) resolves the --db target to a DATABASE_URL_* setting"
      pattern: "db_url_for_target"
    - from: "scripts/coverage_report_tactic_motifs.py"
      to: "app/services/tactic_detector.py"
      via: "_INT_TO_MOTIF maps tactic_motif ints to readable motif names in the by-motif table"
      pattern: "_INT_TO_MOTIF|TacticMotifInt"
---

<objective>
Build the two net-new code artifacts this phase needs BEFORE any DB mutation: (1) extend the
existing backfill test suite to assert the tactic columns (Nyquist Wave 0 gap — the Phase 108
suite predates Phase 124 and never asserts `tactic_motif`), and (2) a standalone read-only
coverage-report script implementing D-04, re-runnable on dev/benchmark/prod.

Purpose: The Wave 0 test gives an automated feedback signal that the backfill writes tactic
columns at all (the operational dev run in Plan 02 cannot be a pytest). The coverage report is
the SC#1 acceptance instrument ("coverage is honest; NULL = genuine no-fire, not skipped") and
is the same tool the deferred prod runbook (Plan 03) will invoke.

Output: `tests/test_backfill_flaws.py` (one new test) + `scripts/coverage_report_tactic_motifs.py`.
No DB rows are written by either artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-backfill-tactic-motifs/125-CONTEXT.md
@.planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md
@.planning/phases/125-backfill-tactic-motifs/125-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test_backfill_flaws.py to assert tactic columns (Nyquist Wave 0)</name>
  <files>tests/test_backfill_flaws.py</files>
  <read_first>
    - tests/test_backfill_flaws.py (the full existing suite — the `committed_analyzed_game` fixture seeds 10 positions plies 0-9 with `move_san=None` and no `pv`; blunder at ply 2, mistake at ply 4; tests pass `db="dev"` but inject `session_maker` so `db` is ignored)
    - scripts/backfill_flaws.py (run_backfill signature ~line 112; it accepts `session_maker`; the classify path reads `positions[n+1].pv`)
    - app/services/flaws_service.py lines ~373-391 (`_detect_tactic_for_flaw`: requires all three of `fen_before_flaw` from PGN replay at ply n, `pv` from `positions[n+1].pv`, and `move_san_of_flaw` from `positions[n].move_san`; returns None,None,None otherwise)
    - app/services/tactic_detector.py lines 58-150 (TacticMotifInt enum + _INT_TO_MOTIF mapping)
    - app/models/game_flaw.py lines 69-75 (tactic_motif / tactic_piece / tactic_confidence columns, all nullable)
    - app/models/game_position.py line 183 (`pv: Mapped[Optional[str]]`)
  </read_first>
  <behavior>
    - Test A (no-PV NULL path): with the existing fixture (no `pv` set on any position, `move_san=None`), after a real `run_backfill`, every materialized GameFlaw row has `tactic_motif IS NULL` and `tactic_piece IS NULL` — proves the no-PV bucket is honest, not an error.
    - Test B (PV-fires path): seed a `pv` at `blunder_ply + 1` (and the required `move_san` at `blunder_ply`) with a refutation line that fires a detector, run `run_backfill`, assert the blunder row has `tactic_motif IS NOT NULL` AND `tactic_confidence IS NOT NULL`.
  </behavior>
  <action>
    Add a new test class (e.g. `TestBackfillTacticColumns`) to tests/test_backfill_flaws.py. Do NOT modify the existing `committed_analyzed_game` fixture in place (the existing dry-run/real-run/idempotency tests depend on its current shape) — instead add a sibling committed fixture that extends the seed with a `pv` and a `move_san` at the flawed ply, OR parametrize a new local fixture.

    Test A reuses the existing `committed_analyzed_game` fixture: run `run_backfill(db="dev", user_id=game.user_id, dry_run=False, limit=None, session_maker=session_factory)`, then query the GameFlaw rows and assert all have `tactic_motif is None` (no `pv` was seeded, so `_detect_tactic_for_flaw` short-circuits — this is the honest no-PV NULL bucket).

    Test B uses a new committed fixture that seeds the same game but sets `move_san` on the position at the blunder ply and a `pv` string at `blunder_ply + 1`. To pick a pv that reliably fires a detector, derive it deterministically: import `detect_tactic_motif` from app.services.tactic_detector and, in the test setup, compute the board FEN after the flawed move from the PGN, then choose a pv whose first move produces a recognized motif. Simpler and more robust: reuse a known-firing (board_fen, pv) pair from the Phase 124 detector fixtures in tests/services/test_tactic_detector.py — read that file, copy one validated fork/hanging-piece case, and construct the fixture game so the flawed-move board matches that FEN. Assert the corresponding GameFlaw row has `tactic_motif is not None` and `tactic_confidence is not None`. If aligning the fixture board to a detector fixture proves brittle, instead assert the weaker-but-sufficient property that WITH a pv seeded the detection path is exercised (no exception) AND the no-pv control row stays NULL — but prefer the strong assertion.

    Use named constants for any cp/ply literals (CLAUDE.md no-magic-numbers); explicit return types on all test helpers. No `text-xs` / frontend concerns here.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backfill_flaws.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backfill_flaws.py -q` passes including the new class and all 5 pre-existing tests.
    - The new test asserts BOTH `tactic_motif IS NOT NULL` (pv-fires path) and `tactic_motif IS NULL` (no-pv path) — grep the test file shows both an `is not None` and an `is None` assertion on `tactic_motif`.
    - No existing test was deleted or had its fixture shape changed (the 5 Phase 108 tests still reference `committed_analyzed_game` unmodified).
  </acceptance_criteria>
  <done>tests/test_backfill_flaws.py contains a passing tactic-column test covering both the PV-fires and no-PV NULL paths; the full file passes pytest with the Phase 108 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/coverage_report_tactic_motifs.py (D-04 read-only report)</name>
  <files>scripts/coverage_report_tactic_motifs.py</files>
  <read_first>
    - scripts/backfill_eval.py lines 1-130 (the `--db dev|benchmark|prod` precedent: argparse `--db` choices, `_db_url`/`db_url_for_target`, async main, `_log` timestamped output, `create_async_engine` + `async_sessionmaker`, sys.path bootstrap for `app.*` imports)
    - scripts/backfill_flaws.py lines 30-69 (the sys.path bootstrap + `_log` pattern to mirror)
    - app/core/config.py lines 98-130 (`db_url_for_target(target)` resolves the URL; raises ValueError on unknown target)
    - app/services/tactic_detector.py lines 123-150 (`_INT_TO_MOTIF` for the by-motif name table) and lines 58-85 (TacticMotifInt)
    - app/models/game_flaw.py (table `game_flaws`, columns `user_id, game_id, ply, severity` [1=mistake,2=blunder], `tactic_motif`)
    - app/models/game_position.py (table `game_positions`, composite PK `(user_id, game_id, ply)`, column `pv`)
    - app/models/game.py (column `full_evals_completed_at`)
    - .planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md "Pattern 4: Coverage Report SQL" and "D-04 Coverage Report: Concrete Recommendation" sections (the exact join shape + required output sections + dev ground-truth numbers: 68,150 M+B flaws, 12,916 with PV / 55,234 without)
  </read_first>
  <action>
    Create scripts/coverage_report_tactic_motifs.py following the backfill_eval.py structure: sys.path bootstrap, `--db` argparse with choices `["dev","benchmark","prod"]` (required), resolve the URL via `db_url_for_target`, `create_async_engine(url, pool_pre_ping=True)` + `async_sessionmaker(expire_on_commit=False)`, async `main`, `_log` timestamped output. The script is READ-ONLY — never writes, deletes, or commits mutations.

    Implement the four D-04 sections using the verified SQL from RESEARCH Pattern 4 (use SQLAlchemy `select()` with `text()` or core constructs; bind all parameters — no string interpolation of user data, though `--db` is a fixed-choice value):

    1. Overall: total M+B flaw rows on full-eval'd games (`games.full_evals_completed_at IS NOT NULL AND game_flaws.severity IN (1,2)`), count with PV at `ply+1`, count tagged (`tactic_motif IS NOT NULL`), and the two NULL buckets. Print absolute counts AND percentages (% non-NULL of has-PV rows; % no-PV of total). Extract any threshold/severity literals into named constants (e.g. `_MISTAKE_BLUNDER_SEVERITIES = (1, 2)`).
    2. By-motif counts: GROUP BY `tactic_motif` where non-NULL, joined/mapped through `_INT_TO_MOTIF` to print the readable motif name + count, ordered by count desc. Also print a line for each of the 8 query-suppressed motifs that did NOT fire so their absence is visible (read the suppressed set from tests/services/test_tactic_detector.py `_QUERY_SUPPRESSED_MOTIFS` or list them inline as a named constant — do NOT filter them out; Phase 125 stores all motifs).
    3. NULL split: print `no_pv_null` (flaws with no PV at `ply+1`) vs `pv_no_fire` (PV present but `tactic_motif IS NULL`) with counts and a one-line interpretation that NULL = honest (no-PV or low-confidence), not skipped.
    4. Spot-check: SELECT a small sample (named constant `_SPOT_CHECK_SAMPLE_SIZE = 3` or similar) from each NULL bucket — for no_pv_null, a few `(user_id, game_id, ply, severity)` rows confirming `pv IS NULL`; for pv_no_fire, a few rows showing the `pv` text so a human can eyeball that no detector should have fired.

    Use the LEFT JOIN to `game_positions` at `gp_next.ply = gf.ply + 1` (composite key incl. user_id + game_id) exactly as in RESEARCH Pattern 4. Add an explicit return type to every function; pass `uv run ty check`. Keep functions small (one function per report section, a thin `main` orchestrator listing the section calls — per CLAUDE.md function-size limits). Do NOT add query-time motif suppression to the report.
  </action>
  <verify>
    <automated>uv run python -c "import ast,sys; ast.parse(open('scripts/coverage_report_tactic_motifs.py').read()); print('parse-ok')" && uv run ruff check scripts/coverage_report_tactic_motifs.py && uv run ty check app/ tests/</automated>
    <human-check>Run `uv run python scripts/coverage_report_tactic_motifs.py --db dev` against the pre-backfill dev DB and confirm it prints all four sections; pre-backfill the tagged count is 0 (baseline), no_pv_null ~55,234, has-PV ~12,916. (Full numeric verification happens in Plan 02 after the backfill.)</human-check>
  </verify>
  <acceptance_criteria>
    - File parses, passes `uv run ruff check`, and `uv run ty check app/ tests/` reports zero errors.
    - argparse exposes `--db` with choices dev/benchmark/prod and the script resolves the URL via `db_url_for_target`.
    - The script issues only SELECT statements (grep shows no INSERT/UPDATE/DELETE/`commit(`).
    - Output contains all four labeled sections (overall coverage, by-motif counts, NULL split, spot-check samples) and the by-motif section references `_INT_TO_MOTIF` so ints render as names.
  </acceptance_criteria>
  <done>scripts/coverage_report_tactic_motifs.py exists, is read-only, takes `--db dev|benchmark|prod`, prints the four D-04 sections with names+percentages, and passes ruff + ty.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This phase (across all plans) creates the following NEW symbols/files — the plan-review source-grounding pass must treat these as net-new (not drift):
- `scripts/coverage_report_tactic_motifs.py` (NEW script — D-04 coverage report)
- A new test class/function in `tests/test_backfill_flaws.py` (e.g. `TestBackfillTacticColumns`) asserting the tactic columns
- `.planning/phases/125-backfill-tactic-motifs/PROD-RUNBOOK.md` (NEW doc — created in Plan 03)
No new ORM columns, migrations, API routes, or frontend symbols are created (the schema + detector already shipped in Phase 124).
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_backfill_flaws.py -q` green (including the new tactic-column test).
- `uv run ruff check scripts/coverage_report_tactic_motifs.py tests/test_backfill_flaws.py` clean.
- `uv run ty check app/ tests/` zero errors.
- `scripts/coverage_report_tactic_motifs.py` is read-only (no mutating SQL).
</verification>

<success_criteria>
The Nyquist Wave 0 gap is closed (tactic columns asserted) and the SC#1 acceptance instrument
(coverage report) exists and runs read-only on any --db target, ready for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/125-backfill-tactic-motifs/125-01-SUMMARY.md` when done.
</output>
