---
phase: 125-backfill-tactic-motifs
plan: 02
type: execute
wave: 1
depends_on: ["125-01"]
files_modified:
  - .planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md
autonomous: true
requirements: [TACSCH-03]

must_haves:
  truths:
    - "A --dry-run --limit smoke test of backfill_flaws.py --db dev --full-evald-only runs clean before the full run (D-02)"
    - "The full dev backfill (backfill_flaws.py --db dev --full-evald-only) completes and writes tactic_motif/tactic_piece for flaws where the detector fires (D-02)"
    - "The coverage report on dev shows honest coverage: no_pv_null bucket matches the pre-backfill no-PV count (~55,234), the by-motif table lists fired motifs, and the pv_no_fire bucket is the only PV-present-but-NULL bucket (SC#1, D-04)"
    - "Re-running the backfill on a --limit sample produces identical rows (no duplication, no corruption) — idempotency demonstrated, not just asserted (SC#3, D-05)"
    - "Non-tactic game_flaws columns are byte-identical before/after on a sample of games with >9 positions that already had flaw rows; any net-new rows are confined to the ~126 short games per the 831bae38 coverage-fix (D-06)"
  artifacts:
    - path: ".planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md"
      provides: "Recorded dev backfill output, coverage report numbers, idempotency proof, and D-06 blast-radius result"
  key_links:
    - from: "scripts/backfill_flaws.py"
      to: "game_flaws.tactic_motif"
      via: "classify_game_flaws -> _detect_tactic_for_flaw -> flaw_record_to_row writes the tactic columns"
      pattern: "tactic_motif"
    - from: "scripts/coverage_report_tactic_motifs.py"
      to: "game_flaws / game_positions"
      via: "read-only LEFT JOIN at ply+1 proving the no-PV vs PV-but-no-fire NULL split"
      pattern: "full_evals_completed_at"
---

<objective>
Run the tactic-motif backfill on the live dev DB and PROVE it is honest, idempotent, and
blast-radius-safe. This is the operational core of Phase 125 (D-01: phase completes on dev).

Purpose: TACSCH-03 / SC#1+SC#3. The backfill script already does the work (no code change);
this plan exercises it on dev's ~11,199 full-eval'd games, then uses the Plan 01 coverage
report to verify SC#1 ("coverage honest; NULL = genuine no-fire"), demonstrates SC#3 idempotency
by re-run, and confirms D-06 (non-tactic columns unchanged).

Output: a SUMMARY recording the actual dev numbers (overall %, by-motif table, NULL split,
idempotency diff, D-06 result). These observed numbers feed the Plan 03 prod runbook.

NOTE: this plan MUTATES the live dev DB (recomputes game_flaws). The dev DB is up
(PostgreSQL 18 in Docker). Do NOT run `bin/reset_db.sh` — verification works against the
existing dev DB (project rule). The recompute is idempotent and concurrent-safe (D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-backfill-tactic-motifs/125-CONTEXT.md
@.planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md
@.planning/phases/125-backfill-tactic-motifs/125-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture D-06 before-snapshot, smoke-test, then run the full dev backfill (D-02, D-06)</name>
  <files>.planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md</files>
  <read_first>
    - scripts/backfill_flaws.py (the full script — `--full-evald-only` targeting at lines 159-171, batch loop, delete-then-insert at lines 241-253, BACKFILL_GAMES_PER_BATCH=100)
    - .planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md "Confirmed CLI Invocations", "Dev DB Ground Truth" (11,199 full-eval'd games; pre-backfill tactic_motif=0), and "D-06 Blast-Radius Verification Approach" (the COPY before/after snapshot on non-tactic columns, scoped to games with >9 positions; the ~126 short games may legitimately gain rows from the 831bae38 coverage fix)
    - .planning/phases/125-backfill-tactic-motifs/125-CONTEXT.md D-02, D-03, D-06
    - CLAUDE.md "Commands" (dev DB up: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`) and "Database Access (MCP)" (dev DB url is `postgresql://flawchess:flawchess@localhost:5432/flawchess`)
  </read_first>
  <action>
    Confirm the dev DB is up (`docker compose -f docker-compose.dev.yml -p flawchess-dev ps`); if not, bring it up with the compose command from CLAUDE.md (do NOT reset it).

    Step 0 — D-06 before-snapshot: capture the non-tactic columns for a fixed sample of full-eval'd games that already have flaw rows and have >9 positions (excludes the ~126 short games that may legitimately gain rows). Use a deterministic ordered sample (e.g. first 50 such games by id). Save the snapshot to `/tmp/flaws_before.csv` via `psql` `COPY (... ORDER BY user_id, game_id, ply) TO ...` selecting `user_id, game_id, ply, severity, tempo, phase` plus the other non-tactic flaw columns present on the model (read app/models/game_flaw.py to enumerate them — explicitly EXCLUDE `tactic_motif, tactic_piece, tactic_confidence`). Record the exact game-id sample list in the SUMMARY so Step in Task 2 re-snapshots the SAME games.

    Step 1 — smoke (D-02): `uv run python scripts/backfill_flaws.py --db dev --full-evald-only --dry-run --limit 20`. Confirm it classifies without error and reports a non-zero flaw count, writing nothing.

    Step 2 — full run: `uv run python scripts/backfill_flaws.py --db dev --full-evald-only`. Let it rip (D-03 — no throttle; pure CPU, no Stockfish; benign race with any active eval drain). Capture the final summary lines (games processed, skipped, errors, flaw rows written).

    Record all three steps' outputs verbatim in the SUMMARY. Do NOT modify scripts/backfill_flaws.py — it needs no change.
  </action>
  <verify>
    <automated>test -f /tmp/flaws_before.csv && uv run python scripts/backfill_flaws.py --db dev --full-evald-only --dry-run --limit 20 2>&1 | grep -i -E 'flaw rows|dry-run'</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/flaws_before.csv` exists, has a header row + data rows, and contains NONE of the three tactic columns (grep the header shows no `tactic_motif`).
    - The dry-run smoke output reports a non-zero "flaw rows (dry-run)" count and exits 0.
    - The full-run summary in the SUMMARY shows games processed > 0 and errors = 0 (or each error captured/explained).
    - The recorded D-06 sample game-id list is fixed and reproducible (an explicit list, not "the first 50" prose).
  </acceptance_criteria>
  <done>The dev DB has a populated game_flaws.tactic_motif set; the dry-run smoke passed; a before-snapshot of non-tactic columns for a fixed >9-position game sample is saved to /tmp/flaws_before.csv and its sample list recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run coverage report (SC#1/D-04), idempotency re-run (SC#3/D-05), and D-06 after-diff</name>
  <files>.planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md</files>
  <read_first>
    - scripts/coverage_report_tactic_motifs.py (the Plan 01 report — its four sections and --db surface)
    - scripts/backfill_flaws.py lines 241-253 (delete-then-insert per (game_id, user_id) — the idempotency mechanism)
    - .planning/phases/125-backfill-tactic-motifs/125-RESEARCH.md "Pitfall 1" (sparse PV: ~19% of M+B flaws have PV — a LOW overall non-NULL % is EXPECTED, not a failure; the bar is "NULL = honest"), "Pitfall 2" (D-06 short-game new rows are correct), and "D-04 Coverage Report" acceptance bar (no_pv_null must match pre-backfill ~55,234 on dev)
    - .planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md (the fixed D-06 game-id sample list recorded in Task 1)
    - .planning/phases/125-backfill-tactic-motifs/125-CONTEXT.md D-04, D-05, D-06
  </read_first>
  <action>
    Step 1 — coverage report (SC#1, D-04): run `uv run python scripts/coverage_report_tactic_motifs.py --db dev`. Record the full output in the SUMMARY. Interpret it explicitly: confirm (a) overall non-NULL tagged count > 0, (b) the by-motif table lists which detectors fired (and which of the 8 query-suppressed motifs are absent vs present), (c) the NULL split — `no_pv_null` should match the pre-backfill no-PV count (~55,234 per RESEARCH) and `pv_no_fire` is the only PV-present-but-NULL bucket, (d) the spot-check samples look sane. Frame a low overall non-NULL % as EXPECTED (Pitfall 1: only ~19% of M+B flaws have a PV at ply+1 on dev) — the acceptance bar is "NULL = honest no-fire / no-PV, not skipped", NOT a coverage threshold.

    Step 2 — idempotency (SC#3, D-05): pick a fixed `--user-id` (or rely on `--limit`) sample, snapshot its flaw rows (all columns) to `/tmp/flaws_idem_a.csv` via psql COPY, re-run `uv run python scripts/backfill_flaws.py --db dev --full-evald-only --user-id <id>` (or `--limit <n>`), snapshot again to `/tmp/flaws_idem_b.csv`, and `diff` the two. The diff MUST be empty (delete-then-insert produces byte-identical rows). Record the diff result (empty) in the SUMMARY.

    Step 3 — D-06 after-diff: re-snapshot the SAME fixed game-id sample from Task 1 (>9 positions, pre-existing flaw rows) to `/tmp/flaws_after.csv` using the identical SELECT/ORDER BY, and `diff /tmp/flaws_before.csv /tmp/flaws_after.csv`. The diff MUST be empty — non-tactic columns are byte-identical (Phase 124 only added tactic detection; no severity/tempo/phase/fen logic changed). Document that net-new rows are expected ONLY for the ~126 short games (excluded from this sample by the >9-position filter). If the diff is non-empty on the sampled games, STOP and flag drift (do not hand-wave).
  </action>
  <verify>
    <automated>uv run python scripts/coverage_report_tactic_motifs.py --db dev 2>&1 | grep -i -E 'no.?pv|by.?motif|coverage|null' && diff /tmp/flaws_before.csv /tmp/flaws_after.csv && diff /tmp/flaws_idem_a.csv /tmp/flaws_idem_b.csv && echo "ALL_DIFFS_EMPTY"</automated>
  </verify>
  <acceptance_criteria>
    - The coverage report prints all four D-04 sections; tagged (non-NULL) count > 0; `no_pv_null` matches the pre-backfill no-PV count within expected drift (any active eval drain may have added a few PVs — note the delta if so).
    - Idempotency: `diff /tmp/flaws_idem_a.csv /tmp/flaws_idem_b.csv` is EMPTY (SC#3 demonstrated, not just asserted).
    - D-06: `diff /tmp/flaws_before.csv /tmp/flaws_after.csv` is EMPTY on the >9-position sample (non-tactic columns byte-identical); the combined verify chain prints `ALL_DIFFS_EMPTY`.
    - The SUMMARY records the coverage interpretation (esp. that a low overall non-NULL % is expected per the sparse-PV reality), the empty idempotency diff, and the empty D-06 diff.
  </acceptance_criteria>
  <done>SC#1 (honest coverage via the report + NULL split), SC#3 (idempotency demonstrated by empty re-run diff), and D-06 (non-tactic columns unchanged on the sample) are all verified and recorded in the SUMMARY.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no NEW source symbols — it RUNS existing tooling (`scripts/backfill_flaws.py`,
unchanged) and the Plan 01 `scripts/coverage_report_tactic_motifs.py`, recording results in
`.planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md`. The phase-wide net-new artifact
list lives in Plan 01's `<artifacts_this_phase_produces>`.
</artifacts_this_phase_produces>

<verification>
- Dry-run smoke exits 0 with a non-zero flaw count (D-02).
- Full dev backfill completes; `game_flaws.tactic_motif` non-NULL count > 0 on full-eval'd games.
- Coverage report prints the four D-04 sections; NULL split is interpretable (SC#1).
- Idempotency re-run diff is empty (SC#3, D-05).
- D-06 before/after diff on the >9-position sample is empty.
</verification>

<success_criteria>
TACSCH-03 met on dev: the backfill is run, coverage is proven honest (SC#1), idempotency is
demonstrated (SC#3), lichess-eval-only games are untouched (SC#2 — auto-handled by
`--full-evald-only`), and the recompute introduced no non-tactic-column drift (D-06).
ROADMAP SC#1 recorded as met-on-dev / prod-pending (D-01).
</success_criteria>

<output>
Create `.planning/phases/125-backfill-tactic-motifs/125-02-SUMMARY.md` when done, recording the
verbatim backfill output, coverage report numbers, idempotency diff result, and D-06 diff result.
</output>