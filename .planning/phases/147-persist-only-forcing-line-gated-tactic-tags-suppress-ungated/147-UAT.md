---
status: complete
phase: 147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated
source: [147-VERIFICATION.md]
started: 2026-07-01T21:50:00Z
updated: 2026-07-01T22:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Live atomic gated-write cycle against a local dev backend
expected: |
  Run the upgraded worker against a live local dev backend and confirm a real atomic gated-write cycle:
  1. `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`
  2. `uv run uvicorn app.main:app --reload`
  3. Queue at least one game for eval (fresh import, or explicitly enqueue an existing tier-1 game).
  4. `uv run python scripts/remote_eval_worker.py --base-url http://localhost:8000 --once`

  Success: logs show `Atomic-leased game_id=...` then
  `Atomic-submitted game_id=...: flaws_written=N, blobs_written=M`; afterward the game's rows show
  `full_evals_completed_at` and `full_pv_completed_at` both set, and `tactic_motif` values are the
  forcing-line-gated result (no raw/ungated value ever observable between the two completion markers).
why_human: |
  The current dev DB has zero pending `eval_jobs` rows and `EVAL_AUTO_DRAIN_ENABLED=false` blocks the
  tier-3 idle path locally, so `/atomic-lease` legitimately 204s end-to-end today — there is no queued
  tier-1/2/3 game to drive a real 200 lease→submit cycle. Manufacturing one via direct DB seeding would
  violate the project's "no dev DB reset/seeding in plans" convention. This is live network/DB/timing
  behavior that cannot be proven by static analysis; 147-VALIDATION.md's Manual-Only Verifications table
  already lists this exact item.
result: pass
note: |
  User queued a full-eval game and re-ran the worker; confirmed the real atomic cycle worked
  (Atomic-leased → Atomic-submitted, completion markers set, gated tactic_motif). An earlier run with no
  queued full-eval game correctly fell through to the flaw-blob backfill rung (Part A self-heal), which
  also succeeded (game_id=682036, blobs_written=1).

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
