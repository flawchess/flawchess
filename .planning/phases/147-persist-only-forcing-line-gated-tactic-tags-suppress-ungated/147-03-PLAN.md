---
phase: 147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated
plan: 03
type: execute
wave: 2
depends_on: [147-01]
files_modified:
  - app/routers/eval_remote.py
  - tests/test_eval_worker_endpoints.py
autonomous: true
requirements: [SEED-074]

must_haves:
  truths:
    - "A game whose total walkable flaw-blob lease positions exceed MAX_SUBMIT_EVALS (1024) no longer 500s on /flaw-blob-lease; it writes `[]` sentinels for EVERY NULL-blob flaw ply and returns 204 in one pass."
    - "Such a game reaches zero NULL-blob flaws immediately (never re-picked by the tier-4 lottery)."
    - "The over-cap game's existing (or A-suppressed) tactic tags are unchanged — sentinel-ing does not run the gated retag."
  artifacts:
    - "app/routers/eval_remote.py — new `elif len(lease_positions) > MAX_SUBMIT_EVALS:` branch in flaw_blob_lease that sentinels all NULL-blob flaw plies and returns 204"
  key_links:
    - "flaw_blob_lease over-cap branch → _batch_update_flaw_pv_lines({ply: ([], [])}) for the full NULL-blob flaw-ply set → clears allowed_pv_lines IS NULL predicate."
  prohibitions:
    - "MUST NOT reuse the `if not lease_positions:` all-sentinel branch — over-cap games have a non-empty lease_positions; add a DISTINCT `elif` branch."
    - "MUST NOT raise MAX_SUBMIT_EVALS or add a new cap constant (shared DoS guard T-145-05/T-123-05 stays 1024)."
    - "The sentinel ply set MUST cover EVERY NULL-blob flaw ply of the game, not just the un-walkable `sentinel_lines` — derive it from the game's NULL-blob flaw rows."
    - "MUST NOT touch /flaw-blob-submit or its token validation (over-cap games never return a lease)."
---

<objective>
Implement the SEED-073 over-cap sentinel — the genuine functional PREREQUISITE that Part B reuses (the
CONTEXT/seed language "reuses the SEED-073 over-cap sentinel" refers to code that is currently dormant and
unimplemented). Fix the real dormant bug in the existing `/flaw-blob-lease` handler: fat games (>1024
walkable flaw-blob positions) currently raise a Pydantic ValidationError → 500, are re-picked by the
tier-4 lottery, and never drain.

Purpose: establishes the over-cap sentinel pattern that Part B's new atomic-lease endpoint reuses, and
removes the pre-existing prod 500 residue on the ~17 fattest games (SEED-073).
Output: the over-cap branch + a router test.

## Artifacts this plan produces
- `app/routers/eval_remote.py` — a new `elif len(lease_positions) > MAX_SUBMIT_EVALS:` branch in
  `flaw_blob_lease`.
- New test in `tests/test_eval_worker_endpoints.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated/147-CONTEXT.md
@.planning/phases/147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated/147-RESEARCH.md
@.planning/seeds/SEED-073-fat-game-flaw-blob-lease-500.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the over-cap sentinel branch to flaw_blob_lease</name>
  <files>app/routers/eval_remote.py</files>
  <read_first>
    - app/routers/eval_remote.py: `flaw_blob_lease` (~760-785) — the existing `if not lease_positions:`
      all-sentinel branch that builds `sentinel_blob_map = {ply: ([], [])}` and calls
      `_batch_update_flaw_pv_lines` then returns 204; `_build_flaw_blob_lease_positions` (returns
      `lease_positions, sentinel_lines`).
    - SEED-073 "Decided fix" + "Plan-time checks" — the sentinel ply set MUST cover EVERY NULL-blob flaw
      ply of the game (derive from the flaw rows, not only `sentinel_lines`).
    - RESEARCH.md "Landmine: SEED-073's over-cap sentinel is NOT implemented yet".
  </read_first>
  <action>
    In `flaw_blob_lease`, after `lease_positions, sentinel_lines = await _build_flaw_blob_lease_positions(
    game_id)` and after the existing `if not lease_positions:` branch, add a DISTINCT `elif
    len(lease_positions) > MAX_SUBMIT_EVALS:` branch. In it, build the sentinel ply set from ALL of the
    game's NULL-blob flaw plies (query the flaw rows for the game where the blob column is NULL — do not
    derive only from `sentinel_lines`, which holds just the un-walkable lines), construct `sentinel_blob_map
    = {ply: ([], []) for ply in <all NULL-blob flaw plies>}`, write it via `_batch_update_flaw_pv_lines`
    inside an `async with async_session_maker() as write_session:` + commit block (mirroring the existing
    all-sentinel branch), and return `Response(status_code=status.HTTP_204_NO_CONTENT)`. Add a bug-context
    comment citing SEED-073: over-cap games were 500ing on FlawBlobLeaseResponse construction and being
    re-picked forever; the sentinel clears the `allowed_pv_lines IS NULL` predicate in one pass. Do NOT
    raise `MAX_SUBMIT_EVALS`, do NOT add a new constant, do NOT change the submit path. If a helper is
    needed to fetch the NULL-blob flaw plies, reuse existing repository/query utilities rather than inlining
    raw SQL. Capture non-trivial exceptions with `sentry_sdk.capture_exception` per project rules.
  </action>
  <verify>
    <automated>uv run ty check app/</automated>
  </verify>
  <acceptance_criteria>
    - A new `elif len(lease_positions) > MAX_SUBMIT_EVALS:` branch exists, distinct from the empty-lease branch.
    - The sentinel covers every NULL-blob flaw ply, written via `_batch_update_flaw_pv_lines`, returns 204.
    - `MAX_SUBMIT_EVALS` unchanged; no new constant; submit path untouched.
    - `uv run ty check app/` passes.
  </acceptance_criteria>
  <done>Fat games sentinel cleanly on /flaw-blob-lease instead of 500ing; ty is green.</done>
</task>

<task type="auto">
  <name>Task 2: Router test — over-cap game sentinels to 204, no 500, tags unchanged</name>
  <files>tests/test_eval_worker_endpoints.py</files>
  <read_first>
    - tests/test_eval_worker_endpoints.py — existing `/flaw-blob-lease` tests (the all-sentinel/204 test is
      the closest analogue for fixture setup and the operator-token auth dependency).
    - SEED-073 "Add a test" — a synthetic game with > MAX_SUBMIT_EVALS walkable positions leases → 204 and
      afterward has zero NULL-blob flaws, existing tactic tags unchanged.
  </read_first>
  <behavior>
    - A seeded game whose walkable positions exceed 1024 → `/flaw-blob-lease` returns 204 (not 500).
    - After the call the game has zero NULL-blob flaws (all sentineled).
    - The game's `allowed_tactic_motif`/`missed_tactic_motif` are unchanged by the sentinel write.
  </behavior>
  <action>
    Add a test seeding a game with enough NULL-blob flaws × PV length to exceed `MAX_SUBMIT_EVALS` walkable
    positions (or monkeypatch/parametrize `_build_flaw_blob_lease_positions` to return >1024 lease_positions
    if seeding that many is impractical — prefer real seeding if cheap, else patch the builder). Assert the
    lease call returns 204, that all the game's flaw rows have non-NULL blob columns afterward (zero remain
    NULL), and that the pre-existing tactic-tag columns are unchanged. Use the existing operator-token auth
    fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_worker_endpoints.py -k "flaw_blob and (sentinel or over_cap or overcap)" -x</automated>
  </verify>
  <acceptance_criteria>
    - Test proves 204 (no 500), zero remaining NULL-blob flaws, tags unchanged.
    - Test passes under the per-run DB fixture.
  </acceptance_criteria>
  <done>The over-cap sentinel behavior is locked by an integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote worker → /flaw-blob-lease | operator-token-gated; unbounded lease payload is the DoS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-04 | Denial of Service / Availability | fat-game over-cap lease (>1024 positions) | medium | mitigate | Over-cap sentinel returns 204 and clears the predicate so the game is never re-picked; keeps MAX_SUBMIT_EVALS at 1024 (no DoS-guard weakening). |
| T-147-SC | Tampering | dependency installs | high | accept | No new packages this plan. |
</threat_model>

<verification>
- `uv run pytest tests/test_eval_worker_endpoints.py -k flaw_blob -x`
- `uv run ty check app/ tests/` (zero errors)
- `uv run ruff check app/ tests/`
</verification>

<success_criteria>
Over-cap games sentinel cleanly to 204 on `/flaw-blob-lease`, reach zero NULL-blob flaws in one pass, keep
their existing tactic tags, and the over-cap sentinel pattern is available for Part B to reuse.
</success_criteria>

<output>
Create `.planning/phases/147-persist-only-forcing-line-gated-tactic-tags-suppress-ungated/147-03-SUMMARY.md` when done
</output>
