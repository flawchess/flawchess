---
phase: 124-schema-tactic-detector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/20260617_120000_phase_124_tactic_motifs.py
  - app/models/game_flaw.py
  - app/services/tactic_detector.py
  - app/services/flaws_service.py
  - app/repositories/game_flaws_repository.py
autonomous: true
requirements: [TACSCH-01, TACSCH-02]
must_haves:
  truths:
    - "After `alembic upgrade head` the dev game_flaws table has tactic_motif, tactic_piece, tactic_confidence columns, all nullable SmallInteger, all NULL on existing rows"
    - "TacticMotifInt encodes all 24 motifs and roundtrips through _INT_TO_MOTIF / _MOTIF_TO_INT without loss"
    - "A FlawRecord with tactic fields populated produces a game_flaws insert dict carrying tactic_motif/tactic_piece/tactic_confidence ints (or None)"
  artifacts:
    - path: "alembic/versions/20260617_120000_phase_124_tactic_motifs.py"
      provides: "Migration adding the three nullable columns + matching downgrade"
      contains: "tactic_motif"
    - path: "app/services/tactic_detector.py"
      provides: "TacticMotifInt IntEnum + TacticMotif Literal + _INT_TO_MOTIF/_MOTIF_TO_INT + named constants"
      contains: "class TacticMotifInt"
    - path: "app/models/game_flaw.py"
      provides: "Three nullable SmallInteger ORM columns"
      contains: "tactic_motif"
  key_links:
    - from: "app/repositories/game_flaws_repository.py"
      to: "app/services/flaws_service.py"
      via: "flaw_record_to_row reads tactic_motif_int/tactic_piece/tactic_confidence from FlawRecord"
      pattern: "tactic_motif_int|tactic_piece|tactic_confidence"
---

<objective>
Lay the schema + encoding foundation for tactic-motif detection. This Wave-1 plan adds the three nullable `game_flaws` columns (Alembic migration + ORM), creates the `tactic_detector.py` module SHELL holding the `TacticMotifInt` IntEnum / `TacticMotif` Literal / bidirectional dicts / named constants (detector functions land in plan 02), and plumbs the three new fields through `FlawRecord` and `flaw_record_to_row`. Per D-01 the migration is the foundational task every later plan depends on. Per D-02, `tactic_motif` is a single enum (NOT a bitmask, NOT a join table), encoded via the `EndgameClassInt` precedent: a `*Int` IntEnum + `_INT_TO_*` / `_*_TO_INT` dicts mapping to a `Literal[...]` motif-name type.

Purpose: every other plan in this phase reads or writes these columns / this encoding. Building it first unblocks the detector (02), fixtures (03), and wiring (04) waves.
Output: migrated dev DB (3 columns), `tactic_detector.py` encoding block, extended `FlawRecord` + `flaw_record_to_row`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-schema-tactic-detector/124-CONTEXT.md
@.planning/phases/124-schema-tactic-detector/124-RESEARCH.md
@.planning/phases/124-schema-tactic-detector/124-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration + ORM columns for the three tactic fields</name>
  <files>alembic/versions/20260617_120000_phase_124_tactic_motifs.py, app/models/game_flaw.py</files>
  <read_first>
    - app/models/game_flaw.py (the file being modified; the `tempo` nullable-SmallInteger pattern at lines 46-50 and the `fen` column at line 66 — add the three new columns after it)
    - alembic/versions/20260616_120000_phase_123_entry_eval_lease.py (PATTERNS exact analog: nullable ADD COLUMN migration with `down_revision` chaining and a matching downgrade; do NOT import app constants inside the migration)
  </read_first>
  <action>
    Create the migration file `alembic/versions/20260617_120000_phase_124_tactic_motifs.py` following the phase_123 analog. `upgrade()` calls `op.add_column("game_flaws", sa.Column(name, sa.SmallInteger(), nullable=True))` for each of `tactic_motif`, `tactic_piece`, `tactic_confidence` (TACSCH-01, TACSCH-02). `downgrade()` drops them in reverse order. Set `down_revision` to the current Alembic head — determine it with `uv run alembic heads` (the phase_123 revision 20260616_120000 is the expected parent; verify, do not assume). Use literal `sa.SmallInteger()`, no symbolic refs, no `from app...` imports in the migration (RESEARCH §Migration note). Then add the three columns to `app/models/game_flaw.py` after the `fen` column (line 66), each as `Mapped[Optional[int]] = mapped_column(SmallInteger, nullable=True)` with the per-D-01/D-12 comment block from PATTERNS describing the int encoding. `Optional` and `SmallInteger` are already imported — add no new imports.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && docker compose -f docker-compose.dev.yml -p flawchess-dev up -d >/dev/null 2>&1; uv run alembic upgrade head && uv run python -c "
import asyncio, asyncpg
from app.core.config import settings
EXPECTED = {'tactic_motif', 'tactic_piece', 'tactic_confidence'}
async def main():
    dsn = settings.DATABASE_URL.replace('postgresql+asyncpg://', 'postgresql://')
    conn = await asyncpg.connect(dsn)
    rows = await conn.fetch(\"SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='game_flaws' AND column_name LIKE 'tactic_%'\")
    await conn.close()
    found = {r['column_name']: (r['data_type'], r['is_nullable']) for r in rows}
    missing = EXPECTED - set(found)
    assert not missing, f'missing columns: {missing}'
    for name in EXPECTED:
        dt, nul = found[name]
        assert dt == 'smallint', f'{name} data_type is {dt}, expected smallint'
        assert nul == 'YES', f'{name} is_nullable is {nul}, expected YES'
    print('cols-check-OK')
asyncio.run(main())
" && uv run alembic downgrade -1 && uv run alembic upgrade head
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` runs cleanly against the dev DB with no error.
    - `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips cleanly (proves downgrade is correct).
    - After upgrade, the dev `game_flaws` table physically has columns `tactic_motif`, `tactic_piece`, `tactic_confidence`, all `smallint` and nullable. Verify with the dev DB MCP query tool (`mcp__flawchess-db__query`): `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='game_flaws' AND column_name LIKE 'tactic_%' ORDER BY column_name;` returns exactly those three rows, `data_type='smallint'`, `is_nullable='YES'`.
    - `SELECT count(*) FROM game_flaws WHERE tactic_motif IS NOT NULL;` returns 0 (existing rows carry NULL).
    - The migration file contains no `from app` import (`grep -v '^#' alembic/versions/20260617_120000_phase_124_tactic_motifs.py | grep -c 'from app'` returns 0).
  </acceptance_criteria>
  <done>game_flaws has three nullable SmallInteger tactic columns on the dev DB; migration upgrades and downgrades cleanly; ORM model declares all three.</done>
</task>

<task type="auto">
  <name>Task 2: tactic_detector.py encoding shell — IntEnum, Literal, dicts, named constants</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/endgame_service.py (lines 108-129: the `EndgameClassInt` IntEnum + `_INT_TO_CLASS` / `_CLASS_TO_INT` precedent to copy exactly)
    - app/services/flaws_service.py (lines 34-50: named-constants-block style; module docstring style at lines 1-15)
    - app/services/tactic_detector.py (this is the file being created — PATTERNS §tactic_detector.py gives the full TacticMotifInt value table 1-24, the TacticMotif Literal list, both dicts, and the named constants `_PIECE_VALUES`, `TACTIC_CONFIDENCE_HIGH`, `TACTIC_MIN_SEVERITY`)
  </read_first>
  <action>
    Create `app/services/tactic_detector.py` with the module docstring (cite TACDET-01/02/03/04, "pure CPU, no I/O, no Stockfish") and the encoding block ONLY (detectors come in plan 02 — leave a clearly-marked section comment where they will go). Define: `TacticMotifInt(IntEnum)` with the 24 members and integer values from PATTERNS (FORK=1 … DOVETAIL_MATE=24; values MUST never be reordered — DB rows encode them); the `TacticMotif = Literal[...]` type alias with all 24 motif strings; `_INT_TO_MOTIF: dict[int, TacticMotif]` mapping each int to its string; `_MOTIF_TO_INT: dict[TacticMotif, int] = {v: k for k, v in _INT_TO_MOTIF.items()}`. Add named constants (CLAUDE.md no-magic-numbers): `_PIECE_VALUES: dict[int, int]` (PAWN=1,KNIGHT=3,BISHOP=3,ROOK=5,QUEEN=9,KING=99); `TACTIC_CONFIDENCE_HIGH: int = 100` (D-11 fixed-high for core/named-mate, cook is boolean); `TACTIC_MIN_SEVERITY: frozenset[str] = frozenset({"mistake", "blunder"})` (D-05 tunable severity gate). Also define a `MATE_MOTIFS: frozenset[TacticMotif]` constant grouping the 9 mate strings (D-03 coarsening helper). Add explicit return-type annotations everywhere; `import chess` for the piece-value keys. Do NOT define `detect_tactic_motif` yet (plan 02 owns it) — but DO declare the public function name in a section comment so plan 02 has a clear anchor.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/tactic_detector.py && uv run python -c "from app.services.tactic_detector import TacticMotifInt, _INT_TO_MOTIF, _MOTIF_TO_INT, MATE_MOTIFS, TACTIC_MIN_SEVERITY; assert len(TacticMotifInt)==24; assert len(_INT_TO_MOTIF)==24; assert all(_MOTIF_TO_INT[_INT_TO_MOTIF[i]]==i for i in _INT_TO_MOTIF); assert len(MATE_MOTIFS)==9; assert TACTIC_MIN_SEVERITY=={'mistake','blunder'}; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/services/tactic_detector.py` passes with zero errors.
    - The roundtrip assertion above prints `OK`: 24 enum members, 24 dict entries, `_MOTIF_TO_INT[_INT_TO_MOTIF[i]] == i` for every i, `MATE_MOTIFS` has exactly 9 entries, `TACTIC_MIN_SEVERITY == {"mistake","blunder"}`.
    - `_INT_TO_MOTIF` keys are exactly 1..24 contiguous (no gaps).
    - File contains `class TacticMotifInt` and `TacticMotif = Literal[`.
  </acceptance_criteria>
  <done>The encoding shell exists, type-checks, and roundtrips; detectors are stubbed by a section comment for plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Plumb three tactic fields through FlawRecord + flaw_record_to_row</name>
  <files>app/services/flaws_service.py, app/repositories/game_flaws_repository.py</files>
  <read_first>
    - app/services/flaws_service.py (lines 119-130: the `FlawRecord` TypedDict to extend — note it has no `total=False`, so add the three fields as plain `int | None` keys)
    - app/repositories/game_flaws_repository.py (lines 100-116: the `flaw_record_to_row` return dict; lines 20-39: the `_*_INT` encoding-map block — the tactic ints are already encoded by the detector, so NO new encoding map is needed here, just `.get()` passthrough)
  </read_first>
  <action>
    Extend the `FlawRecord` TypedDict in `flaws_service.py` with three fields: `tactic_motif_int: int | None`, `tactic_piece: int | None`, `tactic_confidence: int | None` (PATTERNS §flaws_service.py). These store the already-encoded int (not the string) to avoid a dict lookup in the hot path. Do NOT yet populate them in `_build_flaw_record` (plan 04 owns the detector call + integration); for now the existing `_build_flaw_record` return must still construct a valid FlawRecord — since the TypedDict is not `total=False`, set all three to `None` in the existing `return FlawRecord(...)` so the record stays valid. Then extend `flaw_record_to_row` in `game_flaws_repository.py`: append three entries to the returned insert dict — `"tactic_motif": flaw.get("tactic_motif_int")`, `"tactic_piece": flaw.get("tactic_piece")`, `"tactic_confidence": flaw.get("tactic_confidence")`. Use `.get()` (PATTERNS note) so older FlawRecord construction paths that omit the keys map to None rather than KeyError.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/flaws_service.py app/repositories/game_flaws_repository.py && uv run pytest -n auto tests/services/test_flaws_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/services/flaws_service.py app/repositories/game_flaws_repository.py` passes with zero errors.
    - `uv run pytest -n auto tests/services/test_flaws_service.py -x -q` passes (existing classify tests still green with the extended FlawRecord defaulting to None).
    - `flaw_record_to_row`'s return dict contains keys `tactic_motif`, `tactic_piece`, `tactic_confidence` (`grep -c '"tactic_motif"\|"tactic_piece"\|"tactic_confidence"' app/repositories/game_flaws_repository.py` returns 3).
    - A FlawRecord constructed without the tactic keys still maps via `flaw_record_to_row` to a dict with those three keys = None (covered by the existing repository test path, or add a 1-line unit assertion).
  </acceptance_criteria>
  <done>FlawRecord carries the three optional tactic fields (defaulting None), flaw_record_to_row emits them, existing tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | No new external input, network, auth, or user-facing surface. This plan adds DB columns + an internal encoding module + a TypedDict field. All inputs are internally-computed (engine PV, classify output). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | Alembic migration on game_flaws (131k+ rows) | mitigate | Nullable ADD COLUMN is a pure catalog update (no rewrite, no extended lock; verified Phase 117/123). Downgrade tested via `downgrade -1 && upgrade head` round-trip so a bad deploy is reversible. |
| T-124-02 | Denial of Service | enum/dict encoding | accept | Pure in-memory constants, no runtime input; no DoS surface. |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: "No new packages"). No supply-chain surface. |
</threat_model>

<verification>
- `uv run alembic upgrade head` clean; columns present + nullable + NULL-on-existing (dev DB MCP query).
- `uv run ty check app/ tests/` zero errors for touched files.
- `uv run ruff check app/ tests/` clean.
- `uv run pytest -n auto tests/services/test_flaws_service.py -x` green.
- Encoding roundtrip assertion prints OK.
</verification>

<success_criteria>
- game_flaws has nullable tactic_motif / tactic_piece / tactic_confidence SmallInteger columns on the dev DB (TACSCH-01, TACSCH-02).
- Migration up/down round-trips cleanly.
- TacticMotifInt / TacticMotif / dicts / constants exist and roundtrip.
- FlawRecord + flaw_record_to_row carry the three fields; existing suite green.
</success_criteria>

<output>
Create `.planning/phases/124-schema-tactic-detector/124-01-SUMMARY.md` when done.
</output>
