---
phase: 124-schema-tactic-detector
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - app/services/tactic_detector.py
autonomous: true
requirements: [TACDET-01, TACDET-02]
must_haves:
  truths:
    - "detect_tactic_motif(board_after_flaw, pv_str) returns at most one motif via the D-07 priority order: mate > geometric > tier-3 > hanging-piece"
    - "Mate ALWAYS dominates a co-firing geometric motif (a forced-mate PV is labelled by its mate type per D-07)"
    - "hanging-piece is the catch-all and only wins when no more specific motif fires (D-07 tier 4)"
    - "Core 8 + named-mate motifs return tactic_confidence = TACTIC_CONFIDENCE_HIGH; tier-3 motifs return a graded 0-100 confidence (D-11)"
    - "tactic_piece follows the per-motif semantic of D-12 (fork=attacker, hanging=victim, pin/skewer=line piece, mates=mating piece) and is NULL for the D-12 ambiguous set"
    - "A None/empty/malformed pv_str returns (None, None, None) without raising"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "detect_tactic_motif() dispatcher + PV-parse helper + Core 8 + named-mate + tier-3 detectors"
      contains: "def detect_tactic_motif"
  key_links:
    - from: "app/services/tactic_detector.py:detect_tactic_motif"
      to: "app/services/tactic_detector.py priority dispatcher"
      via: "runs detectors in D-07 order, returns first winner"
      pattern: "def detect_tactic_motif"
---

<objective>
Implement the full tactic-motif detector inside `app/services/tactic_detector.py` (encoding shell created in plan 01). Deliver the PV-parse helper, all Core 8 detectors, the named-mate subtype detectors, all tier-3 detectors, and the D-07 priority-order dispatcher that returns a single `(motif_int, piece_int, confidence_int)` winner per flaw. Heuristics are REIMPLEMENTED from RESEARCH cook-heuristic pseudocode in original code — never copy AGPL cook.py source. Per D-04 the detector set is exactly the in-scope Core 8 + 8 tier-3 + named-mate subtypes. Per D-06, the excluded cook motifs are deliberately NOT implemented: move-type descriptors, positional attack-theme tags, puzzle metadata, crushing/advantage/equality, oneMove/short/long, endgame-type tags, and `overloading` (a `return False` stub in cook — unavailable). Do not add detectors for any D-06 excluded motif.

Purpose: this is the cause-of-error detection engine; plans 03 (fixtures) and 04 (wiring) consume `detect_tactic_motif`.
Output: a pure-CPU `detect_tactic_motif(board_after_flaw, pv_str) -> tuple[int|None, int|None, int|None]`.

Note (D-04 file-size risk): this file holds 24+ detector functions. If `app/services/tactic_detector.py` exceeds the CLAUDE.md 200 logic-LOC soft limit, split it into helper submodules (e.g. move the named-mate detectors into their own module like `tactic_detector_mates.py` and import them back) rather than letting one file bloat — the public `detect_tactic_motif` dispatcher stays the single entry point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-schema-tactic-detector/124-CONTEXT.md
@.planning/phases/124-schema-tactic-detector/124-RESEARCH.md
@.planning/phases/124-schema-tactic-detector/124-PATTERNS.md
@app/services/tactic_detector.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PV-parse helper + utilities + Core 8 detectors</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (the encoding shell from plan 01: TacticMotifInt, _PIECE_VALUES, TACTIC_CONFIDENCE_HIGH — extend this file)
    - .planning/phases/124-schema-tactic-detector/124-RESEARCH.md (Cook Heuristic Algorithms: Utility functions + Core 8 Detectors; PV Data Shape; Dont Hand-Roll API table; Pitfalls 2/3/4/5)
  </read_first>
  <behavior>
    - _parse_pv(board_after_flaw, pv_str) returns (boards, moves) where boards[0]=board_after_flaw, len(boards)=len(moves)+1; boards[k] is the board BEFORE moves[k].
    - pov = board_after_flaw.turn (the refuting side).
    - detect_fork: a pov move whose landing piece attacks at least 2 opponent pieces each higher-value-than-mover OR hanging fires True; piece is the forking piece type.
    - detect_hanging_piece: moves[0] is a capture of a non-pawn on an undefended square fires True; piece is the victim type.
    - detect_pin / detect_skewer: a line piece (B/R/Q) delivers; uses board.pin() not-equal-to BB_ALL (Pitfall 2); piece is the line piece type.
    - detect_double_check: a board where the opponent is in check by 2+ checkers fires True; piece is None (D-12 ambiguous).
    - detect_discovered_attack: discovered check (mover to_square not among checkers) or discovered capture fires True; piece is the unveiled attacker.
    - A hard-negative quiet line fires no Core-8 detector.
  </behavior>
  <action>
    Extend tactic_detector.py. First implement `_parse_pv(board_after_flaw: chess.Board, pv_str: str) -> tuple[list[chess.Board], list[chess.Move]]` per RESEARCH Pattern 2 (let chess.Move.from_uci raise on malformed UCI; the dispatcher in Task 3 guards). Implement utilities `_piece_value` (uses _PIECE_VALUES), `_is_hanging(board, sq, color)` (strict precision-first: board.attackers(not color, sq) non-empty AND board.attackers(color, sq) empty), `_material_diff(board, pov)`. Implement the Core 8 boolean detectors per RESEARCH Core 8 section: detect_fork, detect_hanging_piece, detect_pin, detect_skewer, detect_double_check, detect_discovered_attack, detect_back_rank_mate, detect_generic_mate. Each must also yield enough to set tactic_piece per D-12 — implement each returning a consistent shape (bool plus optional piece int; your discretion, but document the convention in a module comment so Tasks 2/3 follow it). Use ONLY the python-chess API from the Dont Hand-Roll table (attackers, attacks, pin, pieces, piece_at, king, checkers, is_check, is_checkmate, SquareSet.between, square_rank/square_file). Keep each detector shallow (nesting hard-limit 4); invert with early continue/return. Add explicit return types. Do NOT copy cook.py text — reimplement from the pseudocode.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/tactic_detector.py && uv run ruff check app/services/tactic_detector.py && uv run python -c "import chess; from app.services.tactic_detector import _parse_pv; b=chess.Board(); boards,moves=_parse_pv(b,'e2e4 e7e5'); assert len(boards)==3 and len(moves)==2; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/services/tactic_detector.py` passes zero errors.
    - `uv run ruff check app/services/tactic_detector.py` clean.
    - `_parse_pv` returns boards/moves with len(boards)==len(moves)+1 and boards[0] equal to the input board (assertion above prints OK).
    - All eight Core-8 detector functions are defined and importable (`grep -c '^def detect_' app/services/tactic_detector.py` returns at least 8).
    - No `cook` AGPL source pasted: the file has no verbatim cook.py function bodies (reviewer/grep confirms heuristics are described in original prose, not copied).
  </acceptance_criteria>
  <done>PV parser, utilities, and all 8 Core detectors exist, type-check, lint clean, and each exposes its D-12 piece.</done>
</task>

<task type="auto">
  <name>Task 2: Named-mate subtype detectors + tier-3 graded detectors</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (Core 8 + utilities from Task 1; the convention for returning piece ints)
    - .planning/phases/124-schema-tactic-detector/124-RESEARCH.md (Named-Mate Subtype Detectors section; Tier-3 Detectors section with graded-confidence scoring; Assumptions A3/A4 on provisional priority + count-of-conditions scoring)
  </read_first>
  <behavior>
    - Each named-mate detector requires boards[-1].is_checkmate() as a precondition and returns False when not mate.
    - detect_boden_or_double_bishop_mate distinguishes boden (bishops opposite sides of king file) vs double-bishop (same side), returning the specific motif or None.
    - All named-mate + back-rank + generic-mate set tactic_piece = mating piece type (D-12).
    - Each tier-3 detector returns (bool, confidence_int) where confidence is 0-100 graded by count-of-conditions-met / total (RESEARCH A4).
    - tier-3 tactic_piece: deflection/attraction = target piece; sacrifice = sacrificed piece; capturing-defender = captured defender; x-ray/interference/self-interference/clearance/intermezzo = None (D-12 ambiguous).
  </behavior>
  <action>
    Add the named-mate detectors per RESEARCH (detect_smothered_mate, detect_anastasia_mate, detect_hook_mate, detect_arabian_mate, detect_boden_or_double_bishop_mate, detect_dovetail_mate), each gated on boards[-1].is_checkmate(); set the mating-piece type for tactic_piece. Add the tier-3 detectors per RESEARCH (detect_deflection, detect_attraction, detect_intermezzo, detect_x_ray, detect_interference, detect_self_interference, detect_clearance, detect_capturing_defender, detect_sacrifice), each returning (bool, confidence_int) with the graded count-of-conditions scoring (define a small `_grade(met, total)` helper returning `round(met / total * 100)` so the scoring is a named, testable function not a magic literal). Per D-12, return the appropriate piece for deflection/attraction/sacrifice/capturing-defender, None for the ambiguous tier-3 set. Intra-mate and intra-tier-3 ordering is provisional (D-08, A3) — encode the order as a list/sequence the Task-3 dispatcher iterates, so it can be reordered without touching detector bodies. Keep functions shallow; explicit return types; reimplement from pseudocode, no cook.py copy.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/tactic_detector.py && uv run ruff check app/services/tactic_detector.py && uv run python -c "from app.services.tactic_detector import _grade; assert _grade(5,5)==100 and _grade(0,5)==0 and 50<=_grade(3,5)<=70; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/services/tactic_detector.py` passes zero errors.
    - `uv run ruff check app/services/tactic_detector.py` clean.
    - `_grade(5,5)==100`, `_grade(0,5)==0`, partial counts scale monotonically (assertion prints OK).
    - Named-mate detector for a known smothered-mate position returns True and a knight (PieceType 2) as the mating piece (covered by a fixture in plan 03; here at minimum the function exists and is importable).
    - All named-mate + tier-3 detector functions are defined (`grep -c '^def detect_' app/services/tactic_detector.py` now totals at least 22 across Tasks 1+2).
    - Tier-3 detectors return a 2-tuple (bool, int) — confirmed by signature/return-type and a smoke call.
  </acceptance_criteria>
  <done>All named-mate and tier-3 detectors exist with graded confidence and D-12 piece semantics; type-check + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: D-07 priority dispatcher — detect_tactic_motif</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (all detectors from Tasks 1+2; the encoding dicts from plan 01)
    - .planning/phases/124-schema-tactic-detector/124-CONTEXT.md (D-07 full priority order; D-11 confidence rules; D-12 piece rules)
    - .planning/phases/124-schema-tactic-detector/124-PATTERNS.md (the public detect_tactic_motif signature returning tuple[int|None, int|None, int|None])
  </read_first>
  <behavior>
    - detect_tactic_motif returns the FIRST motif in D-07 order that fires: (1) named-mate subtypes, then back-rank-mate, then generic mate; (2) fork, skewer, pin, discovered-attack, double-check; (3) tier-3 in the D-07 order; (4) hanging-piece last.
    - Mate always dominates any geometric/tier-3 co-firing motif.
    - hanging-piece only wins when no earlier detector fired.
    - Returns (motif_int, piece_int_or_None, confidence): TACTIC_CONFIDENCE_HIGH for core/named-mate, the graded value for tier-3.
    - pv_str None/empty/malformed returns (None, None, None) — no raise.
    - No detector fires returns (None, None, None).
  </behavior>
  <action>
    Implement `detect_tactic_motif(board_after_flaw: chess.Board, pv_str: str) -> tuple[int | None, int | None, int | None]` (PATTERNS signature). Guard: if not pv_str, return (None, None, None); wrap _parse_pv in try/except (ValueError) for malformed UCI and return (None, None, None). Build (boards, moves, pov) once. Run detectors strictly in the D-07 order encoded as an ordered structure (so D-08 reorders are data, not control-flow edits). For the winning detector, map its motif via TacticMotifInt.<NAME>.value, take its D-12 piece (int or None), and set confidence = TACTIC_CONFIDENCE_HIGH for core+named-mate or the detector's graded int for tier-3. Return the int triple (never the string — the repo layer stores ints, per plan 01). Add a module-level public `__all__` or at least ensure detect_tactic_motif and the encoding names are importable. Keep the dispatcher itself shallow — it should read as an ordered list of detector calls with an early return on the first hit (the antipattern to avoid: a 30-branch if/elif tree; prefer iterating an ordered (detector, motif, piece-extractor) registry).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/tactic_detector.py && uv run ruff check app/services/tactic_detector.py && uv run python -c "import chess; from app.services.tactic_detector import detect_tactic_motif; assert detect_tactic_motif(chess.Board(), '')==(None,None,None); assert detect_tactic_motif(chess.Board(), 'zzzz')==(None,None,None); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/services/tactic_detector.py` passes zero errors.
    - `uv run ruff check app/services/tactic_detector.py` clean.
    - `detect_tactic_motif(board, "")` and `detect_tactic_motif(board, "zzzz")` (malformed UCI) both return `(None, None, None)` without raising (assertion prints OK).
    - The dispatcher is data-driven: the D-07 order is expressed as an ordered registry/list iterated with first-hit early return (not a long if/elif chain) — confirmed by reading the function (nesting <=4, logic LOC <100).
    - Return values are ints (or None), never motif strings — confirmed by signature `tuple[int | None, int | None, int | None]`.
    - A forced-mate PV that also contains a delivering fork returns the mate motif int, not the fork (covered by a priority fixture in plan 03; the dispatcher order makes this structurally true).
  </acceptance_criteria>
  <done>detect_tactic_motif returns one D-07 winner with correct piece + confidence, handles None/malformed PV safely, and is data-driven/shallow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored PV to detector | The pv_str is internally-computed engine output (game_positions.pv), NOT user input. The only realistic failure is a malformed/None PV reaching the detector and crashing the classify path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-03 | Denial of Service | detect_tactic_motif on malformed/None PV | mitigate | Guard `if not pv_str` + try/except ValueError around _parse_pv returning (None,None,None); a malformed PV can never raise out of the detector into classify_game_flaws. Asserted in Task 3 verify. |
| T-124-04 | Tampering | reimplemented heuristics vs AGPL cook.py | mitigate | Heuristics reimplemented from RESEARCH pseudocode in original prose; validated against OWN hand-labeled fixtures (plan 03), never against cook.py output — no AGPL source text copied. |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: no new packages). No supply-chain surface. |
</threat_model>

<verification>
- `uv run ty check app/services/tactic_detector.py` zero errors.
- `uv run ruff check app/services/tactic_detector.py` clean.
- Malformed/empty PV returns (None,None,None) without raising.
- All 23+ detector functions defined; _grade monotone; _parse_pv correct.
- (Precision validation against fixtures is plan 03.)
</verification>

<success_criteria>
- detect_tactic_motif implements the D-07 priority order, returns one (motif_int, piece_int|None, confidence) winner (TACDET-01, TACDET-02).
- Core/named-mate confidence = TACTIC_CONFIDENCE_HIGH; tier-3 graded (D-11).
- tactic_piece follows D-12 per-motif semantics.
- Malformed/None PV is safe.
- Heuristics original, not AGPL-copied.
</success_criteria>

<output>
Create `.planning/phases/124-schema-tactic-detector/124-02-SUMMARY.md` when done.
</output>
