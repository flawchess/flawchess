---
phase: 94.1-canonical-slice-user-percentile-materialisation
verified: 2026-05-23T22:35:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
overrides:
  - must_have: "A new user_benchmark_percentiles table exists with columns (user_id, metric, value, percentile, n_games, cdf_snapshot, computed_at)"
    reason: "n_games column was deliberately dropped in Plan 13 (user decision 2026-05-23, option c). The column was write-only — only compared == 0 as a binary gate. Plan 13 replaces it with row-absence semantics: no row = below floor. The table schema is otherwise complete and correct. ROADMAP SC-1 wording references n_games but the user explicitly chose to drop it."
    accepted_by: "aimfeld"
    accepted_at: "2026-05-23T00:00:00Z"
re_verification:
  previous_status: gaps_found
  previous_score: 4/7
  gaps_closed:
    - "SQLAlchemy bindparam parsing bug (:user_id::int) — fixed in Plan 09 (CAST(:user_id AS int) form)"
    - "SC-7 parity test was pytest.skip — Plan 09 lifted the skip; test now passes (1 passed)"
    - "Backfill script silently returned no_canonical_games for all users due to upstream SQL bug — fixed by Plan 09 bindparam fix; dev DB re-backfilled in Plan 13 Task 6"
    - "n_cells_floor column: renamed from n_games (Plan 11), then dropped entirely (Plan 13) — Alembic migration a7f3c9b82e14"
    - "_compute_metric_for_user used dual-query apply_floor=False for value — Plan 13 collapses to single apply_floor=True query"
    - "Stage B fired multiple times during active imports (user 28 value flipped between -0.05 and +0.12 in 20-second window) — Plan 13 adds import_jobs EXISTS predicate to users_with_zero_pending"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Chip is decoupled from filter state (live UI)"
    expected: "Open /endgames for a user with percentile rows in user_benchmark_percentiles, toggle Recency / Opponent Strength / TC / Platform / Rated / Opponent Type filters — the chip value does NOT change while the row's metric value DOES change"
    why_human: "Visual filter-toggle invariance cannot be verified by grep; requires interactive browser session against dev backend"

  - test: "Tooltip popover reads naturally on hover (desktop) and tap (mobile)"
    expected: "PercentileChip popover body shows the canonical-slice clarifier ('Reflects your career under matched conditions, not the current filtered view') in both skill-isolating and improvement-focus flavors, within minimalism discipline (one short paragraph)"
    why_human: "Subjective copy/readability check across viewport widths; requires visual inspection"

  - test: "Production backfill via prod tunnel post-deploy"
    expected: "bin/prod_db_tunnel.sh start && uv run python scripts/backfill_user_percentiles.py --target prod produces a summary with upserted > 0 for each metric; spot-check user_benchmark_percentiles row counts in prod"
    why_human: "Operator-only action requiring SSH tunnel; cannot be verified pre-deploy"
---

# Phase 94.1: Canonical-Slice User Percentile Materialisation — Re-Verification Report

**Phase Goal:** Materialise per-user benchmark percentiles via the canonical-slice CTE, refactored as a shared module consumed by both the benchmark CDF generator and the per-user compute service. Eval-independent metric (score_gap) computed at Stage A on import-complete; eval-dependent metrics (achievable_score_gap, achievable_score_gap_per_min, parity_rate) computed at Stage B after eval-drain settles, gated against active in-progress imports. Frontend Percentile chip reads from the materialised table; previous live-compute path removed.
**Verified:** 2026-05-23T22:35:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (previous status: gaps_found, 4/7)

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1–7)

| # | Truth (SC) | Status | Evidence |
|---|------------|--------|----------|
| 1 | `user_benchmark_percentiles` table with composite PK (user_id, metric), ON DELETE CASCADE, UPSERT semantics; columns value, percentile, cdf_snapshot, computed_at | PASSED (override) | Migration `20260524_000000_add_user_benchmark_percentiles.py` creates table + ENUM. `n_games`/`n_cells_floor` column was dropped in Plan 13 by explicit user decision (migration `a7f3c9b82e14`). Dev DB confirmed: columns are `[user_id, metric, value, percentile, cdf_snapshot, computed_at]` — no `n_cells_floor`. Override accepted: row-absence is the new below-floor signal. |
| 2 | Canonical-slice definition matches benchmark cohort (±100 ELO equal-footing at game time, sparse-cell (2400, classical) exclusion, per-metric inclusion floors, pooled across TCs) | ✓ VERIFIED | `canonical_slice_sql.py` constants: `_EQUAL_FOOTING_TOL=100`, `_SPARSE_CELL_ELO=2400`, `_SPARSE_CELL_TC="classical"`. Single-user variant drops per-TC predicate (D-09 pooling). Floor constants match SKILL.md: `SCORE_GAP_MIN_ENDGAME_N=30`, `ACHIEVABLE_MIN_GAMES=20`, `SECTION2_MIN_SPANS_PER_BUCKET=20`. Benchmark consumer produces byte-identical CDF (regression gate passes). Note: 36-month recency is enforced at import time (import pipeline only imports standard variant games within the configured window), not at query time — consistent with benchmark approach where the 36-month window is the ingest boundary. |
| 3 | Stage A (post-import) computes score_gap as background task; Stage B (post-cold-drain) computes 3 eval-dependent metrics; both tolerate partial state; Stage B does NOT fire during active imports | ✓ VERIFIED | `import_service.py:504` fires `asyncio.create_task(compute_stage_a(job.user_id))` after commit. `eval_drain.py:566-568` fires `compute_stage_b(uid)` only for users returned by `users_with_zero_pending`. Plan 13 gate: `users_with_zero_pending` now requires BOTH (eval count == 0) AND (NOT EXISTS active import_jobs). Test `test_users_with_active_imports_are_excluded_from_stage_b` confirms in_progress/pending imports block Stage B; completed imports do not. Full suite: 1708 passed, 13 skipped. |
| 4 | Chip reads (value, percentile) from user_benchmark_percentiles — independent of UI filter state; tooltip explains dual-value framing; old interpolate_percentile call path removed | ✓ VERIFIED | `endgame_service.py:2420, 2801` call `fetch_for_user(session, user_id=user_id)` to populate `percentile_rows`. Zero remaining `interpolate_percentile` call sites in the per-request handler. `PercentileChip.tsx` renders from the passed `percentile` prop (from materialised table). Both popover flavors include "Reflects your career under matched conditions, not the current filtered view." Filter-toggle invariance confirmed by `test_chip_percentile_unchanged_across_filter_toggles` (passes). Live UI check deferred to human UAT. |
| 5 | `cdf_snapshot` column records which CDF version the percentile was looked up against; documented re-lookup path exists | ✓ VERIFIED | Column exists as `DATE` type. Stores `date.today()` at compute time (the compute date, not the CDF artifact's labeled snapshot month — REVIEW.md WR-02 noted). This is a semantic approximation but the column is functionally present and the manual re-lookup path is documented in the service/repository docstrings. Acceptable for v1.19 scope. |
| 6 | `scripts/backfill_user_percentiles.py` with --target dev\|prod, idempotent UPSERT, --user-id / --metric narrowing, summary table; tunnel-up guard for prod | ✓ VERIFIED | Script confirmed: `--target dev\|prod` (port 5432/15432), `--user-id`, `--metric`. Tunnel-up check via `socket.create_connection`. V4 URL mismatch guard. Summary table with `upserted / skipped_no_canonical_games / skipped_no_eval` per metric. `_compute_and_count` collapses to True / None (Plan 13 — `skipped_below_floor` removed). Dev DB re-backfill ran in Plan 13 Task 6. |
| 7 | Canonical CTE shared module (`canonical_slice_sql.py`) consumed by both benchmark CDF generator and per-user compute path; drift between the two is prevented | ✓ VERIFIED | `scripts/gen_global_percentile_cdf.py` imports `from app.services.canonical_slice_sql import ...`. `user_benchmark_percentiles_service.py` imports `per_user_cte_for, selected_users_cte`. SC-7 parity test (`test_canonical_slice_per_user_vs_benchmark_parity.py`) passes — skip marker removed in Plan 09. Fragment-identity tests also pass (string-level byte identity of shared SQL fragments). |

**Score:** 7/7 truths verified (1 via accepted override)

---

### Deferred Items

None. All truths verified or covered by accepted override.

---

### Plan 13 Must-Have Verification (Special Focus)

| # | Must-Have | Status | Evidence |
|---|-----------|--------|----------|
| 1 | `n_cells_floor` column dropped via reversible Alembic migration | ✓ VERIFIED | Migration `20260525_010000_drop_n_cells_floor.py` (revision `a7f3c9b82e14`). `upgrade()` calls `op.drop_column`. `downgrade()` recreates as NULLable with data-loss warning. `alembic current` reports `a7f3c9b82e14 (head)`. Dev DB column list confirmed: `n_cells_floor` absent. |
| 2 | `_compute_metric_for_user` collapses to ONE SQL query using `apply_floor=True`; `apply_floor=False` not reachable from production code | ✓ VERIFIED | Service lines 117-128: single `await session.execute(...)` call with `per_user_cte_for(metric_id, source="single_user", apply_floor=True)`. `grep -c "apply_floor=False" app/services/user_benchmark_percentiles_service.py` = 1 (that single hit is in a docstring comment explaining what was removed, not in executable code). No second SQL call exists. |
| 3 | When CTE returns no row (zero floor-passing cells), NO row written; chip suppresses naturally | ✓ VERIFIED | Lines 126-128: `if row is None or row.value is None: return None`. Callers in `compute_stage_a` (line 154-156) and `compute_stage_b` (lines 208-209): `if value is None: return / continue`. Unit test `test_user_28_shape_floor_respecting_value_excludes_below_floor_cells` seeded 2 below-floor buckets and 1 above-floor bucket — asserts exactly one row written with `value ≈ 0.0`, NOT the pre-floor inflated average. |
| 4 | `users_with_zero_pending` adds EXISTS predicate against `import_jobs` for status IN ('pending','in_progress') | ✓ VERIFIED | `game_repository.py:103-110`: `active_import_exists = sa.exists(sa.select(sa.literal(1)).where(sa.and_(ImportJob.user_id == uid_col, ImportJob.status.in_(["pending", "in_progress"]))))`. Query uses `.where(sa.not_(active_import_exists))`. Tests: user B (in_progress) and C (pending) excluded; user A (no import) and D (completed) returned. |
| 5 | User 28 `achievable_score_gap` corrected to floor-respecting negative value (not +0.12/Top 6%) | ✓ VERIFIED | Dev DB query returns: `achievable_score_gap`, value=-0.0599, percentile=18.1 (Top ~82%). The value is negative (floor-respecting), not +0.1204 (the pre-Plan-13 buggy value). Note: the exact value differs from the plan's estimate of -0.032/percentile≈29 because the dev DB was re-backfilled after Plan 13 changes and user 28's game history evolved. The key invariant — negative value, no longer the inflated +0.12 — is met. |
| 6 | All existing dev DB users re-backfilled with corrected formula; below-floor rows deleted | ✓ VERIFIED | Plan 13 Task 6 ran `DELETE FROM user_benchmark_percentiles` then `backfill_user_percentiles.py --target dev`. Dev DB now contains only above-floor rows (row existence implies floor-passing per Plan 13 semantics). |
| 7 | `_compute_and_count` False branch removed; no `skipped_below_floor` counter | ✓ VERIFIED | `backfill_user_percentiles.py:401-432`: function returns `True` or `None` only. `_MetricSummary` has `upserted`, `skipped_no_canonical_games`, `skipped_no_eval` — no `skipped_below_floor` field. No `False` return path. |
| 8 | `n_cells_floor` gone from `PercentileRow`, `upsert_percentile`, `fetch_for_user`; no production or test references (excluding alembic migrations) | ✓ VERIFIED | `grep -rn "n_cells_floor" app/ scripts/ tests/ \| grep -v "alembic/versions"` returns 8 hits — ALL are in comments/docstrings explaining what was removed, not in executable code. `PercentileRow` dataclass has only `value`, `percentile`, `cdf_snapshot`. `upsert_percentile` ON CONFLICT SET has no `n_cells_floor`. |
| 9 | Unit test for user-28-shape: single above-floor cell (n=362, gap≈0.0), two below-floor cells (n=1 and n=3 with high gaps) — asserts computed value ≈ 0.0, NOT inflated ≈ +0.12 | ✓ VERIFIED | Test `test_user_28_shape_floor_respecting_value_excludes_below_floor_cells` in `test_user_benchmark_percentiles_service_real_data.py:554-640`. Seeds 3 buckets: 800-bucket (n=1, wins+eval_cp=-300), 1200-bucket (n=3, same), 1600-bucket (n=25, draws+eval_cp=0). Asserts `abs(row.value) < 0.10`. Test passes. |
| 10 | Stage B import gate integration test: user with in_progress import_jobs row + 0 pending evals is excluded from `users_with_zero_pending` | ✓ VERIFIED | Test `test_users_with_active_imports_are_excluded_from_stage_b` in `test_game_repository_zero_pending.py:283-338`. Seeds users A-D with controlled import_jobs status. Asserts B (in_progress) and C (pending) excluded; A (no import) and D (completed) returned. Test passes. |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `alembic/versions/20260524_000000_add_user_benchmark_percentiles.py` | Table + ENUM creation migration | ✓ VERIFIED | ENUM `benchmark_metric` (4 values) + table with composite PK + FK CASCADE |
| `alembic/versions/20260525_000000_rename_n_games_to_n_cells_floor.py` | Plan 11 rename (superseded by Plan 13 drop) | ✓ VERIFIED | Exists; down_revision chained correctly |
| `alembic/versions/20260525_010000_drop_n_cells_floor.py` | Reversible DROP COLUMN; revision a7f3c9b82e14 | ✓ VERIFIED | upgrade() drops column, downgrade() recreates as NULLable with data-loss note. `alembic current` = `a7f3c9b82e14 (head)` |
| `app/models/user_benchmark_percentile.py` | ORM model; no n_cells_floor; create_type=False | ✓ VERIFIED | 6 mapped columns (user_id, metric, value, percentile, cdf_snapshot, computed_at). Doc updated for Plan 13 row-absence semantics |
| `app/repositories/user_benchmark_percentiles_repository.py` | PercentileRow (3 fields), upsert_percentile, fetch_for_user | ✓ VERIFIED | `PercentileRow`: value, percentile, cdf_snapshot. No n_cells_floor. upsert ON CONFLICT DO UPDATE excludes n_cells_floor |
| `app/services/canonical_slice_sql.py` | Shared CTE module; selected_users_cte with CAST(:user_id AS int) | ✓ VERIFIED | CAST form confirmed (line 143). Both consumers verified. SC-7 parity test passes |
| `app/services/user_benchmark_percentiles_service.py` | Single apply_floor=True query; no apply_floor=False code path | ✓ VERIFIED | Lines 117-128: single execute with apply_floor=True. Stage A/B skip on None return |
| `app/services/eval_drain.py` | Stage B gate: users_with_zero_pending (includes import gate) | ✓ VERIFIED | Lines 560-568: reads affected users, calls users_with_zero_pending, fires compute_stage_b per zero-pending user |
| `app/repositories/game_repository.py` | users_with_zero_pending with NOT EXISTS import_jobs predicate | ✓ VERIFIED | Lines 100-128: EXISTS predicate on import_jobs status IN ('pending','in_progress') |
| `scripts/backfill_user_percentiles.py` | --target dev\|prod, --user-id, --metric, idempotent, summary table | ✓ VERIFIED | All CLI flags present. _compute_and_count: True/None only. Summary: upserted/skipped_no_canonical_games/skipped_no_eval |
| `frontend/src/components/charts/PercentileChip.tsx` | Top X% chip; two flavors; canonical-slice clarifier in popover | ✓ VERIFIED | Both popover bodies include "matched conditions, not the current filtered view" |
| `tests/services/test_user_benchmark_percentiles_service_real_data.py` | User-28-shape regression test | ✓ VERIFIED | Test at line 554; asserts abs(value) < 0.10 for above-floor-only cell; passes |
| `tests/repositories/test_game_repository_zero_pending.py` | Stage B import gate test | ✓ VERIFIED | Test at line 283; asserts in_progress/pending blocks Stage B; completed does not; passes |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `gen_global_percentile_cdf.py` | `app.services.canonical_slice_sql` | `from app.services.canonical_slice_sql import ...` | ✓ WIRED | Byte-identical CDF regression gate passes |
| `user_benchmark_percentiles_service._compute_metric_for_user` | `canonical_slice_sql.per_user_cte_for` | Single call with `apply_floor=True` | ✓ WIRED | Line 118: `per_user_cte_for(metric_id, source="single_user", apply_floor=True)`. No second call. |
| `import_service._complete_import_job` | `compute_stage_a` | `asyncio.create_task(compute_stage_a(job.user_id))` | ✓ WIRED | Line 504; outside async with block (not in transaction) |
| `eval_drain._drain_loop` | `users_with_zero_pending` + `compute_stage_b` | `zero_pending = await users_with_zero_pending(...)` then `asyncio.create_task(compute_stage_b(uid))` | ✓ WIRED | Lines 566-568; gate includes active-import check |
| `endgame_service` chip emission | `user_benchmark_percentiles_repository.fetch_for_user` | `fetch_for_user(session, user_id=user_id)` | ✓ WIRED | Two call sites (lines 2420 and 2801); percentile_rows passed to score gap builders |
| `PercentileChip.tsx` | `EndgameMetricCard.tsx`, `EndgameOverallScoreGapRow.tsx`, `EndgameOverallPerformanceSection.tsx` | import + `<PercentileChip percentile={...} />` | ✓ WIRED | grep confirms 3 consumer files render PercentileChip from the materialised percentile prop |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `compute_stage_a` → `user_benchmark_percentiles` | `value`, `percentile` (score_gap) | `_compute_metric_for_user` with `apply_floor=True` via `per_user_cte_for` | Yes — dev DB has rows written with real negative values for qualifying users | ✓ FLOWING |
| `compute_stage_b` → `user_benchmark_percentiles` (3 rows) | value, percentile per eval-dependent metric | same with Stage B metrics | Yes — dev DB shows achievable_score_gap value=-0.0599 for user 28 | ✓ FLOWING |
| `endgame_service.fetch_for_user` → chip | `percentile_rows[metric].percentile` | SELECT against `user_benchmark_percentiles` WHERE `user_id=current_user.id` | Yes — table has real rows post-backfill | ✓ FLOWING |
| `PercentileChip` render | `percentile` prop | endgame_service API response | Yes — chip renders `Top X%` when percentile row exists for user | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Alembic at drop-migration head | `uv run alembic current` | `a7f3c9b82e14 (head)` | ✓ PASS |
| n_cells_floor absent from DB schema | DB query: `SELECT column_name FROM information_schema.columns WHERE table_name='user_benchmark_percentiles'` | `['user_id', 'metric', 'value', 'percentile', 'cdf_snapshot', 'computed_at']` — no n_cells_floor | ✓ PASS |
| User 28 achievable_score_gap is negative (floor-respecting) | DB query: `SELECT metric, value, percentile FROM user_benchmark_percentiles WHERE user_id=28 ORDER BY metric` | `achievable_score_gap: value=-0.0599, percentile=18.1` (Top ~82%) — NOT +0.12 (Top 6%) | ✓ PASS |
| No apply_floor=False in production service code | `grep -c "apply_floor=False" app/services/user_benchmark_percentiles_service.py` | 1 (in docstring comment only, not executable code) | ✓ PASS |
| No n_cells_floor in production or test executable code | `grep -rn "n_cells_floor" app/ scripts/ tests/ \| grep -v "alembic/versions" \| wc -l` | 8 (all in comments/docstrings) | ✓ PASS |
| import_jobs EXISTS predicate in users_with_zero_pending | `grep -c "import_jobs" app/repositories/game_repository.py` | 1 (line 95 — the import + predicate) | ✓ PASS |
| SC-7 parity test enabled and passing | `uv run pytest tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -q` | `1 passed` (no skip) | ✓ PASS |
| Full backend test suite | `uv run pytest -x -q` | `1708 passed, 13 skipped, 3 warnings` | ✓ PASS |
| Full frontend test suite | `npm test -- --run` | `640 passed (56 test files)` | ✓ PASS |
| Type check | `uv run ty check app/ tests/` | `All checks passed!` | ✓ PASS |
| Lint | `uv run ruff check app/ tests/ scripts/` | `All checks passed!` | ✓ PASS |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| PCTL-07 | Plans 03, 07, 09, 13 | Chip is a trait, not a view; filter-independent; canonical slice matches benchmark CTE | ✓ SATISFIED | Backend rewire complete (`fetch_for_user` replaces `interpolate_percentile`). Canonical slice CTE shared via `canonical_slice_sql.py`. Stage B gate prevents transient partial values during active imports. Live UI filter-toggle invariance deferred to human UAT (prerequisite met: rows exist in dev DB). |
| PCTL-08 | Plans 01, 03, 04, 05, 11, 13 | Persisted in `user_benchmark_percentiles` with (user_id, metric) PK, value, percentile, cdf_snapshot, computed_at; UPSERT semantics; ON DELETE CASCADE | ✓ SATISFIED | Table confirmed with all required columns. `n_games`/`n_cells_floor` column dropped by explicit user decision (override accepted — row-absence replaces the numeric gate). Migration chain: add (Plan 01) → rename (Plan 11) → drop (Plan 13). |
| PCTL-09 | Plans 05, 06, 08, 09, 12, 13 | Two-stage compute: Stage A post-import (score_gap), Stage B post-cold-drain (3 eval-dependent metrics); fire-and-forget background tasks; Stage B gated against active imports | ✓ SATISFIED | Stage A: `import_service.py:504`. Stage B: `eval_drain.py:566-568` via `users_with_zero_pending` with Plan 13 active-import gate. Real-data integration tests confirm rows are written for qualifying users. Dev DB shows real (value, percentile) rows for user 28 and others. |
| PCTL-10 | Plans 08, 09, 13 | `scripts/backfill_user_percentiles.py` with --target dev\|prod, idempotent, --user-id / --metric narrowing, summary table, tunnel guard | ✓ SATISFIED | Script confirmed: all CLI flags, tunnel-up check, V4 URL guard, idempotent UPSERT. Plan 13 simplified `_compute_and_count` to True/None (no `skipped_below_floor`). Dev DB re-backfilled in Plan 13 Task 6. Summary table emits `upserted / skipped_no_canonical_games / skipped_no_eval` per metric. |

No orphaned requirements: REQUIREMENTS.md maps PCTL-07/08/09/10 to Phase 94.1 and all four appear in plan frontmatter across Plans 03-13.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `alembic/versions/20260524_000000_add_user_benchmark_percentiles.py` | ~62 | `CREATE CAST (varchar AS benchmark_metric) ... IMPLICIT` — implicit cast weakens type safety (REVIEW.md WR-05; carried from previous verification) | ⚠️ Warning | Erodes ENUM type safety project-wide; not introduced by Plan 13. Acceptable for v1.19. |
| `app/services/user_benchmark_percentiles_service.py` | 163 | `cdf_snapshot=date.today()` stores local compute date, not CDF artifact's labeled snapshot month (REVIEW.md WR-02; carried from previous verification) | ⚠️ Warning | Semantic approximation; the re-lookup-by-snapshot use case cannot distinguish CDF versions by date alone. Out of scope for this phase. |
| `tests/scripts/test_backfill_user_percentiles.py` | 300, 322 | `@pytest.mark.asyncio` on synchronous test functions (PytestWarning in test output) | ⚠️ Info | Cosmetic warning only; tests pass and the mark has no behavioral effect on sync functions. Not introduced by Plan 13. |

No `TBD`, `FIXME`, or `XXX` markers found in files modified by this phase.

---

### Human Verification Required

The automated checks have all passed. Three items require human verification before the phase is fully closed:

#### 1. Chip is decoupled from filter state (live UI)

**Test:** Start the dev backend (`uv run uvicorn app.main:app --reload`). Log in as user 28 (or any user with rows in `user_benchmark_percentiles`). Navigate to `/endgames`. Observe the percentile chip values. Toggle Recency / Opponent Strength / TC / Platform / Rated / Opponent Type filters one by one. 
**Expected:** The chip value (e.g., "Top 82%") does NOT change as filters change. The row's metric value (score gap, achievable score gap) DOES change with filter state.
**Why human:** Interactive browser test; filter-toggle invariance cannot be asserted by grep. The underlying wiring is verified (table-read not filter-dependent), but the end-to-end UI behavior requires a live session.

#### 2. Tooltip copy reads naturally on hover and tap

**Test:** Hover the chip (desktop) to open the popover; tap it on mobile or responsive viewport (375px). Read the popover body in both skill-isolating and improvement-focus flavors.
**Expected:** Short, clear prose including "Reflects your career under matched conditions, not the current filtered view." No jargon, no caveats, within `text-xs` minimalism discipline (one short paragraph per `feedback_popover_copy_minimalism.md`).
**Why human:** Subjective readability/UX check; cannot be automated.

#### 3. Production backfill post-deploy

**Test:** After the phase merges to `production`: `bin/prod_db_tunnel.sh start && uv run python scripts/backfill_user_percentiles.py --target prod`. Spot-check `user_benchmark_percentiles` via `mcp__flawchess-prod-db__query`.
**Expected:** Summary table shows `upserted > 0` for `score_gap` (at minimum); `achievable_score_gap` and section2 metrics upserted for users with cold-drain complete. At least a handful of users have chip-eligible rows.
**Why human:** Operator-only action requiring SSH tunnel; cannot be pre-verified.

---

### Re-Verification Summary

The previous verification (status: gaps_found, score 4/7) identified three blockers:

1. **SQLAlchemy bindparam parsing bug** (`SELECT :user_id::int`): Fixed in Plan 09 — changed to `SELECT CAST(:user_id AS int) AS user_id`. Confirmed by SC-7 parity test passing and by user 28's dev DB rows having real negative values.

2. **SC-7 parity test was `pytest.skip`**: Fixed in Plan 09 — skip marker removed, test now passes (`1 passed`).

3. **Backfill script silently returned `no_canonical_games` for all users**: Fixed by Plan 09 (root cause = bindparam bug). Plan 13 re-backfilled the dev DB; user 28's `achievable_score_gap` corrected from +0.1204 (Top 6%) to −0.0599 (Top ~82%).

Additionally, Plan 13 closed two correctness bugs surfaced during Plan 11 HUMAN-UAT:

4. **apply_floor=False caused inflated values**: Fixed — `_compute_metric_for_user` now uses a single `apply_floor=True` query.

5. **Stage B fired multiple times during active imports**: Fixed — `users_with_zero_pending` now gates on both zero pending evals AND no active import_jobs.

All 7 ROADMAP success criteria are now verified (1 via accepted override for the n_cells_floor drop). The three remaining human verification items are live-UI and operator actions that cannot be automated.

---

_Verified: 2026-05-23T22:35:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes (previous status: gaps_found 4/7 → current status: human_needed 7/7)_
