---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 03
type: execute
wave: 1
depends_on:
  - 94.1-01
files_modified:
  - app/services/canonical_slice_sql.py
  - scripts/gen_global_percentile_cdf.py
autonomous: true
requirements:
  - PCTL-07
  - PCTL-08
must_haves:
  truths:
    - "Shared module `app/services/canonical_slice_sql.py` exists exporting the 7 builders parametrised on `source: Literal['benchmark','single_user']`"
    - "scripts/gen_global_percentile_cdf.py imports from the shared module — no inline _per_user_cte_* helpers remain in the script"
    - "Byte-identical CDF emission post-refactor — Plan 01's regression test `test_gen_global_percentile_cdf_unchanged.py` passes (D-11, ROADMAP SC 7)"
    - "Cross-consumer fragment identity test passes — `test_canonical_slice_sql.py::test_per_user_cte_for_all_four_metrics_byte_identical_shared_fragments`"
    - "Single-user variant drops `g.time_control_bucket::text = su.tc_bucket` predicate per D-09 pooling"
    - "`per_user_cte_for` (and the 3 per-metric builders) accepts `apply_floor: bool = True`; when False, the HAVING floor gate is dropped so Plan 05 can fetch value-only below-floor rows"
  artifacts:
    - path: "app/services/canonical_slice_sql.py"
      provides: "Shared canonical-slice CTE builders consumed by both benchmark CDF gen + per-user compute service (Plan 05)"
      contains: "selected_users_cte, elo_bucket_expr, equal_footing_filter_sql, sparse_exclusion_sql, per_user_cte_score_gap, per_user_cte_achievable, per_user_cte_section2, per_user_cte_for"
    - path: "scripts/gen_global_percentile_cdf.py"
      provides: "Refactored consumer — imports from app.services.canonical_slice_sql; produces byte-identical CDF output"
  key_links:
    - from: "scripts/gen_global_percentile_cdf.py"
      to: "app/services/canonical_slice_sql"
      via: "import statement"
      pattern: "from app.services.canonical_slice_sql import"
    - from: "app/services/canonical_slice_sql.py"
      to: "scripts/gen_global_percentile_cdf.py:214-509 (extraction source)"
      via: "verbatim SQL fragments + `source` parameter toggling 2 known structural diffs"
      pattern: "if source == \"benchmark\""
---

<objective>
Extract the 7 canonical-slice CTE builders from `scripts/gen_global_percentile_cdf.py` (lines 214-509) into a new shared module `app/services/canonical_slice_sql.py`, parametrised by `source: Literal["benchmark", "single_user"]`. Refactor `scripts/gen_global_percentile_cdf.py` to consume the shared module. Both consumers MUST produce identical output for the benchmark path (regression-guarded by Plan 01's `test_gen_global_percentile_cdf_unchanged.py`).

This is D-11 + ROADMAP SC 7. It is the gate for Plan 05 (per-user compute service consumes the same module).

Purpose: Single source of truth for canonical-slice SQL — accidental drift between benchmark methodology and per-user compute methodology becomes impossible at the source code level.
Output: 1 new module + 1 modified script + 2 Wave-0 tests turning green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-VALIDATION.md

<interfaces>
Existing — from `scripts/gen_global_percentile_cdf.py` (the extraction source):
- `_canonical_selected_users_cte()` — at line 214; produces the `benchmark_selected_users JOIN benchmark_ingest_checkpoints JOIN users` cohort CTE
- `_elo_bucket_expr(user_elo_alias)` — at line 242; CASE WHEN expression mapping median_elo → ELO bucket label
- `_equal_footing_filter_sql()` — at line 257; the ±100 ELO opponent + status='completed' + standard variant + 36-month recency filter
- `_sparse_exclusion_sql(elo_col, tc_col)` — at line 267; `(elo_col, tc_col) NOT IN ((2400, 'classical'))`
- `_per_user_cte_score_gap()` — at line 272
- `_per_user_cte_achievable()` — at line 327
- `_per_user_cte_section2(bucket_label: Literal["conversion", "parity"])` — at line 394
- `_per_user_cte_for(metric_id: CdfMetricId)` — at line 499 (dispatch)
- `CdfMetricId` Literal — from `app.services.global_percentile_cdf`
- Per-metric inclusion floors at lines 58-61 (`SCORE_GAP_MIN_ENDGAME_N`, `SCORE_GAP_MIN_NON_ENDGAME_N`, `ACHIEVABLE_MIN_GAMES`, `SECTION2_MIN_SPANS_PER_BUCKET`) — extract as module-level constants in canonical_slice_sql.py per CLAUDE.md no-magic-numbers rule

After refactor — `app/services/canonical_slice_sql.py` exports:
- `selected_users_cte(*, source: Literal["benchmark", "single_user"]) -> str`
- `elo_bucket_expr(user_elo_alias: str) -> str`
- `equal_footing_filter_sql() -> str`
- `sparse_exclusion_sql(*, elo_col: str, tc_col: str) -> str`
- `per_user_cte_score_gap(*, source: Literal["benchmark", "single_user"], apply_floor: bool = True) -> str`
- `per_user_cte_achievable(*, source: Literal["benchmark", "single_user"], apply_floor: bool = True) -> str`
- `per_user_cte_section2(*, bucket_label: Literal["conversion", "parity"], source: Literal["benchmark", "single_user"], apply_floor: bool = True) -> str`
- `per_user_cte_for(metric_id: CdfMetricId, *, source: Literal["benchmark", "single_user"], apply_floor: bool = True) -> str`
- Module-level constants: `SCORE_GAP_MIN_ENDGAME_N`, `SCORE_GAP_MIN_NON_ENDGAME_N`, `ACHIEVABLE_MIN_GAMES`, `SECTION2_MIN_SPANS_PER_BUCKET`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared canonical-slice SQL module</name>
  <files>app/services/canonical_slice_sql.py</files>
  <read_first>
    - scripts/gen_global_percentile_cdf.py (lines 1-540 — read in full; this is the verbatim extraction source)
    - app/services/global_percentile_cdf.py (CdfMetricId Literal definition)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`app/services/canonical_slice_sql.py` analog section — signatures + the 2 structural diffs)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 5 + §Security Domain — V5 SQL injection mitigation note)
    - tests/services/test_canonical_slice_sql.py (Plan 01 — the contract this module satisfies)
  </read_first>
  <action>
    Author `app/services/canonical_slice_sql.py` — pure-Python SQL string builders, NO DB I/O.

    Structure:

    1. Module docstring referencing D-11, ROADMAP SC 7, and the extraction source `scripts/gen_global_percentile_cdf.py:214-509`. Document the 2 structural differences between `source="benchmark"` and `source="single_user"`: (a) selected_users CTE shape; (b) `g.time_control_bucket::text = su.tc_bucket` predicate (kept for benchmark, dropped for single_user per D-09 pooling). Also document the `apply_floor` dual-mode: `apply_floor=True` (default) retains the per-metric HAVING inclusion-floor gate (existing benchmark CDF behaviour, no semantic change for the benchmark consumer); `apply_floor=False` drops the floor predicate so Plan 05 can emit a "value-without-floor" branch for the below-floor `percentile=NULL` case (resolves RESEARCH Open Q3).

    2. Imports — `from typing import Literal` + `from app.services.global_percentile_cdf import CdfMetricId`. NO SQLAlchemy import; this module emits raw SQL strings consumed by `text(...)` at the call site.

    3. Module-level constants (per CLAUDE.md no-magic-numbers, extract from gen_global_percentile_cdf.py lines 58-61):
       - `SCORE_GAP_MIN_ENDGAME_N: int = 30`
       - `SCORE_GAP_MIN_NON_ENDGAME_N: int = 30`
       - `ACHIEVABLE_MIN_GAMES: int = 20`
       - `SECTION2_MIN_SPANS_PER_BUCKET: int = 20`

    4. The 7 builders with `source` parameter. Implementation strategy:
       - Copy each helper's body verbatim from `scripts/gen_global_percentile_cdf.py:214-509`.
       - For `selected_users_cte`: if `source == "benchmark"` → return the existing benchmark cohort JOIN. If `source == "single_user"` → return `"selected_users AS (SELECT :user_id::int AS user_id)"` (bindparam, not f-string — Security V5).
       - For `per_user_cte_score_gap` / `per_user_cte_achievable` / `per_user_cte_section2`: locate the `g.time_control_bucket::text = su.tc_bucket` predicate inside each builder (script lines 304, ~360, ~430 — verify exact lines on read). If `source == "single_user"`, omit that predicate; if `"benchmark"`, keep it. Additionally: locate the per-metric `HAVING` clause that enforces the inclusion floor (`HAVING n_endgame_games >= :score_gap_min_endgame_n AND n_non_endgame_games >= :score_gap_min_non_endgame_n`, `HAVING n_games >= :achievable_min_games`, `HAVING n_spans >= :section2_min_spans_per_bucket`). When `apply_floor=True` (default), keep the HAVING clause verbatim — benchmark behaviour is unchanged. When `apply_floor=False`, omit the HAVING clause so the CTE emits all rows regardless of floor; Plan 05 uses this branch to surface a value with `percentile=NULL` when below floor.
       - For `per_user_cte_for(metric_id, *, source, apply_floor=True)`: dispatch by metric_id to the appropriate per-metric builder, threading both `source` and `apply_floor` through.

    5. Type-safety per CLAUDE.md — every function has explicit return type `-> str`; `source` is `Literal["benchmark", "single_user"]`; `bucket_label` is `Literal["conversion", "parity"]`; `apply_floor` is `bool = True` (keyword-only, default preserves benchmark semantics).

    6. Security note in module docstring (V5): all SQL fragments embed STATIC SQL only. User-controlled values (`user_id`) flow through `:user_id` bindparams resolved by SQLAlchemy `text(...).bindparams(user_id=user_id)` at the call site (Plan 05). Never f-string user input into the SQL strings here.

    Do NOT call this module from `scripts/gen_global_percentile_cdf.py` yet — Task 2 wires the consumer.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `app/services/canonical_slice_sql.py`
    - All 8 functions exported (5 builders with `source` param + 3 helpers — elo_bucket_expr / equal_footing_filter_sql / sparse_exclusion_sql)
    - `grep -c "def " app/services/canonical_slice_sql.py` ≥ 8
    - `grep -n "f\"" app/services/canonical_slice_sql.py` returns no matches for any `user_id` substitution (V5 — must use `:user_id` bindparam syntax)
    - 4 module-level constants present (`SCORE_GAP_MIN_ENDGAME_N`, etc.); no magic 20 or 30 literals in builder bodies
    - All `Plan 01` cross-consumer fragment-identity tests pass: `uv run pytest tests/services/test_canonical_slice_sql.py -x` exits 0
    - `uv run ruff check app/services/canonical_slice_sql.py` exits 0
    - `uv run ty check app/services/canonical_slice_sql.py` exits 0
    - Module docstring references D-11 and the extraction source line range
    - Module docstring documents the `apply_floor` dual-mode (True = HAVING gate kept; False = gate dropped) and references RESEARCH Open Q3
    - Plan 01's `tests/services/test_canonical_slice_sql.py` includes (or — if scaffolded by Plan 01 without this case — must be extended in this task to include) at least one assertion that `per_user_cte_for(metric_id, source='single_user', apply_floor=False)` returns SQL with no `HAVING` clause, and that `apply_floor=True` (default) returns SQL containing the per-metric HAVING gate. If Plan 01's scaffold lacks the test, add it here and confirm it goes RED→GREEN with this task's implementation
    - The benchmark consumer (Plan 03 Task 2) MUST call `per_user_cte_for(metric_id, source='benchmark')` without specifying `apply_floor`, relying on the default `True` so the CDF output stays byte-identical (the regression gate `test_gen_global_percentile_cdf_unchanged.py` enforces this)
  </acceptance_criteria>
  <done>Shared module committed; Plan 01 fragment-identity tests green; `scripts/gen_global_percentile_cdf.py` is NOT yet rewired (Task 2 does that).</done>
</task>

<task type="auto">
  <name>Task 2: Refactor gen_global_percentile_cdf.py to consume shared module</name>
  <files>scripts/gen_global_percentile_cdf.py</files>
  <read_first>
    - scripts/gen_global_percentile_cdf.py (full file — confirm post-Task-1 the duplicated helpers can be removed without touching the orchestration code at lines 1-200 + 510-end)
    - app/services/canonical_slice_sql.py (just authored)
    - tests/scripts/test_gen_global_percentile_cdf_unchanged.py (Plan 01 — the byte-identical CDF regression test this refactor MUST pass)
  </read_first>
  <action>
    Refactor `scripts/gen_global_percentile_cdf.py`:

    1. Add imports at the top:
       `from app.services.canonical_slice_sql import (`
       `    selected_users_cte, elo_bucket_expr, equal_footing_filter_sql, sparse_exclusion_sql,`
       `    per_user_cte_for, per_user_cte_score_gap, per_user_cte_achievable, per_user_cte_section2,`
       `    SCORE_GAP_MIN_ENDGAME_N, SCORE_GAP_MIN_NON_ENDGAME_N, ACHIEVABLE_MIN_GAMES, SECTION2_MIN_SPANS_PER_BUCKET,`
       `)`

    2. Delete the now-duplicated helpers from the script:
       - `_canonical_selected_users_cte` (~line 214)
       - `_elo_bucket_expr` (~line 242)
       - `_equal_footing_filter_sql` (~line 257)
       - `_sparse_exclusion_sql` (~line 267)
       - `_per_user_cte_score_gap` (~line 272)
       - `_per_user_cte_achievable` (~line 327)
       - `_per_user_cte_section2` (~line 394)
       - `_per_user_cte_for` (~line 499)
       - The 4 inline constants at lines 58-61 (now imported from shared module)

    3. Update call sites within the script (likely in `_build_metric_breakpoint_query` around line 518): change `_per_user_cte_for(metric_id)` → `per_user_cte_for(metric_id, source="benchmark")`; same for all other inlined-helper call sites — thread `source="benchmark"` through every call. Do NOT pass `apply_floor` explicitly — the default `True` matches the pre-refactor benchmark behaviour and is what the byte-identical CDF regression gate expects.

    4. Verify byte-identical CDF output:
       - Run `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` — expected GREEN.
       - If RED, the refactor changed SQL output; debug by string-diffing the pre/post outputs (the fixture file from Plan 01 captures the pre-refactor strings).

    5. Add a comment block near the imports referencing D-11 and PHASE-94.1: "Canonical-slice CTE builders extracted to `app.services.canonical_slice_sql` per Phase 94.1 D-11. Single source of truth — both this script and the per-user compute service consume the same module."

    Do NOT regenerate `app/services/global_percentile_cdf.py` (the data artifact). The refactor changes only the SQL-builder consumer, not the data output, so the artifact is unchanged. Confirm: `git diff --stat app/services/global_percentile_cdf.py` shows zero changes post-refactor.

    Pre-PR gate at the end of Task 2 — required since this is the last task in Plan 03:
    - `uv run ruff format app/services/canonical_slice_sql.py scripts/gen_global_percentile_cdf.py`
    - `uv run ruff check app/services/canonical_slice_sql.py scripts/gen_global_percentile_cdf.py --fix`
    - `uv run ty check app/ tests/`
    - `uv run pytest tests/services/test_canonical_slice_sql.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x`
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_canonical_slice_sql.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x && uv run ty check scripts/ app/services/canonical_slice_sql.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gen_global_percentile_cdf.py` imports from `app.services.canonical_slice_sql`
    - `grep -n "^def _per_user_cte\|^def _canonical_selected_users\|^def _equal_footing_filter_sql\|^def _sparse_exclusion_sql\|^def _elo_bucket_expr" scripts/gen_global_percentile_cdf.py | grep -v '^#'` returns 0 (all 7 helpers removed)
    - `grep -c "source=\"benchmark\"\|source='benchmark'" scripts/gen_global_percentile_cdf.py` ≥ 4 (one per metric call site)
    - `git diff app/services/global_percentile_cdf.py` returns empty (data artifact unchanged)
    - `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` exits 0 (byte-identical CDF gate)
    - `uv run pytest tests/services/test_canonical_slice_sql.py -x` exits 0
    - Pre-PR gate clean: ruff format/check + ty check + targeted pytest all exit 0
  </acceptance_criteria>
  <done>Script refactored to import shared module; regression gate passes; both consumers exercise the same SQL builders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical_slice_sql builder → caller | Builders emit STATIC SQL strings; only `:user_id` bindparam carries user-controlled data (resolved at call site in Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-06 | Tampering (V5) | canonical_slice_sql.selected_users_cte single_user variant | mitigate | Use `:user_id` bindparam in the returned SQL string; never f-string the user_id. Plan 05's compute service invokes `text(sql).bindparams(user_id=user_id)`. Per RESEARCH §Security Domain row 1. |
| T-94.1-07 | Tampering | Per-user CTE drift from benchmark CTE | mitigate | Single shared module + Plan 01's `test_per_user_cte_for_all_four_metrics_byte_identical_shared_fragments` + Plan 01's `test_gen_global_percentile_cdf_unchanged` together guarantee no drift at extraction time and no drift at future edit time. Pitfall 8 guard. |
</threat_model>

<verification>
- `app/services/canonical_slice_sql.py` exports the 8 builders + 4 constants
- `scripts/gen_global_percentile_cdf.py` no longer defines any of the 7 extracted helpers
- Regression: byte-identical CDF emission
- Pre-PR gate: ruff format/check + ty check + targeted pytest exits 0
</verification>

<success_criteria>
- Cross-consumer fragment-identity test (Plan 01) is GREEN
- Byte-identical CDF regression test (Plan 01) is GREEN
- `app/services/global_percentile_cdf.py` data file unchanged (git diff empty)
- Plan 05 can now import the shared module without re-deriving canonical-slice SQL
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-03-SUMMARY.md` — list the new module's exported builders, count lines extracted from the script, confirm regression gate green, note any structural differences encountered between benchmark/single_user variants.
</output>
