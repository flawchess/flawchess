---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 06
type: execute
wave: 3
depends_on:
  - 94.1-05
files_modified:
  - app/services/import_service.py
  - app/services/eval_drain.py
autonomous: true
requirements:
  - PCTL-09
must_haves:
  truths:
    - "Stage A hook fires inside `_complete_import_job` AFTER `session.commit()` at line 499, NOT inside the import transaction (D-03 / ROADMAP SC 3)"
    - "Stage A is enqueued as `asyncio.create_task(compute_stage_a(user_id))` — fire-and-forget, non-blocking"
    - "Stage B hook fires after `_mark_evals_completed` + `session.commit()` at `eval_drain.py:547-548`"
    - "Stage B groups just-drained game_ids by user_id, queries `count_pending_evals(session, user_id)` per affected user, fires `compute_stage_b` ONLY for users whose count is now exactly zero (D-01 / Pitfall 1)"
    - "Stage B uses a fresh read session opened after the eval-write commit (CLAUDE.md — never share AsyncSession across coroutines)"
    - "Neither hook extends import latency or drain-tick latency — both rely on `asyncio.create_task` fire-and-forget"
  artifacts:
    - path: "app/services/import_service.py"
      provides: "Stage A trigger after `_complete_import_job`'s session.commit"
      contains: "asyncio.create_task(compute_stage_a(...))"
    - path: "app/services/eval_drain.py"
      provides: "Stage B trigger after `_mark_evals_completed` commit; per-user pending-count grouping"
      contains: "asyncio.create_task(compute_stage_b(...))"
  key_links:
    - from: "import_service._complete_import_job"
      to: "user_benchmark_percentiles_service.compute_stage_a"
      via: "asyncio.create_task outside the async-with session block"
      pattern: "asyncio.create_task(compute_stage_a"
    - from: "eval_drain (post _mark_evals_completed commit)"
      to: "user_benchmark_percentiles_service.compute_stage_b"
      via: "for each affected user with pending_count == 0 → create_task"
      pattern: "if pending == 0:"
    - from: "eval_drain stage B"
      to: "game_repository.count_pending_evals (reused verbatim)"
      via: "import + function call per affected user_id"
      pattern: "count_pending_evals"
---

<objective>
Wire the two non-blocking background-task hooks. Stage A fires after the import-completion commit (1 modified file). Stage B fires after the eval-drain `_mark_evals_completed` commit, with per-user pending-count grouping (1 modified file).

Purpose: Connects the import + eval-drain pipelines to the compute service. No new behavior beyond `asyncio.create_task` + the existing `count_pending_evals` repo helper.
Output: 2 modified files; Plan 02's `test_import_service_stage_a.py` + `test_eval_drain_stage_b.py` turning green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md

<interfaces>
Existing — consumed:
- `app.services.user_benchmark_percentiles_service.compute_stage_a(user_id: int) -> None` (Plan 05)
- `app.services.user_benchmark_percentiles_service.compute_stage_b(user_id: int) -> None` (Plan 05)
- `app.repositories.game_repository.count_pending_evals(session, user_id) -> int` (lines 46-53 — reused verbatim, no edit)
- `app.core.database.async_session_maker`
- `app.models.game.Game`
- `sqlalchemy.select` + `Game.user_id.distinct()` + `Game.id.in_(game_ids)`

Hook sites:
- `app/services/import_service.py:480-499` — `_complete_import_job`, hook fires AFTER `await session.commit()` at line 499
- `app/services/eval_drain.py:538-548` — `_mark_evals_completed` + `session.commit()` block; hook fires AFTER the commit, with a fresh read session
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage A hook in import_service._complete_import_job</name>
  <files>app/services/import_service.py</files>
  <read_first>
    - app/services/import_service.py (lines 440-510 — read the `_complete_import_job` function in full; identify the exact line where `session.commit()` runs and the `async with async_session_maker()` block closes; this is where Stage A must fire)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`app/services/import_service.py:480` section)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 3, §Anti-patterns "Running Stage A inside the import transaction")
    - app/routers/imports.py (lines 67-69 — `asyncio.create_task` precedent)
    - tests/services/test_import_service_stage_a.py (Plan 02 — the contract this satisfies)
  </read_first>
  <action>
    Locate `_complete_import_job` (around line 480). Read the function in full. Identify:
    - The `JobState` parameter carrying `user_id`.
    - The `async with async_session_maker() as session:` block that updates `import_jobs` and ends with `await session.commit()`.

    Add the Stage A hook OUTSIDE that `async with` block — after the session has committed AND closed:

    1. Import `asyncio` if not already imported.
    2. Import `compute_stage_a` from `app.services.user_benchmark_percentiles_service`. Note: this creates a service→service import in the import pipeline. If this triggers a circular import warning, use a local function-scope import inside `_complete_import_job` (a common pattern in this codebase for breaking late-binding cycles).
    3. After the existing `async with` block closes (the `session.commit()` already ran), add a single line:
       ```
       asyncio.create_task(compute_stage_a(job.user_id))
       ```
       (Adjust the field accessor to match `JobState` — if it's `job_state.user_id` or `job.user_id`, use whichever the local symbol actually is.)
    4. Add a comment above the line: `# Phase 94.1 D-03 / ROADMAP SC 3: Stage A fires AFTER commit, NOT inside the import transaction. Fire-and-forget — errors are captured to Sentry inside compute_stage_a; never propagated.`

    Critical:
    - The `asyncio.create_task(...)` line MUST be at the same indentation level as the closing of the `async with` block, NOT nested inside it (RESEARCH §Anti-patterns "Running Stage A inside the import transaction").
    - Do not `await` the task — fire-and-forget. The Plan 02 test `test_stage_a_exception_does_not_break_import_completion` verifies non-propagation.

    Verify by re-reading the diff: the `asyncio.create_task` line is OUTSIDE the `async with async_session_maker()` block.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_import_service_stage_a.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "asyncio.create_task(compute_stage_a" app/services/import_service.py` returns exactly 1 match
    - The matched line is OUTSIDE any `async with async_session_maker()` block at the `_complete_import_job` site (verifiable by reading the diff context)
    - A comment near the hook references D-03 / ROADMAP SC 3
    - All 3 tests in `test_import_service_stage_a.py` pass: after-commit ordering, runs-outside-transaction, exception-does-not-propagate
    - `uv run ty check app/services/import_service.py` exits 0
    - `grep -n "from app.services.user_benchmark_percentiles_service" app/services/import_service.py` returns ≥ 1 match (top-level OR function-scope import)
  </acceptance_criteria>
  <done>Stage A hook landed; Plan 02 import_service tests green; no extension of import latency (task is `asyncio.create_task`, not awaited).</done>
</task>

<task type="auto">
  <name>Task 2: Stage B hook in eval_drain post-commit</name>
  <files>app/services/eval_drain.py</files>
  <read_first>
    - app/services/eval_drain.py (lines 500-580 — read the full block containing `_mark_evals_completed` + the commit; locate the `for batch in ...` loop or equivalent surrounding context; identify the variable holding the just-drained `game_ids`)
    - app/repositories/game_repository.py (lines 46-53 — `count_pending_evals` shape; reused verbatim, do not modify)
    - app/models/game.py (Game model — confirm `Game.user_id` and `Game.id` mapped attributes)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`app/services/eval_drain.py:547-548` section — verbatim Stage B trigger shape)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 4, §Common Pitfalls 1 + 5)
    - tests/services/test_eval_drain_stage_b.py (Plan 02 — the contract this satisfies)
  </read_first>
  <action>
    Locate the `_mark_evals_completed(session, game_ids)` + `await session.commit()` site (around line 547-548). Read enough surrounding context to identify the local symbol holding the just-drained `game_ids` list.

    After the `async with async_session_maker() as session:` block closes (the eval-write session committed AND closed), insert the Stage B trigger logic OUTSIDE that block — opening a fresh read session per CLAUDE.md "never share AsyncSession across coroutines":

    1. Import `asyncio` if not already imported.
    2. Import `select`, `func` from sqlalchemy if not present.
    3. Import `Game` from `app.models.game` if not present.
    4. Import `count_pending_evals` from `app.repositories.game_repository` if not present.
    5. Import `compute_stage_b` from `app.services.user_benchmark_percentiles_service` (function-scope if circular, top-level otherwise).

    Insert the trigger block:
    ```
    # Phase 94.1 D-01 / Pitfall 1: Stage B fires for users whose pending-eval
    # count just transitioned to zero. Group just-drained game_ids by user_id;
    # for each affected user, query count_pending_evals; only fire when count == 0.
    # Fresh read session — never share the eval-write session across coroutines.
    async with async_session_maker() as read_session:
        user_id_rows = await read_session.execute(
            select(Game.user_id).distinct().where(Game.id.in_(game_ids))
        )
        affected_user_ids = [row[0] for row in user_id_rows.all()]
        for user_id in affected_user_ids:
            pending = await count_pending_evals(read_session, user_id)
            if pending == 0:
                asyncio.create_task(compute_stage_b(user_id))
    ```

    Critical:
    - The new block is OUTSIDE the original eval-write `async with`. The original commit must complete BEFORE the read session opens.
    - `count_pending_evals` is reused verbatim from `game_repository.py:46-53`. Do NOT add a duplicate helper.
    - The `affected_user_ids` query targets the `ix_games_evals_pending` partial index implicitly via the JOIN on `Game.id` then the per-user count query (which IS partial-indexed). Pitfall 1's "fires too often" risk is bounded by the per-user count check.
    - Per Pitfall 5: a user with zero canonical-slice games for the 3 eval-dependent metrics will still have `compute_stage_b` fire — the compute service handles "zero canonical-slice rows → no row" cleanly (Plan 05 contract).

    Verify by re-reading the diff: the new `async with async_session_maker() as read_session:` block is at the same indentation as the original eval-write block's `async with` line, NOT nested inside it.

    Pre-PR gate at end of Task 2:
    - `uv run ruff format app/services/import_service.py app/services/eval_drain.py`
    - `uv run ruff check app/services/import_service.py app/services/eval_drain.py --fix`
    - `uv run ty check app/`
    - `uv run pytest tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x`
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_drain_stage_b.py -x && uv run ty check app/services/eval_drain.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "asyncio.create_task(compute_stage_b" app/services/eval_drain.py` returns exactly 1 match
    - The matched line is INSIDE a `for user_id in affected_user_ids` loop AND guarded by `if pending == 0:`
    - The new `async with async_session_maker() as read_session:` block is at the same indentation as the original eval-write block (verifiable by reading the diff context)
    - `grep -n "count_pending_evals" app/services/eval_drain.py` returns ≥ 1 match (reused verbatim from game_repository)
    - A comment references D-01 / Pitfall 1
    - All 4 tests in `test_eval_drain_stage_b.py` pass: fires-only-on-zero, fires-once-per-user-per-batch, count-query-targets-correct-user, exception-does-not-crash-drain
    - Pre-PR gate clean: ruff format/check + ty check + targeted pytest exits 0
  </acceptance_criteria>
  <done>Stage B hook landed; Plan 02 eval_drain tests green; drain-tick latency unchanged in the common no-user-completes case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import worker → background task | `compute_stage_a` exception MUST NOT propagate to import worker (D-04) |
| drain coroutine → background task | `compute_stage_b` exception MUST NOT propagate to drain coroutine (D-04) |
| eval-write session → Stage B read session | Fresh session per CLAUDE.md AsyncSession-not-shared rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-13 | Denial of Service | Stage B fires too often | mitigate | Per-user `count_pending_evals == 0` gate ensures Stage B fires at most ONCE per user per "drain queue drained" event (Pitfall 1). Typical 1-3 users per drain batch (RESEARCH Assumption A3). |
| T-94.1-14 | Tampering | Stage A inside import transaction | mitigate | `asyncio.create_task(compute_stage_a(...))` placed OUTSIDE the `async with async_session_maker()` block — physically impossible to run inside the tx. Plan 02 `test_stage_a_runs_outside_import_transaction` is the inversion guard. |
</threat_model>

<verification>
- `grep -n "create_task(compute_stage_a" app/services/import_service.py` → 1 match
- `grep -n "create_task(compute_stage_b" app/services/eval_drain.py` → 1 match
- All 7 tests across `test_import_service_stage_a.py` + `test_eval_drain_stage_b.py` pass
- Pre-PR gate clean
</verification>

<success_criteria>
- Stage A fires once per import completion, after commit
- Stage B fires once per (user, pending-count→0 transition), after eval-write commit
- No exceptions propagate from either stage to the trigger site
- Reused `count_pending_evals` — no duplicate helper added
- Forward-only — no existing test regressed
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-06-SUMMARY.md` — record exact line numbers where the two `asyncio.create_task` calls live, confirm both hooks are outside their respective `async with` blocks, list tests newly green.
</output>
