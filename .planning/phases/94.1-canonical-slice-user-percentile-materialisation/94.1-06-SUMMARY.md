---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: "06"
subsystem: api
tags: [asyncio, background-tasks, import-pipeline, eval-drain, percentile]

# Dependency graph
requires:
  - phase: 94.1-05
    provides: compute_stage_a and compute_stage_b entry points in user_benchmark_percentiles_service.py
  - phase: 94.1-02
    provides: test scaffolding for Stage A and Stage B hooks
provides:
  - Stage A hook wired in import_service._complete_import_job (fires asyncio.create_task after import commit)
  - Stage B hook wired in eval_drain (fires asyncio.create_task per user after pending-eval count reaches zero)
affects: [94.1-07, 94.1-08, import_service, eval_drain]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fire-and-forget asyncio.create_task OUTSIDE async-with session block (D-03 / ROADMAP SC 3)"
    - "Stage B per-user pending-count gate: count_pending_evals == 0 before firing (D-01 / Pitfall 1)"
    - "Fresh read session opened AFTER eval-write commit for Stage B user lookup (CLAUDE.md AsyncSession rule)"

key-files:
  created: []
  modified:
    - app/services/import_service.py
    - app/services/eval_drain.py

key-decisions:
  - "Top-level import of compute_stage_a into import_service (no circular import risk confirmed)"
  - "Top-level import of compute_stage_b and count_pending_evals into eval_drain (no circular import risk confirmed)"
  - "Stage B read session is separate from the eval-write session per CLAUDE.md AsyncSession-not-shared rule"

patterns-established:
  - "Stage A pattern: asyncio.create_task(compute_stage_a(job.user_id)) immediately after async-with closes"
  - "Stage B pattern: fresh read session groups game_ids by user_id, count_pending_evals gate, then create_task"

requirements-completed:
  - PCTL-09

# Metrics
duration: 15min
completed: 2026-05-23
---

# Phase 94.1 Plan 06: Stage A + Stage B Background-Task Hook Wiring Summary

**asyncio.create_task hooks wired into import_service and eval_drain — Stage A fires after import commit, Stage B fires per user when pending-eval count reaches zero**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-23T00:00:00Z
- **Completed:** 2026-05-23T00:15:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Stage A hook: `asyncio.create_task(compute_stage_a(job.user_id))` placed in `_complete_import_job` OUTSIDE the `async with async_session_maker() as session:` block — fires after the import transaction commits, never inside it (D-03 / ROADMAP SC 3)
- Stage B hook: fresh read session groups just-drained `game_ids` by `user_id` via `select(Game.user_id).distinct().where(Game.id.in_(game_ids))`; `count_pending_evals` gate ensures `create_task(compute_stage_b(user_id))` fires only when pending count == 0 (D-01 / Pitfall 1)
- Both hooks are fire-and-forget — exceptions captured inside the compute services, never propagated to the trigger site (D-04)
- Pre-PR gate clean: ruff format/check + `ty check app/` + 7 target tests passing (skipped = pass with `pytest.importorskip`)

## Task Commits

1. **Task 1: Stage A hook in import_service._complete_import_job** - `8c840d0f` (feat)
   - Line 504: `asyncio.create_task(compute_stage_a(job.user_id))` outside `async with` block
2. **Task 2: Stage B hook in eval_drain post-commit** - `a0396eb7` (feat)
   - Lines 552-564: fresh read session + per-user pending-count gate + `create_task(compute_stage_b(user_id))`

## Files Created/Modified

- `app/services/import_service.py` - Added top-level import of `compute_stage_a`; added Stage A hook at line 504 (outside `_complete_import_job`'s `async with` block)
- `app/services/eval_drain.py` - Added imports of `count_pending_evals` and `compute_stage_b`; added Stage B trigger block (lines 552-564) between the eval-write `async with` block and the `except asyncio.CancelledError:` clause

## Hook Locations (exact lines)

| Hook | File | Line | Context |
|------|------|------|---------|
| Stage A | `app/services/import_service.py` | 504 | After `_complete_import_job`'s `async with` closes |
| Stage B create_task | `app/services/eval_drain.py` | 564 | Inside `for user_id in affected_user_ids` + `if pending == 0:` |
| Stage B read session | `app/services/eval_drain.py` | 556 | After Step 5 eval-write `async with` closes, before `except CancelledError` |

## Decisions Made

- Top-level imports used for both `compute_stage_a` (in import_service) and `compute_stage_b` + `count_pending_evals` (in eval_drain) — no circular import risk confirmed by inspection of `user_benchmark_percentiles_service.py`
- Stage B uses `select(Game.user_id).distinct().where(Game.id.in_(game_ids))` to resolve the affected user_ids from the just-drained batch — minimizes DB round-trips (one query for all users)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Both hooks landed cleanly with no circular imports, type errors, or linting issues.

## Known Stubs

None.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The two `asyncio.create_task` calls are background-only triggers; they extend no trust boundary beyond what Plan 05 already established in the compute service.

## Next Phase Readiness

- Import pipeline now triggers Stage A (score_gap percentile) on every import completion
- Eval drain now triggers Stage B (3 eval-dependent percentiles) when a user's pending-eval queue drains to zero
- Plan 07 (chip read-path rewire in endgame_service + tooltip copy update) can proceed — the `user_benchmark_percentiles` table will be populated by the hooks wired here

---
*Phase: 94.1-canonical-slice-user-percentile-materialisation*
*Completed: 2026-05-23*
