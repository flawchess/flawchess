---
phase: 94.1
plan: "01"
subsystem: backend/tests
tags:
  - tests
  - wave-0
  - nyquist
  - canonical-slice
  - user-percentile
dependency_graph:
  requires: []
  provides:
    - tests/services/test_canonical_slice_sql.py
    - tests/scripts/test_gen_global_percentile_cdf_unchanged.py
    - tests/integration/test_alembic_migrations.py
    - tests/integration/test_benchmark_metric_enum.py
    - tests/repositories/test_user_benchmark_percentiles_repository.py
  affects:
    - plans/94.1-03 (canonical_slice_sql module — activated by importorskip removal)
    - plans/94.1-04 (repository + model — activated by importorskip removal)
tech_stack:
  added: []
  patterns:
    - pytest.importorskip for pre-implementation module guards
    - @pytest.mark.skipif with _migration_present() helper for migration guards
    - tests/integration/ directory pattern (first integration test dir in project)
key_files:
  created:
    - tests/services/test_canonical_slice_sql.py
    - tests/scripts/test_gen_global_percentile_cdf_unchanged.py
    - tests/integration/__init__.py
    - tests/integration/test_alembic_migrations.py
    - tests/integration/test_benchmark_metric_enum.py
    - tests/repositories/test_user_benchmark_percentiles_repository.py
  modified: []
decisions:
  - "Used pytest.importorskip at module level for canonical_slice_sql and repository tests — entire module skips gracefully on pre-Plan-03/04 CI runs"
  - "Used @pytest.mark.skipif(not _migration_present(), ...) for migration-dependent tests — activates automatically once the migration file lands"
  - "Captured _build_metric_breakpoint_query baseline as inline string constants in test file (not a separate fixtures/ file) — simpler, self-contained, and the SQL strings are readable alongside the test context"
  - "Downgrade test implemented as a source-file ordering check (non-destructive) rather than actually running alembic downgrade — avoids destabilising the shared test_engine session for other tests"
  - "Created tests/integration/ as the project's first integration test directory"
metrics:
  duration: "7 minutes"
  completed: "2026-05-23"
  tasks_completed: 2
  tasks_total: 2
  files_created: 6
  files_modified: 0
---

# Phase 94.1 Plan 01: Wave-0 Test Scaffolding Summary

Wave-0 Nyquist scaffolding: 5 test files (+ 1 `__init__.py`) defining the contracts for Plans 03 and 04 — shared SQL module, migration/ENUM, and repository — all failing gracefully via importorskip/skipif until the implementations land.

## What Was Built

### Test Files Created

| File | Tests | Guard Mechanism | Activates After |
|------|-------|-----------------|-----------------|
| `tests/services/test_canonical_slice_sql.py` | 6 (skipped via importorskip) | `pytest.importorskip("app.services.canonical_slice_sql")` | Plan 03 |
| `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` | 4 (skipped via `@pytest.mark.skip`) | Explicit skip with Plan 03 TODO | Plan 03 |
| `tests/integration/test_alembic_migrations.py` | 2 (skipped via importorskip + skipif) | importorskip model + `_migration_present()` | Plan 04 |
| `tests/integration/test_benchmark_metric_enum.py` | 1 (skipped via importorskip + skipif) | importorskip model + `_migration_present()` | Plan 04 |
| `tests/repositories/test_user_benchmark_percentiles_repository.py` | 6 (skipped via importorskip) | `pytest.importorskip("app.repositories.user_benchmark_percentiles_repository")` | Plan 04 |

**Total: 19 test functions across 5 files** (4 collected by pytest --collect-only, rest skip at module import time).

### Verification

`uv run pytest --collect-only -q` on the 5 files: 4 tests collected (the 4 parametrised regression tests), 5 module-level skips. Suite stays green with zero failures.

`uv run ruff check tests/ && uv run ruff format --check tests/ && uv run ty check tests/`: all pass with zero errors.

## Task Breakdown

### Task 1 — Canonical-slice SQL + gen-script regression tests (commit 61383a29)

Created `tests/services/test_canonical_slice_sql.py` with 6 test functions covering the contract for `app.services.canonical_slice_sql`:
1. `test_equal_footing_filter_sql_is_constant` — deterministic output + ±100 tolerance check
2. `test_sparse_exclusion_sql_parametrises_columns` — column-name substitution, V5 security boundary documented
3. `test_elo_bucket_expr_format` — CASE WHEN structure, 5 ELO anchors, NULL for sub-800
4. `test_selected_users_cte_benchmark_vs_single_user` — benchmark joins vs scalar single-user select
5. `test_per_user_cte_for_all_four_metrics_byte_identical_shared_fragments` — byte-identical drift guard (D-11 / RESEARCH Pitfall 8)
6. `test_per_user_cte_single_user_drops_tc_bucket_predicate` — D-09 pooled-TC assertion

Created `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` with 4 parametrised regression tests capturing pre-refactor SQL output for all 4 metric IDs as inline string constants (`BUILD_METRIC_BREAKPOINT_QUERY_BASELINE`). Tests are skipped with a Plan 03 TODO until the refactor lands.

### Task 2 — Migration + repository + ENUM introspection tests (commit 1ecd3f53)

Created `tests/integration/` directory (first integration test directory in the project) with:
- `test_alembic_migrations.py` — 2 tests: upgrade schema validation (all 7 D-08 columns, nullability, ENUM type, composite PK) + downgrade Pitfall-4 ordering guard (source-file ordering check, non-destructive)
- `test_benchmark_metric_enum.py` — 1 test: `SELECT enumlabel FROM pg_enum` asserting exactly 4 alphabetical labels (T-94.1-02 threat mitigation)

Created `tests/repositories/test_user_benchmark_percentiles_repository.py` with 6 tests:
1. `test_upsert_inserts_when_no_row_exists`
2. `test_upsert_overwrites_existing_row`
3. `test_upsert_handles_percentile_null`
4. `test_fk_cascade_on_user_delete` — ON DELETE CASCADE verified via row count drop to 0
5. `test_pk_rejects_duplicate_metric_for_same_user_via_plain_insert`
6. `test_fetch_for_user_returns_dict_keyed_by_metric_id`

## Deviations from Plan

None — plan executed exactly as written.

The downgrade test uses a source-file ordering check (non-destructive) rather than executing `alembic downgrade` against the shared test DB. This is a minor implementation detail: actually running downgrade in the test session would destabilise the session-scoped `test_engine` fixture for other tests. The ordering check verifies the same Pitfall-4 invariant without side effects.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. This plan creates test files only.

## Known Stubs

None. Test files contain no stubs — only skip guards that will activate after Plans 03/04 land.

## Self-Check: PASSED

All 6 created files confirmed present on disk. Both task commits (61383a29, 1ecd3f53) confirmed in git log.
