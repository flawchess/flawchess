---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 11
type: execute
wave: 7
depends_on:
  - 94.1-09
  - 94.1-10
files_modified:
  - alembic/versions/20260525_000000_rename_n_games_to_n_cells_floor.py
  - app/models/user_benchmark_percentile.py
  - app/repositories/user_benchmark_percentiles_repository.py
  - app/services/user_benchmark_percentiles_service.py
  - scripts/backfill_user_percentiles.py
  - tests/repositories/test_user_benchmark_percentiles_repository.py
  - tests/services/test_endgame_service_chip_decoupling.py
  - tests/integration/test_alembic_migrations.py
  - tests/services/test_user_benchmark_percentiles_service.py
  - frontend/src/pages/Import.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - PCTL-08
gap_closure: true
gap_source: |
  94.1-REVIEW.md (CR-01 — semantic column-name mismatch).
  Additional scope folded in during 94.1-09 HUMAN-UAT: after the SQL bindparam fix
  was verified writing real rows, the user reported that percentile badges only
  appear on /endgames after a hard refresh. Root cause: the import-complete
  TanStack invalidation list (`App.tsx:539-541` and `Import.tsx:73-75, 197-199`)
  covers `['games']`, `['gameCount']`, `['userProfile']`, `['imports', 'eval-coverage']`
  but NOT `['endgameOverview']`. Default `staleTime: 30_000` (`queryClient.ts:24`)
  then serves the stale pre-import response with no percentile data.

must_haves:
  truths:
    - "The column formerly known as `user_benchmark_percentiles.n_games` is renamed to `n_cells_floor` (matching what is actually stored: the count of HAVING-floor-passing (elo_bucket, tc_bucket) cells, NOT a game count)"
    - "The new Alembic migration is reversible: `alembic upgrade head && alembic downgrade -1 && alembic upgrade head` round-trips cleanly"
    - "All ORM / repository / service / script / test references to `n_games` AS THE PERCENTILE-TABLE COLUMN are renamed to `n_cells_floor`. Unrelated `n_games` identifiers elsewhere in the codebase (e.g., benchmark CTE locals, opening-insights service, import-fixture loop counters) are NOT touched."
    - "Migration runs against dev DB AFTER 94.1-09 has been verified to write real rows, so the rename operates on a non-empty table and proves the rename preserves data"
    - "REVIEW.md CR-01 is resolved; no future operator dashboard will mistake the column for a game count"
    - "The import-complete TanStack invalidation list in `frontend/src/pages/Import.tsx` and `frontend/src/App.tsx` includes `['endgameOverview']` so percentile badges appear on /endgames without a hard refresh after a fresh import (gap surfaced during 94.1-09 HUMAN-UAT)"
  artifacts:
    - path: "alembic/versions/20260525_000000_rename_n_games_to_n_cells_floor.py"
      provides: "Reversible RENAME COLUMN migration with updated COMMENT explaining the semantic correction"
    - path: "app/models/user_benchmark_percentile.py"
      provides: "ORM column attribute renamed n_games to n_cells_floor"
    - path: "app/repositories/user_benchmark_percentiles_repository.py"
      provides: "PercentileRow field + upsert_percentile param + fetch_for_user projection renamed"
  key_links:
    - from: "app/services/user_benchmark_percentiles_service.py:196, 250"
      to: "app/repositories/user_benchmark_percentiles_repository.py:upsert_percentile"
      via: "kwarg n_cells_floor=n_cells_floor (was n_games=n_cells_floor: the misleading keyword that motivated the rename)"
      pattern: "n_cells_floor=n_cells_floor"
---

<objective>
Close REVIEW.md CR-01: the `user_benchmark_percentiles.n_games` column was documented as "Number of canonical-slice games that contributed to `value`" but the service writes `n_games=n_cells_floor`, a count of HAVING-floor-passing `(elo_bucket, tc_bucket)` cells, off by ~25x from any real game count. Per the user decision in the gap-closure scope ("rename column, NOT add a new game-count column: the stored value already matches the new name"), this plan ships a reversible Alembic rename migration plus all downstream code/test renames.

This plan MUST land AFTER 94.1-09 (the bindparam fix + dev backfill HUMAN-UAT) so the rename runs against a table that actually contains real rows, providing visible confidence that the rename preserves data, not just empty schema.

Purpose: Eliminate a latent semantic landmine; align the column name with what is physically stored; prevent future operator/dashboard consumers from trusting a misleading name.
Output: 1 new migration + ORM/repository/service/script renames + ~25 test-fixture renames.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-REVIEW.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-VERIFICATION.md
@app/models/user_benchmark_percentile.py
@app/repositories/user_benchmark_percentiles_repository.py
@app/services/user_benchmark_percentiles_service.py
@scripts/backfill_user_percentiles.py
@alembic/versions/20260524_000000_add_user_benchmark_percentiles.py
@tests/integration/test_alembic_migrations.py
@tests/repositories/test_user_benchmark_percentiles_repository.py
@tests/services/test_endgame_service_chip_decoupling.py
</context>

<interfaces>
<!-- Migration template (NEW FILE: generate revision id; do NOT use `alembic revision --autogenerate` because the column rename will not be detected from a no-op ORM diff; use `alembic revision` and write the rename manually) -->
revision: str = "<generated>"
down_revision: Union[str, Sequence[str], None] = "82a2b6d4888f"  # the original 94.1-04 migration

def upgrade() -> None:
    op.alter_column(
        "user_benchmark_percentiles",
        "n_games",
        new_column_name="n_cells_floor",
    )
    op.execute(
        "COMMENT ON COLUMN user_benchmark_percentiles.n_cells_floor IS "
        "'Count of (elo_bucket, tc_bucket) cells that passed the per-metric HAVING inclusion floor at compute time. "
        "NOT a game count. See Phase 94.1 REVIEW.md CR-01 for the rename rationale.'"
    )

def downgrade() -> None:
    op.execute("COMMENT ON COLUMN user_benchmark_percentiles.n_cells_floor IS NULL")
    op.alter_column(
        "user_benchmark_percentiles",
        "n_cells_floor",
        new_column_name="n_games",
    )

<!-- ORM model after rename (app/models/user_benchmark_percentile.py:59) -->
n_cells_floor: Mapped[int] = mapped_column(Integer, nullable=False)

<!-- Repository dataclass after rename -->
@dataclass(frozen=True)
class PercentileRow:
    value: float
    percentile: float | None
    n_cells_floor: int
    cdf_snapshot: datetime.date

<!-- Repository function signature after rename -->
async def upsert_percentile(session, *, user_id, metric, value, percentile, n_cells_floor: int, cdf_snapshot) -> None
async def fetch_for_user(session, *, user_id) -> dict[CdfMetricId, PercentileRow]  # projection: UserBenchmarkPercentile.n_cells_floor

<!-- Service call sites after rename (user_benchmark_percentiles_service.py:196, 250) -->
await upsert_percentile(session, user_id=user_id, metric=..., value=..., percentile=..., n_cells_floor=n_cells_floor, cdf_snapshot=...)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate the Alembic rename migration with reversible upgrade/downgrade + updated COMMENT</name>
  <read_first>
    - alembic/versions/20260524_000000_add_user_benchmark_percentiles.py (the migration this one revises)
    - alembic/versions/20260521_015028_bd54be3a66bf_add_evals_completed_at_to_games.py (latest non-94.1 migration: confirms current head before 82a2b6d4888f)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-REVIEW.md (CR-01: full rationale)
    - CLAUDE.md (§Critical Constraints: migration policy: reversible, runs on container startup; §Database Access for the MCP query tool used in verify)
  </read_first>
  <action>
    1. Generate a new revision file: `uv run alembic revision -m "rename n_games to n_cells_floor in user_benchmark_percentiles"`. This creates a file like `alembic/versions/<rev_id>_rename_n_games_to_n_cells_floor.py`. The actual filename will be auto-generated; rename it to `alembic/versions/20260525_000000_rename_n_games_to_n_cells_floor.py` for consistency with the 94.1-04 naming convention, OR use whatever Alembic produced. Both are acceptable per the existing mix in `alembic/versions/`.
    2. Confirm `down_revision` in the new file equals `"82a2b6d4888f"` (the 94.1-04 head). If Alembic auto-detected a different head, fix it explicitly.
    3. Write the `upgrade()` body. NOTE: the COMMENT text MUST NOT contain em-dashes (CLAUDE.md style rule). Use period or colon instead:
       ```python
       op.alter_column(
           "user_benchmark_percentiles",
           "n_games",
           new_column_name="n_cells_floor",
       )
       op.execute(
           "COMMENT ON COLUMN user_benchmark_percentiles.n_cells_floor IS "
           "'Count of (elo_bucket, tc_bucket) cells that passed the per-metric HAVING inclusion floor at compute time. "
           "NOT a game count. See Phase 94.1 REVIEW.md CR-01 for the rename rationale.'"
       )
       ```
    4. Write the `downgrade()` body (mirror, in reverse order):
       ```python
       op.execute("COMMENT ON COLUMN user_benchmark_percentiles.n_cells_floor IS NULL")
       op.alter_column(
           "user_benchmark_percentiles",
           "n_cells_floor",
           new_column_name="n_games",
       )
       ```
    5. Use modern type-hint imports (per REVIEW.md IN-01: NOT a hard gate for this plan but an opportunistic cleanup since this is a new file): `from collections.abc import Sequence` and `Union[X, Y]` to `X | Y` if applicable.
    6. Add a module-level docstring explaining the rename rationale and citing REVIEW.md CR-01 + 94.1-VERIFICATION.md. Em-dash discipline applies to the docstring too.
    7. Verify the round-trip BEFORE touching any code that consumes the column: `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head`. This must exit 0. If it fails (e.g., the table is in a state where the rename can't be applied), abort and report.
  </action>
  <verify>
    <automated>uv run alembic upgrade head</automated>
    <automated>uv run alembic downgrade -1</automated>
    <automated>uv run alembic upgrade head</automated>
    <!-- Use the project's MCP DB query tool (CLAUDE.md §Database Access) to inspect schema. Run mcp__flawchess-db__query against dev DB with:
         SELECT column_name, data_type, is_nullable
         FROM information_schema.columns
         WHERE table_name = 'user_benchmark_percentiles' AND column_name IN ('n_games', 'n_cells_floor')
         ORDER BY column_name;
         Expected post-upgrade: [{'column_name': 'n_cells_floor', 'data_type': 'integer', 'is_nullable': 'NO'}].
         Expected post-downgrade: [{'column_name': 'n_games', 'data_type': 'integer', 'is_nullable': 'NO'}]. -->
    <human-check>Run mcp__flawchess-db__query with the SQL above against the dev DB and confirm the expected single-row result (n_cells_floor post-upgrade, n_games post-downgrade).</human-check>
  </verify>
  <acceptance_criteria>
    - New file `alembic/versions/*rename_n_games_to_n_cells_floor*.py` exists.
    - `grep -l "alter_column.*n_games.*new_column_name.*n_cells_floor" alembic/versions/*rename*.py` returns the new file.
    - `grep -l "alter_column.*n_cells_floor.*new_column_name.*n_games" alembic/versions/*rename*.py` returns the new file (downgrade path).
    - `down_revision = "82a2b6d4888f"` appears in the file.
    - Round-trip `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` exits 0.
    - After upgrade, MCP-query inspection of `information_schema.columns` shows `n_cells_floor` and NOT `n_games` for `user_benchmark_percentiles`.
    - The migration COMMENT text contains no em-dash characters: `grep -c "—" alembic/versions/*rename_n_games_to_n_cells_floor*.py` returns `0`.
  </acceptance_criteria>
  <done>Reversible migration in place; dev DB renamed; round-trip verified; column comment explains the semantic correction without em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename n_games to n_cells_floor across ORM, repository, service, scripts, and tests</name>
  <read_first>
    - app/models/user_benchmark_percentile.py (line 59: column attribute)
    - app/repositories/user_benchmark_percentiles_repository.py (lines 25-36 dataclass; 39-82 upsert_percentile; 85-125 fetch_for_user: 5 references total)
    - app/services/user_benchmark_percentiles_service.py (lines 196, 250: the misleading `n_games=n_cells_floor` kwargs)
    - scripts/backfill_user_percentiles.py (check first with `grep -n "n_games" scripts/backfill_user_percentiles.py` — if any hits refer to the percentile column, they need renaming)
    - tests/repositories/test_user_benchmark_percentiles_repository.py (~10 references; check exact lines with `grep -n "n_games" tests/repositories/test_user_benchmark_percentiles_repository.py`)
    - tests/services/test_endgame_service_chip_decoupling.py (~8 references at lines 66, 72, 78, 84, 194, 204, 294, 300, 306, 312)
    - tests/integration/test_alembic_migrations.py (line 67: the expected column list)
    - tests/services/test_user_benchmark_percentiles_service.py (line 46: docstring only, but rename for consistency)
  </read_first>
  <action>
    1. **Repo-wide scan FIRST** (before touching any file) to enumerate every reference that points at the percentile-table column. Run:
       ```bash
       grep -rn "n_games" scripts/ app/ tests/ alembic/ frontend/src \
           --include='*.py' --include='*.ts' --include='*.tsx' --include='*.sql' \
           2>/dev/null
       ```
       Triage every hit into ONE of two buckets:
       - **RENAME bucket** (the hit is the `user_benchmark_percentiles.n_games` column / its ORM attribute / its kwarg / its raw-SQL column-list reference / a percentile-row test fixture / the backfill script's column reference): rename to `n_cells_floor`.
       - **LEAVE-ALONE bucket** (the identifier is something else entirely): leave untouched. Known leave-alone identifiers include the original migration `alembic/versions/20260524_000000_add_user_benchmark_percentiles.py` (historical: keeps its original column name in its `upgrade()` body verbatim), the import-fixture helper `_yield_n_games` in `tests/test_import_service.py`, `opening_insights.n_games`, `endgame_service.conversion_games`, and any benchmark-CTE local named e.g. `n_games_per_user`. If unsure, INSPECT the file context (a few lines around the hit) before renaming.
       Document the triage decisions inline as you go.
    2. **`app/models/user_benchmark_percentile.py:59`**: rename `n_games:` to `n_cells_floor:`. Update the class docstring (lines 38-47) to mention the column stores a cell count (per CR-01 fix); the percentile is gated on a separate per-metric inclusion floor. Em-dash discipline applies: use period or colon, NOT em-dash, in the docstring update.
    3. **`app/repositories/user_benchmark_percentiles_repository.py`**:
       - Line 35 (PercentileRow field): `n_games: int` to `n_cells_floor: int`.
       - Line 46 (upsert_percentile kwarg): `n_games: int` to `n_cells_floor: int`.
       - Line 61 (docstring): "Number of canonical-slice games that contributed to ``value``." to "Count of (elo_bucket, tc_bucket) cells that passed the per-metric HAVING inclusion floor at compute time. NOT a game count. See Phase 94.1 REVIEW.md CR-01." (period, NOT em-dash, per CLAUDE.md style rule).
       - Line 69 (insert values): `n_games=n_games` to `n_cells_floor=n_cells_floor`.
       - Line 77 (on_conflict_do_update set_): `"n_games": stmt.excluded.n_games` to `"n_cells_floor": stmt.excluded.n_cells_floor`.
       - Line 112 (fetch_for_user projection): `UserBenchmarkPercentile.n_games` to `UserBenchmarkPercentile.n_cells_floor`.
       - Line 121 (PercentileRow construction): `n_games=row.n_games` to `n_cells_floor=row.n_cells_floor`.
    4. **`app/services/user_benchmark_percentiles_service.py`**:
       - Line 196: `n_games=n_cells_floor` to `n_cells_floor=n_cells_floor`.
       - Line 250: `n_games=n_cells_floor` to `n_cells_floor=n_cells_floor`.
    5. **`scripts/backfill_user_percentiles.py`**: rename every percentile-table `n_games` reference identified in step 1 to `n_cells_floor`. Include this file in `files_modified` if any rename was actually applied; if the grep produced zero percentile-related hits in this file (only `_yield_n_games`-style unrelated locals), note that fact in the task SUMMARY and the entry stays informational. Pay particular attention to: any kwarg passed to `upsert_percentile`, any raw SQL string referencing the column, any local variable named `n_games` that holds the cell-count-floor value about to be persisted.
    6. **`tests/repositories/test_user_benchmark_percentiles_repository.py`**: rename every `n_games` kwarg (10 sites at lines 96, 129, 146, 183, 224, 278, 295, 323, 332) AND the raw SQL strings at lines 272-273, 289-290 (`"(user_id, metric, value, n_games, cdf_snapshot)"` to `"(user_id, metric, value, n_cells_floor, cdf_snapshot)"`). Also rename the local var `new_n_games` at lines 137, 146, 162 to `new_n_cells_floor`. Constant `_DEFAULT_N_GAMES` (line 96 reference) to rename to `_DEFAULT_N_CELLS_FLOOR` (find its definition; preserve value).
    7. **`tests/services/test_endgame_service_chip_decoupling.py`**: rename every `n_games=` kwarg at lines 66, 72, 78, 84, 194, 204, 294, 300, 306, 312 to `n_cells_floor=`. Rename constants `_SEED_N_GAMES` to `_SEED_N_CELLS_FLOOR` and `_BELOW_FLOOR_N_GAMES` to `_BELOW_FLOOR_N_CELLS_FLOOR`. Preserve values; this is a pure rename.
    8. **`tests/integration/test_alembic_migrations.py:67`**: the expected-columns list contains `"n_games"` — change to `"n_cells_floor"`.
    9. **`tests/services/test_user_benchmark_percentiles_service.py:46`**: the docstring mentions `n_games >= 30` — update to `n_cells_floor >= 0` (the floor is on cells, not the value of this attribute; or just remove the n_games clause since it's a comment).
    10. After all renames: re-run the original scan from step 1 and confirm every remaining `n_games` hit is in the LEAVE-ALONE bucket (the original migration's `upgrade()` body, `_yield_n_games`, `opening_insights.n_games`, etc.). Document each remaining hit and why it stays.
    11. Re-run targeted tests to confirm: `uv run pytest tests/repositories/test_user_benchmark_percentiles_repository.py tests/services/test_endgame_service_chip_decoupling.py tests/services/test_user_benchmark_percentiles_service.py tests/integration/test_alembic_migrations.py -x`.
  </action>
  <verify>
    <automated>test $(grep -rn "n_games" app/models/user_benchmark_percentile.py app/repositories/user_benchmark_percentiles_repository.py app/services/user_benchmark_percentiles_service.py | wc -l) -eq 0</automated>
    <!-- Allow-list scan: only the original migration and unrelated identifiers should remain. -->
    <automated>test $(grep -rn "n_games" scripts/ app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py app/models/user_benchmark_percentile.py | grep -v "n_games_per_user\|_yield_n_games\|20260524_000000_add_user_benchmark_percentiles" | wc -l) -eq 0</automated>
    <automated>uv run pytest tests/repositories/test_user_benchmark_percentiles_repository.py tests/services/test_endgame_service_chip_decoupling.py tests/services/test_user_benchmark_percentiles_service.py tests/integration/test_alembic_migrations.py -x</automated>
    <automated>uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "n_games" app/models/user_benchmark_percentile.py app/repositories/user_benchmark_percentiles_repository.py app/services/user_benchmark_percentiles_service.py` returns 0 hits.
    - Allow-list scan returns 0: `grep -rn "n_games" scripts/ app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py app/models/user_benchmark_percentile.py | grep -v "n_games_per_user\|_yield_n_games\|20260524_000000_add_user_benchmark_percentiles" | wc -l` returns `0`.
    - `grep -c "n_cells_floor" app/repositories/user_benchmark_percentiles_repository.py` returns at least `5`.
    - `grep -c "n_cells_floor=n_cells_floor" app/services/user_benchmark_percentiles_service.py` returns `2` (Stage A + Stage B call sites).
    - `tests/integration/test_alembic_migrations.py` no longer expects `n_games` in the column list.
    - `uv run pytest tests/repositories/test_user_benchmark_percentiles_repository.py tests/services/test_endgame_service_chip_decoupling.py tests/services/test_user_benchmark_percentiles_service.py tests/integration/test_alembic_migrations.py -x` exits 0.
    - `uv run ty check app/ tests/` exits 0 (the rename must not introduce type errors).
    - Em-dash check on the rename-affected docstrings/comments: `grep -c "—" app/repositories/user_benchmark_percentiles_repository.py app/models/user_benchmark_percentile.py` does not increase from its baseline (rename additions use period or colon).
  </acceptance_criteria>
  <done>All percentile-table references are renamed across production code, scripts, and tests; the allow-list scan confirms the only remaining `n_games` hits are unrelated identifiers (benchmark CTE locals, `_yield_n_games` fixture, the original migration's historical `upgrade()` body); targeted tests pass; type-checker stays clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add `['endgameOverview']` to import-complete TanStack invalidation list</name>
  <read_first>
    - frontend/src/pages/Import.tsx (current invalidation calls on import-success paths)
    - frontend/src/App.tsx (mirror invalidation block on lines 539-541)
    - frontend/src/hooks/useEndgames.ts (queryKey shape: ['endgameOverview', params, window])
    - frontend/src/lib/queryClient.ts (staleTime: 30_000 default — explains why stale cache wins)
  </read_first>
  <action>
    1. In `frontend/src/pages/Import.tsx`, locate the two `queryClient.invalidateQueries` blocks (around lines 73-75 and 197-199) that run on chess.com / lichess import-success. Add `queryClient.invalidateQueries({ queryKey: ['endgameOverview'] });` to each block, alongside the existing `['games']` / `['gameCount']` / `['userProfile']` invalidations.
    2. In `frontend/src/App.tsx`, locate the mirror invalidation block (around lines 539-541) and add the same `['endgameOverview']` invalidation.
    3. Partial-match semantics: `queryKey: ['endgameOverview']` invalidates every cached `['endgameOverview', params, window]` variant regardless of filter params — this is intentional, since any import affects all filter views.
    4. Add a one-line comment above each new invalidation explaining WHY (Stage A is non-blocking background; without invalidation the staleTime cache hides newly-written percentile rows until hard refresh). CLAUDE.md "Comment bug fixes" rule applies.
    5. Update / add a frontend test asserting the import-complete handler invalidates `['endgameOverview']`. Co-locate near existing Import.tsx tests.
  </action>
  <verify>
    <automated>grep -n "endgameOverview" frontend/src/pages/Import.tsx frontend/src/App.tsx | wc -l</automated>
    <automated>cd frontend && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "endgameOverview" frontend/src/pages/Import.tsx` returns ≥ 2 (two invalidation call sites).
    - `grep -c "endgameOverview" frontend/src/App.tsx` returns ≥ 1.
    - Frontend lint + tests green.
    - New / updated test verifies the invalidation contract (queryClient receives `['endgameOverview']` in the import-success handler).
  </acceptance_criteria>
  <done>The import-complete invalidation list covers `['endgameOverview']`; the percentile chip appears on /endgames without a hard refresh after a fresh import. Verified end-to-end in Task 5 HUMAN-UAT.</done>
</task>

<task type="auto">
  <name>Task 4: Pre-PR gate — full backend + frontend test suites + lint + type-check</name>
  <read_first>
    - CLAUDE.md (§Pre-PR checklist)
  </read_first>
  <action>
    Run the standard FlawChess pre-PR checklist. Apply `--fix` on ruff; commit any formatter diff with `style(94.1-11)` prefix before continuing. Confirm the dev DB is on the new head (Task 1 already migrated it; ensure no rollback was performed after).
  </action>
  <verify>
    <automated>uv run ruff format app/ tests/ scripts/ alembic/</automated>
    <automated>uv run ruff check app/ tests/ scripts/ alembic/ --fix</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>uv run pytest -x</automated>
    <automated>cd frontend && npm run lint && npm test -- --run</automated>
    <automated>uv run alembic current | grep -v '82a2b6d4888f'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff format --check app/ tests/ scripts/ alembic/` exits 0.
    - `uv run ruff check app/ tests/ scripts/ alembic/` exits 0.
    - `uv run ty check app/ tests/` exits 0.
    - `uv run pytest -x` exits 0.
    - `cd frontend && npm run lint && npm test -- --run` exits 0.
    - `uv run alembic current` reports the NEW rename migration as head (not `82a2b6d4888f`).
  </acceptance_criteria>
  <done>All pre-PR gates green; dev DB is on the new head with `n_cells_floor` column.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: HUMAN-UAT — confirm renamed column preserved data, chip renders, and no-refresh invariant holds after import</name>
  <what-built>Reversible Alembic migration renaming `user_benchmark_percentiles.n_games` to `n_cells_floor`, plus ORM/repository/service/script/test renames. The rename runs after 94.1-09 has already populated the table with real rows, so this UAT verifies (a) data preservation, (b) the column comment explains the rename, and (c) the chip still renders for users whose percentile rows already exist.</what-built>
  <how-to-verify>
    1. Confirm Alembic head: `uv run alembic current` (expect the new rename revision, NOT `82a2b6d4888f`).
    2. Spot-check the column rename preserved row data via the project MCP DB tool: `mcp__flawchess-db__query "SELECT user_id, metric, value, percentile, n_cells_floor, cdf_snapshot FROM user_benchmark_percentiles WHERE user_id IN (8, 14, 31, 46, 7) ORDER BY user_id, metric;"`. Expected: same rows as 94.1-09 HUMAN-UAT, with the column now named `n_cells_floor` (not `n_games`). Values unchanged.
    3. Confirm the COMMENT is in place: `mcp__flawchess-db__query "SELECT col_description('user_benchmark_percentiles'::regclass, attnum) FROM pg_attribute WHERE attrelid = 'user_benchmark_percentiles'::regclass AND attname = 'n_cells_floor';"`. Expected: comment text mentions "cells" and "CR-01"; no em-dash characters in the comment.
    4. Open `/endgames` for a user whose row exists (user_id=8 or similar). Chip MUST still render (the rewire path uses the renamed dataclass field; if any consumer wasn't updated, the chip would disappear or the page would 500).
    5. (Optional regression check) Re-run dev backfill: `uv run python scripts/backfill_user_percentiles.py --target dev`. Expected: idempotent — same summary as the post-94.1-09 baseline, no errors. (If Task 2 step 5 renamed anything in this script, this is also the runtime smoke for those renames.)
    6. Production migration plan note: the deploy entrypoint will auto-run this rename migration at next backend container startup. Confirm with the user that this is acceptable timing (per CLAUDE.md "Alembic migrations run automatically on backend container startup via deploy/entrypoint.sh").
    7. No-refresh invariant (Task 3 verification): run a small dev import for a user whose percentile row was previously missing (delete the row via `mcp__flawchess-db__query "DELETE FROM user_benchmark_percentiles WHERE user_id = <test_user>;"`). Trigger a re-import in the UI; once the import-complete toast appears, navigate to `/endgames` WITHOUT a hard refresh. Expected: percentile badges appear on the metric cards. Pre-fix baseline: badges only appeared after Cmd+R.
  </how-to-verify>
  <resume-signal>Type "approved" to merge, or describe issues (e.g., "chip disappeared for user 8", "alembic current still shows 82a2b6d4888f", "backfill script crashed with AttributeError on n_games", "percentile badges still require hard refresh after import").</resume-signal>
</task>

</tasks>

<verification>
```bash
# Source-level: rename complete in production code paths
test $(grep -rn "n_games" app/models/user_benchmark_percentile.py app/repositories/user_benchmark_percentiles_repository.py app/services/user_benchmark_percentiles_service.py | wc -l) -eq 0

# Allow-list scan: scripts/ is clean of percentile-column references
test $(grep -rn "n_games" scripts/ app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py app/models/user_benchmark_percentile.py | grep -v "n_games_per_user\|_yield_n_games\|20260524_000000_add_user_benchmark_percentiles" | wc -l) -eq 0

# Schema-level: dev DB has the renamed column and the explanatory COMMENT (verify via MCP tool, see Task 4 step 2-3).

# Migration round-trip
uv run alembic downgrade -1 && uv run alembic upgrade head

# Integration
uv run pytest -x
cd frontend && npm run lint && npm test -- --run
```
</verification>

<success_criteria>
- New reversible Alembic migration renames the column; round-trip works.
- ORM, repository, service, scripts/, and ~25 test sites are renamed; type-checker clean.
- Allow-list grep returns 0 percentile-column-related `n_games` hits outside the original historical migration.
- Dev DB now has `n_cells_floor` (no `n_games`) on the percentile table; rows preserved across the rename.
- Column COMMENT explains the rename rationale (per CR-01) without em-dashes.
- HUMAN-UAT confirms chip still renders post-rename.
- Import-complete handler now invalidates `['endgameOverview']` in both `Import.tsx` and `App.tsx`; percentile badges appear on /endgames without a hard refresh after a fresh import (gap surfaced during 94.1-09 HUMAN-UAT).
- REVIEW.md CR-01 is resolved.
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-11-SUMMARY.md` when done.
</output>
</content>
</invoke>