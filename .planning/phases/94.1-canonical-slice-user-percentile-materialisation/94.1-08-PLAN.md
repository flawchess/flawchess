---
phase: 94.1-canonical-slice-user-percentile-materialisation
plan: 08
type: execute
wave: 4
depends_on:
  - 94.1-05
  - 94.1-06
files_modified:
  - scripts/backfill_user_percentiles.py
autonomous: false
requirements:
  - PCTL-10
must_haves:
  truths:
    - "`scripts/backfill_user_percentiles.py` exists with `--target dev|prod` (required) + optional `--user-id` + optional `--metric` flags"
    - "Idempotent under UPSERT — re-running produces no state drift (computed_at advances; value/percentile preserved if inputs unchanged)"
    - "`--target prod` refuses to run if `bin/prod_db_tunnel.sh` is not up (Pitfall 6 / SC-6 mitigation)"
    - "`--target dev` refuses if resolved URL does not contain `:5432`; `--target prod` refuses if URL does not contain `:15432`"
    - "Iterates users in `created_at ASC` order (oldest accounts first per CONTEXT discretion); per-user commit (simpler error recovery)"
    - "Builds a backfill-local `engine` + `async_sessionmaker` against the resolved `--target` URL and passes it via `session_maker=` to `compute_stage_a` / `compute_stage_b` — the global `app.core.database.async_session_maker` is NEVER touched, so `--target prod` writes only to the prod URL and `--target dev` writes only to the dev URL (V4 cross-environment guard)"
    - "Acceptance test asserts `engine.url` matches the resolved `--target` URL before any DB I/O; aborts otherwise"
    - "Pre-skips users with zero canonical-slice games via LEFT JOIN at top (avoids per-user query overhead)"
    - "Emits per-metric summary table with upserted/skipped counts and per-skip-reason breakdown (CONTEXT §Specifics shape)"
    - "Stage A runs for all users; Stage B runs only for users whose `count_pending_evals == 0` (matches steady-state hook semantics)"
    - "Final blocking-human checkpoint after dev backfill to confirm rollout readiness before merging to main; prod backfill is run by operator post-deploy per VALIDATION §Manual-Only Verifications"
  artifacts:
    - path: "scripts/backfill_user_percentiles.py"
      provides: "One-shot backfill CLI populating user_benchmark_percentiles for existing users"
  key_links:
    - from: "scripts/backfill_user_percentiles.py"
      to: "app.services.user_benchmark_percentiles_service (Plan 05)"
      via: "main() iterates users and calls compute_stage_a + compute_stage_b in-process, passing the backfill-local session_maker via keyword arg"
      pattern: "await compute_stage_a(user_id, session_maker=backfill_session_maker)"
    - from: "scripts/backfill_user_percentiles.py"
      to: "scripts/backfill_eval.py _db_url + _assert_*_db (verbatim shape reuse)"
      via: "copied helper functions"
      pattern: "_assert_target_safe"
---

<objective>
One-shot backfill script + final phase ship gate. Populates `user_benchmark_percentiles` for all existing users so the chip lights up at deploy time, not only for users who import after the phase ships. Ends with a blocking human-verify checkpoint to confirm dev-DB rollout health before the PR is merged.

This is PCTL-10 + D-14. The script's safety guards (Pitfall 6 — `--target prod` refusal without tunnel) are the V4 mitigation against accidental cross-environment writes.

Purpose: Deploy-time bootstrapping + sustained operator tool for re-running after CDF regeneration.
Output: 1 new script + Plan 02's `test_backfill_user_percentiles.py` turning green; blocking human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md
@.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-VALIDATION.md

<interfaces>
Existing — consumed:
- `app.services.user_benchmark_percentiles_service.compute_stage_a(user_id) -> None` (Plan 05)
- `app.services.user_benchmark_percentiles_service.compute_stage_b(user_id) -> None` (Plan 05)
- `app.services.user_benchmark_percentiles_service.STAGE_A_METRIC`, `STAGE_B_METRICS` (Plan 05)
- `app.repositories.game_repository.count_pending_evals(session, user_id) -> int` (existing)
- `app.core.database` — engine + async_session_maker factory
- `app.core.config.settings.DATABASE_URL`
- Patterns to reuse verbatim — copy `_db_url`, `_assert_*_db`, `_mask_password` from `scripts/backfill_eval.py` (lines ~30-100)
- CLI naming convention — `--target dev|prod` per `scripts/import_stress_monitor.py:13-19`

Will be created:
- `scripts/backfill_user_percentiles.py` with:
  - `main(target: Literal["dev","prod"], user_id: int | None = None, metric: CdfMetricId | None = None) -> None` — constructs a backfill-local async engine + `async_sessionmaker` against the resolved `--target` URL and threads it into Plan 05's `compute_stage_a` / `compute_stage_b` via the `session_maker=` keyword
  - `_db_url(target: str) -> str`
  - `_assert_target_safe(url: str, target: str) -> None`
  - `_iter_users(session, *, user_id_filter: int | None) -> AsyncIterator[int]` (yields user_ids in created_at ASC, pre-skipped via LEFT JOIN)
  - argparse entry point
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill script implementation</name>
  <files>scripts/backfill_user_percentiles.py</files>
  <read_first>
    - scripts/backfill_eval.py (lines 1-150 — verbatim source for `_db_url`, `_assert_*_db`, `_mask_password`, argparse shape; the `--db dev|benchmark|prod` flavour adapts to our `--target dev|prod`)
    - scripts/import_stress_monitor.py (lines 1-50 — `--target dev|prod` argparse + tunnel-up check pattern)
    - app/services/user_benchmark_percentiles_service.py (Plan 05 — entry points + STAGE_A_METRIC / STAGE_B_METRICS)
    - app/repositories/game_repository.py (lines 46-53 — count_pending_evals reused to gate Stage B per user)
    - app/models/game.py (Game model — user_id, evals_completed_at fields)
    - app/models/user.py (or wherever the User model lives — confirm `created_at` field for ordering; check filename via grep first)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-PATTERNS.md (`scripts/backfill_user_percentiles.py` section + Example 4 — `--target dev|prod` argparse + safety guard)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-RESEARCH.md (§Pattern 4, §Example 4, §Common Pitfalls 6)
    - .planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-CONTEXT.md (§Specifics — desired summary-table shape)
    - tests/scripts/test_backfill_user_percentiles.py (Plan 02 — the contract this satisfies)
  </read_first>
  <action>
    Author `scripts/backfill_user_percentiles.py`.

    Structure:

    1. Module header docstring referencing PCTL-10 + D-14 + ROADMAP SC 6. Document the CLI: `uv run python scripts/backfill_user_percentiles.py --target dev|prod [--user-id N] [--metric METRIC]`. Document the tunnel requirement: for `--target prod`, `bin/prod_db_tunnel.sh` must be up; the script refuses with a clear error otherwise.

    2. Imports — argparse, asyncio, os, socket, sys, urllib.parse (urlparse + urlunparse), typing (Literal), datetime, `create_async_engine` + `async_sessionmaker` (from `sqlalchemy.ext.asyncio`), the Game + User models, settings, the compute service entry points (`compute_stage_a`, `compute_stage_b`, `STAGE_A_METRIC`, `STAGE_B_METRICS`), `count_pending_evals`.

    3. Constants:
       - `_TARGET_PORT: dict[Literal["dev", "prod"], int] = {"dev": 5432, "prod": 15432}`
       - `_LOCAL_HOSTS: frozenset[str] = frozenset({"localhost", "127.0.0.1", "::1"})`
       - `_PROD_TUNNEL_HINT: str = "Run `bin/prod_db_tunnel.sh start` first"`

    4. Helpers (copy verbatim from `scripts/backfill_eval.py` with `--db` → `--target` renames):
       - `_db_url(target: Literal["dev","prod"]) -> str` — resolves the URL from `BACKFILL_DEV_DB_URL` / `BACKFILL_PROD_DB_URL` env vars OR derives from `settings.DATABASE_URL` with the target port substituted. Validates that override hosts are in `_LOCAL_HOSTS`.
       - `_assert_target_safe(url: str, target: Literal["dev","prod"]) -> None` — per RESEARCH §Example 4:
         - Refuse if URL does not contain `:{_TARGET_PORT[target]}` token.
         - For `target == "prod"`: `socket.create_connection(("localhost", 15432), timeout=2)` — refuse with `_PROD_TUNNEL_HINT` if connect fails.
       - `_mask_password(url: str) -> str` — masks password for logging (verbatim from backfill_eval.py).

    5. `async def _iter_users(session, *, user_id_filter: int | None) -> AsyncIterator[int]`:
       - Build a query: `SELECT u.id FROM users u WHERE EXISTS (SELECT 1 FROM games g WHERE g.user_id = u.id AND g.status = 'completed')` ordered by `u.created_at ASC`.
       - If `user_id_filter is not None`: add `AND u.id = :user_id_filter`.
       - Yield user_ids one at a time.
       - Per CONTEXT discretion: pre-skips users with zero games via the EXISTS sub-query (LEFT JOIN equivalent), surfaces oldest accounts first.

    6. `async def main(target: Literal["dev","prod"], user_id_filter: int | None, metric_filter: CdfMetricId | None) -> None`:
       - Resolve URL via `_db_url(target)`, call `_assert_target_safe(url, target)`.
       - (Engine + session_maker construction is detailed below — see the dedicated bullet that builds `backfill_session_maker` and asserts `engine.url == resolved url` before any DB I/O.)
       - Initialize summary counters: a dict keyed by CdfMetricId with sub-counters `upserted`, `skipped_below_floor`, `skipped_no_canonical_games`, `skipped_no_eval`.
       - Build the backfill-local session factory (the surface Plan 05 expects via its `session_maker=` keyword):
         - `engine = create_async_engine(url, pool_pre_ping=True)` against the resolved `--target` URL (NOT the app's global engine)
         - `backfill_session_maker = async_sessionmaker(engine, expire_on_commit=False)`
         - Assert `str(engine.url) == url` (or that the masked form matches) immediately after creation — abort with a clear error if not, before any DB I/O. This is the V4 cross-environment guard the checker called out.
       - Iterate users via `_iter_users`. For each user:
         - Open a fresh session via `backfill_session_maker()` for `count_pending_evals` and the post-call row-count introspection.
         - If `metric_filter is None or metric_filter == "score_gap"`: call `await compute_stage_a(user_id, session_maker=backfill_session_maker)`. Plan 05's contract now accepts `session_maker` as a keyword arg, so the compute runs against the backfill engine — `--target prod` writes only to prod, `--target dev` writes only to dev. No global mutation, no monkey-patching.
         - For Stage B metrics: open a session via `backfill_session_maker()` and check `count_pending_evals(session, user_id)`. If `== 0`, call `await compute_stage_b(user_id, session_maker=backfill_session_maker)` for the 3 eval-dependent metrics (filtered by `metric_filter` if set). If `> 0`, increment `skipped_no_eval` for the 3 Stage B metrics.
         - Capture per-metric outcomes (upserted vs skipped) — Plan 05's compute service returns None on skip, so wrap the call with row-count introspection: `SELECT 1 FROM user_benchmark_percentiles WHERE user_id = ? AND metric = ?` post-call (using a session from `backfill_session_maker`) to determine if a row was written.
         - Commit per user (CONTEXT discretion: simpler error recovery on Ctrl-C than batched commit).
         - Log per-user progress line at INFO level (`user_id`, metrics processed, skip reasons).
       - At end of `main()`, dispose the backfill engine cleanly: `await engine.dispose()`.
       - Emit summary table at end (CONTEXT §Specifics shape):
         ```
         Backfill complete (target=<target>, took 12.3s)
          score_gap                      upserted=N, skipped=M (below_floor=A, no_canonical_games=B)
          achievable_score_gap           upserted=N, skipped=M (below_floor=A, no_eval=C)
          section2_score_gap_conv        upserted=N, skipped=M (below_floor=A, no_eval=C)
          section2_score_gap_parity      upserted=N, skipped=M (below_floor=A, no_eval=C)
         ```

    7. `argparse` entry point at the bottom:
       ```
       parser = argparse.ArgumentParser(...)
       parser.add_argument("--target", choices=["dev", "prod"], required=True, ...)
       parser.add_argument("--user-id", type=int, default=None, ...)
       parser.add_argument("--metric", choices=["score_gap", "achievable_score_gap", "section2_score_gap_conv", "section2_score_gap_parity"], default=None, ...)
       args = parser.parse_args()
       asyncio.run(main(target=args.target, user_id_filter=args.user_id, metric_filter=args.metric))
       ```

    8. CLAUDE.md compliance:
       - All function signatures have explicit return type annotations.
       - `target` is `Literal["dev","prod"]`, never bare `str`.
       - `metric_filter` is `CdfMetricId | None`, never bare `str | None`.
       - No magic numbers (`_TARGET_PORT` constants, no inline `5432`/`15432`).
       - `ty check` clean.
       - Sentry capture: per-user compute failures are already captured inside Plan 05's compute service; the backfill loop itself wraps each per-user iteration in try/except `Exception` → log + `sentry_sdk.capture_exception(exc)` + `set_context("backfill_user_percentiles", {"user_id": user_id})`. Never propagate; continue to next user. Backfill exit code is 0 only if NO per-user iteration raised (otherwise exit 1 with summary at the end indicating failed user count).

    9. Help text mentions `bin/prod_db_tunnel.sh` requirement for `--target prod` (CONTEXT.md §Canonical Refs — script-script convention).

    Pre-PR gate at end of Task 1:
    - `uv run ruff format scripts/backfill_user_percentiles.py`
    - `uv run ruff check scripts/backfill_user_percentiles.py --fix`
    - `uv run ty check scripts/ app/`
    - `uv run pytest tests/scripts/test_backfill_user_percentiles.py -x` (Plan 02 — 7 tests green)
    - Smoke test: `uv run python scripts/backfill_user_percentiles.py --target dev` runs end-to-end on the local dev DB (idempotent — safe to re-run); summary table prints.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/test_backfill_user_percentiles.py -x && uv run ty check scripts/backfill_user_percentiles.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/backfill_user_percentiles.py`
    - `--target dev|prod` is REQUIRED (argparse `required=True`); `--user-id` + `--metric` are optional
    - `grep -c "create_connection.*15432" scripts/backfill_user_percentiles.py` ≥ 1 (tunnel-up check for prod target)
    - `grep -c "_TARGET_PORT\|_assert_target_safe\|_db_url" scripts/backfill_user_percentiles.py` ≥ 3 (helpers present)
    - `grep -c "create_async_engine\|async_sessionmaker" scripts/backfill_user_percentiles.py` ≥ 2 (backfill-local engine + factory built explicitly, not borrowed from `app.core.database`)
    - `grep -c "session_maker=backfill_session_maker\|session_maker=" scripts/backfill_user_percentiles.py` ≥ 2 (Plan 05's `session_maker=` keyword is wired for both compute_stage_a and compute_stage_b)
    - `grep -c "engine.dispose" scripts/backfill_user_percentiles.py` ≥ 1 (engine cleanly disposed at end of main)
    - Acceptance test in `tests/scripts/test_backfill_user_percentiles.py` (Plan 02): the test asserts that when `main()` is invoked with `--target dev`, every DB write is observable on the dev URL and zero writes hit any other URL. The cheapest implementation is to monkey-patch `app.core.database.async_session_maker` to a sentinel that raises on `__call__` for the duration of the test — any accidental fall-through to the global factory would raise immediately. (If Plan 02's scaffold lacks this test, add it here as part of the Plan 02 contract extension.)
    - All 7 tests in `test_backfill_user_percentiles.py` pass: idempotency, --user-id filter, --metric filter, --target prod tunnel-down refusal, --target dev port mismatch refusal, summary-stdout, zero-canonical-games handling
    - `uv run ty check scripts/backfill_user_percentiles.py` exits 0
    - Smoke test against dev DB: `uv run python scripts/backfill_user_percentiles.py --target dev` exits 0 and prints the 4-row summary table
    - Re-running the smoke test does not change `value` / `percentile` columns for existing rows (idempotency)
    - Pre-PR gate clean
  </acceptance_criteria>
  <done>Backfill script committed; idempotent against dev DB; prod-target-without-tunnel refused with clear error; per-metric summary table emitted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dev-DB rollout sanity check + final phase ship gate</name>
  <what-built>
    Phase 94.1 end-to-end:
    1. Shared `canonical_slice_sql` module (Plan 03).
    2. Migration + ORM model + repository for `user_benchmark_percentiles` (Plan 04).
    3. Compute service `compute_stage_a` + `compute_stage_b` (Plan 05).
    4. Stage A + Stage B background-task hooks (Plan 06).
    5. Chip rewire + tooltip copy update (Plan 07).
    6. Backfill script (this plan, Task 1).
  </what-built>
  <how-to-verify>
    The dev backfill must run cleanly before merging to `main`. The prod backfill happens post-deploy per VALIDATION.md §Manual-Only Verifications — it is NOT part of this phase's pre-merge gate. (The PR includes the backfill script; running it in prod is an operator action documented in the PR description.)

    Run the following locally and report each result:

    1. **Confirm Alembic migration applied to dev DB:**
       ```
       uv run alembic current
       ```
       Expected: shows the new `add_user_benchmark_percentiles` revision as current.

    2. **Smoke-run backfill against dev DB:**
       ```
       uv run python scripts/backfill_user_percentiles.py --target dev
       ```
       Expected: summary table prints, exit code 0, per-metric counts non-negative.

    3. **Idempotency check — run it again:**
       ```
       uv run python scripts/backfill_user_percentiles.py --target dev
       ```
       Expected: same summary numbers (or `upserted=N` unchanged + `skipped=0` increases), no errors.

    4. **Spot-check the table:**
       ```
       docker compose -f docker-compose.dev.yml -p flawchess-dev exec db psql -U flawchess flawchess -c "SELECT metric, COUNT(*), AVG(percentile) FILTER (WHERE percentile IS NOT NULL) AS avg_pctl FROM user_benchmark_percentiles GROUP BY metric ORDER BY metric;"
       ```
       Expected: 0-4 metric rows with row counts ≤ total dev-DB user count.

    5. **Run full backend test suite:**
       ```
       uv run pytest -x
       ```
       Expected: all green (≈90 seconds).

    6. **Run frontend test suite:**
       ```
       cd frontend && npm test -- --run
       ```
       Expected: all green; PercentileChip canonical-slice clarifier tests included.

    7. **Pre-PR checklist gate (CLAUDE.md mandatory):**
       ```
       uv run ruff format app/ tests/
       uv run ruff check app/ tests/ --fix
       uv run ty check app/ tests/
       uv run pytest -x
       cd frontend && npm run lint && npm test -- --run
       ```
       Expected: all green; commit any formatter diffs as a `style(94.1):` commit before pushing.

    8. **Live UI HUMAN-UAT (per VALIDATION §Manual-Only):**
       - Start the dev backend + frontend (`bin/run_local.sh` or equivalent).
       - Open `/endgames` for a user with ≥30 endgame games (dev DB likely has your own user).
       - Note the percentile chip values for Endgame Score Gap, Achievable Score Gap, Parity ΔES, Conversion ΔES.
       - Toggle Recency / Opponent Strength / TC / Platform / Rated / Opponent Type filters.
       - Confirm: the chip values do NOT change (PCTL-07 satisfied).
       - Confirm: the row's filter-applied metric value DOES change.
       - Hover the chip (desktop) and tap it (mobile/responsive); confirm the popover body reads naturally and includes the canonical-slice clarifier within the minimalism discipline.

    Post-deploy operator step (NOT part of this checkpoint, documented for the PR description):
    ```
    bin/prod_db_tunnel.sh start
    uv run python scripts/backfill_user_percentiles.py --target prod
    bin/prod_db_tunnel.sh stop
    ```
    Spot-check the summary table; investigate any user-count discrepancies between `SELECT COUNT(DISTINCT user_id) FROM user_benchmark_percentiles` and `SELECT COUNT(*) FROM users WHERE EXISTS (SELECT 1 FROM games WHERE games.user_id = users.id AND status='completed')`.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "approved" — dev backfill clean, all gates green, UAT pass; ready to PR
    - "issue: <description>" — describe what failed; I'll investigate and rerun
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → backfill DB target | `--target dev|prod` flag selects connection; safety guard refuses on URL mismatch (V4 Tampering) |
| script → prod DB (when `--target prod`) | Requires tunnel up; refuses otherwise (V4 mitigation against accidental cross-env writes) |
| per-user backfill iteration → Sentry | Per-user failures captured; loop continues; exit code reflects final aggregate (Pitfall 6 + CLAUDE.md Sentry rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-16 | Tampering (V4) | backfill `--target prod` against wrong DB | mitigate | `_assert_target_safe` refuses on URL mismatch + socket.create_connection check for tunnel; Plan 02 `test_backfill_target_prod_refuses_when_tunnel_down` enforces. RESEARCH Pitfall 6. |
| T-94.1-17 | Information Disclosure (V4) | Sentry capture in per-user backfill failure | mitigate | `set_context("backfill_user_percentiles", {"user_id": ...})`; exception message must not include user_id. CLAUDE.md Sentry rule. |
</threat_model>

<verification>
- Backfill script committed
- Plan 02 backfill tests (7 tests) all pass
- Dev-DB smoke run clean + idempotent
- Final pre-PR gate (ruff/ty/pytest/frontend lint+test) clean
- Blocking human-verify checkpoint signs off the phase
</verification>

<success_criteria>
- All 7 plans land green
- All 11 Wave-0 test files exist and have green assertions
- HUMAN-UAT for the live chip-decoupling behavior passes
- Operator can run `--target prod` post-deploy to backfill production users
- VALIDATION §Manual-Only Verifications all documented for the PR description
</success_criteria>

<output>
Create `.planning/phases/94.1-canonical-slice-user-percentile-materialisation/94.1-08-SUMMARY.md` — record backfill summary numbers from the dev-DB smoke run, list all tests now green across the phase, confirm pre-PR gate clean, document the prod-deploy operator step (`bin/prod_db_tunnel.sh start && uv run python scripts/backfill_user_percentiles.py --target prod`) for inclusion in the PR description.
</output>
