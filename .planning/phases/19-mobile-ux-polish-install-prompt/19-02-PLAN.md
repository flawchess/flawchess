---
phase: 19-mobile-ux-polish-install-prompt
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/filters/FilterPanel.tsx
  - frontend/src/components/board/BoardControls.tsx
  - frontend/src/pages/GlobalStats.tsx
  - frontend/src/components/ui/toggle-group.tsx
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "No page displays a horizontal scrollbar at 375px viewport width"
    - "All filter buttons and board control buttons have at least 44px tap height on mobile"
    - "Desktop filter buttons and controls retain their compact sizing"
    - "Body element prevents horizontal overflow globally"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "overflow-x: hidden on body"
      contains: "overflow-x"
    - path: "frontend/src/components/filters/FilterPanel.tsx"
      provides: "44px touch targets on filter buttons"
      contains: "min-h-11"
    - path: "frontend/src/components/board/BoardControls.tsx"
      provides: "44px touch targets on board control buttons"
      contains: "h-11 w-11"
    - path: "frontend/src/pages/GlobalStats.tsx"
      provides: "Responsive padding to prevent overflow"
      contains: "px-4 sm:px-6"
  key_links:
    - from: "frontend/src/components/filters/FilterPanel.tsx"
      to: "Tailwind responsive classes"
      via: "min-h-11 sm:min-h-0 pattern"
      pattern: "min-h-11.*sm:min-h-0"
    - from: "frontend/src/components/board/BoardControls.tsx"
      to: "Tailwind responsive classes"
      via: "h-11 w-11 sm:h-8 sm:w-8 pattern"
      pattern: "h-11 w-11.*sm:h-8 sm:w-8"
---

<objective>
Ensure all interactive elements meet 44px minimum touch targets on mobile and eliminate horizontal overflow at 375px viewport width.

Purpose: Small touch targets and horizontal scrollbars make the app frustrating to use on phones. These are baseline mobile UX requirements.
Output: All buttons/controls meet 44px minimum on mobile, no horizontal scroll on any page at 375px.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-mobile-ux-polish-install-prompt/19-CONTEXT.md
@.planning/phases/19-mobile-ux-polish-install-prompt/19-RESEARCH.md
@.planning/phases/19-mobile-ux-polish-install-prompt/19-UI-SPEC.md

<interfaces>
<!-- Current filter button classes that need 44px fixes -->

From frontend/src/components/filters/FilterPanel.tsx (time control buttons, line 101-102):
```typescript
className={cn(
  'rounded border px-2 py-0.5 text-xs transition-colors',
  // ...active/inactive styles
)}
```

From frontend/src/components/filters/FilterPanel.tsx (platform buttons, line 125-126):
```typescript
className={cn(
  'rounded border px-2 py-0.5 text-xs transition-colors',
  // ...active/inactive styles
)}
```

From frontend/src/components/filters/FilterPanel.tsx (ToggleGroupItem for Rated/Opponent, lines 141-175):
```typescript
<ToggleGroup ... size="sm">
  <ToggleGroupItem value="all" data-testid="filter-rated-all">All</ToggleGroupItem>
```

From frontend/src/components/board/BoardControls.tsx (icon buttons, line 23-66):
```typescript
<Button variant="ghost" size="icon" ...>
```
shadcn Button size="icon" default is `h-8 w-8` (32px).

From frontend/src/pages/GlobalStats.tsx (line 26):
```typescript
<div data-testid="global-stats-page" className="mx-auto max-w-4xl space-y-6 p-6">
```
`p-6` = 24px padding. At 375px that leaves 327px for content.

From frontend/src/pages/GlobalStats.tsx (platform buttons, line 72-73):
```typescript
className={cn(
  'rounded border px-2 py-0.5 text-xs transition-colors',
  // ...active/inactive styles
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overflow-x:hidden to body and fix GlobalStats padding</name>
  <files>frontend/src/index.css, frontend/src/pages/GlobalStats.tsx</files>
  <read_first>
    - frontend/src/index.css
    - frontend/src/pages/GlobalStats.tsx
  </read_first>
  <action>
    **Part A: Global overflow safety net (index.css)**

    Add `overflow-x: hidden` to the `body` element in `index.css`. Place it after the existing CSS custom property definitions. If there's already a `body` rule, add the property to it. If not, create one:

    ```css
    body {
      overflow-x: hidden;
    }
    ```

    This prevents any horizontal scrollbar from appearing on any page. It's safe because body-level horizontal scroll is never intentional in this app.

    **Part B: Fix GlobalStats responsive padding (GlobalStats.tsx)**

    On line 26, change the outer container class from `p-6` to `px-4 py-6 sm:px-6`:

    ```typescript
    <div data-testid="global-stats-page" className="mx-auto max-w-4xl space-y-6 px-4 py-6 sm:px-6">
    ```

    This gives 16px padding on mobile (leaving 343px for content) instead of 24px (327px).

    **Part C: Fix GlobalStats platform buttons to 44px touch targets**

    The platform filter buttons on GlobalStats.tsx (line 72-73) use `py-0.5` which creates ~24px height buttons. Update the class to add mobile-responsive touch target:

    Change from:
    ```
    'rounded border px-2 py-0.5 text-xs transition-colors'
    ```
    To:
    ```
    'rounded border px-3 py-2 sm:px-2 sm:py-0.5 text-xs transition-colors min-h-11 sm:min-h-0'
    ```
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - index.css contains `overflow-x: hidden` in a body rule
    - GlobalStats.tsx contains `px-4 py-6 sm:px-6` (not `p-6`)
    - GlobalStats.tsx platform buttons contain `min-h-11 sm:min-h-0`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>No horizontal scroll at 375px on any page (body overflow-x hidden). GlobalStats has responsive padding. GlobalStats platform buttons meet 44px touch target.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 44px touch targets on FilterPanel and BoardControls</name>
  <files>frontend/src/components/filters/FilterPanel.tsx, frontend/src/components/board/BoardControls.tsx</files>
  <read_first>
    - frontend/src/components/filters/FilterPanel.tsx
    - frontend/src/components/board/BoardControls.tsx
    - frontend/src/components/ui/toggle-group.tsx (to understand ToggleGroupItem default sizing)
    - frontend/src/pages/Openings.tsx (lines 280-330 to see ToggleGroupItem usage for Played-as and Piece filter)
  </read_first>
  <action>
    **Part A: FilterPanel time control buttons (FilterPanel.tsx)**

    Lines 94-110: The time control `<button>` elements use `'rounded border px-2 py-0.5 text-xs transition-colors'`. Update to:

    ```typescript
    className={cn(
      'rounded border px-3 py-2 sm:px-2 sm:py-0.5 text-xs transition-colors min-h-11 sm:min-h-0',
      isTimeControlActive(tc)
        ? 'border-primary bg-primary text-primary-foreground'
        : 'border-border bg-transparent text-muted-foreground hover:border-foreground hover:text-foreground',
    )}
    ```

    **Part B: FilterPanel platform buttons (FilterPanel.tsx)**

    Lines 118-134: The platform `<button>` elements use same `py-0.5` pattern. Apply identical fix:

    ```typescript
    className={cn(
      'rounded border px-3 py-2 sm:px-2 sm:py-0.5 text-xs transition-colors min-h-11 sm:min-h-0',
      isPlatformActive(p)
        ? 'border-primary bg-primary text-primary-foreground'
        : 'border-border bg-transparent text-muted-foreground hover:border-foreground hover:text-foreground',
    )}
    ```

    **Part C: FilterPanel ToggleGroupItem (Rated, Opponent sections)**

    The `ToggleGroupItem` components at lines 152-154 and 172-174 inherit default `size="sm"` from parent `ToggleGroup`. Add `className="min-h-11 sm:min-h-0"` to each `ToggleGroupItem`:

    ```typescript
    <ToggleGroupItem value="all" data-testid="filter-rated-all" className="min-h-11 sm:min-h-0">All</ToggleGroupItem>
    <ToggleGroupItem value="rated" data-testid="filter-rated-rated" className="min-h-11 sm:min-h-0">Rated</ToggleGroupItem>
    <ToggleGroupItem value="casual" data-testid="filter-rated-casual" className="min-h-11 sm:min-h-0">Casual</ToggleGroupItem>
    ```

    Same for Opponent section:
    ```typescript
    <ToggleGroupItem value="human" data-testid="filter-opponent-human" className="min-h-11 sm:min-h-0">Human</ToggleGroupItem>
    <ToggleGroupItem value="bot" data-testid="filter-opponent-bot" className="min-h-11 sm:min-h-0">Bot</ToggleGroupItem>
    <ToggleGroupItem value="both" data-testid="filter-opponent-both" className="min-h-11 sm:min-h-0">Both</ToggleGroupItem>
    ```

    **Part D: BoardControls icon buttons (BoardControls.tsx)**

    Lines 23-66: Each `<Button variant="ghost" size="icon" ...>` renders at shadcn default `h-8 w-8` (32px). Add responsive sizing to make them 44px on mobile, 32px on desktop. Add `className` to each Button:

    ```typescript
    <Button
      variant="ghost"
      size="icon"
      className="h-11 w-11 sm:h-8 sm:w-8"
      onClick={onReset}
      disabled={!canGoBack}
      title="Reset to start"
      aria-label="Reset to start"
      data-testid="board-btn-reset"
    >
    ```

    Apply `className="h-11 w-11 sm:h-8 sm:w-8"` to all four board control buttons (reset, back, forward, flip).

    **Part E: Openings page Played-as and Piece filter ToggleGroupItems**

    In Openings.tsx (lines 297-304 and 326-329), the `ToggleGroupItem` components for "Played as" (White/Black) and "Piece filter" (Mine/Opponent/Both) also need `className="min-h-11 sm:min-h-0"`:

    ```typescript
    <ToggleGroupItem value="white" data-testid="filter-played-as-white" className="min-h-11 sm:min-h-0">
    <ToggleGroupItem value="black" data-testid="filter-played-as-black" className="min-h-11 sm:min-h-0">
    <ToggleGroupItem value="mine" data-testid="filter-piece-filter-mine" className="min-h-11 sm:min-h-0">
    <ToggleGroupItem value="opponent" data-testid="filter-piece-filter-opponent" className="min-h-11 sm:min-h-0">
    <ToggleGroupItem value="both" data-testid="filter-piece-filter-both" className="min-h-11 sm:min-h-0">
    ```

    **Note:** MoveList move buttons are intentionally excluded from 44px targets per CONTEXT.md: "MoveList move buttons are compact by design; keep as-is since they're not primary controls."

    **Note:** The `SelectTrigger size="sm"` for recency dropdowns is already tall enough at ~36px on mobile and is a single-tap target — acceptable.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - FilterPanel.tsx time control buttons contain `min-h-11 sm:min-h-0`
    - FilterPanel.tsx platform buttons contain `min-h-11 sm:min-h-0`
    - FilterPanel.tsx ToggleGroupItem elements for Rated contain `min-h-11 sm:min-h-0`
    - FilterPanel.tsx ToggleGroupItem elements for Opponent contain `min-h-11 sm:min-h-0`
    - BoardControls.tsx all four Buttons contain `h-11 w-11 sm:h-8 sm:w-8`
    - Openings.tsx ToggleGroupItem for Played-as contain `min-h-11 sm:min-h-0`
    - Openings.tsx ToggleGroupItem for Piece filter contain `min-h-11 sm:min-h-0`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All filter buttons, toggle groups, and board control buttons meet 44px minimum touch target on mobile. Desktop sizing preserved via sm: breakpoint reset.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript compilation succeeds
- `uv run pytest -x` — Backend tests still pass (no backend changes, regression guard)
- Manual: Open each page (Import, Openings, Global Stats) at 375px viewport in Chrome DevTools — no horizontal scrollbar
- Manual: Inspect filter buttons and board controls at 375px — all should be at least 44px tall
- Manual: At desktop width, buttons should be compact (not 44px)
</verification>

<success_criteria>
- No horizontal scrollbar on any page at 375px viewport width
- All filter buttons (time control, platform, rated, opponent, played-as, piece filter) are 44px+ on mobile
- All board control icon buttons are 44px on mobile
- Desktop compact sizing preserved
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/19-mobile-ux-polish-install-prompt/19-02-SUMMARY.md`
</output>
