---
phase: 157-flawchess-agreement-verdict-prose-hoverable-moves
plan: 02
type: execute
wave: 2
depends_on: [157-01]
files_modified:
  - frontend/src/components/analysis/ProseSpan.tsx
  - frontend/src/components/analysis/MaiaMoveQualityBar.tsx
  - frontend/src/components/analysis/FlawChessAgreementVerdict.tsx
  - frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx
  - frontend/src/pages/Analysis.tsx
autonomous: true
requirements: [REVIEW-02]

must_haves:
  truths:
    - "Below the FlawChess ranked lines, a one-line prose verdict narrates agreement or divergence between FlawChess's #1 practical move and Stockfish's #1 objective move, citing both evals (D-01/D-07, SC1)."
    - "No UI string reads a bare 'best move'; move names are the interactive spans (D-08, SC1)."
    - "Hovering a move span isolates that pick's board arrow (amber = FlawChess, blue = Stockfish), overriding the persistent two-arrow layer; leaving restores both (D-09, SC2)."
    - "The span popover shows engine-labeled two lines — `FlawChess: X (practical)` / `Stockfish: Y (objective)` — and omits the FlawChess line for the Stockfish pick when the engine did not also rank that move (D-10, SC2)."
    - "Clicking a span (while its popover is open) plays the move as a free move via the existing onPlayMove wiring (D-11, SC2)."
    - "The verdict renders only when engineEnabled === true AND a FlawChess snapshot exists; when Stockfish is off, a fixed-height muted prompt `Turn on Stockfish to compare picks.` occupies the slot with no layout jump (D-02/D-03, SC3)."
    - "The tier refines live inside a fixed-height slot as the search progresses (D-06, SC3)."
    - "The verdict is inserted ONCE inside the shared `flawChessCard` JSX in Analysis.tsx, so it appears identically in free analysis and game review (`?game_id&ply`) by construction (SC4 / REVIEW-01)."
  artifacts:
    - "frontend/src/components/analysis/ProseSpan.tsx (extracted content-agnostic hover/click-to-play span shell)"
    - "frontend/src/components/analysis/FlawChessAgreementVerdict.tsx (the prose verdict component)"
    - "frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx (component tests)"
  key_links:
    - "The component's onHoverMovesChange feeds the existing setHoveredQualityMoves -> qualityHoverArrows overlay that already wins over engineArrows (D-09)."
    - "The component's onPlayMove is the existing playProseMove free-move handler (D-11)."
    - "The tier comes from computeFlawChessVerdict (Plan 01); the SF-side objective is engine.pvLines[0], never engineTopLines[0] (D-01)."
    - "The extracted ProseSpan keeps MaiaMoveQualityBar's 3 shipped prose-span tests green (byte-identical testids/aria/behavior)."
---

<objective>
Render the FlawChess agreement verdict on `/analysis`. Extract the content-agnostic hover-intent + click-to-play span shell from `MaiaMoveQualityBar`'s private `ProseMoveSpan` into a shared `ProseSpan.tsx` (keeping the Maia surface's tests green), then build `FlawChessAgreementVerdict.tsx` that consumes the Plan 01 classifier and `ProseSpan` to render the three D-07 prose templates with hoverable/click-to-play move spans, arrow isolation (D-09), the engine-labeled two-line popover (D-10), and the Stockfish-off muted prompt in a fixed-height slot (D-02/D-03). Wire it once into the shared `flawChessCard` JSX in `Analysis.tsx` so game-review parity (SC4/REVIEW-01) holds by construction.

Purpose: Turn the side-by-side score badges into a plain-language "engines are flawless, humans play FlawChess" read, reusing shipped Phase 151-156 plumbing rather than inventing new mechanics.
Output: `ProseSpan.tsx`, `FlawChessAgreementVerdict.tsx`, its test, and the single `Analysis.tsx` insertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-flawchess-agreement-verdict-prose-hoverable-moves/157-CONTEXT.md
@.planning/phases/157-flawchess-agreement-verdict-prose-hoverable-moves/157-RESEARCH.md
@.planning/phases/157-flawchess-agreement-verdict-prose-hoverable-moves/157-PATTERNS.md
@.planning/phases/157-flawchess-agreement-verdict-prose-hoverable-moves/157-01-SUMMARY.md

# Reuse targets (READ before implementing):
@frontend/src/components/analysis/MaiaMoveQualityBar.tsx
@frontend/src/components/analysis/__tests__/MaiaMoveQualityBar.test.tsx
@frontend/src/components/analysis/FlawChessEngineLines.tsx
@frontend/src/lib/flawChessVerdict.ts
@frontend/src/lib/liveFlaw.ts
@frontend/src/lib/theme.ts
@frontend/src/components/ui/popover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the content-agnostic ProseSpan shell</name>
  <read_first>
    - frontend/src/components/analysis/MaiaMoveQualityBar.tsx:145-207 — the private `ProseMoveSpan` (button + Popover + `wasOpenAtPress` ref + hover-intent/content-bridge wiring). This is the exact shell to lift.
    - frontend/src/components/analysis/MaiaMoveQualityBar.tsx:52-55 — the exported `HoveredQualityMove` interface.
    - frontend/src/components/analysis/__tests__/MaiaMoveQualityBar.test.tsx:143-200 — the 3 prose-span tests that MUST stay green (testids `maia-prose-move-*` / `maia-prose-move-tooltip-*`, aria-labels, focus/pointerDown/click behavior).
    - frontend/src/components/ui/popover.tsx — `Popover`/`PopoverAnchor`/`PopoverContent` exports.
  </read_first>
  <files>frontend/src/components/analysis/ProseSpan.tsx, frontend/src/components/analysis/MaiaMoveQualityBar.tsx</files>
  <action>
    Create `frontend/src/components/analysis/ProseSpan.tsx` exporting a content-parameterized span component and its props type:
    - `export interface ProseSpanProps { label: string; textColor: string; ariaLabel: string; testId: string; tooltipTestId: string; isOpen: boolean; onOpenDelayed: () => void; onOpenNow: () => void; onClose: () => void; onPlay?: () => void; children: React.ReactNode; }`
    - `export function ProseSpan(props: ProseSpanProps): React.ReactElement` — the SAME button/`Popover`/`PopoverAnchor`/`PopoverContent` wiring as `ProseMoveSpan` (the `wasOpenAtPress` ref read synchronously in `onPointerDown`, the reveal-vs-play `onClick` branch, the content-bridge `onMouseEnter`/`onMouseLeave` on both the button and the popover content). The button renders `{label}`; the popover content renders `{children}`. Keep the exact className/style conventions from the original (dotted-underline button, `style={{ color: textColor }}`, popover `text-xs text-background bg-foreground` body — the documented text-xs tooltip exception applies).

    Then refactor `MaiaMoveQualityBar.tsx`'s private `ProseMoveSpan` into a thin wrapper that calls `ProseSpan`, passing its Maia-specific `children` (`{`${move.maiaPct}% at this rating · ${evalText}`}`), the same `data-testid={`maia-prose-move-${move.san}`}` / tooltip testid, the same aria-label string, and `label={move.san}`. Do NOT change any testid, aria-label text, or observable behavior — the 3 existing Maia tests are the guard.

    Keep `HoveredQualityMove` exported from `MaiaMoveQualityBar.tsx` as-is (Plan 02 Task 2 imports it from there; do not duplicate the interface). Extract only the span shell, not the popover content.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analysis/__tests__/MaiaMoveQualityBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ProseSpan.tsx` exists exporting `ProseSpan` + `ProseSpanProps`, content-agnostic (popover body via `children`).
    - `MaiaMoveQualityBar.tsx`'s `ProseMoveSpan` is now a wrapper over `ProseSpan`; all 3 prose-span tests in `MaiaMoveQualityBar.test.tsx` pass unchanged.
    - No testid, aria-label, or class change observable on the Maia surface.
  </acceptance_criteria>
  <done>The hover-intent/click-to-play shell is a single reusable component; the shipped Maia surface behaves identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build FlawChessAgreementVerdict component + tests</name>
  <read_first>
    - frontend/src/lib/flawChessVerdict.ts — `computeFlawChessVerdict`, `FlawChessVerdictResult`, `FlawChessVerdictMove`, `SHARP_DROP_THRESHOLD`.
    - frontend/src/components/analysis/MaiaMoveQualityBar.tsx:215-261 (`renderVerdictSentence`), :272-318 (parent-side state: `activeProseSan`, hover timer, `openProseNow`/`openProseDelayed`/`closeProse`, the hover-lift `useEffect`), :442-464 (fixed-height resting slot).
    - frontend/src/components/analysis/__tests__/MaiaMoveQualityBar.test.tsx:143-200 — the `fireEvent.focus`/`fireEvent.pointerDown`+`fireEvent.click` patterns to mirror (this component needs NO matchMedia/ResizeObserver stubs).
    - frontend/src/components/analysis/FlawChessEngineLines.tsx — exported `formatScore` (reuse for cross-badge-consistent eval strings, Open Q1); score-pair badge precedent.
    - frontend/src/lib/liveFlaw.ts — `sideToMoveFromFen` (to derive the mover from `baseFen`).
    - frontend/src/lib/theme.ts — `FLAWCHESS_ENGINE_ARROW`, `BEST_MOVE_ARROW`.
  </read_first>
  <files>frontend/src/components/analysis/FlawChessAgreementVerdict.tsx, frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx</files>
  <behavior>
    - D-02/D-03: when `engineEnabled === false`, render ONLY the fixed-height muted prompt `Turn on Stockfish to compare picks.` (testid `flawchess-verdict-prompt`); `computeFlawChessVerdict` is not consulted / no move spans render.
    - D-06 loading: when `engineEnabled === true` but `computeFlawChessVerdict(...)` returns null (partial snapshot), render the same fixed-height muted slot (no tier prose, no layout jump).
    - D-04/D-05/D-07: for a non-null verdict, render the tier's prose template (aligned / safe / sharp) with the named moves as `ProseSpan`s; neutral "for a human here" phrasing; both evals cited via `formatScore`. No literal "best move" substring anywhere.
    - D-09: focusing/hovering a span calls `onHoverMovesChange` with exactly one `{ san, color }` in that pick's tier color (amber for the FlawChess pick, blue for the Stockfish pick); blur/leave calls `onHoverMovesChange(null)`.
    - D-10: the span popover body is two engine-labeled lines — `FlawChess: <practical> (practical)` and `Stockfish: <objective> (objective)`. For the FlawChess pick, both lines always show. For the Stockfish pick, the FlawChess line shows ONLY if that UCI move also appears in `flawChessRankedLines` (rootMove match) — otherwise the FlawChess line is omitted entirely (no `—`, no invented number).
    - D-11: clicking a span while its popover is already open calls `onPlayMove(san)`; a first click (popover closed) only reveals, and does NOT call `onPlayMove`.
  </behavior>
  <action>
    Create `FlawChessAgreementVerdict.tsx`. Props:
    `{ flawChessLine: RankedLine | null; stockfishLine: PvLine | null; flawChessRankedLines: RankedLine[]; engineEnabled: boolean; baseFen: string; onHoverMovesChange?: (moves: HoveredQualityMove[] | null) => void; onPlayMove?: (san: string) => void }`.

    Structure (mirror `MaiaMoveQualityBar`'s prose sub-pattern):
    1. If `!engineEnabled`, render the fixed-height (`min-h-[1.5rem]`) muted prompt and return — do not call the classifier (D-02/D-03).
    2. Derive `mover = sideToMoveFromFen(baseFen)`; call `computeFlawChessVerdict(flawChessLine, stockfishLine, mover)`. If null, render the same fixed-height muted slot (D-06).
    3. Convert each pick's UCI to SAN legal at `baseFen` for display/hover/click: parse UCI as `{ from: uci.slice(0,2), to: uci.slice(2,4), promotion: uci[4] }` via `new Chess(baseFen).move(...)` wrapped in try/catch (skip a pick whose SAN is unresolvable — never feed raw UCI to the hover callback, Pitfall 2). Keep this helper local; do NOT push chess.js into `flawChessVerdict.ts`.
    4. Own parent-side span state exactly like `MaiaMoveQualityBar`: one `activeProseSan: string | null`, a hover-timer ref, and `openProseNow`/`openProseDelayed`/`closeProse` (reuse the same 100ms open delay — import or restate the existing `PROSE_POPOVER_OPEN_DELAY_MS` value, do not invent a different delay). A hover-lift `useEffect` feeds `onHoverMovesChange` a 0/1-element `HoveredQualityMove[]` in the hovered pick's tier color, clearing to null on unmount/leave (D-09).
    5. Render the tier prose via a plain `renderVerdictSentence`-style function switching on tier, interpolating `ProseSpan`s for the 1-2 named moves (aligned names 1 move; safe/sharp name 2). Import `ProseSpan` from `./ProseSpan`; give each span the D-10 popover body as `children`, tier `textColor`, an aria-label including the move + "Click to play it.", and testids `flawchess-verdict-move-<san>` / `flawchess-verdict-tooltip-<san>`. `interleaveWithConjunction`/`joinMoveNames` are NOT needed (fixed 1-2 moves).
    6. For the Stockfish pick's popover, look up the SF UCI in `flawChessRankedLines` (rootMove match) to decide whether to include the FlawChess line (D-10).
    7. Click plays via `onPlayMove(san)` (D-11), passed to `ProseSpan.onPlay`.

    Wrap the whole thing in a `min-h-[...]` fixed-height container so tier refinement never shifts layout (D-06). Use `formatScore` for eval strings (Open Q1: cross-badge consistency with the adjacent FlawChessEngineLines badges). `data-testid` on every span; semantic `<button>` (inherited from `ProseSpan`); popover body may use `text-xs` (documented tooltip exception).

    Co-locate `__tests__/FlawChessAgreementVerdict.test.tsx` mirroring `MaiaMoveQualityBar.test.tsx`'s prose-span tests (no matchMedia/ResizeObserver stubs). Cover the D-02/D-03 muted-prompt, D-09 hover-callback color, D-10 two-line vs omitted-FC-line, and D-11 reveal-then-play cases from the `<behavior>` block. Feed direct `RankedLine`/`PvLine` prop fixtures and a legal `baseFen`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` cases (D-02/D-03, D-04/D-05/D-07, D-09, D-10, D-11) have passing tests.
    - `grep -in 'best move' frontend/src/components/analysis/FlawChessAgreementVerdict.tsx` returns nothing (D-08).
    - The component imports `ProseSpan` from `./ProseSpan` and `computeFlawChessVerdict` from `@/lib/flawChessVerdict`; it reads the Stockfish side from the passed `stockfishLine` prop (sourced from `engine.pvLines[0]` at the call site), never from `engineTopLines`.
    - Hover callback emits amber for the FlawChess pick and blue for the Stockfish pick (theme constants).
    - `cd frontend && npx tsc -b` reports zero errors.
  </acceptance_criteria>
  <done>The verdict component renders all three tiers with working hover-arrow isolation, the engine-labeled popover, and click-to-play, and is unit-green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the verdict into the shared flawChessCard in Analysis.tsx</name>
  <read_first>
    - frontend/src/pages/Analysis.tsx:1476-1515 — the `flawChessCard` JSX variable and the `FlawChessEngineLines` render inside its CardBody (the single insertion site).
    - frontend/src/pages/Analysis.tsx around L411 (`hoveredQualityMoves`/`setHoveredQualityMoves`), L513-520 (`playProseMove`), L1049-1067 (`qualityHoverArrows`), L1139-1142 (arrow precedence chain), and the `engine.pvLines` / `flawChessEngine.rankedLines` / `engineEnabled` reads.
    - 157-PATTERNS.md "frontend/src/pages/Analysis.tsx (modify)" section — the exact insertion + wiring template.
  </read_first>
  <files>frontend/src/pages/Analysis.tsx</files>
  <action>
    Add the new component ONCE, as a sibling directly BELOW `<FlawChessEngineLines .../>` inside the same `flawChessCard` CardBody (do NOT fold it into `FlawChessEngineLines`'s props — it is a body-only sibling per that component's docstring). Because `flawChessCard` is a single JSX variable rendered at both the mobile human tab and the desktop human column, this one edit satisfies both surfaces (SC4/REVIEW-01) — do NOT duplicate the insertion.

    Wire it to already-existing state/handlers (declare nothing new):
    ```
    <FlawChessAgreementVerdict
      flawChessLine={flawChessEngine.rankedLines[0] ?? null}
      stockfishLine={engine.pvLines[0] ?? null}
      flawChessRankedLines={flawChessEngine.rankedLines}
      engineEnabled={engineEnabled}
      baseFen={position}
      onHoverMovesChange={setHoveredQualityMoves}
      onPlayMove={playProseMove}
    />
    ```
    Source the Stockfish objective from `engine.pvLines[0]` (D-01), NOT `engineTopLines[0]`. Respect `noUncheckedIndexedAccess`: the `?? null` on the indexed reads is required. No change to `qualityHoverArrows` or the arrow precedence chain — the new component just becomes a second caller of `setHoveredQualityMoves`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `FlawChessAgreementVerdict` appears exactly once in `Analysis.tsx`, inside `flawChessCard`, below `FlawChessEngineLines`.
    - `grep -n 'engineTopLines' ` shows the new component is NOT fed `engineTopLines` (it receives `engine.pvLines[0]`).
    - `npx tsc -b` zero errors; `npm run lint` clean (knip finds no dead exports); `npm test -- --run` fully green.
  </acceptance_criteria>
  <done>The verdict renders live on `/analysis` free analysis and game review, wired to the existing arrow/free-move plumbing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine-worker → component | FlawChess/Stockfish worker outputs (already on the page) render as prose and drive board arrows/free moves. No new user text input, no network call, no auth surface. |
| span click → board | A click replays a move that is a legal, engine-surfaced move via the same `onPlayMove` path `MaiaMoveQualityBar` already uses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-02 | Tampering | engine-derived SAN/eval strings rendered as prose | low | accept | All strings render as React children (auto-escaped); no `dangerouslySetInnerHTML`. Matches the T-137-03 convention in `FlawChessEngineLines.tsx`. |
| T-157-03 | Tampering | UCI→SAN replay for the free move | low | accept | `new Chess(baseFen).move(...)` wrapped in try/catch; illegal/stale moves are silently skipped, never executed. Same guard as the existing `playProseMove`. |
| T-157-SC | Tampering | npm installs | low | accept | No packages installed this phase. Nothing to verify. |

No high-severity items — client-only display over already-trusted, already-rendered engine state; no new input, network, auth, or injection sink.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx src/components/analysis/__tests__/MaiaMoveQualityBar.test.tsx` — GREEN (new component + unchanged Maia surface).
- `cd frontend && npx tsc -b && npm run lint && npm test -- --run` — zero type errors, clean lint/knip, full suite green.
- `grep -rin 'best move' frontend/src/components/analysis/FlawChessAgreementVerdict.tsx` — empty (D-08).
- Manual UAT (SC4): open `/analysis?game_id=<id>&ply=<n>` with Stockfish on; confirm the verdict prose, hover arrow isolation, popover, and click-to-play behave identically to free analysis.
</verification>

<success_criteria>
- SC1: the FlawChess card shows a prose agreement/divergence verdict citing both evals, no bare "best move".
- SC2: named moves are interactive spans — hover isolates the tier-colored arrow, the engine-labeled popover shows practical/objective, click plays the free move.
- SC3: three tiers via named constants, refining live in a fixed-height slot (no jump); muted prompt when Stockfish is off.
- SC4/REVIEW-01: renders identically on the game-review board by construction (single shared insertion site).
</success_criteria>

<output>
Create `.planning/phases/157-flawchess-agreement-verdict-prose-hoverable-moves/157-02-SUMMARY.md` when done.
</output>
