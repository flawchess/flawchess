---
phase: 112-flaws-subtab-card-rework
plan: 04
type: execute
wave: 4
depends_on: ["112-02", "112-03"]
# Integration note (WARNING 6): 112-04 lands together with 112-03 as ONE integration unit.
# This plan's Task 3 import of FlawCard into FlawsTab closes the only knip dead-export window
# for FlawCard. knip is validated at the full-gate squash-merge to main after this plan
# (CLAUDE.md — knip runs at the integration gate, not between waves).
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/pages/library/FlawsTab.tsx
  - frontend/src/components/library/__tests__/FlawCard.test.tsx
  - CHANGELOG.md
autonomous: true
requirements: [SC-1, SC-7, SC-8]
must_haves:
  truths:
    - "FlawsTab renders flaws as FlawCards in a responsive grid (grid-cols-1 lg:grid-cols-2), replacing the flex-col gap-3 list and the old FlawRow"
    - "A 'View game' button in each FlawCard opens a Dialog modal showing the full LibraryGameCard for that game"
    - "The modal fetches the game on open via useLibraryGame(gameId) -> GET /api/library/games/{game_id}"
    - "The modal handles loading (spinner), error (LoadError), and success (LibraryGameCard) states; isError never falls through to empty"
    - "All new interactive elements (card, view-game button, modal) carry data-testid + ARIA; grid collapses to 1-up on mobile"
  artifacts:
    - path: "frontend/src/hooks/useLibrary.ts"
      provides: "useLibraryGame(gameId: number | null) query hook (enabled when gameId !== null)"
      contains: "useLibraryGame"
    - path: "frontend/src/api/client.ts"
      provides: "libraryApi.getGame(gameId) -> GameFlawCard"
      contains: "getGame"
    - path: "frontend/src/components/library/FlawCard.tsx"
      provides: "View game button + Dialog modal wrapping LibraryGameCard with loading/error/success states"
      contains: "flaw-game-modal"
    - path: "frontend/src/pages/library/FlawsTab.tsx"
      provides: "responsive FlawCard grid replacing FlawRow + flex-col list; FlawRow + MINI_BOARD_SIZE removed"
      contains: "flaw-grid"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] bullet for the Flaws subtab card + View-game modal rework"
      contains: "Flaws"
  key_links:
    - from: "frontend/src/pages/library/FlawsTab.tsx"
      to: "frontend/src/components/library/FlawCard.tsx"
      via: "grid map over flaws"
      pattern: "FlawCard"
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "frontend/src/hooks/useLibrary.ts useLibraryGame"
      via: "fetch on modal open"
      pattern: "useLibraryGame"
    - from: "frontend/src/hooks/useLibrary.ts"
      to: "GET /api/library/games/{game_id}"
      via: "libraryApi.getGame"
      pattern: "getGame"
---

<objective>
Wire the "View game" modal end-to-end and replace the Flaws list with the responsive `FlawCard` grid. Add `libraryApi.getGame` + `useLibraryGame`, the "View game" button + `Dialog` (loading/error/success) inside `FlawCard`, swap `FlawsTab`'s `flex-col` list for `grid grid-cols-1 lg:grid-cols-2`, delete the old `FlawRow`, and add the CHANGELOG entry.

Purpose: SC-1 (grid replaces the row list), SC-7 (View-game modal via the new endpoint), SC-8 (a11y + mobile parity).
Output: API method + hook, modal-enabled `FlawCard`, gridded `FlawsTab`, modal tests, CHANGELOG bullet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-flaws-subtab-card-rework/112-CONTEXT.md
@.planning/phases/112-flaws-subtab-card-rework/112-RESEARCH.md
@.planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md
@.planning/phases/112-flaws-subtab-card-rework/112-UI-SPEC.md
@.planning/phases/112-flaws-subtab-card-rework/112-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (112-04) produces:
- `libraryApi.getGame(gameId)` in `frontend/src/api/client.ts`.
- `useLibraryGame(gameId: number | null)` hook in `frontend/src/hooks/useLibrary.ts`.
- "View game" `Button` + `Dialog` modal (testid `flaw-game-modal`) inside `FlawCard` with spinner / `LoadError` / `LibraryGameCard` states.
- Responsive `FlawCard` grid (testid `flaw-grid`) in `FlawsTab`; removal of `FlawRow` and `MINI_BOARD_SIZE`.
- Modal interaction tests added to `FlawCard.test.tsx`.
- `CHANGELOG.md` `[Unreleased]` bullet.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: libraryApi.getGame + useLibraryGame hook</name>
  <read_first>
    - frontend/src/api/client.ts (libraryApi block — getFlaws method pattern; import of types from @/types/library)
    - frontend/src/hooks/useLibrary.ts (useLibraryFlaws lines 89-102; LIBRARY_STALE_TIME line ~10)
    - frontend/src/types/library.ts (GameFlawCard type — the getGame return type)
    - .planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md (§api/client.ts + §useLibrary.ts templates)
  </read_first>
  <behavior>
    - useLibraryGame(null) is disabled (enabled: gameId !== null) — no fetch fires.
    - useLibraryGame(123) calls libraryApi.getGame(123) → GET /library/games/123.
    - Query key is ['library-game', gameId]; staleTime = LIBRARY_STALE_TIME; refetchOnWindowFocus false.
  </behavior>
  <action>
    Add `getGame: (gameId: number) => apiClient.get<GameFlawCard>(`/library/games/${gameId}`).then(r => r.data)` to the `libraryApi` object in `frontend/src/api/client.ts` (import `GameFlawCard` from `@/types/library` if not already imported). Add `export function useLibraryGame(gameId: number | null)` to `frontend/src/hooks/useLibrary.ts` using `useQuery` with `queryKey: ['library-game', gameId]`, `queryFn: () => libraryApi.getGame(gameId!)`, `enabled: gameId !== null`, `staleTime: LIBRARY_STALE_TIME`, `refetchOnWindowFocus: false`.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `useLibraryGame` exists with `enabled: gameId !== null` and query key `['library-game', gameId]`.
    - `libraryApi.getGame` targets `/library/games/${gameId}` and is typed to return `GameFlawCard`.
    - `cd frontend && npm run lint && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The single-game fetch hook + API method exist and type-check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add "View game" button + Dialog modal to FlawCard</name>
  <read_first>
    - frontend/src/components/library/FlawCard.tsx (the component from 112-03 — append the button + modal at the reserved bottom-of-stack spot)
    - frontend/src/components/ui/dialog.tsx (Dialog, DialogContent, DialogTitle; default sm:max-w-sm — override needed)
    - frontend/src/components/ui/load-error.tsx (LoadError resource/variant props)
    - frontend/src/components/results/LibraryGameCard.tsx (the verbatim modal body; confirm its prop name for the game/card data)
    - frontend/src/components/ui/button.tsx (variant="default" primary, size="sm")
    - frontend/src/hooks/useLibrary.ts (useLibraryGame from Task 1)
    - .planning/phases/112-flaws-subtab-card-rework/112-UI-SPEC.md (§"View game" Button + Modal contracts; Pitfall 2 note)
  </read_first>
  <behavior>
    - Clicking the "View game" button sets selectedGameId and opens the Dialog; closing resets selectedGameId to null.
    - Modal shows a spinner while loading, LoadError on isError, and LibraryGameCard on success.
    - isError never falls through to empty/success (CLAUDE.md mandatory branch).
    - Button + modal carry data-testid (flaw-card-view-game-{id}-{ply}, flaw-game-modal) and ARIA labels.
  </behavior>
  <action>
    In `FlawCard.tsx`, add `const [open, setOpen] = useState(false)` and call `useLibraryGame(open ? flaw.game_id : null)`. Render at the bottom of the content stack a `Button variant="default" size="sm"` with a `BookOpen` icon (mr-2), label "View game", `self-start`, `data-testid={`flaw-card-view-game-${flaw.game_id}-${flaw.ply}`}`, `aria-label={`View full game for ${whiteName} vs ${blackName}`}`, onClick `() => setOpen(true)`. Add a `Dialog open={open} onOpenChange={(v) => !v && setOpen(false)}` with `DialogContent className="sm:max-w-4xl overflow-y-auto max-h-[90vh]" data-testid="flaw-game-modal" aria-label="View full game"` containing a visually-hidden `DialogTitle` ("Full game view", `className="sr-only"`) and the state chain: `{isLoading && <spinner in p-8 wrapper>}`, `{isError && <LoadError resource="game" variant="centered" />}`, `{data && <LibraryGameCard game={data} />}` (use the actual LibraryGameCard prop name confirmed in read_first). Do not modify `LibraryGameCard`. Reuse the existing Spinner component (match the FlawsTab loading spinner import).
    Extend `FlawCard.test.tsx`: clicking the view-game button opens the modal (testid `flaw-game-modal` appears); with a mocked error state, `LoadError` renders; with mocked success, `LibraryGameCard` renders.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/library/__tests__/FlawCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/components/library/__tests__/FlawCard.test.tsx` passes including the modal open/error/success cases.
    - DialogContent carries `data-testid="flaw-game-modal"` and `aria-label`; button carries its testid + aria-label.
    - The modal renders all three of: spinner branch, `LoadError` branch, `LibraryGameCard` branch (isError handled explicitly).
    - `DialogContent` className overrides the default to `sm:max-w-4xl` with `overflow-y-auto max-h-[90vh]`.
    - `cd frontend && npm run lint` clean.
  </acceptance_criteria>
  <done>FlawCard's View-game button opens a modal that loads + renders the full LibraryGameCard with proper loading/error states.</done>
</task>

<task type="auto">
  <name>Task 3: Replace FlawsTab list with the FlawCard grid; remove FlawRow; CHANGELOG</name>
  <read_first>
    - frontend/src/pages/library/FlawsTab.tsx (FlawRow lines 48-141 — delete; MINI_BOARD_SIZE=80 line 41 — delete; list block lines 414-419 — replace; existing imports of LazyMiniBoard/SeverityBadge/TagChip that move into FlawCard; the flaws loading/error/empty chain to keep)
    - frontend/src/components/library/FlawCard.tsx (the finished component to import)
    - .planning/phases/112-flaws-subtab-card-rework/112-UI-SPEC.md (§Grid contract — grid-cols-1 lg:grid-cols-2 gap-4, flaw-grid testid)
    - CHANGELOG.md ([Unreleased] section + subsection grouping convention from CLAUDE.md)
  </read_first>
  <behavior>
    - Flaws render in a `grid grid-cols-1 lg:grid-cols-2 gap-4` container with data-testid="flaw-grid".
    - The old FlawRow function and MINI_BOARD_SIZE constant are removed; now-unused imports cleaned (knip clean).
    - The existing flaws loading / isError / empty-state chain in FlawsTab is preserved unchanged.
  </behavior>
  <action>
    In `FlawsTab.tsx`, replace the `<div className="flex flex-col gap-3">{flaws.map(... <FlawRow ...>)}</div>` block (lines ~414-419) with `<div className="grid grid-cols-1 lg:grid-cols-2 gap-4" data-testid="flaw-grid">{flaws.map((flaw) => <FlawCard key={`${flaw.game_id}-${flaw.ply}`} flaw={flaw} />)}</div>`. Delete the `FlawRow` function (lines ~48-141) and the `MINI_BOARD_SIZE` constant (line ~41). Import `FlawCard` from `@/components/library/FlawCard`; remove imports now only used by the deleted FlawRow (LazyMiniBoard, SeverityBadge, TagChip, sanToSquares, etc. as applicable) so knip stays clean. Leave the filters/drawer/pagination/URL-sync/loading/isError/empty-state scaffolding untouched (the existing `flawsError` and "No flaws matched" branches stay). Add a `CHANGELOG.md` `## [Unreleased]` bullet under `### Changed` (user-facing, terse, no em-dash): the Library Flaws subtab now shows each flaw as a card in a responsive 2-up grid with a "View game" modal opening the full analyzed game.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run src/pages/library src/components/library</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` clean (knip reports no dead exports / unused deps): after this grid swap imports `FlawCard`, `FlawCard` is no longer a dead export, and `FlawRow` + its now-unused imports are removed — so `npm run knip` passes at the integration gate (WARNING 6: this is the point where the FlawCard dead-export window closes; knip is validated here after 112-04, not between waves).
    - `cd frontend && npm test -- --run src/pages/library src/components/library` passes.
    - `grep -n "FlawRow\|MINI_BOARD_SIZE" frontend/src/pages/library/FlawsTab.tsx` returns nothing.
    - The grid container has `class` `grid grid-cols-1 lg:grid-cols-2 gap-4` and `data-testid="flaw-grid"`.
    - `CHANGELOG.md` `[Unreleased]` has the Flaws subtab bullet.
  </acceptance_criteria>
  <done>FlawsTab renders the responsive FlawCard grid; FlawRow gone; CHANGELOG updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /api/library/games/{game_id}` | modal fetch supplies a game_id; backend IDOR guard (T-112-01, plan 112-02) enforces ownership |
| server payload → modal render | full GameFlawCard rendered in the Dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Information Disclosure (IDOR) | `useLibraryGame` → `GET /games/{game_id}` | mitigate (upstream) | Object-level authorization enforced server-side in plan 112-02 (`get_library_game` returns None → 404 for non-owned ids); client only ever passes `flaw.game_id` from the user's own flaw list |
| T-112-08 | Denial of Service / errors | TanStack Query fetch failure | mitigate | `isError` → `LoadError` (no fall-through); errors auto-captured by the global QueryCache.onError (no duplicate Sentry call needed — CLAUDE.md) |
| T-112-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run`
- Full backend+frontend gate at wave merge: `uv run pytest -n auto -x` + `( cd frontend && npm run lint && npm test -- --run )`.
- Manual UAT (HUMAN, per 112-VALIDATION.md): (1) Flaws grid 2-up at lg, 1-up below 1024px; (2) View-game modal renders LibraryGameCard; (3) modal eval-chart tooltip not clipped inside the scrollable Dialog (Pitfall 2 — acceptable to clip on mobile only).
</verification>

<success_criteria>
- SC-1: flaws render as cards in `grid-cols-1 lg:grid-cols-2`, replacing the row list.
- SC-7: "View game" opens a modal showing the full `LibraryGameCard` fetched via `GET /api/library/games/{game_id}`.
- SC-8: data-testid/ARIA on card + button + modal; grid collapses to 1-up on mobile.
</success_criteria>

<output>
Create `.planning/phases/112-flaws-subtab-card-rework/112-04-SUMMARY.md` when done.
</output>
