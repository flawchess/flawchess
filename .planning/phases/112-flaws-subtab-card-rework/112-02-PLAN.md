---
phase: 112-flaws-subtab-card-rework
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - app/services/library_service.py
  - app/routers/library.py
  - tests/test_library_service.py
  - tests/test_library_router.py
autonomous: true
requirements: [SC-7]
must_haves:
  truths:
    - "GET /api/library/games/{game_id} returns a single GameFlawCard for a game owned by the authenticated user (200)"
    - "GET /api/library/games/{game_id} returns 404 for a game_id the authenticated user does not own (IDOR guard)"
    - "GET /api/library/games/{game_id} returns 404 for a non-existent game_id"
    - "The single-game endpoint reuses the existing _build_card builder (no duplicate card-building logic)"
  artifacts:
    - path: "app/services/library_service.py"
      provides: "get_library_game(session, user_id, game_id) -> GameFlawCard | None reusing _build_card; returns None for non-owned/missing game"
      contains: "get_library_game"
    - path: "app/routers/library.py"
      provides: "GET /games/{game_id} route returning GameFlawCard, 404 when service returns None"
      contains: "/games/{game_id}"
    - path: "tests/test_library_service.py"
      provides: "test_get_library_game (own game card; cross-user None IDOR guard; missing None)"
      contains: "get_library_game"
    - path: "tests/test_library_router.py"
      provides: "test_library_game_by_id (own game 200, cross-user 404, missing 404)"
      contains: "library_game_by_id"
  key_links:
    - from: "app/routers/library.py::get_library_game"
      to: "app/services/library_service.py::get_library_game"
      via: "user_id=user.id passed from current_active_user"
      pattern: "get_library_game"
    - from: "app/services/library_service.py::get_library_game"
      to: "app/services/library_service.py::_build_card"
      via: "scoped to one game_id"
      pattern: "_build_card"
---

<objective>
Add `GET /api/library/games/{game_id}` returning a single `GameFlawCard`, reusing the existing list card-builder scoped to one id, with a strict IDOR guard (404 — not 403 — for games the authenticated user does not own). This backs the Wave 3 "View game" modal.

Purpose: SC-7 (single-game endpoint for the modal). Headline security threat for the phase: IDOR / broken object-level authorization.
Output: `library_service.get_library_game`, the new router route, and IDOR + happy-path tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-flaws-subtab-card-rework/112-CONTEXT.md
@.planning/phases/112-flaws-subtab-card-rework/112-RESEARCH.md
@.planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (112-02) produces:
- New endpoint `GET /api/library/games/{game_id}` (response_model `GameFlawCard`).
- New service function `library_service.get_library_game(session, user_id, game_id) -> GameFlawCard | None`.
- New service test `tests/test_library_service.py::test_get_library_game` (own game card / cross-user None / missing None — IDOR guard at the service level).
- New router test `tests/test_library_router.py::test_library_game_by_id` (200 own / 404 cross-user / 404 missing).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_library_game service function (reuse _build_card, IDOR guard)</name>
  <read_first>
    - app/services/library_service.py (_build_card lines ~294-390; get_library_games loop ~393+ as structural analog; the three batch repo calls: fetch_page_analyzed_set, fetch_page_game_flaws, fetch_page_eval_positions)
    - app/repositories/library_repository.py (signatures of fetch_page_analyzed_set / fetch_page_game_flaws / fetch_page_eval_positions — confirm they accept a list of game_ids and return dicts keyed by game_id)
    - app/models/game.py (Game.user_id for the ownership check)
    - tests/test_library_service.py (existing service-test structure + two-user / analyzed-game seeding fixtures for the service-level IDOR assertion)
    - .planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md (§services/library_service.py — get_library_game template) and 112-RESEARCH.md (§"New GET /api/library/games/{game_id} endpoint")
  </read_first>
  <behavior>
    - get_library_game returns a GameFlawCard for a game whose user_id == the passed user_id.
    - Returns None when the game does not exist OR belongs to a different user (single None path → 404 at the router; do not distinguish missing vs not-owned).
    - Reuses _build_card with the same three batch queries as get_library_games, called with [game_id].
    - Queries run sequentially on the one AsyncSession (no asyncio.gather — CLAUDE.md).
  </behavior>
  <action>
    Add `async def get_library_game(session: AsyncSession, user_id: int, game_id: int) -> GameFlawCard | None` to `app/services/library_service.py`. Fetch the game (`session.get(Game, game_id)` or a `select(...).where(Game.id == game_id)`), and return `None` if the game is `None` or `game.user_id != user_id` (IDOR guard — T-112-01). Otherwise compute `is_analyzed = game_id in await library_repository.fetch_page_analyzed_set(session, user_id, [game_id])`, `flaw_rows = (await library_repository.fetch_page_game_flaws(session, user_id, [game_id])).get(game_id, [])`, and `positions` (only if analyzed) via `fetch_page_eval_positions(session, user_id, [game_id]).get(game_id, [])`; call `_build_card(game, flaw_rows, is_analyzed, positions)` and return it. Run all awaits sequentially. Add a docstring noting the IDOR guard and the no-gather constraint.
    Add `tests/test_library_service.py::test_get_library_game` (service-level, no router dependency): seed two users A and B, give A an analyzed game with at least one flaw, and assert (a) `get_library_game(session, user_id=A.id, game_id=A_game.id)` returns a `GameFlawCard` with the expected `game_id`; (b) `get_library_game(session, user_id=B.id, game_id=A_game.id)` returns `None` (cross-user IDOR guard asserted at the service level); (c) `get_library_game(session, user_id=A.id, game_id=<nonexistent>)` returns `None`. This test does NOT touch the router — it is Task 1's verify and must pass before Task 2's router test exists.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run pytest -n auto tests/test_library_service.py::test_get_library_game -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -n auto tests/test_library_service.py::test_get_library_game -x` passes (service-level, no router dependency).
    - The service test asserts the cross-user IDOR case: `get_library_game(user_id=B, game_id=A_game)` returns `None`.
    - `get_library_game` returns `None` when `game.user_id != user_id` and when the game is missing.
    - The function uses `_build_card` (no inline duplicate card construction).
    - No `asyncio.gather` on the session.
    - `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>Single-game card builder exists, user-scoped, reusing _build_card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /library/games/{game_id} route + IDOR + happy-path tests</name>
  <read_first>
    - app/routers/library.py (get_library_flaws route lines ~144-190 — exact structural pattern: prefix="/library", relative path, current_active_user dep, get_async_session dep; import block for schemas)
    - app/schemas/library.py (GameFlawCard — the response_model)
    - tests/test_library_router.py (existing auth fixtures, two-user setup pattern for cross-user assertions, analyzed-game seeding)
    - .planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md (§routers/library.py — route template)
  </read_first>
  <behavior>
    - Route is GET /games/{game_id} (relative path under the /library prefix — final path /api/library/games/{game_id}), response_model=GameFlawCard.
    - Returns 200 + a valid GameFlawCard for the authenticated owner's game.
    - Returns 404 (HTTPException, detail "Game not found") when the service returns None.
    - game_id path param is typed int (FastAPI auto-validates; non-int → 422).
  </behavior>
  <action>
    Add the route `@router.get("/games/{game_id}", response_model=GameFlawCard)` `async def get_library_game(game_id: int, session: Annotated[AsyncSession, Depends(get_async_session)], user: Annotated[User, Depends(current_active_user)]) -> GameFlawCard:` that calls `library_service.get_library_game(session, user_id=user.id, game_id=game_id)` and raises `HTTPException(status_code=404, detail="Game not found")` when the result is `None`, else returns it. Add `GameFlawCard` to the `app.schemas.library` import. Keep the router thin (no business logic). Add `tests/test_library_router.py::test_library_game_by_id` with three cases: (a) user A requests A's analyzed game → 200 and the JSON has the expected `game_id` plus eval/flaw fields; (b) user A requests user B's game id → 404 with detail "Game not found"; (c) user A requests a non-existent game id → 404. Use the existing two-user auth fixtures.
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/test_library_router.py::test_library_game_by_id -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -n auto tests/test_library_router.py::test_library_game_by_id -x` passes all three cases.
    - Cross-user request returns HTTP 404 (NOT 403, NOT another user's game body).
    - Route uses a relative path `/games/{game_id}` (no `/library` prefix duplicated in the decorator).
    - `grep -n "/library/games" app/routers/library.py` returns nothing (prefix convention respected).
    - `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>The single-game endpoint is wired, user-scoped, and the cross-user 404 IDOR test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /api/library/games/{game_id}` | authenticated user supplies an arbitrary integer game_id (untrusted, object reference) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Information Disclosure (IDOR / broken object-level authz) | `GET /games/{game_id}` → `get_library_game` | mitigate | Service checks `game.user_id == user_id` (user_id always from `current_active_user`); returns `None` → router raises 404 (not 403, to avoid confirming id existence). Test `test_library_game_by_id` asserts cross-user 404. |
| T-112-04 | Information Disclosure | GameFlawCard payload | accept | GameFlawCard never includes `*_hash` columns (existing Phase 109 design); ratings are public game data |
| T-112-05 | Tampering (input) | `game_id` path param | mitigate | Typed `int` — FastAPI rejects non-integer with 422 before service logic |
| T-112-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- `uv run pytest -n auto tests/test_library_service.py tests/test_library_router.py tests/test_library_repository.py -x`
- `uv run ruff format app/ tests/ && uv run ruff check app/ tests/ --fix && uv run ty check app/ tests/`
</verification>

<success_criteria>
- SC-7 (backend half): `GET /api/library/games/{game_id}` returns one `GameFlawCard`; IDOR cross-user request returns 404; reuses `_build_card`.
</success_criteria>

<output>
Create `.planning/phases/112-flaws-subtab-card-rework/112-02-SUMMARY.md` when done.
</output>
