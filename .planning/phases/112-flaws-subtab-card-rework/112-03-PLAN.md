---
phase: 112-flaws-subtab-card-rework
plan: 03
type: execute
wave: 3
depends_on: ["112-01"]
# Integration note (WARNING 6): 112-03 (FlawCard creation) and 112-04 (FlawsTab grid swap
# that consumes FlawCard) land together as ONE integration unit. The wave split is for
# execution ordering only — knip runs at the full-gate squash-merge to main (CLAUDE.md),
# after 112-04, so FlawCard is never a standalone dead export at any merge point.
# Do NOT add a knip-ignore comment.
files_modified:
  - frontend/src/types/library.ts
  - frontend/src/lib/openingInsights.ts
  - frontend/src/lib/__tests__/openingInsights.test.ts
  - frontend/src/lib/formatFlawEval.ts
  - frontend/src/lib/__tests__/formatFlawEval.test.ts
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/components/library/__tests__/FlawCard.test.tsx
autonomous: true
requirements: [SC-1, SC-3, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "FlawCard renders a banded CardHeader with ■ White (rating) vs □ Black (rating) (single line desktop, two stacked lines mobile) + exact-ply platform link"
    - "FlawCard renders a 132px miniboard (board before the flaw) with the flaw-move arrow"
    - "The flawed move shows in standard notation (white N. / black N...) with the eval swing (before → after) on the same line, plus the severity badge"
    - "Eval swing is user-POV: eval_cp/eval_mate negated when user_color === 'black'; mate formatted as #±N, cp as signed 1-decimal pawns"
    - "Tag chips use family-colored TagChip + a single TagLegend (Explanation) tooltip"
    - "Within-card layout: 132px board left + content stack right (move+swing+severity → chips → Explanation → metadata)"
  artifacts:
    - path: "frontend/src/lib/openingInsights.ts"
      provides: "formatMoveNotation(plyIndex, san) shared notation primitive (white N. / black N...); formatCandidateMove refactored to delegate to it (D-04 — shared helper, no duplication)"
      contains: "formatMoveNotation"
    - path: "frontend/src/lib/formatFlawEval.ts"
      provides: "formatFlawEval(eval_cp_before, eval_mate_before, eval_cp_after, eval_mate_after, user_color) -> swing string; user-POV negation + mate/cp formatting"
      contains: "formatFlawEval"
    - path: "frontend/src/components/library/FlawCard.tsx"
      provides: "FlawCard component (sibling to LibraryGameCard) — header, 132px board, move+swing line, severity badge, tag chips, TagLegend, metadata"
      contains: "flaw-card-"
    - path: "frontend/src/lib/__tests__/formatFlawEval.test.ts"
      provides: "user-POV negation + mate-sign tests (SC-6)"
      contains: "formatFlawEval"
    - path: "frontend/src/components/library/__tests__/FlawCard.test.tsx"
      provides: "header/board/move-notation render tests (SC-4, SC-5)"
      contains: "FlawCard"
  key_links:
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "frontend/src/lib/formatFlawEval.ts"
      via: "import + render of the eval swing"
      pattern: "formatFlawEval"
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "frontend/src/types/library.ts FlawListItem"
      via: "props typed against the extended payload"
      pattern: "FlawListItem"
---

<objective>
Create the new `FlawCard` component (sibling to `LibraryGameCard`) and a shared `formatFlawEval` utility, replicating the Games-card visual language: banded header with names + ratings + exact-ply platform link, 132px miniboard with the flaw arrow, standard-notation move + user-POV eval swing on one line, severity badge, family-colored tag chips with a single `TagLegend`, and the metadata block. The "View game" button and modal are added in 112-04; this plan focuses on the card surface and the eval formatter (Pitfall 3 — eval POV + mate sign).

Purpose: SC-1 (card surface for the grid), SC-3 (132px board), SC-4 (move + eval swing line), SC-5 (standard notation), SC-6 (user-POV eval negation).
Output: `formatFlawEval.ts` + tests, `FlawCard.tsx` + tests, extended `FlawListItem` TS type.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-flaws-subtab-card-rework/112-CONTEXT.md
@.planning/phases/112-flaws-subtab-card-rework/112-RESEARCH.md
@.planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md
@.planning/phases/112-flaws-subtab-card-rework/112-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan (112-03) produces:
- `frontend/src/lib/openingInsights.ts` — new exported `formatMoveNotation(plyIndex, san)` primitive (white `N.`, black `N...`); existing `formatCandidateMove` refactored to delegate to it (no behavior change). FlawCard imports `formatMoveNotation` directly — no inline notation helper (D-04).
- `frontend/src/lib/formatFlawEval.ts` — `formatFlawEval(...)` swing formatter (user-POV negation, mate `#±N`, cp signed 1-decimal via `formatSignedEvalPawns`).
- `frontend/src/components/library/FlawCard.tsx` — new component (header, 132px board, move+swing line, SeverityBadge, TagChip row, TagLegend, metadata). No modal yet (added in 112-04).
- Extended `FlawListItem` TS type in `frontend/src/types/library.ts` (ratings + before/after eval; `es_before`/`es_after` removed).
- Tests: `formatFlawEval.test.ts`, `FlawCard.test.tsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FlawListItem TS type + formatFlawEval utility (user-POV + mate sign)</name>
  <read_first>
    - frontend/src/types/library.ts (FlawListItem type — mirror the backend schema change from 112-01: add white_rating/black_rating + eval_cp_before/eval_mate_before/eval_cp_after/eval_mate_after as number | null; keep move_san string | null; remove es_before/es_after)
    - frontend/src/components/library/EvalChart.tsx (formatEval lines 303-315 — white-POV reference; formatMoveLabel lines 296-301)
    - frontend/src/lib/clockFormat.ts (formatSignedEvalPawns line ~41 — signed 1-decimal pawns)
    - frontend/src/lib/openingInsights.ts (formatCandidateMove lines 14-22 — existing notation logic to extract: `isWhitePly = plyIndex % 2 === 0`; `moveNumber = Math.floor(plyIndex / 2) + 1`; returns `${moveNumber}.${san}` or `${moveNumber}...${san}`)
    - frontend/src/lib/__tests__/openingInsights.test.ts (existing formatCandidateMove test, if present, that must still pass after the refactor)
    - .planning/phases/112-flaws-subtab-card-rework/112-UI-SPEC.md (§"Move Notation + Eval Swing Line Contract")
    - .planning/phases/112-flaws-subtab-card-rework/112-RESEARCH.md (Code Examples Pattern 2 + Pitfall 3)
  </read_first>
  <behavior>
    - Test (SC-6): for user_color='black', eval_cp=-300 → before/after part renders "+3.0" (negated), NOT "-3.0".
    - Test: for user_color='white', eval_cp=470 → "+4.7"; eval_cp=-120 → "-1.2".
    - Test: mate sign — user_color='black', eval_mate=3 (white mates) → "#-3" (mate against the user).
    - Test: null eval → "—".
    - Test: full swing string joins before and after with " → " (e.g. "+4.7 → #-3").
    - Test (formatMoveNotation parity, SC-5): even plyIndex (white) → `${N}.${san}` (e.g. plyIndex=2 → "2.Nxd4"); odd plyIndex (black) → `${N}...${san}` (e.g. plyIndex=3 → "2...c5").
    - Test (delegation parity): `formatCandidateMove(entrySanSequence, san)` returns the same string as `formatMoveNotation(entrySanSequence.length, san)` for representative sequences (no behavior change from the refactor).
  </behavior>
  <action>
    In `frontend/src/types/library.ts`, update the `FlawListItem` type to match the 112-01 backend payload: add `white_rating: number | null`, `black_rating: number | null`, `eval_cp_before: number | null`, `eval_mate_before: number | null`, `eval_cp_after: number | null`, `eval_mate_after: number | null`; keep `move_san: string | null`; remove `es_before`/`es_after`.
    In `frontend/src/lib/openingInsights.ts`, extract the move-notation primitive (D-04 — shared helper, no duplication): add and export `formatMoveNotation(plyIndex: number, san: string): string` containing the exact existing logic (`isWhitePly = plyIndex % 2 === 0`; `moveNumber = Math.floor(plyIndex / 2) + 1`; return `${moveNumber}.${san}` for white plys, `${moveNumber}...${san}` for black). Then refactor the existing `formatCandidateMove(entrySanSequence, candidateMoveSan)` to delegate: `return formatMoveNotation(entrySanSequence.length, candidateMoveSan)` (no behavior change — it already derives the ply index from `entrySanSequence.length`). Do NOT create a separate inline `formatFlawMove`; FlawCard (Task 2) will import `formatMoveNotation` directly. Extend `frontend/src/lib/__tests__/openingInsights.test.ts` with a `formatMoveNotation` unit test asserting white even-ply → `N.` and black odd-ply → `N...`, plus a delegation-parity assertion that `formatCandidateMove` matches `formatMoveNotation(seq.length, san)`; ensure the existing `formatCandidateMove` test still passes.
    Create `frontend/src/lib/formatFlawEval.ts` exporting `formatFlawEval(evalCpBefore, evalMateBefore, evalCpAfter, evalMateAfter, userColor)` returning the swing string. Internally: a `applyUserPov` step that negates both cp and mate when `userColor === 'black'`; a `formatFlawEvalPart(cp, mate)` that returns `#${mate}` when mate is non-null, else `formatSignedEvalPawns(cp / 100)` when cp is non-null, else `'—'`; join the two parts with `' → '`. Reuse `formatSignedEvalPawns` from `clockFormat.ts` (do NOT reuse the ES inverse-sigmoid in tagDefinitions.ts — D-06). Mark a one-line bug-fix comment at the negation site explaining the white-POV → user-POV flip (Pitfall 3).
    Create `frontend/src/lib/__tests__/formatFlawEval.test.ts` covering the behavior cases above.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/formatFlawEval.test.ts src/lib/__tests__/openingInsights.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/lib/__tests__/formatFlawEval.test.ts` passes.
    - Black-POV negation case asserts `eval_cp=-300, user_color='black'` → contains `"+3.0"`.
    - Mate-against-user case asserts `eval_mate=3, user_color='black'` → contains `"#-3"`.
    - `frontend/src/types/library.ts` FlawListItem has the six new fields and no `es_before`/`es_after`.
    - `formatMoveNotation` is exported from `frontend/src/lib/openingInsights.ts`, and `formatCandidateMove` delegates to it (`grep -n "formatMoveNotation" frontend/src/lib/openingInsights.ts` shows both the export and the call inside `formatCandidateMove`); no duplicated `isWhitePly`/`moveNumber` logic exists outside `formatMoveNotation`.
    - The existing `formatCandidateMove` test still passes; a `formatMoveNotation` parity test asserts white even-ply → `N.` and black odd-ply → `N...`.
    - `cd frontend && npx tsc --noEmit` (or `npm run lint`) clean for the new files.
  </acceptance_criteria>
  <done>Eval swing formatter handles user-POV + mate sign; FlawListItem TS type matches the backend payload; the shared `formatMoveNotation` primitive is extracted and `formatCandidateMove` delegates to it (D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FlawCard component (header, 132px board, move+swing line, chips, metadata)</name>
  <read_first>
    - frontend/src/components/results/LibraryGameCard.tsx (imports 1-24; DESKTOP_BOARD_SIZE=132 line 34; CardHeader pattern 280-293; platform-link pattern 253-272; RESULT_ICONS/BORDER_COLORS 73-83 + 236-248; metadata 305-350; formatDate 98-109 + formatTimeControl 114-131 helpers; accentColor + overflowVisible Card usage)
    - frontend/src/pages/library/FlawsTab.tsx (FlawRow lines 48-141 — the component being replaced; move-arrow computation lines 57-60 via sanToSquares)
    - frontend/src/components/library/SeverityBadge.tsx, frontend/src/components/library/TagChip.tsx (TagChip + TagLegend props; definition flag)
    - frontend/src/components/board/LazyMiniBoard.tsx (size + arrows props)
    - frontend/src/lib/platformLinks.ts (flawPlyUrl signature — exact-ply deep link, D-12)
    - frontend/src/lib/theme.ts (SEV_BLUNDER/SEV_MISTAKE/SEV_INACCURACY, WDL_BORDER_* constants)
    - frontend/src/lib/openingInsights.ts (formatMoveNotation — the shared notation primitive extracted in Task 1; FlawCard imports and calls `formatMoveNotation(flaw.ply, flaw.move_san)`)
    - .planning/phases/112-flaws-subtab-card-rework/112-UI-SPEC.md (FlawCard Layout/CardHeader/Metadata/data-testid contracts)
    - .planning/phases/112-flaws-subtab-card-rework/112-PATTERNS.md (§FlawCard.tsx full pattern map)
  </read_first>
  <behavior>
    - Test (SC-4): renders CardHeader containing white + black usernames and ratings (parenthesized) and a 132px board (assert size prop / arrow present when move_san set).
    - Test (SC-5): move notation — ply=2 (white) → "2.Nxd4"; ply=3 (black) → "2...c5" (kernel parity: even ply = white).
    - Test: rating omitted (null) renders no empty parentheses.
    - Test: data-testid `flaw-card-{game_id}-{ply}` on the root <article>; platform link testid present.
  </behavior>
  <action>
    Create `frontend/src/components/library/FlawCard.tsx` exporting `FlawCard({ flaw }: { flaw: FlawListItem })`. Copy `DESKTOP_BOARD_SIZE = 132`, `formatDate`, `formatTimeControl`, `RESULT_ICONS`, and the result-indicator markup from `LibraryGameCard` (the helpers may be copied verbatim — do not fold logic into LibraryGameCard, D-05 standalone). Render: `<Card as="article" accentColor={severityColor} overflowVisible data-testid={`flaw-card-${flaw.game_id}-${flaw.ply}`}>` where `severityColor` maps `flaw.severity` → SEV_BLUNDER/SEV_MISTAKE/SEV_INACCURACY; a `CardHeader` (as="h4", size="compact", rounded-t-md) with the desktop single-line + mobile two-line names+ratings pattern (ratings parenthesized, omitted when null) and an `ml-auto` platform group using `PlatformIcon` + the `flawPlyUrl(flaw.platform, flaw.platform_url, flaw.ply, flaw.user_color)` external link (Tooltip "Open at this move on platform", `aria-label`, `data-testid={`flaw-card-platform-link-${flaw.game_id}-${flaw.ply}`}`, target=_blank rel=noopener); a body `div.flex.gap-3.items-start.p-3` with a 132px `LazyMiniBoard` (fen=flaw.fen, flipped when user_color==='black', arrows from `sanToSquares(`${flaw.fen} ${sideToMove} - - 0 1`, flaw.move_san)` colored SEV_BLUNDER) and a content `div.flex.flex-col.gap-1.5.min-w-0.flex-1` containing, in order: the move+swing line (`formatMoveNotation(flaw.ply, flaw.move_san)` imported from `@/lib/openingInsights` in text-foreground font-medium + a muted `formatFlawEval(...)` span, two spaces between; when `flaw.move_san` is null fall back to `Ply ${flaw.ply}`), `SeverityBadge`, a `flex flex-wrap gap-1.5` TagChip row (definition={false}) over `flaw.tags`, the `TagLegend`, and the metadata block (date · TC · termination + result indicator) following the desktop flex-wrap / mobile flex-col split from the UI-SPEC. Do NOT define a local move-notation helper — import and call `formatMoveNotation` from `@/lib/openingInsights` (D-04, the shared primitive from Task 1). Reserve a placeholder spot at the bottom of the content stack for the "View game" button (added in 112-04) — do not add the button or modal here. Use only theme.ts color constants; text-sm floor (tooltip bodies may stay text-xs). Apply both desktop and mobile renderers.
    Create `frontend/src/components/library/__tests__/FlawCard.test.tsx` covering the behavior cases above (render header names/ratings, board size + arrow, move notation parity for white/black plies, root + platform testids).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/library/__tests__/FlawCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/components/library/__tests__/FlawCard.test.tsx` passes.
    - Move-notation test: ply=2 → "2.Nxd4", ply=3 → "2...c5".
    - Root `<article>` carries `data-testid="flaw-card-{game_id}-{ply}"`; platform link carries its testid + `aria-label`.
    - Board renders at size 132 with an arrow when `move_san` is set.
    - No hard-coded semantic colors (all from theme.ts); no `text-xs` outside tooltip bodies.
    - `cd frontend && npm run lint` clean for the FlawCard files. FlawCard is consumed when FlawsTab imports it in 112-04; 112-03 and 112-04 land together as a single integration unit (see frontmatter note), and knip is validated at the full-gate squash-merge after 112-04 — there is no separate per-wave merge where a dead-export window exists. Do NOT add a knip-ignore comment.
  </acceptance_criteria>
  <done>FlawCard renders the full card surface (minus the View-game button/modal) matching the Games-card visual language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server payload → FlawCard render | flaw payload (incl. opponent usernames, ratings) rendered into DOM; external platform link target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-06 | Tampering / Info Disclosure | external platform `<a>` link | mitigate | `target="_blank"` always paired with `rel="noopener noreferrer"` (reverse-tabnabbing guard); href from server-provided `platform_url` only |
| T-112-07 | Info Disclosure | rendered eval / ratings | accept | Ratings + eval are user's own game data; no internal hash exposed in the TS type |
| T-112-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/lib/__tests__/formatFlawEval.test.ts src/components/library/__tests__/FlawCard.test.tsx`
- `cd frontend && npm run lint`
- Note: 112-03 and 112-04 land together as one integration unit; knip is run only at the full-gate squash-merge to main (CLAUDE.md), after 112-04 wires FlawCard into FlawsTab. There is no inter-wave merge, so no standalone dead-export window exists. Do NOT add a knip-ignore comment.
</verification>

<success_criteria>
- SC-1/SC-3: card surface with 132px board exists (grid wiring in 112-04).
- SC-4: move + eval swing on one line + severity badge.
- SC-5: standard-notation move.
- SC-6: eval swing user-POV negated for black, mate-sign correct.
</success_criteria>

<output>
Create `.planning/phases/112-flaws-subtab-card-rework/112-03-SUMMARY.md` when done.
</output>
