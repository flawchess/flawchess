---
phase: 141-jsonb-schema-gate-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/game_flaw.py
  - alembic/versions/
  - tests/test_game_flaws_model.py
autonomous: true
requirements: [STORE-01, STORE-02]
must_haves:
  truths:
    - "allowed_pv_lines and missed_pv_lines JSONB columns exist on game_flaws after `alembic upgrade head`"
    - "A representative stats-style select(GameFlaw) does NOT load or emit the two blob columns"
    - "An explicit undefer() round-trip CAN load the blob columns (Phase 143 opt-in path works)"
    - "All 5 existing select(GameFlaw) sites are confirmed not to implicitly touch the blob attrs"
  artifacts:
    - "app/models/game_flaw.py — two new deferred JSONB Mapped columns"
    - "alembic/versions/<new>.py — nullable add_column migration, down_revision c4d4588ed2b8"
    - "tests/test_game_flaws_model.py — TestDeferredBlobLeak regression class"
  key_links:
    - "deferred=True on both columns is the structural leak guard (D-02) — the ONLY thing keeping the blobs out of stats scans"
    - "Migration down_revision must chain off the current head c4d4588ed2b8"
---

<objective>
Add the storage substrate for the v1.30 Forcing-Line Tactic Gate: two nullable JSONB
columns (`allowed_pv_lines`, `missed_pv_lines`) on `game_flaws`, declared `deferred=True`
so they never load on existing `select(GameFlaw)` stats scans, plus the Alembic migration
and a regression test proving the leak guard holds (and that the Phase 143 `undefer()`
opt-in still works).

Purpose: STORE-01 (columns exist via one migration) and STORE-02 (zero stats regression —
blobs never fetched by existing scans).

Output: model columns, one migration revision, and a deferred-leak regression test class.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-jsonb-schema-gate-logic/141-CONTEXT.md
@.planning/phases/141-jsonb-schema-gate-logic/141-PATTERNS.md
@app/models/game_flaw.py
@app/models/llm_log.py
@tests/test_game_flaws_model.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- ORM column `GameFlaw.allowed_pv_lines: Mapped[list[Any] | None]` (JSONB, nullable, deferred)
- ORM column `GameFlaw.missed_pv_lines: Mapped[list[Any] | None]` (JSONB, nullable, deferred)
- Alembic migration revision `<new_id>` (down_revision `c4d4588ed2b8`), `upgrade()` adds both
  nullable JSONB columns, `downgrade()` drops them in reverse order
- Test class `TestDeferredBlobLeak` in `tests/test_game_flaws_model.py` (deferred-unloaded
  assertion + undefer round-trip assertion)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add two deferred JSONB columns to GameFlaw</name>
  <files>app/models/game_flaw.py</files>
  <read_first>
    - app/models/game_flaw.py (the file being modified — existing allowed_*/missed_* tactic block, lines 68-94)
    - app/models/llm_log.py (JSONB analog per D-06 — lines 13-15 imports, line 60 nullable JSONB column)
    - .planning/phases/141-jsonb-schema-gate-logic/141-PATTERNS.md (the JSONB + deferred=True pattern assignment for game_flaw.py)
  </read_first>
  <action>
    Add `allowed_pv_lines` and `missed_pv_lines` to GameFlaw, mirroring app/models/llm_log.py
    exactly per D-06: `from sqlalchemy.dialects.postgresql import JSONB`, typed
    `Mapped[list[Any] | None]` (D-05 blob is a LIST of per-node dicts, not a dict — so use
    `list[Any]`, NOT `dict`), `mapped_column(JSONB, nullable=True, deferred=True)`. Add
    `from typing import Any` (the file currently imports only Optional). Do NOT use
    MutableList/MutableDict and do NOT add any manual asyncpg codec setup (auto-registered) —
    these are write-once blobs (D-06).

    Place the two declarations immediately after the existing `missed_tactic_*` block (after
    line 94), with a `#` comment family block matching the existing tactic-comment density
    (lines 68-86). The comment must cite D-05 and document the per-node blob shape keys
    `b` (best_cp|null), `bm` (best_mate|null), `s` (second_cp|null), `sm` (second_mate|null),
    `su` (second-best uci string) and state white-perspective cp (matching the existing
    eval_cp convention; the gate converts to side-to-move at read time). Mirror the existing
    orientation split: `allowed_pv_lines` = refutation line (flaw_ply+1 PV); `missed_pv_lines`
    = best-move line (flaw_ply PV).

    `deferred=True` is a NEW pattern for this codebase (zero existing usage in app/ per
    PATTERNS.md) — it is the D-02 structural leak guard; apply it deliberately on BOTH columns.
  </action>
  <verify>
    <automated>uv run ty check app/models/game_flaw.py</automated>
    Also: `uv run ruff check app/models/game_flaw.py` is clean.
  </verify>
  <acceptance_criteria>
    - Both columns declared `Mapped[list[Any] | None]` with `JSONB, nullable=True, deferred=True`
    - `from typing import Any` and `from sqlalchemy.dialects.postgresql import JSONB` imported
    - No MutableList/MutableDict, no manual codec
    - Comment block cites D-05 and lists keys b/bm/s/sm/su with white-perspective note
    - ty and ruff clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generate the Alembic migration for the two columns</name>
  <files>alembic/versions/</files>
  <read_first>
    - alembic/versions/20260619_134442_9be5294cfe3c_add_tactic_depth_to_game_flaws.py (exact analog — nullable add_column to game_flaws)
    - .planning/phases/141-jsonb-schema-gate-logic/141-PATTERNS.md (migration boilerplate + confirmed head c4d4588ed2b8)
  </read_first>
  <action>
    Ensure the dev DB is up first: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`.
    Then run `uv run alembic revision --autogenerate -m "add pv_lines blobs to game_flaws"`
    (CLAUDE.md command). Autogenerate fills the revision id and chains down_revision off the
    current head `c4d4588ed2b8` automatically. Confirm the generated `upgrade()` calls
    `op.add_column("game_flaws", sa.Column("allowed_pv_lines", postgresql.JSONB(...), nullable=True))`
    and the same for `missed_pv_lines`, and that `downgrade()` drops them in reverse order
    (missed first, then allowed). Add `from sqlalchemy.dialects import postgresql` to the
    migration imports if autogenerate references `postgresql.JSONB`.

    Both columns are nullable with NO data backfill (STORE-01) — nullable JSONB adds are a
    metadata-only change, no table rewrite, safe on the populated game_flaws table. Remove any
    spurious unrelated autogenerate diffs (drift from other models) before saving — keep the
    migration to exactly these two add_column / two drop_column operations.

    Apply it: `uv run alembic upgrade head`.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic check</automated>
    `alembic check` must report no new pending operations (model now matches DB). Round-trip
    safety: `uv run alembic downgrade -1 && uv run alembic upgrade head` both succeed.
  </verify>
  <acceptance_criteria>
    - Exactly one new revision file with down_revision c4d4588ed2b8
    - upgrade() adds both nullable JSONB columns; downgrade() drops both (reverse order)
    - `alembic upgrade head` succeeds and `alembic check` shows no drift
    - downgrade/upgrade round-trip is clean
    - No unrelated model diffs included in the migration
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Audit the 5 select(GameFlaw) sites and add the deferred-leak regression test</name>
  <files>tests/test_game_flaws_model.py</files>
  <read_first>
    - tests/test_game_flaws_model.py (the file being modified — _create_test_users fixture, _seed_game, _make_flaw_row helpers, lines 31-85)
    - app/repositories/library_repository.py (the 5 audit sites — confirm at/near lines 737, 1017, 1118, 1151, 2255; line numbers may have drifted, grep `select(GameFlaw` to locate)
    - .planning/phases/141-jsonb-schema-gate-logic/141-PATTERNS.md (D-02a/b/c audit table + the two assertion strategies)
  </read_first>
  <action>
    AUDIT (D-02a, confirm-only — do NOT rewrite per D-02b): grep `select(GameFlaw` in
    app/repositories/library_repository.py and confirm each of the 5 sites does not implicitly
    read `allowed_pv_lines`/`missed_pv_lines`. PATTERNS.md assessment to verify: site ~737 is
    column-projected `select(GameFlaw.ply)` (safe); sites ~1017/1118/1151/2255 select the full
    entity but downstream consumers read only severity/tempo/phase/is_*/fen/*_tactic_* and the
    joined columns — confirm none touch the new blob attrs. `deferred=True` makes them
    structurally safe (an implicit deferred access under async raises MissingGreenlet — a
    fail-loud signal); the audit just confirms no site trips it today. Record the audit result
    in the SUMMARY (per-site one-liner). No production code changes expected — STORE-02 is
    satisfied by `deferred=True` + this audit, NOT a literal projection rewrite (D-02b).

    REGRESSION TEST (D-02c): add a `class TestDeferredBlobLeak` to tests/test_game_flaws_model.py
    reusing the existing _create_test_users autouse fixture, _seed_game, and _make_flaw_row
    helpers. Two assertions:
    (1) Unloaded-attribute proof: seed one flaw row, run a representative stats-style
        `select(GameFlaw).where(...)`, `scalar_one()`, then via `from sqlalchemy import inspect
        as sa_inspect` assert both blob attr names are present in `sa_inspect(flaw).unloaded`.
        Use the inspection API — do NOT touch the attribute directly (implicit async deferred
        access raises MissingGreenlet).
    (2) undefer round-trip proof (the Phase 143 opt-in path): run
        `select(GameFlaw).options(undefer(GameFlaw.allowed_pv_lines), undefer(GameFlaw.missed_pv_lines))`
        and assert the attrs load without error (value None on a freshly seeded row is fine).
    Optionally also add a compiled-SQL check (no session): assert the blob column names do not
    appear in `str(select(GameFlaw).compile(...))` — an established pattern in test_query_utils.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_game_flaws_model.py -x</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records a per-site confirmation for all 5 select(GameFlaw) sites (no leak)
    - TestDeferredBlobLeak asserts both blob attrs are in inspect(flaw).unloaded after a stats select
    - TestDeferredBlobLeak asserts undefer() loads both attrs (Phase 143 path works)
    - No production repository code changed (D-02b)
    - Full file test passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine/re-tagger (future) → game_flaws JSONB | Write-once blobs written by a future Phase 142 pass; in THIS phase the columns are created empty (no writer). No external/user input crosses any boundary in Plan 01. |
| game_flaws JSONB → API responses | The blobs are Python-read-only and must never be echoed to API responses (CLAUDE.md "API responses never expose internal hashes"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-01 | Information Disclosure | allowed_pv_lines / missed_pv_lines leaking into stats scans or API payloads | low | mitigate | `deferred=True` on both columns (D-02) keeps them out of every `select(GameFlaw)`; the 5-site audit + TestDeferredBlobLeak regression test enforce it. No router/schema in this phase serializes GameFlaw blobs. |
| T-141-02 | Tampering | Migration corrupting populated game_flaws on upgrade | low | mitigate | Nullable JSONB adds are metadata-only (no table rewrite, no backfill); downgrade/upgrade round-trip verified before merge. |
| T-141-03 | Denial of Service | Large JSONB bloating stats-scan I/O | low | accept | Postgres TOASTs large JSONB out-of-line; `deferred=True` means stats scans never read the column. No mitigation needed beyond the existing deferral. |

No new third-party packages are installed in this plan (no npm/pip/cargo install), so no
supply-chain (T-141-SC) checkpoint is required.
</threat_model>

<verification>
- `uv run alembic upgrade head` then `uv run alembic check` clean (columns exist, no drift)
- `uv run pytest tests/test_game_flaws_model.py -x` passes (round-trip + deferred-leak)
- `uv run ty check app/ tests/` zero errors; `uv run ruff check .` clean
- Audit of all 5 select(GameFlaw) sites recorded in SUMMARY with no leak
</verification>

<success_criteria>
- allowed_pv_lines / missed_pv_lines JSONB columns exist on game_flaws via an Alembic migration
- Existing mistake-stats / flaw-comparison / benchmark-delta queries show zero regression
  (blobs never fetched — proven by deferred-unloaded test + 5-site audit)
- Every select(GameFlaw) site confirmed not to fetch the blobs (STORE-02 via deferred + audit, D-02b)
</success_criteria>

<output>
Create `.planning/phases/141-jsonb-schema-gate-logic/141-01-SUMMARY.md` when done.
Include the per-site audit result for all 5 select(GameFlaw) sites.
</output>
