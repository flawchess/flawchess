---
phase: 127-detector-hardening-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/select_tagger_fixtures.py
  - tests/scripts/tagger/__init__.py
  - tests/scripts/tagger/conftest.py
  - tests/scripts/tagger/motif_theme_map.py
  - fixtures/tagger/detector_fixture.csv
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: [SC#2, SC#4, SC#5]
must_haves:
  truths:
    - "A re-runnable selector reads the full CC0 lichess puzzle download and emits a committed stratified fixture (N per motif-theme per Rating band)"
    - "The committed fixture has columns PuzzleId, FEN, FirstMove, PV, Themes, Rating and contains the board-after-flaw FEN (first Moves entry already applied)"
    - "An explicit motif->lichess-theme map exists, plus an explicit list of motifs with no lichess equivalent marked unvalidated"
    - "tests/scripts/tagger/ exists AND pyproject addopts ignores it, landing together (D-14)"
    - "CI runs the tagger harness as a dedicated step with an explicit path (uv run pytest tests/scripts/tagger), overriding the ignore"
    - "The harness docstring records the CC0-data / AGPL-cook.py boundary (SC#4)"
  artifacts:
    - path: "scripts/select_tagger_fixtures.py"
      provides: "one-time re-runnable stratified puzzle selector"
    - path: "fixtures/tagger/detector_fixture.csv"
      provides: "committed stratified CC0 puzzle sample"
    - path: "tests/scripts/tagger/motif_theme_map.py"
      provides: "motif->theme map + unvalidated-motif list (D-10)"
      contains: "self-interference"
    - path: "tests/scripts/tagger/__init__.py"
      provides: "tagger test package marker"
    - path: "tests/scripts/tagger/conftest.py"
      provides: "fixture CSV loader"
  key_links:
    - from: "tests/scripts/tagger/conftest.py"
      to: "fixtures/tagger/detector_fixture.csv"
      via: "loads the committed fixture CSV"
      pattern: "detector_fixture"
    - from: "pyproject.toml"
      to: "tests/scripts/tagger"
      via: "addopts --ignore=tests/scripts/tagger"
      pattern: "ignore=tests/scripts/tagger"
    - from: ".github/workflows/ci.yml"
      to: "tests/scripts/tagger"
      via: "dedicated CI step with explicit path"
      pattern: "pytest tests/scripts/tagger"
---

<objective>
Build the validation harness infrastructure (no measurement yet): the one-time stratified puzzle selector, the committed fixture CSV, the motif->theme map, the excluded `tests/scripts/tagger/` package, the `pyproject.toml` ignore landing with the dir, and the dedicated CI step. The actual precision/recall measurement and floor-setting is Plan 03 (which depends on both this plan and the fixed detector in Plan 01) — per D-09 the floor cannot be hardcoded before measurement.

Purpose: SC#2 (read-only puzzle harness mapping motifs to lichess themes, listing unvalidated motifs), SC#4 (CC0 data only, no AGPL cook.py — recorded in the docstring), SC#5 (independent ground truth replacing self-labeled fixtures). D-07 (committed stratified fixture, re-runnable selector), D-10 (multi-label theme map), D-14 (excluded dir + ignore land together; explicit-path CI step).

Output: the offline harness scaffold + fixture that Plan 03 runs against the corrected detector.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-detector-hardening-validation/127-CONTEXT.md
@.planning/phases/127-detector-hardening-validation/127-RESEARCH.md
@.planning/notes/tactic-detector-precision-gaps.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- **Selector** `scripts/select_tagger_fixtures.py` — reads the full lichess CC0 puzzle CSV (zstd, via the existing `zstandard` dev dep, mirroring `scripts/select_benchmark_users.py`), splits `Moves` into `first_move=Moves[0]` and `pv=" ".join(Moves[1:])`, applies `first_move` to the FEN to produce `board_after_flaw`, filters to puzzles whose themes intersect the motif->theme map, and stratified-samples N per (our_motif, Rating band) into the fixture. Re-runnable.
- **Fixture** `fixtures/tagger/detector_fixture.csv` — committed columns `PuzzleId, FEN, FirstMove, PV, Themes, Rating` where `FEN` is the board-after-first-move (board-after-flaw).
- **Map module** `tests/scripts/tagger/motif_theme_map.py` — `MOTIF_TO_THEMES: dict[str, tuple[str, ...]]` (multi-label, D-10) and `UNVALIDATED_MOTIFS: frozenset[str]` (motifs with no lichess equivalent: at minimum `self-interference`, and `double-bishop-mate` pending data verification).
- **Excluded package** `tests/scripts/tagger/__init__.py` + `conftest.py` (offline fixture loader; no DB skip fixture — harness reads only the CSV).
- **Constants** sample size N per stratum and the minimum-floor-per-stratum (collapse Rating bands below it) + Rating-band bounds — extracted, no magic numbers.
- **pyproject.toml** `addopts` gains `--ignore=tests/scripts/tagger` alongside the existing benchmark ignore.
- **CI step** in `.github/workflows/ci.yml` — `uv run pytest tests/scripts/tagger -v` after the default pytest step.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: motif->theme map + excluded tagger package + pyproject ignore (land together)</name>
  <files>tests/scripts/tagger/__init__.py, tests/scripts/tagger/conftest.py, tests/scripts/tagger/motif_theme_map.py, pyproject.toml</files>
  <read_first>
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"Motif -> Lichess Theme Map" (the full table), §"Harness Architecture" (directory structure, conftest), §"pyproject.toml Change", and Open Questions 1-2 (hanging-piece / double-bishop-mate spelling caveats).
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-10, D-14.
    - pyproject.toml lines ~35-42 — current `addopts = "--ignore=tests/scripts/benchmarks"` and the comment above it.
    - tests/scripts/benchmarks/conftest.py — the directory-exclusion precedent to mirror (but the tagger conftest needs NO DB skip fixture — it is offline).
    - app/services/tactic_detector.py — the `TacticMotif` string values / registries so the map keys match the actual motif strings (fork, pin, skewer, discovered-attack, double-check, back-rank-mate, mate, the named mates, the tier-3 set, hanging-piece).
  </read_first>
  <action>
    Create `tests/scripts/tagger/__init__.py` (empty package marker) and `tests/scripts/tagger/conftest.py` with an offline fixture-loading helper that reads `fixtures/tagger/detector_fixture.csv` via stdlib `csv` and yields parsed rows (PuzzleId, FEN, FirstMove, PV, Themes split on space, Rating as int); no DB connection, no asyncio.

    Create `tests/scripts/tagger/motif_theme_map.py` exposing `MOTIF_TO_THEMES: dict[str, tuple[str, ...]]` built from the RESEARCH map table (multi-label per D-10 — e.g. mate maps to mate plus the mateInN variants), and `UNVALIDATED_MOTIFS: frozenset[str]` containing the motifs with no lichess equivalent (`self-interference`, and `double-bishop-mate` pending data verification). Add a module docstring noting that `hanging-piece` -> `hangingPiece` and `double-bishop-mate` spellings are to be confirmed against actual CSV theme values by the selector (RESEARCH Open Questions 1-2).

    Add `--ignore=tests/scripts/tagger` to the `addopts` string in `pyproject.toml` `[tool.pytest.ini_options]`, parallel to the existing benchmark ignore, and extend the adjacent comment to mention the explicit-path override for the tagger harness. This dir + the ignore MUST be committed together (D-14 — no dangling ignore for a non-existent dir; the package files in this same task satisfy that).
  </action>
  <verify>
    <automated>uv run python -c "from tests.scripts.tagger.motif_theme_map import MOTIF_TO_THEMES, UNVALIDATED_MOTIFS; assert MOTIF_TO_THEMES['fork'] == ('fork',); assert 'self-interference' in UNVALIDATED_MOTIFS; print('ok', len(MOTIF_TO_THEMES))"</automated>
    <automated>uv run python -c "import tomllib; a=tomllib.load(open('pyproject.toml','rb'))['tool']['pytest']['ini_options']['addopts']; assert '--ignore=tests/scripts/tagger' in a, a; print('ignore ok')"</automated>
    <automated>uv run pytest tests/scripts/tagger --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/scripts/tagger/__init__.py` and `conftest.py` exist; conftest reads the fixture CSV offline (no DB, no asyncio).
    - `MOTIF_TO_THEMES['fork'] == ('fork',)`, `MOTIF_TO_THEMES['discovered-attack']` contains `'discoveredAttack'`, and `mate` maps to a tuple including `'mate'`.
    - `UNVALIDATED_MOTIFS` contains `self-interference`.
    - `pyproject.toml` addopts contains `--ignore=tests/scripts/tagger` (verifiable via tomllib).
    - `uv run pytest tests/scripts/tagger --collect-only` works (the explicit path overrides the ignore) and does not error on import.
  </acceptance_criteria>
  <done>
    The excluded tagger package, the motif->theme map with the unvalidated list, and the pyproject ignore all exist and are committed together; the explicit path is collectable.
  </done>
</task>

<task type="auto">
  <name>Task 2: stratified puzzle selector + committed fixture + CI step</name>
  <files>scripts/select_tagger_fixtures.py, fixtures/tagger/detector_fixture.csv, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"lichess CC0 Puzzle Database" (FEN/Moves convention — critical), §"Selector Script", §"Harness Architecture", §"CI Integration Pattern", §"Standard Stack" (zstandard already in dev deps), §"Don't Hand-Roll" (use chess.Board.push, csv.reader), Open Questions 1-2 + 4 (N=50, min 10, collapse bands; print top-50 themes on first run).
    - scripts/select_benchmark_users.py — the existing zstd-streaming CSV selector pattern to mirror (decompression + row streaming).
    - tests/scripts/tagger/motif_theme_map.py — the map this selector filters against (from Task 1).
    - .github/workflows/ci.yml lines ~98-101 — the `Run pytest` step to add the tagger step after.
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-07, D-11, D-14.
  </read_first>
  <action>
    Write `scripts/select_tagger_fixtures.py` as a one-time re-runnable selector. It accepts the path to the full lichess CC0 puzzle download (`.csv.zst`), streams it with `zstandard` + `csv.reader` (mirror `select_benchmark_users.py`), and for each puzzle: splits `Moves` into `first_move = Moves[0]` and `pv = " ".join(Moves[1:])`; applies `first_move` to the puzzle `FEN` via `chess.Board.push` to produce the board-after-flaw FEN (CRITICAL convention per RESEARCH Pitfall 1 — the published FEN is BEFORE the opponent's blunder; Moves[0] is that blunder; Moves[1:] is the refutation PV); keeps the puzzle only if its `Themes` set intersects any value in `MOTIF_TO_THEMES`; stratified-samples N per (our_motif, Rating band) with bands `<1200 | 1200-1600 | 1600-2000 | >2000`. Extract N, the per-stratum minimum (below which Rating bands collapse), and the band bounds as named constants (no magic numbers). On run, print the 50 most common observed themes (to validate `hangingPiece` / `double-bishop-mate` spellings — RESEARCH Open Q1/Q2) and per-stratum coverage counts. Record the CC0-data-only / AGPL-cook.py boundary in the module docstring (D-11/SC#4): the dataset is CC0; cook.py is AGPL and is neither vendored nor ported.

    Emit `fixtures/tagger/detector_fixture.csv` with header `PuzzleId,FEN,FirstMove,PV,Themes,Rating` where `FEN` is the board-after-flaw. Run the selector against the downloaded puzzle DB to produce the committed fixture (hundreds of rows). The download itself is not committed; only the sampled fixture is.

    Add a dedicated CI step to `.github/workflows/ci.yml` immediately after the `Run pytest` step: name it "Tagger precision gate", run `uv run pytest tests/scripts/tagger -v`. No DB env (the harness is offline). The explicit path overrides the `--ignore`. This step must run on every CI invocation.
  </action>
  <verify>
    <automated>test -f fixtures/tagger/detector_fixture.csv && head -1 fixtures/tagger/detector_fixture.csv | grep -q '^PuzzleId,FEN,FirstMove,PV,Themes,Rating$' && echo header-ok</automated>
    <automated>uv run python -c "import csv; rows=list(csv.DictReader(open('fixtures/tagger/detector_fixture.csv'))); assert len(rows) >= 100, len(rows); import chess; chess.Board(rows[0]['FEN']); print('rows', len(rows))"</automated>
    <automated>uv run python -c "import yaml; y=yaml.safe_load(open('.github/workflows/ci.yml')); s=str(y); assert 'pytest tests/scripts/tagger' in s, 'CI step missing'; print('ci ok')"</automated>
    <automated>grep -Rn "cook" scripts/select_tagger_fixtures.py | grep -vi "agpl\|not vendored\|not ported\|boundary" || true</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/select_tagger_fixtures.py` exists, streams a `.zst` CSV, applies `Moves[0]` to the FEN (board-after-flaw), uses `pv = " ".join(Moves[1:])`, and stratified-samples by (motif, Rating band) with N + band bounds as named constants.
    - The selector docstring records the CC0-data-only / AGPL-cook.py boundary (SC#4); no AGPL code is vendored or ported.
    - `fixtures/tagger/detector_fixture.csv` exists with header `PuzzleId,FEN,FirstMove,PV,Themes,Rating` and >= 100 rows; row[0].FEN parses as a `chess.Board`.
    - `.github/workflows/ci.yml` has a dedicated step running `uv run pytest tests/scripts/tagger -v` after the default pytest step, with no DB env.
  </acceptance_criteria>
  <done>
    The re-runnable selector produces a committed stratified board-after-flaw fixture; the CI precision step targets the tagger path explicitly; the AGPL boundary is documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external puzzle CSV -> selector | locally-held CC0 download, filesystem read only; no network at runtime |
| committed fixture -> harness | static CSV in git; offline test input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | Tampering | external puzzle download | accept | Selector reads a local file the developer downloads; only the sampled fixture is committed; FEN/PV are parsed by python-chess which rejects malformed input |
| T-127-04 | Information Disclosure | committed fixture CSV | accept | CC0 public-domain puzzle data only; no PII, no secrets |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | No new packages (zstandard already in dev deps per RESEARCH §Standard Stack); nothing to audit |
</threat_model>

<verification>
- `uv run pytest tests/scripts/tagger --collect-only` succeeds (explicit path overrides ignore)
- fixture header + row count + FEN parse checks (automated above)
- CI yaml contains the dedicated tagger step
- `uv run pytest -n auto -x` (default suite) still EXCLUDES tagger (regression check on the ignore)
</verification>

<success_criteria>
- SC#2 partial: harness scaffold + motif->theme map + unvalidated list + committed fixture exist (measurement is Plan 03).
- SC#4: selector docstring records the CC0/AGPL boundary; no cook.py vendored or ported.
- SC#5 partial: independent puzzle fixture exists as the future source of CI numbers.
- D-14: tagger dir + pyproject ignore land together; CI uses explicit path.
</success_criteria>

<output>
Create `.planning/phases/127-detector-hardening-validation/127-02-SUMMARY.md` when done
</output>
