---
phase: 127-detector-hardening-validation
plan: 03
type: execute
wave: 2
depends_on: [127-01, 127-02]
files_modified:
  - tests/scripts/tagger/test_detector_precision.py
  - tests/scripts/tagger/precision_floors.py
  - app/repositories/library_repository.py
  - tests/services/test_tactic_detector.py
autonomous: true
requirements: [SC#2, SC#3, SC#5]
must_haves:
  truths:
    - "The harness prints a per-motif PRECISION and RECALL table against the committed fixture, plus the list of unvalidated motifs (no lichess equivalent)"
    - "The harness reports the depth-vs-puzzle-Rating correlation as a first-class output"
    - "Precision floors are set from the MEASURED post-fix numbers (not hardcoded before measurement), block per shipped motif, and recall is printed but non-blocking"
    - "Any tier-3 motif that misses the floor is query-suppressed via the existing tactic_confidence lever, not deleted"
    - "Fork and pin precision measurably improved versus the pre-gate baseline (SC#3)"
    - "The self-labeled fixture circularity in tests/services/test_tactic_detector.py is documented as superseded by the independent harness (SC#5)"
  artifacts:
    - path: "tests/scripts/tagger/test_detector_precision.py"
      provides: "precision/recall + depth-vs-Rating harness; precision floor gate"
      contains: "precision"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "per-motif precision floor constants set from measured numbers"
      contains: "PRECISION_FLOOR"
  key_links:
    - from: "tests/scripts/tagger/test_detector_precision.py"
      to: "app/services/tactic_detector.py"
      via: "calls detect_tactic_motif(board_after_flaw, pv) per fixture row"
      pattern: "detect_tactic_motif"
    - from: "tests/scripts/tagger/test_detector_precision.py"
      to: "tests/scripts/tagger/precision_floors.py"
      via: "asserts measured precision >= PRECISION_FLOOR[motif]"
      pattern: "PRECISION_FLOOR"
    - from: "app/repositories/library_repository.py"
      to: "app/models/game_flaw.py"
      via: "tactic_confidence >= floor suppresses sub-floor tier-3 motifs at query time"
      pattern: "tactic_confidence"
---

<objective>
Run the harness against the corrected detector, measure per-motif precision and recall plus the depth-vs-Rating correlation, set the blocking precision floors FROM the measured numbers (D-09 — floors are downstream of measurement, never hardcoded before it), suppress any sub-floor tier-3 motifs via the existing query lever, and document the self-labeled-fixture circularity as superseded.

Purpose: SC#2 (precision AND recall per motif, depth-vs-Rating correlation, unvalidated list), SC#3 (validate the fork/pin precision delta), SC#5 (independent ground truth supersedes the circular fixtures). Sequencing per D-09 / RESEARCH §"Precision Floor Calibration": measure baseline-vs-fixed delta first, THEN lock floors.

Output: a green precision gate with measured-and-justified floors; the trust contract for the dev re-backfill in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-detector-hardening-validation/127-CONTEXT.md
@.planning/phases/127-detector-hardening-validation/127-RESEARCH.md
@.planning/phases/127-detector-hardening-validation/127-01-SUMMARY.md
@.planning/phases/127-detector-hardening-validation/127-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- **Harness test** `tests/scripts/tagger/test_detector_precision.py` — iterates the committed fixture, calls `detect_tactic_motif(board_after_flaw, pv)` per row, computes per-motif precision (multi-label theme credit per D-10) and recall, prints both tables + the unvalidated-motif list + the depth-vs-Rating correlation (`statistics.correlation`, stdlib — no new dep), and asserts `precision[motif] >= PRECISION_FLOOR[motif]` for each shipped (non-suppressed) motif (D-08: precision blocks, recall reports).
- **Floor config** `tests/scripts/tagger/precision_floors.py` — `PRECISION_FLOOR: dict[str, float]` populated from the MEASURED post-fix numbers (core-8/geometric/named-mate near the ~0.90 target per D-09; tier-3 floor set from data), plus a documented `SUPPRESSED_MOTIFS` note for any tier-3 motif that misses the floor. All floors are named constants with a comment citing the measurement run.
- **Query suppression** (only if measurement requires it): adjust the existing `_TACTIC_CHIP_CONFIDENCE_MIN` lever in `app/repositories/library_repository.py` so sub-floor tier-3 motifs (low `tactic_confidence`) are filtered at query time — no schema change, no re-backfill, per D-09 / RESEARCH §"tactic_confidence Query-Suppression Lever".
- No new symbols beyond these; uses Plan 01's detector and Plan 02's fixture + map.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: precision/recall + depth-vs-Rating harness; measure baseline-vs-fixed delta; set floors from measured numbers</name>
  <files>tests/scripts/tagger/test_detector_precision.py, tests/scripts/tagger/precision_floors.py, app/repositories/library_repository.py</files>
  <read_first>
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"Harness Test" (precision/recall definitions, depth-vs-Rating correlation), §"Precision Floor Calibration" (the measure-then-lock sequence; do NOT hardcode 0.90 before measuring), §"Don't Hand-Roll" (statistics.correlation stdlib), §"Dev Re-Backfill" §"tactic_confidence Query-Suppression Lever".
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-06, D-08, D-09, D-10.
    - tests/scripts/tagger/motif_theme_map.py and conftest.py (from Plan 02) — the map and the fixture loader.
    - app/services/tactic_detector.py — the now-4-tuple `detect_tactic_motif` (from Plan 01).
    - app/repositories/library_repository.py — `_TACTIC_CHIP_CONFIDENCE_MIN` (line ~51) and its uses (~201, ~475-483, ~1440); the existing query-time suppression lever for tier-3.
    - .planning/phases/127-detector-hardening-validation/127-01-SUMMARY.md, 127-02-SUMMARY.md — what shipped in Wave 1.
  </read_first>
  <action>
    Write `tests/scripts/tagger/test_detector_precision.py`. For each fixture row, parse `FEN` -> `chess.Board` (this is already board-after-flaw), call `detect_tactic_motif(board, PV)`, map the returned motif int back to its motif string, and score against the row's `Themes` using the motif->theme map: a precision hit when our motif's theme set intersects the puzzle themes (multi-label credit, D-10). Compute per-motif precision = TP / (TP + FP) and recall = TP / (TP + FN). Print a per-motif precision AND recall table, the unvalidated-motif list (`UNVALIDATED_MOTIFS`, recall=N/A), and the depth-vs-Rating Pearson correlation over correctly-detected rows via `statistics.correlation` (stdlib; spearman/`statistics.correlation(..., method="ranked")` fallback noted if low power, RESEARCH A3). The depth-vs-Rating correlation is a first-class printed output (D-06), not gated.

    Measurement sequencing (D-09 — the single most important rule of this plan): FIRST run the harness as a report-only run to capture the post-fix per-motif precision/recall and the fork/pin numbers. Record the measured numbers in the SUMMARY and in a comment in `precision_floors.py`. To establish the SC#3 delta, also capture a baseline by temporarily disabling the relevance gate (e.g. an env flag or a one-off local revert) and re-running — note the fork/pin precision before vs after the gate in the SUMMARY. THEN create `tests/scripts/tagger/precision_floors.py` with `PRECISION_FLOOR: dict[str, float]` set from the measured numbers (core/geometric/named-mate at or just below the achieved precision near the ~0.90 D-09 target; tier-3 floor from data), each constant commented with the measured value it derives from. Do NOT author the `>= floor` assertion with a hardcoded number before this measurement exists.

    Wire the gate: in the harness, `assert precision[motif] >= PRECISION_FLOOR[motif]` for each shipped (non-suppressed) motif; recall is printed only (D-08). For any tier-3 motif whose measured precision misses its floor, add it to a `SUPPRESSED_MOTIFS` set in `precision_floors.py` and ensure it is filtered at query time: confirm the existing `_TACTIC_CHIP_CONFIDENCE_MIN` lever in `library_repository.py` already suppresses it (tier-3 stores graded confidence; core stores 100), and only adjust that constant if a suppressed motif's confidence falls above the current threshold. No schema change, no re-backfill. If the lever is adjusted, document the new value and why.

    Add a harness docstring recording: the CC0-data-only / AGPL-cook.py boundary (SC#4 reinforcement) and that these numbers supersede the self-labeled `tests/services/test_tactic_detector.py` fixtures (SC#5, pointer to Task 2).
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger -v</automated>
    <automated>uv run python -c "from tests.scripts.tagger.precision_floors import PRECISION_FLOOR; assert all(0.0 <= v <= 1.0 for v in PRECISION_FLOOR.values()); assert PRECISION_FLOOR.get('fork', 0) >= 0.80; print('floors', PRECISION_FLOOR)"</automated>
    <automated>uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/scripts/tagger -v` prints a per-motif table with BOTH precision and recall columns, the unvalidated-motif list, and a depth-vs-Rating correlation value.
    - `precision_floors.py` defines `PRECISION_FLOOR: dict[str, float]` with values derived from a recorded measurement run (each commented with its measured basis), and `fork` and `pin` floors are at or near the ~0.90 core target.
    - The harness asserts `precision[motif] >= PRECISION_FLOOR[motif]` only for shipped motifs; recall is printed, never asserted.
    - The SUMMARY records the fork/pin precision BEFORE vs AFTER the relevance gate (SC#3 delta).
    - Any sub-floor tier-3 motif is in `SUPPRESSED_MOTIFS` and is filtered at query time via the existing `tactic_confidence` lever (constant adjusted only if needed, with rationale).
    - `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>
    The harness measures precision+recall+depth-Rating correlation against independent CC0 ground truth; floors are set from those measurements and block per shipped motif; sub-floor tier-3 is query-suppressed; the fork/pin precision improvement is recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: document the self-labeled fixture circularity as superseded (D-12/SC#5)</name>
  <files>tests/services/test_tactic_detector.py</files>
  <read_first>
    - tests/services/test_tactic_detector.py — the module docstring (FIXTURE PROVENANCE / D-10 TIERED PRECISION BARS block at the top) describing the self-labeled, detector-bucketed fixtures.
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-12.
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"State of the Art" (self-labeled fixtures -> CC0 harness).
  </read_first>
  <action>
    Update the module docstring of `tests/services/test_tactic_detector.py` to record that these fixtures are detector-bucketed (circular) and that the authoritative precision/recall numbers now come from the independent lichess CC0 puzzle harness at `tests/scripts/tagger/test_detector_precision.py` (D-12/SC#5). State that these tests remain as fast signature/regression guards but their precision bars are no longer the trust source. Do NOT delete the file or its tests (it is the per-commit fast guard in 127-VALIDATION.md). This is a docstring + comment change only.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
    <automated>grep -q "tests/scripts/tagger" tests/services/test_tactic_detector.py && echo supersession-noted</automated>
  </verify>
  <acceptance_criteria>
    - The `test_tactic_detector.py` docstring states the fixtures are circular/detector-bucketed and superseded by the CC0 harness, with an explicit pointer to `tests/scripts/tagger/`.
    - The file's tests still pass (no deletions); it remains the fast per-commit regression guard.
  </acceptance_criteria>
  <done>
    The circularity is documented and the harness is named as the authoritative source; the fast guard tests are preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture -> harness -> detector | offline test input; no network, no DB |
| precision floor -> CI gate | a wrong floor (too low) could let false positives ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-05 | Repudiation | precision floor provenance | mitigate | Each floor constant is commented with the measured number it derives from; the SUMMARY records the measurement run (no silently-lowered bar) |
| T-127-06 | Tampering | query-suppression lever change | mitigate | Adjust `_TACTIC_CHIP_CONFIDENCE_MIN` only if a sub-floor motif would otherwise surface; document the value + rationale; existing library tests guard the chip behavior |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | No new packages (statistics is stdlib) |
</threat_model>

<verification>
- `uv run pytest tests/scripts/tagger -v` (precision gate green; recall + correlation printed)
- `uv run pytest tests/services/test_tactic_detector.py -x` (fast guard still green)
- `uv run ty check app/ tests/` zero errors
- `uv run pytest -n auto -x` (full default suite green; library_repository suppression behavior intact)
</verification>

<success_criteria>
- SC#2: precision + recall per motif, unvalidated list, depth-vs-Rating correlation all reported; precision blocks, recall reports.
- SC#3: fork/pin precision delta recorded and positive after the gate.
- SC#5: circular fixtures documented as superseded by the independent harness.
- D-09: floors set from measured numbers, sub-floor tier-3 suppressed via the query lever.
</success_criteria>

<output>
Create `.planning/phases/127-detector-hardening-validation/127-03-SUMMARY.md` when done
</output>
