---
phase: 127-detector-hardening-validation
plan: 04
type: execute
wave: 3
depends_on: [127-03]
files_modified:
  - .planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md
autonomous: false
requirements: [SC#1, SC#3]
user_setup: []
must_haves:
  truths:
    - "The corrected detector has been re-run over the existing dev tagged flaws via scripts/backfill_flaws.py --db dev (no DB reset)"
    - "tactic_depth is populated (non-NULL) on re-backfilled tagged rows; pre-existing un-touched rows remain honestly NULL"
    - "Fork and pin tag counts on dev measurably dropped versus the pre-fix counts (false positives removed by the relevance gate)"
    - "A prod re-backfill runbook is documented for out-of-phase execution (deferred per D-13), mirroring Phase 125's deferral"
  artifacts:
    - path: ".planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md"
      provides: "documented, deferred prod re-backfill procedure"
      contains: "backfill_flaws"
  key_links:
    - from: "scripts/backfill_flaws.py"
      to: "app/services/flaws_service.py"
      via: "re-backfill invokes classify_game_flaws which now writes tactic_depth + gated tags"
      pattern: "classify_game_flaws"
---

<objective>
Validate the corrected detector against real data by re-running the dev re-backfill (D-13 — the in-phase real-data validation beyond fixtures), confirm `tactic_depth` is populated and fork/pin false positives dropped, and document the deferred prod re-backfill as a runbook.

Purpose: SC#1 (real `tactic_depth` storage proven on dev), SC#3 (the precision fix proven against live tagged data, not just fixtures). D-13: dev re-backfill in-phase; prod deferred to a runbook (mirrors Phase 125 D-01). Per the project rule, NO dev DB reset — backfill runs against the existing dev DB via the unmodified `scripts/backfill_flaws.py`.

Output: a verified dev re-backfill and a committed prod runbook; the phase gate's real-data confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-detector-hardening-validation/127-CONTEXT.md
@.planning/phases/127-detector-hardening-validation/127-RESEARCH.md
@.planning/phases/127-detector-hardening-validation/127-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- **Runbook** `.planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md` — the deferred prod re-backfill procedure (`scripts/backfill_flaws.py --db prod` via `bin/prod_db_tunnel.sh`), idempotency notes, batch size, expected fork/pin count drop, and the "new drains pick up corrected code automatically" note (D-13). No new code symbols — the dev re-backfill reuses the existing script unchanged (RESEARCH §"Dev Re-Backfill": no modification needed).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: capture pre-fix baseline counts, run dev re-backfill, verify tactic_depth + FP drop</name>
  <files>.planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/127-detector-hardening-validation/127-RESEARCH.md §"Dev Re-Backfill" (existing `backfill_flaws.py` API; dev DB state: 73,201 flaws, 32,712 tagged; fork=10,083, pin=6,299; ~20-30 min for full; idempotent delete-then-insert; `--full-evald-only` flag).
    - .planning/phases/127-detector-hardening-validation/127-CONTEXT.md D-13 (dev in-phase, prod deferred, no DB reset).
    - scripts/backfill_flaws.py — confirm the `--db dev`, `--user-id`, `--dry-run`, `--full-evald-only` flags and that it calls `classify_game_flaws` (no modification needed).
    - CLAUDE.md "No dev DB reset in plans" rule and the Database Access (MCP) section (`mcp__flawchess-db__query`) for verification queries.
  </read_first>
  <action>
    BEFORE re-backfilling, record the current dev tag counts as a baseline via the dev DB MCP query (`mcp__flawchess-db__query`): total tagged flaws, and the fork / pin / discovered-attack / skewer counts, and the count of non-NULL `tactic_depth` (expected 0 pre-backfill). Save these numbers in the SUMMARY.

    Run the dev re-backfill with the UNMODIFIED `scripts/backfill_flaws.py --db dev` (optionally `--full-evald-only` to limit to the taggable set per RESEARCH). Do NOT reset the dev DB (project rule). The script's delete-then-insert is idempotent; it flows the corrected detector + `tactic_depth` write path through `classify_game_flaws` automatically.

    AFTER the re-backfill, re-query the same counts. Confirm: (a) non-NULL `tactic_depth` count is now > 0 on re-backfilled tagged rows; (b) fork and pin tag counts dropped versus the baseline (the relevance gate removed false positives); (c) the drops are plausible (not zeroing out — the gate prunes incidental hits, not all hits). Record before/after counts and the delta in the SUMMARY.

    Write `.planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md`: the exact prod command (`scripts/backfill_flaws.py --db prod` behind `bin/prod_db_tunnel.sh`), idempotency + batch-size notes, the expected fork/pin count reduction direction, that prod re-backfill is executed OUTSIDE the phase gate (D-13, mirroring Phase 125 D-01), and that new drains pick up the corrected code automatically so lichess-only coverage refills over time. Do NOT execute the prod re-backfill.
  </action>
  <verify>
    <automated>test -f .planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md && grep -q "backfill_flaws" .planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md && echo runbook-ok</automated>
  </verify>
  <acceptance_criteria>
    - Pre-fix baseline counts (tagged total, fork, pin, non-NULL tactic_depth) are recorded in the SUMMARY.
    - The dev re-backfill ran via the unmodified `scripts/backfill_flaws.py --db dev` with NO DB reset.
    - Post-backfill: non-NULL `tactic_depth` count > 0; fork and pin counts dropped vs baseline; deltas recorded.
    - `127-PROD-REBACKFILL-RUNBOOK.md` exists, references `backfill_flaws.py --db prod` behind the prod tunnel, marks prod execution as deferred (D-13), and notes drains auto-pick-up.
  </acceptance_criteria>
  <done>
    The corrected detector is validated against live dev data (tactic_depth populated, fork/pin FPs dropped); the prod re-backfill is documented but not executed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human verify dev re-backfill — tactic_depth populated, fork/pin FP drop</name>
  <action>Human reviews the before/after dev re-backfill counts in 127-04-SUMMARY.md and confirms tactic_depth is populated and fork/pin false positives dropped without collapsing the motifs. Pauses execution until approved.</action>
  <what-built>The dev re-backfill ran the hardened detector over the existing dev tagged flaws (no DB reset). `tactic_depth` is now populated on re-backfilled rows, and fork/pin tag counts dropped as the relevance gate removed incidental/deep-scan false positives. Before/after counts are in 127-04-SUMMARY.md.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/127-detector-hardening-validation/127-04-SUMMARY.md` and review the before/after table (tagged total, fork, pin, non-NULL tactic_depth).
    2. Confirm non-NULL `tactic_depth` count went from ~0 to a meaningful number on re-backfilled tagged rows.
    3. Confirm fork and pin counts dropped (false positives removed) but did NOT collapse to near-zero (the gate prunes, it doesn't gut the motif).
    4. Optionally spot-check a few previously fork/pin-tagged flaws in the dev DB that are now NULL, and confirm the refutation PV had no real material-winning motif at the tagged depth.
    5. Confirm `.planning/phases/127-detector-hardening-validation/127-PROD-REBACKFILL-RUNBOOK.md` documents the deferred prod step.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. counts look wrong, a motif collapsed, depth still NULL)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill script -> dev DB | bulk delete-then-insert on game_flaws; idempotent; dev only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-07 | Tampering | dev re-backfill bulk write | mitigate | Use the unmodified, idempotent `backfill_flaws.py`; capture before-counts first; human-verify checkpoint reviews the delta; NO DB reset (project rule) |
| T-127-08 | Denial of Service | prod re-backfill memory pressure | accept | Prod re-backfill is DEFERRED to a runbook (not executed in-phase); the runbook reuses the existing batched script (batch 100) that has run on prod before |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no script modification |
</threat_model>

<verification>
- Runbook file exists and references the prod backfill command (automated above)
- Dev before/after counts recorded in SUMMARY (human-verify checkpoint)
- `tactic_depth` non-NULL count > 0 post-backfill (human-verify via MCP query)
</verification>

<success_criteria>
- SC#1: `tactic_depth` proven populated on real dev data after re-backfill.
- SC#3: fork/pin false-positive drop proven against live tagged data (beyond fixtures).
- D-13: dev re-backfill done in-phase (no reset); prod deferred to a committed runbook.
</success_criteria>

<output>
Create `.planning/phases/127-detector-hardening-validation/127-04-SUMMARY.md` when done
</output>
