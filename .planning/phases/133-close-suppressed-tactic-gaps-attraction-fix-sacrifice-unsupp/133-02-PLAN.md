---
phase: 133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp
plan: 02
type: execute
wave: 2
depends_on:
  - 133-01
files_modified:
  - tests/scripts/tagger/precision_floors.py
  - app/repositories/library_repository.py
  - tests/services/test_tactic_comparison_service.py
  - frontend/src/lib/theme.ts
  - frontend/src/lib/tacticComparisonMeta.ts
  - reports/tactic-tagger/tactic-tagger-2026-06-23.md
autonomous: true
requirements: []   # none mapped; traceability via the investigation-note locked decisions (mechanical unsuppress path + sacrifice unsuppress-only + docstring corrections)
nyquist_compliant: true

must_haves:
  truths:
    - "attraction, sacrifice, arabian-mate, boden-mate, dovetail-mate are no longer in SUPPRESSED_MOTIFS and each clears its PRECISION_FLOOR on TRAIN"
    - "FAMILY_TO_MOTIF_INTS has 17 family keys including new 'attraction' and 'sacrifice' families"
    - "the family-count test asserts the new count (17) and passes; the suppressed-tier3 test asserts only {14} stays unmapped"
    - "attraction + sacrifice surface as 'advanced'-group filter chips in the frontend (tsc -b green)"
    - "the two stale precision_floors.py docstrings (attraction 'PV depth', sacrifice 'never wins dispatch') are corrected"
    - "the regenerated tactic-tagger report shows the five motifs with their measured precision"
  artifacts:
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "attraction/sacrifice/arabian-mate/boden-mate/dovetail-mate removed from SUPPRESSED_MOTIFS; PRECISION_FLOOR entries added; corrected docstrings"
      contains: "\"attraction\""
    - path: "app/repositories/library_repository.py"
      provides: "FAMILY_TO_MOTIF_INTS attraction + sacrifice family keys"
      contains: "\"sacrifice\""
    - path: "frontend/src/lib/theme.ts"
      provides: "TAC_ATTRACTION / TAC_SACRIFICE color tokens"
      contains: "TAC_ATTRACTION"
    - path: "frontend/src/lib/tacticComparisonMeta.ts"
      provides: "attraction + sacrifice in TacticFamily union, color/icon records, and TACTIC_COMPARISON_FAMILIES advanced group"
      contains: "'sacrifice'"
  key_links:
    - from: "tests/scripts/tagger/precision_floors.py::PRECISION_FLOOR"
      to: "tests/scripts/tagger/test_detector_precision.py"
      via: "harness asserts TRAIN precision >= floor for newly-unsuppressed motifs"
      pattern: "attraction|sacrifice|arabian-mate|boden-mate|dovetail-mate"
    - from: "app/repositories/library_repository.py::FAMILY_TO_MOTIF_INTS"
      to: "frontend/src/lib/tacticComparisonMeta.ts::TACTIC_COMPARISON_FAMILIES"
      via: "cross-stack string-for-string family contract"
      pattern: "attraction|sacrifice"
---

<objective>
Surface the five now-correct motifs (attraction, sacrifice, arabian-mate, boden-mate, dovetail-mate) end-to-end via the mechanical unsuppress path (RESEARCH §"Mechanical Unsuppress Path"; investigation-note §"Mechanical unsuppress path" locked decisions), and correct the two factually-wrong docstrings.

Sacrifice is unsuppress-ONLY (locked decision, investigation note §DECISION): NO detector change, NO schema change, NO co-tag. Its detector is already standalone 1.000 precision / 1.000 recall; it just needs to leave SUPPRESSED_MOTIFS and get a family + chip.

Purpose: Plan 01 fixed the four detectors but they (and sacrifice) are still filtered out at query time and have no family/chip. This plan removes the suppression, adds the precision-floor gate, the backend family mapping, the frontend family/chip, updates the family-count + suppressed-tier3 tests, fixes the two stale docstrings, and regenerates the tactic-tagger report as the final validation artifact.

Floors are TRAIN-measured (D-09): RUN the harness first, read the actual measured TRAIN precision for each of the five motifs, then set each floor ~5-8pp below it (rounded to 0.05 per the precision_floors.py convention). RESEARCH's numbers are near-1.000 (floor 0.93 expected), but confirm against the live run — do NOT hardcode blind.

Output: precision_floors.py, library_repository.py, test_tactic_comparison_service.py, theme.ts, tacticComparisonMeta.ts edited; the dated tactic-tagger report regenerated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp/133-RESEARCH.md
@.planning/notes/suppressed-tactic-gaps-investigation.md
@.planning/phases/133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp/133-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols / entries introduced by this plan:
- `PRECISION_FLOOR` keys: `"attraction"`, `"sacrifice"`, `"arabian-mate"`, `"boden-mate"`, `"dovetail-mate"` (floors ~5-8pp below measured TRAIN; expect 0.93).
- `FAMILY_TO_MOTIF_INTS` keys: `"attraction"` → `[10]`, `"sacrifice"` → `[17]` (advanced group). (arabian/boden/dovetail already live under the existing `"mate"` family — no new key.)
- `frontend/src/lib/theme.ts` exports: `TAC_ATTRACTION`, `TAC_ATTRACTION_BG`, `TAC_SACRIFICE`, `TAC_SACRIFICE_BG` (all = TAC_BLUE / TAC_BLUE_BG, per the single-hue convention).
- `frontend/src/lib/tacticComparisonMeta.ts`: `'attraction'` + `'sacrifice'` added to the `TacticFamily` union, to `TACTIC_FAMILY_COLORS`, `TACTIC_FAMILY_ICON`, and two new `TacticFamilyDef` entries in `TACTIC_COMPARISON_FAMILIES` (advanced group).
- Removed from `SUPPRESSED_MOTIFS`: `"attraction"`, `"sacrifice"`, `"arabian-mate"`, `"boden-mate"`, `"dovetail-mate"`.
- The family count contract rises 15 → 17.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Unsuppress all five motifs + add floors + correct stale docstrings (precision_floors.py)</name>
  <read_first>
    - 133-01-SUMMARY.md — the measured TRAIN precision/TP/FP for the five motifs (set floors from these, not from RESEARCH's pre-numbers).
    - tests/scripts/tagger/precision_floors.py lines 169-201 (`SUPPRESSED_MOTIFS`) and 223-283 (`PRECISION_FLOOR`); the stale docstrings are the Phase 132-04 attraction block (~L87-90, "Lure+4-move sequence rarely survives Stockfish PV depth limit") and the sacrifice block (~L96-99, "material-diff predicate never wins single-winner dispatch").
    - 133-RESEARCH.md §"Mechanical Unsuppress Path" step 1 + §"Named mates unsuppress path".
    - tests/scripts/tagger/test_detector_precision.py lines 64-212 — the gate: a motif NOT in SUPPRESSED_MOTIFS and present in PRECISION_FLOOR is asserted `precision_train >= floor`.
  </read_first>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <action>
    1. RUN `uv run pytest tests/scripts/tagger/test_detector_precision.py -s` first and read the printed TRAIN precision/TP/FP for attraction, sacrifice, arabian-mate, boden-mate, dovetail-mate (cross-check against 133-01-SUMMARY).
    2. Remove these five entries from the `SUPPRESSED_MOTIFS` frozenset: `"attraction"`, `"sacrifice"`, `"arabian-mate"`, `"boden-mate"`, `"dovetail-mate"`. Leave `"self-interference"`, `"double-bishop-mate"`, `"trapped-piece"`, `"en-passant"`, `"under-promotion"` in place. Remove the now-stale inline comments tied to the five removed entries (e.g. the attraction "PV depth limit" inline comment block at ~L177-180, the arabian/boden/sacrifice "never fires" grouping comment, the dovetail "0 TP, 23 FP" comment).
    3. Add five `PRECISION_FLOOR` entries set ~5-8pp below the measured TRAIN precision, rounded to 0.05 per the module convention. With measured ~1.000, the floors are `0.93`. If any motif measured below ~0.98 TRAIN, set its floor 5-8pp below the actual measured value instead. Each entry gets a one-line comment `# train <measured> / test <measured> (phase 133 cook port)`.
    4. Correct the two stale docstrings in the Phase 132-04 measurement block:
       - attraction: replace the "Lure+4-move sequence rarely survives Stockfish PV depth limit" text with the factual cause: an off-by-one (cond-5 attacker board was `boards[k+2]`, should be `boards[k+3]`), fixed in Phase 133; now ~1.000 TRAIN, SHIPPED.
       - sacrifice: replace "material-diff predicate never wins single-winner dispatch" with the factual state: standalone 1.000 precision / 1.000 recall; only dispatch-shadowed (~12.2% post-dispatch recall) by higher-priority mates/fork; unsuppressed (unsuppress-only, no co-tag) in Phase 133.
    5. Add a short Phase 133 measurement-run block to the module docstring summarizing the five motifs' measured TRAIN/TEST precision and the SHIPPED decision (mirror the existing Phase 132-04 block format).
    IMPORTANT (comment-text discipline): do NOT leave the literal phrases the corrected docstrings are replacing ("never survives the Stockfish PV depth limit", "never wins single-winner dispatch") anywhere in the file as a quoted "what it used to say" snippet — rephrase by concept. No automated negative-grep gate keys on these phrases, but keep the file clean.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py -s 2>&1 | grep -E "attraction|sacrifice|arabian-mate|boden-mate|dovetail-mate|floor"</automated>
  </verify>
  <acceptance_criteria>
    - The harness run is GREEN (exit 0): each of the five motifs is now gated (status not "SUPPRESSED") and its TRAIN precision ≥ its new floor.
    - `SUPPRESSED_MOTIFS` no longer contains any of the five; it still contains `self-interference`, `double-bishop-mate`, `trapped-piece`, `en-passant`, `under-promotion`.
    - `PRECISION_FLOOR` has keys `attraction`, `sacrifice`, `arabian-mate`, `boden-mate`, `dovetail-mate`, each ~5-8pp below its measured TRAIN.
    - The attraction docstring no longer claims a PV-depth cause; the sacrifice docstring no longer claims it "never wins single-winner dispatch".
  </acceptance_criteria>
  <done>The five motifs are floor-gated and pass; the two stale docstrings state the real causes.</done>
</task>

<task type="auto">
  <name>Task 2: Add attraction + sacrifice families + update backend family tests</name>
  <read_first>
    - app/repositories/library_repository.py lines 109-175 (`FAMILY_TO_MOTIF_INTS`) — add the two new keys at the END of the advanced/tier-3 group (after `capturing_defender`); also lines 154-159 (the comment saying attraction=10/self-interference=14/sacrifice=17 map to no family — update it to list only self-interference=14).
    - tests/services/test_tactic_comparison_service.py lines 161-229 — `test_family_mapping_ten_families` (expected_keys, count 15→17), `test_family_mapping_excludes_suppressed_tier3` (suppressed_tier3_ints {10,14,17} → {14}), `test_family_mapping_covers_selected_motifs` (add attraction==[10], sacrifice==[17]).
    - app/services/tactic_detector.py lines 100-108 — TacticMotifInt.ATTRACTION=10, SACRIFICE=17.
    - 133-RESEARCH.md §"Mechanical Unsuppress Path" steps 2 + 5.
  </read_first>
  <files>app/repositories/library_repository.py, tests/services/test_tactic_comparison_service.py</files>
  <action>
    In `library_repository.py`: add two new keys at the end of `FAMILY_TO_MOTIF_INTS`, in the tier-3 "Advanced" group after `capturing_defender`:
      `"attraction": [int(TacticMotifInt.ATTRACTION)],`  (int 10)
      `"sacrifice": [int(TacticMotifInt.SACRIFICE)],`    (int 17)
    Update the group comment (~L154-159) that lists "Still-suppressed tier-3 (attraction=10, self-interference=14, sacrifice=17) map to no family" to say only self-interference (14) remains unmapped, citing Phase 133.
    Do NOT touch the `"mate"` family — arabian/boden/dovetail (ints 21/22/24) are already members.
    In `test_tactic_comparison_service.py`:
    - `test_family_mapping_ten_families`: add `"attraction"` and `"sacrifice"` to `expected_keys`; update the docstring to say 17 families (10 canonical + 7 tier-3 advanced). Rename considered but NOT required — keep the function name (it is referenced by the family run command); just update the asserted set + docstring.
    - `test_family_mapping_excludes_suppressed_tier3`: change `suppressed_tier3_ints = {10, 14, 17}` to `{14}` (self-interference only); update the docstring to reflect that attraction (10) and sacrifice (17) are now mapped as Phase-133 advanced families and only self-interference (14) stays unmapped.
    - `test_family_mapping_covers_selected_motifs`: add `assert FAMILY_TO_MOTIF_INTS["attraction"] == [10]` and `assert FAMILY_TO_MOTIF_INTS["sacrifice"] == [17]`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_comparison_service.py -k test_family_mapping -v</automated>
  </verify>
  <acceptance_criteria>
    - `FAMILY_TO_MOTIF_INTS` has exactly 17 keys including `attraction` → `[10]` and `sacrifice` → `[17]`.
    - `test_family_mapping_ten_families`, `test_family_mapping_excludes_suppressed_tier3`, `test_family_mapping_covers_selected_motifs` all PASS.
    - `suppressed_tier3_ints` in the exclusion test is `{14}` (not `{10, 14, 17}`).
  </acceptance_criteria>
  <done>Backend family mapping carries attraction + sacrifice; all three family tests pass at the new 17-family count.</done>
</task>

<task type="auto">
  <name>Task 3: Surface attraction + sacrifice as frontend "advanced" filter chips</name>
  <read_first>
    - frontend/src/lib/theme.ts lines 88-123 — the TAC_* single-hue token block (TAC_BLUE / TAC_BLUE_BG; existing tokens like TAC_CAPTURING_DEFENDER).
    - frontend/src/lib/tacticComparisonMeta.ts — imports (lines 21-73), `TacticFamily` union (99-115), `TACTIC_FAMILY_COLORS` (124-140), `TACTIC_FAMILY_ICON` (142-158), `TACTIC_COMPARISON_FAMILIES` advanced group (297-348), and the comment at L206-207 / L358-359 listing sacrifice as belonging to no family.
    - frontend/src/lib/tacticMotifDefinitions.ts — already has `attraction` and `sacrifice` definition strings (no edit needed; reuse for the TacticFamilyDef.definition copy).
    - lucide-react icon set already imported (Magnet for deflection, Shuffle for intermezzo, etc.) — pick two distinct unused icons (e.g. `Magnet` is taken by deflection; consider `Crosshair`/`Target` for attraction and `Flame`/`HandCoins`/`Gift` for sacrifice — confirm the icon exists in lucide-react before importing).
  </read_first>
  <files>frontend/src/lib/theme.ts, frontend/src/lib/tacticComparisonMeta.ts</files>
  <action>
    In `theme.ts`, after `TAC_CAPTURING_DEFENDER_BG` (~L123) add four exports following the single-hue convention:
      `export const TAC_ATTRACTION = TAC_BLUE;`
      `export const TAC_ATTRACTION_BG = TAC_BLUE_BG;`
      `export const TAC_SACRIFICE = TAC_BLUE;`
      `export const TAC_SACRIFICE_BG = TAC_BLUE_BG;`
    In `tacticComparisonMeta.ts`:
    - Import `TAC_ATTRACTION, TAC_ATTRACTION_BG, TAC_SACRIFICE, TAC_SACRIFICE_BG` from `@/lib/theme`.
    - Import two distinct lucide icons for the new families (verify they exist in the installed lucide-react). Suggested: `Target` (attraction) and `Gift` (sacrifice). Do NOT reuse an icon already in `TACTIC_FAMILY_ICON`.
    - Add `'attraction'` and `'sacrifice'` to the `TacticFamily` union (at the end of the tier-3 "Advanced" families list).
    - Add `attraction: { color: TAC_ATTRACTION, bg: TAC_ATTRACTION_BG }` and `sacrifice: { color: TAC_SACRIFICE, bg: TAC_SACRIFICE_BG }` to `TACTIC_FAMILY_COLORS`.
    - Add `attraction: <Icon>` and `sacrifice: <Icon>` to `TACTIC_FAMILY_ICON`.
    - Add two `TacticFamilyDef` entries to `TACTIC_COMPARISON_FAMILIES` at the END of the advanced group (after `capturing_defender`): `{ name: 'Attraction', family: 'attraction', group: 'advanced', chipLabel: 'attraction', definition: <reuse the attraction string from tacticMotifDefinitions / TACTIC_MOTIF_DEFINITIONS>, motifs: ['attraction'] }` and the analogous `sacrifice` entry (chipLabel 'sacrifice', motifs ['sacrifice']).
    - Update the header doc comment "15 families" → "17 families" and the comments at ~L16-17 / L206-207 / L358-359 that list sacrifice (and attraction) as "belonging to no family" — remove sacrifice and attraction from those "no family" lists (only self-interference remains).
    Apply the SAME change to both desktop and mobile renderers IF either references the family list directly — but note this meta file is the single source of truth consumed by the filter panel + grid, so the chip rendering is data-driven; no separate component edit is expected. Confirm via grep that no component hardcodes the family list.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -b` passes (no type errors — the `Record<TacticFamily, ...>` maps are exhaustive with the two new keys).
    - `npm run lint` and `npm test -- --run` pass; knip reports no unused exports (the four new theme tokens are imported by tacticComparisonMeta).
    - `TACTIC_COMPARISON_FAMILIES` contains advanced-group entries for `attraction` and `sacrifice` with `motifs: ['attraction']` / `['sacrifice']`.
    - The "belongs to no family" comments no longer list sacrifice or attraction.
  </acceptance_criteria>
  <done>attraction + sacrifice are typed, colored, iconed, and present as advanced-group families; frontend type-checks and tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite gate + regenerate the tactic-tagger report</name>
  <read_first>
    - CLAUDE.md "Pre-merge gate" section — the exact command sequence.
    - 133-VALIDATION.md "Manual-Only Verifications" — report regeneration row.
    - The `/tactic-tagger-report` skill (`scripts/tactic_tagger_report.py`) — writes `reports/tactic-tagger/tactic-tagger-YYYY-MM-DD.md`.
  </read_first>
  <files>reports/tactic-tagger/tactic-tagger-2026-06-23.md</files>
  <action>
    1. Run the full backend pre-merge gate: `uv run ruff format app/ tests/`, `uv run ruff check app/ tests/ --fix`, `uv run ty check app/ tests/` (zero errors), `uv run pytest -n auto -x` (full backend suite green). Resolve any failures before proceeding.
    2. Run the frontend gate: `cd frontend && npm run lint && npm test -- --run` (and `npx tsc -b` if not already run in Task 3).
    3. Regenerate the tactic-tagger report via the `/tactic-tagger-report` skill (or `uv run python scripts/tactic_tagger_report.py`). It writes the dated report under `reports/tactic-tagger/`. If the date differs from 2026-06-23 on the execution day, the report filename will reflect the actual run date — update `files_modified`/SUMMARY accordingly; the artifact is whatever dated file the script writes.
    4. Confirm the regenerated report shows attraction, sacrifice, arabian-mate, boden-mate, dovetail-mate with their measured precision/recall (no longer NaN / suppressed).
    Do NOT run `bin/reset_db.sh` or any dev-DB reset (project rule — plans must not gate on it). The harness uses committed CC0 fixtures, not the DB.
  </action>
  <verify>
    <automated>uv run pytest -n auto -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -n auto -x` exits 0 (full backend suite green, including the tagger harness floor gate for all five new motifs).
    - `uv run ty check app/ tests/` reports zero errors; ruff format/check clean.
    - Frontend `npm run lint` + `npm test -- --run` + `tsc -b` green.
    - A dated `reports/tactic-tagger/tactic-tagger-*.md` exists showing the five motifs with measured (non-NaN) precision/recall.
  </acceptance_criteria>
  <done>Full cross-stack gate is green and the regenerated tactic-tagger report reflects the five shipped motifs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| family-map keys ↔ frontend family strings | Cross-stack string-for-string contract; a mismatch silently drops chips. Guarded by the family-count test + tsc exhaustive Record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | Tampering | floor set above measured TRAIN → CI flakes / below → no gate | mitigate | floor set ~5-8pp below the live-measured TRAIN value, harness asserts on every run |
| T-133-04 | Repudiation | stale docstrings mislead future maintainers about root causes | mitigate | both corrected with the factual cause per the investigation note |
| T-133-05 | Tampering | frontend family added but Record non-exhaustive → runtime undefined color/icon | mitigate | `tsc -b` exhaustiveness check over `Record<TacticFamily, ...>` |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | no package installs (uses already-installed lucide-react icons + existing deps) |
</threat_model>

<verification>
- `uv run pytest tests/scripts/tagger/test_detector_precision.py -s` — five motifs gated and passing their floors.
- `uv run pytest tests/services/test_tactic_comparison_service.py -k test_family_mapping` — 17-family count + corrected suppressed-tier3 set.
- `uv run pytest -n auto -x` + `uv run ty check app/ tests/` — full backend gate green.
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` — frontend gate green.
- Manual: read the corrected attraction + sacrifice docstrings in precision_floors.py; read the regenerated report.
</verification>

<success_criteria>
- attraction, sacrifice, arabian-mate, boden-mate, dovetail-mate are unsuppressed, floor-gated, and pass on TRAIN.
- FAMILY_TO_MOTIF_INTS = 17 families; family tests green.
- attraction + sacrifice surface as advanced filter chips; tsc/lint/test green.
- Two stale docstrings corrected.
- Tactic-tagger report regenerated showing the five motifs.
- No DB reset used; sacrifice unchanged at the detector level (unsuppress-only locked decision honored).
</success_criteria>

<output>
Create `.planning/phases/133-close-suppressed-tactic-gaps-attraction-fix-sacrifice-unsupp/133-02-SUMMARY.md` when done. Record the final floors set, the family count, and the regenerated report filename.
</output>
