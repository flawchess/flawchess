---
phase: 221-tactic-tagger-real-game-precision-winning-floor-predicate-ti
plan: 02
type: execute
wave: 2
depends_on: [221-01]
files_modified:
  - app/services/tactic_detector.py
  - tests/services/test_tactic_detector.py
  - tests/scripts/tagger/precision_floors.py
autonomous: true
requirements: [TAGFIX-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- goal-backward truths (TAGFIX-05 / D-11 / D-12) ---
    - "`detect_deflection`'s promotion branch is cook's OR: the deflection fires when the capture square is in `grandpa_board.attacks(orig_sq)` OR when the move is a promotion on the same file as the deflected piece's origin with the pov move reachable from it."
    - "`detect_fork` no longer skips a candidate because the material at the end of the line is below the material at the start; the `material_at_start` / `material_at_end` locals and the relevance-gate docstring paragraph are gone with it."
    - "`_piece_is_trapped` returns True for an attacked, unpinned, non-pawn, non-king piece in a bad spot with zero legal escape moves (cook's immobile-attacked rule), and its docstring no longer describes the removed precision-first exclusion."
    - "`detect_discovered_attack` returns `(False, None, None)` — not `continue` — when the opponent's prior move landed on the capture square, and `detect_skewer`'s visually identical recapture line is untouched."
    - "`detect_discovered_attack` returns depth `k` (the pov move index), matching `detect_skewer`; the obsolete WR-02 rationale comment is replaced by one naming D-11 and its consequences."
    - "`detect_boden_or_double_bishop_mate` classifies a bishop sitting ON the opponent king's file as cook does, so it is not bucketed with `b_file < king_file` being false for both same-file and right-of-file."
    - "`self-interference` is absent from `_TIER3_REGISTRY`, so int 14 can never win dispatch; `TacticMotifInt.SELF_INTERFERENCE`, `_INT_TO_MOTIF[14]` and `detect_self_interference` all still exist and are still importable."
    - "The single fast-guard fixture at `tests/services/test_tactic_detector.py` lines 465-469 (`8/2r3pk/1p4n1/1Pb3Pp/p4P2/P3N1P1/1BP4K/4R3 b - - 0 33` with PV `c5e3 e1e3 c7c2 h2h3 c2b2`) is re-labelled `fork` with an inline note that D-11 reclassified it, not deleted."
    - "`uv run pytest tests/scripts/tagger -q` passes with no `PRECISION_FLOOR` entry lowered; `precision_floors.py` records the new measured TRAIN/TEST numbers for every motif whose count changed."
    - "`uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200` still reports no breaches."
    # --- edge-probe derived (TAGFIX-05) ---
    - "adjacency: when the restored deflection promotion clause and the `square in attacks(orig_sq)` clause are BOTH satisfied, deflection fires exactly once — the OR yields one detection, never two candidates."
    - "empty: with `len(moves) < 3` or `len(boards) < 4`, `detect_discovered_attack` returns `(False, None, None)` before indexing `moves[k-2]`; `_piece_is_trapped` with an empty escape set now returns True via cook's fallthrough instead of an early False."
    - "ordering: `discovered-check` (geometric rank 4) still beats `discovered-attack` (rank 5) at equal depth, and removing the `self-interference` tuple leaves the relative rank order of the eight surviving `_TIER3_REGISTRY` motifs unchanged."
    - "idempotency: `detect_tactic_motif` called twice on the same board and PV returns the identical four-tuple — every port fix reads boards, and any board mutation goes through `board.copy(stack=False)`."
    - statement: "concurrency: the port fixes add no module-level mutable state, so a `retag_flaws.py` spawn worker that re-imports `app.services.tactic_detector` produces the same result regardless of `--workers`."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT lower or delete a `PRECISION_FLOOR` entry to absorb a regression from these fixes; a floor moves only together with a recorded measurement and a stated reason in its trailing comment."
      status: unverified
      verification: flagged
    - statement: "MUST NOT copy lichess-puzzler source into the repository; each port fix is described from cook's prose/heuristic only, and the AGPL-boundary comment stays attached."
      status: unverified
      verification: flagged
    - statement: "MUST NOT change `PV_CAP_PLIES`, `ONLY_MOVE_CP_GAP_THRESHOLD`, `ALREADY_WINNING_CP_THRESHOLD` (800), `STILL_WINNING_FLOOR_CP`, or the depth-primary dispatch tiebreak in `_select_shallowest_candidate` — the Phase-144 A/B validation that set them is not re-litigated."
      status: unverified
      verification: flagged
    - statement: "MUST NOT delete `TacticMotifInt.SELF_INTERFERENCE` (14), `_INT_TO_MOTIF[14]` or `detect_self_interference` — persisted prod rows and three existing tests still reference them; only the `_TIER3_REGISTRY` tuple is removed."
      status: unverified
      verification: flagged
    - statement: "MUST NOT regenerate `fixtures/tagger/detector_fixture_train.csv` or `detector_fixture_test.csv`; floors are re-measured against the committed CSVs."
      status: unverified
      verification: flagged
    - statement: "MUST NOT rewrite `except ValueError, chess.IllegalMoveError:` into a parenthesised tuple anywhere — it is valid Python 3.14 (PEP 758) and the change would be pure churn in a diff that must stay surgical."
      status: unverified
      verification: flagged
  artifacts:
    - app/services/tactic_detector.py
    - tests/services/test_tactic_detector.py
    - tests/scripts/tagger/precision_floors.py
  key_links:
    - "`_TIER3_REGISTRY` -> `_dispatch_tier_candidates` (the dispatcher iterates the REGISTRY only, so removing the tuple is what makes int 14 undispatchable; the `_TIER3_DETECTOR_FNS` entry is inert either way)"
    - "`detect_discovered_attack` depth `k` -> `_select_shallowest_candidate` (the depth-primary tiebreak is what makes same-k forks and skewers now beat it)"
    - "`app/services/tactic_detector.py` -> `tests/scripts/tagger/test_detector_precision.py::PRECISION_FLOOR` (the only automated regression gate for these fixes; the default pytest run does not include it)"
    - "`scripts/research/oracle_compare.py` -> the local lichess-puzzler clone (the manual SC1 parity check that confirms each fix; run in plan 06)"
---

<objective>
Apply the seven fixture-verified cook-port fixes (D-11, D-12 / TAGFIX-05) and take the dead
`self-interference` motif out of dispatch.

Purpose: the 2026-09-12 oracle run over all 26,649 fixture rows proved the detector is a
near-exact cook clone except at seven points. Three of those points cost recall for zero
measured precision (deflection -297, fork -130, trapped-piece -107), one adds 16 false
positives (discovered-attack's `continue` on a recapture), one stores a wrong depth on 100%
of discovered-attack rows, one mis-buckets six boden rows, and one lets a motif with no
lichess theme and zero true positives win dispatch and persist an invisible tag. Fixing
them first makes every later measurement in this phase read against a cook-faithful
baseline instead of a moving one.

Output: `app/services/tactic_detector.py` cook-aligned at all seven points, the one
fast-guard fixture D-11 flips re-labelled rather than left red, and `precision_floors.py`
carrying the re-measured numbers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-CONTEXT.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-RESEARCH.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-PATTERNS.md
@.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-01-SUMMARY.md
@.planning/notes/tactic-tagger-cook-alignment.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-wide roster; the full table with per-plan attribution is in
`221-01-PLAN.md` § Artifacts this phase produces. This plan's share:

| Kind | Symbol / path | Status |
|---|---|---|
| Registry change | `self-interference` tuple removed from `_TIER3_REGISTRY` | new |
| Kept deliberately | `TacticMotifInt.SELF_INTERFERENCE = 14`, `_INT_TO_MOTIF[14]`, `detect_self_interference`, `_TIER3_DETECTOR_FNS["self-interference"]` | unchanged |
| Test (new) | `test_tier3_registry_excludes_self_interference` in `tests/services/test_tactic_detector.py` | new |
| Fixture re-label | `_DISCOVERED_ATTACK_FIXTURES` row at `test_tactic_detector.py:465-469` moved to `_FORK_FIXTURES` with a D-11 note | changed |
| Comments deleted | the WR-02 `k-1` rationale block at `tactic_detector.py:886-892`; the empty-escape exclusion comment at `:951-954`; the fork relevance-gate docstring paragraph at `:389-393` | removed |
| Measurement record | re-measured TRAIN/TEST numbers in every changed `PRECISION_FLOOR` trailing comment | changed |

The dead prose this plan's D-11 change creates in `app/services/forcing_line_gate.py`
lines 299-305 (the slim-blob odd-firing-depth paragraph that exists only because of the
`k-1` quirk) is deliberately NOT touched here — plan 04 owns that file and rewrites the
paragraph in the same pass that adds the winning floor, so the two plans never contend for
it.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| stored PV / SAN strings from the database -> `detect_tactic_motif` | Untrusted-shaped text (a truncated or malformed PV) crosses into board replay |
| the local AGPL lichess-puzzler clone -> this repository | An oracle the fixes are verified against, which must remain an external import |
| detector output -> persisted `game_flaws` tag columns | A wrong predicate silently writes a wrong coaching signal to every user's library |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-221-07 | Tampering (data integrity) | the seven predicate edits | high | mitigate | Every fix is gated by `uv run pytest tests/scripts/tagger -q` against the committed CC0 fixture with unchanged or raised `PRECISION_FLOOR` entries, plus the 23 fast-guard fixture sets in `tests/services/test_tactic_detector.py`. The only fixture allowed to change label is the single D-11 flip, named explicitly with its FEN and PV. |
| T-221-08 | Tampering (data integrity) | `self-interference` removal | medium | mitigate | Only the `_TIER3_REGISTRY` tuple is removed. The int, the reverse map and the detector function stay, so `test_all_29_motifs_encoded`, `test_suppressed_motifs_documented_and_storable` and `test_family_mapping_excludes_suppressed_tier3` keep passing unmodified — those three tests ARE the regression guard for existing persisted rows. |
| T-221-09 | Denial of service | malformed PV replay | low | accept | `_parse_pv` raises `ValueError` and `detect_tactic_motif` is contractually never-raises; none of these fixes adds an unguarded index. The empty-input truths above pin the boundary behaviour. |
| T-221-10 | Tampering (legal/compliance) | AGPL boundary during the port | high | mitigate | Each fix is written from cook's prose description recorded in `221-RESEARCH.md` § Implementation Map 5 and `.planning/notes/tactic-tagger-cook-alignment.md`; the clone is read only by `scripts/research/oracle_compare.py` at analysis time. No cook source is copied. |
| T-221-SC | Tampering | package-manager installs | low | accept | No package install in this plan; the lockfile is untouched, so the package-legitimacy gate has no input. |
</threat_model>

<tasks>

<task type="auto">
  <name>T-221-02-01: Restore the three recall-costing cook divergences (deflection OR, fork gate, trapped-piece empty escape)</name>
  <read_first>
    - `.planning/phases/221-.../221-RESEARCH.md` § Implementation Map 5 rows 1-3 (exact anchors and expected deltas), § Pitfall 6, § Pitfall 9
    - `.planning/notes/tactic-tagger-cook-alignment.md` § Index convention (read before touching any detector loop)
    - `reports/tactic-tagger/tactic-tagger-review-2026-09-12.md` § 4 (the oracle divergence table with the +297 / +130 / +107 measurements)
    - `app/services/tactic_detector.py` lines 380-450 (`detect_fork` docstring + relevance gate + victim loop), lines 1500-1600 (`detect_deflection`'s condition chain through condition 11), lines 900-975 (`_piece_is_trapped` gates 1-5)
    - `tests/scripts/tagger/precision_floors.py` lines 240-330 (the `PRECISION_FLOOR` entries for fork, deflection, trapped-piece and their comment style)
    - `tests/services/test_tactic_detector.py` lines 1560-1640 (`_run_fixtures`, `_HARD_NEGATIVES` — note these build bare boards, RESEARCH Pitfall 3)
  </read_first>
  <files>app/services/tactic_detector.py, tests/scripts/tagger/precision_floors.py</files>
  <action>
Three edits, each replacing a wrong-but-commented line. CLAUDE.md requires the comment at
the fix site to describe the NEW behaviour — rewrite each, do not leave prose describing
the removed rule.

**Fix 1 — deflection promotion branch becomes cook's OR (`detect_deflection`, condition 10,
around `tactic_detector.py:1574-1580`).** Today condition 10 is an if/elif: when the pov
move is a promotion it demands BOTH `same_file` (the capture square shares a file with
`orig_sq`, the deflected piece's origin) AND `pov_attacks_from_init` (`move.from_square in
grandpa_board.attacks(orig_sq)`), and otherwise it demands `square in
grandpa_board.attacks(orig_sq)`. cook's rule is a single OR over the whole condition: the
capture square is reachable from the deflected piece's original square, OR the pov move is
a promotion satisfying the same-file clause. Restructure to one boolean expression with
both disjuncts and a single early return, keeping `grandpa_board` (`boards[k-1]`) as the
board both clauses read — the prior port's use of the init board was already corrected and
must stay corrected. Adjacency contract: the two disjuncts are ORed inside one condition,
so a line satisfying both still produces exactly one deflection candidate. Expected effect
per the oracle table: roughly +297 detections, zero new false positives.

**Fix 2 — remove `detect_fork`'s relevance gate (around `tactic_detector.py:399-408`).**
Delete the two-line guard that `continue`s when the loop index is above zero and the
material at the end of the line is below the material at the start, delete the now-unused
`material_at_start` / `material_at_end` locals immediately above it, and delete the
"Relevance gate (D-01)" paragraph from the docstring (around lines 389-393). This gate is
not cook's; the oracle measured it costing 130 detections for zero precision. Keep the
`[:-1]` last-move exclusion and its note — that one IS cook. Verify `_material_diff` is
still used elsewhere in the function before removing any import-level dependency.

**Fix 3 — revert the trapped-piece empty-escape exclusion (`_piece_is_trapped`, around
`tactic_detector.py:951-954`).** Delete the early `return False` taken when
`escape_moves` is empty. cook's rule is that an attacked, unpinned, non-pawn, non-king
piece in a bad spot with no legal escape IS trapped, and the existing Gate-5 fallthrough
already returns True when the escape loop finds nothing — so deleting the early return is
sufficient, no new code path. Rewrite the docstring paragraph that currently describes the
"Empty-escape-set choice (Open Q 2 / D-06)" precision-first deviation: it now records that
the deviation was measured against the oracle, cost 107 detections, produced zero new false
positives, and was reverted to cook in Phase 221. Expected effect: roughly +107 detections.

**Re-measure and record.** Run `uv run pytest tests/scripts/tagger -q` and read the printed
TRAIN and TEST tables. For every motif whose TP/FP counts moved, update that motif's
`PRECISION_FLOOR` trailing comment with the new measured train/test numbers and the change
that produced them, in the file's established style. Do NOT lower any floor: if a
measured precision drops below its floor, the fix is wrong and must be re-derived against
the oracle, not absorbed by the floor. Expect TP gains on several motifs from the freed
dispatch slots — record them.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/scripts/tagger -q` passes with no `PRECISION_FLOOR` value reduced (`git diff "$(git merge-base HEAD main)"..HEAD -- tests/scripts/tagger/precision_floors.py` shows only comment changes and, if any, increases).
    - `uv run pytest tests/services/test_tactic_detector.py -q` passes with no fixture row edited in this task.
    - Behavioural proof of fix 2 (not a source grep, which the mandatory fix-site comment would invalidate): a new fixture line in which a fork fires at a pov move index above 0 while the material at the END of the line is BELOW the material at the start is now DETECTED as fork; the same line returned not-fired before the task.
    - Behavioural proof of fix 3: `_piece_is_trapped` returns True for a board carrying an attacked, unpinned, non-pawn, non-king piece in a bad spot with ZERO legal escape moves; the same board returned False before the task. Asserted as a direct unit call, not a source inspection.
    - Behavioural proof of fix 1: a fixture line whose pov move is a promotion satisfying the same-file clause but whose capture square is NOT in `grandpa_board.attacks(orig_sq)`, and a second line where only the attacks clause holds, BOTH now fire deflection; each is a named fixture row with an inline comment.
    - The measured TRAIN detection totals for deflection, fork and trapped-piece are all HIGHER than the pre-task values recorded in the SUMMARY, and no motif's FP count increased.
    - `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200` reports no breaches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -q &amp;&amp; uv run pytest tests/scripts/tagger -q</automated>
    <fails_when>non-zero exit, or a "TRAIN precision floor(s) not met" line in the pytest output, or either command reporting 0 collected</fails_when>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ scripts/ &amp;&amp; uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, any "error[" line in the ty output, or the function-size script printing a line containing "depth" or "loc" breach detail</fails_when>
  </verify>
  <done>Deflection's promotion clause, fork's relevance gate and trapped-piece's empty-escape exclusion all match cook; detections rose on all three with no floor lowered and no new false positives; every fix-site comment describes the new behaviour.</done>
</task>

<task type="auto">
  <name>T-221-02-02: Discovered-attack recapture return and depth = k (D-11), plus the boden file edge</name>
  <reversibility rating="costly">D-11 changes the stored `allowed_tactic_depth` / `missed_tactic_depth` of every discovered-attack row (226/226 dev rows carry an odd depth today) and changes which motif wins dispatch at equal k. Reverting after the prod retag means running the same full 3.18M-row retag again with the old code. CONTEXT rated this `costly` and locked it, so no decision checkpoint is re-asked.</reversibility>
  <read_first>
    - `.planning/phases/221-.../221-RESEARCH.md` § Implementation Map 5 rows 4-6, § Pitfall 9 (the two visually identical recapture lines), § Measured experiment — which fixture D-11 re-tunes, § Assumptions Log A2 (the boden root cause is reasoned, not oracle-verified)
    - `.planning/notes/tactic-tagger-cook-alignment.md` § Index convention
    - `app/services/tactic_detector.py` lines 835-900 (`detect_discovered_attack`: the recapture `continue` at ~851-853, the WR-02 comment block and the `max(0, k - 1)` return at ~886-893), lines 700-730 (`detect_skewer`'s identical-looking recapture line at ~720 — DO NOT EDIT), lines 1395-1420 (`detect_boden_or_double_bishop_mate`'s file comparison)
    - `tests/services/test_tactic_detector.py` lines 440-500 (`_DISCOVERED_ATTACK_FIXTURES` and the neighbouring `_FORK_FIXTURES`; the Phase-133 "Reclassified" annotation style at lines 443-453 is the precedent for re-labelling in place)
    - `app/repositories/library_repository.py` lines 145-165 (both boden and double-bishop map to the `mate` family, so the boden fix is cosmetic for the UI and required only for oracle parity)
  </read_first>
  <files>app/services/tactic_detector.py, tests/services/test_tactic_detector.py, tests/scripts/tagger/precision_floors.py</files>
  <action>
**Fix 4 — discovered-attack returns on a recapture.** In `detect_discovered_attack`, the
guard that detects the opponent's prior move landing on the capture square currently
`continue`s to the next pov move index; cook short-circuits the whole predicate. Change it
to return the not-fired triple `(False, None, None)`. Anchor the edit by FUNCTION, not by
string: the identical-looking line inside `detect_skewer` is correct there and must stay a
`continue` — a global search-and-replace would break skewer, which currently measures
precision 1.000 on both splits. Expected effect: roughly 16 false positives removed;
discovered-attack TRAIN precision should rise from 0.990 toward 1.000.

**Fix 5 — discovered-attack depth is `k` (D-11).** Change the success return so the third
element is the loop index `k` instead of the clamped `k - 1`. `k` is a pov MOVE index from
`range(2, len(moves), 2)`, exactly like `detect_skewer`'s return, so this makes the
function match its own docstring and the rest of the tier-2 registry. Delete the WR-02
rationale comment block that explains why the `k - 1` was left alone, and replace it with a
comment naming D-11, stating that the depth is now the pov move index consistent with
skewer, and listing the three accepted consequences: same-k forks and skewers now beat
discovered-attack on the `(tier, rank)` tiebreak because the depth-primary key is equal;
the UI difficulty presentation shifts one ply deeper; and the full retag removes every odd
stored depth for motif 6.

Re-label the single flipping fast-guard fixture. Exactly one of the 23 fixture sets changes
its detected motif: the `_DISCOVERED_ATTACK_FIXTURES` row with FEN
`8/2r3pk/1p4n1/1Pb3Pp/p4P2/P3N1P1/1BP4K/4R3 b - - 0 33` and PV
`c5e3 e1e3 c7c2 h2h3 c2b2` now resolves to `fork` at depth 2 instead of
`discovered-attack` at depth 1. Move that tuple into `_FORK_FIXTURES` (or annotate it in
place in the Phase-133 "Reclassified" style) with an inline comment naming D-11 as the
cause. Nine other discovered-attack fixtures shift depth 1 to 2 without changing motif —
leave them; the fast-guard tests assert the motif, not the depth. Do not delete any
fixture row.

**Fix 6 — boden / double-bishop file edge.** The current classifier decides "bishops on
opposite sides of the king's file" with a pair of `b_file < king_file` comparisons whose
XOR is taken. That expression treats "bishop on the king's own file" identically to "bishop
to the right of the king's file", so a same-file bishop is bucketed as one side. cook's rule
is genuine opposite-sides, which a bishop sitting ON the file satisfies for neither. Make
the side classification three-valued (left of the file, on the file, right of the file) and
require one bishop strictly left and the other strictly right for `boden-mate`; anything
else, including a bishop on the file, falls through to `double-bishop-mate`. Expected
effect: six fixture rows move from double-bishop to boden (or the reverse, depending on the
measured direction). This is the one fix whose root cause RESEARCH marks as reasoned rather
than oracle-verified (Assumptions Log A2): before writing it, run
`uv run python scripts/research/oracle_compare.py` and confirm from its divergence table
that the boden/double-bishop rows are the ones predicted. If the oracle shows a different
cause, stop and record the finding in the SUMMARY rather than shipping a guess — both
motifs map to the `mate` family so nothing user-facing depends on getting this wrong fast.

**Re-measure and record** the same way as task 1: update the `PRECISION_FLOOR` trailing
comments for discovered-attack and any other motif whose counts moved, raising floors where
precision rose and never lowering one.
  </action>
  <acceptance_criteria>
    - Behavioural proof of fix 5 (not a source grep, which the mandatory D-11 fix-site comment would invalidate): on a fixture line where discovered-attack fires, the returned depth equals the pov move index `k` — an EVEN number from `range(2, len(moves), 2)` — asserted by a direct `detect_discovered_attack` unit call; the same line returned an odd depth before the task.
    - Behavioural proof of fix 4: a fixture line in which an EARLY pov move is a recapture onto its capture square while a LATER pov move would satisfy the full predicate returns not-fired. Under the old `continue` it fired at the later index, so this case distinguishes return from continue and nothing else does.
    - Behavioural proof that `detect_skewer` is untouched: its TRAIN and TEST TP/FP counts in `uv run pytest tests/scripts/tagger -q` are identical to the values recorded in the SUMMARY before this task.
    - Replaying the flipped fixture through the dispatcher (`detect_tactic_motif` on the pre-flaw FEN plus its PV) returns the `fork` int at depth 2, and `uv run pytest tests/services/test_tactic_detector.py -q` passes with that row in its new home.
    - Discovered-attack TRAIN precision measured by `uv run pytest tests/scripts/tagger -q` is greater than or equal to the pre-task value recorded in the SUMMARY, and its FP count is lower.
    - `uv run python scripts/research/oracle_compare.py` output for boden-mate / double-bishop-mate is pasted into the SUMMARY, and the implemented three-valued side test matches what that table shows.
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- tests/services/test_tactic_detector.py` shows no fixture row deleted (the deletions count stays at 0 for fixture-table lines).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -q &amp;&amp; uv run pytest tests/scripts/tagger -q</automated>
    <fails_when>non-zero exit, a "TRAIN precision floor(s) not met" line, or `test_positives_fire_expected_motif[discovered-attack]` appearing in the failure list</fails_when>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or any "error[" line in the ty output</fails_when>
  </verify>
  <done>Discovered-attack short-circuits on a recapture and reports depth `k`; the one flipping fast-guard fixture is re-labelled with a D-11 note rather than left red; the boden file edge matches what the oracle comparison actually shows; discovered-attack precision rose and no floor was lowered.</done>
</task>

<task type="auto">
  <name>T-221-02-03: Take `self-interference` out of dispatch while keeping int 14 storable</name>
  <read_first>
    - `.planning/phases/221-.../221-RESEARCH.md` § Implementation Map 5 row 7 (the exact list of what to keep)
    - `app/services/tactic_detector.py` lines 90-115 (`TacticMotifInt`), lines 160-200 (`_INT_TO_MOTIF`, `_MOTIF_TO_INT`), lines 1910-1960 (`detect_self_interference`), lines 2373-2400 (`_TIER3_REGISTRY` and `_TIER3_DETECTOR_FNS`), lines 2495-2520 (the dispatcher's registry iteration)
    - `tests/services/test_tactic_detector.py` lines 1625-1645 (`test_all_29_motifs_encoded`) and lines 1720-1745 (`test_suppressed_motifs_documented_and_storable`)
    - `tests/services/test_tactic_comparison_service.py` lines 201-218 (`test_family_mapping_excludes_suppressed_tier3`, which asserts int 14 is unmapped)
    - `tests/scripts/tagger/precision_floors.py` lines 195-230 (`SUPPRESSED_MOTIFS` and its dated-inline-comment convention)
  </read_first>
  <files>app/services/tactic_detector.py, tests/services/test_tactic_detector.py, tests/scripts/tagger/precision_floors.py</files>
  <action>
Remove exactly one tuple: the `("self-interference", TacticMotifInt.SELF_INTERFERENCE)`
entry in `_TIER3_REGISTRY`. The dispatcher builds its candidate list by iterating the
REGISTRY, so with the tuple gone no PV can produce int 14 — which is the whole point: the
motif has no lichess theme equivalent, zero true positives, and today it can still win
dispatch and persist a tag no chip surface can render.

Keep every other reference. `TacticMotifInt.SELF_INTERFERENCE = 14`, `_INT_TO_MOTIF[14]`,
`_MOTIF_TO_INT["self-interference"]` and `detect_self_interference` all stay, because
persisted prod rows carry int 14 until the retag clears them and three existing tests pin
the encoding. The `_TIER3_DETECTOR_FNS["self-interference"]` dict entry is inert once the
registry tuple is gone (the dispatcher never looks it up); leave it, and add a one-line
comment at the dict entry saying it is retained for direct unit calls and is not reachable
through dispatch. Add a comment above `_TIER3_REGISTRY` recording that the tuple was removed
in Phase 221 (D-12) and why, so the next reader does not "restore" it.

Add a new test `test_tier3_registry_excludes_self_interference` to
`tests/services/test_tactic_detector.py`, asserting on the registry contents rather than on
file text: build the int set from `_TIER3_REGISTRY` and assert that
`int(TacticMotifInt.SELF_INTERFERENCE)` is not a member, and separately assert that
`TacticMotifInt.SELF_INTERFERENCE`, `_INT_TO_MOTIF[14]` and `detect_self_interference` are
all still importable and callable. Both halves in one test so the "remove from dispatch,
keep for storage" contract is stated in one place. Confirm — do not modify —
`test_all_29_motifs_encoded`, `test_suppressed_motifs_documented_and_storable` and
`test_family_mapping_excludes_suppressed_tier3`; their continuing to pass unmodified is the
regression signal.

In `precision_floors.py`, add a dated inline comment inside the `SUPPRESSED_MOTIFS` set body
(the file's convention for recording membership changes without deleting lines) noting that
`self-interference` stays in the suppressed set AND is now undispatchable as of Phase 221
(D-12), and that the retag clears persisted int-14 rows — dev has zero such rows, so only
the prod acceptance query can verify that leg.

Then run the full local pre-merge gate on this plan's slice and record the measured
per-motif deltas from all three tasks as one table in the SUMMARY: TP/FP/precision/recall
before and after, per motif, for TRAIN and TEST, plus the freed-dispatch-slot gains. That
table is plan 06's input for raising the floors.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import sys;sys.path.insert(0,'.');from app.services.tactic_detector import _TIER3_REGISTRY, TacticMotifInt, _INT_TO_MOTIF, detect_self_interference;assert int(TacticMotifInt.SELF_INTERFERENCE) not in {int(i) for _, i in _TIER3_REGISTRY};assert _INT_TO_MOTIF[14] == 'self-interference';assert callable(detect_self_interference)"` exits 0.
    - `uv run pytest tests/services/test_tactic_detector.py tests/services/test_tactic_comparison_service.py -q` passes, and `git diff "$(git merge-base HEAD main)"..HEAD -- tests/services/test_tactic_comparison_service.py` is empty.
    - `test_tier3_registry_excludes_self_interference` appears in `uv run pytest tests/services/test_tactic_detector.py --collect-only -q` output.
    - `uv run pytest tests/scripts/tagger -q` passes with no floor lowered.
    - The SUMMARY carries a per-motif before/after table for TRAIN and TEST covering all three tasks.
    - `uv run pytest -n auto -x` passes (nothing outside the tagger harness regressed).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py tests/services/test_tactic_comparison_service.py -q &amp;&amp; uv run pytest tests/scripts/tagger -q</automated>
    <fails_when>non-zero exit, a "TRAIN precision floor(s) not met" line, or `test_tier3_registry_excludes_self_interference` missing from the collected set</fails_when>
    <automated>uv run pytest -n auto -x</automated>
    <fails_when>non-zero exit, or the summary line reporting any failure or error</fails_when>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ scripts/ &amp;&amp; uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, any "error[" line in the ty output, or a breach line from the function-size script</fails_when>
  </verify>
  <done>Int 14 can never be dispatched, every int-14 storage path still resolves, the three pre-existing encoding/family tests pass unmodified, and the phase's port-fix measurement table is recorded.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/services/test_tactic_detector.py -q` — the 23 fast-guard fixture
  sets, with exactly one row re-labelled.
- `uv run pytest tests/scripts/tagger -q` — the CC0 precision gate. Mandatory: the default
  pytest run ignores `tests/scripts/tagger`, so `-n auto` proves nothing here.
- `uv run pytest -n auto -x` — no regression anywhere else.
- `uv run ruff check .`, `uv run ty check app/ tests/ scripts/`,
  `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200`.
- `uv run python scripts/research/oracle_compare.py` — run for the boden confirmation in
  task 2; the full SC1 parity assertion is plan 06's job.
</verification>

<success_criteria>
- All seven port divergences are cook-aligned, each with a rewritten fix-site comment.
- Detections rose for deflection, fork and trapped-piece; false positives fell for
  discovered-attack; no `PRECISION_FLOOR` value was lowered.
- Discovered-attack reports depth `k`; the single flipping fixture is re-labelled in place
  with a D-11 note and no fixture row was deleted.
- `self-interference` cannot win dispatch, while int 14 remains encodable, decodable and
  directly callable.
- The per-motif before/after measurement table is recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/221-tactic-tagger-real-game-precision-winning-floor-predicate-ti/221-02-SUMMARY.md` when done.

Record in it: the per-motif TRAIN/TEST TP/FP/precision/recall before and after for all
three tasks; the oracle divergence rows for boden/double-bishop that justified fix 6 (or
the finding that contradicted the assumption); the fixture row that was re-labelled and
where it now lives; and every `PRECISION_FLOOR` comment that changed.
</output>
