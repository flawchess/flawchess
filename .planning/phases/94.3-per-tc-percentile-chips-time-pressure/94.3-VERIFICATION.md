---
phase: 94.3-per-tc-percentile-chips-time-pressure
verified: 2026-05-24T20:00:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Production backfill rerun"
    expected: "Operator runs `scripts/backfill_user_percentiles.py --target prod` after the migration ships; the run lights up the 12 new per-(metric × TC) percentile rows for existing users so chips render on flawchess.com Time Pressure cards"
    why_human: "Requires prod SSH tunnel + multi-minute prod-side execution + human visual confirmation that chips render correctly on prod after backfill — see 94.3-05-HUMAN-UAT.md (explicitly deferred by user)"
follow_ups:
  - "WR-02 (net_flag denominator mismatch with `net_timeout_rate`): chip cohort uses `JOIN endgame_entry_clocks ee` which drops endgame games with no recorded clock; the same-card `net_timeout_rate` does not. Recommend dropping the JOIN from `per_user_cte_net_flag_rate` to align denominators. Deferred to follow-up."
  - "WR-03 (time_pressure_score_gap redundant WHERE): `WHERE user_clock_pct IS NOT NULL AND opp_clock_pct IS NOT NULL` in `per_user_cte_time_pressure_score_gap` causes asymmetric drop on patchy-clock users. Recommend removing or documenting. Deferred to follow-up."
  - "WR-04 (migration ENUM-value f-string): hardcoded ASCII literals only; no current injection vector but a defensive regex guard would prevent silent breakage on future maintenance. Deferred to follow-up."
  - "IN-01 (stale comment in `_compute_and_count` Stage B branch — '# Stage B computes all 3 metrics'). Comment was made dead by the WR-01 fix; tracker reminder to either delete it or update to '15 metrics'."
  - "IN-02 (`tcFromFlavor` micro-test for pre-94.3 flavors). Nice-to-have."
  - "IN-03 (`_endgame_entry_clocks_cte` duplicates `ENDGAME_PLY_THRESHOLD=6` instead of hoisting a module constant). Nice-to-have."
---

# Phase 94.3: Per-TC Percentile Chips on Time Pressure Cards — Verification Report

**Phase Goal:** Extend the Phase 94.2 pooled-per-user percentile contract to the Time Pressure section. Add 12 new metrics (`{time_pressure_score_gap, clock_gap, net_flag_rate}_{bullet, blitz, rapid, classical}`) with 3 new per-TC SQL builder families, 12 new CDF entries, 12 chip placements (3 per `TimePressureTcCard`), mobile + desktop parity. ENUM extension only — no other schema change.

**Verified:** 2026-05-24T20:00:00Z
**Status:** human_needed (technical implementation complete; prod backfill UAT deferred to user per `94.3-05-HUMAN-UAT.md`)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1–7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `canonical_slice_sql.py` exposes 3 new per-TC builder families; `per_user_cte_for` dispatcher gains 12 arms; both `gen_global_percentile_cdf.py` AND `user_benchmark_percentiles_service.py` consume the SAME shared builder (drift impossible) | VERIFIED | `app/services/canonical_slice_sql.py`: `per_user_cte_time_pressure_score_gap` (line 587), `per_user_cte_clock_gap` (line 669), `per_user_cte_net_flag_rate` (line 726); `per_user_cte_for` dispatcher has 12 new `if metric_id == ...` arms (lines 825–856). `scripts/gen_global_percentile_cdf.py:IN_SCOPE_METRICS` and `app/services/user_benchmark_percentiles_service.py:STAGE_B_METRICS` both reference `CdfMetricId` from `global_percentile_cdf` — single source of truth. Runtime probe: all 12 dispatcher arms invoked successfully. |
| 2 | `benchmark_metric` ENUM extended with 12 new values via Alembic migration; row shape reused verbatim | VERIFIED | `alembic/versions/20260524_170733_fd5b551f381c_extend_benchmark_metric_for_tc_pressure.py` adds 12 values via `ALTER TYPE benchmark_metric ADD VALUE IF NOT EXISTS`. `downgrade()` is a documented no-op stub (Postgres cannot drop ENUM values without table rewrite — per CONTEXT D-7). Dev DB query confirms 16 ENUM labels present. `alembic current` = `fd5b551f381c (head)`. No schema change beyond ENUM extension. |
| 3 | `GLOBAL_PERCENTILE_CDF` gains 12 entries; `interpolate_percentile` signature unchanged | VERIFIED | `app/services/global_percentile_cdf.py`: `CdfMetricId` Literal expanded to 16 entries (lines 80–97). `GLOBAL_PERCENTILE_CDF` registry has 16 `CdfTable` entries (lines 147, 252, 357, 462, 567, 672, 777, 882, 987, 1092, 1197, 1302, 1407, 1512, 1617, 1722). Each entry has 99 breakpoints + `n_users` populated. `interpolate_percentile(metric_id: MetricId \| CdfMetricId, value: float) -> float \| None` signature at line 1875 — unchanged. Runtime probe confirms `len(GLOBAL_PERCENTILE_CDF) == 16`. |
| 4 | All 12 per-user computations run in Stage B; `STAGE_B_METRICS` has 15 entries (3 prior + 12 new) | VERIFIED | `app/services/user_benchmark_percentiles_service.py:STAGE_B_METRICS` (line 76) contains exactly 15 entries: 3 prior (`achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`) + 12 new per-(metric × TC). `STAGE_A_METRIC == "score_gap"` unchanged (line 75). Runtime probe: `len(STAGE_B_METRICS) == 15`, `STAGE_A_METRIC == "score_gap"`. |
| 5 | `scripts/backfill_user_percentiles.py` extended with 12 new metrics in `--metric` choices | VERIFIED | `scripts/backfill_user_percentiles.py:513` uses dynamic `choices=list((STAGE_A_METRIC, *STAGE_B_METRICS))` = 16 entries — cannot drift from service-layer tuple. Phase 94.3 WR-01 fix landed at lines 392–412: `compute_stage_b` called ONCE per user, then per-metric row probe classifies the outcome (no more 225× CTE re-execution per user). Plan 05 SUMMARY documents dev backfill ran successfully against 17 users with per-metric upserted/skipped counts. |
| 6 | Each `TimePressureTcCard` renders 3 `PercentileChip` instances; below-floor → no chip; 4-bullet popover with first two TC-scoped | VERIFIED | `app/schemas/endgames.py:TimePressureTcCard` (line 755) gains 3 nullable percentile fields (lines 801–803). `app/services/endgame_service.py:_compute_time_pressure_cards` (line 1923) attaches percentiles per TC at lines 2021–2025 with `_TC_TO_METRIC_KEYS` dispatch — null when below floor. `frontend/src/components/charts/EndgameTimePressureCard.tsx` renders 3 `<PercentileChip>` instances (lines 180–187 Clock Gap, 235–244 Net Flag Rate, 371–380 Time Pressure Score Gap), each gated on `!= null`. `EndgameTimePressureSection.tsx` renders one `EndgameTimePressureCard` per TC in a responsive grid → 3 × 4 = 12 effective placements. Mobile+desktop parity via single responsive grid component (no duplicated markup). CR-01 BLOCKER fixed: `cohortOutranked = isLowerBetter ? 100 - percentile : percentile` applied to both `lowerBetterLine` and `bullet1` (line 315). 98 frontend tests pass. |
| 7 | SEED-025 open design questions resolved (Net Flag direction, Clock Gap semantics, inclusion floor, <40% cutpoint, Cohen's d table) | VERIFIED | `94.3-CONTEXT.md` documents D-1..D-16 lock-ins: D-2 (direction prop on PercentileChip), D-3 (Net Flag "Lower is better" line), D-4 (Clock Gap "Top X% = furthest positive gap"), D-5 (per-(metric × TC) Cohen's d candidacy table), D-6 (inclusion floor locked at ≥30 + 40% cutpoint locked from seed defaults; no benchmark probes), D-7 (12 ENUM values via reversible migration). `reports/benchmarks-gap-metrics-percentile-candidacy.md` contains the 12-cell tier table with required columns (metric × TC, n_users, n_obs, pooled median, pooled skew, 800 median, 2400 median, Cohen's d, Tier, Tooltip 4th-bullet copy). |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/services/canonical_slice_sql.py` | 3 new per-TC builder families + 12 dispatcher arms | VERIFIED | `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`, all parameterised by `tc: TimeControlBucket` Literal. `_recent_capped_per_tc_cte` shared helper. `_endgame_entry_clocks_cte` shared helper. Dispatcher widened by 12 arms in `per_user_cte_for`. Unit tests pass (167 in `tests/services/test_canonical_slice_sql.py` + `test_user_benchmark_percentiles_service.py`). |
| `app/services/global_percentile_cdf.py` | 16-entry `GLOBAL_PERCENTILE_CDF` + widened `CdfMetricId` | VERIFIED | 16 `CdfTable` entries, 99 breakpoints each, monotone non-decreasing; `CdfMetricId` Literal has 16 keys; `interpolate_percentile` signature unchanged. |
| `app/services/user_benchmark_percentiles_service.py` | `STAGE_B_METRICS` widened to 15 entries | VERIFIED | 15 entries (3 prior + 12 new); `compute_stage_b` iterates all 15 internally — single CTE pass per user. |
| `app/schemas/endgames.py` | `TimePressureTcCard` gains 3 nullable percentile fields | VERIFIED | `time_pressure_score_gap_percentile`, `clock_gap_percentile`, `net_flag_rate_percentile` all default to `None` for backward compat with existing tests. |
| `app/services/endgame_service.py` | `_compute_time_pressure_cards` attaches percentiles | VERIFIED | `_TC_TO_METRIC_KEYS` dispatch maps each TC to the 3 typed `CdfMetricId` keys; per-card row lookup via `_effective_rows.get(...)`. |
| `alembic/versions/...fd5b551f381c_extend_benchmark_metric_for_tc_pressure.py` | 12 ENUM values + documented no-op downgrade | VERIFIED | Migration uses `ADD VALUE IF NOT EXISTS` (Postgres 18 idempotent), values ordered to mirror `CdfMetricId` / `STAGE_B_METRICS`. `downgrade()` carries detailed rollback caveat. Applied to dev DB (`alembic current` = `fd5b551f381c (head)`). |
| `frontend/src/components/charts/PercentileChip.tsx` | 16 flavors + `DIRECTION_BY_FLAVOR` axis + CR-01 fix | VERIFIED | `PercentileChipFlavor` widened from 4 to 16 (4 prior kebab-case + 12 new snake_case). `DIRECTION_BY_FLAVOR` `as const satisfies Record<...>` enforces compile-time exhaustiveness (4 `net_flag_rate_*` flavors → `lower_is_better`; others → `higher_is_better`). CR-01 fix landed: `cohortOutranked` computed once (line 315) and used in both `lowerBetterLine` and `bullet1` — popover no longer contradicts the chip face. `PERCENTILE_METRIC_LABELS` + `RATING_NOTE_BY_FLAVOR` both exhaustive (`satisfies Record<PercentileChipFlavor, string>`). |
| `frontend/src/components/charts/EndgameTimePressureCard.tsx` | 3 chip slots per card, null-gated, 4 TC cards | VERIFIED | 3 `<PercentileChip>` instances inside the single component; rendered once per TC card by `EndgameTimePressureSection` in a responsive grid. Mobile+desktop parity via single component (no duplicated markup). Test file `EndgameTimePressureCard.test.tsx` exists and 98 tests pass. |
| `scripts/backfill_user_percentiles.py` | `--metric` choices = 16; WR-01 fix | VERIFIED | Dynamic `choices=list((STAGE_A_METRIC, *STAGE_B_METRICS))`; WR-01 fix: `compute_stage_b` called once, per-metric row probe classifies outcome. Dev backfill summary in `94.3-05-SUMMARY.md`. |
| `reports/benchmarks-gap-metrics-percentile-candidacy.md` | 12-cell Cohen's d table | VERIFIED | `## Time Pressure metric family (Phase 94.3)` section appended; 12-row tier table with all required columns; tier labels drawn from existing ladder; per-cell tooltip 4th-bullet copy strings consumed verbatim by `RATING_NOTE_BY_FLAVOR` in `PercentileChip.tsx`. Classical cells flagged with `n_users < 150 — chip suppression candidate`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `canonical_slice_sql.per_user_cte_for` | `gen_global_percentile_cdf.py` | shared builder import | WIRED | `scripts/gen_global_percentile_cdf.py:IN_SCOPE_METRICS` calls `per_user_cte_for(metric_id, source="benchmark", ...)` at lines 430, 625, 666, 745. |
| `canonical_slice_sql.per_user_cte_for` | `user_benchmark_percentiles_service._compute_metric_for_user` | shared builder import | WIRED | `app/services/user_benchmark_percentiles_service.py:132` calls `per_user_cte_for(metric_id, source="single_user", snapshot_date=None)`. Drift structurally impossible. |
| `endgame_service._compute_time_pressure_cards` | `TimePressureTcCard` nullable percentile fields | per-TC dispatch via `_TC_TO_METRIC_KEYS` | WIRED | Lines 2003–2007 lookup; 2021–2025 assignment. |
| `TimePressureTcCard.{...}_percentile` | `EndgameTimePressureCard` chip rendering | React prop drilling | WIRED | All 3 fields consumed via `card.clock_gap_percentile`, `card.net_flag_rate_percentile`, `card.time_pressure_score_gap_percentile`; each conditional-rendered behind `!= null`. |
| `RATING_NOTE_BY_FLAVOR` copy | candidacy report 4th-bullet column | manual copy from report → code | WIRED | All 12 copy strings match the report verbatim (verified via spot-grep). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `EndgameTimePressureCard` percentile chips | `card.{clock_gap,net_flag_rate,time_pressure_score_gap}_percentile` | Backend `_effective_rows[CdfMetricId].percentile` from `user_benchmark_percentiles` table, populated by Stage B compute | YES — dev DB has rows for 13 of 16 metrics; classical TC cells (3 metrics) lack rows due to dev-cohort thinness — expected null suppression per inclusion-floor contract (CONTEXT D-6) | FLOWING (dev) — prod data flow contingent on deferred prod backfill UAT |
| `GLOBAL_PERCENTILE_CDF` | breakpoints + n_users | `scripts/gen_global_percentile_cdf.py` against benchmark DB | YES — n_users populated for all 16 entries; breakpoints span realistic Δ ranges | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `len(GLOBAL_PERCENTILE_CDF) == 16` | `python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF; print(len(GLOBAL_PERCENTILE_CDF))"` | `16` | PASS |
| `len(STAGE_B_METRICS) == 15` | `python -c "from app.services.user_benchmark_percentiles_service import STAGE_B_METRICS; print(len(STAGE_B_METRICS))"` | `15` | PASS |
| All 12 dispatcher arms invoke without error | iterate `per_user_cte_for(f'{fam}_{tc}', ...)` over 12 combinations | All 12 produce non-empty SQL with `per_user_values` clause | PASS |
| Migration applied to dev DB | `uv run alembic current` | `fd5b551f381c (head)` | PASS |
| Dev DB has 16 ENUM values | `psql ... pg_enum WHERE enumtypid = 'benchmark_metric'::regtype` | 16 labels present (4 prior + 12 new) | PASS |
| Backend unit tests pass | `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_user_benchmark_percentiles_service.py -x` | 167 passed in 0.25s | PASS |
| Frontend chip tests pass | `npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx EndgameTimePressureCard.test.tsx` | 98 passed | PASS |

### Probe Execution

No phase-declared probes (this phase has no `scripts/*/tests/probe-*.sh` and SUMMARY/PLAN reference standard pytest + vitest gates instead). SKIPPED.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SEED-025 | All 6 plans | Per-TC Percentile Chips on Time Pressure Cards | SATISFIED | Implementation complete across Plans 01–06; design questions resolved via D-1..D-16. |
| TPCTL-01 | Plan 02 | 3 per-TC pooled-aggregate SQL builder families + shared consumer | SATISFIED | `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate` in `canonical_slice_sql.py`. |
| TPCTL-02 | Plan 04 | `benchmark_metric` ENUM extended with 12 values via Alembic migration; row shape reused verbatim | SATISFIED | `fd5b551f381c` migration; documented forward-only downgrade. |
| TPCTL-03 | Plan 03 | `GLOBAL_PERCENTILE_CDF` gains 12 entries; `interpolate_percentile` signature unchanged | SATISFIED | 16 entries; `interpolate_percentile(metric_id: MetricId \| CdfMetricId, value: float)` unchanged. |
| TPCTL-04 | Plan 04 | All 12 per-user computations run in Stage B | SATISFIED | `STAGE_B_METRICS` has 15 entries; `STAGE_A_METRIC == "score_gap"` unchanged. |
| TPCTL-05 | Plan 05 | `scripts/backfill_user_percentiles.py` extended with 12 new metrics; re-run against dev (prod via tunnel after sign-off) | SATISFIED (dev) / PENDING (prod) | Script supports all 16 metrics via dynamic `choices`. Dev backfill ran successfully (per Plan 05 SUMMARY). Prod backfill explicitly deferred to user — see `94.3-05-HUMAN-UAT.md` and `human_verification` block above. |
| TPCTL-06 | Plan 06 | Each `TimePressureTcCard` renders 3 chips with inclusion-floor null-gating; mobile + desktop parity; 4-bullet popover with first two TC-scoped; Net Flag Rate prepends "Lower is better" line | SATISFIED | 3 `<PercentileChip>` instances per card with `!= null` gating; single responsive component → desktop+mobile parity; CR-01 BLOCKER fixed; `lowerBetterLine` prepended only on `net_flag_rate_*` flavors via `DIRECTION_BY_FLAVOR` axis. |
| TPCTL-07 | Plan 01 | 12-cell Cohen's d candidacy extension in `reports/benchmarks-gap-metrics-percentile-candidacy.md` | SATISFIED | 12-row tier table appended; tier labels drawn from existing ladder; classical cells flagged with `n_users < 150 — chip suppression candidate`. |

**No orphaned requirements** — all 8 IDs (SEED-025, TPCTL-01..07) appear in plan frontmatter and have implementation evidence in the codebase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/backfill_user_percentiles.py` | 437–439 | Stale comment "# Stage B computes all 3 metrics" (post-94.3 it's 15) | Info | Confusing for future readers; comment is now dead per WR-01 fix; tracked as follow-up IN-01. |
| `app/services/canonical_slice_sql.py` | 656 | Redundant `WHERE user_clock_pct IS NOT NULL AND opp_clock_pct IS NOT NULL` causes asymmetric drop on patchy-clock users | Warning | WR-03 — not a correctness blocker (FILTER clauses already gate nulls) but causes harder-to-meet inclusion floor than the docstring implies; deferred follow-up. |
| `app/services/canonical_slice_sql.py` | 753–786 vs `endgame_service.py` 1994–1998 | `per_user_cte_net_flag_rate` denominator differs from same-card `net_timeout_rate` denominator (JOIN on `endgame_entry_clocks` silently drops endgames with missing clock data) | Warning | WR-02 — wire-format inconsistency between chip percentile cohort and card-displayed value; can confuse users with patchy clock data; deferred follow-up. |
| `alembic/versions/.../fd5b551f381c_*.py` | 70–71 | `op.execute(f"ALTER TYPE ... '{value}'")` interpolates value into raw SQL | Info / Warning | WR-04 — no current injection vector (hardcoded ASCII literals); defensive regex guard recommended; deferred follow-up. |

No BLOCKER-grade anti-patterns. The single REVIEW BLOCKER (CR-01) was already fixed and verified in the codebase (commit a1f4b56b per task description); WR-01 (backfill 15× overcompute) was already fixed (commit 32050042).

### Human Verification Required

#### 1. Production backfill rerun

**Test:** Run `scripts/backfill_user_percentiles.py --target prod` via `bin/prod_db_tunnel.sh` against the production PostgreSQL after the migration ships to prod.

**Expected:** The rerun lights up 12 new per-(metric × TC) percentile rows for existing users. After completion, chips render on flawchess.com Time Pressure cards for users who clear the per-TC ≥30 inclusion floor.

**Why human:** Requires production SSH tunnel, multi-minute prod-side execution touching the entire user base, and visual confirmation that chips render correctly post-backfill. Explicitly deferred by the user at the Plan 05 execute boundary ("skip HUMAN UAT, I'll do it when you're finished with the whole phase"). The prod chips will render empty for existing users until this rerun completes — Plan F (frontend chip render) ships safely independent of this UAT because empty chips suppress silently. See `94.3-05-HUMAN-UAT.md` for the full runbook.

### Gaps Summary

No goal-blocking gaps. All 7 ROADMAP success criteria are independently verified in the codebase via:
- File-content inspection at the exact module / line locations called out by the plan frontmatter and SUMMARYs.
- Runtime probes (16-entry CDF, 15-entry STAGE_B_METRICS, 12 dispatcher arms, 16 dev-DB ENUM labels, 13/16 metric rows present in dev DB — the 3 missing are classical-TC and expected per CONTEXT D-6 sparse-cohort handling).
- Test suite execution (167 backend tests + 98 frontend chip tests pass).
- Migration applied (`alembic current` = `fd5b551f381c (head)`).
- The 2 REVIEW.md issues that gated this phase (BLOCKER CR-01, WARNING WR-01) were fixed pre-verification in commits a1f4b56b and 32050042; the remaining REVIEW items (WR-02..WR-04, IN-01..IN-03) are quality follow-ups documented in the frontmatter `follow_ups` block — none affect goal achievement.

The only outstanding item is the **production backfill UAT**, which is by user instruction a post-phase deliverable. Status is `human_needed` rather than `passed` solely on that basis.

---

_Verified: 2026-05-24T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
