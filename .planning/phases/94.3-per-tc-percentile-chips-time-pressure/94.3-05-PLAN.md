---
phase: 94.3-per-tc-percentile-chips-time-pressure
plan: 05
type: execute
wave: 2
depends_on: [94.3-02, 94.3-03, 94.3-04]
files_modified:
  - scripts/backfill_user_percentiles.py
autonomous: false
requirements: [SEED-025, TPCTL-05]

must_haves:
  truths:
    - "`scripts/backfill_user_percentiles.py:--metric` argparse choices list widens from 4 to 16 entries. Recommended implementation per RESEARCH §Pattern 6 lines 689-695: replace the inline tuple with `list((STAGE_A_METRIC, *STAGE_B_METRICS))` so the choice list tracks the service-layer tuple automatically. If an import cycle prevents that, keep the inline list AND add a unit test asserting `set(parser_choices) == {STAGE_A_METRIC, *STAGE_B_METRICS}` so the lists cannot drift silently."
    - "The script's per-metric summary loop (lines 305-349) requires NO code change — `all_metrics = (STAGE_A_METRIC, *STAGE_B_METRICS)` automatically picks up the 12 new entries once Plan D widens STAGE_B_METRICS. The summary table grows from 4 rows to 16 rows automatically. The 36-char column width on `{metric_id:<36}` accommodates the new longer metric IDs (max length: `time_pressure_score_gap_classical` = 32 chars)."
    - "`uv run python scripts/backfill_user_percentiles.py --target dev` runs cleanly against the dev DB after Plans B+C+D are merged. The summary table emits 16 rows. For each per-TC metric, the upserted + skipped_below_pooled_floor count sums equal the total user count processed."
    - "A HUMAN-UAT checkpoint gates the prod backfill rerun. The operator opens the prod tunnel via `bin/prod_db_tunnel.sh`, runs `uv run python scripts/backfill_user_percentiles.py --target prod`, captures the per-(metric × TC) summary into the plan summary, then closes the tunnel with `bin/prod_db_tunnel.sh stop`. Per `feedback_no_dev_db_reset_in_plans`, no dev DB reset is gated on completion — the dev rerun is autonomous; prod is operator-gated."
    - "If the dev or prod backfill summary surfaces ANY per-(metric × TC) cell with significantly low coverage (e.g. `upserted < 50` at classical), the executor documents the affected cells in the plan summary so Plan F can apply the chip-suppression escape hatch (per RESEARCH §Pitfall 8 + 94.3-01-SUMMARY.md flagged cells)."
  artifacts:
    - path: scripts/backfill_user_percentiles.py
      provides: argparse choices widened to 16 metrics; summary loop unchanged
      contains: "STAGE_A_METRIC, *STAGE_B_METRICS"
  key_links:
    - from: scripts/backfill_user_percentiles.py
      to: app/services/user_benchmark_percentiles_service.py
      via: STAGE_A_METRIC + STAGE_B_METRICS tuple imports
      pattern: "STAGE_A_METRIC|STAGE_B_METRICS"
---

<objective>
Widen `scripts/backfill_user_percentiles.py`'s argparse `--metric` choices from 4 to 16 entries so the script accepts the 12 new per-TC metric IDs Plan D introduced. Rerun the script against the dev DB to verify the summary-table output renders 16 rows correctly. Then gate a HUMAN-UAT checkpoint for the prod backfill rerun via `bin/prod_db_tunnel.sh`. The summary loop machinery (`all_metrics = (STAGE_A_METRIC, *STAGE_B_METRICS)`) requires no further code change — Plan D's STAGE_B_METRICS widening flows through.

Purpose: Without this rerun, existing users do NOT have rows in `user_benchmark_percentiles` for the 12 new metrics until their next Stage B drain. Most existing users completed their cold drain before Phase 94.3 shipped, so without the rerun the chips on Time Pressure cards would be empty (`null` percentiles → no chip rendered) for the entire user base. Stage B triggers cover only NEW imports + future eval-drain completions; the one-shot backfill is the only mechanism that lights up the chips at rollout.

Output: Updated argparse choices in the script; green dev backfill run with summary captured; blocking HUMAN-UAT checkpoint for the prod rerun.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-CONTEXT.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-01-SUMMARY.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-04-SUMMARY.md
@scripts/backfill_user_percentiles.py
@scripts/import_stress_monitor.py
@bin/prod_db_tunnel.sh

<interfaces>
<!-- Script structure (current 94.2 post-state) extracted from PATTERNS.md
     §`scripts/backfill_user_percentiles.py` (lines 673-721). -->

# argparse choices (lines 512-525) — Plan E widens from 4 to 16:
parser.add_argument(
    "--metric",
    choices=list((
        "score_gap",
        "achievable_score_gap",
        "section2_score_gap_conv",
        "section2_score_gap_parity",
    )),
    default=None,
    help="Process only this metric (optional; default: all metrics).",
)

# Per-metric summary loop (lines 305-349) — UNCHANGED. The driver is:
all_metrics: tuple[CdfMetricId, ...] = (STAGE_A_METRIC, *STAGE_B_METRICS)
summary: dict[CdfMetricId, _MetricSummary] = {m: _MetricSummary() for m in all_metrics}
...
for metric_id in all_metrics:
    s = summary[metric_id]
    reasons = [...]
    print(f"  {metric_id:<36} upserted={s.upserted}, skipped={s.total_skipped}{reason_str}")

# Target convention (lines 13-19) from scripts/import_stress_monitor.py — UNCHANGED:
#   --target dev  → localhost:5432  (Docker dev DB)
#   --target prod → localhost:15432 (prod tunnel via bin/prod_db_tunnel.sh)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen argparse --metric choices to 16 entries</name>
  <files>scripts/backfill_user_percentiles.py</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (full file — particularly lines 305-349 for the summary loop and lines 470-540 for the argparse setup)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`scripts/backfill_user_percentiles.py` (lines 673-721)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-04-SUMMARY.md (confirm STAGE_B_METRICS final composition + check for import cycle warnings)
  </read_first>
  <action>
    Replace the inline 4-tuple in the `--metric` argparse `choices=...` with `list((STAGE_A_METRIC, *STAGE_B_METRICS))` per RESEARCH §Pattern 6 lines 689-695. If this causes a circular import or the script imports `STAGE_A_METRIC` / `STAGE_B_METRICS` from a module that itself imports from the script, fall back to keeping the inline list AND add a unit test in `tests/scripts/test_backfill_user_percentiles_choices.py` (NEW or extend existing) asserting `set(<argparse choices>) == {STAGE_A_METRIC, *STAGE_B_METRICS}` from `app.services.user_benchmark_percentiles_service`. Leave the summary-loop machinery and per-user driver untouched — they pick up the new metrics through the `all_metrics` tuple automatically. Verify with `uv run python scripts/backfill_user_percentiles.py --help` that all 16 metric names appear in the help output.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_user_percentiles.py --help | grep -cE '(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)' | awk '$1 >= 12 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/backfill_user_percentiles.py --help` includes all 12 new metric IDs in the `--metric` choices help text — verifiable by `grep -cE '(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)' <help output>` ≥ 12.
    - Either `grep -c 'STAGE_A_METRIC, \*STAGE_B_METRICS' scripts/backfill_user_percentiles.py` returns ≥ 1 (preferred dynamic form) OR a new drift-guard test exists at `tests/scripts/test_backfill_user_percentiles_choices.py` asserting set equality.
    - `uv run ty check app/ tests/ scripts/` exits 0.
    - `uv run ruff check scripts/` exits 0.
    - `uv run pytest tests/scripts/ -x` exits 0 (if a drift-guard test was added; otherwise no-op).
  </acceptance_criteria>
  <done>Argparse choices include all 16 metric names; the choice list cannot drift silently from STAGE_A/B_METRICS.</done>
</task>

<task type="auto">
  <name>Task 2: Run the dev backfill and capture the 16-row summary</name>
  <files>(no file modified — operational task; output captured in plan summary)</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (post-Task-1 state)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`scripts/backfill_user_percentiles.py` (lines 673-721)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pitfall 6 (16 rows expected, not 12 new)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-01-SUMMARY.md "Cells flagged for suppression" subsection
  </read_first>
  <action>
    Confirm the dev DB is running: `docker compose -f docker-compose.dev.yml -p flawchess-dev ps`. Confirm Plan D's migration has applied: `uv run alembic current` (should reference the new `extend_benchmark_metric_for_tc_pressure` revision). Run the dev backfill: `uv run python scripts/backfill_user_percentiles.py --target dev`. Capture the full stdout summary into the plan summary file. For each of the 16 metric rows, verify: (a) the row prints; (b) `upserted + total_skipped` equals the total user count processed; (c) for the 12 new per-TC metrics specifically, note any cell whose `upserted` count is unexpectedly low. Cross-reference with 94.3-01-SUMMARY.md's flagged-cells list. If any new cell has `upserted < 50` (or is otherwise materially below the cohort sizes Plan A measured), explicitly document the cell in the plan summary and flag it for Plan F's chip-suppression awareness. Spot-check 2-3 representative dev users via `psql` (or `mcp__flawchess-db__query`): `SELECT metric, value, percentile, n_games FROM user_benchmark_percentiles WHERE user_id IN (<id1>, <id2>) ORDER BY user_id, metric;` — confirm the 12 new metric keys are populated for users who clear the floor and absent for users who don't.
  </action>
  <verify>
    <automated>uv run python -c "import asyncio; from app.db import AsyncSessionLocal; from sqlalchemy import text; \
async def _check(): \
    async with AsyncSessionLocal() as s: \
        r = await s.execute(text(\"SELECT COUNT(DISTINCT metric) FROM user_benchmark_percentiles WHERE metric IN ('time_pressure_score_gap_bullet','time_pressure_score_gap_blitz','time_pressure_score_gap_rapid','time_pressure_score_gap_classical','clock_gap_bullet','clock_gap_blitz','clock_gap_rapid','clock_gap_classical','net_flag_rate_bullet','net_flag_rate_blitz','net_flag_rate_rapid','net_flag_rate_classical')\")); \
        n = r.scalar_one(); \
        assert n >= 1, f'Expected at least one new metric populated in dev DB, got {n}'; \
        print(f'OK: {n} new per-TC metric IDs present in dev user_benchmark_percentiles'); \
asyncio.run(_check())"</automated>
    <human-check>The plan summary contains the full backfill stdout table with 16 rows. Per-cell counts are reasonable (no cell at 0 upserted unless the dev DB cohort genuinely lacks players in that TC, e.g. classical). Any cell with `upserted < 50` is explicitly listed for Plan F awareness.</human-check>
  </verify>
  <acceptance_criteria>
    - The backfill stdout summary contains 16 rows (one per metric ID): `<stdout> | grep -cE '^\s+(score_gap|achievable_score_gap|section2_score_gap_conv|section2_score_gap_parity|time_pressure_score_gap_(bullet|blitz|rapid|classical)|clock_gap_(bullet|blitz|rapid|classical)|net_flag_rate_(bullet|blitz|rapid|classical))\s+upserted='` returns 16.
    - For each per-TC metric row, `upserted + skipped_below_pooled_floor` sums equal the total user count processed (verified by inspecting the stdout).
    - A `psql` (or MCP) query against the dev DB confirms `SELECT COUNT(*) FROM user_benchmark_percentiles WHERE metric LIKE '%_bullet' OR metric LIKE '%_blitz' OR metric LIKE '%_rapid' OR metric LIKE '%_classical'` returns > 0.
    - Spot-check on 2-3 representative dev users: at least one user has non-NULL percentiles for at least one of the 12 new metric IDs.
    - Any per-TC cell flagged for suppression (in 94.3-01-SUMMARY.md or surfaced during the dev backfill via low `upserted` count) is explicitly documented in the plan summary so Plan F can decide whether to suppress.
  </acceptance_criteria>
  <done>Dev DB carries the 12 new metric rows for eligible users; the summary table emits 16 rows correctly; any classical-TC thinness or other coverage concern is documented for Plan F.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — Prod backfill rerun via prod tunnel</name>
  <files>(no file modified — production-side operational task; output captured in 94.3-05-SUMMARY.md)</files>
  <read_first>
    - scripts/backfill_user_percentiles.py (post-Task-1 state)
    - bin/prod_db_tunnel.sh (the tunnel-management script — confirm start/stop semantics)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-05-SUMMARY.md (Task 2 dev backfill output — the operator cross-references against this)
    - CLAUDE.md §Production Server (deploy + ssh patterns)
  </read_first>
  <what-built>
    - Plan D's ENUM extension migration has been deployed to prod via `bin/deploy.sh` (prerequisite — the migration applies on container startup via `deploy/entrypoint.sh`).
    - The `scripts/backfill_user_percentiles.py` script supports the 12 new per-TC metric IDs (Plan E Task 1).
    - The dev backfill rerun emitted a coherent 16-row summary (Plan E Task 2).
    - Pending: the prod backfill rerun, which lights up the chips for the existing user base.
  </what-built>
  <how-to-verify>
    Before running the prod backfill:
    1. Confirm Plan D's migration is live in prod: `ssh flawchess "cd /opt/flawchess && docker compose exec backend uv run alembic current"` should show the `extend_benchmark_metric_for_tc_pressure` revision.
    2. Open the prod tunnel: `bin/prod_db_tunnel.sh` (or `bin/prod_db_tunnel.sh start`). Confirm `lsof -i :15432` lists the SSH process (tunnel up).
    3. Run the prod backfill: `uv run python scripts/backfill_user_percentiles.py --target prod`. The run touches the entire prod user base; expect the run to take several minutes per `_BATCH_SIZE` and the number of users.
    4. Capture the full stdout summary. For each of the 16 metric rows, confirm `upserted + total_skipped` equals the total user count processed.
    5. Cross-reference the 12 new per-TC rows against the dev summary from Task 2 and the candidacy report from Plan A: per-cell `upserted` counts should be proportional to the cohort sizes documented there (with prod likely larger than dev).
    6. Close the tunnel: `bin/prod_db_tunnel.sh stop`. Confirm `lsof -i :15432` is empty.
    7. Type "approved" if the run completed cleanly with no unexpected errors and all 12 new metric rows have non-zero `upserted` counts for at least one user.
    8. Report any cell whose prod `upserted` count is materially below dev's — that signals a prod-specific cohort thinness (e.g. classical) Plan F needs to know about.
  </how-to-verify>
  <action>
    Operator-driven production backfill rerun. Follow the steps in `<how-to-verify>` exactly. Capture the entire prod backfill stdout (the 16-row summary table) into `94.3-05-SUMMARY.md` under a "Prod backfill summary" subsection. Document any per-(metric × TC) cell whose prod `upserted` count is materially below dev's so Plan F can apply the chip-suppression escape hatch. Do NOT proceed past this checkpoint without operator approval — Plan F's chips will render against whatever rows the prod DB carries; an incomplete or failed backfill will silently show empty chips for affected users.
  </action>
  <verify>
    <automated>ssh flawchess "cd /opt/flawchess && docker compose exec -T db psql -U flawchess -d flawchess -c \"SELECT COUNT(DISTINCT metric) FROM user_benchmark_percentiles WHERE metric IN ('time_pressure_score_gap_bullet','time_pressure_score_gap_blitz','time_pressure_score_gap_rapid','time_pressure_score_gap_classical','clock_gap_bullet','clock_gap_blitz','clock_gap_rapid','clock_gap_classical','net_flag_rate_bullet','net_flag_rate_blitz','net_flag_rate_rapid','net_flag_rate_classical');\" -At" | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
    <human-check>The 94.3-05-SUMMARY.md "Prod backfill summary" subsection contains the full stdout table with 16 rows. The operator confirms via "approved" in the resume signal that the run completed without unexpected errors.</human-check>
  </verify>
  <acceptance_criteria>
    - The prod backfill stdout summary (captured in 94.3-05-SUMMARY.md) contains 16 metric rows.
    - At least one user in the prod DB has non-NULL percentiles for at least one of the 12 new metric IDs (verified by `ssh flawchess` + `psql` count query in `<verify>`).
    - The operator's resume signal ("approved") is recorded.
    - Any per-(metric × TC) cell with prod `upserted` materially below dev is documented in 94.3-05-SUMMARY.md.
    - The prod tunnel was closed cleanly (`lsof -i :15432` reports no listener after Task 3 completes).
  </acceptance_criteria>
  <resume-signal>Type "approved" or paste the full prod backfill stdout summary + any concerns about per-cell coverage.</resume-signal>
  <done>Prod DB carries the 12 new metric rows for eligible users; the summary table is captured; the tunnel is closed; Plan F can ship safely.</done>
</task>

</tasks>

<verification>
- `uv run python scripts/backfill_user_percentiles.py --help` lists all 16 metric IDs.
- Dev backfill rerun summary has 16 rows; new ENUM values are populated for eligible dev users.
- Prod backfill rerun is gated by a blocking HUMAN-UAT checkpoint (Task 3); the operator approves only after capturing the summary.
- No `bin/reset_db.sh` invocation anywhere (per `feedback_no_dev_db_reset_in_plans`).
</verification>

<success_criteria>
- The backfill script accepts the 12 new metric IDs as `--metric` choices.
- After Plan E completes (including the HUMAN-UAT prod step), every eligible existing user has `user_benchmark_percentiles` rows for the 12 new metrics — chips will render on Time Pressure cards once Plan F's frontend ships.
- Any per-(metric × TC) cell with low coverage is documented and Plan F can decide whether to suppress.
</success_criteria>

<output>
Create `.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-05-SUMMARY.md` when done. The summary must include:
- The exact stdout summary table from the dev backfill (16 rows).
- The exact stdout summary table from the prod backfill (after operator approval — captured in the HUMAN-UAT checkpoint resume signal).
- A "Cells flagged for suppression in production" subsection listing any (metric × TC) cell whose prod `upserted` count is materially below dev — empty if none.
</output>
