---
phase: 94.3-per-tc-percentile-chips-time-pressure
plan: 03
type: execute
wave: 1
depends_on: [94.3-02]
files_modified:
  - scripts/gen_global_percentile_cdf.py
  - app/services/global_percentile_cdf.py
  - tests/scripts/test_gen_global_percentile_cdf_unchanged.py
  - reports/global-percentile-cdf-latest.md
autonomous: true
requirements: [SEED-025, TPCTL-03]

must_haves:
  truths:
    - "`CdfMetricId` Literal is widened from 4 to 16 entries in BOTH `app/services/global_percentile_cdf.py` AND `scripts/gen_global_percentile_cdf.py` (or, preferably, the script imports the Literal from the service module as the single source of truth — recommended per RESEARCH §Pattern 3 lines 222-225)."
    - "`scripts/gen_global_percentile_cdf.py:IN_SCOPE_METRICS` widens from 4 to 16 entries — existing 4 + 12 new `{metric_base}_{tc}` literals grouped by metric family for grep-ability."
    - "`_build_metric_breakpoint_query` signature is UNCHANGED — Plan B's dispatcher extension makes the 12 new IDs route to the correct builders automatically (RESEARCH §Pattern 3 lines 226-241). The structural-drift-impossibility property holds: the script does not know about TCs; only the shared builder does."
    - "`_registry_entry_comment(metric_id)` and `_metric_display_name(metric_id)` (script-local helpers) are widened with 12 new arms — one per metric ID — emitting the per-(metric × TC) inclusion-floor description and the display name used in the report and committed Python comment."
    - "`app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF` literal is REGENERATED via the script against the benchmark DB and committed. The literal now has exactly 16 entries with p1..p99 breakpoints. Numeric breakpoints WILL shift; the new entries are additive — the existing 4 entries' values are regenerated by the same script run but may be byte-identical to the prior 94.2 commit (if so, only the 12 new entries land as diff)."
    - "`tests/scripts/test_gen_global_percentile_cdf_unchanged.py` byte-identical regression goldens are REGENERATED to reflect the new 16-metric output. The prior 94.2 goldens are no longer authoritative."
    - "`reports/global-percentile-cdf-latest.md` is regenerated with 12 new metric-breakpoint tables + per-rating-bucket sanity checks for the 12 new metrics; the prior 94.2-dated report is archived to `reports/archive/global-percentile-cdf-YYYY-MM-DD.md` via the existing `_archive_prior_report` machinery (no manual file move)."
    - "`interpolate_percentile(metric_id, value)` signature in `app/services/global_percentile_cdf.py` is UNCHANGED. The function dispatches on `metric_id` via the existing dictionary lookup against `GLOBAL_PERCENTILE_CDF`; the 12 new keys flow through verbatim."
  artifacts:
    - path: scripts/gen_global_percentile_cdf.py
      provides: 12 new metric IDs in IN_SCOPE_METRICS + widened _registry_entry_comment / _metric_display_name
      contains: "IN_SCOPE_METRICS"
    - path: app/services/global_percentile_cdf.py
      provides: widened CdfMetricId Literal (16 entries) + regenerated GLOBAL_PERCENTILE_CDF with 12 new entries
      contains: "GLOBAL_PERCENTILE_CDF"
    - path: reports/global-percentile-cdf-latest.md
      provides: human-readable report covering 16 metrics including 12 new per-TC entries
      contains: "time_pressure_score_gap_bullet"
  key_links:
    - from: scripts/gen_global_percentile_cdf.py
      to: app/services/canonical_slice_sql.py
      via: per_user_cte_for dispatcher (Plan B)
      pattern: "per_user_cte_for\\("
    - from: app/services/global_percentile_cdf.py
      to: scripts/gen_global_percentile_cdf.py
      via: CdfMetricId Literal re-export (recommended)
      pattern: "from app.services.global_percentile_cdf import CdfMetricId"
---

<objective>
Widen the percentile CDF registry from 4 to 16 metric entries by extending `scripts/gen_global_percentile_cdf.py`'s `IN_SCOPE_METRICS` tuple + script-local helpers (`_registry_entry_comment`, `_metric_display_name`), widening the `CdfMetricId` Literal in `app/services/global_percentile_cdf.py`, regenerating the committed `GLOBAL_PERCENTILE_CDF` Python literal against the benchmark DB, regenerating the byte-identical regression goldens, and producing a fresh `reports/global-percentile-cdf-latest.md` (with the prior 94.2 report archived). `interpolate_percentile(metric_id, value)` signature stays unchanged — the 12 new metric IDs flow through the existing dispatch automatically.

Purpose: This is the second commit of the Wave-1 atomic-cutover trio (B → C → D). Plan B's per-TC dispatcher arms become reachable through `_build_metric_breakpoint_query` here, the byte-identical regression test goes from RED (after Plan B) to GREEN (after this plan), and Plan D's Stage B compute calls then have a populated CDF to interpolate against. The CDF literal regen is the deploy-time concern that ships the new breakpoints with the next image build (RESEARCH §Code Touchpoints: "CDF literal regen is a deploy-time concern").

Output: Regenerated Python CDF literal, regenerated regression-test goldens, fresh human-readable report. The benchmark DB (`flawchess-benchmark-db-1` on port 5433) must be running for the regen step; per `feedback_no_dev_db_reset_in_plans` no dev DB reset is gated on completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-CONTEXT.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md
@.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-02-SUMMARY.md
@scripts/gen_global_percentile_cdf.py
@app/services/global_percentile_cdf.py
@tests/scripts/test_gen_global_percentile_cdf_unchanged.py

<interfaces>
<!-- Script structure (current 94.2 state) extracted from scripts/gen_global_percentile_cdf.py
     and PATTERNS.md (lines 200-265). Plan C extends only IN_SCOPE_METRICS + helpers + ENV. -->

Current 4-entry CdfMetricId Literal (lines 143-155 of script; lines 69-74 of service):
  CdfMetricId = Literal[
      "score_gap",
      "achievable_score_gap",
      "section2_score_gap_conv",
      "section2_score_gap_parity",
  ]
  IN_SCOPE_METRICS: tuple[CdfMetricId, ...] = (...same 4 strings...)

Phase 94.3 widens to 16 entries — append in this order (grep-friendly grouping):
  Existing 4 +
  "time_pressure_score_gap_bullet", "time_pressure_score_gap_blitz",
  "time_pressure_score_gap_rapid", "time_pressure_score_gap_classical",
  "clock_gap_bullet", "clock_gap_blitz",
  "clock_gap_rapid", "clock_gap_classical",
  "net_flag_rate_bullet", "net_flag_rate_blitz",
  "net_flag_rate_rapid", "net_flag_rate_classical"

Script-local helpers (lines 281-298, 424-447, 503+):
  _build_metric_breakpoint_query(metric_id, *, snapshot_date) -> str  # UNCHANGED — Plan B's dispatcher handles new IDs
  _registry_entry_comment(metric_id) -> str                            # WIDEN with 12 new arms
  _metric_display_name(metric_id) -> str                               # WIDEN with 12 new arms

Sentinel-marker rewrite + report-archive machinery (`_rewrite_module_source` lines 450-472, `_archive_prior_report`) — UNCHANGED.

Service-level helper (UNCHANGED):
  interpolate_percentile(metric_id: CdfMetricId, value: float) -> float | None
  CdfTable dataclass (lines 82-96 of service) — unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen CdfMetricId Literal, IN_SCOPE_METRICS, and script-local helpers</name>
  <files>app/services/global_percentile_cdf.py, scripts/gen_global_percentile_cdf.py, tests/scripts/test_gen_global_percentile_cdf_unchanged.py</files>
  <read_first>
    - app/services/global_percentile_cdf.py (full file — `CdfMetricId` Literal definition, `CdfTable` dataclass, current 4-entry `GLOBAL_PERCENTILE_CDF`)
    - scripts/gen_global_percentile_cdf.py (full file — `IN_SCOPE_METRICS`, `_build_metric_breakpoint_query`, `_registry_entry_comment`, `_metric_display_name`, `_rewrite_module_source`, `_archive_prior_report`)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-PATTERNS.md §`scripts/gen_global_percentile_cdf.py` (lines 200-265), §`app/services/global_percentile_cdf.py` (lines 267-304)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-02-SUMMARY.md (confirm Plan B's dispatcher arms match the metric ID list)
  </read_first>
  <behavior>
    - Test 1: `from app.services.global_percentile_cdf import CdfMetricId` exposes a Literal with exactly 16 string values, including all 12 new `{metric_base}_{tc}` literals.
    - Test 2: `scripts.gen_global_percentile_cdf.IN_SCOPE_METRICS` has length 16 and includes the 12 new entries (importable via `from scripts.gen_global_percentile_cdf import IN_SCOPE_METRICS` or read via filesystem inspection if scripts/ is not importable).
    - Test 3: `_registry_entry_comment('time_pressure_score_gap_bullet')` returns a non-empty string containing the substring `pressured` (referencing the per-pressure-cell floor).
    - Test 4: `_registry_entry_comment('clock_gap_classical')` returns a non-empty string containing `clock` and `classical` (or the appropriate per-TC reference).
    - Test 5: `_registry_entry_comment('net_flag_rate_blitz')` returns a non-empty string referencing the pooled-set ≥30 floor.
    - Test 6: `_metric_display_name(<each of the 12 new ids>)` returns a non-empty human-readable string (e.g. `"Time Pressure Score Gap (bullet)"`, `"Clock Gap (classical)"`, `"Net Flag Rate (blitz)"`).
    - Test 7: `_metric_display_name('unknown')` raises `ValueError` (existing fallback unchanged).
  </behavior>
  <action>
    Widen `CdfMetricId` Literal in `app/services/global_percentile_cdf.py` from 4 to 16 entries. Group the 12 new entries by metric family in the order: time_pressure_score_gap × {bullet, blitz, rapid, classical}, clock_gap × {bullet, blitz, rapid, classical}, net_flag_rate × {bullet, blitz, rapid, classical}. In `scripts/gen_global_percentile_cdf.py`, change the local `CdfMetricId` Literal redeclaration into a single-line re-export: `from app.services.global_percentile_cdf import CdfMetricId` (per RESEARCH §Pattern 3 lines 222-225 — drift-impossibility). Widen `IN_SCOPE_METRICS` to 16 entries in the same grouped order. Widen `_registry_entry_comment(metric_id)` with 12 new dispatch arms — for `time_pressure_score_gap_*` mention the per-pressure-cell ≥30 floor and reference `TIME_PRESSURE_CLOCK_PCT_THRESHOLD`; for `clock_gap_*` and `net_flag_rate_*` mention the pooled-set ≥30 floor. Widen `_metric_display_name(metric_id)` with 12 new dispatch arms producing user-readable names — recommended form `"Time Pressure Score Gap ({tc})"`, `"Clock Gap ({tc})"`, `"Net Flag Rate ({tc})"`. Add unit tests to `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` (or `tests/services/test_global_percentile_cdf.py` if separate test files are preferred — check existing test layout via `ls tests/scripts/ tests/services/ | grep global_percentile`) covering Tests 1-7 above. The existing byte-identical golden tests will remain RED until Task 2 regenerates them — do NOT touch the goldens here.
  </action>
  <verify>
    <automated>uv run pytest tests/services/ tests/scripts/ -k 'global_percentile_cdf or CdfMetricId or registry_entry_comment or metric_display_name' -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\s+"(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)"' app/services/global_percentile_cdf.py` returns ≥ 12 (Literal entries).
    - `grep -cE '^\s+"(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)"' scripts/gen_global_percentile_cdf.py` returns ≥ 12 (IN_SCOPE_METRICS entries).
    - `grep -c '^from app.services.global_percentile_cdf import CdfMetricId' scripts/gen_global_percentile_cdf.py` returns 1 (the re-export — drift-impossibility).
    - `uv run ty check app/services/global_percentile_cdf.py scripts/gen_global_percentile_cdf.py` exits 0 with zero errors.
    - All new helper tests pass; the byte-identical regression golden test in `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` is EXPECTED RED (Task 2 fixes it).
  </acceptance_criteria>
  <done>Literal widened in both modules; IN_SCOPE_METRICS and helpers widened; new tests pass; byte-identical golden still red (expected).</done>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 2: Regenerate GLOBAL_PERCENTILE_CDF literal + byte-identical goldens + human-readable report</name>
  <files>app/services/global_percentile_cdf.py, tests/scripts/test_gen_global_percentile_cdf_unchanged.py, reports/global-percentile-cdf-latest.md, reports/archive/(prior dated report)</files>
  <read_first>
    - scripts/gen_global_percentile_cdf.py (full file — particularly `_rewrite_module_source` lines 450-472 + `_archive_prior_report` for the report-rotation machinery)
    - app/services/global_percentile_cdf.py (post-Task-1 state — CdfMetricId widened to 16)
    - reports/global-percentile-cdf-latest.md (current 94.2 version — will be archived)
    - .planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-RESEARCH.md §Pattern 3 + §Pitfall 4 (snapshot-date asymmetry)
  </read_first>
  <action>
    Ensure the benchmark DB is running: `bin/benchmark_db.sh start` (if not already). Run the regeneration script: `uv run python scripts/gen_global_percentile_cdf.py --snapshot-date <YYYY-MM-DD>` where `<YYYY-MM-DD>` is the current date (or a fixed date the executor chooses, recorded in the plan summary). The script: (a) rewrites the `GLOBAL_PERCENTILE_CDF` literal in `app/services/global_percentile_cdf.py` between its sentinel markers — all 16 entries are emitted, including the 4 existing entries (which may be byte-identical to the prior commit if the benchmark data hasn't shifted); (b) regenerates the human-readable report at `reports/global-percentile-cdf-latest.md` and archives the prior dated report to `reports/archive/`. After the script completes, regenerate the byte-identical regression goldens in `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` by following the existing fixture-regen instructions in that test file's docstring (typically `pytest --snapshot-update` or a manual fixture script — check the file). Confirm `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` exits 0. Confirm `uv run pytest -x` (full suite) exits 0 — this catches any downstream consumer that hadn't been updated. The benchmark DB regen is the only manual step; everything else is automated. If a benchmark cell's n_users for any (metric × TC) is below the threshold from 94.3-01-SUMMARY.md's "Cells flagged for suppression" list, document the affected cell(s) in the plan summary so Plan F can decide whether to suppress those chips.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x</automated>
    <human-check>The committed `app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF` has 16 entries (count keys in the dict literal). `reports/global-percentile-cdf-latest.md` contains 16 metric tables (one section per metric). The prior dated report has been moved into `reports/archive/`.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\s+"(score_gap|achievable_score_gap|section2_score_gap_conv|section2_score_gap_parity|time_pressure_score_gap_(bullet|blitz|rapid|classical)|clock_gap_(bullet|blitz|rapid|classical)|net_flag_rate_(bullet|blitz|rapid|classical))": CdfTable' app/services/global_percentile_cdf.py` returns exactly 16.
    - `grep -cE '(time_pressure_score_gap|clock_gap|net_flag_rate)_(bullet|blitz|rapid|classical)' reports/global-percentile-cdf-latest.md` returns ≥ 12.
    - `ls reports/archive/global-percentile-cdf-*.md` lists at least one dated archived report (the prior 94.2 version moved during this task).
    - `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` exits 0 (goldens regenerated; byte-identical regression is GREEN again).
    - `uv run pytest -x` (full suite) exits 0.
    - `uv run ty check app/ tests/` exits 0.
    - For each of the 16 entries in `GLOBAL_PERCENTILE_CDF`, the `breakpoints` tuple has exactly 99 floats (p1..p99 per Phase 93 contract; verified by inspecting the literal or by `python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF; assert all(len(t.breakpoints) == 99 for t in GLOBAL_PERCENTILE_CDF.values())"`).
  </acceptance_criteria>
  <done>16 entries committed in `GLOBAL_PERCENTILE_CDF`; goldens regenerated; report fresh; archive populated; full pytest suite GREEN.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py tests/services/ -x` exits 0.
- `uv run ty check app/ tests/ scripts/` exits 0 (zero errors).
- `uv run ruff check .` and `uv run ruff format --check .` clean.
- `python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF; print(len(GLOBAL_PERCENTILE_CDF))"` prints 16.
- `python -c "from app.services.global_percentile_cdf import interpolate_percentile; print(interpolate_percentile('clock_gap_bullet', 0.05))"` returns a float in [0, 100] (smoke test of the existing dispatch).
- The byte-identical regression test goldens are GREEN; Plan D can proceed.
</verification>

<success_criteria>
- `GLOBAL_PERCENTILE_CDF` has 16 entries with p1..p99 breakpoints each.
- `interpolate_percentile` signature unchanged; new metric IDs dispatch correctly.
- Plan D can now reference any of the 12 new metric IDs at runtime without `KeyError`.
- Plan E can now run the backfill script and `(STAGE_A_METRIC, *STAGE_B_METRICS)` will resolve all 16 metrics (once Plan D widens STAGE_B_METRICS).
</success_criteria>

<output>
Create `.planning/phases/94.3-per-tc-percentile-chips-time-pressure/94.3-03-SUMMARY.md` when done. The summary must include:
- The `--snapshot-date` value used for the regen.
- The 16-entry per-metric n_users count from the regen output (so Plan F can cross-reference with Plan A's candidacy table for chip-suppression decisions).
- Any cell flagged for suppression (n_users < 150 per Plan A's flagging or this task's regen output).
- A note that `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` is back to GREEN; Plan D can proceed.
</output>
