---
phase: 109-per-card-expected-score-eval-chart-games-subtab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/types/library.ts
autonomous: true
requirements: [LIBG-10]

must_haves:
  truths:
    - "Five EVAL_CHART_* color constants exist in theme.ts with the exact UI-SPEC oklch values (no hex/oklch literals will appear in the component)"
    - "GameFlawCard TS type carries eval_series, flaw_markers, phase_transitions matching the backend wire shape (D-05)"
    - "FlawMarker TS interface includes the is_user discriminator for filled-vs-hollow rendering (D-07)"
  artifacts:
    - path: "frontend/src/lib/theme.ts"
      provides: "EVAL_CHART_AREA_WHITE_AHEAD/BLACK_AHEAD, EVAL_CHART_LINE/MIDLINE/PHASE_LINE"
      contains: "EVAL_CHART_AREA_WHITE_AHEAD"
    - path: "frontend/src/types/library.ts"
      provides: "EvalPoint, FlawMarker (with is_user), PhaseTransitions interfaces; extended GameFlawCard"
      contains: "export interface EvalPoint"
  key_links:
    - from: "frontend/src/types/library.ts GameFlawCard"
      to: "EvalPoint/FlawMarker/PhaseTransitions"
      via: "nullable inline fields mirroring app/schemas/library.py"
      pattern: "eval_series|flaw_markers|phase_transitions"
---

<objective>
Add the frontend prerequisites the EvalChart component imports: the five `EVAL_CHART_*` theme
constants and the TypeScript types (`EvalPoint`, `FlawMarker` with `is_user`, `PhaseTransitions`,
extended `GameFlawCard`) that mirror the backend wire contract.

Purpose: theme-first dependency ordering (UI-SPEC: the component imports these constants, so they
must exist before plan 04) and a type contract matching plan 01's backend payload (LIBG-10, D-05/D-07).

Output: five `EVAL_CHART_*` constants in `theme.ts`; `EvalPoint`/`FlawMarker`/`PhaseTransitions`
interfaces + three new `GameFlawCard` fields in `library.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-CONTEXT.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-RESEARCH.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-PATTERNS.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan (02) creates: the five `EVAL_CHART_*` theme constants
(`EVAL_CHART_AREA_WHITE_AHEAD`, `EVAL_CHART_AREA_BLACK_AHEAD`, `EVAL_CHART_LINE`,
`EVAL_CHART_MIDLINE`, `EVAL_CHART_PHASE_LINE`); and the TS interfaces `EvalPoint`, `FlawMarker`
(with `is_user`), `PhaseTransitions`, plus the three new `GameFlawCard` fields. The `EvalChart`
component and card restructure are plan 04; the backend models are plan 01.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add EVAL_CHART_* theme constants + extend library.ts types</name>
  <files>frontend/src/lib/theme.ts, frontend/src/types/library.ts</files>
  <read_first>
    - frontend/src/lib/theme.ts (the `SEV_BLUNDER`/`SEV_MISTAKE`/`SEV_INACCURACY` block, lines 27-29) — the analog for `export const` oklch constants; the file being modified
    - frontend/src/types/library.ts (the `GameFlawCard` interface, lines 51-74; `FlawSeverity`/`FlawTag` types) — the file being modified
    - 109-UI-SPEC.md §"New theme.ts Constants" (exact constant names + oklch values) and §"Component Contract" (type shapes)
    - 109-PATTERNS.md §"frontend/src/lib/theme.ts" and §"frontend/src/types/library.ts" (exact code to add)
    - 109-RESEARCH.md §"Frontend Grounding" (is_user is boolean, precomputed server-side)
  </read_first>
  <action>
    In `frontend/src/lib/theme.ts`, after the existing `SEV_*` block, add the five constants with the
    exact UI-SPEC values: `export const EVAL_CHART_AREA_WHITE_AHEAD = 'oklch(0.70 0 0 / 0.35)';`,
    `EVAL_CHART_AREA_BLACK_AHEAD = 'oklch(0.28 0 0 / 0.45)';`, `EVAL_CHART_LINE = 'oklch(0.82 0 0)';`,
    `EVAL_CHART_MIDLINE = 'oklch(0.55 0 0)';`, `EVAL_CHART_PHASE_LINE = 'oklch(0.55 0 0 / 0.60)';`.
    Add a one-line comment noting these back the Phase 109 EvalChart. Knip runs in CI, so do not add
    constants here that plan 04 will not import — all five are imported by EvalChart.

    In `frontend/src/types/library.ts`, add three exported interfaces after `GameFlawCard` and before
    `LibraryGamesResponse`: `EvalPoint { ply: number; es: number | null; eval_cp: number | null;
    eval_mate: number | null }`; `FlawMarker { ply: number; severity: FlawSeverity; tags: FlawTag[];
    is_user: boolean }`; `PhaseTransitions { middlegame_ply: number | null; endgame_ply: number | null }`.
    Extend `GameFlawCard` with `eval_series: EvalPoint[] | null;`, `flaw_markers: FlawMarker[] | null;`,
    `phase_transitions: PhaseTransitions | null;` (null for unanalyzed games). Match the exact shapes
    in 109-PATTERNS.md §types. Reuse the existing `FlawSeverity`/`FlawTag` types — do not redefine them.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EVAL_CHART_" frontend/src/lib/theme.ts` returns 5 (the five new constants; counting export lines).
    - `grep -q "export interface EvalPoint" frontend/src/types/library.ts` and `FlawMarker`, `PhaseTransitions` present.
    - `grep -q "is_user: boolean" frontend/src/types/library.ts` (FlawMarker discriminator present).
    - `grep -q "eval_series" frontend/src/types/library.ts` (GameFlawCard extended).
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Five theme constants and three new types exist; frontend type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan adds compile-time color constants and TypeScript type declarations only; no runtime data flow, no input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04 | n/a | theme constants + type declarations | accept | No executable logic, no data crossing a trust boundary; type-only changes carry no security surface. The IDOR control lives server-side (T-109-01, plan 01). |
</threat_model>

<verification>
- `cd frontend && npm run lint` clean
- `cd frontend && npx tsc --noEmit` zero errors
</verification>

<success_criteria>
- Five `EVAL_CHART_*` constants present with exact UI-SPEC oklch values.
- `EvalPoint`/`FlawMarker` (with `is_user`)/`PhaseTransitions` exist; `GameFlawCard` carries the three new nullable fields.
</success_criteria>

<output>
Create `.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-02-SUMMARY.md` when done
</output>
