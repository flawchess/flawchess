---
phase: 109-per-card-expected-score-eval-chart-games-subtab
plan: 04
type: execute
wave: 2
depends_on: ["109-02"]
files_modified:
  - .planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md
  - frontend/src/components/library/EvalChart.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
autonomous: false
requirements: [LIBG-10]

must_haves:
  truths:
    - "An analyzed Games-subtab card is laid out as three equal desktop thirds (board+info / chart / tags); mobile stacks the same three blocks (LIBG-10 criterion 1)"
    - "The chart plots white-perspective ES per ply with a 50% midline and two-region shading (light grey >50%, dark grey <50%) (criterion 2)"
    - "Flaw dots render for BOTH players - filled = you, hollow = opponent, color = severity; 6 dot styles total (D-07)"
    - "B/M tooltips show You/Opponent + Severity + tags for both players; inaccuracy tooltips show severity + eval only (D-03/D-08)"
    - "At most two phase-transition vertical lines (middlegame, endgame); no ply-0 line (D-06, criterion 4)"
    - "Tooltip shows eval in pawns (white-perspective) or mate in #N (criterion 5)"
    - "Unanalyzed cards keep NoAnalysisState and render no chart (criterion 1); data-testid + ARIA present (criterion 7)"
    - "109-UI-SPEC.md is amended to describe the dual-marker scheme it ships (D-07/D-08/D-09)"
  artifacts:
    - path: "frontend/src/components/library/EvalChart.tsx"
      provides: "the recharts ComposedChart eval chart with dual-marker flaw dots"
      min_lines: 80
      contains: "ComposedChart"
    - path: "frontend/src/components/results/LibraryGameCard.tsx"
      provides: "three-thirds desktop grid + mobile stacked block wiring EvalChart"
      contains: "EvalChart"
    - path: ".planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md"
      provides: "amended dual-marker dot contract (filled/hollow, 6 styles, You/Opponent tooltip)"
      contains: "hollow"
  key_links:
    - from: "frontend/src/components/results/LibraryGameCard.tsx"
      to: "frontend/src/components/library/EvalChart.tsx"
      via: "analyzed-only render in the middle column / mobile block"
      pattern: "EvalChart"
    - from: "frontend/src/components/library/EvalChart.tsx"
      to: "frontend/src/lib/theme.ts"
      via: "imported EVAL_CHART_* + SEV_* color constants (no inline literals)"
      pattern: "EVAL_CHART_|SEV_"
---

<objective>
Build the EvalChart recharts component (white-perspective ES area chart, 50% midline, two-region
shading, at-most-two phase lines, dual-marker flaw dots for both players, per-ply tooltip) and
restructure LibraryGameCard into three equal desktop thirds plus the matching mobile stacked block.
First, amend 109-UI-SPEC.md so the shipped contract matches the dual-marker scope expansion
(D-07/D-08/D-09), since the UI-SPEC currently specs user-only filled dots.

Purpose: Deliver LIBG-10 criteria 1-5 and 7 on the frontend, honoring the D-07 filled/hollow
both-player marker scheme, the D-08 You/Opponent tooltip labels, and the D-06 no-ply-0-line rule.

Output: amended 109-UI-SPEC.md; new EvalChart.tsx; restructured LibraryGameCard.tsx (desktop grid
plus mobile stack); a human visual-verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-CONTEXT.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-RESEARCH.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-PATTERNS.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04) creates: the EvalChart component (frontend/src/components/library/EvalChart.tsx);
the three-thirds desktop grid + mobile stacked block in LibraryGameCard.tsx; and the amended
109-UI-SPEC.md (dual-marker dot contract). It consumes the theme constants + TS types from plan 02
and the wire payload from plans 01/03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Amend 109-UI-SPEC.md for the dual-marker scheme</name>
  <files>.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md</files>
  <read_first>
    - 109-UI-SPEC.md sections "Flaw Dots (User's Moves Only)", "Component Contract: EvalChart" (props + FlawMarker type), "Tooltip Contract" - the sections to amend; the file being modified
    - 109-CONTEXT.md D-07/D-08/D-09 (filled=player/hollow=opponent, 6 dot styles, You/Opponent labels, density tuning)
    - 109-RESEARCH.md "UI-SPEC Amendment Summary (D-07/D-08/D-09)" (the precise edits: 6 styles, is_user discriminator, hollow=fill none + stroke, custom dot render prop over Scatter, dot radii) and "Recharts 3.8.1 - Dual-Marker Approach" (Scatter size vs r caveat)
  </read_first>
  <action>
    Edit 109-UI-SPEC.md so the shipped contract matches the dual-marker scope expansion. Specifically:
    (1) Rename and rewrite the "Flaw Dots (User's Moves Only)" section to cover BOTH players: filled
    circle uses fill set to the severity color = player; hollow circle uses fill none + stroke set to
    the severity color, strokeWidth 1.5 = opponent; the player-vs-opponent choice is driven by
    marker.is_user - 6 dot styles total (3 severities times player/opponent).
    (2) Replace the Scatter r=3 recommendation with the custom dot render prop on a Line with
    stroke none overlay inside ComposedChart (the established EndgameClockDiffOverTimeChart pattern),
    noting recharts 3.8.1 Scatter uses area-based size, not radius r.
    (3) Add is_user boolean to the FlawMarker type block in the Component Contract.
    (4) Update the Tooltip Contract so the severity line is qualified You + Severity / Opponent +
    Severity (D-08), and inaccuracy tooltips show severity + eval only, no tags (D-03).
    (5) Add the density-tuning note (D-09): inaccuracy dots smaller (r=2) than B/M (r=2.5), hollow
    strokes thin; executor tunes during UAT.
    (6) Confirm the opening boundary (ply 0) gets no ReferenceLine (D-06).
    Keep all other UI-SPEC sections intact (layout, gradient, dims, ARIA). Add a dated amendment note
    near the top of the file recording the D-07/D-08/D-09 reconciliation.
  </action>
  <verify>
    <automated>grep -q "hollow" .planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md && grep -q "is_user" .planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md && grep -qi "opponent" .planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - 109-UI-SPEC.md contains the words hollow, is_user, and Opponent (dual-marker contract present).
    - The Scatter r=3 recommendation is replaced by the custom dot render prop on a Line overlay.
    - The Tooltip Contract qualifies the severity line with You/Opponent and excludes tags for inaccuracies.
    - A dated amendment note records the D-07/D-08/D-09 reconciliation.
  </acceptance_criteria>
  <done>The shipped UI-SPEC describes the dual-marker scheme (filled/hollow, 6 styles, You/Opponent tooltip, density note) rather than user-only filled dots.</done>
</task>

<task type="auto">
  <name>Task 2: Build EvalChart.tsx (ComposedChart + two-region gradient + dual-marker dots + tooltip)</name>
  <files>frontend/src/components/library/EvalChart.tsx</files>
  <read_first>
    - frontend/src/components/library/FlawTrendChart.tsx (recharts-in-card pattern: useId gradient id, ChartContainer config empty, isAnimationActive false, ChartTooltip custom content, linearGradient in defs, no CartesianGrid)
    - frontend/src/components/charts/EndgameClockDiffOverTimeChart.tsx (custom dot render prop returning a circle / empty g; payload narrowing)
    - frontend/src/components/charts/EndgameScoreOverTimeChart.tsx (ComposedChart with Area + Line + ReferenceLine, connectNulls false)
    - 109-PATTERNS.md "frontend/src/components/library/EvalChart.tsx (NEW)" (full imports, props, gradient, customDotRenderer, ComposedChart structure, tooltip, ARIA/data-testid) and 109-RESEARCH.md "Recharts 3.8.1 - Dual-Marker Approach" + Pitfalls 6 (hollow circle fill none) and 7 (empty g not null)
    - frontend/src/types/library.ts (EvalPoint, FlawMarker with is_user, PhaseTransitions, FlawSeverity) and frontend/src/lib/theme.ts (EVAL_CHART_* + SEV_* constants from plan 02)
  </read_first>
  <action>
    Create frontend/src/components/library/EvalChart.tsx. Props interface EvalChartProps: gameId
    number, evalSeries EvalPoint[], flawMarkers FlawMarker[], phaseTransitions PhaseTransitions,
    userColor white|black, and an optional heightClass string defaulting to h-24 (mobile passes
    h-20). Build a useId-derived gradientId (sanitize non-alphanumerics, prefix eval-gradient-).
    Build a ply-keyed Map from flawMarkers for O(1) lookup in the dot renderer. Add a local
    severityColor(sev) returning SEV_BLUNDER/SEV_MISTAKE/SEV_INACCURACY (no inline literals).

    Render a ChartContainer with config empty and className w-full plus heightClass, wrapping a
    ComposedChart over evalSeries with margin top/right/left/bottom 4. Include: a defs linearGradient
    (vertical) with a hard 50% stop - EVAL_CHART_AREA_WHITE_AHEAD for 0%-50%, EVAL_CHART_AREA_BLACK_AHEAD
    for 50%-100%; a hidden XAxis dataKey ply and a hidden YAxis with domain 0 to 1; an Area type
    monotone dataKey es, stroke EVAL_CHART_LINE strokeWidth 1.5, fill url to the gradient, dot false,
    connectNulls false, isAnimationActive false; a ReferenceLine y 0.5 stroke EVAL_CHART_MIDLINE
    strokeDasharray dashed; ReferenceLine x for middlegame_ply and for endgame_ply each rendered only
    when not null, stroke EVAL_CHART_PHASE_LINE (no ply-0 line, D-06); and a Line type monotone
    dataKey es with stroke none, isAnimationActive false, connectNulls false, and a custom dot render
    prop that draws the flaw markers. The dot renderer narrows cx/cy/payload (noUncheckedIndexedAccess),
    returns an empty g (with a key) when there is no marker for the ply or es is null (Pitfall 7),
    draws a filled circle (fill = severity color) when marker.is_user is true, and a hollow circle
    (fill none, stroke = severity color, strokeWidth 1.5) when is_user is false (Pitfall 6); radius
    is 2 for inaccuracy, 2.5 for mistake/blunder (D-09).

    Add a ChartTooltip with a custom content render prop: narrow payload[0].payload to EvalPoint;
    show Ply N plus the eval - mate in abs(eval_mate) with the side from sign when eval_mate is set,
    else white-perspective eval_cp/100 to 2 dp (positive = White ahead); when the ply has a marker,
    add a line colored by severity reading You or Opponent (from marker.is_user) + capitalized
    severity, and append comma-joined tags only when present (inaccuracies have none, D-03). Tooltip
    container uses the project tooltip classes (rounded-lg border bg-background px-3 py-2 text-xs
    shadow-xl). NOTE: the text-xs on the tooltip container intentionally inherits the established
    project-wide recharts chart-tooltip convention (identical to EndgameScoreOverTimeChart.tsx /
    EndgameClockDiffOverTimeChart.tsx and the UI-SPEC opt-in popover-surface exception); it is the
    existing pattern, not an ad-hoc sub-text-sm choice. Wrap the chart in a div with data-testid
    eval-chart-{gameId} and aria-label "Expected score chart for game {gameId}" (role img). Import
    every color from theme.ts; no hex or oklch literals in this file. Keep the component within the
    function-size limits (extract the dot renderer and tooltip content into named local helpers if the
    body grows past the soft limits).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/library/EvalChart.tsx exists and contains ComposedChart, ReferenceLine, and a custom dot render prop.
    - The file contains both a filled circle branch (fill = color) and a hollow circle branch (fill="none" stroke=color) keyed on marker.is_user.
    - grep finds no raw oklch( or # hex color literal in EvalChart.tsx (all colors imported from theme.ts).
    - The file contains data-testid="eval-chart- and an aria-label.
    - cd frontend && npx tsc --noEmit exits 0 (knip is deferred to Task 3, which wires EvalChart into the card so the export is no longer dead).
  </acceptance_criteria>
  <done>EvalChart renders the white-perspective area chart, two-region shading, midline, at-most-two phase lines, dual-marker dots, and the You/Opponent tooltip; type-checks and lints clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure LibraryGameCard into three desktop thirds + mobile stacked block</name>
  <files>frontend/src/components/results/LibraryGameCard.tsx</files>
  <read_first>
    - frontend/src/components/results/LibraryGameCard.tsx (the existing desktop body lines ~285-307 and mobile body lines ~265-283; openingLine, desktopMetadata, mobileMetadata, flawContent locals; NoAnalysisState; LazyMiniBoard; DESKTOP_BOARD_SIZE/MOBILE_BOARD_SIZE constants) - the file being modified
    - 109-PATTERNS.md "frontend/src/components/results/LibraryGameCard.tsx" (full desktop grid + mobile block code) and 109-UI-SPEC.md "Layout Contract" (three equal thirds; unanalyzed shows NoAnalysisState in col 2; mobile stacks three blocks; chart dims h-24 desktop / h-20 mobile)
    - 109-RESEARCH.md "Frontend Grounding" (the hidden sm:grid sm:grid-cols-3 restructure; mobile insertion between board+info and flawContent)
  </read_first>
  <action>
    Restructure the desktop body container from hidden sm:flex to hidden sm:grid sm:grid-cols-3
    sm:gap-3 sm:items-start so the three columns are equal thirds (no fixed-pixel column widths).
    Col 1: the existing mini board (LazyMiniBoard at DESKTOP_BOARD_SIZE, flipped when user_color is
    black) plus openingLine + desktopMetadata. Col 2: when analysis_state is analyzed and
    eval_series, flaw_markers, and phase_transitions are all present, render EvalChart with those
    props plus userColor narrowed to white|black and gameId; otherwise render the existing
    NoAnalysisState pill (centered). Col 3: the existing flawContent (drop the dashed left border
    that was a flex separator). For the mobile body (sm:hidden), keep the existing board+info row,
    then insert a full-width EvalChart block between it and flawContent, rendered only when the game
    is analyzed and the three eval fields are present, passing heightClass h-20; flawContent / the
    NoAnalysisState pill stay as today. Apply the change to BOTH desktop and mobile (CLAUDE.md mobile
    parity). Narrow game.user_color to the white|black union before passing it (it is typed string on
    GameFlawCard) - do not pass a bare string to the white|black prop. Keep data-testid coverage on
    new interactive/layout containers per CLAUDE.md browser-automation rules.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit && npm test -- --run && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - LibraryGameCard.tsx imports and renders EvalChart in both the desktop grid (col 2) and the mobile stacked block.
    - The desktop body uses sm:grid sm:grid-cols-3 (three equal thirds), not the old sm:flex two-column layout.
    - Unanalyzed cards render NoAnalysisState in col 2 and no chart (analysis_state gate present).
    - The mobile EvalChart block passes heightClass h-20.
    - cd frontend && npx tsc --noEmit exits 0; npm run lint clean; existing frontend tests pass with npm test -- --run; npm run knip reports no unused export (EvalChart is now imported by the card).
  </acceptance_criteria>
  <done>Analyzed cards show three equal desktop thirds + the mobile stacked chart block; unanalyzed cards keep NoAnalysisState; mobile parity applied; lint/typecheck/tests/knip green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual UAT - dual-marker dots, layout, shading, tooltips, density (D-09)</name>
  <what-built>
    EvalChart embedded in the restructured three-thirds LibraryGameCard (desktop) and the mobile
    stacked block: white-perspective ES area with two-region shading, 50% midline, at-most-two phase
    lines, dual-marker flaw dots (filled=you, hollow=opponent, color=severity), and per-ply tooltips
    labeling You/Opponent + severity + tags (B/M) or severity + eval only (inaccuracies).
  </what-built>
  <how-to-verify>
    1. Run the dev stack (bin/run_local.sh or the documented backend + frontend dev servers) against
       the existing dev DB - do NOT reset the dev DB.
    2. Visit Library then Games. On desktop, confirm an analyzed card is laid out as three equal
       thirds: miniboard+info / eval chart / tags. Confirm the area is light grey above the 0.5
       midline and dark grey below, with a dashed midline.
    3. Confirm flaw dots: filled circles for your flaws, hollow (outline-only) circles for the
       opponent's, colored by severity (blunder red, mistake orange, inaccuracy yellow). Confirm at
       most two vertical phase lines and no line at ply 0.
    4. Hover/tap dots and plies: tooltip shows Ply N + eval in pawns or mate in #N; B/M dots show
       You/Opponent + severity + tags; inaccuracy dots show severity + eval only (no tags).
    5. Confirm an unanalyzed card shows the NoAnalysisState pill in col 2 and no chart.
    6. Resize to 375px (or use a phone): confirm the three blocks stack (board+info, chart at h-20,
       tags) and dots remain legible. Judge dot density per D-09 - if inaccuracy dots are too dense,
       note the radius/opacity tuning to apply.
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/marker/tooltip/density issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON -> EvalChart render | the component renders server-derived display data scoped to the authenticated user's own games; no new input surface, no user-supplied HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05 | Information Disclosure | EvalChart / LibraryGameCard | accept | The chart renders only the authenticated user's own game data (server enforces user scoping in plan 01 / T-109-01). No cross-user data reaches the component. |
| T-109-06 | Tampering (XSS) | tooltip tag/severity strings | accept | Tags and severities are a fixed server-side Literal set rendered as React text nodes (auto-escaped); no dangerouslySetInnerHTML, no user free-text. |
</threat_model>

<verification>
- cd frontend && npm run lint clean
- cd frontend && npx tsc --noEmit zero errors
- cd frontend && npm run knip clean (no unused export)
- cd frontend && npm test -- --run green
- Human visual UAT approved
</verification>

<success_criteria>
- Analyzed cards render three equal desktop thirds + mobile stacked chart block; unanalyzed cards keep NoAnalysisState.
- Chart shows white-perspective ES, two-region shading, midline, at-most-two phase lines.
- Dual-marker dots (filled you / hollow opponent, color = severity) render with You/Opponent tooltips.
- 109-UI-SPEC.md amended to match the shipped dual-marker contract.
- All frontend gates green; visual UAT approved.
</success_criteria>

<output>
Create `.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-04-SUMMARY.md` when done
</output>
