---
phase: 109-per-card-expected-score-eval-chart-games-subtab
plan: 03
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - tests/test_library_router.py
autonomous: true
requirements: [LIBG-10]

must_haves:
  truths:
    - "GET /library/games returns non-null eval_series/flaw_markers/phase_transitions for an analyzed game (LIBG-10 criterion 6)"
    - "GET /library/games returns null eval fields for an unanalyzed game"
    - "The page is served with a single batched game_positions query (no N+1, D-10)"
    - "A cross-user game_id yields no positions (IDOR control T-109-01 verified)"
    - "The gzipped payload delta vs the pre-Phase-109 response is recorded and negligible (D-05)"
  artifacts:
    - path: "tests/test_library_router.py"
      provides: "integration tests for eval_series presence/nullity, no-N+1 query count, IDOR scoping"
      contains: "eval_series"
  key_links:
    - from: "tests/test_library_router.py"
      to: "GET /library/games"
      via: "authenticated client request asserting GameFlawCard eval fields"
      pattern: "library/games"
---

<objective>
Verify the extended `GET /api/library/games` payload end-to-end: analyzed cards carry non-null
`eval_series`/`flaw_markers`/`phase_transitions`, unanalyzed cards carry null, the page is served
with a single batched positions query (no N+1), the new query is user-scoped (IDOR), and the
gzipped payload delta is negligible.

Purpose: Close LIBG-10 criterion 6 (inline delivery, no N+1, no perceptible payload regression,
D-05/D-10) and confirm the T-109-01 IDOR control with an automated assertion.

Output: extended `tests/test_library_router.py` integration tests + a recorded payload-delta
measurement in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-CONTEXT.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-RESEARCH.md
@.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (03) extends `tests/test_library_router.py` with integration coverage for the new payload
fields, a no-N+1 query-count assertion, and an IDOR-scoping assertion. It also records the gzipped
payload-delta measurement (D-05) in its SUMMARY. It produces no new production symbols — the router
itself is unchanged (Pydantic propagates the plan-01 `GameFlawCard` fields automatically).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Router integration tests — payload presence/nullity, no-N+1, IDOR</name>
  <files>tests/test_library_router.py</files>
  <read_first>
    - tests/test_library_router.py (existing `GET /games` tests — fixtures for creating a user, games, and game_positions; the authenticated test client pattern) — the file being extended
    - 109-RESEARCH.md §"N+1 Query Plan" (5-queries-per-page expectation; the batched seam) and §"Validation Architecture" (router test map: `-k eval_series`, `-k unanalyzed`)
    - 109-VALIDATION.md §"Per-Task Verification Map" (router-inline-payload, router-unanalyzed-null, no-n-plus-1 rows) and §"Wave 0 Requirements" (query-count harness)
    - 109-01-SUMMARY.md (the shipped builder + `fetch_page_eval_positions` behavior to assert against)
  </read_first>
  <action>
    Extend `tests/test_library_router.py`. Add a test (matchable with `-k eval_series`) that seeds a
    user with one ANALYZED game (game_positions rows with eval_cp/eval_mate/phase covering ≥90% so it
    passes the analyzed gate) plus at least one mover-drop large enough to register a flaw, requests
    `GET /library/games`, and asserts the matching card has non-null `eval_series` (one EvalPoint per
    ply, es rounded), non-null `flaw_markers` (with an `is_user` field present), and a
    `phase_transitions` object. Add a test (matchable with `-k unanalyzed`) that seeds an UNANALYZED
    game (no/low eval coverage) and asserts its card's `eval_series`, `flaw_markers`, and
    `phase_transitions` are all null.

    Add a no-N+1 query-count assertion: wrap the `GET /library/games` call in a SQLAlchemy statement
    counter (event listener on `before_cursor_execute`, or the existing query-count helper if the test
    suite already has one — search first) and assert the total query count for a 20-game page is
    constant and does not scale with the number of analyzed games (i.e. seed N analyzed games, assert
    the count is the same as for 1 analyzed game). This proves the batched `fetch_page_eval_positions`
    is a single query, not per-game.

    Add an IDOR assertion (T-109-01): seed game_positions owned by user A for game G; as user B (who
    does not own G), confirm `fetch_page_eval_positions(session, user_b_id, [G])` returns no positions
    for G — or, at the router level, that user B's `GET /library/games` never exposes user A's
    positions. Prefer the repository-level direct assertion for precision.

    Follow the existing Sentry/test conventions in the file; do not add production code — the router is
    unchanged. Annotate tests with explicit return types (ty compliance).
  </action>
  <verify>
    <automated>uv run pytest tests/test_library_router.py -x -k "eval_series or unanalyzed" && uv run ty check tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_library_router.py -x -k eval_series` exits 0 (analyzed card has non-null eval fields).
    - `uv run pytest tests/test_library_router.py -x -k unanalyzed` exits 0 (unanalyzed card has null eval fields).
    - The query-count test asserts the page query count is invariant to the analyzed-game count (no N+1) and passes.
    - The IDOR test asserts user B cannot read user A's positions and passes.
    - `uv run ty check tests/` exits 0.
  </acceptance_criteria>
  <done>Integration tests prove inline payload presence/nullity, single batched query, and user-scoping; all green.</done>
</task>

<task type="auto">
  <name>Task 2: Measure gzipped payload delta (D-05)</name>
  <files>tests/test_library_router.py</files>
  <read_first>
    - 109-RESEARCH.md §"Payload Size Verification (D-05)" (the measurement approach + the <20 KB compressed acceptance threshold)
    - 109-CONTEXT.md D-05 (typed EvalPoint[], rounded ES, rely on gzip; revisit columnar only if a real regression)
    - tests/test_library_router.py (the response object the test client returns; gzip serialization)
  </read_first>
  <action>
    Measure the gzipped size of the `GET /library/games` response for a representative 20-game page
    (mix of analyzed + unanalyzed) and compare against a baseline with the three new fields removed.
    Implement this as a lightweight assertion in `tests/test_library_router.py`: serialize the
    response JSON, gzip it (`gzip.compress(json_bytes)`), and assert the compressed size for the
    full page is below a documented ceiling consistent with RESEARCH's estimate (delta < 20 KB
    compressed for 20 analyzed games). Use a named constant for the threshold (no magic number).
    Record the actual measured compressed bytes (full response and, if practical, the delta) in the
    plan SUMMARY so D-05 has a concrete number. If the measured delta exceeds the threshold, do NOT
    silently pass — flag it in the SUMMARY as a regression requiring the deferred columnar encoding
    (per D-05); do not implement columnar encoding in this phase.
  </action>
  <verify>
    <automated>uv run pytest tests/test_library_router.py -x -k "payload or gzip or eval_series"</automated>
  </verify>
  <acceptance_criteria>
    - A gzipped-payload test exists and passes, asserting compressed size below a named threshold constant.
    - The measured compressed byte count is recorded in 109-03-SUMMARY.md (concrete number, not "negligible").
    - No columnar/wire-format change is introduced (D-05 deferral respected).
  </acceptance_criteria>
  <done>Gzipped payload delta is measured, asserted under threshold, and recorded; no perceptible regression confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /library/games | authenticated; user_id from JWT. This plan adds tests, not new boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Information Disclosure (IDOR) | `fetch_page_eval_positions` | mitigate | This plan adds the automated assertion that user B cannot read user A's positions — verifying the `GamePosition.user_id == user_id` control shipped in plan 01. |
| T-109-03 | Denial of Service (payload bloat) | inline eval fields | mitigate | Task 2 measures the gzipped delta against a named ceiling and records it; pagination bounds the page at 20 games. |
</threat_model>

<verification>
- `uv run pytest tests/test_library_router.py -x -k "eval_series or unanalyzed or payload"` green
- Query-count and IDOR assertions pass
- `uv run ty check tests/` zero errors
</verification>

<success_criteria>
- Analyzed cards expose non-null eval fields; unanalyzed cards expose null.
- Single batched positions query (no N+1) proven by an invariant query-count assertion.
- Cross-user positions disclosure blocked (IDOR test green).
- Gzipped payload delta measured, under threshold, and recorded in the summary.
</success_criteria>

<output>
Create `.planning/phases/109-per-card-expected-score-eval-chart-games-subtab/109-03-SUMMARY.md` when done
</output>
