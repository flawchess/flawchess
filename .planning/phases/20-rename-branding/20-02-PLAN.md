---
phase: 20-rename-branding
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: []
autonomous: false
requirements:
  - BRAND-04

must_haves:
  truths:
    - "User has transferred the repo to flawchess org on GitHub"
    - "Git remote origin points to git@github.com:flawchess/flawchess.git"
    - "git push succeeds to the new remote"
  artifacts: []
  key_links:
    - from: "local git remote"
      to: "github.com/flawchess/flawchess"
      via: "git remote set-url"
      pattern: "flawchess/flawchess"
---

<objective>
Transfer the GitHub repository to the flawchess organization and update the local git remote.

Purpose: The repo must live at github.com/flawchess/flawchess before any external services (Sentry, CI/CD, Caddy config) reference it. GitHub auto-redirects the old URL for 12 months.

Output: Git remote points to flawchess/flawchess. Push/pull works against the new remote.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-rename-branding/20-CONTEXT.md
@.planning/phases/20-rename-branding/20-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User transfers repo to flawchess GitHub organization</name>
  <read_first>.planning/phases/20-rename-branding/20-CONTEXT.md</read_first>
  <action>
The user must perform the GitHub repo transfer manually. This cannot be automated by Claude.

**Steps for the user:**

1. Go to https://github.com/aimfeld/chessalytics/settings
2. Scroll to "Danger Zone" at the bottom
3. Click "Transfer ownership"
4. Set the new owner to: `flawchess`
5. Set the new repository name to: `flawchess`
6. Confirm by typing the repository name
7. Wait for transfer to complete (usually instant)

**What happens after transfer:**
- The repo moves to `github.com/flawchess/flawchess`
- GitHub auto-redirects `github.com/aimfeld/chessalytics` to the new URL for 12 months
- All git history, issues, and PRs are preserved

**Also rename local project directory:**
```bash
cd /home/aimfeld/Projects/Python
mv chessalytics flawchess
cd flawchess
```
  </action>
  <acceptance_criteria>
    - User confirms repo transfer is complete
    - User confirms local directory has been renamed
  </acceptance_criteria>
  <resume-signal>Type "transferred" when both the GitHub repo transfer and local directory rename are complete</resume-signal>
  <done>GitHub repo lives at github.com/flawchess/flawchess and local directory is renamed to flawchess</done>
</task>

<task type="auto">
  <name>Task 2: Update git remote to point to flawchess organization</name>
  <files></files>
  <read_first></read_first>
  <action>
After user confirms the transfer is complete, update the local git remote:

```bash
git remote set-url origin git@github.com:flawchess/flawchess.git
```

Verify the remote is correct:
```bash
git remote -v
```

Expected output:
```
origin  git@github.com:flawchess/flawchess.git (fetch)
origin  git@github.com:flawchess/flawchess.git (push)
```

Test connectivity by fetching:
```bash
git fetch origin
```
  </action>
  <verify>
    <automated>git remote -v | grep "flawchess/flawchess"</automated>
  </verify>
  <acceptance_criteria>
    - `git remote -v` output contains `git@github.com:flawchess/flawchess.git` for both fetch and push
    - `git fetch origin` exits with code 0 (connectivity works)
  </acceptance_criteria>
  <done>Git remote points to flawchess/flawchess. Fetch/push works against the new remote.</done>
</task>

</tasks>

<verification>
1. `git remote -v` shows `flawchess/flawchess.git` for both fetch and push
2. `git fetch origin` succeeds
</verification>

<success_criteria>
- Git remote origin points to git@github.com:flawchess/flawchess.git
- git fetch succeeds against the new remote
- GitHub repo is accessible at github.com/flawchess/flawchess
</success_criteria>

<output>
After completion, create `.planning/phases/20-rename-branding/20-02-SUMMARY.md`
</output>
