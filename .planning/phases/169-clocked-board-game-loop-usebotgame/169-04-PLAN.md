---
phase: 169-clocked-board-game-loop-usebotgame
plan: 04
type: execute
wave: 2
depends_on: [169-01, 169-02, 169-03]
files_modified:
  - frontend/src/hooks/useBotGame.ts
  - frontend/src/hooks/__tests__/useBotGame.test.ts
autonomous: true
requirements: [PLAY-03, PLAY-04, PLAY-05, PLAY-06, PLAY-07, PLAY-08, PLAY-09]
must_haves:
  truths:
    - "The user moves only on their turn to legal squares; illegal/off-turn/off-live-position moves are rejected"
    - "Both clocks tick from a wall-clock delta model and pause while the tab is hidden during the bot's turn"
    - "The bot paces replies at the fixed shipped budget, floored by a randomized reveal delay, debited max(real,synthetic) clamped never-flag; its clock ticks in real time and never freezes"
    - "Every end condition including flag-on-time ends the game and produces a result outcome"
    - "Resign (confirmed) and draw offer/accept/decline behave per D-01..D-04; the bot never offers/resigns"
    - "Move/capture/check/game-end + the two D-09 sounds fire on the right events, honoring mute"
    - "On game end the hook exposes a serializable finished PGN with both-color [%clk] and the correct Termination"
  artifacts:
    - frontend/src/hooks/useBotGame.ts
    - frontend/src/hooks/__tests__/useBotGame.test.ts
  key_links:
    - "useBotGame is the stable seam Phase 170 (serialize) and Phase 171 (settings in, PGN out) consume — game state must stay serializable"
    - "selectBotMove AbortSignal must cancel in-flight thinks on resign/new-game/unmount"
    - "Imports FLAWCHESS_BOT_* from @/lib/engine/botBudget directly (NOT useFlawChessEngine re-export — Pitfall 6)"
---

<objective>
Build `useBotGame(settings)` — the orchestrating game-loop hook that composes
the plan-01/02/03 pure modules with the frozen `selectBotMove` engine core and
the existing `ChessBoard`, delivering the full clocked bot game: turn-gated
legal moves + Fischer increment (PLAY-03), wall-clock dual clocks with
hidden-tab pause (PLAY-04), the locked 168.5 bot pacing model (PLAY-05), all
end conditions incl. flag-on-time (PLAY-06), resign/draw per D-01..D-04
(PLAY-07), event sounds (PLAY-08), and a serializable finished PGN + result
outcome (PLAY-09).

Purpose: This is the phase's core. It is pure orchestration — chess.js owns
rules, selectBotMove owns move selection, the plan-01/02/03 modules own
timing/end-detection/draw-gate/PGN/sound. The hook wires them and exposes the
stable state+callbacks contract Phases 170/171 build on.

Output: `useBotGame.ts` and `useBotGame.test.ts` covering turn-gate, pacing,
end-conditions, resign-draw, and pgn-export with fake timers + a mocked
selectBotMove.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md
@.planning/phases/166-bot-move-selection-core-selectbotmove/166-CONTEXT.md
@frontend/src/hooks/useFlawChessEngine.ts
@frontend/src/hooks/useChessGame.ts
@frontend/src/lib/engine/selectBotMove.ts
@frontend/src/lib/engine/botBudget.ts
@frontend/src/lib/chessClock.ts
@frontend/src/lib/botGameEnd.ts
@frontend/src/lib/botDrawGate.ts
@frontend/src/lib/botGamePgn.ts
@frontend/src/lib/sounds.ts
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — not drift):
- `frontend/src/hooks/useBotGame.ts`: `BotGameSettings` (botElo, blend, baseSeconds, incrementSeconds, userColor); `UseBotGameState` (position, moveHistory, liveGamePly, viewedPly, isBotThinking, whiteClockMs, blackClockMs, activeColor, outcome, pgn, drawOfferPending, canOfferDraw); callbacks `attemptMove(from,to)`, `viewPly(ply)`, `returnToLive()`, `resign()`, `offerDraw()`, `newGame()`; hook `useBotGame(settings)`
- `frontend/src/hooks/__tests__/useBotGame.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useBotGame core — state, move-commit + turn-gate, dual clocks, visibility pause, end detection</name>
  <files>frontend/src/hooks/useBotGame.ts</files>
  <read_first>
    - frontend/src/hooks/useChessGame.ts (lines 160-280 — chessRef, functional setMoveHistory to avoid stale closures, arrow-key nav guard against typing-in-input; the move-commit shape to mirror)
    - frontend/src/hooks/useFlawChessEngine.ts (lines 144-160 provider mount-once effect; 289-304 abort-on-unmount guard — the lifecycle discipline to mirror; do NOT copy the debounced-FEN search effect)
    - frontend/src/lib/chessClock.ts (applyIncrementMs, computeElapsedMs, shiftAnchorForPause, isLowTime — plan 01)
    - frontend/src/lib/botGameEnd.ts (detectEndCondition, BotGameOutcome — plan 02)
    - frontend/src/components/board/ChessBoard.tsx (onPieceDrop contract: `(source,target)=>boolean`; the caller owns turn-gating by returning false — Pattern in 169-PATTERNS.md ChessBoard section)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md (Pattern 4 visibility pause; Anti-Patterns — do not re-derive rules; use chess.js is*())
  </read_first>
  <behavior>
    - attemptMove(from,to): returns false (board snaps back) unless it is the user's turn AND the board is at the live position (viewedPly===liveGamePly) AND chess.move is legal. On a legal user move: auto-promote to queen (promotion:'q') per useAnalysisBoard precedent (RESEARCH Pitfall 2 — no promotion dialog); apply Fischer increment to the mover's clock via applyIncrementMs; append SAN; advance liveGamePly/viewedPly; then run detectEndCondition and, if not over, hand the turn to the bot.
    - dual clocks: whiteClockMs/blackClockMs start at baseSeconds*1000; the ACTIVE side's clock is derived on each ~100ms tick from computeElapsedMs(anchor, Date.now()) — never accumulated tick counts (PLAY-04).
    - visibility pause: a visibilitychange effect shifts the active turn anchor by the hidden duration via shiftAnchorForPause so no time is charged while hidden (PLAY-04) — matters during the bot's turn.
    - flag-on-time: when the active side's derived remaining reaches 0, end the game with a 'timeout' outcome (the ONLY loop-owned end condition; all others via detectEndCondition).
    - view-only: viewPly(ply) sets viewedPly (board shows that historical position, input disabled); returnToLive() snaps viewedPly back to liveGamePly (D-13).
  </behavior>
  <action>
    Create frontend/src/hooks/useBotGame.ts. Define BotGameSettings and
    UseBotGameState per the artifacts block. Keep a chessRef (chess.js
    instance) and refs for clock anchors, per the functional-updater discipline
    (169-PATTERNS.md "Functional state updates" — the bot async path reads state
    after awaits). Model viewedPly and liveGamePly as TWO separate numbers
    (D-13, RESEARCH Pitfall 5 two-state lesson) — never overload one ply
    pointer. Implement attemptMove with the turn+live+legality gate (PLAY-03),
    auto-queen promotion (Pitfall 2), Fischer increment, and end detection
    (PLAY-06). Implement the ~100ms setInterval display tick that recomputes the
    active clock from Date.now() deltas (PLAY-04) and fires the timeout outcome
    at zero. Implement the visibilitychange anchor-shift effect (PLAY-04,
    Pattern 4). Provide viewPly/returnToLive (D-13). Provider bring-up
    (createWorkerPool/createMaiaQueue mount-once) and the bot-turn dispatch land
    in Task 2 — leave a clearly-named seam (e.g. a `runBotTurn` ref or effect
    trigger keyed on "bot to move") so Task 2 fills it. Import FLAWCHESS_BOT_*
    from @/lib/engine/botBudget directly (Pitfall 6 — never via
    useFlawChessEngine's re-export; do NOT touch useFlawChessEngine.ts). Keep
    game state serializable (no class instances beyond the chessRef, which is
    reconstructable from FEN/PGN) for Phase 170. Respect CLAUDE.md nesting (<=3)
    and logic-LOC limits — extract helpers if the hook body grows.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useBotGame.ts` exists, exports useBotGame + BotGameSettings + UseBotGameState, and `cd frontend && npx tsc -b` passes.
    - attemptMove returns false when it is not the user's turn OR viewedPly !== liveGamePly (turn+live gate) — verified in Task 3 tests.
    - The clock tick recomputes from Date.now() deltas (grep shows computeElapsedMs usage, not `count * intervalMs`).
    - `grep -n "from '@/lib/engine/botBudget'" frontend/src/hooks/useBotGame.ts` succeeds AND `git diff --stat` shows useFlawChessEngine.ts is NOT modified (Pitfall 6).
    - viewedPly and liveGamePly are two distinct state values (grep shows both).
  </acceptance_criteria>
  <done>The hook holds a chess.js game with turn-gated auto-queen moves, Fischer increment, wall-clock dual clocks with visibility-pause + flag-on-time, board end-condition detection, and view-only ply navigation — compiling and importing bot constants from botBudget directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useBotGame bot turn — provider bring-up, D-05 pacing, resign/draw, sounds, PGN export</name>
  <files>frontend/src/hooks/useBotGame.ts</files>
  <read_first>
    - frontend/src/lib/engine/selectBotMove.ts (signature + BotSettings/BotMoveDeps + the elo/blend/budget shape and AbortSignal — frozen contract)
    - frontend/src/hooks/useFlawChessEngine.ts (lines 254-276 — `providers = { policy: queue.policy, grade: pool.grade }` deps mapping + the Sentry catch that skips on signal.aborted)
    - frontend/src/lib/chessClock.ts (computeRevealDelayMs, computeSyntheticDebitMs, reconcileBotDebitMs — plan 01)
    - frontend/src/lib/botDrawGate.ts (canOfferDraw, wouldBotAcceptDraw — plan 02)
    - frontend/src/lib/botGamePgn.ts (annotateClock, finalizeBotPgn, formatClockHms, toBackendTcStr — plan 02)
    - frontend/src/lib/sounds.ts (playSound, unlockAudio — plan 03)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md (Pattern 1/2/3 provider bring-up + per-turn AbortController + Promise.all reveal-delay; Pitfall 3 isBotThinking from the real promise; Pitfall 6)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md (D-01..D-05, D-09)
  </read_first>
  <behavior>
    - provider lifecycle: createWorkerPool()+createMaiaQueue() once per game (mount effect), terminated on unmount — NOT re-run per FEN (Pattern 1).
    - bot turn dispatch: when it becomes the bot's turn, start the real-time clock tick on the bot's clock (never frozen, D-05/D-06), create a FRESH AbortController (never shared across turns), and run Promise.all([selectBotMove(fen, {elo:botElo,blend,budget:{maxNodes:FLAWCHESS_BOT_MAX_NODES,maxPlies:FLAWCHESS_BOT_MAX_PLIES,concurrency:FLAWCHESS_BOT_CONCURRENCY,stopRule:FLAWCHESS_BOT_STOP_RULE}}, {policy:queue.policy,grade:pool.grade,rng:Math.random}, signal), delay(computeRevealDelayMs(Math.random))]) (Pattern 3 — Promise.all, NOT Promise.race). isBotThinking is derived from the in-flight promise (Pitfall 3), not a fixed timer.
    - on resolve: realElapsed = Date.now()-turnStart; debit = reconcileBotDebitMs(realElapsed, computeSyntheticDebitMs(botRemaining, incrementMs), botRemaining) (D-05 max-then-clamp, never-flag); apply debit + Fischer increment to the bot clock; commit the bot's chess.move (auto-queen), annotateClock, detect end conditions.
    - resign(): a CONFIRMED resign (the confirmation UI lives in plan 05; the hook exposes resign() as the post-confirm action, D-04) aborts any in-flight think and ends the game with a 'resignation' outcome (user loses). The bot NEVER resigns (D-03).
    - offerDraw(): only permitted when canOfferDraw(movesSinceLastDecline) (D-04 throttle); evaluates wouldBotAcceptDraw(lastRootPracticalScore, chess) (D-01) — accept → draw-by-agreement outcome; decline → increment the decline cooldown, reset movesSinceLastDecline, and playSound('draw-declined') (D-09). The bot NEVER offers a draw itself (D-02).
    - sounds: playSound fires on user AND bot moves ('capture' when a piece was captured, 'check' when the resulting position is check, else 'move'), 'game-end' on any terminal outcome, 'low-time' ONCE when the USER's clock first crosses the low-time threshold (D-09, not repeating), 'draw-declined' on a declined offer. Honors mute (sounds.ts). Call unlockAudio() on the first user gesture (first attemptMove).
    - pgn: on game end, finalizeBotPgn(chess, outcome, toBackendTcStr(baseSeconds, incrementSeconds)) is exposed as state.pgn (both-color [%clk] already annotated per move).
    - abort: resign/newGame/unmount abort the in-flight controller (Pattern 2). Sentry.captureException on a genuine selectBotMove rejection, skipped when signal.aborted, tag { source: 'bot-game' }.
  </behavior>
  <action>
    Extend useBotGame.ts filling the Task-1 bot-turn seam. Add the mount-once
    provider effect (Pattern 1) and per-turn AbortController discipline
    (Pattern 2). Implement the D-05 reveal-delay+search Promise.all
    reconciliation (Pattern 3) using the plan-01 helpers; drive isBotThinking
    off the real promise (Pitfall 3). Wire resign()/offerDraw()/newGame()
    callbacks and the movesSinceLastDecline cooldown counter (D-04) as state
    SEPARATE from the accept-gate (Pitfall 5). Fire sounds per the behavior
    block; the user low-time sound fires exactly once at the first threshold
    crossing (track a hasFiredLowTime ref, D-09). Expose state.pgn via
    finalizeBotPgn on game end (PLAY-09). Add the Sentry catch mirroring
    useFlawChessEngine (skip on aborted, tag source 'bot-game'). Do NOT modify
    useFlawChessEngine.ts or any engine-core file (all frozen). Keep helpers
    small; if the bot-turn function exceeds nesting/LOC limits, extract a
    named async helper.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc -b` passes.
    - `grep -n "Promise.all" frontend/src/hooks/useBotGame.ts` present and `grep -n "Promise.race" frontend/src/hooks/useBotGame.ts` absent (Pattern 3, reveal delay is a floor not a race).
    - `grep -n "reconcileBotDebitMs" frontend/src/hooks/useBotGame.ts` present (D-05 debit).
    - No resign or draw-offer path exists on the bot's side (D-02/D-03) — resign()/offerDraw() are user-initiated only.
    - `grep -n "source: 'bot-game'" frontend/src/hooks/useBotGame.ts` present; the catch skips when signal.aborted.
    - `git diff --name-only` does NOT include frontend/src/hooks/useFlawChessEngine.ts or any frontend/src/lib/engine/* file (frozen).
  </acceptance_criteria>
  <done>The bot paces via Promise.all reveal-delay + fixed-budget search with a never-flag max(real,synthetic) debit, its clock ticks in real time, resign/draw obey D-01..D-04 (bot never offers/resigns), sounds fire per event honoring mute, and the finished PGN carries both-color [%clk] — all without touching frozen engine files.</done>
</task>

<task type="auto">
  <name>Task 3: useBotGame tests — turn-gate, pacing, end-conditions, resign-draw, pgn-export</name>
  <files>frontend/src/hooks/__tests__/useBotGame.test.ts</files>
  <read_first>
    - frontend/src/hooks/__tests__/useFlawChessEngine.test.ts (project React-hook test conventions: renderHook, act, vi.useFakeTimers/advanceTimersByTime, mocking engine providers)
    - frontend/src/hooks/useBotGame.ts (the hook under test, Tasks 1-2)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map: the exact -t filters "turn-gate"/"pacing"/"end-conditions"/"resign-draw"/"pgn-export"; Wave 0 Gaps — this is the first repo test combining fake timers with a continuously-ticking clock, budget for its complexity)
  </read_first>
  <action>
    Create frontend/src/hooks/__tests__/useBotGame.test.ts using renderHook +
    vi.useFakeTimers({ now: 0 }). MOCK selectBotMove (vi.mock the module) to
    resolve a scripted legal UCI after a controlled fake-timer delay, and stub
    the provider factories (createWorkerPool/createMaiaQueue) so no real workers
    spawn. Mock the Audio global and sounds module as needed. Cover, with
    describe/test names containing the RESEARCH filter tokens: (1) "turn-gate" —
    attemptMove rejects an off-turn move and an off-live-position move, accepts a
    legal user move applying Fischer increment. (2) "pacing" — advancing timers
    shows the bot clock ticking down during the think; on resolve the bot debit
    equals max(real, synthetic) clamped, and the bot clock never reaches 0.
    (3) "end-conditions" — seed FENs (via a settings/initial-FEN hook input or
    by playing scripted moves) for checkmate/stalemate/threefold/50-move/
    insufficient-material each producing the right outcome, plus a flag-on-time
    case (user clock hits 0 → 'timeout'). (4) "resign-draw" — resign() ends with
    'resignation'; offerDraw() is blocked during cooldown, wouldBotAccept path
    yields a draw outcome, decline fires draw-declined + resets cooldown; assert
    the bot never auto-offers/resigns. (5) "pgn-export" — after a scripted short
    game, state.pgn contains {[%clk ...]} for both colors and the correct
    [Termination]. Guard against test flake by driving all time via fake timers
    (no real setTimeout waits).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useBotGame.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/hooks/__tests__/useBotGame.test.ts` passes (all green, no flake across two runs).
    - Tests named/described with each token turn-gate, pacing, end-conditions, resign-draw, pgn-export exist and pass (`-t` filters resolve).
    - The pacing test asserts the bot clock is > 0 after the debit (never-flag).
    - The end-conditions test includes a flag-on-time ('timeout') case.
    - The pgn-export test asserts {[%clk on both a white and black ply.
    - selectBotMove and the worker/maia factories are mocked (no real engine spawned) — grep shows vi.mock.
  </acceptance_criteria>
  <done>useBotGame.test.ts is green covering PLAY-03 (turn-gate), PLAY-04/05 (pacing + never-flag), PLAY-06 (all end conditions + flag), PLAY-07 (resign/draw + bot-never-offers), and PLAY-09 (pgn-export), with selectBotMove mocked and time driven by fake timers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client game state → future PGN POST | The hook produces the finished PGN Phase 171 will POST; the backend (Phase 167) is the trust boundary and already validates it. |
| in-flight bot search → CPU | selectBotMove runs client-side workers; bounded by the frozen botBudget (fixed nodes/plies/concurrency). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-169-01 | Tampering | Client game state feeding the finished PGN | medium | mitigate | Backend-validated downstream (Phase 167); the hook produces an honest game. No client-side fairness enforcement duplicated. |
| T-169-03 | Denial of Service | Runaway client-side search (many tabs) | low | accept | Bounded by botBudget's fixed FLAWCHESS_BOT_MAX_NODES/plies/pinned concurrency (168.5); entirely client-CPU-bound, no server cost — affects only the user's own device (RESEARCH Security Domain). Per-turn AbortController cancels orphaned thinks on resign/new-game/unmount. |
| T-169-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed (selectBotMove/chess.js already present). No supply-chain surface added. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b` passes.
- `cd frontend && npx vitest run src/hooks/__tests__/useBotGame.test.ts` green (twice, no flake).
- `git diff --name-only` confirms no frozen engine file (useFlawChessEngine.ts, lib/engine/*) modified.
- `cd frontend && npm run lint` clean.
</verification>

<success_criteria>
useBotGame delivers the full clocked bot game — turn-gated moves + increment,
wall-clock clocks with hidden-tab pause + flag, 168.5 pacing with never-flag
debit, all end conditions, resign/draw per D-01..D-04, event sounds, and a
serializable both-color-[%clk] PGN — with all PLAY-03..09 test tokens green and
no frozen file touched.
</success_criteria>

<output>
Create `.planning/phases/169-clocked-board-game-loop-usebotgame/169-04-SUMMARY.md` when done.
</output>
