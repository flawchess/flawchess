---
phase: 169-clocked-board-game-loop-usebotgame
plan: 09
type: execute
wave: 7
depends_on: [169-08]
gap_closure: true
files_modified:
  - frontend/src/hooks/useBotGame.ts
  - frontend/src/hooks/__tests__/useBotGame.test.ts
  - frontend/src/pages/Bots.tsx
  - frontend/src/components/bots/GameControls.tsx
  - frontend/src/lib/engine/botBudget.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md
autonomous: true
requirements: [PLAY-03, PLAY-04, PLAY-05, PLAY-06, PLAY-07]
must_haves:
  truths:
    - "The bot is debited the real wall-clock time its turn consumed (search + reveal delay) plus the Fischer increment on commit — the same rule the user's clock obeys (D-15)"
    - "The bot can lose on time: a bot whose think outlasts its remaining clock produces a timeout LOSS for the bot (amended SC1)"
    - "The bot enforces a per-move think deadline derived from its remaining clock, cutting the search and committing its best move so far instead of discarding the turn (D-16/D-17)"
    - "A cancel (resign, new game, unmount, bot flagged) still discards the in-flight turn — no move is committed (D-17)"
    - "Time while the tab is hidden during the bot's think is not charged to the bot's COMMITTED debit, not merely to its displayed clock (D-20 / amended SC2)"
    - "A move committed while the tab is still hidden cannot produce a future-dated anchor on resume (WR-02) — no side is ever credited phantom time"
    - "finalizeGame is idempotent: the first outcome wins, so a draw-accept resolving after a checkmate cannot overwrite the checkmate outcome or PGN (WR-03)"
    - "A bot move no longer ejects the user from D-13 view-only scroll-back (WR-05)"
    - "The draw-cooldown tooltip only appears when the cooldown is actually active (WR-04)"
  artifacts:
    - frontend/src/hooks/useBotGame.ts
    - frontend/src/hooks/__tests__/useBotGame.test.ts
    - frontend/src/pages/Bots.tsx
  key_links:
    - "useBotGame builds deps.search from createDeadlineSearch(computeThinkDeadlineMs(botRemaining, increment)) — the ONLY wiring point where the deadline reaches the engine"
    - "The bot's committed debit reads turnStartedAtRef LIVE at resolution time, so the visibility-pause anchor shift reaches it (D-20)"
    - "The clock-tick effect's flag check stays UNGUARDED by color — the bot flagging is now correct behavior, not a bug"
---

<objective>
Rewire `useBotGame` onto the honest clock (D-15) and the per-move think deadline
(D-16), fix the hidden-tab debit exploit (D-20), make finalization idempotent
(WR-03), stop bot moves from ejecting the user out of scroll-back (WR-05), fix
the dead draw-cooldown prop (WR-04), and amend every document that still asserts
the reversed "bot never flags" invariant.

Purpose: This closes the two BLOCKED requirements (PLAY-04, PLAY-05) and the two
failed success criteria (amended SC1, SC2) from `169-VERIFICATION.md`. The bot is
now a clock-managing player that can lose on time — the same rules as the user.

Output: an honest, deadline-managed `useBotGame`; six new regression tests that
cover exactly the behaviors whose absence let these gaps ship; correct
draw-cooldown wiring; and amended docs (`botBudget.ts` D-19 caveat, the 168.5
decision records, REQUIREMENTS.md traceability).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-VERIFICATION.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-REVIEW.md
@.planning/phases/169-clocked-board-game-loop-usebotgame/169-PATTERNS.md
@frontend/src/hooks/useBotGame.ts
@frontend/src/lib/chessClock.ts
@frontend/src/lib/engine/deadlineSearch.ts
@frontend/src/lib/engine/selectBotMove.ts
@frontend/src/lib/engine/botBudget.ts
@frontend/src/pages/Bots.tsx
@frontend/src/components/bots/GameControls.tsx
</context>

<critical_context>
**The controlling document is `169-CONTEXT.md` §"Decision Amendments — 2026-07-13"
(D-15..D-20), plus the AMENDED ROADMAP SC1/SC2.** `169-VERIFICATION.md` is
authoritative on WHAT is broken and WHERE (its file:line evidence is correct), but
its `missing:` prescriptions are SUPERSEDED and must NOT be followed:

- VERIFICATION says: "Restrict the loop-owned flag check to the user's color only:
  `if (remaining <= 0 && activeColor === settings.userColor)`". **DO NOT DO THIS.**
  The bot flagging is now intended behavior (D-15/D-16, amended SC1). The
  clock-tick effect's flag check stays exactly as it is — ungated by color.
- VERIFICATION's "the bot never flags" language throughout is the reversed
  invariant. Ignore it.

Where VERIFICATION.md and D-15..D-20 conflict, **D-15..D-20 win.**

**Do NOT touch the frozen engine core:** `mctsSearch.ts` and `selectBotMove.ts` are
not modified. `botBudget.ts` is edited in Task 3 for ONE thing only — the D-19
documentation comment. Its calibrated constants (maxNodes 50, maxPlies 8,
concurrency 4, the stop rule) do not change.
</critical_context>

<artifacts_this_phase_produces>
Symbols CREATED by this plan:
- `frontend/src/hooks/useBotGame.ts`: internal helper `resetTurnAnchor()` (re-baselines `turnStartedAtRef` and any in-progress `pausedAtRef` together); internal ref `outcomeRef` (idempotency latch); internal ref `viewedPlyRef` (scroll-back preservation)

Symbols CONSUMED (created by plan 08): `computeThinkDeadlineMs`, `createDeadlineSearch`, `BOT_MIN_SEARCH_NODES`

Symbols REMOVED from `useBotGame.ts` (call sites of plan 08's deletions): the
imports and uses of the synthetic-debit helper and the never-flag reconciliation
helper.

Public API of `useBotGame` (`UseBotGameState`, `BotGameSettings`) is UNCHANGED —
Phases 170/171 consume it as a stable contract.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useBotGame — honest debit, per-move think deadline, hidden-tab debit fix, idempotent finalize, scroll-back preservation</name>
  <files>frontend/src/hooks/useBotGame.ts</files>
  <read_first>
    - frontend/src/hooks/useBotGame.ts (the WHOLE file — every change in this task lands in it; note lines 232-242 finalizeGame, 250-287 commitMove, 263-267 the setViewedPly-inside-updater bug, 351-361 the draw-resolution effect, 407-438 the clock tick, 442-454 the visibility handler, 474-568 runBotTurn incl. the 489 dispatch-time snapshot and 509/515 abort discards)
    - frontend/src/lib/chessClock.ts (post-plan-08: computeThinkDeadlineMs + its constants; applyIncrementMs, computeElapsedMs, shiftAnchorForPause, computeRevealDelayMs still present)
    - frontend/src/lib/engine/deadlineSearch.ts (createDeadlineSearch signature + BOT_MIN_SEARCH_NODES + the inner/outer signal contract — plan 08)
    - frontend/src/lib/engine/selectBotMove.ts (lines 80-147 — `BotMoveDeps.search?: SearchRunner` is where the wrapper is injected; selectBotMove does NOT throw on abort, it resolves with best-so-far)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-CONTEXT.md (D-15..D-20 — the controlling decisions)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-REVIEW.md (WR-02 the future-dated anchor, WR-03 the finalize race, WR-05 the scroll-back eject — the three non-blocking items folded in here)
  </read_first>
  <behavior>
    - Honest debit (D-15): on bot move commit, the debit is the real wall-clock elapsed time of the turn (search + reveal delay), read from the LIVE turn anchor. No synthetic amount, no never-flag clamp. The Fischer increment is then applied by the existing shared commitMove, exactly as for the user.
    - Bot can flag (amended SC1): with a low remaining clock and a think that outlasts it, the existing clock-tick effect fires a timeout outcome whose WINNER is the user. The in-flight bot turn is cancelled and its move is NOT committed.
    - Think deadline (D-16/D-17): before dispatching a bot turn, compute the deadline from the bot's remaining clock + increment; inject a deadline-cut search into selectBotMove's `deps.search`. A deadline-cut search still commits the bot's best move so far. A cancel (resign / new game / unmount / bot flagged) still discards the turn.
    - Reveal delay is bounded by the deadline: a low-clock bot is never pushed over its own deadline by the cosmetic reveal-delay floor.
    - Hidden-tab debit (D-20): a tab hidden for N ms during the bot's think does not add N ms to the bot's COMMITTED debit (not just its displayed clock).
    - Mid-hide commit (WR-02): when the turn anchor is reset while a pause is in progress, the pause baseline is reset with it — on resume, no anchor lands in the future and no negative elapsed (phantom time) is ever credited.
    - Idempotent finalize (WR-03): the first outcome wins. A draw-accept resolving after a checkmate leaves the checkmate outcome, its PGN, and its single game-end sound intact.
    - Scroll-back preserved (WR-05): when the user is viewing an earlier ply and the bot commits a move, the viewed ply does NOT snap to live. It snaps only when the viewer was already at the live position.
  </behavior>
  <action>
    Edit `frontend/src/hooks/useBotGame.ts`.

    (1) D-15 honest debit. Drop the imports and uses of plan-08's deleted
    synthetic/reconciliation helpers. In the bot-turn resolution path, the debit
    becomes the real elapsed wall-clock time of the turn — computed with the
    same `computeElapsedMs(anchor, Date.now())` the user path already uses.

    (2) D-20 live anchor. Read `turnStartedAtRef.current` LIVE at resolution
    time. Delete the dispatch-time snapshot local entirely (do not keep it "for
    logging" — it is the bug). The live ref is the one the visibility handler
    shifts, so the pause reaches the committed debit.

    (3) WR-02 pause re-baseline. Introduce a small internal helper that resets
    the turn anchor to now AND, if a pause is currently in progress, re-baselines
    the pause timestamp to the same instant. Use it everywhere the anchor is
    reset (the move commit and the new-game reset). Without this, a bot move
    committing while the tab is hidden makes the resume handler shift the fresh
    anchor by the FULL pre-commit hidden duration, landing it in the future and
    crediting the next mover phantom time.

    (4) D-16/D-17 think deadline. Before each bot turn, compute the deadline from
    the bot's remaining clock and the increment via `computeThinkDeadlineMs`, and
    pass `search: createDeadlineSearch({ deadlineMs, minNodes: BOT_MIN_SEARCH_NODES })`
    in the deps object handed to `selectBotMove` (alongside the existing policy /
    grade / rng deps). Clamp the cosmetic reveal delay to the deadline so it can
    never push a low-clock bot past its own deadline. The node budget passed to
    selectBotMove stays EXACTLY the shipped `BOT_SEARCH_BUDGET` — the deadline
    cuts the search, it does not retune the budget (D-19).

    Document at the dispatch site, in a comment, the D-17 abort contract that
    makes this safe: the deadline aborts an INNER signal inside
    `createDeadlineSearch`, so `selectBotMove` resolves normally with its
    best-so-far move; the OUTER controller's `signal.aborted` therefore means
    exactly one thing — a CANCEL (resign / new game / unmount / bot flagged) —
    and the two existing aborted-checks that discard the turn are correct as-is
    for that meaning. Two abort reasons, separated by construction rather than by
    a reason tag.

    (5) WR-03 idempotent finalize. Add an outcome ref that `finalizeGame` checks
    and sets before doing anything else — first outcome wins, subsequent calls
    are no-ops (no second setOutcome, no PGN regeneration, no duplicate game-end
    sound). Reset it in the new-game path. Also guard the draw-resolution effect:
    if the game has already finished by the time it runs, clear the pending flag
    and return without evaluating the accept-gate. State the ref (not the
    `outcome` state) is required here because finalizeGame is called from async
    continuations and effects that hold stale render closures.

    (6) WR-05 scroll-back. Move `setViewedPly` OUT of the `setMoveHistory`
    updater (a state setter inside another setter's updater violates
    updater-purity — React may invoke updaters twice). Track the viewed ply in a
    ref kept in sync at every place it is set, and in the commit path snap to the
    new live ply ONLY when the viewer was already at the previous live ply.
    A bot move committed while the user is reviewing an earlier position must
    leave their view where it is (D-13; the user's own move can only be made from
    the live position anyway, so it always snaps).

    (7) Do NOT add a color guard to the clock-tick effect's flag check. The bot
    flagging is the intended behavior now. Update the comment there to say so
    explicitly, so the next reader (or verifier) does not "fix" it back.

    (8) Rewrite the module docstring: it currently advertises the
    "max(real, synthetic) never-flag debit" model. Replace with the D-15/D-16
    model (honest clock, per-move think deadline, the bot can lose on time) and
    reference the D-19 caveat that a deadline-cut bot in time trouble plays below
    its advertised ELO by design.

    Respect CLAUDE.md limits throughout (nesting <= 3, cognitive complexity, logic
    LOC) — `runBotTurn` is already dense; extract the deadline/deps assembly into
    a named helper rather than growing the async body.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc -b` passes (this restores the green type-check plan 08 deliberately left broken).
    - The synthetic-debit and never-flag helpers are no longer imported or referenced anywhere in the hook: `grep -rn "Synthetic\|SYNTHETIC\|NEVER_FLAG\|reconcileBotDebit" frontend/src/hooks/useBotGame.ts` returns no hits.
    - The dispatch-time anchor snapshot is gone (D-20): the bot's debit is computed from `turnStartedAtRef.current` read at resolution time — `grep -n "turnStartedAtRef.current" frontend/src/hooks/useBotGame.ts` shows a read inside the bot-turn async continuation, and no `const turnStartMs` local exists (`grep -c "turnStartMs" frontend/src/hooks/useBotGame.ts` is 0).
    - The deadline is wired through the injectable seam: `grep -n "createDeadlineSearch\|computeThinkDeadlineMs" frontend/src/hooks/useBotGame.ts` shows both, and the deps object passed to `selectBotMove` carries a `search` property.
    - `BOT_SEARCH_BUDGET` is unchanged and still assembled from `botBudget.ts`'s constants — `git diff frontend/src/hooks/useBotGame.ts` shows no edit to the budget object's fields.
    - The clock-tick flag check has NO `activeColor === settings.userColor` guard (the bot may flag) — `grep -n "reason: 'timeout'" frontend/src/hooks/useBotGame.ts` shows the call still reachable for either color, with a comment stating the bot flagging is intended (D-15).
    - `finalizeGame` returns early when an outcome already exists (idempotency latch present), and the draw-resolution effect returns early when the game has already finished.
    - `setViewedPly` is NOT called inside the `setMoveHistory` updater callback (WR-05).
    - `frontend/src/lib/engine/mctsSearch.ts` and `frontend/src/lib/engine/selectBotMove.ts` are unmodified: `git diff --stat` lists neither.
  </acceptance_criteria>
  <done>useBotGame runs an honest clock with a per-move think deadline: the bot is debited its real turn time (pause-corrected), speeds up when low, commits its best-so-far move when cut, can lose on time, cannot be exploited via a hidden tab, cannot have its outcome overwritten by a late draw-accept, and no longer ejects the user from scroll-back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useBotGame regression tests — the six behaviors whose absence let these gaps ship</name>
  <files>frontend/src/hooks/__tests__/useBotGame.test.ts</files>
  <read_first>
    - frontend/src/hooks/__tests__/useBotGame.test.ts (the WHOLE file — the existing mocks of selectBotMove / workerPool / maiaQueue / sounds / Sentry, the `advance()` fake-timer helper, and the "pacing" describe at lines 187-213 whose never-flag assertion is now WRONG and must be replaced)
    - frontend/src/hooks/useBotGame.ts (post-Task-1 — the deps object shape passed to selectBotMove, and the exact deadline/debit call sites the tests must pin)
    - frontend/src/lib/chessClock.ts (computeThinkDeadlineMs — tests assert against it, never against re-typed literals)
    - frontend/src/lib/engine/deadlineSearch.ts (createDeadlineSearch — mock this module to assert the hook passes the computed deadline and the node floor)
  </read_first>
  <behavior>
    - Test A (bot flags — the headline new behavior): a bot with a low remaining clock and a mocked think LONGER than its remaining time produces a timeout outcome whose winner is the USER, and the bot's move is never committed. The old suite had no test anywhere near this threshold — its only pacing test used a 300s clock with an 8s think.
    - Test B (deadline cut commits, D-16/D-17): the hook passes a `search` runner built from the computed deadline into selectBotMove's deps; a search that resolves via a deadline cut still COMMITS a legal move (the turn is not discarded). Assert the deadline handed to `createDeadlineSearch` equals `computeThinkDeadlineMs(botRemainingMs, incrementMs)` for the bot's current clock, and that the node floor passed is `BOT_MIN_SEARCH_NODES`.
    - Test C (cancel discards, D-17): a cancel during the bot's think (resign, or newGame) commits NO move even after the mocked search later resolves.
    - Test D (node floor, D-18): the floor value the hook passes is the reused stop-rule floor, never a smaller ad-hoc number — a deadline can never be configured to cut below it.
    - Test E (hidden-tab debit, D-20 / amended SC2): with the tab hidden for a long interval DURING the bot's think, the bot's clock after commit reflects only the VISIBLE elapsed time (+ increment) — assert on the committed clock value, not merely on the displayed value mid-think.
    - Test F (mid-hide commit, WR-02): a bot move that commits while the tab is STILL hidden, followed by a resume, never produces a negative elapsed / future-dated anchor — after resume the next mover's clock is <= its value at commit (no phantom time credited), and no clock ever exceeds its post-increment base.
    - Test G (idempotent finalize, WR-03): a draw offer whose resolution lands AFTER a bot move has delivered checkmate leaves the checkmate outcome and its PGN Result/Termination intact, and fires the game-end sound exactly once.
  </behavior>
  <action>
    Extend `frontend/src/hooks/__tests__/useBotGame.test.ts`.

    First, FIX the existing "pacing" describe: its assertion that the bot's clock
    "never reaches zero after the debit" encodes the reversed never-flag
    invariant and is now false by design. Replace it with the honest-clock
    assertion: after an N-second mocked think the bot's clock has dropped by
    approximately N seconds (minus the increment), i.e. the debit is the REAL
    elapsed time. Update the file's header comment block, which still advertises
    the reconciled-never-flag behavior in its numbered behavior list.

    Then add the seven behaviors above. Notes on driving them:

    - Mock `@/lib/engine/deadlineSearch` alongside the existing mocks so
      `createDeadlineSearch` is a spy: this is how Tests B and D assert the
      deadline and floor the hook computed, without spinning real workers
      (selectBotMove itself stays mocked, so the real wrapper is never invoked
      here — its cut/floor/cancel BEHAVIOR is covered by plan 08's
      deadlineSearch.test.ts; this suite covers the WIRING).
    - Test A: use a small `baseSeconds` and a mocked think whose setTimeout
      exceeds it, then advance fake timers past the bot's remaining time. Expect
      the timeout outcome with the user as winner and an unchanged move history.
    - Tests E/F: drive `document.visibilityState` in jsdom (redefine the property
      and dispatch a `visibilitychange` event) around `vi.advanceTimersByTimeAsync`
      to simulate the hidden interval — assert on `whiteClockMs`/`blackClockMs`
      AFTER the commit, which is what the committed debit produces (asserting
      mid-think only re-tests the display, which was never broken).
    - Test G: reuse the existing Fool's-mate script (the bot's `Qh4#` reply is the
      finalizing move) and call `offerDraw()` in the same act() window that the
      bot's mate commits, so the pending-draw effect resolves after finalization.
      Assert the outcome is still checkmate, the PGN still carries the checkmate
      Termination/Result, and `playSound` was called with the game-end event
      exactly once.
    - Keep the file's existing conventions: fake timers from mount
      (`vi.useFakeTimers({ now: 0 })`), the `advance()` act-wrapped helper, and
      mocked providers/sounds. Do not introduce real timers or real engine
      providers.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useBotGame.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/hooks/__tests__/useBotGame.test.ts` passes, with new tests covering ALL of: bot-flags-on-time (winner = user), deadline wiring (deadline value + node floor asserted against `computeThinkDeadlineMs` / `BOT_MIN_SEARCH_NODES`), deadline-cut still commits, cancel discards, hidden-tab committed debit, mid-hide commit anchor safety, finalize idempotency.
    - The old never-flag assertion is GONE: `grep -rn "never reaches zero\|never-flag\|never flag" frontend/src/hooks/__tests__/useBotGame.test.ts` returns no hits.
    - The bot-flag test asserts the winner is the USER's color (a timeout outcome for the bot), proving the amended SC1 behavior — not merely that some outcome exists.
    - The hidden-tab test asserts on a clock value read AFTER the bot's move commits (the committed debit), not on a mid-think displayed value.
    - The finalize-idempotency test asserts the game-end sound fired exactly once (`toHaveBeenCalledTimes`), which fails against a non-idempotent finalizeGame.
    - No real timers and no real engine providers are used (`grep -c "useFakeTimers" frontend/src/hooks/__tests__/useBotGame.test.ts` is >= 1; the selectBotMove / workerPool / maiaQueue / sounds mocks remain in place).
  </acceptance_criteria>
  <done>The six gap behaviors (plus the corrected pacing test) are locked by automated regression tests that fail against the pre-fix hook and pass against the new one.</done>
</task>

<task type="auto">
  <name>Task 3: Draw-cooldown prop wiring + doc amendments (D-19 in botBudget, 168.5 decision records, REQUIREMENTS traceability) + full frontend gate</name>
  <files>frontend/src/pages/Bots.tsx, frontend/src/components/bots/GameControls.tsx, frontend/src/lib/engine/botBudget.ts, .planning/REQUIREMENTS.md, .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md</files>
  <read_first>
    - frontend/src/pages/Bots.tsx (lines 96-115 — the single GamePanel shared by BOTH the mobile and desktop layouts; this is the only GameControls call site, so one fix covers both surfaces, but CONFIRM that before declaring done, per CLAUDE.md's "apply changes to mobile too" rule)
    - frontend/src/components/bots/GameControls.tsx (lines 14-24 the prop contract; lines 79-96 the unconditionally-rendered Tooltip)
    - .planning/phases/169-clocked-board-game-loop-usebotgame/169-REVIEW.md (WR-04 — the exact two compounding problems)
    - frontend/src/lib/engine/botBudget.ts (the whole file — its header still frames the budget as the bot's fixed, always-honored search cost)
    - .planning/phases/168.5-bot-move-pacing-search-budget-seed-096/168.5-CONTEXT.md (lines 30-55 — D-01/D-02/D-04/D-04b, the decision records that lock the reversed model)
    - .planning/REQUIREMENTS.md (lines 94-100 — the drifted per-phase traceability rows)
  </read_first>
  <action>
    (1) WR-04 draw-cooldown wiring. In `Bots.tsx`, stop hardcoding the cooldown
    prop to false. The hook's `canOfferDraw` IS the D-04 cooldown gate, so feed it
    into the cooldown prop (inverted), and pass the D-01 "not already pending /
    game not over" condition into the gate prop that is documented as such. The
    net disabled state must be unchanged; what changes is that the props now mean
    what their names and doc comments say. In `GameControls.tsx`, render the
    cooldown Tooltip only when the cooldown is actually active — an enabled
    "Offer draw" button must not show "Wait a few more moves before offering
    again" on hover. Keep the existing `data-testid`s and the brand-outline
    variants; do not restyle.

    (2) D-19 in `botBudget.ts` — DOCUMENTATION COMMENT ONLY. Do not change a
    single constant. Add to the module docstring: the ELO mapping was measured at
    the full node budget, so it is accurate only when the bot is NOT low on clock;
    Phase 169's per-move think deadline (D-16) cuts the search early in time
    trouble, and a deadline-cut bot plays materially weaker than its advertised
    ELO. This is accepted and intended (humans get worse in time trouble). A
    future reader must not "fix" this divergence, and must not read the
    calibration harness's numbers as whole-game strength — the harness plays at
    the full budget with no clock (168.5 D-04b), the shipped bot does not.

    (3) 168.5 decision records. In `168.5-CONTEXT.md`, annotate D-01, D-02 and
    D-04 (and D-04b's synthetic-debit clause) as SUPERSEDED by 169 D-15/D-16,
    with a one-line pointer to `169-CONTEXT.md`'s amendment section. Do NOT
    rewrite the original decision text — append superseded markers, so the
    historical record stays intact and the next verifier does not re-fail the
    phase on its own docs. Note explicitly that D-04b's "the harness has none of
    the pacing theater" clause SURVIVES: the harness still plays at the full
    budget with no clock and no reveal delay.

    (4) `REQUIREMENTS.md` traceability. Update the drifted per-phase rows: PLAY-03,
    PLAY-04, PLAY-05, PLAY-06 and PLAY-07 all still read "Partial (Plan 01/7 ...)"
    / "Partial (Plan 02/7 ...)" though plans 04/05 closed them and this plan
    closes the two BLOCKED ones. Set them to Complete with a brief pointer to the
    closing plan. Leave the PLAY-05 REQUIREMENT TEXT itself alone — it already
    reads "think-time budget derived from its remaining clock (best-effort;
    degrades gracefully under time pressure)", which the D-16 deadline restores
    exactly. Leave the top-of-file checkbox list alone (already `[x]`).

    (5) Run the full frontend gate (see verify). `npx tsc -b` is mandatory and
    separate — lint and test do NOT type-check in this repo.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc -b && npm test -- --run && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint && npx tsc -b && npm test -- --run && npm run knip` all pass (the full frontend gate; knip catches any export left dead by plan 08's deletions).
    - `grep -n "drawCooldownActive" frontend/src/pages/Bots.tsx` shows it wired to the hook's real cooldown state — the hardcoded `false` is gone (`grep -c "drawCooldownActive={false}" frontend/src/pages/Bots.tsx` is 0).
    - In `GameControls.tsx`, the cooldown Tooltip is conditional on the cooldown being active (an enabled Offer-draw button renders no cooldown tooltip).
    - `git diff frontend/src/lib/engine/botBudget.ts` shows ONLY comment-line changes — every exported constant's value is byte-identical (`grep -n "= 50\|= 8\|= 4\|marginThreshold\|epsilonThreshold\|stabilityWindow\|minNodes" frontend/src/lib/engine/botBudget.ts` matches the pre-plan values).
    - The botBudget.ts docstring states the ELO holds only at the full node budget and that a deadline-cut bot plays below its advertised ELO (D-19).
    - `168.5-CONTEXT.md` marks D-01/D-02/D-04 as superseded by 169 D-15/D-16 with the original text preserved, and explicitly preserves D-04b's harness clause.
    - `.planning/REQUIREMENTS.md` rows for PLAY-03..PLAY-07 read Complete; the PLAY-05 requirement text is unchanged (`git diff .planning/REQUIREMENTS.md` shows no edit to line 25).
    - No `text-xs` introduced and no button colors hand-rolled in the touched components (CLAUDE.md frontend rules).
  </acceptance_criteria>
  <done>The draw-cooldown prop means what it says (and its tooltip only appears when the cooldown is real), botBudget.ts carries the D-19 calibration caveat without any constant changing, the 168.5 decision records and REQUIREMENTS traceability no longer assert the reversed invariant, and the full frontend gate (lint + tsc + tests + knip) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser tab visibility -> bot's committed clock | The user controls when the tab is hidden. Under the honest clock the bot can now LOSE on time, so any hidden-time that reaches the bot's committed debit is directly exploitable. |
| Client game loop -> Phase 171 store-on-finish POST | The finished PGN/outcome produced here becomes a persisted `platform='flawchess'` Library game and feeds the user's stats. A wrong outcome is not cosmetic — it is written to the user's permanent record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-169-09-01 | Tampering | hidden-tab debit path (`runBotTurn` anchor read) | high | mitigate | D-20: read the LIVE, pause-shifted turn anchor at resolution time. Without this, "background the tab during the bot's think" is a reliable way to farm timeout wins against a now-flaggable bot, and those wins get persisted by Phase 171. Locked by Task 2 Test E. |
| T-169-09-02 | Tampering | WR-02 future-dated anchor after a mid-hide commit | medium | mitigate | Re-baseline the pause timestamp whenever the turn anchor is reset, so a resume can never shift the anchor into the future and credit the next mover phantom time (negative elapsed). Locked by Task 2 Test F. |
| T-169-09-03 | Repudiation | non-idempotent `finalizeGame` overwriting the true outcome | medium | mitigate | WR-03: first-outcome-wins latch. A late draw-accept overwriting a real checkmate would write a false Result/Termination into a stored game. Locked by Task 2 Test G. |
| T-169-09-04 | Spoofing | client-supplied PGN/outcome trusted by the backend | medium | transfer | Out of scope for this phase — the POST and its server-side validation are Phase 171 / STORE-02's `normalize_flawchess_game`. Noted here so it is not lost: a client-side game loop can never be the authority on its own result. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc -b && npm test -- --run && npm run knip` — the full frontend gate, all green (per CLAUDE.md: lint and test do NOT type-check; `tsc -b` is mandatory and separate).
- `npx vitest run src/hooks/__tests__/useBotGame.test.ts src/lib/__tests__/chessClock.test.ts src/lib/engine/__tests__/deadlineSearch.test.ts` — the three suites that own this closure.
- `uv run pytest tests/test_bot_pgn_clk_roundtrip.py -q` — the PGN `[%clk]` contract is unaffected by the clock-model change (both-color annotations still emitted; STORE-02 still satisfied).
- `git diff --stat frontend/src/lib/engine/mctsSearch.ts frontend/src/lib/engine/selectBotMove.ts` — empty (frozen engine core untouched).
</verification>

<success_criteria>
- Amended SC1 holds: the bot manages its clock via a per-move think deadline, is debited its real turn time, speeds up in time trouble, and CAN lose on time — proven by an automated test in which the bot flags and the user wins.
- Amended SC2 holds: hidden-tab time reaches neither side's COMMITTED clock, and no commit-while-hidden can credit phantom time.
- PLAY-04 and PLAY-05 move from BLOCKED to satisfied; PLAY-06/PLAY-07 are hardened (idempotent finalize; correct draw-cooldown affordance).
- Every document that asserted the reversed "never flags" invariant (`chessClock.ts`, `botBudget.ts`, `useBotGame.ts` docstrings; the 168.5 decision records; REQUIREMENTS traceability) is amended — a re-verification cannot fail the phase on its own docs.
- The frozen Phase 166/168.5 engine core and every calibrated `botBudget.ts` constant are unchanged.
</success_criteria>

<output>
Create `.planning/phases/169-clocked-board-game-loop-usebotgame/169-09-SUMMARY.md` when done.
</output>
