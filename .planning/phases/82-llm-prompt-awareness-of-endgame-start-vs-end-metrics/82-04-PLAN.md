---
phase: 82
plan: 04
type: execute
wave: 3
depends_on: [82-01, 82-02, 82-03]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements:
  - D-14
  - D-23

must_haves:
  truths:
    - "Live /endgames insights run on dev DB narrates BOTH `entry_eval_pawns` and `endgame_score` findings (D-23 setup→execution framing visible)"
    - "Tile colors agree with LLM narration (a colored tile has corresponding LLM narration; a neutral tile is silent or narrated only via [near edge] suffix — D-14)"
    - "Borderline-but-sig case (e.g. value inside ±0.50 with high sig) reads neutral on tile AND is not narrated as 'above null' — the user-28 keystone behavior"
    - "Conv/Parity/Recovery (endgame_metrics) section still narrates correctly — no regression"
    - "score_gap timeline section still narrates correctly under renamed metric Literal values — no regression"
    - "CHANGELOG.md `## [Unreleased]` has new entry/entries for Phase 82 under appropriate `### Added` and `### Changed` subsections"
  artifacts:
    - path: CHANGELOG.md
      provides: "Phase 82 changelog entry (per-phase, per CLAUDE.md 'Per-phase changelog' rule)"
      contains: "endgame_start_vs_end"
  key_links:
    - from: CHANGELOG.md
      to: .planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/
      via: "User-facing summary references the phase contents"
      pattern: "Phase 82|endgame_start_vs_end"
---

<objective>
Final-mile verification + documentation for Phase 82.

This plan has two distinct halves:

1. **Manual UAT (checkpoint:human-verify):** A live insights run against the dev DB to confirm the LLM narrates both new metrics, tile colors agree with narration, and the existing Conv/Parity/Recovery section is not regressed. UAT is non-deterministic (LLM output varies) so it requires human verification — automation cannot assert the LLM phrased the narration correctly.

2. **CHANGELOG entry (auto):** Per CLAUDE.md's "Per-phase changelog" rule, add a bullet under `## [Unreleased]` describing what changed in user-facing terms (the LLM now narrates the Endgame Start vs End tiles; the tile-color rule tightened from sig-only to zone × sig; the entry-eval neutral band tightened from ±0.75 to ±0.50).

Purpose: Closes the loop on the phase. Without UAT, we ship a backend + prompt update that may behave unexpectedly under real LLM output. Without the CHANGELOG entry, the v1.16 milestone close has no record of Phase 82.

Output: Verbal user approval of the UAT scenarios + a committed `## [Unreleased]` CHANGELOG bullet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-CONTEXT.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-VALIDATION.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-01-SUMMARY.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-02-SUMMARY.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-03-SUMMARY.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT — live LLM insights run on dev DB; verify both findings narrated, tile colors agree, no Conv/Parity/Recovery regression</name>
  <files>(no source files modified — UAT runs against the live app on dev DB)</files>
  <action>Pause for the human operator. Run the UAT scenarios in `<how-to-verify>` below; do NOT proceed to Task 2 until the user types `approved` (or accepts a `skip` with explicit acknowledgement). If the user types `issues: ...`, file a follow-up plan or issue and decide jointly whether to ship Phase 82 with the regression noted or block on a fix.</action>
  <verify>Human verbal approval (resume-signal). No automated check is possible — LLM output is non-deterministic and per-user dependent.</verify>
  <done>User has typed `approved` or `issues: <description>` in response to the resume-signal prompt.</done>

  <what-built>
    Plans 01–03 wired Phase 81's `entry_eval_mean_pawns` and endgame Score metrics through the LLM pipeline:
    - Backend emits two new SubsectionFindings under subsection `endgame_start_vs_end` (Plan 01).
    - Prompt has glossary entries + a new subsection block with "setup → execution" framing + Time-Pressure cross-link (Plan 02).
    - Frontend tile-color band tightened to ±0.50 so colored tiles agree with LLM narration (Plan 03).
    - `_PROMPT_VERSION` bumped to `endgame_v23` so v22 caches are invalidated automatically.

    The full backend + frontend test suites are green. This UAT validates the live integration end-to-end against real LLM output, which automated tests cannot cover.
  </what-built>

  <how-to-verify>
    **Pre-flight:**

    1. Ensure dev DB is running:
       ```bash
       docker compose -f docker-compose.dev.yml -p flawchess-dev up -d
       ```
    2. Apply any pending migrations (Phase 82 has none, but the db-startup script runs them anyway):
       ```bash
       uv run alembic upgrade head
       ```
    3. Start backend + frontend:
       ```bash
       bin/run_local.sh
       ```
    4. Open the app in a browser (default `http://localhost:5173` or whatever the frontend dev server prints).
    5. Log in to a dev account that has at least 50 endgame games (any imported user from the dev fixtures works; pick one whose Phase 81 tiles are in production).

    **Scenario A — Both findings narrated (D-23 keystone):**

    1. Navigate to `/endgames`.
    2. Apply default filters (or any preset with ≥10 endgame games — confirm the "Endgame Start vs End" twin-tile section renders both tiles at the top of the page).
    3. Click "Generate insights" (or trigger via the cached endpoint if appropriate). Wait for the LLM response to render.
    4. **Verify:** The LLM narration in the **Endgame Overall Performance** section mentions BOTH:
       - "where you start" / "entry eval" / "Stockfish eval at endgame entry" / similar phrasing referring to the entry-eval metric.
       - "what you do with it" / "endgame score" / overall percentage in endgame games / similar phrasing referring to the endgame_score metric.
       - Bonus: the narration uses the **setup → execution** pairing (e.g. "you tend to enter endgames slightly ahead but...").
    5. **Verify:** The Conv/Parity/Recovery section still narrates correctly (it should appear elsewhere in the report). Compare to a pre-deploy /endgames screenshot if available; if not, just confirm the section is non-empty and reads sensibly.
    6. **Verify:** The score-gap timeline section still narrates (the rename of `endgame_score` → `endgame_score_timeline` should be invisible to the user — the LLM still references it by user-facing phrasing).

    **Scenario B — Tile color agrees with LLM narration (D-14 keystone):**

    1. On the same /endgames view, inspect each tile's color:
       - Green tile (entry_eval ≥ +0.5 OR score ≥ 0.55) AND p < 0.05 → expect LLM to narrate as a strength.
       - Red tile (entry_eval ≤ -0.5 OR score ≤ 0.45) AND p < 0.05 → expect LLM to narrate as a weakness.
       - Neutral (gray) tile → expect LLM to be silent on that metric, OR to narrate via the `[near edge]` suffix as "small but real" (per D-14 + D-23).
    2. **The keystone test:** If any tile shows a borderline-but-sig case (e.g. entry_eval = +0.46, p < 0.001), confirm the tile is NEUTRAL (per D-14). The LLM should NOT narrate this as "above null" or "significantly better than zero" — it should either skip the metric or use `[near edge]`. (If the chosen test user does not exhibit this case, find one that does — the user-28 pattern motivated the entire phase.)
    3. **Verify:** No "tile says X, LLM says Y" mismatches.

    **Scenario C — No regression on Conv/Parity/Recovery:**

    1. Confirm the `endgame_metrics` section (Conv / Parity / Recov) still appears in the rendered report.
    2. Confirm the section narrates strengths/weaknesses by material bucket as before — the rename did not touch this subsection's metrics.
    3. Cross-check the per-bucket WDL table on the Endgames page still renders.

    **Scenario D — Cache invalidation works:**

    1. Reload `/endgames` (or click "Generate insights" twice in quick succession).
    2. Confirm the second response is served from the cache (the LLM is not re-invoked — check backend logs or the API response timing). The new `endgame_v23` prompt version should be the cache key suffix.
    3. (Optional) Inspect `llm_logs` table or the cache layer to confirm the new entries are tagged with `prompt_version = "endgame_v23"`.

    **Scenario E — Cross-section link (Time Pressure):**

    1. Pick a user with strong entry_eval but weak endgame_score (or vice versa) — the user-28 archetype.
    2. **Verify:** The LLM narration mentions the Time Pressure / clock-diff context as a possible cause (per D-23's cross-link prose). Wording like "check the Time Pressure section" or "the clock deficit may explain..." or any reference to `avg_clock_diff_pct` / `[low-time-gap]` is acceptable.

    **Reporting:**

    Tell the user (verbally / in chat) which scenarios passed and which (if any) showed unexpected behavior. Include screenshots if helpful. If a scenario fails, propose whether it is a Plan 02 prompt-tuning fix (most likely), a Plan 01 emitter bug (less likely — covered by automated tests), or a Plan 03 frontend bug (unlikely — covered by automated tests).
  </how-to-verify>

  <resume-signal>
    Type "approved" if all scenarios pass.
    Type "issues: <description>" if any scenario fails — include which scenario and what went wrong (e.g. "Scenario A: LLM only narrated entry_eval, skipped endgame_score" or "Scenario B: borderline tile +0.46/p<0.001 was narrated as significant").
    Type "skip" only with explicit acknowledgement that the phase ships without manual UAT (NOT recommended given LLM non-determinism).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG entry under `## [Unreleased]`</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - CHANGELOG.md (full first 60 lines — current `## [Unreleased]` block has Phase 81 entries under `### Added`, plus `### Changed` from Phase 80.1 + various quick tasks; understand the existing tone, terseness, and grouping)
    - CLAUDE.md "Per-phase changelog" rule (terse, user-facing, what-changed-not-how, grouped by Added/Changed/Fixed/Removed/Security/Tests, reference the phase number)
    - .planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-01-SUMMARY.md, 82-02-SUMMARY.md, 82-03-SUMMARY.md (concrete behavioral deltas to summarize)
  </read_first>

  <action>
    Read the current `## [Unreleased]` block in `CHANGELOG.md`. Add Phase 82 entries.

    Place the new bullets near the bottom of each subsection (chronologically newest is at the bottom of `[Unreleased]` per the existing v1.15 / v1.14 pattern in the file — but verify by reading the existing block). If the section already orders newest-first, place at the top.

    **Add under `### Added` (one bullet, terse):**

    ```markdown
    - **Endgame Insights LLM narrates "Endgame Start vs End" tiles** (Phase 82): the LLM-generated Endgame Overall Performance narration now mentions both Phase 81 tiles — `entry_eval_pawns` ("where you start") and `endgame_score` ("what you do with it") — alongside the existing Conv / Parity / Recovery and score-gap-timeline narration. The pair frames as "setup → execution"; the prompt cross-links to the Time Pressure section so a user with strong entry-eval but weak endgame-score reads as "may be losing on the clock, not the board". Closes the gap left by Phase 81 (which shipped the visible tiles but not the LLM hookup). Internal: a new `endgame_start_vs_end` subsection in the insights pipeline; `_PROMPT_VERSION` bumped to `endgame_v23` (auto-invalidates v22 caches).
    ```

    **Add under `### Changed` (two bullets):**

    ```markdown
    - **Endgame Start vs End: tile color rule + tightened neutral band** (Phase 82): the entry-eval tile's neutral band tightens from ±0.75 to ±0.5 pawns (cohort-IQR was wider, but half-a-pawn average swings at endgame entry are narratable). The tile-color rule changes from "significant → green/red, not significant → neutral" (Phase 81) to "value in green/red zone AND p < 0.05 → colored, otherwise neutral". Result: borderline-but-significant cases (e.g. +0.46 pawns at p<0.001) now read as neutral on the tile AND are not narrated as "above null" by the LLM, so the tile and the prose agree on what counts as a meaningful pattern. The endgame-score tile reuses the existing Openings score-bullet 45–55% band for visual parity.
    - **MetricId rename: `endgame_score` → `endgame_score_timeline`, `non_endgame_score` → `non_endgame_score_timeline`** (Phase 82): the score_timeline subsection's two metrics get a `_timeline` suffix to free the clean `endgame_score` name for the new `endgame_start_vs_end` subsection. User-facing impact: none directly (these names appear only in the internal LLM prompt). Internal: glossary entries renamed in `app/prompts/endgame_insights.md`; finding `metric=` Literal values updated in `_findings_score_timeline`; `_PROMPT_VERSION` bump invalidates v22 caches automatically.
    ```

    Tone-match the existing Phase 81 entries — the same level of detail and the same blend of user-facing summary + internal context. Keep em-dashes minimal per CLAUDE.md style (use commas / colons / parens). Do NOT use the explicit Phase number prefix in the heading — the bullet starts with a bolded user-facing label, then `(Phase 82)` parenthetical near the front.

    Do not add a `Tests` subsection — backend + frontend test churn is internal and not user-facing.

    Verify the file structure remains valid markdown (the `[Unreleased]` block is properly closed, the `### Added` / `### Changed` headings are well-formed).
  </action>

  <verify>
    <automated>grep -F 'endgame_start_vs_end' CHANGELOG.md && grep -F 'Phase 82' CHANGELOG.md && head -3 CHANGELOG.md | grep -F 'Changelog'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -F 'Phase 82' CHANGELOG.md | wc -l` returns >= 1 (the new bullets reference the phase number).
    - `grep -F 'endgame_start_vs_end' CHANGELOG.md | wc -l` returns >= 1.
    - `grep -F 'endgame_v23' CHANGELOG.md | wc -l` returns >= 1.
    - `grep -F '±0.5' CHANGELOG.md | wc -l` returns >= 1 (band tightening mentioned) — OR `grep -F '0.5 pawns' CHANGELOG.md` matches if the unicode plus-minus is dropped.
    - `grep -nE '^## \[Unreleased\]' CHANGELOG.md | head -1` returns line number; the new bullets appear AFTER that line and BEFORE the next `^## \[v` versioned section.
    - The new entries are grouped under `### Added` and `### Changed` subsections (verify by reading the diff).
    - The CHANGELOG file still parses as valid markdown (no orphan `###` headings, no broken bullet lists). Spot-check by reading the new section.
  </acceptance_criteria>

  <done>
    Three new bullets land in `CHANGELOG.md` `## [Unreleased]` (one Added, two Changed) describing Phase 82 in user-facing terms with the required internal context (subsection name, prompt version bump, MetricId rename consequences). File still parses as valid markdown.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev environment → live LLM provider (UAT only) | UAT scenario A invokes the LLM with real user data; same trust boundary as production endgame insights |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-07 | Information disclosure | UAT scenario A sends a real user's metrics to the LLM provider | accept | Same trust boundary as production. The UAT is run on dev DB with imported test users; no production user data crosses the boundary. |
| T-82-08 | Repudiation | Manual UAT could be skipped silently (no automated record) | mitigate | Plan 04 Task 1 is `checkpoint:human-verify gate="blocking"` — execute-plan halts until the user provides an explicit `approved` / `issues: ...` / `skip` signal. |
</threat_model>

<verification>
- All three Plan 04 dependencies (Plans 01, 02, 03) are merged and their tests are green.
- The user has typed `approved` or `issues: <description>` after running the five UAT scenarios.
- The CHANGELOG bullets describe the user-visible behavior and reference Phase 82.
- No regression: Conv/Parity/Recovery section + score-gap timeline section still narrate correctly under the renamed metric Literal values.
</verification>

<success_criteria>
- UAT scenarios A through E all pass (or any failures are triaged + accepted/fixed before phase close).
- CHANGELOG has the new entries under `## [Unreleased]`.
- Phase 82 is ready for milestone close (v1.16 will pull these `[Unreleased]` bullets into a versioned section at milestone time).
</success_criteria>

<output>
After completion, create `.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-04-SUMMARY.md` documenting:
- UAT scenarios run + per-scenario verdict (pass/fail/skipped) + any user observations
- CHANGELOG bullets added (verbatim)
- Any deferred follow-ups discovered during UAT (e.g. prompt-wording tweaks not worth blocking the phase)
- Pointer to the v1.16 milestone close for the eventual `[Unreleased]` → versioned section promotion
</output>
