---
phase: 114.1-move-count-to-ply-count
plan: 03
type: execute
wave: 3
depends_on: ["114.1-01", "114.1-02"]
files_modified:
  - app/schemas/openings.py
  - app/schemas/library.py
  - app/services/openings_service.py
  - app/services/library_service.py
  - app/services/endgame_service.py
  - app/repositories/library_repository.py
  - frontend/src/types/api.ts
  - frontend/src/types/library.ts
  - frontend/src/lib/chess.ts
  - frontend/src/components/results/GameCard.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/hooks/__tests__/useLibraryGame.test.tsx
  - frontend/src/components/results/__tests__/GameCard.test.tsx
  - frontend/src/components/library/__tests__/FlawCard.test.tsx
  - frontend/src/pages/library/__tests__/FlawsTab.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "API schemas expose ply_count: int | None and no longer carry move_count"
    - "All four backend readers (openings/library service, endgame service, library repository) pass ply_count=game.ply_count"
    - "The three game cards render Math.ceil(ply_count/2) — the displayed 'N Moves' label is byte-identical to before"
    - "Frontend types carry ply_count: number | null; the null guard wrapping the render is preserved"
  artifacts:
    - path: "frontend/src/lib/chess.ts"
      provides: "plysToFullMoves(plyCount) shared display helper"
      contains: "Math.ceil"
    - path: "app/schemas/openings.py"
      provides: "GameRecord.ply_count"
      contains: "ply_count"
    - path: "app/schemas/library.py"
      provides: "ply_count on the library game + flaw-list schemas"
      contains: "ply_count"
  key_links:
    - from: "frontend/src/components/results/GameCard.tsx"
      to: "frontend/src/lib/chess.ts plysToFullMoves"
      via: "import + call on game.ply_count"
      pattern: "plysToFullMoves"
    - from: "app/services/library_service.py"
      to: "app/models/game.py games.ply_count"
      via: "ply_count=game.ply_count"
      pattern: "ply_count=game\\.ply_count"
---

<objective>
Switch all readers from `move_count` to `ply_count` (D-01, D-03): backend API schemas + four reader sites, and the frontend types + three game cards that derive the unchanged "N Moves" full-move label as `Math.ceil(ply_count / 2)`. Per D-03, readers use `ply_count` directly with NO permanent fallback — dev is fully backfilled in Plan 02, and the prod runbook orders backfill before this reader deploy, so a NULL-tolerant `COALESCE` would be dead code.

This wave runs after the backfill (Plan 02) so reader verification sees real data. `move_count` is still in the DB (dropped in Plan 04) but is no longer referenced by any reader after this plan.

Purpose: the API now exposes the raw half-move count Phase 115 needs, while the visible UI is unchanged.
Output: backend schemas/readers on `ply_count`, frontend types + cards deriving full-moves, all tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-CONTEXT.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-RESEARCH.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-VALIDATION.md
</context>

<read_first>
- RESEARCH §5 — the complete backend reader inventory with exact lines: `app/schemas/openings.py:114`, `app/schemas/library.py:86,145`, `app/services/openings_service.py:243`, `app/services/library_service.py:379`, `app/services/endgame_service.py:1165`, `app/repositories/library_repository.py:328`. (Confirmed: `endgame_service` constructs the openings `GameRecord` — no separate `endgame.py` schema field; the grep in this plan's Task 1 re-confirms zero stray `move_count` in `app/`.)
- RESEARCH §6 — frontend inventory with exact lines: `types/api.ts:111`, `types/library.ts:65,208`, `GameCard.tsx:157-163`, `FlawCard.tsx:226-232`, `LibraryGameCard.tsx:348-352`, plus the four test files + their fixture values. Note Pitfall 6: `LibraryGameCard` renders `{count} Moves` inline as one string while `GameCard`/`FlawCard` split the count from a `<span className="hidden sm:inline">&nbsp;Moves</span>` — both need the derivation but keep their distinct markup.
- RESEARCH Pitfall 2 — `noUncheckedIndexedAccess`: keep the existing `game.ply_count !== null && (...)` null guard (same place `move_count`'s guard was).
- RESEARCH Pitfall 3 + Open Question 2 — fixture numeric values: rename `move_count: N` → `ply_count: 2*N` so the rendered full-move count stays N (e.g. `move_count: 40` → `ply_count: 80`; the FlawCard null-test fixture stays null).
- MEMORY `[Frontend has no Prettier]` — ESLint only; never run `prettier --write`. `cn()` uses tailwind-merge. MEMORY `[Always apply changes to mobile too]` — the cards' mobile/desktop split markup both need the derivation.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Backend — schemas + four reader sites switch to ply_count</name>
  <files>app/schemas/openings.py, app/schemas/library.py, app/services/openings_service.py, app/services/library_service.py, app/services/endgame_service.py, app/repositories/library_repository.py</files>
  <action>
Rename the API schema fields: `app/schemas/openings.py:114` `move_count: int | None` → `ply_count: int | None`; `app/schemas/library.py:86` and `:145` both `move_count: int | None` → `ply_count: int | None`. Update the four reader construction sites to pass the new attribute: `openings_service.py:243` `move_count=g.move_count` → `ply_count=g.ply_count`; `library_service.py:379` `move_count=game.move_count` → `ply_count=game.ply_count`; `endgame_service.py:1165` `move_count=g.move_count` → `ply_count=g.ply_count`; `library_repository.py:328` `move_count=game.move_count` → `ply_count=game.ply_count`. Per D-03 use `ply_count` directly — NO `COALESCE`/fallback (dev backfilled in Plan 02; prod backfill ordered before this deploy). After editing, grep `app/` (excluding the `openings`/`stats_repository` `ply_count`-for-openings-depth references that RESEARCH §5 flagged as unrelated) to confirm zero remaining `games.move_count` / `g.move_count` / `game.move_count` readers — only the model column (Plan 04 drops it) should still mention `move_count`.
  </action>
  <verify>
    <automated>grep -rn "move_count" app/schemas app/services app/repositories | grep -v '^#' | grep -v ply_count | wc -l | grep -qx 0 && uv run pytest tests/ -k "openings or library or endgame" -x</automated>
  </verify>
  <done>Schemas expose `ply_count`; all four readers pass `ply_count=...`; zero `move_count` readers remain in app/ (model column excepted); targeted backend tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — shared helper + types + three cards derive Math.ceil(ply_count/2)</name>
  <files>frontend/src/lib/chess.ts, frontend/src/types/api.ts, frontend/src/types/library.ts, frontend/src/components/results/GameCard.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/FlawCard.tsx</files>
  <action>
Add (or create) `frontend/src/lib/chess.ts` exporting `plysToFullMoves(plyCount: number): number { return Math.ceil(plyCount / 2); }` (Claude's-Discretion choice per D-01/§6 — a single source of truth for the derivation across three cards; ensure it is imported so Knip does not flag a dead export). Rename the TS types: `types/api.ts:111` `move_count: number | null` → `ply_count: number | null`; `types/library.ts:65` and `:208` likewise. In each card, keep the existing `!== null` null guard but switch the field to `ply_count` and the displayed value to `plysToFullMoves(game.ply_count)` (Pitfall 2: guard preserved so `noUncheckedIndexedAccess`/`null` narrows): `GameCard.tsx:157-163` (`{plysToFullMoves(game.ply_count)}` + keep the `<span className="hidden sm:inline">&nbsp;Moves</span>`); `FlawCard.tsx:226-232` (`flaw.ply_count` + `plysToFullMoves(flaw.ply_count)`); `LibraryGameCard.tsx:348-352` (inline `{plysToFullMoves(game.ply_count)} Moves` — keep its single-string markup per Pitfall 6). The rendered "N Moves" output MUST be byte-identical to before. Do not run prettier; ESLint only.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <done>`plysToFullMoves` helper exists + imported; all three TS type fields renamed; all three cards derive the full-move count via the helper with the null guard intact; `npm run lint` (incl. Knip + tsc) passes.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — update test fixtures (ply_count = 2 × old move_count) + run tests</name>
  <files>frontend/src/hooks/__tests__/useLibraryGame.test.tsx, frontend/src/components/results/__tests__/GameCard.test.tsx, frontend/src/components/library/__tests__/FlawCard.test.tsx, frontend/src/pages/library/__tests__/FlawsTab.test.tsx</files>
  <action>
Rename every `move_count` fixture key → `ply_count` and double the numeric value so the rendered full-move count is unchanged (Pitfall 3): `useLibraryGame.test.tsx:55` `move_count: 40` → `ply_count: 80`; `GameCard.test.tsx:48` `move_count: 40` → `ply_count: 80`; `FlawCard.test.tsx` (L105, L204-205, L249 — including the `null` case which stays `ply_count: null`); `FlawsTab.test.tsx` (L88, L241, L266, L319, L344 fixtures + the inline type at L266/L319 area). Where a test asserts the rendered text (e.g. "40 Moves" / "Moves"), keep the asserted display value unchanged (the doubling preserves it). Grep all four files for `move_count` → zero remaining. Then run the frontend suite.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "move_count" src/ | wc -l | grep -qx 0 && npm test -- --run</automated>
  </verify>
  <done>Zero `move_count` in `frontend/src`; fixtures use `ply_count` with doubled values; rendered-text assertions unchanged; `npm test -- --run` green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `app/schemas/openings.py` `GameRecord.ply_count` + `app/schemas/library.py` two `ply_count` fields (API now exposes the raw half-move count; `move_count` removed from responses).
- `frontend/src/lib/chess.ts::plysToFullMoves(plyCount: number): number` — shared display-derivation helper.
- `frontend/src/types/api.ts` `GameRecord.ply_count` + `frontend/src/types/library.ts` two `ply_count` fields.
- Three cards (`GameCard`, `LibraryGameCard`, `FlawCard`) now render `plysToFullMoves(ply_count)`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → client | `ply_count` (int) replaces `move_count` in JSON responses; same shape, no new field type, no PII |
| client render | derived display value, no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114.1-03-01 | Information Disclosure | API response field swap | accept | `ply_count` is a non-sensitive game-length integer (already implied by the prior `move_count`); no internal hash or new data is exposed (CLAUDE.md "responses never expose internal hashes" unaffected). |
| T-114.1-03-02 | Tampering | reader direct-read with no fallback (D-03) | accept | NULL-tolerance removed by design; the deploy ordering (backfill-before-readers) is the control. A NULL `ply_count` on a not-yet-backfilled prod row would render nothing (null guard) — fail-safe, not a crash. |
| T-114.1-03-SC | Tampering | npm installs | mitigate | No new dependencies (helper is local code); nothing to verify. |

No new external input, endpoint, or auth surface. No `high`-severity threat.
</threat_model>

<verification>
- `grep -rn "move_count" app/schemas app/services app/repositories frontend/src` returns nothing (model column in `app/models/game.py` is the only remaining mention, dropped in Plan 04).
- `uv run pytest -n auto` green; `uv run ty check app/ tests/` zero errors; `uv run ruff check`/`format --check` clean.
- `cd frontend && npm run lint && npm test -- --run` green.
- Manual spot-check (optional): a Library/Openings card still shows the same "N Moves" as before the change.
</verification>

<success_criteria>
- API exposes `ply_count`, drops `move_count`; all four backend readers switched.
- Frontend types + three cards derive `Math.ceil(ply_count/2)`; displayed label unchanged.
- No reader references `games.move_count`; full backend + frontend suites green.
</success_criteria>

<output>
Create `.planning/phases/114.1-move-count-to-ply-count/114.1-03-SUMMARY.md` when done. Note the only remaining `move_count` reference is the model column, scheduled for drop in Plan 04 Migration B.
</output>
