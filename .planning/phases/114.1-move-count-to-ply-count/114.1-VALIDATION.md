---
phase: 114.1
slug: move-count-to-ply-count
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-10
---

# Phase 114.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio (backend); vitest (frontend) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`); `frontend/vitest.config.ts` |
| **Quick run command** | `uv run pytest tests/test_zobrist.py tests/test_import_service.py -x` |
| **Full suite command** | `uv run pytest -n auto` |
| **Estimated runtime** | quick ~10s; full backend ~mins; frontend `npm test -- --run` ~secs |

---

## Sampling Rate

- **After every task commit:** Run the quick command for the touched layer (backend: `uv run pytest <touched test file> -x`; frontend: `npm test -- --run`).
- **After every plan wave:** Run `uv run pytest -n auto` (+ `npm run lint && npm test -- --run` for the frontend wave).
- **Before `/gsd-verify-work`:** Full backend suite + frontend lint/tests green; `uv run ty check app/ tests/` zero errors.
- **Max feedback latency:** ~15 seconds (quick path).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| migration-add | — | 1 | ply_count column added nullable | — / — | N/A | integration | `uv run alembic upgrade head && uv run alembic downgrade -1` | ✅ | ⬜ pending |
| import-write | — | 2 | `process_game_pgn` returns `ply_count = len(nodes)` | — | N/A | unit | `uv run pytest tests/test_zobrist.py::test_process_game_pgn_ply_count -x` | ❌ W0 (rename) | ⬜ pending |
| import-bulk | — | 2 | Stage 5 bulk UPDATE writes `ply_count` | — | N/A | unit+integration | `uv run pytest tests/test_import_service.py -k Stage5 -x` | ❌ W0 (fixture) | ⬜ pending |
| backfill-script | — | 2 | `ply_count = max(ply)` per game, idempotent | — | N/A | integration | `uv run pytest tests/test_backfill_ply_count.py -x` (if added) / manual dev run + NULL-count query | ❌ W0 | ⬜ pending |
| readers-backend | — | 3 | API exposes `ply_count`, drops `move_count` | — | N/A | unit | `uv run pytest tests/ -k "openings or library or endgame" -x` | ✅ | ⬜ pending |
| readers-frontend | — | 3 | Cards render `Math.ceil(ply_count/2)`; types updated | — | N/A | TS + unit | `cd frontend && npm run lint && npm test -- --run` | ✅ | ⬜ pending |
| migration-enforce | — | 4 | `move_count` dropped, `ply_count NOT NULL` (dev) | — | N/A | integration | `uv run alembic upgrade head` (dev, post-backfill) | ✅ | ⬜ pending |
| chapter5 | — | 4 | `user_moves_per_game` reads `games.ply_count` | — | N/A | diff-gate + prose | chapter5 diff-gate + §5 narrated review | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_zobrist.py` — rename `test_process_game_pgn_move_count` → `test_process_game_pgn_ply_count`; assert `ply_count == len(nodes)` (e.g. value `3` not `2` for the 3-ply fixture).
- [ ] `tests/test_import_service.py` — `TestFlushBatchStage5*` fixtures seed `Game` rows with `ply_count` (was `move_count`); assert the bulk UPDATE writes `ply_count`.
- [ ] (Optional) `tests/test_backfill_ply_count.py` — integration test for the backfill UPDATE against the per-run test DB, if the planner judges it worth a dedicated test over manual dev verification.

*Framework already installed — no install task needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Prod backfill populates `ply_count` for all existing rows | D-02 | Requires `bin/prod_db_tunnel.sh` + 190M-row prod data; cannot run in CI/dev DB | Open tunnel, run `scripts/backfill_ply_count.py --db prod`, then verify `SELECT count(*) FROM games WHERE ply_count IS NULL` = 0 BEFORE deploying the enforce migration |
| Prod enforce migration (drop `move_count`) deploy ordering | D-02 | Auto-migration on prod must not run before backfill completes | HUMAN-gated: confirm NULL-count = 0, then deploy the enforce migration |
| §5 benchmark narrated prose + headline table after basis change | D-05 | Diff-gate validates `compute()`, not narrated prose | Refresh §5 via `chapter5.compute()` fast path (~1-2 min), review §5 metric sections + headline table |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (prod backfill/enforce are HUMAN-UAT by nature)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (zobrist + import_service fixture renames)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
