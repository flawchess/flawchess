---
phase: 114.1
slug: move-count-to-ply-count
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-10
revised: 2026-06-10
---

# Phase 114.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> **Revised 2026-06-10** to match CONTEXT.md D-02 (single migration: add nullable + backfill +
> drop `move_count`; NO standalone backfill script, NO enforce migration, NO prod runbook).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio (backend); vitest (frontend) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`); `frontend/vitest.config.ts` |
| **Quick run command** | `uv run pytest tests/test_zobrist.py tests/test_import_service.py -x` |
| **Full suite command** | `uv run pytest -n auto` |
| **Estimated runtime** | quick ~10s; full backend ~mins; frontend `npm test -- --run` ~secs |

---

## Sampling Rate

- **After every task commit:** Run the quick command for the touched layer (backend: `uv run pytest <touched test file> -x`; frontend: `npm test -- --run`).
- **After every plan wave:** Run `uv run pytest -n auto` (+ `npm run lint && npm test -- --run` for the frontend wave).
- **Before `/gsd-verify-work`:** Full backend suite + frontend lint/tests green; `uv run ty check app/ tests/` zero errors.
- **Max feedback latency:** ~15 seconds (quick path).

---

## Per-Task Verification Map

| Task | Plan | Wave | Behavior | Test Type | Automated Command | Status |
|------|------|------|----------|-----------|-------------------|--------|
| migration | 01 | 1 | ONE migration adds nullable `ply_count`, backfills `max(ply)`, drops `move_count`; round-trips | integration | `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run ty check app/ tests/` | ⬜ pending |
| import-write | 01 | 1 | `process_game_pgn` returns `ply_count = len(nodes)`; Stage 5 bulk UPDATE writes `ply_count` | unit | `uv run pytest tests/test_zobrist.py::test_process_game_pgn_ply_count tests/test_import_service.py -k "ply_count or Stage5" -x` | ⬜ pending |
| backend-readers | 01 | 1 | Schemas + 4 services expose `ply_count`, no `move_count` left in `app/` | unit | `grep -rn "move_count" app/ \| wc -l \| grep -qx 0 && uv run pytest -n auto -x` | ⬜ pending |
| frontend-readers | 02 | 2 | 3 cards render `Math.ceil(ply_count/2)` via `plysToFullMoves`; types updated; no `move_count` in `src/` | TS + unit | `cd frontend && grep -rn "move_count\|moveCount" src/ \| wc -l \| grep -qx 0 && npm run lint && npm test -- --run` | ⬜ pending |
| chapter5 | 02 | 2 | `user_moves_per_game` reads `games.ply_count`, zero `game_positions` scan | grep + prose | `grep -qn "ply_count" scripts/benchmarks/chapter5.py && grep -c "move_count" scripts/benchmarks/chapter5.py \| grep -qx 0` + §5 narrated review | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_zobrist.py` — rename `test_process_game_pgn_move_count` → `test_process_game_pgn_ply_count`; assert `ply_count == len(nodes)` (e.g. `3` not `2` for the 3-ply fixture).
- [ ] `tests/test_import_service.py` — `TestFlushBatchStage5*` fixtures seed `Game` rows with `ply_count` (was `move_count`); assert the bulk UPDATE writes `ply_count`.

*Framework already installed — no install task needed. No backfill-script test (backfill is in-migration).*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| §5 benchmark narrated prose + headline table after the `ply_count` basis change | Diff-gate validates `compute()`, not narrated prose (MEMORY `[Diff-gate ≠ report narration]`) | Refresh §5 via `chapter5.compute()` fast path (~1-2 min, NOT the ~33-min full `gen_benchmarks`); review §5 metric sections + headline table in `reports/benchmarks-latest.md` |

*No prod backfill / enforce-migration steps — the single migration runs via `alembic upgrade head` on dev and prod identically (auto-runs at prod container startup, measured ~10s).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (zobrist + import_service fixture renames)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
