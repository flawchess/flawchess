---
phase: 114.1-move-count-to-ply-count
plan: 04
type: execute
wave: 4
depends_on: ["114.1-01", "114.1-02", "114.1-03"]
files_modified:
  - app/models/game.py
  - alembic/versions/20260610_HHMMSS_<revB>_enforce_ply_count_drop_move_count.py
  - scripts/benchmarks/chapter5.py
autonomous: false
requirements: []
must_haves:
  truths:
    - "Migration B sets games.ply_count NOT NULL and drops games.move_count, chaining from Migration A (<revA>)"
    - "Migration B applies cleanly on dev (already backfilled in Plan 02); prod apply is HUMAN-gated on ply_count IS NULL = 0"
    - "The games.move_count column and model field are fully removed"
    - "chapter5.py user_moves_per_game reads games.ply_count (FLOOR/CEIL by color) with zero game_positions scan"
  artifacts:
    - path: "alembic/versions/20260610_HHMMSS_<revB>_enforce_ply_count_drop_move_count.py"
      provides: "Migration B — NOT NULL enforce + drop move_count; down_revision=<revA>"
      contains: 'op.drop_column("games", "move_count")'
    - path: "scripts/benchmarks/chapter5.py"
      provides: "Simplified user_moves_per_game CTE reading games.ply_count"
      contains: "ply_count"
  key_links:
    - from: "scripts/benchmarks/chapter5.py"
      to: "games.ply_count"
      via: "FLOOR/CEIL(g.ply_count/2.0) by user_color, JOIN games"
      pattern: "ply_count"
    - from: "alembic Migration B"
      to: "alembic Migration A (<revA>)"
      via: "down_revision chains the enforce step after the add"
      pattern: "down_revision"
---

<objective>
Finalize the migration (D-02 enforce step) and ship the benchmark §5 follow-on (D-05). Migration B sets `games.ply_count NOT NULL` and drops `games.move_count`; it applies on dev (already backfilled in Plan 02) and is HUMAN-gated on prod (`SELECT count(*) FROM games WHERE ply_count IS NULL = 0` must hold before the deploy carrying it). Then simplify `scripts/benchmarks/chapter5.py`'s `user_moves_per_game` to read `games.ply_count` directly (dropping its ~87M-row `game_positions` scan), and regenerate §5 via the fast path (`chapter5.compute()`, ~1-2 min — NOT the 33-min full `gen_benchmarks`).

This plan is `autonomous: false`: the prod enforce-migration deploy is a HUMAN-gated rollout step, and the §5 regen requires narrated-prose review (a green diff-gate alone is insufficient — MEMORY `[Diff-gate ≠ report narration]`).

Purpose: lock in the exact-half-move denominator and pay off the benchmark speedup.
Output: Migration B, `move_count` fully removed, §5 reading `games.ply_count`, regenerated + reviewed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-CONTEXT.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-RESEARCH.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-VALIDATION.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-01-SUMMARY.md
</context>

<read_first>
- `114.1-01-SUMMARY.md` — for the `<revA>` revision id (Migration A) that Migration B chains from via `down_revision`.
- `114.1-02-SUMMARY.md` — confirms dev `ply_count IS NULL = 0` (precondition for applying Migration B on dev).
- RESEARCH §2 — Migration B pattern: `op.alter_column("games", "ply_count", nullable=False)` then `op.drop_column("games", "move_count")`; `downgrade()` re-adds `move_count` nullable + `ply_count` back to nullable. Analog files: `20260607_alter_game_flaws_impact_cols.py` (NOT NULL pattern), `20260609_drop_game_flaws_display_cols.py` (drop pattern). Literal `sa.*` only — no `app.*` imports.
- RESEARCH §2 "Auto-run on prod" + §9 deploy ordering + Pitfall 1 — `SET NOT NULL` after full backfill is a fast scan (no rewrite); the HUMAN gate is operational (verify NULL-count=0 on prod before that deploy), not a code gate.
- RESEARCH §8 — the current `user_moves_per_game` CTE (`chapter5.py` L240-252) and the simplified replacement (`CASE WHEN bg.user_color='white' THEN FLOOR(g.ply_count/2.0) ELSE CEIL(g.ply_count/2.0) END`, JOIN `games g ON g.id = bg.game_id`); fast-path refresh via `chapter5.compute()`.
- `.claude/skills/benchmarks` SKILL.md — the §5 fast-path (`chapter5.compute()`) and the diff-gate; MEMORY `[Benchmark §5 refresh fast path]` (~1-2 min, not the 33-min full gen) and `[Diff-gate ≠ report narration]` (review narrated prose + headline table, not just the gate).
- MEMORY `[No dev DB reset in plans]` — do NOT gate on `bin/reset_db.sh`; Migration B applies to the existing (Plan 02-backfilled) dev DB.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Migration B — enforce ply_count NOT NULL + drop move_count + remove model field</name>
  <files>app/models/game.py, alembic/versions/20260610_HHMMSS_<revB>_enforce_ply_count_drop_move_count.py</files>
  <action>
Author Migration B by hand (`uv run alembic revision -m "enforce ply_count not null, drop move_count"`, NOT `--autogenerate`). Set `down_revision = "<revA>"` (the Migration A id from `114.1-01-SUMMARY.md`) and a unique 12-hex revision id `<revB>`; rename the file to `YYYYMMDD_HHMMSS_<revB>_enforce_ply_count_drop_move_count.py`. `upgrade()`: `op.alter_column("games", "ply_count", nullable=False)` then `op.drop_column("games", "move_count")`, with a docstring stating the PROD GATE ("must not deploy until backfill_ply_count.py --db prod completes and SELECT count(*) FROM games WHERE ply_count IS NULL = 0"). `downgrade()`: `op.add_column("games", sa.Column("move_count", sa.Integer(), nullable=True))` then `op.alter_column("games", "ply_count", nullable=True)`. Literal `sa.*` only — no `app.*` imports. Then in `app/models/game.py` remove the `move_count` column/field entirely and update `ply_count`'s mapping to `Mapped[int]` (non-nullable) `mapped_column(nullable=False)` to match the enforced schema. Apply on dev: `uv run alembic upgrade head` (dev is backfilled, so `SET NOT NULL` succeeds). Confirm `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips, then `uv run ty check app/ tests/` (the model `ply_count: Mapped[int]` change must not break any reader — they were switched in Plan 03).
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run ty check app/ tests/ && uv run pytest -n auto -x</automated>
  </verify>
  <done>Migration B chains from `<revA>`, applies on dev (NOT NULL + drop), round-trips; `move_count` removed from model + DB; `ply_count` is `Mapped[int]` non-nullable; `ty` clean; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify chapter5.py user_moves_per_game to read games.ply_count</name>
  <files>scripts/benchmarks/chapter5.py</files>
  <action>
Replace the `user_moves_per_game` CTE (L240-252) that scans `game_positions` with the direct `games.ply_count` read from RESEARCH §8: `user_moves_per_game AS (SELECT bg.game_id, CASE WHEN bg.user_color = 'white' THEN FLOOR(g.ply_count / 2.0) ELSE CEIL(g.ply_count / 2.0) END AS user_moves FROM base_games bg JOIN games g ON g.id = bg.game_id)`. Update the leading comment to note it reads `games.ply_count` (exact half-moves, Phase 114.1) with no `game_positions` scan, and the `floor=white / ceil=black` convention. Confirm no other part of `chapter5.py` still references the dropped `move_count` or relies on the removed `game_positions` join columns from the old CTE. Keep the result column name `user_moves` so downstream aggregation/diff-gate is unaffected.
  </action>
  <verify>
    <automated>grep -n "ply_count" scripts/benchmarks/chapter5.py && grep -c "move_count" scripts/benchmarks/chapter5.py | grep -qx 0 && echo "chapter5 reads ply_count, no move_count"</automated>
  </verify>
  <done>`user_moves_per_game` reads `games.ply_count` via FLOOR/CEIL by color; no `game_positions` scan in that CTE; no `move_count` reference remains in chapter5.py; `user_moves` column name preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regenerate §5 (fast path) + review narrated prose, and confirm prod rollout gate</name>
  <what-built>Migration B (NOT NULL + drop move_count) and the chapter5 `user_moves_per_game` simplification are in place. The §5 benchmark must be regenerated against the new basis and human-reviewed; the prod enforce-migration deploy is a HUMAN-gated rollout.</what-built>
  <how-to-verify>
1. Regenerate §5 via the fast path: run `chapter5.compute()` (per the `benchmarks` skill, ~1-2 min) — do NOT run the full ~33-min `gen_benchmarks`. If the benchmark DB is needed, `bin/benchmark_db.sh start` first.
2. Review `reports/benchmarks-latest.md` §5: confirm the narrated prose AND the headline table read sensibly after the basis change (MEMORY `[Diff-gate ≠ report narration]` — a green diff-gate alone is NOT sufficient; verify all §5 metric sections + the headline table). Sanity-check user_moves magnitudes vs the prior `game_positions`-based numbers (expect equality, since FLOOR/CEIL(ply_count/2) reproduces the old per-color count).
3. PROD ROLLOUT GATE (operational, do NOT auto-run): the deploy carrying Migration B must ship ONLY AFTER prod is fully backfilled — confirm `SELECT count(*) FROM games WHERE ply_count IS NULL` = 0 on prod (via `bin/prod_db_tunnel.sh`, see Plan 02 `<prod_runbook>`). Deploy order: Migration A + readers already on prod, prod backfill complete, THEN Migration B deploy. Confirm you understand this ordering before approving.
  </how-to-verify>
  <resume-signal>Type "approved" once §5 prose+table reviewed and the prod deploy-ordering gate is acknowledged, or describe issues.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- Migration B revision id `<revB>` (down_revision `<revA>`) — enforces `ply_count NOT NULL`, drops `games.move_count`.
- `games.ply_count` finalized as `Mapped[int]` (non-nullable); `games.move_count` removed entirely.
- `scripts/benchmarks/chapter5.py` `user_moves_per_game` — now reads `games.ply_count` (FLOOR/CEIL by color), zero `game_positions` scan.
- Regenerated §5 in `reports/benchmarks-latest.md` (fast-path).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic auto-run → prod schema | Migration B (NOT NULL + drop column) runs on container start; deploy ordering is the control |
| benchmark script → benchmark DB (read-only) | chapter5 reads `games.ply_count`; offline, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114.1-04-01 | Denial of Service | Migration B `SET NOT NULL` on prod | mitigate | HUMAN gate verifies `ply_count IS NULL = 0` before the Migration B deploy; post-backfill `SET NOT NULL` is a fast constraint scan, not a table rewrite (Pitfall 1). |
| T-114.1-04-02 | Tampering | `drop_column move_count` irreversibility on prod | accept | `downgrade()` re-adds `move_count` nullable (data not restored, but no reader needs it — all switched in Plan 03); deploy is gated + reversible at the schema level. |
| T-114.1-04-03 | Information Disclosure | regenerated §5 report | accept | Aggregate benchmark stats only; no per-user PII; same data shape as before the basis change. |
| T-114.1-04-SC | Tampering | npm/pip installs | mitigate | No new packages introduced. |

No new external input, endpoint, or auth surface. No `high`-severity threat; the only sensitive action (prod enforce migration) is gated by Task 3's blocking human checkpoint.
</threat_model>

<verification>
- `uv run alembic upgrade head` (dev) applies Migration B; `downgrade -1`/`upgrade head` round-trips; `uv run pytest -n auto` green; `uv run ty check app/ tests/` zero errors; `ruff` clean.
- `grep -rn "move_count" app/ scripts/ frontend/src/` returns nothing (column fully removed).
- §5 regenerated via `chapter5.compute()`; narrated prose + headline table human-reviewed (Task 3).
- Prod deploy-ordering gate acknowledged (Task 3).
</verification>

<success_criteria>
- `games.move_count` fully removed; `games.ply_count` NOT NULL; Migration B round-trips on dev.
- `chapter5.py` reads `games.ply_count` with no `game_positions` scan; §5 regenerated + reviewed.
- Prod enforce-migration rollout gate documented and acknowledged.
</success_criteria>

<output>
Create `.planning/phases/114.1-move-count-to-ply-count/114.1-04-SUMMARY.md` when done. Record `<revB>`, the §5 review outcome, and a note that the prod enforce-migration deploy remains HUMAN-gated on prod `ply_count IS NULL = 0`.
</output>
