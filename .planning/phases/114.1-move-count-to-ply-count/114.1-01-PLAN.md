---
phase: 114.1-move-count-to-ply-count
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/game.py
  - app/services/zobrist.py
  - app/services/import_service.py
  - app/schemas/openings.py
  - app/schemas/library.py
  - app/services/openings_service.py
  - app/services/library_service.py
  - app/services/endgame_service.py
  - app/repositories/library_repository.py
  - tests/test_zobrist.py
  - tests/test_import_service.py
  - tests/services/test_import_service.py
  - alembic/versions/20260610_HHMMSS_<rev>_replace_move_count_with_ply_count.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ONE Alembic migration (down_revision d9e3f1a4b5c6) in a single transaction adds nullable ply_count, backfills it from max(ply) per game, and drops move_count; downgrade() reverses it"
    - "ply_count is NULLABLE (Mapped[int | None]) — no NOT NULL constraint; the 971 position-less games stay NULL exactly as move_count was (D-02a)"
    - "process_game_pgn returns ply_count = len(nodes) (exact half-move count), not (len(nodes)+1)//2"
    - "import_service Stage 5 bulk UPDATE writes games.ply_count for every game in the batch"
    - "All backend readers + API schemas expose ply_count (int | None) directly and no longer reference move_count (D-03 — no fallback, no COALESCE)"
    - "The full backend suite is green at the end of this wave (everything bound to the model attribute changes together)"
  artifacts:
    - path: "alembic/versions/20260610_HHMMSS_<rev>_replace_move_count_with_ply_count.py"
      provides: "Single migration — add nullable ply_count + backfill + drop move_count; down_revision=d9e3f1a4b5c6"
      contains: 'op.drop_column("games", "move_count")'
    - path: "app/models/game.py"
      provides: "games.ply_count Mapped[int | None] column; move_count removed"
      contains: "ply_count"
    - path: "app/services/zobrist.py"
      provides: "ply_count = len(nodes) computation + GameProcessingResult key"
      contains: "ply_count"
    - path: "app/schemas/openings.py"
      provides: "GameRecord.ply_count"
      contains: "ply_count"
    - path: "app/schemas/library.py"
      provides: "ply_count on the library game + flaw-list schemas"
      contains: "ply_count"
  key_links:
    - from: "app/services/import_service.py"
      to: "app/models/game.py games.ply_count"
      via: "Stage 5 bulk UPDATE .values(ply_count=bindparam)"
      pattern: "ply_count=bindparam"
    - from: "app/services/import_service.py"
      to: "app/services/zobrist.py GameProcessingResult"
      via: 'processing_result["ply_count"] accumulation'
      pattern: 'processing_result\["ply_count"\]'
    - from: "app/services/library_service.py"
      to: "app/models/game.py games.ply_count"
      via: "ply_count=game.ply_count"
      pattern: "ply_count=game\\.ply_count"
---

<objective>
Replace `games.move_count` (full-move count) with the exact half-move `games.ply_count` across the entire backend in a single coupled wave: one Alembic migration (add nullable + backfill + drop `move_count`, with a reversing `downgrade()`), the model edit, the import-path write (`zobrist.py` + `import_service.py` Stage 5), every backend reader + API schema, and the backend test fixtures. Because everything bound to the model attribute changes together, the backend suite is green at the end of this wave.

Per the REVISED D-02 (measured on prod 2026-06-10): the backfill aggregate `SELECT game_id, max(ply) FROM game_positions GROUP BY game_id` runs in 3.2s at 33MB peak and the write touches only ~590k `games` rows — sub-10s total. Alembic runs at container startup before uvicorn serves, so a single in-transaction migration is safe with no NULL window. There is NO standalone backfill script, NO second "enforce" migration, NO prod runbook, NO HUMAN-gating. Per D-02a `ply_count` stays NULLABLE (mirrors `move_count`; the 971 position-less games keep `ply_count = NULL`). Per D-03 readers use `ply_count` directly with no fallback — reader code and migration ship in the same release.

Purpose: API exposes the exact half-move count Phase 115 needs; the schema and import path are fully cut over to `ply_count` in one release.
Output: `games.ply_count` (nullable) replacing `move_count`, import path writing it, all backend readers switched, full backend suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-CONTEXT.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-RESEARCH.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-VALIDATION.md
</context>

<read_first>
- `app/models/game.py` (L120) — current `move_count` column; replace it with `ply_count: Mapped[int | None] = mapped_column(nullable=True)` (D-02a: stays nullable, mirroring move_count's current `int | None` shape).
- `app/services/zobrist.py` (L67 TypedDict key, L145 + L154 docstrings, L271 computation, L276 return) — the exact rename + computation-change sites (`(len(nodes)+1)//2` → `len(nodes)`).
- `app/services/import_service.py` (L92-106 `_PositionRowsResult`, L695 + L738 + L756-770 Stage 5, L859-860 accumulation) — the `move_counts`→`ply_counts` field rename + bulk-UPDATE column change.
- RESEARCH §5 — the COMPLETE backend reader inventory with exact lines: `app/schemas/openings.py:114`, `app/schemas/library.py:86,145`, `app/services/openings_service.py:243`, `app/services/library_service.py:379`, `app/services/endgame_service.py:1165`, `app/repositories/library_repository.py:328`. (`endgame_service` constructs the openings `GameRecord` — no separate `endgame.py` schema field.) RESEARCH §5 also flags `opening_insights_service.py` / `stats_repository.py` `ply_count` references as the `openings` table's depth column — leave those untouched.
- `alembic/versions/20260610_185500_d9e3f1a4b5c6_games_toast_tuple_target_seed_041.py` — current head + the file naming convention `YYYYMMDD_HHMMSS_<revid>_<slug>.py`.
- RESEARCH §2 analogs: `20260521_*_add_evals_completed_at*.py` (`op.add_column` nullable + inline `op.execute` SQL backfill in the same transaction — the exact pattern for this migration); `20260609_drop_game_flaws_display_cols.py` (`op.drop_column`). Literal `sa.*` only, no `app.*` imports (version-pinned snapshot rule).
- RESEARCH §4 (import path exact lines), §7 (backend tests), Pitfall 4 (`move_counts` plural propagation), Pitfall 5 (`GameProcessingResult` TypedDict key must rename with all callers).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — rename backend test fixtures to assert ply_count (half-moves)</name>
  <files>tests/test_zobrist.py, tests/test_import_service.py, tests/services/test_import_service.py</files>
  <action>
Per VALIDATION.md Wave 0 (these renames make the failing assertions exist BEFORE the implementation). In `tests/test_zobrist.py` rename `test_process_game_pgn_move_count` → `test_process_game_pgn_ply_count`, change the assertion target to `result["ply_count"]` and the expected value from `2` to `3` (the `"1. e4 e5 2. Nf3 *"` fixture has 3 half-moves = `len(nodes)`); update the docstring to "3 half-moves → ply_count=3". Leave `test_process_game_pgn_returns_correct_ply_count` (which asserts `len(result["plies"])`) unchanged — confirm it still passes. In `tests/test_import_service.py`: rename the `_make_mock_processing_result(... move_count=...)` param and dict key to `ply_count`, rename `test_move_count_populated` → `test_ply_count_populated` (+ docstring + local vars), and in `TestFlushBatchStage5` / `TestFlushBatchStage5RealDb` rename every `move_count` reference to `ply_count` (class docstrings, mock dict keys, `Game(...)` constructor kwargs seeded NULL, and the post-UPDATE `assert g.ply_count == ...`). In `tests/services/test_import_service.py` change the two `"move_count"` dict literals to `"ply_count"` with half-move values (`"move_count": 2` → `"ply_count": 4`; `"move_count": 1` → `"ply_count": 1`). Grep both `move_count` AND `move_counts` (plural) across all three files to confirm zero remaining references. These tests FAIL until Tasks 2-4 land (RED).
  </action>
  <verify>
    <automated>grep -rn "move_count" tests/test_zobrist.py tests/test_import_service.py tests/services/test_import_service.py | grep -v '^#' | wc -l | grep -qx 0 && echo "no move_count refs remain"</automated>
  </verify>
  <done>Zero `move_count`/`move_counts` references in the three test files; renamed tests reference `ply_count` with half-move expected values. Tests are RED (implementation not yet done).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute exact ply_count in the import path (zobrist + import_service)</name>
  <files>app/services/zobrist.py, app/services/import_service.py</files>
  <behavior>
    - `process_game_pgn("1. e4 e5 2. Nf3 *")` returns a dict whose `ply_count` key equals `len(nodes)` (= 3), NOT `(len(nodes)+1)//2` (= 2).
    - `flush_batch` Stage 5 issues a bulk UPDATE that sets `games.ply_count` for every game in the batch from the accumulated per-game half-move count.
  </behavior>
  <action>
In `app/services/zobrist.py`: rename the `GameProcessingResult` TypedDict key `move_count: int` → `ply_count: int` (L67); change L271 `move_count = (len(nodes) + 1) // 2` → `ply_count = len(nodes)`; change the return at L276 `move_count=move_count` → `ply_count=ply_count`; update the two docstrings (L145, L154) to say `ply_count`. In `app/services/import_service.py`: rename the `_PositionRowsResult.move_counts` dataclass field → `ply_counts` (L102) and its docstring (L96, L695); in Stage 5 (L756-770) rename the guard `if rows_result.move_counts:` → `ply_counts`, the `update(...).values(move_count=bindparam("b_mc"))` → `.values(ply_count=bindparam("b_pc"))`, and the params builder to use `b_pc`/`ply_counts.items()`; rename the local statement/params vars (`move_count_stmt` → `ply_count_stmt`, etc.); at L859-860 change the accumulation to `out.ply_counts[game_id] = processing_result["ply_count"]`. Keep the existing `# ty: ignore[invalid-argument-type]` on the `update()` call if present. Grep `import_service.py` and `zobrist.py` for both `move_count` and `move_counts` to confirm full propagation (Pitfalls 4 & 5). Do NOT touch `app/models/game.py` here (Task 4 owns the column swap to keep model + migration atomic).
  </action>
  <verify>
    <automated>uv run pytest tests/test_zobrist.py::test_process_game_pgn_ply_count -x && grep -rn "move_count" app/services/zobrist.py app/services/import_service.py | grep -v '^#' | wc -l | grep -qx 0</automated>
  </verify>
  <done>`process_game_pgn` returns `ply_count = len(nodes)`; Stage 5 UPDATE writes `ply_count`; the renamed zobrist test passes; zero stray `move_count`/`move_counts` in either file.</done>
</task>

<task type="auto">
  <name>Task 3: Switch all backend readers + API schemas to ply_count</name>
  <files>app/schemas/openings.py, app/schemas/library.py, app/services/openings_service.py, app/services/library_service.py, app/services/endgame_service.py, app/repositories/library_repository.py</files>
  <action>
Rename the API schema fields: `app/schemas/openings.py:114` `move_count: int | None` → `ply_count: int | None`; `app/schemas/library.py:86` and `:145` both `move_count: int | None` → `ply_count: int | None`. Update the four reader construction sites to pass the new attribute: `openings_service.py:243` `move_count=g.move_count` → `ply_count=g.ply_count`; `library_service.py:379` `move_count=game.move_count` → `ply_count=game.ply_count`; `endgame_service.py:1165` `move_count=g.move_count` → `ply_count=g.ply_count`; `library_repository.py:328` `move_count=game.move_count` → `ply_count=game.ply_count`. Per D-03 use `ply_count` directly — NO `COALESCE`/fallback (the migration backfills before the new code serves; both ship in the same release). Leave the unrelated `openings`-table `ply_count` references in `opening_insights_service.py` / `stats_repository.py` (RESEARCH §5) untouched. After editing, grep `app/schemas app/services app/repositories` to confirm zero remaining `games.move_count` readers (only the model column survives until Task 4 drops it).
  </action>
  <verify>
    <automated>grep -rn "move_count" app/schemas app/services app/repositories | grep -v '^#' | grep -v ply_count | wc -l | grep -qx 0 && echo "no move_count readers remain"</automated>
  </verify>
  <done>Schemas expose `ply_count: int | None`; all four readers pass `ply_count=...`; zero `move_count` readers in app/schemas, app/services, app/repositories.</done>
</task>

<task type="auto">
  <name>Task 4: Single migration (add nullable + backfill + drop move_count) + model swap</name>
  <files>app/models/game.py, alembic/versions/20260610_HHMMSS_<rev>_replace_move_count_with_ply_count.py</files>
  <action>
In `app/models/game.py` replace the `move_count` column (L120) with `ply_count: Mapped[int | None] = mapped_column(nullable=True)` (D-02a: stays nullable, `int | None`, mirroring move_count) and a comment "exact half-move count (replaces move_count, Phase 114.1)". Generate the migration with `uv run alembic revision -m "replace move_count with ply_count"` (do NOT use `--autogenerate` — author it by hand to keep it to exactly these three steps and avoid sweeping in unrelated model drift). Set `down_revision = "d9e3f1a4b5c6"` (current head). Pick a unique 12-hex revision id `<rev>` and rename the file to `YYYYMMDD_HHMMSS_<rev>_replace_move_count_with_ply_count.py`.

`upgrade()` (single transaction, literal `sa.*` only):
1. `op.add_column("games", sa.Column("ply_count", sa.Integer(), nullable=True))`
2. `op.execute("UPDATE games g SET ply_count = sub.mp FROM (SELECT game_id, max(ply) AS mp FROM game_positions GROUP BY game_id) sub WHERE g.id = sub.game_id")`
3. `op.drop_column("games", "move_count")`

`downgrade()` (reverse): `op.add_column("games", sa.Column("move_count", sa.Integer(), nullable=True))`; then `op.execute("UPDATE games SET move_count = (ply_count + 1) / 2 WHERE ply_count IS NOT NULL")` (integer division reproduces the old full-move count); then `op.drop_column("games", "ply_count")`. Add a docstring noting the backfill runs in-transaction at deploy (measured ~10s on prod, no NULL window, no HUMAN gate). Apply on the existing dev DB (do NOT reset it): `uv run alembic upgrade head`, then confirm round-trip `uv run alembic downgrade -1 && uv run alembic upgrade head` and a single head via `uv run alembic heads`. Then `uv run ty check app/ tests/` and the full backend suite (everything bound to the attribute is now switched, so the suite must be green).
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run ty check app/ tests/ && uv run pytest -n auto -x</automated>
  </verify>
  <done>`games.ply_count` (nullable) replaces `move_count` in the model + DB; the migration round-trips (`downgrade -1` then `upgrade head`); single alembic head; `ty` zero errors; full backend suite green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates / modifies these symbols (Plan 02 + Phase 115 depend on them):
- `games.ply_count` — new nullable `INTEGER` column (model `app/models/game.py`); `games.move_count` removed.
- ONE Alembic revision id `<rev>` (down_revision `d9e3f1a4b5c6`) — add nullable + backfill + drop move_count, with a reversing downgrade. (NO second migration.)
- `zobrist.GameProcessingResult["ply_count"]` — TypedDict key (was `move_count`); computed as `len(nodes)`.
- `import_service._PositionRowsResult.ply_counts` — dict field (was `move_counts`) feeding the Stage 5 bulk UPDATE.
- `app/schemas/openings.py` `GameRecord.ply_count` + `app/schemas/library.py` two `ply_count` fields (API now returns `ply_count`, no longer `move_count`).
- Four backend readers (openings/library service, endgame service, library repository) pass `ply_count=...`.
(NO backfill script, NO enforce migration.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic auto-run → prod schema | The single migration (add + backfill + drop) runs in one transaction at container start (`deploy/entrypoint.sh`) before uvicorn serves |
| import pipeline → games table | server-internal write of `ply_count` derived from already-parsed PGN nodes; no new external input |
| API → client | `ply_count` (int | None) replaces `move_count` in JSON responses; same shape, no PII, no internal hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114.1-01 | Denial of Service | in-transaction backfill UPDATE at deploy | accept | Measured on prod (2026-06-10): the `max(ply) GROUP BY game_id` aggregate runs 3.2s at 33MB peak; the write touches ~590k `games` rows; sub-10s total. Runs before uvicorn serves, so no NULL window and no concurrent-import memory pressure (this is NOT the SQLAlchemy-fanout OOM path). Reversible via `downgrade()`. |
| T-114.1-02 | Denial of Service | import-path write | accept | `ply_count = len(nodes)` is a constant-time read of an already-built node list; no added query, scan, or memory. |
| T-114.1-03 | Information Disclosure | API field swap move_count→ply_count | accept | `ply_count` is a non-sensitive game-length integer (game length was already implied by `move_count`); no internal hash or new data exposed. |
| T-114.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan; nothing to verify. |

No user-facing input, auth/authz surface, or new endpoint. The migration's bulk UPDATE uses no external input; the import-path UPDATE uses bound params already present in Stage 5. No `high`-severity threat.
</threat_model>

<verification>
- `uv run alembic upgrade head` then `downgrade -1` then `upgrade head` all succeed (single migration round-trips) against the existing dev DB.
- `uv run pytest -n auto` green (full backend suite — everything attribute-bound is switched in this wave).
- `uv run ty check app/ tests/` zero errors; `uv run ruff check app/ tests/` + `uv run ruff format --check app/ tests/` clean.
- `grep -rn "move_count" app/ tests/ | grep -v '^#'` returns nothing (column + readers + fixtures fully cut over).
</verification>

<success_criteria>
- `games.ply_count` (nullable) replaces `move_count`; the single migration chains from `d9e3f1a4b5c6`, backfills in-transaction, and round-trips.
- New imports write exact half-move `ply_count` via Stage 5; all backend readers + API schemas expose `ply_count` (no fallback).
- Full backend suite green; `ty`/`ruff` clean; no `move_count` reference remains in `app/` or `tests/`.
</success_criteria>

<output>
Create `.planning/phases/114.1-move-count-to-ply-count/114.1-01-SUMMARY.md` when done. Record the chosen `<rev>` revision id.
</output>
