---
phase: 114.1-move-count-to-ply-count
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/game.py
  - app/services/zobrist.py
  - app/services/import_service.py
  - tests/test_zobrist.py
  - tests/test_import_service.py
  - tests/services/test_import_service.py
  - alembic/versions/20260610_HHMMSS_<revA>_add_ply_count_to_games.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Migration A adds a nullable ply_count INTEGER column to games, chaining from d9e3f1a4b5c6"
    - "process_game_pgn returns ply_count = len(nodes) (exact half-move count), not (len(nodes)+1)//2"
    - "import_service Stage 5 bulk UPDATE writes games.ply_count for every game in the batch"
    - "Wave 0 test fixtures assert ply_count (half-moves), not move_count (full moves)"
  artifacts:
    - path: "alembic/versions/20260610_HHMMSS_<revA>_add_ply_count_to_games.py"
      provides: "Migration A — add ply_count nullable; down_revision=d9e3f1a4b5c6"
      contains: 'op.add_column("games"'
    - path: "app/models/game.py"
      provides: "games.ply_count Mapped[int | None] column"
      contains: "ply_count"
    - path: "app/services/zobrist.py"
      provides: "ply_count = len(nodes) computation + GameProcessingResult key"
      contains: "ply_count"
  key_links:
    - from: "app/services/import_service.py"
      to: "app/models/game.py games.ply_count"
      via: "Stage 5 bulk UPDATE .values(ply_count=bindparam)"
      pattern: "ply_count=bindparam"
    - from: "app/services/import_service.py"
      to: "app/services/zobrist.py GameProcessingResult"
      via: 'processing_result["ply_count"] accumulation'
      pattern: 'processing_result\["ply_count"\]'
---

<objective>
Add the exact half-move column `games.ply_count` and make the import path write it. This is the foundation wave: schema (model + Migration A, nullable add only), the import-time computation in `zobrist.py`, the Stage 5 bulk UPDATE in `import_service.py`, and the Wave 0 test-fixture renames that assert the new half-move semantics.

Per D-02, Migration A is the cheap, auto-safe DDL (nullable add, nothing else); the heavy backfill is a separate standalone script (Plan 02). Per D-04, new imports always carry `ply_count`, so the backfill only ever touches pre-existing rows.

Purpose: every new game imported after this ships gets an exact `ply_count`, and the column exists for the backfill to populate.
Output: nullable `games.ply_count` column, import path writing it, Wave 0 fixtures green on the new semantics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-CONTEXT.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-RESEARCH.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-VALIDATION.md
</context>

<read_first>
- `app/models/game.py` (L120) — current `move_count` column; add `ply_count` alongside (do NOT remove `move_count` in this plan — it is dropped in Plan 04 Migration B).
- `app/services/zobrist.py` (L67 TypedDict key, L145 + L154 docstrings, L271 computation, L276 return) — the exact rename + computation-change sites.
- `app/services/import_service.py` (L92-106 `_PositionRowsResult`, L695 + L738 + L756-770 Stage 5, L859-860 accumulation) — the field rename + bulk-UPDATE column change.
- `alembic/versions/20260610_185500_d9e3f1a4b5c6_games_toast_tuple_target_seed_041.py` — current head + the migration file naming convention `YYYYMMDD_HHMMSS_<revid>_<slug>.py`.
- `alembic/versions/20260521_*_add_evals_completed_at*.py` — the `op.add_column` nullable-add pattern to mirror (Migration A is even simpler — no inline backfill, no index).
- RESEARCH §4 (import path exact lines), §7 (tests), Pitfall 4 (`move_counts` plural propagation), Pitfall 5 (`GameProcessingResult` TypedDict key must rename with all callers).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — rename test fixtures to assert ply_count (half-moves)</name>
  <files>tests/test_zobrist.py, tests/test_import_service.py, tests/services/test_import_service.py</files>
  <action>
Per VALIDATION.md Wave 0 (these renames make the failing assertions exist BEFORE the implementation). In `tests/test_zobrist.py` rename `test_process_game_pgn_move_count` → `test_process_game_pgn_ply_count`, change the assertion target to `result["ply_count"]` and the expected value from `2` to `3` (the `"1. e4 e5 2. Nf3 *"` fixture has 3 half-moves = `len(nodes)`); update the docstring to say "3 half-moves → ply_count=3". Leave `test_process_game_pgn_returns_correct_ply_count` (which asserts `len(result["plies"])`) unchanged — confirm it still passes. In `tests/test_import_service.py`: rename the `_make_mock_processing_result(... move_count=...)` param and dict key to `ply_count`, rename `test_move_count_populated` → `test_ply_count_populated` (+ docstring + local vars), and in `TestFlushBatchStage5` / `TestFlushBatchStage5RealDb` rename every `move_count` reference to `ply_count` (class docstrings, mock dict keys, `Game(...)` constructor kwargs seeded NULL, and the post-UPDATE `assert g.ply_count == ...`). In `tests/services/test_import_service.py` change the two `"move_count"` dict literals to `"ply_count"` with half-move values (`"move_count": 2` → `"ply_count": 4`; `"move_count": 1` → `"ply_count": 1`). Grep both `move_count` AND `move_counts` (plural) across all three files to confirm zero remaining references. These tests will FAIL until Tasks 2-3 land — that is expected (RED).
  </action>
  <verify>
    <automated>grep -rn "move_count" tests/test_zobrist.py tests/test_import_service.py tests/services/test_import_service.py | grep -v '^#' | wc -l | grep -qx 0 && echo "no move_count refs remain"</automated>
  </verify>
  <done>Zero `move_count`/`move_counts` references in the three test files; renamed tests reference `ply_count` with half-move expected values. Tests are RED (implementation not yet done).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute exact ply_count in the import path (zobrist + import_service)</name>
  <files>app/services/zobrist.py, app/services/import_service.py</files>
  <behavior>
    - `process_game_pgn("1. e4 e5 2. Nf3 *")` returns a dict whose `ply_count` key equals `len(nodes)` (= 3), NOT `(len(nodes)+1)//2` (= 2).
    - `flush_batch` Stage 5 issues a bulk UPDATE that sets `games.ply_count` for every game in the batch from the accumulated per-game half-move count.
  </behavior>
  <action>
In `app/services/zobrist.py`: rename the `GameProcessingResult` TypedDict key `move_count: int` → `ply_count: int` (L67); change L271 `move_count = (len(nodes) + 1) // 2` → `ply_count = len(nodes)`; change the return at L276 `move_count=move_count` → `ply_count=ply_count`; update the two docstrings (L145, L154) to say `ply_count`. In `app/services/import_service.py`: rename the `_PositionRowsResult.move_counts` dataclass field → `ply_counts` (L102) and its docstring (L96, L695); in Stage 5 (L756-770) rename the guard `if rows_result.move_counts:` → `ply_counts`, the `update(...).values(move_count=bindparam("b_mc"))` → `.values(ply_count=bindparam("b_pc"))`, and the params builder to use `b_pc`/`ply_counts.items()`; rename the local statement/params vars (`move_count_stmt` → `ply_count_stmt`, etc.) for clarity; at L859-860 change the accumulation to `out.ply_counts[game_id] = processing_result["ply_count"]`. Keep the existing `# ty: ignore[invalid-argument-type]` on the `update()` call if present. Grep `import_service.py` and `zobrist.py` for both `move_count` and `move_counts` to confirm full propagation (Pitfalls 4 & 5). Do NOT touch `app/models/game.py` here (Task 3 owns the column add to keep model + migration atomic).
  </action>
  <verify>
    <automated>uv run pytest tests/test_zobrist.py::test_process_game_pgn_ply_count -x && uv run pytest tests/test_import_service.py -k "ply_count or Stage5" -x</automated>
  </verify>
  <done>`process_game_pgn` returns `ply_count = len(nodes)`; Stage 5 UPDATE writes `ply_count`; the renamed zobrist + Stage 5 tests pass; zero stray `move_count`/`move_counts` in either file.</done>
</task>

<task type="auto">
  <name>Task 3: Add ply_count column to model + Migration A (nullable add)</name>
  <files>app/models/game.py, alembic/versions/20260610_HHMMSS_<revA>_add_ply_count_to_games.py</files>
  <action>
In `app/models/game.py` add `ply_count: Mapped[int | None] = mapped_column(nullable=True)` next to `move_count` (L120), with a comment "exact half-move count (replaces move_count, Phase 114.1)". Keep `move_count` for now (dropped in Plan 04). Generate Migration A with `uv run alembic revision -m "add ply_count to games"` (do NOT use `--autogenerate` — author it by hand to keep it to the single nullable add and avoid sweeping in unrelated model drift). Set `down_revision = "d9e3f1a4b5c6"` (current head). `upgrade()`: `op.add_column("games", sa.Column("ply_count", sa.Integer(), nullable=True))`. `downgrade()`: `op.drop_column("games", "ply_count")`. Use literal `sa.*` types only — no `app.*` imports (version-pinned snapshot rule). Pick a unique 12-hex revision id for `<revA>` and rename the file to match `YYYYMMDD_HHMMSS_<revA>_add_ply_count_to_games.py`. Confirm `uv run alembic heads` shows a single head after the new revision.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run ty check app/ tests/</automated>
  </verify>
  <done>`games.ply_count` exists in the model + DB after `alembic upgrade head`; Migration A round-trips (`downgrade -1` then `upgrade head` both succeed); single alembic head; `ty check` zero errors.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates / modifies these symbols (later plans + Phase 115 depend on them):
- `games.ply_count` — new nullable `INTEGER` column (model `app/models/game.py` + Migration A).
- Migration A revision id `<revA>` (down_revision `d9e3f1a4b5c6`) — chains the schema graph; Plan 04 Migration B chains from it.
- `zobrist.GameProcessingResult["ply_count"]` — TypedDict key (was `move_count`); computed as `len(nodes)`.
- `import_service._PositionRowsResult.ply_counts` — dict field (was `move_counts`) feeding the Stage 5 bulk UPDATE.
- Renamed tests: `test_process_game_pgn_ply_count`, `test_ply_count_populated`, and `TestFlushBatchStage5*` assert `ply_count`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import pipeline → games table | server-internal write of `ply_count`; values derived from already-parsed PGN nodes, no new external input |
| Alembic auto-run → prod schema | Migration A runs on container start (`deploy/entrypoint.sh`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114.1-01 | Tampering | Migration A on prod auto-run | accept | Nullable add-column is a fast, non-rewriting DDL; no data touched; reversible via `downgrade()`. No gate needed (the heavy/gated step is Plan 04). |
| T-114.1-02 | Denial of Service | import-path write | accept | `ply_count = len(nodes)` is a constant-time read of an already-built node list; no added query, scan, or memory. |
| T-114.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan; nothing to verify. |

No user-facing input, auth/authz surface, or new endpoint is introduced. No injection vector (the UPDATE uses bound params already present in Stage 5).
</threat_model>

<verification>
- `uv run alembic upgrade head` then `downgrade -1` then `upgrade head` all succeed (Migration A round-trips).
- `uv run pytest tests/test_zobrist.py tests/test_import_service.py tests/services/test_import_service.py -x` green.
- `uv run ty check app/ tests/` zero errors; `uv run ruff check app/ tests/` + `uv run ruff format --check app/ tests/` clean.
- `grep -rn "move_count" app/services/zobrist.py app/services/import_service.py | grep -v '^#'` returns nothing (full propagation).
</verification>

<success_criteria>
- `games.ply_count` nullable column exists; Migration A chains from `d9e3f1a4b5c6` and round-trips.
- New imports write exact half-move `ply_count` via Stage 5.
- All Wave 0 renamed tests pass; `ty`/`ruff` clean.
- `move_count` still present (intact for existing readers + later drop).
</success_criteria>

<output>
Create `.planning/phases/114.1-move-count-to-ply-count/114.1-01-SUMMARY.md` when done. Record the chosen `<revA>` revision id (Plan 04 needs it for `down_revision`).
</output>
