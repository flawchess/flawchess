---
phase: 114.1-move-count-to-ply-count
plan: 02
type: execute
wave: 2
depends_on: ["114.1-01"]
files_modified:
  - frontend/src/lib/chess.ts
  - frontend/src/types/api.ts
  - frontend/src/types/library.ts
  - frontend/src/components/results/GameCard.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/hooks/__tests__/useLibraryGame.test.tsx
  - frontend/src/components/results/__tests__/GameCard.test.tsx
  - frontend/src/components/library/__tests__/FlawCard.test.tsx
  - frontend/src/pages/library/__tests__/FlawsTab.test.tsx
  - scripts/benchmarks/chapter5.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend types carry ply_count: number | null; the null guard wrapping the render is preserved"
    - "The three game cards render Math.ceil(ply_count/2) via a shared plysToFullMoves helper — the displayed 'N Moves' label is byte-identical to before"
    - "Frontend test fixtures use ply_count = 2 × the old move_count so rendered-text assertions are unchanged"
    - "chapter5.py user_moves_per_game reads games.ply_count (FLOOR white / CEIL black) via a games join with zero game_positions scan"
    - "§5 is regenerated via the fast path (chapter5.compute(), ~1-2 min) and the narrated prose + headline table are reviewed (not just the diff-gate)"
  artifacts:
    - path: "frontend/src/lib/chess.ts"
      provides: "plysToFullMoves(plyCount) shared display helper"
      contains: "Math.ceil"
    - path: "frontend/src/types/api.ts"
      provides: "GameRecord.ply_count"
      contains: "ply_count"
    - path: "scripts/benchmarks/chapter5.py"
      provides: "Simplified user_moves_per_game CTE reading games.ply_count"
      contains: "ply_count"
  key_links:
    - from: "frontend/src/components/results/GameCard.tsx"
      to: "frontend/src/lib/chess.ts plysToFullMoves"
      via: "import + call on game.ply_count"
      pattern: "plysToFullMoves"
    - from: "scripts/benchmarks/chapter5.py"
      to: "games.ply_count"
      via: "FLOOR/CEIL(g.ply_count/2.0) by user_color, JOIN games"
      pattern: "ply_count"
---

<objective>
Cut the frontend over to `ply_count` and pay off the benchmark §5 follow-on. The three game cards derive the unchanged "N Moves" full-move label as `Math.ceil(ply_count / 2)` via a shared `plysToFullMoves` helper (D-01); the TS types + fixtures switch to `ply_count`. Then simplify `scripts/benchmarks/chapter5.py`'s `user_moves_per_game` to read `games.ply_count` directly (dropping its `game_positions` scan) and regenerate §5 via the fast path (D-05).

This wave runs after Plan 01 ships the API change, so the frontend consumes the new `ply_count` field and the benchmark reads the new column. The visible UI and the §5 numbers are unchanged — `Math.ceil(ply_count/2)` reproduces the old display and `FLOOR/CEIL(ply_count/2)` reproduces the old per-color user-move count.

Purpose: keep the UI byte-identical while removing the last `move_count` references and dropping the benchmark's ~87M-row scan.
Output: frontend types + cards on `ply_count` (via shared helper), frontend suite green, chapter5 reading `games.ply_count`, §5 regenerated + reviewed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-CONTEXT.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-RESEARCH.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-VALIDATION.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-01-SUMMARY.md
</context>

<read_first>
- RESEARCH §6 — frontend inventory with exact lines: `types/api.ts:111`, `types/library.ts:65,208`, `GameCard.tsx:157-163`, `FlawCard.tsx:226-232`, `LibraryGameCard.tsx:348-352`, plus the four test files + their fixture values. Note Pitfall 6: `LibraryGameCard` renders `{count} Moves` inline as one string while `GameCard`/`FlawCard` split the count from a `<span className="hidden sm:inline">&nbsp;Moves</span>` — both need the derivation but keep their distinct markup.
- RESEARCH Pitfall 2 — `noUncheckedIndexedAccess`: keep the existing `game.ply_count !== null && (...)` null guard (same place `move_count`'s guard was; `ply_count` is `number | null`).
- RESEARCH Pitfall 3 + Open Question 2 — fixture numeric values: rename `move_count: N` → `ply_count: 2*N` so the rendered full-move count stays N (e.g. `move_count: 40` → `ply_count: 80`; the FlawCard null-test fixture stays `null`).
- RESEARCH §8 — the current `user_moves_per_game` CTE (`chapter5.py` L240-252, joins `game_positions`) and the simplified replacement (`CASE WHEN bg.user_color='white' THEN FLOOR(g.ply_count/2.0) ELSE CEIL(g.ply_count/2.0) END`, `JOIN games g ON g.id = bg.game_id`); fast-path refresh via `chapter5.compute()`.
- `.claude/skills/benchmarks` SKILL.md — the §5 fast-path (`chapter5.compute()`) and the diff-gate; MEMORY `[Benchmark §5 refresh fast path]` (~1-2 min, NOT the 33-min full `gen_benchmarks`) and `[Diff-gate ≠ report narration]` (review narrated prose + headline table, not just the gate). `bin/benchmark_db.sh start` if the benchmark DB is needed.
- MEMORY `[Frontend has no Prettier]` — ESLint only; never run `prettier --write`. `cn()` uses tailwind-merge. MEMORY `[Always apply changes to mobile too]` — the cards' mobile/desktop split markup both need the derivation. Knip runs in CI — ensure `plysToFullMoves` is imported (no dead export).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Frontend — shared helper + types + three cards derive Math.ceil(ply_count/2)</name>
  <files>frontend/src/lib/chess.ts, frontend/src/types/api.ts, frontend/src/types/library.ts, frontend/src/components/results/GameCard.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/FlawCard.tsx</files>
  <action>
Add (or create) `frontend/src/lib/chess.ts` exporting `plysToFullMoves(plyCount: number): number { return Math.ceil(plyCount / 2); }` — a single source of truth for the derivation imported by all three cards (so Knip does not flag a dead export). Rename the TS types: `types/api.ts:111` `move_count: number | null` → `ply_count: number | null`; `types/library.ts:65` and `:208` likewise. In each card keep the existing `!== null` null guard but switch the field to `ply_count` and the displayed value to `plysToFullMoves(game.ply_count)` (Pitfall 2: guard preserved so `noUncheckedIndexedAccess`/`null` narrows): `GameCard.tsx:157-163` (`{plysToFullMoves(game.ply_count)}` + keep the `<span className="hidden sm:inline">&nbsp;Moves</span>`); `FlawCard.tsx:226-232` (`flaw.ply_count` + `plysToFullMoves(flaw.ply_count)`); `LibraryGameCard.tsx:348-352` (inline `{plysToFullMoves(game.ply_count)} Moves` — keep its single-string markup per Pitfall 6). The rendered "N Moves" output MUST be byte-identical to before. Do not run prettier; ESLint only.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <done>`plysToFullMoves` helper exists + imported; all three TS type fields renamed; all three cards derive the full-move count via the helper with the null guard intact; `npm run lint` (incl. Knip + tsc) passes.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — update test fixtures (ply_count = 2 × old move_count) + run tests</name>
  <files>frontend/src/hooks/__tests__/useLibraryGame.test.tsx, frontend/src/components/results/__tests__/GameCard.test.tsx, frontend/src/components/library/__tests__/FlawCard.test.tsx, frontend/src/pages/library/__tests__/FlawsTab.test.tsx</files>
  <action>
Rename every `move_count` fixture key → `ply_count` and double the numeric value so the rendered full-move count is unchanged (Pitfall 3): `useLibraryGame.test.tsx:55` `move_count: 40` → `ply_count: 80`; `GameCard.test.tsx:48` `move_count: 40` → `ply_count: 80`; `FlawCard.test.tsx` (L105, L204-205, L249 — including the `null` case which stays `ply_count: null`); `FlawsTab.test.tsx` (L88, L241, L266, L319, L344 fixtures + any inline type referencing the field). Where a test asserts the rendered text (e.g. "40 Moves" / "Moves"), keep the asserted display value unchanged (the doubling preserves it). Grep all four files for `move_count` → zero remaining. Then run the frontend suite.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "move_count" src/ | wc -l | grep -qx 0 && npm test -- --run</automated>
  </verify>
  <done>Zero `move_count` in `frontend/src`; fixtures use `ply_count` with doubled values; rendered-text assertions unchanged; `npm test -- --run` green.</done>
</task>

<task type="auto">
  <name>Task 3: Simplify chapter5.py user_moves_per_game to read games.ply_count</name>
  <files>scripts/benchmarks/chapter5.py</files>
  <action>
Replace the `user_moves_per_game` CTE (L240-252) that scans `game_positions` with the direct `games.ply_count` read from RESEARCH §8: `user_moves_per_game AS (SELECT bg.game_id, CASE WHEN bg.user_color = 'white' THEN FLOOR(g.ply_count / 2.0) ELSE CEIL(g.ply_count / 2.0) END AS user_moves FROM base_games bg JOIN games g ON g.id = bg.game_id)`. Update the leading comment to note it reads `games.ply_count` (exact half-moves, Phase 114.1) with no `game_positions` scan, and the `floor=white / ceil=black` convention. Confirm no other part of `chapter5.py` still references the dropped `move_count` or relies on the removed `game_positions` join columns from the old CTE. Keep the result column name `user_moves` so downstream aggregation/diff-gate is unaffected.
  </action>
  <verify>
    <automated>grep -n "ply_count" scripts/benchmarks/chapter5.py && grep -c "move_count" scripts/benchmarks/chapter5.py | grep -qx 0 && echo "chapter5 reads ply_count, no move_count"</automated>
  </verify>
  <done>`user_moves_per_game` reads `games.ply_count` via FLOOR/CEIL by color; no `game_positions` scan in that CTE; no `move_count` reference remains in chapter5.py; `user_moves` column name preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate §5 (fast path) + review narrated prose</name>
  <files>scripts/benchmarks/chapter5.py</files>
  <action>
Regenerate §5 via the fast path per CONTEXT D-05 + MEMORY `[Benchmark §5 refresh fast path]`: run `chapter5.compute()` (~1-2 min) — do NOT run the full ~33-min `gen_benchmarks`. Start the benchmark DB first if needed (`bin/benchmark_db.sh start`; it is read-only, port 5433). After regen, review `reports/benchmarks-latest.md` §5: confirm the narrated prose AND the headline table read sensibly after the basis change (MEMORY `[Diff-gate ≠ report narration]` — a green diff-gate alone is NOT sufficient; verify ALL §5 metric sections + the headline table). Sanity-check user_moves magnitudes against the prior `game_positions`-based numbers — expect equality, since `FLOOR/CEIL(ply_count/2)` reproduces the old per-color count exactly. Record the §5 review outcome (the metric values + a one-line "matches prior / differs because…") in the SUMMARY.
  </action>
  <verify>
    <automated>echo "Regenerate §5 via chapter5.compute() then review §5 prose + headline table in reports/benchmarks-latest.md; record outcome in SUMMARY"</automated>
    <human-check>§5 regenerated via the fast path; narrated prose + headline table reviewed (not just the diff-gate); user_moves magnitudes match the prior game_positions-based numbers; outcome recorded in SUMMARY.</human-check>
  </verify>
  <done>§5 regenerated via `chapter5.compute()`; prose + headline table reviewed and sane; user_moves magnitudes equal the prior basis; outcome recorded in SUMMARY.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `frontend/src/lib/chess.ts::plysToFullMoves(plyCount: number): number` — shared display-derivation helper.
- `frontend/src/types/api.ts` `GameRecord.ply_count` + `frontend/src/types/library.ts` two `ply_count` fields.
- Three cards (`GameCard`, `LibraryGameCard`, `FlawCard`) now render `plysToFullMoves(ply_count)` — display byte-identical.
- `scripts/benchmarks/chapter5.py` `user_moves_per_game` — now reads `games.ply_count` (FLOOR/CEIL by color), zero `game_positions` scan.
- Regenerated §5 in `reports/benchmarks-latest.md` (fast-path), reviewed.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → client render | `ply_count` (number | null) consumed from the API; derived display value, no user input |
| benchmark script → benchmark DB (read-only) | chapter5 reads `games.ply_count`; offline, no user input, RO role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114.1-02-01 | Tampering | card derivation `Math.ceil(ply_count/2)` | accept | Pure client-side display math on a non-sensitive integer; the `!== null` guard is preserved so a NULL `ply_count` renders nothing (fail-safe), not a crash. |
| T-114.1-02-02 | Information Disclosure | regenerated §5 report | accept | Aggregate benchmark stats only; no per-user PII; same data shape and (expected) same magnitudes as before the basis change. |
| T-114.1-02-SC | Tampering | npm installs | mitigate | No new dependencies (helper is local code); nothing to verify. |

No new external input, endpoint, or auth surface. No `high`-severity threat.
</threat_model>

<verification>
- `grep -rn "move_count" frontend/src scripts/benchmarks/chapter5.py` returns nothing.
- `cd frontend && npm run lint && npm test -- --run` green (incl. Knip + tsc).
- `chapter5.py` `user_moves_per_game` reads `games.ply_count` with no `game_positions` scan; §5 regenerated via `chapter5.compute()` and the prose + headline table human-reviewed.
- Manual spot-check (optional): a Library/Openings card still shows the same "N Moves" as before.
</verification>

<success_criteria>
- Frontend types + three cards derive `Math.ceil(ply_count/2)` via `plysToFullMoves`; displayed label byte-identical; frontend suite green.
- `chapter5.py` reads `games.ply_count` (FLOOR/CEIL by color) with no `game_positions` scan; §5 regenerated + reviewed; magnitudes match the prior basis.
- No `move_count` reference remains anywhere in `frontend/src` or `scripts/benchmarks/chapter5.py`.
</success_criteria>

<output>
Create `.planning/phases/114.1-move-count-to-ply-count/114.1-02-SUMMARY.md` when done. Record the §5 review outcome (metric values + match-vs-prior note).
</output>
