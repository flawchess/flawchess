---
phase: 114.1-move-count-to-ply-count
plan: 02
type: execute
wave: 2
depends_on: ["114.1-01"]
files_modified:
  - scripts/backfill_ply_count.py
  - tests/test_backfill_ply_count.py
autonomous: true
requirements: []
user_setup:
  - service: production-db-tunnel
    why: "Prod backfill is a HUMAN-UAT runbook step (cannot run in CI/dev); requires the SSH tunnel to prod Postgres"
    dashboard_config:
      - task: "Open the prod DB tunnel before running --db prod"
        location: "bin/prod_db_tunnel.sh (forwards localhost:15432 → prod Postgres); close with bin/prod_db_tunnel.sh stop"
must_haves:
  truths:
    - "scripts/backfill_ply_count.py populates games.ply_count = max(ply) per game from game_positions, in 10k-game chunks"
    - "The backfill is idempotent and resumable (AND g.ply_count IS NULL guard + MIN(id) WHERE ply_count IS NULL outer bound)"
    - "--db accepts only dev|benchmark|prod (Literal), with --dry-run and a chunk-size constant"
    - "After running on dev, SELECT count(*) FROM games WHERE ply_count IS NULL = 0"
  artifacts:
    - path: "scripts/backfill_ply_count.py"
      provides: "Standalone resumable ply_count backfill driver mirroring backfill_eval.py"
      contains: "ply_count IS NULL"
    - path: "tests/test_backfill_ply_count.py"
      provides: "Integration test: backfill sets ply_count = max(ply) and is idempotent"
      contains: "ply_count"
  key_links:
    - from: "scripts/backfill_ply_count.py"
      to: "game_positions.ply"
      via: "UPDATE games SET ply_count = MAX(ply) FROM game_positions"
      pattern: "MAX\\(ply\\)"
    - from: "scripts/backfill_ply_count.py"
      to: "db target resolution"
      via: "db_url_for_target(target) shared with backfill_eval.py"
      pattern: "db_url_for_target"
---

<objective>
Create the standalone, resumable, idempotent backfill script `scripts/backfill_ply_count.py` that populates `games.ply_count` for all pre-existing rows from `game_positions` (per D-02, decoupled from auto-running Alembic to avoid the prod OOM history). It mirrors `scripts/backfill_eval.py`'s structure: `--db dev|benchmark|prod` Literal, batched game-id-range UPDATE, idempotent `ply_count IS NULL` guard, async session, Sentry init, progress logging, and a closing `VACUUM ANALYZE games`.

Run it on dev as part of verification. The prod run is a documented HUMAN-UAT runbook step (requires `bin/prod_db_tunnel.sh` + 190M-row prod data) — flagged, not automated.

Purpose: bring existing rows up to exact `ply_count` so Plan 04's enforce migration can set `NOT NULL`.
Output: the backfill script, a small integration test, dev fully backfilled (`ply_count IS NULL = 0`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-CONTEXT.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-RESEARCH.md
@.planning/phases/114.1-move-count-to-ply-count/114.1-VALIDATION.md
</context>

<read_first>
- `scripts/backfill_eval.py` — the script to clone. Keep `_log()`, `_db_url(target)` / `db_url_for_target`, the `parse_args()` skeleton (`--db`, `--dry-run`, `--limit`), `main()` Sentry init (L883-884), the session-maker + `dispose_engine` lifecycle, the game-id-range chunk loop (L622-700), and the closing VACUUM (L800-806). Drop eval-specific args (`--workers`, `--timeout`) — no Stockfish engine is needed for a pure SQL UPDATE.
- RESEARCH §3 — the exact backfill SQL (`UPDATE games g SET ply_count = sub.max_ply FROM (SELECT game_id, MAX(ply) AS max_ply FROM game_positions WHERE game_id BETWEEN :lo AND :hi GROUP BY game_id) sub WHERE g.id = sub.game_id AND g.ply_count IS NULL`), the outer-bound query (`SELECT COALESCE(MIN(id),0), COALESCE(MAX(id),0) FROM games WHERE ply_count IS NULL`), the 10k-game chunk loop shape, and the "why game_id range not position-row range" rationale.
- RESEARCH §9 — verification queries (NULL-count = 0; ply_count = max(gp.ply) sample; ply_count ∈ {2·move_count, 2·move_count−1}); OOM-risk note (bounded server-side join, no Python materialization).
- `tests/conftest.py` (per-run test DB; no `bin/reset_db.sh`) — for the integration test fixture shape.
- CLAUDE.md: `--db` MUST be `Literal["dev","benchmark","prod"]`; no magic numbers (chunk size → named constant); Sentry `capture_exception` in non-trivial except; never `asyncio.gather` on one `AsyncSession` (the chunk loop is sequential — keep it that way).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Backfill integration test (Wave 0 for this plan)</name>
  <files>tests/test_backfill_ply_count.py</files>
  <action>
Write a focused integration test against the per-run test DB (follow `tests/conftest.py` session fixtures). Seed a few `Game` rows with `ply_count = NULL` and matching `game_positions` rows whose `MAX(ply)` is known per game (e.g. game A with plies 0..5 → expected ply_count 5; game B with plies 0..2 → 2). Import and call the backfill's core coroutine (the `run_backfill()`-equivalent, or a smaller `_backfill_chunk(session, lo, hi)` helper if you factor one out) directly against the test session/engine. Assert: (a) after the run, every seeded game has `ply_count == MAX(ply)`; (b) re-running is a no-op (no rows updated the second time — idempotency) and leaves values unchanged; (c) a game with a NULL `ply_count` and NO `game_positions` rows is left NULL (the LEFT side of the join produces no `sub` row). Use `Literal`-typed helpers, explicit return annotations (`ty` clean). This test exists before the script is complete — it is RED until Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backfill_ply_count.py -x</automated>
  </verify>
  <done>Test file exists asserting max(ply) backfill + idempotency + NULL-on-no-positions; RED until Task 2 lands.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/backfill_ply_count.py (clone of backfill_eval.py)</name>
  <files>scripts/backfill_ply_count.py</files>
  <action>
Clone `scripts/backfill_eval.py` into `scripts/backfill_ply_count.py`. Module docstring describes the ply_count backfill + usage (`uv run python scripts/backfill_ply_count.py --db dev|benchmark|prod`, prod via `bin/prod_db_tunnel.sh`). Keep `_log`, the `db_url_for_target`-based `--db` resolution (`Literal["dev","benchmark","prod"]`), and `main()` Sentry init (`sentry_sdk.init(dsn=settings.SENTRY_DSN, environment=settings.ENVIRONMENT)`). `parse_args()`: `--db` (required Literal), `--dry-run` (flag), optional `--limit` (cap games for testing); drop `--workers`/`--timeout`. Define a module constant `PLY_BACKFILL_CHUNK_SIZE = 10_000` (no magic number). `run_backfill()`: create the async engine + session-maker; query the outer bound (`SELECT COALESCE(MIN(id),0), COALESCE(MAX(id),0) FROM games WHERE ply_count IS NULL`); if `hi == 0` log "no rows to backfill (no-op)" and return; else loop `cursor → hi` in `PLY_BACKFILL_CHUNK_SIZE` steps, executing the RESEARCH §3 UPDATE (`ply_count = sub.max_ply` with `AND g.ply_count IS NULL` idempotency guard) with bound params `{lo, hi}`, `await session.commit()` per chunk, log `chunk [lo, hi]: N rows (total=...)`. `--dry-run` wraps the loop so it computes/logs counts but rolls back instead of committing (e.g. run the chunk UPDATEs inside a transaction that is rolled back, or short-circuit before commit). After the loop, run `VACUUM ANALYZE games` in an autocommit connection (mirror backfill_eval.py's VACUUM block). Wrap the run body so a non-trivial failure calls `sentry_sdk.capture_exception` after `sentry_sdk.set_context("backfill", {"db": target})`. Always `dispose_engine` in a finally. Sequential per-session execution only — never `asyncio.gather` on the session. Factor the per-chunk UPDATE into a small helper if it makes Task 1's test cleaner.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backfill_ply_count.py -x && uv run ty check app/ tests/ scripts/backfill_ply_count.py 2>/dev/null || uv run ty check app/ tests/</automated>
  </verify>
  <done>Script runs `--db dev`, populates `ply_count = max(ply)` in 10k chunks, idempotent + resumable; `--dry-run` commits nothing; integration test green; `ty` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Run the backfill on dev + verify NULL-count = 0</name>
  <files>scripts/backfill_ply_count.py</files>
  <action>
Run `uv run python scripts/backfill_ply_count.py --db dev` against the running dev DB (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` must be up). Do NOT reset the dev DB. Then verify with the dev DB MCP (`mcp__flawchess-db__query`) or `psql`: (a) `SELECT count(*) FROM games WHERE ply_count IS NULL` returns 0; (b) the RESEARCH §9 sample-consistency query (`ply_count != MAX(gp.ply)` for `id <= 1000`) returns 0 rows; (c) the relationship check `SELECT count(*) FROM games WHERE ply_count NOT IN (2*move_count, 2*move_count-1) AND ply_count IS NOT NULL AND move_count IS NOT NULL` returns 0 (move_count still present until Plan 04). Record the three query results in the SUMMARY. This is dev-only; the prod run is the HUMAN-UAT runbook in `<prod_runbook>` below — do not attempt it here.
  </action>
  <verify>
    <automated>echo "Manual dev verification — record NULL-count=0 and the two consistency queries (=0 rows) in SUMMARY"</automated>
    <human-check>Dev: SELECT count(*) FROM games WHERE ply_count IS NULL = 0; sample max(ply) consistency = 0 rows; ply_count ∈ {2·move_count, 2·move_count−1} for all non-null = 0 violations.</human-check>
  </verify>
  <done>Dev fully backfilled: `ply_count IS NULL = 0`, both consistency queries return 0, results recorded in SUMMARY.</done>
</task>

</tasks>

<prod_runbook>
## Prod backfill — HUMAN-UAT (do NOT run in this phase's automated execution)

This is operational, gated on the prod tunnel + 190M-row data; it is NOT a dev verification gate (D-02, VALIDATION.md Manual-Only). Sequence:

1. `bin/prod_db_tunnel.sh` — open the tunnel to `localhost:15432`.
2. `uv run python scripts/backfill_ply_count.py --db prod` — runs after Migration A has shipped to prod (Plan 01) but BEFORE Plan 04's enforce migration deploys.
3. Verify `SELECT count(*) FROM games WHERE ply_count IS NULL` returns 0.
4. `bin/prod_db_tunnel.sh stop`.

Expected: hundreds of commits at 10k-game chunks, minutes not hours, negligible memory (no PGN strings in memory — pure server-side SQL). Only after step 3 = 0 may the Plan 04 enforce-migration deploy proceed.
</prod_runbook>

<artifacts_this_phase_produces>
- `scripts/backfill_ply_count.py` — standalone resumable backfill driver.
- `scripts/backfill_ply_count.py::run_backfill` (and any `_backfill_chunk` helper) — the backfill coroutine(s).
- `PLY_BACKFILL_CHUNK_SIZE = 10_000` — named chunk-size constant.
- `--db` `Literal["dev","benchmark","prod"]` CLI arg.
- `tests/test_backfill_ply_count.py` — integration test (max(ply) backfill + idempotency).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill script → prod games table | the only write-capable, prod-touching control surface in this phase; run via tunnel by a human |
| `--db prod` arg | selects the prod target; the single sensitive control input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114.1-02-01 | Tampering | backfill UPDATE on prod | mitigate | Idempotent `AND g.ply_count IS NULL` guard makes re-runs safe; writes only `ply_count`, never reads/writes other columns; `--dry-run` lets the operator preview before committing. |
| T-114.1-02-02 | Elevation of Privilege | `--db prod` target | mitigate | `Literal["dev","benchmark","prod"]` rejects arbitrary DSNs; prod requires the operator to have opened `bin/prod_db_tunnel.sh` (no embedded prod creds in the script). |
| T-114.1-02-03 | Denial of Service | 190M-row backfill on prod | accept | Bounded 10k-game chunks with per-chunk commit + the composite PK index keep each chunk a small server-side join; no Python materialization (the Phase 78 OOM pattern is avoided). |
| T-114.1-02-SC | Tampering | npm/pip installs | mitigate | No new packages (clones existing `backfill_eval.py` deps already in the lockfile). |

No user-facing input or injection vector: the UPDATE uses bound `:lo`/`:hi` integer params; `--db` is a closed Literal. No `high`-severity threat.
</threat_model>

<verification>
- `uv run pytest tests/test_backfill_ply_count.py -x` green.
- Dev run: `ply_count IS NULL = 0`; sample consistency + move_count relationship queries return 0 rows.
- `uv run ty check app/ tests/` zero errors; `uv run ruff check`/`ruff format --check` clean.
</verification>

<success_criteria>
- Resumable, idempotent `scripts/backfill_ply_count.py` with `--db dev|benchmark|prod` Literal + `--dry-run`.
- Dev DB fully backfilled and verified (`ply_count IS NULL = 0`).
- Prod runbook documented and flagged HUMAN-UAT.
</success_criteria>

<output>
Create `.planning/phases/114.1-move-count-to-ply-count/114.1-02-SUMMARY.md` when done. Record the three dev verification query results and note prod backfill remains a HUMAN-UAT runbook step.
</output>
