---
status: complete
phase: 116-all-ply-engine-core
source: [116-01-SUMMARY.md, 116-02-SUMMARY.md, 116-03-SUMMARY.md]
started: 2026-06-12T20:36:31Z
updated: 2026-06-12T20:58:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running backend. Start the dev DB and backend from scratch. Server boots without errors, Alembic migration 20260612120000 (full_evals_completed_at) applies cleanly, and both background drain tasks start (entry-ply drain + full-eval-drain). A basic API call responds.
result: pass
note: Drain tasks start silently by design (no log line). Verified via API (HTTP 200), alembic head = 20260612120000, and live drain activity (full_evals_completed_at marks written at verification time).

### 2. Migration backfill marks fully-evaluated games
expected: After the migration runs, games whose stored positions ALL have evals (e.g. lichess-analyzed games) have full_evals_completed_at set; games with eval holes remain NULL. Verify on dev DB — `SELECT count(*) FROM games WHERE full_evals_completed_at IS NOT NULL` is plausibly nonzero (you have lichess-analyzed games imported), and a spot-checked marked game has no positions with eval_cp IS NULL AND eval_mate IS NULL (excluding the terminal row).
result: pass
note: Dev DB had run the PRE-fix migration (CR-01 backfill marked only 299 games, signature confirmed — zero marked games had a NULL-eval terminal row). Not a code bug — stale env state; prod will run the fixed migration. Resolution (user-approved): re-ran the fixed backfill UPDATE manually on dev → 9,343 games marked, total 10,577. Post-check: only 5 marked games have non-terminal holes, all drain-marked (D-116-07 mark-with-holes, expected).

### 3. Full-eval drain fills all plies of an unmarked game
expected: With the backend idle (no import running), the full-eval drain picks the most recent unmarked non-guest game and populates eval_cp/eval_mate for ALL its plies (not just endgame-entry plies), then sets full_evals_completed_at on that game. Observable on dev DB within a few minutes — pick a game where full_evals_completed_at was NULL and watch its game_positions eval columns fill, then the marker get set.
result: pass
note: Live-verified on dev DB — games 670724/670723/670722 drain-marked at ~10s/game with 100% non-terminal eval coverage (89/89, 82/82, 101/101), descending-id pick order (LIFO D-116-09), non-guest users (D-116-10).

### 4. Opening dedup reuses existing evals (ply <= 20)
expected: For a game being drained, positions in the opening region (ply <= 20) whose full_hash matches an already-fully-evaluated game's position reuse the stored eval instead of calling Stockfish. Observable as the drain completing common-opening games noticeably faster, or via DB check that two games sharing an opening line have identical eval_cp on the shared ply<=20 positions.
result: pass
note: Verified on dev DB — most recent drain-marked game has byte-identical eval_cp to marked-source rows at every ply 0-17 with a matching full_hash (plies 18-20 had no source and went to the engine). Drain pace (~10s per ~90-ply game) consistent with ~20 plies skipped. During this test the user reported Sentry FLAWCHESS-59 (engine restart bug) — orthogonal to dedup, logged as a gap below.

### 5. Yield gate — drain pauses during active import
expected: Start a game import (chess.com or lichess) while the full-eval drain is active. The full drain yields: import fetch + entry-ply evals proceed at normal speed and the full drain stops picking new games until the import and entry-ply backlog finish. No API latency degradation during the import.
result: pass
note: Live-verified — dual import (chess.com 4,354 + lichess 776 games) completed cleanly; zero full-drain marks during the import window (only one in-flight game finished its tick 10s after import start, expected). Drain remained paused after import completion while entry-ply backlog (2,592 pending) drains — both gate predicates confirmed.

## Summary

total: 5
passed: 5
issues: 1
pending: 0
skipped: 0

Note: all 5 tests passed; the 1 issue is a pre-existing engine-restart bug (Sentry FLAWCHESS-59) surfaced during test 4, logged in Gaps with full diagnosis.

## Gaps

- truth: "Engine worker restart after engine-process death recovers the worker slot cleanly"
  status: fixed  # inline fix during UAT session, commit 52d314e8 (broadened except tuples in _restart_worker + stop)
  reason: "User reported: Sentry FLAWCHESS-59 (dev, 2026-06-12T20:37:20Z) — RuntimeError: unable to perform operation on <WriteUnixTransport closed=True>; the handler is closed, raised from _restart_worker via run_full_eval_drain"
  severity: major
  test: 4
  artifacts:
    - app/services/engine.py
  missing: []
  diagnosis: |
    Root cause (diagnosed during UAT, no agent needed): SIGINT from uvicorn --reload killed
    the Stockfish children (exit code -2). EngineTerminatedError in _analyse triggered
    _restart_worker, which calls old_protocol.quit() guarded only against
    chess.engine.EngineError/EngineTerminatedError. With the transport already closed,
    uvloop's stdin.write() raises a bare RuntimeError ("handler is closed"), which escapes
    the except tuple, aborts the restart mid-way (dead protocol stays in the slot, raise-loop
    on every subsequent pickup), and bubbles to the drain catch-all. EnginePool.stop() has
    the same unguarded quit() pattern.
    Pre-existing bug (FLAWCHESS-59 first seen 2026-05-16, before Phase 116); the full drain
    resurfaced it (Sentry substatus: regressed).
    Fix: include RuntimeError in (or broaden) the except around old_protocol.quit() in
    _restart_worker and the per-protocol quit() in stop(), app/services/engine.py.
