---
phase: 116-all-ply-engine-core
plan: 03
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - app/services/engine.py
  - docker-compose.yml
  - CLAUDE.md
autonomous: false
requirements: [QUEUE-07]
must_haves:
  truths:
    - "Real per-worker RSS at the 1M-node budget is measured and the N-workers × footprint + import + Postgres accounting is documented against the 4g backend container limit"
    - "The pool size is set to 8 only if the accounting fits 4g with headroom and a prod soak shows API latency unaffected; otherwise it stays at 6"
    - "The stale STOCKFISH_POOL_SIZE comments in engine.py, docker-compose.yml, and CLAUDE.md are corrected to current prod reality (6, not 4)"
  artifacts:
    - path: "app/services/engine.py"
      provides: "memory-accounting comment next to the pool-size constants; corrected stale pool-size notes"
      contains: "STOCKFISH_POOL_SIZE"
    - path: "CLAUDE.md"
      provides: "Phase 116 memory accounting in the prod section; corrected 'pool lowered' stale note"
      contains: "STOCKFISH_POOL_SIZE"
    - path: "docker-compose.yml"
      provides: "corrected 'up to 6' / pool-size comment with measured accounting"
      contains: "STOCKFISH_POOL_SIZE"
  key_links:
    - from: "app/services/engine.py pool constants"
      to: "QUEUE-07 memory accounting comment"
      via: "code comment next to _DEFAULT_POOL_SIZE / _POOL_SIZE_ENV"
      pattern: "STOCKFISH_POOL_SIZE"
---

<objective>
Satisfy QUEUE-07: measure real per-worker RSS at the 1M-node budget, write down the memory
accounting (N workers × footprint + active import + Postgres vs the 4g backend container
limit), correct the stale pool-size comments across engine.py / docker-compose.yml / CLAUDE.md,
and gate the STOCKFISH_POOL_SIZE bump to 8 behind a measured 4g-headroom check plus a prod
latency soak (D-116-12, D-116-13). This is a deploy-time decision like `_BATCH_SIZE`/`_HASH_MB`,
not runtime machinery.

Purpose: The pipeline materially raises per-call cost (depth-15 ~0.09s → 1M-node ~0.98s) and
the milestone targets bumping the pool to all 8 vCPUs. QUEUE-07 requires the memory budget to be
explicitly bounded against 4g BEFORE the pool size is raised, so the all-ply drain (Plan 02)
cannot re-trigger the import-era OOM class of failures.

Output: a documented memory-accounting block, corrected stale comments, and a human-verified
go/no-go on pool size 8 vs 6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-all-ply-engine-core/116-CONTEXT.md
@.planning/phases/116-all-ply-engine-core/116-RESEARCH.md
@.planning/phases/116-all-ply-engine-core/116-01-SUMMARY.md
@.planning/spikes/002-sf-1m-node-latency-prod/README.md
</context>

## Artifacts this phase produces

Plan 03 produces no new code symbols. It edits documentation/comments only:
- A memory-accounting comment block next to the pool-size constants in `app/services/engine.py`
- Corrected stale pool-size comments (engine.py lines ~76 and ~101; docker-compose.yml line ~79;
  CLAUDE.md prod section "pool lowered" note)
- A new memory-accounting paragraph in the CLAUDE.md prod section

(Consumes from Plan 01: `evaluate_nodes` / `_NODES_BUDGET` for the RSS measurement.)

<tasks>

<task type="auto">
  <name>Task 1: Measure per-worker RSS at 1M-node budget + write the accounting</name>
  <files>app/services/engine.py</files>
  <read_first>
    - app/services/engine.py (pool constants + stale comments: lines 74-105 — the "prod STOCKFISH_POOL_SIZE=4" hash comment ~line 76, the "STOCKFISH_POOL_SIZE=4 to use all 4 vCPUs" pool comment ~line 101; `_DEFAULT_POOL_SIZE`/`_POOL_SIZE_ENV`; `EnginePool.evaluate_nodes` from Plan 01)
    - .planning/phases/116-all-ply-engine-core/116-RESEARCH.md (Memory accounting; QUEUE-07 row; Assumptions A1/A3; live RSS measurements 1/4/6/8 workers = 277/1096/1645/2200 MB)
    - .planning/spikes/002-sf-1m-node-latency-prod/README.md (prod CPX42 latency + 6-worker linear scaling, no memory measurement — that gap is this task)
    - .planning/phases/116-all-ply-engine-core/116-PATTERNS.md (Critical Constraint 6: stale-comment update)
  </read_first>
  <action>
    Measure real per-worker RSS at the 1M-node budget on the dev box: start an EnginePool, run
    evaluate_nodes() on several mid-game boards to force NNUE load + a full 1M-node search per
    worker, then read each worker process RSS (via /proc/<pid>/status VmRSS or psutil). Record the
    1-worker and N-worker totals (the local research baseline is 277 MB @ 1 worker, 2200 MB @ 8;
    NNUE net is page-cache-shared across workers per A3 — confirm the N-worker total is sub-linear).
    Add a memory-accounting comment block in engine.py directly above `_POOL_SIZE_ENV` /
    `_DEFAULT_POOL_SIZE` stating: measured per-worker RSS at 1M nodes, the prod extrapolation
    (Phase 91 ~368 MB/worker → 8 workers ~2.94 GB + FastAPI ~0.3 GB ≈ 3.24 GB), and the headroom
    under the 4g backend mem_limit. Correct the two stale comments in engine.py: the line-~76 note
    saying "prod STOCKFISH_POOL_SIZE=4 the pool reserved 4 * 64 = 256MB" (clarify it was the
    hotfix-era value; prod has run 6 stably for weeks) and the line-~101 note "STOCKFISH_POOL_SIZE=4
    to use all 4 vCPUs" (prod is CPX42 / 8 vCPUs; current pool 6; Phase 116 target 8 contingent on
    the QUEUE-07 check). Do NOT change `_DEFAULT_POOL_SIZE` (stays 1 = dev/CI singleton); the
    prod value is an env var. Keep all UCI options centralized (ENG-03) — this task is
    comment/measurement only, no UCI change.
  </action>
  <verify>
    <automated>uv run ty check app/services/engine.py && uv run ruff check app/services/engine.py && grep -c "STOCKFISH_POOL_SIZE=4 to use all 4 vCPUs" app/services/engine.py</automated>
  </verify>
  <acceptance_criteria>
    - The measured per-worker RSS and the N-workers × footprint + import + Postgres vs 4g accounting appear as a comment block above the pool-size constants in engine.py.
    - `grep -c "all 4 vCPUs" app/services/engine.py` returns 0 (stale comment corrected).
    - The SUMMARY records the actual measured RSS numbers (1-worker and N-worker totals) and the headroom under 4g.
    - `_DEFAULT_POOL_SIZE` is unchanged (still 1).
    - `uv run ty check app/services/engine.py` and `uv run ruff check app/services/engine.py` are clean.
  </acceptance_criteria>
  <done>Real RSS is measured, the 4g accounting is documented next to the pool constants, and the stale engine.py comments are corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Correct stale pool-size comments in docker-compose.yml + CLAUDE.md</name>
  <files>docker-compose.yml, CLAUDE.md</files>
  <read_first>
    - docker-compose.yml (backend service ~lines 63-83: `STOCKFISH_POOL_SIZE: ${STOCKFISH_POOL_SIZE:-4}` line 72, `mem_limit: 4g` / `memswap_limit: 4g` lines 82-83, the stale "Sized for the CPX42 host (16 GB) with STOCKFISH_POOL_SIZE up to 6" comment line ~79)
    - CLAUDE.md (Production Server section: the OOM-recurrence notes mentioning STOCKFISH_POOL_SIZE being "lowered" — the stale note D-116-13 flags for correction)
    - .planning/phases/116-all-ply-engine-core/116-CONTEXT.md (D-116-12, D-116-13, and the "Specific Ideas" note that the "pool lowered" comment is STALE — prod has run 6 for weeks)
  </read_first>
  <action>
    In docker-compose.yml update the backend memory comment (line ~79) from "with
    STOCKFISH_POOL_SIZE up to 6" to reflect the Phase 116 accounting: 4g backend mem_limit holds
    N node-budget workers (measured RSS from Task 1) + an active import + Uvicorn, with documented
    headroom; current prod pool 6, Phase 116 target 8 contingent on the QUEUE-07 check. Do NOT
    change the `${STOCKFISH_POOL_SIZE:-4}` compose default (that is the in-repo/dev default; prod
    overrides via .env) or the `mem_limit: 4g`. In CLAUDE.md's Production Server section, correct
    the stale "STOCKFISH_POOL_SIZE lowered" note: state that prod has run STOCKFISH_POOL_SIZE=6
    stably for weeks, and add a Phase 116 memory-accounting paragraph mirroring the existing
    `_BATCH_SIZE`/`_HASH_MB` deploy-tuning notes — per-worker RSS at 1M nodes (from Task 1), the
    N × footprint + import + Postgres vs 4g math, and that the bump to 8 is gated on (a) the
    accounting fitting 4g with headroom AND (b) a prod API-latency soak (D-116-13). Keep the
    em-dash style guidance (CLAUDE.md communication rules) — prefer commas/periods.
  </action>
  <verify>
    <automated>grep -c "STOCKFISH_POOL_SIZE up to 6" docker-compose.yml; grep -c "STOCKFISH_POOL_SIZE=6 stably\|run STOCKFISH_POOL_SIZE=6" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STOCKFISH_POOL_SIZE up to 6" docker-compose.yml` returns 0 (stale comment replaced).
    - CLAUDE.md's prod section contains a Phase 116 memory-accounting paragraph and no longer asserts the pool was "lowered" as current state.
    - The compose `${STOCKFISH_POOL_SIZE:-4}` default and `mem_limit: 4g` are unchanged.
    - The new CLAUDE.md prose uses at most one em-dash per paragraph.
  </acceptance_criteria>
  <done>Both docs reflect current prod reality (pool 6) and carry the Phase 116 4g memory accounting; deploy defaults are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human go/no-go on pool size 8 vs 6 (QUEUE-07 deploy decision)</name>
  <action>Pause for blocking human verification of the QUEUE-07 / D-116-13 deploy-time decision. Present the measured RSS numbers and the 4g headroom math from Tasks 1-2, confirm the documented plan is "deploy at pool 6 first, soak prod, bump to 8 only if accounting fits 4g with headroom and latency is clean", and do not proceed until the user approves or directs a correction.</action>
  <what-built>
    Measured per-worker RSS at the 1M-node budget and documented the memory accounting against
    the 4g backend container limit in engine.py, docker-compose.yml, and CLAUDE.md (D-116-12).
    Stale "pool lowered" / "4 vCPUs" comments corrected to current prod reality (6).
  </what-built>
  <how-to-verify>
    QUEUE-07 + D-116-13 are explicitly deploy-time decisions that need a human go/no-go before
    the pool size is raised. Review:
    1. The measured RSS numbers in 116-03-SUMMARY.md and the engine.py accounting comment — does
       N=8 workers × measured footprint + an active import + Postgres fit inside 4g with real
       headroom (research estimate ~3.24 GB / ~0.76 GB headroom; confirm against the actual
       measured per-worker RSS, not the estimate)?
    2. Decide the deploy pool size: per D-116-13, deploy at 6 first, soak prod API p50/p90 +
       container RSS for ~24h, then bump to 8 only if the accounting fits AND latency is clean.
       Confirm the documented plan reflects "6 now, 8 contingent" rather than an unconditional 8.
    3. Confirm the CLAUDE.md prod section accurately states current prod pool is 6 (not 4, not
       "lowered").
  </how-to-verify>
  <resume-signal>Type "approved" (accounting fits 4g, deploy-at-6-then-soak plan is correct) or describe the required correction (e.g. measured RSS too high — stay at 6 permanently, or adjust the headroom math).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker pool RSS → 4g container | N Stockfish workers at 1M nodes + import + Postgres share the backend container memory cgroup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-08 | Denial of Service | pool-size bump to 8 exceeding the 4g backend mem_limit → OOM-kill (the import-era failure class) | mitigate | QUEUE-07: measure real per-worker RSS (Task 1), document N × footprint + import + Postgres vs 4g headroom, and gate the bump to 8 behind the human checkpoint + prod latency soak (D-116-13). Deploy at 6 first. |
| T-116-09 | Repudiation | stale docs misleading a future deploy into an unsafe pool size | mitigate | Correct the "pool lowered" / "4 vCPUs" / "up to 6" stale comments across engine.py, docker-compose.yml, CLAUDE.md to current measured reality. |
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH: none to audit). |
</threat_model>

<verification>
- `uv run ty check app/services/engine.py` zero errors; `uv run ruff check app/services/engine.py` clean.
- `grep -c "all 4 vCPUs" app/services/engine.py` = 0; `grep -c "STOCKFISH_POOL_SIZE up to 6" docker-compose.yml` = 0.
- 116-03-SUMMARY.md records measured RSS + the 4g headroom math + the deploy-at-6-then-soak plan.
- Human checkpoint approved.
</verification>

<success_criteria>
- Per-worker RSS at 1M nodes is measured (not estimated) and the N × footprint + import + Postgres vs 4g accounting is documented next to the pool constants and in CLAUDE.md (QUEUE-07 / D-116-12).
- Pool-size bump to 8 is gated on the accounting fitting 4g with headroom AND a prod latency soak; deploy is at 6 first (D-116-13).
- All three stale pool-size comments are corrected to current prod reality.
</success_criteria>

<output>
Create `.planning/phases/116-all-ply-engine-core/116-03-SUMMARY.md` when done. Record the
measured per-worker and N-worker RSS, the 4g headroom math, and the go/no-go decision on pool
size 8 vs 6.
</output>
