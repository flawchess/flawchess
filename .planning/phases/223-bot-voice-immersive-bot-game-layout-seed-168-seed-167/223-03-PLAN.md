---
phase: 223-bot-voice-immersive-bot-game-layout-seed-168-seed-167
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/BoardSoundsSwitch.tsx
  - frontend/src/components/settings/__tests__/BoardSoundsSwitch.test.tsx
  - frontend/src/App.tsx
  - frontend/src/App.test.tsx
autonomous: true
requirements: [BOTVOICE-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A 'Board sounds' switch exists on a settings surface reachable on BOTH breakpoints: the mobile More drawer and the desktop header's account area (SEED-167, SC7)."
    - "The switch's polarity is honest: switching it ON unmutes, switching it OFF mutes, against the same flat localStorage preference the in-game button used, so a user who muted before this phase stays muted."
    - "The switch lands in the same wave as, or earlier than, the removal of the last in-game mute control, so the product never ships a state with no way to unmute."
    - "No new route, no settings page and no settings sheet is introduced (deferred idea, explicitly out of scope)."
  artifacts:
    - frontend/src/components/settings/BoardSoundsSwitch.tsx
    - frontend/src/components/settings/__tests__/BoardSoundsSwitch.test.tsx
  key_links:
    - "`sounds.useMuted` / `sounds.setMuted` (the `MUTE_KEY` localStorage preference) -> `BoardSoundsSwitch` -> `MobileMoreDrawer` and `NavHeader` in `App.tsx`."
  prohibitions:
    - statement: "The phase never ships a state in which board sounds cannot be unmuted."
      status: resolved
      verification: explicit
    - statement: "No new route, settings page or settings sheet is introduced by this phase."
      status: resolved
      verification: explicit
    - statement: "The stored mute preference keeps its exact key and semantics, so an existing muted user stays muted."
      status: resolved
      verification: explicit
    - statement: "No new npm package is installed in this phase."
      status: resolved
      verification: backstop
---

<objective>
Give the board-sound preference a home outside the game, on both breakpoints, before the
in-game mute control is removed.

Purpose: SEED-167 is a hard prerequisite of the layout rework, not a nice-to-have shipped
alongside it. `setMuted` has exactly ONE production call site left in the whole frontend
(the in-game mute button on the bots page); Train's was already removed in Phase 222.
Removing this one without adding the switch first would leave zero ways to mute or unmute
anywhere in the product. This plan lands in wave 1, alongside the tracer and sharing no file with it, so the
removal in wave 3 is always preceded by the replacement.

Output: one small presentational component bound to the existing preference, rendered in
the two account surfaces that already exist. No new route, no settings page.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-CONTEXT.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-RESEARCH.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-PATTERNS.md
@frontend/CLAUDE.md
</context>

## Artifacts this phase produces (plan 03 share)

- `frontend/src/components/settings/BoardSoundsSwitch.tsx` (NEW, new directory):
  `BoardSoundsSwitch`.
- `frontend/src/App.tsx`: the switch rendered in `MobileMoreDrawer` and in `NavHeader`'s
  account area.
- New testids: `settings-board-sounds`.
- New test file: `frontend/src/components/settings/__tests__/BoardSoundsSwitch.test.tsx`.

<tasks>

<task type="auto" tdd="true">
  <name>T-223-03-01: The BoardSoundsSwitch component and its polarity contract</name>
  <files>frontend/src/components/settings/BoardSoundsSwitch.tsx, frontend/src/components/settings/__tests__/BoardSoundsSwitch.test.tsx</files>
  <read_first>
    - `frontend/src/lib/sounds.ts` lines 70-90 (`MUTE_KEY` and the note that the key is flat and therefore guest-safe) and lines 170-205 (`useMuted`'s `useSyncExternalStore` backing and `setMuted`'s try/caught write for private-browsing mode).
    - `frontend/src/components/ui/switch.tsx` in full — the unstyled `{...props}` passthrough with its `cn()` merge; it needs no change.
    - `frontend/src/components/train/TrainScheduleSettings.tsx` lines 230-250 — one of the three shipped `Switch` call sites, showing the `data-testid` + `aria-label` + `checked` + `onCheckedChange` quartet and the control-left / label-right row.
    - `frontend/src/components/bots/GameControls.tsx` lines 115-132 — the in-game mute button this switch replaces, so the new control covers the same preference and nothing more.
    - `frontend/CLAUDE.md` — the testid, `aria-label` and `text-sm`-floor rules.
  </read_first>
  <behavior>
    - With the preference unmuted, the switch renders checked.
    - With the preference muted, the switch renders unchecked.
    - Toggling a checked switch off calls the mute setter with `true`.
    - Toggling an unchecked switch on calls the mute setter with `false`.
    - The control carries an accessible name and a testid, and the visible label reads "Board sounds".
    - The rendered markup contains no sub-`text-sm` font-size utility.
  </behavior>
  <action>
Create the new `frontend/src/components/settings/` directory with one component,
`BoardSoundsSwitch`, returning a single row: a visible "Board sounds" label at the `text-sm`
floor and a `Switch` from `@/components/ui/switch`. It reads `useMuted()` and writes through
`setMuted` from `@/lib/sounds` — never a new store, never a new localStorage key, never a
user-profile field. Watch the inversion: the preference is "muted" and the control reads
"Board sounds", so the switch is checked when the preference is NOT muted and its change
handler inverts the incoming value. Carry `data-testid="settings-board-sounds"` and an
`aria-label` matching the visible label. No props: the component owns nothing the caller
needs to configure, and both call sites render it identically. Keep it well under the global
complexity ceiling of 15.

Write `frontend/src/components/settings/__tests__/BoardSoundsSwitch.test.tsx` (jsdom) covering
every behaviour bullet. Mock `@/lib/sounds` so the assertions are on the setter's argument
rather than on localStorage, and assert the argument VALUE in both directions, since the
inversion is the one thing that can silently ship backwards. Include the font-size invariant
assertion in the form `PersonaGrid.test.tsx` uses. Never add a per-file or per-test timeout.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/components/settings/__tests__/BoardSoundsSwitch.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line</fails_when>
    <automated>( cd frontend && npm run lint && npm run build )</automated>
    <fails_when>non-zero exit, any `error TS` line, or any complexity breach reported for `src/components/settings/BoardSoundsSwitch.tsx`</fails_when>
    <automated>( cd frontend && test "$(grep -c 'localStorage' src/components/settings/BoardSoundsSwitch.tsx)" = "0" )</automated>
    <fails_when>non-zero exit — the component reached for storage directly instead of going through the existing preference helpers, which would bypass the private-browsing guard and the subscription</fails_when>
  </verify>
  <done>A "Board sounds" switch component exists, reads and writes the existing preference with the correct polarity in both directions, and is covered by a test that asserts the setter's argument value.</done>
  <acceptance_criteria>
    - `frontend/src/components/settings/BoardSoundsSwitch.tsx` imports `useMuted` and `setMuted` from `@/lib/sounds` and imports `Switch` from `@/components/ui/switch`.
    - The file contains `data-testid="settings-board-sounds"` and an `aria-label` attribute.
    - `grep -c "text-xs" frontend/src/components/settings/BoardSoundsSwitch.tsx` returns 0.
    - The test file contains one assertion for the setter called with `true` and one for the setter called with `false`, each reached by a toggle from the opposite starting state.
    - `frontend/src/components/settings/BoardSoundsSwitch.tsx` declares no props interface.
  </acceptance_criteria>
  <reversibility rating="reversible">One new component file plus its test; deleting both restores the prior state exactly, and the underlying preference is untouched.</reversibility>
</task>

<task type="auto">
  <name>T-223-03-02: Render the switch in both account surfaces</name>
  <files>frontend/src/App.tsx, frontend/src/App.test.tsx</files>
  <read_first>
    - `frontend/src/App.tsx` lines 330-372 — `NavHeader`'s trailing account `div` with the guest badge, the impersonation pill and `nav-logout`, which is where the desktop switch goes.
    - `frontend/src/App.tsx` lines 555-595 — `MobileMoreDrawer`'s nav list, the divider and the `drawer-logout` button, which is where the mobile switch goes.
    - `frontend/eslint.config.js` lines 118-130 — the entry pinning `src/App.tsx` at complexity 21. Read it; do not edit it.
    - `frontend/src/App.test.tsx` lines 920-1060 — the mobile-bottom-bar describe blocks and their render helpers, the closest shape for new App-level assertions.
    - `frontend/CLAUDE.md` — the "always apply changes to mobile too" rule, which is exactly why this lands in two places.
  </read_first>
  <action>
Render `BoardSoundsSwitch` in both existing account surfaces, adding no branching to either
function.

Mobile: inside `MobileMoreDrawer`, place it above the divider that precedes the logout button,
so the drawer reads as nav items, then preferences, then logout. It is a plain child of the
existing container, not wrapped in a `DrawerClose` — toggling a preference must not dismiss
the drawer.

Desktop: inside `NavHeader`'s trailing account `div`, place it before the logout button so the
account cluster reads as preference then sign-out. Give it whatever spacing class the
surrounding cluster already uses; do not introduce a new layout wrapper.

Extend `frontend/src/App.test.tsx` with one describe block asserting the control is present in
both surfaces: rendered in the header for a signed-in profile, and rendered inside the opened
More drawer. Assert presence by testid only; the polarity is already pinned by the component's
own test, and duplicating it here would couple two files to one behaviour.

`frontend/eslint.config.js` must not be edited. Re-measure both functions with
`npx eslint --no-inline-config --rule 'complexity: ["error", 1]' src/App.tsx` if lint complains,
and record the numbers in the SUMMARY.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/App.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line, or an assertion naming the `settings-board-sounds` testid reports "Unable to find an element"</fails_when>
    <automated>( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 21]' src/App.tsx )</automated>
    <fails_when>non-zero exit, or any output line containing "has a complexity of" — a new branch was added to a function in a file pinned at its measured ceiling</fails_when>
    <automated>( cd frontend && npm run lint && npm run build ) && git diff --quiet "$(git merge-base HEAD main)"..HEAD -- frontend/eslint.config.js</automated>
    <fails_when>non-zero exit from eslint or tsc, any `error TS` line, or a non-empty diff on frontend/eslint.config.js</fails_when>
    <automated>( cd frontend && test "$(grep -c 'BoardSoundsSwitch' src/App.tsx)" = "3" )</automated>
    <fails_when>non-zero exit because the count is not exactly 3 (one import plus two render sites) — the switch reached only one breakpoint, which the "always apply changes to mobile too" rule forbids</fails_when>
  </verify>
  <done>The "Board sounds" switch is reachable from the mobile More drawer and from the desktop header account area; `App.tsx` gained no branch and `eslint.config.js` is unchanged.</done>
  <acceptance_criteria>
    - `frontend/src/App.tsx` contains exactly one import of `BoardSoundsSwitch` and exactly two `<BoardSoundsSwitch` render sites.
    - The mobile render site is NOT wrapped in a `DrawerClose` element.
    - `frontend/src/App.test.tsx` contains an assertion for `settings-board-sounds` in the header render and another in the opened-drawer render.
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- frontend/eslint.config.js` is empty.
    - `npm run knip` reports no unused export for `BoardSoundsSwitch`.
  </acceptance_criteria>
  <reversibility rating="reversible">Two render sites and one import; a git revert restores both surfaces exactly.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> app | The mute preference is read from and written to a flat, unscoped browser key. |
| npm registry -> repository | Any new package would cross an unaudited boundary. This plan installs none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-223-03-01 | Tampering | a hand-edited mute preference in localStorage | low | accept | The worst outcome is that board sounds play or do not play. The value is read through the existing helper, whose parse is a string equality against one known value, so any other content resolves to "not muted". No authorization or scoring decision reads it. |
| T-223-03-02 | Information Disclosure | the preference key being unscoped across accounts on a shared browser | low | accept | Deliberate and pre-existing: the key is flat specifically so it works for guests, as the shipped module documents. It carries no personal data. This plan adds a second reader of the same key and does not change its scope. |
| T-223-03-03 | Denial of Service | a private-browsing storage write throwing | low | accept | Already handled upstream: the setter's write is wrapped in a try/catch, so a blocked write degrades to a session-only preference rather than an error. This plan adds no new write path. |
| T-223-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs nothing (223-RESEARCH.md "Package Legitimacy Audit: not applicable"). Enforced by a lockfile-diff gate at plan close. |

Supply-chain gate (discharges T-223-03-SC): `git diff --exit-code "$(git merge-base HEAD main)"..HEAD -- frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` must exit 0 at plan close. A non-empty diff is a blocking finding, not a warning.
</threat_model>

<verification>
- `( cd frontend && npm run lint && npm run build && npm test -- --run )` — the plan's own full frontend gate.
- `( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 21]' src/App.tsx )` exits 0.
- `git diff --exit-code "$(git merge-base HEAD main)"..HEAD -- frontend/eslint.config.js frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` exits 0.
- No `bin/reset_db.sh` invocation anywhere in this plan; no backend file is touched.
</verification>

<success_criteria>
- Board sounds can be muted and unmuted from the mobile More drawer and from the desktop header account area.
- The polarity is asserted in both directions by a test on the setter's argument.
- The existing preference key and semantics are unchanged, so a previously muted user stays muted.
- No new route, settings page or settings sheet exists.
</success_criteria>

<output>
Create `.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-03-SUMMARY.md` when done.
</output>
</content>
