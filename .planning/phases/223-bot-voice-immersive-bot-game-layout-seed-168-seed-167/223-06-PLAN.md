---
phase: 223-bot-voice-immersive-bot-game-layout-seed-168-seed-167
plan: 06
type: execute
wave: 4
depends_on: ["223-04", "223-05"]
files_modified:
  - frontend/src/components/board/PlayerBar.tsx
  - frontend/src/components/board/__tests__/PlayerBar.test.tsx
  - frontend/src/components/bots/BotGameDesktopLayout.tsx
  - frontend/src/components/bots/ClockDisplay.tsx
  - frontend/src/components/bots/__tests__/ClockDisplay.test.tsx
  - frontend/src/pages/Bots.tsx
  - frontend/src/pages/__tests__/Bots.test.tsx
  - frontend/src/lib/theme.ts
  - frontend/src/lib/chessClock.ts
  - CHANGELOG.md
files_deleted:
  - frontend/src/components/bots/ClockDisplay.tsx
  - frontend/src/components/bots/__tests__/ClockDisplay.test.tsx
autonomous: false
requirements: [BOTVOICE-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Desktop renders a player row above and below the board, each carrying name and ELO on the left and clock and material on the right, and the bubble sits at the top of the side column above the move list (D-11, SC5)."
    - "The bot's ELO renders as its honest tilde-prefixed calibrated label, never as a parenthesised integer and never as a rung number; the player's renders as the rounded tilde-prefixed estimate and is omitted entirely for a guest (D-11)."
    - "The bot's draw offer is accepted or declined from inside the bubble on desktop as well as on mobile, with both original testids (D-12, SC5)."
    - "The retired in-game clock card and its test are deleted, its only production caller is gone, and the dead-export gate is green."
    - "`PlayerBar`'s new prop is purely additive: all four existing render sites and their tests compile and pass untouched."
    - "`BotsGame`'s measured cyclomatic complexity is at or below the 25 pinned in `frontend/eslint.config.js`, with that file unedited, and the number is recorded."
    - "The phase has a user-facing changelog entry under the unreleased heading (root CLAUDE.md: the changelog is not optional)."
  artifacts:
    - frontend/src/components/bots/BotGameDesktopLayout.tsx
    - CHANGELOG.md
  key_links:
    - "`BotsPage.useUserProfile()` -> `BotsGame`'s new `currentStrength` prop -> `BotGameDesktopLayout` -> the user row's rating label."
    - "`persona.calibratedLabel` -> `PlayerBar.ratingLabel` on the bot row, winning over the numeric rating prop."
    - "`useBotGame.botDrawOffer` -> `BotDrawOfferActions` -> the side-column bubble's actions slot."
  prohibitions:
    - statement: "The bot's estimated ELO is never rendered from its rung number; only the generated calibrated label is honest enough to show."
      status: resolved
      verification: explicit
    - statement: "No existing `PlayerBar` call site is modified to accommodate the new prop."
      status: resolved
      verification: explicit
    - statement: "No eslint complexity ceiling is widened or re-baselined."
      status: resolved
      verification: explicit
    - statement: "No in-game mute control is reintroduced on the desktop breakpoint."
      status: resolved
      verification: explicit
    - statement: "No timer, transition or animation is added to the bubble on the desktop breakpoint."
      status: resolved
      verification: explicit
    - statement: "No new npm package is installed in this phase."
      status: resolved
      verification: backstop
---

<objective>
Finish the rework on desktop: two player rows around the board, the bubble at the top of
the side column, the draw offer inside it, the retired clock card deleted. Then close the
phase with a changelog entry and one consolidated human verification pass.

Purpose: desktop is the second half of SC5 and the last surface still rendering the old
clock cards. Once it lands, the retired card has no caller and must be deleted or the
dead-export gate fails. This plan also carries the two things that can only be settled by a
human with a browser: the 375px board-width claim behind SC4, and the calibration of the
two-line copy budget, which no test in jsdom can measure.

Output: the additive rating-label prop, the desktop layout component, the deletion, the
changelog bullet, and a concrete UAT checklist.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-CONTEXT.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-RESEARCH.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-PATTERNS.md
@.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-01-PLAN.md
@CLAUDE.md
@frontend/CLAUDE.md
</context>

## Artifacts this phase produces (plan 06 share)

- `frontend/src/components/board/PlayerBar.tsx`: additive optional `ratingLabel?: string`
  that wins over the numeric `rating` when present.
- `frontend/src/components/bots/BotGameDesktopLayout.tsx` (NEW): `BotGameDesktopLayout`.
- `frontend/src/pages/Bots.tsx`: `BotsGameProps` gains `currentStrength`; the desktop render
  helper is replaced.
- Deleted: `frontend/src/components/bots/ClockDisplay.tsx` and
  `frontend/src/components/bots/__tests__/ClockDisplay.test.tsx`.
- `CHANGELOG.md`: user-facing bullets under the unreleased heading.

<tasks>

<task type="auto" tdd="true">
  <name>T-223-06-01: The additive rating-label prop on the shared player row</name>
  <files>frontend/src/components/board/PlayerBar.tsx, frontend/src/components/board/__tests__/PlayerBar.test.tsx</files>
  <read_first>
    - `frontend/src/components/board/PlayerBar.tsx` in full (85 lines) — the props interface and the doc-comment convention every optional prop follows (naming the phase and why it is optional), plus the numeric rating render in parentheses.
    - `frontend/src/lib/personas/personaRegistry.ts` lines 88-100 — `calibratedLabel` and the registry's explicit statement that it, never a rung expression, is what the persona surfaces render.
    - `frontend/src/components/bots/PersonaGrid.tsx` lines 118-135 — the rounded tilde-prefixed user estimate, the exact expression the user row reuses.
    - `frontend/src/components/board/__tests__/PlayerBar.test.tsx` in full — the existing cases that must keep passing untouched.
    - `frontend/src/pages/Analysis.tsx`, `frontend/src/components/analysis/AnalysisPlayerBar.tsx`, `frontend/src/components/analysis/AnalysisBoardStage.tsx` and `frontend/src/components/analysis/AnalysisTabs.tsx` — the four existing render sites, read only to confirm none of them needs a change.
  </read_first>
  <behavior>
    - With only the numeric rating supplied, the row renders exactly as it does today, in parentheses.
    - With the string label supplied, the row renders the label and NOT the parenthesised number, even when both are supplied.
    - With neither supplied, the row renders the name alone with no empty parentheses and no stray separator.
    - The label is rendered as plain text with the same muted treatment the numeric rating already uses, so the two forms are visually consistent.
  </behavior>
  <action>
Add one optional prop, `ratingLabel?: string`, to the shared player row. Its doc comment
follows the file's convention: name the phase and requirement, state that it exists because
a bot's estimate is an honest tilde-prefixed string rather than an integer, and state that
it wins over the numeric prop when both are present. Render it in the same span and with the
same muted class the numeric form uses, so no new styling enters the component.

Additive means additive: none of the four existing render sites and none of the existing
test cases may change. Verify that by leaving those files untouched and letting the type
check and the existing suite prove it.

Extend `frontend/src/components/board/__tests__/PlayerBar.test.tsx` with the four behaviour
bullets above. Never add a per-file or per-test timeout.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/components/board/__tests__/PlayerBar.test.tsx src/pages/__tests__/Analysis.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line, or any pre-existing analysis-board assertion turning red, which would mean the prop was not additive</fails_when>
    <automated>( cd frontend && git diff --quiet "$(git merge-base HEAD main)"..HEAD -- src/pages/Analysis.tsx src/components/analysis/AnalysisPlayerBar.tsx src/components/analysis/AnalysisBoardStage.tsx src/components/analysis/AnalysisTabs.tsx )</automated>
    <fails_when>non-zero exit — an existing render site was modified, so the prop was not additive after all</fails_when>
    <automated>( cd frontend && npm run lint && npm run build )</automated>
    <fails_when>non-zero exit, any `error TS` line, or any complexity breach reported for `src/components/board/PlayerBar.tsx`</fails_when>
  </verify>
  <done>The shared player row can render an honest tilde-prefixed estimate; all four existing call sites and their tests are untouched and green.</done>
  <acceptance_criteria>
    - `frontend/src/components/board/PlayerBar.tsx` declares `ratingLabel?: string` with a doc comment naming the phase and the win-over rule.
    - `git diff "$(git merge-base HEAD main)"..HEAD --stat -- frontend/src/pages/Analysis.tsx frontend/src/components/analysis/AnalysisPlayerBar.tsx frontend/src/components/analysis/AnalysisBoardStage.tsx frontend/src/components/analysis/AnalysisTabs.tsx` is empty.
    - The test file contains a case supplying BOTH the numeric rating and the string label and asserting the numeric form is absent from the rendered text.
    - `grep -c "text-xs" frontend/src/components/board/PlayerBar.tsx` returns 0.
  </acceptance_criteria>
  <reversibility rating="reversible">One optional prop on a shared component with no call-site changes; a revert is a one-file diff.</reversibility>
</task>

<task type="auto">
  <name>T-223-06-02: The desktop layout — player rows, side-column bubble, and the retired clock card deleted</name>
  <files>frontend/src/components/bots/BotGameDesktopLayout.tsx, frontend/src/pages/Bots.tsx, frontend/src/components/bots/ClockDisplay.tsx, frontend/src/components/bots/__tests__/ClockDisplay.test.tsx, frontend/src/pages/__tests__/Bots.test.tsx, frontend/src/lib/theme.ts, frontend/src/lib/chessClock.ts</files>
  <read_first>
    - `frontend/src/pages/Bots.tsx` lines 145-200 (the desktop render helper and its doc comment on the two stacked rows, the stretched side column and the shared widths) and 425-545 (the pre-built elements and the two call sites), as plan 05 left them.
    - `frontend/src/components/bots/ClockDisplay.tsx` in full — everything worth keeping (the avatar idiom, the active-side highlight, the low-time treatment) has already moved into the bubble and the clock strip; read it once to confirm nothing else is load-bearing before deleting it.
    - `frontend/src/components/bots/__tests__/ClockDisplay.test.tsx` in full before deleting it, so any assertion worth keeping moves into the player-row or clock-strip suites rather than being lost.
    - `frontend/src/components/board/PlayerBar.tsx` as the previous task left it.
    - `frontend/src/pages/Bots.tsx` lines 600-640 — `BotsPage`'s single profile call and the existing prop-drill of the guest flag and the player name, the pattern the strength estimate follows.
    - `frontend/src/pages/__tests__/Bots.test.tsx` lines 255-275 — the matchMedia stub reporting a non-matching query, which is why a desktop-mode describe needs its own override.
    - `frontend/src/lib/theme.ts` line 501 and `frontend/src/lib/chessClock.ts` line 14 — the two doc comments that name the deleted component as the current owner of a token or a helper and therefore go stale.
  </read_first>
  <action>
1. `frontend/src/components/bots/BotGameDesktopLayout.tsx` (NEW), replacing the page's desktop
   render helper. Keep the shipped two-stacked-rows shape and the shared board-column and
   side-column widths, and keep the doc comment explaining why the side column stretches to
   the board's height. Inside it:
   - The board column gets a player row above and a player row below the board, ordered by
     the current board orientation so the side facing away from the user is on top. The bot
     row carries the persona name and the persona's generated calibrated label through the
     new string prop. The user row carries the resolved player name and, when a strength
     estimate is present, the rounded tilde-prefixed estimate through the same prop; for a
     guest with no estimate it renders neither prop, so no empty parentheses appear. Both
     rows get the viewed-ply FEN so material tracks the board during scroll-back, and the
     mover's remaining time in the unit the shared row expects. Reuse the shipped clock-card
     testids on the two rows so the existing page assertions keep matching after the move.
   - The side column gets the avatar-and-bubble row at the TOP, above the move list, with the
     draw-offer action pair passed into the bubble's actions slot so the offer is accepted or
     declined from the bubble on this breakpoint too.
   - The second row keeps the shared board-control row under the board and the resign-only
     control row under the side column. Reset stays on this breakpoint: the locked decision
     removes it from the four-action phone bar, not from the desktop control row.
   - This component owns every branch the layout needs, including the orientation ordering and
     the guest gate, precisely so the page component gains none.

2. `frontend/src/pages/Bots.tsx`: add a `currentStrength` prop to `BotsGameProps` with a doc
   comment naming the single-fetch-then-prop-drill pattern and stating it comes from the
   page's existing profile call, never a second hook call. Pass it from `BotsPage`. Replace
   the desktop render helper with the new component and delete the helper, the two clock-card
   elements and the clock-card import. Add NO new branch to the `BotsGame` function body;
   re-measure and record the number.

3. Delete `frontend/src/components/bots/ClockDisplay.tsx` and
   `frontend/src/components/bots/__tests__/ClockDisplay.test.tsx`. Its only production caller
   is gone, and the dead-export gate runs in CI. Update the two doc comments that name it as
   the current owner of a theme token and a clock helper so they point at the components that
   own those concerns now; leave purely historical mentions elsewhere alone.

4. `frontend/src/pages/__tests__/Bots.test.tsx`: add a desktop-mode describe block with its
   own matchMedia override reporting a matching query, asserting: two player rows render with
   the bot's name and its calibrated label and the player's name and rounded estimate; a guest
   profile renders the user row with a name and no rating text; the bubble renders at the top
   of the side column; the draw offer's two buttons render inside the bubble and call through;
   and no in-page mute control exists on this breakpoint either. These replace the mobile
   clock-label assertions plan 05 deleted.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/pages/__tests__/Bots.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line, or an assertion naming `clock-bot`, `clock-user`, `btn-accept-bot-draw` or `btn-decline-bot-draw` reports "Unable to find an element"</fails_when>
    <automated>( cd frontend && test ! -e src/components/bots/ClockDisplay.tsx && test ! -e src/components/bots/__tests__/ClockDisplay.test.tsx )</automated>
    <fails_when>non-zero exit — the retired clock card or its test still exists on disk, which the dead-export gate will also flag</fails_when>
    <automated>( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 25]' src/pages/Bots.tsx )</automated>
    <fails_when>non-zero exit, or any output line containing "has a complexity of" — a new branch entered a function in the page that sits at its pinned ceiling</fails_when>
    <automated>( cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run ) && git diff --quiet "$(git merge-base HEAD main)"..HEAD -- frontend/eslint.config.js</automated>
    <fails_when>non-zero exit from any leg, any `error TS` line, any "Unused export" line, or a non-empty diff on frontend/eslint.config.js</fails_when>
    <human-check>On a desktop-width browser at /bots, start a persona game and confirm by eye: the bot's row shows its name with a tilde-prefixed estimate and never a parenthesised integer; the player's row shows the resolved display name; the bubble sits above the move list and its copy does not clip at two lines; the side column's bottom still lines up with the board's bottom.</human-check>
  </verify>
  <done>Desktop shows a player row above and below the board with honest ELO labels, the bubble at the top of the side column with the draw offer inside it, and the retired clock card is gone from the tree.</done>
  <acceptance_criteria>
    - `grep -rl "ClockDisplay" frontend/src` returns only files whose matches are inside comments, and no file importing it.
    - `frontend/src/components/bots/BotGameDesktopLayout.tsx` contains exactly two `<PlayerBar` occurrences and exactly one `<BotGameBubble` occurrence.
    - `frontend/src/components/bots/BotGameDesktopLayout.tsx` passes `ratingLabel` on the bot row sourced from the persona's `calibratedLabel`, and contains no `rung` reference.
    - `frontend/src/pages/Bots.tsx` contains exactly one `<BotGameDesktopLayout` occurrence and no `renderDesktopLayout` definition.
    - `frontend/src/pages/Bots.tsx` contains `currentStrength` in `BotsGameProps` and exactly one `useUserProfile(` call in the whole file.
    - `frontend/src/pages/__tests__/Bots.test.tsx` contains a desktop-mode describe with its own matchMedia override and a guest case asserting the user row has a name and no rating text.
    - The measured `BotsGame` complexity after this task is recorded in the plan SUMMARY.
  </acceptance_criteria>
  <reversibility rating="reversible">A layout swap, one additive prop drill and a deletion of a component with a single caller; all git-tracked source, no persisted state.</reversibility>
</task>

<task type="auto">
  <name>T-223-06-03: Changelog entry and the full frontend gate</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-25 — the unreleased heading, its existing entries from the previous phase, and the user-facing register those bullets are written in (no file names, no requirement ids, no internal vocabulary).
    - `CLAUDE.md` section "Version Control" — the rule that the changelog is not optional and that bullets are appended when the phase merges.
    - `CLAUDE.md` section "Communication Style" — the em-dash rule, which applies to changelog prose.
  </read_first>
  <action>
Append user-facing bullets under the unreleased heading, in the existing register: what a
user notices, never how it was built. Cover, in whatever grouping the existing headings
support: the bots now speak in their own voices during a game and on the roster; the phone
game screen is rebuilt around the board with a four-action bar replacing the main nav; the
bot's draw offer is now accepted or declined from the bot's own speech bubble; the board
sounds toggle has moved out of the game onto the account menu on both breakpoints. No
em-dashes, no requirement ids, no file names.

Then run the full frontend leg of the project's pre-merge gate and fix anything it reports.
Do not run a database reset and do not touch any backend file: this phase has no backend
surface at all.
  </action>
  <verify>
    <automated>( cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run )</automated>
    <fails_when>non-zero exit from any leg, any `error TS` line, any "Unused export" line, or any red test in the suite summary</fails_when>
    <automated>test "$(awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -ciE 'bot|sound|board')" -ge 4</automated>
    <fails_when>non-zero exit because fewer than four bullets describing this phase's user-facing changes were appended under the unreleased heading</fails_when>
    <automated>test "$(awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c "$(printf '—')")" = "0"</automated>
    <fails_when>non-zero exit because a long dash reached the changelog prose, which the project's communication rules forbid</fails_when>
    <automated>git diff --exit-code "$(git merge-base HEAD main)"..HEAD -- frontend/package.json frontend/package-lock.json pyproject.toml uv.lock frontend/eslint.config.js</automated>
    <fails_when>non-zero exit — a dependency was added or a complexity baseline was widened somewhere in this phase</fails_when>
  </verify>
  <done>The changelog carries this phase's user-facing bullets and the full frontend gate is green with no dependency or baseline drift.</done>
  <acceptance_criteria>
    - `CHANGELOG.md`'s unreleased section contains at least four new bullets, one per user-facing change listed in the action.
    - No bullet names a file, a component, a requirement id or a decision id.
    - No em-dash appears anywhere in the unreleased section.
    - `git diff --exit-code "$(git merge-base HEAD main)"..HEAD --` on the two lockfiles, the two manifests and the eslint baseline exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-223-06-04: Phase 223 human verification pass — board width, copy budget, both breakpoints, guest roster</name>
  <what-built>
Phase 223 complete in the working tree: per-persona bot lines in a speech bubble during a
game, the rebuilt phone game screen with its four-action bar, the desktop player rows and
side-column bubble, the roster welcome bubble, and the board-sounds switch on the account
surfaces. The full frontend gate (lint, type check, dead-export check, test suite) is green.
Start the dev server before handing this over, and have the 375x667 emulation ready.
  </what-built>
  <how-to-verify>
1. Mobile board width (SC4). At 375x667, open a bot game and read the rendered board
   element's width. Compare it against the same reading taken on the pre-phase build: use
   the deployed site for the baseline, or a checkout of the integration branch if the
   deployed build is behind on this page. The new number must be strictly larger. If it is
   not, the levers in order are: shrink the in-game avatar size constant, trim the page
   root's vertical padding, or switch the board's height fit off and let the page scroll.
2. Copy budget calibration (SC1). At 375px, render the LONGEST authored line in the bubble
   and confirm it wraps to at most two lines with no clipping and no slot growth. If it
   overflows, lower `BOT_LINE_MAX_CHARS` to the measured value, update its doc comment with
   the new calibration, and tighten every line that now exceeds it. If it is comfortably
   short, raise the constant to the measured value rather than leaving the budget wrong.
3. Mobile screen shape (SC4). At 375x667 confirm: only a back arrow on top, no gear; the
   avatar and bubble row above the board; no bot name, style or estimated ELO anywhere; no
   player name; the clock strip below the board with white left, black right and material
   beside each clock; the fixed bar showing exactly Resign, Back, Forward and Flip and no
   main nav; no vertical scrolling; the last rank of the board never hidden behind the bar.
4. Desktop shape (SC5). At a desktop width confirm: a player row above and below the board
   with the bot's tilde-prefixed estimate and the player's, the bubble above the move list,
   and the resign control under the side column.
5. Draw offer on both breakpoints (SC5). Play until the bot offers a draw, or force the
   offer, and confirm the offer copy and the Accept and Decline buttons render inside the
   bubble and work on both breakpoints. Repeat once with a Custom game so the persona-less
   fallback is exercised.
6. Line frequency and tone (SC1, SC2, SC3). Play one full game and count the lines. Expect
   at most a couple of swing lines. Confirm every line that fires is obviously visible on the
   board, that none reads as mean, and that none claims the bot foresaw anything. If the bot
   is chatty, raise the swing threshold constant; if silent across several games, lower it.
7. Roster (SC6). Confirm the page opens with the welcome bubble, the engine explainer
   popover still opens from inside the bubble copy, and the estimated-rating row sits
   beneath it. Then open the page in a guest session and confirm the bubble renders with no
   rating row.
8. Sound switch (SC7). Toggle it from the mobile More drawer and from the desktop header
   account area, confirm board sounds actually stop and start, confirm the preference
   survives a reload, and confirm no mute control exists anywhere inside a game.
  </how-to-verify>
  <resume-signal>Type "approved", or list the items that failed with the measured numbers so the threshold and budget constants can be retuned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user profile -> desktop player row | The player's estimated rating crosses from the page's profile query into a rendered row. |
| generated calibration data -> bot player row | The persona's estimated ELO label comes from a generated, committed calibration file. |
| npm registry -> repository | Any new package would cross an unaudited boundary. This plan installs none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-223-06-01 | Information Disclosure | the user's estimated rating on the desktop player row | low | mitigate | The value is the same one the roster card already renders to the same user from the same single profile query. It is the viewer's own estimate, never another account's, and the guest gate keeps it absent when there is nothing to show. No second fetch is introduced. |
| T-223-06-02 | Spoofing | presenting a bot's strength dishonestly | medium | mitigate | The calibration-honesty rule is the control: the row renders the generated calibrated label and never a rung expression, which the acceptance criteria pin by forbidding a rung reference in the layout component. A parenthesised integer would misrepresent a calibrated estimate as an exact rating. |
| T-223-06-03 | Tampering | deleting a shared component with a live caller | low | mitigate | The dead-export gate and the type check both run in this plan's verify, and the retired card's only production caller is removed in the same task. The existence checks make the deletion explicit rather than incidental. |
| T-223-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs nothing (223-RESEARCH.md "Package Legitimacy Audit: not applicable"). Enforced by a lockfile-diff gate in this plan's own verify. |

Supply-chain gate (discharges T-223-06-SC): `git diff --exit-code "$(git merge-base HEAD main)"..HEAD -- frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` must exit 0 at plan close. A non-empty diff is a blocking finding, not a warning.
</threat_model>

<verification>
- `( cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run )` — the full frontend leg of the project's pre-merge gate.
- `( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 25]' src/pages/Bots.tsx )` exits 0.
- `git diff --exit-code "$(git merge-base HEAD main)"..HEAD -- frontend/eslint.config.js frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` exits 0.
- The human verification checklist above is completed and its measured numbers recorded.
- No `bin/reset_db.sh` invocation anywhere in this plan; no backend file is touched.
</verification>

<success_criteria>
- Desktop renders a player row above and below the board with honest tilde-prefixed estimates, and the bubble at the top of the side column.
- The bot's draw offer is accepted or declined from inside the bubble on both breakpoints, including for a Custom game.
- The retired in-game clock card and its test are deleted and the dead-export gate is green.
- The changelog carries this phase's user-facing bullets.
- The 375px board is measurably wider than before the phase, and the two-line copy budget is calibrated against a real browser.
</success_criteria>

<output>
Create `.planning/phases/223-bot-voice-immersive-bot-game-layout-seed-168-seed-167/223-06-SUMMARY.md` when done.
Record: the measured `BotsGame` complexity, the before and after 375px board widths, the
calibrated value of the copy budget constant, and the final value of the swing threshold
after the play-through.
</output>
</content>
