---
phase: 104-library-page-shell-import-overview-subtab-migration
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/Home.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/openings/GamesTab.tsx
autonomous: true
requirements: [LIB-01, LIB-03, LIB-04, LIB-05, LIB-06, LIB-07, LIB-08, LIB-09]
must_haves:
  truths:
    - "Top-level nav shows Library · Openings · Endgames (+ superuser Admin); Import and Overview are gone as standalone nav items (desktop, mobile bottom bar, More drawer)"
    - "The Library nav item uses the lucide FolderOpen icon in all three nav surfaces (D-01)"
    - "The totalGames === 0 notification dot renders on the Library nav item (not Import) in all three nav surfaces (LIB-06)"
    - "Visiting /import redirects to /library/import; /overview, /rating, /global-stats redirect to /library/overview"
    - "The /library route is never wrapped in ImportRequiredRoute; Openings and Endgames stay route-gated exactly as before (LIB-08)"
    - "Home's gameless-user redirect targets /library/import; the has-games redirect stays /openings (D-05)"
    - "Internal Import Games links in Endgames.tsx and openings/GamesTab.tsx, and the ImportRequiredRoute redirect target, point at /library/import"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Library route (/library/*) with job props, old-route redirects, NAV_ITEMS/BOTTOM_NAV_ITEMS/ROUTE_TITLES/isActive updates, nav-lock exemption + notification-dot move across all three nav surfaces"
      contains: "/library"
    - path: "frontend/src/pages/Home.tsx"
      provides: "gameless redirect repointed to /library/import"
      contains: "/library/import"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/library/LibraryPage.tsx"
      via: "<Route path=\"/library/*\" element={<LibraryPage ... />}>"
      pattern: "LibraryPage"
    - from: "frontend/src/App.tsx"
      to: "/library/import and /library/overview"
      via: "<Navigate ... replace /> redirects from /import, /overview, /rating, /global-stats"
      pattern: "Navigate to=\"/library/(import|overview)\""
---

<objective>
Repoint the entire FlawChess router and navigation at the new Library shell created in Plan 01. Replace the standalone Import + Overview nav items with a single FolderOpen Library item across desktop nav, mobile bottom bar, and the mobile More drawer; move the zero-games notification dot to Library; add the `/library/*` route (carrying the App-owned import-job props) plus redirects from every old entry point; and sweep the internal `Import Games` links and the ImportRequiredRoute redirect target to `/library/import`.

Purpose: Closes LIB-01, LIB-05, LIB-06, LIB-07 (Home redirect), LIB-08 (gating preserved) and completes the route migration for LIB-03/LIB-04. After this plan the migration is observable end-to-end: every old URL and nav affordance lands on the Library shell with identical Import/Overview behavior.

Output: a fully rewired `App.tsx` (nav config, route titles, isActive, nav-lock exemption, notification dot ×3 surfaces, routes + redirects, guard redirect target, dead-import cleanup), a one-line `Home.tsx` redirect change, and two one-line internal-link sweeps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/104-library-page-shell-import-overview-subtab-migration/104-CONTEXT.md
@.planning/phases/104-library-page-shell-import-overview-subtab-migration/104-PATTERNS.md
@.planning/phases/104-library-page-shell-import-overview-subtab-migration/104-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire App.tsx nav config, routes, redirects, guard, and notification dot</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (the whole nav/router file — verified line anchors: lucide import 13, ImportPage import 25, GlobalStatsPage import 29, NAV_ITEMS/BOTTOM_NAV_ITEMS 59-71, ROUTE_TITLES 79-85, isActive 89-93, NavHeader lock 128 + dot 147-155, MobileBottomBar lock 264 + dot 281-289, MobileMoreDrawer 326-367 lock 348 (NO dot currently), ImportRequiredRoute redirect 479, AppRoutes job-state owner + handlers 505/539-571, routes block 588-596)
    - frontend/src/pages/library/LibraryPage.tsx (created in Plan 01 — the component App now routes to and the three props it accepts)
    - .planning/phases/104-library-page-shell-import-overview-subtab-migration/104-PATTERNS.md (§ App.tsx — every verified edit site with exact current/target snippets; § No Analog Found — the net-new MobileMoreDrawer dot)
    - .planning/phases/104-library-page-shell-import-overview-subtab-migration/104-UI-SPEC.md (§ Nav Structure, § Nav-Lock Exemption, § isActive Helper, § Notification Dot, § Route Redirects, § ROUTE_TITLES Update, § data-testid/ARIA Contract)
    - .planning/phases/104-library-page-shell-import-overview-subtab-migration/104-CONTEXT.md (D-01, D-05, D-06, D-08, D-09, D-10, D-11, D-12, D-14)
  </read_first>
  <action>
    Import: add `FolderOpen` to the `lucide-react` import at App.tsx:13. Add `import { LibraryPage } from '@/pages/library/LibraryPage'`.

    NAV_ITEMS (59-64) and BOTTOM_NAV_ITEMS (66-71): replace both tuples with `{ to: '/library', label: 'Library', Icon: FolderOpen }` leftmost, then the existing Openings (`BookOpenIcon`) and Endgames (`TrophyIcon`) entries. Drop the standalone `/import` and `/overview` entries from both (D-05, D-11). Leave `ADMIN_NAV_ITEM` untouched. The derived testids auto-become `nav-library` / `mobile-nav-library` / `drawer-nav-library` — no manual testid needed.

    ROUTE_TITLES (79-85): add `'/library': 'Library'`, remove `'/import'` and `'/overview'`. `MobileHeader` resolves titles by `startsWith`, so `/library/import` and `/library/overview` both show "Library".

    isActive (89-93): add `if (to === '/library') return pathname.startsWith('/library');` so Library highlights on both subtab routes.

    Nav-lock exemption (three sites — NavHeader:128, MobileBottomBar:264, MobileMoreDrawer:348): change the `to !== '/import'` clause to `to !== '/library'` so Library is the always-unlocked top-level item (D-08, D-09). Preserve the existing `&& to !== '/admin'` where present.

    Notification dot MOVE (D-10):
      - NavHeader (147-155): change the `to === '/import' && noGames` guard to `to === '/library' && noGames`; rename testid `import-notification-dot` → `library-notification-dot`.
      - MobileBottomBar (281-289): same guard change; rename `import-notification-dot-mobile` → `library-notification-dot-mobile`.
      - MobileMoreDrawer (326-367): this drawer currently renders NO dot. Add `const noGames = profile != null && totalGames === 0;` near its existing totalGames derivation (App.tsx:330), then render the pulsing red-500 dot span (copy the markup from NavHeader 147-154) gated on `to === '/library' && noGames`, testid `library-notification-dot-drawer`, `aria-hidden="true"` on the dot spans.

    Routes block (588-596): repoint to the Library shell and add redirects.
      - Replace the `/import` route with `<Route path="/library/*" element={<LibraryPage onImportStarted={handleImportStarted} activeJobIds={activeJobIds} onJobDismissed={handleJobDismissed} />} />` — NOT wrapped in ImportRequiredRoute (D-08, D-14). The job props move off the old Import route onto LibraryPage.
      - Add redirects: `/import` → `<Navigate to="/library/import" replace />`; `/overview` → `<Navigate to="/library/overview" replace />`; repoint the existing `/rating` and `/global-stats` aliases directly to `<Navigate to="/library/overview" replace />` (D-12).
      - Remove the old `/overview` `<ImportRequiredRoute><GlobalStatsPage /></ImportRequiredRoute>` route. Leave `/openings/*` and `/endgames/*` ImportRequiredRoute wrapping exactly as-is (LIB-08), and `/admin` SuperuserRoute as-is.
      - The bare-`/library` default is handled inside LibraryPage (Plan 01), not in the route table.

    ImportRequiredRoute redirect (479): `<Navigate to="/import" replace />` → `<Navigate to="/library/import" replace />` (D-13).

    Dead-import cleanup: `ImportPage` import (25) and `GlobalStatsPage` import (29) are no longer referenced by App.tsx (they now live in the subtab wrappers). Remove both imports — knip in CI will fail on dead exports/imports otherwise (CLAUDE.md). Confirm no other App.tsx reference remains before deleting.

    Apply every change identically to all three nav surfaces where the rule says "three sites" — do not leave one surface on the old `/import` exemption or dot (CLAUDE.md "always apply changes to mobile too").
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run knip 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `App.tsx` `NAV_ITEMS` and `BOTTOM_NAV_ITEMS` each contain a `{ to: '/library', label: 'Library', Icon: FolderOpen }` leftmost entry and NO `/import` or `/overview` entry; `FolderOpen` is imported from lucide-react.
    - Source: `ROUTE_TITLES` has `'/library': 'Library'` and no `'/import'`/`'/overview'` keys; `isActive` has a `to === '/library'` prefix branch.
    - Source: all three nav-lock exemptions read `to !== '/library'` (none read `to !== '/import'`); the notification dot is gated on `to === '/library' && noGames` in NavHeader, MobileBottomBar, and (net-new) MobileMoreDrawer with testids `library-notification-dot`, `library-notification-dot-mobile`, `library-notification-dot-drawer`.
    - Source: a `<Route path="/library/*" element={<LibraryPage ... />}>` exists, is NOT wrapped in `ImportRequiredRoute`, and receives `onImportStarted`/`activeJobIds`/`onJobDismissed`; redirects exist for `/import`→`/library/import` and `/overview`,`/rating`,`/global-stats`→`/library/overview`.
    - Source: `/openings/*` and `/endgames/*` remain wrapped in `ImportRequiredRoute`; the ImportRequiredRoute redirect target is `/library/import`.
    - Gate: `cd frontend && npx tsc --noEmit` exits 0; `npm run knip` reports no new unused exports/imports (ImportPage/GlobalStatsPage imports removed from App.tsx).
    - HUMAN-UAT: at 375px the mobile bottom bar shows Library · Openings · Endgames + More, and the red dot renders on Library for a zero-games user.
    - HUMAN-UAT: navigating the browser to `/import` lands on `/library/import`; `/overview`, `/rating`, `/global-stats` land on `/library/overview`; `/library` with games lands on `/library/overview` and with zero games on `/library/import`; deep-linking `/library/import` and `/library/overview` directly works.
  </acceptance_criteria>
  <done>App.tsx routes, nav config (all three surfaces), redirects, guard target, and notification dot are fully repointed at the Library shell; typecheck and knip are clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep internal Import-Games links to /library/import</name>
  <files>frontend/src/pages/Home.tsx, frontend/src/pages/Endgames.tsx, frontend/src/pages/openings/GamesTab.tsx</files>
  <read_first>
    - frontend/src/pages/Home.tsx (lines 621-623: the gameless vs has-games redirect — only the gameless target changes; has-games stays /openings per D-05)
    - frontend/src/pages/Endgames.tsx (line 711: `<Link to="/import">` Import Games)
    - frontend/src/pages/openings/GamesTab.tsx (line 49: `<Link to="/import">` Import Games)
    - .planning/phases/104-library-page-shell-import-overview-subtab-migration/104-PATTERNS.md (§ Home.tsx, § Endgames.tsx & GamesTab.tsx link sweep — exact single-line targets)
    - .planning/phases/104-library-page-shell-import-overview-subtab-migration/104-CONTEXT.md (D-05 keep /openings for has-games, D-13 link sweep)
  </read_first>
  <action>
    Home.tsx (623): change the gameless redirect target `/import` → `/library/import`. Leave the has-games branch as `/openings` unchanged (D-05) — only the gameless arm moves.

    Endgames.tsx (711): change `<Link to="/import">` → `<Link to="/library/import">`. Visible "Import Games" copy and the surrounding markup unchanged.

    openings/GamesTab.tsx (49): change `<Link to="/import">` → `<Link to="/library/import">`. Copy/markup unchanged.

    These are three single-line href/target edits. Do not touch any other line in these files. After this, grep confirms no remaining `to="/import"` or `to={'/import'}` references in `frontend/src` outside the intentional `/import`→`/library/import` redirect route in App.tsx.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "to=[\"']/import[\"']" src/ | grep -v "library/import" | grep -v 'Navigate to="/library/import"' || echo "no stale /import links remain"; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `Home.tsx` gameless redirect targets `/library/import`; has-games branch still targets `/openings`.
    - Source: `Endgames.tsx:711` and `openings/GamesTab.tsx:49` `<Link>` href is `/library/import`.
    - Gate: `cd frontend && grep -rn 'to="/import"' src/` returns only the App.tsx redirect-source route (the `<Navigate to="/library/import">` and the `path="/import"` redirect), no live `<Link to="/import">` or component redirect to bare `/import`.
    - Gate: `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All internal Import-Games links and the Home gameless redirect point at /library/import; no stale live /import navigation targets remain.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 contribution)

New routes:
- `/library` (state-dependent default, resolved inside LibraryPage)
- `/library/import` (Import subtab, deep-linkable)
- `/library/overview` (Overview subtab, deep-linkable)
- `/library/*` wildcard `<Route>` in App.tsx
- redirect routes: `/import` → `/library/import`; `/overview`, `/rating`, `/global-stats` → `/library/overview`

New / renamed test-ids:
- `nav-library`, `mobile-nav-library`, `drawer-nav-library` (derived from the new "Library" nav label)
- `library-notification-dot` (renamed from `import-notification-dot`)
- `library-notification-dot-mobile` (renamed from `import-notification-dot-mobile`)
- `library-notification-dot-drawer` (net-new — the More drawer had no dot before)

Removed symbols/test-ids (consumers must stop referencing them):
- `import-notification-dot`, `import-notification-dot-mobile` (renamed away)
- standalone `/import` and `/overview` NAV_ITEMS/BOTTOM_NAV_ITEMS entries and their `nav-import`/`nav-overview` derived testids
- `ImportPage` / `GlobalStatsPage` imports in App.tsx (moved to subtab wrappers in Plan 01)

These newly created routes/test-ids did not exist before this phase; downstream drift checks should treat them as created-here, and treat the removed import-dot testids and `/import`,`/overview` nav entries as intentionally retired.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side React Router reconfiguration. No new endpoint, no new untrusted input, no auth/session change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03 | Elevation of Privilege | Route gating after migration | mitigate | The one real risk of this routing change is accidentally dropping an import gate or exposing a previously gated surface. Mitigation: `/openings/*` and `/endgames/*` MUST stay wrapped in `ImportRequiredRoute` (explicit acceptance criterion + source assertion); `/admin` MUST stay wrapped in `SuperuserRoute`. `/library` is intentionally ungated (D-08, it hosts Import) — this preserves, not loosens, existing semantics (Import was always reachable by gameless users). Verify via the source assertions in Task 1. |
| T-104-04 | Spoofing / Redirect | /import, /overview, /rating, /global-stats redirects | accept | All redirects are static `<Navigate>` to in-app absolute paths (`/library/...`) with no user-controlled component — no open-redirect surface. |
| T-104-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — `FolderOpen` ships with the already-installed lucide-react. No legitimacy gate required. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npm run knip` reports no new unused exports/imports (the moved ImportPage/GlobalStatsPage imports are removed from App.tsx).
- `cd frontend && npm run lint` exits 0; `npm test -- --run` passes (existing suite must stay green; update any test that asserted on the removed `import-notification-dot` testid or the standalone `/import`/`/overview` nav items to the new Library testids/paths).
- CLAUDE.md nav parity honored: nav-lock exemption, notification dot, and FolderOpen Library item applied identically across NavHeader, MobileBottomBar, and MobileMoreDrawer.
- HUMAN-UAT (browser, automation not practical): 375px bottom-bar rendering; redirect behavior for `/import`,`/overview`,`/rating`,`/global-stats`; state-dependent `/library` landing; deep-link `/library/import` and `/library/overview`; notification dot on Library for a zero-games user. Do NOT gate plan completion on a dev DB reset.
</verification>

<success_criteria>
- Top-level nav shows Library · Openings · Endgames (+ superuser Admin) with the FolderOpen icon; Import and Overview are gone as standalone nav items in all three surfaces (LIB-01, LIB-05).
- The zero-games notification dot renders on the Library nav item in all three surfaces (LIB-06).
- `/import` → `/library/import` and `/overview`,`/rating`,`/global-stats` → `/library/overview` redirects resolve with the full Import/Overview behavior intact (LIB-03, LIB-04).
- `/library` is never import-gated; `/openings`/`/endgames` stay gated; Home's gameless redirect targets `/library/import` while has-games stays `/openings` (LIB-07, LIB-08, D-05).
- Internal Import-Games links and the ImportRequiredRoute redirect target point at `/library/import` (D-13).
- `npx tsc --noEmit`, `npm run knip`, `npm run lint`, and `npm test -- --run` all pass (LIB-09).
</success_criteria>

<output>
Create `.planning/phases/104-library-page-shell-import-overview-subtab-migration/104-02-SUMMARY.md` when done.
</output>
