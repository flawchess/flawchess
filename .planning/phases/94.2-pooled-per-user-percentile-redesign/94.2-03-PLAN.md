---
phase: 94.2-pooled-per-user-percentile-redesign
plan: 03
type: execute
wave: 1
depends_on: [94.2-01, 94.2-02]
files_modified:
  - alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py
  - app/models/user_benchmark_percentile.py
  - app/repositories/user_benchmark_percentiles_repository.py
  - app/services/user_benchmark_percentiles_service.py
  - tests/services/test_user_benchmark_percentiles_service.py
autonomous: true
requirements: [PCTL-08, PCTL-09]

must_haves:
  truths:
    - "Alembic migration adds n_games INT NOT NULL DEFAULT 0 column to user_benchmark_percentiles per D-9-amend (correction to D-9). Downgrade drops the column."
    - "_compute_metric_for_user(session, user_id, metric_id) returns tuple[float, int] | None — (metric_value, n_games) or None when below the ≥30 floor. Return-type widening from float | None is per D-9-amend / RESEARCH A5."
    - "_compute_metric_for_user runs a single pooled SQL query — no apply_floor dual-mode, no dual-query fallback, no per-cell averaging. Selects (metric_value, n_games) from per_user_values."
    - "compute_stage_a and compute_stage_b destructure the (value, n_games) tuple and pass both to upsert_percentile. Public signatures (user_id, *, session_maker) -> None are byte-identical (per PCTL-09)."
    - "upsert_percentile widens to accept n_games: int parameter; INSERT ... ON CONFLICT ... DO UPDATE includes n_games in both insert values and update set."
    - "app/models/user_benchmark_percentile.py adds n_games: Mapped[int] = mapped_column(Integer, nullable=False); class docstring documents per-metric meaning under pooled model (score_gap=endgame games count; achievable=endgame-entry-with-d_i count; section2_conv=conversion span count; section2_parity=parity span count)."
  artifacts:
    - path: alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py
      provides: Alembic migration adding n_games column per D-9-amend
      contains: "op.add_column"
    - path: app/services/user_benchmark_percentiles_service.py
      provides: pooled-per-user compute service (single-query path, tuple return)
      contains: "async def _compute_metric_for_user"
    - path: app/models/user_benchmark_percentile.py
      provides: ORM model with n_games column + per-metric pooled docstring
      contains: "n_games"
    - path: app/repositories/user_benchmark_percentiles_repository.py
      provides: upsert_percentile widened to accept n_games
      contains: "n_games: int"
    - path: tests/services/test_user_benchmark_percentiles_service.py
      provides: unit tests asserting tuple return, single-query path, n_games threading, Stage A/B contract preserved
  key_links:
    - from: app/services/user_benchmark_percentiles_service.py
      to: app/services/canonical_slice_sql.py
      via: per_user_cte_for(metric, source='single_user')
      pattern: "per_user_cte_for"
    - from: app/services/user_benchmark_percentiles_service.py
      to: app/services/global_percentile_cdf.py
      via: interpolate_percentile(metric, value)
      pattern: "interpolate_percentile"
---

<objective>
Rewrite `app/services/user_benchmark_percentiles_service._compute_metric_for_user` to consume the new pooled SQL surface from Plan 01 against the regenerated `GLOBAL_PERCENTILE_CDF` from Plan 02. Drop the `apply_floor` parameter from the call site. Preserve the `float | None` return type (load-bearing per RESEARCH §Pitfall 4 — `compute_stage_a` / `compute_stage_b` rely on it). Update the ORM model docstring for `user_benchmark_percentile.n_games` to document per-metric semantics under the pooled model. Update unit tests.

Purpose: Close the cutover by aligning the per-user compute service with the new methodology so the values stored in `user_benchmark_percentiles.value` interpret correctly against the new CDF. Together with Plans 01 and 02, this plan completes the atomic wave-1 swap per D-3a.

Output: Updated service, updated ORM docstring, updated unit tests. After this plan merges, the full backend suite is green again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-01-PLAN.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-02-PLAN.md
@app/services/user_benchmark_percentiles_service.py
@app/services/canonical_slice_sql.py
@app/services/global_percentile_cdf.py
@app/models/user_benchmark_percentile.py
@app/repositories/user_benchmark_percentiles_repository.py
@tests/services/test_user_benchmark_percentiles_service.py
@tests/services/test_import_service_stage_a.py
@tests/services/test_eval_drain_stage_b.py

<interfaces>
<!-- Existing public surface (PRESERVE — load-bearing for Stage A/B triggers):
       compute_stage_a(user_id, *, session_maker=None) -> None
       compute_stage_b(user_id, *, session_maker=None) -> None

     Internal helper (RESHAPE — widen return per D-9-amend):
       OLD: _compute_metric_for_user(session, user_id, metric_id) -> float | None
            (currently runs ONE query via per_user_cte_for(..., apply_floor=True),
             averages metric_value across floor-passing cells)
       NEW: _compute_metric_for_user(session, user_id, metric_id) -> tuple[float, int] | None
            (runs ONE query via per_user_cte_for(..., source="single_user", snapshot_date=None),
             SELECTs metric_value AND n_games directly — no averaging, one row per user from per_user_values)

     Repository helper (WIDEN per D-9-amend):
       OLD: upsert_percentile(session, *, user_id, metric, value, percentile, cdf_snapshot) -> None
       NEW: upsert_percentile(session, *, user_id, metric, value, n_games, percentile, cdf_snapshot) -> None

     Alembic (NEW per D-9-amend):
       op.add_column("user_benchmark_percentiles", sa.Column("n_games", sa.Integer(), nullable=False, server_default="0"))
       Downgrade: op.drop_column("user_benchmark_percentiles", "n_games")
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Alembic migration + ORM update — re-add n_games column (D-9-amend)</name>
  <files>alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py, app/models/user_benchmark_percentile.py</files>
  <read_first>
    - alembic/versions/20260525_010000_drop_n_cells_floor.py (most recent migration; new migration goes AFTER this — `down_revision` = "a7f3c9b82e14")
    - app/models/user_benchmark_percentile.py (current shape: line 16 docstring references the n_cells_floor drop; line 64 is the last Mapped column — n_games goes after `percentile`)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (Amendment 2026-05-24: `n_games` column re-add — top of <decisions>)
  </read_first>
  <action>
    1. **Create migration** `alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py`:
       - revision id: pick a fresh 12-hex like `b8f4d92c1e25` (follow the existing pattern in `20260525_010000_drop_n_cells_floor.py`; revision string must be unique — run `grep -r "revision: str =" alembic/versions/` to confirm no collision before writing).
       - `down_revision: str | Sequence[str] | None = "a7f3c9b82e14"` (chains after the n_cells_floor drop).
       - `upgrade()` body:
         ```python
         op.add_column(
             "user_benchmark_percentiles",
             sa.Column("n_games", sa.Integer(), nullable=False, server_default="0"),
         )
         ```
         The `server_default="0"` lets existing rows backfill to 0 atomically. Plan 06's backfill rerun then populates correct per-user values within the same merge.
       - `downgrade()` body: `op.drop_column("user_benchmark_percentiles", "n_games")`.
       - Module docstring: explain the re-add per D-9-amend (correction to D-9); 94.1-04 added `n_games` → 94.1-11 renamed to `n_cells_floor` → 94.1-13 dropped → 94.2 re-adds under pooled semantics. Per-metric meaning per the amendment in CONTEXT.md.

    2. **Update `app/models/user_benchmark_percentile.py`**:
       - Add `n_games: Mapped[int] = mapped_column(Integer, nullable=False)` after `percentile` (line 64).
       - Update module docstring (line 16): replace the stale "The `n_cells_floor` column was dropped by migration a7f3c9b82e14." line with: "The `n_games` column was added by Phase 94.2 (re-introduced after the per-cell `n_cells_floor` was dropped in 94.1-13). Under the pooled-per-user model, `n_games` carries per-metric semantics — see the class docstring."
       - Update class docstring on `UserBenchmarkPercentile` (line 44): add a paragraph documenting per-metric `n_games` meaning under the pooled methodology:
         """Phase 94.2 (D-9-amend): the `n_games` column carries per-metric meaning under the pooled-per-user model.
         - `score_gap` → count of endgame games on the user's pool (the binding floor — paired non-endgame count is in the backfill log only, not the table).
         - `achievable_score_gap` → count of endgame-entry games with non-null `d_i` on the pool.
         - `section2_score_gap_conv` → count of spans classified into the conversion bucket on the pool.
         - `section2_score_gap_parity` → count of spans classified into the parity bucket on the pool."""

    3. **Run migration locally** against the dev DB to confirm round-trip:
       ```
       uv run alembic upgrade head
       uv run alembic downgrade -1
       uv run alembic upgrade head
       ```
       Each command exits 0.
  </action>
  <verify>
    <automated>uv run ruff format alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py app/models/user_benchmark_percentile.py &amp;&amp; uv run ruff check alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py app/models/user_benchmark_percentile.py &amp;&amp; uv run ty check app/models/user_benchmark_percentile.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0 against dev DB.
    - `psql $DATABASE_URL -c "\d user_benchmark_percentiles"` shows the `n_games | integer | not null | 0` column (use `mcp__flawchess-db__query` if psql not available locally — query `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_name = 'user_benchmark_percentiles' AND column_name = 'n_games'`).
    - `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips cleanly (both commands exit 0).
    - `grep -c "n_games" app/models/user_benchmark_percentile.py` ≥ 2 (Mapped column + docstring reference).
    - `grep -c "n_cells_floor" app/models/user_benchmark_percentile.py` == 0 (stale comment from line 16 is gone).
    - `uv run ty check app/models/user_benchmark_percentile.py` exits 0.
  </acceptance_criteria>
  <done>Migration created and round-trips clean; ORM model has n_games column with per-metric pooled docstring; stale n_cells_floor comment removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite _compute_metric_for_user (widen return to tuple[float, int] | None) and refresh module docstring</name>
  <files>app/services/user_benchmark_percentiles_service.py, app/repositories/user_benchmark_percentiles_repository.py</files>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py (full file — current implementation)
    - app/services/canonical_slice_sql.py (post-Plan 01 — new per_user_cte_for signature)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-3a, D-8, D-9, D-10)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Pitfall 4 (return-value contract preservation)
  </read_first>
  <behavior>
    - Test 1: `_compute_metric_for_user(session, user_id, "score_gap")` runs exactly one `session.execute(...)` call (no dual-query fallback).
    - Test 2: The SQL passed to `session.execute` contains no occurrence of `apply_floor` and no occurrence of the substring `elo_bucket` inside the per_user_values projection.
    - Test 3: Function returns `tuple[float, int]` (NOT a bare float — return-type widening per D-9-amend) when the CTE emits a row. Verify both elements: `value` is a float, `n_games` is an int ≥ 0.
    - Test 4: Function returns `None` when the CTE emits no row (user below pooled ≥30 floor).
    - Test 5: `compute_stage_a` and `compute_stage_b` signatures are unchanged — `(user_id, *, session_maker=None) -> None` (assert via `inspect.signature`).
    - Test 6: `compute_stage_a` calls `upsert_percentile` exactly once when `_compute_metric_for_user` returns a float; zero times when it returns None.
    - Test 7: `compute_stage_b` iterates over `STAGE_B_METRICS` and calls `upsert_percentile` once per metric that returns a non-None value; the inner per-metric try/except (preserved from Plan 13) still isolates per-metric failures.
    - Test 8: `compute_stage_a` and `compute_stage_b` pass `n_games=` to `upsert_percentile` (assert via `mock.call_args.kwargs["n_games"]` matches the int returned by the mocked `_compute_metric_for_user`).
  </behavior>
  <action>
    1. **Widen `upsert_percentile` in `app/repositories/user_benchmark_percentiles_repository.py`** to accept `n_games: int`:
       - Add `n_games: int` keyword parameter to the signature, after `value` and before `percentile`.
       - Include `n_games=n_games` in the `pg_insert(...).values(...)` block.
       - Include `"n_games": stmt.excluded.n_games` in the `on_conflict_do_update(set_={...})` block (so updates refresh n_games on UPSERT, same as value/percentile).
       - Update the docstring to mention the new parameter under Phase 94.2 (D-9-amend).

    2. Rewrite `_compute_metric_for_user` body in `app/services/user_benchmark_percentiles_service.py`:
       - Drop the `apply_floor=True` kwarg from the `per_user_cte_for` call.
       - The SQL query becomes:
         ```
         WITH {selected_users_cte(source="single_user")},
         {per_user_cte_for(metric_id, source="single_user")}
         SELECT metric_value::float AS value, n_games::int AS n_games
         FROM per_user_values
         LIMIT 1
         ```
         (no `avg(...)` wrapper — per_user_values is one row per user under the pooled model; `LIMIT 1` is defensive — by construction per_user_values for a single user emits zero or one row).
       - Return `None` if `row is None or row.value is None or row.n_games is None`.
       - Return `(float(row.value), int(row.n_games))` otherwise (tuple widening per D-9-amend).
       - Preserve the `:user_id` bindparam pattern via `text(sql).bindparams(user_id=user_id)` (V5 security — load-bearing).
       - Widen the function signature: `async def _compute_metric_for_user(session, user_id, metric_id) -> tuple[float, int] | None:`.

    3. Update `_compute_metric_for_user` docstring to reflect the new behavior. Concrete content:
       """Run the pooled-per-user CTE for one user and one metric (Phase 94.2).

       Returns `(metric_value, n_games)` from the single row of `per_user_values`, or `None` if the user is below the metric's ≥30 inclusion floor (the CTE's HAVING clause emits no row).

       Phase 94.2 supersedes the 94.1 Plan 13 single-query-with-apply-floor=True path: under the pooled model there are no per-(elo_bucket, tc_bucket) cells to average, so the prior `avg(metric_value)` wrapper is gone. One row per user → SELECT (value, n_games) directly.

       Return-type widening to `tuple[float, int] | None` (from 94.1's `float | None`) is per CONTEXT.md Amendment 2026-05-24 (D-9-amend): `n_games` is re-added to the storage table and must thread from the CTE to the UPSERT.

       Security (V5): `user_id` is bound via SQLAlchemy `.bindparams()`, never f-stringed into the query text."""

    4. Update the module docstring to replace the 94.1 floor-convention paragraphs with a Phase 94.2 paragraph:
       """Phase 94.2 (supersedes Phase 94.1 Plan 13): the per-user compute consumes the pooled-per-user CTE from `app/services/canonical_slice_sql.py`. Each `(user_id, metric_id)` lookup runs a single SQL query that emits either one row (`metric_value`, `n_games`) or zero rows (user below the ≥30 floor on the pooled set). The Plan 13 dual-query / dual-floor mechanism and the `apply_floor` argument are gone — no cells, no averaging.

       The compute helper returns `tuple[float, int] | None`; Stage A/B destructure `(value, n_games)` and pass both to `upsert_percentile` per D-9-amend.

       Stage A/B trigger contract preserved verbatim from 94.1 (PCTL-09): `compute_stage_a` fires after `_complete_import_job` for `score_gap`; `compute_stage_b` fires after `eval_drain` reaches zero pending evals with no active import, for the three eval-dependent metrics."""

    5. Update `compute_stage_a` and `compute_stage_b` call sites in `app/services/user_benchmark_percentiles_service.py`:
       - Both functions currently call `_compute_metric_for_user` and pass `value` to `upsert_percentile`. Update to destructure the tuple: `result = await _compute_metric_for_user(...); if result is None: return; value, n_games = result; await upsert_percentile(..., value=value, n_games=n_games, ...)`.
       - The public signatures `(user_id, *, session_maker=None) -> None` stay byte-identical; only the inner destructuring + the new `n_games=` kwarg on the `upsert_percentile` call changes.

    6. Type annotations: `_compute_metric_for_user` return type widens to `tuple[float, int] | None`. No new imports needed (`tuple` is a builtin; the `date` import already exists for `cdf_snapshot=date.today()` calls).
  </action>
  <verify>
    <automated>uv run ruff format app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py &amp;&amp; uv run ruff check app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py &amp;&amp; uv run ty check app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apply_floor" app/services/user_benchmark_percentiles_service.py` returns 0.
    - `grep -c "avg(metric_value)" app/services/user_benchmark_percentiles_service.py` returns 0 (no per-cell averaging — direct SELECT of the single per-user value).
    - `grep -c "SELECT metric_value" app/services/user_benchmark_percentiles_service.py` returns 1 (direct projection with n_games).
    - `grep -c "n_games" app/services/user_benchmark_percentiles_service.py` ≥ 3 (SELECT projection + tuple destructuring + upsert call).
    - `grep -c "n_games" app/repositories/user_benchmark_percentiles_repository.py` ≥ 3 (signature + insert values + on_conflict update).
    - `python -c "import inspect; from app.services.user_benchmark_percentiles_service import compute_stage_a, compute_stage_b; print(str(inspect.signature(compute_stage_a))); print(str(inspect.signature(compute_stage_b)))"` outputs the unchanged public signatures `(user_id: int, *, session_maker: ...|None = None) -> None`.
    - `python -c "import inspect; from app.services.user_benchmark_percentiles_service import _compute_metric_for_user; sig = inspect.signature(_compute_metric_for_user); assert 'tuple' in str(sig.return_annotation).lower() or 'Tuple' in str(sig.return_annotation), str(sig.return_annotation)"` exits 0 (return-type widening verified).
    - `python -c "import inspect; from app.repositories.user_benchmark_percentiles_repository import upsert_percentile; assert 'n_games' in inspect.signature(upsert_percentile).parameters"` exits 0.
    - `uv run ty check app/services/user_benchmark_percentiles_service.py app/repositories/user_benchmark_percentiles_repository.py` exits 0.
  </acceptance_criteria>
  <done>Service rewritten to single pooled query, apply_floor removed, Stage A/B contract preserved, V5 bindparam preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tests/services/test_user_benchmark_percentiles_service.py for pooled compute path</name>
  <files>tests/services/test_user_benchmark_percentiles_service.py</files>
  <read_first>
    - tests/services/test_user_benchmark_percentiles_service.py (existing 94.1 tests — apply_floor-aware)
    - app/services/user_benchmark_percentiles_service.py (post-Task 1)
    - tests/services/test_import_service_stage_a.py (Stage A integration contract — DO NOT touch in this plan, just confirm it still passes)
    - tests/services/test_eval_drain_stage_b.py (Stage B integration contract — same, confirm-only)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Validation Architecture SC-3 row
  </read_first>
  <behavior>
    - Test classes mirror the §behavior list from Task 1 — Test 1 through Test 7.
    - Tests are pure unit (no Postgres). Mock `session.execute` to return either a row with a `value` attribute or `None`. Verify exactly one call.
    - A regression test asserts that calling `_compute_metric_for_user` with `apply_floor=...` raises TypeError (the parameter is gone).
    - A regression test asserts that `compute_stage_b` calls `_compute_metric_for_user` exactly `len(STAGE_B_METRICS) == 3` times when no per-metric raises (preserves Plan 13's per-metric isolation contract).
  </behavior>
  <action>
    Rewrite the existing test module to:
    - Drop all `apply_floor` parametrize cases.
    - Drop the dual-query / dual-floor assertions (no longer applicable post-94.2).
    - Add Tests 1–7 from Task 1's §behavior block.
    - Use `unittest.mock.AsyncMock` for `session.execute`. Verify the SQL string passed (use `mock.call_args[0][0]` or similar) does not contain `avg(metric_value)`.
    - For Test 5 (signature preservation): use `inspect.signature(compute_stage_a).parameters` and assert exact param names and defaults.
    - For Test 6/7 (Stage A/B fan-out): mock `_compute_metric_for_user` via `monkeypatch` and assert call counts.
    - Preserve any existing tests that exercise the Sentry capture path / asyncio.CancelledError re-raise path (Plan 13 behavior, still valid).

    Do NOT touch `tests/services/test_import_service_stage_a.py` or `tests/services/test_eval_drain_stage_b.py` — those test the Stage A/B trigger contract which is byte-preserved.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_user_benchmark_percentiles_service.py tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x` exits 0.
    - `uv run pytest tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x` exits 0 (Stage A/B trigger contract still green — no edits to those modules).
    - `grep -c "apply_floor" tests/services/test_user_benchmark_percentiles_service.py` returns at most 1 (only inside the TypeError-on-apply_floor regression test, if present as a string literal).
    - `uv run ty check tests/services/test_user_benchmark_percentiles_service.py` exits 0.
  </acceptance_criteria>
  <done>Unit tests updated for pooled compute path; Stage A/B integration tests remain green untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage A/B trigger → compute service | Trusted internal — `user_id` flows from the import-job / eval-drain contexts; bound via `:user_id` SQLAlchemy parameter at the SQL composition site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.2-03-01 | Tampering (SQLi via user_id) | `text(sql).bindparams(user_id=user_id)` | mitigate | Preserve verbatim. `selected_users_cte(source="single_user")` from Plan 01 keeps the `CAST(:user_id AS int)` form (load-bearing per 94.1 Plan 09). |
| T-94.2-03-02 | Contract drift (Stage A/B return-type mismatch) | `_compute_metric_for_user` return type | mitigate | Preserve `float | None` return. Task 3's signature-preservation test catches accidental widening; Stage A/B integration tests catch caller-side breakage. |
| T-94.2-03-03 | Information Disclosure (Sentry context leak) | `set_context("percentile_compute", ...)` | accept | Existing 94.1 contexts pass `user_id` only (internal ID, no PII). Unchanged. |
</threat_model>

<verification>
After this plan merges, the full backend test suite is GREEN again:

- `uv run pytest -x` exits 0 (full suite).
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py tests/services/test_user_benchmark_percentiles_service.py tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x` all green.
- `uv run ty check app/ tests/` exits 0.
- `uv run ruff format --check . && uv run ruff check .` both exit 0.

This is the wave-1 cutover gate per D-3a — Plans 01 + 02 + 03 land together (in the same wave / branch / squash-merged PR) such that main never sees an intermediate broken state.
</verification>

<success_criteria>
`_compute_metric_for_user` runs a single pooled query (no apply_floor, no avg, no dual-query); Stage A/B public surface preserved verbatim; ORM `n_games` docstring documents pooled semantics; full backend suite green after merge.
</success_criteria>

<output>
Create `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-03-SUMMARY.md` when done.
</output>
