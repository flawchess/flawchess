---
phase: 94.2-pooled-per-user-percentile-redesign
plan: 03
type: execute
wave: 1
depends_on: [94.2-01, 94.2-02]
files_modified:
  - app/services/user_benchmark_percentiles_service.py
  - app/models/user_benchmark_percentile.py
  - tests/services/test_user_benchmark_percentiles_service.py
autonomous: true
requirements: [PCTL-08, PCTL-09]

must_haves:
  truths:
    - "_compute_metric_for_user(session, user_id, metric_id) returns float | None (raw pooled value or None when below the ≥30 floor) — signature preserved per RESEARCH §Pitfall 4 / A5."
    - "_compute_metric_for_user runs a single pooled SQL query — no apply_floor dual-mode, no dual-query fallback, no per-cell averaging."
    - "compute_stage_a and compute_stage_b public surfaces are byte-identical (signature + Sentry capture + Stage A/B trigger contract preserved per CONTEXT D-9 / PCTL-09)."
    - "app/models/user_benchmark_percentile.py docstring documents the per-metric meaning of n_games under the pooled model (score_gap=endgame count; achievable=endgame-entry-with-d_i count; section2_*=relevant bucket span count)."
  artifacts:
    - path: app/services/user_benchmark_percentiles_service.py
      provides: pooled-per-user compute service (single-query path)
      contains: "async def _compute_metric_for_user"
    - path: app/models/user_benchmark_percentile.py
      provides: ORM model with updated n_games docstring under pooled semantics
    - path: tests/services/test_user_benchmark_percentiles_service.py
      provides: unit tests asserting single-query path, apply_floor removal, Stage A/B contract preserved
  key_links:
    - from: app/services/user_benchmark_percentiles_service.py
      to: app/services/canonical_slice_sql.py
      via: per_user_cte_for(metric, source='single_user')
      pattern: "per_user_cte_for"
    - from: app/services/user_benchmark_percentiles_service.py
      to: app/services/global_percentile_cdf.py
      via: interpolate_percentile(metric, value)
      pattern: "interpolate_percentile"
---

<objective>
Rewrite `app/services/user_benchmark_percentiles_service._compute_metric_for_user` to consume the new pooled SQL surface from Plan 01 against the regenerated `GLOBAL_PERCENTILE_CDF` from Plan 02. Drop the `apply_floor` parameter from the call site. Preserve the `float | None` return type (load-bearing per RESEARCH §Pitfall 4 — `compute_stage_a` / `compute_stage_b` rely on it). Update the ORM model docstring for `user_benchmark_percentile.n_games` to document per-metric semantics under the pooled model. Update unit tests.

Purpose: Close the cutover by aligning the per-user compute service with the new methodology so the values stored in `user_benchmark_percentiles.value` interpret correctly against the new CDF. Together with Plans 01 and 02, this plan completes the atomic wave-1 swap per D-3a.

Output: Updated service, updated ORM docstring, updated unit tests. After this plan merges, the full backend suite is green again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-01-PLAN.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-02-PLAN.md
@app/services/user_benchmark_percentiles_service.py
@app/services/canonical_slice_sql.py
@app/services/global_percentile_cdf.py
@app/models/user_benchmark_percentile.py
@app/repositories/user_benchmark_percentiles_repository.py
@tests/services/test_user_benchmark_percentiles_service.py
@tests/services/test_import_service_stage_a.py
@tests/services/test_eval_drain_stage_b.py

<interfaces>
<!-- Existing public surface (PRESERVE — load-bearing for Stage A/B triggers):
       compute_stage_a(user_id, *, session_maker=None) -> None
       compute_stage_b(user_id, *, session_maker=None) -> None

     Internal helper (RESHAPE):
       OLD: _compute_metric_for_user(session, user_id, metric_id) -> float | None
            (currently runs ONE query via per_user_cte_for(..., apply_floor=True),
             averages metric_value across floor-passing cells)
       NEW: _compute_metric_for_user(session, user_id, metric_id) -> float | None
            (runs ONE query via per_user_cte_for(..., source="single_user", snapshot_date=None),
             SELECTs metric_value directly — no averaging, since per_user_values is one row per user)

     Imported repository helper (UNCHANGED):
       upsert_percentile(session, *, user_id, metric, value, percentile, cdf_snapshot) -> None
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite _compute_metric_for_user and refresh module docstring</name>
  <files>app/services/user_benchmark_percentiles_service.py</files>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py (full file — current implementation)
    - app/services/canonical_slice_sql.py (post-Plan 01 — new per_user_cte_for signature)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-3a, D-8, D-9, D-10)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Pitfall 4 (return-value contract preservation)
  </read_first>
  <behavior>
    - Test 1: `_compute_metric_for_user(session, user_id, "score_gap")` runs exactly one `session.execute(...)` call (no dual-query fallback).
    - Test 2: The SQL passed to `session.execute` contains no occurrence of `apply_floor` and no occurrence of the substring `elo_bucket` inside the per_user_values projection.
    - Test 3: Function returns `float` (not a tuple) when the CTE emits a row.
    - Test 4: Function returns `None` when the CTE emits no row (user below pooled ≥30 floor).
    - Test 5: `compute_stage_a` and `compute_stage_b` signatures are unchanged — `(user_id, *, session_maker=None) -> None` (assert via `inspect.signature`).
    - Test 6: `compute_stage_a` calls `upsert_percentile` exactly once when `_compute_metric_for_user` returns a float; zero times when it returns None.
    - Test 7: `compute_stage_b` iterates over `STAGE_B_METRICS` and calls `upsert_percentile` once per metric that returns a non-None value; the inner per-metric try/except (preserved from Plan 13) still isolates per-metric failures.
  </behavior>
  <action>
    1. Rewrite `_compute_metric_for_user` body:
       - Drop the `apply_floor=True` kwarg from the `per_user_cte_for` call.
       - The SQL query becomes:
         ```
         WITH {selected_users_cte(source="single_user")},
         {per_user_cte_for(metric_id, source="single_user")}
         SELECT metric_value::float AS value
         FROM per_user_values
         ```
         (no `avg(...)` wrapper — per_user_values is one row per user under the pooled model).
       - Return `None` if `row is None or row.value is None`.
       - Return `float(row.value)` otherwise.
       - Preserve the `:user_id` bindparam pattern via `text(sql).bindparams(user_id=user_id)` (V5 security — load-bearing).

    2. Update `_compute_metric_for_user` docstring to reflect the new behavior. Concrete content:
       """Run the pooled-per-user CTE for one user and one metric (Phase 94.2).

       Returns the single pooled metric value (`metric_value` column of `per_user_values`), or `None` if the user is below the metric's ≥30 inclusion floor (the CTE's HAVING clause emits no row).

       Phase 94.2 supersedes the 94.1 Plan 13 single-query-with-apply-floor=True path: under the pooled model there are no per-(elo_bucket, tc_bucket) cells to average, so the prior `avg(metric_value)` wrapper is gone. One row per user → SELECT the value directly.

       Security (V5): `user_id` is bound via SQLAlchemy `.bindparams()`, never f-stringed into the query text."""

    3. Update the module docstring to replace the 94.1 floor-convention paragraphs with a Phase 94.2 paragraph:
       """Phase 94.2 (supersedes Phase 94.1 Plan 13): the per-user compute consumes the pooled-per-user CTE from `app/services/canonical_slice_sql.py`. Each `(user_id, metric_id)` lookup runs a single SQL query that emits either one row (pooled value) or zero rows (user below the ≥30 floor on the pooled set). The Plan 13 dual-query / dual-floor mechanism and the `apply_floor` argument are gone — no cells, no averaging.

       Stage A/B trigger contract preserved verbatim from 94.1 (PCTL-09): `compute_stage_a` fires after `_complete_import_job` for `score_gap`; `compute_stage_b` fires after `eval_drain` reaches zero pending evals with no active import, for the three eval-dependent metrics."""

    4. Preserve `compute_stage_a` / `compute_stage_b` bodies verbatim. The only change is that they receive a `float | None` from `_compute_metric_for_user` exactly as before — no caller-side change required.

    5. Type annotations: `_compute_metric_for_user` keeps its existing `-> float | None` return type. No new imports needed (the `date` import already exists for `cdf_snapshot=date.today()` calls).
  </action>
  <verify>
    <automated>uv run ruff format app/services/user_benchmark_percentiles_service.py &amp;&amp; uv run ruff check app/services/user_benchmark_percentiles_service.py &amp;&amp; uv run ty check app/services/user_benchmark_percentiles_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apply_floor" app/services/user_benchmark_percentiles_service.py` returns 0.
    - `grep -c "avg(metric_value)" app/services/user_benchmark_percentiles_service.py` returns 0 (no per-cell averaging — direct SELECT of the single per-user value).
    - `grep -c "SELECT metric_value" app/services/user_benchmark_percentiles_service.py` returns 1 (direct projection).
    - `python -c "import inspect; from app.services.user_benchmark_percentiles_service import compute_stage_a, compute_stage_b; print(str(inspect.signature(compute_stage_a))); print(str(inspect.signature(compute_stage_b)))"` outputs the unchanged signatures `(user_id: int, *, session_maker: ...|None = None) -> None`.
    - `uv run ty check app/services/user_benchmark_percentiles_service.py` exits 0.
  </acceptance_criteria>
  <done>Service rewritten to single pooled query, apply_floor removed, Stage A/B contract preserved, V5 bindparam preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update app/models/user_benchmark_percentile.py docstring for pooled n_games semantics</name>
  <files>app/models/user_benchmark_percentile.py</files>
  <read_first>
    - app/models/user_benchmark_percentile.py (current 94.1 docstring)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (Claude's Discretion §n_games column semantics)
  </read_first>
  <action>
    Update the ORM model class docstring (and the inline `n_games` column comment if present) to document per-metric meaning under the pooled model. No schema change (D-9), no Alembic migration. Concrete content for the `n_games` Column docstring (or `Mapped[int]` comment):

    """Per-metric pooled-set sample-size count (Phase 94.2; supersedes the 94.1 floor-passing-cell-count semantics).

    Per-metric meaning under the pooled-per-user model:
    - score_gap: count of endgame games on the user's recent-1000-per-TC × 36-month pool (the binding ≥30-of-endgame-AND-≥30-non-endgame constraint's binding side).
    - achievable_score_gap: count of endgame-entry games with a non-null d_i value on the pool.
    - section2_score_gap_conv: count of spans classified into the conversion bucket on the pool.
    - section2_score_gap_parity: count of spans classified into the parity bucket on the pool.

    For score_gap, the secondary constraint (non-endgame count) is NOT stored on this column; it is logged by the backfill script's summary table when relevant."""

    If the ORM model is defined with `Mapped[...]` annotations rather than `Column(...)` legacy form, add the docstring as a triple-quoted string immediately following the `n_games` attribute declaration. Otherwise add a class-level table-level docstring update referencing the per-metric semantics.

    Also update the class-level docstring to add a brief Phase 94.2 note: "Schema unchanged from 94.1 (D-9); `value` and `n_games` column semantics shift under the pooled methodology — see `n_games` docstring and `app/services/user_benchmark_percentiles_service.py` Phase 94.2 module docstring."
  </action>
  <verify>
    <automated>uv run ruff format app/models/user_benchmark_percentile.py &amp;&amp; uv run ty check app/models/user_benchmark_percentile.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 94.2" app/models/user_benchmark_percentile.py` ≥ 1.
    - `grep -c "pooled" app/models/user_benchmark_percentile.py` ≥ 1.
    - No schema-level changes (no new `Column`, no `__table_args__` change, no Alembic migration created).
    - `uv run ty check app/models/user_benchmark_percentile.py` exits 0.
  </acceptance_criteria>
  <done>ORM docstring documents pooled n_games semantics per metric; schema unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update tests/services/test_user_benchmark_percentiles_service.py for pooled compute path</name>
  <files>tests/services/test_user_benchmark_percentiles_service.py</files>
  <read_first>
    - tests/services/test_user_benchmark_percentiles_service.py (existing 94.1 tests — apply_floor-aware)
    - app/services/user_benchmark_percentiles_service.py (post-Task 1)
    - tests/services/test_import_service_stage_a.py (Stage A integration contract — DO NOT touch in this plan, just confirm it still passes)
    - tests/services/test_eval_drain_stage_b.py (Stage B integration contract — same, confirm-only)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Validation Architecture SC-3 row
  </read_first>
  <behavior>
    - Test classes mirror the §behavior list from Task 1 — Test 1 through Test 7.
    - Tests are pure unit (no Postgres). Mock `session.execute` to return either a row with a `value` attribute or `None`. Verify exactly one call.
    - A regression test asserts that calling `_compute_metric_for_user` with `apply_floor=...` raises TypeError (the parameter is gone).
    - A regression test asserts that `compute_stage_b` calls `_compute_metric_for_user` exactly `len(STAGE_B_METRICS) == 3` times when no per-metric raises (preserves Plan 13's per-metric isolation contract).
  </behavior>
  <action>
    Rewrite the existing test module to:
    - Drop all `apply_floor` parametrize cases.
    - Drop the dual-query / dual-floor assertions (no longer applicable post-94.2).
    - Add Tests 1–7 from Task 1's §behavior block.
    - Use `unittest.mock.AsyncMock` for `session.execute`. Verify the SQL string passed (use `mock.call_args[0][0]` or similar) does not contain `avg(metric_value)`.
    - For Test 5 (signature preservation): use `inspect.signature(compute_stage_a).parameters` and assert exact param names and defaults.
    - For Test 6/7 (Stage A/B fan-out): mock `_compute_metric_for_user` via `monkeypatch` and assert call counts.
    - Preserve any existing tests that exercise the Sentry capture path / asyncio.CancelledError re-raise path (Plan 13 behavior, still valid).

    Do NOT touch `tests/services/test_import_service_stage_a.py` or `tests/services/test_eval_drain_stage_b.py` — those test the Stage A/B trigger contract which is byte-preserved.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_user_benchmark_percentiles_service.py tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_user_benchmark_percentiles_service.py -x` exits 0.
    - `uv run pytest tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x` exits 0 (Stage A/B trigger contract still green — no edits to those modules).
    - `grep -c "apply_floor" tests/services/test_user_benchmark_percentiles_service.py` returns at most 1 (only inside the TypeError-on-apply_floor regression test, if present as a string literal).
    - `uv run ty check tests/services/test_user_benchmark_percentiles_service.py` exits 0.
  </acceptance_criteria>
  <done>Unit tests updated for pooled compute path; Stage A/B integration tests remain green untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage A/B trigger → compute service | Trusted internal — `user_id` flows from the import-job / eval-drain contexts; bound via `:user_id` SQLAlchemy parameter at the SQL composition site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.2-03-01 | Tampering (SQLi via user_id) | `text(sql).bindparams(user_id=user_id)` | mitigate | Preserve verbatim. `selected_users_cte(source="single_user")` from Plan 01 keeps the `CAST(:user_id AS int)` form (load-bearing per 94.1 Plan 09). |
| T-94.2-03-02 | Contract drift (Stage A/B return-type mismatch) | `_compute_metric_for_user` return type | mitigate | Preserve `float | None` return. Task 3's signature-preservation test catches accidental widening; Stage A/B integration tests catch caller-side breakage. |
| T-94.2-03-03 | Information Disclosure (Sentry context leak) | `set_context("percentile_compute", ...)` | accept | Existing 94.1 contexts pass `user_id` only (internal ID, no PII). Unchanged. |
</threat_model>

<verification>
After this plan merges, the full backend test suite is GREEN again:

- `uv run pytest -x` exits 0 (full suite).
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py tests/services/test_user_benchmark_percentiles_service.py tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py -x` all green.
- `uv run ty check app/ tests/` exits 0.
- `uv run ruff format --check . && uv run ruff check .` both exit 0.

This is the wave-1 cutover gate per D-3a — Plans 01 + 02 + 03 land together (in the same wave / branch / squash-merged PR) such that main never sees an intermediate broken state.
</verification>

<success_criteria>
`_compute_metric_for_user` runs a single pooled query (no apply_floor, no avg, no dual-query); Stage A/B public surface preserved verbatim; ORM `n_games` docstring documents pooled semantics; full backend suite green after merge.
</success_criteria>

<output>
Create `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-03-SUMMARY.md` when done.
</output>
