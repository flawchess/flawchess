---
phase: 94.2-pooled-per-user-percentile-redesign
plan: 04
type: execute
wave: 2
depends_on: [94.2-01, 94.2-02, 94.2-03]
files_modified:
  - .claude/skills/benchmarks/SKILL.md
autonomous: true
requirements: [PCTL-08, PCTL-09]

must_haves:
  truths:
    - "SKILL.md carries a top-of-file callout clarifying that the SKILL describes its own per-cohort analytical methodology, NOT the production percentile chip's methodology (which is pooled-per-user since Phase 94.2)."
    - "Anywhere SKILL.md previously asserted that its per-cell numbers match (or define) the production CDF, that claim is updated — SKILL's per-cohort analysis is for ad-hoc reports; the chip uses `app/services/canonical_slice_sql.py`."
    - "Per-cohort distribution analysis methodology in SKILL.md (TC × ELO stratification) stays intact — it remains the right tool for /benchmarks analysis even though it's no longer the right tool for the chip."
    - "94.1 phase-directory cross-references stay unannotated — the SKILL still truthfully describes its own analysis methodology, so no Superseded callouts are needed."
  artifacts:
    - path: .claude/skills/benchmarks/SKILL.md
      provides: top-of-file callout pointing to production CDF source
      contains: "Phase 94.2"
  key_links:
    - from: .claude/skills/benchmarks/SKILL.md callout
      to: app/services/canonical_slice_sql.py
      via: "callout names the file as production CDF source of truth"
      pattern: "canonical_slice_sql"
---

<objective>
Add a top-of-file callout to `.claude/skills/benchmarks/SKILL.md` clarifying the SKILL describes its own per-cohort analytical methodology — NOT the production percentile chip's methodology, which shifted to pooled-per-user in Phase 94.2. Update any specific claims where SKILL.md previously asserted its per-cell numbers match (or define) the production CDF.

Per CONTEXT.md amendment D-13-amend: the SKILL is an analysis tool, not a production-CDF spec. Its per-cohort distribution methodology stays valid for analysis purposes (same way `_build_per_bucket_sanity_query` is kept as a diagnostic in Plan 02). No chapter rewrite; no per-cell content flagged as superseded; just a callout plus surgical claim corrections.

Output: SKILL.md with a ~10-line top-of-file callout and any specific incorrect "matches production CDF" claims corrected. Total diff expected: ~30 LOC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md
@.planning/notes/per-user-percentile-pooled-redesign.md
@.claude/skills/benchmarks/SKILL.md
@app/services/canonical_slice_sql.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add top-of-file callout to SKILL.md</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (full file — locate the existing top-of-file frontmatter / first heading to anchor the callout)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-13-amend: scope is callout + surgical claim correction, NOT chapter rewrite)
    - app/services/canonical_slice_sql.py (the production CDF source the callout points to)
  </read_first>
  <action>
    Insert a callout block immediately after the SKILL.md frontmatter (the YAML `---` block at the top) and before the first chapter heading. Use this exact structure (Markdown blockquote):

    > **Phase 94.2 callout:** This SKILL describes the per-cohort analytical methodology used for `/benchmarks` reports — distribution exploration, neutral-zone calibration, "what's typical" breakdowns by (TC × ELO) bucket. The **production percentile chip uses a different methodology** since Phase 94.2: a pooled-per-user model (recent 1000/TC across all played TCs, 36-month window, single ≥30 floor on the pooled set, single globally pooled CDF). See `app/services/canonical_slice_sql.py` for the chip's SQL builders, `app/services/global_percentile_cdf.py` for the committed CDF literal, and `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md` for the design rationale. The per-cohort stratification described below remains correct for analytical purposes — it just no longer mirrors what the production chip computes.

    Do not modify any other sections of SKILL.md in this task — Task 2 handles the surgical claim corrections.
  </action>
  <verify>
    <automated>grep -c "Phase 94.2 callout" .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 94.2 callout" .claude/skills/benchmarks/SKILL.md` returns exactly 1.
    - The callout block sits above the first `## ` chapter heading (`awk '/^## /{print NR; exit}' .claude/skills/benchmarks/SKILL.md` returns a line number larger than the line containing "Phase 94.2 callout").
    - `grep -c "canonical_slice_sql\.py\|global_percentile_cdf\.py" .claude/skills/benchmarks/SKILL.md` ≥ 1 (callout names the production source files).
    - Total LOC delta vs pre-Plan-04 SKILL.md is between 8 and 20 lines added (`git diff --shortstat HEAD -- .claude/skills/benchmarks/SKILL.md` shows `+8..+20 insertions, 0 deletions` for Task 1's commit).
  </acceptance_criteria>
  <done>Top-of-file callout inserted; no other SKILL.md changes in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Correct any SKILL.md claims that its per-cell numbers match (or define) the production CDF</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (full file)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-13-amend: surgical, not full rewrite)
    - reports/global-percentile-cdf-latest.md (post-Plan-02 regenerated CDF — the values SKILL.md may have previously claimed to match)
  </read_first>
  <action>
    Use grep to find any SKILL.md paragraphs that assert its per-cohort numbers correspond to the production CDF or chip:

    ```
    grep -n "production CDF\|chip displays\|chip computes\|chip's underlying\|GLOBAL_PERCENTILE_CDF\|user_benchmark_percentiles\|percentile chip\|PercentileChip" .claude/skills/benchmarks/SKILL.md
    ```

    For each match, determine whether the surrounding text claims correspondence between the SKILL's per-cell numbers and the production chip's numbers:

    - **If yes** (claim of correspondence): rewrite the surrounding sentence(s) to state that the production chip uses pooled-per-user methodology (see callout) and that the SKILL's per-cohort numbers are independent analytical breakdowns. Keep the sentence terse — one or two lines.
    - **If no** (mention is incidental, e.g. "the chip suppresses naturally on a missing row" — a behavioral cross-reference, not a numeric claim): leave the sentence unchanged.

    Expected match count is small (1–5 paragraphs). If no matches require rewriting, that is a valid outcome — note in the SUMMARY that no surgical corrections were needed and the callout alone satisfied D-13-amend. Do not invent claims to correct.

    Do NOT rewrite Chapter 1 cohort construction. Do NOT change the per-metric inclusion-floor table (20/20/30/30 stays — those are the SKILL's analytical floors, not the production chip's). Do NOT flag any per-cell content as "Superseded by Phase 94.2" — the per-cohort analysis methodology is not superseded for analysis purposes; only its claimed correspondence to the production chip is.

    If any of those expanded-scope edits feels needed, STOP and flag the ambiguity in the SUMMARY rather than expanding scope unilaterally.
  </action>
  <verify>
    <automated>git diff --shortstat HEAD -- .claude/skills/benchmarks/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Total LOC delta (Task 1 + Task 2 combined) vs pre-Plan-04 SKILL.md is ≤ 50 lines added (callout ≤ 20 + surgical corrections ≤ 30). If the delta exceeds 50 lines, the task has crept beyond D-13-amend's "trim to callout" scope and must be reviewed before commit.
    - No "Superseded by Phase 94.2" callouts added (`grep -c "Superseded by Phase 94.2" .claude/skills/benchmarks/SKILL.md` returns 0).
    - The per-metric inclusion-floor table is unchanged from its pre-Plan-04 state (`git diff HEAD -- .claude/skills/benchmarks/SKILL.md` does not touch the MIN_GAMES / MIN_SPANS table rows).
    - If the SUMMARY notes "no surgical corrections needed", that is a valid acceptance state — the callout alone satisfies the plan.
  </acceptance_criteria>
  <done>Any SKILL.md claims that its per-cell numbers correspond to the production CDF are corrected; per-cohort analysis methodology and per-metric inclusion-floor table are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → future LLM sessions | Trusted documentation input; no runtime impact. Drift between SKILL.md and production code is a documentation bug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.2-04-01 | Information Disclosure (LLM session reads SKILL.md and assumes per-cell methodology = production chip) | SKILL.md without callout | mitigate | Task 1 callout makes the SKILL-vs-production boundary explicit. Task 2 corrects any specific false-correspondence claims. |
</threat_model>

<verification>
- `grep -c "Phase 94.2 callout" .claude/skills/benchmarks/SKILL.md` returns exactly 1.
- `grep -c "Superseded by Phase 94.2" .claude/skills/benchmarks/SKILL.md` returns 0 (scope was deliberately trimmed; no superseded flagging).
- `git diff --shortstat HEAD~ -- .claude/skills/benchmarks/SKILL.md` shows ≤ 50 insertions total across both tasks.
- No code or test changes in this plan; full backend / frontend suites remain green (no regression risk).
</verification>

<success_criteria>
SKILL.md carries a top-of-file callout clarifying the production-vs-analysis boundary, and any specific claims that its numbers match the production CDF are corrected. Per-cohort analysis methodology and the per-metric inclusion-floor table stay intact.
</success_criteria>

<output>
Create `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-04-SUMMARY.md` when done. The SUMMARY should explicitly note how many surgical corrections were made in Task 2 (zero is a valid count if no false-correspondence claims existed).
</output>
