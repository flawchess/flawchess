---
phase: 94.2-pooled-per-user-percentile-redesign
plan: 02
type: execute
wave: 1
depends_on: [94.2-01]
files_modified:
  - scripts/gen_global_percentile_cdf.py
  - app/services/global_percentile_cdf.py
  - tests/scripts/test_gen_global_percentile_cdf_unchanged.py
  - reports/global-percentile-cdf-latest.md
  - reports/archive/global-percentile-cdf-94.1-2026-05-DD.md
autonomous: true
requirements: [PCTL-08, PCTL-09]

must_haves:
  truths:
    - "scripts/gen_global_percentile_cdf.py:_build_metric_breakpoint_query consumes the pooled per_user_cte_for output and emits SQL whose per_user_values is one row per cohort user (deduped via selected_users_cte('benchmark'))."
    - "The CLI accepts an optional --snapshot-date YYYY-MM-DD flag that threads through to per_user_cte_for(..., snapshot_date=...). Default = BENCHMARK_DB_SNAPSHOT_MONTH-derived date (2026-03-31)."
    - "app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF is regenerated under the new methodology and committed. n_users field on each metric's CdfTable accurately reflects distinct-user count."
    - "reports/global-percentile-cdf-latest.md is the new regen report; the prior 94.1 version is archived under reports/archive/global-percentile-cdf-94.1-YYYY-MM-DD.md."
    - "tests/scripts/test_gen_global_percentile_cdf_unchanged.py goldens are regenerated; the test structure (byte-identical SQL canary) is preserved."
    - "_build_per_bucket_sanity_query is preserved with a prominent in-report disclaimer that it no longer reflects the production CDF (per Claude's Discretion in CONTEXT.md and RESEARCH §Pitfall 6)."
  artifacts:
    - path: scripts/gen_global_percentile_cdf.py
      provides: pooled CDF regeneration script with snapshot_date CLI flag
      contains: "def _build_metric_breakpoint_query"
    - path: app/services/global_percentile_cdf.py
      provides: regenerated GLOBAL_PERCENTILE_CDF literal under pooled methodology
    - path: tests/scripts/test_gen_global_percentile_cdf_unchanged.py
      provides: byte-identical regression test with regenerated goldens
    - path: reports/global-percentile-cdf-latest.md
      provides: new regen report under pooled methodology + per-bucket sanity disclaimer
    - path: reports/archive/global-percentile-cdf-94.1-2026-05-DD.md
      provides: archived 94.1 report for the drift-vs-94.1 reference
  key_links:
    - from: scripts/gen_global_percentile_cdf.py
      to: app/services/canonical_slice_sql.py
      via: import per_user_cte_for, selected_users_cte
      pattern: "from app.services.canonical_slice_sql import"
    - from: app/services/global_percentile_cdf.py
      to: scripts/gen_global_percentile_cdf.py
      via: regenerated literal output
      pattern: "GLOBAL_PERCENTILE_CDF: dict"
---

<objective>
Rewrite `scripts/gen_global_percentile_cdf.py:_build_metric_breakpoint_query` to consume the new pooled SQL surface from Plan 01. Add a `--snapshot-date` CLI flag (default = derived from `BENCHMARK_DB_SNAPSHOT_MONTH` constant) that threads into `per_user_cte_for(..., snapshot_date=...)`. Run the regeneration against the local benchmark Docker DB (`flawchess-benchmark-db-1` on port 5433). Archive the existing `reports/global-percentile-cdf-latest.md` to `reports/archive/global-percentile-cdf-94.1-YYYY-MM-DD.md` BEFORE the regen run. Commit the regenerated `app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF` literal. Regenerate `tests/scripts/test_gen_global_percentile_cdf_unchanged.py` goldens. Preserve `_build_per_bucket_sanity_query` with an explicit "no longer reflects production CDF" disclaimer in the regen report header.

Purpose: Complete the CDF-construction side of the cutover. Together with Plan 01 (builder) and Plan 03 (consumer), this plan delivers the atomic methodology swap per D-3a / D-11. The byte-identical regression test is the canary that all three plans landed coherently.

Output: Updated script, regenerated literal, regenerated test goldens, new regen report, archived prior report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md
@.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-01-PLAN.md
@scripts/gen_global_percentile_cdf.py
@app/services/global_percentile_cdf.py
@app/services/canonical_slice_sql.py
@reports/global-percentile-cdf-latest.md
@tests/scripts/test_gen_global_percentile_cdf_unchanged.py

<interfaces>
<!-- gen_global_percentile_cdf.py consumes (after Plan 01):
       per_user_cte_for(metric_id, *, source, snapshot_date=None) -> str
       selected_users_cte(source="benchmark") -> str   # now emits deduped cohort
     Outputs:
       app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF: dict[CdfMetricId, CdfTable]
       where CdfTable carries: breakpoints, n_users, snapshot_date

     Database connectivity:
       BACKFILL_BENCHMARK_DB_URL env var resolved to a URL containing both
       "flawchess_benchmark" and ":5433" (T-93-03 safety guard preserved).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite _build_metric_breakpoint_query, add --snapshot-date CLI flag, disclaim _build_per_bucket_sanity_query</name>
  <files>scripts/gen_global_percentile_cdf.py</files>
  <read_first>
    - scripts/gen_global_percentile_cdf.py (full file — _build_metric_breakpoint_query at line 233, _build_per_bucket_sanity_query at line 245, BENCHMARK_DB_SNAPSHOT_MONTH constant, _assert_benchmark_db safety guard, CLI argparse block)
    - app/services/canonical_slice_sql.py (post-Plan 01 — pooled per_user_cte_for signature)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-1, D-10, D-11, Claude's Discretion §snapshot_date / §_build_per_bucket_sanity_query)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Pattern 3 (snapshot_date), §Pitfall 1, §Pitfall 6, §Validation Architecture SC-2 row
  </read_first>
  <behavior>
    - Test 1: `_build_metric_breakpoint_query("score_gap", snapshot_date=date(2026, 3, 31))` produces SQL that contains exactly one `selected_users AS (` block (the deduped benchmark cohort), exactly one `per_user_values` table at the end, and a `SELECT percentile_cont(...) ..., count(*) AS n_users FROM per_user_values` final block.
    - Test 2: The output of `_build_metric_breakpoint_query("score_gap", snapshot_date=date(2026, 3, 31))` contains the literal `DATE '2026-03-31' - INTERVAL '36 months'`.
    - Test 3: The output contains `bool_or(NOT (bsu.rating_bucket = 2400 AND bsu.tc_bucket = 'classical'))` (D-1 dedup) inside the selected_users CTE.
    - Test 4: The output does NOT contain `g.time_control_bucket::text = su.tc_bucket` anywhere (D-5 pooled across TCs).
    - Test 5: `_build_per_bucket_sanity_query("score_gap")` still runs without raising; its output is documented as no-longer-CDF-reflective in the regen-report header (verified manually + by a string search for the disclaimer in the script's report-emission section).
    - Test 6: CLI: `python scripts/gen_global_percentile_cdf.py --help` shows a `--snapshot-date YYYY-MM-DD` flag with a default value derived from `BENCHMARK_DB_SNAPSHOT_MONTH`.
  </behavior>
  <action>
    1. Rewrite `_build_metric_breakpoint_query(metric_id: CdfMetricId, *, snapshot_date: date) -> str`. New body composes `selected_users_cte(source="benchmark")` + `per_user_cte_for(metric_id, source="benchmark", snapshot_date=snapshot_date)` + final `SELECT percentile_cont({_breakpoint_sql_array()}) WITHIN GROUP (ORDER BY metric_value) AS breakpoints, count(*) AS n_users FROM per_user_values`. The `count(*) AS n_users` now correctly reflects distinct cohort users (per D-11 / RESEARCH §Pitfall 3).

    2. Update `_query_metric_breakpoints(session, metric_id, snapshot_date)` to accept and thread the snapshot_date kwarg. Propagate through every caller chain up to `main`.

    3. Add CLI flag `--snapshot-date` of type `str` (parsed to `date` via `date.fromisoformat`) in the argparse block. Default value: derive from `BENCHMARK_DB_SNAPSHOT_MONTH` (currently `'2026-03'`) by parsing it as `YYYY-MM` and computing the last day of that month — i.e. `2026-03` → `date(2026, 3, 31)`. Add a module-level helper `_default_snapshot_date() -> date` using `calendar.monthrange` or `dateutil.relativedelta` (prefer stdlib `calendar` to avoid a new dependency). Document the default in `--help` text.

    4. Preserve `_build_per_bucket_sanity_query(metric_id)` verbatim (no signature change — it's a diagnostic). Add a module docstring tweak above it: """DIAGNOSTIC ONLY (Phase 94.2): per-bucket aggregation here groups by `elo_bucket` and does NOT reflect the production CDF, which is computed over one-row-per-deduped-cohort-user under the pooled methodology. The diagnostic still computes per-rating-bucket distribution shape (median, skew, excess kurtosis) for sanity-checking pooled-value tails; that signal is informative but distinct from the production CDF."""

    5. Update the report emission block (the part of `main()` that writes `reports/global-percentile-cdf-latest.md`) to:
       a. Before opening the new report file, archive the existing one to `reports/archive/global-percentile-cdf-94.1-{today.isoformat()}.md` via `shutil.copy2` (idempotent: skip the archive copy if the destination already exists, log a warning).
       b. The new report's title is "Global Percentile CDF — Pooled-Per-User Methodology (Phase 94.2)".
       c. The header section MUST include a verbatim disclaimer paragraph: "**Methodology change (Phase 94.2):** Each CDF point is now one deduped cohort user (pooled across TCs played, capped at 1000 games/TC, ≤36 months from snapshot). Breakpoint values shift materially from prior reports; the per-rating-bucket sanity table below is a distribution-shape diagnostic and does NOT correspond to what the production CDF measures."
       d. Below the breakpoints table, add a "## Drift vs Phase 94.1" subsection that loads `reports/archive/global-percentile-cdf-94.1-{today.isoformat()}.md` (if present), parses its breakpoints table, and emits a side-by-side comparison: `p1 / p10 / p25 / p50 / p75 / p90 / p99` columns × `94.1 / 94.2 / Δ` rows per metric. If the archive file is missing, skip the subsection and log a warning.

    6. Type annotations: `_build_metric_breakpoint_query(metric_id: CdfMetricId, *, snapshot_date: date) -> str`; `_default_snapshot_date() -> date`; `main(..., snapshot_date: date)`. Use `Literal` types preserved from 94.1 (do not loosen).

    7. Update the module docstring to describe the pooled methodology and the snapshot_date CLI semantics. Note that the script's safety guards (`_assert_benchmark_db`) are unchanged.
  </action>
  <verify>
    <automated>uv run ruff format scripts/gen_global_percentile_cdf.py &amp;&amp; uv run ruff check scripts/gen_global_percentile_cdf.py &amp;&amp; uv run ty check scripts/gen_global_percentile_cdf.py</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/gen_global_percentile_cdf.py --help` lists `--snapshot-date YYYY-MM-DD` with default text `(default: 2026-03-31)` or similar.
    - `python -c "from scripts.gen_global_percentile_cdf import _build_metric_breakpoint_query; from datetime import date; print(_build_metric_breakpoint_query('score_gap', snapshot_date=date(2026, 3, 31)))" | grep -c "DATE '2026-03-31'"` returns 1.
    - `python -c "from scripts.gen_global_percentile_cdf import _build_metric_breakpoint_query; from datetime import date; print(_build_metric_breakpoint_query('score_gap', snapshot_date=date(2026, 3, 31)))" | grep -c "g.time_control_bucket::text = su.tc_bucket"` returns 0.
    - `grep -c "bool_or" scripts/gen_global_percentile_cdf.py` returns 0 (the dedup logic lives inside `selected_users_cte` from Plan 01, NOT duplicated in this script).
    - `uv run ty check scripts/gen_global_percentile_cdf.py` exits 0.
  </acceptance_criteria>
  <done>Script consumes pooled SQL surface, exposes snapshot_date CLI flag, preserves sanity-query diagnostic with disclaimer, archives prior report before regen.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate GLOBAL_PERCENTILE_CDF literal + write new regen report against benchmark DB</name>
  <files>app/services/global_percentile_cdf.py, reports/global-percentile-cdf-latest.md, reports/archive/global-percentile-cdf-94.1-YYYY-MM-DD.md</files>
  <read_first>
    - scripts/gen_global_percentile_cdf.py (post-Task 1)
    - app/services/global_percentile_cdf.py (current 94.1 literal — for the archive-then-regenerate flow)
    - reports/global-percentile-cdf-latest.md (current 94.1 report — to be archived)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (D-11)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Probe Results (expected cohort sizes: 1,724 / 1,786 / 1,645 / 1,424)
  </read_first>
  <action>
    Run the regeneration end-to-end against the local benchmark Docker DB. Pre-flight check: confirm the container is running with `docker ps --filter "name=flawchess-benchmark-db-1" --format '{{.Status}}'`; if not running, stop and surface a HUMAN-ACTION (the operator starts it per CLAUDE.md — do not invoke `bin/benchmark_db.sh start` per phase instructions).

    Execution sequence:

    1. Resolve `BACKFILL_BENCHMARK_DB_URL` from the local `.env` (or operator environment). Verify it contains both `flawchess_benchmark` and `:5433` (the `_assert_benchmark_db` guard will enforce this at script-run time too).

    2. Invoke `uv run python scripts/gen_global_percentile_cdf.py --snapshot-date 2026-03-31` (or omit the flag to take the default). The script will:
       - Archive the existing `reports/global-percentile-cdf-latest.md` to `reports/archive/global-percentile-cdf-94.1-{today}.md` (per Task 1 step 5a).
       - Run the breakpoint query for all 4 metrics against the benchmark DB.
       - Write the new `app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF` literal.
       - Write the new `reports/global-percentile-cdf-latest.md` with the disclaimer header + drift-vs-94.1 subsection.

    3. Capture stdout: each metric should log its `n_users` count. Expected per RESEARCH §Probe Results: score_gap≈1,724; achievable≈1,786; conv≈1,645; parity≈1,424. If any count drops below 500, STOP and surface a planning concern per D-2 (do not commit; reach out for D-2 escalation).

    4. After successful regeneration:
       - Confirm the new `app/services/global_percentile_cdf.py` literal is well-formed Python: `uv run python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF; print({m: t.n_users for m, t in GLOBAL_PERCENTILE_CDF.items()})"`.
       - Confirm the archived report exists at `reports/archive/global-percentile-cdf-94.1-{today}.md`.
       - Confirm the new report at `reports/global-percentile-cdf-latest.md` contains the disclaimer paragraph and the drift table.

    5. Commit message (single commit covering this task only — Task 3 follows in a separate commit so the goldens regenerate against the just-committed literal): "feat(94.2-02): regenerate GLOBAL_PERCENTILE_CDF under pooled-per-user methodology".

    Do NOT manually edit the regenerated `GLOBAL_PERCENTILE_CDF` literal — every byte must come from the script run for the regen-report's audit trail to remain valid.
  </action>
  <verify>
    <automated>uv run python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF; assert set(GLOBAL_PERCENTILE_CDF.keys()) == {'score_gap', 'achievable_score_gap', 'section2_score_gap_conv', 'section2_score_gap_parity'}; assert all(t.n_users &gt;= 500 for t in GLOBAL_PERCENTILE_CDF.values()), {m: t.n_users for m, t in GLOBAL_PERCENTILE_CDF.items()}" &amp;&amp; test -f reports/global-percentile-cdf-latest.md &amp;&amp; ls reports/archive/global-percentile-cdf-94.1-*.md</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/global_percentile_cdf.py:GLOBAL_PERCENTILE_CDF` is a dict with exactly the four metric keys; every `CdfTable.n_users` ≥ 500 (soft floor per D-2).
    - `reports/global-percentile-cdf-latest.md` exists and contains the verbatim disclaimer string "**Methodology change (Phase 94.2):**".
    - `reports/global-percentile-cdf-latest.md` contains a "## Drift vs Phase 94.1" subsection.
    - `reports/archive/global-percentile-cdf-94.1-*.md` exists (the prior 94.1 report archived under a dated path).
    - `git diff --stat app/services/global_percentile_cdf.py` shows the literal changed materially (line-level diff on the breakpoint values).
    - `uv run ty check app/services/global_percentile_cdf.py` exits 0 (the regenerated literal type-checks).
  </acceptance_criteria>
  <done>GLOBAL_PERCENTILE_CDF is regenerated, the new report exists with disclaimer + drift table, the prior report is archived. No metric below the 500 soft floor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regenerate tests/scripts/test_gen_global_percentile_cdf_unchanged.py goldens</name>
  <files>tests/scripts/test_gen_global_percentile_cdf_unchanged.py</files>
  <read_first>
    - tests/scripts/test_gen_global_percentile_cdf_unchanged.py (full file — to understand fixture format)
    - scripts/gen_global_percentile_cdf.py (post-Task 1 — the SQL-builder API under test)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-CONTEXT.md (Claude's Discretion §Regen test goldens)
    - .planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-RESEARCH.md §Pitfall 5
  </read_first>
  <behavior>
    - The byte-identical-SQL regression test (parametrised over the 4 metric IDs) asserts that `_build_metric_breakpoint_query(metric_id, snapshot_date=date(2026, 3, 31))` produces a SQL string that matches the committed golden fixture exactly (`assert sql == GOLDEN[metric]`).
    - The test passes after fixtures are regenerated against Task 1's rewritten builder.
    - The test FAILS if a future PR accidentally drifts the SQL builder (e.g. reintroduces a `g.time_control_bucket::text = su.tc_bucket` predicate). This is the canary that the cutover stays coherent.
  </behavior>
  <action>
    Regenerate the goldens by running the new `_build_metric_breakpoint_query` for each of the 4 metrics with `snapshot_date=date(2026, 3, 31)` (matching the script's default) and replacing the inlined golden constants in the test file. If the existing test file uses an external fixture directory (e.g. `tests/scripts/fixtures/*.sql`), regenerate those files instead — preserve the existing fixture layout, only the content changes.

    Use a tight regeneration helper inside the test module or a separate `tests/scripts/regenerate_gen_cdf_goldens.py` if one already exists (read the test file first to determine the pattern). Do NOT manually transcribe the SQL — generate it programmatically:

    ```
    uv run python -c "from datetime import date; from scripts.gen_global_percentile_cdf import _build_metric_breakpoint_query; from app.services.global_percentile_cdf import CdfMetricId; \
    import json; \
    print(json.dumps({m: _build_metric_breakpoint_query(m, snapshot_date=date(2026, 3, 31)) for m in ('score_gap', 'achievable_score_gap', 'section2_score_gap_conv', 'section2_score_gap_parity')}, indent=2))"
    ```

    Pipe the output into the test file's golden constants OR write the JSON to disk if fixtures live there.

    Add (or preserve) a docstring at the top of the test file: """Phase 94.2 canary: ensures `_build_metric_breakpoint_query` SQL output is stable. Drift here means a downstream consumer (gen_global_percentile_cdf.py / user_benchmark_percentiles_service.py) will produce inconsistent numbers across the two paths. Regenerate by running `uv run python scripts/regenerate_gen_cdf_goldens.py` (if it exists) or `git revert` and start over."""
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` exits 0.
    - The golden fixtures contain the string `DATE '2026-03-31' - INTERVAL '36 months'` for at least one metric (verifies snapshot_date threaded through).
    - The golden fixtures contain `bool_or(NOT (bsu.rating_bucket = 2400 AND bsu.tc_bucket = 'classical'))` (verifies cohort dedup is in the rendered SQL).
    - The golden fixtures do NOT contain `g.time_control_bucket::text = su.tc_bucket` (Pitfall 2 canary).
    - The golden fixtures do NOT contain `apply_floor` (Pitfall: stale dual-mode reference).
  </acceptance_criteria>
  <done>Goldens regenerated, byte-identical regression test passes, the canary now defends the new pooled SQL surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → script | Trusted operator-controlled; `--snapshot-date` is a Python `date` parsed via `date.fromisoformat` (raises ValueError on malformed input — no injection). |
| script → benchmark DB | Read-only by intent; `_assert_benchmark_db` guard requires `flawchess_benchmark` + `:5433` in resolved URL (T-93-03 preserved). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.2-02-01 | Tampering (cross-DB write) | `_assert_benchmark_db` | mitigate | Preserve verbatim — guard is unchanged. The Task 2 regeneration run will hit the assertion if `BACKFILL_BENCHMARK_DB_URL` is misconfigured. |
| T-94.2-02-02 | Tampering (silent drift) | byte-identical golden test | mitigate | Task 3 regenerates fixtures against the new builder. The test now defends against future accidental drift of the production CDF SQL surface — exactly the canary intended in 94.1 Plan 03. |
| T-94.2-02-03 | Information Disclosure (BACKFILL_BENCHMARK_DB_URL leak in logs) | script logging | accept | `_mask_password` helper already masks the password in log lines; preserved from 94.1. |
</threat_model>

<verification>
- `uv run pytest tests/scripts/test_gen_global_percentile_cdf_unchanged.py -x` is green.
- `uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -x` remains green (Plan 01 deliverables).
- `uv run ty check scripts/gen_global_percentile_cdf.py app/services/global_percentile_cdf.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py` exits 0.
- `uv run ruff format --check scripts/gen_global_percentile_cdf.py app/services/global_percentile_cdf.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py` exits 0.
- `uv run ruff check scripts/gen_global_percentile_cdf.py app/services/global_percentile_cdf.py tests/scripts/test_gen_global_percentile_cdf_unchanged.py` exits 0.
- Full backend suite is EXPECTED RED at end of this plan because `user_benchmark_percentiles_service._compute_metric_for_user` still calls `per_user_cte_for(..., apply_floor=True)`. Plan 03 closes the loop within the same wave/PR per D-3a.
</verification>

<success_criteria>
`_build_metric_breakpoint_query` consumes the pooled SQL surface with `snapshot_date` threading; `GLOBAL_PERCENTILE_CDF` literal regenerated and committed with `n_users` reflecting distinct-user counts ≥500 per metric; regen report exists with the methodology-shift disclaimer and drift-vs-94.1 table; prior 94.1 report archived; byte-identical regression test passes against new goldens.
</success_criteria>

<output>
Create `.planning/phases/94.2-pooled-per-user-percentile-redesign/94.2-02-SUMMARY.md` when done. Include the regen-run stdout `n_users` counts in the summary.
</output>
