---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 11
type: execute
wave: 2
depends_on: [94.4-09, 94.4-10]
files_modified:
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/PercentileChip.tsx
  - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
autonomous: true
requirements: [PRPCR-03, PRPCR-07]

must_haves:
  truths:
    - "`RatingAnchorOut` Pydantic schema in `app/schemas/endgames.py` is rewritten per the D-12 Reversal Amendment: DROP `source_platform: Literal['lichess', 'chesscom']`, DROP `chesscom_raw_rating: int | None`, DROP `n_games: int`. ADD `n_chesscom_games: int`, `n_lichess_games: int`, `chesscom_median_native: int | None = None`, `lichess_median_native: int | None = None`. KEEP `anchor_rating: int`. Class docstring rewrites to describe blended-anchor + tooltip composition disclosure (no Lichess-precedence language). Cites the amendment date + the design-decision record path."
    - "`app/services/endgame_service.py::rating_anchors` materialisation (currently around line 3030-3050) updates to read the 5-field `RatingAnchorRow` and emit the 5-field `RatingAnchorOut`. The mapping is mechanical: `anchor_rating=row.anchor_rating, n_chesscom_games=row.n_chesscom_games, n_lichess_games=row.n_lichess_games, chesscom_median_native=row.chesscom_median_native, lichess_median_native=row.lichess_median_native`. The comment block around line 3030 (V4 mitigation reminder, chesscom_raw_rating origin note) rewrites to reflect the blended-anchor semantics. The `pickDominantTcAnchor` / dominant-TC selection logic in this service (if any — verify via grep) is unchanged shape-wise: still picks the TC with the highest game count, but the count now comes from `n_chesscom_games + n_lichess_games` rather than the legacy `n_games`."
    - "`frontend/src/types/endgames.ts::RatingAnchor` TypeScript type is regenerated to match the new schema: DROP `source_platform: 'lichess' | 'chesscom'`, DROP `chesscom_raw_rating: number | null`, DROP `n_games: number`. ADD `n_chesscom_games: number`, `n_lichess_games: number`, `chesscom_median_native: number | null`, `lichess_median_native: number | null`. KEEP `anchor_rating: number`. Per CLAUDE.md frontend rules, TypeScript types should come from the API contract (regenerated, not hand-rolled) — if the project uses an OpenAPI generator script (find via `grep -rn 'openapi\\|generate.*types' frontend/ scripts/`), run it. If types are hand-maintained (likely given the file is a manual `interface` declaration), update by hand and document the deviation in the SUMMARY."
    - "`frontend/src/components/charts/PercentileChip.tsx` `PercentileChipProps` interface (line 104-107) updates: DROP `anchorSource: 'lichess' | 'chesscom'`, DROP `chesscomRawRating?: number`. ADD `nChesscomGames: number`, `nLichessGames: number`, `chesscomMedianNative?: number`, `lichessMedianNative?: number`. KEEP `percentile`, `flavor`, `tc?`, `anchorRating`, `metricLabel`, `testId`. The `PercentileChipPopoverBody` component's bullet 4 (rating-anchor disclosure) is rewritten to render BLENDED COMPOSITION per the amendment's suggested copy direction: 'Anchored at ~{anchor_rating} Elo, blending {n_chesscom_games} chess.com games (median {chesscom_median_native}, converted) with {n_lichess_games} lichess games (median {lichess_median_native}).' Suppression and edge-case copy follow the rules below."
    - "Tooltip bullet 4 copy variants per amendment + memory `feedback_percentile_chip_tooltip_disclosure`:\n   (a) **Mixed user** (n_chesscom_games > 0 AND n_lichess_games > 0): full blended-composition prose as above. Always names both platforms with their native medians.\n   (b) **Pure-lichess user** (n_chesscom_games == 0): drop the chess.com clause. 'Anchored at ~{anchor_rating} Elo from {n_lichess_games} lichess games (native rating).' The 'native rating' suffix flags that no conversion was applied (transparency).\n   (c) **Pure-chess.com user** (n_lichess_games == 0): drop the lichess clause. 'Anchored at ~{anchor_rating} Elo from {n_chesscom_games} chess.com games (median {chesscom_median_native}, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26).'\n   (d) **Suppression** (n_chesscom_games == 0 AND n_lichess_games == 0): chip suppresses entirely — no anchor, no tooltip. This branch handled at the caller (the parent component), not inside PercentileChipPopoverBody. The component MAY assert at runtime that at least one count > 0 (defensive)."
    - "The HARD CONSTRAINT from Plan 07 stays locked: NO flame icons. `grep -c '[fF]lame' frontend/src/components/charts/PercentileChip.tsx` returns 0 after the rewrite (excluding code-comment lines if any reference the prohibition by name). The no-flame Vitest regression guard from Plan 07 (Test 9 in `__tests__/PercentileChip.test.tsx`) is preserved verbatim."
    - "Vitest tests in `frontend/src/components/charts/__tests__/PercentileChip.test.tsx` REWRITE bullet 4 assertions (Tests 10, 11, 12 in the Plan 07 numbering). Replace the Lichess-anchored / chess.com-anchored split with the mixed / pure-lichess / pure-chess.com / suppression QUADRUPLE — all four composition branches (C1 mixed, C2 pure-lichess, C3 pure-chess.com, C6 suppression) MUST be exercised by separate test cases with BRANCH-DIFFERENTIATING substring assertions (e.g., mixed asserts BOTH 'chess.com games' AND 'Lichess games' / 'lichess games' substrings appear; pure-lichess asserts 'lichess games' AND 'native rating' appear AND 'chess.com' does NOT appear in bullet 4; pure-chess.com asserts 'chess.com games' AND 'converted' appear AND 'lichess games' does NOT appear in bullet 4; suppression asserts the chip does NOT render at all). The amendment locks branch structure, not exact wording — planner discretion still governs prose under disclosure-minimalism, but the BRANCH DIFFERENTIATION must be observable in tests so a single-branch fallback cannot silently pass."
    - "`frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` and any other component passing anchor props to `PercentileChip` (e.g. `EndgameTimePressureCard.tsx`, `EndgameMetricCard.tsx`) — verify via grep — update their prop wiring to pass the new 4 props (`nChesscomGames`, `nLichessGames`, `chesscomMedianNative`, `lichessMedianNative`) and stop passing `anchorSource` + `chesscomRawRating`. The `pickDominantTcAnchor` helper in `frontend/src/lib/percentileAnchor.ts` (if it exists per Plan 07) updates to use the new `RatingAnchor` shape and select dominance via `n_chesscom_games + n_lichess_games` (total games per TC)."
    - "`npm run knip` stays green after the prop renames — verify no new dead exports surface. The OpenAPI / TypeScript type regen (or manual edit) leaves no stale `source_platform` or `chesscom_raw_rating` references anywhere in `frontend/src/`."
  artifacts:
    - path: app/schemas/endgames.py
      provides: RatingAnchorOut Pydantic schema reshaped for blended anchor
      contains: "n_chesscom_games\\|n_lichess_games\\|chesscom_median_native\\|lichess_median_native"
      min_lines: 870
    - path: app/services/endgame_service.py
      provides: rating_anchors block materialisation updated for the 5-field RatingAnchorRow -> RatingAnchorOut mapping
      contains: "n_chesscom_games\\|n_lichess_games\\|chesscom_median_native\\|lichess_median_native"
    - path: frontend/src/types/endgames.ts
      provides: regenerated RatingAnchor TypeScript type matching the new schema
      contains: "n_chesscom_games\\|n_lichess_games\\|chesscom_median_native\\|lichess_median_native"
    - path: frontend/src/components/charts/PercentileChip.tsx
      provides: PercentileChipProps + PercentileChipPopoverBody updated for blended composition disclosure
      contains: "nChesscomGames\\|nLichessGames\\|chesscomMedianNative\\|lichessMedianNative\\|blending"
      min_lines: 250
    - path: frontend/src/components/charts/__tests__/PercentileChip.test.tsx
      provides: bullet 4 assertion rewrite for mixed / pure-lichess / pure-chess.com / suppression — all four branches with branch-differentiating substring assertions
      contains: "blending\\|nChesscomGames\\|nLichessGames"
  key_links:
    - from: app/schemas/endgames.py RatingAnchorOut
      to: app/services/endgame_service.py rating_anchors materialisation
      via: 5-field RatingAnchorRow.* -> RatingAnchorOut(...) mapping
      pattern: "n_chesscom_games\\|chesscom_median_native"
    - from: app/schemas/endgames.py RatingAnchorOut
      to: frontend/src/types/endgames.ts RatingAnchor
      via: API contract; field names must match exactly (snake_case on both sides per existing convention)
      pattern: "n_chesscom_games\\|chesscom_median_native"
    - from: frontend/src/components/charts/PercentileChip.tsx PercentileChipPopoverBody
      to: feedback_percentile_chip_tooltip_disclosure memory (Plan 13 amendment)
      via: bullet 4 contract — disclosure of composition (mixed / pure / suppression branches)
      pattern: "blending\\|chesscom_median_native\\|lichess_median_native"
---

<objective>
Close the API + frontend loop for the D-12 Reversal Amendment. The schema, service materialisation, TypeScript type, and `PercentileChip` tooltip body all flip from the source-platform / chesscom_raw_rating shape to the blended-composition shape. The tooltip's bullet 4 (rating-anchor disclosure) is rewritten to truthfully render the per-platform composition rather than a single "anchored on platform X" line.

Purpose: This is the user-visible payoff of the D-12 reversal. After this plan, the chip tooltip on `https://flawchess.com` discloses to a mixed-platform user that their anchor blends N chess.com games (median X, converted) with M lichess games (median Y), letting the user reason about why their anchor is a particular value. The HARD CONSTRAINT against flame icons from Plan 07 is preserved by regression test.

Output: 1 schema rewrite + 1 service-materialisation edit + 1 TypeScript type regen + 1 PercentileChip rewrite + 2-3 consumer component edits + 1 test file edit. After this plan, the entire backend-to-frontend D-12-reversal stack is mechanically coherent; Plan 12 backfills the data, Plan 13 closes documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-CONTEXT.md
@.planning/notes/percentile-anchor-d12-reversal.md
@app/schemas/endgames.py
@app/services/endgame_service.py
@app/repositories/user_rating_anchors_repository.py
@frontend/src/types/endgames.ts
@frontend/src/components/charts/PercentileChip.tsx
@frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
@frontend/src/components/charts/EndgameTimePressureCard.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/__tests__/PercentileChip.test.tsx
@frontend/src/lib/percentileAnchor.ts
@.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-07-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Verified anchors against current main:
     - app/schemas/endgames.py:27-49 (RatingAnchorOut — current shape)
     - app/schemas/endgames.py:885 (rating_anchors field on EndgameOverviewResponse)
     - app/services/endgame_service.py:64 (RatingAnchorOut import)
     - app/services/endgame_service.py:3030-3050 (rating_anchors materialisation block)
     - frontend/src/types/endgames.ts:359-371 (RatingAnchor TS interface — current shape with source_platform + chesscom_raw_rating + n_games)
     - frontend/src/components/charts/PercentileChip.tsx:98-110 (PercentileChipProps — current shape)
     - frontend/src/components/charts/PercentileChip.tsx:140-145 (PopoverBodyProps — current shape)
     - frontend/src/components/charts/PercentileChip.tsx:180-200 (bullet 4 conditional — current Lichess vs chess.com branch)
     - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx:198-260 (two PercentileChip call sites passing anchorSource + chesscomRawRating)
-->

New Pydantic schema (replaces app/schemas/endgames.py:27-49):
  class RatingAnchorOut(BaseModel):
      """Blended anchor + per-platform composition for the percentile chip tooltip.

      D-12 Reversal Amendment (CONTEXT 2026-05-27). Anchor is the game-weighted
      median of converted-chess.com + native-lichess ratings. The two
      native-median fields preserve composition for tooltip disclosure.
      """
      anchor_rating: int
      n_chesscom_games: int
      n_lichess_games: int
      chesscom_median_native: int | None = None
      lichess_median_native: int | None = None

New TypeScript type (replaces frontend/src/types/endgames.ts:359-371):
  export interface RatingAnchor {
    anchor_rating: number;
    n_chesscom_games: number;
    n_lichess_games: number;
    chesscom_median_native: number | null;
    lichess_median_native: number | null;
  }

New PercentileChipProps (replaces frontend/src/components/charts/PercentileChip.tsx:98-110):
  export interface PercentileChipProps {
    percentile: number;
    flavor: PercentileChipFlavor;
    tc?: TimeControlBucket;
    anchorRating: number;
    nChesscomGames: number;
    nLichessGames: number;
    chesscomMedianNative?: number;
    lichessMedianNative?: number;
    metricLabel: string;
    testId: string;
  }

New PercentileChipPopoverBody bullet 4 logic (TypeScript):
  function renderBullet4(
    anchorRating: number,
    nChesscomGames: number,
    nLichessGames: number,
    chesscomMedianNative: number | undefined,
    lichessMedianNative: number | undefined,
  ): string {
    if (nChesscomGames > 0 && nLichessGames > 0) {
      return `Anchored at ~${anchorRating} Elo, blending ${nChesscomGames} chess.com games (median ${chesscomMedianNative}, converted) with ${nLichessGames} lichess games (median ${lichessMedianNative}).`;
    }
    if (nChesscomGames === 0 && nLichessGames > 0) {
      return `Anchored at ~${anchorRating} Elo from ${nLichessGames} lichess games (native rating).`;
    }
    if (nChesscomGames > 0 && nLichessGames === 0) {
      return `Anchored at ~${anchorRating} Elo from ${nChesscomGames} chess.com games (median ${chesscomMedianNative}, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26).`;
    }
    return ''; // both zero — caller suppresses the chip; defensive fallback.
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite RatingAnchorOut Pydantic schema + endgame_service rating_anchors materialisation</name>
  <files>app/schemas/endgames.py, app/services/endgame_service.py</files>
  <read_first>
    - app/schemas/endgames.py (lines 1-100 + 870-900 — RatingAnchorOut + rating_anchors field on response)
    - app/services/endgame_service.py (offset 3000 limit 70 — the rating_anchors materialisation block around line 3030-3050)
    - app/repositories/user_rating_anchors_repository.py (post-Plan 09 — RatingAnchorRow new shape)
    - .planning/notes/percentile-anchor-d12-reversal.md (rationale section to cite in the schema docstring)
  </read_first>
  <action>
    **Part A — app/schemas/endgames.py RatingAnchorOut**: Replace the existing `RatingAnchorOut` class (currently ~lines 27-49) with the new 5-field shape per the `<interfaces>` block. Class docstring rewrites to describe the blended-anchor + composition disclosure semantics. Cites:
    - "D-12 Reversal Amendment (CONTEXT 2026-05-27)"
    - ".planning/notes/percentile-anchor-d12-reversal.md (rationale, risk profile)"
    - The 4 tooltip-disclosure branches at the consumer (PercentileChipPopoverBody)

    The Pydantic v2 syntax stays the same (`class RatingAnchorOut(BaseModel)`); the per-field type annotations + defaults match `<interfaces>` exactly. Both native-median fields are nullable with `= None` default so a pure-platform anchor row's missing-platform field serializes as `null` in the API response.

    **Part B — app/services/endgame_service.py rating_anchors materialisation**: Update the materialisation block (around line 3030-3050) to call `RatingAnchorOut(...)` with the new 5 kwargs. The current block reads:
    ```python
    rating_anchors: dict[TimeControlBucket, RatingAnchorOut] = {
        tc: RatingAnchorOut(
            anchor_rating=row.anchor_rating,
            source_platform=row.source_platform,
            chesscom_raw_rating=row.chesscom_raw_rating,
            n_games=row.n_games,
        )
        for tc, row in anchor_rows.items()
    }
    ```
    Replace with:
    ```python
    rating_anchors: dict[TimeControlBucket, RatingAnchorOut] = {
        tc: RatingAnchorOut(
            anchor_rating=row.anchor_rating,
            n_chesscom_games=row.n_chesscom_games,
            n_lichess_games=row.n_lichess_games,
            chesscom_median_native=row.chesscom_median_native,
            lichess_median_native=row.lichess_median_native,
        )
        for tc, row in anchor_rows.items()
    }
    ```

    The surrounding comment block (V4 mitigation reminder, the chesscom_raw_rating origin note around line 3030) rewrites: drop the chesscom_raw_rating sentence; replace with: "Per-platform game counts + native medians come from the blended anchor compute (`compute_anchors_for_user`, Plan 10) — they exist purely to support tooltip composition disclosure. V4 mitigation: `user_id` flows through the FastAPI-Users dep, never as a query parameter."

    Check for any other place in the file that touches `source_platform` or `chesscom_raw_rating` (grep) and update accordingly. If `pickDominantTcAnchor` / dominant-TC selection logic lives in this service, update it to use `(row.n_chesscom_games + row.n_lichess_games)` for the game-count tiebreak.
  </action>
  <verify>
    <automated>uv run ty check app/schemas/endgames.py app/services/endgame_service.py && uv run python -c "from app.schemas.endgames import RatingAnchorOut; r = RatingAnchorOut(anchor_rating=2046, n_chesscom_games=4000, n_lichess_games=100, chesscom_median_native=2200, lichess_median_native=1900); print(r.model_dump())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'source_platform\|chesscom_raw_rating' app/schemas/endgames.py` is 0.
    - `grep -c 'n_chesscom_games\|n_lichess_games\|chesscom_median_native\|lichess_median_native' app/schemas/endgames.py` is >= 4.
    - `grep -c 'D-12 Reversal\|Reversal Amendment\|percentile-anchor-d12-reversal' app/schemas/endgames.py` is >= 1.
    - `grep -c 'source_platform\|chesscom_raw_rating' app/services/endgame_service.py` is 0 (materialisation block updated).
    - `grep -c 'n_chesscom_games\|n_lichess_games' app/services/endgame_service.py` is >= 2.
    - The Python smoke test in the verify command instantiates RatingAnchorOut with the canonical 2046 / 4000 / 100 / 2200 / 1900 example successfully.
    - `uv run ty check app/schemas/endgames.py app/services/endgame_service.py` returns zero errors.
  </acceptance_criteria>
  <done>Backend schema + service materialisation rewritten; canonical example instantiates; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate (or manually update) frontend/src/types/endgames.ts RatingAnchor interface</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts (full file — line 359-371 has the current RatingAnchor interface)
    - app/schemas/endgames.py (post-Task 1 — RatingAnchorOut Pydantic source-of-truth)
  </read_first>
  <action>
    First, check if the project has an OpenAPI / TypeScript type generator: `grep -rn 'openapi-typescript\|openapi.*generate\|json.*to.*ts' package.json frontend/package.json scripts/ 2>/dev/null`. If a generator script exists, run it; the types regenerate automatically from the Pydantic schemas.

    If no generator (which is likely given the manual `interface` declaration), update `frontend/src/types/endgames.ts::RatingAnchor` by hand:

    Replace the current interface (around line 359-371) with the new 5-field shape per `<interfaces>` block. The docstring above the interface updates: DROP the Lichess-precedence / chesscom_raw_rating prose; ADD the blended-anchor + per-platform composition prose. Cite the amendment date + design-decision record path. Keep the snake_case field names (matches the API contract — Pydantic auto-serializes Python snake_case).

    Run `cd frontend && npx tsc --noEmit` after the edit. The consumer components (next tasks) will be ts-broken until they're updated; note the error list in /tmp for Task 3-5.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && grep -c 'n_chesscom_games\|n_lichess_games\|chesscom_median_native\|lichess_median_native' src/types/endgames.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'source_platform\|chesscom_raw_rating' frontend/src/types/endgames.ts` is 0 (legacy fields removed).
    - `grep -c 'n_chesscom_games\|n_lichess_games\|chesscom_median_native\|lichess_median_native' frontend/src/types/endgames.ts` is >= 4.
    - The interface field names match the Pydantic source-of-truth verbatim (snake_case).
    - The interface comment cites the D-12 Reversal Amendment.
    - `cd frontend && npx tsc --noEmit` may fail with errors in consumer files (handled by next tasks) — note the error count for follow-up; only the type definition file is asserted clean here.
  </acceptance_criteria>
  <done>RatingAnchor TS interface matches the Pydantic source-of-truth; consumer files queued for next tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite PercentileChip.tsx (PercentileChipProps + PercentileChipPopoverBody bullet 4 + tests) — ALL 4 composition branches with branch-differentiating substring assertions</name>
  <files>frontend/src/components/charts/PercentileChip.tsx, frontend/src/components/charts/__tests__/PercentileChip.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/PercentileChip.tsx (full file — anchor lines 98-110 for props, 140-145 for popover body props, 180-200 for bullet 4 conditional, 50+ for the constants block including COPY_FILTER_INDEPENDENCE which stays unchanged)
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx (full file — Test 9 the no-flame regression guard MUST be preserved verbatim; Tests 10-12 (bullet 4 variants) rewrite)
    - .planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-07-PLAN.md (Plan 07 — the existing tooltip body contract; bullets 1-3 unchanged; only bullet 4 changes)
    - feedback_percentile_chip_tooltip_disclosure.md (memory — disclosure-mandate context, will be updated in Plan 13)
  </read_first>
  <behavior>
    **Branch-differentiation contract (critical — locked by amendment review).** The amendment locks the *structure* of bullet 4 (4 prose branches: mixed / pure-lichess / pure-chess.com / suppression) but not the *exact wording*. The Vitest suite below MUST explicitly exercise ALL 4 composition branches AND assert branch-differentiating substrings so that a single-branch fallback (e.g., the implementation silently rendering the mixed-user copy for all callers) CANNOT pass tests. Each test names the substrings that MUST appear AND substrings that MUST NOT appear for that branch.

    - **Test C1 (mixed-user blended composition)**: `render(<PercentileChip percentile={50} flavor="score-gap" anchorRating={2046} nChesscomGames={4000} nLichessGames={100} chesscomMedianNative={2200} lichessMedianNative={1900} metricLabel="Endgame Score Gap" testId="t" />)`. Open the popover (mouse-enter the trigger).
        MUST APPEAR (branch-differentiating): both `"chess.com games"` AND `"lichess games"` (or `"Lichess games"` — case-insensitive match acceptable) substrings appear in the rendered bullet 4 text. AND the substrings `"4000"`, `"100"`, `"2200"`, `"1900"`, and `"2046"` all appear (all 5 numbers).
        Optional structural cue: `"blending"` or equivalent compositional verb appears (planner discretion — but at minimum the mixed branch must render BOTH platform mentions). This is the unique mixed-branch signature.

    - **Test C2 (pure-lichess)**: `nChesscomGames={0} nLichessGames={500} lichessMedianNative={1750}` (no chesscomMedianNative; chesscomMedianNative left undefined / not passed).
        MUST APPEAR (branch-differentiating): `"lichess games"` (case-insensitive) AND `"500"` AND `"1750"` AND `"native rating"` (or equivalent native-rating signal — the exact phrase under planner discretion, but the bullet MUST explicitly flag "no conversion applied" so the user understands the lichess-only path).
        MUST NOT APPEAR (branch-differentiation guard): the substring `"chess.com"` MUST NOT appear in bullet 4 (no chess.com clause). AND the substring `"converted"` MUST NOT appear in bullet 4 (no conversion mention — since no chess.com games were blended).

    - **Test C3 (pure-chess.com)**: `nChesscomGames={1500} nLichessGames={0} chesscomMedianNative={1830}` (no lichessMedianNative).
        MUST APPEAR (branch-differentiating): `"chess.com games"` AND `"1500"` AND `"1830"` AND `"converted"` (or `"convert"` — branch signals the conversion is applied) AND `"ChessGoals snapshot 2026-05-26"` (the snapshot date citation stays — preserved from Plan 07 contract).
        MUST NOT APPEAR (branch-differentiation guard): the substring `"lichess games"` (case-insensitive) MUST NOT appear in bullet 4 (no lichess clause). AND no native-lichess-median number should appear in bullet 4.

    - **Test C4 (no flame regression — preserved verbatim from Plan 07 Test 9)**: parametrize over `[1, 23, 50, 75, 90, 95, 99]`. For each: `queryByTestId(/flame/i) === null` AND `container.querySelector('[class*="flame"], [class*="Flame"]') === null`.

    - **Test C5 (props deprecation — sanity guard)**: the component MUST NOT accept `anchorSource` or `chesscomRawRating` as valid props. ts-expect-error block confirms compile-time rejection (or runtime assertion if ts-expect-error is too brittle).

    - **Test C6 (suppression — NEW under amendment review)**: `nChesscomGames={0} nLichessGames={0}`. The parent component (the caller of PercentileChip) is expected to NOT render the chip at all in this state, per amendment §Suppression rule. The test covers this contract from one of two angles, depending on where suppression lives:
        (a) If suppression lives at the PercentileChip level (defensive — the component itself returns null when both counts are 0): assert `render(<PercentileChip ... nChesscomGames={0} nLichessGames={0} />)` produces no rendered output for the chip (`queryByTestId('t') === null`).
        (b) If suppression lives at the caller and PercentileChip's bullet 4 returns the empty-string defensive fallback: assert the rendered bullet 4 content is the empty string (or that bullet 4 is not rendered to the DOM) AND that neither `"chess.com games"` NOR `"lichess games"` appears in the popover body when both counts are 0. ADDITIONALLY, assert that no parent-component call site passes `nChesscomGames=0 nLichessGames=0` to PercentileChip (grep-style assertion or call-site review note in the test comments — this codifies the suppression-at-caller contract).
        Branch differentiation: the suppression branch produces NEITHER the mixed-branch substring `"blending"` NOR the pure-platform substring `"Anchored at"` (the empty-string defensive fallback or the absent-chip outcome).

    - All Plan 07 Tests 1-8 (chip face, MIN_PERCENT floor, p99 ceiling, color band, aria-label, per-TC popover bullet 1, aggregated popover bullet 1, bullet 3 filter independence) MUST CONTINUE TO PASS — they don't touch bullet 4.
  </behavior>
  <action>
    **Part A — PercentileChip.tsx**:

    1. Update `PercentileChipProps` interface (lines 98-110) per `<interfaces>` block. DROP `anchorSource` and `chesscomRawRating`. ADD `nChesscomGames`, `nLichessGames`, `chesscomMedianNative`, `lichessMedianNative`.

    2. Update `PopoverBodyProps` interface (lines 140-145) symmetrically — drop the two old fields, add the four new ones.

    3. Update the `PercentileChipPopoverBody` component (lines 180-200 area). Bullets 1, 2, 3 stay verbatim. Replace bullet 4 with the conditional from `<interfaces>`:
       ```tsx
       const bullet4 = (() => {
         if (nChesscomGames > 0 && nLichessGames > 0) {
           return `Anchored at ~${anchorRating} Elo, blending ${nChesscomGames} chess.com games (median ${chesscomMedianNative}, converted) with ${nLichessGames} lichess games (median ${lichessMedianNative}).`;
         }
         if (nChesscomGames === 0 && nLichessGames > 0) {
           return `Anchored at ~${anchorRating} Elo from ${nLichessGames} lichess games (native rating).`;
         }
         if (nChesscomGames > 0 && nLichessGames === 0) {
           return `Anchored at ~${anchorRating} Elo from ${nChesscomGames} chess.com games (median ${chesscomMedianNative}, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26).`;
         }
         return ''; // defensive — caller should have suppressed the chip
       })();
       ```

    4. Update the parent `PercentileChip` component's prop destructuring + the `PercentileChipPopoverBody` invocation to pass the new fields. Remove all references to `anchorSource` and `chesscomRawRating` from the component file. Update the JSDoc comment block at lines 19+105 to describe the blended composition (no Lichess-vs-chess.com branching prose).

    5. **HARD CONSTRAINT preserved**: no flame icon. Verify final file: `grep -c '[fF]lame' frontend/src/components/charts/PercentileChip.tsx` returns 0 (or only matches inside comments explicitly referencing the prohibition). No `Flame` import from `lucide-react`. No `TIER_BREAKPOINTS`.

    6. The aria-label code path is unchanged — direction word for screen readers stays.

    7. Update the JSDoc block describing PercentileChip's tooltip body to enumerate the 4 branches (mixed, pure-lichess, pure-chess.com, suppression). Cite the D-12 Reversal Amendment + memory `feedback_percentile_chip_tooltip_disclosure` (which Plan 13 will amend to match this new contract).

    **Part B — __tests__/PercentileChip.test.tsx**:

    Update the test file:

    1. PRESERVE Tests 1-8 from Plan 07 verbatim — they test chip face / aria-label / bullets 1-3, none of which change.

    2. PRESERVE Test 9 (the no-flame regression guard) VERBATIM. Do not weaken; do not remove. This is the locked guard against commit `6766898c` regression.

    3. REPLACE Tests 10-12 (bullet 4 Lichess-anchored / chess.com-anchored variants) with the new branch-differentiation suite (Tests C1, C2, C3, C6) per the `<behavior>` block. The mixed / pure-lichess / pure-chess.com / suppression QUADRUPLE replaces the old binary split. Each test asserts BOTH the substrings that MUST appear for its branch AND the substrings that MUST NOT appear (branch-differentiation guard). This is the critical hardening — a single-branch fallback implementation cannot silently pass tests because at least one MUST-NOT-APPEAR assertion in C2 or C3 will fire.

    4. ADD Test C5 (deprecation sanity guard). Use `// @ts-expect-error` to assert the OLD prop names (`anchorSource`, `chesscomRawRating`) are now rejected. If `@ts-expect-error` is fragile, fall back to a runtime sanity assertion that simply re-renders with the new props and asserts the popover body shape.

    5. ADD Test C6 (suppression) per the `<behavior>` block. The test covers the chip-not-rendered contract from either the component level (defensive suppression) or the caller level (the rendered popover body excludes both platforms' substrings).

    6. KEEP Test 13 (filter independence bullet 3) verbatim.

    Run `cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'anchorSource\|chesscomRawRating' frontend/src/components/charts/PercentileChip.tsx` is 0.
    - `grep -c 'nChesscomGames\|nLichessGames\|chesscomMedianNative\|lichessMedianNative' frontend/src/components/charts/PercentileChip.tsx` is >= 4.
    - `grep -c 'blending\|chess.com games' frontend/src/components/charts/PercentileChip.tsx` is >= 1 (mixed-user copy compositional verb OR platform mention).
    - `grep -c 'native rating' frontend/src/components/charts/PercentileChip.tsx` is >= 1 (pure-lichess copy).
    - `grep -c 'ChessGoals snapshot 2026-05-26' frontend/src/components/charts/PercentileChip.tsx` is >= 1 (pure-chess.com copy still cites snapshot date).
    - `grep -c '[fF]lame' frontend/src/components/charts/PercentileChip.tsx` is 0 (HARD CONSTRAINT preserved).
    - `grep -c 'D-12 Reversal\|Reversal Amendment' frontend/src/components/charts/PercentileChip.tsx` is >= 1 (citation).
    - **All 4 composition branches exercised explicitly**: Test C1 (mixed), C2 (pure-lichess), C3 (pure-chess.com), and C6 (suppression) all exist as separate `test(...)` / `it(...)` cases in `__tests__/PercentileChip.test.tsx`. Verify by grep: `grep -cE "(test|it)\\(.*'C[1236]\\b" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is >= 4, OR test names use descriptive substrings — `grep -cE "(mixed|pure-lichess|pure-chess\\.com|suppression)" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is >= 4.
    - **Branch-differentiation assertions present (MUST NOT APPEAR substrings)**: in the pure-lichess test (C2), an assertion of the form `expect(bullet4Text).not.toMatch(/chess\\.com/i)` (or equivalent `not.toContain`) is present. In the pure-chess.com test (C3), `expect(bullet4Text).not.toMatch(/lichess games/i)` (or equivalent) is present. Verify by grep: `grep -cE "not\\.toMatch|not\\.toContain|not\\.to(Have|Be|Equal)" frontend/src/components/charts/__tests__/PercentileChip.test.tsx` is >= 2 (one in C2, one in C3 at minimum).
    - Test C1 / C2 / C3 / C4 (no-flame) / C5 / C6 all pass.
    - Plan 07 Tests 1-8 + 13 still pass.
    - `cd frontend && npx tsc --noEmit src/components/charts/PercentileChip.tsx` (or full tsc) is clean on this file (consumer files broken — handled next).
  </acceptance_criteria>
  <done>PercentileChip + its test file rewritten for blended composition; no-flame regression guard preserved; ALL 4 composition branches of bullet 4 covered with branch-differentiating substring assertions (positive AND negative).</done>
</task>

<task type="auto">
  <name>Task 4: Update consumer components (EndgameOverallPerformanceSection, EndgameTimePressureCard, EndgameMetricCard) + percentileAnchor helper</name>
  <files>frontend/src/components/charts/EndgameOverallPerformanceSection.tsx, frontend/src/components/charts/EndgameTimePressureCard.tsx, frontend/src/components/charts/EndgameMetricCard.tsx, frontend/src/lib/percentileAnchor.ts</files>
  <read_first>
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx (current — lines 198-260 have the two PercentileChip call sites with anchorSource + chesscomRawRating)
    - frontend/src/components/charts/EndgameTimePressureCard.tsx (full — find PercentileChip call sites via grep)
    - frontend/src/components/charts/EndgameMetricCard.tsx (full — find PercentileChip call sites via grep)
    - frontend/src/lib/percentileAnchor.ts (if exists per Plan 07 Task 4 — pickDominantTcAnchor helper)
    - frontend/src/types/endgames.ts (post-Task 2 — new RatingAnchor shape)
  </read_first>
  <action>
    Update every `<PercentileChip ...>` call site in the frontend codebase. Find them via: `cd frontend && grep -rn '<PercentileChip' src/`.

    For each call site, the prop change is mechanical:
    - REMOVE `anchorSource={...}` and `chesscomRawRating={...}` props.
    - ADD `nChesscomGames={anchor.n_chesscom_games}`, `nLichessGames={anchor.n_lichess_games}`, `chesscomMedianNative={anchor.chesscom_median_native ?? undefined}`, `lichessMedianNative={anchor.lichess_median_native ?? undefined}`.
    - KEEP `anchorRating={anchor.anchor_rating}` (unchanged).
    - KEEP all other props (`flavor`, `tc`, `percentile`, `metricLabel`, `testId`).

    For `frontend/src/lib/percentileAnchor.ts` `pickDominantTcAnchor` (or whatever the helper is named — verify via Plan 07 Task 4 / grep): update the dominance computation. The old code probably did `Math.max(...anchors.map(a => a.n_games))` — replace with `Math.max(...anchors.map(a => a.n_chesscom_games + a.n_lichess_games))`. Same algorithm, new field combination. The function signature stays the same (returns the picked anchor or undefined); the docstring updates to describe the field change.

    Apply CLAUDE.md "Always apply changes to mobile too" — if any of these components has separate desktop and mobile renderings, update BOTH.

    After updates, `cd frontend && npx tsc --noEmit` must be fully clean.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit && grep -rn 'anchorSource\|chesscomRawRating' src/ | grep -v '__tests__' || echo 'no anchorSource/chesscomRawRating remaining in production code'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'anchorSource\|chesscomRawRating' frontend/src/` matches NOTHING in production code (no matches outside `__tests__/` directory, and `__tests__` matches MUST refer to deprecation guards — Test C5).
    - `grep -rn 'nChesscomGames\|nLichessGames' frontend/src/components/` returns matches in every component that calls `<PercentileChip>`.
    - `pickDominantTcAnchor` (if exists) computes dominance via the sum of the two new game-count fields.
    - `cd frontend && npx tsc --noEmit` returns zero errors.
  </acceptance_criteria>
  <done>All consumer components pass the new 4 props; pickDominantTcAnchor updated; tsc fully clean.</done>
</task>

<task type="auto">
  <name>Task 5: Plan 11 Pre-PR gate — full backend + frontend Pre-PR checklist</name>
  <files>(no file modified — verification only)</files>
  <read_first>
    - CLAUDE.md §"Pre-PR checklist" (full gate list — both backend and frontend, MANDATORY before push)
  </read_first>
  <action>
    Run the full Pre-PR checklist per CLAUDE.md. The Wave 1 + Wave 2 cluster (Plans 09 + 10 + 11) is now fully coherent; all gates must be green.

    Backend:
    1. `uv run ruff format app/ tests/`
    2. `uv run ruff check app/ tests/ --fix`
    3. `uv run ty check app/ tests/`
    4. `uv run pytest -x`

    Frontend:
    5. `cd frontend && npm run lint`
    6. `cd frontend && npm test -- --run`
    7. `cd frontend && npm run knip`
    8. `cd frontend && npx tsc --noEmit`

    All MUST be green. If anything fails:
    - For Sentry / type / import errors — fix in the same task (likely a leftover reference to an old field name).
    - For test failures referencing OLD RatingAnchorOut / RatingAnchor field names — find the test (likely `app/tests/test_endgame_service.py` or `frontend/src/components/.../__tests__/`), update it to use the new field names.
    - For knip failures — likely unused exports after field rename; clean up.

    Note: the Plan 11 squash-merge PR ships the full D-12 reversal stack (Plans 09 + 10 + 11). The user signs off on this PR before Plan 12 (backfill) runs.
  </action>
  <verify>
    <automated>uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -x && cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run lint && npm test -- --run && npm run knip && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 8 Pre-PR gates green.
    - No leftover references to `source_platform`, `chesscom_raw_rating`, `AnchorSource` in production code (`app/`, `frontend/src/` excluding `__tests__` deprecation guards).
    - `npm run knip` clean — no new dead exports.
    - `npx tsc --noEmit` zero errors.
    - The full Pydantic + TypeScript contract for the new blended-anchor shape matches across the API boundary.
  </acceptance_criteria>
  <done>Wave 1 + Wave 2 cluster (Plans 09 + 10 + 11) is fully coherent; all gates green; ready to PR + ready for Plan 12 backfill execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> frontend | The 5 anchor fields ship through the typed `rating_anchors` block on `EndgameOverviewResponse`; frontend treats as display-only. |
| User -> DOM | Tooltip body strings are template-interpolated with backend-supplied ints. React's auto-escaping handles string values. No user-controlled HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.4-11-01 | Tampering | Flame icon regression | mitigate | Vitest Test C4 (the preserved Plan 07 Test 9) explicitly asserts no flame at every percentile in `[1, 23, 50, 75, 90, 95, 99]`. CI failure if reintroduced. |
| T-94.4-11-02 | Information disclosure | Tooltip discloses another user's anchor + composition | mitigate | Anchor props sourced from `EndgameOverviewResponse.rating_anchors` which is filtered by `current_user.id` at the API layer (V4 mitigation preserved). |
| T-94.4-11-03 | Tampering | XSS via composition tooltip string interpolation | mitigate | React's JSX auto-escapes string values. All interpolated values are typed `number` from the backend schema (Pydantic-validated ints). |
| T-94.4-11-SC | Tampering | npm package installs | accept | No new npm packages introduced. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/charts/__tests__/PercentileChip.test.tsx` green (Plan 07 Tests 1-9 preserved + new C1/C2/C3/C6 for the 4 composition branches WITH branch-differentiating substring assertions).
- `cd frontend && npm run knip` clean.
- `cd frontend && npx tsc --noEmit` clean.
- `uv run pytest -x` green.
- `uv run ty check app/ tests/` clean.
- No leftover `source_platform` / `chesscom_raw_rating` / `anchorSource` / `chesscomRawRating` references in production code.
- Tooltip bullet 4 renders the blended composition for mixed users (BOTH platform substrings appear), drops the chess.com clause for pure-lichess users (chess.com substring NOT present), and drops the lichess clause for pure-chess.com users (lichess substring NOT present). Suppression branch renders neither.
- HARD CONSTRAINT (no flame) preserved verbatim.
</verification>

<success_criteria>
PRPCR-03 + PRPCR-07 ROADMAP scope is now fully implemented on the API + frontend side. The full Wave 1 + Wave 2 D-12 reversal stack (Plans 09 + 10 + 11) lands as one PR with all gates green. The chip tooltip on a mixed-platform user now truthfully discloses the per-platform composition (N chess.com games at median X converted, M lichess games at median Y), enabling the user to reason about why their anchor is a particular value. All 4 composition branches are locked by branch-differentiating Vitest assertions — a single-branch fallback implementation cannot silently pass tests. Plan 12 (backfill execution) can run on dev immediately after merge, and on prod after HUMAN-UAT.
</success_criteria>

<output>
Create `.planning/phases/94.4-peer-relative-percentile-chip-refinement/94.4-11-SUMMARY.md` when done. Record: schema + service-materialisation diffs; TS type regen (manual or generator); PercentileChipPopoverBody bullet 4 final rendered text for ALL 4 branches (mixed / pure-lichess / pure-chess.com / suppression); confirm no-flame regression test still asserts on `[1, 23, 50, 75, 90, 95, 99]`; transcribe the actual branch-differentiation assertions used in C2 and C3 (the `not.toMatch(/chess\\.com/i)` and `not.toMatch(/lichess games/i)` lines); full Pre-PR gate output snippets (ruff / ty / pytest / lint / knip / tsc).
</output>
</output>
