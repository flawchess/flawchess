---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: "09"
subsystem: backend/database
tags: [schema, alembic, orm, repository, test, d12-reversal, blended-anchor]
dependency_graph:
  requires: []
  provides:
    - user_rating_anchors blended-anchor schema (migration c70f5d94b243)
    - UserRatingAnchor ORM model (blended columns)
    - RatingAnchorRow dataclass (blended fields)
    - upsert_anchor / fetch_anchors_for_user (new signatures)
  affects:
    - app/services/user_benchmark_percentiles_service.py (Plan 10 consumer — BROKEN until Plan 10)
    - app/schemas/endgames.py (Plan 11 consumer — BROKEN until Plan 11)
    - app/services/endgame_service.py (Plan 11 consumer — BROKEN until Plan 11)
    - frontend/src/types/endgames.ts (Plan 11 consumer — BROKEN until Plan 11)
    - scripts/backfill_user_percentiles.py (Plan 12 consumer — BROKEN until Plan 12)
tech_stack:
  added: []
  patterns:
    - Alembic drop-and-recreate migration for derived tables (D-02 sanction)
    - Per-platform game-count + native-median columns for tooltip disclosure (D-07)
key_files:
  created:
    - alembic/versions/20260527_125014_c70f5d94b243_reshape_user_rating_anchors_for_blended_.py
  modified:
    - app/models/user_rating_anchors.py
    - app/repositories/user_rating_anchors_repository.py
    - tests/repositories/test_user_rating_anchors_repository.py
decisions:
  - "Drop-and-recreate migration chosen over ALTER for simplicity (D-02 sanction)"
  - "No data preservation needed (derived table; Plan 12 backfill restores rows)"
  - "downgrade() is a no-op stub per CONTEXT D-02"
  - "n_chesscom_games / n_lichess_games are NOT NULL (zero when no games, never NULL)"
  - "chesscom_median_native / lichess_median_native are NULL when respective count is 0"
metrics:
  duration: "~35 minutes"
  completed: "2026-05-27"
  tasks_completed: 6
  files_changed: 4
---

# Phase 94.4 Plan 09: user_rating_anchors Schema Reshape (D-12 Reversal) Summary

Reshaped `user_rating_anchors` per the D-12 Reversal Amendment (2026-05-27). The game-weighted blended anchor replaces the original Lichess-precedence rule. Schema sheds `source_platform` + `chesscom_raw_rating` + `n_games` and gains `n_chesscom_games`, `n_lichess_games`, `chesscom_median_native`, `lichess_median_native` for both the blended compute and tooltip disclosure.

## Consumer Audit (Task 1)

Grep hit count: 189 matches across app/ tests/ scripts/ frontend/.

| Category | Files | Count |
|----------|-------|-------|
| **DROP** (this plan rewrites) | `app/models/user_rating_anchors.py`, `app/repositories/user_rating_anchors_repository.py`, `tests/repositories/test_user_rating_anchors_repository.py` | ~50 hits |
| **CONSUMER - Plan 10** (anchor compute rewrite) | `app/services/user_benchmark_percentiles_service.py` (imports `AnchorSource`, uses `source_platform`/`chesscom_raw_rating`), `app/services/canonical_slice_sql.py` (comment), `scripts/backfill_user_percentiles.py` (imports `AnchorSource`, `_ALL_ANCHOR_SOURCES`, `source_platform` SQL) | ~60 hits |
| **CONSUMER - Plan 11** (API + frontend) | `app/schemas/endgames.py` (`source_platform: Literal[...]`, `chesscom_raw_rating`), `app/services/endgame_service.py` (reads `row.source_platform`, `row.chesscom_raw_rating`), `frontend/src/types/endgames.ts`, `frontend/src/components/charts/EndgameTimePressureCard.tsx`, `EndgameOverallPerformanceSection.tsx`, `EndgameMetricCard.tsx`, test fixtures | ~65 hits |
| **CONSUMER - Plan 12** | `scripts/backfill_user_percentiles.py` (also references anchor summary per TC x source_platform) | ~14 hits (overlap with Plan 10) |
| **STALE** (plan files, comments only) | `tests/services/test_user_benchmark_percentiles_service.py`, `tests/scripts/test_backfill_user_percentiles.py` — old schema references in comments/docstrings | ~10 hits |

Files that Plan 09 LEAVES BROKEN (imports `AnchorSource` which no longer exists):
- `app/services/user_benchmark_percentiles_service.py:93` — fixed in Plan 10
- `scripts/backfill_user_percentiles.py:139` — fixed in Plan 12

These broken imports prevent pytest collection until Plan 10 + 11 + 12 land. Pytest gate runs at the Plan 11 boundary per the Wave 1 broken-state cluster design.

## Migration (Task 2)

**Revision ID:** `c70f5d94b243`
**Parent revision:** `4c42ebc87b7f` (rename_section2_benchmark_metric_values)
**File:** `alembic/versions/20260527_125014_c70f5d94b243_reshape_user_rating_anchors_for_blended_.py`

Verified schema after `uv run alembic upgrade head`:

```
\d user_rating_anchors
  user_id                integer         NOT NULL (PK, FK -> users.id CASCADE)
  time_control_bucket    USER-DEFINED    NOT NULL (PK, timecontrolbucket enum)
  anchor_rating          integer         NOT NULL
  n_chesscom_games       integer         NOT NULL
  n_lichess_games        integer         NOT NULL
  chesscom_median_native integer         YES (NULL when n_chesscom_games == 0)
  lichess_median_native  integer         YES (NULL when n_lichess_games == 0)
  computed_at            timestamptz     NOT NULL
```

- `anchor_source` Postgres ENUM type: **GONE** (`SELECT typname FROM pg_type WHERE typname='anchor_source'` returns 0 rows)
- `user_benchmark_percentiles` rows after upgrade: **0** (TRUNCATED)
- Old columns `source_platform`, `chesscom_raw_rating`, `n_games`: **GONE**

## Narrow Sanity Gates (Task 6)

- ruff format: 4 files already formatted
- ruff check: all checks passed
- ty check (model + repository + test): all checks passed
- `uv run alembic upgrade head`: no-op (already at c70f5d94b243)

**Note:** `uv run pytest tests/repositories/test_user_rating_anchors_repository.py` CANNOT run at Plan 09 boundary because the conftest imports `app.main` which transitively imports `user_benchmark_percentiles_service.py` which imports `AnchorSource` (now gone). This is the intended broken-state cluster; pytest gate runs at Plan 11 boundary.

## Canonical Test 1 Numbers (Regression Checkpoint)

From `.planning/notes/percentile-anchor-d12-reversal.md` "The problem" / "The fix":

```python
RatingAnchorRow(
    anchor_rating=2046,        # blended median: (4000*2050 + 100*1900) / 4100 ≈ 2046
    n_chesscom_games=4000,
    n_lichess_games=100,
    chesscom_median_native=2200,   # pre-conversion chess.com median
    lichess_median_native=1900,    # native lichess median
)
```

This is locked in `test_blended_mixed_user` as a round-trip regression assertion.

## Deviations from Plan

None -- plan executed exactly as written. The only deviation acknowledgement is that `uv run pytest` cannot complete at Plan 09 boundary (known broken-state, documented in plan under Task 6 "DO NOT MERGE Plan 09 STANDALONE"). The wave continues with Plans 10 + 11 before pytest gates run.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries beyond what the plan's threat model covers. T-94.4-09-01 through T-94.4-09-03 all mitigated:
- V4 keyword-only `user_id` preserved in both repository helpers
- `anchor_source` ENUM explicitly dropped in migration
- `user_benchmark_percentiles` TRUNCATEd before `user_rating_anchors` drop

## Self-Check: PASSED

Files created/modified:

- `/home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-a8e70f2e673d3a014/alembic/versions/20260527_125014_c70f5d94b243_reshape_user_rating_anchors_for_blended_.py` -- FOUND (128 lines)
- `/home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-a8e70f2e673d3a014/app/models/user_rating_anchors.py` -- FOUND
- `/home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-a8e70f2e673d3a014/app/repositories/user_rating_anchors_repository.py` -- FOUND
- `/home/aimfeld/Projects/Python/flawchess/.claude/worktrees/agent-a8e70f2e673d3a014/tests/repositories/test_user_rating_anchors_repository.py` -- FOUND

Commits present:
- `d43759af` -- chore(94.4-09): add reshape_user_rating_anchors_for_blended_anchor migration (Tasks 1+2)
- `98e007de` -- refactor(94.4-09): rewrite user_rating_anchors ORM model for blended schema (Task 3)
- `c2a4a76f` -- refactor(94.4-09): rewrite user_rating_anchors_repository for blended schema (Task 4)
- `141e043b` -- test(94.4-09): rewrite test_user_rating_anchors_repository for blended schema (Task 5)
