---
phase: 94.4-peer-relative-percentile-chip-refinement
plan: 11
subsystem: api-schema + frontend-chip
tags: [d12-reversal, blended-anchor, percentile-chip, tooltip-disclosure, tdd]
dependency_graph:
  requires: [94.4-09, 94.4-10]
  provides: [blended-anchor-api-schema, percentile-chip-blended-disclosure]
  affects: [94.4-12, 94.4-13]
tech_stack:
  added: []
  patterns:
    - D-12 Reversal Amendment: blended-anchor shape replaces source-platform/chesscom_raw_rating/n_games shape
    - 4-branch bullet-4 conditional (mixed / pure-lichess / pure-chess.com / suppression)
    - Branch-differentiating MUST-NOT-APPEAR Vitest assertions (C2 and C3 guards)
    - TDD RED/GREEN cycle for PercentileChip component rewrite
key_files:
  created: []
  modified:
    - app/schemas/endgames.py
    - app/services/endgame_service.py
    - frontend/src/types/endgames.ts
    - frontend/src/components/charts/PercentileChip.tsx
    - frontend/src/components/charts/__tests__/PercentileChip.test.tsx
    - frontend/src/components/charts/EndgameOverallPerformanceSection.tsx
    - frontend/src/components/charts/EndgameTimePressureCard.tsx
    - frontend/src/components/charts/EndgameMetricCard.tsx
    - frontend/src/lib/percentileAnchor.ts
decisions:
  - "RatingAnchorOut TS type name kept as RatingAnchorOut (matching existing export convention) rather than renaming to RatingAnchor per plan interface block — plan interface block was illustrative"
  - "Suppression branch renders bullet-4 as empty string and excludes the <p> element from DOM (if bullet4 !== '' gate) rather than rendering an empty paragraph"
  - "C5 deprecation test uses @ts-expect-error blocks to assert old prop names are compile-time rejected; this is the most direct TypeScript-level guard"
metrics:
  duration: ~25 minutes
  completed_date: "2026-05-27"
  tasks_completed: 5
  files_changed: 9
---

# Phase 94.4 Plan 11: API Schema + Frontend PercentileChip Blended Anchor Disclosure Summary

Closed the API-to-frontend loop for the D-12 Reversal Amendment. `RatingAnchorOut` Pydantic schema, service materialisation, TypeScript type, and `PercentileChip` tooltip bullet 4 all adopt the blended-composition shape. The chip tooltip on a mixed-platform user now truthfully discloses per-platform composition (N chess.com games at median X converted, M lichess games at median Y).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | RatingAnchorOut schema + service materialisation | 92c113f3 | app/schemas/endgames.py, app/services/endgame_service.py |
| 2 | RatingAnchorOut TS interface regen | 4944f254 | frontend/src/types/endgames.ts |
| 3 (RED) | PercentileChip tests — RED phase | ab80bd94 | frontend/src/components/charts/__tests__/PercentileChip.test.tsx |
| 3 (GREEN) | PercentileChip.tsx implementation | 46770e49 | frontend/src/components/charts/PercentileChip.tsx |
| 4 | Consumer components + percentileAnchor helper | bb175626 | EndgameOverallPerformanceSection.tsx, EndgameTimePressureCard.tsx, EndgameMetricCard.tsx, frontend/src/lib/percentileAnchor.ts |
| 5 | Pre-PR gate (all 8 gates green) | — | (no new files; all gates clean) |

## What Was Built

### Backend Schema (app/schemas/endgames.py)

`RatingAnchorOut` rewritten per the D-12 Reversal Amendment:

**Dropped:** `source_platform: Literal["lichess", "chesscom"]`, `chesscom_raw_rating: int | None`, `n_games: int`

**Added:** `n_chesscom_games: int`, `n_lichess_games: int`, `chesscom_median_native: int | None = None`, `lichess_median_native: int | None = None`

**Kept:** `anchor_rating: int`

Class docstring documents all 4 disclosure branches, cites D-12 Reversal Amendment (CONTEXT 2026-05-27) and `.planning/notes/percentile-anchor-d12-reversal.md`.

### Service Materialisation (app/services/endgame_service.py)

`rating_anchors` dict comprehension around line 3025 updated to pass the 5 new kwargs. Comment block rewritten: drops `chesscom_raw_rating` origin note; replaces with "Per-platform game counts + native medians come from the blended anchor compute (`compute_anchors_for_user`, Plan 10) — they exist purely to support tooltip composition disclosure."

### TypeScript Type (frontend/src/types/endgames.ts)

`RatingAnchorOut` interface updated (manual edit — no OpenAPI generator in this project):

**Dropped:** `source_platform: 'lichess' | 'chesscom'`, `chesscom_raw_rating: number | null`, `n_games: number`

**Added:** `n_chesscom_games: number`, `n_lichess_games: number`, `chesscom_median_native: number | null`, `lichess_median_native: number | null`

Docstring updated with all 4 branch examples and D-12 Reversal Amendment citation.

### PercentileChip.tsx

**Props changed:**

- Dropped: `anchorSource: 'lichess' | 'chesscom'`, `chesscomRawRating?: number`
- Added: `nChesscomGames: number`, `nLichessGames: number`, `chesscomMedianNative?: number`, `lichessMedianNative?: number`

**Bullet 4 final rendered text by branch:**

(a) **Mixed user** (nChesscomGames=4000, nLichessGames=100, chesscomMedianNative=2200, lichessMedianNative=1900, anchorRating=2046):
> "Anchored at ~2046 Elo, blending 4000 chess.com games (median 2200, converted) with 100 lichess games (median 1900)."

(b) **Pure-lichess** (nChesscomGames=0, nLichessGames=500, lichessMedianNative=1750, anchorRating=1750):
> "Anchored at ~1750 Elo from 500 lichess games (native rating)."

(c) **Pure-chess.com** (nChesscomGames=1500, nLichessGames=0, chesscomMedianNative=1830, anchorRating=1920):
> "Anchored at ~1920 Elo from 1500 chess.com games (median 1830, converted to Lichess-equivalent via ChessGoals snapshot 2026-05-26)."

(d) **Suppression** (both counts == 0): bullet 4 `<p>` element not rendered to DOM (empty-string defensive fallback; caller should suppress the chip).

**HARD CONSTRAINT:** No `Flame` import, no `TIER_BREAKPOINTS`. `grep -c '[fF]lame' PercentileChip.tsx` returns 4 — all in comments referencing the prohibition by name.

### Vitest Tests

**Preserved verbatim:** Plan 07 Tests 1-8 (chip face, floor, ceiling, color band, aria-label, per-TC/aggregated bullet 1), Test 9 / C4 (no-flame regression guard for commit-6766898c at percentiles [1, 23, 50, 75, 90, 95, 99]).

**Replaced Tests 10-12** with branch-differentiation quadruple:

**C1 (mixed):** Asserts `"chess.com games"` AND `"lichess games"` AND `"blending"` AND all 5 numbers (4000, 100, 2200, 1900, 2046).

**C2 (pure-lichess):** Asserts `"lichess games"` AND `"500"` AND `"1750"` AND `"native rating"`.
MUST-NOT-APPEAR guards:
```
expect(body).not.toMatch(/chess\.com/i);  // No chess.com clause
expect(body).not.toContain('converted');  // No conversion mention
```

**C3 (pure-chess.com):** Asserts `"chess.com games"` AND `"1500"` AND `"1830"` AND `"converted"` AND `"ChessGoals snapshot 2026-05-26"`.
MUST-NOT-APPEAR guard:
```
expect(body).not.toMatch(/lichess games/i);  // No lichess clause
```

**C5 (deprecation sanity):** `@ts-expect-error` blocks assert `anchorSource` and `chesscomRawRating` are compile-time rejected.

**C6 (suppression):** Asserts neither `"blending"` NOR `"Anchored at"` NOR platform game substrings appear when both counts are 0.

Total: 41 tests pass (was 38 from Plan 07 + 3 new composition branches).

### Consumer Components

Updated 6 call sites across 3 files to pass the 4 new props. `pickDominantTcAnchor` in `percentileAnchor.ts` updated to compute dominance via `n_chesscom_games + n_lichess_games` sum.

## Pre-PR Gate Results

All 8 gates green:

1. `uv run ruff format app/ tests/` — 183 files unchanged
2. `uv run ruff check app/ tests/ --fix` — All checks passed
3. `uv run ty check app/ tests/` — All checks passed (zero errors)
4. `uv run pytest -x` — 2180 passed, 17 skipped, 2 warnings in 38.83s
5. `cd frontend && npm run lint` — clean
6. `cd frontend && npm test -- --run` — 677 passed (56 test files)
7. `cd frontend && npm run knip` — clean
8. `cd frontend && npx tsc --noEmit` — zero errors

Note: `test_backfill_user_percentiles.py` emits a PytestDeprecationWarning (not an error) because `scripts/backfill_user_percentiles.py` still imports `AnchorSource` (Plan 12 scope, per Plan 09 SUMMARY). This is expected and pre-existing.

## Deviations from Plan

None — plan executed exactly as written.

Minor implementation notes (not deviations):
- TypeScript type kept named `RatingAnchorOut` (existing export name) rather than the plan's interface block name `RatingAnchor` — the plan's interface block was illustrative, not a rename directive.
- `bullet4 !== ''` gate added to avoid rendering an empty `<p>` in the suppression branch — cleaner DOM than an empty paragraph.

## Known Stubs

None. The full API-to-frontend D-12 reversal stack is mechanically coherent. Plan 12 backfills production data; Plan 13 closes documentation.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. T-94.4-11-01 through T-94.4-11-SC all mitigated per the plan's threat register:

- T-94.4-11-01 (flame icon regression): Vitest Test C4 asserts no flame at [1, 23, 50, 75, 90, 95, 99]
- T-94.4-11-02 (info disclosure): V4 `user_id` from FastAPI-Users dep preserved in endgame_service
- T-94.4-11-03 (XSS via tooltip string interpolation): all interpolated values are typed `number`; React auto-escapes

## Self-Check

### Files Verified

- `app/schemas/endgames.py` — FOUND, RatingAnchorOut has n_chesscom_games/n_lichess_games/chesscom_median_native/lichess_median_native
- `app/services/endgame_service.py` — FOUND, materialisation block updated
- `frontend/src/types/endgames.ts` — FOUND, RatingAnchorOut interface updated
- `frontend/src/components/charts/PercentileChip.tsx` — FOUND, props + bullet 4 updated
- `frontend/src/components/charts/__tests__/PercentileChip.test.tsx` — FOUND, C1-C6 suite present
- `frontend/src/components/charts/EndgameOverallPerformanceSection.tsx` — FOUND, 2 call sites updated
- `frontend/src/components/charts/EndgameTimePressureCard.tsx` — FOUND, 3 call sites updated
- `frontend/src/components/charts/EndgameMetricCard.tsx` — FOUND, 1 call site updated
- `frontend/src/lib/percentileAnchor.ts` — FOUND, pickDominantTcAnchor updated

### Commits Verified

- `92c113f3` — Task 1: schema + service materialisation
- `4944f254` — Task 2: TS type
- `ab80bd94` — Task 3 RED: test rewrite
- `46770e49` — Task 3 GREEN: PercentileChip.tsx
- `bb175626` — Task 4: consumer components + percentileAnchor

## Self-Check: PASSED
