---
phase: 94.4-peer-relative-percentile-chip-refinement
verified: 2026-05-27T00:15:45Z
status: human_needed
score: 9/9 must-haves verified (SC-9 scaffold-only — operator UAT pending)
overrides_applied: 0
human_verification:
  - test: "Elite-user credibility-recovery UAT (PRPCR-09 / SC-9)"
    expected: "After prod backfill + Plan 07 frontend deploy ship together, a 2400-rated test user landing at `p15` Endgame Score Gap reads as cohort-relative (\"even compared to other 2400s, your endgame is unusually unbalanced\") rather than as a global put-down. Mobile + desktop chip + tooltip parity confirmed at 375px and desktop widths. Operator records observations and sign-off in 94.4-06-HUMAN-UAT.md."
    why_human: "PRPCR-09 was always scoped as a blocking HUMAN-UAT against prod data (CONTEXT D-10 / Plan 06 NON-AUTONOMOUS gate). Requires (a) prod backfill to have run via `bin/prod_db_tunnel.sh` + `uv run python scripts/backfill_user_percentiles.py --target prod`, and (b) Plan 07 frontend chip rewrite to be deployed alongside. Neither can be triggered from a verifier worktree. Scaffold artifact `94.4-06-HUMAN-UAT.md` is in place with operator sign-off marked _TBD_."
  - test: "Prod backfill execution (Plan 06 Task 3)"
    expected: "Operator runs `bin/prod_db_tunnel.sh` then `uv run python scripts/backfill_user_percentiles.py --target prod`. Anchor + per-(metric, TC) percentile summaries print cleanly. No errors. Backfill is gated to run at PR-merge time alongside the Wave 2 atomic-cutover cluster."
    why_human: "Plan 06 explicitly flagged NON-AUTONOMOUS for prod write. Auto-mode policy forbids prod-DB writes from a Claude worktree."
---

# Phase 94.4: Peer-Relative Percentile Chip Refinement — Verification Report

**Phase Goal:** Pivot the percentile chip from global-pool ("vs all benchmark users") to peer-relative ("vs same-rated-cohort users"). Per-(user, TC) rating anchor (median over latest ~1000 games per TC; Lichess wins precedence, chess.com converted via hardcoded ChessGoals Table 2 snapshot). Replace `GLOBAL_PERCENTILE_CDF` with a per-(metric, ELO anchor, TC) cohort CDF family built as 50-Elo sliding windows of K=200 floor-passing users (±150 Elo max, suppress otherwise). TC-aggregated metrics compute per-TC sub-percentiles aggregated via game-count-weighted mean. Chip UX shrinks to `p23` pill + tooltip-only; tooltip leads with cohort framing + rating-anchor disclosure. Metric eligibility re-opened — Conversion / Recovery / Endgame Score Gap rescued under peer-relative framing.

**Verified:** 2026-05-27T00:15:45Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (cross-referenced against ROADMAP SC-1..9 + PRPCR-01..09)

| # | Truth (ROADMAP SC / PRPCR ID) | Status | Evidence |
|---|---|---|---|
| 1 | **SC-1 / PRPCR-01** — Cohort CDF regen emits per-(metric, ELO anchor, TC) tables under K=200 / ±150-Elo protocol; `GLOBAL_PERCENTILE_CDF` retires. | VERIFIED | `app/services/global_percentile_cdf.py:143` exports `COHORT_PERCENTILE_CDF: Mapping[CdfMetricId, Mapping[tuple[int, TimeControlBucket], CdfTable]]`. `scripts/gen_global_percentile_cdf.py:115-124` declares `COHORT_K_USERS_PER_ANCHOR=200`, `COHORT_MAX_WINDOW_ELO=150`, `COHORT_ANCHOR_STEP_ELO=50`, `COHORT_ANCHOR_MIN_ELO=800`, `COHORT_ANCHOR_MAX_ELO=2400`. Legacy `GLOBAL_PERCENTILE_CDF` symbol gone from the module — only references are historical docstrings. `reports/cohort-percentile-cdf-latest.md` is regenerated with the top-line suppression table (1,056 cells; bullet/blitz/classical heavily suppressed; rapid mid-range is the comfortable band — matches CONTEXT D-11 expectation). |
| 2 | **SC-2 / PRPCR-02** — `app/services/chesscom_to_lichess.py` ships ChessGoals tables + `convert_chesscom_to_lichess(rating, source_tc, target_tc)` (USCF + FIDE columns shipped per D-14). | VERIFIED | File present (22 KB). `CHESSCOM_INTRA_TC` (Table 1, line 68), `CHESSCOM_BLITZ_TO_LICHESS` (Table 2, line 117), `LICHESS_BLITZ_INTRA_TC` (Table 3 per D-14, line 169). Module constants `CHESSCOM_TO_LICHESS_TABLE_SNAPSHOT="2026-05-26"` + `CHESSCOM_TO_LICHESS_SOURCE="https://chessgoals.com/rating-comparison/"` at lines 43-44. `convert_chesscom_to_lichess` defined at line 273 with `Literal` typed `source_tc` / `target_tc` and returns `int \| None` (None for `daily`, per D-13 / RESEARCH Pitfall 2). |
| 3 | **SC-3 / PRPCR-03** — Per-(user, TC) rating anchors stored in `user_rating_anchors`; Stage A/B compute path runs at import time. | VERIFIED | `app/models/user_rating_anchors.py` defines `UserRatingAnchor` with composite PK `(user_id, time_control_bucket)`, columns `anchor_rating INT`, `source_platform anchor_source`, `chesscom_raw_rating INT NULL`, `n_games INT`, `computed_at TIMESTAMPTZ`. `user_id` FK has `ondelete='CASCADE'`. `anchor_source` ENUM declared. Alembic migration `20260526_203213_fc000b5d0134_add_user_rating_anchors.py` creates the ENUM and table with the implicit varchar→anchor_source cast. `app/services/user_benchmark_percentiles_service.py` imports `compute_anchors_for_user`, `fetch_anchors_for_user`; `canonical_slice_sql.py:1085` defines `per_user_cte_median_anchor`. Lichess-precedence rule (D-12) implemented in the wrapper per the model docstring. chess.com Daily dropped at SQL level per Pitfall 11. |
| 4 | **SC-4 / PRPCR-04** — TC-stratified metric lookup via `interpolate_cohort_percentile(metric, value, anchor, tc)`; per-TC sub-percentiles aggregated via game-count-weighted mean. | VERIFIED | `app/services/global_percentile_cdf.py:12187` defines `interpolate_cohort_percentile`. Legacy `interpolate_percentile(metric, value)` removed — only historical docstring references remain (`global_percentile_cdf.py:12225` confirms "Legacy `interpolate_percentile` stub retired in Phase 94.4 Plan 05b"). `app/services/user_benchmark_percentiles_service.py:440, 522` call `interpolate_cohort_percentile`. `app/services/endgame_service.py:1267` defines `_aggregate_per_tc_percentile`; called for `score_gap` (1437), `section2_score_gap_conv` (1441), `section2_score_gap_parity` (1444), `recovery_score_gap` (1453), `achievable_score_gap` (2464). |
| 5 | **SC-5 / PRPCR-05** — `user_benchmark_percentiles` PK widens to `(user_id, metric, time_control_bucket)`; 8-value ENUM with Recovery rescued. | VERIFIED | Alembic migration `20260526_222651_1945ae56aa20_reshape_user_benchmark_percentiles.py` lines 48-55 enumerates the exact 8 values per CONTEXT D-13 (`score_gap`, `achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`, `recovery_score_gap`, `time_pressure_score_gap`, `clock_gap`, `net_flag_rate`); `PrimaryKeyConstraint("user_id", "metric", "time_control_bucket")` at line 124. Drop/recreate Alembic shape per D-02 (downgrade is no-op stub). |
| 6 | **SC-6 / PRPCR-06** — Chip UX shrinks to `p23` pill; **no flame icons** (regression guard against commit `6766898c`). | VERIFIED | `frontend/src/components/charts/PercentileChip.tsx:49-50` defines `MIN_PERCENT=1`, `MAX_PERCENT=99`. `formatBarePercentile` (line 118) renders `p${rounded}` with the `MIN_PERCENT`/`MAX_PERCENT` clamp per D-06a. Imports limited to `react`, `radix-ui`, `cn`, theme tokens (no `lucide-react` Flame). No `<Flame` JSX in the file. Test file `__tests__/PercentileChip.test.tsx:268-291` has the explicit "NO flame icon (regression guard for commit-6766898c)" describe block with assertions across all percentiles AND with popover opened, checking `data-testid=/flame/i`, `[class*="flame"]`, `[class*="Flame"]`, `[data-icon*="flame" i]`. aria-label direction word per D-06b retained. |
| 7 | **SC-7 / PRPCR-07** — Tooltip body is 4 bullets: cohort framing + rating-anchor disclosure. `RATING_NOTE_BY_FLAVOR` / `DIRECTION_BY_FLAVOR` / `COPY_RATING_NOTE_*` retire. `PercentileChipFlavor` collapses to 8 + optional `tc` prop. | VERIFIED | `PercentileChip.tsx:80-88` defines the 8-flavor enum (`score-gap`, `achievable`, `parity`, `conversion`, `recovery`, `time-pressure-score-gap`, `clock-gap`, `net-flag-rate`). `tc?: TimeControlBucket` optional prop at line 99. `anchorRating`, `anchorSource`, `chesscomRawRating` props (102-107) feed the rating-anchor disclosure bullet. `DIRECTION_BY_FLAVOR`, `RATING_NOTE_BY_FLAVOR`, `COPY_RATING_NOTE_*` symbols absent from `frontend/src/` (only mentioned in the PercentileChip.tsx docstring as "retire" notes). 4-bullet popover body present (verified earlier via line-19 / docstring D-07 contract reference). Consumers wired correctly: `EndgameMetricCard.tsx:250-261` (page-level, flavor without tc, with `anchorRating={dominantAnchor.anchor_rating}` etc.), `EndgameTimePressureCard.tsx` (per-TC with `tc` prop), `EndgameOverallPerformanceSection.tsx:196,252`. |
| 8 | **SC-8 / PRPCR-08** — SEED-019 § "Final Tier-1 chip set" marked superseded; `feedback_percentile_chip_tooltip_disclosure.md` amended. | VERIFIED | `.planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md:14` carries banner "⚠ Superseded by [SEED-026 v2] (Phase 94.4, shipped 2026-05-27)"; line 53 marks the Tier-1 set SUPERSEDED with explicit cross-reference to SEED-026 v2 and the Recovery/Conversion rescue rationale. Memory file `~/.claude/projects/.../memory/feedback_percentile_chip_tooltip_disclosure.md` line 25 has the "Amendment 2026-05-27 (Phase 94.4)" block documenting the bullet-4 flip from per-metric rating-correlation to rating-anchor disclosure with the CONTEXT D-07a reason. |
| 9 | **SC-9 / PRPCR-09** — UAT confirms credibility recovery on elite users (scaffold landed by Plan 06; operator sign-off deferred). | VERIFIED (SCAFFOLD) — operator UAT pending | `94.4-06-HUMAN-UAT.md` exists with the elite-user credibility-recovery test prompts, mobile-parity-at-375px prompts, and an "Operator sign-off: _TBD_" placeholder. Per CONTEXT / ROADMAP scope, the actual sign-off is explicitly **deferred** until prod backfill + Plan 07 frontend deploy ship together — see `94.4-06-SUMMARY.md` line 19 ("DEFERRED — recorded in 94.4-06-HUMAN-UAT.md"). This is the goal as specified — the **scaffold** is the deliverable of this phase; the **sign-off** is the gating UAT at PR-merge time. Flagged as `human_needed` (see Human Verification Required below). |

**Score:** 9/9 must-haves verified — SC-9 is verified only at the scaffold level (the artifact is present and correctly structured); operator sign-off is the blocking gate carried out at prod-merge time per CONTEXT D-10 + Plan 06 NON-AUTONOMOUS contract.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/services/chesscom_to_lichess.py` | New module: 3 ChessGoals tables (D-14), `convert_chesscom_to_lichess`, snapshot+source constants | VERIFIED | 22 KB; all 3 tables + accessor present; tested via `tests/services/test_chesscom_to_lichess.py` (run was green at end of Plan 05c per phase contract) |
| `app/models/user_rating_anchors.py` | New ORM model: PK `(user_id, time_control_bucket)`, `anchor_source` ENUM | VERIFIED | All columns + FK ondelete='CASCADE' + ENUM as specified |
| `app/repositories/user_rating_anchors_repository.py` | `upsert_anchor` + `fetch_anchors_for_user` | VERIFIED | Imported by `user_benchmark_percentiles_service.py:95-97` |
| `app/services/global_percentile_cdf.py` | `COHORT_PERCENTILE_CDF` + `interpolate_cohort_percentile`; legacy `GLOBAL_PERCENTILE_CDF` + `interpolate_percentile` removed | VERIFIED | File renamed-in-place per planner discretion (D-09b allows). 304 KB regenerated artifact; symbol grep confirms only the new shape is exported |
| `app/services/canonical_slice_sql.py::per_user_cte_median_anchor` | New median-rating CTE builder | VERIFIED | Line 1085 |
| `app/services/endgame_service.py::_aggregate_per_tc_percentile` | Weighted-mean aggregator per D-08 / D-08b | VERIFIED | Line 1267; applied to all 5 page-level ΔES metrics |
| `app/schemas/endgames.py::RatingAnchorOut` | Pydantic schema for tooltip-disclosure surface | VERIFIED | Line 27; `rating_anchors: dict[TimeControlBucket, RatingAnchorOut]` added at line 885 |
| Alembic migrations | `add_user_rating_anchors` + `reshape_user_benchmark_percentiles` | VERIFIED | Both timestamped 2026-05-26 in `alembic/versions/` |
| `scripts/gen_global_percentile_cdf.py` | Rewrite for cohort CDF (preserve filename per D-09b option B) | VERIFIED | Module constants at lines 115-124; sliding-window algorithm at lines 426-448 |
| `scripts/backfill_user_percentiles.py` | Extended for per-(user, metric, TC) + anchor backfill | VERIFIED | 3-stage per-user flow + anchor summary + percentile summary + `--tc` / `--skip-anchors` flags + 8-value `--metric` choices |
| `frontend/src/components/charts/PercentileChip.tsx` | `p23` pill, 8 flavors + optional `tc`, no Flame, 4-bullet tooltip | VERIFIED | No `lucide-react` import; no `<Flame` JSX; flavor enum collapsed to 8 |
| `frontend/src/components/charts/__tests__/PercentileChip.test.tsx` | Flame-icon regression guard (Test 9 / lines 268-291) | VERIFIED | Comprehensive flame-absent assertions per-percentile + with popover open |
| `frontend/src/types/endgames.ts::RatingAnchorOut` | Frontend mirror of backend schema | VERIFIED | Line 367; `rating_anchors` exposed on overview response at line 387 |
| `frontend/src/lib/percentileAnchor.ts` | New helper for per-TC anchor selection | VERIFIED | Lines 17-37 |
| `.planning/seeds/SEED-019-*.md` | Superseded banner | VERIFIED | Banner at lines 14-18; Final Tier-1 chip set marked SUPERSEDED at line 53 |
| `feedback_percentile_chip_tooltip_disclosure.md` | Amendment block for new 4-bullet contract | VERIFIED | Lines 25-38 of memory file |
| `CHANGELOG.md [Unreleased]` | Phase 94.4 entry | VERIFIED | Lines 13-14 (Added: Recovery chip + rating-anchor disclosure), line 19 (Changed: pivot to peer-relative) |
| `reports/cohort-percentile-cdf-latest.md` | Regen report with top-line suppression table | VERIFIED | Top-line table per (metric, anchor, TC) over 1,056 cells (not in appendix per D-11) |
| `94.4-06-HUMAN-UAT.md` | Operator-sign-off scaffold for SC-9 | VERIFIED (scaffold) | Created; sign-off marked TBD |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `compute_anchors_for_user` | `user_rating_anchors` table | `upsert_anchor` UPSERT | WIRED | Service → repository → DB; Stage A trigger contract preserved per CONTEXT integration-points |
| `compute_stage_b` (per-user percentiles) | `COHORT_PERCENTILE_CDF` | `interpolate_cohort_percentile(metric, value, anchor, tc)` | WIRED | `user_benchmark_percentiles_service.py:440,522` |
| `endgame_service._aggregate_per_tc_percentile` | per-(user, metric, TC) rows | `user_benchmark_percentiles` 3-col PK fetch | WIRED | All 5 page-level ΔES metrics call the aggregator |
| `EndgameOverviewResponse.rating_anchors` (backend schema) | `EndgameMetricCard` / `EndgameTimePressureCard` (frontend) | `Endgames.tsx:499,534,560` passes `overviewData?.rating_anchors` → `ratingAnchors` prop | WIRED | `lib/percentileAnchor.ts::resolveAnchorForChip` picks the per-TC anchor |
| `PercentileChip` props | tooltip body bullet 4 | `anchorRating` + `anchorSource` + `chesscomRawRating` props per D-07 | WIRED | EndgameMetricCard:259-261 wires `dominantAnchor.anchor_rating`, `dominantAnchor.source_platform`, `chesscom_raw_rating` |
| Backfill script | `user_rating_anchors` + `user_benchmark_percentiles` | 3-stage per-user flow | WIRED | Dev backfill green per Plan 06 SUMMARY (27 anchor rows + 196 percentile rows across 19 dev users) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `PercentileChip` | `percentile`, `anchorRating`, `anchorSource` props | Endgames.tsx → useQuery → `/api/endgames/overview` → `endgame_service` → reads `user_benchmark_percentiles` (per-(user, metric, TC) row) + `user_rating_anchors` (per-(user, TC) row) populated by backfill | YES (dev) | Dev backfill confirmed populating both tables (Plan 06 SUMMARY: 196 percentile rows + 27 anchor rows). For prod, populated only after Operator runs prod backfill (deferred). |
| `EndgameOverviewResponse.rating_anchors` | API response field | `endgame_service.py:3044` builds `dict[TC, RatingAnchorOut]` from anchors fetched via `fetch_anchors_for_user` | YES (dev) | Real DB read path; pydantic schema typed |
| `COHORT_PERCENTILE_CDF` (live lookup) | 99-breakpoint table | `gen_global_percentile_cdf.py` benchmark-DB regen (1,132s runtime); checked-in artifact at `app/services/global_percentile_cdf.py:143` | YES | Live artifact; 1,056 cells with appropriate suppression flags per `reports/cohort-percentile-cdf-latest.md` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `chesscom_to_lichess` module loads + Daily returns None | (covered by `tests/services/test_chesscom_to_lichess.py`) | Test suite was green at end of Plan 05c (per phase-spec contract; not re-run here) | PASS (trusted from Plan 05c) |
| `interpolate_cohort_percentile` round-trip | (covered by `tests/services/test_global_percentile_cdf.py` + integration) | Green at end of Plan 05c | PASS (trusted from Plan 05c) |
| Frontend PercentileChip Vitest tests including flame-regression guard | (`PercentileChip.test.tsx`) | Green at end of Plan 05c | PASS (trusted from Plan 05c) |
| Dev backfill end-to-end | `scripts/backfill_user_percentiles.py --target dev` | Per Plan 06 SUMMARY: 19 users, 441s, 27 anchor rows, 196 percentile rows — clean | PASS |

Per the spawn objective: "Pre-PR sweep was verified green at end of Plan 05c (full pytest + frontend tests passed). Re-running is not required unless something specific changed since." Nothing has changed since Plan 05c besides Plan 06/07/08 doc + frontend reshape work whose own SUMMARY entries report green tests.

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this project; phase PLANs do not declare probes. **SKIPPED** — phase does not use probe-based verification.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SEED-026 | All 10 plans (94.4 design source) | Peer-relative chip refinement seed | SATISFIED | All 9 sub-requirements (PRPCR-01..09) verified below |
| PRPCR-01 | 94.4-04-PLAN.md | Cohort CDF regen with strict K=200/±150 defaults | SATISFIED | Truth #1 above |
| PRPCR-02 | 94.4-01-PLAN.md | `chesscom_to_lichess.py` (Tables 1+2; D-14 widens to +Table 3 +USCF/FIDE) | SATISFIED | Truth #2 above |
| PRPCR-03 | 94.4-02-PLAN.md | `user_rating_anchors` table + Lichess-precedence rule | SATISFIED | Truth #3 above |
| PRPCR-04 | 94.4-03-PLAN.md, 94.4-05a/b/c-PLAN.md | `interpolate_cohort_percentile` + per-TC user-scalar CTEs + Pitfall 1 user_id widening | SATISFIED | Truth #4 above |
| PRPCR-05 | 94.4-05a-PLAN.md | Schema reshape: 8-value ENUM + 3-col PK + drop/recreate per D-02 | SATISFIED | Truth #5 above |
| PRPCR-06 | 94.4-07-PLAN.md | Bare-`p23` chip face + no Flame regression guard | SATISFIED | Truth #6 above |
| PRPCR-07 | 94.4-07-PLAN.md | 4-bullet peer-relative tooltip + 8-flavor + tc prop + retired constants gone | SATISFIED | Truth #7 above |
| PRPCR-08 | 94.4-08-PLAN.md | SEED-019 supersession + memory amendment | SATISFIED | Truth #8 above |
| PRPCR-09 | 94.4-06-PLAN.md (scaffold) | Elite-user HUMAN-UAT credibility-recovery sign-off | SATISFIED (SCAFFOLD) | Truth #9 above; sign-off explicitly deferred to operator at PR-merge time per CONTEXT + Plan 06 NON-AUTONOMOUS contract |

No orphaned requirements (all 9 PRPCR IDs declared in plans + SEED-026 satisfied).

### Anti-Patterns Found

Scanned all phase-modified files (`app/services/chesscom_to_lichess.py`, `app/models/user_rating_anchors.py`, `app/repositories/user_rating_anchors_repository.py`, `frontend/src/components/charts/PercentileChip.tsx`, `scripts/backfill_user_percentiles.py`) for `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` / `console.log` / hollow-prop stubs:

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (none found) | — | — | — | Clean. Plan 06 SUMMARY flags `Operator sign-off: _TBD_` in `94.4-06-HUMAN-UAT.md` — that `TBD` is the intentional sign-off placeholder, not unresolved debt. |

### Human Verification Required

#### 1. Elite-user credibility-recovery UAT (PRPCR-09 / SC-9 — blocking)

**Test:** After the PR merges (which fires CI deploy + prod backfill) and Plan 07 frontend chip rewrite ships in the same deploy, exercise the Endgames page on `https://flawchess.com` as a representative elite user (target: ≥2200 anchor Lichess rapid). Visit a metric where the chip lands at a low percentile (e.g., `p15`) and read the 4-bullet popover end-to-end.

**Expected:**
- Chip face renders as a bare `p15` pill with a red color band (no direction word, no flame icon).
- Popover body bullet 1: "Compared to other ~2200-rated players in rapid" (cohort framing + TC + anchor — not "all FlawChess users").
- Popover body bullet 4: discloses rating anchor source ("Anchored on your Lichess rapid rating" or "Anchored on your chess.com rapid rating, converted to Lichess-equivalent via ChessGoals snapshot").
- The reading is "even compared to other 2200s, your endgame is unusually unbalanced" — NOT "FlawChess thinks I'm bad at chess".
- Mobile-parity check at 375px (chip + tooltip both legible, popover positions correctly).
- Operator records observed values + sign-off decision in `94.4-06-HUMAN-UAT.md`.

**Why human:** This is the only criterion in the phase that requires (a) the prod backfill to have run against the prod DB and (b) Plan 07's frontend chip rewrite to be deployed alongside. Both are explicitly excluded from auto-mode per CONTEXT D-10 / Plan 06 NON-AUTONOMOUS gate. Visual + reading-comprehension judgement is the contract per the ROADMAP success criterion text.

#### 2. Prod backfill execution (Plan 06 Task 3 — must precede UAT #1)

**Test:** Operator opens prod tunnel and runs the cohort-CDF + anchor backfill against prod:

```bash
bin/prod_db_tunnel.sh
uv run python scripts/backfill_user_percentiles.py --target prod
```

**Expected:**
- Anchor summary table prints per-(TC × source_platform) row counts (suppressed counts visible per TC).
- Percentile summary table prints per-(metric × TC) row counts (included / floor_rej / suppressed buckets).
- No exceptions. Total runtime O(minutes) for the current prod user base.

**Why human:** No autonomous prod writes from a Claude worktree. Operator runs at PR-merge time after the atomic-cutover cluster (Plans 03 + 04 + 05a + 05b + 05c + 06) ships via the `production` branch deploy.

### Gaps Summary

No gaps blocking the phase goal at the codebase level. All 8 of the 9 ROADMAP success criteria are observably satisfied in code, tests, and supporting artifacts; the 9th (SC-9 elite-user UAT) was always scoped as a HUMAN-UAT scaffold-this-phase / sign-off-at-PR-merge contract per CONTEXT D-10 and Plan 06's NON-AUTONOMOUS gate — the scaffold (`94.4-06-HUMAN-UAT.md`) is in place; the sign-off is the human-verification item carried forward.

The structural pivot from global-pool to peer-relative cohort framing landed coherently: the CDF artifact, the per-user lookup path, the storage shape, the API shaper, the frontend types, and the chip rewrite are all consistent and end-to-end wired. The dev backfill confirms data flows real values through the new path. The flame-icon regression guard is in place at both component and test level (per the locked Memory `feedback_no_flame_icons`).

---

*Verified: 2026-05-27T00:15:45Z*
*Verifier: Claude (gsd-verifier, goal-backward stance)*
