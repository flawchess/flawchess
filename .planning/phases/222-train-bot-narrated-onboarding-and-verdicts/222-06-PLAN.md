---
phase: 222-train-bot-narrated-onboarding-and-verdicts
plan: 06
type: execute
wave: 3
depends_on: ["222-03", "222-04", "222-05"]
files_modified:
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
  - CHANGELOG.md
  - .planning/seeds/SEED-167-train-sound-toggle-rehoming.md
autonomous: true
requirements: [TRAINBOT-02, TRAINBOT-05, TRAINBOT-09, TRAINBOT-10]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The first reveal a user ever reaches runs a three-step Hilda walkthrough: the verdict bubble, the line cards, then the action row, each spotlighted in turn (ROADMAP scope B, D-24)."
    - "Completing the walkthrough stamps reveal_walkthrough_seen_at; abandoning it leaves the column NULL so it replays (D-12)."
    - "The walkthrough's three targets span two columns on desktop, and the step index is owned by TrainSolveScreen — the common parent — and passed to TrainReveal as one optional prop (RESEARCH Finding A)."
    - "The board is fully visible above the bubble on a 375px viewport with the longest first-session copy loaded (SC2)."
    - "The explanation-seen state survives a device switch (SC5)."
    - "The retirement of the reveal's sound toggle is recorded as a follow-up seed, and the phase's user-facing changes are in CHANGELOG.md under Unreleased."
  artifacts:
    - frontend/src/components/train/TrainReveal.tsx
    - CHANGELOG.md
    - .planning/seeds/SEED-167-train-sound-toggle-rehoming.md
  key_links:
    - "TrainSolveScreen walkthroughStep state -> TrainReveal's walkthroughStep prop -> the ring-2 ring-brand-brown class on the line-card group (step 2 only)."
    - "Walkthrough 'Got it' -> useTrainOnboarding().stamp('reveal_walkthrough')."
  prohibitions:
    - statement: "The walkthrough never reuses the spotlightKey / onSpotlightChange channel — those drive board arrows via applyTrainSpotlight, not element highlighting."
      status: resolved
      verification: explicit
    - statement: "No TrainBotBubble is mounted inside TrainReveal — the chat row is a board-column citizen for all states (RESEARCH Finding A)."
      status: resolved
      verification: explicit
    - statement: "No re-baselining of the frontend complexity gate — eslint.config.js is not edited."
      status: resolved
      verification: explicit
    - statement: "No board overlay: the walkthrough highlights already-rendered elements and never covers the position."
      status: resolved
      verification: explicit
    - statement: "Seen-state is never stamped via PUT /train/settings (D-12)."
      status: resolved
      verification: explicit
    - statement: "No bin/reset_db.sh run: onboarding UAT replays via scripts/reset_train_state.py."
      status: resolved
      verification: explicit
    - statement: "No Umami events (D-20)."
      status: resolved
      verification: backstop
    - statement: "No new npm or python package is installed in this phase."
      status: resolved
      verification: explicit
---

<objective>
Close the phase: the first-reveal walkthrough that teaches the solution screen, the
changelog entry, the follow-up seed the ROADMAP requires, and the manual UAT legs that no
unit test can stand in for.

Purpose: D-24 is the third and last onboarding stepper — a newcomer's first reveal is a
wall of cards with no stated purpose, and three Hilda steps turn it into a guided tour.
RESEARCH Finding A makes the structure non-obvious: the three spotlight targets span two
columns on desktop, so the step index must be owned by `TrainSolveScreen` (the common
parent) and travel to `TrainReveal` as one prop. SC2 (board fully visible at 375px with the
longest first-session copy) and SC5 (state survives a device switch) are device-bound and
close here as human-verified legs.

Output: the walkthrough, `CHANGELOG.md` bullets under `## [Unreleased]`,
`SEED-167-train-sound-toggle-rehoming.md`, and a recorded UAT pass.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-CONTEXT.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-RESEARCH.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-PATTERNS.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-04-SUMMARY.md
@.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-05-SUMMARY.md
@frontend/CLAUDE.md
@CLAUDE.md
</context>

## Artifacts this plan produces

- `frontend/src/components/train/TrainReveal.tsx`: `walkthroughStep?: 0 | 1 | 2 | null` on `TrainRevealProps`, and the spotlight ring on the line-card group for step 2.
- `frontend/src/components/train/TrainSolveScreen.tsx`: `walkthroughStep` state, the walkthrough arm of the verdict bubble, and the `reveal_walkthrough` stamp.
- New testids: `train-bot-walkthrough`, `btn-train-bot-walkthrough-next`, `btn-train-bot-walkthrough-done`.
- `CHANGELOG.md`: user-facing bullets under `## [Unreleased]`.
- `.planning/seeds/SEED-167-train-sound-toggle-rehoming.md` (NEW).

<tasks>

<task type="auto" tdd="true">
  <name>T-222-06-01: The first-reveal walkthrough — three Hilda steps across two columns</name>
  <files>frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainSolveScreen.tsx` lines 720–760 — `applyTrainSpotlight` and the `spotlightKey` / `onSpotlightChange` wiring, read specifically to confirm those props drive which BOARD ARROWS are drawn and must NOT be reused for element highlighting.
    - `frontend/src/components/train/TrainSolveScreen.tsx` lines 1180–1210 — the `TrainReveal` mount site and its current prop list.
    - `frontend/src/components/train/TrainReveal.tsx` lines 1030–1045 and 1195–1215 — the existing `cn('cursor-pointer', isSpotlit && 'ring-2 ring-brand-brown')` idiom, the visual to reuse.
    - `frontend/src/components/train/TrainReveal.tsx` lines 1140–1160 — the component's root element and its `lg:max-w-sm` right-column class, the structural fact behind RESEARCH Finding A.
    - `frontend/src/lib/trainBotCopy.ts` (plan 01) — `walkthroughCopy` and its three spotlight target keys.
    - `frontend/src/components/train/TrainBotStepper.tsx` (plan 01) and `TrainBotBubble.tsx` (plan 01).
    - `frontend/src/hooks/useTrainSettings.ts` and `frontend/src/hooks/useTrainOnboarding.ts` (plan 01).
    - `.planning/phases/.../222-CONTEXT.md` §D-05, D-12, D-24, and the "Claude's Discretion" bullet on spotlight styling.
    - `.planning/phases/.../222-RESEARCH.md` §Finding A (the walkthrough consequence paragraph), §Anti-Patterns (never reuse the spotlight channel), §Finding B, §Finding G.
    - `.planning/sketches/004-train-bot-verdict-and-score/README.md` — the recorded walkthrough design (prose is placeholder, superseded by D-24).
  </read_first>
  <behavior>
    - On the first reveal a user reaches with `reveal_walkthrough_seen_at === null`, the verdict bubble's body is replaced by walkthrough step 1 spoken by Hilda, with a Next control, and the verdict bubble itself carries the spotlight ring.
    - Next advances to step 2: the ring moves to the line-card group inside `TrainReveal`, driven by the new `walkthroughStep` prop.
    - Next advances to step 3: the ring moves to the action row inside the bubble; the control reads "Got it".
    - "Got it" dismisses the walkthrough, restores the normal verdict copy, and fires exactly one `stamp('reveal_walkthrough')`.
    - Leaving the reveal mid-walkthrough (Next puzzle, Analyze, unmount) fires no stamp — an abandoned stepper replays.
    - With `reveal_walkthrough_seen_at` non-null, no walkthrough renders and the verdict shows immediately.
    - With `useTrainSettings().data === undefined`, no walkthrough renders (no flash).
    - `TrainReveal` renders no ring when `walkthroughStep` is `null` or absent, so every non-first reveal is visually unchanged.
    - The board arrows are unaffected at every step: the `spotlightKey` channel is never touched by the walkthrough.
  </behavior>
  <action>
1. `TrainReveal.tsx`: add ONE new optional prop `walkthroughStep?: 0 | 1 | 2 | null` to `TrainRevealProps`, and apply the existing `ring-2 ring-brand-brown` class to the line-card group when it is `1`. Reuse the CLASS only — never the `spotlightKey` / `onSpotlightChange` props, which filter which arrows `applyTrainSpotlight` draws on the board and have nothing to do with element highlighting. No `TrainBotBubble` is mounted in this file; the chat row stays a board-column citizen for all states.

2. `TrainSolveScreen.tsx`: own the step index as `walkthroughStep` state, initialised when the verdict first lands AND `settings !== undefined && settings.reveal_walkthrough_seen_at === null`. Feed `walkthroughCopy(step)` into the existing bubble (hosted by Hilda, `HILDA_ID`, per D-05 — teaching is always Hilda) with `TrainBotStepper` supplying Next / Next / "Got it" (testids `btn-train-bot-walkthrough-next`, `btn-train-bot-walkthrough-done`). Apply the ring to the bubble itself on step 1 and to the action row on step 3; pass `walkthroughStep` down to `TrainReveal` for step 2. "Got it" clears the state and fires `useTrainOnboarding().stamp('reveal_walkthrough')` once, ref-guarded.

3. Copy is D-24 verbatim, tightened only within the D-21 rules. Step 1 explains the feedback and that the position comes back; step 2 explains the line cards and that understanding why a move fails is what makes the pattern stick; step 3 explains Analyze and Next.

4. Watch the complexity budget. The walkthrough adds branches to a component that is already at its ceiling — extract the step-to-spotlight-target mapping into `trainBotCopy.ts` or `trainBubbleState.ts` rather than inlining ternaries in the JSX, and re-measure. If the net lands at or above 68, stop and report rather than editing `eslint.config.js`.

5. Tests: the behavior cases above in `TrainSolveScreen.test.tsx` (the walkthrough owner) plus a `TrainReveal.test.tsx` case asserting the ring appears on the line cards for `walkthroughStep === 1` and nowhere for `null`. Assert through classes on the target element and the stepper testids; do not assert on an animation class (the suite's `matchMedia` stub makes `prefersReducedMotion()` true inside these files). Never add a per-file or per-test timeout.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/components/train/__tests__/TrainSolveScreen.test.tsx src/components/train/__tests__/TrainReveal.test.tsx )</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the vitest summary line</fails_when>
    <automated>( cd frontend && grep -v -E "^[[:space:]]*(//|\*|/\*)" src/components/train/TrainReveal.tsx | grep -q "TrainBotBubble" ; test $? -eq 1 )</automated>
    <fails_when>the compound command exits non-zero, meaning a chat bubble was mounted inside TrainReveal on a NON-COMMENT line (RESEARCH Finding A / Pitfall 1; comment lines are filtered so an explanatory note cannot trip the gate)</fails_when>
    <automated>( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 67]' src/components/train/TrainSolveScreen.tsx src/components/train/TrainReveal.tsx )</automated>
    <fails_when>non-zero exit, or any output line containing "has a complexity of"</fails_when>
    <automated>( cd frontend && npm run lint && npm run build && npm run knip ) && git diff --quiet -- frontend/eslint.config.js</automated>
    <fails_when>non-zero exit, any `error TS` line on `TrainRevealProps`, any unused-export report, or a non-empty diff on frontend/eslint.config.js</fails_when>
  </verify>
  <done>The first reveal runs three Hilda steps spotlighting the verdict, the line cards and the actions in turn; "Got it" stamps the timestamp once; every later reveal is visually unchanged; board arrows are untouched throughout.</done>
  <acceptance_criteria>
    - `frontend/src/components/train/TrainReveal.tsx` contains `walkthroughStep` exactly in the props interface and at the one ring application site, and no non-comment line in it mounts a chat bubble component.
    - `frontend/src/components/train/TrainReveal.tsx`'s walkthrough ring application does not reference `spotlightKey` or `onSpotlightChange`.
    - `TrainSolveScreen.test.tsx` asserts `stamp` was called exactly once with `'reveal_walkthrough'` after "Got it", and zero times when the component unmounts on step 2.
    - `TrainReveal.test.tsx` asserts the line-card group carries `ring-2 ring-brand-brown` for `walkthroughStep === 1` and does not for `walkthroughStep === null`.
    - A test asserts the board arrow set is identical across all three walkthrough steps.
    - The complexity gate at 67 exits 0 with `eslint.config.js` unchanged; the final number is recorded in the SUMMARY.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-222-06-02: Changelog entry and the sound-toggle re-homing seed</name>
  <files>CHANGELOG.md, .planning/seeds/SEED-167-train-sound-toggle-rehoming.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1–25 — the `## [Unreleased]` block and the house voice: user-facing outcomes in plain language, no file paths, no internal identifiers, em-dashes used sparingly.
    - `.planning/seeds/SEED-166-train-first-session-retention.md` — the seed file format (problem statement, evidence, scope, planning notes).
    - `.planning/ROADMAP.md` §Phase 222 out-of-scope list — the line requiring the sound toggle's retirement to be recorded as a follow-up seed at phase close.
    - `.planning/phases/.../222-CONTEXT.md` §D-10 and §Deferred Ideas.
    - `.planning/phases/.../222-RESEARCH.md` §Finding L — `setMuted` had exactly two call sites (`Bots.tsx:373` and the now-deleted `TrainSolveScreen` one), the shared preference is persisted, and `/bots` is now the app's only mute control.
    - `CLAUDE.md` §Version Control (changelog is not optional) and §Communication Style (em-dashes sparingly).
  </read_first>
  <action>
1. `CHANGELOG.md`: append user-facing bullets under `## [Unreleased]` in the existing house voice. Cover: Train now explains itself on your first session through the bots (the guess question, the two buttons, what the buttons mean); dragging a piece before you decide now tells you why the board is locked instead of silently snapping back; every solution screen is now narrated by a bot that names your points, tells you when the position comes back, and never promises a return for a warm-up or a mastered position; the solution screen's actions moved into that bot's message; the session summary now says which positions come back and when, and explains the spaced-repetition idea the first time you finish a session; and the mute control on the solution screen has moved to the Bots page for now. Plain language, no internal identifiers, no file paths, em-dashes sparingly.

2. Create `.planning/seeds/SEED-167-train-sound-toggle-rehoming.md` following the SEED-166 format. Record: what was removed (the reveal's mute toggle, D-10); why (the action row moved into the verdict bubble and a fourth icon control did not fit the chat row); the consequence to fix (`setMuted` now has one call site, `Bots.tsx`, so a user who mutes on the Bots page and then only uses Train cannot unmute — the preference is shared and persisted); the proposed home (a Train or general settings page, explicitly out of scope for Phase 222 per the ROADMAP); and the evidence pointer to RESEARCH Finding L. Keep it a seed, not a phase — do not add it to the ROADMAP and do not create a phase for it.
  </action>
  <verify>
    <automated>grep -n "## \[Unreleased\]" -A 40 CHANGELOG.md | grep -qi "train"</automated>
    <fails_when>non-zero exit, meaning no Train bullet was added under the Unreleased heading</fails_when>
    <automated>test -f .planning/seeds/SEED-167-train-sound-toggle-rehoming.md && grep -qi "mute" .planning/seeds/SEED-167-train-sound-toggle-rehoming.md</automated>
    <fails_when>non-zero exit, meaning the follow-up seed file is missing or does not mention the mute control</fails_when>
    <automated>grep -c "SEED-167" .planning/ROADMAP.md ; test $? -eq 1</automated>
    <fails_when>the compound command exits non-zero, meaning the seed was promoted into the roadmap instead of being left as a seed</fails_when>
  </verify>
  <done>The phase's user-facing changes are in CHANGELOG.md under Unreleased in the house voice, and the sound toggle's re-homing is captured as SEED-167 without being promoted to a phase.</done>
  <acceptance_criteria>
    - `CHANGELOG.md`'s `## [Unreleased]` block contains at least five Train bullets covering the intro, the drop feedback, the narrated verdict, the actions moving into the message, and the session summary.
    - No `CHANGELOG.md` bullet contains a file path, a `D-NN` identifier, a `TRAINBOT-NN` identifier or a testid.
    - `.planning/seeds/SEED-167-train-sound-toggle-rehoming.md` exists and names `Bots.tsx` as the sole remaining mute control.
    - `.planning/ROADMAP.md` contains no `SEED-167` reference (it stays a seed, not a phase).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-222-06-03: Full pre-merge gate, plus the device legs no unit test can stand in for</name>
  <files>frontend/src/components/train/TrainSolveScreen.tsx, scripts/reset_train_state.py</files>
  <read_first>
    - `.planning/phases/.../222-RESEARCH.md` §Manual UAT checklist, §Finding H (the 375px budget arithmetic and the 240px board floor), §Finding I (the reset script), §Environment Availability.
    - `.planning/ROADMAP.md` §Phase 222 success criteria 1–7.
    - `CLAUDE.md` §Pre-merge gate, and §Scripts and tooling for the `bin/reset_db.sh` prohibition.
    - `.planning/phases/.../222-CONTEXT.md` §D-21 — the three levers permitted if the 375px leg fails: split intro step 2 into two shorter steps, tighten the wording, or let the bubble body scroll internally on the intro steps only.
  </read_first>
  <action>
Run the full `CLAUDE.md` pre-merge gate, in full, plus this phase's three additions:
`npm run build` because shared types changed, `npm run knip` because new exports landed, and
`npm run check:activity-layout` because a dashboard card landed. A subset run is not
acceptable at this point in the phase.

Then set up and run the device legs yourself. Automate every leg that can be automated: start
the dev server, reset the dev account's Train state with
`uv run python scripts/reset_train_state.py --db dev --user-id <N>`, and drive the browser.
Defer only what genuinely needs separate hardware (the phone-handoff leg). Record each leg as
pass, fail, or deferred-with-reason in the SUMMARY with what was actually observed; never mark
a leg passed on inference. Never run `bin/reset_db.sh`.

The seven legs:
1. 375x667 with the longest copy loaded (intro step 2, roughly 63 words): the board is FULLY
   visible above the bubble and the Next control is on screen without scrolling. This is SC2,
   the phase's highest-risk criterion, and it is a device gate rather than a code review item
   because `useFitBoardToViewport` shrinks the board automatically but floors at 240px and
   skips re-measuring while the page is scrolled. On failure, apply one of the three
   D-21-permitted levers and re-test, recording which one.
2. Phone handoff: complete the intro on desktop, open /train on a phone signed in to the same
   account, and confirm the intro does not replay (SC5).
3. Drop before the guess: drag a piece, confirm it snaps back AND the bubble visibly reacts
   with changed copy; drag again and confirm the reaction replays (SC1).
4. Reveal walkthrough on BOTH viewports, three steps, each spotlighting the right element.
   Desktop is the one that can break: the three targets span two columns there (TRAINBOT-10).
5. Score screen: first completed session shows the full spaced-repetition explanation, a
   second session shows the one-liner, a warm-up session shows the warm-up variant (SC4).
6. Replay: re-run `scripts/reset_train_state.py` and confirm all three steppers replay,
   proving the script's new clears work.
7. Activity card: open /activity as a superuser, confirm the Train funnel card shows both
   shares with numerator and denominator plus the all-time line, and that moving the window
   changes the windowed numbers while the all-time line holds (SC6).
  </action>
  <verify>
    <automated>uv run ruff format app/ tests/ scripts/ analysis/ && uv run ruff check . --fix && uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/ && uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit from any leg of the chain, or any "would reformat" line, or any reported nesting depth over 4 / logic LOC over 200</fails_when>
    <automated>uv run pytest -n auto -x</automated>
    <fails_when>non-zero exit, or any collection error in the pytest output</fails_when>
    <automated>( cd frontend && npm run lint && npm test -- --run && npm run build && npm run knip && npm run check:activity-layout )</automated>
    <fails_when>non-zero exit, any `error TS` line, any unused-export report from knip, or any layout-harness overflow line</fails_when>
    <automated>git diff --exit-code -- frontend/eslint.config.js frontend/package.json frontend/package-lock.json pyproject.toml uv.lock</automated>
    <fails_when>non-zero exit — either the complexity gate was re-baselined or a dependency was added, both of which this phase forbids</fails_when>
    <human-check>At 375x667 with intro step 2 loaded, the board is fully visible above the bubble and the Next control is on screen without scrolling (SC2).</human-check>
    <human-check>The intro completed on desktop does not replay when /train is opened on a phone signed in to the same account (SC5).</human-check>
    <human-check>A piece dragged before the guess snaps back AND the bubble visibly reacts with changed copy; a second drag replays the reaction (SC1).</human-check>
    <human-check>The three-step reveal walkthrough spotlights the right element at each step on BOTH a desktop and a 375px viewport (TRAINBOT-10).</human-check>
    <human-check>The first completed session shows the full spaced-repetition explanation, a second session shows the one-liner, and a warm-up session shows the warm-up variant (SC4).</human-check>
    <human-check>After re-running `scripts/reset_train_state.py --db dev --user-id N`, all three steppers replay (RESEARCH Finding I).</human-check>
    <human-check>The /activity Train funnel card shows both shares with numerator and denominator plus the all-time line, and moving the window changes the windowed numbers while the all-time line holds (SC6).</human-check>
  </verify>
  <done>Every success criterion has either an automated green or a recorded human observation, and the full pre-merge gate plus this phase's three additions are green.</done>
  <acceptance_criteria>
    - The SUMMARY records a pass / fail / deferred-with-reason line for each of the seven legs with what was observed.
    - The SUMMARY records the status of each of the seven pre-merge gate commands.
    - If leg 1 failed, the SUMMARY records which D-21-permitted lever was applied and the re-test result.
    - No `bin/reset_db.sh` invocation appears anywhere in the plan's command history.
    - `git diff --exit-code -- frontend/eslint.config.js frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` exits 0.
  </acceptance_criteria>
  <precondition>A dev account with a Train-eligible puzzle pool exists on the dev database, and the dev PostgreSQL container plus the Vite dev server are running.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> browser (`TrainSettingsResponse`) | The walkthrough's gate reads the seen-state here. |
| browser -> API (`POST /train/onboarding/reveal_walkthrough`) | The walkthrough's stamp call. |
| repository -> published artifacts (`CHANGELOG.md`, seed file) | Prose that ships publicly. |
| npm registry -> repository | Any new package would cross an unaudited boundary. This plan installs none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-06-01 | Information Disclosure | Walkthrough spotlight revealing an unrevealed line | medium | mitigate | The walkthrough only mounts once `verdict !== null`, i.e. after the reveal is already open and its cards are already rendered. It adds a CSS ring class to existing nodes and fetches nothing; the `PuzzleRevealResponse` 409 gate is untouched. |
| T-222-06-02 | Tampering | Walkthrough reusing the board-arrow spotlight channel | medium | mitigate | `spotlightKey` / `onSpotlightChange` drive `applyTrainSpotlight`, which filters which arrows are drawn on the board; reusing them would silently change board state. A separate `walkthroughStep` prop is used instead, with an acceptance-criteria test asserting the arrow set is identical across all three steps. |
| T-222-06-03 | Elevation of Privilege | Stamp fired for a guest | medium | mitigate | Mitigated by two existing patterns: the reveal only mounts behind `Train.tsx`'s `isGuest` early return, and the endpoint 403s guests via `_reject_guest` (`app/routers/train.py:48-55`). |
| T-222-06-04 | Information Disclosure | Changelog or seed leaking production user data | low | mitigate | Both describe features and a UI decision; the only numbers referenced are the funnel baselines already published in SEED-166 and the ROADMAP. Acceptance criteria forbid file paths and internal identifiers in changelog bullets. |
| T-222-06-05 | Information Disclosure | UAT screenshots or notes carrying account data | low | mitigate | UAT runs against a dev account on the dev database; the SUMMARY records observations, not credentials or personal data. |
| T-222-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs nothing (RESEARCH §Package Legitimacy Audit: not applicable). Enforced by a lockfile-diff gate at plan close. |

Supply-chain gate (discharges T-222-06-SC): `git diff --exit-code -- frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` must exit 0 at plan close.
</threat_model>

<verification>
- The full `CLAUDE.md` pre-merge gate, plus `npm run build`, `npm run knip` and `npm run check:activity-layout`, all green.
- `( cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 67]' src/components/train/TrainSolveScreen.tsx src/components/train/TrainReveal.tsx )` exits 0.
- `git diff --exit-code -- frontend/eslint.config.js frontend/package.json frontend/package-lock.json pyproject.toml uv.lock` exits 0.
- All seven UAT legs recorded.
- No `bin/reset_db.sh` invocation anywhere in this phase.
</verification>

<success_criteria>
- The first reveal runs the three-step Hilda walkthrough with correct spotlights on both viewports, stamped once on completion.
- Every later reveal is visually unchanged and the board arrows are untouched throughout.
- `CHANGELOG.md` carries the phase's user-facing bullets under `## [Unreleased]`.
- `SEED-167-train-sound-toggle-rehoming.md` exists and is not promoted to a phase.
- SC2 (375px) and SC5 (device switch) are human-verified, and the full pre-merge gate is green.
</success_criteria>

<output>
Create `.planning/phases/222-train-bot-narrated-onboarding-and-verdicts/222-06-SUMMARY.md` when done.
Record the seven UAT legs, the pre-merge gate status, and the final complexity numbers in it.
</output>
