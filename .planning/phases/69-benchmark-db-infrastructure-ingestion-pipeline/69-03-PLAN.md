---
phase: 69
plan: 03
type: execute
wave: 2
depends_on: [69-01, 69-02]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [INFRA-03]
must_haves:
  truths:
    - "`flawchess-benchmark-db` MCP server is registered in `~/.claude.json` with a connection string pointing at `postgresql://flawchess_benchmark_ro:<actual-password>@localhost:5433/flawchess_benchmark`"
    - "After Claude Code restart, `mcp__flawchess-benchmark-db__query` is callable and returns row counts from `games` and `game_positions` tables"
    - "CLAUDE.md §Database Access (MCP) lists `flawchess-benchmark-db` as the third entry alongside `flawchess-db` and `flawchess-prod-db`"
    - "The read-only password lives only in `~/.claude.json` (user-level) and `deploy/init-benchmark-db.sql` on the user's local machine, never committed to git"
  artifacts:
    - path: CLAUDE.md
      provides: "Documentation of the third MCP DB server"
      contains: "flawchess-benchmark-db"
  key_links:
    - from: "~/.claude.json mcpServers entry"
      to: "deploy/init-benchmark-db.sql"
      via: "shared password set out-of-band before first use"
      pattern: "flawchess_benchmark_ro"
---

<objective>
Register the third read-only MCP server `flawchess-benchmark-db` so the benchmark database is queryable through `mcp__flawchess-benchmark-db__query` from Claude Code, alongside the existing `flawchess-db` and `flawchess-prod-db` servers. Document the new server in `CLAUDE.md` §Database Access (MCP) per INFRA-03.

This plan is `autonomous: false` because two steps require user action that Claude cannot perform safely on its own:
1. Editing `~/.claude.json` (a user-level file outside the project; Claude Code does not write to it directly).
2. Choosing and applying a real read-only password to BOTH `deploy/init-benchmark-db.sql` (local copy only — the committed file keeps `<PASSWORD>`) AND `~/.claude.json`.
3. Restarting Claude Code so the new MCP server entry is loaded.

Purpose: INFRA-03 — read-only MCP server registered and documented.
Output: One CLAUDE.md edit, one user-applied `~/.claude.json` edit, verified MCP query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-benchmark-db-infrastructure-ingestion-pipeline/69-CONTEXT.md
@.planning/phases/69-benchmark-db-infrastructure-ingestion-pipeline/69-RESEARCH.md
@.planning/phases/69-benchmark-db-infrastructure-ingestion-pipeline/69-PATTERNS.md
@CLAUDE.md
@deploy/init-benchmark-db.sql

<interfaces>
<!-- Existing MCP entries the new one mirrors. Source: ~/.claude.json projects[".../flawchess"].mcpServers -->
```json
"flawchess-db": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://flawchess:flawchess@localhost:5432/flawchess"],
  "env": {}
},
"flawchess-prod-db": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://flawchess_ro:***@localhost:15432/flawchess"],
  "env": {}
}
```
The new entry follows the same shape with port `5433`, database `flawchess_benchmark`, and user `flawchess_benchmark_ro`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 03-01: Update CLAUDE.md §Database Access (MCP) with the third server entry</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the existing §Database Access (MCP) section near line 115 — read at minimum the section's full bullet list and the surrounding context)
    - .planning/phases/69-benchmark-db-infrastructure-ingestion-pipeline/69-PATTERNS.md (section "CLAUDE.md (modify §Database Access (MCP))")
  </read_first>
  <action>
In `CLAUDE.md` §Database Access (MCP), append a third bullet immediately after the existing `flawchess-prod-db` bullet. The exact text to add (preserve the existing two bullets unchanged):

```markdown
- **`flawchess-benchmark-db`** — benchmark database (Docker on `localhost:5433`). Requires benchmark DB running: `bin/benchmark_db.sh start` (or `docker compose -f docker-compose.benchmark.yml -p flawchess-benchmark up -d`). Read-only role `flawchess_benchmark_ro`; password is set locally and is not committed to git (the same `<PASSWORD>` placeholder pattern as the prod read-only user). Stop with `bin/benchmark_db.sh stop`.
```

Also update the sentence after the bullets that currently reads "Both are read-only query tools (`mcp__flawchess-db__query`, `mcp__flawchess-prod-db__query`)." Change it to read:

```
All three are read-only query tools (`mcp__flawchess-db__query`, `mcp__flawchess-prod-db__query`, `mcp__flawchess-benchmark-db__query`). The first is read-write at the SQL level (it uses the app user) but the MCP wrapper is query-only by design; the latter two use dedicated read-only DB roles.
```

Wait — re-read the existing CLAUDE.md sentence first. The exact existing sentence is `Both are read-only query tools (\`mcp__flawchess-db__query\`, \`mcp__flawchess-prod-db__query\`).`. The replacement above changes "Both" to "All three" and adds the third server name and an explanatory sentence about the role distinction. Apply this exact replacement.
  </action>
  <verify>
    <automated>grep -q 'flawchess-benchmark-db' CLAUDE.md &amp;&amp; grep -q 'localhost:5433' CLAUDE.md &amp;&amp; grep -q 'mcp__flawchess-benchmark-db__query' CLAUDE.md &amp;&amp; ! grep -q 'Both are read-only query tools' CLAUDE.md</automated>
  </verify>
  <done>
    CLAUDE.md §Database Access (MCP) now has three bullets covering `flawchess-db`, `flawchess-prod-db`, `flawchess-benchmark-db`. The "Both are..." sentence is replaced with the "All three are..." sentence. No other parts of CLAUDE.md are modified.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 03-02: User registers MCP server in ~/.claude.json (manual)</name>
  <files>~/.claude.json (user-level, outside the project tree — not modified by Claude)</files>
  <action>This is a manual human-action task. The user performs the steps below to register the MCP server in `~/.claude.json` and restart Claude Code. Claude does NOT write to `~/.claude.json` (user-level file outside the project sandbox).</action>
  <what-built>
    CLAUDE.md is updated. The third MCP server entry must now be added to `~/.claude.json` (user-level config outside the project). Claude Code does not write to `~/.claude.json` directly; this is a user action.
  </what-built>
  <how-to-verify>
1. Choose a read-only password locally (e.g. `openssl rand -hex 16`). Call this `<RO_PASSWORD>`.

2. **Update the LOCAL COPY of `deploy/init-benchmark-db.sql`** in your working tree to put `<RO_PASSWORD>` in place of the committed `<PASSWORD>` placeholder. Then EITHER:
   - (recommended) keep the change LOCAL — never `git add` this edit. Do `git update-index --assume-unchanged deploy/init-benchmark-db.sql` if you want git to ignore the local edit. The committed file stays as `<PASSWORD>`.
   - OR use `bin/benchmark_db.sh reset` to recreate the volume with the new password. (`bin/benchmark_db.sh start` only runs init SQL on first volume init; if the volume exists, `reset` is required to apply password changes.)

3. Open `~/.claude.json` in your editor. Find the JSON path:
   ```
   projects -> "/home/aimfeld/Projects/Python/flawchess" -> mcpServers
   ```
   Add this entry alongside `flawchess-db` and `flawchess-prod-db`:
   ```json
   "flawchess-benchmark-db": {
     "command": "npx",
     "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://flawchess_benchmark_ro:<RO_PASSWORD>@localhost:5433/flawchess_benchmark"],
     "env": {}
   }
   ```
   Replace `<RO_PASSWORD>` with the actual password from step 1. Save and validate the JSON (e.g. `jq . ~/.claude.json > /dev/null` — no parse errors).

4. Restart Claude Code so the new MCP server is registered.

5. From a fresh Claude Code session, run an MCP query:
   - Either invoke `mcp__flawchess-benchmark-db__query` with `SELECT count(*) FROM games;` (expect `0` since no data ingested yet)
   - And `SELECT count(*) FROM game_positions;` (expect `0`)
   - And confirm an attempted write fails: `INSERT INTO games (id) VALUES (1);` must error (no INSERT privilege).

6. Confirm the `flawchess-benchmark` Docker stack is up: `docker compose -f docker-compose.benchmark.yml -p flawchess-benchmark ps` shows healthy.
  </how-to-verify>
  <acceptance_criteria>
    - `jq '.projects["/home/aimfeld/Projects/Python/flawchess"].mcpServers["flawchess-benchmark-db"]' ~/.claude.json` returns the JSON object (not null) — verify out-of-band by the user
    - `mcp__flawchess-benchmark-db__query` with `SELECT count(*) FROM games` returns `0` (or a number)
    - An attempted INSERT/UPDATE through the MCP server fails with a permission error
    - `docker compose -f docker-compose.benchmark.yml -p flawchess-benchmark ps` shows the container as healthy
  </acceptance_criteria>
  <resume-signal>Type "approved" once the MCP query succeeds, or describe any failure (JSON parse error, connection refused, write permission unexpectedly granted, etc.).</resume-signal>
  <verify>Manual — see acceptance_criteria above (MCP query exercise).</verify>
  <done>MCP server registered, Claude Code restarted, query returns row counts, INSERT through MCP fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.claude.json` (user-level) → benchmark DB | the connection string contains the RO password; file lives outside git |
| MCP runtime → benchmark DB | only SELECT operations should reach the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02 | Elevation of Privilege | flawchess_benchmark_ro role over MCP | mitigate | Plan 01's init SQL grants only SELECT; Task 03-02 step 5 verifies an INSERT through the MCP server fails. |
| T-69-03 | Information Disclosure | RO password in `~/.claude.json` | mitigate | Password lives in user-level config (not committed). The committed `deploy/init-benchmark-db.sql` keeps the `<PASSWORD>` placeholder. CLAUDE.md update in Task 03-01 documents this convention. |
| T-69-08 | Information Disclosure | RO password leaks via shell history or logs | accept | Standard local-dev convention. Same as `flawchess-prod-db` pattern. User is responsible for not echoing the password into shared logs. |
</threat_model>

<verification>
- Task 03-02 acceptance criteria (manual MCP smoke)
- `grep flawchess-benchmark-db CLAUDE.md` non-empty
</verification>

<success_criteria>
- CLAUDE.md documents `flawchess-benchmark-db` MCP server
- `~/.claude.json` contains the new entry (verified by user)
- `mcp__flawchess-benchmark-db__query` is callable from Claude Code after restart
- The RO role rejects writes via MCP
- INFRA-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/69-benchmark-db-infrastructure-ingestion-pipeline/69-03-SUMMARY.md`
</output>
