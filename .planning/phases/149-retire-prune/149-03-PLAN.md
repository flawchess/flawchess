---
phase: 149-retire-prune
plan: 03
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - tests/test_eval_worker_endpoints.py
  - app/routers/eval_remote.py
  - scripts/remote_eval_worker.py
autonomous: true
requirements: [PRUNE-01]
must_haves:
  truths:
    - "job_id → eval_jobs completion-stamping and the lichess-eval-game release-and-skip branch are covered by atomic-lane tests BEFORE the Gen-1 tests are deleted"
    - "The Gen-1 /lease and /submit endpoints, _apply_submit, and the worker _handle_full_ply_response handler no longer exist"
    - "Exactly the 28 pure-Gen-1 / Gen-1-dependent tests are removed; the 67 live-lane tests remain"
    - "The full backend suite is green after deletion"
  artifacts:
    - "New atomic-lane job_id/lichess-skip tests in TestAtomicSubmitEndpoint / TestAtomicLeaseEndpoint"
    - "Modified app/routers/eval_remote.py (Gen-1 endpoints + _apply_submit removed)"
    - "Modified scripts/remote_eval_worker.py (_handle_full_ply_response removed)"
    - "Modified tests/test_eval_worker_endpoints.py (28 Gen-1 tests removed, isolation-proof tests removed)"
  key_links:
    - "Atomic-submit retains the identical eval_jobs completion-stamping logic that _apply_submit had, now the sole covered path"
  prohibitions:
    - "MUST NOT touch /flaw-blob-lease, /flaw-blob-submit, or _apply_flaw_blob_submit (tier-4 backfill actively draining)"
    - "MUST NOT delete any entry-lane, atomic-lane, or flaw-blob test (67 KEEP tests stay)"
    - "MUST NOT delete the whole tests/test_eval_worker_endpoints.py file — surgical removal only"
    - "MUST NOT delete TestTier1Claiming before the atomic-lane job_id/lichess replacement tests are added (same plan, earlier task)"
    - "MUST NOT remove /entry-lease or /entry-submit (misleadingly-adjacent but live)"
---

<objective>
Delete the dead Gen-1 eval protocol — `POST /lease`, `POST /submit`, `_apply_submit` (eval_remote.py), the worker's `_handle_full_ply_response` handler (remote_eval_worker.py), and the 28 pure-Gen-1 tests — while keeping `/flaw-blob-*` and every live entry/atomic/flaw-blob lane untouched (PRUNE-01 / R2). Because deleting `TestTier1Claiming` removes the ONLY end-to-end coverage of job_id → eval_jobs completion-stamping and the lichess-eval-game release-and-skip branch (RESEARCH Pitfall 1), this plan first ports that coverage to the atomic lane, THEN deletes.

Purpose: remove one of three write-path copies so Phase 150 consolidates two, not three.
Output: atomic-lane replacement tests, then source + test deletion, leaving the suite green.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- New test functions in `TestAtomicSubmitEndpoint`: a real job_id stamps eval_jobs='completed'; a late/stale job_id is a no-op; job_id=None never touches eval_jobs.
- New test function in `TestAtomicLeaseEndpoint`: is_lichess_eval_game → release_job + 204 (ported from test_lichess_eval_game_claim_releases_lease).
- No new production symbols — this plan is net-negative on source.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-retire-prune/149-RESEARCH.md
@.planning/phases/149-retire-prune/149-PATTERNS.md
@.planning/phases/149-retire-prune/149-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port job_id stamping + lichess-skip coverage to the atomic lane (prerequisite)</name>
  <files>tests/test_eval_worker_endpoints.py</files>
  <read_first>
    - app/routers/eval_remote.py lines 1609-1624 (_apply_atomic_submit's eval_jobs completion-stamp logic — identical to _apply_submit 494-509) and 689-692 (the is_lichess_eval_game release_job + 204 branch on the lease)
    - tests/test_eval_worker_endpoints.py TestTier1Claiming (lines 727-1093 — the 5 methods whose scenarios must be ported: test_submit_with_job_id_stamps_eval_jobs, test_submit_without_job_id_does_not_touch_eval_jobs, test_late_submit_does_not_corrupt_eval_jobs, test_lichess_eval_game_claim_releases_lease, test_tier1_lease_returns_job_id)
    - tests/test_eval_worker_endpoints.py TestAtomicSubmitEndpoint + TestAtomicLeaseEndpoint (existing fixtures; note all 7 atomic-submit tests currently pass job_id=None)
    - 149-RESEARCH.md Pitfall 1 (exact replacement-test list)
  </read_first>
  <behavior>
    - `/atomic-submit` with a real leased job_id marks the matching eval_jobs row status='completed' (WHERE status='leased' idempotency guard preserved).
    - `/atomic-submit` with a stale/already-completed (or re-leased) job_id is a no-op, not a corruption.
    - `/atomic-submit` with job_id=None (tier-3 pick) never touches eval_jobs.
    - `/atomic-lease` for a game flagged is_lichess_eval_game releases the job and returns 204 (no positions leased).
  </behavior>
  <action>
    Add tests to `TestAtomicSubmitEndpoint` exercising the eval_jobs completion-stamp path that only `TestTier1Claiming` covers today: (a) `test_atomic_submit_with_job_id_stamps_eval_jobs` — seed a leased eval_jobs row, submit with that job_id, assert status becomes 'completed'; (b) `test_atomic_submit_late_job_id_is_noop` — a job_id already completed or re-leased is not corrupted (status unchanged); (c) `test_atomic_submit_without_job_id_does_not_touch_eval_jobs` — job_id=None leaves eval_jobs untouched. Add to `TestAtomicLeaseEndpoint` a `test_atomic_lease_lichess_eval_game_releases_lease` porting test_lichess_eval_game_claim_releases_lease's scenario against the is_lichess_eval_game → release_job + 204 branch. Mirror the existing atomic-lane fixtures (operator token, X-Worker-Id header, seeded game/eval_jobs rows). Run the new tests and confirm green BEFORE any deletion in Task 2/3 — these must exist and pass first (Pitfall 1: otherwise the coverage gap ships silently green).
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_worker_endpoints.py::TestAtomicSubmitEndpoint tests/test_eval_worker_endpoints.py::TestAtomicLeaseEndpoint -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "atomic_submit_with_job_id_stamps_eval_jobs\|atomic_submit_late_job_id_is_noop\|atomic_submit_without_job_id_does_not_touch_eval_jobs\|atomic_lease_lichess_eval_game_releases_lease" tests/test_eval_worker_endpoints.py` is at least 4
    - The four new tests pass against the current (pre-deletion) tree
    - No Gen-1 test has been removed yet (this task only adds)
  </acceptance_criteria>
  <done>The atomic lane now has end-to-end coverage of job_id completion-stamping (real / stale / none) and the lichess-eval-game release branch, all green, before deletion begins.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Gen-1 endpoints, _apply_submit, and the worker handler</name>
  <files>app/routers/eval_remote.py, scripts/remote_eval_worker.py</files>
  <read_first>
    - app/routers/eval_remote.py lines 330-528 (_apply_submit body), 553-647 (the `@router.post("/lease")` handler), 746-775 (the `@router.post("/submit")` handler)
    - scripts/remote_eval_worker.py lines 656-704 (_handle_full_ply_response — confirmed unreachable per Phase 147 D-02/D-05)
    - 149-RESEARCH.md Runtime State Inventory (re-verify zero legacy prod traffic before this ships) + Pitfall 5 (delete bottom-up or match on unique signature, not fixed line numbers)
  </read_first>
  <action>
    Remove the Gen-1 protocol from `app/routers/eval_remote.py`: the `@router.post("/lease", ...)` handler (the exact-path lease, NOT /entry-lease, /atomic-lease, or /flaw-blob-lease), the `@router.post("/submit", ...)` handler `submit_eval` (NOT /entry-submit, /atomic-submit, or /flaw-blob-submit), and the `_apply_submit` function plus any now-unused private helpers it alone called (verify each helper has no other caller via grep before removing). Remove `_handle_full_ply_response` from `scripts/remote_eval_worker.py` and any now-dead references to it in the worker's dispatch (confirm `_run_cycle` no longer names it — it is already unreachable). Delete any now-unused imports left dangling. Match on unique signatures/decorators rather than fixed line ranges and work bottom-up (Pitfall 5). Keep `worker_id_label`, `require_operator_token`, and all live handlers intact. Before merge, re-run the prod-log zero-legacy-traffic check per RESEARCH Runtime State Inventory.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_worker_endpoints.py -x &amp;&amp; uv run ty check app/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@router.post("/lease"' app/routers/eval_remote.py` is 0
    - `grep -c '@router.post("/submit"' app/routers/eval_remote.py` is 0
    - `grep -c "async def _apply_submit" app/routers/eval_remote.py` is 0
    - `grep -c "async def _handle_full_ply_response" scripts/remote_eval_worker.py` is 0
    - `grep -c '"/flaw-blob-lease"\|"/flaw-blob-submit"\|"/entry-lease"\|"/entry-submit"\|"/atomic-lease"\|"/atomic-submit"' app/routers/eval_remote.py` is unchanged (all six live routes still registered)
    - `uv run ty check app/` reports zero errors (no dangling imports)
  </acceptance_criteria>
  <done>The Gen-1 endpoints, _apply_submit, and the worker's dead handler are gone; all live lanes and /flaw-blob-* remain registered.</done>
</task>

<task type="auto">
  <name>Task 3: Surgical removal of the 28 Gen-1 tests</name>
  <files>tests/test_eval_worker_endpoints.py</files>
  <read_first>
    - 149-RESEARCH.md "Verified test-file surgery map" (the exact DELETE table: 287-425, 426-726, TestTier1Claiming 727-1093, 1871-1979, 2071-2195, TestMultipv2BlobsRemote 2196-2493, 2494-2644, 2645-2958, plus test_blob_lease_does_not_touch_submit ~3328-3378 and test_blob_submit_does_not_touch_apply_submit ~4790-4837) and the 67 KEEP list
    - 149-RESEARCH.md Pitfall 4 (the two isolation-proof tests reference the deleted /submit as their subject — delete the functions, not their containing KEEP classes) and Pitfall 5 (line numbers shift — delete bottom-up or match on function/class name)
  </read_first>
  <action>
    Delete exactly the 28 pure-Gen-1 / Gen-1-dependent tests enumerated in the RESEARCH surgery map: the `/lease`-only and `/submit`-only test groups, the whole `TestTier1Claiming` class (its coverage is now ported to the atomic lane in Task 1), the `/lease?scope=` param tests, the `/lease` worker-id tests, the `TestMultipv2BlobsRemote` class, the Phase-146 `/submit` blob tests, `test_submit_suppresses_cp_flaw_tag_then_blob_submit_self_heals` (atomic equivalent already exists), and the two isolation-proof functions `test_blob_lease_does_not_touch_submit` + `test_blob_submit_does_not_touch_apply_submit` (delete just these two functions — their containing flaw-blob classes are KEEP). Match on class/function names (not fixed line ranges) and delete bottom-up so shifting line numbers cannot mis-target (Pitfall 5). Do NOT delete any of the 67 KEEP tests (all TestAtomicLeaseEndpoint/TestAtomicSubmitEndpoint including Task 1's additions, all entry-lane tests, all remaining flaw-blob tests, TestBlobAssemblyHelper, the SEED-076 atomic-cache tests, and test_lease_partition/lifo/reclaim/leased_by which are entry-lane despite the name). Remove any test-only imports/URL constants (`_LEASE_URL`, `_SUBMIT_URL`) that become unused.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_worker_endpoints.py -x &amp;&amp; uv run pytest -n auto -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestTier1Claiming\|class TestMultipv2BlobsRemote" tests/test_eval_worker_endpoints.py` is 0
    - `grep -c "test_blob_lease_does_not_touch_submit\|test_blob_submit_does_not_touch_apply_submit" tests/test_eval_worker_endpoints.py` is 0
    - `grep -c "class TestAtomicSubmitEndpoint\|class TestAtomicLeaseEndpoint\|class TestFlawBlobLeaseEndpoint\|class TestFlawBlobSubmitEndpoint\|class TestBlobAssemblyHelper" tests/test_eval_worker_endpoints.py` is unchanged (5 KEEP classes remain)
    - `uv run pytest tests/test_eval_worker_endpoints.py --collect-only -q | tail -1` shows the collected count dropped by ~28 (from 95 toward ~67 plus Task 1 additions)
    - `uv run pytest -n auto -x` full backend suite green
  </acceptance_criteria>
  <done>Exactly the Gen-1 and Gen-1-dependent tests are removed; all live-lane tests plus Task 1's atomic additions remain; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote worker fleet → deleted endpoints | a not-yet-upgraded worker calling /lease or /submit would 404 after deletion |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-03-DoS | Denial of Service | a stale worker resurfacing on the deleted routes | low | mitigate | Re-run the prod-log zero-legacy-traffic grep immediately before merging (RESEARCH Runtime State Inventory action item); fleet confirmed 100% atomic. |
| T-149-03-RE | Repudiation/Coverage-loss | deleting TestTier1Claiming drops job_id/lichess coverage | high | mitigate | Task 1 ports the coverage to the atomic lane BEFORE Task 3 deletes (Pitfall 1) — the blocking-high mitigation. |
| T-149-03-TA | Tampering | accidental deletion of a live-lane route/test | high | mitigate | Grep gates assert all six live routes + five KEEP classes survive; match-on-name bottom-up deletion (Pitfall 5). |
</threat_model>

<verification>
- Task 1 atomic-lane replacement tests green BEFORE any deletion
- `uv run pytest -n auto -x` full suite green after deletion
- `uv run ty check app/ tests/` zero errors
- Manual: re-verify zero legacy /lease + /submit prod traffic before the branch merges (Runtime State Inventory)
</verification>

<success_criteria>
The dead Gen-1 protocol no longer exists in source or tests, /flaw-blob-* and all live lanes are intact, job_id/lichess coverage lives on the atomic lane, and the full backend suite is green.
</success_criteria>

<output>
Create `.planning/phases/149-retire-prune/149-03-SUMMARY.md` when done
</output>
