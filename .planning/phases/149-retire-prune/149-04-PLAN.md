---
phase: 149-retire-prune
plan: 04
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - app/services/zobrist.py
  - app/services/chesscom_to_lichess.py
  - app/models/game.py
  - app/models/eval_jobs.py
  - tests/test_zobrist.py
  - tests/test_seed_openings.py
  - tests/services/test_chesscom_to_lichess.py
autonomous: true
requirements: [PRUNE-02]
must_haves:
  truths:
    - "hashes_for_game is removed and its only surviving production behavior (import-time hashing) is proven equivalent via process_game_pgn in test_seed_openings"
    - "Only the dead Table-3 lookup surface (LICHESS_BLITZ_INTRA_TC + the two lichess-blitz lookup functions + _LICHESS_BLITZ_KEYS) is removed from chesscom_to_lichess; the live Tables 1/2 conversion path is untouched"
    - "The caller-less Game.needs_engine_full_evals hybrid property (and its inplace expression) is removed with no live query relying on it"
    - "The unused TIER_AUTO_WINDOW constant is removed and its speculative 'future mode' narrative trimmed; the eval_jobs.tier DB column is kept"
    - "No live path changes behavior; the full backend suite is green"
  artifacts:
    - "Modified app/services/zobrist.py, app/services/chesscom_to_lichess.py, app/models/game.py, app/models/eval_jobs.py"
    - "Modified tests (test_zobrist.py deletions, test_seed_openings.py rewrites, test_chesscom_to_lichess.py deletions)"
  key_links:
    - "canonical_slice_sql.py still imports composed_chesscom_to_lichess_grid / convert_chesscom_to_lichess (Tables 1/2) after the edit"
  prohibitions:
    - "MUST NOT delete the whole chesscom_to_lichess.py module (canonical_slice_sql.py depends on Tables 1/2) — RESEARCH correction"
    - "MUST NOT touch CHESSCOM_INTRA_TC, CHESSCOM_BLITZ_TO_LICHESS, convert_chesscom_to_lichess, composed_chesscom_to_lichess_grid, lookup_uscf_from_chesscom_blitz, lookup_fide_from_chesscom_blitz"
    - "MUST NOT drop the eval_jobs.tier DB column or search for a nonexistent tier-2 `if tier == 2` branch (there is none — RESEARCH Pitfall 2)"
    - "MUST NOT delete or rewrite the seed-opening hash-equivalence assertion in test_seed_openings.py — rewrite it to use process_game_pgn, keep it passing"
    - "MUST NOT remove the ix_games partial index or the raw `full_evals_completed_at IS NULL` predicates that live queries use directly"
---

<objective>
Remove genuinely-dead weight (PRUNE-02 / R12), scoped precisely per RESEARCH's corrections: the caller-less `hashes_for_game` wrapper (zobrist.py), only the dead Table-3 lookup surface in `chesscom_to_lichess.py` (NOT the whole module — Tables 1/2 are live), the caller-less `Game.needs_engine_full_evals` hybrid, and the unused `TIER_AUTO_WINDOW` constant (keeping the eval_jobs.tier column). No live path changes behavior.

Purpose: shrink the surface Phase 150 refactors; eliminate misleading dead code that implies capabilities that were already removed in earlier phases.
Output: targeted deletions + test rewrites, suite green.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- No new symbols — net-negative. Removed: `hashes_for_game`; `LICHESS_BLITZ_INTRA_TC`, `_LICHESS_BLITZ_KEYS`, `lookup_uscf_from_lichess_blitz`, `lookup_fide_from_lichess_blitz`; `Game.needs_engine_full_evals` (property + inplace expression); `TIER_AUTO_WINDOW`.
- Rewritten `test_seed_openings.py` hash assertions to use `process_game_pgn`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-retire-prune/149-RESEARCH.md
@.planning/phases/149-retire-prune/149-PATTERNS.md
@.planning/phases/149-retire-prune/149-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hashes_for_game and rewrite the seed-opening equivalence test</name>
  <files>app/services/zobrist.py, tests/test_zobrist.py, tests/test_seed_openings.py</files>
  <read_first>
    - app/services/zobrist.py around line 270 (hashes_for_game) and lines 138-153 (GameProcessingResult TypedDict shape returned by process_game_pgn)
    - 149-RESEARCH.md "hashes_for_game deletion scope" (17 dedicated tests + the equivalence test to delete; the 2 seed-openings references to REWRITE, not delete)
    - tests/test_seed_openings.py lines ~70-76 (the hashes_for_game references that must be swapped to process_game_pgn while keeping the assertion meaningful)
  </read_first>
  <action>
    Confirm `hashes_for_game` has zero production callers (grep across app/ and scripts/ — RESEARCH found none), then delete the `hashes_for_game` function from `app/services/zobrist.py` and any now-unused private helpers it alone used. In `tests/test_zobrist.py`, delete the ~17 dedicated `test_hashes_for_game_*` tests and the `test_hashes_for_game_wrapper_matches_process_game_pgn` equivalence test (its purpose is served once process_game_pgn is the sole implementation). In `tests/test_seed_openings.py`, REWRITE (do not delete) the two references at lines ~70-76: swap `hashes_for_game(pgn)` for the equivalent value pulled from `process_game_pgn(pgn)` (the `plies[...]["full_hash"]` shape per the GameProcessingResult TypedDict at zobrist.py:138-153), keeping the seed-opening-hash-matches-import-time-hash assertion intact and passing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_zobrist.py tests/test_seed_openings.py -x &amp;&amp; uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "def hashes_for_game" app/services/zobrist.py` is 0
    - `grep -rc "hashes_for_game" app/ scripts/` is 0 (no production reference remains)
    - `grep -c "hashes_for_game" tests/test_seed_openings.py` is 0 (references rewritten to process_game_pgn)
    - tests/test_seed_openings.py still contains a passing seed-opening hash-equivalence assertion (rewritten, not deleted)
    - `uv run pytest tests/test_zobrist.py tests/test_seed_openings.py` passes; `uv run ty check app/ tests/` zero errors
  </acceptance_criteria>
  <done>hashes_for_game is gone, dedicated tests removed, and the seed-opening equivalence test still proves import-time hashing correctness via process_game_pgn.</done>
</task>

<task type="auto">
  <name>Task 2: Trim dead Table-3 lookups, needs_engine_full_evals, and TIER_AUTO_WINDOW</name>
  <files>app/services/chesscom_to_lichess.py, app/models/game.py, app/models/eval_jobs.py, tests/services/test_chesscom_to_lichess.py</files>
  <read_first>
    - 149-RESEARCH.md "chesscom_to_lichess.py deletion scope" (DELETE Table 3 + _LICHESS_BLITZ_KEYS + the two lichess-blitz lookup fns; KEEP Tables 1/2 + convert/composed + the two chesscom-blitz lookup fns)
    - app/services/canonical_slice_sql.py (the live importer of Tables 1/2 — confirm its imports still resolve after the edit)
    - app/models/game.py lines 257-276 (needs_engine_full_evals hybrid property + _needs_engine_full_evals_expression) and lines 68-72 (the migration-index comment that references the predicate)
    - app/models/eval_jobs.py lines 12-17 (TIER_AUTO_WINDOW constant + doc comment) and app/services/eval_queue_service.py lines 5 + 19 + 288 (the tier-2 narrative + predicate comments) — 149-RESEARCH.md Pitfall 2 (there is NO deletable tier-2 branch; the tier DB column stays)
  </read_first>
  <action>
    In `app/services/chesscom_to_lichess.py` delete only the dead Table-3 surface: `LICHESS_BLITZ_INTRA_TC`, `_LICHESS_BLITZ_KEYS`, `lookup_uscf_from_lichess_blitz`, `lookup_fide_from_lichess_blitz`. Leave Tables 1/2 (`CHESSCOM_INTRA_TC`, `CHESSCOM_BLITZ_TO_LICHESS`), `convert_chesscom_to_lichess`, `composed_chesscom_to_lichess_grid`, and the chesscom-blitz lookups untouched; verify `canonical_slice_sql.py`'s imports still resolve. In `tests/services/test_chesscom_to_lichess.py` delete the tests that reference `LICHESS_BLITZ_INTRA_TC` (lines ~33, 149, 159). In `app/models/game.py` delete the `needs_engine_full_evals` hybrid property and its `_needs_engine_full_evals_expression` inplace expression (confirmed zero real callers — only comments reference the name); keep the partial index and the raw `full_evals_completed_at IS NULL AND ...` predicates that live queries use directly, and update any comment that pointed at the now-removed property so it references the raw predicate instead (do not leave a comment naming a deleted symbol). In `app/models/eval_jobs.py` delete the unused `TIER_AUTO_WINDOW` constant and trim the speculative "future per-user mode" narrative from its doc comment and the two mirroring comments in `eval_queue_service.py` (lines 5/19/288 region) — keep the `tier` DB column and the tier-agnostic claim SQL exactly as-is (Pitfall 2: no code branch to delete). Do NOT hunt for an `if tier == 2` branch.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_chesscom_to_lichess.py tests/test_import_service.py -x &amp;&amp; uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "LICHESS_BLITZ_INTRA_TC\|lookup_uscf_from_lichess_blitz\|lookup_fide_from_lichess_blitz\|_LICHESS_BLITZ_KEYS" app/ tests/` is 0
    - `grep -c "composed_chesscom_to_lichess_grid\|convert_chesscom_to_lichess" app/services/canonical_slice_sql.py` is unchanged (live Tables 1/2 path intact) and `uv run ty check app/` resolves canonical_slice_sql imports
    - `grep -c "def needs_engine_full_evals\|_needs_engine_full_evals_expression" app/models/game.py` is 0
    - `grep -rc "TIER_AUTO_WINDOW" app/` is 0
    - `grep -c '"tier"\|tier:' app/models/eval_jobs.py` still shows the tier column definition (DB column kept)
    - `uv run pytest tests/services/test_chesscom_to_lichess.py tests/test_import_service.py` passes; `uv run ty check app/ tests/` zero errors
  </acceptance_criteria>
  <done>Dead Table-3 lookups, the caller-less hybrid property, and the unused tier-2 constant are removed with the live conversion path, the tier column, and the raw eval predicate all intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | pure dead-code removal; no data or request boundary changes |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-04-TA | Tampering | over-deletion breaking the live Tables 1/2 conversion path | high | mitigate | Scope deletion to Table-3-only per RESEARCH; grep gate asserts canonical_slice_sql imports survive + ty resolves. |
| T-149-04-TA2 | Tampering | dropping the tier column or the live eval predicate | high | mitigate | Explicit prohibition: keep the tier column + raw predicate; only the unused constant + narrative are removed (Pitfall 2). |
</threat_model>

<verification>
- `uv run pytest tests/test_zobrist.py tests/test_seed_openings.py tests/services/test_chesscom_to_lichess.py tests/test_import_service.py -x` green
- `uv run pytest -n auto -x` full suite green (no live behavior change)
- `uv run ty check app/ tests/` zero errors
</verification>

<success_criteria>
The four dead-weight items are removed with zero behavior change to any live path; the tier column, live conversion tables, raw eval predicate, and a rewritten seed-opening equivalence test all remain; suite green.
</success_criteria>

<output>
Create `.planning/phases/149-retire-prune/149-04-SUMMARY.md` when done
</output>
