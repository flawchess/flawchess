---
phase: 83
plan: 05
type: execute
wave: 4
depends_on: [83-02, 83-04]
files_modified:
  - app/services/insights_service.py
  - app/services/insights_llm.py
  - app/prompts/endgame_insights.md
  - tests/services/test_insights_service.py
autonomous: true
requirements: [D-17, D-18, D-19, D-20]
must_haves:
  truths:
    - "MetricId Literal in app/services/insights_service.py includes 'entry_expected_score'"
    - "_findings_endgame_start_vs_end emits a third SubsectionFinding for entry_expected_score with metric='entry_expected_score', zone via assign_zone, dimension=None, trend='n_a', no verdict field"
    - "Sample-size gate entry_expected_score_n >= 10 is applied; below the gate emits an empty finding via _empty_finding"
    - "_PROMPT_VERSION bumps from endgame_v24 to endgame_v25 with the D-20 changelog string prepended; prior version history preserved"
    - "endgame_insights.md gains a new entry_expected_score glossary entry under '## Metric glossary' with UI label 'Achievable score', the Lichess sigmoid + mate->0/1 derivation, the 2300+ baseline framing, no 'underperformance' framing, and a cohort typical band reference to reports/benchmarks-2026-05-11.md"
    - "The existing '### Subsection: endgame_start_vs_end' block in endgame_insights.md is extended with a paragraph explaining the achievable-vs-achieved gap as the headline diagnostic with entry_eval_pawns as the explanatory unit, and the three-finding ordering logic"
  artifacts:
    - path: "app/services/insights_service.py"
      provides: "Third SubsectionFinding (entry_expected_score) emitted by _findings_endgame_start_vs_end"
      contains: "entry_expected_score"
    - path: "app/services/insights_llm.py"
      provides: "_PROMPT_VERSION = endgame_v25 with prepended changelog"
      contains: "endgame_v25"
    - path: "app/prompts/endgame_insights.md"
      provides: "New entry_expected_score glossary entry + extended endgame_start_vs_end subsection block"
      contains: "entry_expected_score"
    - path: "tests/services/test_insights_service.py"
      provides: "At least 3 new test cases covering finding emission, sample-size gate, and three-finding ordering"
      contains: "entry_expected_score"
  key_links:
    - from: "app/services/insights_service.py"
      to: "app/schemas/endgames.py"
      via: "reads perf.entry_expected_score and perf.entry_expected_score_n from Plan 2 schema"
      pattern: "perf\\.entry_expected_score"
    - from: "app/services/insights_service.py"
      to: "app/services/endgame_zones.py"
      via: "assign_zone('entry_expected_score', value) requires Plan 4's ZONE_REGISTRY entry"
      pattern: "assign_zone\\(\"entry_expected_score\""
---

<objective>
Teach the LLM about the new `entry_expected_score` metric so the endgame insights pipeline narrates the achievable-vs-achieved gap from day one. This is the same agreement-from-launch logic as Phase 82 D-13: tile and LLM must agree on what is narratable.

Three threads:
1. Backend payload (`insights_service.py`): add `"entry_expected_score"` to `MetricId` Literal; extend `_findings_endgame_start_vs_end` to emit a third `SubsectionFinding` mirroring the existing two; bump `_PROMPT_VERSION` to `endgame_v25` (cache key includes prompt_version, so this invalidates cached responses automatically)
2. Prompt asset (`endgame_insights.md`): add a glossary entry for `entry_expected_score` matching the `entry_eval_pawns` shape; extend the existing `### Subsection: endgame_start_vs_end` block with the achievable-vs-achieved gap framing and example narrations (D-18)
3. Tests (`test_insights_service.py`): assert the third finding is emitted with correct shape and sample-size gating

Purpose: D-19 mirrors the no-`verdict` discipline from Phase 82 D-06 (LLM narrates by zone, not by sig-test outcome). D-18 establishes the gap-as-headline framing with `entry_eval_pawns` as the explanatory unit. D-20 documents the version bump explicitly.

Output: 5-field-aware backend emitter + extended glossary + extended subsection block + prompt-version bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-02-SUMMARY.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-04-SUMMARY.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-CONTEXT.md

@app/services/insights_service.py
@app/services/insights_llm.py
@app/prompts/endgame_insights.md
@tests/services/test_insights_service.py
@app/schemas/endgames.py
@app/services/endgame_zones.py

<interfaces>
Existing _findings_endgame_start_vs_end shape (insights_service.py:443-502, Phase 82 emitter) — pattern to mirror for the third finding:

```python
tile1 = SubsectionFinding(
    subsection_id="endgame_start_vs_end",
    parent_subsection_id=None,
    window=window,
    metric="entry_eval_pawns",
    value=entry_eval,
    zone=assign_zone("entry_eval_pawns", entry_eval),
    trend="n_a",
    weekly_points_in_window=0,
    sample_size=n_eval,
    sample_quality=eval_quality,
    is_headline_eligible=eval_quality != "thin",
    dimension=None,
)
# (tile2 mirrors for endgame_score)
return [tile1, tile2]
```

Target: add a parallel `tile3` for `entry_expected_score`; return `[tile1, tile2, tile3]`.

_PROMPT_VERSION format (insights_llm.py:66 — prepend-style changelog):

The new value MUST be `"endgame_v25"` (string literal). The trailing comment prepends a new changelog block with the D-20 text, then preserves the entire historical changelog string verbatim. Cache invalidation is automatic via prompt_version=_PROMPT_VERSION participation in the cache key (insights_llm.py:1892).

D-20 changelog string (verbatim):
> v25 (260511 entry_expected_score): wire Stockfish-baseline achievable score (Lichess sigmoid) into the endgame_start_vs_end subsection alongside entry_eval_pawns and endgame_score. New ENTRY_EXPECTED_SCORE_ZONES from reports/benchmarks-2026-05-11.md. LLM narrates the achievable-vs-achieved gap as the headline diagnostic with entry_eval_pawns as the explanatory unit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add entry_expected_score to MetricId Literal + emit third finding</name>
  <files>app/services/insights_service.py, tests/services/test_insights_service.py</files>
  <read_first>
    - app/services/insights_service.py (full _findings_endgame_start_vs_end at lines 443-502 + the MetricId Literal slot)
    - app/schemas/endgames.py (Plan 2 output: entry_expected_score* field names)
    - app/services/endgame_zones.py (Plan 4 output: ZONE_REGISTRY entry for entry_expected_score; verify assign_zone works for the new metric)
    - tests/services/test_insights_service.py (existing endgame_start_vs_end test cases — extend)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "app/services/insights_service.py _findings_endgame_start_vs_end")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-17, D-19)
    - .planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-CONTEXT.md (D-06: no verdict field; D-17: sample-size gate >= 10)
  </read_first>
  <behavior>
    - "entry_expected_score" is in the MetricId Literal in insights_service.py
    - _findings_endgame_start_vs_end returns 3 findings (was 2)
    - The third finding has metric="entry_expected_score", value=perf.entry_expected_score, zone=assign_zone("entry_expected_score", perf.entry_expected_score), trend="n_a", weekly_points_in_window=0, sample_size=perf.entry_expected_score_n, sample_quality=sample_quality("endgame_start_vs_end", perf.entry_expected_score_n), is_headline_eligible=(quality != "thin"), dimension=None, parent_subsection_id=None, subsection_id="endgame_start_vs_end"
    - When perf.entry_expected_score_n < 10: tile3 = _empty_finding("endgame_start_vs_end", window, "entry_expected_score")
    - The SubsectionFinding has NO `verdict` field (D-19, Phase 82 D-06 carried forward)
    - The existing tile1 and tile2 emissions are unchanged
    - Order returned: [tile1, tile2, tile3]
  </behavior>
  <action>
    Step 1: Add `"entry_expected_score"` to the MetricId Literal in `app/services/insights_service.py`. Locate the existing Literal slot near the top of the file (find via grep for `MetricId =`). Insert near the existing `"entry_eval_pawns"` entry. Add inline comment `# Phase 83 D-17: achievable score (Lichess sigmoid)`.

    Step 2: Modify `_findings_endgame_start_vs_end` (lines 443-502) per PATTERNS.md to emit a third finding. Mirror the existing tile1 emission exactly: read `perf.entry_expected_score` and `perf.entry_expected_score_n`; apply the sample-size gate `< 10` -> `_empty_finding(...)`; else build a `SubsectionFinding` with the field values listed in the Behavior bullet above. Update the docstring on `_findings_endgame_start_vs_end` from "TWO findings" (or its current wording) to "THREE findings (entry_eval_pawns, endgame_score, entry_expected_score)". Return `[tile1, tile2, tile3]`.

    Step 3: Extend `tests/services/test_insights_service.py` with these cases (one test class, or appended to the existing endgame_start_vs_end test class):
      - `test_third_finding_emitted_when_n_at_or_above_10`: build a `EndgamePerformanceResponse(...)` with `entry_expected_score_n = 50`, `entry_expected_score = 0.58`. Call the emitter. Assert `len(findings) == 3`, `findings[2].metric == "entry_expected_score"`, `findings[2].value == 0.58`, `findings[2].zone in {"weak", "typical", "strong"}`, `findings[2].dimension is None`, `findings[2].trend == "n_a"`, `findings[2].sample_size == 50`
      - `test_third_finding_empty_when_n_below_10`: `entry_expected_score_n = 9` -> assert the third finding has the shape returned by `_empty_finding(...)`
      - `test_third_finding_has_no_verdict_field`: assert that `SubsectionFinding` does NOT have a `verdict` attribute set (or that `findings[2]` does not include a `verdict` key when serialized) — guards against a future regression that re-adds a sig-test signal (memory `feedback_llm_significance_signal.md`)
      - Optional: `test_three_finding_ordering_preserved`: assert `[f.metric for f in findings] == ["entry_eval_pawns", "endgame_score", "entry_expected_score"]`

    Use the existing test fixture helpers; do not introduce a new pattern.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_insights_service.py -k 'endgame_start_vs_end or third_finding' -x && uv run ty check app/services/insights_service.py tests/services/test_insights_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"entry_expected_score"' app/services/insights_service.py | grep -v '^#'` returns >= 2 (MetricId Literal + assign_zone call + emitter metric=)
    - `grep -c "tile3\|entry_expected_score" app/services/insights_service.py` reflects the new emitter block
    - `grep -c "verdict" app/services/insights_service.py | grep -v '^#'` returns the SAME count as before the change (no new `verdict` field added; memory `feedback_llm_significance_signal.md`)
    - The emitter returns 3 findings in the order [entry_eval_pawns, endgame_score, entry_expected_score]
    - All new tests pass; existing tests unchanged and passing
    - ty-clean on the modified files
  </acceptance_criteria>
  <done>
    Backend payload now includes a third SubsectionFinding for entry_expected_score that the LLM can read.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bump _PROMPT_VERSION to endgame_v25 + extend endgame_insights.md</name>
  <files>app/services/insights_llm.py, app/prompts/endgame_insights.md</files>
  <read_first>
    - app/services/insights_llm.py (line 66: _PROMPT_VERSION constant + the full prepended changelog string)
    - app/prompts/endgame_insights.md (full file; especially the entry_eval_pawns glossary entry at lines 323-327 and the existing "### Subsection: endgame_start_vs_end" block at lines 260-286)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (sections "app/services/insights_llm.py" and "app/prompts/endgame_insights.md")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-17, D-18, D-20)
    - reports/benchmarks-2026-05-11.md (Plan 4 output: cite section X for the cohort band)
  </read_first>
  <behavior>
    - _PROMPT_VERSION equals the string "endgame_v25"
    - The trailing inline comment begins with the D-20 changelog string (verbatim, see Interfaces block), followed by the prior changelog text intact (every prior vN block preserved)
    - endgame_insights.md "## Metric glossary" section gains a new entry for entry_expected_score with: UI label "Achievable score", derivation (Lichess sigmoid `1 / (1 + exp(-0.00368208 * cp))` + mate->0/1 mapping), explicit note that mate positions ARE included (unlike entry_eval_pawns), scale (percentage in [0,100]), cohort typical band reference to reports/benchmarks-2026-05-11.md section X, sig-test framing (Wilson vs 50% used by the tile; LLM does NOT receive the sig-test outcome), and "2300+ baseline / sub-2300 normally falls below" framing
    - The glossary entry explicitly forbids "underperformance", "fall short", "below your potential" and any synonym framing the gap as a personal failing
    - The "### Subsection: endgame_start_vs_end" block is extended with a paragraph per D-18: the LLM should narrate the gap between entry_expected_score and endgame_score as the headline diagnostic, with entry_eval_pawns as the explanatory unit. Includes at least two worked example narrations from CONTEXT.md D-18 ("Stockfish-baseline says positions like yours score 58%, but you scored 47%..." and "Achievable 49%, you scored 52%...")
    - Three-finding ordering guidance added: lead with the gap when it is the dominant signal; lead with entry_eval when entry_eval is dominant; descriptive (not prescriptive) for sub-2300 users
  </behavior>
  <action>
    Step 1: In `app/services/insights_llm.py` line 66, change the `_PROMPT_VERSION` string from `"endgame_v24"` to `"endgame_v25"`. Prepend the D-20 changelog block (verbatim, Interfaces block) to the inline comment, keeping the entire historical changelog string intact after it (the existing pattern is to prepend new blocks at the front while preserving every prior `vN ...` block chronologically).

    Step 2: In `app/prompts/endgame_insights.md` "## Metric glossary" section, add a new entry for `entry_expected_score` modelled exactly on the `entry_eval_pawns` entry at lines 323-327. Fields to include:
      - UI label: "Achievable score"
      - Derivation: per-user mean Stockfish-baseline expected score from endgame-entry positions, on the 0-100% W+0.5D scale. Lichess winning-chances sigmoid `1 / (1 + exp(-0.00368208 * cp))` applied to signed user-perspective `eval_cp`; mate positions map directly to 0 or 1 (mate-for-user = 1.0; mate-against-user = 0.0). Mate positions ARE included in this cohort (unlike entry_eval_pawns).
      - Scale: whole-number percentage in [0, 100]. Attach `%` when narrating.
      - Cohort typical band: reference `reports/benchmarks-2026-05-11.md` section X
      - Sig-test framing: Wilson test vs 50% used by the tile; the LLM does NOT receive the sig-test outcome — narrate strictly from `zone` + `sample_quality` + `[near edge]`
      - "2300+ baseline" framing per D-10: this is what a 2300+ rated player would score from these positions. Sub-2300 users normally fall below; the gap is rating-tilt by default, not a flaw. Forbidden words: "underperformance", "fall short", "below your potential", and any synonym
      - Emitted in subsection `endgame_start_vs_end`, `dimension=None`

    Step 3: Extend the existing "### Subsection: endgame_start_vs_end" block (lines 260-286). Add a paragraph (per D-18) explaining:
      - The achievable-vs-achieved gap is the headline diagnostic when both findings are present and at least one is in a colored zone
      - entry_eval_pawns is the explanatory unit (signed pawns more intuitive than 0-1 score; both convey the same information)
      - Two worked example narrations lifted verbatim from CONTEXT.md D-18:
        > "Stockfish-baseline says positions like yours score 58%, but you scored 47% — about 11 points below the engine ceiling, mostly explained by entering at +0.4 pawns"
        > "Achievable 49%, you scored 52% — defended slightly better than the engine baseline from these positions"
      - Headline ordering note (Claude's Discretion per CONTEXT.md): lead with the gap when it is dominant; lead with entry_eval when entry_eval is dominant
      - Sub-2300 gap framing: descriptive, not prescriptive; no "underperformance" framing

    Use commas, periods, parentheses, colons in narrative copy. Avoid em-dashes (CLAUDE.md "Em-dashes sparingly"); a single em-dash per paragraph is OK in the worked narrations because the source D-18 strings use them, but elsewhere prefer alternatives.
  </action>
  <verify>
    <automated>grep -c '"endgame_v25"' app/services/insights_llm.py && grep -c 'endgame_v24' app/services/insights_llm.py && grep -c 'entry_expected_score' app/prompts/endgame_insights.md && grep -c 'Achievable score' app/prompts/endgame_insights.md && (grep -ci 'underperformance' app/prompts/endgame_insights.md | grep -E '^[02-9]'; test $? -eq 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"endgame_v25"' app/services/insights_llm.py` returns 1
    - `grep -c 'endgame_v24' app/services/insights_llm.py` returns >= 1 (preserved in changelog)
    - The D-20 changelog block appears at the front of the inline comment, before "v24"
    - `grep -c 'entry_expected_score' app/prompts/endgame_insights.md` returns >= 2 (glossary + subsection block)
    - `grep -c 'Achievable score' app/prompts/endgame_insights.md` returns >= 1
    - `grep -ci 'underperformance' app/prompts/endgame_insights.md` is 0 OR appears ONLY inside the forbidden-words list within the entry_expected_score glossary entry. Verify by manual inspection that no narration guidance uses the word
    - `grep -c 'reports/benchmarks-2026-05-11.md' app/prompts/endgame_insights.md` returns >= 1 (cohort band citation)
    - The extended subsection block includes both worked example narrations from D-18
  </acceptance_criteria>
  <done>
    Prompt version is bumped (cache invalidates automatically); glossary and subsection block teach the LLM how to narrate the third finding without the forbidden framing.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/services/test_insights_service.py -k endgame_start_vs_end -x` passes
- `uv run ty check app/services/insights_service.py app/services/insights_llm.py tests/services/test_insights_service.py` zero errors
- `uv run ruff check app/services/insights_service.py app/services/insights_llm.py tests/services/test_insights_service.py` clean
- `grep -c '"endgame_v25"' app/services/insights_llm.py` returns 1
- The three-finding emission is exercised end to end (Task 1 tests)
</verification>

<success_criteria>
- The LLM receives a third SubsectionFinding for entry_expected_score and has glossary + subsection guidance to narrate it
- The cache invalidates automatically via the _PROMPT_VERSION bump (no manual flush)
- The "underperformance" framing is rejected at the prompt asset level, not just in code review
- The headline-ordering logic (gap-dominant vs entry_eval-dominant) is documented in the prompt so a fresh LLM call respects it
</success_criteria>

<output>
After completion, create `.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-05-SUMMARY.md` documenting: the new MetricId Literal slot, the emitter shape, the prompt version bump, and the glossary entry summary.
</output>
