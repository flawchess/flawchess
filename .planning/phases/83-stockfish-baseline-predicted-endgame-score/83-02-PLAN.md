---
phase: 83
plan: 02
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - app/services/score_confidence.py
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - tests/services/test_score_confidence.py
  - tests/services/test_endgame_service.py
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-21, D-22]
must_haves:
  truths:
    - "EndgamePerformanceResponse exposes entry_expected_score, entry_expected_score_n, entry_expected_score_p_value, entry_expected_score_ci_low, entry_expected_score_ci_high"
    - "Mate games are INCLUDED in the entry_expected_score cohort (D-06) — entry_expected_score_n counts them"
    - "|eval_cp| >= 2000 rows are dropped from the cohort (D-07)"
    - "entry_expected_score_p_value is None when entry_expected_score_n < 10 (gating consistent with entry_eval_p_value)"
    - "Wilson math lives in a single helper _wilson_score_test_vs_half in score_confidence.py — no duplicated math path"
    - "entry_eval_n unchanged by this plan (mate still excluded from entry_eval; cohort divergence per D-06 is locked)"
  artifacts:
    - path: "app/services/score_confidence.py"
      provides: "Refactored Wilson math: _wilson_score_test_vs_half + compute_score_confidence_from_mean siblings; compute_confidence_bucket public signature preserved"
      contains: "_wilson_score_test_vs_half"
    - path: "app/services/endgame_service.py"
      provides: "New ex_sum / ex_n aggregator block + 5 new fields appended to EndgamePerformanceResponse constructor"
      contains: "entry_expected_score"
    - path: "app/schemas/endgames.py"
      provides: "5 new entry_expected_score* fields with safe-empty defaults"
      contains: "entry_expected_score_p_value"
    - path: "tests/services/test_endgame_service.py"
      provides: "≥7 new entry_expected_score test cases including mate-INCLUDED inversion"
      contains: "test_entry_expected_score_mate_INCLUDED"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/services/eval_utils.py"
      via: "from app.services.eval_utils import eval_cp_to_expected_score, eval_mate_to_expected_score"
      pattern: "from app\\.services\\.eval_utils import"
    - from: "app/services/endgame_service.py"
      to: "app/services/score_confidence.py"
      via: "compute_score_confidence_from_mean for Wilson sig test vs 50% on the new mean"
      pattern: "compute_score_confidence_from_mean"
    - from: "app/services/endgame_service.py"
      to: "app/repositories/endgame_repository.py"
      via: "bucket_rows cursor already returns (eval_cp, eval_mate, user_color) — no SQL change"
      pattern: "bucket_rows"
---

<objective>
Wire a per-user `entry_expected_score` metric into the existing `EndgamePerformanceResponse`: per endgame game in the cohort, compute one expected score (sigmoid for `eval_cp`, 0/1 for `eval_mate`), aggregate per user, run a Wilson sig test vs 50%, ship 5 new schema fields. Factor the Wilson math out of `compute_confidence_bucket` so the new aggregator and the existing bucket function share a single Wilson code path.

Purpose: The aggregator block lives next to the existing entry-eval aggregator (`endgame_service.py:1670-1712`) and reuses the same `bucket_rows` cursor. The SQL is unchanged (PATTERNS.md confirms `bucket_stmt` already SELECTs `eval_cp` + `eval_mate` + `user_color`). The Wilson refactor honors memory `feedback_wilson_chess_score.md` (single Wilson path, no parallel methodology).

Output: 5 wire-format fields on `EndgamePerformanceResponse`, plus a refactored `score_confidence.py` whose existing `compute_confidence_bucket` behavior is byte-for-byte preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-01-SUMMARY.md
@.planning/phases/82-llm-prompt-awareness-of-endgame-start-vs-end-metrics/82-CONTEXT.md

# Source-of-truth analogs
@app/services/endgame_service.py
@app/services/score_confidence.py
@app/schemas/endgames.py
@app/repositories/endgame_repository.py
@tests/services/test_endgame_service.py
@tests/services/test_score_confidence.py
@app/services/eval_utils.py

<interfaces>
<!-- New schema field block (mirrors existing entry_eval_* pattern at endgames.py:124-140): -->

```python
# Phase 83 (D-21): Stockfish-baseline achievable score for "Where you start" tile.
entry_expected_score: float = 0.0
entry_expected_score_n: int = 0
entry_expected_score_p_value: float | None = None
entry_expected_score_ci_low: float | None = None
entry_expected_score_ci_high: float | None = None
```

<!-- Refactored score_confidence.py contract: -->

```python
# Pure helper — single source of Wilson math; called by both compute_confidence_bucket and the new sibling.
def _wilson_score_test_vs_half(score: float, n: int) -> tuple[float, float]: ...
    # returns (p_value, se_null); caller gates on n > 0

def compute_score_confidence_from_mean(
    score: float, n: int,
) -> tuple[Literal["low", "medium", "high"], float, float]: ...
    # bucketing identical to compute_confidence_bucket but on a float mean

def compute_confidence_bucket(w: int, d: int, losses: int, n: int) -> ...:
    # Public signature unchanged; internals now delegate Wilson math to _wilson_score_test_vs_half
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor score_confidence.py Wilson math into a shared helper</name>
  <files>app/services/score_confidence.py, tests/services/test_score_confidence.py</files>
  <read_first>
    - app/services/score_confidence.py (full file, focus on lines 141-143 — the Wilson math to factor out)
    - tests/services/test_score_confidence.py (existing fixture and test class shape, especially `test_n_below_gate_returns_low_even_with_strong_evidence` at ~lines 32-46)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "app/services/score_confidence.py (MODIFY — factor Wilson math)")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md ("Critical methodology note" + Pitfall 2)
  </read_first>
  <behavior>
    - `_wilson_score_test_vs_half(score, n)` returns `(p_value, se_null)` for the two-sided Wilson score test of H0: score == 0.5; same math as the existing compute_confidence_bucket inner block
    - `compute_score_confidence_from_mean(score, n)` returns the same `(level, p_value, _se)` triple as `compute_confidence_bucket(w, d, losses, n)` for equivalent inputs (i.e. when `(w + 0.5 * d) / n == score`)
    - `compute_score_confidence_from_mean(0.5, 100)` returns `("low", ≈1.0, _)` (centered, not significant)
    - `compute_score_confidence_from_mean(0.7, 200)` returns `("high", p < 0.05, _)`
    - `compute_score_confidence_from_mean(score, 0)` returns `("low", 1.0, 0.0)` — n <= 0 guard
    - `compute_score_confidence_from_mean(score, 5)` returns `("low", p_value, 0.0)` when n < CONFIDENCE_MIN_N (sample-size gate)
    - **Backward compat:** existing `compute_confidence_bucket(w, d, losses, n)` returns the same `(level, p_value, _se)` for the same inputs as before the refactor — assert against a small fixture (e.g. w=70, d=10, losses=20, n=100)
  </behavior>
  <action>
    Add `_wilson_score_test_vs_half(score: float, n: int) -> tuple[float, float]` private helper to `app/services/score_confidence.py` that contains the existing Wilson math (lines 141-143). Add public `compute_score_confidence_from_mean(score: float, n: int) -> tuple[Literal["low", "medium", "high"], float, float]` that delegates to `_wilson_score_test_vs_half` and applies the same gating constants (`CONFIDENCE_MIN_N`, `CONFIDENCE_HIGH_MAX_P`, `CONFIDENCE_MEDIUM_MAX_P`). Refactor existing `compute_confidence_bucket(w, d, losses, n)` to compute `score = (w + 0.5 * d) / n` and delegate to `_wilson_score_test_vs_half`, preserving its public signature and return shape exactly. Update module docstring to note the new sibling. Add a new test class `TestComputeScoreConfidenceFromMean` to `tests/services/test_score_confidence.py` covering the bulleted behaviors above. Add at least one regression test that asserts `compute_confidence_bucket(70, 10, 20, 100)` is unchanged vs the pre-refactor output (use a hard-coded golden tuple).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_score_confidence.py -x && uv run ty check app/services/score_confidence.py tests/services/test_score_confidence.py</automated>
  </verify>
  <acceptance_criteria>
    - All existing tests in `test_score_confidence.py` still pass (backward-compat regression)
    - New `TestComputeScoreConfidenceFromMean` class has ≥4 test functions covering the behaviors listed
    - `grep -c "math.sqrt(SCORE_PIVOT \* (1.0 - SCORE_PIVOT) / n)" app/services/score_confidence.py | grep -v '^#'` returns 1 (Wilson math appears exactly once — single source of truth)
    - `compute_score_confidence_from_mean` and `compute_confidence_bucket` both return `("low", ≈1.0, _)` for score == 0.5 inputs
    - ty-clean: zero errors
  </acceptance_criteria>
  <done>
    Wilson math is factored into a single helper; both compute_confidence_bucket and compute_score_confidence_from_mean delegate to it; all existing tests pass plus new sibling tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add entry_expected_score fields to EndgamePerformanceResponse schema</name>
  <files>app/schemas/endgames.py</files>
  <read_first>
    - app/schemas/endgames.py (lines 107-140 — existing entry_eval_* block to mirror exactly)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "app/schemas/endgames.py (~107-140)")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-21)
  </read_first>
  <behavior>
    - `EndgamePerformanceResponse(...)` can be instantiated WITHOUT specifying any entry_expected_score* field (defaults work)
    - Default values: `entry_expected_score = 0.0`, `entry_expected_score_n = 0`, all three p/ci fields default to `None`
    - All 5 fields are typed: `float`, `int`, `float | None`, `float | None`, `float | None`
    - Docstrings are descriptive ("Two-sided p-value vs 50%"), NOT prescriptive — no "Wilson test, refactored to accept float means…" methodology editorial (memory `feedback_wilson_chess_score.md`)
  </behavior>
  <action>
    Append 5 new fields to `EndgamePerformanceResponse` mirroring the existing `entry_eval_*` block (lines 124-140): `entry_expected_score: float = 0.0`, `entry_expected_score_n: int = 0`, `entry_expected_score_p_value: float | None = None`, `entry_expected_score_ci_low: float | None = None`, `entry_expected_score_ci_high: float | None = None`. Each field gets a one-line docstring (use PATTERNS.md examples verbatim — they are deliberately descriptive). Add a section comment `# Phase 83 (D-21): Stockfish-baseline achievable score for "Where you start" tile. Defaults match the Phase 81 D-11 safe-empty pattern so existing call sites keep working.` above the new block. Do NOT modify any existing field. Do NOT mention "Wilson" or methodology in docstrings.
  </action>
  <verify>
    <automated>uv run python -c "from app.schemas.endgames import EndgamePerformanceResponse; r = EndgamePerformanceResponse(); assert r.entry_expected_score == 0.0 and r.entry_expected_score_n == 0 and r.entry_expected_score_p_value is None and r.entry_expected_score_ci_low is None and r.entry_expected_score_ci_high is None; print('schema-ok')" && uv run ty check app/schemas/endgames.py</automated>
  </verify>
  <acceptance_criteria>
    - `EndgamePerformanceResponse()` instantiates with no args and exposes all 5 new fields at their default values
    - `grep -E "^\s+entry_expected_score(_n|_p_value|_ci_low|_ci_high)?:" app/schemas/endgames.py | wc -l` returns 5
    - No occurrence of "Wilson" inside the new docstring block (`grep -A 12 "Phase 83 (D-21)" app/schemas/endgames.py | grep -c -i 'wilson'` == 0)
    - ty-clean
  </acceptance_criteria>
  <done>
    Schema exports the 5 new fields with safe-empty defaults; smoke instantiation succeeds; ty passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aggregator block + tests for entry_expected_score in endgame_service</name>
  <files>app/services/endgame_service.py, tests/services/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py (lines 1670-1732 — existing entry-eval aggregator + EndgamePerformanceResponse constructor)
    - app/services/endgame_service.py (lines 160-210 — `EVAL_ADVANTAGE_THRESHOLD` constant location + `_classify_endgame_bucket` sign-flip)
    - app/services/eval_utils.py (Plan 1 output — the helper module to call)
    - app/services/score_confidence.py (Task 1 output — `compute_score_confidence_from_mean`)
    - tests/services/test_endgame_service.py (lines 3850-3945 — existing entry-eval test class with `self._bucket(...)` fixture pattern)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (sections "app/services/endgame_service.py (MODIFY…)" + "tests/services/test_endgame_service.py")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-04, D-05, D-06, D-07, D-22)
  </read_first>
  <behavior>
    - Empty bucket_rows: `entry_expected_score == 0.0`, `entry_expected_score_n == 0`, `entry_expected_score_p_value is None`, both CI bounds `None`
    - 10 rows with `eval_cp=0` (centered): `entry_expected_score ≈ 0.5`, `entry_expected_score_p_value ≈ 1.0` (not significant), `entry_expected_score_n == 10`
    - 10 rows with `eval_cp=+200`, `user_color="black"` (sign-flip): `entry_expected_score < 0.5` (specifically ≈ 0.32 via sigmoid)
    - 9 rows (below n=10 gate): `entry_expected_score` computed, but `entry_expected_score_p_value is None`
    - **Mate INCLUDED (D-06 inversion of entry_eval):** 10 cp rows + 1 mate row (eval_mate=+5, user_color="white") yields `entry_expected_score_n == 11`, and the mate row contributes 1.0 to the sum
    - Mate-against-user (eval_mate=-5, user_color="white") contributes 0.0 to the sum
    - `|eval_cp| >= 2000` rows are DROPPED (D-07 clip): 9 normal rows + 1 row with eval_cp=2500 yields `entry_expected_score_n == 9`
    - NULL eval rows (both `eval_cp is None` and `eval_mate is None`) are dropped
    - CI bounds: when `entry_expected_score_n >= 2`, both `_ci_low` and `_ci_high` are floats in [0, 1]; when `n < 2`, both are `None`
    - The existing `entry_eval_n` value is UNCHANGED by this plan (mate still excluded from entry_eval per Phase 81 cohort)
  </behavior>
  <action>
    In `app/services/endgame_service.py`, immediately after the existing entry-eval aggregator block (around line 1712), add a sibling aggregator block per D-04..D-07 using the loop pattern from PATTERNS.md §"app/services/endgame_service.py (MODIFY…)". Extract `EVAL_CLIP_MAX_CP = 2000` as a module-level constant next to the existing `EVAL_ADVANTAGE_THRESHOLD = 100` (line 166) — no magic number in the loop body (CLAUDE.md "no magic numbers"). Loop pattern:
    - For each `row` in `bucket_rows`:
      - If `row.eval_mate is not None` → call `eval_mate_to_expected_score(row.eval_mate, row.user_color)`, add to `ex_sum`, increment `ex_n` (mate INCLUDED per D-06)
      - Else if `row.eval_cp is not None`:
        - If `abs(row.eval_cp) >= EVAL_CLIP_MAX_CP` → continue (D-07 clip)
        - Else call `eval_cp_to_expected_score(row.eval_cp, row.user_color)`, add to `ex_sum`, increment `ex_n`
      - Else (both NULL) → skip
    Tag every `row.<col>` access with `# ty: ignore[unresolved-attribute]` per the existing pattern at lines 1683-1690. Compute `entry_expected_score = ex_sum / ex_n if ex_n > 0 else 0.0`. Call `_, p_ex_raw, _ = compute_score_confidence_from_mean(entry_expected_score, ex_n)` (note: the helper guards `n <= 0` internally). Gate the wire-format p_value: `entry_expected_score_p_value = p_ex_raw if ex_n >= 10 else None`. Compute Wilson CIs via the existing CI utility on the (score, n) signature; gate to `None` when `ex_n < 2`. (Inspect the existing `endgame_score_ci_low` derivation path nearby to identify the correct CI util — reuse it; do not introduce new statistical machinery, per memory `feedback_wilson_chess_score.md`.) Append the 5 new fields to the `EndgamePerformanceResponse(...)` constructor call at lines 1722-1732 in the same field order as on the schema.

    Add a new test class `TestEntryExpectedScore` (or sibling cases inside the existing entry-eval class) to `tests/services/test_endgame_service.py` covering every behavior bullet above. Reuse the existing `self._bucket(...)` fixture (extend its kwargs to support `eval_mate` if not already supported — inspect lines 3854-3865 first; the existing fixture likely takes `eval_cp=None, eval_mate=None` already since the row shape matches). Use the existing `_get_endgame_performance_from_rows` test entry point. Each test asserts on the new `resp.entry_expected_score*` fields directly.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service.py -k 'expected_score or entry_eval' -x && uv run ty check app/services/endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - All new `test_entry_expected_score_*` tests pass (GREEN)
    - All existing `test_entry_eval_*` tests STILL pass (no regression — Phase 81 cohort unchanged)
    - `grep -c "EVAL_CLIP_MAX_CP" app/services/endgame_service.py | grep -v '^#'` returns >= 2 (defined once at module level, used in the loop)
    - `grep -c "eval_cp_to_expected_score\|eval_mate_to_expected_score" app/services/endgame_service.py | grep -v '^#'` returns >= 2
    - The new aggregator loop tags each `row.<col>` access with `# ty: ignore[unresolved-attribute]`
    - `EndgamePerformanceResponse(...)` constructor at the end of the function passes all 5 new fields (grep for `entry_expected_score=` in the constructor call returns >= 1)
    - Test: `test_entry_expected_score_mate_INCLUDED` exists and asserts `resp.entry_expected_score_n` strictly greater than the matching `resp.entry_eval_n` (mate row is in the expected-score cohort but not the entry-eval cohort)
    - ty-clean on the modified file
  </acceptance_criteria>
  <done>
    `EndgamePerformanceResponse` now ships with populated `entry_expected_score*` fields end-to-end through the existing `bucket_rows` cursor; all targeted pytest cases pass; ty-clean.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/services/test_score_confidence.py tests/services/test_endgame_service.py -x` passes
- `uv run ty check app/services/score_confidence.py app/services/endgame_service.py app/schemas/endgames.py tests/services/test_score_confidence.py tests/services/test_endgame_service.py` zero errors
- `uv run ruff check` clean on all modified files
- A smoke instantiation `EndgamePerformanceResponse()` produces safe defaults
- The existing `entry_eval_n` cohort (mate excluded) is preserved byte-for-byte by the existing tests
- Wilson math appears exactly once in `score_confidence.py` (single source of truth for the methodology — memory `feedback_wilson_chess_score.md`)
</verification>

<success_criteria>
- Plan 3 can consume `data.entry_expected_score`, `data.entry_expected_score_n`, `data.entry_expected_score_p_value`, `data.entry_expected_score_ci_low`, `data.entry_expected_score_ci_high` from the API response
- Plan 4's benchmark calibration loads against the same population definition used here (mate INCLUDED, `|eval_cp| < 2000`, sign-flipped)
- Plan 5's `_findings_endgame_start_vs_end` can read `perf.entry_expected_score_n` and `perf.entry_expected_score`
- No paired sig test is shipped (D-22): two independent Wilson CIs anchor the visual juxtaposition
</success_criteria>

<output>
After completion, create `.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-02-SUMMARY.md` listing: schema field names, the Wilson refactor public/private surface, the aggregator constants (`EVAL_CLIP_MAX_CP`), and the test functions added.
</output>
