---
phase: 83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/eval_utils.py
  - tests/services/test_eval_utils.py
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "eval_cp_to_expected_score(0, 'white') == 0.5"
    - "eval_cp_to_expected_score(+100, 'white') ~ 0.591 and eval_cp_to_expected_score(+100, 'black') ~ 0.409"
    - "eval_mate_to_expected_score(+5, 'white') == 1.0 and eval_mate_to_expected_score(+5, 'black') == 0.0"
    - "eval_mate_to_expected_score(-5, 'white') == 0.0 and eval_mate_to_expected_score(-5, 'black') == 1.0"
    - "LICHESS_K is defined as a named module constant, not a magic literal inside a function"
  artifacts:
    - path: "app/services/eval_utils.py"
      provides: "LICHESS_K constant + eval_cp_to_expected_score + eval_mate_to_expected_score"
      contains: "LICHESS_K"
    - path: "tests/services/test_eval_utils.py"
      provides: "Unit tests covering sigmoid centering, sign convention, saturation, mate-for/against both colors"
      contains: "test_white_black_symmetry"
  key_links:
    - from: "tests/services/test_eval_utils.py"
      to: "app/services/eval_utils.py"
      via: "from app.services.eval_utils import LICHESS_K, eval_cp_to_expected_score, eval_mate_to_expected_score"
      pattern: "from app\\.services\\.eval_utils import"
---

<objective>
Add a pure-math utility module that converts Stockfish eval (signed centipawns or mate score) to a user-perspective expected score in [0, 1] via the Lichess winning-chances sigmoid (cp) and direct 0/1 mapping (mate). Ship with thorough unit tests covering sign convention, centering, saturation, and mate-for/against both colors.

Purpose: Plan 2's aggregator and Plan 4's benchmark CTE depend on this primitive. Pure module, no I/O, no DB — unit-testable in isolation. Phase 82 was bitten by sign-convention bugs without these tests; mate-handling tests for both colors are non-negotiable (RESEARCH Pitfall 1).

Output: `app/services/eval_utils.py` (LICHESS_K + two functions) and `tests/services/test_eval_utils.py` (≥8 unit tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-RESEARCH.md
@.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md

# Source-of-truth analogs
@app/services/endgame_service.py
@app/services/eval_confidence.py
@tests/services/test_eval_confidence.py

<interfaces>
<!-- Target module exports — these are the contracts downstream plans rely on. -->

From the NEW app/services/eval_utils.py (to be written):

```python
LICHESS_K: float  # = 0.00368208

def eval_cp_to_expected_score(
    eval_cp: int,
    user_color: Literal["white", "black"],
) -> float: ...

def eval_mate_to_expected_score(
    eval_mate: int,
    user_color: Literal["white", "black"],
) -> float: ...
```

Sign-flip pattern to mirror (from app/services/endgame_service.py:195-204):

```python
sign = 1 if user_color == "white" else -1
user_eval_cp = sign * eval_cp
```

A positive `eval_cp` is white-favoring (python-chess / Stockfish convention). A user-perspective sign-flip is applied for black users so `eval_cp_to_expected_score(+100, "black")` returns ~0.409.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for eval_utils</name>
  <files>tests/services/test_eval_utils.py</files>
  <read_first>
    - tests/services/test_eval_confidence.py — class-grouped pytest pattern, `pytest.approx` usage, imports scaffolding
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "tests/services/test_eval_utils.py (NEW…)")
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-CONTEXT.md (D-02, D-03)
  </read_first>
  <behavior>
    - LICHESS_K is exported and equals 0.00368208 exactly
    - eval_cp_to_expected_score(0, "white") == 0.5 (centering, pytest.approx abs=1e-9)
    - eval_cp_to_expected_score(+100, "white") ~ 0.591 (sign convention, white-user)
    - eval_cp_to_expected_score(+100, "black") ~ 0.409 (sign convention, black-user)
    - White/black symmetry: f(+x, "white") + f(+x, "black") == 1.0 for any cp
    - Saturation high: eval_cp_to_expected_score(+1500, "white") > 0.99
    - Saturation low: eval_cp_to_expected_score(-1500, "white") < 0.01
    - eval_mate_to_expected_score(+5, "white") == 1.0 (white-user, white mating)
    - eval_mate_to_expected_score(+5, "black") == 0.0 (black-user, white mating)
    - eval_mate_to_expected_score(-5, "white") == 0.0 (white-user, black mating)
    - eval_mate_to_expected_score(-5, "black") == 1.0 (black-user, black mating) — Pitfall 1 coverage
  </behavior>
  <action>
    Create `tests/services/test_eval_utils.py` per D-03 and RESEARCH §"Code Examples — Unit test scaffolding". Group cases into two classes (`TestSigmoid` for cp conversions, `TestMate` for mate conversions). Import directly from `app.services.eval_utils`. Use `pytest.approx(..., abs=1e-9)` for float assertions, exact `==` for mate boundary cases. Module docstring should cite SEED-014 / Phase 83 D-02. Tests MUST fail on first run (the module doesn't exist yet) — that's the RED phase. Do NOT create `app/services/eval_utils.py` in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_utils.py 2>&1 | tail -20  # expect ImportError / module-not-found failures, NOT collection errors</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/services/test_eval_utils.py` exists with ≥10 distinct test functions across `TestSigmoid` and `TestMate` classes
    - All listed behavior cases above have a corresponding test
    - `pytest tests/services/test_eval_utils.py` fails with import error referencing `app.services.eval_utils` (RED phase confirmed)
    - No `# type: ignore` / `# ty: ignore` pragmas needed — pure-function tests
  </acceptance_criteria>
  <done>
    Test file committed; running it fails with `ModuleNotFoundError: app.services.eval_utils` or equivalent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement eval_utils to make tests pass</name>
  <files>app/services/eval_utils.py</files>
  <read_first>
    - app/services/endgame_service.py (lines 170-204 — `_classify_endgame_bucket` sign-flip canonical pattern)
    - app/services/eval_confidence.py (module shape: docstring style, imports, top-level constants, typed pure functions)
    - .planning/phases/83-stockfish-baseline-predicted-endgame-score/83-PATTERNS.md (section "app/services/eval_utils.py (NEW…)")
    - tests/services/test_eval_utils.py (just-written, defines the contract)
  </read_first>
  <action>
    Create `app/services/eval_utils.py` per D-02. Module docstring should cite the Lichess accuracy / winning-chances source for the sigmoid constant. Top of file: `import math` + `from typing import Literal`. Define `LICHESS_K: float = 0.00368208` as a named module-level constant (CLAUDE.md "no magic numbers"). Implement `eval_cp_to_expected_score(eval_cp: int, user_color: Literal["white", "black"]) -> float` returning `1.0 / (1.0 + math.exp(-LICHESS_K * sign * eval_cp))` where `sign = 1 if user_color == "white" else -1`. Implement `eval_mate_to_expected_score(eval_mate: int, user_color: Literal["white", "black"]) -> float` returning `1.0` iff `(eval_mate > 0 and user_color == "white") or (eval_mate < 0 and user_color == "black")`, else `0.0`. Mate is NOT routed through the sigmoid (D-02). No I/O, no DB, stdlib only. Explicit return-type annotations on every function (CLAUDE.md ty-compliance). Now run the tests — they MUST all pass (GREEN phase).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_utils.py -x && uv run ty check app/services/eval_utils.py tests/services/test_eval_utils.py && uv run ruff check app/services/eval_utils.py tests/services/test_eval_utils.py</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `tests/services/test_eval_utils.py` pass (GREEN)
    - `uv run ty check app/services/eval_utils.py tests/services/test_eval_utils.py` returns zero errors
    - `uv run ruff check` clean on both files
    - `grep -c "^LICHESS_K" app/services/eval_utils.py | grep -v '^#'` returns >= 1 (named module constant, not a literal inside a function)
    - Module body contains `Literal["white", "black"]` (not bare `str`) for both `user_color` parameters
    - No magic number `0.00368208` inside any function body — only `LICHESS_K` referenced
  </acceptance_criteria>
  <done>
    `app/services/eval_utils.py` exists; `pytest tests/services/test_eval_utils.py` is green; ty and ruff are clean.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/services/test_eval_utils.py -x` passes
- `uv run ty check app/services/eval_utils.py tests/services/test_eval_utils.py` zero errors
- `uv run ruff check app/services/eval_utils.py tests/services/test_eval_utils.py` clean
- Module exports `LICHESS_K`, `eval_cp_to_expected_score`, `eval_mate_to_expected_score`
- No magic literal `0.00368208` outside `LICHESS_K` definition
</verification>

<success_criteria>
- Plan 2's aggregator can `from app.services.eval_utils import eval_cp_to_expected_score, eval_mate_to_expected_score` without modification
- The sign convention matches `_classify_endgame_bucket` exactly (verified by symmetry tests)
- Mate-for-user maps to 1.0 and mate-against-user maps to 0.0 for both white-user and black-user (Pitfall 1)
</success_criteria>

<output>
After completion, create `.planning/phases/83-stockfish-baseline-predicted-endgame-score/83-01-SUMMARY.md` documenting: module signature, test coverage list, any deviations from PATTERNS.md.
</output>
