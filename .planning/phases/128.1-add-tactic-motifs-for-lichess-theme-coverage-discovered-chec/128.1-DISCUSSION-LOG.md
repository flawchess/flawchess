# Phase 128.1: Add tactic motifs for lichess-theme coverage - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec
**Areas discussed:** Promotion product call, Dispatch priority placement, trapped-piece scope, Chip-surfacing intent

---

## Promotion / under-promotion handling

| Option | Description | Selected |
|--------|-------------|----------|
| Two motifs, move-type family | Detect + store both promotion and under-promotion (under-promotion dominates); group with en-passant as a move-type family excluded from default chips | ✓ |
| One combined promotion motif | Single `promotion` motif, no under-promotion split | |
| Skip promotion entirely | Ship only discovered-check, trapped-piece, en-passant this phase | |
| Normal chips, no grouping | Treat promotion like any motif, default chip | |

**User's choice:** Two motifs, move-type family (D-01/D-02)
**Notes:** lichess splits `promotion`/`underPromotion`; under-promotion is the more tactical signal and dominates promotion like a named-mate subtype. Move-type family captures data + harness parity without polluting the chip row.

---

## Dispatch priority placement (single-tag dispatch)

| Option | Description | Selected |
|--------|-------------|----------|
| Real tactics high, move-type lowest | discovered-check tier 2 above discovered-attack; trapped-piece tier 2 above hanging-piece; move-type new lowest tier | (posture adopted) |
| Move-type can win | Rank move-type above real tactics | |
| Let planner decide intra-tier | Lock tier assignment only; intra-tier rank to planner, harness-baselined (provisional D-08 posture) | ✓ |

**User's choice:** Let planner decide intra-tier (D-03/D-04/D-05)
**Notes:** The "real tactic wins, move-type lowest" tier posture is adopted; only the exact intra-tier rank is left to the planner. Surfaced + accepted: move-type tags will rarely populate under single-tag dispatch (shadowed by real tactics) — this is correct, not a bug.

---

## trapped-piece scope

| Option | Description | Selected |
|--------|-------------|----------|
| Precision-first, strict material gate | Piece under attack AND every escape loses material; floor from TRAIN harness; never lower a floor | ✓ |
| Recall-leaning | Fire on trappable-but-not-yet-attacked pieces, accept more false positives | |

**User's choice:** Precision-first, strict material gate (D-06/D-07/D-08)
**Notes:** Mirrors Phase 127's governing value — a wrong visible tag erodes trust more than a missing one.

---

## Chip-surfacing intent (Phase 129 forward-looking)

| Option | Description | Selected |
|--------|-------------|----------|
| Split: real tactics are chips, move-type isn't | discovered-check + trapped-piece are first-class chips; en-passant/promotion/under-promotion detection-only / separate | ✓ |
| All 4 are eventual chips | Every new motif becomes a user-facing chip | |
| All detection-only for now | None surface as chips in 129 | |

**User's choice:** Split (D-09)
**Notes:** Directional intent executed in Phase 129; 128.1 only provides the family grouping so 129 can apply it. No chip code in this phase.

---

## Claude's Discretion

- Exact intra-tier rank of new motifs within assigned tiers (D-05).
- discovered-check as a new standalone detector vs a refactor lifting Sub-case 1 out of `detect_discovered_attack`.
- Move-type "lowest tier" as a new candidate-pool tier vs post-candidate fallbacks.
- IntEnum integer assignment (append 25-29).
- Location of the move-type family set constant (alongside `MATE_MOTIFS`).
- trapped-piece detector internals, subject to the D-06 gate and D-08 floor.

## Deferred Ideas

- Chip controls / toggle for move-type tags + default-chip membership — Phase 129.
- More named-mate patterns — BLOCKED on SEED-057 (suppressed arabian/boden/dovetail).
- Non-motif metadata + fuzzy/positional themes — out of the deterministic-motif model.
