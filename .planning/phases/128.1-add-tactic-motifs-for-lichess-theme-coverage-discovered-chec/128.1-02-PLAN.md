---
phase: 128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec
plan: 02
type: execute
wave: 2
depends_on:
  - 128.1-01
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/motif_theme_map.py
  - tests/scripts/tagger/precision_floors.py
  - tests/services/test_tactic_detector.py
autonomous: true
requirements: []   # No formal REQ-IDs (seed-derived). Drive from D-01..D-09 in CONTEXT.md.
must_haves:
  truths:
    - "detect_tactic_motif emits 'en-passant' when the refuting move is an en-passant capture and no real tactic fired"
    - "detect_tactic_motif emits 'promotion' when the refuting move promotes a pawn to a queen and no real tactic fired"
    - "detect_tactic_motif emits 'under-promotion' when the refuting move promotes a pawn to a NON-queen and no real tactic fired (under-promotion dominates promotion, D-01)"
    - "A promotion to a non-queen NEVER tags as 'promotion' — it tags as 'under-promotion' (D-01 dominance)"
    - "Move-type motifs sit at a NEW lowest tier BELOW hanging-piece — any real tactic (fork-via-promotion, en-passant-fork) wins; move-type only fires on the residual (D-03/D-04)"
    - "en-passant / promotion / under-promotion belong to a MOVE_TYPE_MOTIFS frozenset mirroring the MATE_MOTIFS pattern (D-02)"
    - "All five new motifs have a precision floor (or documented no-regression floor for underPromotion) baselined from one fresh TRAIN harness run; no existing floor is lowered (D-08)"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "EN_PASSANT=27, PROMOTION=28, UNDER_PROMOTION=29 IntEnum members; their Literals + _INT_TO_MOTIF entries; MOVE_TYPE_MOTIFS frozenset; move-type detectors; new lowest-tier dispatch below hanging-piece"
      contains: "MOVE_TYPE_MOTIFS"
    - path: "tests/scripts/tagger/motif_theme_map.py"
      provides: "'en-passant'->('enPassant',), 'promotion'->('promotion',), 'under-promotion'->('underPromotion',) entries"
      contains: "underPromotion"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "PRECISION_FLOOR entries for discovered-check, trapped-piece, en-passant, promotion (and a documented underPromotion treatment per D-08), all from one fresh TRAIN run"
      contains: "discovered-check"
  key_links:
    - from: "app/services/tactic_detector.py detect_tactic_motif"
      to: "new MOVE_TYPE tier (below Tier-4 hanging-piece)"
      via: "move-type detectors only added to candidates after hanging-piece, with the highest tier number so they lose every tie to a real tactic"
      pattern: "MOVE_TYPE|EN_PASSANT|UNDER_PROMOTION"
    - from: "tests/scripts/tagger/test_detector_precision.py"
      to: "tests/scripts/tagger/precision_floors.py PRECISION_FLOOR"
      via: "TRAIN floor gate asserts the freshly-baselined floors for all shipped new motifs"
      pattern: "discovered-check|trapped-piece|en-passant|promotion"
---

<objective>
Add the move-type family from SEED-058 — `en-passant`, `promotion`, `under-promotion` —
to `detect_tactic_motif` as a NEW lowest dispatch tier below hanging-piece (D-03/D-04:
real tactics always win, move-type fires only on the residual). Group the three in a
`MOVE_TYPE_MOTIFS` frozenset mirroring `MATE_MOTIFS` (D-02). Enforce D-01: under-promotion
dominates promotion. Then run ONE authoritative fresh TRAIN harness baseline and set the
`precision_floors.py` floors for ALL FIVE new motifs (the two from Plan 01 plus these
three), handling underPromotion's 4-label sparsity per the D-08 caveat without inventing a
pretend-statistical floor.

Purpose: close the `enPassant` / `promotion` / `underPromotion` theme gaps and finalize
the never-regress precision gate for the whole phase. Move-type tags are mechanically true
but low-value (D-02/D-09): they are detected + stored for lichess parity, never allowed to
pre-empt a real tactic.

Output: 3 new motifs (ints 27-29), `MOVE_TYPE_MOTIFS` frozenset, a new lowest dispatch
tier, theme-map entries, the full 5-motif precision-floor baseline, and updated guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec/128.1-CONTEXT.md
@.planning/phases/128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec/128.1-01-SUMMARY.md
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `TacticMotifInt.EN_PASSANT = 27` | `app/services/tactic_detector.py` | IntEnum member |
| `TacticMotifInt.PROMOTION = 28` | `app/services/tactic_detector.py` | IntEnum member |
| `TacticMotifInt.UNDER_PROMOTION = 29` | `app/services/tactic_detector.py` | IntEnum member |
| `"en-passant"` / `"promotion"` / `"under-promotion"` | `app/services/tactic_detector.py` | `TacticMotif` Literals + `_INT_TO_MOTIF[27/28/29]` |
| `MOVE_TYPE_MOTIFS` | `app/services/tactic_detector.py` | `frozenset[TacticMotif]` (mirrors `MATE_MOTIFS`) |
| `detect_en_passant(...)` | `app/services/tactic_detector.py` | detector fn, 3-tuple |
| `detect_promotion(...)` | `app/services/tactic_detector.py` | detector fn (returns promotion vs under-promotion per D-01), 3-tuple or motif-string shape |
| `_MOVE_TYPE_REGISTRY` (new lowest tier) | `app/services/tactic_detector.py` | dispatch registry below hanging-piece |
| `"en-passant"`/`"promotion"`/`"under-promotion"` theme keys | `tests/scripts/tagger/motif_theme_map.py` | `MOTIF_TO_THEMES` keys |
| `PRECISION_FLOOR` entries for all 5 new motifs | `tests/scripts/tagger/precision_floors.py` | floor constants (underPromotion per D-08 caveat) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add move-type trio (en-passant, promotion, under-promotion), MOVE_TYPE_MOTIFS frozenset, and new lowest dispatch tier</name>
  <files>app/services/tactic_detector.py, tests/scripts/tagger/motif_theme_map.py, tests/services/test_tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py:69-181 (IntEnum, Literal, _INT_TO_MOTIF, MATE_MOTIFS frozenset pattern at :167-181)
    - app/services/tactic_detector.py:1309-1500 (dispatcher: tier registries, Tier-4 hanging-piece block, _sort_key, candidate tuple shape)
    - app/services/tactic_detector.py:188-200 (_parse_pv — moves[0] is the refuting move; chess.Move.promotion / board.is_en_passant)
    - tests/scripts/tagger/motif_theme_map.py:44-74 (MOTIF_TO_THEMES)
    - tests/services/test_tactic_detector.py:1119-1174 (fixture-set partitions), :1243-1363 (encoding count + partition asserts), :1371-... (TestPriorityOrder dispatch tests)
  </read_first>
  <behavior>
    - Refuting move (moves[0]) is an en-passant capture AND no real tactic fired → motif_int 27 (en-passant).
    - Refuting move promotes a pawn to a QUEEN AND no real tactic fired → motif_int 28 (promotion).
    - Refuting move promotes a pawn to a NON-queen (knight/bishop/rook) AND no real tactic fired → motif_int 29 (under-promotion), NEVER 28 (D-01 dominance).
    - A promotion that is ALSO a fork (or any real tactic) tags as the real tactic, NOT as promotion/under-promotion (D-03/D-04: move-type is the lowest tier and loses every tie).
    - An en-passant capture that is ALSO a real tactic tags as the real tactic.
    - MOVE_TYPE_MOTIFS == frozenset({"en-passant", "promotion", "under-promotion"}); discovered-check and trapped-piece are NOT in it (D-02).
    - Encoding round-trips for 27/28/29.
  </behavior>
  <action>
    Append `EN_PASSANT = 27`, `PROMOTION = 28`, `UNDER_PROMOTION = 29` to `TacticMotifInt`
    (declaration order, preserving 1-26). Add `"en-passant"`, `"promotion"`, `"under-promotion"`
    to the `TacticMotif` Literal and `27/28/29` entries to `_INT_TO_MOTIF`.

    Add a `MOVE_TYPE_MOTIFS: frozenset[TacticMotif]` constant directly below `MATE_MOTIFS`
    (`tactic_detector.py:167-181`), mirroring that pattern exactly:
    `frozenset({"en-passant", "promotion", "under-promotion"})`, with a docstring comment noting
    these are the D-02 move-type family — detected + stored for lichess parity, excluded from the
    default chip set (the exclusion itself is wired in Phase 129, D-09).

    Implement the move-type detectors operating on `moves[0]` / `boards[0]` (the refuting move):
    - `detect_en_passant(boards, moves, pov) -> tuple[bool, int | None, int | None]`: fire when
      `boards[0].is_en_passant(moves[0])` is True. Return `(True, chess.PAWN, 0)`. Confidence is
      `TACTIC_CONFIDENCE_HIGH` (trivial, ~100% precision by construction).
    - `detect_promotion(...)`: inspect `moves[0].promotion`. If it equals `chess.QUEEN` →
      promotion; if it is a non-None non-queen piece type → under-promotion (D-01 dominance:
      under-promotion wins, so a single detector distinguishes by the promotion target). Decide the
      return shape: either two separate detector functions (`detect_promotion` returning queen-only,
      `detect_under_promotion` returning non-queen-only) registered as two registry entries with
      under-promotion ranked above promotion, OR one detector that returns the motif string (like
      `detect_boden_or_double_bishop_mate`). Prefer the two-detector form for registry uniformity;
      under-promotion MUST be ranked above promotion in the move-type registry so the dominance is
      enforced by dispatch order, matching the named-mate-subtype pattern. Return `(True, chess.PAWN, 0)`.

    Add a NEW LOWEST dispatch tier below the Tier-4 hanging-piece block in `detect_tactic_motif`
    (D-03): define `_MOVE_TYPE_REGISTRY: list[tuple[TacticMotif, int]]` (order:
    `under-promotion`, `promotion`, `en-passant` — under-promotion first to win the intra-tier tie,
    D-01) and `_MOVE_TYPE_DETECTOR_FNS`. Collect their firings into the SAME `candidates` list but
    with a tier number STRICTLY GREATER than Tier 4 (use `TIER_MOVE_TYPE = 5` as a named constant).
    Because `_sort_key` sorts by `(tier, rank, depth)` ascending and tier 5 > 4 > 3 > 2, any real
    tactic (tiers 2-4) ALWAYS beats a move-type firing (D-04 — real tactic wins, move-type lowest).
    Add a comment at the new tier explaining the D-03/D-04 "move-type is the residual" posture and
    the EXPECTED-not-a-bug sparse population (move-type only tags PVs where nothing else fired).
    Do NOT add a per-detector relevance gate — move-type detection is purely move-shape, no
    material gate.

    Add the three theme-map entries to `MOTIF_TO_THEMES`: `"en-passant": ("enPassant",)`,
    `"promotion": ("promotion",)`, `"under-promotion": ("underPromotion",)`.

    Update fast-guard tests in `tests/services/test_tactic_detector.py`:
    - Encoding-count asserts → `== 29` (final).
    - Add `_EN_PASSANT_FIXTURES`, `_PROMOTION_FIXTURES`, `_UNDER_PROMOTION_FIXTURES` positive sets.
      Note: en-passant (12) and under-promotion (4) have FEWER than the >=10 required by
      `test_validated_motifs_have_enough_fixtures`. Choose ONE: (a) place these three in a NEW
      `_MOVE_TYPE_FIXTURE_SETS` partition that is checked by `test_positives_fire_expected_motif`
      and the partition-completeness assert but EXEMPT from the >=10 richness rule (extend
      `test_validated_motifs_have_enough_fixtures` to skip move-type, with a comment citing the
      D-08 sparsity caveat), OR (b) hand-augment with curated FENs to reach 10. Prefer (a) — the
      D-08 caveat explicitly accepts thin move-type/under-promotion samples; do not fabricate
      synthetic richness. Whichever set you choose, `validated ∪ suppressed ∪ move_type` must equal
      `set(_INT_TO_MOTIF.values())` so the partition assert at :1359-1363 still holds — update that
      assert to include the move-type partition.
    - Add a `TestPriorityOrder` assertion: a promotion-that-is-also-a-fork fixture returns the real
      tactic (fork), NOT promotion; an under-promotion fixture returns "under-promotion" not "promotion".
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_tactic_detector.py -x -q && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `EN_PASSANT = 27`, `PROMOTION = 28`, `UNDER_PROMOTION = 29` in `TacticMotifInt`; `27/28/29` in `_INT_TO_MOTIF`.
    - SOURCE: `MOVE_TYPE_MOTIFS = frozenset({...})` exists directly below `MATE_MOTIFS` and contains exactly the three move-type motifs (discovered-check/trapped-piece NOT in it).
    - SOURCE: `_MOVE_TYPE_REGISTRY` lists `under-promotion` at a strictly lower index than `promotion`; the move-type tier number (`TIER_MOVE_TYPE = 5`) is strictly greater than the hanging-piece tier (4).
    - SOURCE: `MOTIF_TO_THEMES` contains the three new keys mapping to `enPassant`/`promotion`/`underPromotion`.
    - BEHAVIOR (D-01): an under-promotion fixture returns `"under-promotion"`, never `"promotion"`.
    - BEHAVIOR (D-04): a promotion-and-fork fixture returns the real tactic, not a move-type tag.
    - TEST: `uv run pytest -n auto tests/services/test_tactic_detector.py -x -q` green; `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>
    en-passant/promotion/under-promotion are move-type motifs (ints 27-29) at a new lowest tier
    below hanging-piece, grouped in MOVE_TYPE_MOTIFS, with under-promotion dominating promotion and
    real tactics always winning. ty-clean, fast-guard-tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Baseline all five new precision floors from one fresh TRAIN harness run (D-08); finalize floor gate</name>
  <files>tests/scripts/tagger/precision_floors.py, scripts/tactic_tagger_report.py, tests/services/test_tactic_detector.py</files>
  <read_first>
    - tests/scripts/tagger/precision_floors.py (PRECISION_FLOOR dict, SUPPRESSED_MOTIFS, the measurement-run docstring to extend)
    - tests/scripts/tagger/test_detector_precision.py:131-212 (how floors are asserted on TRAIN only; SUPPRESSED_MOTIFS bypass; recall printed not gated)
    - .planning/phases/128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec/128.1-CONTEXT.md (D-08 fixture-sparsity table — the per-theme floor posture is LOCKED there)
    - scripts/tactic_tagger_report.py:83-117 (GOALS table — aspirational, separate from floors; optionally add the new motifs)
  </read_first>
  <action>
    Run the harness against the TRAIN fixture to MEASURE the precision of all five new motifs with
    the full dispatcher (all 29 motifs present), e.g.
    `uv run pytest -n auto tests/scripts/tagger/test_detector_precision.py -s -q`
    (the `-s` prints the per-motif TRAIN table). Capture the measured TRAIN precision (and recall)
    for `discovered-check`, `trapped-piece`, `en-passant`, `promotion`, `under-promotion`.

    Set `PRECISION_FLOOR` entries per the D-08 LOCKED posture (CONTEXT.md sparsity table) — floors
    sit ~5-8pp below the measured TRAIN precision, rounded to 0.05, matching the existing convention:
    - `discovered-check` (1004 TRAIN labels): normal % floor from the measured value — robust.
    - `promotion` (~176 pure-queen TRAIN): normal % floor from the measured value — adequate.
    - `trapped-piece` (28 TRAIN): % floor OK for PRECISION (denominator = detector firings over all
      ~11.8k rows, NOT the 28 labels). Set the floor from measured precision; add a comment that
      recall is only weakly validated by 28 positives and must not be over-read.
    - `en-passant` (12 TRAIN): thin but ~100% precision by construction — set a floor near the
      measured value (e.g. 0.80-0.90 band consistent with double-check/interference precedent).
    - `under-promotion` (4 TRAIN / 4 TEST): DO NOT assign a pretend-statistical % floor. Pick and
      implement EXACTLY ONE of the three D-08-approved approaches and document the choice in the
      `precision_floors.py` docstring:
        (i) a no-regression / "must fire on the known 4" floor — add a dedicated assertion (in the
            harness or a fast-guard test) that the detector fires under-promotion on the 4 known
            TRAIN rows, and OMIT under-promotion from the percentage `PRECISION_FLOOR` (treat like
            a structurally-validated motif), OR
        (ii) hand-augment the fixture with curated under-promotion FENs to reach a defensible n
             before assigning a % floor, OR
        (iii) mark under-promotion as explicit low-confidence coverage (e.g. add to a documented
              low-confidence set, no % gate).
      RECOMMENDED: approach (i) — least invasive, honest about the 4-label reality, and it directly
      encodes the D-01 dominance ("the 4 underPromotion rows must tag as under-promotion"). Whatever
      you pick, the docstring MUST state the n=4 reality and why no statistical floor is used.

    If a measured new motif has 0 TP / >0 FP (only-FP) or never fires, add it to `SUPPRESSED_MOTIFS`
    instead of `PRECISION_FLOOR` (mirroring the existing suppression convention) and document why.
    NEVER lower any existing floor to make the suite pass — if an existing motif's precision
    regressed because discovered-check was split out of discovered-attack, FIX the detector or the
    registry order, do not weaken the floor (D-08).

    Extend the `precision_floors.py` module docstring's measurement-run summary with a dated
    (2026-06-19) note for the five new motifs: their TRAIN TP/FP/FN, precision, recall, and the
    chosen floor + rationale (especially the under-promotion treatment). Optionally add the two
    real-geometry motifs (discovered-check, trapped-piece) to the GOALS table in
    `scripts/tactic_tagger_report.py` as aspirational precision-first targets (move-type motifs are
    NOT goal candidates — their precision is ~100% by construction and recall is structurally
    shadowed).
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/scripts/tagger/test_detector_precision.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `PRECISION_FLOOR` contains entries for `discovered-check`, `trapped-piece`, `en-passant`, and `promotion` (and `under-promotion` ONLY if approach (ii) chosen; otherwise under-promotion is handled by a documented no-regression assertion / low-confidence note, NOT a % floor).
    - SOURCE: the `precision_floors.py` docstring has a dated 2026-06-19 measurement note covering all five new motifs and the explicit under-promotion treatment rationale.
    - SOURCE: no existing floor value in `PRECISION_FLOOR` was lowered (diff shows only additions to the dict, plus any documented detector/registry fixes — not floor reductions).
    - BEHAVIOR (D-08): the TRAIN floor gate passes at the freshly-baselined floors; the harness prints non-NaN precision for the shipped new motifs.
    - TEST: `uv run pytest -n auto tests/scripts/tagger/test_detector_precision.py -x -q` green (TRAIN floors asserted, TEST printed).
  </acceptance_criteria>
  <done>
    All five new motifs have a coherent, freshly-baselined never-regress floor (or a documented
    no-regression/low-confidence treatment for the 4-label under-promotion), with no existing floor
    lowered, and the harness floor gate is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + type + lint gate</name>
  <files>CHANGELOG.md, app/services/tactic_detector.py, app/repositories/library_repository.py</files>
  <read_first>
    - CLAUDE.md (Pre-PR checklist; ty/ruff/pytest gates; run full backend suite with -n auto)
    - app/services/tactic_detector.py (final state — confirm both PV passes consume the new motifs unchanged)
    - app/repositories/library_repository.py (confirm the stale comment correction from Plan 01 still reads correctly with all 5 motifs)
  </read_first>
  <action>
    Run the full backend gate and resolve all output before declaring done:
    `uv run ruff format app/ tests/`, `uv run ruff check app/ tests/ --fix`,
    `uv run ty check app/ tests/`, and `uv run pytest -n auto -x`. The new motifs flow through both
    the `allowed_*` and `missed_*` PV passes via the same `detect_tactic_motif` dispatcher (Phase
    128 integration point) — no per-pass code, but confirm no existing repository/comparison test
    regressed from the two new Tier-2 motifs entering the candidate pool. Confirm NO schema/migration
    change was introduced (the new ints live in the existing `allowed_tactic_motif` /
    `missed_tactic_motif` SmallInteger columns). Add a terse user-facing CHANGELOG.md bullet under
    `## [Unreleased] > ### Added` referencing Phase 128.1 (five new tactic motifs for lichess-theme
    coverage), tone per CLAUDE.md (what changed, not how).
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run pytest -n auto -x</automated>
  </verify>
  <acceptance_criteria>
    - TEST: `uv run pytest -n auto -x` green across the FULL backend suite (not just the tagger files).
    - TEST: `uv run ty check app/ tests/` zero errors.
    - CLI: `uv run ruff check app/ tests/` and `uv run ruff format --check app/ tests/` clean.
    - SOURCE: no new Alembic migration file was added (`git status` shows no `alembic/versions/*` addition); no new DB column.
    - SOURCE: `CHANGELOG.md` has an `## [Unreleased] > ### Added` bullet for Phase 128.1.
  </acceptance_criteria>
  <done>
    Full backend suite, ty, and ruff are green with all five motifs in place; no migration; CHANGELOG updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored PV string → detector | `moves[0]`/`boards[0]` drive the move-type checks; malformed/empty PV is already guarded upstream in `detect_tactic_motif` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128.1-03 | Tampering | move-type pre-empting a real tactic (wrong visible tag) | mitigate | New tier number 5 > 4 guarantees move-type loses every `_sort_key` tie to a real tactic (D-04); asserted by the promotion-and-fork dispatch test |
| T-128.1-04 | Repudiation | floor gate weakened to pass CI (silent precision regression) | mitigate | D-08 hard rule: never lower an existing floor; acceptance criterion checks the diff adds floors only; dated measurement note documents the baseline |
| T-128.1-05 | Tampering | new IntEnum 27-29 colliding with DB-encoded ints | accept | Append-only; existing 1-26 preserved; `test_int_motif_roundtrip` + no migration (existing rows unaffected) |
| T-128.1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (pure-Python edits); no legitimacy gate needed |
</threat_model>

<verification>
- `uv run pytest -n auto tests/scripts/tagger/test_detector_precision.py -x` green (TRAIN floors asserted at the fresh baseline).
- `uv run pytest -n auto tests/services/test_tactic_detector.py -x` green (encoding count == 29, partition includes move-type, dispatch dominance asserted).
- `uv run pytest -n auto -x` green across the full backend suite (both PV orientations unaffected).
- `uv run ty check app/ tests/` zero errors; `uv run ruff check/format` clean.
- `git status` shows no new migration and no new DB column.
</verification>

<success_criteria>
- en-passant (27), promotion (28), under-promotion (29) exist as move-type motifs at a new lowest
  tier below hanging-piece; under-promotion dominates promotion (D-01); real tactics always win (D-04).
- `MOVE_TYPE_MOTIFS` frozenset groups the three (D-02), mirroring `MATE_MOTIFS`.
- All five new motifs have a coherent fresh-TRAIN precision floor (or documented no-regression /
  low-confidence treatment for the 4-label under-promotion); no existing floor lowered (D-08).
- Theme-map entries present; both PV orientations get all five motifs for free; no schema change.
- Full backend suite + ty + ruff green; CHANGELOG updated.
</success_criteria>

<output>
Create `.planning/phases/128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec/128.1-02-SUMMARY.md` when done.
</output>
