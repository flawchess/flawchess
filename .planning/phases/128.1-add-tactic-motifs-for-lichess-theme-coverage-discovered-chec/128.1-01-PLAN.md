---
phase: 128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/motif_theme_map.py
  - tests/services/test_tactic_detector.py
  - app/repositories/library_repository.py
autonomous: true
requirements: []   # No formal REQ-IDs (seed-derived). Drive from D-01..D-09 in CONTEXT.md.
must_haves:
  truths:
    - "detect_tactic_motif emits 'discovered-check' (not 'discovered-attack') when pov's first move gives a discovered check"
    - "discovered-check ranks ABOVE discovered-attack in Tier 2 (a discovering move that gives check tags as discovered-check)"
    - "detect_tactic_motif emits 'trapped-piece' when an opponent piece is under attack and every legal escape square loses material (net material gain for the trapper)"
    - "trapped-piece ranks ABOVE the Tier-4 hanging-piece catch-all (a trapped piece is never mislabeled as merely hanging)"
    - "trapped-piece does NOT fire on a piece that is capturable-for-free right now (that is hanging-piece, distinct by definition per D-07)"
    - "Both allowed_* and missed_* PV passes emit the two new motifs for free (same dispatcher, no per-pass code)"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "DISCOVERED_CHECK=25 and TRAPPED_PIECE=26 IntEnum members; 'discovered-check'/'trapped-piece' Literal + _INT_TO_MOTIF entries; detect_discovered_check + detect_trapped_piece detectors; Tier-2 registry placement"
      contains: "DISCOVERED_CHECK = 25"
    - path: "tests/scripts/tagger/motif_theme_map.py"
      provides: "'discovered-check' -> ('discoveredCheck',) and 'trapped-piece' -> ('trappedPiece',) entries"
      contains: "discoveredCheck"
    - path: "tests/services/test_tactic_detector.py"
      provides: "Updated encoding-count + partition assertions; positive/negative fixtures for the two new motifs; dispatch-ordering assertions"
  key_links:
    - from: "app/services/tactic_detector.py detect_tactic_motif dispatcher"
      to: "_GEOMETRIC_REGISTRY (Tier 2)"
      via: "discovered-check and trapped-piece appended to the Tier-2 registry list with ranks per D-03/D-05"
      pattern: "DISCOVERED_CHECK|TRAPPED_PIECE"
    - from: "tests/scripts/tagger/test_detector_precision.py"
      to: "tests/scripts/tagger/motif_theme_map.py MOTIF_TO_THEMES"
      via: "harness scores the two new motifs via their theme-map entries"
      pattern: "discoveredCheck|trappedPiece"
---

<objective>
Add the two **real tactical geometry** motifs from SEED-058 to `detect_tactic_motif`:
`discovered-check` (split out of `discovered-attack`, logic already exists at
`tactic_detector.py:547-558`) and `trapped-piece` (a new precision-first escape-square
detector). Both are Tier 2 per D-03. This plan delivers the detectors, their IntEnum /
Literal / theme-map entries, and the Tier-2 dispatch placement, with provisional
precision floors deferred to Plan 02's single authoritative full-baseline run (all five
new motifs must be present for one coherent fresh-TRAIN baseline).

Purpose: close the `discoveredCheck` + `trappedPiece` lichess-theme coverage gaps with
precision-first detectors (D-06 governing value: a wrong visible tag erodes trust more
than a missing one). These are the harder, geometry-bearing half of the phase; the
trivial move-type trio follows in Plan 02.

Output: 2 new motifs (ints 25-26), 2 detector functions, theme-map entries, Tier-2
dispatch ranks, updated fast-guard tests, and a corrected stale family-partition comment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec/128.1-CONTEXT.md
@.planning/seeds/SEED-058-new-tactic-motifs-lichess-coverage.md
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `TacticMotifInt.DISCOVERED_CHECK = 25` | `app/services/tactic_detector.py` | IntEnum member |
| `TacticMotifInt.TRAPPED_PIECE = 26` | `app/services/tactic_detector.py` | IntEnum member |
| `"discovered-check"` | `app/services/tactic_detector.py` | `TacticMotif` Literal value + `_INT_TO_MOTIF[25]` |
| `"trapped-piece"` | `app/services/tactic_detector.py` | `TacticMotif` Literal value + `_INT_TO_MOTIF[26]` |
| `detect_discovered_check(...)` | `app/services/tactic_detector.py` | detector fn, 3-tuple `(fired, piece, depth)` |
| `detect_trapped_piece(...)` | `app/services/tactic_detector.py` | detector fn, 3-tuple `(fired, piece, depth)` |
| `"discovered-check": ("discoveredCheck",)` | `tests/scripts/tagger/motif_theme_map.py` | `MOTIF_TO_THEMES` key |
| `"trapped-piece": ("trappedPiece",)` | `tests/scripts/tagger/motif_theme_map.py` | `MOTIF_TO_THEMES` key |

Note: the matching `precision_floors.py` entries for these two motifs are finalized in
Plan 02 (single coherent fresh-TRAIN baseline with all five motifs present).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add discovered-check motif (split from discovered-attack) and Tier-2 dispatch placement</name>
  <files>app/services/tactic_detector.py, tests/services/test_tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py:69-181 (TacticMotifInt IntEnum, TacticMotif Literal, _INT_TO_MOTIF, MATE_MOTIFS pattern)
    - app/services/tactic_detector.py:532-581 (detect_discovered_attack — Sub-case 1 at :547-558 is the discovered-check logic to lift out)
    - app/services/tactic_detector.py:1343-1358 (_GEOMETRIC_REGISTRY Tier 2 + _GEOMETRIC_DETECTOR_FNS)
    - app/services/tactic_detector.py:1459-1500 (Tier-2 candidate collection + _sort_key — rank = enumerate index)
    - tests/services/test_tactic_detector.py:1243-1363 (encoding-count asserts at :1249-1251 and partition assert at :1359-1363 that MUST be updated)
    - tests/services/test_tactic_detector.py:1119-1174 (_VALIDATED_FIXTURE_SETS / _VALIDATED_IDS — new positive fixtures slot here)
    - tests/services/test_tactic_detector.py:331-... (_DISCOVERED_ATTACK_FIXTURES — pattern for a new _DISCOVERED_CHECK_FIXTURES list)
  </read_first>
  <behavior>
    - A PV whose first pov move delivers a check where the checking piece is NOT the piece
      that moved (a discovered check) → detect_tactic_motif returns motif_int 25
      (discovered-check), depth 0, with tactic_piece = the unveiled checker's piece_type.
    - A discovered attack that is NOT a check (Sub-case 2 capture) → still returns 6
      (discovered-attack), unchanged.
    - discovered-check ranks ABOVE discovered-attack: when the discovering move gives check,
      the dispatcher returns discovered-check, never discovered-attack (D-03).
    - Encoding round-trips: _INT_TO_MOTIF[25] == "discovered-check" and
      _MOTIF_TO_INT["discovered-check"] == 25.
  </behavior>
  <action>
    Append `DISCOVERED_CHECK = 25` to the `TacticMotifInt` IntEnum (after `DOVETAIL_MATE = 24`,
    preserving all existing 1-24 values — they encode existing DB rows, never reorder). Add
    `"discovered-check"` to the `TacticMotif` Literal and add `25: "discovered-check"` to
    `_INT_TO_MOTIF` (the `_MOTIF_TO_INT` reverse map is derived automatically).

    Lift Sub-case 1 (`tactic_detector.py:547-558`) out of `detect_discovered_attack` into a new
    standalone detector `detect_discovered_check(boards, moves, pov) -> tuple[bool, int | None, int | None]`
    returning `(fired, unveiled_checker_piece_type, 0)` on detection (depth 0: discovered check
    is always the first pov move). REMOVE Sub-case 1 from `detect_discovered_attack` so that a
    discovering check no longer tags as discovered-attack (D-03 dominance: more-specific subtype
    wins, mirroring named-mate subtypes). `detect_discovered_attack` keeps only Sub-case 2.
    Add an explanatory comment at the removal site referencing D-03 (split rationale) so a future
    reader understands why Sub-case 1 moved.

    Register the new motif in Tier 2: add `("discovered-check", TacticMotifInt.DISCOVERED_CHECK)`
    to `_GEOMETRIC_REGISTRY` and `"discovered-check": detect_discovered_check` to
    `_GEOMETRIC_DETECTOR_FNS`. Per D-03/D-05, place the `discovered-check` registry entry at a
    LOWER list index (higher priority — rank is the enumerate index, smaller wins in `_sort_key`)
    than the existing `discovered-attack` entry, so when both fire on the same PV at equal depth,
    discovered-check wins. Keep both `double-check` and the geometric workhorses in their relative
    order; only the discovered-check-vs-discovered-attack ordering is load-bearing here.

    Update the fast-guard tests in `tests/services/test_tactic_detector.py`:
    - `test_all_24_motifs_encoded` (:1249-1251): rename to reflect the new count and assert
      `len(_INT_TO_MOTIF) == 26` and `len(_MOTIF_TO_INT) == 26` (will become 29 in Plan 02 — leave
      a code comment noting Plan 02 raises it to 29).
    - Add a `_DISCOVERED_CHECK_FIXTURES` list of >=10 hand-confirmed positive `(fen, pv, "discovered-check")`
      tuples sourced from TRAIN rows tagged `discoveredCheck` (there are 1004 — pull a handful of clear
      ones), append it to `_VALIDATED_FIXTURE_SETS` and `"discovered-check"` to `_VALIDATED_IDS`.
    - The partition assert `test_suppressed_set_matches_validated_partition` (:1359-1363) now passes
      automatically once discovered-check is in `_VALIDATED_FIXTURE_SETS` (validated ∪ suppressed must
      equal all of `_INT_TO_MOTIF.values()`).
    Do NOT inline code samples that contain the literal theme strings the harness negative-greps;
    there is no negative-grep gate here, so this is informational only.
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_tactic_detector.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "DISCOVERED_CHECK = 25" app/services/tactic_detector.py` matches; `25: "discovered-check"` present in `_INT_TO_MOTIF`.
    - SOURCE: `def detect_discovered_check(` exists; `detect_discovered_attack` no longer contains the `is_check()` Sub-case-1 block.
    - SOURCE: in `_GEOMETRIC_REGISTRY`, the `discovered-check` tuple's list index is strictly less than the `discovered-attack` tuple's index.
    - BEHAVIOR: a discovered-check fixture returns `_INT_TO_MOTIF[motif_int] == "discovered-check"`; a Sub-case-2 discovered-attack fixture still returns `"discovered-attack"`.
    - TEST: `uv run pytest -n auto tests/services/test_tactic_detector.py -x -q` is green (encoding-count and partition asserts updated).
  </acceptance_criteria>
  <done>
    discovered-check is a standalone Tier-2 motif (int 25) that dominates discovered-attack at
    equal depth, with passing encoding/partition/fixture guards.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add precision-first trapped-piece detector (Tier 2, beats Tier-4 hanging-piece) + theme-map entries</name>
  <files>app/services/tactic_detector.py, tests/scripts/tagger/motif_theme_map.py, tests/services/test_tactic_detector.py, app/repositories/library_repository.py</files>
  <read_first>
    - app/services/tactic_detector.py:44-61 (_PIECE_VALUES, TACTIC_CONFIDENCE_HIGH, _RAY_PIECES — reuse, no new magic numbers)
    - app/services/tactic_detector.py:233-250 (_material_diff, _grade helpers)
    - app/services/tactic_detector.py:315-341 (detect_hanging_piece — the Tier-4 catch-all trapped-piece must be DISTINCT from and BEAT)
    - app/services/tactic_detector.py:259-312 (detect_fork — reference for escape/attack enumeration via board.attacks / _is_hanging)
    - app/services/tactic_detector.py:1459-1500 (Tier-2 candidate collection, Tier-4 hanging-piece block, _sort_key)
    - tests/scripts/tagger/motif_theme_map.py:44-74 (MOTIF_TO_THEMES — add the two new keys)
    - app/repositories/library_repository.py:60-105 (FAMILY_TO_MOTIF_INTS + the stale "All 24 motif ints" comment at :71 to correct)
    - tests/services/test_tactic_detector.py:1289-1296 (TestHardNegatives — trapped-piece must not over-fire)
  </read_first>
  <behavior>
    - A position where an opponent piece is attacked by pov AND every legal move of that piece
      lands on a square where it is captured/lost for net pov material gain → detect_trapped_piece
      returns (True, trapped_victim_piece_type, depth).
    - A piece that can be captured for free RIGHT NOW (undefended, no escape needed) is hanging,
      NOT trapped: detect_trapped_piece does NOT fire on a bare hanging capture (D-07 distinction).
    - A piece with at least one safe (non-material-losing) escape square → does NOT fire
      (strict D-06 gate: EVERY legal escape must lose material).
    - When both trapped-piece and hanging-piece fire on the same PV, the dispatcher returns
      trapped-piece (Tier 2 < Tier 4 in the sort key), per D-03.
    - Pawns are excluded as victims (mirror hanging-piece's pawn exclusion) unless a clear
      higher-value case justifies inclusion — default: non-pawn victims only.
  </behavior>
  <action>
    Append `TRAPPED_PIECE = 26` to `TacticMotifInt`, add `"trapped-piece"` to the `TacticMotif`
    Literal and `26: "trapped-piece"` to `_INT_TO_MOTIF`.

    Implement `detect_trapped_piece(boards, moves, pov) -> tuple[bool, int | None, int | None]`
    returning the 3-tuple `(fired, victim_piece_type, depth)`. D-06 strict precision-first gate:
    for the board after pov's relevant move, find an opponent (non-pov) non-pawn piece that is
    (a) currently attacked by a pov piece, AND (b) for which EVERY pseudo-legal/legal move of that
    piece (enumerate via the opponent's escape squares — construct the escape set from
    `board.attacks(victim_sq)` filtered to legal destinations, or iterate the victim's legal moves
    on a board copy with the turn set to the opponent) lands the piece on a square where pov wins
    material (the destination square is attacked by pov for a net material gain, accounting for
    defenders via a static-exchange-style check or the existing `_is_hanging`/`_material_diff`
    helpers). Fire only when the escape set is non-empty AND all escapes lose material (a piece with
    NO legal moves that is in check is mate, not trapped — exclude; a piece capturable for free with
    no need to move is hanging — exclude, leave it to the Tier-4 catch-all). Reuse `_PIECE_VALUES` /
    `_material_diff` — NO new magic numbers; if a new threshold is needed, name it as a module
    constant near the other named constants. Keep the function within nesting soft-3 / hard-4 and
    logic LOC soft-100 — extract an `_escape_squares_all_lose_material(board, victim_sq, pov)`
    helper if the escape enumeration pushes nesting past 3. Add a comment at the detector head
    stating the D-06 strict-gate definition and the D-07 distinction from hanging-piece.

    Register in Tier 2: add `("trapped-piece", TacticMotifInt.TRAPPED_PIECE)` to `_GEOMETRIC_REGISTRY`
    and `"trapped-piece": detect_trapped_piece` to `_GEOMETRIC_DETECTOR_FNS`. Because Tier 2 (2) sorts
    before Tier 4 (4) in `_sort_key`, the existing dispatcher already makes trapped-piece beat
    hanging-piece with NO change to the Tier-4 block (D-03 satisfied by tier number alone). Pick the
    intra-Tier-2 rank for trapped-piece by judgement (D-05) — place it after the geometric
    material-winners and the discovery motifs is fine; the only hard constraint is tier, not
    intra-tier rank. Document the chosen rank with a one-line comment.

    Add the theme-map entries to `MOTIF_TO_THEMES` in `tests/scripts/tagger/motif_theme_map.py`:
    `"discovered-check": ("discoveredCheck",)` and `"trapped-piece": ("trappedPiece",)`.

    Correct the now-stale comment in `app/repositories/library_repository.py:71`
    ("All 24 motif ints appear exactly once across all families") to note that the five Phase-128.1
    motifs (discovered-check, trapped-piece, en-passant, promotion, under-promotion) are
    intentionally NOT yet assigned to a chip family — family/chip membership is a Phase 129
    decision per D-09. Do NOT add the new ints to `FAMILY_TO_MOTIF_INTS` (no chip code this phase).

    Add fast-guard coverage in `tests/services/test_tactic_detector.py`: a `_TRAPPED_PIECE_FIXTURES`
    list of >=10 positive `(fen, pv, "trapped-piece")` tuples from TRAIN rows tagged `trappedPiece`
    (28 available — pull clear ones), append to `_VALIDATED_FIXTURE_SETS` + `_VALIDATED_IDS`. Update
    the encoding-count asserts to `== 26` (matching Task 1; Plan 02 raises to 29). Add a dispatch
    assertion proving trapped-piece wins over hanging-piece on a co-firing fixture.
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_tactic_detector.py -x -q && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "TRAPPED_PIECE = 26" app/services/tactic_detector.py` matches; `26: "trapped-piece"` in `_INT_TO_MOTIF`; `def detect_trapped_piece(` exists.
    - SOURCE: `("trapped-piece", TacticMotifInt.TRAPPED_PIECE)` present in `_GEOMETRIC_REGISTRY`; `"trapped-piece": detect_trapped_piece` in `_GEOMETRIC_DETECTOR_FNS`.
    - SOURCE: `MOTIF_TO_THEMES` contains `"discovered-check": ("discoveredCheck",)` and `"trapped-piece": ("trappedPiece",)`.
    - SOURCE: `library_repository.py:71` comment no longer claims "All 24 motif ints appear exactly once" without the Phase-128.1 carve-out note.
    - BEHAVIOR (D-06 strict gate): a fixture where the victim has one safe escape returns `fired=False`; a fixture where all escapes lose material returns `fired=True`.
    - BEHAVIOR (D-07): a bare free-capture (hanging) fixture does NOT return trapped-piece.
    - BEHAVIOR (D-03): a co-firing trapped+hanging fixture returns `"trapped-piece"`.
    - TEST: `uv run pytest -n auto tests/services/test_tactic_detector.py -x -q` green AND `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>
    trapped-piece is a Tier-2 motif (int 26) with a strict D-06 material gate, distinct from and
    dominating hanging-piece (D-07/D-03), theme-mapped, ty-clean, and fast-guard-tested.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored PV string → detector | `pv_str` from `game_positions.pv` is parsed; malformed/empty must never raise (existing T-124-03/T-127-01 guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128.1-01 | Denial of Service | `detect_trapped_piece` escape enumeration on adversarial PV | mitigate | Reuse the existing `_parse_pv` try/except guard in `detect_tactic_motif`; the new detector operates only on already-parsed boards, raises nothing; bounded by piece/escape-square counts (<= 64) — no unbounded loops |
| T-128.1-02 | Tampering | new IntEnum values colliding with existing DB-encoded ints | accept | Append-only 25/26; existing 1-24 never reordered (asserted by `test_int_motif_roundtrip`); no migration, existing rows unaffected |
| T-128.1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase (pure-Python edits to existing modules); no legitimacy gate needed |
</threat_model>

<verification>
- `uv run pytest -n auto tests/services/test_tactic_detector.py -x` green.
- `uv run pytest -n auto tests/scripts/tagger/` green (the two new motifs score without crashing; floors are finalized in Plan 02).
- `uv run ty check app/ tests/` zero errors (explicit return types on both detectors; 3-tuple signatures match `_BoolPieceFn`).
- `uv run ruff check app/ tests/` and `uv run ruff format --check app/ tests/` clean.
</verification>

<success_criteria>
- discovered-check (int 25) and trapped-piece (int 26) exist as Tier-2 motifs with the D-03
  dominance orderings (discovered-check > discovered-attack; trapped-piece > hanging-piece).
- trapped-piece enforces the D-06 strict material gate and is distinct from hanging-piece (D-07).
- Theme-map entries present; both PV orientations get the motifs for free (no per-pass code).
- All fast-guard tests pass; ty and ruff clean. Precision floors deferred to Plan 02.
</success_criteria>

<output>
Create `.planning/phases/128.1-add-tactic-motifs-for-lichess-theme-coverage-discovered-chec/128.1-01-SUMMARY.md` when done.
</output>
