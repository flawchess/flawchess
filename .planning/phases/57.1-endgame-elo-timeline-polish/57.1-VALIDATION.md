---
phase: 57.1
slug: endgame-elo-timeline-polish
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-18
approved: 2026-04-18
---

# Phase 57.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> Per-task verification commands live inside each task's `<automated>` block in `57.1-01-PLAN.md` and `57.1-02-PLAN.md`. This document captures the test infrastructure and Wave 0 dependencies; the plans themselves are the source of truth for which test runs after which task.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (backend) / vitest (frontend) |
| **Config file** | `pyproject.toml` (pytest), `frontend/vitest.config.ts` |
| **Quick run command** | `uv run pytest tests/test_endgame_service.py::TestEndgameEloTimeline -x` |
| **Full suite command** | `uv run pytest && cd frontend && npm test -- --run` |
| **Estimated runtime** | ~30 seconds (quick) / ~120 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run quick run command (targeted file)
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 57.1-01-01 | 01 | 1 | ELO-05 | T-57.1-01..06 | Schema field add (`per_week_endgame_games`) is server-derived integer; no user input | unit + type | `uv run ty check app/ tests/` then `uv run pytest tests/test_endgame_service.py::TestEndgameEloTimeline -x` | ✅ existing | ⬜ pending |
| 57.1-01-02 | 01 | 1 | ELO-05 | T-57.1-01..06 | Asof-join correctness; per-combo isolation; same-day boundary fix | unit + integration | `uv run pytest tests/test_endgame_service.py::TestEndgameEloTimeline tests/test_integration_routers.py::TestEndgameEloTimelineRouter -x` | ✅ existing | ⬜ pending |
| 57.1-01-03 | 01 | 1 | ELO-05 | — | Doc-only edits (UI-SPEC + HUMAN-UAT inline supersede per D-22 / D-17) | grep | `grep -q "Bars show endgame games per week" .planning/phases/57-endgame-elo-timeline-chart/57-UI-SPEC.md` | ✅ existing | ⬜ pending |
| 57.1-02-01 | 02 | 2 | ELO-05 | T-57.1-FE-01..06 | ComposedChart swap, volume bars, copy rewrite, theme constant; noUncheckedIndexedAccess narrowing on visible-sum loop | type + lint + build | `cd frontend && npx tsc --noEmit && npm run lint && npm run build` | ✅ existing | ⬜ pending |
| 57.1-02-02 | 02 | 2 | ELO-05 | T-57.1-FE-01..06 | Human-verify checkpoint (volume bars at bottom 20%, hidden right axis, legend toggle reacts, locked-copy strings present, no "performance rating") | manual | see Manual-Only Verifications below | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing pytest + vitest infrastructure covers all phase requirements — no new framework install, no new test files. Plan 01 extends `tests/test_endgame_service.py::TestEndgameEloTimeline` and `tests/test_integration_routers.py::TestEndgameEloTimelineRouter` in place. Plan 02 covers frontend behavior via the human-verify checkpoint (Phase 57 set the precedent of no chart-level vitest spec for Recharts components).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Volume bars render at chart bottom (~20%) without overlapping line series | ELO-05 | Visual layout — depends on Recharts axis configuration and rendered DOM, not data shape | Open dashboard for a user with ≥12 weeks of endgame data, confirm volume bars are muted, ~20% chart height, hidden right axis, no overlap with both lines |
| Info popover copy frames metric as skill-adjusted rating, not performance rating | ELO-05 | Subjective copy review — phrasing must match CONTEXT.md decision | Open info popover on Endgame ELO Timeline card, verify copy mentions "skill-adjusted rating", `actual_elo + 400·log10(skill/(1-skill))`, and 50% skill ⇒ zero-delta intuition; confirm word "performance rating" is absent |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — existing files cover everything)
- [x] No watch-mode flags (`pytest -x`, `npm test -- --run`)
- [x] Feedback latency < 30s (quick run = ~30s)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-18
