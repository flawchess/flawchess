---
phase: 175-board-filter-gem-great-consumption
plan: 04
type: execute
wave: 4
depends_on: ["175-01", "175-03"]
files_modified:
  - frontend/src/hooks/useFlawFilterStore.ts
  - frontend/src/components/filters/FlawFilterControl.tsx
  - frontend/src/pages/library/GamesTab.tsx
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/api/client.ts
  - frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx
autonomous: true
requirements: [FILT-01]

must_haves:
  truths:
    - "The Library Tags/best-move filter strip gains two independent boolean toggles, 'has gem' and 'has great', on BOTH desktop and mobile (via GamesTab's two FlawFilterControl render sites) (D-05)."
    - "Toggling 'has gem' sends has_gem=true to GET /library/games and returns only games with a qualifying stored user-move gem row; both toggles on sends both params (union at the backend) (D-05)."
    - "The two toggles compose with every existing filter (time control, color, rated, opponent type, recency) because they thread through the same buildLibraryParams -> libraryApi.getGames path the other filters use (D-05a)."
    - "isFlawFilterNonDefault returns true when either hasGem or hasGreat is set, so the filter-active indicator lights up."
  prohibitions:
    - "The toggles must NOT introduce a parallel filter fetch path — they thread has_gem/has_great through the existing buildLibraryParams/getGames wiring only (D-05a)."
    - "No text-xs on the toggle labels; each toggle button needs data-testid (filter-has-gem / filter-has-great) + aria-label + aria-pressed (Browser Automation Rules)."
    - "Do NOT make it a single 3-state cycling control — two independent booleans (D-05)."
  artifacts:
    - "frontend/src/hooks/useFlawFilterStore.ts::FlawFilterState.hasGem, .hasGreat + DEFAULT_FLAW_FILTER defaults + isFlawFilterNonDefault gate"
    - "frontend/src/components/filters/FlawFilterControl.tsx BestMove toggle section (desktop + mobile)"
    - "has_gem / has_great params in useLibrary.buildLibraryParams and api/client.ts libraryApi.getGames"
  key_links:
    - "FlawFilterControl toggle -> useFlawFilterStore.hasGem/hasGreat -> buildLibraryParams -> libraryApi.getGames -> GET /library/games?has_gem (Plan 01 backend)."
---

<objective>
Deliver FILT-01's UI: two independent "has gem" / "has great" toggles in the Library
game filter, threaded through the existing filter-param machinery to the backend
`has_gem`/`has_great` query params (Plan 01). Both toggled on means "gem OR great"
(the backend unions them). Applying a toggle composes with every other filter.

Purpose: let the user surface their own brilliancies ("games where I found a gem")
via the same flaw/tactic filter strip.
Output: `FlawFilterState.hasGem`/`hasGreat`, the two toggle buttons (desktop + mobile),
and the `has_gem`/`has_great` param threading.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-board-filter-gem-great-consumption/175-CONTEXT.md
@.planning/phases/175-board-filter-gem-great-consumption/175-RESEARCH.md
@.planning/phases/175-board-filter-gem-great-consumption/175-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- `FlawFilterState.hasGem` / `.hasGreat` booleans (default false) + `DEFAULT_FLAW_FILTER` entries + `isFlawFilterNonDefault` gate.
- `FlawFilterControl` "Best Moves" toggle section with `filter-has-gem` / `filter-has-great` buttons (desktop + mobile).
- `has_gem` / `has_great` params in `buildLibraryParams` (useLibrary.ts) and `libraryApi.getGames` (api/client.ts).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FlawFilterState toggles + FlawFilterControl best-move section</name>
  <files>frontend/src/hooks/useFlawFilterStore.ts, frontend/src/components/filters/FlawFilterControl.tsx, frontend/src/pages/library/GamesTab.tsx, frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useFlawFilterStore.ts (the `FlawFilterState` interface at ~9-38, `DEFAULT_FLAW_FILTER` at ~39-48, and `isFlawFilterNonDefault` at ~62).
    - frontend/src/components/filters/FlawFilterControl.tsx (the `SeverityFilterButton` + `handleSeverityToggle` pattern at ~215-244, ~388-393 — the pill-toggle shape to mirror; the props interface).
    - frontend/src/pages/library/GamesTab.tsx (the desktop FlawFilterControl render at ~325-349 and the mobile render at ~503-527 — BOTH must receive the new props; note RESEARCH's correction that FlawFilterControl, not FilterPanel/MobileFilterDrawer, is the mirror target).
    - frontend/src/components/icons/GemIcon.tsx + GreatMoveIcon.tsx (Plan 03) for the toggle glyphs.
    - 175-PATTERNS.md §"useFlawFilterStore", §"FlawFilterControl new Best Moves toggle section".
  </read_first>
  <behavior>
    - Test: isFlawFilterNonDefault({...default, hasGem: true}) === true; with hasGreat true === true; all-default === false.
    - Test (FlawFilterControl): clicking the has-gem toggle fires onHasGemToggle; the button reflects aria-pressed from the hasGem prop.
    - Test: has-gem and has-great are independent (toggling one does not change the other).
  </behavior>
  <action>
    Add `hasGem: boolean` and `hasGreat: boolean` to `FlawFilterState`, default `false` in
    `DEFAULT_FLAW_FILTER`, and extend `isFlawFilterNonDefault` to return true when
    `filter.hasGem || filter.hasGreat` (OR'd with the existing checks). Extend
    `FlawFilterControlProps` with optional `hasGem`/`hasGreat` booleans and
    `onHasGemToggle`/`onHasGreatToggle` callbacks. Add a "Best Moves" toggle section rendering
    two pill buttons mirroring `SeverityFilterButton`: `data-testid="filter-has-gem"` (GemIcon +
    "Gem" label) and `data-testid="filter-has-great"` (GreatMoveIcon + "Great" label), each with
    `aria-pressed`, `aria-label` (e.g. "Filter by gem moves" / "Filter by great moves"), colored
    when selected (gem = GEM_GLYPH/MAIA_ACCENT, great = GREAT_GLYPH/GREAT_ACCENT via the Button
    variant/className, no hard-coded semantic hex), text-sm minimum. Wire both GamesTab.tsx render
    sites (desktop + mobile) to pass the store's hasGem/hasGreat and toggle updaters. Add tests to
    FlawFilterControl.test.tsx.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run FlawFilterControl useFlawFilterStore && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hasGem\|hasGreat" frontend/src/hooks/useFlawFilterStore.ts` shows both in the interface, DEFAULT_FLAW_FILTER, and isFlawFilterNonDefault.
    - `grep -n "filter-has-gem\|filter-has-great" frontend/src/components/filters/FlawFilterControl.tsx` returns both testids; each button has `aria-pressed` and `aria-label`.
    - `grep -c "FlawFilterControl" frontend/src/pages/library/GamesTab.tsx` shows the two render sites both updated (hasGem/hasGreat props present at both).
    - `grep -rn "text-xs" frontend/src/components/filters/FlawFilterControl.tsx` shows nothing new.
    - `cd frontend && npm test -- --run FlawFilterControl useFlawFilterStore` passes; `npx tsc -b` clean.
  </acceptance_criteria>
  <done>Two independent gem/great toggles render on desktop and mobile, drive the filter store, light the filter-active indicator, and carry testids/aria per Browser Automation Rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread has_gem/has_great through buildLibraryParams and the API client</name>
  <files>frontend/src/hooks/useLibrary.ts, frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/hooks/useLibrary.ts (`buildLibraryParams` at ~59-79 — how `severity`/`rated` are conditionally included and omitted when empty).
    - frontend/src/api/client.ts (the `libraryApi.getGames` param object at ~200-260 and how `rated`/`severity` are serialized into URLSearchParams at ~83-94).
    - 175-PATTERNS.md §"useLibrary.ts + api/client.ts param threading".
  </read_first>
  <behavior>
    - Test (buildLibraryParams): hasGem true => params include has_gem: true; false => has_gem omitted (mirrors rated's conditional inclusion). Same for hasGreat.
    - Test (client getGames): has_gem/has_great serialize into the query string only when true.
  </behavior>
  <action>
    Extend `buildLibraryParams` to read `filter.hasGem`/`filter.hasGreat` from the passed
    FlawFilterState and include `has_gem`/`has_great` in the params object only when true
    (mirror the `rated` conditional-inclusion pattern; omit when false so a default filter
    sends nothing). Add `has_gem?: boolean` / `has_great?: boolean` to the `libraryApi.getGames`
    param type and serialize each into URLSearchParams only when truthy, exactly like the
    existing `rated` wiring. No new fetch path — same getGames call. Add tests covering the
    conditional inclusion.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run useLibrary client && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "has_gem\|has_great" frontend/src/hooks/useLibrary.ts frontend/src/api/client.ts` shows the param in both, conditionally included.
    - `cd frontend && npm test -- --run useLibrary client` passes (inclusion-when-true / omission-when-false).
    - `npx tsc -b` clean; `npm run lint` clean (knip: no dead exports).
  </acceptance_criteria>
  <done>has_gem/has_great flow from the toggles through buildLibraryParams and libraryApi.getGames to the backend endpoint via the single existing filter path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /library/games | the two boolean toggles become query params on an already-authenticated request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-08 | Information Disclosure (IDOR) | has_gem/has_great filter request | high | mitigate | The client only sets two booleans; all user-scoping and the correlated EXISTS live server-side (Plan 01, T-175-01). The frontend adds no unscoped query and cannot widen the result set beyond the authenticated user's own games. |
| T-175-09 | Tampering (input) | boolean query params | low | accept | FastAPI validates `bool | None` at the boundary; a malformed value is rejected before any query runs. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run FlawFilterControl useFlawFilterStore useLibrary client` green.
- `cd frontend && npm run lint && npx tsc -b` clean.
</verification>

<success_criteria>
FILT-01 UI is met: two independent gem/great toggles on desktop and mobile drive the existing filter-param path so applying a toggle returns only the user's qualifying games and composes with every other filter.
</success_criteria>

<output>
Create `.planning/phases/175-board-filter-gem-great-consumption/175-04-SUMMARY.md` when done.
</output>
