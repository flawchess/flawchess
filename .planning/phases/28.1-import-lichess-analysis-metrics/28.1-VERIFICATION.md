---
phase: 28.1-import-lichess-analysis-metrics
verified: 2026-03-26T08:00:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
---

# Phase 28.1: Import Lichess Analysis Metrics — Verification Report

**Phase Goal:** Import lichess analysis metrics (ACPL, inaccuracy/mistake/blunder counts per color) during game normalization. Storage only — no display, no API endpoints.
**Verified:** 2026-03-26T08:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                              | Status     | Evidence                                                                                                    |
|-----|----------------------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------------------------|
| 1   | Lichess games with computer analysis import ACPL, inaccuracy, mistake, and blunder counts for both colors | VERIFIED | `normalization.py:352-359` extracts all 8 keys from `white_analysis`/`black_analysis`; `test_lichess_analysis_metrics_present` asserts exact values (e.g. `white_acpl==15`, `black_blunders==0`) |
| 2   | Lichess games without analysis import with all 8 metric columns as NULL                            | VERIFIED | `dict.get()` returns `None` when key absent; `test_lichess_analysis_metrics_absent` asserts all 8 are `None` |
| 3   | Chess.com games import with all 8 metric columns as NULL (unchanged normalizer)                    | VERIFIED | `normalize_chesscom_game()` not modified; `test_chesscom_no_analysis_metrics` asserts all 8 keys absent from returned dict |
| 4   | Alembic migration adds 8 nullable SmallInteger columns to games table                             | VERIFIED | `20260326_071723_5acd32d13dbb_add_lichess_analysis_metrics_columns.py` contains 8 `op.add_column` with `sa.SmallInteger(), nullable=True`; downgrade has matching 8 `op.drop_column` |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/models/game.py` | 8 new SmallInteger columns on Game model | VERIFIED | Lines 67-74: all 8 columns present as `Mapped[int \| None] = mapped_column(SmallInteger, nullable=True)`; `SmallInteger` added to sqlalchemy import at line 3 |
| `app/services/normalization.py` | Extraction of ACPL/inaccuracy/mistake/blunder from lichess analysis dict | VERIFIED | Lines 352-359: all 8 keys added to return dict of `normalize_lichess_game()`; `white_analysis`/`black_analysis` already extracted at lines 325-326 |
| `tests/test_normalization.py` | Tests for new lichess analysis metric extraction | VERIFIED | `TestLichessAnalysisMetrics` class with 3 methods: `test_lichess_analysis_metrics_present`, `test_lichess_analysis_metrics_absent`, `test_chesscom_no_analysis_metrics`; all 85 normalization tests pass |
| `alembic/versions/20260326_071723_5acd32d13dbb_add_lichess_analysis_metrics_columns.py` | Migration adding 8 columns | VERIFIED | 8 `op.add_column` in upgrade, 8 `op.drop_column` in downgrade, all SmallInteger nullable |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/services/normalization.py` | `app/models/game.py` | returned dict keys match Game model columns | VERIFIED | Dict keys at lines 352-359 (`white_acpl`, `black_acpl`, `white_inaccuracies`, `black_inaccuracies`, `white_mistakes`, `black_mistakes`, `white_blunders`, `black_blunders`) exactly match the 8 column names defined in the Game model at lines 67-74 |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| LMETRIC-01 | 28.1-01-PLAN.md | System stores per-player analysis metrics (ACPL, inaccuracy count, mistake count, blunder count) as nullable SmallInteger columns on the games table | SATISFIED | 8 `SmallInteger, nullable=True` columns in `app/models/game.py:67-74`; migration `5acd32d13dbb` adds them to DB; REQUIREMENTS.md marks `[x]` complete |
| LMETRIC-02 | 28.1-01-PLAN.md | System imports lichess per-player analysis metrics (ACPL, inaccuracy, mistake, blunder counts) from the API response during game normalization | SATISFIED | `normalize_lichess_game()` returns all 8 metric keys from the analysis dict at `normalization.py:352-359`; REQUIREMENTS.md marks `[x]` complete |

No orphaned requirements — REQUIREMENTS.md traceability table maps both IDs to Phase 28.1 with status Complete.

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments, stub returns, or empty implementations found in any of the 4 modified files.

---

### Human Verification Required

None. All behavior (storage-only, no UI, no API endpoints) is fully verifiable programmatically via the test suite and static analysis.

---

## Commits Verified

All three documented commits exist and are on the branch:

- `a42c4b3` — `test(28.1-01): add failing tests for lichess analysis metrics extraction` (TDD RED)
- `fb68531` — `feat(28.1-01): add lichess analysis metrics to Game model and normalizer` (GREEN)
- `e34b5ee` — `feat(28.1-01): generate alembic migration and update REQUIREMENTS.md`

---

## Summary

Phase 28.1 goal is fully achieved. The 8 nullable SmallInteger columns (`white_acpl`, `black_acpl`, `white_inaccuracies`, `black_inaccuracies`, `white_mistakes`, `black_mistakes`, `white_blunders`, `black_blunders`) exist on the Game model and in the database via a clean Alembic migration. The lichess normalizer extracts all 8 values from the `analysis` dict already present in the API response. The chess.com normalizer is untouched. Three tests cover the three required scenarios (metrics present, metrics absent, chess.com unchanged). All 85 normalization tests pass. Both LMETRIC-01 and LMETRIC-02 are satisfied and marked complete in REQUIREMENTS.md.

---

_Verified: 2026-03-26T08:00:00Z_
_Verifier: Claude (gsd-verifier)_
