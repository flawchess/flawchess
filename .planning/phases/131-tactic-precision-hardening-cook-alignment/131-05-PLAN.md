---
phase: 131-tactic-precision-hardening-cook-alignment
plan: 05
type: execute
wave: 4
depends_on:
  - 131-03
  - 131-04
files_modified:
  - reports/tactic-tagger/tactic-tagger-2026-06-22.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every shipped in-scope motif clears its precision bar on the held-out TEST split; --check-goals --eval-set test exits 0 or every remaining miss is a suppressed motif (D-11)."
    - "The corrected detector is re-run over the existing tagged games on dev via scripts/backfill_flaws.py — no dev DB reset (D-12)."
    - "The dev re-backfill is the real-data validation of the fix beyond the CC0 fixtures (D-12)."
  artifacts:
    - path: "reports/tactic-tagger/tactic-tagger-2026-06-22.md"
      provides: "Refreshed full per-motif precision/recall report after the alignment"
  key_links:
    - from: "scripts/backfill_flaws.py"
      to: "classify_game_flaws (corrected detector)"
      via: "dev re-backfill recomputes game_flaws tactic columns"
      pattern: "classify_game_flaws"
---

<objective>
Close the phase: run the final held-out TEST-split gate (D-11) confirming every shipped in-scope
motif clears its bar (and every remaining miss is a deliberately suppressed motif), regenerate
the full tactic-tagger report, and re-backfill the corrected detector over the existing tagged
games on dev (D-12) — which doubles as the real-data validation beyond the CC0 fixtures. Prod
re-backfill is a documented runbook step OUTSIDE this phase gate; NO dev DB reset (D-12).

Purpose: prove the precision lift holds on held-out data and stop the known-wrong ~131k dev tags
(live in the Phase 126 comparison UI) from rendering. Output: green TEST gate + refreshed report
+ corrected dev tags.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-02-SUMMARY.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-03-SUMMARY.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final held-out TEST-split gate + full report regeneration (D-11)</name>
  <files>reports/tactic-tagger/tactic-tagger-2026-06-22.md</files>
  <read_first>
    - scripts/tactic_tagger_report.py (--check-goals --eval-set test; the report writer; the GOALS table raised in plan 01)
    - tests/scripts/tagger/precision_floors.py (final SUPPRESSED_MOTIFS + PRECISION_FLOOR after plans 02/03)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md (D-11 — judge on TEST + ΔP, never TRAIN; ship a motif only if >0.9 on TEST, else suppress)
  </read_first>
  <action>
    Run `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test`. The gate passes when every NON-suppressed in-scope motif meets its 0.90 precision GOAL on the TEST split; any motif still below is acceptable ONLY if it is in SUPPRESSED_MOTIFS (D-02/D-11) — confirm there is no shipped motif below 0.90. Also run the full CI floor gate `uv run pytest tests/scripts/tagger/test_detector_precision.py`. Regenerate the dated full report (the script writes `reports/tactic-tagger/tactic-tagger-2026-06-22.md`). Run the full backend suite `uv run pytest -n auto -x` and `uv run ty check app/ tests/` to confirm no cross-cutting regression from the detector + flaws_service changes. Record the final TEST precision table + ΔP and the list of suppressed motifs in the SUMMARY.
  </action>
  <verify>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py</automated>
    <automated>uv run pytest -n auto -x</automated>
    <automated>uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `--check-goals --eval-set test` exits 0, OR every remaining unmet motif is confirmed in SUPPRESSED_MOTIFS (no shipped in-scope motif below 0.90 on TEST).
    - `uv run pytest tests/scripts/tagger/test_detector_precision.py` passes.
    - `uv run pytest -n auto -x` passes; `uv run ty check app/ tests/` exits 0.
    - The dated report `reports/tactic-tagger/tactic-tagger-2026-06-22.md` is regenerated and committed.
    - SUMMARY records final TEST precision + ΔP per motif and the suppressed list.
  </acceptance_criteria>
  <done>Held-out TEST gate green (shipped motifs ≥0.90, misses are suppressed); full suite + ty clean; full report regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Dev re-backfill of game_flaws tactic columns (D-12) — real-data validation</name>
  <files>scripts/backfill_flaws.py</files>
  <read_first>
    - scripts/backfill_flaws.py (lines 71-99 args: --db, --user-id, --dry-run; the classify_game_flaws call ~210; the EVAL_COVERAGE_MIN gate)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md (D-12 — dev re-backfill in-phase, prod deferred to runbook, NO dev DB reset)
    - CLAUDE.md (dev DB: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d` must be running; "No dev DB reset in plans")
  </read_first>
  <action>
    Confirm the dev DB is running (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`). Run a dry-run first: `uv run python scripts/backfill_flaws.py --db dev --dry-run` to confirm the corrected detector runs cleanly over the existing tagged games (NO dev DB reset — the backfill runs against the existing dev data, D-12). Then run the real backfill `uv run python scripts/backfill_flaws.py --db dev` to recompute `game_flaws` tactic columns (tactic_motif/tactic_piece/tactic_confidence/tactic_depth) with the aligned detector. This is also the real-data validation of the fix beyond the CC0 fixtures — spot-check a sample of corrected missed-side hanging-piece/fork tags (the D-04 manual prod-spot-check analog on dev). Record the before/after tag-count delta (how many tags changed/dropped) in the SUMMARY. Prod re-backfill is explicitly OUT of scope (runbook step, D-12) — do NOT touch prod.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_flaws.py --db dev --dry-run</automated>
    <human-check>After the real `--db dev` backfill, spot-check a sample of recomputed missed-side hanging-piece/fork tags in dev (via the flawchess-db MCP or a quick query) and confirm previously-wrong chips are corrected/dropped. Record the tag-count delta.</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/backfill_flaws.py --db dev --dry-run` completes without error (corrected detector runs over existing dev data).
    - The real `--db dev` backfill completes; the SUMMARY records the before/after tag-count delta and a spot-check of corrected tags.
    - No dev DB reset performed; prod untouched (D-12).
  </acceptance_criteria>
  <done>Dev game_flaws tactic columns recomputed with the aligned detector; tag delta recorded; spot-check confirms corrected tags; prod deferred to runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The re-backfill writes to the DEV database via the existing SQLAlchemy async path
(classify_game_flaws → flaw_record_to_row), the same path used by the live drain. No new write
surface, no new endpoint, no prod access. The report writer writes a local markdown file.

| Boundary | Description |
|----------|-------------|
| backfill script → dev DB | existing SQLAlchemy write path; dev-only; no new schema or write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-07 | Tampering (data-integrity) | dev re-backfill overwrites game_flaws tactic columns | mitigate | Dry-run first; dev-only (D-12, prod deferred); before/after tag delta recorded; the corrected detector is the validated artifact from plans 01-04. |
| T-131-08 | Repudiation / scope | accidental prod re-backfill | accept | D-12 explicitly defers prod to a runbook; the task uses `--db dev` only and the autonomous=false gate keeps a human in the loop for the write step. |
| T-131-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. No install tasks → no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test` — gate green (shipped ≥0.90, misses suppressed).
- `uv run pytest tests/scripts/tagger/test_detector_precision.py` passes.
- `uv run pytest -n auto -x` passes; `uv run ty check app/ tests/` exits 0.
- `uv run python scripts/backfill_flaws.py --db dev --dry-run` completes; real `--db dev` backfill completes with recorded tag delta.
- Dated report regenerated.
</verification>

<success_criteria>
- Held-out TEST gate green: every shipped in-scope motif ≥0.90; every miss is suppressed (D-11).
- Full backend suite + ty clean.
- Dev game_flaws tactic columns recomputed (no dev DB reset, prod deferred — D-12).
- Full report regenerated and committed.
</success_criteria>

<output>
Create `.planning/phases/131-tactic-precision-hardening-cook-alignment/131-05-SUMMARY.md` when done
</output>
