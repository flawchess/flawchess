---
phase: 131-tactic-precision-hardening-cook-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 131-01
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/precision_floors.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "skewer fires on cook's exact relational predicate (op.from in between + is_in_bad_spot), not the old piece-behind heuristic (D-09)."
    - "discovered-attack requires an earlier pov move to have vacated a between-square and short-circuits on recapture (D-09)."
    - "fork prunes when the forker lands in a bad spot, skips pawn victims, and applies the hanging-victim not-an-attacker clause (D-09)."
    - "pin fires via the full two-sub-test port (pin_prevents_attack + pin_prevents_escape) (D-09)."
    - "Any of the four motifs still below 0.90 TEST precision at full cook fidelity is SUPPRESSED, not shipped (D-02/D-11)."
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "Rebuilt detect_skewer, detect_discovered_attack, detect_fork, detect_pin per cook predicates"
      contains: "def detect_skewer"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Updated PRECISION_FLOOR / SUPPRESSED_MOTIFS for any motif that stays <0.9 on TEST"
  key_links:
    - from: "detect_fork / detect_skewer"
      to: "_is_in_bad_spot / _is_defended (plan 01)"
      via: "forker-safety prune + ray-aware hanging + skewer is_in_bad_spot accept"
      pattern: "_is_in_bad_spot|_is_defended"
---

<objective>
Rebuild the four lowest-precision firing geometrics — skewer (0.15), discovered-attack (0.17),
fork (0.40), pin (0.44) — against cook's exact predicates per the cook-alignment note (D-02/D-09).
These four are 82.6% of all tag volume at confidence 100; volume-weighted held-out precision
across the top five is ~0.31 today (roughly two of three visible chips are wrong). This plan is
the bulk of the precision lift.

Attempt the FULL cook port for each (D-02 — the port is the precision ceiling; measure the real
ceiling before suppressing). Any motif still <0.90 on the held-out TEST split at full cook
fidelity is SUPPRESSED via the precision-floor/query lever (D-11) — explicitly endorsed for
skewer/discovered-attack which may plateau below 0.9.

Purpose: stop rendering false geometric chips. Output: four rebuilt detectors + suppression
decisions recorded against measured TEST precision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md
@.planning/notes/tactic-tagger-cook-alignment.md
@.planning/phases/131-tactic-precision-hardening-cook-alignment/131-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild detect_skewer and detect_discovered_attack against cook predicates</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (detect_skewer ~482-542; detect_discovered_attack ~606-648; detect_discovered_check ~566-603 — MUST NOT regress; the new `_is_in_bad_spot` from plan 01)
    - .planning/notes/tactic-tagger-cook-alignment.md (skewer + discovered-attack + discovered-check sections; the cook↔ours index table)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md ("skewer", "discovered-attack", "discovered-check" divergence audits; Pitfall 1; "The cook index → our index translation"; "Don't Hand-Roll" SquareSet.between)
  </read_first>
  <action>
    Rebuild `detect_skewer` to cook's relational predicate (D-10 — from the note's prose, NO source copy): scan pov moves from the 2nd (`range(2, len(moves), 2)`); fire only for a capture with a ray piece (Q/R/B) that is not checkmate; compute `between = chess.SquareSet.between(move.from_square, move.to_square)`; let `op = moves[k-1]` (prior opponent move); require `op.to_square != capture_square` AND `op.from_square in between`; require `_PIECE_VALUES[moved_op_piece_type] > _PIECE_VALUES[captured_piece_type]` (king-aware) AND `_is_in_bad_spot(board_before_capture, capture_square)`. Rebuild `detect_discovered_attack` sub-case 2 to cook: scan `range(2, len(moves), 2)`; for a pov capture compute `between`; if `op.to_square == capture_square` short-circuit to not-fired (recapture); let `prev = moves[k-2]`; require `prev.from_square in between` AND `capture_sq != prev.to_square` AND `move.from_square != prev.to_square` AND `prev` not castling. Per Pitfall 1, the loop MUST start at index 2 so discovered-check (depth-0 split, D-03) is NOT re-claimed — verify discovered-check TEST precision holds ≥0.85 after the port. Preserve the existing depth convention `max(0, k-1)` per Pitfall 6 / the SEED-057 depth fix. Replace any `_is_hanging` calls in these two detectors with the ray-aware `not _is_defended(...)`.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
  </verify>
  <acceptance_criteria>
    - skewer and discovered-attack each use `chess.SquareSet.between` and the `op.from_square in between` / recapture short-circuit (grep confirms the between geometry).
    - The harness TEST report shows skewer and discovered-attack precision measured at full cook fidelity; discovered-check TEST precision remains ≥0.85 (Pitfall 1 regression guard).
    - `uv run ty check app/ tests/` exits 0.
    - No cook.py source pasted (D-10).
  </acceptance_criteria>
  <done>skewer + discovered-attack rebuilt to cook relational predicates; discovered-check not regressed; ray-aware hanging used; TEST precision measured.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild detect_fork and detect_pin against cook predicates</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (detect_fork 293-346 — note Task 1 of plan 01 already rewired the victim hanging check; detect_pin ~433-479; `board.pin` usage)
    - .planning/notes/tactic-tagger-cook-alignment.md (fork + pin sections — the five fork gaps and the two pin sub-tests)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md ("fork", "pin" divergence audits; Pitfall 4 king vs no-king values; Pitfall 6 depth; Pitfall 7 board.pin convention; "Don't Hand-Roll" board.pin)
  </read_first>
  <action>
    Rebuild `detect_fork` to cook (D-10, from prose): scan pov moves EXCEPT the last (`range(0, len(moves) - 2, 2)` per the index table — but preserve the Phase-124 odd-length-PV fix only if it does not contradict cook's `[:-1]`; align with cook's exclude-last per the note); moved piece not a king; PRUNE if `_is_in_bad_spot(board_after, to_square)` (forker loose); count victims among `board_after.attacks(to_square)` SKIPPING pawn victims; victim qualifies if `_PIECE_VALUES[victim] > _PIECE_VALUES[forker]` (KING:99 already flows) OR (`not _is_defended(board_after, victim, sq)` AND `sq not in board_after.attackers(not pov, to_square)` — the hanging victim is not itself an attacker of the fork square); fire if victims ≥ 2. Rebuild `detect_pin` to cook's two-sub-test port: `pin_prevents_attack` (an opponent piece pinned via `board.pin(piece.color, sq) != chess.BB_ALL` attacks a pov piece on a square OUTSIDE the pin direction, where the pov piece is worth more OR hanging) and `pin_prevents_escape` (a pov attacker INSIDE the pin line attacks the pinned piece; fire if pinned worth more than attacker OR pinned hanging AND has no legal step off the pin line). Per Pitfall 7, `board.pin(color, sq)` is called with the PINNED piece's color/square and returns `BB_ALL` when not pinned. Per Pitfall 6, depth = `max(0, k-1)` on the board index where the pin fires. Per Pitfall 4, fork victim comparison uses `_PIECE_VALUES` (king-aware); `_is_in_bad_spot` already uses `_VALUES_NO_KING` internally. Keep the SEED-057 reject-before-accept ordering in pin.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_tactic_detector.py -x</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
  </verify>
  <acceptance_criteria>
    - detect_fork applies the `_is_in_bad_spot` forker prune, skips pawn victims, and has the hanging-victim not-an-attacker clause (grep + report show the prune + the precision move).
    - detect_pin implements both `pin_prevents_attack` and `pin_prevents_escape` sub-tests using `board.pin`.
    - The harness TEST report shows fork and pin precision at full cook fidelity; fork recall does not collapse (Pitfall 2 watch — flag if it drops well below the prior 0.78).
    - `uv run ty check app/ tests/` exits 0.
  </acceptance_criteria>
  <done>fork + pin rebuilt to cook predicates with the forker-safety prune and the two pin sub-tests; TEST precision measured; recall not collapsed.</done>
</task>

<task type="auto">
  <name>Task 3: Decide ship/suppress per motif on TEST precision; update PRECISION_FLOOR / SUPPRESSED_MOTIFS</name>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <read_first>
    - tests/scripts/tagger/precision_floors.py (lines 100-175 SUPPRESSED_MOTIFS + PRECISION_FLOOR; the suppression note for skewer)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-RESEARCH.md ("Suppression mechanism (Claude's Discretion)" — SUPPRESSED_MOTIFS + _TACTIC_CHIP_CONFIDENCE_MIN levers)
    - .planning/phases/131-tactic-precision-hardening-cook-alignment/131-CONTEXT.md (D-02, D-11, and Claude's-Discretion on the suppression mechanism)
  </read_first>
  <action>
    Read the held-out TEST precision for fork, skewer, pin, discovered-attack from the harness report produced in Tasks 1-2. For each motif that clears >0.90 on TEST: RAISE its `PRECISION_FLOOR` to ~5-8pp below the new measured TEST value (the never-regress lock). For each motif still <0.90 at full cook fidelity (D-02 endorses skewer/discovered-attack possibly plateauing): SUPPRESS it — add the motif to `SUPPRESSED_MOTIFS` in precision_floors.py and remove it from `PRECISION_FLOOR` (the detector code STAYS so the harness can keep measuring it; only the CI gate drops). For UI suppression of a Tier-2 confidence-100 motif, reuse the existing `tactic_confidence` query lever / FAMILY_TO_MOTIF_INTS path in library_repository.py per the note (Claude's Discretion — confirm the chosen mechanism in the SUMMARY). Record the per-motif ship/suppress decision and the measured TEST precision for each of the four in the SUMMARY (D-11 audit trail). Suppressing skewer alone removes ~16% of all tags — note the volume impact if suppressed.
  </action>
  <verify>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test</automated>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py</automated>
  </verify>
  <acceptance_criteria>
    - Every shipped (non-suppressed) in-scope geometric has TEST precision >0.90 in the harness report; every motif <0.90 is in SUPPRESSED_MOTIFS and absent from PRECISION_FLOOR.
    - `uv run pytest tests/scripts/tagger/test_detector_precision.py` passes (floors consistent with measured TRAIN; no shipped motif gated below its measured value).
    - The SUMMARY records the ship/suppress decision + measured TEST precision for fork, skewer, pin, discovered-attack (D-11 trail).
  </acceptance_criteria>
  <done>Each of the four geometrics is either shipped (>0.9 TEST, floor locked) or suppressed (SUPPRESSED_MOTIFS, floor dropped); decisions recorded with measured TEST precision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as plan 01: pure-CPU detector edits, no new endpoints/input/auth/network. The detectors
read already-stored PV data. No new trust boundary.

| Boundary | Description |
|----------|-------------|
| stored PV → detector | already-validated data read at classify time; no new ingress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-03 | Tampering (data-integrity, NOT security) | rebuilt geometric detectors (wrong visible chip) | mitigate | The >0.90 TEST precision gate + suppression of any motif that misses it (D-02/D-11). A wrong chip is a correctness defect; the gate IS the control. |
| T-131-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. No install tasks → no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `uv run ty check app/ tests/` exits 0.
- `uv run pytest tests/services/test_tactic_detector.py -x` passes.
- `uv run pytest tests/scripts/tagger/test_detector_precision.py` passes.
- `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test` — shipped geometrics at >0.90 TEST; suppressed ones removed from the floor gate.
- discovered-check TEST precision ≥0.85 (Pitfall 1 regression guard).
- No cook.py source pasted (D-10).
</verification>

<success_criteria>
- skewer, discovered-attack, fork, pin rebuilt to cook predicates at full fidelity.
- Each is shipped (>0.9 TEST, floor locked) or suppressed (SUPPRESSED_MOTIFS).
- discovered-check not regressed; fork recall not collapsed.
- Ship/suppress decisions recorded with measured TEST precision.
</success_criteria>

<output>
Create `.planning/phases/131-tactic-precision-hardening-cook-alignment/131-02-SUMMARY.md` when done
</output>
