---
phase: 08-rework-games-and-bookmark-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/xxxx_rename_bookmarks_to_position_bookmarks.py
  - app/models/position_bookmark.py
  - app/models/__init__.py
  - app/repositories/position_bookmark_repository.py
  - app/schemas/position_bookmarks.py
  - app/routers/position_bookmarks.py
  - app/main.py
  - tests/test_bookmark_repository.py
  - tests/test_bookmark_schema.py
autonomous: true
requirements: [REWORK-01]

must_haves:
  truths:
    - "All bookmark CRUD operations work at /position-bookmarks API paths"
    - "Database table is named position_bookmarks with correct index"
    - "Existing bookmark data is preserved after migration (rename, not drop+create)"
  artifacts:
    - path: "alembic/versions/xxxx_rename_bookmarks_to_position_bookmarks.py"
      provides: "Table rename migration"
      contains: "op.rename_table"
    - path: "app/models/position_bookmark.py"
      provides: "PositionBookmark ORM model"
      contains: "position_bookmarks"
    - path: "app/repositories/position_bookmark_repository.py"
      provides: "PositionBookmark repository"
      exports: ["PositionBookmarkRepository"]
    - path: "app/routers/position_bookmarks.py"
      provides: "Position bookmarks router"
      contains: "/position-bookmarks"
    - path: "app/schemas/position_bookmarks.py"
      provides: "Pydantic schemas"
      exports: ["PositionBookmarkCreate", "PositionBookmarkUpdate", "PositionBookmarkResponse"]
  key_links:
    - from: "app/main.py"
      to: "app/routers/position_bookmarks.py"
      via: "include_router"
      pattern: "position_bookmarks\\.router"
    - from: "app/routers/position_bookmarks.py"
      to: "app/repositories/position_bookmark_repository.py"
      via: "repository import"
      pattern: "PositionBookmarkRepository"
---

<objective>
Rename `bookmarks` to `position_bookmarks` across the entire backend: Alembic migration to rename the DB table and index, rename model/repository/schemas/router files and all class/function names, update main.py router include, and update existing tests.

Purpose: First half of the full-stack rename per user decision. Backend must be done first so the API paths change before frontend updates.
Output: All backend files renamed, migration created, tests passing at new paths.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rework-games-and-bookmark-tabs-position-filter-section-position-bookmarks-section-rename-bookmarks-to-position-bookmarks/08-CONTEXT.md
@.planning/phases/08-rework-games-and-bookmark-tabs-position-filter-section-position-bookmarks-section-rename-bookmarks-to-position-bookmarks/08-RESEARCH.md

<interfaces>
<!-- Existing backend files to rename. All internal references must be updated. -->

From app/models/bookmark.py:
```python
class Bookmark(Base):
    __tablename__ = "bookmarks"
```

From app/repositories/bookmark_repository.py:
```python
class BookmarkRepository:
    # CRUD methods using Bookmark model
```

From app/schemas/bookmarks.py:
```python
class BookmarkCreate(BaseModel): ...
class BookmarkUpdate(BaseModel): ...
class BookmarkResponse(BaseModel): ...
class BookmarkReorderRequest(BaseModel): ...
```

From app/routers/bookmarks.py:
```python
router = APIRouter(prefix="/bookmarks", tags=["bookmarks"])
# PUT /bookmarks/reorder MUST be defined BEFORE /{bookmark_id}
```

From app/main.py:
```python
from app.routers import analysis, bookmarks, imports, auth
app.include_router(bookmarks.router)
```

From app/routers/analysis.py:
```python
# May import bookmark schemas for time-series — check and update if needed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration and rename backend model/repository/schemas/router files</name>
  <files>
    alembic/versions/xxxx_rename_bookmarks_to_position_bookmarks.py,
    app/models/position_bookmark.py,
    app/models/bookmark.py (DELETE),
    app/repositories/position_bookmark_repository.py,
    app/repositories/bookmark_repository.py (DELETE),
    app/schemas/position_bookmarks.py,
    app/schemas/bookmarks.py (DELETE),
    app/routers/position_bookmarks.py,
    app/routers/bookmarks.py (DELETE),
    app/main.py
  </files>
  <action>
    1. Create manual Alembic migration (do NOT use --autogenerate):
       - `op.rename_table('bookmarks', 'position_bookmarks')`
       - `op.execute('ALTER INDEX ix_bookmarks_user_id RENAME TO ix_position_bookmarks_user_id')`
       - Downgrade reverses both operations in opposite order
       - Use `uv run alembic revision -m "rename bookmarks to position_bookmarks"` to generate the file, then replace the body

    2. Rename model file `app/models/bookmark.py` -> `app/models/position_bookmark.py`:
       - Class `Bookmark` -> `PositionBookmark`
       - `__tablename__ = "position_bookmarks"`
       - Update `app/models/__init__.py` if it re-exports the model

    3. Rename repository `app/repositories/bookmark_repository.py` -> `app/repositories/position_bookmark_repository.py`:
       - Class `BookmarkRepository` -> `PositionBookmarkRepository`
       - All internal references to `Bookmark` model -> `PositionBookmark`
       - Import from `app.models.position_bookmark`

    4. Rename schemas `app/schemas/bookmarks.py` -> `app/schemas/position_bookmarks.py`:
       - `BookmarkCreate` -> `PositionBookmarkCreate`
       - `BookmarkUpdate` -> `PositionBookmarkUpdate`
       - `BookmarkResponse` -> `PositionBookmarkResponse`
       - `BookmarkReorderRequest` -> `PositionBookmarkReorderRequest`

    5. Rename router `app/routers/bookmarks.py` -> `app/routers/position_bookmarks.py`:
       - `prefix="/position-bookmarks"` (hyphenated URL)
       - `tags=["position-bookmarks"]`
       - CRITICAL: Keep `reorder` route defined BEFORE `{bookmark_id}` route (FastAPI first-match)
       - Update all schema/repository imports to renamed versions

    6. Update `app/main.py`:
       - `from app.routers import analysis, position_bookmarks, imports, auth`
       - `app.include_router(position_bookmarks.router)`

    7. Check `app/routers/analysis.py` for any imports from bookmark schemas — update if found.

    8. Delete the old files: `bookmark.py` (model), `bookmark_repository.py`, `bookmarks.py` (schemas), `bookmarks.py` (router).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics && uv run alembic upgrade head && uv run python -c "from app.models.position_bookmark import PositionBookmark; from app.schemas.position_bookmarks import PositionBookmarkCreate, PositionBookmarkResponse; from app.repositories.position_bookmark_repository import PositionBookmarkRepository; print('All imports OK')"</automated>
  </verify>
  <done>Old bookmark files deleted. New position_bookmark files exist with all classes renamed. Alembic migration renames table and index. All imports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing backend tests for renamed modules</name>
  <files>
    tests/test_bookmark_repository.py,
    tests/test_bookmark_schema.py
  </files>
  <action>
    1. Update `tests/test_bookmark_repository.py`:
       - Change all imports from `app.models.bookmark` to `app.models.position_bookmark`
       - Change all imports from `app.repositories.bookmark_repository` to `app.repositories.position_bookmark_repository`
       - Change all imports from `app.schemas.bookmarks` to `app.schemas.position_bookmarks`
       - Rename all class/type references: `Bookmark` -> `PositionBookmark`, `BookmarkCreate` -> `PositionBookmarkCreate`, etc.
       - Do NOT rename the test file itself (keeps git history cleaner)

    2. Update `tests/test_bookmark_schema.py`:
       - Same import and reference updates as above

    3. Run full test suite to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics && uv run pytest -x</automated>
  </verify>
  <done>All tests pass with renamed imports. No references to old `Bookmark`/`bookmark` module names remain in test files.</done>
</task>

</tasks>

<verification>
- `uv run alembic upgrade head` succeeds (table renamed in DB)
- `uv run pytest -x` passes (all tests work with new names)
- `grep -r "from app.models.bookmark " app/` returns no results (old imports gone)
- `grep -r "from app.schemas.bookmarks " app/` returns no results
- `grep -r "from app.repositories.bookmark_repository " app/` returns no results
</verification>

<success_criteria>
- Database table is `position_bookmarks` with index `ix_position_bookmarks_user_id`
- All backend CRUD endpoints respond at `/position-bookmarks` paths
- All tests pass
- No references to old `bookmarks` module names in app/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/08-rework-games-and-bookmark-tabs-position-filter-section-position-bookmarks-section-rename-bookmarks-to-position-bookmarks/08-01-SUMMARY.md`
</output>
