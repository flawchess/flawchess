---
phase: 99.1-move-cohort-cdf-out-of-source-into-a-db-table
plan: 04
type: execute
wave: 3
depends_on:
  - 99.1-01
  - 99.1-02
  - 99.1-03
files_modified:
  - app/services/global_percentile_cdf.py
  - app/services/user_benchmark_percentiles_service.py
autonomous: false
requirements: []   # standalone internal refactor — no ROADMAP requirement IDs; coverage via phase Success Criteria + CONTEXT D-03/D-04
user_setup: []

must_haves:
  truths:
    - "global_percentile_cdf.py no longer contains the generated registry; ~250 lines; BEGIN/END sentinels gone"
    - "interpolate_cohort_percentile stays SYNC and takes a prefetched CdfTable (no session, no async) — D-04"
    - "compute_stage_a and compute_stage_b issue one batched load_cohort_cells query per import (D-03) and resolve from the in-memory dict"
    - "Chip / percentile output is byte-for-byte equivalent to pre-refactor for a representative user (SC#6)"
    - "Backend + frontend gates pass (ruff, ty, pytest, frontend lint/tests)"
  artifacts:
    - path: "app/services/global_percentile_cdf.py"
      provides: "pure sync module: CdfMetricId, CdfTable, breakpoint constants, _interpolate_with_table, _round_anchor_to_grid, sync interpolate_cohort_percentile"
      min_lines: 150
    - path: "app/services/user_benchmark_percentiles_service.py"
      provides: "compute_stage_a/compute_stage_b wired to the batched prefetch + sync interpolator"
      contains: "load_cohort_cells"
  key_links:
    - from: "app/services/user_benchmark_percentiles_service.py"
      to: "app.repositories.benchmark_cohort_cdf_repository.load_cohort_cells"
      via: "one prefetch call per import before the (family, tc) loop"
      pattern: "load_cohort_cells"
    - from: "app/services/user_benchmark_percentiles_service.py"
      to: "app.services.global_percentile_cdf.interpolate_cohort_percentile"
      via: "sync call with cohort.get((metric, rounded_anchor, tc)) prefetched table"
      pattern: "interpolate_cohort_percentile"
---

<objective>
The riskiest edit: shrink `app/services/global_percentile_cdf.py` from ~130k lines to ~250
by deleting the generated `COHORT_PERCENTILE_CDF` registry and BEGIN/END sentinels while
keeping ALL logic, and make `interpolate_cohort_percentile` take a PREFETCHED CdfTable and
stay SYNC (D-04, supersedes ROADMAP SC#3's "becomes async" wording). Wire
`compute_stage_a` / `compute_stage_b` to call `load_cohort_cells` ONCE per import (D-03
batched prefetch) and resolve the ~32 per-import lookups from the in-memory dict. Prove the
percentile output is byte-for-byte equivalent to pre-refactor (SC#6) against the EXISTING
seeded dev DB.

Purpose: complete the relocation; the module becomes pure logic, data lives in the DB.
Output: shrunk pure module, rewired background callers, byte-for-byte parity evidence.

ORDERING SAFETY: this plan runs LAST (wave 3). The dev DB is already seeded (Plan 03), so
the new DB-backed lookup returns real CdfTables and chips render unchanged the moment the
registry is removed. Removing the registry before the table is seeded would break the
lookup mid-phase — Plan 03's seeding is a hard dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-CONTEXT.md
@.planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-refactor byte-for-byte baseline (before any edit)</name>
  <files>(no source files modified — produces a baseline artifact under /tmp)</files>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py lines 135-160 (STAGE_A_METRIC, STAGE_B_METRIC_FAMILIES), 394-460 (compute_stage_a), 462-540 (compute_stage_b)
    - app/services/global_percentile_cdf.py lines 130318-130362 (the CURRENT _round_anchor_to_grid + interpolate_cohort_percentile signature — registry-reading body, still present pre-edit)
    - .planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md (section "app/services/user_benchmark_percentiles_service.py")
  </read_first>
  <action>
    BEFORE touching any source, capture a baseline so SC#6 is provable. Pick a representative
    dev user that has anchors (query `user_rating_anchors` for a user_id with multiple TC
    rows). Write a throwaway script (run via `uv run python -c` or a /tmp file) that, for that
    user, recomputes the percentile output by calling the CURRENT
    `interpolate_cohort_percentile(metric, value, anchor.anchor_rating, tc)` over the same
    `(STAGE_A_METRIC + STAGE_B_METRIC_FAMILIES) x anchor-TC` grid that compute_stage_a/b
    iterate — feeding the SAME `value` inputs (read the per-(metric, tc) score-gap/rate values
    the stages would compute, or simpler: directly enumerate the in-source registry cells the
    user resolves to and record `(metric, rounded_anchor, tc) -> interpolate result`). Save
    the resulting `{(metric, anchor, tc): percentile}` mapping to
    `/tmp/cdf_baseline_pre.json` (sorted keys, fixed float formatting). ALTERNATIVELY and
    preferably: run the actual `compute_stage_a` + `compute_stage_b` for that user against the
    current code, then snapshot the written `user_benchmark_percentiles` rows
    `(metric, time_control_bucket, percentile)` to `/tmp/cdf_baseline_pre.json`. Document in
    the SUMMARY which user_id and which method was used. This artifact is the acceptance
    oracle for Task 4.
  </action>
  <verify>
    <automated>test -f /tmp/cdf_baseline_pre.json && uv run python -c "import json; d=json.load(open('/tmp/cdf_baseline_pre.json')); assert len(d)>0, 'empty baseline'; print('BASELINE OK entries=', len(d))"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/cdf_baseline_pre.json` exists and is non-empty
    - The baseline covers multiple (metric, tc) cells for the chosen representative user (entries > 0, ideally >= 8)
    - The SUMMARY records the chosen user_id and capture method
  </acceptance_criteria>
  <done>A pre-refactor percentile baseline is captured against the current registry-backed
  code; it is the oracle for the post-refactor byte-for-byte check.</done>
</task>

<task type="auto">
  <name>Task 2: Shrink global_percentile_cdf.py — delete registry, make interpolator sync-with-prefetched-table</name>
  <files>app/services/global_percentile_cdf.py</files>
  <read_first>
    - app/services/global_percentile_cdf.py lines 40-148 (docstring purity invariant, imports incl. `from collections.abc import Mapping`, TimeControlBucket Literal, breakpoint/audit constants, CdfMetricId, CdfTable, placeholder-registry comment)
    - app/services/global_percentile_cdf.py line 150-151 and 130270 (the COHORT_PERCENTILE_CDF literal + BEGIN/END sentinels to delete)
    - app/services/global_percentile_cdf.py lines 130278-130362 (KEEP _interpolate_with_table + _round_anchor_to_grid UNCHANGED; CHANGE interpolate_cohort_percentile)
    - .planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md (section "app/services/global_percentile_cdf.py" — exact KEEP/REMOVE/CHANGE list)
  </read_first>
  <action>
    KEEP byte-for-byte: the module docstring + purity invariant ("no DB or I/O ... synchronous
    and side-effect free"), the inline `TimeControlBucket` Literal (circular-import note), all
    breakpoint/audit constants (BENCHMARK_DB_SNAPSHOT_MONTH, COHORT_ANCHOR_STEP_ELO,
    BREAKPOINT_LABELS, BREAKPOINT_PERCENTILES, _LEFT_TAIL_CLAMP, _RIGHT_TAIL_CLAMP), the
    `CdfMetricId` Literal (single source of truth, imported repo-wide), the `CdfTable`
    dataclass, `_interpolate_with_table`, and `_round_anchor_to_grid`.
    REMOVE: the `from collections.abc import Mapping` import (only the registry type used it),
    the `# --- BEGIN GENERATED REGISTRY ---` / `# --- END GENERATED REGISTRY ---` sentinels
    and the ENTIRE `COHORT_PERCENTILE_CDF` literal (lines 150..130270), and the
    placeholder-registry comment block above it.
    CHANGE `interpolate_cohort_percentile` to the SYNC prefetched-table shape (D-04 — do NOT
    add a session, do NOT make it async):
    `def interpolate_cohort_percentile(value: float, table: CdfTable | None) -> float | None:`
    returning `None` if `table is None` else `_interpolate_with_table(table, value)`. The
    `_round_anchor_to_grid` snapping moves to the CALLER (Plan 04 Task 3) when building the
    cohort lookup key — keep `_round_anchor_to_grid` exported. Update the function's docstring
    to describe the new prefetched-table contract (remove references to reading
    COHORT_PERCENTILE_CDF). This explicitly reconciles ROADMAP SC#3 to the module-pure /
    repo-async shape per CONTEXT D-04.
  </action>
  <verify>
    <automated>uv run python -c "import inspect, app.services.global_percentile_cdf as m; assert not hasattr(m,'COHORT_PERCENTILE_CDF'); sig=inspect.signature(m.interpolate_cohort_percentile); assert list(sig.parameters)==['value','table'], list(sig.parameters); assert not inspect.iscoroutinefunction(m.interpolate_cohort_percentile), 'must stay sync'; assert hasattr(m,'_round_anchor_to_grid') and hasattr(m,'CdfTable') and hasattr(m,'CdfMetricId'); print('MODULE OK lines:', len(inspect.getsource(m).splitlines()))"</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l app/services/global_percentile_cdf.py` returns < 300
    - `grep -c 'BEGIN GENERATED' app/services/global_percentile_cdf.py` returns 0 and `grep -c 'END GENERATED'` returns 0
    - `grep -c 'COHORT_PERCENTILE_CDF' app/services/global_percentile_cdf.py` returns 0
    - The verify command prints `MODULE OK` (no COHORT_PERCENTILE_CDF attr; interpolate_cohort_percentile params == ['value','table']; not a coroutine)
    - `grep -c 'from collections.abc import Mapping' app/services/global_percentile_cdf.py` returns 0
    - `uv run ty check app/ tests/` exits 0
  </acceptance_criteria>
  <done>Module is ~250 lines of pure sync logic; registry/sentinels gone; interpolator takes
  a prefetched CdfTable and stays sync (D-04).</done>
</task>

<task type="auto">
  <name>Task 3: Wire compute_stage_a/compute_stage_b to the batched prefetch (D-03)</name>
  <files>app/services/user_benchmark_percentiles_service.py</files>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py lines 1-30 (module docstring referencing COHORT_PERCENTILE_CDF — to update), 90-160 (imports incl. interpolate_cohort_percentile, STAGE_A_METRIC, STAGE_B_METRIC_FAMILIES)
    - app/services/user_benchmark_percentiles_service.py lines 394-460 (compute_stage_a: compute_anchors_for_user, the for tc, anchor loop, interpolate call, upsert, per-cell Sentry try/except, top-level handler)
    - app/services/user_benchmark_percentiles_service.py lines 462-540 (compute_stage_b: fetch_anchors_for_user, the family x tc loop, interpolate call, upsert, Sentry isolation, CancelledError re-raise)
    - app/repositories/benchmark_cohort_cdf_repository.py (Plan 03 load_cohort_cells signature)
    - app/services/global_percentile_cdf.py (Plan 04 Task 2: new sync interpolate_cohort_percentile + _round_anchor_to_grid)
    - .planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-PATTERNS.md (section "app/services/user_benchmark_percentiles_service.py" — exact wiring steps)
  </read_first>
  <action>
    Import `from app.repositories.benchmark_cohort_cdf_repository import load_cohort_cells` and
    `from app.services.global_percentile_cdf import _round_anchor_to_grid` (alongside the now
    sync `interpolate_cohort_percentile`).
    In compute_stage_a: after `anchors = await compute_anchors_for_user(...)` and before the
    `for tc, anchor` loop, compute the rounded-anchor set and the TC set from `anchors`, then
    call ONCE `cohort = await load_cohort_cells(session, rounded_anchors, list(anchors.keys()))`
    (one query per import — D-03). Inside the loop, resolve
    `table = cohort.get((STAGE_A_METRIC, _round_anchor_to_grid(anchor.anchor_rating), tc))`
    and call the new sync `interpolate_cohort_percentile(value, table)`. Keep the upsert and
    the per-cell Sentry try/except UNCHANGED.
    In compute_stage_b: after `anchors = await fetch_anchors_for_user(...)` (and the existing
    `if not anchors` early return), call ONCE the same `load_cohort_cells(...)`. Inside the
    `for family in STAGE_B_METRIC_FAMILIES: for tc, anchor in ...` loop, resolve
    `table = cohort.get((family, _round_anchor_to_grid(anchor.anchor_rating), tc))` and call
    `interpolate_cohort_percentile(value, table)`. Keep upsert + Sentry isolation + the
    `asyncio.CancelledError` re-raise UNCHANGED. The prefetch happens once before the loop, so
    a load failure surfaces via the existing top-level handler.
    Update the module docstring references to `COHORT_PERCENTILE_CDF` /
    `interpolate_cohort_percentile` (lines 7, 18-19, 23) to point at the DB table + repository
    loader. The output MUST stay byte-for-byte equivalent — using `_round_anchor_to_grid` +
    the CdfTable reconstructed from the seeded rows guarantees identical interpolation inputs.
    Do NOT use asyncio.gather on the session. Keep function sizes within project limits — if
    the prefetch-key derivation bloats a stage, extract a tiny `_cohort_prefetch(session,
    anchors)` helper in this module.
  </action>
  <verify>
    <automated>uv run python -c "import app.services.user_benchmark_percentiles_service as s; import inspect; src=inspect.getsource(s); assert 'load_cohort_cells' in src; assert src.count('load_cohort_cells(') >= 2 or 'def _cohort_prefetch' in src, 'prefetch not wired in both stages'; print('WIRED OK')" && uv run pytest tests/services/ -k "benchmark_percentile or stage" -q</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `WIRED OK` (load_cohort_cells called in both stages, or via a shared `_cohort_prefetch` helper)
    - `grep -c 'load_cohort_cells' app/services/user_benchmark_percentiles_service.py` returns >= 1
    - `grep -c 'asyncio.gather' app/services/user_benchmark_percentiles_service.py` returns 0
    - The existing benchmark-percentile / stage service tests pass
    - `uv run ty check app/ tests/` exits 0
  </acceptance_criteria>
  <done>Both stages prefetch once and resolve from the in-memory dict; interpolator called
  with the prefetched table; Sentry isolation + CancelledError contract preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Byte-for-byte parity check + full gate sweep (against seeded dev DB)</name>
  <files>(no source files modified — produces a post artifact and runs gates)</files>
  <read_first>
    - /tmp/cdf_baseline_pre.json (the Task 1 oracle)
    - app/services/user_benchmark_percentiles_service.py (the rewired stages from Task 3)
    - CLAUDE.md (Pre-PR checklist — the exact gate commands)
  </read_first>
  <action>
    Re-capture the SAME representative user's percentile output using the EXACT method Task 1
    used (same user_id, same grid), now against the refactored DB-backed code path, into
    `/tmp/cdf_baseline_post.json`. Diff against `/tmp/cdf_baseline_pre.json` and assert they
    are IDENTICAL (byte-for-byte / value-for-value) — this proves the pure relocation changed
    no behavior (SC#6). If they differ, STOP and investigate (likely an anchor-rounding seam,
    a percentile-ordering bug in the loader, or a precision mismatch between the TSV float
    formatting and the original literal). Then run the full Pre-PR gate sweep from CLAUDE.md:
    `uv run ruff format app/ tests/ scripts/`, `uv run ruff check app/ tests/ scripts/ --fix`,
    `uv run ty check app/ tests/`, `uv run pytest -x`, and
    `( cd frontend && npm run lint && npm test -- --run )`. All must pass. Note: frontend has
    NO change in this phase (the chart endpoint reads already-stored percentile rows), so
    frontend gates are a regression guard only.
  </action>
  <verify>
    <automated>uv run python -c "import json; pre=json.load(open('/tmp/cdf_baseline_pre.json')); post=json.load(open('/tmp/cdf_baseline_post.json')); assert pre==post, 'BYTE-FOR-BYTE MISMATCH'; print('PARITY OK entries=', len(pre))" && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/cdf_baseline_post.json` exists and equals `/tmp/cdf_baseline_pre.json` exactly (verify prints `PARITY OK`)
    - `uv run ruff format --check app/ tests/ scripts/` clean; `uv run ruff check app/ tests/ scripts/` clean
    - `uv run ty check app/ tests/` exits 0
    - `uv run pytest -x` exits 0 (all backend tests)
    - `( cd frontend && npm run lint && npm test -- --run )` exits 0
  </acceptance_criteria>
  <done>Post-refactor percentile output is byte-for-byte identical to the pre-refactor
  baseline; all backend + frontend gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification of CDF relocation parity</name>
  <action>PAUSE for human verification. This is a checkpoint, not an implementation step — present the parity result and module-shrink evidence below and wait for approval before marking the phase complete.</action>
  <what-built>The full CDF relocation: the 130k-line in-source registry is deleted,
  `global_percentile_cdf.py` is ~250 pure sync lines, data lives in the seeded
  `benchmark_cohort_cdf` table, and the background stages prefetch + resolve from the DB. The
  automated parity check (Task 4) proved percentile output is byte-for-byte identical for a
  representative dev user.</what-built>
  <how-to-verify>
    1. Confirm the parity artifact: `diff /tmp/cdf_baseline_pre.json /tmp/cdf_baseline_post.json` shows NO differences.
    2. Confirm module shrink: `wc -l app/services/global_percentile_cdf.py` is < 300.
    3. (Optional, full-stack) Run `bin/run_local.sh`, open the app for a user with imported games and anchors, and visually confirm the per-TC Conversion/Parity/Recovery percentile chips render with the SAME percentile values/colors as before this phase. NOTE: this is the only step that benefits from a fuller environment — the automated parity check already covers the value equivalence against the existing dev DB, so a fresh-DB walkthrough is OPTIONAL (HUMAN-UAT), not a gate. Do NOT run `bin/reset_db.sh`.
    4. Confirm a fresh prod DB would suppress chips until seeded (documented behavior) — no action needed, just acknowledge the runbook note in scripts/seed_cohort_cdf.py.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any chip/value discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background task → repository → DB | In-process trusted call with an AsyncSession; the only async DB access (D-04) |
| pure module ← callers | `interpolate_cohort_percentile` is pure/sync; receives a prefetched in-memory CdfTable, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.1-08 | Tampering / correctness regression | deleting the 130k-line registry could silently change percentile output | mitigate | Byte-for-byte parity gate (Task 4) diffs pre/post percentile output for a representative user against the seeded dev DB; the human checkpoint re-confirms. Ordering (wave 3, after seeding) ensures the DB path is live before the registry is removed. |
| T-99.1-09 | Information disclosure | error context in the rewired stages | mitigate | Sentry isolation preserved unchanged: variables go via `set_context`, never in the message; per-cell try/except retained; `CancelledError` re-raised (lifespan contract). No new error surface. |
| T-99.1-10 | Denial of service | unbounded prefetch query | mitigate | One bounded `load_cohort_cells` query per import over the user's ~few anchors x TCs (D-03); not on any request hot path (~32 in-memory resolves after the single load). |
| T-99.1-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; no audit checkpoint required. |
</threat_model>

<verification>
- Task 1 baseline captured BEFORE edits; Task 4 post snapshot equals it exactly (SC#6).
- `wc -l app/services/global_percentile_cdf.py` < 300; sentinels + registry gone (SC#1).
- `interpolate_cohort_percentile` sync, params == ['value', 'table'] (D-04, SC#3 reconciled).
- Both stages call `load_cohort_cells` once per import (D-03).
- Full Pre-PR gate sweep green (SC#6): ruff format/check, ty, pytest, frontend lint/tests.
</verification>

<success_criteria>
- Module shrunk to ~250 pure sync lines; registry + BEGIN/END sentinels removed (SC#1).
- Lookup path updated; both background callers work; interpolator sync + prefetched (SC#3, D-04).
- Batched prefetch wired (D-03).
- Percentile output byte-for-byte equivalent to pre-refactor against the existing dev DB (SC#6).
- All backend + frontend gates pass (SC#6).
</success_criteria>

<output>
Create `.planning/phases/99.1-move-cohort-cdf-out-of-source-into-a-db-table/99.1-04-SUMMARY.md` when done.
</output>
