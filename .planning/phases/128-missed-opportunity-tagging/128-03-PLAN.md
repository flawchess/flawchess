---
phase: 128-missed-opportunity-tagging
plan: 03
type: execute
wave: 2
depends_on:
  - 128-01
files_modified:
  - app/repositories/query_utils.py
  - app/repositories/library_repository.py
  - app/services/library_service.py
  - app/schemas/library.py
  - app/services/eval_drain.py
  - tests/repositories/test_query_utils.py
  - tests/repositories/test_library_repository.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "An orientation dimension (Literal[missed, allowed]) selects the matching column set at BOTH filter sites; unspecified defaults to allowed, preserving current Library behavior (SC#5, D-07, D-08, D-09)"
    - "FAMILY_TO_MOTIF_INTS and _TACTIC_CHIP_CONFIDENCE_MIN are reused for both orientations unchanged (D-09)"
    - "The flaw + comparison/chip Pydantic schemas expose both orientation column sets, orientation-labeled, so Phase 129 can apply the is_opponent_expr matrix (SC#5, D-07, D-10)"
    - "Every bare tactic_* column reference in app/ is renamed to allowed_tactic_* (no stale references remain) (D-02 ripple)"
  artifacts:
    - path: "app/repositories/query_utils.py"
      provides: "orientation-aware tactic_families filter in apply_game_filters (D-09)"
      contains: "missed_tactic_motif"
    - path: "app/repositories/library_repository.py"
      provides: "orientation-aware flaw filter clause + chip read + comparison aggregation (D-09)"
      contains: "missed_tactic_motif"
    - path: "app/schemas/library.py"
      provides: "both-orientation tactic fields on flaw/marker/list schemas (D-07)"
      contains: "missed_tactic_motif"
  key_links:
    - from: "app/repositories/query_utils.py"
      to: "app/repositories/library_repository.py"
      via: "FAMILY_TO_MOTIF_INTS lazy import, orientation selects the column"
      pattern: "FAMILY_TO_MOTIF_INTS"
---

<objective>
Build the orientation-aware backend filter + schema contract that Phase 129's UI binds to. Add an `orientation ∈ {missed, allowed}` dimension to BOTH tactic filter sites (`query_utils.apply_game_filters` and `library_repository`), defaulting to `allowed` when unspecified so today's Library behavior is preserved (D-08). Complete the D-02 rename ripple: every bare `tactic_*` column reference in `app/` (query/repo/service/schema/eval_drain) becomes `allowed_tactic_*`. Expose both orientation column sets on the flaw/marker/list and comparison Pydantic schemas, orientation-labeled.

Purpose: D-07 (full orientation-aware filter + schema in 128; 129 only wires UI), D-08 (unspecified ⇒ allowed), D-09 (orientation selects the matching column set at both sites; reuse FAMILY_TO_MOTIF_INTS + _TACTIC_CHIP_CONFIDENCE_MIN unchanged), D-10 (keep the schema orientation-labeled so 129 applies the column-set × is_opponent_expr matrix — no narration copy here).

Output: an `orientation` filter param threaded through apply_game_filters and the library repo/service; both column sets on the schemas; no stale `tactic_*` column reference anywhere in `app/`. NO UI, NO narration copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md
@app/repositories/query_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ripple — every bare tactic_* column ref in app/ becomes allowed_*</name>
  <files>app/repositories/query_utils.py, app/repositories/library_repository.py, app/services/library_service.py, app/services/eval_drain.py</files>
  <read_first>
    - app/repositories/query_utils.py (lines 201-227: tactic_families EXISTS clause referencing GameFlaw.tactic_motif)
    - app/repositories/library_repository.py (lines 196-202 filter clause; 472-485 chip read; 1485-1518 comparison aggregation — all reference GameFlaw.tactic_motif / GameFlaw.tactic_confidence)
    - app/services/library_service.py (lines 377-390: tactic_by_ply build reads fr.tactic_motif / fr.tactic_confidence)
    - app/services/eval_drain.py (grep for tactic_ references — confirm whether any reference the ORM column vs the FlawRecord key)
    - .planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md (Integration Points — the grep-driven full set)
  </read_first>
  <action>
    Replace every reference to the ORM column GameFlaw.tactic_motif / GameFlaw.tactic_piece / GameFlaw.tactic_confidence / GameFlaw.tactic_depth (and any flaw-row attribute access like fr.tactic_motif / flaw.tactic_confidence that reads the renamed DB column) with the allowed_tactic_* equivalents across query_utils.py, library_repository.py, library_service.py, and eval_drain.py. This is the data-source rename for the DEFAULT (allowed) orientation; Task 2 adds the orientation switch on top. Do NOT touch FlawRecord-key access in eval_drain/flaws_service that Plan 01/02 already renamed (those are TypedDict keys, not ORM columns). Run a grep to confirm no bare GameFlaw.tactic_ column reference survives anywhere under app/.
  </action>
  <verify>
    <automated>grep -rn "GameFlaw\.tactic_\|\.tactic_motif\b\|\.tactic_confidence\b\|\.tactic_depth\b\|\.tactic_piece\b" app/ --include="*.py" | grep -v "allowed_tactic\|missed_tactic" | grep -c . || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "GameFlaw\.tactic_" app/ --include="*.py"` returns no matches (every ORM column reference renamed to allowed_/missed_).
    - The chip read, the comparison aggregation count columns, and the tactic_by_ply build all reference allowed_tactic_motif / allowed_tactic_confidence.
    - `uv run ty check app/ exits 0` (a stale ORM attribute reference would surface as an unresolved-attribute error after the model rename).
  </acceptance_criteria>
  <done>No bare GameFlaw.tactic_ column reference remains in app/; the default (allowed) data source is wired everywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add orientation dimension to both filter sites + chip read</name>
  <files>app/repositories/query_utils.py, app/repositories/library_repository.py, app/services/library_service.py, tests/repositories/test_query_utils.py, tests/repositories/test_library_repository.py</files>
  <read_first>
    - app/repositories/query_utils.py (lines 88, 201-227: tactic_families param + the EXISTS clause)
    - app/repositories/library_repository.py (lines 51 _TACTIC_CHIP_CONFIDENCE_MIN, 61-98 FAMILY_TO_MOTIF_INTS, 190-204 filter clause, 470-485 chip read, 1440 tactic_confidence_min param, 1485-1518 comparison aggregation)
    - app/services/library_service.py (lines 930, 969, 1296-1360: tactic_families pass-through + get_tactic_comparison)
    - .planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md (D-07, D-08, D-09)
  </read_first>
  <behavior>
    - orientation="allowed" (and unspecified default) → the filter/read uses allowed_tactic_motif + allowed_tactic_confidence (current behavior).
    - orientation="missed" → the same filter/read logic uses missed_tactic_motif + missed_tactic_confidence; FAMILY_TO_MOTIF_INTS expansion and the _TACTIC_CHIP_CONFIDENCE_MIN (70) gate are identical (family→int map is orientation-independent).
    - Unknown family keys still yield no ints (no-op, never raw SQL) for both orientations (T-126-02 preserved).
  </behavior>
  <action>
    Define a module-level type alias TacticOrientation = Literal["missed", "allowed"] (place it in library_repository.py, the owner of FAMILY_TO_MOTIF_INTS, and import where needed; per CLAUDE.md never bare str). Add an orientation: TacticOrientation = "allowed" parameter to apply_game_filters (query_utils.py), to the library_repository filter-clause builder and chip-read function, and thread it through library_service (the tactic_families pass-through + get_tactic_comparison). Inside each site, select the column pair by orientation: allowed → (GameFlaw.allowed_tactic_motif, GameFlaw.allowed_tactic_confidence); missed → (GameFlaw.missed_tactic_motif, GameFlaw.missed_tactic_confidence). Use a small helper or a conditional that resolves the two ORM columns from orientation so the filter/read/aggregation logic is shared, not duplicated per orientation (keep functions under the CLAUDE.md nesting/LOC limits). Reuse FAMILY_TO_MOTIF_INTS and _TACTIC_CHIP_CONFIDENCE_MIN unchanged for both. The default "allowed" means all existing callers that omit orientation are byte-identical to today. Add tests: test_query_utils asserts orientation="missed" produces a clause referencing missed_tactic_motif and orientation="allowed"/default references allowed_tactic_motif; test_library_repository asserts the chip read and filter clause honor orientation.
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_query_utils.py tests/repositories/test_library_repository.py -x</automated>
  </verify>
  <acceptance_criteria>
    - apply_game_filters and the library_repository filter/chip functions accept orientation: TacticOrientation defaulting to "allowed".
    - With orientation default/"allowed", the compiled WHERE/EXISTS references allowed_tactic_motif & allowed_tactic_confidence (a test compiles the statement and asserts the column name appears).
    - With orientation="missed", the same logic references missed_tactic_motif & missed_tactic_confidence (test).
    - FAMILY_TO_MOTIF_INTS and _TACTIC_CHIP_CONFIDENCE_MIN are unchanged (no per-orientation duplicate of the family map): `grep -c "FAMILY_TO_MOTIF_INTS" app/repositories/library_repository.py` shows a single definition.
    - orientation is constrained to Literal["missed","allowed"] (TacticOrientation) — no raw column-name interpolation from caller input (T-128-05).
    - `uv run pytest tests/repositories/test_query_utils.py tests/repositories/test_library_repository.py -x` exits 0; `uv run ty check app/ tests/ exits 0`.
  </acceptance_criteria>
  <done>Both filter sites + the chip read are orientation-aware via a closed enum, defaulting to allowed; family map + confidence gate reused.</done>
</task>

<task type="auto">
  <name>Task 3: Expose both orientation column sets on the flaw + comparison schemas</name>
  <files>app/schemas/library.py, tests/repositories/test_library_repository.py</files>
  <read_first>
    - app/schemas/library.py (lines 48-63 FlawMarker; 172-176 the flaw list item; 351-374 the tactic comparison schemas)
    - app/repositories/library_repository.py (chip read at 470-485 — the producer of these schema fields)
    - .planning/notes/missed-vs-allowed-tactic-design.md (lines 42-49: the narration matrix the labeling must support)
    - .planning/phases/128-missed-opportunity-tagging/128-CONTEXT.md (D-07, D-10)
  </read_first>
  <action>
    On the flaw-card / FlawMarker / flaw-list-item Pydantic models that currently carry tactic_motif / tactic_confidence, rename those two to allowed_tactic_motif / allowed_tactic_confidence and add missed_tactic_motif / missed_tactic_confidence (str | None / int | None, default None) so both orientations are orientation-labeled. Update the chip-read producer in library_repository to populate both (allowed from the renamed columns, missed from the new missed_tactic_* columns, each gated by the same _TACTIC_CHIP_CONFIDENCE_MIN). Update the doc comments to reference the column-set × is_opponent_expr narration matrix from the design note WITHOUT writing any narration copy (that is Phase 129). Keep schema field naming orientation-explicit. Update any existing tests/repository assertions that read the old tactic_motif schema field to the new allowed_tactic_motif name and add an assertion that missed_tactic_motif is present on the schema. Note: the comparison schema (TacticBullet/per-family) currently aggregates the allowed orientation; keep it allowed-by-default and ensure get_tactic_comparison accepts the orientation param from Task 2 so 129 can request missed — do NOT add a second parallel comparison payload, just thread orientation.
  </action>
  <verify>
    <automated>grep -c "allowed_tactic_motif\|missed_tactic_motif" app/schemas/library.py</automated>
  </verify>
  <acceptance_criteria>
    - The flaw-card / FlawMarker / flaw-list schemas expose allowed_tactic_motif, allowed_tactic_confidence, missed_tactic_motif, missed_tactic_confidence (no bare tactic_motif field remains): `grep -c "[^_]tactic_motif:" app/schemas/library.py` == 0 (all are allowed_/missed_-prefixed).
    - The chip-read producer in library_repository populates both orientation pairs, each gated by _TACTIC_CHIP_CONFIDENCE_MIN.
    - get_tactic_comparison threads the orientation param (default allowed); no duplicate comparison payload model is added.
    - `uv run pytest tests/repositories/test_library_repository.py -x` exits 0; `uv run ty check app/ tests/ exits 0`.
  </acceptance_criteria>
  <done>Both orientation column sets are exposed orientation-labeled on the flaw/comparison schemas; 129 can bind to them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/router → apply_game_filters | the orientation value originates from a query/filter param; it must not select an unintended column or enable SQL injection |
| client → tactic_families filter | family keys from the client expand via FAMILY_TO_MOTIF_INTS, never raw SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-05 | Tampering / Injection | orientation filter dimension | mitigate | orientation is a closed enum Literal["missed","allowed"] (TacticOrientation); it maps to a fixed ORM-column pair, never raw column-name interpolation — validate/constrain at the Pydantic/router boundary so an out-of-set value is rejected before reaching the repo |
| T-128-06 | Tampering / Injection | tactic_families expansion | accept | unchanged from Phase 126 (T-126-02): unknown family keys yield no ints, never raw SQL; reused as-is for both orientations |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan; if any are introduced, route through slopcheck + a blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `uv run pytest tests/repositories/test_query_utils.py tests/repositories/test_library_repository.py -x` exits 0.
- `grep -rn "GameFlaw\.tactic_" app/ --include="*.py"` returns no matches.
- `uv run ty check app/ tests/` exits 0; `uv run ruff check app/ tests/` clean.
</verification>

<success_criteria>
Both filter sites and the flaw/comparison schemas are orientation-aware via a closed enum defaulting to allowed; the family map + confidence gate are reused unchanged; the full D-02 rename ripple is complete (SC#5, SC#3 contract for 129).
</success_criteria>

<output>
Create `.planning/phases/128-missed-opportunity-tagging/128-03-SUMMARY.md` when done, listing the orientation param name/shape and every renamed schema field.
</output>
