---
phase: 153-pure-search-core-guardrail-backup-mcts-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/engine/types.ts
  - frontend/src/lib/engine/guardrail.ts
  - frontend/src/lib/engine/leafScore.ts
  - frontend/src/lib/engine/__tests__/leafScore.test.ts
autonomous: true
requirements: [ENGINE-05, ENGINE-06]

must_haves:
  truths:
    - "The frozen SearchRunner interface (position + budget → ranked lines) compiles and both runners will type-check against it (ENGINE-06, D-06)."
    - "A leaf's white-POV Stockfish eval converts to a root-side-to-move expected score 0-1 via the existing lichess sigmoid, using the ROOT mover as a constant (ENGINE-05, D-06, root-relative frame)."
    - "The engine core speaks UCI everywhere; ELO is color-keyed as {w,b}; one node = one expansion (D-07, D-08, D-09)."
  artifacts:
    - frontend/src/lib/engine/types.ts
    - frontend/src/lib/engine/guardrail.ts
    - frontend/src/lib/engine/leafScore.ts
    - frontend/src/lib/engine/__tests__/leafScore.test.ts
  key_links:
    - "leafScore.ts imports evalToExpectedScore from @/lib/liveFlaw (reuse, not reimplement)."
    - "types.ts re-exports MoveGrade from @/lib/moveQuality so the engine has one import surface."
---

<objective>
Establish the frozen public contract of the search core (`types.ts` + `guardrail.ts`) and the leaf-score conversion (`leafScore.ts`) that turns a Stockfish shallow-eval into a root-relative expected score.

Purpose: Everything downstream in this phase (backup, select, mctsSearch, fallback) and in Phases 154-157 builds against the interface locked here. The single subtlest correctness detail in the whole phase — the root-relative score frame (no per-ply sign flip) — is proven in `leafScore.test.ts` before any orchestration exists.

Output: The interface types, the `SearchRunner` type, `leafExpectedScore()`, and a fixture test proving root=White and root=Black each thread a CONSTANT `rootMover` through the sigmoid.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-CONTEXT.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md
@frontend/src/lib/liveFlaw.ts
@frontend/src/lib/moveQuality.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen engine interface (types.ts + guardrail.ts)</name>
  <files>frontend/src/lib/engine/types.ts, frontend/src/lib/engine/guardrail.ts</files>
  <read_first>
    - frontend/src/lib/moveQuality.ts (MoveGrade interface, lines 31-52; import + re-export it, do not redeclare)
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Pattern 1, lines 171-220 — the exact interface shapes per D-04/D-06/D-07/D-08)
    - frontend/src/lib/liveFlaw.ts (MoverColor type, for downstream compatibility)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/types.ts` defining the frozen core types per the locked decisions. Export: `Side` = the string-literal union of `'w'` and `'b'` (D-08 UCI/FEN side field). Import `MoveGrade` from `@/lib/moveQuality` and re-export it from this module so the engine has a single import surface (per RESEARCH "MoveGrade Reuse" — do NOT declare a structurally-duplicate interface; knip.json has `duplicates: off` so the re-export is fine). Export `EngineProviders` with two async methods: `policy(fen: string, elo: number, side: Side): Promise<Record<string, number>>` (UCI-keyed Maia probabilities over legal moves, D-08) and `grade(fen: string, candidateUcis: string[]): Promise<Map<string, MoveGrade>>` (UCI-keyed, white-POV cp, D-08). Export `SearchBudget` with fields `maxNodes: number` (D-09: one node = one expansion event), `elo: { w: number; b: number }` (D-07: color-keyed, never self/opponent-keyed), `maxPlies: number` (locked 6-10 band, SEED-082), `concurrency: number` (D-03: >=1), and optional `extraRootMoves?: string[]` (D-04: unioned with Maia top-k at the ROOT only). Export `RankedLine` with `rootMove: string` (UCI, D-08), `practicalScore: number` (D-06: root-side-to-move expected score, 0-1), `objectiveEvalCp: number | null`, `modalPath: string[]` (UCI sequence, D-08), `visits: number`. Export `EngineSnapshot` with `rankedLines: RankedLine[]`, `nodesEvaluated: number`, `budgetExhausted: boolean`. Add file-header JSDoc naming the phase and citing D-04/D-06/D-07/D-08/D-09, and per-field one-line JSDoc citing the decision each field encodes.
    Create `frontend/src/lib/engine/guardrail.ts` exporting ONLY the `SearchRunner` type (no logic): a function type `(rootFen: string, budget: SearchBudget, providers: EngineProviders, onSnapshot: (snapshot: EngineSnapshot) => void, signal: AbortSignal) => Promise<EngineSnapshot>`. Import the types it references from `./types`. Header JSDoc cites ENGINE-06 / D-06 and states both `mctsSearch` and `fallbackExpectimax` implement this identical type.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | grep -E "engine/(types|guardrail)" || echo "no type errors in types/guardrail"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/engine/types.ts` exports `Side`, `MoveGrade` (re-exported from `@/lib/moveQuality`), `EngineProviders`, `SearchBudget`, `RankedLine`, `EngineSnapshot`.
    - `SearchBudget.elo` has shape `{ w: number; b: number }` (not `{ self; opponent }`) and includes `extraRootMoves?: string[]`.
    - `RankedLine` fields `rootMove` and `modalPath` are typed as UCI strings (`string` / `string[]`); `practicalScore` is `number`.
    - `guardrail.ts` exports a `SearchRunner` function type with exactly the five parameters `(rootFen, budget, providers, onSnapshot, signal)` returning `Promise<EngineSnapshot>`.
    - `npx tsc -b` reports zero errors for these two files.
    - `MoveGrade` is NOT redeclared as a fresh interface in `types.ts` (it is imported from `@/lib/moveQuality`).
  </acceptance_criteria>
  <done>Both files compile; the interface matches the D-04/06/07/08/09 shapes in RESEARCH Pattern 1.</done>
</task>

<task type="auto">
  <name>Task 2: Root-relative leaf score wrapper + frame fixture (ENGINE-05)</name>
  <files>frontend/src/lib/engine/leafScore.ts, frontend/src/lib/engine/__tests__/leafScore.test.ts</files>
  <read_first>
    - frontend/src/lib/liveFlaw.ts (evalToExpectedScore signature lines 36-51, MoverColor line 24, sideToMoveFromFen line 27 — the exact function being wrapped)
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Pattern 3 "Root-relative score frame" lines 250-268, and "Root-Relative Frame Fixture" lines 409-430 — the literal fixture to encode)
    - frontend/src/lib/__tests__/moveQuality.test.ts (lines 1-52 — the vitest fixture-test header/import/helper style to mirror)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/leafScore.ts` exporting `leafExpectedScore(grade: MoveGrade, rootMover: MoverColor): number` that returns `evalToExpectedScore(grade.evalCp, grade.evalMate, rootMover)` — importing `evalToExpectedScore` and the `MoverColor` type from `@/lib/liveFlaw` and `MoveGrade` from `./types`. Pass `grade.evalMate` through unchanged (the existing sigmoid already maps signed mate-in-N to ±MATE_CP_EQUIVALENT — no new mate logic here, per RESEARCH "MoveGrade Reuse"). The header JSDoc MUST state, as a load-bearing acceptance-level rule (not a footnote), that `rootMover` is the ROOT position's side to move, computed ONCE via `sideToMoveFromFen(rootFen)` and threaded as a CONSTANT through the whole search — NEVER the leaf node's own side to move (this is the no-per-ply-sign-flip root-relative frame, D-06 / Pattern 3). Cite ENGINE-05 and D-06.
    Create `frontend/src/lib/engine/__tests__/leafScore.test.ts` encoding the RESEARCH Root-Relative Frame Fixture: (a) with `rootMover = 'white'`, a white-POV `evalCp = +200` leaf grade returns a value strictly greater than 0.5 (good for the White root player); (b) with `rootMover = 'black'`, the SAME white-POV `+200` grade returns a value strictly less than 0.5 (bad for the Black root player); (c) a mate grade (`evalMate = 3`, `evalCp = null`) with `rootMover = 'white'` returns a value near 1.0 and with `rootMover = 'black'` returns a value near 0.0; (d) a null/null grade returns exactly 0.5. Follow moveQuality.test.ts's header-JSDoc + `describe`/`it` + exact-value `expect` style; document each `it` as the frame-correctness property it proves.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/leafScore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `leafScore.ts` exports `leafExpectedScore(grade: MoveGrade, rootMover: MoverColor): number` and imports `evalToExpectedScore` from `@/lib/liveFlaw` (does not reimplement the sigmoid or redefine LICHESS_K).
    - `npx vitest run src/lib/engine/__tests__/leafScore.test.ts` passes with the four behaviors: white-root +200 → >0.5, black-root +200 → <0.5, mate handling for both root colors, null/null → 0.5.
    - The test file asserts the SAME white-POV eval produces mirrored (not identical) results for the two root colors — proving `rootMover` drives the frame.
    - `npx tsc -b` reports zero errors for `leafScore.ts` and its test.
  </acceptance_criteria>
  <done>leafScore wraps the existing sigmoid, the root-relative-frame fixture passes for both root colors and mate/degenerate cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → engine functions | Fabricated, trusted test doubles only; no external/untrusted input crosses in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-01 | Tampering | `fen: string` parameter (V5 input validation) | low | accept | Pure offline compute; fabricated providers are trusted. A user-supplied FEN surface arrives only in Phase 155; `chess.js` already rejects malformed FEN downstream. No new validation code this phase (RESEARCH Security Domain, V5 Partial). |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase — zero new dependencies (RESEARCH Package Legitimacy Audit). No slopcheck applicable. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/leafScore.test.ts` green.
- `cd frontend && npx tsc -b` zero errors.
</verification>

<success_criteria>
- The frozen `SearchRunner`/`types.ts` contract compiles and matches D-04/06/07/08/09.
- `leafExpectedScore` proven root-relative for both root colors (ENGINE-05, D-06).
</success_criteria>

## Artifacts this plan produces

- File `frontend/src/lib/engine/types.ts` — exports: type `Side`; re-export `MoveGrade`; interfaces `EngineProviders`, `SearchBudget`, `RankedLine`, `EngineSnapshot`.
- File `frontend/src/lib/engine/guardrail.ts` — exports: type `SearchRunner`.
- File `frontend/src/lib/engine/leafScore.ts` — exports: function `leafExpectedScore(grade, rootMover)`.
- File `frontend/src/lib/engine/__tests__/leafScore.test.ts` — test file (no exports).

<output>
Create `.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-01-SUMMARY.md` when done.
</output>
