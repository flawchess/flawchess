---
phase: 153-pure-search-core-guardrail-backup-mcts-fallback
plan: 03
type: execute
wave: 2
depends_on: ["153-01"]
files_modified:
  - frontend/src/lib/engine/select.ts
  - frontend/src/lib/engine/__tests__/select.test.ts
autonomous: true
requirements: [ENGINE-02]

must_haves:
  truths:
    - "A raw Maia policy is truncated at ~90% cumulative probability and renormalized over the kept set, with a separate engine constant (ENGINE-02, D-11)."
    - "Root selection uses Q-based PUCT (Q + c_puct·P_root·√N/(1+n)); every non-root node drops the Q term (P̂·√N/(1+n)); ties break by canonical UCI-string order (D-01)."
    - "The root exploration prior is floor-boosted (max(P_maia, ROOT_PRIOR_FLOOR) renormalized) and affects ONLY the PUCT exploration term — never the renormalized values backup.ts consumes (D-05)."
  artifacts:
    - frontend/src/lib/engine/select.ts
    - frontend/src/lib/engine/__tests__/select.test.ts
  key_links:
    - "truncateAndRenormalize output feeds both root expansion (then floor-boosted for exploration only) and every non-root expansion."
    - "The D-05 root-floor boost is a SEPARATE transform layered on top of truncateAndRenormalize — not the same function."
---

<objective>
Build `select.ts`: the Maia top-k truncation/renormalization (ENGINE-02, D-11), the deterministic PUCT selection with the root/non-root formula split and canonical UCI tie-break (D-01), and the root-only floor-boosted exploration prior (D-05).

Purpose: These are the two candidate-flow primitives the orchestrator composes. Keeping them as pure, independently unit-tested functions isolates the D-05-vs-D-11 conflation trap (floor boost must never touch backup values) and the canonical-tie-break determinism requirement before the async loop exists.

Output: `truncateAndRenormalize()`, `POLICY_MASS_THRESHOLD`, the PUCT selection function(s), `C_PUCT` and `ROOT_PRIOR_FLOOR` constants, the root exploration-prior helper, and `select.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-CONTEXT.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md
@frontend/src/lib/moveQuality.ts
@frontend/src/lib/engine/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: select.ts — truncation, PUCT split, root floor-boost (ENGINE-02, D-01/D-05/D-11)</name>
  <files>frontend/src/lib/engine/select.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Pattern 4 truncation lines 270-290; D-01 selection lines 12-13; D-05 floor lines 19; anti-patterns lines 297-302)
    - frontend/src/lib/moveQuality.ts (selectCandidatesByMass lines 129-152 — the mass-cut loop SHAPE to mirror; CUMULATIVE_MASS_THRESHOLD/CANDIDATE_HARD_CAP lines 48-51 — do NOT import these into the engine)
    - frontend/src/lib/engine/types.ts (Side type, from Plan 01)
    - frontend/src/lib/maiaEncoding.ts (lines 234-255 — noUncheckedIndexedAccess-safe softmax/`?? fallback` narrowing to mirror)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/select.ts`. Export `POLICY_MASS_THRESHOLD = 0.9` with a one-line JSDoc citing D-11 and stating it is deliberately separate from moveQuality.ts's 0.95 (do NOT import CUMULATIVE_MASS_THRESHOLD or CANDIDATE_HARD_CAP). Export `truncateAndRenormalize(policy: Record<string, number>): Map<string, number>`: sort entries by probability descending; accumulate into a kept list, breaking when cumulative mass reaches `POLICY_MASS_THRESHOLD` (mirror moveQuality's loop shape — break AFTER the cumulative check so the crossing move is kept per the analog); renormalize the kept probabilities to sum to 1 (`total > 0 ? p/total : 0`). No hard cap (D-11 rationale). Export `C_PUCT` (named tunable, Claude's discretion — use `1.4`) and `ROOT_PRIOR_FLOOR` (named tunable, ~0.10 per D-05 — use `0.1`), each with decision-citing JSDoc. Export `rootExplorationPriors(renormalized: Map<string, number>): Map<string, number>` applying `max(P_maia, ROOT_PRIOR_FLOOR)` per candidate then renormalizing — JSDoc MUST state this output is used ONLY in the root PUCT exploration term and NEVER as a backup value (D-05; do not conflate with truncateAndRenormalize — anti-pattern lines 300). Export a pure selection function that, given a node's children (each with prior P̂, visit count n, and — for the root only — an optional Q value and the floor-boosted exploration prior) plus the parent visit total N and an `isRoot: boolean` flag, returns the selected child's UCI: at the root, `argmax Q + C_PUCT · P_root · √N/(1+n)`; at non-root, `argmax P̂ · √N/(1+n)` (Q term dropped, D-01); break ties by canonical ascending UCI-string comparison (`localeCompare` / `<` on the UCI key). Use `?? fallback` narrowing for all indexed/Map access (`noUncheckedIndexedAccess`). Header JSDoc cites ENGINE-02, D-01, D-05, D-11.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | grep -E "engine/select" || echo "no type errors in select.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `select.ts` exports `POLICY_MASS_THRESHOLD` (value `0.9`), `truncateAndRenormalize`, `C_PUCT`, `ROOT_PRIOR_FLOOR`, `rootExplorationPriors`, and a selection function.
    - `truncateAndRenormalize` returns a `Map` whose values sum to ~1.0 over the kept set and does NOT import `CUMULATIVE_MASS_THRESHOLD` or `CANDIDATE_HARD_CAP`.
    - The selection function drops the Q term for non-root nodes and includes it only when `isRoot` is true.
    - `rootExplorationPriors` and `truncateAndRenormalize` are two distinct exported functions (the floor boost is not folded into truncation).
    - `npx tsc -b` reports zero errors for `select.ts`.
  </acceptance_criteria>
  <done>Truncation, PUCT split, and root floor-boost exist as distinct pure functions; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: select.test.ts — truncation, PUCT formula, tie-break, floor scope (ENGINE-02, D-01/D-05)</name>
  <files>frontend/src/lib/engine/__tests__/select.test.ts</files>
  <read_first>
    - frontend/src/lib/__tests__/moveQuality.test.ts (selectCandidatesByMass describe block + fixture-test style)
    - frontend/src/lib/engine/select.ts (functions under test, from Task 1)
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Pattern 4 lines 270-290, D-05 lines 19)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/__tests__/select.test.ts` importing from `../select`. Cover: (1) truncation keeps the highest-probability moves until ~90% cumulative mass and drops the tail — construct a policy like `{a:0.5, b:0.3, c:0.15, d:0.05}` and assert the kept set and that returned values renormalize to sum ≈1.0; (2) an already-concentrated policy (one move at 0.95) keeps just that move renormalized to 1.0; (3) root PUCT selection picks the child maximizing `Q + C_PUCT·P·√N/(1+n)` on a fixture where the Q term flips the choice vs. exploration alone; (4) non-root selection ignores Q — a fixture where a high-Q child would win at the root but a different child wins non-root because Q is dropped (proving the D-01 split); (5) canonical tie-break: two children with identical PUCT scores select the lexicographically-smaller UCI; (6) `rootExplorationPriors` raises a near-zero-Maia candidate to at least the floor after renormalization, while `truncateAndRenormalize` on the same input does NOT (proving the floor is a separate root-only transform, D-05). Header JSDoc cites ENGINE-02 / D-01 / D-05 and documents each behavior.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/select.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/select.test.ts` passes.
    - A test asserts truncation cuts at ~90% mass and renormalizes to ~1.0.
    - Separate tests prove the root formula INCLUDES the Q term and the non-root formula EXCLUDES it, on a fixture where the two disagree.
    - A tie-break test asserts the lexicographically-smaller UCI wins on equal scores.
    - A test proves `rootExplorationPriors` floor-boosts a near-zero-Maia move while `truncateAndRenormalize` on the same input does not (floor scope isolated).
  </acceptance_criteria>
  <done>Truncation, both PUCT branches, canonical tie-break, and floor-scope isolation all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → select functions | Fabricated policy Records only; no external input surface this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-03 | Tampering | determinism of tie-break / selection | low | mitigate | Canonical UCI-string tie-break removes iteration-order dependence; enforced by the tie-break test. Correctness invariant, not a security threat. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase — zero new dependencies (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/select.test.ts` green.
- `cd frontend && npx tsc -b` zero errors for `select.ts`.
</verification>

<success_criteria>
- Truncation cuts at ~90% mass and renormalizes (ENGINE-02, D-11).
- Root/non-root PUCT split, canonical tie-break, and root-only floor scope all proven (D-01, D-05).
</success_criteria>

## Artifacts this plan produces

- File `frontend/src/lib/engine/select.ts` — exports: constants `POLICY_MASS_THRESHOLD`, `C_PUCT`, `ROOT_PRIOR_FLOOR`; functions `truncateAndRenormalize(policy)`, `rootExplorationPriors(renormalized)`, and the PUCT selection function.
- File `frontend/src/lib/engine/__tests__/select.test.ts` — test file (no exports).

<output>
Create `.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-03-SUMMARY.md` when done.
</output>
