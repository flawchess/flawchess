---
status: complete
phase: 153-pure-search-core-guardrail-backup-mcts-fallback
source: [153-VERIFICATION.md]
started: 2026-07-06T00:25:00Z
updated: 2026-07-06T15:12:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Disposition of CR-01 (Critical) — false cross-concurrency bit-identity claim
expected: Accept as tracked Phase 154 debt, or fix now (correct comments + add non-degenerate c=2 determinism test). ROADMAP SC1 and CONTEXT D-03 are satisfied as worded; the false invariant lives only in code comments and a structurally blind test fixture.
result: pass
disposition: fixed now (commit 5665bb80) — module header gained a "Determinism scope" paragraph and applyExpansion comment rewritten to the true invariant (deterministic per concurrency level; c=1/c=2 trees may legitimately differ); ENGINE-07 c=2 test now asserts repeated-c=2 bit-identity under differing jitter (what D-03 locked), and determinism fixtures hardened with non-neutral hash-derived grades plus a non-0.5 guard assertion.

### 2. Disposition of WR-02 — `backupRootMax([])` returns `-Infinity`
expected: Add the `children.length === 0 → 0.5` guard plus a test (mirroring `backupExpectation`'s precedent), or explicitly accept the risk (no current unguarded call path) and record why.
result: pass
disposition: fixed now (commit 7f0fb56f) — added the 0.5 degenerate guard mirroring backupExpectation, with a test. Verified live at backup.ts:60.

### 3. Disposition of WR-01 — closed-tree retry loop inflates `RankedLine.visits`
expected: Land the structural `fullyClosed` propagation fix with a visits-sum regression test, or explicitly accept for Phase 153 (Phase 155's UI consumption of `visits` not yet built) and record the acceptance.
result: pass
disposition: fixed now (commit b9c1e3f3) — structural `isClosed` flag + root-ward `propagateClosure`; `SELECTION_ATTEMPT_CAP` removed entirely (loop termination is now structural, killing the false-budgetExhausted failure mode); depth-cutoff test gained the `visits === 1` regression assertion.

### 4. Batch disposition of WR-03..WR-08
expected: Decide which to close before Phase 154 consumes the frozen contract versus tracking as follow-up debt. Items: WR-03 tie-break; WR-04/05 runner drift; WR-06 ~150 duplicated lines; WR-07 unhandled illegal UCI; WR-08 missing extraRootMoves/AbortSignal tests.
result: pass
disposition: all six fixed now — WR-03 UCI tie-break (34b44ac0), WR-04 empty-candidate-set guard (886c0f54), WR-05 unified budgetExhausted semantics (94bdb8e4), WR-06 shared treeCommon.ts extraction subsuming IN-02 (a3d19956), WR-07 illegal-UCI containment (4101a10d), WR-08 extraRootMoves + AbortSignal coverage (c73af326). Info-tier IN-01/IN-03 intentionally deferred per fix_scope=critical_warning. See 153-REVIEW-FIX.md.

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all findings resolved; see 153-REVIEW-FIX.md]
