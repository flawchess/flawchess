---
phase: 153-pure-search-core-guardrail-backup-mcts-fallback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/engine/backup.ts
  - frontend/src/lib/engine/__tests__/backup.test.ts
autonomous: true
requirements: [ENGINE-03]

must_haves:
  truths:
    - "A non-root node's value is the Maia-prior-weighted expectation over the FULL truncated top-k set, mixing expanded (backed-up) and unexpanded (leaf-estimate) children, with NO probability mass dropped (ENGINE-03, D-02)."
    - "The value provably does NOT equal a naive average nor a visit-count-weighted average — the search cannot silently degenerate into textbook MCTS (ENGINE-03, SC2)."
    - "The root node's value is a plain max over candidate values — the tree's single max node (ENGINE-03, D-01)."
  artifacts:
    - frontend/src/lib/engine/backup.ts
    - frontend/src/lib/engine/__tests__/backup.test.ts
  key_links:
    - "backup.ts takes `prior` as an explicit parameter independent of `value`/visits — closing the Pitfall-1 degeneration hole structurally."
    - "fallbackExpectimax.ts (Plan 05) and mctsSearch.ts (Plan 04) both import backupExpectation/backupRootMax from here (SC5 reuse mechanism)."
---

<objective>
Build `backup.ts` — the single genuinely novel, highest-risk file in the phase: the Maia-prior-weighted expectation (non-root) + max (root) backup rule — as isolated pure functions, and prove it with the hand-computed SC2 fixture plus the two negative-assertion baselines.

Purpose: This is the piece SEED-082 calls novel. Isolating it as pure functions with an interface that makes `prior` structurally independent of `value`/visits closes the exact "silently degenerates into textbook MCTS" hole (Pitfall 1). Building and proving it before any orchestration exists is the highest-leverage sequencing choice in the phase.

Output: `backupExpectation()`, `backupRootMax()`, the `BackupChild` interface, and `backup.test.ts` with the mixed expanded/unexpanded fixture and negative assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-CONTEXT.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md
@.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md
@frontend/src/lib/moveQuality.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backup.ts — expectation + root-max pure functions (ENGINE-03, D-01/D-02)</name>
  <files>frontend/src/lib/engine/backup.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md (Pattern 2 lines 222-248 — the exact BackupChild interface + function bodies; Pitfall 1 lines 317-322; "Backup Rule: Worked Fixture" lines 355-389)
    - frontend/src/lib/moveQuality.ts (the "interface directly above the pure function that consumes it", zero-I/O, JSDoc-citing-decision-IDs style to mirror)
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-PATTERNS.md (backup.ts section lines 119-148)
  </read_first>
  <behavior>
    - backupExpectation over children [{prior:0.6,value:0.72},{prior:0.3,value:0.55},{prior:0.1,value:0.40}] returns 0.637 (== 0.6*0.72 + 0.3*0.55 + 0.1*0.40).
    - backupExpectation renormalizes when priors do not sum to 1 (divides each prior by totalPrior).
    - backupExpectation returns 0.5 as a degenerate guard when totalPrior is 0.
    - backupRootMax over children with values [0.60, 0.66] returns 0.66.
  </behavior>
  <action>
    Create `frontend/src/lib/engine/backup.ts`. Export interface `BackupChild` with exactly two fields: `prior: number` (the renormalized Maia prior for this child, frozen at expansion time — JSDoc MUST state it is NEVER re-derived from visits or values, per D-02 / Pitfall 1) and `value: number` (either the child's own backed-up expectation if expanded, or the parent-time `sigmoid(shallowEval)` leaf estimate if not — D-02). Export `backupExpectation(children: readonly BackupChild[]): number` = the prior-weighted mean over the FULL set: sum priors into `totalPrior`; if `totalPrior === 0` return `0.5` (degenerate guard, should not occur post-renormalization); otherwise return `Σ (prior/totalPrior) · value`. No probability mass is dropped and no visit count appears anywhere in the file (D-02). Export `backupRootMax(children: readonly BackupChild[]): number` = `Math.max(...children.map(c => c.value))` — the tree's single max node (D-01). Header JSDoc cites ENGINE-03, D-01, D-02 and names Pitfall 1 (the interface makes deriving `prior` from `value`/visits a code-review-visible impossibility). No try/catch (pure math), matching moveQuality.ts/liveFlaw.ts convention.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | grep -E "engine/backup" || echo "no type errors in backup.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `backup.ts` exports `BackupChild` (fields `prior`, `value` only), `backupExpectation`, `backupRootMax`.
    - `backupExpectation` divides each `prior` by the summed `totalPrior` (renormalizes) and returns `0.5` when `totalPrior === 0`.
    - No `visits`, `n`, or visit-count term appears anywhere in `backup.ts` (verify by reading the file).
    - `backupRootMax` returns `Math.max` of child `value`s.
    - `npx tsc -b` reports zero errors for `backup.ts`.
  </acceptance_criteria>
  <done>Both pure functions exist with the prior-independent-of-value interface; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: backup.test.ts — SC2 worked fixture + negative assertions (ENGINE-03)</name>
  <files>frontend/src/lib/engine/__tests__/backup.test.ts</files>
  <read_first>
    - .planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-RESEARCH.md ("Backup Rule: Worked Fixture" lines 355-389 — the literal numbers and both negative assertions)
    - frontend/src/lib/__tests__/moveQuality.test.ts (lines 1-64 — header JSDoc, imports, describe/it, exact-value + negative-assertion style)
    - frontend/src/lib/engine/backup.ts (the functions under test, from Task 1)
  </read_first>
  <action>
    Create `frontend/src/lib/engine/__tests__/backup.test.ts` importing `backupExpectation`, `backupRootMax`, `type BackupChild` from `../backup`. Encode the SC2 primary fixture: a non-root node with three children mixing one EXPANDED child (prior 0.6, value 0.72 — its own backed-up expectation) and two UNEXPANDED children (prior 0.3 value 0.55; prior 0.1 value 0.40 — leaf estimates), and assert `backupExpectation(children)` is close to 0.637 (use `toBeCloseTo` to avoid float-equality brittleness, matching the project's fixture-test convention). Add negative assertion #1: the result is NOT close to the naive average `(0.72+0.55+0.40)/3 = 0.5567` (priors are actually used). Add negative assertion #2: the result is NOT close to a visit-count-weighted collapse to child A's value 0.72 (the textbook-MCTS degeneration — assert `expect(result).not.toBeCloseTo(0.72, 2)`). Add a renormalization case (priors summing to <1 still weight correctly) and a `totalPrior === 0` case returning 0.5. Add the root-companion fixture: `backupRootMax([{prior:_,value:0.60},{prior:_,value:0.66}])` equals 0.66, plus an explicit assertion that this differs from what `backupExpectation` would return for the same children — proving root uses max, not expectation (the root-vs-non-root branch pitfall). Header JSDoc references ENGINE-03 / SC2 and documents each negative assertion's purpose.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/backup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/backup.test.ts` passes.
    - The primary fixture mixes at least one expanded and two unexpanded children in ONE case (D-02 explicit requirement) and asserts ≈0.637.
    - Two negative assertions exist: result ≠ naive average (0.5567) and result ≠ visit-weighted collapse (0.72).
    - A root case asserts `backupRootMax` == 0.66 AND that it differs from `backupExpectation` over the same children (root-vs-non-root branch proven).
    - A `totalPrior === 0` case asserts the 0.5 degenerate guard.
  </acceptance_criteria>
  <done>SC2 fixture and both negative baselines pass; root-vs-non-root branch is explicitly distinguished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → backup functions | Fabricated numeric fixtures only; no external input surface in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-02 | Tampering | backup value integrity (Pitfall 1 degeneration) | low | mitigate | The `BackupChild` interface supplies `prior` independently of `value`/visits, making visit-count leakage code-review-visible; the negative-assertion fixture enforces it in CI. Not a security threat per se — a correctness invariant tracked here for completeness. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase — zero new dependencies (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/backup.test.ts` green.
- `cd frontend && npx tsc -b` zero errors for `backup.ts`.
</verification>

<success_criteria>
- Non-root backup = prior-weighted expectation over the full truncated set (≈0.637), provably ≠ naive/visit-weighted averages (ENGINE-03, SC2).
- Root backup = max (ENGINE-03, D-01), distinct from expectation.
</success_criteria>

## Artifacts this plan produces

- File `frontend/src/lib/engine/backup.ts` — exports: interface `BackupChild`; functions `backupExpectation(children)`, `backupRootMax(children)`.
- File `frontend/src/lib/engine/__tests__/backup.test.ts` — test file (no exports).

<output>
Create `.planning/phases/153-pure-search-core-guardrail-backup-mcts-fallback/153-02-SUMMARY.md` when done.
</output>
