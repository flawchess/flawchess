---
phase: 117-priority-queue-flaw-integration
plan: 03
type: execute
wave: 3
depends_on: ["117-01", "117-02"]
files_modified:
  - app/services/engine.py
  - app/services/eval_drain.py
  - tests/services/test_full_eval_drain.py
autonomous: true
requirements: [EVAL-04, EVAL-06, QUEUE-03]
must_haves:
  truths:
    - "Every evaluated non-dedup'd position gets best_move (PV[0] UCI) written from the SAME 1M-node search — zero extra engine compute (EVAL-04)"
    - "Opening-region best_move is transplanted via the full_hash dedup alongside eval_cp, gated on lichess_evals_at IS NULL (WR-02 repointed, D-117-07)"
    - "When a game's full analysis completes, classify_game_flaws runs automatically, game_flaws rows are inserted, oracle count columns are filled for engine games (D-117-08), and the full PV is written at each flaw-adjacent ply N+1 (D-117-02), all in one write transaction (EVAL-06)"
    - "full_evals_completed_at and full_pv_completed_at are both set on completion; the served eval_jobs row (if any) is reported complete"
    - "The drain picks via the queue claim instead of the LIFO id-DESC pick; tier-1 fans all of one game's plies across the pool (~10s, QUEUE-03)"
  artifacts:
    - path: "app/services/engine.py"
      provides: "evaluate_nodes_with_pv (eval_cp, eval_mate, best_move) + _pv_to_best_move / _pv_to_uci_string extractors"
      contains: "evaluate_nodes_with_pv"
    - path: "app/services/eval_drain.py"
      provides: "queue-lease pick, best_move threading, WR-02 repoint, _classify_and_fill_oracle, _mark_full_pv_completed, flaw PV write, completion signal"
      contains: "_classify_and_fill_oracle"
    - path: "tests/services/test_full_eval_drain.py"
      provides: "EVAL-04 (best_move + dedup transplant + flaw pv), EVAL-06 (classify hook + oracle counts), D-117-07 (WR-02 repoint), QUEUE-03 (gather outside session)"
      contains: "best_move"
  key_links:
    - from: "app/services/eval_drain.py"
      to: "app.services.eval_queue_service.claim_eval_job"
      via: "queue lease replaces LIFO pick"
      pattern: "claim_eval_job"
    - from: "app/services/eval_drain.py"
      to: "app.services.engine.evaluate_nodes_with_pv"
      via: "PV-carrying engine call in the gather"
      pattern: "evaluate_nodes_with_pv"
    - from: "app/services/eval_drain.py"
      to: "classify_game_flaws + count_game_severities"
      via: "_classify_and_fill_oracle post-game hook"
      pattern: "classify_game_flaws"
    - from: "app/services/eval_drain.py"
      to: "game_positions.pv"
      via: "flaw PV write at ply N+1"
      pattern: "ply.*\\+.*1"
---

<objective>
Integrate the queue and PV/flaw flow into the Phase 116 full-ply drain. Three coordinated changes: (1) capture best_move (PV[0] UCI) for every position and the ~12-ply flaw PV via a new evaluate_nodes_with_pv that reuses the existing search (zero extra compute); (2) thread best_move through the dedup transplant + write path and repoint the WR-02 gate + is_analyzed onto lichess_evals_at (D-117-07); (3) replace the LIFO pick with the queue claim, add a post-game classify_game_flaws hook that fills oracle count columns (D-117-08) and writes flaw PVs (D-117-02), set both completion markers, report the job, and signal per-user cache completion (D-117-11). Tier-1 jobs fan all of one game's plies across the pool (~10s, QUEUE-03).

Purpose: EVAL-04, EVAL-06, and the QUEUE-03 fan-out — the user-visible payoff of the phase. The drain loop (run_full_eval_drain) is untouched; only _full_drain_tick's internals change.
Output: engine.py PV capture, eval_drain.py integration, extended test_full_eval_drain.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-priority-queue-flaw-integration/117-CONTEXT.md
@.planning/phases/117-priority-queue-flaw-integration/117-RESEARCH.md
@.planning/phases/117-priority-queue-flaw-integration/117-PATTERNS.md
@.planning/phases/117-priority-queue-flaw-integration/117-VALIDATION.md
@.planning/phases/116-all-ply-engine-core/116-CONTEXT.md

# Depends on 117-01 (columns) and 117-02 (claim)
@.planning/phases/117-priority-queue-flaw-integration/117-01-SUMMARY.md
@.planning/phases/117-priority-queue-flaw-integration/117-02-SUMMARY.md

# Code being modified (exact analogs are these files themselves)
@app/services/engine.py
@app/services/eval_drain.py
@app/services/flaws_service.py
@tests/services/test_full_eval_drain.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine.py — evaluate_nodes_with_pv + PV extractors (zero extra compute)</name>
  <files>app/services/engine.py</files>
  <read_first>
    117-RESEARCH.md §2 (InfoDict["pv"] is list[chess.Move]; info=Info.ALL is the default so PV is already present; _pv_to_best_move + _pv_to_uci_string; sign/perspective note; ~12-ply cap fits varchar/Text), 117-PATTERNS.md §"engine.py" (evaluate_nodes lines 391-398, _analyse lines 349-385, _score_to_cp_mate lines 240-257; prefer adding _analyse_with_pv returning InfoDict|None to avoid touching _analyse's return type), app/services/engine.py (the _NODES_BUDGET, _NODES_TIMEOUT_S constants, ENG-03 UCI-options-only-here invariant — no new UCI options needed).
  </read_first>
  <behavior>
    - evaluate_nodes_with_pv(board) returns (eval_cp, eval_mate, best_move_uci); best_move_uci = info["pv"][0].uci() when PV present, else None; eval_cp/eval_mate identical to evaluate_nodes for the same board (no search difference).
    - On engine failure/timeout: returns (None, None, None) (D-09 failure semantics carried forward — NULL row, no retry).
    - _pv_to_best_move returns None for empty/missing PV.
    - _pv_to_uci_string caps at PV_CAP_PLIES (=12) and space-joins UCI.
  </behavior>
  <action>
    Add to app/services/engine.py a module-level constant PV_CAP_PLIES = 12 (D-117-02; comment that SEED-039's deepest motif tier needs ~3 plies, 12 gives margin). Add two pure extractors mirroring _score_to_cp_mate's info.get(...) guard style: _pv_to_best_move(info) -> str | None (return None if not info.get("pv"), else pv[0].uci()) and _pv_to_uci_string(info, cap: int = PV_CAP_PLIES) -> str | None (None if empty, else " ".join(m.uci() for m in pv[:cap])). Add an _analyse_with_pv method on EnginePool that mirrors _analyse but returns the raw InfoDict | None (None on failure/timeout) using the SAME chess.engine.Limit(nodes=_NODES_BUDGET) + _NODES_TIMEOUT_S — do NOT add a second search or change UCI options (ENG-03). Add EnginePool.evaluate_nodes_with_pv(board) -> tuple[int|None,int|None,str|None] that calls _analyse_with_pv once, then applies _score_to_cp_mate and _pv_to_best_move to the same InfoDict (returns (None,None,None) when the InfoDict is None). Add a module-level evaluate_nodes_with_pv(board) function parallel to the existing module-level evaluate_nodes(board). Keep evaluate_nodes unchanged (the entry-ply depth-15 path is untouched). Explicit return types everywhere (ty). Note: the full ~12-ply PV string is also needed for the flaw write in Task 2 — expose it too. Make _analyse_with_pv's InfoDict reachable to the drain: either return the full (eval_cp, eval_mate, best_move, pv_string) 4-tuple from a richer evaluate_nodes_with_pv, OR return best_move here and compute pv_string in the drain from a returned InfoDict. CHOOSE the 4-tuple form: evaluate_nodes_with_pv returns (eval_cp, eval_mate, best_move_uci, pv_uci_string) so the drain has the flaw PV without a second engine touch. Update the behavior/tests accordingly.
  </action>
  <verify>
    <automated>uv run ty check app/services/engine.py && uv run ruff check app/services/engine.py && uv run python -c "from app.services.engine import evaluate_nodes_with_pv, _pv_to_best_move, _pv_to_uci_string; import chess; b=chess.Board(); assert _pv_to_best_move({})is None; assert _pv_to_uci_string({})is None; import chess as c; mv=[c.Move.from_uci('e2e4'),c.Move.from_uci('e7e5')]; assert _pv_to_best_move({'pv':mv})=='e2e4'; assert _pv_to_uci_string({'pv':mv})=='e2e4 e7e5'; print('pv extractors OK')"</automated>
  </verify>
  <done>evaluate_nodes_with_pv returns (eval_cp, eval_mate, best_move_uci, pv_uci_string) reusing the existing search (no second analyse); extractors handle empty PV; PV_CAP_PLIES caps the string; evaluate_nodes unchanged; ty + ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: eval_drain.py — queue pick, best_move threading, WR-02 repoint, classify+oracle+PV hook, markers</name>
  <files>app/services/eval_drain.py</files>
  <read_first>
    117-RESEARCH.md §1 (precise line map: _FullPlyEvalTarget 100-118, interim LIFO pick 916-938, _fetch_dedup_evals WR-02 gate line 201 + return-type extension, _apply_full_eval_results 246-290, _mark_full_evals_completed 293-306, post-game hook point 1011-1026), §5 (complete repoint edit-list — sites that MUST change vs KEEP), §6 (_classify_and_fill_oracle details, flaw PV ply = N+1, count_game_severities for inaccuracies, classify_game_flaws idempotent via ON CONFLICT), §7 (cache completion signal — simple no-op/set hook for now), Pitfalls 1-6. 117-PATTERNS.md §"_full_drain_tick modifications", §"_classify_and_fill_oracle", §"Shared Patterns" (session discipline, update(Model.__table__), Sentry aggregation). app/services/eval_drain.py (full file), app/services/flaws_service.py (classify_game_flaws line 595, count_game_severities line 665 — reads ONLY game.user_color; "reason" in result discriminates GameNotAnalyzed), app/services/eval_queue_service.py (claim_eval_job, report_job_complete, ClaimedJob), app/services/engine.py (evaluate_nodes_with_pv from Task 1).
  </read_first>
  <behavior>
    - best_move is written for every non-dedup'd evaluated ply (from evaluate_nodes_with_pv).
    - Opening-region (ply ≤ DEDUP_MAX_PLY) best_move is transplanted from the dedup source row alongside eval_cp/eval_mate, gated on lichess_evals_at IS NULL (WR-02 repointed).
    - is_analyzed (driving D-116-04 preserve-vs-overwrite) is derived from lichess_evals_at IS NOT NULL, fetched in the load step (the pick no longer provides it).
    - After evals are applied, classify_game_flaws runs; game_flaws rows inserted (ON CONFLICT DO NOTHING); oracle count columns (white/black inaccuracies/mistakes/blunders) filled (D-117-08); the flaw PV is written to game_positions.pv at ply = flaw_ply + 1 (D-117-02, Pitfall 4).
    - full_evals_completed_at AND full_pv_completed_at are both set; if the claim returned a job_id (tier-1/2), report_job_complete is called — all in the one write transaction.
    - Tier-1 (claim tier==1) fans all of the game's plies across the pool in one asyncio.gather (outside session scope); tiers 2/3 keep game-granular behavior.
    - A per-user completion signal hook is invoked after commit (D-117-11 — a no-op/set insert for now; Phase 118 wires real cache invalidation).
  </behavior>
  <action>
    Modify app/services/eval_drain.py surgically (run_full_eval_drain loop stays untouched; only _full_drain_tick internals + helpers change):
    1. _FullPlyEvalTarget: add trailing field best_move: str | None = None (and a pv: str | None = None field if you carry the per-ply PV string on the target; otherwise hold engine results in the existing engine_result_map). Keep the with-default-after-no-default ordering.
    2. Replace the interim LIFO pick block (lines ~916-938) with a call to eval_queue_service.claim_eval_job(). If it returns None, the tick returns False (idle). Otherwise unpack ClaimedJob(game_id, user_id, tier, is_analyzed, job_id). Because tier-3 is a derived pick, is_analyzed comes from the claim; for tier-1/2 it is already on the ClaimedJob — no separate load needed if the queue service provides it. (If the claim does NOT provide is_analyzed for queued rows, load it in the read step via Game.lichess_evals_at.isnot(None).)
    3. _fetch_dedup_evals: change the WR-02 gate at line 201 from Game.white_blunders.is_(None) to Game.lichess_evals_at.is_(None) (D-117-07, Pitfall 2). Extend the select() to add GamePosition.best_move; change return type to dict[int, tuple[int|None,int|None,str|None]]; update the single caller to unpack the 3-tuple.
    4. The engine gather: use engine.evaluate_nodes_with_pv (4-tuple) instead of evaluate_nodes. For tier==1, gather ALL of the game's non-dedup'd plies in one asyncio.gather across the pool (net-new fan-out, RESEARCH §3 / PATTERNS net-new); for tiers 2/3 keep the existing per-game gather. The gather stays OUTSIDE the session (CLAUDE.md hard rule). Build an engine_result_map keyed by ply → (eval_cp, eval_mate, best_move, pv_string).
    5. _apply_full_eval_results: thread best_move into the per-row UPDATE (.values(eval_cp=, eval_mate=, best_move=)). Honor the D-116-04 preserve-vs-overwrite gate using the now-lichess_evals_at-derived is_analyzed (preserve lichess %evals; best_move/pv still written since they are new columns — T-78-17 protects only the eval numbers).
    6. New _classify_and_fill_oracle(write_session, game_id, ...): load the game + ordered positions, call classify_game_flaws(game, positions); if "reason" in result → skip (GameNotAnalyzed). Else bulk_insert_game_flaws(rows). Fill oracle counts: call count_game_severities twice — once with user_color="white", once with "black" (it reads only game.user_color; pass a shallow-copied/duck-typed Game with the swapped color, or add a color param — choose the minimal-change path, per RESEARCH Open Q2: use count_game_severities for inaccuracies and derive M+B from the classify_game_flaws list, OR call count_game_severities per color for all three — pick one and comment it). update(Game.__table__) sets white/black inaccuracies/mistakes/blunders. Then for each FlawRecord at ply N, write game_positions.pv = pv_string at ply N+1 (Pitfall 4 off-by-one) from engine_result_map. Same try/except + Sentry-context (game_id/user_id, no vars in message) pattern as _classify_and_insert_flaws; aggregate per game (WR-05, no per-ply events).
    7. Clone _mark_full_evals_completed into _mark_full_pv_completed (same update(Game.__table__) shape, column full_pv_completed_at). In the write session (lines ~1011-1026) order: _apply_full_eval_results → _classify_and_fill_oracle → _mark_full_evals_completed → _mark_full_pv_completed → (if job_id is not None) report_job_complete → commit.
    8. After commit, call a new _signal_flaw_completion(user_id) (RESEARCH §7 — module-level set insert / no-op stub; Phase 118 wires caching). Keep it dead-simple and side-effect-free beyond the set.
    Verify NONE of the KEEP sites from RESEARCH §5 are touched (game.py is_analyzed hybrid, library_repository, library_service, normalization white_blunders write, schemas). Keep functions within the nesting/LOC budget — extract helpers (e.g. the tier-1 fan-out gather, the oracle-fill, the flaw-PV write) as needed.
  </action>
  <verify>
    <automated>uv run ty check app/services/eval_drain.py && uv run ruff check app/services/eval_drain.py && grep -n "lichess_evals_at.is_(None)" app/services/eval_drain.py && grep -vn '^#' app/services/eval_drain.py | grep -c "white_blunders.is_(None)"</automated>
  </verify>
  <done>The LIFO pick is replaced by claim_eval_job; WR-02 gate uses lichess_evals_at.is_(None) (and white_blunders.is_(None) no longer appears as the WR-02 discriminator); best_move threaded through dedup + write; _classify_and_fill_oracle fills oracle counts + writes flaw PV at ply N+1; both completion markers set; job reported; gather stays outside session; ty + ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/services/test_full_eval_drain.py — EVAL-04, EVAL-06, D-117-07, QUEUE-03</name>
  <files>tests/services/test_full_eval_drain.py</files>
  <read_first>
    117-VALIDATION.md (exact -k names: best_move, dedup_best_move, flaw_pv, classify_hook, oracle_counts, wr02_repointed, gather_outside_session), 117-RESEARCH.md §Validation + §8 (mock evaluate_nodes_with_pv to return (0,None,"e2e4","e2e4 e7e5") instantly; drive _full_drain_tick directly), tests/services/test_full_eval_drain.py (existing fixtures, _insert_game, the existing QUEUE-07 gather-outside-session AST test to extend, monkeypatch patterns), 117-PATTERNS.md §tests.
  </read_first>
  <behavior>
    - best_move: after a drain tick, evaluated non-dedup'd plies have best_move populated.
    - dedup_best_move: an opening-region ply (ply ≤ DEDUP_MAX_PLY) whose full_hash matches a marker-complete engine source (lichess_evals_at IS NULL) gets best_move transplanted without an engine call.
    - flaw_pv: game_positions.pv is set ONLY at ply N+1 for a flaw at ply N, and NULL elsewhere.
    - classify_hook: after full eval completes, game_flaws rows exist for the game.
    - oracle_counts: white/black inaccuracies/mistakes/blunders columns are filled for an engine-analyzed game and match the game_flaws row counts (keeps the db-report "counts match game_flaws" check green — D-117-08).
    - wr02_repointed: a game with lichess_evals_at IS NOT NULL is NOT used as a dedup source even if white_blunders IS NULL; a game with lichess_evals_at IS NULL IS usable (the gate follows lichess_evals_at, not white_blunders).
    - gather_outside_session: extend the existing AST/structural test so the new evaluate_nodes_with_pv gather is still outside any session scope (QUEUE-03 fan-out discipline).
  </behavior>
  <action>
    Extend tests/services/test_full_eval_drain.py (do not create a new file — VALIDATION targets this file's -k names). Add a flawed-game PGN fixture (a short game with at least one clear blunder so classify_game_flaws emits a FlawRecord) and reuse the existing user/session fixtures + async_session_maker monkeypatch. Mock engine.evaluate_nodes_with_pv to return deterministic (eval_cp, eval_mate, best_move, pv_string) tuples (vary evals at the flaw ply so a blunder is detected). For QUEUE-03, also seed a tier-1 eval_jobs row and assert the served game completes (full_evals_completed_at set, job status='completed'). Add each -k-named test from VALIDATION.md. Keep tests independent (distinct game_ids/user range). Use the per-run isolated DB; no dev DB reset.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_full_eval_drain.py -x</automated>
  </verify>
  <done>All new -k tests pass (best_move, dedup_best_move, flaw_pv, classify_hook, oracle_counts, wr02_repointed, gather_outside_session); oracle counts match game_flaws row counts; the full file is green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **Engine:** `app/services/engine.py::evaluate_nodes_with_pv` (module-level + EnginePool method) → (eval_cp, eval_mate, best_move, pv_string); `_pv_to_best_move`, `_pv_to_uci_string`, `_analyse_with_pv`, `PV_CAP_PLIES`.
- **Drain:** `app/services/eval_drain.py` — queue-lease pick (claim_eval_job), best_move threaded through `_fetch_dedup_evals` + `_apply_full_eval_results`, WR-02 gate repointed to `lichess_evals_at`, new `_classify_and_fill_oracle` (game_flaws + oracle counts + flaw PV at ply N+1), `_mark_full_pv_completed`, `report_job_complete` wiring, `_signal_flaw_completion` stub, tier-1 whole-pool fan-out.
- **Test:** extended `tests/services/test_full_eval_drain.py`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drain → engine gather | asyncio.gather over EnginePool workers OUTSIDE any AsyncSession (CLAUDE.md hard rule; structurally enforced) |
| drain claim → eval_jobs | short SKIP-LOCKED claim transaction, committed before the engine gather (Pitfall 1) |
| engine results → game_positions write | one write transaction commits evals + flaws + oracle counts + markers atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-09 | Denial of service | tier-1 whole-pool fan-out | mitigate | One game's ~60 plies across 6-8 workers ≈ 10s (spike 003); the gate-between-games (D-116-11) protects the quick import lane; only superuser-enqueued tier-1 jobs exist in Phase 117 (no user fan-out path). |
| T-117-10 | Tampering | preserve-vs-overwrite gate (D-116-04) repointed | mitigate | is_analyzed derived from lichess_evals_at IS NOT NULL (D-117-07); lichess %evals preserved (T-78-17), engine evals overwritten — WR-02 gate change lands BEFORE oracle-count writes (Pitfall 2/5 ordering). |
| T-117-11 | Repudiation / data integrity | partial write (evals committed, flaws not) | mitigate | _classify_and_fill_oracle + both markers + report run in the SAME write session as _apply_full_eval_results — atomic commit, no partial state. |
| T-117-12 | Information disclosure | Sentry leaking game/ply identifiers | mitigate | game_id/user_id/ply via set_context/set_tag only, never in the message string; per-game aggregated events (WR-05) — no per-ply flooding. |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (python-chess, SQLAlchemy, sentry_sdk all in-tree). No legitimacy gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_full_eval_drain.py -x` green (all new -k tests).
- `uv run pytest tests/services/test_eval_queue.py tests/test_migration_117.py -x` still green (no regression).
- Full suite: `uv run pytest -n auto -x` green before /gsd-verify-work.
- `uv run ty check app/ tests/` zero errors; `uv run ruff check app/ tests/` clean; `uv run ruff format app/ tests/`.
- WR-02 grep gate: `grep -vn '^#' app/services/eval_drain.py | grep -c "white_blunders.is_(None)"` returns 0 (the WR-02 discriminator is repointed; comment lines excluded).
- db-report "counts match game_flaws" sanity check stays green (oracle counts match game_flaws rows for engine games).
</verification>

<success_criteria>
- best_move populated on every evaluated non-dedup'd ply (zero extra engine compute); opening-region best_move transplanted via dedup gated on lichess_evals_at IS NULL.
- Flaw PV written only at ply N+1; oracle count columns filled for engine games and matching game_flaws.
- classify_game_flaws runs automatically on full-eval completion; both completion markers set; eval_jobs row reported complete.
- Drain picks via the queue claim; tier-1 fans across the pool (~10s); gather stays outside session.
- All Wave 0 tests + full suite green; ty + ruff + format clean.
</success_criteria>

<output>
Create `.planning/phases/117-priority-queue-flaw-integration/117-03-SUMMARY.md` when done.
</output>
