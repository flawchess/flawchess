---
phase: 70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/opening_insights.py
  - tests/services/test_opening_insights_service.py
  - tests/repositories/test_opening_insights_repository.py
  - tests/services/test_opening_insights_arrow_consistency.py
  - tests/routers/test_insights_openings.py
autonomous: true
requirements:
  - INSIGHT-CORE-01
  - INSIGHT-CORE-08
tags:
  - openings
  - insights
  - schemas
  - tdd
user_setup: []

must_haves:
  truths:
    - "Importing OpeningInsightsRequest, OpeningInsightFinding, OpeningInsightsResponse from app.schemas.opening_insights succeeds with no circular-import errors."
    - "Pydantic v2 round-trip (model_validate -> model_dump) preserves all fields with correct Literal-enum validation."
    - "Hash fields entry_full_hash and resulting_full_hash are typed as `str` (per OpeningWDL.full_hash convention) so 64-bit values survive JSON serialization."
    - "Wave 0 test files exist and FAIL with clear MISSING-implementation errors so downstream waves can flip them green."
    - "Frontend constants-consistency test file is present and parses arrowColor.ts via regex."
  artifacts:
    - path: app/schemas/opening_insights.py
      provides: "OpeningInsightsRequest, OpeningInsightFinding, OpeningInsightsResponse Pydantic v2 models (D-01, D-03, D-05, D-10, D-26)"
      contains: "class OpeningInsightsRequest"
    - path: tests/services/test_opening_insights_service.py
      provides: "Wave 0 unit-test scaffolding for service classifier/severity/dedupe/ranking/caps (D-04..D-09, D-21..D-24)"
      contains: "TestClassificationBoundaries"
    - path: tests/repositories/test_opening_insights_repository.py
      provides: "Wave 0 SQL contract test scaffolding for transition CTE (D-30, D-32, D-33)"
      contains: "test_entry_ply_boundaries"
    - path: tests/services/test_opening_insights_arrow_consistency.py
      provides: "CI-enforced consistency test between Python service constants and frontend/src/lib/arrowColor.ts (D-04 alignment)"
      contains: "LIGHT_COLOR_THRESHOLD"
    - path: tests/routers/test_insights_openings.py
      provides: "Wave 0 router-contract test scaffolding for POST /api/insights/openings (D-13, D-14)"
      contains: "test_endpoint_requires_auth"
  key_links:
    - from: "app/schemas/opening_insights.py"
      to: "app/repositories/query_utils.py::DEFAULT_ELO_THRESHOLD"
      via: "import for OpeningInsightsRequest.elo_threshold default"
      pattern: "from app.repositories.query_utils import DEFAULT_ELO_THRESHOLD"
    - from: "tests/services/test_opening_insights_arrow_consistency.py"
      to: "frontend/src/lib/arrowColor.ts"
      via: "regex extract of LIGHT_COLOR_THRESHOLD and DARK_COLOR_THRESHOLD"
      pattern: "LIGHT_COLOR_THRESHOLD\\s*=\\s*55"
---

<objective>
Land the Pydantic v2 schema module that locks the contract between Phase 70's backend and Phase 71's frontend, plus the Wave 0 test scaffolding that downstream plans will turn green.

Purpose: Per-phase plans executing later (repository, service, router) consume and emit these schemas. Defining them first means each downstream task can be written test-first against a stable shape with no codebase exploration.

Output: One new schema file (`app/schemas/opening_insights.py`) plus four new failing-by-design test files containing concrete fixtures and skeletons that downstream waves implement against.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-backend-opening-insights-service/70-CONTEXT.md
@.planning/phases/70-backend-opening-insights-service/70-RESEARCH.md
@.planning/phases/70-backend-opening-insights-service/70-PATTERNS.md
@.planning/phases/70-backend-opening-insights-service/70-VALIDATION.md
@CLAUDE.md
@app/schemas/openings.py
@app/schemas/stats.py
@app/schemas/insights.py
@app/repositories/query_utils.py
@frontend/src/lib/arrowColor.ts

<interfaces>
<!-- Critical contracts for the schema file. Extracted from research; do NOT re-derive. -->

From app/repositories/query_utils.py:
```python
DEFAULT_ELO_THRESHOLD = 50

def apply_game_filters(
    stmt,
    time_control: Sequence[str] | None,
    platform: Sequence[str] | None,
    rated: bool | None,
    opponent_type: str,
    recency_cutoff: datetime.datetime | None,
    color: str | None = None,
    *,
    opponent_strength: Literal["any", "stronger", "similar", "weaker"] = "any",
    elo_threshold: int = DEFAULT_ELO_THRESHOLD,
)
```

From app/services/openings_service.py:78 — `recency_cutoff()` accepted strings:
`"week" | "month" | "3months" | "6months" | "year" | "3years" | "5years" | "all"` (or `None`).

From app/schemas/stats.py::OpeningWDL.full_hash (line 50):
```python
full_hash: str  # 64-bit Zobrist hash stringified at the API boundary for JSON precision
```

From frontend/src/lib/arrowColor.ts:
```typescript
const MIN_GAMES_FOR_COLOR = 10;          // NOT mirrored — Phase 70 uses 20 (D-33)
const LIGHT_COLOR_THRESHOLD = 55;        // mirrored as LIGHT_THRESHOLD = 0.55
const DARK_COLOR_THRESHOLD = 60;         // mirrored as DARK_THRESHOLD = 0.60
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OpeningInsightsRequest / OpeningInsightFinding / OpeningInsightsResponse Pydantic v2 schemas</name>
  <files>app/schemas/opening_insights.py</files>
  <read_first>
    - .planning/phases/70-backend-opening-insights-service/70-CONTEXT.md  (decisions D-01, D-03, D-05, D-10, D-11, D-25, D-26 and §Specifics OpeningInsightFinding field set)
    - .planning/phases/70-backend-opening-insights-service/70-PATTERNS.md (Section "app/schemas/opening_insights.py")
    - app/schemas/openings.py                                              (recency Literal, list[Literal] for time_control/platform, opponent_type values)
    - app/schemas/stats.py                                                 (OpeningWDL.full_hash str convention at line 50)
    - app/schemas/insights.py                                              (Literal idiom — DO NOT re-export FilterContext per D-11)
    - app/repositories/query_utils.py                                      (DEFAULT_ELO_THRESHOLD = 50, exact apply_game_filters signature)
    - app/services/openings_service.py                                     (lines 78-86 — recency_cutoff accepted values; pin Literal exactly to these strings)
  </read_first>
  <behavior>
    - OpeningInsightsRequest(...).model_validate({"recency": "all_time"}) raises ValidationError (only "all" is accepted, not "all_time" — D-11 decoupling avoids reusing endgame FilterContext naming).
    - OpeningInsightsRequest(...).model_validate({"opponent_strength": "average"}) raises ValidationError (only "any"|"stronger"|"similar"|"weaker" accepted).
    - OpeningInsightsRequest(...).model_validate({"color": "all"}) succeeds and exposes color == "all".
    - OpeningInsightsRequest().elo_threshold defaults to 50 (DEFAULT_ELO_THRESHOLD).
    - OpeningInsightFinding(color="white", classification="weakness", severity="major", opening_name="London System", opening_eco="A45", display_name="London System", entry_fen="...", entry_full_hash="123456789012345", candidate_move_san="Nf3", resulting_full_hash="987654321098765", n_games=20, wins=4, draws=4, losses=12, win_rate=0.20, loss_rate=0.60, score=0.30) round-trips through model_dump() and model_validate() with all fields preserved.
    - OpeningInsightFinding(... entry_full_hash=12345 ...) raises ValidationError because entry_full_hash is `str`, not `int` (precision protection per RESEARCH.md Pitfall 1).
    - OpeningInsightFinding(... severity="medium" ...) raises ValidationError (only "minor"|"major" accepted per D-05).
    - OpeningInsightFinding(... classification="neutral" ...) raises ValidationError (only "weakness"|"strength" accepted per D-04, neutrals are dropped at SQL HAVING level).
    - OpeningInsightsResponse() default-constructs with all four lists empty (D-01, D-20).
    - OpeningInsightsResponse(white_weaknesses=[finding], black_weaknesses=[], white_strengths=[], black_strengths=[]).model_dump() yields a dict with all four keys present, even when three are empty.
  </behavior>
  <action>
    Create `app/schemas/opening_insights.py` from scratch. Do NOT extend `app/schemas/insights.py` (D-11, D-26 explicit).

    Imports (top of file):
    ```python
    from typing import Literal

    from pydantic import BaseModel, Field

    from app.repositories.query_utils import DEFAULT_ELO_THRESHOLD
    ```

    Define exactly three classes in this order:

    1) `OpeningInsightsRequest(BaseModel)` — request body for POST /api/insights/openings (D-10, D-13). Fields, types, and defaults (use exactly these Literal value lists; do NOT add or remove members):
       - `recency: Literal["week", "month", "3months", "6months", "year", "3years", "5years", "all"] | None = None` (matches `recency_cutoff()` accepted strings — see app/services/openings_service.py:78-86; do NOT use the endgame "all_time" spelling per D-11)
       - `time_control: list[Literal["bullet", "blitz", "rapid", "classical"]] | None = None`
       - `platform: list[Literal["chess.com", "lichess"]] | None = None`
       - `rated: bool | None = None`
       - `opponent_type: Literal["human", "bot", "both"] = "human"`
       - `opponent_strength: Literal["any", "stronger", "similar", "weaker"] = "any"`
       - `elo_threshold: int = DEFAULT_ELO_THRESHOLD`
       - `color: Literal["all", "white", "black"] = "all"`

    2) `OpeningInsightFinding(BaseModel)` — single finding (D-03, D-05, D-25, CONTEXT.md §Specifics). Fields in this exact order with these exact types:
       - `color: Literal["white", "black"]`
       - `classification: Literal["weakness", "strength"]`
       - `severity: Literal["minor", "major"]`
       - `opening_name: str` (use `"<unnamed line>"` sentinel value at runtime when no openings-table match — see D-23; field type stays plain `str`, never `Optional[str]` per RESEARCH.md §Risks 2)
       - `opening_eco: str` (`""` sentinel per D-23)
       - `display_name: str` (may include `"vs. "` prefix per D-22 / RESEARCH.md Pitfall 4)
       - `entry_fen: str`
       - `entry_full_hash: str` (str-form for JSON precision, mirrors `OpeningWDL.full_hash:str` — RESEARCH.md Pitfall 1)
       - `candidate_move_san: str`
       - `resulting_full_hash: str` (str-form, same reason)
       - `n_games: int`
       - `wins: int`
       - `draws: int`
       - `losses: int`
       - `win_rate: float` (used as classifier for strengths per D-04)
       - `loss_rate: float` (used as classifier for weaknesses per D-04)
       - `score: float` (`(W + D/2)/n`; informative only per D-06)

    3) `OpeningInsightsResponse(BaseModel)` — four-section response (D-01, D-02). Exactly four fields, all default to empty list via `Field(default_factory=list)`:
       - `white_weaknesses: list[OpeningInsightFinding] = Field(default_factory=list)`
       - `black_weaknesses: list[OpeningInsightFinding] = Field(default_factory=list)`
       - `white_strengths: list[OpeningInsightFinding] = Field(default_factory=list)`
       - `black_strengths: list[OpeningInsightFinding] = Field(default_factory=list)`

    DO NOT add `model_validator`, `field_validator`, custom `__init__`, or any other logic. The file is pure data definitions — Pydantic's auto-generated Literal validation is sufficient.

    DO NOT import `FilterContext` from `app.schemas.insights` (D-11 forbids reuse). DO NOT add a `source` field (D-18 dropped bookmark-as-source).

    File length should be ~70-90 lines including imports and class docstrings. Add a single-line module docstring naming the phase: `"""Pydantic v2 schemas for Phase 70 opening insights backend (D-26)."""`.
  </action>
  <verify>
    <automated>uv run python -c "from app.schemas.opening_insights import OpeningInsightsRequest, OpeningInsightFinding, OpeningInsightsResponse; r = OpeningInsightsRequest(); assert r.color == 'all' and r.elo_threshold == 50 and r.opponent_type == 'human'; resp = OpeningInsightsResponse(); assert resp.white_weaknesses == [] and resp.black_strengths == []; print('OK')" && uv run ty check app/schemas/opening_insights.py</automated>
  </verify>
  <acceptance_criteria>
    - File `app/schemas/opening_insights.py` exists.
    - `grep -c "class OpeningInsightsRequest" app/schemas/opening_insights.py` outputs `1`.
    - `grep -c "class OpeningInsightFinding" app/schemas/opening_insights.py` outputs `1`.
    - `grep -c "class OpeningInsightsResponse" app/schemas/opening_insights.py` outputs `1`.
    - `grep -c "from app.repositories.query_utils import DEFAULT_ELO_THRESHOLD" app/schemas/opening_insights.py` outputs `1`.
    - `grep -c "from app.schemas.insights import" app/schemas/opening_insights.py` outputs `0` (D-11 — no FilterContext reuse).
    - `grep -c 'Literal\["weakness", "strength"\]' app/schemas/opening_insights.py` outputs `1`.
    - `grep -c 'Literal\["minor", "major"\]' app/schemas/opening_insights.py` outputs `1`.
    - `grep -c 'Literal\["white", "black"\]' app/schemas/opening_insights.py` outputs at least `1` (color field on Finding).
    - `grep -c 'all_time' app/schemas/opening_insights.py` outputs `0` (D-11: must use `"all"`, not `"all_time"`).
    - `grep -c "entry_full_hash: str" app/schemas/opening_insights.py` outputs `1` (RESEARCH.md Pitfall 1 — str form for 64-bit hashes).
    - `grep -c "resulting_full_hash: str" app/schemas/opening_insights.py` outputs `1`.
    - `grep -cE "^\s*source\s*:" app/schemas/opening_insights.py` outputs `0` (D-18 dropped the source field).
    - `uv run ty check app/schemas/opening_insights.py` exits 0.
    - `uv run ruff check app/schemas/opening_insights.py` exits 0.
  </acceptance_criteria>
  <done>The schema module imports cleanly, Pydantic round-trips a sample finding, ty/ruff are clean, and downstream plans can `from app.schemas.opening_insights import ...` without further edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test scaffolding — fixtures + skeletons for service, repository, router, and arrow-consistency tests</name>
  <files>tests/services/test_opening_insights_service.py, tests/repositories/test_opening_insights_repository.py, tests/services/test_opening_insights_arrow_consistency.py, tests/routers/test_insights_openings.py</files>
  <read_first>
    - .planning/phases/70-backend-opening-insights-service/70-RESEARCH.md (§Validation Architecture, §Critical Sample Dimensions table, §Test Map)
    - .planning/phases/70-backend-opening-insights-service/70-PATTERNS.md  (sections "tests/services/test_opening_insights_service.py", "tests/test_opening_insights_repository.py", "tests/services/test_opening_insights_arrow_consistency.py")
    - .planning/phases/70-backend-opening-insights-service/70-VALIDATION.md (§Wave 0 Requirements)
    - tests/services/test_insights_service.py                              (AsyncMock idiom, synthetic Pydantic instances — analog for service test)
    - tests/test_openings_repository.py                                    (db_session fixture, _seed_game helper, ensure_test_user — analog for repo test)
    - tests/conftest.py                                                    (look up `db_session`, `ensure_test_user` fixtures — copy idiom faithfully)
    - frontend/src/lib/arrowColor.ts                                       (verify `LIGHT_COLOR_THRESHOLD = 55` and `DARK_COLOR_THRESHOLD = 60` literal text shape)
    - app/schemas/opening_insights.py                                      (just created — import target)
  </read_first>
  <behavior>
    - Each test file is collected by pytest (no SyntaxError, no ImportError).
    - All tests in tests/services/test_opening_insights_service.py FAIL with "ModuleNotFoundError: No module named 'app.services.opening_insights_service'" or NotImplementedError, NEVER pass — Wave 0 is intentionally red.
    - All tests in tests/repositories/test_opening_insights_repository.py FAIL with "ImportError" (function not yet defined) or NotImplementedError.
    - tests/services/test_opening_insights_arrow_consistency.py FAILS at import time with "ModuleNotFoundError" since the service module doesn't exist yet.
    - tests/routers/test_insights_openings.py FAILS at import time or at test time because POST /api/insights/openings doesn't exist.
    - `uv run pytest tests/services/test_opening_insights_service.py tests/repositories/test_opening_insights_repository.py tests/services/test_opening_insights_arrow_consistency.py tests/routers/test_insights_openings.py --co -q` exits 0 and lists at least 12 collected tests across the four files.
  </behavior>
  <action>
    Create four files. Each file MUST be collectable by pytest (no SyntaxError) and MUST contain failing test bodies (`raise NotImplementedError("Wave 0 stub — Plan 70-NN implements")`) plus the imports/fixtures/parametrize tables that pin the contract. Downstream plans flip them green.

    --- File 1: `tests/services/test_opening_insights_service.py` ---
    Top-level imports:
    ```python
    from __future__ import annotations
    from unittest.mock import AsyncMock, patch
    import pytest
    ```
    Then attempt to import the not-yet-existing service:
    ```python
    pytest.importorskip("app.services.opening_insights_service", reason="Wave 0 — service not yet implemented")
    from app.schemas.opening_insights import OpeningInsightFinding, OpeningInsightsRequest
    from app.services.opening_insights_service import (
        LIGHT_THRESHOLD, DARK_THRESHOLD, MIN_GAMES_PER_CANDIDATE,
        WEAKNESS_CAP_PER_COLOR, STRENGTH_CAP_PER_COLOR, compute_insights,
    )
    ```
    Define these test functions, each with a body of `raise NotImplementedError("Wave 0 stub — Plan 70-04 (service) implements")`. The function names lock the per-requirement coverage matrix from RESEARCH.md §Test Map:
    - `test_classification_strict_gt_boundary_loss_rate_055()`  — D-04 strict `>`, loss_rate exactly 0.550 → not a finding
    - `test_classification_minor_weakness_at_loss_rate_0551()`
    - `test_classification_major_weakness_at_loss_rate_060()`   — D-05 severity boundary
    - `test_classification_minor_strength_at_win_rate_0599()`
    - `test_classification_major_strength_at_win_rate_060()`
    - `test_min_games_floor_excludes_n19()`                     — D-33 evidence floor
    - `test_min_games_floor_includes_n20()`
    - `test_dedupe_within_section_keeps_deepest_entry()`        — D-21, D-24
    - `test_cross_color_same_hash_kept_as_two_findings()`       — D-21
    - `test_attribution_picks_max_ply_count()`                  — D-22
    - `test_attribution_lineage_walk_to_parent_hash()`          — D-23
    - `test_unnamed_line_fallback_uses_empty_eco_and_sentinel_name()` — D-23
    - `test_ranking_severity_desc_then_n_games_desc()`          — D-07
    - `test_caps_5_weaknesses_3_strengths_per_color()`          — D-02, D-08
    - `test_color_optimization_skips_unused_color_query()`      — D-12
    - `test_display_name_vs_prefix_when_attribution_parity_disagrees()` — D-22 / Pitfall 4
    - `test_bookmarks_not_consumed_by_algorithm()`              — D-18

    Add a module-level docstring referencing the requirement IDs covered: `"""Phase 70 service unit tests — INSIGHT-CORE-04, INSIGHT-CORE-05, INSIGHT-CORE-06, INSIGHT-CORE-07."""`.

    --- File 2: `tests/repositories/test_opening_insights_repository.py` ---
    Create `tests/repositories/__init__.py` if it doesn't exist (empty file).
    Top-level imports + autouse fixture matching the analog `tests/test_openings_repository.py` lines 20-42:
    ```python
    from __future__ import annotations
    import pytest
    import pytest_asyncio
    from sqlalchemy.ext.asyncio import AsyncSession

    pytest.importorskip(
        "app.repositories.openings_repository",
        reason="Wave 0 — query_opening_transitions function not yet defined",
    )
    ```
    Test function names (each body `raise NotImplementedError("Wave 0 stub — Plan 70-03 (repository) implements")`):
    - `test_entry_ply_lower_bound_3_inclusive()`                  — D-32
    - `test_entry_ply_upper_bound_16_inclusive()`                 — D-32
    - `test_entry_ply_2_excluded()`
    - `test_entry_ply_17_excluded_as_entry_but_included_as_candidate()`
    - `test_lag_returns_null_for_first_ply_of_each_game()`        — RESEARCH.md Pitfall 2
    - `test_min_games_per_candidate_floor_at_20()`                — D-33
    - `test_having_strict_gt_055_drops_neutrals()`                — D-04
    - `test_user_color_filter_routes_correct_games()`
    - `test_apply_game_filters_recency_narrows_results()`         — INSIGHT-CORE-01
    - `test_partial_index_predicate_alignment()`                  — RESEARCH.md A6 (smoke check that EXPLAIN uses ix_gp_user_game_ply)
    Add a module docstring naming requirement IDs: `"""Phase 70 repository SQL contract tests — INSIGHT-CORE-02, INSIGHT-CORE-03, INSIGHT-CORE-04."""`.

    --- File 3: `tests/services/test_opening_insights_arrow_consistency.py` ---
    Build per PATTERNS.md §"tests/services/test_opening_insights_arrow_consistency.py". Two real (non-stub) tests because the implementation is purely regex-parsing arrowColor.ts and equality assertion against constants imported from the (not-yet-existing) service module. The `pytest.importorskip` makes them red until Plan 70-04 lands the service.
    ```python
    """CI-enforced consistency: opening_insights_service constants must match
    frontend/src/lib/arrowColor.ts. Catches future arrow-color drift."""
    import re
    from pathlib import Path
    import pytest

    pytest.importorskip(
        "app.services.opening_insights_service",
        reason="Wave 0 — service module not yet implemented",
    )
    from app.services.opening_insights_service import LIGHT_THRESHOLD, DARK_THRESHOLD

    _ARROW_TS = Path(__file__).resolve().parents[2] / "frontend/src/lib/arrowColor.ts"

    def _extract(name: str) -> int:
        text = _ARROW_TS.read_text()
        m = re.search(rf"{name}\s*=\s*(\d+)", text)
        assert m, f"could not find {name} in arrowColor.ts"
        return int(m.group(1))

    def test_light_threshold_matches_frontend() -> None:
        assert _extract("LIGHT_COLOR_THRESHOLD") == int(LIGHT_THRESHOLD * 100)

    def test_dark_threshold_matches_frontend() -> None:
        assert _extract("DARK_COLOR_THRESHOLD") == int(DARK_THRESHOLD * 100)
    ```

    --- File 4: `tests/routers/test_insights_openings.py` ---
    Create `tests/routers/__init__.py` if missing.
    Mirror the existing pattern in `tests/test_insights_router.py` (FastAPI TestClient or httpx AsyncClient). Use `pytest.importorskip` on `app.services.opening_insights_service` so the file is collectable but the tests RED until Plan 70-05 lands the route.
    Test function names (bodies `raise NotImplementedError("Wave 0 stub — Plan 70-05 (router) implements")`):
    - `test_post_openings_endpoint_requires_auth_returns_401()`           — V4 / V2 ASVS
    - `test_post_openings_endpoint_returns_four_section_response()`       — D-01
    - `test_post_openings_endpoint_rejects_invalid_recency_value()`       — Pydantic validation
    - `test_post_openings_endpoint_rejects_user_id_in_request_body()`     — V4 ASVS — endpoint must derive user_id from session only
    - `test_post_openings_endpoint_does_NOT_apply_full_history_gate()`    — D-14 (must NOT inherit `_validate_full_history_filters`)
    - `test_post_openings_endpoint_filter_equivalence()`                  — INSIGHT-CORE-01

    All four files MUST collect under pytest. Verify: `uv run pytest <files> --co -q` exits 0 and reports collection failures of zero. The tests themselves all FAIL (skip due to importorskip OR raise NotImplementedError) — that is the Wave 0 intent.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_opening_insights_service.py tests/repositories/test_opening_insights_repository.py tests/services/test_opening_insights_arrow_consistency.py tests/routers/test_insights_openings.py --co -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/services/test_opening_insights_service.py` exists.
    - File `tests/repositories/test_opening_insights_repository.py` exists.
    - File `tests/repositories/__init__.py` exists (empty is fine).
    - File `tests/services/test_opening_insights_arrow_consistency.py` exists.
    - File `tests/routers/test_insights_openings.py` exists.
    - File `tests/routers/__init__.py` exists (empty is fine).
    - `grep -c "test_classification_strict_gt_boundary_loss_rate_055" tests/services/test_opening_insights_service.py` outputs `1`.
    - `grep -c "test_min_games_floor_excludes_n19" tests/services/test_opening_insights_service.py` outputs `1`.
    - `grep -c "test_dedupe_within_section_keeps_deepest_entry" tests/services/test_opening_insights_service.py` outputs `1`.
    - `grep -c "test_cross_color_same_hash_kept_as_two_findings" tests/services/test_opening_insights_service.py` outputs `1`.
    - `grep -c "test_color_optimization_skips_unused_color_query" tests/services/test_opening_insights_service.py` outputs `1`.
    - `grep -c "test_bookmarks_not_consumed_by_algorithm" tests/services/test_opening_insights_service.py` outputs `1`.
    - `grep -c "test_entry_ply_lower_bound_3_inclusive" tests/repositories/test_opening_insights_repository.py` outputs `1`.
    - `grep -c "test_lag_returns_null_for_first_ply_of_each_game" tests/repositories/test_opening_insights_repository.py` outputs `1`.
    - `grep -c "test_light_threshold_matches_frontend" tests/services/test_opening_insights_arrow_consistency.py` outputs `1`.
    - `grep -c "test_post_openings_endpoint_requires_auth_returns_401" tests/routers/test_insights_openings.py` outputs `1`.
    - `grep -c "test_post_openings_endpoint_does_NOT_apply_full_history_gate" tests/routers/test_insights_openings.py` outputs `1`.
    - `uv run pytest tests/services/test_opening_insights_service.py tests/repositories/test_opening_insights_repository.py tests/services/test_opening_insights_arrow_consistency.py tests/routers/test_insights_openings.py --co -q` exits 0 (collection succeeds).
    - `uv run ruff check tests/services/test_opening_insights_service.py tests/repositories/test_opening_insights_repository.py tests/services/test_opening_insights_arrow_consistency.py tests/routers/test_insights_openings.py` exits 0.
  </acceptance_criteria>
  <done>All four Wave 0 test files exist, collect under pytest, and contain the specific test names that Plans 70-03/04/05 will turn green. Each test body either skips via importorskip or raises NotImplementedError — none pass spuriously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (POST /api/insights/openings) | All filter values cross from untrusted client into Pydantic validation; the Literal enum types are the first defense. |
| schema → service | Once validated, fields are trusted within the backend; the service must still scope queries by authenticated user_id only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01-01 | Tampering | OpeningInsightsRequest input | mitigate | Pydantic v2 Literal enums on `recency`, `time_control[]`, `platform[]`, `opponent_type`, `opponent_strength`, `color` reject any value outside the closed set — enforced automatically by FastAPI request parsing. `elo_threshold: int` rejects non-int. No string passes through to SQL without validation. |
| T-70-01-02 | Information Disclosure | 64-bit hash precision loss in JSON | mitigate | `entry_full_hash: str` and `resulting_full_hash: str` typing forces stringification at the API boundary, mirroring `OpeningWDL.full_hash:str`. Prevents downstream Phase 72 mismatch where JS truncates >2^53 to 53 bits. |
| T-70-01-03 | Spoofing | Misuse of endgame FilterContext shape | mitigate | D-11 explicit decoupling: this plan does NOT import `app.schemas.insights.FilterContext`. Future endgame schema evolution cannot accidentally widen the openings attack surface. Verified by grep gate (`from app.schemas.insights import` count = 0). |
| T-70-01-04 | Elevation of Privilege | user_id injected via request body | mitigate | OpeningInsightsRequest does NOT contain a `user_id` field. The router (Plan 70-05) MUST pull user_id from `current_active_user` only; the test scaffold `test_post_openings_endpoint_rejects_user_id_in_request_body` locks this contract early. |
</threat_model>

<verification>
- `app/schemas/opening_insights.py` exists, imports cleanly, ty-checks zero errors, ruff-clean.
- All four test files exist, collect under pytest, contain the named test functions for downstream waves.
- No reference to `FilterContext` from `app.schemas.insights` (D-11 gate).
- No `source: Literal["top_openings", "bookmark"]` field anywhere in schema (D-18 gate).
</verification>

<success_criteria>
- Schema module satisfies Pydantic round-trip for a representative finding.
- Wave 0 test scaffolding files exist with the exact named test functions; tests collect cleanly but fail (importorskip-skipped or NotImplementedError-raising) so that downstream plans land them green.
- `uv run ty check app/schemas/opening_insights.py` passes.
- All five files committed in a single atomic commit so reverting plan 70-01 reverts the full schema/test scaffolding pair.
</success_criteria>

<output>
After completion, create `.planning/phases/70-backend-opening-insights-service/70-01-SUMMARY.md` capturing: schema fields confirmed locked, Wave 0 test names, any deviation from the test-name list above (none expected), and the path forward to Plan 70-03/04/05.
</output>
