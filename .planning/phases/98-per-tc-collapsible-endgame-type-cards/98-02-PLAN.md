---
phase: 98-per-tc-collapsible-endgame-type-cards
plan: 02
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - frontend/src/lib/primaryTc.ts
  - frontend/src/lib/__tests__/primaryTc.test.ts
  - frontend/src/components/charts/EndgameTypeCard.tsx
  - frontend/src/components/charts/EndgameTypeTcCard.tsx
  - frontend/src/components/charts/EndgameTypeBreakdownSection.tsx
  - frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx
  - frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx
  - frontend/src/pages/Endgames.tsx
  - CHANGELOG.md
autonomous: true
requirements: [standalone]
nyquist_compliant: true

must_haves:
  truths:
    - "The Endgame Type Breakdown renders full-width vertically-stacked collapsible cards, one per TC, replacing the 3-col grid (SC-1)"
    - "The primary TC card is expanded by default (argmax games × NOMINAL_DURATION over TCs passing the games floor), others collapsed, keyboard-accessible with data-testids (SC-2)"
    - "Each TC card shows a 2×2-family grid of four type tiles (rook/minor_piece/pawn/queen); Mixed is no longer rendered; pawnless stays hidden (SC-3)"
    - "Each tile's Conversion + Recovery gauges are restored, banded against PER_CLASS_TC_GAUGE_ZONES[class][tc] (SC-4)"
    - "Each tile's Score Gap is banded per-(class × TC) from achievable_score_gap (SC-5)"
    - "A TC card / tile self-suppresses below the games floor (SC-7)"
    - "Accordion resets to the recomputed primary TC on any filter change (D-12)"
    - "Desktop + mobile render the collapsible cards and 2×2→1×4 tile grid responsively (SC-9)"
    - "Frontend lint/test/knip gates pass; existing tests updated for the new layout; CHANGELOG [Unreleased] entry added (SC-10)"
  artifacts:
    - path: "frontend/src/lib/primaryTc.ts"
      provides: "NOMINAL_DURATION constants + computePrimaryTc shared util (D-09/D-10/D-11)"
      contains: "computePrimaryTc"
    - path: "frontend/src/components/charts/EndgameTypeCard.tsx"
      provides: "Restored 5-element tile with Conv/Recov gauges banded per-(class × TC)"
      contains: "PER_CLASS_TC_GAUGE_ZONES"
    - path: "frontend/src/components/charts/EndgameTypeTcCard.tsx"
      provides: "Per-TC accordion item: full-bleed charcoal header + 4-tile divider grid"
      contains: "AccordionItem"
    - path: "frontend/src/components/charts/EndgameTypeBreakdownSection.tsx"
      provides: "Controlled accordion orchestrator over per-TC cards, primary expanded"
      contains: "computePrimaryTc"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] entry for the restructure + Conv/Recov re-introduction"
      contains: "Endgame Type"
  key_links:
    - from: "EndgameTypeBreakdownSection.tsx"
      to: "stats.categories_by_tc"
      via: "prop from Endgames.tsx parent response"
      pattern: "categories_by_tc"
    - from: "EndgameTypeCard.tsx tile"
      to: "PER_CLASS_TC_GAUGE_ZONES[class][tc]"
      via: "colorizeGaugeZones for gauge zones + achievable_score_gap for ScoreGapRow"
      pattern: "PER_CLASS_TC_GAUGE_ZONES"
    - from: "EndgameTypeBreakdownSection.tsx"
      to: "computePrimaryTc"
      via: "useEffect reset on filter change (D-12)"
      pattern: "setExpandedTc"
---

<objective>
Frontend restructure of the Endgame Type Breakdown: replace the 3-col grid of five per-type cards with full-width vertically-stacked collapsible per-TC cards (bullet/blitz/rapid/classical), the user's primary TC expanded by default, each card holding a divider-separated 2×2-family grid of four restored 5-element type tiles (rook/minor_piece/pawn/queen — Mixed dropped). Gauges + Score-Gap band against the per-(class × TC) bands generated in Plan 98-01.

Purpose: Bring back the Conv/Recov gauges removed 2026-05-29, now judged against the correct per-(class × TC) reference, presented in the mode-3 collapsible-TC-cards disclosure pattern. Honors D-01/D-02 (literal restore of the pre-removal 5-element tile), D-04/D-05/D-06/D-07/D-08 (charcoal header + both-axes divider grammar + responsive staircase), D-09/D-10/D-11 (primary-TC heuristic in a shared util), D-12 (reset-to-primary on filter change), and SC-1…SC-5, SC-7, SC-9, SC-10.

Output: `primaryTc.ts` shared util, restored `EndgameTypeCard.tsx` tile, new `EndgameTypeTcCard.tsx` accordion item, rewritten `EndgameTypeBreakdownSection.tsx`, updated `Endgames.tsx` wiring, updated tests, and a CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-CONTEXT.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add primaryTc shared util + restore EndgameTypeCard 5-element tile (per-(class × TC) bands)</name>
  <files>frontend/src/lib/primaryTc.ts, frontend/src/lib/__tests__/primaryTc.test.ts, frontend/src/components/charts/EndgameTypeCard.tsx, frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx</files>
  <read_first>
    - git show d3453597^:frontend/src/components/charts/EndgameTypeCard.tsx (the pre-removal 5-element anatomy to restore — gauge row, PER_TYPE_GAUGE_SIZE=130, convZones/recovZones derivation, gamesLink Swords span)
    - frontend/src/components/charts/EndgameTypeCard.tsx (current post-removal 4-element form — what to extend back)
    - frontend/src/lib/scoreBulletConfig.ts (analog for primaryTc.ts: named-const + pure-fn idiom, named exports only)
    - frontend/src/components/charts/EndgameGauge.tsx (`size` + `zones` props, 0-100 value scale)
    - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx (ScoreGapRow `neutralMin`/`neutralMax`/`startSlot`/`endSlot` props)
    - frontend/src/lib/theme.ts (`colorizeGaugeZones`, `MIN_GAMES_FOR_RELIABLE_STATS`, `UNRELIABLE_OPACITY`, ZONE_*)
    - frontend/src/generated/endgameZones.ts (the `PER_CLASS_TC_GAUGE_ZONES` export from Plan 98-01)
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md §4 (5-element anatomy), §Code Examples (gauge zone derivation, computePrimaryTc), §8 (test changes), Pitfall 5 + Pitfall 8
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md "primaryTc.ts" + "EndgameTypeCard.tsx" sections
  </read_first>
  <action>
    Create `frontend/src/lib/primaryTc.ts` with a named `NOMINAL_DURATION: Record<'bullet' | 'blitz' | 'rapid' | 'classical', number>` constant = `{ bullet: 60, blitz: 180, rapid: 600, classical: 900 }` (D-10, named constants not magic numbers) and an exported pure `computePrimaryTc(categoriesByTc, minGames)` returning `'bullet' | 'blitz' | 'rapid' | 'classical' | null` = argmax of `tcTotal × NOMINAL_DURATION[tc]` over TCs where `tcTotal >= minGames`, iterating fixed `['bullet','blitz','rapid','classical']` order, returning null when no TC passes the floor (D-09). Place it in `frontend/src/lib/` so the ELO timeline can later consume it (D-11). Create `frontend/src/lib/__tests__/primaryTc.test.ts`: assert argmax picks the time-weighted TC (e.g. rapid wins over a higher-count bullet), respects the games floor (sub-floor TCs excluded), and returns null when all TCs are below floor. In `frontend/src/components/charts/EndgameTypeCard.tsx`, restore the pre-removal 5-element anatomy from `d3453597^` with two changes per D-04: (a) add a `tc: 'bullet' | 'blitz' | 'rapid' | 'classical'` prop to `EndgameTypeCardProps`; (b) derive gauge bands from `PER_CLASS_TC_GAUGE_ZONES[category.endgame_class]?.[tc]` (guard pawnless → undefined, narrow per `noUncheckedIndexedAccess`), `convZones = colorizeGaugeZones(0, 100, classBands.conversion[0]*100, classBands.conversion[1]*100)`, `recovZones` likewise, and `[sgNeutralMin, sgNeutralMax] = classBands?.achievable_score_gap ?? [-0.04, 0.04]`. Restore the `import { EndgameGauge }`, the `PER_TYPE_GAUGE_SIZE = 130` constant, and the gauge-row JSX (`grid grid-cols-2 gap-2` with `data-testid={`${tileTestId}-conv-gauge`}` / `-recov-gauge`, label "Conversion"/"Recovery", `value={category.conversion.conversion_pct}` / `.recovery_pct`, `maxValue={100}`, `size={PER_TYPE_GAUGE_SIZE}`). Replace the old `import { PER_CLASS_GAUGE_ZONES }` with `PER_CLASS_TC_GAUGE_ZONES`. Change the ScoreGapRow `neutralMin`/`neutralMax` from the flat `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX` to `sgNeutralMin`/`sgNeutralMax`. Keep the plain Score bullet (#4) on its existing 50% anchor + Wilson sig-gate UNCHANGED (D-04). Preserve all `data-testid`s, ARIA, `text-sm` floor, theme constants, and the `total < MIN_GAMES_FOR_RELIABLE_STATS` → `UNRELIABLE_OPACITY` + `n={total}` chip behavior. Update `frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx`: restore assertions for the `-conv-gauge` / `-recov-gauge` testids, pass a `tc` prop in fixtures, and assert the gauge zones / ScoreGapRow neutralMin-Max reflect the per-(class × TC) band (e.g. rook+classical). Remove stale fixture fields (`opp_conversion_pct`, `conv_diff_p_value`) only if they break the build (Pitfall 8). Run `npm run knip` and remove the now-unused `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_*` import if knip flags it.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run primaryTc EndgameTypeCard</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/primaryTc.ts` exports `computePrimaryTc` and `NOMINAL_DURATION` with values bullet=60, blitz=180, rapid=600, classical=900.
    - `grep -c "PER_CLASS_TC_GAUGE_ZONES" frontend/src/components/charts/EndgameTypeCard.tsx` ≥ 1 and `PER_CLASS_GAUGE_ZONES` no longer imported there.
    - `EndgameTypeCard.tsx` contains `data-testid` strings ending `-conv-gauge` and `-recov-gauge` and `PER_TYPE_GAUGE_SIZE`.
    - `EndgameTypeCardProps` includes a `tc:` field typed `'bullet' | 'blitz' | 'rapid' | 'classical'`.
    - `cd frontend && npm test -- --run primaryTc EndgameTypeCard` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0 (no `noUncheckedIndexedAccess` violations; no `any`).
  </acceptance_criteria>
  <done>`computePrimaryTc` is a tested shared util; the tile renders the full 5-element anatomy with Conv+Recov gauges and a Score-Gap bullet all banded against `PER_CLASS_TC_GAUGE_ZONES[class][tc]`, while the plain Score bullet keeps its 50% anchor; `EndgameTypeCard.test.tsx` passes against the restored layout.</done>
</task>

<task type="auto">
  <name>Task 2: Create EndgameTypeTcCard accordion item (full-bleed header + both-axes divider 4-tile grid)</name>
  <files>frontend/src/components/charts/EndgameTypeTcCard.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsByTcCard.tsx (full-bleed charcoal header ~line 354-379: `bg-black/20 border-b border-border/40`, TimeControlIcon, TC_LABELS, `Games: X% (count)` + Swords; the single-axis `divider` helper ~line 347 — the grammar to preserve, NOT copy verbatim)
    - frontend/src/components/ui/accordion.tsx (Accordion/AccordionItem/AccordionTrigger/AccordionContent API; default `not-last:border-b` on AccordionItem)
    - frontend/src/pages/Endgames.tsx (existing accordion usage ~line 381 — charcoal-texture rounded-md item, AccordionTrigger styling)
    - frontend/src/components/charts/EndgameTypeCard.tsx (the tile to render inside, from Task 1)
    - frontend/src/components/icons/TimeControlIcon.tsx
    - frontend/src/lib/endgameMetrics.ts (`HIDDEN_ENDGAME_CLASSES`, fixed rook/minor_piece/pawn/queen tile order, Mixed exclusion)
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md §5 (D-08 two-axis divider technique), §7 (AccordionContent padding note: do NOT add px-4), Pitfall 4
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md "EndgameTypeTcCard.tsx" section (per-cell conditional border classes, header pattern, AccordionItem wrapping)
  </read_first>
  <action>
    Create `frontend/src/components/charts/EndgameTypeTcCard.tsx` — a single per-TC accordion item. Props: `tc: 'bullet' | 'blitz' | 'rapid' | 'classical'`, `categories: EndgameCategoryStats[]` (this TC's slice), `grandTotal: number`, `onCategorySelect` (deep-link). Render an `AccordionItem` with `value={tc}`, `className="charcoal-texture rounded-md overflow-hidden border-none"` (suppress the default inter-item border; outer `gap-2` provides separation, PATTERNS Shared Patterns). The `AccordionTrigger` IS the full-bleed header: `bg-black/20 border-b border-border/40 px-4 py-3 flex items-center gap-2`, containing `<TimeControlIcon timeControl={tc} />`, `<h3 className="text-base font-semibold">{TC_LABELS[tc]}</h3>`, and a right-aligned `Games: {pctOfGrandTotal}% ({total})` span with the `Swords` icon. Add `data-testid={`type-breakdown-tc-${tc}-trigger`}` and `data-testid={`type-breakdown-tc-${tc}-header`}` plus `aria-label={`${TC_LABELS[tc]} endgame type breakdown`}` (SC-2 keyboard-accessible header). Inside `AccordionContent` (NO `px-4` on the content wrapper — full-bleed header; the grid body owns its padding), render a `grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 p-4` of the four tiles in fixed rook/minor_piece/pawn/queen order (filter out Mixed + `HIDDEN_ENDGAME_CLASSES`/pawnless, SC-3). Implement the both-axes divider grammar (D-08) with per-cell conditional border classes via `cn(...)` keyed on tile index `i` (NOT `divide-x`/`divide-y`, Pitfall 4): mobile 1×4 → `border-t border-border/40` when `i>0`; tablet 2×2 → reset top borders (`sm:border-t-0`), add `sm:border-t` when `i>=2` (bottom row), add `sm:border-r` when `i%2===0` (left column); desktop 4×1 → `xl:border-t-0`, `xl:border-r` when `i<3` else `xl:border-r-0`. Use `border-border/40` to match the `EndgameMetricsByTcCard` divider color (D-06 grammar, theme-consistent — not a hard-coded color). Pass each tile its `tc` prop (Task 1) so it resolves the per-(class × TC) band. Add `data-testid="endgame-type-tc-card-${tc}"` on the card container. This component does NOT own the games-floor suppression — that is the section's job (Task 3); it renders whatever `categories` it receives.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `EndgameTypeTcCard.tsx` contains `AccordionItem`, `AccordionTrigger`, `AccordionContent`, `TimeControlIcon`, `Swords`, and the full-bleed header classes `bg-black/20 border-b border-border/40`.
    - It does NOT use `divide-x` or `divide-y`; it uses per-cell `border-r`/`border-t` classes gated with `sm:`/`xl:` prefixes.
    - `AccordionContent` does NOT carry a `px-4` class; the inner grid carries `p-4`.
    - The tile grid filters out Mixed and pawnless (rook/minor_piece/pawn/queen only).
    - Contains `data-testid` values `type-breakdown-tc-${tc}-trigger`, `type-breakdown-tc-${tc}-header`, and an `aria-label` on the trigger.
    - `cd frontend && npx tsc --noEmit && npm run lint` exits 0.
  </acceptance_criteria>
  <done>A per-TC accordion item renders a full-bleed charcoal header (icon + label + Games count) and a responsive 4-tile grid (4×1 → 2×2 → 1×4) separated by both-axes dividers (not boxes), Mixed dropped, each tile receiving its `tc` for per-(class × TC) band lookup.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite EndgameTypeBreakdownSection as collapsible orchestrator, wire Endgames.tsx, update tests + CHANGELOG</name>
  <files>frontend/src/components/charts/EndgameTypeBreakdownSection.tsx, frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx, frontend/src/pages/Endgames.tsx, CHANGELOG.md</files>
  <read_first>
    - frontend/src/components/charts/EndgameTypeBreakdownSection.tsx (current 3-col grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`, props, section wrapper + data-testid + aria-label, sub-question copy)
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (per-TC card selection, fixed TC order, MIN_GAMES_PER_TC_CARD null-suppression, empty state ~line 51-93)
    - frontend/src/components/charts/EndgameTypeTcCard.tsx (Task 2)
    - frontend/src/lib/primaryTc.ts (Task 1 — computePrimaryTc)
    - frontend/src/pages/Endgames.tsx (mount site ~line 648 — currently passes `categories` + `endgame_games`; the filter state object available for the D-12 reset key)
    - frontend/src/generated/endgameZones.ts (`MIN_GAMES_PER_TC_CARD`)
    - frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx (the 4 test suites — the locked `grid-cols-3` + 5-card + by-total-sort assertions that MUST change, SC-10)
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md §6 (per-TC scaffolding), §7 (controlled accordion + D-12 reset via useEffect or key), §8 (test changes), Pitfall 3 + Pitfall 6
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md "EndgameTypeBreakdownSection.tsx" section, "Accordion Controlled Pattern", "TC Ordering + Null Suppression"
    - CHANGELOG.md ([Unreleased] section format)
  </read_first>
  <action>
    Rewrite `frontend/src/components/charts/EndgameTypeBreakdownSection.tsx` as a controlled-accordion orchestrator. Keep the outer `<section>` wrapper, its `data-testid="endgame-type-breakdown-section"`, `aria-label`, and the sub-question heading copy. Change props: accept `categoriesByTc: Record<'bullet'|'blitz'|'rapid'|'classical', EndgameCategoryStats[]> | undefined` (from `stats.categories_by_tc`) instead of consuming the pooled `categories` for rendering; gate with `if (!categoriesByTc) return null` (Pitfall 6). Compute `eligibleTcs` in fixed `['bullet','blitz','rapid','classical']` order, including only TCs whose summed `total >= MIN_GAMES_PER_TC_CARD` (SC-7 self-suppression); compute `grandTotal` across eligible TCs. Empty state: when no eligible TC, render the muted "No endgame type data yet. Import more games to see this section." message with a `data-testid="endgame-type-breakdown-empty"`. Render a controlled `<Accordion type="single" collapsible value={expandedTc} onValueChange={setExpandedTc} className="flex flex-col gap-2 mt-2">` mapping `eligibleTcs` to `<EndgameTypeTcCard key={tc} tc={tc} categories={categoriesByTc[tc] ?? []} grandTotal={grandTotal} onCategorySelect={onCategorySelect} />`. Initialize `const [expandedTc, setExpandedTc] = useState<string>(() => computePrimaryTc(categoriesByTc, MIN_GAMES_PER_TC_CARD) ?? '')` (D-09 primary expanded). Implement D-12 reset: a `useEffect` keyed on a `filterKey` prop (or the filter-dependency inputs) that calls `setExpandedTc(computePrimaryTc(categoriesByTc, MIN_GAMES_PER_TC_CARD) ?? '')` — recompute + reset on any filter change; manual expand persists within a stable filter set (controlled `value`, Pitfall 3). In `frontend/src/pages/Endgames.tsx`, update the `EndgameTypeBreakdownSection` mount to pass `categoriesByTc={overviewData?.stats.categories_by_tc}` and the filter-state-derived `filterKey` (serialize the active filter params) instead of `categories`; leave all other sections untouched. Update `frontend/src/components/charts/__tests__/EndgameTypeBreakdownSection.test.tsx` (SC-10): remove the `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` assertion and the 5-card / by-total-sort assertions; add assertions that (a) the section renders one accordion item per eligible TC in bullet/blitz/rapid/classical order with the trigger testids, (b) the primary TC (highest games × NOMINAL_DURATION) is the expanded item by default, (c) each card renders 4 tiles with NO Mixed tile (SC-3), (d) a TC with summed total `< MIN_GAMES_PER_TC_CARD` is suppressed (SC-7), (e) changing the `filterKey` prop resets the expanded card to the recomputed primary (D-12). Build `categoriesByTc` fixtures with realistic per-(class, TC) totals. Add a `## [Unreleased]` `### Changed` (+ `### Added`) bullet in `CHANGELOG.md` recording: the Endgame Type Breakdown restructured into full-width collapsible per-TC cards with the primary TC expanded by default, and the Conversion/Recovery gauges re-introduced banded against per-(class × TC) benchmarks (reference Phase 98). Run `npm run knip` and remove any now-dead exports left by the old 3-col grid path.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameTypeBreakdownSection && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `EndgameTypeBreakdownSection.tsx` does NOT contain the string `lg:grid-cols-3`.
    - It contains `computePrimaryTc`, `Accordion`, `value={expandedTc}`, `setExpandedTc`, and `MIN_GAMES_PER_TC_CARD`.
    - It renders `EndgameTypeTcCard` for eligible TCs and gates on `categoriesByTc` presence (`return null` when undefined).
    - `Endgames.tsx` passes `categories_by_tc` (as `categoriesByTc`) and a `filterKey` to the section.
    - The breakdown test file no longer asserts `grid-cols-3` or a 5-card layout, and asserts: per-TC accordion items, primary expanded, 4 tiles with no Mixed, sub-floor TC suppression, and filter-change reset.
    - `CHANGELOG.md` `[Unreleased]` contains a bullet mentioning the Endgame Type Breakdown restructure + Conv/Recov gauge re-introduction.
    - `cd frontend && npm test -- --run && npm run lint && npm run knip` exit 0.
  </acceptance_criteria>
  <done>The Endgame Type Breakdown renders as full-width stacked collapsible per-TC cards with the primary TC expanded, sub-floor TCs suppressed, four tiles per card (no Mixed), accordion resetting to the recomputed primary on filter change; `Endgames.tsx` feeds `categories_by_tc`; all frontend gates (test/lint/knip/tsc) are green and the CHANGELOG records the change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → React render | `stats.categories_by_tc` (already user-scoped server-side) is rendered client-side; no new untrusted input introduced |
| User keyboard/pointer → accordion | Radix Accordion handles toggle; controlled `value` is client state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Information Disclosure | `categoriesByTc` render | accept | Pure client-side rendering of the authenticated user's own stats payload (scoped server-side in Plan 98-01 / existing endpoint). No new data fetched, no cross-user surface. |
| T-98-05 | Tampering | `noUncheckedIndexedAccess` index access on `categoriesByTc[tc]` / `PER_CLASS_TC_GAUGE_ZONES[class][tc]` | mitigate | All index accesses narrowed (`?? []`, explicit guards) per the enabled TS flag; pawnless/missing-class paths fall back to undefined and skip gauge banding rather than crashing (Pitfall 5/8). |
| T-98-06 | Denial of Service | Accordion filter-change reset loop | accept | `useEffect` keyed on a stable serialized `filterKey` string; recompute is O(4 TCs); no unbounded re-render. Controlled `value` avoids the stale-suppressed-card hazard (Pitfall 3). |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — reuses existing Radix accordion, lucide icons, theme utils. No legitimacy gate needed. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` green (primaryTc, EndgameTypeCard, EndgameTypeBreakdownSection).
- `cd frontend && npm run lint && npm run knip && npx tsc --noEmit` exit 0.
- Full pre-PR checklist (backend + frontend) green before `/gsd-verify-work` (SC-10).
- Manual UAT (per 98-VALIDATION.md): desktop + mobile responsive staircase renders with divider grammar (not boxes); primary-TC default-expand picks the time-weighted TC for a real multi-TC user.
</verification>

<success_criteria>
- SC-1: full-width vertically-stacked collapsible per-TC cards replace the 3-col grid.
- SC-2: primary TC expanded by default (games × NOMINAL_DURATION argmax), keyboard-accessible headers with data-testids.
- SC-3: 2×2-family grid of four tiles (rook/minor_piece/pawn/queen); Mixed absent; pawnless hidden.
- SC-4: Conv/Recov gauges restored, banded against per-(class × TC) IQR.
- SC-5: Score Gap banded per-(class × TC).
- SC-7: TC card / tile self-suppress below the games floor.
- SC-9: desktop + mobile render responsively (full-width, no ragged multi-column).
- SC-10: frontend gates pass; existing tests updated; CHANGELOG [Unreleased] entry added.
</success_criteria>

<output>
Create `.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-02-SUMMARY.md` when done
</output>
