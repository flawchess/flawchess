---
phase: 98-per-tc-collapsible-endgame-type-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_zones.py
  - scripts/gen_endgame_zones_ts.py
  - frontend/src/generated/endgameZones.ts
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - frontend/src/types/endgames.ts
  - tests/test_endgame_zones.py
  - tests/test_endgame_service.py
  - tests/test_insights_service.py
autonomous: true
requirements: [standalone]
nyquist_compliant: true

must_haves:
  truths:
    - "Per-(class × TC) Conversion/Recovery/ScoreGap bands exist in generated endgameZones.ts (SC-6)"
    - "The /stats endgame breakdown exposes per-(class × TC) rates + counts via categories_by_tc (SC-8)"
    - "The LLM insights path response shape is unchanged — categories list and assign_per_class_zone untouched (SC-8)"
  artifacts:
    - path: "app/services/endgame_zones.py"
      provides: "PER_CLASS_TC_GAUGE_ZONES dict (5 classes × 4 TCs) alongside unchanged PER_CLASS_GAUGE_ZONES"
      contains: "PER_CLASS_TC_GAUGE_ZONES"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "PER_CLASS_TC_GAUGE_ZONES export consumed by the type tiles"
      contains: "PER_CLASS_TC_GAUGE_ZONES"
    - path: "app/services/endgame_service.py"
      provides: "_aggregate_endgame_stats_by_tc single-pass aggregation"
      contains: "_aggregate_endgame_stats_by_tc"
    - path: "app/schemas/endgames.py"
      provides: "categories_by_tc optional field on EndgameStatsResponse"
      contains: "categories_by_tc"
    - path: "frontend/src/types/endgames.ts"
      provides: "categories_by_tc optional field on EndgameStatsResponse TS interface"
      contains: "categories_by_tc"
  key_links:
    - from: "scripts/gen_endgame_zones_ts.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "codegen drift gate"
      pattern: "PER_CLASS_TC_GAUGE_ZONES"
    - from: "app/services/endgame_service.py query_endgame_overview"
      to: "EndgameStatsResponse.categories_by_tc"
      via: "_aggregate_endgame_stats_by_tc(bucket_rows)"
      pattern: "categories_by_tc="
---

<objective>
Backend foundation for the per-TC collapsible endgame type cards: add the per-(class × TC) benchmark band registry, regenerate the TS zone file through the CI drift gate, and expose per-(class × TC) rates + counts on the `/stats` endgame breakdown — all additive, leaving the LLM insights path untouched.

Purpose: The frontend tile grid (Plan 98-02) needs (a) per-(class × TC) gauge/ΔES bands in `endgameZones.ts` and (b) a `categories_by_tc` payload to render. This plan produces both. Honors D-13 (per-(class × TC) rates + counts, NO eligible-count weighting payload), D-14 (new `PER_CLASS_TC_GAUGE_ZONES` ALONGSIDE the existing per-class registry, codegen drift gate green), and D-15 (LLM path response shape preserved, additive only).

Output: `PER_CLASS_TC_GAUGE_ZONES` in `endgame_zones.py` + generated TS, `_aggregate_endgame_stats_by_tc` + `categories_by_tc` on `EndgameStatsResponse` (Python + TS), and tests proving the band counts, the drift gate, the aggregation shape, and the LLM-path-unchanged invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-CONTEXT.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md
@.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PER_CLASS_TC_GAUGE_ZONES registry + regenerate endgameZones.ts</name>
  <files>app/services/endgame_zones.py, scripts/gen_endgame_zones_ts.py, frontend/src/generated/endgameZones.ts, tests/test_endgame_zones.py</files>
  <read_first>
    - app/services/endgame_zones.py (current: `PerClassBands` dataclass ~line 445, `PER_CLASS_GAUGE_ZONES`, `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` ~line 538, `TC_METRIC_BANDS`, `MIN_GAMES_PER_TC_CARD`, `assign_per_class_zone`)
    - scripts/gen_endgame_zones_ts.py (imports block ~line 30-40, `_format_per_class_gauge_zones` ~line 98, `_format_tc_metric_bands` ~line 132, `_render` ~line 154, TC_METRIC_BANDS emission ~line 271)
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md §1 (the exact p25/p75 band tuples — numeric source of truth) and §2 (recommended structure)
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md "endgame_zones.py" + "gen_endgame_zones_ts.py" sections (the `PerClassTcBands` dataclass shape, `_format_per_class_tc_gauge_zones` analog, the `_render` emission block)
  </read_first>
  <action>
    In `app/services/endgame_zones.py`, add a frozen `PerClassTcBands` dataclass (fields `conversion: tuple[float, float]`, `recovery: tuple[float, float]`, `achievable_score_gap: tuple[float, float]`) and a `PER_CLASS_TC_GAUGE_ZONES: Mapping[EndgameClass, Mapping[Literal["bullet", "blitz", "rapid", "classical"], PerClassTcBands]]` AFTER `PER_CLASS_GAUGE_ZONES` and BEFORE `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`. Populate exactly 5 classes (rook, minor_piece, pawn, queen, mixed) × 4 TCs = 20 entries using the (p25, p75) Conversion + Recovery tuples from RESEARCH.md §1 and the per-class ΔES `achievable_score_gap` tuple repeated across all 4 TCs (rook=(-0.05,0.05), minor_piece=(-0.04,0.06), pawn=(-0.04,0.05), queen=(-0.04,0.05), mixed=(-0.03,0.04)) per D-04/D-14. OMIT pawnless (below per-(class × TC) floor; hidden in UI). Add an inline comment on `queen` `classical` noting `conversion=(0.88, 1.00)` and `recovery=(0.00, 0.09)` are small-n benchmark artifacts (n≈30–35) accepted as-is per the "use benchmark numbers" rule (RESEARCH Open Question 1). Do NOT modify `PER_CLASS_GAUGE_ZONES` or `assign_per_class_zone` (D-15). In `scripts/gen_endgame_zones_ts.py`: import `PER_CLASS_TC_GAUGE_ZONES` in the existing `from app.services.endgame_zones import (...)` block; add `_format_per_class_tc_gauge_zones()` mirroring `_format_tc_metric_bands` (nested class→TC Record literal); add an emission block in `_render()` after the `TC_METRIC_BANDS` block emitting `export const PER_CLASS_TC_GAUGE_ZONES: Record<EndgameClassKey, Record<'bullet' | 'blitz' | 'rapid' | 'classical', { conversion: [number, number]; recovery: [number, number]; achievable_score_gap: [number, number] }>> = { ... } as const;`. Run `uv run python scripts/gen_endgame_zones_ts.py` to regenerate `frontend/src/generated/endgameZones.ts` and commit the result. In `tests/test_endgame_zones.py` add a test asserting `PER_CLASS_TC_GAUGE_ZONES` has the 5 visible classes, each with all 4 TC keys, that pawnless is absent, that `PER_CLASS_GAUGE_ZONES` is unchanged (still contains pawnless), and a spot-check of one tuple (e.g. `PER_CLASS_TC_GAUGE_ZONES["rook"]["classical"].conversion == (0.74, 0.87)`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_zones.py -k endgame_zones -x && uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PER_CLASS_TC_GAUGE_ZONES" app/services/endgame_zones.py` ≥ 1 and `PER_CLASS_GAUGE_ZONES` still defined unchanged (pawnless entry present).
    - `python -c "from app.services.endgame_zones import PER_CLASS_TC_GAUGE_ZONES as z; assert set(z)=={'rook','minor_piece','pawn','queen','mixed'}; assert all(set(v)=={'bullet','blitz','rapid','classical'} for v in z.values()); assert z['queen']['classical'].recovery==(0.00,0.09)"` exits 0.
    - `frontend/src/generated/endgameZones.ts` contains `export const PER_CLASS_TC_GAUGE_ZONES`.
    - `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0 (drift gate green).
    - `uv run ty check app/ tests/` and `uv run ruff check app/ tests/ scripts/` exit 0.
  </acceptance_criteria>
  <done>20 per-(class × TC) band entries exist in the Python registry and the regenerated TS file; the CI drift gate is green; `PER_CLASS_GAUGE_ZONES` and `assign_per_class_zone` are byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add _aggregate_endgame_stats_by_tc + categories_by_tc on EndgameStatsResponse (LLM path unaffected)</name>
  <files>app/services/endgame_service.py, app/schemas/endgames.py, frontend/src/types/endgames.ts, tests/test_endgame_service.py, tests/test_insights_service.py</files>
  <read_first>
    - app/services/endgame_service.py (`_aggregate_endgame_stats` — its grouping by `endgame_class_int` only, its WDL/conv/recov accumulators, `_compute_score_p_value` / `derive_user_result` helpers; `_compute_per_tc_metric_cards` ~line 2287 as the single-pass-over-bucket_rows-grouping-by-row[6] model; `query_endgame_overview` call site ~line 3447 where `EndgameStatsResponse` is built; the bucket_rows column layout: game_id[0], endgame_class[1], result[2], user_color[3], eval_cp[4], eval_mate[5], time_control_bucket[6], next_entry_eval_cp[7], next_entry_eval_mate[8])
    - app/schemas/endgames.py (`EndgameStatsResponse` ~line 211, `EndgameCategoryStats`, `ConversionRecoveryStats` definitions — confirm the actual current field set, RESEARCH Pitfall 8)
    - frontend/src/types/endgames.ts (`EndgameStatsResponse` interface ~line 67, `EndgameCategoryStats`)
    - app/services/insights_service.py (`_findings_conversion_recovery_by_type` — confirm it reads `response.stats.categories`, NOT `categories_by_tc`, and calls `assign_per_class_zone`)
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-PATTERNS.md "endgame_service.py" + "schemas/endgames.py" + "types/endgames.ts" sections
    - .planning/phases/98-per-tc-collapsible-endgame-type-cards/98-RESEARCH.md §3 (recommended response shape, Pitfall 6 optional-for-back-compat, Pitfall 7 per-(class,TC) score_p_value)
  </read_first>
  <action>
    Add `_aggregate_endgame_stats_by_tc(rows: Sequence[Row[Any] | tuple[Any, ...]]) -> dict[Literal["bullet","blitz","rapid","classical"], list[EndgameCategoryStats]]` to `app/services/endgame_service.py`, modeled on `_compute_per_tc_metric_cards`: single pass over the already-fetched `bucket_rows`, group by `(row[6] tc, row[1] endgame_class_int)`, reusing the same WDL/conv/recov accumulation, `derive_user_result`, span-gap (eval cols 4/5/7/8) and `_compute_score_p_value` logic that `_aggregate_endgame_stats` uses per class — so each per-(class, TC) `EndgameCategoryStats` carries `score_p_value` computed the same way (null when `total` below the p-value reliability floor, per Pitfall 7). Return the dict keyed in fixed `["bullet","blitz","rapid","classical"]` order, each value a list of `EndgameCategoryStats` in rook/minor_piece/pawn/queen order (include mixed in data for completeness; pawnless omitted). Do NOT add any eligible-count weighting payload (D-13). In `query_endgame_overview`, after the existing `_compute_per_tc_metric_cards` call, add `categories_by_tc = _aggregate_endgame_stats_by_tc(bucket_rows)` and pass it into the `EndgameStatsResponse(...)` constructor as `categories_by_tc=categories_by_tc` — leaving `categories`, `total_games`, `endgame_games` exactly as they are. In `app/schemas/endgames.py`, add `categories_by_tc: dict[Literal["bullet","blitz","rapid","classical"], list[EndgameCategoryStats]] | None = None` to `EndgameStatsResponse` (optional for back-compat, Pitfall 6). In `frontend/src/types/endgames.ts`, add optional `categories_by_tc?: Record<'bullet' | 'blitz' | 'rapid' | 'classical', EndgameCategoryStats[]>` to the `EndgameStatsResponse` interface. In `tests/test_endgame_service.py`, add a test feeding known fixture bucket_rows spanning ≥2 TCs and ≥2 classes to `_aggregate_endgame_stats_by_tc`, asserting the dict keys, per-(class, TC) totals/WDL split, and that conversion/recovery rates match hand-computed values. In `tests/test_insights_service.py` (D-15 regression), add a test asserting `_findings_conversion_recovery_by_type` still reads `categories` (not `categories_by_tc`) and that an `EndgameStatsResponse` with `categories_by_tc` populated produces the same LLM findings as one without it (response shape preserved).
  </action>
  <verify>
    <automated>uv run pytest tests/test_endgame_service.py -k endgame_service tests/test_insights_service.py -k insights -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_aggregate_endgame_stats_by_tc" app/services/endgame_service.py` ≥ 2 (definition + call site).
    - `grep -c "categories_by_tc" app/schemas/endgames.py` ≥ 1 and the field type includes `| None = None`.
    - `grep -c "categories_by_tc" frontend/src/types/endgames.ts` ≥ 1 and it is declared optional (`?:`).
    - `_findings_conversion_recovery_by_type` source still references `categories` and still calls `assign_per_class_zone`; the D-15 regression test passes (LLM findings identical with/without `categories_by_tc`).
    - `uv run pytest tests/ -k "endgame_service or insights or endgame_zones"` exits 0.
    - `uv run ty check app/ tests/` exits 0 (Sequence/Literal types correct, explicit return annotations present).
  </acceptance_criteria>
  <done>`/stats` (`query_endgame_overview`) returns a populated `categories_by_tc` keyed by the four TCs with per-(class, TC) `EndgameCategoryStats` (including `score_p_value`); the schema field is optional; the TS type mirrors it; the LLM path reads only `categories` and its findings are provably unchanged when `categories_by_tc` is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/endgames/overview` (`/stats`) | Authenticated user requests their own endgame breakdown; filters (TC, color, recency, opponent) cross here as untrusted query params, already validated + scoped by `apply_game_filters` |
| Python zone registry → generated TS | Build-time codegen; no runtime user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Information Disclosure | `categories_by_tc` field on `EndgameStatsResponse` | mitigate | New field is derived from the same `bucket_rows` already scoped to the authenticated user via the existing `apply_game_filters` + user-id scoping in `query_endgame_bucket_rows`; no new query, no cross-user data path introduced (no IDOR regression). Additive aggregation over already-authorized rows only. |
| T-98-02 | Tampering | `_aggregate_endgame_stats_by_tc` row indexing | accept | Reads fixed positional columns of internally-produced `bucket_rows`; no user-controlled column selection. Out-of-range/typed access guarded by ty + existing row shape. Low risk. |
| T-98-03 | Information Disclosure | LLM insights path (D-15) | mitigate | `categories_by_tc` is additive; `_findings_conversion_recovery_by_type` continues to read only `categories`. Regression test asserts identical findings, preventing accidental exposure of per-TC slices into the LLM payload. |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — pure code + codegen against existing stack (Python 3.13/uv, FastAPI, SQLAlchemy). No legitimacy gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/ -k "endgame_zones or endgame_service or insights"` green.
- `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` exits 0 (drift gate).
- `uv run ruff check app/ tests/ scripts/` and `uv run ty check app/ tests/` exit 0.
- `PER_CLASS_GAUGE_ZONES` and `assign_per_class_zone` are unchanged (LLM path preserved, D-15).
</verification>

<success_criteria>
- SC-6: per-(class × TC) Conv/Recov + ΔES bands generated into `endgameZones.ts`, drift gate green, no eligible-count weighting payload added.
- SC-8: `/stats` exposes per-(class × TC) rates + counts (`categories_by_tc`); LLM insights path response shape preserved (additive only).
</success_criteria>

<output>
Create `.planning/phases/98-per-tc-collapsible-endgame-type-cards/98-01-SUMMARY.md` when done
</output>
