---
phase: 134-trapped-piece-fixture-expansion-option-b-cook-predicate-reim
plan: 03
type: execute
wave: 3
depends_on:
  - 134-02
files_modified:
  - tests/scripts/tagger/precision_floors.py
autonomous: true
requirements: []    # none mapped; traceability via ROADMAP Phase 134 D-EXP-03 (conditional unsuppress)

must_haves:
  truths:
    - "The ship/suppress decision is driven SOLELY by Plan 02's recorded post-dispatch trapped-piece precision against the ~≥0.80 TRAIN-holding-on-TEST bar (D-EXP-03)"
    - "SHIP branch: trapped-piece removed from SUPPRESSED_MOTIFS and a PRECISION_FLOOR added ~5-8pp below measured TRAIN — and the harness still passes"
    - "SUPPRESS branch: trapped-piece stays in SUPPRESSED_MOTIFS with NO PRECISION_FLOOR — but the improved detector + expanded fixtures (Plans 01/02) are still committed"
    - "Either way, the precision_floors.py docstring measurement note reflects the new (larger-fixture) trapped-piece numbers"
    - "No family-map / frontend / family-count-test edits are needed (Phase 129 already wired them)"
  artifacts:
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Conditional unsuppress: SUPPRESSED_MOTIFS membership + optional PRECISION_FLOOR + refreshed measurement note, per the D-EXP-03 outcome"
      contains: "trapped-piece"
  key_links:
    - from: "tests/scripts/tagger/precision_floors.py::PRECISION_FLOOR"
      to: "tests/scripts/tagger/test_detector_precision.py"
      via: "if shipped, the gate asserts trapped-piece TRAIN precision >= its new floor"
      pattern: "trapped-piece"
---

<objective>
Apply the D-EXP-03 conditional unsuppress decision for trapped-piece based on the post-dispatch precision Plan 02 measured and recorded. This is an explicit two-branch plan: SHIP if precision clears ~≥0.80 TRAIN holding on TEST, otherwise keep SUPPRESSED — with the improved detector and expanded fixtures committed regardless.

Purpose: D-EXP-03 forbids a forced ship. The bar is a worthwhile floor (~≥0.80 TRAIN holding on TEST). Below it, shipping a low-precision motif would erode tag trust; above it, trapped-piece becomes a real surfaced motif. Either outcome is a success: even the suppressed branch removes the 153-FP source from the eventual ship surface and leaves a real (~1000-row) test set behind.

Output (exactly one branch, chosen by the Plan-02 number):
- SHIP: trapped-piece removed from `SUPPRESSED_MOTIFS`, a `PRECISION_FLOOR["trapped-piece"]` added ~5-8pp below measured TRAIN (rounded to 0.01-0.05, matching existing comment style), measurement note refreshed.
- SUPPRESS: trapped-piece stays in `SUPPRESSED_MOTIFS` with no floor; the measurement note is refreshed to the new fixture size + the measured-but-below-bar precision, citing D-EXP-03.

The mechanical unsuppress steps 3-5 from the generic Phase 133 recipe (FAMILY_TO_MOTIF_INTS, frontend TACTIC_GROUPS / tacticComparisonMeta.ts, family-count test) are ALREADY DONE by Phase 129's 10-family taxonomy (RESEARCH §"The unsuppress mechanical path") — so the only edits in either branch live in `precision_floors.py`. Verify (do not re-add) that the family/chip wiring is present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/134-trapped-piece-fixture-expansion-option-b-cook-predicate-reim/134-RESEARCH.md
@.planning/phases/134-trapped-piece-fixture-expansion-option-b-cook-predicate-reim/134-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `tests/scripts/tagger/precision_floors.py`:
  - SHIP branch: remove the `"trapped-piece"` line from `SUPPRESSED_MOTIFS`; add `PRECISION_FLOOR["trapped-piece"]` with a measured-value comment.
  - SUPPRESS branch: keep the `SUPPRESSED_MOTIFS` line; add no floor.
  - Both branches: refresh the trapped-piece measurement note (the stale 28-row note at L142-146, already partially updated by Plan 01) to the final post-rewrite numbers.
No detector change (Plan 02). No selector/fixture change (Plan 01). No family-map / frontend edits (Phase 129 wired them).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Read the Plan-02 measurement, then apply the D-EXP-03 ship-or-suppress branch</name>
  <read_first>
    - 134-02-SUMMARY.md — the RECORDED post-dispatch trapped-piece TRAIN precision / TEST precision / ΔP / recall / n. This number decides the branch. (If the SUMMARY is ambiguous, re-run `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --eval-set train 2>&1 | grep trapped-piece` and the `--eval-set test` variant to read TRAIN and TEST precision directly.)
    - tests/scripts/tagger/precision_floors.py lines 142-146 (trapped-piece measurement note), L198-220 (SUPPRESSED_MOTIFS — the `"trapped-piece"` member at ~L204), and the PRECISION_FLOOR dict (L242+, copy the comment style of a neighboring entry like fork/skewer).
    - 134-RESEARCH.md §"The unsuppress mechanical path (and what's gated on ≥0.80)" — the table showing steps 3-5 already done; only steps 1, 2, and the docstring need edits, gated on ≥0.80.
    - app/repositories/library_repository.py ~L137 (`FAMILY_TO_MOTIF_INTS["trapped_piece"]` — confirm present, do NOT edit) and frontend/src/lib/tacticComparisonMeta.ts ~L260 (confirm trapped_piece chip present, do NOT edit).
  </read_first>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <action>
    Read the Plan-02 recorded post-dispatch numbers, then take EXACTLY ONE branch. The bar (D-EXP-03): TRAIN precision ~≥0.80 AND TEST precision holding (TEST not materially below TRAIN — use the existing ΔP-within-0.10 generalization convention the report already applies; a TEST that craters relative to TRAIN means it does NOT hold).

    SHIP BRANCH (TRAIN ≥ ~0.80 and TEST holds):
    1. Remove the `"trapped-piece"` member line from `SUPPRESSED_MOTIFS` (and its trailing comment).
    2. Add `PRECISION_FLOOR["trapped-piece"]` set ~5-8pp below the measured TRAIN, rounded to 0.01-0.05, with a comment in the existing style citing `train X / test Y (N TP, M FP; phase 134 cook trapped-piece port)`.
    3. Refresh the L142-146 measurement note to the new fixture size + measured numbers + "shipped phase 134".
    4. Confirm (do NOT edit) the family map + frontend chip + family-count test already carry trapped_piece (RESEARCH table steps 3-5). If by some chance one is missing, STOP and flag it rather than silently adding scope — RESEARCH asserts they are present.

    SUPPRESS BRANCH (TRAIN < ~0.80 or TEST does not hold):
    1. Leave `"trapped-piece"` in `SUPPRESSED_MOTIFS`; update its trailing comment to the new measured precision (e.g. `# train 0.6x / test 0.6y — below the 0.80 bar; D-EXP-03 keeps suppressed, detector+fixtures committed`).
    2. Add NO `PRECISION_FLOOR`.
    3. Refresh the L142-146 measurement note to the new ~1000-row fixture size + the measured-but-below-bar numbers, citing D-EXP-03 ("kept suppressed; improved detector + expanded fixtures committed; FP source removed from the eventual ship surface").

    Either branch: do NOT touch the detector, the selector, the fixtures, the family map, the frontend, or any GOALS entry. The GOALS entry from Plan 02 stays (it is an aspirational /loop target, not the floor gate). Record the chosen branch and the deciding numbers in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/test_detector_precision.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one branch was applied, and the SUMMARY states which, with the deciding TRAIN/TEST precision quoted from the Plan-02 measurement.
    - SHIP branch only: `grep -c '"trapped-piece"' tests/scripts/tagger/precision_floors.py` shows it absent from SUPPRESSED_MOTIFS and present in PRECISION_FLOOR; `uv run pytest tests/scripts/tagger/test_detector_precision.py -x -q` PASSES with trapped-piece now floor-gated and meeting its floor.
    - SUPPRESS branch only: trapped-piece remains in SUPPRESSED_MOTIFS, has NO PRECISION_FLOOR entry, and the gate PASSES (it is skipped from the floor assertion); the improved detector + expanded fixtures from Plans 01/02 remain committed.
    - Both branches: the L142-146 measurement note no longer describes the 28-row fixture; it reflects the ~1000-row fixture and the final measured numbers.
    - No edit to library_repository.py, tacticComparisonMeta.ts, tacticMotifDefinitions.ts, or any family-count test (verified-present, not re-added).
  </acceptance_criteria>
  <done>The D-EXP-03 decision is applied as a single explicit branch; the tagger gate is green either way and the measurement note is current.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full pre-merge gate and update the CHANGELOG</name>
  <read_first>
    - CLAUDE.md §"Pre-merge gate (MANDATORY before squash-merging to main)" — the exact command set.
    - CLAUDE.md §"Changelog & Releases" / "Per-phase" — append one or more bullets under `## [Unreleased]`, grouped into Added/Changed/Fixed/Tests, referencing the phase, terse + user-facing.
    - CHANGELOG.md `## [Unreleased]` section — the current structure to append into.
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    1. Run the full backend pre-merge gate (this phase touches no frontend files — confirm via `git status` that no `frontend/` file changed across Plans 01-03; if any did, also run `( cd frontend && npm run lint && npm test -- --run )`):
       `uv run ruff format app/ tests/ scripts/` ; `uv run ruff check app/ tests/ scripts/ --fix` ; `uv run ty check app/ tests/` ; `uv run pytest -n auto -x`.
       Resolve every failure. Commit any formatter/lint-only diff with a `style(134):` prefix.
    2. Append a CHANGELOG bullet under `## [Unreleased]` describing the phase outcome in user-facing terms, branch-aware:
       - SHIP branch → `### Added`: trapped-piece is now a surfaced tactic tag (precision X). And `### Fixed`: eliminated the trapped-piece false-positive source.
       - SUPPRESS branch → `### Changed`/`### Fixed`: rebuilt the trapped-piece detector and expanded its CC0 fixture to ~1000 puzzles, removing the prior false-positive source (kept internally-suppressed pending a higher-precision pass).
       Reference Phase 134. Keep it terse; do not describe the cook port mechanics.
    Note: the actual squash-merge to `main` is performed by the human / ship workflow after `/gsd-verify-work`, not by this plan. This task only ensures the gate is green and the changelog is staged.
  </action>
  <verify>
    <automated>uv run pytest -n auto -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -n auto -x` PASSES (full backend suite).
    - `uv run ty check app/ tests/` reports zero errors; `uv run ruff check app/ tests/ scripts/` is clean.
    - `## [Unreleased]` in CHANGELOG.md has a new Phase 134 bullet matching the chosen branch.
    - If any `frontend/` file changed, the frontend lint+test gate also passed (otherwise N/A — recorded in SUMMARY).
  </acceptance_criteria>
  <done>The full backend gate is green and the CHANGELOG reflects the Phase 134 outcome, ready for verify/ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Edits a test-only constants module (`precision_floors.py`) + CHANGELOG. No runtime code, no request path, no DB, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-03 | Tampering | precision_floors.py (CI gate constants) | accept | A mis-set floor is a CI signal-strength issue, not a security event; the gate itself catches detector regressions. No external surface. |
| T-134-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate needed. |

No new attack surface (test-constant + changelog edits only).
</threat_model>

<verification>
- `uv run pytest tests/scripts/tagger/test_detector_precision.py -x -q` — tagger gate green in the chosen branch.
- `uv run pytest -n auto -x` — full backend suite green.
- `uv run ty check app/ tests/` — zero errors.
- `uv run ruff check app/ tests/ scripts/ --fix && uv run ruff format app/ tests/ scripts/` — clean.
- CHANGELOG `## [Unreleased]` has the Phase 134 bullet.
</verification>

<success_criteria>
- The D-EXP-03 decision is applied as exactly one explicit branch, driven solely by Plan 02's measured precision.
- The tagger gate and full suite are green either way.
- The improved detector + expanded fixtures are committed regardless of branch.
- No family-map / frontend / family-count-test edits were needed (Phase 129 already wired them).
</success_criteria>

<output>
Create `.planning/phases/134-trapped-piece-fixture-expansion-option-b-cook-predicate-reim/134-03-SUMMARY.md` when done. State the chosen branch (SHIP / SUPPRESS), the deciding TRAIN/TEST precision, the floor value if shipped, and confirmation that the family/chip wiring was verified-present (not re-added).
</output>
