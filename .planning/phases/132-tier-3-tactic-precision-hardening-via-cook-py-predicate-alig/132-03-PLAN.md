---
phase: 132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig
plan: 03
type: execute
wave: 3
depends_on: ["132-02"]
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/precision_floors.py
autonomous: true
requirements: [D-01, D-05]
must_haves:
  truths:
    - "detect_capturing_defender enforces cook's exact AND-chain including the init-board defender test (the captured piece used to defend the capture square before the prior pov move) and returns TACTIC_CONFIDENCE_HIGH when it fires"
    - "detect_intermezzo enforces cook's exact AND-chain including the moves[k-3] original-capture signature and the was-legal-earlier condition, guarded by k >= 4, and returns TACTIC_CONFIDENCE_HIGH when it fires"
    - "Each motif is measured on TEST and shipped (with a floor) or suppressed by its TEST number per D-01"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "Cook-aligned detect_capturing_defender and detect_intermezzo"
      contains: "def detect_capturing_defender"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Updated SUPPRESSED_MOTIFS / PRECISION_FLOOR for the two motifs"
      contains: "SUPPRESSED_MOTIFS"
  key_links:
    - from: "app/services/tactic_detector.py"
      to: "scripts/tactic_tagger_report.py"
      via: "detect_tactic_motif post-dispatch winner scored at line ~193"
      pattern: "detect_tactic_motif"
  prohibitions:
    - "Must NOT edit detect_tactic_motif (the shallowest-wins dispatcher) — read only"
    - "Must NOT edit detect_interference (1.00 TEST regression lock)"
    - "Must NOT copy cook.py source (AGPL-3.0) — reimplement from RESEARCH.md pseudocode only"
    - "Must NOT reintroduce a _grade(met, total) voting scheme in the rewritten detectors"
    - "Must NOT change tag cardinality / single-winner-per-flaw semantics"
    - "Must NOT reset the dev DB"
---

<objective>
Rewrite `detect_capturing_defender` and `detect_intermezzo` to cook.py's exact relational AND-chains, then measure each on TEST and ship-or-suppress by its TEST number. Both have moderate volume; capturing-defender may be salvageable, intermezzo is a likely-suppress but must be attempted at full fidelity per D-01.

Purpose: Complete the medium-ROI tier of the full-port-then-suppress sweep, following the `detect_interference` structural template.
Output: Cook-aligned `detect_capturing_defender` + `detect_intermezzo`; updated `precision_floors.py`; recorded TEST precision per motif.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-CONTEXT.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md
@.planning/notes/tactic-tagger-cook-alignment.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite detect_capturing_defender to cook's AND-chain (init-board defender test)</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_capturing_defender` ~line 1702 — current approximation; `detect_interference` template; `_is_hanging`, `_VALUES_NO_KING`, `TACTIC_CONFIDENCE_HIGH`)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Per-Motif cook.py Pseudocode" → section 3 `capturing-defender` — the full chain AND the "Corrected index mapping" / "Definitive reading" / "In our index terms" blocks; condition 9 is the init-board defender test: `defender_sq = moves[k-2].to_square`, `init_board = boards[k-2]`, `init_piece = init_board.piece_at(defender_sq)`, fire iff `init_piece is not None` AND `defender_sq in init_board.attackers(init_piece.color, move.to_square)` AND `not init_board.is_check()`; Pitfalls 2, 3, 6)
  </read_first>
  <behavior>
    - Fires (returns `(True, captured.piece_type, TACTIC_CONFIDENCE_HIGH, k)`) only when ALL cook conditions hold: checkmate-on-this-move OR a real capture; capturing piece not a king; value gate `_VALUES_NO_KING[captured] <= _VALUES_NO_KING[captor]`; captured piece hanging before capture (`_is_hanging`); prev_op_move not a recapture on this square; `boards[k-2]` not in check; prev pov move did not land on the capture-from square; AND the init-board defender test (condition 9, exact mapping above).
    - Does NOT fire on general "a defender was defending a target" cases lacking the specific init-board relational chain.
    - Returns `(False, None, 0, None)` otherwise; guard `k >= 2` (needs `boards[k-2]`).
  </behavior>
  <action>
    Replace the body of `detect_capturing_defender` with cook's exact AND-chain (RESEARCH.md section 3, using the "In our index terms" definitive mapping). Use `_VALUES_NO_KING` for the value comparison (Pitfall 3), `_is_hanging` for the hanging check (cook's `util.is_hanging`, not the ray-aware `_is_defended`), and `boards[k-2]` as the init board (Pitfall 2). Determine the capture from the board before the move (Pitfall 6). Return `(True, captured.piece_type, TACTIC_CONFIDENCE_HIGH, k)`; remove any `_grade(...)`. Keep nesting ≤4 (extract a `_capturing_defender_fires_at` helper if needed). Comment-cite Phase 132 D-01 / cook capturing-defender lines.
  </action>
  <verify>
    <automated>uv run ty check app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k "capturing_defender or capturing-defender"</automated>
  </verify>
  <acceptance_criteria>
    - `detect_capturing_defender` contains no `_grade(` and no `met >= ` voting threshold.
    - The init-board defender test (`init_board.attackers(...)` on `boards[k-2]`) is present and uses `boards[k-2]`, not `boards[k-1]`.
    - capturing-defender fixture/fast-guard tests pass.
    - `uv run ty check app/services/tactic_detector.py` reports zero errors.
  </acceptance_criteria>
  <done>detect_capturing_defender enforces cook's exact AND-chain with the init-board defender test, returning TACTIC_CONFIDENCE_HIGH.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite detect_intermezzo to cook's AND-chain (zwischenzug signature, k>=4 guard)</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_intermezzo` ~line 1468 — current 3-condition voting + its existing `len(moves) < 4` guard; `detect_interference` template; `TACTIC_CONFIDENCE_HIGH`)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Per-Motif cook.py Pseudocode" → section 5 `intermezzo` — the full chain: capture at k; opponent's just-moved piece was NOT attacking the capture square on `boards[k-2]`; prior pov move did not go to the capture square; the signature `moves[k-3].to_square == capture_square` AND `boards[k-3].piece_at(capture_square) is not None` AND pov's current move was already legal on `boards[k-2]`; Pitfall 5 — guard `k >= 4`, overall `if len(moves) < 4`)
  </read_first>
  <behavior>
    - Fires (returns `(True, None, TACTIC_CONFIDENCE_HIGH, k)`) only when ALL cook conditions hold for some pov capture at even k ≥ 4: it is a capture; the opponent piece that just moved (`moves[k-1].from_square`) was NOT in `boards[k-2].attackers(not pov, capture_square)`; `moves[k-2].to_square != capture_square`; AND the zwischenzug signature (`moves[k-3]` captured on the same square, that was a real capture on `boards[k-3]`, and pov's current move was already legal on `boards[k-2]`).
    - Does NOT fire on the loose "same square 2 moves ago + check + non-recapture" voting cases.
    - Returns `(False, None, 0, None)` when `len(moves) < 4` or no k qualifies.
  </behavior>
  <action>
    Replace the voting body of `detect_intermezzo` with cook's exact AND-chain (RESEARCH.md section 5). Keep/confirm the `if len(moves) < 4: return False, None, 0, None` guard and add `if k < 4: continue` in the loop (Pitfall 5 — avoid `moves[-1]` wraparound). Determine "was legal earlier" via membership of `moves[k].uci()` in `{m.uci() for m in boards[k-2].legal_moves}`. Determine captures from the board before the move (Pitfall 6). Return `(True, None, TACTIC_CONFIDENCE_HIGH, k)`; remove any `_grade(...)`. Keep nesting ≤4 (extract a `_intermezzo_fires_at` helper if needed). Comment-cite Phase 132 D-01 / cook intermezzo lines.
  </action>
  <verify>
    <automated>uv run ty check app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k intermezzo</automated>
  </verify>
  <acceptance_criteria>
    - `detect_intermezzo` contains no `_grade(` and no `met >= ` voting threshold.
    - A `k >= 4` (or equivalent) guard prevents `moves[k-3]` wraparound.
    - intermezzo fixture/fast-guard tests pass.
    - `uv run ty check app/services/tactic_detector.py` reports zero errors.
  </acceptance_criteria>
  <done>detect_intermezzo enforces cook's exact zwischenzug AND-chain with the k>=4 guard, returning TACTIC_CONFIDENCE_HIGH.</done>
</task>

<task type="auto">
  <name>Task 3: Measure capturing-defender + intermezzo on TEST and ship-or-suppress each</name>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <read_first>
    - tests/scripts/tagger/precision_floors.py (`SUPPRESSED_MOTIFS` frozenset — both motifs are currently suppressed; `PRECISION_FLOOR`; the two-file ship/suppress edit pattern in the docstring)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("tactic_confidence Suppression Lever" → ship vs suppress two-file edit; baseline table — both at ~0.25/0.17 TEST, likely stay suppressed)
  </read_first>
  <action>
    Run `--check-goals --eval-set train` then `--eval-set test`. For EACH of capturing-defender and intermezzo, apply the TEST-driven decision (D-01): if P(test) ≥ 0.9, REMOVE it from `SUPPRESSED_MOTIFS` and add a `PRECISION_FLOOR` entry ~5–7pp below measured TRAIN (rounded to 0.05); if P(test) < 0.9, KEEP it in `SUPPRESSED_MOTIFS` and update its comment to the new measured number with a `(Phase 132 full cook port)` note. Confirm `interference` still ~1.00 P(test). Record final Status + numbers in the SUMMARY.
  </action>
  <verify>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test; echo "ran"; uv run pytest tests/scripts/tagger/ -v -k "capturing or intermezzo or interference"</automated>
  </verify>
  <acceptance_criteria>
    - capturing-defender and intermezzo each have an explicit, recorded ship-or-suppress decision tied to a TEST number.
    - Suppressed motifs remain in `SUPPRESSED_MOTIFS` with updated Phase 132 comments; shipped ones are removed and given a floor.
    - `interference` P(test) still ~1.00.
    - The fixture suite (`-k "capturing or intermezzo or interference"`) passes.
  </acceptance_criteria>
  <done>Both medium-ROI motifs honestly resolved by their TEST number; interference unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure-CPU detector + config edit; no untrusted input, no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-03 | Tampering | detector predicate logic | accept | Deterministic CPU over committed CC0 fixtures; harness-validated. |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- `grep` confirms no `_grade(` / `met >= ` in detect_capturing_defender or detect_intermezzo.
- `uv run pytest tests/scripts/tagger/ -v -k "capturing or intermezzo or interference"` passes.
- `uv run ty check app/ tests/` and `uv run ruff check app/ tests/ scripts/` pass.
- `--eval-set test` recorded; interference still ~1.00.
</verification>

<success_criteria>
detect_capturing_defender and detect_intermezzo are cook-aligned AND-chains (no voting), each measured on TEST and shipped-or-suppressed by its TEST number, with the interference regression lock intact.
</success_criteria>

<output>
Create `.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-03-SUMMARY.md` when done — include per-motif TP/FP/P(train)/P(test) and the ship/suppress decision.
</output>

---

## Artifacts this plan produces

- `app/services/tactic_detector.py`: `detect_capturing_defender` rewritten to cook's AND-chain with the init-board defender test; `detect_intermezzo` rewritten to cook's zwischenzug AND-chain (k>=4 guard). Both return `TACTIC_CONFIDENCE_HIGH`, no `_grade` voting. Optional helpers `_capturing_defender_fires_at` / `_intermezzo_fires_at` (names may vary).
- `tests/scripts/tagger/precision_floors.py`: per-motif `SUPPRESSED_MOTIFS` / `PRECISION_FLOOR` edits for capturing-defender + intermezzo.
- (Newly-created/modified symbols — exclude from drift verification.)
