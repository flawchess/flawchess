---
phase: 132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - app/services/tactic_detector.py
  - tests/scripts/tagger/precision_floors.py
autonomous: true
requirements: [D-01, D-05]
must_haves:
  truths:
    - "detect_deflection enforces cook's exact 11-condition AND-chain (no met >= N voting) and returns TACTIC_CONFIDENCE_HIGH when it fires"
    - "detect_clearance enforces cook's exact AND-chain including the prior-pov-move from-square geometry (conditions 8 and 9) and returns TACTIC_CONFIDENCE_HIGH when it fires"
    - "Each of deflection and clearance is measured on the held-out TEST split; one that clears >0.9 ships with a PRECISION_FLOOR, one that misses is suppressed via SUPPRESSED_MOTIFS"
    - "deflection's 991-FP over-firing is substantially reduced versus the Phase 132 baseline"
  artifacts:
    - path: "app/services/tactic_detector.py"
      provides: "Cook-aligned detect_deflection and detect_clearance"
      contains: "def detect_deflection"
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Updated SUPPRESSED_MOTIFS / PRECISION_FLOOR reflecting ship-or-suppress per motif"
      contains: "SUPPRESSED_MOTIFS"
  key_links:
    - from: "app/services/tactic_detector.py"
      to: "scripts/tactic_tagger_report.py"
      via: "detect_tactic_motif post-dispatch winner scored at line ~193"
      pattern: "detect_tactic_motif"
  prohibitions:
    - "Must NOT edit detect_tactic_motif (the shallowest-wins dispatcher) — read only"
    - "Must NOT edit detect_interference (1.00 TEST regression lock)"
    - "Must NOT copy cook.py source (AGPL-3.0) — reimplement from RESEARCH.md pseudocode only"
    - "Must NOT reintroduce a _grade(met, total) voting scheme in the rewritten detectors"
    - "Must NOT change tag cardinality / single-winner-per-flaw GROUP BY tactic_motif semantics"
    - "Must NOT reset the dev DB"
---

<objective>
Rewrite `detect_deflection` and `detect_clearance` to cook.py's exact relational AND-chain predicates (replacing the current `met >= N` voting), then measure each on the held-out TEST split and ship-or-suppress per its TEST number. These are the two highest-ROI targets: deflection is the single biggest false-positive source in the entire detector (991 FP at 0.21 TEST), and clearance has meaningful volume at 0.37 TEST.

Purpose: Kill deflection's over-firing (the phase's clearest win, D-01) and lift clearance, following the `detect_interference` structural template (strict relational match → high confidence, no graded voting).
Output: Cook-aligned `detect_deflection` + `detect_clearance`; updated `precision_floors.py` reflecting ship-or-suppress; recorded TEST precision per motif.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-CONTEXT.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md
@.planning/notes/tactic-tagger-cook-alignment.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite detect_deflection to cook's 11-condition AND-chain</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_deflection` ~line 1366 — the current `met >= 3` voting body to replace; `detect_interference` ~line 1544 — the structural template; the shared utilities `_piece_value`, `_VALUES_NO_KING`, `_PIECE_VALUES`, `_is_defended`, `TACTIC_CONFIDENCE_HIGH`; the index convention comments)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Per-Motif cook.py Pseudocode" → section 1 `deflection` — the full 11-condition AND-chain and the index mapping; "Common Pitfalls" 1, 2, 3, 6; "interference as the Structural Template")
    - .planning/notes/tactic-tagger-cook-alignment.md (cook↔ours index convention; `boards[k-2]` = before prior pov move)
  </read_first>
  <behavior>
    - Fires (returns `(True, captured.piece_type, TACTIC_CONFIDENCE_HIGH, k)`) only when ALL 11 cook conditions hold for some pov move at even index k ≥ 2: capture-or-promotion; value gate via `_VALUES_NO_KING` (Pitfall 3); prior-pov-move quality gate; square-collision guards; the "forced/deflection geometry" disjunction (cond 9); square-reachability-from-original-position disjunction (cond 10); and the key guard cond 11 (deflected piece no longer covers the capture square from its new position).
    - Does NOT fire on the loose 3-of-5 cases the current voting scheme accepted (the FP pool, especially missing cond 9/10/11).
    - Returns `(False, None, 0, None)` when no k satisfies the chain.
    - Uses `boards[k-2]` (before prior pov move) for the "init" board, NOT `boards[k-1]` (Pitfall 2).
  </behavior>
  <action>
    Replace the `met >= 3` voting loop in `detect_deflection` with an exact AND-chain implementing all 11 cook conditions from RESEARCH.md section 1. Use `_VALUES_NO_KING` for the value comparisons (cook's `values`, NOT `king_values` — Pitfall 3). Map `prev_player_move = moves[k-2]`, `prev_op_move = moves[k-1]`, init board `= boards[k-2]`, pre-this-move board `= boards[k-1]`. Determine capture-or-promotion from the board BEFORE the move (`boards[k].piece_at(moves[k].to_square)`; promotion via `moves[k].promotion`) — Pitfall 6. On a full match return `(True, captured.piece_type, TACTIC_CONFIDENCE_HIGH, k)`; remove the `_grade(...)` return. Keep the existing function signature and docstring contract (target_piece_type as tactic_piece). Keep helpers shallow (nesting hard-limit 4) — extract a private `_deflection_fires_at(boards, moves, pov, k) -> int | None` (returns captured piece_type or None) if the loop body exceeds the depth/LOC budget. Add a brief comment citing Phase 132 D-01 / cook deflection lines.
  </action>
  <verify>
    <automated>uv run ty check app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k deflection</automated>
  </verify>
  <acceptance_criteria>
    - `detect_deflection` no longer calls `_grade(` and contains no `met >= ` voting threshold.
    - The deflection fast-guard / fixture tests in `tests/scripts/tagger/` pass (`-k deflection`).
    - `uv run ty check app/services/tactic_detector.py` reports zero errors.
    - On `--eval-set test`, deflection FP count is substantially lower than the baseline 991 FP (record the new TP/FP/P in the SUMMARY).
  </acceptance_criteria>
  <done>detect_deflection enforces cook's exact 11-condition AND-chain, returns TACTIC_CONFIDENCE_HIGH, and its TEST FP pool is sharply reduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite detect_clearance to cook's AND-chain (prior-pov-move geometry)</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - app/services/tactic_detector.py (`detect_clearance` ~line 1644 — current body + its existing relevance gate docstring; `detect_interference` template; `_is_in_bad_spot`, `TACTIC_CONFIDENCE_HIGH`)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Per-Motif cook.py Pseudocode" → section 2 `clearance` — the 9-condition AND-chain, especially conditions 8 (prior-pov from-square == clear-to OR between(from,to)) and 9 (prior pov landed on empty square OR is now in a bad spot); index mapping `moves[k-2]`/`boards[k-2]`/`boards[k-1]`)
  </read_first>
  <behavior>
    - Fires (returns `(True, None, TACTIC_CONFIDENCE_HIGH, k)`) only when ALL cook clearance conditions hold: pov moves to an empty square; the moved piece is a ray piece (Q/R/B) of pov's color; prior pov move not a promotion; the two `prev_move.to_square` collision guards; not-in-check-before; the check-after/king-response disjunction; condition 8 from-square geometry (`prev_move.from_square == move.to_square` OR `prev_move.from_square in SquareSet.between(move.from_square, move.to_square)`); condition 9 (prior pov landed on empty square OR is now in a bad spot via `_is_in_bad_spot`).
    - Does NOT fire on incidental ray-piece non-captures that lack the prior-pov-move geometry (the current FP source).
    - Returns `(False, None, 0, None)` otherwise.
  </behavior>
  <action>
    Replace the body of `detect_clearance` with cook's exact AND-chain (RESEARCH.md section 2, conditions 1–9). Use `chess.SquareSet.between(move.from_square, move.to_square)` for the between-square test (Don't-Hand-Roll), `_is_in_bad_spot` for condition 9's bad-spot check. Map `prev_move = moves[k-2]`, `boards[k-2]` = before prior pov move, `boards[k-1]` = after prior pov move / before this pov move, `boards[k]`/`boards[k+1]` for this move. Preserve the existing material-relevance gate already in the function if it is compatible with the cook chain; otherwise document its removal/retention in the SUMMARY. Return `(True, None, TACTIC_CONFIDENCE_HIGH, k)` on a full match; remove any `_grade(...)` return. Keep nesting ≤4 (extract a `_clearance_fires_at` helper if needed). Comment-cite Phase 132 D-01 / cook clearance lines.
  </action>
  <verify>
    <automated>uv run ty check app/services/tactic_detector.py && uv run pytest tests/scripts/tagger/ -v -k clearance</automated>
  </verify>
  <acceptance_criteria>
    - `detect_clearance` contains no `_grade(` and no `met >= ` voting threshold.
    - The clearance fixture/fast-guard tests pass (`-k clearance`).
    - `uv run ty check app/services/tactic_detector.py` reports zero errors.
    - clearance TP/FP/P(test) recorded in the SUMMARY.
  </acceptance_criteria>
  <done>detect_clearance enforces cook's exact AND-chain including the prior-pov-move from-square geometry, returning TACTIC_CONFIDENCE_HIGH.</done>
</task>

<task type="auto">
  <name>Task 3: Measure deflection + clearance on TEST and ship-or-suppress each</name>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <read_first>
    - tests/scripts/tagger/precision_floors.py (`SUPPRESSED_MOTIFS` frozenset ~line 123, `PRECISION_FLOOR` dict; the two-file suppress/ship edit pattern in the module docstring)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("tactic_confidence Suppression Lever" → exact ship vs suppress two-file edit; "Open Questions" 2 — deflection plateau guidance: if it lands 0.6–0.8 TEST with real volume, lowering the GOAL is acceptable rather than suppressing a high-volume motif)
  </read_first>
  <action>
    Run `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set train` then `--eval-set test`. For EACH of deflection and clearance, apply the ship-or-suppress decision driven by the TEST number (D-01, no pre-judgment): (a) if P(test) ≥ 0.9, ensure the motif is NOT in `SUPPRESSED_MOTIFS` and add/raise its `PRECISION_FLOOR` entry to ~5–7pp below the measured TRAIN value (rounded to 0.05); (b) if P(test) < 0.9, add the motif string to `SUPPRESSED_MOTIFS` with a one-line `# 0.XX TEST — below 0.9 bar at full cook fidelity (Phase 132)` comment. Special case per RESEARCH.md Open Question 2: if deflection plateaus at 0.6–0.8 TEST WITH meaningful retained volume, you MAY instead lower its GOALS target (in tactic_tagger_report.py) rather than suppress — document the rationale and the retained TP/FP volume in the SUMMARY. Verify `interference` still shows ~1.00 P(test) (unchanged). Record the final per-motif Status (shipped / suppressed) + numbers in the SUMMARY.
  </action>
  <verify>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test; echo "ran"; uv run pytest tests/scripts/tagger/ -v -k "deflection or clearance or interference"</automated>
  </verify>
  <acceptance_criteria>
    - deflection and clearance each have an explicit, recorded ship-or-suppress decision tied to a TEST precision number.
    - A shipped motif is absent from `SUPPRESSED_MOTIFS` and has a `PRECISION_FLOOR` entry; a suppressed motif is present in `SUPPRESSED_MOTIFS`.
    - `interference` P(test) is still ~1.00 (regression lock holds — no edits to its detector).
    - The fixture suite (`-k "deflection or clearance or interference"`) passes.
  </acceptance_criteria>
  <done>Both high-ROI motifs are honestly resolved: shipped with a floor if ≥0.9 TEST, otherwise suppressed; interference unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure-CPU detector algorithm + config edit; no untrusted input, no I/O, no new endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-02 | Tampering | detector predicate logic | accept | Deterministic CPU function over committed CC0 fixtures; validated by the precision harness. No external input. |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `grep` confirms no `_grade(` / `met >= ` remains in detect_deflection or detect_clearance.
- `uv run pytest tests/scripts/tagger/ -v -k "deflection or clearance or interference"` passes.
- `uv run ty check app/ tests/` and `uv run ruff check app/ tests/ scripts/` pass.
- `--eval-set test` recorded; interference still ~1.00.
</verification>

<success_criteria>
detect_deflection and detect_clearance are cook-aligned AND-chains (no voting), each measured on TEST and shipped-or-suppressed by its TEST number, deflection's FP pool sharply reduced, and the interference regression lock holds.
</success_criteria>

<output>
Create `.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-02-SUMMARY.md` when done — include per-motif TP/FP/P(train)/P(test) and the ship/suppress decision for each.
</output>

---

## Artifacts this plan produces

- `app/services/tactic_detector.py`: `detect_deflection` rewritten to cook's 11-condition AND-chain; `detect_clearance` rewritten to cook's 9-condition AND-chain (both return `TACTIC_CONFIDENCE_HIGH`, no `_grade` voting). Optional extracted helpers `_deflection_fires_at` / `_clearance_fires_at` (names may vary).
- `tests/scripts/tagger/precision_floors.py`: per-motif `SUPPRESSED_MOTIFS` / `PRECISION_FLOOR` edits for deflection + clearance driven by their TEST numbers.
- (These are newly-created/modified symbols — exclude from drift verification.)
