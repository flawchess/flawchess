---
phase: 132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig
plan: 05
type: execute
wave: 5
depends_on: ["132-04"]
files_modified:
  - tests/scripts/tagger/precision_floors.py
autonomous: false
requirements: [D-04, D-05]
user_setup:
  - service: dev-postgres
    why: "The D-04 dev re-backfill runs against the existing dev DB; the human confirms it is up and eyeballs before/after tag counts (no reset)"
    dashboard_config:
      - task: "Bring the dev DB up (do NOT reset it)"
        location: "docker compose -f docker-compose.dev.yml -p flawchess-dev up -d"
must_haves:
  truths:
    - "scripts/backfill_tactic_tags.py --db dev runs against the existing dev DB and refreshes the 8 tactic columns via the shared _detect_tactic_for_flaw kernel (parity with the live drain)"
    - "Per-motif game_flaws tag counts are spot-checked before vs after the backfill; suppressed motifs drop to (near-)zero in the shipped read path; the corrected detectors change which motif fires without changing tag cardinality"
    - "A final --eval-set test gate confirms every in-scope motif is either shipped (>=0.9 TEST) or explicitly suppressed, and interference remains >=0.99 TEST"
  artifacts:
    - path: "tests/scripts/tagger/precision_floors.py"
      provides: "Final, reconciled SUPPRESSED_MOTIFS / PRECISION_FLOOR for the whole phase"
      contains: "SUPPRESSED_MOTIFS"
  key_links:
    - from: "scripts/backfill_tactic_tags.py"
      to: "app/services/flaws_service.py"
      via: "_detect_tactic_for_flaw kernel (parity source of truth)"
      pattern: "_detect_tactic_for_flaw"
  prohibitions:
    - "Must NOT reset the dev DB (no bin/reset_db.sh) — backfill runs against existing data"
    - "Must NOT run a prod re-backfill — tagging is not deployed to prod"
    - "Must NOT edit detect_tactic_motif or detect_interference"
    - "Must NOT copy cook.py source (AGPL-3.0)"
    - "Must NOT change tag cardinality / single-winner-per-flaw GROUP BY tactic_motif semantics"
---

<objective>
Run the D-04 dev re-backfill against the existing dev DB as supplementary real-data validation of the cook-aligned detectors, spot-check before/after tag counts, and run the final TEST-split precision gate confirming every in-scope motif is honestly resolved (shipped ≥0.9 or suppressed) with the interference lock intact. This is the closing validation wave; it requires a human to confirm the dev DB is up and eyeball the counts, so it is non-autonomous.

Purpose: Validate the detector changes against real `game_flaws` data through the same `_detect_tactic_for_flaw` kernel the live drain uses (parity guaranteed), and produce the final phase precision table.
Output: Recorded before/after dev tag counts; final TEST-gate per-motif Status table; reconciled `precision_floors.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-CONTEXT.md
@.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm dev DB is up (not reset) and capture BEFORE tag counts</name>
  <files>(none — operational checkpoint)</files>
  <what-built>The cook-aligned Tier-3 detectors (Plans 02–04) are in-tree. This checkpoint confirms the dev DB is up (and NOT reset) before the re-backfill runs, and captures the BEFORE tag-count snapshot.</what-built>
  <how-to-verify>
    1. Bring the dev DB up (do NOT reset): `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`
    2. Capture the BEFORE per-motif tag counts via the dev DB (MCP `mcp__flawchess-db__query` or psql):
       `SELECT allowed_tactic_motif, COUNT(*) FROM game_flaws WHERE allowed_tactic_motif IS NOT NULL GROUP BY allowed_tactic_motif ORDER BY COUNT(*) DESC;`
    3. Confirm the dev DB has real `game_flaws` rows (non-empty) so the backfill is meaningful.
    4. Confirm you did NOT run `bin/reset_db.sh`.
  </how-to-verify>
  <action>Bring the existing dev DB up (no reset) and record the BEFORE per-motif `game_flaws` tag-count snapshot for comparison after the re-backfill. This is a human-gated operational step, not a code edit.</action>
  <verify>
    <human-check>Dev DB is up with real `game_flaws` rows; BEFORE per-motif tag counts recorded; `bin/reset_db.sh` was NOT run.</human-check>
  </verify>
  <done>Dev DB confirmed up (not reset) with real data; BEFORE tag-count snapshot captured.</done>
  <resume-signal>Type "approved" once the dev DB is up with real data and the BEFORE counts are recorded, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Run the dev re-backfill and spot-check before/after tag counts (D-04)</name>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <read_first>
    - scripts/backfill_tactic_tags.py (CLI flags `--db dev` / `--only-tagged` / `--dry-run` / `--user-id`; keyset paging; change-only UPDATEs; the `_detect_tactic_for_flaw` parity guarantee)
    - app/services/flaws_service.py (`_detect_tactic_for_flaw` kernel — the parity source of truth shared with the live drain)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Dev Re-Backfill Runbook (D-04)" → the exact commands, dry-run-first, sanity-check queries; "No prod re-backfill needed")
  </read_first>
  <action>
    First run a dry-run to preview changed-row counts without writing: `uv run python scripts/backfill_tactic_tags.py --db dev --dry-run`. Then run the full refresh against the existing dev DB (recall-affecting changes mean a full refresh, not `--only-tagged`): `uv run python scripts/backfill_tactic_tags.py --db dev`. After it completes, capture the AFTER per-motif tag counts with the same `GROUP BY allowed_tactic_motif` query and compare to the BEFORE snapshot from the checkpoint. Verify: (a) motifs suppressed this phase (e.g. those below 0.9 TEST) drop to (near-)zero in the shipped read path or are NULL-ed by the `_TACTIC_CHIP_CONFIDENCE_MIN=70` lever; (b) deflection's count drops substantially (its over-firing was the main FP source); (c) total tagged-flaw cardinality stays sane (the port changes WHICH motif wins, not how many flaws carry exactly one tag). Record the before/after table and the dry-run changed-row count in the SUMMARY. This task makes no code edits unless a parity discrepancy surfaces — if so, flag it (do not silently patch the kernel).
  </action>
  <verify>
    <human-check>Before/after per-motif tag-count table recorded; suppressed motifs near-zero in the shipped path; deflection count dropped; cardinality sane. Dev DB was NOT reset.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/backfill_tactic_tags.py --db dev` completed against the existing dev DB (no reset).
    - A before/after per-motif `game_flaws` tag-count comparison is recorded in the SUMMARY.
    - Suppressed-this-phase motifs are (near-)zero in the shipped read path; deflection's count dropped substantially.
    - No prod backfill was run.
  </acceptance_criteria>
  <done>The corrected Tier-3 detectors are validated against real dev `game_flaws` data through the live-drain kernel, with before/after counts recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Final TEST-split precision gate + reconcile precision_floors.py</name>
  <files>tests/scripts/tagger/precision_floors.py</files>
  <read_first>
    - tests/scripts/tagger/precision_floors.py (`SUPPRESSED_MOTIFS` + `PRECISION_FLOOR` — reconcile all Phase 132 edits into a coherent final state)
    - .planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-RESEARCH.md ("Validation Architecture" → the authoritative ship gate `--eval-set test`; "CI Step")
  </read_first>
  <action>
    Run the final authoritative gate: `PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals --eval-set test` and `--eval-set train`. Produce the final phase per-motif Status table for all seven in-scope motifs (deflection, clearance, capturing-defender, attraction, intermezzo, x-ray, sacrifice) PLUS interference: each row must be either `shipped` (P(test) ≥ 0.9, present in PRECISION_FLOOR, absent from SUPPRESSED_MOTIFS) or `suppressed` (present in SUPPRESSED_MOTIFS). Reconcile `precision_floors.py` so it is internally consistent (every shipped motif has exactly one floor; every suppressed motif is in the frozenset; comments reflect the final Phase 132 numbers). Assert `interference` ≥ 0.99 P(test). Run the full tagger CI suite `uv run pytest tests/scripts/tagger/ -v` and confirm green, plus `uv run pytest -n auto` (default suite, tagger excluded) green and `uv run ty check app/ tests/ scripts/` clean. Record the final Status table in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/scripts/tagger/ -v && uv run ty check app/ tests/ scripts/ && uv run ruff check app/ tests/ scripts/</automated>
  </verify>
  <acceptance_criteria>
    - The final per-motif Status table (shipped/suppressed) for all 7 in-scope motifs + interference is recorded with TEST numbers.
    - `precision_floors.py` is internally consistent: shipped motifs have floors and are not suppressed; suppressed motifs are in the frozenset.
    - `interference` P(test) ≥ 0.99.
    - `uv run pytest tests/scripts/tagger/ -v`, `uv run pytest -n auto`, `uv run ty check app/ tests/ scripts/`, and `uv run ruff check app/ tests/ scripts/` all pass.
  </acceptance_criteria>
  <done>The phase closes with a coherent, asserted final precision state: every in-scope motif honestly shipped or suppressed, interference locked, all gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev DB ← backfill script | The script reads/writes the local dev Postgres; only this plan touches a database, and only the existing dev DB (no reset, no prod). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-05 | Tampering | dev re-backfill UPDATEs | accept | Change-only UPDATEs via the same `_detect_tactic_for_flaw` kernel as the live drain; dev DB only, no reset; human checkpoint confirms before/after counts. |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- Dev re-backfill ran against the existing dev DB; before/after counts recorded; no reset; no prod backfill.
- Final `--eval-set test` gate run; interference ≥0.99; every in-scope motif shipped-or-suppressed.
- `uv run pytest tests/scripts/tagger/ -v`, `uv run pytest -n auto`, `uv run ty check app/ tests/ scripts/`, `uv run ruff check app/ tests/ scripts/` all green.
</verification>

<success_criteria>
The cook-aligned detectors are validated against real dev data through the live-drain kernel, the final TEST-split gate confirms each in-scope motif is honestly shipped or suppressed with interference ≥0.99, and `precision_floors.py` is reconciled and all CI gates are green.
</success_criteria>

<output>
Create `.planning/phases/132-tier-3-tactic-precision-hardening-via-cook-py-predicate-alig/132-05-SUMMARY.md` when done — include the before/after dev tag-count table and the final per-motif Status table.
</output>

---

## Artifacts this phase produces

Symbols this phase creates or changes (read by the plan-review-convergence source-grounding pass — these are newly-created/modified and must be excluded from drift verification):

**Rewritten detector functions (`app/services/tactic_detector.py`):**
- `detect_deflection` — rewritten to cook's 11-condition AND-chain (was `met >= 3` voting)
- `detect_clearance` — rewritten to cook's 9-condition AND-chain
- `detect_capturing_defender` — rewritten to cook's AND-chain with init-board defender test
- `detect_intermezzo` — rewritten to cook's zwischenzug AND-chain (k>=4 guard)
- `detect_attraction` — rewritten to cook's AND-chain (attracted ∈ {K,Q,R}, king short-circuit)
- `detect_x_ray` — rewritten to cook's three-same-square AND-chain (D-03 cutoff applied)
- `detect_sacrifice` — simplified to cook's material-diff predicate
- `detect_interference` — UNCHANGED logic; gains a `# DO NOT EDIT — interference regression lock (Phase 132, 1.00 TEST)` marker comment only

**New named constants:**
- `MIN_SACRIFICE_DROP = 2` (in `tactic_detector.py`, for the sacrifice material-diff threshold)

**New private helper predicates (only if extracted for the nesting/LOC budget — names may vary):**
- `_deflection_fires_at`, `_clearance_fires_at`, `_capturing_defender_fires_at`, `_intermezzo_fires_at`, `_attraction_fires_at`, `_x_ray_fires_at` (per-motif single-k relational checks)

**GOALS dict changes (`scripts/tactic_tagger_report.py`):**
- `deflection`, `clearance`, `capturing-defender`, `attraction`, `intermezzo`, `x-ray` raised to `{"precision": 0.90, "recall": None}`
- `sacrifice` GOALS entry added ONLY if it achieves any TP (otherwise absent)

**SUPPRESSED_MOTIFS / PRECISION_FLOOR changes (`tests/scripts/tagger/precision_floors.py`):**
- Per-motif additions to / removals from `SUPPRESSED_MOTIFS` driven by each motif's final TEST number
- Per-motif `PRECISION_FLOOR` entries for any motif that ships (≥0.9 TEST)
- Updated Phase 132 measurement comments

**No new fixtures or test functions** — the existing `tests/scripts/tagger/` harness covers all in-scope motifs (RESEARCH.md "Wave 0 Gaps": no new test files needed).
