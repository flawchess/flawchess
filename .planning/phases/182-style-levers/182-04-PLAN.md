---
phase: 182-style-levers
plan: 04
type: execute
wave: 2
depends_on: ["182-01", "182-03"]
files_modified:
  - frontend/src/lib/engine/botStyle.ts
  - frontend/src/lib/engine/__tests__/botStyle.test.ts
  - frontend/src/lib/engine/__tests__/openingBook.test.ts
autonomous: true
requirements: [STYLE-01, STYLE-03, STYLE-04, STYLE-05]
must_haves:
  truths:
    - "BotStyleParams is a plain data object (no function fields) holding feature multipliers, score bonuses, a signed contempt, resign thresholds/hysteresis, and a book-boost multiplier (D-01) — raw numeric knobs, no style names in engine code."
    - "classifyMoveFeatures returns MoveFeatures (isCheck/isCapture/isPawnAdvance/isPawnStorm/isExchange/isRetreat) from a chess.js verbose Move, using .flags/.captured/.piece/.san — no board re-derivation."
    - "applyStylePriorReweighting multiplies each raw Maia policy weight by its move's style feature multipliers and returns an UNNORMALIZED Record (samplePolicy walks it) — reusing botSampling, never adding a normalization step."
    - "applyStyleScoreShaping transforms RankedLine.practicalScore additively (style bonus/malus + variance bonus from childScoreSpread) and applies NO variance bonus when childScoreSpread is null (STYLE-04 boundary edge)."
    - "styleBookWeighting composes maiaPolicyWeighting (boost = base weight × boostMultiplier for style-line candidates, base weight otherwise) — it never replaces the base and never changes BookWeightingFn's signature (Pitfall 2)."
    - "selectBookMove's RAW-policy floor check still runs BEFORE weighting() is invoked, unchanged — a styled book still leaves book when no candidate clears BOOK_POLICY_FLOOR (Pitfall 1)."
    - { statement: "styleBookWeighting over an empty style-prefix set returns weights identical to maiaPolicyWeighting; over zero candidates selectBookMove returns null (STYLE-01 empty/adjacency edges).", verification: backstop }
    - { statement: "A shaped practicalScore stays finite and within [0,1]; a non-finite shaped score is caught by weightedPick's Number.isFinite guard and falls through to fallbackMove (STYLE-04 precision edge).", verification: backstop }
    - "STYLE-03 classifier feature-set completeness is an explicit assumption (probe unclassified): the seed's cheap chess.js flags (check/capture/pawn advance/storm/exchange/retreat) are the agreed set; any additional feature is out of scope for this phase."
  prohibitions:
    - statement: "MUST NOT change BookWeightingFn's signature; maiaPolicyWeighting's existing 2-arg shape must keep compiling (Pitfall 2)."
      status: flagged-unverified
    - statement: "MUST NOT move selectBookMove's RAW-policy floor check after the weighting() call, and MUST NOT parameterize the floor / ply-cap exit rules (Pitfall 1 / D-04)."
      status: flagged-unverified
    - statement: "MUST NOT reimplement weighted sampling or softmax inside botStyle.ts; reuse samplePolicy / sampleRankedLines / weightedPick from botSampling.ts, returning unnormalized weights."
      status: flagged-unverified
    - statement: "MUST NOT modify botSampling.ts (STYLE-05 — its helpers stay pure and untouched)."
      status: flagged-unverified
  artifacts:
    - frontend/src/lib/engine/botStyle.ts
  key_links:
    - "classifyMoveFeatures(move) → MoveFeatures → applyStylePriorReweighting multiplier lookup"
    - "styleBookWeighting(styleLinePrefixes, boostMultiplier)(candidates, rawPolicy) → maiaPolicyWeighting × boost"
    - "applyStyleScoreShaping(rankedLines, style) → per-line practicalScore + variance(childScoreSpread)"
---

<objective>
Build `botStyle.ts` (STYLE-01/03/04/05): the `BotStyleParams` data type (D-01 raw numeric knobs) plus the three pure style transforms and the move-feature classifier. Every helper is a thin, additive, pure composition over primitives phases 153-169.5 already froze; the only genuinely new logic is the chess.js move-feature classifier.

Purpose: STYLE-03 (prior reweighting on Human rungs) and STYLE-04 (score shaping on Light/Deep rungs) are two distinct hooks on `selectBotMove`'s two regimes (Pitfall 5 — never one unified function); STYLE-01 re-weights the book via the existing `BookWeightingFn` seam (D-04) without touching candidate generation or the floor/ply-cap exit rules. `undefined` style is never passed to these functions (Plan 06 guards the call sites), so unstyled play is byte-identical (D-03).
Output: `frontend/src/lib/engine/botStyle.ts` (type + `classifyMoveFeatures` + `applyStylePriorReweighting` + `applyStyleScoreShaping` + `styleBookWeighting`) + `botStyle.test.ts` + an `openingBook.test.ts` floor-check-order regression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-style-levers/182-CONTEXT.md
@.planning/phases/182-style-levers/182-RESEARCH.md
@.planning/phases/182-style-levers/182-PATTERNS.md
@frontend/src/lib/engine/openingBook.ts
@frontend/src/lib/engine/botSampling.ts
</context>

## Artifacts this phase produces

(See the shared list in 182-01-PLAN.md. This plan adds `frontend/src/lib/engine/botStyle.ts`: `BotStyleParams`, `MoveFeatures`, `classifyMoveFeatures`, `applyStylePriorReweighting`, `applyStyleScoreShaping`, `styleBookWeighting`, `PIECE_VALUE`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BotStyleParams type + module header + classifyMoveFeatures</name>
  <files>frontend/src/lib/engine/botStyle.ts, frontend/src/lib/engine/__tests__/botStyle.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/openingBook.ts (the pure-module doc-header convention: purpose + phase/req-ID citation, explicit purity statement, forward pointers; BookWeightingFn / maiaPolicyWeighting)
    - frontend/src/lib/engine/botSampling.ts (existing chess.js Move usage; the pure-helper-catalog style)
    - frontend/src/lib/engine/types.ts (RankedLine incl. the new childScoreSpread field from Plan 01)
    - .planning/phases/182-style-levers/182-RESEARCH.md (Pattern 4 — the full classifyMoveFeatures worked example incl. MoveFeatures + PIECE_VALUE)
  </read_first>
  <behavior>
    - classifyMoveFeatures(move) reports isCheck (san has + or #), isCapture (flags include 'c' or 'e'), isPawnAdvance (piece 'p' and not a capture), isPawnStorm (pawn advancing into the opponent half), isExchange (capture where |value(captured) − value(mover)| is small), isRetreat (non-pawn moving toward its own back rank).
    - White and black geometry is mirrored correctly (pawn storm / retreat direction depends on color).
  </behavior>
  <action>
    Create `frontend/src/lib/engine/botStyle.ts` with the standard pure-module doc-header (one-paragraph purpose citing STYLE-01..05 + D-01/D-03; an explicit purity statement that notes the one deviation — this module DOES import chess.js for classifyMoveFeatures, unlike openingBook.ts, and says why; forward pointers to the Plan 06 selectBotMove call sites). Define `BotStyleParams` as a plain data interface (NO function fields — Tampering guard against player-derived data) with: per-feature prior multipliers (keyed by the MoveFeatures flags), score-shaping bonuses in small expected-score units, a signed `contempt`, resign `threshold`/`hysteresisFloor`, and a `bookBoost` multiplier. Doc-comment each field citing D-01/D-09/D-12 and marking magnitudes `[ASSUMED]`/hand-tuned. Define the `MoveFeatures` interface, the `PIECE_VALUE` lookup, and `classifyMoveFeatures(move: Move): MoveFeatures` per RESEARCH Pattern 4 (adjust only threshold values). Add `botStyle.test.ts` covering each feature flag for both colors: a checking move, a capture, a pawn advance, a pawn storm (white and black), an equal-value exchange, and a knight/piece retreat.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/botStyle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - classifyMoveFeatures flags are asserted for a concrete move of each feature type, for BOTH colors on the color-dependent ones (storm/retreat).
    - `BotStyleParams` has no function-typed field (source assertion — supports the BOT-03 Tampering guard).
    - The module header explicitly states its chess.js-import deviation from the pure-no-chess.js convention.
  </acceptance_criteria>
  <done>botStyle.ts exists with a documented BotStyleParams data type and a correct, color-aware move-feature classifier; feature tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyStylePriorReweighting (STYLE-03) + applyStyleScoreShaping (STYLE-04)</name>
  <files>frontend/src/lib/engine/botStyle.ts, frontend/src/lib/engine/__tests__/botStyle.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/botStyle.ts (the type + classifier from Task 1)
    - frontend/src/lib/engine/botSampling.ts (samplePolicy / sampleRankedLines / weightedPick — the finite-guard at weightedPick, line ~39; unnormalized-weights contract)
    - frontend/src/lib/engine/selectBotMove.ts (the two regime branches these functions hook into; the ?? fallbackMove discipline)
    - frontend/src/lib/engine/types.ts (RankedLine.practicalScore + childScoreSpread)
  </read_first>
  <behavior>
    - applyStylePriorReweighting(rawPolicy, fen, style): for each UCI in rawPolicy, multiply its weight by the product of the style multipliers for its classified features; return an UNNORMALIZED Record (no renormalization).
    - A move with no matching features keeps its raw weight (multiplier 1).
    - applyStyleScoreShaping(rankedLines, style): return a new RankedLine[] with each practicalScore shifted by the style's additive bonus/malus plus a variance term scaled by childScoreSpread; when childScoreSpread is null, apply NO variance term.
    - Shaped practicalScore is clamped to stay within [0,1] and finite.
  </behavior>
  <action>
    In `botStyle.ts`, add two SEPARATE pure functions (Pitfall 5 — never a union-typed single entry point). `applyStylePriorReweighting(rawPolicy: Record<string, number>, fen: string, style: BotStyleParams): Record<string, number>` derives each legal move's features (via a chess.js instance on `fen` + classifyMoveFeatures over the verbose move whose UCI matches the policy key) and multiplies the raw weight by the product of the applicable style feature multipliers, returning an unnormalized Record — do NOT normalize (samplePolicy walks unnormalized weights; reuse it, do not reimplement weightedPick). `applyStyleScoreShaping(lines: RankedLine[], style: BotStyleParams): RankedLine[]` returns a new array where each line's `practicalScore` is additively adjusted by the style's score bonus/malus plus a variance term proportional to `childScoreSpread` (variance-preference), applying zero variance term when `childScoreSpread` is null, then clamped into [0,1]; all other RankedLine fields are copied unchanged (additive-only, so argmaxLine/sampleRankedLines read the shaped score off the same shape). Extend `botStyle.test.ts`: reweighting shifts a checking/capturing move's weight up under an Attacker-like multiplier while an unmatched move is unchanged; the returned Record is unnormalized (its sum differs from 1); score shaping raises a draw-ish line's score under a positive bonus and applies a variance bump only when childScoreSpread is non-null; a null childScoreSpread yields no variance change; a bonus that would exceed 1 is clamped and stays finite.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/botStyle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Reweighting: a feature-matched move's post-weight equals raw × the style multiplier product; an unmatched move's weight is unchanged; the output sum is not renormalized to 1 (behavior assertions).
    - Score shaping: a line with a non-null childScoreSpread gains a variance term; the same line with childScoreSpread null does not (mutation-proof — flipping the null guard changes the result).
    - A shaped score is always finite and within [0,1] (precision edge).
    - `git diff --stat frontend/src/lib/engine/botSampling.ts` shows no changes (STYLE-05 purity prohibition — botSampling untouched).
  </acceptance_criteria>
  <done>Two distinct pure transforms exist: an unnormalized prior reweighter and a null-safe, clamped score shaper; botSampling.ts is untouched; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: styleBookWeighting factory (STYLE-01) + openingBook floor-check regression</name>
  <files>frontend/src/lib/engine/botStyle.ts, frontend/src/lib/engine/__tests__/openingBook.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/openingBook.ts (BookWeightingFn type, maiaPolicyWeighting, selectBookMove's load-bearing order of operations + the floor-check-before-weighting comment)
    - frontend/src/lib/engine/__tests__/openingBook.test.ts (existing selectBookMove test fixtures — candidate builders, prefixSet stubs, rng stubs)
    - .planning/phases/182-style-levers/182-RESEARCH.md (Pattern 2 + Pitfall 1 + Pitfall 2 — the joined-prefix membership gotcha)
  </read_first>
  <behavior>
    - styleBookWeighting(styleLinePrefixes, boostMultiplier) returns a BookWeightingFn that composes maiaPolicyWeighting: each candidate on a style line gets base weight × boostMultiplier; others keep base weight.
    - The membership test uses the full joined move-history prefix (not a bare per-ply SAN) — the closure is constructed per-turn with the current history curried in (Pitfall 2), never by changing BookWeightingFn's signature.
    - An empty style-prefix set makes styleBookWeighting behave identically to maiaPolicyWeighting.
    - selectBookMove with a style weighting still leaves book (returns null) when no candidate clears BOOK_POLICY_FLOOR — the floor is checked on raw policy BEFORE weighting (Pitfall 1).
  </behavior>
  <action>
    In `botStyle.ts`, add `styleBookWeighting` as a factory that takes the style's per-color prefix set (from Plan 03's styleLinesFor) plus the move-history-so-far and a `boostMultiplier`, and returns a `BookWeightingFn` closure. Inside, call `maiaPolicyWeighting(candidates, rawPolicy)` to get the base weights, then for each candidate multiply by `boostMultiplier` iff the joined `[...moveHistorySan, candidate.san].join(' ')` prefix is in the style set (Pitfall 2 — curry the history in; do NOT change BookWeightingFn's signature). Candidates absent from the base map are skipped exactly as maiaPolicyWeighting skips them. Then extend `frontend/src/lib/engine/__tests__/openingBook.test.ts` with a regression proving the floor-check-before-weighting order still holds under a styled weighting: a single boosted candidate whose RAW policy is below BOOK_POLICY_FLOOR must still cause selectBookMove to return null (leave book) — the boost must NOT rescue an implausible line past the floor (Pitfall 1). Also assert an empty-prefix-set styleBookWeighting produces the same Record as maiaPolicyWeighting for the same candidates.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/openingBook.test.ts src/lib/engine/__tests__/botStyle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A style-line candidate's weight == base Maia weight × boostMultiplier; a non-style candidate keeps its base weight (behavior assertion via botStyle.test.ts).
    - selectBookMove with a styled weighting returns null when the sole boosted candidate's RAW policy is below BOOK_POLICY_FLOOR (openingBook.test.ts regression — mutation-proof for Pitfall 1).
    - `styleBookWeighting(emptySet, ...)` output Record equals `maiaPolicyWeighting`'s for identical candidates.
    - `grep -n "BookWeightingFn = (" frontend/src/lib/engine/openingBook.ts` shows the original 2-arg signature unchanged (prohibition: signature not modified).
  </acceptance_criteria>
  <done>styleBookWeighting composes over maiaPolicyWeighting with a joined-prefix boost; the raw-policy floor still guards styled books; BookWeightingFn's signature is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle knobs → engine transforms | BotStyleParams comes only from the Plan 05 exported constants (trusted); the transforms read chess.js-derived features and Maia/Stockfish outputs (internal). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-08 | Tampering | player-derived data threading into BotStyleParams (defeating BOT-03) | high | mitigate | BotStyleParams is a plain data object with no function fields, sourced only from Plan 05's exported bundles; the transforms take it as a value, never derive it from player state (asserted: no function-typed field). |
| T-182-09 | Denial of Service | a NaN/Infinity multiplier or bonus producing a degenerate distribution | medium | mitigate | Reweighting/shaping return into the SAME Record/RankedLine[] shapes weightedPick already guards with `Number.isFinite(total)` — a degenerate result falls through to fallbackMove; no new guard needed, and shaped scores are clamped [0,1]. |
| T-182-10 | Tampering | boost defeating the book's raw-policy floor safety valve (Pitfall 1) | high | mitigate | The floor is checked on raw policy BEFORE weighting; a dedicated openingBook.test.ts regression proves a boosted-but-implausible line still leaves book. |

No package installs in this plan — legitimacy gate not applicable (chess.js already vendored).
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/botStyle.test.ts src/lib/engine/__tests__/openingBook.test.ts` green.
- `cd frontend && npx tsc -b` zero errors.
- `git diff --stat frontend/src/lib/engine/botSampling.ts` empty (purity).
</verification>

<success_criteria>
botStyle.ts exposes a data-only BotStyleParams, a color-aware move classifier, an unnormalized prior reweighter, a null-safe clamped score shaper, and a floor-respecting book-weighting factory; botSampling.ts and BookWeightingFn's signature are untouched.
</success_criteria>

<output>
Create `.planning/phases/182-style-levers/182-04-SUMMARY.md` when done.
</output>
