---
phase: 182-style-levers
plan: 05
type: execute
wave: 3
depends_on: ["182-02", "182-03", "182-04"]
files_modified:
  - frontend/src/lib/engine/botStyleBundles.ts
  - frontend/src/lib/engine/__tests__/botStyleBundles.test.ts
  - scripts/style-lever-measurement.mjs
  - reports/data/
autonomous: true
requirements: [STYLE-01, STYLE-02, STYLE-03, STYLE-04]
must_haves:
  truths:
    - "The 4 named style bundles (Attacker / Trickster / Grinder / Wall) ship as plain exported BotStyleParams data constants (D-02), each pinning: curated book set + book boost, feature multipliers, score bonuses, signed contempt, resign threshold/hysteresis — no style names leak into engine code."
    - "Grinder never resigns early and has a high-positive contempt; Wall has a slightly-negative contempt; Attacker boosts checks/captures/pawn storms; Trickster references the troll/swindle lines (per-style identity from SEED-098)."
    - "The measurement script (D-11) runs each style over N sampled positions and reports per-style move-feature-frequency shift vs the unstyled baseline into reports/data/ as a TSV, importing the LIVE botStyle helpers via the @/ alias hook (never reimplementing them)."
    - "Magnitudes are hand-tuned (D-12) until the feature shift is visibly present without gross strength distortion; final values are documented inline with their tuning rationale."
    - { statement: "styleLinesFor selection inside each bundle is color-correct so a White-only style book never boosts Black-side lines (STYLE-01 correctness).", verification: backstop }
  prohibitions:
    - statement: "MUST NOT reimplement applyStylePriorReweighting / applyStyleScoreShaping / classifyMoveFeatures inside the measurement script; it imports the live @/lib/engine/botStyle exports."
      status: flagged-unverified
    - statement: "MUST NOT reset the dev database or gate completion on bin/reset_db.sh; the measurement script runs standalone against sampled FENs."
      status: flagged-unverified
  artifacts:
    - frontend/src/lib/engine/botStyleBundles.ts
    - scripts/style-lever-measurement.mjs
  key_links:
    - "BOT_STYLE_BUNDLES[style] -> BotStyleParams consumed by selectBotMove (Plan 06) + useBotGame (Plan 07)"
    - "style-lever-measurement.mjs -> applyStylePriorReweighting/applyStyleScoreShaping -> reports/data/ TSV"
---

<objective>
Ship the 4 named style-to-knob bundles as plain exported data (D-02), plus the D-11 Node measurement script and the D-12 hand-tuning that makes each style's feature shift visible without gross strength distortion. The bundles are what Phase 183's persona registry will reference; success criteria for STYLE-01..04 are verified against these REAL bundles, not throwaway configs.

Purpose: D-02 fixes the 4 styles as data constants consuming the Plan 04 numeric knobs and Plan 03 curated books; D-11/D-12 make magnitude selection empirical — iterate against the measurement script until Attacker checks/captures rise, Grinder trades rise, etc., without breaking strength. Strength deltas themselves are Phase 184's job, not this phase's.
Output: `frontend/src/lib/engine/botStyleBundles.ts` (+ test), `scripts/style-lever-measurement.mjs`, and a committed `reports/data/` TSV with the tuning evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-style-levers/182-CONTEXT.md
@.planning/phases/182-style-levers/182-RESEARCH.md
@.planning/phases/182-style-levers/182-PATTERNS.md
@scripts/gem-elo-calibration.mjs
</context>

## Artifacts this phase produces

(See the shared list in 182-01-PLAN.md. This plan adds `frontend/src/lib/engine/botStyleBundles.ts`: `ATTACKER_STYLE`, `TRICKSTER_STYLE`, `GRINDER_STYLE`, `WALL_STYLE`, `BOT_STYLE_BUNDLES`; and `scripts/style-lever-measurement.mjs`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the 4 named style bundles as exported BotStyleParams constants</name>
  <files>frontend/src/lib/engine/botStyleBundles.ts, frontend/src/lib/engine/__tests__/botStyleBundles.test.ts</files>
  <read_first>
    - frontend/src/lib/engine/botStyle.ts (BotStyleParams field shape from Plan 04)
    - frontend/src/lib/engine/styleOpeningLines.ts (styleLinesFor + the curated sets from Plan 03)
    - frontend/src/lib/botDrawGate.ts (RESIGN_MIN_FULLMOVE / hysteresis-floor default + contempt semantics from Plan 02)
    - frontend/src/data/trollOpenings.ts (Trickster identity source)
    - .planning/phases/182-style-levers/182-CONTEXT.md (D-02, D-06 boost ~x20-50, D-09 contempt per style, style identities in specifics)
    - .planning/seeds/SEED-098-bot-personas-playstyle-layer.md (per-style identity + per-tier story)
  </read_first>
  <behavior>
    - BOT_STYLE_BUNDLES maps each of Attacker/Trickster/Grinder/Wall to a BotStyleParams.
    - Attacker: check/capture/pawn-storm multipliers greater than 1; book = attacking/gambit lines with ~x20-50 boost.
    - Trickster: troll/swindle book; high-variance score-shaping preference.
    - Grinder: exchange/trade multiplier greater than 1; high-positive contempt; resign threshold that never fires early.
    - Wall: system-opening book; slightly-negative contempt; simplifying preference.
  </behavior>
  <action>
    Create `frontend/src/lib/engine/botStyleBundles.ts` exporting `ATTACKER_STYLE`, `TRICKSTER_STYLE`, `GRINDER_STYLE`, `WALL_STYLE` as `BotStyleParams` constants and a `BOT_STYLE_BUNDLES` record keyed by style name. Each bundle wires: the color-correct curated book set (via styleLinesFor / the Plan 03 exports) with a `bookBoost` in the D-06 range of roughly 20 to 50; feature multipliers matching the style identity (Attacker boosts isCheck/isCapture/isPawnStorm; Grinder boosts isExchange; Wall boosts simplifying/isExchange with a defensive lean; Trickster leans on variance); a signed `contempt` per D-09 (Grinder high-positive, Wall slightly-negative, Attacker/Trickster near-zero-to-slight); and resign `threshold`/`hysteresisFloor` such that Grinder effectively never resigns early. Give every magnitude a doc comment marking it `[ASSUMED]` and citing D-02/D-06/D-09/D-12 (values are refined in Task 3). Add `botStyleBundles.test.ts` asserting the structural + identity invariants: all 4 bundles present; each is a plain data object with no function fields; Grinder's contempt greater than 0 and Wall's contempt less than 0; Attacker's check/capture multipliers greater than 1; each bundle's book set is non-empty for at least one color.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/botStyleBundles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `BOT_STYLE_BUNDLES` has exactly the 4 keys; each value is a BotStyleParams with no function-typed field (D-02 / BOT-03 source assertion).
    - Grinder contempt greater than 0, Wall contempt less than 0, Attacker check and capture multipliers greater than 1 (behavior assertions).
    - Each bundle references a non-empty curated book set for at least one color and a bookBoost in the 20 to 50 range.
  </acceptance_criteria>
  <done>Four identity-correct BotStyleParams bundles ship as exported data constants with [ASSUMED] magnitudes; structural + identity tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the D-11 style-lever measurement script</name>
  <files>scripts/style-lever-measurement.mjs</files>
  <read_first>
    - scripts/gem-elo-calibration.mjs (TSV emission + reports/data/ output convention)
    - scripts/lib/frontend-alias-hook.mjs (the @/ alias import hook)
    - scripts/calibration-harness.mjs (header/usage-comment convention; imports the LIVE production function directly, no reimplementation)
    - frontend/src/lib/engine/botStyle.ts (applyStylePriorReweighting / applyStyleScoreShaping / classifyMoveFeatures exports)
    - frontend/src/lib/engine/botStyleBundles.ts (BOT_STYLE_BUNDLES from Task 1)
  </read_first>
  <action>
    Create `scripts/style-lever-measurement.mjs` following the calibration-harness family conventions: run via the alias-hook invocation, importing the LIVE `applyStylePriorReweighting` / `classifyMoveFeatures` (and `applyStyleScoreShaping` where a search snapshot is available or synthesized) from `@/lib/engine/botStyle` plus `BOT_STYLE_BUNDLES` — never reimplement them. Sample N positions (a fixed seeded set of mid-opening FENs — reuse the calibration-openings set or a committed FEN list; document N and the sampling strategy, an accepted Claude's-discretion item). For each style and the unstyled baseline, compute the per-move-feature FREQUENCY of the sampled/argmax move (share of sampled moves that are checks / captures / pawn advances / storms / exchanges / retreats), and emit a TSV to `reports/data/style-lever-measurement-<timestamp>.tsv` with columns for style, feature, baseline frequency, styled frequency, and delta. Use `mulberry32` for a deterministic seed. Add a header docstring stating the usage line and the D-11 purpose. Support an `--n <count>` flag so a small run is fast for verification.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && node --import ./scripts/lib/frontend-alias-hook.mjs scripts/style-lever-measurement.mjs --n 20 && ls reports/data/style-lever-measurement-*.tsv >/dev/null 2>&1 && echo MEASUREMENT_TSV_OK</automated>
  </verify>
  <acceptance_criteria>
    - Running the script writes a non-empty TSV under `reports/data/` with per-style per-feature baseline/styled/delta columns.
    - The script imports `applyStylePriorReweighting`/`classifyMoveFeatures` from `@/lib/engine/botStyle` (source assertion — no reimplementation).
    - The run prints `MEASUREMENT_TSV_OK` (CLI output check).
  </acceptance_criteria>
  <done>A deterministic Node measurement script emits per-style feature-frequency shift vs baseline into reports/data/, reusing the live engine helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-tune bundle magnitudes against the measurement report (D-12)</name>
  <files>frontend/src/lib/engine/botStyleBundles.ts, reports/data/</files>
  <read_first>
    - frontend/src/lib/engine/botStyleBundles.ts (the [ASSUMED] magnitudes from Task 1)
    - scripts/style-lever-measurement.mjs (the report the tuning iterates against)
    - .planning/phases/182-style-levers/182-CONTEXT.md (D-12 — small expected-score units ~0.02-0.05 for score bonuses; feature multipliers are tuning territory)
  </read_first>
  <action>
    Run the measurement script at a realistic N, read the emitted TSV, and iterate the bundle magnitudes in `botStyleBundles.ts` (feature multipliers, score bonuses in the ~0.02-0.05 expected-score band per D-12, book boosts) until each style shows a VISIBLE positive shift in its identity features vs the unstyled baseline (Attacker check+capture+storm frequency up, Grinder exchange frequency up, Wall simplifying/retreat up) WITHOUT a gross distortion that would make the bot play random/absurd moves. Replace each `[ASSUMED]` doc comment with the final tuned value and a one-line rationale citing the measured shift. Commit the final measurement TSV under `reports/data/` as the tuning evidence the user reviews in UAT (D-12). Do NOT reset the dev DB or otherwise gate on it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/engine/__tests__/botStyleBundles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The committed `reports/data/` TSV shows a positive identity-feature delta for each style vs baseline (evidence assertion).
    - Every bundle magnitude that was `[ASSUMED]` now carries a tuned value + rationale doc comment (source assertion — no bare `[ASSUMED]` left on shipped magnitudes).
    - botStyleBundles.test.ts remains green after tuning.
  </acceptance_criteria>
  <done>Bundle magnitudes are tuned to a visible-without-distortion shift; final values documented; measurement evidence committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exported bundles → engine + registry | BOT_STYLE_BUNDLES is compile-time data consumed by the engine (this milestone) and Phase 183's persona registry; no runtime external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-11 | Tampering | a bundle carrying a function field or player-derived value (defeating BOT-03) | high | mitigate | Bundles are plain data constants; a test asserts no function-typed field and fixed numeric knobs — the registry chooses a bundle by persona slot, never from player data. |
| T-182-12 | Denial of Service | an over-large multiplier producing degenerate move selection | medium | mitigate | D-12 tuning caps magnitudes at a visible-not-distorting level, validated by the measurement TSV; degenerate distributions still fall through weightedPick's finite guard to fallbackMove. |

No package installs in this plan — legitimacy gate not applicable.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/engine/__tests__/botStyleBundles.test.ts` green.
- `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/style-lever-measurement.mjs --n 20` writes a TSV to reports/data/.
- `cd frontend && npx tsc -b` zero errors.
</verification>

<success_criteria>
Four identity-correct style bundles ship as exported data; a deterministic measurement script reports per-style feature shifts vs baseline; magnitudes are tuned to a visible-without-distortion level with committed evidence.
</success_criteria>

<output>
Create `.planning/phases/182-style-levers/182-05-SUMMARY.md` when done.
</output>
