---
status: passed
phase: 119-eval-drain-coverage-bounded-retry-hole-filling-recency-weigh
source: [119-VERIFICATION.md]
started: 2026-06-14T15:27:18Z
updated: 2026-06-14T17:50:00Z
---

## Current Test

(all tests passed)

## Tests

### 1. EvalCoverageBadge CPU pulse — visual confirmation
expected: Open Library > Games while a game has pending engine analysis. The badge CPU icon visibly pulses (animate-pulse) while analyzedN < totalN, and stops pulsing once coverage reaches 100%.
result: passed

### 2. Live tier-3 lottery — badge unfreezes for a returning user
expected: As a returning prod user with a large backlog (e.g. user 28), navigate to Library > Games and watch the analyzed count over 3-5 minutes. The count advances on ~every tier-3 drain cycle (~10s cadence); the badge does NOT appear frozen; the stall detector (5 polls / 15s of no delta) does not fire.
result: passed

### 3. Prod migration applied — full_eval_attempts column
expected: `SELECT column_name, column_default FROM information_schema.columns WHERE table_name='games' AND column_name='full_eval_attempts'` returns one row with column_default = '0'.
result: passed (migration round-trips cleanly on dev; confirmed by user sign-off — applies on deploy)

### 4. Prod migration applied — ix_eval_jobs_user_active dropped
expected: `SELECT indexname FROM pg_indexes WHERE indexname = 'ix_eval_jobs_user_active'` on prod returns zero rows.
result: passed (confirmed by user sign-off — applies on deploy)

### 5. Prod migration applied — ix_games_needs_engine_full_evals present
expected: `SELECT indexdef FROM pg_indexes WHERE indexname = 'ix_games_needs_engine_full_evals'` on prod returns one row whose indexdef contains `WHERE ((full_evals_completed_at IS NULL) AND (lichess_evals_at IS NULL))`.
result: passed (confirmed by user sign-off — applies on deploy)

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None — all human verification items passed.
