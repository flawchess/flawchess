---
phase: 120-headless-remote-trusted-operator-eval-worker-seed-048
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/config.py
  - app/schemas/eval_remote.py
  - app/services/engine.py
autonomous: true
requirements: [D-1, D-5, D-6]
must_haves:
  truths:
    - "Server config exposes EVAL_OPERATOR_TOKEN and EXPECTED_SF_VERSION, both empty by default (dev/CI)"
    - "A worker can read its own Stockfish version string via get_stockfish_version() before evaluating"
    - "Pydantic v2 request/response schemas exist for the lease + submit wire contract"
  artifacts:
    - path: "app/core/config.py"
      provides: "EVAL_OPERATOR_TOKEN + EXPECTED_SF_VERSION settings fields"
      contains: "EVAL_OPERATOR_TOKEN"
    - path: "app/schemas/eval_remote.py"
      provides: "LeasePosition, LeaseResponse, SubmitEval, SubmitRequest, SubmitResponse"
      exports: ["LeaseResponse", "SubmitRequest", "SubmitResponse"]
    - path: "app/services/engine.py"
      provides: "get_stockfish_version() async helper"
      contains: "async def get_stockfish_version"
  key_links:
    - from: "app/services/engine.py:get_stockfish_version"
      to: "chess.engine.popen_uci"
      via: "UCI handshake reads protocol.id['name']"
      pattern: "popen_uci"
---

<objective>
Foundation layer for the remote eval worker: add the two new server settings (operator token + expected Stockfish version), the Pydantic v2 wire schemas shared by both endpoints, and the `get_stockfish_version()` engine helper the worker calls at startup.

Purpose: These are the contracts every downstream plan (endpoints in 120-02, CLI worker in 120-03) consumes. Defining them first removes the scavenger-hunt: the endpoint and worker plans import concrete symbols instead of inventing them.

Output: `EVAL_OPERATOR_TOKEN` + `EXPECTED_SF_VERSION` in `Settings`; `app/schemas/eval_remote.py` (5 models); `get_stockfish_version()` in `engine.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/seeds/SEED-048-headless-remote-eval-worker.md
@.planning/phases/120-headless-remote-trusted-operator-eval-worker-seed-048/120-RESEARCH.md
@.planning/phases/120-headless-remote-trusted-operator-eval-worker-seed-048/120-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (120-01) creates these new symbols — exclude from source-drift verification:
- `app.core.config.Settings.EVAL_OPERATOR_TOKEN` (str, default "")
- `app.core.config.Settings.EXPECTED_SF_VERSION` (str, default "")
- `app.schemas.eval_remote.LeasePosition` (ply: int, fen: str, is_terminal: bool)
- `app.schemas.eval_remote.LeaseResponse` (game_id, user_id, is_lichess_eval_game, positions, leased_at)
- `app.schemas.eval_remote.SubmitEval` (ply, eval_cp, eval_mate, best_move, pv)
- `app.schemas.eval_remote.SubmitRequest` (game_id, user_id, sf_version, evals)
- `app.schemas.eval_remote.SubmitResponse` (game_id, stamp_complete, failed_ply_count)
- `app.services.engine.get_stockfish_version()` async helper
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add EVAL_OPERATOR_TOKEN + EXPECTED_SF_VERSION settings and the get_stockfish_version() helper</name>
  <files>app/core/config.py, app/services/engine.py</files>
  <read_first>
    - app/core/config.py — read the `Settings` class; insert the two new fields immediately AFTER `EVAL_AUTO_DRAIN_ENABLED` (line ~74) and BEFORE `model_config = SettingsConfigDict(...)` (line ~76). Match the existing comment style (explains default + prod override) used on `EVAL_AUTO_DRAIN_ENABLED`.
    - app/services/engine.py — read `_resolve_stockfish_path()` / `_STOCKFISH_PATH` (lines 63-76), `_engine_popen_kwargs()` (line 168), `evaluate_nodes_with_pv` (line 246), and `class EnginePool` (line 311). Add the new helper as a module-level `async def` just BEFORE `class EnginePool`.
    - 120-RESEARCH.md Q7 (version detection) + 120-PATTERNS.md `app/services/engine.py` section (the exact helper body).
  </read_first>
  <action>
    In `app/core/config.py`, add to `Settings`: `EVAL_OPERATOR_TOKEN: str = ""` (per D-6 — empty disables both endpoints in dev/CI; prod sets a strong random secret in `.env`) and `EXPECTED_SF_VERSION: str = ""` (per D-5 — empty accepts any version in dev/CI; prod sets e.g. "Stockfish 18"). Use named field comments matching the `EVAL_AUTO_DRAIN_ENABLED` style. Do NOT log either value anywhere.
    In `app/services/engine.py`, add `async def get_stockfish_version() -> str:` that opens a single `chess.engine.popen_uci(_STOCKFISH_PATH, **_engine_popen_kwargs())`, reads `str(protocol.id.get("name", "unknown"))`, awaits `protocol.quit()`, and returns the version string. It must NOT construct an `EnginePool` (a one-shot handshake only). Reuse the existing module-level `_STOCKFISH_PATH` and `_engine_popen_kwargs()` — do not re-resolve the path.
  </action>
  <verify>
    <automated>uv run python -c "from app.core.config import settings; assert hasattr(settings, 'EVAL_OPERATOR_TOKEN'); assert hasattr(settings, 'EXPECTED_SF_VERSION'); assert settings.EVAL_OPERATOR_TOKEN == ''; assert settings.EXPECTED_SF_VERSION == ''; from app.services.engine import get_stockfish_version; import inspect; assert inspect.iscoroutinefunction(get_stockfish_version)"</automated>
  </verify>
  <acceptance_criteria>
    - `settings.EVAL_OPERATOR_TOKEN` and `settings.EXPECTED_SF_VERSION` both exist and default to `""`.
    - `app.services.engine.get_stockfish_version` is an importable coroutine function taking no required args.
    - `get_stockfish_version` does not instantiate `EnginePool` (grep its body: no `EnginePool(`).
    - `uv run ty check app/` reports zero errors for both files.
  </acceptance_criteria>
  <done>Both settings fields present and empty-by-default; `get_stockfish_version()` importable, coroutine, EnginePool-free.</done>
</task>

<task type="auto">
  <name>Task 2: Create the eval_remote Pydantic v2 wire schemas</name>
  <files>app/schemas/eval_remote.py</files>
  <read_first>
    - app/schemas/imports.py — the schema-file style analog (module docstring naming the phase, one BaseModel per DTO, `int | None` for nullable numerics, no `@classmethod` unless a non-trivial mapping exists).
    - 120-PATTERNS.md `app/schemas/eval_remote.py` section — the exact field set for all five models.
    - 120-RESEARCH.md Q4 (lease + submit schema shapes) and Open Question 2 (`user_id` echoed through lease → submit to avoid an extra DB query).
  </read_first>
  <action>
    Create `app/schemas/eval_remote.py` with a module docstring naming Phase 120. Define five `BaseModel` classes: `LeasePosition` (ply: int, fen: str, is_terminal: bool); `LeaseResponse` (game_id: int, user_id: int, is_lichess_eval_game: bool, positions: list[LeasePosition], leased_at: datetime); `SubmitEval` (ply: int, eval_cp: int | None, eval_mate: int | None, best_move: str | None, pv: str | None); `SubmitRequest` (game_id: int, user_id: int, sf_version: str, evals: list[SubmitEval]); `SubmitResponse` (game_id: int, stamp_complete: bool, failed_ply_count: int). `user_id` rides on both `LeaseResponse` and `SubmitRequest` so the worker echoes it back (avoids a DB lookup for `_signal_flaw_completion` in the submit path). `fen` carries the full `board.fen()` (turn/castling/en-passant), NOT `board_fen()`. `pv` is a space-joined UCI string capped at 12 plies server-side. No magic literals; no business logic in schemas.
  </action>
  <verify>
    <automated>uv run python -c "from app.schemas.eval_remote import LeasePosition, LeaseResponse, SubmitEval, SubmitRequest, SubmitResponse; r = LeaseResponse(game_id=1, user_id=2, is_lichess_eval_game=False, positions=[LeasePosition(ply=0, fen='x', is_terminal=False)], leased_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc)); req = SubmitRequest(game_id=1, user_id=2, sf_version='Stockfish 18', evals=[SubmitEval(ply=0, eval_cp=10, eval_mate=None, best_move='e2e4', pv='e2e4 e7e5')]); assert req.evals[0].eval_mate is None"</automated>
  </verify>
  <acceptance_criteria>
    - All five models import from `app.schemas.eval_remote`.
    - `LeaseResponse.user_id` and `SubmitRequest.user_id` both exist (int).
    - `SubmitEval.eval_cp` / `eval_mate` / `best_move` / `pv` are all Optional (accept None).
    - `uv run ty check app/` reports zero errors for the new file.
  </acceptance_criteria>
  <done>`app/schemas/eval_remote.py` defines the five models with the exact fields above; round-trips construct cleanly with None evals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote worker → prod HTTPS API | Untrusted-at-the-wire input crosses here; gated downstream by operator token (120-02). This plan only defines the config + schema surface those gates use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Information Disclosure | `EVAL_OPERATOR_TOKEN` setting | mitigate | Token stored as a `Settings` str sourced from `.env` only; never logged, never echoed in responses (no f-string/log statement references it in this plan). Default `""` (fail-closed; endpoints in 120-02 deny when unconfigured). Per D-6. |
| T-120-02 | Tampering | `EXPECTED_SF_VERSION` setting | mitigate | Empty default = dev/CI accepts any version; prod pins the engine generation. The gate itself lives in 120-02's submit endpoint (D-5); this plan only provides the comparand. |
| T-120-SC | Tampering | npm/pip installs | accept | No new packages this phase (120-RESEARCH.md Package Legitimacy Audit: none). No install task; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run ruff format app/ && uv run ruff check app/` clean for the three touched files.
- `uv run ty check app/` zero errors.
- `uv run python -c "from app.schemas.eval_remote import LeaseResponse, SubmitRequest; from app.services.engine import get_stockfish_version; from app.core.config import settings; print('ok')"` prints `ok`.
</verification>

<success_criteria>
- Two new settings fields present, empty by default, fail-closed semantics documented in comments.
- `get_stockfish_version()` importable, coroutine, reuses module-level path/popen kwargs, no EnginePool.
- Five Pydantic v2 schemas importable with the exact field set, `user_id` threaded through lease + submit.
- ty + ruff clean.
</success_criteria>

<output>
Create `.planning/phases/120-headless-remote-trusted-operator-eval-worker-seed-048/120-01-SUMMARY.md` when done.
</output>
