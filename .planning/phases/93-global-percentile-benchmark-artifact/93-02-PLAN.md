---
phase: 93-global-percentile-benchmark-artifact
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/services/global_percentile_cdf.py
  - scripts/gen_global_percentile_cdf.py
  - tests/services/test_global_percentile_cdf.py
  - reports/global-percentile-cdf-latest.md
autonomous: false
requirements: [PCTL-01]
must_haves:
  truths:
    - "app/services/global_percentile_cdf.py exists, is importable, passes ty + ruff, and exports GLOBAL_PERCENTILE_CDF: Mapping[MetricId, CdfTable] keyed by the 4 in-scope MetricId literals."
    - "Each CdfTable entry carries exactly 15 (breakpoint_label, value) pairs in the locked order p1..p99 (ROADMAP success criterion #5)."
    - "Each CdfTable entry carries n_users (per-metric sample count) and snapshot_month ('2026-03') for audit trail (D-04 Claude's Discretion item)."
    - "scripts/gen_global_percentile_cdf.py exists, runs the canonical CTE deterministically against the benchmark DB (port 5433), refuses to run unless DATABASE_URL contains 'flawchess_benchmark' AND ':5433' (safety guard mirrors scripts/backfill_eval.py)."
    - "Running the script regenerates app/services/global_percentile_cdf.py (overwriting the GLOBAL_PERCENTILE_CDF literal) AND writes reports/global-percentile-cdf-latest.md with rotation rule applied (D-07)."
    - "Module exposes interpolate_percentile(metric_id: MetricId, value: float) -> float | None for Phase 94 consumption — Phase 94 hand-off shape per planning_context downstream_consumer."
    - "Module is Python-only — no TS mirror, no scripts/gen_endgame_zones_ts.py modification, no frontend/src/generated/endgameZones.ts modification (D-01)."
    - "Module is a sibling of app/services/endgame_zones.py — NOT a graft into it (D-04). CDF table shape (Mapping[MetricId, CdfTable]) follows the typed-Mapping pattern from endgame_zones.py ZONE_REGISTRY."
  artifacts:
    - path: "app/services/global_percentile_cdf.py"
      provides: "Typed GLOBAL_PERCENTILE_CDF registry + interpolate_percentile helper for Phase 94 consumption"
      contains: "GLOBAL_PERCENTILE_CDF: Mapping[MetricId, CdfTable]"
      exports: ["GLOBAL_PERCENTILE_CDF", "CdfTable", "interpolate_percentile", "BENCHMARK_DB_SNAPSHOT_MONTH"]
    - path: "scripts/gen_global_percentile_cdf.py"
      provides: "Manual recalibration script (mirrors scripts/backfill_eval.py --db benchmark pattern)"
      contains: "argparse.ArgumentParser.*--db.*choices=['benchmark']"
    - path: "tests/services/test_global_percentile_cdf.py"
      provides: "Unit tests for interpolate_percentile + structural invariants of GLOBAL_PERCENTILE_CDF"
      contains: "def test_"
    - path: "reports/global-percentile-cdf-latest.md"
      provides: "Latest-pointer report (rotation rule per D-07)"
      contains: "Global Percentile CDF"
  key_links:
    - from: "scripts/gen_global_percentile_cdf.py"
      to: ".claude/skills/benchmarks/SKILL.md §4"
      via: "module docstring names the SKILL.md chapter as the methodology source-of-truth"
      pattern: "SKILL\\.md.*§?\\s*4|Chapter 4"
    - from: "scripts/gen_global_percentile_cdf.py"
      to: "scripts/backfill_eval.py"
      via: "reuses the --db flag pattern + safety guard (DATABASE_URL must contain 'flawchess_benchmark' AND port 5433)"
      pattern: "flawchess_benchmark|:5433"
    - from: "app/services/global_percentile_cdf.py"
      to: "app/schemas/endgames.py"
      via: "imports MetricId Literal — no new IDs introduced (D-03)"
      pattern: "from app.schemas.endgames import.*MetricId"
    - from: "app/services/global_percentile_cdf.py shape"
      to: "app/services/endgame_zones.py shape"
      via: "follows the typed-Mapping pattern (Mapping[MetricId, ...] + frozen dataclass), NOT a graft into endgame_zones.py (D-04)"
      pattern: "Mapping\\[MetricId,|@dataclass\\(frozen=True\\)"
---

<objective>
Mechanize the global empirical-CDF artifact: write `scripts/gen_global_percentile_cdf.py` that runs the canonical CTE deterministically against the benchmark DB and emits committed Python source at `app/services/global_percentile_cdf.py` as a typed `GLOBAL_PERCENTILE_CDF: Mapping[MetricId, CdfTable]` registry. Includes a runtime `interpolate_percentile(metric_id, value) -> float | None` helper for Phase 94 hand-off. Manual recalibration step (no CI gate), mirroring `scripts/backfill_eval.py --db benchmark`.

Purpose: Phase 94 backend imports `GLOBAL_PERCENTILE_CDF` and `interpolate_percentile` to compute per-user scalar percentiles emitted on endgame API responses. Phase 93 ships the artifact + the helper; Phase 94 wires it into endpoint responses.

Output:
- `app/services/global_percentile_cdf.py` — committed Python source, typed registry, no DB or I/O at module level (mirrors `app/services/endgame_zones.py` purity).
- `scripts/gen_global_percentile_cdf.py` — manual recalibration script with `--db benchmark` flag + safety guard.
- `tests/services/test_global_percentile_cdf.py` — unit tests for structural invariants and `interpolate_percentile` behavior (no DB required).
- `reports/global-percentile-cdf-latest.md` — human-readable report following the rotation rule (D-07). Initial version produced by running the script once against the benchmark DB at the end of this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-global-percentile-benchmark-artifact/93-CONTEXT.md
@CLAUDE.md
@app/services/endgame_zones.py
@app/schemas/endgames.py
@scripts/backfill_eval.py
@reports/benchmarks-gap-metrics-percentile-candidacy.md

<interfaces>
<!-- Key exports the executor needs. Extracted from current codebase. -->

From app/schemas/endgames.py (MetricId Literal — 4 in-scope IDs already exist, no new IDs needed per D-03):
```python
MetricId = Literal[
    "score_gap",
    "achievable_score_gap",
    "section2_score_gap_conv",
    "section2_score_gap_parity",
    # ... (other IDs exist but are NOT in scope for Phase 93)
]
```

From app/services/endgame_zones.py (pattern to mirror — typed Mapping + frozen dataclass):
```python
from collections.abc import Mapping
from dataclasses import dataclass

@dataclass(frozen=True)
class ZoneSpec:
    typical_lower: float
    typical_upper: float
    direction: Literal["higher_is_better", "lower_is_better"]

ZONE_REGISTRY: Mapping[MetricId, ZoneSpec] = {
    "score_gap": ZoneSpec(...),
    ...
}
```

From scripts/backfill_eval.py (script pattern to mirror — `--db` flag, port-derived URL, localhost-only override):
```python
_TARGET_PORT: dict[str, int] = {
    "dev": 5432,
    "benchmark": 5433,
    "prod": 15432,
}

def _db_url(target: str) -> str:
    # Derives URL from settings.DATABASE_URL by replacing host:port with localhost:<target-port>
    # Override env var BACKFILL_{TARGET}_DB_URL must point to a localhost host
    ...

parser.add_argument(
    "--db",
    choices=["dev", "benchmark", "prod"],
    required=True,
    ...
)
```

For Phase 93 the script supports only `--db benchmark` (Phase 93 has no dev or prod equivalent — the artifact is regenerated from the benchmark DB only). The safety guard refuses to run unless the resolved DATABASE_URL contains both `flawchess_benchmark` AND `:5433`.

Existing `app/services/endgame_zones.py` has NO DB / I/O dependencies — it is pure constants + helper functions. `app/services/global_percentile_cdf.py` must match this purity: at module import time it does not touch the DB, the filesystem, or the network. The generator script (`scripts/gen_global_percentile_cdf.py`) is what touches the DB; it emits Python source that becomes the new contents of `app/services/global_percentile_cdf.py`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define module shape + interpolate_percentile + tests</name>
  <files>app/services/global_percentile_cdf.py, tests/services/test_global_percentile_cdf.py</files>
  <read_first>
    - app/services/endgame_zones.py (full — typed-Mapping + frozen dataclass + helper pattern to mirror)
    - app/schemas/endgames.py (lines 30-80 — MetricId Literal definition)
    - .planning/phases/93-global-percentile-benchmark-artifact/93-CONTEXT.md (D-03, D-04, "Claude's Discretion" item #1 + #4)
    - .planning/ROADMAP.md Phase 93 (success criterion #5 — 15 breakpoints)
    - CLAUDE.md (Coding Guidelines: no magic numbers, type safety, ty compliance, ≤3 nesting depth, ≤100 logic LOC soft cap)
  </read_first>
  <behavior>
    - Test 1: `BENCHMARK_DB_SNAPSHOT_MONTH` equals the string `"2026-03"` (audit-trail constant per D-04 Claude's Discretion item).
    - Test 2: `BREAKPOINT_LABELS` is a tuple of exactly 15 strings in order: `("p1","p2.5","p5","p10","p20","p30","p40","p50","p60","p70","p80","p90","p95","p97.5","p99")` (ROADMAP success criterion #5).
    - Test 3: `GLOBAL_PERCENTILE_CDF` is keyed by exactly the 4 in-scope MetricId literals (`score_gap`, `achievable_score_gap`, `section2_score_gap_conv`, `section2_score_gap_parity`) — set equality check.
    - Test 4: For each metric in `GLOBAL_PERCENTILE_CDF`, `len(cdf.breakpoints) == 15` and the breakpoint labels match `BREAKPOINT_LABELS` in order.
    - Test 5: For each metric, the per-breakpoint values are monotonically non-decreasing (p1 ≤ p2.5 ≤ ... ≤ p99) — a hard structural invariant of an empirical CDF.
    - Test 6: For each metric, `cdf.n_users >= 1` (placeholder values fine until the script runs; real values come from regeneration).
    - Test 7: `interpolate_percentile("achievable_score_gap", value)` returns `0.0` when `value` is at or below the p1 breakpoint value (left-edge clamp), `100.0` when at or above p99 (right-edge clamp), and a value in (0, 100) for any value strictly between p1 and p99.
    - Test 8: `interpolate_percentile` uses linear interpolation between adjacent breakpoints — given a synthetic CdfTable with breakpoints labeled p10/p20 and values 0.0/0.10, `interpolate_percentile_with_table(table, 0.05)` returns `15.0` (midpoint of p10 and p20).
    - Test 9: `interpolate_percentile("score_gap", math.nan)` returns `None` (NaN handling — mirrors `assign_zone` NaN tolerance pattern in `endgame_zones.py`).
    - Test 10: Module imports do not touch the database, filesystem, or network — `import app.services.global_percentile_cdf` must not require asyncpg connectivity.
  </behavior>
  <action>
    Create `app/services/global_percentile_cdf.py` modeled on `app/services/endgame_zones.py`. Module docstring states: this is the authoritative Python source for the global empirical-CDF artifact; methodology lives in `.claude/skills/benchmarks/SKILL.md` §4; regenerate via `scripts/gen_global_percentile_cdf.py --db benchmark`; no TS mirror exists (D-01) and module must remain a sibling of `endgame_zones.py`, not a graft into it (D-04).

    **Module-level constants and types:**
    - `BENCHMARK_DB_SNAPSHOT_MONTH: str = "2026-03"` (audit-trail constant per Discretion #4).
    - `BREAKPOINT_LABELS: tuple[str, ...] = ("p1", "p2.5", "p5", "p10", "p20", "p30", "p40", "p50", "p60", "p70", "p80", "p90", "p95", "p97.5", "p99")` — note the 15 strings in exactly this order (ROADMAP success criterion #5). Use a module-level tuple (not list) so it's immutable.
    - `BREAKPOINT_PERCENTILES: tuple[float, ...]` — parallel tuple of float percentiles `(1.0, 2.5, 5.0, 10.0, 20.0, 30.0, 40.0, 50.0, 60.0, 70.0, 80.0, 90.0, 95.0, 97.5, 99.0)` used for interpolation math. Derive from `BREAKPOINT_LABELS` via a single comprehension `tuple(float(label.lstrip("p")) for label in BREAKPOINT_LABELS)` — or hard-code; either is fine, but the constant exists so the interpolator does not re-parse labels per call.
    - `CdfMetricId = Literal["score_gap", "achievable_score_gap", "section2_score_gap_conv", "section2_score_gap_parity"]` — a narrower Literal alias over the in-scope subset of `MetricId`. Justification: `interpolate_percentile` should accept any `MetricId` for caller convenience but the dictionary key type is the narrower set. Implementation note: the public function signature accepts `MetricId` (broader) and returns `None` when the metric is not in `GLOBAL_PERCENTILE_CDF.keys()` — Phase 94 callers iterate over all metrics on the response and the narrowing should not force a Phase-94 type narrow on every call site.

    **CdfTable dataclass:**
    ```
    @dataclass(frozen=True)
    class CdfTable:
        # breakpoints[i] corresponds to BREAKPOINT_LABELS[i] / BREAKPOINT_PERCENTILES[i].
        # 0-1 score-gap units (matches the internal SQL output unit — see SKILL.md §1
        # "Display formatting"). Values monotone non-decreasing.
        breakpoints: tuple[float, ...]
        n_users: int
        snapshot_month: str = BENCHMARK_DB_SNAPSHOT_MONTH
    ```
    `breakpoints` is a tuple so the dataclass remains hashable and immutable. Length-15 invariant is documented and tested.

    **GLOBAL_PERCENTILE_CDF registry:**
    `GLOBAL_PERCENTILE_CDF: Mapping[CdfMetricId, CdfTable]` keyed by the 4 in-scope MetricId literals. Initial values are **placeholders** that satisfy the monotone invariant (e.g. `tuple(round(-0.30 + i * 0.04, 4) for i in range(15))` — a 15-point linear ramp from −0.30 to +0.26 in 0.04 steps); placeholders MUST be overwritten by the first script run at the end of this plan. Each metric carries its inclusion-floor n_users floor: `score_gap` ≥30/≥30 (per Plan 01 chapter prose), `achievable_score_gap` ≥20, `section2_score_gap_conv` ≥20/bucket, `section2_score_gap_parity` ≥20/bucket. Document these floors in inline comments alongside each registry entry, sourced from CONTEXT.md "Claude's Discretion" item #2.

    **MANDATORY: BEGIN/END GENERATED REGISTRY markers.** Wrap the `GLOBAL_PERCENTILE_CDF = {...}` literal with two sentinel comments — `# --- BEGIN GENERATED REGISTRY ---` immediately before the assignment and `# --- END GENERATED REGISTRY ---` immediately after the closing brace. The Task 2 generator script uses these markers to locate and overwrite ONLY the registry block, preserving the module docstring, type aliases, `BENCHMARK_DB_SNAPSHOT_MONTH`/`BREAKPOINT_LABELS`/`BREAKPOINT_PERCENTILES` constants, `CdfTable` dataclass, and `interpolate_percentile` function. Task 2 verification asserts both markers exist; Task 1 ships a broken downstream contract if they are absent.

    **interpolate_percentile function signature and behavior:**
    ```
    def interpolate_percentile(metric_id: MetricId, value: float) -> float | None:
        """Linear-interpolate `value` against the metric's CDF; return percentile in [0, 100] or None.

        Returns None when:
          - metric_id not in GLOBAL_PERCENTILE_CDF (metric is not chip-eligible)
          - value is NaN

        Clamps:
          - value <= breakpoints[0] (p1)  -> 0.0   (left tail beyond resolved range)
          - value >= breakpoints[-1] (p99) -> 100.0 (right tail beyond resolved range)
        """
    ```
    Implementation: nesting depth ≤ 3, logic LOC < 50. Use a small private helper `_interpolate_with_table(table: CdfTable, value: float) -> float | None` so the table-driven test cases (Test 7, Test 8) can pass synthetic tables in. Implementation strategy: binary search via `bisect.bisect_left` on the breakpoints tuple to find the interval, then linear interpolation between `BREAKPOINT_PERCENTILES[i-1]` and `BREAKPOINT_PERCENTILES[i]` in proportion to `(value - bp[i-1]) / (bp[i] - bp[i-1])`. Guard against `bp[i] == bp[i-1]` (degenerate interval, e.g. all values pinned at zero) — return the lower percentile in that case.

    **Coding constraints per CLAUDE.md:**
    - All module-level names typed (`Mapping`, `tuple[str, ...]`, `Final` where applicable).
    - No magic numbers — `BREAKPOINT_LABELS`, `BREAKPOINT_PERCENTILES`, the 0.0 and 100.0 clamp values, and the dataclass field count are all named constants or compile-time-obvious; no bare numerics inside `interpolate_percentile` body except the clamps.
    - `uv run ty check app/ tests/` must pass with zero errors. Use explicit return type annotations. Use `Sequence[T]` (not `list[T]`) for function parameters where applicable.
    - `uv run ruff check app/services/global_percentile_cdf.py tests/services/test_global_percentile_cdf.py` must pass with zero errors.

    **Test file:**
    Create `tests/services/test_global_percentile_cdf.py` with the 10 tests described in `<behavior>`. Use pytest; no fixtures required (module is pure). Tests should NOT require asyncpg or a running benchmark DB — they exercise the in-Python registry and interpolation logic only.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_global_percentile_cdf.py -x</automated>
    <automated>uv run ty check app/services/global_percentile_cdf.py tests/services/test_global_percentile_cdf.py</automated>
    <automated>uv run ruff check app/services/global_percentile_cdf.py tests/services/test_global_percentile_cdf.py</automated>
    <automated>python -c "from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF, interpolate_percentile, CdfTable, BENCHMARK_DB_SNAPSHOT_MONTH, BREAKPOINT_LABELS; assert len(BREAKPOINT_LABELS) == 15; assert set(GLOBAL_PERCENTILE_CDF.keys()) == {'score_gap', 'achievable_score_gap', 'section2_score_gap_conv', 'section2_score_gap_parity'}; print('OK')"</automated>
    <automated>grep -c "GLOBAL_PERCENTILE_CDF: Mapping\[" app/services/global_percentile_cdf.py  # must be 1 — typed registry shape per D-04</automated>
    <automated>! grep -E "import sqlalchemy|import asyncpg|from app.db|open\\(|requests\\." app/services/global_percentile_cdf.py  # module must be pure (no DB, no I/O)</automated>
    <automated>grep -c "BEGIN GENERATED REGISTRY" app/services/global_percentile_cdf.py  # must be 1 — sentinel wraps the registry literal so Task 2's script can rewrite only that block</automated>
    <automated>grep -c "END GENERATED REGISTRY" app/services/global_percentile_cdf.py  # must be 1</automated>
  </verify>
  <done>
    - `app/services/global_percentile_cdf.py` exists with the typed `GLOBAL_PERCENTILE_CDF: Mapping[CdfMetricId, CdfTable]` registry, the `CdfTable` frozen dataclass, the `BENCHMARK_DB_SNAPSHOT_MONTH`/`BREAKPOINT_LABELS`/`BREAKPOINT_PERCENTILES` constants, and the `interpolate_percentile` public helper.
    - 4 in-scope MetricId keys present; 15-breakpoint set present; placeholder values monotone non-decreasing.
    - `# --- BEGIN GENERATED REGISTRY ---` and `# --- END GENERATED REGISTRY ---` sentinel comments wrap the `GLOBAL_PERCENTILE_CDF` literal (Task 2 generator script depends on these markers).
    - All 10 tests pass under `uv run pytest tests/services/test_global_percentile_cdf.py -x`.
    - `uv run ty check` and `uv run ruff check` both pass with zero errors on the new files.
    - Module imports cleanly with no DB/I/O dependency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the generator script + write the initial report</name>
  <files>scripts/gen_global_percentile_cdf.py</files>
  <read_first>
    - scripts/backfill_eval.py (full file — pattern for --db flag, _db_url helper, safety guard, async session, --dry-run mode, _log() formatting)
    - .claude/skills/benchmarks/SKILL.md §1 Standard CTE block (lines 134-156) and the equal-footing filter block (lines 318-356) and user_elo_at_game block (lines 436-455) — these are the canonical SQL fragments the script must compose
    - .claude/skills/benchmarks/SKILL.md §3.1.5 Achievable Score Gap subchapter (around line 1096) for the achievable_score_gap query template
    - .claude/skills/benchmarks/SKILL.md §3.1.6 Endgame Score Gap subchapter (around line 1239) for the score_gap query template
    - .claude/skills/benchmarks/SKILL.md §3.2.2 Section 2 ΔES subchapter (around line 1585) for the section2_score_gap_conv / section2_score_gap_parity query templates
    - .claude/skills/benchmarks/SKILL.md Chapter 4 (added in Plan 01) — methodology source-of-truth
    - app/services/global_percentile_cdf.py (created in Task 1 — the file this script regenerates)
    - reports/benchmarks-gap-metrics-percentile-candidacy.md (report-shape precedent)
    - CLAUDE.md (Critical Constraints: never asyncio.gather on same AsyncSession, httpx async only, asyncpg not requests)
  </read_first>
  <action>
    Create `scripts/gen_global_percentile_cdf.py`. The script is a single-purpose manual recalibration tool (no CI gate, no auto-regen). Module docstring opens with: "Regenerate the global empirical-CDF artifact from the benchmark DB. Mirrors the `scripts/backfill_eval.py --db benchmark` pattern. Methodology source-of-truth: `.claude/skills/benchmarks/SKILL.md` Chapter 4."

    **CLI shape (subset of backfill_eval.py — only the `--db benchmark` target is supported):**
    - `argparse.ArgumentParser` with description matching the module docstring opening line.
    - `--db` argument: `choices=["benchmark"]`, `required=True`. Other targets (`dev`, `prod`) are intentionally not supported — the artifact is regenerated from the benchmark DB only.
    - `--dry-run` flag: connect to the DB, run the safety guard, print the query plan + expected row counts per metric, but do NOT overwrite the Python source or the report file.
    - `--report-only` flag: regenerate `reports/global-percentile-cdf-latest.md` from the queries WITHOUT overwriting `app/services/global_percentile_cdf.py`. Useful for sanity-checking a new dataset before committing the Python source. Mutually exclusive with `--dry-run`.

    **Safety guard (mandatory — mirrors backfill_eval.py D-01 pattern):**
    A `_assert_benchmark_db(url: str) -> None` helper that raises `SystemExit` unless the resolved URL contains `flawchess_benchmark` (database name) AND `:5433` (port). Call this BEFORE any query executes. Refusing to run protects against accidental writes/queries against dev/prod even though the script is read-only at SQL level — the safety guard is paranoia about misconfigured `BACKFILL_BENCHMARK_DB_URL` overrides.

    **Connection pattern:** Reuse `_db_url("benchmark")` adapted from `scripts/backfill_eval.py`. Use `create_async_engine` + `async_sessionmaker` from `sqlalchemy.ext.asyncio` exactly as backfill_eval.py does. One session, sequential queries (no `asyncio.gather` on the same AsyncSession — CLAUDE.md Critical Constraint).

    **Query composition (4 metrics, one query per metric):**
    For each of the 4 in-scope MetricIds, compose a SQL query that:
    1. Starts with the canonical CTE from SKILL.md §1 ("Standard CTE — selected_users").
    2. Joins games + the metric-specific value expression (e.g. for `score_gap`: per-user (eg_score − non_eg_score) over the user's endgame and non-endgame games; for `achievable_score_gap`: per-user mean (actual_score − expected_score) over endgame-entry games; for `section2_score_gap_conv` / `section2_score_gap_parity`: per-user mean span gap within the eval-bucket entry filter).
    3. Applies the canonical filters: `g.rated AND NOT g.is_computer_game`, `g.time_control_bucket::text = su.tc_bucket`, both rating columns NOT NULL, equal-footing `abs(opp_rating - user_rating) <= 100`, sub-800 dropped via `user_elo_at_game >= 800`.
    4. Applies the per-metric inclusion floor (≥30 endgame AND ≥30 non-endgame for `score_gap`; ≥20 for `achievable_score_gap`; ≥20 per bucket for `section2_score_gap_*`). These floors are encoded as constants at the top of the script (no magic numbers).
    5. Excludes the sparse `(elo_bucket = 2400, tc = 'classical')` cell from the pooled distribution.
    6. Computes the 15 breakpoint values via PostgreSQL's `percentile_cont(ARRAY[0.01, 0.025, 0.05, 0.10, ..., 0.99]) WITHIN GROUP (ORDER BY metric_value)` — returns an array of length 15 in the same order as `BREAKPOINT_PERCENTILES` from `app/services/global_percentile_cdf.py`.
    7. Also returns `count(distinct user_id) AS n_users` for the audit-trail field.

    **Per-bucket sanity-check queries (for the report):**
    For each metric, a second query computes per-rating-bucket (5 buckets: 800/1200/1600/2000/2400, game-time) median + skew + kurtosis + n_users. Use `percentile_cont(0.5)` for median; for skew and kurtosis use the standard SQL formulas (`(sum((x - mean)^3) / n) / pow(stddev_pop, 3)` for skew, similarly for kurtosis). Sparse cell excluded.

    **Constants at top of script (no magic numbers):**
    ```
    INCLUSION_FLOOR_SCORE_GAP_EG = 30
    INCLUSION_FLOOR_SCORE_GAP_NON_EG = 30
    INCLUSION_FLOOR_ACHIEVABLE = 20
    INCLUSION_FLOOR_SECTION2_PER_BUCKET = 20
    BREAKPOINTS = (0.01, 0.025, 0.05, 0.10, 0.20, 0.30, 0.40, 0.50, 0.60, 0.70, 0.80, 0.90, 0.95, 0.975, 0.99)  # 15 — matches BREAKPOINT_PERCENTILES in app/services/global_percentile_cdf.py
    SPARSE_CELL_ELO = 2400
    SPARSE_CELL_TC = "classical"
    EQUAL_FOOTING_TOL = 100
    OUTPUT_MODULE_PATH = Path("app/services/global_percentile_cdf.py")
    OUTPUT_REPORT_PATH = Path("reports/global-percentile-cdf-latest.md")
    BENCHMARK_DB_NAME_TOKEN = "flawchess_benchmark"
    BENCHMARK_DB_PORT_TOKEN = ":5433"
    ```

    **Python source emission:**
    A `_emit_python_source(metric_results: Mapping[CdfMetricId, tuple[list[float], int]]) -> str` helper that produces the full text of the new `app/services/global_percentile_cdf.py`. Strategy: read the current `app/services/global_percentile_cdf.py` to preserve the module docstring, type definitions, `BENCHMARK_DB_SNAPSHOT_MONTH`/`BREAKPOINT_LABELS`/`BREAKPOINT_PERCENTILES` constants, `CdfTable` dataclass, and `interpolate_percentile` function; only the `GLOBAL_PERCENTILE_CDF = {...}` literal is replaced with new values. Use a marker comment pair (`# --- BEGIN GENERATED REGISTRY ---` / `# --- END GENERATED REGISTRY ---`) around the registry literal so the script can locate and replace exactly that block without touching the rest of the file. The marker comments are committed to the Python source (Task 1 must include them around the placeholder registry).

    After emission, the script writes the new source to a temp file, runs `uv run ruff format` and `uv run ruff check --fix` on it, and only then renames over `app/services/global_percentile_cdf.py`. This guarantees the committed file is always ruff-formatted regardless of the emission template's whitespace.

    **Report emission:**
    A `_emit_report(metric_results, per_bucket_sanity_check_results) -> str` helper that produces the markdown text of `reports/global-percentile-cdf-latest.md` following the shape described in SKILL.md §4 (Plan 01) and mirroring `reports/benchmarks-gap-metrics-percentile-candidacy.md`. Header: title, snapshot date, snapshot month, methodology cross-link to SKILL.md §4, n_users per metric, in-scope metric list. Body: per-metric breakpoint table (15 rows: percentile label, value in pp), per-metric per-rating-bucket sanity-check table (5 rows: bucket, n_users, median, skew, kurtosis), per-metric sparse-cell footnote.

    **Rotation rule (D-07):**
    Before writing the new `reports/global-percentile-cdf-latest.md`, check whether the file exists. If it does, parse its first-line date header (`# Global Percentile CDF — YYYY-MM-DD`), rename the file to `reports/global-percentile-cdf-{YYYY-MM-DD}.md`, then write the new latest. If the dated archive already exists (operator ran the script twice on the same day), append a `-rev2`, `-rev3`, etc. suffix to avoid overwrite. Implementation: small `_rotate_report(existing_path: Path) -> None` helper.

    **Output ordering and idempotency:**
    Use `ORDER BY 1, 2, 3` (or equivalent stable column ordering) on every query so two runs against the same benchmark DB snapshot produce byte-identical output. Format values to a fixed number of significant figures (4 decimal places for breakpoint values, integer for n_users, 4 decimal places for skew/kurtosis). The Python source emission must produce byte-identical output across runs against the same DB state — operator confirms by re-running and checking `git diff` is empty.

    **Logging:** Use the `_log()` timestamp helper pattern from `scripts/backfill_eval.py` (UTC timestamp prefix). Log each metric's query start, row count, and percentile value spread. On dry-run, print the resolved DB URL (with password masked) + the would-write paths + the per-metric expected row counts.

    **Coding constraints per CLAUDE.md:**
    - No magic numbers (the constants block above covers all).
    - Nesting depth ≤ 3 inside any function. Logic LOC ≤ 100 per function — split the script into `main`, `parse_args`, `_db_url`, `_assert_benchmark_db`, `_query_metric_breakpoints`, `_query_per_bucket_sanity_check`, `_emit_python_source`, `_emit_report`, `_rotate_report` so each is small and testable.
    - `uv run ty check scripts/` passes with zero errors. Explicit return types on every function.
    - `uv run ruff check scripts/gen_global_percentile_cdf.py` passes with zero errors.
    - Use `httpx.AsyncClient` if HTTP were needed (it isn't — this is a SQL-only script), use `asyncpg`/SQLAlchemy async for the DB. Never `requests`. Never `asyncio.gather` on the same `AsyncSession`.
  </action>
  <verify>
    <automated>uv run ty check scripts/gen_global_percentile_cdf.py</automated>
    <automated>uv run ruff check scripts/gen_global_percentile_cdf.py</automated>
    <automated>python scripts/gen_global_percentile_cdf.py --help 2>&amp;1 | grep -E "\\-\\-db.*benchmark"  # CLI flag exposed</automated>
    <automated>python scripts/gen_global_percentile_cdf.py --db dev 2>&amp;1 | grep -iE "invalid choice|usage:"  # only --db benchmark accepted</automated>
    <automated>! python scripts/gen_global_percentile_cdf.py --db benchmark --dry-run 2>&amp;1 | grep -iE "error|traceback"  # dry-run does not crash without benchmark DB up — safety guard rejects, returns exit 1 cleanly with a descriptive error; ensure no python tracebacks</automated>
    <automated>grep -c "flawchess_benchmark" scripts/gen_global_percentile_cdf.py  # safety guard token present</automated>
    <automated>grep -c ":5433" scripts/gen_global_percentile_cdf.py  # port token present</automated>
    <automated>grep -c "BEGIN GENERATED REGISTRY" app/services/global_percentile_cdf.py  # must be 1 — markers committed</automated>
    <automated>grep -c "END GENERATED REGISTRY" app/services/global_percentile_cdf.py  # must be 1</automated>
  </verify>
  <done>
    - `scripts/gen_global_percentile_cdf.py` exists, passes ty + ruff, and exposes `--db benchmark` + `--dry-run` + `--report-only` flags.
    - Safety guard refuses to run unless DATABASE_URL contains `flawchess_benchmark` AND `:5433`.
    - 4 query templates implemented for the 4 in-scope metrics with canonical CTE inheritance.
    - Per-bucket sanity-check queries return median + skew + kurtosis + n_users for each rating bucket.
    - Python source emission uses BEGIN/END marker comments to scope the rewrite; rest of `app/services/global_percentile_cdf.py` is preserved.
    - Report emission produces markdown matching the shape described in SKILL.md §4.
    - Rotation rule implemented (`_rotate_report` helper).
    - Dry-run mode prints query plan + expected counts without writing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — Run the script against the live benchmark DB</name>
  <what-built>
    Plans 01 + 02 have shipped:
    - `.claude/skills/benchmarks/SKILL.md` Chapter 4 (methodology)
    - `app/services/global_percentile_cdf.py` (typed registry + interpolate_percentile, placeholder values)
    - `scripts/gen_global_percentile_cdf.py` (manual recalibration script)
    - `tests/services/test_global_percentile_cdf.py` (10 unit tests, all passing)

    All static gates (ty, ruff, pytest) pass. The remaining step is the end-to-end DB regeneration, which requires the benchmark DB to be running and is therefore a manual UAT step per project memory `feedback_no_dev_db_reset_in_plans.md`.
  </what-built>
  <how-to-verify>
    Operator MUST run these commands locally (the executor cannot do this autonomously because the benchmark DB is not part of the dev environment by default):

    1. **Start the benchmark DB** (port 5433):
       ```
       bin/benchmark_db.sh start
       ```
       Confirm with `docker compose -f docker-compose.benchmark.yml -p flawchess-benchmark ps` — expect one running container.

    2. **Dry-run the generator** to confirm the safety guard fires correctly and the queries resolve:
       ```
       uv run python scripts/gen_global_percentile_cdf.py --db benchmark --dry-run
       ```
       Expected output:
       - Per-metric expected row counts (n_users per metric — verify ≥ ~1000 for each of the 4 metrics, modulo per-metric inclusion floors).
       - Resolved DB URL (password masked) ending in `localhost:5433/flawchess_benchmark`.
       - Confirmation of which paths would be written.
       - No tracebacks.

    3. **Run the generator for real**:
       ```
       uv run python scripts/gen_global_percentile_cdf.py --db benchmark
       ```
       Expected effects:
       - `app/services/global_percentile_cdf.py` is rewritten — the `GLOBAL_PERCENTILE_CDF = { ... }` literal between the BEGIN/END markers contains real per-metric breakpoint tables (not the placeholders).
       - `reports/global-percentile-cdf-latest.md` is created (or, if it existed, rotated to a dated archive + a new latest written).
       - All 4 metrics show monotonically non-decreasing breakpoint values.
       - Per-metric n_users values match the dry-run output.

    4. **Re-run the test suite** to verify the regenerated registry still satisfies all 10 structural invariants:
       ```
       uv run pytest tests/services/test_global_percentile_cdf.py -x
       ```
       All tests pass.

    5. **Sanity-check the report**:
       Open `reports/global-percentile-cdf-latest.md` and confirm:
       - 4 metric sections (score_gap, achievable_score_gap, section2_score_gap_conv, section2_score_gap_parity)
       - Each section has a 15-row breakpoint table
       - Each section has a 5-row per-rating-bucket sanity-check table
       - The Conversion ΔES section shows the expected ~−0.95 skew and ~+1.42 excess kurtosis per the 2026-05-22 pre-flight at `reports/benchmarks-gap-metrics-percentile-candidacy.md` (small numerical drift is acceptable since the benchmark DB may have been refreshed; an order-of-magnitude or sign change is a red flag).

    6. **Idempotency check**:
       Re-run the generator:
       ```
       uv run python scripts/gen_global_percentile_cdf.py --db benchmark
       ```
       Then:
       ```
       git diff app/services/global_percentile_cdf.py
       ```
       Expected: empty diff (a deterministic CTE against an unchanged DB produces byte-identical output). If diff is non-empty, investigate ordering / float-formatting non-determinism in the script.

    7. **Final pre-PR checklist per CLAUDE.md** (run from the working tree, NOT the operator's responsibility but explicitly listed to surface them before merge):
       ```
       uv run ruff format app/ tests/ scripts/
       uv run ruff check app/ tests/ scripts/ --fix
       uv run ty check app/ tests/ scripts/
       uv run pytest -x
       ```

    Stop the benchmark DB when done (optional — leaving it running is harmless):
    ```
    bin/benchmark_db.sh stop
    ```
  </how-to-verify>
  <resume-signal>Type "approved" if (a) the script regenerated the artifact, (b) all 4 metrics have monotonically non-decreasing 15-breakpoint tables, (c) the report file exists with expected structure, (d) re-running produces an empty diff, (e) all four pre-PR checklist commands exit 0. Otherwise describe issues and Plan 02 will be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → benchmark DB (port 5433) | Script connects via asyncpg using the read-only role `flawchess_benchmark_ro`. RO role is enforced by the benchmark Docker init script; even a misconfigured script cannot mutate the DB. |
| script → committed Python source on disk | Script overwrites `app/services/global_percentile_cdf.py` between BEGIN/END marker comments. Operator commits the result manually. No CI auto-commit. |
| script → reports/ on disk | Script writes `reports/global-percentile-cdf-latest.md` and rotates the prior file. Idempotent on identical DB state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03 | Tampering | scripts/gen_global_percentile_cdf.py running against the wrong DB (dev or prod) | mitigate | Safety guard refuses to run unless DATABASE_URL contains BOTH `flawchess_benchmark` (DB name) AND `:5433` (port). `--db` argparse choice is limited to `["benchmark"]`. Mirrors `scripts/backfill_eval.py --db benchmark` pattern. |
| T-93-04 | Tampering | Deterministic CTE producing non-deterministic output | mitigate | Every query uses explicit `ORDER BY`. Float values rendered to fixed precision (4 decimal places). `_emit_python_source` runs the result through `ruff format` before write. Idempotency UAT step 6 confirms `git diff` is empty on re-run against unchanged DB. |
| T-93-05 | Information disclosure | benchmark DB password in env var or override | accept | Password is operator-local (per memory `project_benchmark_db_ro_password.md`). Script masks the password when logging the resolved URL. Read-only role anyway. |
| T-93-06 | Denial of service | Long-running CTE against benchmark DB | accept | Benchmark DB is local Docker on the operator's workstation. Worst case the operator Ctrl-C's the script; no production impact. The CTE has been run before (it's the same shape `/benchmarks` SKILL.md §3.1.5/§3.1.6/§3.2.2 already use) and is known to complete in seconds. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. Uses existing project deps: SQLAlchemy async, asyncpg, sentry_sdk, argparse — all already in `pyproject.toml`. |
</threat_model>

<verification>
- Task 1: All 10 unit tests pass; ty + ruff clean on the new Python module; module imports without DB/I/O.
- Task 2: Script ty + ruff clean; safety guard token present; CLI rejects `--db dev`; BEGIN/END markers present in the Python module.
- Task 3 (HUMAN-UAT): Operator confirms end-to-end regeneration produces a real artifact, a real report, idempotent re-runs, and all pre-PR checks pass.
</verification>

<success_criteria>
- ROADMAP success criterion #1 (script regen produces breakpoint tables for the 4 chipped ΔES metrics) — satisfied after Task 3.
- ROADMAP success criterion #2 (breakpoint tables committed at `app/services/global_percentile_cdf.py` as typed `GLOBAL_PERCENTILE_CDF`, Python-only, no TS mirror) — satisfied after Tasks 1 + 3.
- ROADMAP success criterion #3 (report at `reports/global-percentile-cdf-latest.md` with rotation rule applied) — satisfied after Task 3.
- ROADMAP success criterion #4 (canonical CTE verbatim from SKILL.md §1) — verifiable by inspection of `scripts/gen_global_percentile_cdf.py` after Task 2.
- ROADMAP success criterion #5 (15-breakpoint tail-bounded set p1..p99) — encoded in `BREAKPOINT_LABELS` constant; locked by Test 2.
- D-01: No TS mirror. No edit to `scripts/gen_endgame_zones_ts.py` or `frontend/src/generated/endgameZones.ts`.
- D-04: Module is a sibling of `endgame_zones.py`, not a graft.
- Phase 94 hand-off shape: `interpolate_percentile(metric_id, value) -> float | None` is the public API Phase 94 calls per request.
</success_criteria>

<output>
Create `.planning/phases/93-global-percentile-benchmark-artifact/93-02-SUMMARY.md` when done.

Summary should include:
- Final shape of `CdfTable` (fields + types)
- Final list of constants in `scripts/gen_global_percentile_cdf.py`
- Per-metric n_users from the live UAT regeneration
- Per-metric breakpoint range (p1 value, p50 value, p99 value) for each of the 4 metrics
- Per-metric skew / kurtosis sanity-check values for the pooled distribution
- Comparison to `reports/benchmarks-gap-metrics-percentile-candidacy.md` 2026-05-22 numbers (note any material drift)
- Confirmation of idempotency (re-run produces empty git diff)
- Confirmation that no TS mirror was created (D-01)
- Hand-off note to Phase 94: `from app.services.global_percentile_cdf import GLOBAL_PERCENTILE_CDF, interpolate_percentile`
</output>
