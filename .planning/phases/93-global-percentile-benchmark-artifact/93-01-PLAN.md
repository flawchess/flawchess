---
phase: 93-global-percentile-benchmark-artifact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/benchmarks/SKILL.md
autonomous: true
requirements: [PCTL-01]
must_haves:
  truths:
    - "Operator (or Claude in the benchmarks skill) can read the new CDF subchapter and reproduce the global empirical-CDF methodology from first principles without needing CONTEXT.md or SEED-019."
    - "Subchapter documents the canonical CTE inherited verbatim from Chapter 1 (lichess_username join, status='completed', sparse-cell exclusion, equal-footing filter, game-time ELO bucketing, sub-800 dropped) — D-05."
    - "Subchapter documents the locked 15-breakpoint tail-bounded set (p1, p2.5, p5, p10..p90, p95, p97.5, p99) per ROADMAP success criterion #5."
    - "Subchapter documents the 4 in-scope MetricIds (score_gap, achievable_score_gap, section2_score_gap_parity, section2_score_gap_conv) and explicitly calls out Recovery + raw % gauges as out of scope (D-02)."
    - "Subchapter documents the expected report shape: per-metric breakpoint table + per-rating-bucket sanity-check table (median + skew + kurtosis), mirroring reports/benchmarks-gap-metrics-percentile-candidacy.md."
    - "Subchapter documents the rotation rule for reports/global-percentile-cdf-latest.md (D-07) — prior dated report archived in place, same convention as reports/benchmarks-latest.md."
    - "Subchapter is cross-linked from the existing chapter index in SKILL.md (the table of headings near the top of Chapter 3) and from the 'Report file layout' section so a future skill invocation finds it."
  artifacts:
    - path: ".claude/skills/benchmarks/SKILL.md"
      provides: "New Chapter 4 — Global Percentile CDF subchapter"
      contains: "## 4. Global Percentile CDF"
      min_lines_added: 100
  key_links:
    - from: ".claude/skills/benchmarks/SKILL.md §4"
      to: ".claude/skills/benchmarks/SKILL.md §1 (Standard CTE, Sparse-cell exclusion, Equal-footing opponent filter, Rating-lag selection bias, user_elo_at_game/elo_bucket)"
      via: "explicit reference in subchapter prose (no SQL duplication — the subchapter cites the canonical CTE blocks rather than restating them, mirroring how §3.1.5/§3.1.6/§3.2.2 inherit Chapter 1's building blocks)"
      pattern: "see §1|inherit.*Chapter 1|canonical CTE"
    - from: ".claude/skills/benchmarks/SKILL.md §4"
      to: "scripts/gen_global_percentile_cdf.py (Plan 02)"
      via: "subchapter mentions the script as the mechanization counterpart and notes that DB→Python regen is a manual recalibration step"
      pattern: "gen_global_percentile_cdf\\.py"
---

<objective>
Extend `.claude/skills/benchmarks/SKILL.md` with a new top-level **Chapter 4 — Global Percentile CDF** that documents the methodology, breakpoint set, per-rating-bucket sanity checks, and expected report shape for the global empirical-CDF artifact consumed by Phases 94/95.

Purpose: SKILL.md is the canonical source-of-truth for benchmark methodology. The CDF is a new cross-metric artifact whose methodology must be reproducible without reading CONTEXT.md or SEED-019. Phase 94 will Phase-94-interpolate user values against `GLOBAL_PERCENTILE_CDF` and the chip popover copy will trace back to the methodology documented here.

Output: A new chapter at the end of SKILL.md (after §3.4.3, before the existing "Report file layout" section if present, otherwise as the new final chapter) documenting the CDF methodology in the same authoring style as §3.1.5/§3.1.6/§3.2.2 (canonical CTE referenced not duplicated; per-metric output block; report rotation rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-global-percentile-benchmark-artifact/93-CONTEXT.md
@CLAUDE.md
@.claude/skills/benchmarks/SKILL.md
@reports/benchmarks-gap-metrics-percentile-candidacy.md
@.planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Chapter 4 — Global Percentile CDF to SKILL.md</name>
  <files>.claude/skills/benchmarks/SKILL.md</files>
  <read_first>
    - .claude/skills/benchmarks/SKILL.md (full file — but at 3069 lines, read only: lines 1-100 for chapter index/intro, lines 251-405 for canonical CTE + collapse-verdict methodology + equal-footing filter blocks the new chapter will reference, lines 1096-1340 for §3.1.5/§3.1.6 authoring style to mirror, lines 1585-1791 for §3.2.2 Section 2 ΔES subchapter authoring style, lines 2963-3069 for "Report file layout" section to insert before)
    - .planning/phases/93-global-percentile-benchmark-artifact/93-CONTEXT.md (D-01 through D-07 locked decisions)
    - .planning/ROADMAP.md Phase 93 section (success criteria #1–#5 — note especially #5: the breakpoint set is the **15-breakpoint tail-bounded** set, NOT the 19-breakpoint tail-densified set; the 93-CONTEXT.md D-06 reference to 19 breakpoints is **superseded by ROADMAP success criterion #5** — plan honors ROADMAP per planning_context "ROADMAP is the source of truth for goal + success criteria")
    - reports/benchmarks-gap-metrics-percentile-candidacy.md (2026-05-22 — the report-shape template the new chapter's expected output must mirror)
    - .planning/seeds/SEED-019-global-percentile-annotations-on-endgame-metrics.md (design context only — TS codegen guidance superseded by D-01)
  </read_first>
  <action>
    Append a new top-level Chapter 4 to SKILL.md. Place it immediately before the "Report file layout" section (around line 2963 in current file; verify exact insertion point by reading the surrounding 5–10 lines first). Chapter heading: `## 4. Global Percentile CDF`.

    **Chapter structure (mirror the §3.1.5/§3.1.6/§3.2.2 authoring style — short intro, per-subsection blocks, no fenced SQL duplication of Chapter 1 building blocks; cite by reference):**

    1. **Intro paragraph** — state what the chapter produces (per-metric empirical CDF over the 4 chipped ΔES metrics, global-pooled, fed into Python source at `app/services/global_percentile_cdf.py` via `scripts/gen_global_percentile_cdf.py`), what it does NOT produce (Recovery Score Gap; raw % gauges — explicitly cite D-02 rationale: Recovery is opponent-confounded with d=0.95 inverted rating coupling, raw gauges would be redundant chips on cards whose ΔES row is already chipped), and the downstream consumer (Phase 94 backend interpolates user values, emits scalar `{metric}_percentile` field — D-01: no TS mirror, no client-side codegen, no drift-guard).

    2. **In-scope metrics subsection** — bullet list of the 4 `MetricId` literals from `app/schemas/endgames.py`: `score_gap` (page-level Endgame Score Gap, §3.1.6), `achievable_score_gap` (page-level Achievable Score Gap, §3.1.5), `section2_score_gap_parity` (Section 2 Parity ΔES, §3.2.2), `section2_score_gap_conv` (Section 2 Conversion ΔES, §3.2.2). Each bullet: subchapter cross-link + one-line description + per-user inclusion floor (≥30 endgame AND ≥30 non-endgame for `score_gap`; ≥20 endgame-entry games for `achievable_score_gap`; ≥20 spans per Section-2 entry-eval bucket for `section2_score_gap_*`). Floors per "Claude's Discretion" item #2 in CONTEXT.md — keep the per-metric floors from the pre-flight for continuity.

    3. **Canonical CTE — inherited from §1** — short subsection citing the Chapter 1 building blocks the CDF query uses verbatim, no SQL duplication: Standard CTE — `selected_users` (§1), Sparse-cell exclusion (§1, drop `(elo_bucket=2400, tc='classical')` from pooled distribution), Equal-footing opponent filter `abs(opp − user) ≤ 100` (§1), Rating-lag selection bias mitigation — game-time ELO bucketing via `user_elo_at_game` / `elo_bucket` (§1, sub-800 dropped). Explicitly state that the pooled distribution is **global** (pooled across all `(elo_bucket, tc_bucket)` cells except the sparse cell), not per-cell — the chip phrasing "top X% of all players" requires a single global CDF, not per-(TC, ELO) CDFs.

    4. **Breakpoint set** — present the 15 locked breakpoints from ROADMAP success criterion #5. The label sequence MUST appear verbatim as the contiguous string `p1, p2.5, p5, p10, p20, p30, p40, p50, p60, p70, p80, p90, p95, p97.5, p99` somewhere in the chapter prose (a positive grep gate in `<verify>` asserts on this exact substring). State the rationale: tail-bounded at p1/p99 (NOT extended to p0.1/p99.9) because at the current pooled cohort (n≈2000 across the 4 metrics) the p0.1/p99.9 breakpoints have ~5pp sampling SE and would swing on single outliers; chip-rendered phrasing is "top 1% / bottom 1%" max. Note that tighter tails are a future ops task (cohort re-selection at higher `--per-cell`), not in v1.19 scope. **Important:** call out that 93-CONTEXT.md D-06 lists a 19-breakpoint tail-densified set; ROADMAP success criterion #5 supersedes — use the 15-breakpoint set. (Any incidental mention of p0.1/p99.9 must appear only in a "superseded / out of scope" footnote; SUMMARY.md will flag the CONTEXT vs ROADMAP divergence as the human-review gate.)

    5. **Per-rating-bucket sanity-check methodology** — for each of the 4 metrics, the report must include a per-rating-bucket table showing: rating bucket (800/1200/1600/2000/2400, game-time), n_users, median, skew, kurtosis. This mirrors `reports/benchmarks-gap-metrics-percentile-candidacy.md`. Purpose: verify the pooled distribution is reasonable across rating strata (e.g. Conversion ΔES is known to have skew ≈ −0.95 and excess kurtosis ≈ +1.42 per pre-flight; sanity-check tables document this is expected, not a data bug). Sparse `(2400, classical)` cell excluded from marginals (§1 rule).

    6. **Expected report shape** — describe what `reports/global-percentile-cdf-latest.md` must contain at minimum: (a) per-metric breakpoint table (15 rows × value columns: percentile + value at that percentile), (b) per-metric per-rating-bucket sanity-check table (5 rows: median, skew, kurtosis, n_users), (c) per-metric n_users and snapshot month header. Mirror the layout of `reports/benchmarks-gap-metrics-percentile-candidacy.md` (which is essentially a dry run of the CDF methodology — its per-metric tables and per-bucket medians are the format precedent). Per CONTEXT.md "Claude's Discretion" item #3, the slim format is sufficient (breakpoint table + one per-bucket sanity-check table per metric); rich format (full pre-flight-style per-bucket percentiles + ELO collapse verdicts) is not required.

    7. **Mechanization & rotation rule** — short subsection: the methodology is mechanized by `scripts/gen_global_percentile_cdf.py` (cross-link to Plan 02). The script runs the CTE deterministically against the benchmark DB (port 5433, read-only role; safety guard refuses to run unless `DATABASE_URL` contains `flawchess_benchmark` AND `:5433`, mirroring `scripts/backfill_eval.py` `--db benchmark`). DB → Python regeneration is a **manual recalibration step** (no CI gate, no auto-regen). Report rotation rule (D-07): on each run, the existing `reports/global-percentile-cdf-latest.md` is archived in place as `reports/global-percentile-cdf-YYYY-MM-DD.md` (date taken from the report's first-line header), then the new latest report is written. Same convention as `reports/benchmarks-latest.md`.

    8. **Out of scope (explicit)** — bullet list of what this chapter does NOT cover, to prevent future-mode drift: Recovery Score Gap percentiles (opponent-confounded, d=0.95 inverted — defer until Recovery is repaired or re-framed), raw % gauge percentiles (Conv/Parity/Recov rate gauges — redundant with ΔES chips on same card), per-(TC, ELO)-cell CDFs (SEED-019 deliberately ships global-only comparison — bragging-rights framing is the explicit product call), tier-4 per-endgame-class CDFs (per-class samples too thin, deferred per REQUIREMENTS.md §Future Requirements), opening insights percentiles (out of v1.19 scope, candidate for future Opening Insights v2 milestone), TS mirror / client-side codegen (D-01 — add only when a client-side CDF consumer ships).

    **Cross-references to update inside SKILL.md (small surgical edits beyond the new chapter):**
    - Existing chapter index near the top of SKILL.md (if present) — add a one-line entry for Chapter 4.
    - "Report file layout" section (around line 2963) — add `reports/global-percentile-cdf-latest.md` to the report-file inventory if such an inventory exists; if no inventory exists in that section, skip this edit and rely on the in-chapter rotation rule text.

    **Authoring constraints (mirror the rest of SKILL.md):**
    - No magic numbers in prose — the 15 breakpoints, the per-metric inclusion floors, the sparse-cell rule, the equal-footing tolerance ±100 Elo are all values the reader can grep for; inline them once with their rationale.
    - Use the established Display formatting rules (§1, line 368): score gaps render in `pp` with one decimal (`−2.3pp`), Cohen's d to 2 decimals, sample sizes as integers. The CDF stores raw 0–1 score-difference values internally; the report renders them in `pp`.
    - No fenced SQL blocks duplicating Chapter 1 building blocks — cite by reference (e.g. "see §1 Standard CTE", "see §1 user_elo_at_game / elo_bucket"). The mechanization is in the Plan 02 script.
    - One fenced code block IS acceptable in the chapter: a short illustrative SQL snippet showing how the 15-breakpoint percentile_cont call composes onto the canonical CTE for one example metric (e.g. `achievable_score_gap`). This snippet is illustrative, not authoritative — the authoritative SQL lives in `scripts/gen_global_percentile_cdf.py`.

    Honor the "Claude's Discretion" decisions documented in CONTEXT.md and surface them briefly in chapter prose so future readers see the chosen shape:
    - Discretion #2 (per-user inclusion floor per metric): per-metric floors kept from pre-flight (≥30/≥20 thresholds as listed in subsection 2).
    - Discretion #3 (report depth): slim format (per-metric breakpoint table + one per-bucket sanity-check table per metric).
    - Discretion #4 (audit-trail constants in committed Python): `BENCHMARK_DB_SNAPSHOT_MONTH = "2026-03"` + per-metric `n_users` field — noted in the chapter as required fields that the report header AND the committed Python source carry (Plan 02 implements the Python side).
  </action>
  <verify>
    <automated>grep -c "^## 4\. Global Percentile CDF" .claude/skills/benchmarks/SKILL.md  # must be 1</automated>
    <automated>grep -v '^#' .claude/skills/benchmarks/SKILL.md | grep -c "global-percentile-cdf-latest.md"  # must be >= 1 (rotation rule prose; -v drops headings so comment lines do not self-fulfil the gate)</automated>
    <automated>grep -v '^#' .claude/skills/benchmarks/SKILL.md | grep -Ec "section2_score_gap_(conv|parity)"  # must be >= 2 (both in-scope Section 2 IDs)</automated>
    <automated>grep -v '^#' .claude/skills/benchmarks/SKILL.md | grep -c "achievable_score_gap"  # must be >= 1 (must be present after edit — already in older chapters)</automated>
    <automated>grep -F "p1, p2.5, p5, p10, p20, p30, p40, p50, p60, p70, p80, p90, p95, p97.5, p99" .claude/skills/benchmarks/SKILL.md  # must return >= 1 line — positive assertion that the 15-breakpoint label sequence appears verbatim (replaces fragile negative grep on p0.1/p99.9; the SUMMARY.md "CONTEXT vs ROADMAP divergence flagged" note is the human gate for footnote handling)</automated>
    <automated>uv run ruff format --check .claude/  # no python here but the directory check is fast; skip if ruff doesn't traverse .md (it won't — this is a no-op safety net, expected to pass trivially)</automated>
  </verify>
  <done>
    - SKILL.md now contains a Chapter 4 heading (`## 4. Global Percentile CDF`) with the 8 subsections described above.
    - The 4 in-scope MetricIds are listed by their literal names from `app/schemas/endgames.py`.
    - The 15-breakpoint label sequence appears verbatim as the contiguous string `p1, p2.5, p5, p10, p20, p30, p40, p50, p60, p70, p80, p90, p95, p97.5, p99`; any reference to the 19-breakpoint set appears only in a "superseded by ROADMAP success criterion #5" footnote.
    - Recovery Score Gap and raw % gauges are explicitly listed as out of scope with the D-02 rationale.
    - Canonical CTE / sparse-cell / equal-footing / game-time bucketing are referenced by §1 cross-link, not duplicated.
    - Rotation rule for `reports/global-percentile-cdf-latest.md` is documented.
    - Plan 02's script (`scripts/gen_global_percentile_cdf.py`) and committed artifact (`app/services/global_percentile_cdf.py`) are named in the mechanization subsection.
    - All grep gates in <verify> pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → SKILL.md | Documentation-only edit; no code-path executed at edit time. The methodology is read by a future skill invocation (operator or Claude) that THEN runs queries against the benchmark DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Information disclosure | SKILL.md prose | accept | Methodology is non-sensitive (no credentials, no PII). The benchmark DB schema and CTE shape are already publicly documented in earlier SKILL.md chapters. |
| T-93-02 | Tampering | Documentation drift between SKILL.md §4 and `scripts/gen_global_percentile_cdf.py` | mitigate | Plan 02 script docstring cross-references SKILL.md §4 as the methodology source-of-truth. Future regenerations should re-read §4 before editing the script. No automated drift-guard (per D-01 — Python-only artifact, no CI gate). |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (documentation edit only). |
</threat_model>

<verification>
- Plan 01 acceptance: `grep -c "^## 4\. Global Percentile CDF" .claude/skills/benchmarks/SKILL.md` returns `1`.
- Plan 01 acceptance: 4 in-scope MetricId literals all appear in the new chapter.
- Plan 01 acceptance: 15-breakpoint label sequence appears verbatim; 19-breakpoint set appears only in a superseded footnote (SUMMARY.md flags the divergence as the human gate).
- Plan 01 acceptance: §1 cross-references (Standard CTE, sparse-cell, equal-footing, game-time bucketing) are cited, not duplicated.
- Plan 01 acceptance: Recovery + raw % gauges explicitly out of scope with D-02 rationale.
- Plan 01 acceptance: rotation rule named and cross-referenced to `reports/benchmarks-latest.md` precedent.
</verification>

<success_criteria>
- Chapter 4 added at the end of SKILL.md (before "Report file layout" section if present) with all 8 subsections.
- Methodology is reproducible from §4 alone without reading CONTEXT.md or SEED-019.
- Subchapter cross-references §1 building blocks (no SQL duplication).
- Out-of-scope items match REQUIREMENTS.md §Out of Scope + ROADMAP Phase 93 boundary.
- All <verify> grep gates pass.
- ROADMAP success criterion #3 (the /benchmarks SKILL.md CDF subchapter exists, documenting methodology + SQL templates + expected report shape) is satisfied.
</success_criteria>

<output>
Create `.planning/phases/93-global-percentile-benchmark-artifact/93-01-SUMMARY.md` when done.

Summary should include:
- Final placement (line number where Chapter 4 starts)
- Resolved discretion decisions (per-user floors, report depth, audit-trail fields)
- Any deviations from CONTEXT.md (notably: 15-breakpoint set per ROADMAP, not 19-breakpoint set per 93-CONTEXT.md D-06 — flag this as a ROADMAP/CONTEXT divergence that the planner resolved in favor of ROADMAP)
- Cross-references made to Plan 02 (script + module names)
</output>
</output>
