---
phase: 185-bots-roster-transpose-win-stars
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/personas/personaRegistry.ts
  - frontend/src/components/bots/PersonaGrid.tsx
  - frontend/src/components/bots/__tests__/PersonaGrid.test.tsx
autonomous: true
requirements: []   # No formal REQ-IDs (ROADMAP Requirements: TBD). Behavior-based coverage; spec-less probe fallback visibly skipped.

must_haves:
  truths:
    - "The Bots roster renders 6 rung rows (800 top → 1800 bottom) × 4 style columns, with a single header row of the 4 style names in their accent colors and no row labels"
    - "Persona-card DOM order is rung-major: the 4 personas at rung 800 (in STYLE_SECTION_ORDER), then rung 1000, ... up to 1800"
    - "All 4 style header names (Attacker/Trickster/Grinder/Wall) fit at text-sm in the 4-column header row down to a 320px viewport with no truncation"
    - "Every one of the 6 rung rows renders exactly 4 personas (one per style) — exhaustive by the Record<PersonaId, Persona> typing, no missing-cell state possible"
  artifacts:
    - "frontend/src/lib/personas/personaRegistry.ts — exported rung-major accessor (personasForRung or exported RUNGS)"
    - "frontend/src/components/bots/PersonaGrid.tsx — transposed layout with bots-persona-header-{style} testids"
    - "frontend/src/components/bots/__tests__/PersonaGrid.test.tsx — DOM-order assertion rewritten to rung-major"
  key_links:
    - "PersonaGrid iterates RUNGS (rows) outer × STYLE_SECTION_ORDER (cols) inner via the registry accessor — never Object.values(PERSONA_REGISTRY) with manual sort (Pitfall 1)"
  ui_considerations:
    - "No-row-labels legibility backstop":
        statement: "A user can tell which row is which rung purely from each card's own ELO/calibratedLabel without a row label, because every card in a row shares approximately the same calibratedLabel (~800/~1000/etc, modulo per-persona calibration offsets)"
        verification: backstop
---

<objective>
Transpose the Bots roster (`PersonaGrid`) from the current 4 style `<section>` blocks (each 6 personas ascending by rung) into a single grid: one header row of the 4 style names in their accent colors, then 6 rung rows (800 top → 1800 bottom), 4 `PersonaCard`s per row in `STYLE_SECTION_ORDER`, with no row labels. Pure presentational re-layout of the already-loaded, compile-time-static `PERSONA_REGISTRY` — no new data source, no network.

This is an independent, low-risk feature that shares no architecture with the Plan 01 win-tracking tracer, so it runs in parallel (Wave 1). It touches `PersonaGrid.tsx` and `personaRegistry.ts` only; the win-stars prop threading (Plan 03) edits the same `PersonaGrid.tsx` later in Wave 2, building on this transposed layout.

Purpose: rung-aligned columns make cross-style strength comparison at a glance the primary reading order.
Output: transposed `PersonaGrid`, a rung-major registry accessor, and a rewritten DOM-order test.

**Spec-less probe fallback:** visibly skipped (no requirement IDs to probe).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/185-bots-roster-transpose-win-stars/185-RESEARCH.md
@.planning/phases/185-bots-roster-transpose-win-stars/185-PATTERNS.md
@.planning/phases/185-bots-roster-transpose-win-stars/185-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a rung-major registry accessor</name>
  <files>frontend/src/lib/personas/personaRegistry.ts</files>
  <read_first>
    - frontend/src/lib/personas/personaRegistry.ts (the module-private `RUNGS` const on line 60, the private `personaId(style, rung)` helper on line 120, and `personasForSection(style)` on lines 473-474 — the exact abstraction level to mirror)
  </read_first>
  <action>
    Expose a rung-major accessor so `PersonaGrid` can iterate rows outer, columns inner, without duplicating registry-internal ordering logic in the component (Pitfall 1). Prefer adding `export function personasForRung(rung: Rung): Persona[]` next to `personasForSection`, returning `STYLE_SECTION_ORDER.map((style) => PERSONA_REGISTRY[personaId(style, rung)])` — the 4 personas at that rung in style order. Also `export const RUNGS` (currently module-private on line 60) so the grid can iterate the 6 rungs as rows. Do NOT change `personasForSection` (still used elsewhere) and do NOT reach into `Object.values(PERSONA_REGISTRY)` from the component.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/lib/personas/__tests__/personaRegistry.test.ts 2>/dev/null || npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `RUNGS` is exported and equals `[800, 1000, 1200, 1400, 1600, 1800]` (source assertion)
    - `personasForRung(rung)` returns exactly 4 personas in `STYLE_SECTION_ORDER` order for any rung (source assertion / unit test if a registry test file exists)
    - `npx tsc -b` in frontend passes (no type errors from the new exports)
    - `personasForSection` is unchanged (source assertion)
  </acceptance_criteria>
  <done>A rung-major accessor and RUNGS are exported at the same abstraction level as personasForSection; the transpose can iterate rungs×styles without touching PERSONA_REGISTRY directly.</done>
</task>

<task type="auto">
  <name>Task 2: Transpose PersonaGrid to rung-rows × style-columns + rewrite DOM-order test</name>
  <files>frontend/src/components/bots/PersonaGrid.tsx, frontend/src/components/bots/__tests__/PersonaGrid.test.tsx</files>
  <read_first>
    - frontend/src/components/bots/PersonaGrid.tsx (current per-style `<section>` render on lines 75-89, the STYLE_ACCENT map on lines 25-30, the PersonaGridProps interface on lines 32-40 — all to restructure/preserve)
    - frontend/src/components/bots/__tests__/PersonaGrid.test.tsx (the existing DOM-order test asserting STYLE_SECTION_ORDER.flatMap(personasForSection) — this MUST be rewritten to rung-major, not patched: Pitfall 2)
    - .planning/phases/185-bots-roster-transpose-win-stars/185-UI-SPEC.md (Layout & Interaction Notes → Grid structure: header row, body rows, no row labels, responsive columns, data-testid migration)
    - frontend/src/lib/personas/personaRegistry.ts (the new personasForRung/RUNGS exports from Task 1)
  </read_first>
  <action>
    Replace the `STYLE_SECTION_ORDER.map((style) => <section>...)` block (lines 75-89) with: (1) one header row — a `grid-cols-4` row of 4 cells, one per style in `STYLE_SECTION_ORDER`, each cell's text colored via the existing `STYLE_ACCENT[style]` value (verbatim), `text-sm font-semibold`, centered, carrying `data-testid={`bots-persona-header-${style.toLowerCase()}`}` (this REPLACES the old per-section `bots-persona-section-${style}` testid — Pitfall 2 follow-through); (2) the body — `RUNGS.map((rung) => ...)` outer producing 6 rows, each a `grid-cols-4` row of `personasForRung(rung).map((persona) => <PersonaCard .../>)` in style order. Keep the grid at `grid-cols-4` at ALL breakpoints (do NOT reflow to fewer columns on mobile — the 4 style columns are the whole point; differs from the current `grid-cols-3 sm:grid-cols-6`). Ensure header and body share the same column template so columns align. NO row labels/gutter (locked decision). Keep the player-rating caption line, InfoPopover, and Custom button in their existing relative positions, all unchanged. Every existing `data-testid="bots-persona-card-${persona.id}"` on individual cards stays unchanged. Then REWRITE the DOM-order test's expected-order construction to `RUNGS.flatMap((rung) => personasForRung(rung).map((p) => p.id))` (rung-major) and update the section-testid assertion to assert the 4 header-cell testids (`bots-persona-header-*`) and their accent colors instead of the dropped section wrappers.
    Do NOT add a `useQuery` call or any win-count prop here — that is Plan 03's job (this plan's PersonaGrid takes the same props it does today).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/bots/__tests__/PersonaGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The grid renders exactly one header row of 4 style-name cells with `data-testid="bots-persona-header-{style}"` and `STYLE_ACCENT` colors (test assertion)
    - Persona-card DOM order equals `RUNGS.flatMap((rung) => personasForRung(rung).map((p) => p.id))` — rung-major (rewritten test assertion, not the old style-major order)
    - All 24 `bots-persona-card-{id}` testids still render, unchanged (test assertion)
    - No `bots-persona-section-*` testid remains in the component (source assertion: `grep -c "bots-persona-section" PersonaGrid.tsx` returns 0)
    - The grid uses `grid-cols-4` with no `sm:grid-cols-*` reflow to a different column count (source assertion)
    - `PersonaGrid.test.tsx` passes; reverting the transpose (restoring style-major iteration) makes the rewritten DOM-order assertion fail (mutation check)
  </acceptance_criteria>
  <done>The Bots roster renders 6 rung rows × 4 style columns with an accent-colored header row and no row labels; the DOM-order test is rewritten to rung-major and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side re-layout of a compile-time-static registry; no user input, no network, no new data source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-185-05 | Information Disclosure | PersonaGrid presentational transpose | low | accept | No new trust boundary — re-layout of the static `PERSONA_REGISTRY`; no user-supplied input reaches this code path |
| T-185-SC | Tampering | npm/pip/cargo installs | low | accept | no new packages introduced this plan (reuses lucide-react/existing imports) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/bots/__tests__/PersonaGrid.test.tsx` green
- Wave merge gate: `cd frontend && npm test -- --run` + `npm run lint` + `npx tsc -b`
- Manual (185-VALIDATION.md): open /bots, confirm 6 rung rows × 4 style columns on desktop and mobile drawer widths; visually confirm the no-row-labels legibility backstop (rung readable from each card's calibratedLabel)
</verification>

<success_criteria>
- Transposed grid: header row of 4 accent-colored style names, 6 rung rows, no row labels, 4 columns at all breakpoints
- DOM order rung-major; DOM-order test rewritten and green; no leftover section testids
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan (net-new, excluded from drift verification):
- `personaRegistry.ts` exports: `personasForRung`, `RUNGS`
- `PersonaGrid.tsx` testids: `bots-persona-header-{style}` (replaces removed `bots-persona-section-{style}`)

<output>
Create `.planning/phases/185-bots-roster-transpose-win-stars/185-02-SUMMARY.md` when done
</output>
