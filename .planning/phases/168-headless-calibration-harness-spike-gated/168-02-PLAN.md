---
phase: 168-headless-calibration-harness-spike-gated
plan: 02
type: execute
wave: 2
depends_on: [168-01]
files_modified:
  - scripts/calibration-harness.mjs
  - scripts/lib/calibration-determinism.check.mjs
autonomous: false
requirements: [CAL-03, CAL-02]
must_haves:
  truths:
    - "A single bot-vs-anchor game plays ply-by-ply using the LIVE selectBotMove (via @/) for the bot and the anchor movers for the opponent, ending on a real terminal condition, Stockfish-eval adjudication, or the ply cap."
    - "The per-move search budget is a FIXED harness constant (mirroring the app's maxNodes/maxPlies) constant across the whole grid, with SearchBudget.concurrency fixed at 1 (single shared Stockfish process)."
    - "The spike plays a handful of games in the MOST EXPENSIVE cell (blend=1, full-Stockfish grading) and reports moves/sec + projected full-grid wall-clock + a WASM-vs-onnxruntime-node go/no-go recommendation (SC1/CAL-03)."
    - "The same --seed reproduces byte-identical blend=1 games (deterministic argmax + seeded opening assignment)."
  artifacts:
    - scripts/calibration-harness.mjs
    - scripts/lib/calibration-determinism.check.mjs
  key_links:
    - "selectBotMove is called with deps={policy,grade,rng} and deps.search omitted so it defaults to the real mctsSearch — identical wiring to the app."
    - "The single shared Stockfish process serves bot grade(), Stockfish-skill anchor moves, and adjudication eval; option state is reset before every go."
---

<objective>
Deliver the D-03 throughput spike (CAL-03) — the gate that decides whether the WASM path can build the full grid.

Build the single bot-vs-anchor game loop in `scripts/calibration-harness.mjs` — the bot moves via the LIVE `selectBotMove` (CAL-02 wiring), the anchor moves via the Plan-01 anchor movers, and the game ends on a real chess.js terminal condition, sustained Stockfish-eval adjudication, or a hard ply cap (D-10), all budgeted by FIXED named constants (D-11). Then add throughput instrumentation and run the spike in the MOST EXPENSIVE cell (`blend=1`, full-Stockfish grading every move): report moves/sec, projected full-grid wall-clock, and a proceed-on-WASM vs fall-back-to-`onnxruntime-node` recommendation. Add a determinism check proving `--seed` reproducibility.

This plan GATES Plan 03 (the full grid). Do NOT re-litigate feasibility — Phase 165 already proved Maia runs headless in Node; this measures throughput only (D-03).

Purpose: honor the spike gate before committing to the full grid build.
Output: the harness's game loop + spike mode + a determinism assertion, plus a human go/no-go checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-CONTEXT.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-RESEARCH.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-PATTERNS.md
@.planning/phases/168-headless-calibration-harness-spike-gated/168-01-SUMMARY.md
</context>

<artifacts_produced>
New files / symbols (exclude from drift checks — this phase's own new code):
- `scripts/calibration-harness.mjs` — `playGame(...)` (single bot-vs-anchor game), `main()` setup skeleton, spike-mode timing; named constants `FLAWCHESS_ENGINE_MAX_NODES=400`, `FLAWCHESS_ENGINE_MAX_PLIES=8`, `SEARCH_CONCURRENCY=1`, `ADJUDICATION_CP_THRESHOLD=600`, `ADJUDICATION_SUSTAIN_PLIES=4`, `PLY_CAP=120`. CLI flags: `--elo`, `--blends`, `--anchors`, `--games-per-cell`, `--seed` (grid axes finalized in Plan 03).
- `scripts/lib/calibration-determinism.check.mjs` — same-`--seed` byte-identity assertion.
Note: Plan 03 extends `scripts/calibration-harness.mjs` (grid sweep + TSV) — this plan owns the game loop + spike; Plan 03 owns the outer sweep.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bot-vs-anchor game loop (selectBotMove wiring, terminal + adjudication cutoffs, fixed budget)</name>
  <files>scripts/calibration-harness.mjs</files>
  <read_first>
    - 168-RESEARCH.md Pattern 1 (lines 186-220, selectBotMove call site + budget), Pitfall 2 (lines 344-348, shared-process option reset), Pitfall 3 (lines 350-354, concurrency=1), Open Question 3 (lines 431-434, adjudication constants).
    - frontend/src/lib/engine/selectBotMove.ts lines 61-102; frontend/src/hooks/useFlawChessEngine.ts lines 39,45,220-233 (budget defaults to mirror — module-private, so redefine as harness constants).
    - frontend/src/lib/engine/treeCommon.ts terminalValue (chess.js terminal checks) — call isGameOver/isCheckmate/isStalemate/isThreefoldRepetition/isInsufficientMaterial directly.
    - 168-PATTERNS.md "calibration-harness.mjs" header/imports (lines 27-48) and game-loop row (line 16); scripts/lib/calibration-anchors.mjs + calibration-providers.mjs from Plan 01.
  </read_first>
  <action>
    In `scripts/calibration-harness.mjs`, write `playGame({ providers, stockfish, botElo, botBlend, anchorSpec, startFen, botIsWhite, gameRng })` that drives a chess.js board ply-by-ply from `startFen`. On the bot's turn, call the LIVE `selectBotMove(fen, settings, deps)` where `settings = { elo: botElo, blend: botBlend, budget: { maxNodes: FLAWCHESS_ENGINE_MAX_NODES, maxPlies: FLAWCHESS_ENGINE_MAX_PLIES, concurrency: SEARCH_CONCURRENCY } }` and `deps = { policy: providers.policy, grade: providers.grade, rng: gameRng }` — OMIT `deps.search` so it defaults to the real `mctsSearch` (identical wiring to the app; never branch on `blend` in the harness — CAL-02). On the anchor's turn, dispatch to `maiaArgmaxMove` (raw-Maia rung) or `stockfishSkillMove` (SF skill level) per `anchorSpec`. Apply each UCI move via chess.js. After every move, check terminal conditions in this order (D-10): chess.js `isGameOver` family → classify W/D/L by result + side; else run a single-line Stockfish adjudication eval (`go movetime` at current position, top-line score, reset options first — Pitfall 2) and if `|eval| >= ADJUDICATION_CP_THRESHOLD` holds for the same favored side across `ADJUDICATION_SUSTAIN_PLIES` consecutive plies, adjudicate that side the winner; else if `ply >= PLY_CAP`, adjudicate by final eval sign (draw if within threshold). Define ALL of `FLAWCHESS_ENGINE_MAX_NODES`(400), `FLAWCHESS_ENGINE_MAX_PLIES`(8), `SEARCH_CONCURRENCY`(1), `ADJUDICATION_CP_THRESHOLD`(600), `ADJUDICATION_SUSTAIN_PLIES`(4), `PLY_CAP`(120) as named constants with a doc comment (no magic numbers; flag the adjudication trio as tunable/[ASSUMED] per Open Question 3). Return a structured game result (`{ result: 'win'|'loss'|'draw', reason, plies, moveUcis }`). Keep `playGame` and its helpers within the nesting/logic-LOC limits — split the terminal-check and adjudication logic into small helpers. Add a minimal `main()` that parses `--elo/--blends/--anchors/--games-per-cell/--seed`, does one-time `createMaiaSession` + `spawnStockfish` + `makeNodeProviders`, and can play a single game (full grid sweep is Plan 03).
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1500 --blends 0 --anchors sf0 --games-per-cell 1 --seed 1 2>&1 | grep -Eq 'result[:=].*(win|loss|draw)'</automated>
  </verify>
  <acceptance_criteria>
    - A single game (blend=0 bot vs sf0 anchor) plays to a terminal/adjudicated/ply-cap result and prints a `win|loss|draw` outcome with a reason and ply count.
    - The bot move is produced by the live `selectBotMove` with `deps.search` omitted (executor confirms no local search/argmax/sampling in the harness).
    - All budget + adjudication values are named constants; `SearchBudget.concurrency === 1`.
    - The shared Stockfish process resets its options before every grade/anchor/adjudication `go`.
  </acceptance_criteria>
  <done>One faithful bot-vs-anchor game runs end-to-end using the exact app move-selection code, bounded by the three D-10 cutoffs.</done>
</task>

<task type="auto">
  <name>Task 2: Throughput spike (CAL-03) + determinism check</name>
  <files>scripts/calibration-harness.mjs, scripts/lib/calibration-determinism.check.mjs</files>
  <read_first>
    - 168-CONTEXT.md D-03 (throughput not feasibility; most-expensive cell; go/no-go) and D-01/D-02 (WASM lock, onnxruntime-node fallback only).
    - 168-RESEARCH.md Validation Architecture "Sampling Rate" → Phase gate (lines 472-475) and Pitfall 3 fallback lever (multi-process Stockfish pool, NOT onnxruntime-node, if grading is the bottleneck).
    - 168-RESEARCH.md CAL-03 test-map row (line 467) — the spike invocation shape.
  </read_first>
  <action>
    Add a throughput instrumentation path to `calibration-harness.mjs`: track total moves played and wall-clock elapsed across the games in a run, and after the run print a spike report — `moves/sec`, mean `seconds/game`, and the PROJECTED full-grid wall-clock (bot cells × anchors × games-per-cell × mean game length, using the default grid dimensions as the projection basis). Print an explicit recommendation line: proceed on the WASM (`onnxruntime-web`) path if the projected full-grid wall-clock is within a reasonable budget, else recommend the documented fallback — first a multi-process Stockfish pool if grading is the hot path (Pitfall 3), and only `onnxruntime-node` if Maia inference itself is the bottleneck (D-02). The spike is run as a small invocation of THIS harness in the most expensive cell: `--elo 1900 --blends 1 --anchors sf5 --games-per-cell 4`. Write `scripts/lib/calibration-determinism.check.mjs` mirroring `gem-parity.check.mjs`'s structure: run a single `blend=1` game twice with the same `--seed`/opening/color and assert (`node:assert/strict`) the two `moveUcis` sequences are byte-identical (deterministic argmax + seeded assignment, D-09); print `PASS:` + exit 0.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1900 --blends 1 --anchors sf5 --games-per-cell 4 --seed 7 2>&1 | grep -Eq 'moves/sec|projected'</automated>
  </verify>
  <acceptance_criteria>
    - The spike invocation prints moves/sec, mean seconds/game, projected full-grid wall-clock, and a proceed-vs-fallback recommendation.
    - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs` prints `PASS:` and exits 0 (same seed → byte-identical blend=1 game).
    - The report explicitly names WASM as the recommended path unless throughput mandates the multi-process-pool / onnxruntime-node fallback (D-02).
  </acceptance_criteria>
  <done>CAL-03's throughput measurement + go/no-go recommendation exist as a repeatable harness invocation; seeded reproducibility is proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Spike go/no-go gate (D-03)</name>
  <action>Present the spike throughput report and obtain the human proceed-on-WASM vs adopt-fallback decision before Plan 03 builds the full grid.</action>
  <what-built>The throughput spike (Task 2) prints measured moves/sec, projected full-grid wall-clock, and a WASM-vs-fallback recommendation for the most-expensive cell (blend=1, full-Stockfish). This gates whether Plan 03 builds the full grid on the WASM path (D-03).</what-built>
  <how-to-verify>
    1. Run: `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/calibration-harness.mjs --elo 1900 --blends 1 --anchors sf5 --games-per-cell 4 --seed 7`
    2. Read the printed moves/sec + projected full-grid wall-clock.
    3. Confirm the projection is an acceptable wall-clock for building the full grid (Plan 03) on the WASM path — OR decide to adopt the documented fallback (multi-process Stockfish pool, or onnxruntime-node only if Maia inference is the bottleneck).
  </how-to-verify>
  <resume-signal>Type "proceed" (WASM path is viable → Plan 03 proceeds as designed) or describe the fallback to adopt (which re-scopes Plan 03).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → harness | Offline dev tool; only untrusted input is developer-supplied flags. No network, no auth, no persisted multi-user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-03 | Tampering | shared Stockfish across bot-grade / anchor / adjudication | medium | mitigate | Reset `Skill Level`/`UCI_LimitStrength` before every `go` so a weakened anchor level never leaks into the bot's own grading (Pitfall 2) — corrupts the measurement, not a security exploit. |
| T-168-04 | Tampering | concurrency > 1 against one engine | medium | mitigate | Fix `SEARCH_CONCURRENCY=1`; a single engine cannot serve overlapping `go`s (Pitfall 3). |
| T-168-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Task 1 + Task 2 automated checks pass; determinism check green.
- The spike report is human-readable and the go/no-go checkpoint is answered before Plan 03 runs.
</verification>

<success_criteria>
A single faithful bot-vs-anchor game runs on the live selectBotMove; the throughput spike reports moves/sec + projected wall-clock + a go/no-go recommendation; seeded reproducibility proven; human gate answered.
</success_criteria>

<output>
Create `.planning/phases/168-headless-calibration-harness-spike-gated/168-02-SUMMARY.md` when done.
</output>
