---
phase: 186-import-filters-tc-and-game-cap
plan: 03
type: execute
wave: 2
depends_on: ["186-01"]
files_modified:
  - frontend/src/hooks/useImportSettings.ts
  - frontend/src/components/filters/ImportFilterCard.tsx
  - frontend/src/pages/Import.tsx
  - frontend/src/components/filters/__tests__/ImportFilterCard.test.tsx
autonomous: true
requirements: [IMPORT-01, IMPORT-04]
must_haves:
  truths:
    - "The Import tab shows an 'Import filters' card ABOVE the two platform cards with a TC multiselect row (bullet/blitz/rapid/classical) and a cap single-select row (1000/3000/5000) (D-08)."
    - "Toggling a TC or changing the cap auto-saves via an immediate PATCH to /users/me/import-settings with no Save button and no dirty state (D-09)."
    - "Deselecting the last remaining active TC re-selects it instead (no-op guard) so at least one TC is always enabled (UI-SPEC zero-one-many resolution)."
    - "Budget chips render only for currently-selected TCs; a deselected TC's chip disappears entirely with no dimmed placeholder (D-11, UI-SPEC partial)."
    - "Before any backlog import a budget chip renders the plain count '0/{cap}' with no special empty copy (UI-SPEC empty)."
    - "A budget chip at or over its cap renders in text-foreground font-semibold (full/complete), never in destructive/red styling; over-cap grandfathered counts (e.g. 'Blitz 8000/5000') use the identical full treatment (D-11/D-12, UI-SPEC overflow full-state)."
    - "The Import filters card rides the same profile/settings fetch that already gates the Import page — no separate spinner or skeleton (UI-SPEC loading)."
    - "Guests see the identical filter UI and defaults with no isGuest branch — one code path (D-16, UI-SPEC guest parity)."
    - "The inline helper line reads exactly 'Limits how much older history is imported — new games always sync.' and a HelpCircle InfoPopover carries the full three-sentence rule (D-10)."
    - "Every new interactive element has an import-filter-* / import-budget-chip-* data-testid and the TC/cap toggle buttons carry aria-pressed (browser-automation rules)."
    - statement: "The import-settings GET failure shows the CLAUDE.md-mandated isError copy ('Failed to load import settings. Something went wrong. Please try again in a moment.') and a PATCH failure surfaces an inline text-destructive error with optimistic-rollback of the toggle."
      verification: backstop
    - statement: "At 4 TCs with over-cap 5-digit counts on a 375px-wide viewport the budget-chip row wraps via flex-wrap and never truncates or ellipsizes a count."
      verification: backstop
  artifacts:
    - frontend/src/hooks/useImportSettings.ts (useImportSettings query + useUpdateImportSettings mutation)
    - frontend/src/components/filters/ImportFilterCard.tsx
    - "Import.tsx budget-chip rows inside each platform card"
    - frontend/src/components/filters/__tests__/ImportFilterCard.test.tsx
  key_links:
    - "ImportFilterCard toggle -> useUpdateImportSettings PATCH -> backend persists (nothing runs until Sync, D-03)"
    - "useImportSettings.backlog_counts + game_cap -> per-(platform,TC) budget chips in Import.tsx"
---

<objective>
Build the Import-tab filter UI: an "Import filters" card (TC multiselect + cap single-select + inline copy + info popover) above the platform cards, a settings hook with auto-save-on-toggle, and per-(platform,TC) budget chips inside each platform card.

Implements: IMPORT-01 (filter UI), IMPORT-04 (at-cap legibility). Decisions: D-08 (card placement), D-09 (auto-save, no Save button, no dirty state), D-10 (inline copy + HelpCircle popover), D-11 (budget chips for selected TCs, full reads as complete), D-12 (over-budget honest count with full styling), D-16 (guest parity, one code path).

Purpose: Users can pick TCs and a cap; at-cap state is legible so enabling a TC while at cap doesn't look broken.
Output: useImportSettings hook, ImportFilterCard component, Import.tsx budget chips, component test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-import-filters-tc-and-game-cap/186-CONTEXT.md
@.planning/phases/186-import-filters-tc-and-game-cap/186-UI-SPEC.md
@.planning/phases/186-import-filters-tc-and-game-cap/186-PATTERNS.md
@.planning/phases/186-import-filters-tc-and-game-cap/186-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `frontend/src/hooks/useImportSettings.ts` — `useImportSettings()` (useQuery, GET /users/me/import-settings, queryKey ['import-settings']) and `useUpdateImportSettings()` (useMutation, PATCH, optimistic update + rollback on error).
- `frontend/src/components/filters/ImportFilterCard.tsx` — the "Import filters" card component.
- `frontend/src/pages/Import.tsx` — mounts ImportFilterCard; renders per-(platform,TC) budget-chip rows.
- `frontend/src/components/filters/__tests__/ImportFilterCard.test.tsx` — RTL component test.
- Types: an `ImportSettings` / `ImportSettingsUpdate` TS interface (mirroring the backend schema) in the hook file or a shared types module.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: useImportSettings hook + ImportFilterCard component</name>
  <files>frontend/src/hooks/useImportSettings.ts, frontend/src/components/filters/ImportFilterCard.tsx</files>
  <read_first>
    - frontend/src/hooks/useImport.ts (whole file — TanStack Query useQuery/useMutation shape against apiClient)
    - frontend/src/components/filters/FilterPanel.tsx lines 236-250 (toggleTimeControl last-one-standing guard), 406-433 (TC button grid), 499-514 (rated ToggleGroup single-select — cap row analog)
    - frontend/src/components/ui/card.tsx (Card/CardHeader size="compact"/CardBody — the project wrapper, NOT stock shadcn card)
    - frontend/src/components/ui/info-popover.tsx (InfoPopover — reuse verbatim, HelpCircle, text-xs body already correct)
    - frontend/src/components/ui/toggle-group.tsx (ToggleGroup/ToggleGroupItem)
    - frontend/src/components/icons/TimeControlIcon.tsx (per-TC icon used in the row)
    - frontend/src/lib/theme.ts (verify no new semantic color needed; full-state uses text-foreground/font-semibold, in-progress text-muted-foreground)
    - .planning/phases/186-import-filters-tc-and-game-cap/186-UI-SPEC.md (Copywriting Contract locked copy; Component & Layout Notes 1-7; Color contract full-state rule)
    - .planning/phases/186-import-filters-tc-and-game-cap/186-01-SUMMARY.md (settings endpoint shape + response fields incl. backlog_counts)
  </read_first>
  <action>
    Create `useImportSettings.ts` mirroring `useImport.ts`: `useImportSettings()` (useQuery GET `/users/me/import-settings`, queryKey `['import-settings']`) and `useUpdateImportSettings()` (useMutation PATCH `/users/me/import-settings`). Wire the mutation's `onMutate` to optimistically write the new settings into the `['import-settings']` cache and `onError` to roll back to the snapshot (D-09 "no dirty state" + UI-SPEC error-state optimistic-rollback). Manual apiClient calls are covered by the global TanStack Query error handler per CLAUDE.md — do NOT add a duplicate Sentry.captureException. Define the `ImportSettings` TS type (tc_* booleans, game_cap 1000|3000|5000, backlog_counts Record<string, Record<string, number>>).

    Create `ImportFilterCard.tsx` using the project `Card`/`CardHeader size="compact"` (title "Import filters")/`CardBody`. TC multiselect: copy FilterPanel's raw-`<button>` `grid grid-cols-4 gap-1` idiom verbatim with `h-11 sm:h-7`, active `border-toggle-active bg-toggle-active text-toggle-active-foreground` / inactive `border-border bg-inactive-bg text-muted-foreground`, `aria-pressed`, `TimeControlIcon` per button, and `data-testid="import-filter-time-control-{tc}"` (import-scoped prefix to avoid collision with the Library/Openings FilterPanel instance). Implement the last-one-standing guard verbatim (removing the last selected TC re-selects it — at least one TC always enabled). Cap row: shadcn `ToggleGroup type="single"` with items 1000/3000/5000, `data-testid="import-filter-cap-{value}"`. Under the controls: the inline helper line `text-sm text-muted-foreground` with the exact locked copy "Limits how much older history is imported — new games always sync." followed by an `InfoPopover` (icon HelpCircle, side="top") whose body is the locked three-sentence rule from the Copywriting Contract (interpolate the user's cap). Every change (TC toggle, cap select) calls `useUpdateImportSettings().mutate(...)` immediately (auto-save, D-09). The component takes no `isGuest` prop (D-16). Render identically on mobile/desktop (single inline card, no drawer duplication — UI-SPEC note 6).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` and `npm run lint` pass (knip finds no dead exports; all new exports are imported).
    - ImportFilterCard renders the "Import filters" title, a 4-button TC grid, a 3-item cap ToggleGroup, the exact inline copy string, and an InfoPopover with the three-sentence body.
    - Every interactive element has an `import-filter-*` data-testid; TC/cap buttons carry `aria-pressed`.
    - The last-one-standing guard prevents deselecting the final TC.
    - No new color literals — full/in-progress distinction uses font-weight/contrast per the Color contract.
  </acceptance_criteria>
  <done>The filter card and settings hook exist, auto-save on change, and enforce the at-least-one-TC invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ImportFilterCard + per-(platform,TC) budget chips in Import.tsx</name>
  <files>frontend/src/pages/Import.tsx</files>
  <read_first>
    - frontend/src/pages/Import.tsx (own file — outer space-y-8 column ~line 332, space-y-4 platform-row wrapper ~lines 352-353, platform rows import-platform-chess-com/import-platform-lichess ~lines 355/403, "{N} games (last sync: …)" metadata line)
    - frontend/src/components/filters/ImportFilterCard.tsx (Task 1 — the component to mount)
    - frontend/src/hooks/useImportSettings.ts (Task 1 — backlog_counts + game_cap source)
    - .planning/phases/186-import-filters-tc-and-game-cap/186-UI-SPEC.md (Component & Layout Notes 1 and 4; Color full-state rule; overflow flex-wrap backstop; error isError branch)
  </read_first>
  <action>
    Mount `<ImportFilterCard />` in Import.tsx's outer `space-y-8` column, directly ABOVE the `space-y-4` wrapper that holds the two platform rows (D-08, UI-SPEC note 1) — not inside it. Inside each platform row (`import-platform-chess-com` / `import-platform-lichess`), directly below the existing "{N} games (last sync: …)" line, render a budget-chip row: one `flex flex-wrap gap-x-3 gap-y-1 text-sm` line, dot-separated, one chip per CURRENTLY-SELECTED TC only (D-11 — deselected TCs render no chip). Each chip shows `"{Label} {count}/{cap}"` where count comes from `backlog_counts[platform][tc]` (default 0) and cap is `game_cap`; a chip whose count ≥ cap renders `text-foreground font-semibold` (full/complete), otherwise `text-muted-foreground` (D-11/D-12 — over-cap uses the same full treatment, never destructive/red). `data-testid="import-budget-chip-{platform}-{tc}"` per chip. Add the CLAUDE.md-mandated `isError` branch for the settings query ("Failed to load import settings. Something went wrong. Please try again in a moment.") — never let a settings-fetch failure silently fall back to defaults. flex-wrap ensures 5-digit over-cap counts wrap rather than truncate at narrow widths (UI-SPEC overflow backstop). Apply identically for both platform rows (CLAUDE.md mobile/duplicate-markup rule).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` and `npm run lint` pass.
    - ImportFilterCard renders above the platform cards; budget chips render inside both platform rows below the game-count line.
    - Chips appear only for selected TCs; a full/over-cap chip uses font-semibold text-foreground, an in-progress chip uses text-muted-foreground; no destructive color on any chip.
    - Both platform rows carry identical budget-chip markup; each chip has an `import-budget-chip-{platform}-{tc}` testid.
    - The settings query has an isError branch with the mandated copy.
  </acceptance_criteria>
  <done>The Import tab shows the filter card and legible per-TC budget chips for both platforms, with the full/over-cap state reading as complete.</done>
</task>

<task type="auto">
  <name>Task 3: ImportFilterCard component test</name>
  <files>frontend/src/components/filters/__tests__/ImportFilterCard.test.tsx</files>
  <read_first>
    - frontend/src/components/filters/ImportFilterCard.tsx (Task 1 — component under test)
    - frontend/src/hooks/useImportSettings.ts (Task 1 — mock target)
    - frontend/src/pages/Import.tsx or any existing frontend test under frontend/src/**/__tests__/ using render/fireEvent/waitFor + a QueryClientProvider wrapper (locate the nearest RTL + TanStack Query test idiom; PATTERNS notes no direct FilterPanel test analog)
    - MEMORY note: heavy CPU-bound frontend tests can exceed Vitest's 5s default — add a per-test timeout only if a specific test flakes, not preemptively
  </read_first>
  <action>
    Create `ImportFilterCard.test.tsx` (RTL + QueryClientProvider wrapper). Cover: (1) the TC grid and cap ToggleGroup render with the expected data-testids and aria-pressed states; (2) toggling a TC calls the update mutation (mock `useUpdateImportSettings`) with the new TC set — auto-save (D-09); (3) the last-one-standing guard: attempting to deselect the final active TC keeps it selected (no mutation that empties the set); (4) selecting a different cap calls the mutation with the new game_cap; (5) the inline helper copy string and InfoPopover trigger are present. Use `getByTestId`/`fireEvent`/`waitFor`. Do not assert on the backend (mock the hook).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/filters/__tests__/ImportFilterCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/filters/__tests__/ImportFilterCard.test.tsx` passes all cases.
    - The test asserts auto-save fires on toggle and the last-one-standing guard prevents an empty TC set.
    - `cd frontend && npm run lint` passes with the new test file present.
  </acceptance_criteria>
  <done>ImportFilterCard behavior (auto-save, at-least-one-TC invariant, cap select, copy/popover presence) is covered by an automated component test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (/users/me/import-settings) | Settings read/write ride the existing authenticated apiClient session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-186-05 | Information Disclosure | useImportSettings query/mutation | low | mitigate | The hook calls the same authenticated `apiClient` used across the app; the backend endpoint scopes all reads/writes to `current_active_user` (Plan 01 T-186-01). No user id is sent from the client. No cross-user data reachable from the UI. |
| T-186-06 | Tampering | client-side TC/cap selection | low | accept | The client can only send values the backend re-validates via Pydantic Literal (Plan 01 T-186-02); a malicious client cannot persist an invalid cap. Client-side last-one-standing guard is UX only, not a security control. |
| T-186-SC | Tampering | package installs | low | accept | No new npm packages this phase (all UI primitives already installed per UI-SPEC Registry Safety); no install task. |
</threat_model>

<verification>
- `cd frontend && npx tsc -b && npm run lint && npm test -- --run` green (tsc -b required per CLAUDE.md when touching shared types/property access).
- Manual UAT (deferred to /gsd-verify-work): at 375px width confirm the budget-chip row wraps and never truncates a count (UI-SPEC overflow backstop); confirm PATCH-failure inline error + optimistic rollback (UI-SPEC error backstop).
</verification>

<success_criteria>
- Filter card + auto-save + budget chips work on desktop and mobile (D-08/D-09/D-11/D-12/D-16).
- Locked copy and popover present (D-10); at-least-one-TC invariant enforced.
- Component test green; tsc -b + lint + frontend tests pass.
</success_criteria>

<output>
Create `.planning/phases/186-import-filters-tc-and-game-cap/186-03-SUMMARY.md` when done.
</output>
