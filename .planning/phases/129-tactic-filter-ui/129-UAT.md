---
status: complete
phase: 129-tactic-filter-ui
source: [129-VERIFICATION.md]
started: 2026-06-20T12:00:00Z
updated: 2026-06-20T15:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Flaws-tab depth + orientation controls render in the mobile drawer at 375px
expected: Open the Flaws tab on the Library page at a 375px-wide viewport. The Tactic Difficulty depth control and Either/Missed/Allowed orientation toggle render correctly inside the mobile drawer, above the Tactic Motif section. The motif section now shows the 10-family taxonomy chips. Depth slider operates in full moves (1..5); orientation toggle defaults to Either; the filter dot does NOT light at Either + Intermediate defaults.
result: pass
note: |
  Previously passed (2026-06-20). Re-test required after gap-closure plan 129-05 rewrote the
  family chips from 6 to 10 families — confirm the mobile drawer still lays out cleanly with the
  larger chip set. Re-confirmed pass 2026-06-20 (live, 10-family taxonomy).

### 2. Tactic Comparison grid two-bullet cards + More Tactics accordion at 375px
expected: Open the Tactic Comparison grid (Library stats tab) at a 375px-wide viewport. Each family card shows "Missed {Family}" above "Allowed {Family}" rows, stacked correctly. With the 10-family taxonomy, the top 6 families render in the main grid and the remaining 4 appear under a "More Tactics" accordion whose header has the charcoal-texture band matching the Endgame Statistics Concepts pattern. The grid is unaffected by changing the Flaws-tab orientation or depth controls (D-09 independence).
result: pass
note: |
  Previously FAILED as G-01 (More Tactics accordion unreachable with only 6 families). Gap-closure
  plans 129-04 (backend) + 129-05 (frontend) redesigned the taxonomy to 10 families, so top-6-by-Missed
  now leaves 4 overflow families and the accordion is reachable. Re-confirmed PASS 2026-06-20 in the
  live running app with real data — More Tactics accordion renders with the 4 overflow families. G-01 resolved.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The tactic-comparison grid surfaces a meaningful 'More Tactics' accordion (top-6-by-Missed in the main grid, the rest in the accordion, D-14)."
  status: resolved
  reason: "User reported: there is no 'More Tactics' accordion shown. Root cause traced to the family taxonomy having exactly 6 families, so top-6 always equals all-of-them and overflow is always empty. RESOLVED in code by gap-closure plans 129-04 (backend: FAMILY_TO_MOTIF_INTS rewritten to 10 families) + 129-05 (frontend: TacticFamily union + theme tokens + comparison meta mirror the 10 families). With 10 families, top-6 leaves 4 overflow families so the accordion is reachable. CONFIRMED RESOLVED 2026-06-20 — UAT test 2 passed live (via /gsd-verify-work 129); More Tactics accordion renders the 4 overflow families."
  severity: low
  test: 2
  resolved_by: [129-04, 129-05]
  artifacts: [frontend/src/components/library/TacticComparisonGrid.tsx, app/repositories/library_repository.py (FAMILY_TO_MOTIF_INTS), app/services/library_service.py (get_tactic_comparison top-6 selection), frontend/src/lib/tacticComparisonMeta.ts, frontend/src/components/library/TacticMotifChip.tsx, frontend/src/lib/theme.ts (family colors)]
  missing: []

### G-01 resolution direction: redesign the tactic-family taxonomy (DECIDED — run as gap-closure)

The user wants to replace today's 6 merged families with the **7 tier-2 motifs (each its own family) + Mate**. This produces >6 families, which makes the D-14 top-6 grid + "More Tactics" accordion reachable, resolving the surface-level UAT issue while delivering the product change the user actually wants.

**Target families (DECIDED, include trapped-piece):**
1. `fork` (FORK)
2. `skewer` (SKEWER)  ← split out of today's merged `pin_skewer`
3. `pin` (PIN)        ← split out of today's merged `pin_skewer`
4. `double_check` (DOUBLE_CHECK)        ← split out of today's merged `discovery`
5. `discovered_check` (DISCOVERED_CHECK)
6. `discovered_attack` (DISCOVERED_ATTACK)  ← split out of today's merged `discovery`
7. `trapped_piece` (TRAPPED_PIECE)  — INCLUDED per user, even though the detector currently suppresses it (≈always-empty chip until the detector improves; tracked separately)
8. `mate` (all mate patterns — unchanged; keeps the full mate-subtype int set)

**Removed family:** `combinations` (DEFLECTION, ATTRACTION, INTERMEZZO, INTERFERENCE, SELF_INTERFERENCE, CLEARANCE, CAPTURING_DEFENDER, SACRIFICE) — dropped entirely.

**RESOLVED DECISIONS (2026-06-20, at gap-closure planning):**
- **`hanging` family: KEPT** as its own family (HANGING_PIECE=2). Real, well-detected motif; keeps a meaningful grid category.
- **`x_ray` (X_RAY=12): NOW ITS OWN FAMILY.** Previously merged inside `pin_skewer`; the split orphaned it and the G-01 target list never named it. Per the "each tier-2 motif = its own family" principle, it becomes a standalone `x_ray` family.

**Final taxonomy = 10 families** (≥ 7, so the D-14 top-6 grid + "More Tactics" accordion with 4 overflow families is reachable — resolves G-01):
1. `fork` (FORK=1)
2. `skewer` (SKEWER=4) ← split out of merged `pin_skewer`
3. `pin` (PIN=3) ← split out of merged `pin_skewer`
4. `x_ray` (X_RAY=12) ← split out of merged `pin_skewer`, now standalone
5. `double_check` (DOUBLE_CHECK=5) ← split out of merged `discovery`
6. `discovered_check` (DISCOVERED_CHECK=25) ← standalone tier-2 motif (was unmapped)
7. `discovered_attack` (DISCOVERED_ATTACK=6) ← split out of merged `discovery`
8. `trapped_piece` (TRAPPED_PIECE=26) — detector currently suppresses; ≈always-empty chip until detector improves (tracked separately)
9. `hanging` (HANGING_PIECE=2)
10. `mate` (all mate subtypes: BACK_RANK_MATE=7, MATE=8, SMOTHERED_MATE=18, ANASTASIA_MATE=19, HOOK_MATE=20, ARABIAN_MATE=21, BODEN_MATE=22, DOUBLE_BISHOP_MATE=23, DOVETAIL_MATE=24) — unchanged.

**Dropped:** `combinations` (DEFLECTION=9, ATTRACTION=10, INTERMEZZO=11, INTERFERENCE=13, SELF_INTERFERENCE=14, CLEARANCE=15, CAPTURING_DEFENDER=16, SACRIFICE=17) — removed entirely. Move-type motifs (EN_PASSANT=27, PROMOTION=28, UNDER_PROMOTION=29) were never in `FAMILY_TO_MOTIF_INTS`, so no change.

**Blast radius to plan for (~30 files):**
- Backend: `FAMILY_TO_MOTIF_INTS` and the family chip config in `library_repository.py`; any per-family suppression levers; `query_utils.py` / `library_service.py` family iteration; `schemas/library.py`; tests in `tests/test_query_utils.py`, `tests/test_library_repository.py`, `tests/services/test_tactic_comparison_service.py`, `tests/services/test_flaws_service.py`, `tests/test_flaw_predicate.py`.
- Frontend: family labels + per-family **colors in `theme.ts`**, `tacticComparisonMeta.ts`, `TacticMotifChip.tsx`, the filter family chips in `FlawFilterControl.tsx`, `types/library.ts`, `useFlawFilterStore.ts`, plus the affected `__tests__`.
- Cross-phase: this taxonomy is also consumed by Phase 126 (comparison) and Phase 128 chips — verify no regression there.

**Reference:** `reports/tactic-tagger/tactic-tagger-2026-06-20.md` (tier-2 motif list, suppression status).
