---
phase: 129-tactic-filter-ui
plan: 05
type: execute
wave: 2
depends_on: ["129-04"]
gap_closure: true
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/lib/tacticComparisonMeta.ts
  - frontend/src/lib/tacticMotifDefinitions.ts
  - frontend/src/types/library.ts
  - frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx
  - frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx
autonomous: true
requirements: [TACUI-05, TACUI-06, TACUI-07, TACUI-08]
must_haves:
  truths:
    - "The frontend TacticFamily union has exactly the 10 members fork, skewer, pin, x_ray, double_check, discovered_check, discovered_attack, trapped_piece, hanging, mate — string-for-string equal to the backend FAMILY_TO_MOTIF_INTS keys from 129-04 (the cross-stack contract)"
    - "The Flaws-tab Tactic Motif family filter chips (TACUI-06/07) render the new 10 families on desktop AND in the 375px mobile drawer (CLAUDE.md mobile parity)"
    - "The Tactic Comparison grid groups by the server's 10 families and the D-14 'More Tactics' accordion now renders (6 top + 4 overflow) — closing G-01 end-to-end (TACUI-08)"
    - "TACTIC_FAMILY_FOR_MOTIF maps every surfaced motif string to its new family; discovered-check and trapped-piece motif strings map to discovered_check / trapped_piece; dropped combinations motif strings map to no family"
    - "No hard-coded color values outside theme.ts; the per-family TAC_* token names exist for all 10 families"
  artifacts:
    - path: "frontend/src/lib/tacticComparisonMeta.ts"
      provides: "10-member TacticFamily union, TACTIC_FAMILY_COLORS, TACTIC_FAMILY_ICON, TACTIC_COMPARISON_FAMILIES (display order), derived TACTIC_FAMILY_FOR_MOTIF"
      contains: "discovered_check"
    - path: "frontend/src/lib/theme.ts"
      provides: "per-family TAC_* color tokens for all 10 families (dropped tokens removed/repurposed)"
      contains: "TAC_TRAPPED_PIECE"
  key_links:
    - from: "frontend/src/components/filters/FlawFilterControl.tsx"
      to: "frontend/src/lib/tacticComparisonMeta.ts"
      via: "imports TACTIC_COMPARISON_FAMILIES + TACTIC_FAMILY_COLORS — the family-chip list flows from the meta hub (no code change needed; new families appear automatically)"
      pattern: "TACTIC_COMPARISON_FAMILIES"
    - from: "frontend/src/components/library/TacticComparisonGrid.tsx"
      to: "frontend/src/lib/tacticComparisonMeta.ts"
      via: "groups server bullets by family using the meta hub; accordion now triggers with 4 overflow families"
      pattern: "TACTIC_FAMILY"
  prohibitions:
    - "No hard-coded oklch / color literals outside theme.ts (CLAUDE.md theme-constants rule)"
    - "No text-xs in new code (text-sm floor; tooltip-body exception does not apply to family chips/labels)"
    - "No detector change; no backend change (the backend keys are fixed by 129-04 — mirror them, do not redefine)"
    - "Frontend family keys MUST equal backend FAMILY_TO_MOTIF_INTS keys string-for-string — no client-side renaming"
    - "No client-side re-sort of families in the comparison grid — render server order (the existing GridBody no-resort contract)"
    - "Do NOT rewrite the More Tactics accordion render code in TacticComparisonGrid (already correct) — only the taxonomy makes it trigger; just confirm it does"
---

<objective>
Mirror the backend 10-family taxonomy (locked by plan 129-04) on the frontend so the Flaws-tab family
filter chips (TACUI-06/07) and the Tactic Comparison grid (TACUI-05/08) reflect the new families and
the D-14 "More Tactics" accordion finally renders (4 overflow families) — closing UAT gap G-01
end-to-end.

The frontend taxonomy hub is `tacticComparisonMeta.ts` (the `TacticFamily` union + colors + icons +
`TACTIC_COMPARISON_FAMILIES` display list + the derived `TACTIC_FAMILY_FOR_MOTIF` motif→family map).
Rewriting that hub flows the new families automatically into every consumer that reads it dynamically
(`FlawFilterControl`, `TacticComparisonGrid`, `TacticMotifChip`, `TagChip`, `EvalChart`,
`useFlawFilterStore`, `useLibrary`, `FlawsTab`). The work is: rewrite the hub, add the matching
`theme.ts` per-family color tokens, add the two missing motif-string definitions
(`discovered-check`, `trapped-piece`), update the hard-coded family-key test fixtures, and run
`tsc -b` (esbuild does NOT type-check — a shared-type change must be compiled).

Depends on 129-04: the backend family-key strings are the contract. This plan mirrors them
string-for-string; the keys matching is an explicit acceptance criterion.

Purpose: complete the cross-stack taxonomy redesign and make the More Tactics accordion exercisable in
the running app.
Output: a 10-family frontend taxonomy hub, matching theme tokens, updated tests, type-checked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-tactic-filter-ui/129-UAT.md
@.planning/phases/129-tactic-filter-ui/129-03-PLAN.md
@.planning/phases/129-tactic-filter-ui/129-04-PLAN.md
@.planning/phases/129-tactic-filter-ui/129-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New/changed frontend symbols (must mirror 129-04's backend keys string-for-string):

- `TacticFamily` union (tacticComparisonMeta.ts) — exactly 10 members in display order:
  `'fork' | 'skewer' | 'pin' | 'x_ray' | 'double_check' | 'discovered_check' | 'discovered_attack' | 'trapped_piece' | 'hanging' | 'mate'`.
  Removed: `'pin_skewer'`, `'discovery'`, `'combinations'`.
- `TACTIC_FAMILY_COLORS`, `TACTIC_FAMILY_ICON`, `TACTIC_COMPARISON_FAMILIES` (with `name`/`definition`/`motifs` per family), derived `TACTIC_FAMILY_FOR_MOTIF` — all rekeyed to the 10 families.
- `theme.ts` per-family color tokens: keep `TAC_FORK`/`TAC_FORK_BG`, `TAC_MATE`/`_BG`, `TAC_HANGING`/`_BG`; ADD `TAC_SKEWER`, `TAC_PIN`, `TAC_X_RAY`, `TAC_DOUBLE_CHECK`, `TAC_DISCOVERED_CHECK`, `TAC_DISCOVERED_ATTACK`, `TAC_TRAPPED_PIECE` (+ `_BG` each); REMOVE/repurpose `TAC_PIN_SKEWER`, `TAC_DISCOVERY`, `TAC_COMBINATIONS` (+ `_BG`). All alias the existing shared `TAC_BLUE`/`TAC_BLUE_BG` (the single-blue tactic-group convention is intentional, Phase 126 UAT).
- `tacticMotifDefinitions.ts` — add one-line definitions for `'discovered-check'` and `'trapped-piece'` (newly surfaced as standalone families).

Motif-string → family mapping for `TACTIC_COMPARISON_FAMILIES.motifs` (drives TACTIC_FAMILY_FOR_MOTIF):
fork→['fork'], skewer→['skewer'], pin→['pin'], x_ray→['x-ray'], double_check→['double-check'],
discovered_check→['discovered-check'], discovered_attack→['discovered-attack'],
trapped_piece→['trapped-piece'], hanging→['hanging-piece'],
mate→['back-rank-mate','smothered-mate','anastasia-mate','hook-mate','arabian-mate','boden-mate','double-bishop-mate','dovetail-mate','mate'].
(Dropped combinations motif strings — sacrifice/deflection/attraction/intermezzo/interference/self-interference/clearance/capturing-defender — belong to NO family now.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add per-family theme tokens + the two missing motif-string definitions</name>
  <read_first>
    - frontend/src/lib/theme.ts:84-105 (TAC_* tactic family tokens — all alias TAC_BLUE/TAC_BLUE_BG; per-family-name pattern is intentional, only names differ; the single-blue convention comment at :84-90)
    - frontend/src/lib/tacticMotifDefinitions.ts:11-34 (24 motif-string definitions; discovered-check + trapped-piece are MISSING and must be added — they are now surfaced as their own families)
    - app/services/tactic_detector.py:566-580, :723-760 (definitions of detect_discovered_check + detect_trapped_piece — source for accurate one-line copy)
    - 129-04-PLAN.md <artifacts_this_phase_produces> (the 10 family keys + motif-string membership)
  </read_first>
  <files>frontend/src/lib/theme.ts, frontend/src/lib/tacticMotifDefinitions.ts</files>
  <action>
In `theme.ts`, rework the tactic-family token block (:84-105) to provide one `TAC_*` + `TAC_*_BG`
token per NEW family. Keep `TAC_FORK`/`_BG`, `TAC_MATE`/`_BG`, `TAC_HANGING`/`_BG` (names unchanged).
ADD: `TAC_SKEWER`, `TAC_PIN`, `TAC_X_RAY`, `TAC_DOUBLE_CHECK`, `TAC_DISCOVERED_CHECK`,
`TAC_DISCOVERED_ATTACK`, `TAC_TRAPPED_PIECE`, each with a matching `_BG`. REMOVE the now-unused
`TAC_PIN_SKEWER`/`_BG`, `TAC_DISCOVERY`/`_BG`, `TAC_COMBINATIONS`/`_BG` (knip will flag them as dead
exports once the meta hub stops importing them — removing them keeps knip green). Every new token
aliases the existing shared `TAC_BLUE` / `TAC_BLUE_BG` (preserve the intentional single-blue
tactic-group convention from Phase 126 UAT — do NOT introduce distinct hues; document in the block
comment that the per-family names exist only so consumers key by family while the value stays one
shared blue). No hard-coded oklch outside the existing `TAC_BLUE` constants.

In `tacticMotifDefinitions.ts`, add two entries to the definitions map: `'discovered-check'` and
`'trapped-piece'`, each a one-sentence plain-English definition (no jargon, no em-dashes) consistent
with the backend detector semantics — discovered-check: moving one piece uncovers a check from the
piece behind it; trapped-piece: a piece has no safe squares and will be lost. Keep the existing 24
entries. (These strings are now surfaced as standalone family motifs, so the chip tooltip must have
copy for them.)
  </action>
  <verify>
    <automated>cd frontend && grep -cE 'export const TAC_(SKEWER|PIN|X_RAY|DOUBLE_CHECK|DISCOVERED_CHECK|DISCOVERED_ATTACK|TRAPPED_PIECE)\b' src/lib/theme.ts</automated>
    <automated>cd frontend && grep -cE "'(discovered-check|trapped-piece)':" src/lib/tacticMotifDefinitions.ts</automated>
  </verify>
  <done>theme.ts has the 7 new per-family TAC_* tokens (+ _BG) all aliasing TAC_BLUE; dropped tokens removed; tacticMotifDefinitions.ts defines discovered-check + trapped-piece. (First grep ≥7, second == 2.)</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the tacticComparisonMeta.ts taxonomy hub to the 10 families + fix the family-string comment in types/library.ts</name>
  <read_first>
    - frontend/src/lib/tacticComparisonMeta.ts (full — TacticFamily union :48-54, TACTIC_FAMILY_COLORS :63-70, TACTIC_FAMILY_ICON :72-79, TACTIC_COMPARISON_FAMILIES :98-156, derived TACTIC_FAMILY_FOR_MOTIF :162-166; theme imports :15-31)
    - frontend/src/lib/theme.ts (the new TAC_* tokens from Task 1 — import the new names)
    - frontend/src/types/library.ts:307-319 (TacticBullet.family is plain string; stale `// family key e.g. "fork", "pin_skewer"` comment at :308 to repoint)
    - frontend/src/components/library/TacticMotifChip.tsx:64-68 (reads TACTIC_FAMILY_FOR_MOTIF / TACTIC_FAMILY_COLORS / TACTIC_FAMILY_ICON from the hub — confirm it needs no change)
    - frontend/src/components/filters/FlawFilterControl.tsx:34-35 (imports TACTIC_COMPARISON_FAMILIES + TACTIC_FAMILY_COLORS — the chip list flows from the hub; confirm desktop + mobile-drawer both map over TACTIC_COMPARISON_FAMILIES, no hard-coded 6)
    - 129-04-PLAN.md <artifacts_this_phase_produces> (the authoritative key strings + motif membership — mirror EXACTLY)
    - lucide-react icon names (pick a distinct, semantically-sensible icon per new family; reuse existing imports where they fit)
  </read_first>
  <files>frontend/src/lib/tacticComparisonMeta.ts, frontend/src/types/library.ts</files>
  <action>
Rewrite `tacticComparisonMeta.ts` to the 10-family taxonomy, mirroring the 129-04 backend keys
string-for-string (the keys are the cross-stack contract — copy them verbatim from 129-04's artifacts
block):

1. `TacticFamily` union → exactly the 10 members in display order: `'fork' | 'skewer' | 'pin' |
   'x_ray' | 'double_check' | 'discovered_check' | 'discovered_attack' | 'trapped_piece' | 'hanging' |
   'mate'`. Remove `pin_skewer`/`discovery`/`combinations`.
2. Update the theme imports (:15-31) to the new `TAC_*` token names from Task 1; drop the removed ones.
3. `TACTIC_FAMILY_COLORS` → one entry per new family, each `{ color: TAC_X, bg: TAC_X_BG }`.
4. `TACTIC_FAMILY_ICON` → one icon per new family. Keep GitFork(fork), Crown(mate),
   AlertTriangle(hanging). Assign distinct lucide icons to the split families (e.g. a line/skewer
   glyph for skewer, pin for pin, an x-ray/scan glyph for x_ray, a double-arrow/check for
   double_check and discovered_check, a reveal/eye for discovered_attack, a net/lock for
   trapped_piece). Import any new icons from lucide-react; remove now-unused icon imports so knip stays
   green.
5. `TACTIC_COMPARISON_FAMILIES` → 10 `TacticFamilyDef` entries in display order, each with `name`
   (human label, e.g. "Skewer", "Pin", "X-ray", "Double check", "Discovered check", "Discovered
   attack", "Trapped piece"), `family` (the key), `definition` (one line, no jargon/em-dash), and
   `motifs` (the motif-string list from 129-04's mapping: skewer→['skewer'], pin→['pin'],
   x_ray→['x-ray'], double_check→['double-check'], discovered_check→['discovered-check'],
   discovered_attack→['discovered-attack'], trapped_piece→['trapped-piece'], mate→the 9 mate strings,
   fork/hanging unchanged). Do NOT include any dropped combinations motif strings.
6. Leave the derived `TACTIC_FAMILY_FOR_MOTIF` builder (:162-166) as-is — it auto-derives from the
   rewritten `TACTIC_COMPARISON_FAMILIES`; with combinations gone, those motif strings now map to no
   family (consumers already guard `family == null`).
7. Update the module header comment (count "6"→"10", drop the "collapse the 24 motifs" framing,
   note the cross-stack key contract with backend FAMILY_TO_MOTIF_INTS).

In `types/library.ts`, repoint the stale `family: string  // family key e.g. "fork", "pin_skewer"`
comment (:308) to a surviving key (e.g. "fork", "skewer"). `TacticBullet.family` stays `string` (no
union narrowing needed).

Confirm — do not edit — that `TacticMotifChip.tsx`, `FlawFilterControl.tsx`, `TagChip.tsx`,
`EvalChart.tsx`, `useFlawFilterStore.ts`, `useLibrary.ts`, and `FlawsTab.tsx` consume the hub
dynamically (union type + `TACTIC_COMPARISON_FAMILIES` map + `TACTIC_FAMILY_FOR_MOTIF`) and need no
logic change — the new families flow through automatically (this is why the family chips appear on
both desktop and the mobile drawer, TACUI-06/07 parity). Note in the SUMMARY that old bookmarked
`?tactic=pin_skewer` URLs become inert (backend `.get(fam,[])` no-op + frontend union no longer
includes them) — acceptable, no migration.
  </action>
  <verify>
    <automated>cd frontend && grep -cE "'(skewer|pin|x_ray|double_check|discovered_check|discovered_attack|trapped_piece)'" src/lib/tacticComparisonMeta.ts</automated>
    <automated>cd frontend && grep -c "pin_skewer\|'discovery'\|combinations" src/lib/tacticComparisonMeta.ts</automated>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>TacticFamily union + COLORS + ICON + TACTIC_COMPARISON_FAMILIES rekeyed to the 10 families matching the backend keys string-for-string; no pin_skewer/discovery/combinations refs remain (second grep == 0); types/library.ts comment repointed; tsc -b zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update hard-coded family-key test fixtures + frontend gate</name>
  <read_first>
    - frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx:33-66 (the test iterates `['fork','pin_skewer','discovery','mate','hanging','combinations']` and asserts "all six family buttons" — must become the 10 new families; "six"→"ten" copy)
    - frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx (mocked comparison response uses family-key fixtures; the >6-family / More Tactics accordion assertions from 129-03 — confirm they now use the new keys and a 10-family mock so the overflow assertion is real, not synthetic)
    - 129-04-PLAN.md <artifacts_this_phase_produces> (the 10 keys)
    - CLAUDE.md Frontend "Pre-merge gate" block + project memory frontend_run_tsc_build
  </read_first>
  <files>frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx, frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx</files>
  <action>
In `FlawFilterControl.test.tsx`, replace the hard-coded 6-family list with the new 10 family keys
(`fork`, `skewer`, `pin`, `x_ray`, `double_check`, `discovered_check`, `discovered_attack`,
`trapped_piece`, `hanging`, `mate`) and update the "renders all six family buttons" copy/count to ten.
Keep the add/remove toggle tests (repoint any old key they reference to a surviving key).

In `TacticComparisonGrid.test.tsx`, ensure the dual-orientation mock response carries all 10 families
(so the top-6 main grid + 4-overflow accordion assertion from 129-03 is exercised against the real
taxonomy, not a synthetic >6 mock). Confirm/strengthen the existing assertions: (a) the first 6
families render in the main grid, (b) the remaining 4 render inside `tactic-grid-more-tactics`, (c)
the More Tactics accordion is PRESENT (the G-01 regression — with 10 families it must now appear).
Repoint any old family-key fixtures to the new keys. Do NOT rewrite the grid component's accordion
render code — only its test data + assertions change.

Then run the full frontend pre-merge gate: lint (incl. knip — the removed theme tokens / icon imports
must leave no dead exports), full vitest, and `tsc -b`. Resolve any drift (ESLint only — never run
prettier).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/filters/__tests__/FlawFilterControl.test.tsx src/components/library/__tests__/TacticComparisonGrid.test.tsx</automated>
    <automated>cd frontend && npm run lint && npm test -- --run && npx tsc -b</automated>
  </verify>
  <done>FlawFilterControl test asserts the 10 new family buttons; TacticComparisonGrid test exercises a 10-family mock and asserts the More Tactics accordion now renders (6 main + 4 overflow); frontend lint (incl. knip) clean, full vitest green, tsc -b zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comparison API → grid render | 10-family dual-orientation bullets (player-scoped, beta-gated) grouped + rendered client-side |
| URL `?tactic=` param → filter store | user-supplied family key cast to TacticFamily; unknown keys are inert (backend no-op + union excludes them) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-12 | Tampering | stale `?tactic=pin_skewer` URL param | accept | Old family keys are no longer in the TacticFamily union and the backend expands unknown keys to an empty int list (129-04 `.get(fam,[])` no-op). A stale bookmarked filter is simply ignored — no error, no unscoped query. |
| T-129-13 | Information disclosure | re-grouped 10-family grid render | accept | Same player-scoped, beta-gated (126 D-01) aggregate rows; only the family bucketing + label set changes. Dropped combinations motifs become invisible (less exposed). No internal hashes. |
| T-129-SC | Tampering | npm installs | mitigate | No new dependencies — all lucide icons + accordion already installed (126). No package install in this plan. |
</threat_model>

<verification>
- `npx tsc -b` zero errors (the shared-type change is compiled — esbuild does not type-check).
- `npm run lint` clean incl. knip (no dead theme tokens / icon imports after the rewrite).
- `npm test -- --run` green incl. the updated FlawFilterControl + TacticComparisonGrid tests.
- TacticFamily union members === backend FAMILY_TO_MOTIF_INTS keys (string-for-string).
- Manual/UAT backstop (G-01 close): in the running app with beta on, the Flaws-tab family filter shows 10 chips (desktop + 375px drawer) and the Tactic Comparison grid shows the "More Tactics" accordion with 4 overflow families.
</verification>

<success_criteria>
- TacticFamily union + colors + icons + TACTIC_COMPARISON_FAMILIES rekeyed to the 10 families, matching backend keys string-for-string.
- theme.ts has per-family TAC_* tokens for all 10 families (dropped tokens removed); no hard-coded colors outside theme.ts.
- discovered-check + trapped-piece motif-string definitions added.
- Family filter chips render the 10 families on desktop AND the mobile drawer (TACUI-06/07).
- The More Tactics accordion renders (G-01 closed end-to-end); grid grouping unchanged otherwise.
- Frontend gate green (lint incl. knip, vitest, tsc -b).
</success_criteria>

<output>
Create `.planning/phases/129-tactic-filter-ui/129-05-SUMMARY.md` when done. Confirm the frontend
TacticFamily union equals the backend keys string-for-string, that the More Tactics accordion now
renders with 4 overflow families (G-01 closed), and note the inert-stale-URL effect.
</output>
