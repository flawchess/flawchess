---
phase: 09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/components/results/GameCardList.tsx
  - frontend/src/components/results/GameCard.tsx
  - frontend/src/components/results/GameTable.tsx
  - frontend/src/pages/Dashboard.tsx
autonomous: true
requirements:
  - GAMES-01
  - GAMES-05

must_haves:
  truths:
    - "Games are displayed as full-width cards with colored left border (green=win, gray=draw, red=loss)"
    - "Each card shows result badge, opponent, color indicator, platform icon, user rating vs opponent rating, opening name + ECO, time control, date, move count, and platform link"
    - "Card first line shows result + opponent prominently; second line shows metadata in muted text"
    - "Pagination uses truncated page numbers with ellipsis (e.g., 1 2 3 ... 8 9 10 ... 48 49 50)"
    - "Page size is 20 (not 50)"
    - "Page change scrolls to top of the results area"
    - "X of Y games matched counter remains at the top"
  artifacts:
    - path: "frontend/src/components/results/GameCard.tsx"
      provides: "Single game card component with left border accent"
      min_lines: 40
    - path: "frontend/src/components/results/GameCardList.tsx"
      provides: "Card list with truncated pagination"
      min_lines: 60
  key_links:
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/results/GameCardList.tsx"
      via: "import and render GameCardList instead of GameTable"
      pattern: "GameCardList"
    - from: "frontend/src/components/results/GameCardList.tsx"
      to: "frontend/src/types/api.ts"
      via: "GameRecord type with new fields"
      pattern: "GameRecord"
---

<objective>
Replace the games table with rich game cards and truncated pagination.

Purpose: Transform the flat HTML table into scannable full-width game cards showing richer metadata per game, with truncated pagination that scales to hundreds of pages.

Output: GameCard and GameCardList components replacing GameTable, PAGE_SIZE changed to 20, truncated pagination with ellipsis.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-CONTEXT.md
@.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-RESEARCH.md
@.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-01-SUMMARY.md

<interfaces>
<!-- GameRecord type after Plan 01 expansion -->
From frontend/src/types/api.ts (after Plan 01 updates):
```typescript
export interface GameRecord {
  game_id: number;
  opponent_username: string | null;
  user_result: UserResult;
  played_at: string | null;
  time_control_bucket: string | null;
  platform: string;
  platform_url: string | null;
  // NEW from Plan 01:
  user_rating: number | null;
  opponent_rating: number | null;
  opening_name: string | null;
  opening_eco: string | null;
  user_color: string;
  move_count: number | null;
}
```

From frontend/src/components/results/GameTable.tsx (existing props interface to match):
```typescript
interface GameTableProps {
  games: GameRecord[];
  matchedCount: number;
  totalGames: number;
  offset: number;
  limit: number;
  onPageChange: (offset: number) => void;
}
```

From frontend/src/pages/Dashboard.tsx (consumer -- swap GameTable for GameCardList):
```typescript
// Line 38: const PAGE_SIZE = 50;  // Change to 20
// Line 429-436: <GameTable ... />  // Replace with <GameCardList ... />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand GameRecord type and create GameCard + GameCardList components</name>
  <files>frontend/src/types/api.ts, frontend/src/components/results/GameCard.tsx, frontend/src/components/results/GameCardList.tsx</files>
  <action>
1. Update GameRecord in frontend/src/types/api.ts -- add 6 new fields to match the expanded backend schema:
   - `user_rating: number | null`
   - `opponent_rating: number | null`
   - `opening_name: string | null`
   - `opening_eco: string | null`
   - `user_color: string`
   - `move_count: number | null`

2. Create frontend/src/components/results/GameCard.tsx:
   - A single game card component. Use a plain `div` (NOT shadcn Card -- incompatible with left-border accent design per research).
   - Left border accent using `border-l-4`: `border-l-green-600` for win, `border-l-gray-500` for draw, `border-l-red-600` for loss.
   - Card background: `bg-card border border-border rounded` with padding.
   - **Line 1** (prominent): Result badge (W/D/L with colored background, reuse RESULT_CLASSES pattern from GameTable.tsx), opponent username (bold), color indicator (use a small circle or chess piece unicode: white circle for white, black circle for black), platform link icon (ExternalLink from lucide-react).
   - **Line 2** (muted/subdued text-sm text-muted-foreground): user rating vs opponent rating (e.g., "1523 vs 1487" or "-- vs --" if null), opening name + ECO code (e.g., "B20 Sicilian Defense" or just ECO if no name, or "—" if both null), time control bucket (capitalized), formatted date, move count (e.g., "32 moves" or "—").
   - Platform icon: use text badge "chess.com" or "lichess" in muted text, or a small distinguishing indicator. Keep it simple.
   - Handle null fields gracefully: show "—" for missing ratings, opening, move count.
   - Add `data-testid={`game-card-${game.game_id}`}` on the card container.
   - Add `data-testid` on the platform link.
   - Use semantic HTML: `<a>` for external link.

3. Create frontend/src/components/results/GameCardList.tsx:
   - Props interface matching GameTable: `{ games, matchedCount, totalGames, offset, limit, onPageChange }`
   - Renders "X of Y games matched" counter at top.
   - Renders a vertical stack of GameCard components with gap-2.
   - Implements truncated pagination at bottom:
     - Pure function `getPaginationItems(currentPage, totalPages)` returning `(number | 'ellipsis-start' | 'ellipsis-end')[]`.
     - When totalPages <= 7, show all page numbers.
     - Otherwise: always show page 1 and last page, show a window of 2 pages on either side of current page, fill gaps with ellipsis markers.
     - Render prev/next buttons (disabled at boundaries) with `data-testid="pagination-prev"` and `data-testid="pagination-next"`.
     - Render page number buttons (current page highlighted with `variant="default"`, others `variant="ghost"`).
     - Render ellipsis as non-clickable "..." spans.
   - On page change, scroll to top of the results area using `document.querySelector('[data-testid="game-card-list"]')?.scrollIntoView({ behavior: 'smooth', block: 'start' })`.
   - Add `data-testid="game-card-list"` on the list container.
   - Empty state: "No games to display" when games array is empty.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build</automated>
  </verify>
  <done>GameRecord type has 6 new fields, GameCard renders rich card with colored left border and two-line layout, GameCardList renders card list with truncated pagination and scroll-to-top on page change.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GameCardList into Dashboard and update PAGE_SIZE</name>
  <files>frontend/src/pages/Dashboard.tsx, frontend/src/components/results/GameTable.tsx</files>
  <action>
1. In frontend/src/pages/Dashboard.tsx:
   - Change `const PAGE_SIZE = 50` to `const PAGE_SIZE = 20`.
   - Replace the import of `GameTable` with `GameCardList`: `import { GameCardList } from '@/components/results/GameCardList';`
   - Replace the `<GameTable ... />` usage (around line 429) with `<GameCardList ... />` using the same props.
   - Remove the `GameTable` import.

2. Keep frontend/src/components/results/GameTable.tsx as dead code for now (do not delete -- may be referenced elsewhere). If it is only imported from Dashboard.tsx, then it is safe to delete. Check imports before deciding.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build && npm run lint</automated>
  </verify>
  <done>Dashboard uses GameCardList with PAGE_SIZE=20, GameTable removed or kept as dead code, frontend builds and lints cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `npm run lint` passes
3. Games page shows cards with colored left borders instead of table rows
4. Each card shows result, opponent, color, ratings, opening, TC, date, moves, platform link
5. Pagination shows truncated page numbers with ellipsis for large result sets
6. Page size is 20 games per page
</verification>

<success_criteria>
- Game cards replace the table with colored left border accent (green/gray/red)
- Cards show all required fields in two-line hierarchy
- Truncated pagination with ellipsis works correctly
- PAGE_SIZE is 20
- Frontend builds and lints cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-02-SUMMARY.md`
</output>
