---
phase: 09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/hooks/useUserProfile.ts
  - frontend/src/components/import/ImportModal.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/api/client.ts
autonomous: true
requirements:
  - GAMES-03

must_haves:
  truths:
    - "Returning user sees both platforms with stored username and per-platform Sync buttons"
    - "First-time user sees input fields per platform (similar to current modal)"
    - "After first import, username is saved and modal switches to Sync view on next open"
    - "Edit usernames link switches from sync view to input view"
    - "Per-platform Sync buttons, no Sync All button"
    - "localStorage username storage is removed from frontend"
  artifacts:
    - path: "frontend/src/hooks/useUserProfile.ts"
      provides: "useUserProfile query hook and useUpdateUserProfile mutation hook"
      min_lines: 25
    - path: "frontend/src/components/import/ImportModal.tsx"
      provides: "Redesigned two-mode import modal (sync view vs input view)"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/import/ImportModal.tsx"
      to: "frontend/src/hooks/useUserProfile.ts"
      via: "useUserProfile hook to determine sync vs input mode"
      pattern: "useUserProfile"
    - from: "frontend/src/hooks/useUserProfile.ts"
      to: "/users/me/profile"
      via: "API call to profile endpoint"
      pattern: "users/me/profile"
---

<objective>
Redesign the import modal with backend-stored usernames and two-mode UI.

Purpose: Replace localStorage username storage with backend profile, providing a streamlined sync experience for returning users (one-click per platform) while keeping the input flow for first-time users.

Output: useUserProfile hook, redesigned ImportModal with sync/input views, localStorage username references removed.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-CONTEXT.md
@.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-RESEARCH.md
@.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-01-SUMMARY.md

<interfaces>
<!-- Profile API contract from Plan 01 -->
Backend endpoints:
- GET /users/me/profile -> { chess_com_username: string | null, lichess_username: string | null }
- PUT /users/me/profile <- { chess_com_username?: string | null, lichess_username?: string | null }

From frontend/src/hooks/useImport.ts (existing import trigger hook -- reuse):
```typescript
export function useImportTrigger() {
  return useMutation<ImportStartedResponse, Error, ImportRequest>({
    mutationFn: async (data) => {
      const res = await apiClient.post<ImportStartedResponse>('/imports', data);
      return res.data;
    },
  });
}
```

From frontend/src/components/import/ImportModal.tsx (current props -- keep same interface):
```typescript
interface ImportModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onImportStarted: (jobId: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useUserProfile hook</name>
  <files>frontend/src/hooks/useUserProfile.ts, frontend/src/types/api.ts</files>
  <action>
1. Add UserProfile type to frontend/src/types/api.ts (or in the hook file if preferred):
   ```typescript
   export interface UserProfile {
     chess_com_username: string | null;
     lichess_username: string | null;
   }
   ```

2. Create frontend/src/hooks/useUserProfile.ts:
   - `useUserProfile()` -- TanStack Query hook using `useQuery`:
     - queryKey: `['userProfile']`
     - queryFn: `GET /users/me/profile` via apiClient
     - staleTime: 300_000 (5 minutes) -- so profile is cached and ready when modal opens
   - `useUpdateUserProfile()` -- TanStack Query mutation:
     - mutationFn: `PUT /users/me/profile` via apiClient
     - onSuccess: invalidate `['userProfile']` query OR directly set query data
   - Export both hooks.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build</automated>
  </verify>
  <done>useUserProfile and useUpdateUserProfile hooks exist, typed correctly, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ImportModal with two-mode UI and remove localStorage</name>
  <files>frontend/src/components/import/ImportModal.tsx, frontend/src/pages/Dashboard.tsx</files>
  <action>
1. Rewrite frontend/src/components/import/ImportModal.tsx:
   - Keep the same ImportModalProps interface (open, onOpenChange, onImportStarted).
   - Use `useUserProfile()` to fetch stored usernames.
   - State: `const [editMode, setEditMode] = useState(false)` -- tracks whether user clicked "Edit usernames".
   - State: `const [chessComUsername, setChessComUsername] = useState('')` and `const [lichessUsername, setLichessUsername] = useState('')` for input fields.
   - When profile loads, initialize input states from profile data.
   - Determine view mode: `const isFirstTime = !profile?.chess_com_username && !profile?.lichess_username; const showInputView = isFirstTime || editMode;`

   **Sync View** (returning user, showInputView=false):
   - Show both platforms simultaneously (no platform toggle needed).
   - For each platform (chess.com, lichess):
     - Show platform name + stored username in muted text
     - Show a [Sync] button (per-platform) that triggers import for that platform with the stored username
     - If username is null for that platform, show "Not set" in muted text and no Sync button
   - Below both platforms: "Edit usernames" link/button (`data-testid="btn-edit-usernames"`) that sets `editMode = true`.
   - Each Sync button has `data-testid="btn-sync-chess-com"` / `data-testid="btn-sync-lichess"`.

   **Input View** (first-time user or edit mode):
   - Show two input fields: chess.com username and lichess username (both optional).
   - Pre-fill from profile data if available.
   - "Import" button that:
     a. For each non-empty username, trigger import via useImportTrigger
     b. Close modal and call onImportStarted for each triggered import
   - If editMode, also show a "Cancel" button that returns to sync view (`setEditMode(false)`).
   - Both inputs have data-testid: `data-testid="import-username-chess-com"` and `data-testid="import-username-lichess"`.

   - Remove ALL localStorage references (getStoredUsername, setStoredUsername, localStorage.getItem/setItem calls).
   - The backend auto-saves usernames on import completion (handled by Plan 01), so no frontend save needed.
   - Import still runs in background with progress toast (unchanged behavior).

2. In frontend/src/pages/Dashboard.tsx:
   - Add `useUserProfile()` call at the top of DashboardPage (eagerly fetch profile so it's cached before modal opens, per research pitfall 5).
   - The call is just for prefetching -- no need to use the return value here.
   - Invalidate userProfile query when import completes: in `handleJobDone`, add `queryClient.invalidateQueries({ queryKey: ['userProfile'] })` so that after import the profile reflects the newly saved username. Import `useQueryClient` from tanstack.

3. Add all required data-testid attributes per CLAUDE.md Browser Automation Rules.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build && npm run lint</automated>
  </verify>
  <done>ImportModal shows sync view for returning users (per-platform Sync buttons), input view for first-time users, localStorage references removed, profile query prefetched in Dashboard, all data-testid attributes present.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run lint` passes
3. First-time user sees input fields for both platforms
4. After import, reopening modal shows sync view with stored usernames and Sync buttons
5. "Edit usernames" link switches to input view
6. No localStorage references remain in import-related code
7. Profile is prefetched on Dashboard load (no loading flash in modal)
</verification>

<success_criteria>
- Import modal has two modes: sync view (returning user) and input view (first-time/edit)
- Per-platform Sync buttons for returning users
- localStorage username storage completely removed
- Profile query prefetched on Dashboard load
- Frontend builds and lints cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-rework-the-games-list-with-game-cards-username-import-and-improved-pagination/09-03-SUMMARY.md`
</output>
