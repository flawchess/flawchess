---
phase: 32-endgame-performance-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/endgames.py
  - app/repositories/endgame_repository.py
  - app/services/endgame_service.py
  - app/routers/endgames.py
  - tests/test_endgame_service.py
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-05, PERF-06, PERF-07]

must_haves:
  truths:
    - "GET /api/endgames/performance returns endgame WDL, non-endgame WDL, gauge values, and aggregate conversion/recovery"
    - "GET /api/endgames/timeline returns rolling-window win rate series for overall and per-type"
    - "Both endpoints respect time_control, platform, recency, rated, opponent_type filters"
    - "Division by zero is handled gracefully (zero wins, zero games)"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "EndgamePerformanceResponse, EndgameTimelineResponse, EndgameWDLSummary, EndgameTimelinePoint, EndgameOverallPoint"
      contains: "class EndgamePerformanceResponse"
    - path: "app/repositories/endgame_repository.py"
      provides: "query_endgame_performance_rows, query_endgame_timeline_rows"
      contains: "async def query_endgame_performance_rows"
    - path: "app/services/endgame_service.py"
      provides: "get_endgame_performance, get_endgame_timeline"
      contains: "async def get_endgame_performance"
    - path: "app/routers/endgames.py"
      provides: "/endgames/performance and /endgames/timeline endpoints"
      contains: '@router.get("/endgames/performance"'
    - path: "tests/test_endgame_service.py"
      provides: "Tests for performance and timeline service functions"
      contains: "TestGetEndgamePerformance"
  key_links:
    - from: "app/routers/endgames.py"
      to: "app/services/endgame_service.py"
      via: "get_endgame_performance, get_endgame_timeline"
      pattern: "endgame_service\\.get_endgame_(performance|timeline)"
    - from: "app/services/endgame_service.py"
      to: "app/repositories/endgame_repository.py"
      via: "query functions"
      pattern: "query_endgame_(performance|timeline)_rows"
---

<objective>
Add two new backend endpoints for endgame performance analytics: `/api/endgames/performance` (WDL comparison + gauge data) and `/api/endgames/timeline` (rolling-window win rate time series).

Purpose: Provide the data layer for the new Endgame Performance Charts frontend (plans 02-03). Both endpoints follow the existing repository -> service -> router pattern and reuse `_apply_game_filters`, `derive_user_result`, and the application-level rolling window pattern from `analysis_service.py`.

Output: Two new GET endpoints with Pydantic response schemas, repository queries, service orchestration, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-endgame-performance-charts/32-CONTEXT.md
@.planning/phases/32-endgame-performance-charts/32-RESEARCH.md

@app/schemas/endgames.py
@app/repositories/endgame_repository.py
@app/services/endgame_service.py
@app/routers/endgames.py
@app/services/analysis_service.py
@tests/test_endgame_service.py

<interfaces>
<!-- Existing interfaces the executor must use -->

From app/repositories/endgame_repository.py:
```python
ENDGAME_PIECE_COUNT_THRESHOLD = 6
ENDGAME_PLY_THRESHOLD = 6

def _apply_game_filters(stmt, time_control, platform, rated, opponent_type, recency_cutoff):
    """Applies WHERE clauses for time_control, platform, rated, opponent_type, recency."""
```

From app/services/endgame_service.py:
```python
class EndgameClassInt(IntEnum):
    ROOK = 1; MINOR_PIECE = 2; PAWN = 3; QUEEN = 4; MIXED = 5; PAWNLESS = 6

_INT_TO_CLASS: dict[int, EndgameClass]  # maps int -> string
_CLASS_TO_INT: dict[EndgameClass, int]  # maps string -> int
```

From app/services/analysis_service.py:
```python
def derive_user_result(result: str, user_color: str) -> Literal["win", "draw", "loss"]:
def recency_cutoff(recency: str | None) -> datetime | None:
```

From app/schemas/endgames.py:
```python
EndgameClass = Literal["rook", "minor_piece", "pawn", "queen", "mixed", "pawnless"]
class ConversionRecoveryStats(BaseModel): ...
class EndgameCategoryStats(BaseModel): ...
class EndgameStatsResponse(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas + repository queries for performance and timeline endpoints</name>
  <files>app/schemas/endgames.py, app/repositories/endgame_repository.py</files>
  <read_first>
    - app/schemas/endgames.py (existing schemas to extend)
    - app/repositories/endgame_repository.py (existing queries, _apply_game_filters, ENDGAME_PLY_THRESHOLD)
    - app/services/analysis_service.py (derive_user_result, recency_cutoff signatures)
    - app/models/game.py (Game model columns: played_at, result, user_color, id)
    - app/models/game_position.py (GamePosition model: game_id, user_id, endgame_class, ply)
  </read_first>
  <behavior>
    - EndgameWDLSummary has fields: wins, draws, losses, total, win_pct, draw_pct, loss_pct (all float percentages 0-100)
    - EndgamePerformanceResponse has fields: endgame_wdl, non_endgame_wdl, overall_win_rate, endgame_win_rate, aggregate_conversion_pct, aggregate_recovery_pct, relative_strength, endgame_skill
    - EndgameTimelinePoint has fields: date (str), win_rate (float), game_count (int), window_size (int)
    - EndgameOverallPoint has fields: date (str), endgame_win_rate (float|None), non_endgame_win_rate (float|None), endgame_game_count (int), non_endgame_game_count (int), window_size (int)
    - EndgameTimelineResponse has fields: overall (list[EndgameOverallPoint]), per_type (dict[EndgameClass, list[EndgameTimelinePoint]]), window (int)
    - query_endgame_performance_rows returns two lists: endgame game rows and non-endgame game rows
    - query_endgame_timeline_rows returns endgame rows, non-endgame rows, and per-class rows dict
  </behavior>
  <action>
    **In app/schemas/endgames.py**, add these Pydantic v2 models (append after existing classes):

    1. `EndgameWDLSummary(BaseModel)` with fields: `wins: int`, `draws: int`, `losses: int`, `total: int`, `win_pct: float`, `draw_pct: float`, `loss_pct: float`

    2. `EndgamePerformanceResponse(BaseModel)` with fields:
       - `endgame_wdl: EndgameWDLSummary` (games reaching any endgame class >= ENDGAME_PLY_THRESHOLD)
       - `non_endgame_wdl: EndgameWDLSummary` (games NOT reaching any endgame class)
       - `overall_win_rate: float` (wins / total across ALL games, 0-100)
       - `endgame_win_rate: float` (wins / total for endgame games only, 0-100)
       - `aggregate_conversion_pct: float` (sum of conversion_wins / sum of conversion_games * 100, per D-07)
       - `aggregate_recovery_pct: float` (sum of recovery_saves / sum of recovery_games * 100, per D-07)
       - `relative_strength: float` (endgame_win_rate / overall_win_rate * 100, can exceed 100, per D-05)
       - `endgame_skill: float` (0.6 * conversion_pct + 0.4 * recovery_pct, 0-100, per D-06)

    3. `EndgameTimelinePoint(BaseModel)` with fields: `date: str`, `win_rate: float`, `game_count: int`, `window_size: int`

    4. `EndgameOverallPoint(BaseModel)` with fields: `date: str`, `endgame_win_rate: float | None`, `non_endgame_win_rate: float | None`, `endgame_game_count: int`, `non_endgame_game_count: int`, `window_size: int`

    5. `EndgameTimelineResponse(BaseModel)` with fields: `overall: list[EndgameOverallPoint]`, `per_type: dict[str, list[EndgameTimelinePoint]]`, `window: int`
       (Use `dict[str, ...]` not `dict[EndgameClass, ...]` because Pydantic dict keys must be str-serializable)

    **In app/repositories/endgame_repository.py**, add two new async functions:

    6. `async def query_endgame_performance_rows(session, user_id, time_control, platform, rated, opponent_type, recency_cutoff)`:
       - Build `endgame_game_ids_subq`: `select(GamePosition.game_id).where(user_id, endgame_class.isnot(None)).group_by(game_id).having(func.count(GamePosition.ply) >= ENDGAME_PLY_THRESHOLD).subquery()`
       - Query endgame games: `select(Game.played_at, Game.result, Game.user_color).where(user_id, Game.id.in_(select(endgame_game_ids_subq.c.game_id)), played_at.isnot(None))` + `_apply_game_filters` + `order_by(played_at.asc())`
       - Query non-endgame games: same but `Game.id.notin_(...)` instead of `.in_(...)`
       - Return tuple: `(endgame_rows, non_endgame_rows)` as lists of tuples `(played_at, result, user_color)`

    7. `async def query_endgame_timeline_rows(session, user_id, time_control, platform, rated, opponent_type, recency_cutoff)`:
       - Reuse the same `endgame_game_ids_subq` from above for overall endgame/non-endgame split
       - For per-type: for each `EndgameClassInt` value (1-6), build a per-class subquery `select(GamePosition.game_id).where(user_id, endgame_class == class_int).group_by(game_id).having(count >= ENDGAME_PLY_THRESHOLD)`
       - Use `asyncio.gather()` to run all 8 queries (2 overall + 6 per-type) concurrently
       - Return: `(endgame_rows, non_endgame_rows, per_type_rows: dict[int, list])` where each list contains `(played_at, result, user_color)` tuples

    Import `asyncio` at the top of the repository file. Import `EndgameClassInt` and `_INT_TO_CLASS` from `endgame_service` is NOT needed in repository — just use integer literals 1-6 or import the IntEnum.
    Note: To avoid circular imports, do NOT import from endgame_service in the repository. Instead, define `ENDGAME_CLASS_INTS = range(1, 7)` locally in the repository, or just use `range(1, 7)` inline.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run python -c "from app.schemas.endgames import EndgamePerformanceResponse, EndgameTimelineResponse, EndgameWDLSummary, EndgameTimelinePoint, EndgameOverallPoint; print('Schemas OK')" && uv run python -c "from app.repositories.endgame_repository import query_endgame_performance_rows, query_endgame_timeline_rows; print('Repo OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/schemas/endgames.py contains `class EndgameWDLSummary(BaseModel):`
    - app/schemas/endgames.py contains `class EndgamePerformanceResponse(BaseModel):`
    - app/schemas/endgames.py contains `class EndgameTimelineResponse(BaseModel):`
    - app/schemas/endgames.py contains `class EndgameTimelinePoint(BaseModel):`
    - app/schemas/endgames.py contains `class EndgameOverallPoint(BaseModel):`
    - app/repositories/endgame_repository.py contains `async def query_endgame_performance_rows(`
    - app/repositories/endgame_repository.py contains `async def query_endgame_timeline_rows(`
    - app/repositories/endgame_repository.py contains `ENDGAME_PLY_THRESHOLD` in the subquery HAVING clause
    - app/repositories/endgame_repository.py contains `asyncio.gather` for concurrent per-type queries
    - app/repositories/endgame_repository.py contains `_apply_game_filters` calls on both endgame and non-endgame statements
  </acceptance_criteria>
  <done>All five new Pydantic schemas importable. Both repository functions importable with correct signatures. No circular imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service functions, router endpoints, and tests</name>
  <files>app/services/endgame_service.py, app/routers/endgames.py, tests/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py (existing service functions, _aggregate_endgame_stats, derive_user_result import)
    - app/services/analysis_service.py (derive_user_result implementation, recency_cutoff, rolling window pattern around lines 220-260)
    - app/routers/endgames.py (existing endpoint pattern with query params)
    - tests/test_endgame_service.py (existing test structure, fixtures, conftest patterns)
    - app/schemas/endgames.py (newly added schemas from Task 1)
    - app/repositories/endgame_repository.py (newly added query functions from Task 1)
  </read_first>
  <behavior>
    - get_endgame_performance with 3 endgame wins, 1 draw, 1 loss + 2 non-endgame wins, 3 draws, 5 losses returns correct WDL percentages and gauge values
    - get_endgame_performance with zero games returns all-zero response (no ZeroDivisionError)
    - relative_strength = endgame_win_rate / overall_win_rate * 100; returns 0.0 when overall_win_rate is 0
    - endgame_skill = 0.6 * aggregate_conversion_pct + 0.4 * aggregate_recovery_pct; returns 0.0 when no conversion/recovery games
    - aggregate_conversion_pct uses sum of raw numerators/denominators (not mean of per-type percentages)
    - get_endgame_timeline computes rolling window correctly: with window=3 and 5 games [W,L,W,W,L], last point win_rate = 2/3
    - get_endgame_timeline with fewer games than window still works (uses available games per D-12)
    - Both endpoints return 200 with correct response shapes
    - Both endpoints accept time_control, platform, recency, rated, opponent_type query params per D-16
  </behavior>
  <action>
    **In app/services/endgame_service.py**, add two new async functions:

    1. `async def get_endgame_performance(session, user_id, time_control, platform, recency, rated, opponent_type)`:
       - Call `recency_cutoff(recency)` for the cutoff datetime
       - Call `query_endgame_performance_rows(session, user_id, ...)` to get `(endgame_rows, non_endgame_rows)`
       - Also call the existing `get_endgame_stats(session, user_id, ...)` to get aggregate conversion/recovery data
       - For each row set (endgame, non-endgame): count wins/draws/losses using `derive_user_result(result, user_color)`, compute percentages
       - Build `EndgameWDLSummary` for each
       - Compute gauge values:
         - `overall_win_rate = total_wins / total_games * 100 if total_games > 0 else 0.0` (where total = endgame + non-endgame)
         - `endgame_win_rate = endgame_wins / endgame_total * 100 if endgame_total > 0 else 0.0`
         - `aggregate_conversion_pct = sum(c.conversion.conversion_wins for c in stats.categories) / sum(c.conversion.conversion_games for c in stats.categories) * 100` (guard div by zero)
         - `aggregate_recovery_pct = sum(c.conversion.recovery_saves for c in stats.categories) / sum(c.conversion.recovery_games for c in stats.categories) * 100` (guard div by zero)
         - `relative_strength = endgame_win_rate / overall_win_rate * 100 if overall_win_rate > 0 else 0.0` (per D-05)
         - `endgame_skill = 0.6 * aggregate_conversion_pct + 0.4 * aggregate_recovery_pct` (per D-06), or 0.0 if no conv/recovery data
       - Return `EndgamePerformanceResponse(...)`

    2. `async def get_endgame_timeline(session, user_id, time_control, platform, recency, rated, opponent_type, window=50)`:
       - Call `recency_cutoff(recency)` for the cutoff datetime
       - Call `query_endgame_timeline_rows(session, user_id, ...)` to get `(endgame_rows, non_endgame_rows, per_type_rows)`
       - Define a helper `_compute_rolling_series(rows, window)` that returns `list[dict]` with keys `date`, `win_rate`, `game_count`, `window_size`. Use the exact same pattern as `analysis_service.py` lines ~220-240: accumulate results, slice `[-window:]`, compute `win_count / len(window_slice)`.
       - For the "overall" series: compute rolling series for endgame_rows and non_endgame_rows separately, then merge by date (collect all unique dates, for each date pick the latest point from each series, fill None where one series has no point on that date). Build `list[EndgameOverallPoint]`.
       - For per_type: for each class int key in per_type_rows, compute rolling series and map to `EndgameClass` string key using `_INT_TO_CLASS[class_int]`. Build `dict[str, list[EndgameTimelinePoint]]`.
       - Return `EndgameTimelineResponse(overall=..., per_type=..., window=window)`

    **In app/routers/endgames.py**, add two new endpoints:

    3. `@router.get("/endgames/performance", response_model=EndgamePerformanceResponse)` with params: `session`, `user`, `time_control: list[str] | None = Query(default=None)`, `platform: list[str] | None = Query(default=None)`, `recency: str | None = Query(default=None)`, `rated: bool | None = Query(default=None)`, `opponent_type: str = Query(default="all")`. Calls `endgame_service.get_endgame_performance(...)`.

    4. `@router.get("/endgames/timeline", response_model=EndgameTimelineResponse)` with same filter params plus `window: int = Query(default=50, ge=5, le=200)`. Calls `endgame_service.get_endgame_timeline(...)`.

    Import `EndgamePerformanceResponse, EndgameTimelineResponse` in the router.

    **In tests/test_endgame_service.py**, add test classes:

    5. `TestGetEndgamePerformance`: Mock `query_endgame_performance_rows` to return known rows. Verify WDL counts, percentages, and gauge values. Test zero-game edge case.

    6. `TestEndgameGaugeCalculations`: Unit test `relative_strength` and `endgame_skill` formulas. Test division-by-zero guards. Test aggregate conversion/recovery uses sum-of-raw not mean-of-percentages.

    7. `TestGetEndgameTimeline`: Mock `query_endgame_timeline_rows`. Verify rolling window with known sequence. Test partial window (fewer than 50 games). Test date merge for overall series.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run pytest tests/test_endgame_service.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - app/services/endgame_service.py contains `async def get_endgame_performance(`
    - app/services/endgame_service.py contains `async def get_endgame_timeline(`
    - app/services/endgame_service.py contains `relative_strength` calculation with `if overall_win_rate > 0` guard
    - app/services/endgame_service.py contains `endgame_skill` calculation with `0.6 *` and `0.4 *`
    - app/services/endgame_service.py contains `_compute_rolling_series` or equivalent helper function
    - app/routers/endgames.py contains `@router.get("/endgames/performance"`
    - app/routers/endgames.py contains `@router.get("/endgames/timeline"`
    - app/routers/endgames.py contains `window: int = Query(default=50`
    - tests/test_endgame_service.py contains `TestGetEndgamePerformance`
    - tests/test_endgame_service.py contains `TestGetEndgameTimeline`
    - `uv run pytest tests/test_endgame_service.py -x` exits 0
  </acceptance_criteria>
  <done>Both endpoints return correct JSON. All tests pass. Zero-division edge cases handled. Rolling window computes correctly with partial windows.</done>
</task>

</tasks>

<verification>
```bash
# All endgame tests pass
cd /home/aimfeld/Projects/Python/flawchess && uv run pytest tests/test_endgame_service.py -x -v

# Full backend test suite
uv run pytest -x

# Lint check
uv run ruff check app/schemas/endgames.py app/repositories/endgame_repository.py app/services/endgame_service.py app/routers/endgames.py

# Import verification
uv run python -c "from app.schemas.endgames import EndgamePerformanceResponse, EndgameTimelineResponse; print('OK')"
```
</verification>

<success_criteria>
- GET /api/endgames/performance returns correct JSON matching EndgamePerformanceResponse schema
- GET /api/endgames/timeline?window=50 returns correct JSON matching EndgameTimelineResponse schema
- Both endpoints accept and honor time_control, platform, recency, rated, opponent_type filters
- All gauge calculations handle division-by-zero gracefully
- Rolling window works with fewer games than window size
- All tests pass, no ruff warnings
</success_criteria>

<output>
After completion, create `.planning/phases/32-endgame-performance-charts/32-01-SUMMARY.md`
</output>
