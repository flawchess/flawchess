---
phase: 107-games-subtab-frontend-card-archive-filters-flaw-stats-panel
plan: 04
type: execute
wave: 2
depends_on: ["107-02"]
files_modified:
  - frontend/src/components/library/SeverityBadge.tsx
  - frontend/src/components/library/TagChip.tsx
  - frontend/src/components/library/NoAnalysisState.tsx
  - frontend/src/components/filters/LibraryFilterPanel.tsx
autonomous: true
requirements: [LIBG-01]
must_haves:
  truths:
    - "SeverityBadge renders a colored B/M/I count pill with aria-label '{count} {severity}s'"
    - "TagChip renders a family-colored display-only chip with honest 'not yet linked' ARIA and no navigation"
    - "NoAnalysisState renders the dashed 'No engine analysis' pill and never shows zero counts"
    - "LibraryFilterPanel composes FilterPanel (no color/matchSide) and adds the boolean Blunders/Mistakes severity filter"
  artifacts:
    - path: "frontend/src/components/library/SeverityBadge.tsx"
      provides: "Single severity count badge"
      contains: "severity-"
    - path: "frontend/src/components/library/TagChip.tsx"
      provides: "Family-colored display-only tag chip"
      contains: "not yet linked"
    - path: "frontend/src/components/library/NoAnalysisState.tsx"
      provides: "Dashed no-engine-analysis pill"
      contains: "No engine analysis"
    - path: "frontend/src/components/filters/LibraryFilterPanel.tsx"
      provides: "FilterPanel composition + severity filter"
      contains: "filter-severity-blunder"
  key_links:
    - from: "frontend/src/components/library/TagChip.tsx"
      to: "frontend/src/lib/theme.ts"
      via: "import FAM_TEMPO / FAM_OPPORTUNITY / FAM_IMPACT by tag family"
      pattern: "FAM_(TEMPO|OPPORTUNITY|IMPACT)"
---

<objective>
Build the independent leaf primitives for the analyzed card and the filter sidebar: `SeverityBadge`
(B/M/I count pill), `TagChip` (family-colored, display-only, honest ARIA per D-07), `NoAnalysisState`
(dashed "No engine analysis" pill), and `LibraryFilterPanel` (composes the existing `FilterPanel`
without color/matchSide and adds the boolean Blunders/Mistakes severity filter). These are
genuinely independent of each other and of `LibraryGameCard`, so they parallelize within Wave 2.

Purpose: Isolating these primitives keeps each plan small and lets `LibraryGameCard` (Plan 06)
assemble them without re-implementing chip/badge/state logic. D-07 keeps chips display-only with no
toast and no assumed `/library/...` Flaws route.
Output: Four new components consumed downstream by LibraryGameCard and GamesTab.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-CONTEXT.md
@frontend/src/types/library.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SeverityBadge + NoAnalysisState primitives</name>
  <files>frontend/src/components/library/SeverityBadge.tsx, frontend/src/components/library/NoAnalysisState.tsx</files>
  <read_first>
    - frontend/src/components/results/GameCard.tsx (the `resultIndicator` chip @ lines 71-82 and the `openingLine` pill @ lines 136-143 — the inline pill shape/classes SeverityBadge and NoAnalysisState adapt)
    - frontend/src/lib/theme.ts (the SEV_BLUNDER/MISTAKE/INACCURACY constants from Plan 02 — used at 14% bg / 30% border alpha per UI-SPEC §Severity)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§"Severity count row" and §'"No engine analysis" state' — exact copy, dashed-pill styling, alpha values, the rule that no-analysis NEVER shows "0 Blunders")
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`SeverityBadge.tsx` and §`NoAnalysisState.tsx` — the prop shape + JSX skeletons)
  </read_first>
  <action>
    Create `SeverityBadge` (`{ severity: FlawSeverity; count: number; gameId: number }`) rendering a
    nowrap count pill: count in `text-base font-bold`, full label (`Blunders`/`Mistakes`/`Inacc.`)
    in `text-sm font-bold`, colored from theme.ts (`SEV_*` foreground, background = color at 14%
    alpha, border = color at 30% alpha). Use semantic `<span>`; `aria-label={`${count}
    ${severity}s`}` (e.g. "2 blunders"); `data-testid={`severity-${severity}-${gameId}`}`. Derive
    the alpha bg/border from the base SEV_* constants (define small local color maps; do not
    hard-code new oklch values — only alpha-composite the theme constants).

    Create `NoAnalysisState` (`{ gameId: number }`) rendering the single dashed pill exactly per
    UI-SPEC: dashed border, full radius, muted text, `bg-white/5`, copy "No engine analysis",
    `text-sm font-bold`. `aria-label="No engine analysis available for this game"`;
    `data-testid={`no-analysis-${gameId}`}`. It must contain NO count text — it replaces the entire
    severity row + chips section, and must never render "0 Blunders".
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `SeverityBadge` renders `data-testid="severity-{severity}-{gameId}"`, `aria-label="{count} {severity}s"`, count in `text-base font-bold`, label in `text-sm font-bold` (no `text-xs`)
    - SeverityBadge colors come from theme.ts SEV_* (bg 14% / border 30% alpha), no hard-coded oklch
    - `NoAnalysisState` renders `data-testid="no-analysis-{gameId}"`, the dashed pill, copy exactly "No engine analysis", and contains no count/zero text
    - `cd frontend && npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>Both pill primitives match the UI-SPEC styling/ARIA/testids; no-analysis never shows counts.</done>
</task>

<task type="auto">
  <name>Task 2: TagChip (family-colored, display-only, honest ARIA per D-07)</name>
  <files>frontend/src/components/library/TagChip.tsx</files>
  <read_first>
    - frontend/src/components/results/GameCard.tsx (the inline chip pattern @ lines 71-82 — base chip classes)
    - frontend/src/lib/theme.ts (FAM_TEMPO/_BG, FAM_OPPORTUNITY/_BG, FAM_IMPACT/_BG from Plan 02)
    - frontend/src/types/library.ts (the `FlawTag` union from Plan 02)
    - app/services/flaws_service.py (lines 75-88 — the tag→family taxonomy for the color map)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§"Tag chips (analyzed games — display-only)" and §Interaction "Tag chip interaction" — display-only, cursor-pointer + brightness/translate hover, exact ARIA, the short label set, the per-family glyph)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`TagChip.tsx` — prop shape + getTagFamily + TAG_FAMILY_COLORS skeleton)
  </read_first>
  <action>
    Create `TagChip` (`{ tag: FlawTag; gameId: number }`). Implement a `getTagFamily(tag)` mapping
    to `'tempo' | 'opportunity' | 'impact'` (low-clock/impatient/considered → tempo;
    miss/lucky-escape → opportunity; while-ahead/result-changing → impact) and a `TAG_FAMILY_COLORS`
    map pulling `{ color, bg }` from the theme.ts `FAM_*` constants. Render a `<span>` chip:
    family color foreground + border, `_BG` background, `text-sm font-bold`, a small per-tag glyph
    (lucide icon or inline SVG, `h-3 w-3`), and the short label text equal to the tag string
    (`result-changing`, `low-clock`, `miss`, etc.). Per D-07 the chip is DISPLAY-ONLY: set
    `cursor-pointer` and the `hover: brightness + -translate-y-px` affordance, but NO onClick
    navigation, NO toast, and do NOT reference any `/library/...` Flaws route.
    `aria-label={`Tag: ${tag} (not yet linked)`}`; `data-testid={`chip-${tag}-${gameId}`}`. Phase
    tags (opening/middlegame/endgame) and inaccuracy-level tags are excluded by the Phase 106 chip
    curation upstream, so the chip set passed in already excludes them — TagChip itself just renders
    whatever FlawTag it receives. No `dangerouslySetInnerHTML` (tag strings render as React-escaped
    text).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `TagChip` renders `data-testid="chip-{tag}-{gameId}"` and `aria-label="Tag: {tag} (not yet linked)"`
    - Color is family-derived from theme.ts FAM_* (tempo/opportunity/impact), no per-tag color sprawl, no hard-coded oklch
    - Chip is display-only: cursor-pointer + brightness/translate hover, NO onClick navigation, NO toast, NO `/library/...` route reference (D-07)
    - Label text is `text-sm` font floor (the chip/badge exception applies; no `text-xs`), tag rendered as escaped text (no `dangerouslySetInnerHTML`)
    - `cd frontend && npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>TagChip is family-colored, display-only, honestly labeled, and routes nowhere (D-07).</done>
</task>

<task type="auto">
  <name>Task 3: LibraryFilterPanel (FilterPanel composition + boolean severity filter)</name>
  <files>frontend/src/components/filters/LibraryFilterPanel.tsx</files>
  <read_first>
    - frontend/src/components/filters/FilterPanel.tsx (read `FilterPanelProps` @ lines 134-143 incl. the `visibleFilters?: FilterField[]` prop, the `FilterField` union, the TimeControl ToggleGroup @ ~lines 147-153 for the toggle-button styling, and `FILTER_DOT_FIELDS` @ ~lines 76-85)
    - frontend/src/pages/Endgames.tsx (how Endgames composes FilterPanel with `visibleFilters` and the Reset button — the desktop sidebar + Reset pattern to mirror)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§"Filter Panel Extension — Library Games" — the filters shown/omitted table, the "Show games with:" severity toggle styling/testids/heights, the Reset button contract)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`LibraryFilterPanel.tsx` — visibleFilters list, the severity-toggle JSX, the isModified-with-severity pattern)
  </read_first>
  <action>
    Create `LibraryFilterPanel` composing the existing `FilterPanel` with `visibleFilters` set to
    `['timeControl', 'platform', 'opponent', 'opponentStrength', 'rated', 'recency']` (omit
    color/matchSide — the Games surface shows all colors, no opening/position filter, no chessboard).
    Above the composed FilterPanel, render the new boolean mistake-severity section: a "Show games
    with:" label (`text-sm text-muted-foreground`) and two `<button>` toggles "Blunders" /
    "Mistakes" using the existing FilterPanel toggle classes (active:
    `border-toggle-active bg-toggle-active text-toggle-active-foreground`; inactive:
    `border-border bg-inactive-bg text-muted-foreground`), height `h-11 sm:h-7`,
    `aria-pressed` reflecting state, `data-testid="filter-severity-blunder"` /
    `data-testid="filter-severity-mistake"`. Props: receive `severityFilter: ('blunder' |
    'mistake')[]` and an `onSeverityChange` callback from the parent (severity is NOT part of the
    shared `FilterState`); plus the standard `filters` / `onChange` it forwards to FilterPanel.
    Include the Reset Filters button (`variant="brand-outline"`, `data-testid="btn-reset-filters"`)
    whose reset clears BOTH the FilterState (to DEFAULT_FILTERS) and the severityFilter. Wrap the
    desktop sidebar form in semantic `<aside>` per UI-SPEC §Semantic HTML. Apply identical markup
    for both the desktop sidebar and the mobile-drawer usage (the same component is rendered in
    both contexts — no separate mobile copy needed here, but ensure the toggle touch height
    `h-11 sm:h-7` is preserved).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `LibraryFilterPanel` composes `FilterPanel` with `visibleFilters` excluding color/matchSide
    - Renders the "Show games with:" label and two toggles with `data-testid="filter-severity-blunder"` / `filter-severity-mistake`, `aria-pressed`, height `h-11 sm:h-7`
    - `severityFilter: ('blunder' | 'mistake')[]` is a prop (NOT added to the shared `FilterState`); reset clears both filters + severityFilter
    - Reset button uses `variant="brand-outline"` and `data-testid="btn-reset-filters"`
    - `cd frontend && npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>LibraryFilterPanel shows the right metadata filters + the boolean severity toggle with correct testids/ARIA; severity stays out of FilterState.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend tag/opening strings → rendered chip/label text | User-owned game data (tag names, opening names) rendered as React text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-05 | Tampering (XSS) | TagChip / SeverityBadge label text | mitigate | Tag/severity strings are rendered as React children (auto-escaped). No `dangerouslySetInnerHTML` is used anywhere in these primitives — explicit acceptance criterion on TagChip. |
| T-107-06 | Information Disclosure | severity filter values | accept | `('blunder'|'mistake')[]` are enum-constrained UI toggles, not free input; passed to a user-scoped backend. No risk. |
| T-107-SC | Tampering | npm installs | accept | No new dependency (lucide-react already present for glyphs). |

**No high-severity threats.** Display-only primitives; React-escaped text; enum-constrained filter input.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit`, `npm run lint` clean.
- `npm run knip` clean once these components are imported by Plan 06 (verified there); for this
  plan, ensure no unused exports are added that knip would flag in isolation — export only the
  component(s) that Plan 06 will import.
</verification>

<success_criteria>
- SeverityBadge / TagChip / NoAnalysisState / LibraryFilterPanel exist with exact UI-SPEC
  styling, copy, testids, and ARIA (LIBG-01).
- Chips are display-only with honest ARIA and no assumed Flaws route (D-07).
- Severity filter is a separate prop, not part of FilterState.
</success_criteria>

<output>
Create `.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-04-SUMMARY.md` when done.
</output>
