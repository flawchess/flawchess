---
phase: 107-games-subtab-frontend-card-archive-filters-flaw-stats-panel
plan: 07
type: execute
wave: 4
depends_on: ["107-04", "107-05", "107-06"]
files_modified:
  - frontend/src/pages/library/GamesTab.tsx
  - frontend/src/pages/library/LibraryPage.tsx
autonomous: false
requirements: [LIBG-01, LIBG-03]
must_haves:
  truths:
    - "The Library page exposes a third 'Games' subtab at /library/games and makes it the returning-user default (games > 0)"
    - "GamesTab composes the FlawStatsPanel + LibraryFilterPanel (desktop sidebar / mobile drawer) + LibraryGameCardList, both queries sharing the same filter + severity params"
    - "Both data-loading chains have isError branches; the full empty-state set (no games / no matches / no analyzed games) renders correct copy"
    - "The whole surface works on mobile via the Openings drawer pattern with full data-testid / ARIA / semantic-HTML conventions"
  artifacts:
    - path: "frontend/src/pages/library/GamesTab.tsx"
      provides: "Games subtab root composing panel + filters + card list"
      contains: "games-tab-content"
    - path: "frontend/src/pages/library/LibraryPage.tsx"
      provides: "Third Games TabsTrigger + returning-user default redirect to /library/games"
      contains: "tab-games"
  key_links:
    - from: "frontend/src/pages/library/GamesTab.tsx"
      to: "useLibraryGames / useLibraryFlawStats"
      via: "shared filters + severityFilter params drive both queries"
      pattern: "useLibrary(Games|FlawStats)"
    - from: "frontend/src/pages/library/LibraryPage.tsx"
      to: "/library/games"
      via: "Navigate redirect for users with games + third TabsTrigger"
      pattern: "library/games"
---

<objective>
Final assembly: `GamesTab` (the Games subtab root composing `FlawStatsPanel` + `LibraryFilterPanel`
sidebar/drawer + `LibraryGameCardList`, wiring both `useLibrary*` queries to the shared filter +
local `severityFilter` state) and the `LibraryPage` change to add the third "Games" `TabsTrigger`
and flip the returning-user default redirect from `/library/overview` to `/library/games`. Ends with
a human-verify checkpoint for the live mobile + desktop surface.

Purpose: This wires every Phase 107 piece into the user-facing Games experience and the new
returning-user default (UI-SPEC §Surface Architecture). It owns the full empty/error-state matrix
and the desktop-sidebar / mobile-drawer parity (Success Criterion 5).
Output: Two modified page files + a verified end-to-end Games subtab.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-CONTEXT.md
@frontend/src/types/library.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GamesTab — compose panel + filters (sidebar/drawer) + card list, wire both queries</name>
  <files>frontend/src/pages/library/GamesTab.tsx</files>
  <read_first>
    - frontend/src/pages/Endgames.tsx (the canonical desktop-sidebar + mobile-Drawer analog — read imports @ lines 1-44, filter state wiring (appliedFilters/pendingFilters + useEffect + mobileFiltersOpen) @ lines 190-230, the modified-dot node @ lines 259-270, the mobile filters button @ ~line 913, the mobile Drawer JSX @ lines 931-951, and `handleMobileFiltersOpenChange` @ lines 320-330)
    - frontend/src/hooks/useLibrary.ts (Plan 02 — `useLibraryGames(filters, severity, offset, limit)` and `useLibraryFlawStats(filters, severity)`)
    - frontend/src/components/filters/LibraryFilterPanel.tsx (Plan 04), frontend/src/components/library/FlawStatsPanel.tsx (Plan 05), frontend/src/components/results/LibraryGameCardList.tsx (Plan 06) — the composed children
    - frontend/src/components/filters/FilterPanel.tsx (DEFAULT_FILTERS, areFiltersEqual, FILTER_DOT_FIELDS, FilterState — the shared filter primitives)
    - frontend/src/components/layout/SidebarLayout.tsx (the desktop sidebar layout wrapper used by Endgames)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§"Page Layout — Desktop"/"— Mobile", §"Filter interaction" (desktop live / mobile on-drawer-close), §State Management (query keys, shared params, reset to page 1), §Copywriting Contract (all empty/error copy), §Browser Automation (the `tab-games`/`games-tab-content`/`btn-filters` testids))
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`GamesTab.tsx` — the full Endgames-mirroring skeleton incl. the severityFilter-as-local-state note)
  </read_first>
  <action>
    Create `frontend/src/pages/library/GamesTab.tsx` mirroring `Endgames.tsx`'s sidebar+drawer
    structure. State: the shared `FilterState` via the applied/pending pattern (desktop applies live;
    mobile applies on drawer close per UI-SPEC), PLUS `severityFilter: ('blunder' | 'mistake')[]` as
    SEPARATE local state (not in FilterState), PLUS `offset` (page) state. On any filter or severity
    change, reset `offset` to 0 (UI-SPEC State Management). Drive both queries:
    `useLibraryGames(appliedFilters, severityFilter, offset, PAGE_SIZE)` and
    `useLibraryFlawStats(appliedFilters, severityFilter)` — both share the same params so the panel
    reflects the same game set as the list; pass the severity[] to BOTH.

    Layout: desktop = `SidebarLayout` with `LibraryFilterPanel` in the sidebar (`<aside>`) and the
    main column = `FlawStatsPanel` above `LibraryGameCardList`. Mobile = sticky "Filters" button
    (`btn-filters`, `variant="brand-outline"`, `SlidersHorizontal`, modified-dot when
    `!areFiltersEqual(appliedFilters, DEFAULT_FILTERS, FILTER_DOT_FIELDS) || severityFilter.length >
    0`) opening a right `Drawer` containing `LibraryFilterPanel`, then the stacked panel + list.
    Root container `data-testid="games-tab-content"`.

    States (mandatory): both `useQuery` chains MUST have `isError` branches — games error: "Failed
    to load games. Something went wrong. Please try again in a moment."; stats error handled inside
    FlawStatsPanel (pass `isError`). Empty states per UI-SPEC Copywriting Contract: no games imported
    ("No games imported yet" + Import CTA → `/library/import`), filters match nothing ("No games
    matched" + "Try adjusting the filters."), no analyzed games (the FlawStatsPanel handles its
    zero-N copy). NEVER let an error fall through to an empty state (CLAUDE.md). Reset Filters clears
    both filters and severityFilter. PAGE_SIZE is a named constant (20). Apply full
    `data-testid`/ARIA/semantic-HTML per UI-SPEC.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `GamesTab` root has `data-testid="games-tab-content"`; mobile filters button is `btn-filters` with the modified dot reflecting filters OR severity
    - `useLibraryGames` and `useLibraryFlawStats` both receive the shared `appliedFilters` + `severityFilter`; a filter/severity change resets offset to page 1
    - `severityFilter` is local state, NOT part of `FilterState`
    - Both data chains have `isError` branches with the exact copy; the three empty states render their UI-SPEC copy; no error falls through to an empty state
    - Desktop renders the sidebar layout; mobile renders the Drawer with `LibraryFilterPanel`; both render `FlawStatsPanel` + `LibraryGameCardList`
    - `cd frontend && npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>GamesTab composes the full surface, shares filter+severity params across both queries, and handles every error/empty state on desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 2: LibraryPage — add the Games TabsTrigger and flip the returning-user default redirect</name>
  <files>frontend/src/pages/library/LibraryPage.tsx</files>
  <read_first>
    - frontend/src/pages/library/LibraryPage.tsx (the full file — the default redirect `Navigate` @ lines 28-33, the `activeTab` derivation @ ~line 35, the desktop Tabs block @ lines 47-70 and mobile Tabs block @ lines 73-119; both must gain the third trigger + content)
    - frontend/src/pages/library/GamesTab.tsx (Task 1 — the component to mount in the new TabsContent)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§Surface Architecture + §Interaction "Returning-user default subtab" — the `noGames ? '/library/import' : '/library/games'` redirect, the third trigger `tab-games`/`tab-games-mobile`, the "Games" label + BookOpen/Library icon)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`LibraryPage.tsx (MODIFIED)` — the exact 4 changes + the TabsTrigger snippet)
  </read_first>
  <action>
    Modify `LibraryPage.tsx`: (1) add a third `TabsTrigger value="games"` ("Games" label, `BookOpen`
    or `Library` lucide icon) to BOTH the desktop Tabs block and the mobile Tabs block, with
    `data-testid="tab-games"` (desktop) / `data-testid="tab-games-mobile"` (mobile); (2) add the
    corresponding `TabsContent value="games"` rendering `<GamesTab />` (with the existing
    Suspense/lazy pattern if the page lazy-loads tabs); (3) update the `activeTab` derivation to
    detect `/games`; (4) change the default-landing `Navigate` from
    `noGames ? '/library/import' : '/library/overview'` to
    `noGames ? '/library/import' : '/library/games'` (returning users with games now default to
    Games). Preserve the gameless-user → `/library/import` behavior. Apply the change to BOTH the
    desktop and mobile Tabs renderers (CLAUDE.md mobile-parity). Keep all existing Import/Overview
    triggers and routing intact (this is additive + a one-line redirect flip).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `LibraryPage.tsx` renders a third `TabsTrigger value="games"` with `data-testid="tab-games"` (desktop) and `tab-games-mobile` (mobile), plus `TabsContent value="games"` mounting `<GamesTab />`
    - The default redirect is `noGames ? '/library/import' : '/library/games'`; gameless users still land on Import
    - The change is applied to both the desktop and mobile Tabs blocks
    - Existing Import/Overview triggers + routes remain unchanged
    - `cd frontend && npx tsc --noEmit`, `npm run lint`, `npm run knip` all exit 0
  </acceptance_criteria>
  <done>The Library page exposes the Games subtab at /library/games and defaults returning users to it; both Tabs renderers updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification of the Games subtab (desktop + mobile)</name>
  <what-built>
    The complete Library Games subtab: a returning-user-default subtab at `/library/games` with the
    Flaw-Stats panel (per-game/per-100 toggle, denominator, trend, tag distribution incl. Opportunity/
    Impact from the real D-01 rates), the filterable game-card archive (metadata filters + the
    boolean Blunders/Mistakes severity filter), analyzed cards with B/M/I badges + family-colored
    chips, the explicit "No engine analysis" state for chess.com/unanalyzed games, pagination, and
    the desktop-sidebar / mobile-drawer layouts.
  </what-built>
  <how-to-verify>
    1. Ensure the dev DB is up (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`)
       and run the app (`bin/run_local.sh` or the backend + `cd frontend && npm run dev`). Sign in
       as a user that HAS imported games (ideally with some analyzed Lichess games and some
       chess.com games).
    2. Visit `/library` — confirm a returning user with games lands on the **Games** subtab by
       default (URL becomes `/library/games`), and a brand-new/gameless user still lands on Import.
    3. On the Games subtab (desktop): confirm the Flaw-Stats panel renders the band (toggle "per
       game" ↔ "per 100 moves" updates the numbers + label suffix, no reload), the denominator pill
       ("{pct}% analyzed · N = {n}"), the blunders/game trend chart, and Zone 3 (tempo stacked bar
       WITH an unmeasured gray remainder, By-phase histogram, Opportunity = miss + lucky-escape,
       Impact = while-ahead + result-changing — all showing real percentages, no "coming soon").
    4. Confirm analyzed cards show the B/M/I severity badges + family-colored chips; confirm a
       chess.com / unanalyzed game shows the dashed "No engine analysis" pill and NEVER "0 Blunders".
    5. Toggle the Blunders / Mistakes severity filter and a couple of metadata filters; confirm both
       the card list and the panel update together, and the list resets to page 1. Page through the
       results; confirm scroll-to-top.
    6. Resize to mobile (≤420px) or use device emulation: confirm the "Filters" button opens the
       drawer, filters apply on drawer close, and the panel + cards stack responsively with the
       mobile card layout (stacked header, full-width flaw block).
    7. Hover a tag chip: confirm the brightness/translate affordance, and that clicking does NOTHING
       (display-only, no navigation, no toast) — D-07.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/library/games` + `/library/flaw-stats` | Authenticated, user-scoped queries (Phase 106 + D-01). GamesTab passes only enum-constrained filter + severity params. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-12 | Information Disclosure | cross-user game data via the Games queries | accept | Both endpoints are `current_active_user`-scoped server-side (verified in `app/routers/library.py`); the client cannot request another user's games. No client-side authorization logic to bypass. |
| T-107-13 | Spoofing/Redirect | returning-user default `Navigate` | accept | Redirect targets are static in-app routes (`/library/import`, `/library/games`); no user-controlled redirect target. |
| T-107-14 | Tampering (XSS) | empty/error-state copy + rendered game data | mitigate | All copy is static React text; game data is escaped; no `dangerouslySetInnerHTML`. |
| T-107-SC | Tampering | npm installs | accept | No new dependency. |

**No high-severity threats.** User-scoped authenticated queries, static in-app redirects, escaped rendering.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit`, `npm run lint`, `npm run knip` clean.
- FULL integration gate (run before the squash-merge to main, per CLAUDE.md):
  `uv run ruff format app/ tests/` · `uv run ruff check app/ tests/ --fix` ·
  `uv run ty check app/ tests/` · `uv run pytest -n auto -x` ·
  `(cd frontend && npm run lint && npm test -- --run)`.
- The Endgames pagination tests (`Endgames.overallPerformance.test.tsx`,
  `Endgames.readinessGate.test.tsx`) remain green (D-06 blast-radius regression guard, re-verified
  here at full assembly).
- Human-verify checkpoint (Task 3) signed off.
</verification>

<success_criteria>
- The Games subtab exists at `/library/games`, is the returning-user default, and renders the
  card archive + Flaw-Stats panel from both Phase 106 endpoints (LIBG-01, LIBG-03).
- Metadata filters + the boolean severity filter drive both queries; no chessboard / opening filter.
- Mobile drawer parity, full `data-testid`/ARIA/semantic-HTML, `isError` on every chain
  (LIBG-01, LIBG-03, Success Criterion 5).
- D-06: Openings + Endgames pagination tests still green.
</success_criteria>

<output>
Create `.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-07-SUMMARY.md` when done.
</output>
