---
phase: 107-games-subtab-frontend-card-archive-filters-flaw-stats-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/types/library.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useLibrary.ts
autonomous: true
requirements: [LIBG-01, LIBG-03]
must_haves:
  truths:
    - "theme.ts exports the full named tag-family / severity / phase color constant set per UI-SPEC §Color"
    - "TS types mirror the backend LibraryGamesResponse / GameFlawCard / FlawStatsResponse / TagDistribution incl. the three new rate fields"
    - "useLibraryGames and useLibraryFlawStats hooks fetch both endpoints with a shared params object and the severity[] filter"
  artifacts:
    - path: "frontend/src/lib/theme.ts"
      provides: "SEV_*, FAM_*, PHASE_* semantic color constants"
      contains: "FAM_OPPORTUNITY"
    - path: "frontend/src/types/library.ts"
      provides: "Library API response/request types incl. FlawTag union and analysis_state"
      contains: "FlawStatsResponse"
    - path: "frontend/src/api/client.ts"
      provides: "libraryApi.getGames + libraryApi.getFlawStats"
      contains: "flaw-stats"
    - path: "frontend/src/hooks/useLibrary.ts"
      provides: "useLibraryGames + useLibraryFlawStats TanStack Query hooks"
      contains: "library-flaw-stats"
  key_links:
    - from: "frontend/src/hooks/useLibrary.ts"
      to: "frontend/src/api/client.ts libraryApi"
      via: "queryFn calls libraryApi.getGames / getFlawStats"
      pattern: "libraryApi\\."
---

<objective>
The shared frontend foundations every Phase 107 component consumes: (1) all new `theme.ts`
semantic color constants (severity, tag families, phase histogram) named exactly per UI-SPEC §Color;
(2) the TypeScript types mirroring the Phase 106 + D-01 backend contracts; (3) the `libraryApi`
client methods for `GET /api/library/games` and `GET /api/library/flaw-stats`; (4) the
`useLibraryGames` / `useLibraryFlawStats` TanStack Query hooks.

Purpose: These are leaf dependencies — cards, badges, chips, and the Flaw-Stats panel all read
these constants, types, and hooks. Landing them first (Wave 1) lets every downstream component
build against real constants and real fields rather than placeholders.
Output: One modified config file + three new frontend modules. No component rendering yet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-CONTEXT.md
@app/schemas/library.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add severity / tag-family / phase color constants to theme.ts</name>
  <files>frontend/src/lib/theme.ts</files>
  <read_first>
    - frontend/src/lib/theme.ts (read the existing `WDL_BORDER_*` block ~lines 14-24 — the exact placement target; reuse `WDL_BORDER_WIN/DRAW/LOSS` if already present, do not redefine)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§Color — the authoritative constant name → oklch value table for SEV_*, FAM_*, PHASE_*)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`frontend/src/lib/theme.ts (MODIFIED)` — the exact block to add and its placement after the WDL block)
  </read_first>
  <action>
    Add the new semantic color constants immediately after the existing `WDL_BORDER_*` block, with
    the exact names and oklch values from UI-SPEC §Color / PATTERNS.md §theme.ts:
    `SEV_BLUNDER`, `SEV_MISTAKE`, `SEV_INACCURACY`; `FAM_TEMPO`, `FAM_TEMPO_BG`,
    `FAM_TEMPO_LOW_CLOCK`, `FAM_TEMPO_IMPATIENT`, `FAM_TEMPO_CONSIDERED`, `FAM_TEMPO_UNMEASURED`,
    `FAM_OPPORTUNITY`, `FAM_OPPORTUNITY_BG`, `FAM_IMPACT`, `FAM_IMPACT_BG`; `PHASE_OPENING`,
    `PHASE_MIDDLEGAME`, `PHASE_ENDGAME`. Reuse any constant in this set that already exists in
    theme.ts (e.g. `WDL_BORDER_*`); add only the missing ones. Names must match UI-SPEC §Color
    character-for-character (downstream components import these literally). Group with short
    section comments matching the existing file style. Do NOT hard-code these oklch values anywhere
    else — components import them from here (CLAUDE.md theme-constants rule).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -E "FAM_OPPORTUNITY|FAM_IMPACT|PHASE_OPENING|SEV_BLUNDER|FAM_TEMPO_UNMEASURED" src/lib/theme.ts | grep -c export</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/theme.ts` exports every constant named in UI-SPEC §Color: SEV_BLUNDER/MISTAKE/INACCURACY, FAM_TEMPO(+_BG/_LOW_CLOCK/_IMPATIENT/_CONSIDERED/_UNMEASURED), FAM_OPPORTUNITY(+_BG), FAM_IMPACT(+_BG), PHASE_OPENING/MIDDLEGAME/ENDGAME
    - oklch values match UI-SPEC §Color exactly
    - No pre-existing constant (e.g. WDL_BORDER_*) is duplicated or redefined
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All UI-SPEC §Color constants are exported from theme.ts with exact names and values; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add library TS types (responses, FlawTag union, analysis_state, severity_counts, rates)</name>
  <files>frontend/src/types/library.ts</files>
  <read_first>
    - app/schemas/library.py (the authoritative backend contract: LibraryGamesResponse, GameFlawCard, FlawStatsResponse, SeverityRates @ lines 84-85, TagDistribution @ lines 88-105 + the three new D-01 rate fields, FlawTrendPoint @ lines 108-119; severity_counts shape; analysis_state values)
    - app/services/flaws_service.py (lines 75-88 — the `FlawTag` / `FlawSeverity` / `TempoTag` literal taxonomy: low-clock/impatient/considered/miss/lucky-escape/while-ahead/result-changing/opening/middlegame/endgame)
    - frontend/src/types/api.ts (read `UserResult` @ line 57 and `GameRecord` @ line 101 — reuse `UserResult` and shared game-metadata field shapes; do not redefine them)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§API Data Mapping — the response→UI field mapping that the types must support)
  </read_first>
  <action>
    Create `frontend/src/types/library.ts` mirroring the backend schemas. Define discriminated /
    literal-union types (no loose `string`): `FlawTag` as the exact union of the final tag strings
    (`'low-clock' | 'impatient' | 'considered' | 'miss' | 'lucky-escape' | 'while-ahead' |
    'result-changing' | 'opening' | 'middlegame' | 'endgame'`), `FlawSeverity`
    (`'inaccuracy' | 'mistake' | 'blunder'`), `TempoTag` (`'low-clock' | 'impatient' |
    'considered'`), and an `AnalysisState` union (`'analyzed' | 'no_engine_analysis'`). Define the
    per-game card type (mirror `GameFlawCard`: game metadata fields reused from / aligned with
    `GameRecord` + `UserResult`, plus `analysis_state`, `severity_counts` (nullable when
    `no_engine_analysis`), and `chips: FlawTag[]`). Define `LibraryGamesResponse` (games[],
    matched_count, offset, limit), `SeverityRates` (`per_game` / `per_100_moves` keyed by
    FlawSeverity), `TagDistribution` (tempo dict keyed by TempoTag, `result_changing_rate`,
    `phase_histogram`, and the three new flat floats `miss_rate`, `lucky_escape_rate`,
    `while_ahead_rate`), `FlawTrendPoint` (date, rate, game_count, window_size), and
    `FlawStatsResponse` (per_severity_counts, rates, tag_distribution, trend, analyzed_pct,
    analyzed_n, total_n). Import and reuse `UserResult` from `types/api.ts`. Honor
    `noUncheckedIndexedAccess` — keep dict types as `Record<...>` where the backend returns dicts.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/library.ts` exports `FlawTag`, `FlawSeverity`, `TempoTag`, `AnalysisState`, the per-game card type, `LibraryGamesResponse`, `SeverityRates`, `TagDistribution`, `FlawTrendPoint`, `FlawStatsResponse`
    - `TagDistribution` includes `miss_rate`, `lucky_escape_rate`, `while_ahead_rate` (matching the D-01 backend fields)
    - `FlawTag` is the exact literal union of the final tag taxonomy (no bare `string`)
    - `UserResult` is imported from `types/api.ts`, not re-declared
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Library types mirror the backend contract incl. D-01 fields; literal unions only; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add libraryApi client methods and the useLibrary TanStack Query hooks</name>
  <files>frontend/src/api/client.ts, frontend/src/hooks/useLibrary.ts</files>
  <read_first>
    - frontend/src/api/client.ts (read `buildFilterParams` @ line 75 and the `endgameApi` block @ line 178 incl. its `getGames` method @ ~line 193 — the exact axios `apiClient.get<Response>(path, { params })` + multi-value param convention to mirror)
    - frontend/src/hooks/useEndgames.ts (full file — `buildEndgameParams`, the `useQuery` key + staleTime + `refetchOnWindowFocus: false` conventions to copy into `buildLibraryParams` / `useLibraryGames` / `useLibraryFlawStats`)
    - frontend/src/components/filters/FilterPanel.tsx (read `FilterState` @ ~lines 40-85 and the date-range helpers used by buildEndgameParams — `dateRangeToWireParams` / `resolveDateRange`)
    - frontend/src/types/library.ts (the types created in Task 2 — the response generics)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§Shared Patterns → "TanStack Query hooks for library data" — the buildLibraryParams + hook skeletons)
  </read_first>
  <action>
    In `frontend/src/api/client.ts`, add a `libraryApi` object (same style as `endgameApi`) with
    `getGames(params)` → `apiClient.get<LibraryGamesResponse>('/library/games', { params })` and
    `getFlawStats(params)` → `apiClient.get<FlawStatsResponse>('/library/flaw-stats', { params })`.
    Use the path `/library/flaw-stats` (never `mistake-stats`). Pass the severity multi-value param
    so axios serializes `severity=blunder&severity=mistake`; reuse the existing multi-value param
    convention from `endgameApi.getGames` / `buildFilterParams`.

    Create `frontend/src/hooks/useLibrary.ts` with `buildLibraryParams(filters, severity)` (copy
    `buildEndgameParams`, drop the color/matchSide params — Games surface shows all colors — and add
    `severity: severity.length > 0 ? severity : undefined`), then `useLibraryGames(filters,
    severity, offset, limit)` keyed `['library-games', params, offset, limit]` and
    `useLibraryFlawStats(filters, severity)` keyed `['library-flaw-stats', params]` (no offset —
    the stats panel is page-independent). Both use the library stale-time + `refetchOnWindowFocus:
    false` per the Endgames hook precedent. `severity` is typed `('blunder' | 'mistake')[]`. Ensure
    every new export is actually re-exported / consumed so `knip` stays clean (the hooks will be
    imported by GamesTab in a later plan — export them as the public hook API; do not add unused
    helper exports).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/api/client.ts` exports `libraryApi` with `getGames` and `getFlawStats`, the latter calling path `/library/flaw-stats` (NOT `mistake-stats`)
    - severity is passed as a multi-value query param compatible with `severity=blunder&severity=mistake`
    - `frontend/src/hooks/useLibrary.ts` exports `useLibraryGames` and `useLibraryFlawStats` with the query keys `['library-games', params, offset, limit]` and `['library-flaw-stats', params]`
    - `severity` param typed `('blunder' | 'mistake')[]`, never bare `string[]`
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>libraryApi + the two hooks exist, point at `/library/flaw-stats`, and share a params object; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend library endpoints | The new client methods call existing authenticated, user-scoped endpoints (Phase 106 + the D-01 slice). No new endpoint or auth surface is introduced here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-03 | Information Disclosure | useLibrary query params | accept | Filters/severity are non-sensitive query parameters; the backend scopes results to the authenticated user. No secrets in query keys. |
| T-107-SC | Tampering | npm installs | mitigate | This plan adds NO new dependency (recharts, axios, TanStack Query already present). If any executor proposes a new package, halt — none is required. |

**No high-severity threats.** Pure type/client/hook plumbing over existing authenticated
endpoints; no `dangerouslySetInnerHTML`, no new dependency, no auth change.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npm run lint` clean.
- theme.ts exports the full UI-SPEC §Color constant set with exact names.
</verification>

<success_criteria>
- All UI-SPEC §Color constants exist in theme.ts (LIBG-01/03 visual contract dependency).
- Library types mirror the backend incl. the three D-01 rate fields (LIBG-03).
- `libraryApi` + `useLibrary*` hooks consume `/library/games` and `/library/flaw-stats` with a
  shared params object and the severity[] filter (LIBG-01/03).
</success_criteria>

<output>
Create `.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-02-SUMMARY.md` when done.
</output>
