---
phase: 107-games-subtab-frontend-card-archive-filters-flaw-stats-panel
plan: 06
type: execute
wave: 3
depends_on: ["107-02", "107-03", "107-04"]
files_modified:
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/results/LibraryGameCardList.tsx
autonomous: true
requirements: [LIBG-01]
must_haves:
  truths:
    - "An analyzed LibraryGameCard shows the game metadata/board + a flaw column with B/M/I severity badges and family-colored tag chips"
    - "A chess.com / unanalyzed card shows the NoAnalysisState pill instead of the severity row + chips, never a false zero-flaw game"
    - "LibraryGameCardList paginates LibraryGameCard via the shared Pagination component and scrolls to library-game-card-list on page change"
  artifacts:
    - path: "frontend/src/components/results/LibraryGameCard.tsx"
      provides: "Library card: header + board + info + flaw column / no-analysis state"
      contains: "library-game-card-"
    - path: "frontend/src/components/results/LibraryGameCardList.tsx"
      provides: "Paginated list of LibraryGameCard with matched-count row"
      contains: "library-game-card-list"
  key_links:
    - from: "frontend/src/components/results/LibraryGameCard.tsx"
      to: "SeverityBadge / TagChip / NoAnalysisState"
      via: "render flaw column from severity_counts + chips, or NoAnalysisState when no_engine_analysis"
      pattern: "analysis_state"
    - from: "frontend/src/components/results/LibraryGameCardList.tsx"
      to: "frontend/src/components/results/Pagination.tsx"
      via: "import + <Pagination .../>"
      pattern: "Pagination"
---

<objective>
Assemble the analyzed game card: `LibraryGameCard` (a NEW component per D-05 — full-width header,
3-column desktop body, flaw column, mobile flaw block, and the no-analysis state) borrowing
GameCard's metadata/board/platform patterns, and `LibraryGameCardList` (wraps LibraryGameCard in the
shared `Pagination` from Plan 03). The card composes the Plan 04 primitives (SeverityBadge, TagChip,
NoAnalysisState).

Purpose: LibraryGameCard is the headline per-game surface of LIBG-01. D-05 explicitly rejects
genericizing GameCard — this is a separate component. The no-analysis branch enforces Success
Criterion 1: chess.com / unanalyzed cards show an explicit "no engine analysis" state, never a
false clean game.
Output: Two new components consumed by GamesTab (Plan 07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-CONTEXT.md
@frontend/src/types/library.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LibraryGameCard (header + board + info + flaw column / no-analysis state)</name>
  <files>frontend/src/components/results/LibraryGameCard.tsx</files>
  <read_first>
    - frontend/src/components/results/GameCard.tsx (the full file — esp. imports @ lines 1-9, `BORDER_COLORS` @ lines 17-27, `formatDate`/`formatTimeControl` @ lines 29-61, the platform icon + external link @ lines 84-102, the mobile layout @ lines 198-213, and the desktop layout @ lines 215-229 — these are the patterns LibraryGameCard borrows; do NOT modify GameCard, D-05)
    - frontend/src/components/library/SeverityBadge.tsx, TagChip.tsx, NoAnalysisState.tsx (Plan 04 primitives the flaw column composes)
    - frontend/src/types/library.ts (the per-game card type: `analysis_state`, `severity_counts` (nullable), `chips: FlawTag[]`, plus the reused game-metadata + `UserResult`)
    - frontend/src/lib/theme.ts (WDL_BORDER_* for the left border)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§"Analyzed Game Card (Sketch 001-A) — Detailed Contract" — full-width header desktop/mobile rules, the 3-col desktop body + flaw column dashed left border, the severity nowrap row, the chip row, the no-analysis replacement rule, desktop/mobile metadata order, the card `data-testid` table)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`LibraryGameCard.tsx` — the desktop 3-col + flaw-column JSX skeleton and the copy-verbatim helpers)
  </read_first>
  <action>
    Create `LibraryGameCard` (prop: the per-game card object from `types/library.ts`). Render a root
    `<article data-testid={`library-game-card-${gameId}`}>` with the WDL left border (4px,
    `WDL_BORDER_*` keyed by `user_result`) and the charcoal-texture card classes from GameCard. Copy
    `formatDate` / `formatTimeControl` verbatim from GameCard (same display requirements). Build the
    full-width card header per UI-SPEC: desktop single-line `■ White (rating) vs □ Black (rating)`
    (`text-sm font-bold`), mobile two stacked lines (`text-sm` weight 400, no "vs"), with the
    platform icon + external link pinned `ml-auto` (reuse GameCard's platform-link block + the
    existing `game-card-link-{gameId}` testid), and a bottom border `pb-2 mb-2`.

    Body — DESKTOP (`hidden sm:flex`): three columns — (1) `LazyMiniBoard` (fen=`result_fen`,
    flipped=`user_color === 'black'`), (2) info column (flex-1: opening line + desktop metadata row
    in the GameCard order termination·date·TC·move-count), (3) flaw column
    (`flex: 0 0 auto`, dashed left border, `pl-4`). Body — MOBILE (`flex flex-col sm:hidden`):
    stacked header already above; board + info row; then a full-width flaw block below.

    Flaw column / block content branches on `analysis_state`:
    - `"analyzed"`: a nowrap severity row `data-testid={`severity-row-${gameId}`}` of three
      `SeverityBadge` (blunder/mistake/inaccuracy from `severity_counts`), then a `flex-wrap` chip
      row mapping `chips` to `TagChip`. (Phase tags + inaccuracy-level tags are already excluded
      upstream by Phase 106 curation.)
    - `"no_engine_analysis"`: render `NoAnalysisState` INSTEAD of the severity row + chips — never
      render "0 Blunders". `severity_counts` is null in this state; do not read it.
    Honor `noUncheckedIndexedAccess` (narrow `severity_counts` before use). Use semantic
    `<article>`/`<button>`/`<a>`; every interactive element keeps a `data-testid`. Apply BOTH the
    desktop and mobile renderers (CLAUDE.md mobile-parity rule).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `LibraryGameCard` root is `<article data-testid="library-game-card-{gameId}">` with the WDL left border
    - Analyzed cards render `severity-row-{gameId}` with `severity-blunder/mistake/inaccuracy-{gameId}` badges and `chip-{tagName}-{gameId}` chips
    - `no_engine_analysis` cards render `no-analysis-{gameId}` and NEVER "0 Blunders" / any zero-count text
    - Desktop has the 3-column body (board / info / dashed flaw column); mobile stacks header + board/info + full-width flaw block (both renderers present)
    - GameCard.tsx is NOT modified (D-05); `formatDate`/`formatTimeControl` copied, not imported across a refactor
    - `cd frontend && npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>LibraryGameCard renders the analyzed flaw column or the no-analysis state correctly on desktop and mobile, as a standalone component (D-05).</done>
</task>

<task type="auto">
  <name>Task 2: LibraryGameCardList (paginated list + matched-count row)</name>
  <files>frontend/src/components/results/LibraryGameCardList.tsx</files>
  <read_first>
    - frontend/src/components/results/GameCardList.tsx (the post-refactor file from Plan 03 — the list structure, match-count row @ ~lines 87-98, `handlePageChange` + scroll pattern @ lines 76-80, currentPage/totalPages derivation — to mirror with the library testid)
    - frontend/src/components/results/Pagination.tsx (Plan 03 — the `{ currentPage, totalPages, onPageChange }` contract)
    - frontend/src/components/results/LibraryGameCard.tsx (Task 1 — the card to map over)
    - frontend/src/types/library.ts (`LibraryGamesResponse`: games[], matched_count, offset, limit)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-UI-SPEC.md (§Interaction "Pagination" + §Copywriting "Matched count row" = "{matchedCount} of {total} games"; §Semantic HTML: list is `<section aria-label="Game results">`)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`LibraryGameCardList.tsx` — the scroll target + match-count + Pagination usage)
  </read_first>
  <action>
    Create `LibraryGameCardList` (props: `games`, `matchedCount`, `total`, `offset`, `limit`,
    `onPageChange`). Render a semantic `<section aria-label="Game results"
    data-testid="library-game-card-list">` containing: the matched-count row "{matchedCount} of
    {total} games" (`text-sm text-muted-foreground`), the list of `LibraryGameCard` (keyed by
    gameId), and the shared `<Pagination currentPage totalPages onPageChange={handlePageChange} />`.
    Derive `currentPage`/`totalPages` from offset/limit/matchedCount with named constants (page size
    20 per UI-SPEC — reuse the API default; no magic number). `handlePageChange` converts the
    Pagination page to the new offset, calls `onPageChange(offset)`, and scrolls
    `[data-testid="library-game-card-list"]` into view (`behavior: 'smooth', block: 'start'`) — note
    the LIBRARY testid, distinct from GameCardList's `game-card-list`. Handle the empty `games`
    array gracefully (GamesTab owns the full empty-state copy; the list itself just renders nothing
    or the count row).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `LibraryGameCardList` is a `<section aria-label="Game results" data-testid="library-game-card-list">` mapping games to `LibraryGameCard`
    - It renders the matched-count row "{matchedCount} of {total} games" and the shared `Pagination`
    - Page change scrolls `[data-testid="library-game-card-list"]` (library testid, not `game-card-list`)
    - Page size 20 via a named constant (no magic number); offset/page math correct
    - `cd frontend && npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>LibraryGameCardList paginates LibraryGameCard with the shared Pagination and the correct library scroll target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game metadata (usernames, opening names, platform_url) → rendered card | User-owned game data rendered as React text + an `<a href={platform_url}>`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-09 | Tampering (XSS) | usernames / opening names in the card | mitigate | Rendered as React children (escaped); no `dangerouslySetInnerHTML`. |
| T-107-10 | Tampering | `platform_url` external link | mitigate | Reuse GameCard's `target="_blank" rel="noopener noreferrer"` pattern verbatim (prevents reverse-tabnabbing); link only rendered when `platform_url` present. |
| T-107-11 | Information Disclosure | false-clean game on missing eval | mitigate | `no_engine_analysis` branch renders NoAnalysisState, never a zero-count row (acceptance criterion) — Success Criterion 1. |
| T-107-SC | Tampering | npm installs | accept | No new dependency. |

**No high-severity threats.** Escaped text, safe external-link attrs, explicit no-analysis state.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit`, `npm run lint` clean.
- `npm run knip` clean once GamesTab (Plan 07) imports both components; for this plan export only
  the two components GamesTab will consume.
</verification>

<success_criteria>
- Analyzed cards show B/M/I counts + family-colored chips; chess.com/unanalyzed show the explicit
  no-analysis state, never a false clean game (LIBG-01, Success Criterion 1).
- LibraryGameCard is a standalone component (D-05); GameCard untouched.
- LibraryGameCardList paginates via the shared Pagination with the library scroll target.
</success_criteria>

<output>
Create `.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-06-SUMMARY.md` when done.
</output>
