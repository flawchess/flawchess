---
phase: 107-games-subtab-frontend-card-archive-filters-flaw-stats-panel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/results/Pagination.tsx
  - frontend/src/components/results/GameCardList.tsx
autonomous: true
requirements: [LIBG-01]
must_haves:
  truths:
    - "A shared Pagination component renders prev / numbered / ellipsis / next controls from currentPage + totalPages"
    - "GameCardList delegates pagination rendering to the shared Pagination component"
    - "Openings and Endgames game lists behave identically to before (pagination testids, page math, scroll-to-top unchanged)"
  artifacts:
    - path: "frontend/src/components/results/Pagination.tsx"
      provides: "Shared Pagination component (getPaginationItems + prev/numbered/next controls)"
      contains: "pagination-prev"
    - path: "frontend/src/components/results/GameCardList.tsx"
      provides: "GameCardList rewired to use Pagination, behavior-preserving"
      contains: "Pagination"
  key_links:
    - from: "frontend/src/components/results/GameCardList.tsx"
      to: "frontend/src/components/results/Pagination.tsx"
      via: "import + <Pagination currentPage totalPages onPageChange />"
      pattern: "Pagination"
---

<objective>
Extract `getPaginationItems` + the prev/numbered/next pagination control row out of
`GameCardList.tsx` (D-04) into a shared, type-agnostic `Pagination` component, then rewire
`GameCardList` to consume it WITHOUT changing any observable behavior (D-06). This is the only
genuinely reusable piece of `GameCardList`; `LibraryGameCardList` (a later plan) will import the
same `Pagination`.

Purpose: D-05 rejected genericizing `GameCard`/`GameCardList` at the body level; only the
pagination is reusable. D-06 makes behavior-preservation for the existing Openings + Endgames
callers an explicit acceptance criterion — their tests must stay green.
Output: One new `Pagination.tsx` + a behavior-preserving refactor of `GameCardList.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md
@.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared Pagination component</name>
  <files>frontend/src/components/results/Pagination.tsx</files>
  <read_first>
    - frontend/src/components/results/GameCardList.tsx (read `getPaginationItems` @ lines 28-59, the `PaginationItem` type, and the entire `{totalPages > 1 && <div ...>}` prev/numbered/next control block @ lines 113-164 incl. `data-testid="pagination-prev"`, `pagination-page-{n}`, `pagination-next` and the active-page styling)
    - .planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-PATTERNS.md (§`frontend/src/components/results/LibraryGameCardList.tsx` — D-04 guidance: a `Pagination` component accepting `{ currentPage, totalPages, onPageChange }` is the cleanest fit)
    - frontend/src/components/ui/button.tsx (the `Button` variant used by the existing pagination controls, so the extracted component reuses the same variants/classes verbatim)
  </read_first>
  <action>
    Create `frontend/src/components/results/Pagination.tsx` exporting a `Pagination` component with
    props `{ currentPage: number; totalPages: number; onPageChange: (page: number) => void }`. Move
    `getPaginationItems` and the `PaginationItem` type into this file (or keep them co-located and
    exported). Render the prev / numbered-page / ellipsis / next row by copying the existing JSX
    from `GameCardList.tsx` lines 113-164 VERBATIM — same `Button` variants, same classes, same
    active-page styling, same `data-testid` values (`pagination-prev`, `pagination-page-{n}`,
    `pagination-next`), same disabled-edge behavior, same ellipsis rendering. The component calls
    `onPageChange(page)` with the 1-based page number; the parent owns offset math and scroll. Do
    NOT alter the page-window algorithm. The component renders nothing when `totalPages <= 1`
    (preserve the existing `totalPages > 1` guard — move the guard inside Pagination or keep it at
    the call site, whichever keeps GameCardList behavior identical). Add `data-testid` to any new
    interactive element introduced (there should be none beyond the moved controls). No magic
    numbers — keep the existing `<= 7` / `±2` window constants; extract them to named constants if
    they were inline literals.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c -e "pagination-prev" -e "pagination-next" src/components/results/Pagination.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/results/Pagination.tsx` exports `Pagination` with props `{ currentPage, totalPages, onPageChange }`
    - It renders `data-testid="pagination-prev"`, `pagination-page-{n}`, `pagination-next` with the same `Button` variants/classes and active-page styling as the original GameCardList block
    - The page-window algorithm (`getPaginationItems`) is unchanged (same `<= 7` / `±2` / ellipsis logic)
    - `onPageChange` receives the 1-based page number; offset/scroll math stays in the parent
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A reusable Pagination component renders the identical control row and exposes a clean page-change callback.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire GameCardList to use Pagination (behavior-preserving for Openings + Endgames)</name>
  <files>frontend/src/components/results/GameCardList.tsx</files>
  <read_first>
    - frontend/src/components/results/GameCardList.tsx (the full file — esp. `handlePageChange` @ lines 76-80 with the `[data-testid="game-card-list"]` scroll target, the `currentPage`/`totalPages` derivation @ ~line 82, and the inline pagination block @ lines 113-164 you are replacing)
    - frontend/src/components/results/Pagination.tsx (the component from Task 1 — its prop contract)
    - frontend/src/pages/__tests__/Endgames.overallPerformance.test.tsx and frontend/src/pages/__tests__/Endgames.readinessGate.test.tsx (the tests that render GameCardList indirectly via Endgames — confirm which pagination/game-card-list testids and behaviors they assert, so the refactor keeps them green per D-06)
    - frontend/src/pages/openings/GamesTab.tsx (the Openings caller of GameCardList — confirm its props/usage are unaffected)
  </read_first>
  <action>
    Replace the inline `getPaginationItems` definition and the `{totalPages > 1 && <div>...prev/
    numbered/next...</div>}` block in `GameCardList.tsx` with an import of `Pagination` from
    `./Pagination` and a single `<Pagination currentPage={currentPage} totalPages={totalPages}
    onPageChange={handlePageChange} />` call. Keep `handlePageChange` and its
    `[data-testid="game-card-list"]` scroll-to-top behavior in `GameCardList` unchanged (the
    component still owns the scroll target — Openings/Endgames keep `game-card-list`, NOT
    `library-game-card-list`). Adapt `handlePageChange` to the new callback signature: if the
    extracted `Pagination` emits a 1-based page, convert it to the existing offset the current
    handler expects so the parent contract to Openings/Endgames is unchanged. Remove the now-dead
    local `getPaginationItems`/`PaginationItem` if they were moved to Pagination.tsx (knip must stay
    clean — no dead exports left behind). Do not change the `game-card-list` root testid, the match
    count row, or any other GameCardList behavior. This is strictly behavior-preserving (D-06).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint && npm run knip && npm test -- --run src/pages/__tests__/Endgames.overallPerformance.test.tsx src/pages/__tests__/Endgames.readinessGate.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GameCardList.tsx` imports and renders `Pagination`; the inline pagination JSX + local `getPaginationItems` are removed (no dead code, `npm run knip` clean)
    - `GameCardList` root testid stays `game-card-list` and its scroll target stays `[data-testid="game-card-list"]` (NOT library)
    - `npm test -- --run` for `Endgames.overallPerformance.test.tsx` and `Endgames.readinessGate.test.tsx` both pass (D-06 behavior-preserving for Openings + Endgames callers)
    - The Openings `GamesTab.tsx` caller compiles with no prop changes required
    - `cd frontend && npx tsc --noEmit`, `npm run lint`, `npm run knip` all exit 0
  </acceptance_criteria>
  <done>GameCardList delegates pagination to the shared component with zero behavior change; the Endgames/Openings tests stay green (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a pure client-side render refactor. No data crosses a new trust boundary; no input parsing, no network call changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-04 | Tampering | behavior regression in Openings/Endgames pagination | mitigate | D-06 acceptance criterion runs the existing Endgames tests; refactor copies control JSX verbatim. Behavior-preserving by construction. |
| T-107-SC | Tampering | npm installs | accept | No new dependency added by this refactor. |

**No high-severity threats.** Client-side render extraction only; no input, network, or auth change.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit`, `npm run lint`, `npm run knip` all clean.
- The two Endgames test files pass (D-06 regression guard for the shared GameCardList).
</verification>

<success_criteria>
- A shared `Pagination` component renders the prev/numbered/next row identically.
- `GameCardList` consumes it with no observable behavior change (LIBG-01 reuse seam).
- D-06: Openings + Endgames game lists stay green; `game-card-list` testid/scroll preserved.
</success_criteria>

<output>
Create `.planning/phases/107-games-subtab-frontend-card-archive-filters-flaw-stats-panel/107-03-SUMMARY.md` when done.
</output>
