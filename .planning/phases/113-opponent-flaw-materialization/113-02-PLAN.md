---
phase: 113-opponent-flaw-materialization
plan: 02
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - app/repositories/library_repository.py
  - tests/test_library_repository.py
  - tests/test_flaw_predicate.py
autonomous: true
requirements: [FLAWX-01, FLAWX-04]
tags: [flaws, library, sqlalchemy, data-isolation]

must_haves:
  truths:
    - "Every existing game_flaws reader (the self-only Library UI) returns ONLY player flaws after gating — opponent rows do not leak into Games-tab counts, the Flaws list, the stats panel, the trend, or the cross-tab Flaw filter"
    - "Existing self-only reads return UNCHANGED counts vs the pre-phase player-only baseline (no-regression invariant — highest-risk D-04 invariant)"
    - "The ungated row count (~2x) is recoverable; gating is an explicit per-call-site WHERE clause, omittable by the future phase-115 comparison query that intentionally reads both sides"
  artifacts:
    - path: "app/repositories/library_repository.py"
      provides: "player-only gate (~is_opponent_expr / player_only_gate) on all 5 game_flaws read sites; Game JOIN added to fetch_stats_aggregates and fetch_stats_trend flaw scans"
    - path: "tests/test_library_repository.py"
      provides: "TestPlayerOnlyGate, TestPageFlawsPlayerOnly, TestStatsAggregatesPlayerOnly (no-regression baseline)"
      contains: "class TestStatsAggregatesPlayerOnly"
    - path: "tests/test_flaw_predicate.py"
      provides: "TestFlawExistsPlayerOnly (cross-tab EXISTS gate)"
      contains: "class TestFlawExistsPlayerOnly"
  key_links:
    - from: "app/repositories/library_repository.py read sites"
      to: "app/repositories/query_utils.py is_opponent_expr"
      via: "~is_opponent_expr(GameFlaw.ply, Game.user_color) in each WHERE"
      pattern: "is_opponent_expr"
    - from: "app/repositories/query_utils.py apply_game_filters"
      to: "flaw_exists_from_table (gated R1)"
      via: "delegation — gating R1 fixes R6 automatically"
      pattern: "flaw_exists_from_table"
---

<objective>
Retrofit the player-only gate (`~is_opponent_expr(...)`, i.e. mover is the player) onto EVERY existing `game_flaws` reader (D-04, the new scope that replaces voided FLAWX-03). After Plan 01 the table holds BOTH sides, so any reader that today assumes player-only rows MUST gate or opponent flaws leak into the self-only Library UI (inflated Games-tab counts, opponent blunders in the Flaws list, wrong FlawStatsPanel/trend, false cross-tab Flaw-filter hits).

Purpose: This is a data-isolation correctness boundary. The highest-risk invariant: existing self-only reads must return UNCHANGED counts after gating (no regression). RESEARCH completed the exhaustive reader audit (six sites) — this plan applies the gate at each and proves no-regression with a baseline check.

Output: All 5 `library_repository.py` read sites gated (R1-R5); the cross-tab EXISTS (R6) fixed automatically via the R1 choke point; integration tests proving player-only reads and the no-regression baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-opponent-flaw-materialization/113-CONTEXT.md
@.planning/phases/113-opponent-flaw-materialization/113-RESEARCH.md
@.planning/phases/113-opponent-flaw-materialization/113-VALIDATION.md
@.planning/phases/113-opponent-flaw-materialization/113-01-SUMMARY.md
@app/repositories/library_repository.py
@app/repositories/query_utils.py
</context>

<artifacts_produced>
New symbols this plan introduces:

- Test class `TestPlayerOnlyGate` in `tests/test_library_repository.py` (gates `query_flaws`)
- Test class `TestPageFlawsPlayerOnly` in `tests/test_library_repository.py` (gates `fetch_page_game_flaws`)
- Test class `TestStatsAggregatesPlayerOnly` in `tests/test_library_repository.py` (gates `fetch_stats_aggregates`; asserts no-regression baseline)
- Test class `TestFlawExistsPlayerOnly` in `tests/test_flaw_predicate.py` (extends the existing file; gates `flaw_exists_from_table` / cross-tab Flaw filter)

NO consumer changes outside `library_repository.py`. `query_utils.py` is NOT edited here — `apply_game_filters` delegates to `flaw_exists_from_table`, so gating R1 propagates to R6 (RESEARCH §Pattern 3 implementation note). `count_game_severities` is NOT touched (reads game_positions, has its own filter — verify and leave unchanged).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the EXISTS choke point and the two list/page readers (R1, R2, R3)</name>
  <files>app/repositories/library_repository.py, tests/test_flaw_predicate.py, tests/test_library_repository.py</files>
  <read_first>
    - app/repositories/library_repository.py lines 128-159 (`flaw_exists_from_table` — R1; the EXISTS subquery already correlates on outer `Game.id`, so `Game.user_color` is in scope for the gate)
    - app/repositories/library_repository.py lines 246-266 (`query_flaws` — R2; already JOINs `Game` at line 249, so `Game.user_color` is available)
    - app/repositories/library_repository.py lines 332-356 (`fetch_page_game_flaws` — R3; selects `GameFlaw` with NO Game JOIN — needs a JOIN or correlated subquery for `user_color`)
    - app/repositories/query_utils.py lines 110-131 (the `apply_game_filters` -> `flaw_exists_from_table` delegation path = R6; confirm gating R1 fixes R6, no edit to query_utils.py)
    - 113-RESEARCH.md §Pattern 3 (reader inventory table R1-R6), §Pitfall 3, and the `flaw_exists_from_table` gated code example
    - 113-01-SUMMARY.md (verified parity convention + whether `player_only_gate` wrapper exists)
  </read_first>
  <behavior>
    - TestFlawExistsPlayerOnly (tests/test_flaw_predicate.py): a game where ONLY the opponent has a flaw matching the severity/tag filter must NOT pass the EXISTS check; a game where the player has a matching flaw must pass. Opponent-only flaws never flag a game into the cross-tab Flaw filter.
    - TestPlayerOnlyGate (tests/test_library_repository.py, query_flaws): with both-sides flaws present, `query_flaws` returns only player FlawListItems; opponent plies are absent from the list and from the matched_count.
    - fetch_page_game_flaws (R3): returns only player GameFlaw rows per game; opponent rows excluded (covered by Task 2's TestPageFlawsPlayerOnly, but R3 is gated here).
  </behavior>
  <action>
Write `TestFlawExistsPlayerOnly` and `TestPlayerOnlyGate` FIRST (RED). Add `~is_opponent_expr(GameFlaw.ply, Game.user_color)` (or `player_only_gate(GameFlaw.ply, Game.user_color)` if Plan 01 added the wrapper) to the WHERE clause of: (R1) `flaw_exists_from_table` at lines 153-158 — this single choke point also fixes R6 via `apply_game_filters` delegation, so do NOT separately edit query_utils.py; (R2) `query_flaws` at the `.where(...)` at lines 262-266 (Game is already joined at 249); (R3) `fetch_page_game_flaws` at lines 348-351 — this select has NO Game JOIN, so add `.join(Game, Game.id == GameFlaw.game_id)` and gate, OR use a correlated scalar subquery for `Game.user_color` (RESEARCH recommends the JOIN as simpler). Import `is_opponent_expr` (and wrapper) from `app.repositories.query_utils`; watch for the existing query_utils<->library_repository import cycle (RESEARCH notes a lazy in-function import pattern already used at query_utils.py:119 — mirror it if a top-level import cycles). Do NOT add the gate to `build_flaw_filter_clauses` (RESEARCH anti-pattern: that builder is shared and the gate must stay omittable for the future phase-115 both-sides query). Add a comment at each gated site noting D-04 and the both-sides-table reason.
  </action>
  <verify>
    <automated>uv run pytest tests/test_flaw_predicate.py::TestFlawExistsPlayerOnly tests/test_library_repository.py::TestPlayerOnlyGate -x</automated>
  </verify>
  <done>R1/R2/R3 gated; cross-tab EXISTS (R6) fixed via R1 with no query_utils.py edit; opponent-only flaws never flag a game in the Flaw filter and never appear in the Flaws list; tests pass; `uv run ty check app/ tests/` zero errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the stats aggregate + trend scans with Game JOIN and prove no-regression baseline (R4, R5)</name>
  <files>app/repositories/library_repository.py, tests/test_library_repository.py</files>
  <read_first>
    - app/repositories/library_repository.py lines 455-486 (`fetch_stats_aggregates` — R4; the `func.count().filter(...)` scan with `.where(GameFlaw.user_id..., GameFlaw.game_id.in_(...))` at 468-471; NO Game JOIN currently)
    - app/repositories/library_repository.py lines 489-560 (`fetch_stats_trend` — R5; `flaw_counts_subq` at 543-553 with `.where(GameFlaw.user_id..., GameFlaw.game_id.in_(...))`; NO Game JOIN currently)
    - 113-RESEARCH.md §Pattern 3 (R4/R5 rows), §Pitfall 5 (row-count regression), the `fetch_stats_aggregates` gated code example with the JOIN-vs-correlated-subquery note, and Open Question 2
    - 113-01-SUMMARY.md
  </read_first>
  <behavior>
    - TestStatsAggregatesPlayerOnly (no-regression baseline — the highest-risk D-04 invariant): capture the per-tag aggregate counts for a fixture user BEFORE both-sides materialization (player-only). After classifying both sides and applying the gate, the gated `fetch_stats_aggregates` counts equal the pre-phase baseline (unchanged), while an ungated equivalent count is strictly greater (opponent rows present). Cover at least one white-user and one black-user game so both parity branches are exercised.
    - TestPageFlawsPlayerOnly (fetch_page_game_flaws, R3): with both-sides flaws, the per-game dict contains only player rows; opponent plies excluded.
    - fetch_stats_trend (R5): per-game M+B counts reflect only player flaws (gated), unchanged vs baseline.
  </behavior>
  <action>
Write `TestStatsAggregatesPlayerOnly` and `TestPageFlawsPlayerOnly` FIRST (RED). For (R4) `fetch_stats_aggregates`: add an inner `.join(Game, Game.id == GameFlaw.game_id)` to the `stmt` so `Game.user_color` is in scope, then add `~is_opponent_expr(GameFlaw.ply, Game.user_color)` (or `player_only_gate(...)`) to the `.where(...)` at lines 468-471. For (R5) `fetch_stats_trend`: add the same Game JOIN + gate inside the `flaw_counts_subq` at lines 543-553 so the per-game M+B counts are player-only. Use the JOIN approach (RESEARCH A2/Open-Q2: simpler than a correlated subquery, negligible perf at this scale; the existing `game_id.in_(filtered_analyzed_subq)` scoping is preserved). Keep all existing `GameFlaw.user_id == user_id` IDOR scoping (RESEARCH Security Domain — do not weaken). The no-regression test MUST assert gated == pre-phase baseline AND ungated > baseline (RESEARCH Pitfall 5). Add a D-04 comment at each gated scan. Confirm `count_game_severities` (game_positions, own filter) is untouched and still player-only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_library_repository.py::TestStatsAggregatesPlayerOnly tests/test_library_repository.py::TestPageFlawsPlayerOnly -x</automated>
  </verify>
  <done>R4/R5 gated via Game JOIN; gated aggregate/trend counts equal the pre-phase player-only baseline (no regression) while ungated counts are higher; both white-user and black-user parity branches verified; tests pass; `uv run ty check app/ tests/` zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game_flaws (both sides) -> self-only Library UI | The data-isolation correctness boundary: after Plan 01 the table holds both subjects' flaws; a missed gate site exposes an opponent's flaws inside the user's own self-only view. Same user, different subject — cross-subject exposure within the user's own games. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-03 | Information Disclosure | Missed reader gate (gate-completeness) | mitigate | RESEARCH exhaustively catalogued all 6 read sites (R1-R6); this plan gates R1-R5 and R6 is fixed via the R1 choke point. Per-site integration tests + the no-regression baseline (gated == pre-phase, ungated > baseline) prove completeness. Low severity (same user's own games, no PII/cross-user leak — `GameFlaw.user_id == user_id` IDOR scoping is fully preserved). |
| T-113-04 | Information Disclosure | Cross-tab Flaw filter EXISTS (R6) | mitigate | Gating `flaw_exists_from_table` (R1) at the single choke point automatically gates `apply_game_filters` (R6); TestFlawExistsPlayerOnly asserts opponent-only flaws never flag a game into the filter. |
| T-113-05 | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |

No `high`-severity threats. No new endpoints/input; existing IDOR scoping preserved.
</threat_model>

<verification>
- Per-task: `uv run pytest tests/test_library_repository.py tests/test_flaw_predicate.py -x` green.
- Wave merge: `uv run pytest -n auto -x` green — no regression across the existing Library/flaw suite (the no-regression invariant must hold for every existing self-only reader).
- `uv run ruff format app/ tests/ && uv run ruff check app/ tests/ --fix && uv run ty check app/ tests/` clean.
</verification>

<success_criteria>
- D-04 (replacing voided FLAWX-03): all 5 library_repository read sites gated; cross-tab EXISTS fixed via the choke point; opponent rows never leak into the self-only Library UI.
- No-regression invariant proven: gated self-only reads equal the pre-phase player-only baseline; ungated reads are strictly higher (both-sides present).
- Both parity branches (white-user, black-user) exercised by tests; `count_game_severities` confirmed unchanged.
</success_criteria>

<output>
Create `.planning/phases/113-opponent-flaw-materialization/113-02-SUMMARY.md` when done. Record: which sites got a JOIN vs correlated subquery, the no-regression baseline result, and confirmation that R6 was fixed via R1 (no query_utils.py edit).
</output>
