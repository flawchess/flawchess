---
phase: 31-endgame-classification-redesign
plan: 01
subsystem: database
tags: [postgres, sqlalchemy, alembic, endgame, migration, import-pipeline]

# Dependency graph
requires:
  - phase: 31-research
    provides: endgame classification design decisions (D-06, D-07, D-08)
  - phase: 28-engine-analysis-import
    provides: eval_cp/eval_mate columns and position classifier in game_positions
  - phase: 27-import-wiring-backfill
    provides: material_signature/piece_count columns and classify_position
provides:
  - endgame_class SmallInteger column on game_positions (nullable, 1-6)
  - EndgameClassInt IntEnum with _INT_TO_CLASS/_CLASS_TO_INT bidirectional mappings
  - Alembic migration with batched PL/pgSQL backfill for existing rows
  - Partial index ix_gp_user_endgame_class for endgame query performance
  - Import pipeline populates endgame_class for all new game positions
affects:
  - 31-02 — Plan 02 will query endgame_class column for per-position analytics

# Tech tracking
tech-stack:
  added: []
  patterns:
    - IntEnum-to-SmallInteger: EndgameClassInt IntEnum with bidirectional string/int mappings for type-safe DB storage
    - Batched PL/pgSQL backfill: 50,000-row batches via DO $$ loop in Alembic migration
    - Partial index: postgresql_where=text("endgame_class IS NOT NULL") on composite index

key-files:
  created:
    - alembic/versions/20260326_154019_b7198d53627c_add_endgame_class_column_to_game_.py
  modified:
    - app/services/endgame_service.py
    - app/models/game_position.py
    - app/services/import_service.py
    - app/repositories/game_repository.py

key-decisions:
  - "endgame_class backfill uses CASE on material_signature regex with mixed check FIRST (before queen/rook/minor) to match classify_endgame_class Python logic"
  - "chunk_size reduced 1900 -> 1700 to stay within asyncpg 32767 arg limit with 19 columns per position row"
  - "Unrelated Float(24)/REAL alter_column noise removed from autogenerated migration — semantically equivalent in PostgreSQL"

patterns-established:
  - "IntEnum pattern: EndgameClassInt maps string Literal to int for SmallInteger column; bidirectional dicts _INT_TO_CLASS/_CLASS_TO_INT for conversion"
  - "endgame_class computation lives inside the classify_position try block so failures don't abort the game"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-03-26
---

# Phase 31 Plan 01: Endgame Classification Redesign Summary

**endgame_class SmallInteger column added to game_positions with batched backfill migration, EndgameClassInt IntEnum for type-safe storage, and import pipeline wired to populate per-position classification**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-03-26T15:38:00Z
- **Completed:** 2026-03-26T15:44:01Z
- **Tasks:** 2 of 2
- **Files modified:** 4 + 1 migration created

## Accomplishments

- Added `endgame_class` SmallInteger nullable column to `game_positions` model with partial index
- Added `EndgameClassInt` IntEnum and `_INT_TO_CLASS`/`_CLASS_TO_INT` bidirectional mappings to `endgame_service.py`
- Created Alembic migration with batched PL/pgSQL backfill (50,000 rows/batch) mapping material_signature to integer class for all existing endgame positions
- Wired `endgame_class` computation into `_flush_batch()` in import pipeline using `classify_endgame_class` + `_CLASS_TO_INT`
- Updated `bulk_insert_positions` chunk_size from 1900 to 1700 for safe 19-column operation

## Task Commits

1. **Task 1: Add EndgameClassInt IntEnum, GamePosition column, Alembic migration** - `88f93df` (feat)
2. **Task 2: Wire endgame_class into import pipeline and update chunk_size** - `a4b7f09` (feat)

## Files Created/Modified

- `app/services/endgame_service.py` - Added EndgameClassInt IntEnum, _INT_TO_CLASS, _CLASS_TO_INT above classify_endgame_class
- `app/models/game_position.py` - Added endgame_class SmallInteger column, ix_gp_user_endgame_class partial index, text import
- `alembic/versions/20260326_154019_b7198d53627c_add_endgame_class_column_to_game_.py` - Migration with batched backfill and index creation
- `app/services/import_service.py` - Added module-level imports for endgame_service functions; compute endgame_class in _flush_batch
- `app/repositories/game_repository.py` - Updated chunk_size 1900->1700, updated comment to 19 columns, updated docstring

## Decisions Made

- Mixed class detection uses CASE ordering: mixed (5) checked before queen (4), rook (1), minor (2), pawn (3), pawnless (6) — mirrors classify_endgame_class Python logic where piece_families >= 2 is checked first
- Autogenerated Float(24)/REAL alter_column noise removed — semantically equivalent in PostgreSQL, following established pattern from Phase 28.1

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `endgame_class` column populated for all new imports and backfilled for existing endgame positions
- `EndgameClassInt` and `_CLASS_TO_INT` available for Plan 02 queries via `from app.services.endgame_service import EndgameClassInt, _INT_TO_CLASS, _CLASS_TO_INT`
- Partial index `ix_gp_user_endgame_class` ready for GROUP BY queries in Plan 02

---
*Phase: 31-endgame-classification-redesign*
*Completed: 2026-03-26*

## Self-Check: PASSED

All files verified present and all task commits verified in git history.
