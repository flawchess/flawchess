---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 04
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - scripts/gen_flaw_thresholds_ts.py
  - frontend/src/generated/flawThresholds.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [SC-5]
must_haves:
  truths:
    - "A Python->TS generator emits frontend/src/generated/flawThresholds.ts from flaws_service.py thresholds"
    - "The generator supports --check drift mode and exits 1 on drift"
    - "CI has a dedicated drift-gate step for flawThresholds.ts (separate from the endgameZones step)"
  artifacts:
    - path: "scripts/gen_flaw_thresholds_ts.py"
      provides: "Codegen mirroring gen_endgame_zones_ts.py, flat-scalar output, --check mode"
      contains: "--check"
    - path: "frontend/src/generated/flawThresholds.ts"
      provides: "Generated threshold scalars for the popover copy"
      contains: "WINNING_LINE_ES"
    - path: ".github/workflows/ci.yml"
      provides: "Second git diff --exit-code drift gate for flawThresholds.ts"
      contains: "gen_flaw_thresholds_ts.py"
  key_links:
    - from: "scripts/gen_flaw_thresholds_ts.py"
      to: "frontend/src/generated/flawThresholds.ts"
      via: "_render() write + --check diff"
      pattern: "flawThresholds.ts"
---

<objective>
Add the D-04 focused Python->TS threshold generator (`gen_flaw_thresholds_ts.py`) mirroring `gen_endgame_zones_ts.py`, emit the generated `frontend/src/generated/flawThresholds.ts` scalar module, and wire a SECOND CI drift-gate step (the existing step only covers `endgameZones.ts`). This module is the threshold source the Plan 05 definition popover interpolates — no hard-coded percentages in TS copy.

Purpose: SC-5 (popover thresholds sourced from constants, not hard-coded). The generated module is a hard dependency of Plan 05's `tagDefinitions.ts` rebuild.
Output: The generator script, the generated TS module, and the new CI step.
</objective>

<artifacts_this_phase_produces>
This plan introduces (consumed by Plan 05):
- New script `scripts/gen_flaw_thresholds_ts.py` (with `--check` drift mode).
- New generated module `frontend/src/generated/flawThresholds.ts` exporting flat scalars: `WINNING_LINE_ES`, `LOSING_LINE_ES`, `FROM_WINNING_ES`, `SQUANDERED_EXIT_ES`, plus the tempo constants the popover copy needs (`TIME_PRESSURE_CLOCK_FRACTION`, `TIME_PRESSURE_CLOCK_ABS_SECONDS`, `HASTY_MOVE_FRACTION`, `HASTY_MOVE_ABS_SECONDS`).
- New CI step "Flaw thresholds drift check" in `.github/workflows/ci.yml`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write gen_flaw_thresholds_ts.py + generate flawThresholds.ts</name>
  <files>scripts/gen_flaw_thresholds_ts.py, frontend/src/generated/flawThresholds.ts</files>
  <read_first>
    - scripts/gen_endgame_zones_ts.py (full read — sys.path bootstrap L1-28, authoritative-constant import L30-41, _render() single-string emit + AUTO-GENERATED header L178-314, main() with --check L317-337)
    - frontend/src/generated/endgameZones.ts (head — the 3-line AUTO-GENERATED banner + scalar export shape)
    - app/services/flaws_service.py (confirm exact constant names: WINNING_LINE_ES, LOSING_LINE_ES, FROM_WINNING_ES, SQUANDERED_EXIT_ES, TIME_PRESSURE_CLOCK_FRACTION, TIME_PRESSURE_CLOCK_ABS_SECONDS, HASTY_MOVE_FRACTION, HASTY_MOVE_ABS_SECONDS)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md (§2 + §3 — the exact skeleton to copy, flat-scalar shape, no _format_* helpers)
  </read_first>
  <action>
Create `scripts/gen_flaw_thresholds_ts.py` mirroring `gen_endgame_zones_ts.py` 1:1 in structure: the module docstring (usage incl. `--check`), the `_REPO_ROOT`/`sys.path.insert` bootstrap, the authoritative-constant import from `app.services.flaws_service` (the four ES thresholds + the four tempo constants listed above; `# noqa: E402` on the import), `_OUTPUT = _REPO_ROOT / "frontend" / "src" / "generated" / "flawThresholds.ts"`, a `_render() -> str` that returns the full TS source as one string starting with the 3-line `// AUTO-GENERATED — do not edit by hand.` / `// Source: app/services/flaws_service.py` / `// Regenerate with: uv run python scripts/gen_flaw_thresholds_ts.py` banner followed by flat `export const NAME = {value};` lines (one per imported constant). NO `_format_*` helpers — the output is flat scalars (this shape difference is why D-04 rejected merging the generators). Copy `main()` (with `--check` drift mode that exits 1 on missing/different file) verbatim, changing only the variable/path names.

Then run `uv run python scripts/gen_flaw_thresholds_ts.py` to write `frontend/src/generated/flawThresholds.ts` and commit the generated file.

Do NOT edit `scripts/gen_endgame_zones_ts.py` (D-04). Do NOT merge the two generators.
  </action>
  <verify>
    <automated>uv run python scripts/gen_flaw_thresholds_ts.py --check</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/generated/flawThresholds.ts` exists with the 3-line AUTO-GENERATED banner and `export const WINNING_LINE_ES`, `LOSING_LINE_ES`, `FROM_WINNING_ES`, `SQUANDERED_EXIT_ES` plus the four tempo constants
    - `uv run python scripts/gen_flaw_thresholds_ts.py --check` exits 0 (committed file matches render)
    - `grep -c 'gen_endgame_zones_ts' scripts/gen_flaw_thresholds_ts.py` returns 0 (independent script, no merge)
    - `git diff --exit-code frontend/src/generated/flawThresholds.ts` is clean after running the generator
  </acceptance_criteria>
  <done>Generator emits the flat-scalar threshold module, --check passes, gen_endgame_zones_ts.py untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the second CI drift-gate step for flawThresholds.ts</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the existing "Zone drift check" step ~L49-52 — the analog to mirror)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md (§"second drift gate" — the exact step to add)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
Add a new CI step immediately after the existing "Zone drift check" step, named "Flaw thresholds drift check", that runs `uv run python scripts/gen_flaw_thresholds_ts.py` then `git diff --exit-code frontend/src/generated/flawThresholds.ts`. Mirror the existing step's indentation/format exactly. Do NOT modify the existing Zone drift step (it must keep covering `endgameZones.ts` independently — the two gates are separate per D-04).
  </action>
  <verify>
    <automated>grep -n 'Flaw thresholds drift check' .github/workflows/ci.yml &amp;&amp; grep -n 'git diff --exit-code frontend/src/generated/flawThresholds.ts' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a step "Flaw thresholds drift check" running the generator + `git diff --exit-code frontend/src/generated/flawThresholds.ts`
    - the original "Zone drift check" step is unchanged and still references `endgameZones.ts`
    - `grep -c 'git diff --exit-code' .github/workflows/ci.yml` returns at least 2 (both gates present)
  </acceptance_criteria>
  <done>CI has two independent generated-file drift gates; the flaw thresholds file is now drift-protected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Build-time codegen + a generated constants file. No runtime input, auth, or data exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-04 | Tampering (config drift) | generated flawThresholds.ts vs Python source | mitigate | CI `git diff --exit-code` drift gate blocks any hand-edit/stale generated file from merging, keeping the TS popover thresholds faithful to the Python source of truth. |

No npm/pip/cargo installs in this plan — no package legitimacy gate required (uses only stdlib + existing app imports).
</threat_model>

<verification>
- `uv run python scripts/gen_flaw_thresholds_ts.py --check` exits 0.
- CI yaml has both drift gates.
</verification>

<success_criteria>
- SC-5 (enabler): the popover threshold source module exists and is drift-gated, so Plan 05 can interpolate thresholds with no hard-coded percentages.
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-04-SUMMARY.md` when done.
</output>
