---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/flaws_service.py
  - tests/services/test_flaws_service.py
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "The impact classifier emits at most one of reversed/squandered per flaw, computed only from es_before/es_after (independent of game result)"
    - "reversed fires when es_before >= 0.70 AND es_after <= 0.30; squandered fires when es_before >= 0.85 AND es_after <= 0.60 and not reversed"
    - "78%->45% carries no impact tag (deliberate gap)"
    - "Tempo tags hasty/unrushed replace impatient/considered with identical 0/1/2 code logic"
  artifacts:
    - path: "app/services/flaws_service.py"
      provides: "New constants WINNING_LINE_ES/LOSING_LINE_ES/SQUANDERED_EXIT_ES, renamed FlawTag/TempoTag Literals, _classify_impact helper, updated _build_tags ladder"
      contains: "_classify_impact"
    - path: "tests/services/test_flaws_service.py"
      provides: "TestImpactLadder boundary tests + renamed tempo tests + updated TestConstants"
      contains: "TestImpactLadder"
  key_links:
    - from: "app/services/flaws_service.py::_build_tags"
      to: "_classify_impact"
      via: "single impact-tag append (most-severe-wins)"
      pattern: "_classify_impact"
---

<objective>
Rebuild the backend flaw-tag taxonomy core in `flaws_service.py`: rename the tempo residual pair (`impatient`→`hasty`, `considered`→`unrushed`), replace the outcome-dependent impact family (`while-ahead`/`result-changing`) with the outcome-independent two-rung ladder (`reversed` / `squandered`), churn the constants, and pin every boundary with unit tests. This is the source-of-truth change every downstream plan depends on.

Purpose: SC-1 (new names in literals/constants) and SC-2 (impact ladder, boundary tests). The `FlawTag`/`TempoTag` Literals defined here are imported/mirrored by the repo writer, router, schema, and frontend types in later plans.
Output: New constants, renamed Literals, the `_classify_impact` helper, the updated `_build_tags` order, and the `TestImpactLadder` test class.
</objective>

<artifacts_this_phase_produces>
This plan introduces (consumed by later plans):
- Constants in `flaws_service.py`: `WINNING_LINE_ES = 0.70`, `LOSING_LINE_ES = 0.30`, `SQUANDERED_EXIT_ES = 0.60` (kept: `FROM_WINNING_ES = 0.85`).
- Removed constants: `RESULT_WIN_THRESHOLD`, `RESULT_DRAW_THRESHOLD`.
- Renamed `TempoTag` Literal members: `hasty`, `unrushed` (were `impatient`, `considered`).
- Renamed `FlawTag` Literal members: `reversed`, `squandered` (were `while-ahead`, `result-changing`).
- New helper: `_classify_impact(es_before, es_after) -> Literal["reversed","squandered"] | None`.
- Deleted helper: `_is_result_changing`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/notes/flaw-tag-definitions.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Churn constants + rename Literals + add _classify_impact, rebuild _build_tags ladder</name>
  <files>app/services/flaws_service.py</files>
  <read_first>
    - app/services/flaws_service.py (full read — constants block ~L54-58, FlawTag/TempoTag Literals ~L76-88, _classify_tempo ~L298-331, _is_result_changing ~L396-414, _is_unpunished ~L390, _build_tags ~L430-469, classify_game_flaws ~L498-557)
    - .planning/notes/flaw-tag-definitions.md (authoritative taxonomy — §Impact ladder L68-85, §Tempo renames L52-56, §Threshold reference L110-133)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md (§"new impact helper" L208-222 — the exact _classify_impact body and the user_result-retention rule)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§1 — dead-code map, boundary convention, Pitfall 2/4)
  </read_first>
  <action>
In the constants block: keep `FROM_WINNING_ES = 0.85` (update its trailing comment from "tag: while-ahead" to "squandered entry"). Add `WINNING_LINE_ES: float = 0.70` (reversed entry), `LOSING_LINE_ES: float = 0.30` (reversed exit), `SQUANDERED_EXIT_ES: float = 0.60` (squandered exit). Remove `RESULT_WIN_THRESHOLD` and `RESULT_DRAW_THRESHOLD` entirely.

In the Literals: in `FlawTag`, replace member `"while-ahead"` with `"reversed"` and `"result-changing"` with `"squandered"`, and replace `"impatient"`/`"considered"` with `"hasty"`/`"unrushed"`. In `TempoTag` replace `"impatient"`/`"considered"` with `"hasty"`/`"unrushed"` (order: `low-clock`, `hasty`, `unrushed`).

In `_classify_tempo`: rename only the two return string literals `"impatient"`→`"hasty"` and `"considered"`→`"unrushed"`; the 0/1/2 SmallInteger code logic is unchanged. Update its docstring lines that name the old tags.

Delete `_is_result_changing` entirely. Add a new outcome-independent helper `_classify_impact(es_before: float, es_after: float) -> Literal["reversed", "squandered"] | None` that returns `"reversed"` when `es_before >= WINNING_LINE_ES and es_after <= LOSING_LINE_ES`, else `"squandered"` when `es_before >= FROM_WINNING_ES and es_after <= SQUANDERED_EXIT_ES`, else `None`. Boundary convention is `>=` entry / `<=` exit (inclusive exit — per doc prose "or below"; this differs from the old strict `<` exit, Pitfall 4). Most-severe wins: reversed is checked first, so a 0.90->0.25 swing returns only `reversed`.

In `_build_tags`: replace the two separate `while-ahead`/`result-changing` append branches with a single call to `_classify_impact(es_before, es_after)` appending at most one tag (skip if `None`). Keep the doc-listed family order: impact (reversed/squandered) -> miss -> lucky-escape -> phase -> tempo. CRITICAL (Pitfall 2): do NOT remove the `user_result` parameter from `_build_tags` or from `classify_game_flaws` — `_is_unpunished` (lucky-escape end-of-game rule) still needs it; only the impact branch stops reading `user_result`. Update the `_build_tags` docstring order line.

No magic numbers — reference the named constants only. Add explicit return type annotations. The fix-site comment near `_classify_impact` should note it replaced the outcome-dependent `_is_result_changing` and is now result-independent.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_flaws_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'WINNING_LINE_ES\|LOSING_LINE_ES\|SQUANDERED_EXIT_ES' app/services/flaws_service.py` shows the three new constants
    - `grep -c 'RESULT_WIN_THRESHOLD\|RESULT_DRAW_THRESHOLD\|_is_result_changing' app/services/flaws_service.py` returns 0
    - `grep -E -c 'impatient|considered|while-ahead|result-changing' app/services/flaws_service.py` returns 0
    - `grep -n '_classify_impact' app/services/flaws_service.py` shows both the def and the call in _build_tags
    - `grep -n 'user_result' app/services/flaws_service.py` still shows user_result threaded into _build_tags and classify_game_flaws (NOT removed)
    - `uv run ty check app/` exits 0
  </acceptance_criteria>
  <done>Constants churned, Literals renamed, _classify_impact added with >= entry / <= exit, _build_tags appends at most one impact tag, user_result retained for lucky-escape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite/extend test_flaws_service.py — TestImpactLadder + renamed tempo + TestConstants</name>
  <files>tests/services/test_flaws_service.py</files>
  <read_first>
    - tests/services/test_flaws_service.py (full read — TestConstants ~L102-129, TestTempoTags ~L627-650, TestAttributionTags ~L750+, the while-ahead/result-changing tests at ~L762/776/963/981, the RESULT_WIN_THRESHOLD import ~L18)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§8 — exact tests to update + the required boundary cases)
    - .planning/notes/flaw-tag-definitions.md (§Impact, the 78->45 named no-impact example at L80)
  </read_first>
  <action>
Update the imports at the top of the test file: remove `RESULT_WIN_THRESHOLD`/`RESULT_DRAW_THRESHOLD`; add `WINNING_LINE_ES`, `LOSING_LINE_ES`, `SQUANDERED_EXIT_ES`.

In `TestConstants`: keep the `FROM_WINNING_ES == 0.85` assert; add asserts `WINNING_LINE_ES == 0.70`, `LOSING_LINE_ES == 0.30`, `SQUANDERED_EXIT_ES == 0.60`. Remove any asserts referencing the deleted constants.

In `TestTempoTags`: rename `test_impatient_when_fast_move...` and any `considered` test to assert the tags `"hasty"`/`"unrushed"` instead of `"impatient"`/`"considered"`. Logic unchanged — only the expected tag strings.

Delete the old `while-ahead`/`result-changing` tests (`test_while_ahead_tag_when_es_before_above_threshold`, `test_no_while_ahead_tag...`, `test_result_changing_on_loss_crossing_draw_threshold`, `test_no_result_changing_when_does_not_cross_boundary`).

Add a new `TestImpactLadder` class covering (boundary convention `>=` entry / `<=` exit):
- reversed at the boundary: es_before=0.70, es_after=0.30 -> "reversed" in tags; es_before=0.69, es_after=0.30 -> no "reversed"; es_before=0.70, es_after=0.31 -> no "reversed".
- squandered at the boundary: es_before=0.85, es_after=0.60 -> "squandered" in tags; es_before=0.85, es_after=0.61 -> no "squandered".
- most-severe-wins: es_before=0.90, es_after=0.25 -> "reversed" present and "squandered" absent.
- the deliberate no-impact gap: es_before=0.78, es_after=0.45 -> neither "reversed" nor "squandered" in tags.
- outcome-independence: assert the same es_before/es_after swing produces the same impact tag regardless of the game's `user_result` (drive through `_classify_impact` directly, or through _build_tags with two different user_result values, asserting impact membership is identical).
Follow the existing test-construction idiom in the file (use the same helper/fixture the surrounding TestAttributionTags cases use to build a flaw and read its tags).
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_flaws_service.py::TestImpactLadder tests/services/test_flaws_service.py::TestTempoTags tests/services/test_flaws_service.py::TestConstants -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_flaws_service.py -x` exits 0
    - `grep -n 'TestImpactLadder' tests/services/test_flaws_service.py` shows the new class
    - `grep -E -c 'while.ahead|result.changing|impatient|considered|RESULT_WIN_THRESHOLD|RESULT_DRAW_THRESHOLD' tests/services/test_flaws_service.py` returns 0
    - The 78->45 no-impact gap case and the outcome-independence case are present (grep for `0.78` and `0.45` shows the gap test)
  </acceptance_criteria>
  <done>All impact-ladder boundaries (70/30, 85/60, 78->45 gap, most-severe-wins, outcome-independence) and renamed tempo tests pass; no deprecated names remain in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a pure in-process classifier refactor over already-stored ES floats. No new input surface, no auth/authz path, no data exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | (none) | `_classify_impact` over stored es_before/es_after | accept | No external input; pure numeric transform over already-validated stored columns. The `FlawTag` Literal continues to bound the tag value space. Realistically empty threat surface for this plan. |

No npm/pip/cargo installs in this plan — no package legitimacy gate required.
</threat_model>

<verification>
- `uv run pytest tests/services/test_flaws_service.py -x` green.
- `uv run ty check app/ tests/` zero errors.
- `uv run ruff check app/ tests/` clean.
</verification>

<success_criteria>
- SC-1 (partial): new constant/Literal names in `flaws_service.py`; no `while-ahead`/`result-changing`/`impatient`/`considered` in the service or its tests.
- SC-2: `_classify_impact` emits at most one of reversed/squandered, outcome-independent, with the 70/30, 85/60, and 78->45-gap boundaries pinned by tests.
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-01-SUMMARY.md` when done.
</output>
