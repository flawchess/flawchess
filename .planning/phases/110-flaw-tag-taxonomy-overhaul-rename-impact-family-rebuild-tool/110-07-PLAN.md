---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 07
type: execute
wave: 5
depends_on: ["110-02", "110-03", "110-05", "110-06"]
files_modified: []
autonomous: false
requirements: [SC-1, SC-3, SC-5, SC-6]
must_haves:
  truths:
    - "No occurrence of while-ahead/while_ahead/is_while_ahead, result-changing/result_changing/is_result_changing, or the tag-context impatient/considered remains in app/ or frontend/src/ (excluding the 5 documented prose false positives)"
    - "Full backend suite + ty + frontend lint/test/knip are green"
    - "Both generated-file drift gates pass"
    - "Hover/tap popover + active-filter ring verified on desktop AND mobile on both Games and Flaws cards"
  artifacts: []
  key_links: []
---

<objective>
Final phase gate: prove the grep-clean success criterion (SC-1) with a scoped search that excludes the documented prose false positives, run the full local gate (backend suite + ty + frontend lint/test/knip + both generated-file drift checks), and confirm the visual/interaction behaviors that cannot be asserted headlessly (definition popover + active-filter ring across breakpoints on both card surfaces).

Purpose: SC-1 (grep-clean), SC-3 (full suite + ty green), SC-5 + SC-6 (popover + ring, manual UAT). This plan adds no production code — it is the verification harness for the whole phase.
Output: A clean grep gate, a green full local gate, and a signed-off manual UAT.
</objective>

<artifacts_this_phase_produces>
None (verification-only plan). No new symbols, files, or schema.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scoped grep-clean gate (SC-1) excluding the 5 prose false positives</name>
  <files></files>
  <read_first>
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§3 — the exact grep terms AND the 5 false-positive prose lines: position_classifier.py:70, stats_service.py:43, primaryTc.ts:7, EvalChart.tsx:12, FilterPanel.tsx:76; Pitfall 3)
  </read_first>
  <action>
Run the success-criterion grep across `app/` and `frontend/src/`. The unambiguous hyphenated/underscored impact names have NO legitimate prose use, so they must be exactly zero:
`grep -rn -E 'while-ahead|while_ahead|is_while_ahead|result-changing|result_changing|is_result_changing' app/ frontend/src/` must return nothing.
For the tempo names, `impatient` has no legitimate prose use either:
`grep -rn '\bimpatient\b' app/ frontend/src/` must return nothing.
`considered` is the one word with legitimate English prose (5 known sites). Search for it but confirm every remaining hit is one of the 5 documented prose lines (position_classifier.py, stats_service.py, primaryTc.ts, EvalChart.tsx, FilterPanel.tsx) and NONE is a tag literal:
`grep -rn '\bconsidered\b' app/ frontend/src/` — verify the result set is exactly the 5 prose lines.
If any tag-context occurrence remains, fix it in the owning file (it indicates a missed rename in Plans 01-06). Do NOT touch the 5 prose lines.
  </action>
  <verify>
    <automated>! grep -rn -E 'while-ahead|while_ahead|is_while_ahead|result-changing|result_changing|is_result_changing|\bimpatient\b' app/ frontend/src/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn -E 'while-ahead|while_ahead|is_while_ahead|result-changing|result_changing|is_result_changing' app/ frontend/src/` returns no matches
    - `grep -rn '\bimpatient\b' app/ frontend/src/` returns no matches
    - `grep -rn '\bconsidered\b' app/ frontend/src/` returns ONLY the 5 documented prose lines (no tag literal)
  </acceptance_criteria>
  <done>SC-1 grep-clean satisfied: zero deprecated impact/tempo tag references remain; only the 5 documented prose `considered` lines persist.</done>
</task>

<task type="auto">
  <name>Task 2: Full local gate — backend suite + ty + frontend lint/test/knip + both drift checks</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Pre-PR checklist + full-suite gate before squash-merge)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-VALIDATION.md (sampling rate, full suite command)
  </read_first>
  <action>
Run the complete local gate and resolve any failure (failures indicate a missed rename or wiring error in Plans 01-06; fix in the owning file):
- `uv run ruff format app/ tests/` (apply) and `uv run ruff check app/ tests/ --fix`
- `uv run ty check app/ tests/` (zero errors)
- `uv run pytest -n auto -x` (full backend suite; conftest template auto-refreshes on the new migration head)
- `uv run python scripts/gen_endgame_zones_ts.py --check` and `uv run python scripts/gen_flaw_thresholds_ts.py --check` (both drift gates)
- `( cd frontend && npm run lint && npm run knip && npm test -- --run )`
If ruff/format modifies files, that is acceptable here (this is the integration gate); the executor commits the result.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ &amp;&amp; uv run pytest -n auto -x &amp;&amp; uv run python scripts/gen_flaw_thresholds_ts.py --check &amp;&amp; uv run python scripts/gen_endgame_zones_ts.py --check &amp;&amp; ( cd frontend &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npm test -- --run )</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check app/ tests/` exits 0
    - `uv run pytest -n auto -x` exits 0 (full suite)
    - both `gen_*_ts.py --check` exit 0
    - `cd frontend && npm run lint && npm run knip && npm test -- --run` all exit 0
  </acceptance_criteria>
  <done>Full local gate green across backend (ty + suite), both codegen drift checks, and frontend (lint + knip + tests).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — definition popover + active-filter ring, desktop AND mobile (SC-5, SC-6)</name>
  <files></files>
  <what-built>Plans 05-06 restored the TagChip definition popover (canonical bold tag heading + definition, thresholds from constants, no navigation) and the active-filter ring that fires when a chip's tag matches an active cross-tab Flaw filter, on both Games and Flaws cards. These are visual/interaction behaviors not assertable headlessly.</what-built>
  <how-to-verify>
Prerequisite: dev DB up with users 28 & 44 backfilled (Plan 03 Task 4). Run the app (`bin/run_local.sh` or the backend + `npm run dev`).

1. Open Library -> Games subtab. Hover (desktop) and tap (narrow/mobile width) a tag chip on a game card: a popover appears reading **`tag-name`** (bold, lowercase-with-dash, e.g. **`reversed`**) `: <definition>` with concrete thresholds (e.g. "70%"/"30%") and NO navigation occurs.
2. Open Library -> Flaws subtab. Repeat the hover/tap popover check on a Flaws-card chip.
3. Set one or more active Flaw filters (FlawFilterControl). Confirm chips whose tag matches an active filter render the ring/outline emphasis on BOTH Games and Flaws cards; non-matching chips are unchanged. Confirm the impact filter buttons offer `reversed`/`squandered` (not the old names).
4. Resize to a mobile width (or use device emulation) and repeat steps 1-3 — popover and ring must work on mobile too.
5. Spot-check a game you know had a winning-to-losing blunder for user 28 or 44 shows a `reversed` (or `squandered`) chip.

This is a Manual-Only verification per 110-VALIDATION.md (visual/interaction parity across breakpoints; not assertable headlessly).
  </how-to-verify>
  <action>Run the app and perform the hover/tap popover + active-filter ring checks in how-to-verify on both Games and Flaws cards, at desktop and mobile widths. Manual because the popover open-on-hover/tap and the ring emphasis are visual/interaction behaviors not assertable in the headless test harness.</action>
  <verify>
    <human-check>On both Games and Flaws cards, desktop and mobile: a tag chip opens a popover reading bold canonical tag-name + definition (thresholds present, no navigation), and chips matching an active Flaw filter show the ring while non-matching chips do not.</human-check>
  </verify>
  <done>Definition popover (canonical bold heading + definition + interpolated thresholds, no nav) and the active-filter ring are confirmed on both card surfaces at desktop and mobile widths.</done>
  <resume-signal>Type "approved" once the popover + active-filter ring are confirmed on both card surfaces, desktop and mobile, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification-only plan. No code, no new input/auth/data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-07 | (none) | n/a | accept | No production change in this plan; it only runs gates. The phase-wide threat surface is empty (internal refactor + behavioral classifier change + internal migration; no new external input, auth path, or data exposure — security_config justification). |

No npm/pip/cargo installs in this plan.
</threat_model>

<verification>
- Scoped grep-clean returns zero deprecated tag refs (Task 1).
- Full local gate green (Task 2).
- Manual UAT signed off (Task 3).
</verification>

<success_criteria>
- SC-1: grep-clean across `app/` + `frontend/src/` (excluding the 5 documented prose lines).
- SC-3: full backend suite + ty green; both drift gates pass.
- SC-5 + SC-6: definition popover + active-filter ring confirmed on both card surfaces, desktop and mobile.
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-07-SUMMARY.md` when done.
</output>
