---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 03
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - app/schemas/library.py
  - app/repositories/library_repository.py
  - app/services/library_service.py
  - app/services/stats_service.py
  - app/routers/library.py
  - app/repositories/query_utils.py
  - tests/test_library_repository.py
  - tests/test_library_router.py
  - tests/test_flaw_predicate.py
  - tests/services/test_library_service.py
  - tests/services/test_eval_chart_service.py
  - tests/test_backfill_flaws.py
autonomous: false
requirements: [SC-1, SC-3, SC-4]
must_haves:
  truths:
    - "TagDistribution exposes reversed_rate and squandered_rate and no while_ahead_rate/result_changing_rate"
    - "EXISTS predicates and tag aggregation reference is_reversed/is_squandered and the hasty/unrushed tempo names"
    - "FlawTagFilter Literal members are renamed (reversed/squandered/hasty/unrushed)"
    - "Users 28 and 44 in the dev DB have game_flaws rows repopulated with the new impact columns"
  artifacts:
    - path: "app/schemas/library.py"
      provides: "TagDistribution with reversed_rate/squandered_rate"
      contains: "reversed_rate"
    - path: "app/repositories/library_repository.py"
      provides: "EXISTS impact-family clause + 12-tuple aggregate + _reconstruct_tags using new columns/names"
      contains: "is_reversed"
  key_links:
    - from: "app/services/library_service.py::_build_tag_distribution"
      to: "TagDistribution.reversed_rate / squandered_rate"
      via: "count/total rate computation over is_reversed/is_squandered"
      pattern: "(reversed|squandered)_rate"
---

<objective>
Propagate the impact rebuild + tempo rename through the API layer: swap the `TagDistribution` rate fields (D-03), update every EXISTS predicate / tag-aggregation / 12-tuple / chip-order / opponent-flip reference across the library repository and services, rename the `FlawTagFilter` Literal members at the HTTP boundary, fix the docstring example in `query_utils`, update all affected backend tests, then run the dev-only backfill for users 28 & 44 (D-01, manual).

Purpose: SC-1 (grep-clean across schemas/repos/services/router/tests), SC-3 (full suite green), SC-4 (28 & 44 repopulated). D-02 removes the band's headline impact stat (frontend-only, handled in Plan 06; this plan stops emitting `result_changing_rate` from the schema/service that fed it).
Output: Renamed schema rate fields, updated repos/services/router/query_utils, updated tests, and a repopulated dev DB for users 28 & 44.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- New `TagDistribution` schema fields: `reversed_rate`, `squandered_rate` (drop `while_ahead_rate`, `result_changing_rate`).
- Renamed `FlawTagFilter` Literal members in `app/routers/library.py`: `reversed`, `squandered`, `hasty`, `unrushed`.
- Repopulated `game_flaws` rows for dev users 28 & 44 (data, not a symbol).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-01-SUMMARY.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap TagDistribution rate fields + rename FlawTagFilter Literal + query_utils docstring</name>
  <files>app/schemas/library.py, app/routers/library.py, app/repositories/query_utils.py</files>
  <read_first>
    - app/schemas/library.py (TagDistribution ~L184-213 — while_ahead_rate L201/213, result_changing_rate L193/206/210; sibling miss_rate/lucky_escape_rate fields)
    - app/routers/library.py (FlawTagFilter Literal members L42/43/45, docstring L119)
    - app/repositories/query_utils.py (docstring example L51 — comment only)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§3 grep hit list, D-03)
  </read_first>
  <action>
In `app/schemas/library.py` `TagDistribution`: remove `while_ahead_rate` and `result_changing_rate` fields (and their docstring/comment lines); add `reversed_rate: float` and `squandered_rate: float` mirroring the existing `miss_rate`/`lucky_escape_rate` field style and docstrings. `miss_rate`/`lucky_escape_rate`/`phase_histogram` unchanged.

In `app/routers/library.py` `FlawTagFilter` Literal: rename members `while-ahead`->`reversed`, `result-changing`->`squandered`, `impatient`->`hasty`, `considered`->`unrushed`. Update the docstring at ~L119 that names old tags. (The FastAPI 422-rejection of unknown tags continues automatically.)

In `app/repositories/query_utils.py`: update the docstring example at ~L51 from `["low-clock", "result-changing"]` to use a current tag name (e.g. `["low-clock", "reversed"]`). Comment-only — no logic change.
  </action>
  <verify>
    <automated>uv run ty check app/ &amp;&amp; grep -E -c 'while_ahead|result_changing|while-ahead|result-changing|impatient|considered' app/schemas/library.py app/routers/library.py app/repositories/query_utils.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'reversed_rate\|squandered_rate' app/schemas/library.py` shows both new fields
    - `grep -n 'reversed\|squandered\|hasty\|unrushed' app/routers/library.py` shows the renamed Literal members
    - the verify grep returns 0 across all three files
    - `uv run ty check app/` exits 0
  </acceptance_criteria>
  <done>Schema rate fields swapped, FlawTagFilter Literal renamed, query_utils docstring updated, no deprecated names in the three files.</done>
</task>

<task type="auto">
  <name>Task 2: Update library_repository, library_service, stats_service tag aggregation + EXISTS predicates</name>
  <files>app/repositories/library_repository.py, app/services/library_service.py, app/services/stats_service.py</files>
  <read_first>
    - app/repositories/library_repository.py (EXISTS impact clause + _reconstruct_tags + 12-tuple aggregate + comments — research §3 lines: 96,97,111-118,164,181-184,394,395,425-430,443-448)
    - app/services/library_service.py (_CHIP_ORDER, _curate_chips_from_rows, _build_tag_distribution, _build_opponent_flaw_tags opponent flip docstring L213, _USER_FRAMED_TAGS L69, _TEMPO_TAGS — research §1 + §3)
    - app/services/stats_service.py (any impact/tempo rate references — research §3; note L43 "to be considered" is a FALSE POSITIVE prose, do NOT touch)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§1 opponent-flip note + §3 grep hit list; Pitfall 3 false positives)
  </read_first>
  <action>
In `library_repository.py`: rename the impact-family EXISTS predicate columns/tags to `is_reversed`/`is_squandered` (from `is_while_ahead`/`is_result_changing`), the tempo names to `hasty`/`unrushed`, and update the 12-tuple aggregate + `_reconstruct_tags` mapping + comments accordingly. The aggregate now counts `is_reversed`/`is_squandered` instead of the old impact columns.

In `library_service.py`: update `_CHIP_ORDER`, the `_build_tag_distribution` builder (compute `reversed_rate`/`squandered_rate` = count/total over the new columns, drop `while_ahead_rate`/`result_changing_rate`), `_curate_chips_from_rows`, and the 12-tuple consumer to read the new aggregate positions. Update the `_build_opponent_flaw_tags` docstring (~L213) that says "so while-ahead / result-changing are mover-relative" — impact is now outcome-independent so impact tags are perspective-independent; the function must STILL flip `user_result` for `_is_unpunished` (lucky-escape). Verify `_USER_FRAMED_TAGS` (`{"miss", "lucky-escape"}`) does NOT need the impact tags added (it should not — impact is mover-framed). Rename `_TEMPO_TAGS` members to `hasty`/`unrushed`.

In `stats_service.py`: rename any impact/tempo tag references to the new names. CRITICAL (Pitfall 3): the word "considered" at ~L43 ("to be considered") is ordinary prose, NOT the tag — do NOT touch it. Only rename genuine tag-literal occurrences.

No magic numbers; keep `Literal[...]`/`Sequence[str]` typing per CLAUDE.md.
  </action>
  <verify>
    <automated>uv run ty check app/ &amp;&amp; uv run pytest tests/test_library_repository.py tests/services/test_library_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E -c 'is_while_ahead|is_result_changing|while-ahead|result-changing|while_ahead|result_changing|impatient' app/repositories/library_repository.py app/services/library_service.py app/services/stats_service.py` returns 0
    - `grep -rn '\bconsidered\b' app/services/stats_service.py` still shows the L43 prose line untouched (false-positive preserved)
    - `grep -n 'reversed_rate\|squandered_rate' app/services/library_service.py` shows the rate computation
    - `uv run ty check app/` exits 0
  </acceptance_criteria>
  <done>Repo aggregation + service distribution/chip-order/opponent-flip use the new column/tag names; the stats_service "considered" prose false positive is untouched; ty clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update affected backend tests (library repo/router/predicate/service/eval-chart/backfill)</name>
  <files>tests/test_library_repository.py, tests/test_library_router.py, tests/test_flaw_predicate.py, tests/services/test_library_service.py, tests/services/test_eval_chart_service.py, tests/test_backfill_flaws.py</files>
  <read_first>
    - each test file (grep each for while-ahead/result-changing/while_ahead/result_changing/impatient and the tag-context `considered`)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§8 backend test list; Pitfall 3 false-positive prose lines to leave alone)
  </read_first>
  <action>
Rename all deprecated tag/column/rate references to the new names across these six test files: impact columns/tags -> `is_reversed`/`is_squandered`/`reversed`/`squandered`; rate fields -> `reversed_rate`/`squandered_rate`; tempo tags -> `hasty`/`unrushed`. Where a test sets up a flaw to trigger an impact tag, use ES values that satisfy the new ladder (reversed: es_before>=0.70, es_after<=0.30; squandered: es_before>=0.85, es_after<=0.60). Leave any non-tag prose `considered` untouched. Do not expand test coverage beyond the rename.
  </action>
  <verify>
    <automated>uv run pytest tests/test_library_repository.py tests/test_library_router.py tests/test_flaw_predicate.py tests/services/test_library_service.py tests/services/test_eval_chart_service.py tests/test_backfill_flaws.py -x</automated>
  </verify>
  <acceptance_criteria>
    - the listed test run exits 0
    - `grep -E -c 'is_while_ahead|is_result_changing|while-ahead|result-changing|while_ahead|result_changing|impatient' tests/test_library_repository.py tests/test_library_router.py tests/test_flaw_predicate.py tests/services/test_library_service.py tests/services/test_eval_chart_service.py tests/test_backfill_flaws.py` returns 0
  </acceptance_criteria>
  <done>All six backend test files use the new names and pass; no deprecated impact/tempo tag references remain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Dev-only backfill — repopulate users 28 & 44 (SC-4, mutates live dev DB)</name>
  <files></files>
  <what-built>Plans 01-03 rebuilt the classifier and migrated `game_flaws`. Existing dev rows still carry `is_reversed=false`/`is_squandered=false` (D-01: no SQL-compute). Re-running the backfill through the single classify kernel recomputes correct impact columns for the targeted users.</what-built>
  <how-to-verify>
Prerequisites: dev DB up (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`) and the migration applied (`uv run alembic upgrade head`, done in Plan 02).

1. (Optional) dry-run to confirm row counts: `uv run python scripts/backfill_flaws.py --db dev --user-id 28 --dry-run` and `--user-id 44 --dry-run`.
2. Run the real backfill:
   `uv run python scripts/backfill_flaws.py --db dev --user-id 28`
   `uv run python scripts/backfill_flaws.py --db dev --user-id 44`
3. Spot-check via the `flawchess-db` MCP query tool that `game_flaws` rows for users 28 & 44 have `is_reversed`/`is_squandered` populated (at least some `true` where the ladder applies) and that the `is_while_ahead`/`is_result_changing` columns no longer exist.

This is a Manual-Only verification: it mutates the live dev DB (not the isolated test DB) and cannot run in the test harness (project norm: no dev DB reset in plans). Other dev users are intentionally left stale (dev-only, v1.24 unshipped).
  </how-to-verify>
  <action>Run the dev-only backfill for users 28 & 44 as detailed in how-to-verify (dry-run optional, then the real run for each user-id), then spot-check the rebuilt impact columns. Manual because it mutates the live dev DB outside the isolated test harness; do NOT gate plan completion on the test suite for this task.</action>
  <verify>
    <human-check>game_flaws rows for users 28 & 44 (via the flawchess-db MCP query) show is_reversed/is_squandered populated and the is_while_ahead/is_result_changing columns absent.</human-check>
  </verify>
  <done>Users 28 & 44 have game_flaws repopulated with the new impact columns; spot-check confirms reversed/squandered values where the ladder applies.</done>
  <resume-signal>Type "approved" once users 28 & 44 are repopulated and spot-checked, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /library API (existing) | The `FlawTagFilter` Literal at the HTTP boundary already validates tag values; renaming members preserves the auto-422 on unknown tags. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Information disclosure | EXISTS predicates / aggregation over `game_flaws` | accept | User-scoping (user_id on every read/write, delete scoped to (game_id, user_id)) is preserved unchanged by this rename. No new data exposed; `TagDistribution` exposes only aggregate rates, already public to the owner. |

No npm/pip/cargo installs in this plan — no package legitimacy gate required.
</threat_model>

<verification>
- `uv run pytest -n auto` green; `uv run ty check app/ tests/` zero errors.
- Dev users 28 & 44 spot-checked with the `flawchess-db` MCP query (Task 4).
</verification>

<success_criteria>
- SC-1 (partial): grep-clean across schemas/repos/services/router/query_utils/tests for impact + tempo deprecated names (excluding the documented false-positive prose).
- SC-3 (partial): full suite + ty green after the API-layer churn.
- SC-4: users 28 & 44 repopulated with the new impact columns.
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-03-SUMMARY.md` when done.
</output>
