---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 02
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - app/models/game_flaw.py
  - app/repositories/game_flaws_repository.py
  - alembic/versions/20260607_alter_game_flaws_impact_cols.py
  - tests/test_game_flaws_model.py
  - tests/test_flaws_materialization.py
autonomous: true
requirements: [SC-1, SC-3]
must_haves:
  truths:
    - "game_flaws has is_reversed and is_squandered columns and no is_while_ahead/is_result_changing"
    - "The forward Alembic migration upgrades an existing dev DB without a reset"
    - "The FlawRecord->row writer maps reversed->is_reversed and squandered->is_squandered"
  artifacts:
    - path: "alembic/versions/20260607_alter_game_flaws_impact_cols.py"
      provides: "Forward alter migration dropping 2 impact cols, adding 2, down_revision a7e0b4796501"
      contains: "a7e0b4796501"
    - path: "app/models/game_flaw.py"
      provides: "Renamed is_reversed/is_squandered boolean columns (no server_default)"
      contains: "is_reversed"
    - path: "app/repositories/game_flaws_repository.py"
      provides: "Updated flaw_record_to_row string->column mapping"
      contains: "is_reversed"
  key_links:
    - from: "app/repositories/game_flaws_repository.py::flaw_record_to_row"
      to: "game_flaws.is_reversed / is_squandered columns"
      via: "string-in-tags boolean mapping"
      pattern: "is_(reversed|squandered)"
---

<objective>
Apply the 3-way impact-column change so the DB, model, and the FlawRecord->row writer all agree: drop `is_while_ahead`/`is_result_changing` and add `is_reversed`/`is_squandered`. This is the materialization half of the impact rebuild — the classifier (Plan 01) emits the new tag strings; this plan writes them into `game_flaws` and migrates the table forward.

Purpose: SC-3 (new columns via a forward migration that upgrades the dev DB without a reset; ty + full suite green) and SC-1 (column-name grep-clean).
Output: The new alter migration, the renamed model columns, the updated repo writer, and updated model/materialization tests.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- New columns on `game_flaws`: `is_reversed`, `is_squandered` (Boolean, NOT NULL, no server_default in the model — matching `is_miss`/`is_lucky_escape`).
- Dropped columns: `is_while_ahead`, `is_result_changing`.
- New Alembic migration file `alembic/versions/20260607_alter_game_flaws_impact_cols.py`, `down_revision = "a7e0b4796501"`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename model columns + update the FlawRecord->row writer</name>
  <files>app/models/game_flaw.py, app/repositories/game_flaws_repository.py</files>
  <read_first>
    - app/models/game_flaw.py (full read — impact cols L54-55, sibling is_miss/is_lucky_escape L50-51 carry NO server_default, tempo code comment L43)
    - app/repositories/game_flaws_repository.py (full read — flaw_record_to_row writer L100-115, the exact L109-110 to change, sibling is_miss/is_lucky_escape idiom L107-108, _TEMPO_INT map L29-30 if present)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md (§"the REAL impact-column writer" L195-206)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§2 model detail, §3 the impatient _TEMPO_INT key note)
  </read_first>
  <action>
In `app/models/game_flaw.py`: rename the `is_while_ahead` column to `is_reversed` and `is_result_changing` to `is_squandered`. Keep both as `Boolean, nullable=False` with NO `server_default` (matching the sibling `is_miss`/`is_lucky_escape` convention). Update the tempo-code comment at ~L43 if it spells out `impatient`/`considered` (the 0/1/2 codes are unchanged; only the human label names in the comment change to `hasty`/`unrushed`). Do NOT touch `is_miss`/`is_lucky_escape`/`tempo`/`es_before`/`es_after`.

In `app/repositories/game_flaws_repository.py`: change the writer line `"is_while_ahead": "while-ahead" in tags` to `"is_reversed": "reversed" in tags` and `"is_result_changing": "result-changing" in tags` to `"is_squandered": "squandered" in tags`. If a `_TEMPO_INT` map keys on the string `"impatient"`, rename that key to `"hasty"` (the integer code 1 is unchanged) and update its comment; same for any `"considered"` key -> `"unrushed"`.
  </action>
  <verify>
    <automated>uv run ty check app/ &amp;&amp; grep -E -c 'is_while_ahead|is_result_changing|while-ahead|result-changing|impatient|considered' app/models/game_flaw.py app/repositories/game_flaws_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'is_reversed\|is_squandered' app/models/game_flaw.py` shows both renamed columns
    - `grep -n 'is_reversed\|is_squandered' app/repositories/game_flaws_repository.py` shows both writer lines mapping `"reversed" in tags` / `"squandered" in tags`
    - the verify grep returns 0 (no deprecated names in either file)
    - the model columns have no `server_default` (visual confirm against sibling is_miss)
  </acceptance_criteria>
  <done>Model columns renamed (no server_default), writer maps new tag strings to new columns, no deprecated names in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Write the forward Alembic alter migration (drop 2, add 2, down_revision a7e0b4796501)</name>
  <files>alembic/versions/20260607_alter_game_flaws_impact_cols.py</files>
  <read_first>
    - alembic/versions/20260606_151439_a7e0b4796501_add_game_flaws_table.py (the sibling create migration — header literal-types-only convention, the no-server_default impact cols, current head)
    - alembic/versions/20260422_014425_24baa961e5cf_add_users_beta_enabled.py (the add-NOT-NULL-with-server_default precedent)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md (§1 — the exact upgrade()/downgrade() skeleton, lines 56-86)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§2 — head verification, add-NOT-NULL-then-drop-default rationale, Pitfall 6)
  </read_first>
  <action>
Create a new migration with `down_revision = "a7e0b4796501"` (the verified current head) and a fresh `revision` id. Follow the project's literal-`sa.*`-types-only convention (do NOT import live app constants).

`upgrade()`: `op.drop_column("game_flaws", "is_while_ahead")`, `op.drop_column("game_flaws", "is_result_changing")`, then `op.add_column("game_flaws", sa.Column("is_reversed", sa.Boolean(), nullable=False, server_default=sa.text("false")))` and the same for `is_squandered`. Then drop the transient server_default to match the sibling no-default convention: `op.alter_column("game_flaws", "is_reversed", server_default=None)` and the same for `is_squandered`. Add a comment noting the add-NOT-NULL-with-default-then-drop pattern exists so existing rows get `false` (D-01: no SQL-compute — correct values come later from the dev backfill in Plan 03).

`downgrade()`: the mirror — drop `is_squandered`/`is_reversed`, re-add `is_result_changing`/`is_while_ahead` as NOT NULL with `server_default=sa.text("false")`, then drop those server_defaults.

Do NOT edit the `20260606` create migration (Pitfall 6 — would force a dev-DB reset). Do NOT touch `is_miss`/`is_lucky_escape`/`tempo`/`es_before`/`es_after`.
  </action>
  <verify>
    <automated>uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'down_revision' alembic/versions/20260607_alter_game_flaws_impact_cols.py` shows `down_revision = "a7e0b4796501"`
    - `uv run alembic upgrade head` succeeds against the running dev DB (no reset)
    - `uv run alembic downgrade -1` then `uv run alembic upgrade head` both succeed (round-trip works)
    - `mcp__flawchess-db__query` of `information_schema.columns` for `game_flaws` shows `is_reversed`/`is_squandered` present and `is_while_ahead`/`is_result_changing` absent
    - the migration imports no app constants (literal sa.* types only)
  </acceptance_criteria>
  <done>Forward migration applies on the existing dev DB without reset, round-trips cleanly, columns swapped, down_revision is a7e0b4796501.</done>
</task>

<task type="auto">
  <name>Task 3: Update model + materialization tests for the renamed columns</name>
  <files>tests/test_game_flaws_model.py, tests/test_flaws_materialization.py</files>
  <read_first>
    - tests/test_game_flaws_model.py (grep for is_while_ahead/is_result_changing column assertions)
    - tests/test_flaws_materialization.py (grep for the impact-column write assertions + any while-ahead/result-changing tag setup)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§8 — these two files are listed among the backend tests referencing deprecated names)
  </read_first>
  <action>
Update all column/tag references in both test files: `is_while_ahead`->`is_reversed`, `is_result_changing`->`is_squandered`, and any test that constructs a flaw with the `while-ahead`/`result-changing` tag strings -> `reversed`/`squandered` (using ES values that actually trigger them per the ladder: e.g. es_before=0.90, es_after=0.20 for reversed; es_before=0.88, es_after=0.55 for squandered). In `test_flaws_materialization.py`, assert the writer maps a `reversed`-tagged flaw to `is_reversed=True` and a `squandered`-tagged flaw to `is_squandered=True`. Do NOT add coverage beyond what the renamed/rebuilt columns require.
  </action>
  <verify>
    <automated>uv run pytest tests/test_game_flaws_model.py tests/test_flaws_materialization.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_game_flaws_model.py tests/test_flaws_materialization.py -x` exits 0
    - `grep -E -c 'is_while_ahead|is_result_changing|while-ahead|result-changing' tests/test_game_flaws_model.py tests/test_flaws_materialization.py` returns 0
  </acceptance_criteria>
  <done>Both test files assert the new columns/tags and contain no deprecated names; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal schema DDL + an in-process column writer. No new external input, auth, or data exposure. The only real risk is a migration correctness failure, covered by SC-3, not a security concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-02 | Tampering (data integrity) | the alter migration on an existing dev DB | mitigate | Add-NOT-NULL-with-server_default so existing rows get a valid `false`; a working downgrade; round-trip verified (upgrade/downgrade/upgrade). No edit to the already-applied create migration (avoids checksum/reset). |

No npm/pip/cargo installs in this plan — no package legitimacy gate required.
</threat_model>

<verification>
- `uv run alembic upgrade head` applies on the dev DB without reset; downgrade round-trips.
- `uv run pytest -n auto` green (conftest template auto-refreshes on the new head).
- `uv run ty check app/ tests/` zero errors.
</verification>

<success_criteria>
- SC-3: `game_flaws` has `is_reversed`/`is_squandered` and no `is_while_ahead`/`is_result_changing`, applied by a forward migration that upgrades the existing dev DB without a reset.
- SC-1 (partial): no impact-column deprecated names in model/repo/migration/tests.
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-02-SUMMARY.md` when done.
</output>
