---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 05
type: execute
wave: 3
depends_on: ["110-04"]
files_modified:
  - frontend/src/types/library.ts
  - frontend/src/lib/theme.ts
  - frontend/src/lib/tagDefinitions.ts
  - frontend/src/components/library/TagChip.tsx
  - frontend/src/components/library/__tests__/TagChip.test.tsx
autonomous: true
requirements: [SC-1, SC-5, SC-6]
must_haves:
  truths:
    - "FlawTag/TempoTag frontend unions use reversed/squandered/hasty/unrushed; TagDistribution uses reversed_rate/squandered_rate"
    - "Hovering/tapping a tag chip opens a Radix popover reading 'tag-name'(bold): definition with thresholds from the generated constants"
    - "Chips no longer navigate to /library/flaws?tag=..."
    - "A chip whose tag matches an active useFlawFilterStore tag renders a colored ring (theme.ts constant), on both Games and Flaws cards, desktop + mobile"
    - "The retained TAG_LABELS map (consumed by FlawFilterControl) has keys matching the renamed FlawTag/TempoTag union members; tsc exits 0"
  artifacts:
    - path: "frontend/src/lib/tagDefinitions.ts"
      provides: "Rebuilt TAG_DEFINITIONS map with thresholds interpolated from @/generated/flawThresholds; retained TAG_LABELS with renamed canonical keys for FlawFilterControl"
      contains: "TAG_DEFINITIONS"
    - path: "frontend/src/components/library/TagChip.tsx"
      provides: "Radix Popover trigger chip with definition popover + active-filter ring"
      contains: "PopoverPrimitive"
    - path: "frontend/src/lib/theme.ts"
      provides: "Active-filter ring constant + renamed FAM_TEMPO_HASTY/FAM_TEMPO_UNRUSHED"
      contains: "FAM_TEMPO_HASTY"
  key_links:
    - from: "frontend/src/components/library/TagChip.tsx"
      to: "useFlawFilterStore"
      via: "flawFilter.tags.includes(tag) -> ring class"
      pattern: "useFlawFilterStore"
    - from: "frontend/src/lib/tagDefinitions.ts"
      to: "@/generated/flawThresholds"
      via: "threshold interpolation in definition prose"
      pattern: "@/generated/flawThresholds"
---

<objective>
Rebuild the tag-chip surface to match the new taxonomy: update the frontend `FlawTag`/`TempoTag` unions and `TagDistribution` rate fields, rebuild `TAG_DEFINITIONS` (thresholds interpolated from the generated module), rename the retained `TAG_LABELS` keys (still consumed by `FlawFilterControl`) to the canonical union members, restore `TagChip` from the Phase 107 Radix Popover (commit `8c5ebc81`) with the canonical lowercase-with-dash bold heading, drop the navigation, and layer the new active-filter ring (D-05) that subscribes to `useFlawFilterStore` internally so both Games and Flaws cards get it without prop drilling.

Purpose: SC-1 (frontend grep-clean for tag/rate names), SC-5 (canonical names + definition popover, no nav, thresholds from constants), SC-6 (active-filter ring on both card surfaces, desktop + mobile).
Output: Updated types, theme constants, rebuilt tagDefinitions (TAG_DEFINITIONS + renamed TAG_LABELS), restored TagChip with popover + ring, rewritten TagChip test.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- Renamed `FlawTag`/`TempoTag` union members + `reversed_rate`/`squandered_rate` in `frontend/src/types/library.ts`.
- New active-filter ring constant in `frontend/src/lib/theme.ts` (name/value/width/offset at Claude's discretion; `ring-2` + offset in family color); renamed `FAM_TEMPO_HASTY`/`FAM_TEMPO_UNRUSHED` (were `FAM_TEMPO_IMPATIENT`/`FAM_TEMPO_CONSIDERED`).
- Rebuilt `TAG_DEFINITIONS` map in `frontend/src/lib/tagDefinitions.ts`, plus the retained `TAG_LABELS` map with keys renamed to the canonical union members (consumed by `FlawFilterControl`).
- Restored Radix-Popover `TagChip` with the active-filter ring (consumed by Games + Flaws cards).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/notes/flaw-tag-definitions.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-04-SUMMARY.md
@frontend/src/generated/flawThresholds.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend types + theme constants (unions, rate fields, ring, tempo renames)</name>
  <files>frontend/src/types/library.ts, frontend/src/lib/theme.ts</files>
  <read_first>
    - frontend/src/types/library.ts (FlawTag union L14-24, TempoTag L30, TagDistribution rate fields L142-150 — result_changing_rate L144, while_ahead_rate L149)
    - frontend/src/lib/theme.ts (FAM_* family colors L45-54 — FAM_TEMPO_IMPATIENT L48, FAM_TEMPO_CONSIDERED L49; sibling FAM_* placement for the new ring constant)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§4 frontend, §3 theme renames + consumers)
  </read_first>
  <action>
In `frontend/src/types/library.ts`: in the `FlawTag` union replace `'while-ahead'`->`'reversed'`, `'result-changing'`->`'squandered'`, `'impatient'`->`'hasty'`, `'considered'`->`'unrushed'`; same for `TempoTag`. In `TagDistribution` replace `result_changing_rate`->`reversed_rate` and `while_ahead_rate`->`squandered_rate` (match the backend schema field names from Plan 03), keeping `miss_rate`/`lucky_escape_rate`/`phase_histogram`.

In `frontend/src/lib/theme.ts`: rename `FAM_TEMPO_IMPATIENT`->`FAM_TEMPO_HASTY` and `FAM_TEMPO_CONSIDERED`->`FAM_TEMPO_UNRUSHED` (values unchanged). Add a new active-filter ring constant alongside the `FAM_*` block (Claude's discretion on exact name/value — e.g. an `ACTIVE_FILTER_RING` Tailwind class string like `ring-2 ring-offset-2` combined with a family-color ring color; keep it a single exported constant, never inline per CLAUDE.md theme rule). Document it with a short comment noting it is the D-05 active-filter emphasis (ring/outline only — no fill/bold/size change).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'reversed\|squandered\|hasty\|unrushed' frontend/src/types/library.ts` shows the renamed union members
    - `grep -n 'reversed_rate\|squandered_rate' frontend/src/types/library.ts` shows both rate fields
    - `grep -n 'FAM_TEMPO_HASTY\|FAM_TEMPO_UNRUSHED' frontend/src/lib/theme.ts` shows both renamed constants
    - `grep -E -c 'impatient|considered|while-ahead|result-changing|while_ahead|result_changing|FAM_TEMPO_IMPATIENT|FAM_TEMPO_CONSIDERED' frontend/src/types/library.ts frontend/src/lib/theme.ts` returns 0
    - the new ring constant is exported from theme.ts
  </acceptance_criteria>
  <done>Frontend unions + rate fields + theme tempo constants renamed; active-filter ring constant added to theme.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild tagDefinitions.ts TAG_DEFINITIONS + rename retained TAG_LABELS keys</name>
  <files>frontend/src/lib/tagDefinitions.ts</files>
  <read_first>
    - frontend/src/lib/tagDefinitions.ts (current — module docstring L1-7 retaining TAG_LABELS for FlawFilterControl; TAG_LABELS map L17-28 keyed by the OLD union members; the header note that TAG_DEFINITIONS was removed in Phase 108)
    - frontend/src/components/filters/FlawFilterControl.tsx (the consumer — imports TAG_LABELS L13, renders TAG_LABELS[tag] at L151/L189/L227)
    - `git show 8c5ebc81:frontend/src/lib/tagDefinitions.ts` (the Phase 107 TAG_DEFINITIONS structure to restore — adapt the import + names)
    - frontend/src/generated/flawThresholds.ts (the threshold scalars to interpolate)
    - .planning/notes/flaw-tag-definitions.md (the authoritative definition prose per tag — use this text)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§4 tagDefinitions, Open Question 2 on TAG_LABELS)
  </read_first>
  <action>
Rebuild a `TAG_DEFINITIONS: Record<FlawTag, string>` map (D-07) with one definition string per tag, using the prose from `flaw-tag-definitions.md`, with all thresholds sourced from the D-04 generated module. Import the threshold scalars from `@/generated/flawThresholds` (NOT the deleted `@/lib/flawThresholds`) and interpolate every numeric threshold into the prose (e.g. `${WINNING_LINE_ES * 100}%` -> "70%") — NO hard-coded `70%`/`85%`/`60%`/`30%` string literals. Definitions needed for all current tags: `reversed`, `squandered`, `miss`, `lucky-escape`, `low-clock`, `hasty`, `unrushed`, `opening`, `middlegame`, `endgame`.

TAG_LABELS — retain and rename (do NOT remove). The premise that `FlawFilterControl` does not consume `TAG_LABELS` is FALSE: `FlawFilterControl.tsx` imports `TAG_LABELS` (L13) and renders `TAG_LABELS[tag]` as each filter button's visible label (L151/L189/L227). Per D-07 the chip popover heading uses the raw tag string, but the filter-control buttons are a separate surface where a human-readable label map legitimately stays (planner discretion). So KEEP `TAG_LABELS: Record<FlawTag, string>` and rename its keys to the canonical union members to match the renamed `FlawTag`/`TempoTag` literals: `'while-ahead'`->`'reversed'`, `'result-changing'`->`'squandered'`, `'impatient'`->`'hasty'`, `'considered'`->`'unrushed'`; the `low-clock`/`miss`/`lucky-escape`/`opening`/`middlegame`/`endgame` keys are unchanged. The `Record<FlawTag, string>` type makes this rename compiler-enforced — after Task 1 renames the union, `tsc` errors on the stale keys until they match. Update the value (display label) strings to suit (e.g. `'reversed'`, `'squandered'`, `'hasty'`, `'unrushed'` or sensible title-cased equivalents). Also update the module docstring (L1-7) so it no longer claims TagChip drives definition removal — note that TAG_DEFINITIONS is rebuilt for TagChip popovers and TAG_LABELS is retained for FlawFilterControl buttons. Do not leave a dead export (knip CI gate) — TAG_LABELS has a live consumer, so it stays.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'TAG_DEFINITIONS' frontend/src/lib/tagDefinitions.ts` shows the rebuilt map typed `Record<FlawTag, string>`
    - `grep -n '@/generated/flawThresholds' frontend/src/lib/tagDefinitions.ts` shows the threshold import
    - `grep -E -c '70%|85%|60%|30%' frontend/src/lib/tagDefinitions.ts` returns 0 (no hard-coded thresholds in copy)
    - `grep -n 'TAG_LABELS' frontend/src/lib/tagDefinitions.ts` shows the retained map, and `grep -E -c "'while-ahead'|'result-changing'|'impatient'|'considered'" frontend/src/lib/tagDefinitions.ts` returns 0 (TAG_LABELS keys renamed to the canonical FlawTag/TempoTag literals)
    - `cd frontend && npm run knip` exits 0 (no dead export; TAG_LABELS still consumed by FlawFilterControl)
    - `cd frontend && npx tsc --noEmit` exits 0 (the `Record<FlawTag, string>` keys match the renamed union)
  </acceptance_criteria>
  <done>TAG_DEFINITIONS rebuilt with interpolated thresholds and no hard-coded percentages; the retained TAG_LABELS map has its keys renamed to the canonical FlawTag/TempoTag members (compiler-enforced, tsc clean) and remains a live export for FlawFilterControl; no dead exports.</done>
</task>

<task type="auto">
  <name>Task 3: Restore TagChip Radix Popover + active-filter ring; rewrite TagChip test</name>
  <files>frontend/src/components/library/TagChip.tsx, frontend/src/components/library/__tests__/TagChip.test.tsx</files>
  <read_first>
    - frontend/src/components/library/TagChip.tsx (current navigate-trigger version — useNavigate L1, navigate calls L90/L102, the props/icon map)
    - `git show 8c5ebc81:frontend/src/components/library/TagChip.tsx` (the Phase 107 Radix Popover restore target)
    - frontend/src/hooks/useFlawFilterStore.ts (state shape {severity, tags}; selector `const [flawFilter] = useFlawFilterStore()`)
    - frontend/src/lib/theme.ts (the new active-filter ring constant from Task 1)
    - frontend/src/components/library/__tests__/TagChip.test.tsx (current nav-asserting test to rewrite)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§5 restore-is-not-clean-checkout, Pitfall 7; §4 call sites LibraryGameCard L317 / FlawsTab L117)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md (§"TagChip restore target" + Code Examples)
  </read_first>
  <action>
Restore `TagChip` from commit `8c5ebc81` as a Radix `PopoverPrimitive.Root/Trigger(asChild)/Portal/Content` (trigger = `<span role="button" tabIndex={0}>` with 100ms hover-open on desktop + tap-toggle, `side="top" sideOffset={4}`). Remove `useNavigate` and the `navigate('/library/flaws?tag=...')` calls entirely (D-06). Four adaptations vs a clean checkout: (1) import `TAG_DEFINITIONS` from the rebuilt `tagDefinitions.ts`; (2) the bold popover heading is the literal `{tag}` lowercase-with-dash string, NOT `TAG_LABELS[tag]` (Pitfall 7) — body is `<span className="font-bold">{tag}</span>: {TAG_DEFINITIONS[tag]}`; (3) the popover content may use `text-xs` (CLAUDE.md tooltip exception); (4) layer the new ring.

Active-filter ring (D-05): subscribe to `useFlawFilterStore` INTERNALLY in TagChip (`const [flawFilter] = useFlawFilterStore(); const isActive = flawFilter.tags.includes(tag);`) and apply the theme.ts ring constant to the chip when `isActive` — so both call sites (`LibraryGameCard.tsx:317` Games cards, `FlawsTab.tsx:117` Flaws cards) get the ring with NO prop drilling and no change to either call site. Ring/outline only — no size/fill/bold change. Keep `data-testid={`chip-${tag}-${gameId}`}`, a popover `data-testid={`tag-popover-${tag}-${gameId}`}`, and the `aria-label` describing the tag. Works on desktop + mobile (the hover+tap trigger covers both).

Rewrite `TagChip.test.tsx`: drop all navigation assertions (`mockNavigate`, the `?tag=` deep-link, the "Filter flaws by tag" aria); assert (a) hover/tap opens the popover and its body contains the bold tag string + a definition substring; (b) when `useFlawFilterStore` has the tag active, the chip carries the ring class, and when not active it does not. Use the renamed tag testids.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/library/__tests__/TagChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useNavigate\|/library/flaws?tag=' frontend/src/components/library/TagChip.tsx` returns 0
    - `grep -n 'PopoverPrimitive\|useFlawFilterStore\|TAG_DEFINITIONS' frontend/src/components/library/TagChip.tsx` shows all three
    - `grep -n 'font-bold' frontend/src/components/library/TagChip.tsx` shows the bold `{tag}` heading (and the body does NOT use TAG_LABELS)
    - `cd frontend && npm test -- --run src/components/library/__tests__/TagChip.test.tsx` exits 0
    - the test asserts both popover-open + ring-on-active-filter, with no navigation assertion remaining
  </acceptance_criteria>
  <done>TagChip is a Radix popover trigger again (no nav), shows the canonical bold tag heading + definition, and rings when its tag is an active filter on both card surfaces; test rewritten to match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-only React components reading already-fetched data + a client-side filter store. No new network, auth, or data exposure. Removing the navigation reduces surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | (none) | TagChip popover / ring | accept | Definition prose + thresholds are static generated constants; the filter store is client-only owner state. No untrusted input rendered (tag values are bounded by the FlawTag union). Empty practical threat surface. |

No npm/pip/cargo installs in this plan — Radix/Tailwind already present (no new packages).
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npm run knip` clean; `npm test -- --run` (touched files) green.
- Manual (Plan 07 / VALIDATION manual section): hover/tap popover + active-filter ring on desktop AND mobile widths on both Games and Flaws cards.
</verification>

<success_criteria>
- SC-1 (partial): frontend types/theme/tagDefinitions/TagChip grep-clean for deprecated tag/rate names.
- SC-5: canonical bold tag heading + definition popover with thresholds from constants; chips no longer navigate.
- SC-6 (chip side): matching chips render the ring; TagChip subscribes internally so both Games and Flaws cards get it.
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-05-SUMMARY.md` when done.
</output>
