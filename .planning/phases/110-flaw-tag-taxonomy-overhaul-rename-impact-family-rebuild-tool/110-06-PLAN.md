---
phase: 110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool
plan: 06
type: execute
wave: 4
depends_on: ["110-05"]
files_modified:
  - frontend/src/components/library/FlawStatsBand.tsx
  - frontend/src/components/library/FlawTagDistribution.tsx
  - frontend/src/components/library/FlawStatsPanel.tsx
  - frontend/src/components/filters/FlawFilterControl.tsx
  - frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx
  - frontend/src/pages/library/__tests__/FlawsTab.test.tsx
  - frontend/src/pages/library/__tests__/GamesTab.test.tsx
  - frontend/src/api/__tests__/client.test.ts
autonomous: true
requirements: [SC-1, SC-6]
must_haves:
  truths:
    - "FlawStatsBand no longer shows a headline impact stat (D-02)"
    - "FlawTagDistribution renders reversed_rate and squandered_rate RateBarRows and no while-ahead/result-changing rows (D-03)"
    - "FlawTagDistribution TempoStackedBar reads tempo['hasty']/tempo['unrushed'] and has no bare impatient/considered references"
    - "FlawFilterControl impact/tempo filter buttons use the new tag names + testids"
    - "All remaining frontend tests reference the new tag names and pass"
  artifacts:
    - path: "frontend/src/components/library/FlawTagDistribution.tsx"
      provides: "reversed/squandered RateBarRows using FAM_TEMPO_HASTY/FAM_TEMPO_UNRUSHED + impact families; TempoStackedBar reads hasty/unrushed keys"
      contains: "reversed_rate"
    - path: "frontend/src/components/filters/FlawFilterControl.tsx"
      provides: "Renamed IMPACT_TAGS/tempo tag arrays + testids"
      contains: "reversed"
  key_links:
    - from: "frontend/src/components/library/FlawStatsPanel.tsx"
      to: "FlawStatsBand (impact prop removed) + FlawTagDistribution (new rates)"
      via: "prop wiring"
      pattern: "tag_distribution"
---

<objective>
Finish the frontend taxonomy alignment in the Flaw-Stats panel and filter control: remove the headline impact stat from `FlawStatsBand` (D-02), swap the impact RateBarRows in `FlawTagDistribution` to `reversed_rate`/`squandered_rate` using the renamed `FAM_*` colors (D-03), rename the `TempoStackedBar` tempo tag-key reads to the new backend keys (`hasty`/`unrushed`), update `FlawStatsPanel`'s prop wiring, rename the `FlawFilterControl` impact/tempo tag arrays + testids, and update the remaining frontend tests (FlawFilterControl, FlawsTab, GamesTab, client).

Purpose: SC-1 (frontend grep-clean across panels/filters/tests), SC-6 (panel/filter surface consistency — the chip ring itself is delivered in Plan 05).
Output: Updated panel + filter components, updated frontend tests.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- `FlawStatsBand` with the impact stat cell removed (D-02).
- `FlawTagDistribution` with `reversed`/`squandered` RateBarRows and `hasty`/`unrushed` tempo-bar keys.
- `FlawFilterControl` with renamed impact/tempo tag arrays + testids.
(No new exported symbols — these are edits to existing components/tests.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-PATTERNS.md
@.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop FlawStatsBand impact stat (D-02) + swap FlawTagDistribution impact rows + tempo keys (D-03) + wire FlawStatsPanel</name>
  <files>frontend/src/components/library/FlawStatsBand.tsx, frontend/src/components/library/FlawTagDistribution.tsx, frontend/src/components/library/FlawStatsPanel.tsx</files>
  <read_first>
    - frontend/src/components/library/FlawStatsBand.tsx (result_changing_rate prop L61/L83, the "Result-changing" cell L124-136 with data-testid stat-cell-result-changing)
    - frontend/src/components/library/FlawTagDistribution.tsx (destructure while_ahead_rate/result_changing_rate L178; RateBarRows L233-236; TempoStackedBar tag-key reads tempo['impatient']/tempo['considered'] L87-88, impatientCount/consideredCount vars L87-89, key:/label: segment string literals L94-95, FAM_TEMPO_IMPATIENT/FAM_TEMPO_CONSIDERED usage L94-95, the TempoStackedBar docstring mentioning impatient/considered ~L162)
    - frontend/src/components/library/FlawStatsPanel.tsx (passes result_changing_rate to band L181; passes tag_distribution to distribution L193-194)
    - frontend/src/lib/theme.ts (renamed FAM_TEMPO_HASTY/FAM_TEMPO_UNRUSHED from Plan 05)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§4 — exact line refs; D-02/D-03)
  </read_first>
  <action>
`FlawStatsBand.tsx` (D-02): remove the `result_changing_rate` prop from the component's props type and signature, and delete the entire "Result-changing" stat cell (incl. its `data-testid="stat-cell-result-changing"`). The band keeps only severity/count stats — no aggregate impact number. Apply to BOTH desktop and mobile sections of the band if it has separate markup (CLAUDE.md mobile parity).

`FlawTagDistribution.tsx` (D-03): change the destructure to read `reversed_rate`/`squandered_rate` (not `while_ahead_rate`/`result_changing_rate`); replace the two impact RateBarRows ("While-ahead"/"Result-changing") with "reversed"/"squandered" rows rendering the new rates, labeled with the canonical lowercase-with-dash tag string. `miss_rate`/`lucky_escape_rate`/`phase_histogram` rows unchanged. Apply to desktop + mobile markup.

`FlawTagDistribution.tsx` — TempoStackedBar tempo-key rename (cross-plan data contract): the backend `tempo` distribution dict (Plan 03) now emits `"hasty"`/`"unrushed"` keys (renamed from `"impatient"`/`"considered"`), so `TempoStackedBar` must read the new keys or the segment counts silently fall to 0. Make ALL of these renames in `TempoStackedBar`: (1) the tag-key reads `tempo['impatient']`->`tempo['hasty']` and `tempo['considered']`->`tempo['unrushed']` (L87-88); (2) the local variables `impatientCount`->`hastyCount` and `consideredCount`->`unrushedCount` (L87-89, including their use in `measuredSum`); (3) the segment `key:` and `label:` string literals (L94-95) — `key: 'impatient'`->`key: 'hasty'`, `label: 'Impatient'`->`label: 'Hasty'` (use the canonical lowercase tag for `key`, a presentable label string for `label`), and the `considered` segment likewise to `unrushed`; (4) the `FAM_TEMPO_IMPATIENT`/`FAM_TEMPO_CONSIDERED` color references (L94-95) -> `FAM_TEMPO_HASTY`/`FAM_TEMPO_UNRUSHED` (from Plan 05's theme.ts rename) and their import line; (5) the TempoStackedBar docstring (~L162) that says "low-clock / impatient / considered" -> "low-clock / hasty / unrushed". The `low-clock` and `unmeasured` segments are unchanged.

`FlawStatsPanel.tsx`: stop passing `result_changing_rate` to the band (D-02 removed the prop); keep passing `tag_distribution` to the distribution. Verify no other removed field is still passed.

text-sm floor applies (panel is primary content, not a tooltip body). data-testid on any interactive elements touched.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E -c 'result_changing_rate|while_ahead_rate|stat-cell-result-changing' frontend/src/components/library/FlawStatsBand.tsx frontend/src/components/library/FlawTagDistribution.tsx frontend/src/components/library/FlawStatsPanel.tsx` returns 0
    - `grep -n 'reversed_rate\|squandered_rate' frontend/src/components/library/FlawTagDistribution.tsx` shows both new rate rows
    - `grep -rn 'impatient\|considered' frontend/src/components/library/FlawTagDistribution.tsx` returns no matches (catches the bare tempo-key reads, vars, segment literals, and docstring — not just the FAM_* constants)
    - `grep -E -c 'FAM_TEMPO_IMPATIENT|FAM_TEMPO_CONSIDERED' frontend/src/components/library/FlawTagDistribution.tsx` returns 0
    - `grep -n "tempo\['hasty'\]\|tempo\['unrushed'\]" frontend/src/components/library/FlawTagDistribution.tsx` shows the renamed tempo-key reads
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Band drops the impact stat, distribution shows reversed/squandered rates with renamed family colors, the tempo stacked bar reads the new hasty/unrushed backend keys (no bare impatient/considered remain), panel wiring no longer passes the removed prop; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Rename FlawFilterControl impact/tempo tag arrays + testids</name>
  <files>frontend/src/components/filters/FlawFilterControl.tsx</files>
  <read_first>
    - frontend/src/components/filters/FlawFilterControl.tsx (TAG_LABELS import L13; TIMING_TAGS = ['low-clock', 'impatient', 'considered'] L29; IMPACT_TAGS = ['result-changing', 'while-ahead'] L31; TAG_ICONS map L33-41; the three family groups render TAG_LABELS[tag] at L151/L189/L227; filter-flaw-tag-* testids derive from the tag string)
    - frontend/src/lib/tagDefinitions.ts (TAG_LABELS — Plan 05 Task 2 renames its keys to the canonical union members)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§4 FlawFilterControl, Open Question 2)
  </read_first>
  <action>
Update `IMPACT_TAGS` to `['reversed', 'squandered']` (most-severe-first ordering to match the ladder), and rename the impact `TAG_ICONS` entries accordingly. Rename the tempo tag entries in `TIMING_TAGS` and `TAG_ICONS`: `impatient`->`hasty`, `considered`->`unrushed`. The button `data-testid` values follow the existing `filter-flaw-tag-{tag}` pattern, so they become `filter-flaw-tag-reversed`/`-squandered`/`-hasty`/`-unrushed` automatically once the tag strings change — verify no testid hard-codes the old names.

Note on TAG_LABELS: this control DOES consume `TAG_LABELS` — it imports it (L13) and renders `TAG_LABELS[tag]` as each button's visible label (L151/L189/L227). Per D-07, the chip popover heading uses the raw lowercase-with-dash tag string, but the filter-control buttons are a separate surface where a human-readable label map may legitimately remain (planner discretion confirms TAG_LABELS stays here). Keep the `TAG_LABELS[tag]` button rendering as-is — do NOT switch these buttons to raw tag strings. The actual key rename of the retained `TAG_LABELS` map lives in Plan 05 Task 2 (compiler-enforced by the `FlawTag`/`TempoTag` union rename); this task only renames the tag arrays/icons that index into it. Once both land, `TAG_LABELS[tag]` resolves for every renamed tag with zero `tsc` error.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'reversed\|squandered\|hasty\|unrushed' frontend/src/components/filters/FlawFilterControl.tsx` shows the renamed tag arrays + icon keys
    - `grep -E -c 'result-changing|while-ahead|impatient|considered' frontend/src/components/filters/FlawFilterControl.tsx` returns 0
    - the `TAG_LABELS[tag]` button rendering is retained (the control still imports and uses TAG_LABELS)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>FlawFilterControl impact/tempo arrays + icons + derived testids use the new tag names; no deprecated names remain; the TAG_LABELS-based button labels are retained and resolve cleanly against the renamed map.</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining frontend tests (FlawFilterControl, FlawsTab, GamesTab, client)</name>
  <files>frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx, frontend/src/pages/library/__tests__/FlawsTab.test.tsx, frontend/src/pages/library/__tests__/GamesTab.test.tsx, frontend/src/api/__tests__/client.test.ts</files>
  <read_first>
    - frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx (filter-flaw-tag-while-ahead/-result-changing/-impatient/-considered testids L81-86, L112-127)
    - frontend/src/pages/library/__tests__/FlawsTab.test.tsx and GamesTab.test.tsx (tag arrays + any ?tag= deep-link references)
    - frontend/src/api/__tests__/client.test.ts (?tag=result-changing deep-link + tag arrays)
    - .planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-RESEARCH.md (§8 frontend tests)
  </read_first>
  <action>
Rename all deprecated tag references across these four test files: `while-ahead`->`reversed`, `result-changing`->`squandered`, `impatient`->`hasty`, `considered`->`unrushed`, and the matching `filter-flaw-tag-*` / `chip-*` testids. Since Plan 05 removed chip navigation, any test asserting a `?tag=` deep-link from a chip click must be removed or repointed at the FlawFilterControl filter path (the cross-tab filter, which still uses tags) — do NOT re-assert chip navigation. Keep coverage scope unchanged otherwise (rename, not new behavior).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/filters/__tests__/FlawFilterControl.test.tsx src/pages/library/__tests__/FlawsTab.test.tsx src/pages/library/__tests__/GamesTab.test.tsx src/api/__tests__/client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - the listed test run exits 0
    - `grep -E -c 'while-ahead|result-changing|impatient|considered' frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx frontend/src/pages/library/__tests__/FlawsTab.test.tsx frontend/src/pages/library/__tests__/GamesTab.test.tsx frontend/src/api/__tests__/client.test.ts` returns 0
    - no test asserts chip-triggered navigation to `/library/flaws?tag=`
  </acceptance_criteria>
  <done>All four frontend test files use the new tag names, contain no chip-navigation assertions, and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Display-only panel/filter components over already-fetched aggregate data + the client-side filter store. No new network, auth, or data exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-06 | (none) | FlawStatsBand / FlawTagDistribution / FlawFilterControl | accept | Renders bounded aggregate rates + tag-union filter buttons; no untrusted input. The filter tag values remain bounded by the FlawTag union (and the backend FlawTagFilter Literal 422s unknowns). Empty practical threat surface. |

No npm/pip/cargo installs in this plan — no new packages.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npm test -- --run` (touched files) green.
- `cd frontend && npm run lint && npm run knip` clean.
</verification>

<success_criteria>
- SC-1 (partial): frontend panels/filters/tests grep-clean for deprecated tag/rate names (incl. the bare tempo-bar keys in FlawTagDistribution).
- SC-6 (panel/filter side): impact distribution shows reversed/squandered; tempo bar reads hasty/unrushed; filter buttons use the new names (the active-filter chip ring is delivered by Plan 05).
</success_criteria>

<output>
Create `.planning/phases/110-flaw-tag-taxonomy-overhaul-rename-impact-family-rebuild-tool/110-06-SUMMARY.md` when done.
</output>
