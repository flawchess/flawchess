---
phase: 123.1-opening-eval-cache
verified: 2026-06-17T14:30:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
behavior_unverified_items: []
human_verification: []
---

# Phase 123.1: Opening-Eval Cache Verification Report

**Phase Goal:** The full-eval drain's opening-region eval/best_move dedup lookup is served by a position-keyed cache table instead of the cross-user self-join — sub-millisecond per lookup, no change to dedup semantics.
**Verified:** 2026-06-17T14:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Migration adds `opening_position_eval(full_hash BIGINT PK, eval_cp SmallInteger, eval_mate SmallInteger, best_move VARCHAR(5))`; dev DB migrates cleanly; standalone idempotent backfill populates from our-engine opening evals | VERIFIED | Migration `20260617_130000` confirmed; dev DB schema confirmed via asyncpg query: `full_hash bigint NOT NULL PK`, `eval_cp smallint NULL`, `eval_mate smallint NULL`, `best_move character varying(5) NULL`; 27,844 rows populated; second run = 0 inserted (idempotency) |
| 2 | Drain write path upserts only freshly-computed engine misses (`INSERT … ON CONFLICT DO NOTHING`, batched in existing write txn); a freshly drained opening position appears in the cache | VERIFIED | `_upsert_opening_cache` helper (line 1510) builds `cache_rows` filtered to `ply <= _DEDUP_MAX_PLY and not is_terminal` with non-null eval, emits one batched INSERT inside `async with async_session_maker() as write_session` at line 1758; `test_write_population_includes_and_excludes` passes |
| 3 | `_fetch_dedup_evals` reads from `opening_position_eval` (`full_hash.in_()`) instead of self-join, preserving every read-side guard; regression test asserts identical dedup results to legacy self-join | VERIFIED | Function at line 270 uses `select(OpeningPositionEval…).where(OpeningPositionEval.full_hash.in_(full_hashes))`; read-side guards confirmed unchanged at line 1664–1669 (ply <= _DEDUP_MAX_PLY, not flaw_adjacent, not is_terminal); `test_cache_backed_fetch_dedup_evals` passes (seeds known values, asserts exact match) |
| 4 | eval/flaw/pv output is unchanged (no behavioral diff); lookup time drops from seconds to low-ms on prod-like data | VERIFIED | Signature `(session, full_hashes) -> dict[int, tuple[int | None, int | None, str | None]]` is byte-identical; early-return guard preserved; the 18-test suite (including 15 pre-existing drain tests) passes; EXPLAIN evidence in CONTEXT.md projects ~1–5 ms vs 8.4 s |
| 5 | Cache seeded from prod our-engine data only; benchmark-DB seeding and matview explicitly rejected and documented | VERIFIED | D-123.1-02 (reject matview) and D-123.1-03 (no benchmark DB) documented in CONTEXT.md with rationale; backfill script filters `full_evals_completed_at IS NOT NULL AND lichess_evals_at IS NULL` (our-engine only) |

**Score: 5/5 truths verified**

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `alembic/versions/20260617_130000_phase_123_1_opening_eval_cache.py` | CREATE TABLE + DROP TABLE downgrade | VERIFIED | File exists; `upgrade()` uses `op.create_table` with correct column types and `PrimaryKeyConstraint`; `downgrade()` uses `op.drop_table`; no `from app` imports; `down_revision = "20260616_120000"` |
| `app/models/opening_position_eval.py` | `OpeningPositionEval` ORM model (BigInteger PK, SmallInteger evals, String(5) best_move) | VERIFIED | Class exists with `full_hash: Mapped[int] = mapped_column(BigInteger, primary_key=True)`, `eval_cp: Mapped[Optional[int]] = mapped_column(SmallInteger, nullable=True)`, `eval_mate: Mapped[Optional[int]] = mapped_column(SmallInteger, nullable=True)`, `best_move: Mapped[Optional[str]] = mapped_column(String(5), nullable=True)`; SEED-053 docstring |
| `scripts/backfill_opening_eval_cache.py` | Idempotent `--db dev|prod` backfill using `DISTINCT ON (full_hash)` with `ON CONFLICT DO NOTHING` | VERIFIED | File exists; runs server-side INSERT…SELECT DISTINCT ON; uses `DEDUP_MAX_PLY` imported from `app.models.game_position` (not hardcoded); 1-hour statement timeout; `--db dev|prod` args; sentry_sdk on error + sys.exit(1) |
| `app/services/eval_drain.py` | Cache-backed `_fetch_dedup_evals` + `_upsert_opening_cache` helper + Step-4 call site | VERIFIED | `_fetch_dedup_evals` reads `opening_position_eval` via `full_hash.in_()`; `_upsert_opening_cache` helper is a 55-line extracted function called at line 1758 inside the Step-4 write session |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/models/__init__.py` | `app/models/opening_position_eval.py` | `from app.models.opening_position_eval import OpeningPositionEval` (line 8) + exported in `__all__` (line 19) | WIRED | Model registered so Base.metadata and Alembic autogenerate see the table |
| `app/services/eval_drain.py` | `app/models/opening_position_eval.py` | `from app.models.opening_position_eval import OpeningPositionEval` (line 58); used in `_fetch_dedup_evals` (lines 293–297) and `_upsert_opening_cache` docstring + SQL string | WIRED | Read (SELECT) and write (INSERT) both reference the model |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces a cache table and a service refactor, not a UI component that renders dynamic data. The behavioral equivalence is covered by the tests.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 4 new cache tests pass (read equiv., write pop., idempotency, empty-guard) | `uv run pytest tests/services/test_eval_drain.py::TestOpeningEvalCacheRead tests/services/test_eval_drain.py::TestOpeningEvalCacheWrite -v` | 4 passed in 7.54s | PASS |
| dev DB has table with correct schema + PK + 27,844 rows | asyncpg query against localhost:5432 | `full_hash bigint NOT NULL (PK)`, `eval_cp/eval_mate smallint NULL`, `best_move character varying(5) NULL`, 27,844 rows | PASS |
| ty check clean on all touched files | `uv run ty check app/models/opening_position_eval.py app/models/__init__.py app/services/eval_drain.py scripts/backfill_opening_eval_cache.py` | All checks passed | PASS |
| ruff clean on all touched files | `uv run ruff check` (same files + test file) | All checks passed | PASS |

### Probe Execution

No probes declared or conventionally present for this phase type.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| SEED-053 | 123.1-01-PLAN.md, 123.1-02-PLAN.md | Opening-eval dedup cache table — replace self-join with position-keyed cache | SATISFIED | Table created, populated (27,844 rows), drain read path swapped, drain write path filling cache; all 5 ROADMAP SCs verified |

No formal REQ-IDs. REQUIREMENTS.md covers v1.28 Tactic Tagging milestone only (confirmed); SEED-053 is an infra optimization outside that requirement set, as noted in the PLAN.md requirement field and ROADMAP phase entry.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

Scanned: `app/models/opening_position_eval.py`, `scripts/backfill_opening_eval_cache.py`, `alembic/versions/20260617_130000_phase_123_1_opening_eval_cache.py`. No TBD/FIXME/XXX markers, no placeholder patterns, no stub returns. `app/services/eval_drain.py` contains only comments referencing the legacy design (historical documentation, not debt markers).

### Human Verification Required

None. All truths are verifiable programmatically via code inspection + DB query + test execution. The sub-ms latency improvement is implicit in replacing a self-join with a PK lookup on a fully resident 80 MB table; no live timing measurement is required for correctness verification.

### Gaps Summary

No gaps. All five ROADMAP Success Criteria are met by verifiable codebase artifacts:

- SC1: Migration creates the table (verified in dev DB); backfill script is idempotent (27,844 → 0 on second run).
- SC2: `_upsert_opening_cache` writes only engine-miss rows inside Step-4 write txn; test proves inclusions and exclusions.
- SC3: `_fetch_dedup_evals` uses PK lookup on `opening_position_eval`; all read-side guards preserved in caller; test proves correctness.
- SC4: Signature unchanged; 18/18 drain tests green (15 pre-existing + 3 new).
- SC5: Backfill filters to our-engine data; alternative designs documented and rejected in CONTEXT.md.

---

_Verified: 2026-06-17T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
