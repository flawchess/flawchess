---
phase: 123.1-opening-eval-cache
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/services/eval_drain.py
  - tests/services/test_eval_drain.py
autonomous: true
requirements: [SEED-053]
must_haves:
  truths:
    - "_fetch_dedup_evals returns the same dict[int, tuple[eval_cp, eval_mate, best_move]] as before, but sourced from opening_position_eval via full_hash = ANY(:hashes) — no self-join"
    - "For a seeded fixture, the cache-backed _fetch_dedup_evals returns results identical to the legacy self-join"
    - "After a full-ply drain tick on an engine game, the opening-region positions the engine freshly computed (ply <= DEDUP_MAX_PLY, not terminal, non-null eval, NOT dedup hits) are present in opening_position_eval"
    - "Dedup transplants (cache hits) are NOT re-inserted; flaw-adjacent and terminal plies are NOT inserted as opening-cache rows"
    - "eval/flaw/pv output of the drain tick is unchanged vs the self-join implementation"
  artifacts:
    - path: "app/services/eval_drain.py"
      provides: "Cache-backed _fetch_dedup_evals + batched opening-cache upsert in the Step-4 write txn"
      contains: "opening_position_eval"
  key_links:
    - from: "app/services/eval_drain.py"
      to: "app/models/opening_position_eval.py"
      via: "read (SELECT full_hash=ANY) + write (INSERT ON CONFLICT DO NOTHING) against the cache table"
      pattern: "opening_position_eval"
---

<objective>
Swap the dedup lookup onto the cache and start filling the cache from the drain — the behavior-equivalent change that delivers the speedup. Per D-123.1-05 `_fetch_dedup_evals` keeps its exact signature and ALL read-side guards; only its query body changes (cache `full_hash = ANY` instead of the self-join). Per D-123.1-04 the write path adds ONE batched `INSERT … ON CONFLICT DO NOTHING` of the freshly-computed opening-region engine misses, inside the existing Step-4 write transaction. No dedup-decision logic changes; eval/flaw/pv output must be identical.

Depends on plan 01 (table + populated cache).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/123.1-opening-eval-cache/123.1-CONTEXT.md
@.planning/phases/123.1-opening-eval-cache/123.1-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _fetch_dedup_evals body with a cache lookup (signature + return type unchanged)</name>
  <files>app/services/eval_drain.py</files>
  <read_first>
    - app/services/eval_drain.py:270-334 (`_fetch_dedup_evals` — the function to rewrite; note its return type `dict[int, tuple[int | None, int | None, str | None]]` and the empty-input early return)
    - app/services/eval_drain.py:1640-1645 (the caller `dedup_map = await _fetch_dedup_evals(load_session, dedup_hashes)` and how dedup_hashes is built — DO NOT change this; the guards stay)
    - app/models/opening_position_eval.py (the model from plan 01)
  </read_first>
  <action>
    Rewrite the BODY of `_fetch_dedup_evals` only. Keep the signature `(session: AsyncSession, full_hashes: Sequence[int]) -> dict[int, tuple[int | None, int | None, str | None]]` and the `if not full_hashes: return {}` guard. Replace the aliased self-join + `DISTINCT ON` with:

    ```python
    result = await session.execute(
        select(
            OpeningPositionEval.full_hash,
            OpeningPositionEval.eval_cp,
            OpeningPositionEval.eval_mate,
            OpeningPositionEval.best_move,
        ).where(OpeningPositionEval.full_hash.in_(full_hashes))
    )
    return {row[0]: (row[1], row[2], row[3]) for row in result.all()}
    ```

    Import `OpeningPositionEval`. Update the docstring to note it now reads the position-keyed cache (SEED-053 / D-123.1-05) and that the read-side guards (ply<=DEDUP_MAX_PLY, flaw-adjacent exclusion, terminal exclusion) remain entirely in the caller — unchanged. Remove the now-unused `aliased`/`Game`/`GamePosition` references IF and ONLY IF nothing else in the module uses them (grep first; they almost certainly are used elsewhere — do not remove shared imports).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/eval_drain.py && uv run ruff check app/services/eval_drain.py</automated>
  </verify>
  <acceptance_criteria>
    - `_fetch_dedup_evals` signature + return type byte-identical to before; only the body + docstring changed.
    - The function selects from `opening_position_eval` via `full_hash.in_(...)` and contains no self-join / `distinct`.
    - The caller at ~line 1645 is unchanged; `dedup_hashes` construction (the guards) is unchanged.
    - `uv run ty check` + `uv run ruff check` clean.
  </acceptance_criteria>
  <done>Dedup reads the cache; all read-side guards preserved in the caller; signature stable.</done>
</task>

<task type="auto">
  <name>Task 2: Batched opening-cache upsert of freshly-computed engine misses in the Step-4 write txn</name>
  <files>app/services/eval_drain.py</files>
  <read_first>
    - app/services/eval_drain.py:1648-1696 (engine_targets selection + engine_result_map build — the SOURCE of the miss set: an engine_target with ply<=DEDUP_MAX_PLY, not terminal, and a non-null eval is exactly a freshly-computed opening miss)
    - app/services/eval_drain.py:1698+ (Step-4 write session — the single transaction where all UPDATEs/markers commit; the upsert must go here so it commits atomically with the eval writes)
    - the full-ply lane's existing batched-write helper (per CLAUDE.md SEED-052 / 260616-jq1 the drain already builds batched `UPDATE … FROM (VALUES …)` writes — mirror its VALUES-construction + asyncpg CAST style for the INSERT)
    - app/models/game_position.py (DEDUP_MAX_PLY)
  </read_first>
  <action>
    In the Step-4 write session, after the eval/flaw/pv writes, build the opening-cache write set from the engine results computed THIS tick (D-123.1-04):

    ```python
    cache_rows = [
        (t.full_hash, cp, mate, bm)
        for t in engine_targets
        if t.ply <= _DEDUP_MAX_PLY and not t.is_terminal
        for (cp, mate, bm, _pv) in (engine_result_map.get(t.ply, (None, None, None, None)),)
        if cp is not None or mate is not None
    ]
    ```

    (Only engine_targets — dedup transplants are already cached and must NOT be re-inserted. Exclude terminal. Require a real eval. best_move comes from the same engine_result_map entry.) Then emit ONE batched parameterized `INSERT INTO opening_position_eval (full_hash, eval_cp, eval_mate, best_move) VALUES (…),(…),… ON CONFLICT (full_hash) DO NOTHING` (skip entirely when `cache_rows` is empty). Use the module's existing batched-write VALUES/CAST convention (asyncpg `CAST(:p AS bigint|smallint)` etc.). Wrap nothing new in try/except beyond the existing write-session error handling — a cache insert failure should not be silently swallowed but also must not corrupt the eval write (it's in the same txn; if the INSERT fails the txn rolls back and the game is re-picked, which is acceptable — add a brief comment saying so). Keep the function within nesting/LOC limits; if Step-4 is already large, extract a small `_upsert_opening_cache(session, engine_targets, engine_result_map)` helper.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ty check app/services/eval_drain.py && uv run ruff check app/services/eval_drain.py</automated>
  </verify>
  <acceptance_criteria>
    - The upsert is a SINGLE batched INSERT (not per-row) inside the Step-4 write transaction; no-op when there are no fresh opening misses.
    - The write set excludes dedup transplants, terminal donors, and null-eval holes; includes only engine_targets with ply<=DEDUP_MAX_PLY and a real eval.
    - `uv run ty check` + `uv run ruff check` clean; touched function(s) respect the CLAUDE.md nesting (≤3/4) and LOC limits (helper extracted if needed).
  </acceptance_criteria>
  <done>The drain fills the cache with freshly-computed opening misses, batched and atomic with the eval write.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — read-path equivalence, write-path population, idempotency</name>
  <files>tests/services/test_eval_drain.py</files>
  <read_first>
    - tests/services/test_eval_drain.py (existing drain test fixtures — game/position factories, async session fixture, how a drain tick is invoked in tests)
    - app/services/eval_drain.py (_fetch_dedup_evals + the new upsert) for the seams under test
  </read_first>
  <action>
    Add tests:
    1. **Read equivalence**: seed a small `game_positions` + `games` fixture with a handful of opening positions (some shared across 2 games with identical evals, one flaw-adjacent, one terminal, one ply>DEDUP_MAX_PLY), populate `opening_position_eval` via the same DISTINCT-ON INSERT the backfill uses (or call a shared helper), then assert `_fetch_dedup_evals(session, hashes)` returns a dict equal to the legacy self-join computed inline over the fixture for the SAME hash set. (Deterministic fixture → exact equality including best_move.)
    2. **Write population**: run a full-ply drain tick on a fixture engine game whose opening positions are NOT yet cached (mock the engine to return deterministic evals+best_move), then assert the freshly-computed opening-region positions (ply<=DEDUP_MAX_PLY, non-terminal, real eval) now exist in `opening_position_eval` with the engine's values, and that a dedup-transplanted position and the terminal donor are NOT inserted by this tick.
    3. **Idempotency / first-write-wins**: re-run the tick (or insert a conflicting hash with a different eval first) and assert `ON CONFLICT DO NOTHING` leaves the original cached row unchanged and inserts no duplicate.
    Use the project's per-run-DB async fixtures; keep tests within the existing eval_drain test module.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run pytest -n auto tests/services/test_eval_drain.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All three new tests pass.
    - Read-equivalence test would FAIL if `_fetch_dedup_evals` diverged from the self-join (it compares against an inline self-join over the fixture).
    - Write-population test asserts both inclusion (fresh engine misses) and exclusion (dedup transplant, terminal donor).
    - `uv run pytest -n auto tests/services/test_eval_drain.py -x` green.
  </acceptance_criteria>
  <done>Read equivalence, write population (incl. exclusions), and idempotency are covered and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal drain refactor: a read query swap + an internal cache write. No new external input, auth, or user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123.1-03 | Tampering | dedup correctness (cache vs self-join) | mitigate | Read-equivalence test compares cache-backed `_fetch_dedup_evals` against the inline self-join over a controlled fixture; cache is built from the identical DISTINCT-ON predicate (plan 01), so contents match by construction. |
| T-123.1-04 | Info disclosure | cross-user eval transplant | accept | Unchanged from today — dedup was already cross-user by design (D-116-02); evals are position-intrinsic, non-sensitive. The cache stores no user_id/game_id. |
| T-123.1-05 | DoS | extra write per drain tick | mitigate | One batched INSERT of a handful of rows on an ~80MB resident table, inside the existing txn; replaces an 8.4 s read. Insert-misses-only keeps speculative-insertion dead tuples minimal (D-123.1-04). |
| T-123.1-SC | Tampering | package installs | accept | No new packages. |
</threat_model>

<verification>
- `uv run ty check app/ tests/` + `uv run ruff check` clean for touched files.
- `uv run pytest -n auto tests/services/test_eval_drain.py -x` green (incl. the 3 new tests).
- Manual EXPLAIN spot-check (optional, dev or prod): the new `_fetch_dedup_evals` query is an index/PK lookup on opening_position_eval (low-ms), not a self-join.
- Full suite gate runs at integration time (squash-merge to main) per CLAUDE.md.
</verification>

<success_criteria>
- Dedup lookup reads opening_position_eval; signature + guards unchanged; behavior identical to the self-join (D-123.1-05).
- Drain fills the cache with freshly-computed opening misses, batched + atomic (D-123.1-04).
- Read-equivalence, write-population, and idempotency tests green.
</success_criteria>

<output>
Create `.planning/phases/123.1-opening-eval-cache/123.1-02-SUMMARY.md` when done.
</output>
