---
phase: 136-usestockfishengine-hook-wasm-setup
plan: 02
type: execute
wave: 2
depends_on: ["136-01"]
autonomous: true
requirements: [ENGINE-01, ENGINE-02, ENGINE-03, ENGINE-04, ENGINE-05]
files_modified:
  - frontend/src/hooks/uciParser.ts
  - frontend/src/hooks/useStockfishEngine.ts
  - frontend/src/hooks/__tests__/uciParser.test.ts
  - frontend/src/hooks/__tests__/useStockfishEngine.test.ts
  - frontend/src/hooks/__tests__/useStockfishEngine.integration.test.ts
user_setup: []

must_haves:
  truths:
    - "Given a position FEN, the engine reaches readyok (isReady false→true) and returns a deterministic bestmove for a mate-in-1 FEN"
    - "The hook exposes evalCp/evalMate, pvLines (top 1-2 SAN-able lines, MultiPV=2), bestmove (pvLines[0].moves[0]), and depth as plain data (rendering is Phase 137/138)"
    - "The hook re-analyzes debounced 150ms on FEN change, bounded by go movetime 1500 (primary) with go nodes 2000000 secondary valve (ENGINE-05)"
    - "The hook exposes isReady/isAnalyzing and an enabled control input; the engine can be toggled off/on (ENGINE-04)"
    - "lowerbound/upperbound info lines never update the committed eval; only bound=exact does (Pitfall 5)"
    - "score mate 0 and score mate -N (losing) parse correctly; interleaved MultiPV lines key independently (Pitfall 7)"
    - "The bestmove that follows a stop is discarded via the two-layer guard (debounce + stopPendingRef); rapid FEN changes show only the final position (Pitfall 3)"
    - "On tab hidden the hook sends stop and keeps the Worker alive; on visible it auto re-go on the current FEN (D-04)"
    - "On unmount the Worker is stopped and terminated (no leak, Pitfall 4)"
  artifacts:
    - path: "frontend/src/hooks/uciParser.ts"
      provides: "Pure UCI parser: parseInfoLine + parseBestmove; exports PvLine, ParsedInfoLine, UCIScoreBound types"
      exports: ["parseInfoLine"]
      min_lines: 40
    - path: "frontend/src/hooks/useStockfishEngine.ts"
      provides: "React hook: Worker lifecycle + UCI state machine + debounce + stop-pending guard + tab-hide pause; returns StockfishEngineState"
      exports: ["useStockfishEngine"]
      min_lines: 80
    - path: "frontend/src/hooks/__tests__/uciParser.test.ts"
      provides: "Unit tests: lowerbound/upperbound, mate 0, mate -N, interleaved MultiPV, bestmove parse"
    - path: "frontend/src/hooks/__tests__/useStockfishEngine.test.ts"
      provides: "jsdom mock-Worker tests: stop-pending discard, debounce, isReady/isAnalyzing transitions, visibility pause (also satisfies knip by importing the hook)"
    - path: "frontend/src/hooks/__tests__/useStockfishEngine.integration.test.ts"
      provides: "node-env real-WASM test: initEngine('lite-single') FEN→bestmove for a mate-in-1"
  key_links:
    - from: "frontend/src/hooks/useStockfishEngine.ts"
      to: "frontend/public/engine/stockfish-18-lite-single.js"
      via: "new Worker('/engine/stockfish-18-lite-single.js') classic Worker"
      pattern: "new Worker\\(.*engine/stockfish-18-lite-single"
    - from: "frontend/src/hooks/useStockfishEngine.ts"
      to: "frontend/src/hooks/uciParser.ts"
      via: "imports parseInfoLine to parse worker.onmessage lines"
      pattern: "parseInfoLine"
    - from: "frontend/src/hooks/__tests__/useStockfishEngine.integration.test.ts"
      to: "stockfish (npm)"
      via: "dynamic import('stockfish') default → initEngine('lite-single')"
      pattern: "import\\(['\"]stockfish['\"]\\)"
---

<objective>
Build `useStockfishEngine` — the React hook that wraps the vendored single-thread WASM Stockfish in a Web Worker and exposes a UCI state machine as plain data — plus the pure UCI parser and its tests, the hook's mock-Worker unit tests, and the one real-WASM integration test. This delivers ENGINE-01..05.

**SCOPE — this phase ships NO user-visible UI.** ENGINE-01 (eval bar), ENGINE-02 (candidate lines), ENGINE-03 (best-move arrow), and ENGINE-04 (toggle UI) are written as user-visible outcomes, but their RENDERING ships in Phases 137/138. Phase 136 delivers only the hook DATA contract those requirements consume:
- ENGINE-01 → `evalCp: number | null`, `evalMate: number | null`
- ENGINE-02 → `pvLines: PvLine[]` (MultiPV=2, each with moves + depth)
- ENGINE-03 → `pvLines[0].moves[0]` (the UCI move behind the future arrow)
- ENGINE-04 → `isReady`/`isAnalyzing` + an `enabled` control input (toggle wiring; no toggle UI)
- ENGINE-05 → 150ms debounce + `go movetime 1500 nodes 2000000` + `stopPendingRef` guard

Do NOT create any route, component, dev harness, or temporary toggle on existing boards. Do NOT modify `useChessGame.ts`.

Purpose: The engine data source for Phases 137 (display components) and 138 (`/analysis` route).
Output: `uciParser.ts`, `useStockfishEngine.ts`, and three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-usestockfishengine-hook-wasm-setup/136-CONTEXT.md
@.planning/phases/136-usestockfishengine-hook-wasm-setup/136-RESEARCH.md
@.planning/phases/136-usestockfishengine-hook-wasm-setup/136-PATTERNS.md
@frontend/src/hooks/useTacticLine.ts
@frontend/src/hooks/useDebounce.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 portion)

NEW symbols:
- `useStockfishEngine(options: UseStockfishEngineOptions): StockfishEngineState` — the hook
- `UseStockfishEngineOptions { fen: string | null; enabled: boolean }`
- `StockfishEngineState { evalCp: number | null; evalMate: number | null; pvLines: PvLine[]; depth: number; isAnalyzing: boolean; isReady: boolean }`
- `parseInfoLine(line: string): ParsedInfoLine | null` (and a bestmove parse helper)
- exported types: `PvLine`, `ParsedInfoLine`, `UCIScoreBound`
- named constants (no magic numbers): `MOVETIME_MS = 1500`, `MAX_NODES = 2000000`, `DEBOUNCE_MS = 150`, `MULTIPV = 2`, `ENGINE_PATH = '/engine/stockfish-18-lite-single.js'`

NEW files:
- `frontend/src/hooks/uciParser.ts`
- `frontend/src/hooks/useStockfishEngine.ts`
- `frontend/src/hooks/__tests__/uciParser.test.ts`
- `frontend/src/hooks/__tests__/useStockfishEngine.test.ts`
- `frontend/src/hooks/__tests__/useStockfishEngine.integration.test.ts`

CONSUMES from Plan 01: `stockfish@18.0.8` (integration test) + `/engine/stockfish-18-lite-single.js` (production Worker path).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure UCI parser + unit tests</name>
  <files>frontend/src/hooks/uciParser.ts, frontend/src/hooks/__tests__/uciParser.test.ts</files>
  <read_first>
    - .planning/phases/136-usestockfishengine-hook-wasm-setup/136-RESEARCH.md § "Pattern 2: UCI Parser", § "Pitfall 7", § "UCI Parser Unit Test Inputs"
    - .planning/phases/136-usestockfishengine-hook-wasm-setup/136-PATTERNS.md § "uciParser.test.ts" (test structure + the 9 input strings) and § "noUncheckedIndexedAccess narrowing"
    - frontend/src/hooks/__tests__/useScrollDirection.test.ts (test-file conventions)
  </read_first>
  <behavior>
    - parseInfoLine returns null for any line not starting with the info prefix.
    - An exact `score cp` line returns scoreCp set, scoreMate null, bound exact.
    - A `lowerbound` line and an `upperbound` line both return a NON-exact bound (so the hook will not commit the eval).
    - `score mate 1` → scoreMate 1, scoreCp null; `score mate 0` → scoreMate 0 (terminal); `score mate -3` → scoreMate -3 (side-to-move losing).
    - A `multipv 2` line returns multipv 2; two interleaved lines (multipv 1 and 2 at the same depth) parse independently and preserve their own pv move arrays.
    - The bestmove parse helper extracts the move token from a bestmove line.
  </behavior>
  <action>
    Create `frontend/src/hooks/uciParser.ts` as a pure module (no React, no Worker) exporting `parseInfoLine(line)`, a bestmove parse helper, and the types `PvLine`, `ParsedInfoLine`, `UCIScoreBound` (use the interface shapes from RESEARCH.md Pattern 2). Parse by tokenizing on whitespace and scanning for the `depth`, `multipv`, `score cp|mate`, `lowerbound`/`upperbound`, and `pv` keywords; the bound is exact unless a lowerbound/upperbound token is present. Honor `noUncheckedIndexedAccess`: assign each `tokens[i]` to a const and check `=== undefined` (or `!` only where provably in bounds) before parsing to number — never `// @ts-ignore`. Extract magic numbers as named constants where they appear. Write `frontend/src/hooks/__tests__/uciParser.test.ts` with `// @vitest-environment node` (pure function, no DOM) covering every behavior above, using the exact UCI input strings from PATTERNS.md/RESEARCH.md. The parser must be imported by this test (knip: no dead exports). Do not place fenced code or the literal acceptance strings as comments in the source.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/uciParser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/hooks/__tests__/uciParser.test.ts` passes.
    - Tests assert lowerbound AND upperbound both yield a non-exact bound; mate 0, mate 1, mate -3 each parse to the correct signed scoreMate; multipv 1 and 2 parse independently.
    - `npm run lint` passes (no `noUncheckedIndexedAccess` violations, no `// @ts-ignore`).
    - `parseInfoLine` is imported by the test file (knip-clean).
  </acceptance_criteria>
  <done>Pure UCI parser with full edge-case unit coverage (bounds, mate signs, MultiPV interleaving).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useStockfishEngine hook + mock-Worker unit tests</name>
  <files>frontend/src/hooks/useStockfishEngine.ts, frontend/src/hooks/__tests__/useStockfishEngine.test.ts</files>
  <read_first>
    - .planning/phases/136-usestockfishengine-hook-wasm-setup/136-RESEARCH.md § "Pattern 1: UCI State Machine", § "Pattern 3: Worker Lifecycle", § "Pattern 4: Tab-Hide Pause", § "Anti-Patterns", Pitfalls 3/4/5/7
    - .planning/phases/136-usestockfishengine-hook-wasm-setup/136-PATTERNS.md § "useStockfishEngine.ts" (imports, interface, ref-for-latest-value, event teardown, worker lifecycle, debounce reuse)
    - frontend/src/hooks/useTacticLine.ts (hook conventions: ref-for-latest-value, listener teardown)
    - frontend/src/hooks/useDebounce.ts (reuse for the 150ms debounce)
  </read_first>
  <behavior>
    - After mount, the hook runs the UCI init sequence (uci → wait uciok → setoption MultiPV=2 → isready → wait readyok) and flips isReady false→true.
    - On a FEN change while idle, after the 150ms debounce it sends position + go movetime 1500 nodes 2000000 and sets isAnalyzing true.
    - On a FEN change while thinking, it sends stop, sets stopPendingRef, transitions to stopping; the bestmove that follows is DISCARDED (not committed to pvLines) and the queued FEN is then analyzed — rapid FEN changes commit only the final position's result.
    - lowerbound/upperbound info lines do not change the committed evalCp; only exact lines do.
    - On visibilitychange hidden → sends stop, keeps Worker alive; on visible → auto re-go on currentFen (D-04).
    - On unmount → sends stop + worker.terminate(), nulls the ref (no leak).
    - When enabled is false, no Worker analysis runs / the engine is idle.
  </behavior>
  <action>
    Create `frontend/src/hooks/useStockfishEngine.ts` implementing the hook per RESEARCH Patterns 1–4. Input `UseStockfishEngineOptions { fen: string | null; enabled: boolean }`; return `StockfishEngineState`. Use a classic Worker via `new Worker(ENGINE_PATH)` — NO `{ type: 'module' }` (Emscripten output). Internal state machine `idle | thinking | stopping` via a `stateRef`; `stopPendingRef`, `currentFenRef`, `isReadyRef` as refs synced each render (ref-for-latest-value pattern from useTacticLine). Two-layer stale guard: Layer A = `useDebounce(fen, DEBOUNCE_MS)`; Layer B = on stop set `stopPendingRef`, and on the next bestmove if stopPendingRef is set, clear it, return to idle, discard the result, and re-analyze the queued currentFen. Parse worker lines with `parseInfoLine` from uciParser; maintain a `Map<number, PvLine>` keyed by multipv, update only on exact info lines, snapshot to `pvLines` state on a (non-discarded) bestmove. Commit evalCp/evalMate only on exact bound. Search command: `go movetime ${MOVETIME_MS} nodes ${MAX_NODES}` (movetime primary per ROADMAP SC#2 = 1500; nodes 2000000 secondary valve). Add the `visibilitychange` listener (stop on hidden keeping the Worker alive; auto re-go on visible) and the unmount cleanup (stop + terminate + null ref). Define all constants (`MOVETIME_MS`, `MAX_NODES`, `DEBOUNCE_MS`, `MULTIPV`, `ENGINE_PATH`) — no magic numbers. Keep functions shallow (nesting ≤3); extract the line handler if the hook body gets deep. Write `frontend/src/hooks/__tests__/useStockfishEngine.test.ts` with `// @vitest-environment jsdom`, stubbing `globalThis.Worker` with a controllable mock (capture postMessage calls, manually fire onmessage with synthetic UCI lines) so the test runs with no real WASM: assert isReady transition, debounce coalescing, the stop-pending DISCARD path, exact-vs-bound eval commit, and the visibility stop/resume. This test imports the hook (knip: the hook export must be consumed). Use `renderHook`/`act` from the project's testing-library setup (match existing hook tests).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useStockfishEngine.test.ts && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/hooks/__tests__/useStockfishEngine.test.ts` passes.
    - Test asserts: isReady false→true after the mocked readyok; a stop-pending bestmove is discarded (pvLines reflects only the final FEN); lowerbound/upperbound do not change committed evalCp; visibility hidden sends stop without terminate; unmount terminates the Worker.
    - The search command string contains `movetime 1500` and `nodes 2000000`; MultiPV setoption uses value 2.
    - Worker is instantiated as a classic Worker (no `{ type: 'module' }`).
    - `npx tsc -b` and `npm run lint` pass; `useStockfishEngine` is imported by the test (knip-clean).
  </acceptance_criteria>
  <done>Hook implements the full UCI state machine, two-layer stale guard, tab-hide pause, and clean teardown; verified with mock-Worker unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Real-WASM integration test (initEngine FEN→bestmove)</name>
  <files>frontend/src/hooks/__tests__/useStockfishEngine.integration.test.ts</files>
  <read_first>
    - .planning/phases/136-usestockfishengine-hook-wasm-setup/136-RESEARCH.md § "RESEARCH FLAG Resolution: Integration Test Harness" (full initEngine pattern + open question 1) and § "Open Questions" #1
    - .planning/phases/136-usestockfishengine-hook-wasm-setup/136-PATTERNS.md § "useStockfishEngine.integration.test.ts"
    - node_modules/stockfish/index.js (confirm the listener property name + initEngine signature before writing — RESEARCH open-question 1 / assumption A2)
  </read_first>
  <action>
    Create `frontend/src/hooks/__tests__/useStockfishEngine.integration.test.ts` with `// @vitest-environment node`. Dynamically `import('stockfish')` and use its default `initEngine('lite-single', cb)` to boot the real WASM in Node (NOT a Worker, NOT @vitest/web-worker — vitest#6118). FIRST verify against `node_modules/stockfish/index.js` that the output API is `engine.listener = fn` and input is `engine.sendCommand(cmd)` (open-question 1 / A2); if the property name differs in the installed v18.0.8, use the actual one and note it in the SUMMARY. Drive one deterministic mate-in-1 FEN (the Scholar's-mate pre-mate position from RESEARCH; best move h5f7) with `position fen <FEN>` then `go movetime 500`, collect lines via the listener, resolve on the first bestmove. Assert `bestmove` starts with `h5f7` (do NOT assert an exact centipawn value — eval_cp is non-reproducible across hardware per project memory); additionally assert at least one info line with `score cp` and a `pv` was received (proves eval/pv/depth flow end-to-end). Use a ~10s test timeout to cover WASM boot. Terminate the engine at the end. If `initEngine` proves flaky in CI, the locked fallback is `it.skipIf(process.env.CI === 'true')` plus reliance on the Plan 01 `npm run build` + preview smoke — apply only if a genuine CI flake is observed, and record it in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useStockfishEngine.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/hooks/__tests__/useStockfishEngine.integration.test.ts` passes.
    - Asserts `bestmove` matches `^bestmove h5f7` for the mate-in-1 FEN (deterministic, no centipawn assertion).
    - Asserts at least one received line contains `score cp` and `pv` (eval + pv flow proven).
    - Test uses `// @vitest-environment node` and dynamic `import('stockfish')` (no bundler import of the package elsewhere).
    - The `engine.listener` property name was confirmed against the installed `node_modules/stockfish/index.js` (A2 resolved; any deviation noted in SUMMARY).
  </acceptance_criteria>
  <done>One real-WASM end-to-end FEN→bestmove assertion passes, satisfying D-01's single integration-test intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → hook | `fen` string is user-controlled (future board position) and passed to the engine |
| Worker → main thread | UCI output strings cross from the Worker into the hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-03 | Tampering / DoS | Worker lifecycle | mitigate | Per-mount Worker (never a global singleton); unmount cleanup always sends stop + `worker.terminate()` + nulls the ref (Pitfall 4 — prevents CPU/battery leak on route navigation) |
| T-136-04 | Spoofing (stale data) | stop/bestmove/go race | mitigate | Two-layer guard (debounce + stopPendingRef): the bestmove following a stop is discarded so the displayed eval never reflects a stale position (Pitfall 3) |
| T-136-V5 | Input Validation | FEN passed to engine | accept | FEN is parsed inside the Emscripten-sandboxed WASM (no injection surface); UCI output is same-origin trusted; per RESEARCH Security Domain V5 — low risk, no parsing of untrusted output beyond numeric/token extraction |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/hooks/__tests__/` (all three test files green).
- `cd frontend && npm run lint && npx tsc -b && npm run knip` all pass (knip confirms the hook + parser are consumed by tests; tsc confirms the data-contract types).
- Stale-eval guard test demonstrates rapid FEN changes commit only the final position.
</verification>

<success_criteria>
- ROADMAP SC#1 satisfied (data contract): the engine reaches uciok/readyok and the hook returns evalCp/evalMate, top 1–2 pvLines, bestmove (pvLines[0].moves[0]), and depth — rendering deferred to 137/138.
- ROADMAP SC#2 satisfied: debounced 150ms re-analysis capped at `go movetime 1500`.
- ROADMAP SC#3 satisfied (data contract): `enabled` toggle input + `isReady`/`isAnalyzing` states exposed.
- ENGINE-01..05 each mapped to a hook-data deliverable and covered by tests.
</success_criteria>

<output>
Create `.planning/phases/136-usestockfishengine-hook-wasm-setup/136-02-SUMMARY.md` when done (record the confirmed `stockfish` listener API for A2, and whether the integration test runs in CI or was gated).
</output>
