---
phase: 115-you-vs-opponent-comparison-api-bullet-grid-ui
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - frontend/src/components/charts/MiniBulletChart.tsx
  - frontend/src/components/charts/MiniBulletChart.test.tsx
  - frontend/src/types/library.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/components/popovers/FlawBulletPopover.tsx
  - frontend/src/components/library/FlawComparisonGrid.tsx
  - frontend/src/components/library/__tests__/FlawComparisonGrid.test.tsx
  - frontend/src/components/library/FlawStatsPanel.tsx
  - frontend/src/pages/GlobalStats.tsx
  - frontend/src/components/library/FlawTagDistribution.tsx
autonomous: false
requirements: [FLAWUI-01, FLAWUI-02, FLAWUI-03, FLAWUI-04, FLAWUI-05, FLAWUI-06, FLAWCMP-04]
must_haves:
  truths:
    - "The flaw-stats panel's Zone 3 tag-distribution is replaced by a family-grouped MiniBulletChart grid (Severity / Tempo / Phase / Opportunity / Impact / Combos) (D-01, FLAWUI-01)"
    - "Grid is 3 columns on desktop (lg) and 1 column on mobile; family headers span full width; grid does not reflow as filters change (D-03)"
    - "MiniBulletChart invertColors mode paints ZONE_SUCCESS LEFT of the neutral band and ZONE_DANGER RIGHT; the you−opponent sign is NOT negated (D-08)"
    - "All 6 existing MiniBulletChart callers are unchanged (invertColors defaults false)"
    - "Each bullet has a HelpCircle popover with definition + sign convention + applicable caveats (FLAWUI-02/03, D-15)"
    - "Below 20 analyzed games the grid renders an 'analyze more games' CTA (current count vs 20 + lichess server-analysis guidance); FlawStatsBand + trend chart stay live (D-10)"
    - "Zero-event bullets keep their row with a muted 'no events' placeholder, visually distinct from 'exactly typical' (D-11)"
    - "NormToggle and the per_game rendering path are deleted; FlawStatsBand is fixed to per-100-moves (D-02)"
    - "FlawTagDistribution.tsx is deleted and no longer imported (knip clean)"
    - "Trend chart stays comparison-free (no opponent delta, FLAWUI-05)"
    - "Tooltip filter line uses generic future-proof wording (D-14) with no opponent-gap special-casing (D-16)"
    - "Every new interactive element + container has data-testid + ARIA + semantic HTML, desktop+mobile parity (FLAWUI-06)"
  artifacts:
    - path: "frontend/src/components/charts/MiniBulletChart.tsx"
      provides: "invertColors?: boolean prop (default false) inverting zone+bar color semantics"
      contains: "invertColors"
    - path: "frontend/src/types/library.ts"
      provides: "FlawBullet + FlawComparisonResponse TS interfaces mirroring backend schema"
      contains: "FlawComparisonResponse"
    - path: "frontend/src/hooks/useLibrary.ts"
      provides: "useLibraryFlawComparison hook (queryKey ['library-flaw-comparison', params])"
      contains: "useLibraryFlawComparison"
    - path: "frontend/src/api/client.ts"
      provides: "libraryApi.getFlawComparison"
      contains: "getFlawComparison"
    - path: "frontend/src/components/popovers/FlawBulletPopover.tsx"
      provides: "per-bullet HelpCircle info popover (MetricStatPopover pattern)"
      contains: "FlawBulletPopover"
    - path: "frontend/src/components/library/FlawComparisonGrid.tsx"
      provides: "family-grouped bullet grid with loading/error/below-gate/zero-event states"
      contains: "FlawComparisonGrid"
    - path: "frontend/src/components/library/FlawStatsPanel.tsx"
      provides: "panel surgery: NormToggle + per_game removed, Band fixed per-100, Zone 3 = FlawComparisonGrid"
      contains: "FlawComparisonGrid"
  key_links:
    - from: "frontend/src/components/library/FlawComparisonGrid.tsx"
      to: "frontend/src/hooks/useLibrary.ts::useLibraryFlawComparison"
      via: "grid fetches its own data via the new hook"
      pattern: "useLibraryFlawComparison"
    - from: "frontend/src/hooks/useLibrary.ts::useLibraryFlawComparison"
      to: "/api/library/flaw-comparison"
      via: "libraryApi.getFlawComparison"
      pattern: "flaw-comparison"
    - from: "frontend/src/components/library/FlawComparisonGrid.tsx"
      to: "frontend/src/components/charts/MiniBulletChart.tsx"
      via: "per-bullet MiniBulletChart with invertColors + embedded zone bounds"
      pattern: "invertColors"
    - from: "frontend/src/components/library/FlawStatsPanel.tsx"
      to: "frontend/src/components/library/FlawComparisonGrid.tsx"
      via: "Zone 3 renders the grid (replaces FlawTagDistribution)"
      pattern: "FlawComparisonGrid"
---

<objective>
Build the frontend bullet-grid comparison surface: add an inverted-color mode to
`MiniBulletChart` (D-08), mirror the backend schema as TS types, add the
`useLibraryFlawComparison` hook + API client method, build the family-grouped
`FlawComparisonGrid` (D-01/D-03) and per-bullet `FlawBulletPopover` (D-15), and perform the
`FlawStatsPanel` surgery (D-02: delete NormToggle + per_game path, fix the Band to per-100,
replace the Zone 3 tag-distribution with the grid, delete the dead `FlawTagDistribution`).

Purpose: Render the Plan 01 endpoint's 15-bullet comparison so a user sees, per metric,
how their flaw rate compares to equally-rated opponents against the §5 "typical" zone —
with graceful below-gate (D-10) / zero-event (D-11) degradation and the D-15 tooltips.

Output: invertColors prop, TS types, hook + client, FlawComparisonGrid + FlawBulletPopover,
panel surgery, FlawTagDistribution deletion, and tests for the inverted-color mode + grid
testid/ARIA + below-gate CTA render.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-you-vs-opponent-comparison-api-bullet-grid-ui/115-CONTEXT.md
@.planning/phases/115-you-vs-opponent-comparison-api-bullet-grid-ui/115-RESEARCH.md
@.planning/phases/115-you-vs-opponent-comparison-api-bullet-grid-ui/115-PATTERNS.md
@.planning/notes/flaw-tag-definitions.md
@.planning/phases/115-you-vs-opponent-comparison-api-bullet-grid-ui/115-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (consumes Plan 01's `GET /api/library/flaw-comparison` +
FlawComparisonResponse shape):
- `MiniBulletChart` prop: `invertColors?: boolean` (default false)
- `frontend/src/types/library.ts`: `FlawBullet`, `FlawComparisonResponse` interfaces
- `frontend/src/api/client.ts`: `libraryApi.getFlawComparison(params)`
- `frontend/src/hooks/useLibrary.ts`: `useLibraryFlawComparison(filters, flawFilter)`
- `frontend/src/components/popovers/FlawBulletPopover.tsx`: `FlawBulletPopover`
- `frontend/src/components/library/FlawComparisonGrid.tsx`: `FlawComparisonGrid` (+ internal `FlawBulletRow`, family-section render)
- `FlawStatsPanel` prop additions: `filters` + `flawFilter` (passed down to the grid)
- DELETED: `frontend/src/components/library/FlawTagDistribution.tsx`, local `NormToggle` + `NormalizationMode` in FlawStatsPanel
- Bullet `tag` keys consumed (from registry order): `flaw_rate`, `mistake`, `blunder`, `low_clock`, `hasty`, `unrushed`, `opening`, `middlegame`, `endgame_phase`, `miss`, `lucky`, `reversed`, `squandered`, `hasty_miss`, `low_clock_miss`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MiniBulletChart invertColors + TS types + hook + API client</name>
  <files>frontend/src/components/charts/MiniBulletChart.tsx, frontend/src/components/charts/MiniBulletChart.test.tsx, frontend/src/types/library.ts, frontend/src/api/client.ts, frontend/src/hooks/useLibrary.ts</files>
  <behavior>
    - MiniBulletChart with invertColors=true: a value ABOVE the neutral band paints ZONE_DANGER (right=bad), a value BELOW paints ZONE_SUCCESS (left=good); within band paints ZONE_NEUTRAL
    - MiniBulletChart with invertColors=true: background zone divs render [left=ZONE_SUCCESS, center=ZONE_NEUTRAL, right=ZONE_DANGER]
    - MiniBulletChart without invertColors (default): existing color behavior unchanged (value above band = ZONE_SUCCESS) — regression test on the existing test cases stays green
  </behavior>
  <read_first>
    - frontend/src/components/charts/MiniBulletChart.tsx lines 21 (theme imports), 37-82 (props interface incl. barColor at 82), 90-104 (destructure + axis math), 118-137 (fillColor logic), 157-182 (three background zone divs), 215+ (CI whisker)
    - frontend/src/components/charts/MiniBulletChart.test.tsx (existing tests — the barColor + data-testid assertion idiom to extend)
    - frontend/src/hooks/useLibrary.ts lines 13 (LIBRARY_STALE_TIME), 22-44 (buildLibraryParams), 83-94 (useLibraryFlawStats — exact analog)
    - frontend/src/api/client.ts lines 249-262 (getFlawStats — params shape + apiClient.get analog)
    - frontend/src/types/library.ts lines 164-186 (FlawStatsResponse interface — mirror style)
    - 115-PATTERNS.md "MiniBulletChart.tsx" (383-433), "useLibrary.ts" (556-581), "library.py schema" (103-138 — TS must mirror these exact fields)
  </read_first>
  <action>
    MiniBulletChart: add `invertColors?: boolean` to `MiniBulletChartProps` (doc comment per
    PATTERNS lines 391-399), destructure with default `invertColors = false` (line ~102-103).
    Replace the fillColor logic (lines 127-133): compute `const positiveColor = invertColors ?
    ZONE_DANGER : ZONE_SUCCESS;` and `const negativeColor = invertColors ? ZONE_SUCCESS :
    ZONE_DANGER;`, then `if (value >= absNeutralMax) fillColor = positiveColor; else if (value
    >= absNeutralMin) fillColor = ZONE_NEUTRAL; else fillColor = negativeColor;` (D-08). In the
    three background zone divs (lines 157-182): the LEFT div backgroundColor becomes
    `invertColors ? ZONE_SUCCESS : ZONE_DANGER` and the RIGHT div becomes `invertColors ?
    ZONE_DANGER : ZONE_SUCCESS` (center stays ZONE_NEUTRAL). Use only the theme.ts constants
    already imported; no new hex. Leave the CI-whisker and clamp logic untouched.

    MiniBulletChart.test.tsx: add cases for invertColors=true asserting the value-bar fill
    and background zone order invert (value below band → ZONE_SUCCESS fill), and assert the
    existing (default) cases still produce the original colors (regression guard for the 6
    callers). Use the existing data-testid assertion idiom in the file.

    types/library.ts: add `FlawBullet` interface (tag: string; delta: number | null; ci_low:
    number | null; ci_high: number | null; player_events: number; opp_events: number; zone_lo:
    number; zone_hi: number; domain: number; has_zone: boolean) and `FlawComparisonResponse`
    (bullets: FlawBullet[]; analyzed_n: number; analyzed_gate: number; below_gate: boolean) —
    field names snake_case to match the JSON payload exactly (same convention as
    FlawStatsResponse).

    api/client.ts: add `getFlawComparison` to `libraryApi`, mirroring `getFlawStats` (same
    params object, `apiClient.get<FlawComparisonResponse>('/library/flaw-comparison', { params
    })`).

    hooks/useLibrary.ts: add `useLibraryFlawComparison(filters: FilterState, flawFilter:
    FlawFilterState)` mirroring `useLibraryFlawStats` exactly but with queryKey
    `['library-flaw-comparison', params]` and `queryFn: () =>
    libraryApi.getFlawComparison(params)`. Same `buildLibraryParams` + LIBRARY_STALE_TIME +
    refetchOnWindowFocus:false.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/charts/MiniBulletChart.test.tsx && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    invertColors prop added with default false; existing MiniBulletChart tests still green;
    new invertColors tests assert success-left/danger-right. FlawBullet +
    FlawComparisonResponse types mirror the backend schema field-for-field.
    useLibraryFlawComparison + libraryApi.getFlawComparison exist. lint + tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: FlawBulletPopover + FlawComparisonGrid + grid tests</name>
  <files>frontend/src/components/popovers/FlawBulletPopover.tsx, frontend/src/components/library/FlawComparisonGrid.tsx, frontend/src/components/library/__tests__/FlawComparisonGrid.test.tsx</files>
  <read_first>
    - frontend/src/components/popovers/MetricStatPopover.tsx (full file, 96 lines) — Root/Trigger/Portal/Content structure, HOVER_OPEN_DELAY_MS, hover handlers, role/tabIndex/aria-label/data-testid trigger, text-xs Content classes (swap Search → HelpCircle)
    - frontend/src/components/library/FlawStatsPanel.tsx lines 142-200 (the isError/isLoading/data ternary chain + LoadError usage — copy the error-handling idiom)
    - frontend/src/components/charts/MiniBulletChart.tsx (the prop surface to call: value, neutralMin, neutralMax, domain, center, ciLow, ciHigh, invertColors)
    - .planning/notes/flaw-tag-definitions.md (definition prose source for each of the 15 tags + the 2026-06-09 impact recalibration)
    - 115-PATTERNS.md "FlawComparisonGrid.tsx" (437-487), "FlawBulletPopover.tsx" (490-553), "data-testid + ARIA naming convention" (640-650), "text-sm floor" (656-659)
    - 115-CONTEXT.md D-01,D-03,D-08,D-10,D-11,D-13,D-14,D-15,D-16 + RESEARCH "Frontend Grid Layout" (537-559)
  </read_first>
  <action>
    FlawBulletPopover.tsx: copy the MetricStatPopover shell (Root/Trigger/Portal/Content,
    HOVER_OPEN_DELAY_MS=100, hover handlers), swap the `Search` icon for `HelpCircle` from
    lucide-react. Props: `tag: string; testId: string; ariaLabel: string; triggerClassName?:
    string`. Content (text-xs allowed — info-popover exception per D-15/CLAUDE.md) selects copy
    by `tag` and includes: (1) a one-line definition (from flaw-tag-definitions.md); (2) the
    sign-convention line verbatim "Negative = fewer flaws than opponents = better." (FLAWUI-02);
    (3) a tempo-interaction caveat ONLY for tags in {low_clock, hasty, unrushed, low_clock_miss,
    hasty_miss}; (4) an exposure caveat ONLY for {squandered, lucky} (114 D-03: reads partly as
    how often the situation arose); (5) a severity-basis caveat for ALL tags (D-13: zone
    computed on M+B basis; under a severity filter your delta reflects that filter's scope);
    (6) a single generic filter line (FLAWUI-03, D-14 future-proof: "Filters change your point
    estimate; the typical zone may not follow your filters."). Per D-16, add NO extra
    opponent-gap-specific disclosure — the generic D-14 line covers the gap filter implicitly.
    Keep prose minimal per popover-copy-minimalism (what + sign convention; no jargon, no
    p-values/CI/Wilson talk). data-testid `flaw-bullet-popover-{tag}`, aria-label
    `Flaw comparison: {label}`.

    FlawComparisonGrid.tsx: a component taking `filters: FilterState` + `flawFilter:
    FlawFilterState` props; internally calls `useLibraryFlawComparison(filters, flawFilter)`.
    Handle states in this order (copy the FlawStatsPanel ternary idiom): isLoading → a
    lightweight skeleton with data-testid `flaw-comparison-loading`; isError → LoadError /
    CLAUDE.md error copy ("Failed to load comparison. Something went wrong. Please try again in
    a moment."); !data → null; data.below_gate → a CTA block (data-testid
    `flaw-comparison-gate-cta`) showing `data.analyzed_n` of `data.analyzed_gate` analyzed
    games needed + guidance toward lichess server analysis to get more games analyzed (D-10) —
    FlawStatsBand + trend stay live because they live in the parent. Otherwise render the grid:
    a container `<div data-testid="flaw-comparison-grid" className="grid grid-cols-1
    lg:grid-cols-3 gap-2">`. Group the 15 bullets into 6 families by tag (Severity: flaw_rate,
    mistake, blunder; Tempo: low_clock, hasty, unrushed; Phase: opening, middlegame,
    endgame_phase; Opportunity: miss, lucky; Impact: reversed, squandered; Combos: hasty_miss,
    low_clock_miss). Each family emits a full-width header `<h4 className="col-span-1
    lg:col-span-3 text-sm ...">` (D-01) followed by its bullet rows in registry order. Each
    bullet row (`FlawBulletRow`, can be an inline sub-component, data-testid
    `flaw-bullet-row-{tag}`): a short label + the FlawBulletPopover trigger; then if
    `player_events === 0 && opp_events === 0` render a muted "No events in current filter"
    placeholder occupying the row (D-11 — visually distinct from a typical-zone bar; the grid
    must NOT reflow); else render `<MiniBulletChart value={bullet.delta!}
    neutralMin={bullet.zone_lo} neutralMax={bullet.zone_hi} domain={bullet.domain} center={0}
    ciLow={bullet.ci_low ?? undefined} ciHigh={bullet.ci_high ?? undefined} invertColors />`
    plus a value label. Use semantic HTML (`<section>`/`<h4>`/`<button>`); never `<div
    onClick>`. All text >= text-sm except the popover content. Keep the component under the
    logic-LOC limit — extract the family grouping into a small helper/const and FlawBulletRow
    as a sibling component if the body grows past ~100 logic lines. Both combos (hasty_miss,
    low_clock_miss) MUST be in the Combos family and render via this same zero-event/CI
    machinery (FLAWCMP-04).

    FlawComparisonGrid.test.tsx: render with a mocked useLibraryFlawComparison (mock the hook
    module). Assert: (a) below_gate=true → CTA testid present, grid absent, shows "N of 20";
    (b) below_gate=false with 15 bullets → grid testid present, exactly 6 family headers, 15
    `flaw-bullet-row-{tag}` rows, every row has a `flaw-bullet-popover-{tag}` with aria-label
    (FLAWUI-06); (c) a zero-event bullet renders the "No events" placeholder and NOT a
    MiniBulletChart; (d) isError → error copy rendered. Use the existing test render helper /
    QueryClient wrapper idiom from a neighboring library test.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/library/__tests__/FlawComparisonGrid.test.tsx && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    FlawBulletPopover renders the D-15 content per tag (HelpCircle trigger, text-xs).
    FlawComparisonGrid renders loading/error/below-gate-CTA/grid states; family-grouped 3-col
    desktop / 1-col mobile; zero-event placeholder without reflow; both combos present. Grid
    test asserts CTA, 6 headers, 15 rows, popover testid+ARIA, zero-event placeholder, error
    copy. lint + tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: FlawStatsPanel surgery + GlobalStats wiring + delete FlawTagDistribution</name>
  <files>frontend/src/components/library/FlawStatsPanel.tsx, frontend/src/pages/GlobalStats.tsx, frontend/src/components/library/FlawTagDistribution.tsx</files>
  <read_first>
    - frontend/src/components/library/FlawStatsPanel.tsx (full file) — lines 17-68 (NormalizationMode type + NormToggle component to DELETE), 120-200 (FlawStatsPanel body: normalization state, NormToggle JSX at 133, FlawStatsBand at 176 with normalization prop, FlawTrendChart at 183, FlawTagDistribution at 189)
    - frontend/src/components/library/FlawStatsBand.tsx lines 17,52,55,81 (NormalizationMode type + normalization prop + the "/ game" vs "/ 100 moves" suffix branch) — fix to per-100
    - frontend/src/pages/GlobalStats.tsx lines 9-48 (filters store, DEFAULT_FLAW_FILTER, useLibraryFlawStats call, FlawStatsPanel render) — wire filters+flawFilter into the panel
    - 115-PATTERNS.md "FlawStatsPanel.tsx" (585-596) and "FlawTagDistribution delete" (600-602)
    - 115-RESEARCH.md Pitfall 7 (NormToggle/per_game removal) + Pitfall 8 (FlawTagDistribution knip)
  </read_first>
  <action>
    FlawStatsPanel.tsx: DELETE the local `NormToggle` component, the `NormalizationMode` type,
    the `normalization`/`setNormalization` useState, and the `<NormToggle .../>` JSX (D-02 —
    delete, not hide). Remove the `import { FlawTagDistribution }` and its `<FlawTagDistribution
    ... />` JSX (Zone 3). Add props `filters: FilterState` + `flawFilter: FlawFilterState` to
    `FlawStatsPanelProps`. Call `<FlawStatsBand ... />` WITHOUT a normalization prop (it is now
    fixed to per-100; see Band change below). Render `<FlawComparisonGrid filters={filters}
    flawFilter={flawFilter} />` as Zone 3 (it self-fetches; the panel's existing
    isLoading/isError chain still gates Zones 1-2 only). Keep FlawTrendChart unchanged
    (comparison-free, FLAWUI-05).

    FlawStatsBand.tsx: remove the `normalization` prop and the `NormalizationMode` type from
    this component; hard-fix the suffix to "/ 100 moves" and read the per_100_moves rates only
    (drop the per_game branch at line 81) (D-02). Verify no other caller passes `normalization`.

    GlobalStats.tsx: pass `filters={filters}` and `flawFilter={DEFAULT_FLAW_FILTER}` (the same
    values already used for useLibraryFlawStats) into `<FlawStatsPanel ... />`. Do not change
    the existing stats fetch.

    DELETE `frontend/src/components/library/FlawTagDistribution.tsx` entirely (grep confirmed
    FlawStatsPanel was its only importer; no test file exists). Knip would fail if the dead
    export remained, so deletion (not export-removal) is the correct action.

    Run knip after wiring to confirm no dead exports/unused deps were introduced and that
    nothing still references FlawTagDistribution or the removed NormToggle/NormalizationMode.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit && npm run knip && npm test -- --run</automated>
  </verify>
  <done>
    NormToggle + NormalizationMode + per_game path deleted; FlawStatsBand fixed to per-100;
    Zone 3 renders FlawComparisonGrid with filters+flawFilter; FlawTrendChart unchanged.
    FlawTagDistribution.tsx deleted with no remaining importers. GlobalStats passes
    filters+flawFilter. lint + tsc + knip clean; full frontend suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete bullet-grid comparison surface in Library → Stats tab: a family-grouped
    (Severity/Tempo/Phase/Opportunity/Impact/Combos) grid of 15 inverted-color MiniBulletCharts
    replacing the old tag-distribution zone, with per-bullet HelpCircle tooltips, a
    20-analyzed-game CTA gate, zero-event placeholders, and the NormToggle removed (Band fixed
    to per-100). Trend chart unchanged.
  </what-built>
  <how-to-verify>
    PRE-CHECK (A2 data-freshness — BLOCKING per planning context): Confirm the DEV `game_flaws`
    table is on the SAME basis as the §5 zone constants (2026-06-11 mate-ladder + recalibrated
    `reversed`/`squandered` thresholds). Quick check: query dev DB for a known user's reversed/
    squandered distribution, or confirm whether the impact-threshold `/gsd-quick` (RESEARCH A2)
    has landed in dev. If dev `game_flaws` is stale, the user's live delta will not match the
    zone basis — a `scripts/reclassify_positions.py` reclassify/backfill is needed BEFORE
    trusting the visual. Surface the result to the user; this is a data check, not a code gate.

    Then, with dev backend + frontend running (`bin/run_local.sh`) and logged in as a user with
    >= 20 analyzed games (RESEARCH: users 28/44):
    1. Open Library → Stats tab. Confirm the new grid replaces the old tag-distribution zone.
    2. Desktop (lg viewport): confirm 3 columns, six family headers, ~5 rows of bullets.
       Mobile (375px): confirm 1 column, same families, no horizontal overflow.
    3. Confirm inverted colors: a bullet where you have FEWER flaws than opponents (negative
       delta) paints/sits in the GREEN (left) zone; more flaws → RED (right). Numbers shown
       keep the you−opponent sign (negative = good), matching §5.
    4. Hover a few bullet HelpCircle popovers: confirm definition + "Negative = fewer flaws
       than opponents = better." + the tempo caveat on clock tags + severity-basis + filter
       line read correctly.
    5. Confirm a zero-event bullet (if any) shows a muted "No events" placeholder, not a bar,
       and the grid does not reflow when you change filters.
    6. Switch the user/filters so analyzed games < 20: confirm the CTA ("N of 20 analyzed
       games...") replaces the grid while the Band + trend chart above stay visible.
    7. Confirm the per-game/per-100 toggle is gone and the Band reads "/ 100 moves".
    8. Apply a blunders-only severity filter: confirm all 15 bullets still show zones (D-13),
       with the severity-basis caveat in the tooltip.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (layout, colors, copy, gate, A2 data basis)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (`GET /flaw-comparison`) | Frontend issues authenticated reads; no new secrets or user-controlled identifiers cross this boundary (filters only) |
| rendered payload → DOM | Delta/CI/zone/event-count numbers rendered into the grid; no HTML injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-05 | Information Disclosure (IDOR) | useLibraryFlawComparison → /flaw-comparison | mitigate | The hook sends only filter params; user identity is the session JWT (Bearer), never a request-supplied user_id. Backend (Plan 01, T-115-01) scopes by `current_active_user`. Frontend introduces no user_id param. |
| T-115-06 | Injection (XSS) | FlawComparisonGrid / FlawBulletPopover rendering | mitigate | All values rendered as React text children (auto-escaped); no `dangerouslySetInnerHTML`. Popover copy is static literals keyed by a fixed tag enum. |
| T-115-07 | Tampering | filter params from the client | accept | Filters are validated server-side (Plan 01 T-115-02). A tampered client can only narrow/widen its OWN data view; no cross-user reach. |
| T-115-SC | Tampering | npm installs | n/a | No new npm packages (lucide-react, radix-ui, recharts, tanstack-query all already present). |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` green (MiniBulletChart invertColors + FlawComparisonGrid).
- `npm run lint && npx tsc --noEmit && npm run knip` clean (knip MUST be clean after deleting
  FlawTagDistribution and NormToggle).
- Human-verify checkpoint (Task 4) including the BLOCKING A2 dev-data-basis pre-check.
- Confirm the 6 existing MiniBulletChart callers (endgame/openings) render unchanged
  (invertColors defaults false) — covered by the regression assertion in Task 1's test.
</verification>

<success_criteria>
- Zone 3 of the flaw-stats panel is a family-grouped MiniBulletChart grid (3-col desktop /
  1-col mobile), inverted colors (success left / danger right), sign not negated.
- Per-bullet HelpCircle popovers carry definition + sign convention + applicable caveats
  (FLAWUI-02/03). Below-gate CTA + zero-event placeholders work (no reflow). Trend chart
  comparison-free (FLAWUI-05). All new elements have data-testid/ARIA, desktop+mobile parity
  (FLAWUI-06).
- NormToggle + per_game path deleted, Band fixed per-100. FlawTagDistribution deleted, knip
  clean. Both combos render (FLAWCMP-04). FLAWUI-01..06 satisfied.
</success_criteria>

<output>
Create `.planning/phases/115-you-vs-opponent-comparison-api-bullet-grid-ui/115-02-SUMMARY.md` when done
</output>
