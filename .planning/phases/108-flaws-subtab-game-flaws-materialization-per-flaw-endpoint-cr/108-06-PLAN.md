---
phase: 108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr
plan: 06
type: execute
wave: 3
depends_on: ["108-02"]
files_modified:
  - scripts/backfill_flaws.py
  - scripts/reclassify_positions.py
  - tests/test_backfill_flaws.py
autonomous: true
requirements: [D-09, D-10]
must_haves:
  truths:
    - "scripts/backfill_flaws.py --db dev --user-id 28 populates game_flaws rows (verified)"
    - "Backfill is batched (commit per chunk) to respect the project's OOM memory envelope"
    - "Backfill recompute uses delete-then-insert per game (threshold-change safe, idempotent)"
    - "Backfill, reclassify_positions, and the import hook all call the SAME classify_game_flaws + flaw_record_to_row (one classify path, D-10)"
    - "--dry-run reports counts without writing"
  artifacts:
    - path: "scripts/backfill_flaws.py"
      provides: "batched --db/--user-id/--dry-run/--limit backfill CLI"
      contains: "BACKFILL_GAMES_PER_BATCH"
    - path: "scripts/reclassify_positions.py"
      provides: "also recomputes game_flaws (D-10)"
      contains: "game_flaws"
    - path: "tests/test_backfill_flaws.py"
      provides: "dry-run + batched insert test (session-maker injection)"
  key_links:
    - from: "scripts/backfill_flaws.py"
      to: "app.services.flaws_service.classify_game_flaws + game_flaws_repository.flaw_record_to_row"
      via: "delete-then-insert per game, batched"
      pattern: "classify_game_flaws"
---

<objective>
Build `scripts/backfill_flaws.py` (D-09): a batched, `--db {dev|benchmark|prod}` + optional `--user-id` recompute driver mirroring `scripts/backfill_eval.py`, and wire `reclassify_positions.py` to also recompute `game_flaws` (D-10). All three write paths (import hook, reclassify, backfill) call the SAME `classify_game_flaws` + `flaw_record_to_row` so there is no drift. Verify on `--db dev --user-id 28`.

Purpose: D-09 makes the backfill the threshold-change recompute tool (thresholds last changed 2026-06-05) and the rollout mechanism for the Games-tab migration (existing users' game_flaws is empty until backfilled). D-10 mandates one classify path everywhere. Batching is MANDATORY given the project's OOM history.
Output: `backfill_flaws.py`, the reclassify hook, and a backfill test. Prod/all-users runs are manual out-of-band (NOT a tracked task; phase completion NOT gated on prod backfill).
</objective>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols:
- `scripts/backfill_flaws.py` → `run_backfill`, `_parse_args`, `BACKFILL_GAMES_PER_BATCH` constant
- `scripts/reclassify_positions.py` → game_flaws recompute call (delete-then-insert via the shared mapping)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-CONTEXT.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/backfill_flaws.py (batched, --db/--user-id/--dry-run/--limit)</name>
  <files>scripts/backfill_flaws.py</files>
  <read_first>
    - scripts/backfill_eval.py lines 1-130 (CLI args, db_url_for_target, async_sessionmaker, batch loop, _log helper — the scaffold to mirror; note --workers/EnginePool are NOT needed)
    - app/core/config.py (db_url_for_target signature)
    - app/repositories/game_flaws_repository.py (bulk_insert_game_flaws, delete_flaws_for_game, flaw_record_to_row — the shared write path)
    - app/services/flaws_service.py (classify_game_flaws, GameNotAnalyzed)
    - app/repositories/flaws_repository.py (fetch_game_positions_ordered)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §backfill_flaws.py
    - CLAUDE.md (no magic numbers; Sentry capture on errors; never asyncio.gather on same session; OOM/batching discipline)
  </read_first>
  <action>
    Create `scripts/backfill_flaws.py` mirroring backfill_eval.py's header (sys.path insert, noqa E402 imports) and CLI: `--db {dev,benchmark,prod}` required, `--user-id` optional, `--dry-run`, `--limit`. Define `BACKFILL_GAMES_PER_BATCH = 100` (named constant — no magic numbers). `run_backfill(*, db, user_id, dry_run, limit)`: build engine via `db_url_for_target(db)` + `async_sessionmaker(expire_on_commit=False)`; select analyzed game_ids (eval coverage gate; filtered by user_id when provided; capped by limit); process in BACKFILL_GAMES_PER_BATCH chunks — for each game SEQUENTIALLY (no asyncio.gather): load ordered positions, call classify_game_flaws, skip GameNotAnalyzed, then `delete_flaws_for_game` + map via flaw_record_to_row + `bulk_insert_game_flaws` (delete-then-insert = idempotent, threshold-change safe); commit per chunk; log progress via the _log helper. `--dry-run`: classify and count rows but do NOT write/commit. Wrap per-game work in try/except with `sentry_sdk.set_context(...)` + `capture_exception` + continue (one bad game must not abort the run); no variables in messages. `if __name__ == "__main__": asyncio.run(run_backfill(**vars(_parse_args())))`.
  </action>
  <verify>
    <automated>uv run python scripts/backfill_flaws.py --db dev --user-id 28 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/backfill_flaws.py --db dev --user-id 28 --dry-run` runs without error and reports a count
    - CLI supports `--db {dev,benchmark,prod}` (required), `--user-id`, `--dry-run`, `--limit`
    - `BACKFILL_GAMES_PER_BATCH` named constant; commit per chunk; sequential per-game loop (no gather)
    - Uses delete_flaws_for_game + flaw_record_to_row + bulk_insert_game_flaws (the shared D-10 path)
    - Per-game errors Sentry-captured and skipped (run continues)
  </acceptance_criteria>
  <done>Backfill script: batched, idempotent delete-then-insert, dry-run, Sentry-guarded, reuses the single classify path.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reclassify_positions.py to recompute game_flaws (D-10)</name>
  <files>scripts/reclassify_positions.py</files>
  <read_first>
    - scripts/reclassify_positions.py (the existing PGN-replay reclassify flow — where per-game position metadata is refreshed; add the game_flaws recompute at the same per-game point)
    - app/repositories/game_flaws_repository.py (delete_flaws_for_game + flaw_record_to_row + bulk_insert_game_flaws)
    - app/services/flaws_service.py (classify_game_flaws, GameNotAnalyzed)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-CONTEXT.md D-10
  </read_first>
  <action>
    In `reclassify_positions.py`, after a game's position metadata is refreshed (it already replays the PGN), recompute that game's flaws: `delete_flaws_for_game(session, game_id=..., user_id=...)` then `classify_game_flaws` + map via flaw_record_to_row + `bulk_insert_game_flaws` (skip GameNotAnalyzed). Keep it sequential within the existing per-game loop (no asyncio.gather). Reuse the existing commit/batch cadence of the script — do not introduce a parallel write path; the goal is that reclassify and backfill produce identical game_flaws rows. Sentry-guard per-game with context + continue.
  </action>
  <verify>
    <automated>uv run ty check scripts/reclassify_positions.py scripts/backfill_flaws.py</automated>
  </verify>
  <acceptance_criteria>
    - `reclassify_positions.py` recomputes game_flaws per game via delete-then-insert using the shared mapping
    - The recompute is sequential within the existing loop (no gather) and Sentry-guarded
    - `uv run ty check scripts/reclassify_positions.py scripts/backfill_flaws.py` exits 0
  </acceptance_criteria>
  <done>reclassify_positions also materializes game_flaws via the single classify path; ty clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backfill dry-run + batched-insert test (Wave 0 stub)</name>
  <files>tests/test_backfill_flaws.py</files>
  <behavior>
    - run_backfill with dry_run=True writes zero game_flaws rows
    - run_backfill (real) populates game_flaws for an analyzed game matching classify_game_flaws output
    - re-running run_backfill is idempotent (delete-then-insert yields the same row set, no duplicates)
  </behavior>
  <read_first>
    - tests/test_backfill_eval.py (analog — session-maker injection pattern for testing a backfill script without a real engine)
    - scripts/backfill_flaws.py (run_backfill under test — ensure it accepts an injectable session_maker or refactor to allow it)
    - tests/conftest.py (fixtures; no dev DB reset)
    - tests/test_flaws_materialization.py (how to build an analyzed game with known flaws)
  </read_first>
  <action>
    Create `tests/test_backfill_flaws.py` reusing the session-maker injection pattern from test_backfill_eval.py (so run_backfill runs against the per-run test DB, not a real `--db` target). If backfill_flaws.run_backfill currently builds its own engine, factor the per-game backfill loop into a testable inner function that accepts an `AsyncSession`/`session_maker` so the test can drive it. Assert: (a) dry-run writes nothing; (b) a real run materializes the expected M+B rows for an analyzed game; (c) re-running is idempotent (same row set, no PK duplicates). This is the Wave 0 dependency for the D-09 backfill behavior; the live `--db dev --user-id 28` run remains a manual smoke check (VALIDATION Manual-Only).
  </action>
  <verify>
    <automated>uv run pytest tests/test_backfill_flaws.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backfill_flaws.py -x` exits 0
    - Asserts dry-run writes nothing, real run materializes expected rows, re-run is idempotent
    - Uses session-maker injection against the test DB (no real --db target)
  </acceptance_criteria>
  <done>Backfill test green: dry-run, real materialization, and idempotent re-run verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → DB write | backfill runs with the app DB user against dev/benchmark/prod targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-13 | Tampering | wrong-user rows on backfill | mitigate | Rows are stamped from each game's own `user_id` via flaw_record_to_row; `delete_flaws_for_game` is scoped to (game_id, user_id) — no cross-user overwrite. |
| T-108-14 | Denial of Service | OOM on bulk recompute | mitigate | Batched commit per BACKFILL_GAMES_PER_BATCH chunk; sequential per-game position load; no Stockfish/engine. Mirrors backfill_eval.py memory discipline (CLAUDE.md OOM history). |
| T-108-15 | Repudiation | silent partial backfill | mitigate | Per-chunk progress logging + Sentry capture on per-game errors; --dry-run for safe pre-flight. |
</threat_model>

<verification>
- `uv run pytest tests/test_backfill_flaws.py -x` then `uv run ty check scripts/ app/ tests/`. Manual smoke (out-of-band, not gating): `uv run python scripts/backfill_flaws.py --db dev --user-id 28`.
</verification>

<success_criteria>
- backfill_flaws.py: batched, idempotent, dry-run, multi-target; verified via dry-run on dev user 28 + the injection test.
- reclassify_positions also recomputes game_flaws — one classify path across import hook, reclassify, backfill (D-10).
- Prod/all-users backfill is explicitly manual out-of-band; phase completion not gated on it (D-09).
</success_criteria>

<output>
Create `.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-06-SUMMARY.md` when done.
</output>
