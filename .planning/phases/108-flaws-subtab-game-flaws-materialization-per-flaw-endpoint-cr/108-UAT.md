---
status: testing
phase: 108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr
source: [108-VERIFICATION.md]
started: 2026-06-06T20:05:00Z
updated: 2026-06-06T20:05:00Z
---

## Current Test

number: 1
name: Miniboard marked-move arrow correctness
expected: |
  Navigate to /library/flaws (as a user with analyzed Lichess games). Each row shows a
  miniboard at the correct position with a correctly-placed red arrow marking the actual
  blunder/mistake move. Arrow points from the source square to the destination square of
  the flawed move. No row shows a misleading e1→e1 marker or a missing arrow when move_san
  is present. (Castling/en-passant moves may show no arrow — acceptable graceful fallback,
  since board_fen() omits castling/en-passant rights.)
awaiting: user response

## Tests

### 1. Miniboard marked-move arrow correctness
expected: Navigate to /library/flaws (user with analyzed Lichess games). Each row's miniboard shows a red arrow from the source to the destination square of the actual flawed move. No e1→e1 marker; no missing arrow for ordinary moves with move_san present.
result: [pending]

### 2. Cross-family AND filter (single-flaw EXISTS semantics)
expected: In the Flaws tab FlawFilterControl — (a) select only "Blunders" → only blunder rows; (b) select a tag (e.g. "result-changing") → only rows with that tag; (c) combine a Timing tag and an Impact tag → only rows where a SINGLE flaw is tagged with BOTH families (AND across families, OR within family).
result: [pending]

### 3. Deep-link navigation and pre-population
expected: Click a tag chip on a Games-tab game card → URL changes to /library/flaws?tag=<tagname>, the Flaws tab activates with that tag pre-selected (button highlighted in its family color), severity defaults to M+B, and the list shows only flaws with that tag. No game_id in the URL.
result: [pending]

### 4. Cross-tab filter state persistence
expected: Switch between Games and Flaws tabs while a flaw filter is active. The selected severity/tags are preserved in both directions (shared store).
result: [pending]

### 5. Mobile drawer layout (375px)
expected: Open /library/flaws on a 375px viewport, tap "Filters" → drawer opens with FlawFilterControl fully visible (Timing/Opportunity/Impact sections), tag buttons have adequate tap targets (~44px).
result: [pending]

### 6. Empty-state discrimination
expected: On the Flaws tab with no analyzed Lichess games imported, the empty state reads "No analyzed games" / "Only Lichess games with engine analysis have flaws. Import Lichess games to see your flaws." — NOT the generic "No games imported yet".
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
