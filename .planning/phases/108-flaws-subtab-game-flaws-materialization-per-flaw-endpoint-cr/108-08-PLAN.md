---
phase: 108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr
plan: 08
type: execute
wave: 7
depends_on: ["108-07"]
files_modified:
  - frontend/src/components/filters/LibraryFilterPanel.tsx
  - frontend/src/pages/library/GamesTab.tsx
  - frontend/src/components/library/TagChip.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/library/__tests__/TagChip.test.tsx
  - frontend/src/pages/library/__tests__/GamesTab.test.tsx
autonomous: true
requirements: [D-01, D-04, D-05]
must_haves:
  truths:
    - "The boolean severity toggle is removed from LibraryFilterPanel; the panel now renders FlawFilterControl (D-01)"
    - "GamesTab uses the shared useFlawFilterStore instead of local severityFilter state (D-04)"
    - "Games-card tag chips navigate to /library/flaws?tag={TAG} (no game_id) on click (D-05)"
    - "Switching Games↔Flaws preserves the flaw-filter selection in memory (shared store)"
    - "The Games query passes both severity and tags to the migrated endpoint"
  artifacts:
    - path: "frontend/src/components/filters/LibraryFilterPanel.tsx"
      provides: "panel renders FlawFilterControl; severity toggle removed"
      contains: "FlawFilterControl"
    - path: "frontend/src/components/library/TagChip.tsx"
      provides: "chip navigates to /library/flaws?tag="
      contains: "/library/flaws"
  key_links:
    - from: "frontend/src/components/library/TagChip.tsx"
      to: "/library/flaws?tag={TAG}"
      via: "useNavigate onClick (no game_id, D-05)"
      pattern: "/library/flaws\\?tag="
    - from: "frontend/src/pages/library/GamesTab.tsx"
      to: "useFlawFilterStore"
      via: "shared cross-tab flaw filter state"
      pattern: "useFlawFilterStore"
---

<objective>
Reconcile the Games tab with the new shared flaw filter (D-01/D-04/D-05): remove the Phase 107 boolean severity toggle from `LibraryFilterPanel` and render `FlawFilterControl` instead; migrate `GamesTab` from local `severityFilter` state to the shared `useFlawFilterStore`; and convert the display-only Games-card `TagChip` into a navigation trigger that deep-links to `/library/flaws?tag={TAG}` (no game_id, D-05).

Purpose: D-01 supersedes the boolean toggle with the dedicated severity×tag control; D-04 shares state across tabs so switching Games↔Flaws preserves the selection; D-05 wires Phase 107's display-only chips into broad doorways to the pre-filtered Flaws view.
Output: modified LibraryFilterPanel + GamesTab + TagChip/LibraryGameCard + updated tests.
</objective>

<artifacts_this_phase_produces>
This plan modifies existing symbols (consumes Plan 07's FlawFilterControl + useFlawFilterStore):
- `LibraryFilterPanel` props change: `severityFilter`/`onSeverityChange` → `flawFilter`/`onFlawFilterChange`
- `GamesTab` drops local `severityFilter` state for `useFlawFilterStore`
- `TagChip` API change: popover trigger → navigation `<button>`; `aria-label` becomes "Filter flaws by tag: {tag}"
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-CONTEXT.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-UI-SPEC.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-07-SUMMARY.md
---
NOTE: depends_on 108-07 — FlawFilterControl + useFlawFilterStore must exist. If FlawsTab rendered FlawFilterControl inline (Plan 07 Task 3), move that rendering into LibraryFilterPanel here so both tabs share the panel-hosted control (single source).
---
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap severity toggle for FlawFilterControl in LibraryFilterPanel (D-01)</name>
  <files>frontend/src/components/filters/LibraryFilterPanel.tsx</files>
  <read_first>
    - frontend/src/components/filters/LibraryFilterPanel.tsx (current panel: the boolean severity toggle block to remove; the metadata filters platform/TC/rated/recency/opponent to KEEP)
    - frontend/src/components/filters/FlawFilterControl.tsx (Plan 07 — the control to host, its props)
    - frontend/src/hooks/useFlawFilterStore.ts (FlawFilterState shape)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-UI-SPEC.md §"Components modified" + §"Purpose and placement" (FlawFilterControl renders ABOVE metadata filters; "Clear flaw filter" is separate from "Reset Filters")
    - CLAUDE.md (mirror desktop + mobile; brand-outline for secondary; data-testid)
  </read_first>
  <action>
    Remove the boolean `('blunder'|'mistake')[]` severity toggle block from LibraryFilterPanel (D-01). Change the panel's props from `severityFilter`/`onSeverityChange` to `flawFilter: FlawFilterState`/`onFlawFilterChange: (next: FlawFilterState) => void` (plus an `onClearFlawFilter` for the control's clear affordance). Render `<FlawFilterControl severity={flawFilter.severity} tags={flawFilter.tags} onSeverityChange={...} onTagChange={...} onClear={onClearFlawFilter} />` ABOVE the standard metadata filters (the position the toggle occupied). Keep platform/time-control/rated/recency/opponent untouched. Keep the existing "Reset Filters" button (`brand-outline`) which resets game-metadata filters — the control's "Clear flaw filter" is separate and clears only flaw state. This single panel is rendered by both GamesTab and FlawsTab (FlawsTab updated in Plan 07 to use the panel-hosted control; if Plan 07 rendered it inline, switch FlawsTab to the panel here).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The boolean severity toggle is gone from LibraryFilterPanel; metadata filters remain
    - Panel accepts `flawFilter`/`onFlawFilterChange`/`onClearFlawFilter` and renders FlawFilterControl above metadata filters
    - "Reset Filters" (brand-outline) preserved; separate from "Clear flaw filter"
    - FlawsTab uses LibraryFilterPanel to host FlawFilterControl (no inline rendering); both Games and Flaws tabs share the same panel-hosted control
    - `cd frontend && npx tsc --noEmit && npm run lint` exits 0
  </acceptance_criteria>
  <done>LibraryFilterPanel hosts FlawFilterControl in place of the boolean toggle; metadata filters + reset intact.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate GamesTab to useFlawFilterStore (D-01/D-04)</name>
  <files>frontend/src/pages/library/GamesTab.tsx, frontend/src/pages/library/__tests__/GamesTab.test.tsx</files>
  <read_first>
    - frontend/src/pages/library/GamesTab.tsx lines 57-99 + the query hook call (local severityFilter state + handleSeverityChange/handlePendingSeverityChange to replace; how appliedFilters drive useLibraryGames)
    - frontend/src/hooks/useFlawFilterStore.ts (Plan 07 store)
    - frontend/src/hooks/useLibrary.ts (Plan 07 — useLibraryGames now takes severity+tags; buildLibraryParams extended)
    - frontend/src/components/filters/LibraryFilterPanel.tsx (Task 1 — new props)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §GamesTab.tsx
    - CLAUDE.md (Games tab does NOT URL-sync — D-04; mirror mobile drawer; isError branch)
  </read_first>
  <action>
    Replace the local `const [severityFilter, setSeverityFilter] = useState(...)` (and its pending variants) with `const [flawFilter, setFlawFilter] = useFlawFilterStore()`. Replace `handleSeverityChange`/`handlePendingSeverityChange` with a `handleFlawFilterChange(next: FlawFilterState)` writing to the store and an `handleClearFlawFilter` resetting to DEFAULT_FLAW_FILTER. Pass `flawFilter`/`onFlawFilterChange`/`onClearFlawFilter` to LibraryFilterPanel (Task 1 props). Update the games query to pass both severity and tags: `useLibraryGames(appliedFilters, flawFilter, offset, PAGE_SIZE)` (or the extended signature). Reset to page 1 on flaw-filter change. Games tab does NOT URL-sync (D-04) — no useSearchParams here. Keep the mobile drawer apply-on-close behavior (mirror desktop). Update GamesTab.test.tsx: assert the panel receives flawFilter from the store and that changing it triggers a refetch / page reset (mock useLibraryGames).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run GamesTab && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GamesTab uses `useFlawFilterStore` (no local severityFilter state); passes flawFilter to the panel + query
    - Games query passes both severity and tags to the migrated endpoint
    - Games tab does NOT URL-sync; page resets to 1 on flaw-filter change; mobile parity preserved
    - `cd frontend && npm test -- --run GamesTab && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GamesTab on shared flaw-filter state; severity+tags drive the migrated Games query; no URL sync; mobile parity.</done>
</task>

<task type="auto">
  <name>Task 3: TagChip → navigation deep-link to /library/flaws?tag={TAG} (D-05)</name>
  <files>frontend/src/components/library/TagChip.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/__tests__/TagChip.test.tsx</files>
  <read_first>
    - frontend/src/components/library/TagChip.tsx lines 107-144 (current Radix popover trigger to replace; keep family color/icon/label styling)
    - frontend/src/components/results/LibraryGameCard.tsx (the only consumer of TagChip — confirm and adjust props if gameId becomes optional/unused)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-UI-SPEC.md §"Chip deep-link" (aria-label change, cursor stays pointer, data-testid unchanged, D-05 URL = no game_id)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §LibraryGameCard.tsx
    - CLAUDE.md (semantic <button>, ARIA, data-testid; react-router useNavigate)
  </read_first>
  <action>
    Convert TagChip from the Radix `PopoverPrimitive.Root/Trigger` (display-only) to a navigation `<button type="button">` that calls `useNavigate()(\`/library/flaws?tag=${tag}\`)` (D-05: NO game_id). Change `aria-label` to `Filter flaws by tag: ${tag}` (was the popover definition label). Keep `data-testid="chip-${tag}-${gameId}"` unchanged, keep `cursor-pointer` and the family-color/icon/label visuals. Remove the popover definition body (the chip is navigation now, not an info tooltip). If `gameId` is now only used for the data-testid, keep it as a prop (do not break the test id). Verify LibraryGameCard remains the sole consumer and still passes the needed props. Update TagChip.test.tsx: clicking a chip calls navigate with `/library/flaws?tag={TAG}` (no game_id); aria-label is the new "Filter flaws by tag:" form; data-testid unchanged. Run knip to ensure no dead popover exports remain.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run TagChip && npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - Clicking a TagChip navigates to `/library/flaws?tag={TAG}` with NO game_id (D-05)
    - TagChip is a semantic `<button>` with `aria-label="Filter flaws by tag: {tag}"`; `data-testid="chip-{tag}-{gameId}"` unchanged
    - Popover definition body removed; family-color/icon/label visuals retained
    - `cd frontend && npm test -- --run TagChip && npm run knip` exits 0 (no dead exports)
  </acceptance_criteria>
  <done>Games-card chips deep-link to the all-games pre-filtered Flaws view (no game_id); knip clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip click → client navigation | tag value flows into a client-side route + (downstream) an API query param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-18 | Tampering | crafted tag in deep-link | mitigate | The tag is a known FlawTag value from the card; the destination endpoint (Plan 05) validates `tag` as a Literal (422 on invalid). No authorization decision is made client-side. |
| T-108-19 | Information Disclosure | game_id leakage in URL | mitigate | D-05 explicitly drops game_id from the deep-link — the chip links to all-games `?tag={TAG}` only; no game identifier appears in the URL. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npx tsc --noEmit && npm run knip`.
</verification>

<success_criteria>
- Boolean severity toggle replaced by FlawFilterControl (D-01); GamesTab on shared store (D-04); chips deep-link to /library/flaws?tag= without game_id (D-05).
- Cross-tab selection preserved in memory; Games query passes severity+tags.
- Frontend tests + tsc + lint + knip clean.
</success_criteria>

<output>
Create `.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-08-SUMMARY.md` when done.
</output>
