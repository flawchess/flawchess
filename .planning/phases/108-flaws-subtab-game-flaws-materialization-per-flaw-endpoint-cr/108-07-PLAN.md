---
phase: 108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr
plan: 07
type: execute
wave: 6
depends_on: ["108-05"]
files_modified:
  - frontend/src/types/library.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useFlawFilterStore.ts
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/components/filters/FlawFilterControl.tsx
  - frontend/src/pages/library/FlawsTab.tsx
  - frontend/src/pages/library/LibraryPage.tsx
  - frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx
  - frontend/src/pages/library/__tests__/FlawsTab.test.tsx
autonomous: true
requirements: [D-04, D-06, D-07, D-08]
must_haves:
  truths:
    - "The Flaws subtab is reachable at /library/flaws via a 'Flaws' tab (Import · Games · Flaws · Stats), in both desktop and mobile"
    - "FlawFilterControl renders severity (M+B) + 3 tag-family groups (Timing/Opportunity/Impact), excluding phase tags"
    - "Flaw filter state is shared cross-tab via useFlawFilterStore (mirrors useFilterStore)"
    - "The Flaws tab URL-syncs ?tag=&severity= and a deep-link pre-populates the control (D-04/D-05)"
    - "The flaw list renders one miniboard row per flaw with the marked move + severity/tags, paginated 20/page"
    - "At least one severity stays selected (cannot deselect both)"
  artifacts:
    - path: "frontend/src/hooks/useFlawFilterStore.ts"
      provides: "shared flaw-filter store"
      exports: ["useFlawFilterStore", "DEFAULT_FLAW_FILTER"]
    - path: "frontend/src/components/filters/FlawFilterControl.tsx"
      provides: "severity + tag-family multi-select control"
      exports: ["FlawFilterControl"]
    - path: "frontend/src/pages/library/FlawsTab.tsx"
      provides: "Flaws subtab page (filter + miniboard list + pagination + URL sync)"
      exports: ["FlawsTab"]
  key_links:
    - from: "frontend/src/pages/library/FlawsTab.tsx"
      to: "GET /api/library/flaws"
      via: "useLibraryFlaws → libraryApi.getFlaws"
      pattern: "useLibraryFlaws"
    - from: "frontend/src/pages/library/LibraryPage.tsx"
      to: "/library/flaws"
      via: "Flaws TabsTrigger + route"
      pattern: "tab-flaws"
---

<objective>
Build the Flaws subtab frontend (D-04/D-06/D-07/D-08): the shared `useFlawFilterStore`, the `FlawFilterControl` (severity × tag-family multi-select, per UI-SPEC), the `FlawsTab` page (URL-synced filter + per-flaw miniboard list reusing MiniBoard + marked move + SeverityBadge/TagChip + the shared Pagination), the data layer (`types`, `client.getFlaws`, `useLibraryFlaws`), and the new "Flaws" tab in `LibraryPage`.

Purpose: this is the user-facing Flaws surface — a recent-first, paginable, one-row-per-flawed-position list with the cross-tab flaw filter that deep-links from Games-card chips (Plan 08 wires the chips). Reuses Phase 107 components per D-06.
Output: store + control + page + data layer + tab wiring + vitest tests.
</objective>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols:
- `useFlawFilterStore`, `DEFAULT_FLAW_FILTER`, `FlawFilterState` (frontend store)
- `FlawFilterControl` component + `FlawFilterControlProps`
- `FlawsTab` page component
- `useLibraryFlaws` hook; `libraryApi.getFlaws`; `FlawListItem`/`LibraryFlawsResponse` TS types
- New data-testids: `tab-flaws`, `tab-flaws-mobile`, `flaws-tab-content`, `flaw-filter-control`, `filter-flaw-severity-blunder|mistake`, `filter-flaw-family-tempo|opportunity|impact`, `filter-flaw-tag-{low-clock|impatient|considered|miss|lucky-escape|result-changing|while-ahead}`, `btn-clear-flaw-filter`, `flaw-list`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-CONTEXT.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-UI-SPEC.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer — types, client.getFlaws, useFlawFilterStore, useLibraryFlaws</name>
  <files>frontend/src/types/library.ts, frontend/src/api/client.ts, frontend/src/hooks/useFlawFilterStore.ts, frontend/src/hooks/useLibrary.ts</files>
  <read_first>
    - frontend/src/hooks/useFilterStore.ts (exact useSyncExternalStore pattern to mirror — NO Zustand, zero new deps)
    - frontend/src/hooks/useLibrary.ts lines 1-75 (buildLibraryParams + useLibraryGames — shape to extend with tags + add useLibraryFlaws)
    - frontend/src/api/client.ts (libraryApi.getGames/getFlawStats — multi-value param serialization)
    - frontend/src/types/library.ts (FlawTag/FlawSeverity/GameFlawCard — add FlawListItem/LibraryFlawsResponse mirroring the backend Plan 05 schemas)
    - app/schemas/library.py (FlawListItem/LibraryFlawsResponse — the wire contract to match)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §useFlawFilterStore.ts + §useLibrary.ts + §api/client.ts
  </read_first>
  <action>
    Add TS types `FlawListItem` and `LibraryFlawsResponse` to types/library.ts matching the Plan 05 Pydantic schemas exactly (game_id, ply, fen, move_san, severity, tags, es_before/after, game metadata; flaws/matched_count/offset/limit). Create `useFlawFilterStore.ts` by copying useFilterStore.ts verbatim, with `FlawFilterState { severity: ('blunder'|'mistake')[]; tags: FlawTag[] }` and `DEFAULT_FLAW_FILTER = { severity: ['blunder','mistake'], tags: [] }` (module-level useSyncExternalStore, no Zustand). Add `libraryApi.getFlaws(params)` to client.ts hitting `GET /api/library/flaws`, serializing `tag` and `severity` as multi-value params. Extend `buildLibraryParams` to accept `tags: FlawTag[]` and serialize `tag=...&tag=...` (omit when empty); add `useLibraryFlaws(filters, flawFilter, offset, limit)` mirroring useLibraryGames with queryKey including flawFilter. Narrow Record/array index access per noUncheckedIndexedAccess.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `useFlawFilterStore` exports the store + `DEFAULT_FLAW_FILTER` (severity both, tags empty); no Zustand import
    - `FlawListItem`/`LibraryFlawsResponse` TS types match the backend schema fields
    - `libraryApi.getFlaws` serializes multi-value `tag`/`severity`; `useLibraryFlaws` includes flawFilter in queryKey
    - `cd frontend && npx tsc --noEmit && npm run lint` exits 0
  </acceptance_criteria>
  <done>Data layer in place: shared store, typed flaws response, getFlaws client, useLibraryFlaws hook; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: FlawFilterControl component + vitest test</name>
  <files>frontend/src/components/filters/FlawFilterControl.tsx, frontend/src/components/filters/__tests__/FlawFilterControl.test.tsx</files>
  <read_first>
    - frontend/src/components/library/TagChip.tsx lines 1-145 (family color imports FAM_*/FAM_*_BG, lucide icon mapping, chip shape/height — the selected-tag visual to reuse)
    - frontend/src/lib/theme.ts (FAM_TEMPO/FAM_OPPORTUNITY/FAM_IMPACT + _BG)
    - frontend/src/lib/tagDefinitions.ts (TAG_LABELS/TAG_DEFINITIONS — authoritative labels)
    - frontend/src/pages/library/GamesTab.tsx lines 57-99 (existing severity toggle button pattern to adapt)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-UI-SPEC.md (full FlawFilterControl contract: sections, labels, data-testids, ARIA, at-least-one-severity guard, clear affordance)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §FlawFilterControl.tsx
    - CLAUDE.md (text-sm floor; theme.ts for colors; data-testid + ARIA + semantic <button>; no text-xs)
  </read_first>
  <action>
    Create `FlawFilterControl.tsx` with props `{ severity, tags, onSeverityChange, onTagChange, onClear }`. Render: section label "Show flaws with:"; two severity `<button type="button">` (Blunders/Mistakes) with `aria-pressed`, `data-testid="filter-flaw-severity-blunder|mistake"`, active style `border-toggle-active bg-toggle-active text-toggle-active-foreground`, height `h-11 sm:h-7` — enforce at-least-one-selected (ignore a click that would empty the array, RESEARCH Pitfall 8). Three family groups, each `<div role="group" aria-label="{label} tag filters" data-testid="filter-flaw-family-{tempo|opportunity|impact}">`: Timing (low-clock/impatient/considered), Opportunity (miss/lucky-escape), Impact (result-changing/while-ahead). Tag `<button type="button">` render like a TagChip (same lucide icon + TAG_LABELS), `aria-pressed`, `aria-label="Filter flaws by tag: {tag}"`, `data-testid="filter-flaw-tag-{tag}"`; selected = family color via style prop (FAM_*/FAM_*_BG), unselected = `border-border bg-inactive-bg text-muted-foreground`; `rounded-full px-3 py-0.5`, height `h-11 sm:h-7`, flex-wrap. EXCLUDE phase tags (opening/middlegame/endgame) — Pitfall 5. Show "Clear flaw filter" link (`data-testid="btn-clear-flaw-filter"`, `aria-label="Clear all flaw filter selections"`) only when non-default (any tag, or severity not both). Root `data-testid="flaw-filter-control"`. Use `text-sm` floor everywhere. Add the vitest test: renders all 3 family groups + 7 tag buttons + 2 severity buttons, toggling a tag calls onTagChange, deselecting the last severity is prevented, clear shows only when non-default and calls onClear.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run FlawFilterControl</automated>
  </verify>
  <acceptance_criteria>
    - `FlawFilterControl` renders severity (2) + 3 family groups (7 tag buttons); phase tags absent
    - At-least-one-severity guard prevents deselecting both
    - Selected tags use family color from theme.ts; all interactive elements have data-testid + ARIA; text-sm floor
    - Clear link appears only on non-default state and resets to default
    - `cd frontend && npm test -- --run FlawFilterControl` exits 0
  </acceptance_criteria>
  <done>FlawFilterControl matches the UI-SPEC contract (severity + family groups, ARIA/testids, guards, clear); test green.</done>
</task>

<task type="auto">
  <name>Task 3: FlawsTab page (filter + miniboard list + pagination + URL sync) and LibraryPage tab</name>
  <files>frontend/src/pages/library/FlawsTab.tsx, frontend/src/pages/library/LibraryPage.tsx, frontend/src/pages/library/__tests__/FlawsTab.test.tsx</files>
  <read_first>
    - frontend/src/pages/library/GamesTab.tsx (full structure to mirror: SidebarLayout, LibraryFilterPanel, Drawer mobile, isError/loading/data chain, page reset on filter change)
    - frontend/src/pages/library/LibraryPage.tsx lines 38-143 (activeTab detection + desktop AND mobile Tabs blocks — add a 'flaws' branch + trigger + content in BOTH)
    - frontend/src/components/board/MiniBoard.tsx (MiniBoardProps + arrow overlay — the per-flaw row board reusing the Openings-Insights miniboard + marked move)
    - frontend/src/components/library/SeverityBadge.tsx + TagChip.tsx (display-only badges for the row)
    - frontend/src/components/results/Pagination.tsx (PaginationProps — reuse, page size 20)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-UI-SPEC.md (layout desktop+mobile, copywriting, empty/error states, URL sync, deep-link landing, AlertTriangle icon, tab order Import·Games·Flaws·Stats)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §FlawsTab.tsx + §LibraryPage.tsx
    - CLAUDE.md (mandatory isError branch; mirror desktop AND mobile; data-testid; text-sm floor)
  </read_first>
  <action>
    Create `FlawsTab.tsx` mirroring GamesTab: SidebarLayout with LibraryFilterPanel (rendering FlawFilterControl — Plan 08 modifies the panel to accept it; for THIS plan render FlawFilterControl directly in the sidebar/drawer if the panel change is not yet present, or pass through — coordinate via depends_on Plan 08 being LATER, so render FlawFilterControl inline here using useFlawFilterStore). Use `useFlawFilterStore` + `useFilterStore` (shared game-metadata) + `useLibraryFlaws`. URL sync (D-04, FlawsTab only): on mount, if `?tag=`/`?severity=` present, initialize the store from them (only when params present — RESEARCH OQ3, do not overwrite an existing in-memory selection on plain navigation); on store change, `setSearchParams(..., { replace: true })` (default state → clean URL). Render `<section aria-label="Flaw results" data-testid="flaw-list">` of per-flaw rows: each row = LazyMiniBoard(fen, marked move from move_san) + SeverityBadge + TagChip(s) + game metadata (opponent/date/result). Matched-count row ("{n} flaw{s} matched"). MANDATORY isError branch ("Failed to load flaws. Something went wrong. Please try again in a moment."). Empty states per UI-SPEC (no flaws matched / no analyzed games / no games imported). Reuse Pagination (page size 20, reset to offset 0 on filter change, scroll-to-top on page change). Root `data-testid="flaws-tab-content"`. In LibraryPage.tsx: add `flaws` to activeTab detection, add the `<TabsTrigger value="flaws" data-testid="tab-flaws">` with AlertTriangle icon between Games and Stats, and `<TabsContent value="flaws"><FlawsTab/></TabsContent>` — in BOTH the desktop and mobile Tabs blocks (mobile trigger `data-testid="tab-flaws-mobile"`). Add the `/library/flaws` route mapping. Add a vitest test for FlawsTab: deep-link `?tag=result-changing` pre-selects that tag; isError branch renders the error copy; list renders rows from mocked useLibraryFlaws.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run FlawsTab && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Flaws tab reachable at /library/flaws; trigger present in BOTH desktop and mobile Tabs blocks; order Import·Games·Flaws·Stats
    - FlawsTab renders per-flaw miniboard rows (fen + marked move + severity/tags + metadata), paginated 20
    - URL sync: deep-link `?tag=X` pre-selects the tag; default state yields clean URL; replace-state (no history pollution)
    - Mandatory isError branch present; empty states match UI-SPEC copy
    - `cd frontend && npm test -- --run FlawsTab && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Flaws subtab live: filter + recent-first miniboard list + pagination + URL-synced deep-link; desktop+mobile parity; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → filter state | ?tag=/?severity= from a deep-link become filter state and an API request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-16 | Tampering | malformed URL filter params | mitigate | The backend (Plan 05) validates severity/tag as Literal (422 on invalid); the frontend renders empty results for unmatched combos. No client-side trust of param values for authorization. |
| T-108-17 | Information Disclosure | flaw data in URL | accept | Only tag/severity selectors appear in the URL — no user IDs, no game IDs (D-05 dropped game_id), no PII. The data fetch is user-scoped server-side. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npm test -- --run && npx tsc --noEmit`. Then `cd frontend && npm run knip` (remove any dead exports).
</verification>

<success_criteria>
- Flaws subtab: shared store, FlawFilterControl, recent-first paginated miniboard list, URL-synced deep-link, desktop+mobile parity.
- Reuses Phase 107 MiniBoard/SeverityBadge/TagChip/Pagination (D-06); excludes phase tags from the filter.
- Frontend tests + tsc + lint + knip clean.
</success_criteria>

<output>
Create `.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-07-SUMMARY.md` when done.
</output>
