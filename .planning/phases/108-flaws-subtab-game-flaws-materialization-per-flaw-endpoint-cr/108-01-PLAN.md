---
phase: 108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/game_flaw.py
  - app/models/__init__.py
  - alembic/versions/__ADD_game_flaws_table.py
  - tests/test_game_flaws_model.py
autonomous: true
requirements: [D-02, D-03, D-10]
must_haves:
  truths:
    - "game_flaws table exists with composite PK (user_id, game_id, ply)"
    - "Deleting a user or a game CASCADE-removes that user's/game's game_flaws rows"
    - "game_flaws stores M+B only (severity SmallInteger 1=mistake, 2=blunder) — never inaccuracy"
    - "Each row carries the display payload (es_before, es_after, move_san, fen) so the Flaws list never replays PGN"
  artifacts:
    - path: "app/models/game_flaw.py"
      provides: "GameFlaw ORM model"
      contains: "class GameFlaw"
    - path: "alembic/versions/__ADD_game_flaws_table.py"
      provides: "Migration creating game_flaws table + ix_game_flaws_user_severity"
      contains: "create_table"
    - path: "tests/test_game_flaws_model.py"
      provides: "Model + FK CASCADE + composite PK round-trip test"
  key_links:
    - from: "app/models/game_flaw.py"
      to: "games.id / users.id"
      via: "ForeignKey ondelete=CASCADE"
      pattern: "ondelete=.CASCADE."
---

<objective>
Create the derived `game_flaws` materialization table (D-02/D-03/D-10): the SQLAlchemy ORM model and the Alembic migration. This is the storage layer the rest of the phase reads and writes. M+B-only, composite PK `(user_id, game_id, ply)`, typed tag-family columns, plus the display payload (`es_before`, `es_after`, `move_san`, `fen`) so the Flaws-tab miniboard renders without re-parsing PGN.

Purpose: SEED-038 locks this schema; materializing the Phase 105 classifier output is the prerequisite for the Games-tab migration (D-02), the per-flaw endpoint (D-05/D-07), and the cross-tab filter (SEED-038).
Output: `app/models/game_flaw.py`, the Alembic migration, model registration, and a model round-trip test.
</objective>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (source-grounding drift pass should treat as new, not undefined):
- `app/models/game_flaw.py` → `class GameFlaw` (table `game_flaws`)
- New columns: `user_id`, `game_id`, `ply` (composite PK), `severity`, `tempo`, `phase`, `is_miss`, `is_lucky_escape`, `is_while_ahead`, `is_result_changing`, `es_before`, `es_after`, `move_san`, `fen`
- New index `ix_game_flaws_user_severity`
- New Alembic revision adding the `game_flaws` table
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-CONTEXT.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md
@.planning/seeds/SEED-038-flaw-filter-and-game-flaws-materialization.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the GameFlaw ORM model</name>
  <files>app/models/game_flaw.py, app/models/__init__.py</files>
  <read_first>
    - app/models/game_position.py (analog — composite PK with FKs + ondelete CASCADE, Mapped/mapped_column style, Index in __table_args__)
    - app/models/__init__.py (model registration pattern — register GameFlaw so Alembic autogenerate/metadata sees it)
    - app/services/flaws_service.py lines 75-125 (FlawRecord fields + FlawSeverity/FlawTag/TempoTag Literals the columns encode)
    - .planning/seeds/SEED-038-flaw-filter-and-game-flaws-materialization.md (the locked schema — typed columns, NOT bitmask, NOT TEXT[])
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §game_flaw.py (the exact column list to copy)
  </read_first>
  <action>
    Create `class GameFlaw(Base)` with `__tablename__ = "game_flaws"`. Composite PK columns: `user_id` (ForeignKey users.id ondelete="CASCADE", primary_key=True), `game_id` (ForeignKey games.id ondelete="CASCADE", primary_key=True, index=True), `ply` (SmallInteger, primary_key=True). Tag-family typed columns: `severity` (SmallInteger, not null — 1=mistake, 2=blunder, ordered so `severity >= threshold` works; NEVER 0=inaccuracy per D-03), `tempo` (SmallInteger, nullable — 0=low-clock, 1=impatient, 2=considered, NULL when no clock data), `phase` (SmallInteger, not null — 0/1/2 denormalized from game_positions.phase), `is_miss`/`is_lucky_escape`/`is_while_ahead`/`is_result_changing` (Boolean, not null). Display payload: `es_before` (Float, not null), `es_after` (Float, not null), `move_san` (String, nullable), `fen` (String, not null — board_fen() at this ply; resolves the RESEARCH open question YES because the Flaws miniboard needs FEN and storing it avoids O(page_size * game_length) PGN replay per request). Add `__table_args__ = (Index("ix_game_flaws_user_severity", "user_id", "severity"),)`. Use `SmallInteger` for all ordered enumerations (CLAUDE.md DB rule). Register `GameFlaw` in `app/models/__init__.py` so the declarative metadata includes it.
  </action>
  <verify>
    <automated>uv run ty check app/models/game_flaw.py</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/game_flaw.py` contains `class GameFlaw` with `__tablename__ = "game_flaws"`
    - Composite PK is exactly `(user_id, game_id, ply)`; both FKs use `ondelete="CASCADE"`
    - `severity` and `phase` are non-nullable SmallInteger; `tempo` is nullable SmallInteger
    - Four boolean columns (is_miss, is_lucky_escape, is_while_ahead, is_result_changing) are non-nullable
    - `fen` is a non-nullable String column; `move_san` is nullable
    - `GameFlaw` is importable from `app.models` (registered in __init__)
    - `uv run ty check app/models/game_flaw.py` exits 0
  </acceptance_criteria>
  <done>GameFlaw model defined with the SEED-038 schema, FK CASCADE on both refs, registered for metadata; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Alembic migration for game_flaws</name>
  <files>alembic/versions/__ADD_game_flaws_table.py</files>
  <read_first>
    - alembic/versions/20260603_153628_f4d88c3659c6_gp_natural_composite_pk_seed_035.py (analog — header pattern, create_table, autocommit_block for CONCURRENTLY index, downgrade)
    - app/models/game_flaw.py (the model just created — column types/nullability must match the migration exactly)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §migration (the exact op.create_table body)
  </read_first>
  <action>
    Generate the revision with `uv run alembic revision --autogenerate -m "add game_flaws table"` to get the correct filename (YYYYMMDD_HHMMSS_<hash>) and `down_revision` set to the current head, then hand-edit the body to the SEED-038 schema (do NOT trust autogenerate column ordering blindly — verify every column). `upgrade()`: `op.create_table("game_flaws", ...)` with columns user_id/game_id (sa.Integer, not null), ply/severity/tempo/phase (sa.SmallInteger; tempo nullable), is_* (sa.Boolean, not null), es_before/es_after (sa.Float, not null), move_san (sa.String, nullable), fen (sa.String, not null, server_default=""), two ForeignKeyConstraint with ondelete="CASCADE", PrimaryKeyConstraint(user_id, game_id, ply). Then create `ix_game_flaws_user_severity` on (user_id, severity) inside `with op.get_context().autocommit_block(): ... postgresql_concurrently=True` (CONCURRENTLY cannot run in a transaction). `downgrade()`: `op.drop_table("game_flaws")`. Inline all literals — never import live app constants in a migration. Rename the autogenerated filename placeholder if needed; the planned filename token is `__ADD_game_flaws_table.py` (replace with the real timestamp+hash).
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - The migration file exists in `alembic/versions/` with a real timestamp_hash filename and `down_revision` = prior head
    - `uv run alembic upgrade head` creates the `game_flaws` table and `ix_game_flaws_user_severity` index without error
    - `uv run alembic downgrade -1` drops the table cleanly; re-upgrade succeeds (round-trip)
    - Both FKs declared with `ondelete="CASCADE"`; CONCURRENTLY index built inside autocommit_block
  </acceptance_criteria>
  <done>Migration creates game_flaws + the (user_id, severity) index, upgrade/downgrade round-trips cleanly against dev DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Model round-trip + FK CASCADE test (Wave 0 stub for downstream)</name>
  <files>tests/test_game_flaws_model.py</files>
  <behavior>
    - Inserting a GameFlaw row for an existing user+game persists and reads back with correct typed columns
    - Composite PK rejects a duplicate (user_id, game_id, ply)
    - Deleting the parent game CASCADE-removes its game_flaws rows
    - Deleting the parent user CASCADE-removes that user's game_flaws rows
  </behavior>
  <read_first>
    - tests/conftest.py (per-run DB fixtures, async session fixture, user/game factory helpers — reuse existing fixtures, do NOT reset the dev DB)
    - tests/test_library_service.py (analog for how existing library tests build a user + analyzed game + positions)
    - app/models/game_flaw.py (the model under test)
  </read_first>
  <action>
    Create `tests/test_game_flaws_model.py` using the existing async-session + user/game fixtures from conftest (mirror how test_library_service.py constructs a user and a game). Assert: (a) a GameFlaw row inserts and round-trips all columns (severity=2, tempo=0, phase=1, the four booleans, es_before/es_after, move_san, fen); (b) inserting a second row with the same (user_id, game_id, ply) raises IntegrityError; (c) deleting the game removes its game_flaws rows (CASCADE); (d) deleting the user removes that user's game_flaws rows (CASCADE). This file is the Wave 0 dependency for the materialization round-trip work in Plan 02.
  </action>
  <verify>
    <automated>uv run pytest tests/test_game_flaws_model.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_game_flaws_model.py -x` exits 0
    - Test covers insert round-trip, composite-PK duplicate rejection, game-CASCADE delete, user-CASCADE delete
    - Test uses existing conftest fixtures (no dev DB reset)
  </acceptance_criteria>
  <done>Model test green: round-trip + composite PK + both CASCADE paths verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → game_flaws table | All writes/reads of derived flaw rows; rows are user-owned data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Information Disclosure | game_flaws ownership | mitigate | `user_id` is a PK column + FK with ondelete=CASCADE; all downstream queries (Plans 03/04/05) MUST filter `GameFlaw.user_id == user_id`. This plan establishes the user-scoping column. |
| T-108-02 | Tampering | orphaned rows on delete | mitigate | FK ondelete=CASCADE on both user_id and game_id guarantees no orphan flaw rows survive a user/game delete (verified by Task 3 CASCADE tests). |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: skipped — zero new deps). No install task exists. |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `uv run ty check app/ tests/` (zero errors) then `uv run pytest tests/test_game_flaws_model.py -x`.
</verification>

<success_criteria>
- game_flaws table created by migration (upgrade/downgrade round-trips).
- GameFlaw model registered, ty-clean, M+B-only severity encoding, both FKs CASCADE.
- Model round-trip + CASCADE test green.
</success_criteria>

<output>
Create `.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-01-SUMMARY.md` when done.
</output>
