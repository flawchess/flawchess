---
phase: 108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr
plan: 03
type: execute
wave: 3
depends_on: ["108-01", "108-02"]
files_modified:
  - app/repositories/library_repository.py
  - app/repositories/query_utils.py
  - tests/test_flaw_predicate.py
autonomous: true
requirements: [D-02, D-03]
must_haves:
  truths:
    - "One shared predicate builder produces the WHERE clauses used by BOTH the Games EXISTS filter and the Flaws SELECT"
    - "Tag families combine OR-within-family and AND-across-families (single-flaw EXISTS semantics)"
    - "Severity uses MIN-threshold logic (['mistake'] matches mistakes OR worse)"
    - "Phase tags (opening/middlegame/endgame) are NOT filter predicates"
    - "Every game_flaws-backed clause is user-scoped"
  artifacts:
    - path: "app/repositories/library_repository.py"
      provides: "build_flaw_filter_clauses + flaw_exists_from_table"
      exports: ["build_flaw_filter_clauses", "flaw_exists_from_table"]
    - path: "app/repositories/query_utils.py"
      provides: "apply_game_filters updated to game_flaws-backed EXISTS + flaw_tags param"
      contains: "flaw_tags"
    - path: "tests/test_flaw_predicate.py"
      provides: "OR-within / AND-across / single-flaw EXISTS / user-scoping test"
  key_links:
    - from: "app/repositories/query_utils.py"
      to: "app.repositories.library_repository.flaw_exists_from_table"
      via: "flaw_severity + flaw_tags EXISTS path"
      pattern: "flaw_exists_from_table"
---

<objective>
Build the SINGLE shared flaw-filter predicate builder (SEED-038) that both surfaces reuse, and migrate the Games-tab EXISTS path off the on-the-fly window-scan onto `game_flaws`. `build_flaw_filter_clauses` returns the family-aware WHERE clauses (OR within family, AND across families); `flaw_exists_from_table` wraps them in a `game_flaws`-backed EXISTS for the Games tab; `apply_game_filters` is updated to call it and to accept the new `flaw_tags` param.

Purpose: SEED-038 requires the cross-tab unification be enforced in code, not convention — one predicate builder, used by Games `EXISTS` (this plan + Plan 04) and the Flaws `SELECT f.*` (Plan 05). This mirrors how `apply_game_filters` is the single game-filter implementation.
Output: the shared predicate builder + EXISTS wrapper in `library_repository.py`, the `query_utils.py` migration to the table-backed EXISTS, and a predicate unit test.
</objective>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols:
- `app/repositories/library_repository.py` → `build_flaw_filter_clauses`, `flaw_exists_from_table`
- `app/repositories/query_utils.py` → new keyword-only param `flaw_tags: Sequence[str] | None`
- Retires (or adapts): `flaw_exists_subquery`, `_per_ply_drop_subquery`, `_drop_filter`, `_user_ply_filter` window-scan helpers and the SQL↔kernel cross-check test
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-CONTEXT.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md
@.planning/seeds/SEED-038-flaw-filter-and-game-flaws-materialization.md
@.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared predicate builder + game_flaws-backed EXISTS wrapper</name>
  <files>app/repositories/library_repository.py</files>
  <behavior>
    - build_flaw_filter_clauses([], []) returns [] (no filter = match all)
    - severity ["mistake"] yields severity >= 1; ["blunder"] yields severity >= 2; ["mistake","blunder"] yields >= 1
    - tempo tags map to GameFlaw.tempo.in_([...]) (OR within tempo family)
    - opportunity/impact tags map to or_(boolean ==) within family
    - multiple families produce multiple clauses (caller ANDs them = AND across families)
    - phase tags are ignored (never produce a clause)
    - flaw_exists_from_table scopes the EXISTS to GameFlaw.user_id == user_id and GameFlaw.game_id == Game.id
  </behavior>
  <read_first>
    - app/repositories/library_repository.py lines 164-193 (existing flaw_exists_subquery window-scan — the shape to replace; lines 97-160 for _per_ply_drop_subquery/_drop_filter/_user_ply_filter to retire)
    - app/models/game_flaw.py (GameFlaw columns the clauses reference)
    - app/repositories/game_flaws_repository.py (reuse _SEVERITY_INT / _TEMPO_INT or import them; do not duplicate magic numbers)
    - app/services/flaws_service.py lines 75-90 (FlawSeverity / FlawTag Literals for typing)
    - .planning/seeds/SEED-038-flaw-filter-and-game-flaws-materialization.md §"Family-aware boolean logic" + §"Query patterns"
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §library_repository.py (the exact builder body)
  </read_first>
  <action>
    Add `build_flaw_filter_clauses(severity: Sequence[FlawSeverity], tags: Sequence[FlawTag]) -> list[ColumnElement[bool]]`. Severity: if non-empty, append `GameFlaw.severity >= min(_SEVERITY_INT[s] for s in severity)`. Tempo family: collect tags in {low-clock,impatient,considered}; if any, append `GameFlaw.tempo.in_([_TEMPO_INT[t] for t in tempo_tags])`. Opportunity family: collect {miss,lucky-escape}; if any, append `or_(GameFlaw.is_miss.is_(True) for miss, GameFlaw.is_lucky_escape.is_(True) for lucky-escape)`. Impact family: collect {while-ahead,result-changing}; if any, append `or_(...)` of the matching booleans. Phase tags (opening/middlegame/endgame) are NOT handled (per UI-SPEC + RESEARCH Pitfall 5). Return the clause list; the caller ANDs them. Add `flaw_exists_from_table(user_id: int, severity: Sequence[FlawSeverity], tags: Sequence[FlawTag]) -> ColumnElement[bool]`: compute clauses; if empty return `true()`; else return `exists(select(GameFlaw.ply).where(GameFlaw.game_id == Game.id, GameFlaw.user_id == user_id, *clauses))`. Import `_SEVERITY_INT`/`_TEMPO_INT` from `game_flaws_repository` (single source). Retire the now-dead window-scan helpers (`flaw_exists_subquery`, `_per_ply_drop_subquery`, `_drop_filter`, `_user_ply_filter`) once no caller remains (query_utils is updated in Task 2). Use `Sequence` params for ty covariance; `# noqa: E712` only if you use `== True` rather than `.is_(True)`.
  </action>
  <verify>
    <automated>uv run ty check app/repositories/library_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `library_repository.py` exports `build_flaw_filter_clauses` and `flaw_exists_from_table`
    - Severity uses MIN-threshold; tempo uses `.in_`; opportunity/impact use `or_` within family
    - Phase tags produce no clause
    - `flaw_exists_from_table` scopes to `user_id` and `Game.id`; returns `true()` when no filter
    - The dead window-scan helpers are removed (or no longer referenced)
    - `uv run ty check app/repositories/library_repository.py` exits 0
  </acceptance_criteria>
  <done>One shared family-aware predicate builder + a table-backed EXISTS wrapper; window-scan retired; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate apply_game_filters EXISTS to game_flaws + add flaw_tags param</name>
  <files>app/repositories/query_utils.py</files>
  <read_first>
    - app/repositories/query_utils.py lines 11-114 (apply_game_filters signature + the flaw_severity EXISTS block at 103-114)
    - app/repositories/library_repository.py (the new flaw_exists_from_table to call)
    - .planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-PATTERNS.md §query_utils.py
    - CLAUDE.md (apply_game_filters is the single game-filter implementation — keep it the single seam)
  </read_first>
  <action>
    Add a keyword-only param `flaw_tags: Sequence[str] | None = None` to `apply_game_filters` (alongside the existing `flaw_severity` and `user_id`). Replace the existing `flaw_severity` EXISTS block: when `flaw_severity` OR `flaw_tags` is set, require `user_id` (raise ValueError if None, message must not embed variables), cast to the FlawSeverity/FlawTag types, and apply `stmt = stmt.where(flaw_exists_from_table(user_id=user_id, severity=severities, tags=tag_list))` (lazy import from library_repository to avoid a cycle, mirroring the existing lazy import). Drop the old `flaw_exists_subquery` import. Keep semantics identical for severity-only filtering (existing callers pass only flaw_severity).
  </action>
  <verify>
    <automated>uv run ty check app/repositories/query_utils.py && grep -v '^#' app/repositories/query_utils.py | grep -c "flaw_exists_from_table"</automated>
  </verify>
  <acceptance_criteria>
    - `apply_game_filters` accepts keyword-only `flaw_tags: Sequence[str] | None`
    - The EXISTS path calls `flaw_exists_from_table` (not the old window-scan `flaw_exists_subquery`)
    - `flaw_severity`/`flaw_tags` set without `user_id` raises ValueError (no variable in message)
    - `uv run ty check app/repositories/query_utils.py` exits 0; grep count for `flaw_exists_from_table` ≥ 1
  </acceptance_criteria>
  <done>apply_game_filters is the single seam; its flaw EXISTS now reads game_flaws and supports tag filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Predicate builder unit test (Wave 0 stub)</name>
  <files>tests/test_flaw_predicate.py</files>
  <behavior>
    - empty severity + empty tags → no clauses (match all)
    - single-flaw EXISTS: a game with one flaw that is BOTH low-clock AND result-changing matches "low-clock + result-changing"; a game whose low-clock flaw and result-changing flaw are DIFFERENT plies does NOT match (AND across families is per-flaw, not per-game)
    - OR within family: "miss" OR "lucky-escape" matches a flaw with either
    - severity MIN-threshold: ["mistake"] matches a blunder row too
    - user scoping: another user's matching flaw does not satisfy the EXISTS for the requesting user
  </behavior>
  <read_first>
    - tests/test_library_service.py (analog — building users, games, and now game_flaws rows directly)
    - tests/test_game_flaws_model.py (Plan 01 — how to insert game_flaws rows in a test)
    - app/repositories/library_repository.py (build_flaw_filter_clauses + flaw_exists_from_table under test)
    - tests/conftest.py (fixtures; no dev DB reset)
  </read_first>
  <action>
    Create `tests/test_flaw_predicate.py`. Insert game_flaws rows directly (no need to run the classifier) to construct the exact single-flaw / split-flaw scenarios. Assert via running a `select(Game).where(flaw_exists_from_table(...))` query: (a) one-flaw-satisfies-both matches; (b) split-across-two-flaws does NOT match the cross-family combo (the make-or-break single-flaw EXISTS test from SEED-038); (c) OR-within-family for opportunity tags; (d) severity MIN-threshold (["mistake"] matches a blunder); (e) cross-user isolation. Also assert `build_flaw_filter_clauses([], []) == []`. This is the Wave 0 dependency for the Games migration (Plan 04) and the Flaws endpoint (Plan 05).
  </action>
  <verify>
    <automated>uv run pytest tests/test_flaw_predicate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_flaw_predicate.py -x` exits 0
    - Includes the single-flaw-vs-split-flaw EXISTS distinction (AND across families is per-flaw)
    - Includes OR-within-family, severity MIN-threshold, and cross-user isolation cases
  </acceptance_criteria>
  <done>Predicate test green: single-flaw EXISTS semantics, family boolean logic, and user scoping all verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client filter input → SQL predicate | tag/severity selections become SQL WHERE clauses |
| user → game_flaws rows | EXISTS must never cross user boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-06 | Tampering | SQL injection via tag/severity | mitigate | Inputs are validated as `Literal` enums upstream (Plan 05 router) and mapped through `_SEVERITY_INT`/`_TEMPO_INT` dicts + parameterized SQLAlchemy column comparisons — no string interpolation into SQL. |
| T-108-07 | Information Disclosure | cross-user EXISTS leak | mitigate | `flaw_exists_from_table` always includes `GameFlaw.user_id == user_id`; `apply_game_filters` raises if `user_id` is missing when a flaw filter is set. Verified by the cross-user isolation test. |
</threat_model>

<verification>
- `uv run pytest tests/test_flaw_predicate.py -x` then `uv run pytest -n auto -x` (wave merge) then `uv run ty check app/ tests/`.
</verification>

<success_criteria>
- Single shared predicate builder reused by Games EXISTS (this plan/Plan 04) and Flaws SELECT (Plan 05).
- Games-tab EXISTS migrated off the window-scan onto game_flaws; flaw_tags supported.
- Single-flaw EXISTS + family boolean logic + user scoping proven by test.
</success_criteria>

<output>
Create `.planning/phases/108-flaws-subtab-game-flaws-materialization-per-flaw-endpoint-cr/108-03-SUMMARY.md` when done.
</output>
