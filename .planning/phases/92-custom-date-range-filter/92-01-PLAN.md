---
phase: 92-custom-date-range-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/openings.py
  - app/repositories/openings_repository.py
  - app/services/openings_service.py
  - frontend/src/types/position_bookmarks.ts
  - tests/test_openings_time_series.py
autonomous: true
requirements: [P92-R1]
# Synthetic requirement IDs (no formal REQ-XX IDs defined for phase 92):
#   P92-R1 = ROADMAP scope-in item 1 (Bookmark time-series recency removal — D-19)
#   P92-R2 = ROADMAP scope-in item 2 (API contract: from_date/to_date)
#   P92-R3 = ROADMAP scope-in item 3 (FE preset → date conversion, TZ canonicalization)
#   P92-R4 = ROADMAP scope-in item 4 (Custom range UI: 9th item + popover + drawer)
#   P92-R5 = ROADMAP scope-in item 5 (Hook + URL param migration)
#   P92-R6 = ROADMAP scope-in item 6 (Recency → RecencyPreset, UI-only)
#   P92-R7 = ROADMAP scope-in item 7 (LLM insight prompt audit — covered by D-18)
must_haves:
  truths:
    - "Bookmark time-series request no longer accepts a recency field"
    - "TimeSeriesRequest TypeScript shape has no recency field"
    - "TimeSeriesRequest Pydantic schema has no recency field"
    - "All time-series tests still pass without recency fixtures"
  artifacts:
    - path: app/schemas/openings.py
      provides: "TimeSeriesRequest with recency field removed (line ~153)"
      contains: "class TimeSeriesRequest"
    - path: frontend/src/types/position_bookmarks.ts
      provides: "TimeSeriesRequest type with recency field removed"
      contains: "TimeSeriesRequest"
  key_links:
    - from: app/services/openings_service.py
      to: app/repositories/openings_repository.py
      via: "compute_bookmark_time_series — must no longer pass recency_cutoff for the time-series path"
      pattern: "compute_bookmark_time_series|time_series"
---

<objective>
Pre-work for Phase 92: drop the unused `recency` field from the bookmark time-series request shape on both the Pydantic and TypeScript sides, plus the matching service/repository pass-through. This must land before the main wire-format flip (Plan 02) so the new `from_date`/`to_date` shape does not get extended into a request that should not filter by date at all.

Purpose: Implements D-19. Bookmark time-series displays a per-bookmark trend chart; recency was never wired into any UI surface for it.
Output: TimeSeriesRequest no longer references recency in either stack; time-series tests pass without recency fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-custom-date-range-filter/92-CONTEXT.md
@.planning/phases/92-custom-date-range-filter/92-RESEARCH.md
@.planning/phases/92-custom-date-range-filter/92-PATTERNS.md
@CLAUDE.md
@app/schemas/openings.py
@frontend/src/types/position_bookmarks.ts

<interfaces>
<!-- Existing TimeSeriesRequest shape — recency field to be removed -->
<!-- frontend/src/types/position_bookmarks.ts line 52: -->
<!--   recency?: 'week' | 'month' | '3months' | '6months' | 'year' | '3years' | '5years' | 'all' | null; -->
<!-- app/schemas/openings.py ~line 153: TimeSeriesRequest with recency Literal field -->
<!-- (D-19 — verified via RESEARCH.md §Recency Call-Site Audit; field is unused at UI call boundary) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove recency from TimeSeriesRequest (both stacks) + drop pass-through in service/repo</name>
  <files>app/schemas/openings.py, frontend/src/types/position_bookmarks.ts, app/services/openings_service.py, app/repositories/openings_repository.py, tests/test_openings_time_series.py</files>
  <read_first>
    - app/schemas/openings.py (full file — locate TimeSeriesRequest class around line 153; do not touch OpeningsRequest or NextMovesRequest in this plan)
    - frontend/src/types/position_bookmarks.ts (full file — locate `recency?` field at line 52 and the related comment at line 72)
    - app/services/openings_service.py (search for compute_bookmark_time_series and any time_series call sites at lines 295, 313 per RESEARCH.md §Bookmark Time-Series Pre-Work)
    - app/repositories/openings_repository.py (search for time_series functions that accept recency_cutoff)
    - tests/test_openings_time_series.py (full file — locate recency fixture usage)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§frontend/src/types/position_bookmarks.ts and §app/schemas/openings.py rows)
  </read_first>
  <behavior>
    - Test 1: POSTing to the bookmark time-series endpoint with a request body that omits `recency` returns 200 (unchanged behavior, since recency was previously optional)
    - Test 2: POSTing with `recency: "month"` in the body is silently ignored by Pydantic v2 default `extra="ignore"` behavior, OR returns 422 if the schema uses `extra="forbid"`. Use whichever matches the existing schema config — do not introduce stricter validation as part of this task.
    - Test 3: `tests/test_openings_time_series.py` continues to pass after removing all `recency=` fixture args (no other behavior changes).
  </behavior>
  <action>
    Remove the `recency` field from `TimeSeriesRequest` in `app/schemas/openings.py` (around line 153 per RESEARCH.md — verify by reading; do NOT touch the `recency` field in `OpeningsRequest` at line 39 or `NextMovesRequest` at line 215, which are handled in Plan 02). Remove the `recency?` field declaration from the `TimeSeriesRequest` TypeScript interface at `frontend/src/types/position_bookmarks.ts:52`, and update or remove the related "recency window" comment at line 72 (rewrite to match remaining filter semantics or delete if no longer accurate). In `app/services/openings_service.py`, find every `compute_bookmark_time_series` (or equivalently named) function and remove the `recency_cutoff` argument from any call into the time-series repository function — verify call sites RESEARCH.md flagged around lines 295, 313. In `app/repositories/openings_repository.py`, drop the `recency_cutoff` parameter from the time-series repository function signature (NOT from any other repository functions — those are handled in Plan 02). Update `tests/test_openings_time_series.py`: remove any `recency=` or `recency_cutoff=` keyword args from fixtures and assertions. Do NOT introduce `from_date`/`to_date` for the time-series path — per D-19 the time-series endpoint should not filter by date at all. Implements D-19; addresses synthetic requirement P92-R1.
  </action>
  <verify>
    <automated>uv run ruff format app/schemas/openings.py app/services/openings_service.py app/repositories/openings_repository.py tests/test_openings_time_series.py &amp;&amp; uv run ruff check app/schemas/openings.py app/services/openings_service.py app/repositories/openings_repository.py tests/test_openings_time_series.py &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest tests/test_openings_time_series.py -x &amp;&amp; cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recency" app/schemas/openings.py` shows no match inside the `TimeSeriesRequest` class body (matches in `OpeningsRequest` and `NextMovesRequest` are expected and untouched)
    - `grep -n "recency" frontend/src/types/position_bookmarks.ts` returns no matches
    - `grep -n "recency_cutoff\|recency=" app/services/openings_service.py` shows no matches inside any function named `*time_series*`
    - `grep -n "recency_cutoff" app/repositories/openings_repository.py` shows no matches inside any function named `*time_series*` (matches in other functions are expected and untouched — Plan 02 handles them)
    - `uv run pytest tests/test_openings_time_series.py -x` passes
    - `uv run ty check app/ tests/` passes with zero errors
    - `cd frontend && npx tsc --noEmit` passes
    - No `data-testid` regressions in UI surfaces that consume time-series (none should exist — confirmed by RESEARCH.md UI exposure audit)
  </acceptance_criteria>
  <done>The time-series request shape no longer mentions recency in either stack; downstream Plan 02 can safely flip the OpeningsRequest/NextMovesRequest shapes without accidentally extending date params into time-series.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (time-series endpoint) | Removes one optional field; no new untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-01 | Tampering | TimeSeriesRequest schema | accept | Removing an optional field reduces attack surface; default Pydantic `extra="ignore"` (verify schema config) silently drops any client-supplied `recency`. |
| T-92-01-02 | DoS | time-series query | accept | No change to query shape; removing the field cannot widen the resulting SQL. |
</threat_model>

<verification>
- `grep -n "recency" app/schemas/openings.py | grep -i "TimeSeries"` returns no hits
- `grep -n "recency" frontend/src/types/position_bookmarks.ts` returns no hits
- `uv run pytest tests/test_openings_time_series.py -x` passes
- `uv run ty check app/ tests/` passes
- `cd frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
TimeSeriesRequest has no recency field in either stack; the time-series service/repo call path no longer passes a recency cutoff; existing time-series tests pass without recency fixtures.
</success_criteria>

<output>
Create `.planning/phases/92-custom-date-range-filter/92-01-SUMMARY.md` when done
</output>
