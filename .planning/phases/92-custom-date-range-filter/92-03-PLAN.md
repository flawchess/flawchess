---
phase: 92-custom-date-range-filter
plan: 03
type: execute
wave: 3
depends_on: [92-02]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/types/stats.ts
  - frontend/src/lib/recency.ts
  - frontend/src/lib/__tests__/recency.test.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useOpenings.ts
  - frontend/src/hooks/useEndgames.ts
  - frontend/src/hooks/useEndgameInsights.ts
  - frontend/src/hooks/useOpeningInsights.ts
  - frontend/src/hooks/useStats.ts
  - frontend/src/hooks/useNextMoves.ts
  - frontend/src/hooks/useFilterStore.ts
  - frontend/src/components/filters/FilterPanel.tsx
  - frontend/src/pages/Openings.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/GlobalStats.tsx
  - frontend/src/pages/openings/GamesTab.tsx
  - frontend/src/components/insights/OpeningInsightsBlock.tsx
  - frontend/src/components/insights/EndgameInsightsBlock.tsx
  - frontend/src/components/charts/EndgameEloTimelineSection.tsx
  - frontend/src/hooks/__tests__/useEndgameInsights.test.tsx
  - frontend/src/hooks/__tests__/useOpeningInsights.test.tsx
  - frontend/src/components/insights/OpeningInsightsBlock.test.tsx
  - frontend/src/components/insights/__tests__/EndgameInsightsBlock.test.tsx
autonomous: true
requirements: [P92-R3, P92-R5, P92-R6]
# Synthetic requirement IDs — see Plan 01 frontmatter for full scheme.
# This is the atomic frontend type/hook migration. Splitting causes tsc cascade with no useful intermediate state.
must_haves:
  truths:
    - "TypeScript Recency type is renamed to RecencyPreset with a UI-only comment"
    - "frontend/src/lib/recency.ts exports presetToDates, dateToWire, and a helper that produces the wire shape { from_date?: string; to_date?: string }"
    - "presetToDates is memoized on (preset, today-YYYY-MM-DD-local) so TanStack Query keys stay stable within a calendar day"
    - "All SEVEN consuming hooks pass from_date/to_date instead of recency: useOpenings, useEndgames, useEndgameInsights, useOpeningInsights, useStats's useRatingHistory, useStats's useGlobalStats, useStats's useMostPlayedOpenings, useStats's useBookmarkPhaseEntryMetrics, useNextMoves"
    - "FilterState gains a customRange: { from?: Date; to?: Date } | null field, and filters.recency type becomes RecencyPreset | 'custom' | null"
    - "frontend tests pass (vitest)"
    - "frontend type-check (npx tsc --noEmit) passes"
    - "knip passes (no dead exports introduced)"
  artifacts:
    - path: frontend/src/lib/recency.ts
      provides: "presetToDates and dateRangeToWireParams utilities"
      contains: "presetToDates"
      min_lines: 50
    - path: frontend/src/lib/__tests__/recency.test.ts
      provides: "Unit tests for presetToDates memoization and wire format"
      contains: "presetToDates"
    - path: frontend/src/types/api.ts
      provides: "RecencyPreset type with UI-only comment"
      contains: "RecencyPreset"
  key_links:
    - from: frontend/src/components/filters/FilterPanel.tsx
      to: frontend/src/hooks/useFilterStore.ts
      via: "FilterState shape with customRange sibling field + recency: RecencyPreset | 'custom' | null"
      pattern: "customRange"
    - from: frontend/src/lib/recency.ts
      to: "every consuming hook"
      via: "dateRangeToWireParams(resolveDateRange(filters)) spread into request body"
      pattern: "dateRangeToWireParams|presetToDates"
---

<objective>
Atomic frontend type + hook migration: rename `Recency` to `RecencyPreset` with a UI-only comment, create the shared `frontend/src/lib/recency.ts` utility (mirroring `lib/opponentStrength.ts` conventions), add a `customRange` sibling field to `FilterState`, and migrate all SEVEN consuming hooks (the six listed in D-13 PLUS the hidden seventh `useBookmarkPhaseEntryMetrics` in `useStats.ts:66` — flagged by RESEARCH.md §Pitfall 4 / quality_gate).

This is intentionally atomic — splitting would leave the type system broken (tsc cascade) and the FilterPanel UI in Plan 04 would target shifting ground. The actual Calendar UI lands in Plan 04; this plan delivers the plumbing.

Implements D-11 (rename), D-12 (presetToDates utility), D-13 (six+1 hooks), D-14 (URL param audit — no-op per RESEARCH.md, but enforced via grep); addresses synthetic requirements P92-R3, P92-R5, P92-R6.

Purpose: Frontend now speaks the new wire shape end-to-end; FilterState carries a custom range that the UI in Plan 04 will set.
Output: All frontend code compiles under tsc, vitest passes, knip is clean. The Calendar UI is not yet wired (Plan 04), but the data path is fully operational with the existing 8 preset values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/92-custom-date-range-filter/92-CONTEXT.md
@.planning/phases/92-custom-date-range-filter/92-RESEARCH.md
@.planning/phases/92-custom-date-range-filter/92-PATTERNS.md
@.planning/phases/92-custom-date-range-filter/92-02-SUMMARY.md
@CLAUDE.md
@frontend/src/types/api.ts
@frontend/src/lib/opponentStrength.ts
@frontend/src/lib/__tests__/opponentStrength.test.ts
@frontend/src/api/client.ts
@frontend/src/hooks/useStats.ts
@frontend/src/hooks/useOpenings.ts
@frontend/src/hooks/useFilterStore.ts
@frontend/src/components/filters/FilterPanel.tsx

<interfaces>
<!-- Existing Recency type — frontend/src/types/api.ts:38 -->
<!--   export type Recency = 'week' | 'month' | '3months' | '6months' | 'year' | '3years' | '5years' | 'all'; -->
<!-- Target: -->
<!--   /** UI-only preset, not sent to the API. The wire shape uses from_date/to_date instead. */ -->
<!--   export type RecencyPreset = 'week' | 'month' | '3months' | '6months' | 'year' | '3years' | '5years' | 'all'; -->

<!-- Existing FilterState shape (frontend/src/components/filters/FilterPanel.tsx:17-30): -->
<!--   recency: Recency | null; -->
<!-- Target additions: -->
<!--   recency: RecencyPreset | 'custom' | null;   // 'custom' = look at customRange -->
<!--   customRange: { from?: Date; to?: Date } | null; -->

<!-- presetToDates signature (from RESEARCH.md §Pattern 3 + 92-PATTERNS.md §recency.ts): -->
<!--   export function presetToDates(preset: RecencyPreset | null, now?: Date): { from?: Date; to?: Date } -->
<!--   export function dateToWire(d: Date | undefined): string | undefined  // 'YYYY-MM-DD' via date-fns format -->
<!--   export function dateRangeToWireParams(range: { from?: Date; to?: Date }): { from_date?: string; to_date?: string } -->
<!--   export function resolveDateRange(filters: FilterState): { from?: Date; to?: Date } -->
<!--     // returns filters.customRange for recency='custom', presetToDates(filters.recency) otherwise -->

<!-- Existing buildFilterParams (frontend/src/api/client.ts:75-97): replaces -->
<!--   if (params.recency && params.recency !== 'all') result.recency = params.recency; -->
<!-- with -->
<!--   if (params.from_date) result.from_date = params.from_date; -->
<!--   if (params.to_date) result.to_date = params.to_date; -->

<!-- Existing useRatingHistory queryKey (useStats.ts:14-20): -->
<!--   ['ratingHistory', normalizedRecency, platform, opponentType, gap.min, gap.max] -->
<!-- Target: -->
<!--   ['ratingHistory', dateParams.from_date ?? null, dateParams.to_date ?? null, platform, opponentType, gap.min, gap.max] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install date-fns (verify package legitimacy first), create recency.ts utility + tests, rename Recency → RecencyPreset</name>
  <files>frontend/package.json, frontend/src/types/api.ts, frontend/src/lib/recency.ts, frontend/src/lib/__tests__/recency.test.ts</files>
  <read_first>
    - frontend/package.json
    - frontend/src/types/api.ts (full file, focus line 38)
    - frontend/src/lib/opponentStrength.ts (full file — analog for module shape)
    - frontend/src/lib/__tests__/opponentStrength.test.ts (full file — analog for test shape)
    - frontend/src/lib/relativeDate.ts (for JSDoc preamble pattern)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§recency.ts, §recency.test.ts, §types/api.ts)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Standard Stack, §Package Legitimacy Audit, §Pitfall 1)
  </read_first>
  <behavior>
    - Test 1: `presetToDates('week')` returns `{ from: Date, to: Date }` with `from = startOfDay(now - 1 week)` and `to = endOfDay(now)`
    - Test 2: `presetToDates('all')` returns `{}`
    - Test 3: `presetToDates(null)` returns `{}`
    - Test 4: Two calls to `presetToDates('week')` within the same calendar day return the SAME object reference (===) — cache hit
    - Test 5: `presetToDates('week', new Date('2026-05-21T12:00:00'))` and `presetToDates('week', new Date('2026-05-22T12:00:00'))` return DIFFERENT object references — cache miss across day boundary
    - Test 6: `dateToWire(undefined)` returns `undefined`
    - Test 7: `dateToWire(new Date(2026, 2, 1))` returns `'2026-03-01'` (no timezone leak — local-day format, not UTC ISO)
    - Test 8: `dateRangeToWireParams({})` returns `{}` (omits both keys when no bounds)
    - Test 9: `dateRangeToWireParams({ from: new Date(2026, 2, 1) })` returns `{ from_date: '2026-03-01' }` (no `to_date` key)
    - Test 10: `dateRangeToWireParams({ from: new Date(2026, 2, 1), to: new Date(2026, 3, 1) })` returns `{ from_date: '2026-03-01', to_date: '2026-04-01' }`
  </behavior>
  <action>
    **Step 1 — Package install (run the legitimacy gate first):** Before running `npm install`, verify `date-fns` on https://www.npmjs.com/package/date-fns matches RESEARCH.md §Package Legitimacy Audit (8+ yrs old, ~25M weekly downloads, source github.com/date-fns/date-fns). If verified, run `cd frontend && npm install date-fns@latest` (Plan 04 will install react-day-picker via the shadcn add command, since that also writes calendar.tsx). Do NOT install react-day-picker in this plan — it's not used in the utility or hook code, only in the Calendar UI.

    **Step 2 — Rename Recency → RecencyPreset (frontend/src/types/api.ts:38):** Replace the existing `export type Recency = ...` with `/** UI-only preset, not sent to the API. The wire shape uses from_date/to_date instead. */\nexport type RecencyPreset = 'week' | 'month' | '3months' | '6months' | 'year' | '3years' | '5years' | 'all';`. Do NOT add `'custom'` to the union — `'custom'` is a marker on `FilterState.recency` (added in Task 2), not a preset value.

    **Step 3 — Create frontend/src/lib/recency.ts:** Mirror `opponentStrength.ts` shape exactly. Imports: `format, startOfDay, endOfDay, subDays, subWeeks, subMonths, subYears` from `'date-fns'`; type-only `import type { RecencyPreset } from '@/types/api'`. JSDoc preamble citing Phase 92 and explaining the `(preset, today-string)` memoization rationale (per relativeDate.ts pattern at lines 1-10).

    Exports:
    - `presetToDates(preset: RecencyPreset | null, now: Date = new Date()): { from?: Date; to?: Date }` — returns `{}` when `preset === null || preset === 'all'`; otherwise computes `to = endOfDay(now)` and `from = startOfDay(_subForPreset(preset, now))`; cache key `${preset}|${format(now, 'yyyy-MM-dd')}`; module-level `Map<string, { from?: Date; to?: Date }>` for memoization.
    - Internal `_subForPreset(preset, now)`: `week` → `subWeeks(now, 1)`, `month` → `subMonths(now, 1)`, `3months` → `subMonths(now, 3)`, `6months` → `subMonths(now, 6)`, `year` → `subYears(now, 1)`, `3years` → `subYears(now, 3)`, `5years` → `subYears(now, 5)`. Use TypeScript exhaustive switch (`const _exhaustive: never = preset`) so adding a new preset is a tsc error if `_subForPreset` is not updated.
    - `dateToWire(d: Date | undefined): string | undefined` — returns `undefined` when `d` is undefined; otherwise `format(d, 'yyyy-MM-dd')` (LOCAL date, no UTC conversion — date-fns `format` is timezone-correct by default for the local TZ).
    - `dateRangeToWireParams(range: { from?: Date; to?: Date }): { from_date?: string; to_date?: string }` — mirrors `rangeToQueryParams` from `opponentStrength.ts:106-119`; emits only the keys that have values.
    - `resolveDateRange(filters: FilterState): { from?: Date; to?: Date }` — returns `filters.customRange ?? {}` when `filters.recency === 'custom'`, otherwise `presetToDates(filters.recency)`. (Note: requires `FilterState` to be importable — see Task 2 for the type extension; this function can be added in Task 2 if circular imports surface, but recommended placement is in `recency.ts` alongside the other helpers.)

    NO magic numbers. NO `text-xs`. JSDoc each export.

    **Step 4 — Create frontend/src/lib/__tests__/recency.test.ts:** Mirror `opponentStrength.test.ts` structure (`describe` block per export). Cover the 10 behaviors above. Use vitest's `vi.useFakeTimers()` / `vi.setSystemTime()` to test calendar-day boundaries reliably.

    Implements D-11, D-12; covers P92-R3 and P92-R6 partial.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run src/lib/__tests__/recency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RecencyPreset" frontend/src/types/api.ts` returns ≥ 1
    - `grep -c "^export type Recency " frontend/src/types/api.ts` returns 0 (old name fully renamed)
    - `frontend/src/lib/recency.ts` exists and exports `presetToDates`, `dateToWire`, `dateRangeToWireParams`, and `resolveDateRange` (verify via `grep -n "^export" frontend/src/lib/recency.ts` showing all four)
    - `frontend/src/lib/__tests__/recency.test.ts` exists with ≥ 10 `it(...)` or `test(...)` blocks (`grep -c "it(\|test(" frontend/src/lib/__tests__/recency.test.ts` returns ≥ 10)
    - `cd frontend && npm test -- --run src/lib/__tests__/recency.test.ts` passes
    - `cd frontend && npx tsc --noEmit` passes (this WILL fail for the rest of the codebase until Task 2 runs — acceptable mid-plan, but the recency.ts file itself must compile)
    - `grep -n "date-fns" frontend/package.json` shows the new dep
  </acceptance_criteria>
  <done>Recency type renamed; recency.ts utility implemented and unit-tested; date-fns installed and legitimacy-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend FilterState with customRange + migrate all 7 hooks + buildFilterParams + ripple update Recency → RecencyPreset across 21 importing files</name>
  <files>frontend/src/components/filters/FilterPanel.tsx, frontend/src/hooks/useFilterStore.ts, frontend/src/hooks/useStats.ts, frontend/src/hooks/useOpenings.ts, frontend/src/hooks/useEndgames.ts, frontend/src/hooks/useEndgameInsights.ts, frontend/src/hooks/useOpeningInsights.ts, frontend/src/hooks/useNextMoves.ts, frontend/src/api/client.ts, frontend/src/types/stats.ts, frontend/src/pages/Openings.tsx, frontend/src/pages/Endgames.tsx, frontend/src/pages/GlobalStats.tsx, frontend/src/pages/openings/GamesTab.tsx, frontend/src/components/insights/OpeningInsightsBlock.tsx, frontend/src/components/insights/EndgameInsightsBlock.tsx, frontend/src/components/charts/EndgameEloTimelineSection.tsx, frontend/src/hooks/__tests__/useEndgameInsights.test.tsx, frontend/src/hooks/__tests__/useOpeningInsights.test.tsx, frontend/src/components/insights/OpeningInsightsBlock.test.tsx, frontend/src/components/insights/__tests__/EndgameInsightsBlock.test.tsx</files>
  <read_first>
    - frontend/src/components/filters/FilterPanel.tsx (full file — focus FilterState type at lines 17-30, DEFAULT_FILTERS at 33-42, FILTER_DOT_FIELDS at 51-59, areFiltersEqual at 70-97, and the Recency Select block at 173-195 — DO NOT modify the Select markup in this plan, that's Plan 04)
    - frontend/src/hooks/useFilterStore.ts (full file)
    - frontend/src/hooks/useStats.ts (full file — useRatingHistory at lines 8-21, useGlobalStats, useMostPlayedOpenings, useBookmarkPhaseEntryMetrics at line 66)
    - frontend/src/hooks/useOpenings.ts (full file)
    - frontend/src/hooks/useEndgames.ts (full file)
    - frontend/src/hooks/useEndgameInsights.ts (full file)
    - frontend/src/hooks/useOpeningInsights.ts (full file)
    - frontend/src/hooks/useNextMoves.ts (full file)
    - frontend/src/api/client.ts (full file — buildFilterParams at lines 75-97, plus 3 stats endpoints at 126-149 and 3 endgame endpoints at 157-208)
    - frontend/src/types/stats.ts (BookmarkPhaseEntryRequest at line 96)
    - frontend/src/pages/Openings.tsx (lines 289, 321, 351, 1103)
    - frontend/src/pages/Endgames.tsx (lines 601, 673 — empty-state copy)
    - frontend/src/pages/GlobalStats.tsx (lines 17, 20, 21, 25, 28, 44, 123, 177)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§FilterPanel.tsx, §useStats.ts, §useOpenings.ts, §api/client.ts, §types/api.ts rows)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Recency Call-Site Audit Frontend, §TanStack Query Key Audit, §Frontend State Architecture)
  </read_first>
  <behavior>
    - Test cascade: existing hook tests (`useEndgameInsights.test.tsx`, `useOpeningInsights.test.tsx`, `OpeningInsightsBlock.test.tsx`, `EndgameInsightsBlock.test.tsx`) keep passing after migration
    - Selecting an existing preset value (e.g. setting `filters.recency = 'week'`) in the store produces a request body containing `from_date: 'YYYY-MM-DD'` and `to_date: 'YYYY-MM-DD'` for the past week in local TZ
    - Selecting `filters.recency = null` (= "all time") produces a request body with neither `from_date` nor `to_date`
    - The `customRange: { from?: Date; to?: Date } | null` field defaults to `null` and is included in FILTER_DOT_FIELDS so the modified-dot lights when set
    - The 7th hook `useBookmarkPhaseEntryMetrics` (useStats.ts:66) passes from_date/to_date — explicit pitfall called out in 92-RESEARCH.md §Pitfall 4 and 92-PATTERNS.md
  </behavior>
  <action>
    **Step 1 — Extend FilterState (frontend/src/components/filters/FilterPanel.tsx lines 17-30):** Change `recency: Recency | null;` to `recency: RecencyPreset | 'custom' | null;`. Add a new field below it: `customRange: { from?: Date; to?: Date } | null;` (Discretion call resolved to Option 1 — sibling field — per RESEARCH.md §Frontend State Architecture). Update `DEFAULT_FILTERS` at lines 33-42 to include `customRange: null`. Update `FILTER_DOT_FIELDS` at lines 51-59 to include `'customRange'`. Extend `areFiltersEqual` at lines 70-97 with a new branch for `customRange` mirroring the `opponentStrength` deep-compare at lines 88-93 — compare `from?.getTime()` and `to?.getTime()` between the two values; treat both-null as equal; mismatched null/non-null as not equal (full sketch in 92-PATTERNS.md §FilterPanel.tsx). Update the FilterField union type if necessary.

    **Step 2 — Find/replace `Recency` → `RecencyPreset` across all 21 importing files:** Use `rg -l "import .*Recency" frontend/src/ | xargs sed -i 's/\bRecency\b/RecencyPreset/g'` OR equivalent manual edits, then verify with `rg -n "\bRecency\b" frontend/src/` returns ZERO matches (other than `RecencyPreset` substrings and the type declaration itself). Watch for: hook files (useStats.ts, useOpenings.ts, etc.), page files (Openings.tsx, GlobalStats.tsx, etc.), insight blocks, test files. The string `"recency"` (the lowercase property name and the user-facing label) STAYS — only the `Recency` *type identifier* changes.

    **Step 3 — Migrate the 7 hooks:** For each of `useOpenings.ts`, `useEndgames.ts`, `useEndgameInsights.ts`, `useOpeningInsights.ts`, `useNextMoves.ts`, `useStats::useRatingHistory`, `useStats::useGlobalStats`, `useStats::useMostPlayedOpenings`, `useStats::useBookmarkPhaseEntryMetrics`:
    - Import `resolveDateRange, dateRangeToWireParams` from `@/lib/recency`.
    - In the queryFn body, REPLACE the line that sends `recency: filters.recency` (or `recency: normalizedRecency`) in the request body with `...dateRangeToWireParams(resolveDateRange(filters))`. This spreads `{ from_date?: string; to_date?: string }` — both omitted when "all time".
    - In the queryKey, REPLACE the slot containing `normalizedRecency` or `filters.recency` with TWO slots: `dateParams.from_date ?? null, dateParams.to_date ?? null` (compute `const dateParams = dateRangeToWireParams(resolveDateRange(filters))` once per hook invocation, OR wrap in `useMemo` keyed on `filters`).
    - Drop the `normalizedRecency` variable; not needed anymore.
    - **CRITICAL: useStats.ts:66 useBookmarkPhaseEntryMetrics is the 7th hook NOT named in CONTEXT D-13.** Apply the same migration — this is the silent regression risk flagged by RESEARCH.md §Pitfall 4.

    **Step 4 — frontend/src/api/client.ts buildFilterParams (lines 75-97):** Replace the `recency?: string | null` field in the params type with `from_date?: string | null; to_date?: string | null`. Replace `if (params.recency && params.recency !== 'all') result.recency = params.recency;` with two conditionals: `if (params.from_date) result.from_date = params.from_date;` and `if (params.to_date) result.to_date = params.to_date;`. Update the three `statsApi.*` function signatures (lines 126-149) and the three `endgameApi.*` signatures (lines 157-208) to accept `from_date: string | null, to_date: string | null` instead of `recency: string | null` — verify each pass-through to `buildFilterParams`.

    **Step 5 — frontend/src/types/stats.ts (BookmarkPhaseEntryRequest at line 96):** Replace the `recency` field with `from_date?: string; to_date?: string`.

    **Step 6 — Page-level callsites (Openings.tsx, Endgames.tsx, GlobalStats.tsx, openings/GamesTab.tsx):** Update any place that previously read `debouncedFilters.recency` or constructed a request with `recency`. The migration is already mostly handled by the hooks; page-level changes are mostly empty-state copy updates and the imports. The user-facing label `"recency"` in copy (e.g. Home.tsx:371 "filterable by time control, color, and recency") stays — only the underlying value/type changes.

    **Step 7 — Insight blocks (OpeningInsightsBlock.tsx, EndgameInsightsBlock.tsx):** The modified-filter detector compares the current `filters` to `DEFAULT_FILTERS` (EndgameInsightsBlock.tsx:52). Since DEFAULT_FILTERS now has `customRange: null`, the comparison via `areFiltersEqual` already handles this correctly — verify no special-case for recency remains. Update any copy mentioning "recency" if appropriate (don't change the user-facing word per CONTEXT.md notes; just verify nothing breaks).

    **Step 8 — Test files:** Update `useEndgameInsights.test.tsx`, `useOpeningInsights.test.tsx`, `OpeningInsightsBlock.test.tsx`, `EndgameInsightsBlock.test.tsx` fixtures to use the new FilterState shape (include `customRange: null` in any FilterState literal; assert against `from_date`/`to_date` in request bodies if those assertions exist).

    **Step 9 — D-14 URL param audit (no-op per RESEARCH.md):** Run `rg -n "recency" frontend/src/ | rg -i "search|navigate|URLSearchParams|useSearchParams"` and confirm ZERO matches (RESEARCH.md verified this; the grep is enforcement). If any matches surface, update to `from_date`/`to_date`.

    Implements D-13 (full), D-14 (audit pass), D-12 (consumption via resolveDateRange + dateRangeToWireParams); addresses P92-R3, P92-R5, P92-R6.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run &amp;&amp; npm run knip</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\bRecency\b" frontend/src/ | grep -v "RecencyPreset"` returns ZERO matches
    - `grep -c "RecencyPreset" frontend/src/types/api.ts` returns ≥ 1
    - `grep -n "customRange" frontend/src/components/filters/FilterPanel.tsx` shows the new field in FilterState, DEFAULT_FILTERS, FILTER_DOT_FIELDS, and the areFiltersEqual branch (≥ 4 matches)
    - `grep -n "resolveDateRange\|dateRangeToWireParams" frontend/src/hooks/` returns matches in ALL 7 hook files: useOpenings, useEndgames, useEndgameInsights, useOpeningInsights, useNextMoves, useStats (one for each of useRatingHistory/useGlobalStats/useMostPlayedOpenings/useBookmarkPhaseEntryMetrics — at least once in useStats.ts overall)
    - `grep -n "useBookmarkPhaseEntryMetrics" frontend/src/hooks/useStats.ts` shows the hook body now uses `dateRangeToWireParams` (the hidden 7th hook is explicitly migrated — Pitfall 4 closed)
    - `grep -n "recency:" frontend/src/api/client.ts` returns ZERO matches in `buildFilterParams` (replaced by `from_date`/`to_date`)
    - `cd frontend && npx tsc --noEmit` passes with zero errors
    - `cd frontend && npm run lint` passes
    - `cd frontend && npm test -- --run` passes
    - `cd frontend && npm run knip` passes (no new dead exports — the renamed `RecencyPreset` is consumed everywhere `Recency` used to be)
    - `rg -n "recency" frontend/src/ | rg -i "search|navigate|URLSearchParams|useSearchParams"` returns ZERO matches (D-14 audit)
    - `rg -n "data-testid=\"filter-recency\"" frontend/src/components/filters/FilterPanel.tsx` returns 1 match (the existing test ID is preserved; Plan 04 adds `filter-recency-custom` for the 9th item)
  </acceptance_criteria>
  <done>All 7 hooks pass from_date/to_date; FilterState carries customRange (still null at runtime — Plan 04 wires the UI); all tests pass; type-check clean; knip clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Outgoing request body now carries from_date/to_date strings; locally computed in user-local TZ via date-fns format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-01 | Tampering | npm install date-fns | mitigate | Verify legitimacy per protocol before install (8+ yrs old, ~25M weekly downloads, github.com/date-fns/date-fns) — see RESEARCH.md §Package Legitimacy Audit. `[ASSUMED]` tag; planner approved by sourcing from authoritative shadcn registry. |
| T-92-03-02 | Information Disclosure | wire format | accept | `YYYY-MM-DD` strings carry no PII beyond what `recency` already encoded (which is none); no new exposure. |
| T-92-03-03 | DoS via TanStack Query key churn | presetToDates memoization | mitigate | Memoize on `(preset, today-as-YYYY-MM-DD)` so query keys are stable within a calendar day. RESEARCH.md §Pitfall 1 explicit. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` zero errors
- `cd frontend && npm run lint` clean
- `cd frontend && npm test -- --run` all green
- `cd frontend && npm run knip` clean
- Hook migration count: 7 hooks confirmed (six from D-13 + useBookmarkPhaseEntryMetrics)
- `rg "\bRecency\b" frontend/src/ | grep -v RecencyPreset` zero matches
</verification>

<success_criteria>
- All 7 consuming hooks send from_date/to_date in request bodies and use them in queryKeys
- Recency type renamed to RecencyPreset with UI-only comment
- FilterState gains customRange sibling field with full areFiltersEqual / FILTER_DOT_FIELDS coverage
- recency.ts utility with full unit-test coverage of memoization and wire format
- frontend lint + tsc + vitest + knip all green
- D-14 URL param audit returns no matches
</success_criteria>

<output>
Create `.planning/phases/92-custom-date-range-filter/92-03-SUMMARY.md` when done
</output>
