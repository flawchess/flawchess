---
phase: 92-custom-date-range-filter
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - app/repositories/query_utils.py
  - app/schemas/openings.py
  - app/schemas/insights.py
  - app/schemas/stats.py
  - app/schemas/opening_insights.py
  - app/routers/stats.py
  - app/routers/endgames.py
  - app/routers/insights.py
  - app/services/openings_service.py
  - app/services/stats_service.py
  - app/services/endgame_service.py
  - app/services/insights_service.py
  - app/services/opening_insights_service.py
  - app/repositories/openings_repository.py
  - app/repositories/endgame_repository.py
  - app/repositories/stats_repository.py
  - tests/test_query_utils.py
  - tests/test_openings_repository.py
  - tests/test_endgame_repository.py
  - tests/test_stats_repository.py
  - tests/test_insights_schema.py
  - tests/routers/test_insights_openings.py
  - tests/services/test_insights_service.py
  - tests/test_integration_routers.py
  - tests/test_aggregation_sanity.py
  - scripts/prodcheck_80_1.py
autonomous: true
requirements: [P92-R2, P92-R7]
# Synthetic requirement IDs — see Plan 01 frontmatter comment for full scheme.
# This is the atomic backend wire-format flip. Splitting causes ty cascade with no useful intermediate state.
must_haves:
  truths:
    - "apply_game_filters() accepts from_date and to_date instead of recency_cutoff"
    - "All four Pydantic request schemas (OpeningsRequest, NextMovesRequest, FilterContext, BookmarkPhaseEntryRequest, OpeningInsightsRequest) expose from_date/to_date instead of recency"
    - "POSTing from_date > to_date returns HTTP 422 from every endpoint that accepts the pair"
    - "The insights router gating function blocks the LLM endpoint when either from_date or to_date is set (replacing the old `recency != all_time` gate)"
    - "insights_service.py internal LLM windows use date.today() - timedelta(days=90) for the last_3mo window, preserving existing semantics"
    - "RECENCY_DELTAS dict and recency_cutoff() helper are deleted from openings_service.py"
    - "uv run pytest passes with zero failures"
    - "uv run ty check app/ tests/ passes with zero errors"
  artifacts:
    - path: app/repositories/query_utils.py
      provides: "apply_game_filters with from_date/to_date positional params"
      contains: "from_date: datetime.date | None"
    - path: app/schemas/openings.py
      provides: "OpeningsRequest and NextMovesRequest with from_date/to_date + @model_validator"
      contains: "@model_validator"
    - path: app/schemas/insights.py
      provides: "FilterContext with from_date/to_date fields"
      contains: "from_date"
    - path: app/schemas/stats.py
      provides: "BookmarkPhaseEntryRequest with from_date/to_date"
      contains: "from_date"
    - path: app/schemas/opening_insights.py
      provides: "OpeningInsightsRequest with from_date/to_date"
      contains: "from_date"
    - path: tests/test_query_utils.py
      provides: "Direct unit tests for apply_game_filters date predicates"
      contains: "test_apply_game_filters"
  key_links:
    - from: app/repositories/query_utils.py
      to: app/repositories/{openings,endgame,stats}_repository.py
      via: "apply_game_filters call sites — every keyword arg recency_cutoff= becomes from_date=, to_date="
      pattern: "apply_game_filters"
    - from: app/routers/insights.py
      to: "_validate_full_history_filters gate"
      via: "from_date is not None or to_date is not None"
      pattern: "_validate_full_history_filters|Clear Custom date range filter"
    - from: app/services/insights_service.py
      to: "endgame_service.get_endgame_overview"
      via: "internal LLM windows — last_3mo = today - 90d (D-18)"
      pattern: "from_date.*timedelta\\(days=90\\)"
---

<objective>
Atomic backend wire-format flip: rename `recency` / `recency_cutoff` to `from_date` / `to_date` across the single shared filter helper, four Pydantic request schemas, three router files, five service files, three repository files, all backend tests, and one helper script. Update the insights router gating logic to block on `from_date is not None or to_date is not None` instead of `recency != "all_time"`. Switch the internal LLM windows in `insights_service.py:153,164` to date-based windows that preserve existing semantics (`last_3mo = today - 90d`, `all_time = both None`). Delete the now-dead `RECENCY_DELTAS` dict and `recency_cutoff()` helper from `openings_service.py`.

This is intentionally atomic — splitting would leave the type system broken between intermediate states (ty would fail on the cascade). Implements D-09, D-10, D-13 (backend half), D-14, D-15, D-18, and the deletion of dead conversion code; addresses synthetic requirements P92-R2 and P92-R7.

Purpose: Backend wire shape now matches the new client contract; the SQL predicate becomes `played_at >= from_date AND played_at < to_date + 1 day` (only the relevant clauses appear); the LLM gate prevents silent regression where a date-filtered request produces a "your full history" report.
Output: All backend code compiles under ty, every backend test passes, the new wire format returns 422 on `from_date > to_date`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-custom-date-range-filter/92-CONTEXT.md
@.planning/phases/92-custom-date-range-filter/92-RESEARCH.md
@.planning/phases/92-custom-date-range-filter/92-PATTERNS.md
@.planning/phases/92-custom-date-range-filter/92-01-SUMMARY.md
@CLAUDE.md
@app/repositories/query_utils.py
@app/schemas/openings.py
@app/schemas/insights.py
@app/schemas/stats.py
@app/schemas/opening_insights.py
@app/routers/insights.py
@app/services/insights_service.py
@app/services/openings_service.py

<interfaces>
<!-- Existing apply_game_filters signature (app/repositories/query_utils.py:12-77) -->
<!--   def apply_game_filters( -->
<!--       stmt, time_control, platform, rated, opponent_type, -->
<!--       recency_cutoff: datetime.datetime | None,  ← REPLACE -->
<!--       color: str | None = None, *, opponent_gap_min, opponent_gap_max, -->
<!--   ) -->
<!-- Target signature: ...replace recency_cutoff with two positional params -->
<!--   from_date: datetime.date | None, to_date: datetime.date | None, -->

<!-- Existing FilterContext shape (app/schemas/insights.py:133) — Literal default "all_time" -->
<!-- Target: from_date: datetime.date | None = None, to_date: datetime.date | None = None -->

<!-- Existing gate (app/routers/insights.py:67-75): -->
<!--   if filters.recency != "all_time": blocking.append("Switch Recency to All time") -->
<!-- Target: -->
<!--   if filters.from_date is not None or filters.to_date is not None: -->
<!--       blocking.append("Clear Custom date range filter") -->

<!-- Existing insights_service.py:153,164 LLM windows: recency=None / recency="3months" -->
<!-- Target: from_date=None,to_date=None / from_date=date.today()-timedelta(days=90),to_date=None -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit tests for apply_game_filters date predicates (RED — written first)</name>
  <files>tests/test_query_utils.py</files>
  <read_first>
    - app/repositories/query_utils.py (full file)
    - tests/test_openings_repository.py (existing integration coverage — for fixture patterns)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§tests/test_query_utils.py and §app/repositories/query_utils.py rows)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Phase Requirements → Test Map and §Backend Wire-Format Change)
  </read_first>
  <behavior>
    - Test 1 `test_apply_game_filters_no_date_filter`: passing `from_date=None, to_date=None` produces a Select statement whose compiled WHERE clause does NOT reference `played_at` for the date predicate (other filters may still reference `played_at` via `Game.played_at` column for ordering/joins — assert absence of `>= :from_date_1` and `< :to_date_1` bind params).
    - Test 2 `test_apply_game_filters_from_only`: `from_date=date(2026,3,1), to_date=None` produces compiled SQL containing `games.played_at >= :from_date_1` and NOT containing `games.played_at <`.
    - Test 3 `test_apply_game_filters_to_only`: `from_date=None, to_date=date(2026,4,1)` produces compiled SQL containing `games.played_at <` and NOT containing `games.played_at >=` for the date predicate.
    - Test 4 `test_apply_game_filters_date_range`: both bounds set produces both predicates with the upper bound shifted by 1 day (assert the bind param value equals `date(2026,4,2)` when to_date is `date(2026,4,1)`).
  </behavior>
  <action>
    Create `tests/test_query_utils.py`. Import `apply_game_filters` from `app.repositories.query_utils`, `select` from `sqlalchemy`, `Game` from `app.models` (verify exact import path against existing tests), and `datetime`. For each behavior above, build a `stmt = select(Game)`, call `apply_game_filters(stmt, time_control=None, platform=None, rated=None, opponent_type="human", from_date=..., to_date=...)`, then compile with `stmt.compile(compile_kwargs={"literal_binds": False})` and assert against the resulting SQL string and bind param values. Tests should FAIL at this stage (RED) because `apply_game_filters` still uses `recency_cutoff`. Do not implement the signature change yet — that's Task 2. Implements the test scaffold required by D-15 and Nyquist gate. Tests cover D-10 semantics precisely (the +1-day shift on `to_date`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_query_utils.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_query_utils.py` exists with four `def test_apply_game_filters_*` functions
    - Running `uv run pytest tests/test_query_utils.py -x` FAILS (RED) — the failure must be a TypeError or assertion failure about `recency_cutoff` vs `from_date`/`to_date`, not an import error
    - `grep -c "def test_" tests/test_query_utils.py` returns 4
  </acceptance_criteria>
  <done>Failing test scaffold in place. Tests turn GREEN after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip apply_game_filters signature + all backend call sites + all 5 Pydantic schemas + 3 routers + insights gate + dead-code removal</name>
  <files>app/repositories/query_utils.py, app/schemas/openings.py, app/schemas/insights.py, app/schemas/stats.py, app/schemas/opening_insights.py, app/routers/stats.py, app/routers/endgames.py, app/routers/insights.py, app/services/openings_service.py, app/services/stats_service.py, app/services/endgame_service.py, app/services/insights_service.py, app/services/opening_insights_service.py, app/repositories/openings_repository.py, app/repositories/endgame_repository.py, app/repositories/stats_repository.py, scripts/prodcheck_80_1.py</files>
  <read_first>
    - app/repositories/query_utils.py (full file)
    - app/schemas/openings.py (full file — three recency declarations; line 39 OpeningsRequest, line 153 TimeSeriesRequest already removed in Plan 01, line 215 NextMovesRequest)
    - app/schemas/insights.py (lines 130-160 for FilterContext; lines 350-380 for @model_validator example)
    - app/schemas/stats.py (around line 133 for BookmarkPhaseEntryRequest)
    - app/schemas/opening_insights.py (around line 29 for OpeningInsightsRequest)
    - app/routers/stats.py (lines 20-130 — three endpoints accepting recency Query param)
    - app/routers/endgames.py (lines 25-100 — two endpoints)
    - app/routers/insights.py (lines 50-220 — _validate_full_history_filters gate at 54-88, plus two endpoints)
    - app/services/openings_service.py (lines 50-150 — RECENCY_DELTAS dict at 55-64 and recency_cutoff helper at 137-145, both to DELETE; all callsites)
    - app/services/insights_service.py (lines 130-170 — internal LLM windows at 152-165)
    - app/services/stats_service.py (full file — extensive callsites enumerated in RESEARCH.md)
    - app/services/endgame_service.py (full file — extensive callsites)
    - app/services/opening_insights_service.py (callsites at lines 52, 589, 677, 887, 903, 951)
    - app/repositories/openings_repository.py (full file)
    - app/repositories/endgame_repository.py (full file)
    - app/repositories/stats_repository.py (full file)
    - scripts/prodcheck_80_1.py (full file — local script)
    - .planning/phases/92-custom-date-range-filter/92-RESEARCH.md (§Backend Wire-Format Change, §Validation Pattern, §Recency Call-Site Audit Backend table)
    - .planning/phases/92-custom-date-range-filter/92-PATTERNS.md (§app/repositories/query_utils.py and §app/schemas/openings.py rows)
  </read_first>
  <behavior>
    - Test cascade: `tests/test_query_utils.py` flips from RED to GREEN once the signature change lands
    - All existing repository/service tests pass after their `recency_cutoff=` and `recency=` keyword args are migrated to `from_date=`/`to_date=`
    - New schema validator returns 422 when client sends `{"from_date": "2026-04-01", "to_date": "2026-03-01"}` to any endpoint that accepts the pair (covered by tests/test_insights_schema.py or new assertions in tests/routers/test_insights_openings.py and tests/test_integration_routers.py)
    - Insights LLM gate at `_validate_full_history_filters` returns "Clear Custom date range filter" in the blocking list when `from_date is not None or to_date is not None` (covered by tests/routers/test_insights_openings.py)
  </behavior>
  <action>
    **Step 1 — `app/repositories/query_utils.py`:** Replace the positional parameter `recency_cutoff: datetime.datetime | None` with two positional parameters `from_date: datetime.date | None` followed by `to_date: datetime.date | None`, in that order, in the same slot (per RESEARCH.md §Backend Wire-Format Change line 446 — keep positional, do not switch to keyword-only). Replace the `if recency_cutoff is not None: stmt = stmt.where(Game.played_at >= recency_cutoff)` block with two conditional `.where()` calls per D-10: `if from_date is not None: stmt = stmt.where(Game.played_at >= from_date)` and `if to_date is not None: stmt = stmt.where(Game.played_at < to_date + datetime.timedelta(days=1))`. Add a code comment above the date block noting the UTC boundary fuzz (RESEARCH.md §Pitfall 2) — accepted trade-off, no client_timezone param.

    **Step 2 — Five Pydantic schemas:** In `app/schemas/openings.py`, replace the `recency: Literal["week", "month", ..., "all"] | None = None` field in `OpeningsRequest` (line 39) and `NextMovesRequest` (line 215) with two fields `from_date: datetime.date | None = None` and `to_date: datetime.date | None = None`, plus a `@model_validator(mode="after")` method named `_check_date_range` that raises `ValueError("from_date must be <= to_date")` when both are set and `from_date > to_date` (mirror the pattern from `app/schemas/insights.py:357-371`). Do NOT touch `TimeSeriesRequest` — Plan 01 removed its recency field entirely.

    In `app/schemas/insights.py:133`, replace `FilterContext.recency: Literal["all_time", "week", ...] = "all_time"` with `from_date: datetime.date | None = None` and `to_date: datetime.date | None = None` (no default `"all_time"` — instead, default both to None which corresponds to the old `"all_time"` semantics). Add the same `_check_date_range` model_validator. Note: FilterContext is used as a cache key in `insights_llm.py` via `model_dump()`; the shape change naturally invalidates existing LLM cache rows (acceptable per RESEARCH.md §Runtime State Inventory).

    In `app/schemas/stats.py:133`, replace `BookmarkPhaseEntryRequest.recency: str | None` with the two typed date fields + `_check_date_range`.

    In `app/schemas/opening_insights.py:29`, replace `OpeningInsightsRequest.recency` Literal with the two date fields + `_check_date_range`.

    **Step 3 — Three routers (Query params):** In `app/routers/stats.py`, find every `recency: str | None = Query(default=None)` Query param (lines 27, 52, 77 per RESEARCH.md) and replace with two `Query(default=None)` params: `from_date: datetime.date | None = Query(default=None)` and `to_date: datetime.date | None = Query(default=None)`. In each endpoint body, add an inline check `if from_date is not None and to_date is not None and from_date > to_date: raise HTTPException(status_code=422, detail="from_date must be <= to_date")` near the top (after auth, before service call). Update the service-call kwargs at lines 92, 115 to pass `from_date=from_date, to_date=to_date` instead of `recency=recency`. Apply the same pattern to `app/routers/endgames.py` (lines 33, 56, 70, 95). In `app/routers/insights.py`, do the same for the Query-param endpoints (lines 68, 97, 140, 182, 217) AND update `_validate_full_history_filters` (lines 54-88): replace the `if filters.recency != "all_time": blocking.append("Switch Recency to All time")` line with `if filters.from_date is not None or filters.to_date is not None: blocking.append("Clear Custom date range filter")` — this is the silent-regression risk flagged by RESEARCH.md §Pitfall 3 / quality_gate.

    **Step 4 — Five services:** In `app/services/openings_service.py`, DELETE the entire `RECENCY_DELTAS` dict at lines 55-64 and the `recency_cutoff()` helper at lines 137-145 (CLAUDE.md "Refactor bloated code on sight" applies — they become dead code once FE owns conversion). For every callsite that previously did `cutoff = recency_cutoff(request.recency); ... apply_game_filters(..., cutoff)`, replace with direct pass-through: `apply_game_filters(..., from_date=request.from_date, to_date=request.to_date)`. Remove any now-unused `from datetime import ...` lines and any unused `recency_cutoff` import in callers.

    In `app/services/stats_service.py`, `app/services/endgame_service.py`, `app/services/opening_insights_service.py`: every function signature accepting `recency_cutoff: datetime.datetime | None` or `recency: str | None` (whichever variant the function uses) becomes two parameters `from_date: datetime.date | None, to_date: datetime.date | None`. Every internal call to `apply_game_filters(..., recency_cutoff=X)` becomes `apply_game_filters(..., from_date=X.from_date if X else None, to_date=X.to_date if X else None)` or, more commonly, just forward `from_date=from_date, to_date=to_date`.

    In `app/services/insights_service.py:152-165`, switch the two internal LLM-window callsites: the `all_time` window becomes `from_date=None, to_date=None`; the `last_3mo` window becomes `from_date=datetime.date.today() - datetime.timedelta(days=90), to_date=None` (per Open Question 4 in RESEARCH.md — use `to_date=None`, NOT `today()`, to preserve exact semantics of the previous `recency_cutoff("3months")`). Update the receiving signature in `endgame_service.get_endgame_overview` accordingly.

    In `app/services/insights_llm.py:421`, verify the docstring reference is the only mention — update wording if any user-visible string says "recency", otherwise leave alone (D-18: prompt content unchanged).

    **Step 5 — Three repositories:** In `app/repositories/openings_repository.py`, `endgame_repository.py`, `stats_repository.py`: every function signature accepting `recency_cutoff: datetime.datetime | None` becomes `from_date: datetime.date | None, to_date: datetime.date | None`. Every internal call to `apply_game_filters(..., recency_cutoff=X)` becomes `apply_game_filters(..., from_date=from_date, to_date=to_date)`. Use `rg -n "recency_cutoff|recency" app/repositories/` to enumerate before editing.

    **Step 6 — `scripts/prodcheck_80_1.py`:** Update lines 11, 55 callsites for consistency (non-blocking but cited by RESEARCH.md §Scripts).

    Throughout: every new `datetime.date` import should use the project convention (`import datetime` then `datetime.date` — match existing usage in the file you're editing; do not introduce `from datetime import date` if the file uses the module-level form). Implements D-09, D-10, D-13 (backend), D-15, D-18, deletes dead conversion code. Addresses P92-R2, P92-R7.
  </action>
  <verify>
    <automated>uv run ruff format app/ tests/ scripts/ &amp;&amp; uv run ruff check app/ tests/ scripts/ --fix &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_query_utils.py -x` is GREEN (Task 1 RED tests now pass)
    - `uv run pytest -x` passes with zero failures across the full suite
    - `uv run ty check app/ tests/` returns zero errors
    - `grep -rn "recency_cutoff" app/` returns ZERO matches (helper deleted, all signatures migrated)
    - `grep -rn "RECENCY_DELTAS" app/` returns ZERO matches (dict deleted)
    - `grep -n "from_date" app/repositories/query_utils.py` shows the predicate at the documented spot
    - `grep -n "to_date.*timedelta" app/repositories/query_utils.py` shows the +1-day shift
    - `grep -n "Clear Custom date range filter" app/routers/insights.py` shows the new gate message
    - `grep -n "model_validator" app/schemas/openings.py app/schemas/insights.py app/schemas/stats.py app/schemas/opening_insights.py` shows 4 `_check_date_range` validators (one per affected schema)
    - `grep -n "from_date.*today.*timedelta(days=90)" app/services/insights_service.py` shows the last_3mo window
    - POSTing `{"from_date": "2026-04-01", "to_date": "2026-03-01", ...}` to any updated endpoint returns 422 (verify via at least one integration test in `tests/test_integration_routers.py` or `tests/routers/test_insights_openings.py`)
    - `uv run ruff check app/ tests/` and `uv run ruff format --check app/ tests/` are both clean
  </acceptance_criteria>
  <done>Backend speaks `from_date`/`to_date` end-to-end; all tests pass; insights gating prevents silent regression on the LLM endpoint; dead conversion code removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (every endpoint with from_date/to_date) | New untrusted input: two ISO date strings per request |
| LLM gate (insights router) | from_date/to_date now drive the "full-history filter" block instead of recency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Tampering | Pydantic schema (5 surfaces) | mitigate | Pydantic v2 coerces `YYYY-MM-DD` strings to `datetime.date`; malformed input returns 422 automatically (FastAPI default). |
| T-92-02-02 | Tampering | Cross-field invariant | mitigate | `@model_validator(mode="after")` raises ValueError → FastAPI 422 when `from_date > to_date`. Covered by integration tests. |
| T-92-02-03 | Information Disclosure / Silent regression | insights LLM endpoint | mitigate | Gate updated at `_validate_full_history_filters` to block when `from_date is not None or to_date is not None`; without this update, date-filtered requests would silently produce truncated "full-history" reports. Verified via test assertion. |
| T-92-02-04 | DoS | extreme date inputs (year 9999, negative) | accept | Pydantic v2 `date.date` parser bounds inputs to valid ISO dates; very-old and future dates explicitly allowed per D-16. No additional bound check needed. |
| T-92-02-05 | Tampering | TIMESTAMPTZ vs DATE comparison fuzz | accept | Up to ~24h boundary fuzz documented in apply_game_filters comment; D-16 accepts this trade-off explicitly. No client_timezone param (avoids scope creep). |
| T-92-02-06 | Authorization | None — no new auth surface | accept | Existing `current_active_user` dependency on every router is unchanged. |
</threat_model>

<verification>
- `uv run pytest -x` passes
- `uv run ty check app/ tests/` zero errors
- `uv run ruff format --check app/ tests/` clean
- `uv run ruff check app/ tests/` clean
- `grep -rn "recency_cutoff\|RECENCY_DELTAS" app/` returns ZERO matches
- Pre-PR checklist (per CLAUDE.md) ready for green frontend half (Plan 03)
</verification>

<success_criteria>
- Backend wire shape is `from_date`/`to_date` everywhere; `recency_cutoff` is deleted
- Every endpoint that accepts both returns 422 when `from_date > to_date`
- LLM insights gate blocks on either bound being set
- Internal LLM windows preserve old semantics via date-based equivalents
- ty + ruff + pytest all green
</success_criteria>

<output>
Create `.planning/phases/92-custom-date-range-filter/92-02-SUMMARY.md` when done
</output>
