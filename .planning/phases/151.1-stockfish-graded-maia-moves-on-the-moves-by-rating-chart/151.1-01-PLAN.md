---
phase: 151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/engine/searchmoves-probe.html
autonomous: false
requirements: [SC3]
must_haves:
  truths:
    - "The vendored stockfish-18-lite-single.js build, given `setoption name MultiPV value N` then `go depth D searchmoves <ucis>`, emits N distinct `info` lines whose `pv[0]` are exactly the restricted candidate moves (SC3, Assumption A1)."
    - "Each restricted move's eval refines sensibly as depth climbs (D-05 progressive-grading premise holds on the real binary)."
    - "If the binary silently ignores `searchmoves` OR does not scale past MultiPV=2, the failure is surfaced to the developer BEFORE the grading hook is built (de-risk gate)."
  artifacts:
    - "frontend/public/engine/searchmoves-probe.html (throwaway probe page, deleted after sign-off)"
  key_links:
    - "The probe loads the SAME vendored worker path the grading hook will use (/engine/stockfish-18-lite-single.js), so a green probe directly validates the hook's engine assumption."
---

<objective>
De-risk the single genuine unknown in this phase: whether the vendored
`stockfish-18-lite-single.js` WASM build functionally honors a `searchmoves`-restricted
search combined with `MultiPV = N` (N up to the ~6 candidate cap), producing exactly N
`info` lines each keyed to a distinct restricted move, at real depths in a real browser.

This is the "mandatory hands-on spike" the ROADMAP (SC3) and RESEARCH.md (Assumption A1)
call out as the first plan-time task. Everything downstream (Plan 03 grading hook, Plan 04
chart coloring) assumes this works; confirming it now prevents building a ~250-line hook
against a false premise.

Purpose: Prove Assumption A1 on the real binary before investing in the grading pipeline.
Output: A throwaway probe page + a developer confirmation (or a flagged pivot to the D-04
fallback if the binary misbehaves).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-RESEARCH.md
@frontend/src/hooks/useStockfishEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the throwaway searchmoves+MultiPV probe page</name>
  <files>frontend/public/engine/searchmoves-probe.html</files>
  <read_first>
    - frontend/src/hooks/useStockfishEngine.ts (UCI init order: `uci` → on `uciok` send `setoption name MultiPV value N` → `isready` → on `readyok` send `position fen …` then `go …`; ENGINE_PATH = '/engine/stockfish-18-lite-single.js')
    - frontend/src/hooks/uciParser.ts (the `info … multipv … score cp|mate … pv <moves>` line shape the probe must print)
  </read_first>
  <action>
    Create a single self-contained static HTML page at frontend/public/engine/searchmoves-probe.html
    (served by Vite dev at http://localhost:5173/engine/searchmoves-probe.html). It must:
    (1) create `new Worker('/engine/stockfish-18-lite-single.js')` (classic worker, NO { type: 'module' } — matches the Emscripten glue constraint from useStockfishEngine);
    (2) run the UCI handshake exactly as useStockfishEngine does (`uci`, on `uciok` send `setoption name MultiPV value 4`, `isready`, on `readyok` proceed);
    (3) send a fixed test position with a KNOWN multi-candidate middlegame FEN (hard-code one in the page, e.g. an Italian/Ruy position with several reasonable moves) plus its 4 candidate UCI moves as a hard-coded array;
    (4) send `go depth 16 searchmoves <the 4 ucis joined by spaces>`;
    (5) append every raw `info` and `bestmove` line to a `<pre data-testid="probe-log">` on the page, and additionally render a small live table (data-testid="probe-summary") of each candidate UCI → its latest depth + score, updated on each `info` line, so the reviewer can eyeball that (a) only the 4 restricted moves ever appear as `pv[0]`, and (b) scores refine as depth climbs.
    Keep it dependency-free vanilla JS/HTML — it is deliberately throwaway (deleted in Task 2 after sign-off). Include a one-line header comment marking it a temporary Phase 151.1 SC3 probe.
    Do NOT modify any src/ file; the probe is fully isolated in public/.
  </action>
  <verify>
    <automated>test -f frontend/public/engine/searchmoves-probe.html && grep -q "searchmoves" frontend/public/engine/searchmoves-probe.html && echo PROBE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The probe page exists, loads the vendored worker by its real path, and sends `setoption name MultiPV value 4` + `go depth 16 searchmoves <4 ucis>`.
    - It prints raw engine lines and a per-candidate depth/score summary.
    - No src/ file is touched.
  </acceptance_criteria>
  <done>Opening the probe page in a browser drives the vendored engine and streams info lines to the page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm searchmoves + MultiPV>2 on the real vendored WASM (SC3 spike)</name>
  <what-built>A throwaway probe page (frontend/public/engine/searchmoves-probe.html) that drives the REAL vendored stockfish-18-lite-single.js worker with `setoption name MultiPV value 4` + `go depth 16 searchmoves <4 candidate ucis>` and streams the resulting info lines + a per-candidate depth/score summary.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`.
    2. Open http://localhost:5173/engine/searchmoves-probe.html in desktop Chrome.
    3. Confirm the probe-summary table shows EXACTLY the 4 restricted candidate moves (no other moves appear as pv[0]), each with a score that deepens/refines as depth climbs toward 16.
    4. Confirm 4 distinct `info … multipv 1..4 … pv <move> …` lines appear (not just 2) — i.e. MultiPV scaled past 2 under the searchmoves restriction.
    5. (Bonus, functional sanity only) Open the same URL on a real mobile Safari device on the LAN; confirm it still produces the 4 restricted lines (latency itself is measured later in Plan 04 SC4, not here).
    6. Report: does searchmoves + MultiPV>2 work on the vendored binary — YES or NO?
       - If YES: approve; Claude then deletes the probe page and Plan 03 proceeds as designed.
       - If NO (searchmoves ignored, or MultiPV capped at 2): describe the observed behavior. This flags a pivot to the D-04 fallback discussion (per-move constrained searches) BEFORE the hook is built — do NOT proceed to Plan 03 as-is.
  </how-to-verify>
  <resume-signal>Type "approved — searchmoves works" (Claude will delete the probe), or describe the failure mode to trigger the fallback discussion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user browser → vendored WASM worker | Already-trusted, pinned, in-repo asset (vendored in Phase 136); the probe adds no new asset, only a new caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151.1-01 | Information Disclosure | searchmoves-probe.html left in public/ | low | mitigate | Throwaway page is deleted immediately after checkpoint sign-off (Task 2 resume flow); it exposes only local engine output, no secrets, no network calls. |

No new packages installed (T-151.1-SC N/A — zero npm/PyPI/crates installs this phase). No high-severity threats.
</threat_model>

<verification>
- Probe page drives the real vendored worker (not a mock).
- Human confirms N distinct restricted `info` lines on desktop Chrome (functional SC3 premise).
- Probe deleted after sign-off (no dead public asset shipped).
</verification>

<success_criteria>
- SC3 Assumption A1 confirmed on the real binary (or a failure flagged before Plan 03 build).
- frontend/public/engine/searchmoves-probe.html deleted after approval.
</success_criteria>

<output>
Create `.planning/phases/151.1-stockfish-graded-maia-moves-on-the-moves-by-rating-chart/151.1-01-SUMMARY.md` when done, recording the YES/NO searchmoves verdict (this is the load-bearing input to Plan 03).
</output>
