---
phase: 81-endgame-start-vs-end-twin-tile-section-above-the-wdl-table
plan: 05
type: execute
wave: 4
depends_on:
  - 81-04-PLAN.md
files_modified: []
autonomous: false
requirements:
  - D-04
  - D-09
  - D-13
  - D-16
  - D-17

must_haves:
  truths:
    - "On desktop (≥lg width), the two tiles render side-by-side"
    - "On mobile (<lg width), the two tiles stack vertically with entry-eval first"
    - "Tile 1's score bullet visually matches the Openings ExplorerTab score bullet (D-16)"
    - "Hovering/tapping each popover icon reveals n, p-value, confidence verdict, and methodology copy"
    - "Concept-explainer accordion's 2 new paragraphs use 'we can't tell' framing and read naturally"
    - "Three-state color verdict resolves correctly when impersonating users with sig-positive / sig-negative / not-significant data"
  artifacts: []
  key_links: []
---

<objective>
Manual UAT checkpoint for the visual / interactive layer of Phase 81 — items that automated tests cannot meaningfully cover (visual parity, mobile responsiveness, copy review, three-state color verdict on real user data).

Purpose: Catch issues before merge that the unit tests can't see (CSS layout, popover hover states, copy readability, color rendering on real backgrounds).
Output: User approval (or punch list of issues to fix) before merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/81-endgame-start-vs-end-twin-tile-section-above-the-wdl-table/81-CONTEXT.md
@.planning/phases/81-endgame-start-vs-end-twin-tile-section-above-the-wdl-table/81-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 81 "Endgame Start vs End" twin-tile section is fully wired:
    - Backend: `EndgamePerformanceResponse` returns 6 new fields (entry-eval mean/n/p-value/CI bounds + endgame_score_p_value)
    - Frontend: `EndgameStartVsEndSection` renders above the existing WDL table on the Endgames page, with 2 tiles (entry-eval + endgame score), three-state color verdict, n<10 placeholder, mobile stacking
    - Concept-explainer accordion has 2 new paragraphs using "we can't tell" null framing
  </what-built>
  <how-to-verify>
    Local environment setup (only if not already running):
    ```bash
    docker compose -f docker-compose.dev.yml -p flawchess-dev up -d
    bin/run_local.sh   # starts backend + frontend
    ```

    Then walk through these checks in the browser at http://localhost:5173:

    **1. Section placement (D-01) — desktop**
    - Navigate to /endgames as a user with ≥10 endgame games
    - Confirm the new "Endgame Start vs End" section appears INSIDE the "Endgame Overall Performance" area
    - Confirm it sits BETWEEN the concept-explainer accordion and the existing WDL table ("Games with vs without Endgame")
    - Confirm the existing WDL table and Score Gap column are UNCHANGED below it (D-21)
    - Confirm the "Endgame vs Non-Endgame Score over Time" chart is UNCHANGED below the WDL table (D-21)

    **2. Two tiles side-by-side on desktop (D-04)**
    - Browser viewport ≥ 1024px wide
    - Confirm the two tiles render side-by-side, equal width, both inside `charcoal-texture` cards
    - Confirm tile titles read: left "Where you start", right "What you do with it"

    **3. Mobile stacking + ordering (D-04, D-17)**
    - Resize browser to <1024px (DevTools mobile emulation: iPhone SE / 375px wide)
    - Confirm the two tiles stack vertically
    - Confirm entry-eval tile is FIRST (chronological setup → execution per D-17)
    - Confirm endgame-score tile is SECOND below it

    **4. Visual parity with Openings score bullet (D-16)**
    - Open the Endgames page (Tile 2: "Endgame score") AND the Openings → Explorer subtab (the score bullet there) side-by-side in two windows or tabs
    - Confirm the bullet chart's neutral band, axis domain, colors, and tick density look IDENTICAL between the two pages — this is the explicit point of D-16

    **5. Three-state color verdict (D-09, D-10)**
    - Verify on at least 3 users (use admin impersonation if needed):
      a. A user whose entry-eval is significantly POSITIVE (mean ≥ +0.75, p < 0.05): Tile 1 value text renders in green (ZONE_SUCCESS)
      b. A user whose entry-eval is significantly NEGATIVE (mean ≤ -0.75, p < 0.05): Tile 1 value text renders in red (ZONE_DANGER)
      c. A user with low N (<10) or non-significant: value text renders without zone color (default text color), and for n<10 specifically the placeholder "Not enough data yet" replaces the chart pair

    **6. Popover content (D-03 — n + p inside popover only)**
    - Click/hover Tile 1's info icon (BulletConfidencePopover)
    - Confirm the popover shows: game count (n), p-value, confidence verdict (low/medium/high), and explanatory copy
    - Confirm there is NO duplicate "n=… · p<0.05" stats line on the tile body itself
    - Repeat for Tile 2's info icon (ScoreConfidencePopover)

    **7. Concept-explainer accordion (D-13, D-14)**
    - Open the "Endgame concepts" accordion on the page
    - Confirm 2 new paragraphs are present, IN ORDER:
      - Conversion / Parity / Recovery (existing, unchanged)
      - **NEW: "Avg eval at endgame entry"** — explains Stockfish-eval-at-entry, equal-footing baseline, mate exclusion, "we can't tell" framing for null
      - **NEW: "Absolute endgame score"** — explains 50% break-even line, Opponent Strength filter link, "we can't tell" framing
      - Rating-changes caveat (existing, unchanged)
    - Read both new paragraphs end-to-end and confirm they read naturally and use the "we can't tell" framing rather than "no advantage"

    **8. Hide-when-empty (D-06)**
    - Test as a user with ZERO endgame games (e.g. fresh account or filter-by-no-endgame)
    - Confirm the entire `Endgame Overall Performance` block (including the new section) is hidden — same `showPerfSection` predicate as before

    **9. Sentry / error-handling sanity**
    - Open browser DevTools console; navigate around `/endgames`
    - Confirm no new console errors / warnings tied to the new section or the new schema fields

    **Document any issues found** in a short punch list. If everything passes, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- All 9 manual UAT checks above pass (or are addressed by punch-list fixes)
</verification>

<success_criteria>
- Section visually integrates above the WDL table without disrupting existing layout
- Mobile stacking with entry-eval first is correct
- Visual parity with Openings score bullet confirmed
- Three-state color verdict matches D-09 spec on real user data
- Both popovers render n + p + verdict; no duplicate stats line on the tile body
- Concept-explainer paragraphs read naturally and use "we can't tell" framing
- No regression in the unchanged WDL table / Score Gap / time-series chart (D-21)
</success_criteria>

<output>
After completion, create `.planning/phases/81-endgame-start-vs-end-twin-tile-section-above-the-wdl-table/81-05-SUMMARY.md`
</output>
