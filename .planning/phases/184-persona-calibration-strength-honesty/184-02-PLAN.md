---
phase: 184-persona-calibration-strength-honesty
plan: 02
type: execute
wave: 2
depends_on: [184-01]
files_modified:
  - scripts/calibration_persona_fit.py
  - scripts/gen_persona_calibration.py
  - bin/run_persona_calibration_sweep.sh
  - reports/data/persona-calibration.json
  - frontend/src/generated/personaCalibration.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [CAL-04, CAL-05]

must_haves:
  truths:
    - "calibration_persona_fit.py fits each persona cell TWICE — once against Maia-family anchors only (rating_vs_maia), once against SF-family anchors only (rating_vs_sf) — reusing fit_bot_cell_rating unmodified; the two families are never merged before fitting (Pitfall 2)."
    - "approx_blitz = rating_vs_maia − g_preset_combined[preset] + BLITZ_OFFSET_C, where g_preset_combined and BLITZ_OFFSET_C are read from the canonical source at run time, never hardcoded literals."
    - "gen_persona_calibration.py applies PAVA isotonic pooling per style column, points sorted ascending by RUNG (not botElo), so equal-measuring adjacent rungs collapse to a deterministic shared (tied) label and no style column ever displays an inversion (D-04)."
    - "The global 1800 ceiling (D-07) is applied AFTER PAVA pooling, then labels round to the nearest 50 (D-03)."
    - "The fit/codegen scripts fail loud (raise ValueError) when the persona input has fewer than 24 distinct persona rows — a missing persona is never coerced to a default (empty/missing-input guard)."
    - "The 800-rung Human personas that retarget below every preset floor are carried through as beyond_ladder-flagged extrapolations with a measured/extrapolated value (~900), not dropped (D-06 honesty)."
    - "The generated personaCalibration.ts is a Record<PersonaId, { botElo; label }> that is CI drift-checked identically to botStrengthCurves.ts (git diff --exit-code after regeneration)."
    - "D-05: cross-style divergence at the same rung is displayed as measured — the pipeline never normalizes labels across style columns; only within-column PAVA pooling (D-04), the global cap (D-07), and 50-rounding (D-03) modify a persona's measured value, so attacker-1200 and wall-1200 may legitimately print different labels."
    - "D-10: calibrated values live in the generated personaCalibration.ts produced by the fit-JSON→codegen pipeline (fit script writes reports/data JSON, generator emits the TS keyed by PersonaId with both retargeted botElo and display label), CI drift-checked; personaRegistry.ts consumes it with no hand-transcription."
  prohibitions:
    - "Never refit g_preset per persona — reuse the pooled per-preset g_preset_combined from reports/data/bot-strength-lookup.json (a persona cell's bracket has too few games/family for a stable independent G)."
    - "Never clamp labels to the 1800 ceiling before PAVA pooling — clamping first corrupts the monotonicity fit for lower rungs in the same style column."
    - "Never hardcode g_preset_combined or BLITZ_OFFSET_C — read g_preset_combined from bot-strength-lookup.json components.<preset>.g_preset_combined and reuse the BLITZ_OFFSET_C definition, at run time."
    - "Never fit one combined internal rating against the mixed Maia+SF bracket and subtract g_preset_combined from it — that is a systematically biased conversion (Pitfall 2)."
  artifacts:
    - scripts/calibration_persona_fit.py
    - scripts/gen_persona_calibration.py
    - bin/run_persona_calibration_sweep.sh
    - reports/data/persona-calibration.json
    - frontend/src/generated/personaCalibration.ts
    - reports/data/persona-calibration-cells.tsv
  key_links:
    - "sweep aggregate TSV → calibration_persona_fit.py → persona-calibration.json → gen_persona_calibration.py → personaCalibration.ts."
    - "gen_persona_calibration.py --check + git diff --exit-code = the CI drift gate (mirrors the Bot strength curves step in ci.yml)."
---

<objective>
Build the Python fit + codegen pipeline that converts persona-cell sweep data into calibrated per-persona botElo + display labels, plus the operator runbook and CI drift gate. Bootstrap a committed, type-correct personaCalibration.ts so downstream frontend work and CI are unblocked before the overnight sweep runs.

Purpose: This turns raw measured games into the honest, monotone, floor/ceiling-honoring labels that CAL-04 and CAL-05 require, reusing the hardened Phase-180/181 fit + PAVA + codegen machinery.
Output: calibration_persona_fit.py, gen_persona_calibration.py, run_persona_calibration_sweep.sh, a bootstrap persona-calibration.json + personaCalibration.ts, and a CI drift-check step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-persona-calibration-strength-honesty/184-CONTEXT.md
@.planning/phases/184-persona-calibration-strength-honesty/184-RESEARCH.md
@.planning/phases/184-persona-calibration-strength-honesty/184-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02)

New symbols (exclude from drift verification):
- `scripts/calibration_persona_fit.py` — reads the per-(persona,anchor) aggregate, fits per-persona `rating_vs_maia`/`rating_vs_sf`, converts to `approx_blitz`, writes `reports/data/persona-calibration.json`. Supports `--bootstrap` (retargeting-only, no sweep needed) and `--self-test`.
- `scripts/gen_persona_calibration.py` — reads `persona-calibration.json`, PAVA-pools per style column, clamps to 1800, rounds to 50, emits `personaCalibration.ts` + rewrites the JSON; supports `--check`.
- `bin/run_persona_calibration_sweep.sh` — operator runbook: parallel-launch the persona schedule under `bin/preset-supervisor.sh`, combine per-persona TSVs, invoke the fit.
- `reports/data/persona-calibration.json`, `reports/data/persona-calibration-cells.tsv` — generated data artifacts.
- `frontend/src/generated/personaCalibration.ts` — `PERSONA_CALIBRATION: Record<PersonaId, { botElo: number; label: string }>`.

<tasks>

<task type="auto">
  <name>Task 1: Persona rating fit script (twice-per-cell + retargeting + bootstrap)</name>
  <files>scripts/calibration_persona_fit.py, reports/data/persona-calibration.json, reports/data/persona-calibration-cells.tsv</files>
  <read_first>
    - scripts/calibration_anchor_fit.py (fit_bot_cell_rating ~lines 455-498 — call unmodified; fit_all_bot_cells ~635-666 — the "two families NEVER merged" pattern; bootstrap_bot_cell_ci ~592-632; load_bot_cells-style fail-loud TSV validation)
    - scripts/gen_bot_strength_curves.py (approx_blitz conversion, EXPECTED_CELLS_PER_PRESET fail-loud guard, BLITZ_OFFSET_C definition, load_internal_scale fail-loud loader)
    - reports/data/bot-strength-lookup.json (components.<preset>.g_preset_combined values AND derived.<preset>.lookup retargeting values — both read at run time, never transcribed)
    - reports/data/anchor-ladder-internal-scale.json + scripts/lib/calibration-internal-scale.mjs (the 10-anchor INTERNAL_RATING dict used as fixed_ratings)
    - scripts/lib/calibration-persona-cell-schedule.mjs (the persona list + retargeting helpers + presetNameForBlend, for the bootstrap path and the persona/preset mapping)
  </read_first>
  <action>
    Write a stdlib-only Python script (math/random/argparse/json — no numpy/scipy, mirroring calibration_anchor_fit.py) that reads a per-(persona,anchor) aggregate TSV (`reports/data/persona-calibration-cells.tsv`) with a `persona_id` column, and for each of the 24 personas fits TWICE: `rating_vs_maia = fit_bot_cell_rating(wins_vs_maia, games_vs_maia, fixed_ratings)` over Maia-family anchors only, and `rating_vs_sf` over SF-family anchors only, with `fixed_ratings = INTERNAL_RATING`. Read `g_preset_combined` per preset from bot-strength-lookup.json `components.<preset>.g_preset_combined` and the `BLITZ_OFFSET_C` value from the canonical source; compute `approx_blitz = rating_vs_maia − g_preset_combined[preset_of(persona)] + BLITZ_OFFSET_C`. Read the retargeted botElo per persona from bot-strength-lookup.json `derived.<preset>.lookup[String(rung)]` (clamp 800 to the lowest key). Carry a `beyond_ladder` advisory flag per persona (from the schedule bracket) so the 800-rung Human extrapolations are flagged, not dropped. Write `reports/data/persona-calibration.json` with, per PersonaId: `{ preset, rung, blend, botElo, rating_vs_maia, rating_vs_sf, approx_blitz, beyond_ladder }`. Fail loud (raise ValueError) if fewer than 24 distinct persona rows are present, on any TSV header/truncation mismatch, or on any unmeasured anchor label — never coerce to a default. Add a `--bootstrap` mode that, with NO sweep TSV, writes persona-calibration.json using only retargeting: real `botElo` from the lookup and `approx_blitz = rung` (a provisional, honest pre-measurement placeholder), plus a committed placeholder `persona-calibration-cells.tsv` header/shape so the pipeline is runnable and CI-stable before the overnight sweep. Add a `--self-test` mode that fits a small hardcoded fixture and asserts the expected rating (the Python analog of the .check.mjs convention).
  </action>
  <verify>
    <automated>uv run python scripts/calibration_persona_fit.py --bootstrap && uv run python scripts/calibration_persona_fit.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - After `--bootstrap`, `reports/data/persona-calibration.json` contains exactly 24 PersonaId entries, each with a retargeted `botElo` (e.g. a Human-1200 persona shows botElo 1900, an 800-rung shows the clamped floor) and a provisional `approx_blitz`.
    - Passing a fixture TSV with only 23 persona rows raises ValueError (grep the script for the 24-count guard; the `--self-test` or a manual check exercises it).
    - `g_preset_combined` and `BLITZ_OFFSET_C` are read from files at run time — grep confirms no hardcoded `40.9478`/`186.23`/`247.17` literals in the script body.
    - `--self-test` exits 0.
  </acceptance_criteria>
  <done>The fit script produces a 24-persona calibration JSON, fits Maia/SF families separately, reads G/C from source, and has a runnable bootstrap + self-test.</done>
</task>

<task type="auto">
  <name>Task 2: Codegen script (PAVA per style column, 1800 clamp, round-50) + bootstrap generated file</name>
  <files>scripts/gen_persona_calibration.py, frontend/src/generated/personaCalibration.ts, reports/data/persona-calibration.json</files>
  <read_first>
    - scripts/gen_bot_strength_curves.py (isotonic_fit PAVA ~lines 99-131 — reuse verbatim; _render_ts header convention ~279-299; main() --check drift mode ~302-330; load fail-loud loader)
    - frontend/src/generated/botStrengthCurves.ts (the generated-file header + `export const ... : Record<...> = {...} as const;` shape to mirror)
    - reports/data/persona-calibration.json (the fit output consumed here — from Task 1)
    - frontend/src/lib/personas/personaRegistry.ts (PersonaId union + the 24 ids, to key the generated Record and enforce completeness)
  </read_first>
  <action>
    Write a stdlib-only codegen script mirroring gen_bot_strength_curves.py. Read `reports/data/persona-calibration.json`. Group personas by style column and run `isotonic_fit` (the reused PAVA) SEPARATELY per style, over that style's 6 `(rung, approx_blitz)` points sorted ascending by RUNG — 4 independent pooling passes (D-04 weak-monotonicity-within-style). AFTER pooling, apply the global ceiling `min(pooled_value, 1800)` (D-07) and then round to the nearest 50 (D-03): `label = "~" + str(min(round(pooled/50)*50, 1800))`. Emit `frontend/src/generated/personaCalibration.ts` as `export const PERSONA_CALIBRATION: Record<PersonaId, { botElo: number; label: string }> = {...} as const;` keyed by all 24 PersonaIds, with `botElo` = the retargeted value and `label` = the pooled/capped/rounded tilde string. Include the mandatory `// AUTO-GENERATED — do not edit by hand.` header AND a prominent STALENESS (D-11) doc comment: changing botStyleBundles.ts style params or the anchor ladder invalidates this calibration — re-run the persona sweep before regenerating; no hash-guard automation. Fail loud (raise ValueError) if the input lacks exactly 24 personas or any style column lacks its 6 rungs. Add a `--check` mode that regenerates in memory and exits 1 with a `DRIFT:` message if the committed personaCalibration.ts (or the rewritten JSON) differs, mirroring gen_bot_strength_curves.py's `main()`. Run the generator (non-check) once to commit the bootstrap `personaCalibration.ts` produced from the bootstrap JSON.
  </action>
  <verify>
    <automated>uv run python scripts/gen_persona_calibration.py && uv run python scripts/gen_persona_calibration.py --check && git diff --exit-code frontend/src/generated/personaCalibration.ts reports/data/persona-calibration.json</automated>
  </verify>
  <acceptance_criteria>
    - `personaCalibration.ts` exists with a complete `Record<PersonaId, { botElo; label }>` of 24 entries and the AUTO-GENERATED + STALENESS headers.
    - Within any single style column, labels are non-decreasing by rung (grep/parse the 6 labels per style — no inversion); ties (two adjacent rungs sharing a `~NNNN` label) are permitted.
    - No label exceeds `~1800`.
    - `--check` exits 0 on the freshly generated file and would exit 1 (DRIFT) if the TS were hand-edited (spot-verify by a throwaway edit, then revert).
    - A unit assertion (in the script or a manual reproduction) shows that clamping a >1800 value BEFORE pooling would change a lower rung's label vs clamping after — the post-pool clamp order is honored.
  </acceptance_criteria>
  <done>The codegen produces a monotone-within-style, 1800-capped, round-50 personaCalibration.ts with a working --check drift gate; the bootstrap file is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Operator runbook script + CI drift-check step</name>
  <files>bin/run_persona_calibration_sweep.sh, .github/workflows/ci.yml</files>
  <read_first>
    - bin/run_bot_curves_sweep.sh (the launch/combine/fit pipeline shape + nproc preflight to mirror)
    - bin/preset-supervisor.sh (reused as-is — generic over `<name> <blend> <elo-csv> [adopt-pid]`; understand its launch() to pass persona-sweep params or add a --style flag)
    - .github/workflows/ci.yml (the existing "Bot strength curves drift check" step ~lines 58-63 — mirror it exactly for the persona pair)
    - scripts/calibration-harness.mjs (the persona-mode CLI entry the runbook invokes)
    - scripts/lib/calibration-persona-cell-schedule.mjs (the 24-cell schedule the runbook drives)
  </read_first>
  <action>
    Write `bin/run_persona_calibration_sweep.sh` mirroring run_bot_curves_sweep.sh: an operator runbook that (a) preflights machine capacity (nproc vs footprint, warn on oversubscription), (b) launches the persona-cell schedule through `bin/preset-supervisor.sh` (NEVER the bare driver — the onnxruntime-web wasm OOB crash ~5-6h into blend>0 runs needs the supervisor's --resume auto-relaunch), using `--games-per-cell 24` to match the SEED-098 budget, (c) combines the per-persona ledgers into `reports/data/persona-calibration-cells.tsv`, and (d) invokes `scripts/calibration_persona_fit.py` then `scripts/gen_persona_calibration.py`. Include an in-file header comment documenting the ~2-overnight-run budget, the wasm-OOB crash mode, and that ledger append-mode resume self-heals. Then add a `Persona calibration drift check` step to ci.yml immediately after the `Bot strength curves drift check` step: run `uv run python scripts/gen_persona_calibration.py` then `git diff --exit-code frontend/src/generated/personaCalibration.ts reports/data/persona-calibration.json`.
  </action>
  <verify>
    <automated>bash -n bin/run_persona_calibration_sweep.sh && python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('ci yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `bin/run_persona_calibration_sweep.sh` passes `bash -n`, is executable, references `bin/preset-supervisor.sh` (grep) and `--games-per-cell 24`, and never invokes the harness driver directly for the long run.
    - ci.yml parses as valid YAML and contains a `Persona calibration drift check` step running the generator + `git diff --exit-code` on both persona artifacts.
  </acceptance_criteria>
  <done>The operator has a supervised runbook and CI enforces persona-calibration generated-file drift, mirroring the botStrengthCurves precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep TSV/ledger → fit script | Operator-produced measurement data crosses into the fit; a corrupted/mismatched schema yields plausible-but-wrong labels. |
| generated file → repo | Hand-edits to personaCalibration.ts could diverge from the source of truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-184-02-01 | Tampering | fit TSV/ledger ingestion | high | mitigate | Reuse the fail-loud load_bot_cells discipline (header-mismatch, truncated-final-line, duplicate-key, 24-count guard all raise, never silently degrade). |
| T-184-02-02 | Tampering | generated personaCalibration.ts | medium | mitigate | `--check` drift mode + `git diff --exit-code` CI step blocks a hand-edited/stale generated file. |
| T-184-02-03 | Information disclosure | approx_blitz conversion bias | low | accept | Reusing pooled g_preset_combined (vs per-persona refit) may add a small systematic bias absorbed by the ±100-225 band (RESEARCH A2); accepted per CONTEXT discretion. |
| T-184-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs — stdlib-only Python + existing tooling (Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `uv run python scripts/calibration_persona_fit.py --bootstrap && --self-test` succeed.
- `uv run python scripts/gen_persona_calibration.py --check` passes; `git diff --exit-code` clean on both persona artifacts.
- `bash -n bin/run_persona_calibration_sweep.sh` and ci.yml YAML parse succeed.
</verification>

<success_criteria>
- Fit fits Maia/SF families separately, reads G/C from source, fails loud on <24 personas.
- Codegen PAVA-pools per style, clamps 1800 post-pool, rounds 50, emits a complete Record<PersonaId>.
- Bootstrap generated file committed; CI drift gate wired; supervised runbook shipped.
</success_criteria>

<output>
Create `.planning/phases/184-persona-calibration-strength-honesty/184-02-SUMMARY.md` when done.
</output>
