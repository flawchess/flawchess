---
phase: 159-flawchess-engine-policy-temperature-root-move-findability-se
plan: 04
type: execute
wave: 3
depends_on: [159-02, 159-03]
files_modified:
  - frontend/src/components/analysis/TemperatureSelector.tsx
  - frontend/src/components/analysis/__tests__/TemperatureSelector.test.tsx
  - frontend/src/hooks/useFlawChessEngine.ts
  - frontend/src/pages/Analysis.tsx
autonomous: true
requirements: [SEED-085]
must_haves:
  truths:
    - "A temperature slider renders directly below the ELO slider in BOTH the mobile Human tab and the desktop human column (mobile parity), with plain-language labeling (D-09: a 'Play style'/'Fallibility' label, 'Sharper' ↔ 'More human' endpoint captions, the numeric value shown subtly at text-sm, no 'Temperature'/'T=' jargon in primary copy), a data-testid, an aria-label, and a Radix thumbLabels entry."
    - "The slider is log-symmetric over 0.5–2.0 around a default of 1.0: 1.0 is exactly at the visual center (strict === 1 at center, so the search no-op short-circuit fires there — Pitfall 7), halving and doubling are equal visual steps. State is session-only, resets to 1.0 on every page load (matches the ELO slider; no localStorage/URL param, D-08)."
    - "The selected temperature threads through useFlawChessEngine into SearchBudget.policyTemperature (defaulted to DEFAULT_POLICY_TEMPERATURE at the call site, keeping the no-op short-circuit visible at the orchestrator layer); moving the slider re-runs the search with the new temperature and visibly reshapes the FlawChess ranking, while the Maia 'Moves by Rating' chart continues to show RAW Maia data (D-06)."
  artifacts:
    - "frontend/src/components/analysis/TemperatureSelector.tsx (mirrors EloSelector) exporting the component + TEMPERATURE_MIN/MAX/DEFAULT + the log position↔temperature mapping helpers."
    - "useFlawChessEngine.ts accepting a policyTemperature option and putting it on SearchBudget."
    - "Analysis.tsx holding session temperature state and rendering TemperatureSelector below the shared eloSelector JSX (both layouts)."
  key_links:
    - "Analysis.tsx: useState(TEMPERATURE_DEFAULT) → passed to useFlawChessEngine({ ..., policyTemperature }) AND to <TemperatureSelector value=... onChange=... /> rendered inside the shared eloSelector block (so both the mobile humanTab and desktop human column get it once)."
    - "useFlawChessEngine builds SearchBudget with policyTemperature: policyTemperature ?? DEFAULT_POLICY_TEMPERATURE and re-runs the search effect when policyTemperature changes (added to the effect deps)."
    - "TEMPERATURE_DEFAULT === DEFAULT_POLICY_TEMPERATURE (asserted) so the slider's center is the search no-op value."
---

<objective>
Thread A's user-facing knob: a policy-temperature slider directly below the ELO slider that lets the user flatten toward more human fallibility (T>1) or sharpen toward Stockfish (T<1), composing with the plan 159-03 search transform and the plan 159-01 findability ranking. This plan is UI + hook wiring only; the pure transform and search-core plumbing already exist (159-03).

The slider mirrors EloSelector but over a continuous log-symmetric 0.5–2.0 range (D-08) with plain-language copy (D-09). The critical correctness detail is Pitfall 7: 1.0 must be EXACTLY at the visual center so the search's `temperature !== DEFAULT_POLICY_TEMPERATURE` no-op short-circuit fires for the untouched default that every user starts at.

Purpose: expose the composed Thread A+B behavior on `/analysis` and let the phase's real-position D-03 regression cases be verified live (the phase-close checkpoint).
Output: `TemperatureSelector.tsx`, a `policyTemperature` option on `useFlawChessEngine`, Analysis.tsx state + dual render, and a blocking human-verify checkpoint validating the composed behavior against the D-03 cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-CONTEXT.md
@.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-RESEARCH.md
@.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TemperatureSelector.tsx (log-symmetric slider) + mapping tests</name>
  <files>frontend/src/components/analysis/TemperatureSelector.tsx, frontend/src/components/analysis/__tests__/TemperatureSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/analysis/EloSelector.tsx (the exact structure/styling/testid/thumbLabels pattern to mirror; value-label span styling text-sm font-medium tabular-nums)
    - frontend/src/components/analysis/__tests__/EloSelector.test.tsx (test style to mirror)
    - frontend/src/components/ui/slider.tsx (the underlying Radix Slider — linear only; the log mapping must happen at the component boundary)
    - frontend/src/lib/engine/policyTemperature.ts (DEFAULT_POLICY_TEMPERATURE — TEMPERATURE_DEFAULT must equal it)
    - 159-RESEARCH.md "Log-symmetric slider value mapping (D-08)" code example (lines ~527-546) and Pitfall 7 (center exactness)
    - CLAUDE.md Frontend rules (data-testid on interactive elements, aria-label on controls, text-sm floor, theme constants)
  </read_first>
  <behavior>
    - sliderPositionToTemperature(0) === 1 (strict), sliderPositionToTemperature(1) === 2, sliderPositionToTemperature(-1) === 0.5.
    - temperatureToSliderPosition(1) === 0, temperatureToSliderPosition(2) === 1, temperatureToSliderPosition(0.5) === -1 (round-trip inverse of the above, matching Math.log2/2** bases — Pitfall 7).
    - TEMPERATURE_MIN === 0.5, TEMPERATURE_MAX === 2.0, TEMPERATURE_DEFAULT === 1.0, and TEMPERATURE_DEFAULT === DEFAULT_POLICY_TEMPERATURE.
  </behavior>
  <action>
Create `frontend/src/components/analysis/TemperatureSelector.tsx` mirroring EloSelector's structure. Export named constants `TEMPERATURE_MIN = 0.5`, `TEMPERATURE_MAX = 2.0`, `TEMPERATURE_DEFAULT = 1.0`, and the pure boundary-mapping helpers `sliderPositionToTemperature(position) = 2 ** position` and `temperatureToSliderPosition(temperature) = Math.log2(temperature)` (consistent bases — Pitfall 7). Keep the underlying `<Slider>` LINEAR with min=-1, max=1, a small step (e.g. 0.01), value = [temperatureToSliderPosition(value)]; convert back via sliderPositionToTemperature in the onValueChange handler. Props: `value: number`, `onChange: (temperature: number) => void`.

Copy per D-09: a plain-language group label (e.g. "Play style") and endpoint captions "Sharper" ↔ "More human"; show the numeric temperature subtly at `text-sm font-medium tabular-nums` formatted to one decimal place (e.g. "1.0"), no "T=" prefix, no "Temperature" jargon in the primary copy. Add `data-testid="analysis-temperature-selector"`, `data-testid="analysis-temperature-selector-value"` on the numeric span, an `aria-label` on the group, and a `thumbLabels` entry on the Slider (Radix per-thumb ARIA). Reuse FLAWCHESS_ENGINE_ACCENT or an existing theme constant for any accent — never hard-code a color (add to theme.ts if a new one is needed). Respect the text-sm floor (no text-xs). Explicit React.ReactElement return type.

Create `TemperatureSelector.test.tsx` asserting the Behavior block (especially strict `=== 1` at center per Pitfall 7 — not toBeCloseTo) plus the presence of the data-testid and aria-label. Assert `TEMPERATURE_DEFAULT === DEFAULT_POLICY_TEMPERATURE`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analysis/__tests__/TemperatureSelector.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Test passes, including strict `sliderPositionToTemperature(0) === 1` and `TEMPERATURE_DEFAULT === DEFAULT_POLICY_TEMPERATURE`.
    - TemperatureSelector.tsx has data-testid `analysis-temperature-selector`, an aria-label, a thumbLabels prop, and no `text-xs` (grep confirms absence of text-xs).
    - No "Temperature" or "T=" string in the rendered primary label copy (endpoint captions + subtle numeric only).
    - `cd frontend && npx tsc -b` zero errors.
  </acceptance_criteria>
  <done>Log-symmetric slider component with exact center, plain-language copy, a11y attributes, and mapping tests.</done>
</task>

<task type="auto">
  <name>Task 2: Thread policyTemperature through the hook + render slider in both layouts</name>
  <files>frontend/src/hooks/useFlawChessEngine.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/hooks/useFlawChessEngine.ts (UseFlawChessEngineOptions ~lines 48-59, SearchBudget construction ~lines 211-219, the search-trigger effect deps ~line 243)
    - frontend/src/pages/Analysis.tsx (useFlawChessEngine call ~lines 538-542, the shared `eloSelector` JSX const ~lines 1621-1625, its two usages in humanTab ~line 1649 and desktop human column ~line 1828, useMaiaEloDefault ~line 499 as the session-state precedent)
    - frontend/src/components/analysis/TemperatureSelector.tsx (from Task 1 — component + TEMPERATURE_DEFAULT)
    - frontend/src/lib/engine/policyTemperature.ts (DEFAULT_POLICY_TEMPERATURE)
    - 159-PATTERNS.md "useFlawChessEngine.ts (EDIT)" and "Analysis.tsx (EDIT — new session state + dual mobile/desktop render)"
  </read_first>
  <action>
Edit `useFlawChessEngine.ts`: add `policyTemperature?: number` to `UseFlawChessEngineOptions`, destructure it in the hook, put `policyTemperature: policyTemperature ?? DEFAULT_POLICY_TEMPERATURE` on the constructed `SearchBudget` (keep the `?? DEFAULT_POLICY_TEMPERATURE` at the call site, not hidden inside — the no-op short-circuit stays visible at the orchestrator layer, Pitfall 1), and add `policyTemperature` to the search-trigger effect's dependency array so changing it re-runs the search. Import DEFAULT_POLICY_TEMPERATURE from `@/lib/engine/policyTemperature`.

Edit `Analysis.tsx`: add session-only state `const [temperature, setTemperature] = useState(TEMPERATURE_DEFAULT)` (plain useState — no async re-derivation, no persistence, resets on reload per D-08). Pass `policyTemperature: temperature` into the existing `useFlawChessEngine({ ... })` call. Render `<TemperatureSelector value={temperature} onChange={setTemperature} />` directly below the `<EloSelector>` inside the SHARED `eloSelector` JSX const (which is already rendered in both the mobile humanTab and the desktop human column) — this achieves mobile/desktop parity in one place. Keep the existing `data-testid="analysis-elo-selector-row"` wrapper or add a sibling wrapper row for the temperature slider.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run lint</automated>
    <human-check>
The composed Thread A + B behavior on /analysis (findability ranking from 159-01, temperature transform from 159-03, this plan's slider, and the raw-Maia-gated verdict from 159-02). Run `cd frontend && npm run dev`, open `/analysis`, and confirm:
1. D-03 @600: at the position where Nb5 (~5% Maia) was previously "Best" with Qxf2 (~9%, Good) / Rxf2 (~57%, Mistake) as candidates, ELO 600 → the FlawChess card's top line is Qxf2, NOT Nb5 and NOT Rxf2; the badge still shows the practical score V (D-04). (Reproduce from a live 600-ELO analysis of the SEED-085 position if the exact FEN is not at hand — deterministic per ENGINE-07.)
2. D-03 @1000: at the position where Qb8 was a tail move outside the plotted chart set, ELO 1000 → Qb8 does NOT top the FlawChess ranking.
3. Slider composition: drag "Play style" toward "More human" (T>1) → the FlawChess ranking reshapes (more-human moves gain), the numeric value updates (e.g. 1.4), and the Maia "Moves by Rating" chart still shows RAW Maia data unchanged (D-06). Return to center → reads exactly 1.0 and the ranking matches the untouched default.
4. Verdict copy: a safe-tier position where the FC pick is genuinely more findable AND in the plotted set shows "far easier to find and play"; a position where the FC pick is NOT in the plotted set (or below margin) shows the fallback wording with NO findability claim, never contradicting the chart beneath it.
5. Mobile parity: repeat step 3 in the mobile Human tab (narrow window) — the slider is present and functional.
If any D-03 case fails, capture the FEN + observed-vs-expected top move so P_REF_ANCHORS / FINDABILITY_MARGIN can be recalibrated.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `SearchBudget` in useFlawChessEngine.ts includes `policyTemperature: policyTemperature ?? DEFAULT_POLICY_TEMPERATURE` (grep) and the effect deps include `policyTemperature`.
    - Analysis.tsx renders `<TemperatureSelector` exactly once inside the shared `eloSelector` block and passes `policyTemperature: temperature` to useFlawChessEngine (grep).
    - `cd frontend && npx tsc -b` zero errors; `npm run lint` passes (knip: TemperatureSelector + its exports are imported).
    - The slider appears in both the mobile Human tab and the desktop human column (single shared JSX — verified structurally by the one render site being reused in both).
  </acceptance_criteria>
  <done>Temperature state threads into the search budget and re-runs on change; slider renders once, visible in both layouts; tsc + lint clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- `frontend/src/components/analysis/TemperatureSelector.tsx` (NEW FILE)
- `TemperatureSelector` (component), `TEMPERATURE_MIN`, `TEMPERATURE_MAX`, `TEMPERATURE_DEFAULT`, `sliderPositionToTemperature`, `temperatureToSliderPosition` (exported from TemperatureSelector.tsx)
- `frontend/src/components/analysis/__tests__/TemperatureSelector.test.tsx` (NEW FILE)
- New optional `policyTemperature?: number` on `UseFlawChessEngineOptions` (useFlawChessEngine.ts)
- New `temperature` session state + TemperatureSelector render in Analysis.tsx
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→client control | The slider is a bounded numeric control feeding a pure in-browser search transform. No persistence, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-08 | Tampering | log-slider center exactness | high | mitigate | Strict `sliderPositionToTemperature(0) === 1` unit test + TEMPERATURE_DEFAULT === DEFAULT_POLICY_TEMPERATURE assertion, so the default position hits the search no-op short-circuit (Pitfall 7) and never silently drifts default-user output. |
| T-159-09 | Denial of Service | search re-run on every slider tick | low | mitigate | The hook's existing FEN-navigation debounce + onSnapshot throttle already bound re-run cadence; policyTemperature changes reuse the same throttled search effect. |
| T-159-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase; the Radix Slider is already a dependency (reused, not added). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/analysis/__tests__/TemperatureSelector.test.tsx` green.
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` all clean (full-suite + type-check per CLAUDE.md pre-merge gate; this is the last plan so the full frontend gate runs here).
- Task 2 `<verify><human-check>` (D-03 cases, slider composition, raw-chart non-contradiction, mobile parity) harvested into the end-of-phase UAT batch (human_verify_mode=end-of-phase).
</verification>

<success_criteria>
- Slider renders below the ELO slider in both layouts, log-symmetric, center = 1.0 exactly.
- Temperature threads into SearchBudget and re-runs the search; Maia chart stays raw.
- End-of-phase human-check confirms the three D-03 cases and the composed behavior on real positions.
</success_criteria>

<output>
Create `.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-04-SUMMARY.md` when done.
</output>
