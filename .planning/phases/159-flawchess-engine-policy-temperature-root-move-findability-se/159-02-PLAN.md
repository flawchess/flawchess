---
phase: 159-flawchess-engine-policy-temperature-root-move-findability-se
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/flawChessVerdict.ts
  - frontend/src/lib/flawChessVerdict.test.ts
  - frontend/src/components/analysis/FlawChessAgreementVerdict.tsx
  - frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx
  - frontend/src/pages/Analysis.tsx
autonomous: true
requirements: [SEED-085]
must_haves:
  truths:
    - "The agreement-verdict 'far easier to find and play' findability claim renders ONLY when both conditions hold (D-10): P_you(FC pick) exceeds P_you(SF best) by FINDABILITY_MARGIN, AND the FC pick is inside the chart's plotted candidate set (shownSans). When the gate fails, a D-11 fallback prose variant renders with no findability claim."
    - "The gate reads RAW Maia probability at the selected ELO — the same distribution the Moves-by-Rating chart displays (D-12) — so the prose can never contradict the chart rendered beneath it; the gate never reads any search-internal temperature-adjusted prior (structurally impossible: RankedLine has no prior field, flawChessVerdict.ts imports nothing temperature-related)."
    - "The raw-probability-by-SAN map and shownSans are computed ONCE in Analysis.tsx (from nearestByElo(maia.perElo, selectedElo)) and passed as props; FlawChessAgreementVerdict never calls nearestByElo independently (Pitfall 5 — no ELO-rung-boundary drift)."
  artifacts:
    - "flawChessVerdict.ts exporting FINDABILITY_MARGIN and computeFindabilityGate(pYouFc, pYouSf, fcInPlottedSet)."
    - "FlawChessAgreementVerdict.tsx wiring the gate and one new D-11 fallback prose variant in the safe tier."
    - "Analysis.tsx passing a rawProbBySan map + shownSans down to FlawChessAgreementVerdict."
  key_links:
    - "computeFindabilityGate is called in FlawChessAgreementVerdict.tsx with pYouFc = rawProbBySan[fcSan], pYouSf = rawProbBySan[sfSan], fcInPlottedSet = shownSans.includes(fcSan)."
    - "Analysis.tsx computes rawProbBySan = nearestByElo(maia.perElo, selectedElo)?.moveProbabilities ?? {} once and threads it + shownSans into the verdict component at both render sites."
---

<objective>
The SEED-085 ride-along: gate the agreement-verdict copy on the FlawChess pick's actual Maia probability so the words can never contradict the Maia "Moves by Rating" chart rendered directly beneath them. The current safe-tier prose asserts "far easier to find and play" unconditionally; per D-10/D-11/D-12 that claim must fire only when the raw Maia data visibly backs it, and otherwise fall back to wording the evals actually support (nearly as good / safer follow-ups).

Purpose: close the specific self-contradiction defect (prose says "easier to find" for a move the chart shows as rare) by wiring a pure gate to the SAME raw-Maia, ELO-keyed data source the chart already uses. This plan is architecturally decoupled from Threads A and B (it reads raw Maia, never the search-internal prior) and can run in parallel with the engine-core work (wave 1).
Output: a pure `computeFindabilityGate` + `FINDABILITY_MARGIN`, one new safe-tier prose variant, and the Analysis.tsx prop wiring that feeds raw probabilities down once.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-CONTEXT.md
@.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-RESEARCH.md
@.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add computeFindabilityGate + FINDABILITY_MARGIN to flawChessVerdict.ts + tests</name>
  <files>frontend/src/lib/flawChessVerdict.ts, frontend/src/lib/flawChessVerdict.test.ts</files>
  <read_first>
    - frontend/src/lib/flawChessVerdict.ts (the pure worker-free chess.js-free module header + constraint; existing named-threshold + pure-function convention; SHARP_DROP_THRESHOLD/NEARLY_SAME_EVAL_CP scale reference; it imports ONLY RankedLine from lib/engine)
    - frontend/src/lib/flawChessVerdict.test.ts (existing test style to extend)
    - frontend/src/generated/flawThresholds.ts (INACCURACY_DROP/BLUNDER_DROP — scale reference for the new margin constant)
    - 159-RESEARCH.md "Pattern 3: Verdict gate reads a structurally separate data source" and Assumptions Log A2 (FINDABILITY_MARGIN tuning)
  </read_first>
  <behavior>
    - computeFindabilityGate(pYouFc, pYouSf, fcInPlottedSet) returns true iff pYouFc != null AND pYouSf != null AND fcInPlottedSet === true AND pYouFc > pYouSf + FINDABILITY_MARGIN.
    - Returns false (never throws) when either probability is null.
    - Returns false when fcInPlottedSet is false even if the margin is exceeded.
    - Returns false when the margin is not exceeded even if fcInPlottedSet is true.
  </behavior>
  <action>
Add to `frontend/src/lib/flawChessVerdict.ts` a named exported constant `FINDABILITY_MARGIN` (starting value 0.05, documented as needing UAT tuning like SHARP_DROP_THRESHOLD/NEARLY_SAME_EVAL_CP — same order of magnitude as INACCURACY_DROP; cite D-10) and an exported pure function `computeFindabilityGate(pYouFc: number | null, pYouSf: number | null, fcInPlottedSet: boolean): boolean` implementing the Behavior block. Explicit return type. Add a doc comment stating both probabilities MUST be raw Maia at the selected ELO (D-12) — never the search-internal temperature-adjusted prior, which this module cannot even see (RankedLine has no prior field). Preserve the module's structural constraint: do NOT add any new import from `lib/engine/` beyond the existing RankedLine type; do NOT import maiaQueue/select/policyTemperature.

Extend `flawChessVerdict.test.ts` with cases for the Behavior block: both-conditions-true fires; margin-not-exceeded fails; not-in-plotted-set fails; null inputs return false without throwing.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/flawChessVerdict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/flawChessVerdict.test.ts` passes.
    - flawChessVerdict.ts exports `FINDABILITY_MARGIN` and `computeFindabilityGate` (grep confirms both).
    - flawChessVerdict.ts imports nothing from `@/lib/engine` except the RankedLine type (grep: only the pre-existing `import type { RankedLine }` remains).
    - Test covers all four Behavior outcomes (fires, margin-fail, plotted-set-fail, null-safe).
  </acceptance_criteria>
  <done>Pure gate + margin constant exist and tested; no new engine imports.</done>
</task>

<task type="auto">
  <name>Task 2: Wire gate into FlawChessAgreementVerdict + Analysis.tsx raw-prob props</name>
  <files>frontend/src/components/analysis/FlawChessAgreementVerdict.tsx, frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/components/analysis/FlawChessAgreementVerdict.tsx (existing props, uciToSan helper already in this file, renderVerdictSentence safe-tier branch lines ~153-165 where "far easier to find and play" lives, the fcSan/sfSan useMemos)
    - frontend/src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx (existing test style)
    - frontend/src/pages/Analysis.tsx (shownSans useMemo ~lines 685-687, FlawChessAgreementVerdict render ~lines 1602-1611, maia.perElo + selectedElo in scope)
    - frontend/src/lib/moveQuality.ts (nearestByElo signature ~line 115 returning MoveCurvePoint with moveProbabilities keyed by SAN; selectCandidatesByMass producing shownSans)
    - frontend/src/hooks/useMaiaEngine.ts (MoveCurvePoint: { elo, moveProbabilities: Record<string, number> })
    - 159-PATTERNS.md "FlawChessAgreementVerdict.tsx (EDIT — wire gate, D-11 fallback prose)" and "Analysis.tsx (EDIT ... Raw-probability wiring)"
  </read_first>
  <action>
In `Analysis.tsx`, compute a `rawProbBySan` map ONCE: `nearestByElo(maia.perElo, selectedElo)?.moveProbabilities ?? {}` (memoized on maia.perElo + selectedElo, mirroring the existing shownSans memo). Pass two new props to both `FlawChessAgreementVerdict` render sites (there is a single render in flawChessCard — confirm it flows to both mobile humanTab and desktop via the shared flawChessCard JSX): `rawProbBySan` and `shownSans` (the existing memo). Do NOT let the verdict component call nearestByElo itself (Pitfall 5).

In `FlawChessAgreementVerdict.tsx`, add `rawProbBySan: Record<string, number>` and `shownSans: string[]` to the props interface. Resolve `pYouFc = fcSan ? (rawProbBySan[fcSan] ?? null) : null`, `pYouSf = sfSan ? (rawProbBySan[sfSan] ?? null) : null`, `fcInPlottedSet = fcSan ? shownSans.includes(fcSan) : false` (narrow the Record access per noUncheckedIndexedAccess). Call `computeFindabilityGate(pYouFc, pYouSf, fcInPlottedSet)` and thread the boolean into `renderVerdictSentence`. In the safe tier, split the current copy: when the gate is TRUE keep the findability wording ("far easier to find and play" / "a safe, practical pick that's far easier to find and play"); when the gate is FALSE render the new D-11 fallback variant that makes NO findability claim (wording the evals support: nearly as good / safer follow-ups), following the popover-copy-minimalism norm (no jargon, no p-values). The sharp and aligned tiers are unchanged.

Extend `FlawChessAgreementVerdict.test.tsx`: render with props where the gate passes (FC pick has clearly higher raw prob and is in shownSans) asserting the findability phrase appears, and where it fails (FC pick not in shownSans, or below margin) asserting the fallback phrase appears and the findability phrase does NOT.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx` passes.
    - Gate-pass render contains the findability phrase; gate-fail render contains the fallback phrase and NOT the findability phrase (asserted in tests).
    - FlawChessAgreementVerdict.tsx does not import nearestByElo (grep confirms absence).
    - Analysis.tsx computes rawProbBySan via nearestByElo once and passes rawProbBySan + shownSans to the verdict component (grep: `rawProbBySan` prop present at the render site).
    - `cd frontend && npx tsc -b` reports zero errors.
  </acceptance_criteria>
  <done>Verdict copy is gated on raw Maia probability; fallback variant renders when the gate fails; raw probs wired once from Analysis.tsx; tests green; tsc clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `FINDABILITY_MARGIN` (exported const, flawChessVerdict.ts)
- `computeFindabilityGate(pYouFc, pYouSf, fcInPlottedSet): boolean` (exported function, flawChessVerdict.ts)
- New props `rawProbBySan: Record<string, number>` and `shownSans: string[]` on `FlawChessAgreementVerdictProps`
- New D-11 safe-tier fallback prose variant in `renderVerdictSentence` (FlawChessAgreementVerdict.tsx)
- `rawProbBySan` memo in Analysis.tsx
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render→user | Verdict prose asserts a factual claim about move findability. The trust concern is truthfulness/consistency with the chart, not a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-03 | Repudiation | verdict prose vs chart | medium | mitigate | Gate reads the SAME raw-Maia ELO rung as the chart (single rawProbBySan computed once, D-12/Pitfall 5) so prose and chart cannot disagree at an ELO-rung boundary. |
| T-159-04 | Tampering | Record index access on rawProbBySan | low | mitigate | noUncheckedIndexedAccess narrowing (`?? null`) — no undefined leaks into the numeric comparison. |
| T-159-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase; no supply-chain surface. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/flawChessVerdict.test.ts src/components/analysis/__tests__/FlawChessAgreementVerdict.test.tsx` green.
- `cd frontend && npx tsc -b` zero errors; `npm run lint` passes (knip: computeFindabilityGate imported by FlawChessAgreementVerdict.tsx).
</verification>

<success_criteria>
- Findability claim renders only when gate passes; D-11 fallback otherwise.
- Gate reads raw Maia at selected ELO, computed once in Analysis.tsx.
- No new engine imports in flawChessVerdict.ts; no independent nearestByElo call in the verdict component.
</success_criteria>

<output>
Create `.planning/phases/159-flawchess-engine-policy-temperature-root-move-findability-se/159-02-SUMMARY.md` when done.
</output>
