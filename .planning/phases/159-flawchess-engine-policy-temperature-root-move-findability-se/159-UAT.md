---
status: completed
phase: 159-flawchess-engine-policy-temperature-root-move-findability-se
source: [159-VERIFICATION.md]
started: 2026-07-07T21:08:18Z
updated: 2026-07-08T15:48:57Z
---

## Tests

### 1. D-03 @600 live case — findability re-sort
expected: At a real 600-ELO position, the FlawChess card's top line is Qxf2 (NOT Nb5, NOT Rxf2), and each move's practical-score badge still reflects V(X) unchanged.
result: passed

### 2. D-03 @1000 live case — tail move suppressed
expected: At a real 1000-ELO position where Qb8 was a ~5%-prior tail move outside the plotted Maia chart set, Qb8 is not rankedLines[0].
result: passed

### 3. Slider composition — ranking reshapes, chart stays raw
expected: Dragging the "Play style" slider toward "More human" (T>1) visibly reshapes the FlawChess ranking (more-human moves gain), the numeric value updates, and the Maia "Moves by Rating" chart continues to show RAW (unreshaped) Maia data. Returning the slider to center reads exactly 1.0 and matches the untouched default-temperature output.
result: passed

### 4. Verdict copy non-contradiction
expected: At a safe-tier position where the FC pick is genuinely more findable AND inside the plotted Maia chart set, the verdict prose says "far easier to find and play." At a safe-tier position where the FC pick is NOT in the plotted set (or below FINDABILITY_MARGIN), the fallback wording renders ("safer follow-ups" phrasing) with NO findability claim. Prose never asserts findability ease for a pick the chart shows as rare/absent.
result: passed

### 5. Mobile parity — slider in the mobile Human tab
expected: Repeating the slider-composition check in the mobile Human tab (narrow viewport), the temperature slider is present, functional, and reshapes the ranking identically to desktop.
result: passed

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

_Note for the UAT run:_ also validate/retune `P_REF_ANCHORS` (findability.ts) and `ROOT_CANDIDATE_HARD_CAP` (policyTemperature.ts, =15) against real observed behavior — the code flags both as unvalidated hypotheses. Factor in REVIEW.md WR-02: the anchors may need recalibration in renormalized-prior space, not raw-probability space. Also address REVIEW.md WR-01 (stale "pre-sorted by practicalScore" doc comments in `types.ts`/`useFlawChessEngine.ts` and the `FlawChessEngineLines.tsx` badge-shade comment) as a doc/cosmetic follow-up.
