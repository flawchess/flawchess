---
phase: 66-frontend-endgameinsightsblock-beta-flag
plan: 05
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [BETA-01]
tags: [docs, alignment]

must_haves:
  truths:
    - "REQUIREMENTS.md BETA-01 references users.beta_enabled (not insights_beta_enabled)"
    - "ROADMAP.md Phase 66 goal references beta_enabled (not insights_beta_enabled)"
    - "ROADMAP.md Phase 67 success criterion #3 references beta_enabled (not insights_beta_enabled)"
    - "A grep for 'insights_beta_enabled' across REQUIREMENTS.md + ROADMAP.md returns zero matches"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BETA-01 aligned with D-15 column name"
      contains: "beta_enabled"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 66/67 language aligned with D-15"
      contains: "beta_enabled"
  key_links:
    - from: ".planning/REQUIREMENTS.md BETA-01"
      to: "D-15 locked column name"
      via: "direct text edit replacing insights_beta_enabled → beta_enabled"
      pattern: "beta_enabled"
    - from: ".planning/ROADMAP.md Phase 67 SC#3"
      to: "D-15 locked column name"
      via: "direct text edit"
      pattern: "beta_enabled"
---

<objective>
Align planning documents with the locked column name `users.beta_enabled` (D-15) by replacing references to the placeholder `insights_beta_enabled` in REQUIREMENTS.md and ROADMAP.md. CONTEXT.md is explicit: "REQUIREMENTS.md BETA-01 example text and ROADMAP.md Phase 66/67 references to `users.insights_beta_enabled` → `users.beta_enabled` are edited in the same commit as the Alembic migration."

This plan is wave-independent of Plans 02-04 (pure docs edit) and can run in parallel with Plan 02.

Purpose: Keep prose and schema in sync. Prevent future confusion where a reader sees `insights_beta_enabled` in REQUIREMENTS.md but `beta_enabled` in the DB.
Output: 2 modified planning docs, grep-verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/66-frontend-endgameinsightsblock-beta-flag/66-CONTEXT.md

<interfaces>
<!-- Current BETA-01 in REQUIREMENTS.md line 47 -->
```
- [ ] **BETA-01**: Beta access is controlled by a boolean flag on the `users` table (e.g. `insights_beta_enabled`). Default `false`. Flipping the flag is a direct DB operation (no user-settings UI, no admin page) — the whole point is a small, hand-picked validation cohort
```

<!-- Current Phase 66 goal in ROADMAP.md line 213 -->
```
**Goal**: Users with `insights_beta_enabled=true` see a "Generate insights" button on the Endgame tab ...
```

<!-- Current Phase 67 success criterion #3 in ROADMAP.md line 232 -->
```
3. The `users.insights_beta_enabled` flag has been flipped to `true` for the hand-picked beta cohort via direct DB operation; the rest of the user base continues to see no insights UI
```

<!-- Also Phase 66 success criterion #1 on line 217 references insights_beta_enabled -->
```
1. A user with `insights_beta_enabled=true` sees a "Generate insights" button on the Endgame tab; a user with the flag `false` does not see the button or the block at all
```
</interfaces>
</context>

<tasks>

<task type="execute" tdd="false">
  <name>Task 1: Replace insights_beta_enabled with beta_enabled across REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read fully — find every `insights_beta_enabled` occurrence)
    - .planning/ROADMAP.md (read fully — find every `insights_beta_enabled` occurrence)
  </read_first>
  <action>
    **Step 1: Audit current occurrences**
    ```bash
    grep -n "insights_beta_enabled" .planning/REQUIREMENTS.md .planning/ROADMAP.md
    ```
    Record every line number. Expected (based on current files): 4 occurrences total — 1 in REQUIREMENTS.md (BETA-01), 3 in ROADMAP.md (Phase 66 goal line, Phase 66 SC#1 line, Phase 67 SC#3 line). If PROJECT.md references the name, flag it in the summary but do NOT edit PROJECT.md (scope is REQUIREMENTS + ROADMAP per D-15; PROJECT.md will "naturally realign when Phase 66 ships").

    **Step 2: Edit REQUIREMENTS.md BETA-01** (line 47)

    Change the parenthetical example from `insights_beta_enabled` to `beta_enabled`:
    ```
    - [ ] **BETA-01**: Beta access is controlled by a boolean flag on the `users` table (`beta_enabled`). Default `false`. Flipping the flag is a direct DB operation (no user-settings UI, no admin page) — the whole point is a small, hand-picked validation cohort
    ```
    The parenthetical loses the "e.g." qualifier because `beta_enabled` is now the actual chosen name, not a placeholder example.

    **Step 3: Edit ROADMAP.md Phase 66 goal** (line 213)

    ```
    **Goal**: Users with `beta_enabled=true` see a "Generate insights" button on the Endgame tab and, on click, receive the overview paragraph plus 4 Section blocks rendered inline. Backend config can hide the overview independently while per-section insights stay live.
    ```

    **Step 4: Edit ROADMAP.md Phase 66 success criterion #1** (line 217)

    ```
    1. A user with `beta_enabled=true` sees a "Generate insights" button on the Endgame tab; a user with the flag `false` does not see the button or the block at all
    ```

    **Step 5: Edit ROADMAP.md Phase 67 success criterion #3** (line 232)

    ```
    3. The `users.beta_enabled` flag has been flipped to `true` for the hand-picked beta cohort via direct DB operation; the rest of the user base continues to see no insights UI
    ```

    **Step 6: Edit Phase 66 section header** (line 159)

    Change:
    ```
    - [ ] **Phase 66: Frontend EndgameInsightsBlock & Beta Flag** — Overview + 4 Section blocks inline on the Endgame tab, gated by `users.insights_beta_enabled`
    ```
    To:
    ```
    - [ ] **Phase 66: Frontend EndgameInsightsBlock & Beta Flag** — Overview + 4 Section blocks inline on the Endgame tab, gated by `users.beta_enabled`
    ```

    **Step 7: Verify no stray occurrences remain**
    ```bash
    grep -n "insights_beta_enabled" .planning/REQUIREMENTS.md .planning/ROADMAP.md
    ```
    Must return zero matches.

    **Step 8: Scope check — do NOT edit**
    - `.planning/PROJECT.md` (out of scope per CONTEXT.md: "language will naturally realign when Phase 66 ships")
    - `.planning/seeds/SEED-003-llm-based-insights.md` (historical seed doc, out of scope)
    - `.planning/STATE.md` (auto-managed by GSD tooling)
    - Phase 65 artifacts (they may reference insights_beta_enabled; don't touch sealed phase artifacts)

    If `grep -r "insights_beta_enabled" .planning/` still shows hits in other files, that is expected and OUT OF SCOPE for this plan. Only REQUIREMENTS.md and ROADMAP.md are edited.
  </action>
  <verify>
    <automated>! grep -q "insights_beta_enabled" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "insights_beta_enabled" .planning/REQUIREMENTS.md` returns NO match (exit 1)
    - `grep -q "insights_beta_enabled" .planning/ROADMAP.md` returns NO match (exit 1)
    - `grep -q "beta_enabled" .planning/REQUIREMENTS.md` returns at least one match
    - `grep -c "beta_enabled" .planning/ROADMAP.md` returns at least 4 (Phase 66 header, goal, SC#1, Phase 67 SC#3)
    - BETA-01 in REQUIREMENTS.md does NOT contain "e.g." (the placeholder qualifier is removed) — verify by `grep -A1 "BETA-01" .planning/REQUIREMENTS.md | grep -F "(\`beta_enabled\`)"` matches (the parens now hold the real name directly)
  </acceptance_criteria>
  <done>Both docs reference `beta_enabled` consistently; zero `insights_beta_enabled` references remain in REQUIREMENTS.md + ROADMAP.md; scope strictly limited to those two files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits only internal planning docs with no code impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-14 | Tampering (documentation drift) | Doc says column is named `insights_beta_enabled` but code says `beta_enabled` | mitigate | This plan IS the mitigation — replaces every occurrence in REQUIREMENTS.md + ROADMAP.md. Post-migration the docs and schema agree. |
</threat_model>

<verification>
- `grep -n "insights_beta_enabled" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns zero matches
- `grep -n "beta_enabled" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns the expected edited occurrences
- No other files modified (verify with `git diff --name-only .planning/`)
</verification>

<success_criteria>
- REQUIREMENTS.md BETA-01 references `beta_enabled` as the chosen column name (no placeholder qualifier)
- ROADMAP.md Phase 66 goal, Phase 66 SC#1, Phase 66 header bullet, and Phase 67 SC#3 all reference `beta_enabled`
- Zero `insights_beta_enabled` occurrences remain in those two files
</success_criteria>

<output>
After completion, create `.planning/phases/66-frontend-endgameinsightsblock-beta-flag/66-05-SUMMARY.md` following the standard template.
</output>
