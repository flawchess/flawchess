# 117.1-01 Summary — Post-move write convention + dedup shift

**Status:** Complete (executed inline; full local gate green)
**Requirements:** EVALFIX-01, EVALFIX-02, EVALFIX-03, EVALFIX-05

## What changed

- `app/services/eval_drain.py`
  - `_post_move_eval(pos_eval, ply)` — the single site of the post-move +1 shift. Row k now stores the eval of the position AFTER move k (`pos_eval[k+1]`).
  - `_apply_full_eval_results` — restructured to build a position-keyed `pos_eval` map (incl. the terminal donor) and a decision-keyed `best_move` map, then write each row's eval via `_post_move_eval`. `best_move` is written independent of an after-eval hole.
  - `_collect_full_ply_targets(..., include_terminal)` — engine games append a terminal eval-donor (`is_terminal=True`) so the last move has an after-eval; skipped for game-over terminals (checkmate/stalemate). Lichess games pass `include_terminal=False`.
  - `_fetch_dedup_evals` — reworked to a one-ply self-join: a position's own eval is recovered from the donor row that REACHED it (eval from `cur`, best_move from `nxt`); the lines 182-191 convention comment is rewritten for post-move. `best_move` transplant unchanged.
  - `_FullPlyEvalTarget.is_terminal` flag added.
- `app/services/flaws_service.py` — `_run_all_moves_pass`: NO logic change; docstring clarified that the post-move convention is now uniform across sources.

## Tests

- `tests/services/test_full_eval_drain.py` — updated existing drain tests to the post-move convention (terminal engine call, shifted writes, self-join dedup with predecessor rows). 22/22 pass.
- `tests/services/test_flaws_service.py` — new `TestSeed044PostMoveConvention`: fixture 1420780 h3?? blunder detected under post-move, MISSED under pre-push; lichess-coherent guard (640092).

## Verification

- `_post_move_eval` is the only +1 site (grep-verified). `best_move`/`pv` stay decision-ply-keyed. ruff + ty clean. Backend suite 2591 pass.
