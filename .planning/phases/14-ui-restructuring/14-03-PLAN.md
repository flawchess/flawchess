---
phase: 14-ui-restructuring
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified: []
autonomous: false
requirements: [UIRS-01, UIRS-02, UIRS-03, UIRS-04]

must_haves:
  truths:
    - "All four UIRS requirements verified by human in browser"
    - "No broken links or routes"
    - "Import progress toasts work from any page"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all four UI restructuring requirements in the browser.

Purpose: Frontend-only phase with no automated test framework — browser verification is the only way to confirm the UI works correctly.
Output: Verified UI, ready for phase completion
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-ui-restructuring/14-CONTEXT.md
@.planning/phases/14-ui-restructuring/14-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all UIRS requirements in browser</name>
  <files>frontend/src/pages/Openings.tsx, frontend/src/pages/Import.tsx, frontend/src/App.tsx</files>
  <action>
Present the verification checklist to the user. Start the dev server if not running: `cd frontend && npm run dev`

Guide the user through verifying each requirement:

**UIRS-01: Openings sub-tabs**
1. Navigate to `/openings` — should redirect to `/openings/explorer`
2. Verify three tabs visible: "Move Explorer", "Games", "Statistics"
3. Click each tab — URL changes to `/openings/explorer`, `/openings/games`, `/openings/statistics`
4. Verify Move Explorer shows next moves table (play a move first)
5. Verify Games tab shows game cards with W/D/L bar above
6. Verify Statistics tab shows WDL bar chart and Win Rate Over Time chart (requires bookmarks)
7. Verify sidebar has board, played-as toggle, piece filter, bookmark button visible (NOT inside a collapsible)

**UIRS-02: Filter persistence**
1. On Move Explorer tab, change Played As to Black
2. Switch to Games tab — Played As should still be Black
3. Switch to Statistics tab — Played As should still be Black
4. Play a move on the board, switch tabs — board position preserved
5. Open More Filters, change a time control, switch tabs — filter preserved

**UIRS-03: Import page**
1. Navigate to `/import`
2. Verify page shows "Import Games" heading
3. Verify platform rows (chess.com, lichess) with username display and Sync buttons
4. Verify "Delete All Games" button exists with working confirmation dialog
5. Start an import — verify inline progress appears on the page
6. Navigate away from `/import` — verify toast still fires when import completes

**UIRS-04: Navigation**
1. Verify nav shows exactly: Import | Openings | Rating | Global Stats
2. Click each nav item — correct page loads
3. While on `/openings/games`, verify Openings nav item is highlighted (active)
4. Navigate to `/` — should redirect to `/openings`
5. Try an invalid URL like `/foo` — should redirect to `/openings`
6. Use browser back/forward between tabs — should work correctly
  </action>
  <verify>User confirms all checklist items pass</verify>
  <done>All four UIRS requirements verified and approved by user in browser</done>
  <read_first>
    - .planning/phases/14-ui-restructuring/14-UI-SPEC.md
    - .planning/phases/14-ui-restructuring/14-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - User confirms all four UIRS requirements pass
    - No broken routes or blank pages
    - Filter state persists across all tab switches
    - Import progress works globally (toast fires from any page)
  </acceptance_criteria>
  <resume-signal>Type "approved" to complete the phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All verification is manual browser testing as described in the checkpoint task.
</verification>

<success_criteria>
User approves all four UIRS requirements after browser verification.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-restructuring/14-03-SUMMARY.md`
</output>
