---
phase: 139-tactic-mode-overlay-phase-135-subsume
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
autonomous: true
requirements: [TACTIC-03]
must_haves:
  truths:
    - "The FlawCard Explore button navigates to /analysis?game_id=…&flaw_ply=…&orientation=… instead of opening the modal (D-01)"
    - "The LibraryGameCard Explore button navigates to /analysis with the parked flaw's game_id/ply/orientation (D-01)"
    - "A new 'Analyze position' button on the scrubbed game ply navigates to /analysis?fen=<reconstructed ply FEN> (free-play, D-02) on desktop and mobile"
    - "No FlawCard/LibraryGameCard code references TacticLineExplorer or location.state after the repoint"
  artifacts:
    - path: "frontend/src/components/library/FlawCard.tsx"
      provides: "Explore button repointed to navigate('/analysis?...'); TacticLineExplorer usage + exploreOpen state removed"
    - path: "frontend/src/components/results/LibraryGameCard.tsx"
      provides: "Explore button repointed; new D-02 'Analyze position' (?fen=) button (desktop + mobile); TacticLineExplorer usage removed"
  key_links:
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "react-router-dom useNavigate"
      via: "navigate('/analysis?game_id=...&flaw_ply=...&orientation=...')"
      pattern: "navigate\\('/analysis"
    - from: "frontend/src/components/results/LibraryGameCard.tsx"
      to: "react-router-dom useNavigate"
      via: "Explore → /analysis tactic params; Analyze position → /analysis?fen=encoded ply FEN"
      pattern: "navigate\\('/analysis"
---

<objective>
Repoint the tactic "Explore" entry points from the standalone modal to the `/analysis` route, and
add the D-02 cheap `?fen=` "Analyze position" entry on the scrubbed game ply (TACTIC-03 entry-point
clause; ROUTE-02 game-review clause via D-02). Plain URL params only — NO `location.state`, NO rail
(D-01); return-to-game is browser Back (D-04).

Purpose: make the route the single tactic surface so Plan 03 can delete the modal cleanly.
Output: repointed Explore buttons in `FlawCard` + `LibraryGameCard` and a new "Analyze position"
button (desktop + mobile) in `LibraryGameCard`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint FlawCard Explore to /analysis (drop modal)</name>
  <files>frontend/src/components/library/FlawCard.tsx</files>
  <read_first>
    - frontend/src/components/library/FlawCard.tsx (exploreOpen state ~142, Explore button ~247-262, the `<TacticLineExplorer>` usage ~579-586, the import ~35)
    - .planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md (D-01 URL contract: game_id/flaw_ply/orientation, no location.state)
    - CLAUDE.md Frontend section (data-testid, brand-outline secondary, aria-label, mobile+desktop parity)
  </read_first>
  <action>
    In `FlawCard.tsx` add `useNavigate` from `react-router-dom`. Change the Explore button's
    `onClick` (currently `setExploreOpen(true)`) to
    `navigate('/analysis?game_id=' + flaw.game_id + '&flaw_ply=' + flaw.ply + '&orientation=' + ori)`
    where `ori` = `'missed'` when `flaw.missed_tactic_motif != null` else `'allowed'` (use
    URLSearchParams or a template — keep the param names exactly `game_id`, `flaw_ply`,
    `orientation`). Keep the existing `data-testid="flaw-btn-explore"`, `variant="brand-outline"`,
    aria-label, and the `isTagged` render guard. Remove the `exploreOpen` state, the
    `<TacticLineExplorer ... />` block, and the `import { TacticLineExplorer }` line. Leave the
    "Game" button and the View-game modal (`useLibraryGame`) intact. Do NOT use `location.state`
    (D-01).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "navigate('/analysis?game_id\|navigate(\`/analysis?game_id\|/analysis?game_id=" frontend/src/components/library/FlawCard.tsx` is ≥ 1
    - `grep -c "TacticLineExplorer\|exploreOpen" frontend/src/components/library/FlawCard.tsx` returns 0
    - `grep -c "location.state" frontend/src/components/library/FlawCard.tsx` returns 0
    - `grep -c 'data-testid="flaw-btn-explore"' frontend/src/components/library/FlawCard.tsx` returns 1
    - `cd frontend && npx tsc -b --noEmit` exits 0; `npm run lint` clean
  </acceptance_criteria>
  <done>FlawCard's Explore button navigates to /analysis tactic mode; no modal/exploreOpen/location.state remain.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint LibraryGameCard Explore + add D-02 Analyze position button</name>
  <files>frontend/src/components/results/LibraryGameCard.tsx</files>
  <read_first>
    - frontend/src/components/results/LibraryGameCard.tsx (exploreOpen state ~222, per-ply FEN reconstruction buildPerPly ~87 + boardFen/hoverEntry ~553-556, renderDesktopExploreButton ~898, mobile Explore ~994-1023, the `<TacticLineExplorer>` usage ~1126-1133, import ~35)
    - .planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md (D-01 Explore URL contract; D-02 cheap ?fen= entry: reuse the existing per-ply reconstructed FEN, free-play, no tactic chrome)
    - CLAUDE.md Frontend section (data-testid on interactive elements, brand-outline secondary, aria-label, text-sm floor, mobile+desktop parity)
  </read_first>
  <action>
    In `LibraryGameCard.tsx` add `useNavigate`. (1) Repoint Explore: change the desktop and mobile
    Explore `onClick` from `setExploreOpen(true)` to
    `navigate('/analysis?game_id=' + game.game_id + '&flaw_ply=' + hoverPly + '&orientation=' + ori)`
    where `ori` derives from the parked flaw marker at `hoverPly` (the `flawByPly`/marker map
    already available) — `'missed'` when a missed tactic motif is present else `'allowed'`, default
    `'missed'`. Keep `data-testid="game-card-btn-explore"`, the `isTaggedFlaw` enabled/disabled +
    tooltip behavior, and the brand-outline variant. Remove the `exploreOpen` state, the
    `<TacticLineExplorer>` block (~1126-1133), and the `import { TacticLineExplorer }`. (2) Add the
    D-02 "Analyze position" button: a `variant="brand-outline"` secondary button with
    `data-testid="game-card-btn-analyze-position"`, aria-label "Analyze this position", placed by
    the eval-chart/ply-nav on BOTH the desktop body (near `renderDesktopExploreButton`) and the
    mobile body (near the mobile Explore block) per CLAUDE.md parity. Its `onClick` navigates to
    `'/analysis?fen=' + encodeURIComponent(boardFen)` where `boardFen` is the already-reconstructed
    scrubbed-ply FEN (`hoverEntry?.fen ?? game.result_fen`). Enabled only when `boardFen` is
    non-null. This is free-play: no game_id/flaw_ply/orientation, no tactic chrome (reuses the
    existing v1 `fen` reader). Do NOT use `location.state`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/analysis?game_id=" frontend/src/components/results/LibraryGameCard.tsx` is ≥ 1
    - `grep -c "/analysis?fen=\|encodeURIComponent" frontend/src/components/results/LibraryGameCard.tsx` is ≥ 1
    - `grep -c 'data-testid="game-card-btn-analyze-position"' frontend/src/components/results/LibraryGameCard.tsx` is ≥ 2 (desktop + mobile per parity)
    - `grep -c "TacticLineExplorer\|exploreOpen" frontend/src/components/results/LibraryGameCard.tsx` returns 0
    - `grep -c "location.state" frontend/src/components/results/LibraryGameCard.tsx` returns 0
    - `cd frontend && npx tsc -b --noEmit` exits 0; `npm run lint` clean
  </acceptance_criteria>
  <done>LibraryGameCard's Explore navigates to /analysis tactic mode; a new desktop+mobile "Analyze position" button deep-links to /analysis?fen=<scrubbed ply FEN>; no modal/location.state remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client FEN/ply → /analysis URL | Reconstructed FEN + game/ply values are placed into a navigable URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-04 | Tampering | ?fen= value | mitigate | `encodeURIComponent(boardFen)` on write; the /analysis reader already FEN-guards via chess.js (T-138-01) and degrades malformed FEN to start |
| T-139-05 | Information Disclosure | game_id in URL | accept | game_id is the user's own library game; the tactic-lines/analysis routes are auth-gated (ProtectedLayout); no new exposure vs the prior modal |
</threat_model>

<verification>
- `cd frontend && npx tsc -b --noEmit` exits 0
- `cd frontend && npm run lint` clean
- `cd frontend && npm test -- --run FlawCard LibraryGameCard` passes (update any test that asserted the old modal-open behavior)
- Do NOT run `npm run knip` in this plan — TacticLineExplorer.tsx is now unused but not yet deleted (knip is gated in Plan 03)
</verification>

<success_criteria>
Both Explore buttons navigate to `/analysis` tactic mode via plain URL params; the new "Analyze
position" button (desktop + mobile) deep-links to free-play `/analysis?fen=…`; no entry point
references the modal or `location.state`.
</success_criteria>

<output>
Create `.planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-02-SUMMARY.md` when done.
</output>
