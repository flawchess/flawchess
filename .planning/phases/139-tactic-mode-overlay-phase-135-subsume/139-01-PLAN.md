---
phase: 139-tactic-mode-overlay-phase-135-subsume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/analysis/TacticModeOverlay.tsx
  - frontend/src/pages/Analysis.tsx
  - frontend/src/hooks/useAnalysisBoard.ts
  - frontend/src/hooks/__tests__/useAnalysisBoard.test.ts
  - frontend/src/pages/__tests__/Analysis.tactic.test.tsx
autonomous: true
requirements: [TACTIC-01, TACTIC-02]
must_haves:
  truths:
    - "Opening /analysis?game_id=X&flaw_ply=Y seeds the board at the flaw decision position with the stored PV as the mainline (D-5)"
    - "The motif chip(s), missed/allowed toggle, depth-to-punchline counter, and white-POV eval badge render in tactic mode"
    - "Depth-0 tactics highlight the flaw move with no empty-PV crash (Behavior A)"
    - "Allowed orientation anchors the PV one ply later via tacticDepth.ts (Behavior B)"
    - "Move numbers in the tactic move list use the real game ply, not restart at 1 (Behavior C)"
    - "Changing the opened flaw or orientation re-seeds the board and resets depth/orientation (Behavior D, reinterpreted per D-01)"
    - "On the stored line the board shows stored-PV arrows by default; the arrow-source toggle switches the board arrows to live engine; EvalBar and EngineLines stay live throughout (D-03)"
    - "Deviating from the stored line drops to live engine arrows only and hides/disables the arrow-source toggle (D-03)"
  artifacts:
    - path: "frontend/src/components/analysis/TacticModeOverlay.tsx"
      provides: "Tactic chrome (motif chips/toggle, eval badge, depth counter, arrow-source toggle, decorated move list) + ported buildRootArrows/buildPvArrow"
      exports: ["TacticModeOverlay", "buildRootArrows", "buildPvArrow"]
    - path: "frontend/src/pages/Analysis.tsx"
      provides: "Tactic-mode wiring: param read, useTacticLines, loadMainLine seeding, orientation/arrow-source state, board arrow selection, overlay render"
    - path: "frontend/src/pages/__tests__/Analysis.tactic.test.tsx"
      provides: "The 4 Phase 135 regression behaviors verified against the wired tactic mode"
  key_links:
    - from: "frontend/src/pages/Analysis.tsx"
      to: "frontend/src/hooks/useLibrary.ts"
      via: "useTacticLines(gameId, flawPly, isTacticMode) for the opened flaw"
      pattern: "useTacticLines\\("
    - from: "frontend/src/pages/Analysis.tsx"
      to: "frontend/src/hooks/useAnalysisBoard.ts"
      via: "loadMainLine(activeMoves, position_fen) seeds the stored PV; board reset to decision position"
      pattern: "loadMainLine\\("
    - from: "frontend/src/components/analysis/TacticModeOverlay.tsx"
      to: "frontend/src/lib/tacticDepth.ts"
      via: "toDisplayDepthForOrientation for depth labels (reused, not recomputed)"
      pattern: "toDisplayDepthForOrientation"
---

<objective>
Bring tactic mode on the `/analysis` board to Phase 135 `TacticLineExplorer` parity (TACTIC-01,
TACTIC-02). Build `TacticModeOverlay.tsx` (the conditional tactic chrome) and wire `Analysis.tsx`
to enter tactic mode when `game_id` + `flaw_ply` are present: seed the stored PV via
`loadMainLine` (D-5), drive board arrows off `isOnMainLine`, expose the missed/allowed toggle and
the D-03 board-arrow-source toggle, and verify the 4 Phase 135 regression behaviors before any
deletion (Pitfall 9).

Purpose: replace the standalone modal with an in-page overlay on the shared analysis board while
preserving every Phase 135 behavior (D-01 reinterprets the "rail" item as a re-seed-on-change
check; NO cross-flaw rail, NO `location.state`, plain URL params only).
Output: `TacticModeOverlay.tsx`, tactic-mode wiring in `Analysis.tsx`, a minimal `goToRoot`
addition to `useAnalysisBoard` (if needed to land at the decision position), and the regression
test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TacticModeOverlay.tsx — port chrome + arrow helpers</name>
  <files>frontend/src/components/analysis/TacticModeOverlay.tsx</files>
  <read_first>
    - frontend/src/components/library/TacticLineExplorer.tsx (source of the port: buildRootArrows ~109, buildPvArrow ~151, isBlackToMove ~96, the headerRow/motif-chip toggle ~421, the eval badge ~401, the decorated HorizontalMoveList mapping ~509, resolveVisibleTactic usage ~238)
    - frontend/src/lib/tacticDepth.ts (toDisplayDepthForOrientation, ALLOWED_DECISION_DEPTH_OFFSET, DEPTH_DISPLAY_OFFSET — reuse, do not recompute)
    - frontend/src/lib/tacticComparisonMeta.ts (resolveVisibleTactic, VisibleTactic)
    - frontend/src/types/library.ts (TacticLinesResponse field shape, lines ~377-402)
    - frontend/src/components/board/HorizontalMoveList.tsx (HorizontalMoveItem, props: items/onMoveClick/heightClass/testId)
    - frontend/src/lib/moveNumberLabel.ts (moveLabel) and frontend/src/lib/formatFlawEval.ts (formatFlawEvalPart, mateAtPly)
    - frontend/src/lib/theme.ts (BEST_MOVE_ARROW, PAYOFF_MOVE_ARROW, TAC_MISSED/TAC_ALLOWED + *_LABEL tokens)
    - CLAUDE.md Frontend section (data-testid on every interactive element, aria-label on icon-only buttons, text-sm floor, theme constants in theme.ts, brand-outline for secondary, no text-xs except hover tooltips)
  </read_first>
  <action>
    Create `frontend/src/components/analysis/TacticModeOverlay.tsx`. Port `buildRootArrows`,
    `buildPvArrow`, and `isBlackToMove` from `TacticLineExplorer.tsx` UNCHANGED (same signatures,
    same theme tokens, same `BoardArrow[]` return) and EXPORT `buildRootArrows` + `buildPvArrow`
    (Analysis.tsx consumes them for the shared ChessBoard). Render the tactic chrome as a
    side-panel/stacked block (Claude's discretion on desktop-panel vs mobile-stacked composition):
    the motif chip row that doubles as the missed/allowed switch (reuse `TacticMotifChip` with
    `testId="tactic-toggle-missed"` / `testId="tactic-toggle-allowed"` and `selected` on the active
    orientation; single non-interactive chip when only one line exists), the white-POV eval badge
    (`data-testid="tactic-eval"`, `formatFlawEvalPart` + `mateAtPly`, never flipped to the mover —
    same anchor logic as TacticLineExplorer lines ~386-399), a depth-to-punchline counter, and the
    D-03 board-arrow-source segmented toggle ("Stored PV" / "Engine") with
    `data-testid="tactic-arrow-source-stored"` and `data-testid="tactic-arrow-source-engine"`
    (default Stored PV; the parent hides/disables it off-line). Render the decorated
    `HorizontalMoveList` (`testId="tactic-san-ladder"`, per-move `tactic-san-move-{flaw_ply+i}`,
    `numberLabel={moveLabel(flaw_ply, i)}` for real-game-ply numbering = Behavior C, colored
    depth-0 punchline, dimmed payoff / allowed flaw-lead-in, severity glyph on the allowed
    lead-in). Use `resolveVisibleTactic` (with the live `useFlawFilterStore` filter) to gate which
    orientation/depth labels show, exactly as the modal did. Depth labels MUST come from
    `toDisplayDepthForOrientation` (do not recompute the +1 allowed offset). Props: the
    `TacticLinesResponse` data, `resolvedOrientation`, `currentPly` (index in mainLine, 0 = decision
    position), `displayDepth`, `isPayoff`, `arrowSource`, `showArrowSourceToggle`, and callbacks
    `onOrientationChange(next)`, `onArrowSourceChange(next)`, `onMoveClick(ply)`. NO board, NO
    BoardControls (those live in Analysis.tsx). NO `location.state`, NO next/prev rail (D-01). All
    interactive elements get `data-testid`; container `data-testid="tactic-mode-overlay"`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function buildRootArrows\|export function buildPvArrow' frontend/src/components/analysis/TacticModeOverlay.tsx` returns 2
    - `grep -c 'data-testid="tactic-mode-overlay"' frontend/src/components/analysis/TacticModeOverlay.tsx` returns 1
    - `grep -c 'toDisplayDepthForOrientation\|resolveVisibleTactic\|moveLabel' frontend/src/components/analysis/TacticModeOverlay.tsx` is ≥ 3
    - `grep -c 'tactic-arrow-source-stored\|tactic-arrow-source-engine' frontend/src/components/analysis/TacticModeOverlay.tsx` returns 2
    - `grep -c "location.state\|useLocation" frontend/src/components/analysis/TacticModeOverlay.tsx` returns 0
    - `cd frontend && npx tsc -b --noEmit` exits 0; `npm run lint` reports no errors for the new file
  </acceptance_criteria>
  <done>TacticModeOverlay renders the motif/toggle, eval badge, depth counter, arrow-source toggle, and real-game-ply move list; exports the two arrow builders; reuses tacticDepth math; type-checks and lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Analysis.tsx tactic mode (seed, re-seed, arrows, overlay)</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/hooks/useAnalysisBoard.ts, frontend/src/hooks/__tests__/useAnalysisBoard.test.ts</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (current free-play shell — param read at ~57-72, board/engine wiring, side panel)
    - frontend/src/hooks/useAnalysisBoard.ts (loadMainLine ~219, isOnMainLine ~251, mainLine, currentNodeId, goToNode, rootFen — the Phase 137 contract)
    - frontend/src/hooks/useLibrary.ts (useTacticLines(gameId, ply, enabled) ~228 — query key ['tactic-lines', gameId, ply])
    - frontend/src/components/library/TacticLineExplorer.tsx (the seeding/orientation-resolution/eval-anchor/flip-default logic to mirror: resolvedOrientation ~264, activeMoves ~269, the positionFen flip useEffect ~258)
    - frontend/src/lib/tacticDepth.ts (TacticDepthOrientation, toDisplayDepthForOrientation)
    - .planning/research/ARCHITECTURE.md (Pattern 4 seeding steps + Pattern 5 param read)
    - frontend/src/App.tsx (AnalysisRoute keys AnalysisPage by `fen` only ~546 — tactic entries share key 'start', so the page does NOT remount between tactics; re-seed MUST be a useEffect, not a remount)
  </read_first>
  <action>
    In `Analysis.tsx` read `game_id`, `flaw_ply`, `orientation` from `useSearchParams` alongside the
    existing `fen` reader (Pattern 5): `gameId = Number(...)` with a NaN guard → null, same for
    `flawPly`; `orientation` cast to `TacticDepthOrientation` defaulting `'missed'`. Set
    `isTacticMode = gameId != null && flawPly != null`. Call
    `useTacticLines(gameId, flawPly, isTacticMode)`. Hold `orientation` in component state seeded
    from the URL param, plus `arrowSource` state ('stored' | 'engine', default 'stored'). Resolve
    `hasMissed`/`hasAllowed` and `resolvedOrientation` exactly as TacticLineExplorer does (via
    `resolveVisibleTactic` + `useFlawFilterStore`), pick `activeMoves`/`activeDepthRaw`/
    `activePlyIndex` per orientation. Add a `useEffect` keyed on `[data, resolvedOrientation,
    data?.position_fen]` that calls `loadMainLine(activeMoves ?? [], data.position_fen)` and then
    positions the board at the DECISION position (root) so the user steps forward toward the
    punchline (Behavior D re-seed per D-01; the depth countdown + buildRootArrows require starting
    at ply 0). If the existing `useAnalysisBoard` navigation contract cannot express "jump to root"
    (currentNodeId === null), add a minimal `goToRoot()` to `useAnalysisBoard.ts` that sets
    `currentNodeId` to null WITHOUT altering any existing behavior, expose it in
    `AnalysisBoardReturn`, and cover it with a unit test in `useAnalysisBoard.test.ts`. Compute
    `currentPly = currentNodeId === null ? 0 : mainLine.indexOf(currentNodeId) + 1`, derive
    `displayDepth`/`isPayoff` from `currentPly` + `activeDepthRaw` using the SAME math
    TacticLineExplorer/useTacticLine used (do not invent new offsets). Board flip default: a
    `useEffect` on `data?.position_fen` setting `boardFlipped = isBlackToMove(position_fen)`,
    preserving manual flips per opened flaw. Board arrows (D-03): when `isTacticMode` AND the
    current node `isOnMainLine(currentNodeId)` (or at root) AND `arrowSource === 'stored'`, build
    arrows via the imported `buildRootArrows` (currentPly 0) / `buildPvArrow` (currentPly ≥ 1);
    otherwise (off-line OR `arrowSource === 'engine'`) draw the live engine best-move arrow only —
    NEVER both simultaneously. Set `showArrowSourceToggle = isOnMainLine(currentNodeId) || currentNodeId === null` (hidden/disabled off-line). EvalBar + EngineLines stay LIVE unconditionally
    (already are — do not gate them on the toggle). Render `<TacticModeOverlay>` in the side panel
    when `isTacticMode`, wiring `onOrientationChange` (setOrientation; the re-seed effect handles
    the board), `onArrowSourceChange`, and `onMoveClick` (navigate within the mainline via
    `goToNode`). Free-play mode (no tactic params) is unchanged. NO URL write-back, NO
    `location.state` (D-01, D-04).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTacticLines(" frontend/src/pages/Analysis.tsx` returns 1
    - `grep -c "loadMainLine(" frontend/src/pages/Analysis.tsx` is ≥ 1
    - `grep -c "isOnMainLine\|arrowSource\|buildRootArrows\|buildPvArrow" frontend/src/pages/Analysis.tsx` is ≥ 3
    - `grep -c "game_id\|flaw_ply\|orientation" frontend/src/pages/Analysis.tsx` is ≥ 3
    - `grep -c "location.state\|useLocation" frontend/src/pages/Analysis.tsx` returns 0
    - If goToRoot was added: `grep -c "goToRoot" frontend/src/hooks/useAnalysisBoard.ts` is ≥ 1 and a passing test references it
    - `cd frontend && npx tsc -b --noEmit` exits 0; `npm run lint` clean; `npm test -- --run useAnalysisBoard` passes
  </acceptance_criteria>
  <done>Opening /analysis?game_id=X&flaw_ply=Y seeds the stored PV at the decision position, the overlay renders, the missed/allowed toggle and arrow-source toggle work, arrows switch stored↔engine by line/source while EvalBar+EngineLines stay live, and free-play mode is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression tests for the 4 Phase 135 behaviors</name>
  <files>frontend/src/pages/__tests__/Analysis.tactic.test.tsx</files>
  <read_first>
    - frontend/src/pages/__tests__/Analysis.test.tsx (the existing engine-mock + MemoryRouter + jsdom-shim scaffold to mirror; useStockfishEngine is mocked, useAnalysisBoard runs for real)
    - frontend/src/types/library.ts (TacticLinesResponse — shape the mocked useTacticLines must return)
    - frontend/src/components/library/__tests__/TacticLineExplorer.test.tsx (the existing pinned behaviors to carry over as the acceptance bar)
    - .planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-CONTEXT.md (the 4 behaviors, D-01 reinterpretation of Behavior D)
  </read_first>
  <behavior>
    - Behavior A (depth-0 highlight): a tactic with `missed_depth === 0` renders the flaw/best-move root arrows and the overlay with no crash (no empty-PV step).
    - Behavior B (allowed +1 offset): for an allowed-orientation tactic the first stepped PV move shown is the user's response to the opponent's flaw move (PV anchored one ply later), and the displayed depth uses `toDisplayDepthForOrientation('allowed', ...)`.
    - Behavior C (real-game-ply numbering): a tactic opened at `flaw_ply` (e.g. 42) shows move numbers anchored to the game ply (move 22), not "1.".
    - Behavior D (re-seed on change): changing the `orientation` toggle (and the `flaw_ply`/`game_id` params) re-seeds the board to the new decision position and resets depth/orientation — no stale mainline persists. NO multi-flaw rail asserted.
  </behavior>
  <action>
    Create `frontend/src/pages/__tests__/Analysis.tactic.test.tsx` mirroring the Phase 138
    `Analysis.test.tsx` scaffold: mock `@/hooks/useStockfishEngine` with the mutable engineState,
    mock `@/hooks/useLibrary`'s `useTacticLines` to return a controlled `TacticLinesResponse`
    fixture, keep `useAnalysisBoard` real, render `<AnalysisPage />` inside `<MemoryRouter
    initialEntries={['/analysis?game_id=1&flaw_ply=42&orientation=missed']}>` + `TooltipProvider`,
    and reuse the matchMedia/ResizeObserver/scrollTo jsdom shims. Write one test per behavior A–D
    above, asserting against the overlay testids (`tactic-mode-overlay`, `tactic-eval`,
    `tactic-san-move-42`, `tactic-toggle-missed`/`tactic-toggle-allowed`) and the move-number text.
    For Behavior D, fire the orientation toggle (and/or re-render with a different `flaw_ply`) and
    assert the move list/board re-seeds. These tests are the deletion gate — they MUST pass before
    Plan 03 removes the modal.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run Analysis.tactic 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/__tests__/Analysis.tactic.test.tsx` exists and contains ≥ 4 `it(`/`test(` blocks
    - `grep -c "depth-0\|empty\|Behavior A\|Behavior B\|Behavior C\|Behavior D\|orientation\|move number" frontend/src/pages/__tests__/Analysis.tactic.test.tsx` is ≥ 4
    - `cd frontend && npm test -- --run Analysis.tactic` reports all tests passing (4+ passed, 0 failed)
  </acceptance_criteria>
  <done>All four regression behaviors pass against the wired tactic mode; the file is the gate for Plan 03 deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL search params → Analysis render | `game_id`/`flaw_ply`/`orientation` are attacker-controllable query strings parsed at render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01 | Tampering | game_id/flaw_ply params | mitigate | `Number(...)` parse with `Number.isNaN` guard → null (no tactic mode) on malformed input; useTacticLines disabled when null |
| T-139-02 | Tampering | orientation param | mitigate | cast then resolve against `hasMissed`/`hasAllowed` via resolveVisibleTactic; unknown value falls back to a visible orientation |
| T-139-03 | Denial of Service | TacticLinesResponse.position_fen | accept | server-sourced from the existing tactic-lines endpoint (D-4, trusted); chess.js parse already degrades gracefully in loadMainLine (illegal SAN stops the replay) |
</threat_model>

<verification>
- `cd frontend && npx tsc -b --noEmit` exits 0 (shared-type/prop changes type-check — npm lint+test do NOT type-check)
- `cd frontend && npm run lint` clean
- `cd frontend && npm test -- --run Analysis.tactic useAnalysisBoard` passes
</verification>

<success_criteria>
Tactic mode reaches Phase 135 parity on /analysis: seeded stored PV at the decision position, motif
chip(s) + missed/allowed toggle + depth counter + eval badge, D-03 arrow-source toggle (stored
default on-line, engine off-line, EvalBar/EngineLines always live), and all 4 regression behaviors
pass. No deletion happens in this plan.
</success_criteria>

<output>
Create `.planning/phases/139-tactic-mode-overlay-phase-135-subsume/139-01-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts this phase produces (whole-phase, all plans)

New symbols/files this phase CREATES (exclude from source-drift verification — they are new, not pre-existing):

**New components / files (Plan 01):**
- `frontend/src/components/analysis/TacticModeOverlay.tsx` — new component `TacticModeOverlay`
- `frontend/src/pages/__tests__/Analysis.tactic.test.tsx` — new regression test file

**New exported functions (Plan 01, ported into the new overlay file):**
- `buildRootArrows` (exported from TacticModeOverlay.tsx)
- `buildPvArrow` (exported from TacticModeOverlay.tsx)
- `goToRoot` on `useAnalysisBoard` / `AnalysisBoardReturn` — only if the executor needs it to land the board at the decision position (minimal addition; conditional)

**New URL-param handling (Plan 01, in Analysis.tsx):**
- tactic-mode params read: `game_id`, `flaw_ply`, `orientation` (joining the existing `fen` reader)
- `isTacticMode` flag; `orientation` + `arrowSource` component state

**New data-testids:**
- `tactic-mode-overlay` (overlay container)
- `tactic-toggle-missed`, `tactic-toggle-allowed` (missed/allowed switch)
- `tactic-eval` (white-POV eval badge)
- `tactic-arrow-source-stored`, `tactic-arrow-source-engine` (D-03 board-arrow source toggle)
- `tactic-san-ladder`, `tactic-san-move-{flaw_ply+i}` (decorated move list)
- `game-card-btn-analyze-position` (Plan 02 — new D-02 "Analyze position" button, desktop + mobile)

**New board-arrow toggle control:** the "Stored PV" ⇄ "Engine" segmented control (D-03), default Stored PV on-line, hidden/disabled off-line.

**Repointed (not new) entry points (Plan 02):** `flaw-btn-explore`, `game-card-btn-explore` — existing testids, onClick changed to `navigate('/analysis?...')`.

**Deleted (Plan 03):** `frontend/src/components/library/TacticLineExplorer.tsx`, `frontend/src/hooks/useTacticLine.ts`, and their `__tests__`.
</artifacts_this_phase_produces>
