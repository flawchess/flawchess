---
phase: 97-endgame-metrics-by-time-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/endgame_repository.py
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/services/test_endgame_service.py
autonomous: true
requirements: [standalone]
must_haves:
  truths:
    - "The endgame overview response exposes per-TC conversion win%, parity score%, and recovery save% rate values"
    - "Each per-TC card carries per-bucket WDL counts, ΔES score-gap stats, and the per-TC ΔES-gap percentile read directly from user_benchmark_percentiles"
    - "Cards self-suppress below MIN_GAMES_PER_TC_CARD and emit in fixed bullet→blitz→rapid→classical order (D-12: floor kept at the existing constant, card-level suppression; validated adequate against dev-DB conv/recov denominators in RESEARCH §D-12 — not raised)"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "PerTcBucketStats, EndgameMetricsTcCard, EndgameMetricsCardsResponse + endgame_metrics_cards field on EndgameOverviewResponse"
      contains: "EndgameMetricsTcCard"
    - path: "app/services/endgame_service.py"
      provides: "_compute_per_tc_metric_cards + threading into get_endgame_overview"
      contains: "_compute_per_tc_metric_cards"
    - path: "app/repositories/endgame_repository.py"
      provides: "query_endgame_bucket_rows extended with time_control_bucket + LEAD next-eval columns"
      contains: "time_control_bucket"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/repositories/endgame_repository.py"
      via: "consumes extended bucket_rows (TC + span-gap columns)"
      pattern: "_compute_per_tc_metric_cards"
    - from: "app/services/endgame_service.py"
      to: "app/repositories/user_benchmark_percentiles_repository.py"
      via: "per-TC percentile lookup (no blended aggregation)"
      pattern: "score_gap_conv|recovery_score_gap"
---

<objective>
Add the backend per-TC rate aggregation path (D-15): compute per-TC conversion win%, parity score%, recovery save% rate values plus per-bucket WDL, ΔES score-gap stats, and the per-TC ΔES-gap percentile, and expose them as a new `endgame_metrics_cards` field on `EndgameOverviewResponse`. Mirrors the existing `_compute_time_pressure_cards` per-TC card pattern exactly.

Purpose: Per-TC rate values do not exist today — only per-TC ΔES-gap percentiles do. The Wave 2 frontend cards need rate + WDL + ΔES + percentile per (TC × bucket) in one response object.
Output: New Pydantic schemas, extended repository query, `_compute_per_tc_metric_cards` service function, and the threaded `endgame_metrics_cards` field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-endgame-metrics-by-time-control/97-CONTEXT.md
@.planning/phases/97-endgame-metrics-by-time-control/97-RESEARCH.md
@.planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend query_endgame_bucket_rows and add per-TC metric schemas</name>
  <files>app/repositories/endgame_repository.py, app/schemas/endgames.py</files>
  <read_first>
    - app/repositories/endgame_repository.py — the file being modified. Read `query_endgame_bucket_rows` (lines 360-371, current 6-column SELECT) and `query_endgame_entry_rows` (lines 144-286, the proven LEAD-window + array_agg span pattern at lines 226-244 and 326-339). The LEAD pattern is the analog for adding span-gap (next-entry-eval) columns.
    - app/schemas/endgames.py — the file being modified. Read `TimePressureTcCard` / `TimePressureCardsResponse` / `EndgameOverviewResponse` (lines 858-957) as the exact schema analog, including the `Literal["bullet","blitz","rapid","classical"]` tc field, `| None = None` defaults (B-2 lock), and the `Field(default_factory=...)` pattern on `rating_anchors`.
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 33-102 (schemas) and 294-356 (repository extension); 97-RESEARCH.md §D-15 (D-15 Sub-option A is the chosen shape — extend the bucket-rows query so span gaps come from one query, no second DB roundtrip).
    - CLAUDE.md — async SQLAlchemy 2.x select() API; eval_cp/eval_mate projected raw (white-perspective, no SQL color flip; service applies the flip); Literal types not bare str.
  </read_first>
  <action>
    In `app/schemas/endgames.py`, add three Pydantic models. `PerTcBucketStats` fields: `games: int`, `win_pct: float`, `draw_pct: float`, `loss_pct: float` (percent 0-100), `rate: float | None`, `score_gap_mean: float | None`, `score_gap_n: int | None`, `score_gap_p_value: float | None`, `score_gap_ci_low: float | None`, `score_gap_ci_high: float | None`, `percentile: float | None`. `EndgameMetricsTcCard` fields: `tc: Literal["bullet","blitz","rapid","classical"]`, `total: int`, `conversion: PerTcBucketStats`, `parity: PerTcBucketStats`, `recovery: PerTcBucketStats`. `EndgameMetricsCardsResponse` field: `cards: list[EndgameMetricsTcCard]`. Add `endgame_metrics_cards: EndgameMetricsCardsResponse = Field(default_factory=lambda: EndgameMetricsCardsResponse(cards=[]))` to `EndgameOverviewResponse`, following the `rating_anchors` default-factory pattern. In `app/repositories/endgame_repository.py`, extend `query_endgame_bucket_rows` to project `Game.time_control_bucket` (new column index 6) and LEAD next-entry-eval columns (`next_entry_eval_cp`, `next_entry_eval_mate`, indices 7-8) using the same LEAD window subquery shape as `query_endgame_entry_rows` (partition_by game_id, order_by span_min_ply asc). Update the docstring row-shape comment. Keep the `apply_game_filters` call unchanged. RESEARCH confirms the only `bucket_rows` consumer (`_aggregate_bucket_counts`) reads indices 0-5 only, so the additive columns are safe — verify with a grep for positional unpacking of bucket rows before committing.
  </action>
  <verify>
    <automated>uv run python -c "from app.schemas.endgames import EndgameMetricsTcCard, PerTcBucketStats, EndgameMetricsCardsResponse, EndgameOverviewResponse; assert 'endgame_metrics_cards' in EndgameOverviewResponse.model_fields; assert set(PerTcBucketStats.model_fields) >= {'rate','percentile','win_pct','score_gap_mean'}; assert set(EndgameMetricsTcCard.model_fields) == {'tc','total','conversion','parity','recovery'}; print('ok')" && uv run ty check app/schemas/endgames.py app/repositories/endgame_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/schemas/endgames.py` defines `PerTcBucketStats`, `EndgameMetricsTcCard`, `EndgameMetricsCardsResponse`; `EndgameMetricsTcCard.tc` is a `Literal["bullet","blitz","rapid","classical"]`.
    - Source: `EndgameOverviewResponse.model_fields` includes `endgame_metrics_cards` with a `default_factory` (constructing the response with no `endgame_metrics_cards` arg yields `cards == []`).
    - Source: `query_endgame_bucket_rows` SELECT projects `time_control_bucket` and the two LEAD next-entry-eval columns; docstring row-shape updated.
    - Gate: `uv run ty check app/schemas/endgames.py app/repositories/endgame_repository.py` zero errors.
    - Regression: `uv run pytest tests/test_endgame_service.py -x` passes (existing bucket-rows consumers unaffected by the additive columns).
  </acceptance_criteria>
  <done>Schemas and extended query exist; ty clean; existing endgame tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _compute_per_tc_metric_cards and thread into get_endgame_overview</name>
  <files>app/services/endgame_service.py, tests/services/test_endgame_service.py</files>
  <read_first>
    - app/services/endgame_service.py — the file being modified. Read `_compute_time_pressure_cards` (lines 2088-2209) as the exact structural analog: single-pass per-TC accumulators, `_TIME_CONTROL_ORDER` (line 1640) fixed-order loop, `MIN_GAMES_PER_TC_CARD` gate, and the per-TC percentile lookup that BYPASSES the blended helper (lines 2167-2178, `_effective_rows.get("score_gap_conv", {}).get(tc_bucket)`). Read `_ClockAggregate` (lines 1643-1679) for the `@dataclass(slots=True)` accumulator pattern, `_compute_per_bucket_score_gap` (lines 1237-1260) for the ΔES `compute_paired_difference_test` reuse, `_aggregate_bucket_counts` (lines 1106-1175) for the WDL conversion>recovery>parity bucket-priority tally, `_classify_endgame_bucket` for the eval→bucket classification (raw white-perspective eval flipped by user_color), and `get_endgame_overview` (return at lines 3227-3236) for where to thread the new field. Note the import blocks (lines 42-70) for `EndgameMetricsCardsResponse`/`EndgameMetricsTcCard`/`PerTcBucketStats`.
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 186-291 (service patterns, accumulator, percentile lookup, threading); 97-RESEARCH.md §D-15 (accumulator shape, span-gap source), Pitfall 1 (span gaps need the LEAD columns added in Task 1), Pitfall 7 (no asyncio.gather — this is pure Python over already-fetched rows).
    - CLAUDE.md — keep functions small/shallow (extract per-TC accumulation into a helper if the function nests past depth 3); never asyncio.gather on the same AsyncSession; explicit return types; Sentry only on non-trivial except (this is pure computation, no except needed).
  </read_first>
  <behavior>
    - Given bucket_rows spanning multiple TCs with conv/parity/recovery classifications, `_compute_per_tc_metric_cards` returns an `EndgameMetricsCardsResponse` with one `EndgameMetricsTcCard` per TC whose total ≥ MIN_GAMES_PER_TC_CARD, in bullet→blitz→rapid→classical order.
    - For a TC card: `conversion.rate` == conv_wins / conv_total (None when conv_total == 0); `parity.rate` == (parity_wins + 0.5*parity_draws) / parity_total; `recovery.rate` == (recov_wins + recov_draws) / recov_total.
    - Per bucket, `win_pct`/`draw_pct`/`loss_pct` are computed over that bucket's games (percent 0-100) for the WDL bar.
    - Per-bucket ΔES (`score_gap_mean`/`_n`/`_p_value`/`_ci_low`/`_ci_high`) is computed per-TC from the span gaps (derived from the LEAD next-eval columns), independently accumulated per TC (NOT pooled).
    - `percentile` on each bucket reads `percentile_rows[metric][tc].percentile` directly (conversion→`score_gap_conv`, parity→`score_gap_parity`, recovery→`recovery_score_gap`), None-safe, with NO blended aggregation.
    - A TC with total < MIN_GAMES_PER_TC_CARD produces no card; empty input yields `cards == []`.
  </behavior>
  <action>
    Add a `@dataclass(slots=True)` `_MetricTcAccumulator` (per-bucket wins/draws/total counters + total + per-bucket span-gap lists), then `_compute_per_tc_metric_cards(bucket_rows, *, percentile_rows=None) -> EndgameMetricsCardsResponse` mirroring `_compute_time_pressure_cards`: single pass grouping accumulators by `time_control_bucket` (column 6), classifying each row's bucket via `_classify_endgame_bucket(eval_cp, eval_mate, user_color)`, tallying WDL by the existing conversion>recovery>parity priority, and appending span gaps (from the LEAD next-eval columns) to the matching bucket's per-TC gap list. After the pass, loop `_TIME_CONTROL_ORDER`, skip TCs below `MIN_GAMES_PER_TC_CARD`, build each `PerTcBucketStats` (rate + WDL pcts + ΔES via `compute_paired_difference_test` + per-TC percentile via the direct `_effective_rows.get(metric, {}).get(tc_bucket)` lookup), and assemble the `EndgameMetricsTcCard`. Add the schema imports. Thread the call into `get_endgame_overview` using the already-fetched extended `bucket_rows` and the existing `percentile_rows` (no new DB query, no asyncio.gather), and pass the result as `endgame_metrics_cards=` to the `EndgameOverviewResponse` constructor. Add `test_compute_per_tc_metric_cards` (rate/percentile/order/suppression assertions) and extend the existing `get_endgame_overview` test to assert `endgame_metrics_cards.cards` is populated — write these tests FIRST (RED) per the behavior block, then implement to GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_endgame_service.py::test_compute_per_tc_metric_cards -x && uv run pytest tests/test_endgame_service.py -x && uv run ty check app/services/endgame_service.py</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/services/test_endgame_service.py::test_compute_per_tc_metric_cards -x` exits 0, asserting per-TC rates, fixed bullet→classical order, MIN_GAMES_PER_TC_CARD suppression, and direct (non-blended) percentile pass-through.
    - Behavior: the extended `get_endgame_overview` test asserts `result.endgame_metrics_cards.cards` is non-empty for a multi-TC fixture and that conversion/parity/recovery rates are within [0, 1] or None.
    - Source: `_compute_per_tc_metric_cards` exists, takes pre-fetched `bucket_rows` (V4 — never a user_id query param), and is called once in `get_endgame_overview` with no additional DB query and no `asyncio.gather`.
    - Source: percentile lookup uses `_effective_rows.get(metric, {}).get(tc_bucket)` directly (grep shows no `_aggregate_per_tc_percentile` call inside the new function).
    - Gate: `uv run ty check app/services/endgame_service.py` zero errors; `uv run ruff check app/` clean.
  </acceptance_criteria>
  <done>_compute_per_tc_metric_cards computes correct per-TC rate/WDL/ΔES/percentile, threaded into the overview response; new + extended tests green; ty/ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/endgames/overview | sidebar TC-filter params cross here, already validated by `apply_game_filters` |
| service → DB | `user_id` sourced from FastAPI-Users dep (V4), never from query params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03 | Elevation of privilege | `_compute_per_tc_metric_cards` data scope | mitigate | Function receives `bucket_rows` already fetched with the authenticated `user_id`; never sources user_id itself (existing V4 control preserved) |
| T-97-04 | Tampering | TC filter param | accept/mitigate | `time_control_bucket` validated as Pydantic Literal on the response; inbound TC filter already validated by `apply_game_filters` (no new vector) |
| T-97-05 | Information disclosure | API response | mitigate | Response returns rate values + percentiles only; no hashes (existing FEN-only convention unaffected); no new PII field |

No new auth/session/crypto surface; no new package install.
</threat_model>

<verification>
- `uv run pytest tests/services/test_endgame_service.py -x && uv run pytest tests/test_endgame_service.py -x` green.
- `uv run ty check app/ tests/` zero errors; `uv run ruff check app/ tests/` clean.
- `endgame_metrics_cards` populated on the overview response for a multi-TC fixture; cards in fixed order with correct suppression.
</verification>

<success_criteria>
SC-5 (per-TC conv/parity/recovery rate values computed by the backend and exposed via the endgame overview response) and SC-4 (cards self-suppress below MIN_GAMES_PER_TC_CARD, floor validated against dev-DB distributions in RESEARCH D-12 — kept at 20) are satisfied by this plan.
</success_criteria>

<output>
Create `.planning/phases/97-endgame-metrics-by-time-control/97-02-SUMMARY.md` when done.
</output>
