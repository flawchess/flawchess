---
phase: 97-endgame-metrics-by-time-control
plan: 03
type: execute
wave: 2
depends_on: ["97-01", "97-02"]
files_modified:
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameMetricsByTcSection.tsx
  - frontend/src/components/charts/EndgameMetricsByTcCard.tsx
  - frontend/src/components/charts/__tests__/EndgameMetricsByTcSection.test.tsx
  - frontend/src/components/charts/__tests__/EndgameMetricsByTcCard.test.tsx
  - frontend/src/pages/Endgames.tsx
autonomous: true
requirements: [standalone]
must_haves:
  truths:
    - "The Endgame Metrics section renders one full-width card per eligible time control in fixed bullet→blitz→rapid→classical order"
    - "Each TC card shows the Conversion/Parity/Recovery gauge trifecta + WDL + ΔES score-gap bullet with a per-TC percentile badge, scoped to that TC"
    - "Conversion/Recovery gauges and ΔES bullets use TC-specific bands from TC_METRIC_BANDS; Parity uses the global band on every card"
    - "Empty state mirrors EndgameTimePressureSection; desktop and mobile both render responsively"
  artifacts:
    - path: "frontend/src/components/charts/EndgameMetricsByTcSection.tsx"
      provides: "Per-TC section orchestrator (full-width vertical stacking, empty state)"
      min_lines: 30
    - path: "frontend/src/components/charts/EndgameMetricsByTcCard.tsx"
      provides: "Per-TC card with Conv/Parity/Recovery metric blocks, TC-specific bands, percentile chips"
      min_lines: 60
    - path: "frontend/src/types/endgames.ts"
      provides: "PerTcBucketStats, EndgameMetricsTcCard, EndgameMetricsCardsResponse + endgame_metrics_cards on EndgameOverviewResponse"
      contains: "EndgameMetricsTcCard"
  key_links:
    - from: "frontend/src/components/charts/EndgameMetricsByTcCard.tsx"
      to: "frontend/src/generated/endgameZones.ts"
      via: "TC_METRIC_BANDS[card.tc] → colorizeGaugeZones + ΔES band edges"
      pattern: "TC_METRIC_BANDS"
    - from: "frontend/src/pages/Endgames.tsx"
      to: "frontend/src/components/charts/EndgameMetricsByTcSection.tsx"
      via: "renders with overview.endgame_metrics_cards"
      pattern: "EndgameMetricsByTcSection"
---

<objective>
Build the per-TC Endgame Metrics section: a new `EndgameMetricsByTcSection` (full-width vertical card stacking, mirroring `EndgameTimePressureSection`'s empty state) and `EndgameMetricsByTcCard` (the Conversion/Parity/Recovery trifecta per TC, each block = gauge + WDL + ΔES bullet + percentile chip), wire it into `Endgames.tsx`, and add the matching TS types. Implements D-01..D-07, D-09, D-11, D-13, D-14. Folds the reviewed todo `2026-05-17-recovery-score-gap-popover-copy.md` (opponent-first recovery popover copy).

Purpose: Replace the aggregated metric view with TC-correct cards so a bullet player's typical 35% recovery reads as "typical" (TC band) not "strong" (pooled band), and the per-TC percentile badge no longer contradicts a global band.
Output: New section + card components, TS types, and the page wiring. The OLD `EndgameMetricsSection` / `EndgameMetricCard` are NOT deleted here — Plan 04 removes them after this section is live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-endgame-metrics-by-time-control/97-CONTEXT.md
@.planning/phases/97-endgame-metrics-by-time-control/97-RESEARCH.md
@.planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-TC metric TS types</name>
  <files>frontend/src/types/endgames.ts</files>
  <read_first>
    - frontend/src/types/endgames.ts — the file being modified. Read `TimePressureTcCard` / `TimePressureCardsResponse` (lines 296-343) as the interface analog and `EndgameOverviewResponse` (lines 426-440) for where to add the new field. Note the `'bullet' | 'blitz' | 'rapid' | 'classical'` union and `?: T | null` optionality conventions.
    - app/schemas/endgames.py (from Plan 02) — the backend `PerTcBucketStats` / `EndgameMetricsTcCard` / `EndgameMetricsCardsResponse` shape these TS interfaces must mirror exactly.
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 649-712 (verbatim TS interfaces).
    - CLAUDE.md — type safety, no `any`, discriminated unions over loose strings.
  </read_first>
  <action>
    Add three interfaces mirroring the Plan 02 backend schemas. `PerTcBucketStats`: `games: number`, `win_pct: number`, `draw_pct: number`, `loss_pct: number`, `rate: number | null`, `score_gap_mean: number | null`, `score_gap_n: number | null`, `score_gap_p_value: number | null`, `score_gap_ci_low: number | null`, `score_gap_ci_high: number | null`, `percentile: number | null`. `EndgameMetricsTcCard`: `tc: 'bullet' | 'blitz' | 'rapid' | 'classical'`, `total: number`, `conversion: PerTcBucketStats`, `parity: PerTcBucketStats`, `recovery: PerTcBucketStats`. `EndgameMetricsCardsResponse`: `cards: EndgameMetricsTcCard[]`. Add `endgame_metrics_cards?: EndgameMetricsCardsResponse` to `EndgameOverviewResponse` (optional for back-compat with older fixtures). Do NOT remove any existing fields here — removals happen in Plan 04.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "EndgameMetricsTcCard" src/types/endgames.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Source: `frontend/src/types/endgames.ts` exports `PerTcBucketStats`, `EndgameMetricsTcCard`, `EndgameMetricsCardsResponse`; `EndgameMetricsTcCard.tc` is the four-member literal union.
    - Source: `EndgameOverviewResponse` has optional `endgame_metrics_cards?: EndgameMetricsCardsResponse`.
    - Gate: `cd frontend && npx tsc --noEmit` compiles with zero errors.
  </acceptance_criteria>
  <done>TS interfaces mirror the backend schemas; tsc clean; no existing fields removed.</done>
</task>

<task type="auto">
  <name>Task 2: Build EndgameMetricsByTcCard (trifecta blocks, TC-specific bands, percentile chips, recovery popover copy)</name>
  <files>frontend/src/components/charts/EndgameMetricsByTcCard.tsx, frontend/src/components/charts/__tests__/EndgameMetricsByTcCard.test.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricCard.tsx (full file, 298 lines) — the metric-block anatomy to replicate per-TC: imports (lines 13-44), rate computation per bucket (lines 109-113), `hasGames` opacity guard (114, 184), `showGapRow = gapN > 0` gate (120), `gapColor` from RAW unshifted values (157-164), PercentileChip flavor dispatch (246-254), card shell + testid (171-172), and `POPOVER_COPY` per bucket.
    - frontend/src/components/charts/EndgameTimePressureCard.tsx lines 41-46 — `TC_LABELS` map and `time-pressure-card-${card.tc}` testid convention; TimeControlIcon usage for the TC header.
    - frontend/src/generated/endgameZones.ts (regenerated by Plan 01) — `TC_METRIC_BANDS` shape and `FIXED_GAUGE_ZONES.parity` (global parity band kept).
    - frontend/src/lib/theme.ts — `colorizeGaugeZones`, `ZONE_DANGER`, `ZONE_SUCCESS`.
    - frontend/src/lib/endgameMetrics.ts — `BUCKET_DISPLAY_LABELS`, `SCORE_GAP_BUCKET_DISPLAY_SHIFT` (note: replaced per-TC here, see Pitfall 3).
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 523-647 (card anatomy, gauge-zone construction, display-shift, neutral-band-per-bucket, testids); 97-RESEARCH.md Pitfall 2 (use TC_METRIC_BANDS[tc].recovRate, never the pooled band), Pitfall 3 (recompute displayShift per-TC = -(lower+upper)/2; parity shift 0), Pitfall 5 (noUncheckedIndexedAccess narrowing on TC_METRIC_BANDS[card.tc]).
    - .claude/skills or the todo file `2026-05-17-recovery-score-gap-popover-copy.md` if present — the opponent-first recovery popover copy reframe to fold in.
    - CLAUDE.md — theme constants in theme.ts (no hard-coded semantic colors); data-testid on every interactive element (kebab-case, component-prefixed); ARIA labels on icon-only buttons; mobile parity; text-sm floor (tooltip popover text-xs exception); no magic numbers.
  </read_first>
  <action>
    Create `EndgameMetricsByTcCard.tsx` rendering one TC card: a header (`TimeControlIcon` + `TC_LABELS[card.tc]` label + total games, `data-testid={`metrics-tc-card-${card.tc}`}`) and a 3-column metric grid `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4` (D-03: side-by-side desktop, stacked mobile) holding Conversion / Parity / Recovery blocks. For each block compute the gauge value (conversion→win%/100, recovery→(win%+draw%)/100, parity→score%) and the TC-specific gauge zones via `const bands = TC_METRIC_BANDS[card.tc]` (narrow per Pitfall 5) → `colorizeGaugeZones([{from:0,to:bands.convRate[0]},{from:bands.convRate[0],to:bands.convRate[1]},{from:bands.convRate[1],to:1.0}])` for conversion, the analogous `bands.recovRate` for recovery, and `colorizeGaugeZones(FIXED_GAUGE_ZONES.parity)` for parity (global). Compute the ΔES bullet neutral-band edges and display-shift per TC: conv = `bands.convScoreGap`, recov = `bands.recovScoreGap`, parity = the global parity ΔES band; `displayShift = -(lower+upper)/2` for conv/recov, `0` for parity. Render each block as gauge + `MiniWDLBar` + `ScoreGapRow` (gated `gapN > 0`) + `PercentileChip` reading `block.percentile` with the existing flavor dispatch (`conversion`/`parity`/`recovery`) and `tc={card.tc}` — no per-TC breakdown list (chip reads the per-TC percentile directly per D-11). Preserve the `hasGames` opacity-50 guard and RAW-value `gapColor` tinting. Carry forward `POPOVER_COPY` per bucket; update the recovery block's popover copy to the opponent-first framing from `2026-05-17-recovery-score-gap-popover-copy.md` (one-string reframe). Use these testids on interactive/sub elements: `metrics-tc-{tc}-{bucket}`, `metrics-tc-{tc}-{bucket}-score-gap-bullet`, `metrics-tc-{tc}-{bucket}-score-gap-value`, `metrics-tc-{tc}-{bucket}-score-gap-info`, `metrics-tc-{tc}-{bucket}-percentile-chip`. Write `EndgameMetricsByTcCard.test.tsx` asserting: conversion gauge zone[1].from equals `TC_METRIC_BANDS.bullet.convRate[0]` for a bullet card; recovery uses TC bands not the global pooled band; percentile chip renders when `percentile != null` and is absent when null; all three block testids present.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameMetricsByTcCard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- --run EndgameMetricsByTcCard` exits 0; test asserts the bullet conversion gauge zone boundary equals `TC_METRIC_BANDS.bullet.convRate[0]` (TC-specific, not global) and recovery uses `bands.recovRate`.
    - Behavior: percentile chip rendered iff `block.percentile != null`; ΔES bullet rendered iff `score_gap_n > 0`.
    - Source: `TC_METRIC_BANDS[card.tc]` access is narrowed (no `@ts-ignore`); colors come from `theme.ts` (`colorizeGaugeZones`/`ZONE_*`), no hard-coded semantic hex.
    - Source: recovery popover copy reflects the opponent-first reframe; conversion/parity blocks carry forward existing copy.
    - Source: every interactive/sub element carries a kebab-case `metrics-tc-*` testid; icon-only TC header icon has an aria-label.
    - Gate: `cd frontend && npx tsc --noEmit` zero errors.
  </acceptance_criteria>
  <done>EndgameMetricsByTcCard renders the trifecta with TC-specific bands + per-TC chips; recovery copy reframed; component test green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Build EndgameMetricsByTcSection and wire into Endgames.tsx</name>
  <files>frontend/src/components/charts/EndgameMetricsByTcSection.tsx, frontend/src/components/charts/__tests__/EndgameMetricsByTcSection.test.tsx, frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameTimePressureSection.tsx (full file, 93 lines) — the exact orchestrator analog: `<section>` with subtitle, empty-state branch, `data.cards.map`, `key={card.tc}`. Note the grid constants (lines 464-467) — these collapse to full-width here per D-02.
    - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx — the test structure analog (renders cards in order, shows empty state when `cards=[]`).
    - frontend/src/pages/Endgames.tsx lines 22, 28, 288, 585, 608 — current `EndgameMetricsSection` import and usage (`<EndgameMetricsSection data={scoreGapData} />` at 585), and the `EndgameTimePressureSection` usage (608) for the wiring pattern.
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 454-521 (section adaptations) and 716-734 (Endgames.tsx swap); 97-RESEARCH.md Open Question 3 (D-02 = full-width vertical stacking, NOT the Time Pressure staircase grid) and §TC filter intersection D-14 (backend already pre-filters cards; no extra frontend filtering).
    - CLAUDE.md — data-testid on layout containers; mobile parity; isError handling in data-loading chains.
  </read_first>
  <action>
    Create `EndgameMetricsByTcSection.tsx` modeled on `EndgameTimePressureSection`: props `{ data: EndgameMetricsCardsResponse }`; `<section data-testid="endgame-metrics-tc-section" aria-label="Endgame metrics by time control">` with subtitle "How do you score from winning, balanced, and losing endgames, by time control?"; empty-state branch when `data.cards.length === 0` with `data-testid="endgame-metrics-tc-section-empty"` and text "No endgame data yet. Import more games to see this section."; otherwise map `data.cards` to `EndgameMetricsByTcCard` keyed by `card.tc`. Use full-width vertical stacking (`w-full mt-2` wrapper, one card per row — D-02), NOT the Time Pressure staircase grid. Do NOT add frontend card filtering by selected TCs — the backend already returns the (selected ∩ eligible) set (D-14). In `Endgames.tsx`, add the import for `EndgameMetricsByTcSection`, replace the `<EndgameMetricsSection data={scoreGapData} />` usage at line ~585 with `<EndgameMetricsByTcSection data={overviewData?.endgame_metrics_cards ?? { cards: [] }} />` (the `?? { cards: [] }` fallback handles older responses), and leave the `EndgameMetricsSection` import in place for now (Plan 04 deletes it) OR remove only the usage if leaving the import would trip lint — if removing the usage orphans the import, remove the import too and note it for Plan 04. Write `EndgameMetricsByTcSection.test.tsx` asserting: renders one card per provided TC in bullet→blitz→rapid→classical order; shows the empty-state testid/text when `cards=[]`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run EndgameMetricsByTcSection && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- --run EndgameMetricsByTcSection` exits 0; asserts fixed-order card rendering and the empty-state branch.
    - Source: section uses full-width stacking (`w-full`), not the 2/3/4-column staircase; `data-testid="endgame-metrics-tc-section"` and `...-empty` present.
    - Behavior: `Endgames.tsx` renders `EndgameMetricsByTcSection` with `overviewData?.endgame_metrics_cards ?? { cards: [] }`; the old `EndgameMetricsSection` usage is gone.
    - Gate: `cd frontend && npm run lint` clean (no orphaned-import lint error) and `npx tsc --noEmit` zero errors.
  </acceptance_criteria>
  <done>The per-TC section renders in Endgames.tsx fed by endgame_metrics_cards; empty state mirrors Time Pressure; section test green; lint + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → client render | response data (rates, percentiles) rendered into DOM; no user-authored HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06 | Information disclosure | rendered card data | accept | Cards render the authenticated user's own aggregate rates/percentiles only (scoped server-side in Plan 02); no cross-user data; no hashes exposed |
| T-97-07 | Tampering | TC band rendering | mitigate | Bands come from the codegen'd `TC_METRIC_BANDS` (build-time, CI drift gate), not from API or user input |

No new auth/session/crypto surface; no new package install; React escapes all rendered text by default.
</threat_model>

<verification>
- `cd frontend && npm test -- --run EndgameMetricsByTcCard EndgameMetricsByTcSection` green.
- `cd frontend && npm run lint && npx tsc --noEmit` clean.
- Endgames page renders one card per eligible TC fed by `endgame_metrics_cards`; TC-specific bands applied to Conv/Recov, global band to Parity.
</verification>

<success_criteria>
SC-1 (one card per eligible TC, fixed order, no aggregated cards rendered), SC-2 (Conv/Parity/Recovery trifecta + WDL + Score Gap per TC), SC-3 (TC-specific Conv/Recov bands, global Parity/Score-Gap bands on the rendered cards), and SC-6 (responsive desktop + mobile) are satisfied by this plan.
</success_criteria>

<output>
Create `.planning/phases/97-endgame-metrics-by-time-control/97-03-SUMMARY.md` when done.
</output>
