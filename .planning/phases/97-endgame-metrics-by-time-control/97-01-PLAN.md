---
phase: 97-endgame-metrics-by-time-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_zones.py
  - scripts/gen_endgame_zones_ts.py
  - frontend/src/generated/endgameZones.ts
autonomous: true
requirements: [standalone]
must_haves:
  truths:
    - "Conversion and Recovery gauge/ΔES bands resolve per-TC (distinct bullet/blitz/rapid/classical) from the latest benchmark p25/p75"
    - "Parity bands stay global (unchanged) on every TC"
    - "endgameZones.ts is regenerated and CI drift gate is green"
  artifacts:
    - path: "app/services/endgame_zones.py"
      provides: "TcConvRecovBands dataclass + TC_METRIC_BANDS registry"
      contains: "TC_METRIC_BANDS"
    - path: "scripts/gen_endgame_zones_ts.py"
      provides: "_format_tc_metric_bands codegen emitter"
      contains: "_format_tc_metric_bands"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "TC_METRIC_BANDS TS export"
      contains: "TC_METRIC_BANDS"
  key_links:
    - from: "scripts/gen_endgame_zones_ts.py"
      to: "app/services/endgame_zones.py"
      via: "import TC_METRIC_BANDS"
      pattern: "from app.services.endgame_zones import"
    - from: "frontend/src/generated/endgameZones.ts"
      to: "scripts/gen_endgame_zones_ts.py"
      via: "codegen output"
      pattern: "TC_METRIC_BANDS"
---

<objective>
Add the TC-keyed neutral-band registry that makes Conversion and Recovery gauges + ΔES bullets TC-correct (benchmark §3.2.1 rates, §3.2.2 ΔES gaps), modeled on the existing TC-keyed `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` structure, and thread it through the codegen pipeline into `endgameZones.ts`. Implements D-06, D-07, D-08.

Purpose: Conversion/Recovery genuinely differ per time control (Cohen's d ≈ 0.9 on the TC axis); a single pooled band mislabels a bullet player who recovers at 35% as "strong" when 35% is merely typical for bullet. Parity collapses on TC (d < 0.15) and keeps its global band.
Output: `TC_METRIC_BANDS` Python registry + codegen emitter + regenerated TS constant consumed by the Wave 2 frontend cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-endgame-metrics-by-time-control/97-CONTEXT.md
@.planning/phases/97-endgame-metrics-by-time-control/97-RESEARCH.md
@.planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TcConvRecovBands dataclass and TC_METRIC_BANDS registry to endgame_zones.py</name>
  <files>app/services/endgame_zones.py</files>
  <read_first>
    - app/services/endgame_zones.py — the file being modified. Read `PerClassBands` + `PER_CLASS_GAUGE_ZONES` (lines 445-508) and `PressureBinBand` + `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` (lines 526-590): these are the exact analogs (frozen dataclass + TC-keyed `Mapping[Literal[...], ...]` registry). Confirm `Mapping` is already imported from `collections.abc` (line 14).
    - reports/benchmark/benchmarks-latest.md §3.2.1 (rate p25/p75) and §3.2.2 (ΔES gap p25/p75) — the numeric source of truth. Read-only; do NOT regenerate the benchmark.
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 105-183 — verbatim dataclass + registry shape with the exact band tuples.
  </read_first>
  <action>
    Add a `@dataclass(frozen=True)` named `TcConvRecovBands` with four `tuple[float, float]` fields: `conv_rate`, `recov_rate`, `conv_score_gap`, `recov_score_gap`. Then add a module-level `TC_METRIC_BANDS: Mapping[Literal["bullet", "blitz", "rapid", "classical"], TcConvRecovBands]` registry placed immediately after `PRESSURE_BIN_SCORE_NEUTRAL_ZONES`. Use these exact band tuples (verified against the benchmark report in RESEARCH/PATTERNS): bullet conv_rate=(0.588, 0.719) recov_rate=(0.295, 0.412) conv_score_gap=(-0.195, -0.057) recov_score_gap=(0.074, 0.177); blitz conv_rate=(0.667, 0.769) recov_rate=(0.251, 0.357) conv_score_gap=(-0.085, 0.003) recov_score_gap=(0.011, 0.084); rapid conv_rate=(0.696, 0.800) recov_rate=(0.218, 0.333) conv_score_gap=(-0.063, 0.021) recov_score_gap=(-0.008, 0.062); classical conv_rate=(0.685, 0.833) recov_rate=(0.174, 0.316) conv_score_gap=(-0.053, 0.038) recov_score_gap=(-0.037, 0.035). Do NOT touch `BUCKETED_ZONE_REGISTRY` or `ZONE_REGISTRY` — parity rate stays global there and the registry is still consumed by `assign_bucketed_zone` for LLM insights (per Claude's-discretion: the new registry sits ALONGSIDE the existing one, it does not replace it). Add a comment citing `reports/benchmark/benchmarks-latest.md §3.2.1 / §3.2.2` as the source.
  </action>
  <verify>
    <automated>uv run python -c "from app.services.endgame_zones import TC_METRIC_BANDS, TcConvRecovBands; assert TC_METRIC_BANDS['bullet'].conv_rate == (0.588, 0.719); assert TC_METRIC_BANDS['classical'].recov_score_gap == (-0.037, 0.035); assert set(TC_METRIC_BANDS) == {'bullet','blitz','rapid','classical'}; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/services/endgame_zones.py` contains `class TcConvRecovBands` decorated `@dataclass(frozen=True)` with fields `conv_rate`, `recov_rate`, `conv_score_gap`, `recov_score_gap`.
    - Source: `TC_METRIC_BANDS` is typed `Mapping[Literal["bullet","blitz","rapid","classical"], TcConvRecovBands]` (not `dict`).
    - Behavior: the import-and-assert one-liner above exits 0 with all four TC keys and the exact bullet/classical tuples.
    - Source: `BUCKETED_ZONE_REGISTRY` and `ZONE_REGISTRY` are byte-unchanged (`git diff` shows only additions, no edits to those registries).
    - Gate: `uv run ty check app/` reports zero errors for the file.
  </acceptance_criteria>
  <done>TC_METRIC_BANDS registry exists with the verified per-TC band tuples; existing registries untouched; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Emit TC_METRIC_BANDS via the codegen script and regenerate endgameZones.ts</name>
  <files>scripts/gen_endgame_zones_ts.py, frontend/src/generated/endgameZones.ts</files>
  <read_first>
    - scripts/gen_endgame_zones_ts.py — the file being modified. Read `_format_per_class_gauge_zones` (lines 97-113) and `_format_pressure_bin_zones` (lines 116-128) as the exact emitter analogs, plus the import block (lines 30-39) and the `_render()` assembly (around lines 240-243 where `CLOCK_GAP_NEUTRAL_*` is appended).
    - .planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md lines 359-450 — the verbatim `_format_tc_metric_bands` emitter and the exact TS export header/footer to append in `_render()`.
    - frontend/src/generated/endgameZones.ts — the regeneration target; confirm it is fully generated (no hand edits) so the drift gate is the only contract.
  </read_first>
  <action>
    Import `TC_METRIC_BANDS` from `app.services.endgame_zones` in the script's import block. Add a `_format_tc_metric_bands() -> str` function mirroring `_format_per_class_gauge_zones`: iterate `TC_METRIC_BANDS.items()`, unpack the four tuples, and emit one TS object line per TC of the form `  {tc}: {{ convRate: [lo, hi], recovRate: [lo, hi], convScoreGap: [lo, hi], recovScoreGap: [lo, hi] }},`. In `_render()`, append (after the existing CLOCK_GAP block) the TS export `TC_METRIC_BANDS` typed `Record<'bullet' | 'blitz' | 'rapid' | 'classical', { convRate: [number, number]; recovRate: [number, number]; convScoreGap: [number, number]; recovScoreGap: [number, number] }>` ending with `} as const;`, preceded by a two-line comment citing `reports/benchmark/benchmarks-latest.md §3.2.1 (rates) and §3.2.2 (ΔES gaps)`. Then run `uv run python scripts/gen_endgame_zones_ts.py` to regenerate `frontend/src/generated/endgameZones.ts` and commit the generated file. Do NOT hand-edit the generated TS.
  </action>
  <verify>
    <automated>uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts && grep -q "TC_METRIC_BANDS" frontend/src/generated/endgameZones.ts && echo "drift-clean+present"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `scripts/gen_endgame_zones_ts.py` defines `_format_tc_metric_bands` and imports `TC_METRIC_BANDS`.
    - Behavior: `frontend/src/generated/endgameZones.ts` contains `export const TC_METRIC_BANDS` with `bullet: { convRate: [0.588, 0.719], ... }` and `classical: { ..., recovScoreGap: [-0.037, 0.035] }`, ending `} as const;`.
    - Drift gate (CI-equivalent): `uv run python scripts/gen_endgame_zones_ts.py` followed by `git diff --exit-code frontend/src/generated/endgameZones.ts` produces NO diff after the file is committed.
    - Gate: `cd frontend && npx tsc --noEmit` (or `npm run build` type-check) compiles with the new export present.
  </acceptance_criteria>
  <done>endgameZones.ts contains TC_METRIC_BANDS matching the Python registry; drift gate green; TS compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time codegen | Python registry → generated TS; no runtime/user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | generated `endgameZones.ts` drift vs Python source | mitigate | CI drift gate (`gen_endgame_zones_ts.py` + `git diff --exit-code`) is the acceptance criterion on Task 2; band values are static constants, not user-supplied |
| T-97-02 | Information disclosure | band constants | accept | Benchmark p25/p75 bands are public, non-sensitive aggregate statistics; no PII |

No new auth surface, no untrusted input, no new dependency (no package install).
</threat_model>

<verification>
- `uv run ty check app/` zero errors; `uv run ruff check app/ scripts/` clean.
- Drift gate green: regen produces no diff once committed.
- `TC_METRIC_BANDS` present in both Python and generated TS with verified band tuples.
</verification>

<success_criteria>
SC-3 (Conv/Recov bands TC-specific, Parity/Score-Gap global) and SC-5 (TC-keyed bands threaded to frontend via regenerated endgameZones.ts, CI drift gate green) are satisfied by this plan's artifacts.
</success_criteria>

<output>
Create `.planning/phases/97-endgame-metrics-by-time-control/97-01-SUMMARY.md` when done.
</output>
