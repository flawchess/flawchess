---
phase: 97-endgame-metrics-by-time-control
plan: 04
type: execute
wave: 3
depends_on: ["97-03"]
files_modified:
  - frontend/src/components/charts/EndgameMetricsSection.tsx
  - frontend/src/components/charts/EndgameMetricCard.tsx
  - frontend/src/types/endgames.ts
  - frontend/src/pages/Endgames.tsx
  - app/schemas/endgames.py
  - app/services/endgame_service.py
  - tests/schemas/test_endgames_schema.py
  - tests/services/test_endgame_service_chip_decoupling.py
  - tests/test_endgame_service.py
autonomous: true
requirements: [standalone]
must_haves:
  truths:
    - "The old aggregated EndgameMetricsSection / EndgameMetricCard and their tests are deleted"
    - "The Metrics-section-only blended chip fields are removed from the backend schema and their populating call sites"
    - "Overall Performance chips (score_gap_percentile, achievable_score_gap_percentile and their _per_tc lists) are PRESERVED"
    - "knip is clean and all backend + frontend gates pass"
  artifacts:
    - path: "app/schemas/endgames.py"
      provides: "ScoreGapMaterialResponse with Metrics-section blended fields removed"
      contains: "ScoreGapMaterialResponse"
  key_links:
    - from: "app/services/endgame_service.py"
      to: "app/schemas/endgames.py"
      via: "_compute_score_gap_material no longer populates removed fields"
      pattern: "_compute_score_gap_material"
---

<objective>
Remove the now-dead aggregated Endgame Metrics section: delete the old `EndgameMetricsSection` / `EndgameMetricCard` components and tests, remove the Metrics-section-only blended chip fields from the backend schema and their populating locals/call sites, and run knip to clean up any newly-dead exports. Implements D-10 and the knip/dead-code discretion item, scoped precisely by the A1/A2 grep already resolved during planning.

Purpose: The blended/aggregated chip path is superseded by the per-TC cards (Plan 03). This is a net simplification, NOT a behavior change — but the removal scope is surgical because `_aggregate_per_tc_percentile` and `_build_per_tc_breakdown` are SHARED with the out-of-scope Overall Performance chips.
Output: Deleted old components/tests, trimmed schema, updated backend tests, knip-clean tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-endgame-metrics-by-time-control/97-CONTEXT.md
@.planning/phases/97-endgame-metrics-by-time-control/97-RESEARCH.md
@.planning/phases/97-endgame-metrics-by-time-control/97-PATTERNS.md
</context>

<removal_scope_resolved>
The A1/A2 open questions from RESEARCH were RESOLVED during planning by grepping frontend + backend consumers. Definitive scope (do NOT re-litigate; re-run the grep in Task 1 as a guard, but this is the answer):

REMOVE (Metrics-section-only — consumed only by the deleted EndgameMetricsSection.tsx):
- ScoreGapMaterialResponse.score_gap_conv_percentile, .score_gap_parity_percentile, .recovery_score_gap_percentile
- ScoreGapMaterialResponse.score_gap_conv_per_tc, .score_gap_parity_per_tc, .recovery_score_gap_per_tc
- Their populating locals + call sites in _compute_score_gap_material (endgame_service.py lines ~1533, 1536, 1545 for the percentiles; ~1562, 1565, 1568 for the _per_tc lists) and the matching kwargs in the ScoreGapMaterialResponse(...) constructor.

KEEP (out-of-scope — consumed by EndgameOverallPerformanceSection.tsx, untouched this phase):
- ScoreGapMaterialResponse.score_gap_percentile and .score_gap_per_tc (Overall Performance "Endgame Score Gap" chip + tooltip).
- EndgamePerformanceResponse.achievable_score_gap_percentile and .achievable_score_gap_per_tc (Overall Performance "Achievable Score Gap" chip + tooltip).
- Therefore _aggregate_per_tc_percentile (still called at line ~1529 for score_gap and ~2596 for achievable) and _build_per_tc_breakdown (still called at ~1559 and ~2607) SURVIVE. This narrows RESEARCH's blanket "remove _aggregate_per_tc_percentile" — that helper is shared and stays.
</removal_scope_resolved>

<tasks>

<task type="auto">
  <name>Task 1: Grep-guard the scope, then remove backend Metrics-section blended fields</name>
  <files>app/schemas/endgames.py, app/services/endgame_service.py, tests/schemas/test_endgames_schema.py, tests/services/test_endgame_service_chip_decoupling.py, tests/test_endgame_service.py</files>
  <read_first>
    - app/schemas/endgames.py — ScoreGapMaterialResponse field definitions at lines 552 (score_gap_conv_percentile), 560 (score_gap_conv_per_tc), 570 (score_gap_parity_percentile), 578 (score_gap_parity_per_tc), 596 (recovery_score_gap_percentile), 604 (recovery_score_gap_per_tc); and the KEEP fields score_gap_percentile + score_gap_per_tc nearby.
    - app/services/endgame_service.py — _compute_score_gap_material (lines 1410-1572): percentile locals (1529 KEEP score_gap, 1533/1536/1545 REMOVE conv/parity/recov), _per_tc locals (1559 KEEP score_gap_per_tc, 1562/1565/1568 REMOVE conv/parity/recov), and the ScoreGapMaterialResponse(...) constructor kwargs (1572+). Confirm _aggregate_per_tc_percentile and _build_per_tc_breakdown remain called for score_gap (1529/1559) and achievable (2596/2607) — do NOT delete those two helpers.
    - tests/services/test_endgame_service_chip_decoupling.py — asserts on score_gap_conv_percentile / score_gap_parity_percentile (lines 184-187, 289-293, 369-373); tests/test_endgame_service.py lines 4348-4478; tests/schemas/test_endgames_schema.py lines 89-103 — these assert on removed fields and must be updated or deleted.
    - The removal_scope_resolved block above — the definitive REMOVE/KEEP list.
  </read_first>
  <action>
    Run the grep guard first to re-confirm scope: grep the six REMOVE field names across frontend/src/. Expect consumers ONLY in EndgameMetricsSection.tsx (deleted in Task 2) and as null-filler fixtures in EndgameOverallPerformanceSection.test.tsx (not a render). If any OTHER non-test frontend consumer appears, STOP and flag — scope changed. Then remove the six REMOVE fields from ScoreGapMaterialResponse; remove their populating locals and constructor kwargs in _compute_score_gap_material; KEEP score_gap_percentile, score_gap_per_tc, the _aggregate_per_tc_percentile and _build_per_tc_breakdown helpers, and the achievable call sites. Update the affected backend tests in test_endgame_service_chip_decoupling.py, test_endgame_service.py, and test_endgames_schema.py: delete or rewrite the assertions referencing the six removed fields. Do NOT touch assertions on score_gap_percentile or achievable_score_gap_percentile.
  </action>
  <verify>
    <automated>! grep -rq "score_gap_conv_percentile\|score_gap_parity_percentile\|recovery_score_gap_percentile\|score_gap_conv_per_tc\|score_gap_parity_per_tc\|recovery_score_gap_per_tc" app/ && uv run pytest tests/test_endgame_service.py tests/services/test_endgame_service_chip_decoupling.py tests/schemas/test_endgames_schema.py -x && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - Source: the six REMOVE fields are absent from app/ (grep across app/ returns no hits); the four KEEP fields and both shared helpers remain.
    - Source: grep guard confirmed no non-test frontend consumer of the removed fields outside EndgameMetricsSection.tsx.
    - Behavior: `uv run pytest tests/test_endgame_service.py tests/services/test_endgame_service_chip_decoupling.py tests/schemas/test_endgames_schema.py -x` exits 0 after the assertion updates.
    - Gate: `uv run ty check app/ tests/` zero errors.
  </acceptance_criteria>
  <done>Metrics-section blended fields removed from backend schema + service; shared helpers and Overall-Performance chips preserved; backend tests green; ty clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old frontend components/tests, trim TS types, knip-clean</name>
  <files>frontend/src/components/charts/EndgameMetricsSection.tsx, frontend/src/components/charts/EndgameMetricCard.tsx, frontend/src/types/endgames.ts, frontend/src/pages/Endgames.tsx</files>
  <read_first>
    - frontend/src/components/charts/EndgameMetricsSection.tsx and EndgameMetricCard.tsx — the files to delete; confirm (grep) no consumer other than Endgames.tsx (already swapped in Plan 03) and their own tests imports them.
    - frontend/src/components/charts/__tests__/ — check for EndgameMetricsSection.test.tsx and EndgameMetricCard.test.tsx to delete alongside the sources.
    - frontend/src/types/endgames.ts — ScoreGapMaterialResponse interface (lines 234, 236, 246, 248, 262, 264 hold the six REMOVE fields); EndgameOverviewResponse. Remove the same six fields here to match the backend; KEEP score_gap_percentile, score_gap_per_tc, achievable_score_gap_percentile, achievable_score_gap_per_tc.
    - frontend/src/pages/Endgames.tsx — line 22 (EndgameMetricsSection import) and any residual scoreGapData-only usage; remove the now-orphaned import if Plan 03 left it.
    - .planning/phases/97-endgame-metrics-by-time-control/97-RESEARCH.md Pitfall 6 + 97-PATTERNS.md lines 773-787 (knip note: SCORE_GAP_CONV_NEUTRAL_MIN/MAX, SCORE_GAP_RECOV_NEUTRAL_MIN/MAX in endgameZones.ts / gen_endgame_zones_ts.py may become dead; FIXED_GAUGE_ZONES.parity and parity scalars stay alive for the new section; MaterialBucket may become dead).
    - CLAUDE.md — knip runs in CI (remove dead exports when removing features); when removing a feature, also remove its exports.
  </read_first>
  <action>
    Delete EndgameMetricsSection.tsx and EndgameMetricCard.tsx and their test files (if present). Remove the orphaned EndgameMetricsSection import from Endgames.tsx (if Plan 03 left it). In frontend/src/types/endgames.ts, remove the six Metrics-section fields from ScoreGapMaterialResponse (matching the backend removal); keep the four Overall-Performance fields. Run `npm run knip` and resolve every reported dead export: if knip flags SCORE_GAP_CONV_NEUTRAL_MIN/MAX or SCORE_GAP_RECOV_NEUTRAL_MIN/MAX (now that EndgameMetricCard no longer consumes them and the new card uses TC_METRIC_BANDS instead), remove their emission from scripts/gen_endgame_zones_ts.py and regenerate endgameZones.ts (re-run the drift gate after). If knip flags MaterialBucket or a FIXED_GAUGE_ZONES member, only remove members with zero remaining consumers — FIXED_GAUGE_ZONES.parity and the parity neutral scalars MUST stay (the new parity gauge uses the global band). Do NOT remove anything still imported by EndgameMetricsByTcCard or the Overall Performance section.
  </action>
  <verify>
    <automated>cd frontend && test ! -e src/components/charts/EndgameMetricsSection.tsx && test ! -e src/components/charts/EndgameMetricCard.tsx && npm run knip && npm run lint && npm test -- --run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: EndgameMetricsSection.tsx, EndgameMetricCard.tsx, and their test files no longer exist; no remaining import of them anywhere.
    - Source: the six Metrics-section fields are gone from ScoreGapMaterialResponse in types/endgames.ts; the four Overall-Performance fields remain.
    - Gate: `cd frontend && npm run knip` reports zero dead exports / unused deps.
    - Drift: if the codegen script changed, `uv run python scripts/gen_endgame_zones_ts.py && git diff --exit-code frontend/src/generated/endgameZones.ts` produces no diff after commit; TC_METRIC_BANDS and FIXED_GAUGE_ZONES.parity still present.
    - Gate: `cd frontend && npm run lint && npm test -- --run && npx tsc --noEmit` all clean/green.
  </acceptance_criteria>
  <done>Old components/tests deleted; TS types trimmed to match backend; knip clean; parity band + per-TC bands preserved; all frontend gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time | dead-code removal + codegen; no runtime/user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-08 | Tampering | over-broad removal breaking Overall Performance chips | mitigate | Grep guard (Task 1) confirms scope before deletion; KEEP list enumerated; full backend + frontend test suites + knip gate the change |
| T-97-09 | Denial of service | accidental removal of a live export | mitigate | knip + tsc + lint + vitest must all pass; drift gate re-run if codegen touched |

No new auth/session/crypto surface; no new package install; pure removal of superseded code.
</threat_model>

<verification>
- Backend: `uv run pytest -x` green; `uv run ty check app/ tests/` + `uv run ruff check app/ tests/` clean.
- Frontend: `npm run knip` clean; `npm run lint && npm test -- --run && npx tsc --noEmit` all green.
- Drift gate green if codegen touched; TC_METRIC_BANDS + FIXED_GAUGE_ZONES.parity present.
- Overall Performance section unchanged (its chips/tooltips still render — score_gap + achievable fields preserved).
</verification>

<success_criteria>
SC-1 (no aggregated cards remain), SC-5 (CI drift gate green after any codegen change), and SC-7 (backend pytest/ty/ruff + frontend lint/test/knip all pass) are satisfied by this plan.
</success_criteria>

<output>
Create `.planning/phases/97-endgame-metrics-by-time-control/97-04-SUMMARY.md` when done.
</output>
