---
phase: 145-corpus-backfill-rollout
plan: 06
type: execute
wave: 5
depends_on: [145-04, 145-05]
files_modified:
  - reports/retag/rollout-PLACEHOLDER.md
autonomous: false
requirements: [SHIP-01, SHIP-02]
user_setup:
  - service: prod remote-worker fleet
    why: "The bulk MultiPV-2 backfill compute runs on upgraded remote workers; until they are deployed, the tier-4 lottery fires but leases expire with no submits (RESEARCH Open Question 2)"
must_haves:
  truths:
    - "Dev-first gate: the full tier-4 lottery → flaw-blob lease → submit → per-game gated retag loop runs end-to-end on the dev DB and is idempotent BEFORE any --db prod action (autonomous)"
    - "[HUMAN-VERIFY] A prod 'before' per-motif chip-count snapshot is captured BEFORE any retag (before the upgraded worker begins submitting blobs to prod)"
    - "[HUMAN-VERIFY] The prod corpus backfill drains on the fleet until backfill_multipv.py --db prod --status shows ~0 remaining NULL-blob flaws (only sentinel-covered un-fillable lines remain)"
    - "[HUMAN-VERIFY] retag_flaws.py --db prod sweeps the already-blobbed stale-tag residual (D-08), then a prod 'after' snapshot confirms per-motif noise reduction (SC3)"
  artifacts:
    - reports/retag/rollout-PLACEHOLDER.md
  key_links:
    - "Order: before snapshot → worker deploy → fleet drain + rolling D-07 retag → D-08 sweep → after snapshot"
    - "D-08 sweep runs ONLY when --status shows near-zero NULL blobs (Pitfall 4)"
---

<objective>
Orchestrate the production rollout (D-07 rolling + D-08 sweep + SC3 before/after) with the correct
sequencing. The CODE/CLI/tests are all autonomous (Plans 01-05); the actual prod drain depends on the
upgraded-worker deployment and spare fleet capacity and "finishes eventually" — so the prod steps are
human-verify checkpoints, not synchronously-completable executor work (RESEARCH Open Question 2,
VALIDATION.md Manual-Only).

Critical ordering (phase guidance point 4): the **"before" snapshot must be captured BEFORE any
retag** — including the rolling D-07 retag that fires as blobs arrive. So the before snapshot is taken
BEFORE the upgraded worker begins submitting blobs to prod. Then: deploy upgraded worker → fleet
drains the tier-4 backfill + rolling per-game D-07 retag → observe `--status` to ~0 → run the D-08
`retag_flaws.py --db prod` sweep for the already-blobbed stale-tag residual (only once `--status` is
near-zero, Pitfall 4) → capture the "after" snapshot → commit the before/after report.

Purpose: Land gated tactic tags across the full prod corpus and record the noise reduction.
Output: A committed `reports/retag/rollout-YYYY-MM-DD.md` with before/after per-motif counts.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan's slice)
- `reports/retag/rollout-YYYY-MM-DD.md` — committed before/after per-motif chip-count report (SC3),
  produced by `scripts/snapshot_tactic_counts.py` (Plan 05).
- No new code symbols; this plan runs the Plan 01-05 artifacts against prod under human verification.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-corpus-backfill-rollout/145-CONTEXT.md
@.planning/phases/145-corpus-backfill-rollout/145-RESEARCH.md
@scripts/backfill_multipv.py
@scripts/snapshot_tactic_counts.py
@scripts/retag_flaws.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dev-first end-to-end validation gate (autonomous, blocks prod)</name>
  <files>reports/retag/rollout-PLACEHOLDER.md</files>
  <read_first>
    - scripts/backfill_multipv.py — `--dev-validate` (Plan 05 Task 1)
    - scripts/snapshot_tactic_counts.py — `--phase before` against dev (Plan 05 Task 2)
    - .planning/phases/145-corpus-backfill-rollout/145-RESEARCH.md "Dev-first validation gate" + Pitfall 4
  </read_first>
  <action>
    Run the dev-first gate BEFORE any prod action (CONTEXT.md Claude's-Discretion dev-first decision):
    `uv run python scripts/backfill_multipv.py --db dev --dev-validate` must drive the tier-4 lottery →
    flaw-blob lease → submit → per-game D-07 retag end-to-end on the dev DB and confirm a game gains
    blobs + gated tags and then fails the `allowed_pv_lines IS NULL` predicate (idempotency). Also run
    `--db dev --status` and `snapshot_tactic_counts.py --db dev --phase before` to confirm the
    observability + report tooling works against a real DB. This gate is the autonomous precondition for
    the prod checkpoints below — if it fails, STOP and fix before any `--db prod` step. (Do NOT reset
    the dev DB; run against the existing dev data.)
  </action>
  <verify>
    <automated>uv run python scripts/backfill_multipv.py --db dev --dev-validate && uv run python scripts/backfill_multipv.py --db dev --status</automated>
  </verify>
  <acceptance_criteria>
    - The dev end-to-end loop completes; a validated game has blobs + gated tags and no longer matches the predicate.
    - `--status` and the before-snapshot tooling run clean on dev.
  </acceptance_criteria>
  <done>The full backfill→retag pipeline is proven on dev; prod rollout is unblocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prod BEFORE snapshot + worker deploy + drain to ~0 (human-verify)</name>
  <action>
    Operator-driven (no executor automation): capture the prod "before" per-motif snapshot FIRST,
    confirm the upgraded worker is deployed, then observe the fleet drain via --status until near-zero
    NULL-blob flaws. Follow the numbered how-to-verify steps exactly; the before snapshot MUST precede
    any blob submission so it predates the rolling D-07 retag.
  </action>
  <what-built>
    The dev-validated tier-4 backfill pipeline plus `backfill_multipv.py --db prod --status` and
    `snapshot_tactic_counts.py --db prod --phase before`. The prod "before" snapshot must be captured
    BEFORE the upgraded worker begins submitting blobs (before any rolling D-07 retag mutates tags).
  </what-built>
  <how-to-verify>
    1. Start the prod DB tunnel: `bin/prod_db_tunnel.sh` (forwards localhost:15432).
    2. Capture the prod BEFORE snapshot FIRST: `uv run python scripts/snapshot_tactic_counts.py --db prod --phase before` — confirm reports/retag/rollout-<date>.md has the "Before rollout" per-motif table. This MUST happen before the fleet starts submitting.
    3. Confirm the upgraded remote-worker fleet (with the new /eval/remote/flaw-blob-lease + /flaw-blob-submit support) is deployed to prod (RESEARCH Open Question 2). Until then the tier-4 lottery fires but leases expire harmlessly.
    4. Let the fleet drain on spare capacity. Periodically run `uv run python scripts/backfill_multipv.py --db prod --status` and watch games_with_null_blobs trend toward ~0 (a stable low floor = sentinel-covered un-fillable lines).
    5. Confirm rolling D-07 retag is working: spot-check a freshly-backfilled prod game has both blobs and gated tactic tags.
  </how-to-verify>
  <resume-signal>Type "before snapshot captured + worker deployed + drain near-zero" or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-08 prod sweep + AFTER snapshot + commit report (human-verify)</name>
  <action>
    Operator-driven (no executor automation): once --status is near-zero, run the D-08 retag sweep on
    the prod server, capture the prod "after" snapshot, verify per-motif deltas are reductions, and
    commit the reports/retag/rollout-<date>.md report. Follow the numbered how-to-verify steps.
  </action>
  <what-built>
    The D-08 `retag_flaws.py --db prod` sweep (existing, idempotent, engine-free, change-only UPDATE)
    for the already-blobbed-but-stale-tag residual, and the prod "after" snapshot.
  </what-built>
  <how-to-verify>
    1. Only once `backfill_multipv.py --db prod --status` shows near-zero NULL-blob flaws (Pitfall 4 — running the sweep early locks in unfiltered tags for un-blobbed flaws), run the D-08 sweep ON THE PROD SERVER (the local SSH tunnel is read-only): `ssh flawchess` then `uv run python scripts/retag_flaws.py --db prod --workers 6 --throttle-ms 50` (confirm the exact safe invocation against the script's --db prod note).
    2. Capture the prod AFTER snapshot: `uv run python scripts/snapshot_tactic_counts.py --db prod --phase after` — confirm reports/retag/rollout-<date>.md now has the "After rollout" table + per-motif deltas showing noise reduction (SC3).
    3. Sanity-check the deltas are reductions (gated suppression lowered the inflated motif counts), not anomalous increases.
    4. Commit reports/retag/rollout-<date>.md.
  </how-to-verify>
  <resume-signal>Type "D-08 sweep done + after snapshot shows reduction + report committed" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod (tunnel / SSH) | Human-gated prod operations: read-only status via tunnel, the D-08 sweep run on the prod server per the script's write-safety note. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-15 | Tampering | premature D-08 sweep | medium | mitigate | Human checkpoint gates the sweep on `--status` near-zero (Pitfall 4) so un-blobbed flaws are not locked into unfiltered tags. |
| T-145-16 | Tampering | --db prod write from read-only tunnel | low | mitigate | retag_flaws.py --db prod runs on the prod server (the tunnel is read-only); snapshots are read-only. |

No new dependencies are installed in this plan (no supply-chain threat row).
</threat_model>

<verification>
- Dev gate: `backfill_multipv.py --db dev --dev-validate` passes (autonomous).
- [HUMAN] Prod before snapshot captured before any retag; upgraded worker deployed; `--status` drains to ~0.
- [HUMAN] D-08 sweep applied on prod server; after snapshot shows per-motif reduction; report committed.
</verification>

<success_criteria>
- SHIP-01: prod corpus backfilled (NULL-blob flaws drained to a sentinel-only floor) via the fleet, no second pool.
- SHIP-02: gated tags live across the full prod corpus (rolling D-07 + D-08 sweep); before/after per-motif
  counts recorded in a committed reports/retag/ markdown confirming noise reduction (SC3).
</success_criteria>

<output>
Create `.planning/phases/145-corpus-backfill-rollout/145-06-SUMMARY.md` when done.
</output>
