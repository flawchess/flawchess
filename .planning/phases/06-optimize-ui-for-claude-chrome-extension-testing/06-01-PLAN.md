---
phase: 06-optimize-ui-for-claude-chrome-extension-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Auth.tsx
  - frontend/src/pages/Bookmarks.tsx
  - frontend/src/pages/Stats.tsx
  - frontend/src/components/filters/FilterPanel.tsx
  - frontend/src/components/board/BoardControls.tsx
  - frontend/src/components/board/MoveList.tsx
  - frontend/src/components/import/ImportModal.tsx
  - frontend/src/components/import/ImportProgress.tsx
  - frontend/src/components/bookmarks/BookmarkCard.tsx
  - frontend/src/components/results/GameTable.tsx
  - frontend/src/components/auth/LoginForm.tsx
  - frontend/src/components/auth/RegisterForm.tsx
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "Every interactive element (button, link, input, toggle) has a data-testid attribute"
    - "Nav links are wrapped in a semantic <nav> element"
    - "BookmarkCard label uses a <button> instead of <span onClick>"
    - "All icon-only buttons have aria-label attributes"
    - "Filter toggle buttons have data-testid with descriptive names"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Semantic <nav> wrapper, data-testid on nav links and logout button"
      contains: "<nav"
    - path: "frontend/src/components/filters/FilterPanel.tsx"
      provides: "data-testid on all toggle groups, time control buttons, platform buttons, more-filters trigger, recency select"
    - path: "frontend/src/components/bookmarks/BookmarkCard.tsx"
      provides: "Semantic <button> for label edit, data-testid on Load/Delete/label/card"
    - path: "frontend/src/components/board/MoveList.tsx"
      provides: "data-testid and aria-label on each move button"
    - path: "frontend/src/components/board/BoardControls.tsx"
      provides: "data-testid on all four board control buttons"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "NavHeader nav items"
      via: "data-testid on Button asChild > Link"
      pattern: "data-testid=\"nav-"
    - from: "frontend/src/components/bookmarks/BookmarkCard.tsx"
      to: "label click handler"
      via: "<button> element with onClick"
      pattern: "<button.*onClick.*handleLabelClick"
---

<objective>
Add data-testid attributes, ARIA labels, and semantic HTML fixes across all frontend components (excluding ChessBoard click-to-move, which is in plan 02).

Purpose: Enable the Claude Chrome extension to reliably target every interactive element via stable selectors and the accessibility tree.
Output: All non-board frontend components updated with data-testid, aria-label, and semantic HTML corrections.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-optimize-ui-for-claude-chrome-extension-testing/06-CONTEXT.md
@.planning/phases/06-optimize-ui-for-claude-chrome-extension-testing/06-RESEARCH.md

<interfaces>
<!-- data-testid naming convention (from RESEARCH.md Pattern 1): -->
<!-- kebab-case, component-prefixed: {component}-{element}-{qualifier?} -->
<!-- btn-{action} for standalone buttons -->
<!-- nav-{page} for nav links -->
<!-- filter-{name} for filter controls -->
<!-- board-btn-{action} for board controls -->
<!-- {component}-{element}-{id?} for dynamic elements -->
<!-- move-{ply} for move list items -->
<!-- See RESEARCH.md Pattern 1 for the complete naming reference -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid and semantic fixes to auth, nav, import, and page containers</name>
  <files>
    frontend/src/App.tsx,
    frontend/src/pages/Auth.tsx,
    frontend/src/pages/Dashboard.tsx,
    frontend/src/pages/Bookmarks.tsx,
    frontend/src/pages/Stats.tsx,
    frontend/src/components/auth/LoginForm.tsx,
    frontend/src/components/auth/RegisterForm.tsx,
    frontend/src/components/import/ImportModal.tsx,
    frontend/src/components/import/ImportProgress.tsx
  </files>
  <action>
**App.tsx (NavHeader):**
- Wrap the nav links `<div>` (currently `<div className="flex items-center gap-1">`) in a `<nav aria-label="Main navigation">` element. Keep the brand span outside or inside nav as makes sense visually.
- Add `data-testid={`nav-${label.toLowerCase()}`}` to each `<Link>` child (not the Button wrapper, since asChild merges props into the child Link — put data-testid on the `<Link>` element so it ends up on the rendered `<a>`).
- Add `data-testid="nav-logout"` to the Logout button.

**Auth.tsx:**
- Add `data-testid="auth-tab-login"` to the Sign In TabsTrigger.
- Add `data-testid="auth-tab-register"` to the Register TabsTrigger.
- Add `data-testid="auth-page"` to the outer page container div.

**LoginForm.tsx:**
- Add `data-testid="login-email"` to the email input.
- Add `data-testid="login-password"` to the password input.
- Add `data-testid="btn-login"` to the submit button.
- Add `data-testid="btn-login-google"` to the Google sign-in button (if present).

**RegisterForm.tsx:**
- Add `data-testid="register-email"` to the email input.
- Add `data-testid="register-password"` to the password input.
- Add `data-testid="btn-register"` to the submit button.
- Add `data-testid="btn-register-google"` to the Google sign-in button (if present).

**Dashboard.tsx:**
- Add `data-testid="dashboard-page"` to the outermost div.
- Add `data-testid="btn-filter"` to the Filter/Analyze button.
- Add `data-testid="btn-bookmark"` to the Bookmark button.
- Add `data-testid="btn-import"` to the Import button in the action bar.
- Add `data-testid="btn-import-cta"` to the "Import Games" CTA button in the empty-state view.
- Add `data-testid="bookmark-dialog"` to the bookmark Dialog.
- Add `data-testid="bookmark-label-input"` to the bookmark label Input.
- Add `data-testid="btn-bookmark-save"` to the Save button in bookmark dialog.

**Bookmarks.tsx:**
- Add `data-testid="bookmarks-page"` to the page container.

**Stats.tsx:**
- Add `data-testid="stats-page"` to the page container.
- Add `data-testid="stats-btn-analyze"` to the Analyze button (if separate from dashboard).

**ImportModal.tsx:**
- Add `data-testid="import-modal"` to the Dialog or DialogContent.
- Add `data-testid="import-platform-chess-com"` and `data-testid="import-platform-lichess"` to the platform toggle items.
- Add `data-testid="import-username"` to the username input.
- Add `data-testid="btn-import-start"` to the Import submit button.
- Add `data-testid="btn-import-cancel"` to the Cancel button.

**ImportProgress.tsx:**
- Add `data-testid="import-progress"` to the progress container (if applicable).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build && npm run lint</automated>
  </verify>
  <done>All auth, nav, import, and page container elements have data-testid attributes. Nav links are wrapped in semantic `<nav>`. Build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add data-testid and ARIA to filters, board controls, move list, bookmarks, and game table</name>
  <files>
    frontend/src/components/filters/FilterPanel.tsx,
    frontend/src/components/board/BoardControls.tsx,
    frontend/src/components/board/MoveList.tsx,
    frontend/src/components/bookmarks/BookmarkCard.tsx,
    frontend/src/components/results/GameTable.tsx
  </files>
  <action>
**FilterPanel.tsx:**
- Add `data-testid="filter-played-as"` to the "Played as" ToggleGroup.
- Add `data-testid="filter-played-as-any"`, `data-testid="filter-played-as-white"`, `data-testid="filter-played-as-black"` to each ToggleGroupItem in "Played as".
- Add `data-testid="filter-match-side"` to the "Match side" ToggleGroup.
- Add `data-testid="filter-match-side-white"`, `data-testid="filter-match-side-black"`, `data-testid="filter-match-side-full"` to each ToggleGroupItem in "Match side".
- Add `data-testid="filter-more-toggle"` to the "More filters" CollapsibleTrigger button.
- Add `data-testid={`filter-time-control-${tc}`}` and `aria-label={`${TIME_CONTROL_LABELS[tc]} time control`}` and `aria-pressed={isTimeControlActive(tc)}` to each time control `<button>`.
- Add `data-testid={`filter-platform-${p === 'chess.com' ? 'chess-com' : p}`}` and `aria-label={`${PLATFORM_LABELS[p]} platform`}` and `aria-pressed={isPlatformActive(p)}` to each platform `<button>`.
- Add `data-testid="filter-rated"` to the Rated ToggleGroup.
- Add `data-testid="filter-rated-all"`, `data-testid="filter-rated-rated"`, `data-testid="filter-rated-casual"` to its items.
- Add `data-testid="filter-opponent"` to the Opponent ToggleGroup.
- Add `data-testid="filter-opponent-human"`, `data-testid="filter-opponent-bot"`, `data-testid="filter-opponent-both"` to its items.
- Add `data-testid="filter-recency"` to the Recency Select trigger.

**BoardControls.tsx:**
- Add `data-testid="board-btn-reset"` to the reset button.
- Add `data-testid="board-btn-back"` to the back button.
- Add `data-testid="board-btn-forward"` to the forward button.
- Add `data-testid="board-btn-flip"` to the flip button.
(These buttons already have aria-labels — keep them.)

**MoveList.tsx:**
- Add `data-testid={`move-${ply}`}` to each move button (where ply is the move's ply number).
- Add `aria-label` to each move button describing the move, e.g., `aria-label={`Move ${Math.ceil(ply/2)}. ${moveText} (${ply % 2 === 1 ? 'white' : 'black'})`}`.

**BookmarkCard.tsx:**
- Convert the label `<span onClick={handleLabelClick}>` to a `<button>` element. Style it to look identical: `className="cursor-text text-sm font-medium truncate block w-full text-left bg-transparent border-none p-0"`. Add `aria-label={`Edit bookmark label: ${bookmark.label}`}`.
- Add `data-testid={`bookmark-card-${bookmark.id}`}` to the outer card div.
- Add `data-testid={`bookmark-label-${bookmark.id}`}` to the label button.
- Add `data-testid={`bookmark-label-input-${bookmark.id}`}` to the label edit input.
- Add `data-testid={`bookmark-btn-load-${bookmark.id}`}` to the Load button.
- Add `data-testid={`bookmark-btn-delete-${bookmark.id}`}` to the Delete button.

**GameTable.tsx:**
- Add `data-testid="pagination-prev"` to the Previous page button.
- Add `data-testid="pagination-next"` to the Next page button.
- Add `data-testid="game-table"` to the table container or table element.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build && npm run lint</automated>
  </verify>
  <done>All filter controls, board controls, move list buttons, bookmark card elements, and game table pagination have data-testid and appropriate aria-label attributes. BookmarkCard label is a semantic button. Build and lint pass.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `npm run lint` passes
- Grep for `data-testid` across all modified files confirms presence on all interactive elements listed in the research audit
</verification>

<success_criteria>
Every interactive element identified in the 06-RESEARCH.md "What Needs Work" audit table has a data-testid attribute. The two semantic HTML issues (nav wrapper, BookmarkCard label span) are fixed. All icon-only buttons retain their aria-labels. Build and lint pass clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimize-ui-for-claude-chrome-extension-testing/06-01-SUMMARY.md`
</output>
