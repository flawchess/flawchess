---
phase: 41.1-import-speed-optimization
verified: 2026-04-03T12:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: null
gaps: []
human_verification: []
---

# Phase 41.1: Import Speed Optimization Verification Report

**Phase Goal:** Import pipeline processes games at ~2x current throughput by eliminating triple PGN parsing, redundant DB round-trips, and per-game UPDATEs
**Verified:** 2026-04-03
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `process_game_pgn` returns hashes, classification, eval, clock, move_san, move_count, and result_fen from a single PGN parse | VERIFIED | `zobrist.py` lines 125-242: single mainline walk populates all PlyData fields and GameProcessingResult |
| 2 | `hashes_for_game` remains backward compatible — all existing callers and tests pass without modification | VERIFIED | `hashes_for_game` is a thin wrapper (lines 277-284); 39/39 test_zobrist.py tests pass; test_seed_openings.py and test_reclassify.py callers untouched |
| 3 | `process_game_pgn` returns correct classification fields matching `classify_position` on the same board state | VERIFIED | `test_process_game_pgn_classification_matches_classify_position` verifies all 7 fields; 12 new tests pass |
| 4 | `process_game_pgn` returns correct eval_cp/eval_mate from PGN %eval annotations | VERIFIED | `test_process_game_pgn_eval_extraction` confirms eval_cp=30 for `{[%eval 0.3]}`; test passes |
| 5 | `_flush_batch` calls `process_game_pgn` once per game instead of parsing PGN three times | VERIFIED | `import_service.py` line 415: single `process_game_pgn(pgn)` call; no `chess.pgn`, `classify_position`, or `classify_endgame` imports remain |
| 6 | `_flush_batch` does not SELECT Game.pgn from DB — uses in-memory lookup by platform_game_id | VERIFIED | Lines 383-401: `pgn_by_platform_id` dict built from batch; `select(Game.id, Game.platform_game_id)` only — no pgn column selected |
| 7 | `_flush_batch` issues a single bulk UPDATE for move_count/result_fen per batch instead of per-game UPDATEs | VERIFIED | Lines 458-470: `case(move_counts, value=Game.id)` CASE expression; no individual per-game UPDATE statements |
| 8 | `_BATCH_SIZE` is 28 (up from 10) | VERIFIED | `import_service.py` line 37: `_BATCH_SIZE = 28` |
| 9 | All existing import integration tests pass (mocks updated for new function) | VERIFIED | 32/32 test_import_service.py tests pass; 485/485 full suite passes |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/services/zobrist.py` | `process_game_pgn` unified function + `PlyData`/`GameProcessingResult` TypedDicts | VERIFIED | Lines 32-67: TypedDicts with all 16/3 fields; lines 125-242: full implementation; `hashes_for_game` delegates to it |
| `tests/test_zobrist.py` | Tests for `process_game_pgn` correctness and `hashes_for_game` wrapper equivalence | VERIFIED | 12 new tests present (lines 336-453), all 39 tests pass |
| `app/services/import_service.py` | Refactored `_flush_batch` with single PGN parse, platform_game_id lookup, bulk CASE UPDATE; `_BATCH_SIZE = 28` | VERIFIED | All acceptance criteria confirmed in code; ruff clean |
| `tests/test_import_service.py` | Updated mocks targeting `process_game_pgn` instead of `hashes_for_game` | VERIFIED | 6 patches target `app.services.import_service.process_game_pgn`; `_make_mock_processing_result` helper present; `hashes_for_game` string absent |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/services/zobrist.py` | `app/services/position_classifier.py` | `from app.services.position_classifier import classify_position` | WIRED | Line 24; called at lines 162, 208 inside `process_game_pgn` |
| `app/services/zobrist.py` | `app/services/endgame_service.py` | `from app.services.endgame_service import classify_endgame_class, _CLASS_TO_INT` | WIRED | Line 23; called at lines 179, 211 inside `process_game_pgn` |
| `app/services/import_service.py` | `app/services/zobrist.py` | `from app.services.zobrist import process_game_pgn` | WIRED | Line 28; called at line 415 inside `_flush_batch` |
| `app/services/import_service.py` | SQLAlchemy `case` function | `from sqlalchemy import case` | WIRED | Line 21; used at line 462 for bulk CASE UPDATE |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `app/services/import_service.py` (`_flush_batch`) | `processing_result` | `process_game_pgn(pgn)` where `pgn` comes from `pgn_by_platform_id[platform_game_id]` populated from in-memory batch | Yes — walks real PGN mainline via python-chess | FLOWING |
| `app/services/import_service.py` (`_flush_batch`) | `position_rows` | Populated from `processing_result["plies"]` | Yes — all 18 fields (hashes, classification, eval, clock) sourced from real computation | FLOWING |
| `app/services/import_service.py` (`_flush_batch`) | `move_counts` / `result_fens` | Populated from `processing_result["move_count"]` / `processing_result["result_fen"]` | Yes — derived from actual PGN parse | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `process_game_pgn` returns correct ply count | `uv run pytest tests/test_zobrist.py::test_process_game_pgn_returns_correct_ply_count -q` | 1 passed | PASS |
| `hashes_for_game` wrapper equivalence | `uv run pytest tests/test_zobrist.py::test_hashes_for_game_wrapper_matches_process_game_pgn -q` | 1 passed | PASS |
| Full test suite | `uv run pytest -x -q` | 485 passed, 0 failed | PASS |
| Type checker | `uv run ty check app/ tests/` | All checks passed | PASS |
| Lint | `uv run ruff check app/services/import_service.py app/services/zobrist.py` | All checks passed | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| IMP-01 | 41.1-01 | Unified PGN processing function — single mainline walk per game replacing triple PGN parse | SATISFIED | `process_game_pgn` in `zobrist.py` lines 125-242; verified by 12 new tests |
| IMP-02 | 41.1-01 | Eliminate redundant data extraction — move_count and result_fen derived from unified function, no third PGN parse | SATISFIED | `GameProcessingResult` contains `move_count` and `result_fen`; `hashes_for_game` wrapper no longer parses PGN independently |
| IMP-03 | 41.1-02 | Eliminate redundant DB round-trip — use in-memory platform_game_id lookup instead of SELECT Game.pgn | SATISFIED | `pgn_by_platform_id` dict (line 384) + `select(Game.id, Game.platform_game_id)` (line 401); no `Game.pgn` column queried |
| IMP-04 | 41.1-02 | Bulk UPDATE for move_count/result_fen — single CASE expression query per batch instead of per-game UPDATEs | SATISFIED | `case(move_counts, value=Game.id)` at line 462; single `session.execute(update(...))` for entire batch |
| IMP-05 | 41.1-02 | Increase batch size from 10 to 28 — fewer DB commits per import run | SATISFIED | `_BATCH_SIZE = 28` at line 37 with explanatory comment |

No orphaned requirements — all five IMP-01 through IMP-05 were claimed by plans and verified implemented.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No TODOs, FIXMEs, empty return stubs, hardcoded empty data, or placeholder comments found in modified files. The only suppression comments (`# type: ignore[union-attr]`) are at lines 389 and 392 of `import_service.py` — these are intentional and documented as dict fallbacks for test mocks, not stubs blocking goal achievement.

### Human Verification Required

None. All truths are verifiable through code inspection and automated tests. The phase produces no UI changes and no external service integrations that require manual testing. The throughput improvement (~2x) is a production measurement that cannot be validated in CI, but the structural changes eliminating triple parsing, redundant SELECT, and per-game UPDATE are fully verified by code inspection and test coverage.

### Gaps Summary

No gaps. All 9 must-have truths verified, all 4 artifacts substantive and wired, all 5 key links confirmed, all 5 requirements satisfied. 485 tests pass with zero regressions. Type checker passes with zero errors.

---

_Verified: 2026-04-03_
_Verifier: Claude (gsd-verifier)_
