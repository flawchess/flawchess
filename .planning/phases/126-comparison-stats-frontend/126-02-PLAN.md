---
phase: 126-comparison-stats-frontend
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/lib/tacticComparisonMeta.ts
  - frontend/src/lib/tacticMotifDefinitions.ts
  - frontend/src/types/library.ts
  - frontend/src/components/library/TacticMotifChip.tsx
  - frontend/src/components/library/FlawCard.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/filters/FilterPanel.tsx
  - frontend/src/components/library/__tests__/TacticMotifChip.test.tsx
autonomous: true
requirements: [TACUI-01, TACUI-03, TACCMP-03]
must_haves:
  truths:
    - "Each Library flaw card shows its allowed motif as a family-colored chip with a definition popover, only for beta users (D-01b — this phase establishes the frontend beta-gate pattern; default beta_enabled=false guests/non-beta users see no tactic UI)"
    - "The chip renders on both the Flaws-subtab list rows and the single-game flaw card (D-10)"
    - "Low-confidence motifs render no chip (backend already nulls them below MIN_TACTIC_CHIP_CONFIDENCE)"
    - "A beta-gated multi-select Tactic-motif family filter lives in the existing FilterPanel (desktop + mobile drawer) and flows through the same filter plumbing"
    - "Family colors live in theme.ts as TAC_* constants, not hardcoded in components"
    - "Filter and grid share ONE family taxonomy (the six families in tacticComparisonMeta.ts)"
    - "All new interactive elements have data-testid + ARIA and render at 375px"
  prohibitions:
    - "Tactic chip / filter section MUST NOT render for non-beta users (user.beta_enabled === false)"
    - "Family colors MUST NOT be hardcoded in components — only imported from theme.ts"
    - "The filter MUST NOT introduce a parallel motif-grouping taxonomy distinct from tacticComparisonMeta.ts"
    - "No text below text-sm except the documented chip/popover text-xs exception"
  artifacts:
    - path: "frontend/src/lib/theme.ts"
      provides: "Six TAC_* / TAC_*_BG family color constant pairs"
      contains: "TAC_FORK"
    - path: "frontend/src/lib/tacticComparisonMeta.ts"
      provides: "TacticFamily type, TACTIC_COMPARISON_FAMILIES, TACTIC_FAMILY_COLORS, TACTIC_FAMILY_ICON, TACTIC_FAMILY_FOR_MOTIF, isTacticDeltaSignificant, tacticDeltaZoneColor"
      contains: "TACTIC_COMPARISON_FAMILIES"
    - path: "frontend/src/components/library/TacticMotifChip.tsx"
      provides: "Family-colored motif chip with definition popover"
      contains: "chip-tactic"
    - path: "frontend/src/components/filters/FilterPanel.tsx"
      provides: "Beta-gated tactic-motif multi-select filter section"
      contains: "filter-tactic-motif"
  key_links:
    - from: "frontend/src/components/library/FlawCard.tsx"
      to: "frontend/src/components/library/TacticMotifChip.tsx"
      via: "renders TacticMotifChip when beta + tactic_motif present"
      pattern: "TacticMotifChip"
    - from: "frontend/src/lib/tacticComparisonMeta.ts"
      to: "frontend/src/lib/theme.ts"
      via: "imports TAC_* color constants"
      pattern: "from '@/lib/theme'"
    - from: "frontend/src/components/filters/FilterPanel.tsx"
      to: "frontend/src/lib/tacticComparisonMeta.ts"
      via: "iterates TACTIC_COMPARISON_FAMILIES for the filter toggles"
      pattern: "TACTIC_COMPARISON_FAMILIES"
---

<objective>
Frontend chip + filter for Phase 126. Adds the six tactic-family color constants to theme.ts, a
shared family-taxonomy meta module (the SINGLE taxonomy both the chip, the filter, and the grid in
plan 03 consume), per-motif definitions, the `TacticMotifChip` component (modeled on `TagChip`),
its placement on both flaw-card surfaces (D-10), and a beta-gated multi-select "Tactic motif" family
filter in the existing FilterPanel (desktop + mobile drawer). All tactic UI is beta-gated frontend-only
(D-01).

Purpose: surface the allowed motif on flaw cards (TACUI-01) and let beta users filter the flaw list by
motif family (D-05/D-06), reusing the shipped TagChip + ToggleGroup-filter patterns verbatim.
Output: theme constants, two new lib modules, one chip component + two render sites, one filter section,
and a chip test suite. Mobile parity at 375px (TACUI-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-comparison-stats-frontend/126-CONTEXT.md
@.planning/phases/126-comparison-stats-frontend/126-PATTERNS.md
@.planning/phases/126-comparison-stats-frontend/126-UI-SPEC.md
@.planning/phases/126-comparison-stats-frontend/126-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- Six `TAC_*` / `TAC_*_BG` color constant pairs in `frontend/src/lib/theme.ts`
- `frontend/src/lib/tacticComparisonMeta.ts` — `TacticFamily` type, `TACTIC_COMPARISON_FAMILIES`, `TACTIC_FAMILY_COLORS`, `TACTIC_FAMILY_ICON`, `TACTIC_FAMILY_FOR_MOTIF`, `isTacticDeltaSignificant`, `tacticDeltaZoneColor`
- `frontend/src/lib/tacticMotifDefinitions.ts` — `TACTIC_MOTIF_DEFINITIONS` Record
- `frontend/src/components/library/TacticMotifChip.tsx`
- `tacticFamilies` field on `FilterState` + the `'tacticMotif'` FilterField + the filter section
- `tactic_motif`/`tactic_confidence` on the frontend `FlawListItem`/`FlawMarker` types
(The grid, api client, and hook are produced in plan 126-03, which depends on this plan's meta module.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add TAC_* theme constants + tacticComparisonMeta + tacticMotifDefinitions + frontend type fields</name>
  <files>frontend/src/lib/theme.ts, frontend/src/lib/tacticComparisonMeta.ts, frontend/src/lib/tacticMotifDefinitions.ts, frontend/src/types/library.ts</files>
  <read_first>
    - frontend/src/lib/theme.ts lines 62–82 — the existing `FAM_*` / `FAM_*_BG` color-constant block (exact pattern to copy)
    - frontend/src/lib/flawComparisonMeta.ts — the complete analog for tacticComparisonMeta (family type, *_COLORS Record, family-def array, isFlawDeltaSignificant + flawDeltaZoneColor)
    - frontend/src/lib/tagDefinitions.ts — analog for tacticMotifDefinitions (a Record<string,string>)
    - frontend/src/types/library.ts lines 51, 101–104, 196–203 — FlawListItem / FlawMarker / GameFlawCard types where tactic fields are added
    - app/services/tactic_detector.py lines 92–117 — the 24 TacticMotif Literal strings (the keys for TACTIC_MOTIF_DEFINITIONS and the motifs[] arrays)
    - .planning/phases/126-comparison-stats-frontend/126-UI-SPEC.md "Tactic Motif Family Colors" + "Family Taxonomy" tables — exact oklch values, family keys, display names, lucide icons
  </read_first>
  <action>
    THEME: Append six TAC_* / TAC_*_BG constant pairs to `frontend/src/lib/theme.ts` with the exact oklch values
    from the UI-SPEC color table (TAC_FORK hue 40, TAC_PIN_SKEWER hue 240, TAC_DISCOVERY hue 130, TAC_MATE hue 10,
    TAC_HANGING hue 80, TAC_COMBINATIONS hue 300; each with a `/ 0.15` _BG variant), copying the FAM_* export style.

    META: Create `frontend/src/lib/tacticComparisonMeta.ts` cloning `flawComparisonMeta.ts`: export
    `type TacticFamily = 'fork'|'pin_skewer'|'discovery'|'mate'|'hanging'|'combinations'`; `TACTIC_FAMILY_COLORS:
    Record<TacticFamily, {color;bg}>` wired to the TAC_* imports; `TACTIC_COMPARISON_FAMILIES: TacticFamilyDef[]`
    with {name, family, motifs[]} for all six families using the EXACT motif strings + display names from the
    UI-SPEC taxonomy (must equal the backend D-08 grouping); a `TACTIC_FAMILY_ICON: Record<TacticFamily,
    ComponentType>` using lucide icons (GitFork/Minus/Zap/Crown/AlertTriangle/Swords per UI-SPEC); a derived
    `TACTIC_FAMILY_FOR_MOTIF: Record<string, TacticFamily>` (flatten TACTIC_COMPARISON_FAMILIES); and the stat
    helpers `isTacticDeltaSignificant` + `tacticDeltaZoneColor` (copy from analog, rename, keep invertColors=true
    convention: positive delta = more = bad). Import `TacticBullet` type from '@/types/library'.

    DEFINITIONS: Create `frontend/src/lib/tacticMotifDefinitions.ts` exporting `TACTIC_MOTIF_DEFINITIONS:
    Record<string,string>` with one plain one-sentence definition per of the 24 motif strings (no jargon, no
    em-dashes per CLAUDE.md, cover WHAT the motif is). Keys must match the TacticMotif Literal strings exactly.

    TYPES: Add `tactic_motif: string | null` and `tactic_confidence: number | null` to the frontend `FlawListItem`,
    `FlawMarker`, and (if it carries chips) `GameFlawCard` interfaces in `frontend/src/types/library.ts`, matching
    the backend schema additions from plan 01. Add a `TacticBullet` interface here too (mirrors plan-01
    `TacticBullet` schema) — the grid in plan 03 consumes it, and the meta module imports it.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'TAC_FORK\b' frontend/src/lib/theme.ts` >= 1 and all six TAC_* + six TAC_*_BG constants are exported
    - Source assertion: `TACTIC_COMPARISON_FAMILIES` in tacticComparisonMeta.ts contains exactly six families whose `motifs` arrays collectively contain all 24 TacticMotif strings exactly once
    - Source assertion: `TACTIC_MOTIF_DEFINITIONS` has a non-empty string for each of the 24 motif keys
    - Source assertion: `FlawListItem` and `FlawMarker` in frontend types declare `tactic_motif: string | null` and `tactic_confidence: number | null`
    - Source assertion: no oklch literal for a TAC_ hue appears inside any `.tsx` component (colors come from theme.ts only) — `grep -rl "oklch(0.72 0.18 40" frontend/src/components` returns nothing
    - `npm run knip` reports no new dead exports for these modules (they are imported by later tasks)
  </acceptance_criteria>
  <done>Theme constants, the shared family taxonomy meta, per-motif definitions, and frontend tactic type fields exist and typecheck; the taxonomy matches the backend exactly.</done>
</task>

<task type="auto">
  <name>Task 2: TacticMotifChip component + render on FlawCard and LibraryGameCard (beta-gated, D-10)</name>
  <files>frontend/src/components/library/TacticMotifChip.tsx, frontend/src/components/library/FlawCard.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/__tests__/TacticMotifChip.test.tsx</files>
  <read_first>
    - frontend/src/components/library/TagChip.tsx (complete file) — the exact chip + popover + useIsMobile + hover-timing pattern to clone; note lines 16–29 (useIsMobile), line 36 (HIGHLIGHT_BG), 125–135 (scheduleOpen/close), 148–207 (chip span)
    - frontend/src/components/library/FlawCard.tsx lines 249–272 — the tags row where TagChip + TagLegend render (chip insertion point); note `flaw.tags`, `flaw.game_id`
    - frontend/src/components/results/LibraryGameCard.tsx — the second TagChip render site (single-game / games-card chips)
    - frontend/src/hooks/useAuth.ts — `useAuth()` returns `{ user }`; `user.beta_enabled` is the gate
    - frontend/src/lib/tacticComparisonMeta.ts (from task 1) + frontend/src/lib/tacticMotifDefinitions.ts
    - frontend/src/components/library/__tests__/TagChip.test.tsx — the test harness pattern to clone
    - .planning/phases/126-comparison-stats-frontend/126-UI-SPEC.md "TACUI-01" + "Motif Chip" interaction contract + data-testid table
  </read_first>
  <action>
    Create `TacticMotifChip.tsx` cloning `TagChip.tsx` but purely informational (no onHover/onActivate/filter-ring
    subscription, D-10). Props: `motif: string`, `flawId: number` (and optionally `confidence` — the backend
    already nulls sub-threshold motifs, so the caller renders the chip only when motif != null; do not re-gate on a
    magic number in the component). Resolve family via `TACTIC_FAMILY_FOR_MOTIF[motif]`, colors via
    `TACTIC_FAMILY_COLORS[family]`, icon via `TACTIC_FAMILY_ICON[family]`. Chip span: family-colored border+bg,
    `rounded-full px-2 py-0.5 text-xs font-bold`, `role="button"`, `tabIndex={0}`, `aria-label={`Tactic: ${motif} —
    ${TACTIC_MOTIF_DEFINITIONS[motif]}`}`, `data-testid={`chip-tactic-${motif}-${flawId}`}`. Popover body:
    `<span className="font-bold">{motif}</span>: {TACTIC_MOTIF_DEFINITIONS[motif]}`, `data-testid={`tag-popover-tactic-${motif}-${flawId}`}`,
    side top desktop / bottom mobile via the cloned useIsMobile + hover-timing (100ms open / 80ms close grace),
    Enter/Space toggles. Reuse `text-xs` (documented chip exception).

    RENDER: In `FlawCard.tsx`, inside the tags row (after the TagChip block ~line 262–271), render
    `{user?.beta_enabled && flaw.tactic_motif && <TacticMotifChip motif={flaw.tactic_motif} flawId={flaw.game_id} />}`
    — import `useAuth`. Apply the SAME addition at the corresponding TagChip site in `LibraryGameCard.tsx` (D-10 —
    both flaw-list rows and single-game card). Search both files for every TagChip render to ensure parity; do not
    leave one surface without the chip.

    TEST: Clone `TagChip.test.tsx` into `TacticMotifChip.test.tsx` covering: renders motif text + family color, has
    the chip data-testid, popover shows the definition on open, aria-label is present, keyboard toggle works.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run TacticMotifChip</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `TacticMotifChip.tsx` contains `data-testid={`chip-tactic-${motif}-${flawId}`}` and `aria-label` referencing the motif + definition
    - Source assertion: both `FlawCard.tsx` and `LibraryGameCard.tsx` render `<TacticMotifChip` guarded by `user?.beta_enabled && ...tactic_motif`
    - Behavior assertion: the chip test renders the motif label, opens the popover showing the definition, and asserts the chip data-testid
    - Prohibition check: `grep -rn 'TacticMotifChip' frontend/src/components | grep -v beta_enabled` shows no render site that omits the beta gate (manual confirm both call sites are gated)
    - `cd frontend && npm run lint && npx tsc --noEmit` clean; `npm run knip` clean
  </acceptance_criteria>
  <done>The motif chip renders family-colored with a definition popover on both flaw surfaces, only for beta users, and is test-covered.</done>
</task>

<task type="auto">
  <name>Task 3: Beta-gated Tactic-motif multi-select filter in FilterPanel (desktop + mobile drawer)</name>
  <files>frontend/src/components/filters/FilterPanel.tsx</files>
  <read_first>
    - frontend/src/components/filters/FilterPanel.tsx — `FilterState` interface (line 41), `FilterField` union (line 139), `ALL_FILTERS` (line 161), `visibleFilters`/`show()` (lines 145, 180, 260), and the Time Control multi-select toggle-group section (lines 228–242, the `toggleTimeControl`/`isTimeControlActive` analog)
    - frontend/src/lib/tacticComparisonMeta.ts (task 1) — `TacticFamily`, `TACTIC_COMPARISON_FAMILIES`, `TACTIC_FAMILY_COLORS`
    - frontend/src/hooks/useAuth.ts — beta gate source
    - .planning/phases/126-comparison-stats-frontend/126-PATTERNS.md "FilterPanel.tsx" section (toggle/active helpers + render block) and 126-UI-SPEC.md "Motif Filter" + data-testid table
    - CLAUDE.md "Always apply changes to mobile too" — confirm whether FilterPanel renders one shared body or separate desktop/mobile drawer markup before editing
  </read_first>
  <action>
    Add `tacticFamilies: TacticFamily[] | null` to `FilterState` (null = all families = no filter). Add
    `'tacticMotif'` to the `FilterField` union and `ALL_FILTERS`. Add `toggleTacticFamily` + `isTacticFamilyActive`
    helpers mirroring `toggleTimeControl`/`isTimeControlActive` (per PATTERNS.md: all-selected collapses to null).
    Render a beta-gated section AFTER the existing filter sections: gated by `user?.beta_enabled && show('tacticMotif')`,
    label "Tactic motif" (`text-sm font-medium`), a `ToggleGroup type="multiple"` of `ToggleGroupItem`s iterating
    `TACTIC_COMPARISON_FAMILIES`, each family-colored from `TACTIC_FAMILY_COLORS[family]`, `data-testid={`filter-tactic-motif-${family}`}`,
    section container `data-testid="filter-tactic-motif"`. Import `useAuth`. If FilterPanel has separate desktop and
    mobile drawer render branches, apply the section to BOTH (CLAUDE.md mobile parity); if it is a single shared body,
    one addition suffices — confirm via read_first before editing. Thread `tacticFamilies` into the consumer that
    builds the library flaw-list query params so the new filter actually narrows the list (the param maps to the
    backend `tactic_families` query arg from plan 01) — locate the existing buildLibraryParams/filter-to-params site
    and add the field there.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `FilterState` declares `tacticFamilies: TacticFamily[] | null`; `FilterField` union and `ALL_FILTERS` include `'tacticMotif'`
    - Source assertion: FilterPanel contains `data-testid="filter-tactic-motif"` and per-family `filter-tactic-motif-${family}` testids, guarded by `user?.beta_enabled`
    - Source assertion: the filter section iterates `TACTIC_COMPARISON_FAMILIES` (shared taxonomy, no parallel grouping)
    - Mobile parity: if separate desktop/mobile markup exists, both carry the section (grep both branches); a comment or matching testid confirms parity
    - Param wiring: `tactic_families` is added wherever library flaw-list query params are built from FilterState
    - `cd frontend && npm run lint && npx tsc --noEmit` clean; `npm run knip` clean
  </acceptance_criteria>
  <done>A beta-gated, family-colored, multi-select Tactic-motif filter exists in the FilterPanel (desktop + mobile), uses the shared taxonomy, and threads through to the backend tactic_families param.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user.beta_enabled → tactic UI render | Frontend render-time gate decides whether tactic chip/filter mount |
| filter selection → backend query params | tacticFamilies family keys serialized to the /library/flaws + /tactic-comparison query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-05 | Information Disclosure (beta gate) | TacticMotifChip / filter render for non-beta user | accept | D-01 (locked): frontend-only hide. `user?.beta_enabled` guards every render site; a non-beta user cannot see the UI but could reach the data via direct API (documented residual, T-126-03). |
| T-126-06 | Tampering | family keys from filter toggle | mitigate | Family keys come from the fixed TACTIC_COMPARISON_FAMILIES enum; the backend maps unknown keys to no-op (plan-01 FAMILY_TO_MOTIF_INTS). No injection surface. |
| T-126-07 | Cross-site scripting | motif definition / motif string in chip + popover | mitigate | Definitions are static constants; motif strings come from the typed backend enum; rendered as React text nodes (auto-escaped), no dangerouslySetInnerHTML. |
| T-126-SC | Tampering | npm installs | mitigate | No new npm packages (reuses lucide-react + existing shadcn). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc --noEmit && npm test -- --run TacticMotifChip && npm run knip` all pass
- Chip renders on both FlawCard and LibraryGameCard, beta-gated
- Filter section is beta-gated, uses TACTIC_COMPARISON_FAMILIES, present in desktop + mobile
- No TAC_ oklch literals inside components (theme.ts only)
- Manual 375px check: chip wraps with other chips; filter toggle-group wraps multi-line (TACUI-03)
</verification>

<success_criteria>
- Flaw cards (list rows + single-game) show a family-colored motif chip with a definition popover, beta-only (TACUI-01)
- A beta-gated multi-select Tactic-motif family filter narrows the flaw list via the shared filter plumbing (D-05/D-06, TACCMP-03)
- Family colors are theme.ts constants; the filter, chip, and (plan 03) grid share one taxonomy
- Mobile parity at 375px with data-testid + ARIA (TACUI-03)
</success_criteria>

<output>
Create `.planning/phases/126-comparison-stats-frontend/126-02-SUMMARY.md` when done
</output>
