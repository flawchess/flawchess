---
phase: 126-comparison-stats-frontend
plan: 03
type: execute
wave: 3
depends_on: ["126-01", "126-02"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/hooks/useLibrary.ts
  - frontend/src/components/library/TacticComparisonGrid.tsx
  - frontend/src/components/library/FlawStatsPanel.tsx
  - frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx
autonomous: true
requirements: [TACUI-02, TACUI-03, TACCMP-02]
must_haves:
  truths:
    - "A you-vs-opponent tactic-motif comparison grid renders on the Library page for beta users"
    - "The grid reuses the MiniBulletChart pattern: delta bar + CI whiskers + benchmark zone where available"
    - "Each family row has a per-motif tooltip disclosing definition, sign convention, and confidence verdict"
    - "Below the section-level sample gate the grid shows the gate CTA, not partial data"
    - "The grid self-fetches via useTacticComparison and re-fetches on filter changes"
    - "isError renders the mandated LoadError copy; zero-event families render the muted placeholder"
    - "The grid renders single-column at 375px with data-testid + ARIA parity"
  prohibitions:
    - "The grid MUST NOT render for non-beta users (user.beta_enabled === false)"
    - "The grid MUST NOT compute or hardcode tactic benchmark zones — show a zone only when the API returns non-null bounds (no new benchmark pipeline)"
    - "The grid MUST NOT introduce a parallel family taxonomy — it consumes TACTIC_COMPARISON_FAMILIES"
  artifacts:
    - path: "frontend/src/components/library/TacticComparisonGrid.tsx"
      provides: "Beta-gated you-vs-opponent tactic comparison grid"
      contains: "tactic-comparison-grid"
    - path: "frontend/src/hooks/useLibrary.ts"
      provides: "useTacticComparison query hook"
      contains: "useTacticComparison"
    - path: "frontend/src/api/client.ts"
      provides: "getTacticComparison API fn"
      contains: "getTacticComparison"
  key_links:
    - from: "frontend/src/components/library/TacticComparisonGrid.tsx"
      to: "frontend/src/hooks/useLibrary.ts"
      via: "useTacticComparison self-fetch"
      pattern: "useTacticComparison"
    - from: "frontend/src/hooks/useLibrary.ts"
      to: "frontend/src/api/client.ts"
      via: "libraryApi.getTacticComparison"
      pattern: "getTacticComparison"
    - from: "frontend/src/components/library/FlawStatsPanel.tsx"
      to: "frontend/src/components/library/TacticComparisonGrid.tsx"
      via: "renders the grid in Zone 3"
      pattern: "TacticComparisonGrid"
---

<objective>
The you-vs-opponent tactic-motif comparison grid (TACUI-02) for Phase 126. Adds the API client fn,
the `useTacticComparison` hook, the `TacticComparisonGrid` component (cloned from `FlawComparisonGrid`),
its placement on the Library FlawStatsPanel, and a test suite. The grid is beta-gated, self-fetches,
shows up to six family rows ranked server-side, reuses MiniBulletChart (delta + CI + zone-where-available),
per-row tooltips, the section sample gate, the LoadError state, and renders single-column at 375px.

Purpose: surface the comparison data from plan 01 in the shipped v1.25 bullet-grid UX, degrading
gracefully where no tactic benchmark zone exists (out of scope to compute).
Output: one api fn, one hook, one component + placement, one test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-comparison-stats-frontend/126-CONTEXT.md
@.planning/phases/126-comparison-stats-frontend/126-PATTERNS.md
@.planning/phases/126-comparison-stats-frontend/126-UI-SPEC.md
@.planning/phases/126-comparison-stats-frontend/126-01-SUMMARY.md
@.planning/phases/126-comparison-stats-frontend/126-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `libraryApi.getTacticComparison` in `frontend/src/api/client.ts`
- `useTacticComparison` hook in `frontend/src/hooks/useLibrary.ts`
- `frontend/src/components/library/TacticComparisonGrid.tsx` (+ its `TacticBulletRow` sub-component and a row tooltip)
- Grid placement in `frontend/src/components/library/FlawStatsPanel.tsx` (Zone 3, after FlawComparisonGrid)
(The TacticComparisonResponse/TacticBullet types come from plans 01/02; the family taxonomy + stat helpers come from plan 02's tacticComparisonMeta.ts.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add getTacticComparison API fn + useTacticComparison hook</name>
  <files>frontend/src/api/client.ts, frontend/src/hooks/useLibrary.ts</files>
  <read_first>
    - frontend/src/api/client.ts lines 275–291 — `libraryApi.getFlawComparison` (exact analog incl. buildFilterParams + severity passthrough)
    - frontend/src/hooks/useLibrary.ts lines 110–121 — `useLibraryFlawComparison` (exact analog; note `buildLibraryParams`, `LIBRARY_STALE_TIME` already in-file)
    - frontend/src/components/filters/FilterPanel.tsx — `FilterState` incl. the new `tacticFamilies` field (plan 02)
    - frontend/src/lib/tacticComparisonMeta.ts — `TacticFamily` type
    - .planning/phases/126-comparison-stats-frontend/126-PATTERNS.md — api fn + hook skeletons
  </read_first>
  <action>
    API: Add `getTacticComparison` to `libraryApi` in `client.ts`, cloning `getFlawComparison`: same param shape
    plus `tactic_families?: string[]`, GET `/library/tactic-comparison`, spread `buildFilterParams(params)` +
    conditional severity + conditional tactic_families, return `TacticComparisonResponse` (import the type).

    HOOK: Add `useTacticComparison(filters, flawFilter, tacticFamilies)` to `useLibrary.ts`, cloning
    `useLibraryFlawComparison`: build params via the existing `buildLibraryParams(filters, flawFilter.severity,
    flawFilter.tags)`, queryKey `['library-tactic-comparison', params, tacticFamilies]`, queryFn calls
    `libraryApi.getTacticComparison({ ...params, tactic_families: tacticFamilies.length ? tacticFamilies :
    undefined })`, `staleTime: LIBRARY_STALE_TIME`, `refetchOnWindowFocus: false`. Do NOT add a manual
    Sentry.captureException — TanStack Query errors are captured globally (CLAUDE.md).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'getTacticComparison' frontend/src/api/client.ts` >= 1 and it GETs `'/library/tactic-comparison'`
    - Source assertion: `useTacticComparison` exists in `useLibrary.ts` with queryKey containing `'library-tactic-comparison'`
    - Prohibition: no `Sentry.captureException` added in the new hook (global handler covers it)
    - `cd frontend && npm run lint && npx tsc --noEmit` clean; `npm run knip` clean (hook consumed by the grid task)
  </acceptance_criteria>
  <done>The API fn and self-fetch hook exist, typed, mirroring the flaw-comparison analog, with no duplicate Sentry capture.</done>
</task>

<task type="auto">
  <name>Task 2: TacticComparisonGrid component + Library placement + tests</name>
  <files>frontend/src/components/library/TacticComparisonGrid.tsx, frontend/src/components/library/FlawStatsPanel.tsx, frontend/src/components/library/__tests__/TacticComparisonGrid.test.tsx</files>
  <read_first>
    - frontend/src/components/library/FlawComparisonGrid.tsx (complete file) — the exact grid analog: imports (lines 27–42), FlawBulletRow (lines 44–107), signedDelta helper, state ordering (loading→error→!data→below_gate→grid), Card-per-family layout, the MiniBulletChart props (value/neutralMin/neutralMax/center/ciLow/ciHigh/invertColors/barColor)
    - frontend/src/components/charts/MiniBulletChart.tsx — props + how neutralMin/Max collapse when equal (for the no-zone degradation)
    - frontend/src/components/popovers/FlawBulletPopover.tsx — the row tooltip analog (clone for the tactic row tooltip)
    - frontend/src/components/library/FlawStatsPanel.tsx lines 80–98 — Zone 3 where FlawComparisonGrid mounts (the placement site; pass `filters` + `flawFilter`)
    - frontend/src/hooks/useAuth.ts — beta gate
    - frontend/src/lib/tacticComparisonMeta.ts (plan 02) — TACTIC_COMPARISON_FAMILIES / TACTIC_FAMILY_COLORS / TACTIC_FAMILY_ICON / isTacticDeltaSignificant / tacticDeltaZoneColor
    - frontend/src/components/library/__tests__/FlawComparisonGrid.test.tsx — the test harness (below-gate CTA, full grid, zero-event, error, loading) to clone
    - .planning/phases/126-comparison-stats-frontend/126-UI-SPEC.md "TACUI-02" + copywriting contract (section heading "Tactic Motifs", sub-heading, gate CTA copy, tooltip format) + data-testid table
    - .planning/phases/126-comparison-stats-frontend/126-PATTERNS.md "TacticComparisonGrid.tsx" section (TacticBulletRow skeleton, beta gate, zone degradation)
  </read_first>
  <action>
    Create `TacticComparisonGrid.tsx` cloning `FlawComparisonGrid.tsx`. Beta gate FIRST: `const { user } =
    useAuth(); if (!user?.beta_enabled) return null;`. Self-fetch via `useTacticComparison(filters, flawFilter,
    filters.tacticFamilies ?? [])`. State order: isLoading → skeleton (`data-testid="tactic-comparison-loading"`,
    `aria-label="Loading tactic comparison"`); isError → `<LoadError resource="tactic comparison" />`; !data → null;
    `data.below_gate` → gate CTA (`data-testid="tactic-comparison-gate-cta"`, copy from UI-SPEC: "{analyzed_n} of
    {analyzed_gate} analyzed games needed" + the Lichess server-analysis body); else grid
    (`data-testid="tactic-comparison-grid"`). Section heading "Tactic Motifs", sub-heading "You vs. your opponents —
    flaws allowed per game" (no em-dash — use the UI-SPEC copy verbatim). Iterate the API-returned bullets (server
    already ranked + capped at 6) — DO NOT re-sort; render one Card per bullet keyed/testid'd by `bullet.family`
    (`data-testid={`tactic-family-card-${bullet.family}`}`, header `tactic-family-header-${family}`). Per-row
    `TacticBulletRow`: family icon + label + signed delta (zone-tinted only when `isTacticDeltaSignificant`) +
    tooltip trigger (`tactic-bullet-popover-${family}`); zero-event (`delta === null`) → muted italic "No events in
    current filter" (`text-sm`); else `<MiniBulletChart value={bullet.delta ?? 0} center={0} invertColors
    barColor="neutral" neutralMin={bullet.has_zone ? bullet.zone_lo : 0} neutralMax={bullet.has_zone ? bullet.zone_hi
    : 0} ciLow={bullet.ci_low ?? undefined} ciHigh={bullet.ci_high ?? undefined} />` — when `!has_zone` the equal
    bounds collapse the band gracefully (no "no benchmark" label). Row tooltip body per UI-SPEC: family label + "You:
    {you_rate:.2f} per game | Opponents: {opp_rate:.2f} per game" + sign-convention sentence + a confidence sentence
    ("Result is statistically notable / within normal variation") — no p-values/Wilson/CI jargon in copy.

    PLACEMENT: In `FlawStatsPanel.tsx` Zone 3, render `<TacticComparisonGrid filters={filters} flawFilter={flawFilter}
    />` directly after the existing `<FlawComparisonGrid ... />` (the grid self-gates on beta, so no extra guard
    needed at the call site). Use a `mt-6` wrapper matching the existing zone spacing.

    TEST: Clone `FlawComparisonGrid.test.tsx` into `TacticComparisonGrid.test.tsx`: below-gate CTA, full grid (6
    family cards from a mocked ranked response), zero-event muted row, error LoadError, loading skeleton, and a
    non-beta user → renders null.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run TacticComparisonGrid</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `TacticComparisonGrid.tsx` contains `if (!user?.beta_enabled) return null;`, `data-testid="tactic-comparison-grid"`, `tactic-comparison-gate-cta`, `tactic-comparison-loading`, and per-family `tactic-family-card-${...}`
    - Source assertion: it iterates the API bullets without re-sorting (no `.sort(` on the bullets array) and consumes `TACTIC_COMPARISON_FAMILIES`/meta (no parallel taxonomy)
    - Source assertion: when `!bullet.has_zone`, neutralMin/neutralMax are passed as 0/0 (zone collapses; no hardcoded benchmark)
    - Source assertion: `FlawStatsPanel.tsx` renders `<TacticComparisonGrid` in Zone 3
    - Behavior assertion: tests cover below-gate CTA, full 6-card grid, zero-event row, error state, loading, AND non-beta → null
    - Mobile parity: grid uses `grid-cols-1 lg:grid-cols-3` (single column at 375px); a manual 375px note in the SUMMARY
    - `cd frontend && npm run lint && npx tsc --noEmit && npm run knip` clean
  </acceptance_criteria>
  <done>The beta-gated comparison grid renders the server-ranked family bullets with CI + zone-where-available + tooltips + sample gate + LoadError, is placed in the Library, single-column at 375px, and is test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user.beta_enabled → grid render | Frontend gate decides whether the grid mounts and fetches |
| API response bounds → MiniBulletChart | zone_lo/zone_hi/has_zone from the server drive the band; null bounds must degrade safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-08 | Information Disclosure (beta gate) | TacticComparisonGrid for non-beta user | accept | D-01 (locked): frontend-only hide via `user?.beta_enabled` (self-gates before fetch). Residual API-reachability documented in plan 01 T-126-03. |
| T-126-09 | Denial of Service / overdraw | grid fetch on every filter change | mitigate | Hook reuses LIBRARY_STALE_TIME + refetchOnWindowFocus:false (same as flaw-comparison); below-gate short-circuits server-side; grid capped at 6 rows. |
| T-126-10 | Tampering | malformed/missing zone bounds in response | mitigate | `has_zone` discriminates; when false, neutralMin/Max collapse to 0/0 — no crash, no fabricated band. Zero-event delta=null renders the muted placeholder, never NaN. |
| T-126-SC | Tampering | npm installs | mitigate | No new npm packages. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc --noEmit && npm test -- --run TacticComparisonGrid && npm run knip` all pass
- Grid is beta-gated (non-beta → null), self-fetches, handles loading/error/below_gate/zero-event
- No tactic benchmark computed; zone shown only where has_zone is true
- Grid consumes the shared TACTIC_COMPARISON_FAMILIES taxonomy
- 375px single-column layout (TACUI-03)
</verification>

<success_criteria>
- A you-vs-opponent tactic-motif comparison grid (MiniBulletChart: delta + CI + zone-where-available, per-row tooltips) renders on the Library page with a section sample gate, beta-only (TACUI-02, TACCMP-02)
- Graceful zone degradation where no tactic benchmark exists (no new pipeline)
- Mobile parity at 375px with data-testid + ARIA (TACUI-03)
</success_criteria>

<output>
Create `.planning/phases/126-comparison-stats-frontend/126-03-SUMMARY.md` when done
</output>
