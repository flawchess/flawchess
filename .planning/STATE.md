---
gsd_state_version: 1.0
milestone: v1.22
milestone_name: milestone
status: executing
last_updated: "2026-06-01T18:30:00Z"
last_activity: 2026-06-01 -- Phase 102 plan 03 UAT scaffold built; blocked at human-verify checkpoint
progress:
  total_phases: 5
  completed_phases: 0
  total_plans: 3
  completed_plans: 2
  percent: 0
---

# Project State: FlawChess

## Current Position

Phase: 102 (endgame-llm-statistical-reasoning-rework-v1-23) — EXECUTING
Plan: 3 of 3
Status: Blocked at human-verify checkpoint (102-03 Task 2 — HUMAN-UAT)
Next: Human runs UAT, replies "approved" or lists failures. On approval: complete 102-03-SUMMARY.md, squash-merge to main, start v1.23 milestone.
Last activity: 2026-06-01 -- Phase 102 plan 03 UAT scaffold built (60288f60); blocked at human-verify checkpoint

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-31 after v1.21 milestone)
Core value: Position-precise WDL across openings + endgames + time pressure on top of users' actual chess.com / lichess games, with personalized LLM commentary on endgame performance and an auto-generated opening-strengths/weaknesses report.
Current focus: v1.22 Maintenance — Test Isolation & Frontend Major Upgrades (Phases 100, 101, sequential). Phase 100 (SEED-031): per-run/per-xdist-worker test DB cloned from a migrated template, retiring the session-start `TRUNCATE … CASCADE` lock to unblock concurrent agent runs + `pytest -n auto`. Phase 101 (SEED-032): bump the 11 frontend deps a major behind, one cluster at a time low→high risk (lucide → Vite 8 → jsdom 29 → eslint 10 → typescript 6 → recharts 3), recharts gets visual UAT. Backend has no outstanding major bumps. LLM Statistical Reasoning (LLM-01..07) deprioritized to backlog Phase 999.7. v1.21 just shipped: per-TC Endgame Metrics cards (Phase 97), collapsible per-TC Endgame Type Breakdown cards (Phase 98), per-TC Conv/Parity/Recov percentile chips (Phase 99), and cohort-CDF demoted to a DB table (Phase 99.1). Prod backfill of the 12 new rate-percentile metrics is deferred to deploy.

## Milestone Progress

Twenty-two milestones complete (v1.0–v1.21). v1.21 Time-Control-Aware Endgame Metrics shipped 2026-05-31 — 4 phases (97, 98, 99, 99.1), 15 plans, PRs #160/#163/#164/#167/#168; archived to milestones/v1.21-ROADMAP.md, tagged v1.21. v1.20 Import Pipeline Hardening Follow-Up and Readiness shipped 2026-05-29 — 2 phases (95 asyncpg COPY, 96 Import Readiness Gate), 5 plans, PRs #148/#149/#151; regrouped post-hoc from standalone phases on 2026-05-30, tagged at `dcd22fef`. v1.19 Endgame Percentiles shipped 2026-05-27 — 6 phases (93, 94, 94.1, 94.2, 94.3, 94.4), ~45 plans, delivered via PR #145 + earlier per-phase PRs. 26/26 PCTL/TPCTL/PRPCR requirements satisfied. v1.18 Import Pipeline Hardening shipped 2026-05-22 (3 phases, 17 plans, PRs #130/#137/#138 + hotfix #139 + container-budget hotfix #144). v1.17 Endgame Stats Card Redesign shipped 2026-05-19 — 13 phases (84, 85, 85.1, 86, 87, 87.1, 87.2, 87.4, 87.5, 87.6, 88, 88.3, 88.4), ~54 plans, 203 commits over 8 days, delivered via PRs #89–#117 (plus production-branch OOM hotfix #99/#100/#101). the Polish phase (#89) dropped from scope at close; the percentile-composite phase (#87.3) superseded by 87.4→87.6. v1.16 Stockfish Eval Analyses shipped 2026-05-11 (5 phases, 24 plans, PRs #80/#82/#85/#86/#88).

## Key Context

- Stack: FastAPI + React/TS/Vite + PostgreSQL + python-chess
- ORM: SQLAlchemy 2.x async + Alembic
- Auth: FastAPI-Users 15.0.5 (Bearer JWT, Google SSO, guest sessions, admin impersonation)
- Core algorithm: Zobrist hashes (white_hash, black_hash, full_hash) precomputed at import
- Deployment: Docker Compose on Hetzner CX32 (4 vCPUs, 7.6 GB RAM + 2 GB swap)
- v1.11 LLM stack: pydantic-ai Agent with env-var-driven model selection (`PYDANTIC_AI_MODEL_INSIGHTS`), `genai-prices` for cost accounting, generic `llm_logs` Postgres table
- v1.12 Benchmark DB: separate PostgreSQL 18 instance on port 5433 — used by v1.15 as the first backfill target before prod; SEED-002 / SEED-006 still pending full ingest
- v1.14 score plumbing: trinomial Wald 95% half-width drives `confidence: "low" | "medium" | "high"`; `OpeningInsightFinding` and `NextMoveEntry` payloads expose `score`, `confidence`, `p_value`. `compute_confidence_bucket` is the single shared helper (CI structural assertion). `arrowColor.ts` and backend constants are kept in lock-step by `tests/services/test_opening_insights_arrow_consistency.py`.
- v1.15 outputs (post-cutover, on benchmark + prod): every `game_positions` row carries a `phase` SmallInteger (0=opening, 1=middlegame, 2=endgame) computed from existing `piece_count` + `backrank_sparse` + `mixedness` via Python port of lichess Divider.scala. Endgame span-entry rows (`MIN(ply)` per `(game_id, endgame_class)` with `count(ply) ≥ ENDGAME_PLY_THRESHOLD`) and middlegame entry rows (`MIN(ply)` per game where `phase = 1`) carry Stockfish eval at depth 15 in `eval_cp` / `eval_mate`. Endgame classification thresholds directly on eval (±100 cp, color-flipped to user perspective) — proxy code path removed entirely.
- v1.15 source report: `reports/conv-recov-validation-2026-05-02.md` (historical baseline that motivated the cutover — proxy held at ~81.5% agreement vs Stockfish, missed ~24% of substantive material-edge sequences). Post-cutover comparison is moot: proxy is gone (REFAC-05), so `/conv-recov-validation` Skill was deleted on 2026-05-03 and PHASE-VAL-01 / VAL-01 marked rescinded.
- v1.15 deployed code: `app/services/engine.py` (async Stockfish wrapper, lifespan-managed); `scripts/backfill_eval.py` (idempotent + resumable backfill driver, parallelised via `EnginePool` per quick-task `260503-0t8`); `_classify_endgame_bucket(eval_cp, eval_mate, user_color)` in endgame service; alembic heads `1efcc66a7695` (phase column) + `c92af8282d1a` (ix_gp_user_endgame_game reshape).

## Accumulated Context

### Deferred Items

Acknowledged and deferred at v1.22 milestone close on 2026-05-31 (open-artifact audit: 13 items — all dormant seeds + long-range todos, none v1.22-scoped):

| Category | Item | Status |
|----------|------|--------|
| quick_task | 260531-jga-update-all-backend-and-frontend-dependencies | Resolved in spirit — superseded by Phase 101 (the SEED-032 capture this task seeded); frontmatter status not backfilled |
| todos | 2026-03-11-bitboard-storage, 2026-04-26-phase-70-roadmap-amendments, 2026-04-30-benchmark-rebuild-per-tc-selection, 2026-04-30-benchmark-skill-v2-build, 2026-05-03-rerun-benchmarks-equal-footing (+5 more incl. phase prod-backfills, recovery-score-gap-popover-copy, pt-33-tailwind-axis-label) | Carried forward — long-range ideas + per-phase backfill tasks; not v1.22-scoped |
| seeds | SEED-010 (library page), SEED-012 (client-side Stockfish tactics), SEED-029 (benchmarks deterministic rebuild), SEED-030 (structural code-health Track A), SEED-033 (cap explorer + partial-index Zobrist hashes) | Dormant — genuine future-work backlog |
| seeds | SEED-031 (isolated test DB), SEED-032 (frontend major dep upgrades) | DELIVERED by Phases 100 / 101 this milestone — audit still lists them dormant (known gsd-sdk staleness); not actually open |

Acknowledged at v1.21 milestone close on 2026-05-31 (open-artifact audit: 10 genuine backlog items; the historical "172 quick-task" noise was a stale `gsd-sdk` filename-match bug and was cleared, not deferred — see commit c4641db4):

| Category | Item | Status |
|----------|------|--------|
| todos | 10 pending todos (bitboard storage, benchmark-rebuild-per-tc-selection, benchmark-skill-v2-build, rerun-benchmarks-equal-footing, recovery-score-gap-popover-copy, pt-33-tailwind-axis-label, phase-94.2/94.4/99 prod-backfills, phase-70-roadmap-amendments) | Carried forward as live backlog — long-range ideas + per-phase prod-backfill tasks; not v1.21-scoped. Note: `pt-33-tailwind-axis-label` is likely moot (pt-33 is valid in Tailwind v4) |
| todos | 2026-05-31-phase-99-prod-backfill-rate-percentiles | Open — backfill the 12 new rate-percentile metrics on prod at next deploy (D-11 sign-off) |
| seeds | SEED-010 (library page), SEED-012 (client-side Stockfish tactics), SEED-031 (isolated test DB per run) | Dormant — genuine future-work backlog, not noise |
| seeds | SEED-029 (benchmarks deterministic rebuild) | Open — endgame generator ported in PR #166; faithful-port Phase A + methodology phase remain |
| seeds | SEED-030 (structural code-health) | Open (Track A) — module-splitting remains; Track B (CDF demotion) closed by Phase 99.1 |
| seeds | SEED-032 (frontend major dependency upgrades) | Open — 11 frontend deps a major behind (recharts 3, vite 8, typescript 6, eslint 10, jsdom 29, lucide 1, …); backend has none. Recommended structure: one phase, sequential plan-per-cluster (not a milestone). Captured from quick task 260531-jga. Research typescript-eslint↔TS6/eslint10 compat before planning |
| resolved | 13 done quick tasks (unmarked), quick-260519-ni3, Phases 97/98/99 verification+UAT frontmatter, import-job-db-conn-closed debug session, remove-recency todo | NOT deferred — resolved/archived at this close (commit c4641db4) |

Acknowledged at v1.17 milestone close on 2026-05-19:

| Category | Item | Status |
|----------|------|--------|
| phase | Polish phase #89 (popovers, gating decisions, automation rules, 375px parity) | Dropped from scope — polish absorbed incrementally across inserted-phase UAT cycles |
| phase | Phase 87.3 (Endgame Skill v2 percentile composite) | Superseded by 87.4→87.6 — percentile design retracted at UAT (PR #102) |
| uat | Phase 88.3 / 88.4 in-browser visual UAT (4 + 3 scenarios) | Deferred — automated gates green (569 frontend tests, knip/tsc clean); not blocking close |
| debug | import-job-db-conn-closed (diagnosed 2026-05-16) | Diagnosed — prod OOM hotfixed via PRs #99/#100/#101; resilient failure-state recording deferred to a future phase (see CLAUDE.md) |
| audit | 155 quick-task directory entries without status frontmatter | Historical archive (already merged in git); audit misclassifies as open — same carry-forward as v1.11–v1.16 |
| todos | 5 long-range todos (bitboard storage, etc.) | Carried forward — long-range ideas, not v1.17-scoped |
| seeds | SEED-002 (benchmark population baselines), SEED-006 (zone recalibration) | Dormant — gated on full benchmark ingest |
| tech debt | Pre-existing ORM/DB column drift (`game_positions.clock_seconds`, `games.white_accuracy`, `games.black_accuracy`) | Carried forward from v1.11 — cleanup migration outstanding |
| zones-moot | 3.1.4 Endgame Score per-ELO `ENDGAME_SCORE_ZONES` registry deferral | MOOT — snapshot-ELO ramp was a rating-lag artifact (reports/benchmarks-diff-2026-05-17-vs-2026-05-19.md §2); single global band stands |
| zones-moot | 3.1.5 Achievable Score Gap per-ELO stratification deferral | MOOT — same rating-lag artifact; global band stands (diff §2) |
| zones-moot | §3.3.3 5-quintile blocking decision | RESOLVED to Q0-only — Q1-Q4 dropped to review under game-time bucketing; only Q0 remains a per-(TC x ELO x quintile) promotion candidate (diff §2) |
| zones-defer | C + PER_CLASS_SCORE_BULLET_ZONES (benchmarks-diff §2) | Deferred to a scoped phase — no split-overlay exists for C; per-class bullet is a shared-config feature requiring new registry, codegen, and EndgameTypeCard wiring |

Acknowledged at v1.14 milestone close on 2026-04-29:

| Category | Item | Status |
|----------|------|--------|
| requirements | INSIGHT-UI-04 (soften titles per SEED-008) | Descoped 2026-04-28 (Phase 76 D-04 — severity word never user-facing; confidence badge + sort cover SEED-008 intent) |
| uat | Phase 77 HUMAN-UAT — 3 open scenarios | Deferred — phase shipped via PR #72; remaining scenarios in `77-HUMAN-UAT.md`, not blocking close |
| verification | Phase 77 VERIFICATION — `human_needed` | Deferred — automated gates green; manual verification not blocking close |
| audit | 9 stale debug session entries (diagnosed/awaiting_human_verify, March-April) | Carried forward; not relevant to v1.14 active work |
| audit | 133 quick-task directory entries without status frontmatter | Historical archive (already merged in git); audit misclassifies as open |
| todos | bitboard-storage-for-partial-position-queries (database) | Carried forward — long-range idea |
| todos | phase-70-requirements-roadmap-amendments (planning) | Already landed in Plan 70-05; todo file not pruned |
| seeds | SEED-002 (benchmark population baselines), SEED-006 (zone recalibration) | Dormant — gated on full benchmark ingest |
| future | LLM narration of opening insights | Future seed — v1.14 shipped the calibrated data plumbing (effect size + confidence + p_value) that LLM narration would consume |
| tech debt | Pre-existing ORM/DB column drift (`game_positions.clock_seconds`, `games.white_accuracy`, `games.black_accuracy`) | Carried forward from v1.11 — cleanup migration outstanding |

Acknowledged at v1.13 milestone close on 2026-04-27:

| Category | Item | Status |
|----------|------|--------|
| requirements | INSIGHT-MOVES-01..03, INSIGHT-META-01, INSIGHT-BADGE-01 | Descoped 2026-04-27 (covered by Move Explorer row tint + per-finding cards; alert-fatigue concern on bookmark badge) |
| uat | Phase 71 UAT — 18 open scenarios | Deferred — phase shipped via PR #67; remaining UAT scenarios captured in 71-UAT.md, not blocking close |
| uat | Phase 71.1 HUMAN-UAT — 9 open scenarios | Deferred — phase shipped via PR #68; remaining UAT scenarios captured in 71.1-HUMAN-UAT.md, not blocking close |
| verification | Phase 71.1 VERIFICATION — `human_needed` | Deferred — automated gates green; manual verification not blocking close |
| audit | 8 stale debug session entries (diagnosed/awaiting_human_verify, March-April) | Carried forward from v1.12; not relevant to v1.13 active work |
| audit | 129 quick-task directory entries without status frontmatter | Historical archive (already merged in git); audit misclassifies as open |
| todos | bitboard-storage-for-partial-position-queries (database) | Carried forward — long-range idea, not v1.14 candidate yet |
| todos | phase-70-requirements-roadmap-amendments (planning) | Closed — amendments landed in Plan 70-05 commit |
| seeds | SEED-002 (benchmark population baselines), SEED-006 (zone recalibration), SEED-005 (opening insights) | Dormant — SEED-005 fulfilled by v1.13; SEED-002/006 await full benchmark ingest |

Acknowledged at v1.12 milestone close on 2026-04-26:

| Category | Item | Status |
|----------|------|--------|
| requirements | Plan 69-06 sub-task 06-05 (`--per-cell 30` interim ingest) | Descoped — operational ops, not milestone gate (per 2026-04-26 v1.12 scope-down) |
| requirements | Plan 69-06 sub-task 06-08 (manual cleanup of 2026-03 Lichess dump file) | Pending manual cleanup on Adrian's local disk |
| requirements | VAL-01 / VAL-02 / VAL-03 / VAL-04 / BENCH-01..04 | Moved to SEED-006 (gated on full benchmark ingest) |
| tech debt | Pre-existing ORM/DB column drift (game_positions.clock_seconds, games.white_accuracy, games.black_accuracy) | Deferred again — cleanup migration outstanding from v1.11 |
| ops | Full benchmark DB ingest (`--per-cell 100`, ~205 GB projected) | Operational task; entry criterion for SEED-006 |
| audit | 8 stale debug session entries (status=diagnosed/awaiting_human_verify) | Acknowledged at close; not relevant to active work |
| audit | 125 quick-task directory entries without status frontmatter | Historical archive (already merged in git); audit misclassifies as open |

Carried forward from v1.11 close (still relevant):

| Category | Item | Status |
|----------|------|--------|
| tech debt | `_compute_score_gap_timeline` helper kept old name after subsection rename | Grep noise, not a bug |
| tech debt | `_finding_time_pressure_vs_performance` emits filtered-out finding solely for hash effect | Either drop or promote chart payload |

### Roadmap Evolution

- 2026-05-31: **v1.22 repurposed from "LLM Statistical Reasoning" to "Maintenance — Test Isolation & Frontend Major Upgrades."** New v1.22 holds Phase 100 (Isolated Test DB Per Run, SEED-031) + Phase 101 (Frontend Major Dependency Upgrades, SEED-032), executed sequentially. The previously-planned LLM Endgame-Insights Statistical-Reasoning Rework (the sole v1.22 phase, carried through renumbers 95 → 97 → 98 → 100) was **deprioritized to backlog Phase 999.7** intact rather than renumbered inline; LLM-01..07 stay pending and the REQUIREMENTS.md traceability repoints to 999.7. No v1.23 placeholder created (user chose "backlog only" — the next feature milestone gets named when started). Note: SEED-032 itself recommended *not* cutting a release for tooling upgrades; making this a tagged maintenance milestone is a deliberate override (recharts 3 is user-facing and worth a release). Source: user `/gsd-explore` restructure request.
- 2026-05-22: v1.19 Endgame Percentiles & LLM Statistical Reasoning opened with a 3-phase directional roadmap. Phase 93 (Global Percentile Benchmark Artifact) — global empirical-CDF artifact via the canonical `/benchmarks` CTE locked into `endgame_zones.py` with TS codegen drift-guard (PCTL-01); blocks the rest. Phase 94 (Backend & Frontend Percentile Annotations) — nullable `{metric}_percentile` field + chip on Tier-1 / Tier-2 endgame cards with desktop + mobile parity, honest Tier-2 copy, minimum-N reliability gate (PCTL-02..06). Phase 95 (LLM Endgame-Insights Statistical-Reasoning Rework) — payload extension with p-values + CI bounds + percentiles, prompt rewrite reasoning over CIs / percentiles with guardrails resolving the `feedback_llm_significance_signal` tension, version bump from `endgame_v35`, UAT pass (LLM-01..07). All 13 requirements mapped, no orphans. Per-phase details intentionally directional — refined at `/gsd:discuss-phase` time. Source: SEED-019 §Proposed Scope and recent v1.17 statistical-rigor work (Phases 85.1 / 86 / 87.2 / 87.6 / 88).
- 2026-05-22 (later): v1.19 Phase 93/94 scope **narrowed via empirical pre-flight**. A focused /benchmarks pass on the 5 candidate ΔES metrics (`reports/benchmarks-gap-metrics-percentile-candidacy.md`) measured per-user distributions, per-rating-bucket Cohen's d, and skew/kurtosis. Findings: (a) Endgame Score Gap is the cleanest skill-isolating metric (d=0.19, normal-shaped), not "self-relative noise" as a pre-data Socratic critique suggested; (b) Conversion Score Gap is a heavy rating-proxy (d=1.37) but useful for weaker players under an improvement-focus framing; (c) Recovery Score Gap is decisively dropped (d=0.95 inverted, opponent-confounded — the chip would read as the opposite of truth for both weak and strong players); (d) Parity ΔES is the cleanest Section-2 candidate (d=0.30). Chip set narrowed to **4 ΔES rows**: Endgame Score Gap + Achievable Score Gap (page-level) and Parity ΔES + Conversion ΔES (Section 2 cards). Recovery card and the 3 raw % gauges (Conv/Parity/Recov) get no chips. Plan 1 split into Task A (extend `/benchmarks` SKILL.md with a CDF subchapter — methodology source of truth) and Task B (write `scripts/gen_global_percentile_cdf.py` — deterministic regeneration; commits to a new module `app/services/global_percentile_cdf.py`, separate from `endgame_zones.py`). Popover copy reframed: metric-aware (skill-isolating for low-d trio; improvement-focus for high-d Conversion). PCTL-01 + PCTL-04 in REQUIREMENTS.md and Phase 93/94 detail blocks in ROADMAP.md synced to SEED-019. v1.19 is ready for `/gsd:discuss-phase 93`.
- v1.0–v1.14 shipped (see .planning/MILESTONES.md)
- v1.14 shipped 2026-04-29 with 3 phases (75, 76, 77), 16 plans, delivered via PRs #69, #70, #71 (inline confidence-mute hotfix), #72, #73 (quick task). INSIGHT-UI-04 descoped per Phase 76 D-04.
- 2026-05-02: v1.15 opened — single-phase milestone (Phase 78). Source: `reports/conv-recov-validation-2026-05-02.md`. All 16 v1.15 requirements (ENG-01..03, FILL-01..04, IMP-01..02, REFAC-01..05, VAL-01..02) mapped to Phase 78. SEED-010 Library gated until v1.15 ships.
- 2026-05-02: Phase 79 added to v1.15 — position-phase classifier (opening/middlegame/endgame) via lichess Divider port plus middlegame Stockfish eval. Phase 78's operational backfill (benchmark + prod) and deploy moved to a single combined run after phase 79 ships, so one backfill pass populates both endgame and middlegame eval entries. Phase 78 stays unmerged in the meantime.
- 2026-05-03: Phase 80 added to v1.15 — first downstream consumer of Phase 79 data. Adds three columns to Openings → Stats subtab tables (bookmarked + most-played): avg eval at middlegame entry ± std (user-perspective), one-sample t-test confidence (low/medium/high, 10-game minimum threshold reused from opening insights), and avg clock diff at middlegame entry (mirrors endgame-entry pattern). Depends on Phase 79 backfill landing on prod. Source brainstorm: `.planning/notes/phase-aware-analytics-ideas.md`.
- 2026-05-03: Phase 80 moved out of v1.15 into a new milestone **v1.16 Stockfish Eval Analyses** (planned, multi-phase). Rationale: keep v1.15 focused on the eval-based endgame classification cutover (78+79 backfill+deploy), and bundle downstream consumers of the new Stockfish evals (endgame span-entry + middlegame-entry `eval_cp`/`eval_mate`) into a dedicated milestone so v1.15 can ship as soon as the cutover is verified. v1.16 entry criterion: v1.15 shipped (Phase 79 backfill landed on prod). Phase 80 is the first slot; further phases will be added from `.planning/notes/phase-aware-analytics-ideas.md` and other brainstorms.
- 2026-05-03: v1.15 cutover (Phases 78 + 79) — combined operator cutover ran rounds 1 (dev DB smoke, user 28), 2 (benchmark DB full backfill — phase column UPDATE pass + endgame span-entry eval pass + middlegame entry eval pass; PHASE-INV-01 = 0), 3 (prod DB full backfill via SSH tunnel; PHASE-INV-01 = 0). Draft PR #78 opened against main (40+ commits, +7093 / −2176, 54 files). PHASE-VAL-01 / VAL-01 rescinded as moot — proxy removed in Phase 78 REFAC, so the proxy-vs-Stockfish agreement metric is undefined; `/conv-recov-validation` Skill deleted from `~/.claude/skills/`. PHASE-VAL-02 (operational sanity that backfill populated rows correctly) satisfied by PHASE-INV-01 = 0. Remaining: bin/deploy.sh on the merge, post-deploy UI smoke (VAL-02 / VAL-03) on 3-5 representative test users.
- 2026-05-03: B-1 ordering deviation — prod backfill (Round 3) ran before the combined PR + bin/deploy.sh, against the 79-04-PLAN sequence. Operator confirmed "already handled, no action" via /gsd-progress dispatcher. Mitigation specifics (paused traffic / delta backfill / minimal window) not documented in 79-04-SUMMARY (TBD on milestone retro).
- Phase 87.2 inserted after Phase 87.1: Section 2 — replace rate-based peer-diff Gap with eval-based ΔES Score Gap on Conv/Parity/Recov + Endgame Skill cards (URGENT)
- Phase 87.3 inserted after Phase 87.2: Endgame Skill v2 — Conv+Parity percentile composite (Recovery dropped, opponent-confounded per benchmark spike) (URGENT)
- Phase 88.3 inserted after Phase 88: Endgame Stats viz refinements (timeline inactivity annotations, ELO default series, Overall Performance 2-col card restructure) — from /gsd-explore chart-quality review (URGENT)
- Phase 88.4 inserted after Phase 88.3: Time Pressure card layout refactor (responsive grid, 3-stat header row over Clock Gap bullet, Score-Gap bullets → one zone-banded line chart w/ whiskers + tooltip stats, neutral zone TC-collapsed per §3.3.3) — from user layout review of screenshots/tc-cards.png; frontend-only
- Phase 94.3 inserted after Phase 94.2: Per-TC percentile chips on Time Pressure cards (SEED-025) — extends 94.2 pooled-per-user contract by TC; 12 new metrics; no schema change
- Phase 94.4 inserted after Phase 94.3: Peer-relative percentile chip refinement (SEED-026 v2): pivot from global-pool to per-(metric, ELO anchor, TC) cohort CDFs via 50-Elo sliding windows (K=200), per-(user, TC) rating anchor with ChessGoals Table 2 chess.com→Lichess conversion, chip UX shrink to pill, tooltip cohort framing, rescue Conversion/Recovery/Endgame Score Gap chips dropped in v1. (URGENT)
- Phase 97 inserted: Inserted Phase 97 (Endgame Metrics by Time Control, standalone UX); renumbered prior LLM Statistical-Reasoning Rework 97 → 98 (v1.20).

### Pending Todos

- **Human-like engine analysis** (general) — v2+ engine eval filtered by human move plausibility at target Elo
- **Bitboard storage for partial-position queries** (database) — 12 BIGINT bitboard columns on game_positions

### Blockers/Concerns

- Backfill batch_size MUST be 10 games (~400 rows) per commit — prior OOM at batch_size=50 (production incident)
- Prod backfill sequencing concern (FILL-03) is closed for v1.15 — prod backfill ran post-benchmark on 2026-05-03. For future eval-style backfills, keep the benchmark-first → operator-validates → prod ordering as the default.
- v1.15 deploy outstanding — until `bin/deploy.sh` lands the combined PR #78, prod has the new alembic heads (1efcc66a7695, c92af8282d1a) applied (manually during Round 3) but is still running pre-cutover code. Any prod-side schema-aware code change before deploy should account for this temporary mismatch.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260425-dxh | Replace endgame insights findings_hash cache with structural cache | 2026-04-25 | 9029f7e | [260425-dxh-implement-endgame-insights-structural-ca](./quick/260425-dxh-implement-endgame-insights-structural-ca/) |
| 260425-lii | Fix misleading "chess.com user not found" error during import | 2026-04-25 | 22d561f | [260425-lii-fix-misleading-chess-com-user-not-found-](./quick/260425-lii-fix-misleading-chess-com-user-not-found-/) |
| 260425-lwz | Fallback to month enumeration when chess.com archives-list endpoint 404s for an existing user | 2026-04-25 | af64f66 | [260425-lwz-fallback-to-month-enumeration-when-chess](./quick/260425-lwz-fallback-to-month-enumeration-when-chess/) |
| 260425-nlv | Restructure GameCard and PositionBookmarkCard layouts (full-width identifier line on top, board left + content right below) | 2026-04-25 | 5a7ad30 | [260425-nlv-restructure-gamecard-and-positionbookmar](./quick/260425-nlv-restructure-gamecard-and-positionbookmar/) |
| 260426-pbo | Drop parity filter on top-10 openings, prefix off-color rows with "vs." (PRE-01) | 2026-04-26 | 3301ff5 | [260426-pbo-drop-parity-filter-on-top-10-openings-pr](./quick/260426-pbo-drop-parity-filter-on-top-10-openings-pr/) |
| 260427-g4a | Fix opening insights IllegalMoveError when entry_san_sequence does not start from initial position (#71 hotfix) | 2026-04-27 | 8bc4337 | [260427-g4a-fix-opening-insights-illegalmoveerror-wh](./quick/260427-g4a-fix-opening-insights-illegalmoveerror-wh/) |
| 260427-h3u | Replace OpeningFindingCard whole-card deeplink with explicit Moves + Games links (drops n=, ExternalLink icon) | 2026-04-27 | ae44c6e | [260427-h3u-in-opening-strengths-and-weaknesses-card](./quick/260427-h3u-in-opening-strengths-and-weaknesses-card/) |
| 260427-j41 | Highlight candidate move in Move Explorer when arriving via Insights Moves link (severity-colored row border, pulsating board arrow, auto-scroll, clear on position/filter change) | 2026-04-27 | b6c1f29 | [260427-j41-highlight-candidate-move-in-move-explore](./quick/260427-j41-highlight-candidate-move-in-move-explore/) |
| 260428-doc-framing-refresh | Refresh PROJECT/CLAUDE/README lead sections to match homepage's 5-feature framing (Endgame Analytics + Opening Insights + Time Management + Comparison + System Filter) | 2026-04-28 | ce250a4 | [260428-doc-framing-refresh](./quick/260428-doc-framing-refresh/) |
| 260428-oxr | Replace Wald CI half-width confidence buckets with p-value thresholds and N>=10 gate | 2026-04-28 | f29617d | [260428-oxr-replace-wald-ci-half-width-confidence-bu](./quick/260428-oxr-replace-wald-ci-half-width-confidence-bu/) |
| 260428-tgg | Sort opening insights findings by Wald CI bound (direction-aware tiebreak replacing effect-size) | 2026-04-28 | 45c5a20 | [260428-tgg-sort-opening-insights-findings-by-wald-c](./quick/260428-tgg-sort-opening-insights-findings-by-wald-c/) |
| 260428-v9i | Switch opening insights ranking from Wald CI bound to Wilson score interval bound | 2026-04-28 | 0715fda | [260428-v9i-switch-opening-insights-ranking-from-wal](./quick/260428-v9i-switch-opening-insights-ranking-from-wal/) |
| 260429-gmj | Insights finding cards: arrow for after-move on mini board | 2026-04-29 | de187ed | [260429-gmj-insights-finding-cards-arrow-for-after-m](./quick/260429-gmj-insights-finding-cards-arrow-for-after-m/) |
| 260429-ty5 | Replace Opponent Strength ToggleGroup with dual-handle range slider + 4 preset chips (Spike 001) | 2026-04-29 | 124237b | [260429-ty5-opponent-strength-slider](./quick/260429-ty5-opponent-strength-slider/) |
| 260503-0t8 | Speed up scripts/backfill_eval.py with parallel Stockfish EnginePool, batched UPDATE, and group-by-game PGN parsing (--workers N, default 1) | 2026-05-03 | d1346b4 | [260503-0t8-speed-up-scripts-backfill-eval-py-with-p](./quick/260503-0t8-speed-up-scripts-backfill-eval-py-with-p/) |
| 260503-fef | Add equal-footing opponent filter (abs(opp - user) <= 100) to /benchmarks SKILL §2/§3/§6 + capture framing decision in note (gsd-fast, no quick/ dir) | 2026-05-03 | 6773475 | n/a |
| 260504-my2 | Center opening-stats eval bullet chart on color-specific baseline; recalibrate engine-asymmetry baselines from medians (28/-20) to per-game means (31.5/-18.9) | 2026-05-04 | 147b883 | [260504-my2-eval-bullet-chart-baseline-centering-and](./quick/260504-my2-eval-bullet-chart-baseline-centering-and/) |
| 260504-rvh | Remove eval color-baseline centering: MG-entry bullet chart and backend stats now anchor on 0 cp (engine-balanced); per-color baseline (+0.315 / −0.189) becomes a tick reference instead of the chart center | 2026-05-04 | e1e55bb | [260504-rvh-remove-eval-color-baseline-centering-fro](./quick/260504-rvh-remove-eval-color-baseline-centering-fro/) |
| 260504-ttq | Add current-position score-vs-50% bullet chart under "Results played as White/Black" WDL bar in Openings → Moves tab; muted when n<10 to double as a sample-size trust indicator for the WDL bar above | 2026-05-04 | 6e3cc51 | [260504-ttq-add-a-current-position-score-vs-50-bulle](./quick/260504-ttq-add-a-current-position-score-vs-50-bulle/) |
| 260504-acl | Simplify Moves-tab arrow palette from 5 effect-size buckets to 3 score zones (red <=0.45 / blue 0.45-0.55 / green >=0.55); replace HOVER_BLUE with grey for hovered arrows + row tints; update chessboard InfoPopover to describe Score = (W + 0.5·D)/N | 2026-05-04 | 9f586db | [260504-acl-simplify-arrow-color-from-5-to-3-categories](./quick/260504-acl-simplify-arrow-color-from-5-to-3-categories/) |
| 260505-uzp | Unify Moves-tab arrow + Score column + row background tint on one score-zone signal. Low-data/low-conf rows render in blue (was grey). Blue arrows fade to 0.30 opacity. Hover keeps arrow color, just amplifies size+opacity. Deep-link pulse uses grey alpha levels and lands on the row's natural score-zone tint | 2026-05-05 | 0b3da1f | [260505-uzp-unified-score-zone-palette](./quick/260505-uzp-unified-score-zone-palette/) |
| 260506-rtk | Replace Openings → Stats subtab table with two-column card grid (white left, black right) mirroring Insights tab. New OpeningStatsCard reuses Insights card shell: permanent miniboard, header, WDL chart row, eval bullet chart, Moves + Games links. Drops MostPlayedOpeningsTable and MinimapPopover (hover preview replaced by always-on miniboard) | 2026-05-06 | b08512c5 | [260506-rtk-change-openings-stats-page-layout-to-two](./quick/260506-rtk-change-openings-stats-page-layout-to-two/) |
| 260506-u2b | Align Openings Insights card with Stats card layout: replace MiniBulletChart score bullet with shared WDLChartRow, add MG-entry eval bullet row below it (mirrors OpeningStatsCard), abbreviate prose to "Score X% after <move>". Card border + on-board arrow keep score-zone tint. Backend extends OpeningInsightFinding with eval_* fields and adds per-color eval_baseline_pawns to OpeningInsightsResponse, computed via the existing query_opening_phase_entry_metrics_batch helper (no duplicated SQL) | 2026-05-06 | 37c37bb4 | [260506-u2b-align-openings-insights-card-with-stats-](./quick/260506-u2b-align-openings-insights-card-with-stats-/) |
| 260507-aw5 | Complete Wilson migration on score side: replace trinomial Wald p-value in score_confidence.py with Wilson score-test p-value (null SE = 0.5/sqrt(n)). CI bounds were already Wilson; the test is now the inversion of the CI so they agree by construction. SE=0 degeneracy at all-wins/all-losses gone. Tooltips, Field comments, and module docstrings updated. Eval-side stays on Wald-z (Wilson is binomial-specific) | 2026-05-07 | b47505be | [260507-aw5-complete-wilson-migration-on-score-side-](./quick/260507-aw5-complete-wilson-migration-on-score-side-/) |
| 260507-t4r | Add score bullet chart to Openings Stats and Insights tabs (SEED-011): WDL + Score bullet + Eval bullet 3-row layout, score-zone left border (reliability-gated), neutral light-grey bar mode opt-in on MiniBulletChart, drop "Score X% after [move]" prose (move anchor moves to caption under miniboard), unified single-column card layout on every viewport | 2026-05-07 | b816adf7 | [260507-t4r-add-score-bullet-chart-to-openings-stats](./quick/260507-t4r-add-score-bullet-chart-to-openings-stats/) |
| 260508-dcp | Neutralize non-significant zone fonts on Openings tabs (Score % and Eval only carry zone color when confidence != 'low' AND value is in red/green zone), neutralize all Openings + Endgames Stats MiniBulletChart bars, and recolor the categorical neutral board arrow from blue to transparent grey via new ARROW_NEUTRAL theme token | 2026-05-08 | 4a409726 | [260508-dcp-neutralize-non-significant-zone-fonts-us](./quick/260508-dcp-neutralize-non-significant-zone-fonts-us/) |
| 260508-f9o | Add MG-entry eval bullet chart to Openings → Moves "Results played as" section: backend extends WDLStats + OpeningsResponse with eval_* fields via existing query_opening_phase_entry_metrics_batch helper; frontend restructures section into three same-width chart rows (WDL / Score / Eval) with chart-left, indicator-right (mirrors OpeningStatsCard / OpeningFindingCard layout) | 2026-05-08 | c0b6d6dc | [260508-f9o-in-the-openings-moves-tab-section-result](./quick/260508-f9o-in-the-openings-moves-tab-section-result/) |
| 260508-q1z | Auto-load cached LLM endgame report on Endgames page mount and filter change: new GET /insights/endgame/cached cache-only endpoint (mirrors Tier-1 structural lookup, never invokes LLM, no rate-limit accounting, 200/cache_hit or 404); new useCachedEndgameInsights TanStack query hook (returns null on 404, silent); Endgames.tsx upserts result into existing insightsCache so matchingInsights renders without a Generate click. Existing POST/Generate flow unchanged | 2026-05-08 | (uncommitted) | [260508-q1z-auto-load-cached-llm-endgame-report](./quick/260508-q1z-auto-load-cached-llm-endgame-report/) |
| 260508-r61 | Score info icons and last-played in tooltips: backend threads MAX(played_at) as last_played_at through 4 score-confidence query paths (query_next_moves, query_wdl_counts, query_opening_transitions, OpeningStatsCard query) plus Pydantic schemas + services; frontend new relativeDate.ts helper, WdlConfidenceTooltip renders "Last played: X ago" line; MoveExplorer Score column gains per-row HelpCircle popover (replaces hover-only tooltip; touch-friendly via Radix popover with stopPropagation); troll/sunglasses icon converted to same popover pattern (mobile tap now shows tooltip instead of selecting row); selected-row class gets Tailwind `!` important so highlight is visible on green/red tinted rows | 2026-05-08 | c2483929 | [260508-r61-score-info-icons-and-last-played-in-tool](./quick/260508-r61-score-info-icons-and-last-played-in-tool/) |
| 260509-gm9 | Refactor top-3 bloated functions identified in code-quality scan: `compute_insights` (223 → 41 logic LOC, depth 5 → 2) extracts `_collect_attribution_hashes` / `_fetch_position_wdl_per_color` / `_build_sections` + 2 dataclasses; `_flush_batch` (171 → 59 logic LOC, depth 5 → 2) extracts `_collect_position_rows` / `_collect_midgame_eval_targets` / `_collect_endgame_span_eval_targets` / `_apply_eval_results` + 2 island helpers; `OpeningsPage` (~395 → ~316 non-JSX logic LOC, depth ~6 → ≤4) split into 4 hooks (`useDeepLinkHighlight`, `useSidebarState`, `useTabReset`, `useOpeningsHandlers`) + 4 tab subcomponents (`ExplorerTab`, `StatsTab`, `GamesTab`, `InsightsTab`) under `frontend/src/pages/openings/`. Behavior-preserving only — no API changes, no logic changes, no new tests. All 57 `data-testid` values preserved; mobile drawer + desktop sidebar parity preserved. Branch `refactor/top3-bloated-functions` (4 commits ahead of main, ready for PR). Backend ruff/ty clean, 1268 tests passed. Frontend lint/knip clean, 311 tests passed, build succeeded | 2026-05-09 | 9bc65c71 | [260509-gm9-refactor-top-3-bloated-functions-opening](./quick/260509-gm9-refactor-top-3-bloated-functions-opening/) |
| 260510-p6i | Apply Linux SCHED_IDLE to Stockfish UCI subprocesses and bump STOCKFISH_POOL_SIZE default 3→4 (matches Hetzner CX32's 4 vCPUs). New helpers in `app/services/engine.py`: `_sched_idle_preexec` (calls `os.sched_setscheduler(0, os.SCHED_IDLE, os.sched_param(0))`, swallows OSError so seccomp-blocked hosts degrade to normal priority instead of crashing) and `_engine_popen_kwargs()` (returns `{preexec_fn: _sched_idle_preexec}` on Linux, `{}` elsewhere via `sys.platform == "linux"` guard). Both `popen_uci` call sites (`start` + `_restart_worker`) splat the kwargs dict. `docker-compose.yml` default `:-2` → `:-4`; `.prod.env` `STOCKFISH_POOL_SIZE=3` → `4` (gitignored, edited at parent path, intentionally uncommitted). Net effect: import-eval saturates all 4 vCPUs when idle but yields instantly to Uvicorn / Postgres because kernel preempts SCHED_IDLE the moment a SCHED_NORMAL task is runnable. RAM cost: 4 × 64 MB hash = 256 MB on 7.6 GB box. Backend gates: ruff/ty clean on engine.py, `pytest -k "engine or eval"` 81 passed / 6 skipped. Post-deploy manual verification: `ssh flawchess "docker compose exec backend ps -eo pid,cls,pri,ni,comm \| grep stockfish"` should show 4 rows with `CLS=IDL`. Commits `2a94734b` (SCHED_IDLE) + `5d14dc43` (compose pool=4) + `95ea87c0` (worktree merge) | 2026-05-10 | 95ea87c0 | [260510-p6i-implement-sched-idle-scheduling-for-stoc](./quick/260510-p6i-implement-sched-idle-scheduling-for-stoc/) |
| 260510-ugj | LLM endgame prompt — scope last-3-month language: `app/prompts/endgame_insights.md` drops the "Currently sitting at X%" / "Stable at X% over the recent window" legal frames from the within-noise section and replaces them with explicit-anchor frames ("Stable over the last 3 months at X%", "All-time aggregate sits at X% (the gauge value)"); new top-level "Anchoring window references — last_3mo vs all_time" section forbids vague temporal anchors ("currently", "recently", "now", "today", "lately", "of late", "the recent window", "right now", "presently", "at the moment") when quoting a `last_3mo` value and requires explicit "last 3 months" framing instead, while permitting those vague present-tense anchors when quoting `all_time` aggregates (the gauge values the user actually sees on the dashboard); strengthened the "Stale combos" rule from a framing advisory ("treat that window as historical — do not frame it as present-day performance") to a hard prohibition ("do NOT cite that window's mean or any numeric field — n, trend, std, bucket counts — in the narrative; reference qualitatively in past tense if at all"), explicitly covering both all_time and last_3mo lines of a stale series. `_PROMPT_VERSION` bumped `endgame_v23` → `endgame_v24` in `app/services/insights_llm.py` so cached LLM reports invalidate on next request. ty + 24 prompt-related tests pass without modification. Commit `2a13c2e7` (prompt + version) + `d32a48f4` (worktree merge) | 2026-05-10 | d32a48f4 | [260510-ugj-llm-endgame-prompt-scope-last-3-month-la](./quick/260510-ugj-llm-endgame-prompt-scope-last-3-month-la/) |
| 260512-ogh | Fix MIN/MIN sample-pair transposition bug in `query_opening_transitions` (PR #90 follow-up). The flat-query refactor emitted two independent aggregates `MIN(game_id)` + `MIN(ply)` as the sample pointer for downstream prefix lookup, but the comment claim "all games with the same (entry_hash, move_san) share the same prefix path" is wrong: polyglot Zobrist ignores half-move clock, so the same `entry_hash` can be reached at different plies in different games via transposition (e.g. `Nf3 Nf6 Ng1 Ng8 e4 e5` returns to the same hash as `e4 e5`). Independent MINs de-correlate and the sample `(game_id, ply)` may refer to no real row — silently corrupting `resulting_full_hash` (when `push_san` happens to be legal) or producing false "custom-FEN drop" Sentry captures. Fix replaces the two MINs with a single paired `MIN(ARRAY[ply, game_id])` (PG array MIN is lexicographic, so the minimum array IS one of the input arrays). Service layer unpacks `(ply, game_id)` from `raw_row.sample_pair`. New regression test `test_sample_pair_correlated_under_transposition` seeds 10 deep-ply games first (smaller game_ids) and 10 shallow-ply games second (larger game_ids) at the same entry_hash and asserts the sample lands in the shallow batch. Structural guardrail still passes (`ARRAY[...]` constructor ≠ `ARRAY_AGG`). Backend gates: ruff/ty clean, 1386 passed / 6 skipped (was 1385 + new test). Pushed to `origin/simplify-opening-insights` so PR #90 picks it up | 2026-05-12 | e382a504 | [260512-ogh-fix-the-code-with-these-findings-create-](./quick/260512-ogh-fix-the-code-with-these-findings-create-/) |
| 260512-6zn | Simplify opening insights query: refactor `query_opening_transitions` from a CTE + two window functions (`LEAD`, `array_agg OVER`) + standard-start subquery into a flat `SELECT … GROUP BY … HAVING`. Move `entry_san_sequence` resolution and `resulting_full_hash` derivation out of SQL into the Python service layer. New `query_transition_prefixes` repository helper batch-resolves SAN prefixes for surviving findings via one indexed query. New `_TransitionRow` dataclass + `_signed_full_hash` + `_wrap_transition_row` (replays the prefix, pushes the candidate, derives resulting_full_hash via `chess.polyglot.zobrist_hash` cast to signed int64); `_replay_san_sequence` wrapped in try/except (`chess.IllegalMoveError` / `InvalidMoveError` / `ValueError` / `AssertionError`) with Sentry tag `source="opening_insights"` so the ~0.05% custom-FEN survivors (chess.com thematic tournament + custom-position "Let's Play!" games, NOT puzzles/themed-events/variants) drop gracefully instead of crashing. Structural guardrail test asserts compiled SQL contains no `WITH `, `LEAD(`, or `array_agg`. Eliminates the structural complexity behind the planner-misestimate cascade fixed today via extended-statistics migration `e925558020b9` (stats stay as belt-and-suspenders). Backend: ruff/ty clean, 1385 passed / 6 skipped. Commits `3a8a5748` (repo) + `91fc1915` (service) + `101bc377` (tests) + `2378e4c4` (worktree merge). Feature branch `simplify-opening-insights` — push and open PR for review | 2026-05-12 | 2378e4c4 | [260512-6zn-simplify-opening-insights](./quick/260512-6zn-simplify-opening-insights/) |
| 260512-p5p | Simplify three overcomplicated SQL queries identified by scanning repos/services for the PR #90 pattern (CTE / subquery composition that confuses the Postgres planner). Three independent atomic refactors on branch `refac/simplify-overcomplicated-db-queries`: (1) Consolidate the transposition WDL trio in `openings_repository.py` — `query_transposition_counts` + `query_transposition_wdl` + `query_resulting_position_wdl` were near-identical with the same JOIN/WHERE/GROUP BY, differing only in projected aggregates. Replaced with a single `query_resulting_position_wdl` returning `{hash: (w, d, l, last_played_at)}`; `openings_service.get_next_moves` derives `count = w+d+l` and drops one round trip. (2) Flatten `query_position_wdl_batch` in `stats_repository.py` — eliminated the `DISTINCT (full_hash, game_id)` dedup subquery in favor of a single flat `SELECT full_hash, COUNT(DISTINCT game_id) FILTER (...) … MAX(played_at) … GROUP BY full_hash`. (3) Drop the CTE from `query_opening_phase_entry_metrics_batch` in `stats_repository.py` — the `dedup_select.cte("dedup")` was a planner-tuning workaround; replaced with a plain subquery joined directly into the aggregation, preserving the DISTINCT-ON phase-entry derivation and the 6 filtered aggregates (`eval_sum_mg`, `eval_sumsq_mg`, `eval_n_mg`, `mate_n_mg`, `null_eval_n_mg`, `outlier_n_mg`) so the load-bearing partition invariant (`eval_n + mate_n + null_eval_n + outlier_n == phase_entry_row_count`) holds. Structural guardrails: compiled SQL must not contain `WITH ` (Task 3) nor `SELECT DISTINCT` subqueries (Task 2). Backend: ruff/ty clean, 1388 passed / 6 skipped. Commits `77dfdb86` (openings) + `830ec0b2` (stats wdl batch) + `1c51e188` (stats phase-entry) + `0824dfa8` (worktree merge). Post-deploy follow-up: tunnel into prod and EXPLAIN ANALYZE `query_opening_phase_entry_metrics_batch` for user 84 — confirm planner stays in the same zone as PR #90's `query_opening_transitions` | 2026-05-12 | 0824dfa8 | [260512-p5p-simplify-db-queries-from-first-principle](./quick/260512-p5p-simplify-db-queries-from-first-principle/) |
| 260514-djw | Title-case every named concept from the "Endgame statistics concepts" panel across user-facing copy on the Endgames Stats page, the Home FAQ, and the LLM Insights prompt. Concepts updated: Endgame Phase, Endgame Type(s), Endgame Sequence, Endgame Entry Eval, Achievable Score, Endgame Score, Non-Endgame Score (Conversion / Parity / Recovery and Endgame Score Gap / Loss were already title-cased). Frontend touched 9 files (Endgames.tsx accordion + games-tab dropdown + dialog text, Home.tsx FAQ, EndgameOverallEntryCard / EndgameOverallPerformanceSection / EndgameScoreOverTimeChart / EndgameWDLChart / EndgameConvRecovChart / EndgameTimePressureSection chart copy + aria-labels, AchievableScorePopover body copy). Backend touched `app/prompts/endgame_insights.md` (UI-label table + glossary + prose) and `app/services/insights_llm.py` (`overall_wdl` chart description string + recovery typical-bands inline note + `_PROMPT_VERSION` bumped `endgame_v27` → `endgame_v28` so cached LLM reports invalidate and newly generated narration uses the capitalized terms). Test assertion regexes updated in `Endgames.overallPerformance.test.tsx`, `AchievableScorePopover.test.tsx`, and `tests/services/test_insights_llm.py` (version + glossary). Gates: `npm run lint` clean, `npm test` 346/346, `uv run ty check app/ tests/` clean, `uv run ruff check app/ tests/` clean, `uv run pytest` 1402/1402 (6 skipped). | 2026-05-14 | a177d653 | [260514-djw-capitalize-endgame-stats-concepts](./quick/260514-djw-capitalize-endgame-stats-concepts/) |
| 260514-kei | Tighten the Achievable Score Gap (3.1.5) neutral band from ±10pp to ±5pp by splitting it off from the shared `SCORE_GAP_NEUTRAL_*` constants — the Endgame Score Gap (3.1.6) row stays at ±10pp. Added `"achievable_score_gap"` to `MetricId` and a new `ZONE_REGISTRY["achievable_score_gap"]: ZoneSpec(-0.05, 0.05, "higher_is_better")` in `app/services/endgame_zones.py`; extended `scripts/gen_endgame_zones_ts.py` to emit `ACHIEVABLE_SCORE_GAP_NEUTRAL_MIN/MAX` alongside the existing `SCORE_GAP_NEUTRAL_MIN/MAX`; regenerated `frontend/src/generated/endgameZones.ts`. Parameterized `ScoreGapRow` with required `neutralMin/neutralMax` props (and a parameterized `gapZoneColor(value, neutralMin, neutralMax)` helper) so each row in `EndgameOverallPerformanceSection.tsx` passes its own band — Achievable Score Gap uses the new ±5pp, Endgame Score Gap keeps ±10pp. `MetricStatPopover` `neutralLower/neutralUpper` props wired per-row. Justification (`reports/benchmarks-latest.md §3.1.5`): pooled benchmark IQR `[−3.9pp, +4.6pp]` rounded to ±5pp; ELO axis is d=0.62 keep-separate, so per-ELO stratification is deferred. Gates: ty clean, pytest 1457/1457 (6 skipped), vitest 375/375, lint/knip/build clean, codegen drift clean. Commits `951043ea` (registry + codegen) + `5c303fc8` (frontend wiring) + `7878e782` (worktree merge). | 2026-05-14 | 7878e782 | [260514-kei-tighten-the-blue-zone-for-achievable-sco](./quick/260514-kei-tighten-the-blue-zone-for-achievable-sco/) |
| 260515-wye | Restore the rate-based Endgame Skill composite for the gauge that Phase 87.2 D-05 accidentally deleted. New `ScoreGapMaterialResponse.endgame_skill_rate_mean` field computed as the equal-weighted mean of (Conv, Parity, Recov) chess-scores over active buckets (`bucket_games >= CONFIDENCE_MIN_N`, denominator drops sparse buckets, None when zero active); wired to `EndgameSkillCard.skill={}` in `EndgameMetricsSection.tsx:172`. Bullet stays on `section2_score_gap_skill_mean` (correct as-is). `EndgameSkillCard` InfoPopover gains a paragraph distinguishing gauge (absolute rate composite) from bullet (vs Stockfish baseline). Tests: new `TestEndgameSkillRateMean` (5 cases — 3-active, 2-active drop-sparse, below-floor → None, empty rows, independence-from-ΔES regression guard) plus frontend regression in `EndgameMetricsSection.test.tsx` asserting gauge driver and bullet field are independent. Gates: pytest 1542/6, ty clean, vitest 431, tsc + knip + build clean. Follow-up: benchmark calibration of the 4 `section2_score_gap_*` ZoneSpec entries (pooled IQRs in `reports/benchmarks-latest.md §3.4.4`) — separate task. Commit `3fa0f058`. | 2026-05-15 | 3fa0f058 | [260515-wye-fix-endgame-skill-gauge-regression](./quick/260515-wye-fix-endgame-skill-gauge-regression/) |
| 260514-i3l | Consolidate the 6 tooltips in the Endgame Overall Performance section into a single shared `<MetricStatTooltip>` body + `<MetricStatPopover>` shell with a uniform 4-paragraph anatomy: bold metric name + 1-2 sentence inline explanation, value line, verdict + statistical confidence, italic grey methodology. Vocabulary split by metric semantic — score-based metrics use strength / weakness / difference vs the 50% or 0% baseline; the eval-based Endgame Entry Eval keeps advantage / disadvantage / deviation vs 0 pawns. New files: `frontend/src/components/popovers/MetricStatTooltip.tsx` (pure body, computes value line + verdict headline internally so all 6 call sites stay aligned), `MetricStatPopover.tsx` (Radix shell + HelpCircle trigger), `__tests__/MetricStatTooltip.test.tsx` (21 tests covering all 4 headline shapes per vocabulary, signed vs unsigned value-line formats, `pValue === null` branch, `lastPlayedAt` branch, ported D-10 forbidden-framing contract). Migrated 6 call sites in `EndgameOverallCard.tsx` (Non-Endgame Score + Endgame Score, with new `popoverName` / `popoverExplanation` props), `EndgameOverallEntryCard.tsx` (Endgame Entry Eval + Achievable Score), `EndgameOverallPerformanceSection.tsx` (Achievable Score Gap + Endgame Score Gap). All existing test IDs and ARIA labels preserved verbatim (`score-info-no`, `score-info-yes`, `achievable-score-gap-info`, `endgame-score-gap-info`, `popover-trigger-achievable-score`, `entry-eval-popover-trigger`). Endgame Entry Eval pulls neutral bounds from `ENDGAME_ENTRY_EVAL_NEUTRAL_MIN_PAWNS / _MAX_PAWNS` (`@/lib/endgameEntryEvalZones`), not the MG-entry `EVAL_NEUTRAL_*_PAWNS` from `openingStatsZones`. `ScoreGapPopover.tsx` deleted (knip-orphaned after migration; the other 3 legacy popovers retained for external consumers in move explorer / opening insights / opening stats). Gates: `npm test -- --run` 373/374 (1 pre-existing AchievableScorePopover.test.tsx failure unrelated to this task — confirmed broken on `main` before the change), `npm run lint` clean, `npm run knip` clean, `npm run build` succeeds in 4.51s. Commits `95e08dfa` (components + tests, TDD red→green) + `5919c62b` (migrate call sites + delete ScoreGapPopover) + `45f8faca` (worktree merge). | 2026-05-14 | 45f8faca | [260514-i3l-consolidate-endgame-overall-performance-](./quick/260514-i3l-consolidate-endgame-overall-performance-/) |
| 260516-1h5 | Endgame Skill card: replace plain `Games: N` row with the same games-count + `Swords` indicator + `MiniWDLBar` layout used by the Conv/Parity/Recov cards, driven by `perfData.endgame_wdl` (Games with Endgame). New required `endgameWdl: EndgameWDLSummary` prop on `EndgameSkillCard` (replaces `totalGames`), threaded through `EndgameMetricsSection` from `Endgames.tsx`. Block guarded by `endgameWdl.total > 0`. Gauge, Score Gap bullet, InfoPopover, Cpu icon, and connector-arrows overlay untouched. Tests: `EndgameSkillCard.test.tsx` flipped (now asserts MiniWDLBar renders + games-count text + hidden-when-zero); `EndgameMetricsSection.test.tsx` fixture extended. Gates: lint, `npx tsc --noEmit`, `EndgameSkillCard` 15/15, Endgame suite 116/116. Commits `4166d514` + `8d7f7587` (worktree merge). | 2026-05-16 | 8d7f7587 | [260516-1h5-skill-card-wdl](./quick/260516-1h5-skill-card-wdl/) |
| 260516-0ax | Lock in `MiniBulletChart` asymmetric `(neutralMin, neutralMax)` rendering with 4 regression tests (left-skewed `(-0.11, 0.00)` → band 36.25%/13.75%/50%, right-skewed `(+0.01, +0.11)` → 51.25%/12.5%/36.25%, symmetric control `(-0.05, +0.05)` → 43.75%/12.5%/43.75%, zone-color follows asymmetric bounds via `value < absNeutralMin → DANGER`, `[absNeutralMin, absNeutralMax) → NEUTRAL`, `>= absNeutralMax → SUCCESS`) and drop the misleading "±band" framing from `neutralMin`/`neutralMax` prop JSDoc + file header + the inline comment at the absNeutralMin/Max conversion site — unblocks Section 2 ΔES Score Gap zone calibration (per `reports/benchmarks-latest.md §3.4.4`: `section2_score_gap_conv (-0.11, 0.00)`, `_parity (-0.04, +0.04)`, `_recov (+0.01, +0.11)`, `_skill (-0.03, +0.03)`) without touching production logic — chart math (`absNeutralMin = center + neutralMin`, `absNeutralMax = center + neutralMax`, no symmetrization) was already correct. Decision: keep 0 (or 50%) at visual center as the engine-neutral anchor; the colored band is drawn offset when `lo`/`hi` are not symmetric around 0 (option 2 from the explore conversation — NOT recentering on band midpoint). One implementation note: JSDOM normalizes `oklch(0.50 0.15 25)` → `oklch(0.5 0.15 25)` (trailing zero dropped), so the zone-color test uses a `normalizeOklch` helper (`Number.parseFloat` over each numeric token) to compare against `ZONE_DANGER`/`ZONE_NEUTRAL`/`ZONE_SUCCESS` from `@/lib/theme`. Reuse for future theme-color comparisons in tests. Gates: vitest 32/32 on `MiniBulletChart.test.tsx`, full frontend suite 435/435 across 37 files, `npm run lint` + `npm run knip` clean, `git diff frontend/src/components/charts/MiniBulletChart.tsx` is comments-only (zero executable change). Commits `def43998` (tests) + `6af7754b` (JSDoc) + `b7ea35c6` (worktree merge). | 2026-05-15 | b7ea35c6 | [260516-0ax-bullet-chart-render-asymmetric-neutral-z](./quick/260516-0ax-bullet-chart-render-asymmetric-neutral-z/) |
| 260519-lu0 | Show the move-explorer Score + Eval bullet charts on the Openings AND Endgames *Games* subtabs. Extracted the ExplorerTab "Results played as" three-row block (WDL + Score `MiniBulletChart` + Eval `MiniBulletChart`) into a shared `frontend/src/components/charts/PositionResultsPanel.tsx` (optional `gamesHref` → plain non-link games count when already on the Games subtab; optional `className`). ExplorerTab + Openings `GamesTab` render it from the existing `OpeningsResponse.stats` (no API change). Endgames required a backend extension: `EndgameCategoryStats` now carries Wilson `score`/`confidence`/`ci_low`/`ci_high`/`p_value` + MG-entry eval (`avg_eval_pawns`/`eval_ci_*`/`eval_n`/`eval_p_value`/`eval_confidence`/`eval_baseline_pawns`) + `last_played_at`, populated in `_aggregate_endgame_stats` by reusing `openings_service._build_wdl_stats` (cross-module import, no circular dep, no shared module) and `compute_eval_confidence_bucket`; `query_endgame_entry_rows` projection extended with `Game.played_at` (eval_cp/eval_mate already projected — no data backfill). Decisions: color-agnostic endgame eval baseline = `EVAL_BASELINE_PAWNS_WHITE` (+0.25, no color filter per D-02); legacy gated `score_p_value` preserved alongside the new ungated `p_value` so `EndgameTypeCard` Stats subtab does not regress. `Endgames.tsx` `gamesContent` (single shared element → covers desktop + mobile) renders `PositionResultsPanel`. Gates: ruff/ty clean, backend pytest (incl. 12 new endgame-stats + router-schema tests asserting byte-equal reuse of the shared helpers) 318 passed, tsc/knip/lint clean, frontend 576 passed. **Verified — user-approved via merge to `main`** (PR squash). Includes the post-UAT context-aware Eval-copy fix `a7e99a7a` (Endgames Games-subtab Eval popover now reads "...the position where the endgame begins" + drops the opening-only +0.25 tick). Commits `a8e6c286` + `1d314e94` + `6d7eb100` + `7251ce33` + `458021e8` + `70cb4753` (worktree merge) + `a7e99a7a` (copy fix). | 2026-05-19 | 70cb4753 | [260519-lu0-score-eval-charts-games-subtabs](./quick/260519-lu0-score-eval-charts-games-subtabs/) |
| 260519-vf2 | Apply the benchmarks-diff 2026-05-17→2026-05-19 (game-time bucketing) Net Actionable Worklist: entry_eval_pawns neutral band ±0.75→±0.60 (Python `endgame_zones.py` + hand-written `endgameEntryEvalZones.ts` mirror), `SCORE_GAP_DOMAIN` 0.20→0.22, `PER_CLASS_GAUGE_ZONES` minor_piece.recovery (0.31,0.41)→(0.28,0.38) / rook.achievable_score_gap upper +0.04→+0.05 / queen.achievable_score_gap lower −0.05→−0.04, regenerated `endgameZones.ts` (codegen drift gate clean). Item G (WDL bar) confirmed no-op (`SHOW_WDL_BAR_IN_TYPE_CARDS` stays true; 2026-05-17 drop-rec reversed). STATE Deferred-Items rows added: 3.1.4 / 3.1.5 per-ELO deferrals → MOOT, §3.3.3 5-quintile → RESOLVED to Q0-only, plus item C + `PER_CLASS_SCORE_BULLET_ZONES` → deferred to a scoped phase (user decision: defer-both — no split-overlay exists for C, per-class bullet is a shared-config feature). Test assertions updated in lockstep (`test_endgame_zones.py`, `test_insights_llm.py`, `endgameEntryEvalZones.test.ts`). Gates: ruff/ty/pytest 1564 passed, codegen drift clean, frontend lint/knip/test 586/build all green. Commits `ee7010fb` + `10f88f8b` + `d35991f9` + `ec266f7c` (worktree merge). | 2026-05-19 | ec266f7c | [260519-vf2-implement-the-changes-from-the-net-actio](./quick/260519-vf2-implement-the-changes-from-the-net-actio/) |
| 260519-ni3 | Endgame Type Breakdown cards: per-type **Start** and **End** predicted scores flanking the Score Gap row. 3-column label line in `EndgameTypeCard`: left `<Cpu> Start: x%`, center `Gap: ±y% <info>` (post-UAT: `Cpu` icon now only on Start; center label shortened "Score Gap:" → "Gap:"; `MetricStatPopover` keeps full "Score Gap" name), right `End: z%`; Score Gap `MiniBulletChart` unchanged below. Backend: pure `_compute_span_scores` helper returns `(es_entry, exit_score)` so `_compute_span_gap` signature + per-bucket `gaps_by_bucket` path unchanged; Start/End means accumulate under the same NULL-eval cohort gate so `End_mean − Start_mean == Score Gap mean` exactly (reconciliation invariant, tested to 1e-9). New `float | None` fields `type_achievable_score_start_mean`/`_end_mean` (None when n==0) + TS mirror. `ScoreGapRow` got default-undefined `startSlot`/`endSlot` props (3 other consumers pixel-identical, asserted). Gates: ruff/ty clean, backend pytest 294 passed, tsc/lint/knip clean, frontend 586 passed. **Verified — user-approved via merge to `main`** (PR squash). Commits `3514407f` + `82d0968c` + `be8931c5` (worktree merge) + `66ede60d` (post-UAT: Cpu-on-Start-only + "Gap:" label). | 2026-05-19 | be8931c5 | [260519-ni3-endgame-type-start-end-scores](./quick/260519-ni3-endgame-type-start-end-scores/) |
| 260524-nfh | Phase 94.3 UAT fix: flip 4 `net_flag_rate_{tc}` chips from `lower_is_better` to `higher_is_better` in `frontend/src/components/charts/PercentileChip.tsx` (metric is `(timeout_wins - timeout_losses) / total`, so higher IS better; DB percentile was already correct, only the chip direction map was wrong). Removed the "Lower is better" prepended popover line (no longer fires) and dropped the now-incorrect "in the opposite of the intuitive direction" parenthetical from `COPY_RATING_NOTE_NFR_BULLET`. Also restructured `ClockGapHeaderRow` in `EndgameTimePressureCard.tsx` from a 3-column grid (You \| Gap \| Opp) to a single left-aligned inline stat `"You: x% • Opp: y% • Gap: z% <info>"` with a right-aligned Clock Gap percentile chip per UAT spec. Updated 4 affected tests (PercentileChip direction exhaustiveness + flame/band/label assertions, EndgameTimePressureCard `p=0` chip label expectation `Top 1%` → `Top 100%`, NFR bullet copy assertion). Gates: vitest 699/699 frontend tests pass, eslint + knip clean. Commit `3450eb86`. | 2026-05-24 | 3450eb86 | n/a |
| 260521-d6o | Cold-drain eval lane: replace per-target `_board_at_ply` calls in `app/services/eval_drain.py` with a single PGN parse + single mainline walk per game that snapshots `board.copy()` at every needed target ply at once. New `_TargetSpec` dataclass, `_collect_target_specs` (pure, no parse), `_snapshot_boards` (single-walk + early-break when all targets filled), and `_collect_eval_targets_per_game` (combines the two). `_collect_midgame_eval_targets` and `_collect_endgame_span_eval_targets` rewritten as thin filters over the per-game helper with **byte-identical public signatures** so `import_service.py:_collect_covered_game_ids` still works unchanged. Production cold-drain path (`_collect_eval_targets_from_db`) calls `_collect_eval_targets_per_game` directly so each game gets exactly one mainline walk per drain tick. `_board_at_ply` + the unused stub `_build_game_eval_data_from_pgn` + `_collect_eval_targets_for_games` deleted. Preserves T-78-17 lichess `%eval` skip (specs filter out already-evaluated plies before any board snapshot is taken), D-09 / R-02 cold-drain invariants, and rare-parse-failure silent skip. Scope guard verified empty: `git diff --stat -- app/services/zobrist.py app/services/import_service.py scripts/backfill_eval.py`. TDD with 7 new tests (`TestSingleWalkTargetCollection`) locking the single-parse invariant, lichess-skip, parse-failure tolerance, short-game tolerance, multi-island detection, and pre-push board semantics. Gates: ruff format / ruff check / ty check clean, `uv run pytest -x` = 1612 passed / 6 skipped. Expected payoff: ~3–7% of cold-drain wall time on large imports (advisory, no perf gate). Branch `quick/fix-pgn-replay-cold-drain` off `origin/main` — PR-and-merge to `main` per CLAUDE.md GitLab-flow rules. Commits `56277b78` (perf) + `7e6d10df` (worktree merge). | 2026-05-21 | 7e6d10df | [260521-d6o-fix-redundant-pgn-replays-in-the-cold-dr](./quick/260521-d6o-fix-redundant-pgn-replays-in-the-cold-dr/) |
| 260527-rmv | Drop the `section2_` prefix from score-gap bucket metric names end-to-end. New alembic migration `4c42ebc87b7f` renames `benchmark_metric` ENUM values via non-destructive `ALTER TYPE ... RENAME VALUE` (`section2_score_gap_conv` → `score_gap_conv`, `section2_score_gap_parity` → `score_gap_parity`). Python: `CdfMetricId` / `MetricId` Literals, all 24 `ScoreGapMaterialResponse` fields (`section2_score_gap_{conv,parity,recov,skill}_{mean,n,p_value,ci_low,ci_high,percentile}` → `score_gap_*`), SQL helper functions `per_user_cte_section2(_tc)` → `per_user_cte_score_gap_bucket(_tc)`, constants `_SECTION2_BUCKETS` → `_SCORE_GAP_BUCKETS` and `SECTION2_MIN_SPANS_PER_BUCKET` → `SCORE_GAP_BUCKET_MIN_SPANS`. Frontend: `SECTION2_SCORE_GAP_*_NEUTRAL_{MIN,MAX}` → `SCORE_GAP_*_NEUTRAL_{MIN,MAX}` (regenerated `endgameZones.ts`), `SECTION2_DISPLAY_SHIFT` → `SCORE_GAP_BUCKET_DISPLAY_SHIFT`, local `section2NeutralMin/Max` in `EndgameMetricCard.tsx` simplified to `neutralMin/Max`. 8 SQL golden fixtures under `tests/scripts/fixtures/global_percentile_cdf/` renamed via `git mv`. LLM prompt glossary identifiers updated. Historical alembic migrations and the long version-history changelog in `_PROMPT_VERSION` preserved as-is. No collisions: `score_gap_{conv,parity,recov,skill}` did not exist anywhere prior. Gates: ruff format / ruff check / ty check clean, `uv run pytest` = 2176 passed / 16 skipped (1 env-dependent test deselected: prod-tunnel safety guard fails locally because the prod tunnel is currently open on port 15432, unrelated to rename), npm lint / 673 tests / build / knip clean, alembic upgrade applied to dev DB, `gen_endgame_zones_ts.py` regen produces only the intended diff. Commit `9c81c480`. | 2026-05-27 | 9c81c480 | [260527-rmv-drop-section2-metric-prefix](./quick/260527-rmv-drop-section2-metric-prefix/) |
| 260527-q0b | Rewrite `PercentileChipPopoverBody` bullet 2 so the popover discloses concrete per-TC game counts, values, and percentiles instead of the misleading "your most recent 3000 rated games per time control" framing (which suggested users had 3000 games in every TC). **Aggregated chips** (5: score-gap / achievable / parity / conversion / recovery): bullet 2 now opens with "Based on a weighted average of `<metric>` percentiles from up to 3000 games per time control over the last 36 months. Only games vs opponents within +/-100 Elo are used:" followed by a `<ul>` with one `<li>` per TC the user has played, ordered bullet→blitz→rapid→classical. Per-TC line branches lock the four CONTEXT.md cases: above-floor with percentile → `<tc>: <value> over <n> games -> <p> percentile`, above-floor with null percentile (CDF out-of-range, already excluded from `_aggregate_per_tc_percentile`'s weighted mean) → DROP entry, below-floor with games > 0 → `<tc>: insufficient games`, zero-games → omit. **Per-TC chips** (3: clock-gap / net-flag-rate / time-pressure-score-gap on `EndgameTimePressureCard`) get the simplified one-line bullet 2: "Based on `<n>` of your recent `<tc>` games over the last 36 months, vs opponents within +/-100 Elo. Your value: `<value>`." Bullets 1 (percentile statement + cohort suffix), 3 (filter-independence promise), 4 (94.4 D-12 rating-anchor disclosure with mixed / pure-lichess / pure-chess.com branches) byte-identical. Test 9 flame-icon regression guard byte-identical. Backend (commit `9cbf8830`): added `PerTcBreakdownOut` Pydantic model (`tc`, `value`, `n_games`, `percentile`) in `app/schemas/endgames.py`; widened `ScoreGapMaterialResponse` with 4 `*_per_tc` list fields (score_gap, score_gap_conv, score_gap_parity, recovery_score_gap), `EndgamePerformanceResponse` with `achievable_score_gap_per_tc`, and `TimePressureTcCard` with 6 scalar fields (3 chips × `_n_games` / `_value`). New `_build_per_tc_breakdown` + `_per_tc_game_counts` helpers next to `_aggregate_per_tc_percentile` in `app/services/endgame_service.py`; per-TC game-count dict threaded once through the orchestrator into both `_compute_score_gap_material` and `_get_endgame_performance_from_rows`. Per-TC card scalars wired from chip-cohort `PercentileRow.value` / `PercentileRow.n_games` at the existing per-TC lookup. No DB changes, no migrations, no new repository calls — pure wire-shape widening of already-materialised data. Frontend (commit `d3b18642`): mirrored types in `frontend/src/types/endgames.ts`; extended `PercentileChipProps` in `PercentileChip.tsx` with optional `perTcBreakdown` (aggregated path) and `nGames` + `value` (per-TC path); added `formatChipValue` dispatcher (signed 2-decimal for score family; signed integer percent for `clock-gap` + `net-flag-rate`) and `clampPercentInt` helper matching chip-face [1, 99] floor/ceiling. Bullet 2 rendered inside `<div>` not `<p>` so the `<ul>` is valid HTML. Threaded `per_tc` props through 5 aggregated-chip call sites (`EndgameOverallPerformanceSection.tsx`, `EndgameMetricCard.tsx`, `EndgameMetricsSection.tsx`) and `nGames` + `value` through 3 per-TC card sites in `EndgameTimePressureCard.tsx`. 54 PercentileChip Vitest tests cover all 4 per-TC line branches across all 8 flavors. Gates: ruff format / ruff check / ty check clean; `uv run pytest -x` = 2187 passed / 16 skipped; frontend lint + knip clean; vitest 691/691. Process note: initial Task 1 edits resolved to the main repo via absolute paths (cwd-drift / CC #3099); recovered by copying files into the worktree and `git checkout --`-reverting main. Both task commits live entirely on `worktree-agent-aa86c648418833998` based on `0436525444`; main returned to clean state. Worktree merged back via `6f643863`. On `main`; not pushed. | 2026-05-27 | d3b18642 | [260527-q0b-rewrite-percentilechip-tooltip-bullet-2-](./quick/260527-q0b-rewrite-percentilechip-tooltip-bullet-2-/) |
| 260527-u3u | Fix stale Stage B percentiles after Stage-5c-covered incremental imports. Added a second Stage B trigger inside `_complete_import_job` (after the existing Stage A spawn) gated on `game_repository.users_with_zero_pending([job.user_id])`, opening a fresh `async_session_maker()` read session (never the import-write session). The 7 eval-dependent percentile metric families (`achievable_score_gap`, `score_gap_conv`, `score_gap_parity`, `recovery_score_gap`, `clock_gap`, `net_flag_rate`, `time_pressure_score_gap`) now refresh on import completion when the user has no pending evals, not only on Stockfish cold-drain completion. Motivating evidence: user 28 on prod showed Stage A computed 2026-05-27 18:35:34 with `n_rapid=842` vs Stage B 16:24:59 with `n_rapid=838` — same canonical-slice metric drifting by hours because three Stage-5c-covered lichess imports never ticked the cold drain (every game already carried `%eval` so `evals_completed_at = NOW()` was set at import time and `run_eval_drain` skipped them). Cold-drain Stage B trigger at `eval_drain.py:566-568` preserved unchanged; both sites are idempotent (Stage A/B write disjoint rows, `compute_stage_b` is upsert-safe). `asyncio.CancelledError` from the new block propagates (WR-07 lifespan contract); all other exceptions captured to Sentry with `set_context("percentile_compute", {"user_id", "stage": "B", "trigger": "import_complete"})` and swallowed so the import worker keeps running. 3 new unit tests in `tests/services/test_import_service.py` cover fires-on-zero-pending, does-not-fire-on-pending, swallows-and-captures-on-gate-exception (all three also pin Stage A still scheduled — no regression). CHANGELOG bullet added under `## [Unreleased]` → `### Fixed`. Gates: ruff format / ruff check / ty check clean, `pytest tests/services/test_import_service.py` 7 passed, `pytest tests/services/test_eval_drain.py tests/services/test_eval_drain_stage_b.py` 14 passed / 4 skipped (pre-existing stubs). Manual verification (post-deploy): trigger a small incremental import for an affected user and confirm both Stage A and Stage B `computed_at` columns in `user_benchmark_percentiles` advance together. Commit `696db1df`. On `main`; not pushed. | 2026-05-27 | 696db1df | [260527-u3u-stage-b-percentile-re-trigger-on-import-](./quick/260527-u3u-stage-b-percentile-re-trigger-on-import-/) |
| 260529-015 | Gate endgame Tier-2 readiness on Stage-B percentile completion via a new in-memory `app/services/percentile_compute_registry.py` (`mark`/`clear`/`is_computing` over a module-level `set[int]`, GIL-atomic, no lock, no Sentry). Closes the race where `get_readiness` flipped `tier2=True` the moment `pending evals == 0` and a Stage-A `score_gap` row existed (`has_any_rows`), BEFORE `compute_stage_b` had written the 7 eval-dependent percentile families — so the endgame page unlocked with missing badges on first import and stale prior-import badges on re-import. `mark(uid)` is now called synchronously at BOTH Stage-B trigger sites (`eval_drain.py` ~567, `import_service.py` ~522) immediately before `asyncio.create_task(compute_stage_b(uid))` (marking before scheduling closes the poll window); `compute_stage_b` clears the mark in a `finally` covering success, the `if not anchors: return` no-op, the exception path, and `CancelledError` (clearing even when zero rows are written is what prevents below-floor / no-anchor users from being locked out — the rejected `computed_at >= max(evals_completed_at)` timestamp gate would have locked them out forever). `get_readiness` adds `and not percentile_compute_registry.is_computing(user.id)` as a sync set lookup (no DB query, preserves the at-most-3-queries bound). Accepted caveats documented in the registry docstring: a microsecond residual race between the eval-completed commit and `mark`, and loss of in-memory state on backend restart (mirrors the existing tier1 active-job registry; `compute_stage_b` is upsert-idempotent so a restart self-heals). Backend-only, no DB migration, no frontend change (frontend caching ruled out: TanStack 5min staleTime, no localStorage, `Import.tsx` already invalidates `['endgameOverview']` during active import). 2 readiness regression tests (`tier2=False` while marked, `tier2=True` after clear with identical DB state) + registry unit tests. Gates: ruff format / ruff check / ty check zero errors, `uv run pytest -x` = 2213 passed / 16 skipped. Branch `fix/endgame-tier2-stage-b-gate` off `main` — PR-and-merge per CLAUDE.md GitLab Flow. Commits `b5e4c7eb` + `1c4dba69` + `e7a262ed` + `a6067496` (worktree merge). | 2026-05-29 | a6067496 | [260529-015-gate-endgame-tier-2-readiness-on-stage-b](./quick/260529-015-gate-endgame-tier-2-readiness-on-stage-b/) |
| 260527-hux | Raise `RECENT_GAMES_PER_TC_CAP` from 1000 to 3000 in `app/services/canonical_slice_sql.py` (the per-user pool used by every percentile chip via the canonical slice). Diagnosed during investigation of user 8's blitz `time_pressure_score_gap` percentile chip showing `n_games = 82` despite the user having 5861 eligible blitz endgame games (36mo / equal-footing ±100 / rated) — the 1000-most-recent-per-TC cap was discarding ~91% of eligible games, leaving only 535 endgame games of which 82 satisfied the user-pressured (clock_pct < 0.40) filter. New cap is non-binding for cohort CDF rows (benchmark users are imported at `max_games=1000` per TC at ingest time via `scripts/import_benchmark_users.py`), so `global_percentile_cdf` was deliberately NOT regenerated. Asymmetric on purpose: cohort stays bounded by the import constraint; per-user pool grows ~3× for heavy users. Verified post-bump on dev DB: user 8 blitz endgame games 535 → 1673, `user_pressured_n` 82 → 449, `opp_pressured_n` 124 → 447, displayed `n_games = least(...)` 82 → 447 (5.5× improvement, exceeds the ~250 forecast). Cascaded the new number through every docstring / comment / frontend tooltip / explainer string / test assertion that hardcoded "1000" in the recent-games-per-TC context: `canonical_slice_sql.py` (constant + 5 docstring/comment sites incl. the inline `played_at DESC <= 1000` reference at line 47), `user_benchmark_percentiles_service.py`, `user_rating_anchors.py` (×3), `user_benchmark_percentile.py`, `scripts/backfill_user_percentiles.py`, `frontend/src/components/charts/PercentileChip.tsx` (tooltip bullet 2 per-TC + aggregated variants), `frontend/src/pages/Endgames.tsx` (percentile-badge explainer steps 1 and 3), `frontend/src/types/endgames.ts` (TSDoc examples for `PercentileBadgeRatingAnchorDisclosure`), and 4 frontend test assertions / 2 `it(...)` titles in `PercentileChip.test.tsx`. Two backend test files needed structural fixes (off-plan, blocking): `tests/services/test_canonical_slice_sql.py` (renamed `test_recent_1000_per_tc_cap_substrings_present` → `test_recent_per_tc_cap_substrings_present`, generalised class docstring + failure message), `tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py` (replaced hardcoded `assert "<= 1000" in sql` with `assert f"<= {RECENT_GAMES_PER_TC_CAP}" in sql` so the assertion tracks the constant for future bumps). 32 SQL golden fixtures under `tests/scripts/fixtures/global_percentile_cdf/` regenerated via the snippet documented in the canary test's module docstring (intentional drift per the canary contract). Existing per-user anchor rows in `user_benchmark_percentiles` will refresh organically on the next import / eval drain — no forced backfill triggered. The `least()` symmetry in `canonical_slice_sql.py:683` (could ~double displayed `n_games` again by reporting `user_pressured_n + opp_pressured_n` or both sides separately) is a separate deferred follow-up. Gates: ruff format / ruff check / ty check clean, `uv run pytest --deselect tests/scripts/test_backfill_user_percentiles.py::test_backfill_target_prod_refuses_when_tunnel_down` = 2176 passed / 16 skipped (1 env-dependent test deselected: prod-tunnel safety guard fails locally because the prod tunnel is currently open on port 15432, unrelated to this change), frontend `npm run lint` clean, `npm test -- --run` = 675 passed (56 files). Commits `9d2dec13` (chore: constant + backend docstrings, 39 files incl. 32 regenerated SQL goldens) + `f03da4f3` (copy: frontend tooltip + explainer + test assertions, 4 files). On `gsd/phase-94.4-peer-relative-percentile-chip-refinement`; not pushed. | 2026-05-27 | f03da4f3 | [260527-hux-raise-recent-games-per-tc-cap-from-1000-](./quick/260527-hux-raise-recent-games-per-tc-cap-from-1000-/) |
| 260529-cum | Scope `game_positions` aggregation to the selected user's `recent_capped` games in the per-TC percentile-CTE builders in `app/services/canonical_slice_sql.py`, killing full-table scans on the prod single-user hot path (`compute_stage_a`/`compute_stage_b`, every import + eval-drain completion). /db-report flagged the benchmark-percentile CTE family at ~6s/call, ~58 min cumulative server time: 5 of 6 per-TC builders aggregated the entire `game_positions` table globally (`SELECT … GROUP BY game_id HAVING count >= 6` with no user predicate) before LEFT/INNER-joining to the single selected user's ≤3000 recent games, discarding ~all of it. Three edits: (1) `_endgame_entry_clocks_cte()` gains `JOIN recent_capped rc ON rc.id = gp.game_id` — one edit fixing all three time-pressure builders (`per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`) since all three prepend `_recent_capped_per_tc_cte`; (2) `per_user_cte_score_gap_tc`'s `endgame_game_ids` CTE gains `AND game_id IN (SELECT id FROM recent_capped)`; (3) same predicate in `per_user_cte_achievable_tc` (its `entry_rows` CTE inherits the scoping via its existing join to `endgame_game_ids`). Each edit carries an inline result-equivalence comment. Result-equivalent, not just faster: only `recent_capped` games survive the downstream `scored` / `joined` / `entry_rows` joins (`ON eg.game_id = rc.id` etc.), and the per-game `HAVING count >= 6` plus `array_agg(first clock)` are unaffected by a game_id MEMBERSHIP filter (every row of each retained game is kept). Shared-path safe: the same builders feed the offline benchmark CDF script (`scripts/gen_global_percentile_cdf.py`, `source="benchmark"`) where `recent_capped` derives from the whole cohort, so correctness holds there too. Left untouched: `per_user_cte_score_gap_bucket_tc` (its `spans` CTE already joins `recent_capped` — the proof pattern) and the non-`_tc` pooled builders `per_user_cte_score_gap` / `per_user_cte_achievable` (same global-scan pattern but NOT in the flagged single-user prod hot path; one-line follow-up if cohort parity is wanted). 20 of 32 SQL golden fixtures under `tests/scripts/fixtures/global_percentile_cdf/` regenerated via the canary test's documented heredoc (`score_gap` / `achievable_score_gap` / `time_pressure_score_gap` / `clock_gap` / `net_flag_rate` × 4 TCs, +88/−20); the 12 conv/parity/recovery bucket fixtures stayed byte-identical, confirming the untouched builder. CHANGELOG `## [Unreleased]` → `### Fixed` bullet added. Gates: ruff format / ruff check / ty check zero errors; full `uv run pytest` = 2212 passed / 16 skipped (1 env-dependent failure: `test_backfill_target_prod_refuses_when_tunnel_down` — the local prod SSH tunnel is open on port 15432, so the script's tunnel-down safety guard correctly does not refuse; test file byte-identical to base, same artifact recorded in 260527-hux/260527-rmv, passes in CI where no tunnel runs); the 459 directly-relevant tests (`test_canonical_slice_sql`, `test_canonical_slice_per_user_vs_benchmark_parity`, fixture canary) all pass. Branch `perf/scope-benchmark-percentile-ctes` off `main` — PR-and-merge per CLAUDE.md GitLab Flow. Commits `8d175a14` (perf) + `e6183801` (fixtures) + `a94a5b61` (changelog) + `93a23eb9` (worktree merge). | 2026-05-29 | 93a23eb9 | [260529-cum-scope-game-positions-aggregation-to-sele](./quick/260529-cum-scope-game-positions-aggregation-to-sele/) |
| 260529-l1i | Redesign the PercentileChip tooltip: moved the rating anchor into each per-TC breakdown row (two-line layout) and removed the standalone bottom anchor/platform-blend paragraph (bullet 4). User-approved via previewed AskUserQuestion (chose "two-line rows"). Aggregated chip bullet 2 now renders, per renderable TC, two lines — `{tc} — anchored at ~{anchor} Lichess Elo` then `{value} over {n} games → {p} percentile`; aggregated bullet 1 reworded from "~{anchor}-rated players" to "similarly-rated players, aggregated across the time controls you play" (a single headline anchor can't honestly represent a blended-cohort percentile — fixes the inconsistency surfaced in the 260529-js1 explore session). Per-TC chips (clock-gap / net-flag-rate / time-pressure-score-gap) keep their unambiguous single-anchor bullet 1 unchanged. Deliberate reversal of the Phase 94.4 D-12 platform-blend disclosure amendment: chess.com/lichess composition + ELO-conversion detail is no longer shown anywhere; the rating-matching method is conveyed solely by the per-row "anchored at ~X Lichess Elo" text. Backend: additive `anchor: int | None` on `PerTcBreakdownOut` (`app/schemas/endgames.py`), populated for all 5 aggregated metrics (score_gap / achievable / conversion / parity / recovery) by threading a single `fetch_anchors_for_user` call through both `_compute_score_gap_material` and `_get_endgame_performance_from_rows` into `_build_per_tc_breakdown` (uses `RatingAnchorRow.anchor_rating`); no migration, no data recompute. Frontend: mirrored `anchor` on the TS type; rewrote bullet 2 to the two-line `<ul>`; removed bullet 4 IIFE; made `anchorRating` optional; deleted 4 now-unused platform-composition props (`nChesscomGames` / `nLichessGames` / `chesscomMedianNative` / `lichessMedianNative`) across the chip + 3 call sites + `Endgames.tsx`, and deleted the now-dead `pickDominantTcAnchor` helper (knip-enforced). Aggregated chip render gate simplified `percentile != null && dominantAnchor !== undefined` → `percentile != null` (the old suppression gate was effectively always satisfied when percentile is non-null, since per-TC percentile and anchor share the `user_rating_anchors` source); degrades gracefully (stats line without anchor label) if an anchor is somehow missing. Edge cases preserved: below-floor TC → single-line "insufficient games"; value-but-null-percentile (CDF out-of-range) → dropped; zero-games → omitted. Gates: ruff format/check + ty zero errors; backend pytest 2109 passed / 16 skipped (1 env-dependent failure `test_backfill_target_prod_refuses_when_tunnel_down`, unrelated — local prod tunnel open on 15432); frontend eslint + knip clean, vitest 720/720, tsc clean. Post-merge orchestrator fix: marked the new sync `test_per_tc_breakdown_carries_anchor` async to silence the module-level asyncio-mark warning. CHANGELOG `### Changed` bullet added. Branch `refac/drop-dead-pooled-percentile-ctes` (current branch — now carries the dead-CTE refactor + 260529-js1 anchor fix + this tooltip change; not pushed). Note: displayed anchor values stay stale until the 260529-js1 `compute_anchors_for_user` recompute follow-up runs (separate HUMAN step). Commits `809b0082` (backend field) + `29d7c237` (frontend) + `b324a412` (changelog) + `45fe1d82` (test marker). | 2026-05-29 | 45fe1d82 | [260529-l1i-show-per-tc-rating-anchor-in-percentilec](./quick/260529-l1i-show-per-tc-rating-anchor-in-percentilec/) |
| 260529-js1 | Fix the chess.com→Lichess rating-anchor conversion bug in the `user_rating_anchors` SQL pipeline. The blended-anchor SQL (`_per_user_cte_median_anchor_blended` / `_chesscom_conversion_values_sql` in `canonical_slice_sql.py`) matched every chess.com game's native rating directly against chess.com **Blitz** anchors from `CHESSCOM_BLITZ_TO_LICHESS`, skipping the intra-TC inversion that the canonical `convert_chesscom_to_lichess` applies for rapid/bullet. Result: rapid/classical anchors inflated (chess.com rapid > chess.com blitz), bullet deflated, blitz correct. Verified on dev user 28 rapid: stored anchor 1880 vs corrected 1795 (~85 pts). Fix: new `composed_chesscom_to_lichess_grid(source_tc, target_tc)` in `chesscom_to_lichess.py` builds a native-keyed (chess.com rating → Lichess equiv) grid by calling the canonical converter (single source of truth, now LIVE — previously dead code); bucket→source_tc map bullet→bullet/blitz→blitz/rapid→rapid/classical→rapid; 15-pt grid step (named `_COMPOSED_GRID_STEP`) caps nearest-anchor reconstruction error at 16 pts under a 20-pt test tolerance. `_chesscom_conversion_values_sql` now emits this native-keyed table; the nearest-anchor LATERAL join is unchanged. Repaired 2 stale tests that encoded the old blitz-keyed behavior (`test_a6_*` in `test_canonical_slice_sql.py`, `test_b3_*` in `test_user_benchmark_percentiles_service.py`). Added pure-Python equivalence test (composed grid vs converter, all 4 buckets). Gates: ruff format / ruff check / ty check clean; targeted suites 300 passed; full `uv run pytest` 2108 passed (1 env-dependent failure `test_backfill_target_prod_refuses_when_tunnel_down`, unrelated). HUMAN follow-ups (NOT done — no DB reset per CLAUDE.md): recompute `user_rating_anchors` via `compute_anchors_for_user` for chess.com bullet/rapid/classical users, and regenerate the benchmark CDF (limited impact — benchmark DB is Lichess-only). Branch `refac/drop-dead-pooled-percentile-ctes` (current branch; mixes with the dead-CTE refactor — see process note). Commits `36e87c74` (grid builder) + `64e1892d` (SQL rekey) + `1632b17e` (b3 test) + `c7a0669c` (docstring fix); not pushed. | 2026-05-29 | c7a0669c | [260529-js1-fix-chess-com-to-lichess-rating-anchor-c](./quick/260529-js1-fix-chess-com-to-lichess-rating-anchor-c/) |
| 260529-gl0 | Delete the dead non-per-TC percentile CTE builders from `app/services/canonical_slice_sql.py`. Pure dead-code removal: the metric system was redesigned in Phase 94.3+ to per-TC (`_tc`) lookups whose percentiles are aggregated via weighted average, leaving the pooled all-TC builders with no production caller (verified: the live dispatchers `_per_user_cte_for_family_and_tc` in the service and `_per_user_cte_for_metric_and_tc` in the CDF script route ONLY to `_tc` builders; `per_user_cte_for(` appears nowhere in app/ or scripts/, only tests). Removed 5 symbols: `per_user_cte_score_gap`, `per_user_cte_achievable`, `per_user_cte_score_gap_bucket`, the test-only dispatcher `per_user_cte_for` (also stale — it lacked the `recovery_score_gap` arm the live dispatcher has), and the now-orphaned `_recent_capped_cte` helper (used only by those builders). Also dropped the now-unused `CdfMetricId` import (its only consumer was `per_user_cte_for`). `canonical_slice_sql.py` shrank 1431 → ~1078 lines. Live `_tc` builders (`per_user_cte_score_gap_tc`, `per_user_cte_achievable_tc`, `per_user_cte_score_gap_bucket_tc`, `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`) and `_recent_capped_per_tc_cte` untouched. Test surgery (the bulk of the work): repointed the benchmark-vs-single_user byte-identical/source-mode-parity assertions in `test_canonical_slice_per_user_vs_benchmark_parity.py` from the dead dispatcher to direct live `_tc` builder calls; in `test_canonical_slice_sql.py`, deleted tests that exercised only dead builders (the non-per-TC bucket block) and repointed the 3 properties with no automatic live analog (cap-appears-once-per-render, `recent_capped` prelude substring, negative no-`tc_bucket`/no-`elo_bucket` regression) to a live `_tc` builder. Net −760/+313 across 1 source + 2 test files. Verified: zero grep hits for the 5 deleted names across app/scripts/tests; 32 SQL golden fixtures byte-unchanged (`git diff --stat tests/scripts/fixtures/global_percentile_cdf/` empty). Gates: ruff format / ruff check / ty check zero errors, `uv run pytest` = 2101 passed / 16 skipped (count down from 2212 as dead-builder tests were removed; 1 env-dependent failure `test_backfill_target_prod_refuses_when_tunnel_down`, unrelated — prod tunnel open on 15432). No CHANGELOG entry (pure refactor per CLAUDE.md). Process note: executor cwd-drift (CC #3099) left a stray STATE.md edit + a duplicate untracked SUMMARY in the main tree that blocked the worktree-cleanup helper merge; resolved by manual fast-forward merge of the worktree branch + `git checkout` revert of the stray STATE.md, then a `style` fixup commit for the unused-import/format drift the executor's worktree gates had masked. Branch `refac/drop-dead-pooled-percentile-ctes` off `main`. Commits `cb944127` (delete) + `8cc62d90` (parity tests) + `e1056e63` (structural tests) + `848af940` (style). | 2026-05-29 | 848af940 | [260529-gl0-delete-dead-non-per-tc-percentile-cte-bu](./quick/260529-gl0-delete-dead-non-per-tc-percentile-cte-bu/) |

---
Last activity: 2026-05-27 — Completed quick task 260527-q0b: rewrote `PercentileChipPopoverBody` bullet 2 to disclose concrete per-TC game counts, values, and percentiles instead of the misleading "your most recent 3000 rated games per time control" framing. Aggregated chips (5 — score-gap / achievable / parity / conversion / recovery) now show "Based on a weighted average of `<metric>` percentiles from up to 3000 games per time control over the last 36 months. Only games vs opponents within +/-100 Elo are used:" followed by a per-TC `<ul>` ordered bullet→blitz→rapid→classical with four locked-in line cases: above-floor-with-percentile → `<tc>: <value> over <n> games -> <p> percentile`; above-floor-null-percentile (CDF out-of-range, already dropped from `_aggregate_per_tc_percentile`'s weighted mean) → DROP; below-floor-with-games → `<tc>: insufficient games`; zero-games → omit. Per-TC chips (3 — clock-gap / net-flag-rate / time-pressure-score-gap) get a simplified one-line bullet 2: "Based on `<n>` of your recent `<tc>` games over the last 36 months, vs opponents within +/-100 Elo. Your value: `<value>`." Bullets 1 (percentile statement + cohort suffix), 3 (filter-independence promise), 4 (94.4 D-12 rating-anchor disclosure with mixed / pure-lichess / pure-chess.com branches) byte-identical; Test 9 flame-icon regression guard byte-identical. Backend: added `PerTcBreakdownOut` Pydantic model (`tc`/`value`/`n_games`/`percentile`) in `app/schemas/endgames.py`, widened `ScoreGapMaterialResponse` with 4 `*_per_tc` list fields, `EndgamePerformanceResponse` with `achievable_score_gap_per_tc`, and `TimePressureTcCard` with 6 scalar fields (3 chips × `_n_games`/`_value`). New `_build_per_tc_breakdown` + `_per_tc_game_counts` helpers next to `_aggregate_per_tc_percentile` in `app/services/endgame_service.py`; per-TC game-count dict threaded once through the orchestrator into both `_compute_score_gap_material` and `_get_endgame_performance_from_rows`; per-TC card scalars wired from `PercentileRow.value` / `PercentileRow.n_games` at the existing per-TC lookup. No DB changes, no migrations — pure wire-shape widening of already-materialised data. Frontend: mirrored types in `frontend/src/types/endgames.ts`; extended `PercentileChipProps` with optional `perTcBreakdown` (aggregated path) and `nGames` + `value` (per-TC path); added `formatChipValue` dispatcher (signed 2-decimal for score family; signed integer percent for `clock-gap` + `net-flag-rate`) and `clampPercentInt` helper matching chip-face [1, 99] floor/ceiling. Bullet 2 rendered inside `<div>` not `<p>` so the `<ul>` is valid HTML. Threaded props through 5 aggregated-chip call sites (`EndgameOverallPerformanceSection.tsx`, `EndgameMetricCard.tsx`, `EndgameMetricsSection.tsx`) and 3 per-TC card sites in `EndgameTimePressureCard.tsx`. 54 PercentileChip Vitest tests cover all 4 per-TC line branches across all 8 flavors. Gates: ruff format / ruff check / ty check clean, `uv run pytest -x` = 2187 passed / 16 skipped, frontend lint + knip clean, vitest 691/691. Process note: initial Task 1 edits resolved to the main repo via absolute paths (cwd-drift / CC #3099); recovered by copying files into the worktree and `git checkout --`-reverting main. Commits `9cbf8830` (backend) + `d3b18642` (frontend) on `worktree-agent-aa86c648418833998` based on `0436525444`; worktree merged back via `6f643863`. On `main`; not pushed. Previously: 2026-05-27 — Completed quick task 260527-hux: raised `RECENT_GAMES_PER_TC_CAP` in `app/services/canonical_slice_sql.py` from 1000 → 3000. Surfaced while investigating user 8 blitz `time_pressure_score_gap` chip showing `n_games = 82` despite 5861 eligible endgame games — the per-TC 1000-cap was throwing away ~91% of the data. Asymmetric bump: cohort CDF NOT regenerated because benchmark users are imported at `max_games=1000` per TC at ingest time (cap is non-binding for them); only heavy single-users see their pool grow ~3×. Dev-DB spot check confirmed for user 8 blitz: endgame games 535 → 1673, `least(user_pressured, opp_pressured)` 82 → 447 (5.5× displayed `n_games`). Cascaded the new number through every docstring / comment / frontend tooltip / explainer / test that hardcoded "1000" in the recent-games-per-TC context across `canonical_slice_sql.py` + 4 backend models/services + `PercentileChip.tsx` / `Endgames.tsx` / `endgames.ts` + 4 PercentileChip test assertions. Two backend test files needed off-plan structural fixes (renamed test + replaced hardcoded literal with constant reference); 32 SQL golden fixtures regenerated via the canary test's documented snippet. Existing `user_benchmark_percentiles` rows refresh organically on next import / eval drain. `least()` symmetry at `canonical_slice_sql.py:683` (could double displayed n again by reporting `user_pressured + opp_pressured`) is a deferred follow-up. Gates: ruff/ty/pytest 2176 passed (1 env-dependent deselect), frontend lint/675 tests clean. Commits `9d2dec13` + `f03da4f3` on `gsd/phase-94.4-peer-relative-percentile-chip-refinement`; not pushed. Previously: 2026-05-24 — Completed quick task 260524-nfh: Phase 94.3 UAT fix. (1) Flipped 4 `net_flag_rate_{tc}` chips from `lower_is_better` to `higher_is_better` in `PercentileChip.tsx` — the metric is `(timeout_wins − timeout_losses) / total`, so higher IS better; the DB percentile was already correct and only the chip direction map was wrong. Removed the "Lower is better" prepended popover line (no longer fires) and dropped the now-incorrect "in the opposite of the intuitive direction" parenthetical from `COPY_RATING_NOTE_NFR_BULLET`. (2) Restructured `ClockGapHeaderRow` in `EndgameTimePressureCard.tsx` from a 3-column grid (You | Gap | Opp) to a single left-aligned inline stat `"You: x% • Opp: y% • Gap: z% <info>"` with a right-aligned Clock Gap percentile chip per UAT spec. Updated 4 affected tests (direction exhaustiveness + flame/band/label assertions for `net_flag_rate_bullet`, EndgameTimePressureCard `p=0` chip label expectation `Top 1%` → `Top 100%`, NFR bullet copy assertion). Gates: vitest 699/699 frontend tests pass, eslint + knip clean. Commit `3450eb86`. Previously: 2026-05-21 — Completed quick task 260521-d6o: cold-drain eval lane single-walk PGN refactor in `app/services/eval_drain.py`. Replaced per-target `_board_at_ply` calls with one PGN parse + one mainline walk per game that snapshots `board.copy()` at every needed target ply (new `_TargetSpec` + `_collect_target_specs` + `_snapshot_boards` + `_collect_eval_targets_per_game`). Public wrappers `_collect_midgame_eval_targets` / `_collect_endgame_span_eval_targets` kept byte-identical so `import_service.py:_collect_covered_game_ids` is unaffected; production path `_collect_eval_targets_from_db` calls the per-game helper directly. `_board_at_ply` + two related dead helpers deleted. T-78-17 lichess %eval skip + D-09/R-02 invariants + silent parse-failure tolerance preserved. 7 new TDD tests, ruff/ty/pytest 1612/6 skipped clean. Branch `quick/fix-pgn-replay-cold-drain` — PR-and-merge to `main` per CLAUDE.md GitLab-flow. Expected payoff ~3-7% of cold-drain wall time on large imports (no perf gate). Commits `56277b78` + `7e6d10df`. Previously: 2026-05-19 — Completed quick task 260519-vf2: applied the benchmarks-diff 2026-05-17→2026-05-19 (game-time bucketing) Net Actionable Worklist — four zone-band shifts (entry_eval_pawns ±0.75→±0.60 on both stacks, `SCORE_GAP_DOMAIN` 0.20→0.22, minor_piece.recovery (0.28,0.38), rook/queen achievable_score_gap edges) with regenerated `endgameZones.ts` (codegen drift gate clean), item G WDL-bar reversal confirmed (no code), and STATE Deferred-Items bookkeeping (3.1.4/3.1.5 → MOOT, §3.3.3 → Q0-only, item C + `PER_CLASS_SCORE_BULLET_ZONES` deferred per user decision defer-both). CHANGELOG bullet pending user decision (borderline user-facing zone recalibration). Commits `ee7010fb` + `10f88f8b` + `d35991f9` + `ec266f7c` (worktree merge). Previously: 2026-05-19 — Completed quick task 260519-ni3 (user-approved via merge to `main`): per-type Start/End predicted scores flanking the Score Gap row in the Endgame Type Breakdown cards; backend exposes start/end means over the identical span cohort so `End − Start == Score Gap` exactly (reconciliation invariant tested to 1e-9). Post-UAT: `Cpu` icon only on Start, center label shortened to "Gap:". `ScoreGapRow` slot props keep the 3 other consumers pixel-identical. Commits `3514407f` + `82d0968c` + `be8931c5` + `66ede60d`. Previously: 2026-05-19 — Completed quick task 260519-lu0 (user-approved via merge to `main`): extracted the move-explorer "Results played as" block into a shared `PositionResultsPanel` and rendered it on the Openings + Endgames *Games* subtabs; extended endgame stats backend with Wilson score + MG-entry eval per category via reuse of `openings_service._build_wdl_stats` + `compute_eval_confidence_bucket` (no data backfill, color-agnostic +0.25 eval baseline, legacy `score_p_value` preserved); post-UAT context-aware Eval-copy fix `a7e99a7a`. 5 atomic commits + worktree merge `70cb4753`. Previously: 2026-05-16 — Completed quick task 260516-1h5: surfaced the Games-with-Endgame WDL bar inside the Endgame Skill card with the same `Win/Draw/Loss` label + right-aligned `Games: N` + `Swords` icon indicator + `MiniWDLBar` layout used by Conv/Parity/Recov. `EndgameSkillCard` swaps the legacy `totalGames` prop for a required `endgameWdl: EndgameWDLSummary` driven by `perfData.endgame_wdl`; `EndgameMetricsSection` and `Endgames.tsx` thread the prop through (added `&& perfData` to the section guard so TS narrows non-null). Block guarded by `endgameWdl.total > 0`. Tests updated in lockstep: `EndgameSkillCard.test.tsx` flipped to assert MiniWDLBar renders + games-count text + hidden-when-zero; `EndgameMetricsSection.test.tsx` fixture extended with `DEFAULT_ENDGAME_WDL`. Gates: lint, `npx tsc --noEmit`, `EndgameSkillCard` 15/15, Endgame suite 116/116. Commits `4166d514` (feat) + `8d7f7587` (worktree merge). Previously: 2026-05-15 — Completed quick task 260516-0ax: lock in `MiniBulletChart` asymmetric `(neutralMin, neutralMax)` rendering with 4 regression tests + drop the "±band" framing from JSDoc. Chart math was already asymmetric-safe; this task adds a contractual test net (left-skewed / right-skewed / symmetric-control / zone-color) before Section 2 ΔES Score Gap zones in `endgameZones.ts` are recalibrated to the asymmetric tuples in `reports/benchmarks-latest.md §3.4.4` (`conv (-0.11, 0.00)`, `parity (-0.04, +0.04)`, `recov (+0.01, +0.11)`, `skill (-0.03, +0.03)`). Decision (from explore conversation): keep 0 at the bullet chart's visual center as the engine-neutral anchor; do NOT recenter on band midpoint — the band is drawn offset when the tuple is asymmetric. JSDOM oklch normalization quirk handled with a `normalizeOklch` helper. Gates: vitest 435/435, lint/knip clean, `MiniBulletChart.tsx` diff is comments-only. Commits `def43998` + `6af7754b` + `b7ea35c6` (worktree merge). Previously: 2026-05-15 — Completed Phase 87.1 Plan 03 (frontend per-type Score Gap row on `EndgameTypeCard`). Three atomic commits on the phase branch: `4e61ad71` (types) mirrors Plan 02's 5 new schema fields onto the TS `EndgameCategoryStats` interface (`type_achievable_score_gap_{mean,n,p_value,ci_low,ci_high}: number | null`) with a one-block comment documenting the dual-label rule (user-facing "Score Gap" / "Endgame Type Score Gap", internal `achievable_score_gap` for grep with Phase 85.1). `2a426422` (RED) adds 6 failing tests to `EndgameTypeCard.test.tsx` for the asg testid sub-elements, DOM ordering (gauges → asg row → WDL bar), zone tinting both signs + neutral, and sigmoid-bias caveat presence. `0379f78b` (GREEN) wires the new row: imports `ScoreGapRow` (named export) from `EndgameOverallScoreGapRow.tsx`, imports `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX` from `@/generated/endgameZones` (Plan 01 output) and `ZONE_DANGER/SUCCESS` from `@/lib/theme`; derives `gapMean / gapN / showGapRow / gapFormatted / gapColor / gapLevel` from `category.type_achievable_score_gap_*` fields with the same zone-only tint helper as `EndgameOverallPerformanceSection` (Phase 85.1 D-04). Inserts a `<div data-testid="${tileTestId}-asg-bullet">` between the gauge row container and the WDL bar / Games link block, rendering `ScoreGapRow` with `label="Score Gap:"` (short form per D-02), `MetricStatPopover` `name="Score Gap"` plus the dual-label explanation prose ending with the mandatory sigmoid-bias caveat one-liner per D-09. Uses global `ENDGAME_TYPE_SCORE_GAP_NEUTRAL_MIN/MAX` rather than `PER_CLASS_GAUGE_ZONES[class].achievable_score_gap` per RESEARCH Open Question #1 (all per-class bands shipped as ±0.05 placeholders in Plan 01 anyway; switch is one-line if §3.4.2 benchmark calibration produces variation). One deviation: jsdom normalizes `oklch(0.50 ...)` to `oklch(0.5 ...)`, so the zone-tint test assertions use a `normalize` helper to strip trailing zeros from decimal fractions before string compare (Rule 1 bug). Gates: `npx tsc --noEmit` clean, `npm test -- --run EndgameTypeCard` 17/17 (11 pre-existing + 6 new), `npm test -- --run` full frontend suite 414/414 across 37 files (zero regressions), `npm run lint` clean, `npm run knip` clean, `npm run build` clean (5.31s). SUMMARY: `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-03-SUMMARY.md`. HUMAN-UAT: `87.1-HUMAN-UAT.md` (375px mobile parity, sigmoid-caveat copy review, sign-convention visual check). Next: Plan 04 (LLM payload + prompt v29) running in parallel finalizes Wave 2; then Phase 87.1 verify-work. Previously: 2026-05-15 — Completed Phase 87.1 Plan 02 (backend math + repo + service + schema). Two atomic commits on the phase branch: `884589d8` (repo) extends `query_endgame_entry_rows` from 6 to 8 columns by appending `next_entry_eval_cp` + `next_entry_eval_mate` via `LEAD(entry_eval) OVER (PARTITION BY game_id ORDER BY span_min_ply ASC)` over the existing per-span subquery — adds `func.min(GamePosition.ply).label("span_min_ply")` to `span_subq` (free against `ix_gp_user_endgame_game` since ply is the index sort key), then wraps with a `span_with_next` subquery applying the LEAD window. Terminal spans return NULL on the new columns; existing callers (`_aggregate_endgame_stats`, `_compute_score_gap_material`) use attribute access on Row so the extra columns are silently ignored — no regression. `be0150b0` (service) wires the per-class per-span gap math: new `_compute_span_gap(entry_eval_cp, entry_eval_mate, next_entry_eval_cp, next_entry_eval_mate, result, user_color) -> float | None` helper computes `gap = exit_score - ES_entry` with sign convention matching the page-level Achievable Score Gap (positive = user outperformed Stockfish baseline). Transitory span uses `ES_sigmoid(next_entry_eval, user_color)` via `eval_cp_to_expected_score`/`eval_mate_to_expected_score` (mate saturation conventions preserved); terminal span uses game-result score via new module-level `_GAME_RESULT_TO_SCORE: dict[Literal["win","draw","loss"], float] = {"win": 1.0, "draw": 0.5, "loss": 0.0}` (CLAUDE.md no-magic-numbers, Literal keys so ty narrows). NULL-eval span (both entry fields NULL) returns None → excluded from cohort per D-07. `_aggregate_endgame_stats` loop body restructured to iterate `for row in rows:` and branch on `isinstance(row, tuple)` so it absorbs both 8-column SA Rows (prod) and 6-tuple legacy test fixtures (next-eval padded NULL); per-class gap vectors accumulate into `gaps_by_class: dict[EndgameClass, list[float]]` and pass through `compute_paired_difference_test` (the helper Phase 85.1 SEC1-10 uses for the page-level metric — same n-gates: p_value at n>=10, CI at n>=2, mean always returned). 5 new fields populated on `EndgameCategoryStats` (`type_achievable_score_gap_mean/_n/_p_value/_ci_low/_ci_high`); dual-label per CONTEXT D-02 (user-facing "Score Gap" / "Endgame Type Score Gap", internal `achievable_score_gap` for grep with Phase 85.1). `mean` surfaced as None when n==0 so the frontend hide-row gate has a clean sparse signal. 18 new tests: `TestComputeSpanGap` (9 — NULL entry eval, pure transitory hand-computed reference, terminal win/draw/loss, mate at entry, mate at next-entry, mate precedence, sign-convention anchor), `TestAggregateEndgameStatsTypeScoreGap` (9 — field presence, n=0 all-NULL, NULL-eval exclusion preserves WDL count, p-value gate at n=10, hand-computed reference matches to 1e-6, per-class independence, mate at endpoint, 6-tuple legacy regression guard). Gates: `uv run ruff check` clean, `uv run ty check app/ tests/` clean, `uv run pytest` 1514 passed / 6 skipped (342 plan-scope). SUMMARY: `.planning/milestones/v1.17-phases/87.1-per-span-delta-es-metric-for-endgame-types/87.1-02-SUMMARY.md`. Next: Plan 01 (benchmarks skill + zone codegen, Wave 1) before Wave 2 (Plan 03 frontend card row, Plan 04 LLM payload + prompt v29). Previously: 2026-05-14 — Completed quick task 260514-i3l: consolidated the 6 tooltips in the Endgame Overall Performance section into a single shared `<MetricStatTooltip>` body + `<MetricStatPopover>` shell with a uniform 4-paragraph anatomy (bold metric name + 1-2 sentence explanation / value line / verdict + statistical confidence / italic grey methodology). Vocabulary split by metric semantic — 5 score-based metrics use strength / weakness / difference; the eval-based Endgame Entry Eval keeps advantage / disadvantage / deviation. All 6 call sites migrated (Cards 1+3 via `EndgameOverallCard.tsx`, Card 2 in `EndgameOverallEntryCard.tsx`, gap rows in `EndgameOverallPerformanceSection.tsx`); existing test IDs + ARIA labels preserved verbatim. `ScoreGapPopover.tsx` deleted (knip-orphaned); the other 3 legacy popovers retained for external consumers. New `MetricStatTooltip.test.tsx` covers 21 tests including all 4 headline shapes per vocabulary, signed vs unsigned value-line, pValue=null branch, lastPlayedAt branch, and the ported D-10 forbidden-framing contract. Gates: frontend test 373/374 (1 pre-existing AchievableScorePopover failure unrelated, confirmed broken on `main`), lint/knip/build clean. Commits `95e08dfa` (TDD red→green) + `5919c62b` (migrate + delete) + `45f8faca` (worktree merge). Previously: 2026-05-12 — Completed quick task 260512-p5p: Simplify three overcomplicated SQL queries from first principles, following PR #90 precedent. Branch `refac/simplify-overcomplicated-db-queries`. Three independent atomic refactors: (1) consolidated `query_transposition_counts` + `query_transposition_wdl` + `query_resulting_position_wdl` into one `query_resulting_position_wdl` in `openings_repository.py` — `openings_service.get_next_moves` now derives `count = w+d+l` and drops one round trip; (2) flattened `query_position_wdl_batch` in `stats_repository.py` — eliminated the `DISTINCT (full_hash, game_id)` dedup subquery, replaced with a single `SELECT … COUNT(DISTINCT game_id) FILTER (...) … MAX(played_at) … GROUP BY`; (3) dropped the `dedup_select.cte("dedup")` from `query_opening_phase_entry_metrics_batch` (a planner-tuning workaround), replaced with a plain subquery joined directly into the aggregation; the load-bearing partition invariant `eval_n + mate_n + null_eval_n + outlier_n == phase_entry_row_count` preserved. Structural guardrails: compiled SQL no longer contains `WITH ` (Task 3) nor `SELECT DISTINCT` subqueries (Task 2). Backend ruff/ty clean, 1388 passed / 6 skipped. Commits `77dfdb86` + `830ec0b2` + `1c51e188` + `0824dfa8` (worktree merge). Post-deploy follow-up: tunnel into prod and EXPLAIN ANALYZE `query_opening_phase_entry_metrics_batch` for user 84 to confirm planner stays in same zone as PR #90's `query_opening_transitions`. Previously: 2026-05-10 — Completed quick task 260510-ugj: LLM endgame prompt scope last-3-month language. Tightened `app/prompts/endgame_insights.md` so the LLM no longer uses vague temporal anchors ("currently", "recently", "now", "today", "lately", "of late", "the recent window", "right now", "presently", "at the moment") when quoting `last_3mo` values — explicit "last 3 months" anchor now required (those numbers aren't visible on the dashboard). Vague present-tense framings remain acceptable for `all_time` aggregates (the gauge values the user sees). Replaced the within-noise legal-frames "Currently sitting at X%" / "Stable at X% over the recent window" with explicit-anchor variants. Strengthened the stale-combos rule from a framing advisory ("treat as historical") to a hard prohibition: no numeric field of a stale window (mean, n, trend, std, bucket counts) appears in the narrative — both `all_time` and `last_3mo` lines of a stale series are off-limits. `_PROMPT_VERSION` bumped `endgame_v23` → `endgame_v24` so cached reports invalidate. ty + 24 prompt-related tests pass without modification. Commits `2a13c2e7` (prompt + version), `d32a48f4` (worktree merge). Previously: 2026-05-10 — Completed quick task 260510-p6i: Apply Linux SCHED_IDLE to Stockfish UCI subprocesses (kernel preempts the moment Uvicorn / Postgres has runnable work, so import-eval can saturate all 4 vCPUs without API/Postgres latency hit) and bump `STOCKFISH_POOL_SIZE` default 3→4 in `.prod.env` plus `:-2`→`:-4` in `docker-compose.yml`. New `_sched_idle_preexec` + `_engine_popen_kwargs` helpers in `app/services/engine.py` splatted into both `popen_uci` call sites; `sys.platform == "linux"` guard so dev-on-macOS keeps default scheduling; `OSError` swallowed inside the preexec to fail-soft if a containerised host blocks the syscall via seccomp. RAM: 4 × 64 MB hash = 256 MB on 7.6 GB box. Backend gates: ruff/ty clean on engine.py, pytest `-k "engine or eval"` 81 passed / 6 skipped. Whole-app `ruff format --check app/` reports 16 files with pre-existing drift (already in deferred items). Post-deploy manual verification: `docker compose exec backend ps -eo pid,cls,pri,ni,comm | grep stockfish` should show 4 rows with `CLS=IDL`. Commits `2a94734b` + `5d14dc43`, merged via `95ea87c0`. Previously: 2026-05-08 — Completed quick task 260508-r61: Score info icons in MoveExplorer Move list + last-played date in all WDL score tooltips. Backend (commit `14def5bd`): added MAX(played_at) as last_played_at to four query paths — query_next_moves, query_wdl_counts (parent FEN parent score on Openings stats-board, replacing the planned query_resulting_position_wdl), query_opening_transitions (insights white+black), and the OpeningStatsCard path; propagated through NextMoveEntry / WDLStats / OpeningInsightFinding Pydantic schemas, openings_service / opening_insights_service / stats_service mappers, and TS types (api.ts, insights.ts, stats.ts). Frontend (commit `c5fd7312`): new `frontend/src/lib/relativeDate.ts` (Just now / X minutes/hours/days/weeks/months/years ago, with absolute date in `title`); WdlConfidenceTooltip extended with `lastPlayedAt: string | null` prop — renders "Last played: 3 days ago" between confidence line and italic methodology block, omits when null; ScoreConfidencePopover threads the prop through to all four call sites (MoveExplorer per-move, Openings.tsx stats-board position, OpeningFindingCard, OpeningStatsCard). MoveExplorer (commit `c2483929`): replaced hover-only `<Tooltip>` on Score % with new ScoreInfo component — HelpCircle popover mirroring TranspositionInfo pattern (Radix Popover, 100ms hover delay desktop, tap toggles mobile, `e.stopPropagation()` on trigger so icon tap doesn't play the move), `data-testid="move-explorer-score-info-${move_san}"`; troll/sunglasses icon converted to same popover (fixes mobile-tap-selects-row bug); selected-row class `bg-foreground/10` → `bg-foreground/10!` (Tailwind v4 important suffix) so selection highlight beats inline score-zone tint on green/red rows. Tests added: WdlConfidenceTooltip.test.tsx, relativeDate.test.ts, MoveExplorer popover behavior tests; backend tests for last_played_at in query_next_moves and query_opening_transitions. Backend gates: ruff/ty/pytest 1268 passed / 6 skipped. Frontend gates: lint/knip clean, 311 tests passed, build clean. One out-of-scope follow-up logged in SUMMARY: synthetic OpeningWDL rows in Openings.tsx:buildBookmarkRows lack played_at because the time-series endpoint doesn't carry it; bookmark cards silently omit the line. Previously: 2026-05-08 — Completed quick task 260508-q1z: auto-load cached LLM endgame report on Endgames page mount and filter change. Backend: GET /insights/endgame/cached mirrors the Tier-1 structural cache lookup (`get_latest_successful_log_for_user` + `get_latest_completed_import_with_games_at` staleness check + `_maybe_strip_overview`), returns 200/cache_hit or 404. Never invokes `compute_findings`, never the LLM, no rate-limit accounting; non-default filters and custom opponent gaps just naturally 404 (no `_validate_full_history_filters` gate). Frontend: extracted shared `buildInsightsParams` helper from `useEndgameInsights`, added `useCachedEndgameInsights` TanStack `useQuery` hook (returns the cached response on 200, `null` on 404 silently, no retry, 5-min staleTime, no refetchOnWindowFocus). Endgames.tsx fires the hook with `appliedFilters` and upserts hits into the existing `insightsCache` so `matchingInsights` renders cached reports without a Generate click — POST/Generate path untouched. Tests: 6 backend tests (`TestCachedEndpoint` covering auth, cache_hit with `compute_findings` spy, miss, non-default filter 404, custom-gap 404, import-invalidation 404) + 2 frontend hook tests (200 returns response, 404 returns null silently). Backend `uv run pytest` 1267 passed / 6 skipped, frontend `npm test` 291 passed, ruff/ty/eslint/tsc/knip clean. Previously: 2026-05-08 — Completed quick task 260508-dcp: neutralize non-significant zone fonts on every Openings subtab (Score %/Eval text only paints red/green when confidence is `'medium'` or `'high'`, not `'low'`, AND value falls in colored zone), pass `barColor="neutral"` to the Moves "current position" bullet + all six Endgames Stats MiniBulletCharts, and recolor the categorical neutral board arrow from blue to transparent grey via new `ARROW_NEUTRAL` theme token (`#6B7280`). New `frontend/src/lib/significance.ts` exports `isConfident(confidence)` — gate keys on the categorical bucket so the underlying p-value thresholds in `scoreConfidence.ts` (currently medium <0.05, high <0.01) can move without touching call sites. `DARK_BLUE` constant kept (now re-exports `ARROW_NEUTRAL`) so all `=== DARK_BLUE` categorical equality checks keep working — name change deferred to a future quick task. Frontend pipeline green: lint, knip, build, 283 tests (one redundant artificial low-data test deleted). Commits `912aa058`, `fb8ca37a`, `4a67a901`, `4a409726`. Previously: 2026-05-07 — Completed quick task 260507-t4r: Add score bullet chart to Openings Stats and Insights tabs (SEED-011). Previously: Phase 80.1 (4/4 plans). Plan 80.1-04 closed the regression matrix: backend pytest 1254 passed / 6 skipped / 1 deselected in 16.47s, ty + ruff clean, dedicated transposition tests 16 passed in 0.30s; frontend npm test 281 passed in 1.51s, lint + knip clean, build success in 4.99s. D-09 prod sanity check (read-only via `bin/prod_db_tunnel.sh`) on 3 representative users — heavy (4: 23,225 games), light (18: 261 games), moderate (35: 1,450 games) — verdict PASS: pre 40/2/16, post 40/3/14, all within the planner's 50–200% band, no zero-flip, no recalibration warranted (D-07 honored). New `scripts/prodcheck_80_1.py` committed for future regression checks. CHANGELOG entry queued under `## [Unreleased]` → `### Changed` for v1.16 milestone close. Project-wide ruff format drift (89 files) logged in `deferred-items.md` as out-of-scope follow-up. Commits `a35514f2`, `4e4fa514`, `ab29a3d9`, `7bb10cc0`, `47fe2dc5`. Phase 80.1 ready for PR. Next: open PR for Phase 80.1, then queue Phase 81.
| 2026-05-04 | fast | Mute opening row when total games < 20; drop confidence-based muting | ✅ |
| 2026-05-04 | fast | Score zone color in Moves tab + per-move list; drop severity row tint; bump mobile games-count font size | ✅ |
| 2026-05-15 | fast | Calibrate Section 2 ΔES Score Gap zones to asymmetric bands per §3.4.4: conv (-0.11, 0.00) / parity (-0.04, +0.04) / recov (+0.01, +0.11) / skill (-0.03, +0.03); regenerate frontend/src/generated/endgameZones.ts; update 2 EndgameMetricCard test fixtures whose values were keyed to the old (-0.05, +0.05) placeholders. Commit 0d11ac2e | ✅ |
| 2026-05-16 | fast | Standardize Cpu icon on Endgame page eval-derived metric rows: drop from "Eval at Endgame Entry" card title, move Entry Eval icon from value-suffix to label-prefix, add Cpu label prefix to Achievable Score, Achievable Score Gap, Endgame Score Gap (per-type Score Gap from Phase 87.1 already conformed). Commit 816e1463 | ✅ |
| 2026-05-28 | fast | Raise import `_BATCH_SIZE` 12→30 and rewrite the stale OOM comment (the Phase 41.1 per-batch Stockfish eval pass it blamed was moved to the decoupled cold-drain lane; writes use asyncpg COPY; 2026-05-27 20k-each prod stress test showed the import phase nowhere near memory-bound). Commit bf3f3df4 | ✅ |

## Operator Next Steps

- Start the next milestone with /gsd-new-milestone

| 61 | Fix flaky prod-tunnel safety-gate test (mock socket probe) | 2026-05-29 | 8d209930 | — |
| 260529-ptv | Percentile badge tooltips (endgame page): moved the calculated value out of bullet 2's trailing "Your value: x" clause and into bullet 1's first sentence ("Your recent {metric} {value} is in the bottom X% of ~{anchor}-rated players in {tc}"), gated on the value being present. Reformatted every chip value as signed integer percent (`Math.round(v*100)%`) via a simplified `formatChipValue(v)` (dropped the unused `flavor` param and the `.toFixed(2)` ΔES-family branch) so the chip value matches the adjacent metric tooltip's percent form (was "+0.04", now "+4%"); also applies to the aggregated per-TC breakdown list ("+5% over 137 games"). Per-TC bullet 2 now gates only on nGames. Frontend-only: `PercentileChip.tsx` + `PercentileChip.test.tsx` (updated/added assertions). Gates: vitest 54/54 (chip) + 83 card tests, eslint + knip + tsc clean. Commit `3ae66b2d` on branch `fix/percentile-tooltip-value-in-sentence` (off main; not pushed). | 2026-05-29 | 3ae66b2d | — |
| 260529-une | Declutter the Endgame Type Breakdown cards by removing the two Conversion/Recovery gauges from each `EndgameTypeCard` (both the live render and the empty-state shell, which now shows only "Not enough data yet"). Kept one card per type (no TC split) with WDL bar + Games link, Score bullet, and Score Gap bullet intact. Decision (from `/gsd-explore` 260529, note `endgame-type-card-drop-gauges.md`): the original idea — split type cards into TC-specific cards keeping only Score Gap — was rejected because the benchmark (`benchmarks-latest.md` lines 1064/1100) shows the per-class per-span ΔES Score Gap is the ONE metric that collapses across TC (Cohen's d≈0.13), while Conv/Recov are the metrics that keep separate by TC (d≈1.19–1.67). So a TC split would have multiplied cards 5→20 on the axis the surviving metric is flat across, dropping the metrics that actually justify a split, and worsened sparsity (Score Gap needs ≥20 spans/user/class). Dropping the gauges instead kills the §3.4.1 TC-mispaint by removal AND declutters — no TC dimension needed. Consciously overrides benchmark lines 1022 ("keep all 3 signals") and 1102 ("keep layout"), which predate this call. FRONTEND-ONLY: backend per-type conv/recov is load-bearing for the LLM insights narration (`insights_service.py` `_findings_conversion_recovery_by_type` consumes `cat.conversion.*` through `assign_per_class_zone`), so `category.conversion.*` stays on the `/stats` wire and the Python per-class bands in `endgame_zones.py` are untouched — the data just stops rendering on the cards. Removed unused frontend symbols from `EndgameTypeCard.tsx` (`EndgameGauge`, `PER_CLASS_GAUGE_ZONES`, `colorizeGaugeZones`, `PER_TYPE_GAUGE_SIZE`, `bands`/`convZones`/`recovZones`); `EndgameGauge` the component survives (still used by `EndgameMetricsByTcCard`), `colorizeGaugeZones` stays exported (still used elsewhere). `endgameZones.ts` already in knip's `ignore` list, so the now-dead `PER_CLASS_GAUGE_ZONES` export needed no new ignore. Updated 3 frontend tests (dropped gauge testid + conv/recov-value assertions, re-anchored the "Score Gap row is last" DOM-ordering test on the WDL block) and trimmed the Conv/Recov metric definitions from the breakdown-section h2 InfoPopover in `Endgames.tsx` (kept taxonomy + per-type descriptions + Score/Score Gap explainers). Gates: frontend lint + knip + 717 vitest tests pass; no backend files touched. Commits `d3453597` (feat) + `1de3d408` (tests/copy) + `104255a2` (worktree merge) on `main`; not pushed. | 2026-05-29 | 104255a2 | [260529-une-declutter-endgame-type-breakdown-cards-r](./quick/260529-une-declutter-endgame-type-breakdown-cards-r/) |
| 260530-pll | Make per-TC Endgame Metrics cards collapsible (Phase 98 Accordion pattern) with the primary-TC card expanded by default and the others collapsed; default the Endgame ELO Timeline to only the primary-TC series (both chess.com + lichess when the TC was played on both). Reused `computePrimaryTc` (the time-weighted heuristic placed in `lib/` during Phase 98) verbatim across the breakdown section, the new metrics section, and the timeline. Task 1: `EndgameMetricsByTcCard` div→`AccordionItem` (header band → `AccordionTrigger` with `metrics-tc-card-{tc}-trigger` testid + aria-label; body → `AccordionContent`); `EndgameMetricsByTcSection` → controlled `<Accordion type="multiple">` seeded from `computePrimaryTc(byTc, MIN_GAMES_PER_TC_CARD)`, reset on `filterKey` change; `Endgames.tsx` passes `filterKey={JSON.stringify(appliedFilters)}`. Task 2: replaced the timeline's `computeDefaultHidden` (active-weeks ratio + top-1-by-games) with `computeDefaultHiddenByPrimaryTc` — sums `per_week_total_games` per TC across both platforms, picks the primary TC, hides every combo whose `time_control` isn't primary (so both platform series of the primary TC survive); null-primary fallback shows all; removed now-dead `MAX_DEFAULT_VISIBLE` + `MIN_ACTIVE_WEEKS_RATIO`. Rule-1 fix: `EndgameMetricsByTcCard.test.tsx` render calls wrapped in `<Accordion>` since the card is now an `AccordionItem`. Gates: frontend lint + knip + build clean, 735/735 vitest. FRONTEND-ONLY, no backend/DB. HUMAN-UAT pending: verify default-expand + timeline default against real multi-TC data. Commits `3c7e1340` + `ee599f00` + `982901d7` on `main` (worktree merged via `f93a9441`); not pushed. | 2026-05-30 | f93a9441 | [260530-pll-make-the-tc-specific-endgame-metrics-car](./quick/260530-pll-make-the-tc-specific-endgame-metrics-car/) |
| 260530-rnz | Apply the Endgames-page header-band card style (recessed full-bleed `<h3>` with `bg-black/20 border-b border-border/40 px-4 py-3 text-base font-semibold`, body in a padded div below, `overflow-hidden` on the rounded wrapper) to cards across Overview, Openings, and Bookmarks — STYLE-ONLY, no Accordion/chevron/collapse. Task 1 (`GlobalStatsCharts.tsx` / `GlobalStats.tsx` / `PositionResultsPanel.tsx`): moved card-wrapping into `GlobalStatsCharts` so each `WDLCategoryChart` renders its own headered card, splitting "Results by Color" into a standalone card (alongside Results by Time Control + the two Rating cards); banded the shared `PositionResultsPanel` label (covers the "Results played as White/Black" card on BOTH the Moves and Games subtabs at once) and cleaned the duplicated `charcoal-texture rounded-md p-4` shell off its callers in `GamesTab.tsx` + `Endgames.tsx`. Task 2 (`OpeningStatsCard.tsx` / `OpeningFindingCard.tsx` / `StatsTab.tsx` / `ScoreChart.tsx`): banded the identical accent-card shells, relocating the score-zone `border-l-4` + `borderLeftColor` off the card root onto the inner content `<div>` so the left-border accent runs down the CONTENT only, not the header band (opacity-muting stays on the root); banded the "Bookmarked Openings: Score over Time" card via `ScoreChart`'s title. Section `<h2>`/`<h3>` headers (OpeningStatsSection, OpeningInsightsBlock FindingsSection) stay PLAIN — not banded — and the existing "show N more" toggles are untouched. Test surgery: updated `OpeningStatsCard.test.tsx` + `OpeningFindingCard.test.tsx` border-left assertions to read `borderLeftColor` from a new `*-content` testid (border moved off root), opacity tests still read the root; verified D-18 "no block-level h2" + four-section-header InfoPopover contracts still pass. Gates: frontend lint + knip clean, vitest 735/735. FRONTEND-ONLY, no backend/DB. HUMAN-UAT pending: visual check that bands render flush to rounded corners (no spill) on desktop + mobile and the accent runs only down the content area. Commits `a1433a2d` + `2824668a` + `a9ade8cf` on branch `quick/header-band-cards` off `main`; not pushed. | 2026-05-30 | a9ade8cf | [260530-rnz-apply-the-endgames-page-header-band-card](./quick/260530-rnz-apply-the-endgames-page-header-band-card/) |
| 260531-f7s | Redesign the endgame **Time Pressure** section to match the sibling collapsible full-width-row pattern (kills the 2×2-grid gaps that appear when collapsing all-but-most-active). Converted `EndgameTimePressureSection` from a `grid` of square cards to a controlled `<Accordion type="multiple">` seeded by `computePrimaryTc(byTc, …)` (the same time-weighted heuristic Phase 98 / quick 260530-pll use), reset on `filterKey` change, with `Endgames.tsx` passing `filterKey={JSON.stringify(appliedFilters)}`. `EndgameTimePressureCard` became an `AccordionItem`: collapsed header = TC name + game count + chevron ONLY (no summary metrics, identical to the sibling Endgame-Metrics-by-TC cards); expanded body = `flex-col lg:flex-row` 2-column split — `ScoreGapByTimePressureChart` left (`lg:basis-2/3`), the `ClockGapHeaderRow` + `NetFlagRateRow` gauges stacked right (`lg:basis-1/3`) — reusing the sibling's exact divider pair (`hidden lg:block w-px bg-border/40 mx-6` vertical on desktop, `block lg:hidden border-t border-border/40 my-4` horizontal on mobile). Existing gauge/chart pieces (`ClockGapHeaderRow`, `NetFlagRateRow`, `MiniBulletChart` Clock Gap bullet, `ScoreGapByTimePressureChart`) kept intact, just re-slotted. Deleted module-local `GRID_*` constants + IIFE. Tests rewritten in both `__tests__` suites (grid-layout assertions → accordion trigger testids / primary-TC-expanded / 2-column layout). FRONTEND-ONLY, no backend/DB. Decision lineage: `/gsd-explore` 260531 (conversation, no note file written) — gaps were a symptom of the 2×2 grid; full-width rows are the real fix, the internal 2-column split keeps gauge bars readable + the card short at full width. Gates: frontend lint + `npm test --run` 744/744 + `tsc --noEmit` + knip all clean. HUMAN-UAT pending: visual check of default-expand + 2-column→stacked separators on real multi-TC data, desktop + mobile. Commits `81c6c9a1` + `c4ca3e78` + `0b84877a` on branch `quick/endgame-time-pressure-collapsible-rows` off `main` (worktree merged via `37136dce`); not pushed. | 2026-05-31 | 37136dce | [260531-f7s-redesign-endgame-time-pressure-section-c](./quick/260531-f7s-redesign-endgame-time-pressure-section-c/) |
| 65 | Card-style Endgame Statistics Concepts + collapsible header hover state | 2026-05-30 | d7b7a13a | — |
| 66 | Remove concepts info icon; make Insights a collapsible card (open by default) | 2026-05-30 | 3c610ccc | — |
| 67 | Render insights 3 sections as plain headings/paragraphs (no inner cards) | 2026-05-30 | 98daa838 | — |
| 68 | Per-section insight slots as collapsible header-band cards (open by default) | 2026-05-30 | 32d38f66 | — |
| 260531-jga | Update all backend + frontend deps (incl. dev) to latest within current majors. Scope locked via AskUserQuestion: `uv lock --upgrade` honoring the deliberate caps (`pydantic-ai-slim<2.0`, `genai-prices<0.1.0`); frontend caret ranges bumped in-major only, `overrides`/Dependabot transitive pins preserved verbatim; no major bumps. Notable backend: fastapi 0.135.1→0.136.3, uvicorn 0.41.0→0.48.0, sqlalchemy 2.0.48→2.0.50, pydantic 2.12.5→2.13.4, sentry-sdk 2.54.0→2.61.0, google-genai 1.73.1→2.7.0, ty 0.0.26→0.0.40, ruff 0.15.5→0.15.15. Notable frontend: react 19.2.0→19.2.6, react-router-dom 7.13.1→7.16.0, vite 7.3.1→7.3.3, @tanstack/react-query 5.90.21→5.100.14, tailwindcss 4.2.1→4.3.0, knip 6.2.0→6.15.0. Code fixups: 3 ty errors from ty 0.0.40 (Sentry `Event` type in `app/main.py`, stale ignore + overload in `endgame_repository.py`, list invariance in `insights_llm.py`); eslint-plugin-react-hooks 7.1.1's new `react-hooks/set-state-in-effect` rule disabled globally (pre-existing intentional derive-from-server/filter-sync patterns) + removed now-stale per-line disables in `useDeepLinkHighlight.ts`; knip 6.15.0 surfaced 8 dead exports (removed) + 2 duplicate-export pairs (fixed). No pin-backs. Full local gate re-run by orchestrator and confirmed GREEN: ruff format/check + ty clean, pytest 2198 passed/16 skipped, frontend eslint clean + vitest 744/744 + build OK + knip clean. Commits `2d0fc592` (backend) + `2678b070` (frontend) + `3756afff` (stale CLAUDE.md Tech Stack callouts) on `main`; not pushed. | 2026-05-31 | 3756afff | [260531-jga-update-all-backend-and-frontend-dependen](./quick/260531-jga-update-all-backend-and-frontend-dependen/) |
| 260601-og7 | Cap the move explorer at ply 28 and rebuild the three Zobrist-hash indexes on `game_positions` as PARTIAL `WHERE ply <= 28` (SEED-033), reclaiming ~3 GB of index footprint with no data loss, no table rewrite, fully reversible. Single source of truth `MAX_EXPLORER_PLY = 28` in `app/models/game_position.py` ties the index `postgresql_where` predicate to every capped query and the frontend explorer cap (mirrored in new `frontend/src/lib/explorer.ts`). Migration copies the project's one existing CONCURRENTLY pattern (`80e22b38993a` autocommit_block + `postgresql_concurrently=True`) and uses a temp-name + `ALTER INDEX … RENAME` trick to rebuild each index partial under its canonical name with no ACCESS EXCLUSIVE lock; downgrade recreates the full non-partial indexes. Added `ply <= MAX_EXPLORER_PLY` to every `game_positions` hash equality/IN lookup (grep-driven coverage across `stats_repository`, `openings_repository`, `position_bookmark_repository`, `openings_service`). Seed-correction surfaced + verified: `query_wdl_counts` was NOT already bounded by `ix_gp_user_game_ply` (that index serves the opening-insights transition aggregation, not the `_build_base_query` hash join), so the predicate was added there too. Frontend `makeMove` hard-returns false at ply 28 (caps drag-drop + click-to-move + bookmark creation depth; invisible in practice — dev max bookmark depth 6). Migration verified reversible on dev (upgrade → downgrade → upgrade). Gates GREEN: ruff format/check + ty clean, pytest 2198 passed/16 skipped, frontend lint + vitest 745. **Task 5 prod bookmark-depth safety check DONE 2026-06-01 (PASS):** all 91 prod bookmarks resolve within the cap (reachable_within_cap=91, only_past_cap=0, deepest min_ply=10) — no undercount, no grandfather/degrade decision needed. Cleared to ship; the `CONCURRENTLY` migration applies on prod at `bin/deploy.sh` time. Commits `557fceed` + `1fcedea3` + `0948ab61` + `deb0b25f` + `b7735b38` on `main` (worktree merged via `6bb65b8d`); not pushed. | 2026-06-01 | 6bb65b8d | [260601-og7-cap-move-explorer-at-ply-28-and-partial-](./quick/260601-og7-cap-move-explorer-at-ply-28-and-partial-/) |

## Performance Metrics

| Phase | Plan | Duration | Notes |
|-------|------|----------|-------|
| Phase 99 P02 | 25 | 2 tasks | 6 files |
| Phase 99 P03 | 15 | 2 tasks | 18 files |
| Phase 99 P04 | 5 | 2 tasks | 2 files |
| Phase 100 P01 | 566 | 3 tasks | 3 files |
| Phase 100 P02 | 14min | 2 tasks | 4 files |
| Phase 102 P01 | 30 | 3 tasks | 5 files |

## Decisions

- [Phase ?]: rate_percentile field trio named distinctly from percentile star (D-01 coexistence)
- [Phase ?]: alembic_command.upgrade() in sync context — Alembic env.py calls asyncio.run() internally; _ensure_template_fresh returns template URL or None to signal migration need
- [Phase ?]: DROP DATABASE WITH (FORCE) at teardown: handles residual async connections from session-scoped pytest-asyncio fixtures
- [Phase ?]: seed_openings_for_tests in conftest.py ensures all xdist workers seed openings per-run DB
- [Phase ?]: sorted() required for set-based pytest.mark.parametrize: non-deterministic iteration causes xdist collection mismatch
- [Phase ?]: -n auto: 18.56s vs 40.29s serial (2.2x speedup); two concurrent serial runs both RC=0 (SC-1 + SC-3 satisfied)
- [Phase ?]: Phase 102 Plan 01: real net_timeout_rate scalar + 5-quintile Score-Gap-by-time chart block + pctl= annotations wired into LLM payload
