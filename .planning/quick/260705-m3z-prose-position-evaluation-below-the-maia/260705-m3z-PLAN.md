---
phase: quick-260705-m3z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/positionVerdict.ts
  - frontend/src/lib/positionVerdict.test.ts
  - frontend/src/components/analysis/MaiaMoveQualityBar.tsx
autonomous: true
requirements: [QUICK-260705-m3z]
must_haves:
  truths:
    - "At rest (evals loaded, moves graded), the text slot below the Maia move-quality bar shows a prose position verdict at the selected ELO instead of the static help text."
    - "The verdict sentence classifies the position as safe / tricky / highly difficult using badMass (mistakes + blunders only) with the exact 20% / 50% thresholds."
    - "Named move spans are interactive: a single hover (desktop) / tap (mobile) fires BOTH a severity-colored board arrow AND a tooltip showing Maia % + Stockfish eval."
    - "Hovering a bar SEGMENT still overrides the slot with that segment's move list and board highlights, exactly as before (prose is the resting state only)."
    - "When evals are not yet loaded or moves are still pending/ungraded, the slot falls back to the original help text."
  artifacts:
    - frontend/src/lib/positionVerdict.ts
    - frontend/src/lib/positionVerdict.test.ts
    - frontend/src/components/analysis/MaiaMoveQualityBar.tsx
  key_links:
    - "MaiaMoveQualityBar fires onHoverMovesChange([{san,color}]) on prose-span hover/tap; Analysis.tsx's existing qualityHoverArrows useMemo derives the arrow from the current position — no new prop threading required."
    - "positionVerdict.ts consumes perElo/selectedElo (Maia %) + qualityBySan (quality/evalCp/evalMate) already passed to the component."
---

<objective>
Replace the static resting help text below the Maia move-quality bar
("Hover a segment to list its moves and highlight them on the board.") with a
prose position evaluation at the selected ELO, per the locked /gsd-explore spec.

Purpose: give a human-readable "how hard is this position to play at my level"
verdict with interactive, hoverable/tappable move spans that draw a
severity-colored board arrow and show the move's Maia probability + Stockfish
eval — while preserving the existing bar-segment hover behavior.

Output: a new testable pure module `positionVerdict.ts` (+ unit tests) and the
prose UI wired into `MaiaMoveQualityBar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The component to modify + its exact inputs
@frontend/src/components/analysis/MaiaMoveQualityBar.tsx
@frontend/src/lib/moveQuality.ts
@frontend/src/hooks/useMaiaEngine.ts

# Existing eval-format + quality-word conventions to REUSE verbatim (do not re-derive)
@frontend/src/components/analysis/MovesByRatingChart.tsx

# Interactive-tooltip primitives (Tooltip suppresses touch; Popover supports tap)
@frontend/src/components/ui/tooltip.tsx
@frontend/src/components/ui/popover.tsx

# Theme color constants (import from here, never hard-code)
@frontend/src/lib/theme.ts

# Verify (do NOT change) the two call sites + the existing arrow mechanism already
# thread everything needed: onHoverMovesChange -> qualityHoverArrows derives the
# board arrow from `position`; selectedElo + qualityBySan already reach the bar.
@frontend/src/pages/Analysis.tsx
@frontend/src/components/analysis/MaiaHumanPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure verdict + formatting module with unit tests</name>
  <files>frontend/src/lib/positionVerdict.ts, frontend/src/lib/positionVerdict.test.ts</files>
  <behavior>
    - Verdict thresholds on badMass = summed Maia mass of mistakes + blunders ONLY (inaccuracies excluded):
      - badMass exactly 0.20 -> "tricky" (boundary is inclusive of tricky per 20% <= badMass <= 50%)
      - badMass just below 0.20 -> "safe"; badMass exactly 0.50 -> "tricky"; badMass just above 0.50 -> "difficult"
    - NAMED_MOVE_MIN_MASS = 0.08 floor filtering: a move at 0.08 is included, below 0.08 excluded.
    - safe: names GOOD moves above the floor, probability-descending; if zero good moves above floor -> only the verdict clause, no move list.
    - tricky/difficult: names BAD moves (mistake+blunder only, NOT inaccuracy) above the floor, descending, PLUS always the single 'best'-graded escape move with its Maia % even when below the floor.
    - escape-move-always-present: when a 'best'-graded move exists it appears in tricky/difficult output even at low Maia %; when NO 'best'-graded move exists, the escape clause is omitted (no crash).
    - Oxford-comma grammar over the named list: 1 -> "A", 2 -> "A and B", 3 -> "A, B and C", 4 -> "A, B, C or D" for the bad-move list (use "or" join for the bad list, "and" join for the safe good list — match the copy templates in the spec exactly).
    - Eval formatting (white POV): cp positive -> "+1.2", cp negative -> "-0.8" (one decimal), mate positive -> "M3", mate negative -> "-M2". (Reuse MovesByRatingChart.formatEvalText's cp convention but note the spec asks for "M3"/"-M2" mate notation, NOT "#3"/"#-3" — this module uses the M-notation; keep the chart tooltip's own "#" notation untouched.)
    - Robustness: bad list empty after flooring (mass spread thin) still yields a sensible verdict sentence (verdict clause renders; bad-move clause omitted or degrades gracefully).
  </behavior>
  <action>
    Create `frontend/src/lib/positionVerdict.ts` as a pure, worker-free module (no React). Export named constants SAFE_MAX_BAD_MASS = 0.20, TRICKY_MAX_BAD_MASS = 0.50, and NAMED_MOVE_MIN_MASS = 0.08 (no magic numbers anywhere). Export a `VerdictTier` union type ('safe' | 'tricky' | 'difficult') and a `computePositionVerdict(...)` function that takes the shown SANs, their per-SAN Maia probability at the selected ELO, and the per-SAN quality/eval map (mirror the shapes of bucketMovesByQuality inputs: perElo + selectedElo + shownSans + qualityBySan, OR accept a pre-derived {san, probability, quality, evalCp, evalMate}[] — pick the shape that keeps the component thin and the function easy to unit-test; if you take perElo/selectedElo, reuse the nearestByElo rung logic from moveQuality.ts rather than duplicating it — export/import it if needed). Return a structured result the component can render without further logic: the tier, and an ordered list of named moves each carrying {san, maiaPct (rounded int), role: 'good'|'bad'|'escape', textColor, arrowColor, evalCp, evalMate}. textColor: bad = its severity color (MOVE_QUALITY_MISTAKE / MOVE_QUALITY_BLUNDER), good = MOVE_QUALITY_GOOD, escape TEXT = MOVE_QUALITY_GOOD (light green, readable); arrowColor: escape = MOVE_QUALITY_BEST (dark green arrow), others = same as textColor. Import all colors from '@/lib/theme'. Compute badMass from mistakes + blunders ONLY. Apply the tier thresholds exactly (< SAFE_MAX_BAD_MASS = safe; SAFE_MAX_BAD_MASS..TRICKY_MAX_BAD_MASS inclusive = tricky; > TRICKY_MAX_BAD_MASS = difficult). Select + order named moves per the behavior block. Provide a small pure eval-formatter (cp -> "+1.2"/"-0.8", mate -> "M3"/"-M2") and a pure Oxford-comma list joiner (parameterized by "and"/"or" conjunction) — both exported for direct unit testing. Return a discriminated/nullable result (e.g. null or a `ready:false` sentinel) when there is nothing to narrate yet (no graded moves / totalMass 0) so the component knows to show the fallback help text — do NOT throw. Keep every function under the CLAUDE.md nesting/LOC limits. Then write `frontend/src/lib/positionVerdict.test.ts` (vitest) covering: verdict thresholds at exactly 0.20 and 0.50 and just-below/just-above each; floor filtering at exactly 0.08 and below; safe-with-no-bad-moves (good-only sentence); escape-move-always-present in tricky and difficult even at low Maia %; graceful omit when no 'best' move exists; Oxford-comma grammar for 1/2/3/4 moves (both "and" and "or" joins); eval formatting for cp positive, cp negative, mate positive, mate negative. NEVER inline a fenced code block in this action — the identifiers and signatures above are the contract.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/positionVerdict.test.ts</automated>
  </verify>
  <done>positionVerdict.ts exports the constants + computePositionVerdict + the eval/list formatters; all new vitest cases pass; no magic numbers; ty/tsc-clean shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire prose verdict + interactive move spans into MaiaMoveQualityBar</name>
  <files>frontend/src/components/analysis/MaiaMoveQualityBar.tsx</files>
  <action>
    Modify `MaiaMoveQualityBar.tsx` so the resting state of the text slot (currently the static help text at ~line 198) renders the prose verdict from `computePositionVerdict(...)` using the perElo/selectedElo/shownSans/qualityBySan props ALREADY passed to the component (no new props). Assemble the sentence from the module's structured result following the exact copy templates in the spec (safe / tricky / highly difficult), interpolating the selected ELO, each named SAN, and its rounded Maia %. Render each named SAN as an interactive inline span (semantic <button type="button">, not <div onClick>) colored with the move's textColor from the module result, at the text-sm floor (the prose body stays text-sm; only the tooltip body may use text-xs). Add data-testid="maia-position-verdict" on the prose container and data-testid={`maia-prose-move-${san}`} on each move span, plus aria-label on each span. Interaction: a single hover (desktop) / tap (mobile) on a move span must fire BOTH (1) the board arrow and (2) the tooltip, simultaneously. For the ARROW, REUSE the existing onHoverMovesChange mechanism: call onHoverMovesChange([{ san, color: arrowColor }]) on hover/tap-enter and onHoverMovesChange(null) on leave/dismiss — Analysis.tsx's existing qualityHoverArrows useMemo already derives the from/to arrow from the current board `position` and tints it by the passed color, so no FEN/Chess prop is needed. Use each move's arrowColor (escape = MOVE_QUALITY_BEST dark-green arrow, others = severity color). For the TOOLTIP, show the move's Maia % + Stockfish eval (from the module result's maiaPct + evalCp/evalMate, formatted via the module's eval formatter: "+1.2"/"-0.8", "M3"/"-M2"). Use an opt-in info-tooltip that works on BOTH mouse hover AND touch tap — the ui/tooltip.tsx primitive deliberately suppresses touch, so use ui/popover.tsx (Radix Popover) in a controlled/hover+click mode, or an equivalent tap-toggle, so mobile tap opens it and tapping elsewhere dismisses; text-xs is allowed inside the tooltip body per the CLAUDE.md exception. CRITICAL — coordinate the two hover sources so arrows never conflict: the EXISTING bar-segment hover (activeKey -> useEffect -> onHoverMovesChange with the segment's move list + board highlights) MUST still fully work and take over the slot when a segment is hovered; the prose verdict is the RESTING state only. Ensure prose-span hover and segment hover don't both drive onHoverMovesChange at once (e.g. clear prose-span active-move state when a segment becomes active, and vice versa) so a stale arrow set can't linger. FALLBACK: when computePositionVerdict returns its not-ready sentinel (evals not loaded / all moves pending/ungraded), keep rendering the ORIGINAL help text "Hover a segment to list its moves and highlight them on the board." exactly as today. Extract any new magic numbers/strings as named constants; import all colors from '@/lib/theme'. Keep the component within CLAUDE.md nesting/LOC limits — if the prose rendering grows past ~40 LOC of logic, extract a small helper (a `useXyz` hook or a pure render helper) rather than fattening the body. Apply once here (shared component reached by both desktop and mobile call sites); do NOT touch Analysis.tsx or MaiaHumanPanel — only VERIFY (by reading) both call sites still pass selectedElo + qualityBySan + onHoverMovesChange (they do). No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npm run build && npm run knip</automated>
  </verify>
  <done>At rest the bar's text slot shows the prose verdict with interactive move spans (data-testid="maia-position-verdict" + per-move spans); hovering/tapping a move span draws a severity-colored arrow and a Maia%+eval tooltip; bar-segment hover still lists the segment's moves + highlights; pending/unloaded state shows the original help text; lint, tests, tsc build, and knip all pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint` — clean.
- `cd frontend && npm test -- --run` — all tests pass, including new positionVerdict cases.
- `cd frontend && npm run build` — type-checks (props/types changed; esbuild lint/test do NOT type-check — this is the mandatory gate per CLAUDE.md).
- `cd frontend && npm run knip` — no new dead exports / unused deps.
- Manual (executor reads, does not need to run a browser): confirm Analysis.tsx desktop (~1216) and mobile (~1384) MaiaHumanPanel call sites are unchanged and still pass selectedElo, qualityBySan, onHoverMovesChange.
</verification>

<success_criteria>
- Prose position verdict replaces the static help text as the resting state, with correct safe/tricky/difficult thresholds (20% / 50% on mistakes+blunders mass) and the exact copy templates.
- Named move spans are interactive on both desktop (hover) and mobile (tap), firing a severity-colored board arrow AND a Maia%+eval tooltip together.
- Escape (best) move always named in tricky/difficult with its Maia % (light-green text, dark-green arrow); safe with no bad moves renders only the good-move sentence.
- Bar-segment hover behavior is preserved unchanged.
- Pending/unloaded state falls back to the original help text; no crashes on empty/edge inputs.
- Pure logic lives in positionVerdict.ts with unit tests; no magic numbers; all colors from theme.ts.
- lint + tests + build + knip all green.
</success_criteria>

<output>
Create `.planning/quick/260705-m3z-prose-position-evaluation-below-the-maia/260705-m3z-SUMMARY.md` when done.
</output>
