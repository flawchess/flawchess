---
phase: quick-260616-ey1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/imports.py
  - app/services/eval_queue_service.py
  - app/routers/admin.py
  - frontend/src/components/library/NoAnalysisState.tsx
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/library/EvalCoverageBadge.tsx
  - frontend/src/components/library/analysisCoverageCopy.tsx
  - tests/services/test_eval_queue.py
  - tests/routers/test_imports_tier1_enqueue.py
  - CHANGELOG.md
autonomous: true
requirements: [QUEUE-08-guest-tier1]

must_haves:
  truths:
    - "A guest can enqueue a tier-1 eval for their OWN game via POST /api/imports/eval/tier1/{game_id} and gets status 'enqueued'"
    - "A guest's explicit tier-1 job is drained by the worker (claimable via _claim_queued_job)"
    - "A guest is still excluded from automatic tier-3 bulk/idle analysis (lottery + residual fallback)"
    - "A guest hitting another user's game still gets a 404 (IDOR guard preserved)"
    - "A guest sees the 'Analyze' button on game cards (no longer 'Sign up to unlock analysis')"
    - "The coverage badge guest CTA reads 'Sign up to analyze all games'"
  artifacts:
    - path: "app/services/eval_queue_service.py"
      provides: "Tier-1 guest enqueue allowed; tier-1 worker drain allowed; tier-3 still guest-excluded"
    - path: "frontend/src/components/library/NoAnalysisState.tsx"
      provides: "Guests fall through to the Analyze/Pending/Analyzing path"
  key_links:
    - from: "app/routers/imports.py enqueue_tier1"
      to: "enqueue_tier1_game service"
      via: "guest passes through after IDOR guard"
      pattern: "enqueue_tier1_game"
    - from: "_claim_queued_job SQL"
      to: "guest tier-1 eval_jobs row"
      via: "u.is_guest = false OR ej.tier = 1"
      pattern: "ej.tier = 1"
---

<objective>
Open the QUEUE-08 guest gate for tier-1 (explicit, on-demand) Stockfish eval ONLY. A guest may analyze ONE game at a time via the per-game "Analyze" button → tier-1 enqueue → worker drain. Automatic tier-3 bulk/idle analysis stays gated to authenticated users.

Purpose: Product funnel — guests get a taste of analysis on demand; signup unlocks automatic full-library background analysis.
Output: Backend (router + service SQL), frontend (card CTA + badge copy), and tests updated. No migration. No schema change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Backend touchpoints
@app/routers/imports.py
@app/services/eval_queue_service.py
@app/routers/admin.py

# Frontend touchpoints
@frontend/src/components/library/NoAnalysisState.tsx
@frontend/src/components/results/LibraryGameCard.tsx
@frontend/src/components/library/EvalCoverageBadge.tsx
@frontend/src/components/library/analysisCoverageCopy.tsx

# Tests
@tests/services/test_eval_queue.py
@tests/routers/test_imports_tier1_enqueue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — allow guest tier-1 enqueue + worker drain (keep tier-3 excluded)</name>
  <files>app/routers/imports.py, app/services/eval_queue_service.py, app/routers/admin.py</files>
  <action>
Apply the LOCKED tier-1-only guest opening across three backend layers. Do NOT touch import_service.py. Do NOT add a migration.

In app/routers/imports.py `enqueue_tier1` (~line 325): REMOVE the `if user.is_guest: return EnqueueTier1Response(status="skipped_guest", ...)` early-return so a guest passes through to `enqueue_tier1_game`. KEEP the IDOR ownership guard above it unchanged (`game is None or game.user_id != user.id` → 404). Update the docstring: drop the "Guest users return skipped_guest (QUEUE-08 ...)" paragraph and replace with a note that tier-1 is an explicit per-game request available to guests for their OWN games, while automatic tier-3 analysis stays guest-gated.

In app/services/eval_queue_service.py `enqueue_tier1_game` (~line 598-619): change the guard from `if is_guest is None or is_guest:` to `if is_guest is None:` so a guest may enqueue their OWN game. KEEP the `is_guest is None` (missing/deleted user) branch returning False with the existing bug-fix comment intact. Update the docstring: it currently cites QUEUE-08 refusing guests — rewrite to state tier-1 is an explicit request a guest may make for their own game, and only a missing user row returns False.

In app/services/eval_queue_service.py `_claim_queued_job` SQL (~line 198): change `AND u.is_guest = false` to `AND (u.is_guest = false OR ej.tier = 1)` so a guest's explicit tier-1 job is drained by the worker. The `ej.tier` column is already selected in the candidate CTE (line 193). Update the "Guest exclusion: JOIN users + NOT is_guest filter (QUEUE-08)" docstring line to: guests are excluded EXCEPT for their own explicit tier-1 jobs.

In app/services/eval_queue_service.py `_claim_tier3_derived` (~line 330 user-pick `u.is_guest = false`, and ~line 415 residual-fallback `u.is_guest = false`): LEAVE BOTH UNCHANGED. Add a one-line inline comment at EACH of the two filters noting they are intentionally preserved (guests excluded from automatic bulk analysis; only tier-1 explicit is opened) so a future reader does not "consistency-fix" them to match `_claim_queued_job`.

In app/routers/admin.py `admin_enqueue_tier1` (~line 86-127): since `enqueue_tier1_game` no longer returns False for guests, the `else` branch mapping False+guest → "skipped_guest" is now dead for guests (a guest enqueue returns True/inserted). Cleanest consistent handling: simplify the status logic to `enqueued` when inserted else `already_queued` — drop the is_guest lookup and the `skipped_guest` branch from this admin path. Update the docstring line that says "Returns 'skipped_guest' when the game belongs to a guest user (QUEUE-08)" to reflect that guests now enqueue normally. Do NOT change the `EnqueueTier1Response` schema — the Literal status set still includes "skipped_guest" (the value simply is no longer produced here). If removing the is_guest lookup leaves the `User` import unused in admin.py, leave the import if still used elsewhere; remove only if it becomes truly unused (ruff will flag it).

Honor Sentry rules (no new except blocks here), ty-compliance (keep the `Literal[...]` annotation on `enqueue_status` valid), and no-magic-numbers (none introduced).
  </action>
  <verify>
    <automated>uv run ty check app/ && uv run ruff check app/routers/imports.py app/services/eval_queue_service.py app/routers/admin.py</automated>
  </verify>
  <done>enqueue_tier1 router has no guest early-return; enqueue_tier1_game returns False only on missing user; _claim_queued_job SQL includes `OR ej.tier = 1`; both _claim_tier3_derived filters unchanged with preservation comments; admin path simplified; ty + ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — guests get the Analyze button; relabel coverage CTA</name>
  <files>frontend/src/components/library/NoAnalysisState.tsx, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/EvalCoverageBadge.tsx, frontend/src/components/library/analysisCoverageCopy.tsx</files>
  <action>
In frontend/src/components/library/NoAnalysisState.tsx (~line 65-77): REMOVE the `if (isGuest) { return <Button ...>Sign up to unlock analysis</Button>; }` block so guests fall through to the SAME path as authenticated users (the pulsing pill states and the "Analyze" tier-1 button). Update the component's docstring "Branches" list to drop the isGuest branch. After removal, `isGuest` is no longer read in the component body — remove the `isGuest` prop from `NoAnalysisStateProps` (and its JSDoc) and from the destructured params. Remove the now-unused `useNavigate`/`navigate` import and the `react-router-dom` import IF they become unused (they are only used by the removed guest branch — verify nothing else in the file uses `navigate`). Keep the `Cpu` import (still used by Analyze button + pill).

In frontend/src/components/results/LibraryGameCard.tsx: remove the `isGuest={isGuest}` prop passed to `NoAnalysisState` at the two call sites (~line 617 and ~line 742). The `const isGuest = profile?.is_guest ?? false;` at line 223 may still be used by EvalCoverageBadge — check; if `isGuest` becomes entirely unused in LibraryGameCard after removing the two NoAnalysisState props, remove the `const isGuest` line too (knip/ESLint no-unused-vars will flag it). Do NOT remove it if EvalCoverageBadge or any other JSX in this file still consumes it.

In frontend/src/components/library/EvalCoverageBadge.tsx (~line 106): change the button text from `Sign up to analyze` to exactly `Sign up to analyze all games`. KEEP the `isGuest && isBelowThreshold` gating, the `data-testid="btn-coverage-signup"`, the `variant="brand-outline"`, and the aria-label. Update the inline comment above the block (~line 95-97) so it still reads correctly (guests can now analyze one game on demand via the card "Analyze" button; the badge CTA promotes automatic ALL-games background analysis on signup).

In frontend/src/components/library/analysisCoverageCopy.tsx: review `ANALYSIS_COVERAGE_INFO_COPY`. It already mentions automatic background analysis of recent games AND the per-game "Analyze" button. For guests the key benefit to make clear is that signup analyzes ALL imported games automatically in the background with no per-game clicking. If the current copy does not convey "automatic, all games, background, no clicking" for a guest reader, tighten it minimally (one short clause). Respect popover-copy-minimalism (WHAT only, no jargon, no caveats) and em-dash-sparingly. Do NOT add new exported symbols (knip).

Honor frontend rules: data-testid preserved on the touched button, text-sm floor (no text-xs added), theme.ts (no new hard-coded semantic colors), and mobile parity — the two NoAnalysisState call sites in LibraryGameCard are the desktop+mobile renderers, so removing the prop at BOTH keeps them in sync.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run knip</automated>
  </verify>
  <done>Guests render the Analyze button (no signup-for-analysis button); `isGuest` prop removed from NoAnalysisState and both call sites; badge button text is "Sign up to analyze all games"; ESLint + knip clean (no dead exports/unused vars).</done>
</task>

<task type="auto">
  <name>Task 3: Tests — flip guest tier-1 assertions; preserve tier-3 exclusion</name>
  <files>tests/services/test_eval_queue.py, tests/routers/test_imports_tier1_enqueue.py</files>
  <action>
In tests/services/test_eval_queue.py `TestGuestExclusion.test_guest_exclusion` (~line 511-557): keep part (a) — the tier-3 derived pick must STILL never return the guest's game (assertion unchanged). FLIP part (b): a guest's `enqueue_tier1_game` must now return True (inserted), a tier-1 `eval_jobs` row must exist for the guest game, AND that job must be claimable by the worker via `_claim_queued_job` (call it directly in a fresh session and assert it returns a row whose game_id == guest_game_id, confirming the `OR ej.tier = 1` worker path drains the guest's explicit job). Update the docstring and the class docstring (~line 509) to state tier-1 explicit guest enqueue is allowed while tier-3 stays excluded. Split into clearly-named sub-assertions/comments if it improves clarity. Mirror the existing test's setup/teardown (`_insert_game`, `_delete_games`, `monkeypatch` of `async_session_maker`); look at `_claim_queued_job` usages elsewhere in this file for the correct call signature (worker_id, lease_ttl_seconds) and clean up any leased job rows in the `finally` if needed.

In tests/services/test_eval_queue.py `test_tier3_guest_excluded_from_lottery` (in class `TestTier3Lottery`, ~line 877-917): confirm it remains UNCHANGED and still passes (tier-3 lottery still excludes guests). Do not modify it.

In tests/routers/test_imports_tier1_enqueue.py: the file docstring (line 7) references `test_tier1_guest` asserting "skipped_guest", but no such test function exists. Add (or update if present) a `test_tier1_guest` that POSTs as a guest user to their OWN game and asserts 200 + status "enqueued" (and that an eval_jobs row exists). Then assert the IDOR guard still applies to a guest: a guest POSTing to a game owned by a DIFFERENT user gets 404 with no row inserted (reuse the pattern in `test_tier1_idor`). Update the file docstring line 7 to describe the new guest behavior ("guest enqueues their own game → 200 'enqueued'; guest on another user's game → 404"). Reuse existing fixtures/helpers in the file for seeding a guest user and committed game rows; follow the existing httpx AsyncClient + ASGITransport pattern noted in the module docstring.

Run the dev DB up first (docker compose -f docker-compose.dev.yml -p flawchess-dev up -d) so the per-run test DB template builds. Do NOT use bin/reset_db.sh.
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_eval_queue.py::TestGuestExclusion "tests/services/test_eval_queue.py::TestTier3Lottery::test_tier3_guest_excluded_from_lottery" tests/routers/test_imports_tier1_enqueue.py -x</automated>
  </verify>
  <done>test_guest_exclusion asserts guest tier-1 enqueue=True + row exists + claimable, while tier-3 pick still excludes the guest; test_tier3_guest_excluded_from_lottery still passes; test_tier1_guest asserts 200 "enqueued" for own game and 404 for another user's game; targeted suite green.</done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG + full pre-PR gate</name>
  <files>CHANGELOG.md</files>
  <action>
Add a terse, user-facing bullet under `## [Unreleased]` in CHANGELOG.md, in `### Changed` (create the subsection if absent under Unreleased), noting that guest users can now analyze individual games on demand while automatic full-library analysis remains a signup benefit. Reference quick task 260616-ey1. Em-dash sparingly.

Then run the FULL pre-PR gate (LOCKED) and resolve every dirty output before declaring done. Ensure the dev DB is up first: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`. Do NOT gate on bin/reset_db.sh.

Gate commands:
  - `uv run ruff format app/ tests/`
  - `uv run ruff check app/ tests/ --fix`
  - `uv run ty check app/ tests/`  (zero errors)
  - `uv run pytest -n auto -x`  (full backend suite)
  - `( cd frontend && npm run lint && npm test -- --run )`

If ruff format or --fix modifies files, that is expected — include the changes. Re-run any gate that reported changes until clean.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml -p flawchess-dev up -d && uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -n auto -x && ( cd frontend && npm run lint && npm test -- --run )</automated>
  </verify>
  <done>CHANGELOG.md has the Unreleased bullet referencing 260616-ey1; ruff format --check clean; ruff check clean; ty zero errors; full backend suite green; frontend lint + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/imports/eval/tier1/{game_id} | Authenticated (incl. guest) caller supplies a game_id; ownership must be enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ey1-01 | Information Disclosure (IDOR) | enqueue_tier1 router | mitigate | IDOR guard preserved unchanged: game is None or game.user_id != user.id → 404; covered by test_tier1_guest IDOR assertion (Task 3). |
| T-ey1-02 | Elevation of Privilege | guest opens tier-3 bulk drain | mitigate | Only `_claim_queued_job` opens for tier-1 (`OR ej.tier = 1`); both `_claim_tier3_derived` guest filters left intact with preservation comments; test_tier3_guest_excluded_from_lottery still passes (Task 1/3). |
| T-ey1-03 | Denial of Service | guest spams tier-1 enqueue | accept | Idempotent ON CONFLICT DO NOTHING on the active-job partial unique index caps a guest to one active job per game; no new resource amplification. Existing behavior. |
| T-ey1-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this task; gate is non-applicable. |
</threat_model>

<verification>
- Backend: ty zero errors, ruff clean, full `pytest -n auto -x` green.
- Frontend: ESLint + knip clean, `npm test -- --run` green.
- Guest tier-1 path drains end-to-end (enqueue True + claimable) per Task 3 service test.
- Tier-3 guest exclusion preserved (two filters unchanged + passing lottery test).
- IDOR 404 guard intact for guests.
</verification>

<success_criteria>
- A guest can enqueue and have a worker drain a tier-1 eval for their OWN game.
- Guests remain excluded from automatic tier-3 bulk/idle analysis.
- IDOR guard still 404s a guest on another user's game.
- Frontend: guests see "Analyze" on cards; coverage CTA reads "Sign up to analyze all games".
- CHANGELOG updated; full pre-PR gate green; no migration added.
</success_criteria>

<output>
Create `.planning/quick/260616-ey1-allow-guest-users-to-analyze-a-single-ga/260616-ey1-SUMMARY.md` when done.
</output>
