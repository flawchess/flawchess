---
phase: quick-260614-vy4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Welcome.tsx
  - frontend/src/lib/welcomeDismissal.ts
  - frontend/src/App.tsx
  - frontend/src/pages/Home.tsx
  - frontend/src/pages/__tests__/Welcome.test.tsx
autonomous: true
requirements: [SEED-047]

must_haves:
  truths:
    - "A first-time guest with 0 games lands on /welcome instead of /library/import"
    - "The Welcome page shows the honest guest-vs-signup value split (WDL/lichess-evals for guests; FlawChess deep Stockfish analysis only for signed-up users)"
    - "Proceed button hands off to the existing Import flow (/library/import)"
    - "Checking 'Don't show this again' sets a localStorage flag; a returning guest who dismissed it skips Welcome and lands on Import"
    - "Signed-up (non-guest) users never see /welcome"
  artifacts:
    - path: "frontend/src/pages/Welcome.tsx"
      provides: "Welcome explainer page with value-split table, Proceed CTA, dismissal checkbox"
    - path: "frontend/src/lib/welcomeDismissal.ts"
      provides: "localStorage get/set helpers for the welcome-dismissed flag"
  key_links:
    - from: "frontend/src/pages/Home.tsx"
      to: "/welcome"
      via: "Navigate when guest && 0 games && !dismissed"
      pattern: "welcome"
    - from: "frontend/src/pages/Welcome.tsx"
      to: "/library/import"
      via: "navigate on Proceed"
      pattern: "library/import"
---

<objective>
Add a frontend-only Welcome page that sits before Import for first-time guests, honestly explaining what guest games get (position/WDL analytics + lichess's own evals) versus what signing up unlocks (FlawChess deep Stockfish flaw analysis, cross-device access, data durability). A Proceed button hands off to the existing Import flow; a "Don't show this again" checkbox (localStorage-backed) lets returning guests skip straight to Import.

Purpose: Guests currently land on Import with no idea they get zero FlawChess Stockfish analysis, so the sign-up CTA has no teeth (SEED-047).
Output: New `/welcome` route + page component, a dismissal-flag helper, and the routing gate wiring in Home.tsx.

Scope guardrails (from SEED-047 — do NOT exceed):
- Separate `/welcome` route, NOT an in-place Import redesign or hybrid collapsible intro. Import page stays as-is.
- NO backend changes. NO new user-model "seen" field. NO 30-day reaper.
- Dismissal is a localStorage flag only (guests are device-bound via their localStorage token).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/seeds/SEED-047-guest-welcome-page.md
@./CLAUDE.md
@frontend/src/App.tsx
@frontend/src/pages/Home.tsx
@frontend/src/pages/Import.tsx
@frontend/src/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Welcome page component + dismissal-flag helper</name>
  <files>frontend/src/pages/Welcome.tsx, frontend/src/lib/welcomeDismissal.ts</files>
  <action>
Create `frontend/src/lib/welcomeDismissal.ts` exporting two functions: `isWelcomeDismissed(): boolean` (reads localStorage key `welcome_dismissed`, guards `typeof localStorage !== 'undefined'`, returns true only when the stored value is `'1'`) and `setWelcomeDismissed(dismissed: boolean): void` (sets the key to `'1'` or removes it). Define the key as a module-level named constant `WELCOME_DISMISSED_KEY = 'welcome_dismissed'` (no magic strings). Co-locate here rather than in useAuth so the flag survives independent of guest token lifecycle.

Create `frontend/src/pages/Welcome.tsx` exporting `WelcomePage`. Render a `<main data-testid="welcome-page">` using the same width/spacing wrapper pattern as Import.tsx (`mx-auto w-full max-w-2xl px-4 py-6 md:px-6 space-y-8`). Content, in order:

1. A heading and short intro paragraph: explain that FlawChess works fully as a guest for position/WDL, opening explorer, endgame and time-management analytics, and that lichess-imported games also carry lichess's own move evals. Keep tone soft per CLAUDE.md communication style (no hype, sparing em-dashes).

2. The value-split comparison — render the SEED-047 value-split table as a responsive layout (a real `<table>` is fine on desktop; ensure it reads cleanly on mobile — either a horizontally-safe table or stacked rows). Rows and the exact honest framing come from the seed's "Value Split to Communicate" table:
   - Opening explorer / WDL by position: guest ✓, signed-up ✓
   - Endgame analytics, time management: guest ✓, signed-up ✓
   - lichess games' built-in evals: guest ✓ (freebie), signed-up ✓
   - FlawChess deep Stockfish analysis (per-game blunder/mistake/flaw detection): guest ✗, signed-up ✓  ← this is the differentiator; emphasize it
   - Cross-device access: guest ✗, signed-up ✓
   - Data durability (no inactivity cleanup): guest ✗, signed-up ✓
   For the ✓/✗ cells use lucide `Check` and `X` icons with `aria-label` ("included" / "not included"); color them with theme constants (WDL_WIN for the check, a muted/WDL_LOSS tone for the cross) imported from `@/lib/theme` — never hard-code semantic hex/oklch in the component.

3. A short note on the compute/durability rationale, SOFT framing only: "We don't run our deep Stockfish analysis on guest games because inactive guest data may eventually be cleared." Do NOT say "deleted after 30 days" or state any hard deadline (the reaper does not exist — SEED-047 "Key Factual Corrections"). Sign-up messaging must frame promotion as in-place: signing up preserves your games, bookmarks and eval history (no "fresh account"/merge language).

4. A "Don't show this again" checkbox using `@/components/ui/checkbox` (Checkbox) with a `<label>`, `data-testid="welcome-checkbox-dont-show"`. Track its state in local component state.

5. A primary Proceed CTA: `Button` with `variant="default"` (brand brown — never hand-roll colors), `data-testid="welcome-btn-proceed"`, label "Continue to import". On click: if the checkbox is checked, call `setWelcomeDismissed(true)`; then `navigate('/library/import')` via `useNavigate`.

6. A secondary "Sign up free" affordance using `Button variant="brand-outline"` (NOT `variant="secondary"`), `data-testid="welcome-btn-signup"`, that mirrors the existing Import banner promotion path: `logoutForPromotion()` from `useAuth` then `window.location.href = '/login?tab=register'` (see Import.tsx lines ~296-302 for the exact pattern). Keep this lower-emphasis than Proceed.

All interactive elements need `data-testid` (kebab-case, `welcome-` prefixed) and semantic HTML. Minimum font size `text-sm`. Layout must be mobile-friendly. Extract any thresholds/limits into named constants (none expected here beyond the localStorage key).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>Welcome.tsx renders the value-split table with Check/X theme-colored icons, a dismissal checkbox, a brand-brown Proceed button navigating to /library/import, and a brand-outline Sign up button using the existing promotion path; welcomeDismissal.ts exports get/set helpers backed by localStorage. Lint and typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the /welcome route + guest routing gate</name>
  <files>frontend/src/App.tsx, frontend/src/pages/Home.tsx</files>
  <action>
In `frontend/src/App.tsx`, import `WelcomePage` and add a route inside the `<ProtectedLayout>` block (alongside `/library/*`, `/openings/*`, etc., around line 605-613): `<Route path="/welcome" element={<WelcomePage />} />`. The Welcome page requires an authenticated session (guest token), so it belongs under ProtectedLayout, not as a public route. Do not gate it with `ImportRequiredRoute` (that would redirect away anyone with 0 games — the opposite of what we want).

In `frontend/src/pages/Home.tsx`, update the authenticated routing gate (lines ~610-624). Currently: when `token` and profile loaded and `!hasGames` → `Navigate to '/library/import'`. Change so that when the user is a guest (`profile?.is_guest`) AND `!hasGames` AND the welcome flag is not dismissed (`!isWelcomeDismissed()` from `@/lib/welcomeDismissal`) → `Navigate to '/welcome'`. Otherwise keep the existing behavior (`hasGames ? '/library/games' : '/library/import'`). Non-guest new users and dismissed/returning guests continue to land on `/library/import` exactly as before. Import `isWelcomeDismissed` at the top of the file.

Do NOT change the `LibraryPage.tsx` `/library` → `/library/import` redirect — the Home gate is the single entry point that decides Welcome vs Import; LibraryPage handles only intra-Library subtab defaults. After the guest proceeds (or dismisses), they go directly to `/library/import` which behaves unchanged.

After editing, if Welcome.tsx introduces new exports that knip might flag, ensure `WelcomePage` is imported in App.tsx (it is, via the route) and the dismissal helpers are imported in Home.tsx — so knip sees them used.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run lint && npx tsc --noEmit && npm run knip</automated>
  </verify>
  <done>/welcome route resolves to WelcomePage under ProtectedLayout; a guest with 0 games and no dismissal flag is redirected to /welcome from Home; dismissed guests and non-guests go straight to /library/import; lint, typecheck, and knip all pass with no unused-export errors.</done>
</task>

<task type="auto">
  <name>Task 3: Tests for dismissal gate + Welcome render</name>
  <files>frontend/src/pages/__tests__/Welcome.test.tsx</files>
  <action>
Add `frontend/src/pages/__tests__/Welcome.test.tsx` covering the Welcome page and the dismissal helper. Use the existing frontend test setup (vitest + Testing Library; mirror an existing page test such as `src/pages/library/__tests__/LibraryPage.reroute.test.tsx` for render/router/mocking conventions — read it first for the project's render harness, query-client and auth-context mocking pattern).

Cover:
1. `welcomeDismissal`: `setWelcomeDismissed(true)` then `isWelcomeDismissed()` returns true; `setWelcomeDismissed(false)` then returns false. Clear localStorage between cases.
2. Welcome renders: the page container (`welcome-page`), the Proceed button (`welcome-btn-proceed`), and the dismissal checkbox (`welcome-checkbox-dont-show`) are present, and the value-split table mentions the FlawChess deep Stockfish analysis differentiator row.
3. Clicking Proceed without checking the box navigates to `/library/import` and does NOT set the dismissal flag; clicking Proceed with the box checked sets the flag AND navigates. Assert navigation via the router mock used by the reference test (e.g. a mocked `useNavigate`).

Keep tests focused and fast. Do not test the Home gate redirect end-to-end if it requires heavy profile/query mocking beyond what the reference test already provides — a unit test of the dismissal helper plus the Welcome interaction is sufficient for this quick task.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm test -- --run src/pages/__tests__/Welcome.test.tsx</automated>
  </verify>
  <done>Welcome.test.tsx passes: dismissal helper round-trips through localStorage; Welcome renders its key testids and the Stockfish-differentiator copy; Proceed navigates to /library/import and only sets the dismissal flag when the checkbox is checked.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint` clean
- `cd frontend && npx tsc --noEmit` zero errors
- `cd frontend && npm run knip` no unused exports/deps
- `cd frontend && npm test -- --run` full frontend suite green
- Manual sanity (HUMAN-UAT, optional): as a fresh guest with 0 games, hitting `/` lands on `/welcome`; Proceed → `/library/import`; checking "Don't show again" then revisiting `/` skips straight to Import.
</verification>

<success_criteria>
- New `/welcome` page exists, explains the honest guest-vs-signup value split per SEED-047 (zero FlawChess Stockfish compute for guests; lichess freebie evals acknowledged; soft durability framing with no hard deadline; in-place sign-up messaging).
- Guest with 0 games + no dismissal flag is routed to `/welcome`; Proceed hands off to existing Import; "Don't show again" + localStorage lets returning guests skip it; non-guests never see it.
- All frontend gates (lint, typecheck, knip, tests) pass. No backend changes, no new user-model field, no reaper.
</success_criteria>

<output>
Create `.planning/quick/260614-vy4-guest-welcome-page-explaining-guest-vs-s/260614-vy4-SUMMARY.md` when done.
</output>
