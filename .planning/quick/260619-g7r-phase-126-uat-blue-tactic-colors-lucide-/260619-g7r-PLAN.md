---
quick_id: 260619-g7r
title: "Phase 126 UAT: blue tactic colors, lucide-move-up icon, tactic tooltip explanations"
status: complete
date: 2026-06-19
---

# Quick Task 260619-g7r

Phase 126 UAT feedback for the tactic-motif feature (frontend-only).

## Tasks

1. **All tactic motif colors → blue.** In `frontend/src/lib/theme.ts`, collapse the
   six per-family `TAC_*` color constants (and their `_BG` variants) to the single
   indigo blue previously used only for pin/skewer (`oklch(0.68 0.16 240)`), mirroring
   the existing `FAM_NEUTRAL` collapse pattern. Constant names kept; only values
   changed. Applies everywhere `TAC_*` is consumed (chips, comparison grid, filter).

2. **Pin/skewer icon → `lucide-move-up`.** In `frontend/src/lib/tacticComparisonMeta.ts`,
   swap the `pin_skewer` family icon from `Minus` to `MoveUp` (import updated).

3. **Tactic comparison tooltip explains the tactic.** Add a one-line `definition`
   field to each `TacticFamilyDef` in `tacticComparisonMeta.ts`, and make the first
   paragraph of `TacticBulletPopover` (in `TacticComparisonGrid.tsx`) render the
   family-colored icon + bold label + definition, matching the flaw-tag tooltip
   format (`FlawBulletPopover`).

## Verification

- `tsc -b`, `eslint`, `knip` clean
- `vitest` library + lib suites pass
