---
phase: quick
plan: 260322-lh2
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Openings.tsx
autonomous: true
requirements: [BUG-FIX]
must_haves:
  truths:
    - "Moves tab WDL bar always shows full-position (Both) stats, matching the MoveExplorer move rows"
    - "WDL bar is consistent regardless of piece filter selection"
  artifacts:
    - path: "frontend/src/pages/Openings.tsx"
      provides: "Moves tab WDL bar using nextMoves.data.position_stats"
  key_links:
    - from: "frontend/src/pages/Openings.tsx (moveExplorerContent)"
      to: "useNextMoves hook"
      via: "nextMoves.data.position_stats"
      pattern: "nextMoves\\.data\\.position_stats"
---

<objective>
Fix the Moves tab WDL bar to always use full-position (Both) stats from the nextMoves endpoint, instead of the piece-filter-dependent gamesQuery stats.

Purpose: The MoveExplorer rows show WDL bars computed from fullHash (Both pieces). The summary WDL bar above them should match, but currently it uses gamesQuery.data.stats which respects the piece filter (Mine/Theirs/Both), creating an inconsistency.

Output: Consistent WDL display in the Moves tab regardless of piece filter setting.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/Openings.tsx
@frontend/src/hooks/useNextMoves.ts
@frontend/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Moves tab WDL bar data source</name>
  <files>frontend/src/pages/Openings.tsx</files>
  <action>
In the `moveExplorerContent` block (around line 419-433), replace the WDL bar rendering:

FROM:
```tsx
{gamesQuery.data && gamesQuery.data.matched_count > 0 && (
  <WDLBar stats={gamesQuery.data.stats} />
)}
```

TO:
```tsx
{nextMoves.data && nextMoves.data.position_stats.total > 0 && (
  <WDLBar stats={nextMoves.data.position_stats} />
)}
```

This switches the data source from `gamesQuery` (which respects piece filter / matchSide) to `nextMoves` (which always uses fullHash / Both). The `position_stats` field is type `WDLStats` -- same type WDLBar expects, so no interface changes needed.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Moves tab WDL bar uses nextMoves.data.position_stats instead of gamesQuery.data.stats. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Grep confirms no remaining `gamesQuery.data.stats` usage in moveExplorerContent
- `npm run build` succeeds
</verification>

<success_criteria>
The Moves tab WDL summary bar always displays full-position (Both) stats from the nextMoves endpoint, matching the individual move WDL bars in MoveExplorer, regardless of the piece filter setting.
</success_criteria>

<output>
After completion, create `.planning/quick/260322-lh2-moves-tab-always-uses-piece-filter-both-/260322-lh2-SUMMARY.md`
</output>
