---
phase: quick
plan: 260403-c83
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/analysis.py
  - app/main.py
  - app/schemas/analysis.py
  - frontend/src/hooks/useAnalysis.ts
  - frontend/src/hooks/useNextMoves.ts
  - frontend/src/api/client.ts
  - tests/test_auth.py
autonomous: true
must_haves:
  truths:
    - "Frontend calls /api/openings/* instead of /api/analysis/*"
    - "Backend serves /api/openings/positions, /api/openings/time-series, /api/openings/next-moves"
    - "All existing tests pass with the new URL prefix"
    - "OpenAPI docs show /api/openings/* routes with 'openings' tag"
  artifacts:
    - path: "app/routers/analysis.py"
      provides: "Router with prefix=/openings and tags=['openings']"
      contains: 'prefix="/openings"'
    - path: "frontend/src/hooks/useAnalysis.ts"
      provides: "Frontend hook calling /openings/positions"
      contains: "/openings/positions"
    - path: "frontend/src/hooks/useNextMoves.ts"
      provides: "Frontend hook calling /openings/next-moves"
      contains: "/openings/next-moves"
    - path: "frontend/src/api/client.ts"
      provides: "API client calling /openings/time-series"
      contains: "/openings/time-series"
  key_links:
    - from: "frontend/src/hooks/useAnalysis.ts"
      to: "app/routers/analysis.py"
      via: "/openings/positions POST"
      pattern: "/openings/positions"
    - from: "frontend/src/hooks/useNextMoves.ts"
      to: "app/routers/analysis.py"
      via: "/openings/next-moves POST"
      pattern: "/openings/next-moves"
    - from: "frontend/src/api/client.ts"
      to: "app/routers/analysis.py"
      via: "/openings/time-series POST"
      pattern: "/openings/time-series"
---

<objective>
Rename the API URL prefix from `/api/analysis` to `/api/openings` for consistency with `/api/endgames`.

Purpose: The analysis router serves openings-related endpoints (position WDL stats, next moves, time series). Naming it `/api/openings` aligns with the `/api/endgames` convention where the URL reflects the domain concept, not the operation.

Output: All three endpoints served under `/api/openings/*`, all frontend calls updated, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/routers/analysis.py
@app/main.py
@frontend/src/hooks/useAnalysis.ts
@frontend/src/hooks/useNextMoves.ts
@frontend/src/api/client.ts
@tests/test_auth.py
@app/schemas/analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename backend router prefix and update all references</name>
  <files>app/routers/analysis.py, app/main.py, app/schemas/analysis.py, tests/test_auth.py</files>
  <action>
1. In `app/routers/analysis.py`:
   - Change `prefix="/analysis"` to `prefix="/openings"`
   - Change `tags=["analysis"]` to `tags=["openings"]`
   - Update the module docstring to reference `/openings/positions`, `/openings/time-series`, `/openings/next-moves`

2. In `app/schemas/analysis.py`:
   - Update all docstring references from `/analysis/...` to `/openings/...` (there are 6 docstrings referencing the URL path)

3. In `tests/test_auth.py`:
   - Change both occurrences of `"/api/analysis/positions"` to `"/api/openings/positions"` (lines ~126 and ~157)
   - Update the two docstrings that reference `POST /analysis/positions` to `POST /openings/positions`

4. In `app/main.py` — no changes needed (it imports the `analysis` module by Python name and the URL prefix comes from the router itself).

Internal module names (analysis_service, analysis_repository, schemas/analysis) stay unchanged — they describe the operation, not the URL. Only the HTTP-facing prefix changes.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run pytest tests/test_auth.py -x -q</automated>
  </verify>
  <done>Backend serves endpoints at /api/openings/positions, /api/openings/time-series, /api/openings/next-moves. Auth tests pass with new URLs. OpenAPI tag is "openings".</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend API calls and verify full test suite</name>
  <files>frontend/src/hooks/useAnalysis.ts, frontend/src/hooks/useNextMoves.ts, frontend/src/api/client.ts</files>
  <action>
1. In `frontend/src/hooks/useAnalysis.ts`:
   - Change `'/analysis/positions'` to `'/openings/positions'`

2. In `frontend/src/hooks/useNextMoves.ts`:
   - Change `'/analysis/next-moves'` to `'/openings/next-moves'`

3. In `frontend/src/api/client.ts`:
   - Change `'/analysis/time-series'` to `'/openings/time-series'`

These are the only three frontend files that reference the `/analysis/` API path.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && npm run build --prefix frontend && cd /home/aimfeld/Projects/Python/flawchess && uv run pytest -x -q</automated>
  </verify>
  <done>Frontend calls /openings/* endpoints. Frontend builds cleanly. Full backend test suite passes. Zero remaining references to `/analysis/` as an API URL path in non-planning files.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `uv run pytest -x` — all backend tests pass
2. `npm run build --prefix frontend` — frontend builds without errors
3. `grep -r "'/analysis/" frontend/src/ app/routers/ app/schemas/ tests/` — returns zero matches (no stale URL references)
4. `uv run ruff check .` — no lint issues
5. `uv run ty check app/ tests/` — no type errors
</verification>

<success_criteria>
- All three endpoints respond at /api/openings/* (positions, time-series, next-moves)
- All frontend hooks call /openings/* instead of /analysis/*
- All tests pass (backend + frontend build)
- No remaining /analysis/ URL references in source code (excluding internal module names and .planning/ docs)
- OpenAPI docs (/docs) show routes under "openings" tag
</success_criteria>

<output>
After completion, create `.planning/quick/260403-c83-rename-api-analysis-to-api-openings-for-/260403-c83-SUMMARY.md`
</output>
