---
phase: quick-260413-qq0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/EndgamePerformanceSection.tsx
  - frontend/src/components/charts/EndgameScoreGapSection.tsx
  - frontend/src/pages/Endgames.tsx
autonomous: true
requirements: [QQ0-01, QQ0-02, QQ0-03]
must_haves:
  truths:
    - "Section heading reads 'Games with vs without Endgame' (desktop and mobile)"
    - "Desktop WDL row labels read 'Games with Endgame' and 'Games without Endgame'"
    - "Mobile WDL row labels read 'Games with Endgame' and 'Games without Endgame'"
    - "Endgame Score Difference is rendered inside the renamed section (not in EndgameScoreGapSection) as a MiniBulletChart when scoreGap data is provided"
    - "EndgameScoreGapSection no longer renders the score difference block; it still renders the material breakdown table"
    - "data-testid=\"score-gap-difference\" exists at the new location under EndgamePerformanceSection"
  artifacts:
    - path: "frontend/src/components/charts/EndgamePerformanceSection.tsx"
      provides: "Renamed section + renamed WDL row labels + new score-diff bullet chart block"
      contains: "Games with vs without Endgame"
    - path: "frontend/src/components/charts/EndgameScoreGapSection.tsx"
      provides: "Material breakdown table only (score-difference block removed)"
      contains: "material-table"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Pass scoreGapData prop into EndgamePerformanceSection"
      contains: "scoreGap={scoreGapData}"
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgamePerformanceSection"
      via: "scoreGap prop"
      pattern: "scoreGap=\\{scoreGapData\\}"
    - from: "EndgamePerformanceSection"
      to: "MiniBulletChart"
      via: "import + conditional render when scoreGap provided"
      pattern: "MiniBulletChart"
---

<objective>
Rename the "Endgame vs. Non-Endgame Games" section and its two WDL row labels, then
relocate the Endgame Score Difference display from `EndgameScoreGapSection` into
`EndgamePerformanceSection` (below the two WDL rows) as a `MiniBulletChart`.

Purpose: Consolidate the direct endgame-vs-non-endgame comparison (WDL rows + signed
score delta) into one visual unit, leaving `EndgameScoreGapSection` to focus purely on
the material-stratified breakdown table.

Output:
- Updated `EndgamePerformanceSection.tsx` with new label text, new optional `scoreGap`
  prop, and a bullet-chart row for the score difference.
- Updated `EndgameScoreGapSection.tsx` with the score-difference block removed.
- Updated `Endgames.tsx` composition that wires `scoreGapData` into
  `EndgamePerformanceSection`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/components/charts/EndgamePerformanceSection.tsx
@frontend/src/components/charts/EndgameScoreGapSection.tsx
@frontend/src/components/charts/MiniBulletChart.tsx
@frontend/src/types/endgames.ts
@frontend/src/pages/Endgames.tsx

<interfaces>
<!-- Key types and component contracts for this task. -->

From frontend/src/types/endgames.ts:
```typescript
export interface EndgamePerformanceResponse {
  endgame_wdl: EndgameWDLSummary;
  non_endgame_wdl: EndgameWDLSummary;
  overall_win_rate: number;
  endgame_win_rate: number;
  aggregate_conversion_pct: number;
  aggregate_conversion_wins: number;
  aggregate_conversion_games: number;
  aggregate_recovery_pct: number;
  aggregate_recovery_saves: number;
  aggregate_recovery_games: number;
  relative_strength: number;
  endgame_skill: number;
}

export interface ScoreGapMaterialResponse {
  endgame_score: number;
  non_endgame_score: number;
  score_difference: number;
  overall_score: number;
  material_rows: MaterialRow[];
}
```

From frontend/src/components/charts/MiniBulletChart.tsx:
```typescript
interface MiniBulletChartProps {
  value: number;
  neutralMin?: number;   // default -0.10
  neutralMax?: number;   // default  0
  ariaLabel?: string;
  heightClass?: string;
  valueHeightClass?: string;
}
```

Current EndgamePerformanceSection signature (to be extended):
```typescript
interface EndgamePerformanceSectionProps {
  data: EndgamePerformanceResponse;
  // NEW: optional — only render score-diff block when provided
  scoreGap?: ScoreGapMaterialResponse;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename section + labels and add score-diff bullet-chart row in EndgamePerformanceSection</name>
  <files>frontend/src/components/charts/EndgamePerformanceSection.tsx</files>
  <action>
In `frontend/src/components/charts/EndgamePerformanceSection.tsx`:

1. Add imports:
   - `import { MiniBulletChart } from '@/components/charts/MiniBulletChart';`
   - Extend the existing type import to also pull `ScoreGapMaterialResponse`:
     `import type { EndgamePerformanceResponse, EndgameWDLSummary, ScoreGapMaterialResponse } from '@/types/endgames';`

2. Extend `EndgamePerformanceSectionProps` with an optional prop:
   ```typescript
   interface EndgamePerformanceSectionProps {
     data: EndgamePerformanceResponse;
     scoreGap?: ScoreGapMaterialResponse;
   }
   ```

3. Update the destructuring in `EndgamePerformanceSection` to also accept `scoreGap`:
   `export function EndgamePerformanceSection({ data, scoreGap }: EndgamePerformanceSectionProps) { ... }`.
   (Leave `EndgameGaugesSection` untouched — it still only consumes `data`. If it is
   currently typed with the same props interface and that creates a `scoreGap` being
   "unused" in `EndgameGaugesSection`, give `EndgameGaugesSection` its own narrow
   `{ data: EndgamePerformanceResponse }` props interface to keep the two cleanly
   separated.)

4. Rename the section title from `Endgame vs. Non-Endgame Games` to
   `Games with vs without Endgame`. Also update the `InfoPopover ariaLabel` from
   `"Endgame vs. Non-Endgame Games info"` to `"Games with vs without Endgame info"`.
   Extend the existing InfoPopover body to add one short sentence noting the score
   difference bullet chart shown below — e.g. append: "The bar below shows the signed
   difference between your endgame score and non-endgame score (green = endgame
   stronger, red = endgame weaker, narrow grey band = near parity)." Keep it concise.

5. Rename the desktop WDL row labels:
   - `"Endgame games"` → `"Games with Endgame"`
   - `"Non-endgame games"` → `"Games without Endgame"`
   Apply to both `PerfWDLDesktopRow` label props (desktop block, lines ~93-104) AND
   the mobile `WDLChartRow` label props (lines ~107-119). Do NOT change the testIds
   (`perf-wdl-endgame`, `perf-wdl-non-endgame`) — they are behavioral anchors.

6. Add the score-difference bullet-chart block BELOW the two WDL comparison rows
   (i.e. after the closing `</div>` of the mobile `lg:hidden space-y-3` block, but
   still inside the top-level `space-y-4` wrapper). Only render it when `scoreGap`
   is provided. The block must:

   - Preserve the existing `data-testid="score-gap-difference"` on its outer wrapper.
   - Reuse the same three-column desktop row pattern as `PerfWDLDesktopRow`
     (`grid grid-cols-[minmax(0,1fr)_auto_minmax(120px,200px)] gap-3 items-center py-1.5`):
     - Left column: the label `"Endgame Score Difference"` plus a `MATERIAL_ADVANTAGE_POINTS`-agnostic
       `InfoPopover` (ariaLabel=`"Endgame Score Difference info"`, testId=`"score-gap-diff-info"`,
       side="top") with body text explaining: "Signed difference between your endgame score
       (wins + half draws) and your non-endgame score. Positive = you score higher in
       endgames; negative = you score lower. The narrow grey band around zero marks
       near-parity."
     - Middle column: signed formatted number (e.g. `+0.12` / `-0.04`) colored
       green when `>= 0` and red when `< 0` using the same `text-green-500 font-semibold`
       / `text-red-500 font-semibold` pattern from the old location. Use `tabular-nums
       whitespace-nowrap` so it aligns consistently.
     - Right column: `<MiniBulletChart>` with
       `value={scoreGap.score_difference}`, `neutralMin={-0.05}`, `neutralMax={0.05}`,
       and `ariaLabel={\`Endgame score difference: ${diffFormatted}\`}`.
   - Below the row, render a small caption (same structure as today, but in the new
     location): `<p className="text-xs text-muted-foreground">Endgame: {scoreGap.endgame_score.toFixed(2)} | Non-endgame: {scoreGap.non_endgame_score.toFixed(2)}</p>`.
   - Compute `const diffPositive = scoreGap.score_difference >= 0;` and
     `const diffFormatted = (diffPositive ? '+' : '') + scoreGap.score_difference.toFixed(2);`
     inside an `{scoreGap && (...)}` conditional so these locals are not evaluated
     when `scoreGap` is undefined.
   - For mobile (`<lg`), the 3-column grid shrinks to something readable: wrap the
     block inside a `<div>` that uses the same grid classes (the grid already flexes
     via `minmax(0,1fr)` / `minmax(120px,200px)`) so it works on both. No separate
     mobile variant is required — the grid accommodates narrow widths. If the right
     column clamps awkwardly below `lg`, drop the `minmax(120px,200px)` on small
     screens using a responsive class (e.g. `grid-cols-[minmax(0,1fr)_auto_minmax(80px,160px)] lg:grid-cols-[minmax(0,1fr)_auto_minmax(120px,200px)]`).

Keep existing exports, existing `EndgameGaugesSection`, and existing gauge zone
constants untouched. No behavior change to gauges.
  </action>
  <verify>
    <automated>cd frontend && npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - File compiles with no type errors under `noUncheckedIndexedAccess`.
    - Section heading is `Games with vs without Endgame`.
    - Desktop + mobile WDL labels read `Games with Endgame` / `Games without Endgame`.
    - When a `scoreGap` prop is passed, the section renders a labeled row containing
      the signed number and a `MiniBulletChart` with `neutralMin=-0.05`,
      `neutralMax=0.05`, plus the `Endgame: X.XX | Non-endgame: Y.YY` caption.
    - The outer wrapper of that new block carries `data-testid="score-gap-difference"`.
    - When `scoreGap` is undefined, the block is not rendered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove score-difference block from EndgameScoreGapSection</name>
  <files>frontend/src/components/charts/EndgameScoreGapSection.tsx</files>
  <action>
In `frontend/src/components/charts/EndgameScoreGapSection.tsx`:

1. Delete the "Score difference display" block (currently lines ~63-84, the
   `<div className="flex flex-col gap-1 rounded-md bg-muted/30 px-3 py-2" data-testid="score-gap-difference">`
   wrapper and its children, including the `diffPositive`/`diffFormatted` locals).

2. Because the section now only renders the material breakdown table, remove the
   now-unused locals:
   - `const diffPositive = data.score_difference >= 0;`
   - `const diffFormatted = (diffPositive ? '+' : '') + data.score_difference.toFixed(2);`

   Keep `const overallFormatted = data.overall_score.toFixed(2);` — still used in
   the table header.

3. Update the section heading. The heading is currently
   `Endgame Score Gap & Material Breakdown`. Since the score-gap portion has moved
   out, rename it to `Endgame Material Breakdown`, and update the InfoPopover text
   to drop the opening sentence about comparing endgame/non-endgame score
   (keep only the material-table-focused explanation about Conversion/Even/Recovery
   and the per-bucket neutral zones). Keep `testId="score-gap-section-info"` to
   avoid breaking anything that selects it, or rename to `material-breakdown-section-info`
   — search the frontend codebase first; if no selector references it, rename.

4. Keep the outer `data-testid="score-gap-section"` for now (changing it is out
   of scope — the ID is only the section wrapper, not a semantic claim about
   content). Leave `data-testid="material-table"` and the per-row
   `data-testid="material-row-${row.bucket}"` unchanged.

5. Review imports: `MiniBulletChart` is still used inside the table rows, so its
   import stays. `MaterialBucket` and `ScoreGapMaterialResponse` type imports
   still needed. `InfoPopover` still used. `MiniWDLBar` still used. No imports
   should need to be removed.

Do NOT touch the `NEUTRAL_ZONES` constant or the table rendering logic — the
material-breakdown behavior is unchanged.
  </action>
  <verify>
    <automated>cd frontend && npm run lint &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - The score-difference block (with `data-testid="score-gap-difference"`) no longer
      exists in `EndgameScoreGapSection.tsx`.
    - The material-breakdown table still renders identically (same columns, same rows,
      same testIds).
    - No dangling unused variables; `npm run lint` passes cleanly.
    - Section heading reads `Endgame Material Breakdown`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire scoreGapData into EndgamePerformanceSection in Endgames.tsx</name>
  <files>frontend/src/pages/Endgames.tsx</files>
  <action>
In `frontend/src/pages/Endgames.tsx` around line 248:

1. Change:
   ```tsx
   <EndgamePerformanceSection data={perfData} />
   ```
   to:
   ```tsx
   <EndgamePerformanceSection data={perfData} scoreGap={scoreGapData} />
   ```

2. Leave the subsequent block unchanged:
   ```tsx
   {scoreGapData && (
     <div className="charcoal-texture rounded-md p-4">
       <EndgameScoreGapSection data={scoreGapData} />
     </div>
   )}
   ```
   `EndgameScoreGapSection` still owns the material breakdown table, so it must
   keep rendering below.

3. Confirm `scoreGapData` is already in scope where `EndgamePerformanceSection` is
   rendered. Based on the snippet at lines 230-275, it is available as the guard
   condition for the sibling block. If for any reason it is only available inside
   a narrower conditional, widen the scope so it can be passed to the performance
   section. Passing `undefined` (when the fetch hasn't completed) is fine — the
   component treats it as optional.

4. No other changes in this file. Do not rename or move any other section; layout
   and ordering are preserved.
  </action>
  <verify>
    <automated>cd frontend && npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - `Endgames.tsx` passes `scoreGap={scoreGapData}` to `EndgamePerformanceSection`.
    - `EndgameScoreGapSection` still renders below, wrapped in its existing
      `charcoal-texture` panel.
    - Production build succeeds.
    - Manually opening the Endgames page shows: section titled "Games with vs without
      Endgame" containing the two WDL rows followed by the new "Endgame Score
      Difference" row with the bullet chart; the separate "Endgame Material Breakdown"
      panel renders below with the unchanged material table.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint` passes.
- `cd frontend && npx tsc --noEmit` reports zero errors.
- `cd frontend && npm run build` succeeds.
- `cd frontend && npm run knip` reports no new dead exports (the moved
  `MiniBulletChart` import should now be used in `EndgamePerformanceSection`;
  `EndgameScoreGapSection` still imports it for the table rows).
- Grepping for the old labels should return no matches in `frontend/src/`:
  - `"Endgame games"` (the bare label string)
  - `"Non-endgame games"`
  - `"Endgame vs. Non-Endgame Games"`
- Grepping for `data-testid="score-gap-difference"` must return exactly one match,
  located in `EndgamePerformanceSection.tsx`.
</verification>

<success_criteria>
- Section header reads "Games with vs without Endgame" on desktop and mobile.
- WDL row labels read "Games with Endgame" and "Games without Endgame" on both
  desktop (`PerfWDLDesktopRow`) and mobile (`WDLChartRow`) code paths.
- Endgame Score Difference is rendered as a `MiniBulletChart` below the two WDL
  rows inside `EndgamePerformanceSection`, with `neutralMin=-0.05` and
  `neutralMax=0.05`, and carries `data-testid="score-gap-difference"`.
- The caption "Endgame: X.XX | Non-endgame: Y.YY" still appears in the new location.
- `EndgameScoreGapSection` no longer contains the score-difference block; it renders
  only the material-breakdown table under the heading "Endgame Material Breakdown".
- All TypeScript, lint, and build checks pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260413-qq0-move-endgame-score-difference-into-games/260413-qq0-SUMMARY.md`.
</output>
