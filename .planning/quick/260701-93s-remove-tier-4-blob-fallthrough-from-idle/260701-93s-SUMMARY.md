---
quick_id: 260701-93s
title: Remove tier-4 blob fallthrough from the idle /lease path (SEED-072)
status: complete
date: 2026-07-01
commit: e85164c5
---

# Summary — 260701-93s

Implemented the corrected SEED-072 fix: removed the tier-4 (flaw-blob gating backfill)
fallthrough from `claim_eval_job(scope="idle")` in `app/services/eval_queue_service.py`.

## What changed

- `claim_eval_job(scope="idle")` now returns `None` (→ HTTP 204) once tier-3 is empty,
  instead of falling through to `_claim_tier4_blob`. Remote workers therefore fall to
  their dedicated rung-4 `/flaw-blob-lease`, the only post-146 path that writes blobs
  (MultiPV-2 continuation → `/flaw-blob-submit`).
- The bundled `scope=None` path **keeps** tier-4 — its only consumer is the in-process
  server-pool drain (`eval_drain.run_one_full_eval_tick`), which writes blobs via the
  MultiPV-2 pass (`_build_flaw_multipv2_blobs` → `_run_multipv2_pass`), not the broken
  `/submit` path. Documented inline.
- Docstring updated (`scope="idle" → tier-3 only`) with a SEED-072 rationale block.
- Added `test_idle_scope_returns_none_when_tier3_empty` asserting the idle scope returns
  `None` while the same NULL-blob game remains a valid tier-4 candidate for
  `_claim_tier4_blob`.

## Root cause (why the fix)

Phase 145 added the idle-scope tier-4 fallthrough; Phase 146 removed the inline server
walk that filled blobs on `/submit` (`_apply_submit` forces `blob_map={}`) but left the
routing. So tier-4 games were full-ply re-evaluated via `/lease` and re-submitted with no
blob written → re-served forever (~5:1 submit:completion), `/lease?scope=idle` never 204'd,
`/flaw-blob-lease` never fired, gating backfill stalled.

## Plan-time checks (resolved)

- `/flaw-blob-lease` (`eval_remote.py:755`) selects via `_claim_tier4_blob` directly →
  draining re-routed, not orphaned. `_claim_tier4_blob` stays live.
- Phase-146 D-01 recency ordering (inside `_claim_tier4_blob`) unchanged, still used by the
  blob-lease rung.
- Bundled `scope=None` tier-4 retained (in-process pool writes blobs correctly).
- Guests still excluded; tier-1/2/3 unchanged; blob shape / gate logic unchanged.

## Verification

- `uv run ruff format` / `ruff check --fix`: clean.
- `uv run ty check app/ tests/`: 0 errors.
- `uv run pytest -n auto tests/services/test_eval_queue.py tests/test_eval_queue_service.py
  tests/test_eval_worker_endpoints.py`: 94 passed (incl. new test).
- `uv run pytest -n auto tests/services/test_full_eval_drain.py`: 38 passed (bundled-path
  consumer, no regression).

## Post-deploy expectation (from seed acceptance)

- `/flaw-blob-lease` traffic appears in the backend access log; tier-4 blob-fill rate rises.
- Full-ply submit:completion ratio drops from ~5:1 toward ~1:1.

## Commit

- `e85164c5` — fix(quick-260701-93s): stop serving tier-4 blob games through the idle /lease path (SEED-072)
