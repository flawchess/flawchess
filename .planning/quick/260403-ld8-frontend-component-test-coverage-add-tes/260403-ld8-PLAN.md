---
phase: quick-260403-ld8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/api.test.ts
  - frontend/src/lib/pgn.test.ts
  - frontend/src/lib/utils.test.ts
  - frontend/src/lib/zobrist.test.ts
autonomous: true
requirements: [QUICK-ld8]
must_haves:
  truths:
    - "resolveMatchSide correctly maps all 6 mine/opponent/both x white/black combinations"
    - "pgnToSanArray correctly parses PGN strings into SAN arrays"
    - "Date formatting adapts to date range (month+year for >18 months, month+day otherwise)"
    - "Zobrist hashes match known backend test vectors for starting position, after 1.e4, after 1.e4 e5"
  artifacts:
    - path: "frontend/src/types/api.test.ts"
      provides: "resolveMatchSide unit tests"
    - path: "frontend/src/lib/pgn.test.ts"
      provides: "pgnToSanArray unit tests"
    - path: "frontend/src/lib/utils.test.ts"
      provides: "createDateTickFormatter and formatDateWithYear unit tests"
    - path: "frontend/src/lib/zobrist.test.ts"
      provides: "computeHashes and hashToString unit tests"
  key_links:
    - from: "frontend/src/lib/zobrist.test.ts"
      to: "frontend/src/lib/zobrist.ts"
      via: "import computeHashes, hashToString"
      pattern: "import.*computeHashes.*zobrist"
---

<objective>
Add meaningful unit tests for pure functions across the frontend codebase: match-side resolution, PGN parsing, date formatting utilities, and Zobrist hash computation.

Purpose: These pure functions contain critical business logic with well-defined I/O. Tests lock in correctness and prevent regressions. The Zobrist tests are especially valuable since hash correctness is the foundation of the entire platform — if frontend hashes diverge from backend, all position matching breaks silently.

Output: 4 new test files covering 4 modules with ~40-50 test cases total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/types/api.ts
@frontend/src/lib/pgn.ts
@frontend/src/lib/utils.ts
@frontend/src/lib/zobrist.ts
@frontend/src/lib/arrowColor.test.ts (example test style)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test resolveMatchSide and pgnToSanArray pure functions</name>
  <files>frontend/src/types/api.test.ts, frontend/src/lib/pgn.test.ts</files>
  <behavior>
    resolveMatchSide:
    - mine + white => 'white'
    - mine + black => 'black'
    - opponent + white => 'black'
    - opponent + black => 'white'
    - both + white => 'full'
    - both + black => 'full'

    pgnToSanArray:
    - "1. e4 e5 2. Nf3" => ["e4", "e5", "Nf3"]
    - "1. e4 c6 2. d4 d5 3. Nc3" => ["e4", "c6", "d4", "d5", "Nc3"]
    - "" => []
    - "1. e4" => ["e4"]
    - multi-digit move numbers: "10. Qd2 Bd7 11. O-O-O" parses correctly
    - extra whitespace handled
  </behavior>
  <action>
    Create `frontend/src/types/api.test.ts`:
    - Import `resolveMatchSide` from `@/types/api`
    - Test all 6 combinations of MatchSide x Color exhaustively
    - Use describe/it blocks matching the existing arrowColor.test.ts style

    Create `frontend/src/lib/pgn.test.ts`:
    - Import `pgnToSanArray` from `@/lib/pgn`
    - Test standard PGN, empty string, single move, multi-digit move numbers, extra whitespace
    - Verify castling notation (O-O, O-O-O) is preserved as-is in the SAN array
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/types/api.test.ts src/lib/pgn.test.ts</automated>
  </verify>
  <done>All resolveMatchSide and pgnToSanArray tests pass. 6 match-side tests cover every combination. PGN tests cover standard, edge, and empty cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test date formatting utilities and Zobrist hash computation</name>
  <files>frontend/src/lib/utils.test.ts, frontend/src/lib/zobrist.test.ts</files>
  <behavior>
    createDateTickFormatter:
    - Date span > 18 months => format "Jan '24" (month + abbreviated year)
    - Date span <= 18 months => format "Jan 5" (month + day)
    - Single date array => short format (0-day span)
    - Empty array => returns a formatter (does not throw)

    formatDateWithYear:
    - "2024-06-15" => "Jun 15, 2024" (locale-dependent but stable in test env)

    computeHashes (test vectors from zobrist.ts comments):
    - Starting position: whiteHash = 5858837776588196015n, blackHash = -3976252316203442281n, fullHash = 5060803636482931868n
    - After 1.e4: whiteHash = -6532466553307562974n, blackHash = -3976252316203442281n (black unchanged), fullHash = -9062197578030825066n
    - After 1.e4 e5: whiteHash = -6532466553307562974n (white unchanged), blackHash = 1839718147647814041n, fullHash = 595762792459712928n

    hashToString:
    - Converts bigint to string: 5060803636482931868n => "5060803636482931868"
    - Handles negative: -9062197578030825066n => "-9062197578030825066"
  </behavior>
  <action>
    Create `frontend/src/lib/utils.test.ts`:
    - Import `createDateTickFormatter` and `formatDateWithYear` from `@/lib/utils`
    - Test createDateTickFormatter with >18-month span, <18-month span, single date, empty array
    - Test formatDateWithYear with a known date string
    - The date formatter outputs are locale-dependent; use regex matchers or `toContain` checks for month abbreviations rather than exact string equality if needed

    Create `frontend/src/lib/zobrist.test.ts`:
    - Import `computeHashes`, `hashToString` from `@/lib/zobrist`
    - Import `Chess` from `chess.js`
    - Test starting position hashes against known test vectors (from the comments at top of zobrist.ts)
    - Test after 1.e4 (chess.move('e4')) — white hash changes, black hash stays the same
    - Test after 1.e4 e5 — black hash changes, white hash stays the same as after 1.e4
    - Test hashToString for positive and negative bigints
    - These are the most critical tests in this plan — Zobrist hash agreement between frontend and backend is what makes position matching work
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/utils.test.ts src/lib/zobrist.test.ts</automated>
  </verify>
  <done>All date formatting and Zobrist tests pass. The 3 known test vectors (starting position, after 1.e4, after 1.e4 e5) match exactly. Date formatter correctly adapts output to date range span.</done>
</task>

</tasks>

<verification>
Run the full frontend test suite to confirm no regressions and all new tests pass:
```bash
cd frontend && npm test
```
Expected: All test files pass (arrowColor.test.ts + 4 new files).
</verification>

<success_criteria>
- 4 new test files created, all passing
- resolveMatchSide: all 6 MatchSide x Color combinations covered
- pgnToSanArray: standard, empty, single-move, castling, whitespace edge cases
- createDateTickFormatter: range-adaptive formatting for >18mo and <=18mo spans
- computeHashes: 3 known test vectors match backend exactly (starting, after e4, after e4 e5)
- `npm test` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/quick/260403-ld8-frontend-component-test-coverage-add-tes/260403-ld8-SUMMARY.md`
</output>
