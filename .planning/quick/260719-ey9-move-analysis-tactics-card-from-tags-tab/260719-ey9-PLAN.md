---
quick_id: 260719-ey9
title: Move analysis tactics card to Eval tab; rename Tags tab to Stats
date: 2026-07-19
status: planned
---

# Quick Task 260719-ey9

## Goal

In the mid-range and mobile analysis-page layouts (both share the `analysisTabs`
tabbed panel in `frontend/src/pages/Analysis.tsx`):

1. Move the **tactics card** (Missed/Allowed/Context tags — `AnalysisTagsPanel`
   `section='tags'`) out of the Tags tab and render it at the **bottom of the Eval
   tab**, below the eval chart.
2. **Rename the Tags tab to "Stats"** and swap its lucide `Tag` icon for
   `ChartNoAxesColumn` (`chart-no-axes-column`). The renamed tab keeps only the
   MoveStats / accuracy card (`section='stats'`).

Desktop 3-column layout is unaffected (it already renders `'stats'` and `'tags'`
sections separately and does not use `analysisTabs`).

## Context

- `analysisTabs` is reused verbatim by both the mobile takeover body and the
  mid-range right column — editing it covers both layouts (CLAUDE.md mobile-parity).
- `tagsPanel(withHighlight, section)` already supports `'stats'` / `'tags'` slices.
- The Tags-tab gate `(evalChartReady || evalPending)` and the in-flight Analyzing
  pill behavior (quick 260719-dzh) must be preserved for the renamed Stats tab.

## Tasks

### Task 1 — Relocate tactics card + rename tab (Analysis.tsx)
- files: `frontend/src/pages/Analysis.tsx`
- action:
  - Import: replace `Tag` with `ChartNoAxesColumn` in the `lucide-react` import.
  - `evalTab`: append the tactics card at the bottom —
    `{evalChartReady && <div className="px-3">{tagsPanel(false, 'tags')}</div>}`.
  - `tagsTab` → rename local const to `statsTab`; render `tagsPanel(false, 'stats')`
    (stats only); keep the pending-pill branch. Change `TabsContent value="tags"`
    → `value="stats"`.
  - Tab trigger: `value="stats"`, `data-testid="analysis-tab-stats"`, icon
    `<ChartNoAxesColumn />`, label `Stats`.
  - Update the `{... && tagsTab}` reference in `analysisTabs` to `statsTab`.
- verify: `cd frontend && npx tsc -b && npm run lint`
- done: Eval tab shows tactics card at bottom; the renamed Stats tab shows only
  the accuracy/MoveStats card with the chart-no-axes-column icon.

### Task 2 — Update tests
- files: `frontend/src/pages/__tests__/Analysis.test.tsx`
- action: rename the three `analysis-tab-tags` testid assertions to
  `analysis-tab-stats`.
- verify: `cd frontend && npm test -- --run src/pages/__tests__/Analysis.test.tsx`
- done: Analysis test suite passes.

## Verification
- `cd frontend && npx tsc -b && npm run lint && npm test -- --run`
