---
quick_id: 260710-k7n
slug: engine-hero-homepage
date: 2026-07-10
status: planned
---

# Quick Task 260710-k7n: FlawChess Engine as homepage hero feature

## Description

Make the FlawChess Engine the hero feature on the public homepage (`frontend/src/pages/Home.tsx`) and add it as the lead feature in `README.md`. Copy finalized in a `/gsd-explore` session.

## Tasks

### Task 1 — Homepage hero (`frontend/src/pages/Home.tsx`)
- Import `ChessKnight` from `lucide-react`.
- Prepend a new entry to the `FEATURES` array so it becomes `FEATURES[0]` (the hero); `game-analysis` slides to the #2 slot, all other entries keep their order.
  - `slug: 'flawchess-engine'`, `icon: ChessKnight`, `heading: 'Your Best Practical Move'`, `imagePosition: 'right'`
  - screenshot: `/screenshots/flawchess-engine.png` (already present)
  - 3 bullets (engine value, trap/swindle finding, Stockfish+Maia + Play style dial)
- No layout changes: desktop right column + mobile charcoal section both read `heroFeature = FEATURES[0]`.
- 4th "Play the FlawChess bots" bullet deferred to SEED-091 — NOT added now.

### Task 2 — README (`README.md`)
- Add **FlawChess Engine** as the first bullet in the `## Features` list, matching the existing `— ` em-dash bullet style.

## Verify
- `cd frontend && npm run lint` passes
- `cd frontend && npx tsc -b` (build type-check) passes
- Hero renders "Your Best Practical Move" with the knight icon and 3 bullets.

## must_haves
- FEATURES[0].slug === 'flawchess-engine' with heading 'Your Best Practical Move' and ChessKnight icon
- README Features list leads with the FlawChess Engine bullet
- Frontend lint + build green
