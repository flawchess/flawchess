---
phase: quick-260722-r7t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/data/personaAvatarPrompts.md
  - scripts/gen_persona_avatars.py
  - pyproject.toml
  - uv.lock
  - frontend/src/lib/personas/personaAvatars.ts
  - frontend/src/components/bots/__tests__/PersonaCard.test.tsx
autonomous: true
requirements: [D-15, D-16, D-17]
must_haves:
  truths:
    - "personaAvatarPrompts.md is a self-contained generation source (D-15): each of the 24 descriptor lines carries its persona's bio, and the script reads ONLY the doc, never TypeScript."
    - "scripts/gen_persona_avatars.py parses all 24 personas from the doc, generates only personas with no existing webp, and exposes --limit / --logo-ref / --dry-run flags (D-16)."
    - "resolveAvatarSrc() resolves real art via an import.meta.glob lookup over assets/personas/*.webp keyed by persona id (D-17), falling back to the emoji placeholder when no webp exists — the delete-and-rerun curation loop still works."
  artifacts:
    - scripts/gen_persona_avatars.py
    - frontend/src/data/personaAvatarPrompts.md
    - frontend/src/lib/personas/personaAvatars.ts
  key_links:
    - "doc descriptor line format (- `persona-id` — Name the Species — notes) <-> the script's parser regex — must stay in lockstep or generation silently drops personas."
    - "assets/personas/{persona-id}.webp filename stem <-> PersonaId union <-> the glob key in resolveAvatarSrc."
---

<objective>
Ship the bot-persona avatar generation pipeline: (1) enrich the committed prompts
doc so it is the single source of truth (weave each persona's bio into its
descriptor line), (2) add `scripts/gen_persona_avatars.py` (Nano Banana image
generation via pydantic-ai), and (3) swap the `resolveAvatarSrc()` seam from a
field read to a Vite `import.meta.glob` lookup over `assets/personas/*.webp`.

The avatar image files themselves are NOT generated here — the user runs the
script with their own `GOOGLE_API_KEY`. This task ships the enriched doc, the
script, and a seam that gracefully falls back to emoji until webp files land.

Purpose: turn the Phase-183 placeholder-avatar scaffolding into a working,
repeatable generate-curate-swap loop (D-15/D-16/D-17).
Output: enriched `personaAvatarPrompts.md`, `gen_persona_avatars.py`, Pillow dev
dep, and a glob-backed `resolveAvatarSrc()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/data/personaAvatarPrompts.md
@frontend/src/lib/personas/personaRegistry.ts
@frontend/src/lib/personas/personaAvatars.ts
@frontend/src/components/bots/PersonaCard.tsx
@frontend/src/components/bots/__tests__/PersonaCard.test.tsx
@scripts/gen_persona_calibration.py

# Verified facts (do not re-derive):
# - pydantic-ai-slim is installed at 1.104.0. The CURRENT spelling is
#   Agent(..., capabilities=[NativeTool(ImageGenerationTool(...))]). The older
#   builtin_tools=[ImageGenerationTool(...)] still works but emits a
#   DeprecationWarning (consumed via consume_deprecated_builtin_tools_as_capabilities).
#   Use the capabilities=[NativeTool(...)] form; verify against the installed
#   package before finalizing.
# - Imports available at top level: from pydantic_ai import Agent, NativeTool,
#   BinaryImage, BinaryContent ; from pydantic_ai.native_tools import ImageGenerationTool
# - ImageGenerationTool(aspect_ratio='1:1', size='1K') — both are valid Literals
#   in 1.104 (size accepts '1K'; aspect_ratio accepts '1:1').
# - Agent(output_type=BinaryImage) returns a BinaryImage in result.output.
# - GOOGLE_API_KEY is read automatically by pydantic-ai's GoogleProvider from the
#   environment. Project convention (app/core/config.py):
#   from dotenv import load_dotenv; load_dotenv() — python-dotenv is already a dep.
# - Pillow is NOT currently a dependency. frontend/public/icons/logo-256.png exists.
# - frontend/src/assets/personas/ does NOT exist yet — the script creates it.
# - Existing tests touching the seam: PersonaCard.test.tsx (an avatarSrc-present /
#   avatarSrc-absent pair) and personaRegistry.test.ts (asserts no persona ships
#   an avatarSrc field — stays TRUE, do not change it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich prompts doc so it is the single generation source (D-15)</name>
  <files>frontend/src/data/personaAvatarPrompts.md</files>
  <action>
Per D-15, make `personaAvatarPrompts.md` self-contained so the Python script never
parses TypeScript. For EACH of the 24 per-persona descriptor bullets under
"## Per-Persona Descriptors", weave that persona's bio (from `personaRegistry.ts`,
all 24 bios are in the Read context) into the descriptor notes. Keep the exact
line-start shape `- ` + backtick-wrapped id + ` — ` + Name the Species + ` — ` +
notes intact so a regex parser can extract it; append the bio into the notes
portion after the existing demeanor/accessory notes (a continuation of the same
bullet, wrapping across lines is fine). Do NOT change any persona id, name, or
species — copy the bios verbatim.

Also refresh the framing prose now that the script + seam exist (do NOT delete the
per-style demeanor notes or the master style prompt): the intro paragraph and the
"## Notes for the Future Real-Art PR" section currently claim this is
placeholder-only and that "This phase intentionally ships zero Vite asset-import
machinery" — both are now false. Rewrite them terse to describe the real workflow:
`scripts/gen_persona_avatars.py` reads THIS doc, generates a webp per persona that
has no existing `frontend/src/assets/personas/{persona-id}.webp`, and the
delete-and-rerun curation loop (tune a bad avatar by editing the doc line, deleting
the webp, rerunning). `resolveAvatarSrc()` now globs `assets/personas/*.webp`.
  </action>
  <verify>
    <automated>test $(grep -cE '^- `(attacker|trickster|grinder|wall)-(800|1000|1200|1400|1600|1800)` — ' frontend/src/data/personaAvatarPrompts.md) -eq 24</automated>
  </verify>
  <done>All 24 descriptor bullets keep the parseable prefix and now include the persona's bio; the intro and future-PR notes no longer claim placeholder-only / zero-glob.</done>
</task>

<task type="auto">
  <name>Task 2: Add scripts/gen_persona_avatars.py + Pillow dev dep (D-16)</name>
  <files>scripts/gen_persona_avatars.py, pyproject.toml, uv.lock</files>
  <action>
Add Pillow as a dev dependency first: `uv add --dev pillow` (updates
pyproject.toml [dependency-groups].dev and uv.lock).

Create `scripts/gen_persona_avatars.py` following existing scripts/ conventions
(module docstring with Usage examples like gen_persona_calibration.py, argparse,
_REPO_ROOT = Path(__file__).resolve().parent.parent, explicit type annotations,
from __future__ import annotations, async where natural). The script:

1. Loads env via `from dotenv import load_dotenv; load_dotenv()` (matches
   app/core/config.py) so GOOGLE_API_KEY resolves from the project .env.
2. Parses frontend/src/data/personaAvatarPrompts.md:
   - the Master Style Prompt block (the > blockquote),
   - the per-style demeanor notes (Attacker/Trickster/Grinder/Wall),
   - the 24 per-persona descriptor bullets via a regex on the
     "- `persona-id` — Name the Species — notes" shape (join continuation lines
     until the next "- `" bullet). Derive the persona's style from its id prefix.
   Assemble the full generation prompt per persona = master prompt (with the
   per-style demeanor spliced into the "[insert per-persona demeanor/accessory
   notes below]" placeholder) + the persona's own descriptor notes/bio.
3. Finds personas with no existing frontend/src/assets/personas/{persona-id}.webp
   and targets ONLY those (delete-and-rerun curation loop). Creates the
   frontend/src/assets/personas/ directory if absent.
4. Generation via pydantic-ai: construct
   Agent('google:gemini-3.1-flash-image', output_type=BinaryImage,
   capabilities=[NativeTool(ImageGenerationTool(aspect_ratio='1:1', size='1K'))]).
   IMPORTANT: verify this spelling against the installed 1.104.0 package before
   finalizing (inspect .venv/lib/python3.13/site-packages/pydantic_ai/); the docs
   on main and the older builtin_tools= form differ — use whichever 1.104 supports
   (capabilities=[NativeTool(...)] is the verified current one). Run the agent per
   pending persona with the assembled prompt; read the BinaryImage bytes from
   result.output.
5. --logo-ref (bool, default off): when set, pass
   frontend/public/icons/logo-256.png as a reference image input alongside the
   prompt using pydantic-ai BinaryContent (image media type) in the user message.
6. Post-process each result with Pillow: open the returned image bytes, downscale
   to 512x512 (the model returns 1:1, so a straight resize to (512, 512) is fine),
   save as webp to frontend/src/assets/personas/{persona-id}.webp.
7. CLI flags: --limit N (max avatars generated this run, default no limit),
   --logo-ref (store_true), and --dry-run (store_true): when set, parse + list the
   pending personas and print each assembled prompt, then exit WITHOUT calling the
   API or requiring GOOGLE_API_KEY. --dry-run is the no-API verification seam and a
   prompt-preview tool for the operator.

Handle a missing GOOGLE_API_KEY with a clear stderr error + non-zero exit (only
when NOT --dry-run). This is an operator script, not app/services — no sentry_sdk
needed. scripts/ is NOT in the CI ty-check path (gate checks app/ tests/ only), so
type annotations should match existing script style but a ty pass is best-effort.
  </action>
  <verify>
    <automated>uv run python scripts/gen_persona_avatars.py --help >/dev/null && test $(uv run python scripts/gen_persona_avatars.py --dry-run 2>/dev/null | grep -cE '(attacker|trickster|grinder|wall)-(800|1000|1200|1400|1600|1800)') -ge 24 && uv run ruff check scripts/gen_persona_avatars.py</automated>
  </verify>
  <done>--help and --dry-run run with no API key; --dry-run lists all 24 personas as pending (empty assets dir) and prints their assembled prompts; ruff clean. Pillow is in the dev group.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Swap resolveAvatarSrc seam to import.meta.glob (D-17)</name>
  <files>frontend/src/lib/personas/personaAvatars.ts, frontend/src/components/bots/__tests__/PersonaCard.test.tsx</files>
  <action>
Replace the body of `resolveAvatarSrc()` in
frontend/src/lib/personas/personaAvatars.ts with a Vite import.meta.glob lookup
over the persona assets, keyed by persona id (D-17):

- Build a module-level map from a glob:
  `import.meta.glob('../../assets/personas/*.webp', { eager: true, query: '?url', import: 'default' })`.
  Reduce it into a `Record<string, string>` keyed by the filename stem (the
  persona id): for each glob path, extract the basename without `.webp`. Under
  noUncheckedIndexedAccess the lookup returns `string | undefined`, which matches
  the existing return type.
- resolveAvatarSrc returns `persona.avatarSrc ?? AVATAR_SRC_BY_ID[persona.id]`.
  Keeping `persona.avatarSrc` as an explicit override first preserves the
  forward-compat field's documented purpose AND keeps the existing PersonaCard
  "renders real-art image when avatarSrc present" test green; the glob is the real
  source once webp files land. placeholderAvatarFor stays unchanged (emoji fallback
  preserved for personas with no webp).
- Update the module docstring: it currently states "no glob" / "ships ONLY the
  placeholder path" (RESEARCH.md Pitfall 6). Rewrite it to describe the glob-backed
  lookup and the delete-and-rerun loop. The glob matching zero files today is valid
  (Vite returns {}), so the build stays green until webp files exist.

The empty glob today means resolveAvatarSrc still returns undefined for every
persona (no override, no webp) → every card renders the emoji branch, so
PersonaCard.test.tsx's "falls back to emoji" test and the avatarSrc-present test
both still pass unchanged. Run the frontend tests; only adjust a test if the chosen
implementation actually breaks it. Do NOT touch personaRegistry.test.ts's "no
persona ships an avatarSrc" assertion — it stays true.

Per project memory (frontend lint+test do NOT type-check): after the change run
`npx tsc -b` (or `npm run build`) in frontend to catch glob/type errors that
vitest and eslint miss.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm test -- --run src/components/bots/__tests__/PersonaCard.test.tsx src/lib/personas/__tests__/personaRegistry.test.ts && npm run lint</automated>
  </verify>
  <done>resolveAvatarSrc reads from the assets/personas/*.webp glob keyed by persona id (with avatarSrc override retained); tsc -b, PersonaCard + personaRegistry tests, and lint all pass; module docstring no longer claims "no glob".</done>
</task>

</tasks>

<verification>
Run the CLAUDE.md pre-merge gate scoped to what changed:

- Backend: `uv run ruff format scripts/`, `uv run ruff check scripts/gen_persona_avatars.py`.
  (The script is operator-only; no backend pytest impact — no app/ or tests/ files change.)
- Frontend: `cd frontend && npx tsc -b && npm run lint && npm test -- --run && npm run knip`.
  knip must stay green — the glob-built map and resolveAvatarSrc are both consumed;
  no new dead exports.
- End-to-end doc↔script contract: `uv run python scripts/gen_persona_avatars.py --dry-run`
  lists all 24 personas as pending against the empty assets dir and prints coherent
  assembled prompts (spot-check one per style for the correct demeanor + bio splice).
</verification>

<success_criteria>
- personaAvatarPrompts.md carries all 24 bios woven into parseable descriptor lines; framing prose reflects the shipped script + glob seam (D-15).
- scripts/gen_persona_avatars.py exists, parses the doc, targets only personas lacking a webp, downscales to 512x512 webp via Pillow, and supports --limit / --logo-ref / --dry-run; runs --help and --dry-run with no API key (D-16). Pillow is a dev dependency.
- resolveAvatarSrc() is glob-backed (import.meta.glob over assets/personas/*.webp keyed by id) with emoji fallback intact; tsc -b, frontend tests, lint, and knip pass (D-17).
- No avatar image files are committed; the seam falls back to emoji until the user runs the script.
</success_criteria>

<output>
Create `.planning/quick/260722-r7t-bot-persona-avatar-generation-prompts-do/260722-r7t-SUMMARY.md` when done.
</output>