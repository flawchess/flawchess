---
quick_id: 260516-0ax
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/MiniBulletChart.tsx
  - frontend/src/components/charts/__tests__/MiniBulletChart.test.tsx
autonomous: true
requirements: [260516-0ax]
must_haves:
  truths:
    - "MiniBulletChart with (neutralMin=-0.11, neutralMax=0.00, center=0) renders the colored band to the LEFT of the center tick, with the band's right edge at center."
    - "MiniBulletChart with (neutralMin=+0.01, neutralMax=+0.11, center=0) renders the colored band to the RIGHT of the center tick, with the band's left edge at center."
    - "MiniBulletChart with (neutralMin=-0.05, neutralMax=+0.05, center=0) still renders the colored band centered on 0 (no regression for symmetric tuples)."
    - "Zone color of the value bar follows the asymmetric (lo, hi) bounds correctly: value < absNeutralMin → DANGER, absNeutralMin ≤ value < absNeutralMax → NEUTRAL, value ≥ absNeutralMax → SUCCESS."
    - "No upstream code path silently averages, absolutes, or symmetrizes the (lo, hi) tuple before it reaches the chart."
  artifacts:
    - path: "frontend/src/components/charts/MiniBulletChart.tsx"
      provides: "Asymmetric-band rendering (verified, with clarified contract in JSDoc)"
    - path: "frontend/src/components/charts/__tests__/MiniBulletChart.test.tsx"
      provides: "Asymmetric-zone regression tests"
  key_links:
    - from: "MiniBulletChart.tsx"
      to: "neutralMin/neutralMax props"
      via: "absNeutralMin = center + neutralMin; absNeutralMax = center + neutralMax (no symmetrization)"
      pattern: "absNeutralMin = center \\+ neutralMin"
---

<objective>
Verify and lock in that `MiniBulletChart` correctly renders asymmetric neutral
zones (e.g. `(−0.11, 0.00)`, `(+0.01, +0.11)`) — bands offset from the visual
center tick when `lo` and `hi` are not symmetric around 0.

Purpose: Section 2 ΔES Score Gap zones in `endgameZones.ts` will be calibrated
to asymmetric tuples (per benchmarks-latest.md §3.4.4). The chart must paint
those bands correctly. The chart's existing math already supports this, but the
prop JSDoc currently says `±band` and there is no regression test for the
asymmetric case — both gaps will be closed here.

Output:
- Confirmation (via tests) that the chart honors asymmetric `(lo, hi)` tuples.
- Clarified prop docs on `MiniBulletChart` that drop the symmetric framing.
- New unit tests asserting band position for the three Section 2 scenarios.

Scope explicitly excludes: modifying `ZONE_REGISTRY` values, modifying axis
domain/ticks/labels, modifying any other chart consumer's call sites, and
modifying Python-side endgame_zones.py (calibration is a separate step).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/components/charts/MiniBulletChart.tsx
@frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
@frontend/src/components/charts/__tests__/MiniBulletChart.test.tsx
@frontend/src/lib/theme.ts

<interfaces>
<!-- Key contracts the executor needs. Already in repo. -->

From frontend/src/components/charts/MiniBulletChart.tsx:
```typescript
interface MiniBulletChartProps {
  value: number;
  neutralMin?: number;  // signed offset from `center` (default -0.10)
  neutralMax?: number;  // signed offset from `center` (default 0)
  domain?: number;      // axis half-width around `center` (default 0.40)
  center?: number;      // axis center (default 0)
  ariaLabel?: string;
  heightClass?: string;
  valueHeightClass?: string;
  ciLow?: number;
  ciHigh?: number;
  tickPawns?: number;
  barColor?: 'zone' | 'neutral';
}
```

Existing chart math (already asymmetric-safe — verify, don't replace):
- `axisMin = center - domain`, `axisMax = center + domain`
- `absNeutralMin = center + neutralMin` (NOT `center - Math.abs(...)`)
- `absNeutralMax = center + neutralMax`
- `neutralMinPct = toPct(Math.max(axisMin, absNeutralMin))`
- `neutralMaxPct = toPct(Math.min(axisMax, absNeutralMax))`
- Band rendered as three flexed divs with widths: `neutralMinPct`, `neutralMaxPct - neutralMinPct`, `100 - neutralMaxPct`.

Zone-color rule (must continue to use absolute bounds, not magnitudes):
- `value >= absNeutralMax` → SUCCESS
- `value >= absNeutralMin` → NEUTRAL
- else → DANGER
</interfaces>

<call_sites>
Confirmed call sites that pass Section 2 score gap zones through to the chart
(these MUST NOT be touched in this task — they already pass the tuple unmolested):

- `frontend/src/components/charts/EndgameMetricCard.tsx` (lines 104–120) selects
  the bucket-specific (min, max) and forwards both to `ScoreGapRow`.
- `frontend/src/components/charts/EndgameSkillCard.tsx` (lines 160–161) forwards
  `SECTION2_SCORE_GAP_SKILL_NEUTRAL_MIN/MAX` directly.
- `frontend/src/components/charts/EndgameOverallScoreGapRow.tsx` (lines 78–88)
  forwards `neutralMin`, `neutralMax` directly to `MiniBulletChart`.

No averaging, absoluting, or symmetrization happens along this path. Once the
ZONE_REGISTRY constants in `endgameZones.ts` are recalibrated to asymmetric
tuples (separate task), they will flow through correctly.
</call_sites>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add asymmetric-zone regression tests to MiniBulletChart</name>
  <files>frontend/src/components/charts/__tests__/MiniBulletChart.test.tsx</files>
  <behavior>
    Add a new `describe('MiniBulletChart — asymmetric neutral zone', ...)` block
    in the existing test file. The block must contain at least these four tests:

    1. **Left-skewed band (conv-like, e.g. Section 2 Conversion `(−0.11, 0.00)`):**
       Render `<MiniBulletChart value={0} neutralMin={-0.11} neutralMax={0} center={0} domain={0.40} />`.
       Query the three zone divs (the three direct children of the
       `.absolute.inset-0.flex` zone-band container — select via
       `getByTestId('mini-bullet-chart').querySelector('.absolute.inset-0.flex')`
       then `.children`).
       Assert:
       - First (DANGER) div width is `'36.25%'` (i.e. `toPct(-0.11)` with
         `axisMin=-0.40, axisMax=+0.40` → `(0.29/0.80)*100`).
       - Middle (NEUTRAL) div width is `'13.75%'` (50% − 36.25%).
       - Third (SUCCESS) div width is `'50%'` (100% − 50%).
       This proves the colored neutral band sits entirely to the left of the
       center tick (which is at 50%).

    2. **Right-skewed band (recov-like, e.g. Section 2 Recovery `(+0.01, +0.11)`):**
       Render `<MiniBulletChart value={0} neutralMin={0.01} neutralMax={0.11} center={0} domain={0.40} />`.
       Assert:
       - DANGER width `'51.25%'` (`toPct(0.01) = ((0.41)/0.80)*100`).
       - NEUTRAL width `'12.5%'` (`toPct(0.11) − toPct(0.01) = 63.75% − 51.25%`).
       - SUCCESS width `'36.25%'` (`100% − 63.75%`).
       Confirms the band sits entirely to the right of the center tick.

    3. **Symmetric band still works (`(−0.05, +0.05)`):**
       Render `<MiniBulletChart value={0} neutralMin={-0.05} neutralMax={0.05} center={0} domain={0.40} />`.
       Assert:
       - DANGER width `'43.75%'`.
       - NEUTRAL width `'12.5%'`.
       - SUCCESS width `'43.75%'`.
       Locks in no-regression on the symmetric path.

    4. **Zone color follows asymmetric bounds:**
       Render three charts with the `(−0.11, 0.00)` band and values
       `−0.20`, `−0.05`, `+0.05`. For each, query
       `getByTestId('mini-bullet-value-bar').style.backgroundColor` and assert
       it matches:
       - `-0.20` → ZONE_DANGER (`value < absNeutralMin`)
       - `-0.05` → ZONE_NEUTRAL (`absNeutralMin ≤ value < absNeutralMax`)
       - `+0.05` → ZONE_SUCCESS (`value ≥ absNeutralMax`)
       Import the theme constants from `@/lib/theme` and convert them to the
       browser's `rgb(...)` form via the same approach used elsewhere in this
       file if needed, OR compare against the inline `style.backgroundColor`
       string directly when the theme constants are already hex/rgb. If the
       test runner returns colors in `rgb(r, g, b)` form, normalize by parsing
       both sides through a small helper. Keep it simple.

    All four tests must pass on the CURRENT `MiniBulletChart.tsx` (the math is
    already correct). The test names should make clear that they lock in
    asymmetric-band rendering as a guaranteed contract.
  </behavior>
  <action>
    Open `frontend/src/components/charts/__tests__/MiniBulletChart.test.tsx`.
    Append a new `describe('MiniBulletChart — asymmetric neutral zone', () => {...})`
    block at the end of the file (after the existing CI-whisker block).
    Implement the four tests described above. Use the existing
    `cleanup()`/`afterEach()` already in scope.

    Reuse the rendering pattern from the existing tests (`render(...)`,
    `getByTestId`, `screen.queryByTestId`). For querying the three zone
    background divs, the cleanest approach is:
    ```
    const chart = getByTestId('mini-bullet-chart');
    const zoneRow = chart.querySelector(':scope > div.absolute.inset-0.flex');
    const [danger, neutral, success] = zoneRow.children as ...
    expect((danger as HTMLElement).style.width).toBe('36.25%');
    ```
    Cast via `HTMLElement` for `.style` access — the file already uses
    `style.left` / `style.width` checks so the pattern is established.

    Use `Number.parseFloat(...).toFixed(2)` if floating-point string
    comparison is brittle in any environment, but the inline-style strings
    produced by React for these specific inputs are stable (the percentages
    above are exact for the chosen domain/center).

    Do NOT modify any production code in this task. Do NOT change existing
    tests. Adding a new describe block only.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/charts/__tests__/MiniBulletChart.test.tsx</automated>
  </verify>
  <done>
    - New `describe('MiniBulletChart — asymmetric neutral zone', ...)` block
      with the four tests above passes against the unmodified production code.
    - The full `MiniBulletChart.test.tsx` file passes.
    - `npm run lint` clean on the modified test file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clarify MiniBulletChart JSDoc — drop symmetric framing</name>
  <files>frontend/src/components/charts/MiniBulletChart.tsx</files>
  <action>
    Update the prop JSDoc on `neutralMin` and `neutralMax` in
    `MiniBulletChartProps` (lines 33–35 in the current file) to:
    - State explicitly that `neutralMin` and `neutralMax` are independent
      signed offsets from `center`, and that asymmetric tuples
      (e.g. `(−0.11, 0.00)` or `(+0.01, +0.11)`) are supported and render with
      the band offset from the center tick accordingly.
    - Keep the default values (`-0.10` for `neutralMin`, `0` for `neutralMax`)
      unchanged.
    - Drop or rephrase any wording that implies a ±band magnitude.

    Also update the file-level header comment (lines 1–11) to add one sentence
    confirming asymmetric `(lo, hi)` tuples are supported (the band may sit
    entirely on one side of `center`).

    Replace the inline comment on line 99 (`// Neutral-band bounds are offsets
    from center; convert to absolute eval space before clamping to the chart
    axis.`) with a slightly expanded version that names the asymmetric case:
    add a sentence like "Asymmetric tuples (lo and hi not symmetric around
    center) are supported and produce an off-center band." Keep the comment
    short — one extra sentence max.

    Do NOT change any code logic. JSDoc and comments only. No prop renames,
    no default changes, no math changes.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/charts/__tests__/MiniBulletChart.test.tsx && npm run lint && npm run knip</automated>
  </verify>
  <done>
    - `neutralMin` and `neutralMax` JSDoc clearly describe asymmetric support
      with no `±band` framing.
    - File header notes asymmetric tuples are supported.
    - All `MiniBulletChart.test.tsx` tests (existing + Task 1's new block)
      still pass.
    - `npm run lint` and `npm run knip` clean.
    - `git diff frontend/src/components/charts/MiniBulletChart.tsx` shows
      ONLY comment / JSDoc changes (no executable code changed).
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. **Asymmetric rendering proven by tests** —
   `cd frontend && npm test -- --run src/components/charts/__tests__/MiniBulletChart.test.tsx`
   passes including the four new asymmetric-zone tests.

2. **No symmetrization upstream** — run:
   ```
   grep -rn "Math.abs\|Math\.max.*Math\.abs\|symmetric\|\\bavg\\b" \
     frontend/src/components/charts/MiniBulletChart.tsx \
     frontend/src/components/charts/EndgameOverallScoreGapRow.tsx \
     frontend/src/components/charts/EndgameMetricCard.tsx \
     frontend/src/components/charts/EndgameSkillCard.tsx
   ```
   Expect zero hits that touch `neutralMin` / `neutralMax`. (The grep may
   match unrelated `Math.max` calls; eyeball the hits to confirm none
   averages or absolutes the band bounds.)

3. **Full frontend suite still green** —
   `cd frontend && npm test` passes.

4. **Lint + knip clean** — `cd frontend && npm run lint && npm run knip`.

5. **Manual visual sanity** (optional, since tests cover it) — start
   `npm run dev`, open the Endgames page once the calibration step
   ships, and confirm the Section 2 score-gap bullets render bands off-center
   when the ZONE_REGISTRY is asymmetric. NOT required for this task's `done`.
</verification>

<success_criteria>
- `MiniBulletChart` is provably asymmetric-zone-correct via four new unit tests
  (left-skewed, right-skewed, symmetric-control, zone-color).
- JSDoc on `neutralMin` / `neutralMax` describes asymmetric support without any
  `±band` framing.
- No production logic change in `MiniBulletChart.tsx` — only comments / JSDoc.
- No change to `ZONE_REGISTRY` constants in `endgameZones.ts` or
  `endgame_zones.py`.
- No change to chart axis bounds, ticks, or labels.
- `npm test`, `npm run lint`, `npm run knip` all clean.
- Mobile rule N/A: `MiniBulletChart` is a single shared component with no
  separate desktop/mobile rendering path — verified by grep
  (`grep -rn "MiniBulletChart" frontend/src/`).
</success_criteria>

<output>
After completion, create
`.planning/quick/260516-0ax-bullet-chart-render-asymmetric-neutral-z/260516-0ax-SUMMARY.md`
following the standard summary template (what shipped, why, files modified,
test results).
</output>
