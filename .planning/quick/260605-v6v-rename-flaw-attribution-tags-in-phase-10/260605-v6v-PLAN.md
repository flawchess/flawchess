---
phase: quick-260605-v6v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/flaws_service.py
  - app/services/library_service.py
  - app/schemas/library.py
  - tests/services/test_flaws_service.py
  - tests/services/test_library_service.py
autonomous: true
requirements: [SEED-036-RENAME]

must_haves:
  truths:
    - "Every flaw-attribution tag emitted by the kernel uses the final taxonomy (low-clock, impatient, considered, lucky-escape, while-ahead, opening, middlegame, endgame); miss and result-changing unchanged."
    - "A flaw with missing clock/move-time data carries NO tempo tag (at-most-one rule), not a fallback tag."
    - "Phase tags (opening/middlegame/endgame) are still excluded from card chips after the phase- prefix is gone."
    - "The Endgames time-pressure feature is untouched."
    - "Full backend gate is green: ruff format, ruff check, ty check (zero errors), pytest -n auto."
  artifacts:
    - path: "app/services/flaws_service.py"
      provides: "Renamed FlawTag/TempoTag Literals, optional _classify_tempo, renamed _phase_tag returns"
      contains: "low-clock"
    - path: "app/services/library_service.py"
      provides: "Updated _CHIP_ORDER, _TEMPO_TAGS, _PHASE_TAG_TO_KEY, prefix-free phase exclusion"
      contains: "low-clock"
    - path: "app/schemas/library.py"
      provides: "TempoTag-keyed tempo dict with updated unmeasured-remainder docstring"
      contains: "low-clock"
  key_links:
    - from: "app/services/flaws_service.py::_classify_tempo"
      to: "app/services/flaws_service.py::_build_tags"
      via: "None return skips tempo append"
      pattern: "_classify_tempo"
    - from: "app/services/library_service.py::_curate_chips"
      to: "phase tags"
      via: "membership exclusion (not prefix)"
      pattern: "_PHASE"
---

<objective>
Rename the flaw-attribution tag taxonomy in the shipped Phase 106 backend to the final names locked in the 2026-06-05 /gsd-explore session, and make the tempo tag optional (at-most-one instead of exactly-one). Tags are computed on-the-fly and never persisted, so there is NO DB migration.

Purpose: Phase 107 (frontend) will surface these names on user-facing card chips and the Flaw-Stats panel. Doing the rename standalone first means 107 starts on the final names.
Output: Renamed Literals, optional-tempo logic, updated docstrings/comments, and updated + extended tests, with the full backend gate green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/notes/flaw-tag-naming.md
@app/services/flaws_service.py
@app/services/library_service.py
@app/schemas/library.py
</context>

<authoritative_rename_map>
Old → New (from .planning/notes/flaw-tag-naming.md, which is the source of truth):
- time-pressure → low-clock
- hasty → impatient
- knowledge-gap → considered
- unpunished → lucky-escape
- from-winning → while-ahead
- phase-opening → opening
- phase-middlegame → middlegame
- phase-endgame → endgame
- miss → UNCHANGED
- result-changing → UNCHANGED

Structural change: tempo is now OPTIONAL. _classify_tempo returns None on missing
clock/move-time (was: fallback to knowledge-gap). TempoTag narrows to
low-clock | impatient | considered. FlawRecord.tags invariant becomes "at most one
tempo tag" (was "exactly one").

OUT OF SCOPE — DO NOT TOUCH: the Endgames time-pressure feature. The string
"time-pressure" appears heavily in app/services/canonical_slice_sql.py,
app/services/insights_llm.py, app/services/insights_service.py, app/schemas/insights.py,
app/models/user_benchmark_percentile.py, tests/services/test_canonical_slice_sql.py,
tests/integration/test_benchmark_metric_enum.py, tests/test_insights_schema.py,
tests/test_integration_routers.py, tests/seed_fixtures.py — ALL of these are the
unrelated Endgames feature. Only flaw-tag occurrences in flaws_service.py,
library_service.py, library.py, and the two flaw test files below are in scope.
</authoritative_rename_map>

<tasks>

<task type="auto">
  <name>Task 1: Rename the taxonomy and make tempo optional in the kernel + library service + schema</name>
  <files>app/services/flaws_service.py, app/services/library_service.py, app/schemas/library.py</files>
  <action>
Apply the authoritative rename map and the at-most-one-tempo structural change across the three production files. The kernel constant NAMES (TIME_PRESSURE_CLOCK_FRACTION, HASTY_MOVE_FRACTION, TIME_PRESSURE_CLOCK_ABS_SECONDS, HASTY_MOVE_ABS_SECONDS, FROM_WINNING_ES) and the helper function names (_classify_tempo, _is_unpunished, _phase_tag) MAY remain as-is (they are private/internal and renaming them only enlarges the diff). What MUST change are the tag string VALUES, the Literal type members, the optional-tempo behavior, and all docstrings/comments that quote the old tag names or assert the old invariant.

app/services/flaws_service.py:
- FlawTag Literal: replace members unpunished→lucky-escape, from-winning→while-ahead, time-pressure→low-clock, hasty→impatient, knowledge-gap→considered, phase-opening→opening, phase-middlegame→middlegame, phase-endgame→endgame. Keep miss and result-changing.
- TempoTag Literal: narrow to Literal["low-clock", "impatient", "considered"] (it already only listed the three tempo values; just rename them — the None case is now expressed in the return type, not as a Literal member).
- _classify_tempo: change return annotation to `TempoTag | None`. When clock_after is None or move_time is None, `return None` (was `return "knowledge-gap"`). Rename the three returned strings: time-pressure→low-clock, hasty→impatient, knowledge-gap→considered. Update the docstring: it currently says "Return exactly one tempo tag" / "Every flaw carries exactly one of {...}" / "If clock data is missing, default to knowledge-gap" — rewrite to: returns at most one tempo tag (low-clock | impatient | considered), or None when clock/move-time data is unavailable (no misleading fallback); priority low-clock > impatient > considered. Per the at-most-one rule in flaw-tag-naming.md §"Structural change".
- _build_tags: capture the tempo result in a local (e.g. `tempo = _classify_tempo(...)`) and only `tags.append(tempo)` when `tempo is not None`. Update the docstring: "Tag order: while-ahead, result-changing, miss, lucky-escape, phase, tempo." and replace "Exactly one tempo tag is always present." with "At most one tempo tag is present (absent when clock data is unavailable)."
- _phase_tag: return "opening" (phase==0), "endgame" (phase==2), "middlegame" (default). Update docstring references phase-middlegame/phase-opening/phase-endgame → middlegame/opening/endgame.
- FROM_WINNING_ES line-54 comment `# tag: from-winning` → `# tag: while-ahead`.
- The `if es_before >= FROM_WINNING_ES: tags.append("from-winning")` in _build_tags → append "while-ahead".
- The `if _is_unpunished(...): tags.append("unpunished")` → append "lucky-escape". _is_unpunished docstring mentions "unpunished" and "counts as 'unpunished'" — update those prose references to lucky-escape (keep the function name).

app/services/library_service.py:
- _CHIP_ORDER tuple: rename members to the new names (miss, lucky-escape, while-ahead, result-changing, low-clock, impatient, considered). Update the comment above it if it names old tags.
- CRITICAL — _curate_chips phase exclusion is now broken by the rename: it currently drops phase tags via `tag.startswith(_PHASE_TAG_PREFIX)` where `_PHASE_TAG_PREFIX = "phase-"`. After the rename the phase tags are bare opening/middlegame/endgame and would LEAK into chips. Replace the prefix test with a membership check against the phase tag set. Define `_PHASE_TAGS: frozenset[str] = frozenset({"opening", "middlegame", "endgame"})` and change the filter to `if tag not in _PHASE_TAGS: present.add(tag)`. Remove the now-unused `_PHASE_TAG_PREFIX` constant (knip/ruff will flag a dead local; ty/ruff must stay clean).
- _TEMPO_TAGS tuple → ("low-clock", "impatient", "considered").
- _PHASE_TAG_TO_KEY dict: keys become "opening"/"middlegame"/"endgame" mapping to the same "opening"/"middlegame"/"endgame" values (now an identity map). Since the kernel already emits the final key strings, you may keep the dict for explicitness OR simplify; keep it (identity map) to minimize behavioral change and preserve the typed membership check in _compute_tag_distribution. Update the stale comment `# game_position.phase ints -> human phase label` to note it is now an identity passthrough.
- _compute_tag_distribution docstring: "Each FlawRecord carries exactly one tempo tag and exactly one phase-* tag." → "Each FlawRecord carries at most one tempo tag and exactly one phase tag." The accumulation loop already guards `if tag in _TEMPO_TAGS` so a missing tempo tag is naturally handled (no code change to the loop beyond the renamed constants).

app/schemas/library.py:
- The `from app.services.flaws_service import ... TempoTag` import is unchanged (same symbol name).
- TagDistribution.tempo docstring: currently "tempo = count of each tempo tag (each flaw carries exactly one)." Update to reflect at-most-one and the unmeasured remainder: tempo counts now sum to ≤ M+B flaws because flaws with unavailable clock data carry no tempo tag; the Flaw-Stats panel must show the unmeasured remainder (total M+B − sum(tempo)) rather than normalizing the three measured segments to 100% (per flaw-tag-naming.md §"Structural change"). Also update phase_histogram docstring "each flaw carries exactly one phase-* tag" → "exactly one phase tag".

Do NOT edit any of the OUT OF SCOPE Endgames files listed in authoritative_rename_map.
  </action>
  <verify>
    <automated>grep -rn -E "\"(time-pressure|hasty|knowledge-gap|unpunished|from-winning|phase-opening|phase-middlegame|phase-endgame)\"" app/services/flaws_service.py app/services/library_service.py app/schemas/library.py | grep -v '^#' | wc -l | grep -qx 0 && echo "no old tag literals remain in scoped prod files"</automated>
  </verify>
  <done>
All three production files use the final taxonomy. _classify_tempo returns TempoTag | None and returns None on missing clock/move-time. _build_tags appends tempo only when present. _curate_chips excludes phase tags by membership (not prefix) and the dead _PHASE_TAG_PREFIX is removed. Docstrings reflect at-most-one tempo + unmeasured remainder. Endgames time-pressure code untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update + extend the flaw tests and run the full backend gate</name>
  <files>tests/services/test_flaws_service.py, tests/services/test_library_service.py</files>
  <behavior>
    - test_classify_tempo low-clock: low clock fraction yields "low-clock" (renamed from time-pressure).
    - test_classify_tempo impatient: fast move on comfortable clock yields "impatient" (renamed from hasty).
    - test_classify_tempo considered: adequate clock + adequate move time yields "considered" (renamed from knowledge-gap).
    - NEW behavior — test_classify_tempo missing clock: clock_after=None yields None (was "knowledge-gap"). This is the one genuinely new behavior.
    - NEW behavior — test_classify_tempo missing move_time: move_time=None yields None (was "knowledge-gap").
    - NEW behavior — a FlawRecord built from a flaw with missing clock data carries NO tempo tag (at-most-one rule): assert no member of flaw["tags"] is in {"low-clock","impatient","considered"}.
    - test attribution tags: while-ahead (es_before >= FROM_WINNING_ES), lucky-escape (unpunished blunder), opening/middlegame/endgame phase tags (renamed from phase-*).
    - test chip curation (library): chips use new names, phase tags (now bare opening/middlegame/endgame) are still excluded from chips, dedupe + _CHIP_ORDER ordering still hold.
    - the former "exactly one tempo tag per flaw" test becomes "at most one tempo tag per flaw" (len(tempo_tags) <= 1).
  </behavior>
  <action>
Update tests/services/test_flaws_service.py:
- TestTempoTags: rename the docstring "exactly one tempo tag per flaw" → "at most one tempo tag per flaw". In test_time_pressure_when_low_clock_fraction / test_hasty_when_fast_move_on_comfortable_clock / test_knowledge_gap_when_adequate_time / test_abs_fallback_* update the asserted strings time-pressure→low-clock, hasty→impatient, knowledge-gap→considered. Consider renaming the test method names to match (optional but preferred for clarity: test_low_clock_when_low_clock_fraction, test_impatient_when_fast_move_on_comfortable_clock, test_considered_when_adequate_time).
- test_knowledge_gap_when_clock_data_missing and test_knowledge_gap_when_move_time_missing: these now assert the NEW behavior — change `assert result == "knowledge-gap"` to `assert result is None`, and rename to test_no_tempo_tag_when_clock_data_missing / test_no_tempo_tag_when_move_time_missing with updated docstrings ("missing clock/move-time yields no tempo tag, not a fallback").
- test_move_time_clamps_negative_to_none docstring mentions "tempo falls back to knowledge-gap" — update to "tempo is absent (None) so the move is not mislabeled impatient".
- test_exactly_one_tempo_tag_per_flaw_in_classify_game: rename to test_at_most_one_tempo_tag_per_flaw_in_classify_game; change the tempo membership set to ("low-clock","impatient","considered") and the assertion from `== 1` to `<= 1` with an updated message. (The existing fixture supplies clocks, so this game will still produce a tempo tag — but the invariant under test is now at-most-one.)
- ADD a new test: build positions where the flawed ply has missing clock data (clock_seconds=None on the relevant plies so _move_time returns None and clock_after is None), run classify_game_flaws, and assert the emitted FlawRecord's tags contain NO tempo tag (none of low-clock/impatient/considered). This is the genuinely new behavior, not a string swap.
- TestAttributionTags: update from-winning→while-ahead, unpunished→lucky-escape (assertions and any docstrings), phase-opening→opening, phase-middlegame→middlegame, phase-endgame→endgame (the test_phase_* assertions at lines ~859-894).

Update tests/services/test_library_service.py:
- _make_flaw fixtures and chip assertions: rename old tag strings in the flaw tag lists and expected chip sets/order (from-winning→while-ahead, hasty→impatient, phase-middlegame→middlegame, phase-opening→opening, phase-endgame→endgame). The test at ~242-254 asserting phase exclusion + dedupe + _CHIP_ORDER must still pass with the new bare phase names — this is what proves the _curate_chips membership-exclusion fix works. Update the comment "phase-opening dropped" → "opening dropped". The test at ~266 `_curate_chips([_make_flaw(["phase-endgame"])])` → `["endgame"]` and should still yield an empty chip list (phase tags excluded).

Then run the FULL backend gate per CLAUDE.md (dev DB must be up first):
1. `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`
2. `uv run ruff format app/ tests/`
3. `uv run ruff check app/ tests/ --fix`
4. `uv run ty check app/ tests/`  (must report zero errors)
5. `uv run pytest -n auto`  (full suite must be green)
If ruff format/check modifies files, that is expected — leave the formatted result in place. Resolve any ty or pytest failures before declaring done. Pay special attention to ty: the narrowed `TempoTag | None` return must flow correctly through _build_tags (the `if tempo is not None` guard narrows it before append).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml -p flawchess-dev up -d >/dev/null 2>&1; uv run ruff format --check app/ tests/ && uv run ruff check app/ tests/ && uv run ty check app/ tests/ && uv run pytest -n auto tests/services/test_flaws_service.py tests/services/test_library_service.py</automated>
  </verify>
  <done>
All flaw/library tests assert the new taxonomy and the at-most-one tempo rule. A new test proves a missing-clock flaw carries no tempo tag. `uv run ruff format --check`, `uv run ruff check`, and `uv run ty check app/ tests/` are clean (zero errors), and `uv run pytest -n auto` (full suite) is green.
  </done>
</task>

</tasks>

<verification>
- No old flaw-tag literal strings remain in flaws_service.py, library_service.py, library.py, or the two flaw test files (grep for time-pressure/hasty/knowledge-gap/unpunished/from-winning/phase-opening/phase-middlegame/phase-endgame in those files returns nothing, excluding comments).
- Endgames time-pressure code is byte-identical (git diff shows no changes under canonical_slice_sql.py, insights_*.py, schemas/insights.py, the benchmark/insights tests, seed_fixtures.py).
- Full backend gate green: ruff format --check, ruff check, ty check (zero errors), pytest -n auto.
</verification>

<success_criteria>
- The kernel emits the final taxonomy and at most one tempo tag (None when clock data is unavailable).
- Card-chip phase exclusion survives the prefix removal (phase tags never appear as chips).
- The unmeasured-remainder semantics are documented on TagDistribution.tempo.
- No DB migration was created (tags are on-the-fly only).
- `uv run pytest -n auto` passes the entire suite; ruff + ty clean.
</success_criteria>

<output>
Create `.planning/quick/260605-v6v-rename-flaw-attribution-tags-in-phase-10/260605-v6v-SUMMARY.md` when done.
</output>
