---
phase: quick
plan: 260319-t3x
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Openings.tsx
autonomous: true
requirements: [tooltip-info-icons]
must_haves:
  truths:
    - "Moves tab has an info icon explaining arrow thickness and color meaning"
    - "Position bookmarks section has an info icon explaining bookmark usage and statistics tab connection"
  artifacts:
    - path: "frontend/src/pages/Openings.tsx"
      provides: "Two new tooltip info icons"
  key_links:
    - from: "Moves tab info tooltip"
      to: "arrowColor.ts constants"
      via: "Text content references the same thresholds"
---

<objective>
Add tooltip info hover icons to explain two key UI concepts: (1) how move arrows encode frequency and win/loss rates, and (2) how position bookmarks feed into the Statistics tab charts.

Purpose: Help users understand non-obvious visual encodings and feature relationships without external documentation.
Output: Two new Info icon tooltips in Openings.tsx, matching the existing Piece filter tooltip pattern.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/pages/Openings.tsx
@frontend/src/lib/arrowColor.ts
@frontend/src/components/move-explorer/MoveExplorer.tsx
</context>

<interfaces>
<!-- Existing tooltip pattern from Openings.tsx (Piece filter info, lines 304-315): -->
```tsx
<TooltipProvider>
  <Tooltip>
    <TooltipTrigger asChild>
      <button type="button" className="text-muted-foreground hover:text-foreground" aria-label="..." data-testid="...">
        <Info className="h-3.5 w-3.5" />
      </button>
    </TooltipTrigger>
    <TooltipContent side="top" className="max-w-xs text-sm">
      ...explanation text...
    </TooltipContent>
  </Tooltip>
</TooltipProvider>
```

<!-- Arrow color constants from arrowColor.ts: -->
```ts
const MIN_GAMES_FOR_COLOR = 10;
const GREEN_THRESHOLD = 60;  // win_pct >= 60% → green
const RED_THRESHOLD = 60;    // loss_pct >= 60% → red
// Otherwise grey. Arrow width = game_count / maxCount (proportional thickness).
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add info tooltip to Moves tab and Position bookmarks section</name>
  <files>frontend/src/pages/Openings.tsx</files>
  <action>
Add two Info icon tooltips in Openings.tsx, replicating the exact pattern used for the Piece filter info icon (lines 304-315). The `Info` icon and all tooltip components are already imported.

**1. Moves tab info icon — place next to the "Moves" TabsTrigger text**

Instead of placing it inside the TabsTrigger (which would interfere with tab click behavior), place the info icon in `moveExplorerContent` at the top, just before the WDLBar. Add a small header row:

```tsx
<div className="flex items-center gap-1 mb-1">
  <p className="text-xs text-muted-foreground">Move arrows</p>
  <TooltipProvider>
    <Tooltip>
      <TooltipTrigger asChild>
        <button type="button" className="text-muted-foreground hover:text-foreground" aria-label="Move arrows info" data-testid="move-arrows-info">
          <Info className="h-3.5 w-3.5" />
        </button>
      </TooltipTrigger>
      <TooltipContent side="top" className="max-w-xs text-sm">
        Arrows on the board show moves from your games. Thicker arrows mean more games played. Colors indicate your results: green for high win rate (60%+), red for high loss rate (60%+), and grey otherwise. Moves with fewer than 10 games are always grey.
      </TooltipContent>
    </Tooltip>
  </TooltipProvider>
</div>
```

Place this div as the first child inside `moveExplorerContent`'s outer div (before the WDLBar conditional).

**2. Position bookmarks info icon — place next to the "Position bookmarks" collapsible heading**

In the CollapsibleTrigger Button for "Position bookmarks" (around line 346), add an Info icon AFTER the text. The button currently has `className="w-full justify-between"` with chevron at end. Wrap the label text and info icon together:

Change the button children from:
```
Position bookmarks
{chevron}
```
To:
```
<span className="flex items-center gap-1">
  Position bookmarks
  <TooltipProvider>
    <Tooltip>
      <TooltipTrigger asChild>
        <span
          role="button"
          tabIndex={-1}
          className="text-muted-foreground hover:text-foreground"
          aria-label="Position bookmarks info"
          data-testid="position-bookmarks-info"
          onClick={(e) => e.stopPropagation()}
        >
          <Info className="h-3.5 w-3.5" />
        </span>
      </TooltipTrigger>
      <TooltipContent side="top" className="max-w-xs text-sm">
        Save positions as bookmarks to track your openings. Bookmarks appear as entries in the Statistics tab charts, showing your win/draw/loss breakdown and win rate over time for each saved position.
      </TooltipContent>
    </Tooltip>
  </TooltipProvider>
</span>
{chevron}
```

Use `onClick={(e) => e.stopPropagation()}` on the tooltip trigger span to prevent the collapsible from toggling when hovering/clicking the info icon. Use a `span` with `role="button"` instead of a `button` element to avoid nesting a button inside the CollapsibleTrigger button.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Moves tab content area has an Info icon with tooltip explaining arrow thickness (frequency) and colors (green=60%+ win, red=60%+ loss, grey=default/under 10 games)
    - Position bookmarks collapsible heading has an Info icon with tooltip explaining bookmarks feed into Statistics tab charts
    - Both tooltips use the same visual pattern as the Piece filter info icon (h-3.5 w-3.5, max-w-xs, text-sm)
    - Both have data-testid and aria-label attributes per project conventions
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` succeeds
- Visual check: hover the Info icons next to "Move arrows" label and "Position bookmarks" heading to see tooltip text
</verification>

<success_criteria>
- Two new Info icon tooltips visible and functional
- Tooltip text accurately describes the arrow encoding (thickness=frequency, green/red/grey color thresholds, 10-game minimum)
- Tooltip text accurately describes bookmark-to-statistics relationship
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260319-t3x-add-tooltip-info-hover-icons-to-explain-/260319-t3x-SUMMARY.md`
</output>
