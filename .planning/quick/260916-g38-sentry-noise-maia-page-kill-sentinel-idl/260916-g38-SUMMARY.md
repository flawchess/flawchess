---
quick_id: 260916-g38
type: quick
status: complete
completed: 2026-09-16
commits: [ef344efcf, 4a547a769]
sentry: [FLAWCHESS-BM, FLAWCHESS-5Q]
---

# Quick 260916-g38 Summary

Executed inline by the orchestrator (small, fully specified task; per standing feedback no executor subagent).

## Task 1 — Maia page-kill sentinel idle guard (FLAWCHESS-BM) — `ef344efcf`

- `MAIA_PAGE_KILL_IDLE_THRESHOLD_MS = 10 min` in `maiaPageKillSentinel.ts`; `reportMaiaPageKillFromPreviousSession` clears the record and returns `false` without capturing when the last dispatch is older than that. `last: null` (armed, never dispatched) still reports.
- Tests: 3 new cases (idle record not reported, recent record reported, armed-only reported); the existing capture test's fixture timestamps were absolute (1970-era) and now sit relative to `Date.now()`.
- `vitest`: 8/8, `eslint` clean.

## Task 2 — Full-drain breaker single-target guard (FLAWCHESS-5Q) — `4a547a769`

- `FULL_DRAIN_BREAKER_MIN_TARGETS = 2` in `eval_drain.py`; the WR-05 breaker fires only with `len(engine_targets) >= 2` and all failed. One failed target takes the existing mark-and-continue path.
- Tests: `test_single_failed_target_does_not_trip_breaker` (dedup map covers both plies, terminal donor fails → no Sentry message, attempts → 1) and `test_two_failed_targets_still_trip_breaker` (`1. e4 *` → ply 0 + terminal, both fail → one Sentry message, attempts stay 0). Mutation check: reverting the guard fails the single-target test.
- `pytest tests/services/test_full_eval_drain.py -n auto`: 64/64; `ruff`, `ty` clean.

## Not done / follow-ups

- Sentry issues BM and 5Q are closed by the `Fixes` trailers on the next production release; they stay open until then.
