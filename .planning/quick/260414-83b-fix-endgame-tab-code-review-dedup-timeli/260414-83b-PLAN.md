---
phase: quick-260414-83b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/endgame_repository.py
  - app/services/endgame_service.py
autonomous: true
requirements:
  - CR-83b-01  # Dedup timeline subquery
  - CR-83b-02  # Validate _INT_TO_CLASS lookup (Sentry-safe)
  - CR-83b-03  # Replace row-index access with named access

must_haves:
  truths:
    - "query_endgame_timeline_rows runs a single (game_id, endgame_class) scan; the non-endgame branch derives its exclusion set from that same scan."
    - "Unknown endgame_class integers coming from the DB no longer raise KeyError; they are reported to Sentry via set_context + capture_exception and the row is skipped."
    - "_compute_score_gap_material and the bucket-row consumers access row fields by name (not by integer index)."
    - "uv run ruff check ., uv run ruff format ., uv run ty check app/ tests/, and uv run pytest all pass."
  artifacts:
    - path: "app/repositories/endgame_repository.py"
      provides: "query_endgame_timeline_rows with a single shared (game_id, endgame_class) subquery/CTE"
    - path: "app/services/endgame_service.py"
      provides: "Safe _INT_TO_CLASS lookup in _aggregate_endgame_stats and name-based row access in _compute_score_gap_material"
  key_links:
    - from: "app/repositories/endgame_repository.py::query_endgame_timeline_rows (non-endgame branch)"
      to: "shared per-class subquery/CTE from the endgame branch"
      via: "select(subq.c.game_id) reused to feed Game.id.notin_(...)"
      pattern: "notin_\\("
    - from: "app/services/endgame_service.py::_aggregate_endgame_stats"
      to: "sentry_sdk.capture_exception"
      via: "_INT_TO_CLASS.get(...) miss path"
      pattern: "sentry_sdk\\.(set_context|capture_exception|capture_message)"
    - from: "app/services/endgame_service.py::_compute_score_gap_material"
      to: "row.user_material_imbalance / row.user_material_imbalance_after / row.result / row.user_color / row.endgame_class"
      via: "SQLAlchemy Row attribute access (columns already labeled in query_endgame_bucket_rows / query_endgame_entry_rows)"
      pattern: "\\.user_material_imbalance|\\.user_material_imbalance_after|\\.endgame_class"
---

<objective>
Apply three targeted code-review fixes to the endgame tab:

1. Deduplicate the two identical `(game_id, endgame_class)` subqueries inside `query_endgame_timeline_rows` so the GamePosition scan runs once and both the endgame and non-endgame branches reuse it.
2. Make `_INT_TO_CLASS[endgame_class_int]` resilient to unexpected DB values by switching to `.get()`, reporting the bad value to Sentry via `set_context` + `capture_exception`, and skipping the offending row.
3. Replace positional `r[4]`/`r[5]`/`chosen_row[2..5]` access in `_compute_score_gap_material` with attribute access on the labeled SQLAlchemy `Row` (columns are already labeled in both `query_endgame_bucket_rows` and `query_endgame_entry_rows`).

Purpose: remove a redundant DB scan, harden against unexpected class integers without adding log-only silent failures, and make the row contract explicit at the call site.
Output: two modified files, zero unrelated changes, all lint/type/test gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/repositories/endgame_repository.py
@app/services/endgame_service.py

<interfaces>
Relevant row shapes (already produced with labeled columns in the repository layer):

From `app/repositories/endgame_repository.py::query_endgame_bucket_rows` (≈ lines 240-270) and `query_endgame_entry_rows` (≈ lines 124-237):
```
Row columns (labels):
  game_id
  endgame_class            # SmallInteger, 1..6 per EndgameClassInt
  result                   # "1-0" | "0-1" | "1/2-1/2" etc.
  user_color               # "white" | "black"
  user_material_imbalance          # int | None (sign-flipped to user perspective)
  user_material_imbalance_after    # int | None (sign-flipped, or NULL on non-persistent spans)
```
Because these are labeled columns, `row.game_id`, `row.endgame_class`, `row.result`, `row.user_color`, `row.user_material_imbalance`, and `row.user_material_imbalance_after` all work directly on the SQLAlchemy `Row` objects. Do NOT change the SQL; only switch the Python consumers from `r[i]` to `r.<name>`.

From `app/services/endgame_service.py` (≈ lines 60-82):
```python
class EndgameClassInt(IntEnum): ...
_INT_TO_CLASS: dict[int, EndgameClass] = { ... }  # keys 1..6
_CLASS_TO_INT: dict[EndgameClass, int] = {v: k for k, v in _INT_TO_CLASS.items()}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dedup shared (game_id, endgame_class) subquery in query_endgame_timeline_rows</name>
  <files>app/repositories/endgame_repository.py</files>
  <action>
In `query_endgame_timeline_rows` (starts ≈ line 483), there are currently two structurally identical subqueries against `GamePosition`:

- `per_class_subq` (≈ lines 517-529): groups by `(game_id, endgame_class)`, HAVING `count(ply) >= ENDGAME_PLY_THRESHOLD`.
- `endgame_game_ids_subq` (≈ lines 585-594): groups by `game_id` alone, HAVING `count(ply) >= ENDGAME_PLY_THRESHOLD` — but this over-counts plies across ALL classes, which is a pre-existing subtle mismatch with Query A semantics.

Replace both with a single shared subquery and derive the non-endgame branch from it so semantics match exactly:

1. Keep `per_class_subq` as-is (it already selects `game_id, endgame_class` with the per-class HAVING). This is now the single source of truth for "games that reached a qualifying endgame span in at least one class."
2. Delete `endgame_game_ids_subq`.
3. Build the non-endgame exclusion as `select(per_class_subq.c.game_id).distinct()` (or reuse the subquery directly — `per_class_subq.c.game_id` yields duplicates across classes but `Game.id.notin_(...)` handles that correctly).
4. Change the `non_endgame_stmt.where(...)` clause so `Game.id.notin_(...)` references the derived game-id projection from `per_class_subq` instead of the now-deleted second subquery.

Rationale: one DB scan instead of two, and the endgame/non-endgame split becomes provably consistent (same HAVING, same class filter). This aligns with the existing comment that endgame_rows is "derived from these rows by deduplicating per game_id."

Preserve:
- All existing `apply_game_filters(...)` calls on `per_class_stmt` and `non_endgame_stmt`.
- All existing comments describing the indexes used and ordering guarantees; update any comment that still references the two-subquery design.
- Order and identity of returned tuples (`(endgame_rows, non_endgame_rows, per_type_rows)`).

Do NOT:
- Change the public function signature or return type.
- Change `per_class_stmt` (the joined statement) — it already depends on `per_class_subq`.
- Touch `query_endgame_entry_rows` or `query_endgame_bucket_rows`.

After editing, re-read the function top-to-bottom and confirm only ONE subquery remains that scans `GamePosition` with `endgame_class IS NOT NULL` + ply HAVING.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest tests/ -x -k "endgame or timeline"</automated>
  </verify>
  <done>query_endgame_timeline_rows contains exactly one subquery scanning GamePosition with the endgame_class+ply filter; the non-endgame branch references that subquery's game_id column via notin_(); tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Harden _INT_TO_CLASS lookup with Sentry-safe miss handling</name>
  <files>app/services/endgame_service.py</files>
  <action>
At `_aggregate_endgame_stats` (≈ line 204), replace:

```python
endgame_class = _INT_TO_CLASS[endgame_class_int]
```

with a `.get()` lookup that:

1. Attempts `_INT_TO_CLASS.get(endgame_class_int)`.
2. If the result is `None`, attaches Sentry context and captures, then skips the row via `continue`. Do NOT put the integer in the exception message — per CLAUDE.md Sentry rules, variables go through `set_context` / `set_tag`, never through formatted exception strings (fragments grouping).
3. Keeps all subsequent logic identical for the happy path.

Concrete shape:

```python
endgame_class = _INT_TO_CLASS.get(endgame_class_int)
if endgame_class is None:
    sentry_sdk.set_context(
        "invalid_endgame_class",
        {"class_int": endgame_class_int},
    )
    sentry_sdk.set_tag("source", "endgame_aggregate")
    sentry_sdk.capture_exception(
        ValueError("Unknown endgame_class integer from DB")
    )
    continue
```

Imports:
- Add `import sentry_sdk` at the top of the file (alphabetical with existing stdlib/third-party imports — it belongs with the third-party block). If already imported, reuse.

Also audit `app/services/endgame_service.py` for any OTHER `_INT_TO_CLASS[...]` subscripts (grep confirms one at ≈ line 1157). Line 1157 sits inside a loop iterating `_ENDGAME_CLASS_INTS` (keys that are definitionally valid), so do NOT change it — adding defensive handling there would be dead code. Leave a 1-line comment above line 1157 noting why the bracket access is safe there: keys come from `_ENDGAME_CLASS_INTS`, which is the authoritative class-int set.

Do NOT:
- Introduce logging-only handling — Sentry capture is required per CLAUDE.md.
- Embed `endgame_class_int` in any exception message or f-string used for exception construction.
- Raise; the row must be skipped so a single bad DB value cannot 500 the endpoint.
- Touch the Sentry init code or `app/main.py`.

CLAUDE.md compliance checklist for this task:
- [ ] `sentry_sdk.set_context("invalid_endgame_class", {...})` used for structured data.
- [ ] `sentry_sdk.capture_exception(ValueError("..."))` with a static, grouping-friendly message.
- [ ] No variable interpolation inside the exception message string.
- [ ] Explicit return types and types unchanged on the surrounding function (no new `any`).
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest tests/ -x -k "endgame"</automated>
  </verify>
  <done>_INT_TO_CLASS miss at line ~204 no longer raises KeyError; miss path calls sentry_sdk.set_context + capture_exception with a static message and continues; ty + ruff + tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Replace row-index access in _compute_score_gap_material with attribute access</name>
  <files>app/services/endgame_service.py</files>
  <action>
In `_compute_score_gap_material` (≈ lines 503-606), the function currently reads from rows via integer index:

- `rows_by_game[row[0]].append(row)` → `row.game_id`
- `imb = r[4]` → `r.user_material_imbalance`
- `imb_after = r[5]` → `r.user_material_imbalance_after`
- `min(game_rows, key=lambda r: r[1])` → `key=lambda r: r.endgame_class`
- `result_str: str = chosen_row[2]` → `chosen_row.result`
- `user_color: str = chosen_row[3]` → `chosen_row.user_color`

Both `query_endgame_bucket_rows` and `query_endgame_entry_rows` already return `Row` objects with labeled columns matching those attribute names (see `<interfaces>` block), so no SQL change is needed and callers of `_compute_score_gap_material` do not need to change.

Implementation details:

1. Update the type hint for `rows_by_game` — currently `dict[int, list[Sequence[Any]]]`. Change the value type to `list[Row[Any]]` (import `Row` is already present via `from sqlalchemy.engine import Row`). Similarly update the `chosen_row: Sequence[Any] | None = None` annotation to `chosen_row: Row[Any] | None = None`.
2. Update the `entry_rows` parameter annotation from `Sequence[Row[Any] | tuple[Any, ...]]` to `Sequence[Row[Any]]` since both producers return `Row`. Keep using `Sequence` (not `list`) so the parameter stays covariant — CLAUDE.md: "Use Sequence[str] (not list[str]) for function parameters."
3. Replace each index access listed above with attribute access.
4. Keep all comments (especially the "conversion-over-recovery tiebreak" block) untouched — they document invariants and have an invariant test referenced.
5. Do NOT switch to `row._mapping["..."]`; plain attribute access on `Row` is cleaner and already supported here.
6. Do NOT introduce a `NamedTuple` — the labeled `Row` already provides named access with zero surface-area change. The task context offered NamedTuple OR `._mapping` as alternatives and said "pick whichever is cleanest with minimal surface-area change"; labeled-Row attribute access satisfies that criterion without touching the repository layer.

After the edit:
- Re-read lines 552-606 and confirm no `r[N]` / `chosen_row[N]` / `row[N]` integer-subscript access remains inside `_compute_score_gap_material`.
- Grep the file for `\[[0-9]\]` within the function body to be sure.

Do NOT:
- Change the SQL in the repository functions.
- Change the public signature of `_compute_score_gap_material` (callers at lines 1053/1199/1262 must keep working unchanged).
- Reorder the conversion-over-recovery priority logic.
- Touch `_aggregate_endgame_stats`'s tuple unpacking at ≈ line 196 — that's a separate, already-named destructure and is not brittle.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run pytest</automated>
  </verify>
  <done>No integer-index row access remains inside _compute_score_gap_material; type annotations tightened to Row[Any]; full test suite green including the ScoreGapMaterialInvariant test referenced in the function's comment.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
uv run ruff check .
uv run ruff format --check .
uv run ty check app/ tests/
uv run pytest
```

All four must pass with zero errors/failures. CI runs this exact sequence.

Spot-checks to eyeball before committing:
- `app/repositories/endgame_repository.py`: `query_endgame_timeline_rows` has exactly one `GamePosition` subquery with `endgame_class.isnot(None)` + ply HAVING.
- `app/services/endgame_service.py` line ≈ 204: `.get(...)` + `if ... is None: sentry_sdk.set_context(...); sentry_sdk.capture_exception(...); continue`. Static message string. No `f"..."` in the exception.
- `app/services/endgame_service.py` `_compute_score_gap_material`: zero integer subscripts on rows.
</verification>

<success_criteria>
- Timeline query does a single GamePosition endgame scan.
- Bad endgame_class ints surface in Sentry and do not 500 the endpoint.
- `_compute_score_gap_material` consumes rows by attribute name; types are `Row[Any]` / `Sequence[Row[Any]]`.
- No unrelated refactors, no changes to SQL or public function signatures, no new dependencies.
- `ruff check`, `ruff format --check`, `ty check`, and `pytest` all pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-83b-fix-endgame-tab-code-review-dedup-timeli/260414-83b-SUMMARY.md` with:
- Which lines changed (before/after line ranges) in each file.
- Confirmation that the three fix intents were met (dedup / Sentry-safe / named access).
- Output of the final combined verify command.
</output>
