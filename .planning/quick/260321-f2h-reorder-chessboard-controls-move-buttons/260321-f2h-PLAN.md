---
phase: quick
plan: 260321-f2h
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Openings.tsx
  - frontend/src/components/board/BoardControls.tsx
autonomous: true
requirements: [quick-task]
must_haves:
  truths:
    - "Move buttons row appears directly below the chessboard, before the opening name row"
    - "Chessboard info popover icon is in the move buttons row on the right side"
    - "Move buttons always show bg-muted background (same grey as ToggleGroup active/hover state)"
    - "Move buttons are evenly spaced across the row width, leaving space for the info icon on the right"
    - "On mobile, move buttons are sticky along with the chessboard (inside the sticky container)"
  artifacts:
    - path: "frontend/src/pages/Openings.tsx"
      provides: "Reordered board controls layout for both desktop sidebar and mobile"
    - path: "frontend/src/components/board/BoardControls.tsx"
      provides: "Updated BoardControls with info icon slot, bg-muted background, and evenly spaced buttons"
  key_links:
    - from: "frontend/src/pages/Openings.tsx"
      to: "frontend/src/components/board/BoardControls.tsx"
      via: "InfoPopover passed as children/slot to BoardControls"
      pattern: "BoardControls.*InfoPopover"
---

<objective>
Reorder chessboard controls: move buttons row directly below chessboard (before opening name), with info popover icon integrated into the move buttons row. Style move buttons with persistent bg-muted background and even spacing.

Purpose: Improve mobile and desktop UX by putting interactive controls closer to the board and giving the move buttons a visible, consistent background.
Output: Updated Openings page layout and BoardControls component.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/Openings.tsx
@frontend/src/components/board/BoardControls.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BoardControls to accept info icon slot, add bg-muted background, and space buttons evenly</name>
  <files>frontend/src/components/board/BoardControls.tsx</files>
  <action>
    1. Add an optional `infoSlot` prop (type `React.ReactNode`) to BoardControlsProps for the chessboard info popover icon.

    2. Change the outer container div:
       - Use `flex items-center rounded-lg bg-muted` so the entire row always has the muted grey background (same grey that ToggleGroupItem uses for `data-[state=on]:bg-muted` and `hover:bg-muted`).
       - The row should contain: the 4 move buttons taking up available space, then the info icon on the far right.

    3. Space the 4 move buttons evenly:
       - Wrap the 4 buttons in a `div` with `flex flex-1 items-center justify-evenly` so they spread across the available width.
       - Keep button sizing as-is (`h-11 w-11 sm:h-8 sm:w-8`).
       - Change buttons from `variant="ghost"` to no variant styling interference. Since the parent has `bg-muted`, buttons should NOT have their own `bg-muted` on default state (that would be invisible). Instead, keep `variant="ghost"` which is transparent by default -- the parent bg-muted provides the always-on background. The hover state `hover:bg-muted` from ghost variant won't be visible against bg-muted parent. So override hover to a slightly darker shade: add `hover:bg-accent` class to each button (accent is slightly darker than muted in shadcn themes, matching the ToggleGroup hover feel).

    4. Render `infoSlot` on the right side of the container (after the buttons div), with `flex-shrink-0 mr-1` to keep it from being squished.
  </action>
  <verify>npm run build passes (no type errors)</verify>
  <done>BoardControls accepts infoSlot, renders with bg-muted background, buttons evenly spaced, info icon on right</done>
</task>

<task type="auto">
  <name>Task 2: Reorder Openings page layout -- move buttons below board, opening name below buttons, pass info icon to BoardControls</name>
  <files>frontend/src/pages/Openings.tsx</files>
  <action>
    CRITICAL: This component has SEPARATE desktop sidebar and mobile layout sections. Apply changes to BOTH.

    **Desktop sidebar** (the `sidebar` variable, lines ~230-410):
    Current order: ChessBoard -> Opening name + info icon -> MoveList -> BoardControls
    New order: ChessBoard -> BoardControls (with info icon) -> Opening name -> MoveList

    1. Move the `BoardControls` block to directly after `ChessBoard`.
    2. Pass the chessboard InfoPopover as the `infoSlot` prop to BoardControls. Remove the info icon from the opening name row.
    3. The opening name row (`div.flex.items-center`) should now only contain the opening name (ECO code + name, or "Play some moves" placeholder). Remove the `ml-auto flex-shrink-0` span wrapper that held the info icon.

    **Mobile layout** (lines ~530-723):
    Current order: Sticky ChessBoard -> Opening name + info icon -> BoardControls -> MoveList
    New order: Sticky ChessBoard + BoardControls (with info icon) -> Opening name -> MoveList

    1. Move the mobile `BoardControls` block INSIDE the sticky div (after the ChessBoard), so that move buttons are always visible along with the sticky board.
    2. Pass the mobile chessboard InfoPopover (testId="chessboard-info-mobile") as the `infoSlot` prop to BoardControls. Remove the info icon from the mobile opening name row.
    3. The mobile opening name row should only contain the opening name text.

    Ensure both desktop and mobile InfoPopovers retain their distinct `testId` values ("chessboard-info" for desktop, "chessboard-info-mobile" for mobile).
  </action>
  <verify>npm run build passes and npm run lint passes</verify>
  <done>
    - Move buttons row is directly below chessboard in both desktop and mobile layouts
    - Opening name row is below move buttons row (no info icon in it)
    - Chessboard info popover icon appears in move buttons row on the right
    - Move buttons have persistent grey background matching ToggleGroup active state
    - Buttons are evenly spaced across the row
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `npm run lint` passes
- Visual check: move buttons row appears directly below board with grey background
- Visual check: info icon is in move buttons row on the right
- Visual check: opening name row below move buttons, no info icon
</verification>

<success_criteria>
- Move buttons row positioned directly below chessboard (before opening name) in both desktop sidebar and mobile layout
- Chessboard info popover icon integrated into move buttons row on the right
- Move buttons row has persistent bg-muted grey background
- Buttons are evenly spaced across row width
- Both desktop and mobile layouts updated consistently
- On mobile, move buttons are sticky (inside the sticky board container), always visible when scrolling
</success_criteria>

<output>
After completion, create `.planning/quick/260321-f2h-reorder-chessboard-controls-move-buttons/260321-f2h-SUMMARY.md`
</output>
