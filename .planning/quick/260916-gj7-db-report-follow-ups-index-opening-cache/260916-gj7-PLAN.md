---
quick_id: 260916-gj7
slug: db-report-follow-ups-index-opening-cache
date: 2026-09-16
mode: inline
---

# Quick task 260916-gj7: DB report follow-ups (db-report-prod-2026-09-16)

Three follow-ups from `reports/db-stats/db-report-prod-2026-09-16.md`, executed inline (mechanical, ~10 min; no executor subagent).

## Task 1 — Index `opening_cache_audit.sample_game_id`

`sample_game_id` carries an `ON DELETE SET NULL` FK to `games` with no supporting index. Every user delete / re-import batch seq-scans the 2.5M-row audit table (prod: 3,266 scans x 158 ms, 9B tuples read). Add a partial index `WHERE sample_game_id IS NOT NULL` to the model `__table_args__` and an Alembic migration.

## Task 2 — Drop `ix_games_full_pv_pending`

30 MB, 0 scans in 12 weeks. Every live PV-pending query in `eval_queue_service.py` also filters `lichess_evals_at IS NOT NULL`, which `ix_games_lichess_pv_backfill_pending` covers. Model never declared it (migration-only, see comment in `app/models/game.py`), so drop it in the same migration; downgrade recreates it.

## Task 3 — Tighten db-report Check C

The `n_bad > 5 => INVESTIGATE` rule fires on benign same-sign, already-decisive depth disagreements (19/19 rows in the 2026-09-16 report). Gate the verdict on sign flips + equal-vs-decisive cases only; keep the raw >150cp count as a secondary reported number.

## Verification

- `uv run alembic upgrade head` on the dev DB, then `uv run alembic downgrade -1` / `upgrade head` round-trip.
- `uv run alembic check` reports no model/schema drift.
- ruff + ty on touched files.
