---
quick_id: 260628-1t5
type: execute
branch: gsd/phase-140-full-game-analysis-board
autonomous: true
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/lib/tacticDepth.ts
  - frontend/src/lib/tacticComparisonMeta.ts
  - frontend/src/hooks/useGameOverlay.ts
  - frontend/src/components/results/LibraryGameCard.tsx
  - frontend/src/components/library/EvalChart.tsx
  - frontend/src/pages/Analysis.tsx
  - frontend/src/components/analysis/VariationTree.tsx
  - frontend/src/lib/__tests__/tacticDepth.test.ts
  - frontend/src/lib/__tests__/tacticComparisonMeta.test.ts
  - frontend/src/lib/__tests__/resolveVisibleTactic.test.ts
  - frontend/src/components/analysis/__tests__/VariationTree.test.tsx
  - frontend/src/hooks/__tests__/useGameOverlay.test.ts
must_haves:
  truths:
    - "On a missed-tactic flaw position, the game card + analysis board show a NEW violet arrow for the move that should have been played (the same move FlawCard's blue arrow shows), carrying the missed-tactic depth label."
    - "The violet arrow and the existing blue following-best arrow BOTH render on a missed-tactic flaw position (DECISION 1)."
    - "The missed-tactic chip/marker is back on the flaw ply (not one ply earlier) on the analysis board move list, eval-chart tooltip, and overlay."
    - "Allowed-tactic depth labels on the navigable surfaces (game card, analysis board, move list, eval-chart tooltip) drop the +1 decision-anchor offset; the static FlawCard keeps it (DECISION 2)."
    - "The shared TAC_MISSED color family is recolored violet, so every missed-tactic surface (missed chips, eval-chart tooltip missed entries, the new should-have-played arrow + label) renders violet; the allowed family stays red (DECISION 3)."
    - "When a freely-played move is live-classified as a blunder/mistake, the move list paints the matching BlunderIcon/MistakeIcon, mirroring the board (item 1)."
  artifacts:
    - path: "frontend/src/lib/theme.ts"
      provides: "TAC_MISSED color family recolored from light-blue (hue 258) to violet (hue ~295-305); NO new arrow/label constants"
    - path: "frontend/src/hooks/useGameOverlay.ts"
      provides: "Violet (recolored TAC_MISSED) should-have-played arrow on the analysis board; blue arrow reverted to plain following-best"
    - path: "frontend/src/components/results/LibraryGameCard.tsx"
      provides: "Violet (recolored TAC_MISSED) should-have-played arrow on the game-card miniboard; blue arrow reverted"
  key_links:
    - from: "frontend/src/hooks/useGameOverlay.ts"
      to: "frontend/src/components/library/FlawCard.tsx"
      via: "violet arrow renders the SAME should-have-played move as FlawCard's blue arrow (empirical parity)"
      pattern: "TAC_MISSED"
    - from: "frontend/src/lib/tacticComparisonMeta.ts"
      to: "frontend/src/lib/tacticDepth.ts"
      via: "anchored flag threads through resolveVisibleTactic → toDisplayDepthForOrientation (display only, not the filter predicate)"
      pattern: "anchored"
---

<objective>
Phase 140 UAT round-6 follow-up. Three locked decisions from CONTEXT.md:

- DECISION 1 — Revert the "missed-tactic shown one ply early" shift (commits `cfaa7856`
  + `e116912c`). Put the missed marker/chip back on the flaw ply, and at the flaw ply
  draw the should-have-played (missed) move as a NEW violet arrow alongside the existing
  blue following-best arrow on the navigable surfaces (game card + analysis board).
- DECISION 2 — Drop the allowed `+1` decision-anchor depth offset ONLY on the navigable
  surfaces (game card, analysis board, move list, eval-chart tooltip). KEEP it on the
  static FlawCard. Do NOT touch the backend depth filter or the FE filter-predicate
  mirror (`slotPassesFilter`).
- DECISION 3 — Unify the missed-tactic color to violet by RECOLORING the existing shared
  `TAC_MISSED` family (hue 258 → violet ~295-305) in theme.ts. No separate violet
  constant; the new should-have-played arrow reuses `TAC_MISSED`. This intentionally
  turns every missed-tactic surface violet (missed chips everywhere, eval-chart tooltip
  missed entries) while the allowed family stays red.
- Item 1 — Surface the live free-move blunder/mistake classification in the move list so
  the icon matches the board.

Purpose: the ply-1 shift is conceptually wrong (the user lands AFTER the flawed move; the
counterfactual "you should have played X" belongs at the flaw ply, as a distinct arrow).
Output: violet arrow + reverted attribution + anchored-flag depth display + move-list glyph.

Frontend-only. Continues on the existing branch (no new branch).
</objective>

<context>
@.planning/quick/260628-1t5-phase-140-uat-revert-missed-tactic-ply-1/260628-1t5-CONTEXT.md
@CLAUDE.md

Ground-truth reference (DO NOT MODIFY its rendering):
@frontend/src/components/library/FlawCard.tsx

Commits being partially reverted — read both diffs before starting:
- `git show cfaa7856` (EvalChart + LibraryGameCard + useGameOverlay blue-arrow labeling)
- `git show e116912c` (Analysis.tsx flawMarkerByNodeId ply-1 split + activePvNodeId branch)

Key prior-art facts:
- `EvalPoint.best_move` (`@frontend/src/types/library.ts`) = "engine best move FROM this
  position". The miniboards source the blue following-best arrow from
  `bestMoveByPly.get(k+1)` (best move from the displayed post-move position).
- `FlawCardData.best_move` = "engine best move FROM the pre-flaw position" — the SAME move
  the new violet arrow must render. FlawCard draws it on the decision board; the navigable
  surfaces draw the same from/to on the post-flaw board (a counterfactual arrow).
- CRITICAL RISK: `best_move` ply indexing has caused repeated off-by-one bugs. Do NOT
  hard-code an index assumption. Verify the violet arrow renders the SAME move FlawCard
  shows for the same game+ply (Task 2 carries an automated index-parity test + a browser
  spot-check).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the allowed depth offset by an `anchored` flag (DECISION 2)</name>
  <files>frontend/src/lib/tacticDepth.ts, frontend/src/lib/tacticComparisonMeta.ts, frontend/src/hooks/useGameOverlay.ts, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/analysis/VariationTree.tsx, frontend/src/components/library/EvalChart.tsx, frontend/src/pages/Analysis.tsx, frontend/src/lib/__tests__/tacticDepth.test.ts, frontend/src/lib/__tests__/tacticComparisonMeta.test.ts, frontend/src/lib/__tests__/resolveVisibleTactic.test.ts, frontend/src/components/analysis/__tests__/VariationTree.test.tsx</files>
  <action>
Add an `anchored` parameter (default `true`) to the DISPLAY path only, per DECISION 2:

1. `tacticDepth.ts` — `toDisplayDepthForOrientation(depth, orientation, anchored = true)`:
   apply `ALLOWED_DECISION_DEPTH_OFFSET` only when `orientation === 'allowed' && anchored`.
   When `anchored === false`, allowed reads the same as missed (raw + DEPTH_DISPLAY_OFFSET).
   Keep the existing JSDoc; note the new flag.
2. `tacticComparisonMeta.ts` — thread `anchored` through the DISPLAY computation only:
   - `resolveVisibleTactic(orientation, motif, depth, filter?, anchored = true)` — pass
     `anchored` into the `toDisplayDepthForOrientation` call that builds `depthLabel`.
   - `tacticDepthBadge(motif, depth, orientation, anchored = true)` — forward to
     `resolveVisibleTactic`.
   DO NOT touch `slotPassesFilter`: its `ALLOWED_DECISION_DEPTH_OFFSET` use is the FE mirror
   of the backend filter predicate and must stay (CONTEXT: do not touch the depth filter).
3. Navigable-surface callers pass `anchored = false` (missed/allowed read on one plain scale
   because missed + allowed are no longer co-anchored on one board there):
   - `useGameOverlay.ts` `buildPlyMaps`: both `tacticDepthBadge(...)` calls → `false`.
   - `LibraryGameCard.tsx` `tacticDepthByPly`: both `tacticDepthBadge(...)` calls → `false`.
   - `VariationTree.tsx` `renderFlawChip` (~line 310): `tacticDepthBadge(motif, depth, orientation, false)`.
   - `EvalChart.tsx` tooltip (~line 1083): `toDisplayDepthForOrientation(depth, orientation, false)`.
   - `Analysis.tsx` `rootDisplayDepth` (~line 430): `toDisplayDepthForOrientation(depthRaw, orientation, false)`.
4. `FlawCard.tsx` — leave UNCHANGED (relies on the `anchored = true` default).
5. Update tests:
   - `tacticDepth.test.ts`: keep the existing `anchored`-default assertions (allowed = +2);
     add cases for `anchored = false` (allowed equals missed, e.g.
     `toDisplayDepthForOrientation(0, 'allowed', false) === 1`).
   - `tacticComparisonMeta.test.ts`: keep default-anchored assertions; add an
     `anchored = false` case (e.g. `tacticDepthBadge('fork', 0, 'allowed', false) === '1'`).
   - `resolveVisibleTactic.test.ts`: keep the default depthLabel + filter-parity tests
     unchanged (filter predicate is untouched); add an `anchored = false` depthLabel case.
   - `VariationTree.test.tsx`: the allowed chip now drops the +1 — update test 9b's
     expectation (`allowedDepth: 1` now displays `2`, not `3`); the assertions checking the
     chip text must reflect the new value.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/tacticDepth.test.ts src/lib/__tests__/tacticComparisonMeta.test.ts src/lib/__tests__/resolveVisibleTactic.test.ts src/components/analysis/__tests__/VariationTree.test.tsx</automated>
  </verify>
  <done>
`toDisplayDepthForOrientation` / `tacticDepthBadge` / `resolveVisibleTactic` accept an
`anchored` flag; navigable callers pass `false` (allowed depth no longer +1), FlawCard keeps
`true`, `slotPassesFilter` is untouched. Updated tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Revert the ply-1 shift; recolor TAC_MISSED violet; add the violet should-have-played arrow (DECISIONS 1 + 3)</name>
  <files>frontend/src/lib/theme.ts, frontend/src/hooks/useGameOverlay.ts, frontend/src/components/results/LibraryGameCard.tsx, frontend/src/components/library/EvalChart.tsx, frontend/src/pages/Analysis.tsx, frontend/src/hooks/__tests__/useGameOverlay.test.ts</files>
  <action>
First read `git show cfaa7856` and `git show e116912c` to know exactly what to undo.

A. Recolor the shared TAC_MISSED family violet (DECISION 3) — do NOT add new constants.
   In `theme.ts`, change `TAC_MISSED`, `TAC_MISSED_BG`, `TAC_MISSED_BORDER`, and
   `TAC_MISSED_LABEL` from light-blue (oklch hue 258) to violet/purple (oklch hue ~295-305),
   keeping the existing lightness/chroma values per constant so they stay readable on the
   dark UI (only the hue number changes). The allowed family (`TAC_ALLOWED*`, light red) is
   UNCHANGED. Because the family is shared, this also recolors every missed-tactic chip and
   the EvalChart tooltip missed entries violet — that is the intended unified semantic. No
   separate violet arrow/label constant: the new arrow reuses `TAC_MISSED` + `TAC_MISSED_LABEL`.

B. Revert cfaa7856:
   - `useGameOverlay.ts`: remove `missedAheadLabel` and its `label`/`labelColor` from the
     blue best-move arrow push (~lines 181-196). The blue arrow returns to plain
     following-best (`bestMoveByPly.get(k+1)`), no label. Keep the `TAC_MISSED`/`TAC_MISSED_LABEL`
     imports — the new violet arrow (step D) uses them.
   - `LibraryGameCard.tsx`: remove `missedAhead` and the `label`/`labelColor` from the blue
     arrow in the `boardArrows` memo (~lines 567-584); restore its deps to `[bestMoveSquares]`.
   - `EvalChart.tsx`: revert the tooltip `nextMarker` logic (~lines 774-810) — restore
     `if (activeMarker == null) return [];`, remove `nextMarker`, and source the missed motif
     from `activeMarker.missed_tactic_motif`/`missed_tactic_depth` again (allowed already
     came from `activeMarker`).

C. Revert e116912c (`Analysis.tsx`):
   - `flawMarkerByNodeId`: restore the single-entry-per-flaw-node build — the missed motif +
     depth go back onto the flaw node `mainLine[fm.ply]` (NOT `mainLine[fm.ply-1]`), together
     with the allowed motif + severity. Remove the `ensure(...)` decision-node split and the
     `missedPly` plumbing.
   - `activePvNodeId`: restore `mainLine[activePvFlaw.ply]` for both orientations (remove the
     `orientation === 'missed' ? ply-1 : ply` branch).
   - Remove the now-dead `activePvOrientation` prop wiring IF nothing else needs it; if the
     prop's existence is harmless keep it but it should resolve to a single node again.
   NOTE: leave the item-4 live-eval cache / `useLiveMoveFlaw` wiring from e116912c intact —
   that is the free-move classification, not part of the ply-1 revert.

D. Add the violet should-have-played arrow at the flaw ply (the core of DECISION 1):
   - `useGameOverlay.ts`: when the displayed main-line ply `k` is a flaw ply with a VISIBLE
     missed tactic, push a violet arrow whose from/to = the should-have-played (missed) move.
     Source that move from the eval series (NOT `bestMoveByPly.get(k+1)`, which is the blue
     following-best). The should-have-played move is the engine best move FROM the decision
     position `mainLine[k-1]`. Determine the correct `bestMoveByPly` index EMPIRICALLY so the
     from/to equals FlawCard's blue arrow for the same game+ply (see the parity test below) —
     do not assume `k` vs `k-1` vs `k+1`. Gate the arrow on a visible missed tactic via the
     existing `depthByPly`/`resolveVisibleTactic` path; label it with the missed depth
     (`anchored = false`) using `TAC_MISSED_LABEL`, arrow color `TAC_MISSED` (the now-violet
     family from step A — no separate constant). Both the blue following-best and this violet
     arrow render together (DECISION 1).
   - `LibraryGameCard.tsx`: mirror the same violet arrow in the miniboard `boardArrows` memo
     using its `tacticDepthByPly` + `bestMoveByPly` (same empirically-verified index).
   - Allowed tactic / clean ply: NO violet arrow (unchanged behavior).

E. Parity test (`useGameOverlay.test.ts`): add a fixture where `eval_series[*].best_move`
   differs at each ply (so an off-by-one is caught) and a missed-tactic flaw marker at a known
   ply. Assert the overlay emits a violet arrow (color `TAC_MISSED`) whose from/to equals the
   engine best move from the DECISION position (the pre-flaw position), AND that the blue
   `BEST_MOVE_ARROW` still points at the following-best from the displayed position. This is
   the regression guard for the recurring off-by-one.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/__tests__/useGameOverlay.test.ts</automated>
  </verify>
  <done>
The missed marker/chip/tooltip are back on the flaw ply (cfaa7856 + e116912c ply-1 logic
reverted). The shared `TAC_MISSED` family is recolored violet (hue ~295-305) with no new
constants; the allowed family stays red. On a missed-tactic flaw position both surfaces render
the blue following-best arrow AND a violet (`TAC_MISSED`) should-have-played arrow; the violet
arrow's from/to matches FlawCard's blue arrow for the same game+ply (proven by the new parity
test); its depth label uses the missed depth with no allowed offset. The live free-move
cache/classification from e116912c is untouched. Colors come from theme.ts. Browser spot-check
(claude-in-chrome): open a real game's analysis board, navigate to a known missed-tactic flaw,
confirm (a) the violet arrow matches the FlawCard's blue arrow on the same flaw, mirrored on
the game-card miniboard; (b) the missed chips AND the eval-chart tooltip missed entries now
render violet while allowed entries stay red; (c) the FlawCard's should-have-played ARROW is
still blue (`BEST_MOVE_ARROW`) and only its depth label is violet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Show the live free-move blunder/mistake glyph in the move list (item 1)</name>
  <files>frontend/src/pages/Analysis.tsx, frontend/src/components/analysis/VariationTree.tsx, frontend/src/components/analysis/__tests__/VariationTree.test.tsx</files>
  <action>
The board already paints a live free-move severity glyph via `liveFlaw` (`useLiveMoveFlaw`).
The move list (`VariationTree`) paints severity glyphs only from `flawMarkerByNodeId`, which
covers main-line game nodes only — free-play / what-if nodes get nothing.

1. `Analysis.tsx`: derive the live free-move severity for the current node and inject it into
   the marker map handed to `VariationTree`, keyed by `currentNodeId`, when `liveFlaw`
   classifies a blunder or mistake (inaccuracy/clean render no glyph in the list, matching the
   mobile main-line behavior). The injected entry only needs `severity` (+ a `ply` placeholder).
   - Build a derived map that merges `flawMarkerByNodeId` with the one live entry; pass THIS
     to `VariationTree`. The live glyph must appear even in free-play mode, so do not gate the
     passed map behind `isGameMode` for the live entry (game-mode tactic chips stay game-only).
   - The live severity is available from `liveFlaw.squareMarkers[0].severity` (the same source
     that drives the board glyph) — reuse it so the list and board can never disagree.
2. `VariationTree.tsx`: render the severity glyph for the current free-move/variation node too.
   Today the desktop renderer reads the flaw entry only for `!isVariation` nodes (~line 544)
   and the mobile renderer maps only main-line nodes (~line 388). Allow the variation-chain /
   free node that matches `currentNodeId` to read its injected `flawMarkerByNodeId` entry and
   paint the `BlunderIcon`/`MistakeIcon`, on BOTH desktop and mobile renderers (mobile parity,
   CLAUDE.md). Keep tactic chips main-line-only — this change is the severity glyph only.
3. `VariationTree.test.tsx`: add a case asserting a variation/free node with an injected
   `{ severity: 'blunder' }` entry renders the blunder glyph in the move list.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/VariationTree.test.tsx</automated>
  </verify>
  <done>
Playing a free move the live engine grades as a blunder/mistake paints the matching glyph in
the move list, on desktop and mobile, matching the board glyph (verified live earlier for the
board). Test asserts a free/variation node with an injected severity renders the glyph.
  </done>
</task>

</tasks>

<verification>
Full frontend gate (MANDATORY — lint + tests do NOT type-check, so `tsc -b` is required per
CLAUDE.md):

```bash
cd frontend && npm run lint && npm test -- --run && npx tsc -b
```

Plus the empirical FlawCard-parity check for the violet arrow:
- Automated: the `useGameOverlay.test.ts` parity test (Task 2E) proves the violet arrow's
  from/to is the should-have-played move (decision-position best move), distinct from the blue
  following-best, with a per-ply-varying fixture that catches an off-by-one.
- Browser (claude-in-chrome): on a real game analysis board, navigate to a known missed-tactic
  flaw and confirm the violet arrow renders the SAME move as that flaw's FlawCard blue arrow;
  confirm the same on the game-card miniboard.

Plus the DECISION 3 color check (browser): missed-tactic chips AND eval-chart tooltip missed
entries render violet; allowed entries stay red; the FlawCard should-have-played arrow stays
blue with a violet depth label.
</verification>

<success_criteria>
- Missed-tactic attribution reverted to the flaw ply across board, miniboard, eval-chart
  tooltip, and move list (cfaa7856 + e116912c ply-1 logic undone; live free-move cache kept).
- Violet should-have-played arrow renders at the flaw ply on the game card + analysis board,
  alongside the blue following-best arrow, matching FlawCard's should-have-played move
  (empirically verified). Colors live in theme.ts.
- The shared `TAC_MISSED` family is recolored violet (no new constants), turning missed chips +
  eval-chart tooltip missed entries violet; allowed stays red; the FlawCard arrow stays blue
  with a violet depth label (DECISION 3).
- Allowed depth labels drop the +1 on navigable surfaces (`anchored = false`); FlawCard keeps
  it; the backend/FE filter predicate is untouched.
- Free-move blunder/mistake glyph appears in the move list (desktop + mobile), matching the board.
- `cd frontend && npm run lint && npm test -- --run && npx tsc -b` all pass.
- Atomic commits per task on `gsd/phase-140-full-game-analysis-board`.
</success_criteria>

<output>
Atomic commits per task on the existing branch. No SUMMARY required beyond the quick-task
record; update STATE.md quick-task table on completion.
</output>
