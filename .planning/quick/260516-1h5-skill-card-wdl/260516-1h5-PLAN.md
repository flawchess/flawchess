---
phase: 260516-1h5-skill-card-wdl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/EndgameSkillCard.tsx
  - frontend/src/components/charts/EndgameMetricsSection.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx
autonomous: true
requirements: [SKILL-WDL-01]
must_haves:
  truths:
    - "The Endgame Skill card shows a Win/Draw/Loss MiniWDLBar between the gauge and the Score Gap bullet"
    - "The Skill card games indicator uses the Swords icon and matches the visual styling of the Conv/Parity/Recovery cards' games-count row"
    - "The games-count value reflects endgame_wdl.total (Games with Endgame)"
    - "The WDL block is hidden when there is no skill data (existing 'Not enough data yet' branch)"
    - "Existing gauge, Score Gap bullet, and InfoPopover copy remain unchanged"
  artifacts:
    - path: "frontend/src/components/charts/EndgameSkillCard.tsx"
      provides: "Skill card with MiniWDLBar + Swords games-count row"
      contains: "MiniWDLBar"
    - path: "frontend/src/components/charts/EndgameMetricsSection.tsx"
      provides: "Threads endgame_wdl prop down to EndgameSkillCard"
    - path: "frontend/src/pages/Endgames.tsx"
      provides: "Passes perfData.endgame_wdl into EndgameMetricsSection"
  key_links:
    - from: "frontend/src/pages/Endgames.tsx"
      to: "EndgameMetricsSection"
      via: "endgameWdl prop"
      pattern: "endgameWdl="
    - from: "EndgameMetricsSection"
      to: "EndgameSkillCard"
      via: "endgameWdl prop"
      pattern: "endgameWdl="
    - from: "EndgameSkillCard"
      to: "MiniWDLBar"
      via: "win_pct/draw_pct/loss_pct from endgame_wdl"
      pattern: "MiniWDLBar"
---

<objective>
Replace the plain `Games: N` text in the Endgame Skill card with the same games-count + MiniWDLBar layout used by the Conversion/Parity/Recovery cards.

Purpose: Surface the "Games with Endgame" Win/Draw/Loss split inline in the Skill card so users see the same data shape across all four endgame metric cards.
Output: Updated Skill card, prop threading through EndgameMetricsSection and Endgames.tsx, and updated unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/components/charts/EndgameSkillCard.tsx
@frontend/src/components/charts/EndgameMetricCard.tsx
@frontend/src/components/charts/EndgameMetricsSection.tsx
@frontend/src/pages/Endgames.tsx
@frontend/src/types/endgames.ts
@frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx

<interfaces>
<!-- Key types and the reference layout the executor must mirror. -->

From frontend/src/types/endgames.ts:
- `EndgameWDLSummary` has fields `wins`, `draws`, `losses`, `total`, `win_pct`, `draw_pct`, `loss_pct`.
- `EndgamePerformanceResponse.endgame_wdl: EndgameWDLSummary` is already populated by the backend; no API changes needed.

Reference layout to mirror (EndgameMetricCard.tsx ~lines 159-180):
- Outer `<div className="flex flex-col gap-2">` wraps a header row + MiniWDLBar.
- Header row: `<span className="flex items-center gap-2 text-sm tabular-nums w-full">`
  - Left: `<span className="text-muted-foreground">Win/Draw/Loss</span>`
  - Right (ml-auto): inline-flex containing `Games: N` text and a `<Swords className="h-3.5 w-3.5" aria-hidden="true" />` icon.
  - `data-testid={`${tileTestId}-games-count`}` on the right span.
- MiniWDLBar import: `import { MiniWDLBar } from '@/components/stats/MiniWDLBar'`.

Skill card differences from the reference:
- No share-of-parent percentage. Show `Games: {N}` only (no `45.5% (143)` form). Use `endgame_wdl.total` as N.
- The block lives inside the existing `hasSkill` branch in `EndgameSkillCard.tsx`, replacing the current simple `Games: {totalGames}` span (around lines 144-149 of the file).
- `tileTestId` for the Skill card is the existing `tile-endgame-skill` (so the new testid becomes `tile-endgame-skill-games-count`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Games-with-Endgame WDL block to the Skill card and thread endgame_wdl through</name>
  <files>
    frontend/src/components/charts/EndgameSkillCard.tsx,
    frontend/src/components/charts/EndgameMetricsSection.tsx,
    frontend/src/pages/Endgames.tsx,
    frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx
  </files>
  <action>
    In `EndgameSkillCard.tsx`:
    - Import `Swords` from `lucide-react` (alongside existing `Cpu` import) and `MiniWDLBar` from `@/components/stats/MiniWDLBar`.
    - Import the `EndgameWDLSummary` type from `@/types/endgames`.
    - Add a required prop `endgameWdl: EndgameWDLSummary` to the component's props interface. Remove or repurpose the current `totalGames` prop if it becomes redundant after the change — keep it only if other code paths still need it; otherwise derive the games count from `endgameWdl.total`.
    - Inside the existing `hasSkill` branch, replace the existing simple `Games: {totalGames}` span (around lines 144-149) with the two-part block: a flex column wrapping (a) a header row with `Win/Draw/Loss` label on the left and a right-aligned `Games: {N}` + `Swords` icon span, and (b) a `MiniWDLBar` driven by `endgameWdl.win_pct/draw_pct/loss_pct`. Mirror the exact classNames from `EndgameMetricCard.tsx` lines 159-180 (`flex flex-col gap-2`, `flex items-center gap-2 text-sm tabular-nums w-full`, `ml-auto inline-flex items-center gap-1 text-sm text-muted-foreground tabular-nums whitespace-nowrap`, `Swords` icon with `h-3.5 w-3.5 aria-hidden="true"`, MiniWDLBar wrapped in `<div className="min-w-0">`). Do NOT include a share percentage — render only `Games: {endgameWdl.total}` (per context: there is no natural parent denominator at the aggregate level).
    - Use `data-testid="tile-endgame-skill-games-count"` on the right-aligned span (matching the `${tileTestId}-games-count` pattern used by the metric cards).
    - Guard the WDL block with `endgameWdl.total > 0` so it does not render in an empty/no-data state. The existing `Not enough data yet` branch (when `skill === null`) continues to handle the no-skill case; this guard prevents an empty bar inside an otherwise rendered Skill card.
    - Leave the gauge, the Score Gap bullet, the InfoPopover copy, the `Cpu` icon usage on eval-derived rows, and the connector-arrows overlay untouched.

    In `EndgameMetricsSection.tsx`:
    - Add an `endgameWdl: EndgameWDLSummary` prop (or include it in the existing props bag) and forward it to `<EndgameSkillCard endgameWdl={endgameWdl} ... />`.
    - Import `EndgameWDLSummary` from `@/types/endgames` if not already imported.

    In `frontend/src/pages/Endgames.tsx`:
    - Pass `endgameWdl={perfData.endgame_wdl}` to `<EndgameMetricsSection ... />` (around line 492 per context).

    In `frontend/src/components/charts/__tests__/EndgameSkillCard.test.tsx`:
    - Update existing tests to pass the new `endgameWdl` prop. Build minimal `EndgameWDLSummary` fixtures inline (e.g. `{ wins: 50, draws: 30, losses: 20, total: 100, win_pct: 50, draw_pct: 30, loss_pct: 20 }`).
    - Add at least two new assertions:
      1. When `endgameWdl.total > 0` and `hasSkill` is true, the element with `data-testid="tile-endgame-skill-games-count"` contains the text `Games: 100` (using the fixture above) and the MiniWDLBar is in the DOM (assert by role/test-id matching whatever pattern `MiniWDLBar` exposes — see how `EndgameMetricCard.test.tsx` queries it; if no test-id exists on MiniWDLBar, query by its container class or by the three child segments).
      2. When `endgameWdl.total === 0`, the games-count testid is absent (the WDL block is hidden).
    - Do not loosen any existing assertions; only update prop wiring and add the new cases.

    Honor CLAUDE.md style: no em-dashes in UI copy/comments, min font size `text-sm`, no new interactive elements (so no extra test-ids beyond `tile-endgame-skill-games-count`), and avoid commentary explaining WHAT the code does — a short pointer comment `// Mirror of the games-count row in EndgameMetricCard.tsx` above the block is sufficient.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- EndgameSkillCard --run</automated>
  </verify>
  <done>
    - `EndgameSkillCard` renders a `Win/Draw/Loss` header row with `Games: {endgame_wdl.total}` + Swords icon and a `MiniWDLBar` between the gauge and the Score Gap bullet whenever `endgameWdl.total > 0` and `hasSkill` is true.
    - `EndgameMetricsSection` and `Endgames.tsx` thread `endgameWdl` from `perfData.endgame_wdl` down to the Skill card.
    - Updated `EndgameSkillCard.test.tsx` passes, including the new games-count + hidden-when-zero assertions.
    - `npm run lint`, `npx tsc --noEmit`, and the targeted `npm test -- EndgameSkillCard --run` all pass with zero errors.
    - No other components (Score Gap bullet, InfoPopover, Overall Performance section, connector overlay) are modified.
  </done>
</task>

</tasks>

<verification>
- Visual check (manual, optional): on the Endgames page, the Skill card now mirrors the games-count row layout of the Conv/Parity/Recov cards and shows the MiniWDLBar below it.
- All frontend CI gates pass: `npm run lint`, `npx tsc --noEmit`, `npm test`.
</verification>

<success_criteria>
- Skill card surfaces "Games with Endgame" WDL inline using the same layout/icon as the other three metric cards.
- Prop signature change in `EndgameSkillCard` is reflected at every call site and in tests.
- No scope creep: no changes to gauge, Score Gap bullet, InfoPopover copy, or backend.
</success_criteria>

<output>
After completion, create `.planning/quick/260516-1h5-skill-card-wdl/260516-1h5-SUMMARY.md`.
</output>
