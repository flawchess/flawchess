---
phase: 260508-r61
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/openings_repository.py
  - app/repositories/stats_repository.py
  - app/schemas/openings.py
  - app/schemas/opening_insights.py
  - app/services/openings_service.py
  - app/services/opening_insights_service.py
  - frontend/src/types/api.ts
  - frontend/src/types/insights.ts
  - frontend/src/lib/relativeDate.ts
  - frontend/src/components/insights/WdlConfidenceTooltip.tsx
  - frontend/src/components/insights/ScoreConfidencePopover.tsx
  - frontend/src/components/move-explorer/MoveExplorer.tsx
  - frontend/src/components/move-explorer/__tests__/MoveExplorer.test.tsx
  - frontend/src/pages/Openings.tsx
  - frontend/src/components/stats/OpeningStatsCard.tsx
  - frontend/src/components/insights/OpeningFindingCard.tsx
  - frontend/src/pages/__tests__/Openings.statsBoard.test.tsx
  - frontend/src/components/stats/__tests__/OpeningStatsCard.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "WdlConfidenceTooltip shows 'Last played: <relative>' when lastPlayedAt is provided, omits the line when null"
    - "All 4 score-confidence surfaces (move-explorer, stats-board bullet, OpeningFindingCard, OpeningStatsCard) pass last_played_at from API to tooltip"
    - "MoveExplorer Score cell renders a HelpCircle icon LEFT of the % that opens a tap/hover popover; no hover-only Tooltip on the % anymore"
    - "MoveExplorer troll icon uses the same tap/hover popover pattern as TranspositionInfo (mobile-tappable, stops propagation)"
    - "Selected move row is visibly highlighted on top of score-zone tinted backgrounds (Tailwind v4 important suffix on selected class)"
    - "Backend MAX(games.played_at) added to query_next_moves, query_resulting_position_wdl, query_opening_transitions, and query_position_wdl_batch"
  artifacts:
    - path: "frontend/src/lib/relativeDate.ts"
      provides: "ISO string -> relative label (Just now / X minutes/hours/days/weeks/months/years ago)"
    - path: "frontend/src/components/insights/WdlConfidenceTooltip.tsx"
      provides: "Tooltip body with optional last-played paragraph"
    - path: "frontend/src/components/move-explorer/MoveExplorer.tsx"
      provides: "Score cell with click-to-open popover; troll popover; selected-row important class"
  key_links:
    - from: "MoveExplorer Score cell"
      to: "WdlConfidenceTooltip via local ScoreInfo popover"
      via: "PopoverPrimitive (Radix) like TranspositionInfo"
      pattern: "data-testid=\"move-explorer-score-info-"
    - from: "Repository SELECT clauses"
      to: "Pydantic schemas -> services -> TS types -> components"
      via: "func.max(Game.played_at).label('last_played_at')"
      pattern: "last_played_at"
---

<objective>
Add a "Last played: <relative>" line to the WDL confidence tooltip across all 4 score surfaces, replace the move-explorer Score cell hover-tooltip with a tap-friendly info-icon popover, fix the troll icon mobile-tap bug, and fix the selected-row highlight on tinted backgrounds.

Purpose: distinguish historical vs current strengths/weaknesses; make tooltips and icons fully usable on mobile; restore selected-row visibility on tinted rows.

Output: backend last_played_at threaded through 4 query paths; frontend tooltip + 2 popover patterns + 1 important-class fix; tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Existing patterns the executor must mirror. Extracted from codebase. -->

Touch-friendly popover blueprint -- `frontend/src/components/move-explorer/MoveExplorer.tsx` lines 400-450 (TranspositionInfo):
- Radix `PopoverPrimitive.Root` with controlled `open` state
- 100ms hover delay via setTimeout, cleared on mouseleave
- `onClick={(e) => e.stopPropagation()}` on trigger to prevent row-click move-play
- Same className stack on PopoverPrimitive.Content

HelpCircle popover blueprint -- `frontend/src/components/insights/ScoreConfidencePopover.tsx`:
- Already uses HelpCircle h-4 w-4 + WdlConfidenceTooltip body
- Already has hover+tap behavior via setTimeout(100ms)

Existing WdlConfidenceTooltip props (`frontend/src/components/insights/WdlConfidenceTooltip.tsx`):
```typescript
interface WdlConfidenceTooltipProps {
  level: 'low' | 'medium' | 'high';
  pValue: number;
  score: number;
  gameCount: number;
}
```
Add `lastPlayedAt: string | null` (ISO 8601). When null, omit the paragraph.

Score cell to refactor -- `frontend/src/components/move-explorer/MoveExplorer.tsx` lines 373-388:
```tsx
<td className="py-1 text-right tabular-nums">
  <Tooltip content={<WdlConfidenceTooltip ... />}>
    <span className="font-semibold" style={...}>{Math.round(entry.score * 100)}%</span>
  </Tooltip>
</td>
```
Becomes (pseudocode):
```tsx
<td className="py-1 text-right tabular-nums">
  <span className="inline-flex items-center justify-end gap-1">
    <ScoreInfo entry={entry} />
    <span className="font-semibold" style={...}>{Math.round(entry.score * 100)}%</span>
  </span>
</td>
```

Selected-row highlight bug -- same file lines 319-321:
```tsx
!IS_TOUCH && 'hover:bg-foreground/10!',         // already has !
selectedMove === entry.move_san && 'bg-foreground/10',  // MISSING !
```
Inline `style.backgroundColor` (the score-zone tint) overrides plain Tailwind. Add `!`.

Repository pattern -- `app/repositories/openings_repository.py:372 query_next_moves` already uses `func.count`/`func.sum` style aggregations with `.label(...)`. Add:
```python
func.max(Game.played_at).label("last_played_at")
```
to the SELECT (no GROUP BY change needed; it's an aggregate).

Position-level WDL: `query_resulting_position_wdl` (line 586) feeds the parent-FEN score on the stats board. `query_opening_transitions` (line 670) feeds insights. `query_position_wdl_batch` (`app/repositories/stats_repository.py:372`) feeds OpeningStatsCard.

Insights schemas: `app/schemas/opening_insights.py` -- the per-finding model used in white_weaknesses/black_weaknesses/white_strengths/black_strengths. Add `last_played_at: datetime | None` and propagate from service.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — last_played_at through 4 query paths and schemas/services</name>
  <files>app/repositories/openings_repository.py, app/repositories/stats_repository.py, app/schemas/openings.py, app/schemas/opening_insights.py, app/services/openings_service.py, app/services/opening_insights_service.py, plus relevant tests</files>
  <action>
For each query function, add `func.max(Game.played_at).label("last_played_at")` to the SELECT (the aggregate composes with existing GROUP BY clauses; no GROUP BY changes needed):

1. `app/repositories/openings_repository.py::query_next_moves` (line 372) -- per-move row for the move-explorer table.
2. `app/repositories/openings_repository.py::query_resulting_position_wdl` (line 586) -- parent-FEN aggregate WDL shown on the Openings stats-board score bullet.
3. `app/repositories/openings_repository.py::query_opening_transitions` (line 670) -- feeds opening insights. Add MAX to the SELECT and include it in the row tuple/dict the function returns.
4. `app/repositories/stats_repository.py::query_position_wdl_batch` (line 372) -- feeds OpeningStatsCard. Add MAX per position in the per-hash mapping.

Schemas:
- `app/schemas/openings.py::NextMoveEntry`: add `last_played_at: datetime | None = None` (Pydantic v2). Add to any sibling response model used for the position-level WDL (search for the response shape that wraps the parent-FEN score bullet) and mirror the field there.
- `app/schemas/opening_insights.py`: add `last_played_at: datetime | None = None` to the per-finding model.
- Any response model feeding OpeningStatsCard: add the field. Search backwards from the OpeningStatsCard fetch on the frontend through the API client to find the endpoint, then the response model.

Services:
- `app/services/openings_service.py`: when mapping repo rows to `NextMoveEntry`, pass `row.last_played_at`. Same for the position-level mapper.
- `app/services/opening_insights_service.py`: when constructing finding objects from `query_opening_transitions` rows, thread `row.last_played_at` into each finding.
- Whichever service feeds OpeningStatsCard: thread the field through.

Tests:
- Extend the existing repo test for `query_next_moves` (most likely `tests/repositories/test_openings_repository.py`) to assert `last_played_at == MAX(played_at)` over the grouped games. Mirror the existing fixture pattern.
- If service tests build fixture rows mocking repo output, add `last_played_at` to those fixtures so they keep passing.

CLAUDE.md compliance:
- No magic numbers (none introduced).
- Pydantic v2 throughout.
- Use `Sequence[...]` for any new function params that take list-of-Literal.
- Explicit return type annotations on new/modified helpers.
- ty must pass with zero errors. New `datetime | None` fields require `from datetime import datetime` (already imported in most schemas; verify before adding).

Quality gates (must all pass):
- `uv run ruff check . && uv run ruff format .`
- `uv run ty check app/ tests/`
- `uv run pytest`
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest -x</automated>
  </verify>
  <done>All 4 repo functions return last_played_at; all corresponding schemas/services expose it; ty/ruff/pytest green.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — relativeDate helper, WdlConfidenceTooltip last-played line, propagate through call sites</name>
  <files>frontend/src/lib/relativeDate.ts, frontend/src/types/api.ts, frontend/src/types/insights.ts, frontend/src/components/insights/WdlConfidenceTooltip.tsx, frontend/src/components/insights/ScoreConfidencePopover.tsx, frontend/src/pages/Openings.tsx, frontend/src/components/stats/OpeningStatsCard.tsx, frontend/src/components/insights/OpeningFindingCard.tsx, frontend/src/pages/__tests__/Openings.statsBoard.test.tsx, frontend/src/components/stats/__tests__/OpeningStatsCard.test.tsx</files>
  <action>
1. Create `frontend/src/lib/relativeDate.ts`:
   - Export `formatRelativeDate(iso: string, now?: Date): string` returning "Just now" / "1 minute ago" / "5 minutes ago" / "X hours ago" / "X days ago" / "X weeks ago" / "X months ago" / "X years ago".
   - Pluralize correctly (1 vs >1).
   - Constants for thresholds (no magic numbers): `SECONDS_IN_MINUTE = 60`, `MINUTES_IN_HOUR = 60`, `HOURS_IN_DAY = 24`, `DAYS_IN_WEEK = 7`, `DAYS_IN_MONTH = 30`, `DAYS_IN_YEAR = 365`. Add a `JUST_NOW_THRESHOLD_SECONDS = 30`.
   - Export `formatAbsoluteDate(iso: string): string` returning a long-form local-tz string for the title attribute (e.g. via `new Date(iso).toLocaleString()`).
   - Export both as named exports. Add to knip-watched exports only if used; both will be used.
   - All function signatures explicitly typed.

2. Update `frontend/src/components/insights/WdlConfidenceTooltip.tsx`:
   - Extend `WdlConfidenceTooltipProps` with `lastPlayedAt: string | null`.
   - After the headline+confidence paragraph and BEFORE the italic methodology block, render:
     ```tsx
     {lastPlayedAt && (
       <p>
         Last played: <span title={formatAbsoluteDate(lastPlayedAt)}>{formatRelativeDate(lastPlayedAt)}</span>
       </p>
     )}
     ```
   - When `lastPlayedAt` is null, the paragraph is omitted (not "never").

3. Update API types:
   - `frontend/src/types/api.ts`: add `last_played_at: string | null` to NextMoveEntry, the position-WDL response, and OpeningStatsCard's response (mirror Task 1 schema changes).
   - `frontend/src/types/insights.ts`: add `last_played_at: string | null` to the per-finding type.

4. Update `frontend/src/components/insights/ScoreConfidencePopover.tsx`:
   - Add `lastPlayedAt: string | null` to `ScoreConfidencePopoverProps`.
   - Pass through to the inner `<WdlConfidenceTooltip ... lastPlayedAt={lastPlayedAt} />`.

5. Update all ScoreConfidencePopover call sites:
   - `frontend/src/pages/Openings.tsx` line 877 area: pass `lastPlayedAt` from the position-WDL fetch.
   - `frontend/src/components/stats/OpeningStatsCard.tsx` line 205: pass `lastPlayedAt` from the per-card data.
   - `frontend/src/components/insights/OpeningFindingCard.tsx` line 191: pass `lastPlayedAt` from the finding object.
   - `frontend/src/components/move-explorer/MoveExplorer.tsx`: this file has its own local Tooltip + WdlConfidenceTooltip on the % cell -- handled in Task 3.

6. Update tests:
   - `frontend/src/pages/__tests__/Openings.statsBoard.test.tsx` (lines 68-87): add `lastPlayedAt={null}` (or a sample ISO string) to the test props so types still compile and rendering matches snapshot/asserts.
   - `frontend/src/components/stats/__tests__/OpeningStatsCard.test.tsx`: add `last_played_at` to fixture data so the component still renders (any breaking type errors get fixed here).
   - Add a small test for `formatRelativeDate` covering: just-now, 1 minute ago, 5 minutes ago, 1 hour ago, 1 day ago, 7 days ago (week), 30 days (month), 365 days (year). Place at `frontend/src/lib/__tests__/relativeDate.test.ts`.
   - If a test for `WdlConfidenceTooltip` exists, extend it for the new line; otherwise create one asserting the line renders when `lastPlayedAt` is provided and is omitted when null.

CLAUDE.md compliance:
- `noUncheckedIndexedAccess` is on -- not relevant here, but watch any new array access.
- Theme constants in theme.ts -- no new colors.
- Mobile-first: nothing new visual yet (Task 3 handles the popover UX).
- Knip: ensure new exports are used. The two helpers + `lastPlayedAt` prop are all consumed below.

Quality gates:
- `cd frontend && npm run lint`
- `cd frontend && npm run knip`
- `cd frontend && npm test`
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run knip && npm test -- --run</automated>
  </verify>
  <done>relativeDate helper exists with tests; WdlConfidenceTooltip renders the line; all 3 ScoreConfidencePopover call sites pass lastPlayedAt; tests green; knip clean.</done>
</task>

<task type="auto">
  <name>Task 3: MoveExplorer — score info-icon popover, troll popover, selected-row highlight fix</name>
  <files>frontend/src/components/move-explorer/MoveExplorer.tsx, frontend/src/components/move-explorer/__tests__/MoveExplorer.test.tsx</files>
  <action>
All three changes live in `frontend/src/components/move-explorer/MoveExplorer.tsx`. Make atomic, surgical edits; mirror the existing `TranspositionInfo` component (lines 400-450) as the popover blueprint.

A. Selected-row highlight fix (lines 319-321):
   - Change `'bg-foreground/10'` -> `'bg-foreground/10!'` so the Tailwind v4 important suffix beats the inline `style.backgroundColor` zone tint. Mirror the existing hover line.
   - Comment above the cn() call: explain that selected and hover both need `!` to win over the inline tint set via `rowStyle`.

B. Score cell info-icon popover (lines 373-388):
   - Replace the existing `<Tooltip>...<span>%</span></Tooltip>` with:
     ```tsx
     <span className="inline-flex items-center justify-end gap-1">
       <ScoreInfo entry={entry} />
       <span className="font-semibold" style={scoreColor ? { color: scoreColor } : undefined}>
         {Math.round(entry.score * 100)}%
       </span>
     </span>
     ```
   - Add a new local component `ScoreInfo` after `TranspositionInfo` (~line 451) following the same Radix pattern:
     - Trigger: a `<button type="button">` (NOT a span -- table cell + accessibility) with `<HelpCircle className="inline h-4 w-4" />`.
     - `data-testid={\`move-explorer-score-info-${moveSan}\`}`.
     - `aria-label={\`Score confidence details for ${moveSan}\`}`.
     - `onClick={(e) => e.stopPropagation()}` so taps don't play the move.
     - Hover delay: 100ms via setTimeout, cleared on mouseleave (mirror TranspositionInfo).
     - Body: `<WdlConfidenceTooltip level={...} pValue={...} score={...} gameCount={...} lastPlayedAt={entry.last_played_at} />`.
     - PopoverPrimitive.Content: same className stack as TranspositionInfo.
   - Score column width may need a small bump. Inspect the current `<th>` for the Score column; if width-constrained (e.g. `w-[3rem]`), bump to `w-[4.5rem]` or use `min-w-[4.5rem]`. Otherwise leave alone -- inline-flex with gap-1 is usually enough. Use judgment; don't over-engineer.

C. Troll icon mobile-tap fix (lines 336-357):
   - Replace the existing `<Tooltip content="Considered a troll opening">...</Tooltip>` with a new local component `TrollIcon` (placed after `ScoreInfo`):
     - Same Radix popover pattern as TranspositionInfo and ScoreInfo.
     - Trigger: `<button type="button">` wrapping the existing SVG. Keep the SVG markup (lines 338-355) untouched inside the button.
     - `data-testid={\`move-explorer-troll-${moveSan}\`}`.
     - `aria-label="Considered a troll opening"` on the button. Remove the duplicate aria-label on the inner svg (or leave it; not harmful).
     - `onClick={(e) => e.stopPropagation()}`.
     - Body: a plain text "Considered a troll opening".
     - PopoverPrimitive.Content: same className stack as TranspositionInfo.

Tests (`frontend/src/components/move-explorer/__tests__/MoveExplorer.test.tsx`):
   - Add fixtures with `last_played_at` populated for new entries.
   - Add a test asserting the `move-explorer-score-info-${san}` button exists, clicking it does NOT trigger the row's onMoveClick handler (use `userEvent.click` and assert the move-play callback was not called).
   - Add a test asserting the popover content renders the "Last played:" line when fixture has `last_played_at`.
   - Add a test for the troll icon: `move-explorer-troll-${san}` exists, click does not bubble to row.
   - Add a test for the selected-row class: rendering with `selectedMove === entry.move_san` includes `bg-foreground/10!` in the row's className. (A smoke check against `getByTestId(\`move-explorer-row-${san}\`).className` is enough.)

Mobile compliance per CLAUDE.md:
- ScoreInfo and TrollIcon both use Radix popovers triggered by tap (Radix popover handles click+focus on trigger natively); hover is the desktop accelerator with 100ms delay. This is the pattern already validated by `TranspositionInfo`.
- Both triggers are real `<button>`s (not `<div onClick>`).
- Both have `data-testid`s in the kebab-case `{component}-{element}-{id}` convention.

Quality gates:
- `cd frontend && npm run lint`
- `cd frontend && npm run knip`
- `cd frontend && npm test`
- `cd frontend && npm run build`

Manual sanity (executor: skip in headless run; surface to user on completion):
- Open Openings -> Moves on a touch device or DevTools mobile emulator.
- Tap the new info icon next to a Score % -> popover opens, move does NOT play.
- Tap the troll icon (where present) -> popover opens, move does NOT play.
- Click a tinted (green/red) row -> selection highlight visible on top of the tint.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run knip && npm test -- --run && npm run build</automated>
  </verify>
  <done>Score cell shows HelpCircle + popover with last-played line; troll icon uses popover; selected-row class has `!`; tests cover new behavior; build passes.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

Backend:
- `uv run ruff check . && uv run ruff format --check .`
- `uv run ty check app/ tests/` -- zero errors
- `uv run pytest` -- all green
- Spot-check via SQL (dev DB): `SELECT MAX(played_at) FROM games WHERE user_id = ...` returns a value, and the corresponding API response now includes `last_played_at`.

Frontend:
- `cd frontend && npm run lint`
- `cd frontend && npm run knip`
- `cd frontend && npm test -- --run`
- `cd frontend && npm run build`

Manual UI sanity (single browser session):
1. Openings -> Moves table: each row's Score cell shows a small ? icon + %. Tap/click the ? icon -> popover with last-played line. Tap the row (not the icon) -> move plays.
2. Troll-icon row (e.g. 1.f3): tap the troll icon -> popover with "Considered a troll opening". Move does NOT play.
3. Click a strongly tinted (green or red) row -> selection highlight clearly visible.
4. Stats-board score bullet -> ? icon -> popover shows last-played line.
5. OpeningStatsCard score popover -> last-played line.
6. OpeningFindingCard score popover -> last-played line.
</verification>

<success_criteria>
- All 4 backend query paths return `last_played_at`.
- All 4 surfaces (move-explorer, stats-board bullet, OpeningStatsCard, OpeningFindingCard) display the "Last played: ..." line in the WDL confidence tooltip when data is present.
- MoveExplorer Score cell uses an info-icon popover that works on touch (no hover-only Tooltip).
- MoveExplorer troll icon works on touch and doesn't trigger move-play.
- Selected move row is visibly highlighted on tinted backgrounds.
- All quality gates green: ruff, ty, pytest, npm lint, knip, vitest, build.
- No new magic numbers; no `any`; no hard-coded theme colors.
</success_criteria>

<output>
After completion, create `.planning/quick/260508-r61-score-info-icons-and-last-played-in-tool/260508-r61-SUMMARY.md` covering: backend SELECT additions, schema/service propagation, the new relativeDate helper, the WdlConfidenceTooltip extension, the new ScoreInfo and TrollIcon popovers in MoveExplorer, and the selected-row important-class fix.

Per CLAUDE.md "main branch" rule: do NOT commit to main. Each task can be committed atomically inside the worktree branch only; merge back is the orchestrator's responsibility after user approval.
</output>
