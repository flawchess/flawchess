---
phase: quick-260531-f7s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/EndgameTimePressureSection.tsx
  - frontend/src/components/charts/EndgameTimePressureCard.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx
  - frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx
autonomous: true
requirements: [QUICK-260531-f7s]

must_haves:
  truths:
    - "Time Pressure TC cards render as full-width vertical rows, not a 2×2 grid"
    - "Each TC row is collapsible via the shared Accordion, identical behavior to the Endgame Metrics by TC section"
    - "The most-active TC (time-weighted) starts expanded; all others start collapsed"
    - "Collapsed header shows only TC name + game count + collapse toggle (no summary metrics)"
    - "Expanded body is a 2-column layout: Score Gap chart left (~2/3), gauges right (~1/3), vertical separator between"
    - "On mobile the two columns stack (chart on top, gauges below) split by a horizontal separator"
  artifacts:
    - path: "frontend/src/components/charts/EndgameTimePressureSection.tsx"
      provides: "Accordion-based section orchestrator with primary-TC default-expand"
      contains: "Accordion"
    - path: "frontend/src/components/charts/EndgameTimePressureCard.tsx"
      provides: "AccordionItem card with 2-column body (chart left, gauges right)"
      contains: "AccordionItem"
  key_links:
    - from: "EndgameTimePressureSection.tsx"
      to: "computePrimaryTc"
      via: "default-expand seeding + filterKey reset"
      pattern: "computePrimaryTc"
    - from: "EndgameTimePressureCard.tsx"
      to: "AccordionTrigger/AccordionContent"
      via: "shared accordion primitives"
      pattern: "AccordionTrigger"
---

<objective>
Redesign the endgame "Time Pressure" section so its per-TC cards (bullet/blitz/rapid/classical) match the collapsible, full-width-row pattern already used by the sibling "Endgame Metrics by Time Control" section (`EndgameMetricsByTcSection` / `EndgameMetricsByTcCard`). Reuse the existing shared `Accordion` primitives, the `computePrimaryTc` heuristic, and the existing vertical/horizontal separator styling. No new components, no backend changes, no new API fields.

Purpose: Visual + behavioral consistency across sibling endgame sections. The current 2×2 grid cannot cleanly collapse-all-but-most-active without ragged gaps; full-width stacked rows can.

Output: Updated `EndgameTimePressureSection.tsx`, `EndgameTimePressureCard.tsx`, one Endgames.tsx prop wiring, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# The exact sibling pattern to mirror (read these — they are the template):
@frontend/src/components/charts/EndgameMetricsByTcSection.tsx
@frontend/src/components/charts/EndgameMetricsByTcCard.tsx
@frontend/src/lib/primaryTc.ts

# The files being changed:
@frontend/src/components/charts/EndgameTimePressureSection.tsx
@frontend/src/components/charts/EndgameTimePressureCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert EndgameTimePressureSection to an Accordion with primary-TC default-expand</name>
  <files>frontend/src/components/charts/EndgameTimePressureSection.tsx, frontend/src/pages/Endgames.tsx</files>
  <action>
Replace the dynamic grid (the `GRID_ONE_CARD`/`GRID_TWO_CARDS`/`GRID_THREE_CARDS`/`GRID_FOUR_CARDS` constants and the `gridClass` ternary IIFE) with the controlled `Accordion` pattern copied from `EndgameMetricsByTcSection`. Specifically:

- Delete the four `GRID_*` constants and the `(() => { ... })()` block that selects `gridClass`.
- Import `useState`, `useEffect` from react; `Accordion` from `@/components/ui/accordion`; `computePrimaryTc` from `@/lib/primaryTc`; `MIN_GAMES_PER_TC_CARD` from `@/generated/endgameZones`.
- Add a `filterKey?: string` prop to the component's props (same JSDoc intent as `EndgameMetricsByTcSection`: changing it resets the accordion to the recomputed primary TC).
- Add a `buildByTcForPrimary` helper that maps `data.cards` to `Record<string, { total: number }[]>` using `card.total` (mirror the sibling's helper exactly — `byTc[card.tc] = [{ total: card.total }]`).
- Seed expanded state: `const [expandedTcs, setExpandedTcs] = useState<string[]>(() => { const primary = computePrimaryTc(buildByTcForPrimary(data.cards), MIN_GAMES_PER_TC_CARD); return primary ? [primary] : []; });`
- Add the `useEffect` keyed on `[filterKey]` that recomputes the primary TC and resets `expandedTcs` (copy the sibling's effect, including the eslint-disable comment).
- Keep `grandTotal` computation (`data.cards.reduce((acc, c) => acc + c.total, 0)`) and keep passing `grandTotal` + `ratingAnchor={anchors[card.tc]}` to each `EndgameTimePressureCard`.
- Render the cards inside `<Accordion type="multiple" value={expandedTcs} onValueChange={setExpandedTcs} className="flex flex-col gap-4 mt-2">` (mirror sibling exactly), replacing the old `<div className={gridClass}>`.
- Keep the existing section wrapper (`data-testid="time-pressure-cards-section"`, the intro `<p>`, and the empty-state branch with `data-testid="time-pressure-cards-empty"`) unchanged.

In `frontend/src/pages/Endgames.tsx` (~line 649), add `filterKey={JSON.stringify(appliedFilters)}` to the `<EndgameTimePressureSection ... />` call site, matching how `EndgameMetricsByTcSection` is already wired at ~line 625. Leave `data` and `ratingAnchors` props as-is.

Honor CLAUDE.md frontend rules: no `text-xs`, theme constants for any colors (none new needed here), `noUncheckedIndexedAccess` narrowing already satisfied by the sibling pattern.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Section renders a controlled `Accordion type="multiple"`; primary TC (time-weighted argmax via `computePrimaryTc`) starts expanded; `filterKey` reset effect present; Endgames.tsx passes `filterKey`. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert EndgameTimePressureCard to an AccordionItem with a 2-column body</name>
  <files>frontend/src/components/charts/EndgameTimePressureCard.tsx</files>
  <action>
Restructure the card to mirror `EndgameMetricsByTcCard` (the AccordionItem + AccordionTrigger header band + AccordionContent body), while keeping ALL existing Time Pressure sub-components (`ClockGapHeaderRow`, `NetFlagRateRow`, the `MiniBulletChart` Clock Gap bullet, `ScoreGapByTimePressureChart`, percentile chips, popovers) intact. Steps:

1. Import `AccordionContent, AccordionItem, AccordionTrigger` from `@/components/ui/accordion`.
2. Replace the outer `<div className="charcoal-texture rounded-md overflow-hidden" ...>` with `<AccordionItem value={card.tc} data-testid={`time-pressure-card-${card.tc}`} className="charcoal-texture rounded-md overflow-hidden border-none">` (copy the sibling's class string exactly).
3. Replace the `<h3 ...>` header band with an `<AccordionTrigger>` copied structurally from `EndgameMetricsByTcCard`:
   - `data-testid={`time-pressure-card-${card.tc}-trigger`}`, `aria-label={`${tcLabel} time pressure`}`, and the sibling's className string (`w-full flex items-center gap-2 px-4 py-3 bg-black/20 border-0 rounded-none data-[state=open]:border-b data-[state=open]:border-b-border/40 text-left hover:no-underline hover:bg-black/30 cursor-pointer [&>svg:last-child]:ml-0`).
   - Inside it, an inner div carrying the existing `data-testid={`time-pressure-card-${card.tc}-header`}` containing: the `TimeControlIcon`, the TC label (`<h3 className="text-base font-semibold">{tcLabel}</h3>`), and the existing right-aligned game-count span (`data-testid={`time-pressure-card-${card.tc}-total`}`, `Games: X% (N)` + Swords icon). Keep the `pctOfTotal` computation. The collapsed header must show ONLY TC name + game count + collapse toggle — the chevron is supplied automatically by `AccordionTrigger`. Do NOT surface any summary metrics in the header.
4. Wrap the body in `<AccordionContent className="p-0">` and inside it build a 2-column flex container: `<div className="flex flex-col lg:flex-row p-4">`.
   - LEFT column (~2/3): `<div className="flex-1 min-w-0 lg:basis-2/3">` containing the existing "Score Gap by Remaining Time" block — the subtitle row (`-quintiles-subtitle`), its `InfoPopover` (`-quintiles-info`), the Time Pressure Score Gap `PercentileChip`, and the `<ScoreGapByTimePressureChart quintiles={card.quintiles} tc={card.tc} />` (`-score-gap-chart`). Move this block verbatim from the current bottom of the card.
   - SEPARATOR: reuse the sibling's `divider` JSX pattern — a vertical rule on desktop, horizontal on mobile:
     `<div className="hidden lg:block w-px bg-border/40 mx-6" aria-hidden="true" />` and `<div className="block lg:hidden border-t border-border/40 my-4" aria-hidden="true" />`. Place both between the left and right columns.
   - RIGHT column (~1/3): `<div className="lg:basis-1/3 lg:shrink-0 flex flex-col gap-4">` containing the two gauge bars stacked: Clock Gap on top (the `ClockGapHeaderRow` + `MiniBulletChart` Clock Gap bullet block, `-top-zone`/`-clock-gap-bullet`), then the existing inner horizontal separator (`border-t border-border/40 mt-3`), then `NetFlagRateRow` below. Move this `-top-zone` block verbatim from the current top of the card. Drop the old full-width `border-t border-border/40` separator that previously sat between the top zone and the chart (its role is now played by the column separator).
5. Keep the early return `if (card.total < MIN_GAMES_PER_TC_CARD) return null;` BUT note it now lives inside an Accordion — returning null is fine (the sibling does not early-return, but the backend pre-filters so cards passed in are eligible; null-return for a defensive sub-floor case is acceptable and keeps existing test behavior).

Preserve every existing `data-testid`, `aria-label`, `InfoPopover`, `PercentileChip`, and `MetricStatPopover` exactly — this is a layout reflow, not a content change. Honor CLAUDE.md: `text-sm` floor (the percentile-chip/popover `text-xs` exceptions already in the file are allowed tooltip surfaces — leave them), theme constants, semantic HTML, aria-label on the icon-only toggle (AccordionTrigger chevron is decorative/aria-hidden via the shared primitive; the trigger itself carries the aria-label).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Card renders as `AccordionItem` → `AccordionTrigger` (collapsed header = TC name + count + toggle only) → `AccordionContent` with a `flex-col lg:flex-row` body: Score Gap chart left (~2/3), Clock Gap + Net flag gauges right (~1/3), vertical separator desktop / horizontal mobile. All prior testids/popovers/chips preserved. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests for the accordion + 2-column layout and run all gates</name>
  <files>frontend/src/components/charts/__tests__/EndgameTimePressureSection.test.tsx, frontend/src/components/charts/__tests__/EndgameTimePressureCard.test.tsx</files>
  <action>
Update the two affected test suites so they assert the new structure instead of the removed grid:

In `EndgameTimePressureSection.test.tsx`:
- Replace the entire `describe('EndgameTimePressureSection — SC-1: dynamic grid layout', ...)` block (the four `w-1/2` / `grid-cols-2` / `grid-cols-3` / `lg:grid-cols-2` assertions, ~lines 213-249) with assertions for the accordion: the section renders a Radix Accordion root (query `[data-testid="time-pressure-cards-section"]` and assert it contains elements with `[data-testid^="time-pressure-card-"][data-testid$="-trigger"]`), and that exactly one TC trigger is expanded by default (the time-weighted primary). Use `computePrimaryTc` semantics in the fixture: give one TC the highest time-weighted total and assert its `AccordionContent` is open (`[data-state="open"]`) while others are closed. Add the `matchMedia` + `ResizeObserver` stubs in a `beforeAll` if the suite lacks them (copy from the card test). Render the section with a `filterKey` prop.
- Update the empty-state test if it asserts grid absence (it currently checks "The grid should not be rendered") — keep asserting `time-pressure-cards-empty` is present and no `time-pressure-card-*-trigger` exists.
- Keep the visibility, all-4-TC ordering, legacy-absence, grandTotal-propagation, and section-wrapper tests; adjust only DOM queries broken by the accordion wrapper (e.g. cards now live under AccordionItems). If a card body assertion now requires the item to be expanded, expand it via `fireEvent.click` on the trigger first.

In `EndgameTimePressureCard.test.tsx`:
- The card now renders inside an Accordion context. Wrap renders of `EndgameTimePressureCard` in an `<Accordion type="multiple" value={[card.tc]}>` (import from `@/components/ui/accordion`) so `AccordionItem`/`AccordionContent` mount and the body is open for body-content assertions. Add a small helper `renderCard(card, props)` that wraps in the open Accordion.
- Update any assertion that depended on the old flat `<div>` structure (e.g. the order of top-zone vs chart) to the new 2-column body. Keep all testid-based assertions (`-clock-gap-bullet`, `-net-flag-rate-row`, `-score-gap-chart`, `-clock-gap-header`, etc.) — they still exist.
- Add one assertion that the body uses the 2-column flex layout (query the AccordionContent body wrapper and assert its className contains `lg:flex-row`), and that a vertical separator (`w-px`) and horizontal separator (`border-t`) divider pair is present.

Then run the full frontend gate suite and fix anything that surfaces.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run src/components/charts/__tests__/EndgameTimePressureSection.test.tsx src/components/charts/__tests__/EndgameTimePressureCard.test.tsx && npx tsc --noEmit && npm run knip</automated>
  </verify>
  <done>Both updated test suites pass; lint clean; tsc zero errors; knip reports no new dead exports (the deleted `GRID_*` constants were module-local, not exported, so knip stays clean).</done>
</task>

</tasks>

<verification>
Full frontend gate (the locked completion criteria — no dev DB reset involved):

```bash
cd frontend && npm run lint && npm test -- --run && npx tsc --noEmit && npm run knip
```

All four must pass. Tests run against fixtures, not the dev DB.
</verification>

<success_criteria>
- Time Pressure section renders full-width stacked TC rows inside a shared `Accordion type="multiple"` (no 2×2 grid).
- Most-active TC (time-weighted via `computePrimaryTc`) starts expanded; others collapsed; `filterKey` change resets to the recomputed primary.
- Collapsed header is identical in shape to `EndgameMetricsByTcCard`: TC icon + name + game count + chevron toggle, no summary metrics.
- Expanded body: Score Gap chart left (~2/3), Clock Gap + Net flag gauges stacked right (~1/3), vertical separator desktop / horizontal separator mobile — reusing the sibling's divider pattern.
- All existing data-testids, popovers, percentile chips, and aria-labels preserved.
- `npm run lint && npm test -- --run && npx tsc --noEmit && npm run knip` all pass.
</success_criteria>

<output>
Create `.planning/quick/260531-f7s-redesign-endgame-time-pressure-section-c/260531-f7s-SUMMARY.md` when done.
</output>
