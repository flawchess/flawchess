---
phase: quick-260519-vf2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_zones.py
  - frontend/src/lib/endgameEntryEvalZones.ts
  - frontend/src/generated/endgameZones.ts
  - frontend/src/components/charts/EndgameOverallShared.ts
  - .planning/STATE.md
autonomous: false
requirements: [benchmarks-diff-2026-05-17-vs-2026-05-19]
must_haves:
  truths:
    - "EG-entry-eval bullet paints green/red at ±0.60 pawns (was ±0.75) on both stacks"
    - "minor_piece recovery gauge neutral band is (0.28, 0.38)"
    - "rook achievable_score_gap band is (-0.05, +0.05); queen is (-0.04, +0.05)"
    - "Score Gap bullet half-domain is 0.22 (p05 −22.1pp no longer clipped)"
    - "WDL bar still renders in EndgameTypeCard (decision-reversal confirmed)"
    - "Closed-as-moot bookkeeping recorded for 3.1.4/3.1.5/§3.3.3 deferrals"
  artifacts:
    - path: "app/services/endgame_zones.py"
      provides: "Updated entry_eval_pawns + PER_CLASS_GAUGE_ZONES bands"
    - path: "frontend/src/generated/endgameZones.ts"
      provides: "Regenerated TS mirror (D/E/F), CI drift gate clean"
    - path: "frontend/src/lib/endgameEntryEvalZones.ts"
      provides: "Hand-written ±0.60 EG-entry band (A — not codegen'd)"
  key_links:
    - from: "scripts/gen_endgame_zones_ts.py"
      to: "frontend/src/generated/endgameZones.ts"
      via: "uv run python scripts/gen_endgame_zones_ts.py"
      pattern: "PER_CLASS_GAUGE_ZONES"
---

<objective>
Implement the "Net actionable worklist" from `reports/benchmarks-diff-2026-05-17-vs-2026-05-19.md`
(report is authoritative scope). Game-time rating bucketing retired several deferrals and
shifted four zone bands.

Purpose: keep FlawChess zone bands aligned with the regenerated benchmark cohort
distribution so gauges paint correctly.

Output: updated Python zone registry + regenerated TS mirror + one hand-written
TS band + one frontend domain constant + STATE bookkeeping + two flagged blockers
for user decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@reports/benchmarks-diff-2026-05-17-vs-2026-05-19.md
@reports/benchmarks-latest.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Pinned during planning — exact current values, no guessing -->

app/services/endgame_zones.py:
  ZONE_REGISTRY["entry_eval_pawns"] = ZoneSpec(typical_lower=-0.75, typical_upper=0.75, "higher_is_better")  # lines 253-261
  PER_CLASS_GAUGE_ZONES["minor_piece"].recovery = (0.31, 0.41)                                                # line 473
  PER_CLASS_GAUGE_ZONES["rook"].achievable_score_gap = (-0.05, 0.04)                                          # line 468
  PER_CLASS_GAUGE_ZONES["queen"].achievable_score_gap = (-0.05, 0.05)                                         # line 486
  NOTE: entry_eval_pawns is NOT codegen-emitted (gen script imports only ZONE_REGISTRY
        subset for SCORE_GAP/ACHIEVABLE/etc.; entry_eval has NO codegen line). Its FE
        mirror is the hand-written file below.

frontend/src/lib/endgameEntryEvalZones.ts (HAND-WRITTEN, not generated):
  export const ENDGAME_ENTRY_EVAL_NEUTRAL_MIN_PAWNS = -0.75;   // line 24
  export const ENDGAME_ENTRY_EVAL_NEUTRAL_MAX_PAWNS = 0.75;    // line 27
  (ENDGAME_ENTRY_EVAL_DOMAIN_PAWNS = 2.25 and CENTER = 0 stay UNCHANGED — report only
   tightens the neutral band, not the axis domain.)

frontend/src/components/charts/EndgameOverallShared.ts:
  export const SCORE_GAP_DOMAIN = 0.20;   // line 17

frontend/src/lib/endgameMetrics.ts:
  export const SHOW_WDL_BAR_IN_TYPE_CARDS: boolean = true;   // line 95 — already true (item G is confirm-only)

PER_CLASS_SCORE_BULLET_ZONES values DO exist in reports/benchmarks-latest.md line 280:
  rook(−0.060,+0.071) minor_piece(−0.067,+0.078) pawn(−0.077,+0.089)
  queen(−0.090,+0.125) mixed(−0.038,+0.061) pawnless(−0.197,+0.045)
  — but introducing it is a new registry+codegen+FE-wiring feature AND the report
    offers an explicit alternative ("document the global band as deliberate editorial
    tightening"). See Task 5 BLOCKER.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python zone-registry edits (A, D, E, F) + regenerate TS</name>
  <files>app/services/endgame_zones.py, frontend/src/generated/endgameZones.ts</files>
  <action>
    In app/services/endgame_zones.py make exactly these four edits:
    (A) ZONE_REGISTRY["entry_eval_pawns"]: typical_lower -0.75 → -0.60, typical_upper
        0.75 → 0.60. Update the inline comment (lines ~254-258): replace the
        "max(|p25|,|p75|)=75 cp=0.75 pawns" justification with the new tightened band
        and cite reports/benchmarks-diff-2026-05-17-vs-2026-05-19.md item A
        ("editorial-tighten inside the IQR so the 0-centered EG-entry tile actually
        paints; live was 0.75"). Keep it terse, no em-dashes.
    (D) PER_CLASS_GAUGE_ZONES["minor_piece"].recovery: (0.31, 0.41) → (0.28, 0.38).
        Add a one-line comment referencing the diff report item D (pooled recovery
        32.7% drifts −3.3pp from old midpoint).
    (E) PER_CLASS_GAUGE_ZONES["rook"].achievable_score_gap: (-0.05, 0.04) →
        (-0.05, 0.05). Update its existing inline comment to note the +0.6pp upper
        edge drift > 0.5pp tolerance (diff item E).
    (F) PER_CLASS_GAUGE_ZONES["queen"].achievable_score_gap: (-0.05, 0.05) →
        (-0.04, 0.05). Update its inline comment for the +0.8pp lower edge drift
        (diff item F).
    Do NOT touch any other band. Then run `uv run python scripts/gen_endgame_zones_ts.py`
    to regenerate frontend/src/generated/endgameZones.ts. The script auto-mirrors D/E/F
    via _format_per_class_gauge_zones(); A does NOT appear in the generated file (entry_eval
    is not codegen'd) — that mirror is handled in Task 2.
    No magic numbers introduced; these are existing named ZoneSpec/tuple fields.
  </action>
  <verify>
    <automated>uv run ruff check app/services/endgame_zones.py &amp;&amp; uv run ruff format --check app/services/endgame_zones.py &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run python scripts/gen_endgame_zones_ts.py --check &amp;&amp; uv run pytest -q -k "endgame_zones or zone" 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    Four bands updated; `gen_endgame_zones_ts.py --check` exits 0 (drift gate clean);
    generated TS shows minor_piece recovery [0.28,0.38], rook asg [-0.05,0.05],
    queen asg [-0.04,0.05]; ruff/ty/pytest green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-written EG-entry TS band (A frontend mirror) + SCORE_GAP_DOMAIN (B)</name>
  <files>frontend/src/lib/endgameEntryEvalZones.ts, frontend/src/components/charts/EndgameOverallShared.ts</files>
  <action>
    (A frontend) In frontend/src/lib/endgameEntryEvalZones.ts: ENDGAME_ENTRY_EVAL_NEUTRAL_MIN_PAWNS
    -0.75 → -0.60; ENDGAME_ENTRY_EVAL_NEUTRAL_MAX_PAWNS 0.75 → 0.60. Update the module
    docstring (lines ~6-12) and the two per-const JSDoc comments to reflect the ±0.60
    band and cite the diff report item A. Leave ENDGAME_ENTRY_EVAL_DOMAIN_PAWNS (2.25)
    and ENDGAME_ENTRY_EVAL_CENTER (0) UNCHANGED — only the neutral band tightens. This
    file is the hand-maintained backend-parity mirror of ZONE_REGISTRY["entry_eval_pawns"]
    (not codegen'd); it MUST match the Task 1 Python value exactly.
    (B) In frontend/src/components/charts/EndgameOverallShared.ts: SCORE_GAP_DOMAIN
    0.20 → 0.22 (line 17). Update the comment (lines 15-16): p05 tail −22.1pp was
    clipped at the ±0.20 axis edge; ±0.22 covers it. Cite diff report item B.
    Do NOT change ENDGAME_TYPE_SCORE_GAP_DOMAIN (0.12) or ENDGAME_TILE_SCORE_DOMAIN.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm test -- --run endgameEntryEvalZones EndgameOverall 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    EG-entry band is ±0.60 and exactly matches the Task 1 Python value;
    SCORE_GAP_DOMAIN is 0.22; tsc + lint + affected vitest suites green.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>
    Item C ("unify 3.1.6 timeline overlay into one neutral band [46,56]%") and the new
    PER_CLASS_SCORE_BULLET_ZONES (item 3.4.1) cannot be implemented as literally written
    without a product decision. Two blockers, presented together.
  </decision>
  <context>
    BLOCKER C — no split eg/non-eg cohort overlay exists in the frontend. The 3.1.6
    timeline component is `frontend/src/components/charts/EndgameScoreOverTimeChart.tsx`;
    it draws a sign-aware Area band BETWEEN the user's own endgame and non-endgame
    rolling-score lines (gap-of-two-series), NOT a fixed cohort percentile reference
    band. There is no `[46,56]` (or any) ReferenceArea / cohort overlay to "unify".
    Furthermore reports/benchmarks-latest.md line 113 and the §3.1.4 row (line 361)
    explicitly say "do NOT retune SCORE_BULLET_NEUTRAL_*; static [0.45,0.55] acceptable
    until then" and the per-ELO deferral is "moot, keep (0.45,0.55)". The diff
    worklist's item C therefore appears to describe an overlay that the codebase never
    shipped. Inventing a [46,56] cohort band on this timeline would be a new feature,
    not the "unify" the report describes — out of scope for a no-research quick task.

    BLOCKER PER_CLASS_SCORE_BULLET_ZONES — the per-class numeric values DO exist
    (reports/benchmarks-latest.md line 280: rook(−0.060,+0.071) minor_piece(−0.067,
    +0.078) pawn(−0.077,+0.089) queen(−0.090,+0.125) mixed(−0.038,+0.061)
    pawnless(−0.197,+0.045), as deltas around the 0.5 null). But: (1) the score
    bullet is currently driven by the SHARED `SCORE_BULLET_NEUTRAL_*` /
    scoreBulletConfig.ts constant also used by the Openings score bullet (SKILL.md
    line 393) — a per-class registry is a non-trivial new Python-registry +
    codegen + EndgameTypeCard wiring feature; AND (2) the report itself offers an
    explicit cheaper alternative: "Or document the global band as deliberate
    editorial tightening per feedback_zone_band_judgement.md." This is a genuine
    product/scope decision, not a planner call (planner_authority_limits: missing
    information / scope decision, not difficulty).
  </context>
  <options>
    <option id="defer-both">
      <name>Defer C + PER_CLASS_SCORE_BULLET_ZONES to a scoped phase; ship Tasks 1,2,4,5 now</name>
      <pros>Quick task stays atomic and low-risk; the four band shifts (A/D/E/F) + domain (B) + bookkeeping land immediately; the two ambiguous/feature-sized items get proper scoping (research + codegen + FE wiring + tests).</pros>
      <cons>Two worklist items remain open (tracked as a new STATE deferral row by Task 4).</cons>
    </option>
    <option id="document-editorial">
      <name>Treat the global [0.45,0.55] score-bullet band as deliberate editorial tightening (report's stated alternative) + drop C</name>
      <pros>Zero new code; satisfies the report's own alternative; consistent with feedback_zone_band_judgement.md.</pros>
      <cons>Per-class score bullets stay 2–4× too tight vs cohort IQR for queen/pawnless; only a doc note results.</cons>
    </option>
    <option id="implement-perclass">
      <name>Implement PER_CLASS_SCORE_BULLET_ZONES now as a new registry+codegen+FE feature</name>
      <pros>Closes the 3.4.1 score-bullet recommendation fully with the line-280 values.</pros>
      <cons>Exceeds a no-research quick task: new Python registry block, codegen extension, EndgameTypeCard rewiring off the shared scoreBulletConfig, new tests; SKILL.md explicitly warns the bullet config is shared with Openings — needs its own phase.</cons>
    </option>
  </options>
  <resume-signal>Select: defer-both, document-editorial, or implement-perclass (and confirm whether C is dropped or you can point to a real split-overlay file path I missed)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Decision-reversal confirmation (G) + close-as-moot bookkeeping (item 4)</name>
  <files>.planning/STATE.md</files>
  <action>
    (G) Confirm-only: verify frontend/src/lib/endgameMetrics.ts line 95 still has
    `SHOW_WDL_BAR_IN_TYPE_CARDS: boolean = true` and EndgameTypeCard.tsx line 311
    still gates the MiniWDLBar on it. No code change (it is true; the 2026-05-17
    "drop WDL bar" rec is reversed by diff item G). Record the confirmation in the
    task SUMMARY, not in code.
    (Item 4) In .planning/STATE.md "### Deferred Items" section (under the
    2026-05-19 v1.17 close table, lines ~46-53), add rows marking these MOOT/CLOSED,
    referencing reports/benchmarks-diff-2026-05-17-vs-2026-05-19.md §2:
      - 3.1.4 Endgame Score per-ELO `ENDGAME_SCORE_ZONES` registry deferral → MOOT
        (snapshot-ELO ramp was a rating-lag artifact; single global band stands).
      - 3.1.5 Achievable Score Gap per-ELO stratification deferral → MOOT (same).
      - §3.3.3 5-quintile blocking decision → RESOLVED to Q0-only (Q1–Q4 dropped to
        review under game-time bucketing; only Q0 remains a per-(TC×ELO×quintile)
        promotion candidate).
    Use a terse table row each, no em-dashes, matching the existing Deferred-Items
    table format. This is doc bookkeeping only — no requirements/backlog file other
    than STATE.md tracks these (verified during planning: the deferrals live as
    inline source comments in endgame_zones.py + the benchmarks SKILL.md, not as
    discrete backlog rows).
    If Task 3 resolved to `defer-both`, also add one Deferred-Items row: "C +
    PER_CLASS_SCORE_BULLET_ZONES (benchmarks-diff §2) — deferred to a scoped phase
    (no split-overlay exists for C; per-class bullet is a shared-config feature)."
  </action>
  <verify>
    <automated>grep -q "SHOW_WDL_BAR_IN_TYPE_CARDS: boolean = true" frontend/src/lib/endgameMetrics.ts &amp;&amp; grep -c "MOOT\|RESOLVED\|moot" .planning/STATE.md | grep -qv '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <done>
    WDL-bar flag confirmed true (no code change); STATE.md Deferred Items has
    3.1.4 / 3.1.5 / §3.3.3 moot-close rows (plus the Task-3 deferral row if
    defer-both was chosen).
  </done>
</task>

<task type="auto">
  <name>Task 5: Full gate sweep + commit</name>
  <files>app/services/endgame_zones.py, frontend/src/generated/endgameZones.ts, frontend/src/lib/endgameEntryEvalZones.ts, frontend/src/components/charts/EndgameOverallShared.ts, .planning/STATE.md</files>
  <action>
    Run the full required gate set, then commit. Backend:
    `uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/
    && uv run pytest -q`. Frontend (cd frontend):
    `npm run lint && npm run knip && npm test -- --run && npm run build`.
    Also re-run `uv run python scripts/gen_endgame_zones_ts.py --check` to prove the
    CI drift gate is clean. If ruff format reports the project-wide pre-existing
    drift (documented in STATE.md deferred items), only the files this task touched
    must be clean — confirm with `uv run ruff format --check app/services/endgame_zones.py`.
    Commit on the current branch (NOT main/production per GitLab Flow) with message
    "chore(zones): apply benchmarks-diff 2026-05-17→2026-05-19 net actionable worklist
    (A/D/E/F bands, B domain, moot bookkeeping)". Do NOT push or open a PR unless the
    user asks. Append a one-line CHANGELOG.md [Unreleased] bullet under ### Changed
    only if the user wants it (zone recalibration is borderline user-facing —
    ask in the SUMMARY rather than assume).
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ty check app/ tests/ &amp;&amp; uv run python scripts/gen_endgame_zones_ts.py --check &amp;&amp; cd frontend &amp;&amp; npm run lint &amp;&amp; npm run knip &amp;&amp; npm test -- --run 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    All backend + frontend gates green; codegen drift gate clean; single atomic
    commit on the working branch; no push/PR unless requested.
  </done>
</task>

</tasks>

<verification>
- `uv run python scripts/gen_endgame_zones_ts.py --check` exits 0 (CI drift gate).
- Generated TS: minor_piece recovery [0.28,0.38]; rook achievable_score_gap
  [-0.05,0.05]; queen achievable_score_gap [-0.04,0.05].
- Hand-written endgameEntryEvalZones.ts band == Python entry_eval_pawns band (±0.60).
- SCORE_GAP_DOMAIN == 0.22.
- SHOW_WDL_BAR_IN_TYPE_CARDS still true (G reversal honored).
- STATE.md Deferred Items records 3.1.4 / 3.1.5 / §3.3.3 as moot/resolved.
- Backend ruff/ty/pytest + frontend lint/knip/test/build all green.
</verification>

<success_criteria>
Worklist items A, D, E, F (Task 1), B and A-frontend (Task 2), G + item-4
bookkeeping (Task 4) implemented and gate-green. Items C and
PER_CLASS_SCORE_BULLET_ZONES routed through the blocking decision checkpoint
(Task 3) because C has no implementable target in the codebase and the per-class
bullet is a shared-config feature with a report-stated cheaper alternative —
both require a product/scope decision, not a planner call.
</success_criteria>

<output>
Create `.planning/quick/260519-vf2-implement-the-changes-from-the-net-actio/260519-vf2-SUMMARY.md` when done.
</output>
