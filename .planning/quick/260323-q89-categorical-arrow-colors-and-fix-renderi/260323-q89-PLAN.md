---
phase: quick-260323-q89
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/arrowColor.ts
  - frontend/src/lib/arrowColor.test.ts
  - frontend/src/components/board/ChessBoard.tsx
  - frontend/src/pages/Openings.tsx
autonomous: true
requirements: [categorical-arrow-colors, fix-arrow-render-order]
must_haves:
  truths:
    - "Arrows use categorical colors: grey (45-55%), light green (55-60%), dark green (60%+), light red (40-45%), dark red (<40%)"
    - "Moves with fewer than 10 games always show grey arrows"
    - "Colored arrows (green/red) render on top of grey arrows"
    - "Popover text describes the new categorical color scheme"
  artifacts:
    - path: "frontend/src/lib/arrowColor.ts"
      provides: "Categorical arrow color logic"
      exports: ["getArrowColor", "arrowSortKey"]
    - path: "frontend/src/lib/arrowColor.test.ts"
      provides: "Tests for categorical color behavior"
  key_links:
    - from: "frontend/src/pages/Openings.tsx"
      to: "frontend/src/lib/arrowColor.ts"
      via: "getArrowColor import"
      pattern: "getArrowColor"
    - from: "frontend/src/components/board/ChessBoard.tsx"
      to: "frontend/src/lib/arrowColor.ts"
      via: "arrowSortKey import"
      pattern: "arrowSortKey"
---

<objective>
Replace gradient oklch arrow colors with a simple categorical color scheme and fix arrow rendering order so colored arrows appear on top of grey ones.

Purpose: Gradient colors were hard to distinguish. Categorical colors give clear visual signal. Arrow render order was inverted (grey on top hiding colored arrows).
Output: Simplified arrowColor.ts with categorical hex colors, fixed sort order, updated tests and popover text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/lib/arrowColor.ts
@frontend/src/lib/arrowColor.test.ts
@frontend/src/components/board/ChessBoard.tsx
@frontend/src/pages/Openings.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace gradient with categorical colors and fix tests</name>
  <files>frontend/src/lib/arrowColor.ts, frontend/src/lib/arrowColor.test.ts</files>
  <behavior>
    - getArrowColor(50, 30, 20, false) returns grey (win rate 45-55% neutral zone)
    - getArrowColor(57, 20, 20, false) returns light green (win rate 55-60%)
    - getArrowColor(65, 20, 20, false) returns dark green (win rate 60%+)
    - getArrowColor(20, 57, 20, false) returns light red (loss rate 55-60%, i.e. win rate 40-45%)
    - getArrowColor(20, 65, 20, false) returns dark red (loss rate 60%+, i.e. win rate below 40%)
    - getArrowColor(80, 10, 9, false) returns grey (fewer than 10 games)
    - Hover variants return lighter versions of each categorical color
    - arrowSortKey returns 2 for grey (render first/bottom), 0 for any green, 1 for any red (render last/top)
    - When both winPct and lossPct exceed 55%, the higher one wins
  </behavior>
  <action>
    1. First, rewrite arrowColor.test.ts to test categorical behavior:
       - Remove all oklch parsing helpers and gradient interpolation tests
       - Test each category returns a specific hex color string (exact match, not range)
       - Define 5 normal colors + 5 hover colors (10 named constants in arrowColor.ts)
       - Test boundary conditions: exactly 55% = grey, 55.1% = light green, etc.
       - Test arrowSortKey with the new hex color strings
    2. Run tests — they MUST fail (RED phase).
    3. Rewrite arrowColor.ts:
       - Remove ALL oklch functions: `clamp`, `lerp`, `gradientT`, `oklch` formatter
       - Remove all oklch constants (GREY_L, GREEN_L, etc.)
       - Keep `MIN_GAMES_FOR_COLOR = 10`
       - Define 10 named color constants (hex strings):
         - `GREY` / `GREY_HOVER`
         - `LIGHT_GREEN` / `LIGHT_GREEN_HOVER`
         - `DARK_GREEN` / `DARK_GREEN_HOVER`
         - `LIGHT_RED` / `LIGHT_RED_HOVER`
         - `DARK_RED` / `DARK_RED_HOVER`
       - Choose good hex values: grey around #B0B0B0, light green #6BBF59, dark green #2E8B2E, light red #E07070, dark red #C03030 (adjust for good board visibility — these are starting points)
       - Hover variants: lighter versions of each (e.g. +30-40 lightness)
       - `getArrowColor`: simple if/else chain:
         - gameCount < 10 → grey
         - winPct >= 60 → dark green
         - winPct >= 55 → light green (but only if winPct >= lossPct when lossPct also > 55)
         - lossPct >= 60 → dark red
         - lossPct >= 55 → light red (but only if lossPct > winPct when both > 55)
         - else → grey
       - `arrowSortKey`: simple string equality checks against the 10 known color constants. Grey → 2, green variants → 0, red variants → 1. Unknown → 2.
       - Export the color constants so tests can import them for exact matching
    4. Run tests — they MUST pass (GREEN phase).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/arrowColor.test.ts</automated>
  </verify>
  <done>All arrowColor tests pass with categorical color logic. No oklch code remains. arrowSortKey classifies all 10 color constants correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix arrow render order and update popover text</name>
  <files>frontend/src/components/board/ChessBoard.tsx, frontend/src/pages/Openings.tsx</files>
  <action>
    1. In ChessBoard.tsx line 101-105, fix the arrow sort order. Currently sorts ascending by arrowSortKey which puts green (0) first (bottom of SVG = behind). Reverse to descending so grey (2) is drawn first (bottom) and colored arrows (0, 1) are drawn last (on top):
       Change: `arrowSortKey(a.color) - arrowSortKey(b.color)` to `arrowSortKey(b.color) - arrowSortKey(a.color)`
       Update the comment on line 92-94 to accurately describe the new order: "grey drawn first (bottom), colored arrows drawn last (on top)".
    2. In Openings.tsx, update BOTH popover text instances (desktop around line 261 and mobile around line 569). Replace the gradient description with the categorical scheme description:
       "The arrows on the board show the next moves from your games that match the current filter settings. Thicker arrows mean the move occurred more frequently. Arrow colors indicate your win rate: dark green (60%+), light green (55-60%), grey (45-55%), light red (loss rate 55-60%), dark red (loss rate 60%+). Moves with fewer than 10 games are always grey."
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Arrow sort renders colored arrows on top of grey. Both desktop and mobile popover texts describe the categorical color scheme.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run src/lib/arrowColor.test.ts` passes
- `cd frontend && npm run build` succeeds with no errors
- No oklch references remain in arrowColor.ts
- Both Openings.tsx popover texts updated (grep for "dark green" in Openings.tsx finds 2 matches)
</verification>

<success_criteria>
- Categorical colors: 5 distinct color categories based on win/loss rate thresholds
- No gradient interpolation code remains
- Colored arrows render on top of grey arrows
- Both desktop and mobile popover texts describe the new scheme
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260323-q89-categorical-arrow-colors-and-fix-renderi/260323-q89-SUMMARY.md`
</output>
