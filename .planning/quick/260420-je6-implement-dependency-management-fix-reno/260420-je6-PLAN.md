---
quick_id: 260420-je6
slug: implement-dependency-management-fix-reno
description: implement dependency management fix — Renovate + CI audits + digest-pin base images
status: planned
created: 2026-04-20
---

# Quick Task 260420-je6: Dependency management & supply-chain fix

Source plan: `~/.claude/plans/a-recent-code-audit-generic-spark.md`

Addresses audit §4.14 (C+) by adding:
1. Renovate (version bumps) — replaces the missing dependabot.yml
2. CI vulnerability scanning (pip-audit, npm audit, Trivy)
3. Digest-pinned Docker base images

## Tasks

### T1 — Create `renovate.json` at repo root
- files: `renovate.json` (new)
- action: write config per plan (config:recommended + lockFileMaintenance + grouped minor/patch, majors separate; dependencyDashboard enabled)
- verify: `jq . renovate.json` parses cleanly
- done: file committed, schema validates

### T2 — Add CI vulnerability scans to `.github/workflows/ci.yml`
- files: `.github/workflows/ci.yml`
- action: insert `pip-audit` step after `uv sync --locked`; `npm audit` after `npm ci`; Trivy build+scan at end of test job
- verify: yaml is syntactically valid; steps logically ordered
- done: three new steps present in `test` job

### T3 — Digest-pin Dockerfile base images
- files: `Dockerfile`
- action: replace `python:3.13-slim` → `python:3.13-slim@sha256:d168b8d9eb761f4d3fe305ebd04aeb7e7f2de0297cec5fb2f8f6403244621664` (both builder + runtime stages); replace `ghcr.io/astral-sh/uv:0.10.9` → `ghcr.io/astral-sh/uv:0.10.9@sha256:10902f58a1606787602f303954cea099626a4adb02acbac4c69920fe9d278f82`
- verify: `docker build -t flawchess:ci-test .` succeeds
- done: all 3 FROM/COPY lines pinned to digest

## Out of scope (manual user steps — flagged separately)

- Installing Mend Renovate GitHub App (https://github.com/apps/renovate)
- Enabling Dependabot security alerts in repo Settings → Code security

## Verification

1. `jq . renovate.json` exits 0
2. GitHub Actions workflow lint: `gh workflow view CI` (post-push) or local `yamllint .github/workflows/ci.yml`
3. `docker build .` succeeds with pinned digests

## Notes

- Digests resolved 2026-04-20 via `docker buildx imagetools inspect`
- No commit in this quick task (working on main — per CLAUDE.md, commits require explicit user OK)
