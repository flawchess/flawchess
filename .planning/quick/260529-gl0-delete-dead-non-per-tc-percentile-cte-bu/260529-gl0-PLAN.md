---
phase: quick-260529-gl0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/canonical_slice_sql.py
  - tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py
  - tests/services/test_canonical_slice_sql.py
autonomous: true
requirements: [DEADCODE-GL0]
must_haves:
  truths:
    - "The 5 dead non-per-TC percentile symbols no longer exist in app/services/canonical_slice_sql.py."
    - "grep across app/ scripts/ tests/ for each deleted symbol returns zero hits."
    - "The live prod hot path (compute_stage_a/compute_stage_b) and the offline CDF script still import and dispatch only to the _tc builders."
    - "Source-mode parity for every LIVE _tc builder is still asserted by the test suite."
    - "uv run pytest passes (modulo the one known env-dependent prod-tunnel failure)."
    - "The 32 SQL golden fixtures under tests/scripts/fixtures/global_percentile_cdf/ are byte-unchanged."
  artifacts:
    - path: "app/services/canonical_slice_sql.py"
      provides: "Only the live _tc builders + median anchor + shared helpers remain; 5 dead symbols removed."
      contains: "per_user_cte_score_gap_tc"
    - path: "tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py"
      provides: "Parity assertions repointed to LIVE _tc builders (no per_user_cte_for import)."
    - path: "tests/services/test_canonical_slice_sql.py"
      provides: "Structural tests repointed to LIVE _tc builders; dead-only tests removed (no per_user_cte_for / per_user_cte_score_gap_bucket import)."
  key_links:
    - from: "app/services/user_benchmark_percentiles_service.py::_per_user_cte_for_family_and_tc"
      to: "app/services/canonical_slice_sql per_user_cte_*_tc builders"
      via: "import + dispatch"
      pattern: "per_user_cte_score_gap_tc|per_user_cte_achievable_tc|per_user_cte_score_gap_bucket_tc"
    - from: "scripts/gen_global_percentile_cdf.py::_per_user_cte_for_metric_and_tc"
      to: "app/services/canonical_slice_sql per_user_cte_*_tc builders"
      via: "import + dispatch"
      pattern: "per_user_cte_score_gap_tc|per_user_cte_clock_gap|per_user_cte_net_flag_rate"
---

<objective>
Pure dead-code removal. The percentile metric system was redesigned (Phase 94.3+) to compute per-time-control ("_tc") lookups whose percentiles are aggregated via weighted average against a cohort CDF. The original pooled (all-TC, non-"_tc") CTE builders and their dispatcher `per_user_cte_for` have NO production caller — only tests reference them. Delete the 5 dead symbols and repoint/trim the tests so there is zero loss of meaningful coverage of the LIVE `_tc` builders.

Purpose: Remove confusing dead code that shadows the live per-TC builders (the dispatcher is also stale — it lacks the `recovery_score_gap` arm the live dispatcher has). Reduces the file's surface and prevents a future maintainer from "fixing" or extending a builder no longer on any code path.

Output: `app/services/canonical_slice_sql.py` with 5 symbols removed; two test files repointed to the live `_tc` builders. No behavior change, no CHANGELOG entry (pure refactor per CLAUDE.md), no frontend touch.

DEADNESS RE-CONFIRMED (executor must re-verify in Task 1, do not assume):
- Live prod hot path: `app/services/user_benchmark_percentiles_service.py::_per_user_cte_for_family_and_tc` dispatches ONLY to `_tc` builders (`per_user_cte_score_gap_tc`, `per_user_cte_achievable_tc`, `per_user_cte_score_gap_bucket_tc`, `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`).
- Offline CDF script: `scripts/gen_global_percentile_cdf.py::_per_user_cte_for_metric_and_tc` dispatches ONLY to the same `_tc` builders.
- `per_user_cte_for(` appears NOWHERE in `app/` or `scripts/` — only in `tests/`.

VERIFIED DEAD SET (delete exactly these 5 from app/services/canonical_slice_sql.py):
1. `_recent_capped_cte` (~L260) — non-per-TC recency helper, used ONLY by the three dead builders below (~L343/L398/L490). Orphaned once they are gone. (Do NOT confuse with the LIVE `_recent_capped_per_tc_cte` at ~L285.)
2. `per_user_cte_score_gap` (~L320) — non-_tc pooled.
3. `per_user_cte_achievable` (~L381) — non-_tc pooled.
4. `per_user_cte_score_gap_bucket` (~L454, body ends ~L573) — non-_tc bucket.
5. `per_user_cte_for` (~L1363, body ends ~L1431) — the dispatcher; only callers are tests; also stale (missing the live dispatcher's `recovery_score_gap` arm).

MUST NOT TOUCH (LIVE — reached via the two live dispatchers above):
- `_recent_capped_per_tc_cte` (~L285)
- `_endgame_entry_clocks_cte` (~L576)
- `per_user_cte_score_gap_tc`, `per_user_cte_achievable_tc`, `per_user_cte_score_gap_bucket_tc`
- `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`
- `per_user_cte_median_anchor` and all shared helpers (`_recency_window_sql`, `equal_footing_filter_sql`, `selected_users_cte`, `sparse_exclusion_sql`, `_user_elo_at_game_expr`, constants).
Note: deleting `per_user_cte_for` does NOT remove the 12 time-pressure/clock/net-flag `_tc` dispatch *targets* it references — those builders stay live via the two service/script dispatchers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@app/services/canonical_slice_sql.py
@app/services/user_benchmark_percentiles_service.py
@scripts/gen_global_percentile_cdf.py
@tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py
@tests/services/test_canonical_slice_sql.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm deadness, then delete the 5 dead symbols from canonical_slice_sql.py</name>
  <files>app/services/canonical_slice_sql.py</files>
  <action>
FIRST re-confirm deadness (do not trust the plan blindly):
- Run `grep -rn "per_user_cte_for\b" app/ scripts/` and confirm ZERO hits (the dispatcher must have no prod/script caller).
- Run `grep -rn "per_user_cte_score_gap\b\|per_user_cte_achievable\b\|per_user_cte_score_gap_bucket\b" app/ scripts/ | grep -v "_tc"` and confirm ZERO hits for the non-_tc names.
- Open `app/services/user_benchmark_percentiles_service.py::_per_user_cte_for_family_and_tc` and `scripts/gen_global_percentile_cdf.py::_per_user_cte_for_metric_and_tc` and confirm both dispatch ONLY to `_tc` builders. If ANY of these checks finds a non-test prod/script caller, STOP and report — the symbol is not dead.

THEN delete exactly these 5 definitions (whole function bodies, including their docstrings and the trailing blank lines that separate them from neighbors), leaving the LIVE neighbors intact:
1. `_recent_capped_cte` (the non-per-TC helper at ~L260). Keep `_recent_capped_per_tc_cte` (~L285) — it is the LIVE per-TC sibling.
2. `per_user_cte_score_gap` (~L320).
3. `per_user_cte_achievable` (~L381).
4. `per_user_cte_score_gap_bucket` (~L454, body runs through the `per_user_values` block ending ~L573). Stop the deletion BEFORE `_endgame_entry_clocks_cte` (~L576) — that helper is LIVE.
5. `per_user_cte_for` (~L1363 through ~L1431, the whole dispatcher).

Preserve the module docstring as-is (it describes the pooled methodology; it remains accurate context for the surviving builders). Do not rewrite surrounding comments or reflow unrelated code — scope discipline per CLAUDE.md. After deletion, `uv run ty` and `uv run pytest` will both be RED until Task 2/3 land — that is expected; do NOT attempt to make tests pass in this task.
  </action>
  <verify>
    <automated>grep -rn "def per_user_cte_for\b\|def per_user_cte_score_gap\b\|def per_user_cte_achievable\b\|def per_user_cte_score_gap_bucket\b\|def _recent_capped_cte\b" app/services/canonical_slice_sql.py | grep -v "_tc\|_per_tc" | wc -l | grep -qx 0 && grep -q "def _recent_capped_per_tc_cte" app/services/canonical_slice_sql.py && grep -q "def per_user_cte_score_gap_tc" app/services/canonical_slice_sql.py && grep -q "def _endgame_entry_clocks_cte" app/services/canonical_slice_sql.py && echo OK</automated>
  </verify>
  <done>The 5 dead `def`s are gone; all LIVE `_tc` builders, `_recent_capped_per_tc_cte`, `_endgame_entry_clocks_cte`, and `per_user_cte_median_anchor` remain. `uv run ty check app/` reports no NEW undefined-name errors inside `canonical_slice_sql.py` itself (test files are still RED — that is fine until Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Repoint parity tests to the LIVE _tc builders</name>
  <files>tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py</files>
  <action>
This file's purpose is the source-mode parity guarantee (benchmark vs single_user emit byte-identical pooled bodies, D-10). That property is held by the LIVE `_tc` builders, NOT only by the deleted `per_user_cte_for`. Repoint so the guarantee is asserted against LIVE builders; do NOT delete the file.

Concrete edits:
- Remove `per_user_cte_for` from the import on ~L42 (keep `RECENT_GAMES_PER_TC_CAP`). Add imports for the LIVE builders actually used after repointing: `per_user_cte_score_gap_tc`, `per_user_cte_achievable_tc`, `per_user_cte_score_gap_bucket_tc`, `per_user_cte_time_pressure_score_gap`, `per_user_cte_clock_gap`, `per_user_cte_net_flag_rate`.
- The four `_METRICS`-keyed tests (`test_pooled_cte_body_is_identical_across_sources`, `test_no_per_tc_predicate_on_either_source`, `test_pooled_cte_contains_recent_capped_prelude`, `test_pooled_cte_emits_per_user_values_with_metric_value_and_n_games`, ~L72-134) assert parity / recent_capped prelude / per_user_values shape for `score_gap`/`achievable_score_gap`/`score_gap_conv`/`score_gap_parity`. Their LIVE analogs already exist in this file at L214-268 (`test_score_gap_tc_pooled_body_byte_identical_across_sources`, `test_achievable_tc_pooled_body_byte_identical_across_sources`, `test_score_gap_bucket_tc_pooled_body_byte_identical_across_sources`) — those are STRICTLY STRONGER (byte-identical, parametrised over all TCs and buckets). DELETE the four dead `_METRICS`-keyed tests AND the now-unused `_METRICS` tuple (~L45-50). Note the live byte-identical tests do not assert the `recent_capped`/`<= cap`/`INTERVAL '36 months'` prelude substrings or the explicit `per_user_values` `metric_value`/`n_games` projection — if you want to preserve those specific assertions, fold them into the existing live byte-identical tests rather than dropping them; a byte-identical-across-sources check does not by itself prove the prelude is present. Prefer adding the two substring assertions into `test_score_gap_tc_pooled_body_byte_identical_across_sources` (assert against `bm`) over keeping the dead-builder tests.
- The `_PER_TC_METRIC_IDS`-keyed tests (`test_pooled_per_tc_cte_body_is_identical_across_sources` ~L141, `test_pooled_per_tc_cte_contains_per_tc_predicate` ~L164, `test_pooled_per_tc_cte_emits_per_user_values_with_metric_value_and_n_games` ~L177, and `test_existing_94_3_per_tc_pooled_body_byte_identical_after_pitfall_1` ~L278) route to LIVE builders THROUGH the dead dispatcher (`per_user_cte_for("time_pressure_score_gap_bullet", ...)`). REPOINT these to call the LIVE builder directly: parse the family + tc out of the metric_id (or restructure to parametrize over (family_builder, tc)) and call `per_user_cte_time_pressure_score_gap(tc, source=...)` / `per_user_cte_clock_gap(tc, source=...)` / `per_user_cte_net_flag_rate(tc, source=...)`. Preserve each test's assertion intent (byte-identical across sources; per-TC predicate present; per_user_values shape; user_id projection after Pitfall 1). Drop the `# ty: ignore[invalid-argument-type]  # Plan C widens CdfMetricId` comments — once you call the typed builders directly the ty-ignores are no longer needed (and would become unused-ignore errors).
- Keep `_PER_TC_METRIC_FAMILIES` / `_PER_TC_TCS` if still used after repointing; remove `_PER_TC_METRIC_IDS` if no longer referenced. Keep the Plan-03 direct-call tests (L214-268) and the `_PLAN_03_*` tuples as-is — they already call live builders directly.
- Update the module docstring's references to `per_user_cte_for` to name the live `_tc` builders instead (one-line factual fix; do not over-rewrite).
  </action>
  <verify>
    <automated>grep -c "per_user_cte_for" tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py | grep -qx 0 && uv run pytest tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>No reference to `per_user_cte_for` remains in the parity file; the source-mode parity guarantee is asserted directly against every LIVE `_tc` builder family; `uv run pytest tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint/trim structural tests, grep-clean, and run the full gate</name>
  <files>tests/services/test_canonical_slice_sql.py</files>
  <action>
Trim and repoint the structural test file so no reference to the 5 deleted symbols remains, with zero loss of LIVE-builder coverage. Judgment call throughout: DELETE tests that exercised ONLY a dead builder (a LIVE `_tc` analog already covers the property); REPOINT tests asserting a general property that a LIVE builder also satisfies.

Import line (~L29-46): remove `per_user_cte_for` from the import. Keep `_recent_capped_per_tc_cte` and everything else.

DELETE (dead-only coverage — LIVE `_tc` analogs already exist in this file):
- `TestPooledShape` (~L116-189): all four methods key on `_METRICS` via `per_user_cte_for`. The properties (per_user_values shape, no elo_bucket/tc_bucket leak, no per-TC join predicate, no sparse-cell literal) are covered for the LIVE builders by `TestPerUserCteScoreGapTc` / `TestPerUserCteAchievableTc` / `TestPerUserCteSection2Tc` (~L831-1024) and the per-TC predicate / source-parity tests. Before deleting, confirm the live classes still assert the "no per-TC join predicate `g.time_control_bucket::text = su.tc_bucket`" and "no elo_bucket" properties; if a specific negative assertion has no live analog, REPOINT that one method to a representative live builder (e.g. `per_user_cte_score_gap_tc("bullet", source=source)`) rather than dropping it. Do not silently lose a negative-regression guard.
- `TestRecencyWindow` (~L197-250): keyed on `_METRICS` via `per_user_cte_for`. `TestRecentCappedPerTcCte` (~L439-477) already covers the per-TC recency window / cap / snapshot-date behavior on the LIVE `_recent_capped_per_tc_cte`. Note `test_cap_appears_once_per_builder_output` (~L243) asserts the cap appears EXACTLY once in a full builder render — that is a per-builder property, not a `_recent_capped_per_tc_cte` property. REPOINT that one to a representative live builder (`per_user_cte_score_gap_tc("single_user")`) instead of deleting it. Delete the rest of `TestRecencyWindow` only after confirming `TestRecentCappedPerTcCte` covers the cap/now/explicit-date cases.
- `TestInclusionFloor` (~L258-290): `test_score_gap_having_gates_both_30`, `test_achievable_having_gates_di_ge_30`, `test_score_gap_bucket_conv_having_gates_30`, `test_score_gap_bucket_parity_having_gates_30` are covered by the LIVE `_tc` HAVING tests (`test_having_floors_match_non_per_tc_analog` ~L871, `test_having_floor_matches_non_per_tc_analog` ~L928, and the bucket `_tc` class). DELETE these four. `test_apply_floor_argument_raises_typeerror` (~L282) asserts `per_user_cte_for(..., apply_floor=False)` raises TypeError — this is a dead-dispatcher-only contract; DELETE it (the live `_tc` builders never had an `apply_floor` arg, so there is no equivalent live regression to preserve). Remove the now-unused `_METRICS` tuple (~L102-107) if nothing else references it.
- `TestPerTcDispatcher` (~L545-589): every method calls `per_user_cte_for("<family>_<tc>", ...)` to assert the dispatcher routes to the right LIVE builder. The dispatcher is gone, so the "routes correctly" property is moot. The UNDERLYING builder properties (per-TC predicate + correct HAVING) are already asserted directly by `TestPerTcBuilders` (~L485-542). DELETE `TestPerTcDispatcher` entirely, INCLUDING `test_dispatcher_raises_on_unknown_metric` (the dispatcher no longer exists). Confirm `TestPerTcBuilders` covers all 3 families × 4 TCs for the predicate + HAVING before deleting.
- `TestPerUserCteSection2RecoveryWidening` (~L1027-1079): every method imports and calls the DEAD `per_user_cte_score_gap_bucket`. The `recovery` bucket_label support is covered for the LIVE builder by `TestPerUserCteSection2Tc::test_recovery_branch_returns_non_empty_sql` (~L998) and `test_emits_bucket_where_dispatch` (~L987). DELETE the whole class.
- `test_non_per_tc_builders_are_not_touched_by_pitfall_1` (~L780-794): asserts the DEAD non-per-TC builders lack the Pitfall-1 user_id comment. The builders are gone, so the scope-guard is meaningless. DELETE this single method (keep the rest of `TestPitfall1UserIdWideningExistingBuilders`).

REPOINT (general property a LIVE builder also satisfies) — for any method above flagged "REPOINT", call a representative LIVE `_tc` builder directly (import it locally as the surrounding tests already do, e.g. `from app.services.canonical_slice_sql import per_user_cte_score_gap_tc`) and pass a representative TC. Drop any `# ty: ignore[invalid-argument-type]` comments that become unused after repointing.

AFTER edits, grep-clean across the whole repo and run the full gate (resolve all output):
- `grep -rn "per_user_cte_for\b\|per_user_cte_score_gap\b\|per_user_cte_achievable\b\|per_user_cte_score_gap_bucket\b\|_recent_capped_cte\b" app/ scripts/ tests/ | grep -v "_tc\|_per_tc"` must return ZERO hits (each of the 5 deleted names absent everywhere except inside `_tc`/`_per_tc` longer names which are LIVE). Also grep for any OTHER test file importing the 5 symbols and clean it up.
- `uv run ruff format app/ tests/`
- `uv run ruff check app/ tests/ --fix`
- `uv run ty check app/ tests/` (zero errors; watch for unused `# ty: ignore` after dropping ignores)
- `uv run pytest -x` — dev DB already running on localhost:5432; do NOT reset it. KNOWN UNRELATED FAILURE to ignore: `tests/scripts/test_backfill_user_percentiles.py::test_backfill_target_prod_refuses_when_tunnel_down` fails locally only because a prod SSH tunnel is open on 15432 (passes in CI). If that is the ONLY failure, the gate is green. If `-x` stops on it, re-run with `--deselect tests/scripts/test_backfill_user_percentiles.py::test_backfill_target_prod_refuses_when_tunnel_down` to confirm everything else passes.
- Fixture guard: `git diff --stat tests/scripts/fixtures/global_percentile_cdf/` must show ZERO changes (the 32 goldens are generated from the LIVE `_tc` builders only; this change touches none of them).

SKIP the CHANGELOG entry (pure refactor, no user-facing behavior change, per CLAUDE.md changelog rules). Do NOT touch frontend.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && { grep -rn "per_user_cte_for\b\|per_user_cte_score_gap\b\|per_user_cte_achievable\b\|per_user_cte_score_gap_bucket\b\|_recent_capped_cte\b" app/ scripts/ tests/ | grep -v "_tc\|_per_tc"; } | wc -l | grep -qx 0 && [ -z "$(git diff --stat tests/scripts/fixtures/global_percentile_cdf/)" ] && uv run ty check app/ tests/ && uv run pytest tests/services/test_canonical_slice_sql.py tests/services/test_canonical_slice_per_user_vs_benchmark_parity.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>Zero hits for all 5 deleted symbol names across app/ scripts/ tests/ (excluding LIVE `_tc`/`_per_tc` names); `git diff --stat tests/scripts/fixtures/global_percentile_cdf/` is empty; `uv run ruff format/check`, `uv run ty check app/ tests/`, and `uv run pytest -x` all clean (the only acceptable failure being the documented prod-tunnel test). No CHANGELOG change; no frontend change.</done>
</task>

</tasks>

<verification>
- The 5 dead symbols (`_recent_capped_cte`, `per_user_cte_score_gap`, `per_user_cte_achievable`, `per_user_cte_score_gap_bucket`, `per_user_cte_for`) are removed from `app/services/canonical_slice_sql.py`.
- `grep -rn` for each deleted name across `app/ scripts/ tests/` (excluding the LIVE `_tc`/`_per_tc` names) returns zero hits.
- The LIVE builders and the two live dispatchers are untouched and still import successfully (`uv run ty check app/` clean).
- Source-mode parity for every LIVE `_tc` builder family is still asserted (parity file + structural file).
- `git diff --stat tests/scripts/fixtures/global_percentile_cdf/` shows zero changes (32 goldens byte-unchanged).
- `uv run ruff format/check`, `uv run ty check app/ tests/`, `uv run pytest -x` all pass (excluding the one documented env-dependent prod-tunnel failure).
</verification>

<success_criteria>
- Pure dead-code removal complete: 5 symbols gone, no prod/script caller affected, no behavior change.
- Zero loss of LIVE `_tc`-builder test coverage; only dead-code coverage removed; general-property tests repointed to live builders.
- All gates green (modulo the documented prod-tunnel test). Fixtures unchanged. No CHANGELOG, no frontend touch.
</success_criteria>

<output>
Create `.planning/quick/260529-gl0-delete-dead-non-per-tc-percentile-cte-bu/260529-gl0-SUMMARY.md` when done.
</output>
