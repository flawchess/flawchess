---
phase: quick
plan: 260317-qe1
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Openings.tsx
  - frontend/src/components/position-bookmarks/PositionBookmarkList.tsx
autonomous: true
must_haves:
  truths:
    - "Suggest bookmarks button appears below the bookmark list, not above"
    - "Bookmark button spans full width of its container"
    - "Suggest bookmarks button spans full width of its container"
    - "Piece filter control is right-aligned within the flex row"
  artifacts:
    - path: "frontend/src/pages/Openings.tsx"
      provides: "Full-width Bookmark button, right-aligned Piece filter"
    - path: "frontend/src/components/position-bookmarks/PositionBookmarkList.tsx"
      provides: "Suggest bookmarks button moved below list"
  key_links: []
---

<objective>
Adjust sidebar layout in the Openings page: move "Suggest bookmarks" below the bookmark list, make both bookmark-related buttons full-width, and right-align the Piece filter toggle group.

Purpose: Improve sidebar UX — action buttons should be full-width for easy clicking, suggest bookmarks belongs after the list contextually, and piece filter should sit on the right side of the filter row.
Output: Updated Openings.tsx and PositionBookmarkList.tsx
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/Openings.tsx
@frontend/src/components/position-bookmarks/PositionBookmarkList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Suggest bookmarks below list and make full-width</name>
  <files>frontend/src/components/position-bookmarks/PositionBookmarkList.tsx</files>
  <action>
In PositionBookmarkList.tsx, move the "Suggest bookmarks" button from ABOVE the list (the current `div.flex.justify-end.mb-2` block at lines 56-66) to BELOW the list/empty-state. Specifically:

1. Remove the entire `<div className="flex justify-end mb-2">...</div>` block (lines 56-66) that wraps the Suggest bookmarks button.
2. Place the Suggest bookmarks button AFTER the DndContext/empty-state block (after the closing of the ternary at line 82), but before the SuggestionsModal.
3. Make the button full-width: add `className="w-full"` and keep `variant="outline"` but change `size` from `"sm"` to `"default"`. Add `mt-2` margin-top wrapper or directly on the button for spacing.
4. Keep all existing props: `onClick`, `data-testid="btn-suggest-bookmarks"`, icon, and label text.
  </action>
  <verify>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build</verify>
  <done>Suggest bookmarks button renders below the bookmark list (or empty-state text), spans full width</done>
</task>

<task type="auto">
  <name>Task 2: Full-width Bookmark button and right-align Piece filter</name>
  <files>frontend/src/pages/Openings.tsx</files>
  <action>
In Openings.tsx sidebar section:

1. **Full-width Bookmark button** (lines 354-364): Add `className="w-full"` to the Button element (currently has no className). Keep `variant="outline"` and `size="lg"`. This makes it span the full sidebar width.

2. **Right-align Piece filter**: In the flex row containing "Played as" and "Piece filter" toggle groups (lines 283-350), add `ml-auto` to the Piece filter wrapper div (currently `<div className={pieceFilterDisabled ? 'opacity-50' : ''}>` at line 321). Change to `<div className={\`ml-auto ${pieceFilterDisabled ? 'opacity-50' : ''}\`}>`. This pushes the Piece filter to the right end of the flex row using `justify-between`-like behavior via auto margin.
  </action>
  <verify>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build</verify>
  <done>Bookmark button spans full width; Piece filter control is right-aligned in the filter row</done>
</task>

</tasks>

<verification>
cd /home/aimfeld/Projects/Python/chessalytics/frontend && npm run build
</verification>

<success_criteria>
- Suggest bookmarks button appears below the bookmark list items (not above)
- Both Bookmark and Suggest bookmarks buttons span full container width
- Piece filter toggle group is right-aligned in its row
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260317-qe1-move-suggest-bookmarks-below-list-full-w/260317-qe1-SUMMARY.md`
</output>
