---
phase: quick-260603-q85
plan: "01"
subsystem: frontend/openings
tags: [label-rename, mobile-layout, ux, openings]
dependency_graph:
  requires: []
  provides: [opening-eval-label-disambiguation, mobile-label-above-chart]
  affects: [OpeningsPage, OpeningInsightsPage]
tech_stack:
  added: []
  patterns: [tailwind-responsive-grid, flex-col-mobile-grid-desktop]
key_files:
  created: []
  modified:
    - frontend/src/components/charts/PositionResultsPanel.tsx
    - frontend/src/components/stats/OpeningStatsCard.tsx
    - frontend/src/components/insights/OpeningFindingCard.tsx
decisions:
  - "Per-metric flex-col/sm:grid wrapper pattern chosen over outer-grid approach; each metric is independently responsive without coupling label widths across rows"
  - "Label first in DOM (renders on top on mobile), bullet second with sm:order-first to restore bullet-left desktop layout"
  - "EvalCpuPlaceholder col-span-2 class retained (harmless in flex context); no need to update the component"
metrics:
  duration: ~8 minutes
  completed: "2026-06-03"
  tasks_completed: 2
  tasks_total: 3
  files_changed: 3
---

# Phase quick-260603-q85 Plan 01: Openings Eval Label Disambiguation Summary

**One-liner:** Renamed opening-side "Eval:" to "End Eval:" at three sites and added mobile label-above-chart layout for Score and End Eval rows in both card components.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Rename "Eval:" to "End Eval:" at all three opening-side sites | 971cef10 | PositionResultsPanel.tsx, OpeningStatsCard.tsx, OpeningFindingCard.tsx |
| 2 | Mobile label-above-chart layout for Score + End Eval rows | 070b335d | OpeningStatsCard.tsx, OpeningFindingCard.tsx |
| 3 | Human-verify checkpoint | — | Pending human visual verification |

## Task 3: Pending Human Verification

Task 3 is a `checkpoint:human-verify` (blocking). The executor did not run a browser. The user must verify:

1. DESKTOP: Move explorer (PositionResultsPanel), opening cards (OpeningStatsCard), and insights finding cards (OpeningFindingCard) all show "End Eval:" inline, side-by-side with the bullet chart as before. "Score:" label unchanged.
2. MOBILE (below sm, ~640px): Opening cards and insights finding cards show "Score" and "End Eval" labels (with value + "?" icon) stacked ABOVE their respective bullet charts, not hidden.
3. Endgames page still shows "Entry Eval" (untouched). EvalConfidenceTooltip "?" body still reads "...end of your openings..." (untouched).
4. Text is readable at text-sm (no text-xs labels introduced).

Resume signal: "approved" or describe issues.

## What Was Built

**PART A - Label rename:**
- `PositionResultsPanel.tsx` line 207: `"Eval:"` → `"End Eval:"` (always-visible inline label in move explorer)
- `OpeningStatsCard.tsx` eval label: `"Eval:"` → `"End Eval:"` (opening cards)
- `OpeningFindingCard.tsx` eval label: `"Eval:"` → `"End Eval:"` (insights finding cards)
- "Score:" labels, endgame "Entry Eval", and EvalConfidenceTooltip copy are all untouched

**PART B - Mobile layout:**
- Both `OpeningStatsCard.tsx` and `OpeningFindingCard.tsx` `scoreEvalBlock` restructured
- Outer `grid grid-cols-[minmax(0,1fr)_auto] gap-x-2 gap-y-2 items-center` replaced by `flex flex-col gap-2`
- Each metric (Score, End Eval) wrapped in `flex flex-col gap-1 sm:grid sm:grid-cols-[minmax(0,1fr)_auto] sm:gap-x-2 sm:items-center`
- Label span placed FIRST in DOM with `sm:order-last`; bullet div placed second with `sm:order-first`
- Removed `hidden sm:inline` from Score and End Eval label spans (now always visible)
- All `data-testid`, `aria-label`, and `style` opacity attributes preserved unchanged

## Deviations from Plan

None — plan executed exactly as written.

## Verification Results

- `grep -c 'End Eval:' ...` across 3 files: 3 (one per file) - PASS
- No bare `"Eval:"` remains in the three opening-side files - PASS
- `hidden sm:inline` removed from Score and End Eval labels in both card files - PASS
- EvalConfidenceTooltip.tsx: `git diff` is empty (untouched) - PASS
- ESLint: 0 errors - PASS
- Vitest: 747/747 tests passed (63 test files) - PASS
- `npm run build`: succeeds (chunk size warning is pre-existing, unrelated) - PASS

## Self-Check: PASSED

- Commits exist: 971cef10 (Task 1), 070b335d (Task 2) — verified via `git log`
- Files modified: PositionResultsPanel.tsx, OpeningStatsCard.tsx, OpeningFindingCard.tsx
- No file deletions in either commit

## Known Stubs

None — all label and layout changes are complete. No stub patterns introduced.

## Threat Flags

None — frontend-only label and CSS layout change; no new endpoints, auth paths, or data flows.
