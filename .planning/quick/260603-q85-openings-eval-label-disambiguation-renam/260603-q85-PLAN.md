---
phase: quick-260603-q85
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/PositionResultsPanel.tsx
  - frontend/src/components/stats/OpeningStatsCard.tsx
  - frontend/src/components/insights/OpeningFindingCard.tsx
autonomous: true
requirements: [Q85-RENAME, Q85-MOBILE]

must_haves:
  truths:
    - "Every opening-side 'Eval:' label now reads 'End Eval:' (move explorer, opening cards, insights finding cards)"
    - "On mobile, the Score and End Eval labels (with value + info icon) render ABOVE their bullet charts in OpeningStatsCard and OpeningFindingCard instead of being hidden"
    - "On desktop (sm and up), the inline side-by-side layout is unchanged in all three components"
    - "The endgame-side 'Entry Eval' label and EvalConfidenceTooltip copy are untouched"
  artifacts:
    - path: "frontend/src/components/charts/PositionResultsPanel.tsx"
      provides: "Move explorer 'End Eval:' label (always visible)"
    - path: "frontend/src/components/stats/OpeningStatsCard.tsx"
      provides: "Opening card 'End Eval:' label + mobile label-above-chart layout"
    - path: "frontend/src/components/insights/OpeningFindingCard.tsx"
      provides: "Insights finding 'End Eval:' label + mobile label-above-chart layout"
  key_links: []
---

<objective>
Disambiguate the opening-side "Eval" metric so users no longer misread it as the eval of
the position currently shown on the board. The value is sampled at a fixed point (end of
the opening phase), not the displayed position — a trust/correctness bug.

Two parts:
- PART A: Rename the always-visible inline label "Eval:" → "End Eval:" at three
  opening-side sites, mirroring the existing endgame-side "{qualifier} Eval" pattern
  ("Entry Eval"). Leave "Score" labels and the endgame "Entry Eval" label untouched.
- PART B: On mobile only, in the two card components, the metric label is currently
  `hidden sm:inline` (suppressed entirely on mobile), leaving a bare "+0.4" value — the
  worst case for the bug. Render the label (with value + info "?" icon) stacked ABOVE the
  bullet chart on mobile, for BOTH the Score row and the End Eval row. Desktop keeps the
  current inline side-by-side layout.

Purpose: Eliminate the "+0.4 = displayed position" misreading.
Output: Three modified frontend components; no API/schema/data changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@frontend/src/components/charts/PositionResultsPanel.tsx
@frontend/src/components/stats/OpeningStatsCard.tsx
@frontend/src/components/insights/OpeningFindingCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename "Eval:" → "End Eval:" at all three opening-side sites (PART A)</name>
  <files>
    frontend/src/components/charts/PositionResultsPanel.tsx,
    frontend/src/components/stats/OpeningStatsCard.tsx,
    frontend/src/components/insights/OpeningFindingCard.tsx
  </files>
  <action>
In each of the three files, change the visible label text of the opening-side eval row
from "Eval:" to "End Eval:". The exact current spans are:
- PositionResultsPanel.tsx ~line 207: `<span className="text-muted-foreground">Eval:</span>` (always visible — keep className as-is, change text to "End Eval:").
- OpeningStatsCard.tsx ~line 231: `<span className="hidden sm:inline text-muted-foreground">Eval:</span>` (change text only; the `hidden sm:inline` is handled in Task 2 — leave the className alone in THIS task to keep the diff reviewable, Task 2 restructures it).
- OpeningFindingCard.tsx ~line 216: `<span className="hidden sm:inline text-muted-foreground">Eval:</span>` (same as above — text only here).

Do NOT touch the "Score:" labels. Do NOT touch the endgame-side "Entry Eval" label in
EndgameOverallEntryCard.tsx / MetricStatTooltip.tsx. Do NOT edit EvalConfidenceTooltip.tsx
(its "end of your openings" body copy stays as-is — it already explains the semantics).
The rename mirrors the established "{qualifier} Eval" pattern already used on the Endgames
side ("Entry Eval" = eval sampled at a fixed point, not the displayed position).
  </action>
  <verify>
    <automated>grep -rn 'End Eval:' frontend/src/components/charts/PositionResultsPanel.tsx frontend/src/components/stats/OpeningStatsCard.tsx frontend/src/components/insights/OpeningFindingCard.tsx | grep -c 'End Eval:' | grep -qx 3 && echo OK</automated>
  </verify>
  <done>All three opening-side eval-row labels read "End Eval:". No bare "Eval:" remains in these three files. "Entry Eval" elsewhere is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile label-above-chart layout for Score + End Eval rows (PART B)</name>
  <files>
    frontend/src/components/stats/OpeningStatsCard.tsx,
    frontend/src/components/insights/OpeningFindingCard.tsx
  </files>
  <action>
Both card components lay out the Score and End Eval rows in a 2-column grid:
`grid grid-cols-[minmax(0,1fr)_auto] gap-x-2 gap-y-2 items-center` (col 1 = MiniBulletChart,
col 2 = the text span containing label + value + popover). On mobile today the label span
uses `hidden sm:inline`, so the label disappears and only the bare value remains.

Goal: on mobile, render the label (WITH its value and the info "?" popover trigger) stacked
ABOVE the corresponding bullet chart; on desktop (sm+) keep the EXACT current inline
side-by-side grid layout. Apply to BOTH the Score row and the End Eval row in BOTH files
(mobile-parity rule). Do not introduce per-call props for this — change the shared row
markup in place (the Score row and Eval row are duplicated sibling markup within each file's
`scoreEvalBlock`, so update both rows in each file).

Implementation approach (pick the cleanest seam; both rows in a file should use the same
technique):
- The simplest correct approach: stop hiding the label on mobile and let the text span wrap
  above the chart on small screens, while staying inline at sm+. Concretely, make the
  per-metric cell a flex container that is `flex-col` on mobile and reverts to the inline
  grid behavior at sm+. Because the grid columns are `[bullet] [text]`, the mobile stacked
  form must place the label-row ABOVE the bullet. Achieve this by, on mobile only, ordering
  the text span before the bullet within a wrapping flex column per metric; at sm+, restore
  the existing `grid grid-cols-[minmax(0,1fr)_auto]` two-column row.
- Practically: wrap each metric (Score, End Eval) in a per-metric container that is
  `flex flex-col gap-1 sm:grid sm:grid-cols-[minmax(0,1fr)_auto] sm:gap-x-2 sm:items-center`
  (i.e. stacked column on mobile, two-col grid on sm+), with the label/value/popover span
  rendered first (so it sits on top on mobile) and the MiniBulletChart cell second. At sm+,
  use `sm:order-*` (or DOM order + grid placement) so the bullet sits in col 1 and the text
  in col 2 exactly as today. If reordering via flex `order` is cleaner than reordering DOM,
  use `order-first`/`order-last` gated on the `sm:` breakpoint to reproduce today's
  bullet-left / text-right desktop layout. The current outer `scoreEvalBlock` grid that
  pairs Score and Eval rows must continue to vertically align the two metrics' bullets on
  desktop — preserve that desktop alignment; only the mobile rendering stacks.

On the label span itself, change `hidden sm:inline` → always visible (e.g. `text-muted-foreground`
with no `hidden`), since the label must now show on mobile (stacked) and desktop (inline).

CONSTRAINTS:
- Text stays at the `text-sm` floor — do NOT shrink to `text-xs` (the existing `text-sm`
  on the row spans must be preserved). The info-popover bodies are out of scope and unchanged.
- Preserve ALL existing `data-testid`, `aria-label`, and `style` (opacity dimming via
  UNRELIABLE_OPACITY) attributes on the bullet and text spans. Do not drop or rename them.
- No hard-coded colors — keep theme-constant / zone-hex usage (`scoreZoneHex`, eval zone
  font) exactly as-is.
- This is layout/CSS only — no changes to values, props passed to MiniBulletChart, popover
  props, or data flow.
- Apply identical treatment to both files and both rows; verify no desktop visual regression
  (the inline grid at sm+ must look unchanged).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run</automated>
  </verify>
  <done>On mobile (below sm), the Score and End Eval labels render above their bullet charts in both OpeningStatsCard and OpeningFindingCard. On sm+ the inline two-column layout is visually unchanged. No `hidden sm:inline` remains on the two eval-row labels. Lint and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Renamed opening-side "Eval:" → "End Eval:" at three sites (move explorer, opening cards,
insights finding cards). On mobile, the Score and End Eval labels (with value + "?" icon)
now stack above their bullet charts; desktop inline layout unchanged.
  </what-built>
  <how-to-verify>
1. Run the frontend dev server and open the Openings page.
2. DESKTOP (wide viewport): confirm the move explorer (PositionResultsPanel), the opening
   cards (OpeningStatsCard), and the insights finding cards (OpeningFindingCard) show
   "End Eval:" inline to the left of the value, side-by-side with the bullet chart exactly
   as before (only the label text changed). Confirm "Score:" is unchanged.
3. MOBILE (narrow viewport / responsive mode below the sm breakpoint, ~640px): confirm that
   in the opening cards and insights finding cards, the "Score" and "End Eval" labels —
   each with its value and "?" info icon — appear ON TOP of their respective bullet charts
   (stacked), NOT hidden and NOT a bare "+0.4".
4. Confirm the Endgames page still shows "Entry Eval" (untouched) and the EvalConfidenceTooltip
   "?" popover body still reads "...end of your openings..." (untouched).
5. Confirm text is readable at text-sm (no tiny text-xs labels introduced).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Rename: `grep -rn 'Eval:' frontend/src/components/charts/PositionResultsPanel.tsx frontend/src/components/stats/OpeningStatsCard.tsx frontend/src/components/insights/OpeningFindingCard.tsx` shows only "End Eval:" (no bare opening-side "Eval:").
- Endgame untouched: `grep -rn 'Entry Eval' frontend/src/components/charts/EndgameOverallEntryCard.tsx` still present and unchanged.
- Tooltip untouched: `git diff --stat frontend/src/components/insights/EvalConfidenceTooltip.tsx` is empty.
- Build/lint/test: `cd frontend && npm run lint && npm test -- --run && npm run build` all pass.
- No dev DB reset required (frontend-only change).
</verification>

<success_criteria>
- All three opening-side eval labels read "End Eval:".
- Mobile renders Score + End Eval labels above their bullet charts in both card components;
  desktop inline layout unchanged.
- "Score" labels, endgame "Entry Eval", and EvalConfidenceTooltip copy are untouched.
- text-sm floor preserved; data-testid / aria-label / theme constants preserved.
- Lint, tests, and build pass.
</success_criteria>

<output>
Create `.planning/quick/260603-q85-openings-eval-label-disambiguation-renam/260603-q85-SUMMARY.md` when done
</output>
