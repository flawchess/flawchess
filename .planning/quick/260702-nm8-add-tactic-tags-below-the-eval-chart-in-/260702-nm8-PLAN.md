---
phase: quick-260702-nm8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/analysis/AnalysisTagsPanel.tsx
  - frontend/src/components/analysis/__tests__/AnalysisTagsPanel.test.tsx
  - frontend/src/pages/Analysis.tsx
autonomous: true
requirements: [QUICK-260702-nm8]
must_haves:
  truths:
    - On the desktop /analysis page in game mode, a tags panel appears directly below the eval chart showing a severity-badge row (Blunders/Mistakes/Inaccuracies) on top and a 3-column Missed | Allowed | Context tag block below.
    - On mobile /analysis in game mode, a third "Tags" tab sits alongside Moves and Eval chart, rendering the same severity row + 3-column block.
    - Clicking/tapping any severity badge or tactic/context chip navigates the chess board (and, in sync, the move list and eval-chart crosshair) to that flaw's positions, cycling through them on repeated clicks.
    - The panel/tab only renders when the loaded game has analyzed flaw markers; it never shows a NoAnalysisState placeholder.
  artifacts:
    - frontend/src/components/analysis/AnalysisTagsPanel.tsx
    - frontend/src/components/analysis/__tests__/AnalysisTagsPanel.test.tsx
  key_links:
    - AnalysisTagsPanel.onCyclePly(ply) -> Analysis.tsx goToNode(mainLine[ply]) which syncs board + move list + eval-chart crosshair.
    - AnalysisTagsPanel derivations read gameData.flaw_markers (scoped is_user) exactly like LibraryGameCard.
---

<objective>
Add a flaw-tags panel to the /analysis page (game mode) that mirrors the Library game card's tag block: a severity-badge row on top and a 3-column Missed | Allowed | Context tactic+context block below. Clicking any badge/chip cycles the board through that flaw's positions, synced with the move list and the eval-chart crosshair. On mobile, expose it as a third "Tags" tab below the board.

Purpose: The analyzed game data is already present on the Analysis page (`gameData: GameFlawCard` from `useLibraryGame`, carrying `flaw_markers`, `chips`, `severity_counts`) but is not surfaced. This gives the user the same at-a-glance flaw overview and click-to-navigate cycling they get on the Library card.

Output: A self-contained `AnalysisTagsPanel` component (with unit test) plus wiring into `Analysis.tsx` for both desktop (below eval chart) and mobile (new Tags tab). No backend changes, no new data fetching.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Reference implementation to mirror (study the derivations + render blocks, do NOT modify it):
@frontend/src/components/results/LibraryGameCard.tsx

# Target page to wire into:
@frontend/src/pages/Analysis.tsx

# Leaf components to reuse (do NOT modify — pass props through):
@frontend/src/components/library/TacticMotifGroup.tsx
@frontend/src/components/library/SeverityBadge.tsx

# Types + pure helpers:
@frontend/src/types/library.ts
@frontend/src/lib/tacticComparisonMeta.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the self-contained AnalysisTagsPanel component + unit test</name>
  <files>frontend/src/components/analysis/AnalysisTagsPanel.tsx, frontend/src/components/analysis/__tests__/AnalysisTagsPanel.test.tsx</files>
  <behavior>
    - Given a GameFlawCard with user (is_user) flaw markers, the panel renders three SeverityBadge elements (blunder/mistake/inaccuracy) with counts from severity_counts, and a 3-column grid (Missed | Allowed | Context).
    - Clicking a severity badge (or tactic/context chip) calls the onCyclePly prop with the FIRST ply of that ref's ascending ply list; clicking the SAME ref again calls onCyclePly with the NEXT ply, wrapping modulo the list length.
    - Clicking a DIFFERENT ref restarts the cycle at its first ply.
    - A ref whose ply list is empty does not call onCyclePly.
    - Only distinct (orientation, display-label) tactic motifs render; family-less motifs (TACTIC_FAMILY_FOR_MOTIF[label] == null) are skipped; named-mate subtypes collapse to a single "checkmate" chip per orientation.
    - Test with a fixture GameFlawCard covering: a blunder marker with a missed_tactic_motif, a mistake marker with an allowed_tactic_motif, a context chip, and a non-user (is_user=false) marker that must be EXCLUDED from all counts/cycles.
  </behavior>
  <action>
    Create `frontend/src/components/analysis/AnalysisTagsPanel.tsx` as a NEW standalone component. Do NOT refactor or import from LibraryGameCard (per resolved decision 4 — the card is well-tested; keep it untouched).

    Props (typed, no `any`): `game: GameFlawCard` (import type from `@/types/library`), `onCyclePly: (ply: number) => void`. Optionally `className?: string` for layout composition.

    Re-declare inside this file the small pure helpers/constants copied from LibraryGameCard (they are trivially safe local declarations, not shared extractions): `TacticChipOrientation` type (`'missed' | 'allowed'`), `TACTIC_ORIENTATIONS` (`['missed','allowed']`), `TACTIC_ORIENTATION_LABELS`, `SEVERITY_ORDER` (`['blunder','mistake','inaccuracy']`), the `FlawRef` union (`{kind:'tag';tag} | {kind:'severity';severity} | {kind:'motif';motif;orientation}`), `sameFlawRef`, and `motifPliesKey(orientation, motifLabel)`.

    Derive from `game.flaw_markers ?? []`, ALL scoped to `fm.is_user` (mirroring LibraryGameCard lines ~314-489): `severityPlies` (Map<FlawSeverity, number[]> ascending), `tagPlies` (Map<FlawTag, number[]>), `tagCounts` (Map<FlawTag, number>), `motifPlies` (Map<string, number[]> keyed by motifPliesKey using `tacticMotifLabel` from `@/lib/tacticComparisonMeta`), and `tacticMotifs` (distinct {motif,orientation}, allowed collected before missed, skipping motifs where `TACTIC_FAMILY_FOR_MOTIF[label] == null`). Wrap each in `useMemo` keyed on `game.flaw_markers`.

    Cycle state lives INSIDE the panel: `const [cycle, setCycle] = useState<{ ref: FlawRef; pos: number } | null>(null)`. Implement `pliesForRef(ref)` (tag→tagPlies, severity→severityPlies, motif→motifPlies.get(motifPliesKey(...))) and `handleActivate(ref)`: compute `plies = pliesForRef(ref)`; if empty, return; `pos = cycle && sameFlawRef(cycle.ref, ref) ? (cycle.pos + 1) % plies.length : 0`; `setCycle({ref, pos})`; then guard `const ply = plies[pos]; if (ply !== undefined) onCyclePly(ply);` (noUncheckedIndexedAccess).

    Render (mirror LibraryGameCard's mobile `flawContent` order — severity row THEN chips block, per resolved decision 2): a container `<div data-testid="analysis-tags-panel">` wrapping (a) a `flex items-center gap-1.5 flex-wrap` severity row of `SeverityBadge` (severity, count from `game.severity_counts?.[sev] ?? 0`, `gameId={game.game_id}`, `onActivate={() => handleActivate({kind:'severity',severity:sev})}`; omit onHover for now — hover-highlight is a separate optional task) and (b) a `grid grid-cols-1 md:grid-cols-3 gap-x-3 gap-y-2` block with two `TacticMotifGroup` (Missed then Allowed via TACTIC_ORIENTATIONS) and a `ChipColumn label="Context"` containing `TagChip`s. Import leaf components from `@/components/library/...` (SeverityBadge, TacticMotifGroup, ChipColumn, TagChip, TagLegend). Pass `filterRingActive: false` on every tactic motif (resolved decision 3 — no filter store on /analysis; do not compute any ring). For the Context column, map `game.chips` to `TagChip` with `count={tagCounts.get(tag)}`, `definition={false}`, `onActivate={() => handleActivate({kind:'tag',tag})}`, and a `TagLegend variant="icon"` trailing the label as the card does.

    Guard: if `game.analysis_state !== 'analyzed'` OR `(game.flaw_markers ?? []).length === 0`, return `null` (resolved decision 3 — no NoAnalysisState). The Analysis page also gates rendering, so this is belt-and-suspenders.

    All interactive leaf elements already emit their own data-testids keyed by gameId — pass `game.game_id` through. Add `data-testid="analysis-tags-panel"` on the container. Do NOT introduce text-xs (leaf components comply). Do NOT hard-code theme colors (leaf components own them).

    Keep the component under the CLAUDE.md function-size limits: the derivations are useMemo hooks, the return is a declarative JSX tree with two mapped groups — that is fine. If any single derivation feels heavy, it is a direct copy of the card's and should stay a plain useMemo.

    Write `frontend/src/components/analysis/__tests__/AnalysisTagsPanel.test.tsx` using the existing frontend test setup (Vitest + Testing Library — match the import style of a sibling test such as an existing library component test). Build a minimal `GameFlawCard` fixture inline (only the fields the panel reads: `game_id`, `analysis_state: 'analyzed'`, `severity_counts`, `chips`, `flaw_markers`). Assert: (1) three SeverityBadge testids present with correct counts; (2) a spy `onCyclePly` receives the first then next then wrapped ply on repeated clicks of the same badge; (3) a non-user marker is excluded (its ply never reaches onCyclePly and its motif produces no chip); (4) clicking a different ref restarts at pos 0. Query interactive elements by their leaf data-testids (inspect SeverityBadge / TacticMotifChip / TagChip for the exact `data-testid` format keyed on gameId).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/analysis/__tests__/AnalysisTagsPanel.test.tsx</automated>
  </verify>
  <done>AnalysisTagsPanel renders severity row + 3-column block from a GameFlawCard fixture, cycles onCyclePly correctly (first/next/wrap/restart), excludes non-user markers, and the unit test passes. Component returns null when not analyzed or no markers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AnalysisTagsPanel into Analysis.tsx (desktop below eval chart + mobile Tags tab)</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <action>
    Import `AnalysisTagsPanel` from `@/components/analysis/AnalysisTagsPanel`.

    Add a shared render helper alongside the existing `evalChart` / `variationTree` / `boardControls` helpers (near line ~789), so the panel mounts exactly once regardless of desktop/mobile (only one tree renders per `isMobile`):
    ```
    (define a `tagsPanel` arrow that returns the panel when the same readiness gate as evalChartReady holds, else null)
    ```
    The readiness gate is the existing `evalChartReady` (isGameMode + gameId + gameData eval_series/flaw_markers/phase_transitions/moves all present). Inside the helper, guard `gameData` for null before passing it. The cycle callback reuses the EXACT existing pattern from `handleEvalChartPlyChange` (lines ~678-683) — a single `goToNode(mainLine[ply])` call auto-syncs board + move list + eval-chart crosshair (evalChartPly derives from currentNodeId): pass `onCyclePly={(ply) => { const nodeId = mainLine[ply]; if (nodeId !== undefined) goToNode(nodeId); }}` (T-140-02b out-of-bounds guard for noUncheckedIndexedAccess). Do NOT add commandedPly/PV plumbing (resolved decision 1).

    Desktop: in the board column (the `flex flex-col gap-2 w-full lg:w-[628px]` div, ~line 898), render `{tagsPanel()}` directly AFTER the eval-chart block (`{evalChartReady && (<div data-testid="analysis-eval-chart">...)}` at ~line 909-911), still inside that board column div.

    Mobile: in the `if (isMobile)` block's `Tabs` (~line 846-874), add a THIRD tab. In `TabsList` add `<TabsTrigger value="tags" data-testid="analysis-tab-tags">Tags</TabsTrigger>` after the existing eval trigger. Add a matching `<TabsContent value="tags" className="min-h-0 overflow-y-auto thin-scrollbar">` wrapping `<div className="px-2">{tagsPanel()}</div>` (match the padding/scroll conventions of the sibling tab contents). Keep `defaultValue="moves"`.

    Follow the existing testids (`analysis-tab-moves`, `analysis-tab-eval`) for naming consistency. Do not alter board navigation, evalChartPly derivation, or the eval-chart props.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc -b && npm test -- --run</automated>
  </verify>
  <done>Desktop /analysis game mode shows the tags panel below the eval chart; mobile shows a third "Tags" tab. Clicking a badge/chip navigates the board (synced move list + eval-chart crosshair). Lint, tsc -b, and the full frontend test suite pass.</done>
</task>

<task type="auto">
  <name>Task 3 (OPTIONAL, lower priority): Desktop hover-highlight of eval-chart markers</name>
  <files>frontend/src/components/analysis/AnalysisTagsPanel.tsx, frontend/src/pages/Analysis.tsx</files>
  <action>
    OPTIONAL polish (resolved decision 5). Only do this if Tasks 1-2 are green AND it is genuinely cheap. If it adds meaningful risk or complexity, SKIP it and note in the SUMMARY that hover-highlight was deferred.

    Goal: hovering a badge/chip on desktop dims non-matching eval-chart markers (mirroring the card's `highlightedPlies`). Mobile is out of scope (chart is on a different tab).

    In AnalysisTagsPanel: add optional prop `onHighlightChange?: (plies: Set<number> | null) => void`. Add local `highlight` state and a `highlightedPlies` useMemo mirroring LibraryGameCard lines ~432-454 (match severity/tag/motif refs against is_user markers; a cycled ref also stays lit — read `cycle?.ref` as fallback). Wire `onHover` on SeverityBadge/TagChip/TacticMotifGroup to set `highlight`, and call `onHighlightChange` from an effect when the derived set changes.

    In Analysis.tsx: lift a `highlightedPlies` state, pass `onHighlightChange={setHighlightedPlies}` to the desktop `tagsPanel()` only, and forward the set into the desktop `evalChart(...)` as its highlight prop. Inspect EvalChart's props for the correct prop name (the card passes a highlighted-plies set to dim non-matching markers) — if EvalChart does not already accept such a prop, DEFER this task rather than modifying EvalChart (out of scope). Do NOT pass it on mobile.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc -b && npm test -- --run</automated>
  </verify>
  <done>Either: desktop hover dims non-matching eval-chart markers with lint/tsc/tests green; OR the task is explicitly deferred in the SUMMARY with the reason (EvalChart lacks a highlight prop / risk not worth it). The core feature (Tasks 1-2) is never blocked on this.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client render | flaw markers / chips / usernames already fetched via useLibraryGame; rendered as React children (auto-escaped). No new input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nm8-01 | Tampering | AnalysisTagsPanel rendering server-provided tag/motif strings | low | accept | Values render as React children (auto-escaped); no dangerouslySetInnerHTML; leaf chip components already sanitize. No user-controlled HTML introduced. |
| T-nm8-02 | Denial of Service | out-of-bounds mainLine[ply] on cycle | low | mitigate | Guard `mainLine[ply] !== undefined` before goToNode (T-140-02b pattern) — noUncheckedIndexedAccess enforced by tsc -b. |

No package installs in this change → no supply-chain (T-nm8-SC) threat.
</threat_model>

<verification>
- `cd frontend && npm run lint` passes (no ESLint errors, knip clean — new component IS imported by Analysis.tsx).
- `cd frontend && npx tsc -b` passes with zero type errors (shared-type/prop access — mandatory per memory rule: npm lint+test do NOT type-check).
- `cd frontend && npm test -- --run` full frontend suite passes, including the new AnalysisTagsPanel test.
- Manual (HUMAN-UAT, not a completion gate): on desktop /analysis?game_id=X the tags panel appears below the eval chart; on mobile a "Tags" tab appears; clicking a badge/chip navigates the board with synced move list + eval-chart crosshair.
- Backend untouched — no python/pytest/ruff/ty run needed.
</verification>

<success_criteria>
- Desktop /analysis game mode renders `analysis-tags-panel` below the eval chart with a severity row on top and a 3-column Missed | Allowed | Context block below.
- Mobile /analysis game mode has a third `analysis-tab-tags` tab rendering the same layout.
- Clicking/tapping a severity badge or tactic/context chip cycles the board through the flaw's positions, synced with move list + eval-chart crosshair via a single goToNode call.
- The panel renders only when analyzed markers exist; no NoAnalysisState placeholder; no filter ring on chips.
- LibraryGameCard is unmodified.
- Frontend gate green: lint + tsc -b + tests.
</success_criteria>

<output>
Create `.planning/quick/260702-nm8-add-tactic-tags-below-the-eval-chart-in-/260702-nm8-SUMMARY.md` when done.
</output>
