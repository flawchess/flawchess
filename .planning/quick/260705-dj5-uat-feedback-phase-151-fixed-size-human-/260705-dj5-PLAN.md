# Quick Task 260705-dj5: Phase 151 Maia UAT — fixed-size card, wider chart, move labels, grid lines

**Created:** 2026-07-05
**Type:** UI polish (frontend)
**Surface:** `frontend/src/components/analysis/MovesByRatingChart.tsx` (rendered inside `MaiaHumanPanel`)

## Task

Three UAT tweaks to the "Human Move Probability" card from Phase 151:

1. **Fixed-size card + loading animation** — the empty state rendered a short
   "Waiting for Maia analysis…" text, so the card jumped when the `h-64` chart
   appeared. Give the placeholder the chart's fixed height with a pulsing loading
   animation, mirroring the engine (Stockfish) card's no-jump skeleton.
2. **Wider chart + right-side move labels** — reclaim left whitespace so the plot
   is wider, and label each shown line with its SAN at its right end (an on-chart
   legend, matching the desktop reference image).
3. **Horizontal grid lines** — add horizontal-only grid lines.

## Tasks

- **T1 — Fixed-height loading skeleton.** Replace the empty-state text branch with a
  fixed-height (`h-64`) pulsing placeholder (`animate-pulse`, `aria-busy`). Keep an
  `sr-only` "Waiting for Maia analysis…" text for screen readers and the existing
  `aria-label`.
  - files: `MovesByRatingChart.tsx`
  - verify: `moves-by-rating-chart-skeleton` testid present; container is `h-64`.
  - done: card holds constant height loading → rendered.

- **T2 — Wider plot + end-of-line move labels.** Tighten `YAxis width` (36) to reclaim
  the left gutter, add right margin (40) for labels, and render each line's SAN at its
  last ELO rung via a per-line `LabelList` `content` render (draws only at `lastIndex`,
  in the line's own color).
  - files: `MovesByRatingChart.tsx`
  - verify: `tsc -b` clean; labels use `MOVES_BY_RATING_*` line colors.
  - done: plot is wider on the left; each shown move labeled on the right.

- **T3 — Horizontal grid lines.** Add `<CartesianGrid vertical={false} />` (themed via
  the ChartContainer's `#ccc` → `stroke-border/50` override, same as ScoreChart).
  - files: `MovesByRatingChart.tsx`
  - verify: grid renders horizontal-only.
  - done: horizontal grid lines visible.

## Gate

`npx tsc -b`, `npm run lint`, `npm run knip`, affected vitest suites
(`MovesByRatingChart`, `MaiaHumanPanel`).
