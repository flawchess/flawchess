---
phase: quick
plan: 260318-pp5
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Openings.tsx
autonomous: true
must_haves:
  truths:
    - "Played as and Piece filter controls are always enabled regardless of active tab"
    - "Piece filter label has an info icon that shows a tooltip on hover"
    - "Tooltip explains what the piece filter is good for and that it does not apply to Moves tab"
  artifacts:
    - path: "frontend/src/pages/Openings.tsx"
      provides: "Always-enabled filters with info tooltip on piece filter"
  key_links: []
---

<objective>
Remove tab-based disabling of "Played as" and "Piece filter" controls. Instead, add a hover
info icon (lucide `Info`) next to the "Piece filter" label with a tooltip explaining its purpose.

Purpose: Filters should always be interactive. Users learn what the piece filter does via tooltip
rather than being confused by greyed-out controls.
Output: Updated Openings.tsx with always-enabled filters and info tooltip.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/Openings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove filter disabling and add info tooltip to Piece filter</name>
  <files>frontend/src/pages/Openings.tsx</files>
  <action>
In Openings.tsx, make these changes:

1. Add `Info` to the lucide-react import (alongside ChevronUp, ChevronDown, Save, Sparkles).

2. Delete the two disabled-state variables (lines ~88-89):
   - `const pieceFilterDisabled = ...`
   - `const playedAsDisabled = ...`

3. In the "Played as" section (~line 288-323):
   - Remove the wrapping `Tooltip`/`TooltipTrigger`/`TooltipContent` around the "Played as" div.
   - Remove `className={playedAsDisabled ? 'opacity-50' : ''}` from the outer div (just use no conditional class).
   - Remove the `if (playedAsDisabled || !v) return;` guard in onValueChange — keep only `if (!v) return;`.
   - Remove `disabled={playedAsDisabled}` from the ToggleGroup and both ToggleGroupItems.
   - Remove `aria-disabled={playedAsDisabled}` from the ToggleGroup.

4. In the "Piece filter" section (~line 325-354):
   - Remove the wrapping `Tooltip`/`TooltipTrigger`/`TooltipContent` that showed "Not applicable for this tab".
   - Remove `className={... pieceFilterDisabled ? 'opacity-50' : ''}` conditional — keep `ml-auto`.
   - Remove `if (pieceFilterDisabled || !v) return;` guard — keep only `if (!v) return;`.
   - Remove `disabled={pieceFilterDisabled}` from the ToggleGroup and all three ToggleGroupItems.
   - Remove `aria-disabled={pieceFilterDisabled}` from the ToggleGroup.
   - Replace the label `<p className="mb-1 text-xs text-muted-foreground">Piece filter</p>` with a flex row containing the label plus an info icon with tooltip:
     ```tsx
     <div className="mb-1 flex items-center gap-1">
       <p className="text-xs text-muted-foreground">Piece filter</p>
       <TooltipProvider>
         <Tooltip>
           <TooltipTrigger asChild>
             <button type="button" className="text-muted-foreground hover:text-foreground" aria-label="Piece filter info" data-testid="piece-filter-info">
               <Info className="h-3.5 w-3.5" />
             </button>
           </TooltipTrigger>
           <TooltipContent side="top" className="max-w-xs text-sm">
             Use the piece filter to find games where you set up a specific formation (e.g. the London System) regardless of the opponent's response. "Mine" matches only your pieces, "Opponent" only theirs, and "Both" requires an exact position. This filter applies to the Games and Statistics tabs but not to Moves, which always shows all legal next moves from the current position.
           </TooltipContent>
         </Tooltip>
       </TooltipProvider>
     </div>
     ```

5. Since the outer `<TooltipProvider>` wrapper (line ~286) was only needed for the disabled tooltips, remove it. The new info icon has its own `TooltipProvider`. Keep any other `TooltipProvider` usage intact.

6. Verify there are no remaining references to `pieceFilterDisabled` or `playedAsDisabled`.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/chessalytics/frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - "Played as" toggle is always enabled on all tabs (no opacity, no disabled attribute)
    - "Piece filter" toggle is always enabled on all tabs
    - Info icon (circle-i) appears next to "Piece filter" label
    - Hovering the info icon shows tooltip explaining piece filter purpose and Moves tab exception
    - No references to pieceFilterDisabled or playedAsDisabled remain in the file
    - Build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `npm run build` succeeds
- No grep hits for `pieceFilterDisabled` or `playedAsDisabled` in the codebase
</verification>

<success_criteria>
- Both filter controls are always interactive regardless of active tab
- Info icon with descriptive tooltip is visible next to "Piece filter" label
- Clean build with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260318-pp5-add-hover-info-icon-to-piece-filter-with/260318-pp5-SUMMARY.md`
</output>
