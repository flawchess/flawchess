---
phase: quick-260506-u2b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/opening_insights.py
  - app/services/opening_insights_service.py
  - app/services/opening_insights_constants.py
  - tests/services/test_opening_insights_service.py
  - tests/repositories/test_stats_repository.py
  - frontend/src/types/insights.ts
  - frontend/src/hooks/useOpeningInsights.ts
  - frontend/src/components/insights/OpeningFindingCard.tsx
  - frontend/src/components/insights/OpeningFindingCard.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "OpeningFindingCard renders the same WDLChartRow component used by OpeningStatsCard, replacing the old MiniBulletChart score bullet"
    - "OpeningFindingCard renders an MG-entry eval bullet row below the WDL bar, mirroring OpeningStatsCard's evalLine layout (signed pawns text + per-color baseline tick + BulletConfidencePopover)"
    - "Card prose is abbreviated to 'Score <X>% after <candidate_move>' (no 'You score X% as <Color>')"
    - "Left border + candidate-move arrow on the mini board still use scoreZoneColor(finding.score) — NOT eval-zone color"
    - "Backend OpeningInsightFinding payload exposes avg_eval_pawns, eval_n, eval_ci_low_pawns, eval_ci_high_pawns, eval_confidence, eval_p_value computed for the entry_position+candidate_move junction"
    - "Backend OpeningInsightsResponse exposes top-level eval_baseline_pawns_white and eval_baseline_pawns_black (matching MostPlayedOpeningsResponse)"
    - "MG-entry eval computation REUSES the existing query_opening_phase_entry_metrics_batch helper (no duplicated SQL/aggregation)"
    - "Mobile and desktop layouts in OpeningFindingCard both receive the new stack: header → prose → WDL bar → eval bullet → Moves/Games links"
    - "uv run ty check app/ tests/, uv run ruff check ., uv run pytest, npm run lint, npm test all pass"
  artifacts:
    - path: "app/schemas/opening_insights.py"
      provides: "OpeningInsightFinding extended with eval_* fields; OpeningInsightsResponse extended with eval_baseline_pawns_{white,black}"
    - path: "app/services/opening_insights_service.py"
      provides: "Service builds findings with MG-entry eval populated via shared helper, attaches per-color baselines to response"
    - path: "frontend/src/types/insights.ts"
      provides: "OpeningInsightFinding TS type mirrors backend additions; OpeningInsightsResponse exposes baselines"
    - path: "frontend/src/components/insights/OpeningFindingCard.tsx"
      provides: "Refactored card matching OpeningStatsCard layout (WDLChartRow + eval bullet)"
    - path: "frontend/src/components/insights/OpeningFindingCard.test.tsx"
      provides: "Tests cover new prose, WDL row, eval bullet, removal of legacy score-bullet testid"
  key_links:
    - from: "app/services/opening_insights_service.py"
      to: "app/repositories/stats_repository.py::query_opening_phase_entry_metrics_batch"
      via: "Reused helper called per (color, list_of_resulting_full_hash) — keys findings by their resulting_full_hash"
      pattern: "query_opening_phase_entry_metrics_batch"
    - from: "frontend/src/components/insights/OpeningFindingCard.tsx"
      to: "frontend/src/components/charts/WDLChartRow.tsx"
      via: "Direct import + render with finding.{wins,draws,losses,n_games,...}"
      pattern: "WDLChartRow"
    - from: "frontend/src/components/insights/OpeningFindingCard.tsx"
      to: "frontend/src/components/insights/BulletConfidencePopover.tsx"
      via: "Direct import — same usage as OpeningStatsCard.tsx lines 141-148"
      pattern: "BulletConfidencePopover"
---

<objective>
Align the Openings **Insights** card (`OpeningFindingCard`) with the **Stats** card (`OpeningStatsCard`) layout: replace the legacy MiniBulletChart score bullet + ScoreConfidencePopover with the shared `WDLChartRow`, add an MG-entry eval bullet row below it, abbreviate the prose, and surface the backend MG-entry eval data (per-finding stats + per-color baselines) so the eval row has data to render. The card border and on-board arrow stay score-zone tinted; only the inner bullet row changes to eval-zone semantics.

Purpose: Visual parity between Insights and Stats subtabs so users see the same eval signal at MG entry regardless of which subtab they're on. Insights conforms to Stats, never the other way around.
Output: Updated backend schema + service, FE types, refactored card component, updated tests. All lint/typecheck/test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@frontend/src/components/insights/OpeningFindingCard.tsx
@frontend/src/components/stats/OpeningStatsCard.tsx
@frontend/src/components/insights/BulletConfidencePopover.tsx
@frontend/src/lib/openingStatsZones.ts
@frontend/src/types/insights.ts
@frontend/src/types/stats.ts
@app/schemas/opening_insights.py
@app/schemas/stats.py
@app/services/stats_service.py
@app/services/opening_insights_service.py
@app/repositories/stats_repository.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase so no scavenger hunt is required. -->

### Backend — `OpeningInsightFinding` (current shape, app/schemas/opening_insights.py)
```python
class OpeningInsightFinding(BaseModel):
    color: Literal["white", "black"]
    classification: Literal["weakness", "strength"]
    severity: Literal["minor", "major"]
    opening_name: str
    opening_eco: str
    display_name: str
    entry_fen: str
    entry_san_sequence: list[str]
    entry_full_hash: str
    candidate_move_san: str
    resulting_full_hash: str
    n_games: int
    wins: int; draws: int; losses: int
    score: float
    confidence: Literal["low", "medium", "high"]
    p_value: float
    ci_low: float; ci_high: float
```

### Backend — `OpeningWDL` Phase 80 fields to mirror (app/schemas/stats.py:64-69)
```python
avg_eval_pawns: float | None = None
eval_ci_low_pawns: float | None = None
eval_ci_high_pawns: float | None = None
eval_n: int = 0
eval_p_value: float | None = None
eval_confidence: Literal["low", "medium", "high"] = "low"
```

### Backend — `OpeningInsightsResponse` (current, app/schemas/opening_insights.py:87-97)
```python
class OpeningInsightsResponse(BaseModel):
    white_weaknesses: list[OpeningInsightFinding] = Field(default_factory=list)
    black_weaknesses: list[OpeningInsightFinding] = Field(default_factory=list)
    white_strengths: list[OpeningInsightFinding] = Field(default_factory=list)
    black_strengths: list[OpeningInsightFinding] = Field(default_factory=list)
```
ADD: `eval_baseline_pawns_white: float`, `eval_baseline_pawns_black: float` (mirror MostPlayedOpeningsResponse).

### Backend — Shared helper to reuse (app/repositories/stats_repository.py:452)
```python
async def query_opening_phase_entry_metrics_batch(
    session: AsyncSession,
    user_id: int,
    hashes: list[int],
    color: Literal["white", "black"] | None = None,
    time_control: Sequence[str] | None = None,
    platform: Sequence[str] | None = None,
    rated: bool | None = None,
    opponent_type: str = "human",
    recency_cutoff: datetime.datetime | None = None,
    opponent_gap_min: int | None = None,
    opponent_gap_max: int | None = None,
    hash_column: Literal["full", "white", "black"] = "full",
) -> dict[int, OpeningPhaseEntryMetrics]:
```

The helper aggregates MG-entry eval per `full_hash` for games passing through that position. **For findings, the natural junction key is `resulting_full_hash`** — the position immediately after the candidate move was played — because that's how Stats keys MG entry eval (per opening's full_hash, which is the position-after-the-move-sequence).

### Backend — Eval finalizer pattern (app/services/stats_service.py:464-496)
The function `_phase80_item_from_metrics` already encapsulates "OpeningPhaseEntryMetrics → display fields" cleanly. **Reuse it** for findings — extract a small helper that returns `(avg_eval_pawns, eval_ci_low_pawns, eval_ci_high_pawns, eval_n, eval_p_value, eval_confidence)` from an `OpeningPhaseEntryMetrics`, or call `_phase80_item_from_metrics(...)` and copy the fields. Either approach avoids duplicating the `compute_eval_confidence_bucket` math.

### Backend — Per-color baselines (app/services/opening_insights_constants.py:80)
```python
EVAL_BASELINE_PAWNS_WHITE: float = 0.25
EVAL_BASELINE_PAWNS_BLACK: float = ...   # confirm value in file
```
Already imported by stats_service. Re-import in opening_insights_service.

### Frontend — `WDLChartRow` usage pattern (OpeningStatsCard.tsx:109-125)
```tsx
const wdlData = {
  wins, draws, losses, total, win_pct, draw_pct, loss_pct,
};
<WDLChartRow data={wdlData} showSegmentCounts={false} testId={`${cardTestId}-wdl`} />
```
For findings: `total = n_games`. Compute `win_pct/draw_pct/loss_pct` inline (`round(wins / n_games * 100, 1)` etc., guard `n_games === 0`).

### Frontend — Eval bullet row pattern (OpeningStatsCard.tsx:127-152)
The structure to copy verbatim: `<MiniBulletChart>` with `tickPawns={evalBaselinePawns}` + signed-pawns text + `<BulletConfidencePopover>`. Use `EVAL_NEUTRAL_MIN_PAWNS / EVAL_NEUTRAL_MAX_PAWNS / EVAL_BULLET_DOMAIN_PAWNS` from `@/lib/openingStatsZones`. Use `formatSignedEvalPawns` from `@/lib/clockFormat`.

### Frontend — `OpeningInsightsResponse` (current TS, frontend/src/types/insights.ts:92-97)
ADD: `eval_baseline_pawns_white: number;` `eval_baseline_pawns_black: number;`. Wire through `useOpeningInsights` (no logic change — TanStack Query just returns the response shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — extend finding schema + populate MG-entry eval via shared Stats helper</name>
  <files>
    app/schemas/opening_insights.py,
    app/services/opening_insights_service.py,
    tests/services/test_opening_insights_service.py,
    tests/repositories/test_stats_repository.py
  </files>
  <behavior>
    - `OpeningInsightFinding` accepts and round-trips the new optional eval fields: `avg_eval_pawns: float | None = None`, `eval_ci_low_pawns: float | None = None`, `eval_ci_high_pawns: float | None = None`, `eval_n: int = 0`, `eval_p_value: float | None = None`, `eval_confidence: Literal["low","medium","high"] = "low"`.
    - `OpeningInsightsResponse` carries `eval_baseline_pawns_white: float` and `eval_baseline_pawns_black: float` at the top level.
    - When `query_opening_phase_entry_metrics_batch` returns metrics for a finding's `resulting_full_hash` (keyed by `int(finding.resulting_full_hash)`), the finding emerges with eval_n > 0 and `avg_eval_pawns` matching `compute_eval_confidence_bucket` output (cp / 100). When metrics are absent, fields stay at defaults (eval_n=0, others None / "low").
    - When all findings in a color section are dropped, the response still carries the per-color baselines (they are response-level, not section-level).
    - Test: a service-level test exercises the finding builder by injecting a synthetic `OpeningPhaseEntryMetrics` mapping for a known `resulting_full_hash` and asserts the new fields land on the resulting `OpeningInsightFinding` with correct values. (Use the existing test patterns / fixtures in `tests/services/test_opening_insights_service.py` — extend, don't rewrite.)
    - Test: a repository-level integration test (in `tests/repositories/test_stats_repository.py`) confirms `query_opening_phase_entry_metrics_batch` works correctly with `hash_column="full"` against a finding-shaped hash list (likely already covered — add a small assertion or skip if redundant).
  </behavior>
  <action>
    1. **Extend `OpeningInsightFinding`** in `app/schemas/opening_insights.py`: add the six new optional fields after `ci_high`, with the same defaults / Literal types used by `OpeningWDL` (mirror exactly, including the doc-comment style — e.g. "MG-entry pillar (parity with OpeningWDL Phase 80)"). Keep `extra="forbid"` ConfigDict if present at module level (it's on `OpeningInsightsRequest` only, not the finding — verify).

    2. **Extend `OpeningInsightsResponse`** in the same file: add `eval_baseline_pawns_white: float` and `eval_baseline_pawns_black: float` (no defaults — required, matching `MostPlayedOpeningsResponse`).

    3. **Wire MG-entry eval into the service** (`app/services/opening_insights_service.py`):
       - Import `query_opening_phase_entry_metrics_batch` from `app.repositories.stats_repository`, `EVAL_BASELINE_PAWNS_WHITE` and `EVAL_BASELINE_PAWNS_BLACK` from `app.services.opening_insights_constants` (or wherever they live — confirm), and `compute_eval_confidence_bucket` from `app.services.eval_confidence` if not already imported.
       - **After** all findings are built but **before** the response is constructed (around line 543), call the helper **once per color** (white / black) with `hashes = list({int(f.resulting_full_hash) for f in white_findings})` and `color="white"`, then again for black. Pass `hash_column="full"` (default) plus the same filter args used elsewhere in the service. Sequential awaits (CLAUDE.md AsyncSession rule).
       - For each finding, look up `metrics_by_hash.get(int(f.resulting_full_hash))`. If present and `eval_n_mg > 0`, compute the six display fields the same way `_phase80_item_from_metrics` does (cp→pawns, CI gated by `eval_n_mg >= 2`). Set them on the finding (mutate or rebuild via `model_copy(update=...)`).
       - **Refactor goal: no duplicated math.** Either (a) extract `_phase80_item_from_metrics`'s body into a `_metrics_to_display_fields(pe) -> tuple[...]` helper in `stats_service.py` and import it here, or (b) call `_phase80_item_from_metrics` directly from this service and pluck its fields. (a) is preferred — make `_phase80_item_from_metrics` use the new shared helper internally so both call sites share one implementation. **Pick the approach that adds the fewest lines.**
       - Set `eval_baseline_pawns_white` / `eval_baseline_pawns_black` on the response payload.

    4. **Update `tests/services/test_opening_insights_service.py`**: add coverage for (a) the new fields default to None / 0 / "low" when no MG-entry metrics exist, and (b) the new fields carry through when metrics are injected/mocked. Also assert the response carries the two baseline fields. **Look first** at how existing tests mock the repository layer — follow that pattern. If existing tests don't mock the helper at all (because they don't exercise the new path), add a focused unit test that does.

    5. **Tests for `tests/repositories/test_stats_repository.py`**: only add a test if the existing `query_opening_phase_entry_metrics_batch` coverage doesn't already exercise the `hash_column="full"` path with arbitrary (non-opening-table) hashes. Most likely no change needed — confirm and skip if so.

    Per CLAUDE.md: type annotations explicit, `Literal[...]` for the eval_confidence field, `Sequence[str]` if accepting filters that may be `list[Literal[...]]`. Pass `uv run ty check app/ tests/`. Use `set_context` not f-strings if you need to capture exceptions.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_opening_insights_service.py tests/repositories/test_stats_repository.py -x && uv run ty check app/ tests/ && uv run ruff check .</automated>
  </verify>
  <done>
    Schema validates the new fields, service populates them via the shared helper (no duplicated SQL/math), response carries baselines, all backend tests pass, ty + ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend — refactor OpeningFindingCard to match OpeningStatsCard layout</name>
  <files>
    frontend/src/types/insights.ts,
    frontend/src/hooks/useOpeningInsights.ts,
    frontend/src/components/insights/OpeningFindingCard.tsx,
    frontend/src/components/insights/OpeningFindingCard.test.tsx
  </files>
  <behavior>
    - The TS `OpeningInsightFinding` mirrors the new backend fields (six eval_* fields, all optional / defaulted exactly like `OpeningWDL`).
    - `OpeningInsightsResponse` carries `eval_baseline_pawns_white: number` and `eval_baseline_pawns_black: number`.
    - `useOpeningInsights` returns the new fields untouched (no transformation needed — TanStack Query just deserializes).
    - `OpeningFindingCard`:
      - Border-left color stays `scoreZoneColor(finding.score)`. On-board arrow stays the same color. **No change to score-zone tinting** for these two surfaces.
      - Prose abbreviates to: `Score <X>% after <candidate_move>`. The `<X>%` keeps `style={{ color: borderLeftColor }}` (score-zone tinted). No "You score" / "as <Color>" text. Same `wouldContradict` decimal-fallback rule for the percent.
      - Replaces the entire `bulletLine` (MiniBulletChart + ScoreConfidencePopover for the score) with a `WDLChartRow` rendering wins/draws/losses/n_games (compute pcts inline, guard div-by-zero) — copy `OpeningStatsCard.tsx` lines 109-125 pattern.
      - Adds a NEW eval line below the WDL row, structurally identical to `OpeningStatsCard.tsx` lines 127-152: signed pawns text via `formatSignedEvalPawns`, `MiniBulletChart` anchored on 0cp with `EVAL_NEUTRAL_MIN_PAWNS / EVAL_NEUTRAL_MAX_PAWNS / EVAL_BULLET_DOMAIN_PAWNS`, `tickPawns={evalBaselinePawns}` (per-color), `BulletConfidencePopover`. Render an em-dash placeholder when `eval_n === 0` or `avg_eval_pawns == null` (same `hasMgEval` guard as Stats).
      - The eval baseline tick uses `evalBaselinePawns` derived from the finding's color: pass it down as a prop to the card from the parent (the parent already has the response object).
      - Final stack in BOTH mobile and desktop layouts: `header → prose → WDLChartRow → eval bullet row → linksRow`. Same testid prefixes (`opening-finding-card-${idx}-...`). Tests rely on these.
      - Drops the legacy `opening-finding-card-${idx}-score-bullet` and `opening-finding-card-${idx}-confidence-info` testids (replaced by `-wdl` and `-bullet-popover` respectively).
      - Imports of `MiniBulletChart`, `ScoreConfidencePopover`, `SCORE_BULLET_*` from `@/lib/scoreBulletConfig` are removed (still needed: `scoreZoneColor` for border + arrow). `MiniBulletChart` is re-imported for the eval bullet — keep it.
      - All new interactive elements carry `data-testid` per CLAUDE.md frontend rules (the eval line container reuses the OpeningStatsCard-style testids: `${cardTestId}-bullet` and `${cardTestId}-eval-text` and `${cardTestId}-bullet-popover`).
    - Existing `OpeningFindingCard.test.tsx` is updated to match the new layout: legacy score-bullet assertions are removed, prose assertions now expect `Score X%` / `after Move` (no "as Color"), and new assertions confirm WDLChartRow + eval bullet are present. Add tests for the eval bullet (renders when `eval_n > 0`, falls back to `—` when `eval_n === 0`).

    **Find the parent component** (call site of `OpeningFindingCard`) — likely `frontend/src/components/insights/OpeningInsightsBlock.tsx` or similar. Pass `evalBaselinePawns={response.eval_baseline_pawns_white}` for white findings, black baseline for black findings. **Read that parent first** to confirm the prop wiring.
  </behavior>
  <action>
    1. **Update `frontend/src/types/insights.ts`**: add the six eval fields to `OpeningInsightFinding` (mirror `OpeningWDL` in `frontend/src/types/stats.ts:46-53` exactly). Add `eval_baseline_pawns_white: number;` `eval_baseline_pawns_black: number;` to `OpeningInsightsResponse`.

    2. **No logic change in `useOpeningInsights.ts`** beyond the type flowing through — verify the file still compiles with the updated response type. (Likely no code edits.)

    3. **Refactor `OpeningFindingCard.tsx`** following the behavior block above. Add `evalBaselinePawns: number` to `OpeningFindingCardProps`. Apply the layout change identically to BOTH the mobile (`flex-col gap-2 sm:hidden`) and desktop (`hidden sm:flex`) branches — CLAUDE.md "Always apply changes to mobile too" enforced. The troll watermark, on-board arrow, and `cardStyle` (border + opacity) logic stay unchanged.

    4. **Wire `evalBaselinePawns` from the parent** of `OpeningFindingCard`: `grep -rn "OpeningFindingCard" frontend/src` to find the call site(s). At each call site, derive the baseline from the response: `finding.color === 'white' ? response.eval_baseline_pawns_white : response.eval_baseline_pawns_black` and pass as the prop. Update the parent file accordingly.

    5. **Update `OpeningFindingCard.test.tsx`**:
       - Update `renderCard` helper to accept and pass `evalBaselinePawns` (default 0 or 0.25).
       - Update `makeFinding` to include the new fields with sensible defaults (e.g. `avg_eval_pawns: 0.5, eval_n: 18, eval_ci_low_pawns: 0.2, eval_ci_high_pawns: 0.8, eval_p_value: 0.05, eval_confidence: 'medium'`).
       - Replace the "renders 'You score X% as Black/White' prose" tests with "renders 'Score X% after <move>' prose" (drop the "as Color" assertion).
       - Replace the "Score bullet + confidence-info popover (260505 refactor)" describe block: drop `opening-finding-card-{idx}-score-bullet` / `-confidence-info` lookups, add (a) WDLChartRow presence (`getByTestId('opening-finding-card-{idx}-wdl')` or whatever WDLChartRow uses internally — check `WDLChartRow.tsx` for its data-testid hook), (b) eval bullet container present when `eval_n > 0`, (c) em-dash fallback when `eval_n === 0`, (d) `BulletConfidencePopover` trigger present when `eval_n > 0`.
       - Keep all border-color tests (still drive off `scoreZoneColor`).
       - Keep all troll-watermark and on-board-arrow tests untouched (their behavior didn't change).
       - Keep the unreliable-opacity tests.
    6. Verify no other consumer of `OpeningInsightFinding` (e.g. `frontend/src/components/insights/OpeningInsightsBlock.tsx`) broke from the type addition. (Optional fields → existing callers unaffected.)
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run OpeningFindingCard</automated>
  </verify>
  <done>
    Card renders new layout (WDL row + eval bullet) in mobile + desktop, prose abbreviated, border + arrow still score-zone tinted, parent wires per-color baseline, all FE tests pass, lint clean. `npm run knip` clean (no orphaned imports of `ScoreConfidencePopover` / `SCORE_BULLET_*` if those are now unused — remove them or confirm they have other consumers).
  </done>
</task>

<task type="auto">
  <name>Task 3: Final integration — full test suite + lint + typecheck</name>
  <files>(none — verification only)</files>
  <action>
    Run the full quality gates end-to-end. Fix any issues that surface (likely small: a missed type field, a forgotten import, a dead export caught by knip).

    Order matters — backend first, frontend second:
    1. `uv run ruff format .` (auto-format any new code)
    2. `uv run ruff check .`
    3. `uv run ty check app/ tests/` — must be ZERO errors per CLAUDE.md
    4. `uv run pytest` (full backend suite, not just the touched files — catches downstream breakage)
    5. `cd frontend && npm run lint`
    6. `cd frontend && npm test` (full FE suite)
    7. `cd frontend && npm run knip` — required CI gate per CLAUDE.md frontend rules. Remove any orphaned exports (likely `ScoreConfidencePopover` if Insights was its only consumer — confirm with `grep -rn "ScoreConfidencePopover" frontend/src`).
    8. `cd frontend && npm run build` (smoke — catches type errors that lint may miss).

    Working on `main` per the constraint: do NOT commit (the orchestrator handles the final docs commit). If any per-task atomic commits were already made by Task 1 / Task 2, leave them. If the working tree is dirty at the end, leave it dirty for the orchestrator.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ty check app/ tests/ && uv run pytest -q && cd frontend && npm run lint && npm test -- --run && npm run knip && npm run build</automated>
  </verify>
  <done>
    All seven gates green. Working tree contains only the intended changes per the file allowlist in `files_modified` (plus possibly a `frontend/...Block.tsx` parent for prop wiring, which we discovered during Task 2).
  </done>
</task>

</tasks>

<verification>
- Backend `OpeningInsightFinding` carries the six eval_* fields (defaulted) and round-trips through Pydantic.
- `OpeningInsightsResponse` carries `eval_baseline_pawns_white` and `eval_baseline_pawns_black`.
- `query_opening_phase_entry_metrics_batch` is the SOLE source of MG-entry eval aggregation for findings (grep confirms no duplicated SQL / no parallel `_compute_*` math).
- `OpeningFindingCard.tsx`: legacy `score-bullet` testid grep returns 0 hits in the component file.
- `OpeningFindingCard.tsx`: imports `WDLChartRow`, `BulletConfidencePopover`, `formatSignedEvalPawns`, `EVAL_*` from `openingStatsZones`.
- Mobile and desktop layout branches BOTH render the same `header → prose → WDL → eval bullet → links` stack (visual diff in browser optional but recommended).
- All quality gates pass per Task 3.
</verification>

<success_criteria>
- `uv run pytest` — green.
- `uv run ty check app/ tests/` — zero errors.
- `uv run ruff check .` — green.
- `cd frontend && npm run lint` — green.
- `cd frontend && npm test` — green.
- `cd frontend && npm run knip` — green.
- `cd frontend && npm run build` — green.
- `OpeningStatsCard.tsx` is unchanged (`git diff frontend/src/components/stats/OpeningStatsCard.tsx` empty).
- Manual visual sanity (optional, by user): Insights card now mirrors Stats card layout in browser.
</success_criteria>

<output>
After completion, no SUMMARY.md is required for quick tasks. The orchestrator handles the final docs/commit step. Leave the working tree with the changes staged or unstaged per the orchestrator's convention.
</output>
