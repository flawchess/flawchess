---
quick_id: 260509-gm9
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/opening_insights_service.py
  - app/services/import_service.py
  - frontend/src/pages/Openings.tsx
autonomous: true
must_haves:
  truths:
    - "compute_insights, _flush_batch, OpeningsPage all fall within CLAUDE.md size guidelines (logic LOC ≤200, nesting depth ≤4)"
    - "All existing behavior preserved — no API surface changes, no logic changes, no new exports"
    - "All existing tests pass without modification"
    - "Three atomic commits land in order: compute_insights → _flush_batch → OpeningsPage"
  artifacts:
    - path: "app/services/opening_insights_service.py"
      provides: "compute_insights orchestrator + new module-private helpers (_collect_attribution_hashes, _fetch_position_wdl_per_color, _build_sections)"
    - path: "app/services/import_service.py"
      provides: "_flush_batch orchestrator + new module-private helpers (_collect_position_rows, _collect_midgame_eval_targets, _collect_endgame_span_eval_targets, _apply_eval_results)"
    - path: "frontend/src/pages/Openings.tsx"
      provides: "OpeningsPage shell + extracted hooks (useDeepLinkHighlight, useSidebarState, useTabReset) and tab subcomponents (ExplorerTab, StatsTab, GamesTab, InsightsTab — names matched to existing tab structure)"
  key_links:
    - from: "compute_insights"
      to: "_collect_attribution_hashes / _fetch_position_wdl_per_color / _build_sections"
      via: "sequential stage calls"
      pattern: "await _collect_attribution_hashes|await _fetch_position_wdl_per_color|_build_sections"
    - from: "_flush_batch"
      to: "_collect_position_rows / _collect_midgame_eval_targets / _collect_endgame_span_eval_targets / _apply_eval_results"
      via: "sequential stage calls within existing transaction"
      pattern: "_collect_position_rows|_collect_midgame_eval_targets|_collect_endgame_span_eval_targets|_apply_eval_results"
    - from: "OpeningsPage"
      to: "useDeepLinkHighlight / useSidebarState / useTabReset / *Tab"
      via: "hook calls + JSX subcomponent rendering"
      pattern: "useDeepLinkHighlight\\(|useSidebarState\\(|useTabReset\\(|<ExplorerTab|<StatsTab|<GamesTab|<InsightsTab"
---

<objective>
Three behavior-preserving refactors of the top-3 bloated functions identified in the code-quality scan, bringing each within CLAUDE.md's "Keep functions small and shallow" guidelines (nesting depth ≤4, logic LOC ≤200, cognitive complexity ≤15).

Purpose: Reduce maintenance friction in the three highest-traffic services without changing behavior. Each refactor is independently shippable.
Output: One atomic commit per target, in order: easiest → hardest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@app/services/opening_insights_service.py
@app/services/import_service.py
@frontend/src/pages/Openings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor compute_insights (opening_insights_service.py)</name>
  <files>app/services/opening_insights_service.py</files>
  <action>
Behavior-preserving extraction. Read `compute_insights` (line 427) and the existing helpers (`_classify_row` 81, `_attribute_finding` 170, `_dedupe_within_section` 231, `_rank_section` 346, `_apply_eval_metrics_to_finding` 392) before editing.

Introduce three NEW module-private helpers so `compute_insights` reads as a short pipeline of stage calls:

1. `_collect_attribution_hashes(...)` — pass-1 logic that walks rows, calls `_attribute_finding`, and accumulates the set of entry hashes / per-finding state needed for pass-2. Return a TypedDict (or `@dataclass(slots=True)`) holding `{findings_by_section, entry_hashes, ...}`. Match the existing intermediate shape exactly.
2. `_fetch_position_wdl_per_color(...)` — pass-2 parent walk + per-color WDL fetch (the await-heavy block that batches DB queries by entry hash). Returns a `dict[int, PerColorWDL]` (or whatever the current intermediate is). Keep ALL existing query batching + filter passing identical.
3. `_build_sections(...)` — per-row classify (`_classify_row`) → per-section dedupe (`_dedupe_within_section`) → rank (`_rank_section`) → apply eval metrics (`_apply_eval_metrics_to_finding`). Returns the final `dict[section_name, list[OpeningInsightFinding]]`.

After extraction, `compute_insights` body should be 5–6 stage calls + the final response assembly (≤80 logic LOC, depth ≤3).

Hard rules:
- Helpers are module-private (`_` prefix). Do NOT add to `__all__` or any export list.
- NO logic changes. NO threshold changes. NO new branches. Pure mechanical extraction with parameter passing.
- Use `Sequence[...]` for list params per CLAUDE.md ty rules.
- Preserve all `sentry_sdk.set_context` / `set_tag` / `capture_exception` calls at their original logical positions.
- If an intermediate value is reused across stages, pass it as a typed parameter — do not introduce module-level state.
- Type all new helper signatures with explicit return annotations.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ruff check app/services/opening_insights_service.py && uv run ty check app/ tests/ && uv run pytest tests/services/ -x -q</automated>
  </verify>
  <done>
- `compute_insights` body ≤80 logic LOC, max nesting depth ≤3.
- Three new `_` helpers exist; no new public symbols.
- `uv run ruff check .` clean, `uv run ty check app/ tests/` zero errors, `uv run pytest tests/services/` all pass.
- Single atomic commit: `refactor(insights): extract compute_insights into stage helpers`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor _flush_batch (import_service.py)</name>
  <files>app/services/import_service.py</files>
  <action>
Behavior-preserving extraction. Read `_flush_batch` (line 446) and the existing `_board_at_ply` helper (line 39) and `_EvalTarget` dataclass (line 72) before editing.

Split the four mixed concerns into four NEW module-private helpers, called sequentially from `_flush_batch`:

1. `_collect_position_rows(games_batch, ...)` — bulk-insert games (or accept a list of inserted game IDs from the caller, whichever maintains current behavior), then PGN reparse → produce list of position-row dicts ready for `bulk_insert_mappings` / `execute(insert(...))`. Returns the rows + any per-game indexing data needed downstream.
2. `_collect_midgame_eval_targets(position_rows, ...)` — middlegame entry eval target rows (the existing logic that picks `MIN(ply)` per game where `phase == 1`). Returns `list[_EvalTarget]`.
3. `_collect_endgame_span_eval_targets(position_rows, ...)` — per-class endgame-span island detection (`MIN(ply)` per `(game_id, endgame_class)` with `count(ply) ≥ ENDGAME_PLY_THRESHOLD`). Returns `list[_EvalTarget]`.
4. `_apply_eval_results(session, eval_targets, eval_results, ...)` — bulk update at end (the existing `update().where(...).values(eval_cp=..., eval_mate=...)` block, kept transactional inside the caller's session).

After extraction, `_flush_batch` reads as: bulk-insert games → call 1 → call 2 → call 3 → run engine on (2 ∪ 3) → call 4. Target ≤80 logic LOC, depth ≤3.

Hard rules:
- All helpers are module-private (`_` prefix). NO new public surface.
- The session is owned by the caller — helpers accept `session: AsyncSession` and execute within the existing transaction. NO `asyncio.gather` over the same session (CLAUDE.md hard rule).
- Engine pool / Stockfish call site stays in `_flush_batch` — extraction is for the data plumbing only, not the engine call itself.
- Preserve all `sentry_sdk.capture_exception` / `set_context` / `set_tag` calls at their original logical positions; do NOT add new captures inside helpers (let exceptions propagate).
- Preserve `_BATCH_SIZE` and any other module constants — do not inline.
- Use TypedDict / `@dataclass(slots=True)` for intermediate shapes; do not return bare `tuple`s with >2 elements.
- Type all new helper signatures with explicit return annotations; use `Sequence[...]` for list params.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ruff check app/services/import_service.py && uv run ty check app/ tests/ && uv run pytest tests/services/ -x -q</automated>
  </verify>
  <done>
- `_flush_batch` body ≤80 logic LOC, max nesting depth ≤3.
- Four new `_` helpers exist; no new public symbols.
- `uv run ruff check .` clean, `uv run ty check app/ tests/` zero errors, `uv run pytest tests/services/` all pass (including any `test_import*` files).
- Single atomic commit: `refactor(import): split _flush_batch into stage helpers`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor OpeningsPage (frontend/src/pages/Openings.tsx)</name>
  <files>frontend/src/pages/Openings.tsx, frontend/src/pages/openings/useDeepLinkHighlight.ts, frontend/src/pages/openings/useSidebarState.ts, frontend/src/pages/openings/useTabReset.ts, frontend/src/pages/openings/ExplorerTab.tsx, frontend/src/pages/openings/StatsTab.tsx, frontend/src/pages/openings/GamesTab.tsx, frontend/src/pages/openings/InsightsTab.tsx</files>
  <action>
Behavior-preserving extraction of `OpeningsPage` (line 141, ~647 logic LOC, depth ~6, 59 branches). Read the full component first; identify the existing tab structure (the *Tab subcomponent names below are tentative — match what `activeTab` actually switches on. If the existing tab IDs are e.g. "explore" / "stats" / "games" / "insights", name the components to match).

Step A — Extract three custom hooks under `frontend/src/pages/openings/`:

1. `useDeepLinkHighlight(activeTab)` — owns `highlightedMove`, `pulseActive`, `prevTabForHighlight`, and the `useEffect` blocks at lines 184 and 246 (highlight + pulse-timing). Returns `{ highlightedMove, setHighlightedMove, pulseActive }`.
2. `useSidebarState()` — owns `sidebarOpen`, `filterSidebarOpen`, `bookmarkSidebarOpen`, `playedAsHintDismissed`, `filtersHintDismissed`, and any localStorage persistence effects. Returns the open/close state + setters as a single object.
3. `useTabReset(activeTab, deps)` — owns the cross-tab reset effects (`prevTab` tracking, `gamesOffset` reset on tab switch, etc.).

Step B — Split the `return (...)` JSX into per-tab subcomponents under `frontend/src/pages/openings/`:

- `ExplorerTab.tsx`, `StatsTab.tsx`, `GamesTab.tsx`, `InsightsTab.tsx` (rename to match actual tab IDs).
- Each receives the props it needs explicitly — do NOT pass a god-prop "everything bag". Pass filters, board state, callbacks, and query results as named props.
- The shell `OpeningsPage` keeps: routing/tab state, the chessboard column (shared across tabs), the sidebar/drawer chrome, and the conditional render `{activeTab === 'X' && <XTab ... />}`.

Step C — Mobile/desktop parity (CLAUDE.md hard rule):
- The current file has separate desktop sidebar and mobile drawer markup. When extracting, keep BOTH render paths and route them through the same hook state. Search the file for duplicated sidebar/drawer markup before considering the split complete.

Step D — `data-testid` preservation (CLAUDE.md browser-automation rule):
- Every `data-testid` in the original file MUST appear with the same value in the refactored output. After the edit, run a diff of `data-testid` values old vs new (e.g. `git diff HEAD -- frontend/src/pages/Openings.tsx | grep -E '^[-+].*data-testid'`) and confirm any `+`/`-` pairs balance. ARIA labels must also be preserved on icon-only buttons.

Step E — Keep theme constants imported from `frontend/src/lib/theme.ts` per CLAUDE.md. Do not hard-code colors when extracting.

Hard rules:
- NO new routes. Tab structure stays in `OpeningsPage` via the existing `activeTab` mechanism.
- New files live under `frontend/src/pages/openings/` (subdirectory). Do NOT add them to a barrel/index file unless one already exists.
- Knip must remain clean: every new export must be imported by `Openings.tsx`. Do not export anything that isn't consumed.
- Target shell `OpeningsPage` ≤150 logic LOC after the split (JSX tree excluded from logic LOC count per CLAUDE.md).
- NO behavior changes. NO new features. NO query refactors. Pure structural split.
- Preserve all `useCallback` / `useMemo` dependency arrays exactly — moving a value into a hook still requires the same memoization semantics.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run lint && npm test -- --run && npm run build</automated>
  </verify>
  <done>
- `OpeningsPage` shell function ≤150 logic LOC; max nesting depth ≤4.
- Three hooks + four (or however many tabs exist) tab subcomponents created under `frontend/src/pages/openings/`.
- `npm run lint` (includes Knip) clean, `npm test` all pass without modification, `npm run build` succeeds.
- Every original `data-testid` value preserved in the new render tree.
- Mobile drawer + desktop sidebar parity preserved.
- Single atomic commit: `refactor(openings): split OpeningsPage into hooks + tab subcomponents`.
  </done>
</task>

</tasks>

<verification>
After all three tasks land:
- Backend: `uv run ruff check . && uv run ty check app/ tests/ && uv run pytest -x -q`
- Frontend: `cd frontend && npm run lint && npm test -- --run && npm run build`
- `git log --oneline -3` shows three refactor commits in the order: insights → import → openings.
- Spot-check function sizes:
  - `awk '/^async def compute_insights/,/^[a-zA-Z]/{print NR": "$0}' app/services/opening_insights_service.py | wc -l` shows function span shrank substantially (from ~225 lines to ≤90).
  - Same for `_flush_batch` in `app/services/import_service.py` (from ~175 lines to ≤90).
  - `OpeningsPage` shell in `frontend/src/pages/Openings.tsx` shrank from ~1700 to ≤300 lines (JSX shell + hook calls only).
</verification>

<success_criteria>
- Three atomic commits land on the current branch in order: compute_insights → _flush_batch → OpeningsPage.
- All three functions fall within CLAUDE.md size guidelines.
- All existing tests pass without modification (no test edits, no skips).
- ty / ruff / knip all clean; npm build succeeds.
- No new public exports; no API surface changes; no logic changes.
- Mobile/desktop parity preserved in OpeningsPage; every `data-testid` preserved.
</success_criteria>

<output>
After completion, no SUMMARY.md needed for quick mode — the three commit messages document the work.
</output>
