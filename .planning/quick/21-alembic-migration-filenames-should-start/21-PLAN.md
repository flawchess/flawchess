---
phase: quick-21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic.ini
  - alembic/versions/dcef507678d8_initial_schema.py
  - alembic/versions/9e234104d7f2_add_import_jobs_table.py
  - alembic/versions/ea8ca9526dcf_add_users_table.py
  - alembic/versions/d809d42c7521_add_oauth_account_table.py
  - alembic/versions/0b59137a5005_add_is_computer_game_to_games.py
  - alembic/versions/00e469a985ef_add_bookmarks_table.py
  - alembic/versions/f10322cb88b3_add_is_flipped_to_bookmarks.py
  - alembic/versions/7eb7ce83cdb9_rename_bookmarks_to_position_bookmarks.py
  - alembic/versions/f009f3b41e8e_add_move_count_to_games_and_usernames_.py
  - alembic/versions/1c4985e5016a_add_white_black_username_rating_columns.py
  - alembic/versions/697d7b8842d2_drop_redundant_user_relative_columns.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Migration files are sorted chronologically by filename when listed alphabetically"
    - "New migrations auto-generate with UTC timestamp prefix"
    - "Alembic upgrade head still works after rename"
  artifacts:
    - path: "alembic.ini"
      provides: "file_template with UTC timestamp prefix"
      contains: "file_template"
    - path: "alembic/versions/"
      provides: "All 11 migration files with YYYYMMDD_HHMMSS prefix"
  key_links:
    - from: "alembic/versions/*.py"
      to: "alembic_version table"
      via: "revision hashes unchanged"
      pattern: "revision.*=.*'"
---

<objective>
Rename all existing Alembic migration files to start with a UTC timestamp prefix (YYYYMMDD_HHMMSS) and configure the file_template in alembic.ini so future autogenerated migrations follow the same convention. This ensures migrations sort chronologically by filename.

Purpose: Migration files currently sort by random hex hash prefix, making it hard to see chronological order.
Output: All 11 migration files renamed with timestamp prefix, alembic.ini configured for future migrations.
</objective>

<execution_context>
@/home/aimfeld/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimfeld/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@alembic.ini
@alembic/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure file_template and rename existing migrations</name>
  <files>alembic.ini, alembic/versions/*.py</files>
  <action>
1. In `alembic.ini`, uncomment/set the `file_template` line (around line 14) to:
   ```
   file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d%%(second).2d_%%(rev)s_%%(slug)s
   ```
   Also set the `timezone` setting to `utc` so timestamps are UTC:
   ```
   timezone = utc
   ```
   Note: the `tzdata` package may need to be added if not already present. Check with `uv run python -c "import zoneinfo; print(zoneinfo.ZoneInfo('UTC'))"` first -- if it works, no extra dependency needed.

2. Rename all 11 existing migration files using their `Create Date` from the file header. The mapping (derived from each file's Create Date line) is:

   | Old filename | New filename |
   |---|---|
   | `dcef507678d8_initial_schema.py` | `20260311_133123_dcef507678d8_initial_schema.py` |
   | `9e234104d7f2_add_import_jobs_table.py` | `20260311_142537_9e234104d7f2_add_import_jobs_table.py` |
   | `ea8ca9526dcf_add_users_table.py` | `20260312_102146_ea8ca9526dcf_add_users_table.py` |
   | `d809d42c7521_add_oauth_account_table.py` | `20260312_114006_d809d42c7521_add_oauth_account_table.py` |
   | `0b59137a5005_add_is_computer_game_to_games.py` | `20260312_212351_0b59137a5005_add_is_computer_game_to_games.py` |
   | `00e469a985ef_add_bookmarks_table.py` | `20260313_095730_00e469a985ef_add_bookmarks_table.py` |
   | `f10322cb88b3_add_is_flipped_to_bookmarks.py` | `20260313_123605_f10322cb88b3_add_is_flipped_to_bookmarks.py` |
   | `7eb7ce83cdb9_rename_bookmarks_to_position_bookmarks.py` | `20260314_145243_7eb7ce83cdb9_rename_bookmarks_to_position_bookmarks.py` |
   | `f009f3b41e8e_add_move_count_to_games_and_usernames_.py` | `20260314_164203_f009f3b41e8e_add_move_count_to_games_and_usernames_.py` |
   | `1c4985e5016a_add_white_black_username_rating_columns.py` | `20260314_181258_1c4985e5016a_add_white_black_username_rating_columns.py` |
   | `697d7b8842d2_drop_redundant_user_relative_columns.py` | `20260314_194322_697d7b8842d2_drop_redundant_user_relative_columns.py` |

   Use `git mv` for each rename so git tracks the renames properly. Do NOT modify any file contents -- Alembic identifies migrations by the `revision` variable inside the file, not by filename. The revision hashes and down_revision chains remain unchanged.

3. Delete the `__pycache__` directory inside `alembic/versions/` to avoid stale bytecode references.
  </action>
  <verify>
    <automated>ls alembic/versions/*.py | head -20 && uv run alembic heads && uv run alembic history</automated>
  </verify>
  <done>All 11 migration files renamed with YYYYMMDD_HHMMSS prefix. Files sort chronologically by name. alembic.ini has file_template and timezone=utc configured. `alembic heads` shows single head. `alembic history` shows correct chain.</done>
</task>

<task type="auto">
  <name>Task 2: Verify alembic upgrade works and new migration uses timestamp</name>
  <files></files>
  <action>
1. Run `uv run alembic upgrade head` to confirm all migrations still apply correctly (this is a no-op if already at head, but validates the chain is intact after renames).

2. Run `uv run alembic check` to verify no pending model changes.

3. Verify the file_template works for new migrations by running a dry test:
   - Run `uv run alembic revision --autogenerate -m "test_timestamp_format"`
   - Confirm the generated file starts with a UTC timestamp (YYYYMMDD_HHMMSS pattern)
   - Delete the generated test migration file immediately after confirming
   - Run `uv run alembic heads` to confirm head is still the correct last migration (697d7b8842d2)

4. Run `uv run pytest -x` to confirm tests still pass (tests use `alembic upgrade head` in conftest).
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run pytest tests/conftest.py -x --co -q 2>&1 | head -5 && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Alembic upgrade head succeeds. Tests pass. New migration filename format confirmed as YYYYMMDD_HHMMSS_rev_slug.</done>
</task>

</tasks>

<verification>
- `ls alembic/versions/*.py` shows all files sorted chronologically by date prefix
- `uv run alembic history` shows unbroken revision chain
- `uv run alembic heads` shows single head (697d7b8842d2)
- `uv run pytest -x` passes
</verification>

<success_criteria>
- All 11 migration files renamed with YYYYMMDD_HHMMSS UTC timestamp prefix
- Files sort chronologically when listed alphabetically
- alembic.ini file_template configured for future migrations
- Alembic revision chain intact, upgrade head works
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/21-alembic-migration-filenames-should-start/21-SUMMARY.md`
</output>
