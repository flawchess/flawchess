---
phase: quick-260629-n8e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useStockfishEngine.ts
  - frontend/src/hooks/__tests__/useStockfishEngine.test.ts
autonomous: true
requirements: [QUICK-260629-n8e]

must_haves:
  truths:
    - "A settled move (no recent prior position change) starts the engine search immediately — no fixed pre-search delay — so the first line paints near-instantly."
    - "Rapid successive position changes (arrow-key stepping) still coalesce to a single search via a debounce window; no search storm."
    - "Early/shallow info lines (including lowerbound/upperbound) paint to pvLines + evalCp immediately and update in place as depth climbs."
    - "Results from a superseded search (stop-pending / stopping state) are never committed to state — white-POV normalization and stale-search guards still hold."
    - "Frontend lint, unit tests, and tsc -b all pass; existing useStockfishEngine tests are updated for the intentional debounce + bound behavior changes and stay green."
  artifacts:
    - path: "frontend/src/hooks/useStockfishEngine.ts"
      provides: "Adaptive first-paint debounce + relaxed-bound live-streaming snapshot commits with preserved stale-eval guards"
    - path: "frontend/src/hooks/__tests__/useStockfishEngine.test.ts"
      provides: "Updated tests covering immediate first-paint, rapid-step coalescing, and shallow-bound live painting"
  key_links:
    - from: "frontend/src/hooks/useStockfishEngine.ts"
      to: "frontend/src/hooks/uciParser.ts"
      via: "parseInfoLine() bound field consumed without the exact-only gate"
      pattern: "parseInfoLine"
---

<objective>
Make the `/analysis` live Stockfish feel snappy: the first engine line should paint in well under 100ms and sharpen in place (lichess-style), instead of the current ~0.5-1s wait. Two frontend-only changes in `useStockfishEngine.ts` (parser is read-only):

1. Replace the flat 150ms first-paint debounce with an adaptive one — fire immediately on a settled move, only debounce when positions change in rapid succession.
2. Relax the `bound === 'exact'` commit gate so shallow `info` lines (incl. `lowerbound`/`upperbound`) paint immediately and sharpen in place as depth climbs, while preserving the stale-search / stop-pending guards.

Purpose: Eliminate the dead first-paint latency; let the eval bounce briefly then settle (user-accepted, lichess-style).
Output: Updated hook + updated unit tests, fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@frontend/src/hooks/useStockfishEngine.ts
@frontend/src/hooks/uciParser.ts
@frontend/src/hooks/__tests__/useStockfishEngine.test.ts

LOCKED scope (do NOT touch): MultiPV 2; `movetime 1500` / `nodes 2000000` ceiling; warm-worker lifecycle; tab-hide pause. OUT OF SCOPE: switching the Stockfish build, multithreading / pthreads / SharedArrayBuffer, COOP/COEP headers. Parser (`uciParser.ts`) already exposes `bound` and `pv` — no parser change needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adaptive first-paint debounce + relaxed-bound live streaming in useStockfishEngine.ts</name>
  <files>frontend/src/hooks/useStockfishEngine.ts</files>
  <action>
Two edits to the hook, plus a small refactor to avoid duplicating the white-POV commit logic. No magic numbers — extract every new timing value into a named constant (CLAUDE.md).

A. ADAPTIVE DEBOUNCE (replaces the flat `DEBOUNCE_MS = 150`).
Rename the constant to `RAPID_STEP_DEBOUNCE_MS` (keep the value at 150; this still coalesces held arrow-key auto-repeat). Add a `lastFenChangeAtRef = useRef(0)`. In the FEN-change effect (currently `useEffect(... [fen])` at ~line 137): keep the existing immediate clear of `pvLines`/`evalCp`/`evalMate`/`depth` and the `fen === null → setDebouncedFen(null)` branch unchanged. Then compute `const now = Date.now(); const sinceLast = now - lastFenChangeAtRef.current; lastFenChangeAtRef.current = now;`. If `sinceLast > RAPID_STEP_DEBOUNCE_MS` (settled move, or first mount where the ref is 0) call `setDebouncedFen(fen)` synchronously and return — this fires the search near-instantly. Otherwise (rapid succession) keep the existing `setTimeout(() => setDebouncedFen(fen), RAPID_STEP_DEBOUNCE_MS)` + cleanup so rapid stepping coalesces to one search. Firing immediately before engine init is safe: `analyze()` early-returns on `!isReadyRef.current` and the `debouncedFen`/`isReady` effect re-fires once `isReady` flips true. Update the surrounding doc comment to describe the adaptive behavior (drop the stale "all analyses delayed by the debounce" rationale).

B. RELAXED BOUND + LIVE SNAPSHOT COMMIT.
Extract the white-POV commit logic currently inside the non-stale `bestmove` branch (lines ~262-280: `whitePovSign`, `toWhitePov`, the sorted snapshot `setPvLines`, and the `topLine` → `setEvalCp`/`setEvalMate`/`setDepth`) into a local `commitPvSnapshot()` function declared inside the worker effect (closure over the refs + setters). The `bestmove` branch calls `commitPvSnapshot()` then sets `stateRef.current = 'idle'` and `setIsAnalyzing(false)` (keep the stop-pending discard branch exactly as-is — it must NOT commit). In the `info` branch: FIRST guard against superseded searches — `if (stateRef.current !== 'thinking' || stopPendingRef.current) return;` so info lines from a stopping/old search are ignored (this is the stale-eval guard; do not remove it). Then parse, and commit ALL bounds (remove the `parsed.bound === 'exact'` condition — accept `lowerbound`/`upperbound` too), writing into `pvMapRef` as today, and then call `commitPvSnapshot()` so the line paints live and sharpens in place as depth climbs. Update the "Pitfall 5" comment to note the gate is intentionally relaxed for live first-paint (eval may visibly bounce ~200-300ms, accepted).

Keep `analyze()`'s `pvMapRef.current.clear()` + `analyzedSideToMoveRef` assignment unchanged — the cleared map plus the `thinking && !stopPending` guard is what keeps a new search from painting stale lines.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | tail -5 && echo TSC_DONE</automated>
  </verify>
  <done>Hook compiles; `RAPID_STEP_DEBOUNCE_MS` constant present (no bare 150); `bound === 'exact'` gate removed; info-line handler guarded by `stateRef.current !== 'thinking' || stopPendingRef.current` and calls a shared `commitPvSnapshot()`; bestmove stop-pending discard branch unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update useStockfishEngine unit tests for the intentional behavior changes + full frontend verification</name>
  <files>frontend/src/hooks/__tests__/useStockfishEngine.test.ts</files>
  <action>
Update the existing mock-Worker tests to match the two intentional behavior changes. Keep all unrelated tests (Worker instantiation, UCI init, movetime/nodes, stop-pending discard, visibility, unmount) intact.

DEBOUNCE TESTS (currently "does not send go before the debounce delay (150 ms)" and "sends position + go after the debounce delay"):
- Replace the "does not send go before debounce" test with one asserting a SETTLED first move fires immediately: after `driveInit`, advance timers by 0 (or a tick) and assert a `go ` command was sent without waiting 150ms. The hook fires synchronously on mount/settled, so the search starts near-instantly.
- Keep a test proving rapid successive FEN changes coalesce: render with one FEN, drive init, then `rerender` to a second FEN within the rapid window (advance < RAPID_STEP_DEBOUNCE_MS between them) and assert only a single search is ultimately launched for the latest FEN (no storm). Use the existing stop/`stopPendingRef` re-analyze flow as the model; the key assertion is that the engine ends up analyzing the final FEN, not both.
- The "sends position + go after the debounce delay" test still holds (go is present after advancing) — keep or fold into the immediate-fire test; do not assert the OLD "no go before 150ms" behavior anywhere.

BOUND TESTS (currently "lowerbound info line does NOT update evalCp" and "upperbound info line does NOT update evalCp"):
- Invert both: after init + the search has started (stateRef thinking), a `lowerbound` / `upperbound` info line now DOES paint. Assert `result.current.evalCp` becomes the white-POV-normalized value (TEST_FEN is black-to-move, so UCI `score cp N` → evalCp `-N`) WITHOUT a bestmove yet. Rename the test descriptions to reflect live painting.
- Strengthen "exact info line + bestmove commits evalCp": assert evalCp is already set after the exact info line (before the bestmove), then still correct after bestmove with `isAnalyzing` false.

STALE GUARD (existing "stop-pending bestmove is discarded — rapid FEN changes show only final result"): this test must STILL pass unchanged — it proves the relaxed-bound live commits do not leak a superseded search's result (the FEN-change clears state and the stopping-state guard blocks stale info lines). Run it; if the mock sequencing needs a tweak to match the adaptive immediate-fire timing, adjust the timer advances only — do NOT weaken the final assertions (`pvLines` length 0, `evalCp` null).

Then run the full frontend gate and fix anything red.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run src/hooks/__tests__/useStockfishEngine.test.ts && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <done>All useStockfishEngine tests pass; debounce tests assert immediate first-paint + rapid coalescing; lowerbound/upperbound tests assert live painting; the stop-pending stale-discard test still passes with its original final assertions; lint and tsc -b clean.</done>
</task>

</tasks>

<verification>
Full frontend gate per CLAUDE.md (lint+test do NOT type-check — tsc -b is mandatory):

```
cd frontend && npm run lint && npm test -- --run && npx tsc -b
```

Manual smoke (optional, HUMAN-UAT): open `/analysis`, step a move — the first engine line should appear in well under 100ms and the eval should sharpen in place; holding the arrow key to step rapidly should not launch a visible storm of searches.
</verification>

<success_criteria>
- First search fires near-instantly on a settled move (no fixed pre-search delay).
- Rapid stepping coalesces to one search (no storm).
- Shallow info lines (incl. lowerbound/upperbound) paint immediately and sharpen in place.
- Superseded-search results never reach state (stale guards + white-POV normalization intact).
- `npm run lint`, `npm test -- --run`, and `npx tsc -b` all pass.
</success_criteria>

<output>
Create `.planning/quick/260629-n8e-make-analysis-page-live-stockfish-feel-s/260629-n8e-SUMMARY.md` when done
</output>
