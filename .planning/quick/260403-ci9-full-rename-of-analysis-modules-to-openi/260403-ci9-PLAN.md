---
phase: quick
plan: 260403-ci9
type: execute
wave: 1
depends_on: []
files_modified:
  # Backend renames
  - app/routers/analysis.py -> app/routers/openings.py
  - app/services/analysis_service.py -> app/services/openings_service.py
  - app/repositories/analysis_repository.py -> app/repositories/openings_repository.py
  - app/schemas/analysis.py -> app/schemas/openings.py
  - app/main.py
  - app/services/stats_service.py
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - app/routers/endgames.py
  # Test renames
  - tests/test_analysis_repository.py -> tests/test_openings_repository.py
  - tests/test_analysis_service.py -> tests/test_openings_service.py
  - tests/test_auth.py
  # Frontend renames
  - frontend/src/hooks/useAnalysis.ts -> frontend/src/hooks/useOpenings.ts
  - frontend/src/types/api.ts
  - frontend/src/pages/Openings.tsx
  - frontend/src/hooks/useChessGame.ts
  - frontend/src/lib/zobrist.ts
autonomous: true
must_haves:
  truths:
    - "All 'analysis' module references (files, imports, types, variables) renamed to 'openings' for consistency"
    - "Engine analysis references (ACPL, accuracy, evals) remain unchanged"
    - "Backend passes ruff, ty, and pytest after rename"
    - "Frontend passes build and lint after rename"
  artifacts:
    - path: "app/routers/openings.py"
      provides: "Renamed router module"
    - path: "app/services/openings_service.py"
      provides: "Renamed service module"
    - path: "app/repositories/openings_repository.py"
      provides: "Renamed repository module"
    - path: "app/schemas/openings.py"
      provides: "Renamed schemas module"
    - path: "frontend/src/hooks/useOpenings.ts"
      provides: "Renamed frontend hook"
  key_links:
    - from: "app/main.py"
      to: "app/routers/openings.py"
      via: "router import"
      pattern: "from app\\.routers import openings"
    - from: "app/services/openings_service.py"
      to: "app/repositories/openings_repository.py"
      via: "service-to-repo import"
    - from: "frontend/src/pages/Openings.tsx"
      to: "frontend/src/hooks/useOpenings.ts"
      via: "hook import"
      pattern: "from.*useOpenings"
---

<objective>
Rename all "analysis" module files, imports, types, variables, and comments to "openings" for naming consistency with the endgames module. The API URL was already renamed in quick/260403-c83 — this completes the internal rename.

Purpose: Eliminate naming inconsistency where URLs say "openings" but code internals say "analysis"
Output: All 7 files renamed, all imports/types/variables/comments updated, all checks green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend file renames and import/comment updates</name>
  <files>
    app/routers/analysis.py -> app/routers/openings.py
    app/services/analysis_service.py -> app/services/openings_service.py
    app/repositories/analysis_repository.py -> app/repositories/openings_repository.py
    app/schemas/analysis.py -> app/schemas/openings.py
    app/main.py
    app/services/stats_service.py
    app/services/endgame_service.py
    app/schemas/endgames.py
    app/routers/endgames.py
  </files>
  <action>
    1. Rename files using git mv (preserves history):
       - `git mv app/routers/analysis.py app/routers/openings.py`
       - `git mv app/services/analysis_service.py app/services/openings_service.py`
       - `git mv app/repositories/analysis_repository.py app/repositories/openings_repository.py`
       - `git mv app/schemas/analysis.py app/schemas/openings.py`

    2. In `app/schemas/openings.py` (after rename):
       - Rename class `AnalysisRequest` to `OpeningsRequest`
       - Rename class `AnalysisResponse` to `OpeningsResponse`

    3. In `app/routers/openings.py` (after rename):
       - Update import: `from app.services.analysis_service` -> `from app.services.openings_service`
       - Update import: `from app.schemas.analysis` -> `from app.schemas.openings`
       - Update all references to `AnalysisRequest` -> `OpeningsRequest`, `AnalysisResponse` -> `OpeningsResponse`
       - Update docstring referencing "analysis_service" to "openings_service"

    4. In `app/services/openings_service.py` (after rename):
       - Update import: `from app.repositories.analysis_repository` -> `from app.repositories.openings_repository`
       - Update module docstring and function docstrings: "analysis" -> "openings" (where referring to this module, NOT engine analysis)

    5. In `app/repositories/openings_repository.py` (after rename):
       - Update module docstring: "analysis" -> "openings" (where referring to this module)

    6. In `app/main.py`:
       - `from app.routers import analysis` -> `from app.routers import openings`
       - `analysis.router` -> `openings.router`

    7. In `app/services/stats_service.py`:
       - `from app.services.analysis_service import recency_cutoff` -> `from app.services.openings_service import recency_cutoff`

    8. In `app/services/endgame_service.py`:
       - Update imports from `analysis_service` to `openings_service`
       - Update imports from `schemas.analysis` to `schemas.openings`
       - Update `AnalysisRequest` -> `OpeningsRequest` references
       - Update comments referencing "analysis_service" pattern to "openings_service"

    9. In `app/schemas/endgames.py`:
       - `from app.schemas.analysis import GameRecord` -> `from app.schemas.openings import GameRecord`

    10. In `app/routers/endgames.py`:
        - Update comment referencing "analysis endpoints" to "openings endpoints"

    DO NOT touch any "engine analysis" references (ACPL, accuracy, evals) in normalization.py, lichess_client.py, import_service.py, game.py, game_position.py, normalization schemas, alembic migrations, or test_normalization.py.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ruff check . && uv run ty check app/ tests/</automated>
  </verify>
  <done>All 4 backend Python files renamed, all imports resolve, schema classes renamed, comments updated, ruff and ty pass clean</done>
</task>

<task type="auto">
  <name>Task 2: Frontend renames and test file renames</name>
  <files>
    frontend/src/hooks/useAnalysis.ts -> frontend/src/hooks/useOpenings.ts
    frontend/src/types/api.ts
    frontend/src/pages/Openings.tsx
    frontend/src/hooks/useChessGame.ts
    frontend/src/lib/zobrist.ts
    tests/test_analysis_repository.py -> tests/test_openings_repository.py
    tests/test_analysis_service.py -> tests/test_openings_service.py
    tests/test_auth.py
  </files>
  <action>
    **Frontend:**

    1. Rename hook file:
       - `git mv frontend/src/hooks/useAnalysis.ts frontend/src/hooks/useOpenings.ts`

    2. In `frontend/src/hooks/useOpenings.ts` (after rename):
       - Rename exported function `usePositionAnalysisQuery` -> `useOpeningsPositionQuery`
       - Rename query key `positionAnalysis` -> `openingsPosition`
       - Update `AnalysisResponse` type reference -> `OpeningsResponse` (if used in this file)

    3. In `frontend/src/types/api.ts`:
       - Rename type `AnalysisResponse` -> `OpeningsResponse`
       - Update the section comment "Analysis" to "Openings"

    4. In `frontend/src/pages/Openings.tsx`:
       - Update import: `from '../hooks/useAnalysis'` -> `from '../hooks/useOpenings'`
       - Update `usePositionAnalysisQuery` -> `useOpeningsPositionQuery`

    5. In `frontend/src/hooks/useChessGame.ts`:
       - Rename function `getHashForAnalysis` -> `getHashForOpenings`
       - Update JSDoc comment referencing this function
       - Update return value in hook (where it exposes this function)

    6. In `frontend/src/lib/zobrist.ts`:
       - Update the reference to `AnalysisRequest` in comment to `OpeningsRequest`
       - Keep "analysis API" as-is (user-facing concept)

    DO NOT rename user-facing text like "analysis tool", "opening analysis", "endgame analysis" in Home.tsx, Privacy.tsx, Endgames.tsx.

    **Backend test files:**

    7. Rename test files:
       - `git mv tests/test_analysis_repository.py tests/test_openings_repository.py`
       - `git mv tests/test_analysis_service.py tests/test_openings_service.py`

    8. In `tests/test_openings_repository.py` (after rename):
       - Update imports from `repositories.analysis_repository` -> `repositories.openings_repository`
       - Update imports from `schemas.analysis` -> `schemas.openings`
       - Update `AnalysisRequest` -> `OpeningsRequest` references

    9. In `tests/test_openings_service.py` (after rename):
       - Update imports from `services.analysis_service` -> `services.openings_service`
       - Update imports from `schemas.analysis` -> `schemas.openings`
       - Update `AnalysisRequest` -> `OpeningsRequest`, `AnalysisResponse` -> `OpeningsResponse` references

    10. In `tests/test_auth.py`:
        - Update imports: `from app.schemas.analysis` -> `from app.schemas.openings`
        - Update imports: `from app.services.analysis_service` -> `from app.services.openings_service`
        - Rename test method names containing "analysis" to "openings" (e.g. `test_analysis_endpoint` -> `test_openings_endpoint`)
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && uv run ruff check . && uv run ty check app/ tests/ && uv run pytest -x && cd frontend && npm run build && npm run lint</automated>
  </verify>
  <done>Frontend hook renamed, types updated, all imports resolve. Test files renamed with updated imports. Full backend (ruff, ty, pytest) and frontend (build, lint) checks pass green.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification suite:
- `uv run ruff check .` — no lint errors
- `uv run ty check app/ tests/` — no type errors
- `uv run pytest` — all tests pass
- `cd frontend && npm run build` — clean build
- `cd frontend && npm run lint` — no lint errors
- Grep for stale references: `grep -r "analysis_service\|analysis_repository\|schemas.analysis\|routers.analysis\|AnalysisRequest\|AnalysisResponse\|useAnalysis\|positionAnalysis\|getHashForAnalysis\|usePositionAnalysisQuery" app/ tests/ frontend/src/ --include="*.py" --include="*.ts" --include="*.tsx"` should return zero hits (except engine-analysis contexts in excluded files)
</verification>

<success_criteria>
- All 7 files renamed via git mv (history preserved)
- Zero grep hits for old names in app/, tests/, frontend/src/ (excluding engine-analysis contexts)
- `uv run ruff check .` passes
- `uv run ty check app/ tests/` passes with zero errors
- `uv run pytest` all tests pass
- `npm run build` succeeds
- `npm run lint` passes
</success_criteria>

<output>
After completion, create `.planning/quick/260403-ci9-full-rename-of-analysis-modules-to-openi/260403-ci9-SUMMARY.md`
</output>
