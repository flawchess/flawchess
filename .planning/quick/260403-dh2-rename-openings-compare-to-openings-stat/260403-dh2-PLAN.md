---
phase: quick
plan: 260403-dh2
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Openings.tsx
  - frontend/src/pages/Endgames.tsx
autonomous: true
requirements: []
---

<objective>
Rename URL tab segments for consistency: `/openings/compare` becomes `/openings/stats` and `/endgames/statistics` becomes `/endgames/stats`. Both tabs display "Statistics" in the UI — this aligns the URL slugs to match each other and be shorter.

Purpose: Consistent, shorter URL slugs across both pages (`/stats` instead of `/compare` or `/statistics`).
Output: Updated route paths with backward-compatible redirects for old URLs.
</objective>

<context>
@frontend/src/pages/Openings.tsx
@frontend/src/pages/Endgames.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename tab value and URL slug in Openings.tsx</name>
  <files>frontend/src/pages/Openings.tsx</files>
  <action>
In `frontend/src/pages/Openings.tsx`:

1. **Update activeTab derivation** (around line 74-78): Change the check from `location.pathname.includes('/compare')` to `location.pathname.includes('/stats')`. Keep the ternary structure: games -> 'games', stats -> 'stats', else 'explorer'.

2. **Update the existing redirect logic** (around line 71-72): The current code redirects `/openings/statistics` to `/openings/compare`. Update to redirect BOTH old URLs (`/statistics` AND `/compare`) to `/openings/stats`:
   ```typescript
   const needsLegacyRedirect = location.pathname.endsWith('/statistics') || location.pathname.endsWith('/compare');
   ```
   Remove the separate `needsStatisticsRedirect` variable and its comment. Rename `needsStatisticsRedirect` to `needsLegacyRedirect` (or similar).

3. **Update the redirect Navigate** (around line 786-788): Change `<Navigate to="/openings/compare" replace />` to `<Navigate to="/openings/stats" replace />`. Merge the two redirect blocks into one using the new combined variable.

4. **Update all TabsTrigger and TabsContent `value` props** from `"compare"` to `"stats"` — both desktop (around lines 807, 818) and mobile (around lines 1046, 1057) sections.

5. **Update data-testid attributes**: Change `data-testid="tab-compare"` to `data-testid="tab-stats"` and `data-testid="tab-compare-mobile"` to `data-testid="tab-stats-mobile"`.

The visible tab label stays "Statistics" (no change to displayed text).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && npm run build --prefix frontend && npm test --prefix frontend</automated>
  </verify>
  <done>All occurrences of "compare" as tab value/URL slug replaced with "stats" in Openings.tsx. Old URLs `/openings/compare` and `/openings/statistics` both redirect to `/openings/stats`. Build and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rename tab value and URL slug in Endgames.tsx</name>
  <files>frontend/src/pages/Endgames.tsx</files>
  <action>
In `frontend/src/pages/Endgames.tsx`:

1. **Update activeTab derivation** (line 47): Change `'statistics'` to `'stats'` in the ternary — `location.pathname.includes('/games') ? 'games' : 'stats'`.

2. **Add a legacy redirect** for the old URL: Add a `needsLegacyRedirect` check for `/endgames/statistics`:
   ```typescript
   const needsLegacyRedirect = location.pathname.endsWith('/statistics');
   ```
   Add a redirect block before the main render:
   ```typescript
   if (needsLegacyRedirect) {
     return <Navigate to="/endgames/stats" replace />;
   }
   ```

3. **Update the default redirect** (line 281): Change `<Navigate to="/endgames/statistics" replace />` to `<Navigate to="/endgames/stats" replace />`.

4. **Update all TabsTrigger and TabsContent `value` props** from `"statistics"` to `"stats"` — both desktop (around lines 298, 313) and mobile (around lines 351, 367) sections.

5. **Update data-testid attributes**: Change `data-testid="tab-statistics"` to `data-testid="tab-stats"` and `data-testid="tab-statistics-mobile"` to `data-testid="tab-stats-mobile"`.

The visible tab label stays "Statistics" (no change to displayed text).
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess && npm run build --prefix frontend && npm test --prefix frontend</automated>
  </verify>
  <done>All occurrences of "statistics" as tab value/URL slug replaced with "stats" in Endgames.tsx. Old URL `/endgames/statistics` redirects to `/endgames/stats`. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build --prefix frontend` succeeds with no errors
- `npm test --prefix frontend` passes
- `npm run lint --prefix frontend` passes
- Grep confirms no remaining `value="compare"` or `value="statistics"` in either file
- Grep confirms redirect coverage for old URLs (`/compare`, `/statistics`)
</verification>

<success_criteria>
- `/openings/stats` is the new URL for the openings statistics tab (was `/openings/compare`)
- `/endgames/stats` is the new URL for the endgames statistics tab (was `/endgames/statistics`)
- Old URLs `/openings/compare`, `/openings/statistics`, and `/endgames/statistics` all redirect to their new `/stats` equivalents
- Tab labels remain "Statistics" in the UI (no visible change)
- data-testid attributes updated from `tab-compare`/`tab-statistics` to `tab-stats`
- Build, tests, and lint all pass
</success_criteria>
