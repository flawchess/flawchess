---
phase: quick
plan: 260325-t74
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Import.tsx
  - frontend/src/components/import/ImportModal.tsx
autonomous: true
requirements: [improve-import-ux]
must_haves:
  truths:
    - "Chess.com and lichess platform icons are visible next to each platform label on the Import page and ImportModal"
    - "'Logged in as' text is removed from the Import page"
    - "Explanation text about initial sync vs resync and opponent scouting workflow is visible on the Import page"
  artifacts:
    - path: "frontend/src/pages/Import.tsx"
      provides: "Import page with icons, explanation text, no 'logged in as'"
    - path: "frontend/src/components/import/ImportModal.tsx"
      provides: "Import modal with platform icons next to labels"
  key_links:
    - from: "frontend/src/pages/Import.tsx"
      to: "frontend/src/components/icons/PlatformIcon.tsx"
      via: "import PlatformIcon"
      pattern: "PlatformIcon.*platform"
---

<objective>
Improve the Import page UX for new users by adding platform icons, removing the "Logged in as" text, and adding helpful explanation text about sync behavior and opponent scouting workflow.

Purpose: New users landing on the Import page need visual clarity (platform icons) and guidance (what sync does, how to scout opponents).
Output: Updated Import.tsx and ImportModal.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/Import.tsx
@frontend/src/components/import/ImportModal.tsx
@frontend/src/components/icons/PlatformIcon.tsx
</context>

<interfaces>
<!-- Existing PlatformIcon component to reuse -->
From frontend/src/components/icons/PlatformIcon.tsx:
```typescript
export function PlatformIcon({ platform, className = 'h-4 w-4' }: PlatformIconProps)
// platform: 'chess.com' | 'lichess'
// Renders chess.com green pawn SVG or lichess horse SVG
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Import page — add platform icons, remove "Logged in as", add explanation text</name>
  <files>frontend/src/pages/Import.tsx</files>
  <action>
    1. Import `PlatformIcon` from `@/components/icons/PlatformIcon`.

    2. Remove the "Logged in as {profile.email}" paragraph (lines 171-175). The user already knows they're logged in — this wastes space and adds no value.

    3. Add `PlatformIcon` next to each platform label. In both the chess.com and lichess platform rows, add `<PlatformIcon platform="chess.com" className="h-4 w-4" />` (and likewise for lichess) immediately before the Label element, inside the `flex items-center gap-2` div that contains the Label and game count.

    4. Add an explanation section after the platform rows (after the closing `</div>` of `space-y-4` but before the import progress section). Use a subtle info box style:

    ```tsx
    <div className="space-y-2 rounded-md border border-border bg-muted/50 px-4 py-3 text-sm text-muted-foreground" data-testid="import-info">
      <p>
        <strong>First sync</strong> imports all your games. Later syncs only fetch new games since the last import.
      </p>
      <p>
        <strong>Opponent scouting:</strong> delete your games, import the opponent's games to analyze their openings, then delete and re-import your own games.
      </p>
    </div>
    ```

    5. Ensure the explanation section renders regardless of profile loading state — place it outside the `profileLoading` conditional, as static content.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Import page shows chess.com/lichess icons next to platform labels, "Logged in as" text removed, info box with sync/scouting explanation visible</done>
</task>

<task type="auto">
  <name>Task 2: Update ImportModal — add platform icons next to labels</name>
  <files>frontend/src/components/import/ImportModal.tsx</files>
  <action>
    1. Import `PlatformIcon` from `@/components/icons/PlatformIcon`.

    2. In the input view (first-time user / edit mode form), add PlatformIcon before each Label:
       - For chess.com: wrap the Label in a flex container with `<PlatformIcon platform="chess.com" className="h-4 w-4" />` and the existing Label. Replace the bare `<Label>` with:
         ```tsx
         <div className="flex items-center gap-2">
           <PlatformIcon platform="chess.com" className="h-4 w-4" />
           <Label htmlFor="chess-com-username">chess.com username</Label>
         </div>
         ```
       - Same pattern for lichess with `platform="lichess"`.

    3. In the sync view (returning user), add PlatformIcon before the platform name text. The chess.com row has `<p className="text-sm font-medium">chess.com</p>` — replace with:
       ```tsx
       <div className="flex items-center gap-1.5">
         <PlatformIcon platform="chess.com" className="h-4 w-4" />
         <p className="text-sm font-medium">chess.com</p>
       </div>
       ```
       Same for the lichess row.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>ImportModal shows chess.com and lichess icons next to platform labels in both input view and sync view</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no type errors
- `cd frontend && npm run lint` — no lint errors
- `cd frontend && npm run build` — production build succeeds
- Visual: Import page shows platform icons, no "Logged in as" text, info box with explanation text
- Visual: ImportModal shows platform icons in both first-time and returning user views
</verification>

<success_criteria>
- Platform icons (chess.com green pawn, lichess horse) appear next to each platform label on Import page and ImportModal
- "Logged in as" email text is gone from the Import page
- Info box explains first sync vs resync behavior and opponent scouting workflow
- TypeScript compiles, lint passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260325-t74-improve-import-page-ux-for-new-users/260325-t74-SUMMARY.md`
</output>
