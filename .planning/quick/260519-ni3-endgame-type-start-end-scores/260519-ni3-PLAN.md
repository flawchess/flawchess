---
phase: quick-260519-ni3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/endgame_service.py
  - app/schemas/endgames.py
  - tests/test_endgame_service.py
  - frontend/src/types/endgames.ts
  - frontend/src/components/charts/EndgameOverallScoreGapRow.tsx
  - frontend/src/components/charts/EndgameTypeCard.tsx
  - frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx
  - frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx
autonomous: false
requirements: [quick-260519-ni3]

must_haves:
  truths:
    - "Each EndgameTypeCard with a Score Gap row shows a left-aligned Start: x% and a right-aligned End: z% flanking the centered Score Gap"
    - "For every endgame type, End mean minus Start mean equals the Score Gap mean exactly (reconciliation invariant), since all three are means over the identical span cohort"
    - "Start and End are hidden (no stray label) when their mean is null or the gap row is hidden"
    - "The three existing non-EndgameTypeCard ScoreGapRow callers render pixel-identical (start/end slots default to nothing)"
  artifacts:
    - path: "app/services/endgame_service.py"
      provides: "es_entry/exit_score components exposed alongside the gap over the same cohort; per-class start/end means"
    - path: "app/schemas/endgames.py"
      provides: "type_achievable_score_start_mean / type_achievable_score_end_mean on EndgameCategoryStats"
    - path: "frontend/src/components/charts/EndgameTypeCard.tsx"
      provides: "3-column Start | Score Gap+info | End label line"
    - path: "frontend/src/components/charts/EndgameOverallScoreGapRow.tsx"
      provides: "optional startSlot/endSlot props defaulting to nothing"
  key_links:
    - from: "_compute_span_gap (or sibling helper)"
      to: "gaps_by_class start/end accumulators"
      via: "single source of truth for es_entry/exit_score; per-bucket path untouched"
      pattern: "es_entry.*exit_score"
    - from: "EndgameTypeCard ScoreGapRow"
      to: "category.type_achievable_score_start_mean / _end_mean"
      via: "startSlot / endSlot props"
      pattern: "startSlot|endSlot"
---

<objective>
Add per-type Start and End predicted scores flanking the existing Score Gap row in the Endgame Type Breakdown cards (`EndgameTypeCard`). The label line becomes a 3-column layout: left `<Cpu> Start: x%`, center `<Cpu> Score Gap: y% <info>` (unchanged), right `<Cpu> End: z%`. The Score Gap MiniBulletChart below the label line is unchanged.

Purpose: surface where each endgame type starts and ends in expected-score terms, so the Score Gap is decomposable and self-verifying (End − Start === Gap).

Output: backend math/schema + tests, frontend types/card/slot-props + tests, blocking visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. Use directly; no exploration needed. -->

app/services/endgame_service.py:266-339 — `_compute_span_gap(entry_eval_cp, entry_eval_mate, next_entry_eval_cp, next_entry_eval_mate, result, user_color) -> float | None`. Returns `exit_score - es_entry`, or `None` when `entry_eval_cp is None and entry_eval_mate is None`. Internally computes `es_entry` (mate precedence over cp) and `exit_score` (transitory next-eval vs terminal `_GAME_RESULT_TO_SCORE[derive_user_result(...)]`).

Call site, endgame_service.py:477-490:
```
gap = _compute_span_gap(entry_eval_cp=eval_cp, entry_eval_mate=eval_mate,
    next_entry_eval_cp=next_entry_eval_cp, next_entry_eval_mate=next_entry_eval_mate,
    result=result, user_color=user_color)
if gap is not None:
    gaps_by_class[endgame_class].append(gap)
    gaps_by_bucket[bucket].append(gap)   # per-bucket path: gap ONLY, must stay untouched
```

Per-class builder, endgame_service.py:588-621. None convention at :599 — `type_gap_mean = type_gap_mean_raw if type_gap_n > 0 else None`. Mirror this exactly for start/end.

app/schemas/endgames.py:62-104 — `EndgameCategoryStats`; gap fields default `None` (e.g. `type_achievable_score_gap_mean: float | None = None`). Add the two new fields the same way.

frontend/src/types/endgames.ts:22-44 — `EndgameCategoryStats` interface; gap fields are `number | null`.

frontend/src/components/charts/EndgameOverallScoreGapRow.tsx — `ScoreGapRow`. Shared by 3 OTHER callers that must stay pixel-identical: `EndgameOverallPerformanceSection.tsx:163,205` and `EndgameMetricCard.tsx:204`. New slot props MUST default to undefined and render nothing when absent.

frontend/src/components/charts/EndgameTypeCard.tsx:119-134 — gap derived vars (`gapMean`, `gapN`, `showGapRow`, `gapFormatted`, `gapColor`, `gapLevel`). `gapFormatted` pattern: `(gapMean >= 0 ? '+' : '') + \`${Math.round(gapMean * 100)}%\``. `ScoreGapRow` block at :393-439. Existing testids: `${tileTestId}-asg-bullet`, `-asg-value`, `-asg-info`. `Cpu` already imported (:26).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — expose start/end components over the identical span cohort + schema + tests</name>
  <files>app/services/endgame_service.py, app/schemas/endgames.py, tests/test_endgame_service.py</files>
  <behavior>
    - For a hand-computed multi-span fixture across 2+ endgame classes: assert `type_achievable_score_end_mean - type_achievable_score_start_mean == type_achievable_score_gap_mean` within float tolerance (1e-9) for EACH class (the reconciliation invariant).
    - `type_achievable_score_start_mean` == mean of `es_entry` over the exact same spans that contribute to `type_achievable_score_gap_n` (NULL-entry-eval spans excluded by the same gate).
    - `type_achievable_score_end_mean` == mean of `exit_score` over that same cohort.
    - When a class has n == 0, both new fields are `None` (mirroring `type_achievable_score_gap_mean` at endgame_service.py:599).
    - The per-bucket `gaps_by_bucket` accumulation and any existing `_compute_span_gap` / `gaps_by_bucket` test stays green and numerically unchanged.
  </behavior>
  <action>
    Decompose `_compute_span_gap` so the `(es_entry, exit_score)` components are exposed without duplicating the entry/exit/mate-precedence/terminal-vs-transitory logic (single source of truth — locked decision 2). Preferred seam: extract a pure helper `_compute_span_scores(...) -> tuple[float, float] | None` returning `(es_entry, exit_score)` (None on the both-NULL gate), and reduce `_compute_span_gap` to `s = _compute_span_scores(...); return None if s is None else s[1] - s[0]`. This keeps `_compute_span_gap`'s existing signature and the per-bucket caller (endgame_service.py:490) working unchanged. At the call site (endgame_service.py:477-490), call the components helper once; on non-None, append the gap to `gaps_by_class`/`gaps_by_bucket` as today AND append `es_entry` / `exit_score` to two new per-class accumulators (e.g. `starts_by_class`, `ends_by_class`, same defaultdict(list) shape as `gaps_by_class`). Per-bucket path appends gap ONLY (do not add start/end there — locked decision 4 / per-bucket only needs the gap). In the per-class builder (endgame_service.py:588-621): compute `start_mean = mean(starts_by_class[endgame_class]) if n > 0 else None` and `end_mean` likewise, using the SAME `type_achievable_score_gap_n` cohort count (the lists have identical length by construction since they are appended together under the same `is not None` guard). Use `statistics.fmean` or sum/len; no new stat math, no sigmoid (Start/End come straight from existing es_entry/exit_score — locked decision 1). Add `type_achievable_score_start_mean` and `type_achievable_score_end_mean` to the `EndgameCategoryStats(...)` constructor call. In app/schemas/endgames.py add `type_achievable_score_start_mean: float | None = None` and `type_achievable_score_end_mean: float | None = None` to `EndgameCategoryStats` (after the gap fields, ~:104), with a docstring/comment noting the reconciliation invariant (`end_mean - start_mean == gap_mean`, same cohort, descriptive components — no separate CI/p-value). No magic numbers; reuse existing helpers. No new `except` blocks expected (no Sentry capture needed). Add the fixture-based reconciliation + null-when-n==0 tests to tests/test_endgame_service.py (extend the existing Score Gap test module, do not create a parallel one). Run `uv run ruff format .`, `uv run ruff check .`, `uv run ty check app/ tests/` (zero errors), `uv run pytest tests/test_endgame_service.py`.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run pytest tests/test_endgame_service.py -x -q</automated>
  </verify>
  <done>New fixture test asserts `end_mean - start_mean == gap_mean` (tol 1e-9) per class and `None` when n==0; ty zero errors; full endgame_service test module green including unchanged per-bucket assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend — slot props on ScoreGapRow + 3-column label line in EndgameTypeCard + tests</name>
  <files>frontend/src/types/endgames.ts, frontend/src/components/charts/EndgameOverallScoreGapRow.tsx, frontend/src/components/charts/EndgameTypeCard.tsx, frontend/src/components/charts/__tests__/EndgameTypeCard.test.tsx, frontend/src/components/charts/__tests__/EndgameOverallScoreGapRow.test.tsx</files>
  <behavior>
    - EndgameTypeCard with non-null start/end means renders `${tileTestId}-asg-start` and `${tileTestId}-asg-end` with whole-percent text (e.g. "Start: 41%", "End: 38%"), unsigned, no zone color, `<Cpu>` icon prefix, `text-muted-foreground`.
    - Existing `${tileTestId}-asg-value` and `${tileTestId}-asg-info` still render unchanged (signed gap value + MetricStatPopover).
    - When `type_achievable_score_start_mean` is null, `-asg-start` is NOT in the DOM; same for `-asg-end`. When `showGapRow` is false, none of the three render.
    - `ScoreGapRow` rendered without the new slot props produces identical output to before (snapshot/structure assertion via the existing EndgameOverallScoreGapRow test).
  </behavior>
  <action>
    In frontend/src/types/endgames.ts add `type_achievable_score_start_mean: number | null;` and `type_achievable_score_end_mean: number | null;` to `EndgameCategoryStats` (after the gap fields, ~:44). In EndgameOverallScoreGapRow.tsx add two optional props `startSlot?: ReactNode` and `endSlot?: ReactNode` (default undefined). Restructure the label line so it is a 3-column row: left = `startSlot`, center = the existing `{label}{value}{tooltip}` group, right = `endSlot`. Use a layout that keeps the center group visually centered and unchanged when both slots are undefined (e.g. a flex row where left/right are equal-flex containers that render nothing when the slot is undefined, or `justify-between` with empty left/right spacers). CRITICAL: when `startSlot`/`endSlot` are undefined the rendered output must be pixel-identical to the current single-line layout for the 3 other callers (`EndgameOverallPerformanceSection.tsx:163,205`, `EndgameMetricCard.tsx:204`) — verify by not changing the default DOM structure of the center group. The MiniBulletChart block below is unchanged. In EndgameTypeCard.tsx (block :393-439): derive `startMean = category.type_achievable_score_start_mean`, `endMean = category.type_achievable_score_end_mean`. Build `startSlot`/`endSlot` ReactNodes only when the respective mean is non-null: each is `<span className="inline-flex items-center gap-1 text-muted-foreground" data-testid={`${tileTestId}-asg-start`}><Cpu className="h-3.5 w-3.5" aria-hidden="true" />Start: {Math.round(startMean * 100)}%</span>` (unsigned — locked decision 3; reuse the existing `Math.round(x*100)%` pattern from `gapFormatted` minus the sign), and the End mirror with `-asg-end`. Pass them as `startSlot`/`endSlot` props to the existing `<ScoreGapRow>`. Render nothing for a side when its mean is null (no stray label). EndgameTypeCard renders once for both desktop and mobile (single component, no separate mobile markup in this card) — confirm by inspecting the component; the change therefore applies to mobile automatically. No hard-coded colors (`text-muted-foreground` is the neutral token, no theme color needed); `text-sm` floor respected (label line is `text-sm`); `data-testid` on the new spans. Update EndgameTypeCard.test.tsx: assert `-asg-start`/`-asg-end` render with correct unsigned percent text when means non-null, are absent when null, and absent when `showGapRow` false; keep existing asg-value/asg-info/last-row assertions green. Add an EndgameOverallScoreGapRow.test.tsx case asserting default (no slots) structure is unchanged and that provided slots render. Run `npm run lint`, `npm run knip`, `npm test -- EndgameTypeCard EndgameOverallScoreGapRow`.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run EndgameTypeCard EndgameOverallScoreGapRow EndgameMetricCard EndgameOverallPerformanceSection</automated>
  </verify>
  <done>EndgameTypeCard shows 3-column Start/Gap/End with correct testids and unsigned percent formatting; Start/End hidden when null/gap-hidden; the 3 other ScoreGapRow callers' tests (EndgameMetricCard, EndgameOverallPerformanceSection) still pass unchanged; lint + knip clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>3-column Start | Score Gap (+info) | End label line in the Endgame Type Breakdown cards, backed by start/end means computed over the same span cohort as the Score Gap (so End − Start === Gap exactly).</what-built>
  <how-to-verify>
    1. Start dev env (`bin/run_local.sh` or backend + `npm run dev`), log in / use a user with imported endgame games.
    2. Open the Endgames page, scroll to the Endgame Type Breakdown cards.
    3. Desktop: for a card with a Score Gap row, confirm the label line reads left `Cpu Start: x%`, center `Cpu Score Gap: ±y% (i)`, right `Cpu End: z%`, with the Score Gap MiniBulletChart unchanged directly below.
    4. Numerically reconcile for one real category: confirm `End% − Start% == Score Gap%` (rounding may make it off-by-1 at the displayed precision; the underlying values reconcile exactly — spot check is fine).
    5. Resize to mobile width (or device toolbar): confirm the 3-column line stays readable and does not overflow (text-sm floor, no truncation of the gap value).
    6. Find/confirm a card type with n==0 or null start/end (or none exists) — Start/End must not show a stray label; the Score Gap row alone is acceptable.
    7. Eyeball one of the OTHER ScoreGapRow consumers (Score Differences card "Achievable Score Gap" / "Endgame Score Gap" rows, and the per-bucket Section 2 cards) — confirm they look exactly as before (no stray empty columns / shifted centering).
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/reconciliation issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB rows → service aggregation | Eval values (cp/mate) and result strings from stored game data; already handled by existing `_compute_span_gap` gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ni3-01 | Information disclosure | New schema fields on `EndgameCategoryStats` | accept | Start/End are derived expected-score aggregates (0-1), no PII, no internal hashes exposed; consistent with existing gap fields. |
| T-ni3-02 | Tampering | Cohort divergence (Start/End computed over a different span set than Gap) | mitigate | Append start/end under the exact same `is not None` guard as the gap; explicit reconciliation test `end_mean - start_mean == gap_mean` (tol 1e-9) per class. |
| T-ni3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added by this plan; no install tasks. |
</threat_model>

<verification>
- `uv run ty check app/ tests/` zero errors; `uv run pytest tests/test_endgame_service.py` green incl. reconciliation + null-when-n==0.
- `cd frontend && npm run lint && npm run knip` clean; frontend tests for EndgameTypeCard, EndgameOverallScoreGapRow, EndgameMetricCard, EndgameOverallPerformanceSection green.
- Human checkpoint confirms 3-column layout (desktop + mobile), numeric reconciliation, null-hide behavior, and pixel-identical other ScoreGapRow consumers.
</verification>

<success_criteria>
- Start/End means computed over the identical span cohort as the type Score Gap; `End − Start === Gap` holds exactly (tested).
- EndgameTypeCard shows the 3-column label line with correct testids and unsigned whole-percent formatting; Start/End hidden when null or gap row hidden.
- `_compute_span_gap` signature and per-bucket `gaps_by_bucket` path unchanged; single source of truth for entry/exit logic.
- The 3 other `ScoreGapRow` consumers render pixel-identical.
- CLAUDE.md honored: ty zero errors, no magic numbers, no hard-coded colors, text-sm floor, data-testid on new elements, mobile covered (single-render card).
</success_criteria>

<output>
Create `.planning/quick/260519-ni3-endgame-type-start-end-scores/260519-ni3-SUMMARY.md` when done.
</output>
