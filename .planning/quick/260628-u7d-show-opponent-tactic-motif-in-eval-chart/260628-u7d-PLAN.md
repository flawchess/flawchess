---
phase: quick-260628-u7d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/library_repository.py
  - app/repositories/query_utils.py
  - app/services/library_service.py
  - tests/services/test_library_service.py
autonomous: true
requirements:
  - QUICK-260628-u7d
must_haves:
  truths:
    - "Hovering an opponent (hollow-square) blunder/mistake marker in the eval chart shows its tactic motif chips (missed:/allowed: + depth), identical rendering to the user's own filled-circle markers."
    - "User-facing severity counts (blunder/mistake/inaccuracy totals) and curated chips are byte-for-byte unchanged — opponent flaw rows never leak into any aggregate."
    - "The Games-tab EXISTS flaw filter and all stats stay player-gated (untouched)."
  artifacts:
    - path: "app/repositories/library_repository.py"
      provides: "fetch_page_game_flaws_both_colors — ungated both-mover read used solely to populate eval-chart tactic tooltips"
    - path: "app/services/library_service.py"
      provides: "_build_card builds tactic_by_ply from the both-color rows while counts/chips stay on the player-gated rows; ply-parity mover perspective for decided-lost"
    - path: "tests/services/test_library_service.py"
      provides: "opponent-tactic-populated test + severity-count landmine guard"
  key_links:
    - from: "app/services/library_service.py::_build_card"
      to: "app/repositories/library_repository.py::fetch_page_game_flaws_both_colors"
      via: "tactic_flaw_rows param feeds the tactic_by_ply loop only"
      pattern: "fetch_page_game_flaws_both_colors"
    - from: "app/services/library_service.py::_build_card"
      to: "app/repositories/query_utils.py"
      via: "mover_is_white derived from ply parity (single-source convention)"
      pattern: "mover_is_white_at_ply"
---

<objective>
In the eval-chart hover tooltip, opponent (hollow-square) flaw markers must display their tactic motif chips (`missed:` / `allowed:` + depth) exactly the way the user's own (filled-circle) markers already do. No POV reframing — the tooltip header already reads "Opponent · Blunder/Mistake", so the tactic info is shown literally.

The eval chart is the single shared component `frontend/src/components/library/EvalChart.tsx`, used by both the games card and the `/analysis` board, so one change covers both surfaces and both breakpoints automatically.

Purpose: opponent tactic data already exists in `game_flaws` (Phase 113 emits FlawRecords for both movers at zero added engine cost). It is withheld purely at READ time by the `player_only_gate` on `fetch_page_game_flaws`, so opponent plies reach the tooltip with empty tactic fields. This closes that one gap WITHOUT letting opponent rows leak into any aggregate.

Output: a separate ungated both-color fetch used solely to populate the per-ply tactic map; counts, curated chips, the Games EXISTS filter, and stats all stay player-gated and untouched.

FRONTEND: zero changes required (verified). `tooltipTactics` (EvalChart.tsx ~782-810) builds from `activeMarker` regardless of `is_user`, and the header (~1052) already renders "Opponent". Once the backend populates the opponent marker's tactic fields, the chips render with no frontend edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@app/repositories/library_repository.py
@app/repositories/query_utils.py
@app/services/library_service.py
@app/schemas/library.py
@tests/services/test_library_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ungated both-color flaw fetch + single-source ply-parity mover helper</name>
  <files>app/repositories/library_repository.py, app/repositories/query_utils.py</files>
  <action>
In `app/repositories/query_utils.py`, add a small Python-side helper `mover_is_white_at_ply(ply: int) -> bool` that returns `ply % 2 == _PLY_EVEN_MOVER_WHITE`. This reuses the existing module-level convention constant (`_PLY_EVEN_MOVER_WHITE = 0`, already the single source for `is_opponent_expr`) so the Python read-path and the SQL path agree on ply parity. No magic number — derive from the existing constant. Add an explicit return type annotation and a one-line docstring noting it mirrors `is_opponent_expr`.

In `app/repositories/library_repository.py`, add a new async function `fetch_page_game_flaws_both_colors(session, user_id, game_ids)` that is identical in shape/return type to the existing `fetch_page_game_flaws` (same `dict[int, list[GameFlaw]]` grouped-by-game_id return, same `GameFlaw.user_id == user_id` IDOR scope, same empty-list defaults) EXCEPT it OMITS the `player_only_gate(...)` clause so it returns BOTH movers' rows. Place it directly beneath `fetch_page_game_flaws`. Add a loud docstring/comment: this both-color variant exists SOLELY to populate eval-chart tactic tooltips for opponent plies; it MUST NOT feed severity counts, curated chips, the Games-tab EXISTS filter, or any stats — those stay on the player-gated `fetch_page_game_flaws`. Keep the `Game` JOIN out if it is no longer needed (the gate was the only reason for it) — verify whether the existing query needs the JOIN for anything other than `player_only_gate`; in the gated version the JOIN brings `Game.user_color` into scope purely for the gate, so the ungated version can drop it.
  </action>
  <verify>
    <automated>uv run ty check app/ && uv run ruff check app/repositories/library_repository.py app/repositories/query_utils.py</automated>
  </verify>
  <done>`mover_is_white_at_ply` exists in query_utils with the constant-derived parity; `fetch_page_game_flaws_both_colors` exists, is user_id-scoped, omits the player gate, returns both-mover rows grouped by game_id, and carries the tooltip-only warning comment. ty clean, ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build tactic_by_ply from both-color rows; keep counts/chips player-gated</name>
  <files>app/services/library_service.py</files>
  <action>
Add a keyword-only parameter `tactic_flaw_rows: list[GameFlaw] | None = None` to `_build_card` (after the existing `flaw_severity`/tactic params). Document it: rows used ONLY to populate the per-ply tactic map for eval-chart tooltips (may include opponent plies); when None, the function falls back to the player-gated `flaw_rows` so any other caller keeps exact existing behavior.

In the `tactic_by_ply` construction loop (~lines 452-511), iterate over `tactic_flaw_rows if tactic_flaw_rows is not None else flaw_rows` instead of `flaw_rows`. Leave `mistake_count`/`blunder_count` (~420-421) and `_curate_chips_from_rows(flaw_rows)` (~433) iterating over the player-gated `flaw_rows` — those MUST stay player-gated (the landmine). Update the now-stale comment at ~448-450 ("mover == user on all rows") to reflect that the tactic loop may now see opponent rows.

Fix the decided-lost mover perspective inside that loop: replace `mover_is_white=(game.user_color == "white")` (~461) with `mover_is_white=mover_is_white_at_ply(fr.ply)` (import the new helper from `app.repositories.query_utils`). This is correct for both player and opponent rows — for player rows it equals the old value (mover == user), and for opponent rows it gives the opponent's true color, so the decided-lost gate is computed against the actual mover. Add a one-line comment explaining the per-row parity derivation.

Wire both production callers to pass the both-color rows:
- Page list path (`get_library_games`, ~lines 692-726): after the existing `page_flaws = ... fetch_page_game_flaws(...)`, add a sequential (no asyncio.gather — same AsyncSession) `page_tactic_flaws = await library_repository.fetch_page_game_flaws_both_colors(session, user_id, page_game_ids)`, and pass `tactic_flaw_rows=page_tactic_flaws.get(game.id, [])` into `_build_card`.
- Single-game path (`build_game_flaw_card`, ~lines 596-620): after the existing `flaw_rows = ...`, add `tactic_flaw_rows = (await library_repository.fetch_page_game_flaws_both_colors(session, user_id, game_ids)).get(game_id, [])` and pass `tactic_flaw_rows=tactic_flaw_rows` into `_build_card`.

Do NOT touch the Games EXISTS filter, `query_flaws`, or any stats path. Keep types tight: `tactic_flaw_rows` is `list[GameFlaw] | None`. ty must stay clean.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run ruff check app/services/library_service.py</automated>
  </verify>
  <done>`_build_card` accepts `tactic_flaw_rows`; the tactic_by_ply loop reads it (falling back to flaw_rows when None) and derives mover_is_white per-ply; counts and chips still read player-gated `flaw_rows`; both production callers fetch and pass the both-color rows sequentially. ty + ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend tests — opponent tactic populated + severity-count landmine guard</name>
  <files>tests/services/test_library_service.py</files>
  <behavior>
    - Opponent B/M ply with a confident tactic motif now carries that motif (and depth) on its `is_user=False` FlawMarker in the card response (via `build_game_flaw_card` or `get_library_games`).
    - User B/M tactic ply still carries its motif on the `is_user=True` marker (no regression).
    - Landmine guard: with both a user blunder and an opponent blunder seeded, `severity_counts.blunder` (and mistake) counts ONLY the user's flaw — opponent rows do not inflate the player-gated totals.
    - Curated chips remain player-only (opponent motif does not appear as a curated chip).
  </behavior>
  <action>
Add a DB-backed test class/functions in `tests/services/test_library_service.py` following the established async `db_session` pattern (see `test_opponent_moves_excluded` ~line 214 for opponent-blunder position seeding, and `_seed_tactic_flaw_for_game` ~line 1179 + the tactic suppression class ~line 1223 for seeding GameFlaw tactic fields and reading markers).

Seed an analyzed game with GamePosition rows producing BOTH a user blunder and an opponent blunder (opposite ply parity), plus `game_flaws` rows carrying confident tactic motifs for both the user ply and the opponent ply. Build the card and assert:
1. The `is_user=False` FlawMarker at the opponent ply has a non-null `allowed_tactic_motif` or `missed_tactic_motif` (and matching depth) — the new behavior.
2. The `is_user=True` FlawMarker at the user ply still has its tactic motif — no regression.
3. `severity_counts.blunder == 1` (only the user blunder counted) — the landmine guard. Opponent rows must NOT inflate counts.
4. The opponent motif does not appear in the card's curated `chips`.

Use named constants for any thresholds/eval values (no magic numbers per CLAUDE.md). Reuse existing seeding helpers rather than duplicating fixture logic.
  </action>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_library_service.py -x</automated>
  </verify>
  <done>New tests pass: opponent tactic motif is present on the hollow-square marker, user marker unchanged, blunder/mistake counts unchanged (opponent rows excluded), opponent motif absent from curated chips.</done>
</task>

</tasks>

<verification>
Run the CLAUDE.md pre-merge gate before squash-merging:
- `uv run ruff format app/ tests/`
- `uv run ruff check app/ tests/ --fix`
- `uv run ty check app/ tests/` (zero errors)
- `uv run pytest -n auto -x` (full backend suite)
- `( cd frontend && npm run lint && npm test -- --run )` — frontend untouched, but the gate confirms no incidental break.

No `npx tsc -b` needed: no shared types changed (FlawMarker schema already carried the tactic fields; only their population changed). No DB migration, no schema change.
</verification>

<success_criteria>
- Opponent hollow-square flaw markers in the eval-chart tooltip show `missed:`/`allowed:` motif chips with depth, on both the games card and `/analysis`, desktop and mobile.
- Player-facing blunder/mistake/inaccuracy counts, curated chips, the Games EXISTS filter, and all stats are unchanged (opponent rows confined to the tactic tooltip map).
- ty clean, ruff clean, full backend suite green, frontend gate green.
</success_criteria>

<output>
Create `.planning/quick/260628-u7d-show-opponent-tactic-motif-in-eval-chart/260628-u7d-SUMMARY.md` when done.
</output>
