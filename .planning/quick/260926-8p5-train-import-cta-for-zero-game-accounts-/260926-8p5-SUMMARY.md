---
quick_id: 260926-8p5
status: complete
commit: 284cbcd42
---

# Quick 260926-8p5 Summary: Train import CTA for zero-game accounts

Executed inline (small frontend change, no subagents).

## Done
- `ImportAskActions` (new): primary "Import games" `Link` to `/library/import`, `btn-import-games-{source}`, `trackEvent('import-cta', { source })` (sources `train-landing`, `train-score`).
- Landing (`TrainStartScreen` `TrainHeader`): registered + no games shows `IMPORT_ASK_LANDING` + the button; the reminder ask is suppressed for that audience. `hasGames` threaded from `Train.tsx`. Applies to the fresh/resume and completed landing states.
- Warm-up score screen: bubble actions = import button for registered no-games; `scoreBubbleCopy` warm-up `no_games` variant returns the opener only (it already says "Import your games...").
- Guests unchanged (sign-up ask). Has-games accounts unchanged.
- CHANGELOG `[Unreleased] / Changed` bullet.

## Verification
- New/updated tests in ImportAskActions, TrainStartScreen, TrainScoreScreen, trainBotCopy tests.
- Mutation check: disabling each of the three new branches fails 4 tests; restored.
- `npm run lint`, `npm run knip`, `npm run build`, `npm test -- --run` (270 files, 4342 tests) all green.
- Not browser-UAT'd; covered by component tests only.

## Follow-up
- Umami `import-cta` event (by source) plus `import_jobs` rows for zero-game Train users tell whether this closes the gap.
