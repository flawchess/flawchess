---
quick_id: 260926-8p5
mode: quick
type: execute
---

# Quick 260926-8p5: Train import CTA for zero-game accounts

**Why:** Prod 2026-09-25: 18 of 81 first-time Train users had zero games (never started an import). ~2/3 of them solved nothing vs ~1/4 of users with games. Train copy says "Import your games" but no surface they can reach has an import button (the `no_material` empty state with the only button is unreachable: sharp fillers always compose a session). 7 of the 18 never passed the landing bubble, which does not mention import at all.

Scope: registered accounts with zero imported games (`!hasGames && !isGuest`). Guests keep their sign-up ask unchanged.

## Task 1: `ImportAskActions` component
- files: `frontend/src/components/train/ImportAskActions.tsx`, test
- action: one primary `Button asChild` wrapping `<Link to="/library/import">` "Import games", `TRAIN_BUTTON_CLASS`, testid `btn-import-games-${source}`, `source: 'train-landing' | 'train-score'`. Attribution via `trackEvent('import-cta', { source })` in onClick (internal Link, so never `data-umami-event`).
- done: renders link to /library/import, fires trackEvent with source.

## Task 2: Landing bubble (TrainStartScreen `TrainHeader`)
- files: `TrainStartScreen.tsx`, `pages/Train.tsx`, `lib/trainBotCopy.ts`, tests
- action: thread `hasGames` from Train.tsx. For `!isGuest && !hasGames`: render `IMPORT_ASK_LANDING` sentence + `ImportAskActions source="train-landing"` in the bubble, and suppress the phone-reminder ask (reminders are pointless before own-game puzzles exist).
- done: zero-game registered landing shows the import sentence + button, no reminder ask; has-games and guest paths unchanged.

## Task 3: Warm-up score screen
- files: `TrainScoreScreen.tsx`, `lib/trainBotCopy.ts`, tests
- action: `!isGuest && !hasGames` → bubble `actions={<ImportAskActions source="train-score" />}`; `scoreBubbleCopy` warm-up variant for audience `no_games` drops the reminder-ask line (line 1 already says "Import your games and your own mistakes take over.").
- done: tests cover the no_games registered warm-up variant; has-games/guest copy unchanged.

Verify: `npm run lint`, `npm test -- --run`, `npm run build`.
