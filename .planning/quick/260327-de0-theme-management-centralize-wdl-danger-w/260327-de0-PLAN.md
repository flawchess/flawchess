---
phase: quick-260327-de0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/components/results/WDLBar.tsx
  - frontend/src/components/charts/WDLBarChart.tsx
  - frontend/src/components/stats/GlobalStatsCharts.tsx
  - frontend/src/components/charts/EndgameWDLChart.tsx
  - frontend/src/components/charts/EndgamePerformanceSection.tsx
  - frontend/src/components/charts/EndgameGauge.tsx
  - frontend/src/components/charts/EndgameConvRecovChart.tsx
  - frontend/src/components/move-explorer/MoveExplorer.tsx
  - CLAUDE.md
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04]
must_haves:
  truths:
    - "WDL colors are defined once in theme.ts and used everywhere"
    - "Gauge zones use named danger/warning/success constants from theme.ts"
    - "Low sample size charts/stats use a centralized muted opacity factor"
    - "CLAUDE.md documents the theme.ts convention"
  artifacts:
    - path: "frontend/src/lib/theme.ts"
      provides: "All color constants: WDL, danger/warning/success, glass overlay, muted factor"
    - path: "CLAUDE.md"
      provides: "Theme convention rule"
  key_links:
    - from: "frontend/src/components/results/WDLBar.tsx"
      to: "frontend/src/lib/theme.ts"
      via: "import WDL_WIN, WDL_DRAW, WDL_LOSS, GLASS_OVERLAY"
      pattern: "import.*from '@/lib/theme'"
    - from: "frontend/src/components/charts/EndgameGauge.tsx"
      to: "frontend/src/lib/theme.ts"
      via: "import GAUGE_DANGER, GAUGE_WARNING, GAUGE_SUCCESS"
      pattern: "import.*from '@/lib/theme'"
---

<objective>
Centralize all theme-relevant color constants in `theme.ts` and replace hard-coded values across the codebase.

Purpose: Single source of truth for WDL colors, gauge zone colors, glass overlay, and muted factor — so future branding changes require one edit.
Output: Updated theme.ts with all constants, all consumers importing from theme.ts, CLAUDE.md rule added.
</objective>

<execution_context>
@.planning/quick/260327-de0-theme-management-centralize-wdl-danger-w/260327-de0-PLAN.md
</execution_context>

<context>
@frontend/src/lib/theme.ts
@frontend/src/components/results/WDLBar.tsx
@frontend/src/components/charts/EndgameGauge.tsx
@frontend/src/components/charts/EndgamePerformanceSection.tsx
@frontend/src/components/charts/EndgameWDLChart.tsx
@frontend/src/components/charts/WDLBarChart.tsx
@frontend/src/components/stats/GlobalStatsCharts.tsx
@frontend/src/components/charts/EndgameConvRecovChart.tsx
@frontend/src/components/move-explorer/MoveExplorer.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add color constants to theme.ts and update all consumers</name>
  <files>
    frontend/src/lib/theme.ts,
    frontend/src/components/results/WDLBar.tsx,
    frontend/src/components/charts/WDLBarChart.tsx,
    frontend/src/components/stats/GlobalStatsCharts.tsx,
    frontend/src/components/charts/EndgameWDLChart.tsx,
    frontend/src/components/charts/EndgamePerformanceSection.tsx,
    frontend/src/components/charts/EndgameGauge.tsx,
    frontend/src/components/charts/EndgameConvRecovChart.tsx,
    frontend/src/components/move-explorer/MoveExplorer.tsx
  </files>
  <action>
**In `frontend/src/lib/theme.ts`, add these constant groups:**

1. **WDL colors** — move from WDLBar.tsx:
   ```ts
   export const WDL_WIN = 'oklch(0.50 0.14 145)';
   export const WDL_DRAW = 'oklch(0.60 0.02 260)';
   export const WDL_LOSS = 'oklch(0.50 0.15 25)';
   ```

2. **Glass overlay** — move from WDLBar.tsx (currently duplicated in WDLBar, EndgameWDLChart, EndgamePerformanceSection):
   ```ts
   export const GLASS_OVERLAY = 'linear-gradient(to bottom, rgba(255,255,255,0.35) 0%, rgba(255,255,255,0.05) 60%, rgba(0,0,0,0.05) 100%)';
   ```

3. **Semantic gauge zone colors** (danger/warning/success):
   ```ts
   export const GAUGE_DANGER = 'oklch(0.55 0.20 25)';   // red zone
   export const GAUGE_WARNING = 'oklch(0.65 0.18 80)';   // amber zone
   export const GAUGE_SUCCESS = 'oklch(0.55 0.17 145)';  // green zone
   ```

4. **Muted factor for unreliable data** — used when sample size is too low:
   ```ts
   export const UNRELIABLE_OPACITY = 0.5;
   ```
   Also export the threshold constant:
   ```ts
   export const MIN_GAMES_FOR_RELIABLE_STATS = 10;
   ```

**Update consumers to import from `@/lib/theme` instead of local definitions:**

- **WDLBar.tsx**: Remove local `WDL_WIN/DRAW/LOSS` and `GLASS_OVERLAY` definitions. Import from `@/lib/theme`. Keep re-exporting WDL colors for backward compat is NOT needed — update all importers directly.

- **MoveExplorer.tsx**: Change `import { WDL_WIN, WDL_DRAW, WDL_LOSS } from '@/components/results/WDLBar'` to `import { WDL_WIN, WDL_DRAW, WDL_LOSS } from '@/lib/theme'`.

- **EndgameWDLChart.tsx**: Remove local `GLASS_OVERLAY` and `MIN_GAMES_FOR_RELIABLE_STATS`. Import `GLASS_OVERLAY`, `WDL_WIN/DRAW/LOSS`, `MIN_GAMES_FOR_RELIABLE_STATS`, `UNRELIABLE_OPACITY` from `@/lib/theme`. Replace the hard-coded `'opacity-50'` class on the WDL bar div (line 101) with inline `style={{ opacity: UNRELIABLE_OPACITY }}` when `cat.total < MIN_GAMES_FOR_RELIABLE_STATS` (keep the cn() call but use the constant). Also apply `UNRELIABLE_OPACITY` to the WDL stats text row below the bar for that category.

- **EndgamePerformanceSection.tsx**: Remove local `GLASS_OVERLAY`. Import from `@/lib/theme`. Change `import { WDL_WIN, WDL_DRAW, WDL_LOSS } from '@/components/results/WDLBar'` to `@/lib/theme`. Update all three gauge zone arrays (CONVERSION_ZONES, RECOVERY_ZONES, ENDGAME_SKILL_ZONES) to use `GAUGE_DANGER`, `GAUGE_WARNING`, `GAUGE_SUCCESS` from theme.

- **EndgameGauge.tsx**: Update `DEFAULT_GAUGE_ZONES` to use `GAUGE_DANGER`, `GAUGE_WARNING`, `GAUGE_SUCCESS` from theme.

- **EndgameConvRecovChart.tsx**: Update chartConfig colors — `conversion_pct` uses `GAUGE_SUCCESS` and `recovery_pct` uses `'oklch(0.55 0.18 260)'` (this blue color is unique to this chart, keep it local but add a comment noting it's intentionally different from WDL_DRAW).

- **WDLBarChart.tsx**: Replace the inline oklch WDL color strings in `chartConfig` with `WDL_WIN`, `WDL_DRAW`, `WDL_LOSS` from `@/lib/theme`.

- **GlobalStatsCharts.tsx**: Replace the inline oklch WDL color strings in `chartConfig` with `WDL_WIN`, `WDL_DRAW`, `WDL_LOSS` from `@/lib/theme`.

Keep the arrow colors in `arrowColor.ts` — those are a different color system (categorical hex, not oklch WDL) and intentionally separate.
  </action>
  <verify>
    <automated>cd /home/aimfeld/Projects/Python/flawchess/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WDL_WIN/DRAW/LOSS defined only in theme.ts, no other file defines them
    - GLASS_OVERLAY defined only in theme.ts, no other file defines it
    - GAUGE_DANGER/WARNING/SUCCESS defined in theme.ts, used in all gauge zone arrays
    - UNRELIABLE_OPACITY and MIN_GAMES_FOR_RELIABLE_STATS in theme.ts, used in EndgameWDLChart
    - No oklch WDL color literals remain outside theme.ts (except arrowColor.ts which uses hex)
    - Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add theme convention rule to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
In CLAUDE.md, under the `## Coding Guidelines` section, add a new bullet:

```
- **Theme constants in theme.ts** — all theme-relevant color constants (WDL colors, gauge zone colors, glass overlays, opacity factors) must be defined in `frontend/src/lib/theme.ts` and imported from there. Never hard-code color values that have semantic meaning (win/loss/draw, danger/warning/success, muted states) directly in components.
```

Place it after the existing "No magic numbers" bullet since it follows the same philosophy.
  </action>
  <verify>
    <automated>grep -c "theme.ts" /home/aimfeld/Projects/Python/flawchess/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md contains a coding guideline about centralizing theme constants in theme.ts</done>
</task>

</tasks>

<verification>
- `grep -rn "oklch(0.50 0.14 145)" frontend/src/ --include="*.ts" --include="*.tsx"` returns ONLY `frontend/src/lib/theme.ts`
- `grep -rn "oklch(0.50 0.15 25)" frontend/src/ --include="*.ts" --include="*.tsx"` returns ONLY `frontend/src/lib/theme.ts`
- `grep -rn "GLASS_OVERLAY" frontend/src/ --include="*.ts" --include="*.tsx"` — only defined in theme.ts, imported elsewhere
- `npm run build` passes
- `npm run lint` passes
</verification>

<success_criteria>
All WDL and gauge zone colors flow from theme.ts. No duplicate color definitions. CLAUDE.md documents the convention. Build and lint pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260327-de0-theme-management-centralize-wdl-danger-w/260327-de0-SUMMARY.md`
</output>
