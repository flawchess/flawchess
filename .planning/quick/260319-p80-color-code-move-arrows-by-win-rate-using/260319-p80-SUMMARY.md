---
phase: quick
plan: 260319-p80
subsystem: ui
tags: [react, chess, oklch, color, visualization]

requires: []
provides:
  - "getArrowColor utility mapping win percentage to oklch green/grey/red gradient"
  - "Board arrows on Dashboard and Openings colored by move win rate"
affects: [dashboard, openings, board-arrows]

tech-stack:
  added: []
  patterns:
    - "Win-rate color utility pattern: shared getArrowColor function used by all board-arrow useMemos"
    - "oklch alpha channel for opacity: oklch(L C H / alpha) instead of hex+alpha"

key-files:
  created:
    - frontend/src/lib/arrowColor.ts
  modified:
    - frontend/src/pages/Openings.tsx
    - frontend/src/pages/Dashboard.tsx

key-decisions:
  - "Color anchors reuse exact oklch values from WDLBar.tsx (WDL_WIN/WDL_DRAW/WDL_LOSS) for visual consistency"
  - "Hovered arrows get +0.15 lightness boost at full opacity instead of a separate highlight color"
  - "Hue interpolation uses direct linear lerp (red H=25 -> grey H=260 -> green H=145) — goes via increasing hue, short arc through the color wheel"

patterns-established:
  - "Arrow color logic extracted to shared utility so Dashboard and Openings stay in sync automatically"

requirements-completed: [quick-260319-p80]

duration: 8min
completed: 2026-03-19
---

# Quick Task 260319-p80: Color-Code Move Arrows by Win Rate Summary

**Board move arrows now use a green-grey-red oklch gradient matching WDL chart colors, with opacity still encoding move frequency.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-19T17:10:00Z
- **Completed:** 2026-03-19T17:18:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Created `getArrowColor(winPct, opacity, isHovered)` utility with smooth oklch interpolation between red (<=33%), grey (50%), and green (>=66%) anchors
- Updated `boardArrows` useMemo in both Dashboard.tsx and Openings.tsx to use win-rate colors
- Hovered arrows now appear as a brighter version of their win-rate color (+0.15 lightness) rather than a separate dark-blue highlight
- Frequency-based opacity (MIN_OPACITY=0.15 to 1.0) preserved unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Create arrow color utility with oklch interpolation** - `5cd7082` (feat)
2. **Task 2: Update boardArrows in Openings.tsx and Dashboard.tsx** - `efb2764` (feat)

## Files Created/Modified
- `frontend/src/lib/arrowColor.ts` - New utility: oklch color interpolation from win percentage
- `frontend/src/pages/Openings.tsx` - Import getArrowColor, simplified boardArrows useMemo
- `frontend/src/pages/Dashboard.tsx` - Import getArrowColor, simplified boardArrows useMemo

## Decisions Made
- Color anchors (`oklch(0.45 0.16 145)` green, `oklch(0.55 0.01 260)` grey, `oklch(0.45 0.17 25)` red) pulled directly from WDLBar.tsx constants to ensure visual consistency with the WDL chart
- Hover boost of +0.15 lightness (capped at 1.0) replaces the previous separate dark-blue hovered color — the arrow stays in its win-rate hue family

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Arrow coloring is self-contained; future arrow work can extend getArrowColor without touching page components
