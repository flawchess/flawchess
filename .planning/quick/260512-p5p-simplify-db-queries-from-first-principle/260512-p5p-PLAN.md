---
phase: 260512-p5p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/openings_repository.py
  - app/repositories/stats_repository.py
  - app/services/openings_service.py
  - tests/repositories/test_opening_insights_repository.py
autonomous: true
requirements: [SIMPLIFY-01, SIMPLIFY-02, SIMPLIFY-03]
must_haves:
  truths:
    - "openings_repository exposes exactly one resulting-position WDL function (the two transposition_* helpers are gone)"
    - "openings_service.get_next_moves issues one SQL round trip for resulting-position WDL, not two"
    - "stats_repository.query_position_wdl_batch compiles to a flat single-SELECT GROUP BY (no SELECT DISTINCT subquery, no FROM (SELECT ...))"
    - "stats_repository.query_opening_phase_entry_metrics_batch compiles without a CTE (no 'WITH ' in the compiled SQL)"
    - "All three refactors are behavior-preserving: identical filter parity via apply_game_filters, identical sign-flip on eval_cp, identical partition invariant eval_n + mate_n + null_eval_n + outlier_n == phase_entry_row_count"
    - "uv run ty check app/ tests/ passes with zero errors after each task"
  artifacts:
    - path: "app/repositories/openings_repository.py"
      provides: "Single query_resulting_position_wdl returning {hash: tuple(wins, draws, losses, last_played_at)} — old query_transposition_counts and query_transposition_wdl deleted"
    - path: "app/repositories/stats_repository.py"
      provides: "Flat (no subquery, no CTE) implementations of query_position_wdl_batch and query_opening_phase_entry_metrics_batch"
    - path: "app/services/openings_service.py"
      provides: "get_next_moves derives game_count from wins+draws+losses; only one resulting-position WDL call"
    - path: "tests/repositories/test_opening_insights_repository.py"
      provides: "Structural guardrail tests asserting absence of CTE/DISTINCT subquery shape in stats queries"
  key_links:
    - from: "app/services/openings_service.py:get_next_moves"
      to: "app/repositories/openings_repository.py:query_resulting_position_wdl"
      via: "single sequential await"
      pattern: "await query_resulting_position_wdl"
    - from: "app/services/opening_insights_service.py:669"
      to: "app/repositories/openings_repository.py:query_resulting_position_wdl"
      via: "unchanged sequential await (signature preserved)"
      pattern: "await query_resulting_position_wdl"
    - from: "app/services/stats_service.py"
      to: "app/repositories/stats_repository.py:query_position_wdl_batch"
      via: "unchanged sequential await (signature preserved)"
      pattern: "await query_position_wdl_batch"
---

<objective>
Simplify three overcomplicated SQL queries from first principles, mirroring PR #90 (`refactor(openings): simplify opening insights query to flat aggregation`). All three are behavior-preserving refactors landing as three atomic commits.

Purpose: Reduce surface area (delete two redundant query helpers), eliminate planner workarounds (drop a CTE and a dedup subquery layer), and remove a redundant DB round trip in `get_next_moves`. PR #90 demonstrated that flat `SELECT … GROUP BY … HAVING` consistently plans better than wrapped subqueries/CTEs on the Postgres 18 server.

Output: Three commits, ty-clean, all tests green, plus a structural guardrail test asserting the new shapes do not regress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/repositories/openings_repository.py
@app/repositories/stats_repository.py
@app/repositories/query_utils.py
@app/services/openings_service.py
@app/services/opening_insights_service.py
@app/services/stats_service.py
@tests/repositories/test_opening_insights_repository.py
@tests/services/test_opening_insights_service.py

<interfaces>
<!-- Key contracts the executor needs. The three refactors must not change these. -->

From app/repositories/openings_repository.py (current — survives Task 1):
```python
async def query_resulting_position_wdl(
    session: AsyncSession,
    user_id: int,
    result_hash_list: list[int],
    time_control: Sequence[str] | None = None,
    platform: Sequence[str] | None = None,
    rated: bool | None = None,
    opponent_type: str = "human",
    recency_cutoff: datetime.datetime | None = None,
    color: Literal["white", "black"] | None = None,
    opponent_gap_min: int | None = None,
    opponent_gap_max: int | None = None,
) -> dict[int, tuple[int, int, int, datetime.datetime | None]]:
    # Returns {result_hash: (wins, draws, losses, last_played_at)}
```

From app/repositories/openings_repository.py (current — DELETED in Task 1):
```python
async def query_transposition_counts(...) -> dict[int, int]: ...     # line 473
async def query_transposition_wdl(...) -> dict[int, tuple[int, int, int]]: ...  # line 527
```

From app/repositories/stats_repository.py (signatures preserved across Tasks 2 + 3):
```python
async def query_position_wdl_batch(...) -> dict[int, PositionWDL]: ...           # line 384
async def query_opening_phase_entry_metrics_batch(...) -> dict[int, OpeningPhaseEntryMetrics]: ...  # line 472
```

From app/repositories/query_utils.py:
```python
def apply_game_filters(stmt, time_control, platform, rated, opponent_type, recency_cutoff, *, opponent_gap_min=None, opponent_gap_max=None): ...
```

Reference (already flat, PR #90) — pattern to emulate:
```python
# app/repositories/openings_repository.py:query_opening_transitions (line 693)
# Single SELECT … JOIN … WHERE … GROUP BY … HAVING with COUNT(DISTINCT) FILTER (...)
```
</interfaces>

<call_sites>
Item 1 (Task 1):
- app/services/openings_service.py:510  → query_transposition_counts (delete this call entirely)
- app/services/openings_service.py:528  → query_transposition_wdl (replace with query_resulting_position_wdl, adapt return tuple)
- app/services/opening_insights_service.py:669 → query_resulting_position_wdl (already correct, no change)

Item 2 (Task 2):
- app/services/stats_service.py:319, 347 → query_position_wdl_batch (signature unchanged)

Item 3 (Task 3):
- app/services/stats_service.py:334, 361, 541 → query_opening_phase_entry_metrics_batch (signature unchanged)
- app/services/openings_service.py:193 → query_opening_phase_entry_metrics_batch (signature unchanged)
- app/services/opening_insights_service.py:878, 894 → query_opening_phase_entry_metrics_batch (signature unchanged)
</call_sites>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate transposition trio in openings_repository.py</name>
  <files>app/repositories/openings_repository.py, app/services/openings_service.py</files>
  <behavior>
    - After refactor, `query_resulting_position_wdl(session, user_id, result_hash_list, ...)` is the only resulting-position query in openings_repository.py. The other two helpers (`query_transposition_counts`, `query_transposition_wdl`) no longer exist as module-level symbols.
    - `get_next_moves` in openings_service.py issues one resulting-position WDL call (not two), and derives `trans_counts[result_hash] = wins + draws + losses` from the unified result.
    - For every (user, hash list, filter set) used in existing service tests, the per-move `pos_total` and `game_count` values returned to the API layer are identical to pre-refactor values.
    - Existing tests in tests/repositories/test_opening_insights_repository.py (the `test_query_resulting_position_wdl_*` family — convergence, single_order, filter_parity, color_none_passes_through, empty_list) continue to pass unchanged because `query_resulting_position_wdl` itself is not modified by this task.
    - `uv run ty check app/ tests/` returns zero errors.
  </behavior>
  <action>
    Step 1 — Delete in app/repositories/openings_repository.py: the function `query_transposition_counts` (starts line 473) and `query_transposition_wdl` (starts line 527). Keep `query_resulting_position_wdl` (line 602) unchanged. Remove any now-orphaned imports if SQLAlchemy symbols are referenced only by the deleted functions (verify with `uv run ruff check .`).

    Step 2 — In app/services/openings_service.py: remove the imports of `query_transposition_counts` (line 22) and `query_transposition_wdl` (line 23) and add `query_resulting_position_wdl` instead. Delete the entire `query_transposition_counts` await block (lines 508–522 including the `# --- Transposition counts (batch) ---` comment). Replace the `query_transposition_wdl` await block (lines 524–540) with one call to `query_resulting_position_wdl` using the same kwargs (the signature already matches). The returned dict shape is now `{hash: (wins, draws, losses, last_played_at)}` instead of `{hash: (wins, draws, losses)}`. In the per-move loop (around line 552–567), change `pos = pos_wdl.get(row.result_hash)` unpacking from `w, d, lo = pos` to `w, d, lo, _last = pos` (the `last_played_at` is not needed by get_next_moves — Move Explorer surfaces last_played at a higher level). Derive `trans_counts` inline if it is still referenced downstream by the move-building code: search the function body for `trans_counts` usage and replace each lookup `trans_counts[row.result_hash]` with `(w + d + lo)` computed from the already-unpacked tuple, OR build a dict comprehension `trans_counts = {h: w + d + lo for h, (w, d, lo, _) in pos_wdl.items()}` if that is cleaner. Update the fallback `logger.warning` message and inline comments that mention `query_transposition_wdl` / `query_transposition_counts` to reference `query_resulting_position_wdl`.

    Step 3 — Verify no other module imports the deleted functions: `grep -rn "query_transposition_counts\|query_transposition_wdl" app/ tests/` must return zero hits after the edit.

    Step 4 — Run the verify suite. Commit with message `refactor(openings): consolidate transposition WDL queries into single function`.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest tests/repositories/test_opening_insights_repository.py tests/services/test_opening_insights_service.py tests/services/test_opening_insights_arrow_consistency.py -x</automated>
  </verify>
  <done>
    `grep -rn "query_transposition_counts\|query_transposition_wdl" app/ tests/` returns no matches. `query_resulting_position_wdl` is the only resulting-position WDL helper exported from openings_repository.py. `get_next_moves` issues exactly one resulting-position WDL await (verify with `grep -n "query_resulting_position_wdl\|query_transposition" app/services/openings_service.py`). ty + ruff + the listed pytest subset all pass. Commit landed on the working branch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flatten query_position_wdl_batch in stats_repository.py</name>
  <files>app/repositories/stats_repository.py, tests/repositories/test_opening_insights_repository.py</files>
  <behavior>
    - `query_position_wdl_batch` compiles to a single `SELECT full_hash, COUNT(DISTINCT game_id) FILTER (...) AS wins, ..., MAX(played_at) AS last_played_at FROM game_positions JOIN games ON ... WHERE user_id=? AND full_hash IN (...) GROUP BY full_hash` — no wrapping subquery, no `SELECT DISTINCT` in the compiled SQL.
    - Return shape unchanged: `dict[int, PositionWDL]` where each `PositionWDL` carries `total`, `wins`, `draws`, `losses`, `last_played_at`. `total` is `wins + draws + losses` (i.e. `COUNT(DISTINCT game_id)` filtered by any of the three result conditions) — equivalent to the previous count-of-distinct-games since every game has exactly one result.
    - For every filter combination exercised by existing stats_service tests, the returned counts for each hash are identical to pre-refactor values.
    - A new structural guardrail test asserts the compiled SQL contains neither `SELECT DISTINCT` nor a `FROM (SELECT ...)` subquery shape. Pattern matches the existing `test_query_compiles_to_flat_aggregation` test in test_opening_insights_repository.py (line 1208).
  </behavior>
  <action>
    Step 1 — Rewrite `query_position_wdl_batch` (app/repositories/stats_repository.py:384) as a flat aggregation. The new shape:
    - Start with `select(GamePosition.full_hash, ...).join(Game, GamePosition.game_id == Game.id).where(GamePosition.user_id == user_id, GamePosition.full_hash.in_(hashes))`.
    - Apply `if color is not None: stmt = stmt.where(Game.user_color == color)` before `apply_game_filters`.
    - Apply `apply_game_filters(stmt, time_control, platform, rated, opponent_type, recency_cutoff, opponent_gap_min=..., opponent_gap_max=...)` exactly as before — filter parity is mandatory (CLAUDE.md shared filters rule).
    - Aggregates: `wins = func.count(func.distinct(Game.id)).filter(or_(and_(Game.result == "1-0", Game.user_color == "white"), and_(Game.result == "0-1", Game.user_color == "black")))`, mirror for draws (`Game.result == "1/2-1/2"`) and losses. `total = wins + draws + losses` computed in Python after fetch (avoids a 4th COUNT DISTINCT). `last_played_at = func.max(Game.played_at)`. Match labels used in PR #90's `query_opening_transitions` for consistency.
    - `.group_by(GamePosition.full_hash)`.
    - Remove the entire `dedup = select(...).distinct(...).subquery()` scaffolding (lines 411–438) and the outer `select(dedup.c.full_hash, ...)` (lines 440–461).
    - Keep the existing `PositionWDL` construction in the return comprehension; just adapt to read `wins, draws, losses` from the row and compute `total = wins + draws + losses`.
    - Delete or update the docstring comment about "DISTINCT game_id per hash" — replace with a sentence explaining that `COUNT(DISTINCT game_id) FILTER (...)` handles dedup at aggregate level, mirroring PR #90.

    Step 2 — Add structural guardrail test in tests/repositories/test_opening_insights_repository.py. Place it next to `test_query_compiles_to_flat_aggregation` (around line 1208). Name it `test_query_position_wdl_batch_compiles_flat`. It builds a representative statement using the same SQLAlchemy 2.x pattern as the new implementation, compiles it via `str(stmt.compile(dialect=postgresql.dialect()))`, and asserts `"WITH " not in compiled` and `"SELECT DISTINCT" not in compiled.upper()`. Do not import private helpers from stats_repository; reconstruct the representative statement in the test as PR #90's guardrail does (this isolates the test from helper refactors).

    Step 3 — Commit with message `refactor(stats): flatten query_position_wdl_batch to single-SELECT aggregation`.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest tests/repositories/test_opening_insights_repository.py tests/services/test_stats_service_phase_entry.py tests/services/test_insights_service.py tests/services/test_insights_service_series.py tests/services/test_opening_insights_service.py -x</automated>
  </verify>
  <done>
    `grep -n "SELECT DISTINCT\|\.subquery()\|\.distinct(" app/repositories/stats_repository.py` shows no occurrences inside `query_position_wdl_batch` (lines may still appear in `query_opening_phase_entry_metrics_batch` until Task 3). The new guardrail test passes. All affected service tests pass. ty + ruff clean. Commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flatten query_opening_phase_entry_metrics_batch by removing the CTE</name>
  <files>app/repositories/stats_repository.py, tests/repositories/test_opening_insights_repository.py</files>
  <behavior>
    - `query_opening_phase_entry_metrics_batch` compiles without a CTE (`"WITH " not in compiled_sql`). The DISTINCT-ON phase-entry derivation moves into a plain subquery, not `.cte("dedup")`.
    - Return shape unchanged: `dict[int, OpeningPhaseEntryMetrics]` with `eval_sum_mg`, `eval_sumsq_mg`, `eval_n_mg`, `mate_n_mg`, `null_eval_n_mg`, `outlier_n_mg`.
    - Partition invariant preserved: `eval_n_mg + mate_n_mg + null_eval_n_mg + outlier_n_mg == phase_entry_row_count` per hash, for every hash in the result. This invariant is load-bearing and must be tested with a numeric assertion against existing fixture data.
    - Sign convention preserved: `eval_cp` signed via `case((Game.user_color == "white", 1), else_=-1) * phase_entry.eval_cp`.
    - Outlier threshold preserved: `EVAL_OUTLIER_TRIM_CP = 2000` (module-level constant — do NOT inline as a magic number).
    - Filter parity preserved: `apply_game_filters` invoked with the same kwargs.
    - `hash_column: Literal["full", "white", "black"]` switch behavior preserved.
    - All existing tests covering this function pass without modification.
  </behavior>
  <action>
    Approach (Option A from task_scope): Replace the CTE with two plain subqueries joined into a single flat `GROUP BY full_hash` aggregation. The structure mirrors PR #90: subqueries are fine, materialized CTEs are not.

    Step 1 — Rewrite `query_opening_phase_entry_metrics_batch` (app/repositories/stats_repository.py:472):
    - Keep the `hash_col_map` resolution (lines 513–518) unchanged.
    - Replace `dedup_select.cte("dedup")` with `.subquery("dedup")`. This single change is the structural fix: PostgreSQL's planner inlines subqueries when beneficial; explicit CTEs are an optimization fence the planner respects literally. Verify the resulting compiled SQL no longer contains `WITH ` via the new guardrail test (Step 3).
    - Keep the `phase_entry_subq` DISTINCT-ON derivation (lines 557–569) as a `.subquery("phase_entry")` — it is already a subquery, not a CTE. Its `WHERE game_id IN (SELECT dedup.game_id FROM dedup)` reference must now point at the `.subquery("dedup")` form; the SQLAlchemy expression `select(dedup_subq.c.game_id)` continues to work identically since `.subquery()` and `.cte()` expose the same `.c` interface.
    - Keep all six aggregates, the sign expression, and the four predicates (`has_continuous_in_domain_eval`, `has_mate`, `has_null_eval`, `has_outlier_eval`) exactly as-is — they are the load-bearing partition definition.
    - Keep `apply_game_filters(...)` call on `dedup_select` unchanged. Filter parity must not regress.
    - Update the docstring comments around lines 520–523 and 551–556 to remove the obsolete "materialized as a CTE" rationale; replace with a one-line note that the dedup layer is a plain subquery so the planner can inline it.

    Step 2 — Add a parity assertion to an existing DB-backed test (or add a new one in tests/repositories/test_opening_insights_repository.py near the existing flat-shape guardrails). The test seeds at least 2 hashes × 3+ games with mixed eval states (continuous, mate, null, outlier) and asserts both:
    (a) the returned metrics match hand-computed expected values for each hash, AND
    (b) the partition invariant `eval_n_mg + mate_n_mg + null_eval_n_mg + outlier_n_mg == phase_entry_row_count` holds for each hash.
    If a similar fixture already exists in tests/services/test_stats_service_phase_entry.py or tests/services/test_opening_insights_service.py, augment it with the invariant assertion rather than reseeding from scratch.

    Step 3 — Add a structural guardrail test `test_query_phase_entry_metrics_compiles_without_cte` in tests/repositories/test_opening_insights_repository.py next to the existing flat-shape tests. Reconstruct a representative statement using the same `.subquery("dedup")` + `.subquery("phase_entry")` shape as the implementation. Compile via `str(stmt.compile(dialect=postgresql.dialect()))` and assert `"WITH " not in compiled`. Follow the exact pattern of `test_query_compiles_to_flat_aggregation` (line 1208).

    Step 4 — Sanity check filter parity: run the existing stats_service tests, which cover both color filters and hash_column variants, and confirm zero regressions.

    Step 5 — Commit with message `refactor(stats): drop CTE from query_opening_phase_entry_metrics_batch`.

    Step 6 — In the eventual SUMMARY.md (separate artifact, follow execute-plan template), record the post-deploy step: "Tunnel into prod with `bin/prod_db_tunnel.sh` and EXPLAIN ANALYZE `query_opening_phase_entry_metrics_batch` for user 84 on a representative hash batch; confirm the plan stays in the same zone as `query_opening_transitions` after PR #90."
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest tests/repositories/test_opening_insights_repository.py tests/services/test_stats_service_phase_entry.py tests/services/test_opening_insights_service.py tests/services/test_insights_service.py tests/services/test_insights_service_series.py -x</automated>
  </verify>
  <done>
    `grep -n "\.cte(" app/repositories/stats_repository.py` returns no matches inside `query_opening_phase_entry_metrics_batch`. The new `test_query_phase_entry_metrics_compiles_without_cte` guardrail passes. Partition invariant assertion passes against fixture data. ty + ruff clean. All listed pytest suites green. Commit landed. SUMMARY.md records the prod EXPLAIN ANALYZE follow-up.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full suite once:

```bash
uv run ruff check . && uv run ruff format --check . && uv run ty check app/ tests/ && uv run pytest -x
```

All green. `grep -rn "query_transposition_counts\|query_transposition_wdl" app/ tests/` returns zero hits. `grep -n "\.cte(" app/repositories/stats_repository.py` returns zero hits.
</verification>

<success_criteria>
- Three atomic commits land: `refactor(openings): consolidate transposition WDL queries into single function`, `refactor(stats): flatten query_position_wdl_batch to single-SELECT aggregation`, `refactor(stats): drop CTE from query_opening_phase_entry_metrics_batch`.
- Two helper functions removed (`query_transposition_counts`, `query_transposition_wdl`).
- One redundant DB round trip eliminated from `get_next_moves`.
- Two structural guardrail tests added asserting flat shape (no `SELECT DISTINCT` subquery, no `WITH ` CTE).
- One partition-invariant test added (or augmented) for phase-entry metrics.
- ty, ruff, and the full pytest suite pass.
- No call-site signature changes; no schema changes; no behavior changes observable at the API layer.
- SUMMARY.md (created at end via execute-plan template) records the prod EXPLAIN ANALYZE follow-up for `query_opening_phase_entry_metrics_batch` for user 84.
</success_criteria>

<output>
After completion, create `.planning/quick/260512-p5p-simplify-db-queries-from-first-principle/260512-p5p-SUMMARY.md` summarizing the three commits, the removed surface area, the guardrail tests added, and the prod EXPLAIN ANALYZE follow-up.
</output>
