---
id: SEED-009
status: closed
planted: 2026-04-30
closed: 2026-05-01
planted_during: post-v1.14, after first benchmark DB populated and `/benchmarks` v1 report produced
closed_during: PR #77 (quick-260501-s0u "benchmark calibration v2") merged
scope: phase (or merged into SEED-006 Phase 73)
---

## Closure Note (2026-05-01)

Closed deliberately. PR #77 shipped item #2 (per-class conv/recov bands) verbatim and a partial of #3 (clock band tightened pooled, not per-TC). Items #1, #3-per-TC, and #4 were dropped, and the newer `reports/benchmarks-2026-05-01.md` (1,912 users, up from 991) justifies that decision:

- **#1 (Endgame Skill 0.55 → 0.57): invalidated.** New pooled p75 = 0.559 (not 0.5715). Report recommendation table marks current `[0.45, 0.55]` as "keep acceptable"; the global alternative is `[0.46, 0.56]`, not 0.57. The original recommendation was a small-sample artifact.
- **#3 per-TC clock thresholds: invalidated.** Cohen's d verdict for clock-diff %: TC collapse (d=0.16) / ELO collapse (d=0.12). User-level distributions across TCs collapse — the per-TC plumbing was based on raw event-rate differences, which are not the right level. Single global threshold (current ±5; ±6 would be more literal) is what the report endorses.
- **#3 per-TC timeout thresholds: wrong axis.** Verdict: TC collapse (d=0.04) / ELO keep separate (d=0.60). Real stratification axis is ELO, not TC. Belongs to SEED-006 Phase 73 (rating-bucketed zones), not here.
- **#4 time-pressure-vs-performance per-TC: wrong axis.** Verdict: TC review (d=0.31) / ELO review (d=0.49). Report recommends per-ELO overlay for the 80-100% time-bucket region, not per-TC. Also belongs to SEED-006 Phase 73.

Net: the seed's "ship now without waiting for SEED-006" framing held for the per-class bands, but the per-TC items it called for were the wrong axis. The right axis (per-ELO) is exactly what SEED-006 Phase 73 will handle via the Cohen's d collapse pipeline. No remaining work belongs in a SEED-009-shaped follow-up.

Minor drift left in code: `NEUTRAL_PCT_THRESHOLD = 5.0` is 1pp tighter than the report's literal recommendation (±6.0). Defensible round number; flag if a follow-up calibration PR happens.

---

# SEED-009: Ship-now endgame gauge calibration (pre-SEED-006 cleanup)

## Why This Matters

The 2026-04-29 benchmark report (`reports/benchmarks-2026-04-29.md`) surfaced four calibration findings that do **not** depend on Stockfish-eval validation, rating-bucketed zones, or the skill v2 collapse verdict. They can ship as a quick UI/constants PR without waiting for the full SEED-006 milestone (which needs the classifier-validation gate, rating-stratified offset analysis, and Parity validation — weeks of work).

Splitting these out gives users visible calibration improvements within days while SEED-006 proceeds independently.

## When to Surface

**Trigger options:**
- During `/gsd-add-phase` when user signals readiness to land calibration cleanup ahead of SEED-006.
- During `/gsd-discuss-phase` for SEED-006 Phase 73 if the user prefers folding everything into one coordinated PR.

Do NOT surface mid-milestone unrelated to endgame analytics. The cleanup is small enough to wait.

## What's In Scope

### 1. Endgame Skill upper bound widening

- File: `frontend/src/components/charts/` (consult `ENDGAME_SKILL_ZONES` constant) and the autogenerated `frontend/src/generated/endgameZones.ts` pipeline (`scripts/gen_endgame_zones_ts.py`).
- Change: upper neutral bound `0.55` → `0.57`.
- Rationale: pooled population p75 = 0.5715. Current band labels the upper quartile of users as "success" — too generous. New bound aligns "success" with the top quartile.
- One-line constant edit if generated, plus regen.

### 2. Per-endgame-class conv/recov neutral zones

- File: `frontend/src/components/charts/EndgameConvRecovChart.tsx` (no per-class zones today).
- Add per-class neutral bands derived from pooled per-class rates ± 5pp (Section 6 of the report has the candidate values):
  - rook: conv `[0.62, 0.72]`, recov `[0.28, 0.38]`
  - minor_piece: conv `[0.60, 0.70]`, recov `[0.32, 0.42]`
  - pawn: conv `[0.64, 0.74]`, recov `[0.27, 0.37]`
  - queen: conv `[0.70, 0.80]`, recov `[0.21, 0.31]`
  - mixed: conv `[0.63, 0.73]`, recov `[0.29, 0.39]`
  - pawnless: conv `[0.69, 0.79]`, recov `[0.22, 0.32]`
- Constants must flow through `app/services/endgame_zones.py` registry → codegen → TS, NOT hard-coded in the React component.
- Pooled bands wouldn't fit (9–10pp spread between classes); per-class is required.

### 3. Clock-pressure per-TC thresholds

- File: `frontend/src/components/charts/EndgameClockPressureSection.tsx` + `frontend/src/generated/endgameZones.ts`.
- Change `NEUTRAL_PCT_THRESHOLD` from a single ±10pp to per-TC: bullet ±4pp, blitz ±6pp, rapid ±8pp, classical ±12pp. (Or accept narrower-but-still-pooled ±7pp as a compromise; per-TC is more honest.)
- Change `NEUTRAL_TIMEOUT_THRESHOLD` from a single ±5pp to per-TC: bullet ±10pp, blitz ±7pp, rapid ±3pp, classical ±1pp. **Single threshold cannot fit** — bullet's p75 is +13.7pp net timeout, classical's is +0.9pp. Current ±5 is wrong by 2× in opposite directions.
- Larger UI plumbing change than #1/#2 — need TC-aware lookup at gauge render time. Worth its own discuss-phase.

### 4. Time-pressure-vs-performance per-TC display

- File: `app/services/endgame_service.py` (`_compute_time_pressure_chart` currently pools all TCs into a single user_series + opp_series).
- Backend change: emit per-TC series (4 user_series, 4 opp_series) or split into chart variants.
- Frontend change: `EndgameTimePressureSection.tsx` consumes per-TC data, displays as overlay or per-TC chart.
- Rationale: pooled chart hides a 13pp gap at extreme time pressure (bucket 0: bullet 0.252 vs classical 0.380). Pooling smooths exactly the buckets users care about.
- Alternative if UI complexity is a concern: keep pooled view as default, add per-TC overlay behind a "details" disclosure.

## Out of Scope

- Rating-bucketed zones (SEED-006 Phase 73).
- Score-gap gauge re-centering — explicitly rejected (sub-3pp asymmetry not worth losing round bounds).
- Anything requiring Stockfish-eval validation (SEED-006 Phases 70/71/72).
- Anything requiring the skill v2 Cohen's d collapse verdict (Phase 73 prerequisite, not this seed).

## Phase Decomposition (if shipped standalone)

**Single phase, ~3-5 days work:**
1. Items #1 and #2 are constants edits + codegen. Land first as a small PR.
2. Items #3 and #4 are larger (per-TC plumbing). Could be one phase together or split if discuss reveals scope creep.

If folded into SEED-006 Phase 73, all four items land alongside the rating-bucketed zone work in the same coordinated PR.

## Breadcrumbs

- `reports/benchmarks-2026-04-29.md` — Sections 1, 2, 4, 5, 6 contain the source recommendations.
- `frontend/src/lib/theme.ts`, `frontend/src/generated/endgameZones.ts` — current constant locations.
- `app/services/endgame_zones.py` + `scripts/gen_endgame_zones_ts.py` — Python→TS codegen pipeline; new constants must flow through here, not be hard-coded in React.
- `.planning/notes/benchmark-skill-v2-design.md` — methodology for the rating-bucketed work that this seed deliberately defers.
