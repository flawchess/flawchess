# Benchmarks diff — 2026-05-17 (snapshot bucketing) → 2026-05-19 (game-time bucketing)

Only **actionable** differences. The 2026-05-17 report used `benchmark_selected_users.rating_bucket` (selection-snapshot ELO); 2026-05-19 uses rating-at-game-time. Absolute ELO-axis numbers are not directly comparable; the items below are where the verdict or recommended action *changed in a way that should drive code/decisions*.

## 1. Methodology / DB state (context — not a code change)

- **Rating-lag fix landed.** Both acceptance tests pass: cohort score flat 0.502–0.505 across 800–1600 (was a 0.496→0.506 snapshot ramp); per-color MG eval near-mirror at 1200 (W+25.7/B−20.1 vs old +33/−16). The 2000/2400 residual is the known out-of-scope cheat/rating-lag-tail confound — do not chase.
- **Checkpoint join restored** (all 5 rating buckets have completed checkpoints; the 2026-05-19 *partial* report's no-checkpoint exception is rescinded, archived at `benchmarks-2026-05-19-partial.md`).

## 2. Verdict changes that retire deferred work (no code, but unblocks decisions)

- **3.1.4 Endgame Score ELO: keep (0.84) → review (0.36).** The "per-ELO `ENDGAME_SCORE_ZONES` registry deferred" item is now **moot** — the snapshot ELO ramp was a rating-lag artifact. Single global band stands; close that backlog item.
- **3.1.5 Achievable Score Gap ELO: keep (0.62) → review (0.32).** Same — per-ELO stratification deferral is moot.
- **3.1.3 Achievable Score ELO: review (0.23) → collapse (0.11).** `(0.45,0.55)` now firmly confirmed.
- **§3.3.3 pressure bins: the blocking decision is largely resolved.** 2026-05-17 had *all five quintiles* "ELO does NOT collapse → blocking decision required". Under game-time bucketing only **Q0 keeps separate (ELO d=0.59)**; Q1–Q4 drop to review. Action: ship the scalar `(tc,quintile)` ELO-pooled `PRESSURE_BIN_SCORE_NEUTRAL_ZONES` (17/20 cells already ±0.06-capped — effectively unchanged from live); keep **Q0** as the single standing per-(TC×ELO×quintile) promotion candidate instead of all five.
- **3.2.1 / 3.2.2 ELO effects weakened** (Conversion ELO 0.82→0.50, Endgame Skill 0.78→0.42, ΔES-Parity 0.57→0.31, ΔES-Skill 0.81→0.42). Conv/Recov **TC** keep-separate persists (TC d≈0.87/0.91) — the TC-stratification need for Conv/Recov is unchanged and real.

## 3. Recommended code-constant changes that are NEW or REVERSED vs 2026-05-17

| # | Constant | 2026-05-17 said | 2026-05-19 says | Action |
|---|---|---|---|---|
| A | `ENDGAME_ENTRY_EVAL_NEUTRAL_*_PAWNS` (3.1.2) | keep ±0.75 | **tighten to ±0.60** | Editorial-tighten inside the IQR so the 0-centered EG-entry tile actually paints (live=0.75). Regen `endgameZones.ts`. |
| B | `SCORE_GAP_DOMAIN` (3.1.6) | keep 0.20 | **widen to 0.22** | p05 tail −22.1pp is clipped at the ±20pp axis edge. `EndgameOverallShared.ts`. |
| C | 3.1.6 timeline overlay | (not raised) | **unify eg/non-eg into one band `[46,56]%`** | eg/non-eg percentile bands overlap >50% pooled and per-cell — ship a single timeline neutral band, not split overlays. |
| D | `PER_CLASS_GAUGE_ZONES.minor_piece.recovery` (3.4.1) | (not flagged) | **`(0.31,0.41)→(0.28,0.38)`** | Pooled recovery 32.7% drifts −3.3pp from live midpoint. `endgame_zones.py` + regen TS. |
| E | `PER_CLASS_GAUGE_ZONES.rook.achievable_score_gap` (3.4.2) | keep `(−0.05,+0.04)` | **`(−0.05,+0.05)`** | Upper edge drift +0.6pp > 0.5pp. Regen TS. |
| F | `PER_CLASS_GAUGE_ZONES.queen.achievable_score_gap` (3.4.2) | keep `(−0.05,+0.05)` | **`(−0.04,+0.05)`** | Lower edge drift +0.8pp > 0.5pp. Regen TS. |
| G | `EndgameTypeCard.tsx` chart inventory (3.4.3) | **"Drop WDL bar"** (mode verdict) | **keep all three (Score + Score Gap + WDL)** | **Reversed.** Pooled Pearson r dropped 0.50; Score & Score Gap are correlated-but-not-redundant (queen nearly orthogonal, r=0.22), and 24–80% of score-qualifiers lack a gap bullet. Do **not** remove the WDL bar. |

## 4. Already-implemented since 2026-05-17 (confirm only, no action)

- `clock_gap_pct` ZoneSpec: 2026-05-17 recommended updating the placeholder `(-0.05,0.05)` → `(-0.065,0.047)`. That change is **live now**; the game-time pooled IQR `(-0.067,+0.048)` round-matches it → confirmed, no further change.
- `PER_CLASS_GAUGE_ZONES.pawnless.*`: 2026-05-17 suggested a re-center; pawnless is `HIDDEN_ENDGAME_CLASSES` — drift remains informational only, defer (unchanged stance).

## 5. Unchanged and confirmed (no action)

`EVAL_BASELINE_PAWNS_*` (±0.25), `EVAL_BULLET_DOMAIN_PAWNS` (1.5), `entry_expected_score` (0.45,0.55), `achievable_score_gap` (±0.05), `score_gap` neutral (±0.10), `FIXED_GAUGE_ZONES` conv/parity/recov pooled bands, `NEUTRAL_PCT_THRESHOLD`/`NEUTRAL_TIMEOUT_THRESHOLD` (±5.0), all three `section2_score_gap_*` bands, minor_piece/pawn/mixed `.achievable_score_gap`, global `endgame_type_achievable_score_gap` — all within tolerance under game-time bucketing (within-user-difference families are rating-lag-immune by construction, so 3.2.2 / 3.4.2 bands held).

## Net actionable worklist

1. **`app/services/endgame_zones.py`**: tighten `ENDGAME_ENTRY_EVAL_NEUTRAL_*` ±0.75→±0.60 (A); `minor_piece.recovery` →(0.28,0.38) (D); `rook.achievable_score_gap` →(−0.05,+0.05) (E); `queen.achievable_score_gap` →(−0.04,+0.05) (F); then `uv run python scripts/gen_endgame_zones_ts.py` + commit regenerated `endgameZones.ts` (CI drift gate).
2. **Frontend**: `SCORE_GAP_DOMAIN` 0.20→0.22 (B); unify 3.1.6 timeline to one `[46,56]%` band (C); introduce `PER_CLASS_SCORE_BULLET_ZONES` (3.4.1, global `[0.45,0.55]` is 2–4× too tight per class).
3. **Decision reversal**: do NOT drop the WDL bar from `EndgameTypeCard.tsx` (G) — supersedes the 2026-05-17 layout recommendation.
4. **Close as moot**: per-ELO stratification deferrals for 3.1.4 / 3.1.5, and the §3.3.3 5-quintile blocking decision (now Q0-only).
