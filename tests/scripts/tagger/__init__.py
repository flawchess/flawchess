# Tagger validation harness package.
# Tests in this package are excluded from the default pytest run (pyproject.toml addopts)
# and run on demand and in CI via an explicit path: uv run pytest tests/scripts/tagger -v
